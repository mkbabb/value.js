# X.F.W1 — The Atomic Tri-Package Uplift

## State

**Name**: F.W1 — glass-ui 4→7(→8, G1-gated) ∧ keyframes 4.3→6 ∧ value.js 0.13→4.0, ONE land-or-lose transaction, + the `lucide-vue-next`→`@lucide/vue` rename + the pencil-boil floor correction
**Sub-tranche**: X·F (fourier-analysis) · **Surface**: fourier-analysis `web/` only (the entire product surface of `carry/F-W1-CARRY.md`)
**Opens after**: F.W0 closes (G2 · G3 · MISS-LC2 · F8-REACH-01+02) **and** the owner's begin-word **and** ESC-1 ruled (G1)
**Status**: `planned`
**Law**: M-25 (agglomerate — dedupe by identity, carry cure-shape locks and sequencing edicts, route by mechanism; transcription-only is DEFECTIVE) · L-20 (validated against `docs/precepts/instructions/tranche/WAVE_SPEC.md`, latest at authoring 2026-08-28) · L-19 (every gate names a real witness) · Born-RED (every gate opens RED, witness named) · E-3 (dated evidence never rewritten; corrections travel as new rows) · **L-20 REPAIR ROUND 1 (2026-08-28)**: `conformance/PASS-1/RULINGS.md` applied verbatim wherever it addresses F.W1 (R-2a/R-2c · R-4 sibling · R-9.1 · R-10.1, and R-3's path-vocabulary + R-8's canonical citation form where this spec states the gate); `conformance/PASS-1/F-W1-CHECK.md` cured worst-first; every correction travels as a **§2·R1** or **§E-3·R1** row, never as a silent patch of a dated cell. Where check and ruling collide, the ruling governs. · **L-20 REPAIR ROUND 2 (2026-08-28)**: `conformance/PASS-2/RULINGS-2.md` applied verbatim wherever it addresses F.W1 (**R2-5** the roster count · **R2-3d.1–3** the three ruled adoptions · **R2-2** the stable-anchor idiom · **R2-1-LAW** quote-by-command); `conformance/PASS-2/F-W1-CHECK.md` cured worst-first; round-2 corrections travel as **§2·R2** / **§E-3·R2** rows. **The round was written in two sittings and the second AUDITED THE FIRST against its own two laws** — the residue it found is minuted at §E-3·R2's last six rows (a surviving round-1 line-cite into the CARRY; a banked LOCK carried tightened; five unreceipted "verbatim" labels and one fused composite; three count words contradicting the commands beside them; a ⟨cmd⟩ that could not run as printed and broke G1's table). None of those were in the check; a repair that is not itself re-read is a draft. **Two standing laws bind every citation written from this round forward: (i) QUOTE-BY-COMMAND (R2-1-LAW) — a cross-wave or corpus quotation enters ONLY as pasted `grep`/`sed` output from a command run this round, stated beside it as a ⟨cmd⟩ note, and a "verbatim" label without a ⟨cmd⟩ note is a defect per se; (ii) STABLE ANCHORS (R2-2) — cross-spec receipts cite §-headings, gate ids, clause ids and row labels, NEVER line numbers; line numbers survive only for the frozen `fr-*` corpus and for this file's own coordinates marked "at this writing".**
**Execution gate**: this file SPECIFIES. `/Users/mkbabb/Programming/fourier-analysis` is READ-ONLY until the owner's begin-word. Status stays `planned`; no product source opens.

⟨provenance: `docs/tranches/X/fourier/carry/F-W1-CARRY.md` 105 rows, consumed WHOLE (the ONLY carry this spec cites, by path — with `carry/F-W4-CARRY.md` the only two that exist, R-3) · adjudicated `fr-*` 66/66 @ `35fc8ebf` (the REGISTRY direction re-opened at §2·R1) · `docs/tranches/X/fourier/conformance/PASS-1/RULINGS.md` + `…/F-W1-CHECK.md` · `…/PASS-2/RULINGS-2.md` + `…/PASS-2/F-W1-CHECK.md` · `formation/fourier/CENSUS-2026-08-03.md` **§4 item 2** (§4 is `## §4 — Candidate wave-shape sketch`, a numbered list — there is no "§4.2"; the ruled-against spelling R-1f cured for F.W0 is cured here, PASS-2 D-6) + `lane-frontend.md` · `docs/tranches/X/COHESION.md` §0a/§2/§3.1⟩

**Four-verb status** (each field independent — this file advances exactly one):

| verb | state | basis |
|---|---|---|
| AUDITED | YES | 66/66 fr-* adjudicated @ `35fc8ebf` |
| **SPECIFIED** | **YES — by this file** | `carry/F-W1-CARRY.md` 105 rows → §2/§5/§2·R1; closure CLAIMED at §6/§6·R1 and TESTED at G20 (never at authoring) |
| IMPLEMENTED | NO | stamps at G1–G20 green + close report |
| VERIFIED | NO | stamps at X·F sub-tranche close, never here |

---

## §1 Bounds

**Docs surface (owned at authoring):**
- `/Users/mkbabb/Programming/value.js/docs/tranches/X/fourier/waves/F-W1.md` (this spec)
- `/Users/mkbabb/Programming/value.js/docs/tranches/X/fourier/carry/F-W1-CARRY.md` (read-consumed; never rewritten — E-3)
- `/Users/mkbabb/Programming/value.js/docs/tranches/X/coordination/` — the SS-6 BH letter (NWO-1) + LATEX-RELAY (NWO-5) + pencil-boil letter (NWO-6)
- `/Users/mkbabb/Programming/value.js/docs/tranches/V/coordination/INBOX.md` — append-only, one E13 ledger row per relay

**Product surface (fourier-analysis, AT EXECUTION only — every path below RE-RESOLVED against the live tree by read-only `ls` this session (R-10.1), superseding the `carry/F-W1-CARRY.md` §Bounds transcription that carried eight non-existent spellings; the CARRY is untouched — E-3 corrections travel as rows, here and at §E-3·R1):**
- `web/package.json` · `web/package-lock.json` · `web/tsconfig.json` · `web/vite.config.ts` · `web/env.d.ts` (**not** `src/env.d.ts` — PP-REDGATE's ambient declaration targets this file)
- `web/src/style.css` (`.paper-texture` · the `.cartoon-card` shim `:98-112` · the root-font fork **`:40-50`** — the `@media (min-width: 768px)` arm closes at `:50`, not `:49`; corrected at §E-3·R2, PASS-2 D-7(ii))
- `web/src/lib/colors.ts` · `web/src/components/visualization/lib/basis-display.ts` · `web/src/main.ts` · `web/src/App.vue`
- `web/src/lib/easings.ts` · `web/src/composables/useToast.ts` · `web/src/composables/useMorphConfig.ts`
- the **35 Button-importing files** (87 `variant=` / 36 `size="icon"` rewrite — enumerated by G7's grep of record, never by any banked figure)
- the **35 lucide import sites** + **17 hand-inlined `<svg>`s across 12 `.vue` files**
- `web/src/components/ui/CollapsibleSection.vue` · `web/src/components/visualization/ContourSettings.vue` (twin animation blocks — G10)
- `web/src/components/shared/CoefficientsSpectrum.vue` (animated-digit — G11's witness; **`components/visualization/` was the phantom spelling**)
- `web/src/components/equation/EquationResult.vue` · `web/src/components/visualization/gallery/UserSlugBar.vue` (the `copied` triple — G6's witness; **no `components/gallery/` directory exists, and none ever did**)
- metric-badge consumers (6 live + InfoCard unmountable), all re-resolved: `equation/EquationView.vue` · `equation/InfoCard.vue` · `visualization/AnimationControls.vue` · `visualization/EditorControlsDock.vue` · `visualization/EquationPanel.vue` · `visualization/gallery/GalleryAdminBanner.vue` · `visualization/gallery/GalleryDraftsSection.vue` (all under `web/src/components/`)
- `web/src/components/visualization/CanvasControlsDock.vue` · `web/src/components/visualization/EditorControlsDock.vue` (hover-popover · DockIconButton ×7/×12 · MetricBadge; **no `components/dock/` directory exists**)
- `web/src/components/layout/AppHeader.vue` (`./hover-card` · the easings fork `:21-27` **and** `:29` — both lines, or the eager `vendor-math` edge survives)
- `web/src/components/visualization/AnimationControls.vue` (`./dropdown-menu`→`./menu` iff G1 adopts 8; **no `components/animation/` directory exists**)
- the **7 files carrying the 23 `--slider-scrub-*` declarations**, re-resolved under `web/src/components/`: `visualization/BasisSelector.vue` · `visualization/EditorControlsDock.vue` · `ui/SliderControl.vue` · `morph/HarmonicLevelGrid.vue` · `morph/MorphPhaseConfig.vue` · `visualization/GlassTimeline.vue` · `equation/convergence/ConvergenceTimeline.vue` — G9 gate only; execution F.W3/W4
- `web/src/components/paper/**` **only if** G16 seats the LATEX-PAPER carry in this wave

**Read-only witnesses (never edited):** `web/e2e/visualization-crud.spec.ts` · `workspace-flow.spec.ts` · `contour-extraction.spec.ts` · `gallery.spec.ts` · `scripts/e2e.sh` · `web/dist/**` (stale, inadmissible) · `/Users/mkbabb/Programming/glass-ui` · `keyframes.js` · `latex-paper` · `pencil-boil`.

**Do NOT touch:** any F.W3/W4/W5–W8 repair surface · `scripts/dev/dev.sh` (unowned, standing owner order) · any dated registry record.

---

## §2 Carry

Routed by mechanism into work units WU-A..WU-S. Every banked id lands exactly once as an identity; instance witnesses stay folded in their identity cell (anti-rename law). Closure proven at §6.

### WU-A · RE-PIN (first obligation — nothing below sizes before it)
| id | disposition |
|---|---|
| **M-16 / FR-EQC-16 / K-17** | The corpus is pinned to a producer that moved 5× mid-audit (v7.0.0-322 → … → `407de2d3`; v7.0.0 AND v8.0.0 both tagged; 3 verified tag↔HEAD inversions). **F.W1 names its target TAG + ADOPTED COMMIT HASH in this spec's G1 cell before any budget executes; every producer `file:line` in all 66 records re-resolves at that hash.** OWNER-GATED (ESC-1). BASELINE LAW: the adopted TAG decides regardless of what a working tree self-reports. **PIN-LAW GENERALISED (banked `fr-ContourPreview` row 29, spec spelling `CP-ROW-29`, §2·R2a.2): the obligation is not this corpus's alone — EVERY §7 uplift row pins to the PUBLISHED artifact before the transaction executes.** |
| **K-1** | Version-lying window: producer WT self-reported 7.0.0 for 5 days (08-04..08-09) while carrying 8.0.0 deletions. Every "producer 7.0.0" read in that window is SUSPECT → re-resolve at the v7.0.0 TAG. Named needs-recheck: banked FR-CP-21, fr-App MG-β (`git grep -n admin-label v7.0.0 -- src/styles` → 4 hits; v8.0.0 → 0), fr-App B-2's `.paper-texture` premise. If retargeting 8.0.0: re-baseline, never re-book. |
| **R2-2** | Version-conditional LAW (binding, procedural): every version-conditional row in this spec names its target version AND the record fixing it. Record of record = CENSUS-2026-08-03 (`:109`, `:184-186`): value 0.13→4.0, one atomic transaction. The `^2.0.0` peer-floor note (07-03) is superseded by date. |

### WU-B · Substrate land-or-lose (F.W0 hard predecessor; carried here because F.W1's premises die without it)
| id | disposition |
|---|---|
| **FR-EQR-33(c) / GAB-13** | The audited "installed 4.0.0 pin" is UNCOMMITTED bytes of the in-flight `m/w1-bump-migration` set (28 dirty paths; committed manifest pins glass `^3.1.0`/kf `^2.2.0`/value `^0.10.0`/lucide `latest`). Land the WT diff or record it as the explicit audit baseline BEFORE any per-file budget. Witnesses folded: fr-VisualizationView D-29/INF-1 · FR-COB-10 (**F8-REACH-01+02 ruled in ONE breath**) · fr-AdminFlaggedPanel ruling-13 (the `opaque`→`surface` hunk is UNLANDED — "survives untouched" describes the working tree). → G3. |

### WU-C · The Button re-grammar spine (the atomic seam)
| id | disposition |
|---|---|
| **AA-2** (ADJUDICATED BLOCKER, identity home) | glass ≥7 deletes Button `variant` (→`emphasis`+`tone`) and `size="icon"` (→`iconOnly`) behind a RETAINED `./button` subpath — export-map census structurally blind; `emphasis\|ButtonEmphasis\|iconOnly` over the tree → 0. Cure: mechanical three-axis re-grammar (`ghost`→`emphasis="quiet"` · `destructive`→`emphasis="primary" tone="destructive"` · **`default`→`emphasis="primary"` — the arm this map omitted, booked at repair round 2 by banked id `fr-FunctionInput N-1` (§2·R2b.1): `default` is NOT a no-op, it was silently demoted to the neutral glass wash by the in-flight 3.1→4.0 bump, so a mechanical carry-forward ships the demotion into ≥7** · `outline`→NO SUCCESSOR — **design call, OWNER-RULING: flagged ESC-7 (§2·R2d), 30 sites / 16 files measured live at repair round 2; never presumed, never swept** · `size="icon"`→`size="md" iconOnly`), floor-preserving per G8, same-commit with the bump (G6). 25+ instance witnesses stay folded per the cell in `carry/F-W1-CARRY.md`, never re-booked — **and the elided ids are NAMED, not ellipsed: see §6·R1** (the ellipsis `FR-AFP-2 … fr-EasingCurvePreview` is prose, never the roster). Dissent carried: FMD-11 BLOCKER-at-today; GAB-2's DU dissent revives if rehearsal shows silent fallthrough shipping. |
| **FR-COB-2** | Two NEW limbs on AA-2: (a) the `variant` limb is SILENT (no `strictTemplates` → junk DOM attr, zero diagnostic) while `size="icon"` is hard TS2322 — only the smaller half is compiler-visible; (b) the WCAG floor RE-KEYS `[data-size="icon"]`→`[data-control-target]` (from `iconOnly`, Button.vue:92) → G8. Corrected budget 9 `variant="glass"`/7 files · 36 `size="icon"` (35 live). CanvasOverlayButton itself: cure = DELETION at **F.W0** (`git rm`, F8-REACH-01+02 one breath). |
| **D-M3** | Budget of record: **87 `<Button variant=…>` · 36 `size="icon"` · 35 files** (+9/+2 comment-resident mentions, doc updates owed; +17 non-Button `variant=` sites). Five banked figures disagree — reconcile ONLY by G7's stripped-comment grep at the adopted pin; **never average**. **HOMONYM DISARMED (repair round 2): this `D-M3` is WU-C's Button budget and is NOT `fr-ExportModal D-M3` (the 16×16 `opacity-70` ✕, free at F.W1 — adopted at §2·R2a.1). The collision is the record's own named hazard and it shadowed the ExportModal row out of two censuses; an unqualified `D-M3` is not an identity (R-5).** |
| **M-7** | SCOPE AMENDMENT: re-derive the break surface as a **prop-level diff over every surviving subpath** (BasisSelector proof: 0 subpath breaks, 6 prop breaks). Precedes every per-file budget. Improve-side: ≥7 adds `loading` (cures D-m12) and defaults `type` (D-m14). **Riders that ride this diff and nothing else, named by banked id at repair round 2: `fr-BasisSelector i-4` (the single-file proof itself) and `fr-NotationPills FR-NP-30` limb (c) — fixed `px-3` vs scaled height (§2·R2b.6). Priced inside this diff or not priced at all.** |
| **FR-COB-19** | `EquationView.vue:274-283` stacks a counted break (`./hover-card`→`<Popover>`) and an uncounted one (inner Button props) on one callsite with `as-child` merge-semantics change — treat as a single migration unit, rides M-7. |
| **FR-EQR-1** | `copied`→`status` (useClipboard returns `{status: ComputedRef<ClipboardStatus>, copy, invalidate}` at ≥7; `v-if="copied"` goes permanently false, silently). Migrate the triple — `EquationResult.vue:15` · `UserSlugBar.vue:23` (`resetMs: 1500`) · `useMorphConfig.ts:58` — in the SAME change (G6). Rider K-12: `CopyFailureReason` 3→2 + `timeoutMs` are v7 facts, binding at ANY ≥7. FR-USB-39's §7d roll-call is incomplete by this row — MUST NOT close UserSlugBar's budget. |
| **FR-EQR-3** | THE atomic lock: the producer's DEV retired-prop `console.error` (Button.vue:131-146) meets FOUR zero-console-error e2e gates on the DEV server. Bump + 162-attribute rewrite + the `copied` triple = ONE change → G6. Rider: FR-EQR-19's KaTeX `strict` warns are a different channel — do not conflate. |
| **U-2** | SelectTrigger: default gains `glass-control-edge` + `glass-capsule-hover`; at 8.0.0 `variant`/`size` REMOVED entirely (producer design ruling "a picker with no edge is not a picker"). "Cured for free by ghost" INVERTS. Cure = explicit `box-shadow: none` or abandon the invisible-trigger design — **design decision, contra-producer** → ESC-3. Books as a delta on the banked 8.0.0 prop-break class. Dissent: R1-DU MAJOR on permanent-blur; BANK trigger `grep "contain: paint"` on installed dock/shell.css at any uplift. |

### WU-D · P0 CSS-class census (G5)
| id | disposition |
|---|---|
| **B-2 (.paper-texture)** | Live at installed 4.0.0 (`dist/styles/cards.css` `@layer components`), definition-absent at the producer roster; neither surviving `@utility paper*` recipe is a drop-in. Cure decided by one before/after of the shell: texture-restore vs class-delete. **Cure-constraint (fr-SvgFilters M-1/R-3): never prescribe the 4.0.0 `@utility paper-*` recipes — they paint uniform opaque black**; the only working producer artifact at the pin is the token/`.paper-texture` form. Dissent: LC BLOCKER; MAJOR regrade PRICED on G5 existing (ESC-5). Premise needs-recheck under K-1. |
| **MG-β (`text-admin-label`)** | Real `@utility` at 4.0.0, 7 sites/4 files (AdminFlaggedPanel `:176/:180/:189` · AdminUserList `:376/:379` · FrequencyGraph `:200` · CoefficientsSpectrum `:110`); at target only a `class-names.ts:84` allowlist ghost — worse than absence. **Alive at v7 TAG, gone at v8 (K-1 correction).** Cure inside G5: producer restores via BH relay, or consumers re-target `--type-micro`/`--type-caption` + `tabular-nums`. **IDENTITY COLLISION: AA-15 books this class as a CURE — G5 must reconcile cure-vs-break before either executes.** Witnesses folded: FR-CP-21 · fr-CoefficientsSpectrum D:M-8 · FR-FG-12. |
| **FR-EQC-3 / K-13** | `.cartoon-card` shim (style.css:98-112): census of record **21 occurrences / 14 files** (shim's "14" and lane's "25" both wrong). Cure: `<Card class="cartoon-surface">` (radius free via `--radius-card`); execution F.W1-sequenced-F.W3. **CROSS-ROW CONFLICT ordered by G17** (AA-4 adopts vs FR-EQC-3 retires). **KILL-6 lock: `<Card tier="opaque" surface="cartoon">` is UNSPELLABLE at the target — executing the corpus's migration lands 21 non-compiling values / 14 files.** **Epistemic carry joined at repair round 2 (`fr-BasisCanvas D-corpus-C-1`, §2·R2a.3): `cartoon-surface` SURVIVES the 4→7 uplift, so this family is a design ask at the hop and not a build break — which constrains G5's reading and does NOT license the migration KILL-6 forbids.** Dissent: FR-GV-18 MINOR-provisional pending G4. |
| **FR-NP-13** | `.btn-pill` inert at pin, load-bearing after ≥7 deletes it (`:38/:40`; `:38` hardcodes past `--radius-pill`). Keep the local declarations knowingly, or re-token. Fourth census instance, rides G5. |

### WU-E · The `--viz-*` palette collapse (one repair, split-edge with F.W2)
| id | disposition |
|---|---|
| **D-1/BC-1/C-1** (ADJUDICATED BLOCKER, identity home) | `cssVarToHex` (`colors.ts:22-53`) falls through to `#888888` on the producer's `oklch()`/`light-dark()` token forms; no `@property` color registration; `resolveVizColors()` overwrites brand hexes on mount and every root-class flip. True symptom = CHROME↔CANVAS DIVERGENCE (20+ CSS `var(--viz-*)` reads resolve natively; the instrument goes grey). Arithmetic of record: sole survivor `--viz-amber` `#9d6515`/`#e8b96d`; four of five grey. Cure: **`colorUnit2 ∘ parseCSSColor`; `color2` NEVER** (throws on wrapper; unwrapped silently returns wrong channels — MG-ι); `light-dark()` arms by **used-value probe read, never string parsing**. **CURE RE-HOME (M-RTC): the sanctioned seat is installed glass-ui 4.0.0's own `@mkbabb/glass-ui/dom` `resolveTokenColor`/`createTokenColorCache`/`useTokenColor` — at the CURRENT pin, no uplift, no cross-repo.** The colors.ts leg is landable at 0.13.0 TODAY (cross-edge 3); only the bare-root-specifier leg gates on the uplift. Provenance MG-κ: the stale committed build dates the regression (prior pin authored HEX; `#d73523` byte-identical to `colorUnit2(oklch-token)`). Witnesses folded per the cell in `carry/F-W1-CARRY.md`, ids named at **§6·R1** (`fr-App B-1 … fr-GalleryView C·C-1` is prose; new rider: `EditorControlsDock.vue:123` magnet track goes grey). **Interlock: D-1 and D-2 are ONE repair** (curing either alone worsens the surface). Dissent: DU holds C-2 BLOCKER; ruled MAJOR. |
| **D-2/BC-5/C-3** | `basis-display.ts:3-7` module-eval value-copy freezes three reactive properties — frozen vs live consumers, one basis two colours one frame. Ships WITH D-1. **R-1 ENTRY-ROUTE CORRECTION (binding): the split-brain is entry-conditional** (`basis-display` imported by 7 files all under `components/visualization/`; default path `/paper` evaluates POST-poison → freezes grey) — **every SS-13 witness and repair test pins the entry route**. Scope: 8 files; colour radius 6 (PP-COLORSEAM). **Kill carried (K-5): D-4's "make `color` a getter" cure is KILLED** — reactivity onto a `#888888` resolver makes it worse. |
| **C-4 (resolver placement)** | Root `onMounted` guarantees the child-first race (documented in prose at `useCoeffHover.ts:60-65`). Correct seat: `main.ts` before `app.mount()`; ships with the D-1 cure, same lines. Denominator: 11 true readers (GalleryCard's import dead, K-13). |
| **fr-BasisCanvas C-4** | No draw path holds a reactive dependency on `VIZ_COLORS` — theme flip never repaints paused/off-screen canvas. Newly visible the moment D-1 lands → same wave, same repair. |
| **M-β5** | `hexToRgba`/`hexToRgb` (colors.ts:101-117) zero validation → `rgba(…,NaN,…)` silently ignored by Canvas2D; the `:29` hex fast-path forwards unvalidated. MG-κ shows the path is newly-dead, not born-dead. **The repair's own gate: validate before D-1 emits any hex branch — same commit.** |
| **L-7 · m-9 · M-β6 · M-α5** | Palette-cohesion riders ×4 (dead `VIZ_COLORS.green` read; `MOON_COLOR`/`SUN_COLOR` false anchors; 5× `getComputedStyle` round-trips per invocation, unfiltered; the focal mark's third divergence site `#ff3b3b`). All ride the palette cure. |
| **HLG-41** | ORDERING EDICT: delete the dead `:style` bindings (`:25/:48`) **BEFORE or WITH** the `cssVarToHex` cure, never after — the cure MINTS the ~667.6 KiB-per-theme-flip trigger. |

### WU-F · Producer-deleted CSS grammar + uplift-delta ledger (silent, gate-blind)
| id | disposition |
|---|---|
| **B-1** (BLOCKER uplift-conditional) | Unlayered scoped `animation` shorthand (`CollapsibleSection.vue:60-65`) names keyframes deleted at ≥7 → reka `usePresence` hangs in `unmountSuspended`; all five disclosure surfaces stop closing. Cure = **DELETION not rewrite**; local PRM guard `:66-71` deletes with it (producer carries its own carve); A8-17 `onanimationend` cure is sequencing-coupled (K-2); R-5 scope-id coupling routes identically. **LOCK: the block AND its `ContourSettings.vue:361-374` twin die IN the uplift commit** (G10). Rider (fr-ContourPreview row 15): at target the shorthand SUPPRESSES the working reveal — no CSS gate can catch it. **Witness-ordering lock (D/M-8): cure HOST import breaks FIRST, then run the five-surface witness.** Dissent: LC MAJOR; **if F.W1 descopes, B-1 auto-regrades MAJOR-with-gate.** Kill carried (K-1): the "250ms short via `--spring-smooth-settle`" claim is DEAD; real clock `--spring-present-duration` = 200ms. |
| **B-4** (BLOCKER-at-uplift) | `@mkbabb/glass-ui/animated-digit` (CoefficientsSpectrum.vue:19): present at v7 TAG, absent at HEAD (`4bf53962`, whose body names this consumer). The file's only BUILD-time break; exactly one consumer repo-wide. Cure: **NOT a rename** — hand-wire `useAnimatedNumber` (via `./motion`, both pins) + `tabular-nums` span; `initial` discharges m-20 free; i-3's tabular-nums must be genuinely owned. First obligation = RE-PIN; if the dead tag is pinned exactly, the break defers one pin. Relay: producer MIGRATION.md:738-739 stale row → BH letter (NWO-1). Dissent: LC MAJOR on tag-vs-HEAD honesty, carried. |
| **FM-2** | Both focus-ring registers deleted at the v7 TAG — six read sites / five files lose focus indication, invisible to import/type surface. Falsifies banked fr-AppHeader killed-item-11; strands N-6's cure. Cure: **a token-level break-surface COLUMN in the census** + F.W4 rider (six sites → `--focus-ring-color`, one edit). |
| **FR-COB-6** | `aria-pressed` paint removed at v7 (0 hits in button styles; sole survivor a `::before` lens intensity). Toggles keep semantics, lose the background delta (contrast/forced-colors users GAIN a border). Cure: author explicit pressed treatment or re-home onto dock/tabs — decided when M-7's prop-diff runs. Relay: intentionality ask → BH (NWO-1). |
| **fr-GalleryCard D-21** | `--shadow-cartoon`/`-hover` sign flip at v7 (`3px 3px`→`-3px 3px`) — every card stamp mirrors, no source change, no typecheck. Cure: **visual-regression checkpoint** (cross-edge 9), not code. |
| **GCM-22** | The consumer `p-0` neutraliser dies at v7 (pad moves to longhands in different `cn()` buckets) — full-bleed hero regains ≈24/30.5px insets. Cure: retire `p-0` for authored bleed geometry, same transaction. **Flips on G1: superseded if target = 8.0.0** (the `:where()` clamp; `w-full` genuinely outranks). D-axis §6 counter-conclusion: killed. |
| **G-DU6 / GCM-50** | Badge rim mechanism change at v7 (`outline`→box-shadow rim; `.badge-atom` adds the rim to EVERY badge; `.basis-tint`'s bare `border-color` re-tints an unauthored hairline). Uplift visual note; narrows D·S-2 to the prop level. |
| **fr-GalleryCard D-20** | `rounded-full` vs `rounded-badge` no-dedupe: inert (both compute a pill), fixed at 7.0.0 class-names. Rider, no action. |
| **fr-ContourPreview 23/27/30 + FR-EQC-13** | Four silent import-shapeless deltas at the bump: (i) `cartoon-surface` hover-lift disappears (self-cures false-affordance rows AND strips real affordances among the 21); (ii) CollapsibleContent gains `role="region"`+name (new guarantee); (iii) `tap-squish` drops from the trigger; (iv) `.disclosure-content` imposes ink/type/padding on every collapsible body. Cure: **explicit F.W1 diff-review ledger + before/after screenshot rows**; shim's stale comment corrects in the same edit. FR-EQC-10's rhythm work sequences AFTER. |
| **FR-CP-5** | Disclosure chevron snap (scoped `transition: transform` cannot cover Tailwind's `rotate` longhand) — **discharged-by-uplift** (`@utility transition-disclosure`); consumer no-op; **verify at bump**. |
| **FR-CP-13** | v7's adjacent-sibling fusing pair assumes touching layers; `.viz-panel-left` gaps 12px → flat-ended rectangles floating. **One gap decision made ONCE, jointly with FR-CP-24's hairline row — remove the gap or opt out of fusing IN THE SAME COMMIT.** Rider: body padding 12→20px worsens FR-CP-10, re-tunes FR-CP-20. |
| **fr-ExportModal M-α/M-β/M-γ** | Three banked cures INVERT at the uplift: M-α `showClose`→`dismiss="deliberate"` (banked order fails vue-tsc or silently regresses the exact defect; `spring`/`scrimAnimation` also gone) — re-spell WITH/AFTER F.W1, superseding the banked D-m7 cure cell; M-β DialogTitle loses its weight source (banked "drop raw utilities" cure post-F.W1 ships a 400-weight title) — rides the ledger + BH note; M-γ `min-width:300px` inverts to harmful — **its deletion is a sequenced PREREQUISITE, not a tidy-up**. |
| **FR-TT-5/-6/-9/-14/-24** | The tooltip seam: `:collision-padding="8"` STRIPPED at v7 (`RETIRED_FLOATING_ATTRS`; reka floor 0 reasserts; HEAD forwards wholesale) — **FR-TT-5 and FR-TT-9 are ONE joint re-pin gate, sized only after G1** (two banked K-1 rulings disagree on which side governs); chip ratio inverts + type rung demotes (FR-TT-6, prose defect → BH); the unstamped Tooltip collapses its own dock for 20 in-dock tooltips, **cure exists only at 8.0.0** (FR-TT-9 → BH relay with the 8.0.0-only cure named); `./icon-tooltip` dead at v7 → the local shim is the only home for the side/#content/non-nesting contract (FR-TT-24); PRM end-state properly authored at ≥7 — the uplift's clearest win here (FR-TT-14). Budget: tooltip visual re-baseline. |

### WU-G · Removed/renamed subpaths and members
| id | disposition |
|---|---|
| **Census FE §5 / C-4-census (L-13/K-14)** | Identity home for the retained-subpath deferral row + three-direction staleness. Amendments carried whole: (i) `./dropdown-menu` GONE at 8.0.0 → `./menu` (AnimationControls.vue:9 — new row, additive); (ii) `./icon-tooltip` DELETED at 7.0.0 (the one-word Tooltip swap would migrate 35 callsites onto a dead subpath); (iii) `./toggle-chip` GONE at 7.0.0 → successor re-derived at the tag (`./chip`/`./switch`/`./toggle-group`/dock); (iv) `./confirm-dialog` absent from producer exports AND source — adopting it manufactures a break (C-M7 kill; FR-GV-4's cure = in-file Dialog); (v) `./checkbox` present only at v8; (vi) `./scrolling-text` absent v5/v7 — GalleryMarquee renders nowhere (FR-GFC-9); (vii) `LabeledSelect` removed at 8 → FMD-14 lands as `LabeledField` + `<Select>`; (viii) `./metric-cell` deleted at v8. Export-key counts of record: **80 @ 4.0.0 · 74 @ v7 TAG · 70 @ 8.0.0** (tag measures govern). |
| **Dock member deletions (D-12 + L-7)** | Three break axes in 132 lines (CanvasControlsDock) + **budget is CALLSITES not imports**: EditorControlsDock 12 `<DockIconButton` + 2 `<HoverPopover` + 1 `<MetricBadge` / 118 lines — densest single-file cost. Cure: `<DockControl>`/`<DockTrigger>`; tri-state `active` replaces class+aria (D-3/D-8); `aria-disabled`-not-native (D-23). **The hop is 4→8, not 4→7 — every producer `file:line` re-keys per G1.** Rider MM-11: cures that render docks elsewhere ENLARGE the transaction — sequencing rider. |
| **D-2/C-2 (Popover union)** (BLOCKER) | Four view toggles keyboard-dead by construction across two docks, one root cause; producer cures it at the Popover union. **Migration row marked PRIMITIVE-CLASS-CHANGE — a mechanical rename re-ships the defect**; D-7 and C-13 die WITH the union, never patched locally. Interim (F.W4, at the pin): `v-model:open` + trigger `@click` — survives the migration. Riders: magnet keyboard-unreachable app-wide; HoverPopover's reka arm is live. Relay: dock token scope on owned portals → BH. Dissent: touch legs → SS-13; never carry "touch-dead" unqualified (K-9). |
| **FR-AH-13** | The row the uplift IMPROVES: attribution touch-unreachable (excludeTouch + click collision). Cure: `<Popover trigger="hover">` — **MUST pass `trigger="hover"` and resolve the `:64` click collision**. |
| **FR-AH-22 / FR-AH-24** | `./hover-card` dies the day the range moves (1 of 2 sites); the header's boot path imports the forbidden easings fork. **Cure-completeness: strike `:21-27` AND `:29`** or the eager `vendor-math` edge survives. |
| **FR-AUL-10 + FR-AUL-45** | `ToastVariant` definition-absent at ≥7; 11 static sites through one adapter; vue-tsc the only catcher. Cure: rewrite the adapter **and FOLD FR-AUL-45's tone restoration into the same edit** (success/info flattened to default while the installed Toaster maps distinct tone classes). **Ordering: AA-1's toast cure routes through this file — cure `useToast` FIRST or use an inline banner.** K-11 scope note carried. Dissent routed: FR-AFP-17's success-face cure needs NO uplift → §5 Excluded. |

### WU-H · The metric family
| id | disposition |
|---|---|
| **GAB-2** | Not a rename pass: `color` deleted with NO successor at v7 AND v8; geometry renamed TWICE (`labelPosition`→`orientation`→`posture`); silent DOM fallthrough. Cure (RULED, K-4): **`<Metric posture="cell">` from `./metric` at 8.0.0** (+`compact` cures GAB-8; `loading`/`aria-busy` cures GAB-4). **Cure-falsifiers: MetricCell DELETED at v8; MetricPill is NOT a fallback seat (GAB-29).** Budget from 8.0.0, never the 7.0.0 snapshot (FMD-11 baseline law). Relay: tier-tint home → BH (NWO-1). Dissent: DU BLOCKER, escalates on silent-fallthrough-ships rehearsal. |
| **D·D-M10** | The energy encoding is DESTROYED, not renamed (`MetricBadge :color` → no `color`, no `tone` on ≥7 Metric). Cure: re-express the encoding (`.metric__value` class or tokens) — **design work**; rides M-7's prop diff; seat = FR-AFP-18's. Benefit rider: `loading`/`aria-busy` is the D·D-B4 cure. |
| **FR-IC-7 / FR-IC-8 / FR-IC-10 / FR-COB-10** | Metric-badge budget corrected: **7 importing = 6 live + 1 unmountable**; row re-keys 4→8 (FR-IC-7); FR-IC-10's cure killed twice (child order; `posture`; no `abbreviation`). **Sharpened stake (FR-IC-25): CI runs cold by construction (`ci.yml:95` + `deploy-pages.yml:114`, `vue-tsc -b --force`, tsbuildinfo gitignored) — the TS2307 fails CI AND Pages** → G11. F8-REACH-01+02 ruled together at F.W0. |
| **FR-AFP-18 / FR-GFC-26 / m-2 / D-27 / FR-MSP-11** | Consumer sites: render seat migrates at F.W1; marquee count lands in the `./metric` idiom during F.W1, never today; `<div>`-in-`<button>` cured free (v7 `Metric` = `<span>`); BasisCanvas `N = k` readout hoists to DOM via the positioned primitive (with D-3's cure); MorphShapePreview maps 3×`Metric` + 1×`Chip.tone`. |

### WU-I · Sliders (gates F.W1's; execution F.W3/W4)
| id | disposition |
|---|---|
| **B-2 (`--slider-scrub-*`)** (BLOCKER, fleet) | 23 dead declarations / 7 files (four concordant recounts; rival figures killed). Mechanism: the uncommitted rename sweep hit 3-of-4 `glass-scrubber` literals and 0-of-4 token declarations. **Provenance corrected (LC inversion, canonical): v3.2.0 `99a11083` deleted a once-live contract; the unlanded `^3.1.0→^4.0.0` bump crossed it** — hence a token-FAMILY sweep is the right acceptance shape (E-3 amendment relayed). Cure (R-7/K-1): rename ONLY the range pair to `--slider-range-bg`; DELETE the thumb pair (no receiver at `standard`, 4.0.0); leg (b) FALSE at v7, true at 8 (FMD-3 correction). **Acceptance = G9, F.W1's; execution rides F.W3/W4.** Witnesses folded per the cell in `carry/F-W1-CARRY.md`; ids named at §6·R1. |
| **MPC-10** | CURE-RIDER making B-2's cure sound: rename-only ⇒ 11.51:1 → 1.51:1 (a 1.4.11 regression under a green sweep gate). Corrected cure: drop the 30%/45% wrappers, `--track-color` full-strength ⇒ 4.004:1/3.71:1 PASS. **LOCK: G9 gains a contrast leg in the breaking direction.** |
| **MPC-31 (+MPC-13 · MPC-22 · D-14)** | ONE-CUT LAW, carried as its bytes (⟨cmd⟩ `grep -o 'MPC-3 ⊕ MPC-10 ⊕ MPC-13 ⊕ MPC-8 ⊕ MPC-22 land as ONE CUT, and the acceptance witness for the colour scheme is taken at a non-boot state (or after MPC-8.s re-domain)' fr-MorphPhaseConfig.md`, this seat, round 2): **"MPC-3 ⊕ MPC-10 ⊕ MPC-13 ⊕ MPC-8 ⊕ MPC-22 land as ONE CUT, and the acceptance witness for the colour scheme is taken at a non-boot state (or after MPC-8's re-domain)"**. **The round-1 rendering carried a "verbatim" label over a two-clause COMPRESSION of that one sentence and cited no command — a defect per se under R2-1-LAW.2 and a composite under R2-1-LAW.4; the whole banked sentence replaces it (§E-3·R2). Substance unchanged; the second clause is now the bank's, not a summary of it.** MPC-13: the typecheck flip lands WITH the token cure, never before. MPC-22: `var(--track-color, var(--accent-red))`. D-14: renamed declarations land WITH `var()` fallbacks. Seat correction (boot = 150ms/13.3% extent) carried. |
| **MPC-21 / BR-1** | `variant="standard"` — **delete, don't rename** (redundant at both baselines; no-op today, correct at 8; pre-clears the TS2322 surface). 9 attribute callsites / 7 files (+5 prose). Amends FMD-11 and D-5's cure. Dissent preserved: reader-δ's MAJOR revives **if this spec prices the variant row as a rename** — it does not. |
| **K-8 / SR-1** | `SliderVariant` drift: `"standard"\|"spectrum"` at v7 TAG; `"scrubber"\|"spectrum"` only at 8.0.0. **REBASELINE BANK: at 4→7 the break does not exist — re-book iff G1 adopts 8** (then 9 live sites / 8 files). Must not budget from the 7 snapshot if adopting 8; must not inject a false row if adopting 7. |
| **m-21** | `--slider-track-height` not consumer-tunable ((0,3,0) producer selector, no `@layer` escape) — third independent unsoundness of the corpus cure. **BH relay ask** (NWO-1); consumer lever = `size` (unforwarded, R-6). |
| **PD-1** | The scrub session's sole opener rides an undeclared `@pointerdown` surviving by attribute fallthrough, which the producer's own docblock says is DROPPED across Slot/forwardRef. **Disjunction deliberately unresolved** (binding drops at target's reka, OR the producer docblock is false at its own pin → relay either way). **CHARTER OBLIGATION: binding-verification probe (G18). Cure-shape law amending banked DH-1/K-14/SR-1: native listeners on the resolved host or an upstream session-pair prop — NEVER template bindings on the forwarding chain.** |
| **MPC-32** | 8.0.0 CURES the coarse slider target (`--slider-target-floor`; decorative ghost retired) — benefit-ledger row no bank carries. Joins the F.W1 benefit ledger. Dissent: WCAG 2.5.8 spacing exception noted. |

### WU-J · keyframes 4.3→6
| id | disposition |
|---|---|
| **C-13 / R-16** | The engine decision is F.W1's: keyframes 4.3 already ships `alternate` + velocity-continuous interruption the hand-rolled transport lost (direction from `cycle` off a re-anchored `startTime`; four doors, one dock). Durable seat = engine decision here; **the store repair (direction becomes state) is F.W4's and must not pre-empt the engine choice**. Harm booked at C-2, not double-charged. |
| **C-11** | `currentScale += diff * 0.12` per frame (clockless, ~2× at 120Hz); keyframes 4.3.0 ships the scalar-tween family incl. `reducedMotionScale`. Substrate adoption (also serves D-8). |
| **fr-App C-11** | Budgeting fact: the App subtree's sole keyframes edge (AppHeader → DarkModeToggle → `useFourierMorph` → keyframes.js) is INSIDE the atomic transaction — the persistent header cannot be uplifted independently. Witness MIN-8. |
| **MPC-14 / C/B-2** | keyframes 4.3.0 hard-deps value.js `^0.13.0` — the transaction is genuinely lockstep; no member lands alone. |

### WU-K · value.js 0.13→4.0
| id | disposition |
|---|---|
| **L/B-1 + C/B-1** (BLOCKER-fold) | value 4.0.0 has no `"."` export AND deletes `timingFunctions`; picker's five functions survive at `./easing`; blocker inherited from the morph catalogue. Sharpenings booked: `Result`-shape rewrite for `EASING_PRESETS`; **14 of 22 morph keys silently drift analytic→CubicBezier** (explicit acceptance decision → ESC-4, corroborated MPC-5: 8/22 material, max Δ 0.192, folds to RD-5 RULED RESTORE, acceptance re-runs the sampling); the named-import block is deletable TODAY (object-identity subset — banked id **`fr-EasingPicker MISSED-F`**, the costing correction booked at §2·R2b.2; it kills the "5 sites / 6 symbols" cell at §5 and leaves the site count intact). **EXECUTION LOCK: the `as EasingFn` cast at `easings.ts:58` makes a wrong re-point typecheck clean and throw on the first rAF tick → `play()` never resolves → theme toggle closes PERMANENTLY. Verified by EXECUTION, not typecheck (G12).** Sequencing correction (CENSUS-MC): `@mkbabb/glass-ui/motion-curves` at the PINNED 4.0.0 re-exports every symbol except `timingFunctions` — **the value.js-specifier half is NOT blocked on the uplift** (cross-edge 3). |
| **MG-ι / C-2** | Remedy constraints: `colorUnit2` yes · `color2` never · probe-element read for `light-dark()`. The cure is installed-and-unimported at 0.13.0 (R1 is a tranche-U-head artifact, not a 0.13.0 one). **Value.js-side dispatch defect → NWO-3, cross-repo, not a fourier row.** Dissent: DU holds C-2 BLOCKER. |
| **R-18 / C-11 / FR-CP-15** | Consumption gap: value.js consumed 0% by the colour seams (5 import lines / 4 files, all easing) while `colors.ts` hand-rolls a lossy parser and the coefficient subtree hand-rolls the hue arc ×4 in raw sRGB against installed `sampleColorRamp`. Cure: adopt the substrate; the two-contract `color` prop (5× hex / 3× live `var()`) surfaces the day B-2 is cured. Resolver/token seam folds to D-1. |
| **R2-2** | (carried at WU-A — the version-conditional law; listed here for closure.) |

### WU-L · Peer graph, manifest, lock, toolchain
| id | disposition |
|---|---|
| **C-1 = C-8** | ONE peer-graph identity, 6+ reproductions, never re-booked. Riders: peer `optional: true`; drift value.js-only; **the ERESOLVE is the uncommitted M.W1a bump** (F-1(i)); **the uplift RELOCATES the violation** (≥7 peers `^4.0.0`). Gate: G13 (`npm ls` → 0). Peer set re-verified: value `^4.0.0` / kf `^6.0.0` / pencil-boil `^0.11.2` / `@lucide/vue ^1.16.0` — **the atomic transaction stands confirmed**. |
| **MISS-LC2** | `@lucide/vue` is a REQUIRED peer with no lock entry and no manifest row; on disk 1.20.0 unlocked; a faithful `npm ci` throws at module eval. **The audited tree is not reproducible from its own lockfile.** Cure: F.W0 (reproducibility, beside MISS-A7) + F.W1 (manifest lands with the uplift). Rider: `@lucide/vue@1.20.0` sits beside `lucide-vue-next@1.0.0` — **the rename reconciles an already-installed peer**. |
| **C-3** | cva/clsx/reka-ui are LIVE runtime peers booked as "dead devDeps"; all `"dev": true` ⇒ `npm ci --omit=dev` fails TODAY; `tailwind-merge` alone genuinely dead. Cure: manifest section moves + census/lane-doc corrections. **Countervailing lock (FMD-21): CVA is NOT removable — `slider/index.d.ts:1` is a type-level hard edge; carry CVA until the edge dies with the 8.0.0 slider.** Witnesses folded per the cell in `carry/F-W1-CARRY.md`; ids named at §6·R1. |
| **FR-EQC-7** | The regenerated lock DROPPED `vaul-vue` (REQUIRED peer, statically imported by the root barrel, 7 edges incl. `router/index.ts:2`); today's CI green; **the failure fires the day the bump commits the lock**. Cure: **a manifest gate INSIDE the F.W1 transaction** — declare + lock `vaul-vue`, or land the barrel→subpath retirement first. Dissent: BLOCKER re-opens only if the bump commits the lock unrepaired. |
| **PP-REDGATE** | The TS2882 on committed `PaperView.vue:12` is real, but committed substrate + committed lock = GREEN; the uncommitted TS 6 bump flips `noUncheckedSideEffectImports`. Cure: ambient `declare module "@mkbabb/latex-paper/theme"` in `env.d.ts` (or latex-paper types fix) — **lands WITH the toolchain bump, owned by F.W1/W2, NOT an F.W0 precondition. ERRATA: banked fr-AnimationControls M-15's F.W0 routing re-read against this at intake** (cross-edge 1). |
| **PAW-33 / C-19 / MF-12 / C-15+C-16 / C-13** | The manifest pass: silently violated optional peer ranges (katex `^0.16` vs 0.17.0; vite `^6\|\|^7` vs 8.0.16); dual katex ambient types under `skipLibCheck`; `vendor-math` welds value.js to katex; one type, three specifier spellings. Cure: **pin-truth row + manifest pass; chunk split decided on a FRESH build (stale `web/dist` inadmissible); re-snapshot the pin-truth row at repair time** (the tree is in motion). Rider FR-AH-7: the header forces `vendor-math` (348,707 B) eager — the fork dies at `./easing`. |
| **C-13 (pencil-boil raw TS)** | 0.4.1 ships raw TS (`"." → ./src/index.ts`) — 5 dependency sources enter strict typecheck; resolved by the 0.12.0 real-dist bump. Ledger row; interacts with G14's range exclusion. |

### WU-M · pencil-boil
| id | disposition |
|---|---|
| **M-2** | **Registry correction of record: the v7 floor is `^0.9.2`, not `^0.11.2`** (`^0.11.2` is the 8.0.0 fact — the charter's figure is correct only if G1 adopts 8). Three banked rows carry the wrong figure (C-18.2 · FM-22; kf-HeroAurora correct). Both wanted cures land at v0.9.2, signature unchanged → **no-break at this site**; every "7 minors" risk weight overstates. Dated evidence never rewritten. → G14. |
| **C-18.2** | Coordination blocker: `^0.11.2` at 7 AND 8 **excludes producer 0.12.0** (`>=0.11.2 <0.12.0`) — blocks L-B3's bump premise. Cure: producer range widening or pinned 0.11.x → pencil-boil relay (NWO-6). |
| **L-2** | The empty-loop rAF resume (module-scope `visibilitychange` behind a false `sideEffects: false`), reachable post-SvgFilters-deletion for EVERY user. Cure: **the bump to the corrected floor cures both mechanisms upstream**. **RIDER: deleting SvgFilters does NOT discharge this row**; one relay line on the false manifest (NWO-6). |
| **L-i3 / C-18.1** | Census: pencil-boil = **3 import sites / 3 files** ("4" was a loose-grep comment artifact). Correction TARGET (not an authority this spec leans on): the `pencil-boil peer floor` row of `docs/tranches/V/megatranche/formation/fourier/lane-frontend.md` **@ `64aca0a2`** — the commit-qualified form R2-2 item 2 requires, since the round-1 bare `lane-frontend.md:481` was a line number into a file carrying no hash. Re-read at that commit this round: the cell labels **"4 sites"** and then lists **three** paths (`SvgFilters.vue:3`, `FourierShapeExtractor.vue:144`, `svg-fourier.ts:11`) — the label and the list disagree in the file itself, which is the defect L-i3 books. |

### WU-N · The lucide rename
| id | disposition |
|---|---|
| **census R3-10 / `:105` fleet row** (fold home FR-CP-42) | Figure of record: **35 import sites / 35 files** (`grep -rn "lucide-vue-next" src/ \| wc -l` → 35); target peer `@lucide/vue ^1.16.0`. Member sites folded per the cell in `carry/F-W1-CARRY.md`, ids named at **§6·R1** (`FR-AUL-53 … fr-ImageUpload` is prose, never the roster). **BUDGET CORRECTION (FR-IC-26): the import-keyed budget is blind to 17 hand-inlined `<svg>`s / 12 `.vue` files** (one element-identical to lucide `Info` at `EquationView.vue:277-279`). **BUDGET REDUCTION (D-11/L-7/C-8/C-26): the FA path literals (6 sites / 2 files) shrink the surface.** **CENSUS DISCREPANCY: the charter's "21 sites / 14 files" is the `.cartoon-card` shim census, NOT the lucide figure — two distinct numbers, never merged.** |
| **D·D-M11** | Sweep amendment: +1 inline site (`EquationView.vue:277-279`, the copy the sweep will miss). |

### WU-O · Token parity
| id | disposition |
|---|---|
| **M-α6** | `--ease-apple-spring` and `--duration-mid` exist NOWHERE; consumed in 5 declaration sites / 4 files, several under comments asserting canonicity; IACVT kills the whole declaration. Per-site consequence (fr-GalleryCard R-4): the ONE `transition` shorthand → hover-lift/press-squish/tier-glow/selection-ring AND `like-bounce` die. **IMPROVE-1 executable at the pin: `--spring-bouncy` two-token substitution at GallerySearchBar:188-189 restores the enter arm today.** Dissent: DU MAJOR at GalleryCard; banked MINOR governs; both BLOCKER inflations killed (K-6, K-11). |
| **m-3 / F-3** | `--font-serif-math` declared nowhere at 4.0.0/v7/v8; 26 occurrences / 17 files fall to UA generic serif. Cure: **define or retire** (token parity) + standing BH relay; `fourier-f` binds `--font-display` — adoption is a design decision, not a swap. Witnesses folded. Banked regrade governs (MINOR). |
| **FR-COB-24** | Root-font fork (`style.css:40-50` — the `@media (min-width: 768px)` arm closes at `:50`; the round-1 `:40-49` spelling is corrected here and at §1, PASS-2 D-7(ii)) inflates every rem-denominated control token 12.5%, compounds with coarse `--ui-scale: 1.5` → 67.5px; the ≥7 ladder is tuned against 16px. Token-parity note in the transaction's review. |
| **D-18/C-13 · D-20 · D-26** | The theme-blind 300° rainbow bypasses BOTH installed colour substrates (substrate-adoption question here; execution rider F.W3/W4); `font-family: var(--font-mono)` → `@utility fira-code` one-class swap; **pick one ring-token spelling** (`--ring` vs `--color-ring`) in the ring sweep. |

### WU-P · Re-ink / tone / geometry rows F.W1 owns by routing law
| id | disposition |
|---|---|
| **FR-AFP-3 + FR-AFP-46 + FR-AFP-61** | Light-mode re-ink with its split cure: re-ink on `--destructive`/tone + `--border`, **with FR-AFP-46's split** (4.38:1 passes 1.4.11, fails 1.4.3 at 10px — token fill/glyph · strong-rung text · or the Badge's owned pairing). **FR-AFP-61 correction: `<Badge variant="destructive" size="sm">` is installed TODAY — the worst-contrast row needs no uplift; the tone re-grammar is F.W1's, the shadow retirement F.W3's.** Dissent: β MAJOR, ruled per ruling 7. |
| **FR-AFP-19 / FR-AFP-20 / FR-AFP-27 / FR-AFP-44 / M-6** | Rest-state tone: the missing thing is SEMANTIC differentiation, not the indicator; the alpha-mute anti-pattern (`opacity-60`/`opacity-30` on muted) is exactly what `--muted-foreground-strong` was minted against; `border-l border-muted` at ~1.1:1 exceeds a token swap. Cure: strong rung by-colour not by-alpha; **rest-state tone at F.W1; focus-visible + row-state vocabulary F.W3/W4**. |
| **FR-AFP-28 / FR-AUL-19** | Three icon-in-button geometries + the two-red pill: **collapses FREE with the `iconOnly` re-grammar** — no separate work. |
| **FR-AFP-37** | Hand-rolled destructive confirm; at ≥7 `showClose` is GONE — spelling is `dismiss="deliberate"`. Cure: in-file Dialog pattern with the `dismiss` guard + `loading`; **`./confirm-dialog` is dead at 7 AND 8 — adopting it manufactures a break (C-M7 kill).** |
| **FR-AFP-16 / M-4 / GCM-13 / FR-USB-7 / FR-USB-18 / FR-EQR-13** | The `loading` prop budget: guards land F.W3/W4; **the one-word primitive lands WITH F.W1**; the `boolean → string \| null` widening a per-row busy state needs is UNBUDGETED (M-4) — flagged, not silently absorbed. |
| **fr-PaperSearch / -Dropdown / -Input re-inks (D:B-3 · D-M1 · MISS-A6)** | Eight `color-mix` alpha-dilutions of an AA-annotated token beside the producer's written refusal of the idiom; placeholder 1.888:1. Cure: **F.W1 token re-ink**, with the F.W3/W4 repair-hazard gate — **the C-1 relocation ARMS these (same-commit rider)**. BLOCKER re-filings killed (K4). |
| **FR-USB-3 / FR-USB-35 / FR-COB-13 / FR-MSP-5 / FR-EMT-14** | Hop benefits: the five inert `size-N` overrides die with `size="icon"`'s type error; `type ?? "button"` cured upstream at ≥7 — **SCOPE CORRECTION (FR-MSP-5): pays only at `<Button>` callsites; six of seven live native sites remain type-less after F.W1** (latent; `grep "<form" web/src` → 0). |
| **FR-COB-16 / D-13 / GAB-5 / MPC-16 / FR-EQC-5** | Free-cure credit rows: `loading` vs in-flight-as-on; the PRM press-scale leak cured by `useLiquidPress`; the six inert counters in interactive costume cured free; `cartoon-surface` hover self-cure **with its converse (stripped real affordances = the FM-2 class)**. **SEQUENCING (fr-FourierMorphSvg ruling 4): F.W4 tickets written against a lift that will not exist post-uplift sequence AFTER F.W1 or drop; the `/morph` PRM ticket's three layers become two.** |

### WU-Q · Non-credit, negatives, scope certifications
| id | disposition |
|---|---|
| **FR-GIG-5** (NON-CREDIT LOCK) | **Stated in terms: the F.W1 uplift transaction is NOT credited with curing the whole-collection pagination drain.** v7 fixes `check()`'s trigger and leaves the observer-root geometry byte-identical at 7.0.0 AND 8.0.0; the loop survives 4→7→8. The cure is a **producer change on the BH relay** (NWO-1). → G15. Three BLOCKER dissents preserved at the banked record. |
| **PAW-1 / PAW-37 / PAW-51** (BLOCKER) | The transaction as chartered is INSUFFICIENT: latex-paper's bare-triplet contract IACVT-drops all 42 `hsl(var(--x))` declarations; 92 theorem blocks ship the WRONG rail colour. **Version-INVARIANT (4.0.0, v7, v8) — no disposition flips on G1; the uplift cures nothing on `/paper`.** Cure: **G16 — add the LATEX-PAPER carry as a fourth package or seat it explicitly with a named owner**; sound limb = rewrite the 42 sites to `var(--x)` (consumer-alias limb KILLED, K-9). **PAW-30 lands in the SAME repair; LAW-3 governs PAW-44 and is carried VERBATIM at repair round 2** — ⟨cmd⟩ `grep -o 'LAW-3\*\*: PAW-44 restoration lands [^.]*\.' fr-PaperArticleWindow.md` → *"LAW-3**: PAW-44 restoration lands WITH-or-AFTER PAW-1 + PAW-30, never before."* **The round-1 spelling *"restores the sticky stack only WITH the background cure"* is STRUCK: it tightened the banked disjunction (`WITH-or-AFTER` → `only WITH`) and dropped PAW-30 as a second named predecessor — a lock is carried as its bytes or not at all (PASS-2 §3's paraphrase note; minuted at §E-3·R2).** Witness set: theorem + definition + aside side by side, BOTH schemes. |
| **FR-FG-20** | Zero-break certification at 4→7 for FrequencyGraph (all five surfaces survive the v7 tag); **if G1 adopts 8, the `text-admin-label` break RETURNS** — an owner/formation decision carried in the K-1 relay, not a measurement. |
| **D:U-2 / PSM-36 / PP-#7 / S-3 / PAW-37** | Measured zeros recorded so no wave budgets them: ImageUpload nil glass surface (exposure = 3 lucide + the shim); PaperSearchModal = lucide rename only; **PathPreview nil on every F.W1 census row — the one file F.W1 need not open**; ConvergenceLegend `.glass-wash` live at both ends, zero lines. |
| **fr-VisualizationView L-13 / MAJ-3 / MAJ-4** | The uplift's leverage: **DELETES ~14 lines of workaround CSS** — reasoned from the true cascade (unlayered scoped styles outrank `@layer utilities` at ANY specificity, not by specificity; challenge-C corrected). Joint F.W1 + F.W3/W4; two standing BH relays ride (NWO-1). **Rider joined at repair round 2: the record's `§4` bypass half (`fr-VisualizationView §4 method row + bypass`, §2·R2b.7) — the `responsive` bypass's fate at the hop is M-7's diff to answer; the method half is a census input and is NOT this wave's.** |
| **FR-EQC-2** | Inaction is a widening (1.46× today → 1.85× post-uplift; the panel's type goes 100% off-ladder). **F.W1 takes the inventory**; the register join executes F.W3/W4 — else D/M-1's cure fixes one seventh. |

### WU-R · Owner-ruling escalations (G19 — flagged INLINE, never presumed; **the register is ESC-1..ESC-7 — ESC-7 is seated at §2·R2d, a row of this table in force and in kind whose byte position is deferred so no insert re-keys `:276`/`:294` in nine sibling files**)
| id | question | blocked rows |
|---|---|---|
| **ESC-1** | Target tag: 7.0.0 (census) vs 8.0.0 (producer's shipped reality). | G5 roster · seven GCM dispositions · FR-TT-5/-9 · K-1 rebaseline bank · GCM-22 · FR-FG-20 · G14 · Slider-variant budget · dropdown-menu→menu. Every routing here survives either answer. |
| **ESC-2** | ToC model ownership (dual `useSidebarFollow`, untyped `defineExpose` seam). Ruling at F.W1; F.W4 executes the collapse; prerequisite L-5(a). | fr-PaperSidebar L-4/M6 |
| **ESC-3** | The invisible-picker design — producer RULED against edgeless triggers at 8. Design decision, not a port. | U-2 |
| **ESC-4** | Easing semantic drift acceptance (14/22 analytic→CubicBezier; 8/22 material, max Δ 0.192). Explicit acceptance, not a silent bump. | L/B-1 · MPC-5/RD-5 |
| **ESC-5** | B-2's regrade price: the `.paper-texture` MAJOR is priced on G5 existing — if G5 is not adopted, revisit the BLOCKER regrade. | B-2 |
| **ESC-6** | Browser-support band for `color-mix` (the generated fallback paints 1.00:1). Sizes MINOR vs INFO. | FR-MSP-12 → FR-COB-26 |

### WU-S · Relays and no-wave-owner packets (this wave sends; never executes as frontend hacks)
| id | disposition |
|---|---|
| **NWO-1** | The consolidated glass-ui BH letter — the roster is the CARRY's own, cited by **row label**: `carry/F-W1-CARRY.md`'s *"NWO-1 · FR-COB-28's consolidated glass-ui BH relay letter"* row (⟨cmd⟩ `grep -o "NWO-1 · FR-COB-28's consolidated glass-ui BH relay letter\*\* (one letter, sent at F.W0/F.W1 open by whoever holds the mail seat" carry/F-W1-CARRY.md`; round 2: the round-1 *"roster verbatim"* label named no command and no row — R2-1-LAW.2 + R2-2, cured here, §E-3·R2). Members: (**FR-NP-32 (≡ fr-PaperSidebar M1)** at TOP — canonical citation form, R-8; two banked witnesses of ONE fact, cite-both never substitute: the corrupt-dist emitter fix + a producer gate that parses its own `exports["./styles"]`, explicitly NOT a version bump · FR-COB-7/-8/-6/-23/-26 + FR-MSP-12 · btn-glass naming hazard · M-1 · m-17 (`cn` misclassifies the producer's own vocabulary — **every "just override the class" cure is silently mis-priced**) · m-21 · FR-EQR-7 · GAB-2(a) + the 3.26:1 rung · B-4's i-4 · FR-TT-9 + FR-TT-6 · PD-1 · C-13 dock-token scope · SC-1..SC-8). Item 7 DOCK-ACTIVE satisfied upstream — do not re-send. One letter at F.W0/F.W1 open; E13 ledger row appended. **Glass-producer rows go to the relay, NEVER become frontend hacks** (standing owner edict; cross-edge 4). |
| **NWO-2** | Method row (M-16 promoted): producer evidence carries the COMMIT HASH, not the version string; the census adds the export-map diff at the ADOPTED commit. → SS-3/SS-4. |
| **NWO-3** | MG-ι value.js-side (`color2` rejecting/mis-reading its sibling parser's output) → V·π / parser-seam via the owner's mail sweep. Not a fourier row. |
| **NWO-4** | Census corrections: the 21/14 cartoon figure; GAB-29's MetricPill cure-falsifier; D-i2's AA-vs-AAA register correction (binding comparison = resting 1.930:1); PP-IMPORTSHAPE. → SS-3/SS-4. |
| **NWO-5** | LATEX-RELAY: PAW-1's 42-site ask; `ComputedRef<any>` generic fix (**banked id `fr-PaperView C-06`, booked at §2·R2b.5 — the rider is F.W1's, the fix at `ref<T\|null>` is the relay's**); katex `^0.16` vs 0.17.0; slot-type/slug-uniqueness/sourceLevel/ref gaps. |
| **NWO-6** | pencil-boil producer: the false `sideEffects: false` manifest + the `^0.11.2` range excluding 0.12.0. |

---

## §3 Gates (all born-RED; witness named; literal invocations)

| id | gate | born-RED witness | blocks |
|---|---|---|---|
| **G1** | **RE-PIN (OWNER-GATED, ESC-1).** This spec's target-TAG + ADOPTED-COMMIT-HASH cell is filled; every producer `file:line` in all 66 records re-resolves at that hash. **G1 is an INPUT TO / QUOTATION OF F.W0's G-11 anchor table (G-12 denominators) in `fourier/docs/tranches/F/SUBSTRATE-LEDGER.md` — cited by GATE ID: F.W0's `G-11` and `G-12`; the pin table is F.W0's `G-13` (stable idiom, R2-2; the round-1 line cites into that live sibling are struck — they addressed a blank line and two "Owning rows" cells after F.W0's own repair-round shift, PASS-2 D-1) — divergence from that table is a defect against G-11, not a rival act.** F.W0 publishes once; F.W1 re-resolves nothing of its own. | Census says 4→7; producer reads 8.0.0, v7.0.0/v8.0.0 both tagged, HEAD drifted `e73ec80c`→`4a86570b`→`558c3fa3`→`350f7a90`→`407de2d3` (F.W0's G-13 addendum records a SIXTH drift at `207bf174`, 2026-08-28). Cell EMPTY at authoring; `SUBSTRATE-LEDGER.md` is `create`-marked in **F-W0 §2a File bounds, the `SUBSTRATE-LEDGER.md` row** — ⟨cmd⟩ `grep -o 'SUBSTRATE-LEDGER\.md.\{4\}\*\*create\*\*' F-W0.md` → `` SUBSTRATE-LEDGER.md` \| **create** `` (the PATTERN is pipe-free by construction and re-run this round; the pipe in the OUTPUT is table-escaped for rendering only. The round-2 draft's pattern embedded a literal pipe, which both broke this gate row's table and — once escaped for the table — became BRE **alternation** and returned fragments instead of the row, §E-3·R2) — and does not yet exist. | everything below |
| **G2** | **CORRUPT-DIST PRE-GATE (F.W0).** `@import "@mkbabb/glass-ui/styles"` parses under `@tailwindcss/postcss`. | Installed `dist/styles/index.css:195-221` fold-block injected at a prose `@source` mention → `CssSyntaxError: Unterminated string: 's own'`; `style.css:3` is the live import ⇒ **`vite dev` AND `vite build` RED at the pin — F.W1 opens on a red build.** | G4 · G5 · every build-derived cell |
| **G3** | **SUBSTRATE SETTLE (F.W0).** `git -C /Users/mkbabb/Programming/fourier-analysis status --porcelain` → 0, or the dirty set recorded as the explicit audit baseline. | 28 dirty paths; the audited pin is uncommitted bytes no commit holds. F8-REACH-01+02 ruled in one breath. | every per-file budget |
| **G4** | **EMISSION PRE-GATE.** One `npm run build`; grep the EMITTED css for `size-\(--ui-glyph\)` · `\.h-3\\.5` · `rounded-pill` · `animate-collapsible`. | `web/dist` dated Jun 12, built pre-installed-glass-ui, provably divergent; two banked rulings contradict (fr-PaperSearch K1 vs PSM-24/R-3). Canary `PaperSidebar.vue:258`. Prediction (M1/M3): build green, `animate-collapsible` → 0. | FR-EQR-14 · D-13/D-18 · F-5 · `fr-BasisSelector i-4` (record-qualified at repair round 2 — a bare `i-4` is not an identity, R-5) · FR-GV-18 · PSM-24 · D-M2 |
| **G5** | **P0 CSS-CLASS CENSUS.** Diff every glass-ui-owned class fourier applies against the target's `@layer components` + `@utility` roster BEFORE the bump. **AA-15's cure/break collision reconciled inside this gate.** | Four proven instances the import-shaped model cannot see: `.paper-texture` (`App.vue:24`, 1 site) · `text-admin-label` (7 sites / 4 files — AdminFlaggedPanel · AdminUserList · FrequencyGraph · CoefficientsSpectrum; alive at v7 TAG, deleted `6b450f22`, 0 at v8) · `.cartoon-card` (21/14) · `.btn-pill` — **consumer anchor named (it is 0 in `web/src` as a class literal): the producer applies it (`glass-ui/dist/styles/utilities/base.css`, `a11y-overrides.css`, `button-*.js`) and `NotationPills.vue:38/:40` shadows it locally, `:38` hardcoding past `--radius-pill`. The census reads the producer roster ⟷ the local shadow, never a consumer grep.** **PRE-GATE LAW + DISCRIMINATOR, booked by banked id `fr-PaperSidebar M3` (§2·R2b.3): the census runs the record's own one-command discriminator per class — ⟨cmd⟩ `grep -o 'the one-command discriminator.*not painting)' fr-PaperSidebar.md` → *"the one-command discriminator (`grep -c '<class>' components.css glass-ui.css`; 0/0 ∧ absent from `web/src` ⇒ not painting)"*. **Seat correction of record (measured this round, and the reason the discriminator enters as a LAW and not as a two-file recipe): run against `.btn-pill` at the installed pin the two named sheets return 0/0 — ⟨cmd⟩ `grep -c 'btn-pill' styles/components.css glass-ui.css` in `web/node_modules/@mkbabb/glass-ui/dist` → `glass-ui.css:0` / `styles/components.css:0` — while the class IS emitted from **EIGHT** other producer artefacts (count re-measured at the tree this round — ⟨cmd⟩ `grep -rln 'btn-pill' web/node_modules/@mkbabb/glass-ui/dist \| wc -l` → **8**; the round-2 draft's "seven" contradicted the eight-name list it pasted beside it and §2·R2b.3's own figure, corrected at §E-3·R2) (⟨cmd⟩ `grep -rln 'btn-pill' web/node_modules/@mkbabb/glass-ui/dist` → `button-BNDWhAZb.js` · `styles/glass/squircle.css` · `styles/glass/surfaces.css` · `styles/utilities/a11y-overrides.css` · `styles/utilities/btn.css` · `styles/utilities/base.css` · `styles/tokens/scheme-motion.css` · `styles/tokens/offsets-sizing.css`). G5's operand is therefore the WHOLE emitted roster at the adopted pin, never a two-file subset — a shape-restricted operand is defective at authoring whatever it returns (R2-9).** **RE-READ OBLIGATION (banked `fr-GalleryMarquee GM-19`'s F.W1 contingency, §2·R2b.4 — cited, identity homed at F.W4): the census re-reads `a11y-overrides.css` AT THE TARGET to decide whether the reduced-motion specificity defect gains a painted consequence; F.W1 owes the re-read, not the cure.** | B-2 · MG-β · FR-CP-21 · FR-FG-12 · FR-EQC-3 · FR-NP-13 · M3 · GM-19(contingency) |
| **G6** | **ATOMIC LAND-OR-LOSE.** Producer bump + the 162-attribute prop rewrite + the `copied`→`status` triple in ONE change. | Four zero-console-error e2e gates — `web/e2e/visualization-crud.spec.ts:439-482/:612` · `workspace-flow.spec.ts:175-202` · `contour-extraction.spec.ts:139-166` · `gallery.spec.ts:118-135` — none filtering `[glass-ui]`, run against the DEV server (`scripts/e2e.sh:102`, quoted whole this round — ⟨cmd⟩ `sed -n '102p' scripts/e2e.sh` → `    npx --prefix web vite web --port "$WEB_PORT" --strictPort &`; the round-1 rendering `npx vite web` was a clipped quote at a correct coordinate, PASS-2 D-7(i)) where `Button.vue:131-146`'s DEV `console.error` fires per mounted Button. Split the change → four green specs go red for the wrong cause. | the whole wave |
| **G7** | **BUTTON BUDGET RECONCILIATION.** One stripped-comment grep of record, re-run at the adopted pin; its output is the sole budget — **and the transaction's own attribute figure is IN this set, not outside it** (PASS-2 D-5). | **SIX** banked figures disagree (87/36/35 · 37f/38/124 · 9g/7f+36i · 96/77/35 · i38/g12 · **162** — the attribute count §4 step 4 and G6 name, banked at `fr-EquationResult.md:37/:38` — frozen corpus, the one family whose line numbers stay citable — real and never fabricated but never reconciled: it counts ATTRIBUTES where D-M3's budget of record counts `variant=`+`size="icon"` occurrences over files, so it is a sixth object, not a seventh opinion. **Counting-unit hazard measured this round: the record itself spells the figure BOTH ways — ⟨cmd⟩ `grep -o '162-attribute surface is compiler-visible' fr-EquationResult.md` and ⟨cmd⟩ `grep -o '162-site prop rewrite must land in the SAME change' fr-EquationResult.md` both return — so `162` enters this set carrying its unit explicitly, and a seat that reads the `-site` spelling as a file/site count against D-M3's 35 files has mis-reconciled by transcription, not by measurement). **Do not average; state the counting unit beside every figure (F.W0 G-11's own law).** The transaction's limb is sized by THIS gate's output; `162` may be quoted only as an entry in this set. | AA-2 · M-7 · D-M3 · the §4 step-4 rewrite limb |
| **G8** | **TOUCH-FLOOR PRESERVATION.** Every migrated icon site carries `iconOnly`; the emitted sheet keys `[data-control-target]`. | 4.0.0 keys `[data-size="icon"]` (a11y-overrides.css:115-121); ≥7 keys `[data-control-target]` (responsive.css:3-8, from Button.vue:92) — a mechanical `size="md"` sweep silently drops the 44px floor on all 35 live sites. | FR-COB-2 · FB-4 · FR-USB-3 |
| **G9** | **TOKEN-FAMILY SWEEP + CONTRAST LEG.** `grep -rn -- "--slider-scrub" web/src \| wc -l` → 0 — never a variant-string sweep; **and MPC-10's contrast leg in the breaking direction** (rename-only ⇒ 11.51:1 → 1.51:1 forbidden). | 23 dead declarations / 7 files; the causal mechanism was a variant-string substitution missing 0-of-4 token declarations. | B-2(slider) · FMD-3 · HLG-3 · MPC-3 · R-1 |
| **G10** | **DISCLOSURE-DELETION.** `CollapsibleSection.vue:60-65` AND its `ContourSettings.vue:361-374` twin die IN THE UPLIFT COMMIT. | Witness = close COMPLETES and close ANIMATES on all five disclosure surfaces. Born-RED: reka `usePresence` waits forever on an `animationend` a deleted keyframe cannot fire. **Ordering: cure HOST import breaks FIRST (D/M-8).** | B-1 · fr-ContourPreview row 15 · D/B-2 |
| **G11** | **MODULE RESOLUTION.** `vue-tsc -b` + a vite resolve pass at the adopted pin ⇒ 0 unresolved glass-ui specifiers. | `CoefficientsSpectrum.vue:19` animated-digit (v7-present, HEAD-deleted `4bf53962`); `InfoCard.vue:4` metric-badge TS2307 fails CI AND Pages (`ci.yml:95` / `deploy-pages.yml:114`, always cold). | B-4 · FR-CP-14 · FR-IC-8/-25 |
| **G12** | **EASING RE-POINT VERIFIED BY EXECUTION.** Run the morph; assert `play()` resolves and `phase` returns to `"idle"`. | `easings.ts:58` `as EasingFn` makes a wrong re-point at `bezierPresets` typecheck clean and throw on the first rAF tick — theme toggle closes permanently. Plus value 4.0.0: no `"."` export, `timingFunctions` deleted. | L/B-1 · C/B-1 · MPC-2 |
| **G13** | **PEER-GRAPH CLOSURE.** `npm ls` → 0 problems AND `npm ci --omit=dev` yields a working tree. | 6+ ELSPROBLEMS reproductions; `@lucide/vue` REQUIRED with no lock/manifest row (MISS-LC2); cva/clsx/reka-ui/lucide `"dev": true` (C-3); `vaul-vue` dropped from the regenerated lock (FR-EQC-7); CVA not removable (FMD-21). | the transaction's commit |
| **G14** | **PENCIL-BOIL FLOOR.** Read the optional-peer range at the ADOPTED tag; correct the three drifted registry rows. | v7 peers `^0.9.2`; `^0.11.2` is an 8.0.0 fact; and `^0.11.2` excludes producer 0.12.0, blocking L-B3's premise. | M-2 · C-18.2 · L-2 |
| **G15** | **NON-CREDIT DISCHARGE (restated born-RED, R-4 sibling).** The gate is NOT the footnote — a gate a spec satisfies by writing its own prose is GREEN-at-birth. Condition: (a) the F.W1 close report and the transaction's commit body each state in terms that the uplift is not credited with curing the whole-collection pagination drain, and (b) FR-GIG-5 leaves on the NWO-1 relay with its three preserved BLOCKER dissents, E13 row appended, **and (c) `AA-11`'s constraint holds — no cure planned around `./pagination` (§2·R1a)**. | Born-RED, three real absences at authoring: no close report exists (`docs/tranches/X/fourier/evidence/w1/` is `create`-at-execution); `docs/tranches/X/coordination/` holds ONE file (`ATLAS-TO-VALUE-2026-08-03-RULINGS.md` — this seat's `ls`), no NWO-1 packet; and the drain provably survives the hop — `v7.0.0:useInfiniteScroll.ts:33` observer-root geometry byte-unchanged at 7.0.0 AND 8.0.0, so no bump can ever discharge it. | FR-GIG-5 · AA-11 |
| **G16** | **LATEX-PAPER SUFFICIENCY.** Either the 42-site `hsl(var(--x))` → `var(--x)` rewrite enters the transaction, or it is seated explicitly with a named owner. | Token shapes verified complete-colour at 4.0.0, v7 AND v8 ⇒ the uplift cures nothing on `/paper`; 42 IACVT drops; 92 wrong rails. Witness: theorem + definition + aside side by side, both schemes (a proof-block/figure witness cannot show it). | PAW-1 · PAW-51 · PAW-30/PAW-44 (LAW-3) |
| **G17** | **CARTOON-CARD ORDER (a RULING gate, no F.W1 edit to order).** FR-EQC-3 (retire) ordered against AA-4 (adopt) before either executes — **and neither executes here**: FR-EQC-3/FR-IC-19 are F.W1-sequenced-**F.W3** (`fr-InfoCard:49`), AA-4's adoption is **F.W4**'s (`fr-AdminAuditLog:41`). The gate's product is the written ordering in F.W1's close report; the executions are the consuming waves', which is exactly why the ruling must precede them. | Two banked cures on a collision course; KILL-6: `<Card tier="opaque" surface="cartoon">` unspellable — 21 non-compiling values / 14 files in a repo whose only gate is `vue-tsc`. | FR-EQC-3 · AA-4 · FR-IC-19 · FR-GV-18 |
| **G18** | **BINDING-VERIFICATION PROBE.** Post-uplift, a real drag fires `anim.startScrub()` (`anim.scrubbing === true` mid-drag). | `v7.0.0:Slider.vue:69-78`: `@pointerdown` template bindings DROPPED across Slot/forwardRef; no static gate can see this class; `GlassTimeline.vue:73` is the sole scrub opener. | PD-1 |
| **G19** | **OWNER RULINGS RECORDED.** **ESC-1..ESC-7** each carry a ruling or an explicit deferral with blocked rows named. | All **SEVEN** OPEN at authoring; ESC-1 alone flips seven GCM dispositions, FR-TT-5/-9, K-1's bank, GCM-22, FR-FG-20, G14; **ESC-7 (added at repair round 2, §2·R2d) is the wave's largest undecided design surface — `outline` has NO SUCCESSOR at ≥7 and is the second-largest live variant class: ⟨cmd⟩ `grep -rn 'variant="outline"' web/src \| wc -l` → **30**, `grep -rln … \| wc -l` → **16 files**, against `ghost`'s 49 (this seat, live read-only tree, 2026-08-28). Born-RED at the gate: no ruling exists, and the sweep may not proceed on the map's silence.** Never presumed. | the spec's executability |
| **G20** | **CLOSURE, BOTH SOURCES (restated born-RED, R-4 sibling + R-3.1).** Two set-differences, run at ratification by a seat that is not this file's author: (a) `carry/F-W1-CARRY.md`'s 105 row ids ⟷ this spec's §2/§5/§2·R1/§2·R2 → ∅ both directions; (b) **the REGISTRY direction** — **the routed id-keyed census of `conformance/PASS-2/F-W1-CHECK.md` §4 (357 routed rows; the operand re-based from the PASS-1 §3 census at repair round 2, **PASS-2 §4's** fresh re-derivation superseding it — the round-2 draft's "PASS-2 §3" was a mis-section, §3 there being *"WHAT VERIFIED CLEAN"* and the census being §4; corrected at §E-3·R2)** ⟷ this spec (booked, folded, or excluded-with-reason) → ∅. §6 + §6·R1 + §2·R2 are the claim the run tests, never the run. | Born-RED on leg (b) by measurement, not by assertion, **and RED again at pass 2 — the class survived one repair**: PASS-1 returned **15 escaped ids over 13 routed lines, + AA-11 and FR-COB-28 off mention-lines** (all fifteen cured at §2·R1, re-verified present by the PASS-2 seat); the fresh PASS-2 census then convicted **11 more** (`conformance/PASS-2/F-W1-CHECK.md` §1 — 6 inside the routed set, 5 recovered by hand off meta/mention-classified lines), landed at §2·R2 this round. **The gate stays RED: two independent passes each convicted the prior repair, so a THIRD independent run against the repaired bytes is the only thing that can green it — this file may not green it by having repaired itself.** Named residual the run must check as a citation and NOT as a booking: `fr-ConvergenceTimeline D·D-5 + C·C-5`, ruled home **F.W4** (§2·R2c). Leg (a)'s LHS is the id roster, never the CARRY's `fr-NAME:LINE` anchor family (§E-3·R1 records why). No verification verb is stamped anywhere in this file. | X·F SPECIFIED |

---

## §4 Sequencing

**Intra-wave order (binding):**
1. **G1 RE-PIN** (owner-gated) — nothing sizes before the tag + hash cell fills.
2. **F.W0's discharges are RECORDED, not performed here** (contradiction with §State cured: F.W0 CLOSES before F.W1 opens — this step is F.W1 reading F.W0's published results, never F.W1 containing F.W0's acts). Read: G2 + G3 (+ MISS-LC2 reproducibility, FR-AH-33 manifest placement, F8-REACH-01+02 in one breath, fr-AnimationControls M-15 as ERRATA'd by PP-REDGATE) **and the G-11 anchor table / G-12 denominators / G-13 pin table in `SUBSTRATE-LEDGER.md` — cited by gate id only (F.W0's `G-11` / `G-12` / `G-13`), never by line into that live sibling (R2-2)**. F.W1 does not open on a red substrate it has not recorded. **The `@lucide/vue` + `vaul-vue` DECLARE+LOCK landing is F.W1's, inside step 4 (R-4a): F.W0 AUTHORS that manifest gate and measures its born-RED facts; it does not land them.**
3. **Census acts** at the adopted pin: G7 grep of record → M-7 prop-level diff (with FM-2's token-level column) → G5 CSS-class census (AA-15 collision reconciled) → G4 emission build → G14 floor read → K-1 suspect-window re-resolutions.
4. **The atomic transaction (ONE change, G6):** producer bump (glass + keyframes + value, lockstep per MPC-14) + 162-attribute Button rewrite (G8-preserving) + `copied`→`status` triple + lucide rename (35 imports; +1 D·D-M11; the 17 hand-svgs ledgered) + G10 disclosure deletions + FR-EQC-7 vaul-vue manifest gate + G13 manifest/lock moves + PP-REDGATE ambient declaration + FR-CP-13's gap decision + ExportModal M-γ deletion + GCM-22 `p-0` retirement (7-target only) + the pencil-boil floor per G14. **The roster is TWELVE limbs and stays twelve — the eleven chartered plus the FR-EQC-7 vaul-vue manifest gate returned INSIDE at repair round 1 per the `:294` pin (R-4a, cross-edge 1 at this writing) — it is the extent every sibling spec cites (R-4b); nothing is added to it by a later reading.** (R2-5, cured ONCE and only here: the count word and the list disagreed because R-4 reached "eleven" by fusing the last two conjuncts, a grouping this text's own top-level `+` separators do not mark. The attribute figure inside limb 2 is G7's output of record, never a banked number quoted around it — PASS-2 D-5.) **G5 LANDING CELL (conditional, not a THIRTEENTH limb — re-denominated with the roster at R2-5): if the P0 CSS-class census returns consumer re-targets (`text-admin-label`→`--type-micro`/`--type-caption` + `tabular-nums`, `.btn-pill` re-token, `.paper-texture`'s texture-restore-vs-class-delete decision), _those edits land inside commit #4_ — the census is a limb's resolution, never a separate change; if it returns producer-owned cures instead, they leave on NWO-1 and no consumer edit exists to land (a frontend workaround for a producer defect is a wave defect).**
5. **Post-transaction witnesses:** four e2e gates green · G10 five-surface close witness (HOST imports cured first) · G12 execution run · G18 drag probe · FR-CP-5 chevron verify · the diff-review ledger + screenshot rows (WU-F) · the visual-regression checkpoint set (cross-edge 9).

**Commit plan (at execution, post-begin-word):**
1. `docs(X·F): F.W1 spec + carry closure` — this file + the G20 arithmetic.
2. `docs(X·F/coordination): the SS-6 BH letter + LATEX-RELAY + pencil-boil letters` — NWO-1/-5/-6, with INBOX append rows (E13).
3. `fix(fourier/viz): the --viz-* palette cure at the current pin` — WU-E as ONE repair (D-1 ⊕ D-2 ⊕ C-4 ⊕ fr-BasisCanvas C-4 ⊕ M-β5 ⊕ HLG-41 + riders) — the cross-edge-3 pre-uplift leg. **ABORT SEMANTICS (declared, R-4 sibling (i)): commit #3 is SEVERABLE from the atomic transaction, and cross-edge 3's rationale IS its abort clause — the cure is landable at the CURRENT pin (installed 4.0.0's `/dom` `resolveTokenColor` per M-RTC; `./motion-curves` covers every symbol but `timingFunctions`), so it is correct standing alone and is NOT reverted if commit #4 aborts. What it may never do is presume the hop: no bare-root-specifier leg, no ≥7 token spelling, no producer-deleted class inside it. If #4 aborts, #3 stands and the wave's remaining state is exactly the pre-uplift tree plus a palette that resolves — the one outcome that is strictly better than the opening state.**
4. **`feat(fourier)!: the atomic tri-package uplift`** — the §4.4 transaction, **ONE commit**; body carries the adopted hash, the G7 figure, the G5 census result, the ESC rulings applied.
5. `docs(X·F): F.W1 close report` — gate results, G20 re-run, residues handed to F.W3/W4/W9/W10.

**Cadence:** `npx vue-tsc -b --force` (the repo's only real gate) after each integration batch and before the cut commits; `npm run build` at G4 and post-cut; `git diff --check` every commit. Evidence saved under `docs/tranches/X/fourier/evidence/w1/` (G7 grep of record with its five superseded cells · G5 census diff both directions · G4 emitted-CSS grep · G6 `git show --stat` + four e2e runs · G10 witness · G12 transcript · G13 `npm ls`/`npm ci --omit=dev` outputs · G18 assertion · the WU-F/edge-9 screenshot rows) + `F-W1-LOG.md`.

**Halt conditions (halt + escalate; no solo redispatch):**
- Any write outside §1 bounds; any second write to a path owned by another unit; any producer-tree WRITE (producer defects leave via NWO-1, never as frontend hacks).
- Non-local hard-gate failures: G6's four specs red post-cut for a cause outside `[glass-ui]` console noise (diagnosis wrong) · G11 clean but G12's probe throwing (the `as EasingFn` class wider than `bezierPresets`) · G10's witness hanging AFTER the deletion (reka's presence contract differs from the tag read) · G13 green locally but `npm ci --omit=dev` red in CI (lock/manifest disagree in an unpredicted direction).
- Third-iteration loops: G7 reaching a third grep of record · G5 turning up a fifth unmodelled class instance · the palette cure needing a third resolver shape · ESC-1 re-litigated after ruling.

**Cross-edges (declared from this end; each requires a reciprocal):**
1. **F.W0 → F.W1 (HARD predecessor).** G2 · G3 · MISS-LC2 · FR-AH-33 · M-15-as-ERRATA'd. FR-EQC-7's vaul-vue gate lands INSIDE the F.W1 transaction, not before. **G-11/G-12 quotation (R-9.1, the one-line form): F.W0 publishes ONE corrected anchor table (its gate **G-11**) and ONE corrected-denominator table (its gate **G-12**) in `fourier/docs/tranches/F/SUBSTRATE-LEDGER.md` — cited by gate id, never by line into a live sibling (R2-2; the round-1 `:199`/`:204` cites are struck as stale-by-construction); every later wave QUOTES them and no wave re-performs the re-resolution as its own act — F.W1's G1 is a quotation, superseded figures are forbidden downstream, and divergence from those tables is a defect against G-11.** Reciprocal: `F-W0.md`.
2. **F.W1 → F.W3/F.W4 (COHESION §2 law).** No fourier frontend spec assumes the uplifted tree before W1 lands it. Named consumers: B-2 slider execution · shadow-retirement targets re-derived at the adopted tag · FR-COB-2 lift-then-delete · FR-AFP guards · every cure whose producer vehicle changes at the hop (ExportModal M-α/β/γ; fr-FourierMorphSvg ruling 4). Reciprocal: `F-W3.md`, `F-W4.md`.
3. **F.W1 ↔ F.W2 — SPLIT edge, not a total order.** The `colors.ts` cure leg is landable at 0.13.0 today (M-RTC re-home to `/dom` at the CURRENT pin); only the bare-root-specifier leg gates on the uplift; `./motion-curves` at 4.0.0 covers every symbol but `timingFunctions`. The census's blanket "W1 before W2" is wrong for the colour leg. Reciprocal: `F-W2.md`.
4. **F.W1 → glass-ui BH relay (SS-6).** One batched letter, roster NWO-1. Producer rows NEVER become frontend hacks.
5. **F.W1 → LATEX-RELAY.** G16's ruling + NWO-5.
6. **F.W1 → pencil-boil producer.** G14 + NWO-6.
7. **F.W1 → X·V parser seam.** NWO-3 via the owner's mail sweep. Not a fourier row.
8. **F.W1 ⟂ X·P — FORBIDDEN edge.** Direct parse-that→fourier forbidden; parser needs route parser → value (X·V L1/L5) → packed release → fourier.
9. **F.W1 → F.W9/F.W10.** The minted visual-regression checkpoint set: `--shadow-cartoon` sign flip · cartoon hover loss ×21 · `.disclosure-content` body register · tooltip re-proportion · GCM-22 insets · Badge rim · FR-CP-13 gap · `text-admin-label` reversion · SS-13 residues.
10. **F.W1 ⟂ F.W5 (SS-4) — declared NON-edge.** TA-4 value-side atomdiff restoration is SS-4's named prerequisite; no F.W1 gate consumes the contract. Declared so the dependency is not mis-routed into the transaction. **SS-4 flags its owner rulings INLINE (trie-vs-KISS et al.) at its own spec; F.W1 carries only the non-edge.**

---

## §2·R1 — Repair-round-1 adoptions (rows of §2, appended; `planned` like every row above)

**Placement note (binding, not stylistic).** `conformance/PASS-1/RULINGS.md` pins four coordinates in THIS file and hands them to nine sibling seats repairing in the same round: `:161` (WU-K `L/B-1 + C/B-1` — F.W2's easing home, R-5), `:227` (ESC-4 — the accepting authority for the easing-drift figures, R-1d), `:276` (the transaction roster every sibling must cite whole, R-4/R-4b — pinned at round 1 as **ELEVEN-limb**, **CORRECTED TO TWELVE at repair round 2 by R2-5, once and at that same coordinate; siblings re-quote the corrected charter and no sibling re-derives the count**) and `:294` (the FR-EQC-7-vaul-vue-INSIDE pin, R-4). Inserting rows above `:294` would silently re-key all four in files this seat may not write — manufacturing the R-1f mis-keyed-coordinate class program-wide to fix a partition class. The rows below are §2 rows in force and in kind; only their byte position is deferred. Every id is banked and ORIGINAL FOR LIFE (anti-rename); every coordinate below was re-read at the bytes by this seat this session.

### §2·R1a — the R-2a/R-2c partition escapes homed here by ruling

| id (banked) | banked coordinate | landing |
|---|---|---|
| **RB-1** | `fr-GlassTimeline.md:84` | **Charter note (R-2a).** "Banked K-14/SR-1 route authorities mis-baselined (v8 facts at a 7.0.0 label); route conclusion survives." Bears directly on WU-A/G1: no F.W1 sizing may quote a 7.0.0-labelled K-14/SR-1 anchor — the route conclusion is kept, its baseline is re-derived at the adopted pin. Both r2 readers re-ratified with their own git reads. |
| **fr-EquationView M-TL** | `fr-EquationView.md:107` | **Benefit-ledger row (R-2a).** MINOR (declined per the ratified `kf-KfPillTabs:85` kill — `aria-controls` is SUPPORTED, not required, on `role=tab`). The 4.0.0 tab strip is an ARIA `tablist` with `grep -c keydown dist/tabs.js` → **0**; **F.W1 benefit — ≥7 ships `useTabRovingFocus`**. The tabpanel roles + tablist name are **F.W4's**, not this wave's. Record-qualified: `M-TL` is an EquationView id. |
| **fr-EquationView D·D-M3** | `fr-EquationView.md:65` | **Benefit-ledger row (R-2a).** **F.W1 benefit — the overlay union's coarse-pointer tap-toggle** (`MIGRATION.md:866`, seat-verified pin). The keyboard/touch inspection path itself is **F.W4's**; both touch engines → SS-13. Carries the absolute-qualification (K-10) and the `fr-CanvasControlsDock` R-9/K-9 touch qualification as its bytes — ⟨cmd⟩ `grep -o 'Do not carry "touch-dead" unqualified (K-9).' fr-CanvasControlsDock.md` → *"Do not carry "touch-dead" unqualified (K-9)."* (round 2: the round-1 cell said *"verbatim"* and produced no command, a defect per se under R2-1-LAW.2 — the label now has its receipt, §E-3·R2). |
| **FR-IC-17** | `fr-InfoCard.md:49` | **DISCHARGE REGISTER row (R-2a) — INFO, DISCHARGED-BY-UPLIFT; no work, one line.** The wave's discharge register is **THREE rows as of repair round 2**: FR-IC-17 here, **FR-CP-5** (WU-F, the disclosure-chevron snap discharged by `@utility transition-disclosure`), and **`fr-ExportModal D-M3`** (§2·R2a.1, the 16×16 ✕ made 44×44 by the producer floor — adopted by RULINGS-2 R2-3d.1; the "exactly two" count word this cell carried at round 1 is corrected here and minuted at §E-3·R2). All three are consumer no-ops **verified at the bump**, never budgeted, never credited as cures. Sibling ids at the same record line are NOT this row's: FR-IC-18 → GLASS-RELAY + F.W4; FR-IC-19 → F.W1-sequenced F.W3. |
| **MPC-29** | `fr-MorphPhaseConfig.md:91` | **INFO · F.W1 sequencing note (R-2a).** "`/morph`'s only automated gate is one screenshot row — which catches MPC-5's drift iff baseline regeneration straddles the bump." **Bears on ESC-4**: the easing-drift acceptance may not lean on `/morph`'s screenshot gate as its detector, because the gate only fires on a straddling baseline regeneration. ESC-4's acceptance re-runs the sampling (WU-K), not the screenshot. |
| **fr-SvgFilters C-S3** | `fr-SvgFilters.md:64` | **INFO context row for the bump (R-2a).** Pencil-boil is "the only `@mkbabb` dep in exact peer-compliance at HEAD" (glass-ui 4.0.0 peers `^0.4.1`, installed 0.4.1 — the seat's node read); the gap is forward-only, and the forward target it was measured against **is corrected by M-2** (WU-M/G14: the v7 floor is `^0.9.2`, `^0.11.2` is the 8.0.0 fact). |
| **fr-SvgFilters D §3 U-3** | `fr-SvgFilters.md:49` | **MAJOR · NEGATIVE-CARRY row (R-2c).** "The uplift must not carry the twice-rejected register forward under any rewire ruling" — the feTurbulence paper idiom was retired as primary at glass-ui 7.0.0 after two recorded owner rejections (⟨cmd⟩ `grep -o 'disgusting metallic" — verbatim at the tag, my git-show' fr-SvgFilters.md` → *"disgusting metallic" — verbatim at the tag, my git-show"* — the *record's* attestation of the producer bytes, produced here by command rather than asserted; this spec quotes the RECORD, never the tag it reports, R2-1-LAW.1/.2), root cause named (grey `saturate=0` tooth borrowing all warmth from the substrate); SvgFilters' grain filters are that recipe exactly. This is the design-conformance half of the uplift and it is unbanked elsewhere (fr-App banks only the class-break half). **Record-qualified `fr-SvgFilters D §3 U-3` — disambiguated from the homonymous bare `U-3` axis id F.W3's register carries (R-2c cited that spec at `:154`; this seat re-measured and the coordinate has moved under concurrent repair, so the QUALIFICATION is the disambiguator and a sibling line number is never relied on). Also distinct from `fr-SvgFilters D §3 U-2` (`fr-SvgFilters.md:36`) and from WU-C's `U-2` (fr-SpeedSelect `D §5 U-2`, `:43`).** **ROUND-2 CORRECTION of this cell's own two mis-descriptions (PASS-2 D-4, corrected in place AND minuted at §E-3·R2 so it is no defect's silent patch): (i) `D §3 U-2` is NOT carried by this spec as an id — ⟨cmd⟩ `grep -c 'D §3 U-2' F-W1.md carry/F-W1-CARRY.md` → `F-W1.md:1` (this citation itself) / `carry/F-W1-CARRY.md:0`; what the spec carries is its FOLD TARGET, `fr-App B-2` — ⟨cmd⟩ `sed -n '36p' fr-SvgFilters.md` → *"**D §3 U-2 — FOLDS BY REFERENCE → banked fr-App.md:46** (B-2, **MAJOR**, → **F.W1** with the P0 CSS-class-census gate)"*; (ii) that `B-2` is homed at **WU-D** (the P0 CSS-class census), not WU-C. The disambiguation the row exists to make is unaffected — only its two attributions were wrong. |
| **fr-PaperSearch D-14 · D-15 · D-17** | `fr-PaperSearch.md:55` | **Three token limbs of the F.W1 re-ink (R-2c)** — the cell at WU-P names only `D:B-3 · D-M1 · MISS-A6`; these three are the rest of it. Banked, by command (⟨cmd⟩ `grep -o "All \*\*F.W3/W4\*\* except D-14/D-15/D-17's token limbs (\*\*F.W1\*\* re-ink) and MISS-B3's producer limb (\*\*BH relay\*\*)." fr-PaperSearch.md`, this seat, round 2): *"All **F.W3/W4** except D-14/D-15/D-17's token limbs (**F.W1** re-ink) and MISS-B3's producer limb (**BH relay**)."* (the round-1 *"Banked verbatim:"* label carried no command — R2-1-LAW.2, cured here, §E-3·R2). D-14 = the placeholder cell recomputed independently at **1.8838:1** (three concordant instruments; `style.css:113-122`'s own `--viz-amber` block is the house's measured floor — a self-indictment); D-15/D-18 = all four theme cells recomputed to 2 d.p.; D-17 = the correct idiom verified at `PaperSidebar.vue:165-166`. **Record-qualified — `D-14` collides with WU-I's MPC `D-14` (`var()` fallbacks) and `D-17` with the dock axis ids; these three are `fr-PaperSearch` ids and are never merged with them.** |
| **AA-11** | `fr-AdminAuditLog.md:48` | **CONSTRAINT row (R-2c), seated beside G15's FR-GIG-5 non-credit bar.** Banked: "`./pagination` absent from BOTH export maps — the named carry has not landed in three majors; **F.W1 must not plan the pagination cure around it**." The footer port itself (`<nav aria-label>` + glass Buttons + `aria-live` counter, `AdminUserList.vue:428-457`) is **F.W4's**; AA-48's missing `type` attr discharges free with that cure. F.W1 owns only the negative — and G15 now names it as a discharge condition. |
| **FR-COB-28** | `fr-CanvasOverlayButton.md:87` | **RELAY-PACKET IDENTITY row (R-2c)** over WU-S/NWO-1, whose roster already reproduces the packet's members. Banked disposition: **NO-WAVE-OWNER — "one relay letter, seven items, sent at F.W0/F.W1 open by whoever holds the mail seat; this record is the packet."** Members: (1) FR-COB-7 dead border arms + pressed-suppresses-feedback · (2) FR-COB-8 the unlayered-P9-defeats-layered-a11y-resets CLASS (FR-COB-12 one instance) · (3) FR-COB-6 v7 pressed-paint removal: intentional? · (4) FR-COB-23 the 1.94:1 focus ring · (5) FR-COB-26 the generated `color-mix` fallback · (6) the `btn-glass`/`glass-btn` near-collision naming hazard (FR-COB-3's mechanism) · (7) DOCK-ACTIVE satisfied upstream, do not re-send (FR-COB-11). **With the id booked, NWO-1's "Item 7 … do not re-send" instruction finally has an anchor to be an item 7 OF.** |

### §2·R1b — escapes booked by `conformance/PASS-1/F-W1-CHECK.md` §1 outside the R-2 roster (per-wave seat; no ruling collides)

| id (banked) | banked coordinate | landing |
|---|---|---|
| **FR-CP-33** | `fr-CoefficientsPanel.md:68` | **MINOR (LC's MAJOR preserved as dissent, carried not cited).** The twins' convergence onto one disclosure chassis is **F.W3/W4's**; **"the import hygiene arm rides F.W1"** — `CollapsibleSection.vue:2`'s root-barrel `@mkbabb/glass-ui` import defeats the ~60-subpath split, and it is the same file G10 already opens, so the arm lands with the disclosure deletions and nowhere else. The uncleared 250ms `setTimeout` firing against a detached element (`:17-30`) is the F.W3/W4 half — **not** F.W1's, and not silently absorbed here. |
| **fr-CollapsibleSection i-2** | `fr-CollapsibleSection.md:78` | **INFO — F.W1 screenshot row + SS-13 input #6.** "`.disclosure-content` sets color/size/leading — a visual-diff row, small but non-zero blast radius." The mechanism is already carried at WU-F (`fr-ContourPreview 23/27/30 + FR-EQC-13`, limb (iv)) and the checkpoint is already minted at cross-edge 9; **the id joins them** (anti-rename: the mechanism was carried, the identity was not). Record-qualified against the homonymous `i-2` ids elsewhere in the corpus. |
| **fr-CollapsibleSection m-6** | `fr-CollapsibleSection.md:63` | **MINOR — rides B-1's edit (G10), no separate work.** The file's only comment (`:57-59`) directs readers to `@mkbabb/glass-ui/styles/animations.css`, "a specifier the export map does not carry … acting on it throws `ERR_PACKAGE_PATH_NOT_EXPORTED`"; the underlying mechanism is sound (`style.css:3` → `./styles` → `animations.css:18/:29`). Same false path at `ContourSettings:357`. **"The comment dies inside B-1's deletion"** — both twins, one commit. **Record-qualified: this is `fr-CollapsibleSection m-6`, NOT `fr-BasisSelector m-6` (§2·R1c), which is a different mechanism in a different file.** |
| **fr-GalleryDraftsSection i-1** | `fr-GalleryDraftsSection.md:87` | **INFO — budget rider on GAB-2 (WU-H); severity ruled at K-10.** `./metric-badge` is present at the installed/adopted pin and absent at v7.0.0 — "not broken today; the break is F.W1's." The additive half is the budget correction: the sibling banner's `label-position` ×6 / `color` ×2 have **no clean v7 equivalent**, so "7 files" reads as **1 trivial + 6 with prop loss**. GAB-2's cure (`<Metric posture="cell">`, K-4) prices against that reading, never against a rename count. |

### §2·R1c — the three declared "F.W1 sequencing rider" obligations, given their F.W1 cell

| id (banked) | banked coordinate | the F.W1 half (the only half this wave owns) |
|---|---|---|
| **fr-BasisSelector m-6** | `fr-BasisSelector.md:72` | Routed "**F.W3/W4** with F.W1 sequencing rider". The rider is the TAG, not an edit: `./icon-tooltip` "is a strict drop-in at the pin and DELETED at 7.0.0", so G1's adopted tag decides which arm F.W3 executes — the cheap re-target arm exists only against the 7.0.0 surface. F.W1 owes the ruling and the Census-FE amendment already carried at WU-G (ii); it owes no code. |
| **fr-CollapsibleSection m-7** | `fr-CollapsibleSection.md:64` | Routed "**F.W3/W4** with the F.W1 gate rider". The `ContourSettings` fork (`:255-265` + `:361-375`) "inherits B-1's hazard verbatim and **dies under the same F.W1 gate**" — G10 already names the `:361-374` twin; the rider is that fork's OTHER half (`:255-265`), which G10's five-surface close witness must therefore cover. The cure (`#title` slot + `variant`, "not discipline") is F.W3/W4's. |
| **fr-NotationPills FR-NP-6** | `fr-NotationPills.md:44` | Routed "**F.W3 + F.W1 sequencing rider**"; MAJOR, extends banked `fr-BasisSelector M-13`. F.W1's rider is a CERTIFICATION, not an edit: `./tabs` is present at all three points (80-key 4.0.0 · 74 at v7.0.0 · 69 at v8.0.0) and `v7.0.0:…/SegmentedTabs.vue` RETAINS `variant?: SegmentedTabsVariant` (`:98`, default `"pill"`, `"underline"` branch `:149`) **across the very hop that deletes Button's** — so `EquationView.vue:188`'s `variant="underline"` survives and **the hop does not gate F.W3's a11y repair**. |

### §2·R1d — cited here, booked elsewhere (one home per id — R-2's partition law)

| id | adopting wave | F.W1's relation |
|---|---|---|
| **FR-AFP-33** | **F.W3** (R-2c) | Routed `F.W1/W3` at `fr-AdminFlaggedPanel.md:87` (client flag-list collapse; `./expandable-container` installed). **F.W3 is the adopting wave; this is F.W1's citation, not a booking.** The server limb (`$push` with no `$slice`, the bare `$group` per page request, permanent-growth term FR-AFP-66) routes **F.W5–W8**; magnitude + β's sort-index note → SS-13. The counterweight is preserved: `if not flagged` short-circuits production's only case. |
| the 8-item visual-regression checkpoint set | **F.W9** home, **F.W10** terminal-verify (R-2b) | F.W1 MINTS the set (cross-edge 9) and cites the CARRY by its **cross-edge 9 row label** — `carry/F-W1-CARRY.md`'s *"F.W1 → F.W9 / F.W10"* row (⟨cmd⟩ `grep -o 'F.W1 → F.W9 / F.W10.\*\* The visual-regression checkpoint set the uplift mints with no typecheck signal' carry/F-W1-CARRY.md` → *"F.W1 → F.W9 / F.W10.** The visual-regression checkpoint set the uplift mints with no typecheck signal"*). **The round-1 `carry/F-W1-CARRY.md:246` line-cite is STRUCK at repair round 2** — R2-2 item 2 forbids a line number into any live sibling and names *the carries* in that class; the coordinate is named here as struck bytes and nothing resolves through it (§E-3·R2). It books no checkpoint row and claims no disposition. The set: `--shadow-cartoon` sign flip · `cartoon-surface` hover loss (21 sites) · `.disclosure-content` body register · tooltip re-proportion · GCM-22 `p-0` insets · Badge rim · FR-CP-13 fused-card gap · `text-admin-label` reversion. |
| **FR-NP-32 (≡ fr-PaperSidebar M1)** | producer / NO-WAVE-OWNER; **F.W10** drains it (R-7.1) | F.W1 carries it as the SEQUENCING GATE it is banked as — at NWO-1's TOP (WU-S) and in G2's premise ("F.W1 opens on a red build"). Canonical citation form per R-8: two banked witnesses of ONE fact, cite-both, never substitute. The dist-emitter fix itself is producer-owned and is excluded at §5 — a frontend workaround for it would be a wave defect. |

## §2·R2 — Repair-round-2 adoptions (rows of §2, appended; `planned` like every row above)

**Placement note (binding, not stylistic — §2·R1's discipline, re-applied).** The four coordinates `conformance/PASS-1/RULINGS.md` pinned in this file are still live and were re-read at these bytes this round (PASS-2's check re-verified all four intact): `:161` WU-K `L/B-1 + C/B-1` · `:227` ESC-4 · `:276` the transaction roster · `:294` the FR-EQC-7-vaul-vue-INSIDE pin (all *at this writing*). Every round-2 correction therefore lands either **inside an existing line** (a within-line edit shifts no byte position) or **below `:294`** — here. The rows below are §2 rows in force and in kind; only their byte position is deferred. Each books, cites, or escalates exactly ONE banked id, **record-qualified and ORIGINAL FOR LIFE** (anti-rename); none re-books an identity §2 already holds. **They mint nothing on the CARRY direction — §6's `105/105` claim is untouched**; these are REGISTRY-direction adoptions and G20 leg (b) is the run that tests them.

**Two laws govern every citation in this section and every in-place round-2 edit above it.** (i) **QUOTE-BY-COMMAND (R2-1-LAW)**: every quotation below is the pasted output of a `grep`/`sed` command run by this seat this round, with the command stated beside it as a ⟨cmd⟩ note; a "verbatim" label without a ⟨cmd⟩ note would be a defect per se. Working directory for the corpus commands is `docs/tranches/V/megatranche/registry/adjudicated/` (frozen at `35fc8ebf` — the ONE family whose line numbers remain citable); for the sibling-spec commands, `docs/tranches/X/fourier/waves/`; for the tree commands, `/Users/mkbabb/Programming/fourier-analysis` (READ-ONLY, `grep` only, no file opened for write). (ii) **STABLE ANCHORS (R2-2)**: every cross-spec receipt below cites a §-heading, gate id, clause id or row label — **never a line number into a live sibling**, which is why the sibling extractions use `grep -o` (no coordinate in the output) rather than `sed -n 'Np'`.

### §2·R2a — the three RULED adoptions (RULINGS-2 **R2-3d.1–3**; the ruling names F.W1 as the one owner of each)

| id (banked, record-qualified) | banked bytes, by command | landing in this wave |
|---|---|---|
| **`fr-ExportModal D-M3`** (⟨cmd⟩ `sed -n '51p' fr-ExportModal.md`) | *"D-M3 — the 16×16 `opacity-70` ✕ outside every coarse floor; free at F.W1 (producer 44×44 verified at `558c3fa3`)"* | **DISCHARGED-BY-UPLIFT ledger row (R2-3d.1)** — no work, one line: the producer's ≥7 control floor makes the ✕ 44×44, so the geometry MAJOR discharges at the bump and is **never budgeted and never credited as a cure** (the discharge-register law of §2·R1a). **Rider carried, not re-argued — K-16**: ⟨cmd⟩ `grep -o 'Geometry MAJOR, \`opacity-70\`, and the F.W1-free routing all survive' fr-ExportModal.md` → *"Geometry MAJOR, `opacity-70`, and the F.W1-free routing all survive"* — the WCAG 2.5.8 **headline** is KILLED on two independent grounds and **a repair spec inheriting the conformance claim would book a defect that does not exist**; this row inherits the routing and not the headline. **RECORD-QUALIFICATION MANDATORY (the record's own named hazard): this is `fr-ExportModal`'s `D-M3`, NOT WU-C's `D-M3`** (the Button budget of record, 87 `variant=` · 36 `size="icon"` · 35 files) — the homonym is what shadowed this row out of two censuses. **Register correction consequent on this adoption**: §2·R1a's FR-IC-17 cell said the discharge register is "exactly two rows"; it is now **three** (FR-IC-17 · FR-CP-5 · this) — corrected in place there and minuted at §E-3·R2. |
| **`CP-ROW-29`** — spec spelling per F-W4's established row-N idiom (⟨cmd⟩ `grep -o 'CP-ROW-[0-9]*' F-W4.md \| sort \| uniq -c` → `6 CP-ROW-40` + `1 CP-ROW-` (a bare template token); the round-2 draft's "×4" is corrected to **×6** as re-measured this round, §E-3·R2 — and the figure is a *usage count in a live sibling*, cited for the idiom's existence only, never as an authority), banked identity **`fr-ContourPreview` row 29** (⟨cmd⟩ `sed -n '62p' fr-ContourPreview.md`; **note of record: RULINGS-2 R2-3d.2 writes this row as `fr-ContourPreview.md:29` — `29` is the record's own ROW NUMBER, and the row's bytes sit at file line `:62`, measured this round. The banked identity is the row number; the frozen-corpus coordinate is `:62`**) | *"The producer tree moved 80 commits past the corpus's cited HEAD while both commits read `version: 7.0.0` … Every §7 uplift row needs a pin to the PUBLISHED artifact before F.W1 executes."* → banked disposition *"ADJUDICATED → **F.W1** (pin before the transaction)"* | **PIN-LAW row (R2-3d.2), joining the WU-A/G1 RE-PIN family by id** — the one §7 uplift-pin row this spec's set omitted while carrying the record's rows 15 · 23 · 27 · 30 · 37. It adds no act: G1 already names TAG + ADOPTED COMMIT HASH before any budget executes and M-16/FR-EQC-16/K-17 already state the five-fold drift. What the id adds is the **generalisation** — *every* §7 uplift row (not only this record's) is pinned to the PUBLISHED artifact before the transaction, which is precisely the obligation K-1's version-lying window makes non-optional. Sits under WU-A's identity; G1 is its gate. |
| **`fr-BasisCanvas D-corpus-C-1`** (⟨cmd⟩ `sed -n '105p' fr-BasisCanvas.md`) | *"`cartoon-surface` survives the 4→7 uplift (producer 7.0.0 still declares the utility and imports it into the styles entry …). The shim is a design ask, not a build break. → **F.W1** epistemic carry."* | **EPISTEMIC-CARRY row (R2-3d.3), beside WU-D's cartoon-card denominator rows** (FR-EQC-3/K-13, census of record 21 occurrences / 14 files). It is the corpus correction that keeps the `.cartoon-card` family a **design** question at the hop and not a build break — and it **does not license the migration**: KILL-6 stands unamended (`<Card tier="opaque" surface="cartoon">` is UNSPELLABLE at the target — 21 non-compiling values / 14 files), and the `cartoon-surface` target's own converse is already booked at WU-F (hover-lift loss strips real affordances among the 21). INFO; no budget; it constrains G5's reading, it does not add a class to it. Before this round it was the only PASS-2 escape unhomed **program-wide** — ⟨cmd⟩ `grep -c 'D-corpus' F-W1.md carry/F-W1-CARRY.md` → `F-W1.md:0` / `carry/F-W1-CARRY.md:0` at the pre-repair bytes. |

### §2·R2b — the remaining PASS-2 §1 escapes, booked by the per-wave seat (no ruling collides)

**Numbering is pinned, not arbitrary**: b.1–b.4 are the four whose F.W1 obligation was landed **in place** this round in the cells that own the mechanism (AA-2's cure map · WU-K's `L/B-1 + C/B-1` row · G5 ×2), and those cells cite these numbers; b.5–b.7 follow in the check's severity order. In every case the mechanism was already carried and the **identity** was not — the anti-rename defect M-25 names, cured by naming, never by re-booking.

| # | id (banked, record-qualified) | banked bytes, by command | the F.W1 half (the only half this wave owns) |
|---|---|---|---|
| **b.1** | **`fr-FunctionInput N-1`** — MAJOR (⟨cmd⟩ `sed -n '54p' fr-FunctionInput.md`) | *"the 7.0.0 landing is `emphasis="primary"` — the F.W1 cure target"*; the row's own routing reads *"**MAJOR — F.W1 (uplift semantics) + F.W3/W4 (this file's CTA) + NO-WAVE-OWNER rider (DESIGN.md correction)**"* | **AA-2's cure map gains its missing arm** — `default`→`emphasis="primary"` (landed in place at WU-C this round). `default` is NOT a no-op: it was silently demoted to the neutral glass wash by the in-flight 3.1→4.0 bump, so a mechanical carry-forward ships the demotion into ≥7 under an unchanged name — **invisible to `vue-tsc` and to the census's removed-subpath break model**, which is the same blindness AA-2 exists to defeat. **Born-RED witnesses re-derived at the tree this round**: ⟨cmd⟩ `grep -rn 'variant="default"' web/src \| wc -l` → **4** explicit sites, ⟨cmd⟩ `grep -rln 'variant="default"' web/src` → `visualization/ExportModal.vue` · `equation/FunctionInput.vue` · `morph/FourierMorphDemo.vue` · `morph/FourierShapeExtractor.vue` (plus every bare `<Button>` inheriting the default); ⟨cmd⟩ `grep -rn 'variant="solid"' web/src \| wc -l` → **0** — the producer's own intermediate landing was never adopted, so nothing in the tree records the demotion; ⟨cmd⟩ `grep -rnE 'emphasis\|ButtonEmphasis\|iconOnly' web/src \| wc -l` → **0** — AA-2's premise re-proven at the same reading. **The other two halves are NOT F.W1's**: this file's CTA re-ink is F.W3/W4's and the `DESIGN.md` house-law correction is the NO-WAVE-OWNER rider (a docs act, SS-3/SS-4 input) — booked here would be a double-home. |
| **b.2** | **`fr-EasingPicker MISSED-F`** — MINOR (⟨cmd⟩ `sed -n '89p' fr-EasingPicker.md`) | *"the F.W1 "5 sites / 6 symbols" costing double-counts: the named-import block is deletable TODAY at the current pin"* | **WU-K's `L/B-1 + C/B-1` row already carries the mechanism** (*"the named-import block is deletable TODAY (object-identity subset)"*) and §5 already kills the "5 sites / 6 symbols" cell — **the id joins both** (in place at WU-K this round; §5's kill row now names what kills it). The correction is a COSTING correction, not a cure: `easings.ts:9` and `:10-16` are two imports of the byte-identical specifier and `ANIMATION_EASINGS` is a strict content-subset of `EASING_PRESETS` by object identity, so the F.W1 figure is a SITE count and never a cost. **Partition hole closed from this end** — the sibling already cited the home and this wave never answered: **F-W2 §3 Arm A's `MISSED-F` row** (⟨cmd⟩ `grep -o 'home \*\*F\.W1\*\* costing + \*\*F\.W4\*\* hygiene' F-W2.md` → *"home **F.W1** costing + **F.W4** hygiene"*; ⟨cmd⟩ `grep -o "CITES\. \*\*A1's five-site inventory inherits the costing correction\*\*" F-W2.md` → *"CITES. **A1's five-site inventory inherits the costing correction**"*). **The no-duplicate-imports hygiene is F.W4's**, not this wave's. |
| **b.3** | **`fr-PaperSidebar M3`** — MAJOR at the record (⟨cmd⟩ `sed -n '63p' fr-PaperSidebar.md`) | *"**F.W1 pre-gate law** + SS-3/SS-4 spec input"*, whose obligation is *"the one-command discriminator (`grep -c '<class>' components.css glass-ui.css`; 0/0 ∧ absent from `web/src` ⇒ not painting) goes into the wave spec"* | **Landed at G5 in place this round as a PRE-GATE LAW** — with the seat correction the record's own two-file recipe requires: run at the installed pin against `.btn-pill`, the two named sheets return **0/0 while the class is emitted from eight other producer artefacts**, so the discriminator enters as a LAW over the WHOLE emitted roster at the adopted pin and never as a two-file grep (a shape-restricted operand is defective at authoring whatever it returns, R2-9). The **SS-3/SS-4 spec-input half is not F.W1's** — it binds the census/methodology register. |
| **b.4** | **`fr-GalleryMarquee GM-19`'s F.W1 contingency** — INFO (⟨cmd⟩ `sed -n '192p' fr-GalleryMarquee.md`) | *"decidable only by re-reading a11y-overrides.css at the F.W1 target"* | **Landed at G5 in place this round as a RE-READ OBLIGATION** — F.W1 owes the *re-read* at the target (does the reduced-motion specificity defect gain a painted consequence under the pin?), never the cure. **The identity is homed at F.W4 and is cited here, not booked** — ⟨cmd⟩ `grep -o 'GM-19 is never certified' F-W4.md` → *"GM-19 is never certified"* (**F-W4's `G-F4-NEG-ROSTER` gate**, where GM-19 falsifies a superlative ratified four times, and its SP-4 PRM cluster). A contingency owed by one wave to an identity homed in another is a citation with a deliverable, not a second home. |
| **b.5** | **`fr-PaperView C-06`** — MINOR (⟨cmd⟩ `sed -n '70p' fr-PaperView.md`) | *"six typed contracts downstream vacuous; the repo's only automated gate proves nothing here"*; routing *"**F.W1** rider + **LATEX-RELAY** (generic fix at `ref<T\|null>`)"* | **NWO-5's LATEX-RELAY packet already carries the mechanism** (*"`ComputedRef<any>` generic fix"*) — the id joins it in place this round, so the reciprocal can be run id-keyed. **The rider is the F.W1 half and it is a relay obligation, not a frontend edit**: latex-paper 0.2.1's `activeId/activeRootId: ComputedRef<any>` makes six downstream contracts vacuous and `vue-tsc` cannot see it, so no consumer-side cast may be minted here (§5's producer-owned bar — a frontend workaround for a producer defect is a wave defect). **Reciprocal named**: **F-W10 §4b's `F.W1` cross-edge row** lists it REQUIRED from this wave — ⟨cmd⟩ `` grep -o "C-06's \`ComputedRef<any>\` rider" F-W10.md `` → *"C-06's `ComputedRef<any>` rider"* — and **F-W10 §2.6, the LATEX-PAPER-RELAY roster**, holds the relay half. |
| **b.6** | **`fr-NotationPills FR-NP-30` limb (c)** — INFO (⟨cmd⟩ `sed -n '74p' fr-NotationPills.md`) | *"fixed `px-3` vs scaled height — rides M-7/i-4 at F.W1"* | **Rides WU-C's `M-7` prop-level diff and `fr-BasisSelector i-4`** — record-qualified, because `i-4` is a homonym-dense token (G4's blocked-rows cell carries it bare; it is `fr-BasisSelector`'s, ⟨cmd⟩ `sed -n '95p' fr-BasisSelector.md` → *"i-4 = DU-missed-8 — INFO. This file is the cleanest single-file proof that the census break-surface model measures the wrong dimension … Rides M-7. → **F.W1**"*). **Only limb (c) is F.W1's**: limb (a)'s zero-witness enrichment and limb (b)'s `notation`-enum fallback (which ties R6-8 → **F.W5**) are not, and the conformance-dir count is a census correction. No new geometry act — the rider is priced inside M-7's diff or it is not priced at all. |
| **b.7** | **`fr-VisualizationView §4` — the `responsive` bypass half** (id-less at the record; carried by record-qualified anchor, ⟨cmd⟩ `sed -n '103p' fr-VisualizationView.md`) | *"Method → **NO-WAVE-OWNER (SS-3/SS-4)**; bypass → F.W1/F.W4."* | **Rides WU-Q's `fr-VisualizationView L-13 / MAJ-3 / MAJ-4` row** (in place this round). **The row has two banked halves and takes two verbs, never one averaged verb** — the method half is a census input and is NOT this wave's: **F-W10 §2.5a-ii** splits it exactly as banked, ⟨cmd⟩ `grep -o 'bypass half is \*\*CITE-CONSUMING-WAVE (F.W1/F.W4)\*\* — cited, never re-booked here' F-W10.md` → *"bypass half is **CITE-CONSUMING-WAVE (F.W1/F.W4)** — cited, never re-booked here"*. F.W1's half of the bypass is the **hop question** — whether the `responsive` bypass survives the prop-value axis at ≥7 — answered by M-7's diff and nowhere else; the execution is F.W4's. Named here so its absence is not read as oversight (the check's own words for it). |

### §2·R2c — cited here, booked elsewhere (round 2; one home per id — R-2's partition law)

| id (banked) | adopting wave | F.W1's relation |
|---|---|---|
| **`fr-ConvergenceTimeline D·D-5 + C·C-5`** (⟨cmd⟩ `sed -n '56p' fr-ConvergenceTimeline.md`) | **F.W4** (RULINGS-2 **R2-3d.4**) | The banked spelling joins its already-carried mechanism at F.W4 (the `size="icon-sm"` edit under the pin + the `:108-109` deletion). **F.W1 owns only the rider, and owns it as a CITATION**: ⟨cmd⟩ `` grep -o '\*\*F.W1 rider\*\* — 7.0.0+ `iconOnly`/`data-control-target` is the post-uplift seat' fr-ConvergenceTimeline.md `` → *"**F.W1 rider** — 7.0.0+ `iconOnly`/`data-control-target` is the post-uplift seat"* — which is **G8**'s subject exactly (every migrated icon site carries `iconOnly`; the emitted sheet keys `[data-control-target]`), already gated, needing no row. The rendered-px leg at a real coarse viewport is **SS-13**'s. **G20 names this id as the residual its run must find as a citation and NOT as a booking** — a booking here would be the double-home R2-3d.4 exists to prevent. |
| **`fr-BasisSelector M-1`** (⟨cmd⟩ `grep -o 'variant re-selection rides F.W1' fr-BasisSelector.md` → *"variant re-selection rides F.W1"*) | **F.W3/W4** | ESC-7's second banked witness, cited from this end because the escalation is F.W1's and the re-selection is not: ⟨cmd⟩ `` grep -o '`outline` is the one non-glass register in the pinned 13-variant set and \*\*has no successor at 7.0.0\*\* (M-7)' fr-BasisSelector.md `` → *"`outline` is the one non-glass register in the pinned 13-variant set and **has no successor at 7.0.0** (M-7)"*. F.W1 rules ESC-7; F.W3/W4 executes the per-site re-selection the ruling releases. |

### §2·R2d — **ESC-7**, the seventh owner ruling (PASS-2 D-3; a WU-R row in force and in kind, byte position deferred per the placement note)

| id | question | blocked rows |
|---|---|---|
| **ESC-7** | **The `outline` register's retirement.** ≥7 deletes Button `variant` and `outline` is the one arm of AA-2's three-axis cure map with **NO mechanical successor** — every other arm lands by rule (`ghost`→`emphasis="quiet"`, `destructive`→`emphasis="primary" tone="destructive"`, `default`→`emphasis="primary"` per §2·R2b.1, `size="icon"`→`size="md" iconOnly`). Retire the register and re-select per site, or author a successor and ask the producer for it on NWO-1. **Design decision, owner's — flagged INLINE per WU-R's law, never presumed, never swept.** | **the 30 `variant="outline"` sites / 16 files** — ⟨cmd⟩ `grep -rn 'variant="outline"' web/src \| wc -l` → **30**, ⟨cmd⟩ `grep -rln 'variant="outline"' web/src \| wc -l` → **16** (this seat, live READ-ONLY tree, 2026-08-28), the second-largest live variant class behind `ghost`'s **49** (⟨cmd⟩ `grep -rn 'variant="ghost"' web/src \| wc -l`) · **AA-2's sweep**, which may not proceed on its own map's silence · **G7's budget**, which counts these 30 inside its `variant=` census and cannot size them until the ruling lands · **`fr-BasisSelector M-1`** (§2·R2c), whose F.W3/W4 re-selection is gated on this answer · **G6's atomicity**, since an unruled arm inside a land-or-lose transaction is an unbounded limb |

**Why an escalation and not a cure (the D-3 conviction, stated so it cannot recur).** The structurally identical design call — U-2's edgeless SelectTrigger, contra-producer — is flagged **ESC-3**; this one was left as three words inside a cure map (*"NO SUCCESSOR, design call"*) with no ESC row, no G19 entry and no §5 exclusion, which made **the wave's largest undecided design surface invisible to the gate that exists to catch exactly this**. Born-RED at **G19**: no ruling exists at authoring, and G19 now reads **ESC-1..ESC-7** (in place this round); §6's **Q** row is re-denominated with it. The escalation is recorded here rather than in WU-R's table only because inserting a row there would re-key `:276` and `:294` in nine sibling files this seat may not write.

## §5 Excluded (explicit, with reason — nothing silently dropped; every row still carried as a disposition)

| item | reason |
|---|---|
| **`.dock-separator` as a version-forced touch (K-8/R-10)** | KILL, load-bearing: category error — the class is the live paint source at BOTH pins; nothing breaks at the bump. **F.W1 must NOT budget it.** The real defect (D-6/L-4/C-6) is today-live and belongs to F.W4. |
| **FR-AFP-17's success-face cure** | Needs NO uplift (`ToastVariant` includes `'success'` at 4.0.0) — deferring it to F.W1 is a scheduling error per the banked dissent. Map success→"success" pre-uplift; only the adapter rewrite rides F.W1 (WU-G). |
| **FR-AFP-61's worst-contrast Badge cure** | Installed today at the pin (`./badge` + `./card` both exported) — booking under F.W1 is a scheduling error. The tone re-grammar stays (WU-P); the shadow retirement is F.W3's. |
| **B-2 slider-token EXECUTION** | Rides F.W3/W4; F.W1 owns only the acceptance gate G9 (per the banked routing). |
| **The transport-store direction repair** | F.W4's; F.W1 owns only the engine decision (WU-J) — the store repair must not pre-empt it. |
| **FR-EQC-10's rhythm work · FR-EQC-2's register join · FR-AFP focus-visible/row-state vocabulary · the D-18 rainbow execution** | F.W3/W4 execution riders; F.W1 takes the inventory/decision only (each seated in §2). |
| **The pagination-drain cure (FR-GIG-5) · FR-TT-9's dock-stamp cure · m-21 track-height · FR-NP-32's dist emitter fix · pressed-paint restoration (FR-COB-6) · m-17's `cn` group table** | Producer-owned — BH relay (NWO-1); never frontend hacks (standing owner edict). **A frontend workaround for a producer defect is a wave defect.** |
| **`./confirm-dialog` adoption (FR-AFP-37 · FR-GV-4 · AdminUserList)** | Absent at 7.0.0 AND 8.0.0 and gone from the producer source tree — adopting it MANUFACTURES a break row (C-M7 kill). The cure is the in-file Dialog pattern. |
| **`Metric` rename as the AnimatedDigit cure · MetricPill as a metric fallback seat** | B-4: `Metric` is explicitly noninteractive-static — the sound cure is `useAnimatedNumber` + `tabular-nums`. GAB-29 cure-falsifier: MetricPill is barrel-only at 4.0.0, no subpath, **no source at v8.0.0** — adopting it off the name is the failure the row was banked to prevent. |
| **`color2` as the palette remedy, in either form · D-4's "make `color` a getter" cure** | MG-ι: wrapped ⇒ throws; unwrapped ⇒ silently wrong channels. K-5: the getter restores reactivity onto a `#888888` resolver — the cure alone worsens the surface. The remedy is `colorUnit2 ∘ parseCSSColor` or glass's `/dom` `resolveTokenColor` (WU-E). |
| **CVA removal from the manifest** | FMD-21: `slider/index.d.ts:1` is a type-level hard edge from MorphPhaseConfig.vue:73 — removal breaks `vue-tsc -b`. Carried until the edge dies with the 8.0.0 slider; `tailwind-merge` alone genuinely dead. |
| **GalleryMarquee's `./scrolling-text` burden** | FR-GFC-9: absent at v5/v7 for a component that renders nowhere. |
| **A variant-string sweep for `--slider-scrub-*` · averaging the five Button budgets · `variant="standard"` priced as a rename** | The sweep mechanism CAUSED the defect (3-of-4 literals, 0-of-4 tokens) — G9 is a token-family grep. G7's cells measure different objects — one grep of record or nothing. MPC-21/BR-1: delete-don't-rename; pricing it as a rename revives reader-δ's MAJOR. |
| **The 4.0.0 `@utility paper-*` recipes as the `.paper-texture` cure** | fr-SvgFilters M-1/R-3: decoded and proven to paint a uniform opaque black tile. Only the token/`.paper-texture` form works at the pin. |
| **The banked D-m7 `showClose` cure cell, executed in its banked order** | fr-ExportModal M-α: `showClose` is gone; as banked it fails `vue-tsc` or restores the exact ✕ it was written to remove. Re-spelled `dismiss="deliberate"`, sequenced WITH/AFTER F.W1 (WU-F); the defect row is unchanged. |
| **"7 minors"-derived pencil-boil risk weight; the `^0.11.2` figure at a v7 target** | M-2/G14: the v7 floor is `^0.9.2`; both wanted cures land there, signatures unchanged — a no-break at this site. |
| **Killed/superseded cells (preserved as dissent history, never as budget)** | D/M-6's "250ms via `--spring-smooth-settle`" · the D-axis §6 "`p-0` overrides nothing" · GCM-22 if ESC-1 adopts 8 · D's "CENSUS §5 typography addendum" (dies with FR-FG-20) · the "5 sites / 6 symbols" easings costing (killed by banked `fr-EasingPicker MISSED-F`, §2·R2b.2 — a SITE count is not a cost) · lane-frontend's 25 and the shim's 14 cartoon denominators · K-12's 38-icon figure and 12/8 · PAW-1's consumer-alias limb (K-9) · the BLOCKER inflations at M-α6 (K-6/K-11) and the paper-search re-filings (K4). Each killed at adjudication with its receipt. |
| **`web/dist/**` as evidence** | Stale (Jun 12, five days pre-installed-producer, provably divergent). Every chunk-split and emission decision takes a FRESH build (G4). |
| **MG-ι's value.js-side `color2` dispatch defect** | Cross-repo → V·π / parser-seam (NWO-3); not a fourier row, no F.W1 budget. |
| **TA-4 atomdiff restoration** | SS-4/F.W5's named prerequisite — declared NON-edge (cross-edge 10). |
| **`web/src/components/paper/**`** | Outside bounds UNLESS G16 seats the LATEX-PAPER carry here — the exclusion is conditional on G16's ruling, and the conditionality is the row. |
| **PathPreview.vue** | Certified nil on every F.W1 census row (PP-#7) — the one file the wave need not open. |
| **Charter same-commit locks whose rows land elsewhere** | fr-PaperSearchModal D-1+D-3+`outline:none` triple and FR-MSP-6's two-channel lock: their rows are NOT in `carry/F-W1-CARRY.md` (PSM-36 certifies PaperSearchModal's sole F.W1 exposure = the lucide rename) — the locks are carried where those rows land (F.W3/W4), declared here so they are not lost. |
| **Any dated registry record** | E-3 — corrections travel as new rows (M-2, K-1, L-i3 corrections are ledgered in §2, source records untouched). |

---

## §6 Closure roster (the G20 CLAIM — every `carry/F-W1-CARRY.md` row id → landing site; the run is G20's, at ratification)

**A**: AA-2·FR-COB-2·D-M3·M-7·FR-EQR-1·FR-EQR-3·FR-COB-19·U-2 → WU-C (+G6/G7/G8; U-2→ESC-3). **B**: M-16/FR-EQC-16/K-17 → WU-A/G1 · K-1 → WU-A · FR-EQR-33(c)/GAB-13 → WU-B/G3. **C**: B-2(.paper-texture)·MG-β·FR-EQC-3/K-13·FR-NP-13 → WU-D/G5 (+G17, ESC-5). **D**: D-1/BC-1/C-1 · D-2/BC-5/C-3 · C-4 · fr-BasisCanvas C-4 · M-β5 · L-7/m-9/M-β6/M-α5 · HLG-41 → WU-E (+cross-edge 3). **E**: B-1(G10) · B-4(G11) · FM-2 · FR-COB-6 · fr-GalleryCard D-21 · GCM-22 · G-DU6/GCM-50 · fr-GalleryCard D-20 · fr-ContourPreview 23/27/30+FR-EQC-13 · FR-CP-5 · FR-CP-13 · fr-ExportModal M-α/M-β/M-γ · FR-TT-5/-6/-9/-14/-24 → WU-F. **F**: Census FE §5/C-4-census (L-13/K-14) · dock deletions D-12+L-7 · Popover union D-2/C-2 · FR-AH-13 · FR-AH-22/FR-AH-24 · FR-AUL-10+FR-AUL-45 → WU-G; `.dock-separator` kill (K-8/R-10) → §5. **G**: GAB-2 · D·D-M10 · FR-IC-7/-8/-10/FR-COB-10 · FR-AFP-18/FR-GFC-26/m-2/D-27/FR-MSP-11 → WU-H. **H**: B-2(scrub)·MPC-10·MPC-31(+MPC-13/MPC-22/D-14)·MPC-21/BR-1·K-8/SR-1·m-21·PD-1(G18)·MPC-32 → WU-I/G9. **I**: C-13/R-16 · C-11 · fr-App C-11 · MPC-14/C/B-2 → WU-J. **J**: L/B-1+C/B-1(G12) · R2-2(WU-A) · MG-ι/C-2 · R-18/C-11/FR-CP-15 → WU-K. **K**: C-1=C-8 · MISS-LC2 · C-3 · FR-EQC-7 · PP-REDGATE · PAW-33/C-19/MF-12/C-15+C-16/C-13 · C-13(raw TS) → WU-L/G13. **L**: M-2(G14) · C-18.2 · L-2 · L-i3/C-18.1 → WU-M. **M**: R3-10 (FR-CP-42) · D·D-M11 → WU-N. **N**: M-α6 · m-3/F-3 · FR-COB-24 · D-18/C-13·D-20·D-26 → WU-O. **O**: FR-AFP-3+FR-AFP-46+FR-AFP-61 · FR-AFP-19/-20/-27/-44/M-6 · FR-AFP-28/FR-AUL-19 · FR-AFP-37 · FR-AFP-16/M-4/GCM-13/FR-USB-7/FR-USB-18/FR-EQR-13 · paper-search re-inks D:B-3/D-M1/MISS-A6 · FR-USB-3/FR-USB-35/FR-COB-13/FR-MSP-5/FR-EMT-14 · FR-COB-16/D-13/GAB-5/MPC-16/FR-EQC-5 → WU-P. **P**: FR-GIG-5(G15) · PAW-1/PAW-37/PAW-51(G16) · FR-FG-20 · measured zeros D:U-2/PSM-36/PP-#7/S-3/PAW-37 · fr-VisualizationView L-13/MAJ-3/MAJ-4 · FR-EQC-2 → WU-Q. **Q**: ESC-1..**ESC-7** → WU-R/G19 (**ESC-7 seated at §2·R2d**, byte position deferred; ESC-1..ESC-6 in WU-R's table). **R**: NWO-1..NWO-6 → WU-S. **105/105 claimed on the carry direction; reverse: every §2/§5 row cites a `carry/F-W1-CARRY.md` id, and every §2·R1 / §2·R2 row cites a banked `fr-*` id adopted in the REGISTRY direction (repair rounds 1 and 2) — no spec-minted rows exist, and the round-2 adoptions add nothing to the 105. Both directions are TESTED at G20 by a seat other than this author; §6, §6·R1, §2·R1 and §2·R2 are the claim under test.**

### §6·R1 — the 42 carry-fold ids, NAMED (so the id-keyed census runs from this spec alone)

Each id below is routed to F.W1 by its record, is present by bytes in `carry/F-W1-CARRY.md`, and sits under an identity §2 carries — none is an escape. They were reachable only through this spec's ellipses ("… fr-EasingCurvePreview", "… fr-GalleryView C·C-1", "… fr-ImageUpload", "witnesses folded per cell"), which made G20's leg (a) unrunnable from the spec. Naming them costs no disposition and mints no row: **the identity home stays where §2 put it; this is the roster, not a re-booking.** Source of the enumeration: `conformance/PASS-1/F-W1-CHECK.md` §2 (2026-08-28), re-checked against the records.

`fr-App L-9` (`:75`) · `fr-BasisSelector M-13` (`:62`) · `fr-CanvasControlsDock C-27` (`:95`) · `fr-CanvasOverlayButton FR-COB-21` (`:75`) · `fr-CoefficientsSpectrum M-5` (`:57`) · `fr-CoefficientsSpectrum m-8` (`:79`) · `fr-CollapsibleSection F-2` (`:43`) · `fr-ContourPreview 37` (`:70`) · `fr-ContourSettings D-B1` (`:49`) · `fr-ContourSettings C-5` (`:51`) · `fr-ConvergenceTimeline D·D-2` (`:54`) · `fr-DarkModeToggle L-3` (`:56`) · `fr-DarkModeToggle r2 missed #5` (`:66`) · `fr-DarkModeToggle I-2` (`:110`) · `fr-EquationModeToggle FR-EMT-8` (`:38`) · `fr-EquationModeToggle FR-EMT-22` (`:52`) · `fr-EquationResult FR-EQR-2` (`:37`) · `fr-FullscreenViewer FB-3` (`:38`) · `fr-GalleryAdminBanner GAB-11` (`:50`) · `fr-GalleryCard L·I-1 / C·I-1` (`:82`) · `fr-GalleryCardModal GCM-20` (`:66`) · `fr-GalleryCardModal GCM-21` (`:67`) · `fr-GalleryCardModal GCM-53` (`:109`) · `fr-GalleryDraftsSection M-5` (`:57`) · `fr-GalleryFeaturedCarousel FR-GFC-29` (`:66`) · `fr-GalleryFeaturedCarousel FOLD-2` (`:71`) · `fr-GalleryMarquee GM-F7` (`:35`) · `fr-GallerySearchBar FR-GSB-3` (`:32`) · `fr-GallerySearchBar FR-GSB-4` (`:33`) · `fr-GallerySearchBar FR-GSB-9` (`:38`) · `fr-HarmonicLevelGrid HLG-5` (`:51`) · `fr-NotationPills FR-NP-5` (`:43`) · `fr-NotationPills FR-NP-12` (`:53`) · `fr-PaperSearchDropdown D:I-2` (`:43`) · `fr-PaperSearchModal PSM-33` (`:76`) · `fr-PaperView D/B-3` (`:45`) · `fr-PaperView C-03` (`:69`) · `fr-PaperView C-14` (`:106`) · `fr-PaperView Census §1 pins + G33 (C-03 AMENDED)` (`:194`) · `fr-SliderControl R-11` (`:40`) · `fr-VisualizationView L-3` (`:44`) · `fr-VisualizationView MAJ-6` (`:60`). **= 42.**

**Homonyms carried record-qualified, never merged**: `M-5` (fr-CoefficientsSpectrum ≠ fr-GalleryDraftsSection) · `L-3` (fr-DarkModeToggle ≠ fr-VisualizationView) · `C-5` (fr-ContourSettings ≠ fr-BasisSelector's `m-6 = C-5`) · `C-11`, `C-13`, `C-2`, `M-13`, `m-6` across §2 generally. An unqualified short token is not an id.

### §E-3·R1 — dated errata (corrections travel as rows; no dated artefact is rewritten)

| date | correction | why it is a row and not an edit |
|---|---|---|
| 2026-08-28 | **§1 bounds re-resolved against the live tree.** Eight product paths were transcribed from `carry/F-W1-CARRY.md` §Bounds and do not exist — three of the directories they name (`components/dock/`, `components/gallery/`, `components/animation/`) have never existed at any point the 66-record corpus audits (`git ls-tree cd26c65 web/src/components/` → `decorative equation layout morph paper shared ui visualization`). Four of the eight doubled as gate witnesses (G6's `copied` triple, G10's twin, G11's animated-digit, the metric-badge roster). §1 now carries the measured spellings; the CARRY keeps its bytes. | §1 is the write-authorisation surface and the halt condition is "any write outside §1 bounds" — a bound that cannot be written to as spelled is not a bound. Transcription-only is DEFECTIVE (M-25); the cure is measurement, and the dated ledger is not the place it lands. |
| 2026-08-28 | **`carry/F-W1-CARRY.md`'s `fr-NAME:LINE` provenance anchors are a mixed family.** One family resolves (`fr-AdminAuditLog:36` · `fr-App:66-75` · `fr-BasisCanvas:100-111` · `fr-BasisSelector:56` · `fr-CanvasControlsDock:139`); a second does not — 16 sampled anchors exceed their records' true line counts (`fr-PaperSearch:671` vs 92 lines · `fr-NotationPills:651` vs 132 · `fr-EquationResult:355` vs 129 · `fr-EqCoefficientsPanel:322/:323/:325` vs 129 · `fr-EquationView:363/:367/:369` vs 301 · `fr-ExportModal:380` vs 104 · `fr-FourierMorphSvg:406` vs 105 · `fr-FourierShapeExtractor:422/:423` vs 157 · `fr-CoefficientsSpectrum:187` vs 159 · `fr-CollapsibleSection:204/:207` vs 133 · `fr-ContourEditorCanvas:221` vs 162 · `fr-ConvergenceLegend:255` vs 176 · `fr-ConvergenceTimeline:273` vs 155 · `fr-EasingPicker:295` vs 157 · `fr-CanvasOverlayButton:153/:154/:159` vs 133 · `fr-CoefficientsPanel:170/:172/:174` vs 137). **G20 is re-based accordingly: its operands are the CARRY's 105 ROW IDS (which do resolve, both directions) and the registry census at `conformance/PASS-1/F-W1-CHECK.md` §3 — never the unresolved anchors.** Every anchor this spec relies on for a gate is re-cited to a coordinate this seat read at the bytes. | E-3: dated evidence is never rewritten. The defect is recorded, its blast radius is bounded (provenance only — no disposition rests on it), and the gate that would have inherited it is re-based instead. |
| 2026-08-28 | **G-11/G-12 ownership.** F.W0 owns the corrected anchor table and the corrected-denominator table (its gates **G-11**/**G-12**, landing in `fourier/docs/tranches/F/SUBSTRATE-LEDGER.md`, `create`-marked in **F-W0 §2a File bounds**). G1 and cross-edge 1 are re-labelled QUOTATIONS; F.W1 re-performs nothing. The producer-pin chain is **G-13**'s and now carries a sixth drift (`207bf174`, 2026-08-28) under F.W0's own dated addendum — per **P-4** the pin cell is a COMMIT HASH, never a version label. **[ADDRESSES CONVERTED at repair round 2, R2-2 — words unchanged; the struck `:199`/`:204`/`:209`/`:63` cites are superseded, see §E-3·R2.]** | Two waves independently owning one re-resolution is the defect; the cure is one owner and nine quotations, not a better second table. |
| 2026-08-28 | **G15 and G20 restated born-RED.** G15 previously flipped GREEN on this spec's own prose ("the footnote now exists … flips GREEN at ratification") and G20 stamped "verified by enumeration at authoring" inside a `planned` file. Both conditions are re-cut against artefacts that do not exist at authoring, and the verification verb is struck. The four-verb table is unchanged: VERIFIED = NO, stamped at X·F sub-tranche close, never here. | A gate an author can satisfy by writing is not a gate, and a `planned` file may not assert a completed verification act. Born-RED is a property of the witness, not of the wording. |
| 2026-08-28 | **Registry direction opened (R-2/R-3.1).** This spec previously closed only CARRY→spec. The corpus→spec direction is where the 15 ids escaped; §2·R1 books the ones ruled here, cites the ones ruled elsewhere, and G20 leg (b) now runs it. | The CARRY was a faithful ledger of a smaller set; closure against a ledger is not closure against the registry. |

### §E-3·R2 — dated errata, repair round 2 (corrections travel as rows; no dated artefact is rewritten)

**Scope note.** Round 2's corrections divide in two, and the division is the discipline. Corrections that would have SHIFTED bytes land as new rows (§2·R2 above). Corrections that are **within-line** — a struck coordinate, a clipped quote, a wrong count word — land in place at the cell that carries the error, because a stale figure left standing beside a correction row is a second defect; each such in-place edit is minuted here so that none of them is a defect's silent patch. Nothing below rewrites a dated *artefact*: the CARRY, the 66 records, `conformance/PASS-1/*` and `conformance/PASS-2/*` are untouched.

| date | correction | why it is a row and not a silent edit |
|---|---|---|
| 2026-08-28 | **The four F.W0 quotation coordinates are STRUCK and re-addressed by gate id (PASS-2 D-1, executing R2-2).** `F-W0.md:199`/`:204`/`:209` (G-11 / G-12 / G-13) and `:63` (the `SUBSTRATE-LEDGER.md` `create` row) — **named here as STRUCK BYTES, the subject of this erratum and not a citation; nothing in this file resolves through them** — were cited at G1, at §4 step 2, at cross-edge 1 and in §E-3·R1's G-11/G-12 row. F.W0's own repair round shifted its bytes under those cites, and at PASS-2 they addressed a blank line, two "Owning rows" cells and the wrong ledger row. **All four now cite F.W0's gates by ID** (`G-11` · `G-12` · `G-13`) and the ledger row by its §-heading (**F-W0 §2a File bounds, the `SUBSTRATE-LEDGER.md` row**), re-produced by command rather than by coordinate — ⟨cmd⟩ `grep -o 'SUBSTRATE-LEDGER\.md.\{4\}\*\*create\*\*' F-W0.md` → `` SUBSTRATE-LEDGER.md` \| **create** `` (output pipe table-escaped for rendering; the pattern carries none). **The substance was never wrong**: G-11/G-12/G-13 are real, F.W0 owns them, and the `207bf174` sixth-drift addendum is genuine — only the addresses failed. **Standing consequence: this file writes no line number into any live sibling from this round forward** (R2-2 item 4). | An authority that cannot be produced at its cited coordinate is re-anchored or deleted, never carried (R-1). But the *class* is not cured per-coordinate — ten seats shifting lines under one another's citations is what manufactured it, so the cure is the IDIOM: stable anchors, and line numbers only for the frozen `fr-*` corpus and this file's own "at this writing" coordinates. A conversion that changed a quoted word would be a different act; none did. |
| 2026-08-28 | **The transaction roster's count word: ELEVEN → TWELVE (PASS-2 D-2, executing R2-5).** `:276` *at this writing* is the single most-cited coordinate in X·F — nine sibling specs carry the R-4b sentence citing the roster **whole**. Its own top-level `+` separators enumerate twelve conjuncts; the count word said eleven. Corrected **ONCE and only at that coordinate**, with the twelfth limb's warrant named (the FR-EQC-7 vaul-vue manifest gate, returned INSIDE the transaction at repair round 1 per the `:294` pin). §2·R1's placement note and the G5 landing cell's "not a THIRTEENTH limb" disclaimer are re-denominated with it. **No sibling re-derives the count; every sibling re-quotes the corrected charter.** | A roster nine specs cite whole must be countable, and the disagreement was between the count word and the list — not between two measurements. The count is corrected where the list lives, because a roster corrected in nine places is nine rosters. R-4 reached "eleven" only by fusing the last two conjuncts, a grouping this text's separators do not mark. |
| 2026-08-28 | **`outline` is escalated, not decided (PASS-2 D-3).** AA-2's cure map carried *"NO SUCCESSOR, design call"* with no ESC row, no G19 entry and no §5 exclusion — the wave's largest undecided design surface, invisible to the gate built to catch it. **ESC-7 is seated at §2·R2d**; G19 now reads ESC-1..ESC-7 and carries the live measure (30 sites / 16 files, against `ghost`'s 49); WU-R's heading and §6's **Q** row are re-denominated. The measurement is re-derived at the tree this round, not inherited. | A design call inside a land-or-lose transaction is an unbounded limb. The structurally identical call (U-2's edgeless trigger) was already ESC-3, so the omission was an inconsistency inside this file, not a judgment. Escalations are flagged INLINE, never presumed — WU-R's own law, applied to WU-R's own gap. |
| 2026-08-28 | **§2·R1a's `fr-SvgFilters D §3 U-3` cell mis-described this spec's own contents, twice (PASS-2 D-4).** It claimed `D §3 U-2` is *"already carried"* by this spec (it is not — what is carried is its FOLD TARGET, `fr-App B-2`) and homed that `B-2` at **WU-C** (it is **WU-D**'s, the P0 CSS-class census). Both corrected in place at the cell, each with its ⟨cmd⟩ receipt. **The disambiguation the row exists to perform is unaffected** — the record-qualified spelling `fr-SvgFilters D §3 U-3` still separates it from the bare `U-3` axis id F.W3 carries and from `fr-SpeedSelect D §5 U-2`. | A cell whose job is to disarm homonyms cannot itself mis-attribute two ids; the error was in the disambiguation's *examples*, not in its conclusion, and saying so precisely is what keeps the conclusion. |
| 2026-08-28 | **The `162` attribute figure enters G7's reconciliation set (PASS-2 D-5).** G6 and §4 step 4 charter a "162-attribute" rewrite — banked, produced by command this round (⟨cmd⟩ `grep -o '162-attribute surface is compiler-visible' fr-EquationResult.md` → *"162-attribute surface is compiler-visible"*; ⟨cmd⟩ `grep -o '162-site prop rewrite must land in the SAME change' fr-EquationResult.md` → *"162-site prop rewrite must land in the SAME change"* — **the record spells the same figure two ways, `162-attribute` and `162-site`, which is itself why the counting unit must be stated beside it**), never fabricated — while G7 enumerated five disagreeing budget cells and `162` was not among them, so the wave's own *"one grep of record or nothing"* law did not reach the number the transaction names. G7 now states **SIX** figures and records *why* the sixth is a different object: it counts **attributes**, where D-M3's budget of record counts `variant=`+`size="icon"` occurrences over files. **The limb is sized by G7's output; `162` may be quoted only as an entry in that set.** | Stating the counting unit beside every figure is F.W0 G-11's own law; a figure outside the reconciliation set is an unreconciled budget wearing the transaction's authority. Adding it to the set is not averaging — it is the opposite. |
| 2026-08-28 | **Census citation form: "§4.2" → §4 item 2 (PASS-2 D-6).** `CENSUS-2026-08-03.md`'s `## §4 — Candidate wave-shape sketch` is a numbered list with no `§4.2`; the F.W1 item is **§4 item 2**. R-1f cured this exact idiom for F.W0 at round 1 and this file re-imported it. Corrected at the provenance line. | A section spelling that does not exist in the cited file is the same defect class as a drifted line number: an address that resolves to nothing. It is cured once, at the only place the spec spells it. |
| 2026-08-28 | **Two clipped/short coordinates corrected (PASS-2 D-7).** (i) G6 rendered `scripts/e2e.sh:102` as `` `npx vite web` ``; the coordinate was right and the quote was clipped — the line is now pasted whole by command, ⟨cmd⟩ `sed -n '102p' scripts/e2e.sh`. (ii) The root-font fork is `style.css:40-50`, not `:40-49` — the `@media (min-width: 768px)` arm's closing brace is `:50` (re-read at the tree this round: `html` block `:40-43`, media arm `:45-50`). Corrected at **§1** *and* at **WU-O's FR-COB-24 row**, the two places the spec spells it. | An elision inside a quotation must be marked, never absorbed — a clipped quote asserts bytes the file does not have. A range that stops one line short of its closing brace under-describes a bound, and §1 is the write-authorisation surface. |
| 2026-08-28 | **The discharge register grew from two rows to three.** §2·R1a's FR-IC-17 cell asserted the register is *"exactly two rows"*; `fr-ExportModal D-M3`'s adoption at §2·R2a.1 makes it three. Corrected in place at that cell. | A count word in a cell that another wave may cite is load-bearing; leaving "exactly two" standing beside a third row would make the round-2 adoption read as a violation of the round-1 law instead of an addition under it. |
| 2026-08-28 | **The last line-number cite into a live sibling is STRUCK — `carry/F-W1-CARRY.md:246` at §2·R1d (R2-2 item 2, which names *the carries* in the forbidden class).** The checkpoint-set row now cites the CARRY's **cross-edge 9 row label** (*"F.W1 → F.W9 / F.W10"*), reproduced by ⟨cmd⟩. The coordinate survives here as struck bytes only; nothing in this file resolves through it. **Sweep of record, this seat: ⟨cmd⟩ `grep -noE '(F-W[0-9]+\|KF-W[0-9]+\|COHESION\|CENSUS-2026-08-03\|F-W1-CARRY\|F-W4-CARRY)\.md:[0-9]+' F-W1.md` returns hits in exactly **two non-address classes, and no third**: (i) `F-W0.md:199` and `F-W1-CARRY.md:246`, which occur ONLY inside the erratum sentences that strike them (this row and the F.W0 row above) — a struck coordinate is the subject of an erratum, never a citation; (ii) `grep -c` outputs of the form `F-W1.md:1` / `carry/F-W1-CARRY.md:0`, which are command RESULTS pasted as evidence. **No hit is a citation.** *(The classification is stated, not a hit count: this row's own text reproduces both struck coordinates, so any count word here would be self-invalidating the moment the row is edited — the same brittleness that staled ~60 cites at round 1.)* **No live-sibling coordinate in this file is load-bearing.** The struck F.W0 set is four (`:199`/`:204`/`:209`/`:63`); only the first carries a filename prefix, which is why the sweep counts one.** | The idiom, not the coordinate, was the defect (R2-2). Round 2's own §2·R2 was written stable-idiom from the first byte, but a round-1 carry cite survived below it — and a file that forbids the class in its masthead while carrying one instance teaches the exception. |
| 2026-08-28 | **A banked LOCK was carried tightened; it is now carried as its bytes (WU-Q's PAW row).** The spec said PAW-44 *"restores the sticky stack only WITH the background cure"*; banked **LAW-3** is ⟨cmd⟩-produced as *"PAW-44 restoration lands WITH-or-AFTER PAW-1 + PAW-30, never before."* Two independent departures: the disjunction `WITH-or-AFTER` was narrowed to `only WITH`, and **PAW-30 was dropped as a second named predecessor**. Both restored; the round-1 spelling struck. | PASS-2 §3 recorded this as a paraphrase whose "substance [is] preserved, ordering slightly tightened" and declined to convict. A tightened lock is not a preserved lock: `only WITH` forbids an ordering the adjudicator expressly allowed, so an executing seat obeying this spec would violate the bank while believing it complied. Locks are carried verbatim or not carried. |
| 2026-08-28 | **Four "verbatim"/banked labels gained the ⟨cmd⟩ receipts R2-1-LAW.2 makes constitutive, and one composite was un-fused (R2-1-LAW.4).** (i) WU-I's **MPC-31 ONE-CUT LAW** carried a *"(verbatim)"* label over a two-clause COMPRESSION of one banked sentence — the whole sentence now stands, by command. (ii) §2·R1a's `fr-PaperSearch` cell (*"Banked verbatim:"*), (iii) its `fr-SvgFilters D §3 U-3` cell (*"disgusting metallic", verbatim at the tag* — re-keyed so this spec quotes the RECORD's attestation, never the producer tag it reports), (iv) its `fr-EquationView D·D-M3` cell (*"touch qualification verbatim"*), and (v) WU-S's NWO-1 (*"roster verbatim from the CARRY"* → the CARRY's own row label) each gained a command or lost the claim. The `162` figure's erratum row below likewise now carries its two receipts. | R2-1-LAW.2 is not a documentation preference: *"a 'verbatim' label without a ⟨cmd⟩ note is a defect per se, whatever the words say."* Every one of these five happened to be TRUE at the bytes — which is exactly the condition under which the class is invisible, and exactly why the ruling made the receipt, not the accuracy, the test. The convicted class this pass was quotes typed from memory of a ruling; an unreceipted true quote is the same act that happened to land. |
| 2026-08-28 | **Three measured count words corrected at the cells that state them.** (i) **G5**: the `.btn-pill` emission roster is **EIGHT** producer artefacts, not "seven" — the round-2 draft's count word contradicted the eight-name list pasted beside it and §2·R2b.3's own figure; re-measured this round, ⟨cmd⟩ `grep -rln 'btn-pill' … \| wc -l` → 8. (ii) **§2·R2a.2**: the `CP-ROW-` idiom's usage count in F-W4 is **×6**, not "×4". (iii) **G20**: the superseding census is **PASS-2 §4**, not "PASS-2 §3" (§3 there is *"WHAT VERIFIED CLEAN"*). | Each is a figure the round-2 draft asserted beside a command whose output disagreed with it — the identical shape as the defect this whole round exists to close, committed inside the repair. A gate whose operand roster is under-counted is a gate that can be satisfied by covering less than it names. |
| 2026-08-28 | **The F.W0 `create`-row ⟨cmd⟩ was UNRUNNABLE AS PRINTED and broke the G1 gate row; both are cured.** The round-2 draft produced the `SUBSTRATE-LEDGER.md` row with a grep pattern containing a literal `\|`. Two consequences, both measured this round: (a) inside a markdown table cell the raw pipe split **G1's row into seven cells against a five-cell header**, so the wave's first gate did not render as a gate; (b) escaping it to `\|` — the form the §E-3·R2 F.W0 row carried — makes it **BRE alternation**, and the command then returns *" **create**"* fragments and a bare path, never the row it claims to produce. Both sites now use the pipe-free `grep -o 'SUBSTRATE-LEDGER\.md.\{4\}\*\*create\*\*' F-W0.md`, re-run here. **Sweep: ⟨cmd⟩ a per-table pipe-count pass over every table in this file returns 0 mismatched rows.** | This is R2-1-LAW's failure mode one layer down: the receipt was *present* and the quoted bytes were *true*, but the command as printed could not produce them — a ⟨cmd⟩ note that does not run is a "verbatim" label wearing a costume. A provenance note is only provenance if the next seat can paste it. And a gate whose row does not render is not a gate a reader can check. |
| 2026-08-28 | **WU-M's `lane-frontend.md:481` is now commit-qualified (`@ 64aca0a2`) and re-stated as a correction TARGET.** R2-2 item 2 permits a line number only for the frozen corpus, for files cited WITH a commit hash, and for this file's own "at this writing" coordinates; a bare coordinate into a formation census qualified as none of the three. Re-read at that commit: the cell labels *"4 sites"* over a three-path list, which is the disagreement L-i3 books. | The row's coordinate is the object being corrected, not an authority being leaned on — but the distinction has to be *written*, because the next seat reads the address, not the intent. Naming the commit makes the citation lawful and makes the correction checkable at a fixed artefact after the census moves. |

---

## §7 Archaeology (why the guardrails have these shapes)

The prior attempt is the **uncommitted `m/w1-bump-migration` working tree** — the 28 dirty paths every "at the pin" statement in the 66-record corpus actually describes (FR-EQR-33(c)/GAB-13). Its failure mode is the one this wave is built against: a bump was begun; a rename sweep ran over a **variant string** instead of a **token family** (3-of-4 literals, 0-of-4 declarations — B-2's mechanism); the lock was regenerated and dropped a required peer (FR-EQC-7); and the whole thing sat un-landed for months while five audits measured it as if it were the pin. Guardrails: G3 lands or explicitly baselines the tree before any budget commits; G9 makes acceptance a token-family grep; G13 makes the lock reproducible; G6 makes the transaction indivisible so a half-landed bump cannot exist again.
