# X.F.W7 — Trie-like variant compression: the R-4 greenfield (OWNER-GATED ⊙)

**Sub-tranche**: X·F (`docs/tranches/X/fourier/waves/`), authority `docs/tranches/X/COHESION.md` §0/§1 SS-4, §0a boundary. **Half**: library. **Status**: **planned**.

**Fold provenance (M-23 / M-12)**: this file is the fold-seat output, 2026-08-28. Its census operands are **in-tree authorities only**: **at round 4, `conformance/CENSUS-CANONICAL.md` — the census of record — ⊕ the 66 adjudicated `fr-*.md` at `docs/tranches/V/megatranche/registry/adjudicated/`, and nothing else.** ▲ **STRUCK AT ROUND 4 (R4-3 / R4-10, enforcing R3-4; `PASS-4/F-W7-CHECK.md` D-5)**: through pass 3 this sentence continued *"and this wave's conformance registers `conformance/PASS-1/F-W7-CHECK.md` and `conformance/PASS-2/F-W7-CHECK.md`"* — **naming check files as OPERANDS, which the standing law forbids in terms**: a check file is a measurement and a cure-shape, citable as a **prior run**, never as the thing a census is derived from. Those two registers are re-named here as **prior runs** and are operands of nothing. The clean result the executed census produced does not cure the declaration — R2-9's own rule, turned on this file. **There is no `F-W7-CARRY.md`** — `carry/` holds `F-W1-CARRY.md` and `F-W4-CARRY.md` and nothing else (X·F RULINGS R-3), so no count, quotation or closure gate in this file rests on a per-wave carry ledger; the earlier draft's "verified row-for-row against the CARRY authority · 11 of 11 rows" provenance is **struck as a false receipt over a nonexistent artefact** and survives only as this historical note. Every value-side and fourier-side probe below was **re-run read-only by this seat 2026-08-28 and every figure reproduced** (the guardrail bytes and their line-7 twin citation; 28 dirty entries / HEAD `cd26c653` / pin `14d83356` fatal; the one-comment atomdiff hit; the absent `api/src/lib`; the single opposite-word `Merkle property` hit; the token-probe `F.W7` ∅ over all 66). Three rows are **added by measurement** and marked ⟨NEW⟩; two of those plus one §8 line **correct the founding lane evidence** and are marked ⟨CC⟩.

**Repair round 1 (X·F L-20, 2026-08-28)** — this seat re-measured `waves/`: it holds **11 files** (`F-W0 · F-W1 · F-W2 · F-W3 · F-W4 · F-W5 · F-W6 · F-W7 · F-W8 · F-W9 · F-W10`), so the earlier "F-W0 and F-W6 do not exist" reading is retired. The §5 count is re-stated over the counted table (§5); §8 gains the L-5 exclusion row; the four mis-keyed F.W5 clause ids are re-keyed. *(Round 1's own sibling coordinates are struck below — round 2 abolished them.)*

**Repair round 2 (X·F L-20, 2026-08-28) — THE ANCHOR IDIOM, applied without exception, and the eight substantive corrections.** Round 1 cited nine sibling specs **by line** while all nine were being repaired inside the same four minutes; every one of those addresses moved. Round 2's *first* pass at this file then repeated the failure in miniature — it was written at **14:40:44** and the ten siblings were re-written at **14:40:39–14:41:11** around it — and its *second* pass repeated it a **third** time: this file was written at **15:26:17** while `F-W5.md` (15:26:31) and `F-W2.md` (15:26:30) were still being written **after** it (⟨cmd⟩ `stat -f "%Sm %N" -t "%Y-%m-%d %H:%M:%S" waves/*.md`, this seat). Three generations of one failure is not an accident of timing; it is what a numeric address **is** in a concurrent round. **This spec now cites no sibling by line at all** — the class is closed by construction rather than by re-measurement (RULINGS-2 **R2-2**), and the remaining `:NNN` strings that survived into pass 2 at §8, §11 and §12 are struck below. Cross-spec receipts here are **stable anchors** — §-headings, clause ids (`E16 ▲`), gate ids (`G7 ⊙`), and row labels (*the SS-4-PREREQ row*; *the `F.W5 → F.W7` edge row*) — and **every quotation below is pasted output of a `grep`/`sed` this seat ran this round, marked ⟨cmd⟩ beside it** (**R2-1-LAW**: a "verbatim" label without a ⟨cmd⟩ note is a defect per se; a rulings file is a cure-shape, never a quotable authority). Line numbers survive only for (i) the frozen 66-record corpus (immutable), (ii) product source, which is MEASURE-AT-OPEN anyway (D-19), and (iii) this file's own coordinates, marked *at this writing*. Eight corrections, each measured rather than argued:

1. **The F.W5 spine reciprocal is WRITTEN — round 1's "NO edge to F.W7" was refuted seven seconds after it was typed.** ⟨cmd⟩ `grep -o 'F\.W7 CITES; F\.W5 STATES\.' waves/F-W5.md` → the **`F.W5 → F.W7` row of F-W5 §4**, whose opening the command prints; the row reads on, ⟨cmd⟩ `grep -o 'The trie question is this wave.s clause \*\*E16\*\* and gate \*\*G7 ⊙\*\*[^(]*' waves/F-W5.md`: *"The trie question is this wave's clause **E16** and gate **G7 ⊙** — F.W7 owns the owner-gated design that follows a ruling and **books no registry row of its own** "*. The register carries that row **and** an `F.W5 → F.W8` row — stated as membership, never as a total, ⟨cmd⟩ `grep -ohE '\*\*F\.W5 → F\.W[0-9]+\*\*' waves/F-W5.md | sort -u` → `**F.W5 → F.W1**` · `**F.W5 → F.W2**` · `**F.W5 → F.W6**` · `**F.W5 → F.W7**` · `**F.W5 → F.W8**`. **G-F7-11 and §7c are re-worded to that truth** (R2-8.2(iii) / R2-10.8). ▲ **STRUCK AT ROUND 3 (R3-3.6; F-W7-CHECK D-1) — the paragraph announcing the class closed committed it in its own last sentence.** Through pass 2 this item ended by attributing to that row, under **no ⟨cmd⟩ at all**, the string *"cite E16 and G7 by clause and gate id, never by line"*. ⟨cmd⟩ `grep -o 'cite E16 and G7' waves/F-W5.md` → **no output**: the string's only homes are this file and **a rulings file**, which R2-1-LAW item 3 makes non-quotable — a spec quoting "the banked lock" quotes the BANK at the bank's live bytes, never a ruling's paraphrase of it. **The idiom is F.W7's own and is carried in F.W7's voice throughout this file.** F-W5's live sentence reaches the same law in its own words — ⟨cmd⟩ `grep -o 'clause id at this end and by §-anchor + row label at the other, with NO line number at either end' waves/F-W5.md` → *"clause id at this end and by §-anchor + row label at the other, with NO line number at either end"*.
2. ⟨CC⟩ **F.W0's reciprocal is WRITTEN TOO — and this file has now stated the same gap wrong three times, each time in a receipt its own command refutes.** Round 1 said F.W5 declared no F.W7 edge (false). Round 2's first pass said a `grep -c` over `F-W0.md` returned 0 (false). Round 2's second pass said → 1, "announced but unwritten" (false as of this seat's read). **The live measurement, and — at round 3, per R3-3.10 — stated as MEMBERSHIP rather than as a count, because a banked count of a moving sibling is false the moment the sibling moves**: ⟨cmd⟩ `grep -o '\*\*F\.W7\*\* (the owner-gated trie design)' waves/F-W0.md` → *"**F.W7** (the owner-gated trie design)"* — the register carries the row. Its head, ⟨cmd⟩ `grep -o 'THE RECIPROCAL, written at repair round 2[^*]*' waves/F-W0.md` → *"THE RECIPROCAL, written at repair round 2 (R2-10.8 / R2-8.2(iii)); this end was the one genuinely owed"*. *(The pass-2 figures — 3 hits, 12 rows, "the ninth" — are struck as receipts: each was a count against a file under concurrent repair, the exact form R3-3.10 now forbids outright. The membership probe above answers the only question the gate asks.)* **Every wave-spec end of every F.W7 edge is now DECLARED**; G-F7-11's born-RED residue is re-based on the ends that are not wave specs (SS-4, F.W9/W10's own file, the value.js API row) — see G-F7-11 and §7c. **The lesson is booked as this wave's own, not F.W0's**: a born-RED witness stated as a *count against a live sibling* is a measurement with a shelf life of minutes; the gate is re-written to depend on **declared-or-not at a named row**, which a reader re-checks in one probe, rather than on a number.
3. **Two anchors the pass-2 check called unlocatable are locatable at the live bytes; its receipts were the stale ones.** ⟨cmd⟩ `grep -rl "Declared-not-carried edge" waves/` → `waves/F-W3.md` (and this file) — the bytes live at **F-W3 §4's S-6 entry**. ⟨cmd⟩ `grep -o 'THE GUARDRAIL IS THE INCUMBENT; an owner ruling precedes design' waves/F-W10.md` → the dissent's bytes, at **F-W10 §2.3's SS-4-PREREQ row**. *(Round 3: file-lists and word-output replace the pass-2 `grep -rn`/`grep -n` address forms and the "exactly one hit" count — R3-3.10.)* Both are re-anchored **by label**; neither quotation is struck; the erratum is recorded here rather than the finding inherited. *(E-3 shape: the correction travels as a row, the dated check is never rewritten.)*
4. **The C-14 "verbatim" label was a fusion of two sentences from two homes, inherited from a ruling's paraphrase rather than read from the bank — split and re-sourced (R2-1d).** Its cure is at §5a's `F-MAIL-∅` row; the root cause is minuted there. Same seat, same round: the **TWELVE-limb** F.W1 roster is re-quoted from the corrected charter (**R2-5**) at §7c, replacing this file's single `ELEVEN-limb` cite.
5. ⟨CC⟩ **The E16 dissent this file quoted as "verbatim" was STRUCK AT THE KEYSTONE'S OWN HAND this round, and this file's copy dies with it.** Pass 2 carried, under a *"banked at F-W5 §2 clause E16 ▲"* label, the sentence *"…the trie requirement collides head-on with a standing KISS guardrail present in BOTH trees — `atomdiff.py:12-14` is the INCUMBENT — and the owner ruling PRECEDES design."* ⟨cmd⟩ `grep -o 'the trie requirement collides head-on' waves/F-W5.md` → the phrase survives at F-W5 **only inside §4's `F.W5 → F.W7` row, which quotes it in order to STRIKE it** — ⟨cmd⟩ `grep -o 'a \*\*paraphrase of those bytes, typed from the shape of the ruling rather than read from the sibling\*\* — the convicted class. It is STRUCK' waves/F-W5.md` → *"a **paraphrase of those bytes, typed from the shape of the ruling rather than read from the sibling** — the convicted class. It is STRUCK"*. ⟨cmd⟩ `grep -o 'atomdiff.py:12-14 is the INCUMBENT' waves/F-W5.md` → **no output**, while ⟨cmd⟩ `grep -o 'guardrail is the INCUMBENT[^*]*' waves/F-W5.md` → *"guardrail is the INCUMBENT, but it is NOT carried on both sides today"* (the live, differently-worded sentence). *(Round 3: the three `grep -c` forms this item published are re-cast as word-output above — R3-3.10, no banked count of a live sibling; the classification each was standing in for is now stated directly.)* **Quoting struck bytes as a file's current bytes is R2-1c's convicted class exactly**; both sites (§5a's R-4 row, §5c's N-1 lock) and the §8 echo now quote **the dissent's one real home** — **F-W10 §2.3, the SS-4-PREREQ row** — and, separately, **F-W5 §2 clause E16 ▲**'s *own live* bytes, as two quotes with two ⟨cmd⟩ notes and never fused (R2-1-LAW item 4).
6. ⟨CC⟩ **The P-10 limb sentence this file quoted twice as verbatim exists at NO coordinate in the tree.** ⟨cmd⟩ (base `$V` = `/Users/mkbabb/Programming/value.js`, **added to §0(B)'s map at repair round 5, REST-34**) `/usr/bin/grep -rl "trie ruling stays inline" docs/` → **`docs/tranches/X/fourier/waves/F-W7.md`** — the sole WAVE-SIDE home — **with residue disclosed (R4-1.9): `docs/tranches/X/fourier/conformance/PASS-5/F-W7-CHECK.md` and `docs/tranches/X/fourier/conformance/PASS-5/WORK-ORDER.md`, both conformance artefacts of this repair round and neither a wave spec**, and every occurrence inside it is a self-disclosing strike note (§0, §4, §5a, §12) — the string *"F.W7's trie ruling stays inline in F.W7"* is this file's own paraphrase wearing a quotation mark. *(Round 3: stated as a file-list and a classification rather than as `grep -rn`'s addresses, which move whenever a strike note is edited.)* The **real** banked bytes are F-W3's, produced by ⟨cmd⟩ `grep -o 'Declared-not-carried edge: \*\*F\.W7[^*]*\*\*' waves/F-W3.md` and ⟨cmd⟩ `grep -o '\*\*Explicitly declared as an edge, not carried\*\* — it stays inline in F\.W7 (S-6)' waves/F-W3.md`: *"Declared-not-carried edge: **F.W7's anti-tree KISS guardrail stays inline in F.W7.**"* (**F-W3 §4, the S-6 row's tail**) and *"**Explicitly declared as an edge, not carried** — it stays inline in F.W7 (S-6)"* (**F-W3 §X, the `F.W7's anti-tree KISS guardrail` exclusion row**). Both sites now carry those bytes; the invented sentence is STRUCK and appears nowhere in this file.
7. ⟨CC⟩ **C-25's clause sentence is re-sourced to the FROZEN RECORD, at the record's own case.** F-W5's E13 struck its up-cased *"SUPERSET"* rendering this round as its own D-6 (*"emphasis-capitalisation inside a verbatim marker is a rendering, not the bytes"*), so this file's stated ⟨cmd⟩ `grep -o "an operation's cache identity must be a SUPERSET…" waves/F-W5.md` now returns **nothing** — the transcription had no source. The sentence is banked in the **immutable corpus**, where a line cite is permitted (R2-2.2(i)): ⟨cmd⟩ `grep -n -F 'superset of the request fields' fr-ContourSettings.md` → **`:43`**, reading *"an operation's cache identity must be a superset of the request fields the operation consumes"*. Three sites (§5c's C-25 row, §6 G-F7-5, §7b's lock) take the record's case; **F-W5 §2 clause E13 remains the downstream home, cited by clause id.**
8. ⟨CC⟩ **§1's record denominator is re-derived, not re-asserted.** Pass 2 cured "9"→"12" and the same round's own quote-repairs then pushed the true figure to **15**: ⟨cmd⟩ `awk '/^## 5\. Carry/,/^## 6\. Gates/' F-W7.md | grep -ohE 'fr-[A-Za-z]+' | sort -u | wc -l` → **15**. The AUDITED row now publishes the measured 15 **with its decomposition**, because the three additions are records this file does not cite at all — they appear only *inside verbatim quotations of F-W5 clause heads*, as the anti-rename disambiguators those heads carry. **A denominator that moves when you fix a quotation was never a denominator; it is a probe output, and it is now published as one.**

**Repair round 3 (X·F L-20, 2026-08-28) — THE RECEIPT ITSELF BECAME THE FORGERY SURFACE, and the cure is structural.** Round 3's headline conviction program-wide is that a ⟨cmd⟩ note, adopted in round 2 as the antidote to invented authority, became the instrument the inventions wore. This file contributed one of the seven named cases, and the round's directives land here as **six repairs, one self-sweep and one consumption**:

1. **R3-3.6 — the minted attribution at §0 item 1 is STRUCK and re-sourced** (item 1 above). It was a *rulings file's* sentence published as a sibling's bytes, inside the paragraph declaring that class closed by construction, and it carried no ⟨cmd⟩ of its own.
2. **R3-3.10 — the STANDING AMENDMENT, applied as a sweep, not as an instance: no ⟨cmd⟩ whose output is a COUNT of a live sibling or of this file may be banked here.** **Every such receipt in this file is retired** and re-cast as **set-membership, `grep -o` word-output, or a stated classification** — at §0 items 1/2/3/5/6, §1's own §5-name denominator, §5a's R-4 erratum and N-1 locks, §6 G-F7-1, G-F7-8 and G-F7-11, §7c's F.W0, F.W8 and F.W9/W10 rows, and §11's dependency line. *(The number of them is deliberately not published: a count of this file's own bytes is the very form the amendment forbids, and the sites are named instead so a seat can check each.)* **The cause, named**: this spec's founding defect was never carelessness about substance but the belief that an address is a citation; a frozen count of a moving file is the same error one level up — *a measurement stated as a fact*. Word-output survives the sibling's next edit; a numeral does not. ▲ **AND THE RECEIPTS ARE MADE RUNNABLE AS WRITTEN.** Two reading rules, declared once so no seat has to guess: **(i)** inside a markdown table cell every pipe is escaped `\|`; a reader un-escapes `\|` → `|` before running. **(ii)** After this round **no receipt in this file uses a pipe as a PATTERN character** — the row-shape anchors (`^\| \*\*E13\*\*`) and trailing literal pipes that made rule (i) ambiguous are gone, replaced by pipe-free exact-string probes on each clause head and its banked-id cell. Every surviving `\|` is a shell pipeline or a negated class, both of which un-escape unambiguously. ▲ **THE UNIVERSAL IS STRUCK AT ROUND 4 AND REPLACED BY THE TRUE, NARROWER RULE (`PASS-4/F-W7-CHECK.md` D-7).** It was false when written, at five sites — including **§5a's `F-MAIL-∅` census detector, the single most load-bearing probe in the file**, whose `(^\|[^A-Za-z])F\.W7([^0-9A-Za-z-]\|$)` is ERE alternation twice over; G-F7-8's operand; and the two `^\| \*\*E16\*\*` / `^\| \*\*G7\*\* ` row anchors at §3 and §4, which are the exact form the sentence declared *gone*. **A paragraph that closes a class by construction while the class survives in the paragraph's own census probe is this file's recurring failure mode, restated one level up** — and it is the third generation of it. **THE RULE THAT IS TRUE, and that binds from here**: a pipe appears in a pattern of this file **only** as (a) an **ERE alternation inside `grep -E`**, or (b) a **negated character class**. Both un-escape unambiguously under rule (i), both are load-bearing (a census detector cannot be written without alternation), and **no receipt here uses a pipe as a literal row-shape anchor** — the two that did are retired at their sites this round, so that half of the claim is now earned rather than asserted. This is R3-5.4's shape-agnosticism applied to the whole file rather than to the one gate it named: **a receipt whose meaning depends on how its reader un-escapes it is not reproducible, whatever it returns.**
3. **R3-8.1 — the corrupt-dist gate takes its canonical form `FR-NP-32 (≡ fr-PaperSidebar M1)` at all three statement sites** (§5e's OG-F1 row · §6 G-F7-7's witness · §7c's F.W0 row). This file was the only X·F spec that carried the fact and never its id; two of the three sites were additionally **bare `(M1)`** against a live `M1` homonym class (R-5: a bare token is not an identity). ▲ **DRIFT EXPORTED, DECLARED, NOT CHASED**: §7c's F.W0 sentence is a span **F-W0 §6b quotes back at this file**, so adding the id there makes F-W0's copy stale by exactly that clause. It is minuted at §7c's F.W0 row and routed to F.W0's own seat; **this wave writes no F-W0 byte** — the alternative (leaving the site bare to protect a sibling's quotation) would make a sibling's convenience a reason to disobey a standing canonical form.
4. **R3-5.4 — G-F7-8's set-difference operand is re-cut shape- and spelling-agnostic with its detector stated INLINE.** Both operands were anchored to markdown-table shape; the result was right and the operand was not, which is precisely the condition R2-9 says does not cure it.
5. **R3-3.9's purge share — the drift set, each cured at the bytes**: the one-command-two-quotes receipt at §5a's P-10 row **split into two real commands** (it was hyphen-blind: the stated `grep` never returned the S-6 tail, whose spelling is `Declared-not-carried`) · §7c's *"five further artefacts"* elision count replaced by the F-W0 row **quoted whole** (the true figure was four; a count word inside an elision disclosure is a count of a live sibling twice over) · the emphasis added inside *"verbatim"* markers removed at both its sites — **§5c N-1's two renderings of F-W6's plain `quoted both trees`** (the check located this class at §7c's F.W6 row; at the live bytes that row was already plain and **N-1 was the bolder** — the finding holds, its address did not, and the erratum says so) and §7c's F.W1 *"TWELVE"* · §5c's N-3 corpus quotation given its ⟨cmd⟩ and **the record's own sentence-initial case** · §5b's D1 lock re-quoted from a command that actually produces its bytes, leading `a ` restored · two unmarked truncations closed by **quoting to the end of the sentence** (G-F7-11's COHESION §2 law; §5a's E16 ▲ tail) · the backticks this file added inside K-3's banked parenthetical removed (§7b, §8).
6. **F-W7-CHECK D-11 — §8 now names BOTH stale-premise consumers.** The exclusion table named `F-W8.md` where §5c N-1 and §12 measure the premise stale at two, in two spellings; the table whose whole function is that absence cannot be mistaken for oversight was itself under-naming.
7. **THE SEAT'S OWN SWEEP, so the PURGE SEAT (R3-3.8, which runs last) inherits a baseline rather than a claim**: **every ⟨cmd⟩ in this file was re-run by this seat, read-only, against the settled bytes — including F.W3's landed §X.1-v4 — and every one reproduced.** ▲ **THE UNIVERSAL IS STRUCK AT ROUND 4 (R4-1.9; `PASS-4/F-W7-CHECK.md` D-4). IT WAS FALSE, AND ITS RESIDUE IS ENUMERATED HERE RATHER THAN RE-ASSERTED.** Round 3's sweep did not execute what it attested to, and pass 4 named the exceptions: **(1)** the `.\{0,262\}` receipt at **§6 G-F7-11**'s F-W6 witness — on the pinned BSD binary it prints `grep: maximum repetition exceeds 255` and no bytes at all; **(2)** the same receipt at **§7c's F.W6 row** — one defect, two sites, and both were labelled *"the command's own output"*; **(3)** **§6 G-F7-1**'s span, eleven characters **longer** than its command's true output, under a label reading *"as the command prints it, its cut disclosed"* (R4-1.10); **(4)** **§2a**'s `find` enumeration, false at the write and by a receipt whose own subject-matter — the pass directories — had already grown (R4-1.8). A fifth, no pass caught: **(5)** §5a's `../../keyframes/…` base, which resolved to no directory on any host (round-4 item (B)). **An attestation of sweep must publish its residue or not exist** — R4-1.9's words, and this is the residue. The four *"nothing by design"* absences below re-run and still return nothing; they were never the problem. Four return **nothing by design** and are published as such, never as a hit: `grep -o 'cite E16 and G7' waves/F-W5.md` · `grep -o 'atomdiff.py:12-14 is the INCUMBENT' waves/F-W5.md` · the struck pass-2 `SUPERSET` probe · and the empty half of each absence receipt. **A receipt that does not reproduce is fixed from the live output or struck with everything resting on it — there is no third disposition, and this seat found no third case.** ▲ **The last clause is STRUCK at round 4: there were five, enumerated above.** The disposition rule itself stands and is discharged at each of the five — four fixed from live output, one (the `.{0,262}` form) struck outright as unexecutable and replaced. **The claim "I found no exceptions" is not a measurement; it is the absence of one**, and a sweep that reports a clean bill without its transcript is the same object as a count without its command.
8. **§X.1-v4 CONSUMED (R3-1a, landed first this round).** F.W3's definitive file-criterion partition decides, by one question of a PATH, the twin routings this file merely echoed as *"F.W3/W4"*: `fr-ContourEditorCanvas` **L-5** and `fr-ContourSettings` **M-12 / B-4** are cited against v4's criterion at §5c, §8 and §12, each with its ruled holder named. **F.W7 books none of them and proposes no F-W3 or F-W4 byte** — the consumption is a citation of a landed partition, which is exactly what a wave that books nothing owes the twin surface.

**Repair round 4 (X·F L-20, 2026-08-29) — THE RECEIPTS WERE NEVER RUN ON THE ENGINE THEY NAME, and the census gets EXACTLY ONE OPERAND.** Round 3's cure was to publish word-output instead of counts. **Pass 4 ran the word-output and found three receipts that do not reproduce — one of them cannot execute at all, at two sites** (`PASS-4/F-W7-CHECK.md` D-1 · D-2 · D-3, with D-4 convicting the self-sweep that attested otherwise). `PASS-4/RULINGS-4.md` lands them as **R4-1 items 8 · 9 · 10**, **R4-2** (the portable-command law), **R4-3/R4-10** (the canonical census as the only operand), **R4-7** (verbatim-label law, fifth generation), **R4-8.3** (hash-pinned cross-quotes) and **R4-9.7** (G-F7-8's detector). Every cure below was **re-run under the pinned binary before it was written down**, and the round's own lesson is stated first because it is structural: *round 2 wrote the command beside the quotation; round 3 made the command's output the quotation; round 4 discovers that neither is worth anything until the command is run on the engine the programme actually uses.* A receipt is not a citation, an address, or a paste — **it is an execution, or it is nothing.**

**(A) THE TOOLCHAIN PIN (R4-2.1/.2), stated because this file's worst defect was invisible without it.** Every ⟨cmd⟩ in this spec executes under `bash` with **BSD `/usr/bin/grep`**, the pinned binary. Consequences, binding here and re-verified this round: **no `-P`, no `\K`, no lookaround** (exit 2 on the pin), and **no `.{n,m}` with m > 255** — the interval bound is 255 and a larger one is a hard error, not a silent clamp. This is not theory: the two `.\{0,262\}` receipts this file published through pass 3 print the error `grep: maximum repetition exceeds 255` and **nothing else**, and at the legal maximum `.\{0,255\}` the output stops seven characters short of the span they were offered for — **no legal bound on the pinned engine produces the published bytes.** Both sites are re-cut below as **exact-string probes with no interval bound at all** (R4-2's windowed-extraction principle, taken to its limit: the anchor *is* the window). A shell wrapper that resolves `grep` to some other binary is **not** the pin; a receipt that runs only under the wrapper is unrunnable machinery and is convicted on sight at pass 5 (R4-2.5).

**(B) THE BASE MAP (R4-2.3/.4), declared once, binding on every ⟨cmd⟩ in this file, and it closes a defect no pass had named.** This spec's receipts carried **several implicit working directories** — enumerated below and never counted, because a numeral about this file's own bytes is the form R3-3.10 retires — **and one path that resolved under none of them.** Each receipt family's cwd is now fixed by its operand, and **no receipt mixes two bases**:

- `V=/Users/mkbabb/Programming/value.js` · `F=/Users/mkbabb/Programming/fourier-analysis` (**read-only, always**)
- **`cd $V/docs/tranches/X/fourier`** — every `waves/…`, `conformance/…` and `../keyframes/…` receipt.
- **`cd $V/docs/tranches/V/megatranche/registry/adjudicated`** — every `fr-*.md` receipt (the frozen 66).
- **`cd $V/docs/tranches/X`** — every bare `COHESION.md` receipt.
- **`cd $V/docs/tranches/V/megatranche/formation/fourier`** — every bare `CENSUS-2026-08-03.md` receipt.
- **`cd $V/docs/tranches/X/fourier/waves`** — the bare `F-W7.md` self-probes (§1's roster).
- **`cd $V`** (the repository root) — **every bare `docs/…` receipt** ⟨**added at repair round 5, REST-34 (P5-4, HIGH).** Three receipts in this file took a bare `docs/` operand and the map enumerated **five cwds, none of which was the repository root**; from any of the five the command prints `grep: docs/: No such file or directory` and **exits 2**. §0(B) declares this map *"binding on every ⟨cmd⟩ in this file"*, so the three were unrunnable under the very block that publishes them. ⊘ **The exactness of the recurrence is the point**: this paragraph's own ⟨CC⟩ is titled *"The path that resolved nowhere"* and cures `../keyframes/…` while three receipts on the facing page resolved nowhere under the map that paragraph publishes. `CLOSE-CERT-2` Class A's `F-W7 | 182 | 0 | — | CLEAN` is falsified here.⟩

⟨CC⟩ **The path that resolved nowhere**: §5a's `F-MAIL-∅` lock cited `../keyframes/waves/KF-W1.md`, which from the `fourier/` base is `docs/tranches/keyframes/…` — **a directory that does not exist**. Corrected to `../keyframes/waves/KF-W1.md` and all three C-14 quotations re-run under it this round. Three passes read that receipt and none ran it; it is the same lesson as (A) in a second key.

**(C) THE HASH PIN (R4-8.3) — every sibling this file quotes, hashed at quote time.** A cross-file paste is a claim about another file's bytes *at a moment*; round 3 proved the moment expires. `shasum -a 256`, first 12 hex, ⟨cmd⟩ (base: `$V/docs/tranches/X/fourier`) `shasum -a 256 waves/F-W0.md waves/F-W1.md waves/F-W3.md waves/F-W5.md waves/F-W6.md waves/F-W8.md waves/F-W10.md ../COHESION.md ../keyframes/waves/KF-W1.md conformance/CENSUS-CANONICAL.md`:

| sibling quoted here | sha256-at-quote-time (first 12) | moved mid-round? |
|---|---|---|
| `waves/F-W0.md` | ~~`cd64d6580098`~~ → **pinned per PIN-PURGE-CERT.md §TABLE (2026-08-29)** | **yes** — re-stamped at repair round 5 (REST-38); **moves again this round** (REST-01..REST-08) |
| `waves/F-W1.md` | ~~`0bfd6c8cd369`~~ → **pinned per PIN-PURGE-CERT.md §TABLE (2026-08-29)** | **yes, twice** — re-stamped at repair round 5 (REST-38); **moves again this round** (BATCH-W1) |
| `waves/F-W3.md` | **pinned per PIN-PURGE-CERT.md §TABLE (2026-08-29)** | **YES — the *no* is SUPERSEDED by the certificate: F-W3 moved at repair round 5 and again at round 6** |
| `waves/F-W5.md` | ~~`40c1a5f5a576`~~ → **pinned per PIN-PURGE-CERT.md §TABLE (2026-08-29)** | **yes, three times** — re-stamped at repair round 5 (REST-38) |
| `waves/F-W6.md` | ~~`b11f08894b9d`~~ → **pinned per PIN-PURGE-CERT.md §TABLE (2026-08-29)** | **yes, three times** — re-stamped at repair round 5 (REST-38); **moves again this round** (REST-15..REST-31) |
| `waves/F-W8.md` | **pinned per PIN-PURGE-CERT.md §TABLE (2026-08-29)** | **yes** — re-verified at the round-5 hash |
| `waves/F-W10.md` | **pinned per PIN-PURGE-CERT.md §TABLE (2026-08-29)** | **yes, three times** — re-verified at the round-5 hash |
| `../COHESION.md` | `91c6d974db9b` | no |
| `../keyframes/waves/KF-W1.md` | ~~`ee33bf25cb2a`~~ → **`919e484b8a60`** | **YES** ⟨*the cell read "no" and that statement is now false — re-measured at repair round 5, REST-38*⟩ |
| `conformance/CENSUS-CANONICAL.md` | ~~`a450b8e9f80e`~~ → **`3e0a9acb3381`** | **YES — errata round 5**, §0.4 E5-1..E5-18 |

⌧ **RE-STAMPED AT REPAIR ROUND 5 (REST-38; P5-6) — FIVE STALE, ONE "no" NOW FALSE.** F-W0, F-W1, F-W5, F-W6 and the census had all moved by the pass-5 settle, and `KF-W1.md`'s cell asserted **"no"** against a file that had moved to `919e484b8a60`. ⊘ **The mechanism is acquitted by its own record**: §0(C) explicitly disclaims settled-tree scope (*"the settled-tree hash table is the PURGE SEAT's closing act"*), and **every span re-run from all five moved files reproduced** — so the pin did exactly what R4-8.3 built it for. **Two residues remain and are stated rather than smoothed: the table cannot verify anything without a re-run, and one row's "no" was a false statement.** The authoritative stamp is the purge seat's, after every repair-round-5 byte has landed.

⟨CC⟩ **AND THE PIN EARNED ITS KEEP INSIDE ITS OWN ROUND — SIX OF THE TEN MOVED WHILE THIS SEAT WAS WRITING.** The table's first computation, taken at this seat's first quotation pass, read `69b05ccbb320` · `83ffe83cb37f` · `d9f3c812cf36` · `6f8d3b948982` · `f82115ddeb89` · `c00ec40d8673` for F-W0 · F-W1 · F-W5 · F-W6 · F-W8 · F-W10; **their own round-4 repair seats landed in the interval**, exactly the concurrency R4-8's LAW E is written for. **Every span this file quotes from all six was then RE-RUN against the new bytes and every one reproduced** — the substance was untouched by their repairs — and the table above carries the **post-drift** hashes, at which the quotations are attested. *(This is the mechanism working, not failing: three rounds of struck cross-quotes were the same event happening invisibly. **A hash does not prevent drift; it makes drift a fact instead of an argument.** No sibling byte was written by this seat to make any of it true, and if another lands after this write the next reader will see it in one `shasum` rather than in a fifth round of forensics.)* ▲ **AND THE SCOPE OF THIS PIN IS DECLARED, NOT OVERSOLD.** It is a **quote-time** record — R4-8.3's own word — taken at this seat's **final** verification pass, in which **every span this file quotes from every file in the table was re-run and reproduced, with zero failures**, the hashes captured in the same invocation as the verification so the two cannot disagree. **It is not a claim about the settled tree**: several siblings landed further edits while this round was open, three of them more than once, and under **R4-8.1/.2 the settled-tree hash table is the PURGE SEAT's closing act, issued after every other seat's final byte** — not a wave seat's to pre-empt. **A wave that chased its siblings' hashes to a fixed point would be doing the purge seat's job badly and its own not at all.**

**A later rewrite of any row above is now detectable rather than deceptive** — which is exactly what three rounds of struck cross-quotes were reaching for and never had. The hashes are of the **live** siblings this file quotes; the frozen 66 `fr-*.md`, the formation-lane authorities and the intake adjudication carry none because §2a pins them immutable and they cannot move — **a hash is for what can change.** This wave writes no byte of any file in the table.

**(D) THE CENSUS OF RECORD, AND THE END OF THE CHECK-FILE OPERAND (R4-3 / R4-10 / R3-4).** `conformance/CENSUS-CANONICAL.md` is **the one census operand of this programme** — ⟨cmd⟩ `grep -o 'THE CENSUS OF RECORD — supersedes every prior denominator, g19 operand, and per-spec census claim' conformance/CENSUS-CANONICAL.md` → *"THE CENSUS OF RECORD — supersedes every prior denominator, g19 operand, and per-spec census claim"*, closing (⟨cmd⟩ `grep -o 'a gate that cites another census operand is DEFECTIVE on sight' conformance/CENSUS-CANONICAL.md`) *"a gate that cites another census operand is DEFECTIVE on sight"*. **Two sentences of this file named check registers as OPERANDS** — §0's fold provenance and §2a's bounds reconciliation, the second load-bearing because it admits the fifth bounds path. **Both are struck below**; the registers are re-named as what they lawfully are, **prior runs**, and the operand becomes the canonical ⊕ the frozen 66 ⊕ **F-W5 §2's `§E` clause register**. The executed census at §5a was corpus-derived and clean throughout — **which is precisely the condition R2-9 says does not cure an operand**, and this file has now been on both sides of that rule in one round. **F.W7's canonical roster is ZERO** (R4-10's table: *"F-W7 | **0** — ∅ posture TRUE at the corpus"*), and the canonical says so in its own words: ⟨cmd⟩ `grep -o 'The F.W7 ∅ posture is TRUE AT THE CORPUS, by enumeration' conformance/CENSUS-CANONICAL.md` → *"The F.W7 ∅ posture is TRUE AT THE CORPUS, by enumeration"*; ⟨cmd⟩ `grep -o 'No row in any record routes' conformance/CENSUS-CANONICAL.md` → *"No row in any record routes"* (the sentence wraps in the source; its next line completes it as `F.W7**`). The canonical's §2 carries **no `### F.W7` roster section at all** — ⟨cmd⟩ `grep -o '^### F\.W7' conformance/CENSUS-CANONICAL.md` → **no output**, an absence published as an absence. **This wave books nothing, and the census of record is now the authority for that, in place of four passes of self-measurement.**

**(E) THE TWELVE CURES, each named at its site, each re-run under the pin.** **D-1** (R4-1's `.{0,262}` cluster, R4-2's table row for this wave) — both sites re-cut as three exact-string probes that together print the whole span, **no interval bound anywhere**: §6 G-F7-11's F-W6 witness and §7c's F.W6 row · **D-2** (R4-1.10) — G-F7-1's receipt is truncated to **the command's true 134 characters**, its mid-word cut disclosed, with the completed sentence supplied by a **second** exact probe rather than by silently finishing the word · **D-3** (R4-1.8) — §2a's `find` re-run and pasted live (**eight** directories, not five), the load-bearing negative restated as the **absence probe** it always needed to be · **D-4** (R4-1.9) — §0 item 7's universal attestation **struck** and replaced by an enumerated residue · **D-5** (R4-3) — the two check-file operand sentences struck, per (D) · **D-6** (R4-9.7) — G-F7-8's detector re-cut **token-bounded and delimiter-free**, derived from the spelling list its own axis (ii) declares · **D-7** — §0 item 2's "no pipe as a PATTERN character" declaration **narrowed to what is true**, and the two row-shape anchors it wrongly declared gone are actually retired · **D-8** (R4-7.1) — §5e's X-1 receipt split into **two** exact commands, its third hit named, its silent truncation closed · **D-9** (R4-7.3) — the `(both passes)` count word retired from the do-not-touch glob · **D-10** (R4-7.1) — the `[^*]*` receipt that ran past its sentence into a table pipe replaced by an exact probe at all three sites · **D-11** — the L-5 exclusion re-based on the **canonical's F.W4 roster**, which homes it by enumeration, in place of an assertion about a sibling's bytes · **D-12** — the two-hit locator replaced by the exact-string probe of the span it certifies.

**(F) WHAT THIS SEAT DOES NOT WRITE.** **R4-1.E-3's `PASS-3/CLOSE-CERT.md` erratum row is NOT this seat's to append** — that certificate is a conformance artefact, `conformance/**` is immutable to this wave (§2a), and `F-W7.md` is this seat's only writable file. The erratum is **owed by the purge/certificate seat under R4-8**, and this paragraph is the notice it needs: the cert's `F-W7: 155 markers / 140 run / 140 repro / 0 fixed / 0 struck` row is **false at three receipts**, all three cured above, and the true `fixed` figure for this wave at round 4 is **not zero**. Likewise **R4-8.2's sha256 certificate** is the purge seat's closing act, not this file's; (C) above is this wave's half of R4-8.3 and nothing more. **No F-W0, F-W5, F-W6, F-W8, F-W10, COHESION, corpus or fourier byte is written by this round** — every far-end correction remains a routed request, as it has been since round 2.

**The ∅ posture is CLOSED** (round-2 cross-wave ruling R2-8): two independent seats across two passes measured `routedTotal = 0`, and §5a's census is not re-litigated by a later seat unless the corpus itself changes — a new `fr-*.md` routing a row to F.W7. **Pass 3's fresh seat re-derived it a third time and confirmed CLEAN; pass 4's fourth derivation confirmed it again, and at round 4 the CENSUS OF RECORD adopts it as its own §4 finding** (⟨cmd⟩ at (D)) — so the posture no longer rests on this wave's self-measurement at all. This round changes no census figure.

**EXECUTION IS NOT AUTHORIZED BY THIS FILE.** Nothing opens, writes, builds, or runs product source in either repo until the owner's begin-word. `/Users/mkbabb/Programming/fourier-analysis` is **READ-ONLY, always** — this wave writes zero fourier bytes. Specifying is not doing.

---

## 1. State

**Name**: F.W7 — trie-like variant compression design (the R-4 greenfield)
**Opens after**: **F.W0** (substrate pre-gates — hard, §6 G-F7-7) · **F.W5** (§2 clause **E16** ⊕ §3 **G7** is the same identity; §2 **E1**, §2 **E3** ⊕ §3 **G4**, §2 **E10** are hard predecessors) · **the owner's ruling on G-F7-1**
**Agents**: 3 **serial** (units b and c both write `F-W7.md`; sequencing, never parallelism)
**Hard gate**: 11 conditions in §6 — 9 keyed to banked ids in the 66-record registry and the named in-tree authorities, 2 added because a gate would otherwise be unfalsifiable prose. **All 11 born-RED**; **one is OWNER-GATED and may not be authored unruled (G-F7-1)**.
**Status**: planned

| verb | value | evidence |
|---|---|---|
| AUDITED | **YES** | `docs/tranches/V/megatranche/formation/fourier/lane-crud.md` §2 R-4 + §7 · `docs/tranches/V/megatranche/formation/fourier/CENSUS-2026-08-03.md` §4 sketch 8 + §5 risk 4 · **F-W5 §2's `§E` clause register** — clauses **E1 · E3 · E5 · E7 · E8 · E10 · E13 · E16 ⊙ · E17**, cited **by clause id, never by line** — + **F-W5 §3** gates **G4 ⊙ / G7 ⊙** · **15** distinct adjudicated `fr-*.md` record names **occurring across §5, none routed to F.W7** (§5a `F-MAIL-∅`) — published at round 3 as **the roster itself, in words**, because R3-3.10 forbids banking a ⟨cmd⟩ whose output is a count of the citing file, and pass 2's `\| wc -l` tail was precisely that: ⟨cmd⟩ `awk '/^## 5\. Carry/,/^## 6\. Gates/' F-W7.md \| grep -ohE 'fr-[A-Za-z]+' \| sort -u` → `fr-AdminAuditLog` · `fr-BasisCanvas` · `fr-ContourEditorCanvas` · `fr-ContourSettings` · `fr-EquationView` · `fr-FourierShapeExtractor` · `fr-GalleryAdminBanner` · `fr-GalleryDraftsSection` · `fr-GallerySearchBar` · `fr-GalleryView` · `fr-ImageUpload` · `fr-PaperSidebar` · `fr-SpeedSelect` · `fr-Tooltip` · `fr-VisualizationView`. **Fifteen names, and the arithmetic is over the printed set, never a banked numeral**, decomposed so the figure is a reading of a roster and not an assertion: **10 cited for substance** (`fr-AdminAuditLog · fr-BasisCanvas · fr-ContourEditorCanvas · fr-ContourSettings · fr-GalleryAdminBanner · fr-GalleryDraftsSection · fr-GalleryView · fr-ImageUpload · fr-PaperSidebar · fr-SpeedSelect`) · **2 for disposal** (`fr-Tooltip`, which holds the two `KF.W7` substrings; `fr-VisualizationView`, the L-8 severity precedent) · **3 this file does not cite at all**, occurring only INSIDE verbatim quotations of F-W5 clause heads as those heads' own anti-rename disambiguators (`fr-EquationView` in E10's head; `fr-GallerySearchBar` and `fr-FourierShapeExtractor` in E17's *"NOT … nor …; U-12 qualification"* parenthetical). *(History, recorded not hidden: the pre-repair "9" was a denominator by assertion in the file that retires denominators by assertion; round 2's "12" was true when written and was moved to 15 by that same round's quote-repairs. A count that a quotation fix can change is republished as **the roster the command prints**, never as the number — and at round 3 the counting form itself is retired under R3-3.10.)* · P-10 packet · ▲ **AND, FROM ROUND 4, `conformance/CENSUS-CANONICAL.md` — THE CENSUS OF RECORD, THE ONE CENSUS OPERAND** (R4-3 / R4-10, enforcing R3-4): it homes every row of the corpus and homes **zero** at F.W7, so this wave's ∅ is now a reading of the canonical rather than a self-derivation repeated across four passes (⟨cmd⟩ at §0(D) and §5a's `F-MAIL-∅`). **No check file is an operand of anything in this spec**; `PASS-1`…`PASS-4`'s `F-W7-CHECK` registers appear here only as **prior runs**, which is the one lawful way to name them |
| SPECIFIED | **YES — by this fold, 2026-08-28** | validated against `docs/precepts/instructions/tranche/WAVE_SPEC.md` (L-20, current at authoring: Header · State · Goal · Scope · Dispatch · Bounds · Disjointness · Units · Hard Gate · Cadence · Artefacts · Commits · Dependencies · Archaeology) |
| IMPLEMENTED | **NO** | — no design byte lands before G-F7-1 rules |
| VERIFIED | **NO** | — stamped only at X·F's sub-tranche release close |

### 1a. Goal criterion

This wave succeeds if **exactly one** of two things is true when work ends: either (a) the owner ruled FOR and a structural-sharing/delta design exists whose sharing key is *scoped*, *superset-closed*, *stamped from a snapshot*, and *sized over fields that survive F.W5 §2 clause E10* — or (b) the owner ruled AGAINST and F.W7 lands as a **terminal kill with rationale** naming the incumbent it deferred to. Both is a defect. Neither is a defect. **A completed wave is not the same thing as a wave that produced a design.**

### 1b. What this wave is NOT

It repairs nothing. Eight banked identities whose homes are **F-W5 §2's `§E` clause register** — **E1 · E3 · E5 · E7 · E8 · E10 · E13 · E17** (re-keyed by clause id; the register is `### §E — Provenance, lineage and persistence (the union's core)`, ⟨cmd⟩ `grep -n '^### §E' waves/F-W5.md`) — plus **F.W3's** ImageUpload roster 12/23 ⟨*re-based at repair round 5, REST-32: read **F.W4**; the census of record homes both at **F.W3** — `` | `L:L-M3` | `C:C-6` | `F.W3/W4` | **F.W3** <sub>file-criterion → §5.d</sub> | `` and `` | `R2-missed-3` | — | `F.W3/W4` | **F.W3** <sub>file-criterion → §5.d</sub> | ``; `K-8` is **TERMINAL (∅)** and is named as such*⟩ and the **`F.W5-W8` band's** m-15, whose wave-side duty R4-10 assigns to **F-W5** at clause **§C2** ⟨*REST-37: R4-5 makes F.W5 (28) and F.W5-W8 (89) two distinct canonical denominators; this file states the band correctly elsewhere and the two sentences did not agree*⟩, are **cited and booked at zero** — FR-GIG-5's lesson, adopted as the standing bar at **F-W5 §0b** (⟨cmd⟩ `grep -n 'FR-GIG-5' waves/F-W5.md`, verbatim: *"F.W5 claims credit for none of them — FR-GIG-5's lesson adopted as a standing bar"*). **F.W7 is credited for no cure it did not author.** If a compression design's apparent benefit is actually delivered by E5's idempotency or E1's compound `_id`, F.W7 books ZERO.

---

## 2. Bounds

### 2a. File bounds

| File | Access |
|---|---|
| `docs/tranches/X/fourier/waves/F-W7.md` | **create** — this spec; carries the inline §4 OWNER RULING block (P-10 routing) and, if G-F7-1 rules against, the terminal-kill record **in situ** |
| `docs/tranches/X/fourier/design/R4-enumeration-census.md` | **create** — G-F7-2's enumerated persistence-surface census. **Authorable while G-F7-1 is RED: it is measurement, not design** |
| `docs/tranches/X/fourier/design/R4-variant-storage.md` | **create — CONDITIONAL on G-F7-1 ruling FOR.** Unauthorable while the gate is RED. If the ruling goes against, **this path is never created** |
| `docs/tranches/X/fourier/waves/F-W7/carry-closure.md` | **create** — the G-F7-8 set-difference output, both directions: each of the eight F.W5-homed ids (**E1 · E3 · E5 · E7 · E8 · E10 · E13 · E17**) with its landing clause in `F-W5.md` §2 ⟨addition, reconciled below⟩ |
| `docs/tranches/V/coordination/INBOX.md` | **append-only** (E13) — one ledger row **iff** a relay is sent. F.W7 expects **zero**; see §7c |

**Reconciliation of the bounds set.** Rows 1–3 and the INBOX row stand with their conditionality intact. **One addition**: `F-W7/carry-closure.md`, on the same ground F.W5 admitted its G19 artefact — without a written set-difference, G-F7-8 is prose, and a prose closure gate is exactly the contrivance L-19 forbids. No other expansion. **The reconciliation operand is `F-W5.md` §2's `§E` clause register ⊕ `conformance/CENSUS-CANONICAL.md`, never a per-wave carry ledger and never a check file** ▲ **(RE-BASED AT ROUND 4 — R4-3 / R4-10 / R3-4; `PASS-4/F-W7-CHECK.md` D-5).** Through pass 3 this sentence named *"this wave's conformance registers (`PASS-1` and `PASS-2`)"* as half the operand, and it is the **load-bearing** breach of the two: this clause is the warrant admitting the **fifth bounds path** (`waves/F-W7/carry-closure.md`), so a check-file operand was sitting directly under a `create` row. **Struck.** The registers are **prior runs**, named as such wherever they appear elsewhere in this file, and the operand is now the clause register the artefact actually differences ⊕ the census of record (R-3: `carry/` holds only `F-W1-CARRY.md` and `F-W4-CARRY.md`, and neither carries an `F.W7` token).

**Do NOT touch — every path, without exception**:

- `/Users/mkbabb/Programming/fourier-analysis/**` — whole tree, read-only law. Zero fourier bytes.
- All product source both repos (`value.js/{src,demo,api,test,e2e}/**`, fourier `api/` + `web/`) — execution gate. Read-only measurement is permitted and is the only thing this spec did.
- **Existing, immutable beside the spec (E-1/E-3) — they hold the witnesses, and rewriting one destroys the evidence a gate cites**, every path rooted at the tree's own spelling: `docs/tranches/V/megatranche/registry/adjudicated/fr-*.md` (all 66) · `docs/tranches/V/megatranche/formation/fourier/{lane-crud.md, CENSUS-2026-08-03.md, lane-frontend.md, lane-docs.md}` · `docs/tranches/V/megatranche/audit/codex-provenance/INTAKE-ADJUDICATION-2026-08-03.md` · `docs/tranches/X/fourier/waves/{F-W0.md, F-W1.md, F-W2.md, F-W3.md, F-W4.md, F-W5.md, F-W6.md, F-W8.md, F-W9.md, F-W10.md}` (**all ten siblings — `waves/` holds 11 files including this one**) · `docs/tranches/X/fourier/conformance/**` — **the whole glob, every pass directory and `CENSUS-CANONICAL.md` with them; no count word stands here** ▲ *(round-4 cure, R4-7.3 / `PASS-4/F-W7-CHECK.md` D-9: the parenthetical read "(both passes)" while three existed at the round-3 write and four exist now. The glob was always correct and protective — nothing was ever unprotected — but a numeral about a directory that grows once per pass is the very form R3-3.10 retires everywhere else in this file, and it survived here because it was a word rather than a `grep -c`. **The census of record lives under this glob and is IMMUTABLE to this wave**, which is what makes the pin load-bearing rather than housekeeping.)* · `/Users/mkbabb/Programming/fourier-analysis/docs/tranches/J/design/J-diff-shape.md` (v1, IMMUTABLE) — **the file lives ONLY in the READ-ONLY fourier tree: readable, never writable**; no `docs/tranches/J/design/J-diff-shape.md` exists on the value side and no citation here may spell it that way.
- **Prospective, NOT yet in tree — cited WHEN created, never listed among existing witness-holders**: `docs/tranches/X/fourier/contract/**`, **incl. `contract/OWNER-RULINGS-F.W5.md`** — ▲ **RE-RUN AND RE-PASTED AT ROUND 4 (R4-1.8; `PASS-4/F-W7-CHECK.md` D-3) — the pass-3 form was false at the moment it was written.** It read *"returns the root plus `carry` · `waves` · `conformance` · `conformance/PASS-1` · `conformance/PASS-2` — **five directories**"*; `conformance/PASS-3/` had existed for **94 minutes** by then, and the same round's prose cited PASS-3 at six sites. **The live output, pasted whole** — ⟨cmd⟩ (base: `$V/docs/tranches/X/fourier`) `find . -type d`:

  ⌧ **THE ENUMERATION IS DROPPED AND THE CLASSIFICATION PUBLISHED IN ITS PLACE (repair round 5, REST-36; P5-5).** Re-run at the pass-5 settle the command returns **nine** lines — `./conformance/PASS-5` is absent from the round-4 paste — which is the **third generation of one receipt**: round 2 struck it for understating, round 3 replaced it with a differently-wrong list, round 4 re-listed it, **against this file's own argument written two sentences below the block.** The cure §2a already names and then declines is taken here: publish the CLASSIFICATION, not the enumeration.

  **And the load-bearing negative is restated as the ABSENCE PROBE it always needed to be, because an enumeration of a directory that grows once per pass is a receipt with a shelf life of one round** — ⟨cmd⟩ (base `$X`) `/usr/bin/find . -type d | /usr/bin/grep 'contract\|design'` → **no output, exit 1** — the absence probe every gate actually rests on, over a tree that grows one directory per pass (re-cut at repair round 5, REST-36). *(Both pipes are lawful under §0's narrowed rule: one a shell pipeline, one an ERE alternation. The classification — **neither `contract/` nor `design/` exists** — is what every gate here rests on, and it survives the next pass directory, which no enumeration could. Round 2 struck the pre-repair form for understating the list; **round 3 then replaced it with a differently-wrong list, which is the whole argument for not listing at all.**)* **F-W5 §1a creates them.** Until they exist F.W7's obligation toward them is **prospective**: cited-when-created, **cited then and never extended** (no second ruling file — P-10). A gate here may not rest on their present bytes.
- `docs/tranches/X/COHESION.md` — root-authored at the boundary. F.W7 declares its half of every cross-edge in §7c and **requests** the reciprocal; it does not write the spine.
- `docs/tranches/X/{waves,keyframes,parse-that}/**` · `glass-ui/**`, `keyframes.js/**`, `latex-paper/**` (producer trees, reached only by relay) · `scripts/dev/dev.sh` (unowned dirty row, **never staged**) · `package.json`.

### 2b. Disjointness (and worktree plan)

Unit **a** owns `R4-enumeration-census.md` alone. Units **b** and **c** both write `F-W7.md` and are **sequenced across sub-waves** (`a → b → c`), each committing before the next opens. **No worktree plan**: one writer at a time on clean `tranche-u`; a dirty tree at handoff halts the wave. No other X wave writes any §2a path while W7 is open.

---

## 3. Scope

1. Re-run R-4's absence proof as an **enumeration of the persistence surface** — version writers · asset writers · cache keys · hash folds, both trees — and land it as `R4-enumeration-census.md` (G-F7-2).
2. **Restate the ruling question against the corrected evidence** and put it to the owner inline (§4). Never presume it, never pre-answer it, never mint a second ruling file.
3. If FOR: author `R4-variant-storage.md` — a structural-sharing/delta design whose sharing key satisfies the four inherited locks (scope · superset · snapshot-instant · live-field sizing) and whose scope over object kinds is explicit under G-F7-3.
4. If AGAINST: land the **terminal kill with rationale** in `F-W7.md`, naming the incumbent (`atomdiff.py:12-14` as F.W5 §2 clause E16 records it), the cost accepted, and the census artefact that survives it.
5. Emit the value-side limbs (TA-4 restoration, E1's compound `_id`) to the **value.js API row as value-side rows** — never re-booked as fourier defects, never executed here.
6. Run the G-F7-8 set-difference against `F-W5.md` in both directions and commit the artefact.

**Prohibitions.** No fourier byte. No product byte. **No design byte absent G-F7-1's ruling** — **F.W7's own prohibition**, grounded on two banked cells cited by id: **F-W5 §2 clause E16 ⊙** (⟨cmd⟩ `grep -o 'The honest default[^*]*' waves/F-W5.md`, the command's whole output: *"The honest default v2 MUST carry absent a ruling: no trie; whole-snapshot duplication is the recorded shipped behaviour."*) and **F-W5 §3 gate G7 ⊙**, recorded UNRULED (⟨cmd⟩ `grep -o '\*\*G7\*\* ⊙ . trie disposition (R-4)' waves/F-W5.md` → *"**G7** ⊙ | trie disposition (R-4)"* — the `.` in the pattern stands for the source's table pipe, which the OUTPUT carries and the PATTERN does not) ▲ *(round-4 cure, `PASS-4/F-W7-CHECK.md` D-7: both receipts here were `^| \*\*ID\*\*` **row-shape anchors** — a literal pipe as a pattern character, the exact form §0's round-3 declaration said was gone, and the E16 one additionally published its span **without** the source's terminal period. Retired for pipe-free probes that print what is quoted, period included.)*. *(F.W5 has no "§3 Prohibitions": ⟨cmd⟩ `grep -n '^## 3\.' waves/F-W5.md` → `## 3. Gates — 22, all born-RED`. The prohibition stands in F.W7's voice and is never again presented as F.W5's sentence.)* No re-booking of any F.W5 clause. No credit for any cure this wave did not author. No HEAD figure and no line anchor cited as live before F.W0 re-grounds (D-19). No glass-producer row smuggled in as a design clause. No parse-that→fourier routing.

### 3a. Triumvirate dispatch (mandatory: research + plan augment + redress)

- **Bounds expansion that invalidates the wave**: any write to a fourier path; any write to `registry/adjudicated/**`, the lane files, or the intake adjudication; any product-source byte; any edit to F.W5's contract files or to v1.
- **Gate failures not local-edit-recoverable**: **F.W0 fails to re-ground** (then every anchor here is unciteable — **the wave HALTS, it does not proceed on stale bytes**); **G-F7-1 returns no ruling** and the wave cannot state **F-W5 §2 clause E16**'s default without contradicting a standing law; **G-F7-8 non-empty in either direction after two closure passes** (a silent drop or a re-booking is the defect class this program exists to kill).
- **Diagnostic loops whose third iteration halts**: the enumeration census disagreeing with itself a third time on the surface's membership (the known trap is S-8's, re-proven at §5a N-2 — a term query is both noisy and blind here).

---

## 4. OWNER RULING — inline, per the P-10 routing ⊙

**This block is the wave. It is never presumed, never pre-answered, and never moved to a second ruling file** (`contract/OWNER-RULINGS-F.W5.md` — **to be created by F.W5 (§1a); cited when it exists, never extended**). The inline routing is banked, not paraphrased: ⟨cmd⟩ `grep -o 'Declared-not-carried edge: \*\*F\.W7[^*]*\*\*' waves/F-W3.md` → **F-W3 §4, the `S-6 · Owner-gated flags (a/b/c)` entry**, whose tail reads verbatim *"Declared-not-carried edge: **F.W7's anti-tree KISS guardrail stays inline in F.W7.**"* *(round 3: the command is narrowed to the span it certifies — the pass-2 `grep -n` printed the whole entry and left the reader to find the tail inside it)* ▲ **ERRATUM (E-3, recorded not inherited)**: through pass 2 this parenthesis read *"P-10 verbatim: 'F.W7's trie ruling stays inline in F.W7'"* — a string that ⟨cmd⟩ (base `$V`) `/usr/bin/grep -rl "trie ruling stays inline" docs/` — **re-cut to the file-list form at repair round 5, REST-34; §0 item 6's own round-3 cure retired the `-rn` address form because its addresses move whenever a strike note is edited** — finds **in this file (plus the two PASS-5 conformance artefacts, disclosed) and nowhere else in the tree**. It was this file's own paraphrase of F-W3's sentence, wearing a quotation mark. **STRUCK; the banked bytes above are the only quotation this file makes of the routing.**

**The question.** The commission (**CENSUS §4 wave-sketch 8**, ⟨cmd⟩ `grep -n "structural.sharing" CENSUS-2026-08-03.md`) requires *"a structural-sharing/delta design proven on both object"* *"kinds"* — the line wraps in the source; the elision is the newline and nothing else. The incumbent is an explicit anti-tree KISS guardrail (⟨cmd⟩ `sed -n '1,15p' $F/api/lib/crud/atomdiff.py`): *"the atoms are a flat BAG (not a tree / Merkle / document); the diff is a whole-atom replace …; there is no three-way / DAG / merge."* Both are live. Neither is struck. **Does the union adopt structural sharing for variant storage, or does the guardrail stand?**

**The honest default absent a ruling** (**F-W5 §2 clause E16 ⊙**, ⟨cmd⟩ `grep -o 'The honest default[^*]*' waves/F-W5.md` → *"The honest default v2 MUST carry absent a ruling: no trie; whole-snapshot duplication is the recorded shipped behaviour."* — carried verbatim and not contradicted here; the clause head itself is ⟨cmd⟩ `grep -o '\*\*E16\*\* ⊙ Trie disposition — the requirement with no material on either side' waves/F-W5.md`. *Round-4 cure, D-7: the pass-3 receipt was a `^| \*\*E16\*\*` row-shape anchor, retired with its twin at §3*): **no trie; whole-snapshot duplication is the recorded shipped behaviour.**

**What the corrected evidence changes about the question** ⟨CC ×2 + the §8 line, §5a⟩:

1. The guardrail is **not bilateral**. It is one docstring in one tree, and that docstring names as its adopter a value.js file (`lib/crud/atomdiff.ts`) that TA-4 excised. The value tree's only surviving statement on the subject asserts the opposite word ("Merkle property", `hash.ts:6`). The ruling is therefore **one tree's law being asked to bind two**, not a convergent consensus.
2. The guardrail already **placed sub-object granularity client-side** ("the diff-viewer field-diffs a changed sub-object client-side, F-06"). R-4's named compression targets are precisely those sub-objects. So this is not "no structure ever" — it is "structure at display time, not at rest," and the ruling either extends that line or moves it.
3. "Zero material on either side" is **true for version-atom sharing and false as a statement about the trees**: content-addressed sharing ships today at three named seams and has already produced one BLOCKER.

**Cost of each branch, stated honestly.**

| branch | what it buys | what it costs |
|---|---|---|
| **FOR** (adopt structural sharing) | Stops re-persisting a 12-field `ContourSettings` + 6-field `AnimationSettings` bag on every one-integer variant | Inherits four live hazards on line one: unscoped key (**E1**), non-superset key (**E13**/C-25), wrong-instant stamping (M-12 ⟨NEW⟩), and a share-hit that skips work (**E17**/C-2's BLOCKER mechanism). Reverses a documented standing decision in the tree that authored the pattern |
| **AGAINST** (guardrail stands) | Preserves KISS; costs zero new failure modes; F.W8 proceeds unchanged | The commission's sketch-8 requirement is retired. Whole-snapshot duplication remains the shipped behaviour. **F.W7 closes as a terminal kill with rationale, which is a completed wave** (COHESION §3 item 2) |
| **NO RULING** | — | **The wave cannot open.** Design bytes are prohibited by **§3's own prohibition**, grounded on **F-W5 §2 clause E16 ⊙** (the honest default) + **F-W5 §3 gate G7 ⊙** (recorded UNRULED); the enumeration census (unit a) is the only authorable output |

---

## 5. Carry — the agglomerated rows (**15**, counted)

**Count, stated over the counted table.** §5a **5** + §5b **1** + §5c **4** + §5d **3** + §5e **2** = **15**. The pre-repair table held **14**; the "11 of 11, zero drops" claim was a **miscount** (**PASS-1's** `F-W7-CHECK` D-4 — record-qualified at round 3, because each pass mints its own D-numbers and a bare `D-4` is now a homonym across three registers, which is this file's own R-5 rule turned on itself) and is retired — a census wave may not state its own denominator by assertion. The fifteenth row is the minted `F-MAIL-∅` census row (§5a, R-11). **Zero drops** is now a claim *about the counted set*: every row below is present, numbered, and reproducible.

`‡` BLOCKER-weight at its home · `⊙` OWNER-GATED · `▲` a cure-integrity lock binding the *repair*, not the finding · ⟨NEW⟩ this seat's own measurement · ⟨CC⟩ correction of the founding lane evidence. Dedupe is by identity: where two records witness one mechanism the row carries **both ids and books once**. Cross-listed ids appear by **citation**, never by re-booking.

### §5a — The ruling's substrate (5 rows)

| row | banked ids | witness | lock |
|---|---|---|---|
| **R-4 ≡ E16 ≡ G7** ⊙ | lane-crud §2 R-4 + §7 · CENSUS §4 wave-sketch 8 + §5 risk 4 [P1] · **F-W5 §2 clause E16 ⊙ + F-W5 §3 gate G7 ⊙** (by clause and gate id — never by line, per the keystone's own instruction on its `F.W5 → F.W7` edge row) · **ZERO adjudicated `fr-*.md` rows** | R-4's probe returns zero *true* hits for **version-atom sharing** on either tree; what ships instead is whole-snapshot duplication — fourier's `VisualizationVersion` stores all 5 atoms in full incl. the 12-field `ContourSettings` and 6-field `AnimationSettings` on **every** version row (`visualization.py:241-245`); value's `PaletteVersion` stores the full `colors: PaletteColor[]` (`model.ts:88`). Neither shares structure parent→child; a variant changing one integer re-persists the whole bag | **ONE identity, three witnesses. F.W7 CITES, never re-books** (M-25). ⊙ Absent a ruling, **E16**'s default is carried verbatim and **not contradicted**. **DISSENT RECORDED AND PRESERVED — ONE quotable home, quoted ONCE (R2-1-LAW item 4: two homes are two quotes with two ⟨cmd⟩ notes, never a fusion).** The dissent's banked bytes are **F-W10 §2.3's SS-4-PREREQ row**, pasted from the command that prints them (⟨cmd⟩ `grep -o 'Dissent RECORDED: the commission.s trie-compression collides head-on with the standing anti-tree KISS guardrail in BOTH trees (.atomdiff.py:12-14.) — THE GUARDRAIL IS THE INCUMBENT; an owner ruling precedes design' waves/F-W10.md`): *"Dissent RECORDED: the commission's trie-compression collides head-on with the standing anti-tree KISS guardrail in BOTH trees (`atomdiff.py:12-14`) — THE GUARDRAIL IS THE INCUMBENT; an owner ruling precedes design"*. **F-W5 §2 clause E16 ▲ is the CLAUSE-side home of the same dissent and is cited by clause id, never quoted through** — it does not carry those bytes and says so in terms, quoted here **to the end of its sentence** (⟨cmd⟩ `grep -o '\*\*SS-4 FLAGS THIS RULING INLINE[^\|]*' waves/F-W5.md` — the bracket class is the markdown-escaped table pipe, as everywhere in this file): *"**SS-4 FLAGS THIS RULING INLINE. Dissent recorded at `F-W10.md` §2.3, the `SS-4-PREREQ` row** — its banked bytes are quoted **ONCE** in this file, with their ⟨cmd⟩ provenance note, at **§4's `F.W5 → F.W7` edge row**; this cell quotes nothing and states the substance in F.W5's own voice: the trie/structural-sharing requirement collides with the standing anti-tree guardrail above, the guardrail is the incumbent, and the owner ruling precedes design"*. ▲ **Round-3 cure (F-W7-CHECK D-7(ii)): pass 2 stopped this quotation at *"in F.W5's own voice"* with no ellipsis, dropping the very clause that states the substance. An unmarked truncation inside a "verbatim" marker is the same defect as an added word; the span now runs to the sentence's end and the command prints exactly it.** ▲ **ERRATUM (E-3, recorded not inherited) — the address failed twice, then the QUOTATION failed once.** Pass 1 anchored this dissent at a blank line in `F-W10.md`; pass 2 convicted the re-anchor as *"a sentence that exists nowhere"* on a second, equally numeric reading two lines off; **the row-label cure closed that, and no line number into `F-W10.md` appears anywhere in this spec.** But pass 2 ALSO published, under a *"banked at F-W5 §2 clause E16 ▲, verbatim"* label, a **different** sentence — *"…the trie requirement collides head-on with a standing KISS guardrail present in BOTH trees — `atomdiff.py:12-14` is the INCUMBENT — and the owner ruling PRECEDES design."* ⟨cmd⟩ `grep -o 'the trie requirement collides head-on' waves/F-W5.md` → the phrase survives at F-W5 **only** inside §4's `F.W5 → F.W7` row, which quotes it **in order to strike it**: ⟨cmd⟩ `grep -o 'a \*\*paraphrase of those bytes, typed from the shape of the ruling rather than read from the sibling\*\* — the convicted class. It is STRUCK' waves/F-W5.md` → *"a **paraphrase of those bytes, typed from the shape of the ruling rather than read from the sibling** — the convicted class. It is STRUCK"*; ⟨cmd⟩ `grep -o 'atomdiff.py:12-14 is the INCUMBENT' waves/F-W5.md` → **no output**, while ⟨cmd⟩ `grep -o 'guardrail is the INCUMBENT[^*]*' waves/F-W5.md` → *"guardrail is the INCUMBENT, but it is NOT carried on both sides today"* (the live, differently-worded sentence). *(Round 3, R3-3.10: the three `grep -c` forms are retired for the word-output above, and the paraphrase-strike is now quoted at F-W5's own emphasis rather than this file's re-bolding of it.)* **This file was quoting struck bytes as a live sibling's current bytes — R2-1c's convicted class, committed here after the round had already convicted it elsewhere. STRUCK.** The two real homes above replace it, un-fused. The commission requirement and the standing constraint are both live; neither is struck (the "BOTH trees" premise is corrected — not voided — at N-1) |
| **P-10 (F.W7 limb)** ⊙ | NO-WAVE-OWNER packet P-10 | **The limb's banked spelling, by a command that prints exactly the quoted span** — ⟨cmd⟩ `grep -o 'Declared-not-carried edge: \*\*F\.W7[^*]*\*\*' waves/F-W3.md` → **F-W3 §4, the `S-6 · Owner-gated flags (a/b/c)` entry**, tail verbatim: *"Declared-not-carried edge: **F.W7's anti-tree KISS guardrail stays inline in F.W7.**"* ▲ **STRUCK this round**: the string *"F.W7's trie ruling stays inline in F.W7"*, published here through pass 2 under a verbatim label, is at **no coordinate in the tree** — ⟨cmd⟩ (base `$V`) `/usr/bin/grep -rl "trie ruling stays inline" docs/` — **file-list form, re-cut at repair round 5, REST-34** — returns this file as the sole wave-side home, with the two PASS-5 conformance artefacts disclosed as residue. It was a paraphrase of the bytes above | The §4 block above **is** the discharge. **No second ruling file beside `contract/OWNER-RULINGS-F.W5.md` — which F.W5 creates and F.W7 cites only when it exists** (⟨cmd⟩ `grep -n 'OWNER-RULINGS' waves/F-W5.md` → **F-W5 §1a's create row**, verbatim: *"`$V/docs/tranches/X/fourier/contract/OWNER-RULINGS-F.W5.md` \| create — the inline ruling block SS-4 requires"*, and F-W5 §1a's own ⟨cmd⟩ line records `ls …/contract/` → *No such file or directory*). P-10's other four limbs are NOT F.W7's — see §7c and §8. **Corroborated TWICE, at two stable anchors — TWO QUOTES, TWO COMMANDS (R2-1-LAW item 4; F-W7-CHECK D-2, cured at round 3).** ⟨cmd⟩ `grep -o 'Declared-not-carried edge: \*\*F\.W7[^*]*\*\*' waves/F-W3.md` → **F-W3 §4's `S-6` entry**, verbatim *"Declared-not-carried edge: **F.W7's anti-tree KISS guardrail stays inline in F.W7.**"* · ⟨cmd⟩ `grep -o '\*\*Explicitly declared as an edge, not carried\*\* — it stays inline in F\.W7 (S-6)' waves/F-W3.md` → **F-W3 §X, the `F.W7's anti-tree KISS guardrail` exclusion row**, verbatim *"**Explicitly declared as an edge, not carried** — it stays inline in F.W7 (S-6)"*. ▲ **ERRATUM (E-3), and it is the round's own lesson in miniature**: pass 2 offered **one** command — `grep -rn "not carried" waves/F-W3.md` — as the producer of **both** quotes. It returns the §X row and **never** the S-6 tail, whose spelling is `Declared-not-carried`: a **hyphen-vs-space blindness**, committed in the row that convicts spelling-blind probes, and the precise detector failure R2-9 and R3-5's four-axis standard exist to kill. **A receipt that cannot produce the bytes it is offered for is a fabrication with a command attached.** Both anchors survive F.W3's round-3 re-authoring (⟨cmd⟩ re-run against the landed **§X.1-v4** file, this seat) and are addressed by label at both ends. |
| **N-1 ⟨NEW⟩⟨CC⟩ · The guardrail is not bilateral** | this seat, read-only, re-verified at fold 2026-08-28 and again at repair round 2; corrects **lane-crud §7** (*"both sides carry an explicit anti-tree KISS guardrail (`atomdiff.py:12-14`)"*) and the **two** places the premise propagated, **in two different spellings — which is why one probe never found both** ⟨CC, this seat⟩: ⟨cmd⟩ `grep -rl "carried on BOTH sides" waves/` → **`F-W8.md`** (and this file, whose cross-references quote it), the bytes sitting in **§5c's `F.W7` sibling row**, and ⟨cmd⟩ `grep -rl "quoted both trees" waves/` → **`F-W6.md`** (and this file), the bytes sitting in **§4's `F.W7` cross-edge row** — *"The standing anti-tree KISS guardrail (`atomdiff.py:12-14`, quoted both trees) binds until the owner moves it"* (⟨cmd⟩ `grep -o 'The standing anti-tree KISS guardrail (.atomdiff.py:12-14., quoted both trees) binds until the owner moves it' waves/F-W6.md`) — **one file each, and no third file in `waves/` carries either spelling**. ▲ **Round-3 cure (F-W7-CHECK D-4): the source's `quoted both trees` is PLAIN; pass 2 bolded it inside a verbatim marker here while quoting the same row un-bolded at G-F7-11, so this spec disagreed with itself about one sentence's bytes. Emphasis inside a verbatim marker is a rendering, not the bytes — F-W5's E13 D-6 law, which this file adopts at §5c and had breached at its own N-1.** *(Through pass 2 this cell said "the one place"; the second site is a spelling-variant miss of exactly the shape R2-9's detector law exists to kill, committed inside the row that convicts spelling-blind probes.)* | `atomdiff.py` line 1 reads *"authored once (fourier), adopted twice (value.js twin)"* and line 7 names the twin **`lib/crud/atomdiff.ts`** — the file TA-4 excised. `grep -rniE "merkle\|flat bag\|not a tree" $V/api/src $V/src` → **exactly one hit, and it asserts the opposite word**: `api/src/modules/palette/hash.ts:6` — *"Identical content always produces the same hash (Merkle property)."* | ▲ **The bilateral-guardrail premise is FALSE AS STATED and the ruling question is re-stated against the correction (§4).** The finding is **not** that the guardrail is void — it stands as the incumbent per the dissent banked at **F-W10 §2.3, the SS-4-PREREQ row** (*"THE GUARDRAIL IS THE INCUMBENT; an owner ruling precedes design"*, ⟨cmd⟩ at §5a's R-4 row; the clause-side home is **F-W5 §2 clause E16 ▲**, cited by clause id and not quoted through) — but that its bilaterality was inherited from a docstring citing a deleted file. **Same defect class as S-8**, one register up: a *presence* claim resting on one file's bytes. ▲ **ADOPTED AT THE KEYSTONE THIS ROUND ⟨CC⟩ — the correction is no longer this wave's alone**: ⟨cmd⟩ `grep -o 'guardrail is the INCUMBENT[^*]*' waves/F-W5.md` → **F-W5 §2 clause E16 ▲** now reads *"guardrail is the INCUMBENT, but it is NOT carried on both sides today"*, under a heading this seat pasted from the command that prints it (⟨cmd⟩ `grep -o 'BILATERALITY CORRECTED AT THIS END[^*]*' waves/F-W5.md`): *"BILATERALITY CORRECTED AT THIS END, by this seat's own value-side measurement — the guardrail is the INCUMBENT, but it is NOT carried on both sides today"*, closing (⟨cmd⟩ `grep -o 'The correction narrows the premise; it does NOT void the guardrail' waves/F-W5.md`) *"The correction narrows the premise; it does NOT void the guardrail"* — **which is exactly N-1's own disposition, reached independently at the other end.** ▲ **Pass 2's rendering of this lock, *"`atomdiff.py:12-14` is the INCUMBENT"* attributed to E16 ▲, is STRUCK**: ⟨cmd⟩ `grep -o 'atomdiff.py:12-14 is the INCUMBENT' waves/F-W5.md` → **no output**, while the live sentence is the differently-worded one quoted above. The words are F.W7's paraphrase of two sources; the sources are quoted instead. *(Round 3, R3-3.10: the `grep -c 0 / 1` pair is retired — an absence is published as an empty word-output, never as a banked numeral over a moving file.)* Rider, re-measured this round over **both** spellings: **F-W8 §5c's `F.W7` sibling row** quotes the guardrail correctly and then appends *"— carried on BOTH sides"*, and **F-W6 §4's `F.W7` cross-edge row** carries the same premise as *"(`atomdiff.py:12-14`, quoted both trees)"* (plain in the source; the round-2 bolding is struck) — both declared here as cross-references and routed to their reciprocal requests (§7c); **`F-W8.md` and `F-W6.md` are read-only to this wave and this spec proposes no byte of either.** ▲ **The correction has meanwhile been ADOPTED at the keystone** (F-W5 §2 clause E16 ▲, quoted in this row's lock cell), so the premise now stands corrected at one end and stale at two — **which is the propagation this wave's §12 lesson is about, measured rather than predicted** |
| **N-2 ⟨NEW⟩ · The probe is noisy AND blind** | this seat, 2026-08-28, re-measured at repair round 1; extends S-8; the noise half **already enumerated by lane-crud §2** (credit given, not re-claimed) | `trie` is a substring of `entries`, `asymmetries`, `geometries`, `RETRIES`: the six-term probe re-run over the 66 `fr-*.md` returns **147 lines — 146 substring artefacts and ONE true hit**. Word-bounded (`grep -rniE "\btrie\b\|prefix.?tree\|\bradix\b\|patricia\|structural.?sharing\|delta.?compress" fr-*.md`) the survivor is **`fr-ContourEditorCanvas.md:56` L-5**, routed F.W3/W4 — excluded with reason at §8, never F.W7's to book. The blindness half is S-8's: the probe cannot see sha256 content addressing at all | **A probe that is simultaneously over- and under-inclusive cannot found a wave.** ▲ **The earlier figures ("60+ lines, every one a substring artefact") are STRUCK: the count was wrong and the universal quantifier concealed the corpus's one true hit.** This is the concrete case for G-F7-2's enumeration replacing the term query |
| **F-MAIL-∅ ⟨minted⟩ · Zero adjudicated registry rows target this wave** | the 66 `fr-*.md` whole; the **KF-W1 §5 carry row C-14 (`KF-MAIL-∅`)** form, and its **§8 exclusion row ⟨C-14⟩** | **Stated so the grep it invites cannot falsify it**: the load-bearing measurement is the **TOKEN, not the substring**. `grep -oE '(^\|[^A-Za-z])F\.W7([^0-9A-Za-z-]\|$)' fr-*.md \| wc -l` → **0 across all 66 records**. A bare `grep -o "F\.W7" fr-*.md` returns **2** — `fr-Tooltip.md:10` and `:32` — and **both are the substring inside `KF.W7`**, a keyframes cross-repo relay note (`grep -c "KF\.W7" fr-Tooltip.md` → 2), disposed here inline. `F.W7` is absent from the terminal-disposition alphabet entirely. **`routedTotal = 0`; `bookedCount = 0`** ▲ **AND AT ROUND 4 THE MEASUREMENT IS NO LONGER THIS WAVE'S OWN (R4-3 / R4-10): the CENSUS OF RECORD adopts it.** ⟨cmd⟩ `grep -o 'The F.W7 ∅ posture is TRUE AT THE CORPUS, by enumeration' conformance/CENSUS-CANONICAL.md` → *"The F.W7 ∅ posture is TRUE AT THE CORPUS, by enumeration"*; ⟨cmd⟩ `grep -o 'No row in any record routes' conformance/CENSUS-CANONICAL.md` → *"No row in any record routes"* (the source wraps; its next line completes the sentence as `F.W7**`). **The canonical's §2 carries no F.W7 roster section at all** — ⟨cmd⟩ `grep -o '^### F\.W7' conformance/CENSUS-CANONICAL.md` → **no output**, an absence published as an absence. **A wave whose roster is zero does not get to be its own census authority** — four passes of self-derivation are now superseded by one operand, and this row cites it rather than re-deriving it a fifth time | ▲ **THE LOCK — three sentences, two homes, quoted as three quotes and never fused (R2-1d; R2-1-LAW item 4).** ⟨cmd⟩ `grep -n 'registry rows gets no manufactured ones' ../keyframes/waves/KF-W1.md` → **KF-W1 §5 carry row C-14 (`KF-MAIL-∅`), the LOCK (L-19) clause**, verbatim: *"a wave with no registry rows gets no manufactured ones"*. ⟨cmd⟩ `grep -n 'The empty column is a finding, not a gap' ../keyframes/waves/KF-W1.md` → **the same C-14 cell**, verbatim: *"The empty column is a finding, not a gap."* ⟨cmd⟩ `grep -n 'gets no invented ones' ../keyframes/waves/KF-W1.md` → **KF-W1 §8, the exclusion row ⟨C-14⟩**, verbatim: *"A wave with no adjudicated rows gets no invented ones. The ∅ is a finding ⟨C-14⟩"*. ▲ **ERRATUM, root cause named**: the single fused "verbatim" string this row previously carried (*"…gets no manufactured ones — the ∅ is a finding, not a gap"*) was **prescribed by PASS-1 R-11.1 and copied from the ruling, not read from the bank** — it welded the LOCK clause to the §8 row's tail and to C-14's own *empty column* sentence under one attribution and one line number into a live sibling. **A rulings file is a cure-shape, never a quotable authority; a "verbatim" label may point only at the cited file's own bytes, and only with the ⟨cmd⟩ that produced them.** The wave's specced work is **exactly** the owner-gated design question (§4) and its declarations, and nothing else. **No registry row, gate, or bounds item is minted to fill the empty column.** The band denominator `F.W5–W8` is **F.W5's** row, not F.W7's (§5d, B-2's split) — measured spelling-agnostically (`F\.W5[-–]W8`, BOTH U+002D and U+2013) per the program's census idiom |

### §5b — Method (1 row)

| row | banked ids | witness | lock |
|---|---|---|---|
| **S-8 (+ K-13 co-sign)**, applied to R-4's own absence proof | `fr-AdminAuditLog.md:126` (method law) · `fr-GalleryAdminBanner.md:90` K-13 (co-signed) | Verbatim from the frozen corpus (⟨cmd⟩ `grep -n -F 'an absence-proof must enumerate the surface, not query one name for it' fr-AdminAuditLog.md` → `:126`, one hit): **"an absence-proof must enumerate the surface, not query one name for it"** — adopted from the K-1/K-2/K-6 kills (`./input` missed `./forms`; `log_audit(` missed the inlined second writer) and co-signed at K-13, where *"Not in the corpus"* died against **26 occurrences / 17 files**. Four shipped counter-witnesses the six-term probe cannot see: `image_storage.py:269-283` content-addresses contours on ordered pairs so a hash hit **is** byte-identical reuse (fr-ContourEditorCanvas K-3) · `database.py:98` gives visualizations a **plain** `content_hash` index (fr-GalleryDraftsSection B-2) · `hash.ts:8-17` + `findByHash {_id: hash}` already share **one** version row across palettes (**F-W5 §2 clause E1**, by clause id) · the dedup-consequence family (fr-ImageUpload roster 12/23, m-15) | **F.W7's FIRST act.** Runs **before** the ruling is drafted — the question is mis-stated if the wave presents "zero material" as fact. It does **not** re-open R-4's conclusion for *version-atom sharing* (still absent both trees); it corrects the **scope** of the claim. Same class as the banked counting lock at **F-W5 §2's `§D` register, clause D1** — quoted at its own case and **from a command that prints the whole span it certifies** (⟨cmd⟩ `grep -o 'a .@router.. grep is BLIND to prefixed routers[^*]*' waves/F-W5.md`, the `.` standing for the source's backticks): **"a `@router.` grep is BLIND to prefixed routers (`@gallery_router.get("/cursor")`) — read against 13/44, never as zero."** *(Pass 2's lower-cased "a … is blind to prefixed routers" rendering was struck at round 2 as a casing drift; round 3 closes the other half, F-W7-CHECK D-6: the stated command began its output at "grep is BLIND" while the published quote began at "a `@router.`", so the quotation was a true substring of the source but **not the output of the receipt beside it** — the leading `a ` is restored and the command widened to produce it. A receipt narrower than the quote it certifies certifies nothing.)* |

### §5c — What must be settled before a key can exist (4 rows)

| row | banked ids | witness | lock |
|---|---|---|---|
| **TA-4 ≡ E3** ⊙ ‡ | lane-crud §0/§2 R-3/§7 · CENSUS §5 risk 3 [P0] · **F-W5 §2 clause E3 ⊙ + §3 gate G4 ⊙** (⟨cmd⟩ `grep -o '\*\*E3\*\* ⊙ Diff-clause participation' waves/F-W5.md` → *"**E3** ⊙ Diff-clause participation"*, whose banked-id cell is ⟨cmd⟩ `grep -o '\*\*TA-4\*\* (megatranche COHESION §2 / lane-crud §7 / SS-4 prerequisite)' waves/F-W5.md` → *"**TA-4** (megatranche COHESION §2 / lane-crud §7 / SS-4 prerequisite)"*, and the gate is ⟨cmd⟩ `grep -o 'diff-clause participation (TA-4)' waves/F-W5.md` → *"diff-clause participation (TA-4)"* (**G4 ⊙**) — three pipe-free receipts replacing one row-anchored probe, per R3-5.4: **no receipt in this file matches a table pipe, so none depends on how a reader un-escapes `\|`**) · COHESION §2 SS-4 edge · **F-W10 §2.3, the SS-4-PREREQ row** | Re-measured read-only **at fold, 2026-08-28**: `grep -rn "atomdiff\|atomDiff" api/src src` → **one comment hit** (`api/src/modules/palette/__tests__/palettes-forks.test.ts:9`, which names T.W1/TA-4 as the excision); `ls api/src/lib` → *No such file or directory*. **`/Users/mkbabb/Programming/fourier-analysis/docs/tranches/J/design/J-diff-shape.md` §6** — the file's ONE real home, in the READ-ONLY fourier tree — has a close-gate assuming **both** probes exist; value.js's cannot run | ⊙ Owner rules **RESTORE (a TA-4 reversal)** or **F.W7 re-scopes to ONE object kind EXPLICITLY — never silently.** COHESION §2, pasted from ⟨cmd⟩ `grep -A1 'the value-side atomdiff restoration' COHESION.md` (the sentence wraps in the source; the join below is that newline and nothing else): *"the value-side atomdiff restoration (TA-4) is a named prerequisite or the contract is" / "re-scoped explicitly."* **F.W5 §2 E3 / §3 G4 is the ruling's home — F.W7 cites, does not re-ask.** Restoration is value-side and rides the value.js API row; **never re-booked as a fourier defect** |
| **V-β ≡ E1** — *the single most load-bearing carry* | lane-crud §7 (V-β) · CENSUS §5 risk 5 [P1] · **F-W5 §2 clause E1 — home** (⟨cmd⟩ `grep -o '\*\*E1\*\* Compound per-entity version identity' waves/F-W5.md` → *"**E1** Compound per-entity version identity"*, with its banked-id cell ⟨cmd⟩ `grep -o '\*\*V-β\*\* (lane-crud §V-β, +§7) — value-side' waves/F-W5.md` → *"**V-β** (lane-crud §V-β, +§7) — value-side"*) | Re-measured **at fold, 2026-08-28**: `hash.ts:8-17` `computeContentHash` folds **only** `{name, colors}` — **never `paletteSlug`**; `paletteVersion.ts:13-14` `findByHash` filters `{_id: hash}` with **no slug scope**; `insertIfAbsent` returns early on a hit (`:47`). **Two palettes with identical `(name, colors)` share ONE version row whose `paletteSlug` names only the first.** The adjudicated cure is E1's compound per-entity `_id` — the shape **fourier already solved** (`{viz_slug}:{set_hash}`, CENSUS §3) | ▲ **Content-keyed sharing done unscoped is a DEFECT GENERATOR, not a free win.** This is the shipped witness and the hazard any R-4 design inherits on line one. **E1's compound-`_id` decision PRECEDES any F.W7 sharing-key design** — a key cannot be designed over an identity under repair. F.W7 books ZERO of E1's cure |
| **C-25 ≡ E13** (B-4=C-1, ∘C-25 / R6-8 fold, ⊕ i-7) ‡ | **F-W5 §2 clause E13 — home** (⟨cmd⟩ `grep -o '\*\*E13\*\* Cache identity ⊇ consumed fields ‡' waves/F-W5.md` → *"**E13** Cache identity ⊇ consumed fields ‡"*, banked-id cell ⟨cmd⟩ `grep -o '\*\*fr-ContourSettings B-4 = C-1 ∘ C-25 / R6-8 ⊕ i-7 ⊕ m-18\*\*' waves/F-W5.md` → *"**fr-ContourSettings B-4 = C-1 ∘ C-25 / R6-8 ⊕ i-7 ⊕ m-18**"*) · `fr-ContourSettings.md:43` B-4 | `extraction_cache_key` is a **CLOSED 10-field `json.dumps` literal** omitting `ml_threshold`/`ml_detail_threshold` — the two fields ContourSettings is the **sole producer** of — and the handler short-circuits on the hit **BEFORE** `compute_contours`, while those fields materially drive extraction on the shipped default route | **Clause-level inheritance. The sentence is transcribed from the FROZEN RECORD at the record's own case** — the one place a line cite is permitted (R2-2.2(i)) — ⟨cmd⟩ `grep -n -F 'superset of the request fields' fr-ContourSettings.md` → **`:43`**, one hit, reading **verbatim: *"an operation's cache identity must be a superset of the request fields the operation consumes"*.** Its downstream home is **F-W5 §2 clause E13**, cited by clause id (⟨cmd⟩ `grep -o '\*\*E13\*\* Cache identity ⊇ consumed fields ‡' waves/F-W5.md`, head: *"**E13** Cache identity ⊇ consumed fields ‡"*). ▲ **ERRATUM (E-3) ⟨CC⟩**: through pass 2 this cell published the sentence **up-cased** (*"a SUPERSET of the request fields"*) with a ⟨cmd⟩ against `F-W5.md` that **now returns nothing** — F-W5's E13 struck its own up-cased rendering this round as its D-6 (*"emphasis-capitalisation inside a verbatim marker is a rendering, not the bytes, and a lock that cannot survive a `grep -F` of its own source is not carrying its source"*) and instructs that *"any wave transcribing it takes the record's case, never this file's former one."* **Adopted in terms: the record's case, the record's ⟨cmd⟩, the clause id for the home.** **F.W7's design states its sharing key's superset obligation explicitly or it has no key spec.** F.W7 authors no cure at **E13**'s seam and takes no credit for it. ▲ **Anti-rename, re-verified at round 2: the banked home is E13. F.W5's E12 is a DIFFERENT clause** — ⟨cmd⟩ `grep -o '\*\*E12\*\* Debounced mirror vs synchronous save' waves/F-W5.md`, head: *"**E12** Debounced mirror vs synchronous save"* — a different clause by its own name — **and the earlier "C-25 ≡ E12" keying is retired** |
| **N-3 ⟨NEW⟩⟨CC⟩ · The key's stamping instant** (fr-ContourSettings **S-3 ⊕ B-4 ⊕ M-12**) | `fr-ContourSettings.md:126` (S-3), `:43` (B-4), `:69` (M-12) — the six-term lexical probe registers `:126` as a false positive for `trie` | Lexically the probe is right (*asymme​trie​s*). **Substantively `:126` is the tree's strongest statement on this wave's exact mechanism — and at round 3 it is quoted as TWO spans with TWO commands, at the record's own case** (F-W7-CHECK D-5: pass 2 published it under no ⟨cmd⟩ at all and down-cased the record's sentence-initial *"The same idea"* to fuse the two halves across an ellipsis — a rendering, not the bytes, exactly the D-6 law this file adopts at C-25). ⟨cmd⟩ `grep -o 'S-3 · Client and server independently converged on content-addressed compute identity' fr-ContourSettings.md` → *"S-3 · Client and server independently converged on content-addressed compute identity"* · ⟨cmd⟩ `grep -o 'The same idea, no shared code[^.]*' fr-ContourSettings.md` → *"The same idea, no shared code — and the audit's two sharpest defects are exactly the two asymmetries between the halves (B-4: the server's key is two fields short; M-12: the client stamps its key from the wrong instant)"*. M-12: `lastComputedKey` records the **scheduling** fact, not the result fact, and is stamped from **live refs after the awaits** — an in-flight change is recorded as computed though the request never carried it | ▲ **FOURTH key lock, new to this wave: a sharing key must be stamped from a SNAPSHOT taken at the instant its inputs were read, and must record a RESULT, not a schedule.** Two independent converged content-addressing implementations already exist and **both** of their sharpest defects are key defects — that is the empirical prior any R-4 design must answer. **M-12's cure is the twin surface's one-line snapshot fix; F.W7 takes the CLAUSE only and books nothing.** ▲ **The twin routing is no longer an unresolved `F.W3/W4` in this file — §X.1-v4 decides it** (F.W3's definitive partition, landed first this round; ⟨cmd⟩ `grep -o '^### §X.1-v4 — THE DEFINITIVE FILE-CRITERION PARTITION.*' waves/F-W3.md`). Its criterion, ⟨cmd⟩ `grep -o 'dual .F.W3/W4. rows on ..a.–..e. files home at \*\*F.W3\*\*; all others take the default arm to \*\*F.W4\*\*' waves/F-W3.md` → *"dual `F.W3/W4` rows on `.a`–`.e` files home at **F.W3**; all others take the default arm to **F.W4**"*; and `ContourSettings` is a member of the `.d` unit's Files list — ⟨cmd⟩ `grep -h '^- \*\*Files\*\*' waves/F-W3.md \| grep -o 'ContourSettings' \| sort -u` → `ContourSettings` (the unit lists it inside a brace expansion, which is why a probe written to the full `ContourSettings.vue` spelling misses it — the same spelling-blindness class as D-2). **So M-12 and B-4's fixture leg home at F.W3 `.d`, with F.W4 citing. F.W7 books neither and proposes no byte of either twin.** |

### §5d — What ships today that a design must reconcile with (3 rows)

| row | banked ids | witness | lock |
|---|---|---|---|
| **K-3 ⊕ C-2 ≡ E17** ‡ — *the counter-example to "built from nothing"* | `fr-ContourEditorCanvas.md:48` (C-2), `:105` (K-3), `:139` (recipe) · **F-W5 §2 clause E17 — home** (⟨cmd⟩ `grep -o '\*\*E17\*\* Image bounds on write ‡' waves/F-W5.md` → *"**E17** Image bounds on write ‡"*, and its record-qualified id cell ⟨cmd⟩ `grep -o '\*\*fr-ContourEditorCanvas C-2\*\* \*(NOT fr-GallerySearchBar C-2 at D10, nor fr-FourierShapeExtractor C-2 at G2c; U-12 qualification)\*' waves/F-W5.md` → *"**fr-ContourEditorCanvas C-2** *(NOT fr-GallerySearchBar C-2 at D10, nor fr-FourierShapeExtractor C-2 at G2c; U-12 qualification)*"* — **the record-qualified spelling is the clause's own; the earlier shorthand "C-2 (ContourEditorCanvas)" was this file's paraphrase presented as the clause head and is struck**) | Ratified against a verbatim read of `image_storage.py:269-283`: content-addressing on ordered pairs ⇒ a hash hit is byte-identical points. **The dedup's real consequence cuts the other way and is banked inside C-2: a no-op save re-hits the extraction doc WITH bounds, so the overlay dies exactly when the points CHANGED — i.e. on every genuine edit** | ▲ **K-3's upsert arm is KILLED by both readers — do NOT revive it in any F.W7 design rationale.** Its banked spelling carries **no code formatting**, and this file's added backticks are struck at round 3 (F-W7-CHECK D-8: markup inside a quotation is a rendering, not the bytes) — ⟨cmd⟩ `grep -o 'C-8 upsert arm ("a different point_count can come back") — KILLED' fr-ContourEditorCanvas.md` → *"C-8 upsert arm ("a different point_count can come back") — KILLED"*. ▲ Witness recipe, verbatim at the frozen record's own punctuation (⟨cmd⟩ `grep -n -F 'MOVE A POINT FIRST' fr-ContourEditorCanvas.md` → `:139`, one hit): **"C-2 witness — MOVE A POINT FIRST** (the K-3 dedup makes a no-op save pass spuriously): edit → save → overlay gone; reload → GET backfill restores." *(Pass 2 published this as one em-dashed sentence, fusing the heading with its parenthetical and closing with a period the record does not carry — a punctuation composite under a "verbatim" label, struck.)* ▲ **The design must state what changes when a share-hit short-circuits a write path; a sharing scheme that silently skips work is the mechanism of this BLOCKER.** C-2's cure is **E17**'s; F.W7 books none of it. ▲ **Anti-rename, re-verified at round 2: F.W5's E11 is a DIFFERENT clause** — ⟨cmd⟩ `grep -o '\*\*E11\*\* Easing domain hoisted to the operation' waves/F-W5.md`, head: *"**E11** Easing domain hoisted to the operation"* — a different clause by its own name — **and the earlier "C-2 ⊕ K-3 ≡ E11" keying is retired** |
| **m-15 (=r2-missed-3) ⊕ ImageUpload roster 12 (=L:L-M3/C:C-6) ⊕ roster 23 (=R2-missed-3, K-8)** | `fr-GalleryDraftsSection.md:80` · `fr-ImageUpload.md:22/:46/:57/:90/:124` — m-15 held at the **`F.W5-W8` band** (canonical `` | `m-15` | `r2-missed-3` | `F.W5-W8` | **F.W5-W8** | ``; §2's band roster carries `- **fr-GalleryDraftsSection** (2): \`F-4\` · \`m-15\``), whose wave-side duty R4-10 assigns to **F-W5**, where the cure sits at clause **§C2** ⟨*REST-37*⟩ · roster 12/23 held at **F.W3** (file-criterion §5.d; `K-8` is **TERMINAL (∅)**) ⟨*re-based at repair round 5, REST-32: read **F.W4***⟩ | m-15: the unversioned thumbnail URL + 24 h `public` cache **negates the deliberate re-upload regeneration** the client comment (`workspace.ts:119-121`) and the server's dedup-hit regen branch (`image_storage.py:105-128`) exist to perform. Roster 12: the sha256-stable slug never fires the `:20-23` watcher, so a multi-MB data URL is stranded **and preferred over the regenerated thumbnail** (`:65`). Roster 23 (K-8): `activeReader` is never nulled after a successful read (`:82-84`) — and **on the dedup path `clearPreview` is unreachable, so the double copy is what persists** | Three adjudicated rows prove a content-hash share-hit **invalidates nothing downstream**. ▲ **A share-hit must state its regeneration/versioning consequence** — cache invalidation is part of a sharing design, not a downstream concern. ▲ Verbatim from **F-W5 §2's `§C` register, clause C2** (⟨cmd⟩ `grep -o 'm-15 is CROSS-REFERENCED[^*]*' waves/F-W5.md`): **"m-15 is CROSS-REFERENCED, NOT MERGED with F-4"** — F-4 books *who can fetch*, m-15 books *what they see*. F.W7 authors none of these cures. **DISSENT PRESERVED** (`fr-ImageUpload.md:124`): R1's MAJOR on the dedup-stranded preview held on the ground that defeating the store's own documented regeneration intent is capability loss, not waste; overruled by the banked fr-VisualizationView L-8 precedent (severity-follows-consequence) and kept as the second dissent on the family — **if SS-13 item 5 shows a visibly wrong (pre-EXIF-transpose) bitmap presented as current, the family re-grades** |
| **B-2 (=C-C-1 ∘ L-3, ∘ m-13 rider) ⊕ FR-GV-1 (=L-2/C·C-3) ≡ E5** ‡ | `fr-GalleryDraftsSection.md:40` · `fr-GalleryView.md:34` · **F-W5 §2 clause E5 — home** (⟨cmd⟩ `grep -o '\*\*E5\*\* Create idempotency and dedupe — ONE clause, TWO entry points ‡' waves/F-W5.md`, head: *"**E5** Create idempotency and dedupe — ONE clause, TWO entry points ‡"*) | `createVisualization` sends no `Idempotency-Key` (`api.ts:371-380`); the middleware is explicit **`no header → handler()`** (`idempotency.py:68-70`); `content_hash` carries a **PLAIN** index (`database.py:98`) against flags' deliberate `unique=True` (`:140`). The row never leaves (`savedSnapshots` has ONE writer, the unconditional literal `[]` at `workspace.ts:102`), the button re-enables (`GalleryView.vue:214`), and the second press mints a **second** public visualization | Adjudicated split: props/reconciliation → F.W3/W4; server-side dedupe/idempotency for the create path → the F.W5–W8 union, where **F.W5 took the band row**. ▲ **NO-CREDIT LOCK: if a compression design's apparent benefit is actually delivered by E5's idempotency, F.W7 books ZERO.** The `publishedHashes` misnomer rider (it holds slugs) and m-13 (the success toast prints the USER slug) travel with **E5**, not here |

### §5e — Sizing and substrate (2 rows)

| row | banked ids | witness | lock |
|---|---|---|---|
| **SS-C-1 (write leg) ≡ E7 ⊕ SS-C-2 (rides INSIDE E8) ⊕ M-β4 / L·m-6 ≡ E10** | `fr-SpeedSelect.md:44` (SS-C-1, MAJOR — demoted from C's BLOCKER at component altitude), `:45` (SS-C-2, MAJOR) · **F-W5 §2 homes — clauses E7 · E8 · E10, cited by clause id and quoted by command** (⟨cmd⟩ `grep -o '\*\*E7\*\* PATCH atom coverage + .set_hash. recompute' waves/F-W5.md` → *"**E7** PATCH atom coverage + `set_hash` recompute"* · ⟨cmd⟩ `grep -o '\*\*E8\*\* AnimationSettings — the three-way reconciliation' waves/F-W5.md` → *"**E8** AnimationSettings — the three-way reconciliation"* · ⟨cmd⟩ `grep -o '\*\*E10\*\* Produced-and-unconsumed: ONE disposition' waves/F-W5.md` → *"**E10** Produced-and-unconsumed: ONE disposition"*, whose banked-id cell is ⟨cmd⟩ `grep -o 'M-β4 · \*\*fr-EquationView L·m-6\*\* (=C·D-14) · \*\*FR-EMT-20\*\* (cited F1)' waves/F-W5.md` → *"M-β4 · **fr-EquationView L·m-6** (=C·D-14) · **FR-EMT-20** (cited F1)"*) | SS-C-1: the persisted `speed` atom is settable **only at CREATE/FORK** — `VisualizationUpdate` (`visualization.py:198-210`) and `VisualizationPatch` (`types.ts:256-262`) are the same five fields with `extra="forbid"`, while `animation_settings` is **atom 4-of-5 of the version-identity set** (`atomdiff.py:38`). SS-C-2: **atom 4 is semantically empty** — `fps`/`max_circles`/`duration` have zero cross-wire readers, and the `duration` that `speed` divides is declared 4× with 3 values and a **1000× unit fork** (20000 ms store / 5000 client default / 30.0 s server) | ▲ **Compressing a bag whose members are dead is the wrong cure** — the 12-field `ContourSettings` and 6-field `AnimationSettings` sub-objects R-4 counts as the compression target are exactly where the dead fields live. **E10's produced-and-unconsumed disposition PRECEDES any F.W7 sizing**: a saving measured over fields the contract may retire is **unfalsifiable**. E10's standing requirement, verbatim (⟨cmd⟩ `grep -o 'v2 states ONE disposition[^*]*' waves/F-W5.md`): **"v2 states ONE disposition for produced-and-unconsumed fields, not three ad-hoc deletions."** ▲ **The pairing "E10/B4" is retired: clause E10 closes with its own separation** — ⟨cmd⟩ `grep -o 'Distinct from B4.s liveness predicate' waves/F-W5.md` → *"Distinct from B4's liveness predicate"* (the `.` stands for the source's apostrophe) — **so B4 is a different predicate, cited never conflated.** ▲ *(Round-4 cure at all three sites where this span stands — here, §6 G-F7-6 and §7c's E10 row — R4-7.1 / `PASS-4/F-W7-CHECK.md` D-10: the pass-3 receipt was `grep -o 'Distinct from B4[^*]*'`, whose negated class runs **past the sentence into the markdown table terminator** and prints `Distinct from B4's liveness predicate \|` (that trailing character is the source's table pipe, in the output and never in the pattern). The published span was a true **prefix** of the output but was not the output — the mirror of the D-6 class this file cured at §5b, and a trailing literal pipe of exactly the kind §0's declaration claimed gone. The exact-string probe prints the quoted bytes and nothing else.)* ▲ **Anti-rename, re-verified at round 2: F.W5's E9 owns neither SS-C-1 nor SS-C-2** — ⟨cmd⟩ `grep -o '\*\*E9\*\* One shape, one name' waves/F-W5.md`, head: *"**E9** One shape, one name"* — **and the earlier "SS-C-1 ≡ E8 / SS-C-2 ≡ E9" keying is retired.** F.W7 books none of **E7/E8/E10**; the partial fold `duration = ref(20000)` zero-writers stays banked at **fr-BasisCanvas BC-20** and is never re-booked |
| **OG-F1 (+ D-19 anchor law, X-1 precondition)** — **hard precondition, not a gate of this wave's making** | `docs/tranches/V/megatranche/audit/codex-provenance/INTAKE-ADJUDICATION-2026-08-03.md` §4 OG-F1 (R3-1, R3-4, R4-1, R4-13, R5-1, R6-1) + §3 F.W0 · **F-W5 §4's `X-1 · F.W0 → F.W5` edge row** ▲ **TWO QUOTES, TWO COMMANDS, THE THIRD HIT NAMED, THE TRUNCATION CLOSED (round-4 cure — R4-7.1; `PASS-4/F-W7-CHECK.md` D-8).** ⟨cmd⟩ `grep -o '\*\*Opens after\*\*: \*\*F.W0\*\* (substrate pre-gates — HARD, §4 X-1)' waves/F-W5.md` → F-W5 §0's *"**Opens after**: **F.W0** (substrate pre-gates — HARD, §4 X-1)"* · ⟨cmd⟩ `grep -o '\*\*X-1 · F.W0 → F.W5\*\* . HARD opens-after . Substrate pre-gates FIRST' waves/F-W5.md` → the §4 row's head, *"**X-1 · F.W0 → F.W5** \| HARD opens-after \| Substrate pre-gates FIRST"* (the `.` in the pattern stands for the source's table pipes, which the output carries and the pattern does not), **and the row does not stop there** — its next sentence is this very cell's own canonical-form authority, ⟨cmd⟩ `grep -o 'The corrupt-dist fact carries BOTH its banked witnesses — canonical form: .FR-NP-32. (≡ .fr-PaperSidebar M1.)' waves/F-W5.md` → *"The corrupt-dist fact carries BOTH its banked witnesses — canonical form: `FR-NP-32` (≡ `fr-PaperSidebar M1`)"*, so the lane spelling is uniform at both ends of this edge. *(Pass-3 form, struck: **one** `grep -n 'X-1'` offered as the producer of **two** quotes — it returns **three** lines, the unnamed third being F-W5's `AA-44` row, ⟨cmd⟩ `grep -o "This wave.s own X-1 cell rules that divergence from F-W0.s tables is a defect" waves/F-W5.md` → *"This wave's own X-1 cell rules that divergence from F-W0's tables is a defect"* — and the second published span stopped **mid-sentence with no ellipsis**, in a file that annotates every other elision. This is PASS-3 D-2's class recommitted at a different row in the round that cured it at P-10; the continuation above closes it rather than marking it.)* · **`FR-NP-32` (≡ `fr-PaperSidebar M1`)** · `fr-GalleryAdminBanner` GAB-13 | Re-measured read-only **at fold, 2026-08-28** (unchanged from the F.W5 pass): `git -C $F status --porcelain \| wc -l` → **28**; `git -C $F rev-parse --short=8 HEAD` → **`cd26c653`** on `m/w1-bump-migration`; **`git -C $F cat-file -t 14d83356` → `fatal: Not a valid object name`** — the census pin does not resolve. Plus glass-ui 4.0.0's syntactically corrupt `dist/styles/index.css` — **`FR-NP-32` (≡ `fr-PaperSidebar M1`)**, the canonical form, cited by both ids and never substituted (R2-7.5 / R3-8.1; PASS-3's F-W7-CHECK D-9; through pass 2 this row carried the bare token `(M1)`, which is not an identity: `M1` is a live homonym class across the corpus) — and F8-REACH-01/02 open | **Every anchor R-4 rests on is a pre-F.W0 coordinate against a dirty, unresolvable substrate**, including `visualization.py:241-245`, `atomdiff.py:12-14`, `model.ts:88` — and including this fold's own re-read of `atomdiff.py`, which is a **working-tree** read, not a HEAD read. **F.W0 substrate pre-gates FIRST.** OG-F1's own question (re-root at F.W0 vs freeze the R-coordinates and build from live-tree measurement) is owner-owed; lane evidence favours **freeze-with-adoption**. **If F.W0 fails to re-ground, the wave HALTS; it does not proceed on stale bytes** |

---

## 6. Gates — all born-RED

`F=/Users/mkbabb/Programming/fourier-analysis` (read-only) · `V=/Users/mkbabb/Programming/value.js`.

**Two reading disciplines, binding.** (1) **MEASURE-AT-OPEN**: every fourier figure and anchor is re-confirmed by F.W0 before citation (D-19); the probes below were re-run read-only by this fold seat 2026-08-28 and carry that date. (2) **Split verdict**: a gate closes for F.W7 when *this wave's* obligation is discharged; it goes GREEN only when its named owner lands the change. **F.W7 never claims a GREEN it did not execute.**

| # | gate | born-RED witness (literal probe / named record) | F.W7 close | GREEN owner |
|---|---|---|---|---|
| **G-F7-1** ⊙ | the trie-vs-KISS ruling — **unauthorable unruled** | `sed -n '1,15p' $F/api/lib/crud/atomdiff.py` (lines 12–14 = the guardrail; lines 1 and 7 = the twin citation) · **F-W5 §2 clause E16 ⊙** (⟨cmd⟩ `grep -o 'The honest default[^*]*' waves/F-W5.md`, verbatim: *"The honest default v2 MUST carry absent a ruling: no trie; whole-snapshot duplication is the recorded shipped behaviour."*) · **F-W5 §3 gate G7 ⊙**, recorded **UNRULED** ▲ **THE RECEIPT IS TRUNCATED TO WHAT THE COMMAND ACTUALLY PRINTS (round-4 cure — R4-1.10; `PASS-4/F-W7-CHECK.md` D-2), and the completed sentence is supplied by a SECOND command rather than by finishing the word.** ⟨cmd⟩ `grep -o 'owner rules trie-vs-KISS.\{0,110\}' waves/F-W5.md` → the green-owner cell, **134 characters, cut mid-word and left cut**: *"owner rules trie-vs-KISS. Absent a ruling v2 carries the honest default: **no trie; whole-snapshot duplication is the recorded shipped"* · and the sentence whole, from a probe that prints exactly it: ⟨cmd⟩ `grep -o 'The honest default[^*]*' waves/F-W5.md` → *"The honest default v2 MUST carry absent a ruling: no trie; whole-snapshot duplication is the recorded shipped behaviour."* *(Pass-3 form, struck: the published span appended **`behaviour**`** — eleven characters the command does not produce — silently completing the truncated word **and** closing the bold, under a label reading "as the command prints it, **its cut disclosed**". No cut was disclosed; the cut was repaired. The file demonstrably knew the honest idiom — at G-F7-11 and §7c it discloses F-W8's cut mid-word at "KISS gu" and leaves it there — which makes the divergence **at the wave's keystone, owner-gated gate** a choice rather than an oversight. This is PASS-3 D-6's class: a receipt narrower than the quote it certifies certifies nothing.)*. **The no-design-byte prohibition is F.W7's own (§3), grounded on those two clause/gate ids — F.W5 states no such sentence and has no Prohibitions section at all** (⟨cmd⟩ `grep -o '^## [0-9]\. [A-Za-z-]*' waves/F-W5.md` → `## 0. State` · `## 1. Bounds` · `## 2. Carry` · `## 3. Gates` · `## 4. Cross-edges` · `## 5. Carried` · `## 6. Close` — **and no Prohibitions section among them**; round 3 states the membership and drops both the count word and the pass-2 rendering that truncated each heading's tail without marking it) | **The owner rules. Not before.** While RED, F.W7 authors **ZERO design bytes** and carries E16's default verbatim. **If the ruling goes AGAINST, this gate closes by the wave landing as a TERMINAL KILL WITH RATIONALE** (COHESION §3 item 2) — a completed wave, never a silent drop, never a re-booking | **owner** |
| **G-F7-2** | R-4's absence proof re-run as an **enumeration** | S-8 (`fr-AdminAuditLog.md:126`) co-signed at K-13 (`fr-GalleryAdminBanner.md:90`) + the four counter-witnesses of §5b + N-2's noise measurement | `design/R4-enumeration-census.md` exists and enumerates **four classes on both trees** — version writers · asset writers · cache keys · hash folds — and §4's question is restated against it. The six-term grep is retired as founding evidence | F.W7 itself (unit a) |
| **G-F7-3** | both-object-kinds provability | `grep -rn "atomdiff\|atomDiff" $V/api/src $V/src` → **1 comment hit** (`…/palettes-forks.test.ts:9`); `ls $V/api/src/lib` → *No such file or directory* — **both re-measured at fold 2026-08-28** | **F.W5 G4 rules RESTORE, or F.W7 re-scopes to one object kind EXPLICITLY in its own text. Silence does not clear it** | owner → value.js API row |
| **G-F7-4** | sharing-key identity settled **before** any key design | `sed -n '8,17p' $V/api/src/modules/palette/hash.ts` (folds `{name, colors}`, never `paletteSlug`); `sed -n '13,14p' $V/api/src/modules/palette/repository/paletteVersion.ts` (`findByHash` → `{_id: hash}`, no slug scope) — **both re-measured at fold 2026-08-28** | **F-W5 §2 clause E1** — the compound per-entity version `_id` — is stated. **F.W7 designs no sharing key over an identity under repair** | value.js API row |
| **G-F7-5** | the key states its **superset** obligation | C-25 transcribed from the frozen record at the record's own case (⟨cmd⟩ `grep -n -F 'superset of the request fields' fr-ContourSettings.md` → `:43`), verbatim: *"an operation's cache identity must be a superset of the request fields the operation consumes"*; its downstream home is **F-W5 §2 clause E13**, cited by clause id (the former up-cased rendering and its now-empty `F-W5.md` probe are struck at §5c) — witnessed by `extraction_cache_key`, a closed 10-field `json.dumps` literal omitting `ml_threshold`/`ml_detail_threshold`, with the handler short-circuiting **before** `compute_contours` | Any F.W7 key spec names its consumed-field set and proves the superset relation. **RED by absence: no key spec exists today.** ▲ **On the AGAINST branch G-F7-5 closes VACUOUSLY** (added at repair round 5, REST-39; P5-18), with the kill rationale recording that **no key was designed and no superset relation was owed** — §2a row 3 says `R4-variant-storage.md` is *"never created"* if the ruling goes against, so on AGAINST there is no key spec and **no stated act closed this gate**; §1a's XOR does not cover it | F.W7 (FOR branch) |
| **G-F7-6** | compression targets are **live** fields | `fr-SpeedSelect.md:45` SS-C-2 — atom 4 is semantically empty; `duration` declared 4× / 3 values / 1000× unit fork. **F-W5 §2 clause E10** leaves the produced-and-unconsumed disposition **UNSTATED** — and closes with its own separation (⟨cmd⟩ `grep -o 'Distinct from B4.s liveness predicate' waves/F-W5.md`), *"Distinct from B4's liveness predicate"* | **F.W5 §2 E10** states ONE disposition, and F.W7's sizing is computed **over what survives it** | F.W5 → F.W7 |
| **G-F7-7** | anchor re-grounding *(inherited precondition)* | `git -C $F status --porcelain \| wc -l` → **28**; `git -C $F rev-parse --short=8 HEAD` → **cd26c653**; `git -C $F cat-file -t 14d83356` → **fatal** — **all three re-measured at fold 2026-08-28, and re-run unchanged at repair round 3** · **`FR-NP-32` (≡ `fr-PaperSidebar M1`)**, the corrupt-dist gate in its canonical form (R2-7.5 / R3-8.1, PASS-3's F-W7-CHECK D-9 — cite both ids, never substitute; this was the one X·F spec that stated the fact and never its id) · GAB-13 · F8-REACH-01/02 | **F.W0 re-grounds.** Until then no HEAD figure and no line anchor here is citable (D-19) — including R-4's own and this fold's `atomdiff.py` read. **If F.W0 fails, the wave HALTS** | **F.W0** |
| **G-F7-8** | zero re-booking / zero credit-taking | **Eight F.W5-homed identities, keyed to clause ids in `F-W5.md` §2's `§E` register — E1 · E3 · E5 · E7 · E8 · E10 · E13 · E17.** ▲ **THE DETECTOR, STATED INLINE AND SHAPE-AGNOSTIC (R2-9 closing clause / R3-5.4, PASS-3's F-W7-CHECK D-10 — re-cut at round 3, when the operand was still anchored to markdown-table shape at BOTH ends).** Four axes, binding on this gate and on the `carry-closure.md` artefact that runs it: **(i) shapes** — a clause id counts wherever it is written: table row ⊕ id-headed bullet ⊕ prose walked to its enclosing clause; no row anchor, no row assumption. **(ii) spellings** — `**E13**` · `clause E13` · `§2 clause E13` · `E13 ▲` · `E16 ⊙`, and the ⊙/▲/‡ marks never separate an id from itself. **(iii) positions** — an id counts whether written first or second in a compound (`C-25 ≡ E13`, `E13`/C-25). **(iv) register-qualification** — the test is the **(register, clause-id) PAIR**: `E13` means F-W5 §2's `§E` register's E13 and nothing else; a bare `E13` elsewhere in the program is a homonym, not this identity. ▲ **RUN TODAY, TOKEN-BOUNDED AND DELIMITER-FREE — the detector is now DERIVED FROM AXIS (ii)'s SPELLING LIST rather than from any wrapper (round-4 cure — R4-9.7, executing R3-5.4; `PASS-4/F-W7-CHECK.md` D-6).** ⟨cmd⟩ `grep -ohE '(^\|[^A-Za-z0-9])(E1\|E3\|E5\|E7\|E8\|E10\|E13\|E17)([^0-9A-Za-z]\|$)' waves/F-W5.md \| sed -E 's/[^A-Za-z0-9]//g' \| sort -u` → `E1` · `E10` · `E13` · `E17` · `E3` · `E5` · `E7` · `E8` — **the set, printed as words, never as a total** (R3-3.10). The register is ⟨cmd⟩ `grep -o '^### §E — Provenance, lineage and persistence (the union.s core)' waves/F-W5.md` → *"### §E — Provenance, lineage and persistence (the union's core)"*, and its membership, measured by the same token-bounded detector rather than by bold, ⟨cmd⟩ `awk '/^### §E/,/^### §F/' waves/F-W5.md \| grep -ohE '(^\|[^A-Za-z0-9])(E[0-9]+)([^0-9A-Za-z]\|$)' \| sed -E 's/[^A-Za-z0-9]//g' \| sort -u` → **E1…E20**. *(The `§`-heading range is the register's own **identity**, not a delimiter of an id: axis (iv) makes the test the (register, clause-id) pair, so the boundary must come from the register's name. Every pipe above is a shell pipeline or an ERE alternation, the two lawful roles §0's narrowed rule admits; a reader un-escapes each `\|` to a bare pipe before running, per reading rule (i).)* ▲ **ERRATUM (E-3): the pass-3 re-cut traded table-shape anchoring for BOLD-delimiter anchoring and called the result "shape-free".** Its command matched only `\*\*E13\*\*` and so could not match **four of the five spellings axis (ii) itself declares** — `clause E13` · `§2 clause E13` · `E13 ▲` · `E16 ⊙`. That this was not academic is measurable: token-bounded versus bold-bounded, `E13` occurs on **five times as many lines** of `F-W5.md` as the bold probe sees. **The result was unchanged then and is unchanged now — which is exactly the condition R2-9 says does not cure an operand.** A gate that returns the right answer through a restricted detector was defective at authoring, whatever it returned; this file has now made that mistake in two successive shapes and cures it by deriving the probe from its own declared spelling list instead of from any wrapper at all. **The result is unchanged by the re-cut — which is exactly the condition R2-9 says does not cure an operand: a gate that returns the right answer through a restricted detector is defective at authoring, whatever it returns.** + **F.W3's** ImageUpload roster 12/23 ⟨*REST-32: read F.W4*⟩ + the **`F.W5-W8` band's** m-15, whose cure sits at F-W5 §2's `§C` clause C2 ⟨*REST-37*⟩. FR-GIG-5's bar, standing at **F-W5 §0b** (⟨cmd⟩ `grep -n 'FR-GIG-5' waves/F-W5.md`, verbatim: *"F.W5 claims credit for none of them — FR-GIG-5's lesson adopted as a standing bar"*) | `waves/F-W7/carry-closure.md` shows an id set-difference against **F-W5 §2's `§E` clause register** **∅ in BOTH directions**: F.W7 **books zero** of them and **cites all** of them. ▲ **The operand is the register named by heading and differenced BY CLAUSE ID — never a line window and never a per-wave carry ledger** (R-3; R2-2). *(The pre-repair operand — **a twenty-line window of `F-W5.md` named by line, the numerals of which are deliberately not reproduced here** — is struck: it addressed a span that the same round's writes emptied of clauses, and two of its eight addresses had come to sit on a section heading and a `\|---\|` table separator, structurally impossible occupants for a clause citation. It is the plainest possible proof that a set-difference must run on **ids**; naming its coordinates again would re-commit the defect inside its own erratum.)* **Non-empty in either direction after two closure passes ⇒ triumvirate dispatch** | F.W7 itself (unit c) |
| **G-F7-9** | terminal disposition of census wave-sketch 8 | COHESION §3 item 2, pasted whole from ⟨cmd⟩ `grep -A1 'Every census wave-sketch id' COHESION.md` (the item wraps in the source; the join is that newline and nothing else, and round 3 replaces pass 2's unreceipted elision of the parenthetical with the bytes themselves): *"2. Every census wave-sketch id (KF.W0–W10, F.W0–W10) maps to a full spec **or** a terminal kill" / "with rationale — no silent drops, no re-booking."* `CENSUS-2026-08-03.md` §4 sketch 8 exists; F.W7 today has **neither** | **Exactly one** of {full spec, terminal kill with rationale} lands, keyed to G-F7-1. **Both is a defect; neither is a defect** | F.W7 itself |
| **G-F7-10** ⟨added⟩ | the guardrail's **bilaterality** | `grep -rniE "merkle\|flat bag\|not a tree" $V/api/src $V/src` → **one hit, asserting the opposite word**: `$V/api/src/modules/palette/hash.ts:6` *"(Merkle property)"*; `atomdiff.py` line 7 names the excised `lib/crud/atomdiff.ts` as its adopter — **re-measured at fold 2026-08-28** | §4's ruling question is stated against the corrected premise: **one tree carries the guardrail; the other's copy went out with TA-4.** Added because a ruling asked on a false bilaterality premise is a ruling the wave cannot rely on | F.W7 itself (unit b) |
| **G-F7-11** ⟨added⟩ | cross-edge reciprocity declared from this end | COHESION §2, quoted **to the end of its sentence** at round 3 (F-W7-CHECK D-7(i): pass 2 stopped at *"in the spec files."* and supplied a terminal period the source does not carry there) — ⟨cmd⟩ `grep -o 'Cross-repo edges are declared FROM BOTH ENDS in the spec files (the X·V W4-D2 lesson, now law)' COHESION.md`: *"Cross-repo edges are declared FROM BOTH ENDS in the spec files (the X·V W4-D2 lesson, now law)"*. **Re-measured at repair round 2 and again at round 3, 2026-08-28** — ⟨cmd⟩ `ls waves/` → `F-W0.md` · `F-W1.md` · `F-W2.md` · `F-W3.md` · `F-W4.md` · `F-W5.md` · `F-W6.md` · `F-W7.md` · `F-W8.md` · `F-W9.md` · `F-W10.md` and nothing else. *(Round 0's "F-W0 and F-W6 do not exist" witness was **STRUCK — refuted by its own command**.)* **EVERY WAVE-SPEC FAR END IS NOW DECLARED — five of five, each addressed by label, none by line**: **F-W5 §4's `F.W5 → F.W7` row** · **F-W6 §4's `F.W7` cross-edge row** ▲ **THREE EXACT-STRING PROBES, NO INTERVAL BOUND, EACH PRINTING ITS OWN SPAN — the round-4 re-cut of a receipt that could not execute (R4-1's `.{0,262}` cluster ⊕ R4-2.2; `PASS-4/F-W7-CHECK.md` D-1).** ⟨cmd⟩ `grep -o 'F.W6 carries the documented default — \*\*no trie; whole-snapshot duplication is the recorded shipped behaviour.\*\*' waves/F-W6.md` → *"F.W6 carries the documented default — **no trie; whole-snapshot duplication is the recorded shipped behaviour.**"* · ⟨cmd⟩ `grep -o '.trace. (M-β4) and .reconstructed_points. (L·m-6) strikes wait on the ruling.' waves/F-W6.md` → *"`trace` (M-β4) and `reconstructed_points` (L·m-6) strikes wait on the ruling."* · ⟨cmd⟩ `grep -o 'The standing anti-tree KISS guardrail (.atomdiff.py:12-14., quoted both trees) binds until the owner moves it' waves/F-W6.md` → *"The standing anti-tree KISS guardrail (`atomdiff.py:12-14`, quoted both trees) binds until the owner moves it"* (the `.` in each pattern stands for the source's backticks). **The three concatenate to the whole span with nothing elided and nothing cut** — the row's exclusion twin stands in F-W6 §5. ▲ **ERRATUM (E-3), and it is the sharpest of the four rounds**: through pass 3 this witness was a **single `.\{0,262\}` receipt labelled "the command's own output"**. On the pinned BSD binary that command prints `grep: maximum repetition exceeds 255` and **no bytes whatsoever**; at the legal maximum `.\{0,255\}` it stops at *"…binds until the **owne**"*, **seven characters short** of the published span — **so no legal bound on this engine produces the bytes the label attributed to it.** The words were in `F-W6.md` and the substance held; the provenance was fiction. **A receipt nobody ran is not a measurement at all**, and the cure is not a bigger number but the abolition of the number: the anchor is the window. · **F-W8 §5c's `F.W7` sibling row** (⟨cmd⟩ `grep -o 'F.W8 designs no trie.\{0,110\}' waves/F-W8.md`, likewise the command's own output, the budget's cut disclosed: *"F.W8 designs no trie. If G7 **admits** it, the derive leg must exercise structural sharing; if G7 **sustains the incumbent KISS gu"*) · **F-W3 §4's S-6 declared-not-carried tail + §X's exclusion row** · **F-W0 §6b's `F.W7` row** — stated as MEMBERSHIP, never as a count of a file under concurrent repair (R3-3.10): ⟨cmd⟩ `grep -o '\*\*F\.W7\*\* (the owner-gated trie design)' waves/F-W0.md` → *"**F.W7** (the owner-gated trie design)"*, the row headed (⟨cmd⟩ `grep -o 'THE RECIPROCAL, written at repair round 2[^*]*' waves/F-W0.md`) *"THE RECIPROCAL, written at repair round 2 (R2-10.8 / R2-8.2(iii)); this end was the one genuinely owed"* and closing (⟨cmd⟩ `grep -o 'F.W0 books \*\*no\*\* F.W7 registry row, mints none into F.W7.s empty column, and takes no position on the trie question' waves/F-W0.md`) *"F.W0 books **no** F.W7 registry row, mints none into F.W7's empty column, and takes no position on the trie question"*. ▲ **THE BORN-RED IS RE-BASED, NOT RETIRED (E-3, and this gate's own lesson booked against itself).** This gate has now stated its residue wrong **three times** — round 1 (*"F-W5 §4 enumerates ten edges and declares NONE to F.W7"* — false; the register's membership, ⟨cmd⟩ `grep -ohE '\*\*F\.W5 → F\.W[0-9]+\*\*' waves/F-W5.md \| sort -u` → `**F.W5 → F.W1**` · `**F.W5 → F.W2**` · `**F.W5 → F.W6**` · `**F.W5 → F.W7**` · `**F.W5 → F.W8**`, carrying both the `F.W5 → F.W7` row **and** an `F.W5 → F.W8` row the enumeration omitted), round 2 pass 1 (*"a `grep -c` over `F-W0.md` → 0"* — false), round 2 pass 2 (*"→ 1, announced but unwritten"* — false at this seat's read). **All three are STRUCK.** The common cause is not carelessness: each witness was a **count taken against a live sibling under concurrent repair**, whose truth value expires in minutes — **and at round 3 the program made that structural, not advisory: R3-3.10 forbids banking any ⟨cmd⟩ whose output is a count of a live sibling or of this file. Every receipt in this cell is now membership or word-output.** **The gate's operand is therefore re-cut to the ends that are NOT wave-spec files and cannot silently land**, each measured here: **F.W9/F.W10's own file** — its only mention of this wave sits in **F-W10 §2.8's E-16 erratum minute** about this file's former drifted anchors, **not** in a cross-edge row (⟨cmd⟩ `grep -o 'A repair seat that only fixes its outbound citations exports drift it never minutes' waves/F-W10.md` → the minute's own sentence; the classification, not a tally, is what the gate turns on); **SS-4** and **the X·V value.js API row** — no spec file exists for either (⟨cmd⟩ `ls waves/` → the `F-W*.md` set above and nothing else), so their halves are owed to the SS-4 synthesis and the API-row ledger when those surfaces are created, and are RED by construction until then | §7c declares this end of **every row in its table** (count stated there). **All five wave-spec far ends are DECLARED and cross-cited by label — F.W0's, F.W3's, F.W5's, F.W6's and F.W8's. The three still owed are F.W10's edge row and the two non-spec ends (SS-4, the value.js API row).** Added because "declared from both ends" is otherwise unverifiable prose; **re-based this round because a reciprocity gate whose witness is a count against a moving file measures the clock, not the reciprocity** | **F.W10** (its edge row) · SS-4 · the value.js API row |

---

## 7. Sequencing

### 7a. In-wave order

`a` (enumeration census — **authorable while G-F7-1 is RED**) → **⊙ the ruling** → `b` (§4 ruling record + the FOR/AGAINST branch) → `c` (carry closure). Serial; each commits before the next opens. **Unit b cannot open until the owner speaks.**

### 7b. Cure-integrity locks (bind the repair, not the finding)

▲ **K-3** — the upsert arm is dead (banked spelling, unformatted: *"a different point_count can come back"*); never revive it in a design rationale · ▲ **C-2 witness recipe** — *"MOVE A POINT FIRST"*; a no-op save passes spuriously · ▲ **E1** — unscoped content keying is a defect generator; the identity is under repair · ▲ **`fr-ContourSettings B-4` (= `C-1`, ∘ `C-25` / `R6-8` fold) — home: F-W5 §2 clause E13; bytes: `fr-ContourSettings.md:43`** ⟨**re-keyed to the banked head at repair round 5 (REST-40; P5-15 / E5-14).** `C-25` is an **ALIAS**, never a banked head, and a lock led on an alias is a lock that survives no re-keying of its record. ⊘ **The canonical's own attachment is now cured in this file's favour**: errata **E5-14** (2026-08-29) moved `C-25`/`R6-8` off `i-7` (`:104`) and back onto `B-4` (`:43`), the **first** banked head that claims them inside its own head — the errata's words are *"`F-W7.md`'s own keying already followed the record."* **The spec was right and the operand was not; it is said once, here.**⟩ — *"an operation's cache identity must be a superset of the request fields the operation consumes"* · ▲ **M-12 ⟨NEW⟩** — stamp from a snapshot at the read instant; record a result, not a schedule · ▲ **SS-C-2 (rides inside F.W5 §2 E8)** — do not size a saving over dead fields · ▲ **m-15** — cross-referenced with F-4, **never merged** · ▲ **FR-GIG-5 / F-W5 §0b** — no credit for an unauthored cure · ▲ **BC-20** — the `duration = ref(20000)` zero-writers fold stays banked; never re-booked · ▲ **D-19** — no anchor cited as live pre-F.W0.

### 7c. Cross-edges (**17 rows, counted**: 15 declared edges + the F.W1 sequencing-lock row + the F.W9/W10 row; declared from this end; reciprocal requested where marked)

| edge | direction | this end's declaration | reciprocal |
|---|---|---|---|
| **F.W0** substrate pre-gates | **DEPENDS — HARD** | No line anchor, no HEAD figure, no "LIVE at certified HEAD" citation until F.W0 re-grounds: 28 dirty entries, pin `14d83356` unresolvable, corrupt glass-ui 4.0.0 `dist/styles/index.css` — **`FR-NP-32` (≡ `fr-PaperSidebar M1`)** — GAB-13, F8-REACH-01/02. R-4's own anchors are pre-F.W0 coordinates. **The re-grounding this wave quotes is F.W0's published one, owned once: F-W0 §4's gate G-11** (⟨cmd⟩ `grep -oE '^### G-1[12] — .*' waves/F-W0.md`, the headings whole: *"### G-11 — ONE corrected anchor table published; every later wave quotes it"* and *"### G-12 — ONE corrected-denominator table published; superseded figures FORBIDDEN downstream"*) — **landing in `fourier/docs/tranches/F/SUBSTRATE-LEDGER.md`, F-W0 §2a's create row, quoted WHOLE at round 3 so no count word stands between the reader and the bytes** (⟨cmd⟩ `grep -o "single durable artefact.*OPTIONs" waves/F-W0.md`): *"single durable artefact: the 28-path minute · the STEP-2i divergence minute · the G-11 anchor table · the G-12 denominator table · the G-13 pin table + lattice · the stale-dist admissibility rule · the Codex dispositions · the four rulings (§4 G-15) · the recorded OPTIONs"*. ▲ **ERRATUM (E-3), F-W7-CHECK D-3**: through pass 2 this cell elided that tail and disclosed the elision as *"the row's **five** further artefacts"* — **four**, and a wave that republishes its own denominator as a probe rather than a numeral had published this one as a numeral, wrong by one, about a sibling's live bytes. **The elision is retired rather than corrected: the whole row is cheaper than an accurate count of it.** **F.W7 QUOTES that table; it never re-performs the re-resolution as its own act, and any divergence from it is a defect against G-11, not a rival measurement.** **If F.W0 fails to re-ground, F.W7 HALTS** | **DECLARED — `F-W0.md` §6b, the `F.W7` row; the ask this cell carried through pass 2 is DISCHARGED at the far end and the cell is re-written to say so ⟨CC⟩.** Membership, not a count of a file under concurrent repair (R3-3.10): ⟨cmd⟩ `grep -o '\*\*F\.W7\*\* (the owner-gated trie design)' waves/F-W0.md` → *"**F.W7** (the owner-gated trie design)"* — the §6b row exists and is headed as this file quotes it. Its bytes reciprocate in three clauses: *"**F.W7 DEPENDS ON F.W0, HARD, and F.W0 depends on nothing of F.W7's.** … (1) **What F.W7 consumes**: F.W0's published re-grounding — **§4 G-11's corrected anchor table** and **§4 G-12's corrected-denominator table** … F.W7 **QUOTES** them; it never re-performs the re-resolution as its own act … **F.W0 acknowledges that halt condition from this end: an honest-RED G-11 close (§7a) halts F.W7; it does not license F.W7 to proceed on stale bytes.** (2) **What crosses back: NOTHING.** F.W0 books **no** F.W7 registry row, mints none into F.W7's empty column, and takes no position on the trie question … (3) **Sequencing**: F.W7 opens after F.W0's substrate pre-gates"* (ellipses mark elision, never addition). **The edge is declared at both ends. Nothing is owed here, and this wave still writes no F-W0 byte** ▲ *Two reciprocal-integrity facts recorded, neither chased into a sibling's bytes.* **(a)** F-W0's row **already struck**, at its own hand, a sentence it had attributed to this file's G-F7-11 (*"Genuinely owed, each for its true reason … → 0"*). **The substance stands and its RECEIPT is corrected at round 3**: G-F7-11 never authored that sentence — **no gate, witness, close cell or claim of this spec has ever contained it**, and every occurrence of the phrase inside this file is *this erratum quoting F-W0's strike back at it*. F-W0's own receipt for the strike (a `grep -c` over `F-W7.md` returning 0) was therefore **self-invalidating the moment this cell landed**: quoting a strike puts the struck words in the quoting file. **The honest form is the classification, not the tally** — and it is why R3-3.10 now forbids banking a count of a live sibling or of the citing file at all. Correcting F-W0's copy is F.W0's seat's work (R3-3.1), routed, never chased: **this cell cites F-W0 §6b's row by label and takes no count of F-W0's bytes as a gate operand** (see G-F7-11's re-basing). **(b)** The two cells F-W0 *does* quote from here — **§7c's F.W0 declaration** and **§6 gate G-F7-7's failure branch** — are re-read at round 3 with **one disclosed change and one none**: G-F7-7's failure branch is **byte-identical**, untouched by this repair; **this declaration cell is NOT** — R3-8.1 requires the corrupt-dist gate in its canonical form at this exact site, so the bare `(M1)` F-W0 quotes back is now **`FR-NP-32` (≡ `fr-PaperSidebar M1`)**. ▲ **DRIFT EXPORTED, DECLARED HERE, ROUTED TO F.W0 — and the choice is minuted rather than hidden**: a standing canonical form is not suspended because a sibling quoted the uncanonical bytes first; the alternative would let one sibling's quotation freeze another spec's ids. **F.W7 writes no F-W0 byte**; F.W0's seat re-pastes its quotation at its own hand, and this row is the notice it needs to |
| **F.W5 §2 E16 / §3 G7** | **SAME IDENTITY — cite, never re-book** | R-4 ≡ E16 ≡ G7 is one banked identity; **F.W5 already carries the honest no-trie default at §2 clause E16 and records G7 UNRULED at §3**. The no-design-byte prohibition binding this wave is **F.W7's own (§3), grounded on those two clause/gate ids** — F.W5 has no Prohibitions section. F.W7 exists only downstream of the ruling; **the ruling stays INLINE in F-W7.md per P-10**, cross-referenced to G7 — **no second ruling file** | **DECLARED — `F-W5.md` §4, the `F.W5 → F.W7` row** (⟨cmd⟩ `grep -o '\*\*F.W7 CITES; F.W5 STATES.\*\*' waves/F-W5.md` → *"**F.W7 CITES; F.W5 STATES.**"*, the quoted span's own head ▲ *round-4 cure, `PASS-4/F-W7-CHECK.md` D-12: the pass-3 locator `grep -n 'F\.W5 → F\.W7'` returns **two** lines — the E16 clause row that names the edge, and the §4 row actually quoted — so a receipt offered for one quotation pointed at two places. Harmless as a locator and byte-exact in what it published, but it is the same shape charged at D-8 where it carried two quotes; an exact-string probe of the span certifies the span*), verbatim: *"**F.W7 CITES; F.W5 STATES.** The trie question is this wave's clause **E16** and gate **G7 ⊙** — F.W7 owns the owner-gated design that follows a ruling and **books no registry row of its own** (its routing census is a measured **∅**, R-11)"*, closing *"Declared from this end so the keystone's consumer is not undeclared. Reciprocal requested: `F-W7.md`"* — **this row is that reciprocal's other half, and the edge is now declared at both ends.** ▲ **The prior cell's "owed at the spine … NO edge to F.W7" is STRUCK as refuted at the bytes (see G-F7-11's erratum); the spine wrote the row seven seconds before this file first claimed it absent** |
| **F.W5 §2 clause E1** (compound per-entity `_id`) | **DEPENDS** | The version identity a sharing key would fold is under repair. E1's clause precedes any key design (G-F7-4). **F.W7 books zero of E1's cure** | **covered by the spine's declared row** (above) |
| **F.W5 §2 clause E3 ⊙ / §3 gate G4 ⊙** (TA-4) | **DEPENDS — OWNER-GATED** | The value-side atomdiff restoration is SS-4's named prerequisite **or** the scope is explicitly re-cut. Until G4 rules, "proven on both object kinds" is unprovable (G-F7-3). Restoration is value-side work riding the value.js API row — **never re-booked as a fourier defect** | **covered by the spine's declared row** |
| **F.W5 §2 clause E10** | **DEPENDS** | Field-retirement disposition precedes compression sizing (G-F7-6) — a saving over retirable fields is unfalsifiable. SS-C-2 proves atom 4 is semantically empty. **B4 is a distinct liveness predicate — E10's own closing separation, *"Distinct from B4's liveness predicate"* — cited, never conflated with E10** | **covered by the spine's declared row** |
| **F.W5 §2 clause E13** (banked as C-25) | **CLAUSE INHERITANCE** | *"an operation's cache identity must be a SUPERSET of the **request** fields the operation consumes"* binds any F.W7 key verbatim (G-F7-5). F.W7 authors no cure at **E13**'s seam. *(F.W5's **E12** is the debounced-mirror/save-race clause and is NOT this edge — anti-rename, verified by clause id at §5c.)* | **covered by the spine's declared row** |
| **F.W5 §2 clauses E5 / E7 / E8 / E17 and §2's `§C` clause C2** | **CITED-NOT-BOOKED** | B-2 ⊕ FR-GV-1 (**E5**) · SS-C-1 write leg (**E7**) · SS-C-2 riding inside (**E8**) · C-2 ⊕ K-3 (**E17**) · m-15 (**§C2**). **Credit claimed for none** — verified by G-F7-8's two-direction set-difference over the re-keyed roster | no |
| **F.W6** provenance burn-down | **CONTRIBUTES A WITNESS; books nothing** | Of the five inherited chain-integrity defects (F-α, F-β, V-α, V-β, V-γ), **V-β is F.W7's shipped accidental-sharing witness**. F.W7 authors no burn-down | **DECLARED — F-W6 §4, the `F.W7` cross-edge row**, pasted from **three exact-string probes, each printing its own span, together covering the whole row with no interval bound anywhere** ▲ *(round-4 re-cut — R4-2.2 / `PASS-4/F-W7-CHECK.md` D-1: the pass-3 receipt here was the same unexecutable `.\{0,262\}` command as G-F7-11's — **one defect published at two sites, both labelled as run output**. On the pinned BSD `grep` it errors and prints nothing; the full disposition is at G-F7-11's erratum)*: ⟨cmd⟩ `grep -o 'F.W6 carries the documented default — \*\*no trie; whole-snapshot duplication is the recorded shipped behaviour.\*\*' waves/F-W6.md` → *"F.W6 carries the documented default — **no trie; whole-snapshot duplication is the recorded shipped behaviour.**"* · ⟨cmd⟩ `grep -o '.trace. (M-β4) and .reconstructed_points. (L·m-6) strikes wait on the ruling.' waves/F-W6.md` → *"`trace` (M-β4) and `reconstructed_points` (L·m-6) strikes wait on the ruling."* · ⟨cmd⟩ `grep -o 'The standing anti-tree KISS guardrail (.atomdiff.py:12-14., quoted both trees) binds until the owner moves it' waves/F-W6.md` → *"The standing anti-tree KISS guardrail (`atomdiff.py:12-14`, quoted both trees) binds until the owner moves it"* — **and the third span now runs to its true end, `moves it`, where the dead receipt's imaginary budget had cut it at `moves`**, with the twin exclusion row in **F-W6 §5**, quoted as two spans with two commands (⟨cmd⟩ `grep -o '\*\*F.W7.s compression design\*\* — .trace. and .reconstructed_points. strikes' waves/F-W6.md` · ⟨cmd⟩ `grep -o 'the documented default (no trie, whole-snapshot duplication) is the incumbent until the owner moves it' waves/F-W6.md`): *"**F.W7's compression design** — `trace` and `reconstructed_points` strikes"* … *"the documented default (no trie, whole-snapshot duplication) is the incumbent until the owner moves it"*. **Rider routed there and requested, not written** ⟨CC⟩: this row's *"quoted both trees"* is the SECOND propagation of the bilaterality premise N-1 measures FALSE at the value tree — the pass-2 probe found only F.W8's spelling. **`F-W6.md` is read-only to this wave** |
| **F.W8** CRUD union prototype | **FEEDS — conditionally** | F.W8 is the only wave that could exercise a sharing design across both kinds (create → derive → diff → remix → history walk). **FOR ⇒ F.W7's design lands as F.W8 input; AGAINST ⇒ F.W8 proceeds on whole-snapshot duplication unchanged and inherits the kill rationale** | **DECLARED — F-W8 §5c, the `F.W7` sibling row**, pasted from the command that prints the head (⟨cmd⟩ `grep -o 'F.W8 designs no trie.\{0,110\}' waves/F-W8.md`; the budget's cut is disclosed mid-word, never tidied): *"F.W8 designs no trie. If G7 **admits** it, the derive leg must exercise structural sharing; if G7 **sustains the incumbent KISS gu"*. Rider routed there and **requested, not written**: that same row quotes the guardrail correctly and then appends the bilaterality claim, ⟨cmd⟩ `grep -o 'there is no three-way / DAG / merge.\{0,30\}' waves/F-W8.md` → *"there is no three-way / DAG / merge"* — carried on BOTH sides), t"* (the output's trailing fragment is the budget's cut, disclosed) — **and *"carried on BOTH sides"* is what N-1 measures FALSE at the value tree** |
| **SS-4** union synthesis | **IS a member wave** | SS-4 **FLAGS the trie-vs-KISS ruling INLINE, never presumes it**, and **names the TA-4 restoration a PREREQUISITE or re-scopes explicitly**. Neither side's edits gate the other's waves | **owed** |
| **F.W1** atomic uplift *(sequencing-lock row, not a carried edge)* | **NO GATING either direction** | ▲ F.W1 is ONE atomic land-or-lose transaction — **the TWELVE-limb roster chartered at F-W1 §4 step 4, cited whole and never restated here.** Re-quoted from the corrected charter after the F-W1 edit landed (R2-5) — at round 3 as **TWO spans with TWO commands, at F-W1's own emphasis**: ⟨cmd⟩ `grep -o 'The roster is TWELVE limbs and stays twelve — the eleven chartered plus the FR-EQC-7 vaul-vue manifest gate returned INSIDE at repair round 1' waves/F-W1.md` → *"The roster is TWELVE limbs and stays twelve — the eleven chartered plus the FR-EQC-7 vaul-vue manifest gate returned INSIDE at repair round 1"* · ⟨cmd⟩ `grep -o 'it is the extent every sibling spec cites (R-4b); nothing is added to it by a later reading' waves/F-W1.md` → *"it is the extent every sibling spec cites (R-4b); nothing is added to it by a later reading"*. **What sits between them, and why it is not quoted**: F-W1's own `:294` pin — a line address into a live sibling, which R2-2 forbids this file to republish. *(Pass 2 bolded `**TWELVE**` where F-W1 bolds the whole sentence and left the join as a bare ellipsis; the emphasis is struck and the omission now carries its reason. The count word is the charter's own, quoted, never this file's re-derivation.)* ▲ **This spec's `ELEVEN-limb` CITE is retired with it; F.W7 quotes the charter and never re-derives the count.** The token survives here only inside this retirement note and §0's minute of it — self-disclosing strike-note mentions, which R3-10 row 5 verified across the lane as lawful (a mention that announces its own retirement is not a citation). It is untouched by this wave, and **F.W1 must NOT be credited for the pagination-drain cure (FR-GIG-5)**. F.W1 precedes F.W3/W4 by §2 law — also not F.W7's business. *(The earlier three-limb restatement is struck: a partial roster of a land-or-lose transaction normalises a partial landing.)* | no |
| **F.W9 / F.W10** deploy spine and terminal verification | **EMITS a terminal disposition; books nothing** | **Declared from this end** so the edge is no longer silent at both ends. G-F7-9's XOR outcome — exactly one of {full spec, terminal kill with rationale} for census wave-sketch 8 — is precisely the object of **F-W10 §2.3** (⟨cmd⟩ `grep -oE '^### §2.3 .*' waves/F-W10.md`, the heading whole: *"### §2.3 Substrate + terminal-disposition verification (F.W0 RULES; F.W10 VERIFIES, never re-rules)"*). **F.W10 verifies that the disposition landed; it does not re-rule it, and F.W7 claims no F.W9/W10 GREEN.** No fixture, probe or deploy obligation crosses this edge | **OWED — and the ask is stated as a ROW, never as a count (R3-3.10 now makes that mandatory, not stylistic).** F-W10's only mention of this wave sits in **§2.8's E-16 erratum minute** — a record of *this file's* former drifted anchors into F-W10, whose sentence is ⟨cmd⟩ `grep -o 'A repair seat that only fixes its outbound citations exports drift it never minutes' waves/F-W10.md` → *"A repair seat that only fixes its outbound citations exports drift it never minutes"* — **not** a cross-edge row. *(Pass 2 published that fact as `grep -c → 1`; the classification is what the gate turns on, and it survives F-W10's next edit where the numeral would not.)* **What is requested is one `F.W7` row in F-W10 §4b's cross-edge register**, declaring that F.W10 verifies the §2.3 terminal disposition and books nothing of F.W7's. Requested from this end; never written here ▲ *The classification is context, never the gate's operand — G-F7-11's re-basing was exactly the refusal to let a `grep -c` against a moving file stand in for a named row, and round 3 retires the form itself* |
| **SS-6** glass BH communique | **NEGATIVE DECLARATION** | F.W7's surface carries **ZERO** glass-producer rows. Declared so no producer row is smuggled in as a frontend hack (standing owner edict). **Nothing here accretes to COHESION §4a**; any producer-shaped finding leaves as a letter | no |
| **X·P (parse-that)** | **NEGATIVE — FORBIDDEN EDGE** | Direct parse-that→fourier routing is forbidden (COHESION §2, PLAW-BIND). **Zero parser edges in either direction** | no |
| **X·V API row** (value.js) | **EMITS obligations, never work** | Every value-side limb (TA-4 restoration, E1's compound `_id`) is emitted as a **value-side row** and is never re-booked as a fourier defect. **F.W7 writes no value product byte** | **owed** |
| **P-10 packet** (the other four limbs) | **NEGATIVE DECLARATION** | F.W7 absorbs **ONLY** the anti-tree KISS guardrail limb. The 7.0.0-vs-8.0.0 re-pin target and PaperSidebar's escalation + the M3 pre-gate law home at **F.W1**; the glass 4→8 deadlock vs the landable-at-old-pin ToggleChip route and the default-COLLAPSED ContourSettings IA choice home at **F.W3** (corroborated: **F-W3 §4's S-6 row** flags (a)/(b)/(c) and homes the re-pin at F.W1 — ⟨cmd⟩ `grep -n 'S-6 · Owner-gated flags' waves/F-W3.md`, verbatim tail *"(c) P-10, the 7.0.0-vs-8.0.0 re-pin target (homed at F.W1)"*; **F-W3 §X** carries the declared-not-carried row). None of the packet's five slugs (FrequencyGraph, EasingPicker, ContourSettings, PaperSidebar, CanvasOverlayButton) carries an F.W7 row — see §8 for the two probe hits and their disposition | no |
| **SS-13** live-probe lane | **EXPORTS ZERO** | Docs-and-design wave, no live-probe residue of its own. The dedup-family live questions it cites (roster 12's EXIF-transpose dissent trigger; fr-CanvasControlsDock UNPROVEN #6 — whether `POST /api/visualizations` dedupes on `(image_slug, contour_hash)`) **already ride their home records to SS-13; F.W7 re-books neither** | no |

---

## 8. Excluded — carried nowhere else, with reason

Nothing below is a silent drop; each is named so a later reader cannot mistake absence for oversight. **Zero §5 rows are excluded** — this table routes non-rows, and its first entry is the one adjudicated registry row that touches this wave's subject while belonging to another wave.

| excluded | reason |
|---|---|
| **`fr-ContourEditorCanvas` L-5 (MAJOR, `:56`)** — *the corpus's ONE true hit for R-4's six-term probe* | **Routed elsewhere, NOT F.W7's to book.** Banked at the frozen record, where a line cite is permitted (R2-2.2(i)) — ⟨cmd⟩ `sed -n '56p' fr-ContourEditorCanvas.md`, the elision below marking the record's census-contradiction sentence and never an addition: *"Unbounded deep-reactive undo stack over a 1024-point default (all 36 lines read: no cap, slice+push, deep copies at 4 sites; `points` is `ref` not `shallowRef`; 4096 reachable) … **ADJUDICATED → F.W3/W4** (cap + structural sharing candidate)"*. ▲ **Round 3 — the dual routing is RESOLVED by F.W3's §X.1-v4, consumed here, contested nowhere**: the criterion (⟨cmd⟩ `grep -o 'dual .F.W3/W4. rows on ..a.–..e. files home at \*\*F.W3\*\*; all others take the default arm to \*\*F.W4\*\*' waves/F-W3.md`) *"dual `F.W3/W4` rows on `.a`–`.e` files home at **F.W3**; all others take the default arm to **F.W4**"*, and `ContourEditorCanvas.vue` is **one of the partition's five criterion negatives** — ⟨cmd⟩ `/usr/bin/grep -c 'criterion negative → F.W4' CENSUS-CANONICAL.md` → **5**; `… | /usr/bin/grep -oE 'fr-[A-Za-z]+'` → `fr-BasisCanvas` · `fr-ContourEditorCanvas` · `fr-EquationResult` · `fr-FourierMorphSvg` · `fr-GalleryAdminBanner` ⟨**re-cut at repair round 5, REST-35; P5-8.** The cell read *"four"*. **Two defects in one**: a count word against the census of record, **and a retirement announced and not executed** — §12 of this same file, same round, declares *"the pass-3 count word 'four negatives' is **retired for membership**"*, and the word stood one section away⟩ (⟨cmd⟩ `grep -o 'No §1 bounds row and no §5 file list holds .BasisCanvas.vue., .ContourEditorCanvas.vue., .EquationResult.vue. or .GalleryAdminBanner.vue.' waves/F-W3.md`) — so L-5 takes the **default arm: F.W4 BOOKS, F.W3 CITES** (⟨cmd⟩ `grep -o '\*\*F.W3 CITES, F.W4 BOOKS.\*\*' waves/F-W3.md`). ▲ **AND AT ROUND 4 THE HOME IS THE CENSUS OF RECORD'S, BY ENUMERATION, NOT THIS FILE'S DERIVATION FROM A CRITERION (R4-3 / R4-10; `PASS-4/F-W7-CHECK.md` D-11).** The canonical's §2 roster homes it outright — ⟨cmd⟩ `grep -o '^### F\.W4 — \*\*1014 rows\*\*' conformance/CENSUS-CANONICAL.md` → *"### F.W4 — **1014 rows**"*; ⟨cmd⟩ `awk '/^### F\.W4 —/,/^### F\.W5 —/' conformance/CENSUS-CANONICAL.md \| grep '^- \*\*fr-ContourEditorCanvas\*\*' \| grep -o '\*\*fr-ContourEditorCanvas\*\* (40)'` → *"**fr-ContourEditorCanvas** (40)"*, and `L-5` is a member of that record's F.W4 id list — ⟨cmd⟩ `awk '/^### F\.W4 —/,/^### F\.W5 —/' conformance/CENSUS-CANONICAL.md \| grep '^- \*\*fr-ContourEditorCanvas\*\*' \| grep -o '· .L-5. ·'` → ``· `L-5` · ``. **The criterion derivation and the census of record agree**, which is the outcome a criterion is for. *(Erratum, E-3: pass 3 hardened the derivation into the assertion "F.W4 **BOOKS**" as though it were a landed fact, and it was not — `grep -n 'ContourEditorCanvas' waves/F-W4.md \| grep -c 'L-5'` → **0**, and the same probe against `F-W3.md` → **0**. Nothing was lost — F.W7 books zero of it on either arm — but **§8 is the table whose whole function is that absence cannot be mistaken for oversight, and it was pointing a reader at an empty home.** The canonical roster is the home; the per-id landing at the twin's own file is the twin's obligation, routed there and not asserted here.)* **F.W7 books zero of it either way**; the resolution is cited so a later reader is not left with an unresolved slash in the one row that touches this wave's subject. It names **structural sharing** as an adjudicated cure candidate over a flat coordinate bag — F.W7's exact subject — but its **terminal disposition is F.W3/W4's**, so booking it here would violate the one-home law and manufacture a row for a wave the registry does not route to. **Named so a later reader cannot mistake absence for oversight**, and so §12's "no material on either tree" framing is read as qualified: the registry already holds a structural-sharing cure *candidate*. F.W7 books ZERO of it and takes no credit ⟨CC⟩ |
| **The trie/structural-sharing design itself, and `design/R4-variant-storage.md`** | **⊙ G-F7-1 unruled.** The file path is declared CONDITIONAL in §2a and is **never created** if the ruling goes against. Prohibited until then by **§3's own prohibition** (grounded on **F-W5 §2 clause E16 ⊙** + **F-W5 §3 gate G7 ⊙**, recorded UNRULED) |
| **The value-side atomdiff restoration (TA-4) as work** | **⊙ G-F7-3 / F.W5 G4 unruled.** F.W7 states the prerequisite; the restoration is the **value.js API row's act**, never a fourier defect |
| **Every cure at E1 · E3 · E5 · E7 · E8 · E10 · E13 · E17** | **Homes are F-W5 §2's `§E` clause register, one clause each, cited by clause id** (E1 · E3 · E5 · E7 · E8 · E10 · E13 · E17 — all eight resolving, ⟨cmd⟩ at G-F7-8). Cited, not booked; G-F7-8 verifies both directions over this roster **by id, never by line window** |
| **ImageUpload roster 12 / 23 cures; m-15's cure** | Homes are **F.W3** (roster 12 = `L:L-M3`, roster 23 = `R2-missed-3`, both file-criterion §5.d; `K-8` **TERMINAL (∅)**) and the **`F.W5-W8` band** (m-15), whose cure clause is **F-W5 §C2**. F.W7 carries only the cache-invalidation obligation they prove. ⌧ **Re-based at repair round 5 (REST-32 / REST-37).** Nothing books either way — F.W7's roster is zero and the wave takes no credit — **so no count moves. What moves is where a reader is sent, in the table whose whole declared function is that absence cannot be mistaken for oversight.** Sharpest form: **§8 is the same table in which round 4's D-11 re-based `L-5` onto the canonical — the cure was applied to the row above and not to the row below it.** |
| **M-12's one-line snapshot fix; B-4's server-key cure; m-18's invented `× 0.6` coupling** | **The twin surface's** — and, since round 3, **named**: §X.1-v4's file criterion homes `fr-ContourSettings`'s dual rows at **F.W3 `.d`** (its Files list carries `ContourSettings`; F.W4 cites), so M-12's snapshot fix and B-4's client-side regression fixture are F.W3's, while B-4's contract half stays **F-W5 §2 clause E13's** — cited by clause id, never by line — the pass-2 numeric cite here addressed a *different* clause within the day, and its numerals are deliberately not reproduced, since restating an abolished address inside its own erratum re-commits the defect. F.W7 takes M-12's **clause** (§5c N-3) and books nothing |
| **K-3's upsert arm** (*"a different point_count can come back"* — the record's own unformatted spelling, ⟨cmd⟩ `grep -o 'C-8 upsert arm ("a different point_count can come back") — KILLED' fr-ContourEditorCanvas.md`) | **KILLED by both readers** against a verbatim read of `image_storage.py:269-283`. Must not resurface as design rationale |
| **P-10's other four limbs** | Not F.W7's — and each quotation below is now sourced by command, at its own home, rather than paraphrased from the packet. The **7.0.0-vs-8.0.0 re-pin target** → **F.W1** (⟨cmd⟩ `grep -n 'THE 7.0.0-vs-8.0.0 RE-PIN TARGET' waves/F-W3.md` → **F-W3 §C.K's P-10 row**, verbatim: *"retargeting to 8.0.0 RESURRECTS the `text-admin-label` break"* and *"It is an owner/formation decision, not a measurement."*) · **PaperSidebar's escalation + the M3 pre-gate law** → **F.W1** · **the glass 4→8 deadlock vs the landable-at-old-pin ToggleChip route** → **F.W3**, banked in the frozen corpus (⟨cmd⟩ `grep -n -F 'that trade-off is the ruling the forming F.W3 spec actually owes' fr-EasingPicker.md` → `:70`, MISSED-D): *"…BUT the line re-enters the tri-package deadlock (glass 4→8) whereas DU's ToggleChip route was landable at the old pin — that trade-off is the ruling the forming F.W3 spec actually owes"* · **the default-COLLAPSED ContourSettings IA choice** → **F.W3**, likewise banked (⟨cmd⟩ `grep -n -F 'should not silently flip it without the ruling' fr-ContourSettings.md` → `:100`, i-3): *"An IA choice worth a deliberate ruling. → **NO-WAVE-OWNER** (SS-3/SS-4 spec authoring; the F.W3/W4 pass should not silently flip it without the ruling)."* ▲ *Pass 2 rendered three of these four as bare fragments with no ⟨cmd⟩ and no home; two of the three are corpus bytes, where a line cite is permitted, and were reachable in one probe each.* |
| **The five P-10 slugs** (FrequencyGraph, EasingPicker, ContourSettings, PaperSidebar, CanvasOverlayButton) as F.W7 rows | **None carries one** — consistent with §5a's `F-MAIL-∅`: `routedTotal = 0`. Verified this seat: `fr-CanvasOverlayButton.md:64` is a table header *"claim (adjudicated, compressed)"* — a true false positive. **`fr-ContourSettings.md:126` is a lexical false positive (*asymme​trie​s*) but a substantive HIT** and is therefore **carried, not excluded** (§5c N-3) ⟨CC⟩ |
| **R-4's six-term grep as founding evidence** | **Retired at G-F7-2.** Simultaneously noisy (`trie` ⊂ `entries`/`asymmetries`/`geometries`/`RETRIES` — **147 lines returned over the 66 records, 146 of them substring artefacts**) and blind (invisible to sha256 content addressing). **The 147th is real** — `fr-ContourEditorCanvas.md:56` L-5, excluded-with-reason in the row above. Its *conclusion* for **version-atom sharing** survives; its *scope* does not, and no universal "every one an artefact" claim is made anywhere in this file |
| **Any claim that the guardrail is void** | **N-1 corrects its bilaterality, not its standing.** `atomdiff.py:12-14` remains **the incumbent**, banked at **F-W10 §2.3, the SS-4-PREREQ row** — *"THE GUARDRAIL IS THE INCUMBENT; an owner ruling precedes design"* (⟨cmd⟩ at §5a's R-4 row) — with **F-W5 §2 clause E16 ▲** as its clause-side home, which states the same in F.W5's own voice and, this round, adopted the bilaterality correction itself (*"the guardrail is the INCUMBENT, but it is NOT carried on both sides today"*). The honest default remains **F-W5 §2 clause E16's**. ▲ *Both of this row's pass-2 addresses were line cites into live siblings and both had moved; the sentence one of them carried had additionally been struck at its own end. Addresses abolished, quotations re-sourced (§5a).* |
| **A second owner-rulings file** | **P-10 routes the ruling INLINE.** `contract/OWNER-RULINGS-F.W5.md` is **F.W5's to create (§1a)**; F.W7 cites it when it exists and **never extends it**. Until then the obligation is prospective, not a live citation |
| **Any glass-ui producer row** | SS-6 relay declared **EMPTY**; producer-shaped findings leave as letters, never as frontend hacks |
| **Any parse-that edge** | **Forbidden routing** (COHESION §2, PLAW-BIND) |
| **`docs/tranches/X/COHESION.md` edits; edits to the stale N-1 bilaterality premise at BOTH its consumers — `F-W8.md` §5c's `F.W7` sibling row (*"carried on BOTH sides"*) AND `F-W6.md` §4's `F.W7` cross-edge row (*"quoted both trees"*)** | Root-authored / sibling-owned at the boundary; editing any of them would be a hidden cross-wave write conflict. §7c declares and **requests** at each end. ▲ **Round-3 cure (F-W7-CHECK D-11): through pass 2 this row named ONE consumer while §5c N-1 and §12 both measured the premise stale at TWO, in two spellings — an under-naming in the one table whose whole function is that absence cannot be mistaken for oversight.** The routing requests were correctly declared at §7c throughout, so nothing was lost; what was missing was the exclusion's own completeness, which is the property this table exists to have |
| **An INBOX row** | E13 is append-only **iff a relay is sent**. F.W7 sends none and expects **zero rows**; the declaration is the discharge |
| **All product source, both repos; all fourier bytes** | Execution gate + read-only law |

---

## 9. Agent units (3, serial)

### X.F.W7.a — The enumeration census

- **Goal**: R-4's absence claim rests on an enumerated persistence surface, not a term grep.
- **Mechanism**: enumerate both trees in four classes (version writers · asset writers · cache keys · hash folds); each row: `tree · site · keyed-on · scope · share-hit consequence`. Read-only measurement — **authorable while G-F7-1 is RED**.
- **Files**: `design/R4-enumeration-census.md`.
- **Sub-gate**: **G-F7-2** closes; **G-F7-10** closes (the bilaterality correction is stated in the census).

### X.F.W7.b — The ruling record and its branch

- **Goal**: exactly one of {design, terminal kill} exists, downstream of the owner's word.
- **Mechanism**: author §4 against the corrected evidence; on **FOR**, author `design/R4-variant-storage.md` with the four key locks discharged and the object-kind scope explicit; on **AGAINST**, author the terminal kill with rationale **in situ**. **Cannot open until the owner rules.**
- **Files**: `waves/F-W7.md`; conditionally `design/R4-variant-storage.md`.
- **Sub-gate**: **G-F7-1** closes either way; **G-F7-3/4/5/6** close for F.W7 — **G-F7-5 VACUOUSLY on the AGAINST branch** (REST-39: no key spec is created on AGAINST, so no consumed-field set is named and no superset relation is owed; the kill rationale records that fact and the gate closes on it); **G-F7-9** closes with exactly one branch.

### X.F.W7.c — Carry closure (fresh Fable adjudicator)

- **Goal**: zero re-bookings and zero silent drops, proven by artefact.
- **Mechanism**: run the id set-difference against **`F-W5.md` §2's live clause register** in both directions over the re-keyed roster (**E1 · E3 · E5 · E7 · E8 · E10 · E13 · E17**); confirm zero bookings and full citation; declare the §7c reciprocal requests (**F.W0 and the F.W5 spine** — F.W6's and F.W8's are already declared from their ends).
- **Files**: `waves/F-W7/carry-closure.md`.
- **Sub-gate**: **G-F7-8 ∅ both directions**; **G-F7-11** closed for F.W7 (this end declared; owed reciprocals named).

## 10. Cadence · artefacts · commits

Docs-only. After each unit: `git diff --check`; markdown link check over the new `design/` paths; the G-F7-8 set-difference re-run. **`npm run typecheck` / `test` / `lint` are NOT this wave's gates** — no source byte moves, so a delta in any of them means the wave escaped its bounds and is itself the failure signal (run once at close as that tripwire only).

Committed at close (L-7 — an uncommitted witness does not exist): `design/R4-enumeration-census.md`; `waves/F-W7.md` incl. the §4 ruling record and, on the AGAINST branch, the kill rationale; `design/R4-variant-storage.md` **iff** FOR; `waves/F-W7/carry-closure.md`; the F.W0 re-grounding receipt this wave's anchors were re-resolved against (cited, not copied). Pathspec commits only; `scripts/dev/dev.sh` never staged.

| # | scope | contents |
|---|---|---|
| 1 | `docs(X·F.W7): the R-4 persistence-surface enumeration census` | unit a |
| 2 | `docs(X·F.W7): the trie-vs-KISS ruling record + branch` | unit b |
| 3 | `docs(X·F.W7): carry closure — set-difference against F.W5` | unit c |

## 11. Dependencies

**Every dependency below is addressed by gate id, clause id or row label — no line number into a live sibling appears in this section, and none may be re-introduced (R2-2.2).**

- **Depends on**: **F.W0** (hard, G-F7-7) — specifically **F-W0 §4's gate G-11, the corrected anchor table**, and **§4's gate G-12, the corrected-denominator table**, landing in `fourier/docs/tranches/F/SUBSTRATE-LEDGER.md` per **F-W0 §2a's create row**; those two tables are what this wave quotes and never re-performs. F-W0's own §4 masthead binds the idiom from the publisher's end (⟨cmd⟩ `sed -n '/^## 4\. Gates/,/^### G-1 /p' waves/F-W0.md`, verbatim): *"These gates' citable identities are their **gate ids** … **A line cite into this file is FORBIDDEN downstream from this amendment forward**, and is a defect at authoring even if it resolves on the day it is written."* · **F.W5** — **§2 clause E16 ⊙ ⊕ §3 gate G7 ⊙** (one identity), **§2 clause E1**, **§2 clause E3 ⊙ ⊕ §3 gate G4 ⊙**, **§2 clause E10**, **§2 clause E13** — all six resolving today by id — stated as set-membership over pipe-free probes, never as a count of a live sibling (R3-3.10 / R3-5.4): ⟨cmd⟩ `grep -ohE '(^|[^A-Za-z0-9])(E1|E3|E10|E13|E16)([^0-9A-Za-z]|$)' waves/F-W5.md | sed -E 's/[^A-Za-z0-9]//g' | sort -u` → `E1` · `E10` · `E13` · `E16` · `E3`; ⟨cmd⟩ `grep -ohE '(^|[^A-Za-z0-9])(G4|G7)([^0-9A-Za-z]|$)' waves/F-W5.md | sed -E 's/[^A-Za-z0-9]//g' | sort -u` → `G4` · `G7` ▲ *(round-4 re-cut, R4-9.7 / D-6's class: both probes were **bold-delimiter anchored** and so could not see `clause E13`, `E16 ⊙` or any of the other spellings G-F7-8's axis (ii) declares. Token-bounded and delimiter-free, the membership is unchanged — which is why it is a cure of the operand and not of the answer.)* · **the owner's ruling on G-F7-1**, and G4's on TA-4.
- **Blocks**: **F.W8** (conditionally — the design is its input, or the kill rationale is; **F-W8 §5c's `F.W7` sibling row** already declares the sibling edge from its end, quoted at §7c) · **SS-4** (which flags this ruling inline and cannot synthesize an unflagged union).

## 12. Archaeology

- **Prior attempt**: none. R-4 is the one census requirement with **no material on either tree for version-atom sharing** — but "greenfield" was overstated **twice**. (i) Content-addressed sharing **ships today at three seams** and has already produced a BLOCKER (**E17**/C-2), a second key defect (**E13**/C-25) and a third (M-12). (ii) **The adjudicated registry itself already holds a structural-sharing cure candidate** — `fr-ContourEditorCanvas.md:56` **L-5**, routed F.W3/W4, excluded-with-reason at §8, and since repair round 3 **homed by F.W3's §X.1-v4 file criterion at F.W4** (its target file is a member of the partition's criterion-negatives; F.W3 cites, F.W4 books) — **and at round 4 homed independently by the CENSUS OF RECORD, which carries `L-5` inside `fr-ContourEditorCanvas`'s F.W4 roster by enumeration** (⟨cmd⟩ at §8). *(The pass-3 count word "four negatives" is retired for membership: the canonical enumerates that set differently from the sibling probe this file quoted, and a wave that books none of them has no business publishing either numeral. **Criterion and census agree on the home, which is the only fact this row needs.**)*. Absence is therefore a claim about *routing and version atoms*, never about the corpus. **New guardrail**: G-F7-2 replaces the term query with an enumeration before the ruling is drafted.
- **Standing failure mode this wave is built against**: the guardrail that founds the ruling cites, as its bilateral proof, a file that was deleted three tranches ago. The lesson is S-8's, one register up — **a presence claim, like an absence claim, must enumerate the surface rather than trust one file's prose about another tree.** A wave that asked the owner to rule on that premise unexamined would have manufactured the exact defect it exists to prevent — and the premise had already propagated **twice, in two spellings**: **F-W8 §5c's `F.W7` sibling row** (*"carried on BOTH sides"*) and **F-W6 §4's `F.W7` cross-edge row** (*"quoted both trees"*), measured at §5c N-1. **The second site was invisible to the pass-2 probe because the probe was written to one spelling** — the very failure R2-9's detector law decrees against, committed inside the row that convicts spelling-blind probes. Meanwhile **F-W5 §2 clause E16 ▲ has adopted the correction at its own end**, so the premise now stands corrected at the keystone and stale at two consumers: this is what propagation looks like when it is measured instead of assumed, and it is the reason N-1 routes requests rather than edits.
- **Second failure mode, recorded at repair round 1**: this spec's own earlier draft quoted two authorities that existed at **no** coordinate — a "F-W5 §3 Prohibitions" sentence and a dissent at a blank line in `F-W10.md` — and mis-keyed four F.W5 clause ids, which would have run G-F7-8's set-difference against a key set F.W5 does not use. **Both quotations are now sourced by command to their true homes — F-W10 §2.3's SS-4-PREREQ row and F-W5 §2 clause E16 ▲ / §3 gate G7 ⊙ — or struck outright, and every clause id is re-keyed by ID against F-W5 §2's `§E` register, never against a line window.**
- **Third failure mode, recorded at repair round 2 — the one this file kept committing while curing it in others.** Round 2 found four *more* quotations here that no local attestation had caught, every one of them a sentence typed from the shape of a ruling rather than read from a file: a dissent attributed "verbatim" to **F-W5 §2 clause E16 ▲** that F-W5 had **STRUCK at its own hand the same round** (R2-1c's class, committed after the round convicted it); an *"`atomdiff.py:12-14` is the INCUMBENT"* string that returns **0** hits in F-W5; a P-10 sentence, *"F.W7's trie ruling stays inline in F.W7"*, that exists **nowhere in the tree**; and C-25's clause quoted in an **up-cased rendering** whose stated command had gone empty because F-W5 struck the same casing as its own D-6. Alongside them, one *born-RED witness stated as a count against a live sibling* was refuted for the **third consecutive time** (G-F7-11), and one **denominator moved because the quote-repairs moved it** (§1: 9 → 12 → 15). The generalisation is the wave's own thesis, sharpened by its own record: **a citation is a measurement, and an unreproduced measurement is an invention — and in a concurrent round the only measurement that survives is the one whose command is written beside it.** A "verbatim" label is a claim about *bytes at a moment*; a clause id, a gate id and a row label are claims about *identity*, which is why every cross-spec receipt in this file is now the latter and every quotation carries the former's command. **What convicted this spec four times was never carelessness about substance — every substance held at every pass. It was the belief that an address is a citation.** It is not; it is a measurement with a shelf life of minutes.
- **Fourth failure mode, recorded at repair round 3 — the antidote became the instrument.** Round 2's cure was to write the command beside every quotation. Round 3 found the class had simply moved inside the receipt: a quotation attributed to a sibling whose only real home was **a rulings file** (§0 item 1, R3-3.6); a receipt offered for **two** quotes that returns **one**, missed by a hyphen (§5a's P-10 row, and it was the row convicting spelling-blind probes); a count word inside an elision disclosure that was wrong by one about a live sibling's bytes (§7c's F.W0 row); a *"verbatim"* label carrying this file's own bolding, casing or backticks in four places; a corpus quotation with no command at all; and — the sharpest — **an absence receipt that its own quotation falsified**: F-W0's `grep -c 'Genuinely owed' F-W7.md → 0` stopped being true the instant this file quoted F-W0's strike. **The generalisation, and it is now program law rather than this wave's resolution (R3-3.10): a ⟨cmd⟩ whose output is a COUNT of a live sibling or of the citing file is not evidence — it is a snapshot of a clock.** Membership, classification and word-output survive the next edit; a numeral is false the moment the file moves, and a *self-referential* numeral is false the moment you publish it. Every receipt in this spec is one of the surviving forms with TWO disclosed residues — §8's `L-5` erratum banks two `grep -c` counts over live siblings (`waves/F-W4.md`, `waves/F-W3.md`), both re-run **0** at the pass-5 settle and both re-cut to set-membership at repair round 5. ⌧ **THE ROUND-3 ABSOLUTE WAS FALSE IN THE ROUND THAT WROTE IT (REST-33; P5-5, HIGH).** It read *"Every receipt in this spec is now one of the surviving forms, and the one place a numeral remains — the frozen 66-record corpus — is the one operand in the program that cannot move."* — while §8's `L-5` erratum banked, **in the same round**, `grep -n 'ContourEditorCanvas' waves/F-W4.md | grep -c 'L-5'` → **0** and the same against `F-W3.md` → **0**: **two numerals over two LIVE wave specs**, precisely the R3-3.10 form this section declares extinct. Both re-run to **0** at this seat — *the measurement reproduces; the attestation does not.* ⊘ R4-1.9 / PASS-4 D-4 — *"an attestation of sweep must publish its residue or not exist"* — **recommitted one section later in the same file, in the same round, by the same cure.**
