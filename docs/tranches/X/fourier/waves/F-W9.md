# X.F.W9 - The Deploy Spine, the Coverage-Void Execution, and the Evidence Floor (frontend half)

⟨X·F · **status: planned** · half: frontend · twice-authored 2026-08-28 (Fable seat · Opus seat) and folded WHOLE by the fresh-Fable agglomeration seat (M-23) under M-25 · **repaired 2026-08-28 (L-20 round 1) under `conformance/PASS-1/RULINGS.md`** — R-2b · R-3 · R-4b · R-5 · R-8 · R-9.1 · R-1f applied, receipts at §2.8 · **repaired again 2026-08-28 (L-20 round 2) under `conformance/PASS-2/RULINGS-2.md`** — **R2-1c** (the struck-bytes G-12 quotation replaced by G-12's live bytes; the pathless *"the CARRY"* dies with it) · **R2-2** (every cross-spec citation converted to the STABLE-ANCHOR idiom — gate ids, §-headings, banked row ids, cross-edge labels; **no line number into a live sibling survives in this spec's own voice** — an absolute **re-stated at round 3 only after a FORM-agnostic probe returned ∅**, the round-2 spelling of it having been false at §5 by a space-formed cite its own probe could not match in principle, §2.8 **E-10(a)**) · **R2-5** (the F.W1 roster extent re-quoted, by command) · **R2-6a** (the paired-splice direction restored and **PERFORMED both ends**: F.W9 authored the checkpoint bytes, F.W10 deleted its rival paragraph and received them — the shared region `diff`s **empty**) · receipts at §2.8 **E-4 / E-5 / E-6 / E-7** · **round 2 landed in THREE sittings.** Sitting 1 was wall-killed mid-write and its partial bytes were banked; **sitting 2** resumed, re-ran every ⟨cmd⟩ rather than inheriting one, and struck four of sitting 1's receipts as never-run (six surviving line cites, a probe that returns ∅ under BSD `grep`, an unrun count word, a spent asymmetry finding) — §2.8 **E-8**; **sitting 3** re-ran every ⟨cmd⟩ again AND added the probe no earlier sitting ran — **an existence sweep over every cited `§`** — which convicted **two phantom section anchors that had survived both prior rounds** (`lane-docs §6b` and `census §4.5`, each measuring **0** at its own file), a **fused two-span quotation** of F.W0's mirror clause, **three reciprocity claims already discharged by their siblings** (F.W0 twice, F.W1, F.W4 — only F.W3's is still owed), and **a count word that E-8 had retired only to install another of the same self-counting shape** — §2.8 **E-9**, with the full provenance sweep at **§2.8a QUOTE REGISTER** · **repaired a third time 2026-08-28 (L-20 round 3) under `conformance/PASS-3/RULINGS-3.md`** — **R3-3.5** (E-9(g)'s certification was FALSE at the bytes and is struck: the surviving `lane-frontend :[5]68` in §5's own voice is converted to its row label, and the certification is re-run over a **form-agnostic** probe — `NAME[ .]:[0-9]+` over every external authority this file names, not only the `.md`-spelled ones — pasted beside it) · **R3-3.7** (the `fr-PaperView` D/i-1 *"verbatim"* label re-scoped to the banked bytes with this spec's coupling clause OUTSIDE them; a **PAIRED** edit authored at both ends this round and carried on G-F9-21's face as the **THIRD** paired obligation until the one commit lands) · **R3-7.3** (N-2's *cure* attributed to **F.W4**, where `fr-DarkModeToggle` banks it; F.W9 keeps only the spec-side half) · **R3-9.5** (`$X` / `$V` / `$F` command bases declared at §2.8a; every inline ⟨cmd⟩ rooted, so one cwd per base runs the whole set) · **R3-3.10** (the standing no-banked-counts amendment applied: every count-form receipt over a **live sibling** re-expressed as an absence or word-output probe) · plus the PASS-3 riders — the 36/44 denominator **decomposed inline** rather than inherited, the FR-TT-1 census's `FunctionInput` attribution restored outside the shared bytes, §2.8a's third `VERIFIED` token retired, and §5's two unbound transaction limbs disclosed as named-not-bound — receipts at §2.8 **E-10** · **repaired a fourth time 2026-08-29 (L-20 round 4 — the CENSUS FREEZE round) under `conformance/PASS-4/RULINGS-4.md`** — **R4-3.1 / R4-10** (the record-side roster re-based on the canonical's **16** ⟨*re-based at errata round 5, **E5-16**: read 17; `R2-7` → TERMINAL (∅) and §2.0 row 14 falls with it — REST-52*⟩, booked-or-cited row by row at the new **§2.0**; the check-file operand struck to a prior run) · **R4-1.11 / R4-7.2** (§2.8a's *"the one that failed is E-10(b)"* corrected to **TWO** failures, and **G-F9-21's face now ENUMERATES its paired obligations as a labelled list carrying its own census ⟨cmd⟩** instead of a prose count word that has gone stale in three consecutive rounds) · **R4-2** (the PORTABLE-COMMAND LAW: every base a ⟨cmd⟩ consumes is now **assigned in the published block**, not merely declared in a table — §2.0 and §2.8a; the file was already `-P`-free and its one bounded wildcard is `.{0,18}`, far inside the BSD 255) · **R4-7.1 / R4-7.3** (the seat-local *"verbatim"* label at §2.3 S2 re-labelled a **subset with the cut disclosed**; E-1's absolute re-scoped) · **R4-8.3** (every cross-file paste now carries the sibling's **sha256-at-quote-time**, first 12 hex — §2.8a's PIN TABLE) · plus the PASS-4 local defects **D1**–**D7**, receipts at §2.8 **E-11** · **§X.1-v4 CONSUMED**: F-W3 published the definitive file-criterion partition FIRST this round (R3-1a) and this spec re-grounds its F.W3 cross-edge on those bytes (§4b) · **QUOTE-BY-COMMAND (R2-1-LAW) binds every quotation in this file**: each carries a **⟨cmd⟩** provenance note naming the `grep`/`sed` this seat ran read-only on 2026-08-28, and a "verbatim" label without one is a defect per se; rulings files are cure-shapes, never quotable authority · **provenance note (historical; carries NO gate weight)**: the fold seat's F.W9 intake packet (36 rows · 22 gates · 13 cross-edges) was consumed whole at that session, and it is **not an in-tree authority** — per **R-3** the only citable carries are `carry/F-W1-CARRY.md` and `carry/F-W4-CARRY.md`, **by path**, and this spec's id-keyed closure runs against **`conformance/CENSUS-CANONICAL.md` §2's F.W9 roster — **16** ids ⟨*re-based at errata round 5, **E5-16**: read 17; `R2-7` → TERMINAL (∅) and §2.0 row 14 falls with it — REST-52*⟩, the stamped census of record and the ONLY census operand named anywhere in this file** (**R4-3.1 / R4-10**; enumerated at **§2.0**) over the 66-record registry at `docs/tranches/V/megatranche/registry/adjudicated/`. **`conformance/PASS-1/F-W9-CHECK.md` is a PRIOR RUN and is NOT an operand** — the round-1..3 spelling made it a co-operand of the closure, which is the form **R3-4** declared FAILS BY CONSTRUCTION and made program-wide, and which three rounds walked past because the per-wave directive index did not name F-W9 (PASS-4 **D1**; **R3-8**: absence from a directive index lifts nothing) · zero silent drops (dedupe by identity · cure-shape locks and sequencing edicts carried · routed by mechanism; transcription-only is DEFECTIVE) · L-19 (every gate names a live witness) · L-20 validated against `docs/precepts/instructions/` (TRANCHE-AND-WAVE-SPEC · STYLE · ORCHESTRATION · CONSUMING · LESSONS-LEARNED) · idiom: `docs/tranches/X/waves/W0..W11.md` bounds/gates/provenance ⟨…⟩ cut to the `docs/tranches/X/parse-that/waves/` lean form · **twin**: `F-W10.md` (fold-authored 2026-08-25) — its §2.2 table carried here **byte-identical** (spliced from the twin, never retyped) · witnesses re-measured read-only 2026-08-28 against fourier HEAD `cd26c65` (receipts inline; corrections in §2.8); every deploy/evidence cell re-measures at open⟩

**Alias law**: `F.W9` denotes `X.F.W9` — a search alias, never a rename. The fold seat's working name `waves/W9.md` lands as `F-W9.md`; same identity, alias recorded (the twin correspondingly is `F-W10.md`). Banked ids keep their identity for life; nothing below is re-booked.

**Scope.** The F-side **execution** wave, in three limbs that share one law (*a green must be cited, never asserted*):

1. **The deploy spine** — M.W2/W3/W4 re-homed out of the dead M board and executed: readiness separated from liveness, the API arm made fail-closed on a same-SHA green CI run (inv-28), the silent rollback killed and the inv-31 observability floor stood up.
2. **The coverage void** — the P-12 packet the census gives to no wave at all: a unit harness, a lint floor, five e2e/axe seats (admin tab · `/equation` · `/paper` · un-vacuate · the coarse-pointer matrix), authored **once each** and cited many times.
3. **The evidence floor** — M.W11's half: a green inv-27 run id behind every claimed green, CWV/INP under a hard order-lock, axe artifacts, and a containerization fidelity audit taken **as-running, not as-declared**.

**F.W9 owns GATES. F.W9 owns no CURE.** Every repair named below is homed at F.W0/W1/W3/W4 or rides a producer relay; this wave's claim is the instrument that would have caught it. F.W9 owns the deploy spine; F.W10 owns the correspondence (census-declared split).

**Four-verb**: AUDITED **YES** (registry-adjudicated; re-measured 2026-08-28) · SPECIFIED **YES — by this file** · IMPLEMENTED **NO** · VERIFIED **NO**. **Opens after**: F.W0 (substrate pre-gates, hard) → F.W1 (atomic) → F.W3/W4 (the cures this wave gates). **Agents**: 3 serial (deploy-spine · seats+harness · evidence), never parallel — units 2 and 3 both write `web/package.json` and `.github/workflows/ci.yml`. **Hard gate**: G-F9-1..23, all born-RED (G-F9-23 added at the L-20 round-1 repair per R-2b). **EXECUTION IS NOT AUTHORIZED BY THIS FILE** — status stays `planned`; nothing opens product source until the owner's begin-word.

**Goal criterion.** This wave succeeds if, when work ends, **no claim in the X·F program is green by assertion** — the host runs HEAD, a failed deploy is loud in a watched channel, every gate cites a covering run id, and the five instruments exist that make the packet's ungated rows *falsifiable*. A gate that names no live witness is not a gate; a green that names no run id is not a green.

**Binding law.** M-25 (a spec ignoring adjudicated rows is DEFECTIVE; transcription-only is DEFECTIVE) · L-19 · L-20 · E13 (no wave closes with UNREAD mail) · **R3-9** (an assertion of non-execution is *recorded*, never cited as a method guarantee) · **KF.W4's transferable gate-design law**, binding on every gate authored here: (a) one immutable map keyed by physical coordinate; (b) reauthenticate every payload node and checksum row at use; (c) read every declared predecessor authority, never allowlist; (d) derive every envelope assertion from the pinned artifact; **and a gate may not re-derive its own oracle.** Two standing laws shape every row: **the registry is READ-ONLY** (the F.W9/F.W10 re-cut is a table in two specs plus a census addendum, never an edit to `fr-*.md`), and **producer rows never become frontend hacks** (glass → SS-6; latex-paper → the F.W10 register; fourier-tree writes are asked, never made — COMMISSION §2).

---

## §1 Bounds

### §1a Owned — value.js tree (spec + ledger; writes only post begin-word)

| # | Path | Act |
|---|------|-----|
| 1 | `docs/tranches/X/fourier/waves/F-W9.md` | CREATE — this spec. §2.2's table bytes are identical to `F-W10.md`'s |
| 2 | `docs/tranches/V/megatranche/formation/fourier/CENSUS-ADDENDUM-2026-08-25.md` | MODIFY — **co-owned with F.W10**; F.W9 adds only the §4.10 deploy-spine + coverage-void record and §2.8's errata. Addendum-**beside** (E-3): the dated `CENSUS-2026-08-03.md` bytes are never rewritten |
| 3 | `docs/tranches/V/coordination/INBOX.md` | **APPEND-ONLY** (E13) — the F.W9 evidence rows and the deploy-spine relay. No existing row rewritten |

**Bounds reconciliation (R-3 — re-grounded on the tree, no carry cited).** Rows 1–3 are the three value-side write paths this wave claims, each `ls`-resolved in tree except row 1, which carries its own **CREATE** marker with its path. Row 2 is *modify*, not create: F-W10 §1 already claims the CREATE — two waves cannot both create one file, so F.W9 takes the co-owned §4.10 carve and the §6.7/§4.11 cells stay F.W10's. Disjointness holds by section, and the two waves are serial at the boundary.

### §1b Claimed — fourier tree (EXECUTION bounds; the write-seat is flagged, never presumed)

**WRITE-SEAT DECLARATION (inline flag, M-25 — never presumed).** Every path below sits in `/Users/mkbabb/Programming/fourier-analysis`. COMMISSION §2 makes fourier-tree writes **asked, never made** from this seat. These bounds are therefore a **claim of ownership over the change**, not a licence to write. At execution the edits land either (a) in fourier's own sub-session against this spec, or (b) by this seat under an owner grant that names it. The begin-word settles which; **no seat presumes (b)**, and F-W5's standing sentence ("`fourier-analysis` is READ-ONLY, always") holds until it is lifted in writing.

| Path (fourier) | Act | Row it serves |
|---|---|---|
| `web/package.json` | MODIFY | `test` script + vitest devDependency + `lint` script — PAW-50 execution · FR-AUL-16 devDependency limb · M7 lint limb |
| `web/vitest.config.ts` | CREATE | harness-before-rider; **only after F.W4's decision** |
| `web/eslint.config.js` | CREATE | the lint floor (M7's second limb; tsconfig flags are F.W0's) |
| `web/playwright.config.ts` | MODIFY | the mobile/coarse project — `:46-51` is **one** `chromium` project today (re-measured 2026-08-28) |
| `web/e2e/gallery.spec.ts` | MODIFY | un-vacuate `:19-22` / `:49-53` / `:57-63` / `:66-82`; open a card; console guard error-only→`warn` (**LOCKED behind F.W1**) |
| `web/e2e/visualization-ux.spec.ts` | MODIFY | the axe keystone (`:26-43`) extended into the admin tab; the `:110`/`:133`/`:192` un-fixme rides F.W3/W4's B-3 cure |
| `web/e2e/visualization-crud.spec.ts` | MODIFY | AxeBuilder seat; the `:630` fixme |
| `web/e2e/visual-baseline.spec.ts` | MODIFY | `/equation` (`:34`, re-measured; the superseded `:35` is retired at §2.8 C-4 **under F.W0's G-11 table**) gains its first assertion |
| `web/e2e/paper-performance.spec.ts` | MODIFY | the `/paper` axe seat; the DarkModeToggle locator (`:328`) co-lands with N-2's label cure, same commit |
| `web/e2e/` — remaining 3 of **8** specs | MODIFY | **quote 8, never 9** (K11; re-measured 2026-08-28 → exactly 8 `*.spec.ts`) |
| `web/src/lib/figureDimensions.ts` | MODIFY | PAW-50's relocation target for `resolveFigure` — lands in whatever wave executes PAW-12 |
| `.github/workflows/ci.yml` | MODIFY | inv-27 run-id emission + the new unit job (three jobs today, re-measured 2026-08-28 read-only: `api-tests:` `:37` · `web-build:` `:77` · **`e2e-tests:` `:100`** — the superseded ``e2e`/`:101`` spelling is retired at §2.8 C-6 under F.W0's G-11 table) |
| `.github/workflows/deploy-pages.yml` | **READ** | the inv-28 reference implementation (`:48-57`) — mirrored, not edited, unless the gate demands |
| `scripts/deploy-hook.sh` | MODIFY | M.W2 (readiness ≠ liveness) · M.W3 (fail-closed inv-28) · M.W4 (kill silent rollback + inv-31) |
| `scripts/pages-deploy.sh` | MODIFY | inv-25 deploy-of-record (`cf_deployment_id`) |
| `web/Dockerfile` · `web/.dockerignore` · `api/Dockerfile` | AUDIT | containerization fidelity |
| `docker-compose.yml` · `docker-compose.prod.yml` | AUDIT | prod-overlay hardening verified as-**running** |
| `nginx/fourier.conf` | AUDIT | reconcile against the printf-inlined conf at `web/Dockerfile:33-43` (re-measured; the superseded `:31-42` is retired at §2.8 C-4 **under F.W0's G-11 table**) |
| `infra/apache/api-vhosts.conf.template` · `infra/apache/deploy.babb.dev.conf.template` | AUDIT | host-Apache fidelity |
| `docs/tranches/M/PROGRESS.md` | **ASK-ONLY** | the M.W2/W3/W4/W11 rows disposed explicitly on re-home — fourier's sub-session writes it (COMMISSION §2) |

### §1c Read-only bounds (no write)

`docs/tranches/V/megatranche/registry/adjudicated/fr-*.md` (all **66**, re-measured) — anti-rename; the re-cut is a table, not a rewrite · `formation/fourier/{CENSUS-2026-08-03.md, lane-docs.md, lane-frontend.md, lane-crud.md}` · `audit/codex-provenance/INTAKE-ADJUDICATION-2026-08-03.md` (dated; amended beside only) · `docs/tranches/X/fourier/waves/F-W10.md` (**the twin table; F.W9 does not author F.W10**) · `docs/precepts/**` (L-20 authority; **private submodule — CI checkout deliberately omits it, so no gate may assume it is readable in CI**).

### §1d Explicitly NOT owned

`web/tsconfig.json` — **F.W0's** (MG-θ ≡ M7's `noUnusedLocals`/`noUnusedParameters` = one substrate edit, two records, booked once; and the ⟨re-cut⟩ PAW-49 `noUncheckedIndexedAccess` flag) · **any fourier product source under `web/src/components/**`** — F.W3/W4 owns every cure, F.W9 owns gates only · value.js `{src,demo,api,test,e2e}/**` (execution gate) · `scripts/dev/dev.sh` (unowned dirty row, **NEVER**) · `glass-ui/**`, `latex-paper/**`, `keyframes.js/**` (reached only by relay letter) · `docs/tranches/X/{waves,keyframes,parse-that}/**` · `~/.codex/**`.

---

## §2 Carry

**Thirty-six dispositions, DECOMPOSED here and not inherited** (the round-2 wording *"thirty-six **banked** rows"* is **STRUCK** — §2.8 **E-10(c)**): **26** registry routing identities (§2.2's table) ⊕ **5** by-mechanism entrants (§2.2's entrant block) ⊕ **5** §2.5 rows (M.W2 · M.W3 · M.W4 · M.W11 ⊕ the containerization audit) = **36**. **The last five are NOT banked registry rows** and the old wording said they were: four are re-homed M-board rows and the fifth's own cell reads *"census-assigned scope; no registry row"*. Each term is enumerated at its own section, so the figure is falsifiable by counting the sections rather than by trusting this sentence — which the superseded figure was not: it traced to the fold seat's intake packet (*"36 rows · 22 gates · 13 cross-edges"*), and **the masthead declares that packet NOT an in-tree authority carrying NO gate weight** (R-3), so it could never have been an operand. Agglomerated by mechanism, **plus the eight-item F.W1 visual-regression checkpoint set homed here by R-2b** — **36 + 8 = 44 dispositions in all**, **unchanged** ⟨**re-cut at repair round 5, REST-58 (P5-8).** Round 4 wrote *"now **46** with round 4's two canonical additions"*. **`PAW-50` was already inside the 26, therefore already inside the 44** — ⟨cmd⟩ `awk '/^\| record \| identity/,/^\*\*By-mechanism entrants/' "$W" | /usr/bin/grep -c '^\| fr-'` → **22** rows carrying 26 identities, and row 16 is `| fr-PaperArticleWindow:200 | **PAW-50** | **F.W4** ⟨re-cut⟩ |`: **round 4 moved its HOME, not its membership.** Only `R2-7` was a genuinely new disposition, **and E5-16 has now struck it from the census** — so 44, not 46. **Count the sections, which is the falsifier §2 installs for itself at E-10(c).**⟩ **A DISPOSITION COUNT IS NOT A CENSUS, and round 4 separates the two in terms**: **44 (now 46) is what this wave DISPOSES OF**, enumerated by section; **16 is what the stamped census HOMES HERE** ⟨*re-based at errata round 5, **E5-16**: read 17; `R2-7` → TERMINAL (∅) and §2.0 row 14 falls with it — REST-52*⟩, enumerated at **§2.0** from `$C` §2 and nowhere else (**R4-10**). The two denominators measure different things, neither corrects the other, and **only the second is an operand** — a gate citing any other census is defective on sight (R4-3). **A row's presence here is a disposition, not a work order** — coverage rows execute HERE; substrate rows are carried for gating only; the parity rows (§2.6) execute elsewhere and are carried so the set-difference closes.

### §2.0 THE CANONICAL F.W9 ROSTER — **16** ids ⟨*re-based at errata round 5, **E5-16**: read 17; `R2-7` → TERMINAL (∅) and §2.0 row 14 falls with it — REST-52*⟩, each BOOKED or CITED TO ITS HOLDER (R4-10 · R4-3.1; cures PASS-4 **D1**)

**The law, applied rather than paraphrased.** **R4-10**: *a wave's record-side roster is the CANONICAL §2 roster for its home, verbatim; a wave BOOKS each canonical id, or CITES the holder by name, or cites the canonical packet row; no wave derives, samples, or re-cuts a roster from any other source — not a check file, not a prior pass, not its own §-arithmetic.* **An id absent from both booking and citation is this wave's escape; an id booked that the canonical does not home here is this wave's fabrication.** Both halves are discharged below.

**`conformance/CENSUS-CANONICAL.md` is the only census this file uses as an OPERAND — and the scope of that claim is stated rather than asserted**, because an unscoped absolute is the shape this file has now been convicted of four times (E-1's, the masthead's, E-10(a)'s, and E-11(b)'s). Measured as **set membership, never as a count of this file** (R3-3.10; §2.8a's own rule): ⟨cmd⟩ `grep -o 'CENSUS-[A-Za-z0-9-]*\.md' F-W9.md \| sort -u` → **three distinct names — `CENSUS-2026-08-03.md` · `CENSUS-ADDENDUM-2026-08-25.md` · `CENSUS-CANONICAL.md`**. Their dispositions:

- **`$C` (`CENSUS-CANONICAL.md`)** — the **operand**: the roster, the homes, the homonym qualifications. Every id-level figure in this file comes from here.
- **`$M/CENSUS-2026-08-03.md`** — named at three sites and **an operand at none**: P-2's **absence probe** (`grep -o '4\.5' "$M/CENSUS-2026-08-03.md"` → **∅, exit 1**) and, twice, its **§4 item 5 unit-test-floor DECISION clause** — an *authorization in prose*, not a count, a roster or a denominator. **R4-3 forbids a superseded census as a DENOMINATOR; it does not abolish a decision clause**, and this file takes no figure from that file.
- **`CENSUS-ADDENDUM-2026-08-25.md`** — a **write target** at §1a row 2, never read as authority.

§2.1's `54 / 25 / 66` survives strictly as a **structural corroboration of the two specs' shared bytes** and is disclaimed as a denominator there and on **G-F9-21**'s face.

**COMMAND BASES — ASSIGNED HERE, IN THE BLOCK THAT CONSUMES THEM (R4-2.3).** The round-3 table at §2.8a *declared* bases without assigning them; R4-2.3 rules an unassigned `$R`/`$V` a phantom witness. Every ⟨cmd⟩ in this section and at §2.8a runs from the **value.js repo root** after this exact block — **and from repair round 5 every bare relative operand in those commands is RESOLVED THROUGH THE VARIABLES BELOW, not through the cwd (REST-60; P5-4, HIGH)**:

- a bare `waves/…` operand reads **`"$X"/waves/…`** (e.g. `"$X"/waves/F-W0.md`)
- a bare `formation/fourier/…` operand reads **`"$M"/…`** = `"$V"/formation/fourier/…`
- a bare `registry/adjudicated/…` or `fr-*.md` operand reads **`"$R"/fr-*.md`**
- a bare `carry/…` operand reads **`"$X"/carry/…`** (e.g. `"$X"/carry/F-W4-CARRY.md`)

⌧ **Why the assignment and not a convention.** Forty-four commands in this section took bare `waves/…` (33) · `formation/fourier/…` (7) · `registry/adjudicated/…` (2) · `carry/…` (2) operands, **none of which resolves from the value.js repo root this block declares** — they resolve only from `$X` or `$V`, i.e. **at least three incompatible cwds**, which is verbatim the defect §2.8 E-10(e) records and declares cured. The falsifier, run from the declared root: ⟨cmd⟩ `/usr/bin/grep -o 'No unit-test runner — vitest is ABSENT' formation/fourier/lane-frontend.md` → `No such file or directory`, **exit 2**. ⊘ **Round 3 answered this with a *convention*; R4-2.3 answered it with an *assignment*; the assignment block was written and the consuming commands were never re-rooted to it. R4-2.5 convicts on sight.** **§2.8a's own section-B table — which carries bare `waves/F-W0.md` operands inside the register that publishes the repo-root rule — resolves through this same assignment.**

```
X=docs/tranches/X/fourier
V=docs/tranches/V/megatranche
R="$V/registry/adjudicated"
M="$V/formation/fourier"
C="$X/conformance/CENSUS-CANONICAL.md"
F=/Users/mkbabb/Programming/fourier-analysis          # pinned HEAD cd26c65
```

**The roster, quoted by command** (BSD `/usr/bin/grep`/`awk`, read-only, this seat 2026-08-29; `$C` sha256-at-quote-time **`3e0a9acb3381`** — R4-8.3; re-pinned at errata round 5, §0.4 applied):

```
grep -o '^### F\.W9 — \*\*16 rows\*\*' "$C"                                    → ### F.W9 — **16 rows**
awk '/^### F\.W9 — /,/^### GLASS-RELAY/' "$C" | grep -c '^- \*\*fr-'           → 15
awk '/^### F\.W9 — /,/^### GLASS-RELAY/' "$C" | grep '^- \*\*fr-' | grep -o '`[^`]*`' | wc -l  → 16
```

**15 records · **16** ids.** ⟨*the token probe is narrowed to the roster lines at repair round 5 (REST-52): the cured canonical's `### F.W9` **header** now carries an errata note whose own backticked `R2-7` the unrestricted probe counted, returning **17** for a **16**-id roster — the probe is re-cut so its output IS the claim*⟩ ⟨*re-based at errata round 5, **E5-16**: read 17; `R2-7` → TERMINAL (∅) and §2.0 row 14 falls with it — REST-52*⟩ Every one is disposed below; the aliases sit **beside** their heads and are never standalone identities (§0.1 rule 1, anti-rename).

| # | record | canonical id · aliases beside it | this spec's disposition |
|---|---|---|---|
| 1 | fr-AdminAuditLog | **`AA-44`** | **BOOKED** — §2.2 · S1 · G-F9-3 |
| 2 | fr-AdminFlaggedPanel | **`FR-AFP-49`** · `D-21` · `L-26` | **BOOKED** — §2.2 (fold; dissent preserved) · S1 |
| 3 | fr-App | **`C-13`** | **BOOKED** — §2.2 · §2.4 · G-F9-1. Record-qualified: `C-13` is a **nine-row homonym class** in the canonical (⟨cmd⟩ `grep -cE '^\| .C-13. ' "$C"` → **9**), so the booking is the PAIR `(fr-App, C-13)` and nothing else |
| 4 | fr-ContourPreview | **`C:D-11`** · `L:L-12` | **BOOKED** — §2.4's simplify-heap unit target and the §2.2 routing cell for `fr-ContourPreview:71`. **The shared §2.2 cell spells that cell's identity `38`, which is the record's ROW NUMBER, not an id** (§0.1 rule 1) — a defect inside the paired bytes, disclosed as **P-6** and never fixed unilaterally. Record-qualified: `(fr-ContourPreview, C:D-11)` ≠ `(fr-EquationView, C·D-11)`, which the canonical homes **F.W2** |
| 5 | fr-EquationModeToggle | **`FR-EMT-11`** · `C-16` | **BOOKED** — §2.2 · S2 · G-F9-5/-6. Its rider's *"verbatim"* label is **P-4** (R4-7.2) |
| 6 | fr-EquationResult | **`FR-EQR-31`** · `L-i3` · `C-14` · `D-20` | **BOOKED** — §2.2 · S2 |
| 7 | fr-FunctionInput | **`C-13`** · `D-24` | **BOOKED** — §2.2. The PAIR `(fr-FunctionInput, C-13)` — a **different row** from #3, as §2.2 states twice |
| 8 | fr-GalleryAdminBanner | **`GAB-10`** · `D-M9` · `C-16` | **BOOKED** — §2.2 (folds by reference → AA-44) · S1 |
| 9 | fr-GalleryCardModal | **`GCM-42`** · `L-14` | **BOOKED** — §2.2 · S4 · G-F9-9/-10 |
| 10 | fr-GallerySearchBar | **`FR-GSB-17`** · `L-11` · `C-7` | **BOOKED** — §2.2 · S4 · G-F9-7 |
| 11 | fr-GalleryView | **`FR-GV-36`** · `L-24` · `GAB-10` · `GCM-42` | **BOOKED** — §2.2 · S4 |
| 12 | fr-ImageUpload | **`L:L-i2`** | **BOOKED** — §2.3 S5 · G-F9-13 (the drag/reject/oversize/keyboard void). **The shared §2.2 cell spells it `27` — again the record's ROW NUMBER** — disclosed as **P-7** |
| 13 | fr-PaperArticleWindow | **`PAW-34`** · `C-i-5` | **BOOKED** — §2.2 · S3 |
| 14 | fr-PaperArticleWindow | **`R2-7`** | **STRUCK AT ERRATA ROUND 5 (E5-16) — canonical TERMINAL (∅).** The canonical now reads `` | `R2-7` | — | — | **TERMINAL (∅)** <sub>*errata R5 E5-16*</sub> | ``: `fr-PaperArticleWindow.md:186` is a **Ruled-items** row whose own disposition column reads `**ADMITTED → PAW-50 (INFO).**` (siblings R2-1..R2-6/R2-8 read identically), and its lone `F.W9/W10` token grammatically qualifies **PAW-12's** rider — **`R2-7` and `PAW-50` were one finding banked twice.** Round 4 booked it because the census homed it here; **the census re-derived it and the booking goes with it.** **The no-unit-harness finding itself is not lost: it is `PAW-50`, booked whole at row 15.** ⊘ The pass-5 seat filed this **against the canonical and not against the spec**, and said so: *"if the canonical sustains the reading, nothing moves in F-W9; if it does not, F.W9's roster is 16 and §2.0 row 14 falls with it."* **It did not sustain it.** (REST-52) |
| 15 | fr-PaperArticleWindow | **`PAW-50`** | **BOOKED WHOLE AT ROUND 4** — §2.4. **The round-1..3 ⟨re-cut⟩ that homed PAW-50's DECISION leg at F.W4 is STRUCK as a wave-side re-cut the canonical does not carry**: the canonical routing column reads `F.W9/W10` sole-token and the home is **F.W9**. **Harness-before-rider survives undiminished — as a SEQUENCING LOCK (§4a-4), which is what it always was** — and the F.W4 vitest-floor decision clause (`$M/CENSUS-2026-08-03.md` §4 item 5) stays cited as the authorization F.W9 executes under. Only the *home* moves, and it moves to where the census puts it |
| 16 | fr-PaperView | **`D/i-1`** | **BOOKED** — §2.2 · S3 · §4a-10 |
| 17 | fr-UserSlugBar | **`FR-USB-16`** · `r1-MISSED-5` | **BOOKED** — §2.2 · S4 + S5 · G-F9-7/-13 |

**Roster closure: 16 canonical ids · 16 booked · 0 cited-away · 0 escapes · 0 fabrications.** ⟨*re-based at errata round 5, **E5-16**: read 17; `R2-7` → TERMINAL (∅) and §2.0 row 14 falls with it — REST-52*⟩ The two round-4 additions (#14, #15) are the whole delta, and both are ADDITIONS — nothing the canonical homes here was dropped.

#### §2.0a Ids the shared §2.2 table routes to the F.W9 leg that the canonical homes ELSEWHERE — CITED, never booked

**Why these are citations and not escapes.** §2.2's table splits the corpus's **band token** `F.W9/W10` between the twins; the canonical resolves each row's **home** by §0.1's first-arrow rule, so a row whose own arrow names F.W4 first is **F.W4's row carrying an F.W9/W10 LEG**. The two acts are different and both are real — but only the canonical's home column is a roster. **F.W9 executes the leg and books no identity**, and R4-10's citation form is used: *held at* ⟨wave⟩.

| id, record-qualified | canonical home (⟨cmd⟩ `grep -nE '^\| .ID. ' "$C"`) | citation |
|---|---|---|
| `(fr-AdminFlaggedPanel, FR-AFP-42)` · alias `C:D-18` | **F.W3** <sub>legs F.W9/W10</sub> | **held at F-W3.** Seam-before-seat survives as the §4a-5 sequencing lock; the S1 test seat is a leg, not a booking |
| `(fr-EquationResult, FR-EQR-6)` · `D-5` · `L-m5` · `C-8` | **F.W4** <sub>legs F.W9/W10</sub> | **held at F-W4.** S2's axe leg is an admissibility precondition (§4a-6) |
| `(fr-EquationView, D·D-B2)` | **F.W4** <sub>legs F.W9/W10</sub> | **held at F-W4.** Same shape as the row above |
| `(fr-EquationView, C·D-28)` | **NO CANONICAL ROW** — ⟨cmd⟩ `grep -c 'C·D-28' "$C"` → **0** | The corpus site is a **fold-list prose mention** routing into the banked coverage family, whose canonical head is `(fr-AdminUserList, FR-AUL-16)` → **NWO→SS-3**. **F.W9 books no identity for it**; the `/equation` seat's coverage claim rests on #5, #6 and #7 above, all three canonically F.W9. The round-1..3 treatment of `C·D-28` as a booked identity is **struck** |
| `(fr-PaperArticleWindow, PAW-12)` · `C-M-2` · `L-8` · `C-m-2` | **F.W3** <sub>legs F.W9/W10</sub> | **held at F-W3.** G-F9-14 is F.W9's INSTRUMENT over an id F.W9 does not book — exactly the wave's opening law |
| `(fr-PaperArticleWindow, PAW-49)` | **F.W3** <sub>legs F.W9/W10</sub> | **held at F-W3. The ⟨re-cut → F.W0⟩ is STRUCK** (R4-10: no wave re-cuts a canonical home). The `noUncheckedIndexedAccess` flag remains F.W0's **substrate act** at §1d/§5 — a config edit is not an identity re-home, and §2.6's parity row is re-verbed accordingly |
| `(fr-PaperSidebar, D-B2)` | **F.W4** <sub>legs F.W9/W10</sub> | **held at F-W4.** `D-B2` is a **three-row homonym class** (⟨cmd⟩ `grep -cE '^\| .D-B2. ' "$C"` → **3**); only fr-PaperSidebar's carries the F.W9/W10 leg |
| `(fr-PaperSidebar, C-M4)` | **F.W4** <sub>legs F.W9/W10</sub> | **held at F-W4.** Two-row homonym (⟨cmd⟩ → **2**); `(fr-SpeedSelect, C-M4)` is F.W1's and untouched here |
| `(fr-PaperSidebar, M7)` | **F.W0** <sub>legs F.W9/W10</sub> | **held at F-W0** — the MG-θ identity §2.2 already names. Two-row homonym (⟨cmd⟩ → **2**); `(fr-VisualizationView, M7)` is F.W3's |
| `(fr-Tooltip, FR-TT-1)` · `D-1` · `L-4` | **F.W4** <sub>legs F.W3, F.W9/W10</sub> | **held at F-W4. The ⟨re-cut → F.W10⟩ is STRUCK** — the canonical homes it at F.W4 and neither twin books it. The **promotion lock still binds F.W9 through G-F9-22** without a booking, which is what §2.6 always said it was |

#### §2.0b The by-mechanism entrants, re-based on the canonical

The §2.2 entrant block enters rows **by mechanism**, not by re-cut. The canonical homes four of the five at packets, one at F.W1 — **none at F.W9** — so under R4-10 none is a booking and every one is a citation. **The execution limbs F.W9 proposes are unchanged; only the word "booked" goes.**

| entrant | canonical home | citation |
|---|---|---|
| `(fr-AdminUserList, FR-AUL-16)` | **NWO→SS-3** <sub>routing `NO-WAVE-OWNER · SS-3 · SS-4`</sub> | **A packet row is a census fact, not a wave debt** (R4-10). F.W9 proposes the devDependency + admin-tab axe limbs at §2.3 S1 / §2.4 and **F.W10 records the terminal home**; a decline is recorded with rationale (M-25) |
| `(fr-AppHeader, FR-AH-45)` · `C-16` | **NWO→SS-3** | Same class. §2.3 S5's header limb is the execution proposal; **the terminal home is decided AT F.W10** |
| `(fr-CanvasOverlayButton, FR-COB-20)` | **SS-3** <sub>legs SS-4</sub> | Same class, and doubly conditional: F8-REACH-02's HOLD is RED, so **F.W9 authors no harness for a file that may be DELETE** |
| `(fr-FullscreenViewer, FV-26)` · `D-23` · `C-m7` | **UNROUTED** | The canonical's UNROUTED bucket is *"the routing frontier, owned by the forming SS-3/SS-4 specs"* (R4-10). F.W9 supplies the **teleport-locked fullscreen assertion (G-F9-11)** as an instrument and **books no identity** |
| `(fr-PaperArticleWindow, PAW-33)` · `C-i-2` | **F.W1** | **held at F-W1** — unchanged from round 3, which already excluded it with reason (§5) |

**And the F.W0-gated substrate row named at §4a-2 / G-F9-20**: `(fr-NotationPills, FR-NP-32)` is canonically **F.W1** <sub>legs NO-WAVE-OWNER</sub>, not F.W0 — **held at F-W1**; F.W0's act on it is the relay/pin disposition the gate waits on, not a home. The R-8 canonical citation form `FR-NP-32 (≡ fr-PaperSidebar M1)` is unchanged at all three of its statements.

**Net effect on gates: none.** No gate's RED state moves, no witness changes, no lock or dissent is disturbed. What moves is the **basis**: the roster is the canonical's, the citations name holders, and the word *booked* now appears only where the canonical homes the id here.

### §2.1 RE-CUT-0 — the shared precondition (twin of F-W10 §2.1; recorded, not re-booked)

**BLOCKER (spec-integrity; blocks BOTH specs from stamping).** Re-measured 2026-08-28 in `registry/adjudicated/`: `grep -o 'F\.W9/W10' fr-*.md | wc -l` → **54**; `grep -l 'F\.W9/W10' fr-*.md | wc -l` → **25 records**; bare `F.W10` routing → **0**; `ls fr-*.md | wc -l` → **66**. Identical to the 2026-08-25 fold. This is corroboration of a pinned artifact, **not a re-derivation of the oracle** (KF.W4(d)) — the 26-identity taxonomy is F-W10 §2.1's and is consumed, never recomputed here: 26 routing identities · 14 routing-law boilerplate lines (**zero rows minted**) · 2 same-identity restatements · 12 verdict-prose mentions (dated, immutable). Seven records yield zero re-cut rows. Cure: all 26 identities cut to exactly one of F.W9 / F.W10 / a named other wave, Δ=∅ both directions; **both specs carry the same table; neither stamps until G-F9-21 ≡ G-F10-6 closes.**

**ROUND-4 RE-BASING (R4-3 / R4-10), stated where the figure lives so no reader inherits the old status.** `54 / 25 / 66` is **NOT a census operand and is not this wave's denominator.** It is a **band-token measurement** over the frozen corpus — how many times the two-wave band `F.W9/W10` is spelled, and in how many records — and its only work here is to bound the **shared table both specs must agree on**, which is a structural fact about two files, not a roster. **The roster is `conformance/CENSUS-CANONICAL.md` §2's F.W9 home list (17), enumerated at §2.0, and nothing else.** The 26-identity taxonomy consumed from F-W10 §2.1 is therefore a **leg-split of the band**, not a home census: ten of its identities are canonically homed at F.W3 / F.W4 / F.W0 / a packet with an F.W9/W10 **leg**, and §2.0a cites every one of them to its holder. Rounds 1–3 used the taxonomy as if it were the census; **R4-10 forecloses that reading**, and G-F9-21's operand is re-stated on its own face accordingly.

### §2.2 The re-cut table (26 identities; identical in F-W10.md) + P-12 locks

*Heading mirrored (`identical in F-W9.md` ↔ `identical in F-W10.md`). **SCOPE OF THE SPLICE, stated exactly** (R2-1-LAW.2 — a "verbatim" label may point only at bytes a command produced): the **26-identity table** and the **by-mechanism entrant block** below are carried **verbatim** from F-W10 §2.2 — spliced, never retyped, `diff`-verified at write. **ONE cell of the table moved at round 3** (R3-3.7): the `fr-PaperView:120` **D/i-1** cell, whose *"verbatim lock"* label covered a clause the corpus does not carry. **The direction is the one this region has always had — F.W10 AUTHORED the corrected bytes and F.W9 RECEIVED them by splice**, never retyped and never answered with a rival cell (the round-2 lesson, R2-6a, applied in the other direction): ⟨cmd⟩ `diff <(awk '/^\| record \| identity/,/^\*\*By-mechanism entrants/' F-W9.md) <(awk '/^\| record \| identity/,/^\*\*By-mechanism entrants/' F-W10.md)` → **empty**, this seat, after the receive. **The third paired obligation is therefore discharged AT THE BYTES and lands as ONE commit**; §2.8 **E-10(b)** is its record and G-F9-21's face carries it. The entrant block and the checkpoint block are untouched by it. **This head sentence is NOT itself spliced** and never was — ⟨cmd⟩ `diff <(grep -h '^\*Heading mirrored' F-W9.md) <(grep -h '^\*Heading mirrored' F-W10.md)` → **DIFFER**: the twin's own head declares *"below are the SHARED BYTES, authored here and carried verbatim into F-W9 §2.2"* (⟨cmd⟩ `grep -o 'below are the SHARED BYTES, authored here and carried verbatim into F-W9 §2\.2' F-W10.md`) and points its disclosures at *"Count and deixis disclosures live outside the shared bytes, at §2.8 (E-3 / E-5)"* (⟨cmd⟩ `grep -o 'Count and deixis disclosures live outside the shared bytes, at §2\.8 ([^)]*)' F-W10.md`), this one at E-0 / E-1 — two seat-local sentences over one shared region, and the round-1 claim that the **preamble** was spliced over-reached by exactly this line (§2.8 **E-7**). The **R-2b checkpoint block** below the entrant block is likewise not spliced from the twin: **F.W9 AUTHORED it and F.W10 has RECEIVED it** — ⟨cmd⟩ `grep -o 'RECEIVED HERE BY SPLICE' F-W10.md` (R2-6a, performed both ends; §2.8 **E-2**). **Any change to the shared table, the entrant block, or the checkpoint block is a PAIRED edit landing in both specs in one commit**: a unilateral fix breaks G-F9-21 / G-F10-6's Δ=∅. Count and deixis disclosures live outside the shared bytes at §2.8 E-0 / E-1 / E-10(b). **ROUND 4 OPENS FOUR MORE AND FIXES NONE OF THEM UNILATERALLY** (§2.8 **E-11**): the `fr-EquationModeToggle` rider's *"verbatim"* label closes its quote marks around a period the record puts outside a parenthesis (**P-4**, R4-1.11 / R4-7.2) · the `fr-Tooltip` cell re-spells the record's own coordinate as `glass-ui 8.0.0:69-73` where the bank reads *"Producer corroboration at 8.0.0 (`:69-73`)"* (**P-5**, PASS-4 D7) · the `fr-ContourPreview` and `fr-ImageUpload` identity cells carry the records' **row numbers** `38` and `27` where the canonical's ids are `C:D-11` and `L:L-i2` (**P-6 / P-7**, R4-10 · §0.1 rule 1) · and the three ⟨re-cut⟩ cells (PAW-49 → F.W0 · PAW-50's decision → F.W4 · FR-TT-1 → F.W10) state homes the stamped census does not carry (**P-8**, R4-10). **All eight paired obligations are enumerated ON G-F9-21's FACE with a census ⟨cmd⟩ over the enumeration**, because a prose count of them has now gone stale in three consecutive rounds. **The correct disposition of every one is DISCLOSE-AND-PAIR**, and the correct roster is §2.0's, which is seat-local and needs no permission from the twin.*

Execution seat = **F.W9-widened (or a minted F.W9a)** per the P-12 packet ruling; the F.W10 share of every F.W9-cut row is the **terminal-disposition record only**. Three cuts deviate from the banked digraph **by mechanism** (flagged ⟨re-cut⟩; recorded in the census addendum). Other-wave legs already homed (F.W0/W1/W3/W4) are cited, never re-cut.

| record | identity (kept for life) | cut | mechanism + locks / preserved dissent |
|---|---|---|---|
| fr-AdminAuditLog:87 | **AA-44** | **F.W9** | packet root: zero frontend tests, 9 e2e never enter the audit tab, backend pins its half. Instrument = GAB-10's `grep -rln "admin" web/e2e/` → **0 files**. Cure: extend ONE existing axe keystone (visualization-ux:26-43 + visualization-crud AxeBuilder) into the admin tab; live-region half stays F.W4. **Dissent (FR-AFP-49, preserved): the family carries blocker weight — "every row above incl. all four blockers is invisible to every gate the repo runs"; INFO is a scope convention (L-9.1), never "the debt is small"** |
| fr-AdminFlaggedPanel:95 / :110 | **FR-AFP-42** (test seat) · **FR-AFP-49** | **F.W9** | 0 props / 0 emits / setup-top-level fetch in `defineAsyncComponent` — untestable seam. Seam → F.W3/W4 (homed); only the seat re-cuts. Distinct mechanism from AA-44 (testability ≠ coverage) — never folded; seam-before-seat. **Quote 8 specs, never 9** (K11) |
| fr-App:91 | **C-13 (App)** | **F.W9** | four untyped ambient global side effects, no vitest floor. The four-wave chain made explicit: **MG-θ → F.W0** (tsconfig/lint one-liners) · floor **decision → F.W4** (census §4.5) · **execution → F.W9** · **terminal record → F.W10**. Distinct id from FunctionInput's C-13 — never merged |
| fr-ContourPreview:71 | **38** | **F.W9** | zero coverage; `vue-tsc -b` is precisely the gate row-11 is invisible to. **No repair inherited — both flagship cures REFUTED** (the `<Card tier surface>` migration = 21-site typecheck break; the restore-path blank render cannot occur). The simplify-heap unit target anchors F.W4's harness decision — the cheapest true vitest justification in the packet, not PAW-50's trapped `resolveFigure` |
| fr-EquationModeToggle:41 | **FR-EMT-11** (= C-16) | **F.W9** | no unit runner; `/equation` one assertion-free screenshot slug; axe installed, never pointed here. Rider verbatim: "the gates for FR-EMT-1/-5's cures ride their own waves." **Dissent PRESERVED verbatim: reader-2's MAJOR ("every other row in this record is ungated because of it") ruled a scope error under L-9.1 — "so the F.W9/W10 close cannot treat the MINOR as a statement that coverage debt is small."** The close honours this warning |
| fr-EquationResult:41 / :72 | **FR-EQR-6** (axe leg) · **FR-EQR-31** | **F.W9** | `.eq-scroll-region` scroll container: no tabindex/role/name; KaTeX spans give no focusable descendant. F.W4 = attributes (homed); F.W9 = the single `/equation` seat. Cohort: fr-CoefficientsSpectrum M-6 same class. FR-EQR-31: zero behavioural coverage by construction; D-20's archaeology half rides FR-EQR-27's correction — **not re-booked** |
| fr-EquationView:62 / :191 | **D·D-B2** (axe leg) · **C·D-28** | **F.W9** | zero functional e2e on `/equation` (8 specs / 1 screenshot hit); info button + Wand2 = two nameless icon-only controls; aria-label ×2 → F.W4 (homed). **The packet's single strongest F.W9-seat argument: the ONE interaction spec (compute → notation → budget → reload) mechanically catches B-1, B-2, C-29, D-07, D-14 — five already-booked defects.** BLOCKER→MAJOR per the fr-CT precedent, both readers concur |
| fr-FunctionInput:80 | **C-13 = D-24** | **F.W9** | `/equation` screenshot-only; the nameless Wand2's survival is the corollary. FOLD to banked fr-CollapsibleSection m-8 class (`actions` slot ready). Naming half belongs to FR-TT-1 / EquationView D·D-B2 — never double-book |
| fr-GalleryAdminBanner:49 | **GAB-10** (coverage half) | **F.W9** | GAB-1's `color-contrast` serious class unverified by construction. **FOLDS BY REFERENCE to AA-44**; live-region half → F.W4 riding GAB-4's `aria-busy` (homed). GAB-13 (F.W0) gates it: gates written against the dirty worktree measure a tree no commit holds |
| fr-GalleryCardModal:93 | **GCM-42** (= L-14) | **F.W9** | zero tests every level; console guard filters error-only + 404 — blind to GCM-4's warns three ways. **FOLD → AA-44 / FR-AUL-16 class, never re-booked.** Own increment surviving the fold: widen the guard to `warn`. **Interaction lock: no filter widening before F.W1 lands** — the error-only filter is the blind spot F.W1's four zero-console-error gates depend on (FR-EQR-3). Dissent carried: DU's BLOCKER on GCM-4; the SR composite (GCM-4∘16∘17∘6/8) named blocker-weight with no BLOCKER row carrying it |
| fr-GallerySearchBar:54 | **FR-GSB-17** | **F.W9** | its named e2e is vacuous by construction — every assertion inside `if (await filterToggle.isVisible())` (gallery.spec.ts:57-63); delete the bar → GREEN. Un-vacuate, don't add: **assert the precondition EXISTS**; kill the unreachable `.glass-dock` branch (:49-53; `grep -rn glass-dock src/` → 0) and the `.or()` mask (:19-22). One cure shape authored ONCE for the class (with FR-USB-16 + FR-GV-36's census) |
| fr-GalleryView:78 | **FR-GV-36** (= L-24) | **F.W9** | every defect structurally invisible (no vitest/ESLint/noUnusedLocals/vueCompilerOptions); console e2e passes against a dead API. Folds → FR-GFC-27 + AA-44 route + FR-AUL-16 input. Surviving increment: **L's three-conditionally-vacuous-tests census**. The dead-API fact belongs to the F.W0/F.W9 deploy-spine reckoning — edge declared, API repair NOT booked here |
| fr-ImageUpload:61 | **27** | **F.W9** | all **15** e2e entries across **6** spec files use `setInputFiles` — drag/reject/oversize/failure/keyboard uncovered. **Quote the 15/6 pair, never a re-derived count** (ruling 7's denominator). The drag path needs FR-USB-16's coarse project — one matrix change unblocks both |
| fr-PaperArticleWindow:58 / :90 | **PAW-12** (rider limb) · **PAW-34** (= C-i-5) | **F.W9** | PAW-34: zero e2e on any owned surface ("figure" hits = `test.describe.configure` substrings). PAW-12: two facts on one hand-typed literal (`TRANSCODED_FIGURES = new Set(Object.keys(FIGURE_DIMENSIONS))`) guarding a no-fallback `<picture>`; predicate split → F.W3/W4 (homed); F.W9 = the build-time set-equality assertion (vitest or Vite build plugin per PAW-50). Invariant **HOLDS at HEAD** (28 png / 26 avif / 26 webp; unpaired `fourier.png`+`maintainer-avatar.png`; 26 keys = 26 `\includegraphics`) — the rider guards drift. **PAW-50: harness-before-rider. PAW-49: flag-before-split.** Dissent preserved: LC's MINOR sustained at R-2 |
| fr-PaperArticleWindow:199 | **PAW-49** | **F.W0** ⟨re-cut⟩ | without `noUncheckedIndexedAccess`, `resolveFigure`'s return asserts `width: number` while one whole arm yields `undefined` — vue-tsc green over a false type. The flag is a **repo-config** line, identical in kind to MG-θ/M7's tsconfig flags; **the one coverage-family row whose gate is FREE** — `npm run build` already runs `vue-tsc -b`, no new harness. F.W3/W4 rider on the PAW-12 cure homed |
| fr-PaperArticleWindow:200 | **PAW-50** | **F.W4** ⟨re-cut⟩ | web/ has **no** unit harness at all (five scripts, no vitest/jest, zero `*.test.ts`); `resolveFigure` trapped in the SFC. It *is* the harness-before-rider law — census §4.5's **decision** is its only true home; **F.W9 executes** (move `resolveFigure` beside `FIGURE_DIMENSIONS` in `lib/figureDimensions.ts` in whatever wave executes PAW-12); **F.W10 verifies**. Governs PAW-12, App C-13, ContourPreview 38, FR-AUL-16's devDependency limb, AA-44 |
| fr-PaperSidebar:49 | **D-B2** (axe leg) | **F.W9** | "Contents" heading 2.39:1 light / 3.00:1 dark vs the 4.5 floor; no axe visits `/paper`. Token → F.W4 (homed). The `/paper` seat is **ONE artifact** with M7 + PAW-34 and **runs AFTER PAW-1's cure + the PAW-44 scrollport decision** — else 42 token drops make failures un-attributable. MAJOR, blocker-weight in the a11y aggregate |
| fr-PaperSidebar:58 | **C-M4** | **F.W9** | the expand-if-closed guard reads the **Tooltip's** `data-state` (domain `closed\|delayed-open\|instant-open`, never `"open"`) — a no-op. F.W4 half dies with the `CollapsibleTrigger` restructure (homed); F.W9 re-keys the guard on the Collapsible root. Corrected cite :124–129. Third vacuous-gate instance but a **DISTINCT mechanism** (wrong element's state) — never folded; the cure differs |
| fr-PaperSidebar:67 | **M7** | **F.W9** | dead surface unreachable by any gate; two of four needed gates are one config line each. tsconfig half → **F.W0 = the SAME identity as MG-θ — one substrate edit, two records, booked once**; F.W9 = axe on `/paper` + lint script |
| fr-PaperView:120 | **D/i-1** | **F.W9** | zero coverage of the search surface, no vitest. **Coupled — the bank's own words, and ONLY those** (⟨cmd⟩ `grep -n 'B-1/B-2 cure wave' fr-PaperView.md`, this seat, read-only in the frozen corpus): *"coverage rides the B-1/B-2 cure wave."* The **never-scheduled-independently** clause is **this spec's sequencing lock (§4a.11), outside the quotation marks** — ⟨cmd⟩ `grep -rc 'never scheduled independently' fr-*.md` → **0 in all 66**. **PAIRED EDIT (R3-3.7): re-scoped in F-W9 and F-W10 in ONE commit; a unilateral landing breaks G-F10-6 ≡ G-F9-21's Δ=∅** |
| fr-Tooltip:26 | **FR-TT-1** (promotion note) | **F.W10** ⟨re-cut⟩ | the shim is the ONLY label on **17** icon-only triggers (Editor ×10 · Canvas ×6 · Wand2 ×1), wiring `aria-describedby` only-while-open — a DESCRIPTION, never a NAME. It governs **this wave's close-gate design** (G-F10-10). Naming legs → F.W4 (all 17); shim leg → F.W3 keeping its R3-7a/X-11 identity; folds honoured, never re-booked (EditorControlsDock C-3 · CanvasControlsDock D-1/L-3/C-1 · EquationView D·D-B2). Producer corroboration glass-ui 8.0.0:69-73: "an icon-only trigger labels ITSELF." L-4's 17-count census citable; D-1's 16-count + :42/:49 anchors KILLED (register #8) |
| fr-UserSlugBar:53 | **FR-USB-16** (≡ r1-MISSED-5 ≡ r2-MISSED-1) | **F.W9** | self-disabling test: `gallery.spec.ts:66-82` body wrapped in `isVisible().catch(() => false)`; matrix is one Desktop-Chrome project. Unguard (assert the trigger EXISTS) **+ add the mobile/coarse Playwright project** — a matrix change that must precede every coarse-pointer witness (glass a11y-overrides floor; PaperSidebar `--ui-scale:1.5`/`--control-floor:2.75rem` arm; ImageUpload 27's drag path). **Dissent preserved: r2 graded MINOR; overruled at the ruling table** |

**By-mechanism entrants** (records carry NO digraph — they enter by mechanism, not by re-cut, and must not be counted into the 54):

- **FR-AUL-16** (fr-AdminUserList:54, MAJOR) — the only cascade-delete surface, zero automated coverage of any kind; "un-harnessable" defence KILLED (ruling 3e). NO-WAVE-OWNER: harness decision → F.W4 · devDependency + admin-tab axe limb → F.W9 · **terminal home decided AT F.W10** (§3.3 deadline — last F wave). Cure verbatim: "extend one axe keystone into the admin tab + a vitest devDependency"; slug-bearing aria-labels = ready selector vocabulary.
- **FV-26** (fr-FullscreenViewer:79 = D-23·C-m7) — the only e2e "Fullscreen" hit is a comment (gallery.spec.ts:113). Coverage share → F.W9. **Teleport lock: assert against the teleported root** (FV-27: one of two Teleport sites). FV-30 rides banked BasisCanvas D-4/BC-2/C-2a + AnimationControls C-12, no new booking; FV-31 → F.W4.
- **FR-COB-20** (fr-CanvasOverlayButton:74 = C-7) — interaction contract entirely implicit; no test anywhere. "Dies with the file"; the no-test-for-wrappers observation → SS-3/SS-4 input; the tree favours DELETE. Gated on **F8-REACH-02** at F.W0 — **HOLD is RED; F.W10 verifies no HOLD survives** (that verification is this wave's whole claim, not a re-booking).
- **FR-AH-45** (fr-AppHeader:108 = C-16, NO-WAVE-OWNER spec-input) — zero props/emits/slots, no contract. A question owed an answer ("what contract should a shell header state?") → SS-3/SS-4 spec-input; terminal home or kill-with-rationale at F.W10, never left open. **Discharges together with FR-AH-30's cure-caution: the D-axis rank-5 remedy REVERSES the outcome — only `@layer glass-overrides` appended after `utilities` works** (glass-ui's competing rules already sit in `@layer components` at higher specificity).
- **PAW-33** (fr-PaperArticleWindow:89 = C-i-2) — rides the C-19 ⊕ PAW-33 pin-truth row (§2.6).

**THE F.W1 VISUAL-REGRESSION CHECKPOINT SET — 8 items, HOMED HERE** (**R-2b**, `conformance/PASS-1/RULINGS.md`; direction re-affirmed at **R2-6a**). Source: `carry/F-W1-CARRY.md`, **§CrossEdges item 9** — the `F.W1 → F.W9 / F.W10` row — **cited by path and by row label**, one of the two real carries (R-3; anchor idiom R2-2). **F.W9 is the home, AUTHORS these bytes and owns execution; F.W10 owns terminal disposition and receives them by splice, never retyped**; until that paired edit lands, **G-F9-21 reddens on the asymmetry** and §2.8 **E-2** is its standing disclosure. Carried verbatim from the carry — ⟨cmd⟩ `grep -A0 -n 'F.W1 → F.W9 / F.W10' carry/F-W1-CARRY.md` (read-only, this seat, 2026-08-28): *"The visual-regression checkpoint set the uplift mints with no typecheck signal: `--shadow-cartoon` sign flip · `cartoon-surface` hover loss across 21 sites · the `.disclosure-content` body register · the tooltip re-proportion · GCM-22's `p-0` insets · the Badge rim · FR-CP-13's fused-card gap · the `text-admin-label` reversion. Plus the SS-13 residues each record names."*

**F.W9 owns the checkpoint, never the cure** — every item below is minted by **F.W1's** transaction (cited whole at §4a-3) or cured at F.W3/W4; this wave books only the instrument that makes the drift falsifiable.

| # | checkpoint item (banked wording kept) | why no gate sees it today | F.W9's instrument (gate only) |
|---|---|---|---|
| 1 | **`--shadow-cartoon` sign flip** | a token sign change paints; it never types — `vue-tsc -b` is structurally blind. ⟨cmd⟩ re-measured read-only 2026-08-28 at fourier HEAD `cd26c65`: `grep -rl -- '--shadow-cartoon' web/src/` → **1 file** (`components/visualization/gallery/GalleryCard.vue`), `grep -rn -- '--shadow-cartoon' web/src/` → **3 hits** (`:193` · `:212` the `-hover` twin · `:224`). **The round-1 cell's "2 files" did not reproduce and is STRUCK** (§2.8 **E-5**) — a stated re-measurement that does not re-run is the false-witness class this wave exists to abolish | a checkpoint slug in the post-transaction capture **with a comparison assertion** (G-F9-23) |
| 2 | **`cartoon-surface` hover loss across 21 sites** (the carry's banked wording, kept) | hover state is reached by no spec in the suite. **The 21/14 denominator belongs to F.W0 G-12 and its pattern token is `cartoon-card`, NOT `cartoon-surface`** — ⟨cmd⟩ `grep -o 'shim \*\*21 cartoon-card sites / 14 files\*\*' waves/F-W0.md` → *"shim **21 cartoon-card sites / 14 files**"* (**F-W0 §4 G-12**, the RED-witness shim row, `[fr-AdminAuditLog K-13, re-confirmed FR-EQC-3]`) — **quoted, never re-derived here** (per-site enumeration is G-12's act). Column 2 keeps the carry's own words; **it is not a denominator claim**, and this spec no longer asserts G-12 published a `cartoon-surface` 21/14 (§2.8 **E-6**). At the tree the two tokens are not interchangeable: ⟨cmd⟩ `grep -rl 'cartoon-surface' web/src/` → **1 file** (`style.css`, applied once at `:108` **inside** the `.cartoon-card` shim) vs `grep -rl 'cartoon-card' web/src/ \| wc -l` → **15 files** | the same checkpoint taken in a hover state on one representative site; the site roster is read **from G-12's table**, under G-12's own token |
| 3 | **the `.disclosure-content` body register** | `grep -rl 'disclosure-content' web/src/` → **0** — the register is **producer-side**, so the consumer sees only a render change. **Cure rides SS-6; a producer row never becomes a frontend hack to turn this green** | consumer-visible render checkpoint only; the register itself is relayed, not edited |
| 4 | **the tooltip re-proportion** | FR-TT-1's shim is the only label on **17** icon-only triggers; a re-proportion moves hit area with no assertion anywhere. G-F9-22 owns the **naming** half — this is the **geometry** half, distinct, never folded | checkpoint on one Editor and one Canvas trigger, taken **after** the coarse project lands (§4a-8) |
| 5 | **GCM-22's `p-0` insets** | GCM-22's `p-0` retirement is a **limb inside F.W1's atomic transaction** (**F-W1 §4, intra-wave order step 4** — the atomic transaction, G6; extent per §2.8 **E-4**); worse, **no spec opens the modal at all** (G-F9-9 RED), so the inset is invisible even to a screenshot | **G-F9-9 is its precondition** — the card/modal must be opened before the checkpoint can be taken |
| 6 | **the Badge rim** | a sub-pixel rim change on a primitive no spec asserts | one badge-bearing surface in the checkpoint set |
| 7 | **FR-CP-13's fused-card gap** | the **gap DECISION is a limb of F.W1's transaction** (**F-W1 §4, intra-wave order step 4**) — F.W9 books the checkpoint that makes the decision falsifiable, and **books no decision** | checkpoint pair (pre/post transaction) on the fused-card surface |
| 8 | **the `text-admin-label` reversion** | invisible **twice over**: `grep -rln "admin" web/e2e/` → **0** (AA-44's packet root), and the label is a paint-only token. **7 sites / 4 files is F.W0 G-12's published denominator** — ⟨cmd⟩ `grep -o '`text-admin-label` \*\*7 / 4 files\*\*' waves/F-W0.md` → *"`text-admin-label` **7 / 4 files**"* (**F-W0 §4 G-12**, the RED-witness denominator row); read-only corroboration 2026-08-28 → `grep -rl text-admin-label web/src/` = **4 files** — corroboration of a pinned figure, **never a re-derivation of the oracle** (KF.W4(d)) | rides **S1**'s admin-tab entry (§2.3): once a spec enters the tab, the checkpoint can be taken there |

**"Plus the SS-13 residues each record names"** — carried as a **named class, not absorbed**: each residue stays with the record that names it and rides that record's own wave (F.W3/W4 cures; SS-13 is not F.W9's to book). F.W9's only claim over them is the same instrument: a checkpoint that would catch their visual regression. **No id is re-booked and no residue is renamed** (anti-rename).

**Born-RED witness for the whole set** (re-measured read-only 2026-08-28 against fourier HEAD `cd26c65`, no product source opened): `grep -c toHaveScreenshot web/e2e/*.spec.ts` → **0 in all 8 specs**; **no `*-snapshots` directory exists tree-wide**; `visual-baseline.spec.ts:48` (the **π capture** test, one per route × 3 viewports) writes bare full-page PNGs via `page.screenshot()` at **`:55-59`** (⟨cmd⟩ `sed -n '46,62p' web/e2e/visual-baseline.spec.ts` — the call opens `:55` `await page.screenshot({`, its three option keys run `:56-58`, `:59` closes `});`; the round-1 `:56-60` was off by one and is corrected here under C-4's G-11 quotation status, §2.8 **E-5**) into `docs/tranches/J/audit/screenshots/{before,after}` — **a capture harness, not a regression gate**. All eight items are therefore minted with **no typecheck signal AND no screenshot-diff signal**. Gate: **G-F9-23**.

### §2.3 The five seats (axe-seat economy — author ONCE, cite many)

| seat | artifact | rows it discharges | locks |
|---|---|---|---|
| **S1 · admin tab** | ONE existing axe keystone (`visualization-ux.spec.ts:26-43` + `visualization-crud` AxeBuilder, wcag2a/aa/21a/21aa) **extended**, not cloned | **AA-44** (packet root) · **GAB-10** coverage half (fold-by-reference) · **FR-AUL-16** axe limb · **FR-AFP-49** (fold; books census risk #10 for its record only) · **FR-AFP-42** test seat | Gated on **GAB-13 (F.W0)**. **FR-AFP-42 is seam-BEFORE-seat** — F.W3/W4 must make the component mountable first; testability ≠ coverage, never folded into AA-44. Live-region half stays F.W4 (rides GAB-4's `aria-busy`). Folds cite, they never mint |
| **S2 · `/equation`** | ONE interaction spec: **compute → notation → budget → reload** | **C·D-28** · **FR-EQR-6** axe leg · **FR-EQR-31** · **FR-EMT-11** · **FunctionInput C-13 = D-24** · **EquationView D·D-B2** axe leg | The packet's strongest seat argument — **SUBSET QUOTATION, cut disclosed** (R4-7.1; the round-1..3 *"verbatim"* label covered *"this one spec"*, which is **spec-authored**, PASS-4 D2's rider): the record's own span is *"would have caught B-1, B-2, C-29, D-07 and D-14 mechanically"* (⟨cmd⟩ `grep -o 'would have caught B-1, B-2, C-29, D-07 and D-14 mechanically' "$R/fr-EquationView.md"`), whose subject in the bank is *"one interaction spec (compute → notation → budget → reload)"* — five already-booked defects. FR-EQR-6's attributes and D·D-B2's `aria-label` ×2 are **F.W4's cures**, not this seat's. FR-TT-1 promotion lock applies |
| **S3 · `/paper`** | ONE axe pass + assertions on `paper-search*` selectors | **PaperSidebar D-B2** axe leg · **M7** axe limb · **PAW-34** · **PaperView D/i-1** | **Runs AFTER PAW-1's 42-site cure + the PAW-44 scrollport decision** — else 42 token drops make failures un-attributable. **D/i-1's coupled lock, verbatim: "coverage rides the B-1/B-2 cure wave" — never scheduled independently.** `/paper` **IS** navigated today (`paper-performance.spec.ts:38`; `visual-baseline` paper row × 3 viewports) — "zero mounts / no paper route" must never be quoted from the corpus again. Selectors must not depend on `.sidebar-top-btn` (M2 hard lock) |
| **S4 · un-vacuate** | Rewrite each guard to **assert the precondition EXISTS** | **FR-GSB-17** (`if (await filterToggle.isVisible())`) · **FR-USB-16** unguard half (`isVisible().catch(() => false)`) · **C-M4** (Tooltip `data-state`) · **FR-GV-36** (L's three-conditionally-vacuous census) · **GCM-42** (open a card; widen the guard) | Three DISTINCT mechanisms under one cure shape: self-disabling wrapper (USB) · precondition-conditional body (GSB) · **wrong element's state (C-M4) — never folded, the cure differs**. Kill the unreachable `.glass-dock` branch (`grep -rn glass-dock src/` → 0) and the `.or()` mask. **GCM-42's console widening is LOCKED behind F.W1.** Falsifier for the whole seat: deleting the search bar / slug bar must redden its spec |
| **S5 · matrix + fullscreen + header** | A mobile/coarse Playwright project; a fullscreen assertion; header nav/logo/toggle | **FR-USB-16** matrix half · **27** (ImageUpload: **15** entries / **6** files, `setInputFiles` only — quote the pair) · **FV-26** (teleport lock) · **FR-AH-45** execution limb | **The matrix change precedes every coarse-pointer witness** (glass `[data-size="icon"]` coarse floor · PaperSidebar `--ui-scale:1.5`/`--control-floor:2.75rem` · FV-11's 44-vs-60px discordance · the drag path). FV-26: **assert against the teleported root** (FV-27 — one of exactly two Teleport sites, with `PaperSearchModal.vue:41`). **FR-AH-45 / N-2 rider: the DarkModeToggle a11y label change and the `paper-performance.spec.ts:328` locator edit land in the SAME commit** (the label is a load-bearing selector; `:329` waits 250 ms against a 350 ms morph). **This is a matrix change, NOT a Safari cell** — it discharges no X-W11 G8 row |

### §2.4 The harness and the floor

| row | verb at F.W9 | lock |
|---|---|---|
| **PAW-50** — **BOOKED WHOLE at round 4** (canonical home **F.W9**; the round-1..3 ⟨re-cut⟩ of its DECISION leg to F.W4 is struck — §2.0 row 15, R4-10) | **EXECUTE** the vitest stand-up; move `resolveFigure` beside `FIGURE_DIMENSIONS` in `lib/figureDimensions.ts` in whatever wave executes PAW-12 | **HARNESS-BEFORE-RIDER is this row — and it survives the re-home intact, because it was always a SEQUENCING lock and never a home** (§4a-4). **SEAT F.W0 (G-9)** · the authorizing decision clause F.W4 (**census §4 item 5** — the F.W4 wave-sketch row, the census's only unit-test-floor DECISION clause: ⟨cmd⟩ `grep -o 'a unit-test floor (vitest) decision' formation/fourier/CENSUS-2026-08-03.md` → *"a unit-test floor (vitest) decision"*. **The spelling `census §4.5` is STRUCK — that section does not exist**: ⟨cmd⟩ `grep -o '4\.5' formation/fourier/CENSUS-2026-08-03.md` → **∅ (exit 1)** — an **absence** probe, not a count of a live sibling (**R3-3.10**, applied at round 3; the round-2 `grep -c … → 0` form is retired at §2.8 **E-10(e)**); the census's §4 is a flat list with no `4.x` subsections, and F.W0 ruled the same correction at its own end — §2.8 **E-9**) · execution F.W9 · verification F.W10. F.W9 may not stand a harness the decision has not authorized |
| **C-13 (fr-App)** | the test-floor for four untyped ambient global side effects | chain: **SEAT → F.W0 (G-9)** · MG-θ → F.W0 · decision → F.W4 · execution/FLOOR → F.W9 · record → F.W10. The shared §2.2 cell's F.W0 leg is the **tsconfig** one; the **seat** leg is named here, outside the paired bytes (R-5). **Distinct id from FunctionInput's C-13 — never merged** |
| ~~**`R2-7` (fr-PaperArticleWindow)**~~ — **STRUCK AT ERRATA ROUND 5 (E5-16); canonical TERMINAL (∅)** ⟨*booked at round 4 on a census that has since re-derived it; `R2-7` and `PAW-50` were one finding banked twice, and the finding survives whole at `PAW-50` — REST-52*⟩ | the **no-unit-harness finding itself** (LC missed find 4), banked as a row of its own and homed **F.W9** by the canonical: `web/` carries five scripts, no vitest/jest in any dep block, no `vitest.config`, zero `*.test.ts`/`*.spec.ts` under `src` | Rounds 1–3 read `fr-PaperArticleWindow:186` as a *"same-identity restatement"* of PAW-12's rider and minted no row; **R4-10 makes the canonical the roster and the canonical books it.** It is the **predicate of PAW-50, not a duplicate of it** — R2-7 states the absence, PAW-50 states what the absence blocks. Witness: **G-F9-1**, already RED on exactly these bytes. **Record-qualified**: `(fr-GlassTimeline, R2-7)` is a KILLED row and is untouched |
| **`C:D-11` · `L:L-12` (fr-ContourPreview)** — canonical id restored beside the record's row number **38** (§2.0 row 4; the shared §2.2 cell's `38` is **P-6**) | the simplify-heap unit target — **the cheapest true vitest justification in the packet** | **No repair inherited: both flagship cures REFUTED** (the `<Card tier surface>` migration = a 21-site typecheck break at the uplift target; the SS-13 restore-path blank render cannot occur — `out-in` defers the mount past the ref assignment) |
| **FR-AUL-16** | the vitest devDependency + the admin-tab axe limb | "Un-harnessable by construction" **KILLED** (ruling 3e) — a props-less SFC mounts under `createTestingPinia` + a fetch double; the obstacle is one devDependency. **Terminal home decided AT F.W10.** If SS-4 declines the widening, **the decline is recorded explicitly with rationale (M-25)** — leaving it un-homed re-creates the blind spot |
| **PAW-12 rider** | the **build-time set-equality assertion** (vitest or a Vite build plugin) | Invariant **HOLDS at HEAD** — the rider guards drift, it repairs no live break. Predicate split is F.W3/W4's. **Flag-before-split (PAW-49)** |
| **M7** | the **lint script** (`eslint`/`oxlint`/`biome` configs = **0** tree-wide, re-measured 2026-08-28) | tsconfig half → **F.W0 = the SAME identity as MG-θ — one substrate edit, two records, booked once**. Five `catch (e: any)` in AdminFlaggedPanel have no gate to notice them (FR-AFP-22 / FR-AUL-50) |
| **FR-COB-20** | **harness branch only, and only if F.W0 rules `ISOLATED_HARNESS`** | Gated on **F8-REACH-02** (`CanvasOverlayButton.vue`, N.C39, sha `35f3db91…`, 595 B). **HOLD is RED. F.W9 authors no harness for a file that may be DELETE.** The no-test-for-wrappers observation → SS-3/SS-4 input; the tree favours DELETE |

**RUNNER-SEAT DISJOINTNESS — the W0↔W9 collision, one sentence from this end (R-5).** **F.W0's G-9 owns the SEAT — cited by gate id, `F-W0 §4 G-9`** (R2-2): ⟨cmd⟩ `grep -o '^### G-9 .*' waves/F-W0.md` → *"### G-9 — A unit-runner SEAT exists (the FLOOR's scope belongs to F.W9/W10)"*, and ⟨cmd⟩ `grep -o 'a runner is installed.*proving the seat live' waves/F-W0.md` → *"a runner is installed, **wired into `ci.yml`**, and inside G-8's type scope, with **ONE asserting spec** proving the seat live"*. The heading itself concedes the split. **F.W9's G-F9-1 owns the FLOOR** — the spec population and its thresholds, never the seat's installation act. Two waves, one act each, **no double-booking**. **The reciprocal is NO LONGER OWED — F.W0 HAS WRITTEN IT**, in the identical-at-both-ends form R-5 prescribes (§2.8 **E-9**; round 2's earlier sittings carried *"the reciprocal sentence is owed at F.W0's end"*, which is **false at the live bytes and is STRUCK**): ⟨cmd⟩ `grep -o '\*\*Disjointness with F\.W9 (one sentence, declared identically at both ends — R-5)\*\*' waves/F-W0.md` → *"**Disjointness with F.W9 (one sentence, declared identically at both ends — R-5)**"*, whose cell reads *"F.W0's G-9 owns the SEAT … and F.W9's G-F9-1 owns the FLOOR … neither gate may be discharged by the other's evidence"* (**F-W0 §4 G-9**, the Disjointness paragraph — full text in the §2.8a QUOTE REGISTER). F.W0's mirror clause reproduces as **TWO spans, not one** — ⟨cmd⟩ `grep -o '\*\*Explicitly NOT green-by-coverage\*\* — F\.W0 must not pre-empt the floor.s scope\.' waves/F-W0.md` → *"**Explicitly NOT green-by-coverage** — F.W0 must not pre-empt the floor's scope."* — and the earlier sittings' **single fused quoted span** (bold carried across the em-dash, terminal period dropped) is **STRUCK as a composite quoted under one attribution** (R2-1-LAW.4; §2.8 **E-9**).

### §2.5 The deploy spine, containerization, and the evidence floor

| row | re-home + verb | sequencing |
|---|---|---|
| **M.W2** — readiness ≠ liveness (BLOCKER-weight) | Re-home into F.W9 and execute: separate readiness from liveness in the health gate. **Arm A, bump-independent** — does not wait on F.W1 | Re-homing **disposes of the M board row explicitly** — no double-booking between `docs/tranches/M/PROGRESS.md` and this spec. **OG-F1** (owner ruling owed: re-root fourier source auditing at F.W0, or freeze the R-coordinates) gates whether the M coordinates may be cited at all. Every fourier-tree write is asked, never made |
| **M.W3** — fail-closed inv-28 (BLOCKER-weight) | Make the API deploy fail-closed on a same-SHA green CI run, mirroring `deploy-pages.yml`'s conclusion/branch/event guard | Arm A. **Must not be read as an API-shape gate — the API contract is SS-4's (F.W5–W8).** ASK-3 (palette-api deploy; the rsync deploy-dir blocking `deploy-hook.sh` adoption) is **F.W10's correspondence**, not F.W9's infra execution |
| **M.W4** — kill the silent rollback + inv-31 (BLOCKER-weight, **the chronic**) | Re-home: a failed deploy must be **loud and blocking**, and the inv-31 observability floor stands up | **F.W0 substrate first.** FR-GV-36's "console e2e passes against a dead API" fact belongs to this reckoning; **the API repair is NOT booked here**. See §2.8 CORRECTION C-1 — the residue is narrower and sharper than the banked prose |
| **M.W11** — evidence (BLOCKER-weight, the evidence law) | Re-home the **evidence half**: a green CI run id covering each claimed-green gate, plus CWV/INP and axe artifacts, each citing a committed SHA | **CWV ORDER-LOCK (FR-AH-1)**, hard. **R3-9 binds**: an assertion of non-execution is recorded, never cited as a method guarantee — every green cites a run id |
| **Containerization fidelity audit** (census-assigned scope; no registry row, MAJOR-weight) | Audit fidelity across `web/Dockerfile`, `api/Dockerfile`, `docker-compose.yml`, `docker-compose.prod.yml`, `nginx/fourier.conf` and the host-Apache templates — prod-overlay hardening verified **as-running, not as-declared** | The image **cannot build from `web/` alone**: `web/Dockerfile:24` copies `assets/ → public/assets/` and `:26-29` copies six `paper/fourier_paper.{tex,bib,aux,log,toc,bbl}` files into `/paper/` because `vite-plugin-latex-paper` compiles the TeX at build — the context must be the repo root. **Two nginx authorities coexist unreconciled**: the printf-inlined conf at `web/Dockerfile:33-43` and the 72-line repo-level `nginx/fourier.conf`. In-file prior art (the fidelity audit's own falsifier class): the `163ca47` deps migration left a stale `COPY web/vendor ./vendor` that "broke every Docker build" — `web/Dockerfile:6-10` is now the comment recording its removal (⟨cmd⟩ `sed -n '1,12p' web/Dockerfile` — `:5` is `COPY web/package.json web/package-lock.json ./`; the comment block runs `:6-10`; the round-1 `:5-9` was off by one, §2.8 **E-5**). **CI checkout deliberately omits submodules (`docs/precepts` is private) — any gate assuming precepts are readable in CI is wrong** |

### §2.6 Carried for parity — **NOT booked by F.W9**

| row | where it is booked | why it is carried here |
|---|---|---|
| **`(fr-PaperArticleWindow, PAW-49)`** — **the ⟨re-cut → F.W0⟩ is STRUCK at round 4** | **F.W3** <sub>legs F.W9/W10</sub>, per the canonical (§2.0a) — **held at F-W3** | **The one coverage-family row whose gate is FREE** — `npm run build` already runs `vue-tsc -b`; no new harness. **F.W9 does not book it, and no longer purports to re-home it**: the `noUncheckedIndexedAccess` flag stays F.W0's **substrate act** (§1d/§5), which is a config edit and not an identity re-home. Flag-before-split still governs PAW-12 |
| **`(fr-PaperArticleWindow, PAW-50)`'s DECISION leg** — **the ⟨re-cut → F.W4⟩ is STRUCK at round 4** | the **id is booked whole at F.W9** (§2.0 row 15 / §2.4); the **authorizing clause** remains F.W4's — `$M/CENSUS-2026-08-03.md` **§4 item 5** (the `§4.5` spelling is struck at §2.8 **E-9**) | A decision clause is an **authorization**, not a home — the round-1..3 conflation of the two is what produced a re-cut the census does not carry. **F.W9 may still not stand a harness the F.W4 clause has not authorized** (§4a-4), and **F.W10 cannot record a coverage row terminal while that clause is unread** |
| **`(fr-Tooltip, FR-TT-1)`** — **the ⟨re-cut → F.W10⟩ is STRUCK at round 4** | **F.W4** <sub>legs F.W3, F.W9/W10</sub>, per the canonical (§2.0a) — **held at F-W4**; neither twin books it | **PROMOTION LOCK, binding on F.W9 exactly as before: "BLOCKER at any F.W9/W10 axe close-gate."** The close axe run covers `/paper` + `/equation` + the admin tab with zero serious/critical, **OR** the 17 naming legs are recorded as a named, dated waiver — **it may not be argued green while the legs are open**. L-4's 17-count is the citable census; D-1's 16 and its `:42/:49` anchors are KILLED (register #8) |
| **FR-AUL-16 / FR-AH-45 terminal homes** | F.W10 (the last F wave) | F.W9 proposes the execution limbs (P-12); F.W10 decides terminal. A decline must be recorded with rationale |
| **C-19 ⊕ PAW-33 pin truth** | F.W1 + the relay | Cited so the containerization/pin surfaces do not re-book it: ONE fact, two banked ids, both cited, neither re-booked |

### §2.7 Method law — **R3-9** (F.W0-routed; standing rule for every evidence cell here)

An unverifiable non-execution assertion was carried through the Codex lineage as if it were a method guarantee. **Verb: record, never cite as a method guarantee.** For F.W9 this is *the evidence law*: **no gate closes on an assertion that something did not run; every green cites a covering run id (inv-27, G-F9-17).** It joins KF.W4's transferable gate-design law (a)–(d) plus **a gate may not re-derive its own oracle** — quoted in this file's binding-law block and enforced at §2.1 (the 26-identity taxonomy is consumed from F-W10, never recomputed) and at G-F9-3 (the falsifier **extends** the existing keystone rather than authoring a rival oracle). The row itself is F.W0's — carried, not booked.

### §2.8 Measured corrections and errata (E-3 — recorded beside; dated bytes never rewritten)

| # | finding (re-measured 2026-08-28, fourier HEAD `cd26c65`) | disposition |
|---|---|---|
| **E-0** (deixis, disclosure only) | The shared §2.2 bytes read from F.W10's seat in three cells: FR-TT-1's "governs **this wave's** close-gate design (G-F10-10)" and FR-COB-20's "**this wave's** whole claim" denote **F.W10**, and PAW-33's "(§2.6)" resolves against **F-W10.md's numbering** (its C-19 ⊕ PAW-33 pin-truth row — carried here at §2.6 for parity). **A FOURTH cell joins the class at round 3**: the re-scoped `fr-PaperView` D/i-1 cell (§2.8 **E-10(b)**) reads *"this spec's sequencing lock (§4a.11)"*, and **`§4a.11` is F.W10's numbering — in THIS spec the D/i-1 coupled lock is §4a item 10** (⟨cmd⟩ `grep -o '^10\. \*\*D/i-1 COUPLED LOCK\*\*' F-W9.md` beside ⟨cmd⟩ `grep -o '^11\. \*\*D/i-1 COUPLING\*\*' waves/F-W10.md` — the same lock, two numberings) | Read the shared bytes from the F.W10 seat. In THIS spec, FR-TT-1's F.W9-side share is **G-F9-22** and PAW-33 is excluded (§5). **No byte in the region is edited to fix deixis** — G-F9-21 forbids a unilateral touch |
| **E-1** (erratum, **PAIRED**; **the denominator is QUOTED from F.W0's G-12 table, not re-derived here** — R-9.1) | F-W10 §2.2's **AA-44** cell reads *"9 e2e never enter the audit tab"* while the **FR-AFP-42/-49 cell two rows below** states **"Quote 8 specs, never 9 (K11)"** — the table contradicts itself. **The settling authority is F.W0's G-12 — cited by gate id: `F-W0 §4 G-12`, the RED-witness e2e row** (R2-2), reproduced here by command and by command only (R2-1-LAW.1) — ⟨cmd⟩ `grep -o '\*\*8 `e2e/\*\.spec\.ts`\*\* (banked ×3[^)]*)' waves/F-W0.md`: *"**8 `e2e/*.spec.ts`** (banked ×3 — `fr-AdminFlaggedPanel:110` · `fr-CollapsibleSection:65` · `fr-CanvasOverlayButton:74` — and live-measured at the fold seat 2026-08-28; **any 7-spec figure is superseded and forbidden**)"*, with the positive statement at **F-W0 §1d item 2** (⟨cmd⟩ `grep -o 'The e2e denominator is 8\.\*\* Stated positively' waves/F-W0.md` → *"The e2e denominator is 8.** Stated positively"*). **The round-1 cell's quotation — a "retraction" sentence attributed to G-12's own bytes — is STRUCK as FABRICATED** (R2-1c; ⟨cmd⟩ `grep -o "RETRACTED at the fold seat" waves/F-W0.md` → **∅ (exit 1)** — absence, not a count of a live sibling, re-formed at round 3 under **R3-3.10**: F.W0's own repair struck that framing as resting on a ledger that does not exist), and the pathless *"the CARRY"* vocabulary dies with it — the R-3.3 breach closes in the same edit (§2.8 **E-4**; R2-10 item 6). The **substance is unchanged**: the figure is **8** and G-12 owns it. F-W9 **cites that row**; the read-only 2026-08-28 corroboration (contour-extraction · gallery · paper-performance · settings-persistence · visual-baseline · visualization-crud · visualization-ux · workspace-flow) is **corroboration of a pinned figure, never a rival derivation** (KF.W4(d), and *a gate may not re-derive its own oracle*) | The correction is a **PAIRED edit landing in BOTH specs in one commit** — a unilateral fix breaks G-F9-21 / G-F10-6's Δ=∅. Until it lands, both specs carry the byte-identical table **including the defect**, and this row is the disclosure. **No prose in this file quotes 9 OUTSIDE the shared §2.2 bytes** — **round 4's re-scope** (PASS-4 **D3**; R4-7.3). The round-1 spelling was the unqualified absolute *"No prose in this file quotes 9"*, which **this very cell falsifies two sentences earlier** by conceding that both specs carry the table *"including the defect"*: `fr-AdminAuditLog`'s shared AA-44 cell states *"9 e2e never enter the audit tab"* **in the table's own voice, unquoted**, and it is precisely the byte a unilateral strike may not touch. The masthead's parallel absolute was re-scoped at round 3 after E-10(a) convicted it; **this one was left in its round-1 spelling through three repair rounds and a purge sitting — the same shape, the same file, one cured and one not.** Witnessed in the only form R3-3.10 permits over this file — **word output, never a count**: ⟨cmd⟩ `grep -o '9 e2e never enter the audit tab' F-W9.md \| sort -u` → **one distinct span**, at **three** homes: `:178` (the shared §2.2 bytes, licensed) · `:282` (this row's own cell) · `:452` (**P-1's cell on G-F9-21's face, minted at round 4**) ⟨*re-cut at repair round 5, REST-63 (P5-17): the enumeration accounted for two of three and **predates P-1's cell by one round**; the third is added. Same shape as REST-53 and REST-59 — axis right, enumeration short.*⟩. Superseded figures are FORBIDDEN downstream by G-12's own clause |
| **E-2** (**PAIRED**; opened at round 1, **re-stated at round 2 against the twin's live bytes** — R2-6a) | The 8-item **F.W1 visual-regression checkpoint set** (`carry/F-W1-CARRY.md`, **§CrossEdges item 9**) was carried by **neither twin** at the fold; **R-2b** homed it at F.W9 (author + execution) with F.W10 terminal. Round 1 disclosed the divergence as one-directional (the twin merely *lacked* the bytes); round 2's first sitting corrected that to two-directional (the twin had authored a **rival prose paragraph** over the same carry). **Both descriptions are now spent: THE SPLICE HAS LANDED AT BOTH ENDS.** Measured this seat, read-only, and **by STRUCTURAL receipts only** — the twin is a **live file under its own concurrent repair seat** (its mtime moved between two probes of this very session), so a count over it is not a witness and none is cited here: ⟨cmd⟩ `diff <(awk '/^\*\*THE F.W1 VISUAL-REGRESSION/,/^\*\*Born-RED witness/' F-W9.md) <(awk '/^\*\*THE F.W1 VISUAL-REGRESSION/,/^\*\*Born-RED witness/' F-W10.md)` → **empty** (the checkpoint block is byte-identical) · ⟨cmd⟩ `diff <(awk '/^\| record \| identity/,/^\*\*By-mechanism entrants/' F-W9.md) <(awk '/^\| record \| identity/,/^\*\*By-mechanism entrants/' F-W10.md)` → **empty** (the 26-identity table likewise) · and the twin declares the receipt in its own voice: ⟨cmd⟩ `grep -o 'AUTHORED BY F\.W9 and RECEIVED HERE BY SPLICE\*\* — R2-6a.s ruled direction, performed at repair round 2' waves/F-W10.md` → *"AUTHORED BY F.W9 and RECEIVED HERE BY SPLICE** — R2-6a's ruled direction, performed at repair round 2"*. The rival prose block no longer stands as a rival: **Δ=∅ is the receipt, not a grep count** | **R2-6a's direction is the direction R-2b always had, and it is now PERFORMED: F.W9 AUTHORED these bytes; F.W10 deleted its rival paragraph and RECEIVED them by splice.** **The checkpoint asymmetry is CLOSED and no longer reddens G-F9-21** — that gate's remaining paired obligation was stated here as **E-1 alone** (the 9-vs-8 erratum still standing in the shared table at both ends) — ***a figure this cell was never re-read against, and it went stale twice: E-9(b) made it two at round 2's third sitting and E-10(b) made it three at round 3. STRUCK in favour of the gate's own face, which is the single place the obligations are counted*** (**G-F9-21**: E-1 and E-9(b) outstanding, E-10(b) closed at the bytes by splice). **The lesson is the round's own, one level down**: a disclosure that names a *number of open items* in a file that keeps finding items is a self-staling receipt — the same shape as a banked count over a moving sibling (R3-3.10), and it is why the face, not the erratum, is authoritative. **F.W9 still does not author F.W10** and touched none of its bytes: this seat measured the twin read-only and F.W10's own seat performed the receipt. **Corollary, discharged**: the E-5/E-6 corrections were the bytes the twin received — ⟨cmd⟩ above returns **empty**, so no round-1 figure was re-introduced to make a `diff` close |
| **C-1** (narrowing; **re-grounded on the live tree** per R-3 — the correction is *toward* `scripts/deploy-hook.sh` as it reads at `cd26c65`, not against any carry) | `scripts/deploy-hook.sh` at HEAD (last touched `e9faab6`, G.W7 ε.2) **already carries a real bounded ~60 s health gate** (`health_gate()`, `${HTTP_PORT:-8100}`, polls `/api/health` **and** inv-22 `/`→404), a last-known-green marker, rebuild-on-rollback, and an explicit `ALERT` on double failure. The banked mechanism "*builds, fails the 60 s health-gate, rolls back, exits non-zero into a log nobody watches*" is therefore **half-cured in the script and wholly uncured in the channel** | M.W2/M.W4's residue is **sharper, not smaller**: the rollback is not silent *in the script*, it is silent *in the channel* — **there is no observability floor (inv-31); the loud ALERT lands in an unwatched log.** G-F9-15's falsifier is re-cut accordingly. M.W2's readiness/liveness split remains genuinely open (the gate conflates backend liveness with route readiness) |
| **C-2** (confirmation) | inv-28 asymmetry **CONFIRMED by measurement**: `grep -n 'conclusion\|workflow_run\|gh run\|inv-28' scripts/deploy-hook.sh` → **zero hits**, while `deploy-pages.yml:48-57` carries the named `inv-28 gate` with the three-clause `if:` (`conclusion == 'success'` ∧ `head_branch == 'master'` ∧ `event == 'push'`) — re-run this seat, both halves | **M.W3 stands unmodified.** (**Row shape corrected at round 2's third sitting**: this row carried only TWO cells in a three-column table since round 1, so its disposition rendered as an empty column — §2.8 **E-9**. No figure and no verb changed; the finding and the disposition are now in their own cells, which is the only way a reader can tell a measurement from what the wave does about it) |
| **C-3** (re-scope, hardening) | `docker-compose.prod.yml` **already declares** the full hardening set as authored: api `read_only` + `tmpfs` + `cap_drop` + `no-new-privileges` + 2G; web `read_only` + three tmpfs paths + `cap_drop`/`NET_BIND_SERVICE` + 256M; mongo `--tlsMode requireTLS` with a documented honesty pivot on `--tlsAllowConnectionsWithoutCertificates`, and `read_only`/`cap_drop` recorded as **G.W7 BOOKED residuals** for the stateful DB. `nginx/fourier.conf` carries **2 rate zones** (`api_general` 30r/s `:16`, `api_compute` 2r/s `:17`) and **5 security headers** (`:25-29`) | The gap is **not** as-shipped-vs-as-documented; it is **as-declared-vs-as-running**. G-F9-19's falsifier is re-cut: verify against the **running container** (`docker inspect` on the deployed stack), not against the compose bytes — which are already the documentation the audit would otherwise re-read |
| **C-4** — **INPUT TO / QUOTATION OF F.W0's G-11 anchor table** (R-9.1), **never a rival act**. **F-W0 §4 G-11** (cited by gate id — R2-2; ⟨cmd⟩ `grep -o 'ONE corrected anchor table published; every later wave quotes it' waves/F-W0.md`) publishes ONE corrected anchor table in `fourier/docs/tranches/F/SUBSTRATE-LEDGER.md` (**create**-marked at **F-W0 §2 Bounds, the `SUBSTRATE-LEDGER.md` row** — ⟨cmd⟩ ``grep -o 'SUBSTRATE-LEDGER\.md` \| \*\*create\*\*' waves/F-W0.md`` → *``SUBSTRATE-LEDGER.md` \| **create**``* (**the `\|` is markdown table-cell escaping, not part of the command — the shell string carries a bare pipe**) (**the round-2 second sitting wrote this ⟨cmd⟩ inside a SINGLE-backtick span it contains a backtick of its own — the span broke and the command could not be read as written; re-spanned here, re-run this seat, §2.8 E-9**); cited-WHEN-created, the obligation prospective until then) and binds *"every later wave quotes it"*. **Divergence between any anchor below and that table is a defect against G-11, not a competing measurement.** The FR-CP-45 law (re-resolve every anchor before quoting into a wave spec) is how these were produced; **G-11 owns them once published** | `web/Dockerfile` is **43 lines**: `COPY assets/` at **:24** (superseded: :20-25), the six-file `COPY paper/…` at **:26-29**, `FROM nginx:alpine AS production` at **:31**, the printf block at **:33-43** (superseded: :31-42). `/equation` slug sits at `visual-baseline.spec.ts:34` (superseded: :35). `playwright.config.ts` projects array at **:46-51**, one `chromium` project (superseded: :48) | Corrected anchors used throughout §1b/§3 **as G-11 quotations**; the superseded anchors are recorded beside (E-3) so no seat quotes a stale byte. **Mechanism unchanged in every case** — the anchors drifted, the facts did not. **On publication of G-11's table this row becomes a citation of it and re-measures nothing** |
| **C-6** (anchor correction, **same G-11 quotation status as C-4**) | `.github/workflows/ci.yml`'s third job is **`e2e-tests:` at `:100`**, not ``e2e`` at `:101` — re-measured read-only 2026-08-28 (`grep -n '^  [A-Za-z0-9_-]\+:$' .github/workflows/ci.yml` → `37: api-tests:` · `77: web-build:` · `100: e2e-tests:`). The superseded spelling is retired | Corrected at §1b. A job **name** is a load-bearing selector for inv-27's run-id emission and for F.W0 G-9's *"wired into `ci.yml`"* clause — a mis-named job would have made both un-authorable. **No mechanism changes** |
| **C-7** — **ANCHOR IDIOM ADOPTED (R2-2), the round-2 class cure** | The round-1 repair cited F.W0 by line **fourteen times** and every one of the four coordinates had staled under that same round's own edits. Measured this seat: ⟨cmd⟩ `for n in 189 199 204 63; do sed -n "${n}p" waves/F-W0.md; done` → `:189` **blank** · `:199` **blank** · `:204` = *"**Owning rows**: 18, 17, 19."* (**G-9's** owning-rows line, not G-12) · `:63` = the `fourier/CLAUDE.md` **create** row, not the ledger's. **Cure by idiom, not per coordinate**: every cross-spec citation in this file now names a **STABLE anchor** — `F-W0 §4 G-9 / G-11 / G-12`, `F-W0 §1d item 2`, `F-W0 §2 Bounds, the SUBSTRATE-LEDGER row`, `F-W1 §4 **Intra-wave order (binding)** step 4`, `F-W1 §4 Cross-edges, the F.W0 → F.W1 row`, `carry/F-W1-CARRY.md §CrossEdges item 9`, `carry/F-W4-CARRY.md §CrossEdges, the F.W9/W10 row` | **Not one line number into a live sibling survives in this file** — re-run at the resume with a WIDER probe than the first sitting's (it swept only four filename stems and, worse, was **certified without being run**: six cites survived it — §2.8 **E-8**): ⟨cmd⟩ `grep -noE '(waves/)?F-W[0-9]+\.md:[0-9]+\|CARRY\.md:[0-9]+\|KF-W[0-9]+\.md:[0-9]+\|COHESION\.md:[0-9]+' F-W9.md` → **∅** (any-wave, any-carry, any sibling-tranche form, not just the four stems). Line numbers remain ONLY where R2-2 item 2 permits them: the **frozen corpus** (`fr-*.md`), the **fourier tree at the pinned HEAD `cd26c65`**, and this file's own intra-file coordinates. **New citations are born in the stable idiom** (R2-2.4). The mechanism did not change in a single case — the addresses did, which is exactly why the class was invisible |
| **E-4** — **the F.W1 roster extent, re-quoted by command (R2-5)** | Round 1 wrote *"the ELEVEN-limb roster"*. **R2-5 re-derives the charter's own separators and rules the extent TWELVE** — the eleven chartered **plus the FR-EQC-7 vaul-vue manifest gate returned INSIDE at repair round 1**, which is a limb, not a rider. **THE TWELFTH LIMB HAS LANDED — re-run at the resume, and the round-2 first sitting's receipt for this cell is STRUCK TWICE OVER**: (i) its stated output (*"The roster is ELEVEN"*) is refuted at the bytes, and (ii) **the command it pasted returns nothing at all** under this environment's BSD `grep` (`\?` is a GNU-only BRE quantifier) — ⟨cmd⟩ `grep -o 'The roster is \*\*\?[A-Z]*' waves/F-W1.md` → **∅, exit 1**. The working command, run this seat: ⟨cmd⟩ `grep -o 'The roster is [*]*[A-Z]* limbs and stays [a-z]*' waves/F-W1.md` → *"The roster is TWELVE limbs and stays twelve"*. This file states the extent as **TWELVE** and cites it **by anchor** (`F-W1 §4 **Intra-wave order (binding)** step 4`), never by copying a count out of a ruling as if it were the bank | **F.W9 re-derives nothing** — the charter's own edit has landed and this spec's citation was already correct; **no sibling edit is owed and none was made.** **R-4b is unchanged**: the transaction is cited whole and restated nowhere. **Same-row discharge (R2-10 item 6 / R-3.3)**: the pathless *"the CARRY"* is **used as a citation ZERO times** in this file. Witnessed by a **bracket-class probe whose own ⟨cmd⟩ string is NOT an occurrence** — the sole probe shape that can measure a prohibited vocabulary without joining it: ⟨cmd⟩ `grep -n 'the CARR[Y]' F-W9.md`. Its hits fall **only** in the masthead's R2-1c note, **this errata table**, and **§5's prohibition** — i.e. every occurrence is a **MENTION of the forbidden vocabulary and not one is a use**. **No count is stated, here or anywhere**: the figure moves with every cell that names the prohibition, and a number that changes when it is written is no witness (**E-8** law (i)). **Both earlier count words are STRUCK** — the first sitting's *"exactly once"* (never run) and the second sitting's *"5 lines"/"4"* pair, which disagreed with each other precisely because each was a self-counting probe reporting a file the act of reporting had already changed (§2.8 **E-9**). The E-1 breach itself died with the fabricated quote it lived inside |
| **E-5** — **three false or drifted witnesses in cells stamped `[re-measured]`, struck** | (a) checkpoint item 1's *"`--shadow-cartoon` → **2 files**"* **did not reproduce**: ⟨cmd⟩ `grep -rl -- '--shadow-cartoon' web/src/` → **1 file / 3 hits**. (b) the `page.screenshot()` call is at **`:55-59`**, not `:56-60`. (c) the Dockerfile's removal comment is at **`:6-10`**, not `:5-9` (`:5` is the `COPY web/package.json …` line) | All three corrected at every site. **(a) is the grave one and is named as such**: a *stated* read-only re-measurement that does not re-run is the exact false-witness class this wave exists to abolish (*a green must be cited, never asserted*), and it convicted the round-1 repair from inside its own checkpoint table. **No gate's RED state moves** — the instruments are still absent; only the witnesses are now true |
| **E-6** — **a G-12 denominator quoted under a RENAMED pattern, struck** | Round 1 classified *"**21 cartoon-surface sites / 14 files**"* as a G-12 quotation. G-12's published token is **`cartoon-card`** (⟨cmd⟩ receipt at §2.2 item 2 and in the provenance table). At the tree the two are not the same set: `cartoon-surface` = **1 file** (applied once, inside the `.cartoon-card` shim) vs `cartoon-card` = **15 files** | Restored to **`cartoon-card`** in the provenance table and at the checkpoint item. **The carry's banked wording is untouched** — anti-rename cuts both ways: a record's words stay its words, and a published denominator keeps its own token. Quoting a G-12 figure under a substituted pattern name is what G-12's superseded-figures clause and KF.W4(d) forbid |
| **E-7** — **the splice claim over-reached by one line** | Round 1's §2.2 head declared *"the preamble, table and entrant block … carried **verbatim** … spliced, never retyped."* The table and entrant block are byte-identical — ⟨cmd⟩ `diff <(awk '/^\| record \| identity/,/^\*\*By-mechanism entrants/' F-W9.md) <(awk '/^\| record \| identity/,/^\*\*By-mechanism entrants/' F-W10.md)` → **empty**, this seat; **the head sentence itself is not and never was** — ⟨cmd⟩ `diff <(grep -h '^\*Heading mirrored' F-W9.md) <(grep -h '^\*Heading mirrored' F-W10.md)` → **DIFFER**, two seat-local sentences pointing at different errata. **The first sitting's probe for this cell (`grep -h 'Count and deixis' …`) is retired: it matched THIS erratum row as well as the head, so the command's own output contained the cell describing it** — a self-matching probe is not a witness (E-8) | The head now states the splice's **exact scope** (table + entrant block), names itself as seat-local, and names the **R-2b checkpoint block as F.W9-AUTHORED and F.W10-RECEIVED** (R2-6a, performed). A "verbatim" label that covers bytes no command produced is a defect per se (R2-1-LAW.2) — including when the over-reach is one sentence wide |
| **E-8** — **THE ROUND-2 REPAIR'S OWN RECEIPTS, RE-RUN AT THE RESUME; four struck** (round 2 landed in two sittings — the first was wall-killed mid-write and its partial bytes were banked; this seat resumed and re-ran **every** ⟨cmd⟩ in the file rather than inheriting one) | Four of the first sitting's receipts did not reproduce, and **all four were of the class this round exists to abolish — a stated measurement that was never run**: (a) **C-7's anchor probe certified `→ 0` while SIX line cites into live siblings survived** — two into F-W0/F-W1 at §4b, three into the two carries at §4b/§5, one in the close act; the probe was also stem-narrow (it could not see `F-W3.md:` or `KF-W1.md:` forms at all). (b) **E-4's roster probe returns nothing at all under BSD `grep`** (`\?` is a GNU-only BRE quantifier) yet was written up as returning *"The roster is ELEVEN"*; the bytes now read TWELVE. (c) **E-4's *"the CARRY appears exactly once"* count word was never run.** *(This sitting's own replacement figure — a `grep -c` reported as "4" here while E-4's cell reported "5 lines" — is **itself struck at E-9**: the two disagreed because both were self-counting probes, and E-4 now carries a bracket-class probe and states no number at all. **A count word retired for being unrun must not be replaced by another count word of the same shape** — that is the lesson this row failed to apply to itself.)* (d) **E-2's two-directional-asymmetry finding is spent**: F.W10 has since deleted its rival paragraph and received the splice. **E-7's probe is additionally retired as self-matching** (it matched the erratum row that quoted it). **Two probe-design laws are minuted out of this, binding on every later seat of this file**: (i) **a self-counting probe is no witness** — if the ⟨cmd⟩ string is itself matched by the ⟨cmd⟩, the figure moves when the cell is written (retired at E-4 and E-7); (ii) **no count over a live sibling is a witness** — `F-W10.md`'s mtime moved between two probes of this single session (its own repair seat is concurrent), so E-2 and G-F9-23 cite **structural `diff`s and the twin's own declared clause**, never a `grep -c` over it | All four corrected **at every site**, each now carrying a ⟨cmd⟩ this seat ran on 2026-08-28 read-only: C-7 (wider probe, **∅**) · E-4 (working probe, **TWELVE**; count word **4/all-mentions**) · E-2 (**splice landed**, `diff` empty) · E-7 (non-self-matching `^\*Heading mirrored` probe). **No gate's RED state moves and no booked id, lock or disposition changes** — the substance of every cell survived its receipt; only the receipts were false. **The lesson is entered against this wave's own close act**: a repair seat that certifies its own receipts without re-running them reproduces, one level up, the exact failure the wave gates against — *a green must be cited, never asserted*, and a **repair** is not exempt |
| **E-9** — **ROUND 2, THIRD SITTING: TWO PHANTOM SECTION ANCHORS, A FUSED QUOTATION, THREE SPENT RECIPROCITY CLAIMS, AND A COUNT WORD REPLACED BY A COUNT WORD** (this seat re-ran **every** ⟨cmd⟩ in the file — none inherited — and swept every §-anchor into an external authority for existence, which no earlier sitting did) | **(a) `lane-docs §6b` DOES NOT EXIST** — ⟨cmd⟩ `grep -o '6b' formation/fourier/lane-docs.md` → **∅ (exit 1)** (absence form, re-cut at round 3 under **R3-3.10**; the round-2 `grep -c … → 0` was a count over a live sibling). `lane-docs.md` §6 carries lettered *paragraphs* (a)/(b), never `§6a`/`§6b` headings. The substance is real and sits at **§6 arm (b)** (⟨cmd⟩ `` grep -o '\*\*(b) API → self-hosted Docker at `babb.dev`\.\*\*' formation/fourier/lane-docs.md ``); the anchor was invented. **This is the R2-1a class — a phantom section under a true fact — surviving two repair rounds inside G-F9-16.** **(b) `census §4.5` DOES NOT EXIST** — ⟨cmd⟩ `grep -o '4\.5' formation/fourier/CENSUS-2026-08-03.md` → **∅ (exit 1)** (absence form, **R3-3.10**); §4 is a flat list, and the vitest-floor decision is **§4 item 5**'s clause *"a unit-test floor (vitest) decision"*. **F.W0 had already ruled this correction at its own end at repair round 1** and this file kept the struck spelling in **six** places — four seat-local (cured here) and **two inside the shared §2.2 bytes**, which a unilateral edit may not touch (see disposition). **(c) the F.W0 mirror clause was quoted as ONE fused span** — the bold ended at `coverage`, the em-dash clause is unbolded prose, and the terminal period was dropped; re-quoted byte-exact by ⟨cmd⟩ at §2.4 and §4b, **labelled two spans** (R2-1-LAW.4). **(d) THREE reciprocity claims were spent**: F.W0's reciprocal is written **twice** (F-W0 §4 G-9's Disjointness paragraph + §6b's `F.W9 / F.W10` row), F.W1's at **§4 Cross-edges item 9**, F.W4's at **§5.2 Cross-edges'** `→ F.W9/W10` row — this file went on asserting all three were owed. **Only F.W3's is genuinely owed**, and it now says so. **(e) E-8 retired a count word and installed another of the same shape** — its *"the true count is 4"* and E-4's *"5 lines"* disagreed with each other because both were self-counting probes; E-4 now carries a **bracket-class probe (`the CARR[Y]`) whose own ⟨cmd⟩ string is not an occurrence** and states **no number**. **(f)** C-4's SUBSTRATE-LEDGER ⟨cmd⟩ sat in a single-backtick span containing a backtick — the span broke and the command was unreadable as written; re-spanned. **(g)** `J-diff-shape.md §6` is **path-qualified** to the fourier tree (it is not resolvable from this repo), and the two `lane-frontend` line cites in this spec's own voice (`§1 :[4]8`, `:568` — **both now rendered in E-4's BRACKET CLASS at round 4**, because the plain `§1 :[4]8` spelling survived here and at G-F9-1 as a live occurrence of the very form the strike declares extinct ⟨*bracketed at repair round 5, REST-62 (P5-10): **the clause that ANNOUNCED the bracketing re-spelled the token plain**, minting a new unbracketed occurrence inside the cell declaring the form extinct*⟩: §2.8 **E-11(b)**) are converted to row labels — the `:568` form survives **only** inside FR-GIG-15's banked words, where it is the record's and stays the record's (anti-rename). ***(g) IS FALSE AS WRITTEN AND IS STRUCK — §2.8 **E-10(a)**, round 3: a FOURTH `:568` survived at §5's exclusion table, in the wave's own voice, spelled `lane-frontend :[5]68` — **rendered here and at every other mention in the BRACKET CLASS, E-4's idiom, so that striking the form does not re-create it** (four mentions live in this file — the masthead, E-9(g)'s strike, this cell and §5's row — and under the bracket spelling **not one of them is an occurrence**, which is the only way the residue probe below can return a true ∅ rather than a ∅-modulo-prose) — the real form carried no `.md` and a space before the colon, and the probe this certification rested on matches `file.md:N` and cannot see it in principle. The sitting that wrote (g) had itself convicted the first sitting for a stem-narrow probe (E-8(a)), widened the stems, and certified on the unchanged FORM. The cure is a form-agnostic probe, pasted at E-10(a); this cell's finding about the OTHER three occurrences stands unchanged.)*** | **Nothing booked, no gate's RED state moves, no id or lock changes.** Four seat-local `§4.5` sites cured; **the two inside the shared §2.2 bytes (the `fr-App C-13` and `PAW-50` cells) are a NEW PAIRED OBLIGATION** — disclosed here, landing in BOTH specs in one commit, exactly as **E-1** does, because G-F9-21's Δ=∅ forbids a unilateral fix. **G-F9-21's face is re-stated accordingly: the outstanding paired obligations are now E-1 AND E-9(b).** **The lesson, entered against the close act**: two repair rounds re-resolved every *line* they cited and never once asked whether a cited **§ exists** — an anchor idiom that abolishes line numbers does not, by itself, make a citation real, and *"cite by stable anchor"* becomes *"cite by unfalsifiable anchor"* the moment no seat greps for the heading |
| **E-10** — **ROUND 3: A CERTIFICATION WRITTEN OVER A PROBE THAT COULD NOT MATCH ITS OWN SUBJECT, A "VERBATIM" LABEL INSIDE THE PAIRED BYTES, AN INHERITED DENOMINATOR, A CURE HOMED NOWHERE, AND THREE RECEIPT-FORM DEFECTS** (`conformance/PASS-3/RULINGS-3.md` — R3-3.5 · R3-3.7 · R3-7.3 · R3-9.5 · R3-3.10, plus the PASS-3 local riders; every ⟨cmd⟩ below run read-only by this seat on 2026-08-28) | **(a) E-9(g)'s receipt was FALSE and is STRUCK (MAJOR, R3-3.5).** §5's exclusion table carried `lane-frontend :[5]68` **in the wave's own voice** — the fourth occurrence and the only unlicensed one — while E-9(g) certified that the form survived *"only inside FR-GIG-15's banked words"*. The certifying probe (C-7's) matches `file.md:N`; **the survivor is spelled with no `.md` and a space before the colon, so no widening of the STEM list could ever have found it** — three rounds widened the wrong axis. **Cure**: the cell converts to the row label it already uses at §4b, and the certification is re-run over a probe **widened in FORM but still keyed to a STEM ALTERNATION — round 4 re-scopes this clause to what the probe actually expresses** (PASS-4 **D4**; the round-3 wording claimed *"over every external authority this file names"*, and the alternation below cannot see `atomdiff.py:12-14`, the `version:LINE` shape `8.0.0:69-73`, `SUBSTRATE-LEDGER`, or the `fr-*` stems, all of which this file does name — the true residue is nonetheless ∅ by class, see **E-11(b)**) — `NAME[ .]:[0-9]+` over the **eight stems named in the alternation**, run from `$X/waves/` (§2.8a's fourth base, where a bare `F-W9.md` operand resolves): ⟨cmd⟩ `grep -oE '(F-W[0-9]+(-CARRY)?\|KF-W[0-9]+\|COHESION\|lane-(docs\|frontend\|crud)\|CENSUS[A-Za-z0-9-]*\|INTAKE[A-Za-z0-9-]*\|J-diff-shape\|PROGRESS)(\.md)? ?:[0-9]+' F-W9.md \| sort -u` → **THREE distinct forms — `F-W1.md:272` · `PROGRESS.md:15` · `lane-frontend.md:568`** ⟨**re-run and re-cut at repair round 5, REST-53 (P5-5, HIGH).** Round 4 published *"exactly two"* and certified the residue probe *"∅ (exit 1)"*; **the residue probe exits 0 and prints `F-W1.md:272`** — **a live line number into a LIVE sibling, in this spec's own voice, at two sites (`:297` and `:460`), BOTH minted by round 4's own repair, in the two cells that announce the anchor cure.** The anchor is re-cut to its stable form: *while `F-W1.md` §4's **Intra-wave order (binding)** head reads `**Intra-wave order (binding):**`* — **no numeral** — and **the residue probe is re-run AFTER this edit, not before.** ⊘ *`:272` happens to still resolve — luck, not method,* and F-W1 has moved since the pin. **Fifth consecutive round in which a certification of a CLASS is published over an enumeration short by one, and the first in which the certifying round supplied the counter-example itself.**⟩ (the `\|` are markdown table-cell escapes; the shell string carries bare pipes). **Both are licensed classes and are named as such**: `lane-frontend.md:568` occurs only inside FR-GIG-15's **banked words** and the ⟨cmd⟩ that produces them (the record's coordinate, quoted never adopted — anti-rename), and `PROGRESS.md:15`(-28) addresses **the fourier tree at pinned HEAD `cd26c65`**, which R2-2 item 2 permits. The residue probe is therefore ⟨cmd⟩ *the same command* `\| sort -u \| grep -v -e 'lane-frontend\.md:568' -e 'PROGRESS\.md:15'` → **∅ (exit 1)**, and **only after that ∅ is the masthead's absolute re-stated** — now in its true scope: *no line number into a live sibling survives **in this spec's own voice***. **Standing correction to the probe law**: widen the SHAPE, not the stem list — a certification whose probe cannot express the defect class is not weak evidence, it is no evidence. **(b) The D/i-1 "verbatim" label over-reached, inside the SHARED bytes (R3-3.7).** §2.2's `fr-PaperView:120` cell labelled *"Coupled: rides the B-1/B-2 cure wave, never scheduled independently"* a verbatim lock; the bank reads only *"coverage rides the B-1/B-2 cure wave"* — ⟨cmd⟩ `grep -rc 'never scheduled independently' $R/fr-*.md` → **0 in all 66** (a count over the **frozen** corpus, which R3-3.10 permits). Roughly half the labelled span was spec-authored — **E-7's exact class, one round later and inside the paired region**. **Cure, and the direction it took**: the cell sits in the 26-identity table, which **F.W10 authors and F.W9 receives**; F.W10 published the re-scoped bytes and **this seat RECEIVED them by splice, authoring no rival cell** — ⟨cmd⟩ `diff <(awk '/^\| record \| identity/,/^\*\*By-mechanism entrants/' F-W9.md) <(awk '/^\| record \| identity/,/^\*\*By-mechanism entrants/' F-W10.md)` → **empty**. It was the third paired obligation and it is **closed at the bytes**, landing as one commit; **E-1 and E-9(b) remain outstanding and were deliberately NOT fixed** — the twin's seat was directed to this cell alone, and a fix the other end has not authored is rival bytes, not a cure. The received cell's `§4a.11` is F.W10-seat deixis, disclosed at **E-0**. **(c) §2's denominator was an unsourced count word (PASS-3 D3).** *"Thirty-six banked rows … 44 dispositions in all"* carried no decomposition and traced to the fold seat's intake packet — which **this file's own masthead declares NOT an in-tree authority, carrying NO gate weight** (R-3). Two things were wrong: the arithmetic was never stated, and *"banked"* was false of five of the thirty-six (four re-homed M-board rows ⊕ the containerization row whose own cell reads *"census-assigned scope; no registry row"*). **Cure**: §2 now states **26 ⊕ 5 ⊕ 5 = 36**, names which five are not banked, and prints **36 + 8 = 44** — every term enumerated at its own section, so the figure is falsified by reading the sections, not by trusting the sentence. **(d) N-2's cure was claimed by no wave (PASS-3 D4, R3-7.3).** G-F9-12's falsifier requires the DarkModeToggle label change to land, §1b and §2.3 call it a same-commit rider, §4a-12 said *"this one **is** F.W9's"* — and the string `F.W4` appeared beside N-2 nowhere, while §5's cure-exclusion table enumerated eleven F.W3/W4 cures and omitted it. The bank routes the cure to **F.W4** (⟨cmd⟩ at §4a-12 and G-F9-12). **Cure**: the attribution clause at §4a-12, the same at G-F9-12's falsifier, and a §5 exclusion row — F.W9 keeps the **locator edit and the sequencing** and books no label. This was the wave's own opening law (*F.W9 owns GATES, F.W9 owns no CURE*) failing on the single rider that had escaped it. **(e) The ⟨cmd⟩ set had no declared base, and four receipts were counts over live siblings (PASS-3 D5 · R3-9.5 · R3-3.10).** Three incompatible path roots ran through the inline notes with no base stated anywhere, so **no single cwd reproduced the file** — every command ran, but the receipts were weaker than the law they discharge. **Cure**: `$X` / `$V` / `$F` declared at §2.8a beside `$R`/`$M`, with the prefix rule that roots every note. Separately, **R3-3.10** (no banked ⟨cmd⟩ whose output is a count of a live sibling or of the citing file) retires **every** `grep -c … → 0` receipt over a live formation or wave file — they sat at **§2.4 · E-1 · E-9(a) · E-9(b) · G-F9-16**, five sites carrying three distinct probe strings between them (`4\.5` twice, `6b` twice, `RETRACTED at the fold seat` once), each named here so the retirement is checkable by site and not by a count word — into **absence probes** (`grep -o … → ∅, exit 1`) — same fact, a form that cannot go stale when the sibling moves. Counts over the **frozen** corpus and over the **pinned** fourier tree are untouched, and are the only counts this file banks. **(f) The FR-TT-1 census dropped the record's attribution word (PASS-3 D6).** Banked: *"L-4's table is the citable one (Editor ×10 · Canvas ×6 · FunctionInput Wand2 ×1 = 17)"*. Restored **at G-F9-22 by ⟨cmd⟩**; §2.6's row cites L-4's census without the parenthetical and needs nothing. **The §2.2 rendering is inside the SHARED bytes and is NOT touched** — it carries no quote marks, so R2-1-LAW convicts nothing, and a unilateral edit for a fidelity nick is exactly the trade the paired law forbids. **(g) §2.8a's section-A heading stamped a third `VERIFIED` (PASS-3 D7)** in a round whose subject is stamp discipline. Re-worded to **RE-RUN READ-ONLY**, and this cell's own word choice follows: `RE-VERIFIED` is retired from §4b's F.W3 row in favour of **RE-MEASURED**, so no adjective in this file shares the stamp's spelling. **Mention-vs-use, stated because a naive counter now reads high** (E-4's idiom): the token's only USES are two — the four-verb row (`VERIFIED **NO**`) and the close act (*"VERIFIED is F.W10's close to stamp, never this one's"*); every other occurrence is a **MENTION** naming the token in order to retire it (the masthead's round-3 clause, this cell, and §2.8a's re-worded heading). A count of the string is therefore not a measurement of the posture, and the posture is unchanged: **two uses, both reserving the stamp to F.W10.** **(h) §5 named two transaction limbs no local gate binds (PASS-3 D8).** R-4b licenses naming a limb *where a local gate binds one*; the checkpoint set binds five of the seven, not the lucide and Button legs. **Disclosed as named-not-bound rather than trimmed** — the row books nothing either way, and a limb silently dropped from an atomic transaction's roster is the worse of the two errors. | **Nothing booked moved. No gate's RED state moved. No id, lock, dissent or disposition changed** — round 3, like round 2, convicted receipts and labels, not findings. **The lesson entered against the close act**: the round's headline was a certification whose probe was structurally unable to test the claim it certified, which is the *"green by assertion"* failure this wave exists to abolish, wearing the provenance instrument itself |
| **E-11** — **ROUND 4, THE CENSUS FREEZE: A CHECK FILE STANDING AS A CLOSURE OPERAND, A ROSTER THAT WAS NEVER THE CENSUS'S, A SWEEP DECLARED COMPLETE AND SHORT BY TWO, AND A STRUCK LINE-CITE FORM THAT SURVIVED ITS OWN STRIKE TWICE** (`conformance/PASS-4/RULINGS-4.md` — R4-1.11 · R4-2 · R4-3.1 · R4-7.1/.2/.3 · R4-8.3 · R4-10 — plus the PASS-4 local defects D1–D7; every ⟨cmd⟩ below run read-only by this seat on 2026-08-29 under the bases assigned at §2.0) | **(a) THE CHECK-FILE OPERAND, STRUCK (MAJOR — PASS-4 D1 / R4-3.1).** The masthead named `conformance/PASS-1/F-W9-CHECK.md` as part of *"what the closure runs against"* — an **operand**, in the plainest form **R3-4** forbids, and R3-4 declared its FAIL-BY-CONSTRUCTION law **program-wide**, not as a five-item list. Three rounds walked past it because R3-4 was applied *by name* to five other denominators and the repair seat read the **directive index** rather than the **law** — the exact loophole **R3-8** had already ruled shut one ruling earlier (*"absence from a directive index lifts nothing"*). The aggravation is that the same sentence got the class right twice and wrong once: it refuses the fold seat's intake packet (*"not an in-tree authority"*) and refuses rulings files (*"cure-shapes, never quotable authority"*) and then exempts the one of the three that is literally a superseded seat's enumeration **of this very census**. **Cure**: the check file is named a **PRIOR RUN**; the closure runs against the **canonical roster (17)** over the 66 records. **(b) THE ROSTER RE-BASED ON THE CANONICAL, AND THE `:[4]8` SURVIVOR (R4-10 / R4-3.1).** Rounds 1–3 treated the 26-identity **band-token re-cut** as the census. It is not one: the canonical resolves each row's home by §0.1's first-arrow rule, so **ten** of those identities are homed at F.W3 / F.W4 / F.W0 / a packet with an F.W9/W10 **leg**, and **two canonical F.W9 rows — `(fr-PaperArticleWindow, R2-7)` and `(fr-PaperArticleWindow, PAW-50)` — were not booked here at all** (R2-7 read as a *"restatement"*, PAW-50 ⟨re-cut⟩ away). **§2.0 books all 17 and cites every leg to its holder; §2.0a/§2.0b carry the citations; three ⟨re-cut⟩ labels are struck** (PAW-49 · PAW-50's decision · FR-TT-1) because **R4-10 forbids a wave re-cutting a canonical home**, and the sequencing those re-cuts were really expressing (harness-before-rider · flag-before-split · the promotion lock) survives untouched as **locks**, which is what they always were. Separately, the round-3 `:[5]68` bracket-class strike had a **twin it never rendered**: the plain spelling `§1 :[4]8` survived as a live occurrence at **two** sites in this spec's own voice ⟨*bracketed at repair round 5, REST-62 — the second announcing clause, same defect*⟩ — E-9(g)'s own strike cell and G-F9-1's witness — found only by a **stem-agnostic** sweep (⟨cmd⟩ `grep -oE '[A-Za-z0-9][A-Za-z0-9_.-]* ?:[0-9]+(-[0-9]+)?' F-W9.md \| sort -u`, which returns coordinate shapes the round-3 alternation cannot express, including `atomdiff.py:12-14` and the digit-leading `8.0.0:69-73`). **Both are now in the bracket class**, and **E-10(a)'s scope sentence is re-stated over what its probe expresses** rather than over *"every external authority this file names"* (PASS-4 **D4**). Residue by class after the licensed filter: `fr-*` corpus coordinates · the pinned `$F` tree · this file's own §-coordinates · coordinates inside records' banked words (`:203–222` · `:124–129` · `lane-frontend.md:568`) · CSS/contrast values that are not coordinates at all (`2.39:1` · `--ui-scale:1` · `--control-floor:2`) — **the residue is ENUMERATED, not asserted ∅ (re-cut at repair round 5, REST-62).** The round-4 claim *"∅ unlicensed"* is **false on this axis and on REST-53's `F-W1.md:272`**: the residue after both repair-round-5 edits is the licensed classes named here **plus the two plain `§1 :48` occurrences this cell itself minted, now bracketed**, and the probe is **re-run after those edits, not before**. ⊘ *A strike rendered at one of its occurrences and certified over all of them* — **E-11(b)'s own named lesson, repeating one round later on the very ids E-11(b) struck.** **(c) THE THIRD-AXIS SWEEP, SHORT BY TWO (R4-1.11 / R4-7.1; PASS-4 D2 / D7).** §2.8a's round-3 sentence certified *"the one that failed is E-10(b)"*; the ten-labelled-span / thirteen-row transcript now pasted there shows **three** failures ⟨*"eleven-span" re-cut at repair round 5, REST-59*⟩. **#9 (§2.3's S2 label) is seat-local and is CURED at the bytes** as a subset-with-cut-disclosed. **#8 (the FR-EMT-11 rider, quote marks closed around a period the record puts outside a parenthesis) and #10′ (the FR-TT-1 cell's `glass-ui 8.0.0:69-73` re-spelling of the record's `Producer corroboration at 8.0.0 (`:69-73`)`) sit INSIDE the shared §2.2 bytes** and become **P-4** and **P-5**. **(d) THE PAIRED-OBLIGATION COUNT, RETIRED AS A COUNT (R4-7.2 / R4-7.3).** G-F9-21's face has under-counted its paired obligations in **three consecutive rounds** — two when there were three, three when there were four — because it stated a **prose number** over a set that keeps growing. **The face now carries a LABELLED ENUMERATION (P-1..P-8) and its own census ⟨cmd⟩ over that enumeration**, so the count is machine-checkable and can no longer go stale silently; where prose and list disagree, **the list is authoritative** (R4-7.3). **(e) E-1's ABSOLUTE, RE-SCOPED (PASS-4 D3 / R4-7.3)** — *"No prose in this file quotes 9"* becomes *"…outside the shared §2.2 bytes"*, which is what the cell already argued two sentences earlier; the masthead's identical-shape absolute was re-scoped at round 3 and this one was not. **(f) THE F-W1 ANCHOR, SPELLED AS THE BYTES SPELL IT (PASS-4 D5)** — this file's most-cited cross-spec anchor was written *"intra-wave order"* at **nine** sites while `F-W1.md:272` reads `**Intra-wave order (binding):**`; the existence probe the file installed at round 3 returned **∅** for it over a single capital letter. **Seven of the nine — every seat-local one — now read §4 Intra-wave order (binding) step 4**, and the probe resolves: ⟨cmd⟩ `grep -c 'Intra-wave order (binding)' "$X/waves/F-W1.md"` → **1**. **The other two are inside the paired checkpoint block and are REVERTED to the twin's spelling and disclosed as P-9**: this seat's first pass applied the correction file-wide, the region `diff` went non-empty, and the edit was undone at once — **the paired law catching a unilateral edit in the act, recorded rather than quietly tidied away**, because a repair round that hides its own near-miss is the class this file exists to convict. **(g) THE v4 WARRANT, RECORD-QUALIFIED (PASS-4 D6)** — the bare-token claim *"`L-26` appears nowhere in this spec"* was stated over a **live four-record homonym class one of whose members this spec books**; re-stated as the pair. **(h) PORTABILITY AND PINS (R4-2 / R4-8.3)** — the bases are **assigned** in-block rather than declared in a table, the file is re-certified `-P`-free with one `.{0,18}` bound, and **every sibling this file quotes now carries its sha256-at-quote-time** in §2.8a's PIN TABLE. | **NOTHING BOOKED WAS DROPPED AND NO GATE'S RED STATE MOVED.** What moved is the **basis**: the roster is the canonical's (17, all booked, 0 escapes, 0 fabrications), the legs are cited to their holders, the check file is a prior run, and the paired obligations are a list instead of a number. **Two identities were ADDED** (R2-7 · PAW-50 whole) and none removed ⟨*and `R2-7` was STRUCK at errata round 5, E5-16 — REST-52*⟩. **Eight paired obligations stand outstanding and none was fixed unilaterally** — P-1 · P-2 · P-4 · P-5 · P-6 · P-7 · P-8 · **P-9**, with P-3 closed at the bytes at round 3 ⟨*re-cut at repair round 5, REST-56 (P5-8): **the word said five, its own list named seven, and the census on G-F9-21's face says eight** — the word is replaced by the census figure and the list by the census's members*⟩. **The lesson, entered against the close act**: the standing note pass 4 left for pass 5 was *"assume the axis is right and the enumeration under it is short"*, and it convicted this file three more times in one round — a directive index read instead of a standing law, a strike rendered at one of its two occurrences, and a sweep certified over the labels the seat happened to edit. **Round 4's structural answer is to stop counting in prose**: the roster is enumerated from the census, the paired obligations are enumerated on the gate's face, and both carry a command that re-derives the enumeration from the bytes |
| **C-5** (MEASURE-AT-OPEN, un-verifiable from this seat) | `f2fe447` exists; local HEAD `cd26c65` (2026-07-03) is **49 commits ahead** of it — the banked "*host is 44 commits behind `f2fe447`*" is a **dated measurement from the M deep audit**, and the host's deployed SHA is **not measurable from this seat**. GAB-13's dirty worktree re-measured: **exactly 28** rows (`git status --porcelain \| wc -l` → 28) — unchanged since the fold | G-F9-15 is **MEASURE-AT-OPEN**: the deploy-of-record is read live at wave open and the banked 44 is never quoted as current. GAB-13's 28 stands, corroborating the F.W0 baseline decision either way |

### §2.8a QUOTE REGISTER — every cross-file quotation in this spec, keyed quote → home → ⟨cmd⟩ (R2-1-LAW.1/.2, discharged in one place)

**Why this register exists.** R2-1-LAW.1 admits a cross-wave or corpus quotation **only** as pasted `grep`/`sed` output with a ⟨cmd⟩ note beside it. Rounds 1 and 2 backed the quotations they *edited* and left the quotations they *inherited* unbacked — the same asymmetry that let a fabricated G-12 sentence survive a round. This seat re-ran **every** quotation in the file, read-only, on 2026-08-28 (registry frozen; fourier at HEAD `cd26c65`; `$R` = `docs/tranches/V/megatranche/registry/adjudicated/`, `$M` = `docs/tranches/V/megatranche/formation/fourier/`). **Line numbers appear below only where R2-2 item 2 permits them — the frozen corpus and the pinned fourier tree.**

**COMMAND BASES — declared once, and they root EVERY inline ⟨cmd⟩ in this file, not only this register's rows (round 3, ruling R3-9.5; PASS-3 D5 convicted the omission: three incompatible path roots ran through the file with no base stated, so every command reproduced and no single cwd reproduced the *file*).** Four bases, from the value.js repo root unless noted:

| base | path | the prefixes it roots |
|---|---|---|
| **`$X`** | `docs/tranches/X/fourier/` | `waves/…` · `carry/…` |
| **`$V`** | `docs/tranches/V/megatranche/` | `formation/fourier/…` · `registry/adjudicated/…` (`$M` and `$R` are `$V/formation/fourier/` and `$V/registry/adjudicated/`, unchanged) |
| **`$F`** | the **fourier-analysis checkout at pinned HEAD `cd26c65`** (`/Users/mkbabb/Programming/fourier-analysis`) | `web/…` · `scripts/…` · `.github/workflows/…` · `docs/tranches/M/PROGRESS.md` |
| **this file** | `$X/waves/` | bare `F-W9.md` / `F-W10.md` operands in the `diff` and self-probe receipts |
| **the frozen corpus, unprefixed** | `$R` | bare `fr-*.md` operands — the spelling the SHARED §2.2 bytes use (e.g. the received D/i-1 cell's `fr-PaperView.md` and `fr-*.md`), which this end may not re-spell without breaking Δ=∅; rooted here instead |

**ROUND 4 — THE BASES ARE NOW ASSIGNED, NOT MERELY DECLARED (R4-2.3).** A table that *names* `$R` is not a block that *defines* it, and R4-2.3 rules an unassigned variable a **phantom witness** (the purge seat minted two of them in F-W5). The assignment block is published once at **§2.0** and governs every ⟨cmd⟩ in this file; it is repeated here so this register is self-contained:

```
X=docs/tranches/X/fourier
V=docs/tranches/V/megatranche
R="$V/registry/adjudicated"
M="$V/formation/fourier"
C="$X/conformance/CENSUS-CANONICAL.md"
F=/Users/mkbabb/Programming/fourier-analysis          # pinned HEAD cd26c65
```

**Portability, re-audited this round (R4-2.1/.2).** This file was already clean and is re-certified by command — **in the BRACKET CLASS (E-8 law (i)), because the naive spelling of this probe MATCHES ITS OWN ⟨cmd⟩ NOTE**: the first form this seat ran returned line 1 of 1, and that line was the note describing it. A self-matching probe is no witness, so the forbidden constructs are spelled so the note is not an occurrence of them: ⟨cmd⟩ `grep -nE 'grep -[a-z]*P |\\[K]|\(\?[=!]' F-W9.md` → **∅, exit 1** (no PCRE flag, no `\[K]` escape, no lookaround — none exists on the pinned BSD binary; **the prose mention is bracket-classed for the same reason the ⟨cmd⟩ is** — this seat's second attempt still returned one hit, and it was the word `\[K]` in this very sentence), and ⟨cmd⟩ `grep -on '\.{0,[0-9]*}' F-W9.md` → **four hits, every one the string `.{0,18}`** — one live use at §4b and three mentions in this audit trail — far inside the BSD 255-bound (R4-2.2). No ⟨cmd⟩ here splices two commands over one undisclosed base (R4-2.4).

⌧ **RE-STAMPED AT REPAIR ROUND 5 (REST-64; P5-6).** Four pins were stale and **one moved file was not marked ⟨moved⟩ at all** (`F-W4.md`, `71f3b640d51e` → `39e1a60b3fc9`). ⊘ **Record the asymmetry the pass-5 seat proved, because it cuts both ways: all eighteen quotations this file takes from the moved files still reproduce — and the pin caught nothing at F-W3, where the hash MATCHED and the receipt was false anyway (REST-54).** *A hash proves the bytes did not move; it does not prove the paste was produced by the command.* The authoritative stamp is the purge seat's, after every repair-round-5 byte has landed.

**PIN TABLE — every SIBLING this file quotes, hashed AT QUOTE TIME (R4-8.3; `shasum -a 256 … | cut -c1-12`, this seat 2026-08-29).** A later rewrite of any of these is then **detectable, not deceptive** — which is the whole of LAW E's third clause and the death of the F-W8 `167` class:

| sibling | sha256 (first 12) | what this file quotes from it |
|---|---|---|
| `$X/conformance/CENSUS-CANONICAL.md` | ~~`a450b8e9f80e`~~ → **`3e0a9acb3381`** ⟨moved — **errata round 5**⟩ | **the F.W9 roster (16)** — §2.0, the sole census operand ⟨*17 → 16 at E5-16; `R2-7` → TERMINAL (∅) — REST-52*⟩ |
| `$X/waves/F-W0.md` | ~~`cd64d6580098`~~ → **`282f0c120cd4`** at the pass-5 settle ⟨moved; **moves again this round**, REST-01..REST-08⟩ | G-9 heading · the runner-seat clause · the mirror clause (two spans) · G-11 / G-12 · §1d item 2 · the `SUBSTRATE-LEDGER.md` create row · §6b's cross-edge row |
| `$X/waves/F-W1.md` | ~~`0bfd6c8cd369`~~ → **`a1302689aaa3`** at the pass-5 settle ⟨moved; **moves again this round**, BATCH-W1⟩ | **§4 Intra-wave order (binding) step 4** — the TWELVE-limb roster · Cross-edges item 9 · the checkpoint-homing row |
| `$X/waves/F-W3.md` | `a89c3386f3f8` at the pass-5 settle ⟨held through pass 5; **moves this round**, REST-09..REST-14. ⊘ **This is the row that proves the pin is necessary and not sufficient**: the hash MATCHED and §4b's receipt was false anyway — REST-54⟩ | the `§X.1-v4` heading · the single seam routing cell |
| `$X/waves/F-W4.md` | ~~`71f3b640d51e`~~ → **`39e1a60b3fc9`** ⟨**moved — and the row was NOT marked; marked at repair round 5, REST-64**⟩ | §5.2 Cross-edges' `→ F.W9/W10` row |
| `$X/waves/F-W10.md` | `bb58dc400cff` ⟨moved⟩ | the twin's shared-bytes head · its deixis clause · `RECEIVED HERE BY SPLICE` · both structural `diff`s |
| `$X/carry/F-W1-CARRY.md` | `0d0b091e86ca` | §CrossEdges item 9 — the 8-item checkpoint set |
| `$X/carry/F-W4-CARRY.md` | `bf1da6fe1e7e` | §CrossEdges, the `F.W9/W10` row (four-for-four) |
| `$M/lane-docs.md` | `1a5caf437d54` | §6 arm (b) · the flattened deploy-chronic sentence · the `6b` absence probe |
| `$M/lane-frontend.md` | `4433d809fac7` | §1's `Scripts:` row · §6's `INP / long-task discipline` heading |
| `$M/CENSUS-2026-08-03.md` | `684de3d8a8a0` | §4 item 5's unit-test-floor decision clause · the `4.5` absence probe |

**⟨moved⟩ — AND THE MECHANISM EARNED ITS KEEP INSIDE ONE SESSION.** Three of the eleven rows changed hash **between this seat's first probe and its last**: `F-W0.md` (`69b05ccbb320` → `cd64d6580098`), `F-W1.md` (`83ffe83cb37f` → `0bfd6c8cd369`) and the twin `F-W10.md` (`c00ec40d8673` → `bb58dc400cff`), their own round-4 repair seats running concurrently. **Every quotation this file takes from all three was RE-RUN against the moved bytes and every one still reproduces** — the G-9 heading, the runner-seat clause, G-11/G-12, §1d item 2, the `TWELVE`-limb roster, Cross-edges item 9, and `RECEIVED HERE BY SPLICE` — and **both shared regions still `diff` EMPTY against the new twin**, so the twin's round-4 edits did not enter the paired bytes. **Without the hash rows this would have been invisible**; with them it is a fact on the page, which is R4-8.3's entire point and the death of the F-W8 `167` class. This also re-proves **E-8 law (ii)** at the bytes: no count over a live sibling is banked anywhere in this file, precisely because siblings move mid-session.

**The frozen corpus (`$R`, 66 records) and the fourier tree at `cd26c65` are pinned by their own freeze and their commit; they need no hash row.**

**The rule, stated so it is runnable and not merely declared**: an inline ⟨cmd⟩ resolves from the base its first path component names, and **no note in this file mixes two bases in one command**. Read-only in every case; **no ⟨cmd⟩ in this spec writes, and none whose output is a COUNT of a live sibling or of this file itself is banked (R3-3.10)** — those are stated as absence probes, set-membership, or `grep -o` word output. Counts survive only over the **frozen** corpus (`$R`) and the **pinned** tree (`$F`).

**A · Quotations inside the SHARED §2.2 bytes — RE-RUN READ-ONLY, NOT EDITED** (the round-2 heading read *"VERIFIED READ-ONLY"*; the adjective is retired at round 3 — §2.8 **E-10(g)** — because a file whose four-verb reads VERIFIED **NO** and whose close act reserves the stamp to F.W10 should not grow a third `VERIFIED` token during a repair round about stamp discipline). The paired-bytes law (G-F9-21) forbids a unilateral touch, so no ⟨cmd⟩ note is inserted *into* the region; the provenance lands here instead, which satisfies R2-1-LAW without breaking Δ=∅. **Every one reproduces at the frozen corpus:**

| quoted words (as the shared bytes carry them) | home | ⟨cmd⟩, run this seat |
|---|---|---|
| *"every row above incl. all four blockers is invisible to every gate the repo runs"* | `fr-AdminFlaggedPanel` FR-AFP-49 dissent | `grep -o 'every row above incl. all four blockers is invisible to every gate the repo runs' $R/fr-AdminFlaggedPanel.md` |
| *"the gates for FR-EMT-1/-5's cures ride their own waves"* | `fr-EquationModeToggle` FR-EMT-11 rider | `grep -o "the gates for FR-EMT-1/-5's cures ride their own waves" $R/fr-EquationModeToggle.md` |
| *"every other row in this record is ungated because of it"* ⊕ *"so the F.W9/W10 close cannot treat the MINOR as a statement that coverage debt is small"* — **a COMPOSITE of two spans, labelled as such** (R2-1-LAW.4) | `fr-EquationModeToggle`, reader-2 dissent + the L-9.1 ruling | `grep -o 'every other row in this record is ungated because of it' $R/fr-EquationModeToggle.md` **and** `grep -o 'so the F.W9/W10 close cannot treat the MINOR as a statement that coverage debt is small' $R/fr-EquationModeToggle.md` |
| *"an icon-only trigger labels ITSELF"* | `fr-Tooltip`, glass-ui 8.0.0 corroboration | `grep -o 'an icon-only trigger labels ITSELF' $R/fr-Tooltip.md` |
| *"BLOCKER at any F.W9/W10 axe close-gate"* | `fr-Tooltip` FR-TT-1 promotion note | `grep -o 'BLOCKER at any F.W9/W10 axe close-gate' $R/fr-Tooltip.md` |
| *"coverage rides the B-1/B-2 cure wave"* | `fr-PaperView` D/i-1 coupled lock | `grep -o 'coverage rides the B-1/B-2 cure wave' $R/fr-PaperView.md` |
| *"Un-harnessable by construction"* ⊕ *"extend one axe keystone into the admin tab + a vitest devDependency"* — **two spans, two homes in one record, labelled** | `fr-AdminUserList` FR-AUL-16 (killed defence · cure) | `grep -o 'Un-harnessable by construction' $R/fr-AdminUserList.md` **and** `grep -o 'extend one axe keystone into the admin tab + a vitest devDependency' $R/fr-AdminUserList.md` |
| *"the only e2e \"Fullscreen\" hit is a comment"* | `fr-FullscreenViewer` FV-26 | `grep -o 'the only e2e "Fullscreen" hit is a comment' $R/fr-FullscreenViewer.md` |
| *"dies with the file"* | `fr-CanvasOverlayButton` FR-COB-20 — **a record-wide idiom, not a single sentence**; the shared cell quotes the phrase, never a unique coordinate | `grep -o 'dies with the file' $R/fr-CanvasOverlayButton.md` |
| *"what contract should a shell header state?"* | `fr-AppHeader` FR-AH-45 | `grep -o 'what contract should a shell header state' $R/fr-AppHeader.md` |
| *"**→ F.W1 ledger** (pin-truth row)"* | `fr-PaperArticleWindow` PAW-33 · C-i-2 | `grep -o '\*\*→ F\.W1 ledger\*\* (pin-truth row)' $R/fr-PaperArticleWindow.md` |

**B · Quotations outside the shared bytes — each already carries its ⟨cmd⟩ at its point of use; consolidated here for the sweep.**

| quoted words | home (stable anchor) | ⟨cmd⟩ |
|---|---|---|
| *"NO-WAVE-OWNER (producer dist emitter) + glass-ui BH relay at the TOP of the FR-COB-28 packet; F.W1 SEQUENCING GATE"* | `fr-NotationPills` FR-NP-32 | `grep -o 'NO-WAVE-OWNER (producer dist emitter) + glass-ui BH relay at the TOP of the FR-COB-28 packet; F.W1 SEQUENCING GATE' $R/fr-NotationPills.md` |
| *"35 top-level nodes; **zero** valid `@source`; one garbage at-rule literally named `` source` `` spanning :203–222"* | `fr-PaperSidebar` M1 parse witness | `grep -o '35 top-level nodes[^.]*' $R/fr-PaperSidebar.md` |
| *"the fleet has already elected to treat the working tree as the scope"* | `fr-GalleryAdminBanner`, worker-DU dissent (GAB-13) | `grep -o 'the fleet has already elected to treat the working tree as the scope' $R/fr-GalleryAdminBanner.md` |
| *"any fourier LCP measurement without this fix is measuring `sun.json`"* | `fr-AppHeader` FR-AH-1 | `` grep -o 'any fourier LCP measurement without this fix is measuring `sun.json`' $R/fr-AppHeader.md `` |
| *"unreachable in the shipped tree"* ⊕ *"CONTRADICTS lane-frontend.md:568"* — **two spans; the `:568` is the RECORD's coordinate, quoted never adopted** | `fr-GalleryInfiniteGrid` FR-GIG-15 | `grep -o 'unreachable in the shipped tree' $R/fr-GalleryInfiniteGrid.md` **and** `grep -o 'CONTRADICTS lane-frontend.md:568' $R/fr-GalleryInfiniteGrid.md` |
| *"B-1, B-2, C-29, D-07 and D-14 mechanically"* | `fr-EquationView`, the S2 packet argument | `grep -o 'B-1, B-2, C-29, D-07 and D-14 mechanically' $R/fr-EquationView.md` |
| *"No unit-test runner — vitest is ABSENT"* | `lane-frontend.md` **§1, the `Scripts:` row** | `grep -o 'No unit-test runner — vitest is ABSENT' $M/lane-frontend.md` |
| *"builds, fails the 60 s health-gate, rolls back, exits non-zero into a log nobody watches"* (§2.8 **C-1**'s banked mechanism) | `lane-docs.md` **§6, the standing-deploy-chronic paragraph** — **the sentence WRAPS a line in the source**, so a line-anchored probe cannot produce it; flattened probe below | `tr '\n' ' ' < $M/lane-docs.md \| grep -o 'builds, fails the 60 s health-gate, rolls back, exits non-zero into a log nobody watches'` |
| *"a unit-test floor (vitest) decision"* | `CENSUS-2026-08-03.md` **§4 item 5** (the F.W4 wave-sketch row) | `grep -o 'a unit-test floor (vitest) decision' $M/CENSUS-2026-08-03.md` |
| *"### G-9 — A unit-runner SEAT exists (the FLOOR's scope belongs to F.W9/W10)"* · *"a runner is installed, **wired into `ci.yml`**, … proving the seat live"* · *"**Explicitly NOT green-by-coverage** — F.W0 must not pre-empt the floor's scope."* · the **Disjointness with F.W9** paragraph | **F-W0 §4 G-9** | `grep -o '^### G-9 .*' waves/F-W0.md` · `grep -o 'a runner is installed.*proving the seat live' waves/F-W0.md` · `grep -o '\*\*Explicitly NOT green-by-coverage\*\* — F\.W0 must not pre-empt the floor.s scope\.' waves/F-W0.md` · `grep -o '\*\*Disjointness with F\.W9 (one sentence, declared identically at both ends — R-5)\*\*' waves/F-W0.md` |
| the **8 `e2e/*.spec.ts`** row · *"The e2e denominator is 8.** Stated positively"* · the **21 cartoon-card / 14 files** shim row · the **`text-admin-label` 7 / 4 files** row · *"ONE corrected anchor table published; every later wave quotes it"* · *"Every later wave cites this table rather than a challenge file"* | **F-W0 §4 G-11 / G-12** and **§1d item 2** | the six ⟨cmd⟩s pasted at §2.2 items 2/8, §2.8 **E-1**, and the DENOMINATOR PROVENANCE block below — each re-run this seat |
| *"The roster is TWELVE limbs and stays twelve"* | **F-W1 §4 **Intra-wave order (binding)** step 4** | `grep -o 'The roster is [*]*[A-Z]* limbs and stays [a-z]*' waves/F-W1.md` |
| the 8-item checkpoint set · the `F.W9/W10` test-and-visual-seats row | `carry/F-W1-CARRY.md` **§CrossEdges item 9** · `carry/F-W4-CARRY.md` **§CrossEdges** | the two ⟨cmd⟩s at §2.2 and §5 — both re-run this seat |
| the twin's shared-bytes head · its deixis clause · its `RECEIVED HERE BY SPLICE` receipt | **F-W10 §2.2 / §2.8** | the three ⟨cmd⟩s at §2.2 and §2.8 **E-2** — **structural only; no count over the live twin** (E-8 law (ii)) |
| *"take D's CLAIMS and L/C's LINES"* ⊕ *"re-resolve every anchor before quoting into a wave spec"* — the §5 *"verbatim law"*, **two spans, labelled** | **`fr-CoefficientsPanel` FR-CP-43/-44/-45** — **record-qualified (anti-rename)**: the `FR-CP-` prefix is **fr-CoefficientsPanel's**, NOT fr-ContourPreview's (whose rows are bare numbers — `38`, `29`, `31`, `39`, `41`); the same prefix carries **FR-CP-5 / -11 / -13 / -27 / -32**, all banked in that one record | `grep -o "take D's CLAIMS and L/C's LINES" $R/fr-CoefficientsPanel.md` **and** `grep -o 're-resolve every anchor before quoting into a wave spec' $R/fr-CoefficientsPanel.md` |
| **FR-CP-32** (cited at G-F9-1 as the toolchain-blindness leg) | **one home, two citations, checked**: booked at `fr-CoefficientsPanel:67` (*"FR-CP-32 = L-D-14 — MINOR"*); `fr-EqCoefficientsPanel` and `fr-FrequencyGraph` **route to it, never re-book it** — no double-home | `grep -l 'FR-CP-32' $R/*.md` → three records; `grep -o 'the unused `onUnmounted` is FR-CP-32.s banked cell' $R/fr-EqCoefficientsPanel.md` and `grep -o '\*\*→ FR-CP-32\*\*' $R/fr-FrequencyGraph.md` show the other two CITING |

**Scope of this register — R2-1-LAW.2 discharged for the whole file, in one place.** **Every *"verbatim"* / *"preserved verbatim"* / *"quoted"* label in this spec is backed by a ⟨cmd⟩ this seat ran** — inline at its point of use where the bytes are seat-local, and **here** for the two classes that cannot carry an inline note: (i) labels inside the **shared §2.2 region**, where an insertion would break G-F9-21's Δ=∅ (section A above), and (ii) labels in §2.3's seat table, §4b and §5 that quote a record already registered above (`fr-EquationView`'s S2 argument · `fr-PaperView`'s coupled lock · `fr-GalleryAdminBanner`'s worker-DU dissent · `fr-CoefficientsPanel`'s FR-CP verbatim law). **A label whose quote is not in this register, and carries no inline ⟨cmd⟩, is a defect against this section** — that is the falsifier, and it is runnable by anyone.

**Sweep result.** **Every quotation in this spec reproduces at its named home.** No quotation is typed from memory; no *"verbatim"* label stands without a ⟨cmd⟩; every composite is labelled a composite. **What the sweep additionally caught is not a quotation defect but an ANCHOR defect** — two cited `§`s that do not exist (**E-9(a)/(b)**). Quote-by-command verifies the *words*; only a heading-existence probe verifies the *address*. Both are now run. **ROUND 3 ADDS THE THIRD AXIS — SCOPE — and the register's round-2 sweep result was over-stated by exactly one row** (§2.8 **E-10(b)**): the `fr-PaperView` D/i-1 label reproduced *its quoted words* at the named home and still covered a clause the corpus does not carry (⟨cmd⟩ `grep -rc 'never scheduled independently' $R/fr-*.md` → **0 in all 66**), because a `grep -o` of the *quoted* span can never test the *labelled* span. **The falsifier is now three-fold and each part is runnable: the words (`grep -o` at the home) · the address (a heading-existence probe) · the SPAN (a `grep -F` of the label's WHOLE extent, not of the fragment the seat chose to paste).** **ROUND 3's SWEEP UNDER THAT THIRD AXIS WAS DECLARED COMPLETE AND WAS NOT — the round-3 sentence read *"the one that failed is E-10(b)"*, and it is FALSE BY TWO** (**R4-1.11**; PASS-4 **D2** / **D7**). The axis was right and the enumeration under it was short — the fourth consecutive round convicted on a probe whose SCOPE was narrower than its CLAIM. **Round 4 runs the third test over ten labelled spans over thirteen transcript rows (three primed) and pastes the whole transcript, pass and fail alike** ⟨**re-cut at repair round 5, REST-59 (P5-8).** The cell said **ELEVEN**; ⟨cmd⟩ `awk '/^\| # \| labelled span/,/^\*\*Three failures/' "$W" | /usr/bin/grep -oE '^\| [0-9]+.? \|'` → **13 rows**, of which **ten** are distinct labelled spans (`#1..#10`), the three primed rows being the records' own spans rather than labels. **Neither 10 nor 13 was eleven** — and this is R4-7.3 inside the register that publishes the count law.⟩ (⟨cmd⟩ `grep -cF '<the label's WHOLE extent>' <home>`, BSD `/usr/bin/grep`, bases as assigned at §2.0, this seat 2026-08-29):

| # | labelled span, at its full labelled extent | home | `grep -cF` |
|---|---|---|---|
| 1 | `coverage rides the B-1/B-2 cure wave.` (with period) | `$R/fr-PaperView.md` | **1** ✓ |
| 2 | `BLOCKER at any F.W9/W10 axe close-gate.` (with period) | `$R/fr-Tooltip.md` | **1** ✓ |
| 3 | `L-4's table is the citable one (Editor ×10 · Canvas ×6 · FunctionInput Wand2 ×1 = 17)` | `$R/fr-Tooltip.md` | **1** ✓ |
| 4 | `the only e2e "Fullscreen" hit is a comment` | `$R/fr-FullscreenViewer.md` | **1** ✓ |
| 5 | `extend one axe keystone into the admin tab + a vitest devDependency` | `$R/fr-AdminUserList.md` | **1** ✓ |
| 6 | `The visual-regression checkpoint set the uplift mints with no typecheck signal` | `$X/carry/F-W1-CARRY.md` | **1** ✓ |
| 7 | `harness before rider, flag before split (PAW-49/PAW-50)` | `$X/carry/F-W4-CARRY.md` | **1** ✓ |
| 8 | `the gates for FR-EMT-1/-5's cures ride their own waves.` **(with period — the labelled extent)** | `$R/fr-EquationModeToggle.md` | **0 ✗ FAIL** |
| 8′ | `the gates for FR-EMT-1/-5's cures ride their own waves` (without) | same | **1** — and ⟨cmd⟩ `grep -oF 'ride their own waves).' "$R/fr-EquationModeToggle.md"` returns `ride their own waves).`: **the record puts the period OUTSIDE a parenthesis and the label closes its quote marks INSIDE it** |
| 9 | `this one spec would have caught B-1, B-2, C-29, D-07 and D-14 mechanically` (§2.3 S2's round-3 labelled extent) | `$R/fr-EquationView.md` | **0 ✗ FAIL** |
| 9′ | `would have caught B-1, B-2, C-29, D-07 and D-14 mechanically` (the record's own span) | same | **1** — *"this one spec"* is spec-authored |
| 10 | `Producer corroboration at 8.0.0 (` + `` `:69-73` `` + `)` — the record's own spelling | `$R/fr-Tooltip.md` | **1** ✓ |
| 10′ | `Producer corroboration glass-ui 8.0.0:69-73` — §2.2's rendering of it | same | **0 ✗ FAIL** (a re-spelling, not a word drift) |

**Three failures, disposed by where they sit.** **#9 is SEAT-LOCAL and is CURED at the bytes this round** — §2.3's S2 cell now carries a **SUBSET label with the cut disclosed** and quotes only the record's own span (R4-7.1's *"re-paste byte-true, or re-label"*, second limb). **#8 and #10′ are inside the SHARED §2.2 region** and are therefore **DISCLOSED AND PAIRED, never fixed unilaterally** — they are **P-4** and **P-5** on G-F9-21's face. `PASS-3/CLOSE-CERT.md` §6.1's *"724 label sites byte-checked: 0 word-level drifts"* is falsified by all three, and by the class it could not see: **#10′ is neither a word drift nor a markup drift but a SPELLING of a coordinate, and #8 is a SCOPE drift — the exact axis §2.8a said round 3 had added.** That certificate carries the R4-1.E-3 erratum and may not be cited as an operand without it.

---

**DENOMINATOR PROVENANCE — F.W0's G-11 / G-12 are quoted, never re-performed (R-9.1).** F.W0 publishes two once-owned tables, **cited here by gate id and never by line** (R2-2): **F-W0 §4 G-11**, the corrected **anchor** table — ⟨cmd⟩ `grep -o 'ONE corrected anchor table published; every later wave quotes it' waves/F-W0.md` — and **F-W0 §4 G-12**, the corrected **denominator** table — ⟨cmd⟩ `grep -o 'Every later wave cites this table rather than a challenge file' waves/F-W0.md` → *"Every later wave cites this table rather than a challenge file"* — with superseded figures **FORBIDDEN downstream**. Both land in `fourier/docs/tranches/F/SUBSTRATE-LEDGER.md` (**create**-marked at **F-W0 §2 Bounds, the `SUBSTRATE-LEDGER.md` row**; the citation is prospective until the file exists — R-10). Every figure this spec states is therefore classified on its face:

| figure used here | status |
|---|---|
| **8 `e2e/*.spec.ts`** (§2.2 K11, §2.8 E-1, G-F9-4/-6) · **2 Teleport sites** (§2.3 S5, G-F9-11) · **21 cartoon-card sites / 14 files** and **`text-admin-label` 7 / 4 files** (the checkpoint set, §2.2) | **G-12 QUOTATIONS**, each reproduced by ⟨cmd⟩ at its point of use (R2-1-LAW.1) and each **under G-12's own pattern token**. **`cartoon-card` is that token; the round-1 spelling `cartoon-surface` was a RENAMED pattern and is STRUCK** (§2.8 **E-6**; anti-rename + G-12's superseded-figures clause + KF.W4(d)) — the carry's banked words in §2.2 column 2 keep saying `cartoon-surface` because they are the carry's words, and a banked wording is not a denominator. Published there; re-stated here only to be used. Read-only corroboration is recorded as corroboration of a pinned artifact, never as this spec's derivation |
| **ImageUpload's 15 entries / 6 files** (ruling 7's denominator, `fr-ImageUpload:61`) · **FR-TT-1's 17 icon-only triggers** (L-4's census, `fr-Tooltip:26`; D-1's 16 and its `:42/:49` anchors KILLED, register #8) · **GAB-13's 28 dirty rows** · **54 / 25 / 66** (RE-CUT-0) | **BANKED registry figures**, carried by id at their records — **routed to G-12 for publication** so the ledger is complete. Until G-12 adopts them they are cited **at their banked homes**, never re-derived; if G-12 publishes a different figure, **G-12 wins and this spec is defective against it** |
| every `file:line` in §1b and §3 | **G-11 QUOTATIONS** once its table exists; produced under FR-CP-45 and recorded at **C-4 / C-6** in the meantime. **Divergence from G-11 is a defect against G-11, not a rival act** |

**No gate in §3 re-derives its own oracle** (KF.W4): where a gate needs a denominator, it cites this table's row and the owning authority by path and line.

---

## §3 Gates

Twenty-three. All born RED with the failing probe named. Every gate names a **live witness** (L-19 — no descriptive invocation, no proof-farm scripts). `[re-measured]` = run read-only 2026-08-28 against fourier HEAD `cd26c65`; unmarked rows carry the fold measurement of 2026-08-25; deploy/evidence cells re-measure at open.

| gate | born state · witness | falsifier (turns it GREEN) |
|---|---|---|
| **G-F9-1** A unit-test harness exists and runs in CI | RED **[re-measured]** — `web/package.json` scripts are exactly `dev,build,preview,test:e2e,test:e2e:ui`; `devDependencies.vitest` → **undefined**; no `vitest.config`; `find web/src -name '*.test.ts' -o -name '*.spec.ts'` → **0** (**`formation/fourier/lane-frontend.md` §1, the `Scripts:` row** — cited by row label, never by line: ⟨cmd⟩ `grep -o 'No unit-test runner — vitest is ABSENT' formation/fourier/lane-frontend.md` → *"No unit-test runner — vitest is ABSENT"*; the earlier sittings' `§1 :[4]8` line cite into a live formation file is struck — **bracket-class rendering added at round 4, §2.8 E-11(b)**, since the plain spelling was itself an occurrence, §2.8 **E-9** · PAW-50 · FR-AUL-16 · FR-CP-32) | The **FLOOR** is met: the spec population and its thresholds exist and run in CI (`npm test`, vitest a devDependency, ≥1 assertion). **DISJOINTNESS (R-5)**: the **SEAT** — *"a runner is installed, wired into `ci.yml`, and inside G-8's type scope, with ONE asserting spec"* — is **F-W0 §4 G-9's** (cited by gate id — R2-2; ⟨cmd⟩ `grep -o '^### G-9 .*' waves/F-W0.md` → *"### G-9 — A unit-runner SEAT exists (the FLOOR's scope belongs to F.W9/W10)"*, and its GREEN carries **"Explicitly NOT green-by-coverage"**). **This gate does not green the seat and F.W0 does not green the floor**; F.W9's job name is `e2e-tests:`-adjacent in the same `ci.yml` (§2.8 C-6). **HARNESS-BEFORE-RIDER: the F.W4 decision lands first; F.W9 may not stand a harness the decision has not authorized** |
| **G-F9-2** A lint floor exists | RED **[re-measured]** — `ls web/ \| grep -iE 'eslint\|oxlint\|biome'` → **0**; no `lint` script (MG-θ · M7); five `catch (e: any)` in AdminFlaggedPanel with no gate to notice them (FR-AFP-22 · FR-AUL-50) | A lint script runs in CI and fails on a seeded violation. tsconfig flags are **F.W0's** — MG-θ ≡ M7, one edit, two records, booked once |
| **G-F9-3** The admin tab is entered by ≥1 spec, with an axe pass | RED **[re-measured]** — `grep -rln "admin" web/e2e/ \| wc -l` → **0 files**; e2e `audit` → 4 prose-only hits (AA-44); `admin\|flag` → 0 (FR-AFP-49) | ONE **existing** axe keystone (`visualization-ux.spec.ts:26-43` + `visualization-crud` AxeBuilder, wcag2a/aa/21a/21aa) **extended** into the admin tab, serious/critical empty. Gated on GAB-13's F.W0 disposition. Falsified by authoring a rival oracle instead of extending the keystone (KF.W4) |
| **G-F9-4** axe runs on `/paper` | RED **[re-measured]** — `grep -rln AxeBuilder web/e2e/` → exactly **two** files (`visualization-ux.spec.ts`, `visualization-crud.spec.ts`), both `/visualize`-gated (ruling 9 / K3). `/paper` **IS** navigated (`paper-performance.spec.ts:38`; `visual-baseline` paper row × 3 viewports) — the void is **zero assertions on any `paper-search*` selector and zero axe on the route** | ONE `/paper` axe artifact, serious/critical empty, covering D-B2's 2.39:1 heading. **Runs AFTER PAW-1's 42-site cure + the PAW-44 scrollport decision** — else 42 token drops make failures un-attributable. Falsified by any selector depending on `.sidebar-top-btn` (M2 lock) or by quoting "no paper route" from the corpus |
| **G-F9-5** axe runs on `/equation` | RED **[re-measured]** — `@axe-core/playwright ^4.11.3` in devDeps, never pointed here; the sole axe seat visits `/visualize` only (FR-EQR-6 · FR-EMT-11) | ONE `/equation` axe artifact. **FR-TT-1 promotion lock applies: RED by construction while the 17 naming legs are open, unless a named dated waiver is recorded** |
| **G-F9-6** `/equation` carries ≥1 behavioural assertion | RED **[re-measured]** — `grep -rn "/equation" web/e2e/` → **one hit**, `visual-baseline.spec.ts:34` — an assertion-free screenshot slug (FR-EMT-11 · FR-EQR-6 · FR-EQR-31 · FunctionInput C-13 · C·D-28 all name this one fact) | The ONE interaction spec (compute → notation → budget → reload) exists and asserts — mechanically catching **B-1, B-2, C-29, D-07, D-14**. Axe-seat economy: author once, cite five times |
| **G-F9-7** No gate in the suite is vacuous — deleting the subject must redden its spec | RED — `gallery.spec.ts:57-63` every assertion inside `if (await filterToggle.isVisible())` (FR-GSB-17); `:66-82` whole body inside `isVisible().catch(() => false)` (FR-USB-16); the `.glass-dock` branch `:49-53` unreachable (`grep -rn glass-dock src/` → 0); the `.or()` mask `:19-22`; L's three-conditionally-vacuous census (FR-GV-36) | Each guard rewritten to **assert the precondition EXISTS**; a deliberate deletion of the search bar / slug bar reddens its spec. **C-M4's guard is a DISTINCT mechanism (the wrong element's `data-state`) — cured separately, never folded** |
| **G-F9-8** No axe keystone is `test.fixme` on a stale premise | RED **[re-measured]** — `grep -rn "test.fixme" web/e2e/` → keystones at `visualization-ux.spec.ts:110`, `:133`, `:192` (only `:150` runs) on a booked `^2.0.0` "omits inert" baseline **doubly false** against installed 4.0.0 emitting `inert: !i.value \|\| void 0`; a fourth at `visualization-crud.spec.ts:630`. EasingPicker's sole covering e2e is the `:192` fixme, its unblock condition stale by two majors against the `^4.0.0` pin. **FIFTH SITE, carved explicitly (re-measured read-only 2026-08-28 — the command returns FIVE call sites, not four)**: `visualization-ux.spec.ts:212` (`save_contour_then_recompute`) is **F.W3-homed by its own in-file comment ("Un-skip at W3")** and is therefore **outside this gate's four keystones** — it is named here so the gate's rendered output matches the command it cites, and its un-fixme is **F.W3's**, never a green this wave may claim | All four **keystones** un-fixme'd and green (`ux:110` · `ux:133` · `ux:192` · `crud:630`); `ux:212` is excluded by name and reddens F.W3, not this gate. **The un-fixme is F.W3/W4's cure (fr-BasisSelector B-3 / R-2), not F.W9's** — F.W9 cannot produce admissible axe evidence until it lands; FR-CP-27 records what an un-fixme'd run would hit |
| **G-F9-9** A gallery card / modal is opened by a spec | RED — `gallery.spec.ts` `modal\|dialog\|card\|Open Visualizer` → **0** (GCM-42); 6 `test(` blocks, none reaches a card (FR-GFC-27) | A spec opens a card and asserts. GCM-4's unnamed dialog (two dev `console.warn`s per open) becomes visible only with **G-F9-10** |
| **G-F9-10** The e2e console guard is not blind to warnings | RED — `gallery.spec.ts`'s guard filters `error`-only + `404` — blind to GCM-4's warnings three ways | Guard widened to `warn`. **INTERACTION LOCK: no widening before F.W1 lands** — the error-only filter is the blind spot F.W1's four zero-console-error gates depend on (FR-EQR-3). Declare it, or F.W9 reddens F.W1 on pre-existing warnings |
| **G-F9-11** Fullscreen is exercised | RED — the only e2e "Fullscreen" hit is a **comment** at `gallery.spec.ts:113` (FV-26) | A spec enters fullscreen and asserts **against the teleported root** (FV-27 — one of exactly two Teleport sites tree-wide, with `PaperSearchModal.vue:41`) |
| **G-F9-12** The shell header's nav / logo / toggle are exercised | RED — no e2e touches the header's nav, logo or toggle (FR-AH-45). Only incidental coverage: `paper-performance.spec.ts:328` locating DarkModeToggle by `/switch to dark mode/i` — a locator matching only the light state — with `:329` waiting 250 ms against a 350 ms morph (fr-DarkModeToggle K-1, N-2 rider) | Header nav/logo/toggle asserted, **and the DarkModeToggle a11y label change lands in the SAME commit as the spec locator update** (N-2's rider — the label is a load-bearing selector). **CURE ATTRIBUTION (R3-7.3, round 3): the label change itself is F.W4's cure, banked — ⟨cmd⟩ `grep -o '\*\*F\.W4\*\* — `aria-pressed` (+ consider `role="switch"` per the producer) in the same commit as the spec.s locator update\.' $R/fr-DarkModeToggle.md` → *"**F.W4** — `aria-pressed` (+ consider `role=\"switch\"` per the producer) in the same commit as the spec's locator update."* F.W9 owns ONLY the spec-side half (the locator edit) and the same-commit sequencing; a green here requires F.W4's half to have landed, exactly as CURE-BEFORE-EVIDENCE (§4a-6) requires everywhere else** |
| **G-F9-13** The Playwright matrix carries a mobile/coarse-pointer project | RED **[re-measured]** — `web/playwright.config.ts:46-51` declares exactly **one** project (`chromium` / Desktop Chrome); 8 specs / 29 `test(` blocks. FR-USB-1/3 unreachable in CI by construction (FR-USB-16); FV-11's 44-vs-60px discordance invisible; `fr-ImageUpload L:L-i2` (§2.0 row 12 — row number `27` retired, REST-61)'s drag path unreachable | A mobile/coarse project runs. **This matrix change precedes every coarse-pointer witness.** Falsified by any claim that it discharges a Safari cell — **it does not** (X-W11 G8 edge) |
| **G-F9-14** The figure set-equality invariant is asserted at build time | RED — `TRANSCODED_FIGURES = new Set(Object.keys(FIGURE_DIMENSIONS))` (**`web/src/lib/figureDimensions.ts:52`, sole assignment** — corrected per **R-1f**; re-measured read-only 2026-08-28: `grep -rn TRANSCODED_FIGURES web/src/` → `lib/figureDimensions.ts:52` and `:56` only, `PaperArticleWindow.vue` → **0 hits**. The registry's anchor at `fr-PaperArticleWindow:58` is a **bare `:52`** and **stays bare** — the filename was spec-added and is now the tree's; `resolveFigure` itself IS at `PaperArticleWindow.vue:44`, which is the half PAW-50 relocates) with **no generator, manifest, CI or e2e anywhere** (PAW-12); the assertion has no home because `web/` has no harness (PAW-50). Invariant **HOLDS at HEAD** (28 png / 26 avif / 26 webp; unpaired `fourier.png` + `maintainer-avatar.png`; 26 keys = 26 `\includegraphics`) | A vitest assertion or a Vite build plugin **fails on a seeded mismatch**. Harness-before-rider; `resolveFigure` moves beside `FIGURE_DIMENSIONS` in `lib/figureDimensions.ts` in whatever wave executes PAW-12. The rider guards drift — a green here is **not** evidence of a repair |
| **G-F9-15** The deploy-of-record is current — the host is not behind, and no rollback is silent | RED — the chronic. **MEASURE-AT-OPEN**: `f2fe447` exists and local HEAD `cd26c65` is **49 commits ahead** of it; the banked "*44 behind*" is the dated M-deep-audit number and the host's SHA is unmeasurable from this seat. Local HEAD has not moved since **2026-07-03**; M.W2/W3/W4 all `planned` (`docs/tranches/M/PROGRESS.md:15-28`). **§2.8 C-1 narrows the mechanism**: the script's rollback is loud (`ALERT`), the **channel** is not — no inv-31 floor exists | Host at HEAD **read live at wave open, never inherited**; readiness separated from liveness (M.W2); a failed deploy is **loud *and blocking* in a watched channel** (M.W4 + inv-31 — an `ALERT` line in an unwatched log does not clear this gate). Re-homing **disposes of the M board rows explicitly** — no double-booking |
| **G-F9-16** The API deploy path is fail-closed on a same-SHA green CI run (inv-28) | RED **[re-measured]** — `deploy-pages.yml:48-57` gates the SPA on `conclusion == 'success' && head_branch == 'master' && event == 'push'` with checkout pinned to `head_sha`; `grep -n 'conclusion\|workflow_run\|gh run\|inv-28' scripts/deploy-hook.sh` → **zero hits** — the API ships on a bare webhook (`deploy.babb.dev/hooks/fourier-analysis`). inv-28 holds on one arm only — **`formation/fourier/lane-docs.md` §6, arm (b)**, cited by its own row label: ⟨cmd⟩ `grep -o '\*\*(b) API → self-hosted Docker at `babb\.dev`\.\*\*' formation/fourier/lane-docs.md` → *"**(b) API → self-hosted Docker at `babb.dev`.**"*. **The round-1/round-2 spelling `lane-docs §6b` is STRUCK as a section that does not exist** — ⟨cmd⟩ `grep -o '6b' formation/fourier/lane-docs.md` → **∅ (exit 1)**, an absence probe rather than a count of a live sibling (**R3-3.10**; §2.8 **E-10(e)**); §6 carries lettered *paragraphs* (a)/(b), never `§6a`/`§6b` headings (§2.8 **E-9**) | The API path **refuses any SHA without a covering green CI run**. **Not an API-shape gate — the contract is SS-4's** |
| **G-F9-17** Every claimed-green cites a covering green run id (inv-27) | RED — M.W11 is `planned` (`docs/tranches/M/PROGRESS.md:15-28`); no run id is on record for any F-side green | Each green gate row carries a **run id and a committed SHA**. **R3-9 binds: an assertion of non-execution is recorded, never cited as a method guarantee.** KF.W4 applies — **a gate may not re-derive its own oracle** |
| **G-F9-18** CWV/INP evidence exists and is attributable | RED, **ORDER-LOCKED** — no LCP/TBT trace exists. FR-AH-1: AppHeader statically drags **450,631 B** of Fourier-coefficient JSON into the eager entry chunk (82.5 % long-float literals; 75,210 B read by no reachable code) to draw a 40px icon, with FR-AH-7 (348,707 B) and FR-AH-2 (402,944 B of unusable preloads) competing for the same first paint — "*any fourier LCP measurement without this fix is measuring `sun.json`*"; the value.js **Q14** escalation is the named prior art | A trace taken **AFTER** F.W4's async boundary + generator-side payload prune — **or** a baseline published **with its invalidity stated on its face**. FR-GIG-15 rider: the three-rung INP floor (`scheduler.yield`, chunkSize 24 vs pages capped at 20) is **unreachable in the shipped tree**, contradicting **`formation/fourier/lane-frontend.md` §6, the `INP / long-task discipline` row** (⟨cmd⟩ `grep -o '^### INP / long-task discipline' formation/fourier/lane-frontend.md`) — **do not cite that row as hygiene.** The record's own coordinate stays the record's words, quoted and never adopted as this spec's anchor (⟨cmd⟩ `grep -o 'CONTRADICTS lane-frontend.md:568' registry/adjudicated/fr-GalleryInfiniteGrid.md`; anti-rename + R2-2 — §2.8 **E-9**) |
| **G-F9-19** Containerization fidelity is audited **as-running**, not as-declared | RED **[re-measured]** — `web/Dockerfile:24` copies `assets/ → public/assets/` and `:26-29` copies six `paper/fourier_paper.*` files because `vite-plugin-latex-paper` compiles the TeX at build — **the image cannot build from `web/` alone**; two nginx authorities coexist (printf-inlined `:33-43` vs the 72-line `nginx/fourier.conf`); in-file prior art at **`:6-10`** (the `163ca47` stale `COPY web/vendor ./vendor` that "broke every Docker build" — ⟨cmd⟩ `sed -n '1,12p' web/Dockerfile`; the round-1 `:5-9` is corrected at §2.8 **E-5**). **§2.8 C-3**: `docker-compose.prod.yml` already *declares* read_only/tmpfs/cap_drop/no-new-privileges/2G/256M/Mongo `requireTLS`, and `nginx/fourier.conf` already carries 2 rate zones (`:16-17`) + 5 security headers (`:25-29`) | Every hardening claim verified **against the running artifact** (`docker inspect` on the deployed stack), never against the compose bytes that are themselves the documentation; the **two nginx authorities reconciled to one**; the mongo `read_only`/`cap_drop` **BOOKED residuals** (G.W7 ε.2) and the `--tlsAllowConnectionsWithoutCertificates` honesty pivot each carry an explicit verdict. **No gate may assume `docs/precepts` is readable in CI** — the checkout deliberately omits submodules |
| **G-F9-20** Every evidence artifact cites a committed SHA on a parsing substrate | RED **[F.W0-gated]** — **FR-NP-32 (≡ fr-PaperSidebar M1)** — the canonical citation form (**R-8**: two banked witnesses of ONE fact; cite both, never substitute. `fr-NotationPills:35` grades it **BLOCKER**, disposition *NO-WAVE-OWNER (producer dist emitter) + glass-ui BH relay at the TOP of the FR-COB-28 packet + F.W1 SEQUENCING GATE*; the mechanism below is M1's parse witness): installed glass-ui 4.0.0 `dist/styles/index.css` is syntactically corrupt (`postcss.parse` → 35 top-level nodes, **zero** valid `@source`, one garbage at-rule named `` source` `` at :203–222); strict Tailwind loaders hard-error. **GAB-13**: the audited artifact is the DIRTY WORKTREE — `git status --porcelain \| wc -l` → **28 [re-measured]** (HEAD `web/package.json` pins `^3.1.0`/`^2.2.0`/`^0.10.0`/lucide `latest` vs WT `^4.0.0`/`^4.3.0`/`^0.13.0`/`^1.0.0`). **F8-REACH-01/02**: InfoCard.vue + CanvasOverlayButton.vue unmounted, un-dispositioned; **HOLD is RED** | F.W0 lands: M1 relayed to the glass BH inbox and the dist parses (or the pin moves); the WT diff **landed OR recorded as the audit baseline**; both F8-REACH rows carry `KEEP_WITH_MOUNT \| ISOLATED_HARNESS \| DELETE`. **F.W9 evidence taken before this measures a tree no commit holds.** Dissent preserved verbatim (worker-DU): *"the fleet has already elected to treat the working tree as the scope"* — if F.W0 adopts the worktree as baseline, GAB-13 discharges to a disclosure line and DU's grading was right all along |
| **G-F9-21** The F.W9/F.W10 re-cut table is identical in both specs, **Δ=∅ both directions** | RED **[re-measured]** — `grep -o 'F\.W9/W10' registry/adjudicated/fr-*.md \| wc -l` → **54 / 25 records**; bare `F.W10` → **0**; `ls fr-*.md \| wc -l` → **66**. `F-W10.md` §2.2 carries the 26-identity table and states "F.W10 does not author F.W9" — **the twin of G-F10-6**; this file's §2.2 is spliced from those bytes. **CLOSURE OPERAND, RE-BASED AT ROUND 4 (R4-3 / R4-10)**: this gate's **census operand is `$C` §2's F.W9 roster — **16** ids, enumerated at §2.0** (⟨cmd⟩ `awk '/^### F\.W9 — /,/^### GLASS-RELAY/' "$C" \| grep '^- \*\*fr-' \| grep -o '\`[^\`]*\`' \| wc -l` → **16**; `$C` sha256 **`3e0a9acb3381`**, R4-8.3) ⟨*re-based and re-cut at errata round 5 — read 17 ids and pin `a450b8e9f80e`; **E5-16** made `R2-7` TERMINAL (∅), and the probe is narrowed to the roster lines because the cured header carries a backticked token of its own — REST-52*⟩. **`54 / 25 / 66` is a band-token measurement bounding the SHARED TABLE, never this wave's roster, and no check file and no superseded census is named anywhere in the gate** | Every routing identity cut to exactly one of F.W9 / F.W10 / a named other wave; set-difference ∅ (COHESION §3.1); **and every one of the canonical's 17 F.W9 ids booked at §2.0 or cited to its holder at §2.0a/§2.0b** — 17 booked, 0 escapes, 0 fabrications. **Falsified by**: a row minted from the 14 boilerplate routing-law lines or the 7 boilerplate-only records; a row re-booked under a new id; **a roster derived from any source but `$C`** (R4-10); **any edit to `fr-*.md`** (the registry is read-only). **THE PAIRED OBLIGATIONS ARE ENUMERATED, NOT COUNTED (R4-7.2 / R4-7.3).** This face stated a prose number in three consecutive rounds and it went stale in all three — two when there were three (PASS-3 D2), three when there were four (PASS-4 D2), and the correcting round re-created the staleness inside itself. **It is now a labelled list carrying its own census command**, and where the prose and the list disagree the **list** is authoritative:

| **P-n** | the obligation, inside the shared §2.2 bytes | state |
|---|---|---|
| **P-1** | **E-1** — the 9-vs-8 erratum: the AA-44 cell's *"9 e2e never enter the audit tab"* against the FR-AFP cell's *"Quote 8 specs, never 9 (K11)"*, settled by **F-W0 §4 G-12**'s published **8** | **OUTSTANDING** |
| **P-2** | **E-9(b)** — the `census §4.5` phantom anchor inside the `fr-App C-13` and `PAW-50` cells (⟨cmd⟩ `grep -o '4\.5' "$M/CENSUS-2026-08-03.md"` → **∅, exit 1**), which F.W0 had already corrected at its own end | **OUTSTANDING** |
| **P-3** | **E-10(b)** — the `fr-PaperView` D/i-1 *"verbatim"* label covering *"never scheduled independently"*, a clause the corpus does not carry (⟨cmd⟩ `grep -rc 'never scheduled independently' "$R"/fr-*.md` → **0 in all 66**) | **CLOSED at the bytes, round 3** — F.W10 authored the re-scope, F.W9 received it by splice |
| **P-4** | **the FR-EMT-11 rider label** — quote marks closed around a period the record puts outside a parenthesis (⟨cmd⟩ `grep -cF "the gates for FR-EMT-1/-5's cures ride their own waves." "$R/fr-EquationModeToggle.md"` → **0**; without the period → **1**) | **OUTSTANDING — new at round 4** (R4-1.11 / R4-7.2) |
| **P-5** | **the FR-TT-1 coordinate re-spelling** — the cell renders `glass-ui 8.0.0:69-73` where the record reads *"Producer corroboration at 8.0.0 (`:69-73`)"*; the address is the record's and licensed, the **spelling** is the spec's | **OUTSTANDING — new at round 4** (PASS-4 D7) |
| **P-6** | **the `fr-ContourPreview` identity cell** — spells the record's **row number `38`** where the canonical's banked id is **`C:D-11`** (aliases `L:L-12`); a row number is not an identity (§0.1 rule 1) | **OUTSTANDING — new at round 4** (R4-10) |
| **P-7** | **the `fr-ImageUpload` identity cell** — spells the record's **row number `27`** where the canonical's banked id is **`L:L-i2`** | **OUTSTANDING — new at round 4** (R4-10) |
| **P-8** | **the three ⟨re-cut⟩ cells** — PAW-49 → F.W0 · PAW-50's decision → F.W4 · FR-TT-1 → F.W10, each stating a home the stamped census does not carry (canonically **F.W3** · **F.W9** · **F.W4**). §2.0/§2.0a carry the corrected homes seat-locally; the shared cells are disclosed | **OUTSTANDING — new at round 4** (R4-10) |
| **P-9** | **the F-W1 anchor's CASE, at the two checkpoint-block sites** (items 5 and 7) — the charter's bytes read `**Intra-wave order (binding):**` at `F-W1.md:272` and the cells read *"intra-wave order"*, so the round-3 §-existence probe returns **∅** for this file's most-cited cross-spec anchor over one capital letter (⟨cmd⟩ `grep -c 'intra-wave order' "$X/waves/F-W1.md"` → **0**; `grep -c 'Intra-wave order' …` → **1**). **The seven seat-local sites were corrected this round; these two are inside the shared bytes and were REVERTED to the twin's spelling the moment the region `diff`ed non-empty** — the paired law caught a unilateral edit in the act, which is the strongest evidence this round produced that the law works | **OUTSTANDING — new at round 4** (PASS-4 D5) |

**Census ⟨cmd⟩ over this enumeration, so the figure re-derives from the bytes and cannot go stale in prose**: `grep -Ec '^\| \*\*P-[0-9]+\*\* \|' F-W9.md` → **9** (⊕ **1 CLOSED**, **8 OUTSTANDING** — ⟨cmd⟩ `grep -Ec '^\| \*\*P-[0-9]+\*\* \|.*OUTSTANDING' F-W9.md` → **8**). **Not one of the eight was fixed unilaterally**, and that is the ruling, not a convenience: **E-9(b) and every round-4 addition are disclosed rather than cured here precisely because Δ=∅ outranks a unilateral correction** — the paired law orders the bytes paired, never the defect kept silent, and a fix the other end has not authored is rival bytes rather than a cure (R2-6a's whole purpose). **All eight land in ONE commit with the twin.** ⟨*seven → eight at repair round 5, REST-55 (P5-8): this face's own census ⟨cmd⟩ `grep -Ec '^\| \*\*P-[0-9]+\*\* \|.*OUTSTANDING' "$W"` returns **8**. **R4-7.3 — the LIST is authoritative and the prose is corrected in place**; the list was installed at R4-7.2 and the prose beside it never was. **Fourth consecutive round this gate face under-counted itself, now inside the round that installed the cure** — until fixed, G-F9-21's GREEN condition was indeterminate by one obligation*⟩ **§2.8 E-2's asymmetry is CLOSED**: the 8-item checkpoint set (R-2b) was booked into the shared §2.2 region from this end and **F.W10 has received the identical bytes by splice** at repair round 2 (R2-6a, both ends) — ⟨cmd⟩ `diff <(awk '/^\*\*THE F.W1 VISUAL-REGRESSION/,/^\*\*Born-RED witness/' F-W9.md) <(awk '/^\*\*THE F.W1 VISUAL-REGRESSION/,/^\*\*Born-RED witness/' F-W10.md)` → **empty** and ⟨cmd⟩ `diff <(awk '/^\| record \| identity/,/^\*\*By-mechanism entrants/' F-W9.md) <(awk '/^\| record \| identity/,/^\*\*By-mechanism entrants/' F-W10.md)` → **empty**, both this seat, read-only. Retyped nowhere, and **F.W9 did not author F.W10**. A repair that had left the set unbooked to preserve Δ=∅ would have preserved a silent drop, which M-25 forbids outright: the twin law orders the bytes to be **paired**, not the drop to be **kept**. **The gate stays RED on all EIGHT outstanding paired obligations** ⟨*REST-55: P-1 · P-2 · P-4 · P-5 · P-6 · P-7 · P-8 · **P-9***⟩ **and on the identity-closure act itself; neither spec stamps before this closes** |
| **G-F9-22** The axe close-gate is honest about the FR-TT-1 promotion | RED — the twin of G-F10-10. FR-TT-1: the shim is the only label on **17** icon-only triggers — the census **as the record spells it**, attribution word restored at round 3 (§2.8 **E-10(f)**): ⟨cmd⟩ `grep -o "L-4's table is the citable one ([^)]*)" $R/fr-Tooltip.md` → *"L-4's table is the citable one (Editor ×10 · Canvas ×6 · FunctionInput Wand2 ×1 = 17)"* — wiring `aria-describedby` only-while-open — a description, never a name — with the banked note **"BLOCKER at any F.W9/W10 axe close-gate."** Today: `grep -rln "admin" web/e2e/` → **0**, no axe visits `/paper`, `/equation` = one assertion-free screenshot | The close axe run covers `/paper` **+** `/equation` **+** the admin tab with **zero serious/critical**, **OR** the 17 naming legs are recorded as a **named, dated waiver**. **It may not be argued green while the legs are open** — an axe close-gate authored before F.W4's legs is RED by construction |
| **G-F9-23** The F.W1 visual-regression checkpoint set has an instrument (the 8 items of `carry/F-W1-CARRY.md`, **§CrossEdges item 9**) | RED **[re-measured 2026-08-28, read-only]** — the uplift mints all eight with **no typecheck signal**, and the suite adds **no screenshot-diff signal**: `grep -c toHaveScreenshot web/e2e/*.spec.ts` → **0 in all 8 specs**; **no `*-snapshots` directory exists tree-wide**; `visual-baseline.spec.ts:48` writes bare full-page PNGs via `page.screenshot()` (**`:55-59`**, corrected at §2.8 **E-5**) into `docs/tranches/J/audit/screenshots/{before,after}` — **a capture harness, not a regression gate**. Two items are additionally invisible for a second reason: `text-admin-label` (`grep -rln "admin" web/e2e/` → **0**, AA-44) and GCM-22's `p-0` insets (no spec opens the modal — **G-F9-9**). `.disclosure-content` → **0** hits in `web/src/` (producer-side) | A checkpoint artifact exists that **compares**, covering the eight items, and **fails on a seeded pixel change**. Preconditions, each owned elsewhere: **G-F9-9** (a card is opened) · **G-F9-13** (the coarse project, for the tooltip re-proportion) · **S1**'s admin entry (for `text-admin-label`). **F.W9 owns this checkpoint and NO cure** — items 1/5/7 are limbs of **F-W1 §4 **Intra-wave order (binding)** step 4**, item 3 is producer-side and rides **SS-6**, and a green here is **not** evidence of a repair. **PAIRED — DISCHARGED**: the §2.2 block this gate witnesses is **F.W9-AUTHORED** and **has reached F.W10 by splice** (R2-6a performed both ends; witnessed **structurally, never by a count over a live sibling** — ⟨cmd⟩ `diff <(awk '/^\*\*THE F.W1 VISUAL-REGRESSION/,/^\*\*Born-RED witness/' F-W9.md) <(awk '/^\*\*THE F.W1 VISUAL-REGRESSION/,/^\*\*Born-RED witness/' F-W10.md)` → **empty**, plus the twin's own `RECEIVED HERE BY SPLICE` clause; §2.8 **E-2**; G-F9-21). **The bytes spliced are the E-5/E-6-corrected bytes** — the region `diff`s empty, so no round-1 figure was re-introduced to close it. **The splice's landing greens nothing here**: this gate is RED on the absent instrument, and a paired-bytes discharge is not a coverage fact |

---

## §4 Sequencing

### §4a Ordering locks (carried whole; each lands where its rows land)

1. **RE-CUT-0 precedes everything** — neither F.W9 nor F.W10 stamps before **G-F9-21 ≡ G-F10-6**. §2.8 **E-1**'s correction is a **paired** edit in one commit.
2. **F.W0 SUBSTRATE PRE-GATES FIRST** (binding; **G-F9-20** is its witness) — **FR-NP-32 (≡ fr-PaperSidebar M1)** (R-8: two banked witnesses, ONE fact — cite both, never substitute; `fr-NotationPills:35` is the BLOCKER grade and the F.W1 SEQUENCING GATE) · GAB-13 · F8-REACH-01/02 · MG-θ ≡ M7's tsconfig flags (one edit, two records) · ⟨re-cut⟩ PAW-49 · FR-AFP-54/-55 manifest banding · fr-EasingPicker's stale-build fact · the Codex residue set (R3-9 · R3-16+R4-11+R5-9+R6-10 · R4-12 EVAPORATED · R5-3 · the still-registered `~/.codex/worktrees/d0be/fourier-analysis`). **Owner rulings owed and never presumed: OG-F1, OG-F2.**
3. **F.W1 IS ATOMIC LAND-OR-LOSE** — ▲ F.W1 is ONE atomic land-or-lose transaction — **the TWELVE-limb roster chartered at F-W1 §4 **Intra-wave order (binding)** step 4 (the atomic transaction, G6), incl. the FR-EQC-7 vaul-vue manifest gate INSIDE per F-W1 §4 Cross-edges' `F.W0 → F.W1` HARD-predecessor row — cited whole, never restated here.** (**R2-5** rules the extent TWELVE — the eleven chartered plus the vaul-vue gate returned INSIDE at repair round 1 — and **the charter's own bytes now read TWELVE**: ⟨cmd⟩ `grep -o 'The roster is [*]*[A-Z]* limbs and stays [a-z]*' waves/F-W1.md` → *"The roster is TWELVE limbs and stays twelve"*, re-run this seat; receipt and the struck first-sitting version at §2.8 **E-4**. Quoted, never re-derived here.) *(§4b's F.W1 row carried the round-2 first sitting's two surviving line cites into F-W1; both are struck at §2.8 **E-8**.)* (**R-4b**: a sibling that restates the transaction re-fractures it; the three-limb restatement this line carried before the L-20 round-1 repair is struck. Consequence if it does not land whole: four zero-console-error e2e gates go RED — FR-EQR-3.) **It precedes F.W3/W4 (§2 law)**, and **F.W1 must NOT be credited for the pagination-drain cure (FR-GIG-5)** — that lock lives in F.W1's spec, restated here so F.W9 never books it either. The eight checkpoint items F.W9 instruments (§2.2, G-F9-23) are minted **by that transaction**; three of them (`GCM-22 p-0`, FR-CP-13's gap, and the `--shadow-cartoon`/Badge/`cartoon-surface` paint) are **its limbs, not F.W9's** — F.W9 supplies the checkpoint and books no limb.
4. **HARNESS-BEFORE-RIDER (PAW-50)** governs every vitest-shaped disposition — PAW-12, App C-13, `fr-ContourPreview C:D-11` (aliases `L:L-12`; §2.0 row 4 — row number `38` retired, REST-61), FR-AUL-16's devDependency limb, AA-44: **decision F.W4 · execution F.W9 · terminal record F.W10.** **Flag-before-split (PAW-49)** rides it.
5. **SEAM-BEFORE-SEAT** — FR-AFP-42's mountable surface (F.W3/W4) precedes its test seat; the seat is never folded into AA-44 (testability ≠ coverage).
6. **CURE-BEFORE-EVIDENCE** — F.W3/W4 lands B-3's four un-fixme'd keystones (G-F9-8), FR-TT-1's 17 naming legs (G-F9-5/-22), FR-EQR-6's tabindex+`aria-label`, D·D-B2's `aria-label` ×2, GCM-4's `<DialogTitle>`, PaperSidebar D-B2's token and C-M4's `CollapsibleTrigger` restructure, PAW-12's predicate split, FR-GFC-27's rider, EasingPicker C/m-6's picker test — **before** the corresponding F.W9 gate is admissible.
7. **AXE-SEAT ECONOMY** — the `/equation` seat is **ONE** artifact (FR-EQR-6 · FR-EQR-31 · FR-EMT-11 · FunctionInput C-13 · C·D-28 · D·D-B2): author once, cite six times. The `/paper` seat is **ONE** (D-B2 · M7 · PAW-34 · D/i-1) and **runs AFTER PAW-1's cure + the PAW-44 scrollport decision**. The admin seat **extends** the existing keystone; it does not clone it.
8. **MATRIX-BEFORE-COARSE (FR-USB-16)** — the mobile/coarse Playwright project lands before **any** coarse-pointer witness (glass `[data-size="icon"]` floor · PaperSidebar `--ui-scale:1.5`/`--control-floor:2.75rem` · FV-11's 44-vs-60px · `fr-ImageUpload L:L-i2` (§2.0 row 12 — row number `27` retired, REST-61)'s drag path).
9. **CONSOLE-GUARD INTERACTION (GCM-42)** — **no widening of the error-only filter to `warn` before F.W1 lands**; declare it, or F.W9 reddens F.W1 on pre-existing warnings.
10. **D/i-1 COUPLED LOCK** — `/paper` search coverage **rides the B-1/B-2 cure wave**, never scheduled independently.
11. **CWV ORDER-LOCK (FR-AH-1)** — any LCP measurement before F.W4's payload prune is measuring `sun.json`: take the trace after F.W4, or publish a **stated-invalid** baseline.
12. **REPAIR-ARMS-A-DEFECT SAME-COMMIT RIDERS** (carried where their rows land, F.W3/W4; restated so F.W9 sequences around them): fr-PaperSearchModal **D-1 + D-3 + `outline:none`** · **PAW-44 / LAW-3** restore-only-with-background-cure (the scrollport restoration lands **WITH-or-AFTER** PAW-1 **and** PAW-30) · **MPC-31 / MPC-3⊕10⊕13⊕8⊕22 one-cut law** · **FR-MSP-6 two-channel lock** · **LAW-4** one clearance authority · **N-2's label+locator same-commit rider** (G-F9-12) — **the spec-side half is F.W9's; N-2's CURE is F.W4's (banked), and this file said so nowhere until round 3** (R3-7.3; PASS-3 D4). The bank routes it: ⟨cmd⟩ `grep -o '\*\*F\.W4\*\* — `aria-pressed` (+ consider `role="switch"` per the producer) in the same commit as the spec.s locator update\.' $R/fr-DarkModeToggle.md` → *"**F.W4** — `aria-pressed` (+ consider `role=\"switch\"` per the producer) in the same commit as the spec's locator update."* F.W9 owns the **locator edit and the sequencing**, never the `aria-pressed` label — which is this file's own opening law (*F.W9 owns GATES, F.W9 owns no CURE*) applied to the one rider that had escaped it. Excluded as a cure at §5.
13. **M2 HARD LOCK** — F.W4 must not rename `.sidebar-top-btn` before the latex-paper relay lands; **no `/paper` selector F.W9 authors may depend on that class.**
14. **ROUTE LAW** — **direct parse-that → fourier is FORBIDDEN** (parser → Value PLAW-BIND → V.L1/V.L5 → packed Value release → Fourier F.W0). **This wave mints no such edge.** Glass-producer rows ride SS-6, **never** become frontend hacks. **MEASURE-AT-OPEN** on every relayed number. Deploy spine is **Arm A, bump-independent** — it does not wait on F.W1.
15. **EVIDENCE LAW (R3-9 + inv-27)** — no gate closes on an assertion that something did not run; every green cites a covering run id; **a gate may not re-derive its own oracle** (KF.W4).
16. **CHECKPOINT-ACROSS-THE-TRANSACTION (R-2b, `carry/F-W1-CARRY.md` §CrossEdges item 9)** — the 8-item visual-regression set is only falsifiable as a **pair**: a capture taken **before** F.W1's atomic transaction (extent per §2.8 E-4) and a comparison taken **after**. A single post-hoc capture proves nothing, and **a checkpoint taken over the dirty worktree measures a tree no commit holds** (GAB-13 → G-F9-20 gates this one too). Its preconditions are owned elsewhere and are named at G-F9-23: **G-F9-9** (a card is opened, for GCM-22's insets) · **G-F9-13** (the coarse project, for the tooltip re-proportion) · **S1**'s admin entry (for `text-admin-label`). **F.W10 holds the terminal disposition** of all eight; F.W9 executes.
17. **PAIRED-BYTES LAW (G-F9-21)** — every edit to the shared §2.2 region lands in **both** specs in one commit. §2.8 **E-2** (the checkpoint block) is **DISCHARGED** — authored here, received by F.W10 at repair round 2, region `diff`s empty. **A THIRD was opened and closed inside round 3**: §2.8 **E-10(b)**, the D/i-1 cell's over-reaching *"verbatim"* label — **F.W10 authored the re-scoped bytes and F.W9 received them by splice**, region `diff` empty, the pair landing in one commit. **ROUND 4 OPENS FOUR MORE AND FIXES NONE OF THEM.** The obligations are no longer stated as a number anywhere — **G-F9-21's face carries the labelled enumeration `P-1..P-8` and a census ⟨cmd⟩ over it** (R4-7.2/.3; the prose count went stale in three consecutive rounds, including inside the round that corrected it). Outstanding: **P-1** (E-1's 9-vs-8 erratum) · **P-2** (E-9(b)'s `census §4.5` phantom anchor) · **P-4** (the FR-EMT-11 rider's period-scope) · **P-5** (the FR-TT-1 coordinate re-spelling) · **P-6 / P-7** (the `38` / `27` row numbers standing in the identity column where the canonical's ids are `C:D-11` / `L:L-i2`) · **P-8** (the three ⟨re-cut⟩ cells against the canonical homes) · **P-9** (the F-W1 anchor's CASE at the two checkpoint-block sites) ⟨*added at repair round 5, REST-57 (P5-8): the list enumerated seven and **omitted P-9 entirely** — **P-9 was OUTSTANDING by the list and invisible to every prose statement of what must close**, which is precisely what made REST-55's gate indeterminate*⟩. **P-3** (E-10(b)) is closed at the bytes. **Not one was fixed unilaterally** — a fix the other end has not authored is the rival-bytes failure this law exists to prevent, not a cure; and the seat-local corrections that need no permission (§2.0's roster, §2.0a's citations, §2.3 S2's re-label) were taken in full, which is the other half of the same law ⟨**re-qualified at repair round 5, REST-61 (P5-15).** That absolute was **falsified at five sites** — row numbers standing as identities in this wave's own voice (`ContourPreview 38` · `ImageUpload 27`), **two of them ordering LOCKS** (§4a-4 HARNESS-BEFORE-RIDER, §4a-8 MATRIX-BEFORE-COARSE), **one of which (§2.3 S5) was itself one of them.** All seat-local sites are now keyed to the banked ids §2.0 rows 4/12 restore — `fr-ContourPreview C:D-11` (aliases `L:L-12`) and `fr-ImageUpload L:L-i2`. **The two sites inside the shared §2.2 bytes (`:193`, `:199`) are LICENSED and stay** — P-6/P-7 disclose them and neither is fixed unilaterally. ⊘ *A lock keyed on a row number survives no re-numbering of the record, which is what §0.1 rule 1 exists to prevent.*⟩. **F.W9 does not author F.W10**; it publishes the bytes and F.W10 splices them. **A defect inside the shared region is disclosed and paired, never fixed unilaterally and never left unsaid** — the two failure modes this law exists to exclude are equal.

### §4b Cross-edges (declared from this end; each owed a reciprocal sentence in the named file — the X·V W4-D2 lesson, now law)

| to | edge |
|---|---|
| **F.W0 · re-ground** — **reciprocal WRITTEN, measured this seat** (§2.8 **E-9**; the earlier sittings' *"reciprocal owed"* is struck): F.W0 declares its half in **two** places — **F-W0 §4 G-9's Disjointness paragraph** (⟨cmd⟩ `grep -o '\*\*Disjointness with F\.W9 (one sentence, declared identically at both ends — R-5)\*\*' waves/F-W0.md`) and **F-W0 §6b's `F.W9 / F.W10` cross-edge row** (⟨cmd⟩ `grep -o '\| \*\*F\.W9 / F\.W10\*\* \| F\.W0 owns only the \*\*seats\*\*' waves/F-W0.md` — the `\|` are markdown table-cell escapes; the shell string carries bare pipes). Cited **structurally, never by line** — F-W0's line numbers moved between two probes of this single session (its own repair seat is concurrent), which is E-8 law (ii) demonstrating itself | **G-11 / G-12 ARE QUOTED, NEVER RE-PERFORMED (R-9.1).** **F-W0 §4 G-11** — *"ONE corrected anchor table published; every later wave quotes it"* — and **F-W0 §4 G-12** — *"Every later wave cites this table rather than a challenge file"*, superseded figures FORBIDDEN downstream — are **F.W0's once-owned acts** (both quotations produced by ⟨cmd⟩ at §2.8's DENOMINATOR PROVENANCE block; **cited by gate id, never by line — R2-2**), landing in `fourier/docs/tranches/F/SUBSTRATE-LEDGER.md` (**create**-marked at **F-W0 §2 Bounds, the `SUBSTRATE-LEDGER.md` row**). §2.8 **C-4 / C-6** are declared **inputs to / quotations of** G-11; §2.8 **E-1** cites G-12's own published **8**; §2.8's DENOMINATOR PROVENANCE table classifies every figure this spec states, and **routes the four banked figures G-12 does not yet publish (15/6 · 17 · 28 · 54/25/66) to G-12 for adoption**. Divergence from either table is a defect against F.W0, **not a rival act**. **RUNNER-SEAT DISJOINTNESS (R-5)**: **F-W0 §4 G-9 owns the SEAT** — cited **by gate id, never by line** (R2-2; the round-2 first sitting's surviving line cite into F-W0 is struck at §2.8 **E-8**) — ⟨cmd⟩ `grep -o '^### G-9 .*' waves/F-W0.md` → *"### G-9 — A unit-runner SEAT exists (the FLOOR's scope belongs to F.W9/W10)"* (runner installed, wired into `ci.yml`, ONE asserting spec, *"Explicitly NOT green-by-coverage"*); **F.W9 G-F9-1 owns the FLOOR** (spec population + thresholds) — one act each, no double-booking. **F.W0 HAS WRITTEN the mirror sentence** (the claim that it was owed is struck at §2.8 **E-9**), and its own G-9 GREEN clause carries the guard as **two spans, not one** — ⟨cmd⟩ `grep -o '\*\*Explicitly NOT green-by-coverage\*\* — F\.W0 must not pre-empt the floor.s scope\.' waves/F-W0.md` → *"**Explicitly NOT green-by-coverage** — F.W0 must not pre-empt the floor's scope."* **SUBSTRATE PRE-GATES FIRST**, and **G-F9-20** is its witness: F.W9 evidence taken before F.W0 measures (a) a producer dist that does not parse (**FR-NP-32 ≡ fr-PaperSidebar M1**, R-8 — cite both ids, never substitute) and (b) a tree no commit holds (GAB-13, 28 rows re-measured). Also F.W0's: F8-REACH-01/02 (**HOLD is RED** — FR-COB-20's harness branch cannot be authored for a file that may be DELETE; F.W0 rules the exact verb `KEEP_WITH_MOUNT \| ISOLATED_HARNESS \| DELETE`); MG-θ ≡ M7's tsconfig flags; ⟨re-cut⟩ **PAW-49**; FR-AFP-54/-55 manifest banding; fr-EasingPicker's stale-build fact; the Codex residue set (R3-9 · R3-16+R4-11+R5-9+R6-10 · R4-12 EVAPORATED · R5-3 · the still-registered `~/.codex/worktrees/d0be/fourier-analysis`); **R3-9 is routed here and carried in §2.7 as this wave's standing evidence law**. **OG-F1 / OG-F2 owner-owed, flagged inline, never presumed** |
| **F.W1 · atomic tri-package uplift** — **reciprocal WRITTEN** (§2.8 **E-9**; *"reciprocal owed"* struck): **F-W1 §4 Cross-edges item 9, the `F.W1 → F.W9/F.W10` row** — ⟨cmd⟩ `grep -o '9\. \*\*F\.W1 → F\.W9/F\.W10\.\*\* The minted visual-regression checkpoint set' waves/F-W1.md` → *"9. **F.W1 → F.W9/F.W10.** The minted visual-regression checkpoint set"*, and F-W1 books the set's homing in its own voice (⟨cmd⟩ `grep -o 'the 8-item visual-regression checkpoint set \| \*\*F\.W9\*\* home, \*\*F\.W10\*\* terminal-verify (R-2b)' waves/F-W1.md` — `\|` table-escaped as above) | ATOMIC land-or-lose — **the TWELVE-limb roster chartered at F-W1 §4 **Intra-wave order (binding)** step 4 (FR-EQC-7's vaul-vue gate INSIDE, per F-W1 §4 Cross-edges item 1 — the `F.W0 → F.W1` HARD-predecessor row) cited whole at §4a-3, never restated here** (R-4b; **R2-5** — the charter's own bytes now read TWELVE: ⟨cmd⟩ `grep -o 'The roster is [*]*[A-Z]* limbs and stays [a-z]*' waves/F-W1.md` → *"The roster is TWELVE limbs and stays twelve"*, §2.8 **E-4**; this row's two round-2-first-sitting line cites into F-W1 are struck at §2.8 **E-8**); precedes F.W3/W4 (§2 law). Consequences declared **from this end**: (i) GCM-42's console widening is **LOCKED** until F.W1 lands; (ii) F.W9's console-error gates may not be read as product defects across an unlanded F.W1; (iii) AA-2's re-measured 37-file / 38 `size="icon"` / 124 `variant=` break surface belongs in **F.W1's** table before any budget commits; (iv) **F.W1 must NOT be credited for FR-GIG-5's pagination-drain cure**; (v) the **G33 pin leg** (npm ls ELSPROBLEMS today) greens only via F.W1's transaction |
| **F.W3 / F.W4 · shadow retirement + frontend audit** — **SPLIT, measured this seat** (§2.8 **E-9**; the blanket *"reciprocal owed"* is struck as false on the F.W4 half): **F.W4's is WRITTEN** — **F-W4 §5.2 Cross-edges'** `→ F.W9/W10` row (⟨cmd⟩ `grep -o '\- \*\*→ F\.W9/W10\*\*: axe on `/paper` (PS D-B2)' waves/F-W4.md`), which names the same four legs as `carry/F-W4-CARRY.md` §CrossEdges plus the FR-AFP-42/FR-GFC-27 component-test seats. **F.W3's is GENUINELY OWED and stays owed — RE-MEASURED at round 3 against §X.1-v4, the definitive partition F-W3 published FIRST this round** (R3-1a; heading-existence probed before citation, per this file's standing probe law: ⟨cmd⟩ `grep -o '^### §X\.1-v4 — THE DEFINITIVE FILE-CRITERION PARTITION' waves/F-W3.md` → *"### §X.1-v4 — THE DEFINITIVE FILE-CRITERION PARTITION"*). F-W3 still carries no F.W9 cross-edge: ⟨cmd⟩ `grep -oE 'F\.W9(/(F\.)?W10)?' waves/F-W3.md \| sort -u` → **TWO distinct forms, `F.W9` and `F.W9/W10`** (set-membership, never a count of a live sibling — R3-3.10) ⟨**re-run and re-cut at repair round 5, REST-54 (P5-5, HIGH).** Round 4 published *"one distinct form"*. Re-run at a **matching** hash (`a89c3386f3f8`, identical to the PIN TABLE row — **the sibling had not moved**) returns **two**, the second at `F-W3.md:783` inside a round-4 R4-10 clause. **The CONCLUSION survives** — that occurrence is a census-roster mention, not a cross-edge, so F.W3's reciprocal is still genuinely owed — **but the published receipt was not the output of the published command**, which R4-1's opening law forbids outright. ⊘ **The standing lesson, banked here in one line: _a hash proves the bytes did not move; it does not prove the paste was produced by the command._**⟩, and its whole context is one routing cell of v4's handover table — ⟨cmd⟩ `grep -oE 'the seam; test seat → F\.W9/W10.{0,18}' waves/F-W3.md`, a **pipe-free and backtick-free** probe by construction (a ⟨cmd⟩ carrying either would break its own span or its own table cell — E-9(f)'s lesson obeyed at authoring rather than repaired a round later), whose output names the seam and its holder in one span: *"the seam; test seat → F.W9/W10 \| **F.W4** \|"* (the `\|` are this table's cell escapes; F-W3's bytes carry bare pipes). **A routing cell is not a declared edge**, and the seam it routes is booked F.W4 — which is the disposition §2.2's FR-AFP-42 cell already carries, so v4 confirms this spec rather than moving it. **v4 CONSUMED, and it moves nothing here**: its holders take **zero-homed** residue id-instances only, and every identity F.W9 books is homed at §2.2 of this file, so no F.W9 identity enters the partition; its two dispositive rulings — `fr-VisualizationView L-26` → **F.W3** and the `fr-GalleryAdminBanner GAB-25` entry into F.W4's hand-off — touch **no** row this wave books (F.W9's GAB rows are GAB-10, GAB-4's live-region half at F.W4, GAB-13 at F.W0 and GAB-29 as SS-3/SS-4 input; **`(fr-VisualizationView, L-26)` — the PAIR v4 rules — appears nowhere in this spec**). **The warrant is RECORD-QUALIFIED at round 4** (PASS-4 **D6**; R3-5.3 made the (record, id) pair the booking test on exactly this ground, and the round-3 wording was the bare-token form that ruling convicted at F-W3's g19): bare `L-26` is a **live multi-record homonym class** in the frozen corpus — ⟨cmd⟩ `grep -l 'L-26' "$R"/fr-*.md` returns **five files**, pasted whole: `fr-AdminFlaggedPanel.md` · `fr-AdminUserList.md` · `fr-FourierMorphDemo.md` · `fr-GalleryAdminBanner.md` · `fr-VisualizationView.md` (the fourth's hits are instrument prose rather than a banked head, which is why the class is *five files* and not *five rows* — the distinction is stated rather than filtered) — **and this spec BOOKS one of its members**, `(fr-AdminFlaggedPanel, FR-AFP-49)`, under its own canonical name with `L-26` carried **beside** it as an alias (§2.0 row 2, anti-rename). The conclusion is unchanged and now falsifiable: v4 moves `(fr-VisualizationView, L-26)`, F.W9 books `(fr-AdminFlaggedPanel, FR-AFP-49)`, and the two are different rows in different records. **Anti-rename holds in both directions: F.W9 re-cuts nothing v4 assigns, and v4 renames nothing F.W9 books** | **CURE OWNERSHIP: F.W9 owns gates, never cures.** Required before the matching gate is admissible: B-3/R-2's un-fixme (G-F9-8) · FR-TT-1's 17 naming legs (G-F9-5/-22) · FR-EQR-6's tabindex+label · D·D-B2's `aria-label` ×2 · GCM-4's `<DialogTitle>` · PaperSidebar D-B2's token + C-M4's `CollapsibleTrigger` restructure · PAW-12's predicate split · FR-GFC-27's rider · EasingPicker C/m-6 · FR-AFP-42's mountable seam. **The vitest-floor DECISION is F.W4's — `formation/fourier/CENSUS-2026-08-03.md` §4 item 5, the census's only unit-test-floor DECISION clause (the `§4.5` spelling is struck at §2.8 E-9); F.W9 EXECUTES it** (⟨re-cut⟩ PAW-50) |
| **F.W4 · CWV order-lock (FR-AH-1)** | **HARD ORDER-LOCK on G-F9-18** — any LCP taken before F.W4's async boundary + generator-side payload prune measures `sun.json` (450,631 B / 82.5 % long-float literals / 75,210 B dead; FR-AH-7 348,707 B; FR-AH-2 402,944 B). The value.js **Q14** escalation is the named precedent. Trace after F.W4 or a baseline stated-invalid on its face. FR-GIG-15 rider: the three-rung INP floor is unreachable in the shipped tree — **do not cite `lane-frontend.md` §6's `INP / long-task discipline` row as hygiene** (the `:568` line form is the RECORD's own words, quoted never adopted — §2.8 **E-9**) |
| **F.W5–W8 / SS-4 · CRUD-provenance union** — **both-ends declaration required** | **F.W9's inv-28 fail-closed gate is a DEPLOY gate and must never be read as an API-shape gate.** SS-4 must name the **TA-4 value-side atomdiff restoration** a PREREQUISITE **or re-scope the contract explicitly** — `lib/crud/atomdiff.ts` + `atomDiff` on `PaletteVersion` were excised at T.W1/TA-4, so **`fourier-analysis/docs/tranches/J/design/J-diff-shape.md` §6**'s close-gate probes cannot run value-side (**path-qualified this seat** — the authority lives in the FOURIER tree, not value.js's, and the earlier sittings' bare filename was un-resolvable from this repo: ⟨cmd⟩ `grep -o '^## §6 — The close-gate clause (both conformance probes assert against THIS doc)' $F/docs/tranches/J/design/J-diff-shape.md`, `$F` = the fourier checkout at HEAD `cd26c65`; §2.8 **E-9**) and **a co-signature over un-runnable probes is void**. SS-4's owner rulings are **FLAGGED INLINE, never presumed**: trie-vs-KISS (the documented anti-tree guardrail at `atomdiff.py:12-14` is the **INCUMBENT** — dissent recorded), remix-vs-fork, born-visibility, visibility-enum, version-`_id`, redaction parity, pagination idiom. FR-GV-36's dead-API fact is declared here; **the API repair is not booked in F.W9**. Neither side's edits gate the other's waves |
| **F.W10 · coordination discharge + close** — reciprocal present (F-W10 §4) | **The digraph splits here.** F-W10 §2.2 carries the authoritative 26-identity table; **G-F9-21 ≡ G-F10-6 are twins and neither spec stamps before it closes.** **F.W9 EXECUTES the P-12 packet; F.W10 owns the terminal-disposition record only.** Handed to F.W9 per F-W10 §4: AA-44 · FR-AFP-42/-49 · FR-GSB-17 · FR-USB-16 · FR-EQR-6 · FR-EQR-31 · FR-EMT-11 · FunctionInput C-13 · C·D-28 + D·D-B2's axe leg · GCM-42 · FR-GV-36 · GAB-10's coverage half · FV-26 · App C-13 · `fr-ContourPreview C:D-11` (aliases `L:L-12`; §2.0 row 4 — row number `38` retired, REST-61) · `fr-ImageUpload L:L-i2` (§2.0 row 12 — row number `27` retired, REST-61) · PAW-34 · PAW-12's rider · PaperView D/i-1 · PaperSidebar D-B2/C-M4/M7 · FR-AUL-16's execution limbs · FR-COB-20's harness branch. Three ⟨re-cut⟩ away: FR-TT-1 → F.W10, PAW-49 → F.W0, PAW-50's decision → F.W4. **Added by the L-20 round-1 repair (R-2b)**: the **8-item F.W1 visual-regression checkpoint set** (`carry/F-W1-CARRY.md` **§CrossEdges item 9** — by path and by row label, R-3 / R2-2) — **home F.W9, execution F.W9, terminal disposition F.W10**, booked in the shared §2.2 region and **RECEIVED by F.W10 by splice at repair round 2** (R2-6a, performed both ends; ⟨cmd⟩ `diff <(awk '/^\*\*THE F.W1 VISUAL-REGRESSION/,/^\*\*Born-RED witness/' F-W9.md) <(awk '/^\*\*THE F.W1 VISUAL-REGRESSION/,/^\*\*Born-RED witness/' F-W10.md)` → **empty**, this seat — §2.8 **E-2** · §4a-17 · G-F9-21 · G-F9-23). **F.W9 owns the deploy spine; F.W10 owns the correspondence** (ASK-3 · ASK-5 · INV-22-COLOR · CASCADE-VJS · FN-1..FN-7) — census-declared split. FR-AUL-16's and FR-AH-45's terminal homes are decided **AT F.W10** |
| **X-W11 G8 · real-Safari evidence cell** | **NON-OVERLAP, declared to prevent a false green.** F.W9's CWV and axe evidence is **CHROMIUM** evidence (`playwright.config.ts:46-51`, one project) and **may NEVER be cited as Safari evidence.** The intake's five corroborating rows (V6-32 controls 0/44 · V6-33 iOS Simulator Safari 0 · V6-34 desktop Safari 0 · CM-22 0/44 + D2 0/887 · XF-6 zero Safari on every surface) support G8's premise and clear none of it. Webkit-vs-Safari-app separation law: `X-W6 §MATRIX` / I-20. **FR-USB-16's coarse project (G-F9-13) is a matrix change, NOT a Safari cell** — it discharges no G8 row. OG-V1/OG-V2/OG-V3 remain owner-gated |
| **SS-6 · glass-ui BH relay (owner edict)** | **ASK-ONLY; producer rows NEVER become frontend hacks.** Riding the standing batched letter, not F.W9's bounds: M1's dist-generation bug (producer source at HEAD is intact) · L-10/D-M4's ramp-headroom rebaseline (four failing light-arm stops incl. **3.58 at stop 11**) · GAB's tier-tint home + the 3.26:1 label rung · FR-EQR's light-arm `--success` 2.175:1 · AA-3's violet `batch` tone with no home in 7.0.0's five-member TONES · FR-CP-11/27 · AppHeader's undeclared cm-serif variable + light `--tier-featured`. **F.W9 may not cure any of these consumer-side to turn a gate green** |
| **LATEX-PAPER-RELAY (register created at F.W10)** | **ASK-ONLY. HARD LOCK from fr-PaperSidebar M2: F.W4 must not rename `.sidebar-top-btn` before the relay lands** — latex-paper's shipped `useSidebarFollow` hard-codes fourier's private scoped class in its pointer-exemption, so a rename silently converts scroll-to-top clicks into sticky manual-override suspensions (D-B3's and D-M8's cures both touch it). **Any `/paper` spec selector F.W9 authors must not depend on that class either.** Anchor law **SC-3**: installed latex-paper 0.2.1 ships no composable src — cite `dist/vue.js`, never producer-src. **L-11** (a third ungated rAF clock, no PRM gate) rides the same relay and amends lane-frontend §8 |
| **SS-3 / SS-4 · spec authoring (NO-WAVE-OWNER drain)** | **FR-AUL-16 and FR-AH-45 have no honest home today**; P-12 proposes their execution limbs here and F.W10 decides terminal. **If the widening is declined, the decline is recorded explicitly with rationale (M-25)** — leaving them un-homed re-creates the exact blind spot that let every row in this packet open and stay open. Also routed as spec input: FR-COB-20's no-test-for-wrappers observation · AA-45/-46/-47 · FR-AFP-51/-52 · **FR-CP-43/-44/-45** (verbatim law: take D's CLAIMS and L/C's LINES; **re-resolve every anchor before quoting into a wave spec** — honoured at §2.8 C-4) · **fr-App C-12 as a NEGATIVE record** (App is API-inert; a negative finding is a terminal disposition — do not manufacture work) · **GAB-29** (MetricPill — the falsifier that keeps the wave off two dead primitives) |
| **KF.W4 · transferable gate-design law (intake §3)** | **BINDS EVERY GATE THIS WAVE AUTHORS** — (a) one immutable map keyed by physical coordinate; (b) reauthenticate every payload node and checksum row at use; (c) read every declared predecessor authority, never allowlist; (d) derive every envelope assertion from the pinned artifact; **and a gate may not re-derive its own oracle.** Joined by **R3-9**. **No absorption of KF rows**: B18-12/13/14, the `345/12/57 @ 8281638c` boundary block and B18-27 are **KF.W10's alone** (COHESION §3.1 — no id double-homed) |
| **parse-that → value.js → consumers (PLAW-BIND route law)** | **FORBIDDEN EDGE, declared to prove it is not minted here.** F.W9's evidence may not cite parser work as fourier coverage. Measured clean: 39 hits repo-wide, **0 of them source**; fourier's parsing is a 117-line hand-rolled regex (`web/src/lib/colors.ts`, **no `oklch()` arm**) — the W.L5 item-2 deletion target and the I-9 re-trigger sentinel, **owned by F.W2**. MG-ι relays into the V·π ledger with the owner-mail flag; **no fourier wave adopts it** |
| **fourier sub-session (COMMISSION §2)** — ask-only | **value.js declares, it never edits fourier's tree** (see §1b's write-seat declaration). Their writes: the M.W2/W3/W4/W11 board dispositions on `docs/tranches/M/PROGRESS.md`; the fourier-side INBOX + CLAUDE.md (census F.W0's act — fourier has no E13 analogue and its live value.js mail path is the flat `docs/tranches/N/`); facility-19 tracking (currently UNTRACKED `??`); the two parity-probe residuals re-homed to their M.W12. **F.W9 relays the deploy-spine asks and verifies receipt.** ASK-3 and INV-22-COLOR **infrastructure execution is value.js-maintainer-owned and outside every F wave's product bounds** |

---

## §5 Excluded (nothing silently dropped; every exclusion reasoned)

**Carry law (R-3).** The only citable carries are `carry/F-W1-CARRY.md` and `carry/F-W4-CARRY.md`, **by path**; *"the CARRY"* with no path is **forbidden vocabulary** in this file. Both are read in full against this wave. **Cited by path and by row label, never by line — R2-2** (the round-2 first sitting's `:555` / `:246` cites are struck at §2.8 **E-8**): `carry/F-W4-CARRY.md` **§CrossEdges, the `F.W9/W10` row** is booked four-for-four — ⟨cmd⟩ `grep -o '| \*\*F\.W9/W10\*\* | test + visual seats |.*' carry/F-W4-CARRY.md` → *"| **F.W9/W10** | test + visual seats | axe on `/paper` (PS D-B2) · admin-mode e2e (AA-44/GAB-10) · the PaperSidebar Collapsible-root guard re-key (PS C-M4) · `resolveFigure` set-equality (PAW-12) — **⊘ harness before rider, flag before split (PAW-49/PAW-50).** |"* (= PS D-B2 · AA-44/GAB-10 · PS C-M4 · PAW-12, four-for-four) — and **`carry/F-W1-CARRY.md` §CrossEdges item 9's 8-item checkpoint set is BOOKED at §2.2 with gate G-F9-23** (R-2b — it was dropped by both twins before this repair). **No row of either carry is excluded below.**

| excluded | reason |
|---|---|
| **Every cure named in §2** — FR-EQR-6's tabindex/`aria-label`, D·D-B2's two labels, FR-TT-1's 17 naming legs + shim, GCM-4's `<DialogTitle>`, PaperSidebar D-B2's token + C-M4's `CollapsibleTrigger`, PAW-12's predicate split, GAB-4's `aria-busy`, B-3's un-fixme, FR-GFC-27's rider, FV-31, EasingPicker C/m-6 | **F.W3/W4 owns every cure; F.W9 owns gates only.** Cited as admissibility preconditions, never booked |
| **N-2's a11y label cure** (`aria-pressed`, + `role="switch"` per the producer) — the *cure* half of G-F9-12's same-commit rider | **F.W4's, banked** (`fr-DarkModeToggle` N-2's own terminal cell, quoted at §4a-12 by ⟨cmd⟩) — added at round 3 under **R3-7.3** / PASS-3 D4, which convicted this table for enumerating eleven F.W3/W4 cures and omitting the one the file's own gate falsifier depends on. F.W9 owns the **spec-side locator edit** and the sequencing; the label is a cure and **F.W9 owns no cure** |
| `web/tsconfig.json` flags — MG-θ ≡ M7's `noUnusedLocals`/`noUnusedParameters`, and ⟨re-cut⟩ **PAW-49**'s `noUncheckedIndexedAccess` | **F.W0's**: one substrate edit, two records, **booked once**. PAW-49 is carried in §2.6 for parity with F-W10 §2.2 and is explicitly **not booked here** |
| **M1 / GAB-13 / F8-REACH-01/02 dispositions · the Codex residue verbs · OG-F1/OG-F2** | F.W0's rows; carried in §2.1/§4a-2 and G-F9-20 for gating only; owner rulings flagged inline, never presumed |
| **The ten band-leg identities the canonical homes away from F.W9** — `(fr-AdminFlaggedPanel, FR-AFP-42)` · `(fr-EquationResult, FR-EQR-6)` · `(fr-EquationView, D·D-B2)` · `(fr-EquationView, C·D-28)` · `(fr-PaperArticleWindow, PAW-12)` · `(fr-PaperArticleWindow, PAW-49)` · `(fr-PaperSidebar, D-B2)` · `(fr-PaperSidebar, C-M4)` · `(fr-PaperSidebar, M7)` · `(fr-Tooltip, FR-TT-1)` | **CITED TO THEIR HOLDERS at §2.0a, never booked** (R4-10's second clause). §2.2's shared table splits the **band token** `F.W9/W10`; `$C` §1 resolves each row's **home** by the first-arrow rule, and nine of these are F.W3/F.W4/F.W0 rows carrying an F.W9/W10 **leg** F.W9 executes. `C·D-28` is the tenth and is **no canonical row at all** (⟨cmd⟩ `grep -c 'C·D-28' "$C"` → **0**) — a fold-list prose mention of the coverage family whose head is `(fr-AdminUserList, FR-AUL-16)` → **NWO→SS-3**. **Nothing is dropped: every one is named, homed and reasoned** |
| **PAW-50's DECISION leg** (as opposed to its execution) | **The ⟨re-cut → F.W4⟩ is STRUCK at round 4**: `$C` homes **`(fr-PaperArticleWindow, PAW-50)` at F.W9** and R4-10 forbids a wave re-cutting a canonical home, so the id is **BOOKED WHOLE** at §2.0 row 15 / §2.4. What remains F.W4's is the **authorizing clause** — `$M/CENSUS-2026-08-03.md` **§4 item 5**, the census's only unit-test-floor DECISION clause (`§4.5` struck, §2.8 **E-9**). **F.W9 executes what that clause authorizes; an authorization is not a home** |
| **FR-TT-1** (as a booking) | **The ⟨re-cut → F.W10⟩ is STRUCK at round 4**: `$C` homes `(fr-Tooltip, FR-TT-1)` at **F.W4** (legs F.W3, F.W9/W10) and **neither twin books it**. It still **binds** F.W9 through the promotion lock (G-F9-22) without being booked here — which is what this row always said it was |
| **`conformance/PASS-1/F-W9-CHECK.md` as a closure operand** | **STRUCK at round 4 (R4-3.1 / R3-4; PASS-4 D1).** A check file is a **prior run**, never a quotable operand — *"a closure/census transcript whose LHS is a count copied from any check file FAILS BY CONSTRUCTION"*. It is named in the masthead as a prior run and appears in no gate cell and in no denominator. **The same exclusion binds every superseded census**: `$C` is the only one this file names |
| **The API repair behind FR-GV-36's "dead API"** | The **edge** to the deploy-spine reckoning is declared (G-F9-15/-16); the repair is **SS-4 / the value.js API row's**, not F.W9's |
| **ASK-3 · ASK-5 · INV-22-COLOR · CASCADE-VJS · FN-1..FN-7 · the 2.0.0 note · O-14 parity** | **F.W10 owns the correspondence; F.W9 owns the deploy spine** — the census-declared split. ASK-3's rsync/deploy-dir infrastructure execution is maintainer-owned and outside every F wave's product bounds |
| **Every producer cure** — M1's dist generation, glass ramp headroom, GAB tier-tint, `--success` 2.175:1, `--tier-featured`, latex-paper's PAW-1/2/11/28/45/46, emitter semantics, the flattener | **SS-6 / the LATEX-PAPER-RELAY.** Producer work in producer trees: **this wave asks, it never edits — and a producer row never becomes a frontend hack to turn a gate green** |
| **The pagination-drain credit (FR-GIG-5) · `lane-frontend.md` §6's `INP / long-task discipline` row as hygiene** | Locks live in F.W1's spec and G-F9-18's rider — recorded here so neither is claimed by this wave's greens. **The `lane-frontend :[5]68` spelling this cell carried is STRUCK** (round 3, R3-3.5; §2.8 **E-10(a)**): it was the fourth `:568` in the file and the only one in the wave's own voice, and it survived three rounds because the certifying probe matched `file.md:N` and could not see a space-formed cite. The row label is the anchor at both of this file's statements of it (here and §4b) |
| **Every LIMB behind the 8 checkpoint items** — GCM-22's `p-0` retirement, FR-CP-13's gap decision, the `--shadow-cartoon` / Badge / `cartoon-surface` paint — **⊕ the lucide and Button legs that move with them, named for the transaction's completeness and bound by NO gate in this file** (R-4b licenses naming a limb *where a local gate binds one*; the checkpoint set binds five of the seven named and these two it does not — disclosed at round 3 rather than trimmed, §2.8 **E-10(h)**, because a limb dropped from an atomic transaction's roster is the worse error) | **The TWELVE-limb transaction chartered at F-W1 §4 **Intra-wave order (binding)** step 4 owns them** (cited whole, §4a-3; extent per **R2-5** / §2.8 **E-4**, by anchor and never by line — R2-2). F.W9 books the **checkpoint** (§2.2, G-F9-23) and **no limb**; a green at G-F9-23 is evidence of an instrument, never of a repair |
| **The `.disclosure-content` body register cure** (checkpoint item 3; `grep -rl` in `web/src/` → 0 — producer-side) | **SS-6 relay, ASK-ONLY.** The consumer-visible render is checkpointed; **a producer row never becomes a frontend hack to turn G-F9-23 green** |
| **The SS-13 residues** named in the carry's cross-edge 9 tail | Carried as a **named class** at §2.2, **not absorbed**: each residue stays with the record that names it and rides that record's wave. F.W9's claim over them is the instrument only — **no id re-booked, none renamed** |
| **`visualization-ux.spec.ts:212`'s fixme** (`save_contour_then_recompute`) | The **fifth** `test.fixme` call site the G-F9-8 command returns. **F.W3-homed by its own in-file comment ("Un-skip at W3")** — carved by name at G-F9-8 so the gate's four keystones match the command it cites, and so absence cannot be read as oversight |
| **FV-30 · D-20's archaeology half · PAW-33** | Ride banked rows elsewhere (BasisCanvas D-4/BC-2/C-2a + AnimationControls C-12 · FR-EQR-27's correction · the C-19 ⊕ PAW-33 pin-truth row, F-W10 §2.5) — no new booking |
| **Any edit to `registry/adjudicated/fr-*.md` or to `F-W10.md`** | Read-only bounds + anti-rename — **the re-cut is a table in two specs and one census addendum**; F.W9 does not author F.W10 |
| **The 14 boilerplate routing-law lines, the 12 verdict-prose mentions, and the 7 boilerplate-only records** | Boilerplate **mints no rows** (the RE-CUT-0 dissent's explicit warning); verdict prose is dated adjudication bytes, immutable, annotated by the addendum only. The REAL rows (FR-AUL-16 · FR-COB-20 · F8-REACH-01/02) enter by mechanism at §2.2's entrant block |
| **KF.W10's rows** — B18-12/13/14, the `345/12/57 @ 8281638c` boundary block, B18-27; **the keyframes half of ASK-5** | KF's close / KF's mail seat — **declared, not absorbed; no id double-homed** (COHESION §3.1) |
| **Safari evidence of any kind** | F.W9's matrix is chromium + one coarse project. **X-W11 G8's cell is not touched, not discharged, not partially credited** |
| **Every fourier-tree write** (M board disposal, their INBOX/CLAUDE.md, facility-19 tracking) | COMMISSION §2 — asked in their sub-session; F.W9 relays and verifies receipt |
| **R4-12's row contents** | Consume-nothing lock — and moot: the file evaporated (fold-measured) |
| **A direct parse-that → fourier credit edge** | **FORBIDDEN** by route law; declared at §4b so it is provably not minted here |
| **Any product-source open in either tree; any status advance beyond `planned`** | **The execution gate** — nothing opens, writes, builds or runs product source until the owner's begin-word |

---

**Close act.** When **G-F9-1..23** are GREEN — each citing a covering run id and a committed SHA (inv-27; R3-9), none re-deriving its own oracle — F.W9 stamps **IMPLEMENTED**, hands its terminal records to F.W10 (**VERIFIED is F.W10's close to stamp, never this one's**), and files the §4.10 census-addendum record plus the E13 INBOX rows. E13: no close with UNREAD mail — the 4-path sweep opens every session. **L-18 still applies**: no wave is ACCEPTED until two quartet gestalt passes adjudicated by a fresh Fable instance. The bases that apply here: a harness stood up ahead of the decision that authorizes it; a coverage gate greened by a vacuous assertion of the exact class this wave un-vacuates; an axe artifact argued green over 17 open naming legs; a console-guard widening landed before F.W1 and blamed on the product; a CWV number taken while `sun.json` is still in the entry chunk; a containerization "audit" that re-read the compose file it was supposed to falsify; a deploy called current from a dated 44-behind number nobody re-measured; a re-cut that looks complete because both specs agree — while agreeing on the same wrong count (§2.8 E-1); **a checkpoint set "instrumented" by a single post-transaction capture that can compare against nothing (§4a-16)**; **a gate that re-resolves an anchor or a denominator F.W0's G-11/G-12 already own, and calls the divergence a measurement (§2.8)**; and **a carry row dropped by both halves of a split because each half assumed the other held it — the exact escape `carry/F-W1-CARRY.md` §CrossEdges item 9 suffered until R-2b homed it here**; **a repair seat that certifies its own receipts without re-running them (§2.8 E-8)**; **a citation that names a section which does not exist — verified as to its words and never as to its address (§2.8 E-9)**; and **a certification written over a probe that cannot express the class it certifies about — the round-3 conviction, where three rounds widened a probe's STEM list while its FORM stayed blind to the one surviving cite (§2.8 E-10(a))**. That last base is the one two repair rounds walked past: an idiom that abolishes line numbers in favour of `§`-anchors buys nothing if no seat ever greps for the heading, because an unfalsifiable address is worse than a stale one — a stale line at least fails loudly when it is read. **Standing probe law for every later seat of this file: quote by command AND anchor by command AND scope by command; a `§` cited into any external authority is greped for its own existence before it is cited, a *"verbatim"* label is `grep -F`'d over its WHOLE labelled span and not over the fragment the seat chose to paste, and a probe that certifies a CLASS is written over the class's SHAPES — `NAME[ .]:[0-9]+`, not a list of stems — because widening the wrong axis of a probe three times running is how a false certification survives three repair rounds (§2.8 E-10(a)). Every ⟨cmd⟩ resolves from a declared base (§2.8a), and no receipt banks a count of a live sibling or of this file itself (R3-3.10).**

**ROUND 4 ADDS FOUR BASES AND ONE LAW.** The bases: **a roster taken from a check file, a prior pass, or a wave's own §-arithmetic rather than from the stamped census** — the form R3-4 declared FAILS BY CONSTRUCTION and that survived three rounds here because the repair seat read the directive index instead of the standing law (§2.8 **E-11(a)**; R3-8: *absence from a directive index lifts nothing*); **a wave re-cutting a home the census carries**, which is how PAW-49, PAW-50 and FR-TT-1 came to state three homes `$C` does not (**E-11(b)**); **a paired obligation counted in prose instead of enumerated**, which under-counted this file's own gate face in three consecutive rounds including the round that corrected it (**E-11(d)**); and **a strike rendered at one of its occurrences and certified over all of them** — `:[5]68` was bracketed and its twin `§1 :[4]8` was not, at two sites, one of them inside the strike itself (**E-11(b)**). **The law: a certification of a CLASS must enumerate, and the enumeration must carry a command that re-derives it from the bytes.** Every count word this file still needs is now a list with a ⟨cmd⟩ over the list — the roster at §2.0, the paired obligations on G-F9-21's face, the labelled spans at §2.8a — because four rounds have now proved that the failure mode is never the axis and always the enumeration under it. **And every sibling this file quotes carries its sha256-at-quote-time (§2.8a's PIN TABLE, R4-8.3), so a later rewrite of an authority is detectable rather than deceptive.**

Until then: **planned**, born-RED, every witness named above.
