# X.F.W9 - The Deploy Spine, the Coverage-Void Execution, and the Evidence Floor (frontend half)

⟨X·F · **status: planned** · half: frontend · twice-authored 2026-08-28 (Fable seat · Opus seat) and folded WHOLE by the fresh-Fable agglomeration seat (M-23) under M-25 — the F.W9 intake CARRY consumed **whole**: 36 rows · 22 gates · 13 cross-edges · zero silent drops (dedupe by identity · cure-shape locks and sequencing edicts carried · routed by mechanism; transcription-only is DEFECTIVE) · L-19 (every gate names a live witness) · L-20 validated against `docs/precepts/instructions/` (TRANCHE-AND-WAVE-SPEC · STYLE · ORCHESTRATION · CONSUMING · LESSONS-LEARNED) · idiom: `docs/tranches/X/waves/W0..W11.md` bounds/gates/provenance ⟨…⟩ cut to the `docs/tranches/X/parse-that/waves/` lean form · **twin**: `F-W10.md` (fold-authored 2026-08-25) — its §2.2 table carried here **byte-identical** (spliced from the twin, never retyped) · witnesses re-measured read-only 2026-08-28 against fourier HEAD `cd26c65` (receipts inline; corrections in §2.8); every deploy/evidence cell re-measures at open⟩

**Alias law**: `F.W9` denotes `X.F.W9` — a search alias, never a rename. The CARRY's `waves/W9.md` name lands as `F-W9.md`; same identity, alias recorded (the twin correspondingly is `F-W10.md`). Banked ids keep their identity for life; nothing below is re-booked.

**Scope.** The F-side **execution** wave, in three limbs that share one law (*a green must be cited, never asserted*):

1. **The deploy spine** — M.W2/W3/W4 re-homed out of the dead M board and executed: readiness separated from liveness, the API arm made fail-closed on a same-SHA green CI run (inv-28), the silent rollback killed and the inv-31 observability floor stood up.
2. **The coverage void** — the P-12 packet the census gives to no wave at all: a unit harness, a lint floor, five e2e/axe seats (admin tab · `/equation` · `/paper` · un-vacuate · the coarse-pointer matrix), authored **once each** and cited many times.
3. **The evidence floor** — M.W11's half: a green inv-27 run id behind every claimed green, CWV/INP under a hard order-lock, axe artifacts, and a containerization fidelity audit taken **as-running, not as-declared**.

**F.W9 owns GATES. F.W9 owns no CURE.** Every repair named below is homed at F.W0/W1/W3/W4 or rides a producer relay; this wave's claim is the instrument that would have caught it. F.W9 owns the deploy spine; F.W10 owns the correspondence (census-declared split).

**Four-verb**: AUDITED **YES** (registry-adjudicated; re-measured 2026-08-28) · SPECIFIED **YES — by this file** · IMPLEMENTED **NO** · VERIFIED **NO**. **Opens after**: F.W0 (substrate pre-gates, hard) → F.W1 (atomic) → F.W3/W4 (the cures this wave gates). **Agents**: 3 serial (deploy-spine · seats+harness · evidence), never parallel — units 2 and 3 both write `web/package.json` and `.github/workflows/ci.yml`. **Hard gate**: G-F9-1..22, all born-RED. **EXECUTION IS NOT AUTHORIZED BY THIS FILE** — status stays `planned`; nothing opens product source until the owner's begin-word.

**Goal criterion.** This wave succeeds if, when work ends, **no claim in the X·F program is green by assertion** — the host runs HEAD, a failed deploy is loud in a watched channel, every gate cites a covering run id, and the five instruments exist that make the packet's ungated rows *falsifiable*. A gate that names no live witness is not a gate; a green that names no run id is not a green.

**Binding law.** M-25 (a spec ignoring adjudicated rows is DEFECTIVE; transcription-only is DEFECTIVE) · L-19 · L-20 · E13 (no wave closes with UNREAD mail) · **R3-9** (an assertion of non-execution is *recorded*, never cited as a method guarantee) · **KF.W4's transferable gate-design law**, binding on every gate authored here: (a) one immutable map keyed by physical coordinate; (b) reauthenticate every payload node and checksum row at use; (c) read every declared predecessor authority, never allowlist; (d) derive every envelope assertion from the pinned artifact; **and a gate may not re-derive its own oracle.** Two standing laws shape every row: **the registry is READ-ONLY** (the F.W9/F.W10 re-cut is a table in two specs plus a census addendum, never an edit to `fr-*.md`), and **producer rows never become frontend hacks** (glass → SS-6; latex-paper → the F.W10 register; fourier-tree writes are asked, never made — COMMISSION §2).

---

## §1 Bounds

### §1a Owned — value.js tree (spec + ledger; writes only post begin-word)

| # | Path | Act |
|---|------|-----|
| 1 | `docs/tranches/X/fourier/waves/F-W9.md` | CREATE — this spec. §2.2's table bytes are identical to `F-W10.md`'s |
| 2 | `docs/tranches/V/megatranche/formation/fourier/CENSUS-ADDENDUM-2026-08-25.md` | MODIFY — **co-owned with F.W10**; F.W9 adds only the §4.10 deploy-spine + coverage-void record and §2.8's errata. Addendum-**beside** (E-3): the dated `CENSUS-2026-08-03.md` bytes are never rewritten |
| 3 | `docs/tranches/V/coordination/INBOX.md` | **APPEND-ONLY** (E13) — the F.W9 evidence rows and the deploy-spine relay. No existing row rewritten |

**Bounds reconciliation vs the CARRY.** Rows 1–3 adopt the CARRY's three value-side `boundsFiles` verbatim. Row 2 is *modify*, not create: F-W10 §1 already claims the CREATE — two waves cannot both create one file, so F.W9 takes the co-owned §4.10 carve and the §6.7/§4.11 cells stay F.W10's. Disjointness holds by section, and the two waves are serial at the boundary.

### §1b Claimed — fourier tree (EXECUTION bounds; the write-seat is flagged, never presumed)

**WRITE-SEAT DECLARATION (inline flag, M-25 — never presumed).** Every path below sits in `/Users/mkbabb/Programming/fourier-analysis`. COMMISSION §2 makes fourier-tree writes **asked, never made** from this seat. These bounds are therefore a **claim of ownership over the change**, not a licence to write. At execution the edits land either (a) in fourier's own sub-session against this spec, or (b) by this seat under an owner grant that names it. The begin-word settles which; **no seat presumes (b)**, and F-W5's standing sentence ("`fourier-analysis` is READ-ONLY, always") holds until it is lifted in writing.

| Path (fourier) | Act | Row it serves |
|---|---|---|
| `web/package.json` | MODIFY | `test` script + vitest devDependency + `lint` script — PAW-50 execution · FR-AUL-16 devDependency limb · M7 lint limb |
| `web/vitest.config.ts` | CREATE | harness-before-rider; **only after F.W4's decision** |
| `web/eslint.config.js` | CREATE | the lint floor (M7's second limb; tsconfig flags are F.W0's) |
| `web/playwright.config.ts` | MODIFY | the mobile/coarse project — `:46-51` is **one** `chromium` project today (re-measured 2026-08-28) |
| `web/e2e/gallery.spec.ts` | MODIFY | un-vacuate `:19-22` / `:49-53` / `:57-63` / `:66-82`; open a card; console guard error-only→`warn` (**LOCKED behind F.W1**) |
| `web/e2e/visualization-ux.spec.ts` | MODIFY | the axe keystone (`:26-43`) extended into the admin tab; the `:110`/`:133`/`:192` un-fixme rides F.W3/W4's B-3 cure |
| `web/e2e/visualization-crud.spec.ts` | MODIFY | AxeBuilder seat; the `:630` fixme |
| `web/e2e/visual-baseline.spec.ts` | MODIFY | `/equation` (`:34`, re-measured — CARRY cites `:35`) gains its first assertion |
| `web/e2e/paper-performance.spec.ts` | MODIFY | the `/paper` axe seat; the DarkModeToggle locator (`:328`) co-lands with N-2's label cure, same commit |
| `web/e2e/` — remaining 3 of **8** specs | MODIFY | **quote 8, never 9** (K11; re-measured 2026-08-28 → exactly 8 `*.spec.ts`) |
| `web/src/lib/figureDimensions.ts` | MODIFY | PAW-50's relocation target for `resolveFigure` — lands in whatever wave executes PAW-12 |
| `.github/workflows/ci.yml` | MODIFY | inv-27 run-id emission + the new unit job (three jobs today: `api-tests` `:37` · `web-build` `:77` · `e2e` `:101`) |
| `.github/workflows/deploy-pages.yml` | **READ** | the inv-28 reference implementation (`:48-57`) — mirrored, not edited, unless the gate demands |
| `scripts/deploy-hook.sh` | MODIFY | M.W2 (readiness ≠ liveness) · M.W3 (fail-closed inv-28) · M.W4 (kill silent rollback + inv-31) |
| `scripts/pages-deploy.sh` | MODIFY | inv-25 deploy-of-record (`cf_deployment_id`) |
| `web/Dockerfile` · `web/.dockerignore` · `api/Dockerfile` | AUDIT | containerization fidelity |
| `docker-compose.yml` · `docker-compose.prod.yml` | AUDIT | prod-overlay hardening verified as-**running** |
| `nginx/fourier.conf` | AUDIT | reconcile against the printf-inlined conf at `web/Dockerfile:33-43` (re-measured; CARRY cites `:31-42`) |
| `infra/apache/api-vhosts.conf.template` · `infra/apache/deploy.babb.dev.conf.template` | AUDIT | host-Apache fidelity |
| `docs/tranches/M/PROGRESS.md` | **ASK-ONLY** | the M.W2/W3/W4/W11 rows disposed explicitly on re-home — fourier's sub-session writes it (COMMISSION §2) |

### §1c Read-only bounds (no write)

`docs/tranches/V/megatranche/registry/adjudicated/fr-*.md` (all **66**, re-measured) — anti-rename; the re-cut is a table, not a rewrite · `formation/fourier/{CENSUS-2026-08-03.md, lane-docs.md, lane-frontend.md, lane-crud.md}` · `audit/codex-provenance/INTAKE-ADJUDICATION-2026-08-03.md` (dated; amended beside only) · `docs/tranches/X/fourier/waves/F-W10.md` (**the twin table; F.W9 does not author F.W10**) · `docs/precepts/**` (L-20 authority; **private submodule — CI checkout deliberately omits it, so no gate may assume it is readable in CI**).

### §1d Explicitly NOT owned

`web/tsconfig.json` — **F.W0's** (MG-θ ≡ M7's `noUnusedLocals`/`noUnusedParameters` = one substrate edit, two records, booked once; and the ⟨re-cut⟩ PAW-49 `noUncheckedIndexedAccess` flag) · **any fourier product source under `web/src/components/**`** — F.W3/W4 owns every cure, F.W9 owns gates only · value.js `{src,demo,api,test,e2e}/**` (execution gate) · `scripts/dev/dev.sh` (unowned dirty row, **NEVER**) · `glass-ui/**`, `latex-paper/**`, `keyframes.js/**` (reached only by relay letter) · `docs/tranches/X/{waves,keyframes,parse-that}/**` · `~/.codex/**`.

---

## §2 Carry

Thirty-six banked rows, agglomerated by mechanism. **A row's presence here is a disposition, not a work order** — coverage rows execute HERE; substrate rows are carried for gating only; the parity rows (§2.6) execute elsewhere and are carried so the set-difference closes.

### §2.1 RE-CUT-0 — the shared precondition (twin of F-W10 §2.1; recorded, not re-booked)

**BLOCKER (spec-integrity; blocks BOTH specs from stamping).** Re-measured 2026-08-28 in `registry/adjudicated/`: `grep -o 'F\.W9/W10' fr-*.md | wc -l` → **54**; `grep -l 'F\.W9/W10' fr-*.md | wc -l` → **25 records**; bare `F.W10` routing → **0**; `ls fr-*.md | wc -l` → **66**. Identical to the 2026-08-25 fold. This is corroboration of a pinned artifact, **not a re-derivation of the oracle** (KF.W4(d)) — the 26-identity taxonomy is F-W10 §2.1's and is consumed, never recomputed here: 26 routing identities · 14 routing-law boilerplate lines (**zero rows minted**) · 2 same-identity restatements · 12 verdict-prose mentions (dated, immutable). Seven records yield zero re-cut rows. Cure: all 26 identities cut to exactly one of F.W9 / F.W10 / a named other wave, Δ=∅ both directions; **both specs carry the same table; neither stamps until G-F9-21 ≡ G-F10-6 closes.**

### §2.2 The re-cut table (26 identities; identical in F-W10.md) + P-12 locks

*Heading mirrored (`identical in F-W9.md` ↔ `identical in F-W10.md`); the preamble, table and entrant block below are carried **verbatim** from F-W10.md §2.2 — spliced, never retyped. **Any change is a PAIRED edit landing in both specs in one commit**: a unilateral fix breaks G-F9-21 / G-F10-6's Δ=∅. Count and deixis disclosures live outside the shared bytes at §2.8 E-0 / E-1.*

Execution seat = **F.W9-widened (or a minted F.W9a)** per the P-12 packet ruling; the F.W10 share of every F.W9-cut row is the **terminal-disposition record only**. Three cuts deviate from the banked digraph **by mechanism** (flagged ⟨re-cut⟩; recorded in the census addendum). Other-wave legs already homed (F.W0/W1/W3/W4) are cited, never re-cut.

| record | identity (kept for life) | cut | mechanism + locks / preserved dissent |
|---|---|---|---|
| fr-AdminAuditLog:87 | **AA-44** | **F.W9** | packet root: zero frontend tests, 9 e2e never enter the audit tab, backend pins its half. Instrument = GAB-10's `grep -rln "admin" web/e2e/` → **0 files**. Cure: extend ONE existing axe keystone (visualization-ux:26-43 + visualization-crud AxeBuilder) into the admin tab; live-region half stays F.W4. **Dissent (FR-AFP-49, preserved): the family carries blocker weight — "every row above incl. all four blockers is invisible to every gate the repo runs"; INFO is a scope convention (L-9.1), never "the debt is small"** |
| fr-AdminFlaggedPanel:95 / :110 | **FR-AFP-42** (test seat) · **FR-AFP-49** | **F.W9** | 0 props / 0 emits / setup-top-level fetch in `defineAsyncComponent` — untestable seam. Seam → F.W3/W4 (homed); only the seat re-cuts. Distinct mechanism from AA-44 (testability ≠ coverage) — never folded; seam-before-seat. **Quote 8 specs, never 9** (K11) |
| fr-App:91 | **C-13 (App)** | **F.W9** | four untyped ambient global side effects, no vitest floor. The four-wave chain made explicit: **MG-θ → F.W0** (tsconfig/lint one-liners) · floor **decision → F.W4** (census §4.5) · **execution → F.W9** · **terminal record → F.W10**. Distinct id from FunctionInput's C-13 — never merged |
| fr-ContourPreview:71 | **38** | **F.W9** | zero coverage; `vue-tsc -b` is precisely the gate row-11 is invisible to. **No repair inherited — both flagship cures REFUTED** (the `<Card tier surface>` migration = 21-site typecheck break; the restore-path blank render cannot occur). The simplify-heap unit target anchors F.W4's harness decision — the cheapest true vitest justification in the packet, not PAW-50's trapped `resolveFigure` |
| fr-EquationModeToggle:41 | **FR-EMT-11** (= C-16) | **F.W9** | no unit runner; `/equation` one assertion-free screenshot slug; axe installed, never pointed here. Rider verbatim: "the gates for FR-EMT-1/-5's cures ride their own waves." **Dissent PRESERVED verbatim: reader-2's MAJOR ("every other row in this record is ungated because of it") ruled a scope error under L-9.1 — "so the F.W9/W10 close cannot treat the MINOR as a statement that coverage debt is small."** The close honours this warning |
| fr-EquationResult:41 / :72 | **FR-EQR-6** (axe leg) · **FR-EQR-31** | **F.W9** | `.eq-scroll-region` scroll container: no tabindex/role/name; KaTeX spans give no focusable descendant. F.W4 = attributes (homed); F.W9 = the single `/equation` seat. Cohort: fr-CoefficientsSpectrum M-6 same class. FR-EQR-31: zero behavioural coverage by construction; D-20's archaeology half rides FR-EQR-27's correction — **not re-booked** |
| fr-EquationView:62 / :191 | **D·D-B2** (axe leg) · **C·D-28** | **F.W9** | zero functional e2e on `/equation` (8 specs / 1 screenshot hit); info button + Wand2 = two nameless icon-only controls; aria-label ×2 → F.W4 (homed). **The packet's single strongest F.W9-seat argument: the ONE interaction spec (compute → notation → budget → reload) mechanically catches B-1, B-2, C-29, D-07, D-14 — five already-booked defects.** BLOCKER→MAJOR per the fr-CT precedent, both readers concur |
| fr-FunctionInput:80 | **C-13 = D-24** | **F.W9** | `/equation` screenshot-only; the nameless Wand2's survival is the corollary. FOLD to banked fr-CollapsibleSection m-8 class (`actions` slot ready). Naming half belongs to FR-TT-1 / EquationView D·D-B2 — never double-book |
| fr-GalleryAdminBanner:49 | **GAB-10** (coverage half) | **F.W9** | GAB-1's `color-contrast` serious class unverified by construction. **FOLDS BY REFERENCE to AA-44**; live-region half → F.W4 riding GAB-4's `aria-busy` (homed). GAB-13 (F.W0) gates it: gates written against the dirty worktree measure a tree no commit holds |
| fr-GalleryCardModal:93 | **GCM-42** (= L-14) | **F.W9** | zero tests every level; console guard filters error-only + 404 — blind to GCM-4's warns three ways. **FOLD → AA-44 / FR-AUL-16 class, never re-booked.** Own increment surviving the fold: widen the guard to `warn`. **Interaction lock: no filter widening before F.W1 lands** — the error-only filter is the blind spot F.W1's four zero-console-error gates depend on (FR-EQR-3). Dissent carried: DU's BLOCKER on GCM-4; the SR composite (GCM-4∘16∘17∘6/8) named blocker-weight with no BLOCKER row carrying it |
| fr-GallerySearchBar:54 | **FR-GSB-17** | **F.W9** | its named e2e is vacuous by construction — every assertion inside `if (await filterToggle.isVisible())` (gallery.spec.ts:57-63); delete the bar → GREEN. Un-vacuate, don't add: **assert the precondition EXISTS**; kill the unreachable `.glass-dock` branch (:49-53; `grep -rn glass-dock src/` → 0) and the `.or()` mask (:19-22). One cure shape authored ONCE for the class (with FR-USB-16 + FR-GV-36's census) |
| fr-GalleryView:78 | **FR-GV-36** (= L-24) | **F.W9** | every defect structurally invisible (no vitest/ESLint/noUnusedLocals/vueCompilerOptions); console e2e passes against a dead API. Folds → FR-GFC-27 + AA-44 route + FR-AUL-16 input. Surviving increment: **L's three-conditionally-vacuous-tests census**. The dead-API fact belongs to the F.W0/F.W9 deploy-spine reckoning — edge declared, API repair NOT booked here |
| fr-ImageUpload:61 | **27** | **F.W9** | all **15** e2e entries across **6** spec files use `setInputFiles` — drag/reject/oversize/failure/keyboard uncovered. **Quote the 15/6 pair, never a re-derived count** (ruling 7's denominator). The drag path needs FR-USB-16's coarse project — one matrix change unblocks both |
| fr-PaperArticleWindow:58 / :90 | **PAW-12** (rider limb) · **PAW-34** (= C-i-5) | **F.W9** | PAW-34: zero e2e on any owned surface ("figure" hits = `test.describe.configure` substrings). PAW-12: two facts on one hand-typed literal (`TRANSCODED_FIGURES = new Set(Object.keys(FIGURE_DIMENSIONS))`) guarding a no-fallback `<picture>`; predicate split → F.W3/W4 (homed); F.W9 = the build-time set-equality assertion (vitest or Vite build plugin per PAW-50). Invariant **HOLDS at HEAD** (28 png / 26 avif / 26 webp; unpaired `fourier.png`+`maintainer-avatar.png`; 26 keys = 26 `\includegraphics`) — the rider guards drift. **PAW-50: harness-before-rider. PAW-49: flag-before-split.** Dissent preserved: LC's MINOR sustained at R-2 |
| fr-PaperArticleWindow:199 | **PAW-49** | **F.W0** ⟨re-cut⟩ | without `noUncheckedIndexedAccess`, `resolveFigure`'s return asserts `width: number` while one whole arm yields `undefined` — vue-tsc green over a false type. The flag is a **repo-config** line, identical in kind to MG-θ/M7's tsconfig flags; **the one coverage-family row whose gate is FREE** — `npm run build` already runs `vue-tsc -b`, no new harness. F.W3/W4 rider on the PAW-12 cure homed |
| fr-PaperArticleWindow:200 | **PAW-50** | **F.W4** ⟨re-cut⟩ | web/ has **no** unit harness at all (five scripts, no vitest/jest, zero `*.test.ts`); `resolveFigure` trapped in the SFC. It *is* the harness-before-rider law — census §4.5's **decision** is its only true home; **F.W9 executes** (move `resolveFigure` beside `FIGURE_DIMENSIONS` in `lib/figureDimensions.ts` in whatever wave executes PAW-12); **F.W10 verifies**. Governs PAW-12, App C-13, ContourPreview 38, FR-AUL-16's devDependency limb, AA-44 |
| fr-PaperSidebar:49 | **D-B2** (axe leg) | **F.W9** | "Contents" heading 2.39:1 light / 3.00:1 dark vs the 4.5 floor; no axe visits `/paper`. Token → F.W4 (homed). The `/paper` seat is **ONE artifact** with M7 + PAW-34 and **runs AFTER PAW-1's cure + the PAW-44 scrollport decision** — else 42 token drops make failures un-attributable. MAJOR, blocker-weight in the a11y aggregate |
| fr-PaperSidebar:58 | **C-M4** | **F.W9** | the expand-if-closed guard reads the **Tooltip's** `data-state` (domain `closed\|delayed-open\|instant-open`, never `"open"`) — a no-op. F.W4 half dies with the `CollapsibleTrigger` restructure (homed); F.W9 re-keys the guard on the Collapsible root. Corrected cite :124–129. Third vacuous-gate instance but a **DISTINCT mechanism** (wrong element's state) — never folded; the cure differs |
| fr-PaperSidebar:67 | **M7** | **F.W9** | dead surface unreachable by any gate; two of four needed gates are one config line each. tsconfig half → **F.W0 = the SAME identity as MG-θ — one substrate edit, two records, booked once**; F.W9 = axe on `/paper` + lint script |
| fr-PaperView:120 | **D/i-1** | **F.W9** | zero coverage of the search surface, no vitest. **Coupled: rides the B-1/B-2 cure wave, never scheduled independently** (verbatim lock) |
| fr-Tooltip:26 | **FR-TT-1** (promotion note) | **F.W10** ⟨re-cut⟩ | the shim is the ONLY label on **17** icon-only triggers (Editor ×10 · Canvas ×6 · Wand2 ×1), wiring `aria-describedby` only-while-open — a DESCRIPTION, never a NAME. It governs **this wave's close-gate design** (G-F10-10). Naming legs → F.W4 (all 17); shim leg → F.W3 keeping its R3-7a/X-11 identity; folds honoured, never re-booked (EditorControlsDock C-3 · CanvasControlsDock D-1/L-3/C-1 · EquationView D·D-B2). Producer corroboration glass-ui 8.0.0:69-73: "an icon-only trigger labels ITSELF." L-4's 17-count census citable; D-1's 16-count + :42/:49 anchors KILLED (register #8) |
| fr-UserSlugBar:53 | **FR-USB-16** (≡ r1-MISSED-5 ≡ r2-MISSED-1) | **F.W9** | self-disabling test: `gallery.spec.ts:66-82` body wrapped in `isVisible().catch(() => false)`; matrix is one Desktop-Chrome project. Unguard (assert the trigger EXISTS) **+ add the mobile/coarse Playwright project** — a matrix change that must precede every coarse-pointer witness (glass a11y-overrides floor; PaperSidebar `--ui-scale:1.5`/`--control-floor:2.75rem` arm; ImageUpload 27's drag path). **Dissent preserved: r2 graded MINOR; overruled at the ruling table** |

**By-mechanism entrants** (records carry NO digraph — they enter by mechanism, not by re-cut, and must not be counted into the 54):

- **FR-AUL-16** (fr-AdminUserList:54, MAJOR) — the only cascade-delete surface, zero automated coverage of any kind; "un-harnessable" defence KILLED (ruling 3e). NO-WAVE-OWNER: harness decision → F.W4 · devDependency + admin-tab axe limb → F.W9 · **terminal home decided AT F.W10** (§3.3 deadline — last F wave). Cure verbatim: "extend one axe keystone into the admin tab + a vitest devDependency"; slug-bearing aria-labels = ready selector vocabulary.
- **FV-26** (fr-FullscreenViewer:79 = D-23·C-m7) — the only e2e "Fullscreen" hit is a comment (gallery.spec.ts:113). Coverage share → F.W9. **Teleport lock: assert against the teleported root** (FV-27: one of two Teleport sites). FV-30 rides banked BasisCanvas D-4/BC-2/C-2a + AnimationControls C-12, no new booking; FV-31 → F.W4.
- **FR-COB-20** (fr-CanvasOverlayButton:74 = C-7) — interaction contract entirely implicit; no test anywhere. "Dies with the file"; the no-test-for-wrappers observation → SS-3/SS-4 input; the tree favours DELETE. Gated on **F8-REACH-02** at F.W0 — **HOLD is RED; F.W10 verifies no HOLD survives** (that verification is this wave's whole claim, not a re-booking).
- **FR-AH-45** (fr-AppHeader:108 = C-16, NO-WAVE-OWNER spec-input) — zero props/emits/slots, no contract. A question owed an answer ("what contract should a shell header state?") → SS-3/SS-4 spec-input; terminal home or kill-with-rationale at F.W10, never left open. **Discharges together with FR-AH-30's cure-caution: the D-axis rank-5 remedy REVERSES the outcome — only `@layer glass-overrides` appended after `utilities` works** (glass-ui's competing rules already sit in `@layer components` at higher specificity).
- **PAW-33** (fr-PaperArticleWindow:89 = C-i-2) — rides the C-19 ⊕ PAW-33 pin-truth row (§2.6).

### §2.3 The five seats (axe-seat economy — author ONCE, cite many)

| seat | artifact | rows it discharges | locks |
|---|---|---|---|
| **S1 · admin tab** | ONE existing axe keystone (`visualization-ux.spec.ts:26-43` + `visualization-crud` AxeBuilder, wcag2a/aa/21a/21aa) **extended**, not cloned | **AA-44** (packet root) · **GAB-10** coverage half (fold-by-reference) · **FR-AUL-16** axe limb · **FR-AFP-49** (fold; books census risk #10 for its record only) · **FR-AFP-42** test seat | Gated on **GAB-13 (F.W0)**. **FR-AFP-42 is seam-BEFORE-seat** — F.W3/W4 must make the component mountable first; testability ≠ coverage, never folded into AA-44. Live-region half stays F.W4 (rides GAB-4's `aria-busy`). Folds cite, they never mint |
| **S2 · `/equation`** | ONE interaction spec: **compute → notation → budget → reload** | **C·D-28** · **FR-EQR-6** axe leg · **FR-EQR-31** · **FR-EMT-11** · **FunctionInput C-13 = D-24** · **EquationView D·D-B2** axe leg | The packet's strongest seat argument, verbatim: this one spec *would have caught B-1, B-2, C-29, D-07 and D-14 mechanically* — five already-booked defects. FR-EQR-6's attributes and D·D-B2's `aria-label` ×2 are **F.W4's cures**, not this seat's. FR-TT-1 promotion lock applies |
| **S3 · `/paper`** | ONE axe pass + assertions on `paper-search*` selectors | **PaperSidebar D-B2** axe leg · **M7** axe limb · **PAW-34** · **PaperView D/i-1** | **Runs AFTER PAW-1's 42-site cure + the PAW-44 scrollport decision** — else 42 token drops make failures un-attributable. **D/i-1's coupled lock, verbatim: "coverage rides the B-1/B-2 cure wave" — never scheduled independently.** `/paper` **IS** navigated today (`paper-performance.spec.ts:38`; `visual-baseline` paper row × 3 viewports) — "zero mounts / no paper route" must never be quoted from the corpus again. Selectors must not depend on `.sidebar-top-btn` (M2 hard lock) |
| **S4 · un-vacuate** | Rewrite each guard to **assert the precondition EXISTS** | **FR-GSB-17** (`if (await filterToggle.isVisible())`) · **FR-USB-16** unguard half (`isVisible().catch(() => false)`) · **C-M4** (Tooltip `data-state`) · **FR-GV-36** (L's three-conditionally-vacuous census) · **GCM-42** (open a card; widen the guard) | Three DISTINCT mechanisms under one cure shape: self-disabling wrapper (USB) · precondition-conditional body (GSB) · **wrong element's state (C-M4) — never folded, the cure differs**. Kill the unreachable `.glass-dock` branch (`grep -rn glass-dock src/` → 0) and the `.or()` mask. **GCM-42's console widening is LOCKED behind F.W1.** Falsifier for the whole seat: deleting the search bar / slug bar must redden its spec |
| **S5 · matrix + fullscreen + header** | A mobile/coarse Playwright project; a fullscreen assertion; header nav/logo/toggle | **FR-USB-16** matrix half · **27** (ImageUpload: **15** entries / **6** files, `setInputFiles` only — quote the pair) · **FV-26** (teleport lock) · **FR-AH-45** execution limb | **The matrix change precedes every coarse-pointer witness** (glass `[data-size="icon"]` coarse floor · PaperSidebar `--ui-scale:1.5`/`--control-floor:2.75rem` · FV-11's 44-vs-60px discordance · the drag path). FV-26: **assert against the teleported root** (FV-27 — one of exactly two Teleport sites, with `PaperSearchModal.vue:41`). **FR-AH-45 / N-2 rider: the DarkModeToggle a11y label change and the `paper-performance.spec.ts:328` locator edit land in the SAME commit** (the label is a load-bearing selector; `:329` waits 250 ms against a 350 ms morph). **This is a matrix change, NOT a Safari cell** — it discharges no X-W11 G8 row |

### §2.4 The harness and the floor

| row | verb at F.W9 | lock |
|---|---|---|
| **PAW-50** ⟨re-cut⟩ | **EXECUTE** the vitest stand-up; move `resolveFigure` beside `FIGURE_DIMENSIONS` in `lib/figureDimensions.ts` in whatever wave executes PAW-12 | **HARNESS-BEFORE-RIDER is this row.** Decision F.W4 (census §4.5 is the only test language in the census) · execution F.W9 · verification F.W10. F.W9 may not stand a harness the decision has not authorized |
| **C-13 (fr-App)** | the test-floor for four untyped ambient global side effects | four-wave chain: MG-θ → F.W0 · decision → F.W4 · execution → F.W9 · record → F.W10. **Distinct id from FunctionInput's C-13 — never merged** |
| **38 (fr-ContourPreview)** | the simplify-heap unit target — **the cheapest true vitest justification in the packet** | **No repair inherited: both flagship cures REFUTED** (the `<Card tier surface>` migration = a 21-site typecheck break at the uplift target; the SS-13 restore-path blank render cannot occur — `out-in` defers the mount past the ref assignment) |
| **FR-AUL-16** | the vitest devDependency + the admin-tab axe limb | "Un-harnessable by construction" **KILLED** (ruling 3e) — a props-less SFC mounts under `createTestingPinia` + a fetch double; the obstacle is one devDependency. **Terminal home decided AT F.W10.** If SS-4 declines the widening, **the decline is recorded explicitly with rationale (M-25)** — leaving it un-homed re-creates the blind spot |
| **PAW-12 rider** | the **build-time set-equality assertion** (vitest or a Vite build plugin) | Invariant **HOLDS at HEAD** — the rider guards drift, it repairs no live break. Predicate split is F.W3/W4's. **Flag-before-split (PAW-49)** |
| **M7** | the **lint script** (`eslint`/`oxlint`/`biome` configs = **0** tree-wide, re-measured 2026-08-28) | tsconfig half → **F.W0 = the SAME identity as MG-θ — one substrate edit, two records, booked once**. Five `catch (e: any)` in AdminFlaggedPanel have no gate to notice them (FR-AFP-22 / FR-AUL-50) |
| **FR-COB-20** | **harness branch only, and only if F.W0 rules `ISOLATED_HARNESS`** | Gated on **F8-REACH-02** (`CanvasOverlayButton.vue`, N.C39, sha `35f3db91…`, 595 B). **HOLD is RED. F.W9 authors no harness for a file that may be DELETE.** The no-test-for-wrappers observation → SS-3/SS-4 input; the tree favours DELETE |

### §2.5 The deploy spine, containerization, and the evidence floor

| row | re-home + verb | sequencing |
|---|---|---|
| **M.W2** — readiness ≠ liveness (BLOCKER-weight) | Re-home into F.W9 and execute: separate readiness from liveness in the health gate. **Arm A, bump-independent** — does not wait on F.W1 | Re-homing **disposes of the M board row explicitly** — no double-booking between `docs/tranches/M/PROGRESS.md` and this spec. **OG-F1** (owner ruling owed: re-root fourier source auditing at F.W0, or freeze the R-coordinates) gates whether the M coordinates may be cited at all. Every fourier-tree write is asked, never made |
| **M.W3** — fail-closed inv-28 (BLOCKER-weight) | Make the API deploy fail-closed on a same-SHA green CI run, mirroring `deploy-pages.yml`'s conclusion/branch/event guard | Arm A. **Must not be read as an API-shape gate — the API contract is SS-4's (F.W5–W8).** ASK-3 (palette-api deploy; the rsync deploy-dir blocking `deploy-hook.sh` adoption) is **F.W10's correspondence**, not F.W9's infra execution |
| **M.W4** — kill the silent rollback + inv-31 (BLOCKER-weight, **the chronic**) | Re-home: a failed deploy must be **loud and blocking**, and the inv-31 observability floor stands up | **F.W0 substrate first.** FR-GV-36's "console e2e passes against a dead API" fact belongs to this reckoning; **the API repair is NOT booked here**. See §2.8 CORRECTION C-1 — the residue is narrower and sharper than the banked prose |
| **M.W11** — evidence (BLOCKER-weight, the evidence law) | Re-home the **evidence half**: a green CI run id covering each claimed-green gate, plus CWV/INP and axe artifacts, each citing a committed SHA | **CWV ORDER-LOCK (FR-AH-1)**, hard. **R3-9 binds**: an assertion of non-execution is recorded, never cited as a method guarantee — every green cites a run id |
| **Containerization fidelity audit** (census-assigned scope; no registry row, MAJOR-weight) | Audit fidelity across `web/Dockerfile`, `api/Dockerfile`, `docker-compose.yml`, `docker-compose.prod.yml`, `nginx/fourier.conf` and the host-Apache templates — prod-overlay hardening verified **as-running, not as-declared** | The image **cannot build from `web/` alone**: `web/Dockerfile:24` copies `assets/ → public/assets/` and `:26-29` copies six `paper/fourier_paper.{tex,bib,aux,log,toc,bbl}` files into `/paper/` because `vite-plugin-latex-paper` compiles the TeX at build — the context must be the repo root. **Two nginx authorities coexist unreconciled**: the printf-inlined conf at `web/Dockerfile:33-43` and the 72-line repo-level `nginx/fourier.conf`. In-file prior art (the fidelity audit's own falsifier class): the `163ca47` deps migration left a stale `COPY web/vendor ./vendor` that "broke every Docker build" — `web/Dockerfile:5-9` is now the comment recording its removal. **CI checkout deliberately omits submodules (`docs/precepts` is private) — any gate assuming precepts are readable in CI is wrong** |

### §2.6 Carried for parity — **NOT booked by F.W9**

| row | where it is booked | why it is carried here |
|---|---|---|
| **PAW-49** ⟨re-cut → F.W0⟩ | F.W0 (repo-config line, same kind as MG-θ/M7) | **The one coverage-family row whose gate is FREE** — `npm run build` already runs `vue-tsc -b`; no new harness. Carried for parity with F-W10 §2.2; **F.W9 does not book it.** Flag-before-split governs PAW-12 |
| **PAW-50's DECISION leg** ⟨re-cut → F.W4⟩ | F.W4 (census §4.5) | F.W9 executes only. **F.W10 cannot book a coverage row terminal while the harness decision is unmade** |
| **FR-TT-1** ⟨re-cut → F.W10⟩ | F.W10 (it governs the close-gate design) | **PROMOTION LOCK, binding on F.W9: "BLOCKER at any F.W9/W10 axe close-gate."** The close axe run covers `/paper` + `/equation` + the admin tab with zero serious/critical, **OR** the 17 naming legs are recorded as a named, dated waiver — **it may not be argued green while the legs are open**. L-4's 17-count is the citable census; D-1's 16 and its `:42/:49` anchors are KILLED (register #8) |
| **FR-AUL-16 / FR-AH-45 terminal homes** | F.W10 (the last F wave) | F.W9 proposes the execution limbs (P-12); F.W10 decides terminal. A decline must be recorded with rationale |
| **C-19 ⊕ PAW-33 pin truth** | F.W1 + the relay | Cited so the containerization/pin surfaces do not re-book it: ONE fact, two banked ids, both cited, neither re-booked |

### §2.7 Method law — **R3-9** (F.W0-routed; standing rule for every evidence cell here)

An unverifiable non-execution assertion was carried through the Codex lineage as if it were a method guarantee. **Verb: record, never cite as a method guarantee.** For F.W9 this is *the evidence law*: **no gate closes on an assertion that something did not run; every green cites a covering run id (inv-27, G-F9-17).** It joins KF.W4's transferable gate-design law (a)–(d) plus **a gate may not re-derive its own oracle** — quoted in this file's binding-law block and enforced at §2.1 (the 26-identity taxonomy is consumed from F-W10, never recomputed) and at G-F9-3 (the falsifier **extends** the existing keystone rather than authoring a rival oracle). The row itself is F.W0's — carried, not booked.

### §2.8 Measured corrections and errata (E-3 — recorded beside; dated bytes never rewritten)

| # | finding (re-measured 2026-08-28, fourier HEAD `cd26c65`) | disposition |
|---|---|---|
| **E-0** (deixis, disclosure only) | The shared §2.2 bytes read from F.W10's seat in three cells: FR-TT-1's "governs **this wave's** close-gate design (G-F10-10)" and FR-COB-20's "**this wave's** whole claim" denote **F.W10**, and PAW-33's "(§2.6)" resolves against **F-W10.md's numbering** (its C-19 ⊕ PAW-33 pin-truth row — carried here at §2.6 for parity) | Read the shared bytes from the F.W10 seat. In THIS spec, FR-TT-1's F.W9-side share is **G-F9-22** and PAW-33 is excluded (§5). **No byte in the region is edited to fix deixis** — G-F9-21 forbids a unilateral touch |
| **E-1** (erratum, **PAIRED**) | F-W10 §2.2's **AA-44** cell reads *"9 e2e never enter the audit tab"* while the **FR-AFP-42/-49 cell two rows below** states **"Quote 8 specs, never 9 (K11)"** — the table contradicts itself. Re-measured: `web/e2e/*.spec.ts` = **8** (contour-extraction · gallery · paper-performance · settings-persistence · visual-baseline · visualization-crud · visualization-ux · workspace-flow) | The correction is a **PAIRED edit landing in BOTH specs in one commit** — a unilateral fix breaks G-F9-21 / G-F10-6's Δ=∅. Until it lands, both specs carry the byte-identical table **including the defect**, and this row is the disclosure. **No prose in this file quotes 9** |
| **C-1** (CARRY correction, narrowing) | `scripts/deploy-hook.sh` at HEAD (last touched `e9faab6`, G.W7 ε.2) **already carries a real bounded ~60 s health gate** (`health_gate()`, `${HTTP_PORT:-8100}`, polls `/api/health` **and** inv-22 `/`→404), a last-known-green marker, rebuild-on-rollback, and an explicit `ALERT` on double failure. The banked mechanism "*builds, fails the 60 s health-gate, rolls back, exits non-zero into a log nobody watches*" is therefore **half-cured in the script and wholly uncured in the channel** | M.W2/M.W4's residue is **sharper, not smaller**: the rollback is not silent *in the script*, it is silent *in the channel* — **there is no observability floor (inv-31); the loud ALERT lands in an unwatched log.** G-F9-15's falsifier is re-cut accordingly. M.W2's readiness/liveness split remains genuinely open (the gate conflates backend liveness with route readiness) |
| **C-2** (confirmation) | inv-28 asymmetry **CONFIRMED by measurement**: `grep -n 'conclusion\|workflow_run\|gh run\|inv-28' scripts/deploy-hook.sh` → **zero hits**, while `deploy-pages.yml:48-57` carries the named `inv-28 gate` with the three-clause `if:` (`conclusion == 'success'` ∧ `head_branch == 'master'` ∧ `event == 'push'`). **M.W3 stands unmodified** |
| **C-3** (re-scope, hardening) | `docker-compose.prod.yml` **already declares** the full hardening set as authored: api `read_only` + `tmpfs` + `cap_drop` + `no-new-privileges` + 2G; web `read_only` + three tmpfs paths + `cap_drop`/`NET_BIND_SERVICE` + 256M; mongo `--tlsMode requireTLS` with a documented honesty pivot on `--tlsAllowConnectionsWithoutCertificates`, and `read_only`/`cap_drop` recorded as **G.W7 BOOKED residuals** for the stateful DB. `nginx/fourier.conf` carries **2 rate zones** (`api_general` 30r/s `:16`, `api_compute` 2r/s `:17`) and **5 security headers** (`:25-29`) | The gap is **not** as-shipped-vs-as-documented; it is **as-declared-vs-as-running**. G-F9-19's falsifier is re-cut: verify against the **running container** (`docker inspect` on the deployed stack), not against the compose bytes — which are already the documentation the audit would otherwise re-read |
| **C-4** (anchor corrections; **FR-CP-45 law — re-resolve every anchor before quoting into a wave spec**) | `web/Dockerfile` is **43 lines**: `COPY assets/` at **:24** (CARRY cites :20-25), the six-file `COPY paper/…` at **:26-29**, `FROM nginx:alpine AS production` at **:31**, the printf block at **:33-43** (CARRY cites :31-42). `/equation` slug sits at `visual-baseline.spec.ts:34` (CARRY cites :35). `playwright.config.ts` projects array at **:46-51**, one `chromium` project (CARRY cites :48) | Corrected anchors used throughout §1b/§3; the banked anchors are recorded here so no seat quotes a stale byte. **Mechanism unchanged in every case** — the anchors drifted, the facts did not |
| **C-5** (MEASURE-AT-OPEN, un-verifiable from this seat) | `f2fe447` exists; local HEAD `cd26c65` (2026-07-03) is **49 commits ahead** of it — the banked "*host is 44 commits behind `f2fe447`*" is a **dated measurement from the M deep audit**, and the host's deployed SHA is **not measurable from this seat**. GAB-13's dirty worktree re-measured: **exactly 28** rows (`git status --porcelain \| wc -l` → 28) — unchanged since the fold | G-F9-15 is **MEASURE-AT-OPEN**: the deploy-of-record is read live at wave open and the banked 44 is never quoted as current. GAB-13's 28 stands, corroborating the F.W0 baseline decision either way |

---

## §3 Gates

Twenty-two. All born RED with the failing probe named. Every gate names a **live witness** (L-19 — no descriptive invocation, no proof-farm scripts). `[re-measured]` = run read-only 2026-08-28 against fourier HEAD `cd26c65`; unmarked rows carry the fold measurement of 2026-08-25; deploy/evidence cells re-measure at open.

| gate | born state · witness | falsifier (turns it GREEN) |
|---|---|---|
| **G-F9-1** A unit-test harness exists and runs in CI | RED **[re-measured]** — `web/package.json` scripts are exactly `dev,build,preview,test:e2e,test:e2e:ui`; `devDependencies.vitest` → **undefined**; no `vitest.config`; `find web/src -name '*.test.ts' -o -name '*.spec.ts'` → **0** (lane-frontend §1 :48 · PAW-50 · FR-AUL-16 · FR-CP-32) | `npm test` exists, vitest is a devDependency, ≥1 assertion runs in CI. **HARNESS-BEFORE-RIDER: the F.W4 decision lands first; F.W9 may not stand a harness the decision has not authorized** |
| **G-F9-2** A lint floor exists | RED **[re-measured]** — `ls web/ \| grep -iE 'eslint\|oxlint\|biome'` → **0**; no `lint` script (MG-θ · M7); five `catch (e: any)` in AdminFlaggedPanel with no gate to notice them (FR-AFP-22 · FR-AUL-50) | A lint script runs in CI and fails on a seeded violation. tsconfig flags are **F.W0's** — MG-θ ≡ M7, one edit, two records, booked once |
| **G-F9-3** The admin tab is entered by ≥1 spec, with an axe pass | RED **[re-measured]** — `grep -rln "admin" web/e2e/ \| wc -l` → **0 files**; e2e `audit` → 4 prose-only hits (AA-44); `admin\|flag` → 0 (FR-AFP-49) | ONE **existing** axe keystone (`visualization-ux.spec.ts:26-43` + `visualization-crud` AxeBuilder, wcag2a/aa/21a/21aa) **extended** into the admin tab, serious/critical empty. Gated on GAB-13's F.W0 disposition. Falsified by authoring a rival oracle instead of extending the keystone (KF.W4) |
| **G-F9-4** axe runs on `/paper` | RED **[re-measured]** — `grep -rln AxeBuilder web/e2e/` → exactly **two** files (`visualization-ux.spec.ts`, `visualization-crud.spec.ts`), both `/visualize`-gated (ruling 9 / K3). `/paper` **IS** navigated (`paper-performance.spec.ts:38`; `visual-baseline` paper row × 3 viewports) — the void is **zero assertions on any `paper-search*` selector and zero axe on the route** | ONE `/paper` axe artifact, serious/critical empty, covering D-B2's 2.39:1 heading. **Runs AFTER PAW-1's 42-site cure + the PAW-44 scrollport decision** — else 42 token drops make failures un-attributable. Falsified by any selector depending on `.sidebar-top-btn` (M2 lock) or by quoting "no paper route" from the corpus |
| **G-F9-5** axe runs on `/equation` | RED **[re-measured]** — `@axe-core/playwright ^4.11.3` in devDeps, never pointed here; the sole axe seat visits `/visualize` only (FR-EQR-6 · FR-EMT-11) | ONE `/equation` axe artifact. **FR-TT-1 promotion lock applies: RED by construction while the 17 naming legs are open, unless a named dated waiver is recorded** |
| **G-F9-6** `/equation` carries ≥1 behavioural assertion | RED **[re-measured]** — `grep -rn "/equation" web/e2e/` → **one hit**, `visual-baseline.spec.ts:34` — an assertion-free screenshot slug (FR-EMT-11 · FR-EQR-6 · FR-EQR-31 · FunctionInput C-13 · C·D-28 all name this one fact) | The ONE interaction spec (compute → notation → budget → reload) exists and asserts — mechanically catching **B-1, B-2, C-29, D-07, D-14**. Axe-seat economy: author once, cite five times |
| **G-F9-7** No gate in the suite is vacuous — deleting the subject must redden its spec | RED — `gallery.spec.ts:57-63` every assertion inside `if (await filterToggle.isVisible())` (FR-GSB-17); `:66-82` whole body inside `isVisible().catch(() => false)` (FR-USB-16); the `.glass-dock` branch `:49-53` unreachable (`grep -rn glass-dock src/` → 0); the `.or()` mask `:19-22`; L's three-conditionally-vacuous census (FR-GV-36) | Each guard rewritten to **assert the precondition EXISTS**; a deliberate deletion of the search bar / slug bar reddens its spec. **C-M4's guard is a DISTINCT mechanism (the wrong element's `data-state`) — cured separately, never folded** |
| **G-F9-8** No axe keystone is `test.fixme` on a stale premise | RED **[re-measured]** — `grep -rn "test.fixme" web/e2e/` → keystones at `visualization-ux.spec.ts:110`, `:133`, `:192` (only `:150` runs) on a booked `^2.0.0` "omits inert" baseline **doubly false** against installed 4.0.0 emitting `inert: !i.value \|\| void 0`; a fourth at `visualization-crud.spec.ts:630`. EasingPicker's sole covering e2e is the `:192` fixme, its unblock condition stale by two majors against the `^4.0.0` pin | All four un-fixme'd and green. **The un-fixme is F.W3/W4's cure (fr-BasisSelector B-3 / R-2), not F.W9's** — F.W9 cannot produce admissible axe evidence until it lands; FR-CP-27 records what an un-fixme'd run would hit |
| **G-F9-9** A gallery card / modal is opened by a spec | RED — `gallery.spec.ts` `modal\|dialog\|card\|Open Visualizer` → **0** (GCM-42); 6 `test(` blocks, none reaches a card (FR-GFC-27) | A spec opens a card and asserts. GCM-4's unnamed dialog (two dev `console.warn`s per open) becomes visible only with **G-F9-10** |
| **G-F9-10** The e2e console guard is not blind to warnings | RED — `gallery.spec.ts`'s guard filters `error`-only + `404` — blind to GCM-4's warnings three ways | Guard widened to `warn`. **INTERACTION LOCK: no widening before F.W1 lands** — the error-only filter is the blind spot F.W1's four zero-console-error gates depend on (FR-EQR-3). Declare it, or F.W9 reddens F.W1 on pre-existing warnings |
| **G-F9-11** Fullscreen is exercised | RED — the only e2e "Fullscreen" hit is a **comment** at `gallery.spec.ts:113` (FV-26) | A spec enters fullscreen and asserts **against the teleported root** (FV-27 — one of exactly two Teleport sites tree-wide, with `PaperSearchModal.vue:41`) |
| **G-F9-12** The shell header's nav / logo / toggle are exercised | RED — no e2e touches the header's nav, logo or toggle (FR-AH-45). Only incidental coverage: `paper-performance.spec.ts:328` locating DarkModeToggle by `/switch to dark mode/i` — a locator matching only the light state — with `:329` waiting 250 ms against a 350 ms morph (fr-DarkModeToggle K-1, N-2 rider) | Header nav/logo/toggle asserted, **and the DarkModeToggle a11y label change lands in the SAME commit as the spec locator update** (N-2's rider — the label is a load-bearing selector) |
| **G-F9-13** The Playwright matrix carries a mobile/coarse-pointer project | RED **[re-measured]** — `web/playwright.config.ts:46-51` declares exactly **one** project (`chromium` / Desktop Chrome); 8 specs / 29 `test(` blocks. FR-USB-1/3 unreachable in CI by construction (FR-USB-16); FV-11's 44-vs-60px discordance invisible; ImageUpload 27's drag path unreachable | A mobile/coarse project runs. **This matrix change precedes every coarse-pointer witness.** Falsified by any claim that it discharges a Safari cell — **it does not** (X-W11 G8 edge) |
| **G-F9-14** The figure set-equality invariant is asserted at build time | RED — `TRANSCODED_FIGURES = new Set(Object.keys(FIGURE_DIMENSIONS))` (`PaperArticleWindow:52`, sole assignment) with **no generator, manifest, CI or e2e anywhere** (PAW-12); the assertion has no home because `web/` has no harness (PAW-50). Invariant **HOLDS at HEAD** (28 png / 26 avif / 26 webp; unpaired `fourier.png` + `maintainer-avatar.png`; 26 keys = 26 `\includegraphics`) | A vitest assertion or a Vite build plugin **fails on a seeded mismatch**. Harness-before-rider; `resolveFigure` moves beside `FIGURE_DIMENSIONS` in `lib/figureDimensions.ts` in whatever wave executes PAW-12. The rider guards drift — a green here is **not** evidence of a repair |
| **G-F9-15** The deploy-of-record is current — the host is not behind, and no rollback is silent | RED — the chronic. **MEASURE-AT-OPEN**: `f2fe447` exists and local HEAD `cd26c65` is **49 commits ahead** of it; the banked "*44 behind*" is the dated M-deep-audit number and the host's SHA is unmeasurable from this seat. Local HEAD has not moved since **2026-07-03**; M.W2/W3/W4 all `planned` (`docs/tranches/M/PROGRESS.md:15-28`). **§2.8 C-1 narrows the mechanism**: the script's rollback is loud (`ALERT`), the **channel** is not — no inv-31 floor exists | Host at HEAD **read live at wave open, never inherited**; readiness separated from liveness (M.W2); a failed deploy is **loud *and blocking* in a watched channel** (M.W4 + inv-31 — an `ALERT` line in an unwatched log does not clear this gate). Re-homing **disposes of the M board rows explicitly** — no double-booking |
| **G-F9-16** The API deploy path is fail-closed on a same-SHA green CI run (inv-28) | RED **[re-measured]** — `deploy-pages.yml:48-57` gates the SPA on `conclusion == 'success' && head_branch == 'master' && event == 'push'` with checkout pinned to `head_sha`; `grep -n 'conclusion\|workflow_run\|gh run\|inv-28' scripts/deploy-hook.sh` → **zero hits** — the API ships on a bare webhook (`deploy.babb.dev/hooks/fourier-analysis`). inv-28 holds on one arm only (lane-docs §6b) | The API path **refuses any SHA without a covering green CI run**. **Not an API-shape gate — the contract is SS-4's** |
| **G-F9-17** Every claimed-green cites a covering green run id (inv-27) | RED — M.W11 is `planned` (`docs/tranches/M/PROGRESS.md:15-28`); no run id is on record for any F-side green | Each green gate row carries a **run id and a committed SHA**. **R3-9 binds: an assertion of non-execution is recorded, never cited as a method guarantee.** KF.W4 applies — **a gate may not re-derive its own oracle** |
| **G-F9-18** CWV/INP evidence exists and is attributable | RED, **ORDER-LOCKED** — no LCP/TBT trace exists. FR-AH-1: AppHeader statically drags **450,631 B** of Fourier-coefficient JSON into the eager entry chunk (82.5 % long-float literals; 75,210 B read by no reachable code) to draw a 40px icon, with FR-AH-7 (348,707 B) and FR-AH-2 (402,944 B of unusable preloads) competing for the same first paint — "*any fourier LCP measurement without this fix is measuring `sun.json`*"; the value.js **Q14** escalation is the named prior art | A trace taken **AFTER** F.W4's async boundary + generator-side payload prune — **or** a baseline published **with its invalidity stated on its face**. FR-GIG-15 rider: the three-rung INP floor (`scheduler.yield`, chunkSize 24 vs pages capped at 20) is **unreachable in the shipped tree**, contradicting lane-frontend :568 — **do not cite that line as hygiene** |
| **G-F9-19** Containerization fidelity is audited **as-running**, not as-declared | RED **[re-measured]** — `web/Dockerfile:24` copies `assets/ → public/assets/` and `:26-29` copies six `paper/fourier_paper.*` files because `vite-plugin-latex-paper` compiles the TeX at build — **the image cannot build from `web/` alone**; two nginx authorities coexist (printf-inlined `:33-43` vs the 72-line `nginx/fourier.conf`); in-file prior art at `:5-9` (the `163ca47` stale `COPY web/vendor ./vendor` that "broke every Docker build"). **§2.8 C-3**: `docker-compose.prod.yml` already *declares* read_only/tmpfs/cap_drop/no-new-privileges/2G/256M/Mongo `requireTLS`, and `nginx/fourier.conf` already carries 2 rate zones (`:16-17`) + 5 security headers (`:25-29`) | Every hardening claim verified **against the running artifact** (`docker inspect` on the deployed stack), never against the compose bytes that are themselves the documentation; the **two nginx authorities reconciled to one**; the mongo `read_only`/`cap_drop` **BOOKED residuals** (G.W7 ε.2) and the `--tlsAllowConnectionsWithoutCertificates` honesty pivot each carry an explicit verdict. **No gate may assume `docs/precepts` is readable in CI** — the checkout deliberately omits submodules |
| **G-F9-20** Every evidence artifact cites a committed SHA on a parsing substrate | RED **[F.W0-gated]** — **M1**: installed glass-ui 4.0.0 `dist/styles/index.css` is syntactically corrupt (`postcss.parse` → 35 top-level nodes, **zero** valid `@source`, one garbage at-rule named `` source` `` at :203–222); strict Tailwind loaders hard-error. **GAB-13**: the audited artifact is the DIRTY WORKTREE — `git status --porcelain \| wc -l` → **28 [re-measured]** (HEAD `web/package.json` pins `^3.1.0`/`^2.2.0`/`^0.10.0`/lucide `latest` vs WT `^4.0.0`/`^4.3.0`/`^0.13.0`/`^1.0.0`). **F8-REACH-01/02**: InfoCard.vue + CanvasOverlayButton.vue unmounted, un-dispositioned; **HOLD is RED** | F.W0 lands: M1 relayed to the glass BH inbox and the dist parses (or the pin moves); the WT diff **landed OR recorded as the audit baseline**; both F8-REACH rows carry `KEEP_WITH_MOUNT \| ISOLATED_HARNESS \| DELETE`. **F.W9 evidence taken before this measures a tree no commit holds.** Dissent preserved verbatim (worker-DU): *"the fleet has already elected to treat the working tree as the scope"* — if F.W0 adopts the worktree as baseline, GAB-13 discharges to a disclosure line and DU's grading was right all along |
| **G-F9-21** The F.W9/F.W10 re-cut table is identical in both specs, **Δ=∅ both directions** | RED **[re-measured]** — `grep -o 'F\.W9/W10' registry/adjudicated/fr-*.md \| wc -l` → **54 / 25 records**; bare `F.W10` → **0**; `ls fr-*.md \| wc -l` → **66**. `F-W10.md` §2.2 carries the 26-identity table and states "F.W10 does not author F.W9" — **the twin of G-F10-6**; this file's §2.2 is spliced from those bytes | Every routing identity cut to exactly one of F.W9 / F.W10 / a named other wave; set-difference ∅ (COHESION §3.1). **Falsified by**: a row minted from the 14 boilerplate routing-law lines or the 7 boilerplate-only records; a row re-booked under a new id; **any edit to `fr-*.md`** (the registry is read-only). **§2.8 E-1 must land as a PAIRED edit** or this gate reddens on the fix itself. **Neither spec stamps before this closes** |
| **G-F9-22** The axe close-gate is honest about the FR-TT-1 promotion | RED — the twin of G-F10-10. FR-TT-1: the shim is the only label on **17** icon-only triggers (Editor ×10 · Canvas ×6 · Wand2 ×1), wiring `aria-describedby` only-while-open — a description, never a name — with the banked note **"BLOCKER at any F.W9/W10 axe close-gate."** Today: `grep -rln "admin" web/e2e/` → **0**, no axe visits `/paper`, `/equation` = one assertion-free screenshot | The close axe run covers `/paper` **+** `/equation` **+** the admin tab with **zero serious/critical**, **OR** the 17 naming legs are recorded as a **named, dated waiver**. **It may not be argued green while the legs are open** — an axe close-gate authored before F.W4's legs is RED by construction |

---

## §4 Sequencing

### §4a Ordering locks (carried whole; each lands where its rows land)

1. **RE-CUT-0 precedes everything** — neither F.W9 nor F.W10 stamps before **G-F9-21 ≡ G-F10-6**. §2.8 **E-1**'s correction is a **paired** edit in one commit.
2. **F.W0 SUBSTRATE PRE-GATES FIRST** (binding; **G-F9-20** is its witness) — M1 · GAB-13 · F8-REACH-01/02 · MG-θ ≡ M7's tsconfig flags (one edit, two records) · ⟨re-cut⟩ PAW-49 · FR-AFP-54/-55 manifest banding · fr-EasingPicker's stale-build fact · the Codex residue set (R3-9 · R3-16+R4-11+R5-9+R6-10 · R4-12 EVAPORATED · R5-3 · the still-registered `~/.codex/worktrees/d0be/fourier-analysis`). **Owner rulings owed and never presumed: OG-F1, OG-F2.**
3. **F.W1 IS ATOMIC LAND-OR-LOSE** (producer bump + 162-site prop rewrite + copied→status triple **in ONE change**, else four zero-console-error e2e gates go RED — FR-EQR-3) **and precedes F.W3/W4 (§2 law)**. **F.W1 must NOT be credited for the pagination-drain cure (FR-GIG-5)** — that lock lives in F.W1's spec, restated here so F.W9 never books it either.
4. **HARNESS-BEFORE-RIDER (PAW-50)** governs every vitest-shaped disposition — PAW-12, App C-13, ContourPreview 38, FR-AUL-16's devDependency limb, AA-44: **decision F.W4 · execution F.W9 · terminal record F.W10.** **Flag-before-split (PAW-49)** rides it.
5. **SEAM-BEFORE-SEAT** — FR-AFP-42's mountable surface (F.W3/W4) precedes its test seat; the seat is never folded into AA-44 (testability ≠ coverage).
6. **CURE-BEFORE-EVIDENCE** — F.W3/W4 lands B-3's four un-fixme'd keystones (G-F9-8), FR-TT-1's 17 naming legs (G-F9-5/-22), FR-EQR-6's tabindex+`aria-label`, D·D-B2's `aria-label` ×2, GCM-4's `<DialogTitle>`, PaperSidebar D-B2's token and C-M4's `CollapsibleTrigger` restructure, PAW-12's predicate split, FR-GFC-27's rider, EasingPicker C/m-6's picker test — **before** the corresponding F.W9 gate is admissible.
7. **AXE-SEAT ECONOMY** — the `/equation` seat is **ONE** artifact (FR-EQR-6 · FR-EQR-31 · FR-EMT-11 · FunctionInput C-13 · C·D-28 · D·D-B2): author once, cite six times. The `/paper` seat is **ONE** (D-B2 · M7 · PAW-34 · D/i-1) and **runs AFTER PAW-1's cure + the PAW-44 scrollport decision**. The admin seat **extends** the existing keystone; it does not clone it.
8. **MATRIX-BEFORE-COARSE (FR-USB-16)** — the mobile/coarse Playwright project lands before **any** coarse-pointer witness (glass `[data-size="icon"]` floor · PaperSidebar `--ui-scale:1.5`/`--control-floor:2.75rem` · FV-11's 44-vs-60px · ImageUpload 27's drag path).
9. **CONSOLE-GUARD INTERACTION (GCM-42)** — **no widening of the error-only filter to `warn` before F.W1 lands**; declare it, or F.W9 reddens F.W1 on pre-existing warnings.
10. **D/i-1 COUPLED LOCK** — `/paper` search coverage **rides the B-1/B-2 cure wave**, never scheduled independently.
11. **CWV ORDER-LOCK (FR-AH-1)** — any LCP measurement before F.W4's payload prune is measuring `sun.json`: take the trace after F.W4, or publish a **stated-invalid** baseline.
12. **REPAIR-ARMS-A-DEFECT SAME-COMMIT RIDERS** (carried where their rows land, F.W3/W4; restated so F.W9 sequences around them): fr-PaperSearchModal **D-1 + D-3 + `outline:none`** · **PAW-44 / LAW-3** restore-only-with-background-cure (the scrollport restoration lands **WITH-or-AFTER** PAW-1 **and** PAW-30) · **MPC-31 / MPC-3⊕10⊕13⊕8⊕22 one-cut law** · **FR-MSP-6 two-channel lock** · **LAW-4** one clearance authority · **N-2's label+locator same-commit rider** (G-F9-12) — this one **is** F.W9's.
13. **M2 HARD LOCK** — F.W4 must not rename `.sidebar-top-btn` before the latex-paper relay lands; **no `/paper` selector F.W9 authors may depend on that class.**
14. **ROUTE LAW** — **direct parse-that → fourier is FORBIDDEN** (parser → Value PLAW-BIND → V.L1/V.L5 → packed Value release → Fourier F.W0). **This wave mints no such edge.** Glass-producer rows ride SS-6, **never** become frontend hacks. **MEASURE-AT-OPEN** on every relayed number. Deploy spine is **Arm A, bump-independent** — it does not wait on F.W1.
15. **EVIDENCE LAW (R3-9 + inv-27)** — no gate closes on an assertion that something did not run; every green cites a covering run id; **a gate may not re-derive its own oracle** (KF.W4).

### §4b Cross-edges (declared from this end; each owed a reciprocal sentence in the named file — the X·V W4-D2 lesson, now law)

| to | edge |
|---|---|
| **F.W0 · re-ground** — reciprocal owed | **SUBSTRATE PRE-GATES FIRST**, and **G-F9-20** is its witness: F.W9 evidence taken before F.W0 measures (a) a producer dist that does not parse (M1) and (b) a tree no commit holds (GAB-13, 28 rows re-measured). Also F.W0's: F8-REACH-01/02 (**HOLD is RED** — FR-COB-20's harness branch cannot be authored for a file that may be DELETE; F.W0 rules the exact verb `KEEP_WITH_MOUNT \| ISOLATED_HARNESS \| DELETE`); MG-θ ≡ M7's tsconfig flags; ⟨re-cut⟩ **PAW-49**; FR-AFP-54/-55 manifest banding; fr-EasingPicker's stale-build fact; the Codex residue set (R3-9 · R3-16+R4-11+R5-9+R6-10 · R4-12 EVAPORATED · R5-3 · the still-registered `~/.codex/worktrees/d0be/fourier-analysis`); **R3-9 is routed here and carried in §2.7 as this wave's standing evidence law**. **OG-F1 / OG-F2 owner-owed, flagged inline, never presumed** |
| **F.W1 · atomic tri-package uplift** — reciprocal owed | ATOMIC land-or-lose; precedes F.W3/W4 (§2 law). Consequences declared **from this end**: (i) GCM-42's console widening is **LOCKED** until F.W1 lands; (ii) F.W9's console-error gates may not be read as product defects across an unlanded F.W1; (iii) AA-2's re-measured 37-file / 38 `size="icon"` / 124 `variant=` break surface belongs in **F.W1's** table before any budget commits; (iv) **F.W1 must NOT be credited for FR-GIG-5's pagination-drain cure**; (v) the **G33 pin leg** (npm ls ELSPROBLEMS today) greens only via F.W1's transaction |
| **F.W3 / F.W4 · shadow retirement + frontend audit** — reciprocal owed | **CURE OWNERSHIP: F.W9 owns gates, never cures.** Required before the matching gate is admissible: B-3/R-2's un-fixme (G-F9-8) · FR-TT-1's 17 naming legs (G-F9-5/-22) · FR-EQR-6's tabindex+label · D·D-B2's `aria-label` ×2 · GCM-4's `<DialogTitle>` · PaperSidebar D-B2's token + C-M4's `CollapsibleTrigger` restructure · PAW-12's predicate split · FR-GFC-27's rider · EasingPicker C/m-6 · FR-AFP-42's mountable seam. **The vitest-floor DECISION is F.W4's (census §4.5 is the only test language in the census); F.W9 EXECUTES it** (⟨re-cut⟩ PAW-50) |
| **F.W4 · CWV order-lock (FR-AH-1)** | **HARD ORDER-LOCK on G-F9-18** — any LCP taken before F.W4's async boundary + generator-side payload prune measures `sun.json` (450,631 B / 82.5 % long-float literals / 75,210 B dead; FR-AH-7 348,707 B; FR-AH-2 402,944 B). The value.js **Q14** escalation is the named precedent. Trace after F.W4 or a baseline stated-invalid on its face. FR-GIG-15 rider: the three-rung INP floor is unreachable in the shipped tree — **do not cite lane-frontend :568 as hygiene** |
| **F.W5–W8 / SS-4 · CRUD-provenance union** — **both-ends declaration required** | **F.W9's inv-28 fail-closed gate is a DEPLOY gate and must never be read as an API-shape gate.** SS-4 must name the **TA-4 value-side atomdiff restoration** a PREREQUISITE **or re-scope the contract explicitly** — `lib/crud/atomdiff.ts` + `atomDiff` on `PaletteVersion` were excised at T.W1/TA-4, so `J-diff-shape.md §6`'s close-gate probes cannot run value-side and **a co-signature over un-runnable probes is void**. SS-4's owner rulings are **FLAGGED INLINE, never presumed**: trie-vs-KISS (the documented anti-tree guardrail at `atomdiff.py:12-14` is the **INCUMBENT** — dissent recorded), remix-vs-fork, born-visibility, visibility-enum, version-`_id`, redaction parity, pagination idiom. FR-GV-36's dead-API fact is declared here; **the API repair is not booked in F.W9**. Neither side's edits gate the other's waves |
| **F.W10 · coordination discharge + close** — reciprocal present (F-W10 §4) | **The digraph splits here.** F-W10 §2.2 carries the authoritative 26-identity table; **G-F9-21 ≡ G-F10-6 are twins and neither spec stamps before it closes.** **F.W9 EXECUTES the P-12 packet; F.W10 owns the terminal-disposition record only.** Handed to F.W9 per F-W10 §4: AA-44 · FR-AFP-42/-49 · FR-GSB-17 · FR-USB-16 · FR-EQR-6 · FR-EQR-31 · FR-EMT-11 · FunctionInput C-13 · C·D-28 + D·D-B2's axe leg · GCM-42 · FR-GV-36 · GAB-10's coverage half · FV-26 · App C-13 · ContourPreview 38 · ImageUpload 27 · PAW-34 · PAW-12's rider · PaperView D/i-1 · PaperSidebar D-B2/C-M4/M7 · FR-AUL-16's execution limbs · FR-COB-20's harness branch. Three ⟨re-cut⟩ away: FR-TT-1 → F.W10, PAW-49 → F.W0, PAW-50's decision → F.W4. **F.W9 owns the deploy spine; F.W10 owns the correspondence** (ASK-3 · ASK-5 · INV-22-COLOR · CASCADE-VJS · FN-1..FN-7) — census-declared split. FR-AUL-16's and FR-AH-45's terminal homes are decided **AT F.W10** |
| **X-W11 G8 · real-Safari evidence cell** | **NON-OVERLAP, declared to prevent a false green.** F.W9's CWV and axe evidence is **CHROMIUM** evidence (`playwright.config.ts:46-51`, one project) and **may NEVER be cited as Safari evidence.** The intake's five corroborating rows (V6-32 controls 0/44 · V6-33 iOS Simulator Safari 0 · V6-34 desktop Safari 0 · CM-22 0/44 + D2 0/887 · XF-6 zero Safari on every surface) support G8's premise and clear none of it. Webkit-vs-Safari-app separation law: `X-W6 §MATRIX` / I-20. **FR-USB-16's coarse project (G-F9-13) is a matrix change, NOT a Safari cell** — it discharges no G8 row. OG-V1/OG-V2/OG-V3 remain owner-gated |
| **SS-6 · glass-ui BH relay (owner edict)** | **ASK-ONLY; producer rows NEVER become frontend hacks.** Riding the standing batched letter, not F.W9's bounds: M1's dist-generation bug (producer source at HEAD is intact) · L-10/D-M4's ramp-headroom rebaseline (four failing light-arm stops incl. **3.58 at stop 11**) · GAB's tier-tint home + the 3.26:1 label rung · FR-EQR's light-arm `--success` 2.175:1 · AA-3's violet `batch` tone with no home in 7.0.0's five-member TONES · FR-CP-11/27 · AppHeader's undeclared cm-serif variable + light `--tier-featured`. **F.W9 may not cure any of these consumer-side to turn a gate green** |
| **LATEX-PAPER-RELAY (register created at F.W10)** | **ASK-ONLY. HARD LOCK from fr-PaperSidebar M2: F.W4 must not rename `.sidebar-top-btn` before the relay lands** — latex-paper's shipped `useSidebarFollow` hard-codes fourier's private scoped class in its pointer-exemption, so a rename silently converts scroll-to-top clicks into sticky manual-override suspensions (D-B3's and D-M8's cures both touch it). **Any `/paper` spec selector F.W9 authors must not depend on that class either.** Anchor law **SC-3**: installed latex-paper 0.2.1 ships no composable src — cite `dist/vue.js`, never producer-src. **L-11** (a third ungated rAF clock, no PRM gate) rides the same relay and amends lane-frontend §8 |
| **SS-3 / SS-4 · spec authoring (NO-WAVE-OWNER drain)** | **FR-AUL-16 and FR-AH-45 have no honest home today**; P-12 proposes their execution limbs here and F.W10 decides terminal. **If the widening is declined, the decline is recorded explicitly with rationale (M-25)** — leaving them un-homed re-creates the exact blind spot that let every row in this packet open and stay open. Also routed as spec input: FR-COB-20's no-test-for-wrappers observation · AA-45/-46/-47 · FR-AFP-51/-52 · **FR-CP-43/-44/-45** (verbatim law: take D's CLAIMS and L/C's LINES; **re-resolve every anchor before quoting into a wave spec** — honoured at §2.8 C-4) · **fr-App C-12 as a NEGATIVE record** (App is API-inert; a negative finding is a terminal disposition — do not manufacture work) · **GAB-29** (MetricPill — the falsifier that keeps the wave off two dead primitives) |
| **KF.W4 · transferable gate-design law (intake §3)** | **BINDS EVERY GATE THIS WAVE AUTHORS** — (a) one immutable map keyed by physical coordinate; (b) reauthenticate every payload node and checksum row at use; (c) read every declared predecessor authority, never allowlist; (d) derive every envelope assertion from the pinned artifact; **and a gate may not re-derive its own oracle.** Joined by **R3-9**. **No absorption of KF rows**: B18-12/13/14, the `345/12/57 @ 8281638c` boundary block and B18-27 are **KF.W10's alone** (COHESION §3.1 — no id double-homed) |
| **parse-that → value.js → consumers (PLAW-BIND route law)** | **FORBIDDEN EDGE, declared to prove it is not minted here.** F.W9's evidence may not cite parser work as fourier coverage. Measured clean: 39 hits repo-wide, **0 of them source**; fourier's parsing is a 117-line hand-rolled regex (`web/src/lib/colors.ts`, **no `oklch()` arm**) — the W.L5 item-2 deletion target and the I-9 re-trigger sentinel, **owned by F.W2**. MG-ι relays into the V·π ledger with the owner-mail flag; **no fourier wave adopts it** |
| **fourier sub-session (COMMISSION §2)** — ask-only | **value.js declares, it never edits fourier's tree** (see §1b's write-seat declaration). Their writes: the M.W2/W3/W4/W11 board dispositions on `docs/tranches/M/PROGRESS.md`; the fourier-side INBOX + CLAUDE.md (census F.W0's act — fourier has no E13 analogue and its live value.js mail path is the flat `docs/tranches/N/`); facility-19 tracking (currently UNTRACKED `??`); the two parity-probe residuals re-homed to their M.W12. **F.W9 relays the deploy-spine asks and verifies receipt.** ASK-3 and INV-22-COLOR **infrastructure execution is value.js-maintainer-owned and outside every F wave's product bounds** |

---

## §5 Excluded (nothing silently dropped; every exclusion reasoned; **zero CARRY rows among them**)

| excluded | reason |
|---|---|
| **Every cure named in §2** — FR-EQR-6's tabindex/`aria-label`, D·D-B2's two labels, FR-TT-1's 17 naming legs + shim, GCM-4's `<DialogTitle>`, PaperSidebar D-B2's token + C-M4's `CollapsibleTrigger`, PAW-12's predicate split, GAB-4's `aria-busy`, B-3's un-fixme, FR-GFC-27's rider, FV-31, EasingPicker C/m-6 | **F.W3/W4 owns every cure; F.W9 owns gates only.** Cited as admissibility preconditions, never booked |
| `web/tsconfig.json` flags — MG-θ ≡ M7's `noUnusedLocals`/`noUnusedParameters`, and ⟨re-cut⟩ **PAW-49**'s `noUncheckedIndexedAccess` | **F.W0's**: one substrate edit, two records, **booked once**. PAW-49 is carried in §2.6 for parity with F-W10 §2.2 and is explicitly **not booked here** |
| **M1 / GAB-13 / F8-REACH-01/02 dispositions · the Codex residue verbs · OG-F1/OG-F2** | F.W0's rows; carried in §2.1/§4a-2 and G-F9-20 for gating only; owner rulings flagged inline, never presumed |
| **PAW-50's DECISION leg** (as opposed to its execution) | ⟨re-cut⟩ to **F.W4** — census §4.5 is the only test language in the census. F.W9 executes what F.W4 authorizes |
| **FR-TT-1** (as a booking) | ⟨re-cut⟩ to **F.W10** — it governs the close-gate design. It **binds** F.W9 through the promotion lock (G-F9-22) without being booked here |
| **The API repair behind FR-GV-36's "dead API"** | The **edge** to the deploy-spine reckoning is declared (G-F9-15/-16); the repair is **SS-4 / the value.js API row's**, not F.W9's |
| **ASK-3 · ASK-5 · INV-22-COLOR · CASCADE-VJS · FN-1..FN-7 · the 2.0.0 note · O-14 parity** | **F.W10 owns the correspondence; F.W9 owns the deploy spine** — the census-declared split. ASK-3's rsync/deploy-dir infrastructure execution is maintainer-owned and outside every F wave's product bounds |
| **Every producer cure** — M1's dist generation, glass ramp headroom, GAB tier-tint, `--success` 2.175:1, `--tier-featured`, latex-paper's PAW-1/2/11/28/45/46, emitter semantics, the flattener | **SS-6 / the LATEX-PAPER-RELAY.** Producer work in producer trees: **this wave asks, it never edits — and a producer row never becomes a frontend hack to turn a gate green** |
| **The pagination-drain credit (FR-GIG-5) · lane-frontend :568 as hygiene** | Locks live in F.W1's spec and G-F9-18's rider — recorded here so neither is claimed by this wave's greens |
| **FV-30 · D-20's archaeology half · PAW-33** | Ride banked rows elsewhere (BasisCanvas D-4/BC-2/C-2a + AnimationControls C-12 · FR-EQR-27's correction · the C-19 ⊕ PAW-33 pin-truth row, F-W10 §2.5) — no new booking |
| **Any edit to `registry/adjudicated/fr-*.md` or to `F-W10.md`** | Read-only bounds + anti-rename — **the re-cut is a table in two specs and one census addendum**; F.W9 does not author F.W10 |
| **The 14 boilerplate routing-law lines, the 12 verdict-prose mentions, and the 7 boilerplate-only records** | Boilerplate **mints no rows** (the RE-CUT-0 dissent's explicit warning); verdict prose is dated adjudication bytes, immutable, annotated by the addendum only. The REAL rows (FR-AUL-16 · FR-COB-20 · F8-REACH-01/02) enter by mechanism at §2.2's entrant block |
| **KF.W10's rows** — B18-12/13/14, the `345/12/57 @ 8281638c` boundary block, B18-27; **the keyframes half of ASK-5** | KF's close / KF's mail seat — **declared, not absorbed; no id double-homed** (COHESION §3.1) |
| **Safari evidence of any kind** | F.W9's matrix is chromium + one coarse project. **X-W11 G8's cell is not touched, not discharged, not partially credited** |
| **Every fourier-tree write** (M board disposal, their INBOX/CLAUDE.md, facility-19 tracking) | COMMISSION §2 — asked in their sub-session; F.W9 relays and verifies receipt |
| **R4-12's row contents** | Consume-nothing lock — and moot: the file evaporated (fold-measured) |
| **A direct parse-that → fourier credit edge** | **FORBIDDEN** by route law; declared at §4b so it is provably not minted here |
| **Any product-source open in either tree; any status advance beyond `planned`** | **The execution gate** — nothing opens, writes, builds or runs product source until the owner's begin-word |

---

**Close act.** When **G-F9-1..22** are GREEN — each citing a covering run id and a committed SHA (inv-27; R3-9), none re-deriving its own oracle — F.W9 stamps **IMPLEMENTED**, hands its terminal records to F.W10 (**VERIFIED is F.W10's close to stamp, never this one's**), and files the §4.10 census-addendum record plus the E13 INBOX rows. E13: no close with UNREAD mail — the 4-path sweep opens every session. **L-18 still applies**: no wave is ACCEPTED until two quartet gestalt passes adjudicated by a fresh Fable instance. The bases that apply here: a harness stood up ahead of the decision that authorizes it; a coverage gate greened by a vacuous assertion of the exact class this wave un-vacuates; an axe artifact argued green over 17 open naming legs; a console-guard widening landed before F.W1 and blamed on the product; a CWV number taken while `sun.json` is still in the entry chunk; a containerization "audit" that re-read the compose file it was supposed to falsify; a deploy called current from a dated 44-behind number nobody re-measured; and a re-cut that looks complete because both specs agree — while agreeing on the same wrong count (§2.8 E-1). Until then: **planned**, born-RED, every witness named above.
