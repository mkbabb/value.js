# X.F.W5 — The shared-provenance API contract (ADMISSION KEYSTONE)

**Sub-tranche**: X·F (`docs/tranches/X/fourier/waves/`), authority `docs/tranches/X/COHESION.md` §0/§1 SS-4. Validated against `docs/precepts/instructions/tranche/WAVE_SPEC.md` (L-20, read at authoring 2026-08-28).

**Fold provenance (M-23 / M-25 / M-12)**: this file is the FRESH-FABLE FOLD output, 2026-08-28 — the Opus author half (Draft A) arbitrated against the adjudicated registry itself.

▲ **PROVENANCE AUTHORITY (R-3, `docs/tranches/X/fourier/conformance/PASS-1/RULINGS.md`): there is no `F-W5-CARRY.md`, and none is authored here.** The only carries in tree are `docs/tranches/X/fourier/carry/F-W1-CARRY.md` (F.W1) and `docs/tranches/X/fourier/carry/F-W4-CARRY.md` (F.W4), citable **by path and STABLE ANCHOR only — never by line** (R2-2.2: a line number into a live carry is a defect; ⟨cmd⟩ `ls docs/tranches/X/fourier/carry/` → `F-W1-CARRY.md  F-W4-CARRY.md`, re-run read-only 2026-08-28). This wave's operand is therefore the **66-record adjudicated registry** at `docs/tranches/V/megatranche/registry/adjudicated/fr-*.md` (⟨cmd⟩ `R=docs/tranches/V/megatranche/registry/adjudicated; ls "$R"/fr-*.md | wc -l` → **66**, this seat, repo root) — read for its bytes, never for a denominator.

▲ **THE RECORD-SIDE DENOMINATOR IS NO LONGER DERIVED HERE; IT IS THE CANONICAL'S (repair round 4, rulings R4-5 · R4-3 · R4-10).** `docs/tranches/X/fourier/conformance/CENSUS-CANONICAL.md` is **the one census of record** — *"a gate that cites another census operand is DEFECTIVE on sight"* — and this wave's record-side roster is its **§2 `F.W5` roster (28 rows) ⊕ its §2 `F.W5-W8` roster (90 rows) = 118 record-side rows**, adopted verbatim and enumerated per record at §2c. **F-W5's own `170` is STRUCK** (R4-5), and struck for its construction, not its size: its four printed shape arms were false at two of four by exactly cancelling ±40, and the total was assembled as `167 + δ` — the forbidden check-file figure with an addition in front of it. **`162` (pass 1), `167` (round 2) and `170` (round 3) are all superseded; none may be quoted as this wave's operand**, and neither may any check file, any pass index, nor any §-arithmetic of this file's own (R4-10: *no wave derives, samples, or re-cuts a roster from any other source*). **This file therefore DERIVES NOTHING ARITHMETICALLY about the record side.** It counts its own register lines — a count of what it wrote — and cites the canonical for the roster; every canonical id is **booked in a clause or cited to its holder**, with the per-record landing table at §2c. §2's ROSTER DETECTOR survives as the **reading instrument** for record bytes; it is no longer a rival census and no figure it returns is a denominator.

**FOUR source coordinates** — not two, and none of them a "tail row" — rest on paths that exist and were re-read at the bytes this round. ▲ **THE BASE IS DECLARED IN THIS BLOCK, ONCE, BEFORE ITS FIRST USE (repair round 4, rulings R4-2.3 + R4-4.5; PASS-4 D-4).** Every ⟨cmd⟩ in this block runs from the **repo root** under this assignment, published as part of the receipt:

```
R=docs/tranches/V/megatranche/registry/adjudicated
```

Two are **adjudicated-corpus ranges**, line-citable because the corpus is frozen and immutable (R2-2.2(i)): `$R/fr-FourierShapeExtractor.md:81-87` (⟨cmd⟩ `sed -n '81,87p' "$R"/fr-FourierShapeExtractor.md` → the ids `L-m3 / C-10 · L-m4 / C-12 · L-m5 · M-5 · M-7 · M-8 · M-9`) · `$R/fr-AdminAuditLog.md:87-91` (⟨cmd⟩ `sed -n '87,91p' "$R"/fr-AdminAuditLog.md` → `AA-44 · AA-45 · AA-46 · AA-47 · AA-48`) ⟨**THE PURGE-SEAT CURE IS UNWOUND AND RE-DONE, R4-4.5.** Round 3's purge seat found both `sed` receipts banked **with no file operand** and "fixed" them by rooting to `$R` — **a variable this file defined ZERO times** (3 uses, 0 definitions), so the cure re-committed its own defect one level down: the command remained exactly as unrunnable as the bare form it replaced, and `PASS-3/CLOSE-CERT.md` §3d certified it fixed. `R=` above is the actual cure. Re-run by this seat 2026-08-29 with `R` assigned as printed: both ranges return exactly the ids listed. The FSE ids are additionally re-spaced to the record's own spelling (`L-m3 / C-10`, not `L-m3/C-10`) — the compressed form is a rendering, and this block renders nothing it calls a quotation.⟩ ▲ **THE UNIVERSAL IS STRUCK AND REPLACED BY THE MEMBERSHIP TEST IT SKIPPED (repair round 3, PASS-3 D-2).** This block formerly asserted of the seven FSE ids that *"every one … is homed in a **§G clause** row, not a tail"* — **FALSE at two of the seven**, and manufactured the only way such a claim can be: by enumerating a raw line range without testing membership of the ids inside it. The honest split, per id, this seat 2026-08-28: **five land** — `L-m3/C-10` and `L-m5` at **G10c** · `M-7` and `M-9` at **G9c** · `M-8` at **G3c**. **Two do NOT, and could not**: ⟨cmd⟩ `sed -n '82p;84p' fr-FourierShapeExtractor.md`, terminal cells → *"`id` rides as Button fallthrough at every pin. → **F.W4**."* and *"…the most load-bearing missing sentence in the file. → **F.W4**."* — `L-m4/C-12` and `M-5` are **F.W4-routed**, so homing either in an F.W5 §G clause would be a double-booking; their absence from this file is the one-home law working, not an escape. **The STRUCTURAL claim this block exists to make survives whole, and is now the only one it states**: neither range is a "tail row". ⟨cmd⟩ `grep -n '^### ' fr-FourierShapeExtractor.md` → the last band heading at or above `:81` is `69:### MINOR (21)`; ⟨cmd⟩ `grep -n '^### ' fr-AdminAuditLog.md` → the last at or above `:87` is `85:### INFO`. Both ranges sit inside a graded band. **All five AdminAuditLog ids are carried BY CITATION, none booked in a clause** — `AA-44` → F.W9/W10 (§5) · `AA-45`/`AA-46`/`AA-47` → **F-W10 §2.5**'s NO-WAVE-OWNER drain, cited at A1/A2/A3 (R3-6) · `AA-48` → F.W4 (§5) — stated here so this coordinate is never again read as a booking claim. Two are **carry rows, cited by stable anchor**: **`F-W4-CARRY.md` §0's CORRECTED DENOMINATORS bullet** (P-9's zero-cell export) and **`F-W4-CARRY.md` §Rows D — the transport cluster — its `SS-L-07/SS-C-10` bullet**.

*Historical session note, no gate weight*: earlier folds self-reported a 112-of-126 and then a 128-row receipt. Both are **authoring self-reports over an artefact that does not exist**; neither is G19's operand and neither is cited anywhere below. Per M-25, a spec ignoring adjudicated rows is DEFECTIVE and transcription-only is DEFECTIVE — this file agglomerates: dedupes by identity, carries cure-shape locks and sequencing edicts verbatim, routes by mechanism.

**EXECUTION IS NOT AUTHORIZED BY THIS FILE.** Tranche X's execution gate stands: nothing **writes, builds, or runs** product source in either repo until the owner's begin-word. **READ-ONLY measurement is the only way a born-RED witness exists at all** — §3's value-side witnesses (G2, G4, G5's value leg) were so obtained, 2026-08-28, and are pasted with their commands; no witness in this file opens a shell that mutates either tree. `$F` = `/Users/mkbabb/Programming/fourier-analysis` is **READ-ONLY, always** — this wave writes zero fourier bytes and every fourier-side witness is MEASURE-AT-OPEN (D-19); the co-signature travels as a letter. `$V` = `/Users/mkbabb/Programming/value.js`. Specifying is not doing.

**Status**: **planned**. (Every status field in this file stays `planned` until the begin-word.)

---

## 0. State

**Name**: F.W5 — the shared-provenance API contract (ADMISSION KEYSTONE)
**Opens after**: **F.W0** (substrate pre-gates — HARD, §4 X-1) · the **intake adjudication** (`docs/tranches/V/megatranche/audit/codex-provenance/INTAKE-ADJUDICATION-2026-08-03.md` §3 F.W5 + §2 C-3 — an adjudication record, **not a carry ledger**, per R-3) · the adjudicated fourier registry whole (66/66)
**Agents**: 5 **serial** — units b/c/d all write `J-diff-shape-v2.md`; §1c forces sequencing, never parallelism
**Hard gate**: 22 conditions in §3, **all born-RED**, each with a named witness. Four are ⊙ OWNER-GATED and may not be authored unruled (G4, G7, G10, G11).

| verb | value | evidence |
|---|---|---|
| AUDITED | **YES** | the adjudicated band at `docs/tranches/V/megatranche/registry/adjudicated/fr-*.md` (this wave's 66-record witness set) · `docs/tranches/V/megatranche/formation/fourier/lane-crud.md` (R-1..R-7, §6, §7, F-α/β/γ, V-α/β/γ) · `docs/tranches/V/megatranche/audit/codex-provenance/INTAKE-ADJUDICATION-2026-08-03.md` · `docs/tranches/V/megatranche/audit/codex-provenance/api-gap-remeasure.md` · **`docs/tranches/X/fourier/conformance/CENSUS-CANONICAL.md` — THE CENSUS OF RECORD (R4-3/R4-10), this wave's sole record-side roster** — **all five `ls`-resolve; unrooted spellings are forbidden vocabulary (R-10.5's law, adopted wave-locally), and the `…/codex-provenance/…` ellipsis this cell formerly used is spelled in full above**. ▲ **THE THREE RECEIPT BASES ARE DECLARED HERE, FILE-WIDE (repair round 4, R4-2.4; PASS-4 D-7 convicted three-to-four undeclared cwds).** Every ⟨cmd⟩ in this file runs from **one** of exactly three bases, and each is named at its site by variable: `R=docs/tranches/V/megatranche/registry/adjudicated` (the 66 records — `fr-*.md` spellings) · `X=docs/tranches/X/fourier` (siblings — `$X/waves/F-W0.md`, `$X/carry/F-W4-CARRY.md`, `$X/conformance/CENSUS-CANONICAL.md`) · **repo root** (`docs/tranches/…` spellings, `$V`-rooted product paths). A bare `fr-*.md` / `F-W0.md` / `F-W4-CARRY.md` receipt below is read under `$R` / `$X/waves` / `$X/carry` respectively; the assignment is published in the block that first uses it, per R4-2.3 |
| SPECIFIED | **YES — by this fold, 2026-08-28** | Draft A (Opus half) + fresh-Fable fold arbitration = this file (M-12 tri-fold); L-20 conformance closes at the X·F sub-tranche loop |
| IMPLEMENTED | **NO** | contract bytes land only after the begin-word |
| VERIFIED | **NO** | stamped only at X·F's sub-tranche release close |

### 0a. Goal criterion

The wave succeeds if the shared-provenance contract becomes **a neutral document both repos co-sign**, rather than a fourier-tree file value.js binds to by reference — and if every clause in it is *ruled*, *declined-with-reason*, or *⊙ owner-gated and named as such*, so no later wave repairs a defect this contract silently reproduced. **A clause that names no witness is not a clause.**

### 0b. What this wave is NOT

It does not repair. It authors three documents and one letter. Every render arm, client repair and server cure it names is handed onward with **one home and two citations** (§4). F.W5 claims credit for none of them — FR-GIG-5's lesson adopted as a standing bar. **FR-AUL-13 is this wave's own indictment: a contract DOCUMENT WITHOUT A PROBE repeats the defect it convicts** — hence G18/G20 and the F.W9/W10 edge.

---

## 1. Bounds

### 1a. Files owned — six paths, zero additions (five documents to **create**, one append-only ledger row)

| File | Access |
|---|---|
| `$V/docs/tranches/X/fourier/waves/F-W5.md` | create (this spec — the arbitrated fold) |
| `$V/docs/tranches/X/fourier/contract/J-diff-shape-v2.md` | create — **the neutral co-signed contract** |
| `$V/docs/tranches/X/fourier/contract/operation-register.md` | create — 45 rows, authority class + client-edge disposition. **G8/G9/G19 grep this file; without it they are prose** (G21) |
| `$V/docs/tranches/X/fourier/contract/OWNER-RULINGS-F.W5.md` | create — the inline ruling block SS-4 requires |
| `$V/docs/tranches/X/coordination/value-to-fourier-cosign-J-diff-shape-v2.md` | create — the relay letter; the only vehicle by which fourier receives v2 (G20) |
| `$V/docs/tranches/V/coordination/INBOX.md` | **append-only** — one E13 ledger row for the relay; no rewrite of any existing row |

Reconciliation note: the five content paths plus the INBOX row are exactly the six above, and the set is **derived from this spec's own gate needs** (G8/G9/G19/G20/G21/G22), not inherited from any carry ledger (R-3). `operation-register.md` and the relay letter are *load-bearing for gates*, not scope creep — G8/G9 are ungreppable without the register and G20 is unfalsifiable without the letter. **None of the FOUR to-be-created artifacts exists yet.** The quantifier is narrowed and each receipt is paired with the paths it actually covers — the blanket "none of the five" was false of its own first member and its single receipt did not reach its last:

- ⟨cmd⟩ `ls $V/docs/tranches/X/fourier/contract/` → *No such file or directory* (re-run read-only 2026-08-28) — **covers three**: `J-diff-shape-v2.md` · `operation-register.md` · `OWNER-RULINGS-F.W5.md`.
- ⟨cmd⟩ `ls $V/docs/tranches/X/coordination/` → exactly one file, `ATLAS-TO-VALUE-2026-08-03-RULINGS.md` (re-run read-only 2026-08-28) — **covers the fourth**, the relay letter, which does **not** live under `contract/` and was therefore never reached by the first receipt (this is G20's own witness).
- The fifth path, `waves/F-W5.md`, **EXISTS — it is the file making the claim.** It is marked `create` because the wave authored it; it is not a member of any negative-existence set, and no gate is born-RED on its absence.

Every sibling wave citing the four cites **prospectively, WHEN-created** (R-10.4). G19/G20/G21 are honestly born-RED on the four, not on the five.

### 1b. Do NOT touch — every path below, without exception

- `$F/**` — the whole fourier tree, read-only law. **v1 (`J-diff-shape.md`, fourier tree) is IMMUTABLE**; v2 supersedes **by reference** and never edits it (E-3: addenda, not patches).
- All product source both repos — `$V/api/src`, `$V/src`, `$V/demo`, `$F/api`, `$F/web` — execution gate.
- `lane-crud.md` · `INTAKE-ADJUDICATION-2026-08-03.md` · `api-gap-remeasure.md` · `registry/adjudicated/fr-*.md` — **E-1/E-3 immutable beside the spec**: they hold the witnesses, and rewriting one destroys the evidence a gate cites.
- `docs/tranches/X/COHESION.md` — deliberately excluded. F.W5 declares **its half** of every cross-edge in §4 and *requests* the reciprocal; it does not write the spine.
- `scripts/dev/dev.sh` — unowned dirty row, never staged. Pathspec commits only.

### 1c. Units and disjointness (serial; no worktree plan — one writer on clean `tranche-u`)

| unit | owns | writes |
|---|---|---|
| **a** | the operation register — 45 = 30 + 13 + 1 + 1, one row per operation | `operation-register.md` |
| **b** | v2 §A wire shape + §B the join relation | `J-diff-shape-v2.md` |
| **c** | v2 §C authority + §D denominator/coverage + §E provenance/persistence | `J-diff-shape-v2.md` |
| **d** | v2 §F equation contract + §G geometry + the value-side obligation list | `J-diff-shape-v2.md` |
| **e** | the ruling block, the relay letter, the INBOX row, **G19 carry closure + G22 cite resolution** — *fresh adjudicator seat* | `OWNER-RULINGS-F.W5.md`, relay, INBOX |

b/c/d share one file ⇒ **sequenced across sub-waves** (WAVE_SPEC §4a): a → b → c → d → e, each committing before the next opens. A dirty tree at handoff halts the wave. Third-iteration halts (triumvirate): the register disagreeing with the 45/30/13 triple a third time (K-1's prefixed-router blindness is the known trap); **a `(record, id)` pair of the CANONICAL F.W5 band failing to home three times** — ▲ **the halt's operand is CORRECTED A SECOND TIME, and this time onto a roster a seat can actually run (repair round 4, ruling R4-5; PASS-4 D-18).** Round 1 keyed it to *"a **census row** (§1's PASS-1 roster)"* — a roster §1 does not contain (§1 is *Bounds*) and the one operand this wave bans. Round 3 re-keyed it to *"§2c's fresh corpus-derived roster"*, which cured the FORM and inherited the defect: that roster did not reproduce and its membership was undetermined at three ids, so the halt moved from a forbidden operand to an unreproducible one. **The lawful operand is the same one G19 now runs on: `conformance/CENSUS-CANONICAL.md` §2's `F.W5` (28) ⊕ `F.W5-W8` (90) rosters, verbatim, record-qualified** — never a check file (R3-4), never a pass index, never this file's own arithmetic (R4-10); MF-9 failing to resolve (then it is **struck**, and v2 quotes GCM-10's cure without it).

---

## 2. Carry — the adjudicated band agglomerated (the canonical F.W5 band: 28 ⊕ 90 = 118 record-side rows)

**Operand**: **`docs/tranches/X/fourier/conformance/CENSUS-CANONICAL.md` §2 — the `F.W5` roster (28) ⊕ the `F.W5-W8` roster (90)**, adopted verbatim (R4-3 · R4-5 · R4-10). ▲ **NEITHER A CHECK FILE NOR THIS FILE'S OWN ARITHMETIC IS THIS WAVE'S OPERAND (repair round 4, rulings R4-3 + R4-5; extends R3-4.5).** Two idioms are struck together. The first was round 2's pin to `conformance/PASS-2/F-W5-CHECK.md` §1's landing table — **a closure/census transcript whose LHS is a count copied from any check file FAILS BY CONSTRUCTION**, since a gate whose operand is a superseded seat's enumeration can only re-find that seat's misses (it did: `fr-EquationPanel:48` was bucketed as *"prose, no enclosing roster row"* in the copied table while THIS file books both of its routings, at C4 and F9). The second was round 3's *replacement* for it — this file's own re-derivation, `170`, which R4-5 struck for the same class of failure one level down: a decomposition whose middle terms were false by ±40 and a total assembled as `167 + δ`. **A wave does not author its own census.** The canonical does, once, for all eleven waves; this file books or cites its 118 rows and counts only the register lines it wrote. Dedupe is **by identity, not by text**: where two records witness one mechanism the clause carries **both ids** and books once. Cross-listed ids appear elsewhere **by citation**, never by re-booking. `‡` = BLOCKER-weight · `⊙` = OWNER-GATED (unauthorable unruled) · `▲` = cure-integrity lock (binds the *repair*, not the finding).

▲ **ROSTER DETECTOR — DEMOTED FROM OPERAND TO READING INSTRUMENT (repair round 4, R4-3/R4-5/R4-10).** This block formerly opened *"the shape- and spelling-agnostic **operand**"* and ruled that *"every census, closure and set-difference in this file runs over ONE roster, defined here"* — i.e. a roster this wave cut for itself. **That is precisely what R4-10 forbids** (*no wave derives, samples, or re-cuts a roster from any other source*), and it is how three rounds of oscillation happened: each pass re-invented the detector and got a different number. **The roster is the canonical's; the detector below reads RECORD BYTES and settles nothing about membership.** It is retained because the file quotes records constantly and every such quotation must survive both dash spellings, every row shape and a token boundary. Its figures are receipts about the corpus, never denominators (R2-9's authoring bar still binds: a probe stating a shape- or spelling-restricted operand is DEFECTIVE whatever it returns):

1. **SHAPES (all four book a row; shape-restriction is a defect at authoring, not a result)** — a markdown **table row** whose terminal cell routes; an **id-headed bullet** routed in context (`→` · `**ADJUDICATED →**` · `⇒` · `rides` · `routed` · bold-at-disposition); a **prose routing** walked back to its enclosing row id (S-8); and a **bare-first-cell table row**. There is no shape in which a routing does not count.
2. **SPELLINGS** — the token set `{F.W5, F.W5-W8, F.W5–W8}` probed in **BOTH** U+002D and U+2013, every slash/span form expanded to its member set, and the id match **token-bounded**, never bare-substring. The bare-substring trap is live in this corpus and measured: ⟨cmd⟩ `grep -rl 'P-9' fr-*.md | wc -l` → **6**, but the boundary-exact probe → **0** — all six are `FR-AFP-9`/`FR-CP-9` artefacts. An unbounded probe manufactures six phantom homes for an id that occurs in none of the 66 records (§5's P-9 row is measured, not asserted). ▲ **THE BOUNDED PROBE IS RE-SPELLED PORTABLE, AND SHIPS WITH A POSITIVE CONTROL (repair round 3, R-10.6 applied to this file's own instrument).** The former spelling was the bracket-class ERE `grep -rlE '(^|[^A-Za-z0-9-])P-9([^0-9A-Za-z-]|$)'`. Its printed digit (**0**) is *true*, but the probe **is not discriminating on every toolchain that will re-run it**, and ▲ **THE RECEIPT NOW NAMES ITS ENGINE, BECAUSE THE RESULT IS AN ENGINE FACT (repair round 4, R4-2 + R4-7.1; PASS-4 D-10 convicted the unqualified spelling).** Re-run by this seat 2026-08-29 under **both** binaries: `grep -rlE '(^|[^A-Za-z0-9-])K-13([^0-9A-Za-z-]|$)' fr-*.md | wc -l` → **40 under `/usr/bin/grep` (BSD)** · **0 under the interactive `grep`, which on this host is a shell function routing to `ugrep`**. The former text printed the `0` unqualified, as though it were a property of the pattern rather than of one binary. It is a property of one binary — which is exactly the hazard: a seat whose `grep` is the shim reads a phantom green and cannot tell a real ∅ from a broken pattern. **A probe that returns zero for everything has proved nothing.** The spelling this file uses is `grep -w`, which reproduces identically under **both** `/usr/bin/grep` and the `ugrep` shim — re-run by this seat 2026-08-29 from the base declared at §0 (`R=docs/tranches/V/megatranche/registry/adjudicated`; the bare `fr-*.md` spellings below are read under it): ⟨cmd⟩ `grep -lw 'P-9' fr-*.md | wc -l` → **0** · **positive control, same command shape**: ⟨cmd⟩ `grep -lw 'AA-45' fr-*.md | wc -l` → **1**, ⟨cmd⟩ `grep -lw 'K-13' fr-*.md | wc -l` → **40**. The control is part of the receipt, not decoration: **the ∅ is admissible only because the same instrument returns non-∅ on a token that is there.**
3. **BAND PROBE (R-6), reproduced by this seat 2026-08-28 to the digit** — ⟨cmd⟩ `grep -rho 'F\.W5-W8' fr-*.md | wc -l` → **120** across **26** records; ⟨cmd⟩ `grep -rho 'F\.W5–W8' fr-*.md | wc -l` → **72** across **24** records; union **192 occurrences / 49 records**, of which ⟨cmd⟩ `comm -13` of the two file lists → **23 en-dash-ONLY**. A hyphen-only census is structurally blind to those 23; **no clause below may be sized from one spelling.** The same figures reproduce at **`F-W8.md` §0's intake-authority paragraph** and at **F-W8 §3 gate `G15`'s A-2 amendment** — cited by section and gate id, never by line (R2-2).

▲ **ANTI-RENAME + RECORD-QUALIFICATION (R-5 / U-12)**: banked ids are original for life. Eight tokens collide across records inside this band — **M-13 · L-B1 · L-M3 · C-17 · C-18 · B-1 · B-2 · C-2** — so every occurrence below carries its record (`fr-EquationView B-1`, `fr-ContourSettings B-1`, …). Set-differences run on the qualified form; short forms are never authoritative. ▲ **A NINTH collision, `M-10` — RE-MEASURED AT CORPUS WIDTH AND WITH ITS UNITS SPELLED (repair round 4, rulings R4-7.1/R4-7.3 + R4-2; PASS-4 D-5 · D-6 · D-19 convicted all three halves of the round-3 text).** The round-3 receipt was ⟨cmd⟩ `grep -c 'M-10' fr-ContourSettings.md fr-BasisSelector.md`, and it failed three ways at once, each the inverse of a rule this very block states: (i) **a two-file restricted detector**, three sentences after ruling that a restricted probe is *"DEFECTIVE at authoring, whatever it returns"* — and one paragraph before curing exactly that defect for `K-13` with a 66-record bounded probe; (ii) **an unbounded substring probe**, immediately after ruling the id match *"token-bounded, never bare-substring"*; (iii) **`grep -c` counts MATCHING LINES, not occurrences**, and the round-3 "rendering cure" re-labelled the line counts as occurrence counts — banking a wrong number inside the block whose whole subject is that a count must be spelled as what it is. All three are cured with one portable, bounded, corpus-wide instrument, re-run by this seat 2026-08-29 under `/usr/bin/grep` from the `$R` base (identical under the `ugrep` shim):

- **width** — ⟨cmd⟩ `grep -lw 'M-10' fr-*.md | wc -l` → **23 of the 66 records**. The collision is 23 records wide, not two.
- **occurrences, spelled as occurrences** — ⟨cmd⟩ `grep -ow 'M-10' fr-ContourSettings.md | wc -l` → **2** · ⟨cmd⟩ `grep -ow 'M-10' fr-BasisSelector.md | wc -l` → **4**. (For contrast and disclosure, the *line* counts are ⟨cmd⟩ `grep -cw 'M-10' …` → **2** and **3**: `fr-BasisSelector` carries four occurrences on three lines, which is the exact gap the round-3 mislabel hid.)

**FOUR `M-10`s are managed by this file and all four are record-qualified at their sites**: **`fr-ContourSettings M-10`** — the closed-domain/shared-enum row at **D12** · **`fr-BasisSelector M-10`** — the `basisFilter` normaliser named in **D10**'s WAVE-LOCK · **`fr-CanvasControlsDock M-10`** — the banked NO-WAVE-OWNER reader-LC row, an excluded mis-attribution named at §2c · **`fr-AnimationControls L-11/M-10`** — the fold at **E11**. A bare `M-10` is not an identity and may not enter a set-difference at either end. The U-12 eight remains the *banked* roster; this ninth is a **seat-measured addition, recorded as such, renaming nothing** (anti-rename: every id stays original for life).

▲ **THE DECLARED COLLIDER ROSTER IS WIDENED TO THE ONE THE FILE ACTUALLY MANAGES (repair round 4; PASS-4 D-17).** The block above declared eight ⊕ `M-10` ⊕ `K-n`-as-widest, while this file's own disambiguation notes manage more — so a later seat reading the declaration alone inherited a smaller collision set than the file assumes. The declared roster is therefore: the **U-12 eight** (`M-13 · L-B1 · L-M3 · C-17 · C-18 · B-1 · B-2 · C-2`) ⊕ **`M-10`** (23 records) ⊕ **`M-9`** (`fr-BasisSelector` at D12 vs `fr-FourierShapeExtractor` at G9c) ⊕ **`C-7`** (five sites: B1 · B2 · C1 · E9 · G7c, across `fr-BasisCanvas`, `fr-ConvergencePlot` and `fr-FourierShapeExtractor`) ⊕ **`C-3`** (`fr-FourierShapeExtractor` at G3c vs `fr-GalleryInfiniteGrid` at D2) ⊕ **`R-7`** (FOUR identities, not three: `lane-crud §R-7` at C3/E18 · `GCM-3 R-7` at E6's repair-test lock · **`fr-GalleryInfiniteGrid R-7`**, now booked at D10 — pass-4 D-3's escape, and it escaped *because* this roster under-declared) ⊕ **`R6-8`** (four records carry the intake carry: `fr-AnimationControls` · `fr-CanvasControlsDock` · `fr-ConvergencePlot` · `fr-EasingCurvePreview`) ⊕ the record-LOCAL **`K-n`** index as widest of all. Widths re-run this seat 2026-08-29: ⟨cmd⟩ `grep -lw 'C-7' fr-*.md | wc -l` → **46** · `C-3` → **53** · `R-7` → **32** · `M-9` → **18** · `R6-8` → **37**.

▲ **THE KILL-REGISTER INDEX `K-n` IS THE CORPUS'S WIDEST COLLISION, AND THIS FILE'S OWN LOCKS BROKE THE LAW ONE LINE ABOVE (repair round 3, PASS-3 D-6; R3-5's fourth detector axis applied at this end).** `K-n` is a **record-LOCAL** kill-register index — every record numbers its own kills from 1 — so a bare `K-13` is not an identity at all, and it is wider than any member of the U-12 eight: ⟨cmd⟩ `grep -lw 'K-13' fr-*.md | wc -l` (this seat, 2026-08-28) → **40 of the 66 records**. The block above ruled that *"a bare `M-10` is not an identity and may not enter a set-difference at either end"* and then left every `K-n` in this file unqualified — including **two different records' `K-13` used as two different locks**, at **F2** (`fr-EquationView K-13`, whose banked bytes ⟨cmd⟩ `sed -n '146p' fr-EquationView.md` head as *"| K-13 | L·B-2's path 2 …"*) and at **F7** (`fr-ConvergencePlot K-13`, ⟨cmd⟩ `sed -n '129p' fr-ConvergencePlot.md` → *"| K-13 | C-8's cure ("route `result.latex` to the original curve") | RATIFIED: …"*). **The law is now stated once and applied**: *every `K-n` citation in this file carries its record; a bare `K-n` may not enter a set-difference, be quoted as a lock, or be matched by G19 at either end.* The qualification is landed at the two locks that broke it (F2, F7); the remaining `K-n` mentions in this file (`K-1` §1c/D1 · `K-3` D7 · `K-5`/`K5` D10 · `K-6` D16 · `K-12`/`K12` E5 · `K9` A4 · `K10` D5) are **F.W5's own shorthand for a kill it is carrying, never an id it books** — they enter no roster, no clause's rows-folded cell and no set-difference, and that exemption is stated rather than assumed. **Nothing is renamed**: both `K-13`s keep their banked spellings inside their own records.

▲ **THE TWIN PARTITION IS CONSUMED, NOT RE-DERIVED — `F-W3.md` §X.1-v4 IS THE AUTHORITY WHEREVER AN F.W5 CLAUSE TOUCHES THE F.W3/F.W4 SEAM (repair round 3, ruling R3-1a; v4 landed FIRST this round and this file reads it as landed bytes).** F.W5 books only **sole-`F.W5`** spellings, so the file criterion and this wave never contend — but four of its clauses cite ids the twins partition, and each is now cited at v4's anchor rather than assumed:

- **`fr-BasisCanvas BC-20`** — E8's lock (*"`duration = ref(20000)` zero-writers stays banked at BC-20 — NOT re-booked"*) is confirmed at the other end by v4's own probe: **§X.1-v4's `18`-vs-`19` SLACK block** records `BC-20 -> F-W3.md · F-W5.md · F-W7.md · F-W8.md` and classifies the F.W3 hits as **record-qualified citations, not bookings**, with the id resolving **to F.W4** on the criterion. E8 cites; F.W4 holds; nothing here re-books it.
- **`fr-CoefficientsSpectrum`** — A5 holds this record's **`M-13`** on its own **sole** `F.W5–W8` routing (`:65`). **§X.1-v4 item 3** enters the record into the F.W3 `.d` share for its **dual**-routed residue (`m-19` · `m-22` ⊕ the ids axis (iv) admits) — the record v3's *"every"* was false by. **Two disjoint bands of one record, two waves, no contention**; the A5 homonym note (M-13 is `fr-CoefficientsSpectrum`'s here and `fr-ContourSettings`'s at E14) is unchanged.
- **`fr-ContourEditorCanvas C-2`** (E17) and **`fr-BasisCanvas C-5` / `C-7` / `C-17` / `C-18`** (B1 · E9 · E15) — all **sole-`F.W5`** at the record, so the criterion's *"sole spellings home at their named wave"* arm sends them here; **§X.1-v4 item 2's F.W4 hand-off names none of them** (its `fr-ContourEditorCanvas` set is the `MM-*` band; its `fr-BasisCanvas` set is `BC-*`/`M-α*`/`M-β*`/`D-10-remnant`). Disjointness is measured at v4's rows, not asserted here.
- **`fr-VisualizationView L-26`** is **NOT F.W5's** in any reading and never was — **§X.1-v4 item 4** rules it to **F.W3** as a minuted one-id departure from R2-3b. It is named here only so a reader of the keystone cannot mistake this wave's silence for an escape.

**Clause-id compatibility**: sibling specs and this file's own §4/§5 cite v2 clause ids (`A3`, `B4`, `B5`, `C1`, `C2`, `C4`, `C5`, `D1`, `D7`, `E10`, `E15`, `E16`, `G7c`); this fold preserves each id for the same content so those cross-references resolve. Where a sibling's citation is mis-keyed, it is conformed **at the sibling's end** (R-1e) — never by renaming a clause here. **No clause is numbered D9** — `D9` is reserved throughout for the *ruled owner decision*, which **no clause may contradict**.

▲ **RULING D9 — ROOTED HERE, ONCE, AT FIRST USE (the file's hardest cross-cutting constraint, formerly pathless).** §0's AUDITED cell adopts R-10.5's law wave-locally — *"unrooted spellings are forbidden vocabulary"* — and this file bound five clauses (E4, R8, G5, the value.js API row, and this note) to an id with no address. Cured at the bytes, this seat:

- **Home**: `docs/tranches/V/DECISIONS.md` **§2 "Product and architecture rulings", row `D9`** — cited by section and row id, never by line. ⟨cmd⟩ `grep -n "D9" docs/tranches/V/DECISIONS.md` → one hit, whose bytes are: *"| D9 | Palette visibility is `private | public`; owner lifecycle is `active | trashed`; admin moderation is separately clocked `clear | withdrawn`. The unused `unlisted` state dies. Non-owner reads require active/public/moderation-clear and a visible immutable release. |"*
- **Corroborating restatement** (a formation packet, NOT the authority): ⟨cmd⟩ `grep -rn "D9" docs/tranches/V/megatranche/formation/codex-worktree-7e28/VALUE-FORMATION-PACKET-2026-07-29.md` (this seat, 2026-08-28) → **TWO hits, and the whole return is printed rather than the convenient one** (repair round 3, PASS-3 D-14): `:55` *"Product authority: `DECISIONS.md` D9 is binding."* — the one this rooting rests on — and `:531` *"**Dependencies:** V.F0, D9–D13, and the absorbed vnext oper…"*, which is a **decision-RANGE in a dependency list, not a statement about `D9`**, and is disclosed here precisely because a receipt showing one of two hits cannot be told from a receipt showing one of one. The narrowing is the C4 idiom applied evenly. The packet **points at** `DECISIONS.md`; it is not the ruling. A Codex-worktree packet is the provenance class this program has spent the round re-rooting, and it is recorded here as a pointer only.
- **Two facts the register carries that this file must not lose**: the ruled visibility domain is **two-state** and *"the unused `unlisted` state dies"*, while value.js **today persists three** (⟨cmd⟩ `sed -n '61p' $V/api/src/modules/palette/model.ts` → `/** I.W1 canonical visibility (3-state): \`public\`/\`unlisted\`/\`private\`. */`, re-measured read-only 2026-08-28). That divergence is the **D9 reconciliation** the value.js API row owes (§4) — it is a value-side obligation, never a silent contract overwrite, and never a fourier defect.

Every later `D9` in this file cites **this rooting**, not a bare id.

### §A — Wire shape and envelope

| clause | rows folded | witness | lock / cure-shape |
|---|---|---|---|
| **A1** Row identity on the wire | **BOOKS NOTHING — cites only**: `AA-45` → **F-W10 §2.5** · `AA-48` → §5 (both *"same latent class — recorded, NOT re-booked"*) | `AuditEntry` carries four fields; pydantic extra-ignore drops `_id`; `AdminAuditLog.vue`'s `:key` is positional **because the contract offers no identity** | Every list-bearing response class in v2 carries a stable per-row identity. The `:key` deletion is **F.W4's act**; the clause is F.W5's. ▲ **`AA-45` LEAVES THIS CLAUSE'S ROWS-FOLDED CELL AND BECOMES A CITATION (repair round 3, ruling R3-6.1/6.2; PASS-3 D-1 convicted the double-home).** The row is banked **`NO-WAVE-OWNER`** — ⟨cmd⟩ `sed -n '88p' fr-AdminAuditLog.md`, terminal cell: *"Latent contract gap. **NO-WAVE-OWNER** (recorded for SS-3/SS-4)."* — and it **carries no `F.W5` token anywhere**, so it is not a member of the canonical `F.W5`/`F.W5-W8` rosters (the canonical homes it **NWO→SS-3**, §2c). Its **ONE home is `F-W10.md` §2.5, the NO-WAVE-OWNER drain** (COHESION §3.3 deadline authority), whose row already carries a terminal verb: **NEXT-BOUNDARY LEDGER — API-contract row (identity on the wire), SS-3/SS-4 input**. This clause cites that row by id and stands on its **mechanism**, which is unchanged and was never in dispute: the contract offers no identity, so the `:key` is positional by construction. **`AA-48` was already carried on exactly this model** (*"recorded, NOT re-booked"*, §5) — the cure is the AA-48 treatment applied evenly to the row that was denied it, and the standing tie-breaker is now explicit: **a banked NO-WAVE-OWNER row's one home is the drain; an F.W5 clause needing its mechanism CITES the drain row and never books it** (R3-6.5). AA-48 discharges free with AA-11's F.W4 footer cure |
| **A2** Envelope + cardinality ruled once | **FR-AFP-18** (D-10) — the clause's one banked operand; cites **D10**, and cites `AA-46` → **F-W10 §2.5** as EVIDENCE only | `page`/`pages` discarded and re-derived client-side (agreeing today; the page↔offset round-trip is lossless); the moderation queue has **no size anywhere** — no `count_documents`, `has_more` via limit+1, no `AdminStats.flagged`, six vanity MetricBadges | ONE envelope decision settles pagination, queue size and the featured window jointly. Raw material: fourier opaque cursors + `Link: rel=next` (`lib/crud/cursors.py`) vs value.js offset/limit + `total` (`platform/http/pagination.ts`) — lane-crud §6. FR-AFP-18's render seat is F.W1 (metric-badge→metric). ▲ **`AA-46` IS NOT AN OPERAND OF THIS CLAUSE, AND ITS KILL STANDS (repair round 3, ruling R3-6.1/6.3; PASS-3 D-1).** The two dispositions were **directly contradictory**: F-W10 §2.5 rules it **KILL-WITH-RATIONALE — *"a redundancy record; no defect, no work"*** while this clause made it an operand of an envelope decision. **The contradiction is resolved AT THE BYTES, in F-W10's favour** — ⟨cmd⟩ `sed -n '89p' fr-AdminAuditLog.md`, terminal cell: *"Envelope `page`/`pages` discarded and re-derived client-side (agreeing today); page↔offset round-trip lossless. Redundancy records. **NO-WAVE-OWNER**."* A **redundancy record with a lossless round-trip is not a defect**, and the drain's kill is the byte-faithful reading. **This clause loses nothing**: the envelope decision stands whole on **FR-AFP-18** and **D10** — the moderation queue has no size anywhere, and that is a live gap. AA-46 may be cited as envelope *evidence with the kill noted*, never as an operand |
| **A3** Boundary-validation **evenness** | **FR-CP-16** (=C-17/L·A-3, the denominator) · *(cites `AA-47` → **F-W10 §2.5**, not booked)* · GAB-12 (C-7) · GCM-10 ⊕ GCM-55 · FR-IC-6 (→L·m-7) · ContourPreview row 28 (D:i-3/L:L-8) · FR-FG-13 (=C-9) · C-15 (FSE, build-time) | **The measured denominator: the whole 45-operation client surface lands as ONE unchecked `(await res.json()) as T`.** AA-47's uneven pair (timestamp+target guarded, action+ip_hash not) is what survives pydantic's server-side shape guarantee; drift on the admin banner degrades into six silent em-dashes — no console error, no toast, no test; `save_contour` takes `xs`/`ys` off `dict[str, Any]` with **zero cardinality validation** | One posture per response class — *validated* or *explicitly-not-validated-and-why*. **Evenness is the clause, not paranoia.** ▲ Do NOT author a per-field defensive sweep at F.W4 (L-19). GCM-10/GCM-55 = the **serialization-bypass family**: `json.dumps` bypasses the Pydantic model, so `likes ?? 0` and bare `{{ entry.likes }}` assert opposite nullabilities — **the response goes through the model, or the model is decoration**. FR-FG-13 keeps C's own *"today it is safe"* honesty — a **latent-ordering** clause, not a live defect. FR-IC-6: the posture covers **persisted-then-rehydrated** payloads too (versioned keys blunt the stale-schema limb; the backend-adds-a-tier limb survives whole). C-15: the rule covers **build-time artifacts**, not only HTTP. ▲ **`AA-47` LEAVES THE ROWS-FOLDED CELL AND BECOMES A CITATION (repair round 3, ruling R3-6.1/6.2; PASS-3 D-1).** Banked terminal cell, ⟨cmd⟩ `sed -n '90p' fr-AdminAuditLog.md`: *"…what survives is the *uneven* defensiveness (timestamp and target guarded; action and ip_hash not). **NO-WAVE-OWNER**."* — no `F.W5` token, and the canonical homes it **NWO (packet)**, not in either F.W5 roster; **one home at `F-W10.md` §2.5** with the terminal verb **NEXT-BOUNDARY LEDGER — SS-4 contract input**. The clause cites that row and keeps the **uneven-pair witness**, which is what it always used AA-47 for; the **denominator** the clause actually rests on is **FR-CP-16**'s measured 45-operation `(await res.json()) as T` surface, and that is F.W5-routed and booked. **Evenness is still the clause** |
| **A4** One contract source | FR-AFP-36 (L-16/L-21/C:D-20) · FR-AFP-71 ⊙ (β-miss-9) | the emitted `content_hash` — the actual join key — is omitted client-side; three dead server models encode the retired contract (`FlagRequest` / rotted `user_slug` `FlaggedEntryInfo` / offset `FlaggedListResponse`); **no `response_model` anywhere**. Structural cause: god-module twins (`api.ts` 672 L / `types.ts` 391 L), no codegen, three hand-maintained copies of the flagged shape, one already rotted | ONE generated/checked shape; the dead models delete. ▲ **K9 LOCK: do NOT delete the five `?? item.slug` fallbacks** — `admin.py` emits `doc.get("image_slug")` unvalidated, so they are DEFENSIVE; that limb is NOT PROVEN. ⊙ **Module split / codegen-vs-hand-typed-twins is the union's call**: inv-16/inv-26 (*one contract source, hand-typed twins, no codegen*) is the standing law v1 §1 asserts — **v2 RESTATES or AMENDS, never silently reverses**. ▲ **BOTH IDS ROOTED (R-10.5, same law as D9's rooting; formerly forbidden vocabulary here).** **inv-16** — `docs/tranches/V/megatranche/formation/fourier/lane-crud.md` **§0, the headline block**; ⟨cmd⟩ `grep -n "inv-16" docs/tranches/V/megatranche/formation/fourier/lane-crud.md` (this seat, 2026-08-28 — ▲ **the path is spelled LITERALLY from the repo root; the former `…/lane-crud.md` ellipsis could not execute as written, and R-10.6's law is that a gate command that cannot execute as written is a phantom witness: repair round 3, PASS-3 D-11**) → **TWO hits, both printed** (D-14's discipline): `:19` *"shared-by-contract PATTERN (inv-16), NOT a shared package"* — the rooting — and `:350` *"collection (palettes live in the value.js service, inv-16), so it cannot FK-validate the slug."*, a downstream restatement. Cited by §-anchor (§0, the headline block). **inv-26** — its PRIMARY ledger is the fourier tranche-local invariant ledger, **`$F/docs/tranches/INVARIANTS.md` §1**, which this file does not open (`$F` READ-ONLY, and no witness here reads it): the id is therefore rooted at its in-repo evidence home, `docs/tranches/R/audit/pass1/R4-FOURIER.md` **§6 evidence index**; ⟨cmd⟩ `grep -rn "inv-26" docs/tranches/R/audit/pass1/R4-FOURIER.md` (this seat, 2026-08-28) → ▲ **FIVE hits, not one — the whole return is disclosed and the operative one named (repair round 3, PASS-3 D-14; the former receipt printed `:332` alone, which is indistinguishable from a one-of-one return)**: `:169` · `:188` · `:207` · `:319` · **`:332`**, the last being the evidence-index line this rooting rests on: *"fourier invariant ledger (inv-16 shared-by-contract, inv-26 single-contract-source, inv-32 version-currency): `docs/tranches/INVARIANTS.md §1`"* (`grep -qF` of the full span → hit, this seat). The other four are the same id used in argument, not as an address. The fourier-side ledger anchor is **MEASURE-AT-OPEN (D-19)** like every other fourier anchor in this spec |
| **A5** No untyped operations | **fr-CoefficientsSpectrum M-13** (=C-7) · FR-EQC-8 (=C-11) · **`fr-NotationPills FR-NP-30`** (the record's own `FR-NP-30b` R6-8 sub-arm carried beside the banked head, never instead of it) | the epicycle response is `data: dict`; the only typed component model (`EpicycleComponentDTO`) is dead (one self-hit) and disagrees with the wire; `BasisComponent` is hand-maintained against a Python dict literal — **no derivable join**, while the equation route does it right in the same API; the coefficient crosses the seam as a **positional `[re, im]`** through two independently written remaps (a silent re/im swap type-checks everywhere); the `notation` enum is enumerated **four times** with a silent `.get(…, render_trig)` fallback | Verbatim, **at record case** (⟨cmd⟩ `grep -n -F 'unjoinable in principle' fr-CoefficientsSpectrum.md` → `:65`, this seat 2026-08-28, whose routing cell reads, **at the record's own emphasis and punctuation** — ⟨cmd⟩ `grep -qF` of this full span → hit, this seat: *"**→ F.W5–W8** (the R6-8 companion clause: an operation's response model must be structurally typed or the leaf is unjoinable in principle)."*): ***"an operation's response model must be structurally typed or the leaf is unjoinable in principle"*** — the former up-cased "IN PRINCIPLE" is STRUCK, same D-6 class as E13's. ▲ **AND THE CELL RENDERING ITSELF IS CORRECTED (repair round 3, PASS-3 D-9).** The former printing was *"→ **F.W5–W8** (…)"* — the arrow OUTSIDE the bold and the terminal period dropped — so the cell rendering failed a `grep -F` of itself while the inner core quote passed. **The arrow is inside the record's bold and the sentence ends in a period**; emphasis and punctuation inside a quotation are part of the bytes, exactly as this file ruled at B1 (C-30) and D7 (`fr-ContourSettings B-1`). The substance was never in doubt; the label *verbatim* now survives its own command **Load-bearing for all of §B** — if v2 admits untyped operations, every join clause it writes is unenforceable at those operations. **Name the exception set explicitly or close it.** This clause therefore PRECEDES §B's enforceability. FR-EQC-8 = one adapter, both directions (cites M-13 as family, does not re-book). A silent fallback is a **contract decision**: state it or close the domain. ▲ **`FR-NP-30` IS RESTORED AS THE BANKED HEAD; `FR-NP-30b` RIDES BESIDE IT (repair round 4, anti-rename R-5/U-12; the canonical homes `FR-NP-30` at `F.W5`, leg `F.W1`).** This clause and D12 formerly carried **`FR-NP-30b` standing alone** — and a sub-arm suffix standing alone is a rename, invisible to an id-keyed difference in **both** directions: the canonical's `FR-NP-30` could be found neither booked nor escaped, which is how the roster read one short. ⟨cmd⟩ `grep -n 'FR-NP-30' "$R"/fr-NotationPills.md` (this seat, 2026-08-29) → **THREE hits, all disclosed** (D-14's discipline; an unbounded probe, deliberately, because the suffixed forms are the subject): `:45` — a cross-reference from the `FR-NP-7` row to *"FR-NP-30a"* · **`:74` — the BANKED HEAD**, *"**FR-NP-30 · residual band — INFO.**"*, whose limb (b) is the one this clause consumes — *"the `notation` enum 4× enumerated, silent Python `.get(…, render_trig)` fallback — ties R6-8 → F.W5"* — while the record's own closing taxonomy at `:130` spells the routing *"**F.W5** (FR-NP-30b via R6-8)"*. **Both spellings are the record's; only `FR-NP-30` is the identity.** The `-30a` census-coverage limb is NOT this wave's (the record routes it `NO-WAVE-OWNER → SS-3/SS-4`) and the `F.W1` leg is held there — F.W5 books the head's R6-8 sub-arm and cites the rest |
| **A6** Credential transport — both ends | FR-USB-37 (C-i2) · FR-GV-7 (=M-9/C·M-9; cited at C3) | the bearer sits in a `title` attribute on a globally mounted element on **every route**, and is accepted from the **URL query string**, persisted to localStorage with no expiry | ONE transport clause bounds **URL in, DOM out**. ▲ FR-USB-13's F.W4 cure will likely rework the attribute anyway — **the F.W4 arm must NOT be credited with the contract** |

### §B — R6-8: operation identity independent of client identity (bidirectional)

| clause | rows folded | witness | lock |
|---|---|---|---|
| **B1** The join relation — forward | **R6-8** (ADOPT-AS-FACT, the wave's governing law) — booked here as **FOUR record-qualified canonical rows**: `fr-AnimationControls R6-8` · `fr-CanvasControlsDock R6-8` · `fr-ConvergencePlot R6-8` (all `F.W5`) ⊕ `fr-EasingCurvePreview R6-8` (`F.W5-W8`) · FR-AUL-3 (L-3/C-M1) · FR-GSB-28 · **fr-EquationView M-CK** · **fr-AnimationControls C-30** (`:123`, fold-by-reference) · **the `fr-CanvasControlsDock C-28` fold, carried INSIDE that record's `R6-8` row** (`:150` — R2-3d.5) · fr-BasisCanvas C-5 · row 26 C:C-12 (ImageUpload) · D-L4+C-7 (ConvergenceLegend) · FR-EQR-32 (C-§0.4) · AA-31 (C·D-12) · fr-FourierShapeExtractor C-7 (cited G7c) | `adminFetch` keys the abort registry on the full query-bearing path (`coreFetch<T>(path, /* abortKey */ path, …)` — the comment shows the parameter was considered and the wrong value chosen) ⇒ per-operation cancellation degraded to per-URL for **13** admin operations; its dual: `resetAndFetch()` and `fetchNextPage()` share ONE literal `"listVisualizations"`, so a filter refetch cancels an in-flight page **and the reset's synchronous `entries = []` can abort its own reset**; `computeKey()` returns 4 fields while the POST carries 7 (incl. `notation`, `budget`, both response-determining); the multi-basis fallback re-derives the server's `partial_sums` on the main thread (~160k iterations/frame); `after`/`before` implemented end-to-end and unreachable from any UI | **Carry the identity ONCE and cite the instances, or the wave re-books law as work (M-25).** Every instantiating record says the identity STAYS with the intake (fr-AnimationControls C-30, CanvasControlsDock C-28, EasingCurvePreview RESOLVER, fr-ContourSettings i-7, fr-BasisCanvas C-5) — **fold by reference, never re-book**. ▲ **C-30's JOIN-AMBIGUITY LEG named — the quote is the record's OWN BYTES, emphasis included (R2-1-LAW: a "verbatim" label without a ⟨cmd⟩ note is a defect per se, and a lock that cannot survive a `grep -F` of its own sentence is not carrying its source).** ⟨cmd⟩ `grep -n -F 'display surface of the ambiguity' fr-AnimationControls.md` (this seat, read-only 2026-08-28) → `:123`, whose row reads: *"Fold of intake **R6-8** (operation↔client leaves non-isolable); this component is the display surface of the ambiguity, not the defect site."* — and its disposition cell, same row: *"fold by reference — identity stays with R6-8's **F.W5–W8** carry."* **The former rendering STRIPPED the record's bold markers from inside the quotation** (`**R6-8**` → `R6-8`) and dropped the terminal period, so the "verbatim" label failed a `grep -F` of its own sentence — struck and replaced by the bytes above. F.W5's own voice, stated separately and quoting nothing: the leg is what F.W5 STATES and F.W4 renders; no repair is credited to this component. ▲ **THE `fr-CanvasControlsDock C-28` FOLD, seated beside C-30 (R2-3d.5) — carried inside that record's `R6-8` row, which is where the census of record puts it.** ▲ *Identity minute (round 4, R4-10's fabrication clause)*: this note formerly read *"FOLD ROW … the zero-byte single now has its one home, and it is here"*, which spoke of `C-28` as a row. **It is not one**: `fr-CanvasControlsDock` banks no `C-28` head in the canonical — the token is named inside the record's `R6-8` line, and under §0.1 an id claimed inside another row's body is not a standalone identity. That record's ONE canonical `F.W5` row is `R6-8`, booked above; **the C-28 material below is the body of that booking, and nothing here is a second row.** The substance is unchanged. Banked bytes, ⟨cmd⟩ `grep -n 'C-28' fr-CanvasControlsDock.md` (this seat, 2026-08-28): `:150` — *"**R6-8** (intake, CARRY→F.W5): C-28 folds — this dock is the UI head of the non-isolable client↔operation seam; D-4's server-arm rider sequences after the join split."*; `:102` — *"C-28 into D-4's F.W5–W8 rider"*. So C-28 is **the D-4 server-arm dedupe rider on the non-isolable client↔operation seam**: it books nothing new, it FOLDS BY REFERENCE into this clause's identity, and the sequencing it carries — *server arm AFTER the join split* — is E5's operative constraint, cited there and stated here. **ONE home (B1), two citations (E5's D-4 rider; F-W4's D-4 row); F-W8 names it at its §6a residue as an exclusion-with-reason (R2-7.1).** The record's own `:100` M-10 row stays **NO-WAVE-OWNER** and is NOT booked by this fold — the band token two lines below it belongs to the Full-coverage note, not to M-10. Cure = stable per-operation abort keys; FR-AUL-3 ∥ FR-GSB-28 are **one clause, two directions** (under- and over-broad — the over-broad direction is unbanked elsewhere). The abort-key fix and FR-AUL-59's pipeline fix **compose; neither alone closes the window**. Map-growth rider stays INFO. ▲ **fr-EquationView M-CK SEQUENCING LOCK, verbatim** — ⟨cmd⟩ `grep -n -F 'fix the key FIRST or the B-1 repair ships broken' fr-EquationView.md` (this seat, read-only 2026-08-28) → **`:71`** (frozen corpus, line-citable per R2-2.2(i)), whose bytes read: ***"fix the key FIRST or the B-1 repair ships broken."*** ImageUpload = the **incomplete** join (`thumbnailUrl` traverses NONE of `coreFetch`; 5 un-clienting `<img src>` sites): security half → C2, transport half → E15. FR-EQR-32 = the second seam instance (no SINK CLASS for string-typed response fields) — enforceable **only if F8 chooses the wire field**; if F.W5 declines, **the decline is RECORDED**. D-L4 ruled MINOR on the ConvergenceTimeline L·D-12 precedent — **dissent preserved** |
| **B2** The mirror direction ‡ | fr-ConvergencePlot C-7 | two contradictory `amplitude` conventions in one view, exactly 2× apart, both labelled `A`: the DTO ships `|c_n|` while `groupTrigHarmonics` discards `amplitude`/`phase` and re-derives `√(a²+b²)=2|c_k|` under the same name — tooltip `A = 0.8106` vs pill `A_1 = 0.4053`; FrequencyGraph documents the DTO convention, so **2 of 3 surfaces agree and the plot disagrees undisclosed** | **The R6-8 MIRROR RIDER: a client may not shadow a server field with a same-named different-valued re-derivation. The shared-provenance contract needs the rule in BOTH directions. Do NOT collapse it into the forward direction.** F.W4 arm: rename (`trigAmplitude`) or consume the DTO field |
| **B3** Wrapper types re-derived FROM the router | FR-AUL-11 (C-M2) · FR-AUL-12 (C-M3) · FR-AUL-13 (C-M4/L-11/L-23; second call site `GalleryView.vue:189-191`) | `deleteAdminUser` declares `{deleted; entries_deleted}` while the router returns `{"ok": True, "deleted_entries": …}` — **neither declared key is ever emitted**; `setAdminUserStatus` invents `slug` and drops `ok`; `BatchResponse.errors` is populated by no `admin.py` branch so partial batch failure reports **GREEN**, and `affected` = `modified_count`, so unsuspending 20 already-active users toasts *"Unsuspended 0 user(s)"* as success | One repair family (A.W5.c precedent shows the form). The binding indictment: `api.ts:617-624` is a **dated contract-repair record** for the batch family, and the two singular wrappers twenty lines above were left declaring shapes the router never emits — **in the same pass**. ▲ **FR-AUL-13 verbatim** (⟨cmd⟩ `grep -n -F 'either the router populates' fr-AdminUserList.md`, this seat read-only 2026-08-28 → one hit, `:51`): *"either the router populates `errors`/`matched_count` or the contract drops them; the client compares `affected` vs `slugs.length` either way"* ▲ **— the TERMINAL PERIOD IS REMOVED, because the record does not have one (repair round 3, PASS-3 D-8).** ⟨cmd⟩ `sed -n '51p' fr-AdminUserList.md \| tail -c 200` → the cell ends *"…`affected` vs `slugs.length` either way \|"*: it is a **table cell**, and it closes on the pipe. The former rendering ADDED punctuation inside a *verbatim* marker — the same class this file struck at B1 in the other direction (a dropped period), and it likewise failed a `grep -F` of its own sentence. `grep -qF` of the span exactly as now printed → hit, this seat (re-run 2026-08-29). ▲ *Punctuation note (repair round 4; PASS-4 D-12)*: the sentence terminator dropped here in round 3 is restored — two sentences were welded without punctuation **inside the cure block whose subject is terminal punctuation**, which is the smallest possible instance of a cure re-committing its own defect. Its *"written against a contract document rather than the router"* cell (⟨cmd⟩ `grep -n -F 'written against a contract document rather than the router' fr-AdminUserList.md`, this seat 2026-08-28 → `:51`; the former up-cased *"DOCUMENT"* is struck, D-6 class) is **this wave's own indictment — v2 IS such a document** ⇒ ships with the F.W9/W10 conformance probe or repeats the defect |
| **B4** The **LIVENESS** predicate (registry-novel) | PP-DEADSEAM (C-12) · GM-M4 · VV-R2-B (cited D14) | mutual dead capability: `preview_path` is written `""` at all three server sites while the client leaf is unreachable — **zero implementation on either side**; `softDelete`/`restore` are the gallery store's **only** splice/unshift mutators and its only ETag/If-Match round-trip, with **zero callers** | v2 needs a predicate for the case where a capability is declared on both sides and implemented on neither. ▲ **THE TRIPLE-STAR MARKER IS STRUCK FROM THAT PHRASE (repair round 4, ruling R4-1 item 6; PASS-4 D-11).** The `***"…"***` form is this file's idiom for **corpus bytes carried under a ⟨cmd⟩** — it is used that way at seventeen other sites — and this span had neither a command nor a source: ⟨cmd⟩ `grep -c -F 'declared on both sides, implemented on NEITHER.' fr-*.md` (this seat, 2026-08-29, `$R` base) → **0 across the 66**. It is **F.W5's own coinage**, which the cell already said in the next breath, and a coinage wears plain text. Nothing is lost but a false attribution: the predicate is genuinely new to the registry (both readers' sweeps + the seat's over 206 records) — it sits **BESIDE** R6-8's identity predicate, not inside it, and **separate from E10's produced-and-unconsumed disposition**. GM-M4's `:162-164` contract prose documents a round-trip no user can trigger — the prose dies with the disposition. **GM-M4 is the fact that KILLS L-15's scenario — carry the kill with the row** |
| **B5** Negative controls — **EVIDENCE, never a denominator** | HLG-23 (C:C-12) · C §3 negatives (MorphShapePreview) · P-9 (cited; home F.W4) | `/morph` reaches **ZERO** of the 45 operations and its static-JSON seam is `as any`-cast, voiding the one interface HLG-1 violates; R6-8 and R3-7b are **structurally unreachable** at MorphShapePreview | **F.W5'S OWN FORMULATION, marked as such and quoting nothing** — *a route with no operation edge has no join to get wrong*: the sentence is this file's compression of the banked negatives, and it carried quotation marks it could not earn (⟨cmd⟩ `grep -rn -iF 'join to get wrong' fr-*.md` → **zero hits across the 66**, this seat 2026-08-28 — R2-1-LAW.1: a quotation enters only as pasted command output, so an unfindable sentence loses its quotation marks, not its meaning). The banked bytes it compresses are quoted below with their command. **Must appear in v2's clause JUSTIFICATION and must NOT be counted in any defect denominator (P-9).** Preserved verbatim so **no F.W5-W8 row manufactures overlap** — ⟨cmd⟩ `grep -n -F 'no F.W5-W8 row manufactures overlap' fr-MorphShapePreview.md` (this seat, read-only 2026-08-28) → one hit, the record's own C §3 negative-controls cell. The `as any` cast folds → banked FMD-24 → N-14/N-15/FR-AH-1 (F.W4). Zero-cells copy to F.W2/F.W4 denominators — one home, two citations |

### §C — Authority, session and transport posture (R3-7b)

| clause | rows folded | witness | lock |
|---|---|---|---|
| **C1** Authority class per operation | **R3-7b** (CONTRACT DEFECT, un-refuted) · C-6/C-7 (EditorControlsDock) · GCM-52 (C-15) · F-4 (=C-C-8) | OpenAPI security **0/45** — auth parity cannot be SPECIFIED over a surface that documents none; the dock's only API seam is an **unauthenticated write with a client that always authenticates** (bare `save_contour(req)`, router with no `dependencies=`) which also **declines the `Idempotency-Key` channel its own client core declares** on the one non-idempotent-shaped call it can double-fire; the modal's only API call is unauthenticated, visibility-unchecked and publicly cacheable for a day; the thumbnail GET carries the same posture on the **pre-publication** surface | **Every row of `operation-register.md` carries an explicit AUTHORITY CLASS.** Identity stays with the intake at every instantiation (EditorControlsDock C-6, GCM-52, FR-AFP-4, GalleryDraftsSection F-4 all say so) — fold by reference. ▲ **fr-EditorControlsDock C-6/C-7, verbatim at record case** (⟨cmd⟩ `grep -n -F 'not an exploit, is the defect' fr-EditorControlsDock.md` → `:60`, this seat 2026-08-28): ***"The asymmetry, not an exploit, is the defect (content-addressed store honestly noted)."*** The former down-cased opening (*"the asymmetry …"*) is STRUCK — same D-6 class as E13's, in the other direction: a lock marked *verbatim* must survive a `grep -F` of its own sentence — v2 must not over-state it (the write is content-addressed and abort-keyed; it is **NOT** the sibling's publish-blocker), and the seam is recorded **R6-8-CLEAN as a positive**. F-4 contributes the **2²⁸ slug-entropy arithmetic** (`generate_slug` = 4× `secrets.choice` over 128-word lists). ▲ **THE BANKED HEAD IS CARRIED WHOLE — `fr-EditorControlsDock D-12 / C-4 / C-5` (repair round 3, PASS-3 D-18).** ⟨cmd⟩ `sed -n '59p' fr-EditorControlsDock.md` heads the row **`D-12 / C-4 / C-5`**; this clause formerly carried `D-12`'s rider **alone**, and a dropped alias limb is invisible to G19's id-keyed difference **in both directions** — it can neither be found booked nor found escaped. The full banked spelling is the identity (the discipline D17 already applies correctly to `fr-GalleryCard L·M-4 / D-13 / C-8(a) + L·D-2 / C-12`). Its rider rides the same seam: async commit, no pending state, no double-submit guard, reachable silent unhandled rejection |
| **C2** Image remediation contract — ONE unit | FR-AFP-4 (L-1/C:D-2, BLOCKER-weight survivor) · m-15 · GCM-52 (cited) · F-4 (cited) · ImageUpload row 26 security half (cited) | **NO action the moderation panel offers takes the reported artifact offline**: Delete is a soft-delete of the VISUALIZATION; `images.py` = 7 ops with **zero DELETE**; the blob GET has no `Depends` and ships `Cache-Control: public, max-age=86400`; and every anonymous blob GET ends in `touch_document`, bumping the janitor's recency predicate — **actively-fetched reported content is precisely what never reaps**; the unversioned thumbnail URL + 24 h public cache **negates the deliberate re-upload regeneration** | Delete/quarantine + blob visibility gate + **janitor predicate** + **thumbnail versioning**, as ONE unit. ▲ **C's scope discipline, verbatim at record case** (⟨cmd⟩ `grep -n -F 'soft-delete does de-list from browse' fr-AdminFlaggedPanel.md` → `:47`, this seat 2026-08-28): ***"C's scope discipline preserved: soft-delete does de-list from browse"*** — the up-cased *"DOES"* is STRUCK (D-6 class). Hard-arm zero-callers verified at the same row (`hard = false`, no third arg). ▲ **m-15 is CROSS-REFERENCED, NOT MERGED with F-4**: F-4 books *who can fetch*; m-15 books *what they see*. Deployment auth-proxy question stays UNPROVEN → SS-13 |
| **C3** Session truth and the canonical admin predicate | FR-USB-15 (L-1/C-§0/C-7) · FR-USB-23 ⊙ (L-10) · FR-AH-6 (C-6) · FR-GV-7 (cited A6) | `isLoggedIn` is a localStorage-**presence** check; `getMe` has zero call sites; `grep "401"` over `web/src` returns **ZERO**; `ensureUser` short-circuits on the stale value — a >30-day identity renders authenticated forever until a mutation fails in a vocabulary the component doesn't speak; the admin badge reads `galleryStore.adminMode` (a plain ref, 2 write sites, never rehydrated) while `auth.ts` restores `adminToken` and exposes `isAdminAuthenticated` with **zero consumers** — after reload the app is admin-capable and the badge is gone; `performBatchGallery` authorises directly off the stored token | 401 handling + `getMe` revalidation at the **STORE/API seam** — ▲ **the defect is authored in the STORE; do not route the cure to the leaf** (F.W4 renders whatever truth the store then has). One predicate, one home (F.W4); **the admin-auth seam owns WHICH predicate is canonical (F.W5)**. lane-crud §R-7 adopted: v2's session clause takes the **value.js form** (SHA-256 digest at rest; fourier persists the raw UUIDv4 as `_id`). ⊙ FR-USB-23: delete-or-wire the dead session subsystem (~17% of the store; `logout` never calls `clearSession`; the `else if (sessionToken)` arm would re-attach an anonymous identity on reload if ever wired). FR-AH-6 corroborates — and is corroborated by — AdminAuditLog K-5's *adminMode non-persisted* fact **without re-booking it** |
| **C4** Retry / limiter posture | FR-USB-24 (L-11) · D-L5+C-1 (EquationPanel) | the login path opts into the silent ≤60 s 429 auto-retry (`retryOn429 ?? true`, two sleeps ≤30 s) against a 5/60 s budget with no feedback, no cancel, Escape doesn't abort; a full-payload re-upload runs against the same 5/60 s compute limiter | Auth mutations pass `retryOn429: false` or surface the wait; the two-slot hardening question is answered in the contract. ▲ **ONE-CONTROLLER LOCK — the quotation CORRECTED to its real home and real bytes (R2-1-LAW: the former rendering was a paraphrase carrying a *verbatim* label, and it named no record at all — the convicted class).** The lock is banked at **fr-EquationPanel's ratified abort-seam superlative** (the D-L5/C-1 family this clause folds), ⟨cmd⟩ `grep -n -F 'cannot self-abort because the signal is hoisted outside' fr-EquationPanel.md` (this seat, read-only 2026-08-28 — the narrower fragment is deliberate: the bare *"signal is hoisted outside"* returns two hits, `:48` and `:69`, and a provenance note that does not resolve to ONE line is not a receipt) → `:69`, whose bytes read: ***"the 429 retry loop cannot self-abort because the signal is hoisted outside `while (true)` (lib/api.ts:161-164, my read) — a re-entrancy subtlety any C-1 hardening rewrite must preserve."*** The struck rendering (*"the 429 loop reuses ONE controller … a naive request-scoped rewrite BREAKS that property"*) restated the mechanism in words the record does not use; the property it names is real and is the one quoted above. **F.W5's binding, in F.W5's own voice**: a hardening rewrite of the retry loop preserves the hoisted signal, and FR-USB-24 (⟨cmd⟩ `grep -n -F 'auth mutations pass' fr-UserSlugBar.md` → `:64`, whose routing cell reads *"**→ F.W5-W8** (auth mutations pass `retryOn429: false` or surface the wait)"*) contributes the auth-side arm and carries no controller sentence of its own. Both rows share the lock |
| **C5** Redaction parity | F-γ / R-6 (lane-crud) | value.js collapses non-public hops to `{kind:"unavailable", ordinal}` and states *"no raw document or lineage field … ever crosses the wire for a non-public hop"*; fourier's breadcrumb applies **NO** redaction — a bare `find_one` emitting `slug`, `set_hash`, `author_slug`, `created_at` for every ancestor, because `_readable_or_none` is applied only to the ENTRY row | The breadcrumb redacts to the value.js discriminated-union shape, **or v2 states the asymmetry and why.** Today's witness is a private ancestor's slug on the wire — **the same "private artifact readable by slug" family as GCM-52** at the image seam |

### §D — The denominator, disposition and coverage

*(No clause is numbered D9 — see the compatibility note.)*

| clause | rows folded | witness | lock |
|---|---|---|---|
| **D1** Per-operation disposition — the register | **R3-7c ⊕ X-3** · FR-GV-8 (=M-6/L-12) · FR-GV-27 (=L-23) · **F-6 (= C-C-9 = L-15, fr-GalleryDraftsSection:49)** · AA-31 (cited B1) · FR-AFP-1 (INVERTED, cited D3) | **36 client edges / 9 gap operations**; **SEVEN of thirteen** `/api/visualizations` operations — remix, publish, unpublish, forks, provenance, diff, versions — have **zero client function** (three independent counts agree); `publish` PATCHes `{visibility}` behind an extra GET while dedicated `/publish`/`/unpublish` ship with no wrapper | Every gap operation carries a DISPOSITION (clientable / struck / deliberately server-only). ▲ **CENSUS CORRECTION FORCED (C-3): both sides were right on different denominators — never cite "30" as the whole API again. The triple is 45 = 30 public-non-admin + 13 admin + 1 app + 1 gallery.** ▲ **COUNTING LOCK K-1 (server-side blindness): a `@router.` grep is BLIND to prefixed routers (`@gallery_router.get("/cursor")`) — read against 13/44, never as zero.** ▲ **SECOND BLINDNESS LOCK — F-6 (= C-C-9 = L-15, `fr-GalleryDraftsSection.md:49`, routed *"F.W5 provenance-contract input"*), the CLIENT-side axis: *"the template-`src`-binding operation edge is structurally invisible to a function-keyed operation↔client model (the R5-7 dual)."* Unit a's register is function-keyed (45 = 30+13+1+1, one row per operation), so a `<img src>`/template-bound edge cannot appear in it by construction — the same class ImageUpload row 26 witnesses at B1 (`thumbnailUrl` traverses NONE of `coreFetch`; 5 un-clienting `<img src>` sites). **The register enumerates template-bound edges explicitly or it is armed against server under-count and unarmed against client under-count.** F-6's own severity stays INFO / NO-WAVE-OWNER (SS-3/SS-4 census methodology) — F.W5 consumes it as a register-construction constraint and books no repair.** **The seven unclientted operations ARE the provenance surface v2 re-authors.** Identity/provenance DISPLAY arms fold to banked **`fr-GalleryCard D-7 / C·I-2`** (▲ the FULL banked head, ⟨cmd⟩ `sed -n '52p' fr-GalleryCard.md` → *"- **D-7 / C·I-2 — MAJOR.** The entity's human identity is write-only: …"* — the `C·I-2` limb was formerly dropped at both this clause and E4; repair round 3, PASS-3 D-18), GCM-25, GCM-3 — do NOT re-book. FR-GV-27's phantom-view cell belongs to **E6**, not here |
| **D2** The like verb ‡ | FR-GFC-3 (C-1 ⊕ **fr-GalleryCard C-1-as-corrected** ⊕ GCM-2·C-1 ⊕ **fr-GalleryInfiniteGrid C-3** — **four witnesses, ONE identity**) | `likes:int=0`, the `(visibility, likes, -id)` compound index and `cursors.py:22`'s `"likes"` sort key ALL ship while **no like route exists anywhere** (`api.ts` grep 0; `api/routers/` grep 0; `liked_ips` only as 7 projection exclusions); `gallery.ts` hard-codes `const liked = true` with a monotonic unguarded bump, so the un-like arm is dead — **the server sorts by a counter nothing can increment**, under an `aria-pressed` that lies; `gallery.py:3` asserts the endpoint was carved out TO `visualizations.py`, which has no such route | ▲ **Add the operation or remove the affordance** — banked verbatim, at record case so a `grep -F` of the sentence succeeds: *"add the operation or remove the affordance — the honest repair is stated in C §7.1 and adopted: no third option ships"* (⟨cmd⟩ `grep -n -F 'third option ships' fr-GalleryFeaturedCarousel.md` → `:34`, this seat). The emphasis is the spec's; the **case is the record's**. **The store's own comment DOCUMENTS the stopgap — v2 must not treat it as intent.** Display arm (`aria-pressed` + re-click guard) → F.W3/W4. ▲ **THE ABBREVIATION `GIG` IS RETIRED FROM THIS FILE (repair round 4; PASS-4 D-16 — and D-3's escape rode this exact mechanism).** Record-qualification runs on the **(record, id)** pair; `GIG C-3` is resolvable by a reader and **not** by an id-keyed set-difference, which is why `fr-GalleryInfiniteGrid` scored **0** hits in this spec at pass 4 while two of its rows were substantively carried. Both sites now spell the record: `fr-GalleryInfiniteGrid C-3` here, `fr-GalleryInfiniteGrid C-4 / D-13` and `fr-GalleryInfiniteGrid R-7` at **D10**. **`fr-GalleryInfiniteGrid C-3`'s canonical home is `F.W5-W8`** (legs `NO-WAVE-OWNER` · `SS-3` · `SS-4`) and its banked disposition is *FOLDS BY REFERENCE to banked FR-GFC-3 — do NOT re-book*: it is booked **here, once, as the fourth witness of the one identity**, and its own new grain — the **orphan-client class** (a published emit contract with no reachable operation; R6-8's degenerate case) — rides its `NO-WAVE-OWNER → SS-3/SS-4` leg as census methodology, cited never booked. **Dissent: C axis filed BLOCKER at two sites, preserved against the banked MAJOR** |
| **D3** ⊙ Moderation producer-or-retire — **THE ADMISSION GATE** | FR-AFP-1 (L-1/C:D-1) | the queue has **NO PRODUCER**: two admin routes are the collection's entire live surface; every `db.flags` write repo-wide is a test fixture or a `$rename` migration; `FlagRequest` is declared and referenced nowhere; no client posts a flag; no UI renders a report affordance. Production's only reachable state is *"No flagged content"* — **~110 of 285 panel lines dead by reachability** | Build the user half (`POST /visualizations/{slug}/flags`, the mirror of lane-crud:336-339's value.js booking) **or retire the admin half whole**. **value.js HAS the verb fourier lacks (`POST /:slug/flag`) — the union's cleanest one-directional donation.** Extends R3-7c **INVERTED** (an admin half with no user half); the gap ledger carries both directions. ▲ **SEQUENCING LOCK (fr-AdminFlaggedPanel ruling 5 — the producer conditional), quoted at the record's bytes with its elision marked.** ⟨cmd⟩ `grep -n -F 'grade the populated surface' fr-AdminFlaggedPanel.md` (this seat, read-only 2026-08-28) → `:25`, whose ruling sentence reads: ***"Ruling: severities in this roster grade the populated surface; … FR-AFP-1 is the band's sole reachability gate, and the F.W5-W8 producer-or-retire decision must precede F.W1's sizing where grades are load-bearing."*** (the `…` marks elision of the ruling's L-2/K10 disposition clause — elision, never addition). **The former rendering is STRUCK**: it up-cased three spans, substituted *"the FR-AFP roster"* for *"this roster"* and *"wherever those grades"* for *"where grades"*, and dropped the `F.W5-W8` qualifier from the decision — a paraphrase under a *verbatim* label, which is the convicted class of this round (R2-1-LAW). **The obligation on F.W5 is unchanged and is stated here in F.W5's own voice**: G11's ruling precedes F.W1's sizing wherever FR-AFP grades are load-bearing |
| **D4** Flag identity keys the ENTITY | FR-AFP-7 (L-3/L-4) | flags key on the NON-UNIQUE `content_hash`: one artefact fans out to N rows each claiming the full count (3 rows × "3 flags" = an apparent nine), and the slug-labelled *Dismiss flags* is a hash-scoped `delete_many` that silently clears every sibling — while `content_hash` is documented *"never identity"* | Flag identity moves to the ENTITY, or dismiss scopes to slug. Contract-level |
| **D5** Which identifier a human-facing surface may render | FR-AFP-8 (L-2/C:D-3) · GCM-1's `/w/` shape (cited E4) | the destructive confirm names `image_slug` — an asset FK shared by every remix (`VisualizationRemix` declares no `image_slug`) — while DELETING `slug`, violating the identity law the file's own header states | v2 states **entity vs asset** for human-readable surfaces. **One-token F.W3/W4 rider: pass `item.slug` as the label.** **Dissent: the L axis filed BLOCKER; β's demotion to MAJOR sustained under ruling 5 (K10) — the L axis cannot carry two BLOCKERs when its first proves the second unreachable** |
| **D6** Lifecycle truth and cascade | FR-AFP-9 (C:D-5; member-site rider **FR-GV-34** = MISS-LC-5) · FR-AFP-66 (β-miss-1) | the dialog asserts IRREVOCABILITY over a **30-day-restorable** soft delete; soft-delete does NOT cascade flags, so a restored row returns to the queue still flagged; the purpose-built `?hard=true` grace-bypass has **zero callers**; the client ships an unreachable `gallery.restore`; and `pinned_cron` — which the softdelete docstring **NAMES** as the cascade owner — contains zero flags code, so Delete → grace **strands flags forever**, permanently inflating the unscoped `$group` | `content_hash`-keyed cascade at grace hard-delete **plus the docstring correction** (the module docstring is false for this band). ⊙ hard-arm caller decision → owner; truthful copy → F.W3/W4 (diction rides FR-AFP-35). **The copy is false in the direction that SUPPRESSES a safe action.** FR-GV-34 is a MEMBER-SITE RIDER — one identity, two call sites, **not re-booked** |
| **D7** **Client-derivable bounds** | m-7 (=C-6-as-rescoped) · **`fr-ContourSettings B-1 / L-B1 / C-2` — LEG CITED, HELD AT F.W3** ⊕ R-16 (SliderControl, FOLDS) ‡ · FR-AUL-46 (LC-miss-M5) · FR-FG-21 (=LC-missed-2) · FR-AFP-33 (D-17/L-20/L-22) · cites F3 | the client's numeric window `[1,500]` is **invented at the leaf**, restated ~6× per control, and matches NONE of the server's SIX divergent windows (256 / 4096×3 / 200-unbounded / 200-le); *Min Area %* drives a `[0,1]` fraction from a `0–20, step 0.5` axis — every detent ≥1.0 (**39 of 41**) demands `area ≥ A ∧ area ≤ 0.92·A`, **empty by construction**, with the default `0.001` off-grid rendering "0.0" under a "%" label (a **100× unit lie in the dangerous direction**); the client never enforces `slugs … max_length=50`; `maxBars` is unclamped into a canvas dimension (~3,855 bars crosses the 65,535 px UA ceiling) on the family's **only unbounded request model**; the flag list is unbounded on BOTH ends (server `$push` with no `$slice`) | **Generate client bounds from the operation models (the R6-8 dual); operation records carry field DOMAINS.** **fr-ContourSettings B-1**'s rider, verbatim at record case **and at record emphasis** (⟨cmd⟩ `grep -n -F 'the contract seam owes client-derivable bounds' fr-ContourSettings.md` → `:40`, this seat 2026-08-28): *"the typed range-checked descriptor table for all six fields rides an **F.W5–W8 rider** — the contract seam owes client-derivable bounds"*. ▲ The former rendering STRIPPED the record's bold from `**F.W5–W8 rider**` inside the quotation, so the sentence failed a `grep -F` of itself — the same D-6 class as an up-casing, and cured the same way: **emphasis inside a verbatim marker is part of the bytes.** **R-16 FOLDS — SliderControl books nothing new.** ▲ **`fr-ContourSettings B-1` IS A LEG HERE, NOT A BOOKING (repair round 4, ruling R4-6; R4-10's fabrication clause).** The canonical homes the row at **`F.W3`** (file-criterion §5.d) with legs **`F.W5-W8`** and **`SS-13`**, and its full banked head is `B-1 / L-B1 / C-2` — so **the ROW is F.W3's and only the contract obligation is F.W5's**, which is exactly what the record's own rider says (*"the typed range-checked descriptor table for all six fields rides an **F.W5–W8 rider** — the contract seam owes client-derivable bounds"*). This clause **states the seam obligation and books no row on the id**; the descriptor table lands at F.W3 with the F.W5 contract term as its input. Anti-rename: `L-B1` here is **`fr-ContourSettings`'s**, not `fr-FourierShapeExtractor L-B1` (G1c) nor `fr-FunctionInput L-B1` (F2's fold) — three records, one token, all three record-qualified in this file. Born-RED fixture named at the record: drive Min Area to 0.5/1.0 in e2e and assert non-422. ▲ **K-3 KILL LOCK: m-7's straddle-a-422 scenario is REFUTED FROM SOURCE (`AnimationData` is a server-computed embedded document; the client-reachable bound 4096 ⊇ [1,500]) — v2 must NOT cite it.** LC's six-count confirmed over C-6's five. ▲ **`fr-AdminUserList FR-AUL-46` — THE ONE HOME IS HERE (D7), and this end says so (R2-11).** Banked routing, ⟨cmd⟩ `grep -n 'F\.W5' fr-AdminUserList.md` (this seat): the `:89` row tail reads *"| **F.W5** — surface the cap in the shared contract; client-side chunk or block |"*; ⟨cmd⟩ `grep -cE 'F\.W5[-–]W8' fr-AdminUserList.md` → **1**, the `:28` boilerplate line only — **the row is NOT band-routed**, so no F.W5–W8 consumer inherits a booking from it. Surface the cap; the client chunks or blocks; the ApiProblem-fallback clause adopted. **F.W8 CITES this clause and books nothing** — its §3 J4 cell consumes the bound (*"F.W8 consumes the bound, does not author it"*) and carries no banked id of its own for it. ▲ **THE `doubleHomed = 0` CERTIFICATION IS RE-GROUNDED ON THE CANONICAL, NOT ON A LIVE SIBLING (repair round 4, rulings R4-3 + R4-8.3 + R4-9.3; PASS-4 D-20).** The former sentence certified `doubleHomed = 0` **in the present tense with no witness at this end**, resting on `F-W8.md` §3b's cross-check — a claim this file named but never ran, in the one assertion class where every other statement here carries its command. It may not simply be re-run either: **a banked count of a live sibling is the class R3-3.10 abolishes**, and F-W8 is under repair this same round (its own masthead figures are struck at R4-3.2), so any digit pasted here would be stale before the purge seat closes. The lawful witness is the **stamped** operand: ⟨cmd⟩ `grep -n '\`FR-AUL-46\`' "$X"/conformance/CENSUS-CANONICAL.md` (this seat, 2026-08-29; `$X=docs/tranches/X/fourier` per §0) → **one** §1 row, at `:343`, whose four cells are — banked id `FR-AUL-46` · aliases *none* · routing `F.W5` · home **F.W5** (the row's cell separators are rendered as `·` here so this table survives; no word is changed). **A sole `F.W5` token, no alias, no leg, one home.** The id therefore cannot be double-homed by construction, and **this clause is that home**; F.W8's J4 consumes the bound as a citation. Stated as a property of the census of record rather than as a count of a file that is still moving. FR-AFP-33: server bound → F.W5, client collapse → F.W1/W3; ▲ counterweight verbatim — `if not flagged` short-circuits production's only case, so **the cost is LATENT while D3 stands** |
| **D8** A blanket write is not a transition | FR-AFP-10 ⊕ FR-AFP-70 (C:D-4 · β-miss-7) · FR-GV-9 (=C·M-8 second clause) | *"Mark acceptable (save)"* never dequeues — `set_tier` writes `{tier, updated_at}` only and the flagged listing has **no tier predicate**, while the in-file comment claims the opposite; `handleSetTier` is hardwired to `'saved'` with no inverse, so a repeat click is a real PUT + audit row + success toast + full pagination reset for a no-op; Batch Unfeature `$set`s `tier:"normal"` unconditionally via `update_many`, **silently erasing a SAVED tier** | ONE semantics decision covers the tier↔flag pair; the tier verb gains its inverse; **the false in-file comment dies with the cure**; interim: reflect-and-disable when already saved. **FR-GV-9's first clause was KILLED at R-3/K3 — carry the kill.** ▲ **β's own KILL of its 428-escalation hypothesis (If-Match `*` accepted) is preserved — do not revive it.** Rides banked FR-GFC-20's stale-slug row |
| **D10** The list contract — filters and windows | FR-GFC-4 (=C-2/L-3; **`fr-GalleryInfiniteGrid C-4 / D-13` and FR-GSB-1 fold here**) · **`fr-GalleryInfiniteGrid R-7` (BOOKED HERE — pass-4 D-3)** · FR-GV-13 (=MISS-LC-2) · FR-GFC-1 (=D-1/C-3 ⊕ MISS-1(DU)) — **RIDER ONLY at F.W5** | `entries` is featured ∩ the loaded 20-row window, not the featured SET: **no `tier` param at ANY layer** (api.ts · visualizations.py · gallery.py · cursors.py), `fetchNextPage` appends so the strip grows during vertical scroll, and a featured row outside the window renders **no Featured section at all**; `visibilityFilter` is a **PRODUCERLESS store ref** (3 repo hits, all inside `stores/gallery.ts`, zero writers) so `ownerParam()` returns `undefined` unconditionally and the server's whole `owner == "me"` branch is DEAD from this client; `searchQuery`/`tierFilter`/`basisFilter` have **zero query-builder or `.filter()` readers repo-wide**, so every anonymous visitor who types and pauses tears the strip down to refetch a provably identical page | **An operation-surface hole, not a component patch** (both readers' routing adopted). A `tier` param or a dedicated BOUNDED featured fetch. ▲ **WAVE-LOCK (fr-GallerySearchBar C-2) — the record's own bytes, not this file's restatement of them.** ⟨cmd⟩ `grep -n -F 'carried as a **WAVE-LOCK** on any' fr-GallerySearchBar.md` (this seat, read-only 2026-08-28) → `:24`, reading: ***"carried as a **WAVE-LOCK** on any F.W5-W8 wiring of `basisFilter` — wire it without the banked normaliser and the Fourier pill returns zero rows against `active_bases ∈ {fourier-epicycles, fourier-series}`."*** The former rendering inverted the record's polarity into an imperative (*"any wiring … MUST use the banked normaliser"*) under a *verbatim* label — struck; the normaliser's banked id is **fr-BasisSelector M-10** (record-qualified per §2's ninth-collision block, cited not re-booked). ▲ *Identity minute (round 4)*: the quoted `C-2` is **an ALIAS carried beside `fr-GallerySearchBar FR-GSB-6`** in the census of record (aliases `C-2` · `L-9` · `C-14`; home `F.W3`, file-criterion §5.e), so **the WAVE-LOCK is a quotation, never a booking** — and that record's one canonical `F.W5-W8` row is `FR-GSB-28`, booked at **B1**. Stated so no reader mistakes a lock's source for a claimed row. ▲ **FR-GFC-1's share at F.W5 is the RIDER ONLY** — whether search/tier/basis become real params; **the body cure is F.W4's** (patch in place off `api.setVisualizationTier`'s discarded returned entity, per the store's own `recordView`/`restore` idiom) and **the D-1 skeleton cure is KILLED (K5)**. **F.W1 must NOT be credited for the pagination-drain cure (FR-GIG-5).** FR-GV-13 is the worst sibling — the other three filters at least have producers. **Two BLOCKER filings on FR-GFC-1 (D and C axes) preserved**. ▲ **`fr-GalleryInfiniteGrid R-7` IS BOOKED HERE, AND THE ESCAPE IT MADE IS NAMED (repair round 4; PASS-4 D-3 — the round's ONE escaped row).** Canonical home **`F.W5-W8`** (leg `SS-1`). It is a **bare-first-cell table row** — the shape §2's detector names in terms — whose own cells carry the routing; banked bytes, quoted by `grep -F` so each span survives a check of itself and **each is marked for what it is** (R4-7.1: verbatim means bytes, and a mid-line span says so). ⟨cmd⟩ `grep -c -F 'the param hole is FR-GFC-4 (F.W5-W8).' fr-GalleryInfiniteGrid.md` (this seat, 2026-08-29, `$R` base) → **1** — a **mid-line span, not the terminal cell**: the row's line ends *"…one tier click costs ⌈N/20⌉ round trips, not one. \|"* (the trailing `\|` is this file's table-pipe convention, as everywhere else), disclosed here because an undisclosed narrowing is the pass-4 D-9 class. ⟨cmd⟩ `grep -c -F 'FOLD sustained — identity guard beats re-grade.' fr-GalleryInfiniteGrid.md` → **1**, the row's own ruling cell. ▲ *Emphasis note*: the record writes `(F.W5-W8)` **unbolded**; a first draft of this cell added the record's usual `**…**` and the span then failed a `grep -F` of itself — emphasis inside a quotation is part of the bytes, the rule this file applies at A5, B1, D7 and C2, applied to its own newest paste. **Its substance was always here** — the dead-controls arm inside FR-GFC-1 and the param hole at FR-GFC-4, both booked in this clause — but **its identity was not**, and a row whose substance lands while its id does not is invisible to the only difference G19 states. It survived the *unqualified* difference (the token `R-7` is present three times in this file, as `lane-crud §R-7` at C3/E18 and `GCM-3 R-7` at E6) and failed the record-qualified one: **the `fr-PaperSearchDropdown` class, cured for one record in round 3 and not applied to this one.** Booked here by its full `(record, id)` pair; the genuinely new rider it carries — under the drain, one tier click costs `⌈N/20⌉` round trips, not one — rides FR-GFC-1's banked row and is **not credited to F.W5**. **`fr-GalleryInfiniteGrid C-4 / D-13`** is a different disposition and is spelled as one: its canonical home is **`F.W3`** (file-criterion §5.e) with an `F.W5-W8` **leg** — so this clause **cites the leg and books nothing on it**, and the twin partition is not contended (§2's twin block) |
| **D11** What a count counts | GAB-15 (L·D-10) · GAB-16 · GAB-17 (D-M7/C-11/L·D-13) · FR-AUL-20 (LC-miss-M2) · FR-AUL-25 (DU-miss) | the storage tile carries **TWO UNCORRECTED OPPOSITE BIASES** under one unqualified label (no `$match` ⇒ soft-deleted assets counted; `"bytes": len(content)` records the primary blob only while thumbnails are separate `{slug}.thumb` files sized nowhere) — unbounded, non-cancelling; `total_entries` is **DERIVED** (`sum(tier_counts.values())` over an in-enum fold), so any absent or off-enum `tier` silently **VANISHES**; `entry_count`'s `$lookup` labelled *"Live-entry count"* carries no `deleted_at` filter and `prune_empty_users` uses the SAME unfiltered lookup — **the janitor MANUFACTURES exactly the population the prune then refuses**; the prune confirmation announces an unbounded PERMANENT deletion **without a count**, though `empty_slugs` is computed BEFORE deleting | v2 states **live/tombstoned** and **primary/derived**; the join filters `deleted_at: None` or tombstone-inclusive counting is stated explicitly; honest total = `count_documents(not_deleted_filter())` + an *other* bucket; a count/dry-run endpoint or a two-step confirm. ▲ **GAB-17 is SEQUENCED: render `stats.normal` only AFTER GAB-16 lands, or drop the field with this contract pass — its "verification" limb is a TAUTOLOGY until then.** ▲ **GAB-16 REFUTES D-M7's headline rationale (R-6 ruling) — carry the refutation with the row.** ▲ **PRUNE SEQUENCE: FR-AUL-20 → FR-AUL-21 → FR-AUL-25; the count is meaningless until both land.** **Dissent: axes split MAJOR/MINOR/INFO on GAB-17; MINOR ruled.** Magnitudes → SS-13 |
| **D12** Closed domains at the boundary | FR-AUL-21 ⊕ FR-AUL-31 · **`fr-ContourSettings M-10 / L-M6 / C-19 / D-m9` — LEG CITED, HELD AT F.W3** · **fr-BasisSelector M-9** ⊙ (=L-M4/C-20) · **`fr-NotationPills FR-NP-30`** (cited A5) | a **THIRD** `status` exists — `migrate_visualization.py` upserts `"orphan-migrated"`, `$ifNull` passes it through, `AdminUserInfo.status: str` accepts it, the template branches only on `=== 'suspended'` ⇒ an orphan-migrated user renders as a **HEALTHY ACCOUNT WITH A SUSPEND AFFORDANCE** on the panel whose purpose is separating real accounts from churn; two hand-maintained `Record<string,string>`s stand in for the backend's 7-member `ContourStrategy` enum — `ADAPTIVE_THRESHOLD` is real, implemented and **unrepresentable** in the picker, and once changed away cannot be re-selected; the deliberately-minted "off" state (`[]`, in-file comment *"canvas handles it gracefully"*) is **unrepresentable end-to-end** and silently laundered on every path — **the third click is honoured on screen and destroyed on save with zero diagnostic** | ONE closed set across writers, model and wire (`Literal` + the migration writer folded in); the sort set closed on both ends (reka `AcceptableValue` widening and `sort_map.get(sort, default)` both fall back silently). **The shared-enum contract is F.W5's; the typed vocabulary at the leaf is F.W3/W4's.** ▲ **F.W4 rider: unknown statuses render VISIBLY, never as healthy.** ⊙ **fr-BasisSelector M-9 verbatim** (⟨cmd⟩ `grep -n -F 'admit `[]` in the contract, or stop minting it' fr-BasisSelector.md`, this seat read-only 2026-08-28 → one hit): ***"admit `[]` in the contract, or stop minting it — the silent rewrite must not survive either way."*** Same mechanism family as GAB-16: **closed domain at the boundary, open domain in the collection**. ▲ M-9's corpus line-drift corrected by the seat — **re-resolve anchors after F.W0 (D-19)**. ▲ **RECIPROCAL MINUTE — A RULING WAS DEPARTED FROM HERE, AND THIS END NOW SAYS SO (repair round 3, PASS-3 D-19).** `RULINGS-2` **R2-7.1** directed F-W8's §6a to cite `fr-ContourSettings M-10`'s F-W5 clause home as *"the E13/A5 family"*. **That candidate home does not hold at the bytes**: the id is the closed-domain/shared-enum row and is booked **here, at D12** (§2's ninth-collision block records the same finding). The divergence is correct on the merits and was already minuted **at the other end only** — `F-W8.md` §0's opening item, which reads *"R2-7.1's candidate home for `fr-ContourSettings M-10` … does not hold at the bytes … booked at **clause D12**"*, cited by §-anchor and item, never by line. **A reader of the keystone alone could not see that a ruling had been departed from**; that asymmetry is the defect, and this sentence is its cure. The ruling's LAW — exclusion-with-reason at the F-W5 clause home, homonym named — is applied unchanged, and `fr-BasisSelector M-10` (the `basisFilter` normaliser at D10's WAVE-LOCK) stays a distinct identity. ▲ **AND THE HOME IS RE-STATED AGAINST THE CANONICAL: `fr-ContourSettings M-10` IS AN `F.W3` ROW WITH AN `F.W5-W8` LEG (repair round 4, ruling R4-6).** R2-7.1's candidate home (*"the E13/A5 family"*) did not hold; **D12 is not a rival home either.** The census of record homes the row at `F.W3` (file-criterion §5.d), leg `F.W5-W8` — so this clause **cites the leg, states the closed-set contract term in F.W5's voice, and books no row on the id**, while the shared-enum obligation it names remains genuinely this wave's. The correction is minuted at both ends: this cell, and `F-W8.md` §0's opening item |
| **D13** Required-vs-optional posture stated once | FR-AUL-14 (C-B4, rescoped ruling 2) | **ONE** user document missing `created_at`/`last_seen_at` **500s the ENTIRE admin list** (required model fields, bare inclusion projection, handler-body construction, global exception handler) — and the janitor's reap query **SKIPS fieldless docs**, so they never age out | `$ifNull` the projection or default the model fields; the posture stated once. The client's null guard rides FR-AUL-17's timeAgo repair (F.W3). Absent defence proven on BOTH sides; **production INCIDENCE is UNPROVEN — that question alone rides live → SS-13.** v2 states the posture; the probe answers whether it is violated |
| **D14** Adoption is measurable — the **acceptance surface** | VV-R2-B (MAJOR · NEW, the FRAME row; census verified 10/10) · owns the joint identity of BLK-1 / BLK-2 / VV-R2-A / VV-R2-E | the workspace store's entire converged-CRUD arm is dead surface: **TEN of 24** exported members have zero consumers outside the store (`visualizationSlug`, `visualizationETag`, `revision`, `loadVisualization`, `loadSnapshot`, `setVisibility`, `deleteVisualization`, `invalidateInFlightComputation`, `defaultContourSettings`, `defaultAnimationSettings`), and `gallery.ts:168` bypasses the store's own `deleteVisualization` to call the API raw, **stranding the slug/ETag state maintained for exactly that call** | Reading the cluster as ONE gap changes the cure: **a single wiring unit at this component** (route → `loadVisualization`; publish → `setVisibility`; delete → the store action), not scattered repairs — it does **not** re-book its members. F.W4 executes the wiring; **F.W5 owns the CRUD-CONTRACT §1 adoption as the union tranche's own ACCEPTANCE SURFACE.** ▲ **This is the row that tells F.W5 whether its contract was adopted at all. No wave may claim adoption without it** |
| **D15** Query semantics under the shipped indexes | AA-6 (L-4/C·D-2) · **AA-23** · AA-32 · FR-AUL-59 (LC-miss-M9) | `admin.py:631-632` is a **BARE EQUALITY** on `action` beside an honest regex on target; the placeholder advertises prefixes and its own example `set_tier` matches **zero rows forever** (writers emit `set_tier:{tier}`), and composed with AA-1 *"no matches"* and *"unreachable"* are pixel-identical; `services/database.py:145-146` creates exactly TWO indexes on `admin_audit`; the `target` filter drives an **unindexed** case-insensitive regex plus a second full scan via `count_documents`; the `entries` sort orders `$match → $lookup → $addFields → $sort → $skip → $limit`, so the cross-collection join runs over the **ENTIRE matched user set** per debounced request | ▲ **The regex-action cure BOTH axes prescribed is REJECTED (AA-23).** Sound cures in this stated ORDER only: **(a)** honest placeholder (*"action (exact…)"*), **(b)** `$in` over a generated taxonomy (also cures AA-5/AA-24), **(c)** anchored case-**sensitive** `^prefix`. ▲ **AA-23 CURE LOCK — the quote at record case, the binding in this spec's own voice, no fusion of the two** (⟨cmd⟩ `grep -n -F 'indexed equality into a' fr-AdminAuditLog.md` → `:60`, this seat). Banked bytes: *"A case-insensitive `$regex` … cannot use that index; shipping it converts an indexed equality into a collection scan on the one monotonically growing collection."* and, its own next sentence, *"Binds AA-6's cure set."* **F.W5's addition, stated as F.W5's:** the lock therefore PRECEDES any query-semantics clause v2 writes. ▲ **AA-32 positive, binding: `re.escape` IS applied — no injection; v2 must not manufacture one** (bounded by the 90-day janitor prune). AA-21's placeholder co-sign rides AA-6 (iOS sentence-autocapitalisation defeats an exact match; the server equality carries no `$options:"i"`). FR-AUL-59: pipeline reorder — join AFTER `$limit`, as the sibling branch already does; **composes with B1's abort-key fix; neither alone closes the window**. ▲ **`fr-AdminAuditLog AA-23`'s HOST ROW IS BOOKED HERE, EXPLICITLY (repair round 4, ruling R4-6.4).** The canonical homes it at **`F.W5`** — ⟨cmd⟩ `grep -nw 'AA-23' "$R"/fr-AdminAuditLog.md` (this seat, 2026-08-29) → the banked head at `:60`, *"**AA-23 · worker-2 missed (read by neither axis) — MAJOR, repair-shaping.**"*, routing *"**→ F.W5** (the 45/30/13 join must carry this constraint)"* — and R4-6's law is that **a leg is never a new identity: it books as ONE explicit row at the wave that HOLDS the host id.** The host is held HERE, at D15, on the `45 = 30+13+1+1` join the row's own routing names; **F.W6 keeps only the killed-cure citation, marked as a citation.** The two rows that cite AA-23 in this clause carry no second booking: `AA-6`'s cure set is *bound* by it (the record says so at `:43`) and `AA-32` cites it for the two-index fact at `:72` (*"only two indexes exist, AA-23"*). One home, two citations |
| **D16** The shared action taxonomy | AA-5 (L-3) + AA-24 | `actionTone` is blind to the ENTIRE `janitor:` namespace because the collection's **SECOND writer** (`janitor.py:59-99`) inserts directly into `db.admin_audit` with no `log_audit` call, emitting nine `janitor:*` actions — including the audit log pruning its own history — that all land on the benign default; and inside the map `batch_users:delete`, the vocabulary's most destructive operation, falls past `startsWith("delete")` into the same violet arm as `unsuspend` | A shared ACTION TAXONOMY at the seam (cross-tier F.W5; also cures AA-24 and feeds AA-6's `$in`). Display arm → F.W4. ▲ **S-8 METHOD LAW (kills C·S-2/K-3 via K-6): an absence-proof must ENUMERATE the surface, not query one name for it** — `grep "log_audit("` was structurally blind to the inlined second writer. **v2's absence-proofs inherit this bar** |
| **D17** Date serialization — **serializer arm only** | FR-AFP-32 (D-16/L-13/C:D-15; +FR-AFP-40 fold, +FR-AFP-69 = β-miss-5; **`fr-GalleryCard L·M-4 / D-13 / C-8(a) + L·D-2 / C-12`** folds here — the FULL banked spelling) | ONE router serializes the same concept TWO WAYS: flagged rows via `_json`/`default=str` (space-separated, **non-ECMA**) while audit returns `AuditListResponse` (ISO with `T`) — and the NaN guard sits in the consumer that gets the **conformant** payload while this file, on the non-conformant one, has none | ▲ **F.W5 owns ONLY the serializer arm. The five-copy `timeAgo` family's shared formatter is F.W3's and already banked as FR-AUL-17 — do NOT re-book it here** (GalleryCard states the fold explicitly). ▲ **THE SERIALIZER LEG'S BANKED ID, ADDED WHERE ONLY THE MECHANISM SAT (R2-7.1 reciprocity, confirmed by command at this end).** ⟨cmd⟩ `grep -n 'D-13\|C-8(a)\|L·D-2\|C-12' fr-GalleryCard.md` → `:64` (this seat, 2026-08-28), whose bytes read: *"**L·M-4 / D-13 / C-8(a) + L·D-2 / C-12 — MINOR · FOLDS BY REFERENCE, nothing new to book.** The `timeAgo` family (5 copies, 2 dialects, render-frozen, no NaN guard, non-ISO wire) is banked at **fr-AdminUserList FR-AUL-17** + **fr-AdminFlaggedPanel FR-AFP-32** (both name GalleryCard:53-61; → F.W3; serializer → F.W5–W8; JSC → SS-13)"*. So the record itself routes **serializer → F.W5–W8** and **JSC → SS-13** — this clause is that leg's home, cited by the full record-qualified id, and F-W8's §6a names the same id as an exclusion-with-reason pointing here. JSC acceptance of the non-ECMA form → SS-13 |

### §E — Provenance, lineage and persistence (the union's core)

| clause | rows folded | witness | lock |
|---|---|---|---|
| **E1** Compound per-entity version identity | **V-β** (lane-crud §V-β, +§7) — value-side | `PaletteVersion._id` is GLOBAL, so chains **COLLIDE ACROSS PALETTES**: `hash.ts` folds only `{name, colors}` — never `paletteSlug` — and `findByHash` filters `{_id: hash}` with no slug scope while `insertIfAbsent` returns early on a hit; two palettes with identical `(name, colors)` share ONE version row whose `paletteSlug` names only the first, and **the second's history is EMPTY** *(re-verified value-side 2026-08-28: `$V/api/src/modules/palette/hash.ts` `computeContentHash(name, colors)` canonicalizes `{name, colors}` only; `repository/paletteVersion.ts` `findByHash(hash)`; `insertIfAbsent` idempotent on duplicate hash — its own test asserts "no second write")* | v2 mandates the **compound per-entity version `_id`**; fourier already solved it with `f"{viz_slug}:{set_hash}"` and documents the §2.3-vs-§11 tension. ▲ **Execution belongs to the value.js API row, NEVER re-booked as a fourier defect.** Value-side rows are citable now; fourier-side anchors are MEASURE-AT-OPEN (D-19) |
| **E2** Chain depth: deepen or retire | **F-α** (lane-crud §F-α + intake X-8) — fourier-side | `_write_root_version` hard-codes `parent_hash=None, root_hash=set_hash_value, depth=0` and is the ONLY writer, so every version has `depth == 0`; `GET /{slug}/versions` sorts by `depth` over an always-singleton set; all real lineage rides cross-viz `fork_of` | v2 states whether within-entity chains DEEPEN, **or** the depth/parent/root quadruple is explicitly RETIRED on the fourier side. ▲ **F.W5 owns the CLAUSE; F.W6 owns the burn-down — do NOT double-book.** X-8 corroborates three ways (Codex controls, census C-6, fourier's own M.W10 booking) and **adds no new work** |
| **E3** ⊙ Diff-clause participation | **TA-4** (megatranche COHESION §2 / lane-crud §7 / SS-4 prerequisite) | `lib/crud/atomdiff.ts` + `atomDiff` on `PaletteVersion` were excised at T.W1/TA-4; only hashing survives, so `J-diff-shape.md §6`'s probes **CANNOT RUN** value-side — while v1 §2.5 mandates the exact file and §6's close-gate assumes BOTH probes exist. **A co-signature over un-runnable probes is VOID.** *(Re-verified 2026-08-28: `ls $V/api/src/lib` → No such file or directory; `grep -rn "atomdiff\|atomDiff" $V/api/src $V/src` → ONE comment hit, `__tests__/palettes-forks.test.ts:9`, which names TA-4 as the excision.)* | ⊙ **The owner rules RESTORE (TA-4 reversal), or v2 RE-SCOPES value.js out of the diff clause EXPLICITLY with §6 re-authored to a one-sided verdict. The wave may not assume either.** ▲ **THE CHARTER LAW, QUOTED AT ITS OWN BYTES — ONCE, HERE (R2-1-LAW; the former "(charter law, verbatim)" label carried a restatement, not the charter's words, and named no file at all).** Home: `docs/tranches/X/COHESION.md` **§2 The cross-sub-tranche dependency graph — the `SS-4 contract co-signature → F.W5 ∥ X·V API row` bullet** (cited by §-heading and bullet label, never by line — R2-2.1). ⟨cmd⟩ `grep -n -F 'is a named prerequisite or the contract is' docs/tranches/X/COHESION.md` (this seat, read-only 2026-08-28) → one hit, whose bullet reads — **the source wraps mid-sentence, disclosed rather than silently re-flowed**: *"**SS-4 contract co-signature → F.W5 ∥ X·V API row**: neither side's edits gate the other's / waves; the value-side atomdiff restoration (TA-4) is a named prerequisite or the contract is / re-scoped explicitly."* (the `/` marks the charter's own line breaks; no word is changed). **F.W5's obligation, in F.W5's voice**: SS-4 names the TA-4 value-side restoration as the prerequisite, or the contract is re-scoped explicitly — the wave may not assume either |
| **E4** Born visibility of the derived variant ‡ + the fork/remix vocabulary | **R-5 (lane-crud) ⊕ GCM-1 ⊕ `fr-GalleryCard D-7 / C·I-2` ⊕ X-2**; BLK-1 FOLDS (VisualizationView) | a fourier remix child is born **`draft`**; a value.js fork child is hard-coded **`visibility: "public"`** — OPPOSITE privacy defaults on the same verb, both shipping *(value-side re-verified 2026-08-28: `$V/api/src/modules/palette/service/forks.ts:76` — `visibility: "public"`)*; the modal's only CTA routes to the PRE-SAVE workspace, emits `image_slug`, loads the CALLER's IndexedDB draft, hard-resets to defaults when absent, and **writes a draft row keyed on another user's `image_slug` into the viewer's DB** — while `loadVisualization` has zero call sites and **any subsequent save POSTs a NEW entity with no `fork_of` despite the fork substrate existing**; the entity's HUMAN identity is write-only (`entry.title` readers = 0; label, `alt`, `aria-label` all spend the MACHINE FK) | ONE born-visibility rule, co-signed; **each side's test asserts the child's visibility on create**. ▲ **F.W5's share of GCM-1 is the CONTRACT FACT — a save that should have been a fork carries no lineage — NOT the routing repair (F.W4's; BLK-1 folds; D14 owns the joint identity).** `fork_count` unrendered is load-bearing for the union — it is the counter the fork verb maintains — composing with lane-crud R-1 (`fork_count`↔`forkCount`) and X-2 (**a stored counter cannot stay truthful under viewer filtering**). Vocabulary settled here: **remix vs fork**, **snapshot vs version**. ⊙ owner ruling owed on remix-vs-fork and born-visibility — flagged INLINE per SS-4. ▲ **No clause may contradict ruling D9** — `docs/tranches/V/DECISIONS.md` **§2, row `D9`** (rooted at §2's compatibility note; visibility `private|public`, lifecycle `active|trashed`, moderation `clear|withdrawn`, *"the unused `unlisted` state dies"*) — and value.js today persists a 3-state visibility (`public`/`unlisted`/`private`, `model.ts:61`, re-measured read-only 2026-08-28), which the value-side obligation list must reconcile rather than the contract silently overwrite |
| **E5** Create idempotency and dedupe — ONE clause, TWO entry points ‡ | **fr-GalleryDraftsSection B-2 ⊕ fr-CanvasControlsDock D-4**; FR-GV-1 FOLDS at banked severity; EditorControlsDock C-7 rider; intake X-3 | the publish path is a composed falsehood: `savedSnapshots` has ONE writer — the unconditional literal `[]` — so `!d.savedSnapshots?.length` is invariantly true and **the row never leaves the tab**; `publishDraft` never calls `saveDraft`/`refreshDrafts`; `publishedHashes` holds freshly minted visualization slugs that can never equal a draft's `imageSlug`; the button re-enables and **the second press mints a SECOND public visualization** because `createVisualization` sends no `Idempotency-Key` (the middleware is explicit: *no header → handler()*) and `content_hash` carries a PLAIN index (contrast flags' deliberate `unique=True`). At the dock the same shape is re-entrant on all three hops: **N clicks ⇒ N public gallery rows** | Server-side dedupe/idempotency for the create path. Split adopted: props/reconciliation → F.W3/W4; **server dedupe → F.W5-W8**. ▲ **K12 PROBE-SUPPRESSION LOCK: both axes' UNPROVEN-NEEDS-LIVE hedges are DEAD — fr-GalleryDraftsSection B-2 closes it STATICALLY (no-header idempotency passthrough + the plain index); SS-13 spends NO probe here.** ▲ **fr-CanvasControlsDock `D-4 / L-1 / C-3`'s F.W5 share, verbatim at record case** — ▲ *the banked head is carried WHOLE (repair round 3, PASS-3 D-18): ⟨cmd⟩ `sed -n '45p' fr-CanvasControlsDock.md` heads the row **`D-4 / L-1 / C-3`**, and this clause formerly carried `D-4` alone; a dropped alias limb is invisible to G19's difference in both directions* — (⟨cmd⟩ `grep -n -F 'two-sided delta' fr-CanvasControlsDock.md` → `:45`, this seat): *"**F.W5–W8 rider**: the server-side `(image_slug, contour_hash)` dedupe arm rides the R6-8 seam split (C-28 fold) — the client cure and the operation cure register a two-sided delta until that join is split."* **F.W5's own binding**: the server arm SEQUENCES AFTER the join split. The **C-28 fold itself is homed at clause B1** (R2-3d.5) — cited here, booked there; one home, two citations. Riders: the success toast prints the USER slug (m-13); `publishedHashes` is a misnomer (it holds slugs) — fix with the contract |
| **E6** Unsafe GET / counter provenance | **FR-GV-12 (=MISS-LC-1) ⊕ VV-R2-A ⊕ FR-GV-24** | `GET /api/visualizations/{slug}` is an **UNSAFE READ THAT CANNOT OBSERVE ITS OWN SIDE EFFECT** — `find_one` first, `$inc {"views": 1}` after, `_public_doc` serialising the PRE-increment document; the same verb is the ETag-capture path, so **every publish silently inflates its own view counter** and an entity is born at `views: 1` before any third party sees it; no `viewed_ips` dedup while `liked_ips` exists; `viewedHashes` is component-local and resets on mount | The counter moves to an EXPLICIT VERB, or v2 states the mutating-GET policy against **RFC 9110 §9.2.1**. ▲ **Repair-test lock (GCM-3 R-7 precision): `viewedHashes` DOES work within a session — the defect is SCOPE, not absence — so repair tests must NOT assert a re-open increment.** ▲ **The store's own `recordView` comment documents GET-as-increment as the DELIBERATE mechanism, so the publish-path GET is an ACCIDENTAL self-count: v2 must not codify the accident.** F.W4 arm: publish adopts `store.setVisibility("public")` (implemented, zero callers), dropping one round trip and the phantom view. Rides D14's joint identity. Observable magnitude → SS-13 |
| **E7** PATCH atom coverage + `set_hash` recompute | **F-β (lane-crud) ⊕ SS-C-1 write leg** | `VisualizationUpdate` (api) and `VisualizationPatch` (client) are the SAME five fields with `extra="forbid"`, so the persisted `speed` — atom 4-of-5 of the version-identity set — is settable **only at create/fork**; and the same model lets `palette_slug` (atom 5) be `$set` with **NO `set_hash` recompute and NO version write**, going stale against `enumerate_atoms(doc)` | **The SAME model is both TOO NARROW (speed) and TOO PERMISSIVE (palette_slug)** — atom coverage + recompute stated once. Ruled SPLIT (R2-LC wins): **SS-C-1's READ leg FOLDS to GCM-1/E4 — do not re-book; the WRITE leg books new.** **Dissent: C axis filed BLOCKER; demoted at component altitude (no capability loss in the control) — preserved** |
| **E8** AnimationSettings — the three-way reconciliation | **BC-9/C-6/D-20 ⊕ SS-C-2**; AnimationControls' LC-miss folds | half the persisted contract is **WRITE-ONLY and THREE-WAY DIVERGENT INCLUDING UNITS**: server `fps 30 / duration 30.0 s / max_circles 80`, client defaults `60 / 5000 ms / 100`, renderer hardcoded `ref(80)` and `20000 ms` — only easing/speed/active_bases round-trip, so **a gallery replay cannot reproduce its own frame**; atom 4 is semantically empty because `fps`/`max_circles`/`duration` have zero cross-wire readers, while the `duration` that `speed` divides is declared 4× with 3 values and a **1000× unit fork** | The union owns the reconciliation **including the unit fork**, with server-side units stated. Deduped: SS-C-2 and BC-9 are ONE identity (SS-C-2's unconstrained-`speed` cell carried); ▲ **`duration = ref(20000)` zero-writers stays banked at `fr-BasisCanvas BC-20` — NOT re-booked**, and the other end now confirms it: **`F-W3.md` §X.1-v4's `18`-vs-`19` SLACK block** (the definitive partition, published FIRST this round per R3-1a) records `BC-20` as **NOT zero-homed**, names **F-W5 among its holders**, and settles the id **to F.W4** on the file criterion — so this citation is the correct posture and F.W4 books. §2's twin-surface block carries the full consumption; ▲ AnimationControls' independently re-derived *"write-only AnimationSettings"* is **CORROBORATION, not a new booking** |
| **E9** One shape, one name | **fr-BasisCanvas C-7** | two `AnimationData` types share a NAME and a FIELD with incompatible shapes: server `partial_sums: dict[str, Point2D]` whose docstring **names `BasisCanvas.vue`** and prescribes typed access over a shape the client cannot use, vs client `Record<string, Record<number,{x:number[];y:number[]}>>`; the component's own comment misdescribes even the client type; entity `animation_data` is written and read by nobody | One shape, one name — **the server docstring's prescription is either honoured or retracted** |
| **E10** Produced-and-unconsumed: ONE disposition | M-β4 · **fr-EquationView L·m-6** (=C·D-14) · **FR-EMT-20** (cited F1) | `EpicycleData.trace` — a 3000-sample polyline computed per response — has ZERO readers in the web tree and rides `structuredClone` into every IndexedDB draft; `reconstructed_points` (500+500 floats) is fetched, sessionStorage-persisted whole, and read by NOTHING; `SimplifyResponse.term_count` has zero consumers | **v2 states ONE disposition for produced-and-unconsumed fields, not three ad-hoc deletions.** The R6-8 seam's converse: server computes, nobody consumes. ▲ **FR-EMT-20's "registry-swept: 0 banked hits" cell is CORRECTED by fr-EquationView L·m-6 — one identity, two witnesses.** Distinct from B4's liveness predicate |
| **E11** Easing domain hoisted to the operation | **RESOLVER (D-10·L-4·C-D-4) ⊕ L/M-3 (EasingPicker)** — merged, both ids preserved | `easings.ts:105` is the tree's ONLY unguarded catalog index (three siblings guard), called from a template expression with zero error boundaries, over a corridor that is a bare `str` at both seam ends with a cast as the whole validation — **five** unguarded hops (localStorage draft spread over defaults is the fifth). Failure: a radio group with zero checked members over a silently-linear animation | F.W4 owns the one `?? linear` guard-parity line; **F.W5 hoists the union to the operation record as `Literal[…]` on `AnimationSettings.easing`.** The seed/normalization half FOLDS to banked AnimationControls L-11/M-10 — not re-booked. ▲ **Registry-integrity (G22): EasingPicker booked its easing-domain server half as "new" when RESOLVER already carried the cure — MERGED, both ids preserved.** L/M-3 **KILLS C/i-1**: the field IS persisted and remixed (`animation_settings` on all four models, seat-verified) |
| **E12** Debounced mirror vs synchronous save | MISSED-E (EasingPicker save-race) | the picker's write reaches persistence through a 500 ms `watchDebounced` with **no flush-on-save**, while `saveVisualization()` reads `toRaw(animationSettings.value)` SYNCHRONOUSLY — select-then-save inside the window persists the PREVIOUS curve | Persistence semantics: the flush seam is the banked L-12/C-25 `setEasing` action. ▲ **The narrow reachability is HONESTLY STATED and the honesty CARRIES: a debounced mirror is a contract hazard wherever a synchronous save reads the same state** |
| **E13** Cache identity ⊇ consumed fields ‡ | **fr-ContourSettings B-4 = C-1 ∘ C-25 / R6-8 ⊕ i-7 ⊕ m-18** | the ML Threshold slider is INERT after the first extraction: `extraction_cache_key` is a CLOSED 10-field `json.dumps` literal that **OMITS `ml_threshold`/`ml_detail_threshold`** — the two fields this control is the sole producer of — while the handler short-circuits on the cache hit BEFORE `compute_contours`, and those fields materially drive extraction on the SHIPPED default route (`auto`). The user drags 0.50→0.85, waits out the debounce, and receives the 0.50 contour with no signal | Verbatim clause (C-25), **at record case so a `grep -F` of the sentence succeeds** (⟨cmd⟩ `grep -n -F 'superset of the request fields' fr-ContourSettings.md` → `:43`, this seat 2026-08-28): ***an operation's cache identity must be a superset of the request fields the operation consumes*** — the R6-8 seam made product-visible. ▲ **The former up-cased "SUPERSET" rendering is STRUCK (D-6)**: emphasis-capitalisation inside a verbatim marker is a rendering, not the bytes, and a lock that cannot survive a `grep -F` of its own source is not carrying its source. **This sentence is inherited downstream by clause id `E13`** — any wave transcribing it takes the record's case, never this file's former one. **i-7 FOLDS (B-4's payload, not a second row)**; m-18's invented `ml_detail_threshold = mlThreshold * 0.6` coupling rides here. F.W3/W4 owns the regression fixture: assert **`contour_hash` INSTABILITY across an ML-threshold change** (also the SS-13 probe) |
| **E14** Contour provenance — an editor-saved contour is FIRST CLASS ‡ | **fr-ContourSettings M-13 ⊕ fr-BasisSelector M-14** *(NOT fr-CoefficientsSpectrum M-13 — the A5 homonym; U-12 qualification)* | the hand-edit pipeline **DESTROYS THE EDIT ON EXIT**: nothing recomputes from `store.contour`, so merely LEAVING the editor remounts ContourSettings, whose immediate watcher auto-runs `runCompute` → `extractContour`, and the server **cache-hits the ORIGINAL** extraction because editor-saved contours carry no `extraction_cache_key` (`store_contour_asset(..., source="editor")`); the second trigger is a Harmonics/Sample-Points nudge from the sibling panel doing the same thing | ▲ **THE BANKED ID, CONFIRMED BY COMMAND AT THIS END (R2-7 reciprocity — the primary-band MAJOR is HELD HERE, at E14).** ⟨cmd⟩ `grep -n 'M-13' fr-ContourSettings.md` (this seat, read-only 2026-08-28) → `:70`, whose row reads *"**M-13 = LC-missed-2, RATIFIED + SHARPENED by this seat — MAJOR.** … Sibling row: fr-BasisSelector M-14 (the settings-nudge trigger) — same provenance seam, two triggers, ONE cure. **→ F.W5–W8** (contour provenance/CRUD union: an editor-saved contour must be a first-class input to compute, and extraction must not silently overwrite `source="editor"` assets)"* — the `…` marks elision of the record's mechanism prose, never addition. **The record's own words are "two triggers, ONE cure"**; this file's former all-caps compression carried a *verbatim* label it could not survive a `grep -F` of (D-6 class, struck). **F.W8 cites this clause at its §6a residue as an exclusion-with-reason and books nothing** (R2-7.1); the `fr-CoefficientsSpectrum M-13` homonym is disarmed in the rows-folded cell and lives at A5. M-14's end-to-end arm is UNPROVEN-NEEDS-LIVE → SS-13; **the mechanism is source-certain** |
| **E15** ONE transport clause | **fr-BasisCanvas C-17 + fr-BasisCanvas C-18** (transport) · row 26 C:C-12 transport half (ImageUpload) | the overlay is fetched unconditionally then refused when `image_bounds` is null (a second correctly-sized fetch follows — the first is waste); `overlayUrl` is a raw string-concat built **OUTSIDE `coreFetch`**, yielding `/api/api/...` under the Dockerfile default while compose passes empty; `thumbnailUrl` is a template literal straight into `<img src>` — no session token, no abort registry, no 429 retry, no ApiProblem, no ETag | Every URL the app fetches goes through the client core, or the exception is stated. ▲ **The race discipline is a recorded superlative (S-6): do NOT cure the waste by removing the guard** |
| **E16** ⊙ Trie disposition — the requirement with no material on either side | **R-4** (lane-crud §R-4 + COHESION SS-4) | the R-4 probe returns **ZERO true hits on BOTH trees** (trie / prefix-tree / radix / patricia / structural-sharing / delta-compress). What exists on both sides is **WHOLE-SNAPSHOT DUPLICATION**, and the anti-tree KISS guardrail is banked at **lane-crud §R-4** — ⟨cmd⟩ `grep -n -F 'flat BAG' docs/tranches/V/megatranche/formation/fourier/lane-crud.md` (this seat, read-only 2026-08-28 — ▲ **the path is spelled LITERALLY from the repo root; the former bare `lane-crud.md` resolved from no declared cwd and could not execute as written: repair round 3, PASS-3 D-11, R-10.6**) → **one hit, `:240`, and that line ENDS mid-sentence at *"the diff is a"*.** ▲ **THE WRAP IS DISCLOSED, NOT SILENTLY RE-FLOWED (repair round 3, PASS-3 D-10).** Under R2-1-LAW.1 a quotation enters only as the pasted output of the stated command, and the former rendering printed a whole sentence that **exceeds** its own command's single-line return — the identical operation E3 performs *with* disclosure three clauses away, done here without it. The sentence spans **two lines**, and both are named: `:240` *"`atomdiff.py:12-14`: "the atoms are a flat BAG (not a tree / Merkle / document); the diff is a"* ⟶ continuing on `:241` *"whole-atom replace …; there is no three-way / DAG / merge.""* — the break falls after *"the diff is a"*, no word is changed, and the inner `…` is the source's own elision, carried, never widened (each half verified independently by `grep -qF`, this seat). ▲ **BILATERALITY CORRECTED AT THIS END, by this seat's own value-side measurement — the guardrail is the INCUMBENT, but it is NOT carried on both sides today**: ⟨cmd⟩ `grep -rniE "merkle\|flat bag\|not a tree\|structural.?sharing" $V/api/src $V/src` (the `\|` are markdown-escaped table pipes, as everywhere else in this file — the shell reads them as alternation) → **exactly ONE hit, and it asserts the opposite word** — `api/src/modules/palette/hash.ts:6` *"Identical content always produces the same hash (Merkle property)."*; ⟨cmd⟩ `ls $V/api/src/lib` → *No such file or directory*, so the value-side twin the guardrail's own docstring names is the file **TA-4 excised** (this file's G4 measures the same absence). lane-crud's *"both sides carry"* spelling (⟨cmd⟩ `grep -n 'atomdiff.py:12-14' docs/tranches/V/megatranche/formation/fourier/lane-crud.md` — literal path, D-11's cure applied here too — → **TWO hits, `:240` and `:399`**, the second reading *"and both sides carry an explicit anti-tree KISS guardrail (`atomdiff.py:12-14`)."*, verbatim on ONE line, `grep -qF` → hit, this seat) rests on a docstring citing a deleted file. **The correction narrows the premise; it does NOT void the guardrail** — one live guardrail plus a deleted twin is still an incumbent constraint, and E16's default is unchanged by it. | ⊙ **The honest default v2 MUST carry absent a ruling: no trie; whole-snapshot duplication is the recorded shipped behaviour.** A greenfield build against a documented standing constraint. ▲ **SS-4 FLAGS THIS RULING INLINE. Dissent recorded at `F-W10.md` §2.3, the `SS-4-PREREQ` row** — its banked bytes are quoted **ONCE** in this file, with their ⟨cmd⟩ provenance note, at **§4's `F.W5 → F.W7` edge row**; this cell quotes nothing and states the substance in F.W5's own voice: the trie/structural-sharing requirement collides with the standing anti-tree guardrail above, the guardrail is the incumbent, and the owner ruling precedes design |
| **E17** Image bounds on write ‡ | **fr-ContourEditorCanvas C-2** *(NOT fr-GallerySearchBar C-2 at D10, nor fr-FourierShapeExtractor C-2 at G2c; U-12 qualification)* | the POST **never derives bounds**: `store_contour_asset(xs, ys, req.image_slug, source="editor")` passes no `image_bounds`, the param defaults `None` and is written verbatim, and backfill is GET-only — so `imageOverlayRect` goes null and the `<image>` v-if drops; because the store is content-addressed, a no-op save re-hits the extraction doc WITH bounds, meaning **the overlay dies exactly when the points CHANGED** | **Derive bounds on POST or backfill on write.** ▲ **The tree's own workaround comment sits on the LOAD path — do NOT mistake it for a cure** |
| **E18** Attribution — the actor is a FIELD | AA-10 (D-M2/L-13) + lane-crud R-7 / **V-γ** | the log never answers WHO: `_JANITOR_ACTOR = "system:janitor"` is written into **`ip_hash`** and rendered `system:jan` by an unconditional `.slice(0,10)` — convicted by the producer's own comment (*"self-documenting in the viewer"*); and fourier's `owner_slug` is required non-null vs value.js `userSlug: string \| null`, whose `if (userSlug)` guard means **an unattributable edit writes NO version row (V-γ)** | **An audit row's actor is a FIELD, not a repurposed `ip_hash`**; the attribution asymmetry is stated once. Display cure (sentinel-aware branch + legend) → F.W4 |
| **E19** Vocabulary the contract inherits — **noun + positional arm only** | D-14 / MIN-4 / MIN-5 / MIN-10 / MIN-11 (VisualizationView cluster) | three names for one affordance; a RETIRED `snapshot` noun still on the wire; a dead `_imageSlug` positional kept *"so the pre-existing call site need not change"* with exactly ONE caller | **Noun + positional → F.W5; the rest → F.W4.** v2 fixes the nouns it inherits (remix vs fork; snapshot vs version — lane-crud §6) |
| **E20** The exposed-ref seam — the one non-wire contract row | ContourPreview row 13 (L:L-5) | the prop is sourced through the sibling's `defineExpose`d **MUTABLE** ref (the undo ring's subject) across a v-if boundary; the channel is read-WRITE (`proxyRefs` set-trap) and **the tree ALREADY writes through it one key over** — `magnetRadius` via the computed setter; `getPoints()` is exposed two lines earlier | Verbatim at record case, from the row's own ADJUDICATED cell — ⟨cmd⟩ `grep -n -F 'a TWO-member change' fr-ContourPreview.md` (this seat, read-only 2026-08-28) → `:46`, reading: ***"ADJUDICATED → **F.W5** (readonly/`getPoints` seam; a TWO-member change — `points` AND `magnetRadius`)"***. The former up-cased *"TWO-MEMBER"* and re-punctuated the pair into parentheses — struck (D-6 class); the substance — a two-member readonly/`getPoints` seam — is unchanged. ▲ **Row 11's one-character `?:` fix pairs at F.W4 "per the L-lane carry" — F.W4 must NOT land row 11 presuming row 13's seam shape.** Owned here because the record routed it here |

### §F — Equation contract and error envelope

| clause | rows folded | witness | lock |
|---|---|---|---|
| **F1** Partial projection ‡ | **fr-EquationView B-1 ⊕ FR-EMT-1**; rider R1-missed-4 | the Notation control is **INERT IN THE DEFAULT DISPLAY MODE**: `SimplifyResponse = {latex, energy_captured, term_count}` on BOTH sides with no sigma arm, while the server proves sigma IS notation-dependent (`render_latex_sigma(terms, req.notation)`); a notation flip routes only to `doSimplify`, which writes `displayLatex` alone, so a stale-but-truthy `displayLatexSigma` wins, `lastDisplayKey` advances and suppresses every retry, the energy badge DOES move so the control looks alive, and **the mismatch PERSISTS ACROSS RELOAD**; the leaf's contract has no `disabled`/`available`/`stale` input | **`latex_sigma` joins `SimplifyResponse` on BOTH models — a CONTRACT DECISION, not a patch — or explicit invalidation.** ▲ **CURE BOUND — a COMPOSITE, labelled as one: two sentences, two homes, two ⟨cmd⟩ notes, never fused under a single attribution (R2-1-LAW.4).** (i) The cure-bound sentence is the **B-1 row's own disposition cell** — ⟨cmd⟩ `grep -n -F 'walks into the under-determined' fr-EquationView.md` (this seat, read-only 2026-08-28) → `:47`: ***"**CURE BOUND by M-CK below**: the alternative cure ("route notation through doCompute") walks into the under-determined `computeKey` and re-ships B-1 one layer down."*** (the former up-cased *"RE-SHIPS"* is struck). (ii) The imperative is **M-CK's own row**, a different line — ⟨cmd⟩ `grep -n -F 'fix the key FIRST or the B-1 repair ships broken' fr-EquationView.md` → `:71`: ***"fix the key FIRST or the B-1 repair ships broken."*** The former rendering welded (ii) onto the end of (i) under one *verbatim* label; the weld is struck and the two quotes stand apart at their true coordinates. Filed BESIDE R6-8 as the second instance of the partial-projection law (with E10 the converse direction — **one envelope row, both directions**). F.W4 rider: per-option availability (FR-EMT-7's fold supplies `disabled` free). Rider: `useCoeffHover` reads its LABEL KIND from stale rendered markup while computing VALUES from fresh state — a second victim of the same staleness |
| **F2** One shared bound constant ‡ | **fr-EquationView B-2 ⊕ fr-FunctionInput L-B1/C-1 (FOLDS) ⊕ fr-FunctionInput C-8** *(NOT fr-FourierShapeExtractor L-B1 at G1c; U-12 qualification)* | `budget` walks past `le=50` from the shipped defaults in ONE Harmonics drag — the rescale recurrence yields `budget(v) = v − 10` on a monotone up-drag (61→51; a single track-click jump from defaults yields exactly 50) against `ge=2, le=50` on both pydantic bounds; through `doSimplify` the 422 lands in `/* silent */`, and **`budget` restores from sessionStorage, so the NEXT session's first compute 422s before the user touches anything**; Harmonics `:max="100"` separately forfeits half of the operation's `le=200` range | F.W5 owns **one shared bound constant across the seam** (the R6-8-correct form). ▲ **Clamp at the two request sites or the ref — NOT at the slider: the restore path never passes through it (M-BR rider).** ▲ **KILL LOCK (`fr-EquationView K-13` — record-qualified, per §2's kill-register block; PASS-3 D-6): fr-FunctionInput L-B1's "without ever touching the offending slider" headline rides the ratio-1 state proven UNREACHABLE from defaults** — the kill is banked in **fr-EquationView's** register (⟨cmd⟩ `sed -n '146p' fr-EquationView.md` heads *"\| K-13 \| L·B-2's path 2 …"*) and corroborated at **fr-FunctionInput** `:100` (*"the ratio-1 variant is banked-killed (K-13)"*), which cites the same register rather than minting a second one. **This is NOT the `K-13` at F7** (that one is `fr-ConvergencePlot`'s); C-1's sessionStorage-restore path is the TRUE one — v2 cites the true path only.** fr-FunctionInput L-B1 FOLDS and is not re-booked; fr-FunctionInput C-8 rides the same constant |
| **F3** No unbounded field beside bounded siblings | **fr-EquationView M-SB ⊕ fr-FunctionInput N-4 (AMENDMENT RELAY)**; cites D7 | `SimplifyRequest.coefficients` has NO `max_length` beside fully-bounded scalars (the client's 2N+1 ≤ 201 bound is a coincidence, not a contract term) — **and `ComputeEquationRequest.expression: str` is bare too** (no max_length, no pattern), above six fully-bounded scalars, flowing through a seven-keyword denylist into `parse_expr` + symbolic integration with no `maxlength` at its sole editor | One `max_length` per unbounded field — the R6-8 attributability asymmetry exactly. ▲ **AMENDMENT RELAY (verbatim discipline): fr-FunctionInput N-4 CORRECTS banked fr-EquationView M-SB, whose quoted span is the record's own (⟨cmd⟩ `grep -n -F 'the ONE unbounded field' fr-EquationView.md`, this seat 2026-08-28 → one hit): *"the ONE unbounded field"* — there are TWO, and the FREE-TEXT one is the harder half. The relay is recorded at the AMENDING record; the banked file is NEVER rewritten.** Damage bounded by the banked 30 s-semaphore / 429 row |
| **F4** Server-side domain validator | **fr-EquationView M-DV** (seat-booked rider on the folded L·M-1/C·C-32) | no `model_validator`/cross-field constraint on `ComputeEquationRequest`, so `domain_start == domain_end` ⇒ period 0 ⇒ the catch-all 500 that the error envelope renders as NOTHING; reversed domains succeed with a negative period | A pydantic `model_validator`. **The client half is banked (CoefficientsPanel M-L1) — book only the INCREMENT** |
| **F5** The error envelope is actually spoken ‡ | **fr-EquationView C·D-02 ⊕ `fr-ContourSettings M-15 / DU-missed-4 / D-m2 / D-m3` (LEG CITED, HELD AT F.W3)** — one server identity, two witnesses | `main.py` has ONE `@app.exception_handler(Exception)` and **no HTTPException handler**, so FastAPI emits `{"detail": …}` while `api-problem.ts` destructures `detail` away (and drops FastAPI's ARRAY-shaped `[{loc,msg,type}]`) and falls back to `response.statusText` — **under HTTP/2 `statusText` is `""`, and `"" ?? x` does NOT fall through: `error = ""` ⇒ falsy ⇒ no toast, no banner, SILENCE END-TO-END** — and the one hand-authored 422 (*"No contours extracted — try lowering min area…"*) is parked on an unread field and toasted as *"Unprocessable Entity"* | An HTTPException handler so **the RFC 7807 contract the client was BUILT FOR is actually spoken** (F.W5); the client prefers `detail ?? title ?? statusText ?? fallback` and stops discarding array `detail` (F.W3/W4). ▲ **Narrowing adopted and binding: the middleware's rate-limit 429 IS problem+json (`rate_limited()`) — v2 must NOT claim zero coverage.** Transport arm (is the browser hop HTTP/2?) → SS-13; **the static arm needs no probe**. ▲ **`fr-ContourSettings M-15` IS A LEG (repair round 4, ruling R4-6)**: canonical home **`F.W3`** (file-criterion §5.d), legs `F.W5-W8` and `SS-13`, full banked head `M-15 / DU-missed-4 / D-m2 / D-m3`. **`fr-EquationView C·D-02` is likewise an F.W4 row** cited for the same server identity. F5 therefore **books neither witness and owns the server-side envelope clause**: the HTTPException handler is the contract act, the two records are its evidence, and both cures land at their holders. *(Server identity, two witnesses, zero double-homes — the shape D14's acceptance surface exists to make checkable.)* |
| **F6** A response field states WHICH population it measures | **fr-FunctionInput L-M3 ⊕ fr-ConvergencePlot L-m13** *(NOT fr-FourierShapeExtractor L-M3 at G5c; U-12 qualification)* | the Auto tooltip prints a BUDGET-TRUNCATED energy fraction directly beneath its own ≥99.99%-at-N_eff assertion (`energy_fraction = kept_energy/total_energy` over `truncate_by_budget(terms, budget)`), and the shipped defaults (budget 10, effectiveN 20–40) make the mismatch the **common case**; separately `nHarmonics` is a surviving-harmonic cap in the client and an index bound on the wire — **two denominators under one name** | **v2 states the denominator WITH the field.** Provenance carried verbatim, at the record's bytes — ⟨cmd⟩ `grep -n -F 'best library-axis find in the corpus' fr-FunctionInput.md` (this seat, read-only 2026-08-28) → `:53`: ***"The best library-axis find in the corpus (both readers concur; I concur)."*** ▲ The former rendering re-voiced the adjudicator's first person into the third (*"the seat concurs"*) and down-cased the opening — a small edit that made a *verbatim* label unreproducible; **struck. The record's voice is the record's.** Same family as B2's amplitude mirror and E8's unit fork. F.W4 owns the copy and the naming |
| **F7** The missing original-expression field | L-M7 + C-8 (ConvergencePlot) | the *sum* and *original* tooltips are CHARACTER-IDENTICAL at the only callsite (the `?? "\\text{sum}"` fallback unreachable), the golden partial sum is labelled `f(x) =` ⟨the original⟩ — **asserting the identity the plot exists to DISPROVE** — and the string is raw SymPy through KaTeX while `latex`/`latex_sigma` arrive unconsumed on the same response | ▲ **`fr-ConvergencePlot K-13` BINDS THE CURE** — record-qualified per §2's kill-register block (PASS-3 D-6: a bare `K-13` carried two different records' identities inside this one spec, at F2 and here; ⟨cmd⟩ `sed -n '129p' fr-ConvergencePlot.md` → *"\| K-13 \| C-8's cure ("route `result.latex` to the original curve") \| RATIFIED: …"*, **this** record's kill, **not** fr-EquationView's) **: `latex` is the budget-TRUNCATED series render (seat-read `equations.py:91` + the `simplify_series` docstring), so routing it to the ORIGINAL curve re-commits the same inversion. The prescribed cure is DEAD; a NEW backend field (`sp.latex(expr)`) on `ComputeEquationResponse` is the only sound one** — plus C §6's field-level-gap counting rule. F.W4 owns label honesty now |
| **F8** **THE SEAM CHOICE** — portable LaTeX | FR-EQR-4 (reader-2 missed M-1); FR-EQR-32 (cited B1) | the component's sole affordance emits INVALID LaTeX in the app's DEFAULT mode: `eqMode` defaults `"sigma"`, `activeLatex` prefers the sigma form, and `copyLatex()` copies it raw — a string embedding `\htmlClass{eq-coeff …}` that only KaTeX-with-trust consumes; all four `\htmlClass` emission sites live in the `*_sigma` renderers while the expanded renderers are clean, so **the app copies PORTABLE LaTeX in the non-default mode and UNPORTABLE LaTeX in the default one** | ▲ **Verbatim at record case** — ⟨cmd⟩ `grep -n -F 'the forming spec chooses the seam' fr-EquationResult.md` (this seat, read-only 2026-08-28) → `:39`, whose routing cell reads: ***"**→ F.W4** (a `plainLatex()` strip beside the FR-EQR-17 singleton) **or F.W5** (the hooks as a separate wire field — filed beside R6-8; the forming spec chooses the seam)."*** The up-cased *"THE FORMING SPEC CHOOSES THE SEAM."* is STRUCK (D-6). **F.W5's own voice, quoting nothing: this wave MAKES the choice, it does not inherit it.** If v2 chooses the **WIRE FIELD** (hooks as a separate field), FR-EQR-32's sink-class clause becomes enforceable; if it chooses the **CLIENT STRIP**, **F.W5 RECORDS THE DECLINE** so F.W4 may proceed with `plainLatex()` beside the FR-EQR-17 singleton. Registry-swept: unbanked elsewhere |
| **F9** A parameter that does not govern | D-10 + D-L12 + C-22 (EquationPanel) | *Terms* names a quantity it STOPS GOVERNING — `budget` is never forwarded (re-verified at `latex_rendering.py:281`) | Composes with F2's shared constant and B1's under-determined key: **the SAME `budget` field is unbounded at the client, unforwarded at the server, and absent from the operation's identity.** Same family as G4c's sampling contract |

### §G — Canonical geometry: the build-time provenance contract

| clause | rows folded | witness | lock |
|---|---|---|---|
| **G1c** The moon's true source ‡ — **TRIPWIRE** | **fr-FourierShapeExtractor L-B1** | the generator has **DIVERGED FROM THE ARTIFACT IT GENERATES**: 3.7% of shipped `moon.json` points fall within 1.0 unit of any extracted point, p90 **37.6** against a 1.23-unit resample spacing, and the crescent-alone bbox strictly contains the shipped bbox by 12.97 x-max / 6.55 y-min — while the SUN is consistent. `9e5ba74` (asset-only, *"remove stars … cap at 50"*) is the divergence event the generator never received; the component still renders the six removed decorations; shipped params 50/10 vs the script's 100/12. **The site-wide dark-mode toggle renders geometry whose source no longer exists** | Provenance union: **decide the moon's TRUE SOURCE or RE-AUTHOR it.** ▲ **TRIPWIRE — a COMPOSITE from two rows of one record, quoted as two (R2-1-LAW.4).** (i) ⟨cmd⟩ `grep -n -F 'DO-NOT-REGENERATE on `master`' fr-FourierShapeExtractor.md` (this seat, read-only 2026-08-28) → `:51`, the L-B1 routing cell: ***"**DO-NOT-REGENERATE on `master`.**"*** (ii) ⟨cmd⟩ `grep -n -F 'revive at BLOCKER with the DO-NOT-REGENERATE rider as the tripwire' fr-FourierShapeExtractor.md` → the record's own severity ruling: ***"if any wave attempts regeneration before F.W5-W8 lands the pipeline, both rows revive at BLOCKER with the DO-NOT-REGENERATE rider as the tripwire."*** The former single quotation fused the two and substituted *"L-B1 and L-B2/C-2"* for the record's *"both rows"* — struck; the two ids are named here in **F.W5's own voice** (they are the two the ruling's *"both rows"* denotes, and they keep their banked spellings). The eager-payload arm is banked FR-AH-1 → F.W4 (not re-booked). *"What `moon.json` WAS generated from"* → SS-13 / **undetermined**. **Dissent: reader-1 filed BLOCKER; demoted on LATENCY — preserved WITH ITS REVIVAL CONDITION** |
| **G2c** The seam is tracked and the generator runs | **fr-FourierShapeExtractor L-B2 / C-2 (one identity) ⊕ fr-FourierShapeExtractor L-B3** | the ONLY consumer of this component's output is UNTRACKED **and** BROKEN AT IMPORT **and** PARAMETER-DIVERGED: `.gitignore:53 scripts/*` swallows both `precompute_svg_fourier.py` and `raw-contours.json`; the script imports `order_contours`, which **no module defines**; it hard-codes 100/12 vs the shipped 50/10 — a clone holds the producer and 450 KB of product with nothing between; and canonical geometry is a pure function of an **unpinned third-party PRNG with zero drift detection** | Track the seam (`.gitignore` negation lines), fix the import (`build_contour_tour(...).path`), reconcile 50/10, and **prefer the tracked in-process nav-icons idiom** for retiring the browser round-trip. ▲ **REPAIR-SHAPING CORRECTION BOUND IN (M-2, verified): `git ls-files scripts/` = 16 paths including a TRACKED, WORKING, in-process `precompute_nav_icons.py` producing 5 of the 7 `fourier-paths/*.json` assets. The correct frame is "a tracked path exists for 5 of 7; only sun/moon route through an untracked browser detour" — BOTH axes' falsifier sweeps asserted otherwise on a FALSE ENUMERATION.** L-B3's concrete 0.4.1→0.12.0 exposure is **CLOSED** (Δ 6.10e-6); the absent drift detector survives, and **tracking `raw-contours.json` IS the golden file** — L-B3 RIDES this cure, not a separate act. **Dissent: reader-1 BLOCKER on L-B2/C-2, demoted on M-2's repair-shrink; revival = the tripwire** |
| **G3c** Closure is CARRIED, never inferred | **fr-FourierShapeExtractor L-M1 (+ the 44.08 arm) ⊕ M-8 ⊕ C-3** | closed shapes are emitted OPEN under ONE convention — every branch is endpoint-exclusive, so closed shapes lose exactly one step (crescent 3.75, disc 2.45) and polygons lose a whole authored edge (diamonds 23.6–27.1%, stars 8.6–11.6%) — **and the largest instance was in NO axis table: `sun[0]`, the 20-vertex ray polygon, a 44.08-unit dropped closing flank on the page's dominant silhouette**; the mirror wrong follows (open paths lose their TERMINAL point); and `[number,number][][]` cannot express open-vs-closed while two open strokes (spiral gap 44.97, moon inner stroke 91.54) feed a closed-only downstream (`pointsToSvgPath(…, closed = true)`, all callers 1-arg) | A closure FLAG + wrap at extraction — ONE cure with C-3. ▲ **The "unambiguous discriminator" sub-claim is KILLED (44.08 closed vs 44.97 open OVERLAP — no post-hoc heuristic can recover closure), which STRENGTHENS the type complaint: the flag must be CARRIED, not INFERRED. v2 must not specify a heuristic** |
| **G4c** A parameter that governs | **fr-FourierShapeExtractor C-4 ⊕ C-14** | `samplesPerPath` is honoured by ONE of three branches — paths get 128, circles get `max(16, round(128·r/50))` (a magic 50, no constant), polygons IGNORE it — measured output `[20,123,128,4,4,4,16,16]`: **a parameter named "samples per path" produced a 4-point contour**; and the polygon branch's exact-vertex care is undone by `resample_arc_length(stitched, 512)` downstream, documented in neither file | A contract doc or a uniform post-pass. ▲ **C-14 BOUNDS C-4's geometric cost — book them together.** Same family as F9 |
| **G5c** A DIAGNOSED result type — precondition for regeneration | **fr-FourierShapeExtractor L-M3 / C-6** | THREE silent-drop gates with ZERO diagnostics on the path that DEFINES shipped geometry (`:40` sub-unit bail, `:47-49` bare catch, `:52` floor; `grep console\|throw\|warn` → 0), the caller records no counts, the exit is a bare return — while the SERVER TWIN ships `extract_contours_result` WITH diagnostics | A diagnosed result type mirroring the server twin. ▲ **This is the mechanism that makes G1c structurally UNOBSERVABLE at the human seam — the diagnostics row is a PRECONDITION for trusting any future regeneration and SEQUENCES WITH THE TRIPWIRE** |
| **G6c** A typed, named machine handoff | **fr-FourierShapeExtractor L-M5 / C-5**; cured with fr-FourierShapeExtractor C-15 (A3) | the machine handoff is dead, untyped, and hostage to a presentational node: `if (el)` wraps BOTH writes, `(window as any)` with no `declare global` anywhere, ZERO readers, and the Playwright comment is false at HEAD — while the file owns the correct ref idiom twice | A NAMED EXPORTED INTERFACE + ref emission, **decided WITH the pipeline repair**. ▲ **Corpus correction bound in: SE-05's "just delete it" is WRONG on the tree — `precompute_svg_fourier.py:4-5` names this component as its INPUT; the seam needs a real harvester or a real deletion WITH the decision, not either alone** |
| **G7c** Which stage-0 survives | **fr-FourierShapeExtractor C-7** | zero consumption of the 39-function API surface: the leaf **REIMPLEMENTS STAGE 0** of the server's contour pipeline, with no test runner in `web/` at all — the R6-8 echo at build time (runtime twin: C-5/BasisCanvas at B1) | The pipeline decision names WHICH of the two stage-0s survives. ▲ **C's honesty adopted and BINDING: the decomposition is NOT duplicated and the offline detour is ELECTIVE (`saveContour` accepts ingested contours) — v2 must not overstate the duplication** |
| **G8c** Extraction SCOPE — space and visibility (two clauses, one family) | **fr-FourierShapeExtractor L-M2 · M-11** | CTM-naive extraction (transform-blind) with `polyline` absent from the selector (latent: live SVGs use bare `<g>`s); and `extractContours` has **no paint-visibility filter** — `<defs>`/`<mask>`/`<clipPath>`/`display:none` subtrees would contribute canonical geometry — while the repo ALREADY mounts a global `<defs>` surface app-wide (SvgFilters), so **the hazard idiom is LIVE in-tree** | Two clauses, one family — **space-blindness and visibility-blindness are distinct**. L-M2 magnitude → SS-13 |
| **G9c** The guards check the wrong predicate — twice | **fr-FourierShapeExtractor M-7 · M-9** *(NOT fr-BasisSelector M-9 at D12)* | `if (totalLen < 1) continue;` is **NaN-PERMISSIVE** (`NaN < 1` is false): a NaN `getTotalLength()` emits 128 `[NaN,NaN]` samples that pass every gate and serialize as `null` pairs into the handoff; and the `points.length >= 3` floor guards CARDINALITY, not EXTENT — an `r = 0` circle yields 16 coincident points that pass and reach the decomposition, the one branch with no degenerate bail | Correct guard `!(totalLen >= 1)`; an extent predicate beside the cardinality one. **One family: the extraction guards check the wrong predicate twice.** ▲ **M-9 BOUNDS L·S-4's superlative — carry the bound WITH the superlative so v2 does not inherit a false credit.** M-7 registry-swept clean |
| **G10c** The artifact's provenance prose is false | **fr-FourierShapeExtractor L-m3 / C-10 ⊕ L-m5** | the SINGLE provenance comment is arithmetically FALSE (42·100+42 = 4242) and cites a frame-seed formula existing in NEITHER repo (`useLineBoil` takes no seed) — **the only prose explaining why the canonical geometry is what it is, wrong twice** — while the canonical seed set (42; 1,2,3; 10,20,30), the artifact's only free parameters, is scattered inline with no single knob | **Deduped: the false comment and the scattered seeds are ONE cure** — one named seed constant, one true provenance note. The provenance union's most literal row |

### 2a. Owner rulings owed — flagged INLINE (SS-4)

| # | ruling | honest default absent a ruling | cost of the other branch |
|---|---|---|---|
| **R1** | **TA-4** — restore `atomdiff.ts` + `atomDiff`, or re-scope value.js out of the diff clause (G4/E3) | **none may be assumed** — the wave may not author §6 either way | restore = value-side work; re-scope = a one-sided §6 verdict, stated explicitly |
| **R2** | **trie vs KISS** (G7/E16) | **no trie**; whole-snapshot duplication is the recorded shipped behaviour | a trie contradicts `atomdiff.py:12-14`, the incumbent guardrail in BOTH trees. **Dissent recorded** |
| **R3** | **flags producer-or-retire** (G11/D3) | none — this is **the admission gate** | **PRECEDES F.W1's sizing** wherever FR-AFP grades are load-bearing (ruling 5) |
| **R4** | **the like verb** (G10/D2) | none — *add the operation or remove the affordance; no third option ships* | the affordance lies under `aria-pressed` either way until ruled |
| **R5** | **off-state `[]` admission** (M-9, D12) | none — *admit it in the contract or stop minting it* | the silent rewrite must not survive either branch |
| **R6** | **hard-delete arm** (FR-AFP-9, D6) | copy stays truthful about restorability | `?hard=true` has zero callers today |
| **R7** | **codegen vs hand-typed twins** (FR-AFP-71, A4) | inv-16/inv-26 **RESTATED** | amendment is permitted; **silent reversal is not** |
| **R8** | **remix-vs-fork + born visibility** (E4) | none — both defaults ship, opposite | must not contradict **ruling D9** |
| **R9** | **delete-or-wire the dead session subsystem** (FR-USB-23, C3) | delete (zero external call sites) | wiring re-attaches an anonymous identity on reload |

### 2b. Dissent register — preserved, not resolved by this wave

D-L4 (R2 graded MAJOR: twelve untyped edges, no gate) · FR-USB-15 (K1's BLOCKER filing stands as recorded dissent; r2's demotion RATIFIED) · FR-AFP-8 (L axis BLOCKER; β's MAJOR sustained under K10) · FR-GFC-3 (C axis BLOCKER at two sites) · FR-GFC-1 (two BLOCKER filings, D and C axes) · GAB-17 (axes split MAJOR/MINOR/INFO; MINOR ruled) · F-β/SS-C-1 (C axis BLOCKER; demoted at component altitude) · L-B1 and L-B2/C-2 (reader-1 BLOCKER; demoted **with revival conditions** = the DO-NOT-REGENERATE tripwire) · R-4 (the trie requirement vs the documented KISS guardrail — unresolved).

### 2c. Closure roster (G19's material) — **the CANONICAL's, adopted verbatim; no carry ledger, no wave-authored census**

**LHS = `docs/tranches/X/fourier/conformance/CENSUS-CANONICAL.md` §2 — `F.W5` (28 rows) ⊕ `F.W5-W8` (90 rows) = 118 record-side rows**, record-qualified, taken as stamped. **RHS = this file's registers**: the §A–§G clause tables ⊕ §4's cross-edge citations ⊕ §5's carried-by-citation table. **Result: booked-or-cited = 118 · escaped = 0** after this round's three cures (D10 · A5/D12 · D2).

▲ **THIS SECTION NO LONGER DERIVES A CENSUS, AND THE ONE IT USED TO DERIVE IS STRUCK WHOLE (repair round 4, rulings R4-5 · R4-3 · R4-10; PASS-4 D-1 · D-2 · D-8 · D-14 · D-15 · D-18 all convict the same paragraph).** The struck text printed a four-arm shape classification — *"70 markdown table rows · 100 id-headed bullets (96 bulleted ⊕ 4 numbered) · 42 bullets with no bold head · 28 non-bullet prose"* — and reached `routedTotal = 170`. **Two of those four arms were false by exactly ±40** (the corpus holds **two** no-bold-head bullets, not 42, and **68** non-bullet prose lines, not 28), so the arms summed to 240 only because the errors cancelled: *a balanced decomposition whose middle terms are individually false is not a run, it is a reconstruction.* The total was then built a second, incompatible way — `167 + 2 + 1` — i.e. **the round-2 check-file figure this same paragraph forbade, with an addition in front of it**; and the two constructions disagreed about three members (`fr-EquationPanel:48`, `fr-AdminUserList:161`), so the roster G19 ran on was undetermined at exactly the level a set-difference means anything. Two further arms were unauditable by construction: the subtraction named *"the six `Routing law:` boilerplate lines"* exactly while leaving ~40 sibling boilerplate lines (`Routes:` ×11, `Dispositions:` ×2, closing verdicts, fold notes) unenumerated, and *"the four lines that carry more than one routing"* counted **token** multiplicity as **routing** multiplicity (only `fr-EquationPanel:48` carries two routings; the other three are one identity whose token appears twice in its own cells). **None of it is repaired arm-by-arm, because none of it should exist**: R4-10 forbids a wave to derive, sample or re-cut a roster from any source. The canonical derives once, for all eleven waves, from the 66 records read whole under §0's rules; this file adopts its §2 rosters and books against them.

▲ **WHAT THIS FILE MAY STILL COUNT: ITS OWN REGISTER LINES.** A count of what a spec wrote is not a census. ⟨cmd⟩ `grep -cE '^\| \*\*[A-G][0-9]+c?\*\*' F-W5.md` (this seat, 2026-08-29, `$X/waves` base) → **93**, of which **22** are the §3 gate rows `G1`–`G22` matched by the same pattern, leaving **71 clause rows** — §A 6 · §B 5 · §C 5 · §D 16 (`D1`–`D8`, `D10`–`D17`; no clause is numbered D9) · §E 20 · §F 9 · §G 10 (`G1c`–`G10c`) — ⊕ **4** §5 carried-by-citation rows. The 71 and the 118 are **different denominators measuring different things** (clauses agglomerate: one clause folds many rows by identity), and **neither is derived from the other**. Any future sentence in this file of the form *"N routings"* about the corpus is a defect on sight; the corpus figure is the canonical's or it does not exist.

▲ **THE BAND FIGURES SURVIVE AS RECEIPTS ABOUT THE CORPUS, NOT AS A DENOMINATOR.** Re-run by this seat 2026-08-29 under `/usr/bin/grep` from `$R`: **120 occurrences / 26 records** hyphen ⊕ **72 / 24** en-dash = **192 / 49 records**, of which **23 are en-dash-ONLY**; token-bounded `F.W5` = **245 occurrences on 240 lines across 54 records** (sole-`F.W5` = 245 − 192 = **53**). These reproduce to the digit and are what justify §2's both-dash law — **a hyphen-only reading is structurally blind to 23 records** — and nothing more. They size no clause and close no gate.

▲ **THE `fr-AdminUserList` R6-8 QUESTION IS DISSOLVED, NOT RE-DECIDED (supersedes round 3's D-16 admission).** Round 3 spent a paragraph admitting `fr-AdminUserList:161`'s R6-8 intake carry *into the denominator*, on the correct principle that **a detector applied to four of five instances of one shape is a restricted detector, DEFECTIVE at authoring whatever it returns** (R2-9/R3-5). **The principle stands and is retained; the arithmetic it served no longer exists.** Under §0.1's one-home rule the canonical homes `R6-8` at **four** records — `fr-AnimationControls` · `fr-CanvasControlsDock` · `fr-ConvergencePlot` (all `F.W5`) and `fr-EasingCurvePreview` (`F.W5-W8`) — and reads `fr-AdminUserList:161`'s line as a **citation inside another row's body**, which books no row of its own: ⟨cmd⟩ `sed -n '161p' "$R"/fr-AdminUserList.md` → *"- Intake identities folded by reference, never re-booked: … **R6-8** (FR-AUL-3/11/12 are instances; **carry → F.W5**) …"* — the line's own words. **No escape either way**: `R6-8` is booked at **B1** as this wave's governing law, and `FR-AUL-3`/`-11`/`-12` are booked at **B1**/**B3** as canonical `F.W5` rows in their own right.

▲ **LEG-vs-HOST: WHERE THE CANONICAL HOMES AN ID ELSEWHERE WITH AN `F.W5`/`F.W5-W8` LEG, THIS FILE CITES AND BOOKS NOTHING (repair round 4, ruling R4-6; R4-10's fabrication clause).** *"A leg is never a new identity; it books as ONE explicit row at the wave that HOLDS the host id."* Four sites in this file carried a leg in a booking voice, and each is re-marked at its clause: **`fr-ContourSettings B-1`** at **D7** (canonical home `F.W3`, file-criterion §5.d; legs `F.W5-W8`, `SS-13`) · **`fr-ContourSettings M-10`** at **D12** (home `F.W3`; leg `F.W5-W8`) · **`fr-ContourSettings M-15`** at **F5** (home `F.W3`; legs `F.W5-W8`, `SS-13`) · **`fr-GalleryInfiniteGrid C-4 / D-13`** at **D10** (home `F.W3`; legs `F.W5-W8`, `SS-1`). In every case **the contract obligation is genuinely F.W5's and the ROW is F.W3's** — the leg is cited, the cure-shape is stated in F.W5's voice, and no id is claimed twice. Separately, **`fr-CanvasControlsDock C-28` is not a canonical identity at all**: it is named *inside* that record's `R6-8` row. ⟨cmd⟩ `grep -n 'C-28' "$R"/fr-CanvasControlsDock.md` (this seat, 2026-08-29) → **THREE hits, all disclosed** (D-14's discipline): `:45` — the `D-4 / L-1 / C-3` publish-re-entrancy row · `:102` — the *Full-coverage note* (*"C-28 into D-4's F.W5–W8 rider"*, the second excluded mis-attribution) · **`:150`** — the operative one, *"**R6-8** (intake, CARRY→F.W5): C-28 folds …"*. **Not one of the three is a `C-28` banked head**, which is the whole point: that record's ONE canonical `F.W5` row is `R6-8`, booked at **B1**. B1's C-28 material is the body of that booking, not a second one.

▲ **THE 118 ENUMERATED, PER RECORD, WITH THE REGISTER EACH LANDS IN — so the set-difference is computable from this section as written (R4-10's wave duty: *book each canonical id, or cite the holder by name*).** Record-qualified throughout; a bare id below is read with the record heading it.

**`F.W5` — 28 rows**

| record (rows) | id → register |
|---|---|
| **fr-AdminAuditLog** (4) | `AA-6` → D15 · **`AA-23` → D15 (HOST, R4-6.4)** · `AA-31` → B1 · `AA-32` → D15 |
| **fr-AdminUserList** (11) | `FR-AUL-3` → B1 · `-11` → B3 · `-12` → B3 · `-13` → B3 · `-14` → D13 · `-20` → D11 · `-21` → D11⊕D12 · `-25` → D11 · `-31` → D12 · `-46` → D7 · `-59` → D15 |
| **fr-AnimationControls** (1) | `R6-8` → B1 (the intake carry; identity stays with the intake) |
| **fr-CanvasControlsDock** (1) | `R6-8` → B1 (its `C-28` body-mention books nothing — see above) |
| **fr-ContourPreview** (1) | `L:L-5` → E20 |
| **fr-ConvergencePlot** (3) | `C-7` → B2 · `K-13` → F7 (record-qualified; **not** F2's `fr-EquationView K-13`) · `R6-8` → B1 |
| **fr-EquationModeToggle** (2) | `FR-EMT-20` → E10 · `FR-EMT-1` → F1 |
| **fr-EquationResult** (1) | `FR-EQR-32` → B1, enforceable-iff at F8 |
| **fr-GalleryAdminBanner** (2) | `GAB-15` → D11 · `GAB-16` → D11 |
| **fr-NotationPills** (1) | **`FR-NP-30` → A5** (banked head restored this round; `-30b` rides beside it), cited D12 |
| **fr-VisualizationView** (1) | `D-14 / MIN-4 / MIN-5 / MIN-10 / MIN-11` → E19 (full banked head; `F.W4` leg cited, not booked) |

**`F.W5-W8` — 90 rows**

| record (rows) | id → register |
|---|---|
| **fr-AdminAuditLog** (1) | `AA-5` → D16 |
| **fr-AdminFlaggedPanel** (12) | `FR-AFP-1` → D3 · `-4` → C2 · `-7` → D4 · `-8` → D5 · `-9` → D6 · `-10` → D8 · `-18` → A2 · `-33` → D7 · `-36` → A4 · `-66` → D6 · `-70` → D8 · `-71` → A4 |
| **fr-AnimationControls** (1) | `C-30` → B1 |
| **fr-BasisCanvas** (6) | `BC-9` → E8 · `C-5` → B1 · `C-7` → E9 · `M-β4` → E10 · `C-17` → E15 · `C-18` → E15 |
| **fr-BasisSelector** (3) | `M-9` → D12 ⊙ · `M-14` → E14 · `m-7` → D7 |
| **fr-CoefficientsPanel** (1) | `FR-CP-16` → A3 (the measured 45-operation denominator) |
| **fr-CoefficientsSpectrum** (1) | `M-13` → A5 |
| **fr-ContourEditorCanvas** (1) | `C-2` → E17 |
| **fr-ContourSettings** (3) | `B-4` → E13 · `M-13` → E14 · `i-7` → B1 |
| **fr-EasingCurvePreview** (1) | `R6-8` → B1 |
| **fr-EasingPicker** (1) | `MISSED-E` → E12 |
| **fr-EditorControlsDock** (1) | `C-6` → C1 |
| **fr-EqCoefficientsPanel** (1) | `FR-EQC-8` → A5 |
| **fr-EquationPanel** (2) | `D-10` → F9 · `D-L5` → C4 (both routings of the record's one `F.W5`-bearing line) |
| **fr-EquationView** (4) | `B-1` → F1 · `L·m-6` → E10 · `M-SB` → F3 · `M-DV` → F4 |
| **fr-FourierShapeExtractor** (18) | `L-B1` → G1c · `L-B2` → G2c · `L-B3` → G2c · `L-M1` → G3c · `C-3` → G3c · `M-8` → G3c · `C-4` → G4c · `C-14` → G4c · `L-M3` → G5c · `L-M5` → G6c · `C-15` → G6c ⊕ A3 · `C-7` → G7c · `L-M2` → G8c · `M-11` → G8c · `M-7` → G9c · `M-9` → G9c · `L-m3` → G10c · `L-m5` → G10c |
| **fr-FrequencyGraph** (2) | `FR-FG-13` → A3 · `FR-FG-21` → D7 |
| **fr-FunctionInput** (2) | `C-8` → F2 · `N-4` → F3 |
| **fr-GalleryAdminBanner** (1) | `GAB-12` → A3 (MEASURE-AT-OPEN, D-19) |
| **fr-GalleryCard** (2) | `C-1-as-corrected` → D2 · `D-7 / C·I-2` → D1, cited E4 |
| **fr-GalleryCardModal** (3) | `GCM-2` → D2 · `GCM-52` → C1 · `GCM-55` → A3 |
| **fr-GalleryDraftsSection** (2) | `F-4` → C1 · `m-15` → C2 |
| **fr-GalleryFeaturedCarousel** (2) | `FR-GFC-3` → D2 · `FR-GFC-4` → D10 |
| **fr-GalleryInfiniteGrid** (2) | **`R-7` → D10** (pass-4 D-3's escape, booked this round) · **`C-3` → D2** (spelled with its record; the `GIG` abbreviation is retired) |
| **fr-GalleryMarquee** (1) | `GM-M4` → B4 |
| **fr-GallerySearchBar** (1) | `FR-GSB-28` → B1 |
| **fr-GalleryView** (7) | `FR-GV-8` → D1 · `-9` → D8 · `-12` → E6 · `-13` → D10 · `-24` → E6 · `-27` → D1 · `-34` → D6 |
| **fr-ImageUpload** (1) | `C:C-12` → B1 (security half C2, transport half E15) |
| **fr-PaperSearchDropdown** (1) | `C:C-23` → **cited, not booked**: §4's F.W5→F.W2 edge, with F.W2 named as the adopting wave |
| **fr-PathPreview** (1) | `PP-DEADSEAM` → B4 |
| **fr-SpeedSelect** (2) | `SS-C-1` → E7 · `SS-C-2` → E8 |
| **fr-UserSlugBar** (3) | `FR-USB-15` → C3 · `FR-USB-23` → C3 ⊙ · `FR-USB-24` → C4 |

▲ **THE THREE `NO-WAVE-OWNER` AUDIT-LOG ROWS ARE EXCLUDED FROM THE LHS, WITH THE RECEIPT THIS SECTION GAVE AA-48 AND DENIED THEM (repair round 3, rulings R3-6.1/6.2; PASS-3 D-1).** `AA-45` · `AA-46` · `AA-47` are banked **NO-WAVE-OWNER** at `$R/fr-AdminAuditLog.md:88`–`:90` and **carry no `F.W5` token anywhere**, so they are **not members of the LHS** — and the canonical, derived independently, homes them the same way: `AA-45` → **NWO→SS-3**, `AA-46` and `AA-47` → **NWO (packet)**, none of the three in either F.W5 roster. The exclusion is now a property of the census of record rather than of this wave's reading; the criterion by which AA-48 was excluded is applied evenly instead of to one of four. **Their ONE home is `F-W10.md` §2.5, the NO-WAVE-OWNER drain**, whose rows already carry terminal verbs (`AA-45` NEXT-BOUNDARY LEDGER · `AA-46` **KILL-WITH-RATIONALE**, *"a redundancy record; no defect, no work"* · `AA-47` NEXT-BOUNDARY LEDGER). Clauses **A1/A2/A3** cite those rows and book none of them; `AA-46`'s contradiction resolves at the bytes **in F-W10's favour** and its kill **STANDS**. This file previously certified `doubleHomed = 0` for FR-AUL-46 under R-5's one-home law while breaking that law three times two clauses above — the disclosure is the cure, and the **standing tie-breaker** now governs the seam: *a banked NO-WAVE-OWNER row's one home is the drain; the clause bank cites it and never books it; the converse never occurs* (R3-6.5).

▲ **THE TWO MIS-ATTRIBUTIONS, RETAINED AS READING NOTES AND NOW CORROBORATED BY THE CANONICAL.** They were once subtraction terms in a total this file no longer computes; they survive because both are traps a later reader will re-enter at the record bytes, and because the canonical **independently homes neither id in this band** — which is the strongest available check on a reading. In both, the band token belongs to an adjacent fold-note, not to the row it sits under:

- **`fr-AnimationControls M-13` (LC)** is NOT a routing of M-13. ⟨cmd⟩ `sed -n '127p;129p' fr-AnimationControls.md` (this seat, 2026-08-28) → the row's own disposition is *"fold-rider on **fr-BasisCanvas D-9/BC-8**"*, while the `F.W5–W8` token two lines down opens *"**Reader misses folded WITHOUT re-booking (identity guard)** … **= fr-BasisCanvas BC-9/C-6/D-20**, already banked MAJOR → F.W5–W8"* — that id is booked at **E8**, and M-13 is not booked at all. (Anti-rename note: this is a THIRD `M-13`, distinct from `fr-ContourSettings M-13` at E14 and `fr-CoefficientsSpectrum M-13` at A5 — all three keep their banked spellings.)
- **`fr-CanvasControlsDock M-10` (banked; reader-LC)** is NOT a routing either — its disposition is **NO-WAVE-OWNER**. ⟨cmd⟩ `sed -n '100p;102p' fr-CanvasControlsDock.md` → the row is the L-axis line-citation-drift finding, and the token at `:102` sits inside the *Full-coverage note* (*"C-28 into D-4's F.W5–W8 rider"*) — that **C-28 fold is booked at B1** (R2-3d.5). The `fr-BasisSelector M-10` homonym is a fourth spelling, named at §2's anti-rename block.

▲ **THE MEASURED NEGATIVES: WHAT THEY ARE AND WHERE THEY GO — THE QUESTION THAT COST THREE ROUNDS, NOW SETTLED BY THE CENSUS OF RECORD (repair round 4; PASS-4 D-8 closes the arc that PASS-3 D-15 opened).** Round 2 defined the census as routings *"whose **terminal disposition names** `F.W5`"*, which was false of the negatives; round 3 re-worded it to *"an id-keyed row **whose row carries** an `F.W5` routing token"* and ruled the negatives IN — and then reached a total only obtainable by leaving them OUT, so the definition and the arithmetic contradicted each other at exactly two ids. **The canonical settles it by construction and in the other direction**: neither `fr-App` nor `fr-MobileFloatingToc` appears in the `F.W5` or `F.W5-W8` rosters at all, because a record spending a row to say *nothing routes here* banks no routing to this band. **They are not rows of this wave; they are EVIDENCE, and their home is §4's F.W2 zero-cell export.** That is what B5 and P-9 have said all along — *zero-cells are evidence, never a denominator* — and this section now says it identically. The bytes, unchanged and re-verified this seat 2026-08-29 from `$R`: ⟨cmd⟩ `sed -n '144p' fr-App.md` → *"**Nothing routes → F.W5–W8**: App's coupling budget is spent entirely on the CSS-token↔parser seam and the mount seam; the CRUD/provenance union has no edge in this component (C-12)"*, and ⟨cmd⟩ `sed -n '97p' fr-MobileFloatingToc.md` → *"**F.W5–W8 gets nothing** — the component is API-inert, value.js-inert, keyframes-inert"*. Both **land** — in §4's F.W2 zero-cell export, as **evidence, never a denominator** (B5/P-9). A wave that counted them as work would manufacture two clauses out of two negatives.

▲ **THE THREE IDS AT §5 THAT ARE *NOT* MEMBERS OF THE 118 — stated separately, never folded into any denominator (D-4 cure, re-based on the canonical this round; §2c and §5 say the same thing).** Pass 2 convicted the former tally for reaching its total by mixing row-counts with id-counts and admitting non-routed ids. The partition is explicit, and each exclusion is now corroborated by the census of record rather than resting on this wave's reading alone:

| id | why it is OUTSIDE the 118 | receipt |
|---|---|---|
| **AA-48** | recorded at A1 as the same latent class as AA-45, **never routed to F.W5**. ▲ *Canonical minute (round 4)*: the census of record homes it **UNROUTED** — its row carries no routing token at all, not the `NO-WAVE-OWNER` this cell formerly attributed to it. The exclusion is unchanged and now stronger (an UNROUTED row is a fortiori not an F.W5 row); the mis-attribution is corrected rather than carried, and `AA-48` sits in the canonical's 307-row UNROUTED frontier owned by the forming SS-3/SS-4 specs | ⟨cmd⟩ `sed -n '91p' fr-AdminAuditLog.md` → *"**AA-48 · worker-2 missed.** The two raw `<button>`s carry no `type` (default `submit`); inert today (no form ancestor), discharged free by AA-11's cure. Recorded as the same latent class as AA-45."* |
| **P-9** | a **carry-ledger id, not a registry id** — it occurs in **none** of the 66 records, and the canonical (which censuses only banked-head lines in those records) therefore holds **zero** `P-9` rows — ⟨cmd⟩ `grep -cw 'P-9' "$X"/conformance/CENSUS-CANONICAL.md` → **0** (the same bounded instrument as the corpus probe, run against the census of record) | ⟨cmd⟩ `grep -lw 'P-9' fr-*.md \| wc -l` (the portable spelling ruled at §2 item 2 — the former bracket-class ERE returns **0 for every token** under a `ugrep` shim and so proved nothing; this one carries its positive control) → **0** (against **6** for the unbounded probe — all six `FR-AFP-9`/`FR-CP-9` artefacts; §2 item 2 carries the trap) |
| **AA-44** | routed at the record to **F.W9/W10**, not to F.W5 — the canonical homes it **F.W9** (leg `F.W9/W10`) and lists it in the F.W9 roster of 17, so its holder is named and it is no debt of this wave | ⟨cmd⟩ `sed -n '87p' fr-AdminAuditLog.md` → *"**AA-44 · L-18.** Zero frontend tests; nine e2e specs never exercise the audit tab … → **F.W9/W10**."* |

▲ **`SS-L-07 / SS-C-10` MOVES OUT OF THE LHS TOO, AND FOR A BETTER REASON THAN THE ONE THAT KEPT IT IN (repair round 4, ruling R4-6; supersedes round 3's *"the one §5 row that IS inside the 170"*).** The canonical homes the row at **`F.W4`** with an **`F.W5-W8` leg** — one banked head `SS-L-07`, alias `SS-C-10` beside it — so it is **not a member of this wave's 118**: its body is F.W4's and **F.W5's share is the type-reach only**, which is exactly what the record's own terminal cell says. The whole 120-character return, ⟨cmd⟩ `sed -n '59p' fr-SpeedSelect.md | rev | cut -c1-120 | rev` (this seat, 2026-08-29, `$R` base), pasted **in full**: *"L-11+M-10 empty-trigger state unrepresentable at the type level. | **F.W4** (+ its type reaches the F.W5-W8 wire rows) |"*. ▲ *Label note (R4-7.1; PASS-4 D-9)*: this receipt was twice introduced with *"reads **exactly**"* above a ~55-character span — under half of what the command prints, with the narrowing undisclosed, at both this site and §5. **A verbatim label means bytes**; the full return is printed here and at §5, and neither site claims a narrowing it did not mark. **Carried by citation is a LANDING, not a booking** — and with the canonical's home named, the id sits on exactly one side at both ends.

▲ **This tally is an AUTHORING SELF-REPORT, not the gate.** G19 greens only when unit e runs the set-difference against the **EXECUTED** documents. **No `F-W5-CARRY.md` is authored to satisfy it** (R-3.2: minting a new authority mid-repair multiplies the defect class) — the operand is the 66-record registry plus the two real carries, all of which exist and all of which are cited by path and stable anchor.

---

## 3. Gates — 22, all born-RED

`$V` = `/Users/mkbabb/Programming/value.js` · `$F` = `/Users/mkbabb/Programming/fourier-analysis` (READ-ONLY).

▲ **D-19 discipline**: every fourier-side witness below is **MEASURE-AT-OPEN** — no line anchor, HEAD figure or *"LIVE at certified HEAD"* citation stands until F.W0 re-grounds the substrate (GAB-12's cell was measured on uncommitted bytes — GAB-13). Value-side witnesses (G2, G4, and G5's value leg) were **re-measured read-only 2026-08-28** and are pasted with their commands. ▲ **Consistent with the masthead gate as narrowed**: the execution gate bars **writes, builds and runs**, not reads — a born-RED witness is *definitionally* a read, and a spec that forbade reading its own witnesses would convict itself on the first line. Every command in this table is non-mutating (`ls`, `grep`, `sed`) and **no witness in this file writes a byte in either tree** — that is the posture the execution gate demands, and it holds without exception. ▲ **THE SECOND HALF OF THIS SENTENCE WAS FALSE AND IS STRUCK (repair round 3, PASS-3 D-13).** It formerly read *"…and none reads `$F` at all"*, which the table contradicts three rows down: **G1** `grep -rn "class AuditEntry" $F/api --include='*.py'` · **G3** `grep -rn "_write_root_version" $F/api` · **G7** `grep -rniE "trie|…" $F/api $F/web/src $V/api/src $V/src`, the last **asserting a measured result** (*"ZERO true hits on BOTH trees"*). **Reading `$F` is lawful and is what those witnesses do**; what is unlawful is writing it, and no witness here does. The honest statement: **`$F` is READ-ONLY and is READ — never written** — and every `$F` figure stated anywhere in this file is **MEASURE-AT-OPEN (D-19)**, re-taken after F.W0 re-grounds the substrate, precisely because a read taken today against a dirty worktree is not HEAD truth (GAB-13).

| # | gate | born-RED witness | F.W5 close | green owner |
|---|---|---|---|---|
| **G1** | identity on the wire | `grep -rn "class AuditEntry" $F/api --include='*.py'` — four fields; pydantic extra-ignore drops `_id`; `AdminAuditLog.vue`'s `:key` is positional **because** the contract offers no identity (AA-45; AA-48 same class) | every list-bearing response class in v2 carries a stable per-row identity | F.W4 (the `:key` deletion) |
| **G2** | version-identity collision (V-β) | **measured 2026-08-28**: `$V/api/src/modules/palette/hash.ts` folds `{name, colors}` only; `repository/paletteVersion.ts` `findByHash(hash)` filters `{_id: hash}` with **no slug scope**; `insertIfAbsent` returns early on a hit (its own test asserts *"no second write"*) ⇒ two same-content palettes share one version row and the second's history is empty | v2 mandates the compound per-entity version `_id` (fourier's `f"{viz_slug}:{set_hash}"` form) | value.js API row **+ a test asserting two same-content palettes keep separate histories — none exists today** |
| **G3** | chain depth (F-α) | `grep -rn "_write_root_version" $F/api` → def + create + remix, the ONLY writer; `parent_hash=None, root_hash=set_hash_value, depth=0`; `GET /{slug}/versions` sorts by `depth` over an always-singleton set | v2 states **deepen-or-retire** for the depth/parent/root quadruple | F.W6 burn-down (X-8 cross-edge). **F.W5 owns the CLAUSE only — no double-booking** |
| **G4** ⊙ | diff-clause participation (TA-4) | **measured 2026-08-28**: `grep -rn "atomdiff\|atomDiff" $V/api/src $V/src` → **ONE** comment hit (`__tests__/palettes-forks.test.ts:9`, which names TA-4 as the excision); `ls $V/api/src/lib` → *No such file or directory*. v1 §2.5 mandates the exact file; §6's close-gate assumes BOTH probes exist | owner rules RESTORE, **or** v2 re-scopes value.js out of the diff clause EXPLICITLY with §6 re-authored to a one-sided verdict. **The wave may not assume either** | owner → value.js API row |
| **G5** | born-visibility of the derived variant | fourier remix child born `draft` (`visualization.py`, MEASURE-AT-OPEN) vs **value.js fork child measured 2026-08-28 at `$V/api/src/modules/palette/service/forks.ts:76` — `visibility: "public"` hard-coded** — opposite privacy defaults on the same verb, both shipping (lane-crud R-5) | one co-signed born-visibility rule that does not contradict **ruling D9** | both API rows **+ a create-visibility test each side** |
| **G6** | redaction parity (F-γ / R-6) | value.js collapses non-public hops to `{kind:"unavailable", ordinal}`; fourier's breadcrumb emits `slug`/`set_hash`/`author_slug`/`created_at` for EVERY ancestor — `_readable_or_none` applied only to the entry row | the breadcrumb redacts to the discriminated-union shape, **or** the asymmetry is stated with its reason | fourier API row |
| **G7** ⊙ | trie disposition (R-4) | `grep -rniE "trie\|prefix.?tree\|radix\|patricia\|structural.?sharing\|delta.?compress" $F/api $F/web/src $V/api/src $V/src` → **ZERO true hits on BOTH trees**; `atomdiff.py:12-14` is the explicit anti-tree KISS guardrail | owner rules trie-vs-KISS. Absent a ruling v2 carries the honest default: **no trie; whole-snapshot duplication is the recorded shipped behaviour** | owner (SS-4 flags INLINE; **dissent recorded**) |
| **G8** | security documentation (R3-7b) | OpenAPI security **0/45**; `save_contour(req)` takes no `Depends`/`Header`/`Request` and its router declares no `dependencies=`; blob + thumbnail GETs carry no `Depends` with `Cache-Control: public, max-age=86400` | **EVERY row of `operation-register.md` carries an authority class**; the `save_contour` and image-GET decisions are made | fourier API row |
| **G9** | client-gap closure (R3-7c ⊕ X-3) | 36 client edges / 9 gap operations; **SEVEN of 13** `/api/visualizations` operations (remix, publish, unpublish, forks, provenance, diff, versions) have zero client function — three independent counts agree | every gap operation carries a disposition (clientable / struck / deliberately server-only). ▲ **Counting lock K-1: read against 13/44, never zero; cite 45 = 30+13+1+1** | F.W1/F.W4 (clients) · fourier API row (strikes) |
| **G10** ⊙ | the like verb | `likes:int=0` + the `(visibility, likes, -id)` index + `cursors.py:22`'s `"likes"` sort key all ship; **no like route anywhere** (`api.ts` grep 0, `api/routers/` grep 0); `gallery.ts` hard-codes `const liked = true` | ship the 46th operation **or** delete the affordance — **NO THIRD OPTION SHIPS** | fourier API row, or F.W4 (deletion) |
| **G11** ⊙ | moderation producer-or-retire — **THE ADMISSION GATE** | every `db.flags` write repo-wide is a fixture or a `$rename` migration; `FlagRequest` referenced nowhere; no client posts a flag; **~110 of 285 panel lines dead by reachability**; value.js HAS the verb (`POST /:slug/flag`) | producer-or-retire **RULED** | owner → fourier API row / F.W1. ▲ **This ruling PRECEDES F.W1's sizing wherever FR-AFP grades are load-bearing (ruling 5)** |
| **G12** | unsafe GET / counter provenance | `find_one` first, `$inc {views:1}` after, `_public_doc` serialises the **pre-increment** doc; the same verb is the ETag-capture path so publish adds a phantom view and an entity is born at `views: 1` | the read is safe (an explicit verb), **or** v2 states the mutating-GET policy against **RFC 9110 §9.2.1** | fourier API row + F.W4. ▲ **repair tests must NOT assert a re-open increment (FR-GV-24 lock)** |
| **G13** | cache identity ⊇ consumed fields | `extraction_cache_key` is a CLOSED 10-field `json.dumps` literal omitting `ml_threshold`/`ml_detail_threshold`; the handler short-circuits on the hit BEFORE `compute_contours`; `auto` is the shipped default, so the control is live on every fresh workspace | the superset clause stated **verbatim at record case** — the sentence and its ⟨cmd⟩ provenance note live ONCE, at clause **E13**; this gate cites that clause and quotes nothing (R2-1-LAW.1) | fourier API row. Fixture: **`contour_hash` INSTABILITY across an ML-threshold change** (also the SS-13 probe) |
| **G14** | contour provenance | editor-saved contours carry no `extraction_cache_key` (`source="editor"`), so exiting the editor re-runs extraction and the server cache-hits the ORIGINAL — the edit is destroyed with zero settings touched; the sibling panel's Harmonics nudge is the second trigger | an editor-saved contour is a first-class compute input; extraction cannot overwrite a `source="editor"` asset | fourier API row |
| **G15** | image bounds on write | `store_contour_asset` is called with **no `image_bounds`** on the POST path; the param defaults `None` and is written verbatim; backfill is GET-only — content-addressing means the overlay dies exactly when the points CHANGED | derive-on-POST or backfill-on-write is **ruled** | fourier API row |
| **G16** | canonical-geometry provenance — **TRIPWIRE** | 3.7% of shipped `moon.json` points within 1.0 unit of any extracted point; p90 37.6 vs a 1.23-unit spacing; the crescent-alone bbox strictly contains the shipped bbox; the only claimed regenerator imports `order_contours` (no module defines it) and is itself gitignored | the pipeline decision (true source named or artifact re-authored) **WITH** the diagnosed-result-type row (G5c) | F.W6 / build lane. ▲ **Until then: DO-NOT-REGENERATE on `master`; any regeneration attempt revives L-B1 and L-B2/C-2 at BLOCKER** |
| **G17** | boundary-validation evenness | the whole 45-operation client surface lands as ONE unchecked `(await res.json()) as T` (FR-CP-16); AA-47's uneven pair; `save_contour` takes `xs`/`ys` off `dict[str, Any]` with zero cardinality validation; six admin tiles render em-dashes on drift with no console error, no toast, no test | every response class v2 names is validated at the boundary **OR** stated not-validated-and-why — **evenness is the clause, not paranoia** | fourier API row + F.W4. ▲ **Do NOT author a per-field defensive sweep at F.W4 (L-19)** |
| **G18** | casing + envelope isomorphism | v1 §4:140 names TS-camel ↔ Python-snake as the ONLY allowed envelope difference and §6 binds each repo's probe to the doc, not the sibling; fourier's `DiffResponse` conforms — **value.js has NO diff surface to conform**, so the parity verdict is unrunnable (see G4) | v2 restates the casing rule as a **mechanical check on each side**; §6 re-authored; both probes exist **OR** the one-sided verdict is explicit | F.W9/W10 + value.js API row |
| **G19** | **census closure** (M-25 / COHESION §3.1) | **LHS (exists, path-cited) — ONE OPERAND, EACH ID ON EXACTLY ONE SIDE (repair round 3, rulings R3-4.5 + R3-9; PASS-3 D-3/D-4 convicted the former cell, which put `P-9` on both sides of its own LHS and counted `SS-L-07/SS-C-10` twice, two sentences apart)**: **the CANONICAL F.W5 band — `conformance/CENSUS-CANONICAL.md` §2's `F.W5` roster (28) ⊕ `F.W5-W8` roster (90) = 118 record-qualified rows, verbatim** — **and nothing else** (repair round 4, rulings R4-3 · R4-5 · R4-10; PASS-4 D-1/D-2/D-8/D-18 convicted the round-3 operand). Three consequences, stated so the set-difference is computable from this cell as written. **(a) No check file is an operand, AND NEITHER IS THIS WAVE'S OWN ARITHMETIC** — round 2's LHS was pinned to `conformance/PASS-2/F-W5-CHECK.md` §1's landing table, struck under R3-4's standing law (*a closure transcript whose LHS is a count copied from a check file FAILS BY CONSTRUCTION*) and falsified in fact by this spec's own bookings at C4/F9; round 3's replacement — this file's own re-derived `170` — is struck under R4-5 for the same class of failure one level down (arms false at two of four by cancelling ±40; the total assembled as `167 + δ`). **`162`, `167` and `170` are all superseded and none may be quoted as this gate's operand.** The canonical is the operand; §2's detector is a reading instrument only and settles no membership. **(b) `SS-L-07 / SS-C-10` is NOT an LHS member at all** — the canonical homes it at **`F.W4`** (banked head `SS-L-07`, alias `SS-C-10`) with an **`F.W5-W8` leg**, so under R4-6 it books at its holder and this wave cites the leg. Round 2 counted it twice (declared a member at §2c, then added again as a distinct LHS operand); round 3 cured the double-count by ruling it a single member; **round 4 removes the question by naming its holder.** Its `F-W4-CARRY.md` §Rows D anchor is a **corroborating citation, never an operand**. **(c) `P-9` is on the RHS side ONLY and is not an LHS operand at all** — it was formerly admitted to the LHS as a carry-row operand **and excluded from the LHS** in the same cell. It is a **carry-ledger id occurring in ZERO of the 66 records** (⟨cmd⟩ `grep -lw 'P-9' fr-*.md \| wc -l` → **0**, with the positive control at §2 item 2 — the escaped `\|` is this file's table-pipe convention, as everywhere else), so it cannot be a member of a corpus-derived roster; the `F-W4-CARRY.md` §0 `CORRECTED DENOMINATORS` bullet is cited **by stable anchor** (R2-2.2 — never by line into a live carry) as the home of its zero-cell export, on the RHS side, exactly as §5 states. ▲ **DETECTOR STATED INLINE (R2-9 — a gate that states a shape- or spelling-restricted operand is DEFECTIVE AT AUTHORING, whatever its result)**: the operand is **§2's ROSTER DETECTOR**, i.e. (i) **SHAPES** = table rows ⊕ id-headed route-marked bullets (`→` · `**ADJUDICATED →**` · `⇒` · `rides` · `routed` · bold-at-disposition) ⊕ prose routings walked back to their enclosing row id (S-8) ⊕ bare-first-cell table rows — **no shape in which a routing does not count**; (ii) **SPELLINGS** = `{F.W5, F.W5-W8, F.W5–W8}` in **BOTH** U+002D and U+2013, every span/slash form expanded to its member set; (iii) **MATCHING** = **token-bounded, never bare-substring**, and the id side record-qualified — spelled portably as `grep -w`, or as the `-E` alternation `grep -E 'F\.W5([^0-9]\|$)'` (the escaped `\|` is this file's table-pipe convention, as everywhere else). ▲ *Portability note (repair round 4, ruling R4-2.1)*: this axis formerly published the boundary as the PCRE lookahead `F\.W5(?![0-9])`. **PCRE does not exist on the pinned toolchain** — BSD `/usr/bin/grep` exits 2 on `-P`, and a lookahead in an `-E` pattern matches literally — so the spelling named an instrument no seat on this host can run. The requirement is unchanged; only its spelling is now executable. ▲ **AND THE DETECTOR IS DEMOTED HERE TOO (round 4): it reads record bytes, it does not constitute the roster** — the roster is §2's canonical operand above, and a gate that re-cuts a detector to produce its own LHS is the exact oscillation the census of record was stamped to end. **`162`, `167` and `170` are superseded figures and none may be quoted as this gate's operand; nor may any check-file table, pass index, or §-arithmetic of this file stand in for the canonical.** **RHS (does not exist)**: an AUTHORED v2 + register + ruling block — `ls $V/docs/tranches/X/fourier/contract/` → *No such file or directory*, re-verified read-only 2026-08-29. **Born-RED because the RHS is empty, not because the LHS is missing.** §2c's per-record landing table is an authoring self-report, never the gate | set-difference **∅ in BOTH directions**, run on the **record-qualified** id form (R-5/U-12), against §2's clause tables + §5's carried-by-citation table. **SEVEN ids are excluded from the LHS, each with the receipt §2c carries and each with its holder NAMED (repair round 4; PASS-4 D-13 corrected the side-word).** The former cell said all six were *"asserted on the RHS side ONLY"* — **false of three of them**: this gate's RHS is defined two sentences above as *an AUTHORED v2 + register + ruling block*, and `AA-45`/`AA-46`/`AA-47` are asserted on **neither** side of this gate. Their one home is `F-W10.md` §2.5. **Exclusion is not a side.** The seven, with the side or holder each actually sits on: **`AA-48`** — canonical **UNROUTED**, cited at A1 and §5, RHS side · **`P-9`** — a carry-ledger id in **zero** records, cited at §5 against the `F-W4-CARRY.md` §0 `CORRECTED DENOMINATORS` bullet **by stable anchor** (R2-2.2, never by line into a live carry), RHS side · **`AA-44`** — canonical **F.W9**, held there, cited at §5 and the §4 F.W9/W10 edge · **`SS-L-07 / SS-C-10`** — canonical **F.W4**, held there, its `F.W5-W8` leg cited at §5 · **`AA-45` · `AA-46` · `AA-47`** — canonical **NWO→SS-3** / **NWO (packet)** ×2, held at `F-W10.md` §2.5's drain, carrying no `F.W5` token, **cited at A1/A2/A3 and asserted on NO side of this gate** (R3-6.1). Folding any of the seven into the LHS is the arithmetic pass 2 convicted; booking any of the three drain rows is the one-home breach pass 3 convicted; **calling a citation a side is the smaller error pass 4 convicted, and it is corrected here rather than carried.** A row appearing in neither is a **SILENT DROP** — the defect class this program exists to kill. ▲ **No `F-W5-CARRY.md` may be authored to green this gate (R-3.2)** | F.W5 itself (unit e, closing) |
| **G20** | co-signature actually relayed | `COHESION.md` **§2 The cross-sub-tranche dependency graph** (stable anchor, never by line), quoted at its own bytes — ⟨cmd⟩ `grep -n -F 'Cross-repo edges are declared FROM BOTH ENDS' docs/tranches/X/COHESION.md` (this seat, read-only 2026-08-28) → one hit: *"Cross-repo edges are declared FROM BOTH ENDS in the spec files (the X·V W4-D2 lesson, now law)."* (the former down-cased opening and the dropped provenance clause are struck — D-6 class) — today `J-diff-shape-v2.md` does not exist and no letter has been sent (`ls $V/docs/tranches/X/coordination/` → one file, the atlas rulings relay, verified 2026-08-28); **a contract nobody received is not co-signed** | the relay letter exists at `$V/docs/tranches/X/coordination/`, is ledgered in `INBOX.md` (E13), and this spec declares this end of every cross-edge (§4) | F.W5 itself (unit e) + the fourier side's reciprocal |
| **G21** | the operation register is a FILE, not prose | no `operation-register.md` exists (`ls $V/docs/tranches/X/fourier/contract/` → *No such file or directory*); G8/G9/G19 are ungreppable without it, and the 45/30/13 triple has already been mis-stated once (census conflict C-3 — *"both sides were right on different denominators"*) | `operation-register.md` enumerates **45 = 30 + 13 + 1 + 1**, one row per operation, authority class (R3-7b) + client-edge disposition (R3-7c) | F.W5 itself (unit a) |
| **G22** | dangling-cite resolution (registry integrity) | GCM-10's cure cites a companion row **"MF-9"** that resolves to NOTHING in the adjudicated registry — `grep -rn "MF-9" $V/docs/tranches/V/megatranche/registry/adjudicated/` → exactly ONE hit, the dangling citation inside `fr-GalleryCardModal.md:56` itself (re-measured 2026-08-28); second item: fr-EasingPicker booked its easing-domain server half as "new" when RESOLVER already carried the same cure (merged at E11, both ids preserved) | **MF-9 resolved or STRUCK before v2 quotes GCM-10's cure** (the strike is the third-iteration halt in §1c); the E11 merge is recorded in v2's clause provenance | F.W5 itself (unit e) |

---

## 4. Cross-edges — declared from THIS end (one home, two citations)

F.W5 declares its half of every edge below; the reciprocal is requested via the root-authored COHESION spine, never written here (§1b). Sequencing locks land verbatim where their rows land.

| edge | direction | content |
|---|---|---|
| **X-1 · F.W0 → F.W5** | HARD opens-after | Substrate pre-gates FIRST. **The corrupt-dist fact carries BOTH its banked witnesses — canonical form: `FR-NP-32` (≡ `fr-PaperSidebar M1`)** (R-8; banked ids are original for life, the cure is cite-both, never substitute). **FR-NP-32** (`fr-NotationPills.md:35`, **BLOCKER**, *"NO-WAVE-OWNER (producer dist emitter) + glass-ui BH relay at the TOP of the FR-COB-28 packet; F.W1 SEQUENCING GATE"*) is the stronger reproduction — *"`vite dev` and `vite build` are RED at the `^4.0.0` pin"*, reproduced with the app's own toolchain; **fr-PaperSidebar M1** is the parse census of the same emitter defect. **Both travel; neither is dropped for the other.** Also: the fourier worktree is dirty — uncommitted bytes no commit holds (fr-GalleryAdminBanner **GAB-13**); F8-REACH-01/02. ▲ **G-11/G-12 QUOTATION (R-9.1) — RE-ANCHORED TO THE STABLE IDIOM (R2-2.1: a line number into a live sibling is a defect from that ruling forward)**: this spec's anchor and denominator cells are **input to / quotation of F-W0 §4's gate `G-11` (the corrected ANCHOR table) and F-W0 §4's gate `G-12` (the corrected DENOMINATOR table), both published in the `SUBSTRATE-LEDGER.md` artefact chartered at F-W0 §2a's file bounds** — cited by **gate id and §-anchor, never by line**. ⟨cmd⟩ `grep '^### G-1[12]' F-W0.md` (this seat, read-only 2026-08-28) → *"### G-11 — ONE corrected anchor table published; every later wave quotes it"* · *"### G-12 — ONE corrected-denominator table published; superseded figures FORBIDDEN downstream"*. ⟨cmd⟩ `grep -n 'SUBSTRATE-LEDGER' F-W0.md` → its **create** row, reading *"`fourier/docs/tranches/F/SUBSTRATE-LEDGER.md` — **create** — the wave's single durable artefact: the 28-path minute · the STEP-2i divergence minute · the G-11 anchor table · the G-12 denominator table · the G-13 pin table + lattice …"* (the cell separators of the source row are rendered as dashes here so this table survives; no word is changed and the `…` marks elision). That **create** row's true home is **F-W0 §2a File bounds** — an address correction made by measurement at this end. ▲ **ERRATUM, minuted — AND ITS OWN RECEIPT NOW STRUCK RATHER THAN REFRESHED A FOURTH TIME (repair round 3, ruling R3-10 row 2 ⊕ the R3-3.10 standing amendment; PASS-3 D-12).** This cell formerly carried R-9.1's three line coordinates unmeasured, and then — to prove they addressed nothing — **pasted `sed -n '63p;199p;204p' F-W0.md`'s output as evidence.** That receipt is **a line-number probe into a live sibling, banked as a gate-adjacent fact**: the class R3-3.10 abolishes outright (*no ⟨cmd⟩ whose output is a count of, or a coordinate into, a live sibling may be banked in a spec*). It behaved exactly as the law predicts — the pasted return has now drifted **three times** across three rounds (pass 2 read a blank line and an *"18, 17, 19"* owning-rows cell; round 3's paste read a bounds-table header and two *"Owning rows"* cells; a re-run today against F-W0's later mtime returns three different lines again). **A receipt that must be re-pasted every round to stay true is the disproof of its own idiom, not evidence.** It is therefore replaced by the CLASSIFICATION it was only ever used to establish, which no edit to F-W0 can stale: **the three R-9.1 coordinates named no authority at any of their addresses, and no coordinate is carried here in their place.** Gate ids and §-anchors do not move; `G-11` and `G-12` above are cited as identities and are the only addresses this cell asserts. Divergence from those two tables is a defect against G-11, not a rival act — F.W5 re-performs no re-resolution as its own act. Until F.W0 re-grounds, **every fourier-side anchor in this spec is MEASURE-AT-OPEN (D-19)** — GAB-12's "LIVE at certified HEAD" cell was measured on uncommitted bytes and is not HEAD truth; fr-BasisSelector M-9's and other line anchors re-resolve after F.W0 |
| **F.W5 → F.W1** | emits + sequencing | FR-AFP-18's render seat (metric-badge→metric); FR-AFP-33's client collapse. ▲ **G11's ruling PRECEDES F.W1's sizing** wherever FR-AFP grades are load-bearing (ruling 5). ▲ F.W1 is ONE atomic land-or-lose transaction — **the TWELVE-limb roster chartered at `F-W1.md` §4 step 4 — cited whole, never restated here** (R2-5; stable anchor, no line into a live sibling). **Re-quoted AFTER F-W1's own R2-5 edit landed, never re-derived at this end**: ⟨cmd⟩ `grep -n "The roster is" F-W1.md` (this seat, read-only 2026-08-28) returns one hit, whose bytes read *"The roster is TWELVE limbs and stays twelve — the eleven chartered plus the FR-EQC-7 vaul-vue manifest gate returned INSIDE at repair round 1 per the `:294` pin (R-4a, cross-edge 1 at this writing) — it is the extent every sibling spec cites (R-4b); nothing is added to it by a later reading."* (emphasis is the charter's; the `:294` pin is **F-W1's own intra-file coordinate carried inside the quotation**, marked *at this writing* by its author — it is not adopted as a citation of this file). ▲ **The former "ELEVEN-limb" spelling here is STRUCK**: this wave states the extent only by citation, and the count word it cites is now TWELVE. (The four zero-console-error e2e gates are its failure semantics — fr-EquationResult FR-EQR-3.) **F.W1 must NOT be credited for the pagination-drain cure (FR-GIG-5)**. **F.W1 precedes F.W3/W4 (§2 law)** |
| **F.W5 → F.W2** | exports zero-cells (P-9) | The measured negative roster copies into F.W2's denominator: App API-inert · MobileFloatingToc · CollapsibleSection · PaperSearch/PaperSearchInput/**`fr-PaperSearchDropdown C:C-23 / C:C-24 / L:§5`** · MorphShapePreview (structurally unreachable) · CanvasControlsDock/AnimationControls zero migration cost · **UserSlugBar FR-USB-38 — do NOT book into the F.W2 migration surface**. One home (F.W4), two citations; evidence never a denominator. ▲ **THE PaperSearchDropdown ROW IS NOW ID-KEYED, CITING ITS ADOPTING WAVE (repair round 3, ruling R3-2.5; PASS-3 D-17).** It formerly landed **by component name only**, and an id-keyed set-difference — which is the form G19 states — cannot resolve a component name. The record's banked row, ⟨cmd⟩ `sed -n '77p' fr-PaperSearchDropdown.md` (this seat, 2026-08-28): *"**C:C-23 / C:C-24 / L:§5 — INFO · FOLDS into banked fr-PaperSearch C-Z1/C-Z2.** Load-bearing zeros: sole producer import `glass-ui/button`; zero value.js/keyframes/canvas/WebGL/API contact; data via the build-time virtual module. **F.W5-W8 = no rows.**"* — **the record itself rules F.W5-W8 a zero-cell**, which is exactly why this wave exports rather than books it. **The ADOPTING WAVE IS F.W2, and the citation is of that wave, by id** (R-2c assigned `C:C-23`/`C-24` there; F.W2 carries them at its §7 exclusion list and its roster). **F.W5 books none of the three ids**; naming them here is what makes the export findable by an id-keyed difference at both ends |
| **F.W5 → F.W3/W4** | emits display/render arms | the `:key` deletion A1's mechanism enables — **cited, never dispatched from here: `AA-45`'s one home is F-W10 §2.5's drain (R3-6.1) and this edge books nothing on it** (AA-48 discharges with AA-11) · AA-6 placeholder honesty · AA-10 actor legend · AA-5/AA-24 display taxonomy · GCM-10's `v-else-if` guard · GAB-15/-17 label honesty (GAB-17 AFTER GAB-16) · FR-AUL-21's visible unknown-status render · FR-AUL-25/FR-AFP-9 truthful copy (diction rides FR-AFP-35) · FR-AFP-8's one-token `item.slug` label · the like-verb `aria-pressed` + re-click guard · FR-AFP-32's shared formatter (**banked FR-AUL-17 → F.W3, do NOT re-book**) · B-1's unit-true control · M-10's typed vocabulary · ContourPreview row 11's `?:` (NOT presuming row 13's seam shape) · FR-GFC-1's in-place patch (D-1 skeleton KILLED, K5) · FR-EQR-4's `plainLatex()` **iff F.W5 declines the wire field** · FR-USB-13's attribute rework (not credited to the contract). F.W4-side same-commit riders named **by their BANKED ids** so emission does not strip them and G19's id-keyed set-difference can find them (anti-rename; pre-adjudication axis ids collide across records and do not survive it): **fr-PaperSearchModal PSM-1** (`:34`, = D-1 · L-1 · C-1) ⊕ **PSM-4** (`:37`, = D-3 — *"Must land in the same wave as PSM-1 or the cure ships a WCAG regression"*) ⊕ **PSM-13** (`:49` — the `outline:none` 2.4.7 rider, *"MANDATORY rider inside the PSM-1 commit"*; the record's own verdict at `:140` names the pair as *"(PSM-4, PSM-13)"*); PAW-44/LAW-3 restore-only-with-background-cure; MPC-31/MPC-3⊕10⊕13⊕8⊕22 one-cut law; FR-MSP-6 two-channel lock |
| **F.W5 → F.W6** | emits burn-down | F-α's deepen-or-retire execution (G3); the G16 canonical-geometry pipeline WITH G5c's diagnosed result type; **DO-NOT-REGENERATE stands until F.W6 lands the pipeline** (tripwire: L-B1 + L-B2/C-2 revive at BLOCKER) |
| **F.W5 → F.W7** | states, never books | **F.W7 CITES; F.W5 STATES.** The trie question is this wave's clause **E16** and gate **G7 ⊙** — F.W7 owns the owner-gated design that follows a ruling and **books no registry row of its own** (its routing census is a measured **∅**, R-11). The dissent travels with the clause and is banked HERE, at **`F-W5.md` §2 clause `E16` ▲** ≡ **`F-W10.md` §2.3, the `SS-4-PREREQ` row** — cited by **clause id at this end and by §-anchor + row label at the other, with NO line number at either end** (R2-2.1; the round-1 `:114` and this file's own round-2 `:116` re-measurement are both **abolished as citations**, not merely corrected — a coordinate that has to be re-measured every round was never an address). The banked bytes, ⟨cmd⟩ `grep -n 'THE GUARDRAIL IS THE INCUMBENT' F-W10.md` (this seat, read-only 2026-08-28) → one hit, reading: *"Dissent RECORDED: the commission's trie-compression collides head-on with the standing anti-tree KISS guardrail in BOTH trees (`atomdiff.py:12-14`) — THE GUARDRAIL IS THE INCUMBENT; an owner ruling precedes design"*. ▲ **R2-1-LAW CURE, stated against this file's own former hand**: the sentence this cell previously carried under a *"verbatim"* label (*"the trie requirement collides head-on with a standing KISS guardrail present in BOTH trees … the owner ruling PRECEDES design"*) was a **paraphrase of those bytes, typed from the shape of the ruling rather than read from the sibling** — the convicted class. It is STRUCK; the pasted bytes above, with their ⟨cmd⟩ note, are the only quotation of the dissent this file makes. Absent a ruling, the honest default both waves carry is identical and stated once here: **no trie; whole-snapshot duplication is the recorded shipped behaviour.** ▲ **ANCHOR-IDIOM DISCLOSURE (E-3 / R2-2.1), re-keyed off line numbers at BOTH ends**: F-W7's founding pair anchors this spec's E16 and G7 **by line into a live sibling**, and this file's repairs have moved both **twice**. The citation owed at that end is **clause id `E16` ▲ and gate id `G7` ⊙** — the identities, which do not move. This end states the identities and declines to publish a coordinate for them; a future drift table is not the cure, the idiom is. Declared from this end so the keystone's consumer is not undeclared. Reciprocal requested: `F-W7.md` |
| **F.W5 → F.W8** | the RECIPROCAL, declared from this end | F.W5 STATES the clauses; F.W8 ASSERTS them (fixture/e2e direction). ONE home (F.W5), two citations — the shared ids are enumerated at `F-W8.md` §1's census. No F.W8 row re-books a clause; no F.W5 clause claims F.W8's assertion work. Reciprocal: `F-W8.md` §5c. ▲ **Set-difference precondition (R-5)**: the shared ids run in the **record-qualified** form on both ends (`fr-BasisCanvas C-17`, `fr-EquationView B-1/B-2`, `fr-ContourEditorCanvas C-2`, `fr-FunctionInput L-B1/L-M3`, …) — the U-12 eight (**M-13 · L-B1 · L-M3 · C-17 · C-18 · B-1 · B-2 · C-2**) are ambiguous bare and cannot be differenced. F.W8's three required artifacts are §1a's `create` rows, cited **WHEN-created** |
| **F.W5 → F.W9/W10** | emits conformance obligations | v1 §6 re-authored by v2 and **executed there**, each side's probe asserting against the DOC, never the sibling (inv-26); **FR-AUL-13's anti-repeat probe** (a contract document without a probe repeats the defect it convicts); **AA-44**'s one-sided test seam (zero frontend tests; **EIGHT** e2e specs never touch the audit tab); GAB-12's test seam; FR-AFP-66's orphan-flag coverage (the existing test exercises only the reporter-keyed path); G18's mechanical casing checks |
| **F.W5 → SS-4** | is its input | SS-4 **FLAGS THE OWNER RULINGS INLINE** (R1–R9, §2a: TA-4 restore-or-rescope · trie-vs-KISS · like verb · flags producer-or-retire · off-state `[]` · hard-delete arm · codegen-vs-twins · remix/fork+born-visibility · dead session subsystem) and **NAMES THE TA-4 VALUE-SIDE ATOMDIFF RESTORATION AS PREREQUISITE OR RE-SCOPES EXPLICITLY** — the charter law is quoted at its own bytes **ONCE in this file, with its ⟨cmd⟩ note, at clause E3**, and cited here by stable anchor (`COHESION.md` §2, the `SS-4 contract co-signature → F.W5 ∥ X·V API row` bullet); this cell quotes nothing and states the obligation in F.W5's voice (R2-1-LAW.1: one quotation, one command, one home). GIG's orphan-client class rides here as census methodology (NO-WAVE-OWNER → SS-3/SS-4) |
| **F.W5 → SS-6 (BH relay)** | producer routing | **Glass-producer rows go to the BH relay, NEVER become frontend hacks** — any producer-side finding surfacing during contract authoring is relayed to the active glass-ui BH inbox, not specified as a demo-side workaround |
| **F.W5 → SS-13** | probe ledger | Deployment auth-proxy (C2) · JSC acceptance of the non-ECMA date form (D17) · the HTTP/2 transport arm (F5; **the static arm needs no probe**) · FR-AUL-14 production incidence (D13) · L-M2 magnitude (G8c) · M-14's end-to-end arm (E14) · FR-GSB-28 symptom magnitude (B1) · D11 magnitudes · E6 observable magnitude · `contour_hash` instability fixture (E13). ▲ **K12: SS-13 spends NO probe on E5 — closed statically** |
| **F.W5 → value.js API row** | value-side obligations, never fourier defects | V-β compound version `_id` + the two-histories test (G2) · V-γ attribution (`userSlug` null ⇒ no version row, E18) · TA-4 per G4's ruling · fork born-visibility (`forks.ts:76`) + create-visibility test (G5) · G18's value-side probe · D9 reconciliation: value.js persists 3-state visibility (`public`/`unlisted`/`private`, `model.ts:61`) — the obligation list reconciles it, the contract does not silently overwrite it |
| **FORBIDDEN** | — | **Direct parse-that→fourier routing is FORBIDDEN** — any parse-that-derived material reaches fourier only through value.js-authored specs and letters |

---

## 5. Carried by citation — the excluded-from-clause-tables set (G19's other operand)

Four rows are carried **by citation with their homes named**, not homed in a §2 clause. They appear here so G19's set-difference finds them; none is a silent drop. Every source coordinate below `ls`-resolves and was re-read at the bytes 2026-08-28.

▲ **THE §2c/§5 PARTITION, STATED IDENTICALLY AT BOTH ENDS (D-4 cure).** ▲ **ALL FOUR rows below are now OUTSIDE the LHS, and each names its holder (repair round 4, rulings R4-5 + R4-6; supersedes the round-3 three-out/one-in split).** `AA-48` → canonical **UNROUTED** · `P-9` → a carry-ledger id occurring in **zero** of the 66 records, so no canonical row exists for it at all · `AA-44` → canonical **F.W9**, held there · **`SS-L-07 / SS-C-10` → canonical `F.W4`** with an `F.W5-W8` leg, held there. Round 3 ruled the last of these *"the ONE §5 row that IS a member"*, which was the best reading available against a wave-derived roster; the census of record names its holder instead, which is better. The former tally folded P-9 and AA-44 into a denominator this section simultaneously said they sat outside — two sections asserting opposite things about the same ids. **Neither section may state that partition alone again**; the receipts live at §2c and are cited, not restated, here.

| id | disposition | home |
|---|---|---|
| **AA-48** (`fr-AdminAuditLog.md:91`) | Recorded at A1 (same latent class as AA-45 `:88` — raw `<button>`s with no `type`, inert today), **NOT re-booked**; discharges FREE with AA-11's footer cure | F.W4 |
| **P-9** — `carry/F-W4-CARRY.md` **§0, the `CORRECTED DENOMINATORS` bullet** (stable anchor; a line number into a live carry is a defect, R2-2.2) | The negative-control law: zero-cells are **EVIDENCE, never a denominator** (B5); F.W5 EXPORTS the zero-cell roster into F.W2's and F.W4's denominators — one home, two citations. Banked bytes, ⟨cmd⟩ `grep -n -F 'Zero-cells are EXPORTED from F.W5' F-W4-CARRY.md` (this seat, 2026-08-28) → one hit: *"**Zero-cells are EXPORTED from F.W5 (P-9), copied into F.W2's and F.W4's denominators, one home two citations**: App API-inert · MobileFloatingToc · CollapsibleSection · PaperSearch/Input/Dropdown · MorphShapePreview (structurally unreachable) · CanvasControlsDock/AnimationControls zero migration cost · **UserSlugBar FR-USB-38 — do NOT book into the F.W2 migration surface.**"* **A carry-ledger id, not a registry id** — ⟨cmd⟩ `grep -lw 'P-9' fr-*.md \| wc -l` (the portable spelling ruled at §2 item 2 — the former bracket-class ERE returns **0 for every token** under a `ugrep` shim and so proved nothing; this one carries its positive control) → **0** of the 66 records, which is why it is stated **OUTSIDE** the canonical F.W5 band (§2c's partition), never folded into it — and the census of record, which censuses only banked heads in those records, holds zero `P-9` rows for the same reason. §2c and this row say the same thing | F.W4 (body) + F.W2 (copy) |
| **SS-L-07 / SS-C-10** — `fr-SpeedSelect.md:59` (frozen corpus, line-citable per R2-2.2(i)) ≡ `carry/F-W4-CARRY.md` **§Rows D — the transport cluster — its `SS-L-07/SS-C-10` bullet** (stable anchor, never by line) | Banked routing, quoted from the record's own terminal cell — ⟨cmd⟩ `sed -n '59p' fr-SpeedSelect.md \| rev \| cut -c1-120 \| rev` (this seat, 2026-08-29, `$R` base), **whole 120-character return, no narrowing**: *"L-11+M-10 empty-trigger state unrepresentable at the type level. \| **F.W4** (+ its type reaches the F.W5-W8 wire rows) \|"*. ▲ *(R4-7.1; PASS-4 D-9: this site and §2c both printed under half of it beneath the word "exactly" — the label means bytes, so the bytes are here.)* **The canonical homes the row at `F.W4`** (banked head `SS-L-07`, alias `SS-C-10`, leg `F.W5-W8`), so it is **held at F.W4 and cited here**, not a member of this wave's 118 (§2c's partition) — carried by citation is a landing, not a booking. The **body is F.W4's** — the literal-union speed catalog (`lib/speeds.ts` / `SpeedName`, the `EASING_OPTIONS`/`AnimationEasingName` idiom one file away). **F.W5's share is the type-reach only**: the speed domain arrives at the wire as `speed: number` on both sides, so it is admitted to the operation record under **D12**'s closed-domain law and **E11**'s hoist form (`Literal[…]` on `AnimationSettings.easing` — the same treatment, the sibling field). Booked by citation, not re-booked: the record's own cure lands at F.W4 and **F.W5 claims no credit** (FR-GIG-5 bar). Named here by both ids so G19's id-keyed difference resolves them | F.W4 (body) + F.W5 (type-reach, by citation) |
| **AA-44** | The one-sided test seam: zero frontend tests; **EIGHT** e2e specs never exercise the audit tab; the backend pins its half — why AA-5/AA-6/AA-24 could open and stay open. F.W5 emits the conformance obligation only (§4 F.W9/W10 edge). ▲ **THE SUPERSEDED DENOMINATOR IS STRUCK FROM F.W5's OWN VOICE, BOTH SITES (repair round 3, R3-3.9 purge share; PASS-3 D-5).** This row and the §4 F.W9/W10 edge formerly restated the record's *"nine e2e specs"* **unquoted and unqualified — F.W5 asserting a figure two sibling waves have jointly corrected.** ⟨cmd⟩ `grep -o 'Spec count re-measured at fold: [0-9]*' F-W0.md` (this seat, 2026-08-28) → **`Spec count re-measured at fold: 8`**; `F-W9.md` §2.8 **E-1** and `F-W10.md` §2.8 **E-5** both declare the paired 9→8 correction, E-5 re-measuring at fourier HEAD (`ls web/e2e/*.spec.ts` → **8**) and both quoting the standing rule ***"Quote 8 specs, never 9 (K11)"***. This wave's own X-1 cell rules that divergence from F-W0's tables is a defect against **G-11**, and **G-12**'s heading is *"superseded figures FORBIDDEN downstream"* — so the two live divergences were this file convicting itself. **The `9` survives here ONLY inside the quoted banked bytes of AA-44 at §2c, which are the record's and are not this wave's to edit** | F.W9/W10 |

---

## 6. Close (unit e checklist — all `planned`)

1. G19: run the set-difference both directions — **the CANONICAL F.W5 band (`conformance/CENSUS-CANONICAL.md` §2: `F.W5` 28 ⊕ `F.W5-W8` 90 = 118 rows), verbatim — that roster ALONE, run once per id** ↔ §2's clause tables + §4's cited cross-edges + §5 — against the AUTHORED v2/register/rulings, on the **record-qualified** id form; ∅ or halt (third failure → triumvirate). ▲ **THE OPERAND IS RE-BASED FOR THE LAST TIME (repair round 4, rulings R4-3 · R4-5 · R4-10).** This item has now cited three different rosters in three rounds — a check-file table, then this file's own re-derivation — and each was struck for the same reason: **the wave was supplying its own witness.** It supplies none now; §2c's per-record landing table is the *answer sheet*, the canonical is the *question*, and the two are authored by different seats. ▲ **THIS ITEM CARRIED G19's OWN CONTRADICTION AND IS RE-STATED WITH IT DISSOLVED (repair round 3, ruling R3-4.5; PASS-3 D-3 named the identical shape in both places).** It formerly added *"⊕ the two `F-W4-CARRY.md` citations"* as extra LHS operands: that **double-counts `SS-L-07/SS-C-10`**, which is already a member of the roster (§2c), and **admits `P-9` to a side it is simultaneously excluded from**. The two carry anchors are **corroborating citations on the RHS side, never LHS operands** — G19's cell states the partition once and this checklist now cites it rather than restating it. ▲ **No check file is an operand** (the round-2 pin to `PASS-2/F-W5-CHECK.md` §1 is struck under R3-4's standing law), **the superseded `162`, `167` and `170` are NOT admissible operands, and no line number into a live carry may be used as an address (R2-2.2).** **No `F-W5-CARRY.md` is authored (R-3.2).**
2. G22: resolve or STRIKE MF-9 before v2 quotes GCM-10's cure; record the E11 merge in v2's provenance.
3. G20: write the relay letter, append the E13 INBOX row, request the fourier reciprocal.
4. Commit discipline: pathspec commits only; `scripts/dev/dev.sh` never staged; each unit commits before the next opens.
5. No wave closes with UNREAD mail (E13 4-path sweep at open and close).

**Status: planned.** The specs await the owner's begin-word.
