# X.F.W5 — The shared-provenance API contract (ADMISSION KEYSTONE)

**Sub-tranche**: X·F (`docs/tranches/X/fourier/waves/`), authority `docs/tranches/X/COHESION.md` §0/§1 SS-4. Validated against `docs/precepts/instructions/tranche/WAVE_SPEC.md` (L-20, read at authoring 2026-08-28).

**Fold provenance (M-23 / M-25 / M-12)**: this file is the FRESH-FABLE FOLD output, 2026-08-28 — the Opus author half (Draft A) arbitrated against the adjudicated registry itself.

▲ **PROVENANCE AUTHORITY (R-3, `docs/tranches/X/fourier/conformance/PASS-1/RULINGS.md`): there is no `F-W5-CARRY.md`, and none is authored here.** The only carries in tree are `docs/tranches/X/fourier/carry/F-W1-CARRY.md` (F.W1) and `docs/tranches/X/fourier/carry/F-W4-CARRY.md` (F.W4), citable **by path only**. This wave's operand is therefore the **66-record adjudicated registry** at `docs/tranches/V/megatranche/registry/adjudicated/fr-*.md`, rostered by the id-keyed census at `docs/tranches/X/fourier/conformance/PASS-1/F-W5-CHECK.md` §1 — **routedTotal = 162 id-keyed routings** whose terminal disposition names `F.W5` / `F.W5-W8` / `F.W5–W8`. Two tail rows rest on paths that exist and were re-read at the bytes: `docs/tranches/V/megatranche/registry/adjudicated/fr-FourierShapeExtractor.md:81-87` · `…/fr-AdminAuditLog.md:87-91` · `docs/tranches/X/fourier/carry/F-W4-CARRY.md:18` (P-9) and `:234` (SS-L-07 / SS-C-10).

*Historical session note, no gate weight*: earlier folds self-reported a 112-of-126 and then a 128-row receipt. Both are **authoring self-reports over an artefact that does not exist**; neither is G19's operand and neither is cited anywhere below. Per M-25, a spec ignoring adjudicated rows is DEFECTIVE and transcription-only is DEFECTIVE — this file agglomerates: dedupes by identity, carries cure-shape locks and sequencing edicts verbatim, routes by mechanism.

**EXECUTION IS NOT AUTHORIZED BY THIS FILE.** Tranche X's execution gate stands: nothing **writes, builds, or runs** product source in either repo until the owner's begin-word. **READ-ONLY measurement is the only way a born-RED witness exists at all** — §3's value-side witnesses (G2, G4, G5's value leg) were so obtained, 2026-08-28, and are pasted with their commands; no witness in this file opens a shell that mutates either tree. `$F` = `/Users/mkbabb/Programming/fourier-analysis` is **READ-ONLY, always** — this wave writes zero fourier bytes and every fourier-side witness is MEASURE-AT-OPEN (D-19); the co-signature travels as a letter. `$V` = `/Users/mkbabb/Programming/value.js`. Specifying is not doing.

**Status**: **planned**. (Every status field in this file stays `planned` until the begin-word.)

---

## 0. State

**Name**: F.W5 — the shared-provenance API contract (ADMISSION KEYSTONE)
**Opens after**: **F.W0** (substrate pre-gates — HARD, §4 X-1) · the **intake adjudication** (`docs/tranches/V/megatranche/audit/codex-provenance/INTAKE-ADJUDICATION-2026-08-03.md` §3 F.W5 + §2 C-3 — an adjudication record, **not a carry ledger**, per R-3) · the adjudicated fourier registry whole (66/66)
**Agents**: 5 **serial** — units b/c/d all write `J-diff-shape-v2.md`; §1c forces sequencing, never parallelism
**Hard gate**: 22 conditions in §3, **all born-RED**, each with a named witness. Four are ⊙ OWNER-GATED and may not be authored unruled (G4, G7, G10, G11).

| verb | value | evidence |
|---|---|---|
| AUDITED | **YES** | the adjudicated band at `docs/tranches/V/megatranche/registry/adjudicated/fr-*.md` (this wave's 66-record witness set) · `docs/tranches/V/megatranche/formation/fourier/lane-crud.md` (R-1..R-7, §6, §7, F-α/β/γ, V-α/β/γ) · `docs/tranches/V/megatranche/audit/codex-provenance/INTAKE-ADJUDICATION-2026-08-03.md` · `…/codex-provenance/api-gap-remeasure.md` — **all four `ls`-resolve; unrooted spellings are forbidden vocabulary (R-10.5's law, adopted wave-locally)** |
| SPECIFIED | **YES — by this fold, 2026-08-28** | Draft A (Opus half) + fresh-Fable fold arbitration = this file (M-12 tri-fold); L-20 conformance closes at the X·F sub-tranche loop |
| IMPLEMENTED | **NO** | contract bytes land only after the begin-word |
| VERIFIED | **NO** | stamped only at X·F's sub-tranche release close |

### 0a. Goal criterion

The wave succeeds if the shared-provenance contract becomes **a neutral document both repos co-sign**, rather than a fourier-tree file value.js binds to by reference — and if every clause in it is *ruled*, *declined-with-reason*, or *⊙ owner-gated and named as such*, so no later wave repairs a defect this contract silently reproduced. **A clause that names no witness is not a clause.**

### 0b. What this wave is NOT

It does not repair. It authors three documents and one letter. Every render arm, client repair and server cure it names is handed onward with **one home and two citations** (§4). F.W5 claims credit for none of them — FR-GIG-5's lesson adopted as a standing bar. **FR-AUL-13 is this wave's own indictment: a contract DOCUMENT WITHOUT A PROBE repeats the defect it convicts** — hence G18/G20 and the F.W9/W10 edge.

---

## 1. Bounds

### 1a. Files owned — six paths, zero additions (five documents to **create**, one append-only ledger row)

| File | Access |
|---|---|
| `$V/docs/tranches/X/fourier/waves/F-W5.md` | create (this spec — the arbitrated fold) |
| `$V/docs/tranches/X/fourier/contract/J-diff-shape-v2.md` | create — **the neutral co-signed contract** |
| `$V/docs/tranches/X/fourier/contract/operation-register.md` | create — 45 rows, authority class + client-edge disposition. **G8/G9/G19 grep this file; without it they are prose** (G21) |
| `$V/docs/tranches/X/fourier/contract/OWNER-RULINGS-F.W5.md` | create — the inline ruling block SS-4 requires |
| `$V/docs/tranches/X/coordination/value-to-fourier-cosign-J-diff-shape-v2.md` | create — the relay letter; the only vehicle by which fourier receives v2 (G20) |
| `$V/docs/tranches/V/coordination/INBOX.md` | **append-only** — one E13 ledger row for the relay; no rewrite of any existing row |

Reconciliation note: the five content paths plus the INBOX row are exactly the six above, and the set is **derived from this spec's own gate needs** (G8/G9/G19/G20/G21/G22), not inherited from any carry ledger (R-3). `operation-register.md` and the relay letter are *load-bearing for gates*, not scope creep — G8/G9 are ungreppable without the register and G20 is unfalsifiable without the letter. **None of the five exists yet** (`ls $V/docs/tranches/X/fourier/contract/` → *No such file or directory*, re-measured 2026-08-28): every sibling wave citing them cites **prospectively, WHEN-created** (R-10.4).

### 1b. Do NOT touch — every path below, without exception

- `$F/**` — the whole fourier tree, read-only law. **v1 (`J-diff-shape.md`, fourier tree) is IMMUTABLE**; v2 supersedes **by reference** and never edits it (E-3: addenda, not patches).
- All product source both repos — `$V/api/src`, `$V/src`, `$V/demo`, `$F/api`, `$F/web` — execution gate.
- `lane-crud.md` · `INTAKE-ADJUDICATION-2026-08-03.md` · `api-gap-remeasure.md` · `registry/adjudicated/fr-*.md` — **E-1/E-3 immutable beside the spec**: they hold the witnesses, and rewriting one destroys the evidence a gate cites.
- `docs/tranches/X/COHESION.md` — deliberately excluded. F.W5 declares **its half** of every cross-edge in §4 and *requests* the reciprocal; it does not write the spine.
- `scripts/dev/dev.sh` — unowned dirty row, never staged. Pathspec commits only.

### 1c. Units and disjointness (serial; no worktree plan — one writer on clean `tranche-u`)

| unit | owns | writes |
|---|---|---|
| **a** | the operation register — 45 = 30 + 13 + 1 + 1, one row per operation | `operation-register.md` |
| **b** | v2 §A wire shape + §B the join relation | `J-diff-shape-v2.md` |
| **c** | v2 §C authority + §D denominator/coverage + §E provenance/persistence | `J-diff-shape-v2.md` |
| **d** | v2 §F equation contract + §G geometry + the value-side obligation list | `J-diff-shape-v2.md` |
| **e** | the ruling block, the relay letter, the INBOX row, **G19 carry closure + G22 cite resolution** — *fresh adjudicator seat* | `OWNER-RULINGS-F.W5.md`, relay, INBOX |

b/c/d share one file ⇒ **sequenced across sub-waves** (WAVE_SPEC §4a): a → b → c → d → e, each committing before the next opens. A dirty tree at handoff halts the wave. Third-iteration halts (triumvirate): the register disagreeing with the 45/30/13 triple a third time (K-1's prefixed-router blindness is the known trap); a **census row** (§1's PASS-1 roster) failing to home three times; MF-9 failing to resolve (then it is **struck**, and v2 quotes GCM-10's cure without it).

---

## 2. Carry — the adjudicated band agglomerated (162 id-keyed routings)

**Operand**: the 66-record registry, rostered at `docs/tranches/X/fourier/conformance/PASS-1/F-W5-CHECK.md` §1 — **routedTotal = 162**. Dedupe is **by identity, not by text**: where two records witness one mechanism the clause carries **both ids** and books once. Cross-listed ids appear elsewhere **by citation**, never by re-booking. `‡` = BLOCKER-weight · `⊙` = OWNER-GATED (unauthorable unruled) · `▲` = cure-integrity lock (binds the *repair*, not the finding).

▲ **BAND-PROBE LAW (R-6, program-wide)**: the routing token is read **spelling-agnostically** — `grep -rhoE "F\.W5[-–]W8" fr-*.md`, BOTH U+002D and U+2013 — with S-8's enumeration law (walk back to the enclosing row id; never trust a single literal). Measured union: **192 occurrences / 49 records, 23 of them en-dash-ONLY** (ruled at R-6; the same figures reproduce at `F-W8.md:5`). A hyphen-only census is structurally blind to 23 records; **no clause below may be sized from one spelling.**

▲ **ANTI-RENAME + RECORD-QUALIFICATION (R-5 / U-12)**: banked ids are original for life. Eight tokens collide across records inside this band — **M-13 · L-B1 · L-M3 · C-17 · C-18 · B-1 · B-2 · C-2** — so every occurrence below carries its record (`fr-EquationView B-1`, `fr-ContourSettings B-1`, …). Set-differences run on the qualified form; short forms are never authoritative.

**Clause-id compatibility**: sibling specs and this file's own §4/§5 cite v2 clause ids (`A3`, `B4`, `B5`, `C1`, `C2`, `C4`, `C5`, `D1`, `D7`, `E10`, `E15`, `E16`, `G7c`); this fold preserves each id for the same content so those cross-references resolve. Where a sibling's citation is mis-keyed, it is conformed **at the sibling's end** (R-1e) — never by renaming a clause here. **No clause is numbered D9** — `D9` is reserved throughout for the *ruled owner decision* (visibility `private|public`; lifecycle `active|trashed`; moderation `clear|withdrawn`), which **no clause may contradict**.

### §A — Wire shape and envelope

| clause | rows folded | witness | lock / cure-shape |
|---|---|---|---|
| **A1** Row identity on the wire | AA-45 (+ **AA-48**, same latent class — recorded, NOT re-booked) | `AuditEntry` carries four fields; pydantic extra-ignore drops `_id`; `AdminAuditLog.vue`'s `:key` is positional **because the contract offers no identity** | Every list-bearing response class in v2 carries a stable per-row identity. The `:key` deletion is **F.W4's act**; the clause is F.W5's. This clause stands on its two real authorities and nothing else: **AA-45** (`fr-AdminAuditLog.md:88`) and **AA-48** (`:91`), both banked `NO-WAVE-OWNER`. AA-48 discharges free with AA-11's F.W4 footer cure |
| **A2** Envelope + cardinality ruled once | AA-46 (C·D-13/C·D-14) · FR-AFP-18 (D-10) · cites **D10** | `page`/`pages` discarded and re-derived client-side (agreeing today; the page↔offset round-trip is lossless); the moderation queue has **no size anywhere** — no `count_documents`, `has_more` via limit+1, no `AdminStats.flagged`, six vanity MetricBadges | ONE envelope decision settles pagination, queue size and the featured window jointly. Raw material: fourier opaque cursors + `Link: rel=next` (`lib/crud/cursors.py`) vs value.js offset/limit + `total` (`platform/http/pagination.ts`) — lane-crud §6. FR-AFP-18's render seat is F.W1 (metric-badge→metric) |
| **A3** Boundary-validation **evenness** | AA-47 (C·D-17) · **FR-CP-16** (=C-17/L·A-3, the denominator) · GAB-12 (C-7) · GCM-10 ⊕ GCM-55 · FR-IC-6 (→L·m-7) · ContourPreview row 28 (D:i-3/L:L-8) · FR-FG-13 (=C-9) · C-15 (FSE, build-time) | **The measured denominator: the whole 45-operation client surface lands as ONE unchecked `(await res.json()) as T`.** AA-47's uneven pair (timestamp+target guarded, action+ip_hash not) is what survives pydantic's server-side shape guarantee; drift on the admin banner degrades into six silent em-dashes — no console error, no toast, no test; `save_contour` takes `xs`/`ys` off `dict[str, Any]` with **zero cardinality validation** | One posture per response class — *validated* or *explicitly-not-validated-and-why*. **Evenness is the clause, not paranoia.** ▲ Do NOT author a per-field defensive sweep at F.W4 (L-19). GCM-10/GCM-55 = the **serialization-bypass family**: `json.dumps` bypasses the Pydantic model, so `likes ?? 0` and bare `{{ entry.likes }}` assert opposite nullabilities — **the response goes through the model, or the model is decoration**. FR-FG-13 keeps C's own *"today it is safe"* honesty — a **latent-ordering** clause, not a live defect. FR-IC-6: the posture covers **persisted-then-rehydrated** payloads too (versioned keys blunt the stale-schema limb; the backend-adds-a-tier limb survives whole). C-15: the rule covers **build-time artifacts**, not only HTTP |
| **A4** One contract source | FR-AFP-36 (L-16/L-21/C:D-20) · FR-AFP-71 ⊙ (β-miss-9) | the emitted `content_hash` — the actual join key — is omitted client-side; three dead server models encode the retired contract (`FlagRequest` / rotted `user_slug` `FlaggedEntryInfo` / offset `FlaggedListResponse`); **no `response_model` anywhere**. Structural cause: god-module twins (`api.ts` 672 L / `types.ts` 391 L), no codegen, three hand-maintained copies of the flagged shape, one already rotted | ONE generated/checked shape; the dead models delete. ▲ **K9 LOCK: do NOT delete the five `?? item.slug` fallbacks** — `admin.py` emits `doc.get("image_slug")` unvalidated, so they are DEFENSIVE; that limb is NOT PROVEN. ⊙ **Module split / codegen-vs-hand-typed-twins is the union's call**: inv-16/inv-26 (*one contract source, hand-typed twins, no codegen*) is the standing law v1 §1 asserts — **v2 RESTATES or AMENDS, never silently reverses** |
| **A5** No untyped operations | **fr-CoefficientsSpectrum M-13** (=C-7) · FR-EQC-8 (=C-11) · FR-NP-30b | the epicycle response is `data: dict`; the only typed component model (`EpicycleComponentDTO`) is dead (one self-hit) and disagrees with the wire; `BasisComponent` is hand-maintained against a Python dict literal — **no derivable join**, while the equation route does it right in the same API; the coefficient crosses the seam as a **positional `[re, im]`** through two independently written remaps (a silent re/im swap type-checks everywhere); the `notation` enum is enumerated **four times** with a silent `.get(…, render_trig)` fallback | Verbatim: ***"an operation's response model must be structurally typed or the leaf is unjoinable IN PRINCIPLE."*** **Load-bearing for all of §B** — if v2 admits untyped operations, every join clause it writes is unenforceable at those operations. **Name the exception set explicitly or close it.** This clause therefore PRECEDES §B's enforceability. FR-EQC-8 = one adapter, both directions (cites M-13 as family, does not re-book). A silent fallback is a **contract decision**: state it or close the domain |
| **A6** Credential transport — both ends | FR-USB-37 (C-i2) · FR-GV-7 (=M-9/C·M-9; cited at C3) | the bearer sits in a `title` attribute on a globally mounted element on **every route**, and is accepted from the **URL query string**, persisted to localStorage with no expiry | ONE transport clause bounds **URL in, DOM out**. ▲ FR-USB-13's F.W4 cure will likely rework the attribute anyway — **the F.W4 arm must NOT be credited with the contract** |

### §B — R6-8: operation identity independent of client identity (bidirectional)

| clause | rows folded | witness | lock |
|---|---|---|---|
| **B1** The join relation — forward | **R6-8** (ADOPT-AS-FACT, the wave's governing law) · FR-AUL-3 (L-3/C-M1) · FR-GSB-28 · **fr-EquationView M-CK** · **fr-AnimationControls C-30** (`:123`, fold-by-reference) · fr-BasisCanvas C-5 · row 26 C:C-12 (ImageUpload) · D-L4+C-7 (ConvergenceLegend) · FR-EQR-32 (C-§0.4) · AA-31 (C·D-12) · fr-FourierShapeExtractor C-7 (cited G7c) | `adminFetch` keys the abort registry on the full query-bearing path (`coreFetch<T>(path, /* abortKey */ path, …)` — the comment shows the parameter was considered and the wrong value chosen) ⇒ per-operation cancellation degraded to per-URL for **13** admin operations; its dual: `resetAndFetch()` and `fetchNextPage()` share ONE literal `"listVisualizations"`, so a filter refetch cancels an in-flight page **and the reset's synchronous `entries = []` can abort its own reset**; `computeKey()` returns 4 fields while the POST carries 7 (incl. `notation`, `budget`, both response-determining); the multi-basis fallback re-derives the server's `partial_sums` on the main thread (~160k iterations/frame); `after`/`before` implemented end-to-end and unreachable from any UI | **Carry the identity ONCE and cite the instances, or the wave re-books law as work (M-25).** Every instantiating record says the identity STAYS with the intake (fr-AnimationControls C-30, CanvasControlsDock C-28, EasingCurvePreview RESOLVER, fr-ContourSettings i-7, fr-BasisCanvas C-5) — **fold by reference, never re-book**. ▲ **C-30's JOIN-AMBIGUITY LEG named (`fr-AnimationControls.md:123`, verbatim): *"Fold of intake R6-8 (operation↔client leaves non-isolable); this component is the display surface of the ambiguity, not the defect site"* — identity stays with R6-8's F.W5–W8 carry, so the leg is what F.W5 STATES and F.W4 renders; no repair is credited to this component.** Cure = stable per-operation abort keys; FR-AUL-3 ∥ FR-GSB-28 are **one clause, two directions** (under- and over-broad — the over-broad direction is unbanked elsewhere). The abort-key fix and FR-AUL-59's pipeline fix **compose; neither alone closes the window**. Map-growth rider stays INFO. ▲ **fr-EquationView M-CK SEQUENCING LOCK, verbatim (`:71`): "fix the key FIRST or the B-1 repair ships broken."** ImageUpload = the **incomplete** join (`thumbnailUrl` traverses NONE of `coreFetch`; 5 un-clienting `<img src>` sites): security half → C2, transport half → E15. FR-EQR-32 = the second seam instance (no SINK CLASS for string-typed response fields) — enforceable **only if F8 chooses the wire field**; if F.W5 declines, **the decline is RECORDED**. D-L4 ruled MINOR on the ConvergenceTimeline L·D-12 precedent — **dissent preserved** |
| **B2** The mirror direction ‡ | fr-ConvergencePlot C-7 | two contradictory `amplitude` conventions in one view, exactly 2× apart, both labelled `A`: the DTO ships `|c_n|` while `groupTrigHarmonics` discards `amplitude`/`phase` and re-derives `√(a²+b²)=2|c_k|` under the same name — tooltip `A = 0.8106` vs pill `A_1 = 0.4053`; FrequencyGraph documents the DTO convention, so **2 of 3 surfaces agree and the plot disagrees undisclosed** | **The R6-8 MIRROR RIDER: a client may not shadow a server field with a same-named different-valued re-derivation. The shared-provenance contract needs the rule in BOTH directions. Do NOT collapse it into the forward direction.** F.W4 arm: rename (`trigAmplitude`) or consume the DTO field |
| **B3** Wrapper types re-derived FROM the router | FR-AUL-11 (C-M2) · FR-AUL-12 (C-M3) · FR-AUL-13 (C-M4/L-11/L-23; second call site `GalleryView.vue:189-191`) | `deleteAdminUser` declares `{deleted; entries_deleted}` while the router returns `{"ok": True, "deleted_entries": …}` — **neither declared key is ever emitted**; `setAdminUserStatus` invents `slug` and drops `ok`; `BatchResponse.errors` is populated by no `admin.py` branch so partial batch failure reports **GREEN**, and `affected` = `modified_count`, so unsuspending 20 already-active users toasts *"Unsuspended 0 user(s)"* as success | One repair family (A.W5.c precedent shows the form). The binding indictment: `api.ts:617-624` is a **dated contract-repair record** for the batch family, and the two singular wrappers twenty lines above were left declaring shapes the router never emits — **in the same pass**. ▲ FR-AUL-13 verbatim: *either the router populates `errors`/`matched_count` or the contract drops them; the client compares `affected` vs `slugs.length` either way.* Its *"written against a contract DOCUMENT rather than the router"* cell is **this wave's own indictment — v2 IS such a document** ⇒ ships with the F.W9/W10 conformance probe or repeats the defect |
| **B4** The **LIVENESS** predicate (registry-novel) | PP-DEADSEAM (C-12) · GM-M4 · VV-R2-B (cited D14) | mutual dead capability: `preview_path` is written `""` at all three server sites while the client leaf is unreachable — **zero implementation on either side**; `softDelete`/`restore` are the gallery store's **only** splice/unshift mutators and its only ETag/If-Match round-trip, with **zero callers** | v2 needs a predicate for ***"declared on both sides, implemented on NEITHER."*** Genuinely new to the registry (both readers' sweeps + the seat's over 206 records) — it sits **BESIDE** R6-8's identity predicate, not inside it, and **separate from E10's produced-and-unconsumed disposition**. GM-M4's `:162-164` contract prose documents a round-trip no user can trigger — the prose dies with the disposition. **GM-M4 is the fact that KILLS L-15's scenario — carry the kill with the row** |
| **B5** Negative controls — **EVIDENCE, never a denominator** | HLG-23 (C:C-12) · C §3 negatives (MorphShapePreview) · P-9 (cited; home F.W4) | `/morph` reaches **ZERO** of the 45 operations and its static-JSON seam is `as any`-cast, voiding the one interface HLG-1 violates; R6-8 and R3-7b are **structurally unreachable** at MorphShapePreview | *"A route with no operation edge has no join to get wrong."* **Must appear in v2's clause JUSTIFICATION and must NOT be counted in any defect denominator (P-9).** Preserved verbatim so **no F.W5-W8 row manufactures overlap**. The `as any` cast folds → banked FMD-24 → N-14/N-15/FR-AH-1 (F.W4). Zero-cells copy to F.W2/F.W4 denominators — one home, two citations |

### §C — Authority, session and transport posture (R3-7b)

| clause | rows folded | witness | lock |
|---|---|---|---|
| **C1** Authority class per operation | **R3-7b** (CONTRACT DEFECT, un-refuted) · C-6/C-7 (EditorControlsDock) · GCM-52 (C-15) · F-4 (=C-C-8) | OpenAPI security **0/45** — auth parity cannot be SPECIFIED over a surface that documents none; the dock's only API seam is an **unauthenticated write with a client that always authenticates** (bare `save_contour(req)`, router with no `dependencies=`) which also **declines the `Idempotency-Key` channel its own client core declares** on the one non-idempotent-shaped call it can double-fire; the modal's only API call is unauthenticated, visibility-unchecked and publicly cacheable for a day; the thumbnail GET carries the same posture on the **pre-publication** surface | **Every row of `operation-register.md` carries an explicit AUTHORITY CLASS.** Identity stays with the intake at every instantiation (EditorControlsDock C-6, GCM-52, FR-AFP-4, GalleryDraftsSection F-4 all say so) — fold by reference. ▲ EditorControlsDock verbatim: **"the asymmetry, not an exploit, is the defect"** — v2 must not over-state it (the write is content-addressed and abort-keyed; it is **NOT** the sibling's publish-blocker), and the seam is recorded **R6-8-CLEAN as a positive**. F-4 contributes the **2²⁸ slug-entropy arithmetic** (`generate_slug` = 4× `secrets.choice` over 128-word lists). D-12's rider rides the same seam: async commit, no pending state, no double-submit guard, reachable silent unhandled rejection |
| **C2** Image remediation contract — ONE unit | FR-AFP-4 (L-1/C:D-2, BLOCKER-weight survivor) · m-15 · GCM-52 (cited) · F-4 (cited) · ImageUpload row 26 security half (cited) | **NO action the moderation panel offers takes the reported artifact offline**: Delete is a soft-delete of the VISUALIZATION; `images.py` = 7 ops with **zero DELETE**; the blob GET has no `Depends` and ships `Cache-Control: public, max-age=86400`; and every anonymous blob GET ends in `touch_document`, bumping the janitor's recency predicate — **actively-fetched reported content is precisely what never reaps**; the unversioned thumbnail URL + 24 h public cache **negates the deliberate re-upload regeneration** | Delete/quarantine + blob visibility gate + **janitor predicate** + **thumbnail versioning**, as ONE unit. ▲ C's scope discipline verbatim: **soft-delete DOES de-list from browse**; hard-arm zero-callers verified (`hard = false`, no third arg). ▲ **m-15 is CROSS-REFERENCED, NOT MERGED with F-4**: F-4 books *who can fetch*; m-15 books *what they see*. Deployment auth-proxy question stays UNPROVEN → SS-13 |
| **C3** Session truth and the canonical admin predicate | FR-USB-15 (L-1/C-§0/C-7) · FR-USB-23 ⊙ (L-10) · FR-AH-6 (C-6) · FR-GV-7 (cited A6) | `isLoggedIn` is a localStorage-**presence** check; `getMe` has zero call sites; `grep "401"` over `web/src` returns **ZERO**; `ensureUser` short-circuits on the stale value — a >30-day identity renders authenticated forever until a mutation fails in a vocabulary the component doesn't speak; the admin badge reads `galleryStore.adminMode` (a plain ref, 2 write sites, never rehydrated) while `auth.ts` restores `adminToken` and exposes `isAdminAuthenticated` with **zero consumers** — after reload the app is admin-capable and the badge is gone; `performBatchGallery` authorises directly off the stored token | 401 handling + `getMe` revalidation at the **STORE/API seam** — ▲ **the defect is authored in the STORE; do not route the cure to the leaf** (F.W4 renders whatever truth the store then has). One predicate, one home (F.W4); **the admin-auth seam owns WHICH predicate is canonical (F.W5)**. lane-crud §R-7 adopted: v2's session clause takes the **value.js form** (SHA-256 digest at rest; fourier persists the raw UUIDv4 as `_id`). ⊙ FR-USB-23: delete-or-wire the dead session subsystem (~17% of the store; `logout` never calls `clearSession`; the `else if (sessionToken)` arm would re-attach an anonymous identity on reload if ever wired). FR-AH-6 corroborates — and is corroborated by — AdminAuditLog K-5's *adminMode non-persisted* fact **without re-booking it** |
| **C4** Retry / limiter posture | FR-USB-24 (L-11) · D-L5+C-1 (EquationPanel) | the login path opts into the silent ≤60 s 429 auto-retry (`retryOn429 ?? true`, two sleeps ≤30 s) against a 5/60 s budget with no feedback, no cancel, Escape doesn't abort; a full-payload re-upload runs against the same 5/60 s compute limiter | Auth mutations pass `retryOn429: false` or surface the wait; the two-slot hardening question is answered in the contract. ▲ **ONE-CONTROLLER LOCK, verbatim: the 429 loop reuses ONE controller because the signal is hoisted OUTSIDE `while(true)` (`lib/api.ts:161-164`) — a naive request-scoped rewrite BREAKS that property.** Both rows share the lock |
| **C5** Redaction parity | F-γ / R-6 (lane-crud) | value.js collapses non-public hops to `{kind:"unavailable", ordinal}` and states *"no raw document or lineage field … ever crosses the wire for a non-public hop"*; fourier's breadcrumb applies **NO** redaction — a bare `find_one` emitting `slug`, `set_hash`, `author_slug`, `created_at` for every ancestor, because `_readable_or_none` is applied only to the ENTRY row | The breadcrumb redacts to the value.js discriminated-union shape, **or v2 states the asymmetry and why.** Today's witness is a private ancestor's slug on the wire — **the same "private artifact readable by slug" family as GCM-52** at the image seam |

### §D — The denominator, disposition and coverage

*(No clause is numbered D9 — see the compatibility note.)*

| clause | rows folded | witness | lock |
|---|---|---|---|
| **D1** Per-operation disposition — the register | **R3-7c ⊕ X-3** · FR-GV-8 (=M-6/L-12) · FR-GV-27 (=L-23) · **F-6 (= C-C-9 = L-15, fr-GalleryDraftsSection:49)** · AA-31 (cited B1) · FR-AFP-1 (INVERTED, cited D3) | **36 client edges / 9 gap operations**; **SEVEN of thirteen** `/api/visualizations` operations — remix, publish, unpublish, forks, provenance, diff, versions — have **zero client function** (three independent counts agree); `publish` PATCHes `{visibility}` behind an extra GET while dedicated `/publish`/`/unpublish` ship with no wrapper | Every gap operation carries a DISPOSITION (clientable / struck / deliberately server-only). ▲ **CENSUS CORRECTION FORCED (C-3): both sides were right on different denominators — never cite "30" as the whole API again. The triple is 45 = 30 public-non-admin + 13 admin + 1 app + 1 gallery.** ▲ **COUNTING LOCK K-1 (server-side blindness): a `@router.` grep is BLIND to prefixed routers (`@gallery_router.get("/cursor")`) — read against 13/44, never as zero.** ▲ **SECOND BLINDNESS LOCK — F-6 (= C-C-9 = L-15, `fr-GalleryDraftsSection.md:49`, routed *"F.W5 provenance-contract input"*), the CLIENT-side axis: *"the template-`src`-binding operation edge is structurally invisible to a function-keyed operation↔client model (the R5-7 dual)."* Unit a's register is function-keyed (45 = 30+13+1+1, one row per operation), so a `<img src>`/template-bound edge cannot appear in it by construction — the same class ImageUpload row 26 witnesses at B1 (`thumbnailUrl` traverses NONE of `coreFetch`; 5 un-clienting `<img src>` sites). **The register enumerates template-bound edges explicitly or it is armed against server under-count and unarmed against client under-count.** F-6's own severity stays INFO / NO-WAVE-OWNER (SS-3/SS-4 census methodology) — F.W5 consumes it as a register-construction constraint and books no repair.** **The seven unclientted operations ARE the provenance surface v2 re-authors.** Identity/provenance DISPLAY arms fold to banked GalleryCard D-7, GCM-25, GCM-3 — do NOT re-book. FR-GV-27's phantom-view cell belongs to **E6**, not here |
| **D2** The like verb ‡ | FR-GFC-3 (C-1 ⊕ GalleryCard C-1-as-corrected ⊕ GCM-2·C-1 ⊕ GIG C-3 — **four witnesses, ONE identity**) | `likes:int=0`, the `(visibility, likes, -id)` compound index and `cursors.py:22`'s `"likes"` sort key ALL ship while **no like route exists anywhere** (`api.ts` grep 0; `api/routers/` grep 0; `liked_ips` only as 7 projection exclusions); `gallery.ts` hard-codes `const liked = true` with a monotonic unguarded bump, so the un-like arm is dead — **the server sorts by a counter nothing can increment**, under an `aria-pressed` that lies; `gallery.py:3` asserts the endpoint was carved out TO `visualizations.py`, which has no such route | ▲ **Add the operation or remove the affordance. NO THIRD OPTION SHIPS** (C §7.1, verbatim). **The store's own comment DOCUMENTS the stopgap — v2 must not treat it as intent.** Display arm (`aria-pressed` + re-click guard) → F.W3/W4. GIG's new grain — the **orphan-client class** (a published emit contract with no reachable operation; R6-8's degenerate case) — rides NO-WAVE-OWNER → SS-3/SS-4 as census methodology. **Dissent: C axis filed BLOCKER at two sites, preserved against the banked MAJOR** |
| **D3** ⊙ Moderation producer-or-retire — **THE ADMISSION GATE** | FR-AFP-1 (L-1/C:D-1) | the queue has **NO PRODUCER**: two admin routes are the collection's entire live surface; every `db.flags` write repo-wide is a test fixture or a `$rename` migration; `FlagRequest` is declared and referenced nowhere; no client posts a flag; no UI renders a report affordance. Production's only reachable state is *"No flagged content"* — **~110 of 285 panel lines dead by reachability** | Build the user half (`POST /visualizations/{slug}/flags`, the mirror of lane-crud:336-339's value.js booking) **or retire the admin half whole**. **value.js HAS the verb fourier lacks (`POST /:slug/flag`) — the union's cleanest one-directional donation.** Extends R3-7c **INVERTED** (an admin half with no user half); the gap ledger carries both directions. ▲ **SEQUENCING LOCK (ruling 5, verbatim): severities in the FR-AFP roster grade the POPULATED surface; FR-AFP-1 is the band's sole reachability gate, and the producer-or-retire decision MUST PRECEDE F.W1's sizing wherever those grades are load-bearing** |
| **D4** Flag identity keys the ENTITY | FR-AFP-7 (L-3/L-4) | flags key on the NON-UNIQUE `content_hash`: one artefact fans out to N rows each claiming the full count (3 rows × "3 flags" = an apparent nine), and the slug-labelled *Dismiss flags* is a hash-scoped `delete_many` that silently clears every sibling — while `content_hash` is documented *"never identity"* | Flag identity moves to the ENTITY, or dismiss scopes to slug. Contract-level |
| **D5** Which identifier a human-facing surface may render | FR-AFP-8 (L-2/C:D-3) · GCM-1's `/w/` shape (cited E4) | the destructive confirm names `image_slug` — an asset FK shared by every remix (`VisualizationRemix` declares no `image_slug`) — while DELETING `slug`, violating the identity law the file's own header states | v2 states **entity vs asset** for human-readable surfaces. **One-token F.W3/W4 rider: pass `item.slug` as the label.** **Dissent: the L axis filed BLOCKER; β's demotion to MAJOR sustained under ruling 5 (K10) — the L axis cannot carry two BLOCKERs when its first proves the second unreachable** |
| **D6** Lifecycle truth and cascade | FR-AFP-9 (C:D-5; member-site rider **FR-GV-34** = MISS-LC-5) · FR-AFP-66 (β-miss-1) | the dialog asserts IRREVOCABILITY over a **30-day-restorable** soft delete; soft-delete does NOT cascade flags, so a restored row returns to the queue still flagged; the purpose-built `?hard=true` grace-bypass has **zero callers**; the client ships an unreachable `gallery.restore`; and `pinned_cron` — which the softdelete docstring **NAMES** as the cascade owner — contains zero flags code, so Delete → grace **strands flags forever**, permanently inflating the unscoped `$group` | `content_hash`-keyed cascade at grace hard-delete **plus the docstring correction** (the module docstring is false for this band). ⊙ hard-arm caller decision → owner; truthful copy → F.W3/W4 (diction rides FR-AFP-35). **The copy is false in the direction that SUPPRESSES a safe action.** FR-GV-34 is a MEMBER-SITE RIDER — one identity, two call sites, **not re-booked** |
| **D7** **Client-derivable bounds** | m-7 (=C-6-as-rescoped) · **fr-ContourSettings B-1** ⊕ R-16 (SliderControl, FOLDS) ‡ · FR-AUL-46 (LC-miss-M5) · FR-FG-21 (=LC-missed-2) · FR-AFP-33 (D-17/L-20/L-22) · cites F3 | the client's numeric window `[1,500]` is **invented at the leaf**, restated ~6× per control, and matches NONE of the server's SIX divergent windows (256 / 4096×3 / 200-unbounded / 200-le); *Min Area %* drives a `[0,1]` fraction from a `0–20, step 0.5` axis — every detent ≥1.0 (**39 of 41**) demands `area ≥ A ∧ area ≤ 0.92·A`, **empty by construction**, with the default `0.001` off-grid rendering "0.0" under a "%" label (a **100× unit lie in the dangerous direction**); the client never enforces `slugs … max_length=50`; `maxBars` is unclamped into a canvas dimension (~3,855 bars crosses the 65,535 px UA ceiling) on the family's **only unbounded request model**; the flag list is unbounded on BOTH ends (server `$push` with no `$slice`) | **Generate client bounds from the operation models (the R6-8 dual); operation records carry field DOMAINS.** **fr-ContourSettings B-1**'s F.W5–W8 rider verbatim: *"the typed range-checked descriptor table for all six fields rides an F.W5–W8 rider — the contract seam owes CLIENT-DERIVABLE BOUNDS."* **R-16 FOLDS — SliderControl books nothing new.** Born-RED fixture named at the record: drive Min Area to 0.5/1.0 in e2e and assert non-422. ▲ **K-3 KILL LOCK: m-7's straddle-a-422 scenario is REFUTED FROM SOURCE (`AnimationData` is a server-computed embedded document; the client-reachable bound 4096 ⊇ [1,500]) — v2 must NOT cite it.** LC's six-count confirmed over C-6's five. FR-AUL-46: surface the cap; client chunks or blocks; the ApiProblem-fallback clause adopted. FR-AFP-33: server bound → F.W5, client collapse → F.W1/W3; ▲ counterweight verbatim — `if not flagged` short-circuits production's only case, so **the cost is LATENT while D3 stands** |
| **D8** A blanket write is not a transition | FR-AFP-10 ⊕ FR-AFP-70 (C:D-4 · β-miss-7) · FR-GV-9 (=C·M-8 second clause) | *"Mark acceptable (save)"* never dequeues — `set_tier` writes `{tier, updated_at}` only and the flagged listing has **no tier predicate**, while the in-file comment claims the opposite; `handleSetTier` is hardwired to `'saved'` with no inverse, so a repeat click is a real PUT + audit row + success toast + full pagination reset for a no-op; Batch Unfeature `$set`s `tier:"normal"` unconditionally via `update_many`, **silently erasing a SAVED tier** | ONE semantics decision covers the tier↔flag pair; the tier verb gains its inverse; **the false in-file comment dies with the cure**; interim: reflect-and-disable when already saved. **FR-GV-9's first clause was KILLED at R-3/K3 — carry the kill.** ▲ **β's own KILL of its 428-escalation hypothesis (If-Match `*` accepted) is preserved — do not revive it.** Rides banked FR-GFC-20's stale-slug row |
| **D10** The list contract — filters and windows | FR-GFC-4 (=C-2/L-3; **GIG C-4/D-13 and FR-GSB-1 fold here**) · FR-GV-13 (=MISS-LC-2) · FR-GFC-1 (=D-1/C-3 ⊕ MISS-1(DU)) — **RIDER ONLY at F.W5** | `entries` is featured ∩ the loaded 20-row window, not the featured SET: **no `tier` param at ANY layer** (api.ts · visualizations.py · gallery.py · cursors.py), `fetchNextPage` appends so the strip grows during vertical scroll, and a featured row outside the window renders **no Featured section at all**; `visibilityFilter` is a **PRODUCERLESS store ref** (3 repo hits, all inside `stores/gallery.ts`, zero writers) so `ownerParam()` returns `undefined` unconditionally and the server's whole `owner == "me"` branch is DEAD from this client; `searchQuery`/`tierFilter`/`basisFilter` have **zero query-builder or `.filter()` readers repo-wide**, so every anonymous visitor who types and pauses tears the strip down to refetch a provably identical page | **An operation-surface hole, not a component patch** (both readers' routing adopted). A `tier` param or a dedicated BOUNDED featured fetch. ▲ **WAVE-LOCK (fr-GallerySearchBar C-2, verbatim): any wiring of `basisFilter` MUST use the banked normaliser (fr-BasisSelector M-10)** — without it the Fourier pill returns zero rows against `active_bases ∈ {fourier-epicycles, fourier-series}`. ▲ **FR-GFC-1's share at F.W5 is the RIDER ONLY** — whether search/tier/basis become real params; **the body cure is F.W4's** (patch in place off `api.setVisualizationTier`'s discarded returned entity, per the store's own `recordView`/`restore` idiom) and **the D-1 skeleton cure is KILLED (K5)**. **F.W1 must NOT be credited for the pagination-drain cure (FR-GIG-5).** FR-GV-13 is the worst sibling — the other three filters at least have producers. **Two BLOCKER filings on FR-GFC-1 (D and C axes) preserved** |
| **D11** What a count counts | GAB-15 (L·D-10) · GAB-16 · GAB-17 (D-M7/C-11/L·D-13) · FR-AUL-20 (LC-miss-M2) · FR-AUL-25 (DU-miss) | the storage tile carries **TWO UNCORRECTED OPPOSITE BIASES** under one unqualified label (no `$match` ⇒ soft-deleted assets counted; `"bytes": len(content)` records the primary blob only while thumbnails are separate `{slug}.thumb` files sized nowhere) — unbounded, non-cancelling; `total_entries` is **DERIVED** (`sum(tier_counts.values())` over an in-enum fold), so any absent or off-enum `tier` silently **VANISHES**; `entry_count`'s `$lookup` labelled *"Live-entry count"* carries no `deleted_at` filter and `prune_empty_users` uses the SAME unfiltered lookup — **the janitor MANUFACTURES exactly the population the prune then refuses**; the prune confirmation announces an unbounded PERMANENT deletion **without a count**, though `empty_slugs` is computed BEFORE deleting | v2 states **live/tombstoned** and **primary/derived**; the join filters `deleted_at: None` or tombstone-inclusive counting is stated explicitly; honest total = `count_documents(not_deleted_filter())` + an *other* bucket; a count/dry-run endpoint or a two-step confirm. ▲ **GAB-17 is SEQUENCED: render `stats.normal` only AFTER GAB-16 lands, or drop the field with this contract pass — its "verification" limb is a TAUTOLOGY until then.** ▲ **GAB-16 REFUTES D-M7's headline rationale (R-6 ruling) — carry the refutation with the row.** ▲ **PRUNE SEQUENCE: FR-AUL-20 → FR-AUL-21 → FR-AUL-25; the count is meaningless until both land.** **Dissent: axes split MAJOR/MINOR/INFO on GAB-17; MINOR ruled.** Magnitudes → SS-13 |
| **D12** Closed domains at the boundary | FR-AUL-21 ⊕ FR-AUL-31 · **fr-ContourSettings M-10** (=L-M6/C-19/D-m9) · **fr-BasisSelector M-9** ⊙ (=L-M4/C-20) · FR-NP-30b (cited A5) | a **THIRD** `status` exists — `migrate_visualization.py` upserts `"orphan-migrated"`, `$ifNull` passes it through, `AdminUserInfo.status: str` accepts it, the template branches only on `=== 'suspended'` ⇒ an orphan-migrated user renders as a **HEALTHY ACCOUNT WITH A SUSPEND AFFORDANCE** on the panel whose purpose is separating real accounts from churn; two hand-maintained `Record<string,string>`s stand in for the backend's 7-member `ContourStrategy` enum — `ADAPTIVE_THRESHOLD` is real, implemented and **unrepresentable** in the picker, and once changed away cannot be re-selected; the deliberately-minted "off" state (`[]`, in-file comment *"canvas handles it gracefully"*) is **unrepresentable end-to-end** and silently laundered on every path — **the third click is honoured on screen and destroyed on save with zero diagnostic** | ONE closed set across writers, model and wire (`Literal` + the migration writer folded in); the sort set closed on both ends (reka `AcceptableValue` widening and `sort_map.get(sort, default)` both fall back silently). **The shared-enum contract is F.W5's; the typed vocabulary at the leaf is F.W3/W4's.** ▲ **F.W4 rider: unknown statuses render VISIBLY, never as healthy.** ⊙ **fr-BasisSelector M-9 verbatim: admit `[]` in the contract, or stop minting it — the silent rewrite must not survive either way.** Same mechanism family as GAB-16: **closed domain at the boundary, open domain in the collection**. ▲ M-9's corpus line-drift corrected by the seat — **re-resolve anchors after F.W0 (D-19)** |
| **D13** Required-vs-optional posture stated once | FR-AUL-14 (C-B4, rescoped ruling 2) | **ONE** user document missing `created_at`/`last_seen_at` **500s the ENTIRE admin list** (required model fields, bare inclusion projection, handler-body construction, global exception handler) — and the janitor's reap query **SKIPS fieldless docs**, so they never age out | `$ifNull` the projection or default the model fields; the posture stated once. The client's null guard rides FR-AUL-17's timeAgo repair (F.W3). Absent defence proven on BOTH sides; **production INCIDENCE is UNPROVEN — that question alone rides live → SS-13.** v2 states the posture; the probe answers whether it is violated |
| **D14** Adoption is measurable — the **acceptance surface** | VV-R2-B (MAJOR · NEW, the FRAME row; census verified 10/10) · owns the joint identity of BLK-1 / BLK-2 / VV-R2-A / VV-R2-E | the workspace store's entire converged-CRUD arm is dead surface: **TEN of 24** exported members have zero consumers outside the store (`visualizationSlug`, `visualizationETag`, `revision`, `loadVisualization`, `loadSnapshot`, `setVisibility`, `deleteVisualization`, `invalidateInFlightComputation`, `defaultContourSettings`, `defaultAnimationSettings`), and `gallery.ts:168` bypasses the store's own `deleteVisualization` to call the API raw, **stranding the slug/ETag state maintained for exactly that call** | Reading the cluster as ONE gap changes the cure: **a single wiring unit at this component** (route → `loadVisualization`; publish → `setVisibility`; delete → the store action), not scattered repairs — it does **not** re-book its members. F.W4 executes the wiring; **F.W5 owns the CRUD-CONTRACT §1 adoption as the union tranche's own ACCEPTANCE SURFACE.** ▲ **This is the row that tells F.W5 whether its contract was adopted at all. No wave may claim adoption without it** |
| **D15** Query semantics under the shipped indexes | AA-6 (L-4/C·D-2) · **AA-23** · AA-32 · FR-AUL-59 (LC-miss-M9) | `admin.py:631-632` is a **BARE EQUALITY** on `action` beside an honest regex on target; the placeholder advertises prefixes and its own example `set_tier` matches **zero rows forever** (writers emit `set_tier:{tier}`), and composed with AA-1 *"no matches"* and *"unreachable"* are pixel-identical; `services/database.py:145-146` creates exactly TWO indexes on `admin_audit`; the `target` filter drives an **unindexed** case-insensitive regex plus a second full scan via `count_documents`; the `entries` sort orders `$match → $lookup → $addFields → $sort → $skip → $limit`, so the cross-collection join runs over the **ENTIRE matched user set** per debounced request | ▲ **The regex-action cure BOTH axes prescribed is REJECTED (AA-23).** Sound cures in this stated ORDER only: **(a)** honest placeholder (*"action (exact…)"*), **(b)** `$in` over a generated taxonomy (also cures AA-5/AA-24), **(c)** anchored case-**sensitive** `^prefix`. ▲ **AA-23 CURE LOCK, verbatim-binding: a case-insensitive `$regex` cannot use that index; shipping it converts an indexed equality into a COLLECTION SCAN on the one monotonically growing collection. It BINDS AA-6's cure set and PRECEDES any query-semantics clause v2 writes.** ▲ **AA-32 positive, binding: `re.escape` IS applied — no injection; v2 must not manufacture one** (bounded by the 90-day janitor prune). AA-21's placeholder co-sign rides AA-6 (iOS sentence-autocapitalisation defeats an exact match; the server equality carries no `$options:"i"`). FR-AUL-59: pipeline reorder — join AFTER `$limit`, as the sibling branch already does; **composes with B1's abort-key fix; neither alone closes the window** |
| **D16** The shared action taxonomy | AA-5 (L-3) + AA-24 | `actionTone` is blind to the ENTIRE `janitor:` namespace because the collection's **SECOND writer** (`janitor.py:59-99`) inserts directly into `db.admin_audit` with no `log_audit` call, emitting nine `janitor:*` actions — including the audit log pruning its own history — that all land on the benign default; and inside the map `batch_users:delete`, the vocabulary's most destructive operation, falls past `startsWith("delete")` into the same violet arm as `unsuspend` | A shared ACTION TAXONOMY at the seam (cross-tier F.W5; also cures AA-24 and feeds AA-6's `$in`). Display arm → F.W4. ▲ **S-8 METHOD LAW (kills C·S-2/K-3 via K-6): an absence-proof must ENUMERATE the surface, not query one name for it** — `grep "log_audit("` was structurally blind to the inlined second writer. **v2's absence-proofs inherit this bar** |
| **D17** Date serialization — **serializer arm only** | FR-AFP-32 (D-16/L-13/C:D-15; +FR-AFP-40 fold, +FR-AFP-69 = β-miss-5; GalleryCard L·M-4/D-13/C-8(a) folds here) | ONE router serializes the same concept TWO WAYS: flagged rows via `_json`/`default=str` (space-separated, **non-ECMA**) while audit returns `AuditListResponse` (ISO with `T`) — and the NaN guard sits in the consumer that gets the **conformant** payload while this file, on the non-conformant one, has none | ▲ **F.W5 owns ONLY the serializer arm. The five-copy `timeAgo` family's shared formatter is F.W3's and already banked as FR-AUL-17 — do NOT re-book it here** (GalleryCard states the fold explicitly). JSC acceptance of the non-ECMA form → SS-13 |

### §E — Provenance, lineage and persistence (the union's core)

| clause | rows folded | witness | lock |
|---|---|---|---|
| **E1** Compound per-entity version identity | **V-β** (lane-crud §V-β, +§7) — value-side | `PaletteVersion._id` is GLOBAL, so chains **COLLIDE ACROSS PALETTES**: `hash.ts` folds only `{name, colors}` — never `paletteSlug` — and `findByHash` filters `{_id: hash}` with no slug scope while `insertIfAbsent` returns early on a hit; two palettes with identical `(name, colors)` share ONE version row whose `paletteSlug` names only the first, and **the second's history is EMPTY** *(re-verified value-side 2026-08-28: `$V/api/src/modules/palette/hash.ts` `computeContentHash(name, colors)` canonicalizes `{name, colors}` only; `repository/paletteVersion.ts` `findByHash(hash)`; `insertIfAbsent` idempotent on duplicate hash — its own test asserts "no second write")* | v2 mandates the **compound per-entity version `_id`**; fourier already solved it with `f"{viz_slug}:{set_hash}"` and documents the §2.3-vs-§11 tension. ▲ **Execution belongs to the value.js API row, NEVER re-booked as a fourier defect.** Value-side rows are citable now; fourier-side anchors are MEASURE-AT-OPEN (D-19) |
| **E2** Chain depth: deepen or retire | **F-α** (lane-crud §F-α + intake X-8) — fourier-side | `_write_root_version` hard-codes `parent_hash=None, root_hash=set_hash_value, depth=0` and is the ONLY writer, so every version has `depth == 0`; `GET /{slug}/versions` sorts by `depth` over an always-singleton set; all real lineage rides cross-viz `fork_of` | v2 states whether within-entity chains DEEPEN, **or** the depth/parent/root quadruple is explicitly RETIRED on the fourier side. ▲ **F.W5 owns the CLAUSE; F.W6 owns the burn-down — do NOT double-book.** X-8 corroborates three ways (Codex controls, census C-6, fourier's own M.W10 booking) and **adds no new work** |
| **E3** ⊙ Diff-clause participation | **TA-4** (megatranche COHESION §2 / lane-crud §7 / SS-4 prerequisite) | `lib/crud/atomdiff.ts` + `atomDiff` on `PaletteVersion` were excised at T.W1/TA-4; only hashing survives, so `J-diff-shape.md §6`'s probes **CANNOT RUN** value-side — while v1 §2.5 mandates the exact file and §6's close-gate assumes BOTH probes exist. **A co-signature over un-runnable probes is VOID.** *(Re-verified 2026-08-28: `ls $V/api/src/lib` → No such file or directory; `grep -rn "atomdiff\|atomDiff" $V/api/src $V/src` → ONE comment hit, `__tests__/palettes-forks.test.ts:9`, which names TA-4 as the excision.)* | ⊙ **The owner rules RESTORE (TA-4 reversal), or v2 RE-SCOPES value.js out of the diff clause EXPLICITLY with §6 re-authored to a one-sided verdict. The wave may not assume either.** ▲ **SS-4 must name the TA-4 value-side restoration as a PREREQUISITE or re-scope explicitly** (charter law, verbatim) |
| **E4** Born visibility of the derived variant ‡ + the fork/remix vocabulary | **R-5 (lane-crud) ⊕ GCM-1 ⊕ D-7 (GalleryCard) ⊕ X-2**; BLK-1 FOLDS (VisualizationView) | a fourier remix child is born **`draft`**; a value.js fork child is hard-coded **`visibility: "public"`** — OPPOSITE privacy defaults on the same verb, both shipping *(value-side re-verified 2026-08-28: `$V/api/src/modules/palette/service/forks.ts:76` — `visibility: "public"`)*; the modal's only CTA routes to the PRE-SAVE workspace, emits `image_slug`, loads the CALLER's IndexedDB draft, hard-resets to defaults when absent, and **writes a draft row keyed on another user's `image_slug` into the viewer's DB** — while `loadVisualization` has zero call sites and **any subsequent save POSTs a NEW entity with no `fork_of` despite the fork substrate existing**; the entity's HUMAN identity is write-only (`entry.title` readers = 0; label, `alt`, `aria-label` all spend the MACHINE FK) | ONE born-visibility rule, co-signed; **each side's test asserts the child's visibility on create**. ▲ **F.W5's share of GCM-1 is the CONTRACT FACT — a save that should have been a fork carries no lineage — NOT the routing repair (F.W4's; BLK-1 folds; D14 owns the joint identity).** `fork_count` unrendered is load-bearing for the union — it is the counter the fork verb maintains — composing with lane-crud R-1 (`fork_count`↔`forkCount`) and X-2 (**a stored counter cannot stay truthful under viewer filtering**). Vocabulary settled here: **remix vs fork**, **snapshot vs version**. ⊙ owner ruling owed on remix-vs-fork and born-visibility — flagged INLINE per SS-4. ▲ **No clause may contradict ruling D9** (visibility `private|public`; lifecycle `active|trashed`; moderation `clear|withdrawn`) — and value.js today persists a 3-state visibility (`public`/`unlisted`/`private`, `model.ts:61`), which the value-side obligation list must reconcile rather than the contract silently overwrite |
| **E5** Create idempotency and dedupe — ONE clause, TWO entry points ‡ | **fr-GalleryDraftsSection B-2 ⊕ fr-CanvasControlsDock D-4**; FR-GV-1 FOLDS at banked severity; EditorControlsDock C-7 rider; intake X-3 | the publish path is a composed falsehood: `savedSnapshots` has ONE writer — the unconditional literal `[]` — so `!d.savedSnapshots?.length` is invariantly true and **the row never leaves the tab**; `publishDraft` never calls `saveDraft`/`refreshDrafts`; `publishedHashes` holds freshly minted visualization slugs that can never equal a draft's `imageSlug`; the button re-enables and **the second press mints a SECOND public visualization** because `createVisualization` sends no `Idempotency-Key` (the middleware is explicit: *no header → handler()*) and `content_hash` carries a PLAIN index (contrast flags' deliberate `unique=True`). At the dock the same shape is re-entrant on all three hops: **N clicks ⇒ N public gallery rows** | Server-side dedupe/idempotency for the create path. Split adopted: props/reconciliation → F.W3/W4; **server dedupe → F.W5-W8**. ▲ **K12 PROBE-SUPPRESSION LOCK: both axes' UNPROVEN-NEEDS-LIVE hedges are DEAD — fr-GalleryDraftsSection B-2 closes it STATICALLY (no-header idempotency passthrough + the plain index); SS-13 spends NO probe here.** ▲ **fr-CanvasControlsDock D-4's F.W5 share verbatim: "the server-side `(image_slug, contour_hash)` dedupe arm rides the R6-8 seam split (C-28 fold) — the client cure and the operation cure register a TWO-SIDED DELTA until that join is split" — the server arm SEQUENCES AFTER the join split.** Riders: the success toast prints the USER slug (m-13); `publishedHashes` is a misnomer (it holds slugs) — fix with the contract |
| **E6** Unsafe GET / counter provenance | **FR-GV-12 (=MISS-LC-1) ⊕ VV-R2-A ⊕ FR-GV-24** | `GET /api/visualizations/{slug}` is an **UNSAFE READ THAT CANNOT OBSERVE ITS OWN SIDE EFFECT** — `find_one` first, `$inc {"views": 1}` after, `_public_doc` serialising the PRE-increment document; the same verb is the ETag-capture path, so **every publish silently inflates its own view counter** and an entity is born at `views: 1` before any third party sees it; no `viewed_ips` dedup while `liked_ips` exists; `viewedHashes` is component-local and resets on mount | The counter moves to an EXPLICIT VERB, or v2 states the mutating-GET policy against **RFC 9110 §9.2.1**. ▲ **Repair-test lock (GCM-3 R-7 precision): `viewedHashes` DOES work within a session — the defect is SCOPE, not absence — so repair tests must NOT assert a re-open increment.** ▲ **The store's own `recordView` comment documents GET-as-increment as the DELIBERATE mechanism, so the publish-path GET is an ACCIDENTAL self-count: v2 must not codify the accident.** F.W4 arm: publish adopts `store.setVisibility("public")` (implemented, zero callers), dropping one round trip and the phantom view. Rides D14's joint identity. Observable magnitude → SS-13 |
| **E7** PATCH atom coverage + `set_hash` recompute | **F-β (lane-crud) ⊕ SS-C-1 write leg** | `VisualizationUpdate` (api) and `VisualizationPatch` (client) are the SAME five fields with `extra="forbid"`, so the persisted `speed` — atom 4-of-5 of the version-identity set — is settable **only at create/fork**; and the same model lets `palette_slug` (atom 5) be `$set` with **NO `set_hash` recompute and NO version write**, going stale against `enumerate_atoms(doc)` | **The SAME model is both TOO NARROW (speed) and TOO PERMISSIVE (palette_slug)** — atom coverage + recompute stated once. Ruled SPLIT (R2-LC wins): **SS-C-1's READ leg FOLDS to GCM-1/E4 — do not re-book; the WRITE leg books new.** **Dissent: C axis filed BLOCKER; demoted at component altitude (no capability loss in the control) — preserved** |
| **E8** AnimationSettings — the three-way reconciliation | **BC-9/C-6/D-20 ⊕ SS-C-2**; AnimationControls' LC-miss folds | half the persisted contract is **WRITE-ONLY and THREE-WAY DIVERGENT INCLUDING UNITS**: server `fps 30 / duration 30.0 s / max_circles 80`, client defaults `60 / 5000 ms / 100`, renderer hardcoded `ref(80)` and `20000 ms` — only easing/speed/active_bases round-trip, so **a gallery replay cannot reproduce its own frame**; atom 4 is semantically empty because `fps`/`max_circles`/`duration` have zero cross-wire readers, while the `duration` that `speed` divides is declared 4× with 3 values and a **1000× unit fork** | The union owns the reconciliation **including the unit fork**, with server-side units stated. Deduped: SS-C-2 and BC-9 are ONE identity (SS-C-2's unconstrained-`speed` cell carried); ▲ **`duration = ref(20000)` zero-writers stays banked at BC-20 — NOT re-booked**; ▲ AnimationControls' independently re-derived *"write-only AnimationSettings"* is **CORROBORATION, not a new booking** |
| **E9** One shape, one name | **fr-BasisCanvas C-7** | two `AnimationData` types share a NAME and a FIELD with incompatible shapes: server `partial_sums: dict[str, Point2D]` whose docstring **names `BasisCanvas.vue`** and prescribes typed access over a shape the client cannot use, vs client `Record<string, Record<number,{x:number[];y:number[]}>>`; the component's own comment misdescribes even the client type; entity `animation_data` is written and read by nobody | One shape, one name — **the server docstring's prescription is either honoured or retracted** |
| **E10** Produced-and-unconsumed: ONE disposition | M-β4 · **fr-EquationView L·m-6** (=C·D-14) · **FR-EMT-20** (cited F1) | `EpicycleData.trace` — a 3000-sample polyline computed per response — has ZERO readers in the web tree and rides `structuredClone` into every IndexedDB draft; `reconstructed_points` (500+500 floats) is fetched, sessionStorage-persisted whole, and read by NOTHING; `SimplifyResponse.term_count` has zero consumers | **v2 states ONE disposition for produced-and-unconsumed fields, not three ad-hoc deletions.** The R6-8 seam's converse: server computes, nobody consumes. ▲ **FR-EMT-20's "registry-swept: 0 banked hits" cell is CORRECTED by fr-EquationView L·m-6 — one identity, two witnesses.** Distinct from B4's liveness predicate |
| **E11** Easing domain hoisted to the operation | **RESOLVER (D-10·L-4·C-D-4) ⊕ L/M-3 (EasingPicker)** — merged, both ids preserved | `easings.ts:105` is the tree's ONLY unguarded catalog index (three siblings guard), called from a template expression with zero error boundaries, over a corridor that is a bare `str` at both seam ends with a cast as the whole validation — **five** unguarded hops (localStorage draft spread over defaults is the fifth). Failure: a radio group with zero checked members over a silently-linear animation | F.W4 owns the one `?? linear` guard-parity line; **F.W5 hoists the union to the operation record as `Literal[…]` on `AnimationSettings.easing`.** The seed/normalization half FOLDS to banked AnimationControls L-11/M-10 — not re-booked. ▲ **Registry-integrity (G22): EasingPicker booked its easing-domain server half as "new" when RESOLVER already carried the cure — MERGED, both ids preserved.** L/M-3 **KILLS C/i-1**: the field IS persisted and remixed (`animation_settings` on all four models, seat-verified) |
| **E12** Debounced mirror vs synchronous save | MISSED-E (EasingPicker save-race) | the picker's write reaches persistence through a 500 ms `watchDebounced` with **no flush-on-save**, while `saveVisualization()` reads `toRaw(animationSettings.value)` SYNCHRONOUSLY — select-then-save inside the window persists the PREVIOUS curve | Persistence semantics: the flush seam is the banked L-12/C-25 `setEasing` action. ▲ **The narrow reachability is HONESTLY STATED and the honesty CARRIES: a debounced mirror is a contract hazard wherever a synchronous save reads the same state** |
| **E13** Cache identity ⊇ consumed fields ‡ | **fr-ContourSettings B-4 = C-1 ∘ C-25 / R6-8 ⊕ i-7 ⊕ m-18** | the ML Threshold slider is INERT after the first extraction: `extraction_cache_key` is a CLOSED 10-field `json.dumps` literal that **OMITS `ml_threshold`/`ml_detail_threshold`** — the two fields this control is the sole producer of — while the handler short-circuits on the cache hit BEFORE `compute_contours`, and those fields materially drive extraction on the SHIPPED default route (`auto`). The user drags 0.50→0.85, waits out the debounce, and receives the 0.50 contour with no signal | Verbatim clause (C-25): ***an operation's cache identity must be a SUPERSET of the request fields the operation consumes*** — the R6-8 seam made product-visible. **i-7 FOLDS (B-4's payload, not a second row)**; m-18's invented `ml_detail_threshold = mlThreshold * 0.6` coupling rides here. F.W3/W4 owns the regression fixture: assert **`contour_hash` INSTABILITY across an ML-threshold change** (also the SS-13 probe) |
| **E14** Contour provenance — an editor-saved contour is FIRST CLASS ‡ | **fr-ContourSettings M-13 ⊕ fr-BasisSelector M-14** *(NOT fr-CoefficientsSpectrum M-13 — the A5 homonym; U-12 qualification)* | the hand-edit pipeline **DESTROYS THE EDIT ON EXIT**: nothing recomputes from `store.contour`, so merely LEAVING the editor remounts ContourSettings, whose immediate watcher auto-runs `runCompute` → `extractContour`, and the server **cache-hits the ORIGINAL** extraction because editor-saved contours carry no `extraction_cache_key` (`store_contour_asset(..., source="editor")`); the second trigger is a Harmonics/Sample-Points nudge from the sibling panel doing the same thing | ▲ **TWO TRIGGERS, ONE CURE (verbatim; both records agree). Extraction may not silently overwrite a `source="editor"` asset.** M-14's end-to-end arm is UNPROVEN-NEEDS-LIVE → SS-13; **the mechanism is source-certain** |
| **E15** ONE transport clause | **fr-BasisCanvas C-17 + fr-BasisCanvas C-18** (transport) · row 26 C:C-12 transport half (ImageUpload) | the overlay is fetched unconditionally then refused when `image_bounds` is null (a second correctly-sized fetch follows — the first is waste); `overlayUrl` is a raw string-concat built **OUTSIDE `coreFetch`**, yielding `/api/api/...` under the Dockerfile default while compose passes empty; `thumbnailUrl` is a template literal straight into `<img src>` — no session token, no abort registry, no 429 retry, no ApiProblem, no ETag | Every URL the app fetches goes through the client core, or the exception is stated. ▲ **The race discipline is a recorded superlative (S-6): do NOT cure the waste by removing the guard** |
| **E16** ⊙ Trie disposition — the requirement with no material on either side | **R-4** (lane-crud §R-4 + COHESION SS-4) | the R-4 probe returns **ZERO true hits on BOTH trees** (trie / prefix-tree / radix / patricia / structural-sharing / delta-compress). What exists on both sides is **WHOLE-SNAPSHOT DUPLICATION**, and both carry an explicit anti-tree KISS guardrail — `atomdiff.py:12-14`: *"the atoms are a flat BAG (not a tree / Merkle / document) … there is no three-way / DAG / merge"* | ⊙ **The honest default v2 MUST carry absent a ruling: no trie; whole-snapshot duplication is the recorded shipped behaviour.** A greenfield build against a documented standing constraint. ▲ **SS-4 FLAGS THIS RULING INLINE. Dissent recorded (F-W10): the trie requirement collides head-on with a standing KISS guardrail present in BOTH trees — `atomdiff.py:12-14` is the INCUMBENT — and the owner ruling PRECEDES design** |
| **E17** Image bounds on write ‡ | **fr-ContourEditorCanvas C-2** *(NOT fr-GallerySearchBar C-2 at D10, nor fr-FourierShapeExtractor C-2 at G2c; U-12 qualification)* | the POST **never derives bounds**: `store_contour_asset(xs, ys, req.image_slug, source="editor")` passes no `image_bounds`, the param defaults `None` and is written verbatim, and backfill is GET-only — so `imageOverlayRect` goes null and the `<image>` v-if drops; because the store is content-addressed, a no-op save re-hits the extraction doc WITH bounds, meaning **the overlay dies exactly when the points CHANGED** | **Derive bounds on POST or backfill on write.** ▲ **The tree's own workaround comment sits on the LOAD path — do NOT mistake it for a cure** |
| **E18** Attribution — the actor is a FIELD | AA-10 (D-M2/L-13) + lane-crud R-7 / **V-γ** | the log never answers WHO: `_JANITOR_ACTOR = "system:janitor"` is written into **`ip_hash`** and rendered `system:jan` by an unconditional `.slice(0,10)` — convicted by the producer's own comment (*"self-documenting in the viewer"*); and fourier's `owner_slug` is required non-null vs value.js `userSlug: string \| null`, whose `if (userSlug)` guard means **an unattributable edit writes NO version row (V-γ)** | **An audit row's actor is a FIELD, not a repurposed `ip_hash`**; the attribution asymmetry is stated once. Display cure (sentinel-aware branch + legend) → F.W4 |
| **E19** Vocabulary the contract inherits — **noun + positional arm only** | D-14 / MIN-4 / MIN-5 / MIN-10 / MIN-11 (VisualizationView cluster) | three names for one affordance; a RETIRED `snapshot` noun still on the wire; a dead `_imageSlug` positional kept *"so the pre-existing call site need not change"* with exactly ONE caller | **Noun + positional → F.W5; the rest → F.W4.** v2 fixes the nouns it inherits (remix vs fork; snapshot vs version — lane-crud §6) |
| **E20** The exposed-ref seam — the one non-wire contract row | ContourPreview row 13 (L:L-5) | the prop is sourced through the sibling's `defineExpose`d **MUTABLE** ref (the undo ring's subject) across a v-if boundary; the channel is read-WRITE (`proxyRefs` set-trap) and **the tree ALREADY writes through it one key over** — `magnetRadius` via the computed setter; `getPoints()` is exposed two lines earlier | Verbatim: **a TWO-MEMBER change (`points` AND `magnetRadius`)** — readonly / `getPoints` seam. ▲ **Row 11's one-character `?:` fix pairs at F.W4 "per the L-lane carry" — F.W4 must NOT land row 11 presuming row 13's seam shape.** Owned here because the record routed it here |

### §F — Equation contract and error envelope

| clause | rows folded | witness | lock |
|---|---|---|---|
| **F1** Partial projection ‡ | **fr-EquationView B-1 ⊕ FR-EMT-1**; rider R1-missed-4 | the Notation control is **INERT IN THE DEFAULT DISPLAY MODE**: `SimplifyResponse = {latex, energy_captured, term_count}` on BOTH sides with no sigma arm, while the server proves sigma IS notation-dependent (`render_latex_sigma(terms, req.notation)`); a notation flip routes only to `doSimplify`, which writes `displayLatex` alone, so a stale-but-truthy `displayLatexSigma` wins, `lastDisplayKey` advances and suppresses every retry, the energy badge DOES move so the control looks alive, and **the mismatch PERSISTS ACROSS RELOAD**; the leaf's contract has no `disabled`/`available`/`stale` input | **`latex_sigma` joins `SimplifyResponse` on BOTH models — a CONTRACT DECISION, not a patch — or explicit invalidation.** ▲ **CURE BOUND by fr-EquationView M-CK, verbatim: the alternative cure ("route notation through doCompute") walks into the under-determined `computeKey` and RE-SHIPS B-1 one layer down. Fix the key FIRST.** Filed BESIDE R6-8 as the second instance of the partial-projection law (with E10 the converse direction — **one envelope row, both directions**). F.W4 rider: per-option availability (FR-EMT-7's fold supplies `disabled` free). Rider: `useCoeffHover` reads its LABEL KIND from stale rendered markup while computing VALUES from fresh state — a second victim of the same staleness |
| **F2** One shared bound constant ‡ | **fr-EquationView B-2 ⊕ fr-FunctionInput L-B1/C-1 (FOLDS) ⊕ fr-FunctionInput C-8** *(NOT fr-FourierShapeExtractor L-B1 at G1c; U-12 qualification)* | `budget` walks past `le=50` from the shipped defaults in ONE Harmonics drag — the rescale recurrence yields `budget(v) = v − 10` on a monotone up-drag (61→51; a single track-click jump from defaults yields exactly 50) against `ge=2, le=50` on both pydantic bounds; through `doSimplify` the 422 lands in `/* silent */`, and **`budget` restores from sessionStorage, so the NEXT session's first compute 422s before the user touches anything**; Harmonics `:max="100"` separately forfeits half of the operation's `le=200` range | F.W5 owns **one shared bound constant across the seam** (the R6-8-correct form). ▲ **Clamp at the two request sites or the ref — NOT at the slider: the restore path never passes through it (M-BR rider).** ▲ **KILL LOCK (K-13): fr-FunctionInput L-B1's "without ever touching the offending slider" headline rides the ratio-1 state proven UNREACHABLE from defaults; C-1's sessionStorage-restore path is the TRUE one — v2 cites the true path only.** fr-FunctionInput L-B1 FOLDS and is not re-booked; fr-FunctionInput C-8 rides the same constant |
| **F3** No unbounded field beside bounded siblings | **fr-EquationView M-SB ⊕ fr-FunctionInput N-4 (AMENDMENT RELAY)**; cites D7 | `SimplifyRequest.coefficients` has NO `max_length` beside fully-bounded scalars (the client's 2N+1 ≤ 201 bound is a coincidence, not a contract term) — **and `ComputeEquationRequest.expression: str` is bare too** (no max_length, no pattern), above six fully-bounded scalars, flowing through a seven-keyword denylist into `parse_expr` + symbolic integration with no `maxlength` at its sole editor | One `max_length` per unbounded field — the R6-8 attributability asymmetry exactly. ▲ **AMENDMENT RELAY (verbatim discipline): fr-FunctionInput N-4 CORRECTS banked fr-EquationView M-SB ("the ONE unbounded field") — there are TWO, and the FREE-TEXT one is the harder half. The relay is recorded at the AMENDING record; the banked file is NEVER rewritten.** Damage bounded by the banked 30 s-semaphore / 429 row |
| **F4** Server-side domain validator | **fr-EquationView M-DV** (seat-booked rider on the folded L·M-1/C·C-32) | no `model_validator`/cross-field constraint on `ComputeEquationRequest`, so `domain_start == domain_end` ⇒ period 0 ⇒ the catch-all 500 that the error envelope renders as NOTHING; reversed domains succeed with a negative period | A pydantic `model_validator`. **The client half is banked (CoefficientsPanel M-L1) — book only the INCREMENT** |
| **F5** The error envelope is actually spoken ‡ | **fr-EquationView C·D-02 ⊕ fr-ContourSettings M-15** — one server identity, two witnesses | `main.py` has ONE `@app.exception_handler(Exception)` and **no HTTPException handler**, so FastAPI emits `{"detail": …}` while `api-problem.ts` destructures `detail` away (and drops FastAPI's ARRAY-shaped `[{loc,msg,type}]`) and falls back to `response.statusText` — **under HTTP/2 `statusText` is `""`, and `"" ?? x` does NOT fall through: `error = ""` ⇒ falsy ⇒ no toast, no banner, SILENCE END-TO-END** — and the one hand-authored 422 (*"No contours extracted — try lowering min area…"*) is parked on an unread field and toasted as *"Unprocessable Entity"* | An HTTPException handler so **the RFC 7807 contract the client was BUILT FOR is actually spoken** (F.W5); the client prefers `detail ?? title ?? statusText ?? fallback` and stops discarding array `detail` (F.W3/W4). ▲ **Narrowing adopted and binding: the middleware's rate-limit 429 IS problem+json (`rate_limited()`) — v2 must NOT claim zero coverage.** Transport arm (is the browser hop HTTP/2?) → SS-13; **the static arm needs no probe** |
| **F6** A response field states WHICH population it measures | **fr-FunctionInput L-M3 ⊕ fr-ConvergencePlot L-m13** *(NOT fr-FourierShapeExtractor L-M3 at G5c; U-12 qualification)* | the Auto tooltip prints a BUDGET-TRUNCATED energy fraction directly beneath its own ≥99.99%-at-N_eff assertion (`energy_fraction = kept_energy/total_energy` over `truncate_by_budget(terms, budget)`), and the shipped defaults (budget 10, effectiveN 20–40) make the mismatch the **common case**; separately `nHarmonics` is a surviving-harmonic cap in the client and an index bound on the wire — **two denominators under one name** | **v2 states the denominator WITH the field.** Provenance carried verbatim: *"the best library-axis find in the corpus (both readers concur; the seat concurs)."* Same family as B2's amplitude mirror and E8's unit fork. F.W4 owns the copy and the naming |
| **F7** The missing original-expression field | L-M7 + C-8 (ConvergencePlot) | the *sum* and *original* tooltips are CHARACTER-IDENTICAL at the only callsite (the `?? "\\text{sum}"` fallback unreachable), the golden partial sum is labelled `f(x) =` ⟨the original⟩ — **asserting the identity the plot exists to DISPROVE** — and the string is raw SymPy through KaTeX while `latex`/`latex_sigma` arrive unconsumed on the same response | ▲ **K-13 BINDS THE CURE: `latex` is the budget-TRUNCATED series render (seat-read `equations.py:91` + the `simplify_series` docstring), so routing it to the ORIGINAL curve re-commits the same inversion. The prescribed cure is DEAD; a NEW backend field (`sp.latex(expr)`) on `ComputeEquationResponse` is the only sound one** — plus C §6's field-level-gap counting rule. F.W4 owns label honesty now |
| **F8** **THE SEAM CHOICE** — portable LaTeX | FR-EQR-4 (reader-2 missed M-1); FR-EQR-32 (cited B1) | the component's sole affordance emits INVALID LaTeX in the app's DEFAULT mode: `eqMode` defaults `"sigma"`, `activeLatex` prefers the sigma form, and `copyLatex()` copies it raw — a string embedding `\htmlClass{eq-coeff …}` that only KaTeX-with-trust consumes; all four `\htmlClass` emission sites live in the `*_sigma` renderers while the expanded renderers are clean, so **the app copies PORTABLE LaTeX in the non-default mode and UNPORTABLE LaTeX in the default one** | ▲ **Verbatim: "THE FORMING SPEC CHOOSES THE SEAM." This wave must MAKE the choice, not inherit it.** If v2 chooses the **WIRE FIELD** (hooks as a separate field), FR-EQR-32's sink-class clause becomes enforceable; if it chooses the **CLIENT STRIP**, **F.W5 RECORDS THE DECLINE** so F.W4 may proceed with `plainLatex()` beside the FR-EQR-17 singleton. Registry-swept: unbanked elsewhere |
| **F9** A parameter that does not govern | D-10 + D-L12 + C-22 (EquationPanel) | *Terms* names a quantity it STOPS GOVERNING — `budget` is never forwarded (re-verified at `latex_rendering.py:281`) | Composes with F2's shared constant and B1's under-determined key: **the SAME `budget` field is unbounded at the client, unforwarded at the server, and absent from the operation's identity.** Same family as G4c's sampling contract |

### §G — Canonical geometry: the build-time provenance contract

| clause | rows folded | witness | lock |
|---|---|---|---|
| **G1c** The moon's true source ‡ — **TRIPWIRE** | **fr-FourierShapeExtractor L-B1** | the generator has **DIVERGED FROM THE ARTIFACT IT GENERATES**: 3.7% of shipped `moon.json` points fall within 1.0 unit of any extracted point, p90 **37.6** against a 1.23-unit resample spacing, and the crescent-alone bbox strictly contains the shipped bbox by 12.97 x-max / 6.55 y-min — while the SUN is consistent. `9e5ba74` (asset-only, *"remove stars … cap at 50"*) is the divergence event the generator never received; the component still renders the six removed decorations; shipped params 50/10 vs the script's 100/12. **The site-wide dark-mode toggle renders geometry whose source no longer exists** | Provenance union: **decide the moon's TRUE SOURCE or RE-AUTHOR it.** ▲ **TRIPWIRE, verbatim: DO-NOT-REGENERATE on `master`. If any wave attempts regeneration before F.W5-W8 lands the pipeline, L-B1 and L-B2/C-2 REVIVE AT BLOCKER with the DO-NOT-REGENERATE rider as the tripwire.** The eager-payload arm is banked FR-AH-1 → F.W4 (not re-booked). *"What `moon.json` WAS generated from"* → SS-13 / **undetermined**. **Dissent: reader-1 filed BLOCKER; demoted on LATENCY — preserved WITH ITS REVIVAL CONDITION** |
| **G2c** The seam is tracked and the generator runs | **fr-FourierShapeExtractor L-B2 / C-2 (one identity) ⊕ fr-FourierShapeExtractor L-B3** | the ONLY consumer of this component's output is UNTRACKED **and** BROKEN AT IMPORT **and** PARAMETER-DIVERGED: `.gitignore:53 scripts/*` swallows both `precompute_svg_fourier.py` and `raw-contours.json`; the script imports `order_contours`, which **no module defines**; it hard-codes 100/12 vs the shipped 50/10 — a clone holds the producer and 450 KB of product with nothing between; and canonical geometry is a pure function of an **unpinned third-party PRNG with zero drift detection** | Track the seam (`.gitignore` negation lines), fix the import (`build_contour_tour(...).path`), reconcile 50/10, and **prefer the tracked in-process nav-icons idiom** for retiring the browser round-trip. ▲ **REPAIR-SHAPING CORRECTION BOUND IN (M-2, verified): `git ls-files scripts/` = 16 paths including a TRACKED, WORKING, in-process `precompute_nav_icons.py` producing 5 of the 7 `fourier-paths/*.json` assets. The correct frame is "a tracked path exists for 5 of 7; only sun/moon route through an untracked browser detour" — BOTH axes' falsifier sweeps asserted otherwise on a FALSE ENUMERATION.** L-B3's concrete 0.4.1→0.12.0 exposure is **CLOSED** (Δ 6.10e-6); the absent drift detector survives, and **tracking `raw-contours.json` IS the golden file** — L-B3 RIDES this cure, not a separate act. **Dissent: reader-1 BLOCKER on L-B2/C-2, demoted on M-2's repair-shrink; revival = the tripwire** |
| **G3c** Closure is CARRIED, never inferred | **fr-FourierShapeExtractor L-M1 (+ the 44.08 arm) ⊕ M-8 ⊕ C-3** | closed shapes are emitted OPEN under ONE convention — every branch is endpoint-exclusive, so closed shapes lose exactly one step (crescent 3.75, disc 2.45) and polygons lose a whole authored edge (diamonds 23.6–27.1%, stars 8.6–11.6%) — **and the largest instance was in NO axis table: `sun[0]`, the 20-vertex ray polygon, a 44.08-unit dropped closing flank on the page's dominant silhouette**; the mirror wrong follows (open paths lose their TERMINAL point); and `[number,number][][]` cannot express open-vs-closed while two open strokes (spiral gap 44.97, moon inner stroke 91.54) feed a closed-only downstream (`pointsToSvgPath(…, closed = true)`, all callers 1-arg) | A closure FLAG + wrap at extraction — ONE cure with C-3. ▲ **The "unambiguous discriminator" sub-claim is KILLED (44.08 closed vs 44.97 open OVERLAP — no post-hoc heuristic can recover closure), which STRENGTHENS the type complaint: the flag must be CARRIED, not INFERRED. v2 must not specify a heuristic** |
| **G4c** A parameter that governs | **fr-FourierShapeExtractor C-4 ⊕ C-14** | `samplesPerPath` is honoured by ONE of three branches — paths get 128, circles get `max(16, round(128·r/50))` (a magic 50, no constant), polygons IGNORE it — measured output `[20,123,128,4,4,4,16,16]`: **a parameter named "samples per path" produced a 4-point contour**; and the polygon branch's exact-vertex care is undone by `resample_arc_length(stitched, 512)` downstream, documented in neither file | A contract doc or a uniform post-pass. ▲ **C-14 BOUNDS C-4's geometric cost — book them together.** Same family as F9 |
| **G5c** A DIAGNOSED result type — precondition for regeneration | **fr-FourierShapeExtractor L-M3 / C-6** | THREE silent-drop gates with ZERO diagnostics on the path that DEFINES shipped geometry (`:40` sub-unit bail, `:47-49` bare catch, `:52` floor; `grep console\|throw\|warn` → 0), the caller records no counts, the exit is a bare return — while the SERVER TWIN ships `extract_contours_result` WITH diagnostics | A diagnosed result type mirroring the server twin. ▲ **This is the mechanism that makes G1c structurally UNOBSERVABLE at the human seam — the diagnostics row is a PRECONDITION for trusting any future regeneration and SEQUENCES WITH THE TRIPWIRE** |
| **G6c** A typed, named machine handoff | **fr-FourierShapeExtractor L-M5 / C-5**; cured with fr-FourierShapeExtractor C-15 (A3) | the machine handoff is dead, untyped, and hostage to a presentational node: `if (el)` wraps BOTH writes, `(window as any)` with no `declare global` anywhere, ZERO readers, and the Playwright comment is false at HEAD — while the file owns the correct ref idiom twice | A NAMED EXPORTED INTERFACE + ref emission, **decided WITH the pipeline repair**. ▲ **Corpus correction bound in: SE-05's "just delete it" is WRONG on the tree — `precompute_svg_fourier.py:4-5` names this component as its INPUT; the seam needs a real harvester or a real deletion WITH the decision, not either alone** |
| **G7c** Which stage-0 survives | **fr-FourierShapeExtractor C-7** | zero consumption of the 39-function API surface: the leaf **REIMPLEMENTS STAGE 0** of the server's contour pipeline, with no test runner in `web/` at all — the R6-8 echo at build time (runtime twin: C-5/BasisCanvas at B1) | The pipeline decision names WHICH of the two stage-0s survives. ▲ **C's honesty adopted and BINDING: the decomposition is NOT duplicated and the offline detour is ELECTIVE (`saveContour` accepts ingested contours) — v2 must not overstate the duplication** |
| **G8c** Extraction SCOPE — space and visibility (two clauses, one family) | **fr-FourierShapeExtractor L-M2 · M-11** | CTM-naive extraction (transform-blind) with `polyline` absent from the selector (latent: live SVGs use bare `<g>`s); and `extractContours` has **no paint-visibility filter** — `<defs>`/`<mask>`/`<clipPath>`/`display:none` subtrees would contribute canonical geometry — while the repo ALREADY mounts a global `<defs>` surface app-wide (SvgFilters), so **the hazard idiom is LIVE in-tree** | Two clauses, one family — **space-blindness and visibility-blindness are distinct**. L-M2 magnitude → SS-13 |
| **G9c** The guards check the wrong predicate — twice | **fr-FourierShapeExtractor M-7 · M-9** *(NOT fr-BasisSelector M-9 at D12)* | `if (totalLen < 1) continue;` is **NaN-PERMISSIVE** (`NaN < 1` is false): a NaN `getTotalLength()` emits 128 `[NaN,NaN]` samples that pass every gate and serialize as `null` pairs into the handoff; and the `points.length >= 3` floor guards CARDINALITY, not EXTENT — an `r = 0` circle yields 16 coincident points that pass and reach the decomposition, the one branch with no degenerate bail | Correct guard `!(totalLen >= 1)`; an extent predicate beside the cardinality one. **One family: the extraction guards check the wrong predicate twice.** ▲ **M-9 BOUNDS L·S-4's superlative — carry the bound WITH the superlative so v2 does not inherit a false credit.** M-7 registry-swept clean |
| **G10c** The artifact's provenance prose is false | **fr-FourierShapeExtractor L-m3 / C-10 ⊕ L-m5** | the SINGLE provenance comment is arithmetically FALSE (42·100+42 = 4242) and cites a frame-seed formula existing in NEITHER repo (`useLineBoil` takes no seed) — **the only prose explaining why the canonical geometry is what it is, wrong twice** — while the canonical seed set (42; 1,2,3; 10,20,30), the artifact's only free parameters, is scattered inline with no single knob | **Deduped: the false comment and the scattered seeds are ONE cure** — one named seed constant, one true provenance note. The provenance union's most literal row |

### 2a. Owner rulings owed — flagged INLINE (SS-4)

| # | ruling | honest default absent a ruling | cost of the other branch |
|---|---|---|---|
| **R1** | **TA-4** — restore `atomdiff.ts` + `atomDiff`, or re-scope value.js out of the diff clause (G4/E3) | **none may be assumed** — the wave may not author §6 either way | restore = value-side work; re-scope = a one-sided §6 verdict, stated explicitly |
| **R2** | **trie vs KISS** (G7/E16) | **no trie**; whole-snapshot duplication is the recorded shipped behaviour | a trie contradicts `atomdiff.py:12-14`, the incumbent guardrail in BOTH trees. **Dissent recorded** |
| **R3** | **flags producer-or-retire** (G11/D3) | none — this is **the admission gate** | **PRECEDES F.W1's sizing** wherever FR-AFP grades are load-bearing (ruling 5) |
| **R4** | **the like verb** (G10/D2) | none — *add the operation or remove the affordance; no third option ships* | the affordance lies under `aria-pressed` either way until ruled |
| **R5** | **off-state `[]` admission** (M-9, D12) | none — *admit it in the contract or stop minting it* | the silent rewrite must not survive either branch |
| **R6** | **hard-delete arm** (FR-AFP-9, D6) | copy stays truthful about restorability | `?hard=true` has zero callers today |
| **R7** | **codegen vs hand-typed twins** (FR-AFP-71, A4) | inv-16/inv-26 **RESTATED** | amendment is permitted; **silent reversal is not** |
| **R8** | **remix-vs-fork + born visibility** (E4) | none — both defaults ship, opposite | must not contradict **ruling D9** |
| **R9** | **delete-or-wire the dead session subsystem** (FR-USB-23, C3) | delete (zero external call sites) | wiring re-attaches an anonymous identity on reload |

### 2b. Dissent register — preserved, not resolved by this wave

D-L4 (R2 graded MAJOR: twelve untyped edges, no gate) · FR-USB-15 (K1's BLOCKER filing stands as recorded dissent; r2's demotion RATIFIED) · FR-AFP-8 (L axis BLOCKER; β's MAJOR sustained under K10) · FR-GFC-3 (C axis BLOCKER at two sites) · FR-GFC-1 (two BLOCKER filings, D and C axes) · GAB-17 (axes split MAJOR/MINOR/INFO; MINOR ruled) · F-β/SS-C-1 (C axis BLOCKER; demoted at component altitude) · L-B1 and L-B2/C-2 (reader-1 BLOCKER; demoted **with revival conditions** = the DO-NOT-REGENERATE tripwire) · R-4 (the trie requirement vs the documented KISS guardrail — unresolved).

### 2c. Closure tally (G19's material) — **registry-based, no carry ledger**

**LHS = the PASS-1 id-keyed census** at `docs/tranches/X/fourier/conformance/PASS-1/F-W5-CHECK.md` §1 over the 66 records at `docs/tranches/V/megatranche/registry/adjudicated/fr-*.md`: **routedTotal = 162**, read under the R-6 both-dash probe. As specified here, **162 of 162 land** — 158 homed in §2's clause tables (F-6 added at D1 by this repair), and **4 carried by citation with their homes named in §5**: AA-48 (F.W4, discharges with AA-11) · P-9 (home F.W4; F.W5 exports zero-cells; `carry/F-W4-CARRY.md:18`) · AA-44 (home F.W9/W10) · **SS-L-07 / SS-C-10** (home F.W4; `carry/F-W4-CARRY.md:234` — its type reaches these wire rows).

▲ **This tally is an AUTHORING SELF-REPORT, not the gate.** G19 greens only when unit e runs the set-difference against the **EXECUTED** documents. **No `F-W5-CARRY.md` is authored to satisfy it** (R-3.2: minting a new authority mid-repair multiplies the defect class) — the operand is the census file and the registry, both of which exist and both of which are cited by path.

---

## 3. Gates — 22, all born-RED

`$V` = `/Users/mkbabb/Programming/value.js` · `$F` = `/Users/mkbabb/Programming/fourier-analysis` (READ-ONLY).

▲ **D-19 discipline**: every fourier-side witness below is **MEASURE-AT-OPEN** — no line anchor, HEAD figure or *"LIVE at certified HEAD"* citation stands until F.W0 re-grounds the substrate (GAB-12's cell was measured on uncommitted bytes — GAB-13). Value-side witnesses (G2, G4, and G5's value leg) were **re-measured read-only 2026-08-28** and are pasted with their commands. ▲ **Consistent with the masthead gate as narrowed**: the execution gate bars **writes, builds and runs**, not reads — a born-RED witness is *definitionally* a read, and a spec that forbade reading its own witnesses would convict itself on the first line. Every command in this table is non-mutating (`ls`, `grep`, `sed`); **no witness in this file writes a byte in either tree, and none reads `$F` at all** — the fourier column is MEASURE-AT-OPEN precisely so this file never needs to.

| # | gate | born-RED witness | F.W5 close | green owner |
|---|---|---|---|---|
| **G1** | identity on the wire | `grep -rn "class AuditEntry" $F/api --include='*.py'` — four fields; pydantic extra-ignore drops `_id`; `AdminAuditLog.vue`'s `:key` is positional **because** the contract offers no identity (AA-45; AA-48 same class) | every list-bearing response class in v2 carries a stable per-row identity | F.W4 (the `:key` deletion) |
| **G2** | version-identity collision (V-β) | **measured 2026-08-28**: `$V/api/src/modules/palette/hash.ts` folds `{name, colors}` only; `repository/paletteVersion.ts` `findByHash(hash)` filters `{_id: hash}` with **no slug scope**; `insertIfAbsent` returns early on a hit (its own test asserts *"no second write"*) ⇒ two same-content palettes share one version row and the second's history is empty | v2 mandates the compound per-entity version `_id` (fourier's `f"{viz_slug}:{set_hash}"` form) | value.js API row **+ a test asserting two same-content palettes keep separate histories — none exists today** |
| **G3** | chain depth (F-α) | `grep -rn "_write_root_version" $F/api` → def + create + remix, the ONLY writer; `parent_hash=None, root_hash=set_hash_value, depth=0`; `GET /{slug}/versions` sorts by `depth` over an always-singleton set | v2 states **deepen-or-retire** for the depth/parent/root quadruple | F.W6 burn-down (X-8 cross-edge). **F.W5 owns the CLAUSE only — no double-booking** |
| **G4** ⊙ | diff-clause participation (TA-4) | **measured 2026-08-28**: `grep -rn "atomdiff\|atomDiff" $V/api/src $V/src` → **ONE** comment hit (`__tests__/palettes-forks.test.ts:9`, which names TA-4 as the excision); `ls $V/api/src/lib` → *No such file or directory*. v1 §2.5 mandates the exact file; §6's close-gate assumes BOTH probes exist | owner rules RESTORE, **or** v2 re-scopes value.js out of the diff clause EXPLICITLY with §6 re-authored to a one-sided verdict. **The wave may not assume either** | owner → value.js API row |
| **G5** | born-visibility of the derived variant | fourier remix child born `draft` (`visualization.py`, MEASURE-AT-OPEN) vs **value.js fork child measured 2026-08-28 at `$V/api/src/modules/palette/service/forks.ts:76` — `visibility: "public"` hard-coded** — opposite privacy defaults on the same verb, both shipping (lane-crud R-5) | one co-signed born-visibility rule that does not contradict **ruling D9** | both API rows **+ a create-visibility test each side** |
| **G6** | redaction parity (F-γ / R-6) | value.js collapses non-public hops to `{kind:"unavailable", ordinal}`; fourier's breadcrumb emits `slug`/`set_hash`/`author_slug`/`created_at` for EVERY ancestor — `_readable_or_none` applied only to the entry row | the breadcrumb redacts to the discriminated-union shape, **or** the asymmetry is stated with its reason | fourier API row |
| **G7** ⊙ | trie disposition (R-4) | `grep -rniE "trie\|prefix.?tree\|radix\|patricia\|structural.?sharing\|delta.?compress" $F/api $F/web/src $V/api/src $V/src` → **ZERO true hits on BOTH trees**; `atomdiff.py:12-14` is the explicit anti-tree KISS guardrail | owner rules trie-vs-KISS. Absent a ruling v2 carries the honest default: **no trie; whole-snapshot duplication is the recorded shipped behaviour** | owner (SS-4 flags INLINE; **dissent recorded**) |
| **G8** | security documentation (R3-7b) | OpenAPI security **0/45**; `save_contour(req)` takes no `Depends`/`Header`/`Request` and its router declares no `dependencies=`; blob + thumbnail GETs carry no `Depends` with `Cache-Control: public, max-age=86400` | **EVERY row of `operation-register.md` carries an authority class**; the `save_contour` and image-GET decisions are made | fourier API row |
| **G9** | client-gap closure (R3-7c ⊕ X-3) | 36 client edges / 9 gap operations; **SEVEN of 13** `/api/visualizations` operations (remix, publish, unpublish, forks, provenance, diff, versions) have zero client function — three independent counts agree | every gap operation carries a disposition (clientable / struck / deliberately server-only). ▲ **Counting lock K-1: read against 13/44, never zero; cite 45 = 30+13+1+1** | F.W1/F.W4 (clients) · fourier API row (strikes) |
| **G10** ⊙ | the like verb | `likes:int=0` + the `(visibility, likes, -id)` index + `cursors.py:22`'s `"likes"` sort key all ship; **no like route anywhere** (`api.ts` grep 0, `api/routers/` grep 0); `gallery.ts` hard-codes `const liked = true` | ship the 46th operation **or** delete the affordance — **NO THIRD OPTION SHIPS** | fourier API row, or F.W4 (deletion) |
| **G11** ⊙ | moderation producer-or-retire — **THE ADMISSION GATE** | every `db.flags` write repo-wide is a fixture or a `$rename` migration; `FlagRequest` referenced nowhere; no client posts a flag; **~110 of 285 panel lines dead by reachability**; value.js HAS the verb (`POST /:slug/flag`) | producer-or-retire **RULED** | owner → fourier API row / F.W1. ▲ **This ruling PRECEDES F.W1's sizing wherever FR-AFP grades are load-bearing (ruling 5)** |
| **G12** | unsafe GET / counter provenance | `find_one` first, `$inc {views:1}` after, `_public_doc` serialises the **pre-increment** doc; the same verb is the ETag-capture path so publish adds a phantom view and an entity is born at `views: 1` | the read is safe (an explicit verb), **or** v2 states the mutating-GET policy against **RFC 9110 §9.2.1** | fourier API row + F.W4. ▲ **repair tests must NOT assert a re-open increment (FR-GV-24 lock)** |
| **G13** | cache identity ⊇ consumed fields | `extraction_cache_key` is a CLOSED 10-field `json.dumps` literal omitting `ml_threshold`/`ml_detail_threshold`; the handler short-circuits on the hit BEFORE `compute_contours`; `auto` is the shipped default, so the control is live on every fresh workspace | the superset clause stated **verbatim** (C-25) | fourier API row. Fixture: **`contour_hash` INSTABILITY across an ML-threshold change** (also the SS-13 probe) |
| **G14** | contour provenance | editor-saved contours carry no `extraction_cache_key` (`source="editor"`), so exiting the editor re-runs extraction and the server cache-hits the ORIGINAL — the edit is destroyed with zero settings touched; the sibling panel's Harmonics nudge is the second trigger | an editor-saved contour is a first-class compute input; extraction cannot overwrite a `source="editor"` asset | fourier API row |
| **G15** | image bounds on write | `store_contour_asset` is called with **no `image_bounds`** on the POST path; the param defaults `None` and is written verbatim; backfill is GET-only — content-addressing means the overlay dies exactly when the points CHANGED | derive-on-POST or backfill-on-write is **ruled** | fourier API row |
| **G16** | canonical-geometry provenance — **TRIPWIRE** | 3.7% of shipped `moon.json` points within 1.0 unit of any extracted point; p90 37.6 vs a 1.23-unit spacing; the crescent-alone bbox strictly contains the shipped bbox; the only claimed regenerator imports `order_contours` (no module defines it) and is itself gitignored | the pipeline decision (true source named or artifact re-authored) **WITH** the diagnosed-result-type row (G5c) | F.W6 / build lane. ▲ **Until then: DO-NOT-REGENERATE on `master`; any regeneration attempt revives L-B1 and L-B2/C-2 at BLOCKER** |
| **G17** | boundary-validation evenness | the whole 45-operation client surface lands as ONE unchecked `(await res.json()) as T` (FR-CP-16); AA-47's uneven pair; `save_contour` takes `xs`/`ys` off `dict[str, Any]` with zero cardinality validation; six admin tiles render em-dashes on drift with no console error, no toast, no test | every response class v2 names is validated at the boundary **OR** stated not-validated-and-why — **evenness is the clause, not paranoia** | fourier API row + F.W4. ▲ **Do NOT author a per-field defensive sweep at F.W4 (L-19)** |
| **G18** | casing + envelope isomorphism | v1 §4:140 names TS-camel ↔ Python-snake as the ONLY allowed envelope difference and §6 binds each repo's probe to the doc, not the sibling; fourier's `DiffResponse` conforms — **value.js has NO diff surface to conform**, so the parity verdict is unrunnable (see G4) | v2 restates the casing rule as a **mechanical check on each side**; §6 re-authored; both probes exist **OR** the one-sided verdict is explicit | F.W9/W10 + value.js API row |
| **G19** | **census closure** (M-25 / COHESION §3.1) | **LHS (exists, path-cited)**: the **162** id-keyed routings of `conformance/PASS-1/F-W5-CHECK.md` §1, re-derivable from `docs/tranches/V/megatranche/registry/adjudicated/fr-*.md` under the R-6 both-dash probe, ⊕ the two `F-W4-CARRY.md` rows this wave carries by citation (`:18` P-9, `:234` SS-L-07/SS-C-10). **RHS (does not exist)**: an AUTHORED v2 + register + ruling block — `ls $V/docs/tranches/X/fourier/contract/` → *No such file or directory*, re-verified 2026-08-28. **Born-RED because the RHS is empty, not because the LHS is missing.** §2c's 162/162 is an authoring self-report, never the gate | set-difference **∅ in BOTH directions**, run on the **record-qualified** id form (R-5/U-12), against §2's clause tables + §5's carried-by-citation table. A row appearing in neither is a **SILENT DROP** — the defect class this program exists to kill. ▲ **No `F-W5-CARRY.md` may be authored to green this gate (R-3.2)** | F.W5 itself (unit e, closing) |
| **G20** | co-signature actually relayed | COHESION §2: *"cross-repo edges are declared FROM BOTH ENDS in the spec files"* — today `J-diff-shape-v2.md` does not exist and no letter has been sent (`ls $V/docs/tranches/X/coordination/` → one file, the atlas rulings relay, verified 2026-08-28); **a contract nobody received is not co-signed** | the relay letter exists at `$V/docs/tranches/X/coordination/`, is ledgered in `INBOX.md` (E13), and this spec declares this end of every cross-edge (§4) | F.W5 itself (unit e) + the fourier side's reciprocal |
| **G21** | the operation register is a FILE, not prose | no `operation-register.md` exists (`ls $V/docs/tranches/X/fourier/contract/` → *No such file or directory*); G8/G9/G19 are ungreppable without it, and the 45/30/13 triple has already been mis-stated once (census conflict C-3 — *"both sides were right on different denominators"*) | `operation-register.md` enumerates **45 = 30 + 13 + 1 + 1**, one row per operation, authority class (R3-7b) + client-edge disposition (R3-7c) | F.W5 itself (unit a) |
| **G22** | dangling-cite resolution (registry integrity) | GCM-10's cure cites a companion row **"MF-9"** that resolves to NOTHING in the adjudicated registry — `grep -rn "MF-9" $V/docs/tranches/V/megatranche/registry/adjudicated/` → exactly ONE hit, the dangling citation inside `fr-GalleryCardModal.md:56` itself (re-measured 2026-08-28); second item: fr-EasingPicker booked its easing-domain server half as "new" when RESOLVER already carried the same cure (merged at E11, both ids preserved) | **MF-9 resolved or STRUCK before v2 quotes GCM-10's cure** (the strike is the third-iteration halt in §1c); the E11 merge is recorded in v2's clause provenance | F.W5 itself (unit e) |

---

## 4. Cross-edges — declared from THIS end (one home, two citations)

F.W5 declares its half of every edge below; the reciprocal is requested via the root-authored COHESION spine, never written here (§1b). Sequencing locks land verbatim where their rows land.

| edge | direction | content |
|---|---|---|
| **X-1 · F.W0 → F.W5** | HARD opens-after | Substrate pre-gates FIRST. **The corrupt-dist fact carries BOTH its banked witnesses — canonical form: `FR-NP-32` (≡ `fr-PaperSidebar M1`)** (R-8; banked ids are original for life, the cure is cite-both, never substitute). **FR-NP-32** (`fr-NotationPills.md:35`, **BLOCKER**, *"NO-WAVE-OWNER (producer dist emitter) + glass-ui BH relay at the TOP of the FR-COB-28 packet; F.W1 SEQUENCING GATE"*) is the stronger reproduction — *"`vite dev` and `vite build` are RED at the `^4.0.0` pin"*, reproduced with the app's own toolchain; **fr-PaperSidebar M1** is the parse census of the same emitter defect. **Both travel; neither is dropped for the other.** Also: the fourier worktree is dirty — uncommitted bytes no commit holds (fr-GalleryAdminBanner **GAB-13**); F8-REACH-01/02. ▲ **G-11/G-12 QUOTATION (R-9.1)**: this spec's anchor and denominator cells are **input to / quotation of F.W0's G-11 anchor table (`F-W0.md:199`) and G-12 denominator table (`F-W0.md:204`), published in `SUBSTRATE-LEDGER.md` (`F-W0.md:63`)**; divergence from those tables is a defect against G-11, not a rival act — F.W5 re-performs no re-resolution as its own act. Until F.W0 re-grounds, **every fourier-side anchor in this spec is MEASURE-AT-OPEN (D-19)** — GAB-12's "LIVE at certified HEAD" cell was measured on uncommitted bytes and is not HEAD truth; fr-BasisSelector M-9's and other line anchors re-resolve after F.W0 |
| **F.W5 → F.W1** | emits + sequencing | FR-AFP-18's render seat (metric-badge→metric); FR-AFP-33's client collapse. ▲ **G11's ruling PRECEDES F.W1's sizing** wherever FR-AFP grades are load-bearing (ruling 5). ▲ F.W1 is ONE atomic land-or-lose transaction — **the ELEVEN-limb roster chartered at `F-W1.md:276`, incl. the FR-EQC-7 vaul-vue manifest gate INSIDE per `:294` — cited whole, never restated here.** (The four zero-console-error e2e gates are its failure semantics — fr-EquationResult FR-EQR-3.) **F.W1 must NOT be credited for the pagination-drain cure (FR-GIG-5)**. **F.W1 precedes F.W3/W4 (§2 law)** |
| **F.W5 → F.W2** | exports zero-cells (P-9) | The measured negative roster copies into F.W2's denominator: App API-inert · MobileFloatingToc · CollapsibleSection · PaperSearch/PaperSearchInput/PaperSearchDropdown · MorphShapePreview (structurally unreachable) · CanvasControlsDock/AnimationControls zero migration cost · **UserSlugBar FR-USB-38 — do NOT book into the F.W2 migration surface**. One home (F.W4), two citations; evidence never a denominator |
| **F.W5 → F.W3/W4** | emits display/render arms | AA-45's `:key` deletion (AA-48 discharges with AA-11) · AA-6 placeholder honesty · AA-10 actor legend · AA-5/AA-24 display taxonomy · GCM-10's `v-else-if` guard · GAB-15/-17 label honesty (GAB-17 AFTER GAB-16) · FR-AUL-21's visible unknown-status render · FR-AUL-25/FR-AFP-9 truthful copy (diction rides FR-AFP-35) · FR-AFP-8's one-token `item.slug` label · the like-verb `aria-pressed` + re-click guard · FR-AFP-32's shared formatter (**banked FR-AUL-17 → F.W3, do NOT re-book**) · B-1's unit-true control · M-10's typed vocabulary · ContourPreview row 11's `?:` (NOT presuming row 13's seam shape) · FR-GFC-1's in-place patch (D-1 skeleton KILLED, K5) · FR-EQR-4's `plainLatex()` **iff F.W5 declines the wire field** · FR-USB-13's attribute rework (not credited to the contract). F.W4-side same-commit riders named **by their BANKED ids** so emission does not strip them and G19's id-keyed set-difference can find them (anti-rename; pre-adjudication axis ids collide across records and do not survive it): **fr-PaperSearchModal PSM-1** (`:34`, = D-1 · L-1 · C-1) ⊕ **PSM-4** (`:37`, = D-3 — *"Must land in the same wave as PSM-1 or the cure ships a WCAG regression"*) ⊕ **PSM-13** (`:49` — the `outline:none` 2.4.7 rider, *"MANDATORY rider inside the PSM-1 commit"*; the record's own verdict at `:140` names the pair as *"(PSM-4, PSM-13)"*); PAW-44/LAW-3 restore-only-with-background-cure; MPC-31/MPC-3⊕10⊕13⊕8⊕22 one-cut law; FR-MSP-6 two-channel lock |
| **F.W5 → F.W6** | emits burn-down | F-α's deepen-or-retire execution (G3); the G16 canonical-geometry pipeline WITH G5c's diagnosed result type; **DO-NOT-REGENERATE stands until F.W6 lands the pipeline** (tripwire: L-B1 + L-B2/C-2 revive at BLOCKER) |
| **F.W5 → F.W7** | states, never books | **F.W7 CITES; F.W5 STATES.** The trie question is this wave's clause **E16** and gate **G7 ⊙** — F.W7 owns the owner-gated design that follows a ruling and **books no registry row of its own** (its routing census is a measured **∅**, R-11). The dissent travels with the clause and is banked HERE, at **`F-W5.md` §2 clause E16** ≡ **`F-W10.md:116`** (*re-measured by this repair seat 2026-08-28: the ruling's `:114` is stale by two lines; the LAW is applied to the live bytes*), verbatim: *"the trie requirement collides head-on with a standing KISS guardrail present in BOTH trees — `atomdiff.py:12-14` is the INCUMBENT — and the owner ruling PRECEDES design."* Absent a ruling, the honest default both waves carry is identical and stated once here: **no trie; whole-snapshot duplication is the recorded shipped behaviour.** ▲ **Line-drift disclosure (E-3 / D-19)**: `F-W7.md:120` anchors E16 at `F-W5.md:153` and G7 at `:226`; **this repair moved both** — cite E16 and G7 **by clause and gate id**, never by line. Declared from this end so the keystone's consumer is not undeclared. Reciprocal requested: `F-W7.md` |
| **F.W5 → F.W8** | the RECIPROCAL, declared from this end | F.W5 STATES the clauses; F.W8 ASSERTS them (fixture/e2e direction). ONE home (F.W5), two citations — the shared ids are enumerated at `F-W8.md` §1's census. No F.W8 row re-books a clause; no F.W5 clause claims F.W8's assertion work. Reciprocal: `F-W8.md` §5c. ▲ **Set-difference precondition (R-5)**: the shared ids run in the **record-qualified** form on both ends (`fr-BasisCanvas C-17`, `fr-EquationView B-1/B-2`, `fr-ContourEditorCanvas C-2`, `fr-FunctionInput L-B1/L-M3`, …) — the U-12 eight (**M-13 · L-B1 · L-M3 · C-17 · C-18 · B-1 · B-2 · C-2**) are ambiguous bare and cannot be differenced. F.W8's three required artifacts are §1a's `create` rows, cited **WHEN-created** |
| **F.W5 → F.W9/W10** | emits conformance obligations | v1 §6 re-authored by v2 and **executed there**, each side's probe asserting against the DOC, never the sibling (inv-26); **FR-AUL-13's anti-repeat probe** (a contract document without a probe repeats the defect it convicts); **AA-44**'s one-sided test seam (zero frontend tests; nine e2e specs never touch the audit tab); GAB-12's test seam; FR-AFP-66's orphan-flag coverage (the existing test exercises only the reporter-keyed path); G18's mechanical casing checks |
| **F.W5 → SS-4** | is its input | SS-4 **FLAGS THE OWNER RULINGS INLINE** (R1–R9, §2a: TA-4 restore-or-rescope · trie-vs-KISS · like verb · flags producer-or-retire · off-state `[]` · hard-delete arm · codegen-vs-twins · remix/fork+born-visibility · dead session subsystem) and **NAMES THE TA-4 VALUE-SIDE ATOMDIFF RESTORATION AS PREREQUISITE OR RE-SCOPES EXPLICITLY** (charter law, verbatim). GIG's orphan-client class rides here as census methodology (NO-WAVE-OWNER → SS-3/SS-4) |
| **F.W5 → SS-6 (BH relay)** | producer routing | **Glass-producer rows go to the BH relay, NEVER become frontend hacks** — any producer-side finding surfacing during contract authoring is relayed to the active glass-ui BH inbox, not specified as a demo-side workaround |
| **F.W5 → SS-13** | probe ledger | Deployment auth-proxy (C2) · JSC acceptance of the non-ECMA date form (D17) · the HTTP/2 transport arm (F5; **the static arm needs no probe**) · FR-AUL-14 production incidence (D13) · L-M2 magnitude (G8c) · M-14's end-to-end arm (E14) · FR-GSB-28 symptom magnitude (B1) · D11 magnitudes · E6 observable magnitude · `contour_hash` instability fixture (E13). ▲ **K12: SS-13 spends NO probe on E5 — closed statically** |
| **F.W5 → value.js API row** | value-side obligations, never fourier defects | V-β compound version `_id` + the two-histories test (G2) · V-γ attribution (`userSlug` null ⇒ no version row, E18) · TA-4 per G4's ruling · fork born-visibility (`forks.ts:76`) + create-visibility test (G5) · G18's value-side probe · D9 reconciliation: value.js persists 3-state visibility (`public`/`unlisted`/`private`, `model.ts:61`) — the obligation list reconciles it, the contract does not silently overwrite it |
| **FORBIDDEN** | — | **Direct parse-that→fourier routing is FORBIDDEN** — any parse-that-derived material reaches fourier only through value.js-authored specs and letters |

---

## 5. Carried by citation — the excluded-from-clause-tables set (G19's other operand)

Four rows are carried **by citation with their homes named**, not homed in a §2 clause. They appear here so G19's set-difference finds them; none is a silent drop. Every source coordinate below `ls`-resolves and was re-read at the bytes 2026-08-28.

| id | disposition | home |
|---|---|---|
| **AA-48** (`fr-AdminAuditLog.md:91`) | Recorded at A1 (same latent class as AA-45 `:88` — raw `<button>`s with no `type`, inert today), **NOT re-booked**; discharges FREE with AA-11's footer cure | F.W4 |
| **P-9** (`carry/F-W4-CARRY.md:18`) | The negative-control law: zero-cells are **EVIDENCE, never a denominator** (B5); F.W5 EXPORTS the zero-cell roster into F.W2's and F.W4's denominators — one home, two citations. **A carry-ledger id, not a registry id** — it occurs in none of the 66 records, which is why its denominator is stated separately from the 162-row census (§2c) rather than folded into it | F.W4 (body) + F.W2 (copy) |
| **SS-L-07 / SS-C-10** (`fr-SpeedSelect.md:59` ≡ `carry/F-W4-CARRY.md:234`) | Banked routing verbatim: **"F.W4** (+ its type reaches the F.W5-W8 wire rows)". The **body is F.W4's** — the literal-union speed catalog (`lib/speeds.ts` / `SpeedName`, the `EASING_OPTIONS`/`AnimationEasingName` idiom one file away). **F.W5's share is the type-reach only**: the speed domain arrives at the wire as `speed: number` on both sides, so it is admitted to the operation record under **D12**'s closed-domain law and **E11**'s hoist form (`Literal[…]` on `AnimationSettings.easing` — the same treatment, the sibling field). Booked by citation, not re-booked: the record's own cure lands at F.W4 and **F.W5 claims no credit** (FR-GIG-5 bar). Named here by both ids so G19's id-keyed difference resolves them | F.W4 (body) + F.W5 (type-reach, by citation) |
| **AA-44** | The one-sided test seam: zero frontend tests; nine e2e specs never exercise the audit tab; the backend pins its half — why AA-5/AA-6/AA-24 could open and stay open. F.W5 emits the conformance obligation only (§4 F.W9/W10 edge) | F.W9/W10 |

---

## 6. Close (unit e checklist — all `planned`)

1. G19: run the set-difference both directions — **the 162-row PASS-1 census roster (`conformance/PASS-1/F-W5-CHECK.md` §1, re-derived from the 66 records under the R-6 both-dash probe) ⊕ the two `F-W4-CARRY.md` citations (`:18`, `:234`)** ↔ §2's clause tables + §5 — against the AUTHORED v2/register/rulings, on the **record-qualified** id form; ∅ or halt (third failure → triumvirate). **No `F-W5-CARRY.md` is authored (R-3.2).**
2. G22: resolve or STRIKE MF-9 before v2 quotes GCM-10's cure; record the E11 merge in v2's provenance.
3. G20: write the relay letter, append the E13 INBOX row, request the fourier reciprocal.
4. Commit discipline: pathspec commits only; `scripts/dev/dev.sh` never staged; each unit commits before the next opens.
5. No wave closes with UNREAD mail (E13 4-path sweep at open and close).

**Status: planned.** The specs await the owner's begin-word.
