# X.F.W5 — The shared-provenance API contract (ADMISSION KEYSTONE)

**Sub-tranche**: X·F (`docs/tranches/X/fourier/waves/`), authority `docs/tranches/X/COHESION.md` §0/§1 SS-4, §0a boundary 2026-08-25. Sub-tranche dir name **confirmed by measurement 2026-08-25**: `docs/tranches/X/` holds `waves/` (X·V), `parse-that/` (X·P), `coordination/`, `COHESION.md` — `fourier/` is absent and is created by X·F formation; COHESION §0 already names `docs/tranches/X/fourier/waves/` as the home, so the boundsFiles path stands unchanged.

**Fold provenance (M-23 / M-12)**: this file is the fold-seat output, 2026-08-25 — the Opus author half (Draft A) verified row-for-row against the CARRY authority and repaired where dropped. Verification receipt: **112 CARRY rows delivered to the fold seat** (delivery truncated mid-row `L-B2 / C-2 (one identity)`); **112 of 112 carried, zero silent drops**; the §G tail ids beyond the truncation point (G3c–G9c row set) are carried from the author half's full-CARRY authoring and re-verify at G19's set-difference run against the 126-row full set. Neither authoring half is the wave; this fold is the wave's spec.

**EXECUTION IS NOT AUTHORIZED BY THIS FILE.** Tranche X's execution gate stands: nothing opens, writes, builds, or runs product source in either repo until the owner's begin-word. `/Users/mkbabb/Programming/fourier-analysis` is **READ-ONLY, always** — this wave writes zero fourier bytes; the co-signature travels as a letter. Specifying is not doing.

## 1. State

**Name**: W5 — The shared-provenance API contract (ADMISSION KEYSTONE)
**Opens after**: **F.W0** (substrate pre-gates — hard, see §5 X-1) · the intake CARRY table (`INTAKE-ADJUDICATION-2026-08-03.md` §3 F.W5 + §2 C-3) · the adjudicated registry whole (66/66 fourier at `35fc8ebf`)
**Agents**: 5 **serial** (units b/c/d all write `J-diff-shape-v2.md`; §3a forces sequencing, never parallelism)
**Hard gate**: 20 conditions in §5 — 18 carried from the CARRY table verbatim in id, 2 added for carry-closure and edge reciprocity. **All 20 born-RED**; 2 are OWNER-GATED and may not be authored unruled (G4, G7).
**Status**: **planned**

### Four-verb status (separate fields; this file moves exactly one)

| verb | value | evidence |
|---|---|---|
| AUDITED | **YES** | 24 of the 66 adjudicated `registry/adjudicated/fr-*.md` records (this wave's witnesses) · `formation/fourier/lane-crud.md` (R-1..R-7, §6, §7, F-α/β/γ, V-α/β/γ) · `INTAKE-ADJUDICATION-2026-08-03.md` · `api-gap-remeasure.md` |
| SPECIFIED | **YES — by this file (the fold), 2026-08-25** | validated against `docs/precepts/instructions/tranche/WAVE_SPEC.md` (L-20, current at authoring); the X·F L-20 conformance loop closes the sub-tranche stamp |
| IMPLEMENTED | **NO** | — the contract bytes land only after the begin-word |
| VERIFIED | **NO** | — stamped only at X·F's sub-tranche release close; no wave stamps VERIFIED at its own close |

### 1a. Goal criterion

This wave succeeds if, when work ends, **the shared-provenance contract is a neutral document that both repos co-sign rather than a fourier-tree file value.js binds to by reference** — and if every clause in it is either *ruled* (with the owner's word where a ruling is owed) or *explicitly declined with its reason*, so that no later wave can repair a defect this contract silently reproduced. The wave states law; it burns down nothing. A clause that names no witness is not a clause.

### 1b. What this wave is NOT

It does not repair. It authors three documents and one letter. Every render half, every client repair, every server cure it names is handed to F.W1 / F.W3 / F.W4 / F.W6 / F.W9 / the value.js API row with **one home and two citations** (§6). F.W5 claims credit for none of them — the mirror of FR-GIG-5's lesson, adopted as a standing bar.

## 2. Bounds

### 2a. File Bounds

| File | Access |
|---|---|
| `/Users/mkbabb/Programming/value.js/docs/tranches/X/fourier/waves/F-W5.md` | create (this spec — the arbitrated fold) |
| `/Users/mkbabb/Programming/value.js/docs/tranches/X/fourier/contract/J-diff-shape-v2.md` | create — **the neutral co-signed contract** |
| `/Users/mkbabb/Programming/value.js/docs/tranches/X/fourier/contract/operation-register.md` | create — the 45/30/13 enumeration: one row per operation, authority class + client-edge disposition. G8/G9/G19 grep it; without it those gates are prose |
| `/Users/mkbabb/Programming/value.js/docs/tranches/X/fourier/contract/OWNER-RULINGS-F.W5.md` | create — the inline ruling block SS-4 requires |
| `/Users/mkbabb/Programming/value.js/docs/tranches/X/coordination/value-to-fourier-2026-08-DD-cosign-J-diff-shape-v2.md` | create — the co-signature relay letter (the only vehicle by which fourier receives v2) |
| `/Users/mkbabb/Programming/value.js/docs/tranches/V/coordination/INBOX.md` | **append-only** — one E13 ledger row for the relay; no rewrite of any existing row |

**Bounds reconciliation against the CARRY `boundsFiles`.** Rows 1–3 adopted verbatim. Two additions, each because a gate would otherwise be unfalsifiable prose: `operation-register.md` (G8/G9) and the relay letter (G20 — a contract nobody received is not co-signed). `INBOX.md` is E13's standing duty, not new scope.

**Do NOT touch — every path below, without exception:**

- `/Users/mkbabb/Programming/fourier-analysis/**` — the whole tree, read-only law. **v1 (`docs/tranches/J/design/J-diff-shape.md`, 271 lines) is IMMUTABLE**; v2 supersedes by reference and never edits it (E-3: addenda, not patches).
- All product source both repos: `value.js/api/src`, `value.js/src`, `value.js/demo`, fourier `api/`, fourier `web/` — execution gate.
- `docs/tranches/V/megatranche/formation/fourier/lane-crud.md` · `audit/codex-provenance/INTAKE-ADJUDICATION-2026-08-03.md` · `audit/codex-provenance/api-gap-remeasure.md` · `registry/adjudicated/fr-*.md` (all 66) — **E-1/E-3 immutable beside the spec**; they hold the witnesses, and rewriting one destroys the evidence a gate cites.
- `docs/tranches/X/COHESION.md` — **excluded deliberately.** The SS-4 status cell and the §2 reciprocal-edge line are root-authored (Fable) at the boundary. F.W5 declares its half of every cross-edge in §6 of this file and requests the reciprocal; it does not write the spine. (WAVE_SPEC prohibition: no hidden cross-wave write conflicts.)
- `scripts/dev/dev.sh` — unowned dirty row, never staged.

### 2b. Disjointness

Units **b**, **c**, **d** all write `J-diff-shape-v2.md`. Per WAVE_SPEC §4a they are **sequenced across sub-waves**, not parallelised: `a → b → c → d → e`, each committing before the next opens. Unit **a** owns `operation-register.md` alone; unit **e** owns `OWNER-RULINGS-F.W5.md`, the relay letter, and the INBOX row alone. No other X wave writes any path in §2a while W5 is open. **No worktree plan is needed** — one writer at a time on clean `tranche-u`; a dirty tree at handoff halts the wave.

## 3. Scope

1. Author `J-diff-shape-v2.md` in the **neutral home**, superseding v1 by reference — v1's §1 self-locates in fourier's tree ("it lives in fourier's tree; value.js binds to it by reference"), and that single-sidedness is the defect v2 exists to correct.
2. State the clause set of §4 — 7 sections, ~60 clauses, every one carrying its named witness — with each clause either RULED, DECLINED-WITH-REASON, or OWNER-GATED. No clause without a witness; no witness without a clause.
3. Enumerate the operation register: **45 = 30 public-non-admin + 13 admin + 1 app + 1 gallery**, per-operation authority class (R3-7b) and client-edge disposition (R3-7c: 36 edges / 9 gaps; clientable / struck / deliberately server-only).
4. Re-author v1 §6's close-gate to bind **each side's probe to the document, never to the sibling** (inv-26), with the one-sided verdict made explicit if G4 rules re-scope.
5. Restate v1 §4:140's casing rule (TS-camel ↔ Python-snake, the ONLY allowed envelope difference) as a **mechanical check on each side**, not prose.
6. Author `OWNER-RULINGS-F.W5.md`: the seven rulings owed, each with the honest default carried absent a ruling, and the cost of each branch.
7. Relay v2 + the ruling block to the fourier side as a letter for co-signature; ledger it in INBOX (E13).
8. Emit the value-side obligation list (V-α / V-β / V-γ / TA-4, cross-checked against `api-gap-remeasure.md`'s nine classes + X-1/X-2/X-3 + D9) to the value.js API row as **value-side rows, never re-booked as fourier defects**.

**Prohibitions.** No fourier byte. No product byte. No trie design absent G7's ruling (§4 §E16 carries the documented default instead). No clause that contradicts a ruled owner decision (D9: visibility `private|public`; lifecycle `active|trashed`; moderation `clear|withdrawn`). No HEAD figure or line anchor cited before F.W0 re-grounds (D-19). No glass-producer row smuggled in as a contract clause (§6). No parse-that→fourier routing.

### 3a. Triumvirate Dispatch

Mandatory (research + plan augment + redress); the orchestrator may not redispatch the failing unit alone.

- **Bounds expansion that invalidates the wave**: any write to a fourier path; any write to `registry/adjudicated/**`, `lane-crud.md`, or the intake adjudication; any product-source byte; any edit to v1.
- **Gate failures not local-edit-recoverable**: **F.W0 fails to re-ground the substrate** (§5 X-1 — then every anchor in this spec is unciteable and the wave halts, it does not proceed on stale bytes); **G4 or G7 returns no ruling** and the wave cannot state the honest default without contradicting a standing law; **G19 set-difference is non-empty in either direction** after two closure passes (a silent drop is the defect class this program exists to kill).
- **Diagnostic loops whose third iteration halts**: the operation-register enumeration disagreeing with the 45/30/13 triple a third time (K-1's prefixed-router blindness is the known trap — a `@router.` grep is blind to `@gallery_router.get`); the carry-closure reconciliation failing to home a row three times; the MF-9 dangling cite failing to resolve (then it is **struck**, and v2 quotes GCM-10's cure without it).

## 4. Carry — the agglomerated clause set

**126 banked rows → ~60 clauses, 7 sections.** Dedupe is by identity, not by text: where two records witness one mechanism, the clause carries **both ids** and books once. Cross-listed ids appear in more than one clause **by citation**, never by re-booking. `‡` marks BLOCKER-weight. `⊙` marks OWNER-GATED (unauthorable unruled). `▲` marks a cure-integrity lock that binds the *repair*, not the finding.

*Fold-seat verification note: the 112 CARRY rows delivered to the fold seat were checked id-for-id against §4 + §5 + §6 + §7 — all 112 land, zero silent drops. The remaining tail (the §G row set beyond the delivery truncation at `L-B2 / C-2`) re-verifies at G19.*

### §A — Wire shape (17 rows → 6 clauses)

| clause | banked rows folded | witness | lock / cure-shape |
|---|---|---|---|
| **A1** Row identity on the wire | AA-45 (+**AA-48** as the same latent class) | `AuditEntry` carries four fields; pydantic extra-ignore drops `_id`; `AdminAuditLog.vue`'s `:key` is positional because the contract offers no identity | Every list-bearing response class carries a stable per-row identity. The `:key` deletion is **F.W4's act**, the clause is F.W5's. AA-48 (raw buttons' missing `type`) **discharges free** with AA-11's F.W4 footer cure — recorded, **NOT re-booked** |
| **A2** Pagination/cardinality ruled once | AA-46·C·D-13/C·D-14 · FR-AFP-18·D-10 (cited) · FR-GFC-4 (cited, primary D1) | `page`/`pages` discarded and re-derived client-side; the moderation queue has no size anywhere; "featured" is window-scoped | One envelope decision settles all three. Raw material carried: fourier opaque cursors + `Link: rel=next` (`lib/crud/cursors.py`) vs value.js offset/limit + `total` (`platform/http/pagination.ts`) — lane-crud §6 |
| **A3** Boundary-validation **evenness** | AA-47·C·D-17 · FR-CP-16 (=C-17/L·A-3) · GAB-12·C-7 · GCM-55 · GCM-10·D-07/L-5/C-3 · FR-IC-6 (folds → L·m-7) · ContourPreview row 28·D:i-3/L:L-8 · FR-FG-13=C-9 · FR-FG-21=LC-missed-2 · C-15 (FSE, build-time) | **The measured denominator: the whole 45-operation client surface lands as ONE unchecked `(await res.json()) as T`.** AA-47's uneven pair (timestamp+target guarded; action+ip_hash not) is what survives the pydantic guarantee | One posture per response class — *validated* or *explicitly-not-validated-and-why*. **Evenness is the clause, not paranoia.** ▲ Do NOT author a per-field defensive sweep at F.W4 (L-19). GCM-10/GCM-55 = the serialization-bypass family: the response goes through the model or the model is decoration. FR-FG-13 keeps C's own "today it is safe" honesty — a *latent-ordering* clause, not a live defect. The rule covers **build-time artifacts** too (C-15), not only HTTP |
| **A4** One contract source | FR-AFP-36·L-16/L-21/C:D-20 · FR-AFP-71·β-miss-9 ⊙ | The emitted `content_hash` — the actual join key — is omitted client-side; three dead server models encode the retired contract; **no `response_model` anywhere**. Structural cause: god-module twins (`api.ts` 672 L / `types.ts` 391 L), no codegen | Cure as ONE unit or the drift returns. ▲ **K9: do NOT delete the five `?? item.slug` fallbacks** — `admin.py` emits `doc.get("image_slug")` unvalidated, so they are defensive. ⊙ **Module split is the union's call**: inv-16/inv-26 ("one contract source, hand-typed twins, no codegen") is the standing law v1 §1 asserts — v2 **restates or amends, never silently reverses** |
| **A5** No untyped operations | M-13 (CoefficientsSpectrum)=C-7 · FR-EQC-8=C-11 · FR-NP-30b | The epicycle response is `data: dict`; the only typed component model is dead and disagrees with the wire; the `notation` enum is enumerated **four times** with a silent Python `.get(…, render_trig)` fallback | Verbatim clause: **"an operation's response model must be structurally typed or the leaf is unjoinable in principle."** Load-bearing for all of §B — if v2 admits untyped operations, every join clause it writes is unenforceable at those operations. **Name the exception set explicitly or close it.** FR-EQC-8 cites M-13 as family (no re-booking); a silent fallback is a contract decision |
| **A6** Credential transport | FR-USB-37·C-i2 (+ FR-GV-7, cited, primary C1) | Bearer in a `title` attribute on a globally mounted element on every route; bearer accepted from the **URL query string** | ONE transport clause bounds both ends (URL in, DOM out). ▲ FR-USB-13's F.W4 cure will likely rework the attribute anyway — **the F.W4 arm must not be credited with the contract** |

### §B — R6-8: operation identity independent of client identity (15 rows → 5 clauses)

| clause | banked rows folded | witness | lock |
|---|---|---|---|
| **B1** The join relation (forward) | **R6-8** (intake CARRY, ADOPT-AS-FACT) · FR-AUL-3·L-3/C-M1 · FR-GSB-28 · M-CK · C-5 (BasisCanvas) · C:C-12 (ImageUpload row 26) · D-L4+C-7 (ConvergenceLegend) · FR-EQR-32=C-§0.4 · AA-31 (cited D1) · C-7 (FSE, cited G7c) | `adminFetch` keys the abort registry on the full query-bearing path → per-operation cancellation silently degraded to per-URL for **13** admin operations; its dual keys **one literal** `"listVisualizations"` for both `resetAndFetch()` and `fetchNextPage()`; `computeKey()` returns 4 fields while the POST carries 7 | **Carry the identity ONCE and cite the instances, or the wave re-books law as work (M-25).** Cure = stable per-operation abort keys. FR-AUL-3 ∥ FR-GSB-28 = **one clause, two directions** (under- and over-broad). ▲ **M-CK SEQUENCING LOCK (verbatim): "fix the key FIRST or the B-1 repair ships broken"** — both prescribed B-1 cures walk into the under-determined key. The Map-growth rider stays INFO. ImageUpload = the **incomplete** join (5 un-clienting `<img src>`; the security half is C2's). D-L4 ruled MINOR on the ConvergenceTimeline L·D-12 precedent — **dissent preserved** (R2 graded MAJOR: twelve untyped edges, no gate) |
| **B2** The mirror direction ‡ | C-7 (ConvergencePlot) | Two contradictory `amplitude` conventions in one view, exactly 2× apart, both labelled `A`: the DTO ships `\|c_n\|`, `groupTrigHarmonics` re-derives `√(a²+b²)=2\|c_k\|` under the same name — tooltip `A=0.8106` vs pill `A_1=0.4053` | **The row that makes R6-8 bidirectional in v2. Do NOT collapse it into the forward direction.** A client may not shadow a server field with a same-named different-valued re-derivation |
| **B3** Wrapper types re-derived FROM the router | FR-AUL-11·C-M2 · FR-AUL-12·C-M3 · FR-AUL-13·C-M4/L-11/L-23 | `deleteAdminUser` declares two keys the router **never emits** (a transposition); `setAdminUserStatus` invents `slug` and drops `ok`; `BatchResponse.errors` is never populated so partial batch failure reports **green**, and `affected` = `modified_count` toasts "Unsuspended 0 user(s)" as success | One repair family. The binding indictment: `api.ts:617-624` is a **dated contract-repair record** for the batch family, and the two singular wrappers twenty lines above were left declaring shapes the router never emits — in the same pass. ▲ FR-AUL-13 verbatim: **either the router populates `errors`/`matched_count` or the contract drops them; the client compares `affected` vs `slugs.length` either way.** Its "written against a contract DOCUMENT rather than the router" cell is **this wave's own indictment — v2 IS such a document**, so it ships with the F.W9/W10 conformance probe or it repeats the defect |
| **B4** Liveness + projection disposition | PP-DEADSEAM·C-12 · M-β4 · L·m-6=C·D-14 ⊕ FR-EMT-20 (cited F1) · GM-M4 · VV-R2-B (cited D1) | `preview_path` written `""` at all three server sites while the client leaf is unreachable — **zero implementation on either side**; `EpicycleData.trace` (3000 samples per response) has zero readers; `softDelete`/`restore` are the gallery store's only ETag/If-Match round-trip and have **zero callers** | **The LIVENESS predicate is genuinely new to the registry** (both readers' sweeps + the seat's, over 206 records) and is PP-DEADSEAM's value: v2 needs a predicate for *"declared on both sides, implemented on neither."* Separate from the produced-and-unconsumed disposition (M-β4, L·m-6) — v2 states **one** disposition for produced-and-unconsumed fields, not three ad-hoc deletions |
| **B5** Negative controls — **EVIDENCE, never a denominator** | HLG-23·C:C-12 · C §3 negatives (MorphShapePreview) | `/morph` reaches **ZERO** of the 45 operations; its static-JSON seam is `as any`-cast; R6-8 and R3-7b are *structurally unreachable* there | "A route with no operation edge has no join to get wrong." **Must appear in v2's clause justification and must NOT be counted in any defect denominator** (P-9). The cast itself folds → banked FMD-24 → N-14/N-15/FR-AH-1 (F.W4). The zero-cells copy to F.W2/F.W4 denominators — one home, two citations |

### §C — Authority (10 rows → 5 clauses)

| clause | banked rows folded | witness | lock |
|---|---|---|---|
| **C1** Explicit authority class per operation ‡ | **R3-7b** (intake CARRY) · C-6/C-7 (EditorControlsDock) + D-12 idempotency rider · GCM-52·C-15 · FR-AFP-4 (cited C2) · F-4 (cited C2) · FR-GV-7=M-9/C·M-9 | **OpenAPI security 0/45** — auth parity cannot be *specified* over a surface that documents none; `POST /api/contours` is an unauthenticated write with a client that **always** authenticates | Identity stays with the intake at every instantiation site (all four records say so). ▲ EditorControlsDock verbatim: **"the asymmetry, not an exploit, is the defect"** — v2 must not over-state it; the write is content-addressed and abort-keyed, so the data effect is bounded, and it is **NOT** the sibling's publish-BLOCKER. D-12's rider: the declared-but-unpassed `Idempotency-Key` channel at the same seam |
| **C2** Image remediation contract ‡ | FR-AFP-4·C:D-2 (+β janitor sharpening) ⊕ F-4=C-C-8 (GalleryDraftsSection) · m-15=r2-missed-3 · GCM-52 (cited C1) | **No action the moderation panel offers takes the reported artifact offline**: soft-delete is of the *visualization*; `images.py` = 7 ops with **zero DELETE**; blob GET has no `Depends` and ships `Cache-Control: public, max-age=86400`; **every anonymous blob GET ends in `touch_document`, so actively-fetched reported content is precisely what never reaps** | Delete/quarantine + blob visibility gate + **janitor predicate** + thumbnail versioning, as one contract. ▲ C's scope discipline verbatim: **soft-delete DOES de-list from browse.** F-4 contributes the 2²⁸ slug-entropy arithmetic and the drafts framing (the PRE-publication surface rides a public cache). ▲ **m-15 is CROSS-REFERENCED, NOT MERGED** with F-4: F-4 books *who can fetch*, m-15 books *what they see* (unversioned thumb + 24 h cache negates the deliberate regeneration). Deployment auth-proxy question stays UNPROVEN → SS-13 |
| **C3** Session truth | FR-USB-15·L-1/C-§0/C-7 · FR-USB-23·L-10 ⊙ · FR-AH-6·C-6 | `isLoggedIn` is a localStorage-**presence** check; `getMe` has zero call sites; `grep "401"` over `web/src` returns **ZERO**; the admin badge reads a plain ref written at 2 sites and never rehydrated while `isAdminAuthenticated` has zero consumers | 401 handling + `getMe` revalidation **at the store/api seam** — the defect is authored in the STORE; **do not route the cure to the leaf.** Two sources of truth for one predicate: the admin-auth seam owns which is canonical. lane-crud §R-7 adopted: v2's session clause takes **the value.js form** (SHA-256 digest at rest; fourier persists the raw UUIDv4 as `_id`). ⊙ FR-USB-23's delete-or-wire decision → owner block. **Dissent preserved**: r2's K1 demotion of FR-USB-15 from BLOCKER is RATIFIED; the BLOCKER filing stands as dissent |
| **C4** Retry policy | FR-USB-24·L-11 · D-L5+C-1 (EquationPanel, cited F9) | Login opts into the silent ≤60 s 429 auto-retry against a 5/60 s budget with no feedback and no cancel; Escape doesn't abort | Auth mutations pass `retryOn429: false` or surface the wait. ▲ **CURE LOCK on any retry change** (D-L1/C-23): the 429 loop reuses ONE controller because the signal is hoisted outside `while(true)` — **a naive request-scoped rewrite breaks that property** |
| **C5** Redaction parity | lane-crud **F-γ** / R-6 (+GCM-52's shape, cited) | value.js collapses non-public hops to `{kind:"unavailable", ordinal}` and states *"No raw document or lineage field … ever crosses the wire for a non-public hop"*; fourier's breadcrumb applies **no redaction** — a bare `find_one` emitting `slug`, `set_hash`, `author_slug`, `created_at` for every ancestor, because `_readable_or_none` is applied only to the ENTRY row | The breadcrumb redacts to the value.js discriminated-union shape, **or v2 states the asymmetry and why**. Today's witness is a private ancestor's slug on the wire — the same "private artifact readable by slug" shape as GCM-52 at the image seam |

### §D — The denominator and coverage (35 rows → 13 clauses)

| clause | banked rows folded | witness | lock |
|---|---|---|---|
| **D1** The triple + per-operation disposition ‡ | **R3-7c** ⊕ register **X-3** · FR-GV-8=M-6/L-12 · FR-GV-27·L-23 · AA-31·C·D-12 · FR-GV-13=MISS-LC-2 · FR-GFC-4=C-2/L-3 · GM-M4 (cited B4) · **VV-R2-B** (the acceptance surface) · FR-AFP-1 (inverted, cited D3) | **36 client edges / 9 gap operations**; **7 of 13 `/api/visualizations` operations — remix, publish, unpublish, forks, provenance, diff, versions — have ZERO client function** (three independent counts agree); `visibilityFilter` is a producerless store ref so the server's whole `owner=="me"` branch is dead; `entries` is featured ∩ the loaded 20-row window, not the featured set | ▲ **Census correction FORCED (C-3): both sides were right on different denominators — never cite "30" as the whole API again.** Cite **45 = 30 + 13 + 1 + 1**. ▲ **Counting lock K-1: a `@router.` grep is blind to prefixed routers (`@gallery_router.get("/cursor")`) — read against 13/44, never as zero.** ▲ **WAVE-LOCK (fr-GallerySearchBar C-2, verbatim): any wiring of `basisFilter` MUST use the banked normaliser (fr-BasisSelector M-10)** — without it the Fourier pill returns zero rows against `active_bases ∈ {fourier-epicycles, fourier-series}`. **The seven unclientted operations ARE the provenance surface v2 re-authors.** Identity/provenance DISPLAY arms fold to banked fr-GalleryCard D-7, GCM-25, GCM-3 — **do not re-book**. FR-GV-27 folds under D1's disposition (the phantom-view cell is E6's). **VV-R2-B is the frame row**: ten of 24 workspace-store members have zero consumers and `gallery.ts:168` bypasses the store's own `deleteVisualization` — it owns BLK-1/BLK-2/VV-R2-A/VV-R2-E's **joint identity**, and **it is the row that tells F.W5 whether its contract was adopted at all** |
| **D2** The like verb ‡ | FR-GFC-3=C-1 ⊕ GalleryCard C-1-as-corrected ⊕ GCM-2·C-1 ⊕ GIG C-3 (4 witnesses) | `likes:int=0`, the `(visibility, likes, -id)` index and `cursors.py:22`'s `"likes":"likes"` sort key all ship while **no like route exists anywhere**; `gallery.ts` hard-codes `const liked = true` (un-like unreachable) — **the server sorts by a counter nothing can increment**, under an `aria-pressed` that lies | ▲ **Add the operation or remove the affordance. NO THIRD OPTION SHIPS** (C §7.1, adopted verbatim). Deduped across four records; GCM-2 and GIG C-3 fold by reference (GIG: "Do NOT re-book"). The store's own comment documents the stopgap — **v2 must not treat it as intent.** GIG's new grain — the **orphan-client class** (a published emit contract with no reachable operation; R6-8's degenerate case) — rides NO-WAVE-OWNER → SS-3/SS-4 as census methodology. **Dissent preserved**: C axis filed BLOCKER at two sites |
| **D3** Moderation producer-or-retire ‡ ⊙ | FR-AFP-1·L-1/C:D-1 (**THE ADMISSION GATE**) · FR-AFP-7·L-3/L-4 · FR-AFP-8·L-2/C:D-3 · FR-AFP-9·C:D-5 ⊕ FR-GV-34=MISS-LC-5 · FR-AFP-66·β-miss-1 · FR-AFP-33·D-17/L-20/L-22 · FR-AFP-18·D-10 · FR-AFP-10·C:D-4 ⊕ FR-AFP-70·β-miss-7 | **NO PRODUCER**: every `db.flags` write repo-wide is a test fixture or a `$rename` migration; `FlagRequest` referenced nowhere; production's only reachable state is "No flagged content"; **~110 of 285 panel lines dead by reachability**. Flags key on the NON-UNIQUE `content_hash` (3 rows × "3 flags" = an apparent nine; "Dismiss flags" is a hash-scoped `delete_many` clearing every sibling). The destructive confirm names `image_slug` — an asset FK shared by every remix — while DELETING `slug`. The dialog asserts irrevocability over a 30-day-restorable soft delete whose `?hard=true` bypass has **zero callers**. Orphan flags accumulate forever because `pinned_cron` — named by the docstring as cascade owner — contains zero flags code. "Mark acceptable (save)" never dequeues and has no inverse | ▲ **SEQUENCING LOCK (ruling 5, verbatim): severities in the FR-AFP roster grade the POPULATED surface; FR-AFP-1 is the band's sole reachability gate, and the producer-or-retire decision MUST PRECEDE F.W1's sizing wherever those grades are load-bearing.** **value.js HAS the verb fourier lacks (`POST /:slug/flag`) — the union's cleanest one-directional donation.** Extends R3-7c **INVERTED** (an admin half with no user half) — fold by reference. FR-GV-34 is a **member-site rider** on FR-AFP-9, one identity two call sites. **False in the direction that SUPPRESSES a safe action.** FR-AFP-66's cure corrects the false module docstring. β's own kill of its 428-escalation hypothesis is preserved — **do not revive it**. FR-AFP-33's counterweight verbatim: `if not flagged` short-circuits production's only case — **the cost is latent while FR-AFP-1 stands** |
| **D4** Query semantics bounded by the indexes | AA-6·L-4/C·D-2 · **AA-23** (repair-shaping) · AA-32 · AA-5·L-3 (+AA-24) · FR-AUL-59·LC-miss-M9 | `services/database.py:145-146` creates exactly two `admin_audit` indexes — `[("timestamp",-1)]` and `[("action",1),("timestamp",-1)]`; server `action` filter is a **bare equality** while the placeholder advertises prefixes and its own example `set_tier` matches zero rows forever; `actionTone` is blind to the whole `janitor:` namespace because the collection's **second writer** inserts directly with no `log_audit` call (nine `janitor:*` actions, incl. the audit log pruning its own history); the `entries` sort joins **before** `$limit`, O(users) not O(pageSize) | ▲ **CURE LOCK (AA-23, verbatim-binding): the regex-action cure BOTH axes prescribed is REJECTED** — a case-insensitive `$regex` cannot use that index and converts an indexed equality into a collection scan on the one monotonically growing collection. **Sound cures in AA-6's stated order only: (a) honest placeholder, (b) `$in` over a generated taxonomy (also cures AA-5/AA-24), (c) anchored case-SENSITIVE `^prefix`.** AA-21's placeholder co-sign rides this line (iOS sentence-autocapitalisation defeats an exact-match filter). AA-32's positive recorded: **`re.escape` IS applied — no injection; v2 must not manufacture one.** ▲ **S-8 method law (kills C·S-2/K-3 via K-6): v2's absence-proofs must ENUMERATE the surface, not query one name for it.** FR-AUL-59 is also the slow path that makes B1's last-to-resolve inversion most likely to fire — **the abort-key fix and the pipeline fix compose; neither alone closes the window** |
| **D5** What a count counts | GAB-15·L·D-10 · GAB-16 · GAB-17·D-M7/C-11/L·D-13 · FR-AUL-20·LC-miss-M2 · FR-AUL-25·DU-miss · X-2 (value-side, cited §G-obligations) | The storage tile carries two **uncorrected opposite biases** under one unqualified label (no `$match` for soft-deleted assets; `bytes` records the primary blob only, thumbs sized nowhere); `total_entries` is `sum(tier_counts.values())`, so an absent/off-enum `tier` **vanishes** from the total; `stats.normal` is fetched, typed, produced and rendered by nothing; `entry_count`'s `$lookup` labelled "Live-entry count" carries no `deleted_at` filter — **and the janitor manufactures exactly that population**; the prune confirm announces an unbounded permanent deletion **with no count** though the router computes `empty_slugs` before deleting | v2 states what a count counts (live/tombstoned; primary/derived assets; in-enum/other). ▲ **GAB-17 is SEQUENCED: render `stats.normal` only AFTER GAB-16 lands, or drop the field with this contract pass — its "verification" limb is a TAUTOLOGY until then.** GAB-16 refutes D-M7's headline rationale (R-6). ▲ **Prune sequence: FR-AUL-20 → FR-AUL-21 → FR-AUL-25** — the count is meaningless until both land. Magnitudes → SS-13 |
| **D6** Closed domains, stated fallback | FR-AUL-21·DU-miss ⊕ FR-AUL-31·L-13/C-i2 · RESOLVER (cited E9) · M-10 (cited E13) · GCM-10 (cited A3) · FR-NP-30b (cited A5) | A **third** `status` exists — `migrate_visualization.py` upserts `"orphan-migrated"`, `$ifNull` passes it, `AdminUserInfo.status: str` accepts it, the template branches only on `=== 'suspended'` → an orphan-migrated user renders as a **healthy account with a Suspend affordance**, on the panel whose purpose is separating real accounts from churn | ONE closed set across writers, model and wire (`Literal` + the migration writer folded in); the sort set closed on both ends. FR-AUL-31 is the same clause at the type level — **merged, both ids preserved**. ▲ **F.W4 rider: unknown statuses render VISIBLY, never as healthy.** Same mechanism as GAB-16: *closed domain at the boundary, open domain in the collection* |
| **D7** Client-derivable bounds | FR-AUL-46·LC-miss-M5 · m-7=C-6-as-rescoped · B-1=L-B1/C-2 ⊕ R-16=C-3 (SliderControl) ‡ · M-SB ⊕ N-4 (cited F2) · FR-FG-21 (cited A3) | The client never enforces `slugs … max_length=50` and FR-AUL-1's selection leak is the mechanism that exceeds it (60 slugs → 422 → bare `statusText`); the client's `[1,500]` window is **invented at the leaf**, restated ~6× per control, matching **none** of the server's six divergent windows; "Min Area %" drives a `[0,1]` fraction from a `0–20, step 0.5` axis — **39 of 41 detents are empty by construction** and the default `0.001` renders "0.0" under a "%" label | **v2 makes request-model bounds CLIENT-DERIVABLE** — one rule: *every request field is bounded, or the exception is named*. ▲ **K-3: m-7's straddle-a-422 scenario is REFUTED from source** (`AnimationData` is server-computed; the client-reachable bound 4096 ⊇ [1,500]) — **v2 must not cite it.** R-16 **FOLDS** (SliderControl books nothing new). B-1's F.W5 share verbatim: *"the typed range-checked descriptor table for all six fields rides an F.W5–W8 rider — the contract seam owes CLIENT-DERIVABLE BOUNDS"*; **operation records must carry field DOMAINS** |
| **D8** Required/optional posture | FR-AUL-14·C-B4 (rescoped, ruling 2) | ONE user document missing `created_at`/`last_seen_at` **500s the entire admin list** (required model fields, bare inclusion projection, handler-body construction, global exception handler) — and the janitor's reap query SKIPS fieldless docs, so they never age out | `$ifNull` the projection or default the model fields. Absent defence proven on BOTH sides; **production INCIDENCE is UNPROVEN — that question alone rides live → SS-13.** v2 states the posture; the probe answers whether it is violated |
| **D9** The actor field | AA-10·D-M2/L-13 (+ lane-crud R-7 attribution asymmetry / **V-γ**) | The log never answers *who*: `_JANITOR_ACTOR = "system:janitor"` is written into **`ip_hash`** and rendered `system:jan` by an unconditional `.slice(0,10)` — convicted by the producer's own comment ("self-documenting in the viewer") | **An audit row's actor is a field, not a repurposed `ip_hash`.** Composes with R-7: fourier `owner_slug` required non-null vs value.js `userSlug: string \| null`, whose `if (userSlug)` guard means **an unattributable edit writes NO version row** (V-γ) |
| **D10** Serializer unification | FR-AFP-32·D-16/L-13/C:D-15 (+FR-AFP-40 fold, +FR-AFP-69=β-miss-5) ⊕ GalleryCard L·M-4/D-13/C-8(a) | **ONE router serializes the same concept two ways**: flagged rows via `_json`/`default=str` (space-separated, non-ECMA) while audit returns `AuditListResponse` (ISO with `T`) — and the NaN guard sits in the consumer receiving the **conformant** payload | ▲ **F.W5 owns ONLY the serializer arm.** The five-copy `timeAgo` family's shared formatter is **F.W3's and already banked as FR-AUL-17 — do not re-book it here** (fr-GalleryCard states the fold explicitly). JSC acceptance → SS-13 |
| **D11** Tier transitions are stated transitions | FR-GV-9=C·M-8 (second clause) · FR-AFP-10 ⊕ FR-AFP-70 (cited D3) | Batch Unfeature sets `{"$set":{"tier":"normal"}}` unconditionally via `update_many` — **unfeaturing a SAVED-tier entry silently erases the saved tier** — while the dialog copy is honest and the button label is not; `handleSetTier` is hardwired to `'saved'` with no inverse | A blanket write is not a transition; the tier verb gains its inverse. FR-GV-9's first clause **killed at R-3/K3**. One semantics decision covers the tier↔flag pair, and **the false in-file comment dies with the cure** |
| **D12** Entity vs asset identity on human surfaces | FR-AFP-8 (cited D3) · FR-AFP-7 (cited D3) · GCM-1 (cited E4) | The confirm names `image_slug` — an asset FK shared by every remix (`VisualizationRemix` declares no `image_slug`) — while deleting `slug`, violating the identity law the file's own header states | **v2 states which identifier a human-facing surface may render.** Same shape as GCM-1's `/w/` routing on `image_slug`. **Dissent recorded**: the L axis filed FR-AFP-8 as a BLOCKER; β's demotion to MAJOR sustained under ruling 5 (K10) — the L axis cannot carry two BLOCKERs when its first proves the second unreachable |
| **D13** Rider only — **not credited to F.W5** | FR-GFC-1=D-1/C-3 ⊕ MISS-1(DU) ‡ | The featured section unmounts for a full round trip on `set-tier`/`delete`, **and the unmount is not admin-only** — search and sort/tier/basis fire `resetAndFetch()` while those three refs have **zero query-builder readers repo-wide**: every anonymous visitor who types and pauses tears the strip down to refetch a provably identical page | ▲ **F.W5's share is the RIDER ONLY** — whether search/tier/basis become real params (D1). **The body cure is F.W4's** (patch in place off the discarded returned entity, per the store's own `recordView`/`restore` idiom; delete or gate the three dead public triggers). ▲ **The D-1 skeleton cure is KILLED (K5).** **Dissent**: two BLOCKER filings (D and C axes); banked MAJOR blocker-weight |

### §E — Provenance and persistence (21 rows → 16 clauses)

| clause | banked rows folded | witness | lock |
|---|---|---|---|
| **E1** Compound per-entity version `_id` | lane-crud **V-β** (+§7 "reusable as-is") | **Measured 2026-08-25 (value-side, not under the F.W0 lock)**: `api/src/modules/palette/hash.ts:8-17` folds only `{name, colors}` — never `paletteSlug`; `repository/paletteVersion.ts` `findByHash` filters `{_id: hash}` with **no slug scope**, and `insertIfAbsent` returns early on a hit. Two palettes with identical `(name, colors)` share ONE version row whose `paletteSlug` names only the first; the second's history is **empty** | fourier already solved it with `f"{viz_slug}:{set_hash}"` and documents the §2.3-vs-§11 tension at `visualization.py:221-226`. v2 mandates the compound form. **Execution is the value.js API row's** |
| **E2** Chain depth | lane-crud **F-α** (+ intake X-8, cross-edge to F.W6) | `_write_root_version` hard-codes `parent_hash=None, root_hash=set_hash_value, depth=0` and is the **only** writer → every version has `depth == 0`; `GET /{slug}/versions` sorts by `depth` over a set that is always a singleton; all real lineage rides cross-viz `fork_of` | v2 states whether within-entity chains deepen, **or** the depth/parent/root quadruple is explicitly retired on the fourier side. **F.W5 owns the CLAUSE; F.W6 owns the burn-down — do not double-book** |
| **E3** Diff-clause participation ⊙ ‡ | **TA-4** (+ lane-crud §7) | **Measured 2026-08-25**: `grep -rn "atomdiff\|atomDiff" api/src src` → **one comment hit** (`__tests__/palettes-forks.test.ts:9`); `ls api/src/lib` → *No such file or directory*. v1 §2.5 mandates the exact file; §6's close-gate assumes BOTH probes exist — **value.js's side cannot run today** | ⊙ **The owner rules RESTORE (TA-4 reversal) or v2 re-scopes value.js out of the diff clause EXPLICITLY, with §6 re-authored to a one-sided verdict. The wave may not assume either** |
| **E4** The derived variant: born-visibility, verb, lineage ‡ | lane-crud **R-5** · D-14/MIN-4/MIN-5/MIN-10/MIN-11 (noun+positional arm) · GCM-1·L-1 ⊕ BLK-1 (FOLD) · D-7·C·I-2 (GalleryCard) · X-2 | A fourier remix child is born **`draft`**; a value.js fork child is hard-coded **`visibility: "public"`** — opposite privacy defaults on the same verb, both shipping. The modal's only CTA routes to the PRE-SAVE workspace, writing a draft row keyed on **another user's `image_slug`** into the viewer's DB, and **any subsequent save POSTs a NEW entity with no `fork_of` despite the fork substrate existing**. The entity's HUMAN identity is write-only (`entry.title` readers = 0); label, `alt` and `aria-label` all spend the MACHINE FK | One born-visibility rule, co-signed; each side's test asserts the child's visibility on create. ▲ **F.W5's share of GCM-1 is the CONTRACT fact — a save that should have been a fork carries no lineage — NOT the routing repair** (F.W4's; BLK-1 folds; VV-R2-B owns the joint identity). `fork_count` unrendered is **load-bearing for the union**: it is the counter the fork verb maintains — composes with lane-crud R-1 (`fork_count`↔`forkCount`) and value-side X-2 (a stored counter cannot stay truthful under viewer filtering). Vocabulary: **remix vs fork**, **snapshot vs version** — v2 fixes the nouns it inherits (lane-crud §6) |
| **E5** Idempotency, create dedupe, CAS ‡ | B-2=C-C-1∘L-3 (∘m-13 rider) ⊕ FR-GV-1=L-2/C·C-3 (FOLD) · D-4/L-1/C-3 (+C-28 fold, CanvasControlsDock) · C-7 rider (EditorControlsDock, cited C1) · X-3 | The publish path is a composed falsehood: `savedSnapshots` has ONE writer — the unconditional literal `[]` — so the row **never leaves**, the button re-enables, and **the second press mints a SECOND public visualization** because `createVisualization` sends no `Idempotency-Key` (middleware: *no header → handler()*) and `content_hash` carries a **plain** index (contrast flags' deliberate `unique=True`). Publish is re-entrant on all three hops in the dock: **N clicks ⇒ N public gallery rows** | Server-side dedupe/idempotency for the create path — **one clause, two entry points**. ▲ **K12: both axes' UNPROVEN-NEEDS-LIVE hedges are DEAD — B-2 closes it STATICALLY; SS-13 spends no probe here.** ▲ D-4's F.W5 share verbatim: *"the server-side `(image_slug, contour_hash)` dedupe arm rides the R6-8 seam split (C-28 fold) — the client cure and the operation cure register a TWO-SIDED DELTA until that join is split"*; **the server arm SEQUENCES AFTER the join split.** FR-GV-1 folds at the banked severity. Riders: the success toast prints the USER slug (m-13); `publishedHashes` is a misnomer |
| **E6** Safe reads / counter provenance | FR-GV-12=MISS-LC-1 ⊕ FR-GV-24·L-19/C·m-9 · VV-R2-A (registry-novel) | `GET /{slug}` is an **unsafe read that cannot observe its own side effect** — `find_one` first, `$inc {views:1}` after, `_public_doc` serialises the PRE-increment doc; the same verb is the ETag-capture path, so **every publish silently inflates its own view counter** and an entity is born at `views: 1`; no `viewed_ips` dedup while `liked_ips` exists | The counter moves to an explicit verb, or v2 states the mutating-GET policy against **RFC 9110 §9.2.1**. ▲ **Repair-test lock (GCM-3 R-7 precision): `viewedHashes` DOES work within a session — the defect is SCOPE, not absence — so repair tests must NOT assert a re-open increment.** ▲ The store's own `recordView` comment documents GET-as-increment as the **deliberate** mechanism, so the publish-path GET is an **accidental** self-count: **v2 must not codify the accident.** Observable → SS-13 |
| **E7** Delete / restore / cascade truth | FR-AFP-9 ⊕ FR-GV-34 (cited D3) · FR-AFP-66 (cited D3) ⊙ | see D3 | ⊙ The hard-delete-arm caller decision → owner block; truthful copy → F.W3/W4 (diction rides FR-AFP-35) |
| **E8** PATCH atom coverage + `set_hash` recompute | lane-crud **F-β** · SS-C-1 (write leg) | `VisualizationUpdate` (api) and `VisualizationPatch` (client) are the same five fields with `extra="forbid"`, so the persisted `speed` — atom 4-of-5 of the version-identity set — is settable **only at create/fork**; and the same model lets `palette_slug` (atom 5) be `$set` with **no `set_hash` recompute and no version write**, going stale against `enumerate_atoms(doc)` | **The SAME model is both too narrow (speed) and too permissive (palette_slug).** Ruled SPLIT (R2-LC wins): SS-C-1's **read leg FOLDS** to GCM-1 (`loadVisualization` zero callers) — do not re-book; the **write leg books new**. **Dissent preserved**: C axis filed BLOCKER; demoted at component altitude |
| **E9** Persisted-settings round-trip | BC-9/C-6/D-20 ⊕ SS-C-2 (2 witnesses) · C-7 (BasisCanvas `AnimationData`) · RESOLVER (D-10·L-4·C-D-4) ⊕ L/M-3 (EasingPicker) · MISSED-E (LC) · **M-9=L-M4/C-20 (BasisSelector)** ⊙ | Half the persisted `AnimationSettings` contract is write-only and **three-way divergent including units** — server `fps 30 / duration 30.0 s / max_circles 80`, client `60 / 5000 ms / 100`, renderer hardcoded `ref(80)` and `20000 ms`: **a gallery replay cannot reproduce its own frame**. Two `AnimationData` types share a NAME and a FIELD with incompatible shapes — and the server docstring **names `BasisCanvas.vue`** and prescribes typed access over a shape the client cannot use. The `easing` domain is wider than the option set at **five unguarded hops** (server bare `str`), and `easings.ts:105` is the tree's only unguarded catalog index. The deliberately-minted `[]` "off" state is **unrepresentable end-to-end and silently laundered on every path** | The union owns the **three-way reconciliation incl. the 1000× unit fork**; the easing union hoists to the operation record as `Literal[…]`. ▲ Deduped: SS-C-2 and BC-9 are one identity (SS-C-2's unconstrained-`speed` cell carried); `duration = ref(20000)` stays banked at BC-20, **not re-booked**; RESOLVER ⊕ L/M-3 are ONE cure, **both ids preserved**, and the seed/normalization half folds to banked AnimationControls L-11+M-10. L/M-3 **KILLS C/i-1** — the field IS persisted and remixed (seat-verified on all four models). MISSED-E's narrow reachability is **honestly stated and the honesty carries**: a debounced mirror is a contract hazard wherever a synchronous save reads the same state. ⊙ **M-9 verbatim: admit `[]` in the contract, or stop minting it — the silent rewrite must not survive either way.** Corpus line-drift corrected by the seat (`:186/:280`) — **re-resolve anchors (D-19)** |
| **E10** Produced-and-unconsumed | M-β4 (cited B4) · L·m-6 ⊕ FR-EMT-20 (cited F1) | see B4 / F1 | One stated disposition, not three ad-hoc deletions |
| **E11** Contour provenance ‡ | M-13 (ContourSettings) ⊕ M-14=LC-missed-3 (BasisSelector) · C-2 (ContourEditorCanvas) · ContourPreview row 28 (write leg) | The hand-edit pipeline **destroys the edit on exit**: nothing recomputes from `store.contour`, so merely leaving the editor remounts ContourSettings, whose immediate watcher auto-runs extraction and the server **cache-hits the ORIGINAL** (editor-saved contours carry no `extraction_cache_key`). The POST never derives bounds — and because the store is content-addressed, **the overlay dies exactly when the points CHANGED**. `save_contour` takes `xs`/`ys` off `dict[str, Any]` with **zero cardinality validation** | ▲ **Two triggers, ONE cure (verbatim, both records agree).** An editor-saved contour is a **first-class compute input**; extraction may not overwrite a `source="editor"` asset; bounds derive on POST or backfill on write. The tree's own workaround comment sits on the LOAD path — **do not mistake it for a cure.** M-14's end-to-end arm is UNPROVEN-NEEDS-LIVE; the mechanism is source-certain. The render leg of row 28 is F.W4's |
| **E12** Cache identity ⊇ consumed fields ‡ | B-4=C-1 (∘C-25 / R6-8 fold, ⊕ i-7) · m-18 (rider) | `extraction_cache_key` is a CLOSED 10-field `json.dumps` literal that **omits `ml_threshold`/`ml_detail_threshold`** — the two fields this control is the sole producer of — and the handler short-circuits on the hit BEFORE `compute_contours`, while those fields materially drive extraction on the SHIPPED default route | Verbatim clause (C-25): ***an operation's cache identity must be a SUPERSET of the request fields the operation consumes*** — the R6-8 seam made product-visible. i-7 **folds** (B-4's payload, not a second row); m-18's invented `ml_detail_threshold = mlThreshold * 0.6` coupling rides here |
| **E13** Vocabulary | D-14/MIN-4/MIN-5/MIN-10/MIN-11 (noun + positional arm) · M-10=L-M6/C-19/D-m9 (cited D6) · L-m13 (cited F5) | Three names for one affordance; a RETIRED `snapshot` noun still on the wire; a dead `_imageSlug` positional kept "so the pre-existing call site need not change" with exactly one caller. Two hand-maintained parallel `Record<string,string>`s stand in for a 7-member `ContourStrategy` enum — `ADAPTIVE_THRESHOLD` is real, implemented and **unrepresentable in the picker** | **Noun + positional → F.W5; the rest → F.W4.** The shared-enum contract is F.W5's; the typed vocabulary at the leaf is F.W3/W4's |
| **E14** The exposed-ref component seam | ContourPreview row 13·L:L-5 (+ exercised-pattern rider) | The prop is sourced through the sibling's `defineExpose`d **mutable** ref (the undo ring's subject) across a v-if boundary; the channel is read-WRITE (`proxyRefs` set-trap) and **the tree ALREADY writes through it one key over** (`magnetRadius`) — a live pattern, not latent | Verbatim: **a TWO-MEMBER change (`points` AND `magnetRadius`)** — readonly / `getPoints` seam. ▲ Row 11's one-character `?:` fix pairs at F.W4 "per the L-lane carry" — **F.W4 must not land row 11 presuming row 13's seam shape.** The one non-wire contract row this wave owns, because the record routed it here |
| **E15** Transport hygiene — URLs built outside the client core | C-17 + C-18 (BasisCanvas) · C:C-12 (ImageUpload row 26, cited B1) | The overlay is fetched unconditionally then refused when `image_bounds` is null (a second correctly-sized fetch follows); `overlayUrl` is a raw string-concat outside `coreFetch` yielding `/api/api/...` under the Dockerfile default; `thumbnailUrl` traverses **none** of `coreFetch` (no token, no abort entry, no 429 retry, no ApiProblem, no ETag) | One transport clause. ▲ **The race discipline is a recorded superlative (S-6): do not cure the waste by removing the guard** |
| **E16** Trie disposition ⊙ | lane-crud **R-4** / gate G7 | The R-4 probe returns **ZERO true hits on both trees** (trie / prefix-tree / radix / patricia / structural-sharing / delta-compress). What exists instead on both sides is **whole-snapshot duplication**, and both carry an explicit anti-tree KISS guardrail: `atomdiff.py:12-14` — *"the atoms are a flat BAG (not a tree / Merkle / document) … there is no three-way / DAG / merge"* | ⊙ **The honest default v2 MUST carry absent a ruling: no trie; whole-snapshot duplication is the recorded shipped behaviour.** This is "the single requirement with no material on either side" — a greenfield build against a documented standing constraint. **SS-4 flags the ruling INLINE** |

### §F — Equation contract and error envelope (12 rows → 9 clauses)

| clause | banked rows folded | witness | lock |
|---|---|---|---|
| **F1** Partial projection, both directions ‡ | B-1 (EquationView) ⊕ FR-EMT-1 (EquationModeToggle) · L·m-6=C·D-14 ⊕ FR-EMT-20 | The Notation control is **inert in the DEFAULT display mode**: `SimplifyResponse` = `{latex, energy_captured, term_count}` on both sides with **no sigma arm**, while the server proves sigma IS notation-dependent — a stale-but-truthy `displayLatexSigma` wins and persists across reload, and the energy badge DOES move so the control looks alive. The same operation pair **over**-projects: `reconstructed_points` (500+500 floats, sessionStorage-persisted whole) and `term_count` have zero consumers | **ONE ENVELOPE ROW, BOTH DIRECTIONS.** `latex_sigma` joins `SimplifyResponse` on BOTH models — **a CONTRACT DECISION, not a patch** — or explicit invalidation. ▲ **CURE BOUND by M-CK (verbatim): the alternative cure — "route notation through doCompute" — walks into the under-determined `computeKey` and RE-SHIPS B-1 one layer down. Fix the key FIRST.** FR-EMT-1 books only the leaf's contract arity; FR-EMT-20 contributes `term_count` (its 0-banked-hits sweep is corrected by EquationView's L·m-6 — one identity, two witnesses). Rider (R1 missed-4): `useCoeffHover` reads its LABEL KIND from stale rendered markup while computing from fresh state |
| **F2** One shared bound constant ‡ | B-2 (EquationView) ⊕ L-B1=C-1 (FunctionInput, FOLD) ⊕ C-8 · M-SB ⊕ **N-4** (amendment relay) | `budget` walks past `le=50` in ONE Harmonics drag (`budget(v)=v−10`; a track-click from defaults yields exactly 50); through doSimplify the 422 lands in `/* silent */`; **`budget` restores from sessionStorage, so the NEXT session's first compute 422s before the user touches anything.** `SimplifyRequest.coefficients` has no `max_length` and **`ComputeEquationRequest.expression: str` is bare too**, flowing through a seven-keyword denylist into `parse_expr` + symbolic integration | ▲ Clamp at the two request sites or the ref — **NOT at the slider: the restore path never passes through it** (M-BR). ▲ **Kill lock: L-B1's "without ever touching the offending slider" headline rides the ratio-1 state K-13 killed as unreachable-from-defaults; C-1's sessionStorage-restore path is the TRUE one — v2 cites the true path only.** ▲ **AMENDMENT RELAY (verbatim discipline): N-4 CORRECTS banked M-SB ("the ONE unbounded field") — there are TWO, and the FREE-TEXT one is the harder half. The relay is recorded at the amending record; the banked file is NEVER rewritten.** C-8 (Harmonics `:max="100"` forfeiting half of `le=200`) rides the same constant |
| **F3** Cross-field server validation | M-DV (seat-booked rider on the folded L·M-1/C·C-32) | No `model_validator` on `ComputeEquationRequest`: `domain_start == domain_end` ⇒ period 0 ⇒ the catch-all 500 that F4 renders as **nothing**; reversed domains succeed with a negative period | **The client half is banked (fr-CoefficientsPanel M-L1) — book only the increment** |
| **F4** The problem+json envelope ‡ | C·D-02 (EquationView) ⊕ M-15=DU-missed-4 ∘ D-m2 ∘ D-m3 (ContourSettings) | `main.py` has ONE `@app.exception_handler(Exception)` and **no HTTPException handler**, so FastAPI emits `{"detail": …}` while `api-problem.ts` destructures `detail` away (and drops the ARRAY-shaped detail) and falls back to `statusText` — **under HTTP/2 `statusText` is `""`, and `"" ?? x` does not fall through: `error = ""` ⇒ falsy ⇒ no toast, no banner, silence end-to-end**; the one hand-authored 422 is parked on an unread field | An HTTPException handler so **the RFC 7807 contract the client was BUILT FOR is actually spoken**. ▲ **Narrowing adopted: the middleware's rate-limit 429 IS problem+json (`rate_limited()`) — v2 must NOT claim zero coverage.** Deduped: one server identity, two witnesses. Transport arm (is the browser hop HTTP/2?) → SS-13; **the static arm needs no probe** |
| **F5** Which population a field measures | L-M3 (FunctionInput) · L-m13 (ConvergencePlot) · FR-FG-13 (cited A3) | The Auto tooltip prints a **budget-truncated** energy fraction directly beneath its own ≥99.99%-at-N_eff assertion, and the shipped defaults (budget 10, effectiveN 20–40) make the mismatch the **common case**; `nHarmonics` is a surviving-harmonic cap in the client and an index bound on the wire — two denominators under one name | The response field must state which population it measures; **v2 states the denominator with the field.** ▲ Provenance carried: *"the best library-axis find in the corpus (both readers concur; the seat concurs)."* Same family as C-7's amplitude mirror and the `duration` unit fork |
| **F6** The original-expression LaTeX field | L-M7 + C-8 (ConvergencePlot) | The 'sum' and 'original' tooltips are **character-identical** at the only callsite; the golden partial sum is labelled `f(x) =` ⟨the original⟩ — **asserting the identity the plot exists to DISPROVE** — while `latex`/`latex_sigma` arrive unconsumed on the same response | ▲ **K-13 BINDS THE CURE: `latex` is the budget-TRUNCATED series render, so routing it to the ORIGINAL curve re-commits the same inversion. The prescribed cure is DEAD; a NEW backend field (`sp.latex(expr)`) is the only sound one** — plus C §6's field-level-gap counting rule |
| **F7** The seam choice — **this wave MAKES it** | FR-EQR-4=reader-2 missed M-1 · FR-EQR-32 (cited B1) | The sole affordance emits **invalid LaTeX in the app's DEFAULT mode**: `eqMode` defaults `"sigma"`, `copyLatex()` copies it raw — a string embedding `\htmlClass{eq-coeff …}` that only KaTeX-with-trust consumes; all four emission sites live in the `*_sigma` renderers while the expanded ones are clean | ▲ **Verbatim: "THE FORMING SPEC CHOOSES THE SEAM." This wave must MAKE the choice, not inherit it.** If v2 chooses the wire field, FR-EQR-32's **sink-class** clause becomes enforceable; if it chooses the client strip, **F.W5 records the decline** so F.W4 may proceed with `plainLatex()`. Registry-swept: unbanked elsewhere |
| **F8** Parameters that govern | D-10+D-L12+C-22 (EquationPanel) · C-4 (FSE, cited G4c) | 'Terms' names a quantity it **stops governing** — `budget` is never forwarded (re-verified) | Composes with F2's shared constant and B1's under-determined key: **the same `budget` field is unbounded at the client, unforwarded at the server, and absent from the operation's identity** |
| **F9** Limiter hardening | D-L5 + C-1 (EquationPanel) · FR-USB-24 (cited C4) | A full-payload re-upload runs against the 5/60 s compute limiter — a two-slot hardening question the contract never answers | Carries C4's one-controller lock verbatim |

### §G — Canonical-geometry provenance, a build-time contract (16 rows → 9 clauses)

| clause | banked rows folded | witness | lock |
|---|---|---|---|
| **G1c** The true source ‡ **TRIPWIRE** | L-B1 (FourierShapeExtractor = FSE; two BLOCKER dissents) | The generator has **diverged from the artifact it generates**: 3.7% of shipped `moon.json` points fall within 1.0 unit of any extracted point; p90 **37.6** against a 1.23-unit resample spacing; the crescent-alone bbox strictly contains the shipped bbox — while the sun IS consistent. `9e5ba74` (asset-only, *"remove stars … cap at 50"*) is the divergence event the generator never received; the component still renders the six removed decorations; shipped params 50/10 vs the script's 100/12 | ▲ **TRIPWIRE (verbatim): if any wave attempts regeneration before F.W5–W8 lands the pipeline, L-B1 and L-B2/C-2 REVIVE AT BLOCKER with the DO-NOT-REGENERATE rider as the tripwire. DO-NOT-REGENERATE on `master`.** The site-wide dark-mode toggle renders geometry whose source no longer exists. "What `moon.json` WAS generated from" → SS-13 / undetermined. **Dissent preserved with its revival condition** |
| **G2c** Track the seam | L-B2/C-2 (one identity) · L-B3 | The only consumer is **untracked AND broken at import AND parameter-diverged**: `.gitignore:53 scripts/*` swallows both the script and `raw-contours.json`; it imports `order_contours`, which **no module defines**; it hard-codes 100/12 vs the shipped 50/10. Canonical geometry is a pure function of an **unpinned third-party PRNG** with zero drift detection | Track (the `.gitignore` negation lines), fix the import (`build_contour_tour(...).path`), reconcile 50/10, **prefer the nav-icons in-process idiom**. ▲ **REPAIR-SHAPING CORRECTION BOUND IN (M-2, verified): `git ls-files scripts/` = 16 paths including a TRACKED, WORKING, in-process `precompute_nav_icons.py` producing 5 of the 7 `fourier-paths/*.json` assets with no browser step. The correct frame is "a tracked path exists for 5 of 7; only sun/moon route through an untracked browser detour" — both axes' falsifier sweeps asserted otherwise on a FALSE ENUMERATION.** L-B3's concrete 0.4.1→0.12.0 exposure is CLOSED (Δ 6.10e-6); the absent drift detector survives, and **tracking `raw-contours.json` IS the golden file** |
| **G3c** Closure carried, never inferred | L-M1 (+ the 44.08 arm) ⊕ M-8 · C-3 | Closed shapes are emitted OPEN under ONE convention: every branch is endpoint-exclusive — diamonds lose 23.6–27.1%, stars 8.6–11.6% — **and the largest instance was in no axis table: `sun[0]`, the 20-vertex ray polygon, a 44.08-unit dropped closing flank on the page's dominant silhouette**; the mirror wrong follows from the same convention (open paths lose their TERMINAL point). The type `[number,number][][]` cannot express open-vs-closed while two open strokes feed a closed-only downstream | A closure **flag** + wrap at extraction; ONE cure with C-3. ▲ **The "unambiguous discriminator" sub-claim is KILLED (44.08 closed vs 44.97 open overlap — no post-hoc heuristic can recover closure), which STRENGTHENS the type complaint: the flag must be CARRIED, not INFERRED. v2 must not specify a heuristic** |
| **G4c** The sampling contract | C-4 · C-14 | `samplesPerPath` is honoured by ONE of three branches — paths 128, circles `max(16, round(128·r/50))` (a magic 50), polygons ignore it — measured output `[20,123,128,4,4,4,16,16]`: **a parameter named "samples per path" produced a 4-point contour**; and the polygon branch's exact-vertex care is undone by `resample_arc_length(stitched, 512)` downstream, documented nowhere | A contract doc or a uniform post-pass; **C-14 bounds C-4's geometric cost — book them together.** Same "parameter that does not govern" family as F8 |
| **G5c** A diagnosed result type | L-M3/C-6 | Three silent-drop gates with **zero diagnostics** on the path that DEFINES shipped geometry (`grep console\|throw\|warn` → 0), no caller counts, a bare-return exit — while the **server twin ships `extract_contours_result` WITH diagnostics** | ▲ **This is the mechanism that makes G1c structurally UNOBSERVABLE at the human seam** — the diagnostics row is a **precondition for trusting any future regeneration** and sequences with the tripwire |
| **G6c** A typed handoff | L-M5/C-5 · C-15 | The machine handoff is dead, untyped and hostage to a presentational node (`(window as any)`, no `declare global`, zero readers, the Playwright comment false at HEAD); the producer's output type is unenforced end-to-end — anonymous literal → untyped JSON → an independently-declared `FourierPathData` → `as any` at both consumers, **with `resolveJsonModule` already providing the structural type being discarded** | A named exported interface + ref emission, **decided WITH the pipeline repair**. ▲ **Corpus correction bound in: SE-05's "just delete it" is WRONG on the tree** — `precompute_svg_fourier.py:4-5` names this component as its INPUT; the seam needs a real harvester or a real deletion *with* the decision, not either alone |
| **G7c** Which stage-0 survives | C-7 (FSE) | Zero consumption of the 39-function API surface: the leaf **reimplements stage 0 of the server's contour pipeline**, with no test runner in `web/` at all — the R6-8 echo at build time | ▲ **C's honesty adopted and binding: the decomposition is NOT duplicated and the offline detour is ELECTIVE (`saveContour` accepts ingested contours) — v2 must not overstate the duplication.** Build-time twin of C-5 (BasisCanvas) |
| **G8c** Extraction scope and guards | L-M2 · M-11 · M-7 · M-9 (FSE) | CTM-naive extraction with `polyline` absent from the selector; **no paint-visibility filter** (`<defs>`/`<mask>`/`<clipPath>`/`display:none` would contribute geometry — and the repo already mounts a global `<defs>` surface app-wide, so the hazard idiom is LIVE); `if (totalLen < 1) continue` is **NaN-permissive** (`NaN < 1` is false) so a NaN length emits 128 `[NaN,NaN]` samples that pass every gate; `points.length >= 3` guards **cardinality, not extent** (an `r = 0` circle yields 16 coincident points) | L-M2 (space) and M-11 (visibility) are **distinct** scope clauses. The correct guard is `!(totalLen >= 1)`. **One family: the extraction guards check the wrong predicate twice.** ▲ M-9 **bounds L·S-4's superlative — carry the bound with the superlative so v2 does not inherit a false credit.** L-M2 magnitude → SS-13 |
| **G9c** Seeds and a true provenance note | L-m3/C-10 ⊕ L-m5 | The **single** provenance comment is arithmetically false (42·100+42 = 4242) and cites a frame-seed formula existing in NEITHER repo (`useLineBoil` takes no seed) — the only prose explaining why the canonical geometry is what it is, **wrong twice** — while the canonical seed set (42; 1,2,3; 10,20,30), the artifact's only free parameters, is scattered inline with no single knob | Deduped: **the false comment and the scattered seeds are ONE cure** — one named seed constant, one true provenance note. The provenance union's most literal row: *the artifact's provenance prose is false* |

## 5. Gates — all born-RED

`F=/Users/mkbabb/Programming/fourier-analysis` (read-only) · `V=/Users/mkbabb/Programming/value.js`.

**Two reading disciplines, binding.** (1) **MEASURE-AT-OPEN**: every figure and anchor carried from a registry record is re-confirmed by F.W0 before citation (D-19); this file re-asserts none of them. (2) **Split verdict**: each gate closes for F.W5 when the **clause** is authored with its disposition; it goes **GREEN** only when the named owner wave lands the change. F.W5 never claims a GREEN it did not execute.

**X-1 (precondition, not a gate — hard).** F.W0 must re-ground the substrate first. Measured read-only **2026-08-25** in the authoring pass: `git -C $F status --porcelain | wc -l` → **28** (27 modified + 1 untracked, including `GalleryAdminBanner.vue`, `AdminFlaggedPanel.vue`, `AdminUserList.vue`, `GalleryCardModal.vue`, `GalleryDraftsSection.vue`, `EquationView.vue`, `BasisSelector.vue` — exactly this wave's witness files); `git -C $F rev-parse --short=8 HEAD` → **`cd26c653`** on `m/w1-bump-migration`; and **`git -C $F cat-file -t 14d83356` → `fatal: Not a valid object name`** — *the fourier substrate the lane-crud census pinned does not resolve in the tree today*. **No fourier line anchor or HEAD figure in this spec may be cited until F.W0 settles this.** GAB-12's "LIVE at certified HEAD" cell is measured on those uncommitted bytes and is **not HEAD truth** before F.W0.

| # | gate | born-RED witness (probe / carried anchor) | F.W5 close (the clause) | GREEN owner |
|---|---|---|---|---|
| **G1** | identity-on-the-wire | `grep -rn "class AuditEntry" $F/api --include='*.py'` — four fields, extra-ignore drops `_id`; the consumer's `:key` is positional **because the contract offers no identity** (AA-45) | every list-bearing response class in v2 carries a stable per-row identity | F.W4 (key deletion) |
| **G2** | version-identity collision (V-β) | **measured 2026-08-25**: `$V/api/src/modules/palette/hash.ts:8-17` folds only `{name, colors}`; `$V/api/src/modules/palette/repository/paletteVersion.ts` `findByHash` filters `{_id: hash}` with no slug scope | v2 mandates the compound per-entity `_id` | value.js API row (+ a test asserting two same-content palettes keep separate histories — none exists) |
| **G3** | chain depth (F-α) | `grep -rn "_write_root_version" $F/api` → def + create + remix, the **only** writer; `parent_hash=None, root_hash=set_hash_value, depth=0` | v2 states deepen-or-retire for the depth/parent/root quadruple | **F.W6** burn-down (X-8 cross-edge) |
| **G4** ⊙ | diff-clause participation (TA-4) | **measured 2026-08-25**: `grep -rn "atomdiff\|atomDiff" $V/api/src $V/src` → 1 comment hit; `ls $V/api/src/lib` → *No such file or directory*. v1 §2.5 mandates the file; §6 assumes both probes exist | **owner rules RESTORE, or v2 re-scopes value.js out EXPLICITLY with §6 re-authored to a one-sided verdict** | owner → value.js API row |
| **G5** | born-visibility of the derived variant | fourier remix child born `draft` (`visualization.py:277`) vs value.js fork child hard-coded `visibility:"public"` (`forks.ts:76`) — opposite defaults, same verb, both shipping | one co-signed born-visibility rule | both API rows (+ a create-visibility test each side) |
| **G6** | redaction parity (F-γ) | value.js collapses non-public hops to `{kind:"unavailable", ordinal}`; fourier's breadcrumb emits `slug`/`set_hash`/`author_slug`/`created_at` for **every** ancestor — `_readable_or_none` applied only to the entry row | breadcrumb redacts to the discriminated-union shape, **or** the asymmetry is stated with its reason | fourier API row |
| **G7** ⊙ | trie disposition | `grep -rniE "trie\|prefix.?tree\|radix\|patricia\|structural.?sharing\|delta.?compress" $F/api $F/web/src $V/api/src $V/src` → **zero true hits both trees**; `atomdiff.py:12-14` is the explicit anti-tree KISS guardrail | **owner rules trie-vs-KISS. Absent a ruling v2 carries the honest default: no trie; whole-snapshot duplication is the recorded shipped behaviour** | owner |
| **G8** | security documentation (R3-7b) | OpenAPI security **0/45**; `save_contour(req)` takes no `Depends`/`Header`/`Request` and its router declares no `dependencies=`; blob + thumbnail GETs carry no `Depends` with `Cache-Control: public, max-age=86400` | **every row of `operation-register.md` carries an authority class**; the `save_contour` and image-GET decisions are made | fourier API row |
| **G9** | client-gap closure (R3-7c) | 36 client edges / 9 gap operations; **7 of 13 `/api/visualizations` operations have ZERO client function** (remix, publish, unpublish, forks, provenance, diff, versions) | every gap operation carries a disposition (clientable / struck / deliberately server-only). ▲ **Counting lock K-1: read against 13/44, never zero; cite 45/30/13** | F.W1/F.W4 (clients) · fourier API row (strikes) |
| **G10** | the like verb | `likes:int=0` + the `(visibility, likes, -id)` index + `cursors.py:22`'s `"likes"` sort key all ship; **no like route anywhere** (`api.ts` 0, `routers/` 0); `gallery.ts` hard-codes `liked = true` | ship the 46th operation or delete the affordance — **no third option ships** | fourier API row **or** F.W4 (deletion) |
| **G11** ⊙ | moderation producer-or-retire (**ADMISSION**) | every `db.flags` write repo-wide is a fixture or a `$rename` migration; `FlagRequest` referenced nowhere; no client posts a flag; **~110 of 285 panel lines dead by reachability**. value.js HAS the verb (`POST /:slug/flag`) | producer-or-retire RULED. ▲ **This ruling PRECEDES F.W1's sizing wherever FR-AFP grades are load-bearing (ruling 5)** | owner → fourier API row / F.W1 |
| **G12** | unsafe GET / counter provenance | `find_one` first, `$inc {views:1}` after, `_public_doc` serialises the **pre**-increment doc; the same verb is the ETag-capture path (publish adds a phantom view; an entity is born at `views: 1`); no `viewed_ips` while `liked_ips` exists | the read is safe (explicit verb) **or** v2 states the mutating-GET policy against RFC 9110 §9.2.1. ▲ **repair tests must NOT assert a re-open increment** | fourier API row + F.W4 |
| **G13** | cache identity ⊇ consumed fields | `extraction_cache_key` is a closed 10-field `json.dumps` literal omitting `ml_threshold`/`ml_detail_threshold`; the handler short-circuits on the hit **before** `compute_contours` | the superset clause is stated | fourier API row (fixture: `contour_hash` **instability** across an ML-threshold change — also the SS-13 probe) |
| **G14** | contour provenance | editor-saved contours carry no `extraction_cache_key` (`source="editor"`), so exiting the editor re-runs extraction and the server cache-hits the ORIGINAL — the edit is destroyed with zero settings touched | editor-saved contour is a first-class compute input; extraction cannot overwrite a `source="editor"` asset | fourier API row |
| **G15** | image bounds on write | `store_contour_asset` called with **no** `image_bounds` on the POST path; the param defaults `None` and is written verbatim; backfill is GET-only — content-addressing means **the overlay dies exactly when the points changed** | derive-on-POST or backfill-on-write is ruled | fourier API row |
| **G16** | canonical-geometry provenance (**TRIPWIRE**) | 3.7% of shipped `moon.json` points within 1.0 unit of any extracted point; p90 37.6 vs 1.23-unit spacing; crescent-alone bbox strictly contains the shipped bbox; the only claimed regenerator imports `order_contours` (**no module defines it**) and is itself gitignored | the pipeline decision (true source named or artifact re-authored) **with** G5c's diagnostics row. ▲ **Until then: DO-NOT-REGENERATE on `master`; any regeneration attempt revives L-B1 and L-B2/C-2 at BLOCKER** | F.W6 / build lane |
| **G17** | boundary validation | the whole 45-operation client surface lands as ONE unchecked `(await res.json()) as T`; `save_contour` takes `xs`/`ys` off `dict[str, Any]` with zero cardinality validation; six admin tiles render em-dashes on drift with **no console error, no toast, no test** | every response class v2 names is validated at the boundary **or** stated not-validated-and-why (the evenness clause) | fourier API row + F.W4 |
| **G18** | casing + envelope isomorphism | v1 §4:140 names TS-camel ↔ Python-snake as the **only** allowed envelope difference and §6 binds each repo's probe to the doc, not the sibling; fourier's `DiffResponse` conforms (`from_hash`/`to_hash`) — **value.js has no diff surface to conform, so the parity verdict is unrunnable** (see G4) | v2 restates the casing rule as a **mechanical check on each side**; §6 re-authored; both probes exist **or** the one-sided verdict is explicit | F.W9/W10 + value.js API row |
| **G19** | carry closure (M-25 / COHESION §3.1) | **this table**: 126 banked rows in the CARRY input. Fold-seat receipt 2026-08-25: **112 rows verified id-for-id at the fold** (the delivery truncated mid-row `L-B2 / C-2`); the full 126-row set-difference has NOT yet run — exactly what keeps this gate RED | **set-difference ∅ in BOTH directions** between the CARRY row ids and the §4 clause tables + §7 Excluded: `grep -o` the id column of §4 and §7 and diff against the CARRY ids. A row that appears in neither is a **silent drop** — the defect class this program exists to kill | F.W5 itself (unit e) |
| **G20** | co-signature actually relayed | COHESION §2: *"Cross-repo edges are declared FROM BOTH ENDS in the spec files"* — today v2 does not exist and no letter has been sent | the relay letter exists at `$V/docs/tranches/X/coordination/`, is ledgered in `INBOX.md` (E13), and §6 of this file declares this end of every edge | F.W5 itself (unit e) + the fourier side's reciprocal |

## 6. Sequencing — locks and cross-edges (declared from this end)

### 6a. In-wave order

`a` (register) → `b` (§A/§B) → `c` (§C/§D/§E) → `d` (§F/§G + value-side obligations) → `e` (rulings + relay + carry closure). Serial; each commits before the next opens.

### 6b. Cure-integrity locks (bind the repair, not the finding)

**AA-23** ▲ the regex-action cure is REJECTED; AA-6's order (a)(b)(c) only · **K9** ▲ do not delete the `?? item.slug` fallbacks · **K12** ▲ B-2's duplicate-row outcome is closed STATICALLY; SS-13 spends no probe · **K-13 (ConvergencePlot)** ▲ `latex` is budget-truncated; the prescribed cure is dead, a new field is the only sound one · **K-13 (FunctionInput)** ▲ the ratio-1 no-touch path is unreachable; cite the sessionStorage path · **K-3** ▲ m-7's 422-straddle is refuted; do not cite it · **K5** ▲ the D-1 skeleton cure is killed · **K10 / ruling 5** ▲ FR-AFP-8 sits at MAJOR; the roster grades the POPULATED surface · **K-1** ▲ 13/44, never zero · **K-6 / S-8** ▲ absence-proofs enumerate the surface · **M-CK** ▲ fix the key FIRST · **M-2** ▲ the tracked in-process generator exists for 5 of 7 assets · **C-3 (FSE)** ▲ no closure heuristic · **S-6** ▲ do not cure the fetch waste by removing the guard · **C4/D-L1** ▲ the 429 loop's hoisted signal is a property, not an accident · **m-15 ≠ F-4** ▲ cross-referenced, never merged · **D-19** ▲ re-resolve every anchor after F.W0 · **FR-GIG-5 mirror** ▲ no wave is credited with another's cure.

### 6c. Cross-edges

| edge | direction | this end's declaration |
|---|---|---|
| **F.W0** substrate pre-gates | F.W5 **DEPENDS — hard** | No line anchor, no HEAD figure, no "LIVE at certified HEAD" citation until F.W0 re-grounds: corrupt glass-ui 4.0.0 `dist/styles/index.css` (fr-PaperSidebar M1), the dirty fourier worktree (GAB-13 — **re-measured 28 entries, and the census pin `14d83356` does not resolve**), F8-REACH-01/02 |
| **F.W1** atomic tri-package uplift | F.W5 **GATES ONE ORDERING**; otherwise independent | **G11's producer-or-retire ruling PRECEDES F.W1's sizing** wherever FR-AFP grades are load-bearing (ruling 5). Otherwise no gating in either direction: F.W1's transaction (producer bump + 162-site prop rewrite + copied→status triple **in ONE change**, else four zero-console-error e2e gates go red — FR-EQR-3) is untouched, and **F.W1 must NOT be credited for the pagination-drain cure (FR-GIG-5)**. F.W5 claims no credit for anything F.W1 owns |
| **F.W3/W4** shadow retirement + frontend audit | F.W5 **EMITS display arms** — one home, two citations | AA-6 placeholder honesty · AA-10 actor legend · AA-5/AA-24 display taxonomy · GCM-10 tier guard · GAB-15/-17 label honesty + render-after-GAB-16 · FR-AUL-21 unknown-status render · FR-AUL-25 / FR-AFP-9 copy · FR-AFP-8 one-token label · the like-verb `aria-pressed` + re-click guard · FR-AFP-32's shared formatter (**already banked FR-AUL-17 → F.W3; do not re-book**) · B-1's unit-true control · M-10's typed vocabulary · ContourPreview row 11's `?:` · FR-GFC-1's in-place patch · FR-EQR-4's `plainLatex()` **if and only if F.W5 declines the wire field**. F.W1 precedes F.W3/W4 (§2 law); these emissions inherit that ordering |
| **F.W2** value.js/colors migration surface | F.W5 **EXPORTS zero-cells** (P-9) | App API-inert · MobileFloatingToc · CollapsibleSection · PaperSearch / PaperSearchInput / PaperSearchDropdown (measured zeros) · MorphShapePreview (structurally unreachable) · **UserSlugBar FR-USB-38 — do NOT book this component into the F.W2 migration surface** · CanvasControlsDock / AnimationControls zero migration cost. Copy into F.W2's **and** F.W4's denominators; one home, two citations, no re-booking |
| **F.W6** provenance defect burn-down | F.W5 **STATES**, F.W6 **BURNS** | Intake X-8: F-α corroborated three ways; **F.W5 owns the chain CLAUSE (G3), F.W6 the burn-down. No double-booking** |
| **F.W9/W10** gates + close | F.W5 **EMITS conformance obligations** | v1 §6's close-gate is re-authored by v2 and **executed there**, each side's probe asserting against the doc, never the sibling (inv-26). Plus the coverage seams: AA-44 (zero frontend tests; nine e2e specs never touch the audit tab) · GAB-12's test seam · FR-AFP-66's orphan-flag test exercising only the reporter-keyed path · **FR-AUL-13's dead error branch — a contract document without a probe REPEATS the defect that row convicts** |
| **SS-4** union synthesis | F.W5 **IS its input** | SS-4 **FLAGS THE OWNER RULINGS INLINE**: G4 (TA-4 restore-or-rescope) · G7 (trie-vs-KISS) · M-9 (off-state `[]` admission) · G10 (like verb) · G11 (flags producer-or-retire) · FR-AFP-9 (hard-delete arm) · FR-AFP-71 (codegen vs hand-typed twins). SS-4 **names the TA-4 value-side atomdiff restoration as PREREQUISITE or RE-SCOPES EXPLICITLY**. Two registry-integrity items: **(a)** GCM-10's cure cites a companion row **"MF-9" that resolves to nothing** in the adjudicated registry — resolve or **strike before v2 quotes the cure**; **(b)** fr-EasingPicker booked its easing-domain server half as "new" when fr-EasingCurvePreview's RESOLVER already carried the same cure — **merged at E9, both ids preserved** |
| **the value.js API row** (CO-SIGNER) | F.W5 **CREATES value-side obligations** | The co-signature is not free: **(1) TA-4** — restore `lib/crud/atomdiff.ts` + `atomDiff` on `PaletteVersion`, or be re-scoped out (G4); **(2) V-α** — revert writes no version row yet still `$inc versionCount`, permanently drifted, no test asserts otherwise; **(3) V-β** — the global version `_id` collision (G2); **(4) V-γ** — nullable `userSlug` means an unattributable edit writes no version row. **These are VALUE-SIDE rows, never re-booked as fourier defects.** Cross-check against `api-gap-remeasure.md`'s nine classes + **X-1** (`computeContentHash` ≠ `payloadHash`), **X-2** (stored `forkCount` as public authority), **X-3** (PATCH TOCTOU / no CAS) and **D9** (visibility `private\|public`; lifecycle `active\|trashed`; moderation `clear\|withdrawn`) — **v2's visibility clause must not contradict a ruled owner decision** |
| **glass-ui BH relay (SS-6)** | **DECLARED EMPTY** | F.W5 carries **ZERO** producer rows. Declared so that (a) no glass-producer row is smuggled in as a contract clause, and (b) no contract row is discharged as a frontend hack. Any producer-shaped finding surfacing during authoring goes to the standing glass-ui BH inbox **as a letter** |
| **X·parse-that** | **FORBIDDEN EDGE (declared)** | Direct parse-that→fourier routing is forbidden. No row in §4 is discharged through the parser sub-tranche; anything parser-shaped routes through the megatranche, never across |
| **SS-13 / live-probe lane** | F.W5 **DEFERS magnitude only** | FR-AUL-14 (incidence of fieldless user docs) · FR-AUL-20 (tombstone magnitude) · M-14 (end-to-end contour-loss reproduction) · VV-R2-A (observable phantom view) · C·D-02/M-15 (is the browser hop HTTP/2 — the empty-`statusText` arm) · FR-AFP-4 (deployment auth-proxy) · FR-AFP-33 (aggregate cost) · FR-GSB-28 (interleave symptom) · L-M2 (extraction-scope magnitude) · L-B1 ("what `moon.json` WAS generated from" — undetermined) · FR-AFP-32 JSC acceptance · D2's symptom magnitude (SS-13). ▲ **Two probe-suppression locks: K12 and FR-GV-24** |

## 7. Excluded — carried nowhere else, with reason

Nothing below is a silent drop; each is named so a later reader cannot mistake absence for oversight.

| excluded | reason |
|---|---|
| **All F.W3/F.W4 display arms** (enumerated in §6c) | Routed, not dropped: one home (that wave), two citations (there and here) |
| **F.W1's atomic tri-package uplift**; FR-GIG-5's pagination-drain cure | F.W1's; F.W5 claims **no credit** — and F.W1 may not be credited with FR-GIG-5's cure either |
| **AA-48** (raw buttons missing `type`) | Same latent class as AA-45, **discharges free with AA-11's F.W4 footer cure — recorded, NOT re-booked** |
| **FR-GFC-1's body cure** (view/store seam, in-place patch) | F.W4's. F.W5 takes the **rider only**. The D-1 skeleton cure is **KILLED (K5)** |
| **The regex-action cure** (AA-6 L-4 / C·D-2's own recommendation) | **REJECTED by AA-23** — an indexed equality would become a collection scan on the one monotonically growing collection |
| **Deleting the five `?? item.slug` fallbacks** (FR-AFP-36's second limb) | **NOT PROVEN (K9)** — `admin.py` emits `doc.get("image_slug")` unvalidated; the fallbacks are defensive |
| **A live probe of B-2's duplicate-row outcome** | **K12** — closed statically (no-header idempotency passthrough + the plain index); SS-13 spends no probe |
| **A repair test asserting a re-open view increment** | **FR-GV-24 lock** — the session guard works; the defect is SCOPE, not absence |
| **m-7's straddle-a-422 scenario** | **REFUTED from source (K-3)**; v2 must not cite it |
| **L-B1's "without ever touching the offending slider" headline** (FunctionInput) | **KILLED (K-13)** — rides the ratio-1 state proven unreachable from defaults; the sessionStorage-restore path is the true one |
| **β's 428-escalation hypothesis** (FR-AFP-10/-70 band) | Killed by its own author (If-Match `*` accepted). **Do not revive** |
| **C·S-2 / K-3 (AA-5's enumeration method)** | **Killed (K-6)** — `grep "log_audit("` is structurally blind to an inlined second writer; the S-8 method law replaces it |
| **A post-hoc closure heuristic** for open-vs-closed contours | **KILLED (C-3)** — 44.08 closed vs 44.97 open overlap; the flag must be **carried**, not inferred |
| **SE-05's "just delete it"** for the `window` handoff | **WRONG on the tree (G6c)** — the precompute script names the component as its INPUT |
| **Regenerating `moon.json` / `sun.json`** | **TRIPWIRE (G16)** — DO-NOT-REGENERATE on `master`; any attempt revives L-B1 and L-B2/C-2 at BLOCKER |
| **Trie / structural-sharing design** | **⊙ G7 unruled.** v2 carries the documented default (no trie; whole-snapshot duplication) and authors no design |
| **The value-side atomdiff restoration itself** | **⊙ G4 unruled.** F.W5 states the prerequisite; the restoration is the value.js API row's act |
| **FR-AFP-71's module split decision** | **⊙ owner-gated.** v1 chose hand-typed twins explicitly; **v2 may not silently reverse it** |
| **GCM-10's "MF-9" companion cite** | Resolves to **nothing** repo-wide in `registry/adjudicated/*.md`. **Struck unless SS-4 resolves it**; v2 quotes GCM-10's cure without it |
| **Negative-control rows in any denominator** | HLG-23 and MorphShapePreview §3 are **EVIDENCE** (P-9); counting them manufactures overlap |
| **Any glass-ui producer row** | SS-6 relay declared **EMPTY**; producer-shaped findings leave as letters |
| **`docs/tranches/X/COHESION.md` edits** | Root-authored at the boundary; editing it here would be a hidden cross-wave write conflict |
| **All product source, both repos; all fourier bytes** | Execution gate + read-only law |

## 8. Agent Units

### X.F.W5.a The operation register
- **Goal**: one machine-readable enumeration in which the API's size, each operation's authority, and each operation's client edge are stated once and agree.
- **Mechanism**: enumerate 45 = 30 + 13 + 1 + 1 from the route inventory (lane-crud §4 + the gallery/app routers), **counting prefixed routers explicitly (K-1)**; one row per operation: `path · method · authority class · client edge (present/gap/struck/server-only) · disposition`.
- **Files**: `contract/operation-register.md`.
- **Sub-gate**: G8, G9 close for F.W5 (every row carries an authority class and a disposition; the triple is cited, "30" never stands alone).

### X.F.W5.b Wire shape and the join relation — v2 §A/§B
- **Goal**: the contract can say what a row *is* and whose identity an operation carries.
- **Mechanism**: author §A (A1–A6) and §B (B1–B5), including the R6-8 clause **once** with its instance list, the mirror rider as a first-class second direction, the untyped-operation clause with its exception set named or closed, and the liveness predicate.
- **Files**: `contract/J-diff-shape-v2.md` (§A, §B).
- **Sub-gate**: G1, G17 close for F.W5; B5's negative controls appear in the clause justification and in **no** denominator.

### X.F.W5.c Authority, coverage, provenance — v2 §C/§D/§E
- **Goal**: every operation states who may call it, every count states what it counts, and every derived variant states its lineage.
- **Mechanism**: author §C (C1–C5), §D (D1–D13), §E (E1–E16), carrying every ▲ lock verbatim; E16 carries the documented no-trie default pending G7.
- **Files**: `contract/J-diff-shape-v2.md` (§C, §D, §E).
- **Sub-gate**: G5, G6, G10, G11, G12, G13, G14, G15 close for F.W5 (clause + disposition); G7 and G11's ruling flagged, not invented.

### X.F.W5.d Equation contract, geometry, and the value-side obligations — v2 §F/§G
- **Goal**: the two seams that are not gallery CRUD — the equation envelope and the build-time artifact pipeline — get the same treatment, and the co-signer's bill is itemized.
- **Mechanism**: author §F (F1–F9) — **including the F7 seam choice, made here** — and §G (G1c–G9c) with the tripwire; re-author v1 §6's close-gate and §4's casing rule as mechanical checks; append the value-side obligation list (TA-4 / V-α / V-β / V-γ + X-1/X-2/X-3 + D9 cross-check).
- **Files**: `contract/J-diff-shape-v2.md` (§F, §G, §close-gate, §value-side obligations).
- **Sub-gate**: G2, G3, G16, G18 close for F.W5; **F7 is decided, not deferred** (a decline is a decision and is recorded as one).

### X.F.W5.e The ruling block, the relay, and carry closure — *fresh Fable adjudicator*
- **Goal**: the rulings the wave may not make are asked cleanly, the contract reaches its co-signer, and no carried row went missing.
- **Mechanism**: author `OWNER-RULINGS-F.W5.md` (seven rulings; each with its honest default, its cost either way, and its dependent clauses); write the relay letter; append the E13 INBOX row; run the G19 set-difference both directions and resolve or strike the MF-9 cite. **Adjudication, not mechanism — M-23/M-12.**
- **Files**: `contract/OWNER-RULINGS-F.W5.md`, `coordination/value-to-fourier-…-cosign-J-diff-shape-v2.md`, `docs/tranches/V/coordination/INBOX.md` (append).
- **Sub-gate**: G4, G7 flagged with defaults carried; **G19 ∅ both directions**; G20 green.

## 9. Format And Lint Cadence

Docs-only wave. After each unit and before close: `git diff --check`; the repository's markdown link check over the new `contract/` paths; and the G19 id set-difference re-run. **`npm run typecheck` / `test` / `lint` are NOT this wave's gates** — no source byte moves, so a delta in any of them means the wave escaped its bounds and is itself the failure signal (run once at close as that tripwire, not as evidence).

## 10. Verification Artefacts

Committed at close (L-7 — an uncommitted witness does not exist):
- `docs/tranches/X/fourier/contract/J-diff-shape-v2.md` + `operation-register.md` + `OWNER-RULINGS-F.W5.md`;
- `docs/tranches/X/fourier/waves/F-W5/carry-closure.md` — the G19 set-difference output, both directions, with the 126 ids listed and each id's landing clause;
- the relay letter path + its INBOX row;
- the F.W0 re-grounding receipt this wave's anchors were re-resolved against (cited, not copied);
- the MF-9 resolution or strike, recorded with its grep.

## 11. Commit Plan

Pathspec commits only; `scripts/dev/dev.sh` never staged.

| # | scope | contents | body |
|---|---|---|---|
| 1 | `docs(X·F.W5): operation register — the 45/30/13 enumeration` | unit a | yes |
| 2 | `docs(X·F.W5): J-diff-shape v2 §A/§B — wire shape + the join relation` | unit b | yes |
| 3 | `docs(X·F.W5): v2 §C/§D/§E — authority, coverage, provenance` | unit c | yes |
| 4 | `docs(X·F.W5): v2 §F/§G + close-gate + value-side obligations` | unit d | yes |
| 5 | `docs(X·F.W5): owner rulings + co-signature relay + carry closure` | unit e, incl. the G19 artefact | yes (status) |

## 12. Dependencies

- **Depends on**: **F.W0** (substrate pre-gates — hard; §5 X-1) · the intake CARRY table · the adjudicated registry (66/66, `35fc8ebf`) · **owner rulings for G4 and G7** to close those two gates rather than merely flag them.
- **Blocks**: **F.W6** (the burn-down needs the chain clause) · **F.W9/W10** (the close-gate they execute is re-authored here) · **the value.js API row** (its four obligations are itemized here) · **F.W1's sizing**, *only* through G11's ruling (ruling 5).

## 13. Archaeology

- **Prior attempt**: `J-diff-shape.md` (J-era, 271 lines) is *already* "repo-neutral, already binding both sides on paper" (lane-crud §7) — and it **lives in one tree**, its §2.5 mandating a value.js file that T.W1/TA-4 excised, its §6 close-gate assuming a probe that cannot run. **New guardrail**: v2 lives in the neutral home, and G4 refuses to let the diff clause be authored as though both sides could conform.
- **Prior attempt**: the atom-diff pattern was *"authored once (fourier), adopted twice (value.js twin)"* — and the twin's only survivor is a compiled corpse in an untracked build dir. **New guardrail**: E3/G4 make participation an explicit ruled state, not an assumption inherited from a docstring.
- **Standing failure mode this wave is built against**: FR-AUL-13's cell — a client *"written against a contract DOCUMENT rather than the router."* **v2 IS such a document.** Its guardrail is the F.W9/W10 conformance obligation declared in §6c, without which this wave manufactures the exact defect it convicts.

## 14. L-18 rider

The arbitrated fold of the two authoring halves is not ACCEPTED on its gates alone. After G1–G20 read as specified, the contract must survive **two quartet challenge passes** (4 Opus skeptics per pass, then a fresh Fable apotheosis) before X·F may cite it as law. A clause the challenge shows to be unfalsifiable is struck and re-authored, and the wave returns to SPECIFIED. Referenced here so no close can claim it unforeseen; the pass runs under `FORMATION-LAWS` L-18, not by this file.
