# F.W13 — the owner's 2026-09-23 fourier docket (rounding · working controls · the image mode's empty state · the dock that morphs on scroll)

## State
**Opens after**: F.W12 CLOSED. **Minted by**: COHESION §0bc (2026-09-23). **Model**: Opus 5.5 for every seat (owner, 2026-09-23).
**Agents**: 4 units — [`.a`] → [`.b`] → [`.c`] → [`.d`]. **Record**: `docs/tranches/X/execution/C/F-W13.md`.
**Tree**: `/Users/mkbabb/Programming/fourier-analysis`, `m/w1-bump-migration`, baseline `3e709e4`.
**Evidence (owner frames, banked)**: `docs/tranches/X/fourier/evidence/W13/owner-2026-09-23-{1..5}.png`.

## Authority — the owner's words, verbatim (2026-09-23)
> The fourier analysis dock, and all docks like it on all pages, should, like in the words app hereof (floridify), ala ios 27, shrink and morph on scroll to go into a smaller state--and change on and expand on focus and hover. And the progress bar for the page scroll should be integrated into the bottom of the dock, too, and properly clip and account for rounding [frame 1]--this is not rounded enough and not glass-ui idiomatic, and it doesn't even work [frame 2]--this is not a circle [frame 3]--many of these items either don't work or are not glass-ui idiomatic, rounded, etc [frame 4]--this "drop click to upload" text is updlicative--we should just display the main area with no right sidebar when nothing is there, and then smoothly animate in the right sidebar when something is dragged over and dropped. [frame 5]--this is good, but not rounded.

## Goal criterion
Every surface the five frames show is rounded on the producer's radius scale, composed from glass-ui
primitives, and WORKS; the image mode shows one drop affordance and no sidebar until there is an image;
the dock shrinks on scroll, expands on hover/focus, and carries the page-scroll progress in its bottom
edge, clipped to its radius — on the producer's dock, never a consumer re-implementation.

## §1 Bounds
`web/src/**` (components, views, composables, `style.css`) · `web/e2e/**` (new `f-w13-*` specs + screenshots)
· value.js `execution/C/F-W13.md` · `LEDGER.md` · `INBOX.md` (mail rows only).
**Do NOT touch**: `glass-ui/**` (READ-ONLY; asks ride relay O-55) · `package.json` pins except as `.d` states · every other spec.

## §2 Agent Units
### F.W13.a (Opus) — frames 1, 2, 5: the radius register and the two broken controls
1. Census every square/under-rounded surface in the paper sidebar (`components/paper/PaperSidebar.vue`, the
   CONTENTS header + its collapse button, frame 1), the back/undo control (frame 2), and the visualizer's
   panels (Function, Controls, Coefficients, the equation card, the plot card, frame 5) — computed
   `border-radius` per surface, pasted. Each re-points to the producer's radius tokens / surface primitive
   (`Card`/`Surface`/`Button` as the producer names them; read their README/types whole); an icon-only
   control is a true circle (`width == height`, radius ≥ 50%, the producer's icon-button). No per-instance override where a root/primitive owns it.
2. Frame 1's CONTENTS toggle "doesn't even work": reproduce, name the cause at the bytes, cure at the owner. Frame 2's control likewise.
3. Gate G-a: a new `web/e2e/f-w13-radius.spec.ts` asserts, per named surface, radius on the scale and icon-only controls circular; the two controls act (toggle collapses the contents; back navigates) ×2.

### F.W13.b (Opus) — frame 3/4: the image mode's controls
1. Walk every control in the image mode's right sidebar and its main area (`components/visualization/ImageUpload.vue` and its siblings): for each, does it work (act + observe) and is it the producer primitive? Table pasted. Cure every non-working control at its owner; every hand-rolled field/button/section moves onto the producer primitive.
2. Gate G-b: `web/e2e/f-w13-image-controls.spec.ts` — each control acts ×2; 0 hand-rolled controls in the census re-run.

### F.W13.c (Opus) — frame 4: one drop affordance, the sidebar arrives with content
1. The duplicate "Drop or click to upload — PNG/JPG/SVG ≤ 10 MB" sidebar card retires; the main area's drop target is the ONE affordance (click-to-browse + the format/size line move into it, the producer's type).
2. With no image, the image mode renders the main area full width and NO right sidebar. On dragenter the drop target signals; on drop (or file pick) the sidebar enters with the producer's motion tokens (a width/translate enter, PRM-honoured), and leaves if the image is cleared.
3. Gate G-c: `web/e2e/f-w13-image-empty.spec.ts` — no sidebar element at rest; one upload affordance; after `setInputFiles` the sidebar is present and its enter ran (an animation observed or the transition class seen); PRM context → no motion ×2.

### F.W13.d (Opus) — the dock: shrink-on-scroll + the progress rim (PRODUCER-GATED)
1. The behaviour is a producer feature: `GlassDock` today has `collapsed | hover | pinned` on hover/focus but no scroll-driven compact state and no progress seat; `ScrollProgressRim` exists standalone. Relay **O-55** (COHESION §0bc) asks glass for both. Reference implementation for the ask: words `frontend/src/components/custom/search/composables/useSearchBarScroll.ts` (hysteresis, expand-on-focus/hover).
2. At this unit's sitting: if the installed/installable glass exposes the ask, adopt it (bump within the app's own major policy, one commit), wire the page scroll into the dock's rim, and gate G-d: scroll 400 px → dock compact (measured width/height drop); hover or focus → expanded; rim fill = scroll fraction ±2 %; the rim is clipped by the dock's radius (no pixel outside the rounded box in a clipped screenshot). If it is NOT available, write NO consumer copy: G-d stays honest-RED by id **`DOCK-SCROLL-MORPH`** (owner: glass, O-55), and the unit records the probe.

## §3 Gates
G-a · G-b · G-c ×2 each; G-d or `DOCK-SCROLL-MORPH`; `npm run typecheck` + unit tests + the existing e2e suite unchanged-GREEN; before/after screenshots of all five frames at the owner's crops, committed under `web/e2e/screenshots/f-w13/`.

**ADDENDUM 2026-09-23 (COHESION §0bm).** ESC-F13a-1 · ESC-F13b-1 · ESC-F13c-1 are one class — existing specs and one golden pin the chrome this wave was ORDERED to retire — ruled as ONE grant to unit **`.e`** (Opus): `web/e2e/visualization-ux.spec.ts:72` → the DockControl `getByRole('button', { name: /(Play|Pause) animation/ })`; `web/e2e/contour-extraction.spec.ts:66-76` → `getByRole('spinbutton', { name: 'Blur Sigma' })`; `web/e2e/f-w13-image-controls.spec.ts` → `openImageMode` waits for `.drop-target`, the rest limbs G-c now owns are dropped, `handRolled()` runs after `uploadImage`; `web/e2e/visual-checkpoint.spec.ts-snapshots/checkpoint-disclosure-body-chromium-darwin.png` re-baselined to the owner-ordered rounded panels (+ the NumberField fields), the diff read and pasted first to prove it is only those. Assertions keep their property; no other test changes. Gates: those four specs GREEN ×2; the suite's remaining REDs are exactly the baseline-RED set measured at `3e709e4` (visual-checkpoint 1·6·7/2/5, fullscreen:40, contrast-floor ×3), each listed with its baseline receipt. `npm run typecheck` in §3 reads `npx vue-tsc -b` (the script's real name). Push the fourier branch after the gates.
