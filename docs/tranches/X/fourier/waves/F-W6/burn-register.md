SERVED MODEL: claude-opus-5[1m]

# F.W6 — BURN REGISTER (Track C · X·F) — the wave's evidence spine

**Opened by unit `a`, 2026-09-17.** Authority: `docs/tranches/X/fourier/waves/F-W6.md` **§1 bounds row 2** —
*"CREATE — one row per burned identity: banked id · gate · commissioned act · fourier-side landing evidence.
**FW6-G17 greps its id column**; without it the carry-closure gate is prose."* This file is that operand.

**Standing conditions on every row below, stated once.**

- `/Users/mkbabb/Programming/fourier-analysis` is **READ-ONLY, always** (spec §1's NOT-in-bounds row; §4's
  last cross-edge). Every act recorded here is a **commissioned ask** — a register row plus its line in unit
  `i`'s courier letter. **Zero fourier bytes move in this wave.** Reads are permitted and are what D-19
  requires; writes are a halt condition.
- **F.W5 STATES, F.W6 BURNS** (§4 lock 2, two homes, quoted separately at §0.3 below). A row credited here
  **and** to F.W1 / F.W3 / F.W4 / F.W5 / F.W8 fails **FW6-G17**.
- **Record-qualification (R-5 / §4 lock 10)**: the ten colliding tokens `M-13 · L-B1 · L-M3 · C-17 · C-18 ·
  B-1 · B-2 · C-2 · M-10 · M-9` are written **with their record** in every row, so no set-difference
  collapses two banked identities into one hit.
- **E-3**: the spec, the adjudicated registry, the census and F.W0's receipt are IMMUTABLE. Every correction
  in this file is a dated **addendum-beside** — a moved coordinate is recorded **beside** the spec's, never
  over it, and the spec is not edited.
- **Append-only**: units `b`–`i` append their rows to §1; no seat rewrites another's row.

---

## §0 Preconditions — the three consumptions and the census pin

### §0.1 FW6-G19's discharge — F.W0's re-grounding receipt, and the anchor re-resolution

**FW6-G19's GREEN (spec §3, gate `FW6-G19`)**: *"F.W0's re-grounding receipt exists and **every anchor cited
by this wave is re-resolved against it**. **If F.W0 fails to re-ground, the wave HALTS — it does not proceed
on stale bytes**"* — owner cell *"F.W0; F.W6 cites"*. The first conjunct was GREEN before this unit opened
(banked at the wave record's baseline, D-4); **the second conjunct is this section**.

**The receipt, named**: `/Users/mkbabb/Programming/fourier-analysis/docs/tranches/F/SUBSTRATE-LEDGER.md` —
⟨cmd⟩ `ls -l $F/docs/tranches/F/SUBSTRATE-LEDGER.md` → **201,985 B, Sep 17 15:03**. Its two tables are cited
**by gate id, never by line**, exactly as §4's F.W0 cross-edge orders (*"Cited hereafter as F-W0 §4 G-11 /
§4 G-12 — by gate id, never by line"*) — ⟨cmd⟩ `/usr/bin/grep -nE "^### G-11 —|^### G-12 —" waves/F-W0.md` →
`364:### G-11 — ONE corrected anchor table published; every later wave quotes it` ·
`369:### G-12 — ONE corrected-denominator table published; superseded figures FORBIDDEN downstream`
(*the line numbers are the command's **output**, never this file's address for them*).

**F.W0 re-grounded. The wave does not halt.** ⟨cmd⟩ `git -C $F rev-parse --short=8 HEAD` → **`3bac3d52`** ·
⟨cmd⟩ `git -C $F status --porcelain | wc -l` → **0**. The spec's born-RED witness figures (`cd26c653`, **28**
dirty paths, GAB-13, the unresolvable census pin `14d83356`) are **F.W5-authoring-pass readings and no longer
reproduce** — which is what F.W0 was for.

#### §0.1a OG-F1's narrowing, stated — quoted from COHESION §0j.D, never re-ruled

> **OG-F1** — **FREEZE-WITH-ADOPTION AND WORKTREE-AS-BASELINE** (the lane's own evidence; measurements
> live-reproduced; R6 `NO_SUCCESSOR`). Consequences as the spec states them: G-1's minute becomes a
> disclosure + the LAND set; **G-11 narrows to drift-correction**; G-14's R-coordinate rows become records;
> **GAB-13 discharges to a disclosure line.**

> **OG-F2 / OG-V2** — **CODEX-ERA-SPECIFIC.** The pre-write root-absence-receipt requirement and the
> Codex-authored prohibitions died with M-15's abrogation (2026-07-27); the *finding* under OG-F2 (the
> absence, TRUE) stays adopted; the `FOURIER-R*` value-side files are records. G-F10-8 closes on the
> charter line.

**What the narrowing means for this wave, said plainly so no burn seat over-reads it.** G-11 is
**drift-correction only** — F.W0's own §2.1.4 says so in its own hand: *"Under **FREEZE-WITH-ADOPTION AND
WORKTREE-AS-BASELINE**, G-11 is **drift-correction only**. It does **not** re-root the R-coordinates, and it
does **not** re-derive the corpus's producer citations at a new epoch."* Two consequences:

1. **G-11's eleven-row drift register names ZERO F.W6 anchors.** Its rows are frontend/producer coordinates,
   and its row 5 (`fr-AdminAuditLog K-13`) states the boundary in its own state cell: **RECORD (api/py
   coordinates are outside F.W0's bounds)**. So G-11 corrects nothing this wave cites, and **F.W6 may not
   read that silence as "the anchors are certified."** The re-resolution is therefore performed **here**,
   read-only, under D-19 MEASURE-AT-OPEN, and published at §0.1c as this wave's own act — which is the
   narrowing's cost, stated rather than absorbed.
2. **GAB-13 is a disclosure line, not a blocker.** Recorded as one, here: *the 28-dirty-path worktree the
   spec's born-RED witnesses were taken over is gone; the audited tree is the committed tree; the F.W6
   figures that rested on it are re-measured below or re-measured at the wave record's baseline.*

**The G-11 export this wave DOES consume, by gate id**: the standing rule minted at F-W0 §4 G-11 —
*"**Producer-side evidence carries the producer COMMIT HASH, never the version string**"* (P-4). F.W6 carries
**zero** producer (glass-ui) rows — §4's SS-6 edge declares the relay **EMPTY** — so the rule binds
vacuously here and is recorded so a later seat does not read the empty relay as an omission.

#### §0.1b G-12's forbidden-figure register, checked against this wave — ZERO hits

F-W0 §4 G-12 publishes a **FORBIDDEN-FIGURE REGISTER** (*"Quoting any of these downstream is a defect against
G-12 on sight"*). Measured over the spec's settled bytes, base `$W`, double-run, engine `/usr/bin/grep`:

⟨cmd⟩ `for pat in '7 e2e' '22 remaining' '8 routes' '12 sites / 8 files' '38 sites' 'text-admin-label'
'v-html' 'a3702f' '9d6415' '14 Button' 'MetricBadge' 'tsconfig.app.json' '7.0.0'; do /usr/bin/grep -c -F
"$pat" F-W6.md; done` → **0** at every one of the thirteen. The fourteenth entry (*any array-literal
`loops[].cardinality`*) returns one `-F 'cardinality'` hit, and it is **not** the forbidden figure —
⟨cmd⟩ `/usr/bin/grep -n -F 'cardinality' F-W6.md` → `430:| fr-ContourPreview row 28 | 2 | carried at §2.6
(boundary cardinality on the write path) |`, an ordinary noun in a landing cell, no `loops[]`, no integer.

**Result: F.W6 quotes no superseded denominator.** Recorded as a measured negative, not as an assumption.

#### §0.1c THE ANCHOR RE-RESOLUTION — every anchor this wave cites, at the true bytes

**Substrate delta, measured first, because it governs how much of this is new work.** G-11's table is dated
at fourier **`8bc7736`**; HEAD is **`3bac3d52`**. ⟨cmd⟩ `git -C $F diff --name-only 8bc7736 3bac3d52 -- api
| wc -l` → **0** — **no `api/` byte moved between G-11's substrate and this wave's open**, so every server
anchor below sits on the same bytes G-11's seat read. ⟨cmd⟩ `git -C $F diff --name-only 8bc7736 3bac3d52 --
web` → **13** files (`e2e/unit/figure-dimensions.unit.ts` · `e2e/visualization-crud.spec.ts` ·
`e2e/visualization-ux.spec.ts` · `InfoCard.vue` · `ConvergenceTimeline.vue` · `HarmonicLevelGrid.vue` ·
`CanvasControlsDock.vue` · `CanvasOverlayButton.vue` · `EditorControlsDock.vue` · `lib/basis-display.ts` ·
`lib/colors.ts` · `main.ts` · `tsconfig.json`) — **not one of them is an F.W6 anchor**; they are G-10's
DELETE + the `aria-pressed` lift and the `--viz-*` cure, which are F.W0's own acts.

**Method.** Each coordinate is printed by ⟨cmd⟩ `/usr/bin/sed -n '<L>p' <rooted path>`, read-only, at
`3bac3d52`, double-run. **HOLDS** = the spec's claim is at the spec's coordinate. **HOLDS ⟨beside⟩** = the
claim is true and the coordinate needed a correction, which is recorded **beside** the spec's spelling and
**never over it** (E-3). No anchor was found false.

**A. The fourier server — `api/` (no byte moved since `8bc7736`)**

| spec's coordinate | re-resolved at `3bac3d52` | state |
|---|---|---|
| `visualizations.py:110` `_write_root_version` def | `async def _write_root_version(` | HOLDS |
| `:138-141` the hard-coded quadruple | `:138 parent_hash=None,` · `:140 root_hash=set_hash_value,` · `:141 depth=0,` | HOLDS ⟨beside: `:139` inside the span is `forked_from_hash=fork_of_hash`, which the spec does not name and which is not part of the claim⟩ |
| `:146` the only `visualization_versions` insert | `await db.visualization_versions.insert_one(version.model_dump(by_alias=True))` | HOLDS |
| `:220` create call · `:592` remix call | both `await _write_root_version(` | HOLDS |
| `:256` → `:268-270` → `:272` the unsafe-GET order | `doc = await db.visualizations.find_one({"slug": slug})` · `:269 {"$inc": {"views": 1}, "$set": {"last_accessed_at": …}}` · `body = _public_doc(doc)` | HOLDS |
| `:381-383` the bare `$set` | `:381 updates = {k: v for k, v in body.model_dump(exclude_unset=True).items() if v is not None}` · `:383 await db.visualizations.update_one({"slug": slug}, {"$set": updates})` | HOLDS |
| `:477-480` `_head_set_hash` | `:477 def _head_set_hash(doc: dict) -> str:` · `:480 return doc.get("set_hash") or atomdiff.set_hash(atomdiff.enumerate_atoms(doc))` | HOLDS |
| `:522-528` the remix tri-state idiom (the KISS lift) | `:522 if "palette_slug" in body.model_fields_set:` … `:528 child_atoms["palette_slug"] = body.palette_slug` | HOLDS |
| `:536-542` the remix no-op 422 | `:536 if not atom_ops:` | HOLDS |
| `:236` create `replay_or_record` · `:612` remix `replay_or_record` | both `return await idempotency.replay_or_record(request, _store(), f"user:{owner_slug}", _handler)` | HOLDS ⟨D-2's site confirmed: the create path already wraps the envelope⟩ |
| `:466` `_readable_or_none` def; `:507` · `:744` · `:816` · `:869` its calls | `:466 async def _readable_or_none(db, slug: str, viewer: str \| None) -> dict \| None:`; `:744`/`:816`/`:869` `doc = await _readable_or_none(db, slug, viewer)`; `:507 source = await _readable_or_none(db, slug, owner_slug)` | HOLDS |
| `:784` the bare `find_one` in the ancestor walk | `cur = await db.visualizations.find_one({"slug": parent_slug})` | HOLDS — **F-γ's witness stands** |
| `:860-889` `GET /{slug}/versions` | `:860 @router.get("/{slug}/versions")` | HOLDS ⟨beside: the file is **889** lines, so the cited span closes at EOF⟩ |
| `:493` *standalone-topology-honest* | ` ``J.W1-crud-remix §11`` (NO Mongo transaction — standalone-topology-honest):` | HOLDS |
| `:129` the compound version `_id` | `_id=f"{viz_slug}:{set_hash_value}",` | HOLDS |
| `models/visualization.py:119` required `owner_slug` | `owner_slug: str  # required, non-null (CRUD-CONTRACT §3)` | HOLDS |
| `models/visualization.py:198-210` `VisualizationUpdate`; `:207` `palette_slug` | `:198 class VisualizationUpdate(BaseModel):` · `:207 palette_slug: str \| None = None` | HOLDS |
| `models/visualization.py:221-226` the in-source tension note; `:235` the compound `_id` | `:221 ``_id`` is the content-addressed compound ``f"{viz_slug}:{set_hash}"`` …` · `:235 id: str = Field(alias="_id")  # f"{viz_slug}:{set_hash}"` | HOLDS ⟨beside: §2.10's V-β cell spells **one** filename for **two** files — `:235` is `api/models/visualization.py`, `:129` is `api/routers/visualizations.py`. Both coordinates true; the spelling is the ambiguity⟩ |
| `models/visualization.py:277` remix born `draft` | `visibility: Visibility = "draft"  # a remix child is born private (§4)` | HOLDS — **R8's fourier half already conforms** |
| `atomdiff.py:12-14` the anti-tree KISS guardrail | `:12 KISS guardrails (J.W1-crud-remix §0/§9): the atoms are a flat BAG (not a tree /` … | HOLDS — **the F-TRIE incumbent** |
| `atomdiff.py:34` `palette_slug` as atom 5; `:38` `animation_settings` in the atom set | `:34 "palette_slug",` · `:38 "active_bases", "n_harmonics", "contour_settings", "animation_settings", "palette_slug"` | HOLDS |
| `database.py:98` plain index vs `:140` unique | `:98 await _db.visualizations.create_index("content_hash")` · `:140 await _db.flags.create_index([("content_hash", 1), ("reporter_slug", 1)], unique=True)` | HOLDS — **the asymmetry is inside one file** |
| `idempotency.py:68-70` the no-header passthrough | `:68 key = request.headers.get("Idempotency-Key")` · `:69 if not key:` · `:70 return await handler()` | HOLDS ⟨beside: rooted `api/lib/crud/idempotency.py`⟩ |
| `admin.py:90-92` the non-ECMA serializer | `:92 content=json.dumps(body, default=str),` | HOLDS |
| `admin.py:110` the sole `response_model` | `"/stats", response_model=AdminStatsResponse, dependencies=[Depends(admin_required)]` | HOLDS — **1 of 13** |
| `admin.py:160-190` `set_tier` | `:160 @admin_router.put(` · `:164 async def set_tier(slug: str, body: SetTierRequest, request: Request) -> Response:` | HOLDS |
| `admin.py:435-440` the unconditional batch unfeature | `:435 elif body.action == "unfeature":` · `:438 {"$set": {"tier": "normal", "updated_at": datetime.now(UTC)}},` | HOLDS ⟨beside: the `update_many` **call opens at `:430`**, one line outside the cited span — the claim is unchanged, the span is one line short of its own verb⟩ |
| `admin.py:509` `/flagged` · `:550` the short-circuit · `:578` the K9 fallback source · `:587` `_json` · `:652` `AuditListResponse` | `:509 @admin_router.get("/flagged", dependencies=[Depends(admin_required)])` · `:550 if not flagged:` · `:578 "image_slug": doc.get("image_slug"),` · `:587 return _json({"items": items, …})` · `:652 return AuditListResponse(items=items, total=total, page=page, pages=pages)` | HOLDS — **one router, one concept, two dialects** |
| §2.4 FR-AFP-9's *"restore route live at `:430`"* | ▲ **`admin.py:430` is the batch `update_many`.** The restore route is `api/routers/visualizations.py:430` — `@router.post("/{slug}/restore")`, with `:445 result = await softdelete.restore(...)` | **HOLDS ⟨beside⟩** — the claim is TRUE at the corrected rooted coordinate; a bare `:430` inside an `admin.py` paragraph is the ambiguity, and it collides with a real `admin.py:430`. **Recorded beside, never over** |
| `janitor.py:54-58`/`:56` the sentinel · `:95` its write · `:59-99` the second writer · `:133`/`:285` two `janitor:*` actions · `:234` the reporter-keyed cascade | `:56 _JANITOR_ACTOR = "system:janitor"` · `:95 "ip_hash": _JANITOR_ACTOR,` · `:59 async def _log_janitor_audit(` · `:133 "janitor:hard_delete_visualizations",` · `:285 "janitor:prune_audit",` · `:234 flags_result = await db.flags.delete_many({"reporter_slug": {"$in": stale_slugs}})` | HOLDS ⟨beside: rooted **`api/services/janitor.py`**, not `api/lib/crud/`⟩ |
| `images.py:132` blob GET · `:145`/`:164`/`:205` the 24 h public cache · `:162-164` thumbnail | `:132 @router.get("/{imageSlug}/blob")` · `:145`/`:164` `headers={"Cache-Control": "public, max-age=86400"},` · `:205 "Cache-Control": "public, max-age=86400",` | HOLDS |
| `image_storage.py:105-128` the re-upload regeneration branch | `:105 existing = await db.images.find_one({"sha256": sha256})` · `:128 existing["thumbnail_uri"] = thumbnail_uri` | HOLDS |
| `image_storage.py:250-266` `extraction_cache_key` | `:248 def extraction_cache_key(image_sha256: str, settings: Any) -> str:` · `:250 payload = json.dumps(` · `:266 return hashlib.sha256(payload.encode()).hexdigest()` | HOLDS ⟨beside: the **def** opens at `:248`; the record's **D-1** minute (eleven keys, not ten) stands unchanged⟩ |
| `image_storage.py:285`/`:290`/`:318` the contour write | `:285 async def store_contour_asset(` · `:290 image_bounds: dict[str, Any] \| None = None,` · `:318 "preview_path": "",` | HOLDS |
| `contours.py:20-30` the editor POST | `:25 doc = await store_contour_asset(xs, ys, req.image_slug, source="editor")` | HOLDS — the call sits inside the cited span |
| `assets.py:85` (PP-DEADSEAM write 1) | **`api/models/assets.py:85`** — `preview_path: str = ""` | HOLDS ⟨beside: there is **no** `api/routers/assets.py`; ⟨cmd⟩ `find api -name 'assets.py'` → `api/models/assets.py`, one path⟩ |
| `responses.py:22` (PP-DEADSEAM write 3) | **`api/responses.py:22`** — `preview_path=asset.get("preview_path", ""),` | HOLDS ⟨beside: rooted at `api/`, not `api/schemas/`⟩ |
| `equations.py:100-106` `reconstructed_points` produced at real cost | `:100 y_recon = np.zeros_like(x_eval, dtype=complex)` — the compute is in the span | HOLDS ⟨beside: the **field** is emitted at `:114` (`"reconstructed_points": {"x": …, "y": …}`) and `:128`, and typed at `api/models/equations.py:33`. The three sites are the strike's real surface⟩ |
| `grep -rn "def order_contours" $F` | ⟨cmd⟩ `/usr/bin/grep -rn "def order_contours" . --include='*.py' \| wc -l` → **0** | HOLDS — **the TRIPWIRE's witness stands** |
| `.gitignore:53 scripts/*` | `scripts/*` | HOLDS |
| `moon.json` is live and tracked | ⟨cmd⟩ `git -C $F ls-files --error-unmatch web/src/assets/fourier-paths/moon.json` → the path | HOLDS |

**B. The fourier web tree — `web/src/` (13 files moved since `8bc7736`; none is an F.W6 anchor)**

| spec's coordinate | re-resolved at `3bac3d52` | state |
|---|---|---|
| `api.ts:138-140`/`:139` the supported-but-unsent key | `web/src/lib/api.ts:139 headers["Idempotency-Key"] = options.idempotencyKey;` | HOLDS |
| `api.ts:371-380` `createVisualization` sends none | `:372 export async function createVisualization(` | HOLDS |
| `api.ts:292-294` the unversioned thumbnail URL | `:293 return ``${BASE}/api/images/${imageSlug}/thumbnail``;` | HOLDS |
| `types.ts:256-262` `VisualizationPatch` | `web/src/lib/types.ts:256 export interface VisualizationPatch {` … `:262 }` | HOLDS |
| `gallery.ts:167` · `:223` the ETag-capture path | both `const etag = etags.get(slug) ?? (await api.getVisualization(slug)).etag;` | HOLDS |
| `gallery.ts:189-199` the hard-coded like | `:189 async function like(slug: string): …` · **`:195 const liked = true;`** | HOLDS — inside the cited span |
| `gallery.ts:207-208` `recordView`'s write-back | `:208 if (idx !== -1) entries.value[idx] = { …entries.value[idx], views: data.views };` | HOLDS |
| `workspace.ts:95-104` the `structuredClone` into every draft | `:95 const raw: WorkspaceDraft = structuredClone({` | HOLDS |
| `AdminAuditLog.vue:146` the unconditional slice | **`web/src/components/visualization/gallery/AdminAuditLog.vue:146`** — `{{ entry.ip_hash.slice(0, 10) }}` | HOLDS ⟨beside: the rooted path; a bare filename is not an address — the same class G-11 exists for⟩ |
| `GalleryCard.vue:39` the banked normaliser (§4 lock 9 WAVE-LOCK) | **`web/src/components/visualization/gallery/GalleryCard.vue:39`** — `const key = b.startsWith("fourier") ? "fourier" : b;` | HOLDS ⟨beside: the rooted path. The WAVE-LOCK's operand is live⟩ |
| `GalleryView.vue:128` *"can only add"* | **`web/src/components/visualization/GalleryView.vue:128`** — `result.liked ? s.add(hash) : s.delete(hash);` | **HOLDS ⟨beside⟩** — the anchor is exact and the gloss is a **reachability** claim, not a syntactic one: the delete arm **is present** and is unreachable only because `gallery.ts:195` hard-codes `const liked = true`. **Recorded so no seat reads the row as a syntax defect and "cures" the ternary** |

**C. The value tree — §2.10's four routed-out rows (explicitly NOT under the F.W0 lock; measured read-only)**

§2.10 states it: *"Measured read-only at the fold (value side is explicitly **not** under the F.W0 lock,
which re-grounds the fourier tree)."* Re-measured here at this seat's clock, base `$V`. **Zero bytes moved**
— ⟨cmd⟩ `git diff --stat -- api/src src` → **empty** · ⟨cmd⟩ `git status --porcelain -- api/src src | wc -l`
→ **0**.

| spec's coordinate | re-resolved 2026-09-17 | state |
|---|---|---|
| `service/versions.ts:46-58` the reference walk (V-α / F-α's cure model) | `:46 let rootHash = hash;` … `rootHash = parent.rootHash ?? parentRef;` · `depth = (parent.depth ?? 0) + 1;` | HOLDS ⟨beside: the live form is `(parent.depth ?? 0) + 1`; the spec's `parent.depth + 1` is that expression without its nullish guard⟩ |
| `service/versions.ts:38` · `:40-44` the early return | `:38 const hash = computeContentHash(name, colors);` · `:44 if (existing) return hash;` | HOLDS |
| `service/versions.ts:121-181` · `:137` · `:148` · `:172` (V-α) | `:121 export async function revertToVersion(` · `:137 const newHash = computeContentHash(version.name, version.colors);` · `:148 await createVersionRecord(` · `:172 $inc: { versionCount: 1 },` | HOLDS |
| `service/forks.ts:64` · `:76` (R8's value half) | `:64 const colors = source.colors;` · `:76 visibility: "public",` | HOLDS |
| `service/forks.ts:167-179` the redaction counter-example (F-γ's adoption target) | `:167 * ordinal (V·W45 item 4). No raw document or lineage field (userSlug,` · `:171 export type ProvenanceStep =` · `:179 \| { kind: "unavailable"; ordinal: number };` | HOLDS |
| `model.ts:60` · `:85-86` (V-β / V-γ) | `:60 userSlug: string \| null;` · `:85 /** _id is the content-hash. */` · `:86 _id: string;` | HOLDS |
| `hash.ts:8-17` · `:41-42` (V-β / TA-4) | `:8 export function computeContentHash(name: string, colors: PaletteColor[]): string {` · `:42 atom-diff algebra that once also consumed it were excised at T.W1 — TA-4.)` | HOLDS |
| `repository/paletteVersion.ts:13-15` · `:44-47` (V-β) | `:13 findByHash(hash: string, session?: ClientSession): …` · `:47 if (existing) return version._id;` | HOLDS |
| `format.ts:45` · `:87` (TA-4's surviving wire field) | `:45 atomSetHash: string;` · `:87 atomSetHash: computeAtomSetHash(rest.colors),` | HOLDS |
| `service/crud.ts:119` · `:204` the conditional attribution (V-γ) | `:119 await createVersionRecord(` · `:204 if (contentChanged && userSlug) {` | HOLDS ⟨beside: the `if (userSlug)` guard fronting `:119` sits at **`:118`**; the spec cites the write, the guard is the line above it⟩ |
| `__tests__/palette-versions.test.ts:83` the suite's only revert case | `it("revertToVersion throws NotFoundError on missing palette", async () => {` | HOLDS |

**FW6-G19's second conjunct is discharged.** Every anchor this wave cites is re-resolved at the true bytes
against F.W0's receipt, by gate id, and **not one anchor is false**. Every correction is recorded **beside**
its spec spelling under E-3 and **none is written over it**.

**The corrections, self-counted over the settled bytes of this file.** ▲ **SELF-COUNT LAW, discharged rather
than dodged: the probe matches its own receipt.** ⟨cmd⟩ `/usr/bin/grep -c '⟨beside' burn-register.md` →
**18**, double-run **18** — of which **one is the notation's own definition in the Method paragraph above**
and **one is this receipt's own command text, three lines up** — so **SIXTEEN table rows carry a
beside-note**. (A figure published as *"17"* here before the arithmetic was disclosed would have been a
count of a file including the line that took the count; it is corrected in place before the file settles,
which is the only moment such a correction is not an erratum.)

1. **SEVEN ROOTINGS** — a bare filename resolved to its true path, the line holding exactly:
   `idempotency.py` → `api/lib/crud/` · `janitor.py` → `api/services/` · `assets.py` → `api/models/` ·
   `responses.py` → `api/` · `AdminAuditLog.vue` · `GalleryCard.vue` · `GalleryView.vue` →
   `web/src/components/visualization/…`. **A bare filename is not an address** — the class G-11 exists for,
   met inside this wave's own citation set.
2. **ONE COLLISION** — §2.4 FR-AFP-9's *"restore route live at `:430`"*: the **only** coordinate that
   resolves to a live **different** statement under the file its own paragraph names (`admin.py:430` is the
   batch `update_many`; the restore route is `visualizations.py:430`). The claim is true at the corrected
   coordinate; the spelling is what would have misled a burn seat.
3. **ONE SPLIT SPELLING** — §2.10's V-β cell writes `visualization.py:235, :129` for **two** files
   (`api/models/visualization.py` and `api/routers/visualizations.py`). Both coordinates true.
4. **FIVE EXTENT DISCLOSURES** — `:138-141`'s unnamed `:139` · `:860-889` closing at EOF ·
   `admin.py:435-440` opening one line after its own `update_many` verb · `extraction_cache_key`'s def at
   `:248` · `equations.py:100-106`'s field emitted at `:114`/`:128`.
5. **TWO READING CORRECTIONS** — `service/versions.ts`'s live `(parent.depth ?? 0) + 1` against the spec's
   `parent.depth + 1`, and `service/crud.ts`'s `if (userSlug)` guard sitting at `:118` against a write cited
   at `:119`. ▲ A **third** reading correction rides row 1's `GalleryView.vue:128` and is counted there,
   not twice: *"can only add"* is a **reachability** claim, and the reachability's falsifier is
   `gallery.ts:195`, not the ternary at `:128`.

**7 ⊕ 1 ⊕ 1 ⊕ 5 ⊕ 2 = 16**, which is the measured row figure above.
**No anchor of this wave is cited by line into a live sibling spec** — the sibling citations at §0.3 below
are by clause id and gate id, which is the form §4's F.W0 edge makes law.

### §0.2 FW6-G18's roster — R1–R9 ⊕ OG-F1 / OG-F2, RULED at COHESION §0j.D

**FW6-G18's GREEN (spec §3)**: *"Each conditioned row either carries its ruling or is explicitly deferred
with its honest default recorded. **F.W6 may not burn a row conditioned on an unruled question — the whole
FR-AFP band rides R3/G11**"*; owner cell: *"owner → F.W5 unit e; **F.W6 consumes, never re-rules**."*

**The rulings exist.** ⟨cmd⟩ `ls -l …/contract/OWNER-RULINGS-F.W5.md` → **18,702 B, Sep 17 18:16** · ⟨cmd⟩
`/usr/bin/grep -c '^| \*\*R[1-9]\*\*' OWNER-RULINGS-F.W5.md` → **9** (double-run: 9). **The authority of
record for what each ruling SAYS is `COHESION.md` §0j.D**, and every quotation below is from it. **This seat
rules nothing, re-opens nothing, and pre-answers nothing.**

| ruling | id at §0j.D | the ruling, quoted | the F.W6 rows it conditions — **and its homing** |
|---|---|---|---|
| **R1** / TA-4 | **F-SS4REST R1** | *"**R1 (TA-4)**: **RE-SCOPE value.js out of the diff clause** (a one-sided §6 verdict, stated explicitly) — `atomdiff.ts` is wholly excised from value.js and restoring it is value-side authoring that would couple value.js's release train to the fourier contract"* | §2.10's **⊙ TA-4** row, read by **FW6-G16** at unit `i`. **Homing unchanged (routed OUT).** The row is entered as **RULED RE-SCOPE**, never as a restoration ask, and the spec's *"UNFALSIFIABLE until G4 closes"* consequence is **discharged by the ruling** and stated as discharged |
| **R2** / trie | **F-TRIE (R2 ≡ E16 ≡ G7 ≡ G-F7-1)** | *"**NO TRIE**; whole-snapshot duplication is the recorded shipped behaviour (the honest default; `atomdiff.py:12-14` is the guardrail; zero material on either tree)"* | unit **`h`** (PP-DEADSEAM ⊕ `fr-EquationModeToggle FR-EMT-20` cited). **§4 lock 8's hold is DISCHARGED** — the `trace` / `reconstructed_points` strikes no longer wait on a compression decision. ▲ **No trie is designed here**: §5's exclusion of F.W7's compression design **stands**, and `atomdiff.py:12-14` remains the incumbent guardrail |
| **R3** / producer-or-retire | **F-PRODRET (R3 ≡ D3 ≡ G11)** | *"**PRODUCER**, as a **port** of value.js's shipped verb (`POST /:slug/flag`) under the SS-4 contract, homed at **F.W8** (the CRUD union prototype) with F.W5 writing the clause; FR-AFP grades re-derive at the populated surface"* | **THE ADMISSION GATE for the whole FR-AFP band (§4 lock 4).** The band is **ADMITTED, not retired** → unit **`e`** opens. ▲ **RE-HOMED, and said here because a seat reading only §2.4 would ship it in this wave**: §2.4's branch *"If PRODUCER: F.W6 ships the flag-write operation"* is **SUPERSEDED ON THE HOMING** — **the port is F.W8's act**. F.W6 records FR-AFP-1 as **ADMITTED-and-ROUTED-to-F.W8**, naming value.js's `POST /:slug/flag` as the reference implementation for that wave, and **ships no flag-write operation**. The band's other server rows (FR-AFP-66 · -7 · -33 · -36/D17 · FR-GV-9) remain F.W6's as written |
| **R4** / the like verb | **F-SS4REST R4** | *"**R4**: **REMOVE the affordance** (no dead affordance under `aria-pressed`)"* | §2.9's **`fr-GFC-3` ⊙** row → unit **`d`**. ▲ **RE-HOMED**: by §2.9's **own** branch — *"If DELETE: F.W4's."* — **F.W6 books NO server act**. The row is entered as **ruled-and-routed**; the `aria-pressed` + re-click guard is F.W3/W4's regardless |
| **R5** / off-state `[]` | **F-SS4REST R5** | *"**R5**: **STOP MINTING** the off-state `[]`; the contract does not admit it (no silent rewrite)"* | **load-bearing at FW6-G18** (the gate names R5 and R9 as *"the two dropped"*). It gates `fr-BasisSelector M-9` (record-qualified; §2.11 row 7) — a **cited** row, not a booked one. **No F.W6 booking**; carried here so the gate's roster is whole |
| **R6** / hard-delete arm | **F-SS4REST R6** | *"**R6**: **KEEP** the hard-delete arm; copy made truthful about irreversibility"* | §2.4's **FR-AFP-66** → unit **`e`**. The cascade is burned **as a build, not as a deletion** (the RETIRE branch of §2.4's sequencing cell — *"if RETIRE, the cascade is deleted with the band rather than built"* — does not fire). The truthful-copy arm is **F.W3/W4's** and is emitted, not claimed |
| **R7** / codegen | **F-SS4REST R7** | *"**R7**: **CODEGEN** — twins derived from one source (inv-16/inv-26 restated; structural invariants over prose)"* | §2.4's **FR-AFP-36 ⊙ + the D17 serializer clause** → unit **`e`**, gate **FW6-G12**. The ⊙ is **LIFTED**: one generated/checked shape is now admissible, and FW6-G12's *"⊙ No codegen without FR-AFP-71's ruling"* is satisfied by this ruling. ▲ **K9 survives the lift: the five `?? item.slug` fallbacks STAY** |
| **R8** / born visibility | **F-SS4REST R8** | *"**R8**: **REMIX + BORN-PRIVATE** (value.js's shipped verb and its explicit publish act), the F.W5 seat verifying non-contradiction with ruling D9 at the record before authoring"* | §2.8's **R-5 / G5 ⊕ E4** row → unit **`d`**. **The fourier half already conforms** — re-resolved above at `models/visualization.py:277` (`visibility: Visibility = "draft"`). What F.W6 burns is the **"no `fork_of` recorded"** server arm plus E4's create-visibility test. ▲ The **value half** (`forks.ts:76` `visibility: "public"`) routes **OUT** at unit `i`; **GCM-1's `/v/` routing cure is F.W4's** |
| **R9** / dead session subsystem | **F-SS4REST R9** | *"**R9**: **DELETE** the dead session subsystem (zero external call sites)"* | **load-bearing at FW6-G18.** ▲ **RE-HOMED — and the gate says so in its own words**: *"R9's ⊙ stays ATTACHED to FR-USB-23 on its F.W8 routing row (§5)."* The ⊙ travels **with** `FR-USB-23` to **F.W8**; **F.W6 neither wires nor deletes it** and books no row |
| **OG-F1** | §0j.D | quoted whole at **§0.1a** above | **G-11 narrows to drift-correction** (→ §0.1a/§0.1c: this wave performs its own re-resolution and says so); **GAB-13 discharges to a disclosure line** (→ §0.1a). Consumed by unit `a`; **re-derived by nobody** |
| **OG-F2 / OG-V2** | §0j.D | quoted whole at **§0.1a** above | **CODEX-ERA-SPECIFIC**: the pre-write root-absence receipt died with M-15's abrogation; the **finding** stays adopted. No F.W6 row rests on the retired requirement |
| **G-15(c)** / FM-19 | §0j.D **G-15** | *"(c) FM-19 → **FROZEN-FOREVER with a golden-file diff** (no regeneration pipeline exists; building one is unsized work)"* | unit **`g`**, gate **FW6-G15** — the **canonical-geometry TRIPWIRE**, which *"stays RED by design"*. The frozen-asset instrument is ruled; **DO-NOT-REGENERATE on `master` is untouched by it**, and an attempt remains a gate FAILURE reviving `fr-FourierShapeExtractor L-B1` + L-B2/`fr-ContourEditorCanvas C-2` at BLOCKER |

**The roster is whole: nine rulings ⊕ OG-F1 / OG-F2 ⊕ G-15(c), each quoted with its ruling id, each mapped
to the rows it conditions.** Three rulings **re-home** a row away from this wave (**R3** → the port at F.W8 ·
**R4** → the affordance at F.W4 · **R9** → FR-USB-23's ⊙ at F.W8) and each re-homing is stated **in its own
row**, because a burn seat reading only §2 would ship it here. **No ruling is re-ruled, none is
pre-answered, and no honest default is substituted for a ruling that exists.**

### §0.3 The F.W5 clause set, consumed by clause id — F.W6 authors no clause

The set exists whole (spec §1 r6, *cited-when-created*) — ⟨cmd⟩ `ls -l …/contract/` → `J-diff-shape-v2.md`
**244,274 B** · `operation-register.md` **38,255 B** · `OWNER-RULINGS-F.W5.md` **18,702 B**.

**The v2 clause register, enumerated by id** — ⟨cmd⟩ `/usr/bin/grep -ohE '^### [A-G][0-9]+c? ' J-diff-shape-v2.md`
(double-run, identical):

`A1 A2 A3 A4 A5 A6` · `B1 B2 B3 B4 B5` · `C1 C2 C3 C4 C5` · `D1 D2 D3 D4 D5 D6 D7 D8 D10 D11 D12 D13 D14
D15 D16 D17` · `E1 E2 E3 E4 E5 E6 E7 E8 E9 E10 E11 E12 E13 E14 E15 E16 E17 E18 E19 E20` · `F1 F2 F3 F4 F5
F6 F7 F8 F9` · `G1c G2c G3c G4c G5c G6c G7c G8c G9c G10c` — ⟨cmd⟩
`/usr/bin/grep -cE '^### [A-G][0-9]+c? (—|⊙)' J-diff-shape-v2.md` → **71** (double-run: 71).

▲ **`D9` is absent from the register and that absence is the record's own** — ⟨cmd⟩
`/usr/bin/grep -cE '^### D9 ' J-diff-shape-v2.md` → **0**. F-W5 states it in its own hand (*"No clause is
numbered D9 — see the compatibility note."*); `D9` is a **value.js product ruling**, not a v2 clause, and
the actor clause is **E18**. Carried so no seat re-mints the invented clause the spec retired at R4-9.3.

**The register and the ruling block**: ⟨cmd⟩ `/usr/bin/grep -c '^| ' operation-register.md` → **114** rows
(the 45 operation rows plus headers and legend) · ⟨cmd⟩ `/usr/bin/grep -c '^| \*\*R[1-9]\*\*'
OWNER-RULINGS-F.W5.md` → **9**.

**The clauses this wave BURNS** (green owner F.W6 / the fourier API row, per §4's F.W5 cross-edge), each
verified present in v2 by ⟨cmd⟩ `/usr/bin/grep -cE "^### <id> (—|⊙)"` → **1** at every id:

`C1` · `C2` · `C5` · `D5` · `D6` · `D8` · `D10` · `D11` · `D12` · `D16` · `D17` · `E2` · `E5` · `E6` · `E7` ·
`E13` · `E14` · `E17` · `E18` · `B4`/`E10` · `G1c`–`G10c`, with `D2` / `D3` **conditionally** (both now
ruled — `D2` by **R4 = REMOVE**, `D3` by **R3 = PRODUCER**, §0.2).

**The clauses this wave CITES and does not own** (enumerated per identity at spec §2.11 / §2.11c / §2.11d):
`A3` · `A4` · `A5` · `A6` · `B1` · `B2` · `C3` · `C4` · `D7` · `E4` · `E8` · `E9` · `E11` · `E12` · `E15` ·
`E20` · `F1`–`F9` · `G3c` / `G4c` / `G8c` / `G9c`.

**The per-unit clause map**, so each burn seat reads its own stating clause and no other:

| unit | stating clauses it burns | the clause's own words, where the burn hinges on them |
|---|---|---|
| `b` | **E2** (F-α) · **E7** (F-β ⊕ SS-C-1 write leg) | E2 carries the split: *"F.W5 owns the CLAUSE; F.W6 owns the burn-down — do NOT double-book."* **E7 is stated but UNGATED across F.W5's G1–G22 (+ §G G1c–G10c)** — FW6-G2/G3/G4 exist for exactly that, and the omission is **not** read as absence of scope |
| `c` | **C5** (redaction parity, F-γ) · **C1**/**C2** (image remediation, FR-AFP-4) | C5 is **stated**, so the redact branch is available — **cited, not re-chosen.** C2 states the image contract as **ONE** unit: delete/quarantine op · blob **and** thumbnail gate · janitor predicate · versioned thumbnail URL |
| `d` | **E6** (FR-GV-12 ⊕ FR-GV-24 ⊕ VV-R2-A) · **E4** (born visibility + lineage) | E6 carries the repair-test lock: **no re-open-increment assertion**. E4 is R8's clause. `fr-GalleryDraftsSection B-2`'s server arm closes **STATICALLY** (K12) |
| `e` | **D4**/**D5** (flag identity + which identifier renders) · **D6** (lifecycle truth and cascade, R6) · **D17** (serializer) · **A4** ⊙ (one contract source, now R7 = CODEGEN) | ▲ **D6, not "E7"** and ▲ **D5, not "D12"** — the R-1e re-keys, carried so no seat re-mints the first pass's labels |
| `f` | **E18** (the actor is a FIELD) · **D16** (the shared action taxonomy) | E18's sentence is the commissioned act. ▲ **AA-10 is CITED to F.W4, never booked**; **AA-23 is a kill-only carry**, host row held at F.W5 |
| `g` | **E13** (cache identity ⊇ consumed fields) · **E14** (editor contour is first class) · **E17** (image bounds on write) · **§G G1c** (RE-AUTHOR, at F.W6, not before) with **G5c** as its **PRECONDITION** | ▲ **E13, not "E12"** · **E14/E17, not "E11"** (R-1e). G1c's act closes **only** by naming the true source or re-authoring under a repaired, tracked, runnable pipeline — **never by regenerating** |
| `h` | **B4**/**E10** (ONE disposition for produced-and-unconsumed) | E10 is **ONE** stated disposition, never three ad-hoc deletions. `FR-EMT-20`'s host is **F.W5**; this wave executes the mechanism and **cites the holder** |
| `i` | none — the closure unit authors no clause | it reads **FW6-G16**'s two authorities and **FW6-G17**'s enumeration |

▲ **E-3, stated for the record**: **F.W6 CONSUMES these clauses and authors none.** The three contract files
are read-only to this wave (spec §1's NOT-in-bounds row); a correction to any of them is F.W5's seat's, by
dated addendum, never this wave's edit.

### §0.4 The census pin — the sole operand, and the band's record-side figure

**Frozen and character-matched** — ⟨cmd⟩ `shasum -a 256 …/conformance/CENSUS-CANONICAL.md | cut -c1-12` →
**`f44362757458`**, the value the spec pins at §0.3's hash table. `CENSUS-CANONICAL.md` is **THE** census of
record (spec §0.1); **a gate citing any other census operand is DEFECTIVE on sight**, and the struck
**132 / 111 / 153** are inadmissible.

⟨cmd⟩ `/usr/bin/grep -nE '^### F\.W5(-W8)? — ' "$C"` → `5078:### F.W5 — **27 rows**` ·
`5092:### F.W5-W8 — **89 rows**` ⟨*the line numbers are the command's output; the census is cited by heading*⟩.

> **27 ⊕ 89 = 116 record-side rows in the band — and ZERO at F.W6.**

⟨cmd⟩ `/usr/bin/grep -o 'F\.W6., .F\.W8. and .F\.W10. are likewise named by \*\*no record\*\*' "$C"` →
*"F.W6`, `F.W8` and `F.W10` are likewise named by **no record**"* ⟨*the paste begins where the pattern
begins; the record's opening backtick is outside it, and a leading backtick added to make the quotation look
whole would be the prepend defect the spec cures at §2.11 row 21*⟩.

▲ **NO ROSTER INTEGER IS RE-DERIVED HERE.** **116 is the canonical's**, quoted, not computed by this wave;
**this wave's own population is folds, gate operands and commissioned acts**, enumerated by register at
§1 below and **summed into nothing**. Because the canonical homes **ZERO** record-side rows at F.W6, a
record-side escape is impossible by construction — **what FW6-G17's reverse direction tests at unit `i` is
fabrication**, not omission.

---

## §1 The register — one row per burned identity

**FW6-G17 greps the `id` column of this table.** Units `b`–`i` append; **`a` opens it and books nothing**
(this unit's product is §0, which is FW6-G18's and FW6-G19's operand, not a burned identity).

Every row carries, in order: the **banked id, record-qualified** (R-5) · the **gate** it turns · the
**commissioned act** (a fourier-side ask, never a value.js edit) · the **F.W5 clause** that states it · the
**fourier-side landing evidence**, which stays *pending* until the sub-session lands it under COMMISSION §2.

| id | gate | commissioned act | stating clause | fourier-side landing evidence |
|---|---|---|---|---|
| *(unit `a` books none — see §0)* | FW6-G18 · FW6-G19 | — | — | — |
| **F-α** — the version chain (spec §2.1; P1, census risk 5). ▲ **Disposition UNRULED — FLAGGED INLINE, never presumed** | **FW6-G1** | **WITNESS, re-measured at this seat's clock** (2026-09-17, fourier `3bac3d52`, working tree clean): `_write_root_version` has **3** source sites — `api/routers/visualizations.py:110` def · `:220` create · `:592` remix — and is **the only writer**; the constructor hard-codes the quadruple at `:138 parent_hash=None,` · `:140 root_hash=set_hash_value,` · `:141 depth=0,`; `visualization_versions` has **8** non-test sites (1 insert `:146` · 2 finds `:749`/`:874` · 1 `find_one` `:822` · 2 migration-script sites · 2 index creations at `api/services/database.py:132-133`). `GET /{slug}/versions` opens at `:860` and its own docstring concedes the shape — *"The viz's own version chain, ``depth``-ordered, bounded ≤50"* — over a collection **every row of which is born `depth=0`**, so the depth sort orders a permanent singleton and all real lineage rides cross-viz `fork_of`. ⟶ **COMMISSIONED ACT (fourier-side; never a value.js edit)**: execute v2 **E2 / F.W5 G3**'s ***deepen-or-retire*** on the depth/parent/root quadruple — **one of the two named branches, never a third**: **(i) DEEPEN** — walk the parent before the version write exactly as value.js already ships (`rootHash = parent.rootHash ?? parentRef; depth = (parent.depth ?? 0) + 1`, the reference walk at `api/src/modules/palette/service/versions.ts:46-58` ⟨b-note: rooted here; the spec spells it `service/versions.ts:46-58`, and the live form carries the nullish guard the spec's `parent.depth + 1` omits — both recorded by unit `a` at §0.1c and not re-derived⟩); **(ii) RETIRE** — remove `depth` / `parent_hash` / `root_hash` from the version model and the two `sort("depth", 1)` readers with them. **Landing falsifier is FW6-G1's own**: *"a fixture asserts either a depth-1 version exists with a non-null `parent_hash`, or the depth/parent/root quadruple is gone from the model"*. ▲ **THE CHOICE IS NOT THIS SEAT'S.** COHESION §0j.D rules R1–R9 ⊕ OG-F1 / OG-F2 ⊕ G-15 and **none of them reaches this quadruple** (§0.2's roster maps R1→`i`, R2→`h`, R3→`e`, R4→`d`, R6/R7→`e`, R8→`d`; R5/R9 are carried, not booked — **unit `b` is unconditioned**). **Dissent of record, carried whole and not resolved here** (spec §2.1: *"retire-vs-deepen is genuinely open"*): fourier's own board books the RETIRE branch — `docs/tranches/M/M.md` **§7**'s terminal-verdict fold row *"Phantom within-viz version chain (depth always 0) \| J \| **DELETE** (W2-transpose; net code decrease, inv-3) \| W10"* and the **M.W10** wave row's *"W2-transpose: DELETE the phantom within-viz version chain; keep the per-viz atom `/diff` + cross-viz `fork_of`"* — while the seam table frames value.js as the side that deepens. F.W6 **commissions the execution and names both branches; the disposition rides unit `i`'s letter as its first ask** | **E2** (`J-diff-shape-v2.md`), the split's own home — *"F.W5 owns the CLAUSE; F.W6 owns the burn-down — do NOT double-book."* Gate owner **F.W6**, named by F.W5 **G3**'s GREEN-owner cell (*"F.W5 owns the CLAUSE only — no double-booking"* — the two homes quoted separately, never fused). **No F.W1 / F.W3 / F.W4 / F.W5 / F.W8 credit is claimed**, both directions of FW6-G17 | **PENDING.** Zero fourier bytes at this seat — HEAD `3bac3d52` unmoved, `git -C $F status --porcelain \| wc -l` → **0** before and after. Lands under COMMISSION §2 through unit `i`'s courier letter `value-to-fourier-2026-09-17-provenance-burndown-F.W6.md`; **the ruling must precede the landing** (a seat that lands a branch before the disposition is ruled has resolved an open question by shipping) |
| **F-β** — the PATCH `set_hash` / version write, and atom 5's tri-state (spec §2.1; P1, chain + stale advertised hash) | **FW6-G2** · **FW6-G3** | **WITNESS, the PATCH handler read whole at this seat's clock** (`/usr/bin/sed -n '355,392p' api/routers/visualizations.py`): after `resolve_session`, the ownership check, `etag.require_if_match` and the visibility-transition guard, the body is **three statements** — `:381 updates = {k: v for k, v in body.model_dump(exclude_unset=True).items() if v is not None}` · `:382 updates["updated_at"] = datetime.now(UTC)` · `:383 await db.visualizations.update_one({"slug": slug}, {"$set": updates})` — **a bare `$set`: no `set_hash` recompute, no `content_hash` recompute, and no `_write_root_version` call** (that writer's three sites are def / create / remix only, enumerated in the F-α row above). `_head_set_hash` at `:480` returns `doc.get("set_hash") or atomdiff.set_hash(atomdiff.enumerate_atoms(doc))` — **the STORED value whenever non-empty** — so the moment atom 5 moves under PATCH the advertised hash stops describing the bag, and `/diff` + `/provenance` report the stale value. `palette_slug` is **atom 5-of-5** (`api/lib/crud/atomdiff.py:34`) **and** a PATCH field (`api/models/visualization.py:207`). ⚑ **Witness sharpened at the bytes, measured not inferred**: `VisualizationUpdate`'s own docstring (`:199-200`) states *"``slug`` is immutable and ``content_hash`` is recomputed server-side (so neither appears here)"* — **the recompute the handler does not perform**; the in-source claim is part of the defect surface and a cure that leaves it standing has cured half. FW6-G3's arm: the same `if v is not None` comprehension **discards a null**, so PATCH can never clear `palette_slug`, while the remix arm twenty lines away already tri-states it — `:522 if "palette_slug" in body.model_fields_set:` … `:528 child_atoms["palette_slug"] = body.palette_slug`, under the in-source comment *"tri-state (F-08): null CLEARS the binding (atom removed from the bag); a slug rebinds; an omitted field inherits the source's."* ⟶ **COMMISSIONED ACT**: on **every atom-touching PATCH** — (1) recompute `set_hash` from `enumerate_atoms(doc)` over the post-write document and persist it, (2) **write the version row** through the existing `_write_root_version` path (or its deepened successor, per F-α's ruled branch), and (3) make `palette_slug` tri-state **by LIFTING the remix arm's `model_fields_set` idiom at `:522-528`** — omit inherits · null clears · slug rebinds. ▲ **KISS lock, binding (spec §2.1): the idiom ships in the same router twenty lines away — LIFT it, do NOT invent one**; two spellings of one tri-state in one router is the defect running the other way. **Landing falsifiers, the gates' own**: FW6-G2 — *"A fixture PATCHes atom 5 and asserts (a) `set_hash == set_hash(enumerate_atoms(doc))` after the write and (b) a new version row exists"*; FW6-G3 — *"Three arms tested: omit inherits, null clears, slug rebinds — **using the remix idiom, not a new one**"*. ⚑ **SECOND dissent of record, carried not resolved, surfaced because a seat reading only §2.1 would never see it**: the same **M.W10** row books ***"W2-fix: `palette_slug` remix-only"*** — a branch under which atom 5 leaves the PATCH surface altogether, the tri-state is moot, and SS-C-1's set equality resolves **by subtraction rather than by addition**. This seat resolves nothing: it records the tension and routes it to the **same ruling seat** as F-α's disposition | **E7** (`J-diff-shape-v2.md`) — *PATCH atom coverage + `set_hash` recompute*. ▲ **E7 is STATED and UNGATED**: F.W5's register runs **G1–G22 plus §G's G1c–G10c** and **none of them gates E7** (spec §2.1's sequencing cell; the first pass's "E8" is the *AnimationSettings* clause and is booked nowhere here). **FW6-G2/G3/G4 exist for exactly that omission**, which F.W6 reads as a gate gap and **not** as absence of scope | **PENDING.** Zero fourier bytes at this seat. Lands with FW6-G4 in **ONE** PATCH-model change under COMMISSION §2 |
| **SS-C-1 (write leg)** — PATCH field set vs atom set (spec §2.1; MAJOR, demoted from challenge-C's BLOCKER at component altitude). ▲ **READ leg NOT re-booked** | **FW6-G4** | **WITNESS, set against set at this seat's clock, both counts double-run identical**: the **PATCH field set is 5** — `visibility` · `title` · `description` · `tags` · `palette_slug` (`api/models/visualization.py:203-207`, sealed by `model_config = ConfigDict(extra="forbid")` at `:209`) — and its wire twin `VisualizationPatch` (`web/src/lib/types.ts:256-262`) carries **the same five**, all optional. The **atom set is 5** — `active_bases` · `n_harmonics` · `contour_settings` · `animation_settings` · `palette_slug` (`api/lib/crud/atomdiff.py:29-35` `ATOM_KEY_ORDER`, restated as the `AtomKey` Literal at `:37-39`). **Intersection = 1** (`palette_slug`) · **PATCH-only = 4** (the metadata quartet, no atom among them) · **atom-only = 4** — so `animation_settings` (atom 4-of-5; `class AnimationSettings` at `api/models/shared.py:65`, whose `speed` the control itself types at `web/src/lib/types.ts:49 speed: number;`) is settable **only at create and fork**, and a preference the control claims to set is silently non-persisted. ⟶ **COMMISSIONED ACT**: make the PATCH field set **equal** the atom set — extend `VisualizationUpdate` ⊕ `VisualizationPatch` with the four missing atoms and route them through the very recompute + version write F-β commissions — **or** state **each divergence per-atom with its reason** on the record. The gate admits either; **it does not admit silence**, and a per-atom statement is a disposition, not a deferral. ▲ **ONE-CUT LAW (spec §4 lock 3), binding: F-β ⊕ SS-C-1 land in ONE PATCH-model change — split them and the second reverts the first's invariant** (a widened field set without the recompute mints four more stale-hash paths; a recompute over a field set that reaches one atom leaves the other four unreachable). FW6-G4's own owner cell says it: **same commit as FW6-G2/G3, one PATCH-model cut**. ▲ **SPLIT RULING (R2-LC) OBSERVED**: the **READ leg** — `loadVisualization`, zero callers — **stays banked at `fr-GalleryCardModal` GCM-1 at blocker weight → F.W4 + the F.W5-W8 rider** and is **NOT re-booked here**; re-merging the axes re-books GCM-1. Only the write leg is un-banked, and only the write leg is in this row | **E7** (shared with F-β — **one clause, two legs**; F.W6 books the **write** leg only). The read leg's stating home travels with GCM-1 to F.W4 and is **cited, never claimed** (the FR-GIG-5 standing bar) | **PENDING.** Zero fourier bytes at this seat. **Must land in the same commit as F-β** — a landing that splits them fails the one-cut law and this row's evidence cell may not be stamped from a split landing |
| **F-γ** — breadcrumb redaction parity (spec §2.2; P1, privacy — *"census calls opposite defaults on privacy-adjacent surfaces contract-BLOCKING"*). ▲ **ONE PRIVACY LIMB with `FR-AFP-4` below — ONE commit** | **FW6-G5** | **WITNESS, the provenance handler read WHOLE at this seat's clock** (2026-09-17, fourier `3bac3d52`, working tree clean; ⟨cmd⟩ `/usr/bin/sed -n '733,791p' api/routers/visualizations.py`): the **entry row IS gated** — `:744 doc = await _readable_or_none(db, slug, viewer)` — and the cross-viz `fork_of` walk is **not**: `:764 breadcrumb: list[ForkCrumb] = []` · `:765 seen: set[str] = set()` · `:767 while cur is not None and len(breadcrumb) < 50:` · `:770 break  # cycle-guard` · `:781 parent_slug = cur.get("fork_of")` · **`:784 cur = await db.visualizations.find_one({"slug": parent_slug})`** — **a bare `find_one`: no viewer, no `visibility` predicate, no `deleted_at` predicate**. `_readable_or_none` has **5** source sites (⟨cmd⟩ `/usr/bin/grep -rn '_readable_or_none' $F/api --include='*.py' \| wc -l` → **5**, double-run 5) — `:466` def · `:507` remix · `:744` provenance · `:816` diff · `:869` versions — **and not one of them is inside the walk**. ⚑ **Witness sharpened at the bytes, measured not inferred — the emitted crumb is FIVE fields, not the four the spec names**: `class ForkCrumb` at **`api/models/visualization.py:326`** declares `slug · set_hash · author_slug · fork_of · created_at` (⟨cmd⟩ `/usr/bin/sed -n '331,335p' api/models/visualization.py \| /usr/bin/grep -c ':'` → **5**, double-run 5), and the fifth — **`fork_of` — IS the next ancestor's slug**, so a *child* crumb hands out its private parent's identifier even where the parent's own crumb is redacted. ⚑ **And the cure's own predicate turns on a second measurement**: `_readable_or_none`'s body (`:466-474`) returns `None` on `deleted_at is not None` and on `visibility == "draft" and not is_owner` — **it ADMITS `unlisted`** (`:34 Visibility = Literal["draft", "unlisted", "public"]`), while value.js's adoption target is strictly public — `isActivePublic` at `api/src/modules/palette/service/visibility.ts:31-38` is `visibility === "public" && (deletedAt === null \|\| deletedAt === undefined)`, and `service/forks.ts:163-169` states the guarantee in-source: *"A public, live ancestor is a minimal, release-scoped `palette` step; a private / unlisted / trashed / purged ancestor collapses to a non-correlatable `unavailable` step carrying ONLY its ordinal (V·W45 item 4). No raw document or lineage field (userSlug, contentHash, createdAt, parent slug) ever crosses the wire for a non-public hop."* ⟶ **COMMISSIONED ACT (fourier-side; never a value.js edit)**: gate **EVERY hop** of the `fork_of` walk and collapse each non-public ancestor to value.js's shipped discriminated-union member **`{kind: "unavailable", ordinal}`** — **ADOPTION, NOT DESIGN**: the counter-example is already in tree at `api/src/modules/palette/service/forks.ts:171-179` (the declared member, `:179 \| { kind: "unavailable"; ordinal: number };`) and `:197 chain.push({ kind: "unavailable", ordinal });` under its `if (!doc)` / `else` arms, and **v2 C5 now STATES the parity, so this seat CITES the clause rather than re-choosing the branch**. Three integrity bounds travel with the act: **(1) ▲ the cycle-guard (`seen` ⊕ `:770 break`) and the ≤50 cap (`:767 len(breadcrumb) < 50`) MUST SURVIVE** (spec §2.2's lock) — and adoption *preserves* them rather than costing them, because value.js's walk ships both (`const visited = new Set<string>()` ⊕ `while (currentSlug && chain.length < 50)`); **(2) `fork_of` leaves the crumb wherever the hop it points at is redacted** — C5's redacted member carries *"ORDINAL AND NOTHING ELSE"*, and a public crumb still pointing at a private parent re-opens the identical leak from the other end; **(3) ⚑ the predicate is the one precision this seat will NOT resolve silently**: §2.2's cure cell names *"apply `_readable_or_none` per hop"* **and** *"collapse non-public ancestors"*, and **at the bytes those are two predicates differing at exactly one value — `unlisted`**. The adoption target is value.js's, whose in-source guarantee enumerates *unlisted* among the collapsing states; the act therefore names **`isActivePublic`-equivalence** and **flags the divergence INLINE** rather than letting a landing seat take the looser reading by default. **Landing falsifier is FW6-G5's own**: *"A fixture with a private ancestor asserts **no lineage field crosses the wire** for that hop (the `{kind:"unavailable", ordinal}` shape) — **or** G6's second branch (stated asymmetry with its reason) is on the record. The cycle-guard and the ≤50 cap survive."* ▲ **G6's SECOND BRANCH IS NOT TAKEN AND IS NOW SPENT**: C5 rules *"If the two ends do not adopt one shape, v2 states the asymmetry and its reason — silence is not a posture"* — **v2 adopted the shape**, so the redact branch is the stated one and the asymmetry branch has no operand left | **C5** — *Redaction parity* (`J-diff-shape-v2.md`), whose RULE is the act in one sentence: *"**A provenance breadcrumb emits, for every hop the viewer may not read, a redacted placeholder carrying ORDINAL and NOTHING ELSE.** The redacted form is a **discriminated union member** … `{ kind: "unavailable", ordinal }`. Redaction applies to **every** hop of a chain, never to the entry row alone."* Its DISPOSITION homes the act away from this tree: *"Booked: **`F-γ` / `R-6`** (lane-crud). The act is the **fourier API row**'s."* ▲ **And C5's own LOCK is the one-limb law in v2's hand, quoted because it is why this row and the next are ONE commit**: *"Today's witness is **a private ancestor's slug on the wire** — the same *private artifact readable by slug* family as **GCM-52** at the image seam (§C1/§C2). The two are one mechanism at two surfaces: **an identifier that is also a capability**. A cure at either surface that leaves the other is not parity."* Gate owner **F.W6** (FW6-G5's cell: *"F.W6 (F.W5 G6 GREEN owner: fourier API row)"*); **no F.W1 / F.W3 / F.W4 / F.W5 / F.W8 credit is claimed**, both directions of FW6-G17 | **PENDING.** Zero fourier bytes at this seat — HEAD `3bac3d52` unmoved, ⟨cmd⟩ `git -C $F status --porcelain \| wc -l` → **0** before and after. Lands under COMMISSION §2 through unit `i`'s courier letter, **in the SAME cut as `FR-AFP-4`**: spec §2.2's ▲ — *"a breadcrumb redaction that leaves a draft/unlisted viz's source image world-readable by slug **has not cured the leak**"* — so this row's evidence cell may not be stamped from a landing that redacts the breadcrumb and leaves the image leaf open |
| **FR-AFP-4** ‡ (⊕ **fr-GalleryDraftsSection F-4** ⊕ **fr-GalleryDraftsSection m-15** ⊕ **fr-GalleryCardModal GCM-52**; **fr-ImageUpload row 26** cited) — the image leaf (spec §2.2; BLOCKER-weight, one of the panel's four survivors · m-15 MINOR · row 26 INFO). ▲ **SAME PRIVACY LIMB as F-γ — ONE commit.** ⟨*c-note: the spec's id cell folds all four tokens; **v2 partitions them** — C2's DISPOSITION books **`FR-AFP-4`** ⊕ **`m-15`** and CITES **`GCM-52`** ⊕ **`fr-GalleryDraftsSection F-4`** as *"booked at §C1"*, with *"ImageUpload row 26's security half … CITED, its transport half living at §E15"*. All four tokens stay in this id cell so FW6-G17's reverse direction finds them; the partition is recorded **beside**, never over*⟩ | **FW6-G11** | **WITNESS, every limb measured separately at this seat's clock, each figure double-run identical.** **(i) There is no asset-level delete.** ⟨cmd⟩ `/usr/bin/grep -cE '^@[a-z_]*router\.(get\|post\|put\|patch\|delete)' api/routers/images.py` → **7** ops (`POST ""` `:93` · `GET /by-hash/{sha256}` `:117` · `GET /{imageSlug}` `:126` · `GET /{imageSlug}/blob` `:132` · `GET /{imageSlug}/thumbnail` `:149` · `GET /{imageSlug}/overlay` `:168` · `POST /{imageSlug}/extract-contour` `:212`); ⟨cmd⟩ `/usr/bin/grep -cE '^@[a-z_]*router\.delete' api/routers/images.py` → **0**. **(ii) The fetch surface is unauthenticated and publicly cached.** ⚑ **Sharpened past the banked row**: ⟨cmd⟩ `/usr/bin/grep -c 'Depends' api/routers/images.py` → **0** — **not one `Depends` anywhere in the router**, so F-4's *"one router, one identity"* is measured at the router, not inferred from two handlers. ⟨cmd⟩ `/usr/bin/grep -c 'Cache-Control' api/routers/images.py` → **4** = **three header sites** (`:145` blob · `:164` thumbnail · `:205` overlay, each `"public, max-age=86400"`) ⊕ **one comment** (`:138`). **(iii) The janitor's recency predicate is bumped by the very fetches that make the content a problem — and the surface is WIDER than the banked row states.** ⟨cmd⟩ `/usr/bin/grep -rn 'touch_document' api/ --include='*.py' \| wc -l` → **6** sites; the reward rides `get_image_asset` (`api/dependencies.py:50`, whose `:75 await touch_document("images", {"image_slug": image_slug})` is unconditional), and ⟨cmd⟩ `/usr/bin/grep -c 'asset = await get_image_asset' api/routers/images.py` → **4** call sites — **blob `:140` · thumbnail `:151` · overlay `:177` · extract-contour `:214`**, so the bump is *every anonymous asset-bytes read*, not the blob GET alone. The predicate it feeds: ⟨cmd⟩ `api/services/database.py:163-165` `touch_document` `$set`s **`last_accessed_at`** to now, and `api/services/janitor.py:168` reaps on **`{"pinned": False, "last_accessed_at": {"$lt": cutoff}}`**. ⚑ **The exactness that makes the defect sharp rather than loose**: `_recompute_pin_flags` (`:291`) **does** unpin a soft-deleted viz's blob — `:351 {"$match": {"deleted_at": None}}`, and its own docstring says so (*"A soft-deleted visualization no longer pins its blobs, so the recency prune … can reap them"*) — **so the soft-delete arm works and the RECENCY arm is what the fetch defeats.** **(iv) The thumbnail URL is unversioned.** `web/src/lib/api.ts:293 return \`${BASE}/api/images/${imageSlug}/thumbnail\`;` — no hash, no version, no bust — while the deliberate re-upload regeneration branch (`api/services/image_storage.py:105-128`) rewrites the **same** path: `:121 slug = existing_asset.image_slug` · `:122-123 (_blob_dir() / f"{slug}.thumb").write_bytes(...)`, `thumbnail_uri = f"fs:{slug}.thumb"`. **Regenerated bytes, identical URL, 24 h public cache — no client observes the remediation for a day** (m-15, at its own bytes). **(v) GCM-52's entropy, re-derived at the DATA rather than quoted.** Image slugs are minted by the **same** generator as viz slugs — `image_storage.py:204 await slug_with_retry(_insert)` → `api/lib/crud/slugs.py:62 candidate = generate_slug()` → `:40-42`, `return "-".join(secrets.choice(_WORD_LISTS[k]) for k in _WORD_KEYS)` over `_WORD_KEYS = ("adjective", "verb", "color", "animal")` — and the four lists measure **128 · 128 · 128 · 128** at `api/lib/crud/slug_words.json`, i.e. **128⁴ = 2²⁸ exactly**, the arithmetic F-4 banks. ⚑ **And the guard is looser than the data**: `_load_word_lists` rejects only `len(words) < 64` (`slugs.py:29`), so **2²⁸ is a property of today's JSON, not an invariant** — at the floor the figure is 2²⁴. ⚑ **The in-source claim, part of the defect surface** (same class as unit `b`'s docstring find): the blob handler's own comment at `:136-138` reads *"Route, **auth** (the ``get_image_asset`` 404 / 410 + ``last_accessed_at`` touch), and ``Cache-Control`` are unchanged."* — **`get_image_asset` performs NO viewer check**; it is a 404/410 typing shim that *bumps the reap clock*. The file states an auth posture it does not have, and a cure that gates the route and leaves that sentence has cured half. ⟶ **COMMISSIONED ACT**: execute v2 **C2**'s image-remediation contract **AS ONE UNIT** — **(i)** a delete/quarantine verb over the **asset**, not only over the entity that references it; **(ii)** a visibility gate on the blob **AND** the thumbnail (⚑ **and the overlay**, which shares the identity by F-4's *one router, one identity* and carries the same `:205` header — named here so a two-handler cure does not leave the third open), decided **together with the cache directive** per C1's decision 2; **(iii)** the janitor-predicate fix at the convergence point (`get_image_asset`'s touch), so that **an anonymous fetch stops rewarding reported content**; **(iv)** a **versioned** thumbnail URL. C2's own rule forbids the partial landing: *"A contract that rules any one of the four alone has ruled none of them, because each of the other three independently restores the artifact."* ▲ **F-4 books WHO can fetch; m-15 books WHAT they see — CROSS-REFERENCED, NEVER MERGED** (the record says it in its own hand — ⟨cmd⟩ `/usr/bin/grep -n 'm-15' fr-GalleryDraftsSection.md` → `:80 … Distinct from F-4's auth/cache posture identity (that row books WHO can fetch; this books WHAT they see) — cross-referenced, not merged.`): **a cache-versioning cure discharges neither the auth cure nor vice-versa.** ▲ **C's scope discipline, and the hole is NOT overstated** — soft-delete **does** de-list from browse, measured: `api/routers/gallery.py:53 base_query = softdelete.with_not_deleted({"visibility": "public"})` and the detail read refuses a deleted row to a non-owner (`api/routers/visualizations.py:263 if doc.get("deleted_at") is not None and not is_owner:`). **The entity de-lists; the ASSET LEAF is what stays world-readable by slug.** ▲ **The deployment auth-proxy question stays UNPROVEN (F.W5 SS-13 defer #5) — this gate may not close by assuming a proxy in EITHER direction.** ▲ **K-1**: cite **45 / 30 / 13**, never 30 alone — the admin arm is inside the denominator, OpenAPI `security` is 0 of 45 while C1 measures **26 of 45 enforced**. ▲ **Homing, stated because a seat reading only §2.2 would ship it here**: **F-PRODRET (R3 ≡ D3 ≡ G11) ADMITS the band** — *"**PRODUCER**, as a **port** of value.js's shipped verb (`POST /:slug/flag`) … homed at **F.W8**"* — so this row **opens**; the **port is F.W8's act and `FR-AFP-1` is unit `e`'s**, and neither is claimed here. **Landing falsifier is FW6-G11's own**: *"A delete/quarantine op exists, blob **and** thumbnail GETs are visibility-gated, the janitor predicate no longer rewards fetching, and the thumbnail URL is versioned."* | **C2** — *Image remediation contract — ONE unit* (`J-diff-shape-v2.md`), the RULE quoted because it IS the act: *"**A moderation action that is advertised as taking content down TAKES THE CONTENT DOWN**, and the four mechanisms that together decide whether it does are **ruled as one unit**: (i) the delete or quarantine verb over the **asset**, not only over the entity that references it; (ii) the **blob visibility gate**; (iii) the **janitor's recency predicate**; and (iv) **thumbnail versioning**."* ⊕ **C1** — *Authority class per operation* — supplying the decision the gate limb executes: *"**The image GETs (rows 23 · 24 · 25) — `ANONYMOUS` is RETAINED for the blob and thumbnail of a PUBLISHED entity and is WITHDRAWN for the pre-publication surface.** … the **cache directive is part of the authority posture** … the contract therefore binds the two together — *the visibility gate and the cache directive are decided in one act*, and §C2 is where that act lands for the moderation case."* C2's LOCK carries the same honesty bar this row observes: *"**de-listing from browse is real and is not take-down**. The four limbs ship together or the unit is not closed; a repair that lands the delete verb and leaves the janitor predicate has moved the artifact from *reachable and listed* to *reachable and unlisted*, which is the state the panel already produces."* Gate owner **F.W6** (FW6-G11's cell: *"F.W6 (F.W5 G8 GREEN owner: fourier API row)"*); **no F.W1 / F.W3 / F.W4 / F.W5 / F.W8 credit is claimed** | **PENDING.** Zero fourier bytes at this seat. **Must land in the SAME commit as `F-γ`** — spec §4 lock 3: *"F-γ ⊕ FR-AFP-4 as one privacy limb (a redacted breadcrumb over a world-readable image is not a cure)"* — and C5's LOCK states the converse (*"A cure at either surface that leaves the other is not parity"*), so **this row's evidence cell may not be stamped from a split landing in either direction** |
| **FR-GV-12** (= **MISS-LC-1**) ⊕ **FR-GV-24** (= `L-19` / `C·m-9` — rides FR-GV-12) — counter provenance on an unsafe read (spec §2.3; **MAJOR, ×2 witnesses**). ⊙ **`fr-VisualizationView` VV-R2-A is CITED, NOT BOOKED** — E6's DISPOSITION holds it as a **LEG at F-W4**; its client cure is **F.W4's and is not credited here** | **FW6-G6** | **WITNESS, the read path taken in STATEMENT ORDER at this seat's clock** (2026-09-17, fourier `3bac3d52`, working tree clean — ⟨cmd⟩ `git -C $F rev-parse --short=8 HEAD` → **`3bac3d52`** · ⟨cmd⟩ `git -C $F status --porcelain \| wc -l` → **0**, both double-run; **unmoved from units `a`–`c`**, so §0.1c's anchor re-resolution holds at this clock). **(i) The ORDER is the defect and it is four statements wide.** ⟨cmd⟩ `/usr/bin/sed -n '250,276p' api/routers/visualizations.py`: `:256 doc = await db.visualizations.find_one({"slug": slug})` → the two 404 gates (`:263` soft-deleted-and-not-owner · `:265` draft-and-not-owner) → `:268-270 await db.visualizations.update_one({"slug": slug}, {"$inc": {"views": 1}, "$set": {"last_accessed_at": datetime.now(UTC)}})` → `:272 body = _public_doc(doc)`. **`doc` is the PRE-`$inc` document, so the operation cannot observe its own side effect.** All three of the spec's coordinates — `:256` · `:268-270` · `:272` — **reproduce byte-for-byte**. ⚑ **Sharpened at the bytes: the two gates above the `$inc` gate NOT-FOUND only** — nothing conditions the increment, so **an owner's own draft GET increments too**, on a row no third party can read. **(ii) The client write-back restores a value the list already held.** `web/src/stores/gallery.ts:201 async function recordView(slug: string)` → `:205 const { data, etag } = await api.getVisualization(slug);` → `:207 const idx = entries.value.findIndex(...)` / `:208 if (idx !== -1) entries.value[idx] = { ...entries.value[idx], views: data.views };` — **`:207-208` exactly as banked**; the counter can never show the user's own open. **(iii) The same verb is the ETag-capture path, and the chain is verified WHOLE at this seat's bytes, not inherited.** `gallery.ts:50 const etags = new Map<string, string>();`, read at `:167` (softDelete) and `:223` (publish) in the identical form `const etag = etags.get(slug) ?? (await api.getVisualization(slug)).etag;`. ⚑ **The ALWAYS-MISS limb, measured rather than assumed** — `VisualizationView.vue:106 async function handlePublish()` → `:110 const snapshot = await store.createSnapshot();` → `:112 await gallery.publish(snapshot.slug, store.imageSlug);`, while `createSnapshot` parks the fresh ETag in a **DIFFERENT STORE** (`web/src/stores/workspace.ts:359 visualizationETag.value = etag;`, the ref declared at `:41`), so `gallery.ts`'s own map **never receives it**, `:223` misses on every publish, the GET fires, and against `api/models/visualization.py:151 views: int = 0` **the entity is born at `views: 1` before any third party sees it** (VV-R2-A, chain verified whole). **(iv) Dedup substrate exists on the write side and not on the read side.** ⟨cmd⟩ `/usr/bin/grep -rn 'viewed_ips' api --include='*.py' \| wc -l` → **0** (double-run 0) against ⟨cmd⟩ `/usr/bin/grep -rn 'liked_ips' api --include='*.py' \| wc -l` → **7** (double-run 7). ⚑ **And all seven are EXCLUSIONS — zero writers**: three dict-comprehension drops (`visualizations.py:80` · `admin.py:87` · `gallery.py:34`, each `if k not in ("_id", "liked_ips")`) ⊕ four Mongo projections `{"liked_ips": 0}` (`visualizations.py:318` · `:705` · `admin.py:555` · `gallery.py:58`). **(v) The client guard is component-local.** ⟨cmd⟩ `/usr/bin/grep -rn 'viewedHashes' web/src` → **3** lines — `GalleryView.vue:46 const viewedHashes = ref(new Set<string>());` ⊕ `:118-119` inside `openModal` (`:117-122`). ⚑ **Coordinate recorded BESIDE, corrected over neither (E-3)**: `fr-GalleryView.md:63` addresses the guard as *"(:46, :117-121)"* — the **enclosing `openModal` block** — while v2 **E6** addresses it as `:46` ⊕ `:118-119` — **the two statements inside it**. Both are true at their own grain, the block contains the statements, and **this seat corrects neither record**; the act names the statements. ⟶ **COMMISSIONED ACT (fourier-side; never a value.js edit)**: execute v2 **E6** — **one of its TWO named branches, never a third**. **(i) SAFE READ + EXPLICIT VERB** — `GET /{slug}` stops writing and the increment moves to an explicit verb. ⚑ **This is ADOPTION, not design, and the idiom is already inside the same router** — ⟨cmd⟩ `/usr/bin/grep -nE '^@[a-z_]*router\.(get\|post\|put\|patch\|delete)' api/routers/visualizations.py` → **13** decorators, **four of which are already dedicated sub-resource POST verbs**: `:430 /{slug}/restore` · `:488 /{slug}/remix` · `:669 /{slug}/publish` · `:675 /{slug}/unpublish` — and **none of the thirteen is a view verb** (⟨cmd⟩ `/usr/bin/grep -rhoE '^@[a-z_]*router\.(get\|post\|put\|patch\|delete)\("[^"]*"' api/routers/ \| /usr/bin/grep -ci 'view'` → **0**, *enumerated over the route-path literals* per the S-8 method law rather than inferred from a name). **Unit `b`'s KISS lesson, same shape one router over: lift the idiom twenty lines away, do not invent one.** **OR (ii) STATED POLICY** — v2 states the mutating-GET policy explicitly **against RFC 9110 §9.2.1** and **names every operation it covers**; silence is not the second branch. **Two bounds travel with WHICHEVER branch lands.** **(a) The publish-path self-count is COLLATERAL, not the intended increment** — E6's own ▲: *"The store's own `recordView` comment documents GET-as-increment as the DELIBERATE mechanism, so the publish-path GET is an ACCIDENTAL self-count: v2 must not codify the accident. The two facts are easy to fuse and must not be: **one increment is intended, the other is collateral**."* A cure that reads the two as one has decided the distinction the clause exists to hold open. **(b) ⚑ This seat's own bar, recorded so the landing seat does not infer it from the row below**: `viewed_ips` would mirror `liked_ips`, and under **F-SS4REST R4** the like verb is **REMOVED** — so **a `viewed_ips` cure may not justify itself by symmetry with a surface being retired**; it stands on E6's rule or not at all. ▲ **THE CURE LOCK, carried as a bar on the LANDING TEST and quoted at v2's bytes — FR-GV-24**: *"**FR-GV-24 REPAIR-TEST LOCK — `viewedHashes` DOES work within a session; the defect is SCOPE, not absence — so REPAIR TESTS MUST NOT ASSERT A RE-OPEN INCREMENT.** A test that opens the same entity twice in one session and expects `views` to rise is asserting the **opposite** of the shipped intent and will fail on correct code."* **This seat writes no test and prescribes none**, and the prohibition travels to the landing seat verbatim. The FR-GV-24 **client** arm — `viewedHashes`'s component-local reset — is the composing client row and is **not claimed here**. ▲ **VV-R2-A's client arm is EMITTED, NEVER CLAIMED, and the emission is MEASURED rather than transcribed**: E6's DISPOSITION reads *"The **F.W4 arm**: publish adopts `store.setVisibility(\"public\")` (implemented, zero callers — §D14), dropping one round trip **and the phantom view**"* — ⟨cmd⟩ `/usr/bin/grep -rn 'setVisibility' web/src` → **2** hits, `stores/workspace.ts:373 async function setVisibility(visibility: "draft" \| "unlisted" \| "public")` (the declaration) ⊕ `:461` (the export) — **zero call sites**, so *"implemented, zero callers"* reproduces at this seat's clock. **F.W6 takes no credit for it** (FW6-G17, both directions). ▲ **Observable magnitude stays SS-13's**: VV-R2-A's probe 4 — *"publish a fresh visualization; read the gallery card's view count (derivation: born at 1)"* — is UNPROVEN-NEEDS-LIVE. **This seat spent NO probe**; the RED rests on source-certain statement order and a store boundary, never on a rendered figure. **Landing falsifier is FW6-G6's own**: *"The read is safe behind an explicit verb, or the mutating-GET policy is stated against RFC 9110 §9.2.1. ▲ **The repair test may NOT assert a re-open increment (FR-GV-24 lock)**"* | **E6** — *Unsafe GET and counter provenance* (`J-diff-shape-v2.md`), whose RULE is the act in three sentences: *"**A read is safe.** A counter is incremented by an **explicit verb**, not as a side effect of the representation request — **or** this contract states the mutating-GET policy explicitly, against **RFC 9110 §9.2.1**, and names every operation it covers. A response that reports a counter it has just changed **reports the wrong value by construction**."* Its DISPOSITION books the pair and homes the arms apart: *"Booked: **`FR-GV-12`** (=MISS-LC-1) ⊕ **`FR-GV-24`**; **`VV-R2-A`** rides as a **LEG held at F-W4**. … Home: the **fourier API row** ⊕ F.W4."* Gate owner, from FW6-G6's own cell: **"F.W6 server arm + F.W4 client arm (VV-R2-A) — *separately credited*"** — so the split is the gate's design, not this seat's convenience; **no F.W1 / F.W3 / F.W4 / F.W5 / F.W8 credit is claimed here** | **PENDING.** Zero fourier bytes at this seat — HEAD `3bac3d52` unmoved, ⟨cmd⟩ `git -C $F status --porcelain \| wc -l` → **0** before and after. Lands under COMMISSION §2 through unit `i`'s courier letter. ▲ **This evidence cell may NOT be stamped from a landing whose repair test asserts a re-open increment** — that landing fails the FR-GV-24 lock even if the counter itself is correct, and a gate closed over a test that contradicts the shipped intent is the born-RED-witness-is-false defect this wave's own §3 convicts |
| **`fr-GalleryDraftsSection` B-2** ‡ — server-side create dedupe (spec §2.3; **BLOCKER, banked**) — **record-qualified per R-5: the homonym `fr-EquationView B-2` is a DISTINCT banked identity, cited at spec §2.11 row 25** (⊕ **`fr-GalleryView` FR-GV-1** FOLDS at banked severity — **holder named: held at F-W3**; **`fr-CanvasControlsDock` D-4 / L-1 / C-3** the FULL banked head ⊕ **`fr-VisualizationView` BLK-2** ride as LEGS held at **F-W4**; **`fr-EditorControlsDock` C-7** rider; intake **X-3** consumed) | **FW6-G7** | **WITNESS, measured LIMB BY LIMB at this seat's clock** (fourier `3bac3d52`, clean), because E5's lock makes the mechanism-count load-bearing. **(i) The client sends no key, though the transport carries one.** `web/src/lib/api.ts:372-380 export async function createVisualization(body: VisualizationCreate): Promise<WithETag<Visualization>> { return apiFetchWithETag<Visualization>("/api/visualizations", "createVisualization", { method: "POST", body: { ...body } }); }` — **no `Idempotency-Key`** — against the transport's support at `:138-140 if (options?.idempotencyKey) { headers["Idempotency-Key"] = options.idempotencyKey; }`, declared and documented **twice** (`:98` and `:210`, both *"honoured by the API for POST + PUT"*). ⚑ **Coordinate recorded BESIDE (E-3)**: spec §2.3 addresses the function at `api.ts:371-380`; at the live bytes `:371` is the JSDoc line and the declaration opens at **`:372`**, closing at `:380`. **The witness is unchanged; the opening coordinate is one line later and is recorded, not corrected over.** **(ii) The envelope declines by design.** `api/lib/crud/idempotency.py:57 async def replay_or_record(...)`, whose own docstring states it at `:65` — *"No header → ``handler()``."* — executed at `:68 key = request.headers.get("Idempotency-Key")` / `:69-70 if not key: return await handler()`. **(iii) The index that would catch it is not unique.** ⟨cmd⟩ `/usr/bin/grep -n 'content_hash' api/services/database.py` → `:98 await _db.visualizations.create_index("content_hash")` — **PLAIN** — against the deliberate contrast two collections over, `:140 await _db.flags.create_index([("content_hash", 1), ("reporter_slug", 1)], unique=True)`. **The same digest is uniqueness-bearing for flags and not for entities.** **(iv) D-2, extended and re-measured — and it makes the RED SHARPER, not softer.** ⟨cmd⟩ `/usr/bin/grep -rn 'replay_or_record' api --include='*.py'` → **two non-test router sites**: `visualizations.py:236` (**CREATE**) and `:612` (remix). The spec names only the remix arm. **The create path ALREADY wraps its handler** — `:236 return await idempotency.replay_or_record(request, _store(), f"user:{owner_slug}", _handler)`, closing the `_handler` that mints the slug (`:225 slug = await slugs.slug_with_retry(_insert)`), inserts the document, and writes the root version (`:219-223`) — and the handler computes the very digest the index does not enforce (`:183 content_hash = _compute_content_hash(body)` → `:199 content_hash=content_hash,`). **So the envelope is not missing. The KEY and the INDEX are.** The ask is therefore small and exact rather than architectural, which is what unit `a`'s D-2 minute predicted and this seat confirms at the bytes. **(v) The third mechanism — the client gate — is invariantly true** (cited, not claimed: it is F.W3/W4's): ⟨cmd⟩ `/usr/bin/grep -rn 'savedSnapshots' web/src --include='*.ts' --include='*.vue'` → **4** hits — `stores/workspace.ts:102 savedSnapshots: [],` (**the only writer, unconditional**), `lib/types.ts:90` (the declaration) and the two readers at `GalleryView.vue:78-79` (`!d.savedSnapshots?.length \|\| !d.savedSnapshots.every((h) => publishedHashes.value.has(h))`) — reproducing E5's witness exactly. ⟶ **COMMISSIONED ACT (fourier-side; never a value.js edit)**: execute v2 **E5** — **server-side dedupe on the CREATE path**, by the adjudicated split, quoted by command (⟨cmd⟩ base `$G`, `/usr/bin/grep -o "props/reconciliation contract.\{0,105\}" fr-GalleryDraftsSection.md`) → *"props/reconciliation contract → **F.W3/W4**; server-side dedupe/idempotency for the create path → **F.W5-W8** (CRUD/provenance union)."* Two admissible mechanisms, **and/or** as the spec's cure cell spells it: **(a)** make the standing envelope EFFECTIVE — the key reaches the create call, both ends of the header path already existing (`api.ts:138-140` ⊕ `idempotency.py:68`); **(b)** make `content_hash` **UNIQUE** at `database.py:98`, adopting the shape `:140` already carries for flags. ▲ **E5's LOCK, binding on the landing and quoted whole**: *"Three independent mechanisms each suffice to mint the duplicate: no header, no unique index, no truthful client gate. **A cure that lands one of the three and reports the clause closed has measured the mechanism it chose, not the outcome.**"* — a landing that ships the key and leaves the index plain has **not** closed E5, and neither has the converse; **the third mechanism is F.W3/W4's and is emitted, never claimed**. ▲ **K12 PROBE-SUPPRESSION LOCK, observed and stated as SPENT**: *"both axes' UNPROVEN-NEEDS-LIVE hedges are DEAD — `fr-GalleryDraftsSection B-2` closes it STATICALLY (no-header idempotency passthrough ⊕ the plain index); SS-13 spends NO probe here."* **This seat ran no probe and needed none**: every limb above is a static read of the shipped bytes, and the two mechanisms K12 names are the two this row measured at `idempotency.py:69` and `database.py:98`. ▲ **The F.W5 SEQUENCING is carried, not silently dropped**: E5 binds the `fr-CanvasControlsDock` `(image_slug, contour_hash)` dedupe rider with *"**F.W5's own binding: the server arm SEQUENCES AFTER the join split (§B1)**"*, and **the `C-28` fold itself is homed at §B1 — cited here, booked there; one home, two citations.** ▲ **Riders named at the banked home and NOT re-booked**: `m-13` (the success toast prints the **USER** slug) and the **`publishedHashes` misnomer** — *"it holds freshly minted visualization slugs, which can never equal a draft's `imageSlug` — fix the name with the contract."* **Landing falsifier is FW6-G7's own**: *"The create path replays or the index is unique. ▲ **K12: closed STATICALLY — SS-13 spends no probe**"* | **E5** — *Create idempotency and dedupe — ONE clause, TWO entry points* ‡ (`J-diff-shape-v2.md`), whose RULE is the act in two sentences: *"**A create operation is idempotent under a caller-supplied key, and the server deduplicates on the identity that makes two creates \"the same\".** The client may not be the dedupe: a disabled button is a rendering, and a second press is a second entity."* Its DISPOSITION carries both the leg-holding and the split: *"**`fr-GalleryDraftsSection B-2`** rides as a **LEG held at F-W3**; **`fr-CanvasControlsDock D-4 / L-1 / C-3`** (the full banked head) rides as a **LEG held at F-W4**; **`FR-GV-1` FOLDS at banked severity** … **Split adopted**: props/reconciliation → **F.W3/W4**; **server dedupe → this contract**."* ⚑ **The holder-citation is carried because a seat reading only spec §2.3 would book the whole identity here**: v2's §Z addendum (2026-09-17, repair round 1) names `fr-GalleryView` **`FR-GV-1`** as **held at F-W3** (canonical home **F.W3**, legs `F.W5-W8` · `SS-13`) and rules *"the fold is into this clause's mechanism, never a re-booking"*. **F.W6 takes the SERVER arm ONLY**; **no F.W1 / F.W3 / F.W4 / F.W5 / F.W8 credit is claimed** (FW6-G17, both directions) | **PENDING.** Zero fourier bytes at this seat. Lands under COMMISSION §2 through unit `i`'s courier letter. ▲ **This evidence cell may not be stamped from a landing that closes ONE of E5's three mechanisms** — the clause's own LOCK forbids reading a single-mechanism cure as closure, and the client mechanism is another wave's, so the fourier-side landing must carry **both** the key and the index or state which it declines and why |
| **`R-5` / `G5`** born-visibility ⊕ **`E4`** lineage (spec §2.8; **P1**, census risk 6 — *"opposite defaults on a privacy-adjacent surface are contract-blocking"*) — ⊕ **`fr-GalleryCardModal` GCM-1** CITED (a LEG held at **F-W4**) ⊕ **`fr-GalleryCard` D-7 / C·I-2** (the FULL banked head) ⊕ **`X-2`**; **`fr-VisualizationView` BLK-1 FOLDS**. ⊙ **RULED — F-SS4REST R8: REMIX + BORN-PRIVATE**; this seat **consumes, never re-rules** | *(**no F.W6 gate of its own** — the wave's §3 opens no born-visibility gate; the GREEN condition is **F.W5's `G5`**, and this row's evidence is read by **FW6-G17**)* | **WITNESS, re-measured at this seat's clock** (fourier `3bac3d52`, clean). **(i) THE FOURIER HALF ALREADY CONFORMS — stated as conformance, not smoothed into a defect.** ⟨cmd⟩ `/usr/bin/sed -n '270,278p' api/models/visualization.py` → `:271 class VisualizationRemix(BaseModel):` … `:277 visibility: Visibility = "draft"  # a remix child is born private (§4)` — **the spec's `:277` anchor reproduces exactly** — and the create body carries the same default: `:177 class VisualizationCreate(BaseModel):` … `:183 visibility: Visibility = "draft"`. **Both entry points are born-private on this side, which is what R8 rules.** **(ii) The divergence is the VALUE half and it routes OUT.** `api/src/modules/palette/service/forks.ts:76 visibility: "public",` — hard-coded in the fork child's document. **Named here, burned nowhere in this wave**: it leaves at unit `i`'s routing receipt with **zero value-tree bytes moved**, per spec §2.10's routing law and COHESION §2's SS-4 row (⟨cmd⟩ base `$H`, `/usr/bin/grep -c "neither side's edits gate the other's" COHESION.md` → **1**). **(iii) The lineage arm — the "no `fork_of` recorded" SERVER arm — SHARPENED past the spec's spelling.** The remix route **does** record lineage (`api/routers/visualizations.py:578 fork_of=slug,` ⊕ `:579 fork_of_hash=source_set_hash,`) and the create route deliberately does not (`:214` comment *"A native row is born an original (fork_of=None, fork_count=0)"*, `:222 fork_of_hash=None`). ⚑ **The server-side mechanism, measured rather than characterised**: `VisualizationCreate` (`api/models/visualization.py:177-195`) carries **NO `fork_of` field at all** and closes the body at `:195 model_config = ConfigDict(extra="forbid")` — **so a client that WANTED to declare lineage at create cannot: the body is closed and the write would 422.** The entity model holds the slot (`:146 fork_of: str \| None = None`), and its **only** writer is the remix route. ⚑ **And that route has no client**: ⟨cmd⟩ `/usr/bin/grep -c 'remix' web/src/lib/api.ts` → **0** (double-run 0) — **not the word anywhere in the client's API surface** — reproducing E4's *"`CLIENTABLE` with zero client function"* at this seat's clock. **So the server arm of GCM-1's silence is not a missing `if`: it is a create contract with no lineage slot standing beside a derivation verb with no caller.** **(iv) GCM-1's client mechanism, measured so the EMISSION is honest and the homing visible** — `web/src/components/visualization/gallery/GalleryCardModal.vue:185 @click="emit('open-visualizer', entry.image_slug)"` hands out the **asset FK**, and `GalleryView.vue:396 @open-visualizer="(slug) => { selectedEntry = null; router.push(\`/w/${slug}\`); }"` routes the **workspace** path — so the editor opens with no entity identity and the subsequent save is a **create**. ▲ **GCM-1's routing cure — emit `entry.slug`, route `/v/`, point the loader at `loadVisualization` — is F.W4's and F.W6 may not claim it**: v2's §Z addendum holds GCM-1 as *"a **LEG — held at F-W4**"*, and E4's own ▲ rules *"F.W5's share of `GCM-1` is the **CONTRACT FACT** — *a save that should have been a remix carries no lineage* — **NOT the routing repair**, which is F.W4's."* **(v) THE TEST CONJUNCT IS THE OPEN ONE, proved absent BY ENUMERATION (S-8 method law), never by a bare grep.** E4's rule: *"**Each side's test asserts the child's visibility on create** — the rule is not adopted until both tests exist."* The fourier surface that could hold it is exactly two files — ⟨cmd⟩ `/usr/bin/grep -rln 'remix' api/tests --include='*.py'` → `api/tests/conformance/test_diff_shape.py` · `api/tests/conformance/test_remix.py` — and **neither is a member of the set that asserts a visibility**: ⟨cmd⟩ `/usr/bin/grep -l 'assert.*visibility' api/tests/conformance/test_remix.py api/tests/conformance/test_diff_shape.py` → **no output** (set-membership, not a count). Corpus-wide, ⟨cmd⟩ `/usr/bin/grep -rc 'child\["visibility"\]' api/tests --include='*.py'` returns **zero non-`:0` files** (double-run). Of the **12** remix call sites (⟨cmd⟩ `/usr/bin/grep -c 'await _remix(' api/tests/conformance/test_remix.py` → **12**, double-run 12), **8** pass `visibility=` explicitly (double-run 8) and the remaining **four** — `:80` · `:101` · `:111` · `:126` — assert, respectively, `image_slug`/`contour_hash`/`animation_data`, a **401**, a **422** `urn:contract:remix-noop`, and a **404**. **The `"draft"` default at `:277` — the exact byte R8 rules — is EXERCISED four times and ASSERTED nowhere.** ⚑ **And the create side's test exists at the WRONG ALTITUDE, stated honestly rather than counted as satisfied**: `api/tests/conformance/test_visibility.py:38-46 test_default_draft` — *"C4.7 — a create body omitting ``visibility`` defaults to ``draft``."* — asserts `VisualizationCreate.model_validate_json(...).visibility == "draft"`: a **MODEL** assertion with **no route, no persisted document, and no child**. **A model default is not a create-visibility test of the derived entity, which is what R8 and E4 name.** ⟶ **COMMISSIONED ACT (fourier-side; never a value.js edit)**: under **F-SS4REST R8 — REMIX + BORN-PRIVATE** the fourier half's **SHAPE conjunct is already met**, so what F.W6 commissions is the **EVIDENCE conjunct and the LINEAGE slot**: **(a)** a **remix-child born-visibility test** that omits `visibility` and asserts the **persisted child** is `draft` — the one assertion the enumerated surface does not carry, and the conjunct without which E4 says *"the rule is not adopted"*; **(b)** the **"no `fork_of` recorded"** server arm — either the create contract gains an explicit lineage slot (so `extra="forbid"` stops being the reason a derivation cannot declare itself), **or** the derivation verb is ruled the only path and the contract says so, so a save that should have been a remix cannot silently become an original. ▲ **R8's LOCK travels whole**: *"**Born-private without a publish verb is not privacy — it is an unreachable entity**, and born-public without an act is not convenience — it is publication without consent. R8 rules the pair; neither half ships alone."* — and this side's pair IS closable, because the publish verb is live (`api/routers/visualizations.py:669 @router.post("/{slug}/publish")` ⊕ `:675 @router.post("/{slug}/unpublish")`); what it lacks is a client, which is another wave's row. ▲ **D9 non-contradiction is v2's, VERIFIED THERE, and is neither re-run nor re-litigated by this seat**: E4 records the three tests and the verdict *"R8 is a narrowing INSIDE D9's domain"*, and the one live divergence — value.js persisting **three** visibility states — is v2's **disclosed** D9 reconciliation **owed by the value.js API row**, *"never a silent contract overwrite, and never a fourier defect."* ▲ **The verb-name reconciliation (remix vs fork) and the delta-required semantics are SETTLED BY R8 and v2's vocabulary act — not re-opened here**, and spec §2.8's *"NOT F.W6's to settle"* stands discharged by ruling rather than by deferral | **E4** — *Born visibility of the derived variant ‡ + the fork/remix vocabulary — RULED: REMIX + BORN-PRIVATE* (`J-diff-shape-v2.md`), whose RULE is the act in one paragraph: *"**RULE — RULED, COHESION §0j.D F-SS4REST R8: REMIX + BORN-PRIVATE.** **The derived-entity verb is `remix`**, and **a derived entity is BORN PRIVATE**, with an **explicit publish act** as the only path to visibility. Vocabulary is settled in the same act: **remix** (not *fork*) for the derivation verb; **version** (not *snapshot*) for the within-entity record. **Each side's test asserts the child's visibility on create** — the rule is not adopted until both tests exist."* Its DISPOSITION homes the pair on both rows and names the test obligation: *"Homes: **both API rows**, ⊕ **a create-visibility test each side**."* ⊕ **F.W5 `G5`** supplies this row's GREEN condition (spec §2.8: *"One co-signed born-visibility rule **+ a create-visibility test on each side** (G5's GREEN condition)"*); **`R8`, not `G5`, is the RULING id** (spec §2.8's own R-1e label correction, carried). **F.W6 burns the FOURIER half only**; **no F.W1 / F.W3 / F.W4 / F.W5 / F.W8 credit is claimed** (FW6-G17, both directions) | **PENDING.** Zero fourier bytes at this seat; zero value-tree bytes — ⟨cmd⟩ base `$V`, `git diff --stat -- api/src src` → **empty**, re-run at this seat and unmoved, which is also FW6-G16's standing condition at unit `i`. Lands under COMMISSION §2 through unit `i`'s courier letter. ▲ **This evidence cell may not be stamped from a landing that ships only the lineage slot**: E4's adoption test is the **pair** — the shape AND the assertion — and the shape is already shipped here, so **the test is the whole of what this side still owes**, and a landing without it leaves R8 un-adopted by E4's own words |
| **`fr-GalleryFeaturedCarousel` FR-GFC-3** ⊙ — the like verb (spec §2.9; **MAJOR**, four witnesses; the **BLOCKER framing KILLED at K-1** and **not revived by this row**) — **ONE identity with FOUR witnesses**: ⊕ **`C-1`** ⊕ **`fr-GalleryCard C-1-as-corrected`** ⊕ **`GCM-2`·`C-1`** ⊕ **`fr-GalleryInfiniteGrid C-3`** (FOLD BY REFERENCE, spelled with its record because an id-keyed set-difference cannot resolve it). ⊙ **RULED — F-SS4REST R4: REMOVE THE AFFORDANCE** | **NONE — NO F.W6 GATE AND NO F.W6 ACT.** The row is entered **RULED-AND-ROUTED** so that **FW6-G17's reverse direction resolves the id to a wave**; it books nothing, turns nothing, and claims nothing | **NO COMMISSIONED ACT IS BOOKED BY F.W6.** The routing is the deliverable, and it is stated on **re-measured bytes** rather than on the spec's paste, so that a later seat can falsify the routing itself. **WITNESS, this seat's clock** (fourier `3bac3d52`, clean). **(i) No like route exists, proved by the NON-BLIND method.** ⟨cmd⟩ `/usr/bin/grep -rniE '^@[a-z_]*router\.(get\|post\|put\|patch\|delete)\(.*like' api/routers/ \| wc -l` → **0** and ⟨cmd⟩ `/usr/bin/grep -rniE 'def .*like\|"/like' api/routers/ \| wc -l` → **0**, both double-run. ▲ **K-1 COUNTING LOCK, carried VERBATIM and REPRODUCED rather than quoted** — *"a bare `@router.` grep is blind to prefixed routers (`@gallery_router.get(\"/cursor\")`) — read against 13/44 and **cite 45/30/13, never zero**."* Measured at this seat, each arm double-run: bare `@router.` → **30** · `@admin_router.` → **13** · `@app.` → **1** · `@gallery_router.` → **1**; **30 + 13 + 1 + 1 = 45**; and the independent total closes — all-prefix decorators under `api/routers/` → **44**, plus the one `@app` route → **45**. **The absence above is therefore stated over 45, never over 30 alone and never over zero**, and the blind form is exhibited beside the true one so the trap is visible rather than described. **(ii) The server sorts by a counter nothing can increment.** `api/lib/crud/cursors.py:17 SortKey = Literal["newest", "popular", "most-forked", "views", "likes"]` with the map's `"likes": "likes"` row at `:22`, and the compound index ships at `api/services/database.py:111 await _db.visualizations.create_index([("visibility", 1), ("likes", -1), ("_id", -1)])`. **(iii) `liked_ips` exists only as an exclusion — measured, not inherited**: **7** lines over **3** files, **zero writers** (three dict-comprehension drops at `visualizations.py:80` · `admin.py:87` · `gallery.py:34`; four projections `{"liked_ips": 0}` at `visualizations.py:318` · `:705` · `admin.py:555` · `gallery.py:58`) — **the dedup substrate for a verb that was never written**. **(iv) The client hard-codes the on-state**: `web/src/stores/gallery.ts:195 const liked = true;`, followed at `:196-197` by an unguarded monotonic bump — no un-like arm. ⟶ **ROUTING, not an act**: **F-SS4REST R4 rules REMOVE THE AFFORDANCE** (*"no dead affordance under `aria-pressed`"*), and **spec §2.9's OWN branch structure sends the act away from this wave**: *"If SHIP: F.W6 is the burn seat, using the `liked_ips` dedup idiom already in-tree. **If DELETE: F.W4's**."* The owner chose the second arm, so **F.W6 books no server act and turns no gate on this row.** What the ruling produces is a **retirement family**, homed by v2 and repeated here so no seat reads silence as a drop: *"The **display arm** (`aria-pressed` + the re-click guard) is **F.W3/W4's**; the route deletion and index/sort-key retirement are the **fourier API row's**."* ⚑ **Sharpening that matters to whoever lands the retirement**: there is **no route to delete** — limb (i) measures zero — so the fourier-side residue is exactly the **counter field, the compound index at `database.py:111`, and the `"likes"` sort key at `cursors.py:17`/`:22`, which ship without a producer**, and D2's rule states why they retire together: *"The counter, its compound index, its sort key and its UI arm are **retired together**, because each one alone is what makes the next look intentional."* ▲ **Dissent preserved, not resolved** (v2's own ▲): *"the **C axis filed BLOCKER at two sites** against the banked MAJOR; the filing stands in the record and is **not** reconciled by the ruling, which decided the *disposition*, not the *grade*."* ▲ **`fr-GalleryInfiniteGrid C-3`'s own new grain — the orphan-client class, R6-8's degenerate case — rides its NWO leg as census methodology, CITED NEVER BOOKED** (v2's DISPOSITION), and is not re-booked here. ▲ **The store's comment is evidence of the gap, not a specification of it** (v2's ▲) — a landing seat may not read the stopgap comment as intent | **D2** — *The like verb ‡ — RULED: REMOVE THE AFFORDANCE* (`J-diff-shape-v2.md`), whose RULE is the disposition in its own hand: *"**RULE — RULED, COHESION §0j.D F-SS4REST R4: REMOVE THE AFFORDANCE.** **No dead affordance ships under an `aria-pressed`.** A surface that reports state to assistive technology reports a state the system can actually hold; a control whose only write path is a client-side constant does not report state, it asserts one. The counter, its compound index, its sort key and its UI arm are **retired together**, because each one alone is what makes the next look intentional."* ⊕ the honest-repair LOCK at the record's own bytes and case: *"**add the operation or remove the affordance — the honest repair is stated in C §7.1 and adopted: no third option ships**"* — **the owner has now chosen the second arm. ⊘ NO GATE OWNER IS CLAIMED BY F.W6**: the act's owners are **F.W3/W4** (display) and the **fourier API row** (retirement), and **FW6-G17's forward direction must find NO F.W6 credit on this id** | **N/A — NOT PENDING, NOT LANDED, NOT BURNED HERE.** Zero fourier bytes at this seat. **This row has no fourier-side landing to await, because F.W6 commissions no act on it**; its evidence IS the routing above, and it is entered so FW6-G17's reverse direction resolves `FR-GFC-3` (and its three folded witnesses) to a wave rather than to silence. ▲ **A later seat that finds a landing stamped in this cell should read it as a defect**: a stamp here would mean F.W6 took an act R4 homed elsewhere |
| **FR-AFP-1 = D3** ‡ ⊙ — **THE ADMISSION GATE** (spec §2.4; **BLOCKER-weight, OWNER-GATED**) ⊕ **FR-AFP-18** CITED (*the queue has no size anywhere — no total in the envelope, no `AdminStats.flagged`*). ⊙ **RULED — COHESION §0j.D `F-PRODRET` (R3 ≡ D3 ≡ G11): PRODUCER, as a PORT of value.js's shipped verb, homed at F.W8.** This seat **consumes, never re-rules** | **NO F.W6 GATE AND NO F.W6 ACT.** Entered **ADMITTED-and-ROUTED**: the ruling **opens the band** (so the five conditioned rows below are burnable at all) **and homes the port itself at F.W8**, so **F.W6 does not ship the flag-write operation**. ⊘ Spec §2.4's *"If PRODUCER: F.W6 ships the flag-write operation with value.js's `POST /:slug/flag` as reference implementation"* is **superseded ON THE HOMING ONLY** — the branch is live, its address moved — exactly as this record's Owner-gated table states it. The three gates this unit turns (**FW6-G8 · FW6-G10 · FW6-G12**) are the band's **other** server rows; **none of them is this row**, and the row is entered so **FW6-G17's reverse direction resolves `FR-AFP-1` to a wave rather than to silence** | **WITNESS — the producer-absence ENUMERATED, never queried for one name** (the S-8 method law; seat clock **2026-09-17**, fourier `3bac3d52`, working tree clean, every figure double-run identical). **(i) The declared request model is referenced nowhere.** ⟨cmd⟩ `/usr/bin/grep -rn 'FlagRequest' $F/api $F/web/src --include='*.py' --include='*.ts' --include='*.vue'` → **exactly one source site, the definition** — `api/models/admin.py:57 class FlagRequest(BaseModel):`. ⟨▲ *the unfiltered form `grep -rn 'FlagRequest' api web/src` returns **2**, and the second is `api/models/__pycache__/admin.cpython-314.pyc` matching as a **binary** — bytecode residue, not a reference. Published so a landing seat reading "2" does not conclude the model is bound to something*⟩ **(ii) Every `db.flags` WRITE repo-wide is a migration or a fixture.** ⟨cmd⟩ `/usr/bin/grep -rn 'flags\.insert\|flags\.update\|flags\.replace' $F/api --include='*.py' \| /usr/bin/grep -v tests \| wc -l` → **1**, and it is `api/scripts/migrate_flags_field.py:104` (`update_many`, the `$rename` migration). The live surface is **read-and-delete only**: `admin.py:530` (`aggregate`) · `:219` · `:358` · `:469` · `:607` (`delete_many`) · `janitor.py:234` (`delete_many`, reporter-keyed). **No `insert_one` into `db.flags` exists outside `api/tests/`.** **(iii) No client posts a flag.** ⟨cmd⟩ `/usr/bin/grep -n 'flag' $F/web/src/lib/api.ts` → the **two admin calls only** — `:547` GET `/api/admin/flagged` · `:558` DELETE `…/{slug}/flags`; the panel is admin-side and is ⟨cmd⟩ `/usr/bin/wc -l < $F/web/src/components/visualization/gallery/AdminFlaggedPanel.vue` → **285** lines, **the spec's figure reproducing exactly**, whose production-reachable state is the empty state. ⟨*the record's "~110 of 285 panel lines dead by reachability" is **the record's figure, cited not re-derived** (D-19); only the denominator was re-measured*⟩ **(iv) FR-AFP-18, cited:** the listing envelope is `{items, next_cursor, has_more}` (`admin.py:587`) with **no total**, and ⟨cmd⟩ `/usr/bin/sed -n '31,38p' $F/api/models/gallery.py` → `AdminStatsResponse` = `total_entries · featured · saved · normal · total_views · total_likes · storage_bytes` — **no `flagged` member**. The queue's size is unavailable on **both** surfaces. **(v) value.js HAS the verb** — `api/src/modules/palette/routes/flags.ts:15 flagsRouter.post("/:slug/flag", async (c) => {` with its service at `api/src/modules/palette/service/flags.ts` (*"Single route: `POST /palettes/:slug/flag` — user flags a palette for admin review"*) and its suite at `api/src/modules/palette/__tests__/palettes-votes-flags.test.ts` (401 unauthenticated · 201 `{flagged}` · 409 double-flag · 400 self-flag). **Read only — zero value-tree bytes moved by this seat.** ▲ **THE ACT THIS ROW CARRIES IS A ROUTING STATEMENT, NOT A CURE**: unit `i`'s letter names the port **F.W8's**, written under §D3's own lock — *"the port is a port … same identity rules (§D4), same authority class discipline (§C1), same envelope (§A2)"* — so **a producer that lands with a different flag identity re-opens §D4 the moment it ships**, which is why the `FR-AFP-7` row below must settle before the port is written | **v2 `D3` ⊙** — *"**RULED, COHESION §0j.D F-PRODRET (R3 ≡ D3 ≡ G11): PRODUCER.** The moderation queue **gains its user half**, and it gains it as a **PORT of value.js's already-shipped verb** under this contract: `POST /visualizations/{slug}/flags` on the fourier side, the mirror of value.js's `POST /:slug/flag`. **The port is homed at F.W8** (the CRUD union prototype). **F.W5 writes this clause and nothing else about it**"* | **N/A — and N/A is not *pending*.** There is nothing for **this** wave to await: the port lands at **F.W8** and is stamped there, by the ruling's own homing. The distinction is written in the row so the close seat does not read an unstamped cell as an unfulfilled F.W6 ask |
| **FR-AFP-66** (β-miss-1) ⊕ **FR-AFP-9** (C:D-5) dialog-truth arm ⊕ **FR-GV-34** (= MISS-LC-5) the member-site rider — **one identity, two call sites, NOT re-booked** — orphan flag accumulation (spec §2.4; **MAJOR, permanent-growth data defect**; FR-AFP-9's arm **MINOR**). ⊙ **RULED — COHESION §0j.D `F-SS4REST` R6: KEEP the hard-delete arm; copy made truthful about irreversibility.** CONDITIONED on D3/G11 — **ruled PRODUCER**, so the cascade is **BUILT, not deleted with the band**. ▲ **ONE CUT with `FR-AFP-33` below — ONE COMMIT** (§4 lock 3) | **FW6-G8** | **WITNESS, the grace pipeline read WHOLE at this seat's clock** (2026-09-17, fourier `3bac3d52`, clean; ⟨cmd⟩ `/usr/bin/sed -n '112,136p' $F/api/services/janitor.py`). **(i) The grace hard-delete cascades NOTHING.** `:125 hard_deleted = await db.visualizations.delete_many({"deleted_at": {"$lt": grace_cutoff}})` — the entity rows go, the flag rows stay; the pass (`:118-136`) touches `db.flags` **zero times**, and the only flags delete in the whole janitor is `:234 db.flags.delete_many({"reporter_slug": {"$in": stale_slugs}})`, the **reporter-keyed** stale-user cascade, which cannot reach a flag whose reporter is alive. **(ii) The module docstring is FALSE, and it is false twice over.** ⟨cmd⟩ `/usr/bin/sed -n '1,6p' $F/api/lib/crud/softdelete.py` → *"No cascade on soft-delete (R-lifecycle-spec §3.2); **the cron handles hard-delete cascade via ``pinned_cron.cron_prune``.** ``hard_delete_past_grace`` is NOT exported (R3 §1.4)."* — and **(a)** `pinned_cron` carries **zero flags-collection code**: ⟨cmd⟩ `/usr/bin/grep -c 'flag' $F/api/lib/crud/pinned_cron.py` → **1**, and the one line is `:1`, the module summary's own word — *"``pinned: bool`` flag + bounded cron prune (CRUD-CONTRACT §8)"*. ⊘ **the count and the fact disagree, which is why the line is printed rather than the digit** (v2 D6's own reading, adopted); **(b)** ⚑ **SHARPENING, measured here: `cron_prune` is not even the function that hard-deletes visualizations past grace** — that is `janitor.py:125`'s inline `delete_many`, while `cron_prune` is called on `db.contours` (`:153`) and `db.images` (`:157`) under `(pinned, last_accessed_at)`. The docstring names the **wrong owner** for an act that **also** does not cascade; **(c)** ⚑ and its third name is a function that does not exist — ⟨cmd⟩ `/usr/bin/grep -rn 'def hard_delete_past_grace' $F \| wc -l` → **0** (double-run): `hard_delete_past_grace` is **not un-exported, it is unwritten**. **(iii) The cure's shape is already in tree, ~100 lines above the defect** — ⟨cmd⟩ `/usr/bin/sed -n '214,219p' $F/api/routers/admin.py` → `# Admin grace-bypass hard-delete (§7): cascade flags by content hash.` · `full = await db.visualizations.find_one({"slug": slug}, {"content_hash": 1})` · `if content_hash:` · `await db.flags.delete_many({"content_hash": content_hash})` — **the ONLY `content_hash`-keyed flag cascade in the repo**, and the janitor's grace pass is **the same act on a timer** without it. KISS: **lift, do not invent.** **(iv) FR-AFP-9's arm:** the confirm dialog asserts irrevocability over a **30-day-restorable** soft delete — `config.py:44 soft_delete_grace_days: int = 30` — with the restore route **live at `api/routers/visualizations.py:430`** (`@router.post("/{slug}/restore")`, `:445 result = await softdelete.restore(...)`) ⟨*§0.1c's addendum consumed and NOT re-derived: the spec's bare `:430` collides with a real `admin.py:430` (the batch `update_many`); the rooted coordinate is `visualizations.py`'s*⟩ — **and a restored row returns to the queue still flagged**, because nothing ever deleted its flags. **(v) The existing orphan-flag test exercises ONLY the reporter-keyed path** — `api/tests/test_janitor_audit.py:306 await db.flags.insert_one({"slug": "orphan-flag", "reporter_slug": "half-reaped"})`, asserted at `:319`/`:327` through `count_documents({"reporter_slug": "half-reaped"})` (and `:167` the same shape for the stale-user row). **No test anywhere inserts a flag keyed on a `content_hash` and asserts its reaping at grace.** ▲ **COMMISSIONED ACT — THREE CONJUNCTS, exactly FW6-G8's GREEN, none severable**: **(1)** a **`content_hash`-keyed cascade at the grace hard-delete** (`janitor.py:118-136`), lifting `admin.py:214-219`'s shape rather than inventing a second one; **(2)** **the docstring correction — the false module prose is part of the cure, not commentary** (v2 D6's DISPOSITION word for word): the corrected prose names the real owner (the janitor's grace pass), states the cascade it now performs, and drops the unwritten `hard_delete_past_grace`; **(3)** **a test covering the entity-keyed path**. ▲ **STAMPING BAR on the evidence cell: this cell may NOT be stamped from a landing that performs (1) and defers (2) or (3).** G8's GREEN names all three, and spec §2.4's *"Coverage obligation → F.W9/W10"* routes the **coverage programme**, not this fixture — a landing that ships the cascade and sends the entity-keyed test to F.W9/W10 leaves **G8 RED by its own words**. ⊘ **Emitted, never claimed**: FR-AFP-9's truthful copy and FR-GV-34's batch-dialog copy are **F.W3/W4's** (diction rides `FR-AFP-35`) | **v2 `D6`** — *"**RULED, COHESION §0j.D F-SS4REST R6: KEEP the arm; the copy is made truthful about irreversibility.** A lifecycle verb's copy states what the verb does … and **soft delete CASCADES the moderation rows it strands**, or the contract states where they go."* · DISPOSITION: *"The acts: a **`content_hash`-keyed cascade at grace hard-delete** ⊕ **the docstring correction** (the module docstring is false for this band)"*, with *"**truthful copy → F.W3/W4** (diction rides `FR-AFP-35`)"*. ▲ D6's LOCK carried whole: *"the copy is false in the direction that SUPPRESSES a safe action … **Truthful copy is the cure; deletion would have been the workaround.**"* ⟨R-1e re-key observed: the arm is **D6 / ruling R6**, **never "E7"** — F.W5's `E7` is the PATCH clause unit `b` burned⟩ | **PENDING** — fourier sub-session, COMMISSION §2, after the begin-word. **Stamped only by a landing carrying all three conjuncts** (cascade ⊕ docstring ⊕ entity-keyed test) **and only together with `FR-AFP-33`'s bound below** — §4 lock 3 makes them one cut, and a half-cut stamps neither cell |
| **FR-AFP-33** (D-17/L-20/L-22) — the unbounded flag list and the unscoped aggregate (spec §2.4; **MAJOR**). CONDITIONED on D3/G11 — **ruled PRODUCER**, which **changes this row's WEIGHT, not its content**. ▲ **ONE CUT with `FR-AFP-66` above — ONE COMMIT** (§4 lock 3: *cascade without bound leaves the cost; bound without cascade leaves the growth*) | *(**no F.W6 gate of its own** — §3 opens none for the bound; the row rides **FW6-G8**'s one-cut commit and its evidence is read by **FW6-G17**)* | **WITNESS, the listing handler read WHOLE at this seat's clock** (⟨cmd⟩ `/usr/bin/sed -n '509,587p' $F/api/routers/admin.py`), both figures double-run. **(i) The `$push` is unbounded.** The pipeline is a **single `$group` stage** — `:533 "$group": {"_id": "$content_hash", "flag_count": {"$sum": 1}, "flags": {"$push": {reporter_slug, reason, detail, created_at}}}` (`:537` the `$push`) — and there is **no `$slice` anywhere in the API**: ⟨cmd⟩ `/usr/bin/grep -rn '\$slice' $F/api --include='*.py' \| wc -l` → **0** · ⟨cmd⟩ `/usr/bin/grep -c '\$slice' $F/api/routers/admin.py` → **0**. Every flag ever filed on a hash is materialised into the page body. **(ii) The aggregate is unscoped, and the ORDER is the defect.** The pipeline carries **no `$match`**: it groups the **WHOLE `flags` collection** first (`:530-547`) and only then joins to the paginated page — `:552 query = {**base_query, "content_hash": {"$in": list(flagged.keys())}}` — so the cost is **O(\|flags\|) per page request**, not O(page), and it is paid on **every** page of every listing. **(iii) The counterweight, preserved by command** (R2-1-LAW, and it is the reason the row is MAJOR rather than urgent): ⟨cmd⟩ `/usr/bin/grep -n 'if not flagged' $F/api/routers/admin.py` → `550:    if not flagged:` — the short-circuit that carries **production's only case** while the band has no producer. ▲ **COMMISSIONED ACT — server, two halves, one act**: **bound the `$push` with `$slice`** (the page renders a window, not a history), **and scope the aggregate** — `$match` the group to the page's content hashes, which requires computing the page **before** the aggregate rather than after, the inversion the current handler ships. ⊘ **Emitted, never claimed:** the **client collapse is F.W1/W3's** (`./expandable-container` is already installed); **magnitude and β's sort-index note are SS-13's** — **this seat spent no probe** and measured the shape statically. ▲ **STAMPING BAR: a landing that bounds the `$push` and leaves the `$group` unscoped (or the reverse) stamps NOTHING** — and neither half stamps without `FR-AFP-66`'s cascade, because the growth term and the per-request cost are one mechanism | **v2 `D7`** — its DISPOSITION books **`FR-AFP-33`** and splits it in its own words: *"`FR-AFP-33`: the **server bound is this clause's**; the **client collapse is F.W1/W3's**; and ▲ its counterweight is carried — `if not flagged` short-circuits production's only case, so **the cost is LATENT while §D3 stands**, which is precisely why §D3's producer ruling changes this row's weight rather than its content."* ⟨▲ **the other ids D7 books are NOT this row's and are not claimed here**: `fr-BasisSelector m-7` · `FR-AUL-46` · `FR-FG-21`, the folded `R-16`, and the **F.W3-held** leg `fr-ContourSettings B-1 / L-B1 / C-2` — record-qualified per R-5, because `L-B1` at D7 is `fr-ContourSettings`'s, not `fr-FourierShapeExtractor L-B1` (unit `g`'s TRIPWIRE row) nor `fr-FunctionInput L-B1`⟩ | **PENDING** — fourier sub-session, COMMISSION §2. **One landing with `FR-AFP-66`, or neither cell is stamped** (§4 lock 3) |
| **FR-AFP-7** (L-3/L-4) ⊕ **FR-AFP-8** (L-2/C:D-3) **identity arm CITED** — a moderation record keyed on a digest instead of the entity (spec §2.4; **MAJOR**). CONDITIONED on D3/G11 — **ruled PRODUCER**, and the port is written against **whichever this row settles** (§D3's own lock). ▲ **FR-AFP-8's cure is a ONE-TOKEN F.W3/W4 rider (pass `item.slug` as the label) — F.W6 may NOT claim it.** ⊘ **Dissent recorded so the row is not read as twice-derived: `FR-AFP-7` was filed on the β/L axes only; the α reader never examined it** | *(**no F.W6 gate of its own** — §3 opens none for flag identity; the row's evidence is read by **FW6-G17**, and its settlement **gates the F.W8 port** by §D3's lock rather than a gate of this wave)* | **WITNESS, the destructive arm read at its own bytes** (⟨cmd⟩ `/usr/bin/sed -n '590,612p' $F/api/routers/admin.py`; seat clock 2026-09-17, fourier `3bac3d52`, clean). **(i) A slug-labelled route executing a hash-scoped `delete_many`.** `:590 @admin_router.delete("/visualizations/{slug}/flags", …)` → `:600 doc = await db.visualizations.find_one({"slug": slug}, {"content_hash": 1})` — **the entity is resolved and then discarded** — → `:607 result = await db.flags.delete_many({"content_hash": doc["content_hash"]})`, which clears the flags of **every** entity sharing that digest. The docstring says so in the open: *"Resolves the entity by slug, then clears the flag stream keyed by its ``content_hash``"*. **(ii) The fan-out is the index's design, not an accident.** ⟨cmd⟩ `/usr/bin/grep -n 'flags.create_index' $F/api/services/database.py` → `:140 [("content_hash", 1), ("reporter_slug", 1)], unique=True` — **unique per (digest, reporter)** — while the entity's own digest index is **PLAIN**, `:98 await _db.visualizations.create_index("content_hash")`, so **N entities may carry one digest** by construction. ⟨beside: `:141` also declares a plain single-field `content_hash` index on `flags`, disclosed at unit `d` — the uniqueness lives on the **compound**⟩ **(iii) The listing then hands each of the N the WHOLE count.** `:552 query = {**base_query, "content_hash": {"$in": list(flagged.keys())}}` and, per doc, `:572 agg = flagged.get(doc["content_hash"], {})` → `:576 "flag_count": agg.get("flag_count", 0)`: **3 rows × "3 flags" reads as an apparent nine** (v2 D4's LOCK, carried). **(iv) The law the collection breaks is already written at BOTH ends** — ⟨cmd⟩ `/usr/bin/grep -rn 'never identity' $F/api --include='*.py'` → `api/models/visualization.py:113` *"substrate, never identity (CRUD-CONTRACT §1)"*, and the client twin says the same: `web/src/lib/types.ts:211 content_hash: string; // dedup key, never identity (§1)`. ⚑ **SHARPENING: nothing has to be decided about what a digest IS — both repos already state it; the flags collection simply keys on it anyway.** **(v) FR-AFP-8's identity arm, CITED and measured, never claimed:** the panel spends the asset FK on **five** human-readable surfaces — `AdminFlaggedPanel.vue:175` the row label · `:202` · `:212` · `:222` three `aria-label`s · `:224 askDelete(item.slug, item.image_slug ?? item.slug)` the destructive confirm — **while the act deletes `item.slug`**, and the remix model declares no `image_slug` of its own, so siblings are indistinguishable on the surface that asks you to destroy one of them. ▲ **K9 LOCK, BINDING AND OBSERVED: the five `?? item.slug` fallbacks STAY** — ⟨cmd⟩ `/usr/bin/grep -rn '?? item.slug' $F/web/src \| wc -l` → **5** (double-run), and the emitter is unvalidated (`admin.py:578 "image_slug": doc.get("image_slug")` inside a hand-built body with **no** `response_model`), so they are **DEFENSIVE, not dead**. **This seat deleted nothing, prescribed no deletion, and records the limb as NOT PROVEN.** ▲ **COMMISSIONED ACT — the server half ONLY**: **flag identity moves to the ENTITY, or the dismiss scopes to the slug — and the contract STATES WHICH**, because the two choices produce different wire shapes and the F.W8 port must be written against the settled one. ▲ **D4's LOCK installed as a stamping bar, verbatim**: *"A repair that fixes the count display and leaves the key has made an incorrect number correct and left the **destructive** arm — the hash-scoped `delete_many` — exactly as wide as it was."* — **this cell may not be stamped from a landing that corrects `flag_count` and leaves `:607` hash-scoped** | **v2 `D4`** (*"A moderation record keys on the ENTITY it reports, never on a content digest … a dismissal scoped to the digest silently clears every sibling"*; DISPOSITION: *"flag identity moves to the ENTITY, **or** dismiss scopes to slug — and the contract states which … **§D3's port is written against whichever this clause settles**"*) ⊕ **v2 `D5`** for the CITED FR-AFP-8 arm (*"A human-facing surface renders the ENTITY identifier of the thing it is about"*; DISPOSITION: *"the **one-token F.W3/W4 rider** is *pass `item.slug` as the label*"*, with its own lock — *"the rider is one token wide and **it is not the clause**"*). ⟨R-1e re-key observed: the identity clause is **D5**, **never "D12"** — F.W5's `D12` is *Closed domains at the boundary* (`M-10 · M-9`), cited at spec §2.11 rows 7 and 11 and carrying ruling **R5**⟩ | **PENDING** — fourier sub-session, COMMISSION §2. ▲ **This cell gates the F.W8 port by §D3's lock**: *"A producer that lands with a different flag identity re-opens §D4 at the moment it ships"* — so a landing stamped here is a **precondition** of the port, not a parallel act |
| **FR-AFP-36** (L-16/L-21/C:D-20) ⊕ the **`D17` serializer clause** — contract-twin drift and two dialects in one router (spec §2.4; **MAJOR**) ⊕ **`FR-AFP-71` ⊙** (β-miss-9), whose **codegen half is ANSWERED by R7** and whose **module-split half stays ⊙, named open, never presumed** · **`FR-AFP-32`** (D-16/L-13/C:D-15; ⊕ `FR-AFP-40` fold ⊕ `FR-AFP-69` = β-miss-5) rides as a **LEG HELD AT F-W3** · **`fr-GalleryCard L·M-4 / D-13 / C-8(a) + L·D-2 / C-12`** FOLDS at D17 — **the FULL banked spelling**, held at **F-W3**, and **only the `F.W5-W8` serializer leg reaches this wave** · **`GCM-55`** CITED · ▲ **`fr-AdminUserList FR-AUL-17`'s shared client formatter is banked → F.W3 and is NOT re-booked here**. ⊙ **RULED — COHESION §0j.D `F-SS4REST` R7: CODEGEN**, which **lifts this row's ⊙ for the codegen half only** | **FW6-G12** | **WITNESS, re-measured at this seat's clock** (2026-09-17, fourier `3bac3d52`, clean; every count double-run identical). **(i) The checked-shape split, at its true magnitude.** ⟨cmd⟩ `/usr/bin/grep -cE 'response_model' $F/api/routers/admin.py` → **1** against ⟨cmd⟩ `/usr/bin/grep -cE '^@[a-z_]*router\.(get\|post\|put\|patch\|delete)' $F/api/routers/admin.py` → **13**. The one checked operation is **`/stats`** (`:110 "/stats", response_model=AdminStatsResponse, …`) and **neither operation this row is about is it**: `/flagged` (`:509`) is hand-built and returns through `:587 return _json({"items": items, "next_cursor": next_cursor, "has_more": has_more})`, and `…/{slug}/flags` (`:590`) likewise (`:609`). ⟨**K-1 carried, never violated**: the repo's route total is **45** — bare `@router.` **30** ⊕ admin **13** ⊕ `@app.` **1** ⊕ `@gallery_router.` **1**, both decompositions run at unit `d` Act 7 — and *"1 of 13"* above is **admin-scoped by name**, which is the non-blind reading; **45/30/13 is cited, never 30 alone and never zero**⟩ **(ii) ONE router, ONE concept, TWO dialects.** `:90-92 def _json(...)` → `content=json.dumps(body, default=str)` — Python's **space-separated, non-ECMA** `str(datetime)` — carries the flagged path, while `:652 return AuditListResponse(items=items, total=total, page=page, pages=pages)` (declared `api/models/admin.py:101`) carries the audit path as **ISO with `T`**. The defensive asymmetry compounds it: the NaN guard sits in the consumer receiving the **conformant** payload. **(iii) The dead server models, measured by reference rather than asserted.** ⟨cmd⟩ per class, `/usr/bin/grep -rn '<Class>' $F/api --include='*.py'` → **`FlagRequest`** (`models/admin.py:57`) **zero references of any kind**; **`FlaggedListResponse`** (`:82-86`) **zero** — and it declares the **retired offset envelope** `total`/`page`/`pages` against a route that ships `{items, next_cursor, has_more}`; **`FlaggedEntryInfo`** (`:69`) and **`FlagInfo`** (`:62`) are reachable **only** from `FlaggedListResponse` (`:83`, `:74`), so they die with it. ⊘ **DISCLOSURE, measured not smoothed**: v2 A4 names **three** dead models (`FlagRequest` · `FlaggedEntryInfo` · `FlaggedListResponse`) — the **root** count plus the rotted field-carrier — and the **deletion closure of the flagged family is FOUR** (`FlagRequest` ⊕ `FlaggedListResponse` → `FlaggedEntryInfo` → `FlagInfo`); a **fifth and sixth**, `GalleryCursorResponse` (`:118`, zero references) → `CursorInfo` (`:113`), are dead by the identical measure but sit **outside this clause's named set** — **cited here for the landing seat, NOT booked, and NOT added to the act's scope by this seat**. **(iv) The twins drifted in OPPOSITE directions, which is the A4 defect exhibited rather than asserted.** The wire emits the join key — `:577 "content_hash": doc["content_hash"]` — and the client twin **omits it**: `web/src/lib/types.ts:152-161 export interface FlaggedVisualization { slug; flag_count; flags; image_slug; owner_slug; tier; created_at }`, against ⟨cmd⟩ `/usr/bin/grep -rn 'content_hash' $F/api --include='*.py' \| wc -l` → **81** vs ⟨cmd⟩ `/usr/bin/grep -rn 'content_hash\|contentHash' $F/web/src \| wc -l` → **2**. Meanwhile the **server's** dead model carries `user_slug` where the wire says `owner_slug` (`:578`) and `total/page/pages` where the wire ships the cursor envelope, while the **client's** own comment (`types.ts:162-164`) already states the cursor envelope correctly. ⚑ **SHARPENING: the client is RIGHT about the envelope and WRONG about the join key; the server's models are wrong about both — three hand-maintained copies, drifting independently, exactly as A4's structural-cause limb predicts.** **(v) `GCM-55`, cited:** `:85-87 def _public_doc(doc)` strips only `_id` and `liked_ips`, and the body leaves through `json.dumps`, so `likes` is served **raw** — the Pydantic model is bypassed, never consulted. ▲ **COMMISSIONED ACT — ONE act with three inseparable limbs**: **ONE generated/checked shape** (now admissible: **R7 = CODEGEN**, and v2 A4 limb 3 records it as *"an **amendment stated in the open**"* of inv-26's hand-typed-twins spelling, **not a silent reversal**) ⊕ **the dead models DELETED** ⊕ **ONE serializer on the server**, the flagged path emitting ECMA-parseable timestamps as the audit path already does. ▲ **A4's LOCK, verbatim, installed as a stamping bar**: *"Codegen without deletion is worse than neither: if the three dead models survive the generator, the contract acquires a machine-checked copy of a retired shape. **The generation and the deletion are one act.**"* ▲ **D17's LOCK, likewise**: *"the cure is a **posture in the contract**, not a guard in each consumer"* — **a landing that adds a parser guard per reader has not unified the serializer.** ▲ **K9 LOCK, binding: the five `?? item.slug` fallbacks STAY** — deleting them under *"the generated type says it is non-null"* **ships the generated type's lie** while `:578` still emits `doc.get("image_slug")` through an unvalidated body. ⊙ **`FR-AFP-71`'s MODULE-SPLIT half is still OWNER-GATED and is NOT answered here** (R7 rules codegen, not the split; the god-module measure that raised it — ⟨cmd⟩ `/usr/bin/wc -l $F/web/src/lib/api.ts $F/web/src/lib/types.ts` → **672** and **391** — is recorded, and no split is designed) | **v2 `A4`** — *"There is **ONE source of the shared shapes**, and both repos' types are **derived from it**"*, limb 3 amended in the open by **R7**: *"`§0j.D` F-SS4REST ruling R7 rules CODEGEN — *twins derived from one source (inv-16/inv-26 restated; structural invariants over prose)*. v2 therefore **amends the hand-typed-twins limb to generated-twins** … **This is an amendment, not a silent reversal**"*, with the consequence stated **as part of the rule**: *"**the three dead server models encoding the retired contract are deleted, not regenerated**"* ⊕ **v2 `D17`** — *"**One concept, one serialization, across every operation of one API** … and that form is **ECMA-parseable**"*, ▲ *"**F.W5 owns ONLY the serializer arm**"* and *"The five-copy `timeAgo` family's shared formatter is **F.W3's and already banked as `FR-AUL-17` — do NOT re-book it here**"*. ⟨R-1e re-key observed: the serializer clause is **D17**, **never "D10"** — F.W5's `D10` is *The list contract: filters and windows*⟩ | **PENDING** — fourier sub-session, COMMISSION §2. **Not stamped by**: a generator that leaves any dead model standing (A4's lock) · a per-consumer parse guard in place of one serializer (D17's lock) · any landing that deletes the five `?? item.slug` fallbacks (K9) · any module split, which remains ⊙ and unruled |
| **FR-GV-9** (= `C·M-8`, **second clause**) ⊕ **FR-AFP-10** (C:D-4) ⊕ **FR-AFP-70** (β-miss-7) — a blanket write is not a transition (spec §2.4; **MAJOR**) ⊕ **`fr-GalleryFeaturedCarousel` FR-GFC-20's SERVER RIDER travelling in this cure** (`BatchResponse.errors` never populated → a partial batch failure reports green) — **carried as a RIDER, NOT re-booked**: the dead-errors arm is banked at **`fr-AdminUserList` FR-AUL-13** (R-5 one-home law), and the **F.W4 arm** of FR-GFC-20 (stale-slug pruning in `selectedHashes`) is **not this row's**. ▲ **`FR-GV-9`'s FIRST clause was KILLED at R-3/K3 — the kill travels with the row.** ▲ **β's 428-escalation hypothesis is KILLED BY ITS OWN AUTHOR (If-Match `*` accepted) and is NOT revived here.** CONDITIONED on D3/G11 for its FR-AFP arms — **ruled PRODUCER** | **FW6-G10** | **WITNESS, the three writes read at their own bytes** (⟨cmd⟩ `/usr/bin/sed -n '160,190p'` and `'411,450p' $F/api/routers/admin.py`; seat clock 2026-09-17, fourier `3bac3d52`, clean). **(i) The destructive converse — an unconditional bulk `$set`.** `:435 elif body.action == "unfeature":` → `:436-439 result = await db.visualizations.update_many({"slug": {"$in": slugs}, "deleted_at": None}, {"$set": {"tier": "normal", "updated_at": datetime.now(UTC)}})`: **the filter names the slugs and liveness and NEVER the current tier**, so Batch Unfeature over a **SAVED**-tier entry silently erases the saved tier — a state the operator was never asked about. ⟨the spec's `:435-440` span reproduces; the `$set` literal is `:438`⟩ The feature arm immediately above (`:429-433`) is the same shape. **(ii) The non-transition.** `set_tier` (`:160` decorator, `:164` def) writes `:181-183 {"$set": {"tier": body.tier, "updated_at": datetime.now(UTC)}}` **and nothing else**, then audits `:185 f"set_tier:{body.tier}"` — **and the flagged listing carries no tier predicate**: ⟨cmd⟩ `/usr/bin/sed -n '509,587p' $F/api/routers/admin.py \| /usr/bin/grep -c '"tier"'` → **1** (double-run), and that single occurrence is the **emitted field** `:577 "tier": doc.get("tier")`, **never a filter** — the query is `:552 {**base_query, "content_hash": {"$in": …}}` over `:523 not_deleted_filter()`. **"Mark acceptable (save)" therefore never dequeues**, exactly as FR-AFP-10 states. **(iii) FR-AFP-70 — hardwired, no inverse.** ⟨cmd⟩ `/usr/bin/grep -n 'handleSetTier' $F/web/src/components/visualization/gallery/AdminFlaggedPanel.vue` → `:116` the handler and `:204` its **only** call site, `@click="handleSetTier(item.slug, 'saved')"`: a repeat click is a **real PUT + audit row + success toast + full pagination reset for a no-op**, and there is no verb to undo it. **(iv) The FR-GFC-20 server rider, measured.** The batch handler returns `:448 return _json({"ok": True, "affected": affected})` (the users arm `:501` likewise) while the **wire twin declares an error channel**: `web/src/lib/types.ts:191-195 export interface BatchResponse { ok: boolean; affected: number; errors?: string[] }` — and ⟨cmd⟩ `/usr/bin/grep -rn 'BatchResponse\|errors=' $F/api --include='*.py' \| /usr/bin/grep -v tests \| wc -l` → **0**: **the server has never heard of the field**. The delete arm's per-slug loop (`:425-428` — `if body.action == "delete": for slug in slugs: if await soft_delete(db.visualizations, slug): affected += 1`) counts successes and **drops every failure silently**, so a partial batch reports green — **the same batch write this row cures**, which is why the rider travels with FW6-G10's transition cure and is burned **with** it. ▲ **COMMISSIONED ACT — the server STATES THE TRANSITION**: the `$set` is **conditioned on the current tier** (the filter carries the state it claims to leave, so unfeature cannot silently erase `saved`) · **every verb gains its inverse** (FR-AFP-70's `'saved'` write is one half of a pair) · **the flagged listing carries the tier predicate `FR-AFP-10` assumes**, so *"Mark acceptable"* dequeues · and, **in the same cure, `BatchResponse.errors` is populated by the arm that drops rows** — a conditioned `$set` that silently discards rows while the envelope reports success is **not a stated transition**. ▲ **D8's LOCK carried**: *"The in-file comment claims the opposite of what the handler does … a cure that leaves the comment has left the defect's documentation in place"* — **the false in-file comment dies with the cure.** ▲ **β's 428-escalation stays killed — it may not be reached for as the conditioning mechanism** (If-Match `*` is accepted; `set_tier` already guards with `require_if_match` at `:179`, which is **not** the missing predicate and must not be sold as it). ⊘ **Emitted, never claimed**: FR-AFP-70's **reflect-and-disable** interim and FR-AFP-59's tier render are **F.W3/W4's**; FR-GFC-20's stale-slug pruning is **F.W4's**; and **`FR-GFC-4`'s missing `tier` param at every layer is F.W8's** (§4's F.W8 edge) — **this row's predicate is the flagged listing's own and the two are NOT fused** | **v2 `D8`** — *"**A verb that claims to move an item between states performs the transition, and every verb has its inverse.** A write that sets a field is not a transition unless the predicate that reads the field participates; and a bulk write that unconditionally `$set`s a shared field **erases states it was never asked about**"*; DISPOSITION: *"Booked: **`FR-AFP-10` ⊕ `FR-AFP-70`** (C:D-4 · β-miss-7) · **`FR-GV-9`** (=C·M-8, second clause). **ONE semantics decision covers the tier↔flag pair**; the tier verb gains its inverse; **the false in-file comment dies with the cure**; the interim posture is **reflect-and-disable when already saved**. ▲ **`FR-GV-9`'s first clause was KILLED at R-3/K3** … ▲ **β's own KILL of its 428-escalation hypothesis (If-Match `*` accepted) is preserved — do not revive it.** Rides banked **`FR-GFC-20`**'s stale-slug row."* ⟨the listing-predicate's contract FORM is **`D10`**'s term — *"A filter that is offered is a parameter the operation accepts"* — **cited for shape only; D10's own booked ids (`FR-GFC-4` · `fr-GalleryInfiniteGrid R-7`) are not this row's**⟩ | **PENDING** — fourier sub-session, COMMISSION §2. **Not stamped by**: a landing that conditions the `$set` and leaves the flagged listing without its tier predicate (the two halves are **one** semantics decision, D8's own words) · one that leaves the false in-file comment standing · one that reports green on a partial batch (the FR-GFC-20 rider travels **in** this cure and is not deferrable) · any revival of the 428 escalation |

### §1.b Unit `b`'s three rows — the receipts, the notation, and what this seat did **not** decide

**SERVED MODEL: claude-opus-5[1m]** · seat clock **2026-09-17** · base `F=/Users/mkbabb/Programming/fourier-analysis`
(**READ-ONLY**), `V=/Users/mkbabb/Programming/value.js` · engine **`/usr/bin/grep`** (§0.2's pin; bare `grep`
is ugrep in this shell). **Zero fourier bytes · zero value-tree product bytes.**

**Substrate, re-measured before any citation** (D-19 MEASURE-AT-OPEN; the F.W0 pre-gate is unit `a`'s and is
not re-litigated): ⟨cmd⟩ `git -C $F rev-parse --short=8 HEAD` → **`3bac3d52`** · ⟨cmd⟩ `git -C $F status
--porcelain | wc -l` → **0**. Unmoved from unit `a`'s reading, so **§0.1c's anchor re-resolution holds at
this seat's clock** and every anchor above is cited from it rather than re-derived.

**The counts this row set publishes, each double-run, both runs identical** (WRITE-THEN-MEASURE):

| figure | ⟨cmd⟩ | run 1 | run 2 |
|---|---|---|---|
| `_write_root_version` source sites | `/usr/bin/grep -rn "_write_root_version" $F/api --include='*.py' \| wc -l` | **3** | **3** |
| `visualization_versions` non-test sites | `/usr/bin/grep -rn "visualization_versions" $F/api --include='*.py' \| /usr/bin/grep -v tests \| wc -l` | **8** | **8** |
| PATCH fields (`VisualizationUpdate`) | `/usr/bin/sed -n '203,207p' $F/api/models/visualization.py \| /usr/bin/grep -c ':'` | **5** | **5** |
| PATCH fields (`VisualizationPatch`, the wire twin) | `/usr/bin/sed -n '257,261p' $F/web/src/lib/types.ts \| /usr/bin/grep -c '?:'` | **5** | **5** |
| atoms (`ATOM_KEY_ORDER`) | `/usr/bin/sed -n '30,34p' $F/api/lib/crud/atomdiff.py \| /usr/bin/grep -c '"'` | **5** | **5** |

▲ **One divergence of FORM, disclosed rather than smoothed**: the unscoped `grep -rn "_write_root_version"
$F/api` the spec's FW6-G1 witness spells also prints a **binary match line** for
`api/routers/__pycache__/visualizations.cpython-314.pyc`. The figure published above is `--include='*.py'`
**source** sites — **3**, the same three the baseline row read. The `.pyc` is build output, is not a site,
and is named here so a later seat re-running the spec's own spelling is not surprised by a fourth line.

**Notation, and why this seat mints neither the marker nor its literal.** §0.1c publishes a SELF-COUNT over
the settled bytes of **this file**: its `-c` probe over unit `a`'s beside-marker reads **18**, of which two
are the notation's own definition and the receipt's own command text, *"so **SIXTEEN** table rows carry a
beside-note"*. An appending unit that minted the same token — **or that merely quoted the probe's pattern
verbatim** — would silently falsify a receipt it did not author, and the second way is the subtler one: a
count over a file counts the lines that take the count. Unit `b`'s coordinate correction therefore carries a
**distinct marker of its own**, spelled `⟨b` + `-note:` and appearing in the **F-α row**, where it records
both of this seat's corrections in one place (the rooting of `service/versions.ts` and the live form's
nullish guard). ▲ **And this seat publishes NO count of that marker** — deliberately, because a figure over
a token whose every mention moves it has no fixed point, and chasing one is how §0.1c's own *"17"* nearly
shipped. **The register's countable operand is the `id` column, which FW6-G17 greps; a notation marker is
not an operand and is not counted.** This paragraph spells the beside-probe with a bracket so that quoting
it
matches nothing: ⟨cmd⟩ `/usr/bin/grep -c '⟨besid[e]' burn-register.md` → **18** at this seat's settle,
**double-run 18**, unmoved from unit `a`'s figure — the bracketed pattern matches exactly the lines the
plain one does, while the line you are reading is not one of them. ⟨*measured the honest way: the first
draft of this paragraph quoted the pattern plainly and drove the count to **22**; it was corrected before
the file settled, which is the only moment such a correction is not an erratum*⟩. **E-3 is served the other
way round too**: nothing of unit `a`'s is rewritten, and the note records a rooting and a live-form
difference **beside** the spec's spelling, never over it.

**What this seat did NOT decide, stated so the close seat does not read silence as consent.**

1. **F-α's disposition is UNRULED.** COHESION §0j.D reaches it nowhere; the register row flags it inline per
   runbook §5.7 (*"Owner rulings are FLAGGED INLINE and never presumed"*) and names **both** branches with
   the falsifier that closes either. **The dissent is carried, not arbitrated**: fourier's board books
   DELETE (`M/M.md` §7's fold row + the M.W10 wave row); the seam table frames value.js as the deepening
   side; the spec itself calls the question *"genuinely open"*.
2. **A second, narrower dissent rides F-β** — M.W10's *"W2-fix: `palette_slug` remix-only"*, which would
   resolve FW6-G3 and FW6-G4 by subtraction. Also carried, also unarbitrated, also routed to unit `i`'s
   letter. ▲ **Neither dissent is a reason to weaken a gate**: the gates' falsifiers are unchanged and
   remain the spec's own words.
3. **No landing is claimed.** §3's split verdict is obeyed to the letter — *"a gate closes for F.W6 when the
   commissioned act is authored with its evidence row in the burn register; it goes GREEN only when the
   named landing occurs. F.W6 never claims a GREEN it did not execute."* All four of this unit's gates are
   reported **RED with the commissioned act installed**, exactly as unit `a` reported FW6-G18.

### §1.c Unit `c`'s two rows — the ONE privacy limb, the five sharpenings, and the two honesty locks

**SERVED MODEL: claude-opus-5[1m]** · seat clock **2026-09-17** · base `F=/Users/mkbabb/Programming/fourier-analysis`
(**READ-ONLY**), `V=/Users/mkbabb/Programming/value.js` · engine **`/usr/bin/grep`** (§0.2's pin; bare `grep`
is ugrep in this shell). **Zero fourier bytes · zero value-tree product bytes.**

**Substrate, re-measured before any citation** (D-19 MEASURE-AT-OPEN; the F.W0 pre-gate is unit `a`'s and is
not re-litigated): ⟨cmd⟩ `git -C $F rev-parse --short=8 HEAD` → **`3bac3d52`** · ⟨cmd⟩ `git -C $F status
--porcelain | wc -l` → **0**, both double-run. **Unmoved from units `a` and `b`**, so §0.1c's anchor
re-resolution holds at this clock and every anchor above is cited from it rather than re-derived.

**The counts these two rows publish, each double-run, both runs identical** (WRITE-THEN-MEASURE):

| figure | ⟨cmd⟩ | run 1 | run 2 |
|---|---|---|---|
| `_readable_or_none` source sites | `/usr/bin/grep -rn '_readable_or_none' $F/api --include='*.py' \| wc -l` | **5** | **5** |
| `ForkCrumb` declared fields | `/usr/bin/sed -n '331,335p' $F/api/models/visualization.py \| /usr/bin/grep -c ':'` | **5** | **5** |
| `images.py` operations | `/usr/bin/grep -cE '^@[a-z_]*router\.(get\|post\|put\|patch\|delete)' $F/api/routers/images.py` | **7** | **7** |
| `images.py` DELETE operations | `/usr/bin/grep -cE '^@[a-z_]*router\.delete' $F/api/routers/images.py` | **0** | **0** |
| `images.py` `Depends` occurrences | `/usr/bin/grep -c 'Depends' $F/api/routers/images.py` | **0** | **0** |
| `images.py` `Cache-Control` lines (3 headers ⊕ 1 comment) | `/usr/bin/grep -c 'Cache-Control' $F/api/routers/images.py` | **4** | **4** |
| `touch_document` sites | `/usr/bin/grep -rn 'touch_document' $F/api --include='*.py' \| wc -l` | **6** | **6** |
| handlers that bump the reap clock | `/usr/bin/grep -c 'asset = await get_image_asset' $F/api/routers/images.py` | **4** | **4** |
| slug word-list lengths (the 2²⁸ operand) | `json.load(slug_words.json)` → `{k: len(v)}` | **128·128·128·128** | **128·128·128·128** |

▲ **Instrument disclosure, in the class §0.2 and C5 both name**: every source count above is
`--include='*.py'`-scoped. Run unscoped, `_readable_or_none` returns **6** — the extra line is a
`Binary file …__pycache__… matches` row, the same `.pyc` class unit `b` disclosed on `_write_root_version`.
**The digit is wrong and the command still "works"**; the scoped figure is the published one.

**FIVE SHARPENINGS, each measured at the bytes and each recorded BESIDE the spec's spelling, never over it
(E-3).** None contradicts a spec claim; each widens or sharpens one, and three of the five change what a
*landing* must do:

1. **The breadcrumb emits FIVE fields, not four.** §2.2 names `slug`, `set_hash`, `author_slug`,
   `created_at`; `ForkCrumb` (`api/models/visualization.py:326`) also declares **`fork_of`**, which *is the
   next ancestor's slug*. **Load-bearing**: a cure that redacts a private hop while its child still carries
   `fork_of` has published the private ancestor's identifier anyway. The act names it.
2. **`_readable_or_none` and *"non-public"* are two predicates, and they differ at `unlisted`.** The gate's
   own body admits an `unlisted` row to any viewer (it tests `visibility == "draft"` alone); value.js's
   `isActivePublic` is `public && not-deleted`. §2.2's cure cell uses both spellings in one sentence.
   **Flagged inline, resolved by CITATION not by choice**: the adoption target is value.js's, whose in-source
   guarantee enumerates *"private / unlisted / trashed / purged"* among the collapsing states.
3. **The janitor reward surface is FOUR handlers, not one.** The banked row and the spec both say *"every
   anonymous **blob** GET ends in `touch_document`"*; the touch actually rides `get_image_asset`
   (`api/dependencies.py:75`), whose call sites are **blob `:140` · thumbnail `:151` · overlay `:177` ·
   extract-contour `:214`**. **Load-bearing**: a predicate fix applied at the blob route alone leaves three
   other paths bumping `last_accessed_at`.
4. **2²⁸ is a property of the DATA, not of a guard.** `_load_word_lists` rejects only `len(words) < 64`
   (`slugs.py:29`) while the four lists measure 128 each. The banked arithmetic holds **today**; the
   invariant that would keep it holding is not in the code. Recorded as a finding, **not** as a new cure —
   GCM-52's cure is the visibility gate, never a longer slug.
5. **The blob handler's own comment calls the 404/410 shim *"auth"*** (`images.py:136-138`).
   `get_image_asset` performs no viewer check. **Same class as unit `b`'s `VisualizationUpdate` docstring
   find**: an in-source statement that is false about the very posture the row is about, and part of the
   cure surface.

**The two honesty locks, observed as bars on THIS seat's own prose.**

- **Soft-delete DOES de-list from browse — the hole is not overstated.** Measured, not conceded:
  `api/routers/gallery.py:53 base_query = softdelete.with_not_deleted({"visibility": "public"})`; the detail
  read refuses a deleted row to a non-owner (`visualizations.py:263`); and `_recompute_pin_flags` **unpins**
  a soft-deleted viz's blob (`janitor.py:351 {"$match": {"deleted_at": None}}`, its docstring saying so).
  **The entity de-lists and the soft-delete arm works. What survives is the ASSET LEAF, reachable by slug,
  and the RECENCY arm that an anonymous fetch resets.** Stating it any larger would be the over-claim C's
  scope discipline exists to stop; stating it any smaller would lose the BLOCKER.
- **The deployment auth-proxy question stays UNPROVEN (SS-13 defer #5), in BOTH directions.** This seat ran
  **no** live probe and asserts **no** proxy, present or absent. FW6-G11 may not close by assuming one, and
  may not be argued RED *harder* by assuming its absence either: the measured surface is the repository's,
  and the deployment's posture is unmeasured by construction.

**What this seat did NOT decide, stated so the close seat does not read silence as consent.**

1. **No branch was chosen where the spec left one open — and none was open here.** Unlike unit `b`'s F-α,
   both of this unit's dispositions are **stated by v2** (C5 states redaction parity; C2 states the
   four-limb contract as one unit), so the act **cites the clause and does not re-choose it**, exactly as
   the brief orders. **G6's second branch** (*"the asymmetry is stated with its reason"*) is therefore
   **spent, not declined** — v2 adopted the shape, so there is no asymmetry left to state.
2. **The `unlisted` precision is FLAGGED, not resolved by fiat.** Sharpening 2 names the divergence and
   names value.js's guarantee as the adoption target because §2.2 says *"adoption, not design"*. If the
   landing seat reads it the looser way, that is a disposition it must write down — **the act forbids it
   being taken by default**, which is the whole of what this seat may lawfully do about it.
3. **Nothing of the moderation band beyond this leaf is booked.** `FR-AFP-1`'s producer decision and the
   **port** it commissions are **F.W8's** by F-PRODRET's own homing; `FR-AFP-66 / -7 / -33 / -36` are unit
   `e`'s; the panel's client and copy arms are **F.W3/W4's** and are emitted, never claimed.
4. **No landing is claimed.** §3's split verdict is obeyed to the letter: both gates are reported **RED with
   the commissioned act installed**, as units `a` and `b` reported theirs.

▲ **Notation, per unit `b`'s standing lesson**: this unit's coordinate note carries a marker of its own and
**this seat publishes NO count of it** — *"a figure over a token whose every mention moves it has no fixed
point"*. The register's countable operand is the `id` column, which FW6-G17 greps. Unit `a`'s beside-marker
figure is **unmoved at 18** by these two rows, and unit `b`'s bracketed probe is the only place that figure
is taken.

### §1.d Unit `d`'s four rows — the counter, the create path, the derived entity, and the ruled-away verb

**SERVED MODEL: claude-opus-5[1m]** · seat clock **2026-09-17** · base `F=/Users/mkbabb/Programming/fourier-analysis`
(**READ-ONLY**), `V=/Users/mkbabb/Programming/value.js` · engine **`/usr/bin/grep`** (§0.2's pin; bare `grep`
is ugrep in this shell). **Zero fourier bytes · zero value-tree product bytes.**

**Substrate, re-measured before any citation** (D-19 MEASURE-AT-OPEN; the F.W0 pre-gate is unit `a`'s and is
not re-litigated): ⟨cmd⟩ `git -C $F rev-parse --short=8 HEAD` → **`3bac3d52`** · ⟨cmd⟩ `git -C $F status
--porcelain | wc -l` → **0**, both double-run. **Unmoved from units `a`, `b` and `c`**, so §0.1c's anchor
re-resolution holds at this clock and every anchor above is cited from it rather than re-derived.

**The counts these four rows publish, each double-run, both runs identical** (WRITE-THEN-MEASURE):

| figure | ⟨cmd⟩ | run 1 | run 2 |
|---|---|---|---|
| `viewed_ips` source sites | `/usr/bin/grep -rn 'viewed_ips' $F/api --include='*.py' \| wc -l` | **0** | **0** |
| `liked_ips` source sites (all exclusions, 0 writers) | `/usr/bin/grep -rn 'liked_ips' $F/api --include='*.py' \| wc -l` | **7** | **7** |
| `viewedHashes` client sites | `/usr/bin/grep -rn 'viewedHashes' $F/web/src \| wc -l` | **3** | **3** |
| `setVisibility` sites (decl + export; **0 call sites**) | `/usr/bin/grep -rn 'setVisibility' $F/web/src \| wc -l` | **2** | **2** |
| `visualizations.py` route decorators | `/usr/bin/grep -cE '^@[a-z_]*router\.(get\|post\|put\|patch\|delete)' $F/api/routers/visualizations.py` | **13** | **13** |
| …of which a **view** verb | `… \| /usr/bin/grep -ci 'view'` over the route-path literals | **0** | **0** |
| `replay_or_record` non-test router sites | `/usr/bin/grep -rn 'replay_or_record' $F/api --include='*.py'` → `:236` create · `:612` remix | **2** | **2** |
| `savedSnapshots` client sites (1 writer, 2 readers, 1 decl) | `/usr/bin/grep -rn 'savedSnapshots' $F/web/src --include='*.ts' --include='*.vue' \| wc -l` | **4** | **4** |
| `remix` in the client API surface | `/usr/bin/grep -c 'remix' $F/web/src/lib/api.ts` | **0** | **0** |
| `_remix(` call sites in `test_remix.py` | `/usr/bin/grep -c 'await _remix(' $F/api/tests/conformance/test_remix.py` | **12** | **12** |
| …of those, passing `visibility=` explicitly | `/usr/bin/grep -c 'await _remix(.*visibility=' …` | **8** | **8** |
| files asserting a **child's** visibility, corpus-wide | `/usr/bin/grep -rc 'child\["visibility"\]' $F/api/tests --include='*.py'` → no non-`:0` file | **0** | **0** |
| **K-1**: bare `@router.` (the BLIND form) | `/usr/bin/grep -rhcE '^@router\.(get\|post\|put\|patch\|delete)' $F/api/routers/*.py` summed | **30** | **30** |
| **K-1**: `@admin_router.` | `/usr/bin/grep -cE '^@[a-z_]*router\.(get\|post\|put\|patch\|delete)' $F/api/routers/admin.py` | **13** | **13** |
| **K-1**: `@app.` | `/usr/bin/grep -cE '^@app\.(get\|post)\(' $F/api/main.py` | **1** | **1** |
| **K-1**: `@gallery_router.` | `/usr/bin/grep -cE '^@gallery_router\.' $F/api/routers/gallery.py` | **1** | **1** |
| **K-1**: all-prefix decorators under `api/routers/` | `/usr/bin/grep -rhcE '^@[a-z_]*router\.(get\|post\|put\|patch\|delete)' $F/api/routers/*.py` summed | **44** | **44** |
| like routes, non-blind method | `/usr/bin/grep -rniE 'def .*like\|"/like' $F/api/routers/ \| wc -l` | **0** | **0** |

▲ **K-1 DISCHARGED BY ARITHMETIC, NOT BY RECITATION: `30 + 13 + 1 + 1 = 45`, and the independent total
closes at `44 + 1 = 45`.** Both decompositions were run at this seat and both reproduce the
`operation-register.md` triple. **The lock's own trap is exhibited beside its cure** — the bare `@router.`
form returns **30**, which is what a seat citing "30" would have published as the whole API. **Every absence
this unit states is stated over 45.**

**FOUR SHARPENINGS, each measured at the bytes and recorded BESIDE the spec's spelling, never over it
(E-3).** None contradicts a spec claim; each widens one, and three change what a *landing* must do:

1. **The publish self-count's ALWAYS-MISS is a STORE BOUNDARY, not a cache miss.** The spec says *"every
   ETag cache-miss on publish/soft-delete adds a phantom view"*; the mechanism is stronger than "miss" — a
   freshly minted draft's ETag is parked in `workspace.ts:359 visualizationETag`, a **different store** from
   `gallery.ts:50 etags`, so `:223` misses **by construction on every publish**, not occasionally.
   **Load-bearing**: a cure that warms the gallery map does not close it; the read must stop writing.
2. **The explicit-verb branch is an ADOPTION — the idiom is four routes away in the same file.**
   `:430 /restore` · `:488 /remix` · `:669 /publish` · `:675 /unpublish` are dedicated sub-resource POST
   verbs already shipping, and **none of the router's 13 decorators is a view verb**. Unit `b` recorded the
   identical lesson on the remix tri-state; it is recorded again because the KISS bar is the same one.
3. **The create path's idempotency envelope is ALREADY INSTALLED (D-2, extended).** `replay_or_record` has
   **two** non-test router sites — `:236` **create** and `:612` remix — and the spec names only the remix
   arm. **Load-bearing**: the ask is a **key** and an **index**, not an envelope; the RED is sharper and the
   landing is smaller.
4. **A derivation cannot declare itself at create even if a client wanted to.** `VisualizationCreate`
   carries **no `fork_of` field** under `extra="forbid"`, while the entity model holds the slot and the only
   writer is a remix route with **zero client function** (`grep -c 'remix' api.ts` → 0). **Load-bearing**:
   GCM-1's server arm is a *closed create contract beside an uncalled verb*, not a missing conditional.

**What this seat measured that the wave had not, and states as the unit's sharpest finding.** E4's rule is
that *"the rule is not adopted until both tests exist"*, and R8's shape conjunct is **already met on the
fourier side** (`:277` and `:183` both default `"draft"`). **The open conjunct is the EVIDENCE one, and it
is absent by enumeration**: the remix surface is exactly two test files, **neither** asserts a visibility,
and of the **12** `_remix(` sites **8** pass `visibility=` explicitly while the four that do not assert
`image_slug`/`contour_hash`/`animation_data`, a 401, a 422 and a 404. **The `"draft"` default R8 rules is
exercised four times and asserted nowhere.** The create side's `test_default_draft`
(`test_visibility.py:38-46`) is a **model** assertion — no route, no persisted document, no child — and is
recorded as existing-at-the-wrong-altitude rather than counted as satisfying G5.

**The locks, observed as bars on THIS seat's own prose and acts.**

- **FR-GV-24 — no re-open-increment assertion.** This seat **wrote no test and prescribed none**. The lock
  is carried into the commissioned act verbatim and into the landing-evidence cell as a stamping bar, so a
  landing that satisfies FW6-G6's counter arm with a test asserting the opposite of the shipped intent
  **cannot** stamp row 1. The defect is **SCOPE, not absence** — `viewedHashes` works within a session.
- **K12 — B-2 closes STATICALLY; SS-13 spends no probe.** **Zero probes were run by this seat**, on either
  row. Every limb of FW6-G7's witness is a static read: the no-header passthrough at `idempotency.py:69`
  and the plain index at `database.py:98` are the two mechanisms K12 names, and both were read, not
  observed live.
- **R8 (F-SS4REST) — the FOURIER HALF ONLY.** The value half (`forks.ts:76`) is **named and routed OUT**,
  not burned; **GCM-1's `/v/` routing cure is F.W4's**; the D9 reconciliation is the value.js API row's and
  is v2's disclosure, not this seat's finding to re-open.
- **R4 (F-SS4REST) — the affordance goes to F.W4 and F.W6 books nothing.** Row 4 turns **no gate**, claims
  **no act**, and carries **N/A** rather than **PENDING** in its evidence cell, because there is nothing for
  this wave to await. A stamp in that cell would itself be the defect.
- **K-1 — cite 45/30/13, never 30 alone or zero.** Discharged by running both decompositions, not by
  repeating the sentence; the blind form's **30** is published **beside** the true **45** so the trap is
  visible.

**What this seat did NOT decide, stated so the close seat does not read silence as consent.**

1. **Neither of FW6-G6's two branches was chosen.** E6 states both and this seat states both, with the
   bounds that bind **either**. Choosing here would pre-empt a contract decision that is not this wave's.
2. **No `viewed_ips` design was authored.** The field is named as the *shape* the cure would take and
   immediately fenced: under **R4** the `liked_ips` surface is being **retired**, so **symmetry with it is
   not an argument** — recorded as this seat's own bar so the landing seat does not reach for it.
3. **B-2's "and/or" was not collapsed to one mechanism.** E5's LOCK forbids reading a single-mechanism cure
   as closure; the act carries **both** admissible mechanisms and the stamping bar carries the lock.
4. **No re-homing was invented.** Row 3 carries **no F.W6 gate** because §3 opens none, and row 4 carries
   none because R4 sent the act away; both are stated **in the row**, since a seat reading only spec §2.8 /
   §2.9 would ship them here.
5. **No landing is claimed.** §3's split verdict is obeyed to the letter: **FW6-G6 and FW6-G7 are reported
   RED with the commissioned act installed**, exactly as units `a`, `b` and `c` reported theirs.

▲ **Notation, per unit `b`'s standing lesson and unit `c`'s restatement**: this unit publishes **no count of
the register's own beside-markers** — *"a figure over a token whose every mention moves it has no fixed
point"*. The register's countable operand is the `id` column, which FW6-G17 greps; these four rows add
**four** ids to it, three booked and **one entered ruled-and-routed with no act**, and that distinction is
written **in the row** rather than left to the close seat to infer.

---

## §2 Unit stamps

| unit | gates turned | commit | stamp |
|---|---|---|---|
| **a** | FW6-G19 (2nd conjunct) · FW6-G18 (roster carried) | *(this commit)* | **CLOSED 2026-09-17** |
| **b** | FW6-G1 · FW6-G2 · FW6-G3 · FW6-G4 — **all four RED with the commissioned act installed** (§3's split verdict; no landing claimed) | *(this commit — the **ONE COMMIT** family of §2.1's head: F-α ⊕ F-β ⊕ SS-C-1 land together)* | **CLOSED 2026-09-17** |
| **c** | FW6-G5 · FW6-G11 — **both RED with the commissioned act installed** (§3's split verdict; no landing claimed) | *(this commit — the **ONE PRIVACY LIMB** of §2.2 ⊕ §4 lock 3: F-γ ⊕ FR-AFP-4 land together, because a redacted breadcrumb over a world-readable image leaf has not cured the leak)* | **CLOSED 2026-09-17** |
| **d** | FW6-G6 · FW6-G7 — **both RED with the commissioned act installed** (§3's split verdict; no landing claimed). ⊘ **Two of this unit's four rows turn NO gate**: §2.8's born-visibility/lineage row has none in §3 (its GREEN condition is F.W5's `G5`; evidence read by FW6-G17), and §2.9's `FR-GFC-3` is **ruled-and-routed under F-SS4REST R4 — no F.W6 act, no F.W6 gate** | *(this commit)* | **CLOSED 2026-09-17** |
