SERVED MODEL: claude-opus-5[1m]

# F.W6 — BURN REGISTER (Track C · X·F) — the wave's evidence spine

**Opened by unit `a`, 2026-09-17.** Authority: `docs/tranches/X/fourier/waves/F-W6.md` **§1 bounds row 2** —
*"CREATE — one row per burned identity: banked id · gate · commissioned act · fourier-side landing evidence.
**FW6-G17 greps its id column**; without it the carry-closure gate is prose."* This file is that operand.

**Standing conditions on every row below, stated once.**

- `/Users/mkbabb/Programming/fourier-analysis` is **READ-ONLY, always** (spec §1's NOT-in-bounds row; §4's
  last cross-edge). Every act recorded here is a **commissioned ask** — a register row plus its line in unit
  `i`'s courier letter. **Zero fourier bytes move in this wave.** Reads are permitted and are what D-19
  requires; writes are a halt condition.
- **F.W5 STATES, F.W6 BURNS** (§4 lock 2, two homes, quoted separately at §0.3 below). A row credited here
  **and** to F.W1 / F.W3 / F.W4 / F.W5 / F.W8 fails **FW6-G17**.
- **Record-qualification (R-5 / §4 lock 10)**: the ten colliding tokens `M-13 · L-B1 · L-M3 · C-17 · C-18 ·
  B-1 · B-2 · C-2 · M-10 · M-9` are written **with their record** in every row, so no set-difference
  collapses two banked identities into one hit.
- **E-3**: the spec, the adjudicated registry, the census and F.W0's receipt are IMMUTABLE. Every correction
  in this file is a dated **addendum-beside** — a moved coordinate is recorded **beside** the spec's, never
  over it, and the spec is not edited.
- **Append-only**: units `b`–`i` append their rows to §1; no seat rewrites another's row.

---

## §0 Preconditions — the three consumptions and the census pin

### §0.1 FW6-G19's discharge — F.W0's re-grounding receipt, and the anchor re-resolution

**FW6-G19's GREEN (spec §3, gate `FW6-G19`)**: *"F.W0's re-grounding receipt exists and **every anchor cited
by this wave is re-resolved against it**. **If F.W0 fails to re-ground, the wave HALTS — it does not proceed
on stale bytes**"* — owner cell *"F.W0; F.W6 cites"*. The first conjunct was GREEN before this unit opened
(banked at the wave record's baseline, D-4); **the second conjunct is this section**.

**The receipt, named**: `/Users/mkbabb/Programming/fourier-analysis/docs/tranches/F/SUBSTRATE-LEDGER.md` —
⟨cmd⟩ `ls -l $F/docs/tranches/F/SUBSTRATE-LEDGER.md` → **201,985 B, Sep 17 15:03**. Its two tables are cited
**by gate id, never by line**, exactly as §4's F.W0 cross-edge orders (*"Cited hereafter as F-W0 §4 G-11 /
§4 G-12 — by gate id, never by line"*) — ⟨cmd⟩ `/usr/bin/grep -nE "^### G-11 —|^### G-12 —" waves/F-W0.md` →
`364:### G-11 — ONE corrected anchor table published; every later wave quotes it` ·
`369:### G-12 — ONE corrected-denominator table published; superseded figures FORBIDDEN downstream`
(*the line numbers are the command's **output**, never this file's address for them*).

**F.W0 re-grounded. The wave does not halt.** ⟨cmd⟩ `git -C $F rev-parse --short=8 HEAD` → **`3bac3d52`** ·
⟨cmd⟩ `git -C $F status --porcelain | wc -l` → **0**. The spec's born-RED witness figures (`cd26c653`, **28**
dirty paths, GAB-13, the unresolvable census pin `14d83356`) are **F.W5-authoring-pass readings and no longer
reproduce** — which is what F.W0 was for.

#### §0.1a OG-F1's narrowing, stated — quoted from COHESION §0j.D, never re-ruled

> **OG-F1** — **FREEZE-WITH-ADOPTION AND WORKTREE-AS-BASELINE** (the lane's own evidence; measurements
> live-reproduced; R6 `NO_SUCCESSOR`). Consequences as the spec states them: G-1's minute becomes a
> disclosure + the LAND set; **G-11 narrows to drift-correction**; G-14's R-coordinate rows become records;
> **GAB-13 discharges to a disclosure line.**

> **OG-F2 / OG-V2** — **CODEX-ERA-SPECIFIC.** The pre-write root-absence-receipt requirement and the
> Codex-authored prohibitions died with M-15's abrogation (2026-07-27); the *finding* under OG-F2 (the
> absence, TRUE) stays adopted; the `FOURIER-R*` value-side files are records. G-F10-8 closes on the
> charter line.

**What the narrowing means for this wave, said plainly so no burn seat over-reads it.** G-11 is
**drift-correction only** — F.W0's own §2.1.4 says so in its own hand: *"Under **FREEZE-WITH-ADOPTION AND
WORKTREE-AS-BASELINE**, G-11 is **drift-correction only**. It does **not** re-root the R-coordinates, and it
does **not** re-derive the corpus's producer citations at a new epoch."* Two consequences:

1. **G-11's eleven-row drift register names ZERO F.W6 anchors.** Its rows are frontend/producer coordinates,
   and its row 5 (`fr-AdminAuditLog K-13`) states the boundary in its own state cell: **RECORD (api/py
   coordinates are outside F.W0's bounds)**. So G-11 corrects nothing this wave cites, and **F.W6 may not
   read that silence as "the anchors are certified."** The re-resolution is therefore performed **here**,
   read-only, under D-19 MEASURE-AT-OPEN, and published at §0.1c as this wave's own act — which is the
   narrowing's cost, stated rather than absorbed.
2. **GAB-13 is a disclosure line, not a blocker.** Recorded as one, here: *the 28-dirty-path worktree the
   spec's born-RED witnesses were taken over is gone; the audited tree is the committed tree; the F.W6
   figures that rested on it are re-measured below or re-measured at the wave record's baseline.*

**The G-11 export this wave DOES consume, by gate id**: the standing rule minted at F-W0 §4 G-11 —
*"**Producer-side evidence carries the producer COMMIT HASH, never the version string**"* (P-4). F.W6 carries
**zero** producer (glass-ui) rows — §4's SS-6 edge declares the relay **EMPTY** — so the rule binds
vacuously here and is recorded so a later seat does not read the empty relay as an omission.

#### §0.1b G-12's forbidden-figure register, checked against this wave — ZERO hits

F-W0 §4 G-12 publishes a **FORBIDDEN-FIGURE REGISTER** (*"Quoting any of these downstream is a defect against
G-12 on sight"*). Measured over the spec's settled bytes, base `$W`, double-run, engine `/usr/bin/grep`:

⟨cmd⟩ `for pat in '7 e2e' '22 remaining' '8 routes' '12 sites / 8 files' '38 sites' 'text-admin-label'
'v-html' 'a3702f' '9d6415' '14 Button' 'MetricBadge' 'tsconfig.app.json' '7.0.0'; do /usr/bin/grep -c -F
"$pat" F-W6.md; done` → **0** at every one of the thirteen. The fourteenth entry (*any array-literal
`loops[].cardinality`*) returns one `-F 'cardinality'` hit, and it is **not** the forbidden figure —
⟨cmd⟩ `/usr/bin/grep -n -F 'cardinality' F-W6.md` → `430:| fr-ContourPreview row 28 | 2 | carried at §2.6
(boundary cardinality on the write path) |`, an ordinary noun in a landing cell, no `loops[]`, no integer.

**Result: F.W6 quotes no superseded denominator.** Recorded as a measured negative, not as an assumption.

#### §0.1c THE ANCHOR RE-RESOLUTION — every anchor this wave cites, at the true bytes

**Substrate delta, measured first, because it governs how much of this is new work.** G-11's table is dated
at fourier **`8bc7736`**; HEAD is **`3bac3d52`**. ⟨cmd⟩ `git -C $F diff --name-only 8bc7736 3bac3d52 -- api
| wc -l` → **0** — **no `api/` byte moved between G-11's substrate and this wave's open**, so every server
anchor below sits on the same bytes G-11's seat read. ⟨cmd⟩ `git -C $F diff --name-only 8bc7736 3bac3d52 --
web` → **13** files (`e2e/unit/figure-dimensions.unit.ts` · `e2e/visualization-crud.spec.ts` ·
`e2e/visualization-ux.spec.ts` · `InfoCard.vue` · `ConvergenceTimeline.vue` · `HarmonicLevelGrid.vue` ·
`CanvasControlsDock.vue` · `CanvasOverlayButton.vue` · `EditorControlsDock.vue` · `lib/basis-display.ts` ·
`lib/colors.ts` · `main.ts` · `tsconfig.json`) — **not one of them is an F.W6 anchor**; they are G-10's
DELETE + the `aria-pressed` lift and the `--viz-*` cure, which are F.W0's own acts.

**Method.** Each coordinate is printed by ⟨cmd⟩ `/usr/bin/sed -n '<L>p' <rooted path>`, read-only, at
`3bac3d52`, double-run. **HOLDS** = the spec's claim is at the spec's coordinate. **HOLDS ⟨beside⟩** = the
claim is true and the coordinate needed a correction, which is recorded **beside** the spec's spelling and
**never over it** (E-3). No anchor was found false.

**A. The fourier server — `api/` (no byte moved since `8bc7736`)**

| spec's coordinate | re-resolved at `3bac3d52` | state |
|---|---|---|
| `visualizations.py:110` `_write_root_version` def | `async def _write_root_version(` | HOLDS |
| `:138-141` the hard-coded quadruple | `:138 parent_hash=None,` · `:140 root_hash=set_hash_value,` · `:141 depth=0,` | HOLDS ⟨beside: `:139` inside the span is `forked_from_hash=fork_of_hash`, which the spec does not name and which is not part of the claim⟩ |
| `:146` the only `visualization_versions` insert | `await db.visualization_versions.insert_one(version.model_dump(by_alias=True))` | HOLDS |
| `:220` create call · `:592` remix call | both `await _write_root_version(` | HOLDS |
| `:256` → `:268-270` → `:272` the unsafe-GET order | `doc = await db.visualizations.find_one({"slug": slug})` · `:269 {"$inc": {"views": 1}, "$set": {"last_accessed_at": …}}` · `body = _public_doc(doc)` | HOLDS |
| `:381-383` the bare `$set` | `:381 updates = {k: v for k, v in body.model_dump(exclude_unset=True).items() if v is not None}` · `:383 await db.visualizations.update_one({"slug": slug}, {"$set": updates})` | HOLDS |
| `:477-480` `_head_set_hash` | `:477 def _head_set_hash(doc: dict) -> str:` · `:480 return doc.get("set_hash") or atomdiff.set_hash(atomdiff.enumerate_atoms(doc))` | HOLDS |
| `:522-528` the remix tri-state idiom (the KISS lift) | `:522 if "palette_slug" in body.model_fields_set:` … `:528 child_atoms["palette_slug"] = body.palette_slug` | HOLDS |
| `:536-542` the remix no-op 422 | `:536 if not atom_ops:` | HOLDS |
| `:236` create `replay_or_record` · `:612` remix `replay_or_record` | both `return await idempotency.replay_or_record(request, _store(), f"user:{owner_slug}", _handler)` | HOLDS ⟨D-2's site confirmed: the create path already wraps the envelope⟩ |
| `:466` `_readable_or_none` def; `:507` · `:744` · `:816` · `:869` its calls | `:466 async def _readable_or_none(db, slug: str, viewer: str \| None) -> dict \| None:`; `:744`/`:816`/`:869` `doc = await _readable_or_none(db, slug, viewer)`; `:507 source = await _readable_or_none(db, slug, owner_slug)` | HOLDS |
| `:784` the bare `find_one` in the ancestor walk | `cur = await db.visualizations.find_one({"slug": parent_slug})` | HOLDS — **F-γ's witness stands** |
| `:860-889` `GET /{slug}/versions` | `:860 @router.get("/{slug}/versions")` | HOLDS ⟨beside: the file is **889** lines, so the cited span closes at EOF⟩ |
| `:493` *standalone-topology-honest* | ` ``J.W1-crud-remix §11`` (NO Mongo transaction — standalone-topology-honest):` | HOLDS |
| `:129` the compound version `_id` | `_id=f"{viz_slug}:{set_hash_value}",` | HOLDS |
| `models/visualization.py:119` required `owner_slug` | `owner_slug: str  # required, non-null (CRUD-CONTRACT §3)` | HOLDS |
| `models/visualization.py:198-210` `VisualizationUpdate`; `:207` `palette_slug` | `:198 class VisualizationUpdate(BaseModel):` · `:207 palette_slug: str \| None = None` | HOLDS |
| `models/visualization.py:221-226` the in-source tension note; `:235` the compound `_id` | `:221 ``_id`` is the content-addressed compound ``f"{viz_slug}:{set_hash}"`` …` · `:235 id: str = Field(alias="_id")  # f"{viz_slug}:{set_hash}"` | HOLDS ⟨beside: §2.10's V-β cell spells **one** filename for **two** files — `:235` is `api/models/visualization.py`, `:129` is `api/routers/visualizations.py`. Both coordinates true; the spelling is the ambiguity⟩ |
| `models/visualization.py:277` remix born `draft` | `visibility: Visibility = "draft"  # a remix child is born private (§4)` | HOLDS — **R8's fourier half already conforms** |
| `atomdiff.py:12-14` the anti-tree KISS guardrail | `:12 KISS guardrails (J.W1-crud-remix §0/§9): the atoms are a flat BAG (not a tree /` … | HOLDS — **the F-TRIE incumbent** |
| `atomdiff.py:34` `palette_slug` as atom 5; `:38` `animation_settings` in the atom set | `:34 "palette_slug",` · `:38 "active_bases", "n_harmonics", "contour_settings", "animation_settings", "palette_slug"` | HOLDS |
| `database.py:98` plain index vs `:140` unique | `:98 await _db.visualizations.create_index("content_hash")` · `:140 await _db.flags.create_index([("content_hash", 1), ("reporter_slug", 1)], unique=True)` | HOLDS — **the asymmetry is inside one file** |
| `idempotency.py:68-70` the no-header passthrough | `:68 key = request.headers.get("Idempotency-Key")` · `:69 if not key:` · `:70 return await handler()` | HOLDS ⟨beside: rooted `api/lib/crud/idempotency.py`⟩ |
| `admin.py:90-92` the non-ECMA serializer | `:92 content=json.dumps(body, default=str),` | HOLDS |
| `admin.py:110` the sole `response_model` | `"/stats", response_model=AdminStatsResponse, dependencies=[Depends(admin_required)]` | HOLDS — **1 of 13** |
| `admin.py:160-190` `set_tier` | `:160 @admin_router.put(` · `:164 async def set_tier(slug: str, body: SetTierRequest, request: Request) -> Response:` | HOLDS |
| `admin.py:435-440` the unconditional batch unfeature | `:435 elif body.action == "unfeature":` · `:438 {"$set": {"tier": "normal", "updated_at": datetime.now(UTC)}},` | HOLDS ⟨beside: the `update_many` **call opens at `:430`**, one line outside the cited span — the claim is unchanged, the span is one line short of its own verb⟩ |
| `admin.py:509` `/flagged` · `:550` the short-circuit · `:578` the K9 fallback source · `:587` `_json` · `:652` `AuditListResponse` | `:509 @admin_router.get("/flagged", dependencies=[Depends(admin_required)])` · `:550 if not flagged:` · `:578 "image_slug": doc.get("image_slug"),` · `:587 return _json({"items": items, …})` · `:652 return AuditListResponse(items=items, total=total, page=page, pages=pages)` | HOLDS — **one router, one concept, two dialects** |
| §2.4 FR-AFP-9's *"restore route live at `:430`"* | ▲ **`admin.py:430` is the batch `update_many`.** The restore route is `api/routers/visualizations.py:430` — `@router.post("/{slug}/restore")`, with `:445 result = await softdelete.restore(...)` | **HOLDS ⟨beside⟩** — the claim is TRUE at the corrected rooted coordinate; a bare `:430` inside an `admin.py` paragraph is the ambiguity, and it collides with a real `admin.py:430`. **Recorded beside, never over** |
| `janitor.py:54-58`/`:56` the sentinel · `:95` its write · `:59-99` the second writer · `:133`/`:285` two `janitor:*` actions · `:234` the reporter-keyed cascade | `:56 _JANITOR_ACTOR = "system:janitor"` · `:95 "ip_hash": _JANITOR_ACTOR,` · `:59 async def _log_janitor_audit(` · `:133 "janitor:hard_delete_visualizations",` · `:285 "janitor:prune_audit",` · `:234 flags_result = await db.flags.delete_many({"reporter_slug": {"$in": stale_slugs}})` | HOLDS ⟨beside: rooted **`api/services/janitor.py`**, not `api/lib/crud/`⟩ |
| `images.py:132` blob GET · `:145`/`:164`/`:205` the 24 h public cache · `:162-164` thumbnail | `:132 @router.get("/{imageSlug}/blob")` · `:145`/`:164` `headers={"Cache-Control": "public, max-age=86400"},` · `:205 "Cache-Control": "public, max-age=86400",` | HOLDS |
| `image_storage.py:105-128` the re-upload regeneration branch | `:105 existing = await db.images.find_one({"sha256": sha256})` · `:128 existing["thumbnail_uri"] = thumbnail_uri` | HOLDS |
| `image_storage.py:250-266` `extraction_cache_key` | `:248 def extraction_cache_key(image_sha256: str, settings: Any) -> str:` · `:250 payload = json.dumps(` · `:266 return hashlib.sha256(payload.encode()).hexdigest()` | HOLDS ⟨beside: the **def** opens at `:248`; the record's **D-1** minute (eleven keys, not ten) stands unchanged⟩ |
| `image_storage.py:285`/`:290`/`:318` the contour write | `:285 async def store_contour_asset(` · `:290 image_bounds: dict[str, Any] \| None = None,` · `:318 "preview_path": "",` | HOLDS |
| `contours.py:20-30` the editor POST | `:25 doc = await store_contour_asset(xs, ys, req.image_slug, source="editor")` | HOLDS — the call sits inside the cited span |
| `assets.py:85` (PP-DEADSEAM write 1) | **`api/models/assets.py:85`** — `preview_path: str = ""` | HOLDS ⟨beside: there is **no** `api/routers/assets.py`; ⟨cmd⟩ `find api -name 'assets.py'` → `api/models/assets.py`, one path⟩ |
| `responses.py:22` (PP-DEADSEAM write 3) | **`api/responses.py:22`** — `preview_path=asset.get("preview_path", ""),` | HOLDS ⟨beside: rooted at `api/`, not `api/schemas/`⟩ |
| `equations.py:100-106` `reconstructed_points` produced at real cost | `:100 y_recon = np.zeros_like(x_eval, dtype=complex)` — the compute is in the span | HOLDS ⟨beside: the **field** is emitted at `:114` (`"reconstructed_points": {"x": …, "y": …}`) and `:128`, and typed at `api/models/equations.py:33`. The three sites are the strike's real surface⟩ |
| `grep -rn "def order_contours" $F` | ⟨cmd⟩ `/usr/bin/grep -rn "def order_contours" . --include='*.py' \| wc -l` → **0** | HOLDS — **the TRIPWIRE's witness stands** |
| `.gitignore:53 scripts/*` | `scripts/*` | HOLDS |
| `moon.json` is live and tracked | ⟨cmd⟩ `git -C $F ls-files --error-unmatch web/src/assets/fourier-paths/moon.json` → the path | HOLDS |

**B. The fourier web tree — `web/src/` (13 files moved since `8bc7736`; none is an F.W6 anchor)**

| spec's coordinate | re-resolved at `3bac3d52` | state |
|---|---|---|
| `api.ts:138-140`/`:139` the supported-but-unsent key | `web/src/lib/api.ts:139 headers["Idempotency-Key"] = options.idempotencyKey;` | HOLDS |
| `api.ts:371-380` `createVisualization` sends none | `:372 export async function createVisualization(` | HOLDS |
| `api.ts:292-294` the unversioned thumbnail URL | `:293 return ``${BASE}/api/images/${imageSlug}/thumbnail``;` | HOLDS |
| `types.ts:256-262` `VisualizationPatch` | `web/src/lib/types.ts:256 export interface VisualizationPatch {` … `:262 }` | HOLDS |
| `gallery.ts:167` · `:223` the ETag-capture path | both `const etag = etags.get(slug) ?? (await api.getVisualization(slug)).etag;` | HOLDS |
| `gallery.ts:189-199` the hard-coded like | `:189 async function like(slug: string): …` · **`:195 const liked = true;`** | HOLDS — inside the cited span |
| `gallery.ts:207-208` `recordView`'s write-back | `:208 if (idx !== -1) entries.value[idx] = { …entries.value[idx], views: data.views };` | HOLDS |
| `workspace.ts:95-104` the `structuredClone` into every draft | `:95 const raw: WorkspaceDraft = structuredClone({` | HOLDS |
| `AdminAuditLog.vue:146` the unconditional slice | **`web/src/components/visualization/gallery/AdminAuditLog.vue:146`** — `{{ entry.ip_hash.slice(0, 10) }}` | HOLDS ⟨beside: the rooted path; a bare filename is not an address — the same class G-11 exists for⟩ |
| `GalleryCard.vue:39` the banked normaliser (§4 lock 9 WAVE-LOCK) | **`web/src/components/visualization/gallery/GalleryCard.vue:39`** — `const key = b.startsWith("fourier") ? "fourier" : b;` | HOLDS ⟨beside: the rooted path. The WAVE-LOCK's operand is live⟩ |
| `GalleryView.vue:128` *"can only add"* | **`web/src/components/visualization/GalleryView.vue:128`** — `result.liked ? s.add(hash) : s.delete(hash);` | **HOLDS ⟨beside⟩** — the anchor is exact and the gloss is a **reachability** claim, not a syntactic one: the delete arm **is present** and is unreachable only because `gallery.ts:195` hard-codes `const liked = true`. **Recorded so no seat reads the row as a syntax defect and "cures" the ternary** |

**C. The value tree — §2.10's four routed-out rows (explicitly NOT under the F.W0 lock; measured read-only)**

§2.10 states it: *"Measured read-only at the fold (value side is explicitly **not** under the F.W0 lock,
which re-grounds the fourier tree)."* Re-measured here at this seat's clock, base `$V`. **Zero bytes moved**
— ⟨cmd⟩ `git diff --stat -- api/src src` → **empty** · ⟨cmd⟩ `git status --porcelain -- api/src src | wc -l`
→ **0**.

| spec's coordinate | re-resolved 2026-09-17 | state |
|---|---|---|
| `service/versions.ts:46-58` the reference walk (V-α / F-α's cure model) | `:46 let rootHash = hash;` … `rootHash = parent.rootHash ?? parentRef;` · `depth = (parent.depth ?? 0) + 1;` | HOLDS ⟨beside: the live form is `(parent.depth ?? 0) + 1`; the spec's `parent.depth + 1` is that expression without its nullish guard⟩ |
| `service/versions.ts:38` · `:40-44` the early return | `:38 const hash = computeContentHash(name, colors);` · `:44 if (existing) return hash;` | HOLDS |
| `service/versions.ts:121-181` · `:137` · `:148` · `:172` (V-α) | `:121 export async function revertToVersion(` · `:137 const newHash = computeContentHash(version.name, version.colors);` · `:148 await createVersionRecord(` · `:172 $inc: { versionCount: 1 },` | HOLDS |
| `service/forks.ts:64` · `:76` (R8's value half) | `:64 const colors = source.colors;` · `:76 visibility: "public",` | HOLDS |
| `service/forks.ts:167-179` the redaction counter-example (F-γ's adoption target) | `:167 * ordinal (V·W45 item 4). No raw document or lineage field (userSlug,` · `:171 export type ProvenanceStep =` · `:179 \| { kind: "unavailable"; ordinal: number };` | HOLDS |
| `model.ts:60` · `:85-86` (V-β / V-γ) | `:60 userSlug: string \| null;` · `:85 /** _id is the content-hash. */` · `:86 _id: string;` | HOLDS |
| `hash.ts:8-17` · `:41-42` (V-β / TA-4) | `:8 export function computeContentHash(name: string, colors: PaletteColor[]): string {` · `:42 atom-diff algebra that once also consumed it were excised at T.W1 — TA-4.)` | HOLDS |
| `repository/paletteVersion.ts:13-15` · `:44-47` (V-β) | `:13 findByHash(hash: string, session?: ClientSession): …` · `:47 if (existing) return version._id;` | HOLDS |
| `format.ts:45` · `:87` (TA-4's surviving wire field) | `:45 atomSetHash: string;` · `:87 atomSetHash: computeAtomSetHash(rest.colors),` | HOLDS |
| `service/crud.ts:119` · `:204` the conditional attribution (V-γ) | `:119 await createVersionRecord(` · `:204 if (contentChanged && userSlug) {` | HOLDS ⟨beside: the `if (userSlug)` guard fronting `:119` sits at **`:118`**; the spec cites the write, the guard is the line above it⟩ |
| `__tests__/palette-versions.test.ts:83` the suite's only revert case | `it("revertToVersion throws NotFoundError on missing palette", async () => {` | HOLDS |

**FW6-G19's second conjunct is discharged.** Every anchor this wave cites is re-resolved at the true bytes
against F.W0's receipt, by gate id, and **not one anchor is false**. Every correction is recorded **beside**
its spec spelling under E-3 and **none is written over it**.

**The corrections, self-counted over the settled bytes of this file.** ▲ **SELF-COUNT LAW, discharged rather
than dodged: the probe matches its own receipt.** ⟨cmd⟩ `/usr/bin/grep -c '⟨beside' burn-register.md` →
**18**, double-run **18** — of which **one is the notation's own definition in the Method paragraph above**
and **one is this receipt's own command text, three lines up** — so **SIXTEEN table rows carry a
beside-note**. (A figure published as *"17"* here before the arithmetic was disclosed would have been a
count of a file including the line that took the count; it is corrected in place before the file settles,
which is the only moment such a correction is not an erratum.)

1. **SEVEN ROOTINGS** — a bare filename resolved to its true path, the line holding exactly:
   `idempotency.py` → `api/lib/crud/` · `janitor.py` → `api/services/` · `assets.py` → `api/models/` ·
   `responses.py` → `api/` · `AdminAuditLog.vue` · `GalleryCard.vue` · `GalleryView.vue` →
   `web/src/components/visualization/…`. **A bare filename is not an address** — the class G-11 exists for,
   met inside this wave's own citation set.
2. **ONE COLLISION** — §2.4 FR-AFP-9's *"restore route live at `:430`"*: the **only** coordinate that
   resolves to a live **different** statement under the file its own paragraph names (`admin.py:430` is the
   batch `update_many`; the restore route is `visualizations.py:430`). The claim is true at the corrected
   coordinate; the spelling is what would have misled a burn seat.
3. **ONE SPLIT SPELLING** — §2.10's V-β cell writes `visualization.py:235, :129` for **two** files
   (`api/models/visualization.py` and `api/routers/visualizations.py`). Both coordinates true.
4. **FIVE EXTENT DISCLOSURES** — `:138-141`'s unnamed `:139` · `:860-889` closing at EOF ·
   `admin.py:435-440` opening one line after its own `update_many` verb · `extraction_cache_key`'s def at
   `:248` · `equations.py:100-106`'s field emitted at `:114`/`:128`.
5. **TWO READING CORRECTIONS** — `service/versions.ts`'s live `(parent.depth ?? 0) + 1` against the spec's
   `parent.depth + 1`, and `service/crud.ts`'s `if (userSlug)` guard sitting at `:118` against a write cited
   at `:119`. ▲ A **third** reading correction rides row 1's `GalleryView.vue:128` and is counted there,
   not twice: *"can only add"* is a **reachability** claim, and the reachability's falsifier is
   `gallery.ts:195`, not the ternary at `:128`.

**7 ⊕ 1 ⊕ 1 ⊕ 5 ⊕ 2 = 16**, which is the measured row figure above.
**No anchor of this wave is cited by line into a live sibling spec** — the sibling citations at §0.3 below
are by clause id and gate id, which is the form §4's F.W0 edge makes law.

### §0.2 FW6-G18's roster — R1–R9 ⊕ OG-F1 / OG-F2, RULED at COHESION §0j.D

**FW6-G18's GREEN (spec §3)**: *"Each conditioned row either carries its ruling or is explicitly deferred
with its honest default recorded. **F.W6 may not burn a row conditioned on an unruled question — the whole
FR-AFP band rides R3/G11**"*; owner cell: *"owner → F.W5 unit e; **F.W6 consumes, never re-rules**."*

**The rulings exist.** ⟨cmd⟩ `ls -l …/contract/OWNER-RULINGS-F.W5.md` → **18,702 B, Sep 17 18:16** · ⟨cmd⟩
`/usr/bin/grep -c '^| \*\*R[1-9]\*\*' OWNER-RULINGS-F.W5.md` → **9** (double-run: 9). **The authority of
record for what each ruling SAYS is `COHESION.md` §0j.D**, and every quotation below is from it. **This seat
rules nothing, re-opens nothing, and pre-answers nothing.**

| ruling | id at §0j.D | the ruling, quoted | the F.W6 rows it conditions — **and its homing** |
|---|---|---|---|
| **R1** / TA-4 | **F-SS4REST R1** | *"**R1 (TA-4)**: **RE-SCOPE value.js out of the diff clause** (a one-sided §6 verdict, stated explicitly) — `atomdiff.ts` is wholly excised from value.js and restoring it is value-side authoring that would couple value.js's release train to the fourier contract"* | §2.10's **⊙ TA-4** row, read by **FW6-G16** at unit `i`. **Homing unchanged (routed OUT).** The row is entered as **RULED RE-SCOPE**, never as a restoration ask, and the spec's *"UNFALSIFIABLE until G4 closes"* consequence is **discharged by the ruling** and stated as discharged |
| **R2** / trie | **F-TRIE (R2 ≡ E16 ≡ G7 ≡ G-F7-1)** | *"**NO TRIE**; whole-snapshot duplication is the recorded shipped behaviour (the honest default; `atomdiff.py:12-14` is the guardrail; zero material on either tree)"* | unit **`h`** (PP-DEADSEAM ⊕ `fr-EquationModeToggle FR-EMT-20` cited). **§4 lock 8's hold is DISCHARGED** — the `trace` / `reconstructed_points` strikes no longer wait on a compression decision. ▲ **No trie is designed here**: §5's exclusion of F.W7's compression design **stands**, and `atomdiff.py:12-14` remains the incumbent guardrail |
| **R3** / producer-or-retire | **F-PRODRET (R3 ≡ D3 ≡ G11)** | *"**PRODUCER**, as a **port** of value.js's shipped verb (`POST /:slug/flag`) under the SS-4 contract, homed at **F.W8** (the CRUD union prototype) with F.W5 writing the clause; FR-AFP grades re-derive at the populated surface"* | **THE ADMISSION GATE for the whole FR-AFP band (§4 lock 4).** The band is **ADMITTED, not retired** → unit **`e`** opens. ▲ **RE-HOMED, and said here because a seat reading only §2.4 would ship it in this wave**: §2.4's branch *"If PRODUCER: F.W6 ships the flag-write operation"* is **SUPERSEDED ON THE HOMING** — **the port is F.W8's act**. F.W6 records FR-AFP-1 as **ADMITTED-and-ROUTED-to-F.W8**, naming value.js's `POST /:slug/flag` as the reference implementation for that wave, and **ships no flag-write operation**. The band's other server rows (FR-AFP-66 · -7 · -33 · -36/D17 · FR-GV-9) remain F.W6's as written |
| **R4** / the like verb | **F-SS4REST R4** | *"**R4**: **REMOVE the affordance** (no dead affordance under `aria-pressed`)"* | §2.9's **`fr-GFC-3` ⊙** row → unit **`d`**. ▲ **RE-HOMED**: by §2.9's **own** branch — *"If DELETE: F.W4's."* — **F.W6 books NO server act**. The row is entered as **ruled-and-routed**; the `aria-pressed` + re-click guard is F.W3/W4's regardless |
| **R5** / off-state `[]` | **F-SS4REST R5** | *"**R5**: **STOP MINTING** the off-state `[]`; the contract does not admit it (no silent rewrite)"* | **load-bearing at FW6-G18** (the gate names R5 and R9 as *"the two dropped"*). It gates `fr-BasisSelector M-9` (record-qualified; §2.11 row 7) — a **cited** row, not a booked one. **No F.W6 booking**; carried here so the gate's roster is whole |
| **R6** / hard-delete arm | **F-SS4REST R6** | *"**R6**: **KEEP** the hard-delete arm; copy made truthful about irreversibility"* | §2.4's **FR-AFP-66** → unit **`e`**. The cascade is burned **as a build, not as a deletion** (the RETIRE branch of §2.4's sequencing cell — *"if RETIRE, the cascade is deleted with the band rather than built"* — does not fire). The truthful-copy arm is **F.W3/W4's** and is emitted, not claimed |
| **R7** / codegen | **F-SS4REST R7** | *"**R7**: **CODEGEN** — twins derived from one source (inv-16/inv-26 restated; structural invariants over prose)"* | §2.4's **FR-AFP-36 ⊙ + the D17 serializer clause** → unit **`e`**, gate **FW6-G12**. The ⊙ is **LIFTED**: one generated/checked shape is now admissible, and FW6-G12's *"⊙ No codegen without FR-AFP-71's ruling"* is satisfied by this ruling. ▲ **K9 survives the lift: the five `?? item.slug` fallbacks STAY** |
| **R8** / born visibility | **F-SS4REST R8** | *"**R8**: **REMIX + BORN-PRIVATE** (value.js's shipped verb and its explicit publish act), the F.W5 seat verifying non-contradiction with ruling D9 at the record before authoring"* | §2.8's **R-5 / G5 ⊕ E4** row → unit **`d`**. **The fourier half already conforms** — re-resolved above at `models/visualization.py:277` (`visibility: Visibility = "draft"`). What F.W6 burns is the **"no `fork_of` recorded"** server arm plus E4's create-visibility test. ▲ The **value half** (`forks.ts:76` `visibility: "public"`) routes **OUT** at unit `i`; **GCM-1's `/v/` routing cure is F.W4's** |
| **R9** / dead session subsystem | **F-SS4REST R9** | *"**R9**: **DELETE** the dead session subsystem (zero external call sites)"* | **load-bearing at FW6-G18.** ▲ **RE-HOMED — and the gate says so in its own words**: *"R9's ⊙ stays ATTACHED to FR-USB-23 on its F.W8 routing row (§5)."* The ⊙ travels **with** `FR-USB-23` to **F.W8**; **F.W6 neither wires nor deletes it** and books no row |
| **OG-F1** | §0j.D | quoted whole at **§0.1a** above | **G-11 narrows to drift-correction** (→ §0.1a/§0.1c: this wave performs its own re-resolution and says so); **GAB-13 discharges to a disclosure line** (→ §0.1a). Consumed by unit `a`; **re-derived by nobody** |
| **OG-F2 / OG-V2** | §0j.D | quoted whole at **§0.1a** above | **CODEX-ERA-SPECIFIC**: the pre-write root-absence receipt died with M-15's abrogation; the **finding** stays adopted. No F.W6 row rests on the retired requirement |
| **G-15(c)** / FM-19 | §0j.D **G-15** | *"(c) FM-19 → **FROZEN-FOREVER with a golden-file diff** (no regeneration pipeline exists; building one is unsized work)"* | unit **`g`**, gate **FW6-G15** — the **canonical-geometry TRIPWIRE**, which *"stays RED by design"*. The frozen-asset instrument is ruled; **DO-NOT-REGENERATE on `master` is untouched by it**, and an attempt remains a gate FAILURE reviving `fr-FourierShapeExtractor L-B1` + L-B2/`fr-ContourEditorCanvas C-2` at BLOCKER |

**The roster is whole: nine rulings ⊕ OG-F1 / OG-F2 ⊕ G-15(c), each quoted with its ruling id, each mapped
to the rows it conditions.** Three rulings **re-home** a row away from this wave (**R3** → the port at F.W8 ·
**R4** → the affordance at F.W4 · **R9** → FR-USB-23's ⊙ at F.W8) and each re-homing is stated **in its own
row**, because a burn seat reading only §2 would ship it here. **No ruling is re-ruled, none is
pre-answered, and no honest default is substituted for a ruling that exists.**

### §0.3 The F.W5 clause set, consumed by clause id — F.W6 authors no clause

The set exists whole (spec §1 r6, *cited-when-created*) — ⟨cmd⟩ `ls -l …/contract/` → `J-diff-shape-v2.md`
**244,274 B** · `operation-register.md` **38,255 B** · `OWNER-RULINGS-F.W5.md` **18,702 B**.

**The v2 clause register, enumerated by id** — ⟨cmd⟩ `/usr/bin/grep -ohE '^### [A-G][0-9]+c? ' J-diff-shape-v2.md`
(double-run, identical):

`A1 A2 A3 A4 A5 A6` · `B1 B2 B3 B4 B5` · `C1 C2 C3 C4 C5` · `D1 D2 D3 D4 D5 D6 D7 D8 D10 D11 D12 D13 D14
D15 D16 D17` · `E1 E2 E3 E4 E5 E6 E7 E8 E9 E10 E11 E12 E13 E14 E15 E16 E17 E18 E19 E20` · `F1 F2 F3 F4 F5
F6 F7 F8 F9` · `G1c G2c G3c G4c G5c G6c G7c G8c G9c G10c` — ⟨cmd⟩
`/usr/bin/grep -cE '^### [A-G][0-9]+c? (—|⊙)' J-diff-shape-v2.md` → **71** (double-run: 71).

▲ **`D9` is absent from the register and that absence is the record's own** — ⟨cmd⟩
`/usr/bin/grep -cE '^### D9 ' J-diff-shape-v2.md` → **0**. F-W5 states it in its own hand (*"No clause is
numbered D9 — see the compatibility note."*); `D9` is a **value.js product ruling**, not a v2 clause, and
the actor clause is **E18**. Carried so no seat re-mints the invented clause the spec retired at R4-9.3.

**The register and the ruling block**: ⟨cmd⟩ `/usr/bin/grep -c '^| ' operation-register.md` → **114** rows
(the 45 operation rows plus headers and legend) · ⟨cmd⟩ `/usr/bin/grep -c '^| \*\*R[1-9]\*\*'
OWNER-RULINGS-F.W5.md` → **9**.

**The clauses this wave BURNS** (green owner F.W6 / the fourier API row, per §4's F.W5 cross-edge), each
verified present in v2 by ⟨cmd⟩ `/usr/bin/grep -cE "^### <id> (—|⊙)"` → **1** at every id:

`C1` · `C2` · `C5` · `D5` · `D6` · `D8` · `D10` · `D11` · `D12` · `D16` · `D17` · `E2` · `E5` · `E6` · `E7` ·
`E13` · `E14` · `E17` · `E18` · `B4`/`E10` · `G1c`–`G10c`, with `D2` / `D3` **conditionally** (both now
ruled — `D2` by **R4 = REMOVE**, `D3` by **R3 = PRODUCER**, §0.2).

**The clauses this wave CITES and does not own** (enumerated per identity at spec §2.11 / §2.11c / §2.11d):
`A3` · `A4` · `A5` · `A6` · `B1` · `B2` · `C3` · `C4` · `D7` · `E4` · `E8` · `E9` · `E11` · `E12` · `E15` ·
`E20` · `F1`–`F9` · `G3c` / `G4c` / `G8c` / `G9c`.

**The per-unit clause map**, so each burn seat reads its own stating clause and no other:

| unit | stating clauses it burns | the clause's own words, where the burn hinges on them |
|---|---|---|
| `b` | **E2** (F-α) · **E7** (F-β ⊕ SS-C-1 write leg) | E2 carries the split: *"F.W5 owns the CLAUSE; F.W6 owns the burn-down — do NOT double-book."* **E7 is stated but UNGATED across F.W5's G1–G22 (+ §G G1c–G10c)** — FW6-G2/G3/G4 exist for exactly that, and the omission is **not** read as absence of scope |
| `c` | **C5** (redaction parity, F-γ) · **C1**/**C2** (image remediation, FR-AFP-4) | C5 is **stated**, so the redact branch is available — **cited, not re-chosen.** C2 states the image contract as **ONE** unit: delete/quarantine op · blob **and** thumbnail gate · janitor predicate · versioned thumbnail URL |
| `d` | **E6** (FR-GV-12 ⊕ FR-GV-24 ⊕ VV-R2-A) · **E4** (born visibility + lineage) | E6 carries the repair-test lock: **no re-open-increment assertion**. E4 is R8's clause. `fr-GalleryDraftsSection B-2`'s server arm closes **STATICALLY** (K12) |
| `e` | **D4**/**D5** (flag identity + which identifier renders) · **D6** (lifecycle truth and cascade, R6) · **D17** (serializer) · **A4** ⊙ (one contract source, now R7 = CODEGEN) | ▲ **D6, not "E7"** and ▲ **D5, not "D12"** — the R-1e re-keys, carried so no seat re-mints the first pass's labels |
| `f` | **E18** (the actor is a FIELD) · **D16** (the shared action taxonomy) | E18's sentence is the commissioned act. ▲ **AA-10 is CITED to F.W4, never booked**; **AA-23 is a kill-only carry**, host row held at F.W5 |
| `g` | **E13** (cache identity ⊇ consumed fields) · **E14** (editor contour is first class) · **E17** (image bounds on write) · **§G G1c** (RE-AUTHOR, at F.W6, not before) with **G5c** as its **PRECONDITION** | ▲ **E13, not "E12"** · **E14/E17, not "E11"** (R-1e). G1c's act closes **only** by naming the true source or re-authoring under a repaired, tracked, runnable pipeline — **never by regenerating** |
| `h` | **B4**/**E10** (ONE disposition for produced-and-unconsumed) | E10 is **ONE** stated disposition, never three ad-hoc deletions. `FR-EMT-20`'s host is **F.W5**; this wave executes the mechanism and **cites the holder** |
| `i` | none — the closure unit authors no clause | it reads **FW6-G16**'s two authorities and **FW6-G17**'s enumeration |

▲ **E-3, stated for the record**: **F.W6 CONSUMES these clauses and authors none.** The three contract files
are read-only to this wave (spec §1's NOT-in-bounds row); a correction to any of them is F.W5's seat's, by
dated addendum, never this wave's edit.

### §0.4 The census pin — the sole operand, and the band's record-side figure

**Frozen and character-matched** — ⟨cmd⟩ `shasum -a 256 …/conformance/CENSUS-CANONICAL.md | cut -c1-12` →
**`f44362757458`**, the value the spec pins at §0.3's hash table. `CENSUS-CANONICAL.md` is **THE** census of
record (spec §0.1); **a gate citing any other census operand is DEFECTIVE on sight**, and the struck
**132 / 111 / 153** are inadmissible.

⟨cmd⟩ `/usr/bin/grep -nE '^### F\.W5(-W8)? — ' "$C"` → `5078:### F.W5 — **27 rows**` ·
`5092:### F.W5-W8 — **89 rows**` ⟨*the line numbers are the command's output; the census is cited by heading*⟩.

> **27 ⊕ 89 = 116 record-side rows in the band — and ZERO at F.W6.**

⟨cmd⟩ `/usr/bin/grep -o 'F\.W6., .F\.W8. and .F\.W10. are likewise named by \*\*no record\*\*' "$C"` →
*"F.W6`, `F.W8` and `F.W10` are likewise named by **no record**"* ⟨*the paste begins where the pattern
begins; the record's opening backtick is outside it, and a leading backtick added to make the quotation look
whole would be the prepend defect the spec cures at §2.11 row 21*⟩.

▲ **NO ROSTER INTEGER IS RE-DERIVED HERE.** **116 is the canonical's**, quoted, not computed by this wave;
**this wave's own population is folds, gate operands and commissioned acts**, enumerated by register at
§1 below and **summed into nothing**. Because the canonical homes **ZERO** record-side rows at F.W6, a
record-side escape is impossible by construction — **what FW6-G17's reverse direction tests at unit `i` is
fabrication**, not omission.

---

## §1 The register — one row per burned identity

**FW6-G17 greps the `id` column of this table.** Units `b`–`i` append; **`a` opens it and books nothing**
(this unit's product is §0, which is FW6-G18's and FW6-G19's operand, not a burned identity).

Every row carries, in order: the **banked id, record-qualified** (R-5) · the **gate** it turns · the
**commissioned act** (a fourier-side ask, never a value.js edit) · the **F.W5 clause** that states it · the
**fourier-side landing evidence**, which stays *pending* until the sub-session lands it under COMMISSION §2.

| id | gate | commissioned act | stating clause | fourier-side landing evidence |
|---|---|---|---|---|
| *(unit `a` books none — see §0)* | FW6-G18 · FW6-G19 | — | — | — |
| **F-α** — the version chain (spec §2.1; P1, census risk 5). ▲ **Disposition UNRULED — FLAGGED INLINE, never presumed** | **FW6-G1** | **WITNESS, re-measured at this seat's clock** (2026-09-17, fourier `3bac3d52`, working tree clean): `_write_root_version` has **3** source sites — `api/routers/visualizations.py:110` def · `:220` create · `:592` remix — and is **the only writer**; the constructor hard-codes the quadruple at `:138 parent_hash=None,` · `:140 root_hash=set_hash_value,` · `:141 depth=0,`; `visualization_versions` has **8** non-test sites (1 insert `:146` · 2 finds `:749`/`:874` · 1 `find_one` `:822` · 2 migration-script sites · 2 index creations at `api/services/database.py:132-133`). `GET /{slug}/versions` opens at `:860` and its own docstring concedes the shape — *"The viz's own version chain, ``depth``-ordered, bounded ≤50"* — over a collection **every row of which is born `depth=0`**, so the depth sort orders a permanent singleton and all real lineage rides cross-viz `fork_of`. ⟶ **COMMISSIONED ACT (fourier-side; never a value.js edit)**: execute v2 **E2 / F.W5 G3**'s ***deepen-or-retire*** on the depth/parent/root quadruple — **one of the two named branches, never a third**: **(i) DEEPEN** — walk the parent before the version write exactly as value.js already ships (`rootHash = parent.rootHash ?? parentRef; depth = (parent.depth ?? 0) + 1`, the reference walk at `api/src/modules/palette/service/versions.ts:46-58` ⟨b-note: rooted here; the spec spells it `service/versions.ts:46-58`, and the live form carries the nullish guard the spec's `parent.depth + 1` omits — both recorded by unit `a` at §0.1c and not re-derived⟩); **(ii) RETIRE** — remove `depth` / `parent_hash` / `root_hash` from the version model and the two `sort("depth", 1)` readers with them. **Landing falsifier is FW6-G1's own**: *"a fixture asserts either a depth-1 version exists with a non-null `parent_hash`, or the depth/parent/root quadruple is gone from the model"*. ▲ **THE CHOICE IS NOT THIS SEAT'S.** COHESION §0j.D rules R1–R9 ⊕ OG-F1 / OG-F2 ⊕ G-15 and **none of them reaches this quadruple** (§0.2's roster maps R1→`i`, R2→`h`, R3→`e`, R4→`d`, R6/R7→`e`, R8→`d`; R5/R9 are carried, not booked — **unit `b` is unconditioned**). **Dissent of record, carried whole and not resolved here** (spec §2.1: *"retire-vs-deepen is genuinely open"*): fourier's own board books the RETIRE branch — `docs/tranches/M/M.md` **§7**'s terminal-verdict fold row *"Phantom within-viz version chain (depth always 0) \| J \| **DELETE** (W2-transpose; net code decrease, inv-3) \| W10"* and the **M.W10** wave row's *"W2-transpose: DELETE the phantom within-viz version chain; keep the per-viz atom `/diff` + cross-viz `fork_of`"* — while the seam table frames value.js as the side that deepens. F.W6 **commissions the execution and names both branches; the disposition rides unit `i`'s letter as its first ask** | **E2** (`J-diff-shape-v2.md`), the split's own home — *"F.W5 owns the CLAUSE; F.W6 owns the burn-down — do NOT double-book."* Gate owner **F.W6**, named by F.W5 **G3**'s GREEN-owner cell (*"F.W5 owns the CLAUSE only — no double-booking"* — the two homes quoted separately, never fused). **No F.W1 / F.W3 / F.W4 / F.W5 / F.W8 credit is claimed**, both directions of FW6-G17 | **PENDING.** Zero fourier bytes at this seat — HEAD `3bac3d52` unmoved, `git -C $F status --porcelain \| wc -l` → **0** before and after. Lands under COMMISSION §2 through unit `i`'s courier letter `value-to-fourier-2026-09-17-provenance-burndown-F.W6.md`; **the ruling must precede the landing** (a seat that lands a branch before the disposition is ruled has resolved an open question by shipping) |
| **F-β** — the PATCH `set_hash` / version write, and atom 5's tri-state (spec §2.1; P1, chain + stale advertised hash) | **FW6-G2** · **FW6-G3** | **WITNESS, the PATCH handler read whole at this seat's clock** (`/usr/bin/sed -n '355,392p' api/routers/visualizations.py`): after `resolve_session`, the ownership check, `etag.require_if_match` and the visibility-transition guard, the body is **three statements** — `:381 updates = {k: v for k, v in body.model_dump(exclude_unset=True).items() if v is not None}` · `:382 updates["updated_at"] = datetime.now(UTC)` · `:383 await db.visualizations.update_one({"slug": slug}, {"$set": updates})` — **a bare `$set`: no `set_hash` recompute, no `content_hash` recompute, and no `_write_root_version` call** (that writer's three sites are def / create / remix only, enumerated in the F-α row above). `_head_set_hash` at `:480` returns `doc.get("set_hash") or atomdiff.set_hash(atomdiff.enumerate_atoms(doc))` — **the STORED value whenever non-empty** — so the moment atom 5 moves under PATCH the advertised hash stops describing the bag, and `/diff` + `/provenance` report the stale value. `palette_slug` is **atom 5-of-5** (`api/lib/crud/atomdiff.py:34`) **and** a PATCH field (`api/models/visualization.py:207`). ⚑ **Witness sharpened at the bytes, measured not inferred**: `VisualizationUpdate`'s own docstring (`:199-200`) states *"``slug`` is immutable and ``content_hash`` is recomputed server-side (so neither appears here)"* — **the recompute the handler does not perform**; the in-source claim is part of the defect surface and a cure that leaves it standing has cured half. FW6-G3's arm: the same `if v is not None` comprehension **discards a null**, so PATCH can never clear `palette_slug`, while the remix arm twenty lines away already tri-states it — `:522 if "palette_slug" in body.model_fields_set:` … `:528 child_atoms["palette_slug"] = body.palette_slug`, under the in-source comment *"tri-state (F-08): null CLEARS the binding (atom removed from the bag); a slug rebinds; an omitted field inherits the source's."* ⟶ **COMMISSIONED ACT**: on **every atom-touching PATCH** — (1) recompute `set_hash` from `enumerate_atoms(doc)` over the post-write document and persist it, (2) **write the version row** through the existing `_write_root_version` path (or its deepened successor, per F-α's ruled branch), and (3) make `palette_slug` tri-state **by LIFTING the remix arm's `model_fields_set` idiom at `:522-528`** — omit inherits · null clears · slug rebinds. ▲ **KISS lock, binding (spec §2.1): the idiom ships in the same router twenty lines away — LIFT it, do NOT invent one**; two spellings of one tri-state in one router is the defect running the other way. **Landing falsifiers, the gates' own**: FW6-G2 — *"A fixture PATCHes atom 5 and asserts (a) `set_hash == set_hash(enumerate_atoms(doc))` after the write and (b) a new version row exists"*; FW6-G3 — *"Three arms tested: omit inherits, null clears, slug rebinds — **using the remix idiom, not a new one**"*. ⚑ **SECOND dissent of record, carried not resolved, surfaced because a seat reading only §2.1 would never see it**: the same **M.W10** row books ***"W2-fix: `palette_slug` remix-only"*** — a branch under which atom 5 leaves the PATCH surface altogether, the tri-state is moot, and SS-C-1's set equality resolves **by subtraction rather than by addition**. This seat resolves nothing: it records the tension and routes it to the **same ruling seat** as F-α's disposition | **E7** (`J-diff-shape-v2.md`) — *PATCH atom coverage + `set_hash` recompute*. ▲ **E7 is STATED and UNGATED**: F.W5's register runs **G1–G22 plus §G's G1c–G10c** and **none of them gates E7** (spec §2.1's sequencing cell; the first pass's "E8" is the *AnimationSettings* clause and is booked nowhere here). **FW6-G2/G3/G4 exist for exactly that omission**, which F.W6 reads as a gate gap and **not** as absence of scope | **PENDING.** Zero fourier bytes at this seat. Lands with FW6-G4 in **ONE** PATCH-model change under COMMISSION §2 |
| **SS-C-1 (write leg)** — PATCH field set vs atom set (spec §2.1; MAJOR, demoted from challenge-C's BLOCKER at component altitude). ▲ **READ leg NOT re-booked** | **FW6-G4** | **WITNESS, set against set at this seat's clock, both counts double-run identical**: the **PATCH field set is 5** — `visibility` · `title` · `description` · `tags` · `palette_slug` (`api/models/visualization.py:203-207`, sealed by `model_config = ConfigDict(extra="forbid")` at `:209`) — and its wire twin `VisualizationPatch` (`web/src/lib/types.ts:256-262`) carries **the same five**, all optional. The **atom set is 5** — `active_bases` · `n_harmonics` · `contour_settings` · `animation_settings` · `palette_slug` (`api/lib/crud/atomdiff.py:29-35` `ATOM_KEY_ORDER`, restated as the `AtomKey` Literal at `:37-39`). **Intersection = 1** (`palette_slug`) · **PATCH-only = 4** (the metadata quartet, no atom among them) · **atom-only = 4** — so `animation_settings` (atom 4-of-5; `class AnimationSettings` at `api/models/shared.py:65`, whose `speed` the control itself types at `web/src/lib/types.ts:49 speed: number;`) is settable **only at create and fork**, and a preference the control claims to set is silently non-persisted. ⟶ **COMMISSIONED ACT**: make the PATCH field set **equal** the atom set — extend `VisualizationUpdate` ⊕ `VisualizationPatch` with the four missing atoms and route them through the very recompute + version write F-β commissions — **or** state **each divergence per-atom with its reason** on the record. The gate admits either; **it does not admit silence**, and a per-atom statement is a disposition, not a deferral. ▲ **ONE-CUT LAW (spec §4 lock 3), binding: F-β ⊕ SS-C-1 land in ONE PATCH-model change — split them and the second reverts the first's invariant** (a widened field set without the recompute mints four more stale-hash paths; a recompute over a field set that reaches one atom leaves the other four unreachable). FW6-G4's own owner cell says it: **same commit as FW6-G2/G3, one PATCH-model cut**. ▲ **SPLIT RULING (R2-LC) OBSERVED**: the **READ leg** — `loadVisualization`, zero callers — **stays banked at `fr-GalleryCardModal` GCM-1 at blocker weight → F.W4 + the F.W5-W8 rider** and is **NOT re-booked here**; re-merging the axes re-books GCM-1. Only the write leg is un-banked, and only the write leg is in this row | **E7** (shared with F-β — **one clause, two legs**; F.W6 books the **write** leg only). The read leg's stating home travels with GCM-1 to F.W4 and is **cited, never claimed** (the FR-GIG-5 standing bar) | **PENDING.** Zero fourier bytes at this seat. **Must land in the same commit as F-β** — a landing that splits them fails the one-cut law and this row's evidence cell may not be stamped from a split landing |

### §1.b Unit `b`'s three rows — the receipts, the notation, and what this seat did **not** decide

**SERVED MODEL: claude-opus-5[1m]** · seat clock **2026-09-17** · base `F=/Users/mkbabb/Programming/fourier-analysis`
(**READ-ONLY**), `V=/Users/mkbabb/Programming/value.js` · engine **`/usr/bin/grep`** (§0.2's pin; bare `grep`
is ugrep in this shell). **Zero fourier bytes · zero value-tree product bytes.**

**Substrate, re-measured before any citation** (D-19 MEASURE-AT-OPEN; the F.W0 pre-gate is unit `a`'s and is
not re-litigated): ⟨cmd⟩ `git -C $F rev-parse --short=8 HEAD` → **`3bac3d52`** · ⟨cmd⟩ `git -C $F status
--porcelain | wc -l` → **0**. Unmoved from unit `a`'s reading, so **§0.1c's anchor re-resolution holds at
this seat's clock** and every anchor above is cited from it rather than re-derived.

**The counts this row set publishes, each double-run, both runs identical** (WRITE-THEN-MEASURE):

| figure | ⟨cmd⟩ | run 1 | run 2 |
|---|---|---|---|
| `_write_root_version` source sites | `/usr/bin/grep -rn "_write_root_version" $F/api --include='*.py' \| wc -l` | **3** | **3** |
| `visualization_versions` non-test sites | `/usr/bin/grep -rn "visualization_versions" $F/api --include='*.py' \| /usr/bin/grep -v tests \| wc -l` | **8** | **8** |
| PATCH fields (`VisualizationUpdate`) | `/usr/bin/sed -n '203,207p' $F/api/models/visualization.py \| /usr/bin/grep -c ':'` | **5** | **5** |
| PATCH fields (`VisualizationPatch`, the wire twin) | `/usr/bin/sed -n '257,261p' $F/web/src/lib/types.ts \| /usr/bin/grep -c '?:'` | **5** | **5** |
| atoms (`ATOM_KEY_ORDER`) | `/usr/bin/sed -n '30,34p' $F/api/lib/crud/atomdiff.py \| /usr/bin/grep -c '"'` | **5** | **5** |

▲ **One divergence of FORM, disclosed rather than smoothed**: the unscoped `grep -rn "_write_root_version"
$F/api` the spec's FW6-G1 witness spells also prints a **binary match line** for
`api/routers/__pycache__/visualizations.cpython-314.pyc`. The figure published above is `--include='*.py'`
**source** sites — **3**, the same three the baseline row read. The `.pyc` is build output, is not a site,
and is named here so a later seat re-running the spec's own spelling is not surprised by a fourth line.

**Notation, and why this seat mints neither the marker nor its literal.** §0.1c publishes a SELF-COUNT over
the settled bytes of **this file**: its `-c` probe over unit `a`'s beside-marker reads **18**, of which two
are the notation's own definition and the receipt's own command text, *"so **SIXTEEN** table rows carry a
beside-note"*. An appending unit that minted the same token — **or that merely quoted the probe's pattern
verbatim** — would silently falsify a receipt it did not author, and the second way is the subtler one: a
count over a file counts the lines that take the count. Unit `b`'s coordinate correction therefore carries a
**distinct marker of its own**, spelled `⟨b` + `-note:` and appearing in the **F-α row**, where it records
both of this seat's corrections in one place (the rooting of `service/versions.ts` and the live form's
nullish guard). ▲ **And this seat publishes NO count of that marker** — deliberately, because a figure over
a token whose every mention moves it has no fixed point, and chasing one is how §0.1c's own *"17"* nearly
shipped. **The register's countable operand is the `id` column, which FW6-G17 greps; a notation marker is
not an operand and is not counted.** This paragraph spells the beside-probe with a bracket so that quoting
it
matches nothing: ⟨cmd⟩ `/usr/bin/grep -c '⟨besid[e]' burn-register.md` → **18** at this seat's settle,
**double-run 18**, unmoved from unit `a`'s figure — the bracketed pattern matches exactly the lines the
plain one does, while the line you are reading is not one of them. ⟨*measured the honest way: the first
draft of this paragraph quoted the pattern plainly and drove the count to **22**; it was corrected before
the file settled, which is the only moment such a correction is not an erratum*⟩. **E-3 is served the other
way round too**: nothing of unit `a`'s is rewritten, and the note records a rooting and a live-form
difference **beside** the spec's spelling, never over it.

**What this seat did NOT decide, stated so the close seat does not read silence as consent.**

1. **F-α's disposition is UNRULED.** COHESION §0j.D reaches it nowhere; the register row flags it inline per
   runbook §5.7 (*"Owner rulings are FLAGGED INLINE and never presumed"*) and names **both** branches with
   the falsifier that closes either. **The dissent is carried, not arbitrated**: fourier's board books
   DELETE (`M/M.md` §7's fold row + the M.W10 wave row); the seam table frames value.js as the deepening
   side; the spec itself calls the question *"genuinely open"*.
2. **A second, narrower dissent rides F-β** — M.W10's *"W2-fix: `palette_slug` remix-only"*, which would
   resolve FW6-G3 and FW6-G4 by subtraction. Also carried, also unarbitrated, also routed to unit `i`'s
   letter. ▲ **Neither dissent is a reason to weaken a gate**: the gates' falsifiers are unchanged and
   remain the spec's own words.
3. **No landing is claimed.** §3's split verdict is obeyed to the letter — *"a gate closes for F.W6 when the
   commissioned act is authored with its evidence row in the burn register; it goes GREEN only when the
   named landing occurs. F.W6 never claims a GREEN it did not execute."* All four of this unit's gates are
   reported **RED with the commissioned act installed**, exactly as unit `a` reported FW6-G18.

---

## §2 Unit stamps

| unit | gates turned | commit | stamp |
|---|---|---|---|
| **a** | FW6-G19 (2nd conjunct) · FW6-G18 (roster carried) | *(this commit)* | **CLOSED 2026-09-17** |
| **b** | FW6-G1 · FW6-G2 · FW6-G3 · FW6-G4 — **all four RED with the commissioned act installed** (§3's split verdict; no landing claimed) | *(this commit — the **ONE COMMIT** family of §2.1's head: F-α ⊕ F-β ⊕ SS-C-1 land together)* | **CLOSED 2026-09-17** |
