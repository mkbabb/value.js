# X.F.W6 — Provenance defect burn-down (library half)

⟨X·F · **status: planned** · half: library · Opus-drafted, **fresh-Fable FOLD-VERIFIED** (M-23) 2026-08-28 — verified row-for-row against the F.W6 CARRY, consumed **whole**: 24 rows · 19 gates · 13 cross-edges · zero silent drops (M-25: dedupe by identity, cure-shape locks and sequencing edicts carried verbatim, routed by mechanism) · **no registry row names this wave**: measured at the fold, `grep -o 'F\.W6' docs/tranches/V/megatranche/registry/adjudicated/fr-*.md | wc -l` → **0**, while `F.W5-W8` → **120 occurrences / 26 records** over the 66-record corpus (3 of them routing-law boilerplate that mints nothing) — so this spec is built from **census §4.7 + the lane-crud SEAM TABLE + intake X-8** and re-cut out of that span roster · L-20 validated against `docs/precepts/instructions/` (TRANCHE-AND-WAVE-SPEC · tranche/WAVE_SPEC · STYLE · ORCHESTRATION · CONSUMING · LESSONS-LEARNED) · idiom `docs/tranches/X/waves/W0..W11.md`, cut to the lean `parse-that/waves/` + `F-W10.md` form · substrate re-measured read-only 2026-08-28, receipts inline⟩

**Alias law**: `F.W6` denotes `X.F.W6` — a search alias, never a rename. The spec lands as `F-W6.md` beside its authored siblings in `docs/tranches/X/fourier/waves/`; every banked id keeps its identity for life.

**Scope.** The F-side **burn-down** wave: F.W5 states the shared-provenance clauses, F.W6 executes the **server half** of the fourier CRUD/provenance defects those clauses name — the version chain (F-α), the PATCH cut (F-β ⊕ SS-C-1 write leg), breadcrumb redaction (F-γ), counter provenance, create-path idempotency, the moderation band's server routes, the audit actor field, contour/cache provenance, the canonical-geometry TRIPWIRE, and the produced-and-unconsumed strikes — plus the **routing receipt** by which V-α / V-β / V-γ / TA-4 leave for the value.js API row with zero value-tree bytes moved. It authors no clause, reverses no ruling, claims no client or display arm, and touches no frontend.

**Four-verb**: AUDITED **YES** (24 CARRY rows, each carrying its adjudicated witness; substrate re-measured 2026-08-28) · SPECIFIED **YES — by this file** · IMPLEMENTED **NO** · VERIFIED **NO** (stamped only at the X·F release close; no wave stamps VERIFIED at its own close). **EXECUTION IS NOT AUTHORIZED BY THIS FILE** — status stays `planned`; nothing opens, writes, builds, or runs product source in either repo until the owner's begin-word.

**Binding law.** M-25 (a spec ignoring adjudicated rows is DEFECTIVE; transcription-only is DEFECTIVE). L-19 (every gate names a real witness — no proof-farm scripts, no descriptive invocation). L-20 (precepts-latest). D-19 (no anchor cited before F.W0 re-grounds). Born-RED: **every gate below starts RED with its failing witness named.** Three standing laws shape every row:

1. **`/Users/mkbabb/Programming/fourier-analysis` is READ-ONLY, always.** This wave writes **zero fourier bytes**. Every "burn" here is a value.js-tree register row plus a commissioned ask in one letter; the fourier edits happen in fourier's own sub-session under COMMISSION §2, after the begin-word. Specifying is not doing.
2. **F.W5 STATES, F.W6 BURNS — no double-booking.** Where F.W5 owns a clause, F.W6 owns only its execution; where F.W3/W4/W8 own a client or display arm, F.W6 emits and does **not** claim it (the FR-GIG-5 mirror, adopted as a standing bar at F.W5 §1b).
3. **Producer rows never become frontend hacks, and server cures are never discharged as frontend hacks.** Glass rows ride SS-6 (declared empty here); parse-that→fourier routing is forbidden.

---

## §1 Bounds

Owned (writes happen only at execution, post begin-word):

| # | Path | Act |
|---|------|-----|
| 1 | `docs/tranches/X/fourier/waves/F-W6.md` | CREATE — this spec |
| 2 | `docs/tranches/X/fourier/waves/F-W6/burn-register.md` | CREATE — one row per burned identity: banked id · gate · commissioned act · fourier-side landing evidence. **FW6-G17 greps its id column**; without it the carry-closure gate is prose |
| 3 | `docs/tranches/X/fourier/waves/F-W6/value-side-routing-receipt.md` | CREATE — V-α / V-β / V-γ / ⊙ TA-4 named to the value.js API row, citing F.W5 §6c's itemized obligation list. **FW6-G16's whole artefact** |
| 4 | `docs/tranches/X/coordination/value-to-fourier-2026-08-DD-provenance-burndown-F.W6.md` | CREATE — the commissioned-ask letter (the only vehicle by which the fourier side receives this burn-down). `DD` resolves at execution, never back-dated |
| 5 | `docs/tranches/V/coordination/INBOX.md` | **append-only** — one E13 ledger row for the relay; no rewrite of any existing row |

**Bounds reconciliation vs the CARRY `boundsFiles`**: all five adopted **verbatim, zero additions** — rows 2 and 3 are exactly the two evidence surfaces FW6-G17 and FW6-G16 need, and row 5 is E13's standing duty, not new scope. Measured at the fold (2026-08-28): `docs/tranches/X/fourier/waves/` holds `F-W2.md F-W3.md F-W4.md F-W5.md F-W8.md F-W9.md F-W10.md` — **no `F-W6.md` and no `F-W6/` subdirectory exist**, and this file's creation is not evidence any sibling artefact does. `docs/tranches/X/coordination/` exists (one file) — F.W5's relay lands there too; same channel, no third courier invented.

Read-only (bounds, no write): `registry/adjudicated/fr-*.md` (all 66) · `formation/fourier/{CENSUS-2026-08-03.md, lane-crud.md}` · `audit/codex-provenance/{INTAKE-ADJUDICATION-2026-08-03.md, api-gap-remeasure.md}` · `intakes/lane-fourier-r3-r6.md` · `docs/tranches/X/fourier/waves/F-W5.md` + its `contract/` outputs (`J-diff-shape-v2.md`, `operation-register.md`, `OWNER-RULINGS-F.W5.md`) · `docs/tranches/J/design/J-diff-shape.md` v1 (271 lines, **IMMUTABLE** — v2 supersedes by reference and never patches it, E-3).

**Explicitly NOT in bounds:** `/Users/mkbabb/Programming/fourier-analysis/**` (whole tree, read-only law) · product source both repos — `value.js/{src,demo,api,test,e2e}/**`, fourier `api/`, fourier `web/` (execution gate) · `docs/tranches/X/COHESION.md` (root-authored at the boundary; F.W6 declares its half of every edge in §4 and requests the reciprocal — it does not write the spine) · `docs/tranches/X/{waves,keyframes,parse-that}/**` · the immutable evidence set above (rewriting one destroys a witness a gate cites) · `package.json` · `scripts/dev/dev.sh` (unowned dirty row, **never staged**) · `~/.codex/**`.

---

## §2 Carry — 24 identities, agglomerated by mechanism

`‡` BLOCKER-weight · `⊙` OWNER-GATED (may not be burned unruled) · `▲` a cure-integrity lock binding the **repair**, not the finding. Every anchor below is **MEASURE-AT-OPEN** (D-19): re-resolved against F.W0's re-grounding receipt before it is cited in any commissioned act.

### §2.0 RE-CUT-F6 — this wave's own precondition (recorded, not booked; no banked id exists)

**BLOCKER (spec-integrity).** Measured 2026-08-28: bare `F.W6` appears **0 times** across the 66 adjudicated records; the routing that reaches this wave is the **`F.W5-W8` span** — 120 occurrences / 26 records, of which 3 are the routing-law header line (mints nothing, per the RE-CUT-0 dissent's explicit warning). A span is not a home. The re-cut this spec performs: the span's **server/provenance mechanism subset** lands here; its client-edge subset stays with F.W8 (operation wiring + conformance) and its render/copy subset with F.W3/W4. **Both directions must be empty at FW6-G17** — a row cut here and also credited to F.W1/W3/W4/W5/W8 is a double-booking; a span row cut nowhere is a silent drop.

### §2.1 The version chain and the PATCH cut — **ONE COMMIT** (3 rows)

| id | sev | mechanism (witness) | cure + locks | sequencing |
|---|---|---|---|---|
| **F-α** | P1 (census risk 5) | `_write_root_version` is the **only** writer of `visualization_versions` and hard-codes `parent_hash=None, root_hash=set_hash_value, depth=0` (visualizations.py:138-141), called from create :220 and remix :592 — every version has `depth==0`, `GET /{slug}/versions` (:860-889) depth-sorts a permanent singleton, and all real lineage rides cross-viz `fork_of` | Deepen the quadruple by walking the parent as value.js already does (`rootHash = parent.rootHash ?? parentRef; depth = parent.depth + 1`, service/versions.ts:46-58) **or** retire depth/parent/root from the model. F.W5 G3's close verbatim: *"v2 states deepen-or-retire for the depth/parent/root quadruple"* | F.W0 first (D-19). **F.W5 owns the CLAUSE, F.W6 the burn-down — NO DOUBLE-BOOKING** (F-W5 §6c verbatim); F.W5 §12 declares F.W6 blocked on it. X-8 verbatim: *"strengthens the census routing, **no new work**"* — three-way corroboration adds evidence, not scope. **Dissent, carried and surfaced to F.W5's ruling, never silently overridden by this seat**: retire-vs-deepen is genuinely open — fourier's own board books this as DELETE (W2-transpose) at M.W10 (`M/M.md §7`, quoted in X-8) while the seam table frames value.js as the side that deepens; census C-6's *"known unfinished work, not an overlooked bug"* is confirmed verbatim from the source tree |
| **F-β** | P1 (chain + stale advertised hash) | `palette_slug` is atom 5 (atomdiff.py:34) **and** a PATCH field (visualization.py:207), yet the handler does a bare `$set` (visualizations.py:381-383) with **no `set_hash` recompute and no version write**; `_head_set_hash` returns the *stored* value when non-empty (:477-480), so the advertised hash goes STALE vs `enumerate_atoms(doc)` and `/diff` + `/provenance` then report it. Adjacent: `updates = {… if v is not None}` (:381) means PATCH can **never clear** `palette_slug` | Recompute `set_hash` from `enumerate_atoms(doc)` on every atom-touching PATCH + write the version row; make `palette_slug` tri-state as remix **already is** — `model_fields_set` at :522-528 (omit=inherit / null=clear / slug=rebind). ▲ **KISS lock: the tri-state idiom ships in the same router twenty lines away — LIFT it, do not invent one** | **Declared gate gap: F.W5's G1–G20 carries NO gate for clause E8** — F.W6 supplies FW6-G2/G3/G4 rather than read the omission as absence of scope. ▲ **ONE-CUT LAW with SS-C-1: both edit the same PATCH model; they land in ONE change or the second reverts the first's invariant** |
| **SS-C-1** (write leg) | MAJOR (demoted from challenge-C's BLOCKER at component altitude — no capability loss in the control) | `VisualizationUpdate` (visualization.py:198-210) and `VisualizationPatch` (types.ts:256-262) are the same five fields with `extra="forbid"`, while `animation_settings` is atom 4-of-5 (atomdiff.py:38) — the persisted `speed` atom is settable **only** at create/fork; silent non-persistence of a preference the control claims to set | PATCH field set **equals** the atom set, or each divergence is stated per-atom with its reason | ▲ **SPLIT RULING (R2-LC wins), binding: the READ leg — `loadVisualization` zero callers — is banked at fr-GalleryCardModal GCM-1 at blocker weight → F.W4 + F.W5-W8 rider. Do NOT re-book the read leg here.** Only the write leg is un-banked. Same-commit rider on F-β. No dissent — the axis split was adjudicated deliberately; re-merging re-books GCM-1 |

### §2.2 Redaction and the image leaf — one privacy cut (2 rows)

| id | sev | mechanism (witness) | cure + locks | sequencing |
|---|---|---|---|---|
| **F-γ** | P1 (privacy; census calls opposite defaults on privacy-adjacent surfaces contract-BLOCKING) | `GET /{slug}/provenance` (visualizations.py:733-791) walks `fork_of` with a bare `find_one` (:784) emitting `slug`, `set_hash`, `author_slug`, `created_at` for **every** ancestor incl. private/draft ones — `_readable_or_none` is applied only to the ENTRY row (:744). value.js already redacts: `ProvenanceStep` is a discriminated union (service/forks.ts:171-179) collapsing non-public hops to `{kind:"unavailable", ordinal}`, with the guarantee stated in-source at :167-169 (tagged V·W45 item 4) | Apply `_readable_or_none` per hop and collapse non-public ancestors to that shape — **adoption, not design** (census §7: "reusable as-is, value.js is ahead"). The existing cycle-guard and the ≤50 cap **must survive the cure** | **G6's second branch stands open** — *"…**or** the asymmetry is stated with its reason"*; the burn seat **may not choose the redact branch until F.W5 closes the clause**. ▲ One-cut discipline with the image row: a breadcrumb redaction that leaves a draft/unlisted viz's source image world-readable by slug **has not cured the leak** |
| **FR-AFP-4** ‡ (⊕ **F-4** ⊕ **m-15** ⊕ **GCM-52**; ImageUpload row 26 cited) | BLOCKER-weight (one of the panel's four survivors) · m-15 MINOR · row 26 INFO | **No action the moderation panel offers takes the reported artifact offline**: Delete is a soft-delete of the *visualization*; `images.py` = 7 ops with **zero DELETE**; blob GET (:132) has no `Depends` and ships `Cache-Control: public, max-age=86400`; every anonymous blob GET ends in `touch_document` — **actively-fetched reported content is precisely what never reaps**. Thumbnail GET carries the identical posture (F-4: one router, one identity), so a draft/unlisted viz's source image is world-readable by slug (GCM-52 — R3-7b at the leaf; 2²⁸ slug entropy from 4× `secrets.choice` over 128-word lists). m-15: unversioned thumbnail URL (api.ts:292-294) + 24 h cache (images.py:162-164) negates the deliberate re-upload regeneration branch (image_storage.py:105-128) | Image remediation contract executed: **delete/quarantine op + blob visibility gate + janitor predicate fix + versioned thumbnail URL**. ▲ **C's scope discipline preserved: soft-delete DOES de-list from browse — the cure may not overstate the hole.** ▲ **F-4 books WHO can fetch; m-15 books WHAT they see — cross-referenced, NOT merged**; a cache-versioning cure discharges neither the auth cure nor vice-versa | F.W5 C1/C2/G8 state the authority class per operation first (**cite 45/30/13, never 30 alone** — the admin arm is inside the denominator; OpenAPI security 0 of 45). ▲ **The deployment auth-proxy question stays UNPROVEN (F.W5 SS-13 defer #5) — the gate may not close by assuming a proxy in either direction** |

### §2.3 Counters, identity, idempotency (2 rows)

| id | sev | mechanism (witness) | cure + locks | sequencing |
|---|---|---|---|---|
| **FR-GV-12** (⊕ **FR-GV-24** · **VV-R2-A** cited) | MAJOR (×2 witnesses) | `GET /api/visualizations/{slug}` is an unsafe read that cannot observe its own side effect: `find_one` first (:256), `$inc {"views": 1}` after (:268-270), `_public_doc(doc)` serialises the **pre-increment** document (:272). So (a) `recordView`'s write-back (gallery.ts:207-208) restores a value the list already held — the counter can never show the user's own open; (b) the same verb is the ETag-capture path (gallery.ts:167, :223), so every ETag cache-miss on publish/soft-delete adds a **phantom view** to the row being mutated and an entity is **born at `views: 1`** (VV-R2-A, chain verified whole); (c) no `viewed_ips` dedup while `liked_ips` exists, and a mutating GET is unsafe against any proxy/prefetch (RFC 9110 §9.2.1) | Explicit view verb making the read safe — **or** v2 states the mutating-GET policy against RFC 9110 §9.2.1 (G12's two branches). ▲ **CURE LOCK (FR-GV-24, restated at F-W5 §7): a repair test must NOT assert a re-open view increment — the session guard DOES work within a session; the defect is SCOPE, not absence** | F.W5 E6/G12 states first. **VV-R2-A's client arm — publish adopts `store.setVisibility("public")`, dropping one round trip — is F.W4's; F.W6 may not be credited with it.** FR-GV-24's `viewedHashes` component-local reset is the composing client row. SS-13 owns the observable (publish a fresh viz, read the card's count; derivation: born at 1) |
| **B-2** ‡ (⊕ **FR-GV-1** FOLD; CanvasControlsDock D-4/L-1/C-3 +C-28, VisualizationView BLK-2 folded) | BLOCKER (banked) | The second press of publish mints a SECOND public visualization: `createVisualization` sends no `Idempotency-Key` (api.ts:371-380) though `coreFetch` supports it (:138-140); the middleware is an explicit `no header → handler()` passthrough (idempotency.py:68-70); `content_hash` carries a PLAIN index (database.py:98) while flags carry a deliberate `unique=True` (:140). The asymmetry is **inside one file** — the same router's remix arm already calls `idempotency.replay_or_record(...)` at :612 | Server-side dedupe/idempotency for the create path — the ADJUDICATED SPLIT verbatim: *"props/reconciliation contract → **F.W3/W4**; server-side dedupe/idempotency for the create path → **F.W5-W8**"*. Lift the remix arm's `replay_or_record`, or make `content_hash` unique. ▲ **K12 LOCK: both axes' UNPROVEN-NEEDS-LIVE hedges on the duplicate-row outcome are DEAD — B-2 closes STATICALLY; SS-13 spends no probe here** | **F.W6 takes the SERVER arm ONLY.** The `savedSnapshots: []` single-writer, the vacuous `!d.savedSnapshots?.length` filter, the re-enabling button and the `publishedHashes` misnomer are F.W3/W4's — one home, two citations. Rider at the banked home: m-13 (the success toast prints the USER slug). No dissent — FR-GV-1 was graded BLOCKER at the banked severity and explicitly folded; the fold is the identity guard, not a demotion |

### §2.4 The moderation band ⊙ — **every row rides FR-AFP-1** (6 rows)

| id | sev | mechanism (witness) | cure + locks | sequencing |
|---|---|---|---|---|
| **FR-AFP-1 = D3** ‡ ⊙ **THE ADMISSION GATE** | BLOCKER-weight, OWNER-GATED | The moderation queue has **no producer**: two admin routes (GET `/flagged`, DELETE `/{slug}/flags`) are the collection's entire live surface; every `db.flags` write repo-wide is a test fixture or a `$rename` migration; `FlagRequest` referenced nowhere; no client posts a flag; **~110 of 285 panel lines dead by reachability**; production's only reachable state is the empty state. **value.js HAS the verb (`POST /:slug/flag`).** FR-AFP-18 (cited): the queue has no size anywhere — no total in the envelope, no `AdminStats.flagged` | ⊙ **producer-or-retire RULED at F.W5 (G11) — F.W6 does not decide it.** If PRODUCER: F.W6 ships the flag-write operation with value.js's `POST /:slug/flag` as reference implementation. If RETIRE: the band's **server routes are deleted here**, the panel at F.W3/W4 | ▲ **This ruling PRECEDES F.W1's sizing wherever FR-AFP grades are load-bearing (ruling 5).** **Every FR-AFP row below is CONDITIONED on it; F.W6 may not open the moderation band unruled.** Dissent-of-record (ruling 5 itself): severities grade the POPULATED surface and FR-AFP-1 gates the band's live reachability — any grade cited from that band carries the conditional or it overstates. The L-1/L-2 dual-BLOCKER incoherence was resolved here: the axis cannot carry two BLOCKERs when its first proves the second unreachable |
| **FR-AFP-66** (⊕ **FR-AFP-9** ⊕ **FR-GV-34**) | MAJOR (permanent-growth data defect) · FR-AFP-9 dialog-truth arm MINOR | Orphan flag accumulation: soft-delete does not cascade flags, the janitor's grace hard-delete cascades nothing, and `pinned_cron` — **named by the softdelete docstring as the cascade owner** — contains zero flags code. The panel's own Delete → grace pipeline strands flags forever, permanently inflating the unscoped `$group` every page request runs; the module docstring is FALSE for this band; the existing orphan-flag test exercises only the reporter-keyed stale-user cascade. FR-AFP-9: the confirm dialog asserts irrevocability over a 30-day-restorable soft delete (restore route live at :430), and a restored row returns to the queue still flagged | `content_hash`-keyed cascade at grace hard-delete **+ docstring correction** (the false module prose is part of the cure, not commentary). Composes with FR-AFP-4 and FR-AFP-33 (the growth term) | ⊙ **the hard-delete-arm caller decision sits in F.W5's owner block (E7)**. CONDITIONED on D3/G11 — **if RETIRE, the cascade is deleted with the band rather than built**. FR-AFP-9's truthful copy → **F.W3/W4** (diction rides FR-AFP-35); FR-GV-34's batch-dialog copy likewise. Coverage obligation → F.W9/W10 |
| **FR-AFP-7** (⊕ **FR-AFP-8** identity arm) | MAJOR | Flags key on **non-unique `content_hash`**: one artefact fans out to N rows each claiming the full count (3 rows × "3 flags" = an apparent nine), and the slug-labelled "Dismiss flags" action is hash-scoped. FR-AFP-8: the destructive confirm names `image_slug` — an asset FK **shared by every remix** (`VisualizationRemix` declares no `image_slug`) — while deleting `slug`, violating the identity law the file's own header states at :18-23; every human-readable surface renders the FK | F.W5 D12 states which identifier a human-facing surface may render; **F.W6 burns only the server half — the flags keying and the dismiss scope** | ▲ **FR-AFP-8's cure is a one-token F.W3/W4 rider (pass `item.slug` as the label) — F.W6 may not claim it.** Same shape as GCM-1's `/w/` routing on an asset FK (cited; GCM-1's repair unit is F.W4's). CONDITIONED on D3/G11. **Dissent recorded so the row is not read as twice-derived: FR-AFP-7 was filed on the β/L axes only (α remit); the α reader never examined it** |
| **FR-AFP-33** | MAJOR | The flag list is unbounded on both ends — server `$push` with no `$slice`, client no cap/collapse — and the aggregate is a bare `$group` over the WHOLE collection per page request, O(pages × \|flags\|), whose permanent-growth term is FR-AFP-66. Counterweight preserved verbatim: `if not flagged` short-circuits production's only case | Server → bound the `$push` (`$slice`) and scope the aggregate | **Client collapse → F.W1/W3 (`./expandable-container` is already installed) — NOT F.W6's.** Magnitude + β's sort-index note → SS-13. CONDITIONED on D3/G11. Composes with FR-AFP-66: curing the cascade without bounding the aggregate leaves the cost; bounding the aggregate without the cascade leaves the growth |
| **FR-AFP-36** (dead-models arm) ⊕ **D10 serializer** (FR-AFP-32/-40/-69; GCM-55 cited) | MAJOR | Contract-twin drift: the emitted `content_hash` — the actual join key — is omitted client-side; three dead server models encode the retired contract; **no `response_model` anywhere** to check against. D10: ONE router serializes the same concept two ways — flagged rows via `_json`/`default=str` (space-separated, non-ECMA) while audit returns `AuditListResponse` (ISO with `T`) — and the NaN guard sits in the consumer receiving the **conformant** payload. GCM-55 (cited): the wire serves `likes` raw because `json.dumps` bypasses the Pydantic model | One generated/checked shape; **dead models delete**; serializer unification on the server. ▲ **K9 LOCK, binding: do NOT delete the five `?? item.slug` fallbacks** — `admin.py` emits `doc.get("image_slug")` through an unvalidated hand-built body with no `response_model`, so they are DEFENSIVE, not dead (the limb is NOT PROVEN) | F.W5 owns ONLY the serializer clause; F.W6 burns the server half. ▲ **The shared client formatter is already banked at fr-AdminUserList FR-AUL-17 → F.W3 — do NOT re-book.** ⊙ **FR-AFP-71's codegen/module-split decision is OWNER-GATED — v1 chose hand-typed twins explicitly and v2 may not silently reverse it; F.W6 may not build codegen unruled** |
| **FR-GV-9** (⊕ **FR-AFP-10** ⊕ **FR-AFP-70**) | MAJOR | Batch Unfeature sets `{"$set": {"tier": "normal"}}` unconditionally via `update_many` (admin.py:435-440) — **unfeaturing a SAVED-tier entry silently erases the saved tier** — while the :423-425 dialog copy is honest about the mechanism and the :326-333 button label is not. FR-AFP-10: "Mark acceptable (save)" never dequeues — `set_tier` writes `{tier, updated_at}` only (:160-190) and the flagged listing has **no tier predicate**. FR-AFP-70: `handleSetTier` is hardwired to `'saved'` with no inverse; a repeat click is a real PUT + audit row + success toast + full pagination reset for a no-op | **A blanket write is not a transition** — the server states the transition (a `$set` conditioned on the current tier) and the flagged listing carries the tier predicate FR-AFP-10 assumes. ▲ **β's 428-escalation hypothesis is KILLED by its own author (If-Match `*` accepted) — do NOT revive it as the cure** | Interim client mitigation (reflect-and-disable when already saved) is **F.W3/W4's**; FR-AFP-59's tier-render arm (the 10px raw wire token) likewise. Rides banked FR-GFC-20's stale-slug row (F.W4). FR-AFP arms CONDITIONED on D3/G11. **Dissent: FR-AFP-10 and FR-AFP-70 were filed on the β axis only (α remit)** |

### §2.5 The audit actor (1 row)

| id | sev | mechanism (witness) | cure + locks | sequencing |
|---|---|---|---|---|
| **AA-10** (⊕ **AA-5** server arm; AA-6/AA-24 cited) | MAJOR ×2 | The audit log never answers *who*: `janitor.py:56` `_JANITOR_ACTOR = "system:janitor"` is written into **`ip_hash`** at :95 and rendered `system:jan` by an unconditional `.slice(0,10)` at :146 — convicted by the producer's own comment (:50-55, *"self-documenting in the viewer"*). AA-5: the collection's **second writer** (`janitor.py:59-99`) inserts directly into `db.admin_audit` with no `log_audit` call, emitting nine `janitor:*` actions (spot-verified :133 `hard_delete_visualizations`, :285 `prune_audit`) that match no arm of the tone map — including the audit log pruning its own history | Verbatim: **"an audit row's actor is a field, not a repurposed `ip_hash`."** AA-5's server arm: route the janitor through the shared action taxonomy / `log_audit` seam. ▲ **K-6 METHOD LOCK: `grep "log_audit("` is structurally BLIND to an inlined second writer — enumerate with the S-8 method law, not the killed one.** ▲ **AA-23: the regex-action cure BOTH axes prescribed is REJECTED** (an indexed equality would become a collection scan on the one monotonically growing collection); AA-6's sound order is (a) honest placeholder, (b) `$in` over a generated taxonomy, (c) anchored case-**sensitive** `^prefix` — in that order only | F.W5 D9/D4 state; F.W6 burns. **Display arms are F.W4's and F.W6 may not claim them:** the sentinel-aware branch + legend (AA-10), the display taxonomy (AA-5/AA-24), the placeholder honesty (AA-6). Composes with R-7/V-γ — the fourier and value halves of user provenance are one clause with two burn seats. Coverage → F.W9/W10 (AA-44: nine e2e specs never exercise the audit tab while the backend pins its half at `test_janitor_audit.py` — the one-sided seam that let AA-5/AA-6/AA-24 open and stay open) |

### §2.6 Contour, cache identity, and canonical geometry (3 rows)

| id | sev | mechanism (witness) | cure + locks | sequencing |
|---|---|---|---|---|
| **B-4 = C-1** ‡ (∘ **i-7** = C-25/R6-8 fold; **m-18** rider) | BLOCKER (banked) | The ML Threshold slider is inert after the first extraction: `extraction_cache_key` is a **CLOSED 10-field `json.dumps` literal** (image_storage.py:250-266 — `_v, image_sha256, strategy, …`) that OMITS `ml_threshold` / `ml_detail_threshold` — the two fields this control is the sole producer of — and the handler short-circuits on the cache hit **BEFORE** `compute_contours`, while those fields materially drive extraction on the shipped default route | **Cache identity ⊇ consumed request fields** — the sharpened constraint banked at i-7 as B-4's explicit F.W5-W8 payload. ▲ Rider **m-18**: `ml_detail_threshold: mlThreshold.value * 0.6` is an invented, unnamed coupling that makes an independent contract field unsettable and overwrites any restored value on the next compute — **the cure must not preserve it** | F.W5 E12 states the superset clause; F.W6 burns. **GREEN fixture named by G13: `contour_hash` INSTABILITY across an ML-threshold change** — also the SS-13 probe. i-7 is INFO-weight fold, not a second booking |
| **M-13** ‡ (⊕ **M-14** ⊕ **C-2** ⊕ ContourPreview row 28) | BLOCKER (data-loss class) | Editor-saved contours carry no `extraction_cache_key` (`source="editor"`), so merely exiting the editor re-runs extraction and the server **cache-hits the ORIGINAL** — the hand edit is destroyed with zero settings touched; a Harmonics/Sample-Points nudge reaches the same end through the debounced `runCompute()` → `extractContour()` (M-14). And the POST never derives bounds: `store_contour_asset(xs, ys, req.image_slug, source="editor")` is called with **no `image_bounds`**; the param defaults `None` and is written verbatim (image_storage.py:290/:318); backfill is GET-only — and because the asset is content-addressed, **the overlay dies exactly when the points changed** (C-2) | G14 verbatim: **editor-saved contour is a first-class compute input; extraction cannot overwrite a `source="editor"` asset.** G15: **derive-on-POST or backfill-on-write is RULED**, not left to the burn seat's taste | F.W5 E11/G14/G15 state; F.W6 burns the **server half**. ▲ **The client half is F.W3/W4's and F.W6 may not claim it:** L-2's orchestrator move — the compute pipeline hoisted out of the `out-in` leaf — is REQUIRED (the 3-call-site compute census closes the remount-only escape hatch), carried with an urgency rider at the data-loss class. M-14's end-to-end reproduction → SS-13 (mechanism source-certain; end-to-end UNPROVEN-NEEDS-LIVE) |
| **G16 / L-B1** (⊕ **L-B2/C-2** ⊕ **L-B3**; L-M3/C-6, L-M5/C-5+C-15, C-7, L-m3/C-10+L-m5) — **TRIPWIRE** | MAJOR at the seat's grade (two BLOCKER dissents); **BLOCKER on any regeneration attempt** | The generator has diverged from the artifact it generates: only **3.7%** of shipped `moon.json` points fall within 1.0 unit of any extracted point, p90 **37.6** against a 1.23-unit resample spacing, and the crescent-alone bbox strictly contains the shipped bbox by 12.97 x-max / 6.55 y-min — while the sun IS consistent (0.326/3.44). The only claimed regenerator is **untracked AND broken at import AND parameter-diverged**: `.gitignore:53 scripts/*` swallows both `precompute_svg_fourier.py` and `raw-contours.json`; it imports `order_contours`, which **no module defines**; it hard-codes 100/12 against the shipped 50/10. Canonical geometry is a pure function of an unpinned third-party PRNG with zero drift detection | ▲ **TRIPWIRE, binding and verbatim: DO-NOT-REGENERATE `moon.json` / `sun.json` on `master`; any regeneration attempt revives L-B1 and L-B2/C-2 at BLOCKER.** The wave's act is to **name the true source or re-author the artifact — WITH G5c's diagnostics row**, which is a PRECONDITION, not a companion: three silent-drop gates with zero diagnostics (`grep console\|throw\|warn` → 0) are what makes G1c structurally UNOBSERVABLE at the human seam, while the server twin ships `extract_contours_result` WITH diagnostics. ▲ **G7c honesty lock: the decomposition is NOT duplicated and the offline detour is ELECTIVE (`saveContour` accepts ingested contours) — the burn-down must not overstate duplication.** ▲ **SE-05's "just delete it" for the `window` handoff is WRONG on the tree (G6c)** — the precompute script names the component as its INPUT | F.W5 §G states G1c–G9c; **F.W6 / the build lane burns — G16's GREEN-owner cell names this wave BY NAME** (the one non-CRUD row F.W5 hands here). **Track the seam first (G2c)** — an untracked, import-broken regenerator cannot be a cure input. *"What `moon.json` WAS generated from"* is **UNDETERMINED** → SS-13. **Dissent preserved: reader-1 held L-B1 and L-B2/C-2 at BLOCKER ("the tool's output is a regression and nothing in the tree says so"); the seat demoted both to MAJOR on latency — the pipeline is already dead, nothing regressed a user today. The TRIPWIRE restores BLOCKER weight the moment anyone regenerates** |

### §2.7 Liveness and projections (1 row)

| id | sev | mechanism (witness) | cure + locks | sequencing |
|---|---|---|---|---|
| **PP-DEADSEAM** (⊕ **M-β4** ⊕ **L·m-6** ⊕ **GM-M4**; VV-R2-B cited) | INFO–MINOR band | `preview_path` is written as `""` at all three server sites (assets.py:85, image_storage.py:318, responses.py:22) while the client leaf is unreachable — **zero implementation on either side**. `EpicycleData.trace` — a 3000-sample polyline computed per response — has zero readers in the web tree and rides `structuredClone` into every IndexedDB draft (workspace.ts:95-104). `reconstructed_points` (500+500 floats) is fetched, sessionStorage-persisted whole, and read by nothing. `softDelete`/`restore` are the gallery store's only ETag/If-Match round-trip and have zero callers | ▲ **E10 verbatim: ONE stated disposition, not three ad-hoc deletions.** F.W5 states the liveness predicate and the projection disposition (B4/E10); **F.W6 executes only the server-side strikes** — the three `preview_path` writes and the produced-and-unconsumed projections | ▲ **F.W6 must NOT pre-empt E16/G7:** a projection strike that is really a compression decision (`trace`, `reconstructed_points`) waits on the trie ruling — absent it, whole-snapshot duplication is the recorded shipped behaviour. The client-side `softDelete`/`restore` wiring belongs to **VV-R2-B's single F.W4 wiring unit** (route → `loadVisualization`; publish → `setVisibility`; delete → the store action) — F.W6 may not scatter it |

### §2.8 Born-visibility and lineage (1 row)

| id | sev | mechanism (witness) | cure + locks | sequencing |
|---|---|---|---|---|
| **R-5 / G5** born-visibility ⊕ **E4** lineage (**GCM-1** cited) | P1 (census risk 6 — opposite defaults on a privacy-adjacent surface are contract-blocking) | A fourier remix child is born **`draft`** (visualization.py:277); a value.js fork child is hard-coded **`visibility: "public"`** (forks.ts:76) — opposite privacy defaults on the same verb, both shipping. Same-verb divergences ride alongside: remix requires an atom change (no-op → 422 `urn:contract:remix-noop`, :536-542) while fork is a verbatim copy (`colors = source.colors`, forks.ts:64); fourier runs no transaction by deliberate choice ("standalone-topology-honest", :493) while value.js's fork is transactional; fourier records `atom_ops` on the edge, value.js records nothing. GCM-1: a save after the modal's CTA POSTs a NEW entity with **no `fork_of`** despite the fork substrate existing — lineage silently unrecorded | One co-signed born-visibility rule **+ a create-visibility test on each side** (G5's GREEN condition). **F.W6 burns the fourier half only**; the value half routes to the value.js API row | ▲ **GCM-1's routing cure — emit `entry.slug`, route `/v/`, point the loader at `loadVisualization` — is F.W4's; F.W6 takes only the "no `fork_of` recorded" server arm.** F.W5 E4/G5 states the rule; the ruling is owed. Neither side's edits gate the other's (COHESION §1 SS-4). **The verb-name reconciliation (remix vs fork) and the delta-required semantics are in the census's owed-rulings set and are NOT F.W6's to settle** |

### §2.9 The like verb ⊙ (1 row)

| id | sev | mechanism (witness) | cure + locks | sequencing |
|---|---|---|---|---|
| **FR-GFC-3** ⊙ (⊕ **GCM-2** ⊕ GalleryCard **C-1-as-corrected** ⊕ GIG **C-3** FOLD) | MAJOR (4 witnesses; BLOCKER framing KILLED at K-1) | `likes: int = 0`, the `(visibility, likes, -id)` index and `cursors.py:22`'s `"likes": "likes"` sort key all ship while **no like route exists anywhere** (`api.ts` grep 0; `routers/` census 0 over the 44/45-decorator surface); `gallery.ts:189-199` hard-codes `const liked = true` with a monotonic unguarded local bump (un-like unreachable; `GalleryView.vue:128` can only add), under an `aria-pressed` that lies. **The server sorts by a counter nothing can increment** | ⊙ **G10 verbatim: ship the 46th operation or delete the affordance — no third option ships.** If SHIP: F.W6 is the burn seat, using the `liked_ips` dedup idiom already in-tree. If DELETE: F.W4's | Owner ruling G10 **PRECEDES**; F.W6 may not implement or delete unruled. ▲ The `aria-pressed` + re-click guard display arm is **F.W3/W4's regardless.** ▲ **Counting lock K-1: a bare `@router.` grep is blind to prefixed routers (`@gallery_router.get("/cursor")`) — read against 13/44 and cite 45/30/13, never zero.** **Dissent: challenge-C graded this BLOCKER on the carousel; K-1 killed the BLOCKER framing at MAJOR; GIG's C-3 re-grade to BLOCKER was refused by the identity guard (R-7: fold sustained, identity beats re-grade)** — GIG C-3 and GCM-2 fold BY REFERENCE, never re-booked |

### §2.10 ROUTED OUT — value-side, burned nowhere in this wave (4 rows)

**Routing law, verbatim and binding**: census §4.7 — *"value-side V-α/V-β/V-γ routed to the value.js API row per the contract (**value-tree edits are NOT fourier-wave gates**)"*; COHESION §1 SS-4 — *"neither side's edits gate the other's."* F.W6's deliverable for all four is the **routing receipt** (bounds #3): it stamps nothing on the value tree and takes no credit for any cure. Measured read-only 2026-08-28 at the fold (value side is explicitly **not** under the F.W0 lock, which re-grounds the fourier tree): `git diff --stat -- api/src src` → **empty**.

| id | sev | mechanism (witness) | cure-shape (elsewhere) |
|---|---|---|---|
| **V-α** | P1 | `revertToVersion` (service/versions.ts:121-181) computes `newHash = computeContentHash(version.name, version.colors)` (:137); the stored `version._id` was computed by the SAME expression at creation (:38), so `newHash === version._id` **always**. `createVersionRecord` (:148) then calls `findByHash(hash)` → found → returns the hash **before any insert** (:40-44), while the palette update runs `$inc: { versionCount: 1 }` unconditionally (:172). Net: a revert bumps `versionCount`, records no author attribution, adds no `depth`, and leaves `versionCount` permanently drifted. No test asserts otherwise — the suite's only revert case is `"revertToVersion throws NotFoundError on missing palette"` (`__tests__/palette-versions.test.ts:83`) | value.js API row's act; the receipt names the row and cites F.W5 §6c obligation (2) |
| **V-β** | P1 | `PaletteVersion._id` is GLOBAL: the content-hash of `(name, colors)` alone (model.ts:85-86; hash.ts:8-17 folds only `name` + `colors`, never `paletteSlug`). `findByHash` filters `{_id: hash}` with no slug scope (repository/paletteVersion.ts:13-15) and `insertIfAbsent` returns early on a hit (:44-47) — two palettes with identical `(name, colors)` share ONE version row whose `paletteSlug` names only the FIRST, so the second's `findByPaletteSlug` returns `[]`. fourier already solved this: `_id = f"{viz_slug}:{set_hash}"` (visualization.py:235, :129), tension documented in-source at :221-226 | Adopt fourier's compound per-entity `_id` **+ a test asserting two same-content palettes keep separate histories — none exists today** (G2's stated GREEN condition) |
| **V-γ** | P1 | `Palette.userSlug: string \| null` is nullable (model.ts:60) against fourier's required non-null `owner_slug` (visualization.py:119), and attribution on write is CONDITIONAL — an `if (userSlug)` guard fronts the version write at crud.ts:119, crud.ts:204 and versions.ts:147 — so an unattributable edit mutates the palette and writes **no version row at all** | value.js API row; **composes with F.W5 D9 — AA-10 and V-γ are the two halves of one R-7 clause with two burn seats** |
| **TA-4** ⊙ | **P0** (census risk 3 — the union's diff layer is one-sided), OWNER-GATED | Re-measured 2026-08-28 at the fold: `grep -rln "atomdiff\|atomDiff" api/src src` → **one hit, a test comment** (`api/src/modules/palette/__tests__/palettes-forks.test.ts`); `ls api/src/lib` → *No such file or directory*. `J-diff-shape.md §2.5` mandates the exact path `lib/crud/atomdiff.ts` — the file T.W1/TA-4 excised — and §6's close-gate assumes BOTH probes exist, so value.js's side cannot run. value.js retains only the hashing half (`computeAtomHash`, `computeAtomSetHash`) and still emits `atomSetHash` on the wire (format.ts:45, :87), with hash.ts:41-42 saying so explicitly | ⊙ **the owner rules RESTORE (a TA-4 reversal), or v2 re-scopes value.js out EXPLICITLY with §6 re-authored to a one-sided verdict** (G4). **SS-4 must name the TA-4 restoration as PREREQUISITE or re-scope explicitly** — standing edict, carried verbatim. ▲ **Declared consequence from this end: F-β's `set_hash` recompute reaches `/diff`, so any cross-repo diff assertion this burn-down makes is UNFALSIFIABLE until G4 closes.** F.W6 states the dependency and **manufactures no probe over an absent file.** Prior on record: the atom-diff pattern was "authored once (fourier), adopted twice (value.js twin)" and the twin's only survivor is a compiled corpse in an untracked build dir — E3/G4 exist to stop participation being inherited from a docstring again |

---

## §3 Gates — 19, all born RED

`F=/Users/mkbabb/Programming/fourier-analysis` (read-only) · `V=/Users/mkbabb/Programming/value.js`.

**Two reading disciplines, binding.** (1) **MEASURE-AT-OPEN** — every fourier figure and anchor below is re-confirmed by F.W0 before citation (D-19); this file re-asserts none of them. (2) **Split verdict** — a gate closes for F.W6 when the commissioned act is authored with its evidence row in the burn register; it goes GREEN only when the named landing occurs. F.W6 never claims a GREEN it did not execute.

| gate | born-RED witness | GREEN (falsifier) | owner |
|---|---|---|---|
| **FW6-G1** chain depth burned (F-α) | `grep -rn "_write_root_version" $F/api` → def visualizations.py:110 + create :220 + remix :592, the ONLY writer; `parent_hash=None, root_hash=set_hash_value, depth=0` at :138-141. `grep -rn "visualization_versions" $F/api --include='*.py' \| grep -v tests` → 1 insert (:146), 2 finds, 1 find_one + the migration script + 2 index calls | The **ruled** disposition is executed AND a fixture asserts either a depth-1 version exists with a non-null `parent_hash`, or the depth/parent/root quadruple is gone from the model | **F.W6** (F.W5 G3's GREEN-owner cell names this wave) |
| **FW6-G2** `set_hash` recompute on atom PATCH (F-β) | visualizations.py:381-383 is a bare `$set`; `_head_set_hash` (:477-480) returns the STORED value when non-empty; `palette_slug` is atom 5 (atomdiff.py:34) and a PATCH field (visualization.py:207). **No probe exists today — F.W5's G1–G20 carries no gate for clause E8** | A fixture PATCHes atom 5 and asserts (a) `set_hash == set_hash(enumerate_atoms(doc))` after the write and (b) a new version row exists | **F.W6** (gate supplied here because F.W5 states E8 without gating it) |
| **FW6-G3** `palette_slug` is clearable | `updates = {… if v is not None}` at visualizations.py:381 — PATCH can never clear `palette_slug` to null, while the remix arm twenty lines away distinguishes omit/null/rebind via `model_fields_set` (:522-528) | Three arms tested: omit inherits, null clears, slug rebinds — **using the remix idiom, not a new one** | F.W6 |
| **FW6-G4** PATCH field set = atom set (SS-C-1 write leg) | `VisualizationUpdate` (visualization.py:198-210) = five fields, `extra="forbid"`; `VisualizationPatch` (types.ts:256-262) the same five; `atomdiff.py:38` lists `animation_settings` in the atom set — the two sets differ, so `speed` is settable only at create/fork | Set equality holds, or every divergence is stated per-atom with its reason | F.W6 — **same commit as FW6-G2/G3 (one PATCH-model cut)** |
| **FW6-G5** breadcrumb redaction parity (F-γ) | `_readable_or_none` appears at visualizations.py:744 (entry row) and **not** in the ancestor walk at :784 — a bare `find_one` emitting slug/set_hash/author_slug/created_at for private and draft ancestors. Counter-example: forks.ts:167-179 | A fixture with a private ancestor asserts **no lineage field crosses the wire** for that hop (the `{kind:"unavailable", ordinal}` shape) — **or** G6's second branch (stated asymmetry with its reason) is on the record. The cycle-guard and the ≤50 cap survive | F.W6 (F.W5 G6 GREEN owner: fourier API row) |
| **FW6-G6** unsafe GET / counter provenance | visualizations.py:256 `find_one` → :268-270 `$inc {"views": 1}` → :272 `_public_doc(doc)` serialises the pre-increment doc; `grep -rn "viewed_ips" $F/api` → **0** while `liked_ips` exists | The read is safe behind an explicit verb, or the mutating-GET policy is stated against RFC 9110 §9.2.1. ▲ **The repair test may NOT assert a re-open increment (FR-GV-24 lock)** | F.W6 server arm + F.W4 client arm (VV-R2-A) — **separately credited** |
| **FW6-G7** create-path idempotency / dedupe | api.ts:371-380 sends no `Idempotency-Key`; idempotency.py:68-70 is an explicit no-header passthrough; database.py:98 `content_hash` is a PLAIN index while :140 flags carry `unique=True`; the same router's remix arm calls `replay_or_record` at :612 | The create path replays or the index is unique. ▲ **K12: closed STATICALLY — SS-13 spends no probe** | F.W6 (server arm only; the reconciliation contract is F.W3/W4's) |
| **FW6-G8** flag cascade at hard delete | `pinned_cron` — named by the softdelete docstring as cascade owner — contains **zero** flags code; the existing orphan-flag test exercises only the reporter-keyed stale-user cascade | A `content_hash`-keyed cascade runs at grace hard-delete, the docstring is corrected, and a test covers the entity-keyed path | F.W6 ⊙ — **blocked until G11 rules producer-or-retire** |
| **FW6-G9** the actor field | janitor.py:56 `_JANITOR_ACTOR = "system:janitor"` written into `ip_hash` at :95 and rendered `system:jan` by the unconditional `.slice(0,10)` at :146; `admin_audit` carries no actor field; the second writer at janitor.py:59-99 bypasses `log_audit` entirely | An `actor` field exists and **both** writers populate it. ▲ **Enumerate with the S-8 method law — `grep "log_audit("` is structurally blind to an inlined writer (K-6 killed that method).** ▲ **AA-23: no regex-action cure** | F.W6 (display legend is F.W4's) |
| **FW6-G10** tier transitions are stated transitions | admin.py:435-440 `update_many` with `{"$set": {"tier": "normal"}}` unconditional; `set_tier` writes `{tier, updated_at}` only (:160-190) and the flagged listing carries no tier predicate | The write is conditioned on the current tier and the listing carries the predicate FR-AFP-10 assumes. ▲ **β's 428-escalation cure stays killed** | F.W6 ⊙ — conditioned on G11 |
| **FW6-G11** image remediation executed | `images.py` = 7 ops, **ZERO DELETE**; blob GET :132 carries no `Depends` and ships `Cache-Control: public, max-age=86400`; thumbnails the same at :162-164; every anonymous blob GET ends in `touch_document`, bumping the janitor recency predicate | A delete/quarantine op exists, blob **and** thumbnail GETs are visibility-gated, the janitor predicate no longer rewards fetching, and the thumbnail URL is versioned. ▲ **The deployment auth-proxy question is UNPROVEN (SS-13) — the gate may not close by assuming a proxy** | F.W6 (F.W5 G8 GREEN owner: fourier API row) |
| **FW6-G12** one checked shape / one serializer | `grep -rn "response_model" $F/api/routers/admin.py` → **0**; flagged rows serialize via `_json`/`default=str` (space-separated, non-ECMA) while audit returns `AuditListResponse` (ISO with `T`); three dead server models encode the retired shape; the emitted `content_hash` is omitted client-side | One generated/checked shape; dead models deleted; one serializer. ▲ **K9: the five `?? item.slug` fallbacks STAY (defensive, not dead).** ⊙ **No codegen without FR-AFP-71's ruling** | F.W6 server arm (FR-AUL-17's shared client formatter is banked → F.W3, **never re-booked**) |
| **FW6-G13** cache identity ⊇ consumed fields | `extraction_cache_key` at image_storage.py:250-266 is a closed 10-field `json.dumps` literal omitting `ml_threshold` / `ml_detail_threshold`; the handler short-circuits on the hit **before** `compute_contours` | **The named fixture: `contour_hash` INSTABILITY across an ML-threshold change** (also the SS-13 probe). Rider m-18's `* 0.6` coupling is not preserved by the cure | F.W6 (F.W5 G13 GREEN owner: fourier API row) |
| **FW6-G14** contour provenance / bounds on write | a `source="editor"` asset carries no `extraction_cache_key`, so exiting the editor re-runs extraction and the server cache-hits the ORIGINAL; `store_contour_asset(xs, ys, req.image_slug, source="editor")` passes no `image_bounds`, the param defaults `None` and is written verbatim (image_storage.py:290/:318); backfill is GET-only | Extraction cannot overwrite a `source="editor"` asset, and derive-on-POST or backfill-on-write is executed per the ruling. **The client orchestrator move (L-2) is F.W3/W4's and is not credited here** | F.W6 |
| **FW6-G15** canonical-geometry TRIPWIRE (**stays RED by design**) | 3.7% of shipped `moon.json` points within 1.0 unit of any extracted point; p90 37.6 vs 1.23-unit spacing; crescent-alone bbox strictly contains the shipped bbox; `.gitignore:53 scripts/*` swallows the only claimed regenerator and its `raw-contours.json`; `grep -rn "def order_contours" $F` → **0** (the script imports a symbol no module defines); shipped 50/10 vs the script's 100/12 | **Closes ONLY when the true source is named or the artifact is re-authored WITH G5c's diagnostics row. ▲ DO-NOT-REGENERATE on `master` — a regeneration attempt is a gate FAILURE, not a pass, and revives L-B1 + L-B2/C-2 at BLOCKER.** Track the seam (G2c) before anything else | **F.W6 / build lane** (F.W5 G16's GREEN-owner cell names this wave by name) |
| **FW6-G16** value-side routing receipt (**zero value-tree bytes**) | No receipt exists: V-α / V-β / V-γ / TA-4 are itemized in F.W5 §6c as value-side obligations and no fourier-side artefact records their routing. Census §4.7: *"value-tree edits are NOT fourier-wave gates"*; COHESION §1 SS-4: *"neither side's edits gate the other's"* | The receipt (bounds #3) names the value.js API row per obligation, cites F.W5's list, and `git diff --stat` over `$V/api/src` + `$V/src` shows **zero** bytes moved by this wave (measured empty at the fold — it must still be empty at close) | F.W6 |
| **FW6-G17** no double-booking / carry closure (M-25) | This spec cuts **24 identities** to F.W6 while the same ids are cited in F.W5 §4 (E2/E5/E6/E7/E8/E11/E12/C2/C5/D9/D10/D11/D12/D3/D2/B4/E10/§G G1c) — the overlap is **by CITATION** and must be proven so. Measured: bare `F.W6` → **0** in the registry; the span `F.W5-W8` → **120 / 26 records**. The failure class is live: FR-GIG-5's cure was nearly credited to a wave that did not perform it (F.W5 §1b's standing bar) | Set-difference runs **both** directions: every CARRY id lands in §2 or §5, and **no row credited to F.W6 is also credited to F.W1, F.W3/W4, F.W5 or F.W8**. `grep -o` the id column of the burn register (bounds #2) and diff against §2 + §5. Zero silent drops | F.W6 itself (close unit) |
| **FW6-G18** owner rulings present before any conditioned burn | Seven rulings owed and unreturned — **G4** (TA-4 restore-or-rescope) · **G7** (trie-vs-KISS) · **G10** (like verb) · **G11** (moderation producer-or-retire) · **G5** (born-visibility) · **E7** (hard-delete arm) · **FR-AFP-71** (codegen vs hand-typed twins) — plus intake **OG-F1 / OG-F2**. `OWNER-RULINGS-F.W5.md` does not exist | Each conditioned row either carries its ruling or is explicitly deferred with its honest default recorded. **F.W6 may not burn a row conditioned on an unruled question — the whole FR-AFP band rides G11** | owner → F.W5 unit e; **F.W6 consumes, never re-rules** |
| **FW6-G19** F.W0 re-grounding (D-19) | At the F.W5 authoring pass: `git -C $F rev-parse --short=8 HEAD` → **`cd26c653`**, `git -C $F status --porcelain \| wc -l` → **28** dirty paths no commit holds (GAB-13); the census pin `14d83356` does not resolve; glass-ui 4.0.0 ships a syntactically corrupt `dist/styles/index.css` (fr-PaperSidebar M1); F8-REACH-01/02 open | F.W0's re-grounding receipt exists and **every anchor cited by this wave is re-resolved against it**. **If F.W0 fails to re-ground, the wave HALTS — it does not proceed on stale bytes** | F.W0; F.W6 cites |

---

## §4 Sequencing — locks and cross-edges (declared from this end)

**Execution shape.** Under the gate, each burn unit's product act is a **commissioned ask** in the bounds #4 letter plus its burn-register row; the fourier edits land in fourier's own sub-session (COMMISSION §2) after the begin-word. Nothing in this wave opens product source in either repo.

**In-wave order** (serial; each commits before the next opens):

`a` preconditions (FW6-G19 receipt + F.W5 clause set + FW6-G18 rulings) → `b` **the PATCH/chain one-cut** (F-α · F-β · SS-C-1 — G1/G2/G3/G4) → `c` privacy (F-γ + FR-AFP-4 — G5/G11, one cut) → `d` counters + idempotency (G6/G7) → `e` **moderation band, only if G11 ruled** (G8/G10/G12 + FR-AFP-7/33) → `f` audit actor (G9) → `g` contour + geometry (G13/G14/G15) → `h` liveness strikes (PP-DEADSEAM, only what E16/G7 does not touch) → `i` routing receipt + carry closure (G16/G17).

**Ordering and cure-integrity locks, carried whole:**

1. **F.W0 substrate pre-gates FIRST** — no line anchor, no HEAD figure, no "LIVE at HEAD" citation until the re-grounding receipt exists (28 dirty paths, unresolvable census pin, corrupt glass dist, F8-REACH-01/02). **F.W0 failing to re-ground HALTS this wave.**
2. **F.W5 STATES, F.W6 BURNS — no double-booking** (F-W5 §6c verbatim on F-α; extended here to every clause whose GREEN owner is F.W6 or "the fourier API row").
3. **ONE-CUT LAWS**: F-β ⊕ SS-C-1 in one PATCH-model change (else the second reverts the first's invariant) · F-γ ⊕ FR-AFP-4 as one privacy limb (a redacted breadcrumb over a world-readable image is not a cure) · FR-AFP-66 ⊕ FR-AFP-33 (cascade without bound leaves the cost; bound without cascade leaves the growth).
4. **G11 is the ADMISSION GATE for the whole FR-AFP band**, and **precedes F.W1's sizing wherever FR-AFP grades are load-bearing** (ruling 5).
5. **F.W1 stays ATOMIC land-or-lose** — producer bump + 162-site prop rewrite + copied→status triple in ONE change, else four zero-console-error e2e gates go red (FR-EQR-3); F.W1 precedes F.W3/W4 (§2 law). **Reciprocal credit law, both directions: F.W1 must NOT be credited for the pagination-drain cure (FR-GIG-5), and F.W6 claims no F.W1 credit.**
6. **Killed cures stay killed**: AA-23's regex action · β's 428-escalation · the K9 fallback deletion · SE-05's "just delete it" for the `window` handoff · m-7's 422-straddle. **K-6/S-8**: absence-proofs enumerate the surface. **K-1**: 13/44, cite 45/30/13, never 30 alone or zero. **K12**: B-2 closes statically. **FR-GV-24**: no re-open-increment assertion.
7. **TRIPWIRE**: DO-NOT-REGENERATE `moon.json` / `sun.json` on `master`; track the seam (G2c) before any geometry act.
8. **No projection strike that is really a compression decision** until G7/E16 rules; the anti-tree KISS guardrail (`atomdiff.py:12-14`, quoted in both trees) is the incumbent.

**Cross-edges** (declared from this end; each owed a reciprocal sentence in the named file):

| to | direction | this end's declaration |
|---|---|---|
| **F.W0** | **DEPENDS — HARD** | Re-grounding of the 28-path dirty worktree (GAB-13), the unresolvable pin `14d83356`, glass-ui 4.0.0's corrupt `dist/styles/index.css` (M1), F8-REACH-01/02. **Failure HALTS; no proceeding on stale bytes** |
| **F.W5** | **DEPENDS — HARD; F.W5 STATES, F.W6 BURNS** | F.W5 §12 declares it blocks F.W6 ("the burn-down needs the chain clause"); §6c fixes the split. Extended to every clause whose GREEN owner is F.W6 / the fourier API row: C2, C5, D9, D10, D11, D12, E5, E6, E7, E8, E11, E12, B4/E10, §G G1c–G9c, and D2/D3 conditionally. **Declared gate gap: F.W5 states E8 (F-β / SS-C-1) but carries NO gate across G1–G20 — F.W6 supplies FW6-G2/G3/G4 rather than read the omission as absence of scope.** F.W6 authors no clause and reverses no ruling |
| **F.W1** | **NO GATING either way, one exception** | The exception: **G11's producer-or-retire PRECEDES F.W1's sizing** (ruling 5). F.W1's atomicity and the FR-GIG-5 non-credit lock are untouched |
| **F.W3 / F.W4** | **F.W6 EMITS client/display arms — one home, two citations** | Emitted and **not claimed**: AA-10's sentinel-aware branch + legend · AA-5/AA-24 display taxonomy · AA-6 placeholder honesty · FR-AFP-8's one-token `item.slug` label · FR-AFP-9 / FR-GV-34 truthful copy (diction rides FR-AFP-35) · FR-AFP-33's client collapse (→ F.W1/W3) · FR-AFP-70's reflect-and-disable interim · FR-AFP-59's tier render · VV-R2-A's publish→`store.setVisibility` · **VV-R2-B's single wiring unit** (route→`loadVisualization`, publish→`setVisibility`, delete→the store action, incl. GM-M4's `softDelete`/`restore`) · GCM-1/GCM-25's `/v/` routing repair · **L-2's orchestrator move** (REQUIRED; urgency rider at the data-loss class) · the like verb's `aria-pressed` + re-click guard · FR-GV-13's `?owner=me` control · **FR-AUL-17's shared formatter (already banked → F.W3; do NOT re-book)** |
| **F.W7** | **F.W6 must NOT pre-empt** | ⊙ G7/E16 unruled; F.W6 carries the documented default — **no trie; whole-snapshot duplication is the recorded shipped behaviour.** `trace` (M-β4) and `reconstructed_points` (L·m-6) strikes wait on the ruling. The standing anti-tree KISS guardrail (`atomdiff.py:12-14`, quoted both trees) binds until the owner moves it |
| **F.W8** | **F.W6 BURNS SERVER DEFECTS; F.W8 WIRES CLIENTS + RUNS CONFORMANCE** | Not F.W6's, declared so no re-cut drifts: FR-GV-8 (7 of 13 `/api/visualizations` ops — remix, publish, unpublish, forks, provenance, diff, versions — with zero client function) · FR-GV-27's `/publish`/`/unpublish` wrappers · FR-GFC-4's missing `tier` param at every layer · FR-USB-15/23/24 (session truth, dead session subsystem, the 429 retry posture — C3/C4's cures land at the store/api seam; **C4's one-controller lock binds any retry change**) · D1's per-operation dispositions. ▲ **Counting lock K-1: a bare `@router.` grep is blind to prefixed routers (`@gallery_router.get`) — read against 13/44 and cite 45/30/13, never 30 alone** |
| **F.W9 / F.W10** | **F.W6 EMITS coverage obligations** | A burn-down with no probe repeats exactly the defect FR-AUL-13 convicts (a client written against a contract DOCUMENT rather than the router). Emitted: AA-44 (zero frontend tests; nine e2e specs never touch the audit tab while the backend pins its half at `test_janitor_audit.py` — the one-sided seam that let AA-5/AA-6/AA-24 stay open) · FR-AFP-49 = AA-44 = FR-AUL-16 (zero automated coverage of any kind; all four AFP blockers invisible to every gate the repo runs) · FR-AFP-66's orphan-flag test exercising only the reporter-keyed path · G2's absent same-content-palette history test · G5's per-side create-visibility tests |
| **the value.js API row** (CO-SIGNER) | **F.W6 ROUTES, never burns** | V-α, V-β, V-γ and ⊙ TA-4 leave as the routing receipt citing F.W5's obligation list. Census §4.7 + COHESION §1 SS-4 binding. **Declared consequence: F-β's `set_hash` recompute reaches `/diff`, so any cross-repo diff assertion this wave makes is UNFALSIFIABLE until G4 rules TA-4 restore-or-rescope.** The reciprocal edge is REQUESTED from that end, not written here |
| **SS-4** | **F.W6 IS SS-4's burn half** | SS-4 **FLAGS THE OWNER RULINGS INLINE** — G7 trie-vs-KISS · G4 TA-4 · G10 like verb · G11 producer-or-retire · G5 born-visibility · E7 hard-delete arm · FR-AFP-71 codegen-vs-hand-typed-twins (+ intake OG-F1 freeze-with-adoption / OG-F2) — and **names the TA-4 value-side atomdiff restoration as PREREQUISITE or re-scopes explicitly.** Two registry-integrity carries: GCM-10's "MF-9" companion cite resolves to **nothing** repo-wide and is **STRUCK** unless SS-4 resolves it; the challenge-row-census vs roster-census diff (which caught the L-27 drop) is commended as a standing gate |
| **glass-ui BH relay (SS-6)** | **DECLARED EMPTY** | F.W6 carries **ZERO** producer rows — declared so (a) no glass-producer row is smuggled in as a server cure and (b) no server cure is discharged as a frontend hack. Any producer-shaped finding leaves as a letter to the standing glass-ui BH inbox (the owner's fond, a standing formation invariant) |
| **X·parse-that** | **FORBIDDEN EDGE** | Direct parse-that→fourier routing is forbidden; anything parser-shaped routes through the megatranche, never across |
| **SS-13 / live-probe lane** | **F.W6 DEFERS MAGNITUDE ONLY** (never used to close a gate) | VV-R2-A's observable phantom view (publish fresh, read the card count; derivation: born at 1) · FR-AFP-4's deployment auth-proxy question · FR-AFP-33's aggregate cost + β's sort-index note · M-14's end-to-end contour-loss reproduction · B-4's `contour_hash` instability across an ML-threshold change · L-B1's "what `moon.json` WAS generated from" (UNDETERMINED). **Probe parsimony binds** |
| **`/Users/mkbabb/Programming/fourier-analysis`** | **READ-ONLY, ALWAYS** | Zero fourier bytes. Immutable beside this spec (E-1/E-3): `lane-crud.md`, `INTAKE-ADJUDICATION-2026-08-03.md`, all 66 `fr-*.md`, `J-diff-shape.md` v1 (271 lines — v2 supersedes by reference and never patches it). `COHESION.md` is root-authored at the boundary and excluded. `scripts/dev/dev.sh` is an unowned dirty row and is never staged |

---

## §5 Excluded — nothing silently dropped; every exclusion reasoned

| excluded | reason |
|---|---|
| **SS-C-1's READ leg** (`loadVisualization` zero callers) | banked at **fr-GalleryCardModal GCM-1** at blocker weight → F.W4 + F.W5-W8 rider. The axis split was adjudicated deliberately; re-merging the halves re-books GCM-1 |
| Every client/display arm listed in the F.W3/F.W4 cross-edge (AA-10 legend, AA-5/AA-24 taxonomy, AA-6 placeholder, FR-AFP-8's label, FR-AFP-9/FR-GV-34 copy, FR-AFP-33's collapse, FR-AFP-70's interim, FR-AFP-59's render, VV-R2-A/B wiring, GCM-1/GCM-25 routing, **L-2's orchestrator move**, `aria-pressed` guard, FR-GV-13's `?owner=me`, FR-AUL-17's formatter) | one home, two citations — **F.W6 emits, never claims** (the FR-GIG-5 mirror, standing bar) |
| **F.W8's client-wiring set**: FR-GV-8, FR-GV-27, FR-GFC-4, FR-USB-15/23/24, D1's per-operation dispositions | F.W8 wires clients and runs conformance; F.W6 burns server defects. Declared so no re-cut of the `F.W5-W8` span drifts a row into two homes |
| **F.W7's compression design** — `trace` and `reconstructed_points` strikes | ⊙ G7/E16 unruled; the documented default (no trie, whole-snapshot duplication) is the incumbent until the owner moves it |
| **Regeneration of `moon.json` / `sun.json`** | the TRIPWIRE: an attempt is a gate FAILURE, not a pass, and revives L-B1 + L-B2/C-2 at BLOCKER |
| **Owner rulings themselves** — G4, G5, G7, G10, G11, E7, FR-AFP-71, OG-F1, OG-F2 | owner acts: **flagged inline throughout, never presumed, never pre-answered.** F.W6 consumes rulings; it does not rule, and it may not burn a conditioned row unruled (FW6-G18) |
| **Codegen / contract-module split** | ⊙ FR-AFP-71 is owner-gated; v1 chose hand-typed twins explicitly and v2 may not silently reverse it |
| **Deleting the five `?? item.slug` fallbacks** · the **AA-23 regex-action** cure · **β's 428-escalation** · **SE-05's "just delete it"** · **m-7's 422-straddle** · **D-1's skeleton cure** | KILLED at their registers (K9 · AA-23 · self-killed by its author · G6c · K-3 · K5) — no repair is inherited from a killed corpus |
| **Any value-tree byte** (`$V/api/src`, `$V/src`) for V-α/V-β/V-γ/TA-4 | census §4.7 + COHESION §1 SS-4: value-tree edits are not fourier-wave gates; **FW6-G16 fails if any byte moves** |
| **FR-GV-1 · GIG C-3 · GCM-2 · i-7 · GCM-52 · ImageUpload row 26 · FR-AFP-18 · FR-AFP-40/-69 · GCM-55 · VV-R2-B · ContourPreview row 28** | folded **BY REFERENCE** into their carrying identities above — cited, never re-booked (the fold is the identity guard, not a demotion) |
| **The 3 routing-law boilerplate lines** among the 120 `F.W5-W8` mentions | boilerplate mints no rows (the RE-CUT-0 dissent's explicit warning) |
| **GCM-10's "MF-9" companion cite** | resolves to nothing repo-wide — **STRUCK unless SS-4 resolves it**; no cure is quoted through it |
| **Glass-producer rows; latex-paper rows; keyframes rows** | SS-6 / the LATEX-PAPER register / KF's seat — producer rows never become frontend hacks, and none is smuggled in as a server cure. The paper register's same-commit riders (fr-PaperSearchModal D-1+D-3+outline:none · PAW-44/LAW-3 restore-only-with-background-cure · MPC-31/MPC-3⊕10⊕13⊕8⊕22 one-cut law · FR-MSP-6 two-channel lock) travel with their rows, which do not land here |
| **Every fourier-tree write** | COMMISSION §2 — their sub-session. F.W6 asks by letter and verifies receipt; it never edits |
| **Any product-source open in either repo; any status advance beyond `planned`** | the execution gate — nothing opens product source until the owner's begin-word |

---

**Close act**: when FW6-G1..G19 are GREEN (G15 closing only by naming the true source or re-authoring the artifact with diagnostics — never by regenerating), F.W6 stamps its burn register, lands the routing receipt, relays the commissioned-ask letter, and ledgers it in INBOX (E13 — no wave closes with UNREAD mail). **L-18 applies**: no wave is ACCEPTED until two quartet gestalt passes adjudicated by a fresh Fable instance; the bases that apply here — a burn credited to a wave that did not perform it; a "cure" that is really a client arm; a gate argued green over an unruled owner question; a redaction that leaves the asset leaf open; a projection strike that was a compression decision; a set-difference declared empty without running both directions. Until then: **planned**, born-RED, every witness named above.
