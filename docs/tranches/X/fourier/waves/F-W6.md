# X.F.W6 — Provenance defect burn-down (library half)

⟨X·F · **status: planned** · half: library · Opus-drafted; **L-20 repair round 2 applied 2026-08-28** under `docs/tranches/X/fourier/conformance/PASS-2/RULINGS-2.md` (R2-1a · R2-1b · R2-1-LAW · R2-2 · R2-5 · R2-9 + the P2-9 cluster) against its local register `docs/tranches/X/fourier/conformance/PASS-2/F-W6-CHECK.md`; round 1 (PASS-1 `RULINGS.md`: R-1e · R-3 · R-4b · R-5 · R-6 · R-9.1 · R-10) is retained where round 2 did not supersede it (M-25: dedupe by identity, cure-shape locks and sequencing edicts carried verbatim, routed by mechanism) · **QUOTE-BY-COMMAND (R2-1-LAW), binding on this file**: every cross-spec and corpus quotation below enters as the pasted output of a `grep`/`sed` run **this round**, with its **⟨cmd⟩** provenance note beside it; a "verbatim" label without a ⟨cmd⟩ note is a defect per se, and a rulings file is a cure-shape, never a quotable authority — **and the law is discharged by RUNNING it, not by wearing it: at round-2b every standing ⟨cmd⟩ receipt was re-executed against the live bytes and **four** did not reproduce — and 7 further quotations were found carrying no ⟨cmd⟩ at all, every one of them wrong (eleven quotation cures: five right-words/wrong-file, two anti-rename-inside-quotes, three added-emphasis, two commands that could not produce their own pasted output; register at the round-2b close note). Sibling specs were re-written by their own seats AFTER this file was authored, so a receipt true when typed is not thereby true now** · **ANCHOR IDIOM (R2-2), binding**: cross-spec receipts cite **stable anchors** — §-headings, gate ids, clause ids, banked row ids, row labels — **never line numbers into a live sibling**; line numbers survive only for the frozen `fr-*.md` corpus and for this file's own intra-file coordinates, marked *at this writing* · **QUARANTINE RULE for struck matter (round-2b, standing here)**: where an erratum below recites a **retracted** address or a **retracted** sentence so the correction is auditable, the recited matter is quarantined by its strike and is **not a citation and not an authority** — no seat may lift, follow or re-quote it. Four line-cites into live siblings (`F-W0.md:199`/`:204`, `F-W1.md:276`/`:294`) and one retracted sentence survive in this file **only** in that form; a downstream grep hit on any of them resolves to the erratum that killed it. Struck matter kept unmarked would re-mint what the strike removed, so it is marked, and marked matter is not cited · **provenance stated honestly (R-3)**: there is **no `F-W6-CARRY.md`** — `ls docs/tranches/X/fourier/carry/` → `F-W1-CARRY.md`, `F-W4-CARRY.md`, the only two carries in tree — so this spec's closure operands are the **66-record registry itself** and the census beside it, never a per-wave carry; every earlier "verified row-for-row against the CARRY / consumed whole / zero silent drops" claim in this header is **STRUCK** as a verification receipt over a nonexistent artefact · **no registry row names this wave**: `grep -rhoE 'F\.W6' docs/tranches/V/megatranche/registry/adjudicated/fr-*.md | wc -l` → **0**, so routing reaches this wave ONLY through the `F.W5–W8` span, measured **spelling-agnostically** per R-6 — `grep -rhoE "F\.W5[-–]W8"` (U+002D **and** U+2013) → **192 occurrences / 49 records** (hyphen 120/26 ⊕ en-dash 72/24; **23 records en-dash-ONLY**), of which **23** occurrences are routing-law boilerplate that mints nothing (**round-1's "3" was itself un-measured and is STRUCK** — ⟨cmd⟩ `grep -rnE "F\.W5[-–]W8" fr-*.md | grep -c "tri-package uplift"` → **23**); the corrected span roster is re-cut at **§2.11**, cross-read against **census §4, item 7 + the lane-crud SEAM TABLE + intake X-8** · **L-20 repair round 3 applied 2026-08-28** under `docs/tranches/X/fourier/conformance/PASS-3/RULINGS-3.md` (**R3-5.2** four band spellings + the ten escapes landed + the upper operand enumerated · **R3-7.1** `FR-EMT-20` booked · **R3-8.2** FW6-G19's witness in canonical form · **R3-9.3** the §2.11b discriminator squared with the landings, then run and printed · **R3-3.9** purge share Q-1..Q-7) against its local register `docs/tranches/X/fourier/conformance/PASS-3/F-W6-CHECK.md`; the round's **standing amendment R3-3.10** binds here too — *no ⟨cmd⟩ whose output is a COUNT of a live sibling or of this file itself may be banked*, so sibling receipts below state **classification, set-membership or `grep -o` word-output**, and the frozen `fr-*.md` corpus is the only surface a count may be taken over. **The twin's hand-off is CONSUMED, not re-derived**: `F-W3.md` **§X.1-v4** published FIRST this round (R3-1a) and is the definitive file-criterion partition; where a row of this wave touches a file that partition holds, the citation is to §X.1-v4 and F.W6 books nothing on the twin surface · L-20 validated against `docs/precepts/instructions/` (TRANCHE-AND-WAVE-SPEC · tranche/WAVE_SPEC · STYLE · ORCHESTRATION · CONSUMING · LESSONS-LEARNED) · idiom `docs/tranches/X/waves/W0..W11.md`, cut to the lean `parse-that/waves/` + `F-W10.md` form · substrate re-measured read-only 2026-08-28, receipts inline · **L-20 repair round 4 applied 2026-08-29 under `PASS-4/RULINGS-4.md` — READ `§0` FIRST: it strikes this file's roster integer, adopts `conformance/CENSUS-CANONICAL.md` as the SOLE census operand (record-side figure for F.W6: **ZERO**), pins the toolchain and the base block (R4-2), and hash-pins every live sibling quoted below (R4-8.3). Every ⟨cmd⟩ in this file was re-run under `/usr/bin/grep` at that round; where a round-3 receipt could not produce its own paste, the command was re-cut and the output re-pasted, never the reverse**⟩

**Alias law**: `F.W6` denotes `X.F.W6` — a search alias, never a rename. The spec lands as `F-W6.md` beside its authored siblings in `docs/tranches/X/fourier/waves/`; every banked id keeps its identity for life.

---

## §0 ROUND-4 HEADER — the census re-scope, the toolchain pin, and the hash-pinned sibling set

**L-20 repair round 4 applied 2026-08-29** under `docs/tranches/X/fourier/conformance/PASS-4/RULINGS-4.md` (**R4-1.7 → R4-4.2** · **R4-2** · **R4-5** · **R4-6.4** · **R4-7.1** · **R4-7.3** · **R4-9.3** · **R4-9.5** · **R4-10**) against its local register `PASS-4/F-W6-CHECK.md`. Rounds 1–3 are retained where round 4 did not supersede them (M-25). The round's base, stated once so the next seat does not re-derive it: **round 1 was blind to a dash; round 2 to a bracket and to the union's own name; round 3 to the records whose band line is boilerplate; round 4 stops re-cutting detectors altogether** — the census is no longer this file's to derive.

### §0.1 THE CENSUS OPERAND (R4-5 ⊕ R4-10 ⊕ R4-3, binding on every gate below)

▲ **`../conformance/CENSUS-CANONICAL.md` is THE census of record. A gate in this file that cites any other census operand is DEFECTIVE on sight** — including this file's own prior arithmetic, including any `PASS-n/F-W6-CHECK.md`, including the superseded `formation/fourier/CENSUS-2026-08-03.md` **as a denominator** (it survives here only as the *routing-law authority* quoted at §2.10 and FW6-G16, never as a count).

1. **THE 132 IS STRUCK (R4-5).** It was unproducible from this file's own §2.11c counting rule — 111 with the rule applied, 153 with it ignored, never 132 — so the set-difference FW6-G17 specifies had **no determinate denominator**. It is not re-derived, not corrected, and not replaced by another integer of this file's own manufacture. Every *"roster of 132"*, *"the 132 identities"* and *"re-denominated to 132"* below is struck in place and re-based on this section.
2. **F-W6's RECORD-SIDE FIGURE IS ZERO.** The canonical settles by enumeration what four passes measured against rows that do not exist — ⟨cmd⟩ `grep -o 'F\.W6., .F\.W8. and .F\.W10. are likewise named by \*\*no record\*\*' "$C"` → *"F.W6`, `F.W8` and `F.W10` are likewise named by **no record**"* — the paste **begins where the pattern begins**, so the record's opening backtick is outside it; a leading `` ` `` added to make the quotation look whole would be the prepend defect this round cures at §2.11 row 21 (canonical §4.1). **No adjudicated record routes `F.W6`.** This wave's population is therefore **folds, gate operands and commissioned acts** — never a record-side roster — and it is enumerated by register (§2.1–§2.10 booked · §2.11 / §2.11c cited-and-folded · §2.11d the holder-cited · §2.11a the negative operand · §5 excluded), never counted into a denominator.
3. **THE RECORD-SIDE DENOMINATOR OF THE BAND IS THE CANONICAL'S, NOT THIS FILE'S.** ⟨cmd⟩ `grep -o '^### F\.W5 — \*\*28 rows\*\*' "$C"` → *"### F.W5 — **28 rows**"* · ⟨cmd⟩ `grep -o '^### F\.W5-W8 — \*\*90 rows\*\*' "$C"` → *"### F.W5-W8 — **90 rows**"*. **118 record-side rows in the band, all homed at F.W5 or the band — none at F.W6.** Where this file needs to know whether an id is in the band, it reads the canonical §2 roster; it does not re-run a detector.
4. **THE FOURTH DETECTOR ARM IS RE-CUT UNSCOPED (R4-9.5).** Round 3 scoped bare-`F.W5` by `comm -13` against the band arm, which by construction can only return records carrying **no** band form at all — so a record whose only band line is routing-law boilerplate was excluded from the fourth arm while contributing nothing to the first three. The five records round 3 named were exactly that population. Unscoped, the bare spelling reaches **eighteen** records, not five. **The consequence is booked at §2.11d, and the detector question is then closed for good by clause 1–3 above: the canonical is the operand, so no fifth arm can be needed.**

### §0.2 PORTABLE-COMMAND LAW (R4-2) — the toolchain pin and the ONE base block

**Every ⟨cmd⟩ in this file executes on `bash` + BSD `/usr/bin/grep` / `sed` / `awk` / `cut` on the recorded host.** In this shell bare `grep` resolves to **ugrep**, which errors on some bounded-wildcard idioms and silently differs elsewhere; **`/usr/bin/grep` is the pinned binary and every receipt below was run under it.** No `-P`, no `\K`, no lookaround — **PCRE does not exist on the pinned binary (exit 2)**, and this file's receipts are `-E`/`-F`/BRE throughout. *(No self-probe is banked for that property: a `grep` for the forbidden token over this file matches the sentence that forbids it, so the honest control is the pass-5 seat's on-sight conviction under R4-2.5, not a receipt this file can rig.)* No `.{n,m}` with **m > 255** — the BSD bound; the widest bound standing in this file is `.{0,190}` and every windowed extraction wider than a clause uses the **two-stage** form (`grep -n` to locate → `sed -n 'Np' | cut -cA-B` to print), never a giant bounded wildcard.

**THE BASE BLOCK — every variable any ⟨cmd⟩ in this file consumes, defined here, once (R4-2.3):**

```
F=/Users/mkbabb/Programming/fourier-analysis                                    # READ-ONLY, always
V=/Users/mkbabb/Programming/value.js
W=$V/docs/tranches/X/fourier/waves                                              # waves/F-W*.md, carry/ is $W/../carry
G=$V/docs/tranches/V/megatranche/registry/adjudicated                           # the 66 frozen fr-*.md
M=$V/docs/tranches/V/megatranche                                                # formation/, audit/
C=$V/docs/tranches/X/fourier/conformance/CENSUS-CANONICAL.md                    # THE census
H=$V/docs/tranches/X/COHESION.md
```

A ⟨cmd⟩ below written against a bare relative path (`waves/F-W5.md`, `fr-*.md`, `formation/fourier/lane-crud.md`, `COHESION.md`) runs from the base its own row names and resolves to `$W` / `$G` / `$M` / `$H` respectively. **One command, one base** (R4-2.4): no receipt in this file splices two commands over one undisclosed base.

### §0.3 HASH-PIN TABLE (R4-8.3) — every LIVE sibling this file quotes, pinned at quote time

A cross-file paste that names no version is deceptive the moment the sibling is rewritten — which is exactly how the round-2b receipts died. **Every ⟨cmd⟩ in this file that reads a live sibling is pinned to the hash below, computed by this seat AFTER its final edit (`shasum -a 256 <file> | cut -c1-12`).** A later reader whose `shasum` differs knows the sibling moved and re-runs rather than trusting the paste; a seat that writes after these are stamped re-stamps them.

▲ **AND THE PIN EARNED ITSELF INSIDE THIS ROUND — disclosed, not smoothed over.** This seat stamped the table at the start of its sitting, again after its content edits, and again at its final byte. **Siblings moved between all three stamps** — `F-W5.md` `d9f3c812cf36` → `72f65042fdac` → `d4f47eb3ec4f`; `F-W0.md` `69b05ccbb320` → `cd64d6580098`; `F-W1.md` `83ffe83cb37f` → `0ee11f1fedaa` → `839d9964010b`; `F-W9.md` `41ecdf52b78c` → `71bc362d8004` → `eda1fab4e865` — because their own round-4 repair seats were writing concurrently with this one. **Every quoted WORD was re-verified byte-true against the newest bytes at each stamp and every one held; one banked ADDRESS did not** (`grep -nF 'FR-GV-13 (=MISS-LC-2)' F-W5.md` → `:162`, then `:177`), and that cell was re-cut to `grep -oF` so it banks no line at all. **A hash pin does not stop a sibling moving; it makes the movement DETECTABLE instead of silent — and inside one sitting it detected eight moves across four files.** ⊙ **Consequence, stated plainly rather than papered over: the table below is THIS SEAT'S CLOSE-TIME stamp, not a settled cert.** Under **R4-8.1's LAW E** the authoritative hashes are the **purge seat's**, computed after every other seat's final byte, and **any seat writing after that cert voids it by construction**. This table is the mechanical check that survives while ordering is still slipping — and ordering slipped throughout this round, which is the empirical case for LAW E rather than an argument against it.

| sibling (live) | sha256[0:12], this seat's close-time stamp |
|---|---|
| `waves/F-W5.md` | ~~`d4f47eb3ec4f`~~ → **pinned per PIN-PURGE-CERT.md §TABLE (2026-08-29)** ⟨moved⟩ |
| `waves/F-W0.md` | ~~`cd64d6580098`~~ → **pinned per PIN-PURGE-CERT.md §TABLE (2026-08-29)** ⟨moved; **moves again in repair round 5** — REST-01..REST-08⟩ |
| `waves/F-W1.md` | ~~`839d9964010b`~~ → **pinned per PIN-PURGE-CERT.md §TABLE (2026-08-29)** ⟨moved; **moves again in repair round 5** — BATCH-W1⟩ |
| `waves/F-W3.md` | **pinned per PIN-PURGE-CERT.md §TABLE (2026-08-29)** ⟨held through pass 5; **moves in repair round 5** — REST-09..REST-14⟩ |
| `waves/F-W9.md` | ~~`eda1fab4e865`~~ → **pinned per PIN-PURGE-CERT.md §TABLE (2026-08-29)** ⟨moved; **moves again in repair round 5** — REST-52..REST-64⟩ |
| `carry/F-W4-CARRY.md` | `bf1da6fe1e7e` ⟨unmoved⟩ |
| `../COHESION.md` | ~~`91c6d974db9b`~~ → **`956e71a83e32`** ⟨moved; re-measured this seat⟩ |
| `../conformance/CENSUS-CANONICAL.md` | ~~`a450b8e9f80e`~~ → **`3e0a9acb3381`** ⟨moved — **errata round 5**, §0.4 E5-1..E5-18⟩ |

⌧ **RE-STAMPED AT REPAIR ROUND 5 (REST-25; P5-6), AND THE FINAL STAMP IS THE PURGE SEAT'S.** **Five of the eight pins were stale at the pass-5 settle** — F-W5, F-W0, F-W1, F-W9 and the census — and the values above are that settle's, not this round's: **five of these files are re-written by repair round 5 itself**, so the authoritative stamp belongs to the purge seat that runs LAST, after every other seat's final byte. ⊕ **The R4-8.2 certificate that does not exist is still owed**: no purge-seat sha256 cert has ever been written for this wave, so LAW E's authoritative stamp remains undischarged. ⊘ **Record the mitigation, because it is the finding that acquits the mechanism**: **thirteen live-sibling receipts in this file reproduced despite five pins having moved — the pin did its job; movement was detected, not silent.**

Pinned for completeness, though declared immutable at §1/§4 (E-1/E-3): `formation/fourier/lane-crud.md` `3bcc387cbb2a` · `formation/fourier/CENSUS-2026-08-03.md` `684de3d8a8a0` · `audit/codex-provenance/INTAKE-ADJUDICATION-2026-08-03.md` `c61b3ed01141`. The 66 `fr-*.md` are frozen by decree and carry no pin; **that freeze is why they remain the only surface over which this file may take a count at all** (R3-3.10, standing).

**— end §0. The spec's standing preamble laws resume below, unchanged from rounds 1–3 except where §0 supersedes them; §1 follows. —**

---

**Record-qualification law (R-5, binding on F.W5 and F.W6 alike).** **At least TEN** banked tokens collide across records (UNION U-12 ⊕ repair round 5, REST-20): **`M-13` · `L-B1` · `L-M3` · `C-17` · `C-18` · `B-1` · `B-2` · `C-2` · `M-10` · `M-9`**. ⟨**`M-10` and `M-9` added at repair round 5 (P5-4/P5-11, HIGH).** `M-10` carries **18 hits in this file** across `fr-ContourSettings M-10` (F.W3) · `fr-BasisSelector M-10` (F.W3) · the `fr-FourierShapeExtractor` innerPoly **rider** inside banked head `D-16` (F.W4); `M-9` spans `fr-BasisSelector M-9` ⊙ (row 7) · `fr-FourierShapeExtractor M-9` (row 36) · `fr-GalleryView FR-GV-7`'s `M-9` alias (row 51). **The file documented both collisions and declined to act on them** — row 36 carries an explicit collision guard — and **FW6-G17's reverse falsifier is UNRUNNABLE at both**: its instruction is to *"grep **its banked id token** … the token, not the record-qualified spelling"*, and a reverse probe on either token returns a set spanning three identities across two or three canonical waves.⟩ Every occurrence of one of these in this spec is written **record-qualified** — `fr-ContourSettings M-13`, `fr-CoefficientsSpectrum M-13`, `fr-FourierShapeExtractor L-B1`, `fr-FunctionInput L-B1`, and so on — so the set-difference at FW6-G17 can run without collapsing two identities into one hit. Qualification is a **read aid, never a rename**: the banked id is the bare token, and the record prefix is address, not identity.

**Scope.** The F-side **burn-down** wave: F.W5 states the shared-provenance clauses, F.W6 executes the **server half** of the fourier CRUD/provenance defects those clauses name — the version chain (F-α), the PATCH cut (F-β ⊕ SS-C-1 write leg), breadcrumb redaction (F-γ), counter provenance, create-path idempotency, the moderation band's server routes, the audit actor field, contour/cache provenance, the canonical-geometry TRIPWIRE, and the produced-and-unconsumed strikes — plus the **routing receipt** by which V-α / V-β / V-γ / TA-4 leave for the value.js API row with zero value-tree bytes moved. It authors no clause, reverses no ruling, claims no client or display arm, and touches no frontend.

**Four-verb**: AUDITED **YES** — **22 identities BOOKED** at §2.1–§2.10 ⊕ **three CITED to their canonical holders** (`FR-EMT-20` → F-W5 · `AA-10` → F-W4 · `B-2` ⊕ `FR-GV-1` → F-W3) ⊕ four non-record commissioned acts (`F-α` · `F-β` · `F-γ` ⊕ the value-side quartet routed OUT at branch 1), which are outside the roster law ⟨**re-summed at repair round 5 (REST-18).** The section arithmetic is re-run in the same edit: §2.1 **3** + §2.2 **2** + §2.3 **1** (B-2 cited, REST-17) + §2.4 **6** + §2.5 **0** (AA-10 cited, REST-16) + §2.6 **3** + §2.7 **1** (FR-EMT-20 cited, REST-15) + §2.8 **1** + §2.9 **1** + §2.10 **4** = **22 booked**, and 1 + 1 + 1 = **3 cited**; the round-3 figure was 25 booked, of which the three conversions above were never this wave's to book⟩, each carrying its adjudicated witness, plus **the rest homed by citation/fold/holder-citation/exclusion at §2.11, §2.11c, §2.11d and §5** (**round 4**: no roster integer — the census is `CENSUS-CANONICAL.md`'s and it homes **zero** record-side rows at F.W6, §0.1); every id traces to the registry or to the canonical, not to a carry (R-3); substrate re-measured read-only 2026-08-28 · SPECIFIED **YES — by this file** · IMPLEMENTED **NO** · VERIFIED **NO** (stamped only at the X·F release close; no wave stamps VERIFIED at its own close). **EXECUTION IS NOT AUTHORIZED BY THIS FILE** — status stays `planned`; nothing opens, writes, builds, or runs product source in either repo until the owner's begin-word.

**Binding law.** M-25 (a spec ignoring adjudicated rows is DEFECTIVE; transcription-only is DEFECTIVE). L-19 (every gate names a real witness — no proof-farm scripts, no descriptive invocation). L-20 (precepts-latest). D-19 (no anchor cited before F.W0 re-grounds). Born-RED: **every gate below starts RED with its failing witness named.** Three standing laws shape every row:

1. **`/Users/mkbabb/Programming/fourier-analysis` is READ-ONLY, always.** This wave writes **zero fourier bytes**. Every "burn" here is a value.js-tree register row plus a commissioned ask in one letter; the fourier edits happen in fourier's own sub-session under COMMISSION §2, after the begin-word. Specifying is not doing.
2. **F.W5 STATES, F.W6 BURNS — no double-booking.** Where F.W5 owns a clause, F.W6 owns only its execution; where F.W3/W4/W8 own a client or display arm, F.W6 emits and does **not** claim it (the FR-GIG-5 mirror, adopted as a standing bar at **F-W5 §0b — *"What this wave is NOT"***). ⟨cmd⟩ **single-hit, re-cut at round 4** `grep -o "F\.W5 claims credit for none of them.\{0,46\}" waves/F-W5.md` (base `$W`; sibling pinned at §0.3) → *"F.W5 claims credit for none of them — FR-GIG-5's lesson adopted as a standing bar"*, inside §0b. *(The round-3 form `grep -n "FR-GIG-5" waves/F-W5.md` returns **four** hits — `:39`, `:162`, `:316`, `:341` — and this file used the identical command at TWO cells for TWO different quotations, so neither cell's paste was the command's output. Both are re-cut to single-hit anchors: this one, and §2.11 row 56a's.)* **Round-2 correction (R2-2)**: round 1 keyed this bar to *"F.W5 §1b"*, which is F-W5's *"Do NOT touch — every path below, without exception"* heading — a different section. The bar is cited by **§-heading** here and everywhere below, never by line.
3. **Producer rows never become frontend hacks, and server cures are never discharged as frontend hacks.** Glass rows ride SS-6 (declared empty here); parse-that→fourier routing is forbidden.

---

## §1 Bounds

Owned (writes happen only at execution, post begin-word):

| # | Path | Act |
|---|------|-----|
| 1 | `docs/tranches/X/fourier/waves/F-W6.md` | CREATE — this spec |
| 2 | `docs/tranches/X/fourier/waves/F-W6/burn-register.md` | CREATE — one row per burned identity: banked id · gate · commissioned act · fourier-side landing evidence. **FW6-G17 greps its id column**; without it the carry-closure gate is prose |
| 3 | `docs/tranches/X/fourier/waves/F-W6/value-side-routing-receipt.md` | CREATE — V-α / V-β / V-γ / ⊙ TA-4 named to the value.js API row. **FW6-G16's whole artefact.** **The receipt cites TWO authorities, because one does not cover all four (R2-1b, correction of record):** (a) for **V-β · V-γ · TA-4** — **F-W5 §4's `F.W5 → value.js API row` edge row**, which itemizes them; ⟨cmd⟩ `grep -n 'value.js API row' waves/F-W5.md` → *"value-side obligations, never fourier defects \| V-β compound version `_id` + the two-histories test (G2) · V-γ attribution (`userSlug` null ⇒ no version row, E18) · TA-4 per G4's ruling · fork born-visibility (`forks.ts:76`) + create-visibility test (G5) · G18's value-side probe · D9 reconciliation…"*. **That list does NOT itemize V-α.** (b) for **V-α** — **lane-crud §V-α**, an F-W5 §0 bounds authority (⟨cmd⟩ `grep -n 'V-α/β/γ' waves/F-W5.md` → the §0 AUDITED row naming *"lane-crud.md (R-1..R-7, §6, §7, F-α/β/γ, V-α/β/γ)"*), stated in lane-crud §2's R-2 row: ⟨cmd⟩ `sed -n '161p' formation/fourier/lane-crud.md` → *"**value.js defect V-α — revert writes no version row but still increments the counter.**"* **Round-2 strike (R2-1a/R2-1b): round 1 cited an "F.W5 §6c itemized obligation list"; `F-W5.md` has no §6c** (⟨cmd⟩ `grep -l '§6c' waves/F-W5.md` → **no output — F-W5 is not a member of the matching set** (set-membership, never a banked count of a moving file: R3-3.10)) — the citation is replaced by the two real homes above, and no invented list membership is asserted for V-α |
| 4 | `docs/tranches/X/coordination/value-to-fourier-2026-08-DD-provenance-burndown-F.W6.md` | CREATE — the commissioned-ask letter (the only vehicle by which the fourier side receives this burn-down). `DD` resolves at execution, never back-dated |
| 5 | `docs/tranches/V/coordination/INBOX.md` | **append-only** — one E13 ledger row for the relay; no rewrite of any existing row |

**Bounds reconciliation — tree-derived, no carry operand** (R-3: *"the CARRY" with no path is forbidden vocabulary from this ruling forward*). The five rows are justified against the tree itself: rows 2 and 3 are exactly the two evidence surfaces FW6-G17 and FW6-G16 need; row 5 is E13's standing duty, not new scope; rows 1 and 4 are this spec and its only courier. **Re-measured 2026-08-28 (R-10.7)**: `ls docs/tranches/X/fourier/waves/` holds **eleven** files — `F-W0.md F-W1.md F-W2.md F-W3.md F-W4.md F-W5.md F-W6.md F-W7.md F-W8.md F-W9.md F-W10.md` — superseding this spec's first-pass seven-file enumeration, which was falsified by `ls` and is cited by no gate. **No `F-W6/` subdirectory exists**, and this file's existence is not evidence any sibling artefact does. `docs/tranches/X/coordination/` exists — F.W5's relay lands there too; same channel, no third courier invented.

Read-only (bounds, no write) — **every path rooted, `ls`-resolved 2026-08-28 (R-10.5: a bounds-keyed halt condition may not rest on a path that does not exist)**:

| # | Path (rooted) | Status |
|---|---|---|
| r1 | `docs/tranches/V/megatranche/registry/adjudicated/fr-*.md` (all 66) | exists |
| r2 | `docs/tranches/V/megatranche/formation/fourier/{CENSUS-2026-08-03.md, lane-crud.md}` | exists |
| r3 | `docs/tranches/V/megatranche/audit/codex-provenance/{INTAKE-ADJUDICATION-2026-08-03.md, api-gap-remeasure.md}` | exists |
| r4 | `docs/tranches/V/megatranche/audit/codex-provenance/intakes/lane-fourier-r3-r6.md` | exists — and it is the **ONLY** file of that name in the megatranche. **Round-2 strike (R2-2)**: round 1 asserted a homonym at `formation/fourier/lane-fourier-r3-r6.md`; ⟨cmd⟩ `find docs/tranches/V/megatranche -name 'lane-fourier-r3-r6.md'` returns exactly **one** path, the `intakes/` one. A bounds table under R-10 may not carry an existence claim its own `find` refutes — the homonym sentence is **STRUCK**, and the `intakes/` rooting stands on its own — the frozen corpus records the same resolution: ⟨cmd⟩ `grep -o 'located at .audit/codex-provenance/intakes/.' fr-FourierShapeExtractor.md` → *"located at `audit/codex-provenance/intakes/`"* (`fr-FourierShapeExtractor.md:10`) |
| r5 | `docs/tranches/X/fourier/waves/F-W5.md` | exists |
| r6 | `docs/tranches/X/fourier/contract/{J-diff-shape-v2.md, operation-register.md, OWNER-RULINGS-F.W5.md}` | **TO BE CREATED BY F.W5** (`F-W5.md` §1a owns all three). **CITED-WHEN-CREATED (R-10.4)**: until they exist the obligation is *prospective* — they are never listed among existing immutable witness-holders, and FW6-G18 reads their absence as the RED it already declares |
| r8 | `docs/tranches/X/fourier/conformance/CENSUS-CANONICAL.md` | exists — **added at round 4 (R4-3/R4-5/R4-10): THE census of record and the sole census operand of every gate below.** Read-only here; this wave amends it by ruling or not at all. Its round-4 stamp is hash-pinned at §0.3. **The superseded `formation/fourier/CENSUS-2026-08-03.md` (r2) survives in bounds ONLY as the routing-law authority quoted at §2.10 / FW6-G16 — never as a denominator** |
| r7 | `/Users/mkbabb/Programming/fourier-analysis/docs/tranches/J/design/J-diff-shape.md` v1 (271 lines, `wc -l` verified) | **IMMUTABLE** — v2 supersedes by reference and never patches it (E-3). **R-10.3: this file exists ONLY in the fourier tree** (`find` over value.js → none). It is **readable, never writable** — the read-only-tree law of §1's NOT-in-bounds row admits reads and forbids writes; there is no contradiction, and the earlier value.js-shaped spelling `docs/tranches/J/design/J-diff-shape.md` is **STRUCK** as a phantom path |

**Explicitly NOT in bounds:** `/Users/mkbabb/Programming/fourier-analysis/**` (whole tree, read-only law — **NOT-WRITABLE, not un-readable**: r7 above reads inside it and moves zero bytes) · product source both repos — `value.js/{src,demo,api,test,e2e}/**`, fourier `api/`, fourier `web/` (execution gate) · `docs/tranches/X/COHESION.md` (root-authored at the boundary; F.W6 declares its half of every edge in §4 and requests the reciprocal — it does not write the spine) · `docs/tranches/X/{waves,keyframes,parse-that}/**` · the immutable evidence set above (rewriting one destroys a witness a gate cites) · `package.json` · `scripts/dev/dev.sh` (unowned dirty row, **never staged**) · `~/.codex/**`.

---

## §2 Carry — 22 booked ⊕ 3 cited, agglomerated by mechanism ⟨*re-summed at repair round 5, REST-18; read "25 identities"*⟩

`‡` BLOCKER-weight · `⊙` OWNER-GATED (may not be burned unruled) · `▲` a cure-integrity lock binding the **repair**, not the finding. Every anchor below is **MEASURE-AT-OPEN** (D-19): re-resolved against F.W0's re-grounding receipt before it is cited in any commissioned act.

### §2.0 RE-CUT-F6 — this wave's own precondition (recorded, not booked; no banked id exists)

**BLOCKER (spec-integrity).** Measured 2026-08-28: bare `F.W6` appears **0 times** across the 66 adjudicated records; the routing that reaches this wave is the **`F.W5–W8` span**. A span is not a home.

▲ **SPAN-PROBE LAW (R-6, ruled program-wide and binding here).** The canonical probe is **spelling-agnostic** — `grep -rhoE "F\.W5[-–]W8" fr-*.md`, U+002D **and** U+2013 — followed by the **S-8 enumeration law**: walk back from each hit to the enclosing adjudicated row id; never trust a single literal. This spec's first pass used the hyphen literal alone and was therefore structurally blind to 23 records; that is the same **K-6 method failure** this file quotes at §2.5 (*"`grep "log_audit("` is structurally BLIND to an inlined second writer"*), turned on its own corpus. Corrected measurement:

| form | occurrences | records |
|---|---|---|
| `F.W5-W8` (U+002D) | 120 | 26 |
| `F.W5–W8` (U+2013) | 72 | 24 |
| **union** | **192** | **49** |
| en-dash-ONLY records | — | **23** |

Of the 192, **23** are the census lane-taxonomy routing-law header line, which mints nothing (the RE-CUT-0 dissent's explicit warning). ⟨cmd⟩ `grep -rnE "F\.W5[-–]W8" fr-*.md | grep -c "tri-package uplift"` → **23**, one per record: fr-AdminAuditLog · fr-AdminFlaggedPanel · fr-AdminUserList · fr-AnimationControls · fr-AppHeader · fr-CanvasControlsDock · fr-CanvasOverlayButton · fr-CoefficientsPanel · fr-ContourPreview · fr-DarkModeToggle · fr-EasingCurvePreview · fr-EasingPicker · fr-EditorControlsDock · fr-EqCoefficientsPanel · fr-EquationView · fr-FourierShapeExtractor · fr-FrequencyGraph · fr-GalleryAdminBanner · fr-GalleryView · fr-GlassTimeline · fr-HarmonicLevelGrid · fr-InfoCard · fr-VisualizationView.

**FIVE of the 49 records carry exactly ONE band-form line and it is that header** — ⟨cmd⟩ per-record loop comparing total span lines to boilerplate span lines → **fr-AdminUserList · fr-CanvasOverlayButton · fr-DarkModeToggle · fr-GlassTimeline · fr-HarmonicLevelGrid** (1 line each, all boilerplate). They sit inside FW6-G17's **49-record denominator** and outside its **band-form** identity operand; naming them is what keeps the record count and the identity count from being read as the same measurement.

▲ **ROUND-4 CORRECTION (R4-7.3, count-word law: where the prose and the list disagree, the LIST is authoritative).** Rounds 2–3 wrote that these five *"mint no identity"* — a claim about **identities** resting on a measurement of **band-form lines**, and **false at the bytes for `fr-AdminUserList`**, which routes eleven rows to the union under the bare-`F.W5` spelling the round-3 fourth arm could not see (§0.1 clause 4). The canonical settles it by enumeration rather than by a re-cut: ⟨cmd⟩ `grep -o '\*\*fr-AdminUserList\*\* (11): .FR-AUL-3.*FR-AUL-59.' "$C"` → *"**fr-AdminUserList** (11): `FR-AUL-3` · `FR-AUL-11` · `FR-AUL-12` · `FR-AUL-13` · `FR-AUL-14` · `FR-AUL-20` · `FR-AUL-21` · `FR-AUL-25` · `FR-AUL-31` · `FR-AUL-46` · `FR-AUL-59`"* — **eleven rows, every one homed at F.W5, none at F.W6.** The corrected statement is exactly this: **FOUR of the five mint nothing into the union at the canonical** (⟨cmd⟩ `sed -n '4990,5038p' "$C" | grep -c 'fr-CanvasOverlayButton\|fr-DarkModeToggle\|fr-GlassTimeline\|fr-HarmonicLevelGrid'` → **0** — a count over the census, which is a stamped operand, never over a live sibling), **and `fr-AdminUserList` mints eleven, held at F.W5.** The eleven are cited to their holder at **§2.11d** and counted into nothing here. *(The parenthetical about band-form line counts was always true and is preserved; only the identity claim was false, and it is the identity claim that is corrected.)*

**Round-2 correction (P2-9 cluster).** Round 1's cure re-measured the union spelling-agnostically but asserted *"3 are the routing-law header line"* without running the instrument on it — a false arithmetic claim inside the very cure that convicted un-measured arithmetic. It is **STRUCK** and replaced by the enumeration above; the sibling wave's conforming form is **F-W9 §5's** boilerplate row — ⟨cmd⟩ `grep -n "boilerplate routing-law lines" waves/F-W9.md` → *"The 14 boilerplate routing-law lines, the 12 verdict-prose mentions, and the 7 boilerplate-only records"* — which separates the three populations instead of asserting one integer over them. **The 192 → 122 derivation is unchanged**: boilerplate lines never minted identities in either count, so no operand of FW6-G17 moves.

The union yields **122 band-form span-routed identities**, enumerated whole at **§2.11** — **and the union of two dashes is not the union of the corpus's spellings; the round-3 re-cut immediately below adds ten more, and the round-4 unscoping at §0.1 clause 4 + §2.11d adds fifteen after that.** ▲ **Round 4 stops the sequence rather than extending it: these figures survive as MEASUREMENTS of the corpus (the corpus is frozen; they reproduce), never as a ROSTER TOTAL — the roster total is struck at §2.0's re-denomination cell and the census is the canonical's.**

▲ **ROUND-3 RE-CUT (R3-5.2, binding): the band token is not the corpus's only spelling, and a two-dash detector is still a restricted detector.** Round 2 cured spelling-blindness *within one token*; the corpus routes to the same CRUD/provenance union in **FOUR** spellings, and the two this file could not see carried **ten identities**. Measured over the frozen corpus this round:

| band spelling | occurrences | where |
|---|---|---|
| `F.W5-W8` (U+002D) | 120 | 26 records |
| `F.W5–W8` (U+2013) | 72 | 24 records |
| `F.W5(-W8` (parenthesised) | **1** | `fr-ContourPreview`'s closing verdict |
| bare **`F.W5`** as the *name of the union* | — | **5 records** whose routing taxonomy spells the union that way |

⟨cmd⟩ `grep -rhoE 'F\.W5[^ ]{0,6}W8' fr-*.md | sort | uniq -c` → `1 F.W5(-W8` · `120 F.W5-W8` · `72 F.W5–W8`. ⟨cmd⟩ `comm -13 <(grep -rlE 'F\.W5[-–(]W8' fr-*.md | sort) <(grep -rlE 'F\.W5' fr-*.md | sort)` → **fr-ConvergenceLegend.md · fr-ConvergencePlot.md · fr-EquationModeToggle.md · fr-EquationResult.md · fr-NotationPills.md** ⟨*the `.md` suffix the command prints on all five was dropped at rounds 1–4 and is restored at repair round 5, REST-31*⟩ — five records that route to the union and sat **outside FW6-G17's 49-record denominator entirely**. In those five the routing header reads *"**F.W5** CRUD/provenance union"* rather than the band form, so their rows route to `F.W5` by name; the same boilerplate-exclusion applies (their taxonomy header carries `tri-package uplift` and mints nothing), and their verdict-prose mentions are walked back to the enclosing row id by S-8 exactly as the band arm is.

**The ten escaped identities, each landed at §2.11c** — every one of them is **program-held** and **every one has a real `F-W5.md` stating clause**, which is why nine land as citations and one books:

| # | identity (record-qualified) | corpus coordinate | `F-W5.md` stating clause | landing |
|---|---|---|---|---|
| E-1 | `fr-ContourPreview` **row 13 = `L:L-5`** | `fr-ContourPreview.md:46` | **E20** *The exposed-ref seam* | EXCLUDED-with-reason (client contract) |
| E-2 | `fr-EquationModeToggle` **FR-EMT-1 = L-B2/C-1/D-9** ‡ | `:31` | **F1** *Partial projection* ‡ | CITED |
| E-3 | `fr-EquationModeToggle` **FR-EMT-20** | `:50` | **E10** *Produced-and-unconsumed* | **CITED — held at F-W5 §2 clause E10** (canonical **F.W5**, no band leg; re-cut at repair round 5, REST-15) |
| E-4 | `fr-EquationResult` **FR-EQR-4** | `:39` | **F8** *THE SEAM CHOICE — portable LaTeX* | CITED |
| E-5 | `fr-EquationResult` **FR-EQR-32** | `:73` | **F8** (cited B1) | CITED |
| E-6 | `fr-NotationPills` **FR-NP-30b** | `:74`, verdict `:130` | **D12** *Closed domains at the boundary* (cited A5) | CITED ⊙ |
| E-7 | `fr-ConvergenceLegend` **D-L4 + C-7** | `:72` | **B1** *The join relation — forward* | CITED |
| E-8 | `fr-ConvergencePlot` **L-M7 + C-8** | `:64` | **F7** *The missing original-expression field* | CITED |
| E-9 | `fr-ConvergencePlot` **C-7** | `:65` | **B2** *The mirror direction* ‡ | CITED |
| E-10 | `fr-ConvergencePlot` **L-m13** | `:85` | **F6** *A response field states WHICH population it measures* | CITED |

~~**Re-denominated: the roster is 132** — 122 band-form ⊕ 10 escapes. **Of the 132: 25 booked · 64 cited · 43 disposed at §5.**~~ ▲ **STRUCK WHOLE (R4-5).** The partition never closed: §2.11c's own counting rule declares branch-2 folds **not** roster members, and BLOCK C marks **42** rows branch-2 while the arithmetic needed **42 tail MEMBERS** — the same integer playing opposite roles, so the enumerated roster reads **111** with the rule applied and **153** with it ignored, **never 132**. A denominator that cannot be reproduced from its own file's rule is not a denominator, and this file does not get a fourth attempt at manufacturing one. **Re-based per §0.1: the record-side figure for F.W6 is ZERO (canonical §4.1 — no record routes `F.W6`), the band's record-side denominator is the canonical's 28 + 90, and this wave's own population is FOLDS AND GATE OPERANDS, enumerated by register and counted into nothing.**

The register that replaces the integer, each member enumerated at its own section and none of them summed: **§2.1–§2.10** the identities *cut to* this wave (booked, each with witness · cure · locks · sequencing) · **§2.11 + §2.11c BLOCKS A/C** the cited and the folded · **§2.11d** the canonically-F.W5-held, cited to their holder · **§2.11a** the seven measured zeros (the lower operand) · **§5** the excluded-with-reason. ▲ **The transcript at §2.11c BLOCK C prints MORE rows than any roster has members, and that is by construction, not by drift**: a **named fold** is transcribed at its carrier so a seat can find it — *"the fold is the identity guard, not a demotion"*. **That is precisely why no integer can be the gate's operand**, and why round 4 stops trying: the enumeration is the operand, and the canonical is the census.

▲ **The lesson this re-cut books, stated so a fifth spelling is caught by the gate and not by a later seat**: round 1 was blind to a dash, round 2 was blind to a bracket and to the union's own name. A detector enumerated by *tokens the author happens to know* reproduces the K-6 method failure at every round. FW6-G17's operand is therefore re-stated at §3 as **the run itself** — the transcript at §2.11c — rather than as an integer over a token list.

The re-cut this spec performs: the span's **server/provenance mechanism subset** lands here as a §2 row; its client-edge subset stays with F.W8 (operation wiring + conformance) and its render/copy subset with F.W3/W4; and — the correction R-6 forces — **every remaining span identity whose STATING home is an F.W5 clause lands as a CITATION of that clause at §2.11, never as a booking here** (booking would violate R-5's one-home law). **Both directions must be empty at FW6-G17** — a row cut here and also credited to F.W1/W3/W4/W5/W8 is a double-booking; a span row cut nowhere is a silent drop.

### §2.1 The version chain and the PATCH cut — **ONE COMMIT** (3 rows)

| id | sev | mechanism (witness) | cure + locks | sequencing |
|---|---|---|---|---|
| **F-α** | P1 (census risk 5) | `_write_root_version` is the **only** writer of `visualization_versions` and hard-codes `parent_hash=None, root_hash=set_hash_value, depth=0` (visualizations.py:138-141), called from create :220 and remix :592 — every version has `depth==0`, `GET /{slug}/versions` (:860-889) depth-sorts a permanent singleton, and all real lineage rides cross-viz `fork_of` | Deepen the quadruple by walking the parent as value.js already does (`rootHash = parent.rootHash ?? parentRef; depth = parent.depth + 1`, service/versions.ts:46-58) **or** retire depth/parent/root from the model. F.W5 G3's close, **re-quoted at the bytes this round (Q-3 cure, R3-3.9)**: ⟨cmd⟩ `grep -o 'v2 states \*\*deepen-or-retire\*\* for the depth/parent/root quadruple' waves/F-W5.md` → *"v2 states **deepen-or-retire** for the depth/parent/root quadruple"*. **Rounds 1–2 pasted this sentence with the bank's bold on `deepen-or-retire` stripped and no ⟨cmd⟩ beside it** — a "verbatim" label without a receipt is a defect per se (R2-1-LAW.2) and the un-emphasised form is not F-W5's string at any commit; both faults cured in one edit | F.W0 first (D-19). **The F.W5/F.W6 split, quoted at its real home** — ⟨cmd⟩ `grep -n "owns the CLAUSE" waves/F-W5.md` → **F-W5 §2 §E clause E2, the ▲ sentence**: *"F.W5 owns the CLAUSE; F.W6 owns the burn-down — do NOT double-book."* (the same law is stated a second time at **F-W5 §3 gate G3**'s green-owner cell — *"F.W5 owns the CLAUSE only — no double-booking"* — two homes, quoted separately, never fused). **Round-2 strike (R2-1a/R2-1b), both halves FABRICATED**: round 1 attributed a composite sentence to *"F-W5 §6c"* and asserted *"F.W5 §12 declares F.W6 blocked on it"* — ⟨cmd⟩ `grep -l '§6c' waves/F-W5.md` → **no output — F-W5 is not a member of the matching set** (set-membership, never a banked count of a moving file: R3-3.10) · ⟨cmd⟩ `grep -l '§12' waves/F-W5.md` → **no output — not a member** (R3-3.10: set-membership, never a banked count of a moving file) · ⟨cmd⟩ `grep -rn "burn-down needs" waves/` → this file only. Quotation, attribution and the *"declares it blocks"* framing are **STRUCK together**. The real edge is stated in F.W6's own voice from **F-W5 §4's `F.W5 → F.W6` row** — ⟨cmd⟩ `grep -n 'F.W5 → F.W6' waves/F-W5.md` → *"emits burn-down \| F-α's deepen-or-retire execution (G3); the G16 canonical-geometry pipeline WITH G5c's diagnosed result type; **DO-NOT-REGENERATE stands until F.W6 lands the pipeline** (tripwire: L-B1 + L-B2/C-2 revive at BLOCKER)"*. **The sequencing fact this row needs — F.W5 states the clause before F.W6 burns it — is carried by that edge row and by clause E2 / gate G3; nothing about it requires a blocking declaration, and none exists.** The intake's own disposition, **re-sourced and de-emphasised this round (Q-1 cure, R3-3.9)**: ⟨cmd⟩ `/usr/bin/grep -rn -F 'strengthens the census routing' docs/tranches/V/megatranche/audit/codex-provenance/INTAKE-ADJUDICATION-2026-08-03.md` → `docs/tranches/V/megatranche/audit/codex-provenance/INTAKE-ADJUDICATION-2026-08-03.md:197:booking); strengthens the census routing, no new work.` ⟨*the path prefix the `-rn` form prints was omitted; restored at repair round 5, REST-31*⟩ — **plain, in the bank's hand**. **Two faults cured together**: rounds 1–2 set *"no new work"* in bold **inside quotation marks**, emphasis the intake does not carry, and labelled the string *"X-8 verbatim"* with **no ⟨cmd⟩** — `X-8` names an intake ROW, not the file the words live in, and R2-1-LAW.2 makes an unreceipted "verbatim" a defect before anyone reads the words. Three-way corroboration adds evidence, not scope. **Dissent, carried and surfaced to F.W5's ruling, never silently overridden by this seat**: retire-vs-deepen is genuinely open — fourier's own board books this as DELETE (W2-transpose) at M.W10 (`M/M.md §7`, quoted in X-8) while the seam table frames value.js as the side that deepens; census C-6 confirmed at the source bytes, quoted whole this round — ⟨cmd⟩ `sed -n '71,72p' formation/fourier/CENSUS-2026-08-03.md` → **both lines whole** *"  into a stalled M — i.e. the half-built chain is *known* unfinished work on their side, not an"* / *"  overlooked bug. Strengthens the finding and re-homes it as formation work, not blame."* (the `/` marks the census's own line wrap; the two-space indent is the census's). ⌧ **Re-cut at repair round 5 (REST-29; P5-10).** The round-2b paste dropped ~25 characters **before** its quotation (`  into a stalled M — i.e. `) and a **whole trailing sentence after** it (`Strengthens the finding and re-homes it as formation work, not blame.`), both unmarked — **in the cell whose own round-2b erratum convicts the narrower `71p` bound for producing less than the sentence pasted beside it.** Both lines are now printed whole. **Round-2b correction, this seat**: round 2 restored the tail round 1 had abridged, but addressed it to `sed -n '71p'` — which stops at *"not an"*, so the command could not produce the sentence pasted beside it — **and set *"on their side"* in bold**, emphasis the census does not carry (⟨cmd⟩ `grep -l '\*\*on their side\*\*' formation/fourier/CENSUS-2026-08-03.md` → **no output — the emphasised form is not in the census** (membership, not a count)). Curing an abridgement by adding emphasis is the same defect running the other way; the command now spans both lines and every mark inside the quotation is the bank's |
| **F-β** | P1 (chain + stale advertised hash) | `palette_slug` is atom 5 (atomdiff.py:34) **and** a PATCH field (visualization.py:207), yet the handler does a bare `$set` (visualizations.py:381-383) with **no `set_hash` recompute and no version write**; `_head_set_hash` returns the *stored* value when non-empty (:477-480), so the advertised hash goes STALE vs `enumerate_atoms(doc)` and `/diff` + `/provenance` then report it. Adjacent: `updates = {… if v is not None}` (:381) means PATCH can **never clear** `palette_slug` | Recompute `set_hash` from `enumerate_atoms(doc)` on every atom-touching PATCH + write the version row; make `palette_slug` tri-state as remix **already is** — `model_fields_set` at :522-528 (omit=inherit / null=clear / slug=rebind). ▲ **KISS lock: the tri-state idiom ships in the same router twenty lines away — LIFT it, do not invent one** | **Declared gate gap (re-keyed per R-1e, verified against `F-W5.md` §2 §E clause **E7** and the §3 register head `## 3. Gates — 22, all born-RED` (cited by ID and HEADING, never by line: F-W5.md is under concurrent L-20 repair this round, so its line numbers are not a stable coordinate)): the clause is `F-W5.md` **E7** — *PATCH atom coverage + `set_hash` recompute* (F-β ⊕ SS-C-1 write leg) — and F.W5's gate register runs **G1–G22 plus §G's G1c–G10c**, none of which gates E7.** (This spec's first pass wrote "E8" over "G1–G20"; both were wrong. F.W5's real **E8** is *AnimationSettings — the three-way reconciliation*, BC-9/C-6/D-20 ⊕ SS-C-2, cited at §2.11 rows 1–2 and booked nowhere here.) F.W6 supplies FW6-G2/G3/G4 rather than read the omission as absence of scope. ▲ **ONE-CUT LAW with SS-C-1: both edit the same PATCH model; they land in ONE change or the second reverts the first's invariant** |
| **SS-C-1** (write leg) | MAJOR (demoted from challenge-C's BLOCKER at component altitude — no capability loss in the control) | `VisualizationUpdate` (visualization.py:198-210) and `VisualizationPatch` (types.ts:256-262) are the same five fields with `extra="forbid"`, while `animation_settings` is atom 4-of-5 (atomdiff.py:38) — the persisted `speed` atom is settable **only** at create/fork; silent non-persistence of a preference the control claims to set | PATCH field set **equals** the atom set, or each divergence is stated per-atom with its reason | ▲ **SPLIT RULING (R2-LC wins), binding: the READ leg — `loadVisualization` zero callers — is banked at fr-GalleryCardModal GCM-1 at blocker weight → F.W4 + F.W5-W8 rider. Do NOT re-book the read leg here.** Only the write leg is un-banked. Same-commit rider on F-β. No dissent — the axis split was adjudicated deliberately; re-merging re-books GCM-1 |

### §2.2 Redaction and the image leaf — one privacy cut (2 rows)

| id | sev | mechanism (witness) | cure + locks | sequencing |
|---|---|---|---|---|
| **F-γ** | P1 (privacy; census calls opposite defaults on privacy-adjacent surfaces contract-BLOCKING) | `GET /{slug}/provenance` (visualizations.py:733-791) walks `fork_of` with a bare `find_one` (:784) emitting `slug`, `set_hash`, `author_slug`, `created_at` for **every** ancestor incl. private/draft ones — `_readable_or_none` is applied only to the ENTRY row (:744). value.js already redacts: `ProvenanceStep` is a discriminated union (service/forks.ts:171-179) collapsing non-public hops to `{kind:"unavailable", ordinal}`, with the guarantee stated in-source at :167-169 (tagged V·W45 item 4) | Apply `_readable_or_none` per hop and collapse non-public ancestors to that shape — **adoption, not design**. ⟨cmd⟩ `grep -n 'Reusable as-is (value.js is ahead)' formation/fourier/lane-crud.md` → *"**Reusable as-is (value.js is ahead):** revert as a verb; version-get-by-hash; the redacting…"*, under **lane-crud §7 — *Raw-material summary*** (**round-2b**: round 2's looser `grep -n 'Reusable as-is'` returns **two** hits — the sibling at `:387` is *"Reusable as-is (fourier is the reference implementation)"*, the opposite direction — so the command as written did not produce the single line pasted beside it. Narrowed to the one that does; R2-1-LAW requires the quote to BE the output, not a selection from it). **Round-2 correction (R2-2)**: round 1 addressed this string to *"census §7"*; the census has no §7 (⟨cmd⟩ `grep -nE '^#{1,4} ' formation/fourier/CENSUS-2026-08-03.md` → §0–§6 + ADDENDUM). Wrong FILE, right words — the address is corrected, the quotation is unchanged. The existing cycle-guard and the ≤50 cap **must survive the cure** | **G6's second branch stands open** — *"…**or** the asymmetry is stated with its reason"*; the burn seat **may not choose the redact branch until F.W5 closes the clause**. ▲ One-cut discipline with the image row: a breadcrumb redaction that leaves a draft/unlisted viz's source image world-readable by slug **has not cured the leak** |
| **FR-AFP-4** ‡ (⊕ **F-4** ⊕ **m-15** ⊕ **GCM-52**; ImageUpload row 26 cited) | BLOCKER-weight (one of the panel's four survivors) · m-15 MINOR · row 26 INFO | **No action the moderation panel offers takes the reported artifact offline**: Delete is a soft-delete of the *visualization*; `images.py` = 7 ops with **zero DELETE**; blob GET (:132) has no `Depends` and ships `Cache-Control: public, max-age=86400`; every anonymous blob GET ends in `touch_document` — **actively-fetched reported content is precisely what never reaps**. Thumbnail GET carries the identical posture (F-4: one router, one identity), so a draft/unlisted viz's source image is world-readable by slug (GCM-52 — R3-7b at the leaf; 2²⁸ slug entropy from 4× `secrets.choice` over 128-word lists). m-15: unversioned thumbnail URL (api.ts:292-294) + 24 h cache (images.py:162-164) negates the deliberate re-upload regeneration branch (image_storage.py:105-128) | Image remediation contract executed: **delete/quarantine op + blob visibility gate + janitor predicate fix + versioned thumbnail URL**. ▲ **C's scope discipline preserved: soft-delete DOES de-list from browse — the cure may not overstate the hole.** ▲ **F-4 books WHO can fetch; m-15 books WHAT they see — cross-referenced, NOT merged**; a cache-versioning cure discharges neither the auth cure nor vice-versa | F.W5 C1/C2/G8 state the authority class per operation first (**cite 45/30/13, never 30 alone** — the admin arm is inside the denominator; OpenAPI security 0 of 45). ▲ **The deployment auth-proxy question stays UNPROVEN (F.W5 SS-13 defer #5) — the gate may not close by assuming a proxy in either direction** |

### §2.3 Counters, identity, idempotency (2 rows)

| id | sev | mechanism (witness) | cure + locks | sequencing |
|---|---|---|---|---|
| **FR-GV-12** (⊕ **FR-GV-24** · **VV-R2-A** cited) | MAJOR (×2 witnesses) | `GET /api/visualizations/{slug}` is an unsafe read that cannot observe its own side effect: `find_one` first (:256), `$inc {"views": 1}` after (:268-270), `_public_doc(doc)` serialises the **pre-increment** document (:272). So (a) `recordView`'s write-back (gallery.ts:207-208) restores a value the list already held — the counter can never show the user's own open; (b) the same verb is the ETag-capture path (gallery.ts:167, :223), so every ETag cache-miss on publish/soft-delete adds a **phantom view** to the row being mutated and an entity is **born at `views: 1`** (VV-R2-A, chain verified whole); (c) no `viewed_ips` dedup while `liked_ips` exists, and a mutating GET is unsafe against any proxy/prefetch (RFC 9110 §9.2.1) | Explicit view verb making the read safe — **or** v2 states the mutating-GET policy against RFC 9110 §9.2.1 (G12's two branches). ▲ **CURE LOCK (FR-GV-24), restated at its real home — F-W5 §2 §D/§E clause `E6` ▲** (⟨cmd⟩ `grep -n "FR-GV-24" waves/F-W5.md` → E6's carried-rows cell *"FR-GV-12 (=MISS-LC-1) ⊕ VV-R2-A ⊕ FR-GV-24"* and its lock *"▲ **Repair-test lock (GCM-3 R-7 precision): `viewedHashes` DOES work within a session — the defect is SCOPE, not absence — so repair tests must NOT assert a re-open increment.**"*; the same lock rides F-W5 §3 gate `G12`): **a repair test must NOT assert a re-open view increment.** **Round-2 correction (R2-2)**: round 1 keyed this to *"F-W5 §7"* — F-W5 has no §7 (⟨cmd⟩ `grep -nE '^#{2,3} ' waves/F-W5.md` → §0…§6 only; its four `§7` strings all cite *other* documents, `lane-crud §7` / `CRUD-CONTRACT §7`). Cited by **clause id and gate id** from here on | F.W5 E6/G12 states first. **VV-R2-A's client arm — publish adopts `store.setVisibility("public")`, dropping one round trip — is F.W4's; F.W6 may not be credited with it.** FR-GV-24's `viewedHashes` component-local reset is the composing client row. SS-13 owns the observable (publish a fresh viz, read the card's count; derivation: born at 1) |
| **fr-GalleryDraftsSection B-2** ‡ (⊕ **FR-GV-1** FOLD; CanvasControlsDock D-4/L-1/C-3 +C-28, VisualizationView BLK-2 folded — **and `fr-VisualizationView` BLK-1 is a DISTINCT banked id, cited at §2.11 row 54, not folded here**) — **record-qualified per R-5; the homonym `fr-EquationView B-2` is a distinct identity, cited at §2.11 row 25** | BLOCKER (banked) | The second press of publish mints a SECOND public visualization: `createVisualization` sends no `Idempotency-Key` (api.ts:371-380) though `coreFetch` supports it (:138-140); the middleware is an explicit `no header → handler()` passthrough (idempotency.py:68-70); `content_hash` carries a PLAIN index (database.py:98) while flags carry a deliberate `unique=True` (:140). The asymmetry is **inside one file** — the same router's remix arm already calls `idempotency.replay_or_record(...)` at :612 | Server-side dedupe/idempotency for the create path — the ADJUDICATED SPLIT, by command (R2-1-LAW): ⟨cmd⟩ `grep -o "props/reconciliation contract.\{0,105\}" ⟨*bound re-cut at repair round 5, REST-23: `.{0,120}` over-ran to `(CRUD/provenance union). Riders: m-13 (`, unmarked*⟩ fr-GalleryDraftsSection.md` → *"props/reconciliation contract → **F.W3/W4**; server-side dedupe/idempotency for the create path → **F.W5-W8** (CRUD/provenance union)."* Lift the remix arm's `replay_or_record`, or make `content_hash` unique. ▲ **K12 LOCK: both axes' UNPROVEN-NEEDS-LIVE hedges on the duplicate-row outcome are DEAD — B-2 closes STATICALLY; SS-13 spends no probe here** | **F.W6 takes the SERVER arm ONLY.** The `savedSnapshots: []` single-writer, the vacuous `!d.savedSnapshots?.length` filter, the re-enabling button and the `publishedHashes` misnomer are F.W3/W4's — one home, two citations. Rider at the banked home: m-13 (the success toast prints the USER slug). No dissent — FR-GV-1 was graded BLOCKER at the banked severity and explicitly folded; the fold is the identity guard, not a demotion |

### §2.4 The moderation band ⊙ — **every row rides FR-AFP-1** (6 rows)

| id | sev | mechanism (witness) | cure + locks | sequencing |
|---|---|---|---|---|
| **FR-AFP-1 = D3** ‡ ⊙ **THE ADMISSION GATE** | BLOCKER-weight, OWNER-GATED | The moderation queue has **no producer**: two admin routes (GET `/flagged`, DELETE `/{slug}/flags`) are the collection's entire live surface; every `db.flags` write repo-wide is a test fixture or a `$rename` migration; `FlagRequest` referenced nowhere; no client posts a flag; **~110 of 285 panel lines dead by reachability**; production's only reachable state is the empty state. **value.js HAS the verb (`POST /:slug/flag`).** FR-AFP-18 (cited): the queue has no size anywhere — no total in the envelope, no `AdminStats.flagged` | ⊙ **producer-or-retire RULED at F.W5 (G11) — F.W6 does not decide it.** If PRODUCER: F.W6 ships the flag-write operation with value.js's `POST /:slug/flag` as reference implementation. If RETIRE: the band's **server routes are deleted here**, the panel at F.W3/W4 | ▲ **This ruling PRECEDES F.W1's sizing wherever FR-AFP grades are load-bearing (ruling 5).** **Every FR-AFP row below is CONDITIONED on it; F.W6 may not open the moderation band unruled.** Dissent-of-record (ruling 5 itself): severities grade the POPULATED surface and FR-AFP-1 gates the band's live reachability — any grade cited from that band carries the conditional or it overstates. The L-1/L-2 dual-BLOCKER incoherence was resolved here: the axis cannot carry two BLOCKERs when its first proves the second unreachable |
| **FR-AFP-66** (⊕ **FR-AFP-9** ⊕ **FR-GV-34**) | MAJOR (permanent-growth data defect) · FR-AFP-9 dialog-truth arm MINOR | Orphan flag accumulation: soft-delete does not cascade flags, the janitor's grace hard-delete cascades nothing, and `pinned_cron` — **named by the softdelete docstring as the cascade owner** — contains zero flags code. The panel's own Delete → grace pipeline strands flags forever, permanently inflating the unscoped `$group` every page request runs; the module docstring is FALSE for this band; the existing orphan-flag test exercises only the reporter-keyed stale-user cascade. FR-AFP-9: the confirm dialog asserts irrevocability over a 30-day-restorable soft delete (restore route live at :430), and a restored row returns to the queue still flagged | `content_hash`-keyed cascade at grace hard-delete **+ docstring correction** (the false module prose is part of the cure, not commentary). Composes with FR-AFP-4 and FR-AFP-33 (the growth term) | ⊙ **the hard-delete-arm caller decision sits in F.W5's owner block as ruling `R6` against clause `D6` — *Lifecycle truth and cascade*** (R-1e re-key: this spec's first pass mis-labelled it "E7", which is the PATCH clause). CONDITIONED on D3/G11 — **if RETIRE, the cascade is deleted with the band rather than built**. FR-AFP-9's truthful copy → **F.W3/W4** (diction rides FR-AFP-35); FR-GV-34's batch-dialog copy likewise. Coverage obligation → F.W9/W10 |
| **FR-AFP-7** (⊕ **FR-AFP-8** identity arm) | MAJOR | Flags key on **non-unique `content_hash`**: one artefact fans out to N rows each claiming the full count (3 rows × "3 flags" = an apparent nine), and the slug-labelled "Dismiss flags" action is hash-scoped. FR-AFP-8: the destructive confirm names `image_slug` — an asset FK **shared by every remix** (`VisualizationRemix` declares no `image_slug`) — while deleting `slug`, violating the identity law the file's own header states at :18-23; every human-readable surface renders the FK | F.W5 **D5** — *Which identifier a human-facing surface may render* (FR-AFP-8) — states it (R-1e re-key: the first pass wrote "D12"; F.W5's real **D12** is *Closed domains at the boundary*, M-10 · M-9, cited at §2.11 rows 7 and 11); the flags-key-the-ENTITY arm is F.W5 **D4**. **F.W6 burns only the server half — the flags keying and the dismiss scope** | ▲ **FR-AFP-8's cure is a one-token F.W3/W4 rider (pass `item.slug` as the label) — F.W6 may not claim it.** Same shape as GCM-1's `/w/` routing on an asset FK (cited; GCM-1's repair unit is F.W4's). CONDITIONED on D3/G11. **Dissent recorded so the row is not read as twice-derived: FR-AFP-7 was filed on the β/L axes only (α remit); the α reader never examined it** |
| **FR-AFP-33** | MAJOR | The flag list is unbounded on both ends — server `$push` with no `$slice`, client no cap/collapse — and the aggregate is a bare `$group` over the WHOLE collection per page request, O(pages × \|flags\|), whose permanent-growth term is FR-AFP-66. Counterweight preserved, by command (R2-1-LAW) — ⟨cmd⟩ `grep -n "if not flagged" $F/api/routers/admin.py` → `550:    if not flagged:` — the short-circuit that carries production's only case | Server → bound the `$push` (`$slice`) and scope the aggregate | **Client collapse → F.W1/W3 (`./expandable-container` is already installed) — NOT F.W6's.** Magnitude + β's sort-index note → SS-13. CONDITIONED on D3/G11. Composes with FR-AFP-66: curing the cascade without bounding the aggregate leaves the cost; bounding the aggregate without the cascade leaves the growth |
| **FR-AFP-36** (dead-models arm) ⊕ the **D17 serializer** clause (FR-AFP-32/-40/-69; GCM-55 cited) | MAJOR | Contract-twin drift: the emitted `content_hash` — the actual join key — is omitted client-side; three dead server models encode the retired contract; **the response-model check is present on 1 of 13 admin operations and absent from both operations this row is about** — **round-2 correction (FW6-G12's re-measured witness)**: round 1 wrote *"no `response_model` anywhere"*, which the bytes refute — ⟨cmd⟩ `grep -nE 'response_model' api/routers/admin.py` → `110:    "/stats", response_model=AdminStatsResponse, dependencies=[Depends(admin_required)]`, the only hit, against ⟨cmd⟩ `grep -cE '^@[a-z_]*router\.(get\|post\|put\|patch\|delete)' api/routers/admin.py` → **13**. The overstatement is struck; **the defect is unchanged and sharper** — the one checked operation is `/stats`, while `/flagged` (`:509`) is hand-built and unchecked. D17 (R-1e re-key — `F-W5.md` **D17** *Date serialization, serializer arm only*; the first pass wrote "D10", which is F.W5's *list contract — filters and windows*, cited at §2.11 rows 46–48): ONE router serializes the same concept two ways — flagged rows via `_json`/`default=str` (space-separated, non-ECMA) while audit returns `AuditListResponse` (ISO with `T`) — and the NaN guard sits in the consumer receiving the **conformant** payload. GCM-55 (cited): the wire serves `likes` raw because `json.dumps` bypasses the Pydantic model | One generated/checked shape; **dead models delete**; serializer unification on the server. ▲ **K9 LOCK, binding: do NOT delete the five `?? item.slug` fallbacks** — `admin.py` emits `doc.get("image_slug")` through an unvalidated hand-built body with no `response_model`, so they are DEFENSIVE, not dead (the limb is NOT PROVEN) | F.W5 owns ONLY the serializer clause (**D17**); the one-contract-source arm is F.W5 **A4** ⊙. F.W6 burns the server half. ▲ **NAMED FOLD, restored this round (P2-9 cluster)**: `fr-GalleryCard L·M-4 / D-13 / C-8(a) + L·D-2 / C-12` **folds into D17** and is named here so no seat reads its absence as a drop — ⟨cmd⟩ `grep -n '\*\*D17\*\*' waves/F-W5.md` → D17's carried-rows cell *"FR-AFP-32 (D-16/L-13/C:D-15; +FR-AFP-40 fold, +FR-AFP-69 = β-miss-5; **`fr-GalleryCard L·M-4 / D-13 / C-8(a) + L·D-2 / C-12`** folds here — the FULL banked spelling)"* (**round-2b re-quote, this seat**: round 2 pasted this cell in the compressed spelling *"GalleryCard L·M-4/D-13/C-8(a) folds here"* — ⟨cmd⟩ `grep -l 'GalleryCard L·M-4/D-13/C-8(a) folds here' waves/F-W5.md` → **no output — the compressed spelling is not a member** (R3-3.10). F-W5's own round-2 seat expanded the cell to the record-qualified banked id in the same round this file was written, so the quotation staled against a live sibling *while being authored*; re-read at the bytes and conformed, which is also what the anti-rename law requires of the spelling); the banked record states the same fold and its split — ⟨cmd⟩ `/usr/bin/grep -n 'L·M-4' fr-GalleryCard.md` → *"64:- **L·M-4 / D-13 / C-8(a) + L·D-2 / C-12 — MINOR · FOLDS BY REFERENCE, nothing new to book.** The `timeAgo` family (5 copies, 2 dialects, render-frozen, no NaN guard, non-ISO wire) is banked at **fr-AdminUserList FR-AUL-17** + **fr-AdminFlaggedPanel FR-AFP-32** (both name GalleryCard:53-61; → F.W3; serializer → F.W5–W8; JSC → SS-13)"*. **Only the `serializer → F.W5–W8` leg reaches this wave, and it reaches it INSIDE D17 — no second row, no re-grade**; the JSC leg is SS-13's. ▲ **The shared client formatter is already banked at fr-AdminUserList FR-AUL-17 → F.W3 — do NOT re-book.** ⊙ **FR-AFP-71's codegen/module-split decision is OWNER-GATED — v1 chose hand-typed twins explicitly and v2 may not silently reverse it; F.W6 may not build codegen unruled** |
| **FR-GV-9** (⊕ **FR-AFP-10** ⊕ **FR-AFP-70**) | MAJOR | Batch Unfeature sets `{"$set": {"tier": "normal"}}` unconditionally via `update_many` (admin.py:435-440) — **unfeaturing a SAVED-tier entry silently erases the saved tier** — while the :423-425 dialog copy is honest about the mechanism and the :326-333 button label is not. FR-AFP-10: "Mark acceptable (save)" never dequeues — `set_tier` writes `{tier, updated_at}` only (:160-190) and the flagged listing has **no tier predicate**. FR-AFP-70: `handleSetTier` is hardwired to `'saved'` with no inverse; a repeat click is a real PUT + audit row + success toast + full pagination reset for a no-op | **A blanket write is not a transition** — the server states the transition (a `$set` conditioned on the current tier) and the flagged listing carries the tier predicate FR-AFP-10 assumes. ▲ **β's 428-escalation hypothesis is KILLED by its own author (If-Match `*` accepted) — do NOT revive it as the cure** | Interim client mitigation (reflect-and-disable when already saved) is **F.W3/W4's**; FR-AFP-59's tier-render arm (the 10px raw wire token) likewise. Rides banked FR-GFC-20 — **BOTH banked arms carried this round (P2-9 cluster; round 1 kept only the F.W4 half)**: ⟨cmd⟩ `/usr/bin/grep -n "FR-GFC-20" fr-GalleryFeaturedCarousel.md` → *"54:- **FR-GFC-20 = C-9 — MINOR.** A carousel-initiated delete leaves the stale slug in `selectedHashes` … and the batch path posts the stale set. Fold-in by reference: the `BatchResponse.errors` reporting arm is itself dead per banked **FR-AUL-13** (never populated by admin.py) — partial failures report green. **→ F.W4** + **F.W5-W8 rider**."* The **F.W4 arm** is the stale-slug pruning (client). **The `F.W5-W8` rider is the SERVER arm — `BatchResponse.errors` never populated by `admin.py`, so a partial batch failure reports green** — and it is the same batch write this row cures, so it **travels with FW6-G10's transition cure and is burned WITH it**: a conditioned `$set` that silently drops rows while the envelope reports success is not a stated transition. **Carried as a rider, NOT booked as a new identity** — the dead-errors arm is banked at `fr-AdminUserList FR-AUL-13` and is not re-booked here (R-5 one-home law). FR-AFP arms CONDITIONED on D3/G11. **Dissent: FR-AFP-10 and FR-AFP-70 were filed on the β axis only (α remit)** |

### §2.5 The audit actor (1 row)

| id | sev | mechanism (witness) | cure + locks | sequencing |
|---|---|---|---|---|
| **AA-10** — **CITED, NOT BOOKED (R4-6.1 / R4-10; repair round 5, REST-16)** (⊕ **AA-5** server arm; AA-6/AA-24 cited) | MAJOR ×2 | **CITED — held at F-W4 §2.A.** Canonical `` | `AA-10` | `D-M2` · `L-13` | `F.W4` · `F.W5–W8` | **F.W4** <sub>legs: F.W5-W8</sub> | ``, and the record names the primary itself — `fr-AdminAuditLog.md:47` *"Display cure (sentinel-aware branch + legend) **→ F.W4**; the actor-field contract question **→ F.W5–W8**."* R4-6.1: *a leg books as ONE row at the wave that HOLDS the host id.* **The F.W5-W8 leg — the actor-field contract question — is what this wave carries, and it carries it under the holder's id.** The audit log never answers *who*: `janitor.py:56` `_JANITOR_ACTOR = "system:janitor"` is written into **`ip_hash`** at :95 and rendered `system:jan` by an unconditional `.slice(0,10)` at :146 — convicted by the producer's own comment (:50-55, *"self-documenting in the viewer"*). AA-5: the collection's **second writer** (`janitor.py:59-99`) inserts directly into `db.admin_audit` with no `log_audit` call, emitting nine `janitor:*` actions (spot-verified :133 `hard_delete_visualizations`, :285 `prune_audit`) that match no arm of the tone map — including the audit log pruning its own history | The clause sentence, **re-quoted at the bytes this round (Q-2 cure, R3-3.9)**: ⟨cmd⟩ `grep -o "An audit row's actor is a FIELD, not a repurposed .ip_hash." waves/F-W5.md` → *"An audit row's actor is a FIELD, not a repurposed `ip_hash`"*, inside `F-W5.md` clause **E18**. **Rounds 1–2 wore the word *Verbatim* over a string that is F-W5's at no commit** — the opening *"An"* down-cased to *"an"*, the clause's shouted **FIELD** flattened to lower case, and a trailing period pulled inside the emphasis — with **no ⟨cmd⟩** beside it. That is the exact drift class this file convicts at §2.11 row 49, recommitted at a cell wearing the label; the words are now the bank's and the receipt is beside them. AA-5's server arm: route the janitor through the shared action taxonomy / `log_audit` seam. ▲ **K-6 METHOD LOCK: `grep "log_audit("` is structurally BLIND to an inlined second writer — enumerate with the S-8 method law, not the killed one.** ▲ **AA-23 — KILLED-CURE CITATION ONLY, marked as such (R4-6.4, binding).** ⊙ **The HOST ROW is held at F-W5**: `fr-AdminAuditLog AA-23` sits in the canonical's `F.W5` roster (§2.11d H-11) and the record routes it *"the 45/30/13 join must carry this constraint"* at **MAJOR, repair-shaping** — so **F.W6 books it nowhere and carries only the kill below.** Rounds 1–3 carried the killed cure while the host row appeared in no landing cell at all, which is precisely the **R-6.4 correction this file makes correctly for `fr-BasisSelector m-7` one record over** — *a killed sub-claim never kills its host row* — left unapplied where it bit hardest, on a row whose live constraint binds the very K-1 counting lock this cell repeats. **The kill, unchanged: the regex-action cure BOTH axes prescribed is REJECTED** (an indexed equality would become a collection scan on the one monotonically growing collection); AA-6's sound order is (a) honest placeholder, (b) `$in` over a generated taxonomy, (c) anchored case-**sensitive** `^prefix` — in that order only | F.W5 **E18** — *Attribution: the actor is a FIELD* (AA-10 + lane-crud R-7 / V-γ) — and **D16** — *The shared action taxonomy* (AA-5 + AA-24) — state; F.W6 burns. (R-1e: this spec's first pass cited "D9/D4", and D4 is *Flag identity keys the ENTITY*, a moderation clause. ▲ **ROUND-4 CURE of the D9 gloss (R4-9.3 / R3-3.10; cures P4-D6) — the erratum had itself become the defect.** Rounds 1–3 discharged this with *"`F-W5.md` has NO clause D9 — `grep '\*\*D9\*\*'` → 0"* plus the gloss *"'D9' in F.W5 is an owner-ruling label inside R8, never a clause."* **Both halves are retired**: the first was a **banked COUNT of a live sibling**, the exact form line 516 of this file declares retired, published with no ⟨cmd⟩ and no rooted operand; the second is **false at F-W5's bytes**. The true state, by receipt, in the only forms R3-3.10 permits over a moving file — ⟨cmd⟩ `grep -o '(No clause is numbered D9 — see the compatibility note.)' waves/F-W5.md` (base `$W`; sibling pinned at §0.3) → *"(No clause is numbered D9 — see the compatibility note.)"*, **F-W5 saying it in its own hand, which is what the count was standing in for** · ⟨cmd⟩ `grep -o 'RULING D9 — ROOTED HERE, ONCE, AT FIRST USE' waves/F-W5.md` → *"RULING D9 — ROOTED HERE, ONCE, AT FIRST USE"* · ⟨cmd⟩ `grep -o '§2 "Product and architecture rulings", row `D9`' waves/F-W5.md` → *"§2 \"Product and architecture rulings\", row `D9`"* · ⟨cmd⟩ `grep -o 'must not contradict \*\*ruling D9\*\*' waves/F-W5.md` → *"must not contradict **ruling D9**"*. **Classification, not a count: `D9` in F-W5 is a value.js PRODUCT RULING carrying its own rooted block, whose Home is `docs/tranches/V/DECISIONS.md` §2's rulings table, row `D9` — and R8 is a clause that must not CONTRADICT it, not the label's container.** The clause-register fact survives unchanged and is now stated at F-W5's own words: **no F-W5 clause is numbered D9**, so this spec's first-pass clause cite was still wrong and the actor clause is still **E18**. ▲ **The generalisable half — fourth generation of the same lesson**: this file quotes the `F.W5 → value.js API row` edge row whose tail reads *"D9 reconciliation"* at §2.10 and at FW6-G16, so the receipt it pastes contradicted the gloss it wrote. *Cure addresses with anchors; cure facts with classifications; and never let a count of a moving file stand in for reading it.*) **Display arms are F.W4's and F.W6 may not claim them:** the sentinel-aware branch + legend (AA-10), the display taxonomy (AA-5/AA-24), the placeholder honesty (AA-6). Composes with R-7/V-γ — the fourier and value halves of user provenance are one clause with two burn seats. Coverage → F.W9/W10 (AA-44: nine e2e specs never exercise the audit tab while the backend pins its half at `test_janitor_audit.py` — the one-sided seam that let AA-5/AA-6/AA-24 open and stay open) |

### §2.6 Contour, cache identity, and canonical geometry (3 rows)

| id | sev | mechanism (witness) | cure + locks | sequencing |
|---|---|---|---|---|
| **B-4 = C-1** ‡ (∘ **i-7** = C-25/R6-8 fold; **m-18** rider) | BLOCKER (banked) | The ML Threshold slider is inert after the first extraction: `extraction_cache_key` is a **CLOSED 10-field `json.dumps` literal** (image_storage.py:250-266 — `_v, image_sha256, strategy, …`) that OMITS `ml_threshold` / `ml_detail_threshold` — the two fields this control is the sole producer of — and the handler short-circuits on the cache hit **BEFORE** `compute_contours`, while those fields materially drive extraction on the shipped default route | **Cache identity ⊇ consumed request fields** — the sharpened constraint banked at i-7 as B-4's explicit F.W5-W8 payload. ▲ Rider **m-18**: `ml_detail_threshold: mlThreshold.value * 0.6` is an invented, unnamed coupling that makes an independent contract field unsettable and overwrites any restored value on the next compute — **the cure must not preserve it** | F.W5 **E13** — *Cache identity ⊇ consumed fields* ‡ (B-4 ∘ C-25/R6-8 ⊕ i-7 ⊕ m-18) — states the superset clause; F.W6 burns. (R-1e re-key: the first pass wrote "E12", which is F.W5's *Debounced mirror vs synchronous save* — MISSED-E, cited at §2.11 row 20.) **GREEN fixture named by G13: `contour_hash` INSTABILITY across an ML-threshold change** — also the SS-13 probe. i-7 is INFO-weight fold, not a second booking |
| **fr-ContourSettings M-13** ‡ (⊕ **fr-BasisSelector M-14** ⊕ **fr-ContourEditorCanvas C-2** ⊕ ContourPreview row 28) — **record-qualified per R-5; the homonym `fr-CoefficientsSpectrum M-13` is a DISTINCT banked identity, cited (never booked) at §2.11 row 13** | BLOCKER (data-loss class) | Editor-saved contours carry no `extraction_cache_key` (`source="editor"`), so merely exiting the editor re-runs extraction and the server **cache-hits the ORIGINAL** — the hand edit is destroyed with zero settings touched; a Harmonics/Sample-Points nudge reaches the same end through the debounced `runCompute()` → `extractContour()` (M-14). And the POST never derives bounds: `store_contour_asset(xs, ys, req.image_slug, source="editor")` is called with **no `image_bounds`**; the param defaults `None` and is written verbatim (image_storage.py:290/:318); backfill is GET-only — and because the asset is content-addressed, **the overlay dies exactly when the points changed** (C-2) | G14, by command (R2-1-LAW; **bound re-cut at round 4 under R4-7.1 so the output IS the paste** — the round-3 `.{0,90}` ran on to `` asset | fourier API row | ``, a table cell boundary the cell stopped at without an elision mark): ⟨cmd⟩ `grep -o "editor-saved contour is a first-class.\{0,69\}" waves/F-W5.md` (base `$W`; sibling pinned at §0.3) → *"editor-saved contour is a first-class compute input; extraction cannot overwrite a `source=\"editor\"` asset"*. G15: **derive-on-POST or backfill-on-write is RULED**, not left to the burn seat's taste | F.W5 **E14** — *Contour provenance: an editor-saved contour is FIRST CLASS* ‡ — and **E17** — *Image bounds on write* ‡ (C-2) — plus gates G14/G15 state; F.W6 burns the **server half**. (R-1e re-key: the first pass wrote "E11", which is F.W5's *Easing domain hoisted to the operation* — RESOLVER ⊕ L/M-3, cited at §2.11 rows 18–19.) ▲ **The client half is F.W3/W4's and F.W6 may not claim it:** L-2's orchestrator move — the compute pipeline hoisted out of the `out-in` leaf — is REQUIRED (the 3-call-site compute census closes the remount-only escape hatch), carried with an urgency rider at the data-loss class. M-14's end-to-end reproduction → SS-13 (mechanism source-certain; end-to-end UNPROVEN-NEEDS-LIVE) |
| **G16 / fr-FourierShapeExtractor L-B1** (⊕ **fr-FourierShapeExtractor L-B2 / C-2** ⊕ **L-B3**; L-M3/C-6, L-M5/C-5+C-15, C-7, L-m3/C-10+L-m5 — **all `fr-FourierShapeExtractor`'s**) — **record-qualified per R-5: the homonyms `fr-FunctionInput L-B1` (→ F.W5 F2, §2.11 row 39), `fr-FunctionInput L-M3` (→ F.W5 F6, row 40) and `fr-ContourEditorCanvas C-2` (→ F.W5 E17, cited at §2.6) are DISTINCT banked identities** — **TRIPWIRE** | MAJOR at the seat's grade (two BLOCKER dissents); **BLOCKER on any regeneration attempt** | The generator has diverged from the artifact it generates: only **3.7%** of shipped `moon.json` points fall within 1.0 unit of any extracted point, p90 **37.6** against a 1.23-unit resample spacing, and the crescent-alone bbox strictly contains the shipped bbox by 12.97 x-max / 6.55 y-min — while the sun IS consistent (0.326/3.44). The only claimed regenerator is **untracked AND broken at import AND parameter-diverged**: `.gitignore:53 scripts/*` swallows both `precompute_svg_fourier.py` and `raw-contours.json`; it imports `order_contours`, which **no module defines**; it hard-codes 100/12 against the shipped 50/10. Canonical geometry is a pure function of an unpinned third-party PRNG with zero drift detection | ▲ **TRIPWIRE, binding and verbatim: DO-NOT-REGENERATE `moon.json` / `sun.json` on `master`; any regeneration attempt revives L-B1 and L-B2/C-2 at BLOCKER.** The wave's act is to **name the true source or re-author the artifact — WITH G5c's diagnostics row**, which is a PRECONDITION, not a companion: three silent-drop gates with zero diagnostics (`grep console\|throw\|warn` → 0) are what makes G1c structurally UNOBSERVABLE at the human seam, while the server twin ships `extract_contours_result` WITH diagnostics. ▲ **G7c honesty lock: the decomposition is NOT duplicated and the offline detour is ELECTIVE (`saveContour` accepts ingested contours) — the burn-down must not overstate duplication.** ▲ **SE-05's "just delete it" for the `window` handoff is WRONG on the tree (G6c)** — the precompute script names the component as its INPUT. ▲ **M-10 innerPoly ANTI-CURE RIDER — RESTORED THIS ROUND (P2-9 cluster; M-25 requires locks CARRIED where their rows land, and this one names the exact "cure" a burn seat would reach for first).** The banked record binds it to every cure of the rows booked in this cell — ⟨cmd⟩ **single-hit anchor, re-cut at round 4** `grep -nF '**M-10 bound in, seat-verified by read of the RNG interleave' fr-FourierShapeExtractor.md` (base `$G`) → **one hit, `:96`** *(the round-3 form `grep -n "innerPoly" fr-FourierShapeExtractor.md` returns **three** — `:10`, `:96`, `:157` — with one pasted and no statement of the selection)*. ⌧ **CUT DISCLOSED AT REPAIR ROUND 5 (REST-21).** The command prints line 96 **entire** (~200 chars), opening `96:- **D-16 / C-9 / L-m2-arm (+ M-10 rider, reader-2's find — REPAIR-CRITICAL)** — dead seams and stale prose …` — **including a different banked head, `fr-FourierShapeExtractor D-16` (≡ `C-9` · `L-m2-arm`), which the canonical homes at F.W4** — and the round-4 cure dropped that opening before the paste with no cut mark. **The banked head is named here and at every carrying site: `fr-FourierShapeExtractor D-16` (≡ `C-9` · `L-m2-arm`) — canonical F.W4 — and the innerPoly anti-cure rider `M-10` rides it and is not an identity of its own.** ⊘ **Round 4's own P4-D7 cure created this**: it replaced a 3-hit `grep -n "innerPoly"` with a `-F` anchor returning one line and then quoted a substring of that line. *A single-hit anchor is not a single-quote anchor.* The elided head is restored to the paste: → *"[96:- **D-16 / C-9 / L-m2-arm (+ M-10 rider, reader-2's find — REPAIR-CRITICAL)** — dead seams and stale prose (the three unread hooks; the false comment; `innerPoly` computed and discarded). ] ****M-10 bound in, seat-verified by read of the RNG interleave: `innerPoly`'s six per-iteration draws are entangled with `outerPoly`'s stream — any "cure" that skips the innerPoly work re-rolls the canonical sun.** All three axes booked the discard as waste; the naive fix is a trap. → **F.W4** for the seams, the M-10 rider travels with EVERY L-B1/L-B2 cure."* — and the record's verdict attaches it to this wave's span routing by name: ⟨cmd⟩ **two-stage windowed extraction (R4-2.2 — no giant bounded wildcard), base `$G`, run by this seat at round 4**: `L=$(grep -nF 'its geometry pipeline routes to' fr-FourierShapeExtractor.md | cut -d: -f1)` → `157`, then `sed -n "${L}p" fr-FourierShapeExtractor.md | cut -c281-409` → *"its geometry pipeline routes to F.W5-W8 (provenance union, with the DO-NOT-REGENERATE rider and the M-10 innerPoly trap attached)"* — **byte-true, and the cut is disclosed: line 157 continues past the closing paren with `, its page-surface debt…`, which is a different clause and is not part of this quotation.** ▲ **ROUND-4 CURE (R4-1.7 → R4-4.2): the round-3 pair is REVOKED, not patched.** Round 3 cured this cell's *emphasis* correctly — the plain form is restored and holds — but narrowed the bound in the same edit to `.{0,90}`, whose output stops at *"trap a"*; at `.{0,120}` it overshoots to *"attached), its page-surface deb"*. **No `-o` bound produces the span pasted beside it**, so an emphasis defect was converted into a command defect at the same cell, and the round-3 certificate reported this file at zero command-level defects while it stood. The lesson is the reason the two-stage form is now law: **a receipt cured by adjusting the instrument instead of re-running it is not a receipt.** The words are unchanged, the record is frozen, and **plain throughout remains the finding.** Round 2 pasted this sentence with `F.W5-W8` and the trap clause set in **bold**: emphasis added **inside quotation marks over a FROZEN, immutable record**, which is the one surface where a drift can never be blamed on a sibling's later edit. The command beside the quotation is the one that produces it. **Binding here: `innerPoly` is computed-and-discarded WASTE whose removal is NOT a cure — deleting the six per-iteration draws re-rolls the canonical sun geometry silently.** The seam-cure arm is F.W4's; **what travels into F.W6 is the prohibition**, and it binds every L-B1 / L-B2/C-2 act this row authorizes. DO-NOT-REGENERATE stops a deliberate regeneration; this rider stops an *accidental* one | **F.W5 §G states G1c–G10c** (⟨cmd⟩ `grep -n "G10c" waves/F-W5.md` → *"**G10c** The artifact's provenance prose is false \| **fr-FourierShapeExtractor L-m3 / C-10 ⊕ L-m5**…"* — **round-2 correction**: round 1 wrote "G1c–G9c" here and at §4's F.W5 cross-edge (both cured this round) while writing G1c–G10c elsewhere in the same file; **G10c is the home of `L-m3 / C-10 ⊕ L-m5`, which this cell's own header cites**, so the short range dropped a clause this row depends on); **F.W6 / the build lane burns — G16's GREEN-owner cell names this wave BY NAME** (the one non-CRUD row F.W5 hands here). **Track the seam first (G2c)** — an untracked, import-broken regenerator cannot be a cure input. *"What moon.json WAS generated from"* is **UNDETERMINED** → SS-13. **Dissent preserved: reader-1 held L-B1 and L-B2/C-2 at BLOCKER ("the tool's output is a regression and nothing in the tree says so"); the seat demoted both to MAJOR on latency — the pipeline is already dead, nothing regressed a user today. The TRIPWIRE restores BLOCKER weight the moment anyone regenerates** |

### §2.7 Liveness and projections (2 rows)

| id | sev | mechanism (witness) | cure + locks | sequencing |
|---|---|---|---|---|
| **PP-DEADSEAM** (⊕ **M-β4** ⊕ **L·m-6** ⊕ **GM-M4**; VV-R2-B cited) | INFO–MINOR band | `preview_path` is written as `""` at all three server sites (assets.py:85, image_storage.py:318, responses.py:22) while the client leaf is unreachable — **zero implementation on either side**. `EpicycleData.trace` — a 3000-sample polyline computed per response — has zero readers in the web tree and rides `structuredClone` into every IndexedDB draft (workspace.ts:95-104). `reconstructed_points` (500+500 floats) is fetched, sessionStorage-persisted whole, and read by nothing. `softDelete`/`restore` are the gallery store's only ETag/If-Match round-trip and have zero callers | ▲ **E10's cure, quoted by command (R2-1-LAW) — and CORRECTED, because the round-1 label was a paraphrase wearing a "verbatim" tag**: ⟨cmd⟩ `grep -n '\*\*E10\*\*' waves/F-W5.md` → *"**v2 states ONE disposition for produced-and-unconsumed fields, not three ad-hoc deletions.** The R6-8 seam's converse: server computes, nobody consumes."* (round 1 rendered this *"E10 verbatim: ONE stated disposition, not three ad-hoc deletions"* — a compression of the clause's subject into a phrase F-W5 does not contain; **the words are now the bank's**). F.W5 states the liveness predicate and the projection disposition (B4/E10); **F.W6 executes only the server-side strikes** — the three `preview_path` writes and the produced-and-unconsumed projections | ▲ **F.W6 must NOT pre-empt E16/G7:** a projection strike that is really a compression decision (`trace`, `reconstructed_points`) waits on the trie ruling — absent it, whole-snapshot duplication is the recorded shipped behaviour. The client-side `softDelete`/`restore` wiring belongs to **VV-R2-B's single F.W4 wiring unit** (route → `loadVisualization`; publish → `setVisibility`; delete → the store action) — F.W6 may not scatter it |
| **fr-EquationModeToggle FR-EMT-20** — **CITED, NOT BOOKED (R4-10; repair round 5, REST-15).** Canonical `` | `FR-EMT-20` | `R2` · `missed-5` | `F.W5` | **F.W5** | `` — **F.W5 with NO band leg at all**; the record routes `F.W5` alone (`fr-EquationModeToggle.md:50`). **Held at F-W5 §2's `E10` clause.** The E10 family's server strike that §2.7 executes is the MECHANISM; the identity is F.W5's. ⟨*census escape E-3, disposed by citation*⟩ | MAJOR (dead wire fields) | The same E10 family at the equations operation pair: `ComputeEquationResponse.reconstructed_points` is **produced at real compute cost** (`equations.py:100-106`), serialised, and has **zero consumers** — the client recomputes partial sums from `coefficients` — while `SimplifyResponse.term_count` likewise has **zero consumers**. The record's own words, at the frozen bytes: ⟨cmd⟩ `grep -o 'dead projection on the same operation pair.\{0,75\}' fr-EquationModeToggle.md` → *"dead projection on the same operation pair FR-EMT-1 convicts for under-projection. One envelope row, both directions."* **One operation pair, two directions — under-projection at F1 (FR-EMT-1, cited at §2.11c), over-projection here** | The **same** cure as the row above, never a second one: **E10's ONE stated disposition** applied to these two fields. ▲ **NO ad-hoc third deletion** — E10 exists precisely to stop three produced-and-unconsumed fields being struck three different ways. ▲ **`reconstructed_points` is the SAME field PP-DEADSEAM names**, so the two rows are ONE cut; `SimplifyResponse.term_count` is the only member of the family no other row reaches | F.W5 **E10** states; F.W6 burns the server strike. **Booked rather than cited because this wave ALREADY executes E10's family at the row above** — the disposition is a fourier server strike performed by this seat, which is the branch-3 test as amended at §2.11b. E10's carried-rows cell names the id: ⟨cmd⟩ `grep -o '\*\*FR-EMT-20\*\* (cited F1)' waves/F-W5.md` → *"**FR-EMT-20** (cited F1)"*. ▲ **Registry correction carried, not silently applied**: the record's *"Registry-swept by this seat: 0 banked hits"* cell is corrected by `fr-EquationView L·m-6` — one identity, two witnesses — and the correction is recorded at the amending clause; **the frozen record is never rewritten** (E-3/the amendment-relay idiom of §2.11 row 42). ▲ **The E16/G7 lock binds here exactly as above**: a `reconstructed_points` strike that is really a compression decision waits on the trie ruling |

### §2.8 Born-visibility and lineage (1 row)

| id | sev | mechanism (witness) | cure + locks | sequencing |
|---|---|---|---|---|
| **R-5 / G5** born-visibility ⊕ **E4** lineage (**GCM-1** cited; ⊕ **fr-GalleryCard D-7 / C·I-2** — named in E4's own folded-rows cell as *R-5 ⊕ GCM-1 ⊕ D-7 ⊕ X-2*, one identity, and cited at §2.11 row 44 rather than dropped) | P1 (census risk 6 — opposite defaults on a privacy-adjacent surface are contract-blocking) | A fourier remix child is born **`draft`** (visualization.py:277); a value.js fork child is hard-coded **`visibility: "public"`** (forks.ts:76) — opposite privacy defaults on the same verb, both shipping. Same-verb divergences ride alongside: remix requires an atom change (no-op → 422 `urn:contract:remix-noop`, :536-542) while fork is a verbatim copy (`colors = source.colors`, forks.ts:64); fourier runs no transaction by deliberate choice ("standalone-topology-honest", :493) while value.js's fork is transactional; fourier records `atom_ops` on the edge, value.js records nothing. GCM-1: a save after the modal's CTA POSTs a NEW entity with **no `fork_of`** despite the fork substrate existing — lineage silently unrecorded | One co-signed born-visibility rule **+ a create-visibility test on each side** (G5's GREEN condition). **F.W6 burns the fourier half only**; the value half routes to the value.js API row | ▲ **GCM-1's routing cure — emit `entry.slug`, route `/v/`, point the loader at `loadVisualization` — is F.W4's; F.W6 takes only the "no `fork_of` recorded" server arm.** F.W5 **E4** states the rule and ⊙ **owner ruling `R8`** (remix-vs-fork + born visibility) is owed — **R8, not "G5"** (R-1e label correction; G5 is the F.W5 *gate* over the clause, not the ruling id). Neither side's edits gate the other's — ⟨cmd⟩ `grep -n "neither side's edits gate the other's" COHESION.md` → *"- **SS-4 contract co-signature → F.W5 ∥ X·V API row**: neither side's edits gate the other's"*, under **COHESION §2 — *The cross-sub-tranche dependency graph*** (⟨cmd⟩ `grep -nE '^#{2,3} ' COHESION.md`; **round-2 correction (R2-2)**: round 1 addressed it to §1 three times — §1's SS-4 register row does not contain the sentence). **The verb-name reconciliation (remix vs fork) and the delta-required semantics are in the census's owed-rulings set and are NOT F.W6's to settle** |

### §2.9 The like verb ⊙ (1 row)

| id | sev | mechanism (witness) | cure + locks | sequencing |
|---|---|---|---|---|
| **FR-GFC-3** ⊙ (⊕ **GCM-2** ⊕ GalleryCard **C-1-as-corrected** ⊕ GIG **C-3** FOLD) | MAJOR (4 witnesses; BLOCKER framing KILLED at K-1) | `likes: int = 0`, the `(visibility, likes, -id)` index and `cursors.py:22`'s `"likes": "likes"` sort key all ship while **no like route exists anywhere** (`api.ts` grep 0; `routers/` census 0 over the 44/45-decorator surface); `gallery.ts:189-199` hard-codes `const liked = true` with a monotonic unguarded local bump (un-like unreachable; `GalleryView.vue:128` can only add), under an `aria-pressed` that lies. **The server sorts by a counter nothing can increment** | ⊙ **G10, quoted by command (R2-1-LAW) and RE-CASED to the bank**: ⟨cmd⟩ `grep -o "ship the 46th operation.\{0,57\}" waves/F-W5.md` (base `$W`; sibling pinned at §0.3) → *"ship the 46th operation **or** delete the affordance — **NO THIRD OPTION SHIPS**"* (round 1 down-cased the gate's own emphasis inside quotation marks; the bank shouts and so does this cell. **Round-4 bound cure, R4-7.1**: the round-3 `.{0,70}` ran on to `` SHIPS** | fourier AP `` — a truncation into the next table cell, unmarked; the bound now ends where the bank's clause ends). If SHIP: F.W6 is the burn seat, using the `liked_ips` dedup idiom already in-tree. If DELETE: F.W4's | Owner ruling G10 **PRECEDES**; F.W6 may not implement or delete unruled. ▲ The `aria-pressed` + re-click guard display arm is **F.W3/W4's regardless.** ▲ **Counting lock K-1: a bare `@router.` grep is blind to prefixed routers (`@gallery_router.get("/cursor")`) — read against 13/44 and cite 45/30/13, never zero.** **Dissent: challenge-C graded this BLOCKER on the carousel; K-1 killed the BLOCKER framing at MAJOR; GIG's C-3 re-grade to BLOCKER was refused by the identity guard (R-7: fold sustained, identity beats re-grade)** — GIG C-3 and GCM-2 fold BY REFERENCE, never re-booked |

### §2.10 ROUTED OUT — value-side, burned nowhere in this wave (4 rows)

**Routing law, verbatim and binding.** ⟨cmd⟩ `sed -n '200,202p' formation/fourier/CENSUS-2026-08-03.md` → **census §4, item 7** (the F.W6 wave sketch): *"value-side V-α/V-β/V-γ routed to the value.js API row per the contract (value-tree edits are NOT fourier-wave gates)"*. ⟨cmd⟩ `grep -n "neither side's edits gate the other's" COHESION.md` → **COHESION §2**, the SS-4 row: *"neither side's edits gate the other's"* (**round-2 corrections (R2-2)**: the census section is spelled **§4, item 7** — the census has no sub-numbered §4.7 heading; and the COHESION sentence lives in **§2**, not §1). F.W6's deliverable for all four is the **routing receipt** (bounds #3): it stamps nothing on the value tree and takes no credit for any cure. Measured read-only 2026-08-28 at the fold (value side is explicitly **not** under the F.W0 lock, which re-grounds the fourier tree): `git diff --stat -- api/src src` → **empty**.

| id | sev | mechanism (witness) | cure-shape (elsewhere) |
|---|---|---|---|
| **V-α** | P1 | `revertToVersion` (service/versions.ts:121-181) computes `newHash = computeContentHash(version.name, version.colors)` (:137); the stored `version._id` was computed by the SAME expression at creation (:38), so `newHash === version._id` **always**. `createVersionRecord` (:148) then calls `findByHash(hash)` → found → returns the hash **before any insert** (:40-44), while the palette update runs `$inc: { versionCount: 1 }` unconditionally (:172). Net: a revert bumps `versionCount`, records no author attribution, adds no `depth`, and leaves `versionCount` permanently drifted. No test asserts otherwise — the suite's only revert case is `"revertToVersion throws NotFoundError on missing palette"` (`__tests__/palette-versions.test.ts:83`) | value.js API row's act; the receipt names the row and cites **lane-crud §V-α** — ⟨cmd⟩ `sed -n '161p' formation/fourier/lane-crud.md` → *"**value.js defect V-α — revert writes no version row but still increments the counter.**"* (lane-crud §2's R-2 row; an F-W5 §0 bounds authority). ▲ **Correction of record (R2-1b), stated so no seat repairs from a phantom**: round 1 cited *"F.W5 §6c obligation (2)"*; there is no §6c (⟨cmd⟩ `grep -l '§6c' waves/F-W5.md` → **no output — F-W5 is not a member of the matching set** (set-membership, never a banked count of a moving file: R3-3.10)), **and F-W5 §4's `F.W5 → value.js API row` edge row — the real itemized obligation list — does NOT itemize V-α** (⟨cmd⟩ `grep -n 'value.js API row' waves/F-W5.md`: V-β · V-γ · TA-4 · fork born-visibility · G18's probe · D9 reconciliation). V-α's authority is lane-crud, not that list, and the receipt says so rather than manufacturing membership |
| **V-β** | P1 | `PaletteVersion._id` is GLOBAL: the content-hash of `(name, colors)` alone (model.ts:85-86; hash.ts:8-17 folds only `name` + `colors`, never `paletteSlug`). `findByHash` filters `{_id: hash}` with no slug scope (repository/paletteVersion.ts:13-15) and `insertIfAbsent` returns early on a hit (:44-47) — two palettes with identical `(name, colors)` share ONE version row whose `paletteSlug` names only the FIRST, so the second's `findByPaletteSlug` returns `[]`. fourier already solved this: `_id = f"{viz_slug}:{set_hash}"` (visualization.py:235, :129), tension documented in-source at :221-226 | Adopt fourier's compound per-entity `_id` **+ a test asserting two same-content palettes keep separate histories — none exists today** (G2's stated GREEN condition) |
| **V-γ** | P1 | `Palette.userSlug: string \| null` is nullable (model.ts:60) against fourier's required non-null `owner_slug` (visualization.py:119), and attribution on write is CONDITIONAL — an `if (userSlug)` guard fronts the version write at crud.ts:119, crud.ts:204 and versions.ts:147 — so an unattributable edit mutates the palette and writes **no version row at all** | value.js API row; **composes with F.W5 E18 — AA-10 and V-γ are the two halves of one R-7 clause with two burn seats** (R-1e re-key from the invented "D9"; `F-W5.md` **E18** names *AA-10 (D-M2/L-13) + lane-crud R-7 / V-γ* in one cell) |
| **TA-4** ⊙ | **P0** (census risk 3 — the union's diff layer is one-sided), OWNER-GATED | Re-measured 2026-08-28 at the fold: `grep -rln "atomdiff\|atomDiff" api/src src` → **one hit, a test comment** (`api/src/modules/palette/__tests__/palettes-forks.test.ts`); `ls api/src/lib` → *No such file or directory*. `J-diff-shape.md §2.5` mandates the exact path `lib/crud/atomdiff.ts` — the file T.W1/TA-4 excised — and §6's close-gate assumes BOTH probes exist, so value.js's side cannot run. value.js retains only the hashing half (`computeAtomHash`, `computeAtomSetHash`) and still emits `atomSetHash` on the wire (format.ts:45, :87), with hash.ts:41-42 saying so explicitly | ⊙ **the owner rules RESTORE (a TA-4 reversal), or v2 re-scopes value.js out EXPLICITLY with §6 re-authored to a one-sided verdict** (G4). **SS-4 must name the TA-4 restoration as PREREQUISITE or re-scope explicitly** — a standing edict, carried in this spec's own words (**round-2**: the round-1 label *"carried verbatim"* claimed a quotation this cell does not make; under R2-1-LAW a "verbatim" tag without a ⟨cmd⟩ note is a defect per se, so the tag is dropped rather than a receipt manufactured for it). ▲ **Declared consequence from this end: F-β's `set_hash` recompute reaches `/diff`, so any cross-repo diff assertion this burn-down makes is UNFALSIFIABLE until G4 closes.** F.W6 states the dependency and **manufactures no probe over an absent file.** Prior on record: the atom-diff pattern was "authored once (fourier), adopted twice (value.js twin)" and the twin's only survivor is a compiled corpse in an untracked build dir — E3/G4 exist to stop participation being inherited from a docstring again |

### §2.11 SPAN RE-CUT — the band-form identities, all homed (R-6) ⊕ the four-spelling escapes landed at §2.11c (R3-5.2) ⊕ the holder-cited at §2.11d (R4-9.5/R4-10) — **no roster integer (R4-5); the census is the canonical's, §0.1**

**Why this section exists.** The first pass of this spec measured the span with a single hyphen literal and was blind to 23 records; **56 of the 122 routed identities landed nowhere** — not a row, not a fold, not an exclusion — an escape rate of 46 % against this file's own §2.0 law (*"a span row cut nowhere is a silent drop"*). R-6 rules the census idiom en-dash-tolerant and orders each escape homed. It is homed below.

**Landing law, binding and narrow.** Where an identity's **STATING home is an F.W5 clause** — which is the case for the large majority — its landing here is a **CITATION of that clause, never a booking**. Booking would break R-5's one-home law and manufacture the double-booking FW6-G17 exists to forbid. Only the **22 booked** identities of §2.1–§2.10 are *cut to* F.W6 — the three cited at §2.3/§2.5/§2.7 are held elsewhere (REST-15/16/17); every row below is **cited, folded, or excluded**, and F.W6 claims credit for none of them. Clause ids are the **R-1e-corrected** ones, each verified against `F-W5.md`'s live register this pass. Registry coordinates are **MEASURE-AT-OPEN** (D-19) like every other anchor in this file.

| # | identity (record-qualified per R-5) | registry coord | STATING home (`F-W5.md` clause, R-1e-corrected) | landing |
|---|---|---|---|---|
| 1 | **BC-9 / C-6 / D-20** | fr-BasisCanvas:55 | **E8** *AnimationSettings — the three-way reconciliation* | CITED → F.W5 E8. **This is the clause this spec's §4 wrongly listed as GREEN-owned by F.W6 while carrying no row for it** — the mis-key is cured, the clause is F.W5's, and F.W6 books no AnimationSettings row |
| 2 | **fr-SpeedSelect SS-C-2** | fr-SpeedSelect:45 | **E8** | CITED → F.W5 E8, which **dedupes SS-C-2 and BC-9 as ONE identity** ("SS-C-2's unconstrained-`speed` cell carried"). F.W6 books SS-C-1's write leg only (§2.1); the sibling stays with the clause |
| 3 | **fr-BasisCanvas C-7** | fr-BasisCanvas:58 | **E9** *One shape, one name* | CITED → F.W5 E9 (`animation_data` written and read by nobody) |
| 4 | **fr-BasisCanvas C-5** | fr-BasisCanvas:57 | **B1** *The join relation — forward* | CITED → F.W5 B1 as a **fold by reference**; B1's own cell names it among the instantiating records |
| 5 | **fr-BasisCanvas C-17** | fr-BasisCanvas:102 | **E15** *ONE transport clause* | CITED → F.W5 E15. ▲ its recorded superlative rides, quoted whole this round — ⟨cmd⟩ `grep -n "race discipline" waves/F-W5.md` → *"▲ **The race discipline is a recorded superlative (S-6): do NOT cure the waste by removing the guard**"* (round 1 compressed *"is a recorded superlative (S-6):"* to *"is S-6 —"* inside quotation marks; restored) |
| 6 | **fr-BasisCanvas C-18** | fr-BasisCanvas:103 | **E15** | CITED → F.W5 E15 (`/api/api/…` under the Dockerfile default; config-gated) |
| 7 | **fr-BasisSelector M-9** ⊙ | fr-BasisSelector:58 | **D12** *Closed domains at the boundary* | CITED → F.W5 D12, **and its ⊙ owner ruling `R5` is restored to FW6-G18's roster** (*"admit it in the contract or stop minting it — the silent rewrite must not survive either branch"*) |
| 8 | **fr-BasisSelector m-7 — LIVE ARM RESTORED** | fr-BasisSelector:73 | **D7** *Client-derivable bounds* | CITED → F.W5 D7. ▲ **Lock correction (R-6.4)**: this spec previously carried only m-7's **killed** 422-straddle sub-claim in §5 and dropped the surviving row. Restored, by command (R2-1-LAW) — ⟨cmd⟩ **single-hit anchor, re-cut at round 4** `grep -nF '**m-7 = C-6-as-rescoped' fr-BasisSelector.md` (base `$G`) → **one hit, `:73`** *(the round-3 form `grep -n "m-7" fr-BasisSelector.md` returns **three** — `:73`, `:102`, `:118` — and pasted one of them without saying which, the shape §2.2 convicts in its own words: "the quote to BE the output, not a selection from it")* → *"73:- **m-7 = C-6-as-rescoped — MINOR** (422 scenario KILLED, K-3). The client's numeric window `[1,500]` is invented at the leaf, restated ~6× per control, and matches NONE of the server's **six** divergent windows … Constructive cure: generate client bounds from the operation models (the R6-8 dual). → **F.W5–W8**."* (the ellipsis marks the six-window enumeration, elided for length and never added to). The 422-straddle stays **KILLED at K-3** — a killed sub-claim never kills its host row |
| 9 | **fr-ContourSettings B-1** ‡ | fr-ContourSettings:40 | **D7** | CITED → F.W5 D7, whose cell carries the rider — quoted by command (R2-1-LAW): ⟨cmd⟩ `grep -o "typed range-checked descriptor table[^\"]*" waves/F-W5.md` → *"typed range-checked descriptor table for all six fields rides an **F.W5–W8 rider** — the contract seam owes client-derivable bounds"* (**round-2 correction**: round 1 up-cased the tail to *"CLIENT-DERIVABLE BOUNDS"* inside quotation marks — emphasis the bank does not carry. **Round-3 correction (Q-5 cure, R3-3.9), this seat**: round 2 then committed the mirror fault at the same string — it **stripped F-W5's bold from `**F.W5–W8 rider**`** and **prepended a leading `the `** to a `grep -o` output that begins at `typed`, so the command as written could not produce the text pasted beside it. Emphasis restored, the prepended article removed, and the command re-cut to a delimiter that ends where the bank's sentence ends. **The cell that convicts a drift is the cell most likely to commit one** — this string has now drawn the fault in both directions, which is why the receipt, not the label, is the control) |
| 10 | **fr-SliderControl R-16 = C-3** | fr-SliderControl:45 | **D7** | CITED as a **named fold** → fr-ContourSettings B-1 → F.W5 D7. **The fold sentence and its rider are the RECORD's bytes, not F.W5's** — ⟨cmd⟩ `grep -o "R-16 = C-3 —.\{0,150\}" fr-SliderControl.md` → *"R-16 = C-3 — BLOCKER (as banked) — FOLD → fr-ContourSettings B-1 → F.W3/W4**, with the R6-8 extension (operation records must carry field DOMAINS) already banked …"* (frozen corpus, `fr-SliderControl.md:45`, line-citable per R2-2.2(i)). **Round-2b re-address, this seat**: rounds 1 and 2 floated *"already banked there as the F.W5–W8 rider"* inside the F.W5 D7 landing with no ⟨cmd⟩, where it reads as F-W5's bytes — ⟨cmd⟩ `grep -l 'already banked there as the' waves/F-W5.md` → **no output — not a member** (R3-3.10). Right words, wrong file: the same R2-2 defect the PASS-2 check convicted at *"census §7"*, in a second instance this file had not yet found. **R-16 FOLDS — SliderControl books nothing new** |
| 11 | **fr-ContourSettings M-10** | fr-ContourSettings:67 | **D12** | CITED → F.W5 D12 (the shared-enum contract; two hand-maintained `Record<string,string>`s over a 7-member server enum) |
| 12 | **fr-ContourSettings M-15** | fr-ContourSettings:72 | **F5** *The error envelope is actually spoken* ‡ | CITED → F.W5 F5, which folds **C·D-02 ⊕ M-15 as one server identity, two witnesses** (no HTTPException handler; problem+json owed) |
| 13 | **fr-CoefficientsSpectrum M-13** | fr-CoefficientsSpectrum:65 | **A5** *No untyped operations* | CITED → F.W5 A5. ▲ **Collision guard (R-5)**: this is **NOT** the `M-13` booked at §2.6 — that one is `fr-ContourSettings M-13`, contour provenance. Two banked identities, one token, both kept |
| 14 | **fr-CoefficientsPanel FR-CP-16 = C-17 / L/A-3** | fr-CoefficientsPanel:48 | **A3** *Boundary-validation evenness* (A3 names it *"the denominator"*) | CITED → F.W5 A3 |
| 15 | **fr-FrequencyGraph FR-FG-13 = C-9** | fr-FrequencyGraph:48 | **A3** | CITED → F.W5 A3 (validate/brand the ordering at the boundary) |
| 16 | **fr-FrequencyGraph FR-FG-21** | fr-FrequencyGraph:59 | **D7** | CITED → F.W5 D7 (`maxBars` unclamped into a canvas dimension; the family's only unbounded request model) |
| 17 | **fr-EqCoefficientsPanel FR-EQC-8 = C-11** | fr-EqCoefficientsPanel:45 | **A5** | CITED → F.W5 A5 (positional `[re, im]` across two independently written remaps — one adapter, both directions) |
| 18 | **fr-EasingCurvePreview RESOLVER (D-10·L-4·C-D-4)** | fr-EasingCurvePreview:59 | **E11** *Easing domain hoisted to the operation* | CITED → F.W5 E11 — **the clause this spec's first pass mis-used the label "E11" for** (§2.6 contour provenance, now correctly E14+E17). The real E11's rows land here |
| 19 | **fr-EasingPicker L/M-3** | fr-EasingPicker:65 | **E11** (merged, **both ids preserved** per F.W5's G22 registry-integrity note) | CITED → F.W5 E11. Its own words, quoted whole this round — ⟨cmd⟩ **narrowed to the span at repair round 5 (REST-22)** `/usr/bin/grep -noE 'Server half.{0,120}' fr-EasingPicker.md` → *"Server half (Literal/enum on `AnimationSettings.easing`, shared.py:69): **F.W5-W8** — new booking, the CRUD/provenance union owns the model."* ⌧ *The round-3 form `grep -n "Server half" fr-EasingPicker.md` returns line 65 **whole** — a ~700-character table row opening `65:| **L/M-3 (decides C/i-1)** | No unknown-value posture over a persisted domain…` — of which ~600 characters were dropped undisclosed. **The words are the record's; the receipt was not the command's output.** The command is narrowed to the span so that paste and output now coincide.* (round 1 elided the `shared.py:69` anchor **and** the `F.W5-W8` route without ellipsis — the route was the very fact that puts this row in the span roster, so its elision was the load-bearing one; both restored. **Round-2b conformance, this seat**: round 2 restored the anchor but set it **bold**, emphasis the record does not carry — the record bolds `F.W5-W8` and nothing else on that clause — so the bold is removed and the marks inside the quotation are now the bank's alone). The booking is discharged **by F.W5's clause E11**, not by an F.W6 row |
| 20 | **fr-EasingPicker MISSED-E (save-race)** | fr-EasingPicker:88 | **E12** *Debounced mirror vs synchronous save* | CITED → F.W5 E12 — the clause whose label this spec's first pass mis-used for cache identity (now correctly E13) |
| 21 | **fr-EditorControlsDock D-12 / C-4 / C-5** | fr-EditorControlsDock:59 | **E5** *Create idempotency and dedupe* (E5's cell names the EditorControlsDock C-7 rider) ⊕ **C1**'s async-commit rider | CITED → F.W5 E5 + C1. ▲ **Sequencing carried from E5, quoted by command (R2-1-LAW) and CORRECTED**: ⟨cmd⟩ `grep -o "F\.W5's own binding.\{0,50\}" waves/F-W5.md` (base `$W`; sibling pinned at §0.3) → *"F.W5's own binding**: the server arm SEQUENCES AFTER the join split."* ▲ **ROUND-4 CURE (R4-7.1), and it is the Q-5 class recommitted at a second string — by the cell that had just convicted it.** Round 3 banked this at `.{0,60}`, whose true output is `F.W5's own binding**: the server arm SEQUENCES AFTER the join split. The **C-2` — so the cell committed **both** faults of the class it was curing one section over: an opening `**` **prepended** to a `grep -o` output that begins at `F`, and a tail **truncated with no elision mark**. **The bound is re-cut to `.{0,50}` so the command's output IS the paste, and the leading `**` is gone** — the quotation now opens mid-emphasis exactly as the instrument prints it, which is ugly and true, and the label means bytes or it means nothing. Row 9's own parenthetical named this in advance: *"The cell that convicts a drift is the cell most likely to commit one."* — round 1 rendered this *"SEQUENCES AFTER the **R6-8** join split **(D-4's C-28 fold)**"*, inserting two glosses inside the quotation marks; **the gloss is true and now sits outside them** (the join in question is the R6-8 seam split, and the C-28 fold itself is homed at F.W5 clause **B1**, cited there and booked there). §2.3's B-2 burn is the *create-path* arm; this is *the same seam's idempotency channel at the dock* — one clause, two instantiations, no double-booking |
| 22 | **fr-EditorControlsDock C-6 / C-7** | fr-EditorControlsDock:60 | **C1** *Authority class per operation* | CITED → F.W5 C1 — the clause that **instantiates intake R3-7b (OpenAPI security 0 of 45)** at this operation. ▲ C1's honesty lock rides: *"the asymmetry, not an exploit, is the defect"*; and **K-1 counting binds — 45 = 30 + 13 + 1 + 1, never 30 alone, never zero** |
| 23 | **fr-EquationPanel D-L5 + C-1** | fr-EquationPanel:48 | **C4** *Retry / limiter posture* | CITED → F.W5 C4 (full-payload re-upload vs the 5/60 s compute limiter; two-slot hardening) |
| 24 | **fr-EquationView B-1** ‡ | fr-EquationView:47 | **F1** *Partial projection* ‡ | CITED → F.W5 F1 (`latex_sigma` joins `SimplifyResponse` on both models). ▲ F1's **cure bound by M-CK: fix the key FIRST** |
| 25 | **fr-EquationView B-2** ‡ | fr-EquationView:48 | **F2** *One shared bound constant* ‡ | CITED → F.W5 F2. ▲ **K-13 kill lock carried: the "without ever touching the offending slider" headline is UNREACHABLE from defaults; the sessionStorage-restore path is the true one** |
| 26 | **fr-EquationView C·D-02** | fr-EquationView:56 | **F5** | CITED → F.W5 F5 (one identity with fr-ContourSettings M-15, row 12) |
| 27 | **fr-EquationView M-CK** | fr-EquationView:71 | **B1** | CITED → F.W5 B1. ▲ **M-CK SEQUENCING LOCK, by command (R2-1-LAW)**: ⟨cmd⟩ `grep -o "fix the key FIRST.\{0,50\}" waves/F-W5.md` → *"fix the key FIRST or the B-1 repair ships broken."* |
| 28 | **fr-EquationView M-SB** | fr-EquationView:112 | **F3** *No unbounded field beside bounded siblings* | CITED → F.W5 F3 (one `max_length`) |
| 29 | **fr-EquationView M-DV** | fr-EquationView:113 | **F4** *Server-side domain validator* | CITED → F.W5 F4 (pydantic `model_validator`; `domain_start == domain_end` ⇒ the catch-all 500) |
| 30 | **fr-FourierShapeExtractor L-M1 (+ the 44.08 arm)** | fr-FourierShapeExtractor:53 | **G3c** *Closure is CARRIED, never inferred* | CITED → F.W5 §G G3c (**one cure with `fr-FourierShapeExtractor C-3`**) |
| 31 | **fr-FourierShapeExtractor C-3** | fr-FourierShapeExtractor:54 | **G3c** | CITED → F.W5 G3c (one change with L-M1) |
| 32 | **fr-FourierShapeExtractor C-4** | fr-FourierShapeExtractor:55 | **G4c** *A parameter that governs* | CITED → F.W5 G4c |
| 33 | **fr-FourierShapeExtractor L-M2** | fr-FourierShapeExtractor:72 | **G8c** *Extraction SCOPE — space and visibility* | CITED → F.W5 G8c; magnitude → SS-13 |
| 34 | **fr-FourierShapeExtractor M-7** | fr-FourierShapeExtractor:85 | **G9c** *The guards check the wrong predicate — twice* | CITED → F.W5 G9c (correct guard: `!(totalLen >= 1)`) |
| 35 | **fr-FourierShapeExtractor M-8** | fr-FourierShapeExtractor:86 | **G3c** | CITED → F.W5 G3c (endpoint-exclusive sampler; same cure) |
| 36 | **fr-FourierShapeExtractor M-9** | fr-FourierShapeExtractor:87 | **G9c** | CITED → F.W5 G9c. ▲ **Collision guard: distinct from `fr-BasisSelector M-9` (row 7) and from `fr-GalleryView` FR-GV-7's `M-9` alias (row 51)** |
| 37 | **fr-FourierShapeExtractor M-11** | fr-FourierShapeExtractor:88 | **G8c** | CITED → F.W5 G8c (no paint-visibility filter) |
| 38 | **fr-FourierShapeExtractor C-14** | fr-FourierShapeExtractor:90 | **G4c** | CITED → F.W5 G4c (`resample_arc_length` undoes the polygon branch's exact-vertex care) |
| 39 | **fr-FunctionInput L-B1** | fr-FunctionInput:38 | **F2** | CITED as a **named fold** → fr-EquationView B-2 → F.W5 F2, at the bytes — ⟨cmd⟩ `grep -o "fr-FunctionInput L-B1 FOLDS and is not re-booked" waves/F-W5.md` → *"fr-FunctionInput L-B1 FOLDS and is not re-booked"*, and F2's carried-rows cell states the collision guard in the bank's own hand — ⟨cmd⟩ `grep -o "fr-EquationView B-2 ⊕ fr-FunctionInput L-B1/C-1 (FOLDS).\{0,90\}" waves/F-W5.md` → *"fr-EquationView B-2 ⊕ fr-FunctionInput L-B1/C-1 (FOLDS) ⊕ fr-FunctionInput C-8** *(NOT fr-FourierShapeExtractor L-B1 at G1c; U-12 qualification)*"*. **Round-2b anti-rename conformance, this seat**: rounds 1 and 2 rendered the fold sentence *"FunctionInput's L-B1 FOLDS…"* — a **possessive substituted for the record-qualified banked spelling, inside quotation marks** (⟨cmd⟩ `grep -l "FunctionInput's L-B1 FOLDS" waves/F-W5.md` → **no output — the possessive spelling is not a member** (R3-3.10)). A record prefix is address, not decoration; dropping it inside a quotation is exactly the collapse R-5's qualification law and FW6-G17's set-difference exist to prevent. ▲ **Collision guard: distinct from `fr-FourierShapeExtractor L-B1`, the TRIPWIRE row booked at §2.6 — and F.W5's own cell now carries that guard verbatim, so the guard is banked at both ends** |
| 40 | **fr-FunctionInput L-M3** | fr-FunctionInput:53 | **F6** *A response field states WHICH population it measures* | CITED → F.W5 F6; the copy arm is F.W4's. ▲ **Collision guard: distinct from `fr-FourierShapeExtractor L-M3 / C-6`, cited at §2.6 and homed at G5c** |
| 41 | **fr-FunctionInput C-8** | fr-FunctionInput:69 | **F2** | CITED → F.W5 F2 — ⟨cmd⟩ `grep -o "fr-FunctionInput C-8 rides the same constant" waves/F-W5.md` → *"fr-FunctionInput C-8 rides the same constant"* (**round-2b anti-rename conformance, this seat**: rounds 1 and 2 quoted it bare as *"C-8 rides the same constant"*, stripping the record prefix inside the quotation — the row-39 defect in its second instance, and `C-8` is a token this file record-qualifies everywhere else); Harmonics `:max="100"` forfeits half of `le=200` |
| 42 | **fr-FunctionInput N-4** | fr-FunctionInput:73 | **F3** | CITED → F.W5 F3. ▲ **AMENDMENT RELAY, emitted here and owed at the amending record**: N-4 **CORRECTS** banked M-SB (*"the ONE unbounded field"* — there are **two**, and the free-text one is the harder half). The relay is recorded at the AMENDING record; **the banked file is NEVER rewritten** |
| 43 | **fr-GalleryAdminBanner GAB-12 · C-7** ⊙ | fr-GalleryAdminBanner:51 | **A3** (generated client / runtime boundary) ⊕ **A4** ⊙ *One contract source* | CITED → F.W5 A3 + A4. The codegen-vs-hand-typed decision is ⊙ **owner ruling R7 (FR-AFP-71/A4)** — unruled, and F.W6 builds no codegen. Test seam with AA-44 → emitted to F.W9/W10. **This spec's substrate cites GAB-13 (the 28-path dirty worktree); GAB-12 is a different row and is now carried, not dropped** |
| 44 | **fr-GalleryCard D-7 / C·I-2** | fr-GalleryCard:52 | **E4** *Born visibility + the fork/remix vocabulary* ‡ | CITED → F.W5 E4, whose folded-rows cell names **R-5 ⊕ GCM-1 ⊕ D-7 ⊕ X-2** in one identity. §2.8's R-5/G5 row now carries the D-7 leg by citation rather than omitting it |
| 45 | **fr-GalleryCardModal GCM-10 · D-07/L-5/C-3** | fr-GalleryCardModal:56 | **A3** (⊕ GCM-55, same cell) with the `tier`-default arm in **D17**'s serialization-bypass family | **RE-LANDED THIS ROUND (R3-9.3) — branch 2, a NAMED FOLD into §2.4's booked `FR-AFP-36 ⊕ D17 ⊕ GCM-55` row**, not a branch-3 citation. The PASS-3 seat was right that a 3-CITE landing here contradicted the procedure: GCM-10's routed cure — *a `tier` field with a default on the model* — **is** a fourier server act, so the clause-green discriminator sent it to BOOK while the cell said CITE. The amended discriminator resolves it without minting a row: the act is **already carried inside a booked row** (GCM-55's registry cure reads *"with GCM-10's model fix"*), so it lands as branch 2 at its carrier — the fold is the identity guard, not a demotion, and no second home is created. Its **A3** leg (generated/checked boundary) remains a citation of F.W5 A3, stated at the carrier. ▲ **Correction, retained**: this spec previously named GCM-10 **only** to strike its dangling "MF-9" companion cite; the routed server cure (*a `tier` field with a default on the model*) is now carried — and it is **load-bearing for §2.4's booked GCM-55**, whose registry cure reads *"with GCM-10's model fix"*. The "MF-9" strike stands unchanged (§5) |
| 46 | **fr-GalleryFeaturedCarousel FR-GFC-1 = D-1/C-3 ⊕ MISS-1(DU)** ‡ | fr-GalleryFeaturedCarousel:32 | **D10** *The list contract — filters and windows* | CITED → F.W5 D10 at blocker weight. **The list-contract sentence is the RECORD's, not F.W5's** — ⟨cmd⟩ `grep -o "F\.W5-W8 rider\*\* (the list contract owns.\{0,61\}" ⟨*bound re-cut at repair round 5, REST-23: `.{0,80}` over-ran the paste to `see FR-GFC-4). |`, one table-cell boundary past the quoted span, unmarked*⟩ fr-GalleryFeaturedCarousel.md` → *"F.W5-W8 rider** (the list contract owns whether search/tier/basis become real params — see FR-GFC-4)"* (frozen corpus, `fr-GalleryFeaturedCarousel.md:32` — and this is the routing cell that puts the row in the span roster at all). F.W5's own share is stated in different words: ⟨cmd⟩ `grep -o "FR-GFC-1's share at F.W5 is the RIDER ONLY.\{0,49\}" waves/F-W5.md` (base `$W`; sibling pinned at §0.3) → *"FR-GFC-1's share at F.W5 is the RIDER ONLY** — whether search/tier/basis become real params"* (**round-4 bound cure, R4-7.1**: the round-3 `.{0,60}` ran on to `` params; **the bod ``, and the cell stopped at *"params"* with no elision mark — the bound now ends where the paste does). **Round-2b re-address, this seat**: rounds 1 and 2 attributed the sentence to F.W5 D10 **and dropped its final word** — ⟨cmd⟩ `grep -l "the list contract owns whether" waves/F-W5.md` → **no output — not a member** (R3-3.10), and *"become real"* without *"params"* changes a claim about request parameters into a claim about the features themselves. Wrong file, and a truncation inside quotation marks with no ellipsis. **Two BLOCKER filings on FR-GFC-1 (D and C axes) preserved in dissent** |
| 47 | **fr-GalleryInfiniteGrid C-4 / D-13** | fr-GalleryInfiniteGrid:51 | **D10** | CITED as a **named fold** → FR-GFC-1 + FR-GFC-4 → F.W5 D10 / F.W8. Its FR-GFC-1 leg was the unhomed one; it is homed at row 46 |
| 48 | **fr-GallerySearchBar FR-GSB-1** | fr-GallerySearchBar:30 | **D10** | CITED as a **named fold** → FR-GFC-1 (F.W4 arm) + FR-GFC-4 (list contract → F.W8) |
| 49 | **fr-GallerySearchBar FR-GSB-28** | fr-GallerySearchBar:68 | **B1** | CITED → F.W5 B1 (`resetAndFetch`/`fetchNextPage` share one abort key). ▲ B1's compose-lock rides, at the bytes — ⟨cmd⟩ `grep -o "The abort-key fix and FR-AUL-59's pipeline fix.\{0,46\}" waves/F-W5.md` (base `$W`; sibling pinned at §0.3) → *"The abort-key fix and FR-AUL-59's pipeline fix **compose; neither alone closes the window**."* (**round-4 bound cure, R4-7.1**: the round-3 `.{0,60}` ran on to `` window**. Map-growth ri ``, unmarked. ▲ **And the id inside this lock is `fr-AdminUserList FR-AUL-59`, held at F.W5 and cited at §2.11d H-10** — for three rounds this quotation was the only place its token appeared in this file, which is a citation of the lock, not a disposition of the id) (**round-2b conformance, this seat**: rounds 1 and 2 down-cased the clause's opening *"The"* and dropped the bold F.W5 puts on the operative half — emphasis-drift inside quotation marks, the same class cured at rows 9 and 19 this round) |
| 50 | **fr-GallerySearchBar C-2 grade — ▲ WAVE-LOCK, RESTORED** | fr-GallerySearchBar:24 | fold → fr-BasisSelector M-10 (banked MAJOR → F.W3/W4) | **NOT a row — a LOCK, carried by command (R-6.4 + R2-1-LAW; M-25 requires locks CARRIED, not cited away)** — ⟨cmd⟩ `grep -o "carried as a \*\*WAVE-LOCK\*\*.\{0,180\}" ⟨*bound re-cut at repair round 5, REST-23: `.{0,190}` over-ran to ``fourier-series}`. | Both re``, unmarked*⟩ fr-GallerySearchBar.md` → *"carried as a **WAVE-LOCK** on any F.W5-W8 wiring of `basisFilter` — wire it without the banked normaliser and the Fourier pill returns zero rows against `active_bases ∈ {fourier-epicycles, fourier-series}`."* This is the corpus's **only** lock binding *"any F.W5-W8 wiring"* by name; it binds **any** burn seat in this span, this wave included, and is repeated at §4 lock 9 |
| 51 | **fr-GalleryView FR-GV-7 = M-9 / C·M-9** | fr-GalleryView:43 | **A6** *Credential transport — both ends* (cited at **C3**) | CITED → F.W5 A6/C3 (an admin bearer accepted from the URL query string); the F.W3/W4 arm is not F.W6's |
| 52 | **fr-AppHeader FR-AH-6 · C-6** | fr-AppHeader:52 | **C3** *Session truth and the canonical admin predicate* | CITED → F.W5 C3 — *Session truth and the canonical admin predicate*. **The lower-case sentence rounds 1–2 quoted is the RECORD's, not F.W5's** — ⟨cmd⟩ `grep -o "F\.W4\*\* (one predicate, one home).\{0,76\}" ⟨*bound re-cut at repair round 5, REST-23: `.{0,90}` over-ran to `is canonical) |`, unmarked*⟩ fr-AppHeader.md` → *"F.W4** (one predicate, one home) + **F.W5-W8 rider** (the admin-auth seam owns which predicate is canonical)"* (frozen corpus, `fr-AppHeader.md:52` — again the routing cell that admits the row to the span). F.W5 states the same law in its own emphasis: ⟨cmd⟩ `grep -o "the admin-auth seam owns WHICH predicate is canonical (F\.W5)" waves/F-W5.md` → *"the admin-auth seam owns WHICH predicate is canonical (F.W5)"* — **upper-case WHICH, and it names its own wave**. **Round-2b re-address, this seat**: one sentence, two homes, two spellings; rounds 1–2 pasted the record's spelling under F.W5's attribution, so neither file's bytes matched the quotation as addressed. Both are now quoted where they live, per R2-1-LAW clause 4 (two homes are two quotes, never one fused attribution) |
| 53 | **fr-InfoCard FR-IC-6** | fr-InfoCard:42 | **A3** (A3's carried-rows cell names it in the banked parenthesised spelling — ⟨cmd⟩ `grep -o "FR-IC-6 (→L·m-7)" waves/F-W5.md` → *"FR-IC-6 (→L·m-7)"*; round 2's re-spaced *"FR-IC-6 → L·m-7"* is conformed to the bytes) | CITED → F.W5 A3; the F.W4 arm is F.W4's |
| 54 | **fr-VisualizationView BLK-1** | fr-VisualizationView:45 | **E4** (E4's cell: *"BLK-1 FOLDS"*) | CITED as a **named fold** → fr-GalleryCardModal GCM-1 → **F.W4** (held) / the **F.W5-W8** band leg. **Correction**: §2.3 names BLK-**2** as folded; BLK-**1** is a distinct banked id and is now carried. ⌧ **Re-cut at repair round 5 (REST-28; P5-19).** The landing read *"→ F.W5 E4 / F.W4"*; canonical `` | `BLK-1` | — | `F.W4` · `F.W5–W8` | **F.W4** <sub>legs: F.W5-W8</sub> | `` — **the canonical knows no F.W5 home for it.** E4 is the clause the cure runs at; the identity is F.W4's and its band arm is a leg, not a home |
| 55 | **fr-SpeedSelect SS-L-07 / SS-C-10** | fr-SpeedSelect:59 | **F.W5 §5 — the *Carried by citation* row `SS-L-07 / SS-C-10`**, which itself cites **F-W4-CARRY §Rows D (the transport cluster), the `SS-L-07/SS-C-10 ⟨SpeedSelect⟩` row** (R-2a — **re-anchored at the round-2 close**: the bare *"§D"* spelling addresses a heading `carry/F-W4-CARRY.md` does not have; ⟨cmd⟩ `grep -h '^## §Rows$' carry/F-W4-CARRY.md` → `## §Rows` and ⟨cmd⟩ `grep -h '^### D · ' carry/F-W4-CARRY.md` → `### D · fr-AnimationControls · fr-GlassTimeline · fr-SpeedSelect · fr-EasingPicker · fr-EasingCurvePreview (the transport cluster)` — **`-h`, prefix-free by design**, per F-W10 §2.8 **E-11**'s standing lesson that a provenance note must not smuggle back the address the ruling abolished; the citable anchor is **§Rows D** — the spelling F-W5 and F-W8 §5c already carry, and the one F-W8's own round-2 minute names as the correction of *"§D"*) | CITED → F.W5; the copy arm is F.W4's. Its type reaches the wire rows this wave burns, so the dependency is declared and no F.W6 row claims it. ⟨cmd⟩ `grep -n 'SS-L-07 / SS-C-10' waves/F-W5.md` → *"**F.W5's share is the type-reach only**: the speed domain arrives at the wire as `speed: number` on both sides… Booked by citation, not re-booked"*; ⟨cmd⟩ `sed -n '234p' carry/F-W4-CARRY.md` → *"Its type reaches the F.W5–W8 wire rows."* **Round-2 re-anchor (R2-2)**: round 1 addressed the carry by line (`:234`); a carry is a live sibling, so the address is now the **§D row label** and the line survives only as this round's ⟨cmd⟩ receipt |
| 56 | **fr-AnimationControls C-30** | fr-AnimationControls:123 | **B1** — R-2a homes it at F.W5 as a fold-by-reference row *"with R6-8's carry — the join-ambiguity leg named"* | CITED → F.W5 B1. §2.6 carries R6-8 only through i-7's cache-identity leg; **the join-ambiguity leg is C-30's and is homed at F.W5, not here** |

| **56a** | **fr-GalleryView FR-GV-13 = MISS-LC-2 — the SERVER arm** ⟨**the one escape the PASS-2 seat measured, homed here**⟩ | fr-GalleryView:49 | **D10** *The list contract — filters and windows* (⟨cmd⟩ **single-hit anchor, re-cut at round 4 — the round-3 cell shared a 4-hit `grep -n "FR-GIG-5"` with §0's standing-bar quotation, so neither paste was its command's output**: `grep -oF 'FR-GV-13 (=MISS-LC-2)' waves/F-W5.md` (base `$W`; sibling pinned at §0.3) → *"FR-GV-13 (=MISS-LC-2)"*, **one output line, no line number banked** — R2-2 forbids a line cite into a live sibling, and this round proved why: the `-n` form of this very anchor returned `:162` at the start of this seat's sitting and `:177` an hour later, because F-W5's own round-4 seat was writing while this was being authored. The words did not move; only the address did. The anchor names D10's carried-rows cell, whose body reads: *"`visibilityFilter` is a **PRODUCERLESS store ref** (3 repo hits, all inside `stores/gallery.ts`, zero writers) so `ownerParam()` returns `undefined` unconditionally and the server's whole `owner == "me"` branch is DEAD from this client"*) | CITED → F.W5 D10. **Banked routing, quoted whole** — ⟨cmd⟩ `grep -n "FR-GV-13" fr-GalleryView.md` → *"**FR-GV-13 = MISS-LC-2 (adopted after re-proof) — MAJOR → F.W5-W8 (+ F.W3/W4 control).** … the server's whole `owner == \"me\"` branch (visualizations.py:305-309, with its own session-invalid error path) is dead from this client — a shipped capability with a documented contract (`gallery.ts:34-36`), server support, a client parameter, and no control."* **The banked PRIMARY route is the span; the F.W3/W4 control is the parenthetical.** Round 1 landed only the parenthetical — the `?owner=me` control at the F.W3/W4 cross-edge and a §5 client-arm exclusion — so the **server** arm (the dead `owner == "me"` branch, its session-invalid error path, and whether the list contract keeps or retires it) exited through no branch at all. It is **CITED to D10, not booked**: D10 is the clause that owns *"whether search/tier/basis become real"*, and the same clause owns whether a producerless filter's server branch is wired or deleted — one home, and the disposition is the list contract's, not this burn seat's. **Denominator note (no double count)**: FR-GV-13 is ONE identity of the 122 with TWO landing cells — this citation for the server arm, the §5 exclusion for the client control — the standard *one home, two citations* shape used throughout this file. The roster stays **122**; this cell is numbered `56a` precisely so no arithmetic below shifts |

**The roster tail is ENUMERATED AT §2.11c, not asserted here (round-3 cure, R3-5.2 / PASS-3 D-7).** Rounds 1–2 closed this section with *"**Rows 57–122** are the 66 identities already booked in §2.1–§2.10 and §5 … **They are not restated here**"* — and since §2.1–§2.10 books 22 ⟨*25 until repair round 5, REST-18*⟩ and §5's rows are largely *classes*, **more than half of FW6-G17's upper operand was asserted rather than listed and the gate's own set-difference could not be run from this file.** That sentence is **STRUCK**; every remaining identity is printed by id, with its branch and its landing section, at **§2.11c BLOCK B** (the booked) and **BLOCK C** (the tail's folds and routings). `FR-GV-13` remains ONE identity with two landing cells — row 56a above is its server arm, BLOCK C carries its client control — and is counted once. FW6-G17 now reads §2.1–§2.11c and §5 as one **enumerated** operand.

#### §2.11a NEGATIVE ROSTER — the seven measured zeros (FW6-G17's lower-bound operand)

A set-difference with only one operand cannot run in both directions. Seven adjudicated records declare an **explicit zero** to the span; they bound the roster from below and are carried verbatim (R-6.3), **each re-read at its coordinate BY COMMAND this round (R2-1-LAW; the corpus is frozen and immutable, so line coordinates are the one address form R2-2 still permits)** — ⟨cmd⟩ `for f in fr-App:144 fr-CollapsibleSection:131 fr-MobileFloatingToc:97 fr-PaperSearch:90 fr-PaperSearchDropdown:77 fr-PaperSearchInput:125 fr-MorphShapePreview:110; do sed -n "${f#*:}p" ${f%:*}.md | grep -oE '.{0,10}F\.W5[-–]W8.{0,80}'; done` → **the loop is a LOCATOR, not the claim.** ⌧ **RE-CUT AT REPAIR ROUND 5 (REST-24; P5-8 / P5-10).** Round 4 attested *"all seven reproduce, in order, with the cells below"* — **false at three of seven.** The loop prints a ~90-character window and three cells do not coincide with it: `fr-App`'s exceeds it at **both** ends, `fr-MorphShapePreview`'s **prepends** the `preser` the `.{0,10}` prefix cut and drops the closing `.`, and `fr-MobileFloatingToc`'s continues past the window after its ellipsis. **The corrected claim, measured this seat: all seven cells are verbatim in their records** — ⟨cmd⟩ `/usr/bin/grep -cF '<cell>' <record>.md` → **1** at each of the seven — **and the loop's window does not coincide with three of them; the cells are the claim, the loop is a locator.** ⊘ Receipt-vs-paste, never fabrication — **and round 3's Q-7 cure completed one cell of seven and declared the label exact.** **Round-3 precision (Q-7 cure, R3-3.9)**: rounds 1–2 labelled this *"byte-for-byte"*, and at one cell it was not — `fr-CollapsibleSection:131` continues past the point the cell stops at, and the cell carried **no elision mark**. The label is now exact because the cell is: the `fr-CollapsibleSection` declaration below is completed to its clause boundary — ⟨cmd⟩ `sed -n '131p' fr-CollapsibleSection.md | grep -oE '.{0,10}F\.W5[-–]W8.{0,110}'` → *"tirement; F.W5–W8 gets nothing — the component is API-inert, and that negative is recorded; the census-methodology rows go to S"* — so the quoted span ends where the bank's clause ends and nothing after it is silently dropped. **An unmarked truncation inside a cell labelled byte-for-byte is the same defect as an added emphasis: the label asserts a property the bytes do not have.** The remaining six reproduce whole:

| record | declaration (verbatim) |
|---|---|
| fr-App:144 | *"**Nothing routes → F.W5–W8**: App's coupling budget is spent entirely on the CSS-token↔parser seam and the mount seam; the CRUD/provenance union has no edge in this component (C-12)."* |
| fr-CollapsibleSection:131 | *"F.W5–W8 gets nothing — the component is API-inert, and that negative is recorded;"* (clause-complete this round; the `;` is the bank's, and the sentence continues into an unrelated census-methodology routing) |
| fr-MobileFloatingToc:97 | *"**F.W5–W8 gets nothing** — the component is API-inert, value.js-inert, keyframes-inert … the negative is recorded so no CRUD/provenance wave budgets this file"* |
| fr-PaperSearch:90 | *"**F.W5-W8 empty** (measured zeros)"* |
| fr-PaperSearchDropdown:77 | *"**F.W5-W8 = no rows.**"* |
| fr-PaperSearchInput:125 | *"**F.W5-W8 empty** (the banked zeros hold)"* |
| fr-MorphShapePreview:110 | *"preserved so no F.W5-W8 row manufactures overlap"* |

▲ **B5 discipline, carried from `F-W5.md`: negative controls are EVIDENCE, never a denominator.** These seven bound the roster; they are not counted into any defect denominator, and **no F.W6 row may manufacture overlap into a record that declared zero**.

#### §2.11b DERIVATION LAW — how a span identity becomes an F.W6 row

Round 1 stated the mechanism subset as a decision procedure, so FW6-G17 would have a rule to check rather than a taste to agree with. **The procedure as written was UNRUNNABLE and is re-authored here (P2-9 cluster).** The defect, stated plainly so it cannot recur: its branch 2 read *"Does an `F-W5.md` clause already STATE it? If yes → CITE the clause; do not book"* and sat **above** the booking branch — but **every one of the identities booked at §2.1–§2.10 is a server act that an F.W5 clause states**, and each of those rows says so in its own sequencing cell (F-α→E2/G3 · F-β ⊕ SS-C-1→E7 · F-γ→C5/G6 · FR-AFP-4→C1/C2/G8 · FR-GV-12→E6/G12 · B-2→E5 · FR-AFP-1→D3 · FR-AFP-66→D6/R6 · FR-AFP-7→D5/D4 · FR-AFP-33→D7 · FR-AFP-36→A4/D17 · FR-GV-9→D8 · AA-10→E18/D16 · B-4→E13 · fr-ContourSettings M-13→E14+E17 · fr-FourierShapeExtractor L-B1→§G · PP-DEADSEAM→B4/E10 · **FR-EMT-20→E10** · R-5/G5→E4 · FR-GFC-3→D2). Run in order, the old branch 2 captured every one of them and routed them to CITATION — so *"every one of the 122 exits through exactly one branch"* was **false**, and FW6-G17's promised rule could not be executed. The missing discriminator is the one this wave exists for: **F.W5 STATES; a clause whose GREEN-owner cell names F.W6 (or "the fourier API row") is one F.W6 BURNS.** Stating and burning are the two halves of one clause, never two homes for one row.

**The procedure, corrected.** For each identity this file disposes — the register enumerated at §0.1, **not an integer** (R4-5: the "132" this sentence used to name is struck) — in order, the first matching branch decides:

1. **Is its terminal disposition a fourier-side SERVER/persistence act** (model, router, index, serializer, cascade, cache key, audit write, blob/asset op)? If **no** → it is not F.W6's; route by its own arm (F.W3/W4 render+copy · F.W8 client wiring + conformance · F.W1 producer transaction · F.W9/W10 coverage) and record the routing. *(This branch alone disposes of every client/display arm at §5.)*
2. **Does an adjudicated record FOLD it into another identity?** If yes → carry it as a **named fold** at the carrier, never as a second row (the fold is the identity guard, not a demotion).
3. **Does an `F-W5.md` clause STATE it — and is THIS ROW'S OWN terminal disposition an act F.W6 performs on the fourier server?**
   - **YES to both** → **F.W6 BOOKS the EXECUTION** as a §2.1–§2.10 row, citing the stating clause by id. This is *not* a second home: F.W5 owns the CLAUSE, F.W6 owns the BURN-DOWN — ⟨cmd⟩ `grep -n "owns the CLAUSE" waves/F-W5.md` → **F-W5 §2 §E clause E2 ▲**: *"F.W5 owns the CLAUSE; F.W6 owns the burn-down — do NOT double-book."*
   - **Clause states it, and the row's terminal disposition is something else** — a decision the clause itself discharges (a contract rule, a closed domain, an admission, a stated policy), or an act another wave performs (F.W3/W4 render+copy, F.W8 wiring, F.W1's transaction, the value.js API row, F.W9/W10 coverage) → **CITE the clause; do not book.** (R-5 one-home law. This is the branch that takes most of §2.11.)
4. **Is it ⊙ owner-gated?** If yes → it may be carried and its landing stated, but **not burned**, until FW6-G18's roster shows the ruling; the honest default is recorded inline and never presumed. *(⊙ conditions a burn; it does not re-route it — the row keeps whatever landing branches 1–3 gave it. **Amended this round**: where a ruling could convert a clause-discharged decision into a server act, the row is CITED today and its **conditional promotion** is stated in its landing cell — `fr-BasisSelector M-9` ⊙ under R5 is the worked instance.)*
5. **Otherwise** — a fourier server act that no F.W5 clause states — → it books as a §2 row here on its own witness, with its cure, its locks and its sequencing. *(§2.0's RE-CUT-F6 is the only spec-integrity row of this shape, and it is recorded, not booked.)*

▲ **THE ROUND-3 AMENDMENT, and why the round-2 discriminator was still wrong (R3-9.3).** Round 2 keyed branch 3 to *"does that clause's GREEN-owner cell name F.W6 or the fourier API row"*. Run against this file's own clause lists it contradicted the landings in **both** directions, and the PASS-3 seat proved it at the bytes: **`FR-AFP-33` is booked at §2.4 while its stating clause `D7` sits in the CITES-not-owned list** (branch 3 sent it to CITATION), and **seven §2.11 citations sit on clauses this file lists as F.W6-owned** — rows 7 (`D12`), 11 (`D12`), 21 (`E5`+`C1`), 22 (`C1`), 45 (`A3`+`D17`), 46 (`D10`), 56a (`D10`) — so branch 3 sent all seven to BOOK. **The clause's green owner was never the discriminator; the ROW's own terminal disposition is.** A clause is a rule, and one rule is instantiated by many rows whose cures land in different waves: `D12` states *closed domains at the boundary* and F.W6 burns its server half, but `fr-ContourSettings M-10`'s cure is two hand-maintained client `Record<string,string>`s and `fr-BasisSelector M-9`'s is an ⊙ contract admission — neither is an act this seat performs. `D10` states the list contract and owns *whether* a filter becomes a real param; that decision is the clause's, not the burn seat's. `C1`/`E5` state authority class and create idempotency; rows 21/22 instantiate them **at the dock**, where the cure is the client's. `D7` states client-derivable bounds and its green owner is elsewhere — but `FR-AFP-33`'s own terminal cure is *bound the `$push` with `$slice` and scope the aggregate*, which is an act on `admin.py` performed here.

**Eight landings, re-run under the amended branch and printed** — seven stand, one is re-landed:

| identity | stating clause | round-2 branch said | terminal disposition (the amended test) | landing NOW |
|---|---|---|---|---|
| `fr-AdminFlaggedPanel FR-AFP-33` | D7 | CITE | server: bound the `$push`, scope the aggregate (`admin.py`) | **BOOK** (§2.4) — unchanged, now legible |
| `fr-BasisSelector M-9` ⊙ | D12 | BOOK | ⊙ contract admission — *admit it or stop minting it* | **CITE** (row 7) — unchanged; conditional promotion declared |
| `fr-ContourSettings M-10` | D12 | BOOK | two hand-maintained client enum maps | **CITE** (row 11) — unchanged |
| `fr-EditorControlsDock D-12/C-4/C-5` | E5 ⊕ C1 | BOOK | the dock's async-commit arm — client | **CITE** (row 21) — unchanged |
| `fr-EditorControlsDock C-6/C-7` | C1 | BOOK | an instantiation of the authority class at one operation | **CITE** (row 22) — unchanged |
| `fr-GalleryCardModal GCM-10` | A3 ⊕ D17 | BOOK | a `tier` field default on the model — a server act **already carried inside a booked row** | **RE-LANDED → branch 2**, named fold at §2.4's `FR-AFP-36 ⊕ GCM-55` |
| `fr-GalleryFeaturedCarousel FR-GFC-1` | D10 | BOOK | the list contract's own decision (*whether* search/tier/basis become params) | **CITE** (row 46) — unchanged |
| `fr-GalleryView FR-GV-13` (server arm) | D10 | BOOK | same — the list contract's disposition | **CITE** (row 56a) — unchanged |

**One re-landing, seven confirmations, and the discriminator now names the thing that actually decides.** The transcript at **§2.11c** runs the corrected procedure over the whole roster and prints the branch each identity took, which is the only form in which a decision procedure can be checked rather than agreed with.

Every identity in the register exits through exactly one branch (**R4-5**: the count-word here was "132" and is struck — the claim is *one branch each*, which is checkable per row and does not need a total), and the branch it took is legible from its landing cell: branch 1 → §5 or a cross-edge; branch 2 → a named fold at its carrier; branch 3-YES → §2.1–§2.10; branch 3-CITE → §2.11 / §2.11c; branch 5 → §2.1–§2.10 on its own witness. **A row that exits through none is the silent drop FW6-G17 fails on — and a procedure whose branches cannot be run in order is itself that failure, disguised as a rule. It has now been run; §2.11c is the run.**

#### §2.11c THE RUN — the corrected procedure executed, branch printed, upper operand ENUMERATED (R3-9.3 ⊕ R3-5.2; cures PASS-3 D-3 and D-7)

**Why this section exists.** FW6-G17's falsifier (a) required that every span identity appear in §2.1–§2.10, §2.11 or §5. §2.11 enumerated **56 (+56a)**; the next sentence disposed of *"rows 57–122"* **without naming them**, and §5's rows are largely *classes* — so **more than half of the gate's upper operand was asserted rather than listed, and the set-difference the gate's own GREEN condition specifies could not be executed from this file.** That is the same unrunnable shape the round-2 repair cured one section below it. It is enumerated here.

▲ **THE COUNTING RULE — re-stated at round 4 so it no longer contradicts an arithmetic it never fit.** This transcript prints **every identity this file disposes**. A **named fold** (branch 2) is transcribed at its carrier and is **NOT a separate identity** — that is what *"the fold is the identity guard, not a demotion"* means. Where an identity has two landing cells (`FR-GV-13`: server arm at §2.11 row 56a, client arm here) it is **one** identity, disposed once. ~~**The roster stays 132.**~~ ▲ **STRUCK (R4-5).** That sentence and this transcript were irreconcilable and the check proved it at the bytes: **BLOCK C marks 42 rows branch-2 — the branch this very rule declares NOT roster members — while the 132 partition required exactly 42 tail MEMBERS**, so the two 42s were the same number playing opposite roles and the roster closed at 111 or 153, never 132. **The rule was right and the integer was wrong, so the integer goes and the rule stays.** The transcript is the runnable operand; the record-side census is the canonical's (§0.1), which homes **zero** rows at F.W6; and **nothing in this section is summed into a denominator by this file ever again.**

**BLOCK A — the ten census escapes (R3-5.2), each landed by the branch law.** Every one is program-held and every one has a real `F-W5.md` stating clause; the clause homes were verified at F-W5's live register this round and are the reason nine cite and one books.

| # | identity | clause | branch | landing |
|---|---|---|---|---|
| E-1 | `fr-ContourPreview` row 13 = **`L:L-5`** | **E20** *The exposed-ref seam — the one non-wire contract row* | **1** | **EXCLUDED-with-reason → §5.** The terminal disposition is a **component contract**, not a server act: make the `defineExpose`d ref readonly and pass the pair (`points` **and** `magnetRadius`) explicitly. **Consuming `F-W3.md` §X.1-v4 (R3-1a, landed first this round)**: the row's two subject files are `ContourEditorCanvas.vue` — one of the partition's four **F.W4 default-arm negatives**, named per id at §X.1-v4 item 2 — and `VisualizationView.vue` ∈ unit **`.e`**, whose residue §X.1-v4 item 5 assigns to **F.W3**. F.W6 therefore **emits and claims nothing**, and asserts no twin home of its own: the partition already answers the question about a path. E20 is added to §4's CITES list |
| E-2 | `fr-EquationModeToggle` **FR-EMT-1 = L-B2/C-1/D-9** ‡ | **F1** *Partial projection* ‡ — ⟨cmd⟩ `grep -o 'fr-EquationView B-1 ⊕ FR-EMT-1' waves/F-W5.md` → *"fr-EquationView B-1 ⊕ FR-EMT-1"* | **3-CITE** | CITED → F.W5 **F1**, the clause §2.11 row 24 already cites for `fr-EquationView B-1`: **one clause, two instantiating records**. F1's cure is *"a CONTRACT DECISION, not a patch"* (`latex_sigma` joins `SimplifyResponse` on both models, **or** explicit invalidation) — a decision the clause discharges, so F.W6 does not book it. ▲ **M-CK's sequencing lock reaches this row through F1 exactly as it reaches row 24: fix the key FIRST.** The F.W4 rider (per-option availability on the mode selector; the FR-EMT-7 fold supplies `disabled` free) is **F.W4's and is not claimed here** |
| E-3 | `fr-EquationModeToggle` **FR-EMT-20** | **E10** *Produced-and-unconsumed: ONE disposition* | **3-YES** | **CITED → held at F-W5 §2 clause E10** (re-cut at repair round 5, REST-15; round 3's R3-7.1 booking predates the canonical and R4-10 postdates and governs). The escape's terminal disposition is an act this seat performs — the E10 family's server strike, which §2.7 executes — but **the act is the mechanism and the identity is F.W5's** |
| E-4 | `fr-EquationResult` **FR-EQR-4** | **F8** *THE SEAM CHOICE — portable LaTeX* | **3-CITE** | CITED → F.W5 **F8**. The record routes it *"**→ F.W4** … **or F.W5** … the forming spec chooses the seam"* — **the seam choice is the clause's, and F.W5 is the forming spec.** F.W6 may not pre-empt it; **conditional promotion declared**: if F8 chooses the wire-field seam, the field lands as an F.W6 execution under E10's one-disposition law, and if it chooses `plainLatex()` it is F.W4's entirely. Recording the branch now is what stops a burn seat from picking the seam by default |
| E-5 | `fr-EquationResult` **FR-EQR-32** | **F8** (cited **B1**) | **3-CITE** | CITED → F.W5 **F8/B1** — ⟨cmd⟩ `grep -o 'FR-EQR-32 (C-§0.4)' waves/F-W5.md` → *"FR-EQR-32 (C-§0.4)"*, inside F8's carried-rows cell. INFO-weight: the **operation record** carries no sink class for its string-typed response fields. Its artefact is `contract/operation-register.md`, **owned by F.W5 §1a and CITED-WHEN-CREATED** here (§1 r6) — F.W6 authors no operation record |
| E-6 | `fr-NotationPills` **FR-NP-30b** ⊙ | **D12** *Closed domains at the boundary* (cited **A5**) — ⟨cmd⟩ `grep -o 'R6-8 sub-arm carried beside the banked head, never instead of it' waves/F-W5.md` → *"R6-8 sub-arm carried beside the banked head, never instead of it"* ⟨**RE-CUT AND RE-PASTED BY THE PASS-4 TRULY-LAST PURGE SEAT, 2026-08-29 — the round-4 receipt was FALSE at the live sibling bytes.** It published ⟨cmd⟩ `grep -o 'FR-NP-30b (cited A5)' waves/F-W5.md` → *"FR-NP-30b (cited A5)"*; re-run on the pinned toolchain that command returns **∅, exit 1**. F-W5's own round-4 seat restored **`FR-NP-30` as the banked head with `FR-NP-30b` riding beside it** (anti-rename R-5/U-12) and re-wrote both its A5 and D12 cells, so the quoted spelling no longer exists there. The re-cut above returns the live A5 cell's own words. **The CITATION is unchanged — F.W5 D12/A5, 3-CITE ⊙ — only the receipt moves**; and the identity this row cites is now read as the record's `FR-NP-30` head with its sub-arm beside it, exactly as the sibling and the canonical spell it⟩ | **3-CITE ⊙** | CITED → F.W5 **D12**. The `notation` enum is enumerated 4× with a **silent Python `.get(…, render_trig)` fallback** — the same *silent rewrite* D12 names, and **⊙ owner ruling `R5` governs both branches** (*admit it in the contract or stop minting it*). **Conditional promotion declared** (branch 4 as amended): under the *stop minting* branch the fallback's removal becomes a server act this wave would burn; under *admit it* the cure is the contract's. **F.W6 may not choose, and records the honest default rather than presuming one** |
| E-7 | `fr-ConvergenceLegend` **D-L4 + C-7** | **B1** *The join relation — forward* — ⟨cmd⟩ `grep -o 'D-L4+C-7 (ConvergenceLegend)' waves/F-W5.md` → *"D-L4+C-7 (ConvergenceLegend)"* | **3-CITE** | CITED → F.W5 **B1**. Only the **R6-8-echo arm** reaches this span (*child identity derived from parent iteration order; the join re-derived at read time*); the `CurveKey` union + `harmonicKey(i)` in `lib/harmonics.ts` is **F.W4's**. B1's own law binds the landing: *carry the identity ONCE and cite the instances* — which is why this is a citation beside rows 4, 27, 49 and 56 and not a fifth booking |
| E-8 | `fr-ConvergencePlot` **L-M7 + C-8** | **F7** *The missing original-expression field* — ⟨cmd⟩ `grep -o 'L-M7 + C-8 (ConvergencePlot)' waves/F-W5.md` → *"L-M7 + C-8 (ConvergencePlot)"* | **3-CITE** | CITED → F.W5 **F7**. ▲ **The killed cure travels with it: K-13 binds** — `latex` is the budget-**TRUNCATED** series render, so routing it to the ORIGINAL curve re-commits the inversion the row diagnoses; **the prescribed cure is DEAD and no burn seat may revive it.** The replacement is a NEW backend field (`sp.latex(expr)`) on `ComputeEquationResponse` — an envelope decision F7 discharges, with the label-honesty arm at F.W4 |
| E-9 | `fr-ConvergencePlot` **C-7** | **B2** *The mirror direction* ‡ — ⟨cmd⟩ `grep -o 'fr-ConvergencePlot C-7' waves/F-W5.md` → *"fr-ConvergencePlot C-7"* | **3-CITE** | CITED → F.W5 **B2**, the intake **R6-8 mirror rider** — a client shadowing a server field with a same-named, different-valued re-derivation (`\|c_n\|` on the wire vs `√(a²+b²)=2\|c_k\|` at the leaf, both labelled `A`). B2 is a **contract rule in the reverse direction**; the rename-or-consume arm is F.W4's. **B2 is added to §4's CITES list — it was in neither list, which is a second instance of the gap E20 exposes** |
| E-10 | `fr-ConvergencePlot` **L-m13** | **F6** *A response field states WHICH population it measures* — ⟨cmd⟩ `grep -o 'fr-FunctionInput L-M3 ⊕ fr-ConvergencePlot L-m13' waves/F-W5.md` → *"fr-FunctionInput L-M3 ⊕ fr-ConvergencePlot L-m13"* | **3-CITE** | CITED → F.W5 **F6**, beside §2.11 row 40's `fr-FunctionInput L-M3` — **one clause, two records, and the U-12 qualification note in F6's own cell keeps `fr-FourierShapeExtractor L-M3` (G5c) out of the collision.** *"F.W5 contract note"* is the whole of the span leg; the naming arm is F.W4's |

**BLOCK B — the 25 booked, with the branch each took.** These are the identities *cut to* this wave; each carries its witness, cure, locks and sequencing at the section named.

| § | identities (record-qualified) | branch |
|---|---|---|
| §2.1 | **F-α** (E2/G3) · **F-β** (E7) · **fr-SpeedSelect SS-C-1** write leg (E7 rider) | 3-YES ×3 |
| §2.2 | **F-γ** (C5/G6) · **fr-AdminFlaggedPanel FR-AFP-4** ‡ (C1/C2/G8) | 3-YES ×2 |
| §2.3 | **fr-GalleryView FR-GV-12** (E6/G12) · **fr-GalleryDraftsSection B-2** ‡ (E5) — **CITED to F-W3** (canonical **F.W3** <sub>file-criterion → §5.d</sub> <sub>legs: F.W5-W8</sub>; repair round 5, REST-17) | 3-YES ×2 |
| §2.4 | **FR-AFP-1 = D3** ‡ ⊙ (D3/G11) · **FR-AFP-66** (D6/R6) ⊙ · **FR-AFP-7** (D5/D4) ⊙ · **FR-AFP-33** (D7 — *the worked re-run above*) ⊙ · **FR-AFP-36** (A4/D17) · **fr-GalleryView FR-GV-9** (D8) ⊙ | 3-YES ×6 |
| §2.5 | **fr-AdminAuditLog AA-10** (E18/D16) — **CITED to F-W4** (canonical **F.W4** <sub>legs: F.W5-W8</sub>; repair round 5, REST-16) | 3-YES |
| §2.6 | **fr-ContourSettings B-4 = C-1** ‡ (E13) · **fr-ContourSettings M-13** ‡ (E14+E17) · **fr-FourierShapeExtractor L-B1** (§G G1c–G10c; **TRIPWIRE**) | 3-YES ×3 |
| §2.7 | **PP-DEADSEAM** (B4/E10) — booked · **fr-EquationModeToggle FR-EMT-20** (E10) — **CITED to F-W5, not booked** (repair round 5, REST-15) | 3-YES ×2 |
| §2.8 | **R-5 / G5** born-visibility ⊕ **E4** ⊙ R8 | 3-YES |
| §2.9 | **fr-GalleryFeaturedCarousel FR-GFC-3** ⊙ (D2/G10) | 3-YES |
| §2.10 | **V-α** · **V-β** · **V-γ** · **TA-4** ⊙ | **1** ×4 — routed OUT to the value.js API row; **zero value-tree bytes**, receipt at bounds #3 |

**BLOCK C — the tail, id-keyed: every remaining disposition this file makes, with its branch and the section that holds it.** This is the enumeration the PASS-3 seat found missing. Read with the counting rule above: **branch-2 rows are folds transcribed at their carriers and are not separate roster members.**

| identity (record-qualified) | branch | carrier / destination |
|---|---|---|
| `fr-AdminAuditLog AA-5` | 2 | server arm carried at **§2.5 AA-10** (itself cited to F-W4, REST-16); display taxonomy → F.W4 |
| `fr-AdminAuditLog AA-6` | 1 | F.W4 — placeholder honesty (AA-23's regex cure stays KILLED) |
| `fr-AdminAuditLog AA-24` | 1 | F.W4 — display taxonomy |
| `fr-AdminAuditLog AA-44` | 1 | **canonical home F.W9** <sub>legs: F.W9/W10</sub> — the one-sided test seam; a **distinct banked identity** from `FR-AFP-49` and from `FR-AUL-16` (REST-27) |
| `fr-AdminFlaggedPanel FR-AFP-8` | 2 | identity arm carried at **§2.4 FR-AFP-7**; the one-token `item.slug` label is F.W3/W4's |
| `fr-AdminFlaggedPanel FR-AFP-9` | 2 | carried at **§2.4 FR-AFP-66** (dialog-truth arm); truthful copy → F.W3/W4 |
| `fr-AdminFlaggedPanel FR-AFP-10` | 2 | carried at **§2.4 FR-GV-9** (the flagged listing's tier predicate) |
| `fr-AdminFlaggedPanel FR-AFP-18` | 2 | cited at **§2.4 FR-AFP-1** (the queue has no size anywhere) |
| `fr-AdminFlaggedPanel FR-AFP-32` | 2 | the **D17** serializer clause at **§2.4 FR-AFP-36** — **canonical home F.W3** <sub>legs: F.W5-W8, SS-13</sub> ⟨*added at repair round 5, REST-26: the landing is where the cure runs, the home is where the identity lives, and R4-10 requires the second to be named*⟩ |
| `fr-AdminFlaggedPanel FR-AFP-40` | 2 | fold inside FR-AFP-32 |
| `fr-AdminFlaggedPanel FR-AFP-69` | 2 | fold inside FR-AFP-32 (= β-miss-5 extension) |
| `fr-AdminFlaggedPanel FR-AFP-35` | 1 | F.W3/W4 — the diction the FR-AFP-9 copy cure rides |
| `fr-AdminFlaggedPanel FR-AFP-49` | 1 | **one MECHANISM, three banked identities, two homes** ⟨**re-cut at repair round 5, REST-27.** The cell read `FR-AFP-49 = AA-44 = FR-AUL-16` as ONE identity; the canonical homes them at **three different places**: `` | `FR-AFP-49` | `D-21` · `L-26` | `F.W9/W10` · `SS-3` · `SS-4` | **F.W9** <sub>legs: SS-3, SS-4, F.W9/W10</sub> | `` · `AA-44` → **F.W9** <sub>legs: F.W9/W10</sub> · `` | `FR-AUL-16` | — | `NO-WAVE-OWNER` · `SS-3` · `SS-4` | **NWO→SS-3** | ``⟩ — `FR-AFP-49` ⊕ `AA-44` **held at F-W9**; `FR-AUL-16` at the **NWO→SS-3 packet**, disposed at F-W10's drain. **Never collapsed into one token.** Zero automated coverage is the shared mechanism |
| `fr-AdminFlaggedPanel FR-AFP-59` | 1 | F.W3/W4 — the tier-render arm (10px raw wire token) |
| `fr-AdminFlaggedPanel FR-AFP-70` | 2 | carried at **§2.4 FR-GV-9**; the reflect-and-disable interim is F.W3/W4's |
| `fr-AdminFlaggedPanel FR-AFP-71` ⊙ | 1 | ⊙ **R7** — codegen vs hand-typed twins; **F.W6 builds no codegen unruled** |
| `fr-AdminUserList FR-AUL-13` | 1 | cited at **§2.4 FR-GV-9**'s FR-GFC-20 rider; **banked there, not re-booked here** |
| `fr-AdminUserList FR-AUL-17` | 1 | **banked → F.W3** (the shared client formatter); **do NOT re-book** |
| `fr-BasisCanvas M-β4` | 2 | carried at **§2.7 PP-DEADSEAM** (`EpicycleData.trace`) |
| `fr-BasisSelector M-10` | 1 | F.W3/W4 — the banked normaliser; fold target of the row-50 **WAVE-LOCK** |
| `fr-BasisSelector M-14` | 2 | carried at **§2.6 fr-ContourSettings M-13** (the debounced `runCompute()` path) |
| `fr-CanvasControlsDock D-4/L-1/C-3` | 2 | folded at **§2.3 fr-GalleryDraftsSection B-2** (itself cited to F-W3, REST-17) |
| `fr-CanvasControlsDock C-28` | 2 | fold-by-reference at F.W5 **B1**, cited at §2.11 row 21 — **homed at B1, not here** |
| `fr-ContourEditorCanvas C-2` | 2 | carried at **§2.6 fr-ContourSettings M-13** (E17, image bounds on write) |
| `fr-ContourPreview` row 28 | 2 | carried at **§2.6** (boundary cardinality on the write path) |
| `fr-ContourSettings i-7` (= C-25/R6-8) | 2 | INFO-weight fold at **§2.6 B-4** — cache identity ⊇ consumed fields |
| `fr-ContourSettings m-18` (= L-i1/C-18) | 2 | rider at **§2.6 B-4** — **canonical home F.W3** <sub>file-criterion → §5.d</sub> <sub>legs: F.W5-W8</sub> ⟨*REST-26*⟩; the `* 0.6` coupling **must not survive the cure** |
| `fr-EquationView L·m-6` (= C·D-14) | 2 | carried at **§2.7 PP-DEADSEAM** / FR-EMT-20 (E10, cited to F-W5) |
| `fr-FourierShapeExtractor L-B2 / C-2` | 2 | carried at **§2.6 L-B1** — **TRIPWIRE revives it at BLOCKER on any regeneration** |
| `fr-FourierShapeExtractor L-B3` | 2 | carried at **§2.6 L-B1** |
| `fr-FourierShapeExtractor L-M3 / C-6` | 2 | carried at **§2.6 L-B1** (G5c) — distinct from `fr-FunctionInput L-M3`, row 40 |
| `fr-FourierShapeExtractor L-M5/C-5` ⊕ `C-15` | 2 | carried at **§2.6 L-B1** |
| `fr-FourierShapeExtractor C-7` | 2 | carried at **§2.6 L-B1** (cited G7c) — distinct from `fr-ConvergencePlot C-7`, escape E-9 |
| `fr-FourierShapeExtractor L-m3 / C-10 ⊕ L-m5` | 2 | carried at **§2.6 L-B1**; homed at F.W5 **G10c** |
| `fr-GalleryCard C-1-as-corrected` | 2 | carried at **§2.9 FR-GFC-3** |
| `fr-GalleryCard L·M-4 / D-13 / C-8(a) + L·D-2 / C-12` | 2 | **NAMED FOLD** into **D17** at §2.4; only the `serializer → F.W5–W8` leg reaches this wave |
| `fr-GalleryCardModal GCM-1` | 1 | F.W4 — emit `entry.slug`, route `/v/`, point the loader at `loadVisualization`; the *"no `fork_of` recorded"* server arm is booked at §2.8 |
| `fr-GalleryCardModal GCM-2` | 2 | folds BY REFERENCE at **§2.9 FR-GFC-3** |
| `fr-GalleryCardModal GCM-10` | 2 | **named fold** at §2.4 (re-landed this round — see the eight-landing table above) — **canonical home F.W3** <sub>file-criterion → §5.e</sub> <sub>legs: F.W5-W8</sub> ⟨*REST-26; the cell read "→ F.W5 A3 + D17", which the canonical contradicts*⟩ |
| `fr-GalleryCardModal GCM-52` | 2 | carried at **§2.2 FR-AFP-4** (the world-readable asset leaf) |
| `fr-GalleryCardModal GCM-55` | 2 | carried at **§2.4 FR-AFP-36**; *"with GCM-10's model fix"* is why row 45 folds rather than cites |
| `fr-GalleryDraftsSection F-4` | 2 | carried at **§2.2 FR-AFP-4** — WHO can fetch (never merged with m-15) |
| `fr-GalleryDraftsSection m-15` | 2 | carried at **§2.2 FR-AFP-4** — WHAT they see (never merged with F-4) |
| `fr-GalleryDraftsSection m-13` | 1 | F.W3/W4 rider at the banked home (the success toast prints the USER slug) |
| `fr-GalleryFeaturedCarousel FR-GFC-4` | 1 | F.W8 — the missing `tier` param at every layer |
| `fr-GalleryFeaturedCarousel FR-GFC-20 = C-9` | 2 | **BOTH arms**: client stale-slug pruning → F.W4; **canonical home F.W4** <sub>legs: F.W5-W8</sub> ⟨*REST-26*⟩; the **`BatchResponse.errors` server arm rides §2.4's FR-GV-9 and is burned WITH FW6-G10** |
| `fr-GalleryInfiniteGrid C-3` | 2 | folds at **§2.9 FR-GFC-3**; the re-grade to BLOCKER was refused by the identity guard (R-7) |
| `fr-GalleryMarquee GM-M4` | 2 | carried at **§2.7 PP-DEADSEAM**; the `softDelete`/`restore` wiring is VV-R2-B's F.W4 unit |
| `fr-GalleryView FR-GV-1` | 2 | FOLD at **§2.3 fr-GalleryDraftsSection B-2** — graded BLOCKER at the banked severity, explicitly folded. **Canonical home F.W3** (file-criterion §5.d, legs F.W5-W8, SS-13) — **CITED to F-W3, never booked here** (repair round 5, REST-17) |
| `fr-GalleryView FR-GV-8` | 1 | F.W8 — 7 of 13 `/api/visualizations` ops with zero client function |
| `fr-GalleryView FR-GV-13` (client control) | 1 | F.W3/W4 — the `?owner=me` control. **Same identity as row 56a's server arm; counted once** |
| `fr-GalleryView FR-GV-24` | 2 | the **repair-test lock** at **§2.3 FR-GV-12** — no re-open increment |
| `fr-GalleryView FR-GV-27` | 1 | F.W8 — the `/publish`/`/unpublish` wrappers |
| `fr-GalleryView FR-GV-34` | 2 | carried at **§2.4 FR-AFP-66**; batch-dialog copy → F.W3/W4 |
| `fr-SpeedSelect SS-C-1` (READ leg) | 2 | **same identity as the booked write leg**; the read leg is banked at `fr-GalleryCardModal GCM-1` → F.W4 + rider. **Re-merging the halves re-books GCM-1** |
| `fr-UserSlugBar FR-USB-15` | 1 | F.W8 — session truth |
| `fr-UserSlugBar FR-USB-23` ⊙ | 1 | F.W8 — **⊙ `R9` travels with the row across the routing**; honest default *delete (zero external call sites)* |
| `fr-UserSlugBar FR-USB-24` | 1 | F.W8 — the 429 retry posture; **C4's one-controller lock binds any retry change** |
| `fr-VisualizationView BLK-2` | 2 | folded at **§2.3**; **`BLK-1` is a DISTINCT banked id and is cited at §2.11 row 54** |
| `fr-VisualizationView VV-R2-A` | 2 | cited at **§2.3 FR-GV-12** (born at `views: 1`); the publish→`setVisibility` client arm is F.W4's |
| `fr-VisualizationView VV-R2-B` | 1 | F.W4 — **one wiring unit**, never scattered |
| `fr-ImageUpload` row 26 | 2 | cited at **§2.2 FR-AFP-4** (INFO) |
| **D1's per-operation dispositions** | 1 | F.W8 — conformance |

**HOW THE GATE RUNS OVER THIS.** Falsifier (a) is re-stated at FW6-G17 against **this transcript** rather than against an integer. **Reverse direction, executable as written**: for each identity printed above and at §2.11, `grep` **its banked id token** in this file and confirm the landing section named holds it — **the token, not the record-qualified spelling**, because R-5 qualifies **at least TEN** colliding tokens (`M-13` · `L-B1` · `L-M3` · `C-17` · `C-18` · `B-1` · `B-2` · `C-2` ⊕ `M-10` · `M-9`) and leaves every other id bare by design ⟨**COUNT WORD CORRECTED AT REPAIR ROUND 6 (PASS-6 D-5, MEDIUM).** This site published *"exactly **eight**"* against the roster §0's record-qualification law raised to **at least TEN** at repair round 5 (REST-20, P5-4/P5-11) — the count-word-against-its-own-list class, surviving one section away from the edit that cured it, in the paragraph whose stated job is to tell a successor how to run the gate. **The roster is §0's and is READ from there, never re-counted here**; a figure at this site is a citation of that clause and moves only when it does.⟩ ⊘ **And on the two tokens added at round 5 the bare-token probe is UNRUNNABLE, which §0 states in terms**: `M-10` and `M-9` each return a set spanning three identities across two or three canonical waves, so at those two the probe is the **(record, id) PAIR** and never the bare token. BLOCK C prints the record beside each id as an **address**, so a seat can resolve a homonym without the probe having to match it; for those ten, and for the four homonym pairs BLOCK C names explicitly (`fr-FourierShapeExtractor C-7` vs `fr-ConvergencePlot C-7` · `fr-ContourSettings M-13` vs `fr-CoefficientsSpectrum M-13` · `fr-FourierShapeExtractor L-B1` vs `fr-FunctionInput L-B1` · `fr-FourierShapeExtractor L-M3` vs `fr-FunctionInput L-M3`), the probe is the **(record, id) PAIR** — R3-5's fourth detector axis, applied at this end too. Forward direction, **as the gate face at FW6-G17 now states it**: read the canonical's own rosters — every id `../conformance/CENSUS-CANONICAL.md` §2 homes at `F.W5` or `F.W5-W8` — and confirm each is printed here, at §2.11 / §2.11c, at §2.11d (cited-to-holder), at §2.1–§2.10 (booked), or at §5 (excluded-with-reason). ⌧ **The four-spelling corpus detector of §2.0 is retained as dated round-3 provenance and STRUCK AS AN OPERAND at repair round 5 (REST-19; R4-10), and it is NOT a co-equal falsifier here** ⟨**RE-VOICED AT REPAIR ROUND 6 (PASS-6 D-5, MEDIUM)**: this paragraph went on presenting the struck detector as a live forward direction one section away from `FW6-G17`'s own strike face, which is the R4-10 derived-operand class inside the successor-facing gate instructions. A walk under that detector may still be run for interest — it produced the escapes at §2.11c and the identities at §2.11d — but **it settles nothing and no closure reading rests on it**⟩. **Two directions, both executable, neither resting on a count, and neither deriving its own roster.** A row printed with no landing section, or a corpus id printed nowhere, is the silent drop the gate exists to fail on.

#### §2.11d THE FIFTEEN HOLDER-CITED — the round-3 fourth arm's own blind spot, closed by the canonical (R4-9.5 ⊕ R4-10; cures P4-D1)

**What escaped, and why the cure was the defect.** Round 3 added a fourth detector arm — bare `F.W5` used as the NAME of the union — and then scoped it with `comm -13 <(band-form records) <(bare-F.W5 records)`. **That construction returns only records carrying NO band form at all.** A record whose single band-form line is the routing-law boilerplate therefore sat outside the fourth arm *by construction* while contributing nothing to the first three — and the five records §2.0 names as boilerplate-only are exactly that population. Unscoped, the bare spelling reaches **eighteen** records, not five. **This is the round-3 cure reproducing the round-3 defect one scope-level out: a new arm scoped by a set-difference against the old arm inherits the old arm's blind spot.** The lesson §2.0 books — *a detector enumerated by tokens the author happens to know reproduces the K-6 method failure at every round* — gains its second half here, and then **round 4 retires the whole practice**: this file no longer derives a census (§0.1).

**The disposition, and it is narrow.** Every one of the fifteen is **held at F.W5 by the canonical**, so under R4-10's roster law F.W6's duty is to **CITE THE HOLDER BY NAME** — never to book, never to count, and never to route. ⟨cmd⟩ `grep -o '\*\*fr-AdminUserList\*\* (11): .FR-AUL-3.*FR-AUL-59.' "$C"` → *"**fr-AdminUserList** (11): `FR-AUL-3` · `FR-AUL-11` · `FR-AUL-12` · `FR-AUL-13` · `FR-AUL-14` · `FR-AUL-20` · `FR-AUL-21` · `FR-AUL-25` · `FR-AUL-31` · `FR-AUL-46` · `FR-AUL-59`"* · ⟨cmd⟩ `grep -o '\*\*fr-AdminAuditLog\*\* (4): .AA-6. · .AA-23. · .AA-31. · .AA-32.' "$C"` → *"**fr-AdminAuditLog** (4): `AA-6` · `AA-23` · `AA-31` · `AA-32`"* · ⟨cmd⟩ `grep -o '\*\*fr-GalleryAdminBanner\*\* (2): .GAB-15. · .GAB-16.' "$C"` → *"**fr-GalleryAdminBanner** (2): `GAB-15` · `GAB-16`"* — all three inside the canonical's **`### F.W5 — **28 rows**`** roster.

▲ **These rows are GATE-DEFECT ROWS, not bookings.** They record that FW6-G17's operand missed them for three rounds and name where they actually live. **F.W6 performs no act on any of them, claims no credit, and adds none of them to any denominator** — which is also why the observation *"every one of the fifteen is a server/persistence act"* changes nothing: a server act **held at another wave** is that wave's to burn, and R-5's one-home law is not suspended because this seat could have done the work.

| # | identity (record-qualified) | corpus coordinate | routing at the bytes (bare-`F.W5` spelling) | holder |
|---|---|---|---|---|
| H-1 | `fr-AdminUserList` **FR-AUL-3** | `fr-AdminUserList.md:41` | stable per-operation abort keys (the R6-8 identity class) | **held at F-W5** — canonical §2 `F.W5` roster |
| H-2 | `fr-AdminUserList` **FR-AUL-11** | `:49` | wrapper types re-derived from the router (R6-8 class) | held at F-W5 |
| H-3 | `fr-AdminUserList` **FR-AUL-12** | `:50` | same repair family as FR-AUL-11 | held at F-W5 |
| H-4 | `fr-AdminUserList` **FR-AUL-14** | `:52` | `$ifNull` the projection or default the model fields | held at F-W5 |
| H-5 | `fr-AdminUserList` **FR-AUL-20** | `:58` | the join filters `deleted_at: None`, or tombstone-inclusive counting is stated | held at F-W5 |
| H-6 | `fr-AdminUserList` **FR-AUL-21** | `:59` | one closed status domain across writers, model and wire | held at F-W5 |
| H-7 | `fr-AdminUserList` **FR-AUL-25** | `:63` | a count/dry-run endpoint or a two-step confirm | held at F-W5 |
| H-8 | `fr-AdminUserList` **FR-AUL-31** | `:74` | one `Literal`/union across wire and client | held at F-W5 |
| H-9 | `fr-AdminUserList` **FR-AUL-46** | `:89` | surface the cap in the shared contract | held at F-W5 |
| H-10 | `fr-AdminUserList` **FR-AUL-59** | `:107` | the `$lookup`-before-`$limit` pipeline | **held at F-W5** — ▲ and it is the id F.W5's **B1** compose-lock already names (*"The abort-key fix and FR-AUL-59's pipeline fix **compose; neither alone closes the window**."*, quoted at §2.11 row 49). Rounds 1–3 carried the token **only inside that quotation, with no landing cell of its own** — a citation of a lock is not a disposition of the id it locks |
| H-11 | `fr-AdminAuditLog` **AA-23** ‡ | `fr-AdminAuditLog.md:60` | *"the 45/30/13 join must carry this constraint"* (MAJOR, repair-shaping) | **held at F-W5 (R4-6.4, explicit)** — see §2.5's marked killed-cure citation |
| H-12 | `fr-AdminAuditLog` **AA-31** | `:71` | the `after`/`before` window against the audit index | held at F-W5 |
| H-13 | `fr-AdminAuditLog` **AA-32** | `:72` | same join row as AA-23 | held at F-W5 |
| H-14 | `fr-GalleryAdminBanner` **GAB-15** | `fr-GalleryAdminBanner.md:57` | with the R3-7c client-gap rows; label honesty → F.W4 | held at F-W5 |
| H-15 | `fr-GalleryAdminBanner` **GAB-16** | `:58` | `total_entries` is DERIVED, not counted — an off-enum `tier` vanishes from the total | held at F-W5 |

**Two adjacent ids, disclosed rather than absorbed.** `fr-AdminAuditLog` **AA-6** and `fr-AdminUserList` **FR-AUL-13** are also canonically **F.W5**-held, and both are already transcribed in this file — AA-6 at §2.11c BLOCK C (with a display-arm note routing to F.W4) and FR-AUL-13 at BLOCK C as a citation of the FR-GFC-20 rider. **Their holder is F.W5 in both cases and neither is booked here**; the BLOCK C notes describe *which arm this wave emits*, never a home. `fr-AdminUserList` **FR-AUL-17** is canonically **F.W3**-held, exactly as BLOCK C already states — ⟨cmd⟩ `grep -o '\*\*fr-AdminUserList\*\* (7): .FR-AUL-6. · .FR-AUL-17.' "$C"` → *"**fr-AdminUserList** (7): `FR-AUL-6` · `FR-AUL-17`"*, inside the canonical's `F.W3` roster — so *"banked → F.W3; do NOT re-book"* stands unchanged.

---

## §3 Gates — 19, all born RED

`F=/Users/mkbabb/Programming/fourier-analysis` (read-only) · `V=/Users/mkbabb/Programming/value.js`.

**Two reading disciplines, binding.** (1) **MEASURE-AT-OPEN** — every fourier figure and anchor below is re-confirmed by F.W0 before citation (D-19); this file re-asserts none of them. (2) **Split verdict** — a gate closes for F.W6 when the commissioned act is authored with its evidence row in the burn register; it goes GREEN only when the named landing occurs. F.W6 never claims a GREEN it did not execute.

| gate | born-RED witness | GREEN (falsifier) | owner |
|---|---|---|---|
| **FW6-G1** chain depth burned (F-α) | `grep -rn "_write_root_version" $F/api` → def visualizations.py:110 + create :220 + remix :592, the ONLY writer; `parent_hash=None, root_hash=set_hash_value, depth=0` at :138-141. `grep -rn "visualization_versions" $F/api --include='*.py' \| grep -v tests` → 1 insert (:146), 2 finds, 1 find_one + the migration script + 2 index calls | The **ruled** disposition is executed AND a fixture asserts either a depth-1 version exists with a non-null `parent_hash`, or the depth/parent/root quadruple is gone from the model | **F.W6** (F.W5 G3's GREEN-owner cell names this wave) |
| **FW6-G2** `set_hash` recompute on atom PATCH (F-β) | visualizations.py:381-383 is a bare `$set`; `_head_set_hash` (:477-480) returns the STORED value when non-empty; `palette_slug` is atom 5 (atomdiff.py:34) and a PATCH field (visualization.py:207). **No probe exists today — F.W5's G1–G22 (+ §G's G1c–G10c) carries no gate for clause E7** (R-1e re-key; `F-W5.md` §2 §E states clause **E7**, and its §3 head reads *"Gates — 22, all born-RED"* — cited by ID and HEADING per the note at §2.1) | A fixture PATCHes atom 5 and asserts (a) `set_hash == set_hash(enumerate_atoms(doc))` after the write and (b) a new version row exists | **F.W6** (gate supplied here because F.W5 states clause **E7** without gating it — **round-2 correction**: this owner cell read "E8", the *AnimationSettings* clause, while the witness cell beside it correctly read E7; the two now agree, and no AnimationSettings row is booked in this wave — E8's rows are cited at §2.11 rows 1–2) |
| **FW6-G3** `palette_slug` is clearable | `updates = {… if v is not None}` at visualizations.py:381 — PATCH can never clear `palette_slug` to null, while the remix arm twenty lines away distinguishes omit/null/rebind via `model_fields_set` (:522-528) | Three arms tested: omit inherits, null clears, slug rebinds — **using the remix idiom, not a new one** | F.W6 |
| **FW6-G4** PATCH field set = atom set (SS-C-1 write leg) | `VisualizationUpdate` (visualization.py:198-210) = five fields, `extra="forbid"`; `VisualizationPatch` (types.ts:256-262) the same five; `atomdiff.py:38` lists `animation_settings` in the atom set — the two sets differ, so `speed` is settable only at create/fork | Set equality holds, or every divergence is stated per-atom with its reason | F.W6 — **same commit as FW6-G2/G3 (one PATCH-model cut)** |
| **FW6-G5** breadcrumb redaction parity (F-γ) | `_readable_or_none` appears at visualizations.py:744 (entry row) and **not** in the ancestor walk at :784 — a bare `find_one` emitting slug/set_hash/author_slug/created_at for private and draft ancestors. Counter-example: forks.ts:167-179 | A fixture with a private ancestor asserts **no lineage field crosses the wire** for that hop (the `{kind:"unavailable", ordinal}` shape) — **or** G6's second branch (stated asymmetry with its reason) is on the record. The cycle-guard and the ≤50 cap survive | F.W6 (F.W5 G6 GREEN owner: fourier API row) |
| **FW6-G6** unsafe GET / counter provenance | visualizations.py:256 `find_one` → :268-270 `$inc {"views": 1}` → :272 `_public_doc(doc)` serialises the pre-increment doc; `grep -rn "viewed_ips" $F/api` → **0** while `liked_ips` exists | The read is safe behind an explicit verb, or the mutating-GET policy is stated against RFC 9110 §9.2.1. ▲ **The repair test may NOT assert a re-open increment (FR-GV-24 lock)** | F.W6 server arm + F.W4 client arm (VV-R2-A) — **separately credited** |
| **FW6-G7** create-path idempotency / dedupe | api.ts:371-380 sends no `Idempotency-Key`; idempotency.py:68-70 is an explicit no-header passthrough; database.py:98 `content_hash` is a PLAIN index while :140 flags carry `unique=True`; the same router's remix arm calls `replay_or_record` at :612 | The create path replays or the index is unique. ▲ **K12: closed STATICALLY — SS-13 spends no probe** | F.W6 (server arm only; the reconciliation contract is F.W3/W4's) |
| **FW6-G8** flag cascade at hard delete | `pinned_cron` — named by the softdelete docstring as cascade owner — contains **zero** flags code; the existing orphan-flag test exercises only the reporter-keyed stale-user cascade | A `content_hash`-keyed cascade runs at grace hard-delete, the docstring is corrected, and a test covers the entity-keyed path | F.W6 ⊙ — **blocked until G11 rules producer-or-retire** |
| **FW6-G9** the actor field | janitor.py:56 `_JANITOR_ACTOR = "system:janitor"` written into `ip_hash` at :95 and rendered `system:jan` by the unconditional `.slice(0,10)` at :146; `admin_audit` carries no actor field; the second writer at janitor.py:59-99 bypasses `log_audit` entirely | An `actor` field exists and **both** writers populate it. ▲ **Enumerate with the S-8 method law — `grep "log_audit("` is structurally blind to an inlined writer (K-6 killed that method).** ▲ **AA-23: no regex-action cure** | F.W6 (display legend is F.W4's) |
| **FW6-G10** tier transitions are stated transitions | admin.py:435-440 `update_many` with `{"$set": {"tier": "normal"}}` unconditional; `set_tier` writes `{tier, updated_at}` only (:160-190) and the flagged listing carries no tier predicate | The write is conditioned on the current tier and the listing carries the predicate FR-AFP-10 assumes. ▲ **β's 428-escalation cure stays killed** | F.W6 ⊙ — conditioned on G11 |
| **FW6-G11** image remediation executed | `images.py` = 7 ops, **ZERO DELETE**; blob GET :132 carries no `Depends` and ships `Cache-Control: public, max-age=86400`; thumbnails the same at :162-164; every anonymous blob GET ends in `touch_document`, bumping the janitor recency predicate | A delete/quarantine op exists, blob **and** thumbnail GETs are visibility-gated, the janitor predicate no longer rewards fetching, and the thumbnail URL is versioned. ▲ **The deployment auth-proxy question is UNPROVEN (SS-13) — the gate may not close by assuming a proxy** | F.W6 (F.W5 G8 GREEN owner: fourier API row) |
| **FW6-G12** one checked shape / one serializer | **Witness RE-MEASURED this round (round-2 cure of a gate argued RED over a figure that does not reproduce; the first pass wrote `→ 0` and the bytes say otherwise — a born-RED gate whose witness is false is the defect, not the RED)**: ⟨cmd⟩ `grep -nE 'response_model' $F/api/routers/admin.py` → **exactly ONE hit** — `admin.py:110  "/stats", response_model=AdminStatsResponse, dependencies=[Depends(admin_required)]` — against ⟨cmd⟩ `grep -cE '^@[a-z_]*router\.(get\|post\|put\|patch\|delete)' $F/api/routers/admin.py` → **13** route decorators. (**R3-3.10 note**: this is a count over the **fourier product tree**, which is read-only to this wave and re-confirmed by F.W0 under **D-19 MEASURE-AT-OPEN** before citation — not a banked count of a live sibling spec or of this file, which the amendment forbids.) **The RED is intact and now stated at its true magnitude: 1 of 13 admin operations is response-model-checked, and it is not one of the two this row is about.** The flagged listing (`@admin_router.get("/flagged")`, `:509`) carries **no** `response_model` and returns a hand-built dict through `_json` (`:587`), whose serializer is `json.dumps(body, default=str)` (`:90-92`) — space-separated, non-ECMA — while the audit route returns the Pydantic `AuditListResponse` (`:652`, ISO with `T`): **ONE router, one concept, two dialects.** The K9-protected `doc.get("image_slug")` sits inside exactly that unvalidated body (`:578`). Three dead server models encode the retired shape; the emitted `content_hash` is omitted client-side | One generated/checked shape; dead models deleted; one serializer. ▲ **K9: the five `?? item.slug` fallbacks STAY (defensive, not dead).** ⊙ **No codegen without FR-AFP-71's ruling** | F.W6 server arm (FR-AUL-17's shared client formatter is banked → F.W3, **never re-booked**) |
| **FW6-G13** cache identity ⊇ consumed fields | `extraction_cache_key` at image_storage.py:250-266 is a closed 10-field `json.dumps` literal omitting `ml_threshold` / `ml_detail_threshold`; the handler short-circuits on the hit **before** `compute_contours` | **The named fixture: `contour_hash` INSTABILITY across an ML-threshold change** (also the SS-13 probe). Rider m-18's `* 0.6` coupling is not preserved by the cure | F.W6 (F.W5 G13 GREEN owner: fourier API row) |
| **FW6-G14** contour provenance / bounds on write | a `source="editor"` asset carries no `extraction_cache_key`, so exiting the editor re-runs extraction and the server cache-hits the ORIGINAL; `store_contour_asset(xs, ys, req.image_slug, source="editor")` passes no `image_bounds`, the param defaults `None` and is written verbatim (image_storage.py:290/:318); backfill is GET-only | Extraction cannot overwrite a `source="editor"` asset, and derive-on-POST or backfill-on-write is executed per the ruling. **The client orchestrator move (L-2) is F.W3/W4's and is not credited here** | F.W6 |
| **FW6-G15** canonical-geometry TRIPWIRE (**stays RED by design**) | 3.7% of shipped `moon.json` points within 1.0 unit of any extracted point; p90 37.6 vs 1.23-unit spacing; crescent-alone bbox strictly contains the shipped bbox; `.gitignore:53 scripts/*` swallows the only claimed regenerator and its `raw-contours.json`; `grep -rn "def order_contours" $F` → **0** (the script imports a symbol no module defines); shipped 50/10 vs the script's 100/12 | **Closes ONLY when the true source is named or the artifact is re-authored WITH G5c's diagnostics row. ▲ DO-NOT-REGENERATE on `master` — a regeneration attempt is a gate FAILURE, not a pass, and revives L-B1 + L-B2/C-2 at BLOCKER.** Track the seam (G2c) before anything else | **F.W6 / build lane** (F.W5 G16's GREEN-owner cell names this wave by name) |
| **FW6-G16** value-side routing receipt (**zero value-tree bytes**) | No receipt exists, and no fourier-side artefact records the routing of V-α / V-β / V-γ / TA-4. **Round-2 strike (R2-1a/R2-1b): the operand list of this gate is re-stated over authorities that exist.** Round 1 wrote *"itemized in F.W5 §6c"* — ⟨cmd⟩ `grep -l '§6c' waves/F-W5.md` → **no output — F-W5 is not a member of the matching set** (set-membership, never a banked count of a moving file: R3-3.10); the phantom leg is DROPPED and the gate now stands on **TWO** real homes, because neither covers all four: **(a)** for **V-β · V-γ · TA-4** — **F-W5 §4's `F.W5 → value.js API row` edge row** (⟨cmd⟩ `grep -n 'value.js API row' waves/F-W5.md` → *"value-side obligations, never fourier defects \| V-β compound version `_id` + the two-histories test (G2) · V-γ attribution (`userSlug` null ⇒ no version row, E18) · TA-4 per G4's ruling · fork born-visibility (`forks.ts:76`) + create-visibility test (G5) · G18's value-side probe · D9 reconciliation…"*), which **does NOT itemize V-α**; **(b)** for **V-α** — **lane-crud §2's R-2 row** (⟨cmd⟩ `sed -n '161p' formation/fourier/lane-crud.md` → *"**value.js defect V-α — revert writes no version row but still increments the counter.**"*), an F-W5 §0 bounds authority. Routing law, at its corrected addresses (R2-2): ⟨cmd⟩ `sed -n '200,202p' formation/fourier/CENSUS-2026-08-03.md` → **census §4, item 7** — *"value-side V-α/V-β/V-γ routed to the value.js API row per the contract (value-tree edits are NOT fourier-wave gates)"*; ⟨cmd⟩ `grep -n "neither side's edits gate the other's" COHESION.md` → **COHESION §2 — *The cross-sub-tranche dependency graph***, the SS-4 row: *"neither side's edits gate the other's"* (round 1 addressed these as *"census §4.7"* and *"COHESION §1"*; the census has no sub-numbered §4.7 heading and the COHESION sentence is in §2) | The receipt (bounds #3) names the value.js API row per obligation and **cites the authority that actually holds each one** — the F-W5 §4 edge row for V-β/V-γ/TA-4, lane-crud §V-α for V-α, **asserting no list membership for V-α** — and `git diff --stat` over `$V/api/src` + `$V/src` shows **zero** bytes moved by this wave (measured empty at the fold — it must still be empty at close) | F.W6 |
| **FW6-G17** no double-booking / span closure (M-25) — **RE-BASED on real operands (R-3, R-6) and RE-BASED AGAIN on the SHAPE DECREE (R2-9)** | **Both operands are in tree; neither is a carry.** ▲ **DETECTOR, STATED INLINE (R2-9 ⊕ R3-5.2, binding — a gate that states a shape- or spelling-restricted operand is DEFECTIVE at authoring, whatever its result):** the roster is **shape-agnostic** — a markdown **table row** whose terminal cell routes ⊕ an **id-headed bullet** routed in context (`→` · `**ADJUDICATED →**` · `⇒` · `rides` · `routed` · bold-at-disposition) ⊕ a **prose routing walked back to its enclosing row id** (S-8) — **and spelling-agnostic across FOUR band forms, not one token in two dashes**: `F.W5-W8` (U+002D) ⊕ `F.W5–W8` (U+2013) ⊕ **`F.W5(-W8`** (the parenthesised verdict form, `fr-ContourPreview`) ⊕ **bare `F.W5` used as the NAME of the union — UNSCOPED at round 4 (R4-9.5)**: rounds 1–3 restricted this arm to the five records a `comm -13` against the band arm returned, which by construction excludes every record whose only band line is boilerplate; **unscoped, the bare spelling reaches eighteen records** and the fifteen identities that escaped are cited to their F.W5 holder at **§2.11d**. **A gate that names a record-restricted operand inline is DEFECTIVE on R2-9's own terms whatever it returns, and this arm was one — the restriction is gone, and §0.1 removes the need for the arm at all by adopting the stamped census.** With every slash form expanded to its member set and every compound member matched in **either** position. Bullet rows ARE rows; a shape- **or token-** restricted census may not be re-stated here or anywhere. **Round-3 correction of record: rounds 1–2 probed the single token `F\.W5[-–]W8`, and ten identities in six records lived outside it — the operand's spelling half convicted itself on R2-9's own terms.** The escapes and their landings are at **§2.0** and **§2.11c BLOCK A**. ▲▲ **ROUND-4 RE-BASE (R4-3 ⊕ R4-5 ⊕ R4-9.5 ⊕ R4-10) — THE OPERAND IS NO LONGER THIS GATE'S TO DERIVE.** Three rounds of detector re-cuts each closed one blind spot and opened the next: a dash, then a bracket and the union's own name, then the records whose only band line is boilerplate — **and the round-3 fourth arm was scoped by a `comm -13` against the first three arms, so it could not, by construction, see the very records it was added to reach** (fifteen union-routed identities in four records sat outside both operands; §2.11d). This gate therefore **stops re-cutting detectors and adopts the stamped census**: `../conformance/CENSUS-CANONICAL.md` (§0.1). ~~UPPER = the **132-identity span roster**.~~ **STRUCK (R4-5)** — unproducible from §2.11c's own counting rule (111 or 153, never 132), so it never was a denominator. **The record-side census is the canonical's and it homes ZERO rows at F.W6** (⟨cmd⟩ at §0.1 clause 2: *"F.W6`, `F.W8` and `F.W10` are likewise named by **no record**"*), with the band's own record-side denominator at **F.W5 = 28 rows · F.W5-W8 = 90 rows**. **The corpus measurements below survive as measurements of a FROZEN corpus — they reproduce, and R3-3.10 permits a count there and nowhere else — but they are no longer a roster and no longer this gate's denominator.** UPPER = **the ENUMERATION this file publishes**, which is what a set-difference can actually run against: (⟨cmd⟩ `grep -rhoE "F\.W5[-–]W8" docs/tranches/V/megatranche/registry/adjudicated/fr-*.md` → 192 occurrences / 49 records; hyphen 120/26 ⊕ en-dash 72/24; 23 records en-dash-only; **23 of the 192 are the census lane-taxonomy boilerplate line and mint nothing**, five records carrying exactly one band-form line and it that header), enumerated **WHOLE** at **§2.11** (rows 1–56, 56a) ⊕ **§2.11c** (BLOCK A the escapes · BLOCK B the booked · BLOCK C the tail, id-keyed) ⊕ **§2.11d** (the fifteen held at F.W5, cited to their holder, counted into nothing) — **the enumeration is the operand, not an integer, and not any check file's roster.** ▲ **R3-4's standing law binds this cell: a closure denominator copied from a check file FAILS BY CONSTRUCTION** — a gate whose operand is a superseded seat's enumeration can only re-find that seat's misses. Rounds 1–2 offered the PASS-1/PASS-2 `F-W6-CHECK.md` §2 censuses as a cross-check; **that offer is STRUCK** — those files are prior runs, nameable as such, never operands. The operand is the frozen corpus under the four-spelling detector, walked to §2.11c. LOWER = the **7-record NEGATIVE roster** at **§2.11a**. Born-RED because at the first pass the hyphen-only detector missed 23 records and **56 of 122 identities landed nowhere (46 %)** — the exact failure this gate exists to catch, which it could not see while both its operands were this spec's own §2 and §5; **spelling-blindness was cured at round 1 and shape-blindness is decreed away here, so the next format the corpus uses is caught by the gate rather than by a later seat.** The failure class is live in the neighbourhood too: FR-GIG-5's cure was nearly credited to a wave that did not perform it (the standing bar at **F-W5 §0b — *"What this wave is NOT"*** — **round-2 re-key (R2-2)**: round 1 wrote *"F.W5 §1b"*, which is F-W5's *"Do NOT touch — every path below, without exception"* heading) | Set-difference runs **both** directions against the two operands above, **and both directions are now executable from this file** (R3-5.2 · R3-9.3): **(a)** **forward** — **UPPER = `CENSUS-CANONICAL.md` §2's rosters, and nothing else.** Read every id the canonical homes at `F.W5` or `F.W5-W8` and confirm it is printed at §2.1–§2.10 (booked), §2.11 / §2.11c (cited / folded), §2.11d (cited-to-holder) or §5 (excluded-with-reason). ⌧ **The four-spelling corpus detector is retained as dated round-3 provenance and STRUCK AS AN OPERAND at repair round 5 (REST-19; R4-10: *"No wave derives, samples, or re-cuts a roster from any other source — not a check file, not a prior pass, not its own §-arithmetic"*)** ⟨*it is the machinery that produced the `FR-EMT-20` booking REST-15 converts; **round 4 retired this file's derived census and left the derivation installed in the gate that was supposed to retire it**, standing beside the canonical read as a co-equal falsifier*⟩; **reverse** — for every identity printed in §2.11c's three blocks **and §2.11d's fifteen**, `grep` its **banked id token** in this file (the **(record, id) pair** for the eight R-5 collisions and the four homonym pairs §2.11c names) and confirm the landing section named actually holds it. Zero silent drops in either direction. ▲ **Round-4 addition to the forward direction (R4-10)**: the forward walk is no longer a detector run over the corpus but a read of the **canonical §2 rosters** — every id the canonical homes at `F.W5` or `F.W5-W8` must appear here **booked, cited, or cited-to-its-holder**, and every id this file books must be one the canonical does not home elsewhere. **The canonical homes ZERO rows at F.W6, so this wave can have no record-side escape by construction; what it can still have is a fabrication, and that is what the reverse direction now tests.** **The upper operand is enumerated, not asserted: the round-2 device *"rows 57–122 … are not restated here"* is RETIRED, and with it the 54 % of the operand a seat could not run.** **(b)** **no row credited to F.W6 is also credited to F.W1, F.W3/W4, F.W5 or F.W8** — every §2.11 landing is a CITATION and takes no credit; **(c)** no row is manufactured into any of the 7 negative-roster records. `grep -o` the id column of the burn register (bounds #2) and diff against §2 + §5 + §2.11, record-qualifying the eight colliding tokens per R-5 | F.W6 itself (close unit) |
| **FW6-G18** owner rulings present before any conditioned burn — **ROSTER COMPLETED TO R1–R9 (R-1e)** | **`F-W5.md` §2a books NINE rulings owed, R1–R9.** The first pass of this gate booked seven, and mislabelled two of those by gate id instead of ruling id. The complete roster, each verified at `F-W5.md` §2a *"Owner rulings owed — flagged INLINE (SS-4)"* this pass (by HEADING, not line): **R1** TA-4 restore-or-rescope (E3/G4) · **R2** trie-vs-KISS (E16/G7) · **R3** flags producer-or-retire (D3/G11) · **R4** the like verb (D2/G10) · **R5** off-state `[]` admission (**M-9 · D12**) — *"admit it in the contract or stop minting it; the silent rewrite must not survive either branch"* · **R6** hard-delete arm (**FR-AFP-9 · D6** — **not "E7"**) · **R7** codegen vs hand-typed twins (FR-AFP-71 · A4) · **R8** remix-vs-fork + born visibility (**E4** — **not "G5"**) · **R9** delete-or-wire the dead session subsystem (**FR-USB-23 · C3**), honest default *delete (zero external call sites)*. Plus intake **OG-F1 / OG-F2**. `OWNER-RULINGS-F.W5.md` does not exist (F.W5 §1a owns its creation; cited-when-created per §1 r6) | Each conditioned row either carries its ruling or is explicitly deferred with its honest default recorded. **F.W6 may not burn a row conditioned on an unruled question — the whole FR-AFP band rides R3/G11.** ▲ **R5 and R9 are load-bearing here and were the two dropped**: R5 gates `fr-BasisSelector M-9` (§2.11 row 7), and **R9's ⊙ stays ATTACHED to FR-USB-23 on its F.W8 routing row (§5)** — a wave reading only this file must not wire or delete an unruled subsystem | owner → F.W5 unit e; **F.W6 consumes, never re-rules** |
| **FW6-G19** F.W0 re-grounding (D-19) | At the F.W5 authoring pass: `git -C $F rev-parse --short=8 HEAD` → **`cd26c653`**, `git -C $F status --porcelain \| wc -l` → **28** dirty paths no commit holds (GAB-13); the census pin `14d83356` does not resolve; glass-ui 4.0.0 ships a syntactically corrupt `dist/styles/index.css` (**`FR-NP-32 (≡ fr-PaperSidebar M1)`** — **canonical form, restored at this witness cell this round under R3-8.2**: one fact, two banked witnesses, **cite both, never substitute**. The gate's own witness was the single site in this file that carried `M1` alone, and R-8's LAW scopes to *"everywhere the gate is stated"* — R2-7.5 having already closed the reading that absence from a directive index lifts anything. `M1` is additionally a **live homonym token**, so the bare spelling is an identity collapse as well as a citation defect); F8-REACH-01/02 open | F.W0's re-grounding receipt exists and **every anchor cited by this wave is re-resolved against it**. **If F.W0 fails to re-ground, the wave HALTS — it does not proceed on stale bytes** | F.W0; F.W6 cites |

---

## §4 Sequencing — locks and cross-edges (declared from this end)

**Execution shape.** Under the gate, each burn unit's product act is a **commissioned ask** in the bounds #4 letter plus its burn-register row; the fourier edits land in fourier's own sub-session (COMMISSION §2) after the begin-word. Nothing in this wave opens product source in either repo.

**In-wave order** (serial; each commits before the next opens):

`a` preconditions (FW6-G19 receipt + F.W5 clause set + FW6-G18 rulings) → `b` **the PATCH/chain one-cut** (F-α · F-β · SS-C-1 — G1/G2/G3/G4) → `c` privacy (F-γ + FR-AFP-4 — G5/G11, one cut) → `d` counters + idempotency (G6/G7) → `e` **moderation band, only if G11 ruled** (G8/G10/G12 + FR-AFP-7/33) → `f` audit actor (G9) → `g` contour + geometry (G13/G14/G15) → `h` liveness strikes (PP-DEADSEAM, only what E16/G7 does not touch) → `i` routing receipt + carry closure (G16/G17).

**Ordering and cure-integrity locks, carried whole:**

1. **F.W0 substrate pre-gates FIRST** — no line anchor, no HEAD figure, no "LIVE at HEAD" citation until the re-grounding receipt exists (28 dirty paths, unresolvable census pin, corrupt glass dist, F8-REACH-01/02). **F.W0 failing to re-ground HALTS this wave.**
2. **F.W5 STATES, F.W6 BURNS — no double-booking.** The law has **two real homes and is quoted from both, never fused** (R2-1b; round 1 attributed a composite of them to a nonexistent *"F-W5 §6c"* — ⟨cmd⟩ `grep -l '§6c' waves/F-W5.md` → **no output — F-W5 is not a member of the matching set** (set-membership, never a banked count of a moving file: R3-3.10)): ⟨cmd⟩ `grep -n "owns the CLAUSE" waves/F-W5.md` → **F-W5 §2 §E clause E2, the ▲ sentence** — *"F.W5 owns the CLAUSE; F.W6 owns the burn-down — do NOT double-book."* — and **F-W5 §3 gate G3, the green-owner cell** — *"F.W5 owns the CLAUSE only — no double-booking"*. Stated on F-α at clause E2 / gate G3, and extended here to every clause whose GREEN owner is F.W6 or "the fourier API row".
3. **ONE-CUT LAWS**: F-β ⊕ SS-C-1 in one PATCH-model change (else the second reverts the first's invariant) · F-γ ⊕ FR-AFP-4 as one privacy limb (a redacted breadcrumb over a world-readable image is not a cure) · FR-AFP-66 ⊕ FR-AFP-33 (cascade without bound leaves the cost; bound without cascade leaves the growth).
4. **G11 is the ADMISSION GATE for the whole FR-AFP band**, and **precedes F.W1's sizing wherever FR-AFP grades are load-bearing** (ruling 5).
5. ▲ F.W1 is ONE atomic land-or-lose transaction — **the TWELVE-limb roster chartered at `F-W1 §4 step 4` — cited whole, never restated here.** **Round-2 re-quote (R2-5), by command against the corrected charter now that the F-W1 edit has landed**: ⟨cmd⟩ `grep -o "The roster is TWELVE limbs and stays twelve[^*]*" waves/F-W1.md` → *"The roster is TWELVE limbs and stays twelve — the eleven chartered plus the FR-EQC-7 vaul-vue manifest gate returned INSIDE at repair round 1 per the `:294` pin (R-4a, cross-edge 1 at this writing) — it is the extent every sibling spec cites (R-4b); nothing is added to it by a later reading."* Round 1's *"ELEVEN-limb roster chartered at `F-W1.md:276`, incl. the … gate INSIDE per `:294`"* is **superseded on both counts**: the count word is TWELVE at the charter's own hand, and the two line cites into a live sibling are struck for the stable anchor (R2-2 — line numbers into `waves/` are a defect from that ruling forward; the `:294` pin survives only *inside* F-W1's own quoted bytes, as its intra-file coordinate, not as this file's address). **This spec re-derives no count and names no limb** (R-4b: a sibling that restates the transaction re-fractures it. The first pass of this cell named three limbs, so a downstream seat could have landed the manifest gate, the RE-PIN at the adopted commit hash, or the P0 CSS-class census separately and believed itself conformant. The extent of the transaction is now stated **only by citation**.) F.W1 precedes F.W3/W4 (§2 law). **Reciprocal credit law, both directions: F.W1 must NOT be credited for the pagination-drain cure (FR-GIG-5), and F.W6 claims no F.W1 credit.**
6. **Killed cures stay killed**: AA-23's regex action · β's 428-escalation · the K9 fallback deletion · SE-05's "just delete it" for the `window` handoff · **m-7's 422-straddle sub-claim (K-3) — the KILL is of the straddle scenario ONLY; m-7's live MINOR row and its constructive cure survive and are carried at §2.11 row 8**. **K-6/S-8**: absence-proofs enumerate the surface — **and this wave's own span census is bound by the same law (R-6: both dash forms, then walk back to the enclosing row id)**. **K-1**: 13/44, cite 45/30/13, never 30 alone or zero. **K12**: B-2 closes statically. **FR-GV-24**: no re-open-increment assertion.
7. **TRIPWIRE**: DO-NOT-REGENERATE `moon.json` / `sun.json` on `master`; track the seam (G2c) before any geometry act.
8. **No projection strike that is really a compression decision** until G7/E16 rules; the anti-tree KISS guardrail (`atomdiff.py:12-14`, quoted in both trees) is the incumbent.
9. ▲ **WAVE-LOCK — `fr-GallerySearchBar` C-2 grade, binding on this span; the ⟨cmd⟩ receipt for these bytes is at §2.11 row 50 (R2-1-LAW: one command, one quote, cited once rather than re-typed twice)**: *"carried as a **WAVE-LOCK** on any F.W5-W8 wiring of `basisFilter` — wire it without the banked normaliser and the Fourier pill returns zero rows against `active_bases ∈ {fourier-epicycles, fourier-series}`."* The banked normaliser is `GalleryCard.vue:39`'s `startsWith("fourier")`; the fold target is fr-BasisSelector M-10 (banked MAJOR → F.W3/W4). **This is the only lock in the corpus that binds the span by name — it binds F.W6 exactly as it binds F.W5, F.W7 and F.W8** (R-6.4; restored, §2.11 row 50).
10. ▲ **Record-qualification (R-5)**: the **ten** colliding tokens — `M-13` · `L-B1` · `L-M3` · `C-17` · `C-18` · `B-1` · `B-2` · `C-2` · **`M-10`** · **`M-9`** — are written with their record everywhere in this file, so no set-difference collapses two banked identities into one hit ⟨*`M-10` and `M-9` added at repair round 5, REST-20; the list read "eight" through four rounds while both tokens were failing silently, and FW6-G17's reverse falsifier is unrunnable on a bare probe at either*⟩.

**Cross-edges** (declared from this end; each owed a reciprocal sentence in the named file):

| to | direction | this end's declaration |
|---|---|---|
| **F.W0** | **DEPENDS — HARD** | Re-grounding of the 28-path dirty worktree (GAB-13), the unresolvable pin `14d83356`, glass-ui 4.0.0's corrupt `dist/styles/index.css` (**FR-NP-32 ≡ fr-PaperSidebar M1** — one fact, two banked witnesses; cite both, never substitute), F8-REACH-01/02. **Failure HALTS; no proceeding on stale bytes.** ▲ **R-9.1, published-once law**: every anchor and every denominator this wave cites is an **input to / quotation of F.W0's G-11 anchor table and G-12 denominator table in `fourier/docs/tranches/F/SUBSTRATE-LEDGER.md`** — **divergence from those tables is a defect against G-11, not a rival act, and F.W6 re-performs no re-resolution as its own.** **Round-2 re-anchor (R2-2 + the R-1 re-measure law): round 1 addressed the two tables by LINE, as `F-W0.md:199` / `:204`.** The LAW that erratum teaches is right — a line into a live sibling is a defect from R2-2 forward — but **round 2 then stated a FACT about where those two lines had moved to**, and that fact staled against F-W0's next write within the same round. Round 2 asserted that `:199` had come to sit inside G-8's body and `:204` had become G-9's *"Owning rows"* cell; **this seat ran `sed -n '199p;204p' waves/F-W0.md` against the live bytes and neither claim holds — and the replacement content differs again from what this seat saw minutes earlier in the same sitting, because F-W0 is under its own round-3 repair as this is written.** No new content claim about those two coordinates is made here, and none can be made truthfully by any spec: **that is the finding, not a detail of it.** **Round-3 cure (Q-6, R3-3.9 ⊕ R3-10 row 2): the stale FACT is retired and replaced by a CLASSIFICATION, which is the only form that cannot stale** — *the two retracted line addresses now resolve inside F-W0 to whatever its current repair put there; they are quarantined coordinates, not citations, and no seat may follow them.* The live address is the **gate heading**, which is stable because F-W0's own §4 forbids line cites downstream: ⟨cmd⟩ `grep -n "^### G-11 —\|^### G-12 —" waves/F-W0.md` → *"### G-11 — ONE corrected anchor table published; every later wave quotes it"* and *"### G-12 — ONE corrected-denominator table published; superseded figures FORBIDDEN downstream"* (the line numbers this command prints are its **output**, never this file's address for them — R3-3.10's distinction between a receipt and a banked coordinate). **The general lesson, fourth generation and now stated as a rule rather than re-learned: an erratum that cures a stale address by asserting a new fact about a live sibling has moved the defect, not removed it. Cure addresses with anchors; cure facts with classifications.** **Cited hereafter as F-W0 §4 G-11 / §4 G-12 — by gate id, never by line** (F-W0 is the shift-source of this round's staling class, and its own §4 forbids line cites downstream). G-13's corollary binds equally: **producer-side evidence carries the producer COMMIT HASH, never the version string** (P-4) |
| **F.W5** | **DEPENDS — HARD; F.W5 STATES, F.W6 BURNS** | **Round-2 strike (R2-1a), both halves FABRICATED and struck together**: round 1 opened this cell *"F.W5 §12 declares it blocks F.W6 ('the burn-down needs the chain clause')"* — ⟨cmd⟩ `grep -l '§12' waves/F-W5.md` → **no output — not a member** (R3-3.10: set-membership, never a banked count of a moving file), and ⟨cmd⟩ `grep -rn "burn-down needs" waves/` → this file only. **F-W5 has no §12, no blocking declaration, and that sentence exists nowhere in the tree.** Quotation, attribution and the *"declares it blocks"* framing die together; **no phantom leg is left carrying the dependency.** ▲ **QUARANTINE MARKER (round-2b, this seat)**: the struck sentence is reproduced ONCE above and ONCE at §2.1, in strike, as **this file's own retracted bytes** — it was never any other file's, which is precisely what convicted it. It is **not an authority and may not be cited, lifted or re-quoted by any seat**; a downstream `grep -rn "burn-down needs" waves/` hit resolves to this erratum and to nothing else. Retaining the retracted words under a marker is what makes the retraction auditable; retaining them unmarked would re-mint the invention. The real edge, in F.W6's own voice: ⟨cmd⟩ `grep -n 'F.W5 → F.W6' waves/F-W5.md` → **F-W5 §4's `F.W5 → F.W6` row** — *"emits burn-down \| F-α's deepen-or-retire execution (G3); the G16 canonical-geometry pipeline WITH G5c's diagnosed result type; **DO-NOT-REGENERATE stands until F.W6 lands the pipeline** (tripwire: L-B1 + L-B2/C-2 revive at BLOCKER)"* — which fixes the split and says nothing about blocking; the sequencing fact this wave needs (F.W5 states the clause before F.W6 burns it) rides that edge row plus clause **E2 ▲** and gate **G3**, and needs no blocking declaration to hold. **Clause ids re-keyed against `F-W5.md`'s live register (R-1e) — the first pass of this cell re-labelled six clauses and invented a seventh.** Clauses whose GREEN owner is F.W6 / the fourier API row: **C1, C2, C5, D5, D6, D8, D10, D11, D12, D16, D17, E2, E5, E6, E7, E13, E14, E17, E18, B4/E10, §G G1c–G10c**, and D2/D3 conditionally. (**Round-2 correction**: this cell wrote *"G1c–G9c"* while FW6-G2 and the gate-gap declaration below wrote *"G1c–G10c"* — the short range is struck as internally inconsistent AND as dropping a clause §2.6's own row depends on: ⟨cmd⟩ `grep -n "G10c" waves/F-W5.md` → *"**G10c** The artifact's provenance prose is false \| **fr-FourierShapeExtractor L-m3 / C-10 ⊕ L-m5**…"*, the home of two identities §2.6 cites.) Corrections of record: **`E7`** is *PATCH atom coverage + `set_hash`* (not "E8") · **`E13`** is *Cache identity ⊇ consumed fields* (not "E12") · **`E14`+`E17`** are *contour provenance* + *image bounds on write* (not "E11") · **`D5`** is *which identifier a human surface may render* (not "D12") · **`D17`** is *date serialization* (not "D10") · **`D6`/ruling `R6`** is the *hard-delete arm* (not "E7") · **no F-W5 CLAUSE is numbered `D9`** — the actor clause is **`E18`**. ▲ **Round-4 precision (R4-9.3): "no clause D9" is not "no D9".** `D9` is a **value.js product ruling** with its own rooted block in F-W5 (Home: `docs/tranches/V/DECISIONS.md` §2's rulings table, row `D9`), which clause `R8` must not contradict — receipts and the retired count at §2.5. The clause-register correction stands; the gloss that made `D9` a label inside `R8` is struck. Clauses whose rows this wave CITES but does not own — **B1, B2, E4, E8, E9, E11, E12, E15, E20, D7, A3, A4, A5, A6, C3, C4, F1–F9, G3c/G4c/G8c/G9c** — are enumerated per-identity at §2.11 and §2.11c. (**Round-3 completion**: rounds 1–2 wrote *"F1–F6"* and omitted **B1, B2, E20, F7, F8, F9** from BOTH lists — yet §2.11 rows 4/27/49/56 already cite **B1**, and the ten census escapes land on **B1 · B2 · D12 · E10 · E20 · F1 · F6 · F7 · F8**. A clause cited by four standing rows and absent from both lists is the same unenumerated-operand defect §2.11c cures one section up; the register runs A1–A6 · B1–B5 · C1–C5 · D1–D8 · D10–D17 · E1–E20 · F1–F9, and these lists are now closed against it.) ▲ **These two lists are DESCRIPTIVE, not the discriminator.** Round 2 made a clause's green owner the test for booking-vs-citation and it contradicted this file's landings in both directions (PASS-3 D-3); **the test is now the ROW's own terminal disposition (§2.11b as amended)**, and the lists survive only as a map of which clause states what. **Declared gate gap: F.W5 states clause `E7` (F-β ⊕ SS-C-1 write leg) but carries NO gate across `G1–G22` (+ §G's `G1c–G10c`) — F.W6 supplies FW6-G2/G3/G4 rather than read the omission as absence of scope.** F.W6 authors no clause and reverses no ruling |
| **F.W1** | **NO GATING either way, one exception** | The exception: **G11's producer-or-retire PRECEDES F.W1's sizing** (ruling 5). F.W1's atomicity and the FR-GIG-5 non-credit lock are untouched |
| **F.W3 / F.W4** | **F.W6 EMITS client/display arms — one home, two citations** | Emitted and **not claimed**: AA-10's sentinel-aware branch + legend ⟨*canonical home **F.W4**, cited not booked — REST-16*⟩ · AA-5/AA-24 display taxonomy · AA-6 placeholder honesty · FR-AFP-8's one-token `item.slug` label · FR-AFP-9 / FR-GV-34 truthful copy (diction rides FR-AFP-35) · FR-AFP-33's client collapse (→ F.W1/W3) · FR-AFP-70's reflect-and-disable interim · FR-AFP-59's tier render · VV-R2-A's publish→`store.setVisibility` · **VV-R2-B's single wiring unit** (route→`loadVisualization`, publish→`setVisibility`, delete→the store action, incl. GM-M4's `softDelete`/`restore`) · GCM-1/GCM-25's `/v/` routing repair · **L-2's orchestrator move** (REQUIRED; urgency rider at the data-loss class) · the like verb's `aria-pressed` + re-click guard · FR-GV-13's `?owner=me` control · **FR-AUL-17's shared formatter (already banked → F.W3; do NOT re-book)** |
| **F.W7** | **F.W6 must NOT pre-empt** | ⊙ G7/E16 unruled; F.W6 carries the documented default — **no trie; whole-snapshot duplication is the recorded shipped behaviour.** `trace` (M-β4) and `reconstructed_points` (L·m-6) strikes wait on the ruling. The standing anti-tree KISS guardrail (`atomdiff.py:12-14`, quoted both trees) binds until the owner moves it. ▲ **F.W7's routing census is a measured ∅ and that ∅ is a finding, not a gap** — corroborated at the canonical's §4.1 (`F.W6`, `F.W8` and `F.W10` are likewise named by **no record**). **F.W6 asserts nothing against it and adds no row to it.** ⟨**STATED LIVE AT REPAIR ROUND 5 (REST-30; P5-17).** Round 4's preservation lists claimed *"F.W7's ∅ posture"* preserved; ⟨cmd⟩ `/usr/bin/grep -noE '.{0,45}∅.{0,45}' waves/F-W6.md` returned **three hits, none of them a live statement of it** — one a grep exit code and two the preservation lists themselves. **A posture preserved that was never stated is a preservation of nothing**; it is stated once, here, at the edge it governs.⟩ |
| **F.W8** | **F.W6 BURNS SERVER DEFECTS; F.W8 WIRES CLIENTS + RUNS CONFORMANCE** | Not F.W6's, declared so no re-cut drifts: FR-GV-8 (7 of 13 `/api/visualizations` ops — remix, publish, unpublish, forks, provenance, diff, versions — with zero client function) · FR-GV-27's `/publish`/`/unpublish` wrappers · FR-GFC-4's missing `tier` param at every layer · FR-USB-15/23/24 (session truth, dead session subsystem, the 429 retry posture — C3/C4's cures land at the store/api seam; **C4's one-controller lock binds any retry change**). ⊙ **FR-USB-23 CARRIES ITS OWNER GATE ACROSS THE ROUTING — ruling `R9`, delete-or-wire the dead session subsystem (C3), honest default *delete (zero external call sites)*, unruled.** (R-1e: the first pass routed this row to F.W8 with its ⊙ stripped, so a wave reading only F-W6 could wire or delete an unruled subsystem. The gate travels with the row.) · D1's per-operation dispositions. ▲ **Counting lock K-1: a bare `@router.` grep is blind to prefixed routers (`@gallery_router.get`) — read against 13/44 and cite 45/30/13, never 30 alone** |
| **F.W9 / F.W10** | **F.W6 EMITS coverage obligations** | A burn-down with no probe repeats exactly the defect FR-AUL-13 convicts (a client written against a contract DOCUMENT rather than the router). Emitted: AA-44 (zero frontend tests; nine e2e specs never touch the audit tab while the backend pins its half at `test_janitor_audit.py` — the one-sided seam that let AA-5/AA-6/AA-24 stay open) · FR-AFP-49 = AA-44 = FR-AUL-16 (zero automated coverage of any kind; all four AFP blockers invisible to every gate the repo runs) · FR-AFP-66's orphan-flag test exercising only the reporter-keyed path · G2's absent same-content-palette history test · G5's per-side create-visibility tests |
| **the value.js API row** (CO-SIGNER) | **F.W6 ROUTES, never burns** | V-α, V-β, V-γ and ⊙ TA-4 leave as the routing receipt, **each citing the authority that actually holds it (R2-1b)**: **F-W5 §4's `F.W5 → value.js API row` edge row** for **V-β · V-γ · TA-4** (⟨cmd⟩ `grep -n 'value.js API row' waves/F-W5.md` — the row itemizes *"V-β … · V-γ … · TA-4 per G4's ruling · fork born-visibility … · G18's value-side probe · D9 reconciliation"* and **does NOT itemize V-α**), and **lane-crud §2's R-2 row** for **V-α** (⟨cmd⟩ `sed -n '161p' formation/fourier/lane-crud.md` → *"**value.js defect V-α — revert writes no version row but still increments the counter.**"*). *"F.W5's obligation list"* as a single operand for all four is **struck**; no membership is manufactured. Binding: **census §4, item 7** + **COHESION §2**'s SS-4 row (**round-2 re-address (R2-2)**: round 1 wrote *"Census §4.7 + COHESION §1"* — the census has no sub-numbered §4.7 heading and the sentence *"neither side's edits gate the other's"* lives under COHESION's `## §2 The cross-sub-tranche dependency graph`). **Declared consequence: F-β's `set_hash` recompute reaches `/diff`, so any cross-repo diff assertion this wave makes is UNFALSIFIABLE until G4 rules TA-4 restore-or-rescope.** The reciprocal edge is REQUESTED from that end, not written here |
| **SS-4** | **F.W6 IS SS-4's burn half** | SS-4 **FLAGS THE OWNER RULINGS INLINE — all NINE, by ruling id (R-1e; the first pass flagged seven and mislabelled two)**: **R1** TA-4 (E3/G4) · **R2** trie-vs-KISS (E16/G7) · **R3** producer-or-retire (D3/G11) · **R4** the like verb (D2/G10) · **R5** off-state `[]` admission (M-9/D12) · **R6** hard-delete arm (FR-AFP-9/D6) · **R7** codegen-vs-hand-typed-twins (FR-AFP-71/A4) · **R8** remix-vs-fork + born visibility (E4) · **R9** delete-or-wire the dead session subsystem (FR-USB-23/C3) — plus intake **OG-F1** freeze-with-adoption / **OG-F2**. Each is flagged where its row sits, **never presumed and never pre-answered**; where a row is conditioned, its honest default is recorded beside the flag. SS-4 also **names the TA-4 value-side atomdiff restoration as PREREQUISITE or re-scopes explicitly.** Two registry-integrity carries: GCM-10's "MF-9" companion cite resolves to **nothing** repo-wide and is **STRUCK** unless SS-4 resolves it; the challenge-row-census vs roster-census diff (which caught the L-27 drop) is commended as a standing gate |
| **glass-ui BH relay (SS-6)** | **DECLARED EMPTY** | F.W6 carries **ZERO** producer rows — declared so (a) no glass-producer row is smuggled in as a server cure and (b) no server cure is discharged as a frontend hack. Any producer-shaped finding leaves as a letter to the standing glass-ui BH inbox (the owner's fond, a standing formation invariant) |
| **X·parse-that** | **FORBIDDEN EDGE** | Direct parse-that→fourier routing is forbidden; anything parser-shaped routes through the megatranche, never across |
| **SS-13 / live-probe lane** | **F.W6 DEFERS MAGNITUDE ONLY** (never used to close a gate) | VV-R2-A's observable phantom view (publish fresh, read the card count; derivation: born at 1) · FR-AFP-4's deployment auth-proxy question · FR-AFP-33's aggregate cost + β's sort-index note · M-14's end-to-end contour-loss reproduction · B-4's `contour_hash` instability across an ML-threshold change · L-B1's "what `moon.json` WAS generated from" (UNDETERMINED). **Probe parsimony binds** |
| **`/Users/mkbabb/Programming/fourier-analysis`** | **READ-ONLY, ALWAYS** | Zero fourier bytes. Immutable beside this spec (E-1/E-3): `lane-crud.md`, `INTAKE-ADJUDICATION-2026-08-03.md`, all 66 `fr-*.md`, `J-diff-shape.md` v1 (271 lines — v2 supersedes by reference and never patches it). `COHESION.md` is root-authored at the boundary and excluded. `scripts/dev/dev.sh` is an unowned dirty row and is never staged |

---

## §5 Excluded — nothing silently dropped; every exclusion reasoned

| excluded | reason |
|---|---|
| **SS-C-1's READ leg** (`loadVisualization` zero callers) | banked at **fr-GalleryCardModal GCM-1** at blocker weight → F.W4 + F.W5-W8 rider. The axis split was adjudicated deliberately; re-merging the halves re-books GCM-1 |
| Every client/display arm listed in the F.W3/F.W4 cross-edge (AA-10 legend ⟨canonical F.W4, cited⟩, AA-5/AA-24 taxonomy, AA-6 placeholder, FR-AFP-8's label, FR-AFP-9/FR-GV-34 copy, FR-AFP-33's collapse, FR-AFP-70's interim, FR-AFP-59's render, VV-R2-A/B wiring, GCM-1/GCM-25 routing, **L-2's orchestrator move**, `aria-pressed` guard, FR-GV-13's `?owner=me`, FR-AUL-17's formatter) | one home, two citations — **F.W6 emits, never claims** (the FR-GIG-5 mirror, standing bar) |
| **F.W8's client-wiring set**: FR-GV-8, FR-GV-27, FR-GFC-4, FR-USB-15, ⊙ **FR-USB-23** *(its `R9` owner gate travels with it — see §4's F.W8 edge; it is routed, not un-gated)*, FR-USB-24, D1's per-operation dispositions | F.W8 wires clients and runs conformance; F.W6 burns server defects. Declared so no re-cut of the `F.W5–W8` span drifts a row into two homes |
| **F.W7's compression design** — `trace` and `reconstructed_points` strikes | ⊙ G7/E16 unruled; the documented default (no trie, whole-snapshot duplication) is the incumbent until the owner moves it |
| **Regeneration of `moon.json` / `sun.json`** | the TRIPWIRE: an attempt is a gate FAILURE, not a pass, and revives L-B1 + L-B2/C-2 at BLOCKER |
| **Owner rulings themselves** — the **nine**, by ruling id: **R1** (TA-4/E3/G4) · **R2** (trie/E16/G7) · **R3** (producer-or-retire/D3/G11) · **R4** (like verb/D2/G10) · **R5** (off-state `[]`/M-9/D12) · **R6** (hard-delete arm/FR-AFP-9/D6) · **R7** (codegen/FR-AFP-71/A4) · **R8** (born visibility/E4) · **R9** (dead session subsystem/FR-USB-23/C3) — plus OG-F1, OG-F2 | owner acts: **flagged inline throughout, never presumed, never pre-answered.** F.W6 consumes rulings; it does not rule, and it may not burn a conditioned row unruled (FW6-G18). R5 and R9 were dropped at the first pass and are restored; the gate-id labels "G5" and "E7" are corrected to **R8/E4** and **R6/D6** |
| **Codegen / contract-module split** | ⊙ FR-AFP-71 is owner-gated; v1 chose hand-typed twins explicitly and v2 may not silently reverse it |
| **Deleting the five `?? item.slug` fallbacks** · the **AA-23 regex-action** cure · **β's 428-escalation** · **SE-05's "just delete it"** · **`fr-BasisSelector` m-7's 422-STRADDLE SCENARIO ONLY** · **D-1's skeleton cure** | KILLED at their registers (K9 · AA-23 · self-killed by its author · G6c · K-3 · K5) — no repair is inherited from a killed corpus. ▲ **Scope correction (R-6.4): K-3 kills m-7's straddle SCENARIO, not m-7.** The row itself is live MINOR and its constructive cure (*generate client bounds from the operation models*) is carried at §2.11 row 8 → F.W5 D7. **A killed sub-claim never kills its host row**, and the first pass's carrying-the-kill-while-dropping-the-row is cured |
| **Any value-tree byte** (`$V/api/src`, `$V/src`) for V-α/V-β/V-γ/TA-4 | **census §4, item 7** — *"value-tree edits are NOT fourier-wave gates"* — + **COHESION §2**'s SS-4 row — *"neither side's edits gate the other's"* (**round-2 re-address (R2-2)**: round 1 wrote *"census §4.7 + COHESION §1"*; both addresses are corrected here and at FW6-G16 and §2.10, the three sites that carried them); **FW6-G16 fails if any byte moves** |
| **FR-GV-1 · GIG C-3 · GCM-2 · i-7 · GCM-52 · ImageUpload row 26 · FR-AFP-18 · FR-AFP-40/-69 · GCM-55 · VV-R2-B · ContourPreview row 28** | folded **BY REFERENCE** into their carrying identities above — cited, never re-booked (the fold is the identity guard, not a demotion) |
| **The 23 routing-law boilerplate occurrences** among the **192** union-form `F.W5[-–]W8` mentions — **and, separately, the FIVE records whose only band-form line is that header** (fr-AdminUserList · fr-CanvasOverlayButton · fr-DarkModeToggle · fr-GlassTimeline · fr-HarmonicLevelGrid) ▲ **ROUND-4 CORRECTION (R4-7.3), the same one made at §2.0 and made here because this is the second site that carried the claim: FOUR of these five mint nothing into the union at the canonical; `fr-AdminUserList` mints ELEVEN, all held at F.W5 and cited at §2.11d.** The band-line measurement was always true; the identity claim resting on it was not, and an exclusion row is exactly where a false identity claim does the most damage | boilerplate mints no rows (the RE-CUT-0 dissent's explicit warning). **Round-2 strike (P2-9 cluster): "3" was an un-measured integer asserted inside the very cure that convicted un-measured arithmetic, and it is STRUCK** — ⟨cmd⟩ `grep -rnE "F\.W5[-–]W8" fr-*.md \| grep -c "tri-package uplift"` → **23**, one per record, enumerated at §2.0. The three populations are separated rather than fused into one integer (the conforming sibling form is **F-W9 §5's boilerplate row**: *"The 14 boilerplate routing-law lines, the 12 verdict-prose mentions, and the 7 boilerplate-only records"*). **The 192 → 122 derivation is UNCHANGED** — boilerplate minted no identity under either count, so no operand of FW6-G17 moves; the five boilerplate-only records sit inside G17's 49-record denominator and outside **both** its identity operands. The denominator is the **spelling-agnostic** one (R-6); the first pass's "120" was hyphen-only and is superseded. **Round-3 (R3-5.2): "spelling-agnostic" now means FOUR band forms, not two dashes** — the parenthesised `F.W5(-W8` and the bare `F.W5`-as-union-name carried **ten identities** past a detector that called itself agnostic, and the **five bare-form records sat outside the 49-record denominator entirely**. Their taxonomy headers are boilerplate on the same reading and mint nothing; ~~the roster is re-denominated to **132** at §2.0~~ — **STRUCK at round 4 (R4-5); the ten are still landed at §2.11c, but no roster integer survives and the census is the canonical's (§0.1)** |
| **`fr-ContourPreview` row 13 = `L:L-5`** — census escape **E-1**, the one escape that does not cite | **EXCLUDED-with-reason (new this round, R3-5.2).** Its stating home is `F-W5.md` **E20** — *the exposed-ref seam, the one non-wire contract row* — and its terminal disposition is a **component contract**, not a server act: make the `defineExpose`d ref readonly and pass **both** members (`points` AND `magnetRadius`). **Consuming `F-W3.md` §X.1-v4 (published FIRST this round, R3-1a)**: `ContourEditorCanvas.vue` is one of the partition's four **F.W4 default-arm negatives**, named per id at §X.1-v4 item 2, and `VisualizationView.vue` ∈ unit **`.e`**, whose residue item 5 assigns to **F.W3**. The twin partition answers the question about a path; **F.W6 asserts no home of its own and books nothing on the twin surface**. Landed in full at §2.11c BLOCK A |
| **The nine CITED census escapes** — `FR-EMT-1` ‡ · `FR-EQR-4` · `FR-EQR-32` · `FR-NP-30b` ⊙ · `D-L4+C-7` · `L-M7+C-8` · `C-7` (ConvergencePlot) · `L-m13` — **and `FR-EMT-20`, which is NOT excluded and NOT booked — CITED at §2.7 to its canonical holder F-W5** ⟨*re-cut at repair round 5, REST-15*⟩ | **not excluded — CITED**, on `F-W5.md` clauses **F1 · F8 · F8/B1 · D12 · B1 · F7 · B2 · F6**, each verified at F-W5's live register this round. Listed here so no reader mistakes their absence from §2.1–§2.10 for the drop they were before §2.0's detector was re-cut: **rounds 1–2 could not see them at all.** Two carry conditional promotions (FR-EQR-4's seam choice at F8; FR-NP-30b's ⊙ R5 branches) and both are recorded rather than presumed |
| **The 56 span identities re-homed at §2.11** | **not excluded — CITED.** Listed here only so no reader mistakes their absence from §2.1–§2.10 for a drop: their STATING home is an `F-W5.md` clause (or, for four, a named fold / F.W4 carry), so R-5's one-home law forbids booking them here. **F.W6 claims credit for none of them.** **Round-3**: the tail beyond row 56 is no longer asserted — it is enumerated by id at **§2.11c BLOCK C**, with the branch each identity took |
| **The 7 negative-roster records** (fr-App · fr-CollapsibleSection · fr-MobileFloatingToc · fr-PaperSearch · fr-PaperSearchDropdown · fr-PaperSearchInput · fr-MorphShapePreview) | they declared an explicit **zero** to the span; they are carried at §2.11a as FW6-G17's lower-bound operand and **no row may be manufactured into them** (B5: negative controls are evidence, never a denominator) |
| **GCM-10's "MF-9" companion cite** — *the cite only, never the row* | the cite resolves to nothing repo-wide — **STRUCK unless SS-4 resolves it**; no cure is quoted through it. ▲ **Scope correction**: striking a dangling cite is not disposing of the identity. **`fr-GalleryCardModal GCM-10`'s routed server cure — a `tier` field with a default on the model, the serialization-bypass family — is CARRIED at §2.11 row 45 → F.W5 A3 + D17**, and it is load-bearing for §2.4's booked GCM-55, whose registry cure reads *"with GCM-10's model fix"* |
| **Glass-producer rows; latex-paper rows; keyframes rows** | SS-6 / the LATEX-PAPER register / KF's seat — producer rows never become frontend hacks, and none is smuggled in as a server cure. The paper register's same-commit riders (fr-PaperSearchModal D-1+D-3+outline:none · PAW-44/LAW-3 restore-only-with-background-cure · MPC-31/MPC-3⊕10⊕13⊕8⊕22 one-cut law · FR-MSP-6 two-channel lock) travel with their rows, which do not land here |
| **Every fourier-tree write** | COMMISSION §2 — their sub-session. F.W6 asks by letter and verifies receipt; it never edits |
| **Any product-source open in either repo; any status advance beyond `planned`** | the execution gate — nothing opens product source until the owner's begin-word |

---

**Round-4 repair record (L-20, `PASS-4/RULINGS-4.md`, 2026-08-29) — the round the census stopped being this file's to derive.** The L-18 base met at each prior round was a defect *inside* an instrument this file built: round 1, *a set-difference declared empty without running both directions*; round 2, *a gate argued RED over a witness that does not reproduce*; round 3, *a gate whose operand cannot see the thing it is supposed to count*. **Round 4's base is the one that ends the sequence: *a cure that inherits the blind spot of the thing it cures*, and *a transcript built to retire an integer that cannot reproduce that integer*.** Cured, by directive:

- **R4-5 / R4-10 — THE ROSTER INTEGER IS STRUCK AND THE CENSUS IS RE-SCOPED.** The 132 was unproducible from §2.11c's own counting rule: **BLOCK C marks 42 rows branch-2 — the branch the rule declares NOT roster members — while the partition required exactly 42 tail MEMBERS**, so the enumerated roster closed at **111** with the rule applied or **153** with it ignored, and the gate's set-difference had no determinate denominator. Struck at §2.0, §2.11's heading, §2.11b (twice), §2.11c's counting rule, FW6-G17 and §5. **The record-side figure for F.W6 is ZERO** — canonical §4.1, *"F.W6`, `F.W8` and `F.W10` are likewise named by **no record**"* — and this wave's population is folds, gate operands and commissioned acts, **enumerated by register at §0.1 and summed into nothing.** The band's record-side denominator is now the canonical's **F.W5 = 28 · F.W5-W8 = 90**. **The rule survived and the integer died, which is the right way round: an integer could never carry the fold distinction, and every round so far broke on one.**
- **R4-9.5 / R4-3 — THE FOURTH DETECTOR ARM RE-CUT UNSCOPED, AND THE FIFTEEN CITED TO THEIR HOLDER (§2.11d, new).** Round 3's bare-`F.W5` arm was scoped by `comm -13` against the band arm, a construction that **can only return records carrying no band form at all** — so a record whose single band line is routing-law boilerplate was excluded from the new arm by the same operation that was supposed to catch it. Unscoped, the spelling reaches **eighteen** records, not five, and **fifteen union-routed identities in four records** (`fr-AdminUserList` ×10 · `fr-AdminAuditLog` ×3 · `fr-GalleryAdminBanner` ×2) sat outside both operands. Every one is **held at F.W5 by the canonical**, so under R4-10's roster law they enter as **gate-defect rows citing their holder — cited, never booked, never counted, never routed.** `FR-AUL-59` is the sharpest: for three rounds its only occurrence in this file was **inside a quotation of F.W5's B1 compose-lock**, and a citation of a lock is not a disposition of the id it locks.
- **R4-7.3 — THE COUNT-WORD CORRECTED AT BOTH SITES.** *"FIVE of the 49 records … mint no identity"* was a claim about **identities** resting on a measurement of **band-form lines**, and false at the bytes for `fr-AdminUserList`, which the canonical homes **eleven** rows at F.W5. Corrected in place at §2.0 and at §5's exclusion row: **four of the five mint nothing into the union; one mints eleven, held elsewhere.** The parenthetical about line counts was always true and is preserved — *where the prose and the list disagree, the LIST is authoritative.*
- **R4-1.7 → R4-4.2 — §2.6's Q-4 PAIR REVOKED AND RE-PUBLISHED.** Round 3 cured the added emphasis on the frozen `fr-FourierShapeExtractor` sentence (correctly; the plain form holds) **and narrowed the bound in the same edit to one that stops at *"trap a"***, converting an emphasis defect into a command defect at the same cell — under a certificate reporting this file at zero command-level defects. Replaced with the **two-stage windowed extraction** R4-2.2 makes law (`grep -nF` to locate → `sed -n 'Np' | cut -c281-409` to print), **cut disclosed**. *A receipt cured by adjusting the instrument instead of re-running it is not a receipt.*
⟨**REPAIR ROUND 5, REST-23 — THE SAME CLASS AT FOUR MORE SITES.** R4-7.1 reported this class cured at six receipt sites and left it live at four others: §2.11 row 46 (`.{0,80}` → `…see FR-GFC-4). |`), row 52 (`.{0,90}` → `…is canonical) |`), row 50 / §4 lock 9 (`.{0,190}` → ``…fourier-series}`. | Both re``) and §2.3 (`.{0,120}` → `…(CRUD/provenance union). Riders: m-13 (`). **Each paste stopped one cell-boundary short of its own command's output, unmarked.** All four bounds are re-cut to `.{0,61}` · `.{0,76}` · `.{0,180}` · `.{0,105}`, each verified this seat to print exactly the span pasted beside it.⟩
⟨**REPAIR ROUND 5, REST-31 — "THE PASTE IS NOT THE COMMAND'S OUTPUT", AT FIVE MORE SITES, CURED IN ONE EDIT.** The class does not close site by site, so all five are restored to what their commands actually print: §2.0's `comm -13` cell (the **`.md` suffix on all five filenames**) · §2.5's Q-1 receipt (the **path prefix** its own `-rn` form prints) · and three `grep -n` receipts that dropped the **`NN:- ` list-marker prefix** — `73:- **m-7 = C-6-as-rescoped…`, `64:- **L·M-4 / D-13 / C-8(a)…`, `54:- **FR-GFC-20 = C-9 — MINOR.**…`. Each re-run at this seat; each paste now byte-equal to its output.⟩
- **R4-7.1 — THE VERBATIM LABEL MEANS BYTES: SIX RECEIPT SITES RE-CUT.** §2.11 row 21's `F.W5's own binding` carried **both** faults of the class row 9 had just been cured for — a `**` **prepended** to a `grep -o` output that begins at `F`, and an **unmarked truncation** — and is re-cut to `.{0,50}` so the output IS the paste, opening mid-emphasis exactly as the instrument prints it. Four further unmarked truncations (`editor-saved contour` `.{0,90}`→`.{0,69}` · `ship the 46th operation` `.{0,70}`→`.{0,57}` · `FR-GFC-1's share` `.{0,60}`→`.{0,49}` · `The abort-key fix` `.{0,60}`→`.{0,46}`) each had their bound narrowed to the clause boundary the cell already stopped at. **No word was changed at any of the six; the instruments were made to produce what was already true.**
- **P4-D7 — THREE MULTI-HIT RECEIPTS GIVEN SINGLE-HIT ANCHORS.** `grep -n "m-7" fr-BasisSelector.md` (3 hits) · `grep -n "innerPoly" fr-FourierShapeExtractor.md` (3) · `grep -n "FR-GIG-5" waves/F-W5.md` (4, **used at two cells for two different quotations**) — each replaced by a `-F` anchor returning exactly one line. This is the shape §2.2 convicts in its own words — *"the quote to BE the output, not a selection from it"* — surviving three rounds inside the file that states it.
- **R4-9.3 / R3-3.10 — THE D9 COUNT RETIRED AND ITS GLOSS CORRECTED.** *"`F-W5.md` has NO clause D9 — `grep '**D9**'` → 0"* was a **banked count of a live sibling**, published with no ⟨cmd⟩ and no rooted operand, and the gloss resting on it — *"'D9' in F.W5 is an owner-ruling label inside R8"* — is **false at F-W5's bytes**: `D9` is a **value.js product ruling with its own rooted block**, Home `docs/tranches/V/DECISIONS.md` §2 row `D9`, which clause R8 must not *contradict*. Replaced by four single-hit `grep -o` receipts, headed by F-W5's own sentence *"(No clause is numbered D9 — see the compatibility note.)"* — **the file saying what the count was standing in for.** The clause-register fact is unchanged: no F-W5 **clause** is numbered D9, and the actor clause is **E18**.
- **R4-6.4 — AA-23's HOST ROW BOOKED AT ITS HOLDER; THIS FILE KEEPS THE KILL, MARKED AS SUCH.** `fr-AdminAuditLog AA-23` is canonically F.W5-held at **MAJOR, repair-shaping**, carrying the live constraint *"the 45/30/13 join must carry this constraint"*. Rounds 1–3 carried its **killed** regex cure while the host row landed nowhere — the exact **R-6.4** correction this file makes correctly for `fr-BasisSelector m-7` one record over (*a killed sub-claim never kills its host row*), unapplied where it bit hardest. §2.5 now marks the citation as a kill-only carry and names the holder; §2.11d H-11 records the row.
- **R4-2 — THE TOOLCHAIN AND THE BASE BLOCK PINNED (§0.2).** `/usr/bin/grep` declared as the pinned binary (bare `grep` is **ugrep** in this shell and silently differs); no `-P`/`\K`/lookaround; no bound above 255 (widest standing: `.{0,190}`); **and `$F` / `$V` — used at §2.4 and §5, outside §3's defining line — now have one published base block covering every variable and every relative base this file consumes.** One command, one base.
- **R4-8.3 — HASH-PINNED CROSS-QUOTES (§0.3).** Eight live siblings are pinned by `sha256[0:12]`, **re-stamped after this seat's final edit**; three declared-immutable operands are pinned too. **The mechanism proved itself inside the round it was adopted**: this seat stamped three times and recorded **eight moves across four files** (`F-W5.md` · `F-W0.md` · `F-W1.md` · `F-W9.md`, all under their own concurrent round-4 repair). **Every quoted word re-verified byte-true at each stamp and every one held; one banked ADDRESS did not** — `grep -nF 'FR-GV-13 (=MISS-LC-2)' F-W5.md` moved `:162`→`:177` — so that cell was re-cut to `grep -oF` and banks no line at all. **The table is this seat's close-time stamp and says so; the settled hashes are the purge seat's under LAW E, and this round is the empirical case for that ordering.** **The round-2b failure mode dies here**: a paste that names no version cannot tell a stale quotation from a true one, and the pin makes movement detectable rather than silent.
- **R4-1.E-3 / P4-D10 — NOT THIS SEAT'S TO CURE, and said so rather than absorbed.** `PASS-3/CLOSE-CERT.md` certifies this file at `111/111 · fixed 0 · no command-level defect` against the four receipt defects above, and the file has not been written since. Under **E-3 the cert body is not edited** and the erratum is appended by the seat that owns that file; **F-W6's only writable surface is F-W6.md**, so the falsification is recorded here and the row is owed there. **This spec cites no certificate as an operand.**

**Preserved intact at round 4** — nothing below was touched by any directive above and nothing in this round changed a severity, a witness, a killed cure, a booking or a routing: the **25 booked identities** of §2.1–§2.10 with their witnesses, cures, locks and sequencing · the **born-RED witness set**, every gate still RED on a witness that reproduces (FW6-G12's 1-of-13, FW6-G15's `order_contours` → 0, FW6-G19's 271-line pin) · the **DO-NOT-REGENERATE TRIPWIRE** and the **M-10 innerPoly anti-cure rider** (only the *receipt* was defective; the prohibition never was) · the `fr-GallerySearchBar` **C-2 WAVE-LOCK** · the **one-cut laws** and the **killed-cure register** · the **R1–R9 owner roster** with R5/R9 restored and FR-USB-23's ⊙ travelling · the **R-1e clause re-keys** · the **§2.11a negative roster**, all seven · the **§2.11b amended discriminator** and its eight-landing table · the **twin hand-off** consumed from `F-W3.md` §X.1-v4 and never re-derived · **F.W7's ∅ posture** · and the **read-only / E-3 / `planned`** discipline. **The 192 / 49 / 23 span arithmetic and the 122 band-form measurement stand as measurements of a frozen corpus — they reproduce — and stand as a roster no longer.**

**Round-3 repair record (L-20, `PASS-3/RULINGS-3.md`, 2026-08-28).** The L-18 base this round met is the third in the family: round 1 met *"a set-difference declared empty without running both directions"*; round 2 met *"a gate argued RED over a witness that does not reproduce"*; round 3 met **"a gate whose operand cannot see the thing it is supposed to count."** Cured, by directive:

- **R3-5.2 — the detector re-cut to FOUR band spellings.** `F.W5-W8` ⊕ `F.W5–W8` ⊕ `F.W5(-W8` ⊕ **bare `F.W5` as the name of the union** in five records that sat outside the 49-record denominator entirely. **Ten identities in six records escaped both operands**; all ten are landed at §2.11c BLOCK A — nine cited on real `F-W5.md` clauses (**F1 · F8 · F8/B1 · D12 · B1 · F7 · B2 · F6 · E20**), one booked, one excluded-with-reason. ~~Roster re-denominated **122 → 132**.~~ *(STRUCK at round 4 under R4-5 — the round-3 record is preserved as history, and this figure is the one thing in it that did not survive.)* The lesson booked with it: *a detector enumerated by tokens the author happens to know reproduces K-6 at every round* — which is why FW6-G17's operand is now **the run**, not an integer.
- **R3-7.1 — `fr-EquationModeToggle FR-EMT-20` CITED at §2.7** ⟨**re-cut at repair round 5 (REST-15).** Round 3 ordered this BOOKED before the canonical existed; **R4-10 postdates and governs**, and the canonical homes the id at **F.W5 with no band leg**. The cure is a one-line conversion, not a substantive error — and **the file convicted itself in its own hand**: §2.11d states *"Every one of the fifteen is **held at F.W5 by the canonical**, so under R4-10's roster law F.W6's duty is to **CITE THE HOLDER BY NAME** — never to book, never to count, and never to route"*, and a sixteenth F.W5-held id was booked four sections above the cell that forbids it⟩, beside PP-DEADSEAM, on `F-W5.md` clause **E10** whose carried-rows cell names it. Simultaneously census escape E-3 and the M-25 leg-drop at the clause this wave executes; **one booking cures both**, and it is the only escape whose terminal disposition is an act this seat performs.
- **R3-8.2 — FW6-G19's born-RED witness takes the canonical form `FR-NP-32 (≡ fr-PaperSidebar M1)`.** The gate's own witness cell was the single site in this file carrying `M1` alone, against R-8's *"everywhere the gate is stated"* and a live `M1` homonym class.
- **R3-9.3 — the §2.11b discriminator squared with the landings, then RUN and PRINTED.** The round-2 test (*does the clause's GREEN-owner cell name F.W6?*) contradicted this file in both directions — it sent booked `FR-AFP-33` to CITATION and seven cited rows to BOOK. **The clause's green owner was never the discriminator; the ROW's own terminal disposition is.** Amended, re-run over the eight, printed: **seven confirmations, one re-landing** (`GCM-10` → branch 2, a named fold at the booked `FR-AFP-36 ⊕ GCM-55` row, which is what its own cell already implied). Branch 4 gains **conditional promotion** so an ⊙ ruling that would convert a clause decision into a server act is declared, never presumed.
- **PASS-3 D-7 — the upper operand ENUMERATED.** *"Rows 57–122 … are not restated here"* is **STRUCK**; §2.11c BLOCKS B and C print every remaining identity by id with its branch and landing section, under a stated counting rule (**a named fold is transcribed at its carrier and is not a separate roster member** — which is why the transcript is a superset of 132 and why the *transcript*, not an integer, is the operand). R3-4's standing law is honoured in the same cell: the PASS-1/PASS-2 check-file censuses are struck as cross-check operands and named only as prior runs.
- **R3-3.9 purge share — Q-1..Q-7, all seven.** **Q-1** the intake's *"strengthens the census routing, no new work"* re-sourced to `INTAKE-ADJUDICATION-2026-08-03.md:197` with its invented bold removed (`X-8` names a ROW, not the file the words live in) · **Q-2** the actor clause re-quoted at F-W5 **E18** — *"An audit row's actor is a FIELD…"*, capital **An**, shouted **FIELD**, no period inside the emphasis · **Q-3** G3's close re-bolded on `deepen-or-retire` with its receipt · **Q-4** the **frozen** `fr-FourierShapeExtractor` innerPoly sentence restored to plain, the added bold struck · **Q-5** the D7 rider's `**F.W5–W8 rider**` bold restored and the prepended `the ` removed so the command produces its own paste · **Q-6** the §4 F.W0 erratum's stale FACT retired for a CLASSIFICATION · **Q-7** the §2.11a *byte-for-byte* label made true by completing `fr-CollapsibleSection:131` to its clause boundary. **Four of the seven were emphasis or truncation inside quotation marks and three carried no ⟨cmd⟩ at all — the same seven-for-seven correlation round 2b measured, holding at a third round.**
- **R3-3.10, the standing amendment, applied here**: no ⟨cmd⟩ banked in this file takes a **COUNT of a live sibling or of this file itself** as its output. Sibling receipts state classification, set-membership or `grep -o` word-output; counts survive only over the **frozen** `fr-*.md` corpus, which cannot move. Four of round 3's seven program-wide fabrications were banked counts of moving files; the form is retired here rather than the instances patched.
- **The twin hand-off CONSUMED, never re-derived (R3-1a).** `F-W3.md` **§X.1-v4** published first this round and is cited where this wave's rows touch the twin surface — escape **E-1**'s two subject files (`ContourEditorCanvas.vue` on the F.W4 default arm per item 2; `VisualizationView.vue` ∈ `.e` per item 5) and the §5 exclusion that rests on them. **F.W6 books nothing on the twin surface and asserts no home the partition has not already assigned.**

**Preserved intact, as the PASS-3 seat required and §7 of its check directs**: the 192/49 span arithmetic and the 23-boilerplate / five-boilerplate-only split · the R-1e clause re-keys and the retirement of the invented `D9` · the R1–R9 owner roster · the born-RED witness set, every witness re-run against the live fourier tree · FW6-G12's 1-of-13 re-measure · the DO-NOT-REGENERATE TRIPWIRE with its BLOCKER-revival clause · the killed-cure register (AA-23 · β's 428-escalation · K9's five fallbacks · SE-05 · m-7's straddle only · D-1's skeleton · K12 · FR-GV-24 · K-1 · K-6/S-8) · the one-cut laws · the M-10 innerPoly anti-cure rider · the `fr-GallerySearchBar` C-2 WAVE-LOCK · the TWELVE-limb citation at its stable anchor · the negative roster · F.W7's ∅ posture · and the read-only / E-3 / `planned` discipline. **Nothing in this round changed a severity, a witness, a killed cure or a routing that the rulings did not name.**

**Close act**: when FW6-G1..G19 are GREEN (G15 closing only by naming the true source or re-authoring the artifact with diagnostics — never by regenerating), F.W6 stamps its burn register, lands the routing receipt, relays the commissioned-ask letter, and ledgers it in INBOX (E13 — no wave closes with UNREAD mail). **L-18 applies**: no wave is ACCEPTED until two quartet gestalt passes adjudicated by a fresh Fable instance; the bases that apply here — a burn credited to a wave that did not perform it; a "cure" that is really a client arm; a gate argued green over an unruled owner question; a redaction that leaves the asset leaf open; a projection strike that was a compression decision; a set-difference declared empty without running both directions. **Round-1 repair record (L-20, 2026-08-28)**: the last of those bases was MET at the first pass and is now cured — the span detector is spelling-agnostic (R-6), the 56 escapes are homed at §2.11, the negative roster bounds the difference from below at §2.11a, the derivation law at §2.11b makes the cut checkable, six F.W5 clause labels are re-keyed and the invented "D9" retired (R-1e), the owner roster is complete at R1–R9 with R5 and R9 restored and FR-USB-23's ⊙ kept, the phantom carry receipt is struck and FW6-G17 re-based on operands that exist (R-3), F.W1's transaction is cited whole rather than restated (R-4b), the eight colliding tokens are record-qualified (R-5), and every bounds path is rooted and `ls`-resolved (R-10).

**Round-2 repair record (L-20, `PASS-2/RULINGS-2.md`, 2026-08-28)** — the L-18 base *"a gate argued green over"* had a twin this file met at round 1: **a gate argued RED over a witness that does not reproduce**. Cured, with the rest of the round's directives: **R2-1a** — the phantom `F.W5 §12` and its invented sentence *"the burn-down needs the chain clause"* are STRUCK at both sites (§2.1 and the §4 F.W5 edge), the dependency restated over F-W5 §4's real `F.W5 → F.W6` edge row; **R2-1b** — the five `F.W5 §6c` citations are replaced by their two true homes (clause **E2 ▲** / gate **G3**, quoted separately and never fused) and the value-side obligation operand is split into the two authorities that actually hold it, with **V-α's non-membership in F-W5 §4's list stated as a correction of record** rather than papered over; **R2-1-LAW** — every cross-spec and corpus quotation in this file now enters as pasted `grep`/`sed` output with a **⟨cmd⟩** note, and a *"verbatim"* label without one is a defect per se; **R2-2** — `census §4.7`→**§4, item 7**, `COHESION §1`→**§2** (three sites), `F-W5 §7`→clause **E6**, `F.W5 §1b`→**§0b**, the refuted `lane-fourier-r3-r6.md` homonym STRUCK, and every line cite into a live sibling (`F-W1.md:276`/`:294`, `F-W0.md:199`/`:204`) converted to stable anchors — **F-W1 §4 step 4**, **F-W0 §4 G-11 / G-12** by gate id; **R2-5** — the roster is re-quoted by ⟨cmd⟩ as **TWELVE**, not eleven, from the corrected charter, and this spec still names no limb; **R2-9** — FW6-G17 states its **shape- and spelling-agnostic detector INLINE**, so bullet rows and prose routings can never again be invisible to it; **P2-9 cluster** — the boilerplate arithmetic is re-measured **3 → 23** (with the five boilerplate-only records named), §2.11b's derivation law is re-authored so its branches run in order over all 122, the `fr-FourierShapeExtractor` **M-10 innerPoly** anti-cure rider and the **FR-GFC-20** `F.W5-W8` server rider are restored where their rows land, and the `fr-GalleryCard L·M-4 / D-13 / C-8(a)` D17 fold is named; plus the **FW6-G12** witness re-measured at the bytes (`response_model` in `admin.py` → **1 of 13**, not 0 — the RED intact and sharper), the **FW6-G2** owner cell's E8→**E7** mis-key cured, the `§G G1c–G9c` range corrected to **G1c–G10c**, and **FR-GV-13's server arm** homed at §2.11 row 56a as a citation to F.W5 **D10**. Preserved intact, as the PASS-2 seat required: the whole R-6 cure, the R-1e clause re-keys, the R1–R9 owner roster, the verified born-RED witness set, the DO-NOT-REGENERATE TRIPWIRE with its BLOCKER-revival clause, the killed-cure register, the one-cut laws, and the read-only / E-3 / status discipline.

**Round-2b — the QUOTE-BY-COMMAND sweep (same round, this seat, 2026-08-28).** R2-1-LAW binds *every* seat and is *standing*, so this seat re-ran **every** ⟨cmd⟩ in this file against the live bytes rather than trusting the round-2 write — the necessary discipline, because F-W5, F-W0, F-W1 and F-W9 were all re-written by their own repair seats **after** this file was authored, and round 1's whole failure mode was ten seats staling one another's citations. **Result, measured, not asserted** ⟨**Round-3 strike-within-the-strike (R3-3.9 item 8 ⊕ R3-3.10, this seat).** Round 2b banked *"⟨cmd⟩ `git show HEAD:…/F-W6.md | grep -o '⟨cmd⟩' | wc -l` → **80** receipts standing at round-2 close — a **stable** figure, pinned to a commit."* **It was never pinned to a commit: `HEAD` is a moving reference**, and the round-2 repair commit landed on top of it, so the figure stopped reproducing the moment the round it measured was committed. This seat re-ran it and got a different integer. **The receipt is STRUCK, not re-pasted** — re-pasting would bank a second count of a moving reference, which is exactly what R3-3.10 retires — and the sentence it warranted survives as a **classification**: *at the round-2b close, four standing receipts failed to reproduce and seven quotations carried no ⟨cmd⟩ at all.* The proportions below are the finding; the denominators were never the point, and a denominator that cannot be re-derived is not evidence. **The lesson generalises past this cell: "pinned to a commit" means a HASH, and this file wrote `HEAD` and called it pinned.**⟩: **the great majority reproduce unchanged; FOUR do not** (the round-2b denominator is struck above as a count of a moving reference — the four failures are the finding, and each is named at its row). And the sharper half of the finding — **seven further quotations carried NO ⟨cmd⟩ note at all**, which R2-1-LAW clause 2 makes a defect per se before anyone reads the words; **all seven were also wrong at the bytes.** That correlation is the round's real lesson: **the unreceipted quotations were wrong at seven-for-seven, the receipted ones at four, out of a standing set an order of magnitude larger.** The receipt is not paperwork over a quotation — writing one is what makes a seat look, and every quotation this file inherited without one had drifted. **Eleven quotation cures below, plus the §4 quarantine marker = twelve edits.** Stated exactly, because the distinction matters to what gets fixed: **seven of the eleven were attributed to `F-W5.md` and appear there at NO commit** — ⟨cmd⟩ `git show HEAD:docs/tranches/X/fourier/waves/F-W5.md > /tmp/w5head.md`, then each failing string re-grepped against both that file and the live one, returns **0** on both, so **none of these is F-W5's repair seat staling them under this file — they were wrong when written and wrong at every commit since.** They were typed from memory in round 1 and carried through round 2 by a seat that checked the *addresses* it was ordered to fix and not the *words* beside them. **Four of those seven are not inventions at all but MIS-ADDRESSINGS** — real sentences living in the frozen corpus (class i below); one is a truncation of a true F-W5 substring with the record prefix stripped (row 41); and the remaining four failures are emphasis or command defects over quotations whose words were already right. **No cure in this sweep rests on a claim that a sentence exists nowhere — each names the file the words DO live in, or says plainly that it is emphasis the bank does not carry.** The classes, each cured at its row: **(i) right words, wrong file — five instances**, all of one shape, and the shape is diagnostic: the sentence quoted was the **adjudicated record's own routing cell** — the very bytes that admit the row to the span — while the attribution named the **F-W5 clause the row lands at** (`fr-SliderControl` R-16's rider at row 10 · `fr-GalleryFeaturedCarousel` FR-GFC-1's list-contract sentence at row 46, quoted minus its final word *"params"* · `fr-AppHeader` FR-AH-6's admin-predicate rider at row 52, where F.W5 states the same law in a different case and the two are now quoted separately per R2-1-LAW clause 4 · and the census/lane-crud pair already cured at round 2). A landing cell is where a record's words and a clause's words sit closest together, which is exactly where an attribution slips; **the fix is per-row and the lesson is per-idiom — a landing cites TWO authorities and says which is speaking.** **(ii) anti-rename inside quotation marks — two instances**: `fr-FunctionInput L-B1` quoted as *"FunctionInput's L-B1"* (row 39) and `fr-FunctionInput C-8` quoted bare (row 41). A record prefix is address, not decoration; stripping it inside a quotation is the identity collapse R-5's qualification law and FW6-G17's set-difference exist to prevent, committed by the file that declares that law at §0. **(iii) emphasis added inside quotation marks — three instances**: `**on their side**` over the census's plain bytes (§2.1), `**shared.py:69**` over `fr-EasingPicker`'s (row 19), and B1's compose-lock down-cased and de-bolded (row 49) — the identical drift this file convicts at row 9, re-committed by its own cure. **(iv) commands that could not produce the text pasted beside them — two**: `sed -n '71p'` for a sentence wrapping to `:72`, and a `grep` returning two hits with one pasted (§2.2). **(v) a quotation staled against a live sibling while being authored**: F-W5's D17 cell was expanded to the FULL banked `fr-GalleryCard` spelling by its own seat in this same round (§2.4). Plus the **quarantine marker** on the retracted `§12` sentence, so the strike is auditable without the invention being re-mintable. **The standing lesson, stated so the next round does not need to relearn it: a repair round that fixes addresses without re-reading the words at them converts fabricated quotations into *well-addressed* fabricated quotations.** Nothing in this sweep changed a finding, a booking, a gate operand, a severity or a routing — **every edit is a receipt conformed to bytes that were already there**, which is the only kind of edit R2-2.3 permits after the content repairs land. Until the begin-word: **planned**, born-RED, every witness named above.
