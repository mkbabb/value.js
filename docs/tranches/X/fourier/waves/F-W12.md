# F.W12 — F.W11's tail spent (numeric fields on the producer's NumberField · e2e readiness · the residual rows)

## State
**Opens after**: F.W11 CLOSED (CONFORMANT, fourier `41e49af`). **Minted by**: COHESION §0as (2026-09-22).
**Agents**: 2 units — [`.a`] → [`.b`]. **Status**: planned. **Record**: `docs/tranches/X/execution/C/F-W12.md`.
**Tree**: `/Users/mkbabb/Programming/fourier-analysis`, `m/w1-bump-migration`, baseline `41e49af`.

## Goal criterion
No functional regression survives F.W11's Input move: every numeric field steps by keyboard again,
on the producer's `NumberField`; the keystone suite exercises `:290` in a full run; every stale row
that describes retired chrome is retired with it.

## §1 Bounds
`web/src/components/morph/HarmonicLevelGrid.vue` · `web/src/components/morph/MorphPhaseConfig.vue` ·
`web/src/components/visualization/BasisSelector.vue` (`:164` `:190`) · `web/e2e/contrast-pairs.ts`
(the two `.level-input` rows) · `web/src/style.css` (`:307` `:323` prose only) ·
`web/e2e/visualization-ux.spec.ts:33` + `web/e2e/resolve-stack.ts:102` (the readiness signal) ·
`web/e2e/screenshots/f-w12/**` · value.js `execution/C/F-W12.md` · `LEDGER.md` · `INBOX.md` (mail).
**Do NOT touch**: `glass-ui/**` · every other spec · `package.json`.

## §2 Agent Units
### F.W12.a (Opus)
1. **R-e-1** — the numeric fields (`.level-input` ×2, `.num-input`, BasisSelector's two `type="number"`)
   move from `Input` (text-shaped) onto glass-ui's **`NumberField`** (`glass-ui/src/components/number-field`;
   read its README/types whole): min/max/step preserved, ArrowUp/ArrowDown step again, the adjacent
   Slider stays. Never a local number input styled to look like the producer's.
2. **R-e-2** — `contrast-pairs.ts:109` / `:146` (`HLG-37`, `FMD-19`) retired with the chrome they
   described; a dated line beside says why. **Prose**: `style.css:307/:323` cite `AppHeader.vue:174-177`,
   retired at F.W11.c — re-pointed to `AppDock.vue`.
3. **R-e-3** — the browser's search-cancel glyph on `GallerySearchBar` / `AdminUserList` `type="search"`:
   a producer observation; ONE BK relay row (mail), no consumer hack.
4. **e2e readiness (check-2 MINOR)** — `openWorkspace` (`visualization-ux.spec.ts:33`) and
   `resolve-stack.ts:102` wait on `networkidle`, which is load-sensitive; replace with the app's own
   readiness signal (the workspace's mounted landmark / the compute response the page actually awaits),
   never a longer timeout or a retry loop. `:290` must run inside a full 9-worker run.
### F.W12.b (Opus, verify-only close)
Gates ×2, bounds, E13, the LEDGER row, push.

## §3 Gates (born-RED)
- **G-F12-1** the five numeric sites import `NumberField` from `@mkbabb/glass-ui` (5/5 ×2); a Playwright
  probe: ArrowUp on each field increments by its step (5/5 ×2), screenshots committed.
- **G-F12-2** `grep -c 'level-input' web/e2e/contrast-pairs.ts` → **0**; `grep -c 'AppHeader.vue' web/src/style.css` → **0**.
- **G-F12-3** `grep -c networkidle web/e2e/visualization-ux.spec.ts web/e2e/resolve-stack.ts` → **0**;
  two full 9-worker runs: `:290` EXECUTED and green in both; no failure absent at `41e49af`.
- **G-F12-4** `npm run build` exit 0; E13 0 UNREAD; the R-e-3 relay row present.

## §4 Close act
G-F12-1..4 GREEN → CLOSED; a producer-owned RED → CLOSED honest-RED with its relay id.

---

## ADDENDUM 2026-09-22 (dated, beside — E-3; COHESION §0au) — unit `.a2` and the close `.b2`
**`F.W12.a2`** (Opus) — writable: `web/e2e/visualization-ux.spec.ts` (the `.serial` block and any per-test bootstrap it needs) · `web/e2e/screenshots/f-w12/**` · the record. Acts: prove independence (each test of the block alone from a fresh page, ×2, receipts); give any dependent test its own bootstrap; remove `test.describe.serial`; two full nine-worker runs. Gate **G-F12-3 (full-run leg)**: `:322` (the spec's `:290`) EXECUTED and green in both runs; no failure absent at `41e49af`; `:109` counted under (c) only if it passes alone ×2. **`F.W12.b2`** (Opus, verify-only close): G-F12-1..4 ×2, bounds, E13, the LEDGER row, push.
