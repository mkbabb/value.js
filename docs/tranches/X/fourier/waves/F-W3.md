# X.F.W3 - Shadow Retirement and Glass Suffusion (frontend half)

**Sub-tranche**: X·F (`docs/tranches/X/fourier/waves/`), authority `docs/tranches/X/COHESION.md` §0a/§2 · the `fr-*` adjudicated registry (`docs/tranches/V/megatranche/registry/adjudicated/`, 66/66 banked at `35fc8ebf`) · `INTAKE-ADJUDICATION-2026-08-03.md`. **Alias law (R-B idiom)**: `F.W3` denotes `X.F.W3`; a search alias, never a rename.

**Law (verbatim, binding)**: **M-25** — this spec agglomerates the adjudicated registry (dedupe by identity, cure-shape locks and sequencing edicts carried, routed by mechanism); a spec ignoring adjudicated rows is DEFECTIVE and transcription-only is DEFECTIVE. **L-20** — validated against `docs/precepts/instructions/tranche/WAVE_SPEC.md` (latest at authoring, 2026-08-28). **L-19** — no gate exists without a real named witness; every gate below cites measured seat evidence or a literal command, never narrative. **Born-RED** — every gate starts RED with its witness named. **K-20 corollary (binding)**: every a11y argument in execution re-derives from `axe.js`, never from challenge text.

**EXECUTION IS NOT AUTHORIZED BY THIS FILE.** Nothing opens product source until the owner's begin-word. Status fields stay `planned`. `/Users/mkbabb/Programming/fourier-analysis` is READ-ONLY until then.

## §0 Fold note (M-23 fresh-Fable agglomeration; twice-authored)

Two drafts, agglomerated choosily. **Structure**: draft A's front (bounds table, mechanism-clustered carry §C.A–§C.K, gates g1–g18) — its clusters ARE the routing law M-25 demands; draft B's back (cross-edges, agent units, disjointness, worktree plan, excluded table, close) — draft A had none. **Dropped from B**: the `C-01..C-90` alias layer over the carry rows — banked ids are the identity (anti-rename law); an alias layer is a second rename surface, so units and gates below cite banked ids and cluster letters directly. Also dropped: B's unverifiable "26 corrections / 15 additions" bounds tally (the reconciled table in §1 is the record, not the arithmetic). **Kept from B**: g19/g20 (closure and register gates, both with real witnesses), the six-unit dispatch with the three shared-glob splits stated at authoring, the excluded/struck/dissent tables. **Grafts into A's rows**: none needed — A is the tighter fold of the same authority; B's row content is the same rows under C-numbers. Dissent registers union'd; no dissent dropped.

**▲ PROVENANCE DEMOTION (repair round 1, ruling R-3 — binding).** The "two drafts / **90 rows** / `C-01..C-90`" lineage recorded above is a **HISTORICAL SESSION NOTE with NO GATE WEIGHT**: those drafts are session artefacts, never in-tree bytes, and no gate below may take them as an operand. **`carry/F-W1-CARRY.md` and `carry/F-W4-CARRY.md` are the ONLY carries in the tree** (⟨cmd⟩ `ls docs/tranches/X/fourier/carry/` → `F-W1-CARRY.md` `F-W4-CARRY.md`, this seat, 2026-08-28) — **F.W3 authors none** (R-3 item 2), and "the CARRY" with no path is forbidden vocabulary in this file.

**▲▲ THE ROSTER IS RE-BASED SHAPE-AGNOSTIC (repair round 2, rulings R2-3a + R2-9 — binding; PASS-2 D-1 convicted the prior operand).** Round 1's roster was the PASS-1 census — *317 F.W3-routed registry rows across 37 records* — and that measurement is **real, reproduced, and too narrow**: it measured ONE row shape. ⟨cmd⟩ this seat, `census.py` over the 66 `fr-*.md`, both detectors, 2026-08-28:

```
W3-axis table rows  : 317   records 37     <- reproduces PASS-1 EXACTLY
W3-axis bullet rows : 681   records 37     (id-headed, strict atomiser)
W3-axis bullet rows : 764   records 43     (bold-headed, loose atomiser)
dual F.W3/W4 rows   : 903        sole F.W3 rows : 151
```

**R2-3a's ROW-SHAPE DECREE governs**: a *row* is a markdown table row whose terminal cell routes, **or** an id-headed bullet routed in context (`→` · `**ADJUDICATED →**` · `⇒` · `rides` · `routed` · bold-at-disposition), **or** a prose routing walked back to its enclosing row id (S-8). The route-marked bullets ARE rows — it is how ~30 records carry their defect rosters — and a closure gate blind to them is measured over a third of its subject. **The ruled operand is the shape-agnostic roster: 1,008 rows / 62 records (317 table ⊕ 691 bullet)**, re-denominated through §X, §X.1 and §Z, with **844** as the dual-`F.W3/W4` denominator (R2-3a).

**THE OPERAND IS THE DETECTOR, NEVER A FROZEN INTEGER.** This seat's re-run brackets the ruled figure rather than landing on it (681 ≤ 691 ≤ 764 on the bullet arm), and the whole of that spread is **id-atomisation**, not shape: `L·D-1` (U+00B7) atomises to `L-D-1` and then greps nowhere; `MISS-DU1` and `MISS-DU-1` are different strings. **Atomisation is the third blindness axis, after row shape (this ruling) and dash spelling (R-6)** — so g19 states its detector INLINE (shapes ⊕ both dash spellings U+002D/U+2013 ⊕ the slash-form expansion ⊕ the atomisation rule) and runs it at close; **1,008 is the floor the gate may not report below**, not the number it reports. The **corpus itself** (`docs/tranches/V/megatranche/registry/adjudicated/`, 66 records) is the operand of record; the two conformance check files are its measurements, not its substitute.

**▲▲▲ A THIRD DETECTOR, WRITTEN FROM THE DECREE ALONE (repair round 2, second repair seat — R2-9's "the gate, not a later seat, catches the next format").** The operand was re-derived once more this round by a detector authored from R2-3a item 1 alone, without reading either prior script, so the ruled figure is bracketed by independent runs rather than by one seat's arithmetic — ⟨cmd⟩ this seat, 2026-08-28, `python3 /private/tmp/claude-504/-Users-mkbabb-Programming-value-js/6614e90c-8bd6-434f-b017-5ad4277c6e5e/scratchpad/w3repair/census.py` (66-file walk; table arm = terminal-cell routing; bullet arm = id-headed bullet ⊕ route context; both dash spellings):

```
table rows  317   records 37     <- reproduces PASS-1 and round 1 EXACTLY, a third time
bullet rows 742   records 42
total      1059   records 65
dual        885        sole 174
```

**The reading, stated so it cannot be laundered**: the table arm is **317 in every run ever taken** — that arm is reproducible. The bullet arm has now returned **681 · 691 · 742 · 764** from four detectors, and every total has cleared the ruled floor (1,059 ≥ 1,008). That is exactly why R2-3a's 1,008 is carried here as a **FLOOR** and never as an integer to copy: a run reporting below it has mis-stated its detector (g19's FAIL condition), and a run reporting above it has found more of the same subject, not a different one.

**THE ALIAS AXIS — the atomisation spread has a NAMED cause, and it is a gate-design defect if the detector does not carry it (this seat's find, round 2).** The corpus writes bullet heads in the form `**<banked id> = <reading-axis alias> — GRADE.**` — ⟨cmd⟩ this seat, 2026-08-28, `grep -n 'DU-missed-2' fr-CoefficientsPanel.md` → `71:- **FR-CP-36 = DU-missed-2 — MINOR (demoted per R-8).** …`; ⟨cmd⟩ `grep -n 'D-24-as-corrected' fr-CoefficientsPanel.md` → `42:- **FR-CP-10 = D-24-as-corrected — MAJOR (R-3).** …`; ⟨cmd⟩ `grep -n 'r1-missed-2' fr-GalleryDraftsSection.md` → `82:- **m-17 = r1-missed-2 (re-proven) — MINOR (r1's MAJOR preserved as dissent).** …`; ⟨cmd⟩ `grep -n 'C-C-2' fr-GalleryDraftsSection.md` → `62:- **M-10 = C-C-2 (regraded from BLOCKER, K-12) — MAJOR.** …`. **The token to the RIGHT of `=` is the reading axis's own label; the banked id is the LEFT token** — the anti-rename law read forward: an alias is carried for life, never booked, and never a hole in a closure roster. An atomiser that tests both sides manufactures escapes no bank ever minted: ⟨cmd⟩ this seat, `escapes.py` (both sides) vs `escapes_left.py` (left-token rule), same roster, same boundary-exact test → zero-homed id-instances **254 → 172**, with whole phantom families gone (`fr-GalleryDraftsSection`'s eighteen `C-C-*` → **0**; `fr-ContourSettings` 11 → 1; `fr-CoefficientsPanel` 41 → 17; `fr-CoefficientsSpectrum` 9 → 0). The same shape recurs on `·`/`/`-joined heads — ⟨cmd⟩ `grep -n 'D-m10' fr-DarkModeToggle.md` → `96:| **N-10 · D-m9/D-m10** | The 5rem `--toggle-size` default has never rendered …`, whose banked head **`N-10` is already booked at §C.H** — so **172 is itself an upper bound, not a residue count**. g19 carries the rule INLINE; without it the set-difference is non-empty by construction and the gate can never close — F-W8's G15 runnability lesson (R2-7.2) in this wave's own idiom.

**Anchor idiom (R2-2, binding from here on)**: cross-spec receipts in this file cite **stable anchors** — §-headings, gate ids, clause ids, banked row ids, row labels — **never line numbers into a live sibling**. Line numbers survive only for the frozen corpus (`fr-*.md`, immutable), for files cited with a commit hash, and for this file's own intra-file coordinates marked *at this writing*.

## State

**Name**: W3 - Shadow Retirement and Glass Suffusion (frontend half)
**Opens after**: F.W0 (substrate: the GAB-13 dirty-set settle, the **`FR-NP-32` (≡ `fr-PaperSidebar M1`)** corrupt-dist settle — two banked ids, one fact, cited together for life (R-8; OP-5), F8-REACH-01/02 ruled in one breath, the CanvasOverlayButton `git rm`) and F.W1 (the atomic tri-package uplift; COHESION §2: "F.W1 tri-package uplift precedes F.W3/F.W4") — **except the §C.H rows expressly available at the installed pin, which need F.W0 only** (K-7/K-12 refuted their version framing).

**▲▲ THE UPLIFT'S EXTENT IS STATED ONCE, BY CITATION, AND NOWHERE ELSE (repair round 2, ruling R2-5; PASS-2 D-10).** "Tri-package" names the three producers, never the transaction's size: the transaction is **the TWELVE-limb roster chartered at F-W1 §4 (Sequencing), step 4 — "The atomic transaction (ONE change, G6)"**, and F.W3 quotes that charter rather than re-deriving it. ⟨cmd⟩ this seat, 2026-08-28, `grep -ho 'The roster is TWELVE limbs[^*]*' F-W1.md`:

> The roster is TWELVE limbs and stays twelve — the eleven chartered plus the FR-EQC-7 vaul-vue manifest gate returned INSIDE at repair round 1 per the `:294` pin (R-4a, cross-edge 1 at this writing) — it is the extent every sibling spec cites (R-4b); nothing is added to it by a later reading.

**F.W3 adds nothing to it and subtracts nothing from it**: the tri-package prose above stands as colour, this sentence is the only statement of extent in this file, and no F.W3 cell may count the limbs for itself (R-4b). Every §C.H row that says "no F.W1 dependency" is a claim about THIS roster's contents, testable against it.
**Agents**: 6 — 5 parallel Opus implementation seats (`.a`–`.e`, file-disjoint per §5a) + 1 serial fresh-Fable adjudicator (`.f`) — M-12 tri-fold; receipts every seat.
**Hard gate**: 20 born-RED gates g1–g20 (§3), each naming its measured witness or literal command.
**Status**: `planned`

### Four-verb status (this file moves exactly one)

| verb | value | evidence |
|---|---|---|
| AUDITED | **YES** | the `fr-*` adjudicated registry (66/66) · fourier census + `lane-frontend.md` · `INTAKE-ADJUDICATION-2026-08-03.md` |
| SPECIFIED | **YES — by this file, 2026-08-28** | validated against `WAVE_SPEC.md`; CARRY authority folded whole (§Z tally) |
| IMPLEMENTED | **NO** | gates green + bytes landed stamps this; nothing opens product source until the begin-word |
| VERIFIED | **NO** | stamped only at the sub-tranche release close; no wave stamps VERIFIED at its own close |

### Goal criterion

This wave succeeds if, when work ends, **no fourier frontend surface hand-rolls a component the installed producer ships, no scrub or search surface carries a defect its own prescribed cure would arm, one vocabulary names the active state, one law governs every wrapper, and every producer-owned defect is on the SS-6 letter instead of patched locally** — with every do-not-execute cure recorded, never silently dropped.

### Open preconditions (checked at wave-open; NOT close gates)

| # | precondition | state at authoring | check at open |
|---|---|---|---|
| OP-1 | **Owner begin-word for X·F execution.** | STANDING GATE — no begin-word. | The owner's dated word, cited by commit body. No inference from this file. |
| OP-2 | **F.W0 substrate settled** (g18's witness set). | RED: 28 dirty rows incl. `web/package.json`; glass-ui 4.0.0 `dist/styles/index.css` syntactically corrupt. | `git status --porcelain` clean-or-recorded-baseline; dist parses; both REACH rows ruled. |
| OP-3 | **F.W1 landed** — or the unit is expressly pin-scoped (§C.H). | F.W1 spec authored (`carry/F-W1-CARRY.md`); not executed. | F.W1 close commit named; g6's probe verdict available to `.a`. |
| OP-4 | **The owner rulings `.b` and `.d` consume**: MISSED-D (easing route), P-10 (re-pin target), i-3 (default-collapsed IA). | OWED — flagged inline at their rows, never presumed. | Each ruling dated and cited before its consuming unit dispatches. |
| OP-5 | **`FR-NP-32` (≡ `fr-PaperSidebar M1`) — THE CORRUPT-DIST SEQUENCING GATE** (banked `fr-NotationPills.md:35`, **BLOCKER**, *"NO-WAVE-OWNER (producer dist emitter) + glass-ui BH relay at the TOP of the FR-COB-28 packet; F.W1 SEQUENCING GATE"*). Two banked ids, ONE fact: the adopted `@mkbabb/glass-ui@4.0.0` `dist/styles/index.css` is a CSS syntax error (the emitter injected the fold-block at a PROSE `@source` mention, `:195-203`, leaving `:203-221` un-commented prose whose apostrophes open an unterminated string), reproduced with the app's own toolchain — `CssSyntaxError: Unterminated string: 's own'` — so **`vite dev` and `vite build` are RED at the `^4.0.0` pin** and *"F.W1 opens on a red build."* Anti-rename: cite BOTH ids, substitute neither (R-8). | **RED** — unsettled at authoring; F.W0 G-4 owns the clearance, the relay ask is the emitter fix + a producer gate that parses its own published `exports["./styles"]` (**not** a version bump — no v8 dist exists to read). | The clearance receipt named by F.W0 (`SUBSTRATE-LEDGER.md`), OR the recorded standing-RED baseline against which §7's per-batch build cadence reads (§7's disclosure sentence). No F.W3 build gate may be reported GREEN or RED without stating which side of FR-NP-32 it was taken on. |

**Scope in one line**: GlassTimeline/ConvergenceTimeline → one composition; EasingPicker/EasingCurvePreview → the ruled easing disposition (no fourth fork); the PaperSearchModal fork-retirement/colocation cluster; shadow retirement at the installed pin; the cartoon-card and `--viz-amber` forks relayed to the glass BH inbox; the five-spelling active-state consolidation; the wrapper law; the standing P-11 clause with its four named anti-cures.

---

## §1 Bounds

All paths relative to the fourier-analysis worktree (`web/src/…` unless noted). Access is `modify` unless noted. Cluster letters key to §2; unit letters to §5.

**▲ BOUNDS RE-DERIVED FROM THE LIVE TREE (repair round 1, ruling R-10.2 — binding).** Every path below `ls`/`find`-resolves at `/Users/mkbabb/Programming/fourier-analysis` **as spelled**, re-measured read-only by this seat 2026-08-28 (`find web/src -name '<file>'`); the twenty phantom spellings the PASS-1 check convicted (`F-W3-CHECK.md` D-4) are corrected in place, and the corpus's own corrections are honoured.

**▲▲ THE BOUNDS RECEIPT IS RE-KEYED TO THE LINE THAT HOLDS THE WORDS (repair round 2, PASS-2 D-6).** Round 1 attributed a matched PAIR of corpus corrections to two different lines — `:23` and `:9` — and only one was right. Both live on ONE line, re-measured this round; the corpus is FROZEN, so a line cite into it is permitted (R2-2 item 2(i)) and is here made true rather than dropped. ⟨cmd⟩ this seat, 2026-08-28, `grep -n 'live at' fr-NotationPills.md`:

```
9:**This seat's own instruments … EquationPanel.vue (live at `components/visualization/`) imports NotationPills :9, mounts :96; … `--pill-c` verified at gallery/GallerySearchBar.vue:121/:214-221 + GalleryCard.vue:121/:271-273 (anchors EXACT) + GalleryCardModal (4 occurrences), live path `components/visualization/gallery/` …
```

— one line, both fragments, in the same instrument run (elision marked by `…`, never addition — R2-1-LAW 4). **The struck attribution**: `fr-NotationPills.md:23` is the record's FR-NP-10 source-order-tie ruling row (⟨cmd⟩ `sed -n '23p' fr-NotationPills.md` → `| 1 | **Is FR-NP-10's source-order tie statically decidable?** | YES — entry-vs-async-chunk emission order (M3) | …`) and contains **neither** `EquationPanel` **nor** `live at`. The `:23` cite is therefore STRUCK at both of its sites (here and §5a), not re-pointed at a guess — and the corroborating correction row the record itself carries is cited beside it: ⟨cmd⟩ `sed -n '124p' fr-NotationPills.md` → *"**Reader 2's path cites drift**: gallery files live at `components/visualization/gallery/` (my find), and EquationPanel at `components/visualization/` — line anchors exact, directory prose off. The K-21 habit, minor key."* The paths this block asserts were always right; the receipt for them was half wrong, inside the very block that proves R-10.2. Three consequences are stated, not hedged: **(1)** there is **no `components/gallery/` and no `components/admin/`** — both sets live under `components/visualization/gallery/`; the old "spelling follows the tree at execution" hedge is WITHDRAWN, because §5a's disjointness proof is a directory partition and a hedge cannot partition anything. **(2)** the repo-root `index.html` **does not exist** — the one real file is **`web/index.html`** (`ls index.html` → No such file; `ls web/index.html` → present), and every cite in this spec (§1, §5e, §Z) now names that single file. **(3)** bare `types.ts` is disambiguated: the I-cluster's `GalleryTier`/sort-canon work is **`lib/types.ts`** (`GalleryTier` declared at `lib/types.ts:96`, this seat's read) — the two homonyms `lib/equation/types.ts` and `components/visualization/lib/canvas-drawing/types.ts` are **not** in bounds. A bounds row whose path fails to resolve at wave-open is a §6 triumvirate event, never a silent re-spelling.

| Path | Access / cluster |
|---|---|
| `components/visualization/GlassTimeline.vue` | re-derive (fork dies) — A |
| `components/equation/convergence/ConvergenceTimeline.vue` | re-derive (fork dies) — A |
| `components/visualization/AnimationControls.vue` | A, G |
| `components/visualization/EasingPicker.vue` | C |
| `components/visualization/EasingCurvePreview.vue` | **delete-on-adoption** (pending MISSED-D ruling) — C |
| `components/visualization/SpeedSelect.vue` | G (tooltip budget) |
| `components/visualization/BasisSelector.vue` | B, H, I |
| `components/visualization/CoefficientsPanel.vue` | H, I |
| `components/equation/EqCoefficientsPanel.vue` | H, I |
| `components/visualization/ImageUpload.vue` | H |
| `components/visualization/ContourPreview.vue` | E, H |
| `components/morph/HarmonicLevelGrid.vue` | B, H |
| `components/visualization/FullscreenViewer.vue` | I |
| `components/decorative/SvgFilters.vue` | **delete** (delete-evidence decisive) — H |
| `components/visualization/CanvasOverlayButton.vue` | **`git rm` @ F.W0**; F.W3 same-commit rider — F |
| `components/visualization/ContourSettings.vue` | K (i-3 RECORDED, not executed); tooltip — G |
| `components/visualization/ExportModal.vue` | K (LC-1/LC-2 keystone repair; M-α/M-γ/M-ε sequenced) |
| `components/visualization/GalleryView.vue` | I (FR-GV-4 confirm adoption) |
| `components/visualization/CanvasControlsDock.vue` | G (tooltip consumer) |
| `components/visualization/EditorControlsDock.vue` | G (tooltip consumer; C-3 touch rider) |
| `components/visualization/VisualizationView.vue` | E (D-15 false-comment correction), J |
| `components/morph/MorphPhaseConfig.vue` | B, C |
| `components/morph/MorphShapePreview.vue` | H, J |
| `components/morph/FourierMorphDemo.vue` | H |
| `components/morph/FourierShapeExtractor.vue` | H (M-3 FadingScroll instance; pencil-boil edge) |
| `components/equation/EquationModeToggle.vue` | E, F, H |
| `components/equation/NotationPills.vue` | F, H |
| `components/equation/FunctionInput.vue` | F, H (forms; aria-pressed; `.is-active`) |
| `components/equation/EquationView.vue` | F, I (D·D-M12; D·D-M5 disposition; L·m-8; D·D-M14) |
| `components/visualization/EquationPanel.vue` | I (colocation sweep) |
| `components/equation/ConvergencePlot.vue` | G (the 10th tooltip surface ONLY; amplifier pair is F.W4's) |
| `components/equation/convergence/ConvergenceLegend.vue` | H (D-7), I (D-15 disc ring) |
| `components/equation/FrequencyGraph.vue` | K (P-10 consumer) |
| `components/equation/InfoCard.vue` | disposition per F8-REACH-01 — K |
| `components/shared/CoefficientsSpectrum.vue` | G, H, I |
| `components/paper/PaperSearch.vue` (the ONLY family member outside `search/`; holds the sole `<style scoped>` at `:41`) · `components/paper/search/PaperSearchInput.vue` · `search/PaperSearchDropdown.vue` · `search/PaperSearchModal.vue` | D |
| `components/paper/PaperSidebar.vue` | G (budget +1); P-10 consumer |
| `components/paper/PaperArticleWindow.vue` | I |
| `components/paper/PaperView.vue` | D (★NAV-1), F (the `:is-active` prop) |
| `components/paper/MobileFloatingToc.vue` | D, E |
| `components/paper/search/index.ts` | dead surface, clean-before-diff — D |
| `components/visualization/gallery/GalleryCard.vue` · `GalleryCardModal.vue` · `GalleryFeaturedCarousel.vue` · `GalleryDraftsSection.vue` · `GallerySearchBar.vue` · `GalleryMarquee.vue` · `GalleryInfiniteGrid.vue` · `UserSlugBar.vue` | H, I, J |
| `components/visualization/gallery/AdminUserList.vue` · `AdminFlaggedPanel.vue` · `AdminAuditLog.vue` (the admin trio shares the gallery directory — `fr-NotationPills.md:9`) | H, I |
| `components/layout/AppHeader.vue` · `DarkModeToggle.vue` | H |
| `components/ui/tooltip/Tooltip.vue` · `ui/tooltip/index.ts` | G (re-parameterise, NOT retire) |
| `components/ui/CollapsibleSection.vue` | H (i-4 decision) |
| `components/ui/SliderControl.vue` | B, G |
| `components/ui/PathPreview.vue` | **delete-or-complete** — K |
| `lib/easings.ts` | C (one sampler survives) |
| `lib/equation/notation.ts` · `components/visualization/lib/canvas-drawing/labels.ts` · `components/visualization/lib/basis-display.ts` | I |
| `lib/equation/index.ts` | **delete** (dead 4-line barrel) — I |
| `lib/svg-fourier.ts` | H (pencil-boil import edge check) |
| `components/paper/search/paperSearchIndex.ts` · `components/paper/search/searchHelpers.ts` | D (there is no `lib/paper/`) |
| `components/paper/search/usePaperSearch.ts` | D |
| `composables/useMorphConfig.ts` · `useFourierMorph.ts` | C (barrel seams) |
| `components/paper/useScrollNavigation.ts` | D (★NAV-1) |
| `components/visualization/composables/useWorkspaceLoader.ts` | I (basis-key site) |
| `stores/animation.ts` | A, C (compat-barrel seam; **`.a` then `.b`**, §5a) |
| `App.vue` | H |
| `style.css` | B, E |
| `lib/types.ts` | I (GalleryTier at `:96` / sort canon) — the ONE `types.ts` in bounds; the `lib/equation/` and `canvas-drawing/` homonyms are OUT |
| `<repo>/web/index.html` (NOT `web/src/`, NOT the repo root — the repo-root file does not exist) | H (MG-γ pre-paint dark script) |
| `<repo>/web/e2e/**` (8 live specs, this seat's `ls`) | A (g4/g5/g8 specs) then I (keystone repair) — **sequenced `.a` → `.d`**, §5a |

**Creates** (named units; exact paths fixed at execution, all under `web/src/lib/**` or `web/src/composables/**`, no new `shared/` dirs — KISS): one shared `timeAgo` util (I) · the basis-key unit `BasisKey`/`normalizeBasisKey()`/`basisModeLabel()` (I) · one fourier-local confirm component over `./dialog` (H) · `useAdminMultiSelect` / `useAdminAction` / `useCursorPagination` + one shared admin toolbar (I) · the extracted notation library + its unit tests (I).

**Do NOT touch**: `parse-that/**` (direct parse-that→fourier FORBIDDEN — S-7) · glass-ui producer source (producer rows go to the BH relay, NEVER frontend hacks — S-5) · `value.js/src/**` (pattern edges only; installed `@mkbabb/value.js@0.13.0` dist is the sole reference) · `api/**` (F.W5–W8) · `scripts/dev/dev.sh` (unowned, NEVER) · `web/package.json` / `web/tsconfig.json` beyond what F.W0/F.W1 land · `registry/adjudicated/fr-*.md`, `lane-frontend.md`, the dated intake (read-only for life; corrections land as tables in wave specs).

---

## §2 Carry

Agglomerated by mechanism. Identity is the **banked id** — folds are listed with ⊕/≡ and are never re-booked. ⟨…⟩ = provenance. → gN = the gate that closes it.

### §C.A — The timeline forks: one re-derivation, one session

- **fr-AnimationControls L-1 (⊕ L-6)** — MAJOR (blocker-class). Scrub opens on unconditional native `@pointerdown` but closes only on reka's hasChanged-gated `valueCommit`; no `pointercancel`/`lostpointercapture` anywhere in SliderImpl — a null-delta press or UA-cancelled touch freezes the clock while the pause glyph and `.is-playing` assert motion. **Cure lock**: ONE unconditional session pair re-derived across BOTH forks in a single cut (incl. `pointercancel`). **Channel lock (PD-1)**: "native-listeners-on-resolved-host or an upstream session-pair prop, never template bindings on the forwarding chain." **Order lock (rK-22/R2-7)**: handler order on the span is `[rekaImpl, consumer, snapshot]` — reka seeks BEFORE the consumer's startScrub; no consumer-channel cure can pre-empt reka. The value.js `useSliderTouchGates.ts`/`ComponentSliders.vue` precedents are pattern, NEVER re-booked. One-file fixes are DEFECTIVE (L-6). DISSENT (preserved): fr-ConvergenceTimeline §2 would rank L·D-3+C·C-2+D·D-1 above L·D-2 within the MAJORs; no ruling required. ⟨AnimationControls; ConvergenceTimeline L·D-1/C·C-1/L·D-2; GlassTimeline L-B1/C-1/DH-1⟩ → g3, g4.
- **fr-GlassTimeline PD-1** — MAJOR. The sole session opener rides an undeclared attribute-fallthrough channel across four boundaries that the producer's own `Slider.vue:69-78` docblock declares DEAD. **▲ P-4 ANCHOR CONFORMANCE (repair round 2, PASS-2 D-5 — R-9.2 had landed in the g6 cell ONLY, and this is the row that FEEDS it).** The bank's own anchor is version-labelled, and that spelling is carried as the BANK's, never re-asserted in F.W3's voice — ⟨cmd⟩ this seat, 2026-08-28, `grep -n 'v7.0.0:Slider.vue' ../../../V/megatranche/registry/adjudicated/fr-GlassTimeline.md` — **3 matches, and the elision is disclosed rather than presented as the whole return** (`:11` the seat's instruments line · `:46` the PD-1 row's anchor cell, quoted here · `:106` the lesson row): `46:… | v7.0.0:Slider.vue:69-78/:119-124 (git-pinned, this seat); Slider.vue.d.ts emit surface; GlassTimeline.vue:49-51/:73 | …`. **The evidence anchor THIS SPEC reads is the producer COMMIT HASH of the adopted pin from F.W0 §4's gate G-13 pin table, and the `:69-78` span is re-resolved through F.W0 §4's gate G-11 anchor table before it is read** — the identical rule g6's ⊙ cell states, now stated where the seat actually reads it. A seat re-deriving the cure SHAPE from this row therefore reads a hash, never a version string; g6's FAIL branch sends that seat back HERE, which is exactly why striking the label in the gate alone left the defect live. **CURE-SHAPE RIDER** amending banked DH-1/K-14/SR-1: native listeners on the resolved host or an upstream session-pair prop — the banked K-14/SR-1 route prescribes three MORE template bindings on exactly this channel unless re-shaped. GLASS-RELAY: the docblock-vs-render contradiction at reka 2.9.10 is producer information either way. The binding-verification probe is **F.W1's charter obligation** (assert `anim.scrubbing === true` mid-drag); F.W3 consumes its verdict; SS-13 #10 is F.W1's lane, not the cosmetic lane. DISSENT 6 preserved and **deliberately unresolved**: docblock says the channel drops, δ's render at reka 2.9.10 says it lands; ruling it statically would repeat the mis-baselining pattern this registry killed five rows for. ⟨GlassTimeline⟩ → g6.
- **fr-AnimationControls L-3** — MAJOR. Keyboard seeks (←/→/Home/End/Page) never open a session: reka routes all through `updateValues({commit:true})`, the `!scrubbing` guard eats it, the rAF's once-anchored `startTime` rewrites `t` next frame. Dies in the SAME cut as L-1; cure MUST carry an explicit keyboard-seek acceptance test (hard requirement). The amplifier pair (ConvergencePlot's unguarded `startLoop`, uncleared `playing`) is ConvergencePlot's own row → **F.W4**, attribution exactly as L filed it. ⟨AnimationControls; ConvergenceTimeline L·D-5(a); GlassTimeline L-M2/C-2/D-7⟩ → g5.
- **fr-AnimationControls C-4 / M-1 / M-14 (⊕ fr-ConvergenceTimeline L·D-7 ⊕ fr-GlassTimeline C-9/L-m3/L-i4)** — MAJOR. `GlassTimeline.vue` is a name-identical fork of the shipped `./timeline` component (no `modelValue`, no emits, hard `useAnimationStore()` bind); the tree paid with a verbatim 146-line sibling fork. **ADOPTION-ROUTE LOCK (SR-1)**: the prescribed 4.0.0 `ScrubberTimeline` adoption is REJECTED — stale twice over (K-14 7.0.0 progressbar ruling; 8.0.0 `SliderVariant` → `"scrubber" | "spectrum"`), and it regresses the accessible name (M-3's hardcoded "Timeline"). K-16: the prescribed import is unexported at both layers; the reachable dispatcher route is dead at 8.0.0 (RB-1 — an F.W1 charter note: banked route authorities were mis-baselined, v8 facts at a 7.0.0 label). Cure = unconditional session pair on the Slider composition + the GLASS-RELAY items. **M-4 rider (INFO)**: the header comment INVERTS the shadow relation (`glass-track`/`glass-fill`/`glass-thumb` are the producer's live literals; P.W5 migrated OFF the correct component) — corrected in the same cut. ⟨AnimationControls; ConvergenceTimeline; GlassTimeline; census §3a HARD-shadow⟩ → g3.
- **fr-GlassTimeline D-8 / D-9 / D-10 / L-m5 (CORRECTED by rK-20 to FOUR legs)** — MINOR. Four de-tokenised paints (`--type-small`→`text-base`, `--radius-sm`→`0.25rem`, `--shadow-sm`→theme-blind rgba, `--duration-fast var(--ease-standard)`→`0.2s ease`) die WITH the fork; the transition leg folds to banked fr-AnimationControls M-7. **REGISTRY CORRECTION in place**: the fifth leg (bare `6px`) is STRUCK — the producer's own `.timeline-caret` carries `bottom:calc(100% + 6px)` bare. **S-alpha (R2-6)** preserved: the sweep spared exactly the three scheme-paired declarations (`--popover-foreground`/`--popover`/`--border`) — any re-derivation keeps them paired. ⟨GlassTimeline⟩
- **fr-GlassTimeline AX-1 (⊕ D-5, D-6)** — MINOR, **the two-attribute law** (verbatim): `aria-hidden="true"` on `.timeline-caret` AND `aria-valuetext` on the control land in ONE edit — either alone leaves the readout absent or duplicated as ~5×/s noise. rK-21 kills D-5 clause 3 (`opacity:0`/`pointer-events:none`/`user-select:none` remove nothing from the a11y tree). D-6 rides: no `:focus-within` arm — keyboard drives an invisible value. The banked three-item GLASS-RELAY ask becomes FOUR items with the same-edit law; engine caveat → SS-13 #7. ⟨GlassTimeline⟩
- **fr-GlassTimeline CU-1** — MINOR, **rider**: the re-derivation must not land a cursor-less drag surface (zero `cursor-*` at every level today). Producer-owned deletion → GLASS-RELAY, same letter as D-1/D-4/C-6. SS-13 #8/#9. ⟨GlassTimeline⟩
- **fr-GlassTimeline LF-1** *(adopted in repair round 1, ruling R-2a — the id was dropped in BOTH directions: absent from §2 and absent from the S-5 letter roster)* — banked verbatim at `fr-GlassTimeline.md:65`: *"Inline `left` % written every frame vs the producer's own 'NEVER `style.left`' rule"*, routed `**GLASS-RELAY** + **F.W3** *(r1)*`. **BOTH limbs land, and they are one row**: (a) the **F.W3 carry** — the re-derivation writes the playhead position through the producer's own transform/inset channel, never a per-frame `style.left`, so the fork's last write-path defect dies WITH the fork in `.a`'s single cut (it rides the C-4/M-1/M-14 composition, never a separate edit); (b) the **GLASS-RELAY limb** — the producer's "NEVER `style.left`" rule and this consumer's contradiction go on the ONE batched SS-6 letter beside AX-1 / CU-1 / PD-1 (S-5, g20). Neither limb discharges the other: booking only the §2 limb loses the producer's information, filing only the letter leaves the per-frame write in the tree. ⟨GlassTimeline⟩ → g3, g20.
- **fr-AnimationControls L-1b** *(adopted in repair round 1 — sole-`F.W3` route, banked `fr-AnimationControls.md:75`)* — MINOR (DEMOTED at bank by R-15: compound-conditional, self-heals on the next drag). Stranded `anim.scrubbing === true` (writers exactly `:117`/`:122`; `reset()` never clears it) forces the trail's full-rebuild branch per frame. **Disposition verbatim**: *"**F.W3** (dies with L-1); cheapest interim: timelines read `anim.scrubbing` instead of triplicating private flags."* Dies inside `.a`'s unconditional-session pair — the same cut that closes L-1; the interim is recorded so a partial landing has a named floor, never as a licence to split the cut. ⟨AnimationControls⟩ → g4.
- **fr-GlassTimeline D-11 / L-m2 (AMENDED by DB-1)** — MINOR. The docblock at `:16-18` states the freeze bug as the file's CONTRACT, wrong three ways; L-m6 (`onValueCommitStart` names a nonexistent emit) dies in the same block. **SEQUENCING EDICT (verbatim)**: "F.W3 must rewrite :16-18 in the same cut as the pairing fix or the next reader re-derives the bug from the comment" (the ColorSpaceSelector addendum-3 stale-comment law; cf. fr-SliderControl R-21). ⟨GlassTimeline⟩
- **fr-GlassTimeline DT-1 / C-16 / Z-1 / RC-1 / L-B2 / L-M1 / C-8-cluster** — MINOR/INFO, dead-surface hygiene: the dead conduit triple (3-line delete), the unscoped `.timeline-row` ancestor compound (delete), the inert `--z-popover`, no `event.button` filter, dual mount/no lifecycle, fresh-array-per-rAF, the `number[] | undefined` adapter hole, the `scrub-start` de-dup latch — all die in the single re-derivation; if the Slider composition is kept, widen the setter parameter → F.W4. C·C-9's provable order and C·C-13's `[0..1]` contract become stated facts. `.timeline-track-wrap` + 0.125rem inset sequence AFTER M-1's fragment-root collapse → **F.W4**. ⟨GlassTimeline; ConvergenceTimeline L·D-12/L·D-13/C·C-9/C·C-13⟩
- **fr-ConvergenceTimeline C·C-7** — MINOR. ×100 step-1 under-resolves the harmonic axis (0.004823 min-Δt; 99-of-99 pairs under step). **ANTI-CURE NAMED (verbatim)**: "the naive `:max="1000"` cure would ×10 the announcement rate D·D-9 flags — the cure is adjudicated separately from the defect." Dissolves if the re-derivation adopts the float 0..1 axis (the producer scrubber's own contract); else F.W4 with an EXPLICIT announcement-rate decision. Decided INSIDE the fork re-derivation, not after. ⟨ConvergenceTimeline⟩
- **fr-AnimationControls M-3 / M-13 / TS-1** — MINOR. The re-derivation must DECIDE an at-rest, touch-reachable readout (D-23's swap is the ready answer); M-13 books as a fold-rider on fr-BasisCanvas D-9/BC-8 (fabricated-`N` family — dock-side face only); TS-1: banked BC-20/M-β3 + L-1 are ONE edit. Two-clocks-one-marker (L-M3) is a NEW FACE of the banked caret-clock family, never a re-book. DISSENT preserved: reader-δ's L-M3 MAJOR against the banked MINOR (fr-GlassTimeline r1 DISSENT-2). ⟨AnimationControls; GlassTimeline TS-1/L-M3/C-3/D-13; BasisCanvas D-9/BC-8⟩

### §C.B — The `--slider-scrub` token family (one cut)

- **fr-AnimationControls C-5 — AMENDED by fr-ConvergenceTimeline M-2 to 23 declarations / 7 files** — BLOCKER at fr-BasisSelector B-2's scope. The family is definition-absent in installed glass-ui 4.0.0 (`grep -rl slider-scrub node_modules/@mkbabb/glass-ui` → EMPTY); five colour-coded sliders silently drop `--track-color` to `var(--primary)`. **Cure lock (banked R-7/K-1, fr-SliderControl R-1)**: rename ONLY the range pair, DELETE the thumb pair, height via `size`/inline — a bare rename loses at (0,3,0) vs (0,2,0) with 0 `@layer`. **ANTI-CURE**: `--popover-offset` has 0 occurrences in the installed package AND in web/src (rK-20) — prescribing it injects a phantom. **MPC-10 CURE-RIDER**: rename-only ⇒ net α 0.264 ⇒ fill 11.51:1 → 1.51:1 (dark 5.32 → 1.42) under a green token-sweep gate; the corrected cure drops the 30%/45% wrappers and runs `--track-color` full-strength (4.00:1 light / 3.71:1 dark PASS). **MPC-31 ONE-CUT LAW (verbatim)**: "MPC-3 ⊕ MPC-10 ⊕ MPC-13 ⊕ MPC-8 ⊕ MPC-22 land as ONE CUT, and the acceptance witness for the colour scheme is taken at a non-boot state (or after MPC-8's re-domain)" — MPC-8 is the witnessability precondition; MPC-13's `glass-scrubber`→`standard` flip lands WITH the token cure, never before. Budget cell is F.W1's; execution rides F.W3/W4. DISSENT recorded: MPC-31 MINOR conjunction rider vs readers' framing; reader-1's "pixel-unchanged boot render" OVERTURNED (settle cards boot at 150ms/13.3% extent — the render IS changed; the information is not). ⟨ConvergenceTimeline M-2; BasisSelector B-2; SliderControl R-1; MorphPhaseConfig MPC-3; GlassTimeline D-3/C-4/L-m1; AnimationControls C-5⟩ → g2.

### §C.C — The easing disposition

- **fr-EasingPicker "Producer-arrival severity" (`fr-EasingPicker.md:30`, the L30 row)** *(adopted in repair round 1, ruling R-2a — a FORMATION-INPUT row, not a cure row)* — **adjudicated MAJOR as formation input** (reader-DU's INFO grading preserved in provenance, per the record). Banked verbatim: *"The arrival re-homes an entire F.W3 disposition, kills two prescribed cures, converts EasingCurvePreview.vue from a re-style into a deletion, and dates the census a full major. That is budget-moving, not color."* **Why it is booked HERE and by id**: this is the row that makes MISSED-D a ruling rather than a preference — the arrival of producer `EasingCurve` is what re-homes §C.C whole, and the two cures it kills are exactly the ones §X retains as struck (viewBox attribute surgery; the "not-drop-in" half). Without the id in this file the entire §C.C disposition rests on an unnamed premise. It **decides nothing on its own**: `.b` still blocks on the owner's MISSED-D ruling (S-6a, OP-4), and this row may not be read as the ruling. ⟨EasingPicker; EasingCurvePreview⟩ → g7.
- **fr-EasingPicker MISSED-D — THE ADOPTION-ROUTE RULING F.W3 OWES (P-10)** — MAJOR as formation input. glass-ui 8.0.0 `1bc09dde` extracts `EasingCurve` (zero-state; `strokes: EasingStroke[]`, `d` a PATH not a callable) and its header names this fork by exact line count (98+41). **OWNER RULING OWED, FLAGGED INLINE, NEVER PRESUMED** (verbatim): "D-3 dies by construction on adoption; EasingCurvePreview.vue becomes a DELETION not a re-style; MorphPhaseConfig's twin re-homes onto the same unit; the accent gets both arms free; BUT the line re-enters the tri-package deadlock (glass 4→8) whereas DU's ToggleChip route was landable at the old pin — that trade-off is the ruling the forming F.W3 spec actually owes." Downstream of the F.W1-homed re-pin ruling; F.W0 owns the substrate re-derivation (the census's 4→7 delta table is one major stale). DISSENT preserved: reader-DU graded the arrival INFO; adjudicated MAJOR as formation input. ⟨EasingPicker; EasingCurvePreview⟩ → g7.
- **fr-EasingCurvePreview FORK (⊕ fr-EasingPicker L/M-4 + C/m-8 ⊕ FMD-32 ⊕ MPC-17)** — MAJOR. Two in-tree curve renderers over two samplers share the literal `.easing-preview` (4 grep hits), 6-member prop domain vs the 22-member catalog; MorphPhaseConfig inlines a fifth fork's 40×20 sampler; `easings.ts` carries TWO samplers nine lines apart. Cure: both previews re-home onto producer `<EasingCurve>` (constant square frame; `clipped` excursion contract). Identity = census §3a HARD-shadow + intake R3-12 — folded, NEVER re-booked; the "not-drop-in" half of the old disposition is KILLED. WRONGPAD is the strongest single argument (this preview pads 37.5% for overshoot it cannot produce; the morph twin, which DOES overshoot ±0.097/1.093, clips it). Census carry-6 correction rides; FMD-32's m-5 residue (`overflow: visible`) lands as an INFO note inside this cure. Gated on MISSED-D. ⟨EasingCurvePreview; EasingPicker; FourierMorphDemo; MorphPhaseConfig; intake R3-12⟩ → g7.
- **fr-EasingCurvePreview GEO-1 / GEO-2 / DEGEN / VEFF / FACET / SAMPLE (⊕ MPC-33, MPC-23)** — MAJOR (GEO-1)/MINOR. The Button CVA's `[&_svg:not([class*=size-])]:size-(--ui-glyph)` at (0,2,1) silently overrides the entire size contract; both `:width`/`:height` inert; unreachable ±0.3 viewBox headroom; the two builders disagree on `d` precision in opposite directions. **ANTI-CURE LOCK (verbatim)**: "No interim attribute surgery: the box is not the tree's to set" — both prescribed attribute repairs bind on cascade-overridden attributes. The re-home deletes the `size` prop (EasingCurve is CSS-sized, `block aspect-square w-full`) and ships `vector-effect` + frame furniture. ONE `d`-precision decision covers the module's two builders (MPC-33 is a FACET rider, never a fresh row); MPC-23's 2.25× baked anisotropy dies in the square frame. F.W4 rider: sweep the other `[&_svg]`-governed icon sites (FR-COB-18 is the fleet home). **`fr-MorphPhaseConfig MISS-2` joins by id** *(adopted in repair round 1 — sole-`F.W3` route, banked `fr-MorphPhaseConfig.md:30`, adjudicated "MPC-33 — FACET rider, not a fresh row")*: `easings.ts:115-127` writes `${x},${y}` with **no quantisation** nine lines from `generateCurveSVGPath`'s `toFixed(3)` — *"Same module, opposite polarity … same F.W3 sweep — folds onto banked fr-EasingCurvePreview **SAMPLE**'s FACET decision as a second input (ONE `d`-precision policy covers both builders)."* The id is carried, not re-booked: the FACET/SAMPLE decision discharges it, and the ONE policy must be stated in the same edit that touches either builder. **`fr-EasingCurvePreview R-4` and `fr-EasingCurvePreview R-9` join by record-qualified id** *(adopted in repair round 1 — both sole-`F.W3`; the bare tokens `R-4`/`R-9` are fleet homonyms, so they are carried qualified, never bare, per the R-5 qualification law)*: **R-4** (`:25`) rules `size` **INERT under K-GEOM** — "not a weight knob" — and preserves *"The `vector-effect` absence survives as a contract-portability rider on the F.W3 target (which ships it — producer EasingCurve.vue:12-14/:166)"*, which is the rider VEFF's cure inherits; **R-9** (`:30`) is the seat's MINOR demotion of reader-DU's M-3 — the frame furniture (unit rect, dashed diagonal, grids, endpoints, HTML axis captions) is verified present at the producer and *"books as D-2's design cause, cured free by the F.W3 adoption"*. Both are carried as adjudication facts binding the re-home; neither mints a new edit. ⟨EasingCurvePreview; MorphPhaseConfig; EasingPicker D/D-3, D/D-6⟩
- **fr-EasingCurvePreview TOKEN / CCOLOR / CARRY-1.4.3 / INVERT (⊕ fr-EasingPicker D/D-2, MISSED-B)** — MAJOR. `--easing-accent: hsl(248 88% 71%)` has no scheme arm; the selected chip's 9px label fails 1.4.3 in BOTH schemes (3.40:1 light / 4.05 bare / 3.67 washed; accent ceiling 3.60:1 vs white); the selected state's two non-text channels measure 2.00:1 and ≈1.03:1 — every channel under its floor. **ANTI-CURE NAMED (verbatim)**: "the cure is NOT the `--viz-amber` one-arm precedent (style.css:113-127): both arms need re-minting, and the producer now ships the landing chain with both arms free — `--motion-accent` → `--easing-curve-accent` over `--viz-legendre`." `color` prop dies (EasingCurve strokes `currentColor`). GLASS-RELAY: re-scope the filed `--viz-easing` ask to the `--motion-accent` seam AND correct the false carry-note ("sole in-tree consumer" — false by the file's own :12) in the SAME relay. Fallback: "If EasingCurve adoption defers past F.W1, the two-arm retune is the F.W4 fallback." One edit with CARRY-1.4.3/INVERT; cross-ref banked fr-AnimationControls M-6 (same element, distinct defect). U-5 → SS-13. ⟨EasingCurvePreview; EasingPicker⟩
- **fr-EasingPicker D/D-1 + C/M-6 (AMENDING banked fr-AnimationControls D-9/C-9 to BLOCKER)** — BLOCKER, escalation discharged statically. Six easing chips keyboard-unreachable inside the DropdownMenu (all four reach mechanisms key on `[data-reka-collection-item]`, which a bare glass Button never mints; unconditional Tab swallow; `handleMountAutoFocus` traps focus). Cure: `DropdownMenuRadioGroup`/`RadioItem`/`Label` at the installed pin, WITH the three complications booked: indicator dot; vertical arrow map over 3-col grid ⇒ column layout or 2-D handling; `./dropdown-menu`→`./menu` at 8.0.0. **CHASSIS LOCK** (Toggle.js:68, statically decided): ToggleChip under `role="menuitemradio"` double-exposes state — "adoption is sound as *styling* only, never as chassis; the chassis is D-1's `DropdownMenuRadioItem`." Identity banked at fr-AnimationControls D-9/C-9, AMENDED here (K-11 is the enforcement event). The banked "live keystroke" SS-13 item demotes to confirmatory; SS-L-09's un-fixme rides this re-home. DISSENT preserved: reader-DU's BLOCKER on the missed M-1 (the booking was killed as a re-book, the severity was not). ⟨EasingPicker; AnimationControls; EasingCurvePreview K-11/R-7⟩ → g8.
- **fr-EasingPicker D/D-4 + C/M-3 (⊕ D/D-5, D/D-10, D/D-11..13, D/D-17..18)** — MAJOR/MINOR. ~40 lines of scoped CSS reimplement `ToggleChip variant="cell"` row-for-row; `size="sm"` overflows `--control-h-sm` by ~7.6px; opaque `var(--muted)` hover inside a translucent popover via an unlayered scoped rule. Styling adoption rides the D-1 chassis cure (`chipVariants({ shape: "cell" })`; `./toggle-chip` absent at 7.0.0 → `./chip`). DropdownMenuLabel/Group erase the three sibling-rhythm breaks; decorative-SVG and `aria-labelledby` cures are native to the D-1 primitives. All dissolve with the chassis re-home; landed under the pin instead, they degrade to F.W4 (one-line `color-mix` for D/D-10). Painted geometry → SS-13. ⟨EasingPicker⟩
- **fr-EasingCurvePreview BARREL (⊕ MPC-19 → FMD-34)** — MINOR. A stateless leaf imports a pure function through a Pinia store's self-described compat barrel — the forbidden shim shape — ×3 (`animation.ts:9-11`, `useMorphConfig.ts:20`, `useFourierMorph.ts:29`), two barrels, zero direct importers. Colocation repair has THREE seams; `EasingName` (dominant 3-vs-2) reconciles in the re-home. Bundle half correctly DECLINED (pinia in `vendor-vue`). `./clipboard` subpath ask → BH relay. F.W3/F.W4 split by seam. ⟨EasingCurvePreview; MorphPhaseConfig⟩
- **fr-EasingCurvePreview A11Y / DEADCOPY / NOFRAME / NAMES / PARDEF / ONECALL / PROSE / FLOOR** — MINOR/INFO sweep riders on the re-home: adoption resolves A11Y (producer `label` → `role="img" :aria-label`); DEADCOPY wires as the `label` copy OR deletes with rationale; NOFRAME free in adoption (MINOR, no double-charge of D-2); any surviving remnant takes a coordinate-space + pad-derivation header. FLOOR (the 9px 11-char caption sets the grid column min-content floor) is a layout decision in the re-laid-out host → F.W3/F.W4; U-1/U-3 → SS-13. ⟨EasingCurvePreview⟩

### §C.D — The paper-search family

- **fr-PaperSearchModal PSM-1 (≡ fr-PaperSearch D-1/L-1/C-B1 ≡ fr-PaperSearchDropdown B-1 ≡ fr-PaperSearchInput C-1 ≡ fr-MobileFloatingToc B-1)** — BLOCKER, **the head of four same-commit riders**. Every CSS rule governing the family is orphaned by Vue scoping (the only `<style scoped>` lives in `PaperSearch.vue:41`; the modal subTree is a Teleport vnode, the dropdown a two-root Fragment, the input's children never carry the hash) — the modal ships unstyled to `<body>`. Cure: mechanical rule-move against **THE SEAT-ENUMERATED SET (R-5, verbatim)**: 48 rule blocks = 40 selector + 8 transition, of which 5 live (:43, :47, :58, :199, :204) and 43 dead — "F.W3/W4's mechanical rule-move takes THIS enumerated set" (the 49/5/44 recount is the kill of a kill, K-10). Provenance: refactor casualty at `ffba307`. ⟨PaperSearchModal; PaperSearch; PaperSearchDropdown; PaperSearchInput; MobileFloatingToc⟩ → g1, g13.
- **fr-PaperSearchInput MISS-LC1** — MAJOR, **mandatory rider on PSM-1**: the corpus's OWN prescribed cure orphans five cross-SFC shared hooks (`paper-search-action-btn` emitted by Input AND Modal; `-result`/`-badge`/`-number`/`-label` by Dropdown AND Modal) while both repair plans assign each class to exactly ONE child. Cure: shared hooks go in a non-scoped or shared home (or the markup moves with them) — never one-child-per-class. Same commit as PSM-1. ⟨PaperSearchInput⟩ → g13.
- **fr-PaperSearchModal PSM-13** — MAJOR. `.search-modal-input { outline: none }` is inert only because PSM-1 kills it; the prescribed colocation strips the primary control's focus indicator with no replacement (a WCAG 2.4.7 regression). **The colocation edit lands a focus indicator in the same hunk.** Same commit as PSM-1; named P-11 instance. ⟨PaperSearchModal⟩ → g13.
- **fr-PaperSearchModal PSM-4 (⊕ fr-PaperSearchInput D-M1 ⊕ fr-PaperSearchDropdown D:B-3)** — BLOCKER (LATENT, armed by PSM-1's cure). Four chrome surfaces composite `--muted-foreground` through 45%/50%/40% dilutions to 1.88/2.39/2.04/1.74:1; eight further dilutions in the dropdown; the producer's own written refusal of the idiom stands beside them. "Must land in the same wave as PSM-1 or the cure ships a WCAG failure." House floor precedent: style.css:113-122 (`--viz-amber` 3.54:1). Token re-ink is F.W1's leg; **the gate is F.W3's**. DISSENT: the dropdown D:B-3 BLOCKER re-filing was killed (K4); the modal's grade stands. ⟨PaperSearchModal; PaperSearchInput; PaperSearchDropdown; PaperSearch D-14⟩ → g13.
- **fr-PaperSearch FR-PS-BDT (⊕ fr-PaperSearchDropdown FR-PSD-CB)** — MAJOR (repair hazard). The revived `.paper-search-backdrop { position:fixed; inset:0 }` resolves against the BAR's padding box (the host's `backdrop-filter` makes it the containing block) — the click-away shim covers the INPUT (tap fires `close()`, wipes the query). **The D-6/L-1¶3 cure may NOT be executed as prescribed**; outside-tap dismissal re-derives outside the filter containing block (or via the adopted dialog chassis). Same repair as PSM-1; the axes' "un-selectable" consequence is false and must not be inherited. ⟨PaperSearch; PaperSearchDropdown⟩ → g13.
- **fr-PaperSearch FR-PS-CLIP (⊕ fr-PaperSearchDropdown D:M-2)** — MAJOR (repair hazard). The revived `.paper-search-results` cannot escape `.sidebar-nav`'s clip; 50vh never honoured; the D-3 cure as prescribed is VOID. Portal/chassis adoption is the sound direction. Same repair as PSM-1. ⟨PaperSearch; PaperSearchDropdown⟩ → g13.
- **fr-PaperSearchDropdown FR-PSD-BASE** — MINOR (latent-on-repair). `align-items: baseline`, no height, `<Button>` with no `size` ⇒ CVA `h-(--control-h-md)` applies before AND after the cure. **INVERSION LOCK**: "the un-overridden height is the only thing keeping coarse-pointer rows at the WCAG 2.5.8 floor" — any geometry cure supplies the floor explicitly. Rides PSM-1; same edit family as D:M-7. ⟨PaperSearchDropdown⟩
- **fr-PaperSearchModal PSM-9 (≡ fr-PaperSearch MISS-B3 ≡ fr-PaperSearchDropdown C:C-6 ≡ fr-PaperSearchInput C-2)** — MAJOR (census escalation 🟡→🔴 HARD). `@mkbabb/glass-ui/search` at INSTALLED 4.0.0 is a generalisation of this exact fork (same docstring, separators, 120/30 defaults, ten state members, prop-for-prop host, byte-identical geometry/`<kbd>` legend), imported ZERO times; the producer copy already fixes the truncated-cache and display:none defects. **DECISION ROW + BH RELAY.** Adoption is COMPONENT-for-component (`FuzzySearch.vue` is the 1:1 twin; `.interactive-item`/`CommandItem` are the row primitives). Honest costs carry: 3-field scorer, no `number`×18/`rawTex`×6 weighting, `searchVariants.ts` absent at 4.0.0. **ANTI-CURE**: PSM-14 proves the prescribed adoption does NOT fix the short-query death — adoption must be priced with an explicit scoring decision, not assumed to inherit one. ★MF-11's dead surface is cleaned BEFORE the `./search` diff. ⟨PaperSearchModal; PaperSearch; PaperSearchDropdown; PaperSearchInput; PaperView census §3a / lane-frontend:438⟩ → g12.
- **fr-PaperSearchModal PSM-5 (⊕ PSM-30 ⊕ fr-PaperSearchDropdown L:L-2/C:C-10 ⊕ fr-PaperSearchInput L-§3)** — MAJOR. The prefix-narrowing cache stores the TRUNCATED top-30 and narrows the next keystroke from it (one `.slice()` placement divergence from the producer); exported `maxResults` inert on the cache-hit path. Dies with fork retirement (producer caches the FULL scored array). PSM-30 rides as the exported-contract rider. C-10's flagship `"thm 3"` scenario is KILLED (K2) and L's BLOCKER grade KILLED (K4) — inherit neither. DISSENT: L-axis BLOCKER preserved as overturned-on-the-record; MAJOR sustained. ⟨PaperSearchModal; PaperSearchDropdown; PaperSearchInput; PaperSearch L-2/C-M4⟩
- **fr-PaperSearchDropdown FR-PSD-TYPE (⊕ fr-PaperSearchModal PSM-14, PSM-28)** — MAJOR (LIVE, seat-CONFIRMED BY EXECUTION). `_lc.type` (internal enum, byte-identical per type) weighted 10 over `rawTex` 6 / `plainText` 3 ⇒ content-blind document-ordered slabs, +37.2% over genuine hits; length penalty scales with FIELD while bonuses scale with PATTERN (exact mid-body 'fourier' scores negative). **The scoring decision is made explicitly at adoption (PSM-9) — NOT inherited.** PSM-28 rides: UTF-16 code-unit indices over a code-point array ⇒ off-by-one, highlight corruption, lone surrogate into `escapeHtml`. ⟨PaperSearchDropdown; PaperSearchModal⟩
- **fr-PaperSearchModal PSM-2 (⊕ PSM-6, PSM-3, PSM-15)** — BLOCKER. Not a dialog: zero `role=`/`aria-` across the family, no name, no trap/restore, no `inert`, no scroll lock, no combobox semantics, Escape input-bound only; below 1024px TWO modal instances mount against one `isExpanded` while a document-global `querySelector` drives the invisible sidebar instance. Cure: adopt `@mkbabb/glass-ui/dialog` as chassis + combobox semantics; replace the global `querySelector(".search-modal-results")` with the template ref `modalInputRef` already proves. Identity guard: intake R3-11/X-6 adjudicated the duplicate-Teleport row — fold, never re-book. The house idiom exists two files away (`MobileFloatingToc.vue:37-59`). Rides PSM-9's decision. ⟨PaperSearchModal; PaperSearchDropdown D:M-1/C:C-11; PaperSearchInput C-7/D-M4; MobileFloatingToc M-16⟩
- **fr-PaperSearchModal PSM-7 (⊕ PSM-39, PSM-29, PSM-17, fr-PaperSearchDropdown L:L-14/L:L-17)** — MAJOR/MINOR. `SearchEntry.id` not unique (code/proof take `id: sectionId`); `sectionId`/`depth` write-only; `TYPE_LABELS: Record<string,string>` erases the closed 13-key union; four badge abbreviation strategies incl. `aside`→'Note'. Cure MINTS stable identity at `buildSearchIndex`, then DELETES the index term from the `:key` (PSM-39 is cost, not correctness); the projection deletes `sectionId`/`depth`; the label map types on the union. C·S5's celebration of the index-spliced key is KILLED (K-3) — not inherited as a superlative. ⟨PaperSearchModal; PaperSearchDropdown; PaperSearch FR-PS-LBL⟩
- **fr-PaperSearchModal PSM-25 (⊕ fr-PaperSearchInput MISS-DU2)** — MINOR (missed-adoption precedent). Adopt the scope-proof global recipes AS SHIPPED: the `.kbd` atom (producer's own FuzzySearch uses it on these EXACT four keycaps) and `.input-bar-field` (five of six dying declarations incl. D-B1's `min-width:0`) — immune to the scope defect BY CONSTRUCTION, the cheapest part of the PSM-1 repair. Honest cost sustained: mono family needs one override; the `.input-bar` CONTAINER is not adopted. Precedent: kf-KeyboardShortcutsModal banks `.kbd`-as-shipped as a superlative. ⟨PaperSearchModal; PaperSearchInput⟩
- **fr-PaperSearchModal PSM-18 (⊕ fr-PaperSearchInput M-1, fr-PaperSearchDropdown L:L-11)** — MINOR. 20 lines of result-row markup duplicated verbatim; Input/Modal are the same component twice and have ALREADY drifted (`h-3` 'Clear search' vs `h-3.5` 'Close', same `close()`). Dies with PSM-9's adoption; the drift is the first symptom, not a separate row. ⟨PaperSearchModal; PaperSearchInput; PaperSearchDropdown⟩
- **fr-PaperSearchModal PSM-26 / PSM-27 / PSM-37 (⊕ fr-PaperSearchInput D-M7/C-6/m-3, fr-MobileFloatingToc M-13/M-17)** — MINOR / MAJOR (D-M7). Twin ghost buttons with opposite data semantics; the legend teaches 'esc close' while Escape COLLAPSES; 'Clear search' calls `close()` and deletes itself under the pointer; `close()` async ⇒ reopening ≤120ms paints stale rows. **One state-ownership repair**: `open()`/`close()`/`clear()` as sole mutators with narrow per-child interfaces (M-4/C-5); the legend becomes true; the clear control survives its own action. One unit with M-13/M-17's desync; rides the chassis adoption. ⟨PaperSearchModal; PaperSearchInput; MobileFloatingToc⟩
- **fr-PaperSearchModal PSM-22 (⊕ FR-PSD-PURE, PSM-31, PSM-11, PSM-41, PSM-16, ★MF-11, fr-PaperSearchInput m-2/m-6)** — MINOR/INFO. Module-global query-keyed cache with no owner/identity/disposal; side-effecting computed; eager synchronous index build per mount; `matches` spread and read by nobody; `search/index.ts` ZERO consumers publishing an attribute-unsafe `escapeHtml`. **Clean the dead surface BEFORE the `./search` diff** (★MF-11 verbatim). Producer contrast is the target shape (WeakMap-per-index + `clearSearchCache` + `onScopeDispose` + deferred computed build). New fourier member of the fleet dead-barrel class (ApiOfflineChip AP-11 · kf-EditorHeader EH-6 · kf-KeyframeCard KF-KC-45 · PaperSearchInput C-9). F.W0 rider: no configured gate can report any of it (`noUnusedLocals`/lint absent). ⟨PaperSearchModal; PaperSearchDropdown; PaperSearchInput; PaperView; EquationPanel M-N14 class⟩
- **fr-PaperSearchModal S-4 — THE GREPPABLE SCOPE LAW** — spec-authoring input. **THE LAW (verbatim)**: "no `<style scoped>` block may declare a class appearing exclusively in a CHILD component's template" — "would have caught this at `ffba307` and generalises to every future split." Lands as a GATE (g1), authored once, inherited by F.W4 — not a per-component edit. ⟨PaperSearchModal challenge-L §6, both returns, ratified⟩ → g1.
- **fr-PaperView ★NAV-1 (⊕ ★MF-13)** — MAJOR/MINOR. Cross-reference and non-section search navigation are silent no-ops (`performScroll` accepts flat-section ids only; 71/164 `\ref`-family uses and 66/374 search entries dead), and every dead click still `preventDefault()`s AND pushes a bogus navStack entry. Cure: de-fork to the producer's scroll domain (`useScrollTo` is getElementById-based) or widen `performScroll` with a getElementById arm; **the navStack push MUST move after a successful scroll**. ★MF-13: one-line spelling fix (`useSidebarState` snapshot `sections:` beside getters) during the sidebar work — the adoption itself stays exemplary (S-4). SS-13 confirms one click. ⟨PaperView⟩

- **THE `.c` ESCAPE SET — six banked ids the round-1 roster could not see** *(adopted in repair round 2, ruling R2-3b under R2-3a's TWIN LAW: every one is a **route-marked bullet**, invisible to the table-shape roster; every one's target file is `paper/PaperSearch.vue` or `paper/search/**`, which are **`.c`'s** by §5a 1c, so the dual `F.W3/W4` spelling homes at **F.W3**, not F.W4)*. All six quoted from the corpus by command, this seat, 2026-08-28 — ⟨cmd⟩ `grep -rn -- '<id>' fr-*.md`:
  - **`fr-PaperSearch FR-PS-Z`** (`fr-PaperSearch.md:40`, MINOR, LATENT) — the Z-token altitude error three ways: dropdown filed at `--z-bar` (30) where `--z-popover` (130) exists for exactly this surface; the backdrop given the IDENTICAL token as the plate it must sit behind (*"the FR-PS-BDT ingredient, an authoring error in its own right"*); and on the floating consumer both clamped inside `.floating-toc` → effective altitude 20, beneath the `--z-overlay` chrome. Routing cell verbatim: *"**F.W3/W4** (rung assignment is spec input)."* **It lands as spec input, here**: the rung assignment is made INSIDE the PSM-1/PSM-2 chassis adoption (a portal'd dialog chassis re-parents the surface and dissolves the clamp) — not as a token nudge on a clamped element. The row goes live with the D-1 repair, so it cannot be deferred past the cure that arms it.
  - **`fr-PaperSearch FR-PS-HL`** (`:41`, MINOR) — *"Two query sources in one render"*: rows come from `debouncedQuery` (120 ms) but their `<mark>`s compute from the raw `query`, so any keystroke breaking a subsequence blanks ALL highlights for 120 ms on rows still presented as matches, then re-marks. *"Cure is free: highlight from `debouncedQuery`."* Distinct from banked D-29 and L-9 — the record swept for it. Rides the PSM-9 scoring decision's edit (one highlight source, chosen once).
  - **`fr-PaperSearchInput MISS-DU1`** (`fr-PaperSearchInput.md:75`, MINOR) — the field grows **~16px TALLER on the first keystroke** (empty ≈36.6px; the undebounced clear Button mounts at a DEFINITE 40px → ≈52.6px), and `<PaperSearch>` is `.sidebar-nav`'s FIRST child, so *"the whole Contents tree translates ~16px down on the first character and back up on clear."* Banked correction carried: *"All three axes priced the Buttons' arrival as horizontal; the live consequence is vertical."* Cured by the same `icon-sm` swap as banked `D-B2` — which is why it is booked with the family and not alone.
  - **`fr-PaperSearchInput MISS-DU3`** (`:77`, MINOR) — **the gate-design row**: the repo's checked-in horizontal-overflow gate on `/paper` at three viewports is *"STRUCTURALLY BLIND"* to `D-B1`, because `.sidebar-nav`'s `overflow-y:auto` with no `overflow-x` declared computes `overflow-x` to `auto` and absorbs row overflow into its own scroll port, never reaching `documentElement.scrollWidth`. Routing cell verbatim: *"**F.W3/W4** gate design."* **Consequence, binding on `.c`**: the cure's assertion is **CONTAINER-scoped** (`.sidebar-nav` scrollWidth vs clientWidth), never document-scoped — a document-scoped assertion is green against a live defect.
  - **`fr-PaperSearchInput MISS-DU4`** (`:78`, MINOR) — the field's chrome is **pointer-dead**: no `<label>`, no click-to-focus on wrap or glyph, so *"the largest mark in the field (the 24px glyph) and all wrap padding are inert hit area on both arms"* while the needed `focus()` method already exists. *"A wrapping `<label>` is also the cheapest cure for C-7's naming half"* — so it lands inside PSM-2's combobox-semantics edit, one element, two rows. Registry-swept: MFT `M-3` books the TRIGGER span, a different element.
  - **`fr-PaperSearchModal PSM-40`** (`fr-PaperSearchModal.md:82`, INFO-graded MISS) — the enter transition's root and child disagree on duration (overlay `opacity 0.2s ease` vs child transform `0.25s var(--ease-out-expo)`), so *"the `enter-active` removal cancels the child transform at ~80% time (≈99.6% progress under the expo curve — sub-pixel snap, honestly graded INFO)"*. The leave pair matches, *"proving the author knew to match them."* Booked as a **one-line hygiene rider on the colocation commit** (g13) — the rule-move already rewrites this block; leaving the mismatch behind would re-author it.
  - **Anti-rename note**: `MISS-DU1/3/4` are **`fr-PaperSearchInput`'s** banked ids and are carried record-qualified for life. They are NOT `fr-GalleryView`'s `MISS-DU-1..4` family (different record, different hyphenation) — the two spellings differ by one character and the collision is exactly the kind that hid these rows; it is named here so it is disarmed. ⟨PaperSearch; PaperSearchInput; PaperSearchModal⟩ → g13, g19.

### §C.E — The relay re-scopes (producer rows, never frontend hacks)

- **fr-ContourPreview row 4 (D:M-3 rewritten) — THE CARTOON-CARD RELAY RE-SCOPE** — MAJOR (re-scope). The booked upstream carry is STALE: at 4.0.0 the producer ships the surface; at 7.0.0 its docblock ENDORSES the consumer-written `cartoon-surface` class — exactly what the shim `@apply`s; the shim is the version-stable form. **RE-SCOPE (verbatim)**: "send `--viz-amber` only, RETIRE the cartoon-card ask" + F.W1 rider "do NOT migrate; correct the shim's stale comment." **ANTI-CURE**: the D-lane `<Card tier="opaque" surface="cartoon">` migration is a 21-site typecheck break at the uplift target (KILL-6); the corpus's cure is TWO MAJORS STALE (K-7 — at 8.0.0 `cartoon` is not a prop; the form is `<Card class="cartoon-surface">`). Census of record: 21 sites / 14 files (the shim self-reports 14/13 — stale by 7). fr-VisualizationView D-15 rider: the `:188-193` "migration complete" comment is FALSE (four surviving sites incl. the host's own :163) — corrected in whatever wave touches the file. PAW-48 adopts `cartoon-surface` on the paper callout (delivers PAW-10's 2px boundary + stamp, retires PAW-20's `transform` shorthand in the same edit). ⟨ContourPreview; EqCoefficientsPanel FR-EQC-3/4/5; InfoCard FR-IC-19; MorphShapePreview D-13; BasisCanvas D-12; VisualizationView D-15; PaperArticleWindow PAW-48⟩ → g14.
- **fr-EquationModeToggle FR-EMT-5 — THE `--viz-amber` CARRY (the relay that ships)** — MAJOR + fleet limb. Active label paints `--viz-amber` on `--card` under an 8% wash → 3.942:1 (third independent derivation), under AA, hierarchy inverted (selected 3.94 < unselected 5.116); the D.W4.d carry is calibrated against `--background` and is mis-based for every tinted plate. **THE CURE IS THE WASH, NOT THE TOKEN** (all calibration arms reproduce ≈4.6; the v7-v8 uplift improves and still fails). The fleet limb — the carry's calibration base — is what the BH relay carries; `--viz-amber` is the ONLY cartoon/amber item on the letter. The fleet limb escalates AT THIS WAVE if the re-derivation finds further tinted-surface AA failures. DISSENT preserved verbatim (reader-2 BLOCKER): "the finding convicts the D.W4.d carry for every tinted surface in the tree, and a carry-conviction outranks an instance." ⟨EquationModeToggle; MobileFloatingToc M-6 folds here; BasisCanvas D-7; ContourEditorCanvas D/B-1; EasingPicker D/D-2 cite⟩ → g14.

### §C.F — Active-state vocabulary + the aria-pressed family

- **fr-CanvasOverlayButton FR-COB-3 — THE FIVE-SPELLING ACTIVE-STATE VOCABULARY** — MAJOR. Five spellings of one state (`.is-active`, `.is-active-sub`, `.is-playing`, `.liked`, the `:is-active` PROP at `PaperView.vue:344`): 11 scoped-CSS rule lines / 7 files, 17 `:class` bindings, 1 prop binding; the subject's own `.is-active` inert via a `btn-glass`/`glass-btn` transposition. Cure: fourier-wide consolidation with the **SWEEP-LAW RIDER (verbatim)**: "never key the sweep on a bare `is-active` grep (it corrupts the prop)." **ANTI-CURE**: re-adding `.glass-btn` is NOT the cure — surfaces.css:46-56 declares it and `.btn-pill` MUTUALLY EXCLUSIVE (R8-17). **SCOPE CORRECTION (FR-EMT-23, verbatim)**: banked R-3's cell "every CSS rule is compound-scoped, none reaches a `<Button>`" is factually WRONG — at least three of seven files put `.is-active` directly on a glass Button root (EquationModeToggle :13-14/:63, FunctionInput :158-163/:244, GallerySearchBar :69/:159); on Button-rooted sites `aria-pressed` interacts with the CVA's `aria-pressed:` arms. The subject's instance dies with F.W0's `git rm`. ⟨CanvasOverlayButton; EquationModeToggle FR-EMT-23; PaperView⟩ → g9.
- **fr-CanvasOverlayButton FR-COB-17 (⊕ FR-COB-4's toggle-vs-action law, FR-COB-11)** — MAJOR. A bare `git rm` deletes the tree's only correct reading of the producer's toggle contract (7 dock toggles + ConvergenceTimeline:61 carry class-vocabulary state with ZERO ARIA while v7's prefers-contrast/forced-colors arms key on ARIA exclusively; AT-only defect). **The 8-site `:aria-pressed` LIFT lands IN THE SAME COMMIT AS THE `rm`** — using L's list, NOT C's (K-11: C adds `aria-pressed` to a one-shot close action and miscounts). **GOVERNING LAW (FR-COB-4, verbatim)**: "the lifted `aria-pressed` idiom binds ONLY on true toggles — never on the FullscreenViewer close action." FR-COB-11 rider: the "no backing paint" cell is STALE at installed 4.0.0 (icon-button.css:109-115 paints) — the DOCK-ACTIVE ask was answered upstream; do NOT re-send. ⟨CanvasOverlayButton⟩ → g9.
- **fr-CanvasOverlayButton FR-COB-5 (⊕ FR-COB-22)** *(both adopted in repair round 1, ruling R-2a — rows riding FR-COB-17's lift, sequenced with F.W0's `rm`)* — **FR-COB-5** (`:54`, D-5/L-7/C-5): the icon-only-button wrapper carries **no accessible-name discipline anywhere in its contract** (whole API = one boolean; no label prop, no default, no `defineSlots`, no warning; Button + Primitive inject nothing), while the producer names the delegated duty at v7 (`iconOnly` doc `:24` — *"accessibly named icon command"*), and the nameless sites already ship: `FullscreenViewer.vue:110-112` (`Minimize2`, no `aria-label`, no `title`) and `ConvergenceTimeline.vue:61` (*"a named-less toggle announces a state change of nothing"*). Disposition verbatim: **"F.W3/W4 — the icon-command naming sweep (36 `size="icon"` sites are the denominator); the wrapper's own instance dies with the file."** **The F.W3 half is the LAW and the two named sites**, landing in the same commit as the FR-COB-17 `aria-pressed` lift and F.W0's `git rm` — the 36-site sweep ITSELF stays F.W4's edit budget (§X, unchanged): the denominator is stated here so F.W4's sweep cannot be authored against a remembered count, and FR-COB-4's toggle-vs-action law governs both (a close action gets a NAME, never `aria-pressed`). **FR-COB-22** (`:81`, D-12): the glyph-to-box ratio **0.40** is scale-invariant by construction (`--ui-glyph` 1rem / `--control-h-md` 2.5rem, both × `--ui-scale`) — the invariance is *deliberate and credited* — but thin against the 0.50–0.60 icon-button register, and per FR-COB-18 *"0.40 is not what two live sites even render."* Disposition verbatim: **"F.W3/W4 register note; producer constant question rides the BH relay."** It lands as a **register note only** — no F.W3 edit re-tunes a producer constant (S-5); the constant question goes on the SS-6 letter, and FR-COB-18's glyph-register sweep stays F.W4's (§X). ⟨CanvasOverlayButton; FullscreenViewer; ConvergenceTimeline⟩ → g9, g20.
- **fr-EquationView D·D-M12 — THE aria-pressed FAMILY** — MAJOR. Two mechanisms serve one job in one file (`SegmentedTabs variant="underline"` + a hand-rolled colour-only toggle) while the house pattern is documented three files away (`notation.ts:5-7`) and the Button CVA ships `aria-pressed:` arms. Cure: retire onto SegmentedTabs OR add the one attribute; NotationPills rides the same edit. **CURE RE-RANK (FR-NP-3)**: `ToggleGroup type="single"`/`SegmentedTabs` FIRST (roving focus free), hand-rolled radiogroup LAST. **ANTI-CURE (FR-NP-19/K-18)**: the banked "one attribute — `role="radiogroup"`" cure is KILLED — it ships axe-critical `aria-required-children` and announces an unimplemented APG contract, "a strictly worse a11y outcome than the honest three-button set"; correct naming cure = `role="group"` + `aria-label="Notation"`. G-DU2's cure is free and symmetric: MOVE the attribute off the control that cannot toggle onto the two that can. Increment booked per site (FunctionInput's 8 preset pills + wand; +1 for FunctionInput:192's Wand2 per R2-r1); fleet-law identity STAYS at fr-DarkModeToggle N-2 — never re-booked. ⟨EquationView; NotationPills; FunctionInput D-4.2+C-6; GalleryCard G-DU2; GallerySearchBar FR-GSB-15; DarkModeToggle N-2; EquationModeToggle FR-EMT-3⟩ → g9.

### §C.G — The wrapper law + the tooltip shim

- **fr-CanvasOverlayButton FR-COB-15 — THE STANDING WRAPPER POLICY** — MAJOR. Goldilocks failure: the wrapper unified ZERO of 9 live sites; the single natural consumer hand-rolls it. **▲▲ THE "POLICY SENTENCE" IS RE-LABELLED AT THE BYTES (repair round 2, PASS-2 D-8; R2-1-LAW 2 — a "verbatim" label without a ⟨cmd⟩ note is a defect per se, and this one was also a mis-description of its coordinate).** The cited coordinate is not a policy sentence; it is a **per-file ledger table row**, and the words are a CLAUSE inside its findings cell. ⟨cmd⟩ this seat, 2026-08-28, `sed -n '93p' /Users/mkbabb/Programming/fourier-analysis/docs/tranches/A/audit/W3-button-ledger.md`:

  ```
  | `visualization/CanvasOverlayButton.vue:8` | same | `glass` | `icon` | — | naked wrapper component forwarding `active` as `aria-pressed`; `<Button variant="glass" size="icon">` IS the surface | A.W3.b.1 |
  ```

  ⟨cmd⟩ `grep -ic 'policy' W3-button-ledger.md` → **0**: the file contains no wrapper-policy sentence anywhere, so no re-pointing to a better line is available — **the LAW is F.W3's own, and it is stated here in F.W3's voice**, with the ledger row cited as its EVIDENCE (one naked wrapper, forwarding one boolean, over a surface the producer already ships) rather than as its authority. **THE POLICY (F.W3's, authored here)**: *a wrapper earns its existence only by adding contract the primitive lacks; `<Button variant="glass" size="icon">` IS the surface, so a wrapper that only forwards is deleted, not re-styled.* The ledger row's own identity is `A.W3.b.1` and is cited by that id, never as a rule it does not carry. The wrapper's instance dies at F.W0; the policy carries HERE into every remaining wrapper decision. ⟨CanvasOverlayButton; W3-button-ledger row `A.W3.b.1`⟩ → g10.
- **fr-SliderControl R-6 — THE WRAPPER LAW'S EXACT SHAPE** — MAJOR. 8 props / 1 emit against the producer's `SliderRootProps & {…}` + two emits: `disabled`/`size`/`orientation`/`keepDockOpen` unreachable; `valueCommit` DESTROYED — precisely why the consumer's busy-state defect (fr-ContourSettings M-2) is uncurable at the callsite. **THE LAW with its counterweight (verbatim)**: "`class` fallthrough is WANTED (`FunctionInput:180 class="flex-1"`) — the cure is explicit passthrough + a re-emitted `valueCommit`, NEVER blanket `inheritAttrs:false`." **Operational form**: a surviving wrapper either declares `inheritAttrs:false` WITH a pre-placed `v-bind="$attrs"` on its intended host, or it does not wrap at all. One repair unit with R-7 (the settle event is the cure: three coalescing dialects 1000ms/300ms/none + synchronous `sessionStorage.setItem` per step); the F.W1 benefit rider survives separately. ⟨SliderControl; ContourSettings M-2⟩ → g10.
- **fr-Tooltip FR-TT-15 — THE SHIM RE-PARAMETERISATION SURFACE** — MINOR (the row that IS the surface). 2 knobs exposed; the ones that fix its own defects amputated; `class` worse than absent (fragment root drops it with a dev warn). **THE SURVIVING LIST (verbatim)**: "measure (FR-TT-3), `align`/`class`-with-element-root-decision, hoverable policy (FR-TT-10), offset rationale (FR-TT-13), contract guard (FR-TT-12)" — `ariaLabel` leg DROPPED. Governed by S-1's conditional-forwarding law: bind only what the consumer passed. **FR-TT-24 reframes the migration**: "`./icon-tooltip` does not exist at v7.0.0 … after F.W1 there is no single-component tooltip in the package at all; the local shim is the only possible home for the `side` + `#content` + provider-non-nesting contract" — R3-7a's "barrel → `@mkbabb/glass-ui/tooltip`" restates as **RE-PARAMETERISATION OF THE SHIM, NOT RETIREMENT**. fr-BasisSelector K-12 kills the one-word-swap cheap arm at the 7.0.0 export map. ⟨Tooltip⟩ → g11.
- **fr-Tooltip FR-TT-4 — ANTI-CURE 3: THE ariaLabel FORWARD (P-11)** — MAJOR (defect stands; cure RETIRED). Rich `#content` announced as one unpunctuated `textContent` run, live at CoefficientsSpectrum:105-120 and FunctionInput:198-210. **The corpus's unanimous cure is REPUDIATED BY THE PRODUCER**: `ariaLabel` declared at 4.0.0/v7.0.0, DELETED at 8.0.0 with the standing comment (`:69-73`) — "the content-side override replaced the very node the describedby reads." "Prescribing it routes F.W3/W4 at a prop the adoption chain is retiring." Re-target required; cross-cited in F.W4's edit budget. ⟨Tooltip⟩
- **fr-Tooltip FR-TT-10 — CURE-CONFLICT: hoverable vs WCAG 1.4.13 (P-11)** — MINOR. **LOCK (verbatim)**: "the cure conflicts with D §6's banked WCAG 1.4.13 CLEARED pass (hoverable is the *requirement*; `disable-hoverable-content` would delete the pass). Cure adjudicated separately from defect. → F.W3 — only with a 1.4.13-preserving design; otherwise a deliberate no-op, recorded." A recorded deliberate no-op is an acceptable outcome here — never a silent drop. ⟨Tooltip⟩
- **fr-Tooltip FR-TT-7 (≡ fr-CoefficientsPanel FR-CP-3) — ANTI-CURE: 'make the shim single-root'** — MAJOR. The shim bottoms out in a reka `PopperRoot` `renderSlot` Fragment ⇒ the entire `coeff-list` TransitionGroup FLIP register is dead + one dev warn per row (a B.W2.c regression). **ANTI-CURE LOCK (DU K6, re-proved)**: "`renderSlot` mints the Fragment UNCONDITIONALLY — C-1's 'make the shim single-root' arm is KILLED." Surviving cures: key the `v-for` on the `.coeff-row` element with the Tooltip inside, OR adopt an element-root primitive. C-1's portal provenance corrected: reka's TooltipContent renders NO portal — the portal is glass's own. One edit with M-11's key fix (`comp.index` is unique by construction); warn multiplicity → SS-13. ⟨Tooltip; CoefficientsPanel; CoefficientsSpectrum M-1⟩
- **intake R3-7a — THE TOOLTIP MIGRATION BUDGET (CARRY→F.W3)** — the adapter is the only label on 17 icon-only triggers (Editor ×10 · Canvas ×6 · FunctionInput ×1) and wires a DESCRIPTION (`aria-describedby`, only-while-open), never a NAME. **Budget verbatim**: "35 callsites / 9 consumers, barrel `web/src/components/ui/tooltip/index.ts` → `@mkbabb/glass-ui/tooltip`" — **AMENDED IN PLACE by X-11**: "re-point the import AND mint a naming leg." RESTATED by FR-TT-8: 35/9/≥86 mounted; 51 TooltipRoots on first paint of `/paper` from ONE authored callsite. **UN-BUDGETED SURFACES the sweep must add**: ConvergencePlot's hand-rolled `v-html` tooltip (a 10th surface); PaperSidebar depth-2 rows (+1, free once M4's row component lands); the native-`title` surface (42 occurrences / 19 files — FR-EMT-10: the `<Tooltip`-keyed budget is BLIND to it; CONVERT to Tooltip + `aria-label`, never delete); CoefficientsSpectrum's 2 callsites of which one is m-2's phantom (budget counts 2, deletes 1). Do NOT port AnimationControls' two malformed anchors as-is; `icon-tooltip` is the displaced producer seat. **FR-TT-20**: normalize the TWO import identities (8 barrel + PaperSidebar's deep SFC) BEFORE the migration — the choke-point count is 2, not 1. Identity STAYS WITH THE INTAKE; every component record's tooltip cell rides as a rider, never re-booked. X-9's member-scope law publishes before any percentage. ⟨intake; Tooltip; CanvasControlsDock; EditorControlsDock C-3; ContourSettings i-6; BasisSelector m-6; CoefficientsSpectrum i-1; SpeedSelect SS-C-9/SS-L-11; PaperSidebar L-8; AnimationControls R-7; ConvergencePlot D-10⟩ → g11.
- **fr-Tooltip FR-TT-2 / FR-TT-3 / FR-TT-12 / FR-TT-13 / FR-TT-18 / FR-TT-19 / FR-TT-17** — BLOCKER (FR-TT-2)/MAJOR/MINOR. No touch path at any of 35 callsites (three independent producer arms) and keyboard structurally dead at 8; no `max-width` anywhere at either pin against unbounded input (both pins discard `--reka-tooltip-content-available-width`); `as-child` silently accepts non-focusable/`display:none`/SVG triggers; three uncoordinated anchor gaps in one dock (4/6/8); four tooltips inside a HoverPopover paint beneath their panel (`--z-tooltip:120` < `--z-popover:130`). The re-parameterised shim MUST STATE ITS INPUT-MODALITY CONTRACT and document the one-focusable-element-child contract with a DEV guard; bound the measure (the sibling `hover-popover.css:28 max-width:16rem` bounds itself in a sheet whose docblock cites the tooltip register); offsets become declared rationalised parameters + a GLASS-RELAY ask for the missing floating-offset token; per-density side defaults are a re-parameterisation input (26 of 35 accept `top`). Per-site focusable-host/naming work → F.W4; denominators/overlap/parser enumeration → SS-13. ⟨Tooltip; EditorControlsDock C-3; FunctionInput D-11.1⟩ → g11.

### §C.H — Shadow retirement at the installed pin (no F.W1 dependency unless stated)

- **fr-FourierMorphDemo FMD-12** — MAJOR. Four hand-rolled glass surfaces whose seats ship at the CURRENT pin (8 `.info-chip`s; 5 label+control rows; 5 raw `<input type=number>` + ~20 lines spin-button CSS; a hand-rolled overflow strip) vs `./labeled-field ./number-field ./fading-scroll ./badge ./metric-badge` all in the installed 4.0.0 export map. **Critical scheduling fact (verbatim)**: "The component imports NONE of the census's break subpaths, so F.W1's grep will not find these." Chip-arm caution FOLDS → banked FM-14: `./metric-badge` GONE at 8.0.0 — target `./chip`. Mapping rider (MorphShapePreview D-11): 3×`Metric` + 1×`Chip.tone`. L-06's API re-shape belongs to the re-basing decision, KISS. F.W3 owns it TODAY. FR-MSP-1 checks F.W3's outcome first. **`fr-MorphShapePreview FR-MSP-11` joins by id** *(adopted in repair round 1 — sole-`F.W3` route, `fr-MorphShapePreview.md:103`, MINOR, "ADJUDICATED → **F.W3** — one ring vocabulary chosen at the FMD-12 re-basing")*: a cross-component state-vocabulary collision inside ONE view — the primary control's HOVER (`:108-112`, `--accent-red` border + `0 0 0 3px` 15%-mix flat ring) is the same recipe as a grid cell's SELECTED state (`HarmonicLevelGrid:251-254`, `--accent-red` + `0 0 0 2px` 15%-mix ring), differing by 3px vs 2px — *"'the pointer is here' and 'this is the current level' wear one costume, on screen simultaneously."* Within the grid the two states ARE distinguished (`:242-245`: 50% border-mix + `scale(1.04)`) — *"proving per-component authorship never reconciled across components."* Identity guard carried: **HLG-9 books the grid's literals and ARIA absence, NOT the collision** — this row is the collision and is not folded into HLG-9. The re-basing chooses ONE ring vocabulary across both files or the row is not discharged. **`fr-MorphShapePreview L-15 ⊕ L-18` joins as the INFO context row** *(same round; `:74`, "**INFO** — context records for F.W3")*: *"64% of the file is CSS serving two concerns; instance arithmetic exact (1+1 consumers, 8 unrolled chips, 12 grid instances)."* Carried as the re-basing's sizing context — measurements, not a defect; they bound the edit, they do not authorise one. **Homonym note (anti-rename)**: this `L-18` is `fr-MorphShapePreview`'s and is NOT `fr-CanvasControlsDock L-18` (which folds to `fr-BasisCanvas BC-10/C-9` and is F.W4's, §X.1) — both are carried qualified. DISSENT-note: D-13 books the design inversion (primary affordance wears a decoration-only CARD recipe while secondary Export/Reset are genuine glass Buttons) as re-basing, not break-cure. ⟨FourierMorphDemo; HarmonicLevelGrid HLG-11/12; MorphPhaseConfig MPC-11; MorphShapePreview D-11⊕L-14⊕C-4, D-13, L-06; BasisSelector m-14⟩ → g12.
- **fr-AdminUserList FR-AUL-6 (⊕ fr-FunctionInput D-1 + C-5 ⊕ FR-GSB-10/FR-GSB-12)** — MAJOR. Raw `<input>`s with `outline-none` + a hand-rolled recipe core annihilate the focus indicator under forced-colors (v4 `.outline-hidden` absent from the build), while `./forms` → `Input` (`input-pill`, one of the nine forced-colors restored selectors) ships. **Verbatim**: "`./forms` survives at producer 7.0.0 … One import cures the forced-colors loss, the `focus:`-vs-`focus-visible:` aggravation, and part of the header height stagger, with ZERO uplift debt." One repair unit per consumer (D-1 + C-5 together; FR-GSB-12's tokened placeholder cures the 2.02:1/2.70:1 placeholder). Reader-2's nick adopted: the RECIPE CORE is what triplicates, not bytes. ⟨AdminUserList; FunctionInput; GallerySearchBar⟩ → g12.
- **fr-AdminFlaggedPanel FR-AFP-61 (⊕ fr-AdminAuditLog AA-19)** — MAJOR (α-miss-4). Flag pill and moderation card are bespoke `./badge`/`./card` re-implementations, both exported AT THE PIN; `<Badge variant="destructive" size="sm">` resolves the worst-contrast row TODAY. **SCHEDULING RULING (verbatim)**: "booking that cure under the F.W1 uplift is a scheduling error" — F.W3 now; F.W1 for the tone re-grammar only. **CENSUS AMENDMENT CARRIED (AA-19)**: "the file-granular shadow table cannot see FRAGMENT shadows (this chip, the spinner, the raw footer)." `./chip` NOT exported at 4.0.0 — challenge-D's §7 home corrected. Violet/batch tone has no TONES home → the AA-3 relay line. ⟨AdminFlaggedPanel; AdminAuditLog⟩
- **fr-CoefficientsSpectrum M-6 — THE FadingScroll ADOPTION FAMILY** — MAJOR. Unnamed 300px scroll ports, no tabindex/role/name — keyboard users press 'Show more' and reach nothing — while `./fading-scroll` ships at BOTH pins (producer's own note: "Unnamed ports remain ordinary focusable scroll containers"), ZERO imports. Cure: `<FadingScroll axis="y">`, available NOW. fr-FourierShapeExtractor M-3 books the INSTANCE only and folds by reference (its overflow is unconditional and horizontal — the caveat does not apply). fr-EquationView D·D-M14's desktop-only state-blind `::after` fade dies in the same adoption. Keyboard consequence + `overscroll-behavior` → SS-13. ⟨CoefficientsSpectrum; EquationView; ConvergenceLegend D-7; FourierShapeExtractor M-3; HarmonicLevelGrid HLG-12; CoefficientsPanel FR-CP-10; EqCoefficientsPanel FR-EQC-6⟩ → g12.
- **fr-ImageUpload roster 10 (⊕ roster 4, fr-ContourPreview rows 3 + 24)** — MAJOR. The LAST bespoke panel in a stack whose mount host declares the migration complete; 47 lines of hand-rolled indeterminate progress beside `./progress` (variant `gradient`, `indeterminate`) at the INSTALLED pin. Cure: ConfiguratorLayer adoption (carries rosters 14 and 18) + `glass-ui/progress` (also closes roster 24's PRM residue — the correct routing is a NON-MOTION channel, not a new PRM carve). **INTERLOCK LOCK (verbatim)**: "adoption must CARRY the `<h3>` (ConfiguratorLayer supplies no heading) or the route drops to ZERO headings." K-12 correction: installed 4.0.0 exports `./toast` AND `./progress` AND `./card` — F.W3/W4 work, NOT F.W1 items. ContourPreview row 3's own `<ConfiguratorLayer>` adoption carries row 24 free (the producer trigger ships `:focus-visible` box-shadow). ⟨ImageUpload; ContourPreview⟩
- **fr-EquationModeToggle FR-EMT-7 (⊕ FR-EMT-4, FR-EMT-9, FR-EMT-12, K-7)** — MAJOR. An 80-line hand-rolled binary segmented control 82 lines below its own parent's import and mount of `SegmentedTabs`. Cure: retire onto `SegmentedTabs` (`./toggle-group` also a real 4.0.0 export, K-1). **K-7 KILL (verbatim)**: "L-M4's routing hedge — '4.0.0 availability PLAUSIBLE, not CONFIRMED; route to F.W3 post-uplift' — KILLED (the `"option"` scoped slot compiles into both render branches at the pin). The F.W3 routing stands for the correct reason (shadow-retirement wave), with ZERO F.W1 dependency." FR-EMT-4/9/12 DISSOLVE in the fold. **Escalation carried into the fold**: FR-EMT-2 (the `overflow:hidden` clipping the producer's outset focus ring) escalates post-uplift (≥7.0.0 adds `contain: paint` to `.glass-wash` — deleting `overflow` alone stops being sufficient). ⟨EquationModeToggle⟩
- **fr-BasisSelector M-13 (⊕ M-4, fr-NotationPills FR-NP-6)** — MAJOR. The pill row hand-rolls ~46 lines of what `./toggle-chip`/`./toggle-group` ship (0 imports repo-wide); `ToggleGroup type="single"` natively expresses the tri-state cycler. **MIGRATION RIDER CONFIRMED**: `./toggle-chip` GONE at 7.0.0, `./chip` present — target `./chip` under co-resident uplift. **CENSUS CORRECTION (fr-FunctionInput L-m5, NO-WAVE-OWNER)**: `./chip` ABSENT at installed 4.0.0, `./toggle-chip` present; CENSUS:100 dropped the version qualifier lane-frontend.md:441 carries. **FR-NP-6 receipt**: `./tabs` present at ALL THREE points and `SegmentedTabs` RETAINS `variant` across the hop that deletes Button's — "the deadlock does NOT gate the a11y repair." **BUDGET CORRECTION (fr-FunctionInput C §0 row 7, NO-WAVE-OWNER)**: lane-frontend.md:441's "currently 6× Button" is FALSE — ONE callsite in a `v-for` over 3 options; the budget is 1 line. ⟨BasisSelector; NotationPills; FunctionInput⟩
- **fr-GalleryFeaturedCarousel FR-GFC-7** — MAJOR. Four anonymous divs + a styled `<span>`: no landmark/heading/carousel semantics/prev-next, while `./carousel` ships `aria-roledescription`, ArrowLeft/Right, `CarouselPrevious/Next` at BOTH pins. Cure: adopt `./carousel`. Cost honoured: the Embla peer trio is hoisted but `embla-carousel-vue` is undeclared in `web/package.json` — a one-line **F.W0** cost. FR-GFC-5's edge-fade (a verbatim static instance of the producer's retired R8-08 'Shy' defect at HALF the tokenized ramp) and FR-GFC-6's computed `overflow-y:auto` clipping the featured glow ride the same re-derivation. ⟨GalleryFeaturedCarousel⟩
- **fr-GalleryDraftsSection M-2 — "THE CURE IS THE PRIMITIVE, NEVER THE WRAPPER"** — MAJOR. Hand-rolled disclosure, header span byte-identical to `ui/CollapsibleSection.vue:39`, `./collapsible` exported at the pin. **CURE LOCK (verbatim)**: "The cure is the PRIMITIVE, never the wrapper — K-6: the wrapper is banked-defective across exactly the axes the cure would buy (B-1 hang at the F.W1 target, M-2 unmountOnHide teardown, M-3 ungated scrollIntoView, M-6 no controlled open)." Available TODAY. New consumers are never routed through the wrapper. ⟨GalleryDraftsSection; CollapsibleSection⟩
- **fr-CollapsibleSection i-4 — THE COLLAPSIBLE-VS-ACCORDION DECISION** — INFO (decision input) over MAJOR defects. glass-ui 7.0.0 ships the heading-emitting disclosure (`AccordionTrigger.vue:41` `<RekaAccordionHeader as="h3">`, hover, 44px floor) while the local wrapper emits zero heading semantics, no hover at either pin, ~32px trigger, and an unforwarded `unmountOnHide`. **ONE DECISION, the KISS reading**: Collapsible-vs-Accordion for titled sections converts M-1 + M-5 + m-9 into a single choice. m-15's one-token cure stands (`block:'nearest'` deletes the probe, the predicate, both forced reflows and the dead arm — M-4 dies with it); i-5: the repo already holds the JS reduced-motion idiom M-3 needs (two-line in-tree copy); m-16: `overflow:hidden` is permanent and clips the producer's outward focus ring. m-7's ContourSettings fork is UNAVOIDABLE given the wrapper's surface — the cure is a `#title` slot + `variant`, not discipline. M-5's producer question → BH relay ("is the bare Collapsible trigger MEANT to ship affordance-free? cite the Accordion asymmetry"). DISSENT preserved: B-1's LC MAJOR as the uplift-conditional qualifier on the BLOCKER. ⟨CollapsibleSection⟩
- **fr-CoefficientsPanel FR-CP-33 (⊕ fr-EqCoefficientsPanel FR-EQC-11, fr-CoefficientsSpectrum m-11)** — MINOR. One shared body, two disclosure chassis with divergent mount lifecycles (ConfiguratorLayer mounted-collapsed `aria-hidden`+`inert` vs CollapsibleSection Presence-unmount); root-barrel glass import defeating the ~60-subpath split; an uncleared 250ms timer against a detachable element. Cure: converge the twins on ONE chassis; import-hygiene arm rides F.W1. **The asymmetry must be RECORDED** — it decides where FR-CP-35/M-15 runs and where the `expanded` latch persists. FR-EQC-11 ("neither register because it is neither") dies inside the convergence. DISSENT preserved: LC's MAJOR on FR-CP-33 and DU's MAJOR on FR-CP-34 against the banked MINOR. ⟨CoefficientsPanel; EqCoefficientsPanel; CoefficientsSpectrum⟩
- **fr-AppHeader FR-AH-1's F.W3 rider (⊕ fr-DarkModeToggle I-2/SR-2, N-10, N-11)** — MAJOR host (F.W4) / MINOR reconcile (here). **SHADOW-RECONCILE, NOT REPLACE (I-2 RATIFIED, verbatim)**: "glass-ui 4.0.0 ALREADY exports `DarkModeToggle` at `./controls` — the shadow-reconcile seat is live today, not a 7.0.0 wait." **SR-2 RE-BASELINE**: at 8.0.0 `./controls` is GONE — the target moves to `./dark-mode-toggle`; `./dark` remains; "Every 'at 7.0.0' disposition in this folder re-reads as 'at 8.0.0'." N-10/N-11 resolve INSIDE the reconcile. Gated on the P-10 re-pin ruling. The 450,631 B eager-payload row itself is **F.W4**. ⟨AppHeader; DarkModeToggle⟩
- **fr-App MG-γ ⊕ m-2/L-3/C-7 ⊕ L-6 (the one dark-sync item)** — MAJOR. **`web/index.html`**'s pre-paint script (the ONE real file — R-10.2) mishandles `"auto"` (both arms fail — dark-OS users get a guaranteed light flash for the initial chunk fetch) while the app hand-rolls a never-disconnected MutationObserver (5 `getComputedStyle` reads per firing) beside the producer's `installDarkModeSync`. **ONE ITEM**: adopt `@mkbabb/glass-ui/dark` — "the dependency edge already exists (`DarkModeToggle.vue:18`), the adoption is free," and it is the SAME edit as MG-γ's FOUC cure, "where the MAJOR weight of this family actually lives." L-6 folds in (a `useVizColorSync()` returning a stop handle costs no new edge); R-8 coupling rider on `colorScheme`. DISSENT preserved: LC's MAJOR on m-2/L-3/C-7 against the ruled MINOR (R-7). ⟨App; DarkModeToggle r2 missed #1, D-I2/C-13/L-17⟩
- **THE APP-SHELL ESCAPE SET — `fr-App MG-α ⊕ MG-ε ⊕ MG-η`** *(adopted in repair round 2, ruling R2-3b under the TWIN LAW: all three are **route-marked bullets** the round-1 table-shape roster could not see; all three target `App.vue` / `AppHeader.vue`, which are **`.e`'s** by §5e, so the dual spelling homes at **F.W3**)*. Quoted from the corpus by command — ⟨cmd⟩ `grep -rn -- 'MG-α' fr-*.md` (likewise `MG-ε`, `MG-η`), this seat, 2026-08-28:
  - **`MG-α`** (`fr-App.md:58`, MAJOR) — *"Scroll position is never restored and structurally cannot be"*: `App.vue:24-26` makes `<main>` the sole scroller (document scroll range = 0) while the router declares no `scrollBehavior` — *"vue-router's hook is absent AND would target `window`, and native `history.scrollRestoration` has nothing to restore. Every back-navigation into the book-length `/paper` treatise lands at section 1."* Banked self-criticism carried: *"The single UX cost of the shell's (good) scroller decision, and all three axes walked past it."* **Cure shape is constrained by the mechanism**: a `scrollBehavior` hook alone is a no-op here — the restore must address `<main>`, which is why this books with the shell rows and not as a router one-liner. It composes with `★NAV-1`'s scroll-domain de-fork (§C.D) — **one scroll authority for the paper route, decided once**.
  - **`MG-ε`** (`:78`, MINOR, *"missed find, BOTH readers independently — banked once"*) — `AppHeader`'s `sticky top-0` + `backdrop-blur-md` glass are **structurally inert**: the header is a flex SIBLING of `<main>` (the sole scroller), its own scrollport is the `overflow-hidden` shell whose scrollTop is pinned at 0 — *"`sticky` never engages and nothing ever paints behind the blur"*, at the cost of a permanent compositing layer. Banked disposition preserved verbatim: *"Repair is a design choice: move the header inside the scroller (making both real) or retire the treatment."* **It is therefore RECORDED AS A DECISION, not executed blind** — the at-rest screenshot the record asks for is the record's own SS-13 item (fr-App probe 8), routed to S-9; a silent flip either way is the failure mode.
  - **`MG-η`** (`:80`, MINOR) — *"`aria-current` appears nowhere in the tree"*; the nav dropdown marks the active section by decoration only, *"a SR user hears five undifferentiated items."* Banked cure verbatim: *"Two-line cure in the same edit as M-1."* **This is a FIFTH member of the §C.F five-spelling family by mechanism** — the decoration-only active state is exactly `FR-COB-3`'s subject — so it lands under the SWEEP-LAW RIDER, and the sweep may **not** key on a bare `is-active` grep (the nav's binding is `:class="{ 'is-active': … }"`, which the prop-corrupting grep would also hit).
  - **`MG-λ` is NOT booked here, and that is the LAW discriminating rather than an omission**: its target is `composables/useToast.ts`, which is in **no** §5 unit file list and **no** §1 bounds row, so the dual spelling homes at **F.W4** by default (R2-3a item 2). It is named in §X.1 with that holder. A criterion that never excludes anything is not a criterion. ⟨App; AppHeader⟩ → g9, g19.
- **fr-App L-5 ⊕ L-8 ⊕ m-1 (the SvgFilters scrub) — DELETE-VS-REWIRE** — MAJOR. 178 lines of dead DOM mounted unconditionally, all four filter ids orphaned; two `useLineBoil` clocks mutate `baseFrequency` on unreferenced nodes ~13×/s for the tab's life. **THE DELETE-EVIDENCE SET IS DECISIVE**: no `seed` variation + un-centred turbulence DC bias ("the animated register cannot produce the register it names"; "REWIRE-as-is is not risky but pointless" — M-3/R-6); full-amplitude grain vs a producer that attenuates three ways ("rewiring as-authored harms legibility" — D-4); `stitchTiles="stitch"` in objectBoundingBox geometry (M-4). **SAME-CUT TRAP (R-1/L-2)**: the pencil-boil module-scope `visibilitychange` listener SURVIVES the deletion and WIDENS to every user (`sideEffects:false` is a false manifest; barrel `index.ts:17` + `svg-fourier.ts:11` + `FourierShapeExtractor.vue:144` keep it installed); cure floor verified at pencil-boil v0.9.2 (`hasActiveSubscriber()` guard + reactive-PRM MQL listener). Census correction rides: §3a's "12 SVG surfaces" → 8 live. Bundler-contingency check = one `vite build` + chunk grep. D-5 re-grades MAJOR automatically if F.W3/W4 rules REWIRE. DISSENTS preserved: reader-2's MINOR on M-3, reader-1's MAJOR on D-5; the delete-vs-rewire ruling is the wave's. ⟨App; SvgFilters⟩
- **fr-AdminUserList FR-AUL-28 (⊕ fr-GalleryView FR-GV-4, fr-GalleryMarquee GM-22)** — MINOR (duplication) / MAJOR (the live `confirm()`). Three hand-assemblies of one confirm structure + the FOURTH consumer the enumeration missed: the frontend's last live `window.confirm()` at `GalleryView.vue:138`, on the exact verb the batch Dialog twenty lines away styles. **CURE LAW (both readers, ratified, verbatim)**: "`@mkbabb/glass-ui/confirm-dialog` is TRUE at the installed 4.0.0 and ABSENT at v7.0.0/v8.0.0 — the cure is the in-file Dialog pattern" and "a fourier-local shared component over `./dialog` (which survives 7.0.0), never the retired primitive" (K1 kills the prescribed `ConfirmDialog` swap). BH RELAY (rerouted): "`./confirm-dialog` was retired at 7.0.0 with three hand-rolled consumers standing." DISSENT 4 preserved: "The F.W1 spec must restate its target tag before this ledger executes." ⟨AdminUserList; GalleryView; GalleryMarquee⟩

### §C.I — Structural consolidations

- **fr-PaperArticleWindow PAW-8 (⊕ PAW-20, PAW-41, PAW-22→GCM-23, PAW-53)** — MAJOR. `.callout-btn` is a divergent twin (identical CTA, contradictory chassis, one shared class, bare `<router-link>` beside six glass-Button siblings) under one shared UNLAYERED global focus rule. Cure: glass Button `as-child`, thin skin — "the R3-7a precedent shape." PAW-20 dies inside the adoption; PAW-41's off-token radii ride it. PAW-22 FOLDS to banked GCM-23 (drop `border-radius: inherit` rule-wide) with **K-5's LOCK**: deletion must be SITE-SCOPED — this bare router-link has no other indicator. **PAW-53 RIDER (verbatim)**: "NOT discharged by it automatically (SC-10): the adopted chassis needs a real border under WHC or the glass-ui rung extended to it" → BH relay note. Composes with PAW-48's cartoon-surface adoption in one edit. ⟨PaperArticleWindow; GalleryCardModal GCM-23/GCM-33/K-5⟩
- **fr-FullscreenViewer FV-2 (⊕ FV-1, FV-3, FV-5, FV-6, FV-7, FV-10, FV-17)** — BLOCKER. Full dialog behaviour, zero dialog semantics, over a `<Teleport to="body">` backdrop at `--z-fullscreen` (150) painting every glass floating primitive (≤130) behind it; the trap's visibility predicate blind to `visibility:hidden`+`inert`. Cure: **ADOPT THE DIALOG CHASSIS** — in-tree one directory over (`ExportModal.vue` imports `@mkbabb/glass-ui/dialog`). "FV-3/FV-5/FV-6/FV-10/FV-17 all dissolve into the same adoption; the z-tier caveat of R-6 rides." FV-7's structurally dead `@click.self` is replaced by chassis outside-click semantics. FB-1 (the second `<ContourEditorCanvas>` with no ref/listeners — fullscreen edits silently discarded) rides fr-ContourEditorCanvas D/B-5's banked BLOCKER with an F.W1 sequencing rider. ⟨FullscreenViewer; ContourEditorCanvas⟩
- **fr-AdminUserList FR-AUL-17 — ONE SHARED timeAgo** — MAJOR. Exactly five divergent copies in two dialects, no sub-minute floor/cap/negative guard/`<time>`/reactive clock/NaN guard; ONE router serializes the same concept two ways. Cure: ONE shared util, one dialect, CAPPED AND GUARDED. m-17 rider: the relative-time REFERENT collides across adjacent tabs — the cure is a word or `<time :datetime>`. F.W3 owns the util; SERIALIZER UNIFICATION → F.W5-W8; JSC date acceptance → SS-13. Every other record FOLDS BY REFERENCE (fr-GalleryCard L·M-4 rules "nothing new to book"; GM R-5 is the enforcement event). **`fr-GalleryCardModal GCM-34` is named BY ITS BANKED ID** *(repair round 1 — the id was previously carried only inside the shorthand "GCM-29/34", which the anti-rename law does not accept as a booking)*: `fr-GalleryCardModal.md:85`, routed **FOLD (F.W3)** — *"`timeAgo` truncates at days forever and samples `Date.now()` during render, no ticker. **FOLD → FR-AUL-17.**"* The fold target is this row; the cap, the guard and the reactive clock are exactly what discharge it, so GCM-34 closes when g15's ONE `timeAgo` lands — but it closes **as GCM-34**, and a close transcript that cannot grep the id has not closed it. ⟨AdminUserList; AdminFlaggedPanel FR-AFP-32; GalleryCard; GalleryCardModal GCM-29 / GCM-34; GalleryDraftsSection F-2/m-9/m-17; GalleryMarquee GM-F14/R-5⟩ → g15.
- **fr-BasisSelector M-10 — THE BASIS-KEY CANON** — MAJOR. The basis-key domain duplicated tree-wide while its 7-LOC owner holds none of it: `startsWith("fourier")` ×7, the mode-label map ×4, the gallery pair's 15-line `basisLabels` verbatim clone, `basisDisplay` keying 3 names against an emitted vocabulary of 4. Cure: **ONE REPAIR UNIT** — `BasisKey` union + `normalizeBasisKey()` + `basisModeLabel()` + `satisfies` ("the repair deletes two whole functions, not two expressions"). **WAVE-LOCK CARRIED (fr-GallerySearchBar ruling 5, verbatim)**: "carried as a WAVE-LOCK on any F.W5-W8 wiring of `basisFilter` — wire it without the banked normaliser and the Fourier pill returns zero rows against `active_bases ∈ {fourier-epicycles, fourier-series}`." FLEET LESSON carried (GDS DISSENT-2): a component-name-keyed registry sweep "structurally cannot find defect-identity folds." ⟨BasisSelector; GallerySearchBar FR-GSB-6; GalleryDraftsSection F-1/R-3; GalleryCardModal GCM-29; GalleryCard L·D-5; intake R3-12⟩ → g15.
- **fr-BasisSelector M-6 (≡ fr-SliderControl R-8, ⊕ HLG-11, MPC-11)** — MAJOR. The census-blessed labelled-slider chassis bypassed and hand-copied (`.inline-number` byte-identical 13 hits / 2 files; clamp regressed from `Number.isFinite` to `|| min`; `<label>` lost), the chassis itself used by 2 of 8. Cure: converge on the chassis AND fix the chassis in the same motion (R-6's wrapper law governs its prop surface). K-11 kills the visible-divergence half: all 23 `--slider-scrub-*` declarations are dead, every slider renders identically today — the cure is structural, not visual. Lands with the R-1/B-2 token cure and the R-2/M-N8 NumberField unit. ⟨BasisSelector; SliderControl; HarmonicLevelGrid; MorphPhaseConfig⟩
- **fr-AdminUserList FR-AUL-27 / FR-AUL-18 / FR-AUL-51 / FR-AUL-52 (⊕ FR-AFP-38, FR-AFP-39)** — MAJOR/MINOR. 529 LOC / ~7 concerns with one seam already cloned token-for-token; the batch toolbar authored twice divergent on six positioning decisions (one pushes the just-clicked row out from under the pointer); no shared cursor composable; a 25%-dead `useOffsetPagination` surface; page numbers re-derived by inverting an offset computed one line earlier. Cure: extract `useAdminMultiSelect` + `useAdminAction` ("each seam has a second consumer today"); one shared local toolbar (D-17's z-10/z-20 residue folds here); extract `useCursorPagination`; fix the `fetchFn(limit, offset)` signature so authoritative `page`/`pages` survive. FR-AFP-39's zero-rule `<style scoped>` (4 files, ~40 elements stamped) = AA-29, fold by reference. Result-surface consequences → F.W4. **`fr-AdminFlaggedPanel FR-AFP-33`'s CLIENT limb is adopted HERE** *(repair round 1, ruling R-2c — the row is routed `server → **F.W5-W8**; client collapse → **F.W1/W3**`, and **F.W3 is the adopting wave for the client limb**; F.W1 cites this row rather than re-booking it)*: the flag list is *"unbounded on both ends (server `$push` no `$slice`; client no cap/collapse — `./expandable-container` installed)"*, banked at `fr-AdminFlaggedPanel.md:87` (D-17/L-20/L-22). **What lands here**: the client-side cap/collapse over the installed `./expandable-container`, inside the same `useCursorPagination` extraction that this bullet already books — one repair unit, not a second pass over the file. **What does NOT**: the server `$push`/`$slice` half and the bare `$group`-per-page aggregate (permanent-growth term = FR-AFP-66) are **F.W5–W8's** (S-4), and the counterweight is preserved verbatim — *"`if not flagged` short-circuits production's only case"* — so no F.W3 cell may grade the server limb urgent. Magnitude + β's sort-index note → SS-13, and per `fr-AdminFlaggedPanel.md:160` the 60-flag geometry **needs a seeded fixture no product path can produce** (FR-AFP-1) — the collapse cure lands on the mechanism, never on an unproducible screenshot. ⟨AdminUserList; AdminFlaggedPanel⟩
- **fr-GalleryCardModal GCM-47 (⊕ fr-GalleryCard G-LC3, fr-GalleryFeaturedCarousel FR-GFC-24, fr-GallerySearchBar FR-GSB-29)** — MINOR. The closed `GalleryTier` union re-declared inline 8-9 sites / 7-8 files (verbatim ×6, hand-widened ×2) against ONE canonical export the tree already imports; the `sort` union authored ×3 with no canon, none matching the server's five keys. Cure: import the type; mint the sort canon. Adjacent-but-distinct from banked FR-AFP-59/31 (the WIDENING direction) — this is RE-DECLARATION; rides the M-10-style single-home unit, with GCM-39's `hash`→slug rename at source. CORRECTION on the record: D·S-3's superlative credit — the canonical export delivers the same guarantee plus a single home. ⟨GalleryCardModal; GalleryCard; GalleryFeaturedCarousel; GallerySearchBar; AdminFlaggedPanel FR-AFP-59/31⟩ → g15.
- **fr-CoefficientsPanel FR-CP-28 (⊕ fr-CoefficientsSpectrum m-14/m-15, fr-UserSlugBar FR-USB-26, fr-FunctionInput L-M5, fr-EquationPanel M-N14, fr-PathPreview PP-KIT)** — MINOR (DU's MAJOR-library preserved as dissent). Colocation inverted tree-wide: a 247-line canvas under `equation/` consumed only from `visualization/`; `shared/` holds one file; UserSlugBar filed under `visualization/gallery/` consumed only by `layout/AppHeader.vue`; ~36 lines of pure notation library trapped in an SFC; `lib/equation/index.ts` a wholly dead 4-line barrel dragging the 672-LOC fetch core into any entrant's graph. Cure: structure/colocation sweep with the FR-CP-8/FR-CP-6 edits; extract the notation library to `lib/equation/` — "extraction + the missing unit tests are one motion"; DELETE the dead barrel. Fleet dead-barrel class member. Census correction noted for SS-3/SS-4 (lane-frontend:133 mis-attributes the render path); F.W0 rider: no lint step can report the dead barrel. ⟨CoefficientsPanel; CoefficientsSpectrum; UserSlugBar; FunctionInput; EquationPanel; PathPreview⟩
- **fr-AdminFlaggedPanel FR-AFP-34 / FR-AFP-59 / FR-AFP-62 / FR-AFP-45 (admin suffusion riders)** — MINOR. FR-AFP-34 RIDES R3-7a's title→Tooltip migration (fold by reference): one label source, no `title`, drop "(save tier)". FR-AFP-59 reuses the sibling `data-tier` recipe + FR-AFP-31's narrowing in the same edit. FR-AFP-62: two-file sweep to `rounded-card` (semantic 16px vs resolved 10px). FR-AFP-45 re-semantics icon+label (reject glyph tinted green for benign dismiss), composing with FR-AFP-19's rest-state tone — **which is F.W1's**. ⟨AdminFlaggedPanel⟩
- **fr-EquationView D·D-M5 — THE F8-REACH-01 STANDING DEMAND** — MINOR (orphan limb); **HOLD is already RED**. `equation/InfoCard.vue` is one of exactly two zero-inbound SFCs, disposed-in-demand but unexecuted, drifting from its live twin twice + a duplicated computed pair, while the frozen tree spends a `:amount=`→`:value=` edit on it. Cure: EXECUTE the standing demand — F8-REACH-01 requires exact KEEP_WITH_MOUNT, ISOLATED_HARNESS, or DELETE. Budget correction rides: lane-frontend's `./metric-badge` "7 imports / 6 files" is 5 live + 1 unmountable. F.W0 rules BOTH REACH rows in one breath; **F.W3 executes the InfoCard disposition**. DISSENT (RD-7): reader-L's MAJOR preserved; its computed-duplication increment ADOPTED. ⟨EquationView; CanvasOverlayButton R-6/FR-COB-10⟩
- **fr-EquationView L·m-8 (⊕ fr-EqCoefficientsPanel FR-EQC-9)** — MINOR. A second UNSCOPED `<style>` ships the global `.info-hovercard` from a lazy route chunk; `slide-down` defined ×4 with divergent durations, the equation-route copy declaring no PRM bracket. Cure: the unscoped global dies with the hover-card migration (K-4's popover destination); one recipe, one PRM intent, DECLARED. fr-ContourSettings m-2 holds that file's instance; this row books the dedup + the EquationView instance. The mitigation-by-substrate-allowlist accident must not be inherited as intent. ⟨EquationView; EqCoefficientsPanel; ContourSettings⟩

- **THE `.e` ESCAPE SET — `fr-VisualizationView MAJ-5 ⊕ MAJ-7`, `fr-InfoCard FR-IC-16`, `fr-GallerySearchBar FR-GSB-30`** *(adopted in repair round 2, ruling R2-3b under the TWIN LAW — four route-marked bullets whose target files (`VisualizationView.vue`, `equation/InfoCard.vue`, `visualization/gallery/GallerySearchBar.vue`) are all **`.e`'s** by §5e, so the dual spelling homes at **F.W3**)*. Quoted by command — ⟨cmd⟩ `grep -rn -- 'MAJ-5' fr-*.md` (likewise `MAJ-7`, `FR-IC-16`, `FR-GSB-30`), this seat, 2026-08-28:
  - **`fr-VisualizationView MAJ-5`** (`fr-VisualizationView.md:59`, MAJOR) — banked verbatim: *"**MAJ-5 — MAJOR · FOLD → fr-ContourSettings.md M-16** *(r1-carried)*. `v-model` over zero `defineEmits`, twice; same-tag idiom split at `:265-267`. → **F.W3/W4**."* **It lands as a NAMED FOLD, and the fold target is booked with it** — `fr-ContourSettings M-16` (banked `fr-ContourSettings.md:73`, *"M-16 = C-9 / L-M3(contract) / D-i2 — MAJOR (R-1)"*: both mount sites bind `v-model:n-harmonics`/`v-model:n-points` over a component declaring props only, *"a contract that advertises writeback the panel structurally cannot h[onour]"*; the orphan-DOM-listener leg was KILLED at bank via `isModelListener`, K-4, and only the CONTRACT half survives). ⟨cmd⟩ this seat: `grep -c 'ContourSettings M-16' F-W3.md` → **0** before this repair — a fold whose target no spec names is not a fold, it is a drop. `.d` owns `ContourSettings.vue` and `.e` owns `VisualizationView.vue`, so the contract repair is authored at the panel (`.d`) and the two call sites follow (`.e`) — **sequenced `.d` → `.e`**, added to §5a as split (4).
  - **`fr-VisualizationView MAJ-7`** (`:61`, MAJOR) — banked verbatim: *"One failed save, two toasts; the component discards the diagnostic it holds and volunteers a retired noun."* Two rows in one: the duplicate channel and the **false noun**. Cure = one error channel per action, surfacing the diagnostic the component already holds. **Anti-rename note**: this `MAJ-7` is `fr-VisualizationView`'s; the `roster MAJ-7` named inside `fr-EditorControlsDock`'s K-4 kill row is a different record's token and is not this row — both are carried qualified.
  - **`fr-InfoCard FR-IC-16`** (`fr-InfoCard.md:48`, MINOR, *"the craft tail"*, amended three ways) — it rides the standing F8-REACH-01 disposition this wave already executes (`fr-EquationView D·D-M5`, §C.I), and it carries a **correction the wave must not re-inherit**: banked r1 prose *"the twin's HoverCard announces on open"* is FALSE and inverts (ruling R2-3) — the twin's trigger is nameless and pointer-only (`D·D-B2` / `D·D-M3`), so *"mounting InfoCard would not be an a11y regression relative to the status quo."* **Consequence, binding**: the KEEP_WITH_MOUNT / ISOLATED_HARNESS / DELETE ruling may not be argued from the killed comparative. D-11's mobile column is RESTORED to 30 vs 27 (K-16).
  - **`fr-GallerySearchBar FR-GSB-30`** (`fr-GallerySearchBar.md:70`, MINOR band) — the band, verbatim-sampled: `max-width: 32rem` duplicated with no shared property; `justify-content: flex-start` dead *"on corrected grounds (K-10: live when the root exceeds 32rem; dead because `normal` behaves as `flex-start`)"*; **dual placeholder authorities** (the `:52` utility inert under the unlayered scoped `:155-157` rule — *"the utility is also dead code"*, which is why banked `FR-GSB-12`'s tokened-placeholder cure **rides FR-GSB-30 and cannot land without it**); the hand-rolled divider at **1.22:1 / 1.26:1** — *"functionally invisible"* — while `./separator` ships at BOTH pins; no `role="search"`, no `role="group"`/label; `hasActiveFilters` *"folds `sort !== "newest"` into a predicate named filters and omits `searchQuery`"*; the emitted query never trimmed; four hand-written prop/emit pairs where four `defineModel`s collapse 13 lines here + 8 at the call site. **It books as ONE band with the FR-AUL-6 `./forms` adoption** (`.e` already opens the file for FR-GSB-12) — the divider leg is a `./separator` adoption, not a contrast nudge, and the dead-utility leg must be deleted in the same hunk that re-inks the placeholder or the two authorities survive the cure. ⟨VisualizationView; ContourSettings M-16; InfoCard; GallerySearchBar; EquationView D·D-M5⟩ → g12, g15, g19.

### §C.J — Standing laws and the four named anti-cures (binding on every row above; cross-cited into F.W4's edit budget)

- **P-11 LAW — "A CURE IS ADJUDICATED SEPARATELY FROM THE DEFECT"** — standing clause, binding on every F.W3 row. A proposedCure inherits NO evidential weight from its defect's evidence — the registry has watched prescribed cures authored from `.d.ts` surfaces twice against one record in one day. **Operational form**: every carry out of a record "re-verifies its cure against BOTH pins before landing." Named instances carried: **LC-3** (`LabeledSwitch` not drop-in — column-stacked full-width, label-first, REQUIRED `tooltip`, `cursor:help`, `v-model:checked`; "accept the anatomy explicitly, or take the narrower `id`/`for` local wiring"); **M-α** (`:show-close="false"` gone at 8.0.0 — "executed in the banked order the cure fails `vue-tsc` or falls through as a dead DOM attribute with the ✕ back on screen"; re-spell `dismiss="deliberate"`, sequence WITH/AFTER F.W1); **§6.3** (`LabeledSlider` ships AT THE PIN — presenting it as an F.W1 gain mis-schedules the repair, and it is not drop-in). ⟨ConvergenceTimeline C·C-7; ExportModal R2-3/LC-3/M-α/§2; SliderControl D§6.3/K-7; GalleryInfiniteGrid FR-GIG-4; MorphShapePreview K-13; ColorSpaceSelector addendum-2 = naming authority⟩
- **P-11 ANTI-CURE 1 — THE NAIVE `@layer` WRAP** — standing. Unlayered scoped SFC CSS outranks every producer `@layer components` rule; consumer two-leg `transition` shorthands silently CLOBBER the producer's six-leg sets (deleting `scale` — presses SNAP; shedding `.tap-squish`'s PRM reset); single-value `box-shadow` strips the rim; `background:transparent` deletes the plate under live blur — the producer documents the hazard IN CAPITALS in both files. **DO NOT EXECUTE the naive `@layer` wrap: it silently LOSES every consumer override.** Only `@layer glass-overrides` appended AFTER utilities preserves them. **THE DURABLE FIX IS A LINT RULE, NOT A PER-COMPONENT EDIT.** Per-site cures carry the FULL six-leg set or delete outright. Class identity banked ONCE at fr-App MG-δ (precedent-controlling home per FR-EMT's R-ruling); further sites book as declarants only. DISSENT preserved: FR-EMT reader-1's MAJOR — "a two-button leaf cannot out-price the primary nav under the same mechanism." ⟨App MG-δ/MG-ζ; EasingPicker MISSED-A; GalleryCard G-DU3; EquationModeToggle FR-EMT-6; GallerySearchBar FR-GSB-14; MobileFloatingToc M-12; NotationPills FR-NP-10; VisualizationView L-13; CoefficientsSpectrum m-21⟩ → g16.
- **ANTI-CURE 2 — THE D-2 TINT-PLATE CURE (fr-MorphShapePreview FR-MSP-6 ⊕ K-13) (P-11)** — MAJOR (the void-cure lock). The phase-tint plate is a DEAD state channel (1.205:1 light / 1.117:1 dark vs a 3:1 floor) while the corpus's D-2 cure cites a 16.21:1 TEXT fact as a PLATE proof. **THE TWO-CHANNEL LOCK (verbatim)**: "any cure must EITHER (deepen the plate to ≥3:1 vs backdrop AND re-pair the text to ≥4.5:1 on the deepened plate) OR add a non-colour channel (hairline border, icon, or the phase text itself once D-15's label map lands) — never a single-channel edit." The clearing mix is 67/79/67/61%, at which point `--foreground` is no longer legal text. Executing the prescribed cure "removes the only working (if sub-AA) state channel and leaves the inert one — a net regression under a green-looking edit." **F.W3 carries the LAW and the do-not-execute entry; the FM-4/FMD-19 contrast edit itself is F.W4's, guarded by this lock.** DISSENT recorded: "arguably the lock is process, not product" — booked MAJOR on the ColorSpaceSelector L-3 anti-projection precedent. ⟨MorphShapePreview⟩
- **ANTI-CURE 3 — THE ariaLabel FORWARD** — see fr-Tooltip FR-TT-4 (§C.G): repudiated by the producer at 8.0.0; re-target required.
- **P-11 ANTI-CURE 4 — THE TOKEN-SWAP THAT LEAVES THE COMPOUNDING PRESS INTACT** — standing. Press transforms COMPOUND (`scale` longhand 0.96/0.97 ∘ consumer `transform: scale(.95)` → net ≈0.912; `cn()` buckets classes, never properties); pressed background wins over hover/active by unlayered source order — a toggled-ON control gives ZERO hover feedback. **A token swap does NOT cure this: the cure must DELETE the consumer's `transform` leg.** Rider: base.css's PRM carve resets only `scale` — the consumer's `transform` survives PRM. **CONSUMER AWARENESS NOTE (FR-COB-7)**: "any fourier pressed-state work must not assume a border channel exists" — the `glass` arm's hover/active border arms are dead fleet-wide; FR-COB-7 itself is producer-owned → NO-WAVE-OWNER / BH relay, TOP of the FR-COB-28 list. ⟨GalleryCard G-DU4; CanvasOverlayButton FR-COB-7⟩
- **fr-GalleryCard G-DU8 ⊕ D-5 (tier-chip plate) + fr-ConvergenceLegend D-15 (disc ring)** — MAJOR/MINOR. The tier chip is markup for a chip that never paints (`rounded-full` on a surface-less div) — the natural home for the plate that would cure the 1.56:1 tier glyph ("Not a stray circle; a missing one" — the recipe is two elements away in `.basis-tint`'s 12% self-tinted plate); ONE cure with D-5. Four of eight spectrum discs under 3:1 with no ring: the disc ring is an ARM of the banked ramp identity (fr-BasisCanvas M-β1 / FR-CP-2) — the OKLCH/`sampleColorRamp` re-home carries lightness co-variation + the disc ring, **one ramp home, never four forks**. BH-relay rider for the `--tier-*` token family (AA-3 precedent). Dark arm → SS-13. ⟨GalleryCard; ConvergenceLegend; BasisCanvas; CoefficientsPanel⟩

### §C.K — Decisions, rulings, and cure-sequencing riders

- **fr-ContourSettings i-3 — THE DEFAULT-COLLAPSED IA CHOICE (P-10)** — INFO, decision-bearing. The one panel owning the entire compute pipeline defaults COLLAPSED, its body inert — on a fresh workspace none of its six controls is reachable. **OWNER RULING OWED, FLAGGED INLINE (verbatim)**: "An IA choice worth a deliberate ruling. → NO-WAVE-OWNER (the F.W3/W4 pass should not silently flip it without the ruling)." Adjacent rider: R-6's identity fold (the `:133` a11y keystone is one of the four fixmes banked inside fr-BasisSelector B-3 — un-fixme is a four-test operation) + the stale-premise text correction ("pending the glass-ui `inert` release" is false against an installed 4.0.0 that emits `inert`). ⟨ContourSettings⟩
- **P-10 — THE 7.0.0-vs-8.0.0 RE-PIN TARGET** — owner ruling, **homed at F.W1, consumed here, never presumed**. Named consequences carried into F.W3: retargeting to 8.0.0 RESURRECTS the `text-admin-label` break (FR-CP-21 must target a surviving rung — `--type-micro`/`--type-caption` + `tabular-nums`, reconciled inside MG-β's F.W1 census gate); `./controls`, `./hover-card`, `./hover-popover`, `./metric-badge`, `./toggle-chip` all vanish; `SliderVariant` loses `standard` (14 more fourier sites join the break surface); `showClose`/`spring`/`scrimAnimation` gone from `DialogContentProps`. "It is an owner/formation decision, not a measurement." The EasingCurve-vs-ToggleChip ruling (§C.C) is downstream of it. ⟨FrequencyGraph; EasingPicker; ContourSettings; PaperSidebar; CanvasOverlayButton; DarkModeToggle SR-2; ConvergenceTimeline SR-1; CoefficientsPanel FR-CP-21⟩
- **fr-EquationModeToggle FR-EMT-21 — THE SHADOW AGGREGATE IS NOT INCREMENTABLE** — MAJOR (corpus meta-defect). **Quoted by command, not from memory (repair round 2, R2-1-LAW 1; PASS-2 D-9 — the round-1 spelling of this quote could not be found in the file it named)** — ⟨cmd⟩ this seat, 2026-08-28, `sed -n '446p' ../../../V/megatranche/formation/fourier/lane-frontend.md`:

  > **Aggregate shadow surface: 9 components / ~1 990 LOC** (127 + 98 + 41 + 109 + 547 + 764 + 168 + 91 + 134 ≈ 2 079 counting the canvas-drawing lib; **1 315 excluding it**).

  The headline and its own parenthetical disagree **in the same sentence**, and the parenthetical offers a THIRD total (`1 315`) for a different member scope — which is the defect: three figures, no published member-scope. Three uncoordinated '10' increments; six 🟡 CANDIDATE members excluded. **The figure is re-derived BY ENUMERATION under X-9's member-scope law BEFORE any F.W3 budget.** Companion census corrections ride: `:382` (prose-only → live producer CSS one import away); `:446` (+1); the goto census (×13, SC-1); CENSUS:191 SPLIT (preview half mechanical, picker half a product decision); the callsite-keyed ~20× understatement; lane-frontend:443's `./metric-stack → ./metric` re-seat; lane-frontend:441's false "6× Button." Gate precedes budget; ownerless methodology rows → NO-WAVE-OWNER register. ⟨EquationModeToggle; ConvergenceTimeline L·D-7; EasingCurvePreview PICKER-RES; CoefficientsSpectrum i-5(a); CoefficientsPanel FR-CP-44; FunctionInput C§0 row 7⟩ → g17.
- **fr-PathPreview PP-DEAD / PP-SHAPE / PP-CLOSE / PP-STROKE / PP-LEN / PP-A11Y / PP-NOSHADOW** — MAJOR/MINOR. A 69-line component in the 5-file shared `ui/` kit with zero render sites and one dead import; split-array props mirroring none of the five `{x,y}` API carriers; unconditional `Z`; cascade-fragile `strokeWidth / size`; NaN-or-misframed on length mismatch; zero a11y. Cure: **DELETE-OR-COMPLETE — a design decision, not a defect fix.** If completed: one `path: {x,y}[] | null` prop makes PP-LEN/PP-NULL impossible by construction (`xyToPoints` already zips); `vector-effect="non-scaling-stroke"` is the in-tree cure. PP-NOSHADOW re-proven at BOTH baselines; `./handmark` opened and rejected on the merits. **ANTI-CURE (PP-DUP)**: both prescribed cures KILLED — P-5(a) would resurrect the Invariant-20-violating `getPathBounds` the fleet routed for deletion. LOCAL SHADOW half folds: ContourPreview is the weaker re-derivation, PathPreview the sanctioned one. PP-A11Y's cure adds role AND name together (role without name turns the axe gate RED). F.W0 `noUnusedLocals` rider. DISSENT: the unanimous BLOCKER on PP-DEAD NOT sustained — zero users harmed, zero bytes ship. ⟨PathPreview; GalleryCard L·D-3; GalleryFeaturedCarousel; ContourPreview row 10⟩
- **fr-GalleryAdminBanner GAB-13 — SUBSTRATE PRE-GATE (F.W0-owned, F.W3-binding)** — MAJOR. 28 dirty rows incl. `web/package.json` (HEAD pins glass-ui `^3.1.0` / keyframes `^2.2.0` / value.js `^0.10.0` / lucide `latest`): "every per-file F.W1/F.W3 budget and the 'resolved producer is 4.0.0' premise rest on bytes `git stash` would erase." Companion: glass-ui 4.0.0's syntactically corrupt `dist/styles/index.css` (fr-PaperSidebar M1). **No F.W3 budget cell may be authored against unsettled bytes.** ⟨GalleryAdminBanner; PaperSidebar; COHESION §0a⟩ → g18.
- **fr-ExportModal M-δ — THE INITIAL-FOCUS LANDING** *(adopted in repair round 1, ruling R-2a; `fr-ExportModal.md:41`, MINOR)* — initial focus lands on a **state-dependent toggle**: `:46` binds `:open` + `@update:open` only, so reka's FocusScope runs `focusFirst(getTabbableCandidates(container))` in DOM order; the ✕ renders AFTER the slot (4.0.0 render) so the first tabbable is the first `<Switch>` — and `v-if="hasEpicycles"` (`:55`) decides whether that is **"Epicycles" or "Trace path"**. *"The open-then-Space habit mutates a different option depending on workspace state"*; the landing is under-signalled (D-m5's missing row focus style) and it compounds L-6 (the first thing announced is the control whose name is the open probe). Provenance carried honestly: the row is **in no axis, neither reader's confirmations, and not the r1 bank** — it is this seat's read of FocusScope `:90-104` + utils `:40-45` + DialogContentImpl `:68` + the 4.0.0 slot-first render. **Cure (one line)**: `@open-auto-focus.prevent` + an explicit target, or the producer's dismissal/primary convention. **Sequencing**: it is the same dialog surface M-α re-spells (`dismiss="deliberate"`), so it lands WITH M-α's edit, never as a separate visit; landing-element probe in BOTH workspace states → SS-13 #17. **Cross-wave**: F.W4's `GCM-44` cites this row as the **pattern-class** for the identical `@open-auto-focus.prevent` cure on the GCM-4 title — the pattern is stated once, here, and cited there (§X.1). ⟨ExportModal; GalleryCardModal GCM-44⟩
- **fr-ExportModal M-α / M-γ / M-ε — CURE-SEQUENCING RIDERS** — MINOR, F.W3/W4 edits with F.W1 ORDER dependencies (executing in the banked order regresses the defects they close): `:show-close="false"` → re-spell `dismiss="deliberate"`, WITH/AFTER F.W1; `min-width:300px` flips inert→harmful at F.W1 (binds below 340px, halves the producer's 320px gutter guarantee) — its deletion is "a sequenced PREREQUISITE of the free improvement, not a tidy-up"; the honest type edit (`font-size: var(--type-body)`) also retires the sole `@apply` the `@reference` machinery serves. ⟨ExportModal⟩
- **fr-ExportModal LC-1 / LC-2 / K-20 — THE e2e KEYSTONE REPAIR RIDER** — MAJOR. The tree's sole live axe keystone is silently narrowed by its own subject (open modal ⇒ `isModalOpen()` true ⇒ all three `aria-hidden-focus` checks flip `incomplete`/`true` page-wide; neither helper carries `.include()`/`.exclude()`); an adjacent `test.fixme` rationale is false on all three clauses at the pin. Cure: scope `checkA11y` with `.include()` per surface; ASSERT `results.incomplete` for the modal-suppressed rules; re-run the fixme'd keystone at the pin and "delete the fixme AND its rationale rather than porting either forward" (banked K-13 predicts green). i-2 rider: any F.W3/W4 cure lands with the coefficients layer OPENED in at least one axe pass. Corroboration → SS-13 #13/#14. K-20 binds every carried a11y argument. ⟨ExportModal; CoefficientsSpectrum i-2; CollapsibleSection m-8; BasisSelector B-3⟩
- **fr-GalleryInfiniteGrid FR-GIG-5 — THE PAGINATION-DRAIN CREDIT LOCK** — binding sequencing note. **F.W1 must NOT be credited for the pagination-drain cure.** Composition rider binding F.W3's edits: the uncached `onClick` forces all N cards to patch on every parent render, so banked fr-GalleryCard L·M-2's one-line `:liked` hoist delivers ZERO measured win alone and banked FR-GFC-19's `click: [entry]` payload cure must land IN THE SAME EDIT — one repair unit; neither banked record states the interaction. The drain itself is F.W4/producer. DISSENT preserved: three BLOCKER filings on FR-GIG-1 against the ruled blocker-weight MAJOR. ⟨GalleryInfiniteGrid; GallerySearchBar FR-GSB-7⟩

---

## §3 Gates

All born-RED (L-19: each names its live witness or literal command; no descriptive invocation). GREEN flips only at the named cure. Execution-gated: no gate runs until the owner's begin-word.

| # | gate | witness (RED baseline) | GREEN check |
|---|---|---|---|
| **g1** | **SCOPE-ORPHAN** — the S-4 greppable law as a gate | PSM-1/R-5: `PaperSearch.vue:41` `<style scoped>` holds 48 rule blocks (40 selector + 8 transition), exactly 5 live (:43, :47, :58, :199, :204), 43 dead; the modal ships unstyled to `<body>`. vue-tsc + vite build structurally blind (S-4); 8 e2e specs / 0 hits on `paper-search*`. | For every `<style scoped>` block: no declared class appears exclusively in a CHILD component's template. GREEN = 0 violations; today ≥43. Authored once, inherited by F.W4. |
| **g2** | **SLIDER-SCRUB retired** | `grep -rn -- "--slider-scrub" web/src` → 23 declarations / 7 files; `grep -rl slider-scrub node_modules/@mkbabb/glass-ui` → EMPTY. | `grep -rn -- "--slider-scrub" web/src \| wc -l` → 0, **with MPC-10's contrast leg asserted** (≥4.00:1 light / ≥3.71:1 dark) — absence alone regresses 11.51:1 → 1.51:1 under a green sweep. |
| **g3** | **TIMELINE FORK — one composition** | `wc -l`: `GlassTimeline.vue` = 127, `ConvergenceTimeline.vue` = 146, verbatim forks (same latch/adapter/dead token); `grep glass-ui/timeline web/src` → 0. | Both forks resolved into ONE unconditional-session composition; single session source; `pointercancel` handled. SR-1 REJECTS the prescribed `ScrubberTimeline` adoption — the gate asserts the re-derivation, not an import. |
| **g4** | **SCRUB SESSION — freeze unreachable** | K-4-narrowed triggers: (a) press+release inside the ±0.5-step band at the playhead; (b) `pointercancel`. Today both leave rAF cancelled with `playing===true`, pause glyph + `.is-playing` asserting motion, latch suppressing every subsequent `scrub-start`. | Both gestures leave `anim.scrubbing === false` and the clock running; the latch is gone. Live repro → SS-13 #6. |
| **g5** | **KEYBOARD-SEEK acceptance** | Reka routes ←/→/Home/End/Page through `updateValues({commit:true})` with no pointer event; `!scrubbing` guard eats it; latched `startTime` discards the keystroke next tick. | Explicit test: an arrow/Home/End press moves `t` and survives the next frame. HARD requirement on the cure. |
| **g6** | **PD-1 binding verification** (F.W1-owned probe; F.W3 acceptance) | **⊙ MEASURE-AT-OPEN (P-4, ruling R-9.2)** — the RED baseline is the producer's `Slider.vue:69-78` docblock (the `@pointerdown` fallthrough DROPPED across Slot/forwardRef; *"vue-tsc + units pass; only a real drag catches it"*; emit surface = `"update:modelValue"` + `valueCommit` only), and it is anchored at **the producer COMMIT HASH of the ADOPTED pin, read from F.W0's G-13 pin table in `SUBSTRATE-LEDGER.md`** — **never a version string**. F.W0 row 20 (M-10/K-9) mints P-4: *"producer-side evidence carries the producer COMMIT HASH, never the version string"*, and **F-W0 §4 (Gates — all born-RED), gate id G-13** re-derives the pin at the ADOPTED commit — ⟨cmd⟩ this seat, `grep -h '^### G-1[123] ' F-W0.md` → *"### G-13 — The producer pin is re-derived at the ADOPTED commit; no F.W1 sizing quotes a 7.0.0 anchor"* (the round-1 coordinate `F-W0.md:209` addressed an `**Owning rows**` line after F.W0's own repair-round shifts, PASS-2 D-3; per R2-2 it is **abolished, not re-pointed**). Until that table exists this cell is **UNMEASURED BY CONSTRUCTION**: the hash is filled in at wave-open from G-13, the `:69-78` line span is re-resolved through G-11's anchor table before it is read, and the gate does not run against a `7.0.0` label. (The `v7.0.0:` spelling this cell carried was a P-4 violation and is struck; §C.H's blanket "every 'at 7.0.0' disposition re-reads at 8.0.0" rule stands but is SUBORDINATE to this re-anchor — a blanket re-read is not a re-resolved anchor.) | A real drag at the **adopted** pin fires `anim.startScrub()` (`anim.scrubbing === true` mid-drag), with the pin cited as a hash in the transcript. Cure-shape (native listeners on the resolved host) chosen so the gate cannot regress silently. **If G-13's table is absent at open, g6 does not run and the unit halts to §6** — the cure SHAPE re-derives on g6's verdict, so an unanchored baseline would silently pick the shape. |
| **g7** | **EASING FORK — one renderer** | `.easing-preview` = 4 grep hits across `EasingCurvePreview.vue` + `MorphPhaseConfig.vue` (two renderers, two samplers: 32-sample memoized vs 24-sample unmemoized); FMD-32 names a fifth fork. | One curve renderer, one `d`-precision policy (FACET ⊕ SAMPLE ⊕ MPC-33); `.easing-preview` collapses to one home or disappears. **Gated on the MISSED-D ruling; may not presume 8.0.0.** |
| **g8** | **EASING CHIPS keyboard-reachable** | All four reach mechanisms key on `[data-reka-collection-item]`, which a bare glass Button never mints (chunk grep: 0); unconditional Tab swallow; `handleMountAutoFocus` traps focus. | Six chips reachable by arrows/Tab/typeahead/Home-End on the `DropdownMenuRadioItem` chassis (NOT ToggleChip — Toggle.js:68 double-exposes state). Banked live-keystroke SS-13 item demotes to confirmatory. |
| **g9** | **ACTIVE-STATE — one spelling** (**TREE-WIDE; closes at `.f`**) | Seat census, mechanically classified: 11 scoped-CSS rule lines / 7 files + 17 `:class` bindings + 1 prop binding (`PaperView.vue:344`); five spellings live. **▲▲ THE OPERAND'S UNIT-SPREAD, MEASURED (repair round 2, PASS-2 D-4)**: ⟨cmd⟩ this seat, read-only, 2026-08-28, `grep -rl 'is-active' web/src \| sort` → **11 files spanning ALL FIVE units** (`.a` EditorControlsDock · `.b` EasingPicker · `.c` PaperView/PaperSidebar/MobileFloatingToc · `.d` EquationModeToggle/FunctionInput · `.e` App-shell AppHeader, CanvasControlsDock, CanvasOverlayButton, GallerySearchBar) — the full mapping is §5a-v2's. This is a **disjointness operand, not a rival census**: it does not re-denominate FR-COB-3's banked figures, which are G-12's to publish (g17). | One vocabulary, SWEEP-LAW enforced: the check may NOT key on a bare `is-active` grep (it corrupts the prop — ⟨cmd⟩ `sed -n '344p' components/paper/PaperView.vue` → `                        :is-active="isActive"`, a `.c`-owned CONTRACT). FR-EMT-23 scopes it: three of seven files put `.is-active` on a glass Button root. Paired with the FR-COB-17 8-site `aria-pressed` lift in the SAME commit as F.W0's `git rm`. **CLOSURE (§5a-v2)**: `.e` publishes the vocabulary; each owning unit applies it in its own files; **`.f` reports this gate** — a green reported from inside `.e` would be measured over one unit's fifth of the operand. |
| **g10** | **WRAPPER LAW** | Three live violations: `SliderControl.vue` (8 props/1 emit vs `SliderRootProps &{…}`+2 emits; `valueCommit` DESTROYED); the Tooltip shim drops `class` at a fragment root with a dev warn; `CanvasOverlayButton` unified 0 of 9 sites. | Every surviving wrapper declares `inheritAttrs:false` WITH a pre-placed `v-bind="$attrs"` on its intended host, or does not wrap. Anti-rule: NEVER blanket `inheritAttrs:false` on SliderControl — `class` fallthrough is WANTED; cure = explicit passthrough + re-emitted `valueCommit`. **Policy: F.W3's own, authored at §C.G's FR-COB-15 row** — *a wrapper earns its existence only by adding contract the primitive lacks* — with `W3-button-ledger.md` row `A.W3.b.1` as its EVIDENCE row, not its authority (repair round 2, PASS-2 D-8: `grep -ic 'policy'` over that ledger → **0**; the old cell hung an anti-rule on a cite that carries no rule). |
| **g11** | **TOOLTIP ADAPTER re-parameterised** (R3-7a, X-11-amended) (**TREE-WIDE; closes at `.f`**) | FR-TT-1: the shim is the only label on 17 icon-only triggers (Editor ×10 · Canvas ×6 · FunctionInput ×1), wiring `aria-describedby` only-while-open, never a NAME. FR-TT-3: `max-width` grep over the whole tooltip chain at BOTH pins → 0. **▲▲ THE OPERAND'S UNIT-SPREAD, MEASURED (repair round 2, PASS-2 D-4)**: ⟨cmd⟩ this seat, read-only, 2026-08-28, `grep -rl '<Tooltip' web/src \| sort` → **11 files spanning FOUR units** (`.a` AnimationControls/BasisSelector/EditorControlsDock · `.c` PaperSidebar · `.d` FunctionInput/CoefficientsSpectrum/ContourSettings · `.e` App.vue, Tooltip.vue, CanvasControlsDock, VisualizationView) — §5a-v2 carries the mapping. `ConvergencePlot.vue` is **absent from that list by construction** (its tooltip is the hand-rolled `v-html` surface R3-7a books as the 10th), so the grep's silence corroborates the record rather than contradicting it. **This does NOT re-denominate the 35-callsite / 9-consumer budget** — that is FR-TT-8's figure and G-12's to publish (g17); this is a disjointness operand only. | 35 callsites / 9 consumers re-pointed AND a naming leg minted, with the FR-TT-15 knob list landed (measure · align/class element-root decision · hoverable policy · offset rationale · contract guard). FR-TT-20: the TWO import identities normalized FIRST. NOT retirement (FR-TT-24). |
| **g12** | **FADING-SCROLL / FORMS / SEARCH adoption at the pin** | Three zero-import shadows against shipped subpaths: `./fading-scroll` (both pins, 0 imports); `grep -rn glass-ui/forms web/src` → 0; `grep -rn glass-ui/search web/src` → 0 (the 4.0.0 `./search` is a generalisation of the fork). | **Per subpath, one of exactly two MEASURED outcomes — the OR is closed, not rhetorical** (repair round 1, PASS-1 D-9): either **(a)** `grep -rn "glass-ui/<subpath>" web/src` returns **≥1 import** at the adopting site named in §C.H, or **(b)** an id-keyed **NOT-ADOPTED row exists in `F-W3-DO-NOT-EXECUTE.md`** (g20's artefact, path `docs/tranches/X/fourier/F-W3-DO-NOT-EXECUTE.md`) carrying the banked id, the refusal reason, and the seat + date. **GREEN = 3 of 3 subpaths in state (a) or (b); FAIL = any subpath in neither.** A rationale that is not a row in that file does not count, and prose in this spec never counts. Available TODAY — no F.W1 dependency (K-12 refutes the version framing). PSM-9's adoption priced with an explicit scoring decision (PSM-14/FR-PSD-TYPE) — never inherited. |
| **g13** | **PAPER-SEARCH same-commit rider set** | Four latent defects ARMED by PSM-1's cure, each measured today: PSM-4 (1.88/2.39/2.04/1.74:1 chrome); PSM-13 (`outline:none`, no `:focus-visible` anywhere, global rescue list names four OTHER classes); MISS-LC1 (five cross-SFC hooks orphaned by one-child-per-class); FR-PS-BDT/FR-PS-CLIP (backdrop and results both trap on the filter containing block / sidebar clip). | The scope cure and ALL FOUR riders land in ONE commit; no rider deferred. Every one is a named P-11 instance. |
| **g14** | **CARTOON-CARD relay re-scoped; `--viz-amber` alone on the letter** | KILL-6: the `<Card tier="opaque" surface="cartoon">` migration is a 21-site typecheck break; K-7: the corpus's cure is two majors stale (at 8.0.0 `cartoon` is not a prop). Census of record 21 sites / 14 files; the shim self-reports 14/13 — stale by 7. | The SS-6 batched BH letter carries `--viz-amber` ONLY; the cartoon-card ask RETIRED; the shim's stale comment corrected in the wave that touches it; the FALSE `:188-193` "migration complete" comment corrected (D-15). |
| **g15** | **SHARED-UTIL consolidation** (timeAgo · basis-key · GalleryTier) | `grep -c "function timeAgo"` → exactly 5, two dialects; `grep 'startsWith("fourier")'` → exactly 7 sites incl. `labels.ts` and `useWorkspaceLoader.ts:113`; `GalleryTier` re-declared inline 8-9× over 7-8 files against one canonical export with 3 importers. | ONE `timeAgo` (one dialect, capped, guarded, referent-disambiguated); ONE `BasisKey`/`normalizeBasisKey()`/`basisModeLabel()` unit; ONE imported `GalleryTier` + a minted sort canon. The basis-key WAVE-LOCK binds F.W5-W8's `basisFilter` wiring; the serializer half is F.W5-W8's. |
| **g16** | **NO NAIVE `@layer` WRAP** (anti-cure gate) | The unlayered-scoped-beats-layered inversion live at ≥8 sites, with the producer's own capitalised RED-witness comment in both `base.css` and `surfaces.css`. | No cure introduces a naive `@layer` wrap; any layering cure appends `@layer glass-overrides` AFTER utilities AND proves override survival. The durable fix is a LINT RULE, not a per-component edit. Cross-cited in F.W4's budget. |
| **g17** | **SHADOW AGGREGATE — F.W3 QUOTES, F.W0 PUBLISHES** ⊙ | FR-EMT-21: the aggregate headline disagrees with its own parenthetical; three uncoordinated '10' increments; six 🟡 CANDIDATE members excluded. **▲▲ THE HEADLINE RE-QUOTED AT THE BYTES (repair round 2, PASS-2 D-9)** — ⟨cmd⟩ this seat, 2026-08-28, `sed -n '446p' ../../../V/megatranche/formation/fourier/lane-frontend.md`: `**Aggregate shadow surface: 9 components / ~1 990 LOC** (127 + 98 + 41 + 109 + 547 + 764 + 168 + 91 + 134 ≈ 2 079 counting the canvas-drawing lib; **1 315 excluding it**).` — **the figures are DIGIT-GROUPED WITH A SPACE**, so round 1's forbidden-token list (`~1990` / `2079`) could not match the bytes it forbids: ⟨cmd⟩ `grep -c '1990' lane-frontend.md` → **0**, `grep -c '2079' lane-frontend.md` → **0**, `grep -cE '1 990\|2 079' lane-frontend.md` → **1**. Correction of record, measured rather than inherited: the separator is a **plain ASCII space (U+0020)**, not a thin space — ⟨cmd⟩ `grep -c $' ' lane-frontend.md` → **0** — so PASS-2 D-9's "thin-spaced" characterisation is itself corrected here, and the third figure **`1 315`** joins the forbidden set the same way (it is a third superseded total the old list never named at all). **▲ OWNERSHIP CORRECTED (ruling R-9.1)**: this gate is an **INPUT TO / QUOTATION OF F.W0's G-11 anchor table and G-12 denominator table** in `SUBSTRATE-LEDGER.md`, cited by **stable anchor — F-W0 §4 (Gates — all born-RED), gate ids G-11 and G-12** (⟨cmd⟩ this seat, `grep -h '^### G-1[123] ' F-W0.md` → *"### G-11 — ONE corrected anchor table published; every later wave quotes it"* · *"### G-12 — ONE corrected-denominator table published; superseded figures FORBIDDEN downstream"*), the ledger itself **create**-marked at **F-W0 §2a (File bounds), the `fourier/docs/tranches/F/SUBSTRATE-LEDGER.md` row** — ⟨cmd⟩ `grep -ho 'SUBSTRATE-LEDGER\.md` . \*\*create\*\* — [^·]*' F-W0.md` (cell-delimiter-free by construction, so the receipt survives quotation inside this table) → `` SUBSTRATE-LEDGER.md` \| **create** — the wave's single durable artefact: the 28-path minute ``. Round 1's coordinates `F-W0.md:199`/`:204`/`:63` addressed a blank line, an `**Owning rows**` line and the wrong bounds row after F.W0's own repair-round shifts (PASS-2 D-3); per R2-2 they are **abolished, not re-pointed**, and RULINGS-2's own "§1a" spelling for the ledger row is corrected to **§2a** against the live headings (⟨cmd⟩ `grep -h '^### 2a' F-W0.md` → *"### 2a. File bounds"*) — a ruling prescribes the cure-shape, never a coordinate to copy (R2-1-LAW 3). **F.W3 does not re-perform the re-resolution as its own act**, and divergence from F.W0's table is a defect against G-11 — not a rival measurement. The S-8 NO-WAVE-OWNER emission stands (the methodology row is ownerless); the aggregate itself is F.W0's to publish. | **GREEN (three conditions, all falsifiable)**: **(1)** the corrected aggregate exists as a row of F.W0's G-12 denominator table in `SUBSTRATE-LEDGER.md`, cited by path from `.f`'s close block; **(2)** every F.W3 percentage or budget cell authored at execution quotes that row's figure verbatim — `grep` of the close transcript for a shadow-aggregate number returns **only** the G-12 figure, and **0** occurrences of any superseded total. **THE FORBIDDEN-TOKEN DETECTOR IS STATED INLINE AND IS SEPARATOR-AGNOSTIC (repair round 2, PASS-2 D-9 — a detector that cannot match the bytes it forbids is a defect at authoring, whatever its result; R2-9's law applied to a forbid-list)**: the probe is `grep -nE -e '1[[:space:]]?990' -e '2[[:space:]]?079' -e '1[[:space:]]?315'` over the transcript (written with repeated `-e` rather than regex alternation so the pattern carries no cell delimiter and cannot be corrupted by quotation), covering the un-grouped spelling, the live ASCII-space spelling, **and** any thin/no-break variant a later re-flow introduces, over **all three** superseded totals (`~1 990` headline · `≈ 2 079` with-lib · `1 315` without-lib) — the third was never in round 1's list at all; **(3)** the companion census corrections ride as named rows (CENSUS:191 split; the false "6× Button"; the `./metric-stack → ./metric` re-seat; the ~20× callsite understatement). **FAIL = any budget cell authored before the table exists** — this is a triumvirate event under §6, not a re-derivation licence. |
| **g18** | **F.W0 SUBSTRATE settled** (inherited pre-gate; F.W0-owned, F.W3-binding) | GAB-13: 28 dirty rows incl. `web/package.json` (HEAD pins glass-ui `^3.1.0` / keyframes `^2.2.0` / value.js `^0.10.0` / lucide `latest`) — re-measured `git status --porcelain \| wc -l` → **28**, 2026-08-28; **`FR-NP-32` (≡ `fr-PaperSidebar M1`)**: glass-ui 4.0.0's corrupt `dist/styles/index.css`, BLOCKER, *"`vite dev` and `vite build` are RED at the `^4.0.0` pin"* (both banked ids cited, neither substituted — R-8; OP-5). | Both settled AND F8-REACH-01 + F8-REACH-02 ruled in one breath before any F.W3 measurement. Declared here as an open precondition, not owned: F.W0 owns the settle; F.W3 consumes it (= OP-2). |
| **g19** | **CENSUS CLOSURE — set-difference over a SHAPE-AGNOSTIC roster** | **▲▲ RE-BASED AGAIN IN REPAIR ROUND 2 (rulings R2-3a + R2-9; PASS-2 D-1 convicted round 1's operand).** Round 1 struck the addressless "CARRY … 90 rows" and put a real, path-cited roster in its place — and that roster was **format-blind**: it was the PASS-1 table-terminal-cell census, so **691 route-marked bullet rows across 36 records were never eligible to be found missing** (68.5% of the subject). Same defect class as R-6's en-dash blindness, transposed to the row-shape axis. **The operand is now the CORPUS ITSELF, read through a detector stated here**: the 66 `fr-*.md` records at `docs/tranches/V/megatranche/registry/adjudicated/`, ruled at **1,008 rows / 62 records (317 table ⊕ 691 bullet)** by R2-9, of which **844 are dual-`F.W3/W4`** and the remainder sole-`F.W3`. **DETECTOR, INLINE AND EXHAUSTIVE (R2-9 — a gate that states a restricted operand is DEFECTIVE at authoring, whatever its result)**: **(i) shapes** — table rows whose terminal cell routes ⊕ id-headed bullets routed in context (`→` · `**ADJUDICATED →**` · `⇒` · `rides` · `routed` · bold-at-disposition) ⊕ prose routings walked to the enclosing row id (S-8); **(ii) spellings** — the token set `{F.W3, F.W4, F.W3/W4}` in **BOTH** U+002D and U+2013, every slash form expanded to its member set; the en-dash arm is **∅ for W3 and stays in the probe so it can never silently stop being ∅** (⟨cmd⟩ this seat: `grep -rhoE 'F\.W[0-9]+[–-]W[0-9]+' fr-*.md \| sort \| uniq -c` → `120 F.W5-W8` · `72 F.W5–W8` and nothing else); **(iii) atomisation** — ids taken from cell 1 / bullet head; **on an `=`-joined head the LEFT token is the banked id and every right-hand token is the reading axis's ALIAS — carried for life, never booked, never a hole in this roster** (repair round 2, second seat: testing both sides manufactures escapes no bank minted — zero-homed id-instances **254 → 172** under the left-token rule, `fr-GalleryDraftsSection`'s eighteen `C-C-*` among the phantoms, §0 ▲▲▲); then split on `/ ≡ ⊕ ∘ , +`, **`·` preserved** (`L·D-1` is NOT `L-D-1`) and hyphen-runs preserved (`MISS-DU1` is NOT `MISS-DU-1`), grade tails and parentheticals stripped; boundary-exact `(?<![A-Za-z0-9-])ID(?![A-Za-z0-9-])` thereafter. **A detector without the alias rule is DEFECTIVE at authoring** (R2-9): its set-difference is non-empty by construction, so its green is unreachable and its red is uninformative. **RED baseline, re-measured across three detectors (⟨cmd⟩ `census.py` · `escapes.py` · `escapes_left.py`, all saved to §8)**: table arm **317/37 — reproduces PASS-1 EXACTLY in every run**; bullet arm **681 · 691 · 742 · 764**, the ruled 691 inside the bracket; totals **1,008 (ruled) / 1,059 (this seat)**, both at or above the floor; zero-homed escaped id-instances **288 (loose) · 254 (both-sides) · 172 (left-token rule)** against the ruling's spot-verified **103** — and 172 is an UPPER bound, since `·`/`/`-joined heads carry the same alias shape (`fr-DarkModeToggle D-m10`, whose banked head `N-10` is booked at §C.H). The spread is atomisation and aliasing, never shape. | At `.f`'s close, **both directions, each with a saved transcript (§8)**, over the detector above **re-run at close, never over a copied integer**: **(→)** every routed id in the roster either greps in this file by boundary-exact bytes, or appears in **§X / §X.1** with a named holder and a reason; **(←)** every §2 row traces to a corpus record row or a declared registry correction. **GREEN = both set-differences EMPTY, with the roster's own cardinality printed in the transcript and ≥ 1,008** — a run that reports fewer rows than the floor has mis-stated its detector and is a FAIL, not a green. **FAIL by construction if the transcript's LHS is a count copied from any check file** (R2-2: check files and rulings are measurements and cure-shapes, never quotable operands). The escapes are the standing RED: 12 booked into §2 at round 1 (LF-1 · L-1b · FR-COB-5 · FR-COB-22 · M-δ · EasingPicker L30 · FR-MSP-11 · MorphShapePreview L-15⊕L-18 · MISS-2 · GCM-34 · EasingCurvePreview R-4 · R-9, ⊕ FR-AFP-33's client limb) and **13 more booked at round 2 under R2-3b's TWIN LAW** (§X.1-v2); the remainder are named per id in **§X.1** with their holding wave. No new carry ledger is authored (R-3 item 2); "the CARRY" with no path is forbidden vocabulary. |
| **g20** | **THE REGISTER AND THE LETTER exist** | `ls docs/tranches/X/fourier/F-W3-DO-NOT-EXECUTE.md` → No such file (verified 2026-08-28); `grep -c "F.W3" docs/tranches/V/coordination/INBOX.md` → 0 — no SS-6 batched letter for this wave's producer rows exists. | `.f` has written the DO-NOT-EXECUTE register (the four anti-cures + every killed prescription of §X, each with its kill cite) AND filed ONE batched SS-6 letter in INBOX carrying every §C.E/GLASS-RELAY item — `--viz-amber` the only cartoon/amber ask; DOCK-ACTIVE not re-sent. |

---

## §4 Cross-wave edges (S-1..S-11)

- **S-1 · F.W0 (consumed)** — the substrate settle (GAB-13 + the corrupt dist, **`FR-NP-32` ≡ `fr-PaperSidebar M1`**, OP-5), the CanvasOverlayButton `git rm` (with F.W3's same-commit FR-COB-17 rider — now carrying FR-COB-5's naming law and FR-COB-22's register note — coordinated across the boundary), F8-REACH-01/02 ruled in one breath, the `embla-carousel-vue` declaration, `noUnusedLocals`/lint introduction (the g1 checker and the g16 `@layer` rule are F.W0 additions this wave consumes). **▲ THE PUBLISHED-ONCE QUOTATION (ruling R-9.1, one line, binding on every anchor this spec reads) — ▲▲ RE-ANCHORED TO GATE IDS IN REPAIR ROUND 2 (R2-2; PASS-2 D-3 convicted all four coordinates).** F.W0 publishes three gates, cited here **by gate id under their §-heading and never by line** — quoted by command, ⟨cmd⟩ this seat, 2026-08-28, `grep -h '^### G-1[123] ' F-W0.md`:

```
### G-11 — ONE corrected anchor table published; every later wave quotes it
### G-12 — ONE corrected-denominator table published; superseded figures FORBIDDEN downstream
### G-13 — The producer pin is re-derived at the ADOPTED commit; no F.W1 sizing quotes a 7.0.0 anchor
```

— all three under **F-W0 §4 (Gates — all born-RED)** (⟨cmd⟩ `grep -h '^## ' F-W0.md` → `## 1. State` · `## 2. Bounds` · `## 3. Carry` · **`## 4. Gates — all born-RED`** · `## 5. Owner rulings — FLAGGED INLINE, never presumed` · …), and all three landing in `fourier/docs/tranches/F/SUBSTRATE-LEDGER.md`, **create**-marked at **F-W0 §2a (File bounds), the SUBSTRATE-LEDGER row** (⟨cmd⟩ `grep -h 'SUBSTRATE-LEDGER' F-W0.md` → ``| `fourier/docs/tranches/F/SUBSTRATE-LEDGER.md` | **create** — the wave's single durable artefact: the 28-path minute · …``); cited-WHEN-created. **P-4** rides G-13: the pin cell is the COMMIT HASH, never a version string. **What died here, and why it is not re-pointed**: round 1 cited these as `F-W0.md:199`/`:204`/`:209`/`:63`, and after F.W0's own repair-round line-shifts those addresses land on a blank line, on two `**Owning rows**` lines, and on the `fourier/CLAUDE.md` bounds row respectively — four dead anchors under a masthead attesting re-reading. **Per R2-2 a line number into a live sibling is a defect from that ruling forward**, so the class is not re-measured, it is ABOLISHED: every F-W0 receipt in this file is a gate id or a §-heading, and a later F.W0 edit can no longer stale one. (Corollary for `.f`: a close transcript containing `F-W0.md:` followed by digits is a g19-adjacent authoring defect, not a stale cite to fix.) **F.W3 QUOTES all three and re-performs none**: the ~100 raw record anchors this spec carries (`PaperSearch.vue:41`, `PaperView.vue:344`, `GlassTimeline.vue:16-18`, `Toggle.js:68`, `surfaces.css:46-56`, `style.css:113-127`, `W3-button-ledger.md:93`, `lane-frontend.md:441/443/446`, …) are **re-resolved through G-11 before any lift is applied — never followed raw** (F-W0's own D-19: *"a repair agent following D's anchors lands OUTSIDE the file"*); every denominator this spec states is G-12's or is a defect against G-11 (g17); every producer-side pin is G-13's hash (g6, P-4). This includes the FR-COB-17 8-site `:aria-pressed` lift that rides F.W0's `rm` commit: the eight anchors re-resolve first, in F.W0's own commit, or the lift does not land.
- **S-2 · F.W1 (precedes; COHESION §2)** — the atomic tri-package uplift; the `--slider-scrub` BUDGET cell; the PD-1 binding-verification probe (g6's evidence); token re-inks (PSM-4's leg, FR-AFP tone re-grammar, MG-β census gate); the P-10 re-pin ruling is HOMED there — F.W3 consumes the verdict and may not presume it. **F.W1 must NOT be credited for the pagination-drain cure (FR-GIG-5).**
- **S-3 · F.W4 (blocks)** — inherits g1 and g16; consumes the P-11 packet and the four anti-cures cross-cited into its edit budget; receives the named deferrals (§X): SW-1 cap→floor, the `size="icon"` naming sweep, FR-COB-18's glyph sweep, contrast residues, the ConvergencePlot amplifier pair, FR-EMT-2's post-uplift escalation.
- **S-4 · F.W5–W8 / SS-4 (hands over)** — serializer unification, GCM-10 tier default, the like-verb decision, `ml_threshold`, `ContourStrategy`. **THE WAVE-LOCK (fr-GallerySearchBar ruling 5)**: any F.W5-W8 wiring of `basisFilter` must consume the banked `normalizeBasisKey()` or the Fourier pill returns zero rows against `active_bases ∈ {fourier-epicycles, fourier-series}`. **SS-4's own obligations, not F.W3's**: SS-4 flags its owner rulings INLINE (trie-vs-KISS et al.) and names the value-side TA-4 atomdiff restoration as prerequisite or re-scopes explicitly.
- **S-5 · SS-6 producer relay (feeds)** — producer rows NEVER become frontend hacks. ONE batched BH letter (g20) carries: the GlassTimeline consolidated items (AX-1's four-item same-edit law · CU-1's cursor deletion · PD-1's docblock-vs-render contradiction · **`LF-1` — the inline `left`% written every frame against the producer's own "NEVER `style.left`" rule**, restored to this roster in repair round 1 per R-2a: the id was routed `GLASS-RELAY + F.W3` and had been dropped from BOTH the letter and §2; its §2 carry now lives in §C.A and neither limb discharges the other) · **`FR-COB-22`'s producer-constant question** (the 0.40 glyph-to-box ratio against the 0.50–0.60 icon-button register — a register note here, an ask there) · `--viz-amber` (the ONLY cartoon/amber item; cartoon-card ask RETIRED) · the `--motion-accent` re-scope of the `--viz-easing` ask (+ the false carry-note correction) · `./confirm-dialog`'s retirement note · the `.disclosure-trigger` hover question (M-5) · the floating-offset token ask · `./clipboard` · PAW-53's forced-colors rung · the `--tier-*` family · FR-COB-7 (TOP of the FR-COB-28 list) · AA-3's violet/batch tone. **DOCK-ACTIVE is closed upstream — do NOT re-send (FR-COB-11).**
- **S-6 · Owner-gated flags (a/b/c)** — flagged inline, never presumed: (a) MISSED-D, the easing adoption route (§C.C); (b) fr-ContourSettings i-3, the default-collapsed IA choice (§C.K); (c) P-10, the 7.0.0-vs-8.0.0 re-pin target (homed at F.W1). Declared-not-carried edge: **F.W7's anti-tree KISS guardrail stays inline in F.W7.**
- **S-7 · parse-that** — direct parse-that→fourier is **FORBIDDEN**. The value.js-consumption rows read the installed `@mkbabb/value.js@0.13.0` dist only.
- **S-8 · NO-WAVE-OWNER register (F.W3 EMITS; COHESION §4)** — census-methodology rows no wave owns: **FR-EMT-21** (the §4 shadow aggregate not incrementable until X-9's member-scope law publishes) · **fr-CoefficientsSpectrum i-5(a)** (callsite-keyed budgets understate ~20×) · **fr-CoefficientsPanel FR-CP-44** (the `./metric-stack → ./metric` re-seat) · **fr-EasingCurvePreview PICKER-RES** (CENSUS:191 SPLIT — preview half mechanical, picker half a product decision) · **fr-FunctionInput L-m5 + C §0 row 7** (the `./chip`-vs-`./toggle-chip` version qualifier; the false "6× Button" budget) · **fr-CanvasOverlayButton FR-COB-7** (producer-owned cascade composite) · **fr-ContourSettings i-1** (Multi-threshold ships zero strategy-specific controls — a product-surface decision needing an authored ruling).
- **S-9 · SS-13 visual audit (feeds)** — every UNPROVEN-NEEDS-LIVE residue routes keyed to its banked row id: SW-1 rendered widths (#1) · caret severed-pixel count at BOTH roots (#2) · caret-vs-fill offsets + endpoint overhang (#3) · the D-12 negative probe (#4) · the K-4-narrowed freeze repro (#6) · the AX-1 a11y-tree dump (#7) · CU-1's one-glance cursor confirm (#8) · the C-6+CU-1 discoverability read (#9, one screenshot not three) · PD-1's #10 **which is F.W1's lane, not the cosmetic lane** · the LC-1/LC-2 corroboration runs (#13/#14). ▲ **Probe #5 (WCAG 2.5.8) is DISCHARGED STATICALLY (rK-19), retained as a STRUCK row so no later seat re-opens it.**
- **S-10 · X·KF and X·V (pattern-only edges)** — RECORDED, never re-booked: value.js's `useSliderTouchGates.ts`/`ComponentSliders.vue` cured the same reka pointer-capture class downstream · the fleet dead-barrel class (ApiOfflineChip AP-11 · kf-EditorHeader EH-6 · kf-KeyframeCard KF-KC-45 · PaperSearchInput C-9) · kf-KeyboardShortcutsModal banks `.kbd`-as-shipped as a superlative; its K-2 banks the focusable-scroll-port engine fact · shell-dock AT-15 / kf-EditorShell D-15 are the toolbar-roving-tabindex precedent · fr-AppHeader FR-AH-1 is "the same shape as value.js's own Q14 escalation." **No edge creates a scheduling dependency in either direction.**
- **S-11 · E13 mail law** — every session/wave OPENS with the 4-path coordination-mail sweep; `docs/tranches/V/coordination/INBOX.md` is the durable ledger; the three S-6 flags and the SS-6 letter row are FILED there by `.f`; no wave closes with UNREAD mail.

---

## §5 Agent Units

Six units. `.a`–`.e` are **file-disjoint** per §5a; `.f` is serial after all five. Model seats declared (M-12 tri-fold): `.a`–`.e` **Opus implementation seats**; `.f` is a **fresh Fable adjudicator** — the P-11 anti-cure register and the SS-6 letter are adjudication, and an author may not adjudicate his own cure.

### X.F.W3.a The single timeline composition
- **Goal**: one scrub session that cannot freeze, reachable by keyboard, over one composition instead of two forks.
- **Mechanism**: §C.A + §C.B as one cut — the unconditional session pair (incl. `pointercancel`) on the resolved host per PD-1's channel lock; the keyboard-seek arm (L-3); both forks deleted into one (C-4/M-1/M-14); docblock `:16-18` rewritten in the same hunk (D-11); the `--slider-scrub` sweep at the 23/7 budget under MPC-31's one-cut law with the contrast leg asserted (C-5); the `.timeline-caret` two-attribute a11y law (AX-1); the dead-surface deletes (DT-1 cluster); the C·C-7 axis decision made INSIDE the re-derivation.
- **Files** (live spellings, §1): `visualization/GlassTimeline.vue`, **`equation/convergence/ConvergenceTimeline.vue`**, `visualization/AnimationControls.vue`, `stores/animation.ts`, `ui/SliderControl.vue`, `visualization/EditorControlsDock.vue`, `visualization/BasisSelector.vue`, **`morph/{HarmonicLevelGrid,MorphPhaseConfig}.vue`**, `<repo>/web/e2e/` (g4/g5 specs).
- **Sub-gate**: g2, g3, g4, g5, g6 (verdict consumed), g10's SliderControl leg.

### X.F.W3.b The easing disposition
- **Goal**: one curve renderer and one keyboard-reachable chip chassis, chosen by the owner's ruling rather than by default.
- **Mechanism**: §C.C whole. **Blocks on the MISSED-D ruling (S-6a).** Then either the EasingCurve re-home (deletion of `EasingCurvePreview.vue`, both accent arms free, `size`/`color` props dead — FORK/GEO/TOKEN rows) or the pin-landable `DropdownMenuRadioItem` chassis + `chipVariants({shape:"cell"})` styling (D/D-1 + D/D-4) — the chassis is D-1's either way (Toggle.js:68 lock). The BARREL colocation seams and the A11Y/DEADCOPY sweep riders ride the chosen route.
- **Files**: `EasingPicker.vue`, `EasingCurvePreview.vue`, `morph/MorphPhaseConfig.vue`, `lib/easings.ts`, `stores/animation.ts` (barrel seam), `composables/{useMorphConfig,useFourierMorph}.ts`.
- **Sub-gate**: g7, g8.

### X.F.W3.c The paper-search cluster
- **Goal**: the search family painted, announced as a dialog, and scored deliberately — in one commit that arms nothing.
- **Mechanism**: §C.D whole. The R-5 enumerated rule-move with all four riders (PSM-1 ⊕ PSM-4 ⊕ PSM-13 ⊕ MISS-LC1 ⊕ FR-PS-BDT/FR-PS-CLIP, ONE commit); the `./search` adoption decision priced with the scoring decision (PSM-9 + FR-PSD-TYPE); dialog chassis + combobox semantics (PSM-2); stable identity minted at `buildSearchIndex` (PSM-7); `.kbd`/`.input-bar-field` adopted as shipped (PSM-25); dead surface cleaned **before** the diff (PSM-22/★MF-11); `performScroll` widened with the navStack push moved after success (★NAV-1).
- **Files** (live spellings, §1 — the whole search family is colocated at `paper/search/`, there is no `lib/paper/` and no `composables/usePaperSearch.ts`): `paper/PaperSearch.vue`, **`paper/search/{PaperSearchInput,PaperSearchDropdown,PaperSearchModal}.vue`**, `paper/search/index.ts`, **`paper/search/{paperSearchIndex,searchHelpers,usePaperSearch}.ts`**, `paper/{PaperView,PaperSidebar,MobileFloatingToc}.vue`, **`paper/useScrollNavigation.ts`**.
- **Sub-gate**: g1, g12's `./search` leg, g13.

### X.F.W3.d Shadow retirement at the installed pin
- **Goal**: every surface whose seat already ships is retired onto it, or ruled-not-adopted with its reason.
- **Mechanism**: §C.H whole + the §C.I admin/disclosure/dialog rows — `FadingScroll` (M-6 family), `Input`/forms (FR-AUL-6), `Badge`/`Card` (FR-AFP-61), ConfiguratorLayer+`Progress` (ImageUpload roster 10), `SegmentedTabs` (FR-EMT-7), `ToggleGroup`/chip (M-13), `Carousel` (FR-GFC-7), `Collapsible`-vs-`Accordion` (i-4, ruling recorded), the dialog chassis (FV-2; the fourier-local confirm over `./dialog`, FR-AUL-28), the callout `as-child` (PAW-8), PathPreview delete-or-complete, the admin suffusion riders (FR-AFP-34 set), the FR-CP-33 chassis convergence, the e2e keystone repair (LC-1/LC-2). ContourSettings: i-3's ruling is RECORDED, not executed.
- **Files** (live spellings, §1 — no `components/gallery/`, no `components/admin/`; `EqCoefficientsPanel` lives under `equation/`): `visualization/{ImageUpload,ContourPreview,FullscreenViewer,ContourSettings,ExportModal,CoefficientsPanel,GalleryView}.vue`, **`equation/EqCoefficientsPanel.vue`**, **`visualization/gallery/{GalleryFeaturedCarousel,GalleryDraftsSection,AdminFlaggedPanel,AdminAuditLog}.vue`**, `equation/{EquationModeToggle,NotationPills,FunctionInput,EquationView,ConvergencePlot}.vue`, `equation/convergence/ConvergenceLegend.vue`, `morph/{FourierMorphDemo,MorphShapePreview,FourierShapeExtractor}.vue`, `shared/CoefficientsSpectrum.vue`, `paper/PaperArticleWindow.vue`, `ui/{CollapsibleSection,PathPreview}.vue`, `<repo>/web/e2e/` (keystone, after `.a`).
- **Sub-gate**: g12's forms/fading-scroll legs, g17 (budget precedes edits).

### X.F.W3.e Vocabulary, utils, structure, app shell
- **Goal**: one spelling per state, one home per util, one dark-mode owner, and a ruled disposition for the dead SVG register.
- **Mechanism**: §C.F + the §C.I consolidations + the §C.H app-shell rows — the five-spelling consolidation under the sweep-law rider (FR-COB-3); the `aria-pressed` family per FR-NP-3's re-rank (D·D-M12) with the FR-COB-17 lift coordinated into F.W0's `rm` commit; the wrapper law (FR-COB-15/R-6); `glass-ui/dark` adoption + the `index.html` `"auto"` fix (MG-γ); SvgFilters delete-vs-rewire with the pencil-boil same-cut trap (L-5⊕L-8⊕m-1); `timeAgo`/`BasisKey`/`GalleryTier` canons (FR-AUL-17, M-10, GCM-47); the colocation sweep + the dead barrel (FR-CP-28); the tier-chip plate on the ramp re-home (G-DU8); the tooltip shim re-parameterisation + budgeted sweep (FR-TT-15, R3-7a); the FR-GIG-5 one-repair-unit composition (`:liked` hoist + `click: [entry]` payload, same edit).
- **Files** (live spellings, §1): **`<repo>/web/index.html`**, `App.vue`, `style.css`, **`lib/types.ts`** (the one `types.ts` in bounds), **`lib/equation/notation.ts`**, **`components/visualization/lib/canvas-drawing/labels.ts`**, **`components/visualization/lib/basis-display.ts`**, `lib/svg-fourier.ts`, `lib/equation/**`, **`decorative/SvgFilters.vue`**, `visualization/CanvasOverlayButton.vue` (rider only — the `rm` is F.W0's), `layout/{AppHeader,DarkModeToggle}.vue`, `ui/tooltip/**`, `visualization/{CanvasControlsDock,VisualizationView,SpeedSelect}.vue`, **`visualization/EquationPanel.vue`**, **`visualization/composables/useWorkspaceLoader.ts`**, **`visualization/gallery/{GalleryCard,GalleryCardModal,GallerySearchBar,GalleryMarquee,GalleryInfiniteGrid,UserSlugBar,AdminUserList}.vue`**, `equation/{InfoCard,FrequencyGraph}.vue`.
- **Sub-gate**: g10, g15 (`.e`-scoped, closable here) · **g9 and g11 are TREE-WIDE and are NOT closable by this unit** (§5a-v2, repair round 2 / PASS-2 D-4): `.e` OWNS the vocabulary decision (FR-COB-3) and the shim contract (FR-TT-15/FR-TT-24) and publishes them; `.a`/`.b`/`.c`/`.d` apply them inside the files they already own; **`.f` reports both gates** over the 11-file operands measured at §5a-v2.

### X.F.W3.f The law, the register, and the letter
- **Goal**: one signed reading of which cures may not be executed, and one letter that carries every producer row out.
- **Mechanism**: fresh Fable adjudicator, obligated under L-14 to attempt refutation — of the four anti-cures' proofs, of at least one adopted cure shape, and of the claim that g19's set-difference is empty. Writes `F-W3-DO-NOT-EXECUTE.md`; runs the g19 bidirectional grep proof; emits the S-8 NO-WAVE-OWNER rows into COHESION §4; files the three S-6 flags and the SS-6 batched letter in INBOX (S-11/E13); records every deliberate no-op (FR-TT-10's clause) rather than dropping it.
- **Files**: `<vjs>/docs/tranches/X/fourier/waves/F-W3.md` (close block), `<vjs>/docs/tranches/X/fourier/F-W3-DO-NOT-EXECUTE.md` (create), `<vjs>/docs/tranches/X/COHESION.md` (modify-carve, §4), `<vjs>/docs/tranches/V/coordination/INBOX.md` (append).
- **Sub-gate**: g14, g16, g19, g20, **plus g9 and g11** (the two tree-wide sweeps, received from `.e`'s published decision and reported here over §5a-v2's 11-file operands — repair round 2, PASS-2 D-4); **no gate reported green on a narrative**; at least one load-bearing claim carries a recorded refutation attempt.

### §5a Disjointness

No two units share a `modify` path. The splits are stated **at authoring** and, since repair round 1, **against the tree that exists** (ruling R-10.2) — a shared glob is the hidden conflict the X·V W5-D1 defect taught, and a partition drawn over phantom directories partitions nothing. **The partition is BY FILE, and the file list is the authority; the directory names below are descriptive.** **(1) `components/visualization/`** — `.a` holds `GlassTimeline.vue` *(this file IS in `visualization/`)*, `AnimationControls.vue`, `EditorControlsDock.vue`, `BasisSelector.vue` + `ui/SliderControl.vue`; `.b` holds `EasingPicker.vue`, `EasingCurvePreview.vue`; `.d` holds the retirement set (`ImageUpload/ContourPreview/FullscreenViewer/ContourSettings/ExportModal/CoefficientsPanel/GalleryView`); `.e` holds `CanvasControlsDock.vue`, `VisualizationView.vue`, `SpeedSelect.vue`, `CanvasOverlayButton.vue` (rider only) and `EquationPanel.vue` *(live at `visualization/`, not `equation/` — `fr-NotationPills.md:9`, the record's instrument line; the round-1 `:23` cite is STRUCK at both its sites per §1's ▲▲ receipt, repair round 2 / PASS-2 D-6)*. **(1a) THE THREE FILES THE OLD SPLIT MIS-PLACED, re-cut here**: **`ConvergenceTimeline.vue` is `.a`'s** and lives at **`components/equation/convergence/`** — `.a` therefore owns two directories, and `.d`/`.e` touch **no** file under `equation/convergence/` except `ConvergenceLegend.vue`, which is **`.d`'s**; **`SvgFilters.vue` is `.e`'s** and lives at **`components/decorative/`** — `.e` owns that directory whole (it holds one bounded file); **`HarmonicLevelGrid.vue` is `.a`'s** and lives at **`components/morph/`**, where `.a` also holds `MorphPhaseConfig.vue` for the token cut, `.b` takes `MorphPhaseConfig.vue` after `.a` (split 3), and `.d` holds `MorphShapePreview/FourierMorphDemo/FourierShapeExtractor`. **(1b) THERE IS NO `components/gallery/` AND NO `components/admin/`** — both sets live under **`components/visualization/gallery/`**, so the gallery/admin split is stated by FILE inside one directory: `.d` holds `GalleryFeaturedCarousel.vue`, `GalleryDraftsSection.vue`, `AdminFlaggedPanel.vue`, `AdminAuditLog.vue`; `.e` holds `GalleryCard.vue`, `GalleryCardModal.vue`, `GallerySearchBar.vue`, `GalleryMarquee.vue`, `GalleryInfiniteGrid.vue`, `UserSlugBar.vue`, `AdminUserList.vue`. **No unit may take `visualization/gallery/**` as a glob** — that glob is the conflict. **(1c) `components/paper/`** — `.c` holds `PaperSearch.vue` + the whole of `components/paper/search/**` (four SFCs + `index.ts` + `paperSearchIndex.ts` + `searchHelpers.ts` + `usePaperSearch.ts`, all colocated there) + `PaperView.vue`, `PaperSidebar.vue`, `MobileFloatingToc.vue`, `useScrollNavigation.ts` *(live at `components/paper/`, not `composables/`)*; `.d` holds `PaperArticleWindow.vue` alone. `.e` holds `components/visualization/composables/useWorkspaceLoader.ts` and `lib/types.ts`, `lib/equation/**`, `components/visualization/lib/**` — none of which any other unit opens. **(2)** `stores/animation.ts` is touched by `.a` (session) and `.b` (barrel seam) — **`.b` opens only after `.a` commits**. **(3)** `e2e/**` is `.a`'s for g4/g5 and `.d`'s for the LC-1/LC-2 keystone — **sequenced `.a` → `.d`**, never parallel. `ContourSettings.vue` is `.d`'s alone (i-3's ruling is recorded, not executed). `MorphPhaseConfig.vue` is `.a`'s for the slider-token cut and `.b`'s for the easing twin — **`.b` after `.a`**, same rule as the store. **(4) `ContourSettings.vue` (`.d`) → `VisualizationView.vue` (`.e`)** — the split §C.I's `fr-VisualizationView MAJ-5` row declares and round 1 never landed here: the `fr-ContourSettings M-16` contract repair is authored at the panel (`.d` owns `ContourSettings.vue`) and the two `v-model` call sites follow at the host (`.e` owns `VisualizationView.vue`), so **`.e` opens only after `.d` commits the panel's contract** — a call site edited against an un-landed contract is the same class of hidden conflict as a shared glob.

**▲▲ §5a-v2 — THE TWO TREE-WIDE SWEEPS, DECLARED (repair round 2, PASS-2 D-4).** The opening sentence is TRUE and stays: **no two units share a `modify` path**, and the file lists above are the authority. What round 1 never said — and what PASS-2 D-4 convicted — is that **two of `.e`'s sub-gates are not `.e`-scoped at all**: g9 (active-state vocabulary) and g11 (tooltip adapter) measure the WHOLE tree, so a partition that is honest about files was silently dishonest about gates. Re-measured at the tree this seat, read-only, 2026-08-28, and the crossing is far wider than one file:

⟨cmd⟩ `grep -rl 'is-active' web/src | sort` → **11 files**, mapped to their §5 owners:

```
components/equation/EquationModeToggle.vue          .d
components/equation/FunctionInput.vue               .d
components/layout/AppHeader.vue                     .e
components/paper/MobileFloatingToc.vue              .c
components/paper/PaperSidebar.vue                   .c
components/paper/PaperView.vue                      .c   (the `:is-active` PROP, `:344`)
components/visualization/CanvasControlsDock.vue     .e
components/visualization/CanvasOverlayButton.vue    .e   (rider only — the `rm` is F.W0's)
components/visualization/EasingPicker.vue           .b
components/visualization/EditorControlsDock.vue     .a
components/visualization/gallery/GallerySearchBar.vue .e
```

⟨cmd⟩ `grep -rl '<Tooltip' web/src | sort` → **11 files**, likewise:

```
App.vue                                             .e
components/equation/FunctionInput.vue               .d
components/paper/PaperSidebar.vue                   .c
components/shared/CoefficientsSpectrum.vue          .d
components/ui/tooltip/Tooltip.vue                   .e
components/visualization/AnimationControls.vue      .a
components/visualization/BasisSelector.vue          .a
components/visualization/CanvasControlsDock.vue     .e
components/visualization/ContourSettings.vue        .d
components/visualization/EditorControlsDock.vue     .a
components/visualization/VisualizationView.vue      .e
```

**g9 crosses ALL FIVE units; g11 crosses four (`.a` `.c` `.d` `.e`).** (`ConvergencePlot.vue` is absent from the second list because its tooltip is the hand-rolled `v-html` surface R3-7a books as the 10th — the grep's silence is the record's own finding, not a miss.)

**THE LAW THAT RESOLVES IT — DECIDE ONCE, APPLY BY OWNER, CLOSE AT `.f`** (KISS: no file moves between units, no unit gains a second seat):
1. **`.e` DECIDES and publishes, in its own files, the ONE vocabulary (FR-COB-3) and the ONE re-parameterised shim contract (FR-TT-15/FR-TT-24).** That decision is a written artefact, not a diff — it is what the other units consume.
2. **Every other unit APPLIES the published decision inside the files it already owns**, in its own commit: `.a` for `EditorControlsDock`/`AnimationControls`/`BasisSelector`, `.b` for `EasingPicker`, `.c` for `PaperView`/`PaperSidebar`/`MobileFloatingToc`, `.d` for `EquationModeToggle`/`FunctionInput`/`ContourSettings`/`CoefficientsSpectrum`. **No unit opens a file another unit owns** — the partition is untouched, and that is the point.
3. **Sequencing**: `.e` publishes BEFORE the applying units' sweeps land; the applying edits are ordinary per-unit work thereafter. `.e`'s publication is a hard precondition of the sweep, exactly as `.a`'s store commit is of `.b`'s.
4. **g9 and g11 therefore CLOSE AT `.f`, not at `.e`** — they are tree-wide set-difference gates over an operand no single unit can see, and reporting them green from inside `.e` would be a closure claim measured over one unit's share. §5e's sub-gate list is read accordingly: `.e` OWNS the decision, `.f` REPORTS the gate.
5. **The SWEEP-LAW RIDER binds every applying unit, not just `.e`**: the sweep may never key on a bare `is-active` grep, because that grep hits `PaperView.vue`'s `:is-active` **prop** (⟨cmd⟩ this seat, `sed -n '344p' components/paper/PaperView.vue` → `                        :is-active="isActive"`) — a prop rename corrupts a contract in a file `.c` owns while `.e` believes it is renaming a class. The 11-file list above is the sweep's operand of record; a unit that cannot see it must not sweep.

### §5b Worktree Plan

All five implementation units run in worktrees **inside** the fourier root (`/Users/mkbabb/Programming/fourier-analysis/.worktrees/f3{a..e}`), never beside it, each with its own `web/node_modules`. **No worktree is created before F.W0 settles the 28 dirty rows** — a worktree of a dirty root inherits nothing and hides the settle. `.f` is serial on clean main. (JS repo: no `CARGO_TARGET_DIR`; the per-worktree isolation surface is `web/node_modules` + the vite cache.)

**▲ THE PRECONDITION'S OWN RESIDUE, DECLARED (repair round 1, PASS-1 D-10).** `.worktrees/` inside the repo root is itself an untracked path, so creating five worktrees dirties the very porcelain OP-2 gates on: `grep -n worktree /Users/mkbabb/Programming/fourier-analysis/.gitignore` → **no match** (this seat's read, 2026-08-28), and `git status --porcelain | wc -l` → **28** today. Two consequences, both stated rather than assumed: **(1)** the `.gitignore` row for `.worktrees/` is **F.W0's** to land — `.gitignore` is NOT in F.W3's §1 bounds and F.W3 does not add it (adding a bounds row mid-wave is a §6 triumvirate event, §6 line 1); it is carried to F.W0 as a coordination item beside the GAB-13 settle. **(2)** until it lands, **OP-2's "clean-or-recorded-baseline" reads with `.worktrees/` named in the recorded baseline** — the porcelain count at each unit's open is `28 → settled → settled + 1 untracked dir`, and a gate transcript that reports a non-empty porcelain must say which of the two it is. A worktree plan that silently violates its own precondition is the failure mode; this row is why it cannot.

| Agent unit | Worktree absolute path |
|---|---|
| X.F.W3.a | `/Users/mkbabb/Programming/fourier-analysis/.worktrees/f3a` |
| X.F.W3.b | `/Users/mkbabb/Programming/fourier-analysis/.worktrees/f3b` |
| X.F.W3.c | `/Users/mkbabb/Programming/fourier-analysis/.worktrees/f3c` |
| X.F.W3.d | `/Users/mkbabb/Programming/fourier-analysis/.worktrees/f3d` |
| X.F.W3.e | `/Users/mkbabb/Programming/fourier-analysis/.worktrees/f3e` |
| X.F.W3.f | clean main (serial, docs-side) |

---

## §6 Triumvirate Dispatch

Mandatory (not optional) triggers; the orchestrator may not redispatch the failing unit alone:

- **Bounds expansion that invalidates the wave**: any needed edit outside §1 — above all `parse-that/**`, glass-ui producer source, `api/**`, `web/package.json`/`tsconfig.json` — halts the unit; a bounds row added mid-wave is a triumvirate event, not an edit.
- **Hard-gate failures that are not local-edit-recoverable**: g6 RED at the adopted pin (the binding channel is dead ⇒ the cure SHAPE re-derives, not the edit); g2's contrast leg failing after a green sweep (the MPC-10 regression class); any g13 rider that cannot land in the same commit (the cure is arming — stop, do not split the commit); g7 forced while MISSED-D is unruled (the unit may not presume).
- **Diagnostic loops whose third iteration must halt**: the g4 freeze repro, the g5 keyboard-seek acceptance, and the g1 scope-orphan checker's false-positive loop — three failed iterations each ⇒ triumvirate (research + plan augment + redress).

## §7 Format and Lint Cadence

`npx vue-tsc -b --noEmit` and `npx vite build` after each integration batch and before close; `npx playwright test` (the 8 existing specs + the new g4/g5/g8 specs) at each unit close; `git diff --check` on every commit.

**▲ THE CADENCE'S STANDING RED, DISCLOSED (ruling R-8, verbatim)**: *"the per-batch `npx vite build` cadence is RED BY CONSTRUCTION at the installed ^4.0.0 pin until FR-NP-32 clears (F.W0 G-4); a batch's build gate reads against that standing RED, not against the batch."* `FR-NP-32` (≡ `fr-PaperSidebar M1`, `fr-NotationPills.md:35`, BLOCKER) is reproduced with the app's own toolchain — `CssSyntaxError: Unterminated string: 's own'` from `style.css:3`'s live `@import "@mkbabb/glass-ui/styles"` — so a red `vite build` in this wave is **not evidence about a batch** until F.W0's clearance receipt exists (OP-5). `vue-tsc` and `playwright` are unaffected by the dist stylesheet and remain honest batch signals. **Two replacements are named because the tools do not exist (R-19): there is no eslint config and no `noUnusedLocals`, so g1's S-4 checker and g16's `@layer` rule are the wave's only mechanical style gates — both are F.W0 additions this wave consumes, and both name real witnesses (43 dead rules; the producer's own capitalised RED comment), so neither is a proof-farm script under L-19.** Docs-side (`.f`): `git diff --check` on the four value.js paths.

## §8 Verification Artefacts

Saved at close, all under `docs/tranches/X/fourier/waves/evidence/F-W3/` unless a standing home exists: per-gate transcripts g1–g18 (command + output, RED→GREEN pairs; g6's cites the producer commit HASH per P-4, g17's cites F.W0's G-12 row) · the g19 bidirectional census proof — **id→file** and **file→roster** — run over **the corpus itself** (`docs/tranches/V/megatranche/registry/adjudicated/`, 66 records) through g19's inline detector, **the roster re-derived in the transcript and its cardinality printed (floor 1,008)**, never over a count copied from a check file (repair round 2, R2-2/R2-9; the round-1 artefact named the 317-row PASS-1 roster and `conformance/PASS-1/F-W3-CHECK.md` §3 as its operand — that is the format-blind roster PASS-2 D-1 convicted, and a check file is a measurement, never a quotable operand). **file→roster** traces every §2 row back to a corpus record row or a declared registry correction; both transcripts saved, and the escape residue printed per id with its holding wave **against §X.1-v3's record-level partition** (a residue id whose record is not in that table is a NEW record, not a new escape — say which); **the three round-2 detector scripts are saved beside the transcripts** (`census.py` — the roster · `escapes.py` — the both-sides atomiser · `escapes_left.py` — the ruling left-token/alias atomiser, whose difference IS the alias-axis receipt, §0 ▲▲▲), so a later seat re-runs the detector rather than re-inventing it; **no `F-W3-CARRY.md` is authored** (R-3) · `F-W3-DO-NOT-EXECUTE.md` (g20) · the SS-6 letter as an INBOX row id · SS-13 hand-off list keyed to banked row ids (S-9) · playwright reports for the g4/g5/g8 specs and the repaired keystone · the `vite build` + chunk grep for the SvgFilters bundler contingency · commit hashes per unit in the close block.

## §9 Commit Plan

Pathspec commits only (standing law). Per-unit worktree commits scoped to mechanism, e.g. `refactor(fourier-web): F.W3.a one scrub session — forks folded, slider-scrub retired`; commit bodies required (broad/deletion/gate changes). **Same-commit locks restated as commit shapes**: the `.c` scope cure + four riders = ONE commit (g13); the FR-COB-17 `aria-pressed` lift rides F.W0's `git rm` commit (coordination note to F.W0, not an F.W3 commit); MPC-31's one-cut = one commit inside `.a`; the D-11 docblock rewrite is a hunk of the pairing-fix commit, never its own. Orchestrator integration commits sequenced `.a` → `.b` (store seam) and `.a` → `.d` (e2e). Close: `docs(X·F): F.W3 close — <gates green count>, register + letter filed`.

## §10 Dependencies

- **Depends on**: F.W0 (S-1; g18/OP-2) · F.W1 (S-2; COHESION §2 precedence; g6's probe) · the three owner rulings S-6 a/b/c · the owner's begin-word (OP-1).
- **Blocks**: F.W4 (inherits g1/g16; consumes the P-11 packet and §X deferrals) · F.W5–W8/SS-4 (the `basisFilter` WAVE-LOCK) · SS-6 (the batched letter is authored here) · SS-13 (the S-9 probe list).

---

## §X Excluded — carried nowhere in F.W3, each with its reason

| item | reason |
|---|---|
| The `--slider-scrub` **budget cell** | **F.W1's** (S-2); F.W3 owns execution only (fr-AnimationControls C-5) |
| The tone re-grammar behind FR-AFP-61; FR-AFP-19's rest-state tone; MG-β's `text-admin-label` census gate; the `showClose`→`dismiss` prop deletion; the `min-width:300px` producer clamp | **F.W1's legs** (S-2); F.W3 carries the sequencing dependency, not the edit (FR-AFP-61, fr-ExportModal M-α/M-γ) |
| The 450,631 B eager-payload row (async boundary + generator-side prune) | **F.W4** — only DarkModeToggle's shadow-reconcile is F.W3's (fr-AppHeader FR-AH-1) |
| The pagination drain itself; ConvergencePlot's `startLoop`/`playing` amplifier pair; `.timeline-track-wrap` + the 0.125rem off-scale inset; the widened setter parameter if the Slider composition is kept; C·C-7's announcement-rate decision if the float axis is not adopted; per-site tooltip focusable-host and naming work; FR-EMT-2's post-uplift escalation | **F.W4 / producer** (S-3), each named at its row with its trigger — **and the promise is KEPT PER ID in §X.1**, which enumerates the F.W3/W4-routed banked ids this spec hands over. **▲▲ THE WORD "EVERY" IS STRUCK HERE (repair round 2, R2-3a; PASS-2 D-7)**: §X.1's 59 lines were denominated against the round-1 table-shape roster of 317, and the real dual-`F.W3/W4` population is **844** rows inside a **1,008**-row roster (§0 ▲▲) — 59 of 844 cannot carry the word *every*. What §X.1 now promises is **exact and testable**: the handovers this spec names, plus **§X.1-v2's per-id split of the escapes** under R2-3a's TWIN LAW. Completeness is g19's to prove by re-running the detector, never §X's to assert |
| SW-1 cap→floor · the 36-site `size="icon"` naming sweep · FR-COB-18's glyph-register sweep · the FR-CP/FR-GSB/FR-GV/PSM-4 contrast residues | **F.W4's edit budget** (S-3), cross-cited from here with the P-11 packet |
| **PAW-44/LAW-3** — restore-only-with-background-cure | **F.W4's** (S-3): same-commit rider class of the charter's lock list (beside PSM-1's set · MPC-31 · FR-MSP-6, which land HERE) — its row lands with F.W4's restore work and the lock travels WITH the row; named here so it is never read as silently dropped |
| F8-REACH-01/02 **rulings**; the `git rm`; the `embla-carousel-vue` declaration; `noUnusedLocals`/lint introduction; the dirty-worktree settle; the corrupt-dist settle | **F.W0's** (S-1) — F.W3 executes only the InfoCard **disposition** after the ruling (fr-EquationView D·D-M5) |
| The serializer unification; the `tier` model default (GCM-10); the like-verb decision; FR-AFP-8's identity surface beyond the one-token slug rider; the `ml_threshold` cache-key contract; the shared `ContourStrategy` enum; SS-4's own owner rulings (trie-vs-KISS et al.) and the TA-4 atomdiff prerequisite | **F.W5–W8 (SS-4)** (S-4); F.W3 hands them over and imposes only the `basisFilter` wave-lock |
| **F.W7's anti-tree KISS guardrail** | **Explicitly declared as an edge, not carried** — it stays inline in F.W7 (S-6) |
| **SS-13 probe #5 (WCAG 2.5.8)** | **DISCHARGED STATICALLY (rK-19)** — retained as a **STRUCK** row so no later seat re-opens it (S-9) |
| Every producer-owned row: the GlassTimeline consolidated letter items · CU-1's cursor deletion · PD-1's docblock contradiction · FR-COB-7's cascade composite · the `.disclosure-trigger` hover rung · the floating-offset token · the rich-body single-root monolith · the header-actions slot · `./clipboard` · `./confirm-dialog`'s retirement · PAW-53's forced-colors rung · the `--tier-*` family · `--motion-accent` | **SS-6 relay only** (S-5) — "producer rows NEVER become frontend hacks." The **DOCK-ACTIVE ask is closed upstream and must NOT be re-sent** (FR-COB-11) |
| The census-methodology rows of S-8 | **NO-WAVE-OWNER register**; F.W3 emits them, does not own them |
| Any parse-that surface | **FORBIDDEN edge** (S-7); the three value.js-consumption rows read the installed `@mkbabb/value.js@0.13.0` dist only |
| Edits to `registry/adjudicated/fr-*.md`, `lane-frontend.md`, the dated intake, `<web>/package.json`, `<web>/tsconfig.json`, `scripts/dev/dev.sh` | **Out of bounds** (§1). The registry is read-only for life; the re-cut lands as tables in wave specs |
| The **killed** prescriptions, retained as struck so no seat re-derives them | `ScrubberTimeline` adoption (SR-1/K-16) · `--popover-offset` (rK-20) · `:max="1000"` (C·C-7) · viewBox attribute surgery (GEO anti-cure) · ToggleChip-as-chassis (Toggle.js:68) · `role="radiogroup"` (K-18) · `<Card tier surface="cartoon">` (KILL-6/K-7) · re-adding `.glass-btn` (R8-17) · "make the shim single-root" (K6) · the `ariaLabel` forward (8.0.0 deletion) · `ConfirmDialog` swap (K1) · P-5(a)'s `getPathBounds` (Invariant-20) · the naive `@layer` wrap · the token-swap press · one-child-per-class colocation (MISS-LC1) · the D-6/L-1¶3 backdrop revival · the D-3 dropdown revival · adoption-inherits-scoring (PSM-14) |
| **Killed grades and celebrations that must not be inherited** | C-10's `"thm 3"` scenario (K2) · L's PSM-5 BLOCKER (K4) · the dropdown D:B-3 BLOCKER re-filing (K4) · C·S5's index-spliced-key superlative (K-3) · the 49/5/44 recount (K-10) · reader-1's "pixel-unchanged boot render" (overturned) · the unanimous PP-DEAD BLOCKER (not sustained) · D·S-3's inline-literal superlative (corrected) · the "not-drop-in" half of the easing disposition · FR-COB-11's "no backing paint" cell (stale) · the "un-selectable" backdrop consequence (false) |
| **Preserved dissents** (recorded, not resolved) | fr-ConvergenceTimeline §2 ranking · **DISSENT 6** (PD-1, deliberately unresolved) · MPC-31 grade · fr-GlassTimeline r1 DISSENT-2 (L-M3) · reader-DU on MISSED-D and on the missed M-1 · reader-2's BLOCKER on FR-EMT-5 (**the truer reading if the re-derivation finds further tinted-surface failures**) · L-axis PSM-5 BLOCKER · LC on FR-CP-33 / DU on FR-CP-34 · LC on m-2/L-3/C-7 · reader-2 MINOR on M-3 and reader-1 MAJOR on D-5 · fr-CollapsibleSection B-1 LC · RD-7 on D·D-M5 · FR-MSP-6's grade · fr-GalleryView DISSENT 4 · the three FR-GIG-1 BLOCKERs · fr-MorphShapePreview D-13 · fr-GalleryDraftsSection DISSENT-2 · DU's MAJOR-library on FR-CP-28 · fr-EquationModeToggle reader-1 on the clobber |

### §X.1 The dual-routed handover, named per id (repair round 1 — ruling R-2c's sibling correction)

**What this table is, exactly.** §X above promised that every F.W4 deferral is *"named at its row with its trigger."* PASS-1 measured that promise and found it unkept for the tail: **71 of 317 F.W3-routed banked rows escaped this file by bytes** (`F-W3-CHECK.md` D-2/D-8). Twelve of the 71 are now **booked in §2** by this repair (LF-1 · L-1b · FR-COB-5 · FR-COB-22 · M-δ · fr-EasingPicker L30 · FR-MSP-11 · fr-MorphShapePreview L-15⊕L-18 · MISS-2 · GCM-34 · fr-EasingCurvePreview R-4 · R-9), plus FR-AFP-33's client limb. **The remaining 59 are enumerated below — one line each, by banked id, with the trigger the record itself states.**

**These lines are CITATIONS, NOT BOOKINGS** (R-2's one-home law): the **rows are held by the adopting wave named in column 3**, and no cell here re-books, re-grades or re-scopes them. Banked ids are original for life; short tokens are record-qualified. A reader who greps an id from column 1 and lands here has found its home, not its grave — which is the whole point: an escape is silent only when nothing names it.

**▲▲ §X.1-v2 — THE TWIN-LAW SPLIT (repair round 2, rulings R2-3a + R2-3b; the ESCAPES only — the already-booked rows stand, and nothing below re-enumerates them).** Round 1's table below was measured against the table-shape roster and is **kept whole**; what it could not contain is the escape population the shape-agnostic roster exposes (§0 ▲▲). **THE SPELLING PARTITION, applied mechanically and stated once so no seat re-derives it**: sole `F.W3` → **F.W3 books**; sole `F.W4` → **F.W4 books**; dual `F.W3/W4` → **F.W4 books BY DEFAULT** — this is §X.1's own column-3 declaration (*"the rows are held by the adopting wave named in column 3"*) generalised to the whole dual population — **EXCEPT** rows whose **target file appears in a §5 unit file list (`.a`–`.e`)**, which home at **F.W3**. The criterion is **mechanical, decidable today, and file-based**: it asks one question of a path, not a judgement of a defect. The other twin CITES; a citation is never a booking.

| escape set (banked ids, record-qualified) | target file | §5 unit | homes at | landing shape |
|---|---|---|---|---|
| `fr-PaperSearch FR-PS-Z` · `FR-PS-HL` | `paper/PaperSearch.vue` | `.c` | **F.W3** | §2 rows, §C.D (`.c` escape set) |
| `fr-PaperSearchInput MISS-DU1` · `MISS-DU3` · `MISS-DU4` | `paper/search/PaperSearchInput.vue` | `.c` | **F.W3** | §2 rows, §C.D |
| `fr-PaperSearchModal PSM-40` | `paper/search/PaperSearchModal.vue` | `.c` | **F.W3** | §2 row (g13 commit rider), §C.D |
| `fr-App MG-α` · `MG-ε` · `MG-η` | `App.vue` · `layout/AppHeader.vue` | `.e` | **F.W3** | §2 rows, §C.H (app-shell escape set); `MG-ε` lands as a RECORDED DECISION |
| `fr-VisualizationView MAJ-5` (fold) · `MAJ-7` | `visualization/VisualizationView.vue` | `.e` | **F.W3** | §2 rows, §C.I; `MAJ-5` = named fold → `fr-ContourSettings M-16`, target booked with it |
| `fr-InfoCard FR-IC-16` | `equation/InfoCard.vue` | `.e` | **F.W3** | §2 row, §C.I — rides the F8-REACH-01 disposition |
| `fr-GallerySearchBar FR-GSB-30` | `visualization/gallery/GallerySearchBar.vue` | `.e` | **F.W3** | §2 row, §C.I — one band with the `./forms` adoption |
| `fr-App MG-λ` | `composables/useToast.ts` | *(none)* | **F.W4** | citation only — the file is in no §5 list and no §1 bounds row; the default arm of the LAW |
| `fr-VisualizationView L-26` | — | — | **F.W4** | **THE CONTROL** — escapes F-W3 and IS held at `F-W4.md` + `carry/F-W4-CARRY.md`. ⟨cmd⟩ this seat, **re-run AFTER this round's writes**, 2026-08-28: `grep -rln 'L-26' waves/ carry/` → `waves/F-W4.md` · **`waves/F-W3.md`** · `carry/F-W4-CARRY.md` — the F-W3 hit is THIS citation row itself, and it is stated rather than trimmed because the round-1 spelling of this receipt (`F-W4.md` `F-W4-CARRY.md`) stopped reproducing the instant it was written down. A control whose command no longer returns what it prints is not a control (R2-1-LAW 1: the pasted output is the output of a command run THIS round, self-reference included). This is what a correct hand-off looks like, and it is why the rows above were defects and this one is not |

**What this table does NOT claim.** It is the split of the escapes **this seat verified at the bytes by command**, not a re-enumeration of the whole 844. The ruling's spot-verified population is **103 zero-homed rows**; this seat's own re-run at a looser atomiser returns **288 zero-homed id-instances across 33 records** (§0 ▲▲), and the difference is atomisation, not disagreement about the class. **The remainder is g19's operand, not a number this section may launder into a closure claim** — the gate re-runs the detector at close and the residue lands in its transcript. A section that named 59 against 844 and wrote *"every"* is exactly the failure being cured; it is not cured by writing a larger number with the same confidence.

**▲▲▲ §X.1-v3 — THE LAW APPLIED TO THE WHOLE RESIDUE, BY RECORD (repair round 2, second seat; R2-3b: each escape "lands in its LAW-assigned wave as a §2 row, a named fold, or an exclusion-with-reason").** v2 books the ruling's thirteen worked examples per id and routes the remainder to g19 — but **a gate is a test, not a holder**, so the remainder was left homeless by the very section that names homes. It need not be: the file criterion asks **ONE question of a PATH** — is the record's target file in a §5 unit file list? — and that question is answerable over the WHOLE residue today, **independently of atomisation**, because it keys on the record, not on the id. Whatever the id-level roster settles at (172 · 254 · 288 · 103), each id's HOME is already decided by the file its record targets. ⟨cmd⟩ this seat, 2026-08-28, `escapes_left.py` (left-token atomiser; boundary-exact against `F-W3.md`, then against every `waves/F-W*.md` + `carry/*.md`): **172 zero-homed id-instances across 30 records**, partitioned mechanically against §5's own file lists:

| holder (LAW-assigned) | records whose escape residue it holds | id-instances | landing shape |
|---|---|---|---|
| **F.W3 `.a`** | `fr-ConvergenceTimeline` · `fr-SliderControl` · `fr-BasisSelector` · `fr-MorphPhaseConfig` (shared with `.b`, split (2)/(3)) · `fr-EditorControlsDock` | 12 | rows of the unit's own §C.A/§C.B cluster, authored at the unit's open from each record's routing cell |
| **F.W3 `.c`** | `fr-PaperSearchModal` · `fr-PaperSearchInput` · `fr-PaperSearch` · `fr-PaperSearchDropdown` | 14 | §C.D rows on the ONE g13 commit — the family is already one commit, so no rider is deferred by arriving late |
| **F.W3 `.d`** | `fr-GalleryView` · `fr-CoefficientsPanel` · `fr-PaperArticleWindow` · `fr-EquationModeToggle` · `fr-GalleryFeaturedCarousel` · `fr-EqCoefficientsPanel` · `fr-FullscreenViewer` · `fr-ContourSettings` · `fr-NotationPills` | 60 | §C.H/§C.I rows inside the retirement set the unit already opens |
| **F.W3 `.e`** | `fr-FrequencyGraph` · `fr-GalleryCard` · `fr-GalleryInfiniteGrid` · `fr-GallerySearchBar` · `fr-GalleryMarquee` · `fr-VisualizationView` · `fr-EquationPanel` · `fr-DarkModeToggle` · `fr-Tooltip` | 58 | §C.F/§C.I rows inside the vocabulary + consolidation sweep |
| **F.W4 — the LAW's negatives** | `fr-BasisCanvas` (18) · `fr-ContourEditorCanvas` (9) · `fr-EquationResult` (1) | 28 | **citation only**: no §1 bounds row and no §5 file list holds `BasisCanvas.vue`, `ContourEditorCanvas.vue` or `EquationResult.vue` (⟨cmd⟩ this seat, `grep -c 'components/[a-z/]*<file>\.vue' F-W3.md` → **0** for each of the three), so the dual spelling takes the LAW's default arm — as `fr-App MG-λ` does. F.W3 cites these records (fr-BasisCanvas at §C.A/§C.E, fr-ContourEditorCanvas at §C.I's FV-2) and books none of their rows |

**What this homes, and what it deliberately refuses.** It homes **every** zero-homed escape to a NAMED holder — the unit that already opens the record's file, or F.W4 — which is the decision R2-3b asks the partition to make, made by path rather than by judgement, and stable under every atomiser this round has run. It **refuses to transcribe 172 rows into §2**: that is the shape M-25 calls DEFECTIVE (*"transcription-only is DEFECTIVE"*), it is the shape §0 already dropped once (the `C-01..C-90` alias layer, dropped because an alias layer is a second rename surface), and it would bank a roster this seat has just proved an upper bound (the alias axis — `fr-DarkModeToggle D-m10`'s banked head `N-10` is already booked at §C.H, so a share of the 172 is alias residue, not escape). **The landing shape is therefore the unit's own row, at the unit's open, from the record's own routing cell** — the bank is never re-authored — and g19's close transcript prints the per-id residue against THIS partition. A holder for every id now; an authored row for every id at execution; an invented row at neither point.

| banked id | trigger (the record's own routing cell) | held by |
|---|---|---|
| `fr-AdminFlaggedPanel FR-AFP-5` | refreshing-not-destroying reload + focus restoration; destination residue → SS-13; family = FR-AUL-24 | **F.W4** (`F-W4.md`, `carry/F-W4-CARRY.md`) |
| `fr-AdminFlaggedPanel FR-AFP-6` | explicit error + retry branch | **F.W4** |
| `fr-AdminFlaggedPanel FR-AFP-11` | delegate to the store or a shared invalidation channel | **F.W4** |
| `fr-AdminFlaggedPanel FR-AFP-12` | guard the catches; seam identity = banked FR-AUL-3 / AA-8 (R6-8 class) — fold by reference | **F.W4** |
| `fr-AdminFlaggedPanel FR-AFP-14` | generation token or shared key; interim `hasMore && !loading` gate | **F.W4** |
| `fr-AdminFlaggedPanel FR-AFP-15` | splice-in-place + natural key | **F.W4** |
| `fr-AdminFlaggedPanel FR-AFP-16` | F.W3/W4 guards; the one-word primitive (`Button loading`, producer `:27`) lands with **F.W1** | **F.W4** (primitive leg F.W1) |
| `fr-AdminFlaggedPanel FR-AFP-17` | map success→"success" now; the adapter rewrite rides **F.W1** | **F.W4** (rewrite leg F.W1) |
| `fr-AdminFlaggedPanel FR-AFP-21` | render the evidence being adjudicated | **F.W4** |
| `fr-AdminFlaggedPanel FR-AFP-22` | typed catch posture at the seam | **F.W4** |
| `fr-AdminFlaggedPanel FR-AFP-23` | await-then-close + guard; primitive shape per FR-AFP-37 | **F.W4** |
| `fr-AdminFlaggedPanel FR-AFP-35` | copy pass — the truth clause is owned by FR-AFP-9's wave | **F.W4** |
| `fr-AdminFlaggedPanel FR-AFP-41` | clear on close; the null-target guard must not silently close | **F.W4** |
| `fr-AdminFlaggedPanel FR-AFP-42` | the seam; test seat → F.W9/W10 | **F.W4** |
| `fr-AdminFlaggedPanel FR-AFP-43` | persistent region, swapped text; AT trace → SS-13 | **F.W4** |
| `fr-AdminFlaggedPanel FR-AFP-60` | col-direction gap + order fix + `dismiss`-guarded preset (FR-AFP-37); flushness witness → SS-13 | **F.W4** |
| `fr-AdminFlaggedPanel FR-AFP-67` | rethrow-or-return from reload (one error channel per action); binds to FR-AFP-6's error state | **F.W4** |
| `fr-CanvasControlsDock L-18` | fold by reference → **fr-BasisCanvas BC-10/C-9** (banked MAJOR, F.W3/W4 there); this record adds only the entry-point fact — *"not re-booked"* | **F.W4** (fold target's wave; also held at `F-W0.md`) |
| `fr-GalleryCardModal GCM-6` | contingency-noted on the F.W1 target (free only if the target becomes 8.0.0) | **F.W4** |
| `fr-GalleryCardModal GCM-12` | empty state or `v-if` | **F.W4** |
| `fr-GalleryCardModal GCM-24` | with GCM-3; cross-cite FR-AFP-11's *"delegate to the store or a shared invalidation channel"* | **F.W4** (R-2a adopting wave) |
| `fr-GalleryCardModal GCM-28` | dies inside GCM-30's dedup | **F.W4** |
| `fr-GalleryCardModal GCM-30` | dies with GCM-52's wrapper deletion | **F.W4** |
| `fr-GalleryCardModal GCM-31` | GLASS-RELAY (producer: teach `cn` its own aliases) + site cure; NEEDS-BUILD probe | **F.W4** + SS-6 relay |
| `fr-GalleryCardModal GCM-35` | → F.W3/W4 (D-26) | **F.W4** |
| `fr-GalleryCardModal GCM-36` | → F.W3/W4 (D-27) | **F.W4** |
| `fr-GalleryCardModal GCM-37` | → F.W3/W4 hygiene (D-28/C-17) | **F.W4** |
| `fr-GalleryCardModal GCM-38` | → F.W3/W4 (D-29) | **F.W4** |
| `fr-GalleryCardModal GCM-44` | `@open-auto-focus.prevent` onto the GCM-4 title — one edit, two rows; **cites F.W3's `fr-ExportModal M-δ` (§C.K) as pattern-class**, per R-2a | **F.W4** (pattern-class stated at F.W3) |
| `fr-GalleryCardModal GCM-45` | → F.W3/W4 | **F.W4** (R-2a) |
| `fr-GalleryCardModal GCM-46` | → F.W3/W4 | **F.W4** (R-2a) |
| `fr-GalleryCardModal GCM-48` | delete; kills GCM-30 (D-31/D-32/L-15) | **F.W4** |
| `fr-GalleryCardModal GCM-49` | → F.W3/W4 (D-33) | **F.W4** (R-2a) |
| `fr-ImageUpload roster 22` | `URL.createObjectURL`/`revokeObjectURL` is the primitive and appears NOWHERE in the product tree; `readAsDataURL` costs a main-thread decode | **F.W4** |
| `fr-PaperArticleWindow PAW-4` | sequenced strictly AFTER PAW-2 (the coupling law) | **F.W4** |
| `fr-PaperArticleWindow PAW-6` | cure CONSTRAINED by K-17 — **no whole-figure boolean is correct for f20** (mixed panels) | **F.W4** |
| `fr-PaperArticleWindow PAW-7` | `alt=""` beside the adjacent rendered caption, or a distinct short description | **F.W4** (R-2a annex) |
| `fr-PaperArticleWindow PAW-9` | `max-block-size: min(60svh, 34rem)` in the scoped block — cascade-reachable | **F.W4** (R-2a) |
| `fr-PaperArticleWindow PAW-11` | rides the PAW-1 cure for visuals; the focusability arm needs real `href`/tabindex semantics at the latex-paper emitter | **F.W4** (R-2a annex) + LATEX-PAPER carry |
| `fr-PaperArticleWindow PAW-13` | dies with the PAW-1 cure; recorded so the repair's visual test asserts the HOVER state too | **F.W4** (R-2a) |
| `fr-PaperArticleWindow PAW-14` | print stylesheet that unwinds the window, or an explicit PDF affordance — a formation decision | **F.W4** (R-2a) |
| `fr-PaperArticleWindow PAW-15` | signposting + a windowing-aware a11y posture; AT-behaviour residue → SS-13 | **F.W4** (R-2a annex) |
| `fr-PaperArticleWindow PAW-18` | one computed per figure in the PAW-3 extraction | **F.W4** |
| `fr-PaperArticleWindow PAW-19` | → F.W3/W4 (D-m-1 / C-m-4 / L-14-easing) | **F.W4** (R-2a) |
| `fr-PaperArticleWindow PAW-21` | `aria-hidden` on the span | **F.W4** (R-2a) |
| `fr-PaperArticleWindow PAW-23` | → F.W3/W4 (D-m-8) | **F.W4** (R-2a) |
| `fr-PaperArticleWindow PAW-24` | delete + correct the comment | **F.W4** (R-2a) |
| `fr-PaperArticleWindow PAW-26` | distinguish the two nulls; keep the type | **F.W4** |
| `fr-PaperArticleWindow PAW-29` | width variants ride the PAW-12 generator | **F.W4** |
| `fr-PaperArticleWindow PAW-30` | sequenced WITH PAW-1 | **F.W4** |
| `fr-PaperArticleWindow PAW-31` | cheap: add the SAI term | **F.W4** |
| `fr-PaperArticleWindow PAW-35` | correct the comment in whatever wave touches the file | **F.W4** |
| `fr-PaperArticleWindow PAW-40` | rides PAW-6's inversion redesign | **F.W4** |
| `fr-PaperArticleWindow PAW-47` | under **LAW-4**: ONE clearance authority (`getScrollOffset()` reads the theme constant, or `scroll-padding-block-start` on `.paper-scroll`) | **F.W4** |
| `fr-PaperArticleWindow PAW-52` | router `scrollBehavior` + hash wiring through `ensureTargetWindow` + hash emission on active-section change — an app+component repair | **F.W4** + LATEX-PAPER carry |
| `fr-PaperArticleWindow PAW-54` | rides the PAW-44 decision (LAW-3's scrollport question decides clip-vs-scroll on x) | **F.W4** (with PAW-44/LAW-3, §X) |
| `fr-PaperArticleWindow PAW-55` | declare the intended pair explicitly — `overflow-y: hidden` + sufficient padding, or `clip` with `overflow-clip-margin` | **F.W4** (R-2a) |
| `fr-PaperArticleWindow PAW-57` | delete-or-comment in whatever wave touches the file; zero behaviour change today | **F.W4** (R-2a) |
| `fr-FourierMorphSvg FM-4..FM-16` (band) | the r1 ruling's band: chip contrast demoted-with-rationale · always-inline colour style · no proportion contract · fallback-less `var()` · the 4-site pass | **F.W4** — the whole FM-4..FM-16 band is booked there (R-2a); **F.W3's one citation is `FM-14` at §C.H's FMD-12 row** (`./metric-badge` GONE at 8.0.0 → target `./chip`), which is a citation, not a booking |

---

## §Z Close

**Census closure — ▲▲ RE-DENOMINATED OVER THE SHAPE-AGNOSTIC ROSTER (repair round 2, rulings R2-3a + R2-9; PASS-2 D-1/D-7 convicted the round-1 denominator).** **The roster of record is the CORPUS** — the 66 `fr-*.md` records at `docs/tranches/V/megatranche/registry/adjudicated/`, read through g19's inline detector (shapes ⊕ both dash spellings ⊕ slash-form expansion ⊕ the `·`/hyphen-run atomisation rule), ruled at **1,008 rows / 62 records (317 table ⊕ 691 bullet)**, of which **844 are dual-`F.W3/W4`** and the remainder sole-`F.W3` (§0 ▲▲). **The two conformance check files are measurements of that corpus, never its substitute** and never a quotable operand (R2-2); round 1's closure quoted PASS-1's **317/37** as "the roster of record", and that is precisely the format-blind operand PASS-2 D-1 convicted — it measured one row shape and left 691 routed rows ineligible to be found missing. Round 1's own figures are kept as HISTORY, correctly labelled: at PASS-1's 317-row shape, **246 booked · 71 escaped**, of which this repair booked **12 into §2** (§C.A LF-1 · L-1b; §C.C fr-EasingPicker L30 · MISS-2 · fr-EasingCurvePreview R-4 · R-9; §C.F FR-COB-5 · FR-COB-22; §C.H FR-MSP-11 · fr-MorphShapePreview L-15⊕L-18; §C.I GCM-34 ⊕ FR-AFP-33's client limb; §C.K M-δ) and named **59 per id in §X.1**. **Repair round 2 books 13 more** under R2-3a's TWIN LAW, each a route-marked bullet the table-shape roster could not see and each homed by the mechanical file criterion (§X.1-v2): the `.c` set (`fr-PaperSearch FR-PS-Z` · `FR-PS-HL`; `fr-PaperSearchInput MISS-DU1` · `MISS-DU3` · `MISS-DU4`; `fr-PaperSearchModal PSM-40`), the app-shell set (`fr-App MG-α` · `MG-ε` · `MG-η`), and the `.e` set (`fr-VisualizationView MAJ-5` — with its fold target `fr-ContourSettings M-16` booked beside it — · `MAJ-7`; `fr-InfoCard FR-IC-16`; `fr-GallerySearchBar FR-GSB-30`), with `fr-App MG-λ` and `fr-VisualizationView L-26` recorded as the LAW's discriminating negatives. **The residue is g19's to print, not §Z's to assert**: no number in this section closes anything, because the gate re-runs the detector at close over a floor of **1,008** and a run reporting fewer rows has mis-stated its detector (FAIL, not green). **Round 2's second seat added the two things a floor cannot supply**: a THIRD detector written from the decree alone (table **317/37** a third time · bullet **742/42** · total **1,059/65** · dual **885** — above the floor, §0 ▲▲▲), and the **alias rule** that makes the gate closable at all (on an `=`-joined head the banked id is the LEFT token; the right-hand token is the reading axis's label — without it the set-difference is non-empty by construction, and zero-homed id-instances fall **254 → 172** the moment it is applied). With that rule the whole escape residue is HOMED rather than deferred: **§X.1-v3** partitions all 30 residue-bearing records to a named holder by the file criterion — `.a` 12 · `.c` 14 · `.d` 60 · `.e` 58 id-instances at F.W3, and **28 at F.W4** across `fr-BasisCanvas` · `fr-ContourEditorCanvas` · `fr-EquationResult`, whose files appear in no §1 bounds row and no §5 unit list. The partition is stable under every atomiser run this round because it keys on the record's FILE, not on the id. Identity is the banked id throughout — original for life, record-qualified where the token is short; the `C-01..C-90` alias layer is dropped (§0) and the "90 rows / two drafts" lineage carries **no gate weight**. Gates **g1..g18** stand on their own named witnesses; **g19** (census closure, re-based on the roster above) and **g20** (register + letter) close them. 11 cross-edges → **S-1..S-11**. §1 bounds re-derived from the live tree and reconciled against every carried row's file set (R-10.2). **g19 proves the set-difference ∅ in both directions at close; until it runs, this is a claim — and it is now a claim a seat can actually test.**

**Status**: `planned`. **AUDITED YES · SPECIFIED YES · IMPLEMENTED NO · VERIFIED NO.** Nothing opens product source until the owner's begin-word; the three owner-gated questions (S-6 a/b/c) are flagged, never presumed.

**Files this spec would create/modify (absolute), none written at execution by this session:**
- `/Users/mkbabb/Programming/value.js/docs/tranches/X/fourier/waves/F-W3.md` (this spec)
- `/Users/mkbabb/Programming/value.js/docs/tranches/X/fourier/F-W3-DO-NOT-EXECUTE.md` (`.f`, create)
- `/Users/mkbabb/Programming/value.js/docs/tranches/X/COHESION.md` (`.f`, §4 modify-carve)
- `/Users/mkbabb/Programming/value.js/docs/tranches/V/coordination/INBOX.md` (`.f`, E13 append)
- subject tree, at execution only, in fourier's sub-session: `/Users/mkbabb/Programming/fourier-analysis/web/{src,e2e}` + `/Users/mkbabb/Programming/fourier-analysis/web/index.html` (the ONE real file — the repo-root `index.html` does not exist; corrected in repair round 1 per R-10.2, and the same spelling now stands at §1 and §5e)


