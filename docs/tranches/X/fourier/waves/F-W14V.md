# F.W14V — fourier: the detached pane made distinct (owner frame 2026-09-24), the anonymous publish path, and the AUDIT-2 fourier register

## State
**Opens after**: F.W14U CLOSED. **Minted by**: COHESION §0cz (2026-09-24). **Model**: Opus 5.5 every seat; design seats at effort high.
**Units**: serial — [`.s2`] → [`.p`] → [`.au0`] → the AUDIT-2 family units the open seat groups (`.au1`…, by surface, the way F.W14U grouped its UIA rows). **Record**: `docs/tranches/X/execution/C/F-W14V.md`.
**Repo**: fourier-analysis (`web/**`, `api/**` only for `.p` if the server half needs it). Glass is READ-ONLY; asks go to O-75/O-74 and the live glass session.

## Authority — the owner's words, verbatim (2026-09-24)
*"the background area between the two elements is not right--this should not be displayed--they should be distincitly there"* (frame: `docs/tranches/X/fourier/evidence/W14/owner-2026-09-24-configurator-shell-band.png`, READ it).
Earlier, the same day: *"the side controls pane in fourier should be seperated, not totally attached, like it is now"* · *"All issues should be fixed at the glass-ui root"* · *"Ensure proper design hierarchy and usage of space in all UIs hereof"*.

## §1 Units
### F.W14V.s2 (Opus, effort high) — the stage and the pane as two distinct elements (F.W14U `.s` landed incomplete)
1. **The cause** (O-75): glass `Configurator`'s shell (`Configurator.vue:169-199`) is one plate around both `#stage` and the aside. `.s` (`10c8e1a`) placed a detached `Card` inside it, so the gutter shows the plate. Read the frame, then measure at lg+ (1440×900 and 1024×768), light and dark: sample the pixels in the gutter between the stage's edge and the pane's edge. **Goal: the gutter reads the page ground** (the same pixels as the page outside the Configurator), and the stage and the pane each show their own surface edge.
2. **Consumer-owned contributors, cured at the root:** read the `glass-opaque` class on `.viz-configurator` (`VisualizationView.vue:342`) and every consumer rule that paints the shell or the stage. A consumer choice that makes the band opaque is corrected. Nothing may restyle glass's shell classes.
3. **The glass half:** if glass publishes O-75's detached placement (possibly in the early 10.x minor with O-68 `#actions`), pin it exactly, adopt it, and delete the `.s` `Card` wrap and its placement rule (`:528`). If not, record honest-RED **CONFIGURATOR-DETACHED** with the before frame and the measured gutter pixels. Paint no consumer copy over the shell.
4. **Gates:** the gutter-ground falsifier (a committed `web/e2e/f-w14v-detached.spec.ts`, headed, real GPU) is RED on the before bytes and GREEN ×2 after adoption, or honest-RED as above; mobile (< lg) is unchanged, with the sheet form GREEN.

### F.W14V.p (Opus) — the anonymous publish path (seen in the same frame)
1. The frame shows a raw toast, "Error · A session is required to publish." (`api/routers/visualizations.py:176` answers `owner_required`), overlapping the controls pane.
2. **Cure:** a signed-out Publish never ends in a raw error. It opens the inline sign-in (the shell's existing login surface) and resumes the publish after sign-in, or it is presented as needing sign-in before the click (a glass `Tooltip` or a DialogDescription). The typed 401/403 stays as the server contract. Toasts never cover the controls pane: their placement uses glass's Toaster offset, not a consumer overlay.
3. **Gates:** an e2e that publishes while signed out reaches the sign-in (never the raw toast) ×2; the api contract test for `owner_required` is unchanged and GREEN.

### F.W14V.au0 … — the AUDIT-2 fourier register (re-homed from F-W14U.md addendum (e))
F-W14U.md addendum (e) was written after F.W14U opened, so its plan never carried it (§0cz). **Its whole text is this wave's scope**, including:
- `docs/tranches/X/audit/AUDIT-2-fourier.md` (62 rows, `9c7552d3`; the "Routing § F.W14U" consumer-half list, 56 rows);
- the re-baseline to fourier HEAD (the register audited `798c98f` plus uncommitted edits; F.W14U has moved the tree since);
- the views Lens 3 never read, including the Audit Log and tablet 768×1024 and 1024×768;
- the cross-app rows (useSafeStorage, AdminFlaggedPanel, the four search inputs, the easing pickers per O-74a E-3);
- the §11 type rows, HELD for glass's ruling (O-74a E-2).

`.au0` re-baselines and measures. The open seat groups the rows into family units after it. A row F.W14U already cured by its UIA twin is cited, not redone. Safe areas are measured with the CDP override.

## §2 Close
Every unit's rows are CURED (falsifier GREEN ×2 on the served page, :3100 against the API on :8000), ADOPT-AT-LANDING (with the O-74 or O-75 row), or honest-RED with a relayed id. `vue-tsc` 0, vitest GREEN, full e2e `--workers=1` GREEN except the named honest-RED set. The row flips CLOSED on a CONFORMANT check.

## ADDENDUM 2026-09-24 (a) — `.c3`, the editor dock's leftovers from F.W14U.c2 (COHESION §0dc)
- **The magnet's state must show without opening a menu.** `dd123a9` moved the Magnet toggle into the More-tools menu. When the magnet is on, the closed dock now gives no sign of it (before, a red glyph sat in the row). Ruled: show state where it is visible. The More-tools trigger carries an "on" mark in the magnet's hue while the magnet is engaged, through glass's published DockControl props or tokens, never a consumer overlay. If glass has no mark or badge seat on DockControl, ask for it in a dated addendum beside O-76 and record honest-RED MAGNET-STATE-HIDDEN.
- **The menu rows' icon-to-label gap:** Smooth, Simplify and Reset have none. Use glass's `DropdownMenuItem` icon anatomy (its published icon slot or gap), not a consumer margin.
- **Gates:** headed frames at 1440 and 390, both themes; a falsifier for the mark (magnet on shows the mark, off shows none) and for the gap (≥ glass's menu icon gap), RED before and GREEN ×2 after.

## ADDENDUM 2026-09-24 (b) — the glass half of `.s2` landed early; the leftovers (COHESION §0dh)
- **Landed ahead of the wave** (fourier `239845f`; record `db0058c7`): glass 10.1.0 exact; `layout="detached"` on /visualize (the `.s` Card wrap deleted); Decomposition's and Contour's resets moved into `#actions`, with the body rows deleted; `f-w14v-detached.spec.ts` RED ×2 before and 5/5 GREEN ×2 after. CONFIGURATOR-DETACHED and CONFIGURATOR-HEADER-ACTIONS are CURED. When `.s2` opens, it verifies this and does not redo it.
- **LAYER-HEADER-LABEL** (O-77): the section label truncates ("Decomposit…") once `#actions` takes a column. Glass's half is asked; the consumer records honest-RED and uses no override.
- **Ruled, the `.eq` seat's open question** (whether /equation moves onto the Configurator): **yes, if its layout is stage plus inspector.** The owner's complaint was inconsistency across fourier's pages ("inconsistent"), so one page shape means one primitive. A new unit, **`.eq2`**: measure /equation's shape. If it has a stage and a controls inspector, it moves onto glass `Configurator` (`layout="detached"`, its sections as ConfiguratorLayers with any reset in `#actions`). If not, record why in one line. Headed frames before and after at 1440 and 390, both themes.

## ADDENDUM 2026-09-24 (c) — unit `.u`: F.W14U's 26 open rows (COHESION §0dp)
Scope: F-W14U.md addendum (h), whole: F-14 (BROKEN), F-68, F-74, F-79, F-81, F-85, F-173, F-183, the F-77 consumer half, the 15 PARTIAL rows (F-71 F-93 F-146 F-168 F-170 F-172 F-177 F-182 F-201 F-203 F-238 F-239 F-241 F-244 F-253), and the F-149 consumer half. It also covers the vc and f-w13-radius outside-set REDs, read diff-first, and the managed preview instrument. Bounds: `web/src/**`, `web/e2e/**`, and `api/**` for the server limbs. Every row ends CURED (falsifier ×2), or honest-RED with its glass id (O-82 for the anchor rows). The order is **`.s2` (verify) → `.u` → `.c3` → `.eq2` → `.p` → `.au0`…**. `.u` may split by surface (`.u1` stage/dock, `.u2` equation, `.u3` shell/toasts).

## ADDENDUM 2026-09-25 (d) — the standard sample image (COHESION §0ds)
The owner: *"We'll use this image for all of our samples going forward"*, and *"Public in the repo is fine."* fourier `assets/portraits/daraksha.jpg` (`4c38b12`, EXIF and GPS stripped) becomes the default upload sample in every `web/e2e/**` spec that uploads an image (settings-persistence, f-w14-control-row, f-w13-image-empty, f-w14-veil, f-w14u-d, f-w14-dpr, visualization-ux, f-w14u-s, fullscreen, f-w13-image-controls, visualization-crud, f-w14-residuals, and any other), through one shared fixture constant, not a per-spec path. A golden that changes because of the new sample is re-baselined with the diff described in the record. Assertions stay; only the sample changes.

## ADDENDUM 2026-09-25 (e) — the section nav is ONE dropdown at every width: owner ruling, reversing UIA-F-151's tab limb (COHESION §0dw)
- **The owner, verbatim, on a 1440 dark frame of the fourier dock showing Paper · Visualize · Gallery · Equation · Morph as an inline tab row:** *"this should be a dropdown, not expanded out into paper, visualize, etc."*
- **Ruling:** the five sections are the one `DockTrigger for="dropdown"` menu (`nav-trigger`: the current section's glyph and name plus the chevron, accessible name `navName`) **at every viewport**. `web/src/components/layout/AppDock.vue:76` `inlineNav = useMediaQuery("(min-width: 1024px)")` and the `<nav v-if="inlineNav">` `DockControl shape="tab"` branch (`:125-138`, from b744993 F.W14U.shell F-151) are deleted, along with the doc comment's F-151 bullet (`:12`, `:18-20`). The dropdown branch loses its "Below 1024px" guard. The F-231 and X-5 behaviour stays: non-modal, the current section in the name, glyph plus chevron below 24rem, and a route outside the five claims none.
- **The tests follow the ruling; they are not weakened.** The e2e assertions that pin the ≥1024 tab row (`e2e/f-w14u-shell.spec.ts` F-151 cases and the `shell-header.spec.ts` width split) are rewritten to assert the dropdown at 1440, 1024, 768 and 390: one `nav-trigger`, no `nav[aria-label=Sections]` tab row, the menu items with `aria-current` on the active section, and keyboard open/close. Each changed assertion is named in the receipt as an owner-ruling re-baseline (§0bt adjacent-edit law), never a silent deletion.
- **Unit:** this is `.nav`, run next in Track C's order before close. Falsifier: at 1440 the dock holds exactly one section trigger and no inline section links, RED at the before bytes and GREEN after, ×2. It is served at `:3100` light and dark at 1440 and 390, with frames in the receipt.

## ADDENDUM 2026-09-25 (f) — two more owner frames: the playback dock's floating readout, and the dark-mode toggle's size (COHESION §0dx)
Frames: `docs/tranches/X/fourier/evidence/W14V/owner-2026-09-25/{playback-dock-floating,dark-toggle-small,section-nav-row}.png`.
- **`.pd`: the playback dock** (*"this dock is still wrong and has floating elements"*). In `/visualize`'s `AnimationControls.vue` `GlassDock` (dark, collapsed posture), the Play/Pause `DockControl` sits in `#persistent`, then a short track, then the speed readout `1` and `×` **outside the pill's surface**, floating on the canvas. Find the root: whether the collapsed summary's readout, or the `#persistent` region (rendered outside `.dock-layers`), or the dock's width token (`--animation-dock-max-width`) leaves content outside the glass surface. The bar: every element of the dock sits inside one glass surface at both postures, at 1440, 1024 and 390, in light and dark. Cure the consumer half at its root. If the geometry fault is glass's (`#persistent` placement or the collapsed summary's width), write the relay into `../glass-ui/docs/tranches/BK/coordination/` and mirror it to value.js `docs/tranches/X/relay/` (next O-number after O-83, in INBOX), and hold that consumer bit honest-RED until the fix lands. No consumer shim over a glass geometry fault.
- **`.dm`: the dark-mode toggle** (*"the darkmode toggle is too small as well"*). `DarkModeToggle.vue`'s `FourierMorphSvg` glyph inside `DockControl` reads visibly smaller than the dock's other glyphs. The bar: its optical size matches the dock's icon size token (the same box as the section and account glyphs, measured by bounding box at 1440 and 390) while keeping the morph glyph and the identity colour. This goes with `.nav` (same dock, same seat).

## ADDENDUM 2026-09-25 (g) — the daraksha sample switch landed; two routings (COHESION §0dv, executed)
- **Landed:** fourier `0eaff2b` (pushed, `m/w1-bump-migration`). `web/e2e/fixtures/sample.ts` exports `SAMPLE_IMAGE` = `assets/portraits/daraksha.jpg`. All 16 upload specs, `global-seed.ts` (content type fixed to `image/jpeg`) and the f-w12 probe use it. `contour-extraction.spec.ts` still iterates `assets/animals/`. The contour step on the new sample takes 7.8 s, inside every wait. Runs: 92/1/3 and 91/2/3.
- **Routing 1, to the u4 owner: `f-w14-residuals` G-c1 is stale** (RED at HEAD with either sample). It asserts the aside's "Uploading the image" bar on a *first* upload, but `8e19043` (u4, F-71 ⊕ F-238) made the first upload's single busy mark the stage button's, keeping the aside bar only for a replace. Re-baseline G-c1 to u4's rule (first upload: the stage button busy and no aside bar; replace: the aside bar) as a named adjacent edit (§0bt). Do not delete it.
- **Routing 2: 28 committed evidence PNGs under `web/e2e/screenshots/f-w14/`** (`after-page-*`, `after-veil-*`, `after-dpr-epicycles-*`) were rewritten in the working tree by the sample-switch runs and are **uncommitted**. Under E-3, dated evidence is not rewritten. Restore them to HEAD (`git checkout -- <those paths>`) unless the current seat is deliberately re-capturing them, in which case commit them as a new, dated capture with a receipt line. The seat at the next start decides and records which.

## ADDENDUM 2026-09-25 (h) — the four owner-held escalations RULED (COHESION §0eb, the orchestrator under the owner's grant "Ratify the above with your judgement")
Check 3 (NOT-CONFORMANT, `5 failed` reproduced) named C3-1..C3-4 as owner-held. G-c1, the 28 PNGs and the LW-1 restatement are already GREEN (Repair 1). The rulings:
1. **C3-1 HIGH, ESC-u1-2 = ESC-au2-1 (A2-FO-L2-15 at 360/390/430, vedit v88): option (b), extended.** Below `sm`, the expanded editor dock's row holds **Undo, Redo and More editor tools** only. **Delete** and **View options** move into the More editor tools menu: Delete as its last item behind a separator, keeping its destructive tone and confirm path; View options as a submenu group, the same `ViewLayersMenu` rows. The persistent Metric and Save are unchanged. At ≥ sm nothing changes.
   - **Why:** the destructive, infrequent action belongs behind one more step on a phone. Removing Delete alone does not fit 360 (the row is about 167 px), so View options follows. Both are menus already, so no control is lost.
   - v88 is restated as a named owner-ruling re-baseline (§0bt) to the new set, and the L2-15 falsifier asserts `scrollWidth <= clientWidth` at 360/390/430. RED before, GREEN ×2 after.
   - **The glass half, relayed as O-85 DOCK-PRIORITY-OVERFLOW** (a dock priority-overflow seat, the 11.0.0 dock band with O-65). When it lands, the consumer's manual `< sm` split becomes the glass idiom (ADOPT-AT-LANDING then). The row is CURED now, not held.
2. **C3-2 MEDIUM, ESC-au3-1 (L1-12, CardTitle off glass's type scale at 23.67 px): option (a).** Glass puts `.card-title` on a named rung (`--type-heading`). This is relayed as **O-74b CARD-TITLE-RUNG** and asked for 10.2.0 band 0 (mechanical, one declaration). L1-12 is **ADOPT-AT-LANDING**: at the 10.2.0 repin the consumer adopts `Card`/`CardHeader`/`CardTitle` at both sites and deletes the hand-set rung, and G-h and the au3 falsifier must both be GREEN. The CardTitle adoption is not re-landed before then.
3. **C3-3 MEDIUM, ESC-u1-1 (F-81 plus F-9's final form): split.**
   - **(i) One easing catalogue now.** The six stored animation keys migrate to the one catalogue's names **at the root, server-side, with no alias layer** (standing law: no backwards-compat shims):
     - a one-time idempotent migration (`scripts/` or `api/migrations/`) rewrites stored visualization documents;
     - `api/models/shared.py:69` takes the catalogue's `Literal` with its default;
     - the client's persisted view state bumps its storage version, so old local values are discarded rather than aliased;
     - an api test proves a legacy document reads migrated.
     - `api/**` is granted for this limb. The morph pickers stay ADOPT-AT-LANDING on O-74a E-3 (`.au3`).
   - **(ii) The Timeline transport** is glass's: relayed as **O-86 TIMELINE-TRANSPORT** (the 11.0.0 band), ADOPT-AT-LANDING. There is no consumer-built transport.
   - G-u then reads 255/256 dispositioned, plus one ADOPT-AT-LANDING.
4. **C3-4 LOW, A2-FO-X-7 against UIA-F-59: both are satisfied.** At ≥ lg the inline paper search results render **inside the sidebar column** (the plate's inline size is the sidebar's, never breaking out over the article), and each result title **wraps** (multi-line, `text-wrap: pretty`, no ellipsis). That cures F-59's truncation without X-7's breakout, in line with the owner's no-ellipsis ruling (§0dy). Falsifier: the plate rect is inside the sidebar rect at 1024/1440/1920, and no result title has `text-overflow: ellipsis` in effect or is clipped (`scrollWidth <= clientWidth`).
**Order:** `.r1` (L2-15) → `.r3` (the easing migration) → `.r4` (X-7), then close and check. L1-12 and O-86 are honest-RED or ADOPT-AT-LANDING only.
