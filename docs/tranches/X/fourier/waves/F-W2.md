# X.F.W2 — value.js consumption to spec: root retirement, colour routing, facility 19

**Sub-tranche**: X·F (`docs/tranches/X/fourier/waves/`) · **half**: **frontend** — W.L5's fourier half under the **D-15 direct-edit grant** (fourier only; no keyframes analogue, RD-11) · authority `registry/adjudicated/library-band.md` §W.L5 (`:294-313`) + RD-6/RD-7/RD-8/RD-11 · **Status: planned**

**Fold provenance (M-23 / M-12)**: this file is the fold-seat output, 2026-08-28 — twice-authored (two Fable arms), agglomerated choosily: arm B's structure, live seat measurements (§2b anchor reconciliation, G15–G18), and gate table carried whole; arm A's inline ruling texts (R-i/R-ii, §4) grafted as the wave's proposed dispositions. Verification receipt (**RE-BASED at repair round 1, 2026-08-28 — R-3**): the closure instrument is no longer a per-wave carry. **No `F-W2-CARRY.md` exists in tree** — `ls docs/tranches/X/fourier/carry/` → `F-W1-CARRY.md`, `F-W4-CARRY.md`, the program's ONLY two carries, citable only by path. G19 now closes against the **PASS-1 ID-keyed census roster — 54 routed ids** at `docs/tranches/X/fourier/conformance/PASS-1/F-W2-CHECK.md` §1–§2 — and the 66-record registry directly (§8). The fold seat's "26 of 26" receipt is retained below only as a historical note on the wave's own §3 row ledger; it carries **no gate weight**. Fold re-probes 2026-08-28 confirmed G1/G2/G3/G5/G15/G16/G17 live and **RESHAPED G18** (no throw on string input; the corrupt-tail `css_syntax` diagnostic instead **corroborates FR-NP-32** — G14 amended to match). Neither authoring arm is the wave; this fold is the wave's spec.

**Repair round 2 (2026-08-28) — `conformance/PASS-2/RULINGS-2.md` + `PASS-2/F-W2-CHECK.md` applied; every load-bearing figure below re-derived AT THE BYTES by this seat this round.** *(Round 2 landed in two sittings: the first was cut off mid-file, having corrected the §2/§3 statement sites but not the §5 gate table, §6 sequencing or §7/§8 rosters — so the same ten figures stood corrected in one half of the file and stale in the other, which is worse than uncorrected. The resume seat re-ran **every** witness command from scratch rather than trusting the half-landed text, and swept each figure to **all** of its statement sites. **A figure corrected at one site and left standing at another is not a corrected figure** — the resume is minuted here so no reader mistakes the split for two measurements.)*

- **R2-1-LAW · QUOTE-BY-COMMAND.** Every cross-spec and corpus quotation in this file carries a **⟨cmd⟩** provenance note naming the `grep`/`sed`/`node` command this seat ran **this round**. A *"verbatim"* label without a ⟨cmd⟩ note is a defect per se. **Rulings files are cure-shapes, never quotable authority** — where a ruling paraphrases a bank, this spec quotes the BANK, by command, at the bank's own bytes. Composites are never fused under one attribution.
- **R2-2 · THE ANCHOR IDIOM.** Every cross-spec citation is a **STABLE anchor** — §-heading, gate id, clause id, banked row id, or row label. **No line number into a live sibling survives in this file** (`waves/**`, the two carries, COHESION) — verifiable by ⟨cmd⟩ `grep -noE 'F-W[0-9]+\.md:[0-9-]+\|W9\.md:[0-9]+' F-W2.md` → **∅**, run at the close of this round. Line numbers remain only for (i) the frozen corpus (`registry/adjudicated/**`, immutable), (ii) product-source and probe anchors, which D-19/MEASURE-AT-OPEN governs, and (iii) this file's own intra-file coordinates, marked *at this writing*. Where R2-2.3 prescribes an anchor, the anchor written here is the one this seat **measured** at the target's live bytes, never one copied from a ruling. **The conversions performed this round, by class**: the `:253`/`:272` class ×7 → **F-W5 §4's `F.W5 → F.W2` edge row** (export) and **F-W5 §5's `P-9` row** (home) · the F-W0 class ×8 → **F-W0 §4 gates `G-11`/`G-12`/`G-13`**, **F-W0 §2a's file-bounds `SUBSTRATE-LEDGER.md` create row**, and **F-W0 §3's carry row 1 (`FR-NP-32 ≡ fr-PaperSidebar M1`), gate `G-4`** · the F-W1 class ×3 → **§2 WU-K's `L/B-1 + C/B-1` row · §2 WU-R's `ESC-4` row · §3 gate `G12`** · the X-W9 class ×2 → **X-W9 §Hard Gate gates `G24`/`G33`** (with `G21`). **Every struck coordinate is recorded as struck, never silently re-keyed** — three of them had already drifted twice in two rounds, which is the demonstration the idiom rests on.
- **R2-9 · SHAPE- AND SPELLING-AGNOSTIC CLOSURE.** G19 states its detector INLINE (§5).
- **Ten corrected figures** (R2-4 + the PASS-2 local register), each by its own witness command below **and each swept to EVERY site that states it, by enumeration of the sites rather than by a self-counting grep** — a `grep -c` receipt written INTO the file it counts is falsified by its own bytes the moment it is written, so the sweep is stated as its site list. The sites, all re-read at the close of this round: `hexToRgba` 5/3 → §2a's `BasisCanvas.vue` row · §2b item 6 · B7 · **§5 G5** · **§6a's same-commit rider** · this masthead. `DIRECT_EASINGS` 19 → A2 §3 · **§5 G2**. glass-ui 80 → B9 · **§5 G8**. G13's 1-line/1-file → **§5 G13**. The `:94-114` anchor → A6's drift figure · **§5 G15**. `basis-display.ts:1-7` → §2a · B4 · **§5 G4**. The amber cut 9 → B3 · **§6a's one-cut lock**. §2b item 3 → §2b alone. The §3 ledger 29 → §3's head · §8b. The roster TWELVE → §6a · §6b's F.W1 row. **Every site in bold above was stale after the first sitting and is corrected here**; every superseded figure that remains in the file remains ONLY inside an explicit strike note that names it as superseded, which R2-2.3 requires and G-12 forbids reading as current. The figures themselves: `hexToRgba` **5 call sites / 3 files** (was 4/3) · `DIRECT_EASINGS` **19 entries** (was 20) · glass-ui 4.0.0 exports **80 keys** (was 78) · G13's quantified witness **1 line / 1 file, `-o` → 2** (was "7 hits") · the `DIRECT_EASINGS` anchor **`src/easing.ts:94-114`** (was `:113-132` — transcribed from the drift probe's own stale inline comment, the M-25 class this wave polices elsewhere) · `basis-display.ts` **`:1-7`** (the banked anchor; was `:1-6`) · the amber cut **9 sites** (was "ten") · §2b item 3's convergence-directory enumeration (**two** SFCs, not one) · the §3 ledger **29 rows** (was 26) · the F.W1 transaction roster **TWELVE limbs** (R2-5).

**Nothing in this round changes a banked word, a gate's born-RED state, the census (54 routed / 54 landed / 0 escaped), or the wave's posture. Every correction is a figure, an address, or a provenance note.**

**Repair round 3 (2026-08-28) — `PASS-2/RULINGS-2.md` + `PASS-2/F-W2-CHECK.md` re-executed AT THE BYTES; every witness command re-run from scratch, nothing trusted from the round-2 text.** The twelve PASS-2 local defects and all four RULINGS-2 directives addressed to F-W2 (R2-4 · R2-5 · R2-9 · R2-1-LAW) **re-verified as landed and correct** — ⟨cmd⟩ receipts re-run this round: `hexToRgba` **5** (`golden-shimmer.ts:55`,`:58` · `epicycles.ts:258`,`:283` · `BasisCanvas.vue:261`) · `DIRECT_EASINGS` **19** with the block at **`:94-114`**, `CubicBezier` `:116-129`, `steppedEase` `:131+` · glass-ui exports **80**, `./package.json` **absent** from the keyset, `./dom` **present** · G13 **1 line / 1 file, `-o` → 2** · amber **1 + 8 = 9** · `equation/convergence/` = **two** SFCs · `basis-display.ts` literal opens `:3`, closes `:7` · `colors.ts` **117** lines · census detector **68 occurrences / 57 lines / 25 records, sole form `F.W2`, en-dash arm ∅** · `easing` exports **16**, `bezierPresets` **30**, fourier's `EASING_LABELS` **22** · `spectrumColor` **4** · `sampleColorRamp|mixColorsN` **0** · `colorUnit2|color2` **0** · every stable anchor this file cites re-resolved at its target (**F-W0 §2a**'s create row · **§3** carry row 1 · **§4** `G-11`/`G-12`/`G-13` · **F-W1 §2** WU-K/WU-R · **§3** `G12` · **§4** step 4 and cross-edge 1 · **F-W5 §4**'s `F.W5 → F.W2` row and **§5**'s `P-9` row · **X-W9 §Hard Gate** `G21`/`G23`/`G24`/`G33`) · the anchor-idiom self-receipt **still ∅**.

**Eight defects the round-2 text carried are CURED here, and they are all one class: a ⟨cmd⟩ note reporting what the seat expected instead of what the command returned.** A provenance idiom does not enforce itself. (1) **A FABRICATED QUOTATION survived the round that outlawed them** — *"NO-WAVE-OWNER (producer emitter) + F.W0 pre-gate + relay item 0"*, attributed at **B11** and **G14** to F-W0 §3 carry row 1's R-column, is at **no byte of F-W0** (⟨cmd⟩ `grep -o 'NO-WAVE-OWNER (producer emitter)' F-W0.md` returns **no output** — the count form rounds 3 used here is converted at repair round 4 under R3-3.10); it was a fusion of the banked `fr-NotationPills.md:35` disposition with that row's separate pre-gate sentence. Struck; replaced by the row's real cell (**`G-4`, relay item 0**) and its real sentence, each by command. (2) **§6a's vaul-vue receipt said "six hits" over a four-site enumeration; the command returns EIGHT** — corrected with the true spanning list. (3) **§6b named a third `zero-cells` row in F-W5 that does not exist** — D3, quoting the same command, named only the two real ones, so one command was reported two ways inside one file. *(Round 3 fixed the integer; **repair round 4 retires the form** — both cells now state SET-MEMBERSHIP, the `F.W5 → F.W2` edge row and the `P-9` row by label, per R3-3.10.)* (4) **The W.L5 3-leg probe was point-cited `library-band.md:295` while its words span `:295-296`** — a false witness by the file's own soft-wrap rule, whose count word ("two soft-wrapped spans") is corrected to **three**. (5) **§6b rendered the banked FR-NP-32 disposition with `+` for `;`**, dropped *glass-ui*, lower-cased *TOP*, and kept the word *banked* — conformed. (6) **D3's P-9 quotation elided `(B5)` without an ellipsis** — restored. (7) **Two casing misquotes inside verbatim labels** — the bank writes *"One identity, one booking, forever."* and *"Booking the fix behind a cut…"*; both corrected, the same one-character class this file already catches at A2's up-cased `execution`. (8) **G7's "verbatim" label and R-iii's two corpus quotations carried no ⟨cmd⟩ note** — attached, per R2-1-LAW.2, which makes the bare label a defect per se whatever the words say. **Every superseded string above is retained only inside its own strike note. Round 3 changes no figure, no banked word, no gate state, no census number, and no posture — the ten round-2 figures all reproduce.**

**Repair round 4 (2026-08-28) — `conformance/PASS-3/RULINGS-3.md` + `PASS-3/F-W2-CHECK.md` applied at the bytes; every figure re-run by this seat, nothing trusted from the round-3 text.** RULINGS-3's three directives to F-W2 (**R3-5.1 · R3-9.1 · R3-3.9 purge share**) and the standing amendment **R3-3.10** are landed, and **§X.1-v4 is consumed fresh from F-W3's landed bytes** (the twin partition published FIRST this round per R3-1a) at every row where F.W2 hands to the twins — §6b's F.W3/W4 edge and B5.

**The round's headline is one defect and one class.** **THE DEFECT (R3-5.1 · PASS-3 D-1, HIGH): G19's detector was POSITION-RESTRICTED, and it reproduced its number and its blind spot together.** `grep -nE 'F[.·–-]?W2'` cannot match a slash form whose SECOND member is `W2`; `fr-PathPreview` routes two rows to **`F.W1/W2`** and was invisible to the gate's operand, to §8's roster and to three mastheads at once. **This is the exact failure G19's own RED-input cell describes — *drop a row silently* — surviving inside the gate written to prevent it.** Aggravating, and minuted because it is the lesson: **F-W1 §4 step 4 books *"PP-REDGATE ambient declaration"* as a limb of the TWELVE, and this file cites that very sentence twice as its extent authority.** F.W2 quoted the charter containing the id and did not see the id. Cured: the detector gains **POSITIONS** and **RECORD-QUALIFICATION** as declared axes (four in all); `PP-REDGATE`, `fr-PathPreview` roster item 7, and `fr-GalleryInfiniteGrid C-6` (PASS-3 D-9 · R3-2.5 — never a detector miss, a booking miss) are homed as **§7 exclusions-with-reason**, enumerated at **§8c**; the census re-states as **57 routed = 54 booked ⊕ 3 excluded-with-reason, 0 escaped**. **⚠ Every "54 routed / 54 landed / 0 escaped" figure standing in the round-2 and round-3 minutes above is SUPERSEDED by that line and is retained only as the history of a superseded run**; likewise the round-3 masthead's *"census detector 68 occurrences / 57 lines / 25 records … en-dash arm ∅"* — those numbers all reproduce, and reproducing was never the test.

**THE CLASS (R3-3.10, the standing amendment): no ⟨cmd⟩ whose output is a COUNT of a live sibling or of THIS FILE may be banked in a spec.** Round 3 cured eight receipts one at a time and left the shape that generates them. Two of the survivors were not merely unlawful — **they were FALSE at these bytes, and each was falsified by the very repair that discharged its defect**: G19's `grep -cE 'carry/F-W|F-W2-CARRY' F-W2.md` → **0** (the R-3 cure made this file cite `carry/F-W1-CARRY.md` by path) and G20's `grep -c 'EasingPicker|…' F-W2.md` → **0** (A6's cure filled the file with those very tokens). A banked self-count is a claim that its own correction refutes. **Six receipts converted to classifications, set-membership or `grep -o` word-output**: G19's carry probe · G20's origin-RED probe · §6a's `ELEVEN`/`TWELVE`/`vaul-vue` trio against F-W1 · §6b's and D3's `zero-cells` probe against F-W5 (**the split-figure that said three in one cell and two in another**) · B11's two F-W0 probes.

**The remaining seven, each cured at every site that states it.** **(1)** `fr-MorphShapePreview C·S-2`, labelled a PRESERVATION obligation at three sites, **did not byte-match the ⟨cmd⟩ printed beside it** — the bank's `carried.` dropped with no ellipsis and its capital `Preserve` lower-cased into a subordinate clause (D-2, HIGH). **(2)** RD-8's law, under this file's strongest label — *"binding and verbatim"* — carried **bold on `one-way` and `bidirectional` that the bank does not set** (D-6), on the clause that constitutes facility 19's entire law. **(3)** *"sixteen … against the real export"*: the export enumerates **TEN**; sixteen is F.W2's roster — ten export members (none dropped) ⊕ six F.W2 measured itself, now separated at D3, G11 and §7 (D-7). **(4)** The `:294-297` / `:295-296` self-contradiction three sentences apart in one paragraph — **receipt range and span are now stated separately and measured for all three soft-wrapped quotations** (D-5). **(5)** Two gate commands were `…`-elided and could not execute, under a preamble reading *"Commands are literal"* (D-4); **every elided path in the file is completed** — ⟨cmd⟩ `grep -oE '…/[a-zA-Z@]' F-W2.md` returns no output — and **the completion was load-bearing at A1, where the two manifests the ellipsis could name return opposite answers.** **(6)** G12's four PNGs required writes at a path that does not resolve, under a Denominator note asserting they carried create markers; **both halves were false** — the artefacts enter §2a as create rows at the live `docs/tranches/V/megatranche/audit/probes/app-wave/`, and the note is restated truthfully rather than re-asserted (D-3, R3-9.1). **(7)** Two unmarked elisions/casings at A6 and D2 (D-8, D-10).

**Round 4 changes no banked word, no gate's born-RED state, no measured figure of the fourier tree or the 4.0.0 export surface, and no posture. It changes a detector, three homings, six receipt forms, seven quotations, one census total, and — under one file-wide base declaration (`$W`/`$K`/`$F`) — **ten elided command paths, one non-resolving gate path and six mixed-base sibling spellings**. And it strikes two receipts that had become false without anyone editing this file.**

**EXECUTION IS NOT AUTHORIZED BY THIS FILE.** Tranche X's execution gate stands: nothing opens, writes, builds, or runs product source until the owner's begin-word. `/Users/mkbabb/Programming/fourier-analysis` is **READ-ONLY at spec time**; every measurement below was taken read-only. Specifying is not doing.

**Measurement substrate (fold seat, 2026-08-28).** value.js `tranche-u`, `package.json` version **4.0.0**, built `dist/subpaths/`; fourier `cd26c65`, **28 dirty worktree entries**, `git cat-file -t 14d83356` → *not a valid object*; fourier pins `@mkbabb/glass-ui ^4.0.0` · `@mkbabb/keyframes.js ^4.3.0` · `@mkbabb/value.js ^0.13.0`; node v26, darwin arm64. Both banked spine probes were **re-run live and both are RED**. Every fourier line anchor below re-resolves at F.W0 (**D-19 / MEASURE-AT-OPEN**); §2b lists the eight anchors this seat already found stale.

---

## 1. State

**Opens after**: **F.W0 closed** (substrate pre-gates — hard, §6) · **F.W1 landed** for the specifier arm only (§6; the colours arm is *not* so gated, fr-App C-2) · the adjudicated registry whole.

**Units**: 4 — `.c` rulings (serial, first: records §4's R-i/R-ii/**R-iii** + the G2/G15 mapping re-derivation) → `.a` specifier+easing ∥ `.b` colours (parallel; no shared modify path) → `.d` facility 19 + the letter. **Peak concurrency 2**, inside the owner's four-workflow cap.

**Hard gate**: **20 conditions** (§5) — **G1–G14 carry their ids verbatim from this wave's authoring register** (R-3: there is no `F-W2-CARRY.md`; the only citable carries are `carry/F-W1-CARRY.md` and `carry/F-W4-CARRY.md`, neither of which owns these ids), **G15–G19 minted at authoring 2026-08-28**, **G20 minted at repair round 1, 2026-08-28**. **All 20 born RED.** G8, G20 and the AA-20 leg of G7 are **RULING gates** (this wave must rule, not cure). G14 is a **hard precondition F.W2 states and never closes**.

| verb | value | evidence |
|---|---|---|
| AUDITED | **YES** | **57 routed ids — 54 BOOKED (§8a) ⊕ 3 EXCLUDED-WITH-REASON (§7 + §8c)**, derived at repair round 4 from **the 66 `registry/adjudicated/fr-*.md` records DIRECTLY** through G19's four-axis detector: first-position `grep -lE 'F[.·–-]?W2' fr-*.md` → **25 records**; **second-position `grep -loE '[FW][.·–-]?W?[0-9]+[/–-]W2' fr-*.md` → `fr-PathPreview.md`** (form `F.W1/W2`) — the arm rounds 1–3 could not express; union **26 records**. ⊕ the non-literal W.L5 ACT1/ACT2/ACT3 routings ⊕ the declared inbound P-9 export at **F-W5 §4's `F.W5 → F.W2` edge row** — over `library-band.md` §W.L5 + `formation/fourier/{CENSUS-2026-08-03.md,lane-frontend.md,lane-docs.md}`. **`PASS-1/F-W2-CHECK.md` §1–§2's 54 is named as the PRIOR RUN, never the operand** (R3-4). Every §3 row cites its banked id |
| SPECIFIED | **YES — this file (the fold), 2026-08-28** | validated against `docs/precepts/instructions/tranche/WAVE_SPEC.md` (L-20, current at authoring); goal + hard gate paired |
| IMPLEMENTED | **NO** | — fourier bytes land only after F.W0 + F.W1 (specifier arm) + the begin-word |
| VERIFIED | **NO** | — stamped only at the X·F sub-tranche release close |

### 1a. Goal criterion

This wave succeeds if, when work ends, **fourier's value.js consumption is to spec at the 4.0.0 target with zero silently-dropped adjudicated rows**: the five bare-root specifiers resolve against 4.0.0's published subpaths; the five `colors.ts` hand-rolled colour arms are **gone rather than extended**, with their live consumers joined in the same commits; the curve and colour a user sees is provably the one that shipped before (or a declared, probe-pinned divergence — G15); the **three** contests this wave owns (M-RTC seat, AA-20 charter, and the easing cluster's two cure-shape locks) are **ruled or routed, never sidestepped and never pre-empted**; facility 19 exists under RD-8's one-way law with all seven FN answers on the record; and every hand-rolled colour authority in the repo is either named in the charter or **excluded with its reason in writing**. The wave's own failure mode is named by FR-GFC-8 and is the criterion it is measured against: *"the colors.ts migration must fix the SNAPSHOT, not merely swap `cssVarToHex` for value.js's parser, or the wave closes green on a surface that never changed colour."*

### 1b. What this wave is NOT

It does not repair the palette resolver (F.W1 owns the banked fr-BasisCanvas D-1/BC-1/C-1 identity and the D-2/BC-5/C-3 snapshot fix), does not land the ramp (F.W3/W4), does not cure `actionTone` or `energyColor` (F.W4), does not touch `fourier-analysis/web/package.json` (F.W1's atomic transaction), does not edit any value.js `src/**` (X-W9 / W.L6), and does not answer FN-1/2/3/4/6 on the merits (F.W5/F.W6). Per **FR-GIG-5's bar**, F.W2 claims credit for nothing F.W1 owns — and F.W1 claims none of F.W2's.

---

## 2. Bounds

### 2a. File Bounds

| File | Access |
|---|---|
| `/Users/mkbabb/Programming/value.js/docs/tranches/X/fourier/waves/F-W2.md` | **create** — this spec |
| `/Users/mkbabb/Programming/value.js/docs/tranches/X/fourier/facility-19.md` | **create** — the RD-8 one-way correspondence ledger (measured absent). Home is owner-confirmable; SS-3/SS-4 may re-seat it beside the 18 HTTP facilities |
| `/Users/mkbabb/Programming/value.js/docs/tranches/X/fourier/RULINGS-F.W2.md` | **create** — the record of the four rulings this wave owes: AA-20 widen-or-exclude (G7, R-i), the M-RTC cure-seat contest (G8, R-ii), **the easing cluster's two cure-shape locks — MPC-2 and CENSUS-MC — routed to F.W1 (G20, R-iii)**, and the `easing()` mapping re-derivation (G2/G15). §4 authors the proposed texts; unit `.c` records them — that recording is when G7/G8/G20 go GREEN. **Measured ABSENT at repair round 1** (`ls docs/tranches/X/fourier/RULINGS-F.W2.md` → *No such file or directory*) |
| `/Users/mkbabb/Programming/value.js/docs/tranches/V/coordination/INBOX.md` | **append-only** — E13 rows for the FN-1..FN-7 + 2.0.0-note reply and the facility-19 relay. No rewrite of any existing row |
| `/Users/mkbabb/Programming/value.js/docs/tranches/X/coordination/value-to-fourier-2026-08-DD-fn-answers.md` | **create** — the reply letter (the only vehicle) |
| `/Users/mkbabb/Programming/value.js/docs/tranches/X/coordination/value-to-glassui-2026-08-DD-token-color-seat.md` | **create** — the SS-6 BH relay (three asks, §6) |
| `docs/tranches/V/megatranche/audit/probes/app-wave/fourier-viz-light-before.png` · `docs/tranches/V/megatranche/audit/probes/app-wave/fourier-viz-light-after.png` · `docs/tranches/V/megatranche/audit/probes/app-wave/fourier-viz-dark-before.png` · `docs/tranches/V/megatranche/audit/probes/app-wave/fourier-viz-dark-after.png` | **create ×4** — **G12's artefacts, brought inside bounds at repair round 4 (PASS-3 D-3 · ruling R3-9.1).** A gate whose GREEN requires writing four files the wave has no bounds authority to write is the phantom-witness class R-10.6 cured for G5/G8/G11/G13/G17, left standing one row away from that cure. The directory is live and holds this program's app-wave probe artefacts already — ⟨cmd⟩ `ls docs/tranches/V/megatranche/audit/probes/app-wave/` → `BORN-RED-2026-07-27.json`, `app-shell-truth-probe.mjs`. **The path G12 carried through rounds 1–3, `audit/probes/app-wave/…`, does not resolve from the declared cwd** (⟨cmd⟩ `ls /Users/mkbabb/Programming/value.js/audit` → *No such file or directory*); it is re-pointed here and at G12 in one edit. Force-added past `.gitignore:34` (⟨cmd⟩ `sed -n '34p' .gitignore` → `*.png`). This is a **value.js-tree** create; the fourier tree stays READ-ONLY |
| `docs/tranches/V/vnext/API-FACILITY-ISOMORPHISM.json` · `docs/tranches/V/vnext/api-facility-isomorphism.schema.json` | **READ** — precedent only. **RD-8 ruled facility 19 takes a DIFFERENT, one-way law; do not extend this file's bidirectional schema** |
| `docs/tranches/V/megatranche/registry/adjudicated/**` (66 `fr-*.md` + `library-band.md`) | **READ ONLY** — immutable (E-1/E-3) |
| `docs/tranches/V/megatranche/formation/fourier/{CENSUS-2026-08-03.md,lane-frontend.md,lane-docs.md}` | **READ ONLY** — `:38` five-site inventory · `:480` peer-floor · §8b/§8c/`:473-483` obligations |
| `docs/tranches/V/megatranche/audit/probes/{fourier-value-import-drift.mjs,fourier-vizcolor-oklch.mjs}` | **READ + EXECUTE** — the wave's two spine witnesses; both live, both RED at authoring |
| `/Users/mkbabb/Programming/value.js/src/subpaths/{easing,math,color,css}.ts` · `package.json` | **READ ONLY** — the 4.0.0 export surface; gate witnesses. **Not an edit surface**; value.js library changes are X-W9 / W.L6 |

**Fourier edit bounds — unreachable until the begin-word.** Vehicle: the D-15 direct-edit grant, fourier half only. Paths rooted at `/Users/mkbabb/Programming/fourier-analysis/`.

| File | What this wave does | Carry |
|---|---|---|
| `web/src/lib/easings.ts` `:9` `:10-16` | retire 2 bare-root statements; re-derive the `timingFunctions` map | CENSUS-38, W.L5-ACT1, G15 |
| `web/src/components/equation/ConvergencePlot.vue` `:5` `:11` | specifier → `/easing`; re-point the `lerp` import | CENSUS-38, C-19 |
| `web/src/components/equation/composables/useCurveTransition.ts` `:8` `:85` | specifier → `/easing`; **delete the sole hand-rolled `lerp`** → `/math` | CENSUS-38, C-19, SC-4 |
| `web/src/components/equation/lib/harmonics.ts` `:5` `:81-88` | specifier → `/easing`; the `spectrumColor` fork is **locked here, landed in F.W3/W4** | CENSUS-38, M-β1 |
| `web/src/lib/colors.ts` `:22-117` | **delete** `cssVarToHex`/`hslToHex`/`rgbToHex`/`hexToRgb`/`hexToRgba`; declare the 3-line hex residual with a validation arm + a deletion date | W.L5-ACT2, RD-6/RD-7, G6, G17 |
| `web/src/components/visualization/lib/basis-display.ts` `:1-7` | the FR-GFC-8 snapshot **wave-lock** (green-criterion; the fix is F.W1's). **Anchor corrected at repair round 2 to the BANKED range** — ⟨cmd⟩ `grep -n 'basis-display' fr-GalleryFeaturedCarousel.md fr-GalleryInfiniteGrid.md` → both records read **`basis-display.ts:1-7`** (fr-GalleryInfiniteGrid C-5 adds *"(read whole)"*); ⟨cmd⟩ `sed -n '1,9p' /Users/mkbabb/Programming/fourier-analysis/web/src/components/visualization/lib/basis-display.ts` → the `basisDisplay` literal opens `:3` and closes `:7`. The fold seat's `:1-6` was short by the closing brace | FR-GFC-8, G4 |
| `web/src/lib/golden-shimmer.ts` `:55` `:58` | the C-2 join — swatch and shimmer reunite on one source, **same commit as the `hexToRgba` deletion** | C-2, G5 |
| `web/src/components/visualization/lib/canvas-drawing/epicycles.ts` `:258` `:283` | second + third `hexToRgba` consumer; joins the same commit | M-α5, G5 |
| `web/src/components/visualization/BasisCanvas.vue` `:261` | **the fifth `hexToRgba` call site and the third file — absent from the authoring register's bounds**; joins the same commit. The complete set is five coordinates in three files (G5) | G5 (seat-corrected, re-sized at repair round 2) |
| `web/src/components/visualization/ContourPreview.vue` `:45` + `ContourEditorCanvas.vue` `:261,:307,:308,:315,:319,:328,:329,:333` | mint `--contour-stroke` once; cure **9 sites / 2 files / five alphas** as ONE cut | ContourPreview rows 1/14/22 |

**EXPLICITLY NOT IN BOUNDS** (F.W1 owns; F.W2 claims no credit — FR-GIG-5): `web/package.json` (the tri-package pin) · `App.vue:11,:13-17` (resolver trigger + MutationObserver) · `main.ts` (C-4's resolver re-seat) · every glass-ui producer file (GLASS-RELAY / SS-6 only). Also never: `scripts/dev/dev.sh` (unowned, never staged) · dated evidence files (append-never-rewrite) · anything direct parse-that→fourier (**FORBIDDEN**).

### 2b. D-19 anchor reconciliation — eight authoring-register anchors are stale at HEAD

⟨**R-9.1 · this section is an INPUT TO / QUOTATION OF F.W0's G-11 ONE corrected anchor table** — cited by **gate id: F-W0 §4 gate `G-11`**, whose heading is its own citable identity ⟨cmd⟩ `grep -n '^### G-1[0-9]' F-W0.md` → `G-11 — ONE corrected anchor table published; every later wave quotes it` · `G-12 — ONE corrected-denominator table published; superseded figures FORBIDDEN downstream` · `G-13 — The producer pin is re-derived at the ADOPTED commit; no F.W1 sizing quotes a 7.0.0 anchor`. The table lands in `fourier/docs/tranches/F/SUBSTRATE-LEDGER.md` — **F-W0 §2a's file-bounds `SUBSTRATE-LEDGER.md` create row** ⟨cmd⟩ `grep -n 'SUBSTRATE-LEDGER' F-W0.md` (the create row names the G-11 anchor table, the G-12 denominator table and the G-13 pin table as that file's contents). **G-12's corrected denominators** (**F-W0 §4 gate `G-12`**) govern every figure below. **Addresses converted to the stable idiom at repair round 2 (R2-2): all four of the fold seat's F-W0 line coordinates — the G-11, G-12 and G-13 cites and the SUBSTRATE-LEDGER cite — had drifted onto a blank line, an "Owning rows" cell, a second "Owning rows" cell and the wrong bounds row respectively, and a line number into a live sibling is a defect from that ruling forward. Gate ids and a file-bounds row label cannot drift.** F.W2 does **not** re-perform the re-resolution as its own rival act: the eight corrections are F.W2's **contributions** to F.W0's table, published there and quoted here. **Divergence between this list and that table is a defect against G-11, not a competing measurement**; superseded figures are forbidden here as there.⟩

Measured read-only, 2026-08-28. These are corrections to the authoring register's own bounds, not new defects. **§2b is this wave's pre-discharge of K-24 against its own bounds.**

1. `basis-display.ts` is at **`web/src/components/visualization/lib/basis-display.ts`**, not `web/src/lib/`.
2. `epicycles.ts` is at **`web/src/components/visualization/lib/canvas-drawing/epicycles.ts`** — the `lib/` segment is missing from the authoring register's path.
3. `ConvergencePlot.vue` is at **`web/src/components/equation/ConvergencePlot.vue`**, not under `equation/convergence/`. **CORRECTED at repair round 2** — the fold-seat form of this item (*"only `ConvergenceLegend.vue` lives there"*) was itself wrong, and a wrong correction inside F.W0's single owned anchor table is worse than no correction: ⟨cmd⟩ `ls web/src/components/equation/convergence/` → **`ConvergenceLegend.vue`, `ConvergenceTimeline.vue`** — **two** SFCs live there; `ConvergencePlot.vue` is neither of them. ⟨cmd⟩ `find web/src -name 'Convergence*.vue'` → `equation/ConvergencePlot.vue` · `equation/convergence/ConvergenceTimeline.vue` · `equation/convergence/ConvergenceLegend.vue`.
4. `golden-shimmer.ts` calls `hexToRgba` at **`:55` and `:58`** — two calls, not one at `:52`.
5. `epicycles.ts` calls it at **`:258` and `:283`** — two calls, not one.
6. **`BasisCanvas.vue:261` is a `hexToRgba` call site in a third file**, named nowhere in the authoring register. **G5's born-RED count is 5 call sites / 3 files, not 2/2 — RE-SIZED at repair round 2** (R2-4.1): the fold-seat cell enumerated five coordinates under a "4 call sites / 3 files" label and called this one "a fourth", both refuted by the gate's own command. ⟨cmd⟩ `grep -rn 'hexToRgba(' /Users/mkbabb/Programming/fourier-analysis/web/src/ | grep -v 'export function'` → **5 lines**: `golden-shimmer.ts:55` · `golden-shimmer.ts:58` · `epicycles.ts:258` · `epicycles.ts:283` · `BasisCanvas.vue:261`. **The enumeration IS the figure**; no count word in this spec may exceed or fall short of these five coordinates.
7. **C-19 is half wrong at HEAD.** There is **one** hand-rolled `lerp`, declared at `useCurveTransition.ts:85`; `ConvergencePlot.vue:11` **imports** it. There is no ConvergencePlot-local fork at `:8/:18`. The cure is one deletion + one re-point, not two migrations — **and the re-point is mandatory or ConvergencePlot's import breaks.**
8. `hexToRgb` (`colors.ts:111`) has **zero call sites** outside its own declaration — a dead export (G17).

---

## 3. Carry

**29 rows**, agglomerated by mechanism, banked ids preserved. `⟨…⟩` = provenance. Dedupe by identity; cross-listed ids cite, never re-book. *(Count corrected at repair round 2: the fold-seat masthead read "26" while §8b enumerated the ledger at 29 — A1–A7 · B1–B13 · C1–C2 · D1–D4 · E1–E2 · F1. The two figures are one ledger; the stale one is struck, not averaged. §8b is the enumeration, this is its head, and they agree at **29**.)*

**⟨cmd⟩ PROVENANCE MANIFEST for §3's corpus quotations (R2-1-LAW.1, run this round from `docs/tranches/V/megatranche/registry/adjudicated/`).** Every quotation below that carries a *verbatim* or *banked* label was reproduced this round by the `sed`/`grep` beside it; **no quotation in this section is typed from memory, and none is copied from a rulings file** (R2-1-LAW.3 — a ruling prescribes a cure-shape, never a quotable text). The batch, each returning its quoted span at the stated coordinate: `sed -n '57p' fr-AdminAuditLog.md` (C1) · `sed -n '303p' fr-PaperArticleWindow.md` (D2) · `sed -n '96p' fr-CanvasControlsDock.md` (D1) · `sed -n '120p;192p' fr-AnimationControls.md` (D4) · `sed -n '47p' fr-ImageUpload.md` (B12) · `sed -n '35p' fr-NotationPills.md` (B11, G14, §6b) · `sed -n '112p' fr-FunctionInput.md` · `sed -n '81p' fr-UserSlugBar.md` · `sed -n '56p' fr-Tooltip.md` · `sed -n '138p' fr-MorphShapePreview.md` · `sed -n '27p' fr-CollapsibleSection.md` · `sed -n '57p' fr-PaperSearch.md` · `sed -n '107p' fr-PaperSearchInput.md` · `sed -n '77p' fr-PaperSearchDropdown.md` (D3) · `sed -n '123p' fr-ConvergenceTimeline.md` (B9's 80-key figure) · `sed -n '118p' fr-GalleryView.md` (E2) · `sed -n '50p' fr-InfoCard.md` (A7) · `sed -n '113p' fr-HarmonicLevelGrid.md` (B1/B10/§6a) · `sed -n '39p' fr-GalleryFeaturedCarousel.md` + `sed -n '49p' fr-GalleryInfiniteGrid.md` (B4) · `sed -n '94p' fr-BasisCanvas.md` (B8's M-β5 clause) · `sed -n '75p' fr-App.md` (B8's L-9/MG-κ clause) · `sed -n '23p' fr-EquationPanel.md` (C2) · `sed -n '114,117p' library-band.md` (RD-7) · `sed -n '124,128p' library-band.md` (RD-8's law text) · `sed -n '294,297p' library-band.md` (the 3-leg probe). **Line numbers are admissible here and ONLY here-class**: the 66 `fr-*.md` and `library-band.md` are the FROZEN adjudicated corpus (R2-2.2(i)). **THREE** of these quotations are **soft-wrapped in their source** and are therefore receipted across a range, never with a single-line `sed`; a one-line receipt for any of them would be a false witness. **RECEIPT RANGE AND SPAN ARE NOW STATED SEPARATELY (repair round 4, PASS-3 D-5)** — rounds 2–3 printed the `sed` argument as if it were the span, and then, three sentences later in this same paragraph, ruled the probe's span to be something narrower, so the paragraph contradicted itself at arm's length. Each is measured here at the bytes: RD-7's *"deferral shape"* clause — receipt `sed -n '114,117p'`, **span `:115-116`** (the word *Booking* closes `:115`) · RD-8's law text — receipt `sed -n '124,128p'`, **span `:124-127`** (the italic closes on `:127`; `:128` is the following `counts` sentence) · the W.L5 3-leg probe — receipt `sed -n '294,297p'`, **span `:295-296`** (`:294` is the `### W.L5 — FOURIER…` heading and `:297` is the `fourier-vizcolor-oklch.mjs` leg, so neither carries a quoted word). **Every cite of these three in this file is now the SPAN**; the wider `sed` arguments survive only as the reproducing commands, labelled as such. **Corrected two→three at repair round 3**: rounds 1–2 declared the rule and then broke it in the same file — the probe quotation *"5 × ERR_PACKAGE_PATH_NOT_EXPORTED; `timingFunctions` deleted; 8/22 curves drift, max 0.192"* was point-cited as `library-band.md:295` at A6 and G15, but ⟨cmd⟩ `sed -n '295p' library-band.md` returns only *"- **BORN:** RED ×3 — `fourier-value-import-drift.mjs` 3/3 legs (5 × ERR_PACKAGE_PATH_NOT_EXPORTED;"* — the `timingFunctions` and `8/22` clauses live on `:296`. **All such cites are now `:295-296`**, the span the words actually occupy. A false witness authored by the rule's own author is still a false witness.

### Arm A — the specifier leg (gated on F.W1)

**A1 · CENSUS-2026-08-03:38 / lane-frontend:480** — MAJOR at the specifier, latent at HEAD. Five bare-root `@mkbabb/value.js` statements across 4 files, **6 distinct symbols, easing-only — confirmed exactly by this seat**: `easeInOutSine` ×3 (ConvergencePlot.vue:5 · useCurveTransition.ts:8 · harmonics.ts:5) + `timingFunctions` (easings.ts:9) + `{easeInOutSine, easeInOutCubic, easeInOutQuad, easeInOutExpo, easeInOutCirc}` (easings.ts:10-16). They resolve today only because installed 0.13.0 exports `['.']`; 4.0.0 publishes seven subpaths and **no `"."` key** — ⟨cmd⟩ `node -p "String(require('/Users/mkbabb/Programming/value.js/package.json').exports['.'])"` → **`undefined`** (the key is ABSENT), beside ⟨cmd⟩ `node -p "Object.keys(require('/Users/mkbabb/Programming/value.js/package.json').exports).join(' ')"` → `./color ./value ./css ./easing ./math ./transform ./quantize`. **The elided path this receipt carried through rounds 1–3 was completed at repair round 4 (R3-9.1) and the completion was load-bearing, not cosmetic**: read against the OTHER manifest the ellipsis could denote — fourier's installed 0.13.0 at `web/node_modules/@mkbabb/value.js/package.json` — the same command returns an OBJECT (`{types, import, default}`), the exact opposite answer, which is precisely A1's point that the sites resolve today. An elided path in a receipt whose two readings disagree is not an address defect; it is a measurement with two values. **Cure**: migrate to `@mkbabb/value.js/easing`. **Seat receipt: all five named symbols survive at `./easing` and are numerically identical to 0.13.0 (max|Δ| ≤ 4.44e-16 over 1001 samples) — the direct-symbol arm is a drift-free rename.** **K-7 lock**: the specifier grep does not undercount — ConvergenceLegend's only mount is ConvergencePlot.vue:5, already booked; the "specifier-grep UNDERCOUNTS" escalation is killed. **FR-AUL-55 scope lock**: bare-root is producer-imposed, not consumer sloppiness; the surface does not reach files with zero value.js imports. **Banked identity — CITED, never re-minted (R-1d / R-5).** The specifier + `timingFunctions` failure this row measures is banked as **`fr-EasingPicker L/B-1 + C/B-1`** (`fr-EasingPicker.md:52`) and its **carrying home is F.W1** (**F-W1 §2 WU-K's `L/B-1 + C/B-1` row**, BLOCKER-fold, incl. the `Result`-shape rewrite for `EASING_PRESETS`). A1 cites that identity beside the census ids; **F.W2 mints no easing identity of its own and claims none of F.W1's GREEN** (FR-GIG-5). The whole five-record easing cluster and its two cure-shape locks are carried at **A6**; the duplicate-import costing correction at **A6 (MISSED-F)**. **Dissent preserved**: fr-ConvergenceLegend C-1 filed BLOCKER, adjudicated INFO at the component / MAJOR at the specifier (K-7); fr-App C-2's opposite sequencing binds **the colour half only, never this arm**. ⟨fr-ConvergencePlot L-M6+C-1 · fr-ConvergenceLegend C-1+U-2, K-7 · fr-AdminUserList FR-AUL-55 · fr-EquationView K-5 · CENSUS:38 · lane-frontend:480 · **fr-EasingPicker L/B-1 + C/B-1 (banked identity; home F.W1 @ **F-W1 §2 WU-K's `L/B-1 + C/B-1` row**)**⟩

**A2 · W.L5-ACT1 (library-band.md:294-313)** — MAJOR, cure-shape refutation. **Provenance corrected (R-1d): NOT seat-minted.** ACT(1) prescribes `timingFunctions` → `easingNames()` + `easing(name)`. **Both symbols are absent at 4.0.0** (`grep -rn 'easingNames|timingFunctions' src/ dist/` → 0 hits, re-run this seat); `easing` is `easing(name: string): Result<EasingFunction, EasingIssue>` (`src/easing.ts:166`). **The mapping is RE-DERIVED, never transcribed.** Seat-re-measured surviving surface: 16 named exports at `./easing`, `bezierPresets` (30 keys), `easing(name)` over **DIRECT_EASINGS (19 keys)** ∪ PRESETS (30 keys). **Corrected 20→19 at repair round 2 (R2-4.2), re-derived at the CURRENT bytes** — ⟨cmd⟩ `sed -n '95,113p' src/easing.ts | grep -c ','` → **19**; ⟨cmd⟩ `grep -n 'Object.freeze\|^});' src/easing.ts` → the `Object.freeze({…})` block opens **`:94`** and closes **`:114`** (at this writing); the nineteen are ten base names (`linear`, `easeOutCubic`, `easeInOutSine`, `easeInOutCubic`, `easeInOutQuad`, `easeInOutExpo`, `easeInOutCirc`, `easeOutExpo`, `smoothStep3`, `easeInBounce`) plus nine hyphenated aliases — **`linear` has no alias**, which is the whole of the 20-vs-19 discrepancy. ⟨cmd⟩ `node -e "import('./dist/subpaths/easing.js').then(m=>{console.log('exports:',Object.keys(m).length);console.log('bezierPresets keys:',Object.keys(m.bezierPresets).length)})"` (cwd `$V`) → `exports: 16` · `bezierPresets keys: 30` (both figures re-confirmed, unchanged; **the command is completed to a literal, runnable form by the purge seat — rounds 1–3 banked it `…`-elided, which is the R3-9.1 unrunnable-machinery class, and it re-ran EXACT once written out**). **The 22-key resolution claim is BANKED, not seat-minted (R-1d)**: *"all 22 morph keys DO resolve through `easing(): Result` at 4.0.0 (PRESETS carries ease-in/out/in-out :37-39 and the three back rows :62-64)"* — `fr-EasingPicker.md:52`, **LC's sharpenings RATIFIED with CC-2**; this seat's re-run is a **reproduction of that ratification**, not its origin, and the accepting authority is **F.W1's ESC-4** (**F-W1 §2 WU-R's `ESC-4` row**). The mapping is *derivable*; what it is not is *free* (G15). **Unwrap lock — BANKED, home F.W1 (R-5 credit bar)**: `Result` is a discriminated union (`src/foundation/result.ts:1-3`), so bare `.value` does not type-check — and `easings.ts:58` already launders through `as EasingFn`. That arm is banked verbatim at **`fr-FourierMorphSvg.md:46`** (*"**The** one unbanked arm, booked as a rider on that F.W1 ticket … an INCORRECT re-point at `bezierPresets` (4-tuples, not functions) typechecks clean and throws on the first rAF tick … the `!== "idle"` guard closes the app-wide theme toggle PERMANENTLY"* — **opening word conformed at repair round 4**, PASS-3 D-8: rounds 1–3 lower-cased the bank's capital **T**, the same one-character class this file already strikes three times), with the rider text quoted **as the bank spells it** — *"the F.W1 rewrite must be verified by execution, not typecheck."* ⟨cmd⟩ `sed -n '46p' fr-FourierMorphSvg.md` (the fold-seat rendering had up-cased `execution`; a "verbatim" label may only carry the cited file's own bytes, R2-1-LAW.2) — **already gated at F-W1 §3 gate `G12`** ⟨cmd⟩ `grep -n 'EASING RE-POINT VERIFIED BY EXECUTION' F-W1.md` → the `G12` row of F-W1's §3 gate table. **F.W2 cites it and claims no gate.** The second seam this seat adds is only the sibling shape: `getEasingFn`'s `?? EASING_PRESETS.linear.fn` **swallows an `undefined` fn into a silent linear curve** — and even that overlaps banked **`fr-AnimationControls C-17`** (`:87` — `linear: (t) => t` hand-rolled at `easings.ts:77`, third identity in the store's `?? ((x)=>x)` at `animation.ts:28`; **home F.W4**, cited here, never re-booked). The issue arm must be handled explicitly, never cast. **Deletion receipt**: W.L5's "mapping recorded" obligation now has one to record against. **Authored BEFORE F.W1 opens** — F.W1's atomicity means a wrong mapping ships with the uplift or blocks it. ⟨library-band.md:294-313 · **fr-EasingPicker L/B-1 + C/B-1 (`:52`, CC-2) — banked identity, home F.W1 @ **F-W1 §2 WU-K's `L/B-1 + C/B-1` row**** · **fr-FourierMorphSvg r2 fold (`:46`) — `as EasingFn` rider, gated at F-W1 §3 gate `G12`** · **fr-AnimationControls C-17 (`:87`) — home F.W4** · seat reproduction 2026-08-28⟩

**A3 · C-19 (⊕ SC-4, folding C·C-30)** — MINOR. Hand-rolled `lerp` beside the value.js import. **Cure**: import from `@mkbabb/value.js/math` — seat-verified live: `src/subpaths/math.ts` exports `clamp, scale, lerp, lerpArray, logerp, deCasteljau, cubicBezier, interpBezier, cubicBezierToString`. **Rides A1's edit, same commit.** Identity guard: C·C-30 **FOLDS here and is never re-booked** (SC-4: "neither reader drew it"). R-D's K5 cure-kill (un-landable at 0.13.0) rides as the sequencing note. **§2b item 7 amends the mechanism**: one declaration, not two — and `ConvergencePlot.vue:11`'s import is re-pointed in the same commit or the file breaks. **Dissent**: R-D confirmed C·C-30 fresh; ruled a fold by the fr-EquationView seat. ⟨fr-ConvergencePlot C-19 · fr-EquationView SC-4⟩

**A4 · C-22** — INFO, budget note, **explicitly not a defect**. `@mkbabb/keyframes.js` declared and chunked, consumed by zero of a closure running two hand-rolled rAF engines. Record the declared-and-unconsumed edge in the denominator; **mint no consumption obligation**. K-6 rules C·C-31's MAJOR grade down to INFO by the bank; the facts stand. The keyframes pin moves in F.W1's atomic transaction, never here. ⟨fr-ConvergencePlot C-22 · fr-EquationView I-5, K-6⟩

**A5 · FR-GIG-17 (= C-18)** — INFO. value.js `^0.13.0` and keyframes `^4.3.0` declared and wholly unconsumed in the gallery subtree — "the component has a colour problem value.js already solves and does not reach for it." Denominator cell; cites A1's inventory as its denominator and **adds no site to it**. Same class as A4; **no record folds them, so both stand with a cross-reference**. ⟨fr-GalleryInfiniteGrid FR-GIG-17⟩

**A6 · the EASING CLUSTER — five banked records, CITED WHOLE (repair round 1; R-1d / R-5 / R-2 one-home law)** — MAJOR as a formation input, **zero cures minted here**. The fold-seat spec measured this cluster's substance under census/W.L5 ids alone and opened none of the five records. They are opened here, by their banked ids, each with its adopting home named. **F.W2 books none of them and claims none of their GREEN** (FR-GIG-5).

**⟨cmd⟩ provenance for this whole table (R2-1-LAW.1), run this round from `docs/tranches/V/megatranche/registry/adjudicated/`**: `sed -n '52p' fr-EasingPicker.md` · `sed -n '89p' fr-EasingPicker.md` · `sed -n '46p' fr-MorphPhaseConfig.md` · `sed -n '58p' fr-EasingCurvePreview.md` · `sed -n '46p' fr-FourierMorphSvg.md` — **all five reproduce the quoted spans exactly** at the frozen corpus, where line numbers remain admissible (R2-2.2(i)). Each row's ⟨cmd⟩ is the `sed` for its own `record:line` cell; no quotation below is typed from memory of a ruling.

| banked id | record:line ⟨cmd⟩ = `sed -n '<line>p' <record>` | what it says (banked) | home | what F.W2 does with it |
|---|---|---|---|---|
| **L/B-1 + C/B-1** | `fr-EasingPicker.md:52` | no `"."` export at 4.0.0 (7 subpaths; main/module/types absent) AND `timingFunctions` deleted; the picker's five functions survive at `./easing`; **LC's sharpenings RATIFIED with CC-2** — all 22 morph keys resolve through `easing(): Result`, **but 14 of 22 silently drift analytic→CubicBezier-approximation (8 stay analytic incl. ease-out-cubic/-expo)** | **F.W1** (**F-W1 §2 WU-K's `L/B-1 + C/B-1` row**, BLOCKER-fold; acceptance at **ESC-4**, **F-W1 §2 WU-R's `ESC-4` row**) | CITES as the identity behind A1/A2/G2/G15. Anti-rename: the id is original for life; F.W2's census ids are *addresses of the same fact*, never a second identity |
| **MISSED-F** | `fr-EasingPicker.md:89` | `easings.ts:9` and `:10-16` are two import statements from the **byte-identical specifier**, and `ANIMATION_EASINGS` is a strict content-subset of `EASING_PRESETS` **by object identity** (seat node probe 5/5 at 0.13.0) — *"the F.W1 '5 sites / 6 symbols' costing **double-counts**: the named-import block is deletable TODAY at the current pin"* | **F.W1** (costing correction) **+ F.W4** (no-duplicate-imports hygiene) | CITES. **A1's five-site inventory inherits the costing correction**: two of the five sites are one duplicated specifier, so A1's figure is a SITE count, not a cost. F.W2 mints no hygiene cure |
| **MPC-2 · C-2 ⊕ L-D16** | `fr-MorphPhaseConfig.md:46` | *"Easing catalog cannot resolve at the value.js bump: bare-root specifiers, no `"."` export at 4.0.0, `timingFunctions` absent from every entry point."* → *"**FOLD → fr-EasingPicker L/B-1 + C/B-1 · F.W1** (lockstep tri-package bump per MPC-14, never a specifier rewrite)."* | **F.W1** (by its own fold) | **CARRIED VERBATIM AND CONTESTED, not swept.** §2a rows 1–4 prescribe `specifier → /easing` ×4 files, which the lock's parenthetical appears to forbid. **The contest is F.W1's to resolve, not F.W2's** — MPC-2 folds INTO the F.W1-homed identity, so its cure-shape clause belongs to that home. F.W2 records the collision, states the measurement that bears on it (**G1: 5 of 5 sites `ERR_PACKAGE_PATH_NOT_EXPORTED` at 4.0.0 — a lockstep bump alone does not make a bare-root specifier resolve**), routes it to the F.W1 cross-edge (§6b) and **pre-empts nothing**. Recorded as **R-iii**; the recording is what turns **G20** green |
| **CENSUS-MC (C-D-5)** | `fr-EasingCurvePreview.md:58` | *"`@mkbabb/glass-ui/motion-curves` exists at the PINNED 4.0.0 (seat-read exports map) and value-re-exports every easing symbol `lib/easings.ts` imports bare-root (both readers verified the dist module; exception `timingFunctions` holds) — **the value.js-specifier half of the scheduled migration is NOT blocked on the uplift**"* → census §1/§3 rows **AMENDED** | **F.W1** (sequencing input; component-altitude ruling R-10) | **CARRIED AS AN AMENDMENT TO G10's PREMISE.** G10 is re-stated so the banked amendment is not contradicted: what is gated on F.W1 is the **bare-root→`./easing` re-point against value.js's own 4.0.0 export map** (held from below by keyframes 4.3.0 → `value.js ^0.13.0`, C·D-11); what CENSUS-MC shows is that a **producer-re-export route exists at the current pin** for every symbol except `timingFunctions`. F.W2 does not choose between the routes — the route choice is F.W1's, and A2's `timingFunctions` re-derivation is required under **either** |
| **r2 easings/value.js fold** | `fr-FourierMorphSvg.md:46` | the `as EasingFn` cast at `easings.ts:58` erases the shape change → an incorrect re-point at `bezierPresets` typechecks clean and throws on the first rAF tick → `play()` never resolves → **the theme toggle closes PERMANENTLY**. Rider, at the bank's own casing: *"the F.W1 rewrite must be verified by execution, not typecheck."* ⟨cmd⟩ `sed -n '46p' fr-FourierMorphSvg.md` | **F.W1** (rider on the L/B-1 ticket; **gated at F-W1 §3 gate `G12`**) | CITES in A2's Unwrap lock. **F.W2 authored this as a fold-seat find; that credit is withdrawn** — the arm is banked and already gated |

**Drift figure, banked-verbatim (R-1d; ⟨cmd⟩ receipts at repair round 2 — R2-1-LAW).** **14 of 22 drift analytic→CubicBezier, of which 8/22 are MATERIAL (max Δ 0.192); `ease-out-cubic` and `ease-out-expo` are among the 8 that STAY analytic.** ⟨cmd⟩ `sed -n '52p' fr-EasingPicker.md` · `sed -n '46p' fr-FourierMorphSvg.md` · `sed -n '294,297p' library-band.md` — all three reproduce. **Independently corroborated at the tree this round**: ⟨cmd⟩ `sed -n '92,135p' src/easing.ts` shows `DIRECT_EASINGS` (`:94-114`) retaining `easeOutCubic`/`"ease-out-cubic"` and `easeOutExpo`/`"ease-out-expo"` as analytic entries — exactly the two the bank names among the eight that STAY analytic — while every analytic in/out arm 0.13.0's `timingFunctions` carried is absent and falls through to the cubic-bezier `PRESETS` table (`:34-65`, exported as `bezierPresets` at `:67`). Sources: `fr-EasingPicker.md:52` (CC-2 ratification) · `fr-FourierMorphSvg.md:46` (14/22 under a CORRECT rewrite) · `library-band.md:295-296` (the 3-leg probe: *"`fourier-value-import-drift.mjs` 3/3 legs (5 × ERR_PACKAGE_PATH_NOT_EXPORTED; `timingFunctions` deleted; 8/22 curves drift, max 0.192)"*). **The two numbers are one fact at two denominators — 14 shape-class changes, 8 of them material by the probe's Δ threshold — never rival measurements.** Accepting authority: **F.W1's ESC-4** (**F-W1 §2 WU-R's `ESC-4` row**, *"14/22 analytic→CubicBezier; 8/22 material, max Δ 0.192"*, corroborated MPC-5, folds to RD-5 RULED RESTORE). **F.W2 declares the drift (G15); F.W1 accepts it; W.L6 may cure it. F.W2 claims no part of that.** ⟨fr-EasingPicker L/B-1+C/B-1, MISSED-F · fr-MorphPhaseConfig MPC-2 · fr-EasingCurvePreview CENSUS-MC · fr-FourierMorphSvg r2 fold · **F-W1 §2 WU-K's `L/B-1 + C/B-1` row · F-W1 §2 WU-R's `ESC-4` row · F-W1 §3 gate `G12`** (the fold seat's three F-W1 line coordinates for these three homes are **retired to stable anchors** at repair round 2, R2-2 — ⟨cmd⟩ `grep -n '^### WU-\|^## §' F-W1.md` locates all three by heading, and a heading does not drift) · library-band.md:295-296⟩

**A7 · FR-IC-20 (fr-InfoCard:50)** — INFO, consumption posture, **on F.W2's own surface**. Banked: *"AMENDED by ruling R2-2: the bare-root break row now carries its target: **fires at the census-adopted `value 0.13→4.0`** (CENSUS-2026-08-03 ~:109/:184-186, the later record governing over cd26c65's interim ^2.0.0 note …). Reader-B's registry context adopted: the W.L5 library-band row (timingFunctions deleted; 8/22 curves drift) and `safeAccentColor` surviving at v4 `./color` ride their banked homes. Anti-superlative framing (immunity by omission) sustained."* **Three things F.W2 takes from it**: (i) the break's **target is named** — 0.13→4.0, F.W1's transaction, not an unspecified future bump, which is exactly A1's opens-after; (ii) the row's "8/22 curves drift" is the **material subset** of A6's banked 14/22 — cited so the two figures are never read as rivals; (iii) `safeAccentColor` **survives at v4 `./color`** — a negative for the deletion arm's budget (B8 removes no consumer of it) and a denominator cell under G11. **The two riders ride their banked homes; F.W2 re-books neither.** **Identity note**: `fr-InfoCard`'s subject is `web/src/components/equation/InfoCard.vue` (43 lines) — **the same file C2's `energyColor` blast radius names at `:5,:14`**. Before this repair the spec touched that file only as a consumer path with the record unopened; the two are joined here, and C2's cure remains **F.W4's** while FR-IC-20's posture is F.W2's denominator cell. ⟨fr-InfoCard FR-IC-20⟩

### Arm B — the colours leg (landable at 0.13.0; **not** gated on F.W1)

**B1 · fr-ContourPreview row 1 (D:B-1 · C:D-1(a) · L:L-9)** — **BLOCKER**. The component's only object strokes `hsl(40 90% 55% / 0.85)`, compositing to **1.660:1** against light `--card` (token chain unbroken, zero consumer override) — SC 1.4.11 fail, five independent reproductions to three digits. **Cure**: `stroke="var(--viz-amber)"`, alpha via `stroke-opacity` → **4.625:1 at both pins**, with B3's token mint. **Adopt the HLG-4 remedy hierarchy verbatim** — ⟨cmd⟩ `sed -n '113p' fr-HarmonicLevelGrid.md` reproduces it this round: *"the F.W2 remedy hierarchy (CSS-var passthrough first, `parseCSSColor` only where JS must see colour) adopted"* — the bank's spelling is kept inside the quotation; the shipped 4.0.0 symbol is `parseCssColor` (B10's transcription note). Independent of F.W1. R2's soft-leg bound carried (the on-stage editor renders the same failing literal); the grade is not disturbed. ⟨fr-ContourPreview row 1⟩

**B2 · fr-ContourPreview row 14 (C:D-1(b))** — MAJOR. The failing literal is **UNREACHABLE by the repo's own resolver**: `cssVarToHex` has no slash-alpha arm and falls to the `#888888` sentinel, so the literal sits **outside** the 117-LOC surface F.W2 deletes. **LOCK: the migration cannot sweep it as collateral; it is cured by name.** This is the standing proof that **the colors.ts-deletion arm and the colour-defect arm have DIFFERENT surfaces** — the wave's scope table shows both (§2a does). ⟨fr-ContourPreview row 14⟩

**B3 · fr-ContourPreview row 22 (D:m-8 · L:L-9(system))** — MINOR. Brand amber as inline SVG presentation attributes: **9 sites / 2 files / five alphas {0.85, 0.6, 0.5, 0.3, none}, zero declarations — re-measured exactly this seat** (ContourEditorCanvas.vue :261,:307,:308,:315,:319,:328,:329,:333 + ContourPreview.vue:45). Invisible to CSS-only token audits, weakest cascade rung; the semantic home ships at both pins (`color-radius.css:266`). ⟨cmd⟩ `grep -c 'hsl(40' /Users/mkbabb/Programming/fourier-analysis/web/src/components/visualization/ContourPreview.vue /Users/mkbabb/Programming/fourier-analysis/web/src/components/visualization/ContourEditorCanvas.vue` → **1 + 8 = 9**. **Cure**: mint `--contour-stroke` once, cure **these nine sites** with B1 as **ONE cut** — **corrected from "ten" at repair round 2**: B1's `ContourPreview.vue:45` is one OF the nine, not a tenth beside them, and the count word and the measurement must not disagree inside one row. Feeds G11 — the eight ContourEditorCanvas sites are invisible to any import-specifier-keyed census. ⟨fr-ContourPreview row 22⟩

**B4 · FR-GFC-8 (= C-5 as rescoped, R-3)** — MAJOR. `basis-display.ts:1-7` snapshots the `VIZ_COLORS` reactive proxy at **module evaluation** into a plain object literal (seat-confirmed; **anchor corrected `:1-6`→`:1-7` at repair round 2 to the banked range** — ⟨cmd⟩ `sed -n '39p' fr-GalleryFeaturedCarousel.md` and `sed -n '49p' fr-GalleryInfiniteGrid.md` both read `basis-display.ts:1-7`, the latter adding *"(read whole)"*); `resolveVizColors()` can never retro-write it; `GalleryCard.vue:121` projects it as `--pill-c` into background, border **and** text (`:270-274`). **WAVE LOCK, verbatim from C §7.2** — ⟨cmd⟩ `sed -n '39p' fr-GalleryFeaturedCarousel.md`: *"the colors.ts migration must fix the SNAPSHOT, not merely swap `cssVarToHex` for value.js's parser, or the wave closes green on a surface that never changed colour."* **GCM-21/K-9 amendment**: on the DEFAULT entry both sites agree grey (post-poison `#888888`) — every witness pins the entry route. **fr-App R-1 rider, binding on any witness**: the split-brain is entry-route conditional; on a clean `/paper`-first visit frozen and live sides AGREE and the recipe **MUST NOT be expected to reproduce**. In-repo counter-idiom: `useCoeffHover.ts:60-65`. **The FIX ships at F.W1** (banked fr-BasisCanvas D-2/BC-5/C-3); **F.W2 owns the LOCK and the green-criterion, not a second cure.** Absolutization ("forever", "two defects cancelling") killed again (K4/R-3 + GCM K-9); reader-A's M4 folds as an amendment note, not a row. ⟨fr-GalleryFeaturedCarousel FR-GFC-8 · fr-GalleryInfiniteGrid C-5 · fr-App m-8/L-2/C-3, R-1 · fr-BasisCanvas D-2/BC-5/C-3 · **fr-GalleryView C·C-1 (`:118`) — the seam-discipline id whose implied wave-lock this row IS; folded at E2, cross-referenced here**⟩

**B5 · M-β1 (the ramp identity)** — MAJOR (BLOCKER at fr-CoefficientsSpectrum B-3; ruled MAJOR at FR-CP-2 R-4, LC's grade preserved). **Seat-re-measured: `spectrumColor` exists FOUR times across 8 files** — ⟨cmd⟩ `grep -rn 'const spectrumColor\|function spectrumColor' /Users/mkbabb/Programming/fourier-analysis/web/src/` → **4 declarations**: `canvas-drawing/transforms.ts:3` (pow(t,0.6) gamma fork, `export function`) · `equation/lib/harmonics.ts:81` (`export function`) · `FrequencyGraph.vue:42` · `shared/CoefficientsSpectrum.vue:47` (latter two byte-identical, both file-local) — keyed to rank-in-the-current-slice rather than the datum, frozen `hsl(h,85%,55%)` both themes, bypassing the palette owner entirely. **Cure**: ONE shared, **theme-routed** ramp owned by the palette owner, keyed on the **datum**. **Binding amendments, all carried**: **(RD-5-i)** tokenisation is NOT the cure — **lightness must co-vary**, law for the identity; **(RD-5-ii / K-11)** `sampleColorRamp`/`mixColorsN` are GONE at 4.0.0 — **re-route to `oklch`/`mixColors`**, both live at `./color` (seat-re-confirmed: 0 hits src+dist; `./color` exports `oklch, mixColors, convertColor, interpolateHue, mapColorToGamut`); **(FR-CP-2-c / K4)** `VIZ_COLORS` is a **five-entry categorical palette, not a ramp** — derive-from-VIZ_COLORS is structurally impossible (seat-confirmed: `resolveVizColors()` assigns exactly `fourier, chebyshev, legendre, amber, green`); **(FR-CP-2-c / K13)** the "one import edge" cure is **KILLED** — importing visualization/lib's gamma fork into `shared/` or `equation/` **is** the colocation inversion; the honest cure is a **new route-neutral ramp home**. **Riders folded in**: M-R4 (hue step → ~3.0° at N=100, below JND) · M-R5 (index-space mismatch: grouped-|n| vs raw-signed-incl-0, different denominators) · C-15 (legend-site instance) · D-15's generator half (four discs 1.34–1.72:1; the disc needs a ring) · FR-CP-2-a (denominator authored at `CoefficientsPanel.vue:20 :max-bars="40"` vs the child's collapsed 12 — colour is a property of how many rows are on screen) · D-8's surviving arms (non-monotonic luminance; no CVD-safe redundancy; the light 55°–110° band) · **the gamma fork reconciled EXPLICITLY**. **Route**: F.W2 owns the **substrate ANGLE** + 4.0.0 re-route; **the twins own execution and the fork collapse**; cross-ref F.W1 (inv-33). **ONE identity, anti-rename law.** **§X.1-v4 CONSUMED (repair round 4, read at F-W3's landed bytes; the partition is FINAL and F.W3 publishes it FIRST per R3-1a)**: the twin split of this row is no longer an unresolved pair. `fr-CoefficientsSpectrum` **enters the partition at F.W3 `.d`** (§X.1-v4 item 3) because `shared/CoefficientsSpectrum.vue` is a member of §5d's Files list — that is this row's fourth `spectrumColor` declaration, so **the fork collapse's holder is F.W3 `.d`**; and `M-β1` is named in §X.1-v4 item 2's fourth line as one of **`BC-20 · M-β1 · M-β3` — *cited at F.W3, booked nowhere*** across the twins. **B5 is consequently the identity's ONLY booking, and it books the substrate angle alone**; the execution half owes a named holder at the twins and F.W2 does not supply one, does not re-book the identity under a census id, and does not choose between `.d` and the F.W4 default arm. The two ids this row cites from the same record — `B-3` and `M-10` — are **not** v4's residue pair and are unaffected by that assignment. §8a rows 5–6 stand as F.W2 bookings of the substrate angle; §X.1-v4 governs the execution half. Final ratios + the dark arm → SS-13. **Dissents preserved**: LC's BLOCKER on FR-CP-2 (R-4); R-L's inversion argument survives as the binding cure-shape constraint; both fr-ConvergenceLegend readers filed M-R5 as new — caught banked (fleet evidence: hostile re-readers do not sweep the registry). ⟨fr-BasisCanvas M-β1, D-18/C-13 · fr-CoefficientsPanel FR-CP-2 a/b/c · fr-CoefficientsSpectrum B-3, M-10 · fr-ConvergenceLegend D-15/M-R4/M-R5/C-15 (all FOLDED) · fr-ConvergencePlot D-2+C-14(b), RD-5⟩

**B6 · K-11 (fr-ConvergencePlot killed-register)** — MAJOR, cross-repo cure-substrate deletion. `sampleColorRamp` and `mixColorsN` — cited by three banked records as the adoption target and by fourier's own M.W7 booking as "dischargeable-on-adopt" — are absent at 4.0.0 — ⟨cmd⟩ `grep -rn 'sampleColorRamp\|mixColorsN' src/ dist/ \| wc -l` → **0**, re-run this round; they ship in installed 0.13.0 only. **Three consequences, all carried**: (i) **amends B5's angle** — build from `oklch`/`mixColors`; never re-books the ramp; (ii) fourier's `VALUEJS-2.0.0-NOTE.md` §3 strike (**not struck** at `M/PROGRESS.md:22`) is now un-dischargeable-by-adoption — the correspondence half strikes it **RETIRED-BY-DELETION with the receipt**, not discharged; (iii) FR-CP-15's premise is true at installed, false at target — the row survives as the **consumption gap**, its substrate does not. **Seat correction the authoring register does not carry**: `sampleColorRamp` is **booked for re-ship at 4.1** (X-W9 §Scope item 8 / G21, "SCI-1") — "RETIRED-BY-DELETION" is accurate **only as of 4.0.0**; the strike must say *deleted at 4.0.0, scheduled for 4.1 under SCI-1*, or the correspondence ledger states a falsehood the moment W.L6 lands. Authored before B5's substrate is chosen and before the 2.0.0 answer. Re-pin at wave open (SR-1/K-8 lesson, third occurrence). ⟨fr-ConvergencePlot K-11 · fr-CoefficientsPanel FR-CP-15 · fr-ConvergenceLegend C-15 · lane-docs §8c · X/waves/W9.md §Scope 8, G21⟩

**B7 · C-2 (fr-ConvergenceLegend)** — MAJOR. The Sum swatch/label paint `var(--viz-amber)` while the sum curve strokes `VIZ_COLORS.golden = #f0b632` both themes — **seat-confirmed: `resolveVizColors()` never assigns `.golden`; `golden: STATIC.golden` is a frozen literal.** One token doing two incompatible jobs (AA ink vs identity paint). Introduced by `2e4a452`(d): the 3+/3− diff **is** the hex→token swap. **Cure**: *the join must be made — the swatch and the shimmer pipeline reunite on ONE source, NOT independent migration*, because **W.L5 ACT(2) deletes `hexToRgba`, a live dependency of golden-shimmer** (:55/:58), **of epicycles.ts** (:258/:283, M-α5 the tip glow), **and of BasisCanvas.vue:261** (§2b item 6). **The hardest sequencing lock in the wave: the deletion list and live consumers collide in one commit — FIVE call sites, three files** (`golden-shimmer.ts:55`, `:58` · `epicycles.ts:258`, `:283` · `BasisCanvas.vue:261`). **Re-sized 4→5 at repair round 2 (R2-4.1)**; ⟨cmd⟩ `grep -rn 'hexToRgba(' /Users/mkbabb/Programming/fourier-analysis/web/src/ | grep -v 'export function'` → **5**. A rider law sized against a false count is a rider law that lets one consumer through. Consumer-side render fix → **F.W4** (fr-ConvergencePlot D-1's item), never claimed here; D-1 **cross-referenced, never double-cured**. **Dissent**: K-11 (legend register) kills L's dismissal of D-L8 as "a design decision, not counted" — two COLOURS from two never-joined token families, introduced by the very commit the D axis lionised. ⟨fr-ConvergenceLegend C-2 · fr-ConvergencePlot D-1 · fr-BasisCanvas M-β5/M-α5 · fr-App L-9⟩

**B8 · W.L5-ACT2 (library-band.md:294-313; RD-6/RD-7)** — MAJOR, cure-shape lock on the wave's central act. **Delete** `cssVarToHex`/`hslToHex`/`rgbToHex`/`hexToRgb`/`hexToRgba` (`colors.ts:22-117` — seat-confirmed ⟨cmd⟩ `wc -l < /Users/mkbabb/Programming/fourier-analysis/web/src/lib/colors.ts` → **117**, re-run this round; declarations at `:22`, `:56`, `:70`, `:101`, `:111`) — **not extend**. The 4.0.0-viable route: **fourier-side context resolution (owns the DOM read + theme observer) + `parseCssColor` + `convertColor` + a declared 3-line hex residual**, deleted at the 4.1 adoption. Seat-verified: `parseCssColor` exported from `./css` (`src/subpaths/css.ts:47`), `convertColor` from `./color` — **both prescribed symbols exist**. **The residual ships WITH a validation arm and a deletion date** (G6): `hexToRgba`/`hexToRgb` do zero validation — 3/4/8-digit hex → `rgba(…, NaN, …)`, silently ignored by Canvas2D. M-β5's lock ⟨cmd⟩ `sed -n '94p' fr-BasisCanvas.md`: masked today by the 7-char fallback, **"detonates the moment D-1 is cured with a hex-emitting branch"** — it is the cure's own gate. fr-App L-9 + MG-κ ⟨cmd⟩ `sed -n '75p' fr-App.md`: the hex fast-path (`colors.ts:29`) is **newly-dead, not born-dead** — the stale build `dist/assets/index-57FkGzlZ.css` → `--viz-fourier:#d73523` proves it was the PRIMARY path at the prior pin, *"so a hex-token reversion repair walks straight into it."* B2's lock stands: no slash-alpha arm — the deletion sweeps no slash-alpha literal as collateral. **RD-7 RULED order-independent (F) of the 4.1 cut** ⟨cmd⟩ `sed -n '114,117p' library-band.md` (a RANGE — the clause soft-wraps across `:115-116`): *"Booking the fix behind a cut that can decline to land is the deferral shape"* (capital **B**, and the sentence continues in the source — corrected at repair round 3; the clause opens at `:115` and the quoted words close on `:116`, which is why the receipt is a RANGE) **Dissent preserved**: O sequenced after 4.1; ruled for F because a decline on RD-6 would never unblock the var/light-dark half. **CONTESTED by B9 — ruled before the act is authored (G8, R-ii).** ⟨library-band.md:294-313, RD-6/RD-7 · fr-BasisCanvas M-β5 · fr-App L-9/MG-κ · fr-ContourPreview row 14⟩

**B9 · M-RTC** — MAJOR, seat-ratified NEW. **Installed glass-ui 4.0.0 already ships the token-colour resolver `lib/colors.ts` hand-rolls and gets wrong** — seat-re-verified live at fourier's own `node_modules`: `resolveTokenColor`/`createTokenColorCache`/`useTokenColor` present in `dist/dom.js` + `dist/composables/dom/{useTokenColor,useResolveTokenColor}.d.ts`; `./dom` is in the 4.0.0 exports keyset **(80 keys)** AND the v7 keyset **(74 keys)**. **Corrected 78→80 at repair round 2** — the fold-seat figure contradicted the tree AND the bank: ⟨cmd⟩ `node -p "Object.keys(require('/Users/mkbabb/Programming/fourier-analysis/web/node_modules/@mkbabb/glass-ui/package.json').exports).length"` → **80** (`./package.json` is not among them, so 80 is also the subpath count; `./dom` present: true); ⟨cmd⟩ `git show v7.0.0:package.json` + `node -p` → **74** keys, `./dom` present: true; ⟨cmd⟩ `grep -n '80-key' fr-ConvergenceTimeline.md` → the banked S-9 row reads *"real keys in the 80-key exports map"*. It resolves by **painting onto a real property and reading `getComputedStyle(el).color`** — never parses token text; it **is** the fr-App B-1 "used-value probe-element read, never string parsing" remedy, shipped, at the current pin, no uplift. Registry sweep: **zero hits anywhere in the bank or corpus.** **F.W2 MUST RULE the contest** (§4 R-ii): a wave that authors ACT(2) without ruling this **re-implements a producer surface** (standing glass-ui-first law). Cure lands at F.W1 with the banked D-1 identity; **F.W2 owns the RULING and the charter correction**. `getComputedStyle` oklch-vs-rgb serialization → SS-13; any adoption question is a **GLASS-RELAY row (SS-6), never a frontend hack.** ⟨fr-EquationView M-RTC⟩

**B10 · HLG-4 (= D:D-4 ⊕ C:C-2, superlative 3)** — MAJOR, **folded**. `cssVarToHex` has no `oklch()`/`light-dark()` arm → `VIZ_COLORS.chebyshev` → `#888888` (fallback `:53`); the installed token is oklch in all three arms, zero `@property`. The `--viz-amber` asymmetry confirmed: `color-radius.css:266` is `var(--section-color-5)` while fourier **re-authors it in `hsl()`** — seat-corroborated by the live vizcolor probe (amber the sole survivor via "fourier style.css:120 local override"); the style.css D.W4.d justification comment is **STALE at the installed pin**. **FOLD → banked FMD-17 → fr-BasisCanvas D-1 / fr-App B-1 (+K-12: HEAD greys both themes) · F.W4/F.W2. Never re-booked.** **The ratified remedy hierarchy binds the whole wave**: *CSS-var passthrough first, `parseCssColor` only where JS must see colour* ("`stroke="var(--accent-red)"` … twenty lines from the code that gets it wrong" — `fr-HarmonicLevelGrid.md:113`, quoted at its bytes). **Transcription note (M-25 class, caught at repair round 1)**: the bank spells the hierarchy's symbol **`parseCSSColor`** — 0.13.0's spelling. The 4.0.0 export is **`parseCssColor`** (`src/subpaths/css.ts:47`, seat-verified). The hierarchy is quoted with the bank's spelling; every OTHER use in this spec is the shipped 4.0.0 symbol. A cure authored against the quoted spelling imports a symbol that does not exist at the target. **Sequencing carried**: moot for these two call sites **if FMD-3's dead channel deletes first** — F.W2 checks FMD-3's disposition before sizing them. The stale comment is a **GLASS-RELAY input** (lane-frontend:604, second denominator per fr-ConvergenceLegend D-L11/C-4). ⟨fr-HarmonicLevelGrid HLG-4⟩

**B11 · FR-NP-9 (★AMENDED ruling 4; K-8)** — MAJOR, extends banked fr-BasisSelector M-12. The three notation colour literals are a **byte-exact hand transcription of the producer tokens' LIGHT arms — nine of nine channels** — *"proven with value.js's own dist, the very declared-and-bypassed dependency this row books."* Cross-package duplicate-by-value with **no typecheck, no lint, no visual witness**; the producer has re-tuned this cohort once already (`--viz-amber`). **Cure (FR-NP-1's collapsed clause)**: `var(--viz-fourier)`/`var(--viz-chebyshev)`/`var(--viz-legendre)` straight into the `color-mix` at `:43-45` (the `FunctionInput.vue:244-249` idiom, fifteen lines away) — byte-identical light, **zero visual delta**, dark arms free; **NEVER through `VIZ_COLORS`/`cssVarToHex` (K-8)**. **That NEVER-clause is a cure-shape lock for the whole colour arm: CSS-expressible colour must not route through the JS resolver being deleted.** Cure lands F.W3/W4; **F.W2 owns the tie** — the duplicate-by-value has no witness; the wave's strongest argument for a colour-VALUE census (G11). Gated behind **FR-NP-32 (≡ fr-PaperSidebar M1)** — the 4.0.0 dist stylesheet parse error. **Re-home note (R-8), because the citation matters as much as the fact**: the banked disposition at `fr-NotationPills.md:35` is ***"NO-WAVE-OWNER (producer dist emitter) + glass-ui BH relay at the TOP of the FR-COB-28 packet; F.W1 SEQUENCING GATE"*** — **not** an F.W0 pre-gate as first written here. The F.W0 re-home is performed at **F-W0 §3's carry row 1 (`FR-NP-32 ≡ fr-PaperSidebar M1`), whose R-column cell reads **`G-4`, relay item 0**, and whose body states the re-home in F.W0's own words — ⟨cmd⟩ `grep -o 'F.W0 additionally owns the pre-gate (G-4) + relay item 0' F-W0.md` returns that sentence, and it occurs **inside that carry row**: *"F.W0 additionally owns the pre-gate (G-4) + relay item 0"*. (**Receipt form converted at repair round 4, R3-3.10** — rounds 2–3 banked *"→ one hit"*, a count of a live sibling; `grep -o` word-output plus the row label is the form that survives F-W0's next edit.) **⚠ STRUCK at repair round 3, R2-1-LAW.1 — the convicted class, re-committed under a QUOTE-BY-COMMAND masthead**: rounds 1–2 carried, as this row's R-column, the quotation *"NO-WAVE-OWNER (producer emitter) + F.W0 pre-gate + relay item 0"*, which exists nowhere in F-W0 — ⟨cmd⟩ `grep -o 'NO-WAVE-OWNER (producer emitter)' F-W0.md` returns **no output**; the string is ABSENT (round-4 form of the same strike, R3-3.10). It was a FUSION of the banked `fr-NotationPills.md:35` disposition (which says *"producer dist emitter"*, not *"producer emitter"*, and is F-W0's quotation of the BANK, not F-W0's own gate text) with the row's separate pre-gate sentence, typed from memory of a re-home rather than read from the file. Replaced by the two real strings above, each by command. **Cited by banked row id and gate id from repair round 2 (R2-2); the fold seat's F-W0 line coordinate had drifted onto that table's header row and is struck.** **F.W2 cites that re-home; it does not assert the pre-gate bare, and it does not disturb the banked F.W1 SEQUENCING GATE arm.** Two banked witnesses of ONE fact — FR-NP-32 and `fr-PaperSidebar M1` (independently derived by postcss) — **cite both, never substitute** (anti-rename). ⟨fr-NotationPills FR-NP-9 = C·D-4 ∘ L·L-4 ⟨cmd⟩ `sed -n '35p' fr-NotationPills.md` · fr-NotationPills FR-NP-32 ≡ fr-PaperSidebar M1 · **F-W0 §3 carry row 1, gate `G-4`**⟩

**B12 · fr-ImageUpload row 13 (C:C-11)** — MINOR. Six hexes byte-duplicate `lib/colors.ts` `STATIC.rainbow` (`:150-156` ≡ `colors.ts:13-16`) in an SFC with **zero `@mkbabb` imports**. Load-bearing clause, verbatim ⟨cmd⟩ `sed -n '47p' fr-ImageUpload.md`: the duplicates are **"invisible to any import-specifier-keyed F.W2 inventory."** Feeds G11. **Dies with roster 5's cure**; F.W2 books only the census obligation. Theming arm KILLED (ruling 6) — not revived. ⟨fr-ImageUpload row 13⟩

**B13 · FR-CP-15 (= C-2, fr-CoefficientsPanel)** — MAJOR. `@mkbabb/value.js@0.13.0` installed and declared; the subtree consumes **zero** while hand-rolling the N-stop hue arc four times in raw sRGB. Identity checked: **distinct from fr-BasisCanvas C-8** (the peer-range row); this is the consumption gap. Its named substrate is dead at the target (B6) — **the gap survives, the substrate does not**; the row **re-premises onto `oklch`/`mixColors`**. F.W2 owns the re-premise; F.W3/W4 owns execution as FR-CP-2's cure substrate, cross-ref the F.W1 tri-package charter. ⟨fr-CoefficientsPanel FR-CP-15⟩

### Arm C — the charter widening (rulings, not cures)

**C1 · AA-20 (= C·D-4 / L-14)** — MAJOR. Five single-theme raw palette triples in an otherwise fully tokenised file; **8 repo `-300` tone sites, 5 owned here** (sole owner of violet/sky/emerald). Seat-confirmed live: `actionTone` at `AdminAuditLog.vue:67`, consumed `:135`. **`actionTone` is a SECOND hand-rolled colour authority living entirely outside `colors.ts`** — the census scopes hand-rolled colour to `colors.ts` alone. Verbatim ⟨cmd⟩ `sed -n '57p' fr-AdminAuditLog.md`: *"**F.W2 must widen or exclude with reasons**. **→ F.W4** (cure) + **F.W2** (charter note)."* The ruling is authored at §4 R-i with the full G7 enumeration; the cure is F.W4's and dies with AA-3's Badge cure. The repo's own AA standard: `style.css:113-118`. One home, two citations. ⟨fr-AdminAuditLog AA-20⟩

**C2 · D-3+C-5 (fr-EquationPanel; kill 19 / ruling 4)** — MAJOR. `energyColor` is a **THIRD** hand-rolled colour authority outside `colors.ts` — a three-arm step over ℝ, unconditioned literals, total on the render path. The D axis's falsifier ("consumed `EquationPanel.vue:6,38` and nowhere else") is **FALSE at HEAD — seat-re-confirmed**: declared `lib/equation/notation.ts:44`, consumed at `EquationPanel.vue:6,:38` · `InfoCard.vue:5,:14` · `EquationView.vue:7,:70`. **AMENDED** ⟨cmd⟩ `sed -n '23p' fr-EquationPanel.md` (the record's own ruling-4 row, *"KILL SUSTAINED at my own grep"*, enumerating `EquationPanel.vue:6/:38` **+ `EquationView.vue:7/:70` + `InfoCard.vue:5/:14`*): cure blast radius = **3 consumers / 2 routes**; 6-literal budget per M-N1 unchanged; *"a fourier-local darkened arm must be regression-checked on the two `/equation` surfaces no D-axis probe ever opened."* **Cure → F.W4**, bound into M-N11's one-edit law with D-7/D-9/M-N12 (each partially regresses another if split). **F.W2 carries only the charter-widening citation** — scope evidence for R-i, not a second cure. ⟨fr-EquationPanel D-3+C-5⟩

### Arm D — the denominator

**D1 · C-25 (fr-CanvasControlsDock)** — INFO, negative + denominator cell. Zero value.js and zero keyframes consumption — **this file's F.W2 migration cost is zero**. Verbatim ⟨cmd⟩ `sed -n '96p' fr-CanvasControlsDock.md`: *"its two `--viz-amber` CSS reads join the F.W2 colours denominator. **NO-WAVE-OWNER** observation (spec-authoring input); the colors.ts identity stays with census W.L5."* **The exact shape of the wave's denominator problem: zero import cost, non-zero colour cost.** Cited by G11; also a P-9 negative-roster member — one home in F.W5's export, two citations. ⟨fr-CanvasControlsDock C-25⟩

**D2 · K-24 (fr-PaperArticleWindow)** — MINOR, method obligation. Challenge-C §0's self-declared ground-truth inventory is **wrong** (4 distinct glass-ui tokens across 6 sites — `--border`:182, `--muted`:184, `--primary-foreground`:195, `--primary`:196/:200/:205, hover box-shadow omitted — not *"3 utility classes, 0 component imports … + 5 theme custom properties (L182,184,195,196,200)"*). **Elision restored at repair round 4** (PASS-3 D-10): rounds 1–3 rendered the challenged cell as *"3 utility classes + 5 theme custom properties"*, dropping `, 0 component imports …` with no ellipsis — an unmarked elision inside the very row whose banked ruling is that the inventory must be exact. Verbatim ⟨cmd⟩ `sed -n '303p' fr-PaperArticleWindow.md`: *"the F.W1/F.W2 blast-radius argument rests on it — the inventory must be exact."* (the bank sets no emphasis inside this clause; the bolded rendering rounds 1–3 carried is **struck at repair round 4**, same class as D-6). **F.W2's denominator may not inherit any corpus §0 inventory cell without re-measurement.** Binds G11 and every budget figure; MEASURE-AT-OPEN (D-19). **§2b is this wave's discharge of K-24 against its own bounds: eight authoring-register anchors re-measured, eight found stale, and all eight contributed to F.W0's G-11 table rather than asserted as a rival act (R-9.1).** ⟨fr-PaperArticleWindow K-24⟩

**D3 · NEGATIVE-ROSTER (packet P-9 citation, NOT a booking)** — INFO, measured zeros; **evidence, never a denominator**. **SIXTEEN** independently measured zero-cells fence the scope. **THE TWO DENOMINATORS ARE SEPARATED AT REPAIR ROUND 4 (PASS-3 D-7), because rounds 1–3 fused them under one attribution.** The inbound export at **F-W5 §4's `F.W5 → F.W2` edge row** enumerates **TEN** members — App API-inert · MobileFloatingToc · CollapsibleSection · PaperSearch · PaperSearchInput · PaperSearchDropdown · MorphShapePreview · CanvasControlsDock · AnimationControls · UserSlugBar FR-USB-38 (⟨cmd⟩ `grep -o 'exports zero-cells (P-9)' F-W5.md` returns that row's act cell as word-output; its enumerated members are read off the row, never counted out of a live sibling — R3-3.10). **F.W2 drops none of the ten.** The remaining **six are F.W2's OWN measured zero-cells**, carried on this wave's authority and attributed to no other home: fr-Tooltip FR-TT-22 · fr-EquationModeToggle FR-EMT-24 · fr-FunctionInput item 6 · fr-PaperView C-08 · fr-ConvergenceTimeline S-9 · fr-AdminUserList FR-AUL-55. **10 + 6 = 16**: sixteen is a true count of F.W2's roster and was a FALSE count *"against the real export"*, which names ten — six of the sixteen were attributed to a home that does not carry them, in cells (D3, G11, §7) that cite that home as their authority. The fold-seat roster said *"eleven"* and was **short by five of the export's ten** (it carried App · MobileFloatingToc · MorphShapePreview · CanvasControlsDock · UserSlugBar and dropped CollapsibleSection · PaperSearch · PaperSearchInput · PaperSearchDropdown · AnimationControls); that half of the round-1 correction stands unchanged (R-1f + R-2c):

*Carried at authoring (11)*: fr-App C-12 (API-inert; the general law: *"a per-component sweep must not manufacture one"*) · fr-Tooltip FR-TT-22 ("a zero-cost row — a negative budget statement") · fr-UserSlugBar FR-USB-38 (**"do NOT book"**) · fr-EquationModeToggle FR-EMT-24 ("a load-bearing negative the sweep must not violate") · fr-FunctionInput C·S-1/S-2 item 6 (**"already at the F.W2 target posture"** — every colour a token composed with `color-mix()`) · fr-CanvasControlsDock C-25 · fr-MobileFloatingToc item 9 (closure grep) · fr-PaperView C-08 ("provably does not reach this subtree") · fr-ConvergenceTimeline S-9 ("no purchase here") · fr-MorphShapePreview C·S-2 · fr-AdminUserList FR-AUL-55.

*Restored at repair round 1 (5)* — each named in the export and absent from the fold-seat list:
- **fr-AnimationControls C-18** — booked as its own denominator row at **D4** (the export names it; the row is D1's twin), cited here.
- **fr-CollapsibleSection** — record-level zero-cell; the record mints **no** component-level value.js id, so it is carried **by record with its roster membership stated at F-W5 §4's `F.W5 → F.W2` edge row** (which names `CollapsibleSection` in its enumerated negative roster; stable anchor from repair round 2, R2-2) and its one tri-package cell named: **R-3** (`fr-CollapsibleSection.md:27`) folds the peer-deadlock **BY REFERENCE to banked fr-AnimationControls C-1** — *"One identity, one booking, forever."* (capital **O** — the bank's own casing; the lower-cased rendering rounds 1–2 carried is corrected at repair round 3, the same one-character misquote class this file catches at A2's up-cased `execution`) No id is invented for it.
- **fr-PaperSearch C-Z1/C-Z2** (`:57`) — *"load-bearing zeros re-run by δ"*, twice-ratified with **PSM-36/S-11**; the banked home of the load-bearing-zero idiom.
- **fr-PaperSearchInput C-13/C-14** (`:107`) — *"value.js 0 · keyframes 0 · fourier-API 0, **each with its own falsifier**, closed over the import closure"*; bounds the 69-line file's tri-package exposure to the lucide rename.
- **fr-PaperSearchDropdown C:C-23 / C:C-24 / L:§5** (`:77`) — *"Load-bearing zeros: sole producer import `glass-ui/button`; **zero value.js/keyframes/canvas/WebGL/API contact**; data via the build-time virtual module. **F.W5-W8 = no rows.**"* — itself a FOLD into `fr-PaperSearch C-Z1/C-Z2`, carried as such.

**Address settled by LABEL, not by coordinate (R-1f as re-cut by R2-2 at repair round 2).** The export is **F-W5 §4's `F.W5 → F.W2` edge row** — act cell *"exports zero-cells (P-9)"* — and **P-9's home row is F-W5 §5's `P-9` row** (*"zero-cells are **EVIDENCE, never a denominator** (B5); F.W5 EXPORTS the zero-cell roster into F.W2's and F.W4's denominators — one home, two citations"*, home F.W4 body + F.W2 copy). **RECEIPT CONVERTED TO SET-MEMBERSHIP at repair round 4 (R3-3.10)**: rounds 1–3 banked *"`grep -n 'zero-cells' F-W5.md` returns exactly those two rows"* — a frozen count of a moving sibling, false the moment F-W5 is edited, and the identical receipt §6b reported with a different number in the same file. The lawful form: ⟨cmd⟩ `grep -o 'zero-cells' F-W5.md` returns the token, and the two rows carrying it are named by their labels — the `F.W5 → F.W2` edge row (§4) and the `P-9` row (§5). **The membership is the claim; the cardinality is not F.W2's to bank.** **The round-1 fix was correct in substance and wrong in kind**: it replaced one line number into a live sibling — which had drifted onto an unrelated F.W5 gate row — with two more, and **both of those drifted again inside a single repair round**, which is the whole demonstration R2-2 rests on. The erratum is kept as its LAW — *the fold seat cited an F.W5 gate row as the export; the export is the `F.W5 → F.W2` edge row* — and its coordinates are struck. **P-9's home is settled and F.W2 is a citing end.** **Two clauses are PRESERVATION obligations.** fr-MorphShapePreview C·S-2, **quoted at its own bytes at repair round 4 (PASS-3 D-2 · R2-1-LAW.2)** ⟨cmd⟩ `sed -n '138p' fr-MorphShapePreview.md`: *"C·S-2 — dodging `lib/colors.ts`'s broken oklch path by staying in CSS — carried. Preserve verbatim through F.W2."* **⚠ The rounds 1–3 rendering is STRUCK**: it read *"dodging `lib/colors.ts`'s broken oklch path by staying in CSS — **preserve verbatim through F.W2**"*, which dropped the word **`carried.`** with no ellipsis and lower-cased **`Preserve`**, silently demoting a standalone imperative sentence into a subordinate clause of the one before it. Three sites in this spec called that string *verbatim* (here, §8a row 36, §6b's F.W5 row) while the ⟨cmd⟩ printed beside it returned something else — a ninth instance of the class round 3 declared closed, on a clause this wave thrice calls a preservation obligation. **The obligation is the bank's two sentences, both of them.** The second: fr-FunctionInput's target posture — the wave must not regress a file already at its own target. Every zero-cell subtracts from the budget; **none may be counted as a defect.** ⟨16 records ⟨cmd⟩ `sed -n '138p' fr-MorphShapePreview.md` · `sed -n '112p' fr-FunctionInput.md` · **F-W5 §4's `F.W5 → F.W2` edge row** (export) · **F-W5 §5's `P-9` row** (home)⟩

**D4 · C-18 (fr-AnimationControls:120)** — INFO, negative + denominator cell; **the colors.ts-deletion denominator TWIN of D1's C-25**, adopted here per R-2c. Banked verbatim ⟨cmd⟩ `sed -n '120p' fr-AnimationControls.md`: *"Zero value.js colour consumption in the tree's most colour-dense transport surface (honestly scoped by the axis: colors.ts is NOT in this graph)."* → ***"NO-WAVE-OWNER observation; the colors.ts deletion identity stays with census W.L5."*** Corroborated at `:192` ⟨cmd⟩ `sed -n '192p' fr-AnimationControls.md`: ***"Census W.L5 item 2: colors.ts deletion — C-18 scoped out, no claim on this graph."*** **Exactly D1's shape and therefore exactly D1's landing**: zero import cost, and here **zero colour cost too** — the strongest single negative in the denominator, because it is the surface a colour-keyed census would most expect to hit. **F.W2 books the denominator cell only; the W.L5 deletion identity is not re-homed here, and no cure is minted.** Distinguish from same-record neighbours that do **not** land here: **C-17** (hand-rolled `linear`, → **F.W4**, cited at A2) and **C-30** (fold of intake R6-8, identity stays with R6-8's **F.W5–W8** carry — **adopted at F.W5 per R-2a; F.W2 neither books nor cites it as its own**). ⟨fr-AnimationControls C-18 (`:120`, `:192`) ⟨cmd⟩ `sed -n '120p;192p' fr-AnimationControls.md` · roster membership at **F-W5 §4's `F.W5 → F.W2` edge row**, which names AnimationControls in its enumerated negative roster⟩

### Arm E — sequencing facts and constraints

**E1 · C·D-11 (fr-EquationView)** — MAJOR, sequencing fact. The glass-ui→value.js peer edge is **unmet TODAY** (installed 0.13.0 vs peer `^0.10.0 || ^0.11.0`; optional ⇒ EBADPEER warning, not failure), and the 0.13.0 pin is **held from BELOW**: keyframes.js@4.3.0 hard-deps `value.js ^0.13.0` + optionalDeps `glass-ui ~4.0.0`. Seat-re-confirmed at fourier's manifest. Verbatim ⟨cmd⟩ `grep -n 'sequencing fact for the tri-package' fr-EquationView.md` → the `C·D-11` row: *"F.W0/F.W2 — sequencing fact for the tri-package transaction; corrects the corpus's 'latent, not live' framing."* **The backward pin is what makes F.W2 un-landable alone; this spec states it as its OWN opens-after condition, not inherited** (§1). The EBADPEER reproduction folds to the census risk-1 family (banked ×3: fr-CoefficientsSpectrum, fr-CanvasControlsDock, fr-CanvasOverlayButton) — no re-book. ⟨fr-EquationView C·D-11⟩

**E2 · MG-ι (value.js-side half) — packet P-7** — MINOR at fr-App; **MAJOR as a cross-repo trap once the uplift lands**. At installed 0.13.0: `color2(parseCSSColor(x), …)` **throws**, and `color2(parseCSSColor(x).value, …)` does **not** throw and returns **wrong channels** ({r:0.33,g:0.54,b:0.32}, green for a red input) — *"the obvious unwrap repair ships a wrong-colour bug with no error signal."* **NEW SEAT FACT, re-measured 2026-08-28: `colorUnit2` and `color2` are BOTH DELETED at 4.0.0** (0 hits src+dist). **The banked constraint — "route through `colorUnit2`; `color2` never" — is a 0.13.0-WINDOW RULE THAT DOES NOT TRANSFER and must not be transcribed into a post-uplift spec** (M-25's transcription defect). At 4.0.0 the sanctioned remedy is `parseCssColor` (`./css`) + `convertColor` (`./color`) — exactly B8's prescription. R-6's double-keyed receipt (`#d73523` = MG-κ's stale-dist hex) is **0.13.0 evidence only**. **The `light-dark()` probe-element rule survives BOTH pins and is the one constraint that transfers whole.** **P-7 home ruling adopted: F.W2 BOOKS the constraint** — *"fourier's value.js-consumption wave is the declaring end … Booking it NO-WAVE-OWNER leaves a live trap stateless"* (COHESION §2 both-ends). The reciprocal row is owed to X·V / V·π (§6). **R-5 cascade lock on any witness**: expect `oklch(` in DARK and `light-dark(` in LIGHT (`dark-arm.css` imports AFTER `light-dark.css`, `tokens.css:32→:33`). **NAMED FOLD — `fr-GalleryView` superlative 5 (`C·C-1`, `:118`)**, the banked id for the method this row and G9 already practise, carried by id at last: *"**C·C-1's value.js seam discipline** — traced `lib/colors.ts` to the installed `@mkbabb/value.js` surface and **refused to book the cure blind, gating it on the parser-proof R1 oklch crash**. The single most action-relevant sentence in the corpus; inverts the census's 'cheapest leg' framing, and the bank already adopted the wave-lock it implies (FR-GFC-8's F.W2 rider)."* **This is G9's charter in the corpus's own words and the reason B8's deletion is not a cheap leg.** Folded here — no second row, no re-book; the FR-GFC-8 wave-lock it implies is carried at **B4**. **Dissents preserved**: fr-App DU held C-2 at BLOCKER; ruled MAJOR (R-6). fr-App K-11's R1-crash discharge was measured **at 0.13.0 only — the 4.0.0 target is unprobed** (G9). ⟨fr-App MG-ι, R-6, K-11 · **fr-GalleryView superlative 5 = C·C-1** · packet P-7 · seat measurement⟩

### Arm F — correspondence

**F1 · W.L5-ACT3 / RD-8 (facility 19)** — MAJOR; the wave's correspondence half — **an obligation, not a defect**. The facility-19 letter (O-14, sent 2026-07-27) delivered, **never committed, never acknowledged** — untracked in fourier's tree, cited by nothing in their `M.md`/`PROGRESS.md`/`ADOPTION-ASKS.md`. **FN-1..FN-7 (`VALUEJS-R-UPLIFT-ASKS.md`, `83d4c9f`) all seven UNANSWERED**; the 2.0.0 peer-floor note **two majors stale** (`web/package.json:18` still `^0.13.0`, seat-re-confirmed — a caret that cannot resolve 2.0.0 **or** 4.0.0). **RD-8's law, binding and verbatim** ⟨cmd⟩ `sed -n '124,128p' library-band.md` — a RECEIPT RANGE, one line wider than the law text's own **span `:124-127`**, because the clause soft-wraps across four source lines and a single-line receipt for it would be a false witness: *"every symbol fourier imports from `@mkbabb/value.js` appears exactly once, with its subpath, its status against the shipped version, and the fourier obligation it discharges; direction one-way (value publishes, fourier consumes); completeness bidirectional (no unlisted import, no listed symbol without a consumer)."* **EMPHASIS STRUCK at repair round 4 (PASS-3 D-6 · R2-1-LAW.2).** Rounds 1–3 bolded *one-way* and *bidirectional* inside this quotation; the bank sets the whole clause in ONE italic span and bolds neither word. Added emphasis is a rendering the cited file does not carry — carried here under the strongest label in this spec, on the clause that constitutes facility 19's entire law. The direction-vs-completeness contrast below is F.W2's own statement of the law's consequence and lives **outside** the quotation marks, where emphasis is F.W2's to set. **RULED for F's one-way law — NOT the bidirectional law of the 18 HTTP facilities**; O's contrary argument preserved as dissent — **the label is load-bearing: "same law" would oblige fourier to publish a mirror.** Populated from O's I-1..I-10 table; `counts` annotated **as-specified** with an **as-built** sibling census + its command (**fourier http = 30 at HEAD, not 41**, library-band seat re-measure). **The six-symbol import set the ledger must enumerate is measured in A1 and is complete at HEAD.** **Route**: F.W2 owns the **LEDGER** and the **ANSWER**, not the substance. FN-1/2/3/4/6 API-shaped → F.W5/F.W6. **FN-5** chained to the TA-4 value-side atomdiff restoration = **SS-4's OWNER-GATED G4** ("owner rules RESTORE, or v2 re-scopes value.js out EXPLICITLY") — **F.W2 answers the letter and names the gate, never pre-empts the ruling**; FN-5's standing rider ("authored BEFORE or WITH fourier M.W10") travels. **FN-7** (neutral home for `J-diff-shape.md`) is F.W5's, both-ends. The 2.0.0 note's §3 strike re-authored **RETIRED-BY-DELETION per B6** — with B6's seat correction that 4.1's SCI-1 re-ships the symbol. Vehicle: the D-15 grant, fourier half only (RD-11). **E13 mail law: the reply lands in the INBOX ledger.** ⟨library-band.md §W.L5 ACT(3), RD-8 · lane-docs §8b/§8c/:473-474⟩

---

## 4. Rulings — the three contests this wave owns

Authored here as the wave's proposed dispositions (arm-A graft; R-iii added at repair round 1); **recorded at unit `.c` in `RULINGS-F.W2.md` before any act is authored** — that recording is what turns G7/G8/G20 GREEN. Falsified by G7/G8/G20's witnesses.

**R-i (AA-20): WIDEN.** The charter's "hand-rolled colour = `colors.ts`" scope is **widened** to: *any code path that produces or transcribes colour values outside the token cascade*. Enumeration (the G7 roster, complete or the spec is defective): `actionTone` (C1) · `energyColor` (C2) · `spectrumColor` ×4 (B5) · the NotationPills triad (B11) · ImageUpload's `STATIC.rainbow` duplicates (B12) · ContourPreview's 9-site inline amber (B3). Reason to widen rather than exclude: the wave's own denominator law (G11) is a colour-VALUE census precisely because the import-keyed census was provably blind three ways — excluding authorities by file would rebuild the blindness the census obligation exists to kill. **Cures stay homed** (F.W4 for `actionTone`/`energyColor`; F.W3/W4 for the ramp and triad; this wave for the amber cut) — widening the charter mints zero new cures here.

**R-ii (M-RTC): the producer facility is the PRESUMPTIVE seat, conditionally.** Under the standing glass-ui-first law, `resolveTokenColor`/`createTokenColorCache`/`useTokenColor` at `@mkbabb/glass-ui/dom` is ruled the sanctioned seat for **token→used-value resolution** (it IS the fr-App B-1 remedy, shipped, at the current pin, no uplift); W.L5 ACT(2)'s `parseCssColor`+`convertColor` route is **narrowed to colour STRINGS that are not tokens** (the declared hex residual; literal parsing where JS must see colour) — the two prescriptions partition rather than contest. **Conditions, both escalated, neither pre-empted**: (a) the SS-6 GLASS-RELAY ask confirms the facility as the sanctioned consumer seat; (b) SS-13 answers the `getComputedStyle` oklch-vs-rgb serialization question (drop-in vs format adapter). If either returns against, ACT(2)'s fourier-side context resolution stands as the fallback exactly as written. The landing is F.W1's either way (banked D-1 identity); this ruling is the charter correction M-RTC demands, and it prevents ACT(2) from re-implementing a producer surface.

**R-iii (the easing cluster's two cure-shape locks): ROUTED TO F.W1, NOT RULED HERE.** Repair round 1. Two banked locks bear on the specifier arm and were previously neither cited nor refuted; both are now carried at **A6** and dispositioned here:

- **`fr-MorphPhaseConfig MPC-2`** (`:46`) folds into `fr-EasingPicker L/B-1 + C/B-1 · F.W1` with the parenthetical ***"lockstep tri-package bump per MPC-14, never a specifier rewrite"*** — ⟨cmd⟩ `sed -n '46p' fr-MorphPhaseConfig.md`, run this round (⟨cmd⟩ note added at repair round 3: A6's table carried the receipt, this ruling quoted the same bytes without one, and R2-1-LAW.1 attaches the note to the QUOTATION, not to the file). §2a rows 1–4 prescribe `specifier → /easing` across four files. **Ruling: F.W2 does not resolve this.** MPC-2's terminal disposition is a FOLD into an F.W1-homed identity, so its cure-shape clause is **F.W1's to state**; a consuming wave that overrode it would be re-homing another wave's ruling. What F.W2 owes — and discharges — is (a) carrying the lock verbatim, (b) putting the measurement that bears on it on the record: **G1 measures 5 of 5 bare-root sites `ERR_PACKAGE_PATH_NOT_EXPORTED` against 4.0.0's export map, so a lockstep bump alone does not make a bare-root specifier resolve**, and (c) routing the contest to the F.W1 cross-edge (§6b) with **no pre-emption**. If F.W1 rules the lock literal, §2a rows 1–4's cure-shape changes and this spec is amended by addendum, never silently.
- **`fr-EasingCurvePreview CENSUS-MC`** (`:58`) AMENDS the census: ***"the value.js-specifier half of the scheduled migration is NOT blocked on the uplift"*** — ⟨cmd⟩ `sed -n '58p' fr-EasingCurvePreview.md`, run this round — `@mkbabb/glass-ui/motion-curves` at the PINNED 4.0.0 value-re-exports every symbol `lib/easings.ts` imports bare-root, **exception `timingFunctions`**. **Ruling: G10's premise is NARROWED, not defended.** What is gated on F.W1 is the **bare-root→`./easing` re-point measured against value.js's own 4.0.0 export map**, held from below by the keyframes 4.3.0 → `value.js ^0.13.0` hard-dep (C·D-11). CENSUS-MC's producer-re-export route is a **different route** whose availability F.W2 records and does not choose; **the route choice is F.W1's**, and A2's `timingFunctions` re-derivation is required under either. Both routes are named in `RULINGS-F.W2.md`; recording them is what turns **G20** green.

**Neither lock is overruled, neither is swept, and neither ruling touches an SS-4 owner-gated question.**

---

## 5. Gates

**All 20 born RED.** G1–G14 carry their ids verbatim from this wave's authoring register (**R-3: no `F-W2-CARRY.md` exists; "the CARRY" with no path is forbidden vocabulary**); **G15–G19 minted at authoring 2026-08-28**; **G20 minted at repair round 1, 2026-08-28**. **COMMAND BASES, DECLARED ONCE AND FILE-WIDE (repair round 4; the R3-9.5 idiom applied to this spec's own incompatible bases).** Rounds 1–3 mixed **three** of them in one file — bare `F-W0.md`, `waves/F-W1.md`, and `docs/tranches/X/fourier/carry/` — so at most one of the three could resolve from any single cwd, and PASS-3's D-4 conviction (*"Commands are literal"* against two commands that cannot run) is that defect at gate altitude only. The bases, binding on **every** ⟨cmd⟩ in this file: **`$W` = `/Users/mkbabb/Programming/value.js/docs/tranches/X/fourier/waves/`** — sibling specs, cited by bare filename (`F-W0.md`, `F-W1.md`, `F-W5.md`, and this file); **`$K` = `/Users/mkbabb/Programming/value.js/docs/tranches/V/megatranche/registry/adjudicated/`** — the frozen corpus, the base §3's manifest already declares; **`$F` = `/Users/mkbabb/Programming/fourier-analysis/`** — every fourier-side path, written absolutely and never relatively (R-10.6). **Everything else is literal and runs from `/Users/mkbabb/Programming/value.js`.** The six `waves/F-W1.md` spellings are normalised onto `$W` in this round's edit; no receipt's output changes, only its runnability. **R-10.6 correction**: every fourier-side command is now **absolutely rooted at `/Users/mkbabb/Programming/fourier-analysis/`** — there is no `fourier-analysis/` under the declared cwd (`ls /Users/mkbabb/Programming/value.js/fourier-analysis` → *No such file or directory*), so the five relative commands G5/G8/G11/G13/G17 previously carried were phantom witnesses. **The fourier tree stays READ-ONLY in every witness below** (greps, `ls`, node reads only).

| id | claim | command / named live witness | state | RED input (how to break it) |
|---|---|---|---|---|
| **G1** | Every specifier resolves at the target | `node docs/tranches/V/megatranche/audit/probes/fourier-value-import-drift.mjs` — **leg 1** | **RED [re-measured 2026-08-28]** — `exports = [./color, ./value, ./css, ./easing, ./math, ./transform, ./quantize]`, no `"."`, no `main`, no `module`; **5 of 5 sites ERR_PACKAGE_PATH_NOT_EXPORTED** (easings.ts:9,:16 · ConvergencePlot.vue:5 · useCurveTransition.ts:8 · harmonics.ts:5) | Leave any bare-root `@mkbabb/value.js` statement in the fourier tree. Green owner: **F.W2's edit, only after F.W1 lands the pin** |
| **G2** | The `timingFunctions` mapping is re-derived, not transcribed | same probe **leg 2** + `grep -rn 'easingNames\|timingFunctions' src/ dist/` | **RED [re-measured]** — leg 2 RED; grep **0 hits for BOTH symbols**; `src/easing.ts:166` returns `Result<EasingFunction, EasingIssue>`. Surviving domain: `bezierPresets` **30 keys** ∪ DIRECT_EASINGS **19 keys** — **corrected 20→19 at repair round 2 (R2-4.2), re-derived at the current bytes**: ⟨cmd⟩ `grep -n 'Object.freeze' src/easing.ts` → the block opens **`:94`**; ⟨cmd⟩ `grep -n '^});' src/easing.ts` → it closes **`:114`**; ⟨cmd⟩ `sed -n '95,113p' src/easing.ts \| grep -c ','` → **19** (ten base names + nine hyphenated aliases; `linear` alone has no alias — the whole of the 20-vs-19 discrepancy, A2 §3). **All 22 `EASING_LABELS` resolve, 0 unresolvable** | Transcribe W.L5 ACT(1)'s `easingNames()` mapping without re-deriving it, or unwrap `.value` without handling the issue arm — `getEasingFn`'s `?? linear.fn` swallows it into a silent linear curve |
| **G3** | The oklch/light-dark tokens resolve | `node docs/tranches/V/megatranche/audit/probes/fourier-vizcolor-oklch.mjs` | **RED [re-measured] — 5/6** — `--viz-fourier`, `--viz-chebyshev`, `--viz-legendre`, `--viz-green`, dark-form `--viz-fourier` all → `#888888`; **only `--viz-amber` → `#9d6515`, and only via fourier's own `style.css:120` local override.** Fourier = Chebyshev = Legendre = grey, both themes (K-12) | Ship a resolver that still string-matches token text. **Green owner: F.W1** (banked D-1/BC-1/C-1). **F.W2 never claims this GREEN — it owns the deletion of the arms that caused it** |
| **G4** | The SNAPSHOT is fixed, not the parser swapped | `basis-display.ts:1-7` read at module scope (**anchor corrected `:1-6`→`:1-7` at repair round 2 to the BANKED range** — ⟨cmd⟩ `sed -n '39p' fr-GalleryFeaturedCarousel.md` · `sed -n '49p' fr-GalleryInfiniteGrid.md` both read `basis-display.ts:1-7`; ⟨cmd⟩ `sed -n '1,9p' /Users/mkbabb/Programming/fourier-analysis/web/src/components/visualization/lib/basis-display.ts` → the `basisDisplay` literal opens `:3` and closes `:7`, so `:1-6` was short by the closing brace); `GalleryCard.vue:121` → `--pill-c` into background/border/text (`:270-274`); fr-App R-1 importer census (7 importers, **all** under `components/visualization/`, never the `/paper` chunk) | **RED [seat-confirmed]** — `color: VIZ_COLORS.fourier` read into a plain object literal at declaration; a theme flip cannot retro-write it on **any** entry path | **Entry-route-pinned by construction**: deep-link `/gallery` = fallback hexes; default `/paper`-first = post-poison grey on both sides (frozen and live **AGREE** — the split-brain does **NOT** reproduce). **A witness that does not pin the entry route proves nothing.** Green owner: F.W1's fix; **F.W2 owns the green-criterion** |
| **G5** | The `hexToRgba` deletion lands with its join | `grep -rn 'hexToRgba(' /Users/mkbabb/Programming/fourier-analysis/web/src/ \| grep -v 'export function'` | **RED [seat-re-measured — the authoring register undercounts; RE-SIZED 4→5 at repair round 2, R2-4.1]** — **5 call sites / 3 files**, and **the enumeration IS the figure**: ⟨cmd⟩ the command at left, re-run this round → **5 lines** — `golden-shimmer.ts:55` · `golden-shimmer.ts:58` · `epicycles.ts:258` · `epicycles.ts:283` · **`BasisCanvas.vue:261`** (named nowhere in that register, which said 2/2). The fold-seat cell enumerated these same five coordinates under a "4 call sites / 3 files" label — refuted by its own enumeration and by its own command; no count word in this spec may exceed or fall short of the five. §2b items 4–6 are this correction's contribution to F.W0's G-11 anchor table | **Same-commit rider law**: deleting `hexToRgba` without the B7 colour join (swatch + shimmer pipeline on ONE source) breaks **five call sites in three files**. **A rider law sized against a false count is a rider law that lets one consumer through** — the wave's hardest sequencing lock may not be sized at four |
| **G6** | The declared hex residual has a validation arm and a deletion date | `colors.ts:101-117` read; M-β5's NaN reproduction (3/4/8-digit hex → `rgba(…, NaN, …)`, Canvas2D silently keeps the previous style) | **RED [measured]** — zero validation exists; the residual W.L5 prescribes would inherit the hazard whole. `colors.ts` is exactly 117 lines | Ship the residual without validation. **Masked today by the 7-char fallback; detonates the moment D-1 is cured with a hex-emitting branch** (M-β5) — and the fast-path is **newly-dead, not born-dead** (L-9/MG-κ: `dist/assets/index-57FkGzlZ.css` → `--viz-fourier:#d73523`). Deletion date = fourier's 4.1 adoption (RD-6/RD-7) |
| **G7** | The charter names every hand-rolled colour authority | `docs/tranches/X/fourier/RULINGS-F.W2.md` records R-i, each authority **widened or excluded with a reason** | **RED — six authorities measured against a charter that names one.** Seat-confirmed live: `actionTone` (`AdminAuditLog.vue:67`) · `energyColor` (`lib/equation/notation.ts:44`, 3 consumers / 2 routes) · `spectrumColor` **×4 declarations / 8 files** · the NotationPills triad · ImageUpload's 6 `STATIC.rainbow` duplicates · ContourPreview's **9 inline amber sites** | *"F.W2 must widen or exclude with reasons"* (AA-20, verbatim — ⟨cmd⟩ `sed -n '57p' fr-AdminAuditLog.md`, run this round; the label carried no ⟨cmd⟩ through rounds 1–2, which R2-1-LAW.2 makes a defect per se). **A silent scope is a DEFECTIVE spec.** Ruling gate — F.W2 rules (R-i); F.W4 cures |
| **G8** | The cure-seat contest is ruled before ACT(2) is authored | `ls /Users/mkbabb/Programming/fourier-analysis/web/node_modules/@mkbabb/glass-ui/dist/composables/dom/useTokenColor.d.ts` + `./dom` in the exports keyset; `RULINGS-F.W2.md` records R-ii | **RED [seat-verified live at glass-ui 4.0.0]** — `resolveTokenColor`/`useTokenColor` present in `dist/dom.js` and both `.d.ts`; `./dom` one of the **80** published subpaths. **Corrected 78→80 at repair round 2 (R2-4 class; the fold figure contradicted tree AND bank)**: ⟨cmd⟩ `node -p "Object.keys(require('/Users/mkbabb/Programming/fourier-analysis/web/node_modules/@mkbabb/glass-ui/package.json').exports).length"` → **80** (`./package.json` is not among them, so 80 is also the subpath count); ⟨cmd⟩ `grep -n '80-key' fr-ConvergenceTimeline.md` → the banked S-9 row reads *"real keys in the 80-key exports map"* (B9 carries the same correction). **Registry sweep: ZERO hits anywhere in the bank or corpus** | Author ACT(2)'s fourier-side resolver without ruling the contest — that **re-implements a producer surface** (standing glass-ui-first law). Green owners: **F.W2 the ruling (R-ii) · F.W1 the landing · GLASS-RELAY the adoption ask (SS-6)**; serialization → SS-13 |
| **G9** | The post-uplift parser constraint is measured, not transcribed | `grep -rn 'colorUnit2\|color2' src/ dist/` + an `oklch()`/`light-dark()` probe of `parseCssColor` **at 4.0.0** | **RED [re-measured]** — **0 hits, both symbols deleted**; `parseCssColor` survives at `./css`, `convertColor`/`oklch`/`mixColors` at `./color`. **fr-App K-11's R1 discharge was measured at 0.13.0 ONLY — the 4.0.0 target is unprobed** | Transcribe "`colorUnit2` yes, `color2` never" into a post-uplift spec — it names two symbols that do not exist at the target (**the M-25 transcription defect**). The `light-dark()` probe-element rule is the one constraint that transfers whole |
| **G10** | F.W2's **value.js-direct** specifier arm does not attempt to land alone — **premise NARROWED at repair round 1 (R-iii), because a banked amendment refutes the unqualified form** | fr-EquationView K-5 (0.13.0 exports = `['.']`, no `./easing`, no `./math`) · C·D-11 (keyframes 4.3.0 hard-deps `value.js ^0.13.0` + optionalDeps `glass-ui ~4.0.0`) · lane-frontend §RESOLUTION DEADLOCK · **`fr-EasingCurvePreview.md:58` CENSUS-MC (C-D-5), the amending authority** | **RED [seat-re-confirmed]** — fourier pins `glass-ui ^4.0.0`, `keyframes.js ^4.3.0`, `value.js ^0.13.0`. **The pin is held from BELOW and cannot move except inside F.W1's ONE atomic change.** **AMENDMENT CARRIED**: CENSUS-MC banks *"`@mkbabb/glass-ui/motion-curves` exists at the PINNED 4.0.0 … and value-re-exports every easing symbol `lib/easings.ts` imports bare-root … **exception `timingFunctions` holds** — the value.js-specifier half of the scheduled migration is **NOT blocked on the uplift**"* (→ **F.W1**, census §1/§3 AMENDED). So the gate reads over the **value.js-direct route only**: against value.js's own 4.0.0 export map, G1 measures **5 of 5 `ERR_PACKAGE_PATH_NOT_EXPORTED`**. The producer-re-export route exists and is **F.W1's to choose** — F.W2 records both and picks neither | Schedule the **value.js-direct** specifier edit before F.W1 — **or** state the unqualified "the specifier arm is blocked on the uplift" that CENSUS-MC refutes. **The colours arm is the declared EXCEPTION** — fr-App C-2: *"the `colors.ts` cure is landable at 0.13.0 today, alone; only the bare-root-specifier leg is genuinely gated on the uplift"*. **`timingFunctions` is gated under EITHER route** (A2) |
| **G11** | The denominator is colour-value-keyed, not import-keyed | a colour-VALUE census over `/Users/mkbabb/Programming/fourier-analysis/web/src/`, with **all sixteen** measured zero-cells **subtracted and cited** — **the TEN the inbound export enumerates** (**F-W5 §4's `F.W5 → F.W2` edge row**, cited by row label since repair round 2, R2-2; F.W2 drops none of them) **⊕ the SIX F.W2 measured itself** (Tooltip FR-TT-22 · EquationModeToggle FR-EMT-24 · FunctionInput item 6 · PaperView C-08 · ConvergenceTimeline S-9 · AdminUserList FR-AUL-55), enumerated at D3/D4. **Attribution corrected at repair round 4 (PASS-3 D-7)**: rounds 1–3 read *"the sixteen P-9 zero-cells"*, sourcing all sixteen to an export that names ten — six of them attributed to a home that does not carry them, inside the gate cell that cites that home as its authority. Eleven at the fold seat, five of the export's ten restored at repair round 1 | **RED — no such census exists; the current surface is import-shaped.** Three measured blindness instances: ImageUpload's 6 hexes in a zero-`@mkbabb` SFC · ContourPreview's 9 inline SVG attributes with zero declarations · CanvasControlsDock's zero imports + two `--viz-amber` reads. Method: K-24 — *"the inventory must be exact"*; fr-App B-2's break-class lesson, third instance at MG-β | Build the budget from import specifiers. Re-book any P-9 zero-cell as a defect, or count one in the denominator |
| **G12** | The π probe pair exists | one headed evaluate + before/after convergence-plot pair, light and dark, **force-added past `.gitignore:34 *.png`** (⟨cmd⟩ `sed -n '34p' .gitignore` → `*.png`), at **`docs/tranches/V/megatranche/audit/probes/app-wave/fourier-viz-{light,dark}-{before,after}.png`** — **path corrected and brought INSIDE §2a's File Bounds at repair round 4** (PASS-3 D-3 · R3-9.1): rounds 1–3 wrote `audit/probes/app-wave/…`, which does not resolve from the declared cwd, and named the four artefacts in no bounds row at all | **RED — no such files exist.** DELTA: four grey curves → four distinct hues | **R-5 cascade lock binds the readback**: expect `oklch(` in DARK, `light-dark(` in LIGHT (`tokens.css:32`→`:33`). **fr-App SS-13 item 1**: *"the divergence, not the greyness, is the diagnostic"* — confirm the **chrome** is correctly coloured in the same frame. Probe parsimony binding (owner edict) |
| **G13** | Facility 19 exists under RD-8's one-way law | `ls docs/tranches/X/fourier/facility-19.md` · `grep -rniE 'megatranche\|facility-19\|library-band' /Users/mkbabb/Programming/fourier-analysis/docs/` | **RED [measured; quantified witness CORRECTED at repair round 2, R2-4.3]** — `facility-19.md` **absent**; the correspondence grep returns **1 line in 1 fourier file** (`-o` → **2** occurrences), not the fold seat's "7 hits": ⟨cmd⟩ `grep -rniE 'megatranche\|facility-19\|library-band' /Users/mkbabb/Programming/fourier-analysis/docs/ \| wc -l` → **1**, the sole file being `docs/tranches/N/valuejs-inbound-2026-07-27-facility19-migration-table.md`; ⟨cmd⟩ same grep with `-o \| wc -l` → **2**. **The RED state is real and keeps its gate — only the number was unreproducible**, and an unreproduced number is an invention whatever the state. That file is itself `??` in git status. O-14 sent 2026-07-27, no reply, untracked. **FN-1..FN-7 all seven unanswered**; the 2.0.0 note two majors stale; the §3 strike un-struck at `M/PROGRESS.md:22`. **ZERO inbound rows FROM fourier** | Extend `API-FACILITY-ISOMORPHISM.json`'s **bidirectional** schema (RD-8 ruled a **different, one-way** law). Publish `counts` without the `as-built` census and its command. List a symbol with no consumer, or import one that is unlisted |
| **G14** | The substrate compiles and the anchors are re-ground | **F.W0's gate, stated here as F.W2's opens-after — F.W2 claims none of its GREEN** | **RED [partly re-measured]** — fourier worktree **28 dirty entries**; `git cat-file -t 14d83356` → *not a valid object* (both re-measured this seat). **DIVERGENCE RESOLVED AT THE FOLD SEAT**: arm B's brace-balance probe (223 lines, balanced) measured the wrong property — value.js's own `parseStylesheet` returns `ok: false` with a `css_syntax` diagnostic pinning chars **12396–13833**: the installed stylesheet's tail is **PROSE, not CSS** (begins ``"@source` line, Tailwind scanned an empty dir…"``). **FR-NP-32 (≡ fr-PaperSidebar M1) is CORROBORATED with a sharper witness than the original.** **Re-home note (R-8)**: the banked disposition at `fr-NotationPills.md:35` is **NO-WAVE-OWNER (producer dist emitter) + glass-ui BH relay at the TOP of the FR-COB-28 packet + F.W1 SEQUENCING GATE** ⟨cmd⟩ `sed -n '35p' fr-NotationPills.md`; the **F.W0 re-home is performed at F-W0 §3's carry row 1 (`FR-NP-32 ≡ fr-PaperSidebar M1`), whose R-column cell reads **`G-4`, relay item 0** (the fused *"NO-WAVE-OWNER (producer emitter) + …"* rendering rounds 1–2 attributed to that cell is **STRUCK at repair round 3** — it is at no byte of F-W0; the strike and its ⟨cmd⟩ receipts are minuted once, at B11, and are not restated here); that row's own words: *"THE F.W0 RE-HOME IS PERFORMED HERE (R-8) … Any later wave that needs 'an F.W0 pre-gate' cites THIS row rather than asserting it bare"* — ⟨cmd⟩ `grep -n 'THE F.W0 RE-HOME IS PERFORMED HERE' F-W0.md`). **Address converted at repair round 2 (R2-2): the fold seat's F-W0 line coordinate for this row had drifted onto the carry table's header row and is struck without replacement in kind; a banked row id plus its gate id cannot drift.** **F.W2 cites that re-home and never asserts an F.W0 pre-gate bare**, and it leaves the F.W1 sequencing arm undisturbed. F.W0 still re-grounds **with the app's own toolchain** — banked repro at `fr-NotationPills.md:35`: *"`@import "@mkbabb/glass-ui/styles"` under `@tailwindcss/postcss` → `CssSyntaxError: Unterminated string: 's own'` … **`vite dev` and `vite build` are RED at the `^4.0.0` pin**"*; the producer-side notice rides the GLASS-RELAY. F8-REACH-01/02 stand; fr-App MG-θ (no lint config) booked F.W0/F.W2 jointly | Cite any line anchor, HEAD figure, or "LIVE at certified HEAD" before F.W0 re-grounds (D-19). No F.W2 probe (G3/G4/G12) may execute on a build that does not compile |
| **G15** | The easing migration declares its curve drift **in the BANKED figures** | same drift probe **leg 3** (`fourier-value-import-drift.mjs`), read against the bank | **RED — RE-WRITTEN BANKED-VERBATIM at repair round 1 (R-1d); the `⭑ seat-minted` stamp is STRUCK.** The figure is **`14 of 22 drift analytic→CubicBezier-approximation, of which 8/22 are MATERIAL (max Δ 0.192)`** — banked at **`fr-EasingPicker.md:52`** (*"**14 of 22 silently drift analytic→CubicBezier-approximation** (8 stay analytic incl. ease-out-cubic/-expo)"*, LC's sharpenings **RATIFIED with CC-2**), at **`fr-FourierMorphSvg.md:46`** (*"14/22 presets drift analytic→CubicBezier under a CORRECT rewrite"*), and at **`library-band.md:295-296`** (the 3-leg probe: *"5 × ERR_PACKAGE_PATH_NOT_EXPORTED; `timingFunctions` deleted; **8/22 curves drift, max 0.192**"*). **ONE fact at two denominators — 14 shape-class changes, 8 material by the probe's Δ threshold — never rival numbers.** **`ease-out-cubic` and `ease-out-expo` are among the 8 that STAY ANALYTIC**; the fold-seat cell named `ease-in-cubic`/`ease-in-expo` in its material eight, which is consistent with the bank only under the material reading and must never be re-stated as the drift total. The material eight this seat re-ran: `ease-out-circ` 1.923e-1 · `ease-in-expo` 6.930e-2 · `ease-in-circ` 4.489e-2 · `ease-in-quad` 4.157e-2 · `ease-in-cubic` 3.162e-2 · `ease-out-sine` 3.082e-2 · `ease-in-sine` 3.038e-2 · `ease-out-quad` 2.520e-2. Cause: 0.13.0's `timingFunctions` carried analytic in/out curves; 4.0.0 keeps only the in-out arms in `DIRECT_EASINGS` (**`src/easing.ts:94-114`** — **anchor corrected at repair round 2 (R2-4.3) from the fold seat's `:113-132`**, which was transcribed from the drift probe's own stale inline comment and spanned the table's last three entries plus the whole `CubicBezier` function: ⟨cmd⟩ `grep -n 'Object.freeze' src/easing.ts` → `:94`, ⟨cmd⟩ `grep -n '^});' src/easing.ts` → `:114`, with `CubicBezier` at `:116-129` and `steppedEase` at `:131+`. **Never transcribe a probe's stale comment — this is the M-25 class this wave polices at E2/G9**) and falls through to the cubic-bezier `PRESETS` table. **The drift is confined entirely to the `timingFunctions` re-derivation** — the five named symbols are numerically identical (max\|Δ\| ≤ 4.44e-16). **Provenance (R-1d)**: authority = `fr-EasingPicker` **L/B-1 + C/B-1** (CC-2 ratification) + `library-band.md:295-296`'s probe; **accepting authority = F.W1's ESC-4** (**F-W1 §2 WU-R's `ESC-4` row**, *"14/22 analytic→CubicBezier; 8/22 material, max Δ 0.192"*, corroborated MPC-5, folds to RD-5 RULED RESTORE). **F.W2 claims no credit for this measurement** (FR-GIG-5) | **X-W9's G24 is the producer-side twin; ACT(1) IS the consumer-side drift site.** Migrate `easings.ts:9` and ship silently different animation curves. **Restate the drift as "8 of 22" without its 14/22 parent, re-stamp it seat-minted, or name a curve the bank puts among the 8 that stay analytic — each is a defect against the bank, not a measurement.** Green: either the mapping records a **probe-pinned declared divergence row in the banked figures**, or W.L6's analytic-arm restoration lands first (RD-7: order-independent, either way) |
| **G16** ⭑ | `easing()` is called once, never on the render path | `easing('ease-in-out').value === easing('ease-in-out').value` | **RED [measured] — `false`; fresh closure per call** (matches X-W9 G23). **Neutralized by shape at HEAD**: `EASING_PRESETS` is built once by `Object.fromEntries` at module scope (`easings.ts:55-60`), so the instability is absorbed | Re-arm it: rewrite `getEasingFn` to call `easing(name)` per invocation — every rAF tick allocates a new closure and every identity/dep comparison downstream flips |
| **G17** ⭑ | The deletion count is honest | `grep -rn 'hexToRgb(' /Users/mkbabb/Programming/fourier-analysis/web/src/ \| grep -v hexToRgba` | **RED [measured] — `hexToRgb` has ZERO call sites outside its own declaration** (`colors.ts:111`); a dead export | Count `hexToRgb` as one of five "migrated" arms. It is deleted for free; only `hexToRgba` (G5) carries consumers, and the 3-line residual (G6) must not be sized as if it replaced both |
| **G18** ⭑ | The value.js-side reciprocal is filed, not folded into fourier's ledger | `parseStylesheet` (`dist/subpaths/css.js`, built Aug 3, unchanged between seats) invoked on fourier's installed `glass-ui@4.0.0 dist/styles/index.css` (13,949 B = 13,833 chars, sha256 `ce97a135…`) | **RED — the filing does not exist, and the defect RESHAPED at the fold seat.** Arm B recorded a THROW (`reading 'slice'`, "13,949-byte"); fold re-probe 2026-08-28, same dist, same input: **string input does NOT throw** — returns `ok: false` + ONE `css_syntax` diagnostic spanning chars **12396–13833** (the stylesheet's tail is PROSE, not CSS — see G14); **Buffer input throws `e.startsWith is not a function`**. Arm B's exact throw reproduced with **neither** input — divergence recorded | File arm B's unreproduced throw as fact, or route the corrupt-tail diagnostic as a value.js defect (it is glass evidence, G14). The reciprocal that files to **X-W9**: the Buffer-input totality nit (*"no public entry throws"* — the string arm HOLDS on this input). F.W2 files it and claims no GREEN |
| **G19** ⭑ | **Census closure — RE-BASED at repair round 1 (R-3); DETECTOR STATED INLINE at repair round 2 (R2-9); OPERAND RE-BASED ONTO THE CORPUS AND THE DETECTOR RE-CUT ON ALL FOUR AXES at repair round 4 (rulings R3-4 + R3-5.1; PASS-3 D-1)** | **LHS = THE 66 `fr-*.md` RECORDS THEMSELVES** at `docs/tranches/V/megatranche/registry/adjudicated/`, read through the detector below, ⊎ the non-literal `library-band.md` §W.L5 ACT1/ACT2/ACT3 routings ⊎ the declared inbound P-9 export at **F-W5 §4's `F.W5 → F.W2` edge row**. **The PASS-1/PASS-2/PASS-3 check files are named as PRIOR RUNS and are never the operand** — R3-4's standing law, *a closure transcript whose LHS is a count copied from any check file FAILS BY CONSTRUCTION*, and here it is not an abstraction: rounds 1–3 keyed this gate to `PASS-1/F-W2-CHECK.md` §1⊎§2's **54**, so the gate could only ever re-find that seat's misses, and the two `fr-PathPreview` rows PASS-1 never saw were structurally ineligible to be found missing for three rounds. Set-difference against §3 + §4 + §6 + §7 + the §8 roster: **∅ both ways**, checked id-for-id at wave open. **DETECTOR — FOUR AXES, INLINE (R2-9 ⊕ R3-5's standard): (i) SHAPES** = table rows whose terminal cell routes ⊕ **id-headed route-marked bullets** (`→` · `**ADJUDICATED →**` · `⇒` · `rides` · `routed` · bold-at-disposition) ⊕ prose routings walked back to their enclosing banked row id (S-8) — a route mark in ANY format books the row; **(ii) SPELLINGS** = every separator the corpus uses, `F`,`.`,`·`,`-`,`–` and the zero-separator form, probed in **BOTH U+002D and U+2013**; **(iii) POSITIONS — ADDED AT REPAIR ROUND 4, THE AXIS THAT WAS MISSING**: a member matches **whether it is written FIRST or SECOND in a compound**, so the probe is `(F[.·–-]?W[0-9]+[/–-])?F?[.·–-]?W2` in both dash spellings, run as two arms — ⟨cmd⟩ `grep -lE 'F[.·–-]?W2' fr-*.md` (first-position: **25 records**) and ⟨cmd⟩ `grep -loE '[FW][.·–-]?W?[0-9]+[/–-]W2' fr-*.md` (second-position: `fr-PathPreview.md`, form `F.W1/W2`), union **26 records**; **(iv) RECORD-QUALIFICATION** = the booking test is the **`(record, id)` PAIR**; a bare token is not an identity, and a short token books only where this file names its record on the same line (the law §8a already obeys and §3 states at A6). **EXPANSION** = every slash form expanded to its member set **in both positions** (`F.W3/W4` contributes to neither operand here; `F.W5–W8` band rows are F.W5's; **`F.W1/W2` contributes to THIS one**). Counts over the frozen corpus are lawful (R2-2.2(i)); counts over any live sibling or over this file are **not** (R3-3.10). **A shape-, spelling- or position-restricted operand is DEFECTIVE at authoring whatever its result** | **RED — the instrument itself was unresolvable at the fold seat and the difference was non-empty.** (a) The old LHS was "the CARRY", an authority with **no path**: `ls docs/tranches/X/fourier/carry/` → `F-W1-CARRY.md`, `F-W4-CARRY.md` **only**. *(The self-count rounds 1–3 banked beside this — `grep -cE 'carry/F-W\|F-W2-CARRY' F-W2.md` → **0** — is **STRUCK at repair round 4**. It is not merely unlawful under R3-3.10; it is **FALSE at these bytes**, because the very repair that cured the defect made this file cite `carry/F-W1-CARRY.md` by path. A banked count of the citing file is falsified by the edit that cites it. The lawful statement is the classification, and it is stated so that no later edit can falsify it: **the carry paths this file names are exactly `carry/F-W1-CARRY.md` and `carry/F-W4-CARRY.md`** — ⟨cmd⟩ `grep -oE 'carry/[A-Za-z0-9._-]+' F-W2.md | sort -u` returns those two and nothing else that resolves (the third token it prints, `carry/F-W`, is the fragment of the struck probe quoted above). **`F-W2-CARRY` occurs in this file only inside the R-3 minutes that declare the file nonexistent and inside this round's strike notes — never as a path F.W2 cites.**)* (b) Against the real 54-roster the fold-seat spec dropped **12** ids (PASS-1 §2 E-1..E-12), four of them members of the **inbound** P-9 export it claimed to carry whole. Repair round 1 lands all twelve — A6 (×5: L/B-1+C/B-1, MISSED-F, MPC-2, CENSUS-MC, the FourierMorphSvg r2 fold) · A7 (FR-IC-20) · E2 named fold (C·C-1) · D4 (C-18) · D3 (×4: CollapsibleSection, PaperSearch C-Z1/C-Z2, PaperSearchInput C-13/C-14, PaperSearchDropdown C:C-23/C-24) — and §8 was re-authored over the 54. **(c) RED DEEPENED AT REPAIR ROUND 4: the round-2 detector reproduced its number and its blind spot together.** `grep -nE 'F[.·–-]?W2'` **cannot match a slash form whose SECOND member is `W2`**, so `fr-PathPreview`'s two `F.W1/W2` rows were invisible to the operand, to the §8 roster and to the masthead's re-verified figure alike — three statements of one measurement, all wrong in the same direction, for three rounds. ⟨cmd⟩ `grep -noE '[FW][.·–-]?W?[0-9]+[/–-]W2' fr-*.md` → five hits, all `fr-PathPreview.md` (`:50` ×2 · `:80` · `:107` · `:114`). The two routed rows — **`PP-REDGATE`** (`:50`, re-affirmed `:80`) and **roster item 7** (`:107`) — plus **`fr-GalleryInfiniteGrid C-6`** (`:48`, a first-position literal that the detector saw and the roster never booked) are homed at **§7 as exclusions-with-reason** in this round, which G19's own FAIL clause counts as carried. The gate's RED-input cell was self-describing: *drop a row silently*. **Routed is now 57 = 54 booked ⊕ 3 excluded-with-reason; escaped 0. RED stands until the difference is re-run at wave open on the FOUR-AXIS detector against the 66 records, not against any roster, check file or memory of one** | Drop a row silently. **State an operand that cannot match a member written in second position** — the round-2 detector's exact failure, and the reason a gate that reproduces its number is not thereby verified. **This is the defect class the program exists to kill (M-25).** **Cite an authority with no path** — "the CARRY" is forbidden vocabulary from R-3 forward; only `carry/F-W1-CARRY.md` and `carry/F-W4-CARRY.md` are citable carries, and only by path. Excluded-with-reason counts as carried; excluded-without-reason does not. A non-empty difference after two closure passes is a triumvirate halt, not a redispatch |
| **G20** ⭑ | The banked easing cluster is CITED, never re-minted, and its two cure-shape locks are dispositioned without pre-emption | `ls docs/tranches/X/fourier/RULINGS-F.W2.md` records **R-iii** — MPC-2's *"never a specifier rewrite"* and CENSUS-MC's *"NOT blocked on the uplift"* each quoted at its coordinate with its **F.W1** home named, plus the §2a-rows-1–4 collision stated and routed | **RED [measured 2026-08-28]** — `ls docs/tranches/X/fourier/RULINGS-F.W2.md` → *No such file or directory*; the ruling has no record to live in. Origin RED, retained as a **classification, not a self-count (R3-3.10, repair round 4)**: at the fold seat this spec cited **none** of the five easing-cluster records — none of `fr-EasingPicker`, `fr-EasingCurvePreview`, `fr-MorphPhaseConfig`, `fr-FourierMorphSvg` or their ids `L/B-1`, `MISSED-F`, `MPC-2`, `CENSUS-MC`, `ESC-4` appeared anywhere in it. *(The rounds 1–3 receipt `grep -c '…' F-W2.md` → **0** is STRUCK: it is a banked count of the citing file, and A6's own cure made it false — the same edit that discharged the defect falsified the receipt certifying it. This is the structural cause R3-3.10 removes, caught inside the gate that names the anti-rename law.)* A6 cures the citation; only the **recording** clears the gate | Re-mint the easing identity under census/W.L5 ids (the M-25 anti-rename defect). Author ACT(1) with MPC-2's lock unreconciled against G1's 5/5 `ERR_PACKAGE_PATH_NOT_EXPORTED`. **Or overrule either lock here** — both fold into F.W1-homed identities; F.W2 routes, F.W1 rules. Ruling gate: F.W2 records (R-iii); the cure-shape decision is F.W1's |

**Denominator note (L-19).** Every gate names a product consumer, a published artifact, or a live executable probe committed in this repo. **No gate is a `scripts/**/proof-*.mjs`.** The two spine probes were re-run read-only at authoring and both are RED; the remaining eighteen name a file, a line, or a grep. **Runnability (R-10.6) — RESTATED TRUTHFULLY AT REPAIR ROUND 4 (PASS-3 D-3/D-4 · ruling R3-9.1); both halves of the rounds 1–3 sentence were false.** What it claimed: that every command executes as written, and that the four to-be-created paths *"and the four PNGs each carry an explicit **create** marker with its path."* What was true: **two gate commands were `…`-elided and could not run at all** (G4's `sed` and G8's `node -p`), and **the four PNGs carried no create marker anywhere in §2a**, at a path (`audit/probes/app-wave/…`) that does not resolve from the declared cwd. Both are cured in this round's edits, not re-asserted. **What holds now, each verifiable by running it:** every command in this table is literally pathed — **no `…` survives anywhere in this file** (⟨cmd⟩ `grep -oE '…/[a-zA-Z@]' F-W2.md` returns no output, run at the close of this round); the fourier-side commands are absolutely rooted at `/Users/mkbabb/Programming/fourier-analysis/`, which resolves; and the **nine** created paths each carry an explicit **create** marker with a resolving path in §2a — `facility-19.md`, `RULINGS-F.W2.md`, the two letters, and the **four PNGs under `docs/tranches/V/megatranche/audit/probes/app-wave/`** (the directory is live), plus this spec itself. **The elision was never merely an address defect**: A1's completed `exports['.']` receipt returns *the opposite answer* under the other manifest its ellipsis could have named, which is why R3-9.1's cure is completion and not annotation. **Every fourier-side witness is a read** — grep, `ls`, node module read; **no witness writes the fourier tree**, at spec time or after.

---

## 6. Sequencing

### 6a. The locks (carried where their rows land)

- **F.W0 FIRST** (G14) — substrate pre-gates (FR-NP-32/M1 · GAB-13/X-1 · F8-REACH-01/02 · MG-θ) re-ground; every fourier anchor re-resolves (D-19).
- **Atomicity, F.W1 (FR-EQR-3).** ▲ F.W1 is ONE atomic land-or-lose transaction — **the TWELVE-limb roster chartered at F-W1 §4 step 4, incl. the FR-EQC-7 vaul-vue manifest gate INSIDE the transaction per F-W1 §4's cross-edge 1 (F.W0 → F.W1) — cited whole, never restated here.** (R-4b: the fold-seat text restated three limbs and thereby re-fractured the transaction; the restatement is struck.) **Extent corrected at repair round 2 (R2-5): TWELVE, not eleven** — the eleven chartered limbs plus the FR-EQC-7 vaul-vue manifest gate **returned INSIDE the transaction at repair round 1**, whose warrant is the vaul-vue-INSIDE pin at F-W1 §4's cross-edge 1. **F.W2 does not re-derive the count and does not quote the charter's sentence; it cites the charter's extent as F.W1 states it.** The F-W1 edit has landed and this seat re-read it. **THE SIBLING RECEIPTS ARE RESTATED AS CLASSIFICATIONS AT REPAIR ROUND 4 (R3-3.10, the standing amendment to R2-1-LAW): no ⟨cmd⟩ whose output is a COUNT of a live sibling may be banked in a spec.** Rounds 1–3 banked three such counts here (`ELEVEN` → 0 · `TWELVE` → one hit · `vaul-vue` → EIGHT), and the third of them had to be corrected 6→8 in round 3 for having been reported against a four-site enumeration — a frozen count of a moving file is false the moment the file moves, and F-W1 moves every round. The lawful forms, all `grep -o` word-output or set-membership: ⟨cmd⟩ `grep -o 'The roster is ELEVEN' F-W1.md` returns **no output** — the pre-correction wording is **absent** from F-W1; ⟨cmd⟩ `grep -o 'The roster is TWELVE' F-W1.md` returns the phrase, and it occurs **inside F-W1 §4 step 4's atomic-transaction sentence**, which states the extent and names the vaul-vue gate as the twelfth limb per the round-1 pin; ⟨cmd⟩ `grep -o 'vaul-vue' F-W1.md` returns the token, and its SITE SET — the classification that survives F-W1's next edit — is **§2 WU-L's FR-EQC-7 row · §3's `G13` peer-graph-closure gate · §4 step 2 (F.W0's discharges recorded) · §4 step 4 · §4's cross-edge 1 (where the INSIDE pin lives) · §2·R1's placement note · §2·R2's placement note · §E-3·R2's dated errata row (the ELEVEN→TWELVE minute itself)**. A ⟨cmd⟩ note is a receipt for the command's ACTUAL return, not for the return the seat expected — and, from this round, not for an integer that another seat's edit can falsify without touching this file. Coordinates deliberately omitted: F-W1 is a live sibling and a line cite into it is a defect from R2-2 forward. The G5 landing cell remains **conditional, not a thirteenth limb**. **F.W2's value.js-direct specifier arm may not be scheduled before it** (G10) — and F.W2 may NAME an individual limb only where a local gate binds one (G3/G4's ceding, §6b).
- **Credit bar (FR-GIG-5).** F.W1 must not be credited for the pagination-drain cure; **by the same law F.W2 must not be credited for F.W1's** — G3 and G4's GREENs belong to F.W1.
- **The colours exception (fr-App C-2).** The `colors.ts` cure is landable at 0.13.0 today, alone. **This correction binds the colours arm only, NEVER the specifier arm** (A1's dissent line).
- **Same-commit riders.** A2's re-derived mapping travels with A1's five-site edit; A3's `lerp` re-point rides the same commit; the ACT(2) deletion lands only with the B7 join (G5 — **FIVE call sites, three files**, re-sized 4→5 at repair round 2 per R2-4.1, §2b item 6) and the validated residual (G6).
- **One cut, B1⊕B2⊕B3.** **Nine** amber sites land together with the `--contour-stroke` mint (**corrected from "ten" at repair round 2**: B1's `ContourPreview.vue:45` is already one of the nine, so "ten" double-counted it — ⟨cmd⟩ `grep -c 'hsl(40' /Users/mkbabb/Programming/fourier-analysis/web/src/components/visualization/ContourPreview.vue /Users/mkbabb/Programming/fourier-analysis/web/src/components/visualization/ContourEditorCanvas.vue` → **1 + 8 = 9**). B2 is cured **by name** — the 117-LOC deletion sweeps no slash-alpha literal as collateral.
- **NEVER-clause, B11/K-8.** CSS-expressible colour is never routed through the JS resolver this wave is deleting. Ratified as the **HLG-4 remedy hierarchy** (`fr-HarmonicLevelGrid.md:113` — ⟨cmd⟩ `sed -n '113p' fr-HarmonicLevelGrid.md`, reproduced this round, same receipt as B1/B10): *CSS-var passthrough first, `parseCSSColor` only where JS must see colour* — quoted at the bank's spelling; the shipped symbol is `parseCssColor` (B10).
- **One-edit law, C2.** `energyColor`'s cure is bound into M-N11 with D-7/D-9/M-N12 — each partially regresses another if split. **F.W4's, not F.W2's.**
- **Anti-rename law, B5.** ONE ramp identity, never re-booked per surface.
- **Author-before-choose.** A2 before F.W1 opens; B6 before B5's substrate is chosen and before the 2.0.0 answer; unit `.c` (R-i/R-ii/**R-iii** + the G2/G15 mapping) **before** `.b` authors ACT(2), and **R-iii before `.a` authors ACT(1)** — the cure-shape locks are read before the specifier edit is shaped; B10 checks FMD-3's disposition before sizing its two sites. §2 law: F.W1 precedes F.W3/W4.
- **MEASURE-AT-OPEN, D-19.** Every producer figure, export keyset, and count in this spec re-measures at F.W0/F.W2 open (SR-1/K-8, third occurrence); §2b is the pre-discharge.

### 6b. Cross-edges, declared from this end

| edge | direction | this end's declaration |
|---|---|---|
| **F.W0** substrate pre-gates | **DEPENDS — hard** | Not a gate F.W2 closes. ⟨**R-9.1, one line, owed by every later wave**: F.W0's **§4 gate `G-11`** ONE corrected anchor table and **§4 gate `G-12`** ONE corrected-denominator table, landing in `fourier/docs/tranches/F/SUBSTRATE-LEDGER.md` (**F-W0 §2a's file-bounds `SUBSTRATE-LEDGER.md` create row**, which names all three tables as that file's contents), are the **single owned authority**; **§2b and every figure in this spec are QUOTATIONS of / inputs to them, never rival re-derivations**, and superseded figures are forbidden here as there. **F-W0 §4 gate `G-13`**'s pin table governs every producer pin by **COMMIT HASH, never a version string** (P-4). **All four addresses converted to the stable idiom at repair round 2 (R2-2)** — the fold seat's `:199`/`:204`/`:209`/`:63` had drifted onto a blank line, an "Owning rows" cell, a second "Owning rows" cell and the wrong bounds row; ⟨cmd⟩ `grep -n '^### G-1[0-9]' F-W0.md` and `grep -n 'SUBSTRATE-LEDGER' F-W0.md` re-run this round confirm the gate headings and the create row are the citable identities, and headings do not drift⟩ Corrupt glass-ui `dist/styles/index.css` (**FR-NP-32 ≡ fr-PaperSidebar M1**; banked, **at the bank's own bytes** — *"NO-WAVE-OWNER (producer dist emitter) + glass-ui BH relay at the TOP of the FR-COB-28 packet; F.W1 SEQUENCING GATE"* (**conformed at repair round 3**: rounds 1–2 rendered this cell with `+` where the bank writes `;`, dropped *glass-ui*, and lower-cased *TOP*, while keeping the word *banked* — a paraphrase under a banked label is R2-1-LAW.2's defect per se, whatever the words say) at `fr-NotationPills.md:35` ⟨cmd⟩ `sed -n '35p' fr-NotationPills.md`, **re-homed at F-W0 §3's carry row 1 (`FR-NP-32 ≡ fr-PaperSidebar M1`), gate `G-4`** — F.W2 cites that re-home, R-8) → `vite dev`/`vite build` RED at the pin, so **G3/G4/G12 cannot execute**; the 28-entry dirty worktree and the unresolvable `14d83356` (GAB-13 / F-W5 X-1) — **both re-measured RED this seat**; F8-REACH-01/02 stand. **G14 records the fold seat's parser corroboration of the CSS corruption (prose tail, chars 12396–13833, `css_syntax`) — F.W0 re-grounds with the app's own toolchain; the producer-side notice rides the GLASS-RELAY.** Also inbound, booked F.W0/F.W2 jointly: fr-App MG-θ (no lint config) and E1's backward-pin fact |
| **F.W1** atomic tri-package uplift | **DEPENDS ON — and CEDES credit to** | ▲ **F.W1 is ONE atomic land-or-lose transaction — the TWELVE-limb roster chartered at F-W1 §4 step 4, incl. the FR-EQC-7 vaul-vue manifest gate INSIDE the transaction per F-W1 §4's cross-edge 1 (F.W0 → F.W1) — cited whole, never restated here** (R-4b; extent corrected ELEVEN→**TWELVE** at repair round 2 per R2-5, the twelfth limb being the vaul-vue gate returned INSIDE at repair round 1 — **F.W2 quotes no charter sentence and re-derives no count; it cites the charter**). Two limbs the fold-seat edge omitted are therefore no longer omitted: **the FR-EQC-7 vaul-vue manifest gate** (inside the transaction, not before it — F-W1 §4's cross-edge 1) and **the RE-PIN at the ADOPTED producer COMMIT HASH** (F.W0's pin table, **F-W0 §4 gate `G-13`**; **P-4: hash, never a version string**) — B6's "Re-pin at wave open (SR-1/K-8)" is F.W2's consumption of that pin, never a second act. F.W1 owns and keeps: the banked palette identity (fr-BasisCanvas D-1/BC-1/C-1 via fr-App B-1) · the `basisDisplay` fix (D-2/BC-5/C-3) · C-4's resolver re-seat to `main.ts` · M-β5's hex-validation gate · B-2's P0 CSS-class census · MG-β's `text-admin-label` census instance and its AA-15 collision · **the whole easing cluster of A6 — `fr-EasingPicker L/B-1 + C/B-1` (**F-W1 §2 WU-K's `L/B-1 + C/B-1` row**), its acceptance decision **ESC-4 (F-W1 §2 WU-R's `ESC-4` row)**, the `as EasingFn` execution gate **G12 (F-W1 §3)**, MISSED-F's costing correction, and the MPC-2 / CENSUS-MC cure-shape locks routed here by R-iii**. **F.W2 cites all of these and claims none of their GREEN** (FR-GIG-5). The value.js-direct specifier arm is un-landable before it (G10); the colours arm is not |
| **F.W3/W4** shadow retirement + frontend audit | **HANDS OFF — one home, two citations** | ⟨**§X.1-v4 CONSUMED AT REPAIR ROUND 4, read fresh at F-W3's landed bytes.** The twin partition is now definitive and F.W2's *"F.W3/W4"* hand-offs resolve against it rather than naming an unsplit pair. The criterion is a question of a PATH — *is the record's target file in a §5 unit file list?* — and it settles the three places this spec hands to the twins. **(1) `fr-CoefficientsSpectrum` ENTERS the partition** (§X.1-v4 item 3): `shared/CoefficientsSpectrum.vue` is a member of §5d's Files list, so the record homes at **F.W3 `.d`**, with `m-19`/`m-22` as its residue ids. That is B5's fourth `spectrumColor` declaration (`shared/CoefficientsSpectrum.vue:47`) — **the fork collapse lands at F.W3 `.d`, not at an undetermined twin**. **(2) `fr-ContourEditorCanvas` takes the DEFAULT arm to F.W4** (§X.1-v4 item 2: its file is in no unit list; the receive-set names `MM-1 · MM-2 · MM-3 · MM-4 · MM-5 · MM-7 · MM-8 · MM-9 · MM-12`) — so B3's eight `ContourEditorCanvas.vue` amber sites sit in a file whose *residue rows* are F.W4's. **They do not collide**: B3's nine sites are `fr-ContourPreview` row 22's, F.W2 cuts them as ONE cut, and none of the nine is an `MM-*` id. F.W2 states the adjacency so F.W4 is not surprised by a file it holds rows in having been edited. **(3) `M-β1` is named in §X.1-v4 item 2's fourth line — `BC-20 · M-β1 · M-β3`, *cited at F.W3, booked nowhere***; v4's own probe reports its two homes as this file and F-W3, and F-W3's is a record-qualified citation. **B5 is therefore the identity's only booking, and it books the substrate ANGLE only** — the execution half still owes a named holder, which is F.W4's to take under R3-1b or F.W3's to take with the `.d` unit. F.W2 neither pre-empts nor re-books; **anti-rename holds — ONE ramp identity, `M-β1`, original for life.**⟩ Execution halves of rows F.W2 only locks: **B5/FR-CP-2's ramp collapse** (F.W2 owns the substrate re-premise to `oklch`/`mixColors`; the twins land it, per §X.1-v4 above) · **C1's `actionTone` cure** · **C2's `energyColor` cure** with its 3-consumer/2-route regression surface, bound into M-N11 · **B11/FR-NP-1's collapsed clause** (`var(--viz-*)` into the `color-mix`, never through `VIZ_COLORS`/`cssVarToHex`) · **B7's consumer-side render fix** and **fr-ConvergencePlot D-1's F.W4 palette item** (cross-referenced, **never double-cured**) · **B10's two call sites** — moot if FMD-3's dead channel deletes first, so F.W2 checks that disposition before sizing them |
| **F.W5 (SS-4)** shared-provenance contract | **BIDIRECTIONAL** | **INBOUND**: **F-W5 §4's `F.W5 → F.W2` edge row** (the export row, whose act cell reads *"exports zero-cells (P-9)"*) already exports the P-9 zero-cells to F.W2's denominator, with **F-W5 §5's `P-9` row** (the carried-by-citation set) as P-9's home row (*"one home, two citations"*; home F.W4 body + F.W2 copy) — **ADDRESSES RETIRED, NOT RE-KEYED, at repair round 2 (R2-2/R2-4.3).** The repair-round-1 note this cell used to carry (*"the fold seat cited one F.W5 coordinate twice, which is F.W5's gate row G15, image bounds on write"*) was itself a line cite into a live sibling and has **twice** gone stale in two rounds — the round-0 coordinate drifted onto F.W5's G5 born-visibility row, then round 1's two replacements drifted off the export and P-9 rows in their turn. The erratum survives as its LAW, not its coordinates: *the fold seat cited an unrelated F.W5 gate row as the export; the export is the `F.W5 → F.W2` edge row and P-9's home is the §5 `P-9` row, and a row label cannot drift.* ⟨cmd⟩ `grep -o 'zero-cells' F-W5.md` returns the token, and the rows carrying it are the two named above — **the `F.W5 → F.W2` edge row (§4) and the `P-9` row (§5)**; F-W5 §2c spells the singular *"zero-cell export"*, which this detector does not match and which is therefore not a third row. **THE RECEIPT IS A SET-MEMBERSHIP CLAIM FROM REPAIR ROUND 4 (R3-3.10)**, not a count: rounds 1–2 said three hits and round 3 said two, so one command was reported two ways inside one file, and the round-3 fix replaced a wrong integer with a right one instead of retiring the form. A banked count of a live sibling is falsified by that sibling's next edit, whoever writes it. **F.W2 is a citing end and re-books none of them.** D3 carries the roster whole — **the export's TEN members, none dropped, plus SIX zero-cells F.W2 measured itself** (the sixteen-vs-ten attribution corrected at repair round 4, PASS-3 D-7; D3 states which six are F.W2's own) — the five the fold seat dropped restored by id (D3, D4), and the two PRESERVATION clauses (MorphShapePreview's CSS dodge, quoted at its own bytes since repair round 4 — *"…— carried. Preserve verbatim through F.W2."*; FunctionInput's target posture, unregressed). **OUTBOUND**: FN-1/2/3/4/6's substance is F.W5's/F.W6's — **F.W2 owns only the ANSWER and the ledger.** **FN-5 is chained to the TA-4 value-side atomdiff restoration = SS-4's OWNER-GATED G4** — **F.W2 names the gate and does not pre-empt the ruling**; FN-5's rider ("authored BEFORE or WITH fourier M.W10") travels. **FN-7 is F.W5's, both-ends.** Owner rulings SS-4 flags inline (trie-vs-KISS et al.) are **not re-litigated here** |
| **X·V / V·π** parser-seam program | **F.W2 DECLARES; value.js OWES the reciprocal** | Packet P-7's reciprocal, per COHESION §2's both-ends law: **(a)** `color2` rejects its own sibling parser's output and the unwrap silently returns wrong channels; **(b)** `colorUnit2`/`color2` are **both deleted at 4.0.0** — the banked constraint is a 0.13.0-window rule; the 4.0.0 remedy is `parseCssColor` + `convertColor`; **(c)** `parseCssColor` on `oklch()`/`light-dark()` is probed at 0.13.0 **only** (G9); **(d)** the G18 reciprocal, reshaped at the fold seat — `parseStylesheet` is **total on the string arm** (returns `ok: false` + a `css_syntax` diagnostic on the corrupt glass dist, corroborating FR-NP-32) but **throws on Buffer input** (`e.startsWith is not a function`), the surviving totality nit for X-W9; **(e) direct parse-that→fourier is FORBIDDEN** — the seam travels through the parser-seam program. Owner-mail flag stands |
| **X-W9 / X-W9.f · W.L6** the 4.1 cut | **DEPENDS ON / DECLARES** | CC-091 folds W.L5's value half → X-W9.f and the relay → X-W9.i, with **fourier-tree edits explicitly NOT X-W9 gates**. **X-W9 §Hard Gate's `G24` is the drift gate (banked max\|Δ\| = 0.192, RED, MEASURE-AT-OPEN); G15 is its consumer-side twin — this seat re-measured both legs live.** (Cited by gate id from repair round 2; W9.md is a live sibling and its line coordinates are not this spec's to quote — R2-2.2.) **RD-7 RULED the colour arm ORDER-INDEPENDENT of the 4.1 cut.** The 3-line hex residual's **deletion** lands at fourier's 4.1 adoption (W.L6), not here. **X-W9 §Hard Gate's `G21` / SCI-1 re-ships `sampleColorRamp` at 4.1** — B6's strike wording must say so. **X-W9 §Hard Gate's `G33`** counts the 5 packets + 5 INBOX rows + `npm ls @mkbabb/value.js` in keyframes AND fourier post-window (gate ids, not lines — R2-2.2) |
| **GLASS-RELAY (SS-6)** | **DECLARES OUTBOUND — producer rows NEVER become frontend hacks** | Three asks: **(1)** B9/R-ii's `resolveTokenColor`/`useTokenColor` adoption question at `@mkbabb/glass-ui/dom` — sanctioned consumer seat? and what does `getComputedStyle` serialize for an `oklch()` token (drop-in vs format adapter → SS-13); **(2)** the `--viz-amber` carry stays **ONE** carry at `lane-frontend:604`, gaining the second denominator from fr-ConvergenceLegend D-L11/C-4 (amber 4.56:1 page-only, **3.17:1** over the golden sum, **2.53:1** over a hue-0 curve) plus B10's stale D.W4.d comment finding — seat-corroborated: the live probe attributes amber's survival to fourier's own `style.css:120` override; **(3)** MG-β's `text-admin-label` — a real `@utility` at 4.0.0, **gone from producer 7.0.0's 25-name roster**, surviving only as a `class-names.ts:84` regex-allowlist entry, on a collision course with banked AA-15's cure |
| **SS-13** the visual audit | **DECLARES OUTBOUND — UNPROVEN-NEEDS-LIVE** | Five inputs: **(1)** G12's π probe pair, light and dark, force-added past `.gitignore:34 *.png`; **(2)** *"the divergence, not the greyness, is the diagnostic"* — confirm the chrome paints correct brand hues **in the same frame** as the grey instrument; **(3)** the R-5 cascade expectation on every readback (`oklch(` dark, `light-dark(` light); **(4)** the entry-route-pinned snapshot witness (G4) — the split-brain does **not** reproduce on a clean `/paper`-first visit; **(5)** B9's `getComputedStyle` serialization question. Also: B5's final ratios + dark arm. **Probe parsimony is binding** (owner edict) |

---

## 7. Excluded

Named, with reasons. Nothing below is dropped; each has a stated home or a stated refutation. Excluded-with-reason counts as carried (G19).

| item | reason |
|---|---|
| **The palette-resolver cure** (fr-BasisCanvas D-1/BC-1/C-1 via fr-App B-1) and **the `basisDisplay` snapshot fix** (D-2/BC-5/C-3) | **F.W1 owns both.** F.W2 owns G3's deletion half and G4's green-criterion only (FR-GIG-5's bar) |
| **`actionTone`, `energyColor`, the NotationPills triad, the `spectrumColor` ramp, ImageUpload's `STATIC.rainbow`** — as **cures** | **F.W3/W4.** F.W2 carries them as **charter-widening enumeration only** (G7/R-i) and as B5's substrate re-premise. One home, two citations |
| **`fourier-analysis/web/package.json`**, `App.vue:11,:13-17`, `main.ts` | F.W1's atomic transaction. Editing any of them here breaks atomicity (FR-EQR-3) |
| **Any `value.js` `src/**` edit** — including restoring `sampleColorRamp`, `mixColorsN`, `easingNames()`, memoised `easing()`, or the analytic in/out arms | **X-W9 / W.L6 (the 4.1 cut).** RD-7 ruled the colour arm order-independent of it; F.W2 consumes the 4.0.0 surface as published and declares the drift (G15) rather than curing the producer |
| **Any glass-ui producer edit**, including adopting `resolveTokenColor` into the producer or restoring `text-admin-label` | **GLASS-RELAY / SS-6 only.** Producer rows never become frontend hacks (owner edict) |
| **A keyframes-side analogue of any of this** | **RD-11**: the D-15 grant has no keyframes analogue; the keyframes pin moves inside F.W1's transaction |
| **FN-1, FN-2, FN-3, FN-4, FN-6 on the merits** | API-shaped → **F.W5/F.W6.** F.W2 writes the answer and the ledger, not the substance (F1) |
| **FN-5's substance** | Chained to the TA-4 value-side atomdiff restoration = **SS-4's OWNER-GATED G4.** F.W2 **names the gate and does not pre-empt the ruling**; the "before or with fourier M.W10" rider travels |
| **FN-7** (neutral home for `J-diff-shape.md`) | **F.W5's, both-ends** |
| **Extending `API-FACILITY-ISOMORPHISM.json`'s schema to facility 19** | **RD-8 RULED a different, one-way law.** O's bidirectional argument preserved as dissent; the label is load-bearing |
| **Direct parse-that → fourier routing** | **FORBIDDEN EDGE, declared.** Anything parser-shaped routes through the parser-seam program |
| **`parseStylesheet` on the glass-ui dist stylesheet** (G18) | Re-probed at the fold seat: **no throw on string input** — `ok: false` + one `css_syntax` diagnostic pinning the stylesheet's prose tail (chars 12396–13833), which **corroborates FR-NP-32** (F.W0's pre-gate, and a GLASS-RELAY input). The surviving value.js totality nit (Buffer input → `TypeError`) files to **X-W9**. Claimed nowhere |
| **The "specifier-grep UNDERCOUNTS" escalation** | **KILLED by K-7** — ConvergenceLegend's only mount is already in the five-site inventory |
| **The "one import edge" ramp cure** | **KILLED (FR-CP-2-c / K13)** — it *is* the colocation inversion. The honest cure is a new route-neutral ramp home |
| **Derive-the-ramp-from-`VIZ_COLORS`** | **Structurally impossible (FR-CP-2-c / K4)** — five categorical entries, not a ramp |
| **Tokenisation as the ramp cure** | **Not the cure (RD-5-i)** — lightness must co-vary. Adopted as law for the identity |
| **The sixteen measured zero-cells as defects or as denominator** | **Cited, never re-booked** (D3 — the roster was eleven at the fold seat and is **sixteen** here: the export's **TEN**, none dropped, ⊕ **SIX F.W2 measured itself**. **Corrected at repair round 4, PASS-3 D-7** — rounds 1–3 read *"sixteen against the real export"*, and the export names ten). Home of the ten is F.W5's export row — **F-W5 §4's `F.W5 → F.W2` edge row**, with P-9's own home at **F-W5 §5's `P-9` row** (stable anchors from repair round 2, R2-2); F.W2 is a citing end. The one exception is **fr-AnimationControls C-18**, which is *also* booked as F.W2's own denominator cell at **D4** — exactly as C-25 is booked at D1: the export names both, and the denominator cell is a citation of the zero, never a re-booking of the record |
| **`fr-AnimationControls C-17` and `C-30`** | Same record as D4's C-18, **different homes**: C-17 (hand-rolled `linear`) → **F.W4**, cited at A2 as the third identity of the silent-linear hazard; C-30 (fold of intake R6-8) → identity stays with R6-8's **F.W5–W8** carry, adopted at **F.W5**. F.W2 books neither |
| **`fr-EasingPicker L30` ("Producer-arrival severity", `:30`)** | **F.W3's** formation-input row — the producer arrival re-homes an entire F.W3 disposition and converts `EasingCurvePreview.vue` from a re-style into a deletion. A6 opens the `fr-EasingCurvePreview` record only for **CENSUS-MC**; the severity row is not F.W2's and is not carried here |
| **The EBADPEER reproduction** | Folds to the census risk-1 family, banked ×3; re-booking it is the defect class |
| **The "`colorUnit2` yes, `color2` never" constraint as a post-uplift rule** | **Excluded by measurement (E2/G9)** — both symbols deleted at 4.0.0. Carried as a 0.13.0-window record with its receipt; the `light-dark()` probe-element rule is the one clause that transfers |
| **The "forever" / "two defects cancelling" absolutization of FR-GFC-8** | **Killed again by K4/R-3 + GCM K-9.** Carried as an amendment note, not a row |
| **C·C-30 and C·C-31 as fresh rows** | **Folded** — C·C-30 → A3 (SC-4); C·C-31's grade ruled down to INFO by K-6 (A4) |
| **The M-R5 "new find" filings** | Already banked at the ramp identity (B5); recorded as fleet evidence only |
| **ImageUpload row 13's theming arm** | **KILLED (ruling 6)**; only the census obligation survives |
| **Reproducing the FR-GFC-8 split-brain on a default `/paper`-first visit** | **Excluded as a witness by fr-App R-1** — frozen and live sides AGREE there. Any recipe expecting it is wrong |
| **Closing G14** | **F.W0's, hard.** F.W2 states it as opens-after and claims none of its GREEN |
| **`fr-PathPreview PP-REDGATE`** (roster item 15, L-2 as re-adjudicated) — **the first of three rows homed at repair round 4** (PASS-3 D-1 · ruling R3-5.1) | **F.W1 OWNS IT, INSIDE THE ATOMIC TRANSACTION.** ⟨cmd⟩ `sed -n '50p' fr-PathPreview.md`: *"**MAJOR → F.W1/W2 + M-25 errata**. … The cure (ambient `declare module "@mkbabb/latex-paper/theme"` in env.d.ts, or a latex-paper types fix) lands WITH the toolchain bump, owned by F.W1/W2 — NOT as an F.W0 precondition."*; re-affirmed at `:80` (*"The fact survives as PP-REDGATE, re-routed F.W1/W2."*). The slash form makes F.W2 a member — **and F.W1 already books the cure as a limb of the TWELVE**: ⟨cmd⟩ `grep -o 'PP-REDGATE ambient declaration' F-W1.md` returns that phrase, and it occurs inside **F-W1 §4 step 4's atomic-transaction sentence**, the very sentence §6a and §6b cite twice as this wave's extent authority. **F.W2 quoted the charter containing the id and did not notice the id.** The toolchain bump IS F.W1's transaction (FR-EQR-3), so a second home here would fracture it; excluded with reason, carried under G19 |
| **`fr-PathPreview` roster item 7** (the committed substrate's gate under its own toolchain) — the second | **F.W1's, same transaction, same reason.** ⟨cmd⟩ `sed -n '107p' fr-PathPreview.md`: *"no seat has run vue-tsc 2.2.12 against the committed lock (install = a write) … F.W1/W2 should confirm at execution."* The record's own adjudication arithmetic proves the pair is **two rows, not one** — ⟨cmd⟩ `sed -n '114p' fr-PathPreview.md`: *"29 rows routed (F.W0 ×4 · F.W1/W2 ×2 · F.W3/W4 ×15 …)"* — the emphasis stays OUTSIDE the quotation marks: **the tally names two rows on this route**. **`install = a write`** puts the confirmation on the far side of the begin-word and inside the wave that performs the install; F.W2's own witnesses are reads (§5) and could not run it without breaking that law. Excluded with reason; the confirmation rides F.W1's execution |
| **`fr-GalleryInfiniteGrid C-6`** — the third (PASS-3 D-9 · ruling R3-2.5) | **Folds by reference to the F.W1-homed identity; rides B4's wave-lock; no F.W2 booking.** ⟨cmd⟩ `sed -n '48p' fr-GalleryInfiniteGrid.md`: *"C-6 — MAJOR here · FOLDS BY REFERENCE to banked fr-BasisCanvas D-1/BC-1/C-1 via fr-App B-1 + GCM-20 (→ F.W1). Do NOT re-book."* … *"Rides banked FR-GFC-8's F.W2 wave-lock."* **This one was never a detector miss** — it is a first-position literal `F.W2` in a record F.W2 already opens twice (C-5 at §8a row 30, FR-GIG-17 at row 31). Row 1 of this table excluded the *identity* (fr-BasisCanvas D-1/BC-1/C-1) and never this **id**, and under G19 excluded-without-reason is not carried. The wave-lock it rides is **B4**, which F.W2 owns; the cure it folds into is F.W1's, which F.W2 does not. Both halves already have homes, so the row needs a reason and not a booking — this is it |

---

## 8. Census-closure roster (G19's instrument) — RE-BASED, repair round 1; OPERAND AND DETECTOR RE-CUT, repair round 4

**Instrument (R-3, as re-based at repair round 4 under rulings R3-4 + R3-5.1)**: **the 66 `fr-*.md` records at `docs/tranches/V/megatranche/registry/adjudicated/` READ DIRECTLY**, through G19's inline **four-axis** detector, ⊎ the non-literal `library-band.md` §W.L5 ACT1/ACT2/ACT3 routings ⊎ the declared inbound P-9 export at **F-W5 §4's `F.W5 → F.W2` edge row**. **The PASS-1 roster is named as a PRIOR RUN, never as the operand** — rounds 1–3 keyed this section to `PASS-1/F-W2-CHECK.md` §1–§2's **54 ids**, and R3-4's standing law rules that a closure whose LHS is a count copied from a check file **fails by construction**: it can only re-find that seat's misses. It did. **The detector's spellings arm, re-measured this round at the frozen corpus (where counts are lawful, R2-2.2(i)): first-position literal `grep -lE 'F[.·–-]?W2' fr-*.md` → 25 records; second-position `grep -loE '[FW][.·–-]?W?[0-9]+[/–-]W2' fr-*.md` → `fr-PathPreview.md`, form `F.W1/W2`; union 26 records.** The en-dash arm stays a measured ∅ and stays in the probe so it can never silently stop being ∅. **Routed = 57: 54 booked at §8a ⊕ 3 excluded-with-reason at §7, enumerated at §8c. Escaped 0.** **The former instrument — "the 26 CARRY row ids" — is struck: no `F-W2-CARRY.md` exists in tree** (`ls docs/tranches/X/fourier/carry/` → `F-W1-CARRY.md`, `F-W4-CARRY.md`), so it named no resolvable set. Banked ids below are **original for life**; short tokens are record-qualified. Set-difference re-run at wave open, not asserted from memory.

### 8a. The 54 BOOKED ids (of 57 routed; the other three at §8c)

| # | banked id (record-qualified) | lands at |
|---|---|---|
| 1 | `fr-AdminAuditLog AA-20` (C·D-4 / L-14) | C1 · G7 · R-i |
| 2 | `fr-AdminUserList FR-AUL-55` | A1 · D3 |
| 3 | `fr-App C-2` (R-6) | A1 dissent · G10 · §6a |
| 4 | `fr-CanvasControlsDock C-25` | D1 · G11 · D3 |
| 5 | `fr-CoefficientsSpectrum B-3` (D:B-3/L-B1/C-1) | B5 |
| 6 | `fr-CoefficientsSpectrum M-10` (L-M4/D-m11/C-2) | B5 ⟨provenance⟩ |
| 7 | `fr-ContourPreview row 1` (D:B-1 · C:D-1(a) · L:L-9) | B1 |
| 8 | `fr-ContourPreview row 14` (C:D-1(b)) | B2 |
| 9 | `fr-ContourPreview row 22` (D:m-8 · L:L-9) | B3 |
| 10 | `fr-ConvergencePlot RD-5` | B5 |
| 11 | `fr-ConvergencePlot D-1` | B7 · §6b · §7 (cross-ref only; cure F.W4) |
| 12 | `fr-ConvergencePlot L-M6+C-1` | A1 |
| 13 | `fr-ConvergencePlot C-19` | A3 · §2b item 7 |
| 14 | `fr-ConvergencePlot C-22` | A4 |
| 15 | `fr-ConvergencePlot K-11` | B6 |
| 16 | `fr-ConvergenceTimeline S-9` (C·S-3) | D3 |
| 17 | `fr-ConvergenceLegend C-2` | B7 · G5 |
| 18 | `fr-ConvergenceLegend C-15` | B5 · B6 |
| 19 | `fr-ConvergenceLegend C-1 + U-2` | A1 |
| 20 | `fr-ConvergenceLegend K-7` | A1 · §7 |
| 21 | `fr-EquationPanel D-3+C-5` | C2 |
| 22 | `fr-EquationModeToggle FR-EMT-24` | D3 |
| 23 | `fr-EquationView SC-4` | A3 |
| 24 | `fr-EquationView C·D-11` | E1 · G10 |
| 25 | `fr-EquationView M-RTC` | B9 · G8 · R-ii |
| 26 | `fr-EquationView I-5` (C·C-31) | A4 · §7 |
| 27 | `fr-EquationView K-5` | A1 |
| 28 | `fr-EquationView K-6` | A4 |
| 29 | `fr-FunctionInput C·S-1/S-2 item 6` | D3 (preservation) |
| 30 | `fr-GalleryInfiniteGrid C-5` | B4 · G4 |
| 31 | `fr-GalleryInfiniteGrid FR-GIG-17` (= C-18 **of that record** — distinct from row 49) | A5 |
| 32 | `fr-GalleryFeaturedCarousel FR-GFC-8` (= C-5 R-3) | B4 · G4 |
| 33 | `fr-ImageUpload row 13` (C:C-11) | B12 · G11 |
| 34 | `fr-HarmonicLevelGrid HLG-4` (D:D-4 ⊕ C:C-2) | B10 |
| 35 | `fr-HarmonicLevelGrid superlative 3` (D:S-2 ⊕ C:S-1) | B1 · B10 · §6a |
| 36 | `fr-MorphShapePreview C·S-2` | D3 (preservation, verbatim) |
| 37 | `fr-MobileFloatingToc item 9` (method) | D3 |
| 38 | `fr-NotationPills FR-NP-9` (C·D-4 ∘ L·L-4) | B11 · G11 |
| 39 | `fr-PaperView C-08` | D3 |
| 40 | `fr-PaperArticleWindow K-24` | D2 · §2b |
| 41 | `fr-UserSlugBar FR-USB-38` | D3 |
| 42 | `fr-Tooltip FR-TT-22` (= C-10) | D3 |
| 43 | `fr-EasingPicker L/B-1 + C/B-1` ⟳ | **A6** (cite; home **F.W1** **F-W1 §2 WU-K's `L/B-1 + C/B-1` row**) · A1 · A2 · G15 · G20 |
| 44 | `fr-EasingPicker MISSED-F` ⟳ | **A6** (cite; home **F.W1** costing + **F.W4** hygiene) · A1's site-vs-cost correction |
| 45 | `fr-EasingCurvePreview CENSUS-MC` (C-D-5) ⟳ | **A6** · **G10** premise narrowed · **R-iii** · G20 |
| 46 | `fr-MorphPhaseConfig MPC-2` (C-2 ⊕ L-D16) ⟳ | **A6** (lock carried verbatim) · **R-iii** (routed to F.W1) · G20 |
| 47 | `fr-FourierMorphSvg r2 easings/value.js fold` ⟳ | **A6** · A2 Unwrap lock (cite; gated at **F-W1 §3 gate `G12`**) |
| 48 | `fr-InfoCard FR-IC-20` ⟳ | **A7** |
| 49 | `fr-AnimationControls C-18` ⟳ | **D4** · D3 · G11 |
| 50 | `fr-GalleryView superlative 5` (C·C-1) ⟳ | **E2 named fold** · B4 provenance · G9 |
| 51 | `fr-CollapsibleSection` zero-cell (record-level; no id minted in the record) ⟳ | **D3** |
| 52 | `fr-PaperSearch C-Z1/C-Z2` (⊕ PSM-36/S-11) ⟳ | **D3** |
| 53 | `fr-PaperSearchInput C-13/C-14` ⟳ | **D3** |
| 54 | `fr-PaperSearchDropdown C:C-23 / C:C-24 / L:§5` ⟳ | **D3** |

**⟳ = landed at repair round 1** (the twelve PASS-1 §2 escapes, E-1..E-12). Rows 11, 30, 32, 43, 44, 45, 46, 47 are carried as **citations or locks with credit ceded to their adopting wave** — correct under FR-GIG-5 and R-5's one-home law; none is a second booking.

### 8b. The wave's own §3 row ledger (29 rows — index, no gate weight)

A1 · A2 · A3 · A4 · A5 · **A6** · **A7** ∥ B1 · B2 · B3 · B4 · B5 · B6 · B7 · B8 · B9 · B10 · B11 · B12 · B13 ∥ C1 · C2 ∥ D1 · D2 · D3 · **D4** ∥ E1 · E2 ∥ F1. *(Historical note, no gate weight: the fold seat's ledger held 26 rows and reported "26 of 26 carried" against an authority with no path. That receipt is superseded by §8a.)*

### 8c. The 3 routed ids carried by EXCLUSION-WITH-REASON (repair round 4 — rulings R3-5.1 + R3-2.5; PASS-3 D-1/D-9)

Excluded-with-reason counts as carried and excluded-without-reason does not — G19's own clause. These three complete the routed set at **57**; each has its §7 row, its corpus receipt and its named home.

| # | banked id (record-qualified) | how the detector reaches it | §7 row / home |
|---|---|---|---|
| 55 | `fr-PathPreview PP-REDGATE` (roster item 15, L-2 re-adjudicated) | **second-position** `F.W1/W2` (`:50`, re-affirmed `:80`) — invisible to the first-position-only probe rounds 1–3 ran | §7 · **F.W1**, inside the atomic transaction; F-W1 §4 step 4 books *"PP-REDGATE ambient declaration"* as a limb of the TWELVE |
| 56 | `fr-PathPreview` roster item 7 (the committed substrate's gate under its own toolchain) | **second-position** `F.W1/W2` (`:107`); the record's own tally `:114` reads *"F.W1/W2 ×2"* — the arithmetic proof the pair is two rows | §7 · **F.W1** at execution (*"install = a write"*; F.W2's witnesses are all reads) |
| 57 | `fr-GalleryInfiniteGrid C-6` | **first-position literal** `F.W2` (`:48`) — seen by every round's detector, booked by none of them; a booking miss, not a detector miss | §7 · folds by reference to **F.W1**'s D-1/BC-1/C-1 identity; rides **B4**'s wave-lock |

**The two classes are distinguished on purpose.** Rows 55–56 escaped because the operand could not express them; row 57 escaped although the operand found it. **Widening a detector cures the first class and cannot touch the second** — which is why G19's closure is a set-difference re-run id-for-id at wave open and not a probe whose output is read as a roster.

---

⭑ = minted by this wave (G16–G20; G15's ⭑ **struck** at repair round 1 — its figures are banked, R-1d). **57 of 57 routed ids carried — 54 BOOKED (42 at authoring + 12 at repair round 1) ⊕ 3 EXCLUDED-WITH-REASON (repair round 4, §7 + §8c); 20 gates, all born RED; 8 of 8 cross-edges declared; 8 stale anchors corrected (§2b) and contributed to F.W0's G-11 table; zero silent drops (G19, re-run at wave open on the four-axis detector against the 66 records).**

*Format/lint cadence (docs-only at spec time): `git diff --check` + the repo's doc checks at each commit. Commit plan: pathspec-only commits, `docs(X·F)` scope — spec · rulings · facility-19 + INBOX row + letters · close. Depends on: F.W0, F.W1 (specifier arm), the begin-word. Blocks: F.W3/W4 (colour-arm locks), the facility-19-consuming halves of F.W5/F.W6, X-W9.f's relay reading.*
