# F.W11 — Owner Audit Docket, F-side (rounded · glass-idiomatic · app-wide grid · a dock)

## State
**Opens after**: F.W10S CLOSED (honest-RED). **Minted by**: COHESION §0ao (2026-09-22, the owner's
live audit, verbatim there). **Agents**: 4 units — [`.a` ∥ `.b`] → [`.c`] → [`.d`]. **Status**: planned.
**Record**: `docs/tranches/X/execution/C/F-W11.md`. **Tree**: `/Users/mkbabb/Programming/fourier-analysis`,
branch `m/w1-bump-migration`, baseline `735319e`. Every write here is post begin-word (§0j).

## Goal criterion
No squared edge survives in `web/src`; every radius reads a glass-ui token or a `rounded-*`
utility; the visualizer's chrome is built from glass-ui surfaces; the grid is the APP's background
under every route; the app's chrome is a `GlassDock` from `@mkbabb/glass-ui/dock`, carrying the
header's controls with their accessible names intact.

## §1 Bounds
| Path | Access |
|---|---|
| `web/src/**` (`.vue` · `.ts` · `.css`) | modify — root cures only |
| `web/src/components/layout/AppDock.vue` | create (`.c`) |
| `web/e2e/shell-header.spec.ts` · `web/e2e/visualization-ux.spec.ts` | modify ONLY in the same commit as the markup they measure, accessible names preserved; never `skip`/`fixme`/a loosened assertion |
| `web/e2e/screenshots/**` (evidence) | create |
| value.js `docs/tranches/X/execution/C/F-W11.md` · `LEDGER.md` · `docs/tranches/V/coordination/INBOX.md` (mail rows) | modify/create |
**Do NOT touch**: `glass-ui/**` (READ-ONLY always; gaps ride mail) · `web/package.json` deps · `fr-*.md` · `scripts/dev/dev.sh` · every other `web/e2e/*.spec.ts`.

## §2 Agent Units
### F.W11.a Radii and surfaces (Opus)
Abrogate every squared edge: the six literal sites (⟨cmd⟩ `grep -rEn 'border-radius:\s*0[^.]|rounded-none' web/src` → `PaperView.vue:623` · `PaperSearchDropdown.vue:217` · `FullscreenViewer.vue:179/:196` · `PaperSearchInput.vue:219` + the one `rounded-none`) and every literal-px radius (⟨cmd⟩ `grep -rEn 'border-radius:\s*[0-9]' web/src`), each re-expressed as a glass-ui radius token (`var(--radius-…)` as the producer's `dist/**/*.css` names them — read the token, never invent one) or a `rounded-*` utility at the component's root class; where a component is one that glass-ui ships (input, dropdown, dialog, tile), the local styling yields to the producer's surface. One commit per component family.
### F.W11.b The grid is the app's (Opus)
`PaperView.vue:455/:691` carries `.paper-grid`; the grid moves to the app root (`App.vue` / `style.css`) as a fixed, full-viewport, theme-aware layer under every route, and the paper sits on it (paper keeps its own surface, not the grid). Measured in a browser: the grid element's bounding box equals the viewport at 3 viewports on `/`, `/paper`, `/visualize`.
### F.W11.c The dock and the visualizer's glass idiom (Fable — design-author)
Design and land `AppDock.vue` on `GlassDock` (`@mkbabb/glass-ui/dock`: `DockControl` · `DockSeparator` · `DockLayer` · `DockTrigger`; read `../glass-ui/src/components/dock/README.md` whole), carrying `AppHeader.vue`'s controls — navigation, search, `About Fourier analysis`, `Dark mode` — with their accessible names and 44px floors intact; `AppHeader.vue` is retired or reduced to what the dock cannot carry (one home per control). Then the visualizer chrome — `VisualizationView.vue`'s panels (`CoefficientsPanel` · `ContourSettings` · `EquationPanel` · `AnimationControls` · `CanvasControlsDock` · `EditorControlsDock` · `GlassTimeline` · `ExportModal`) — is re-expressed on glass-ui surfaces (the producer's panel/card/configurator/dialog primitives), rounded per `.a`'s tokens, with the existing keystone e2e green. A producer gap (a dock/surface capability glass-ui lacks) is a relay letter to BK, never a local dock or a copied selector. Design notes (why each surface is what it is) go in the record.
### F.W11.d Close (Opus, verify-only)
Re-run G-F11-1..7 double; bounds path-by-path; E13; the LEDGER row; push.

## §3 Gates (born-RED; baselines banked at open)
- **G-F11-1** ⟨cmd⟩ `grep -rEn 'border-radius:\s*0[^.]|rounded-none' web/src | wc -l` → **0** (baseline 6).
- **G-F11-2** ⟨cmd⟩ `grep -rEn 'border-radius:\s*[0-9]' web/src | wc -l` → **0**; every remaining `border-radius` reads `var(--radius` (⟨cmd⟩ `grep -rEn 'border-radius' web/src | grep -vc 'var(--radius'` → **0**).
- **G-F11-3** `.paper-grid` absent from `PaperView.vue`; a browser probe at 3 viewports × 3 routes records the grid layer's box = viewport (9/9), screenshots committed.
- **G-F11-4** `AppDock.vue` imports `GlassDock` from `@mkbabb/glass-ui/dock`; `shell-header.spec.ts` green with every control reachable by its unchanged accessible name; `coarse-pointer.spec.ts` green (44px floors).
- **G-F11-5** each of the eight visualizer chrome files imports from `@mkbabb/glass-ui` (⟨cmd⟩ per file `grep -c '@mkbabb/glass-ui'` ≥ 1, 8/8); `visualization-ux.spec.ts` keystones green.
- **G-F11-6** `npm run build` exit 0; the full e2e run at close has NO failure absent at baseline `735319e` (the known producer-owned REDs from F.W10S are cited, not re-counted).
- **G-F11-7** E13: no UNREAD in scope; every producer gap has a BK relay row.

## §4 Close act
G-F11-1..7 GREEN → CLOSED; any producer-owned RED → CLOSED honest-RED with the relay id. The
fresh L-20 check follows per the chassis. Push `m/w1-bump-migration` at every unit's close.
