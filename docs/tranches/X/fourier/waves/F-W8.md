# X.F.W8 — The CRUD union prototype, end to end (the horizon probe)

**Sub-tranche**: X·F (`docs/tranches/X/fourier/waves/`), authority `docs/tranches/X/COHESION.md` — **§1's SS-4 row** (the sub-session register) and **§2's SS-4 contract co-signature row** (the dependency graph), cited by row label and never by line (R2-2; the round-1 addresses `COHESION.md:36`/`:69` are struck **as addresses**, their bytes re-verified unchanged this round). ⟨cmd⟩ (**base `docs/tranches/X`** — added at repair round 5, REST-44(a): from `docs/tranches/X/fourier`, the base at which every other operand in this file resolves, `COHESION.md` **does not exist**) `/usr/bin/grep -n 'SS-4' COHESION.md` → **seven lines: `36` `57` `69` `135` `146` `147` `165`**; **two are quoted here and the residue is named rather than implied (R4-1.9, REST-44(b))** — line **36** (§1's SS-4 row) and the §2 contract co-signature row. Line 36, **byte-true and cut with a disclosed ellipsis** (REST-44(c): the round-4 paste stopped at `X-W3 P0 rows` under a **verbatim** label while the bytes continue `· owner rulings owed (trie-vs-KISS et al.)` **plus four further cells and a trailing clause** — R4-7.1: *re-paste byte-true, or re-label*): *"| SS-4 | **CRUD/provenance union** — fourier CRUD ∥ palette CRUD (contract v2 · defect burn-down · trie design · union prototype → F.W5–W8, + the value-side counterparty riding X·V W3/W9 surfaces) | fourier lane-crud SEAM TABLE · J-diff-shape (J-era) · TA-4 atomdiff-excision record · X-W3 P0 rows · owner rulings owed (trie-vs-KISS et al.) | the co-signed shared-provenance contract + X·F CRUD wave specs | contract design TWICE-AUTHORED; Fable adjudication; owner rulings flagged inline | AWAITS intake CARRY + owner rulings |"* — **the full row, no cut** (REST-44(c)); and §2's row, verbatim: *"**SS-4 contract co-signature → F.W5 ∥ X·V API row**: neither side's edits gate the other's"*.

**Fold provenance (M-23 / M-12)**: this file is the fold-seat output, 2026-08-28, **repaired at X·F L-20 repair round 1 (2026-08-28)** under `docs/tranches/X/fourier/conformance/PASS-1/RULINGS.md` (R-1f · R-3 · R-4b · R-5 · R-9.1) and its local register `conformance/PASS-1/F-W8-CHECK.md`, then **repaired again at X·F L-20 repair round 2 (2026-08-28)** under `conformance/PASS-2/RULINGS-2.md` — **R2-7.1** (§6a residue rows) · **R2-7.2** (record-qualification inside §3; B5→B1) · **R2-7.3** (G15's LHS re-pointed off the corrupted operand) · **R2-7.4** (the closure statement re-computed, stamp-follows-operand) · **R2-7.5** (R-8 canonical form) · **R2-7.6** (quote conformed; count words) · **R2-11** (the one double-home dissolved) · **R2-9** (closure gate on a shape- and spelling-agnostic operand, detector inline) · **R2-5** (the roster extent re-cited, ELEVEN→TWELVE) — with **R2-1-LAW** and **R2-2** binding as standing law, against its local register `conformance/PASS-2/F-W8-CHECK.md`. ▲ **Round 2 landed in THREE sittings, and each one's first act was to measure the last one's word rather than inherit it.** Sitting 1 was cut off mid-round, leaving a masthead that described repairs its bytes had not yet made (§6a still 36 rows against a §1 that already read 124/84/40; §0's conversion register listing anchors still spelled as line numbers at §5c). Sitting 2 measured that gap and closed it: the §6a row count, the closure arithmetic, the anchor idiom, and the ⟨cmd⟩ provenance of every quotation. **Sitting 3 (this seat) re-ran EVERY ⟨cmd⟩ in this file against the live bytes** — the four frozen-corpus `sed` pastes, the seven band negatives, the F-W5 clause/gate/edge quotations (E14 · D12 · D17 · B1 · B5 · C2 · G22 · §4's two edge rows · §2's NINTH-collision block · §5's SS-L-07 row), the F-W0 quotations (§4 G-11/G-12 · §2's ledger row · §3's carry row 1), F-W1's TWELVE-limb charter sentence, the F-W4-CARRY bullet, and COHESION's two SS-4 rows — **all reproduce byte-identical — and the F-W5 set was re-checked a SECOND time after that file's own repair seat rewrote it at 16:38:31 mid-sitting, so the attestation holds on both sides of a concurrent sibling write** (which is precisely why every one of those citations is an anchor and not a line). The whole §1 census re-derives exactly (49 token-bearing · the same 5 boilerplate-only records BY NAME · 7 negatives · 37 positive-routing · 192/120/72 · 26/24 records · 23 en-dash-only · `F.W8` → 0). **Four residual defects of this round's own convicted classes survived sittings 1–2 and are cured here**, each minuted at its site: (i) §0 ERRATUM 3's **self-falsifying receipt** (a stated `grep -c` printing **0** for a token the minute itself had just introduced — re-run, it returns 1); (ii) §5c's overlap passage carrying a **⟨cmd⟩ marker with no command beside it**, now discharged by a runnable extraction whose output is pasted — and which, re-run sixty seconds later over a byte-identical roster, **returned a different answer because `F-W5.md` was rewritten between the two runs (mtime 16:38:31)**, so the passage now records the measured time-dependence rather than a fourth spurious figure; (iii) the `F-W4-CARRY` anchor spelled **"§D"**, a heading that carry does not have — corrected to **§Rows D**, the spelling the keystone independently uses; (iv) the masthead's own **truncated heading list** for `F-W5.md`, quoted as bytes without ellipsis or ⟨cmd⟩, now pasted complete. **A stable anchor that does not exist is no better than a line number that has drifted, and a receipt that cannot be re-run is not a receipt** — which is exactly the standard this file applies to its siblings, and now, at its own bytes, to itself.

▲ **ROUND 3 (2026-08-28) — the third repair, and the smallest.** Under `conformance/PASS-3/RULINGS-3.md` this file's directive row is **two** items, and both are executed here: **R3-7.2** — §6a row 19's band identity **re-attached** at F-W5 clause **F9**, the non-band `D-3+C-5` demoted to that row's footnote **outside the 40**, and the §6a header and closure stamp **re-derived after the rows moved** rather than defended (R2-7.4) — and the **R3-3.9 purge share**: **PASS-3 D-3** (the `F-W4-CARRY` heading paste elided without ellipsis, under a ⟨cmd⟩ whose output it did not reproduce) and **PASS-3 D-4** (the F-W0 `G-11`/`G-12` quotations carrying **bold markup where the bytes carry heading markup**, inside a span labelled *verbatim*). Two things are done beyond the letter of that row, and both are the round's own standing law rather than this seat's invention. **First, R3-3.10** — *no ⟨cmd⟩ whose output is a COUNT of a live sibling or of the citing file itself may be banked in a spec* — is applied at **every** site in this file that carried one: the `§6b`/`§6c` probe at §0, the subject-less-rider self-count at **§0 ERRATUM 3** (its token is deliberately NOT repeated here — that erratum's cure is a set-membership claim about this file, and re-spelling its subject in the masthead would falsify the claim in the act of announcing it; R3-3.7's self-invalidating-receipt law, obeyed rather than cited), and the two renderings of the Read-1 minute (§0 and §5c) that banked `grep -c "F\.W8" F-W5.md` → **0**. ▲ **That last receipt had already stopped reproducing**: the reciprocal landed later that same session, so a banked count of a live sibling had become false at the bytes exactly as the amendment predicts — the READING is kept and dated, the FIGURE is struck (R3-3.8). **Second**, both purge-share cures are applied at the **second site of their own class**, found by looking rather than by being told: the `^### G-1[123]` probe returns a **G-13** line this spec does not cite and round 2 silently dropped, disclosed at §5c. ▲ **The twin surface is CONSUMED, not assumed.** Where a row of this file touches F.W3/F.W4 it now cites **`F-W3.md` §X.1-v4** — the definitive file-criterion partition published FIRST this round (R3-1a), which this seat quotes at its criterion sentence and never at a count: **row 17**'s home sharpens from the vague *"F.W3/W4"* to **F.W4** (a sole spelling homes at its named wave), and **row 19's footnote** homes the demoted `D-3+C-5` the same way. F.W8 books nothing on that surface and asks nothing of it.

▲ **ROUND 4 (2026-08-29) — THE CENSUS FREEZE, and the round this file's own arithmetic does not survive.** Under `conformance/PASS-4/RULINGS-4.md` the F-W8 row is two items, and both are executed here: **R4-3.2** — *"the check-descended figures are struck; F-W8 carries **ZERO record-side routings** (canonical §4.1 — no record names F.W8), so its census is gate-machinery only, enumerated from its own fold/operand lists and NEVER from a check file"* — and **R4-10**'s wave row, *"**0** record-side (§4.1) … 84/124 struck (R4-3.2); masthead re-pasted under R4-8.3"*. The operand for both is `conformance/CENSUS-CANONICAL.md`, **adopted here as this file's SOLE census operand**; every other `census §…` citation in this file points at the intake census's *risk / artefact / evidence* registers and supplies **no id, no roster, and no denominator** — where any of them ever did, the figure is struck below rather than re-scoped.

```
C='docs/tranches/X/fourier/conformance/CENSUS-CANONICAL.md'   # sha256 3e0a9acb3381… · re-pinned at errata round 5, REST-51 (read a450b8e9f80e)
⟨cmd⟩ grep -nF 'are likewise named by **no record**' "$C"
  5394:   `F.W6`, `F.W8` and `F.W10` are likewise named by **no record** — their wave-side populations are
⟨cmd⟩ sed -n '5393,5396p' "$C"        (the enclosing sentence of canonical §4.1, pasted whole and hard-wrapped as the file wraps it)
   two `F.W7` substrings in all 66 records are inside `KF.W7` (the keyframes relay at `fr-Tooltip`).
   `F.W6`, `F.W8` and `F.W10` are likewise named by **no record** — their wave-side populations are
   folds, operands and gate machinery, never record routings. Four passes measured F-W6/F-W8
   "escapes" against record-side rows that do not exist; §1 is the denominator that settles it.
⟨cmd⟩ grep -nE '^### F\.W5(-W8)? — ' "$C"      → 5038:### F.W5 — **28 rows**    5052:### F.W5-W8 — **89 rows** *(errata round 5, 2026-08-29: read 90; `C:C-23` → UNROUTED at E5-15)* ⟨*re-run at the cured canonical `3e0a9acb3381`, REST-51*⟩
```

*(Line figures above sit **inside** pasted command output as this seat's dated reading and provenance — the citations themselves are the canonical's **§4.1**, its **§2 `F.W5` / `F.W5-W8` roster headers** and its **§3 TOTALS** rows, per §0's addressing law; the canonical is a live sibling and is hash-pinned under R4-8.3, never line-cited.)*

**Four consequences, applied at the bytes.** **(i)** `routedTotal = 124` and `bookedCount = 84` are **STRUCK wherever they stood as figures** — §1, §3's masthead, G15's cell, §6's preamble, §6a's header and the closure statement — because their whole derivation was `87 − 3` and `123 + 1` over `PASS-1/F-W8-CHECK.md` §1's mark table, which is the construction R3-4 made fail-by-construction and R4-3 now forbids by name. **(ii)** This file **BOOKS no canonical row.** F.W8's record-side roster is empty by the canonical's own enumeration, so every band identity named anywhere here — in §3's witness cells as much as in §6a's table — is a **CITATION to its holder**: the canonical's **§2 `F.W5-W8` roster (89 rows)** ⟨*re-based at errata round 5, **E5-15**: read 90; `C:C-23` → UNROUTED*⟩, whose wave-side duty R4-10 assigns to **F-W5**. Nothing in this file's substance moves; what moves is the claim its numbers were making. **(iii)** The **A-2 spelling census is retired as an operand** (its readings survive at G15 as a dated *detector* discipline, which is all they ever were), and with it the local check's **D-1** dissolves at the root: a wave that runs no record-side denominator cannot run a spelling-restricted one, and the identity that escaped the two-dash probe — **`fr-ContourPreview L:L-5`**, spelled `F.W5(-W8)` at its record's tally — is homed by the canonical at **F.W5**, not in the band, and is cited to that holder at §6a's header. **(iv)** The masthead's `F-W5.md` heading paste — PASS-4 **D-2**, the last banked count of a live sibling in this file — is re-cut immediately below under **R4-8.3**.

▲ **One finding of this seat's own, machine-derived and directed by nobody.** With the canonical's band roster as the operand, every one of its **89** rows (E5-15) was tested for bytes in this file. ⌧ **THE RECEIPT WAS TRUE AS PRINTED AND THE WRONG INSTRUMENT BY THIS FILE'S OWN LAW (re-cut at repair round 5, REST-45; P5-5).** The round-4 form was ⟨cmd⟩ per row `grep -qF '<id>' waves/F-W8.md` — and **`grep -qF` over a bare token is exactly what R-5 forbids as an identity test**, which this file says at §3, §5b, G15 and §6a and then warranted its only coverage claim with. **Re-cut record-qualified**: an id counts as identified when it is **corpus-unique** *or* **its record's name co-occurs on the line carrying it**. Re-run at this seat over the cured canonical's band roster: **40 of the 89 are corpus-AMBIGUOUS; 38 of those 40 qualify; two do not — `fr-BasisSelector m-7` and `fr-GalleryDraftsSection m-15`** (disposed at REST-47 and REST-46). **That partition is the claim; the 90/90 is withdrawn.** **At the reading that OPENED this round — before the citation below landed — exactly one row had zero bytes here: `fr-PaperSearchDropdown C:C-23`.** ▲ **The reading is dated because the repair consumes it**: re-run the same sweep against these bytes and **no row is absent**, since the cure *is* the citation. Stated the other way — as a standing *"one row is missing"* — the sentence would have been false the instant it was acted on, which is this round's whole lesson about receipts that describe their own file. It is also the one row where this file's old negative roster and the census of record *disagree*, since that record's own verdict line declares **"F.W5-W8 = no rows."** while the canonical's rule 2 (*a row's ROUTING is the X-token(s) on its line*) homes the line's band token in the band. **The canonical governs and the divergence is minuted, not laundered**: the id is cited to its canonical holder at §6a's header, F.W8 books it not, and the re-grading question — if there is one — belongs to the census and the keystone, never to this wave (§5c's F.W2 row carries the same minute).

▲ **R4-2 (the portable-command law) applied to this file entire.** `R=` is now declared **in-block beside its first use** at §1, §3 and §6a — the §4 gate preamble is no longer the sole declaration site, since a ⟨cmd⟩ five sections above its variable is a phantom witness by R4-2.3. No probe here uses PCRE, an escape-capture, lookaround, or a bounded wildcard: ⟨cmd⟩ `grep -nE -- '-P |\\K|\.\{[0-9]*,[0-9]*\}' waves/F-W8.md` returns **exactly one line — this one**, the minute that names the banned forms in order to ban them, and **no second line anywhere in the file**. ▲ **The form is deliberate and is the same one §0's ERRATUM 3 was forced into**: a self-descriptive count is false the moment the file moves, so the claim is stated as **set-membership** — a violation introduced by any later seat surfaces as a **second line a reader can see**, never as a stale integer a reader must trust (R3-3.10). Stating it as *"returns no output"* would have been the shorter sentence and a **self-falsifying receipt**, since writing the ban creates the string it bans. ▲ **R4-4's two-key falsifier is honoured in the only way an author seat can honour it**: every cure below publishes the command that produced it, this seat re-ran each one at the live bytes this sitting, and **none is offered as verified-by-its-own-author** — the pass-5 seat re-runs them, and R4-8.1's ordering puts the purge seat after every byte written here.

▲ **QUOTE-BY-COMMAND (R2-1-LAW) — binding on this file from round 2 forward.** A cross-spec or corpus quotation enters this file ONLY as the pasted output of a `grep`/`sed` run **by this seat, this round**, with the command stated beside it as a **⟨cmd⟩** provenance note; **a "verbatim" label without a ⟨cmd⟩ note is a defect per se.** **Rulings files are cure-shapes, never quotable authority** — where RULINGS-2 names a candidate home, this seat quotes the **BANK at the bank's own live bytes**, and where the two differ the ruling's **LAW** is applied to the **measured** home and the difference is minuted (erratum below). Composites are never fused: two sentences from two homes enter as two quotes with two ⟨cmd⟩ notes. Quotations carried unchanged from round 1 keep their round-1 attribution; every quotation this round **adds or re-states** carries its ⟨cmd⟩.

▲ **ROUND-2 ERRATUM — two ruling coordinates corrected AT THE BYTES (applied-with-correction; nothing declined).**
1. **R2-7.1's candidate home for `fr-ContourSettings M-10` — "the E13/A5 family" — does not hold at the bytes.** ⟨cmd⟩ `grep -n "M-10" waves/F-W5.md` → the id is booked at **clause D12**, *"Closed domains at the boundary"*, whose witness cell carries **`fr-ContourSettings M-10` (=L-M6/C-19/D-m9)**. §6a row 38 therefore cites **D12**; the ruling's LAW (exclusion-with-reason at the F-W5 clause home, homonym named) is applied unchanged.
2. **R2-2.3's anchor for F-W0's ledger row — "F-W0 §1a, the SUBSTRATE-LEDGER create row" — does not hold at the bytes.** ⟨cmd⟩ `grep -n "^## \|^### 1a" waves/F-W0.md` → `## 1. State` · `### 1a. Four-verb status` · `## 2. Bounds` · `## 3. Carry`; ⟨cmd⟩ `grep -n "SUBSTRATE-LEDGER.md" waves/F-W0.md` returns the **create** row inside the `## 2. Bounds` span, not §1a (§1a is the four-verb table). §5c cites it as **F-W0 §2 (Bounds), the `SUBSTRATE-LEDGER.md` create row**.
3. **R2-7.6's `raw-findings.json` rider has NO SUBJECT in this file — minuted so its absence is not read as an unapplied directive.** ⟨cmd⟩ `grep -n 'raw-findings' waves/F-W8.md` (re-run this sitting, 2026-08-28) → **the sole occurrence is this erratum line itself** — stated as set-membership, never as a figure (R3-3.10). F.W8 cites the two-candidate `raw-findings.json` in no substantive position, so there is nothing here to path-qualify. ▲ **The measurement's FORM is corrected here, and the correction is the point.** The earlier sitting stated the same fact as *"⟨cmd⟩ `grep -c 'raw-findings' waves/F-W8.md` → **0**"* — a figure true of the bytes it measured and **false of the bytes it was written into**, because writing the minute *created* the only occurrence. A stated command whose re-run at this file's own bytes returns something other than the printed figure is precisely the class this round convicts elsewhere; a self-falsifying receipt is not excused by being about an absence. ▲ **ROUND-3 (R3-3.10, the standing amendment): round 2's diagnosis was right and its instrument was wrong.** It replaced a **false** self-count with a **true** one — and a self-descriptive count in a moving file is now banned outright, because it is false the moment the file moves, and this file has moved again since. The claim is therefore restated as **set-membership**: `raw-findings` occurs in this file only inside this erratum, so R2-7.6's path-qualification rider still has no subject here. Re-run the probe and the sole line it returns is this one — a form whose failure would surface as a **second hit a reader can see**, never as a stale integer a reader must trust. The rider's siblings in the same R2-7.6 clause **do** have subjects here and are all applied: the F-W5 §4 quotation conformed to its bytes (§5c, PASS-2 D-10) and the count words re-derived or struck (the record denominator 43→37 at §1, and the "64 shared ids" figure struck at §5c, PASS-2 D-5/D-13). **Nothing in R2-7 is declined.**

**Intake authority — stated once, and it is NOT a carry.** `ls docs/tranches/X/fourier/carry/` → `F-W1-CARRY.md`, `F-W4-CARRY.md`: the only two carries in tree, and neither is F.W8's. Per R-3.2 **no F.W8 carry is authored here**, and the vocabulary *"the CARRY"* is retired from this file. This wave's rows are derived from **the 66 adjudicated records at `docs/tranches/V/megatranche/registry/adjudicated/`**. ▲ **Two operands, and round 4 separates them for good.** The **EVIDENCE** operand is the frozen corpus — the 66 adjudicated records, immutable, line-citable, the source of every witness quoted in §3. The **CENSUS** operand is `conformance/CENSUS-CANONICAL.md` and nothing else (R4-3 / R4-10): no check file, no prior pass, no re-derivation of this seat's own. Round 1 re-checked itself against `conformance/PASS-1/F-W8-CHECK.md` §1, a **bare-token** roster carrying three homonym false positives and one omission; round 2 retired that file as an operand (R2-7.3) but kept its *arithmetic* — and round 4 strikes the arithmetic too, because a figure inherited from a retired operand is that operand, wearing a later date. The §3 table agglomerates **28 rows** (P1–P8 · J1–J7 · R1–R6 · M1–M5 · D1–D2, enumerated in place) over band identities **held elsewhere and cited here** — F.W8 books no canonical row (FR-GV-8 ⊕ FR-GV-27 are deduped by identity into one row at §3 P5, both ids preserved). The value-side amendments **A-1** and **A-3** were re-measured by this seat and every receipt reproduced (the excision commit; the `model.ts:84` anchor); **A-2 is retired as a census** and survives only as G15's dated detector reading, its figures struck under R4-3.2 with everything else the check file fathered.

**The keystone section actually read** (R-1f; the prior draft attested to a section that does not exist): `F-W5.md` has **no §6b and no §6c** — ⟨cmd⟩ `grep -o '§6b\|§6c' waves/F-W5.md` (re-run this sitting, 2026-08-28) → **no output**. ▲ **The probe's FORM changed at round 3 (R3-3.10)**: round 2 banked this as a `grep -c … → 0`, and *no banked ⟨cmd⟩ whose output is a count of a live sibling may stand in a spec* — a `grep -o` word-output states the same absence in a form the keystone's next edit cannot silently falsify into a stale integer. Its section headings are cited **by label**, and where a label is pasted the paste stops at the label — ⟨cmd⟩ `grep -n '^## ' waves/F-W5.md` (this seat, 2026-08-29, run **twice**: first at `F-W5.md` **sha256 `d9f3c812cf36`…**, then re-run at **sha256 `dec6c00599e6`…** after the keystone's own round-4 seat rewrote it **mid-sitting** — both pinned per **R4-8.3**), the seven labels with **one elision, marked and disclosed**: *"`## 0. State`"* · *"`## 1. Bounds`"* · *"`## 2. Carry — the adjudicated band agglomerated (…)`"* · *"`## 3. Gates — 22, all born-RED`"* · *"`## 4. Cross-edges — declared from THIS end (one home, two citations)`"* · *"`## 5. Carried by citation — the excluded-from-clause-tables set (G19's other operand)`"* · *"`## 6. Close (unit e checklist — all `planned`)`"*. **The `(…)` is §2's id-keyed-routing count, elided deliberately and named as elided.** ▲ **ROUND-4 (PASS-4 D-2 · R4-9.3 · R4-8.3) — the last banked count of a live sibling in this file, and the only one of thirty-six re-run receipts that stopped reproducing.** Round 3 pasted this list under the label *"verbatim and complete"* carrying **`167`**; the keystone's live heading no longer says 167, and **R4-5 has since STRUCK F-W5's census figure outright** — so re-pasting the round-3 bytes publishes a receipt that cannot run, and re-pasting the *current* bytes imports a struck figure into a spec forbidden to quote one. Both roads fail, which is the tell that the paste itself was the defect. The cure is the idiom this file already uses for `F-W0`'s gates and the carry's `§Rows D`: **cite the label, elide the count under a marked ellipsis, pin the sibling's hash.** ▲ **The class dies by construction rather than by vigilance**: a heading is a stable **anchor**; the **text** of a heading carrying a census figure is not, and this was the one site where this file pasted a sibling's text-with-a-figure instead of citing its label. ▲ **AND THE HASH PIN EARNED ITSELF INSIDE ONE SITTING — the strongest receipt this round produced, and this seat did not arrange it.** Between the two runs above, `F-W5.md` was rewritten by its own round-4 seat: **`d9f3c812cf36`… → `dec6c00599e6`…**. **All seven labels held**; the elided parenthetical did not — it has now read three different things in three rounds (`167`, then `170`, and at the new bytes a re-based span naming the canonical's **28 ⊕ 89 = 117** record-side rows ⟨*re-based at errata round 5, **E5-15**: read 90; `C:C-23` → UNROUTED*⟩). ▲ **Had this file pasted the figure a third time it would have been stale a third time, inside the very round that convicted it** — and had it pasted without the pin, the staleness would have been invisible. **That is R4-8.3 working: a later rewrite of a sibling is now *detectable*, not deceptive.** ▲ **The convergence is worth stating plainly**: the keystone's new heading counts the band at the same canonical rows this file now cites it to (§2 `F.W5` 28 ⊕ §2 `F.W5-W8` **89**), so the two ends of the reciprocal reach the same roster from the same file — which is exactly what PASS-4 **D-4** asked for and what neither end could give while each carried its own hand-cut detector. ▲ **The anchor idiom absorbed the rewrite whole**: every clause and gate id this file cites at F-W5 — `E14` · `D12` · `F9` · `B1` · `B5` · `C2`'s *"m-15 is CROSS-REFERENCED"* · `G22`'s *"dangling-cite resolution"* · `D17`'s *"ONLY the serializer arm"* · §5's `SS-L-07` row · §4's `v1 §6` row — still resolves at the new bytes, and the `F.W8` reciprocal token is still there. **Not one citation in this file broke; the one thing that moved was a count inside a heading, which is the one thing this file no longer quotes.** ▲ The round-2 defect that the paste was authored to cure is still cured — the earlier sitting had rendered the last two labels as bare `## 5. Carried by citation` and `## 6. Close`, truncations offered as bytes with no ellipsis and no ⟨cmd⟩ — and the three cures now coexist: **complete list, marked elision, hash-pinned read.** ▲ **Closing minute, so no fifth pass reads the pins as warranties**: at this seat's last measurement before close `F-W5.md` had moved a **third** time (**`549fd38a0aed`**…), its seven labels and every clause id cited here **still resolving**. The pins are **dated readings of a file still under its own round-4 write**, not a claim about its current bytes — which is why the citation is the **label** and the hash is only the tripwire beside it. Under **R4-8.1** the purge seat runs after every one of us; its sha256 table, not this line, is the state of record at close. This seat read **`F-W5.md` §4**, twice: at first reading it declared **no edge to F.W7 or F.W8**, the `F.W8` token being absent from the keystone altogether at that reading — the defect §5c declares; **on re-measure later the same session the R-5 reciprocal had LANDED** and §5c below states the corrected fact. ▲ **ROUND-3 (R3-3.8 / R3-3.10): round 2 banked that dated reading as `grep -c "F\.W8" F-W5.md` → 0, and that receipt no longer reproduces** — ⟨cmd⟩ `grep -o 'F\.W8' waves/F-W5.md` (this seat, 2026-08-28) now returns the token, from the reciprocal row itself. **The reading is kept and dated; the figure is struck**, which is the whole of the amendment's point: a banked count of a live sibling is false the moment the sibling moves, and this one already was. Neither authoring half is the wave; this fold is the wave's spec.

▲ **ADDRESSING LAW FOR EVERY LIVE-SIBLING CITATION IN THIS FILE (E-3 / D-19; generalised at round 2 per R2-2, which rules the idiom program-wide).** `F-W5.md` is **under concurrent repair in this same L-20 round**, and its line numbers moved *between two reads by this seat in one session* (§4 was at `:245`, then `:255`). A line number is therefore an **address, not an identity**: every citation below names the **CLAUSE or GATE ID and its section** — `F-W5` clause **E15** (§E), gate **G22** (§3) — which are banked and anti-rename-stable. Line readings in this file are this seat's, **2026-08-28**. Where a sibling's repair moves a clause, the citation still resolves; where it renames one, that is a defect against anti-rename, not against this file.

**The carve-out is no longer scoped to `F-W5.md`** (PASS-2 D-9: the F-W0 anchors this file quoted had drifted +12 under F-W0's own concurrent repair while the carve-out reached only the keystone). **R2-2's LAW, adopted whole and applied here at round 2:**
1. **Cross-spec receipts cite STABLE anchors** — §-headings, gate ids, clause ids, banked row ids, row labels — **never line numbers.** This binds every live sibling: `waves/F-W0..F-W10`, `carry/F-W1-CARRY.md`, `carry/F-W4-CARRY.md`, `COHESION.md`, and F.W5's contract artifacts.
2. **Line numbers are permitted ONLY** for (i) the **frozen corpus** (`registry/adjudicated/fr-*.md`, adjudicated and immutable) and files cited with a commit hash, and (ii) this file's **own** intra-file coordinates, marked *"at this writing"*.
3. **Line figures for live siblings appear in this file only INSIDE a pasted ⟨cmd⟩ output**, as this seat's dated reading and as provenance for the quoted bytes — **never as the citation**. A reader re-resolves by the anchor, never by the figure.
4. **New citations are written in the stable idiom from the first byte** (R2-2.4). Round 2 converted **every one of this file's surviving live-sibling line-cites — seven sites, six files** — to anchors: `F-W5.md:251` → **F-W5 §3 gate G22** (J5) · `F-W5.md:114` → **F-W5 §2 clause C2** (M4) · `F-W0.md:199`/`:204`/`:63` → **F-W0 §4 gates G-11 / G-12** + **F-W0 §2 (Bounds), the `SUBSTRATE-LEDGER.md` create row** (§5c F.W0) · `F-W1.md:276`/`:294` → **F-W1 §4 step 4** + **F-W1 §4 cross-edge 1** (§5c F.W1) · `carry/F-W4-CARRY.md:234` → **§Rows D (the transport cluster), the `SS-L-07/SS-C-10` ⟨SpeedSelect⟩ bullet** (§6a row 35; the earlier sitting wrote this anchor as *"§D"*, a heading the carry does not have — corrected at the resumption sitting against the file's own heading tree, and spelled identically at F.W5's end) · `COHESION.md:36`/`:69` → **COHESION §1's SS-4 row** + **§2's SS-4 co-signature row** (the masthead) · and F-W5's §4 close-gate row → **the `F.W5 → F.W9/W10` edge row** (§5c). **Every quoted word was re-verified by ⟨cmd⟩ this round and left byte-identical**, except the one quotation that was NOT byte-identical and is cured as a quotation defect, not an address defect (§5c F.W9/W10, PASS-2 D-10). ▲ **A struck address is not a struck reading**: where round 1's coordinate had drifted (the three F-W0 cites, +12 under F.W0's own concurrent repair — PASS-2 D-9), the *substance* held at the true anchor in every case, and this round changed the citation, never the claim. **The Re-anchor seat's F-W8 share is therefore ∅** — ⟨cmd⟩ `grep -n 'F-W[0-9]*\.md:[0-9]\|CARRY\.md:[0-9]\|COHESION\.md:[0-9]' waves/F-W8.md` returns only lines where the figure sits **inside** a struck-address minute or a pasted ⟨cmd⟩ output, never as a citation. If a later seat finds a live-sibling line-cite here *used as an address*, that is a round-2 defect of this seat.

**Half**: **library half — Opus author seat (M-12 tri-fold)**. Stated honestly: F.W8 owns **no product source at all**, so the wave's substance does not divide along a library/demo axis; this half authors the complete spec and the Fable arbiter fold reconciles it against the sibling half. Neither half is the wave; the fold is.

**Status**: **planned**.

**EXECUTION IS NOT AUTHORIZED BY THIS FILE.** Nothing **writes, builds, or runs** product source in either repo until the owner's begin-word. `/Users/mkbabb/Programming/fourier-analysis` is **READ-ONLY, always** — this wave writes zero fourier bytes; the FN-6 ask travels as a letter. Specifying is not doing.

▲ **READ-ONLY MEASUREMENT CARVE-OUT (stated so the absolute is not self-contradicted).** The prior draft said *"nothing opens"* while the spec's strongest witnesses (A-1/A-3, G1, G16, §3 R3) are **reads**. The absolute is restated precisely: **inspection — `ls`, `grep`, `git log`, `git show`, reading a file — is permitted, must be DATED and ATTRIBUTED at its row, and produces no authorization**; **mutation — writing, building, running, installing — waits for the begin-word.** The fourier tree is inspected under this carve-out and **written never**, in any verb, at any time. A read that finds a corpus anchor stale is **recorded as a re-resolve obligation (D-19)**, never adopted as this file's anchor (§4, discipline 1).

---

## 1. State

**Name**: F.W8 — the union prototype walked end to end: create → derive → diff → remix → history, against **both** palette and fourier-viz objects, with conformance fixtures in **both directions**.
**Opens after**: **F.W0** (substrate pre-gates — hard) · **F.W1.close** (DAG; one cycle DECLARED, §5c) · **F.W2** (ordering) · **F.W5** (the ADMISSION KEYSTONE — its three artifacts are inputs, not prose).
**Agents**: 5 **serial** (units b/c/d share the fixture-register identity space; §5a forces sequencing).
**Hard gate**: 16 conditions in §4, **all born-RED**, **each keyed by id to a banked witness carried in a §3 row — none minted without a registry or in-tree authority** (the EVIDENCE operand is the frozen corpus, never a carry — R-3; the CENSUS operand is `conformance/CENSUS-CANONICAL.md`, never a check file and never a re-derivation of this seat's own — R4-3 / R4-10). 4 are OWNER-GATED (G1, G7, G12, and G3's denominator freeze) and may not be authored unruled.

### Four-verb status (separate fields; this file moves exactly one)

| verb | value | evidence |
|---|---|---|
| AUDITED | **YES** | 26 of the 66 adjudicated `registry/adjudicated/fr-*.md` records · `formation/fourier/lane-crud.md` (R-1..R-7, F-α/β/γ, V-α/β/γ) · `INTAKE-ADJUDICATION-2026-08-03.md` §3 · `FOURIER-AUXILIARY-…-2026-08-03.md` §4/§5 · `api-gap-remeasure.md` · census §4.9 |
| SPECIFIED | **YES — fold-sealed 2026-08-28** | validated against `docs/precepts/` current at authoring (L-20) |
| IMPLEMENTED | **NO** | — fixture bytes land only after the begin-word |
| VERIFIED | **NO** | — stamped only at X·F's release close |

**ZERO record-side rows — and at round 4 the authority for that sentence is the CENSUS OF RECORD, not this seat's own grep.** `CENSUS-CANONICAL.md` **§4.1**, quoted whole at the masthead: *"`F.W6`, `F.W8` and `F.W10` are likewise named by **no record** — their wave-side populations are folds, operands and gate machinery, never record routings."* Corroborated, never substituted, at the frozen corpus: `R='/Users/mkbabb/Programming/value.js/docs/tranches/V/megatranche/registry/adjudicated'` (declared in-block, R4-2.3); ⟨cmd⟩ `grep -rho "F\.W8" "$R"/fr-*.md` → **no output** across the 66 immutable records. ▲ **The round-3 sentence that followed it is CORRECTED, not carried**: *"the entire band routes as the atom `F.W5-W8`"* was **false at the corpus** — the band is spelled at least four ways, one of them `F.W5(-W8)` (PASS-4 D-1), which is precisely why a spelling is not a roster and why the roster is now the canonical's.

**F.W8 HAS NO RECORD-SIDE DENOMINATOR (R4-3.2 / R4-10).** There is no `routedTotal` here, no `bookedCount`, and no partition of the band into this wave's booked-versus-named halves; the figures **124** and **84** are **STRUCK**. The band's record-side roster of record is the canonical's **§2 `F.W5-W8` (89 rows)** ⟨*re-based at errata round 5, **E5-15**: read 90; `C:C-23` → UNROUTED*⟩, whose wave-side duty **R4-10 assigns to F-W5**, beside the canonical's **§2 `F.W5` (28 rows)**; F.W8 books **no row of either** and cites every identity it names to that holder. What this wave *does* have a population of is **gate machinery, enumerated in place and countable only from its own lists**: **16 gates** (§4, `G1`–`G16`), **28 carry rows** (§3, P1–P8 · J1–J7 · R1–R6 · M1–M5 · D1–D2), **40 citation rows** (§6a, numbered 1–40), **9 file-bounds paths** (§2a) and **14 declared cross-edges** (§5c) ⟨**re-cut at repair round 5, REST-43 — one character (P5-8).** ⟨cmd⟩ `/usr/bin/awk 'NR>=265 && NR<=278 && /^\| /' waves/F-W8.md | wc -l` → **14**: F.W0 · F.W1 · F.W2 · F.W5 · F.W6 · F.W7 · F.W9/W10 · the value.js API row · SS-4 · SS-13 · the glass-ui BH relay · X·parse-that · the fourier tree · the execution gate. **R4-7.3: the LIST is authoritative.** ⊘ Four of five figures (9 · 28 · 16 · 40) survive their own instruction; the fifth is the shape R4-3.2 was written to end, **surviving one sentence past the strike that produced it**⟩. Every one of those is a list a reader can count without trusting a number — which is the whole of R4-3.2's instruction and the reason it costs this file nothing but its arithmetic.

- **Measured band NEGATIVES are EVIDENCE, never a denominator (P-9), and at round 4 they are no longer a denominator of anything here.** Each was re-read at the frozen corpus and each quote is byte-exact (⟨cmd⟩ `sed -n 'Np' "$R"/<record>` per row): fr-App `:144` *"**Nothing routes → F.W5–W8**…"* ⟨**emphasis restored at repair round 5, REST-50 (P5-10).** The bytes are `- **Nothing routes → F.W5–W8**: App's coupling budget is spent entirely on the CSS-token↔parser seam and the mount seam…` — **the `**` was stripped inside a span labelled verbatim**, while the sibling paste one clause later preserves it byte-true (`fr-MobileFloatingToc :97`). ⊘ **PASS-3 D-4's convicted class inverted**: round 3 convicted markup *added*, this is markup *removed*, and R4-7.1 covers both — *"nothing is added, nothing is re-styled."*⟩ · fr-CollapsibleSection `:131` *"F.W5–W8 gets nothing — the component is API-inert, and that negative is recorded;…"* ⟨*ellipsis added at repair round 5, REST-50 — the bytes continue `, and that negative is recorded; the census-methodology rows go to SS-3/SS-4 with NO-WAVE-OWNER.`, and the round-4 paste was a mid-sentence subset with no cut mark*⟩ · fr-MobileFloatingToc `:97` *"**F.W5–W8 gets nothing**"* · fr-MorphShapePreview `:110` *"preserved so no F.W5-W8 row manufactures overlap"* · fr-PaperSearch `:90` *"**F.W5-W8 empty** (measured zeros)"* · fr-PaperSearchDropdown `:77` *"**F.W5-W8 = no rows.**"* · fr-PaperSearchInput `:125` *"**F.W5-W8 empty** (the banked zeros hold)"*.
- ▲ **ONE OF THOSE SEVEN DISAGREES WITH THE CENSUS OF RECORD, and the disagreement is minuted rather than resolved by this wave.** The canonical homes exactly one band row at `fr-PaperSearchDropdown` — ⟨cmd⟩ `C='docs/tranches/X/fourier/conformance/CENSUS-CANONICAL.md'; grep -nF 'fr-PaperSearchDropdown' "$C"` (variable declared beside its use, R4-2.3) → among its rows, `5034:- **fr-PaperSearchDropdown** (1): `C:C-23`` inside the **§2 `F.W5-W8`** roster — while that record's own line `:77`, the very line the canonical read, ends *"**F.W5-W8 = no rows.**"*. The canonical's rule 2 takes the band token **on the line**; the record's sentence declares the opposite about itself. **The canonical governs (R4-3 / R4-10), F.W8 books nothing either way, and the re-grading question belongs to the census and to F-W5 as the roster's holder** ⌧ **RESOLVED AT THE CENSUS, ERRATA ROUND 5 (E5-15), ON THIS SPEC'S EVIDENCE (REST-51).** The canonical now reads `` | `C:C-23` | `C:C-24` | — | **UNROUTED** <sub>*errata R5 E5-15*</sub> | ``: the only `F.W5-W8` on `fr-PaperSearchDropdown.md:77` was the record's own **`F.W5-W8 = no rows.`** — **a load-bearing negative read as a positive routing** — on an INFO row that FOLDS by reference into banked `fr-PaperSearch C-Z1/C-Z2`. **F-W8 minuted the divergence three times, booked nothing either way, and said in terms that _"a wave with zero record-side rows may not settle a record-side reading."_ That posture was correct and the repair was always the census's.** The band roster is **89**, not 90. — a wave with zero record-side rows may not settle a record-side reading, and this one does not try. The id is cited to its holder at §6a's header.
- **The four identities named at §6a rows 37–40** entered this file at round 2 by re-derivation from the corpus after a bare-token operand hid them; their provenance receipts are kept below because §6a's rows rest on them, **stripped of the census arithmetic that used to ride alongside** (R4-3.2).

```
R='/Users/mkbabb/Programming/value.js/docs/tranches/V/megatranche/registry/adjudicated'   # declared in-block (R4-2.3)
the four, each re-proved at the FROZEN corpus by this seat (line cites permitted — §0 addressing law item 2):
⟨cmd⟩ sed -n '70p'  $R/fr-ContourSettings.md    → "M-13 = LC-missed-2 … **→ F.W5–W8** (contour provenance/CRUD union …)"
        every `M-13` in this file is fr-CoefficientsSpectrum's or a collision roster  → FALSE BOOKING, now §6a row 37
⟨cmd⟩ sed -n '67p'  $R/fr-ContourSettings.md    → "M-10 = L-M6 / C-19 / D-m9 … the shared-enum contract itself is an
        **F.W5–W8 rider**"; this file's only `M-10` is fr-BasisSelector's → FALSE BOOKING, now §6a row 38
⟨cmd⟩ sed -n '64p'  $R/fr-GalleryCard.md        → "L·M-4 / D-13 / C-8(a) + L·D-2 / C-12 … serializer → F.W5–W8; JSC → SS-13"
        matched only through the bare `D-13` of fr-GalleryInfiniteGrid → FALSE BOOKING, now §6a row 39
⟨cmd⟩ sed -n '102p' $R/fr-CanvasControlsDock.md → "C-28 into D-4's F.W5–W8 rider"
        never in ANY prior operand — 0 bytes in this file before round 2 → OMISSION, now §6a row 40
```

▲ **THE ARITHMETIC IS STRUCK — the whole chain, at its root (R4-3.2; PASS-4 D-3). Dated here rather than deleted, per E-3.** What stood in this slot was a reconciliation of `PASS-1/F-W8-CHECK.md` §1's mark table: that file split itself *"bookedCount = 86 · escapedCount = 37"* while its own marks tallied **87 B / 36 E**; round 1 corrected the split to 87/36; round 2 re-based the operand and computed `87 − 3 = 84`, `36 + 4 = 40`, `123 + 1 = 124`. ▲ **Every one of those figures descends from a check file, and no amount of correcting the subtraction changes what is being subtracted.** R3-4 named the shape — *a census transcript whose LHS is a count copied from any check file **FAILS BY CONSTRUCTION**, because a gate whose operand is a superseded seat's enumeration can only re-find that seat's misses* — and R4-3 turned it into a rule with an operand attached: the canonical, or nothing. So `124` and `84` are struck as figures, the derivation is preserved above only as the history of a retired instrument, and **nothing in this file re-computes them under a new name.** ▲ **The keystone is the proof that this is not bookkeeping**: F-W5 carried the identical shape, struck it this programme, re-derived from the 66 records, and its own figure moved — the delta being the exact measure of what inheriting had hidden. F.W8's cure is cheaper only because F.W8's record-side roster is **empty**: there is no re-derivation to run, only a claim to withdraw. ▲ **What survives is the law the figures were standing in for**, and it is unchanged: G15's closure run (unit **e**) differences this file's citations against the canonical roster **at closure time**, and if that run disagrees with anything stated here, **the run governs and this cell is amended, not defended** (R2-7.4).

### 1a. Goal criterion

The wave succeeds if the five verbs of the union can be **walked against both object kinds**, and — for every leg either side cannot answer — the inability is **stated in the register with its reason** rather than discovered by a later repair. The walk is a diagnostic instrument, not a repair: it repairs nothing and claims credit for no other wave's cure.

### 1b. The TA-4 branch — stated, never silent

⊙ **G1 is unruled.** The honest default this spec carries absent the owner's word is **EXPLICIT RE-SCOPE**: *the prototype's diff leg is one-sided — fourier-only — and the union's diff verdict is a one-sided verdict, said out loud in the walk record and in every fixture row that touches it.* The alternative branch is TA-4 **restoration as a PREREQUISITE**, owned by the value.js API row, not by this wave.

▲ **AMENDMENT A-1 (measured 2026-08-28, value-side, READ-ONLY measurement per §0's carve-out — the F.W0 lock reaches fourier anchors, not these).** FN-6's value-side half — five wire-envelope shape rows in `api/test/conformance/diff.test.ts`, landed at **`7351297f`** (*"test(R · W6): the 5 wire-envelope shape-fixture rows in diff.test.ts"*) and merged at **`8a2a617e`** — is **true of history, false at HEAD.** ▲ The authority for that sentence is **git itself, re-run below**, and the record at §D D1 (FN-6/FN-5): the prior draft attributed it to a quoted "CARRY G14 cell" that is in no in-tree document, and that attribution is **struck, not paraphrased** (R-1 law / R-3).

```
find . -name "diff.test.ts" -not -path "./node_modules/*"      → (nothing)
git log --oneline --all -- api/test/conformance/diff.test.ts   → 7351297f (landed) … a8ff7792 (last)
git show --name-status --oneline a8ff7792                      → D api/src/lib/crud/atomdiff.ts
                                                                 D api/test/conformance/diff.test.ts   (371 lines)
```

`a8ff7792` is *"refactor(T.W1 · api · TA-4): excise the write-only atom-diff apparatus (the /remix+/diff fold)"* — **the same commit that excised atomdiff excised the conformance fixture with it.** Consequences, all VALUE-SIDE, never re-booked as fourier defects: **(i)** TA-4's restoration scope has a **third, previously unbooked limb** — the fixture file, not only `lib/crud/atomdiff.ts` and `atomDiff` on `PaletteVersion`; **(ii)** FN-6 asks fourier to wire a reader for a file value.js **no longer holds**, so the correspondence is not merely one-way, it is **zero-way at HEAD**; **(iii)** G14's born-RED reading corrects from *"1 direction landed, 0 reciprocated"* to **"0 extant · 1 landed-then-excised · 0 reciprocated"**. Survivors confirmed: `api/src/modules/palette/hash.ts:26` `computeAtomHash` / `:44` `computeAtomSetHash`; the compiled corpse `api/dist/lib/crud/atomdiff.{js,d.ts,+maps}` (untracked, 4 files); `ls api/src/lib` → *No such file or directory*.

▲ **AMENDMENT A-3 (anchor drift, value side).** FN-5's contract-currency invariant cites *"the `PaletteVersion` shape (`models.ts:153`)"*. At HEAD the file is `api/src/modules/palette/**model**.ts` (singular) and the interface is at **`:84`**; `atomDiff` appears nowhere in the module. Re-resolve before citation — the same D-19 discipline, applied on the side F.W0's lock does not reach.

### 1c. What this wave is NOT

It repairs nothing. It wires no client (F.W1/F.W3/F.W4), burns no defect (F.W6), designs no trie (F.W7), rules nothing F.W5's ruling block already owns, and runs no live probe (SS-13). **FR-GIG-5 mirror, adopted as a standing bar: no wave is credited with another's cure** — F.W8 claims none of F.W4's wiring unit, none of F.W1's tri-package transaction, and none of F.W1's *non*-cure of the pagination drain.

---

## 2. Bounds

### 2a. File bounds

| File | Access |
|---|---|
| `$V/docs/tranches/X/fourier/waves/F-W8.md` | create (this spec) |
| `$V/docs/tranches/X/fourier/conformance/union-prototype-walk.md` | create — the five-verb × two-object walk map and its acceptance record |
| `$V/docs/tranches/X/fourier/conformance/fixture-register.md` | create — one row per fixture, **both directions**, each naming the operation model it is generated FROM |
| `$V/docs/tranches/X/fourier/conformance/C31-two-sided-reproduction.md` | create — the reproduction, then the F8-C31A/F8-C31B split |
| `$V/docs/tranches/X/fourier/conformance/OWNER-RULINGS-F.W8.md` | create — **only** the rulings F.W5's block does not already own (§5d) |
| `$V/docs/tranches/X/fourier/conformance/walk/union-walk.mjs` | create — **authored here, EXECUTED at F.W9/W10.** No run under this wave |
| `$V/docs/tranches/X/coordination/value-to-fourier-2026-08-DD-fn6-reader-and-fixture-register.md` | create — the relay letter (the only vehicle by which fourier receives the register) |
| `$V/docs/tranches/V/coordination/INBOX.md` | **append-only** — one E13 ledger row; no rewrite of any existing row |
| `$V/docs/tranches/X/fourier/waves/F-W8/carry-closure.md` | create — G15's set-difference output, both directions |

**Bounds authority — the table above, and nothing behind it.** There is no F.W8 carry to reconcile against (R-3), so the nine rows are **this spec's own tree-derived enumeration**: every one is a path under `$V/docs/tranches/X/` (or the E13 INBOX) that this wave creates or appends to, and every one is named because a gate or an edge below binds it. Seven are the `conformance/` + coordination artefacts **census §4.9** assigns this wave; **two are added here and justified in the open**, each because a gate would otherwise be unfalsifiable prose: `walk/union-walk.mjs` (§5c's F.W9/W10 edge gives F.W8 the fixture artifacts **and** the prototype walk script, and F.W9/W10 the run), and `F-W8/carry-closure.md` (G15 is prose without its artifact — the `F-W5.md` G19 precedent). **No path here is adopted on the authority of a document not in tree**, and every path either exists or is marked `create` above.

**The value-side fixture bytes are NOT in these bounds.** `api/test/conformance/**` is product-test source under the execution gate, and A-1 proves its diff fixture was excised. F.W8 **specifies** the value-side fixture rows in `fixture-register.md`; **landing** them is the value.js API row's act, gated on the begin-word **and** on G1's ruling. Binding by **TRANSCRIPTION**, never cross-repo read (the api suite must run where fourier is not checked out).

### 2b. Do NOT touch — every path below, without exception

- `/Users/mkbabb/Programming/fourier-analysis/**` — whole tree, read-only law. **`J-diff-shape.md` v1 (271 lines) is IMMUTABLE**; v2 supersedes by reference (E-3: addenda, not patches).
- All product source both repos: `$V/api/src`, `$V/api/test`, `$V/src`, `$V/demo`, fourier `api/`, fourier `web/`.
- **F.W5's artifacts** — `contract/J-diff-shape-v2.md`, `contract/operation-register.md`, `contract/OWNER-RULINGS-F.W5.md`, `F-W5.md`. F.W8 **consumes** them; editing one would re-rule what F.W5 ruled.
- The evidence records: `registry/adjudicated/**` (all 66) · `formation/fourier/lane-crud.md` · `INTAKE-ADJUDICATION-2026-08-03.md` · `api-gap-remeasure.md` · `FOURIER-AUXILIARY-…-2026-08-03.md` · the census. **E-1/E-3 immutable beside the spec** — they hold the witnesses; rewriting one destroys the evidence a gate cites.
- `docs/tranches/X/COHESION.md` — root-authored at the boundary. F.W8 declares its half of every edge in §5c and **requests** the reciprocal; it does not write the spine.
- `scripts/dev/dev.sh` — unowned dirty row, never staged.

### 2c. Disjointness

Units **b**, **c**, **d** share the fixture-register identity space (`F8-CLIENT-*`, `F8-C31*`); per WAVE_SPEC §4a they are **sequenced**, not parallelised: `a → b → c → d → e`, each committing before the next opens. One writer at a time; a dirty tree at handoff halts the wave. No other X wave writes any path in §2a while F.W8 is open.

▲ **Read-only law, precisely scoped:** it binds fourier's **TREE (bytes)**. `union-walk.mjs`, when F.W9/W10 runs it, mutates fourier **DATA** in a non-production environment — that mutation is the instrument, is declared, and is subtracted (G9). **No fourier byte is written, ever.** The environment is F.W9's deploy spine, not this wave's.

---

## 3. Carry — 28 rows citing the band's identities to their canonical holder

**Operands, separated (R4-3 / R4-10).** EVIDENCE = the 66 adjudicated records, frozen, not a carry (R-3) — `R='/Users/mkbabb/Programming/value.js/docs/tranches/V/megatranche/registry/adjudicated'`, declared here beside the first ⟨cmd⟩ of this section that consumes it (R4-2.3). CENSUS = `conformance/CENSUS-CANONICAL.md`, sole. ▲ **ROUND-4: the rows below BOOK NO CANONICAL ROW.** F.W8's record-side roster is empty (canonical §4.1), so every banked id in a witness cell is **CITED to its holder** — the canonical's §2 `F.W5-W8` roster (**89 rows**) ⟨*re-based at errata round 5, **E5-15**: read 90; `C:C-23` → UNROUTED*⟩ and §2 `F.W5` roster (28 rows), both held at **F-W5** per R4-10 — and the round-2 sentence that stood here, *"84 of the band's 124 routed identities are booked in the rows below"*, is **STRUCK with its figures** (R4-3.2). The **28** rows are enumerated in place (P1–P8 · J1–J7 · R1–R6 · M1–M5 · D1–D2) and the **40** citation rows at §6a are numbered 1–40: both are countable from their lists, which is the only kind of count this file publishes now. ▲ **VOCABULARY, FIXED ONCE AND BINDING ON EVERY CELL IN THIS FILE.** Where a row below says F.W8 *books* an id, it means **carries it as an assertion row of this wave** — it does **not** mean *books a canonical row*, which F.W8 cannot do and nowhere does. For three rounds the census verb and the work verb were the same word, which is how a wave with zero record-side rows came to publish a `bookedCount`; separating them is the whole content of R4-3.2 at this file, and the separation is what makes G15 runnable in a direction that can actually fail. Dedupe is by **identity**, not text: where records witness one mechanism the row carries **all ids** and books once. **Every short id is record-qualified** (R-5) — the corpus reuses `B-1`, `B-2`, `C-2`, `C-3`, `C-4`, `C-17`, `C-18`, `D-13`, `L-B1`, `L-M3`, `M-10`, `M-13`, **`m-7`** and **`m-15`** across records ⟨*the last two added at repair round 5, REST-47 — a roster round 2 built "by CONVICTION, not by caution" from tokens that had already failed, **which is precisely why it did not contain the two that were failing silently***⟩, so a bare token is not an identity and a closure keyed on one cannot run. ▲ **Round-2**: `C-3`, `C-4`, `D-13`, `M-10` and `M-13` are on that list **because the round-1 operand proved it by failing on exactly them** (§1's four escapes) — the qualification below is not a style rule, it is the mechanism that makes G15 runnable. `‡` BLOCKER-weight · `⊙` OWNER-GATED · `▲` a lock binding the **repair**, not the finding. Every anchor below is **carried, not re-asserted** — MEASURE-AT-OPEN after F.W0 (D-19), except the three value-side amendments dated 2026-08-28.

### §P — The walk (the five verbs, both object kinds)

| row | banked ids | witness (carried) | F.W8's act + locks | gate |
|---|---|---|---|---|
| **P1** The seam map | **R-1..R-7** (lane-crud §2/§4, census §3b) | Every verb is asymmetric: R-3 diff EXISTS-fourier / **ABSENT-value**; R-5 remix EXISTS-fourier (per-atom overrides, tri-state `palette_slug`, `urn:contract:remix-noop`, idempotency, deliberately non-transactional) / PARTIAL-value (fork only, transactional); R-6 history PARTIAL-fourier (no revert, no by-hash get) / EXISTS-value. Route gap: fourier has `/diff` + `/restore`; value.js has `/revert` + `/versions/:hash` + `/vote` + `/flag`; **neither has the other's** | The walk runs the same five verbs against both kinds and reports **per verb which side cannot answer**. Two divergences **STATED, never harmonised**: **(a)** a fourier remix child is born `draft`, a value.js fork child born `public` — opposite privacy defaults on one verb (`visualization.py:277` / `forks.ts:76`); **(b)** fourier runs **no transaction by deliberate choice** ("standalone-topology-honest", `:493`) while value.js's fork is transactional. ▲ **fourier's `/diff` is NOT a general two-point diff** — `on_chain` is only `{head_hash} ∪ {fork_of_hash}` (`visualizations.py:827`), it reads the STORED `atom_diff` and never recomputes, 404s any other pair: **the fixture must not assume a general diff.** ▲ **D9 lock**: visibility `private\|public` (`unlisted` DIES), lifecycle `active\|trashed`, moderation `clear\|withdrawn`. ▲ **X-4 (`/versions` rail) is CHANGED–CONTESTED** — the value-side history leg must not assume its shape | G2 |
| **P2** ⊙ ‡ Diff-leg participation | **TA-4** (census §5 risk 3 · lane-crud §0/§2 R-3/§7 · INTAKE §3 · F-W5 G4) | `lib/crud/atomdiff.ts` + `atomDiff` on `PaletteVersion` excised at T.W1; only the hashing half survives. `J-diff-shape.md §2.5` (line 75) **mandates a path the tree does not hold** — "no ruling" is not a neutral state | Prerequisite **or** explicit re-scope, §1b. ▲ **A-1 widens the restoration to a third limb (the fixture).** Ruling is **F.W5's G4**; F.W8 consumes it, never re-books it. VALUE-SIDE row | **G1** |
| **P3** ‡ The only UI entry | **GCM-1** (BLK-1 FOLDS · SS-C-1 read leg FOLDS) | The modal's only CTA emits `image_slug` → `GalleryView:396` `router.push('/w/${slug}')` → the **caller's IndexedDB draft**, hard-reset to defaults when absent, writing a draft row keyed on **another user's** `image_slug`; the correct loader `loadVisualization` (`workspace.ts:197-232`) has **zero call sites** | F.W4 wires (route → `loadVisualization`); **F.W8 asserts end-to-end**: opening a published visualization loads the **saved entity** with slug + ETag captured, never a draft, and writes no cross-owner draft row. ▲ **Identity guard: BLK-1 and SS-C-1's read leg FOLD here — never re-booked** (the fr-ImageUpload registry-motion event proves the guard must re-run *at* adjudication time). Sibling of F-α (F.W6): publishing twice yields two unrelated roots | **G16** |
| **P4** Adoption surface | **VV-R2-B** (owns the JOINT identity of BLK-1 · BLK-2 · VV-R2-A · VV-R2-E) | Ten of twenty-four workspace-store exports have zero consumers outside the store file; `gallery.ts:168` bypasses the store's own `deleteVisualization`; four of the ten are exactly the `/v/` shell's surface | Read as **ONE gap**: "a single wiring unit at this component, not scattered repairs." F.W4 owns the wiring; **F.W8 owns the acceptance assertion only** — *the CRUD-CONTRACT §1 adoption is the union tranche's own acceptance surface*. Does not re-book BLK-1/BLK-2 | G16 |
| **P5** The verb set is unclientted | **FR-GV-8** ⊕ **FR-GV-27** (rides) | Seven of thirteen `/api/visualizations` operations — remix · publish · unpublish · forks · provenance · diff · versions — have **zero client function**, and that set **is** the walk's verb set (`F8-CLIENT-03..09`). `publish` PATCHes `{visibility}` behind an extra GET (`gallery.ts:223`) that adds a phantom view, while `/publish` and `/unpublish` ship with no wrapper at all | Each gap operation gets a bound client method + consumer state **or an explicit `SERVER_ONLY` law** (36/45 → 45/45). ▲ **K-1: a `@router.` grep is blind to prefixed routers — `gallery.py`'s `@gallery_router.get("/cursor")` reads against 13/44, never zero.** Does **not** repair the 0/45 security descriptions nor the three absent/prune surfaces. Consumes F.W5's register — **F.W8 does not re-derive the enumeration**. Wiring seat = F.W4 (VV-R2-B's single unit); identity/provenance DISPLAY arms FOLD to banked fr-GalleryCard D-7, GCM-25, GCM-3 — do not re-book | G2 |
| **P6** The owning-actor enumeration | **FR-GV-13** (=MISS-LC-2) | `visibilityFilter` is a **producerless** store ref — three hits, all inside `stores/gallery.ts`, zero writers — so `ownerParam()` returns `undefined` unconditionally and the server's whole `owner=="me"` branch (`visualizations.py:305-309`) is dead from this client | Load-bearing here specifically: **the history walk needs `owner=me` to enumerate the actor's own chain**, so a dead branch means the walk cannot run as the owning actor at all. F.W3/W4 owns the control; **F.W8 owns the enumeration dependency** | G2 |
| **P7** ‡ The create leg | **D-4/L-1/C-3** (CanvasControlsDock) ⊕ **fr-GalleryDraftsSection B-2** (=C-C-1∘L-3, `:40`; **FR-GV-1 FOLDS**; record-qualified per R-5 — *not* fr-EquationView B-2, the `budget`-past-`le=50` row at `:48`, which F-W5 books at clause **F2**, §F's one-shared-bound-constant clause) ⊕ **C-6/C-7** (EditorControlsDock) ⊕ ⟨**`C-28` carried here at repair round 5 (REST-41) — as a NAMED FOLD on `D-4`, exactly as the record carries it, never as an identity.** `fr-CanvasControlsDock.md:102`'s `**Full-coverage note**:` prose folds *"C-28 into D-4's F.W5–W8 rider"*; the canonical banks no `C-28` row for this record, and the row that numbered it as an identity in §6a is struck⟩ | Re-entrant and idempotency-free on every hop: `saveVisualization` is an unconditional POST minting a fresh slug per call (`visualizationSlug` written `:358`, never read as a guard); `handlePublish` has no re-entry guard and no `:disabled` — **N clicks ⇒ N public gallery rows**; the drafts path reconciles against `savedSnapshots`, whose ONE writer is the literal `[]` (`workspace.ts:102`); the contour write **declines the `Idempotency-Key` channel its own client core declares** (`:98-99`/`:138-139` declared, `:316-324` unpassed) | **Idempotent-or-declared on both sides.** The union holds both halves in different repos on different verbs and **neither side has both**: fourier's remix carries `idempotency.replay_or_record(...)` (`visualizations.py:612`) with no transaction; value.js's fork is transactional with in-txn source re-read (`forks.ts:94-137`, `:99-105`) with no idempotency key. ▲ FR-GV-1's split law verbatim: *"split F.W3/W4 contract + F.W5-W8 server dedupe"* — **the `:disabled` cure is F.W3/W4's and F.W8 claims no credit for it.** Cross-checks `api-gap-remeasure.md` X.A3 (strong `If-Match` + required `Idempotency-Key` + `matchedCount === 1`) and **X-3 (PATCH TOCTOU / no CAS, VERIFIED-GAP)** | **G10** |
| **P8** The list contract | **FR-GFC-4** (home) ⊕ **FR-GSB-1** (FOLDS) ⊕ **FR-GSB-28** · **fr-GalleryInfiniteGrid C-4 / D-13** FOLD, R-7 sustained (record-qualified at round 2, R2-7.2) | `entries` is featured **∩** the loaded 20-row cursor window with **no `tier` param at ANY layer** (`api.ts:397-402` · `visualizations.py:288-294` · `gallery.py:38-41` · `cursors.py:20-23`); `fetchNextPage` appends so the strip grows during scroll; `resetAndFetch()`/`fetchNextPage()` share one abort key (the literal `"listVisualizations"`, `api.ts:411`; `abortable()` `:54-57`; `:161` defaults every call's signal to it) | The walk **cannot enumerate a chain through a list endpoint that returns an intersection of a filter and a page window**: server-side `tier`/filter param + per-call abort key. ▲ **WAVE-LOCK (fr-GallerySearchBar C-2, verbatim): any F.W5-W8 wiring of `basisFilter` MUST use the banked normaliser (fr-BasisSelector M-10)** — without it the Fourier pill returns zero rows against `active_bases ∈ {fourier-epicycles, fourier-series}` (`GalleryCard.vue:39` has it; GallerySearchBar does not). ▲ **FR-GIG-5: the F.W1 tri-package bump does NOT cure the pagination drain and F.W1 must not be credited for it** — the true cure is a producer change riding the BH relay (SS-6). **fr-GalleryInfiniteGrid C-3 / C-4 / D-13** FOLD by reference — do not re-book. ▲ **RECORD-QUALIFIED AT ROUND 2 (R2-7.2 / R-5), and the homonyms named so the collision that hid three escapes is disarmed** — ⟨cmd⟩ `grep -n "C-3\b\|C-4\b\|D-13" $R/fr-GalleryInfiniteGrid.md` returns this record's own rows: `:50` *"**C-3 — MAJOR (banked) · FOLDS BY REFERENCE to banked FR-GFC-3 (→ F.W5-W8). Do NOT re-book.**"* and `:51` *"**C-4 / D-13 — MAJOR (banked) · FOLDS BY REFERENCE to banked FR-GFC-1 (incl. its MISS-1 dead-controls arm) + GCM-24 (→ F.W3/W4) and FR-GFC-4 (→ F.W5-W8). Do NOT re-book**"*. The **same three tokens spell four OTHER banked identities**: `C-3` at fr-CanvasControlsDock (inside `D-4/L-1/C-3`, which F.W8 books at **P7**) and at fr-FourierShapeExtractor `:54` (D2's block); `C-4` at fr-FourierShapeExtractor `:55` (D2's block); and `D-13` at **fr-GalleryCard `L·M-4 / D-13 / C-8(a)`** — the serializer identity a bare `D-13` match reported as booked here while its bytes were absent (§1's escape 3, now §6a row 39). FR-GSB-28's interleave symptom → SS-13 (magnitude only). **Dissent preserved**: FR-GFC-1 carries two BLOCKER filings; adjudicated MAJOR (blocker-weight) | G2 |

### §J — The join seam and the fixture-direction law (R6-8, three directions)

| row | banked ids | witness (carried) | F.W8's act + locks | gate |
|---|---|---|---|---|
| **J1** ‡ The seam itself — **the wave's reason to exist** | **R6-8 / C31** (intake `lane-fourier-r3-r6.md:142`, adjudicated INTAKE §3 secondary; AUXILIARY §5) · folded-by-reference (all record-qualified, R-5): fr-AnimationControls C-30 · fr-BasisCanvas C-5 · fr-EasingCurvePreview RESOLVER · fr-ContourSettings i-7/C-25 · fr-FourierShapeExtractor C-7 · fr-MorphShapePreview §3 negatives · fr-EditorControlsDock S-7 (the POSITIVE) · fr-CoefficientsSpectrum M-13 · fr-InfoCard §R6-8 | `operation:PATCH:/api/visualizations/{slug}` embeds `"clients": ["client:updateVisualization"]` + `"clientDisposition": "CLIENT_MATCH_SOURCE_DERIVED"`, so C31's client-side edit at `web/src/lib/api.ts:420` (PATCH→PUT) breaks the client↔operation match **and mutates the operation leaf too** — the leaves are structurally non-isolable by construction | **REPRODUCE, then SPLIT.** First the exact two-sided failure — decoded stdout `{"code":"control.wrong-reason","errors":["client.method.visualization-update","operation.method.visualization-update"],"controlId":"R4.C31","verdict":"REJECT"}`, **exit 43**, against `web/src/lib/api.ts:420` + `api/routers/visualizations.py:350`. Then **F8-C31A** (sole mutable target `client.method.visualization-update`; owner result: exactly its client-method leaf fails; non-owner result: the operation-method leaf and all other predicates retain) and **F8-C31B** (the mirror). Verbatim constraint: *"Both must use raw receipts, the same production validator, an owner-only bypass, all-non-owner retention, and no caller-supplied expected code."* **Reproduction is the DIAGNOSTIC; isolation is the ACCEPTANCE.** ▲ Denominator: C31 terminal RED at **30/37**; **32/38 only if lawfully replaced by C31A/C31B**, and the later denominator **must be owner-frozen**. F.W5 STATES the contract clause (operation identity independent of client identity; the join lives in a SEPARATE RELATION); **F.W8 EXECUTES** — one home, two citations. Governance note carried: the R-series' authority is dead (OG-F1 owner-gated; R6 self-declared `NO_SUCCESSOR`) but **the MEASUREMENT enters by adjudication — "zero UNVERIFIED credit", not zero credit** (census ADDENDUM §6.7). Exactly one R-row was refuted corpus-wide (R4-4, a filename transcription) | **G3** |
| **J2** The direction law | **FR-IC-6** (FOLDS → L·m-7) | The client leaf invents an operation-value domain (`EquationTier`) the operation record never constrains (`tier: str`); the parse is throw-guarded but shape-unvalidated (`:36-41`) — R6-8's **mirror image** | **THE FIXTURE-DIRECTION LAW, this wave's own acceptance constraint: every fixture is generated FROM the operation model, never from the client union.** Verbatim consequence: *"a fixture generated from the client union will never exercise the `??` branch, so the relabel bug is invisible to exactly the tests meant to catch it."* Generalised: *the operation must own its value domains, or the client's narrowing is fiction.* Sharpening adopted: `sessionStorage` keys are schema-versioned `eq-tab-*-v2` (`useEquationCache.ts:7-8`), blunting the stale-schema limb — **the backend-adds-a-tier limb survives whole**. F.W4 owns the client guard; **F.W8 enforces the direction across the whole register** | **G4** |
| **J3** The companion clause | **fr-CoefficientsSpectrum M-13 = that record's own C-7** (`:65`; record-qualified per R-5 — *not* fr-BasisCanvas C-7, which F-W5 books at clause **E9** (§E, *"One shape, one name"*), nor fr-FourierShapeExtractor C-7, which sits in D2's block) | The epicycle response is `data: dict`; its only typed component model (`EpicycleComponentDTO`) is dead with one self-hit and disagrees with the wire; `BasisComponent` is hand-maintained against a Python dict literal — **no derivable join** | Verbatim: ***an operation's response model must be structurally typed, or the leaf is unjoinable in principle.*** Consequence owned here: *"the value.js↔fourier conformance fixtures (census §4 F.W8 / FN-6) cannot be generated for this operation as it stands"* — so each such operation is **typed at F.W5 or NAMED AND STRUCK in the register with its reason**. The equation route does it right in the same API — the in-tree exemplar. ▲ Cross-edge: **F.W5's `operation-register.md` must carry a per-operation joinable/unjoinable cell or G4 is unfalsifiable prose** | G4 |
| **J4** Bounds are generated, not invented | **fr-BasisSelector m-7** (= `C-6-as-rescoped`) ⟨*record-qualified at repair round 5, REST-47; `m-7` is held by **7** records in canonical §2 and **17** files in the frozen corpus, and this file books `C-6/C-7` (EditorControlsDock) at §3 P7 — two different `C-6`s one screen apart*⟩; riders/folds: fr-SliderControl **R-16** (FOLDS, books nothing) · fr-ContourSettings **B-1** · fr-FrequencyGraph **FR-FG-21** · fr-EquationView **M-SB** · fr-FunctionInput **C-8/N-4** | The client's numeric window `[1,500]` is invented at the leaf and restated ~6× per control, matching **none** of the server's six divergent windows (256 / 4096×3 / 200-unbounded / 200-le) | Constructive cure verbatim: ***generate client bounds from the operation models (the R6-8 dual)*** — F.W5 states the RULE (D7: *every request field is bounded, or the exception is named*; *operation records must carry field DOMAINS*); **F.W8 GENERATES them inside the fixtures**. B-1's rider carried: *the typed range-checked descriptor table for all six fields rides an F.W5–W8 rider — the contract seam owes CLIENT-DERIVABLE BOUNDS*. FR-FG-21 rides (`maxBars` unclamped public prop → canvas dimension, over the family's only unbounded request model). ▲ **Preserve the enforcement SHAPE while fixing the numbers** — this control is the only thing between a hand-typed `-9e9` and an FFT call (`ComputeEpicyclesRequest`/`ComputeBasesRequest` carry no `Field`, no bounds, no validator). ▲ **K-3 lock, verbatim: m-7's straddle-a-422 scenario is REFUTED from source (`AnimationData` is a server-computed embedded document; the client-reachable bound 4096 ⊇ [1,500]) — v2 must not cite it, and neither may the fixtures.** M-SB/N-4 are F.W5's D7 clause: **F.W8 consumes the bound, does not author it**. ▲ **`fr-AdminUserList FR-AUL-46` is OUT of this row's banked-ids cell (R2-11, round 2) — its ONE home is F-W5 clause D7; the cap bound is CITED, NOT BOOKED here**, which is exactly what this cell's own act sentence already claims to do. ⟨cmd⟩ `sed -n '89p' $R/fr-AdminUserList.md \| tail -c 120` → that row's terminal routing cell, verbatim: *"**F.W5** — surface the cap in the shared contract; client-side chunk or block"*; ⟨cmd⟩ `grep -cE "F\.W5[-–]W8" $R/fr-AdminUserList.md` → **1** — the record's boilerplate routing-law line only, so **the row is not band-routed at all** and booking it here was an out-of-band booking against this file's own *"no §3 row re-books an F.W5 clause"* (§5c) and R-5's one-home rule. ⟨cmd⟩ `grep -n "FR-AUL-46" waves/F-W5.md` → the **D7** clause row, which carries the id in its witness cell (*"FR-AUL-46 (LC-miss-M5)"*) and its disposition in its act cell (*"FR-AUL-46: surface the cap; client chunks or blocks; the ApiProblem-fallback clause adopted"*). **doubleHomed = 0.** | G4 |
| **J5** Serialization bypass | **GCM-10 ⊕ GCM-55** | `json.dumps` bypasses the Pydantic model on `visualizations.py`: `tier` is absent from the model under `extra="forbid"` yet written by `admin.py:183/:432/:438` and passed raw by `_public_doc` (a full-strength Bookmark chip with an empty label over `undefined`); `likes` is served raw while the store's `likes ?? 0` and the template's bare `{{ entry.likes }}` assert **opposite nullabilities — one is wrong by construction** | Cure at the **model** (a `tier` field WITH A DEFAULT; GCM-55 rides the same fix), not at the render guard; F.W3/W4 owns the `v-else-if`. Acceptance: **a fixture generated from the model matches the wire byte-for-byte, or the bypass is NAMED in the register.** ▲ **REGISTRY-INTEGRITY LOCK — banked at **F-W5 §3 gate G22**, *"dangling-cite resolution (registry integrity)"*, cited by GATE ID and never by line (R2-2; the round-1 `:251` address is struck as an address, its bytes re-verified unchanged), re-anchored per R-1f** (the prior "§6c" address does not exist in `F-W5.md`). ⟨cmd⟩ `grep -n 'dangling-cite resolution' waves/F-W5.md` → the G22 row, verbatim: *"GCM-10's cure cites a companion row **"MF-9"** that resolves to NOTHING in the adjudicated registry — `grep -rn "MF-9" $V/docs/tranches/V/megatranche/registry/adjudicated/` → exactly ONE hit, the dangling citation inside `fr-GalleryCardModal.md:56` itself (re-measured 2026-08-28)"*; **MF-9 resolved or STRUCK before v2 quotes GCM-10's cure**, G22's GREEN owner being F.W5 itself (unit e). If struck, the cure is quoted without it | **G5** |
| **J6** Validate at the boundary | **FR-CP-16** (= **fr-CoefficientsPanel C-17** / L·A-3 — record-qualified per R-5; *not* fr-BasisCanvas C-17, the overlay double-fetch rider at `:102`, which F-W5 books at clause **E15**, §E's ONE-transport clause); folds: fr-FrequencyGraph **FR-FG-13** + C-8/L-m4 · **fr-FourierShapeExtractor C-15** | The descending-amplitude invariant is enforced only in Python (`epicycles.py:54`), expressed nowhere in TypeScript, and the whole **45-operation client surface lands as ONE unchecked `(await res.json()) as T`** (`api.ts:192`); IndexedDB draft rehydration (`workspace.ts:171`) replays older payloads through the same zero checking, so a violation surfaces as **`NaN%` bar widths, not an error** | The fixtures are the instrument that proves a boundary exists — *validate-at-boundary for the epicycle payload at minimum*. F.W4 owns the ordering brand and reading `n_components`. Sibling clause folded: **C-15** — the producer's output type unenforced end-to-end (anonymous literal → untyped JSON → independently-declared `FourierPathData` → `as any` at both consumers, **with `resolveJsonModule` already providing the structural type being discarded**). FR-FG-13's cross-ref names FR-CP-16 as the enforcement point — one home, two citations | **G11** |
| **J7** ⊙ One generated shape | **FR-AFP-71** (named cause) ⊕ **FR-AFP-36** (the drift) ⊕ **GAB-12** (same mechanism, gallery twin) | Hand-maintained god-module twins with no codegen — `api.ts` 672 L / `types.ts` 391 L, **three hand-maintained copies of the flagged shape (one already rotted)**, three dead server models encoding the retired contract (`FlagRequest` · `FlaggedEntryInfo` with rotted `user_slug` · offset `FlaggedListResponse`), **no `response_model` anywhere**; the gallery twin (`types.ts:110-118` ↔ `api/models/gallery.py`) is isomorphic today with `res.json()` unvalidated, and drift degrades into **six silent em-dashes with the tier tint dropped — indistinguishable from an empty database, no console error, no toast, no test** | *"One generated/checked shape"*; dead models delete; **module split is the union's call**. ▲ **K9 lock, verbatim: do NOT delete the `?? item.slug` fallbacks** — `admin.py` emits `doc.get("image_slug")` unvalidated, so they are defensive and the "five dead branches" limb is **NOT PROVEN**. F.W5 rules (its G11/FR-AFP-71); **F.W8 is the wave whose register either IS the generated shape or documents why it is not.** GAB-12's test seam is also an F.W9/W10 coverage emission — one home, two citations | **G12** |

### §R — Round-trip, persistence, cache identity (the derive and history legs)

| row | banked ids | witness (carried) | F.W8's act + locks | gate |
|---|---|---|---|---|
| **R1** Replay reproduces its own frame | **BC-9 / C-6 / D-20 ⊕ SS-C-2** | Half the persisted `AnimationSettings` contract is write-only and **three-way divergent INCLUDING UNITS** — server `fps 30 / duration 30.0 s / max_circles 80` (`shared.py:65-71`), client `60 / 5000 ms / 100` (`defaults.ts`), renderer `ref(80)` (`:47`) and `20000 ms` (`animation.ts:23`); only easing/speed/active_bases round-trip; `speed` agrees **by luck**, unconstrained on both sides (`shared.py:70`, no validator) | Verbatim: *"settings round-trip is a persistence-contract row; the CRUD/provenance union owns the three-way reconciliation"* — **with server-side units**. Acceptance instrument: **a gallery replay must reproduce its own frame.** ▲ The `duration` unit fork (20000 ms store / 5000 ms client / 30.0 s server — **a 1000× fork across three declarations**) must be reconciled in the contract *before* the fixture can assert anything — cross-edge to F.W5. ▲ Partial fold: `duration = ref(20000)` zero-writers stays banked at **BC-20** — not re-booked | **G6** |
| **R2** The update verb reaches every atom | **SS-C-1 (write leg)** | The persisted `speed` atom is settable **only at CREATE/FORK** — `VisualizationUpdate` (`visualization.py:198-210`) and `VisualizationPatch` (`types.ts:256-262`) are the same five fields with `extra="forbid"` — while `animation_settings` is **atom 4-of-5 of the version-identity set** (`atomdiff.py:38`): a preference the control claims to set is silently non-persistent | The update verb reaches **every atom of the version-identity set, or the atom set is re-declared to exclude what it cannot touch — stated either way, never left divergent.** The instrument is the derive→history leg: **mutate atom 4, walk the chain, assert a version row exists carrying it.** F.W5 states the atom set; F.W8 asserts reachability. **Read leg FOLDED to GCM-1.** Severity grades the CONTRACT, not the control (demoted from the challenge's BLOCKER at component altitude) | G6 |
| **R3** ⊙ Cardinality admission | **M-9** (BasisSelector) = L-M4 / C-20 | The deliberately-minted "off" state `[]` (`:103-104`, *"canvas handles it gracefully"*) is **unrepresentable end-to-end and laundered on every path** — server forbids it (`min_length=1`), the store substitutes (`workspace.ts:351-353`, `gallery.ts:252`), the loader refuses to restore it (`:53`) and force-prepends on first data (`:109-114`): honoured on screen, destroyed on save, **zero diagnostic** | Verbatim: ***"Needs an owner ruling: admit `[]` in the contract, or stop minting it — the silent rewrite must not survive either way."*** The **cardinality axis of the R6-8 seam**: the fixture asserts that a state the client can mint either **survives the round trip or is refused with a diagnostic, never laundered**. F.W5's `OWNER-RULINGS-F.W5.md` owns the ruling; **F.W8 owns the fixture that proves whichever branch lands — it may not author on a guessed branch.** ▲ **Anchor-drift WARNING recorded, not an assertion** (§4 discipline 1): the corpus cites `:184/:279`, a read-only seat inspection gives `:186/:280` — the divergence is booked as a **re-resolve obligation after F.W0 (D-19)**; this spec adopts neither pair as its anchor | **G7** |
| **R4** ‡ Cache identity | **B-4** (ContourSettings) = C-1 ∘ C-25/R6-8, ⊕ **i-7** (folds) | `extraction_cache_key` is a **CLOSED 10-field `json.dumps` literal** (`image_storage.py:250-266`) omitting `ml_threshold`/`ml_detail_threshold` — the two fields this control solely produces — while `images.py:219-226` short-circuits on the cache hit **BEFORE** `compute_contours`: the user drags 0.50→0.85, waits out the debounce, receives the 0.50 contour with **no signal**. `auto` is the shipped default, so the control is live on every fresh workspace | Contract constraint verbatim: ***an operation's cache identity must be a superset of the request fields the operation consumes*** — the R6-8 seam made product-visible. **F.W8's derive leg is where the cross-repo fixture lives**; the client-side regression fixture (assert `contour_hash` **instability** across an ML-threshold change) is F.W3/W4's + the SS-13 probe. **i-7/C-25 folds here — never re-booked.** *The derive step cannot be trusted while a cache key under-determines its own operation.* ▲ **Round 2 (R2-7.1.1) — `fr-ContourSettings M-13` (= LC-missed-2, the PRIMARY band routing) is CITED here and BOOKED NOWHERE by F.W8**: it sits beside `B-4 ⊕ i-7` in the same record and in the same derive subject matter (an editor-saved contour must be a first-class input to compute), so the assertion seat names it — but its **STATING home is F-W5 clause E14**, and §6a row 37 carries the exclusion-with-reason. ⟨cmd⟩ `grep -n '\*\*E14' waves/F-W5.md` → the clause row *"**E14** Contour provenance — an editor-saved contour is FIRST CLASS ‡"* over the witness cell *"**fr-ContourSettings M-13 ⊕ fr-BasisSelector M-14** *(NOT fr-CoefficientsSpectrum M-13 — the A5 homonym; U-12 qualification)*"*. **F.W8 asserts over E14; it states none of it.** Magnitude → SS-13 | **G8** |
| **R5** The flush seam | **MISSED-E** (EasingPicker save-race) | The picker's write reaches persistence through a 500 ms `watchDebounced` (`VisualizationView:53-64`) with **no flush-on-save**, while `saveVisualization()` reads `toRaw(animationSettings.value)` synchronously (`workspace.ts:344-357`) — select-then-save inside the window persists the **previous** curve | Persistence semantics: ***the flush seam is the banked L-12/C-25 `setEasing` action*** — **do not invent a second seam.** The round-trip fixture asserts **the last user-visible value is the persisted value**. Narrow reachability (menu dismiss + save within 500 ms) **honestly stated, not inflated**. Composes with **SS-C-8** (`{once:true}` loader seeding + in-place `/w/:slug` nav + the same 500 ms writer ⇒ workspace B's persisted speed clobbered by workspace A's live value) — SS-C-8's repair seat is F.W4, its ordering witness SS-13; **F.W8 asserts only the round trip** | G6 |
| **R6** The liveness predicate | **M-β4** (BasisCanvas) ⊕ **L·m-6 = C·D-14** (EquationView) ⊕ **PP-DEADSEAM = C-12** (PathPreview) | Three envelope fields dead in one direction or both: `EpicycleData.trace` — a 3000-sample polyline computed per response — has **zero readers** yet rides `structuredClone` into every IndexedDB draft (`workspace.ts:95-104`); `reconstructed_points` (500+500 floats) is fetched, sessionStorage-persisted whole and read by **nothing** (`equations.py:114`/`:128`); `preview_path` is written `""` at all three server sites (`assets.py:85`, `image_storage.py:318`, `responses.py:22`) with the client leaf unreachable — **zero implementation on either side** | **THE LIVENESS PREDICATE — verbatim: "beside R6-8's identity/isolation predicate, genuinely new to the register." As contract law: every field in the envelope has a PRODUCER and a CONSUMER, or it is dropped.** Per-field disposition: *drop the field, or consume it*. **The walk is the instrument** — a create → derive → diff → remix → history traversal that touches every envelope field and **reports the unconsumed set**. M-β4 is *"the R6-8 seam's converse: server computes, nobody consumes"* — the third direction of one seam, deduped by mechanism with **all three ids preserved**. F.W5 states the predicate; F.W8 measures it | **G13** |

### §M — Measurement integrity (the probe's own honesty)

| row | banked ids | witness (carried) | F.W8's act + locks | gate |
|---|---|---|---|---|
| **M1** Non-perturbation | **FR-GV-12** (home) ⊕ **VV-R2-A** ⊕ **FR-GV-24** (rides) | `GET /api/visualizations/{slug}` is an unsafe read that **cannot observe its own side effect** — `find_one` `:256`, `$inc {"views":1}` `:268-270`, `_public_doc` serialising the **pre**-increment doc `:272` — and the same verb is the ETag-capture path (`gallery.ts:167`, `:223`), so every ETag cache-miss on publish or soft-delete **adds a phantom view to the row being mutated**; a freshly minted draft's ETag lives in a different store (`workspace.visualizationETag`, `workspace.ts:359`) so **the map ALWAYS misses**. No `viewed_ips` dedup exists while `liked_ips` does | ***PROBE NON-PERTURBATION: the union prototype's own READS must not mutate the entity under measurement*** — either the walk **declares and subtracts** its perturbation, or the read verb stops mutating (a mutating GET is unsafe against any proxy/prefetch, **RFC 9110 §9.2.1**). VV-R2-A's cheap arm is F.W4's (publish adopts `store.setVisibility("public")`, `workspace.ts:373-389` — the exact ETag-guarded PATCH with **zero callers repo-wide** — dropping one round trip and the phantom view; it rides BLK-2's repair unit). ▲ **Two probe-suppression locks: K12 and FR-GV-24 — SS-13 spends no probe re-deriving them.** ▲ **FR-GV-24 cure-integrity lock (banked GCM-3 R-7 precision): repair tests must NOT assert a re-open increment — the guard DOES work within a session; the defect is SCOPE, not absence** | **G9** |
| **M2** The authenticated leg's honesty | **FR-USB-24 ⊕ FR-USB-23** | The login path opts into the silent **≤60 s** 429 auto-retry (`retryOn429 ?? true`, two sleeps ≤30 s) against a 5/60 s budget with no feedback and no cancel (Escape doesn't abort — FR-USB-27); the dead session subsystem (~17 % of the store, zero external call sites) hides a **latent identity-mixing bootstrap** — `logout` never calls `clearSession`, and the `else if (sessionToken)` arm would re-attach an anonymous identity on reload if revived | Verbatim: ***auth mutations pass `retryOn429: false` or surface the wait*** — **a horizon probe that silently retries for 60 s manufactures false timings and false greens in the authenticated walk.** The session subsystem is **retired or wired, never left latent**, because R-7 (user provenance) is a union seam and an identity-mixing bootstrap **poisons every attribution the history walk records**. The value-side mirror is **V-γ** (nullable `userSlug` ⇒ an unattributable edit mutates the palette and writes **no** version row: `crud.ts:119`, `crud.ts:204`, `versions.ts:147`) — VALUE-SIDE row, never re-booked as a fourier defect. ▲ **Session-token-at-rest is the one place value.js is strictly ahead** (SHA-256 digest as `_id` vs fourier's plaintext UUIDv4) — **recorded so the union does not regress it** | G9 |
| **M3** Batch writes preserve orthogonal state | **FR-GV-9** (= C·M-8 second clause) | Batch Unfeature sets `{"$set": {"tier": "normal"}}` unconditionally via `update_many` (`admin.py:435-440`) — **unfeaturing a SAVED-tier entry silently erases the saved tier** — while the `:423-425` dialog copy is honest about the mechanism and the `:326-333` button label is not | *Tier is a state machine, not a scalar collapse.* **F.W8 owns only the batch-write state-preservation assertion**, recorded in the walk's admin leg (**no separate gate — L-19: a gate here would name no witness the walk does not already carry**). Composes with **FR-AFP-10** (tier↔flag: "Mark acceptable (save)" never dequeues — `set_tier` writes `{tier, updated_at}` only, the flagged listing has no tier predicate) and **FR-AFP-70** (`handleSetTier` hardwired to `'saved'`, no inverse — an unrecoverable one-way write). **Their SEMANTICS ruling is F.W5/F.W6's** — one home, two citations | G2 (walk record, admin leg) |
| **M4** Authority asserted per operation | **R3-7b** (INTAKE §3 primary, un-refuted); instantiated at GCM-52 · **fr-GalleryDraftsSection F-4** (= `C-C-8` · `FR-AFP-4`-as-alias · `R3-7b`) ⊕ **fr-AdminFlaggedPanel FR-AFP-4** — **two DISTINCT canonical band rows, F-W5 clauses C1 and its own** ⟨**re-cut at repair round 5, REST-49 (P5-14).** The cell collapsed them into one unqualified token `F-4 (= banked FR-AFP-4)`; the canonical banks `` | `F-4` | `C-C-8` · `FR-AFP-4` · `R3-7b` | `F.W5-W8` | **F.W5-W8** | `` at **fr-GalleryDraftsSection** and `` | `FR-AFP-4` | — | `F.W5-W8` | **F.W5-W8** | `` at **fr-AdminFlaggedPanel**, and F-W5 homes them at **two different clauses**. ⊘ **R-5's exact prohibition, at a token the keystone had already separated — and this same cell is where `m-15` entered bare (REST-46), so both halves of fr-GalleryDraftsSection's band pair were unqualified at one site.**⟩ · fr-EditorControlsDock C-6 | **OpenAPI security-description coverage is 0 of 45** — the admin arm inside the denominator — instantiated at live leaves: the modal's only API call is unauthenticated, visibility-unchecked and **publicly cacheable for a day** (a draft/unlisted viz's source image world-readable by slug); the drafts thumbnail GET carries the same posture; the contour write is an **unauthenticated write from a client that always authenticates** | *"Auth parity cannot be specified over a surface documenting none"* — **and it cannot be ACCEPTED without the walk: the authenticated leg asserts, per operation, the authority class F.W5's register declares.** F.W5 states; F.W8 asserts. **F.W8 does not re-derive the 0/45 figure; it consumes it.** ▲ **Lock — banked at **F-W5 §2 clause C2** (§C, *"Image remediation contract — ONE unit"*), cited by CLAUSE ID and never by line (R2-2; the round-1 `:114` address is struck as an address, its bytes re-verified unchanged), re-anchored per R-1f** (the prior "§6b" address does not exist in `F-W5.md`). ⟨cmd⟩ `grep -n 'm-15 is CROSS-REFERENCED' waves/F-W5.md` → the C2 row, verbatim: *"**m-15 is CROSS-REFERENCED, NOT MERGED with F-4**: F-4 books *who can fetch*; m-15 books *what they see*."* FR-AFP-4's image-remediation contract is F.W5/F.W6's; the deployment auth-proxy question → SS-13 | G2 |
| **M5** The denominator of record | **R3-7c ⊕ X-3** (INTAKE §3 + census ADDENDUM adopted-fact 4 + AUXILIARY §4 `F8-CLIENT-01..09`) | 45 total decorators = **30 public-non-admin + 13 admin + 1 app + 1 gallery**; 36 client edges exist; **9 operations have none** — triangulating census risk 9 and fourier's own M.W10 "7 endpoints, 0 callers" | Cite the **45/30/13 triple** as the denominator of record; every gap operation receives an authenticated client edge **or an explicit `SERVER_ONLY` law** before the walk may claim coverage (36/45 → 45/45); per-gap results enumerated as `F8-CLIENT-01..09` (**-01** `GET /api/gallery/cursor`; **-02** `GET /api/health` *"without treating health as product readiness"*; **-03** diff UI/error receiver; **-04** forks empty/error; **-05** provenance; **-06** version-history; **-07** publish auth/success/error; **-08** remix new-resource/conflict; **-09** unpublish auth/success/error). ▲ **K-1: prefixed-router blindness — read against 13/44, never zero.** ▲ **Method law inherited: *"a registry audit that re-hashes bytes but never re-derives products proves the bytes unchanged, not the summary true"*** — the F.W4/F.W5 evidence standard, binding on G15's closure. ▲ **Identity ordering is `UTF8_BYTEWISE_CODEPOINT`, never `localeCompare`** (binds register row ordering and any digest). ▲ **X-9: publish ONE member-scope law before any percentage** — **no F.W8 coverage percentage is published without it.** Also carried: *"an operation record embedding derived client back-references cannot attribute a defect to one side of the seam"* (the R6-8 root) | G2 · G15 |

### §D — The deliverable, and the standing tripwire

| row | banked ids | witness (carried) | F.W8's act + locks | gate |
|---|---|---|---|---|
| **D1** ‡ Fixtures both directions | **FN-6 ⊕ FN-5** (`R.W6.md:65,:122` · `R/PROGRESS.md:124` · `dispatch-homes.md:20,:26` · census §3c) | FN-6 was BOOKED *"value.js lands the fixture; fourier wires the reader"* — but the correspondence is one-way in fact and law: **four unanswered value→fourier letters, zero inbound rows from fourier in the V INBOX**. ▲ **AMENDMENT A-1 (§1b): the value-side fixture was landed at `7351297f`/`8a2a617e` and then DELETED at `a8ff7792` with atomdiff — 0 extant, 1 landed-then-excised, 0 reciprocated** | F.W8 authors the register for **BOTH directions** and relays it. ▲ **FN-6's reader strategy stays fourier-owned: "value.js neither dictates it nor reads from fourier's tree"** (dispatch-homes A.1; vendor-a-copy recommended, sibling-read is fourier's alternative). Guarded by **FN-5**, the contract-currency invariant, sequencing rider **"BEFORE or WITH fourier M.W10"**. ▲ **`canonical_digest` remains SKIPPED-SANCTIONED** (fourier sorts keys, value.js hand-orders literals; adoption = a stored-hash migration, not parity) — **a fixture that re-opens it fails G14**; the three named twin divergences stay un-unified BY DESIGN. FN-5 names exactly what re-opens the twin question: `api/src/lib/crud/atomdiff.ts`, `api/src/hash.ts`, the `PaletteVersion` shape (**A-3: `model.ts:84`, not `models.ts:153`**), the URN catalog. ▲ **Test-isolation law**: the api suite must run where fourier is **not** checked out, so each side holds its own in-tree copy and the binding is by **TRANSCRIPTION**. E13: one append-only INBOX row. **Dissent recorded, not re-litigated**: R.W6's §11-Q9 fixture-home question was resolved to *"value.js lands, in-tree, read locally"* (M4) over the proto's fourier-tree recommendation — **F.W8 does not reopen it** | **G14** |
| **D2** The geometry tripwire — **a LOCK, not a gate** | **fr-FourierShapeExtractor pipeline block** — every id below is that record's, record-qualified per R-5 so none collides with the homonyms elsewhere in the corpus: **L-B1** (`:51`) · **L-B2 / C-2** (`:52`, one identity — *not* fr-ContourEditorCanvas C-2, which is F-W5 E17) · **L-M1** (`:53`) · **C-3** (`:54`) · **C-4** (`:55`) · **L-M3 / C-6** (`:56`, one identity — *not* fr-FunctionInput L-M3, which is F-W5 F6) · **L-M5 / C-5** (`:57`, ONE identity: the record books a single row and the prior draft listed its two halves as two rows) · **C-7** (`:59`) · **L-B3** (`:71`) · **L-M2** (`:72`) · **L-m3 / C-10** (`:81`) · **L-m5** (`:83`) · **M-7** (`:85`) · **M-8** (`:86`) · **M-9** (`:87`, *not* fr-BasisSelector M-9, which is R3/G7) · **M-11** (`:88`) · **C-14** (`:90`) · **C-15** (`:91`) — **18 rows, one identity block.** ▲ **Count corrected (D-5)**: the prior "17 rows" was arithmetically consistent only by accident — the `L-M5`/`C-5` double-listing cancelled the omission of **C-3**, which is now **admitted into the block** (it remains a standing lock in the act cell besides). 18 reconciles exactly to the record's 18 band-routed **rows**. ▲ **The reconciliation is stated at row altitude, and the gap to a bare count is named here so no later seat reads it as a drop (resumption sitting, 2026-08-28)**: ⟨cmd⟩ `grep -nE "F\.W5[-–]W8" $R/fr-FourierShapeExtractor.md \| cut -d: -f1` → **22** lines — `:16 :41 :51 :52 :53 :54 :55 :56 :57 :59 :71 :72 :81 :83 :85 :86 :87 :88 :90 :91 :149 :157`. The four non-row lines are the record's **own prose about its routings, not routings**: `:16` the nine reader-vs-reader severity splits, `:41` the `Routes:` boilerplate header, `:149` reader-1's preserved dissent, `:157` the terminal VERDICT tally. **18 rows + 4 record-level prose lines = 22**, which is why a bare `grep -c` over this record is not the block's denominator and the block is 18 | The canonical-geometry pipeline has diverged from the artifact it generates and its only producer is **untracked, broken at import, parameter-diverged**: shipped `moon.json` is not derivable from the component's Moon SVG (proximity 0.037 / median 2.93 / p90 37.57 / max 44.83 against 1.23-unit spacing); `.gitignore:53 scripts/*` swallows both `precompute_svg_fourier.py` and `raw-contours.json`; `:40` imports `order_contours` which **no module defines**; `:139-142` hard-codes 100/12 against the shipped 50/10 | ▲ **TRIPWIRE, verbatim: "if any wave attempts regeneration before F.W5–W8 lands the pipeline, L-B1 and L-B2/C-2 REVIVE AT BLOCKER with the DO-NOT-REGENERATE rider as the tripwire. DO-NOT-REGENERATE on `master`."** **F-W5 G1c/G16 already banks it — F.W8 does NOT re-book it**; it is carried here solely as a **binding lock on the prototype: the union walk must not regenerate canonical geometry.** ▲ **fr-FourierShapeExtractor C-3 lock (`:54`): NO closure heuristic** (44.08 closed vs 44.97 open overlap — the closed/open flag must be **CARRIED, not inferred**); the id now sits **both** in the identity block above and here as a standing lock. ▲ **M-2 lock: the tracked in-process generator exists for 5 of 7 assets.** Cure shapes carried for the owning wave: the pipeline decision names which stage-0 survives (C-7); `raw-contours.json` TRACKED (L-B3 rides L-B2/C-2); one named interface end-to-end (C-15 + C-5); a uniform post-pass or contract doc for `samplesPerPath` (C-4 — *a parameter named "samples per path" produced a 4-point contour*); diagnostics on the three silent-drop gates (L-M3 — the mechanism that makes L-B1 structurally unobservable at the human seam). ▲ **The pipeline DECISION has NO wave owner in the census taxonomy — F.W8 declares it a cross-edge and requests adjudication at the spine (candidate home: F.W6).** "What `moon.json` WAS generated from" → SS-13 / undetermined. **Dissent PRESERVED WITH ITS REVIVAL CONDITION**: reader-1's BLOCKER positions on L-B1 and L-B2/C-2 were demoted to MAJOR on latency and M-2's repair-shrink; **they revive at BLOCKER the moment any wave attempts regeneration before the pipeline lands** | — (lock; banked at F-W5 G16) |

---

## 4. Gates — 16, all born-RED

`V=/Users/mkbabb/Programming/value.js` · `F=/Users/mkbabb/Programming/fourier-analysis` (read-only) · `R=$V/docs/tranches/V/megatranche/registry/adjudicated` · `C=$V/docs/tranches/X/fourier/conformance/CENSUS-CANONICAL.md` (the sole census operand). ▲ **ROUND-4 (R4-2.3): this line is a convenience, NOT the declaration of record.** A ⟨cmd⟩ five sections above its variable is a phantom witness, so **every block in this file that consumes `$R` or `$C` now declares it beside its own first use** — §1, §3, §6a and this section — and a later seat that finds an undeclared variable in a published command here convicts it on sight (R4-2.5) rather than reading intent into it.

**Two reading disciplines, binding.** **(1) MEASURE-AT-OPEN** — every fourier figure and anchor is re-confirmed by F.W0 before citation (D-19); **this file re-asserts none as fact.** Where a seat READ (permitted under §0's read-only-measurement carve-out) diverges from the corpus, the divergence is **RECORDED as a re-resolve obligation** — never adopted as this file's anchor and never re-booked as a fourier defect; §3 R3's `:184/:279` → `:186/:280` note is exactly that shape and nothing more. Value-side measurements dated **2026-08-28** are taken read-only and are **not under the F.W0 lock**. **(2) Split verdict** — a gate closes *for F.W8* when the fixture/assertion is **authored with its disposition**; it goes **GREEN** only when the named owner lands the change. **F.W8 never claims a GREEN it did not execute.**

**No gate is added.** 16, each keyed by id to a banked witness in the §3 rows; G14 absorbs amendment A-1 and G15 absorbs A-2 because both are **corrections to existing witnesses, not new obligations** (L-19). The repair rounds re-based G15's operand twice — on the registry at round 2 (R-3 / R-5, homing leg re-worded dischargeable) and on **`CENSUS-CANONICAL.md` at round 4** (R4-3.2 / R4-10, the record-side denominator withdrawn entirely) — both **operand corrections, never a seventeenth gate**. ▲ **A-2 is no longer absorbed as a witness but demoted to a dated detector reading** at G15's own cell, since a spelling census cannot be a witness to a roster it cannot enumerate (PASS-4 D-1).

| # | gate | born-RED witness — command / carried anchor | clears when | GREEN owner |
|---|---|---|---|---|
| **G1** ⊙ | TA-4 disposition declared | **measured 2026-08-28**: `grep -rn "atomdiff\|atomDiff\|AtomDiff" $V/api/src $V/src` → **1 hit**, a comment at `api/src/modules/palette/__tests__/palettes-forks.test.ts:9` recording the T.W1 excision · `ls $V/api/src/lib` → *No such file or directory* · `ls $V/api/dist/lib/crud/` → 4 untracked build artifacts (the corpse) · `git -C $V log --oneline --all -- api/test/conformance/diff.test.ts` → `7351297f` … **`a8ff7792`**, and `git show --name-status a8ff7792` → **`D` both `api/src/lib/crud/atomdiff.ts` and `api/test/conformance/diff.test.ts`**. `J-diff-shape.md §2.5` (line 75) mandates a path the tree does not hold | F.W5's **G4 ruling lands** AND this spec carries either **(a)** TA-4 restoration as a named PREREQUISITE (**all three limbs**, owner = the value.js API row) or **(b)** an EXPLICIT re-scope of the diff leg to fourier-only with the one-sidedness stated. **Silence is RED** | owner → value.js API row |
| **G2** | five-verb walk has a client edge on both sides | **FR-GV-8**: 7 of 13 `/api/visualizations` operations (remix · publish · unpublish · forks · provenance · diff · versions) have **zero client function** — independently enumerated `F8-CLIENT-03..09`; edges 36/45, 9 gaps. Probe: `union-prototype-walk.md` must carry **10 cells (5 legs × 2 object kinds)**, each with a bound client method + named consumer state, or `SERVER_ONLY` | all 10 cells carry a disposition **and an authority class** (M4). **0 of 5 legs clear on the fourier side today.** ▲ K-1: 13/44, never zero · ▲ X-9 before any percentage | F.W1/F.W4 (clients) · fourier API row (strikes) |
| **G3** ⊙ | two-sided C31 reproduced, then isolated | decoded stdout `{"code":"control.wrong-reason","errors":["client.method.visualization-update","operation.method.visualization-update"],"controlId":"R4.C31","verdict":"REJECT"}`, **exit 43**, against live `web/src/lib/api.ts:420` + `api/routers/visualizations.py:350`; fourier controls **terminal RED at 30/37** | the pair (i) **reproduces exactly** and (ii) `F8-C31A`/`F8-C31B` isolate each leaf under the stated constraints — raw receipts, same production validator, owner-only bypass, all-non-owner retention, **no caller-supplied expected code**. **0 of 2 controls exist. The later denominator must be OWNER-FROZEN** (32/38 only if lawfully replaced) | F.W8 (unit c) + owner (denominator) |
| **G4** | every fixture generated FROM the operation model | **FR-IC-6** (client-invented `EquationTier` over server `tier: str`) · **m-7** (`[1,500]` matching none of six windows) · **fr-CoefficientsSpectrum M-13** (`data: dict`; `EpicycleComponentDTO` dead with one self-hit; `BasisComponent` hand-maintained against a Python dict literal) | every register row names its **source operation model**; every structurally-untyped operation is **typed at F.W5 or NAMED AND STRUCK with its reason**. **A row sourced from a client type is a gate failure by construction.** ▲ Requires F.W5's per-operation joinable/unjoinable cell, or this gate is prose | F.W5 (typing) · F.W8 (register) |
| **G5** | model output byte-matches the wire | **GCM-10 + GCM-55**: `json.dumps` bypasses the model; `tier` absent under `extra="forbid"` yet written by `admin.py:183/:432/:438`; `likes` raw against `likes ?? 0` + bare `{{ entry.likes }}` — **two fields provably divergent** | a model-generated fixture matches the wire for every field, or **each bypass is named in the register with its reason**. ▲ **Blocked on resolving-or-striking the dangling "MF-9" cite before the cure is quoted** | fourier API row + F.W3/W4 (guard) |
| **G6** | round-trip replay reproduces its own frame | **BC-9/C-6/D-20 + SS-C-2** (server `30 / 30.0 s / 80` vs client `60 / 5000 ms / 100` vs renderer `ref(80)` / `20000 ms`) · **SS-C-1** (`speed` settable only at create/fork under `extra="forbid"`, while `animation_settings` is atom 4-of-5) | every atom of the version-identity set survives **create → update → reload with server-side units**, or the atom set is re-declared. **3 of 5 `AnimationSettings` fields fail today; a gallery replay cannot reproduce its own frame.** Includes R5's assertion: the last user-visible value is the persisted value | F.W5 (units) · fourier API row |
| **G7** ⊙ | off-state / cardinality admission ruled | **M-9**: `[]` minted `:103-104`, forbidden `min_length=1` (`visualization.py:186/:280`), substituted (`workspace.ts:351-353`, `gallery.ts:252`), refused (`:53`), force-prepended (`:109-114`) — honoured on screen, destroyed on save, **zero diagnostic** | the owner rules (admit or stop minting) and the fixture proves the ruled branch. **Unruled = RED; F.W8 may not author on a guessed branch.** The silent rewrite must not survive either way | owner → F.W5 ruling block |
| **G8** | cache identity ⊇ consumed request fields | **B-4**: closed 10-field `json.dumps` literal (`image_storage.py:250-266`) omitting `ml_threshold`/`ml_detail_threshold`; `images.py:219-226` short-circuits **before** `compute_contours`; `auto` is the shipped default (`defaults.ts:4`) | the derive-leg fixture asserts `contour_hash` **INSTABILITY** across an ML-threshold change. **Today it is stable — which is the defect** | fourier API row |
| **G9** | the probe does not perturb what it measures | **FR-GV-12** (`find_one` `:256` → `$inc {views:1}` `:268-270` → `_public_doc` serialises pre-increment `:272`) · **VV-R2-A** (the publish ETag map always misses; `workspace.ts:359`) · no `viewed_ips` while `liked_ips` exists · **FR-USB-24** (silent ≤60 s retry manufactures false timings in the authenticated leg) | the walk's **reads** are provably non-mutating, or the perturbation is **declared and subtracted** in the walk record; auth mutations pass `retryOn429: false` or surface the wait. ▲ **K12 and FR-GV-24 hold: SS-13 spends no probe here; repair tests must not assert a re-open increment** | fourier API row + F.W4 |
| **G10** | create leg idempotent-or-declared, both sides | **D-4/L-1/C-3** (`saveVisualization` unconditional POST, fresh slug per call, `:358` never read as a guard ⇒ **N clicks = N public rows**) · **fr-GalleryDraftsSection B-2** (idempotency-free publish POST over a write-only-empty `savedSnapshots`) · **C-6/C-7** (`Idempotency-Key` declared `:98-99`/`:138-139`, unpassed `:316-324`). Counter-witnesses: fourier `idempotency.replay_or_record(...)` (`visualizations.py:612`); value.js transactional fork (`forks.ts:94-137`) | each side's create verb **carries idempotency or declares its absence with the reason**, and the register records which half each repo holds. **Neither side holds both today.** ▲ F.W8 claims **no credit** for the `:disabled` cure | fourier API row · value.js API row |
| **G11** | validate-at-boundary exists somewhere on the client surface | **FR-CP-16**: the whole 45-operation surface lands as **ONE** unchecked `(await res.json()) as T` (`api.ts:192`); IndexedDB rehydration (`workspace.ts:171`) replays through the same zero checking; a violation surfaces as **`NaN%` bar widths** · **fr-FourierShapeExtractor C-15**: `as any` at both consumers with `resolveJsonModule` already providing the discarded type | at minimum the epicycle payload validates at the boundary and the fixture proves **the failure mode is an error, not a `NaN`**. **0 of 45 operations validate today** | F.W4 + fourier API row |
| **G12** ⊙ | one generated/checked contract shape | **FR-AFP-71** (672 L / 391 L, no codegen; three copies of the flagged shape, one rotted) · **FR-AFP-36** (three dead server models; **no `response_model` anywhere**) · **GAB-12** (`types.ts:110-118` ↔ `api/models/gallery.py` hand-mirrored, `res.json()` unvalidated; drift = six silent em-dashes) | the owner rules codegen-vs-hand-typed-twins and **the register either IS the generated shape or documents why it is not**. ▲ **K9 holds: the `?? item.slug` fallbacks are NOT deleted** | owner → F.W5 ruling block |
| **G13** | liveness — every envelope field has a producer AND a consumer | **M-β4** (`EpicycleData.trace`, 3000 samples/response, zero web-tree readers, `structuredClone`d into every IndexedDB draft, `workspace.ts:95-104`) · **L·m-6/C·D-14** (`reconstructed_points`, 500+500 floats, `equations.py:114`/`:128`, read by nothing) · **PP-DEADSEAM** (`preview_path` written `""` at `assets.py:85`, `image_storage.py:318`, `responses.py:22`; client leaf unreachable) | the walk **touches every envelope field** and the unconsumed set is empty **or each member carries a drop-or-consume disposition**. **3 fields already proven dead** | F.W5 (predicate) · F.W8 (measurement) |
| **G14** | fixtures in BOTH directions; FN-6 relayed | ▲ **AMENDED (A-1, measured 2026-08-28)**: the value-side half is **not extant** — landed `7351297f`, merged `8a2a617e`, **deleted `a8ff7792`** with atomdiff (`D`, 371 lines). Census §3c: **four unanswered value→fourier letters, zero inbound rows in the V INBOX**. Reading: **0 extant · 1 landed-then-excised · 0 reciprocated** | the register covers **both directions** and the relay letter is **sent + INBOX-ledgered (E13)**. ▲ The fourier reader stays **fourier-owned**; value.js neither dictates it nor reads fourier's tree. ▲ **`canonical_digest` stays SKIPPED-sanctioned — a fixture re-opening it FAILS this gate.** ▲ The value-side fixture bytes land via the value.js API row (execution gate + G1) | F.W8 (register + letter) · value.js API row (bytes) · fourier (reciprocal) |
| **G15** | citation closure — every band identity this file names resolves to a canonical row at its holder, and this file books none | ▲ **RE-KEYED AT ROUND 4 ON THE CENSUS OF RECORD (R4-3.2 / R4-10; PASS-4 D-1/D-3/D-4).** The born-RED witness is the canonical, quoted at §1: F.W8 is *"named by **no record**"*, so this gate has **no record-side denominator to close over** and never had one — what it closes over is **this file's own citation list against the canonical roster**. Roster of record: `CENSUS-CANONICAL.md` **§2 `F.W5-W8` (89 rows)** ⟨*re-based at errata round 5, **E5-15**: read 90; `C:C-23` → UNROUTED*⟩ and **§2 `F.W5` (28 rows)**, both held at **F-W5** per R4-10's table. ▲ **A-2 IS DEMOTED FROM CENSUS TO DETECTOR READING, and the demotion is the cure for two convicted defects at once.** Its 2026-08-28 measurement stands as a dated reading — the ASCII spelling `F.W5-W8`, the en-dash spelling `F.W5–W8`, and the records reachable by neither alone — and its **figures are struck as operands** with the rest of the check-descended arithmetic. **PASS-4 D-1 proves why it had to be**: the corpus carries a **THIRD** band spelling, `F.W5(-W8)`, at `fr-ContourPreview`'s verdict tally, invisible to `F\.W5[-–]W8` **and admitted by this gate's own expansion clause** — a gate whose denominator is a regex convicts itself the moment the corpus writes a fourth form. It cannot happen here again, because **the denominator is no longer a regex; it is a roster** (R4-10: *no wave derives, samples, or re-cuts a roster from any other source*). ▲ **PASS-4 D-4 dissolves in the same motion**: the reciprocal's two ends are no longer two detectors that must be argued into agreement — F-W5's roster and F-W8's are **the same canonical §2 rows read from the same file**, and the divergence a check seat could measure between two hand-cut probes has nothing left to measure. ▲ **The identity D-1 caught is CITED, not adopted**: `fr-ContourPreview **L:L-5**` is homed by the canonical at **F.W5** — ⟨cmd⟩ `C='docs/tranches/X/fourier/conformance/CENSUS-CANONICAL.md'; /usr/bin/grep -nF 'fr-ContourPreview** (1): ' "$C"` → **three lines, residue disclosed** — `5044:- **fr-ContourPreview** (1): `L:L-5`` · `5091:- **fr-ContourPreview** (1): `C:D-11`` · `5358:- **fr-ContourPreview** (1): `R6-8`` — of which the first is inside the **§2 `F.W5`** roster (with `C:D-11` at F.W9 and `R6-8` at UNROUTED on the same probe, disclosed rather than trimmed) — so it is **not a band row at all**, it is F-W5's, and F.W8 cites it there. ▲ **It is also a TENTH colliding token, named here so the next seat does not re-find it**: **the fact is true and the round-4 receipt was DEAD (re-cut at repair round 5, REST-42; P5-5, HIGH).** The fixed-string filter above cannot return the second `L:L-5`: it **excludes that line by construction, at any reading, on any base** — `| /usr/bin/grep -c 'L:L-5'` over the filter's own output → **1**. **One command, one claim; the collision needs its own probe** — ⟨cmd⟩ `/usr/bin/grep -nF 'L:L-5' "$C"` → `:4968` **fr-PaperSearchDropdown** (§2 `F.W3` roster) · `:5044` **fr-ContourPreview** (§2 `F.W5` roster), plus the two per-record rows at `:1305` (F.W5) and `:4037` (F.W3) — **two records, one spelling, two homes: the TENTH colliding token.** A bare-token difference over it would have been unsound in exactly the way R-5 has been saying since round 2. ⊘ R4-2.5 forbids the charitable reading that would save the old receipt, **and it sat inside a gate cell — the site this file itself calls "the worst place this class can hide" when it struck round 2's dead probe from the identical position.** | **set-difference ∅ in BOTH directions** between **the canonical rows this file names** — resolved at closure time against `CENSUS-CANONICAL.md` §2, never re-cut from the corpus and never inherited from a check file — and **the §3 rows + §6 Excluded + §6a's 40 citation rows**, with the standing bar that **no row on either side is a BOOKING**: F.W8's canonical home count is zero, so a booked row here would be a fabrication under R4-10's own word for it. Output committed to `F-W8/carry-closure.md`. ▲ **LHS RE-POINTED AT ROUND 2 (R2-7.3 / PASS-2 D-4): the LHS is the REGISTRY under record-qualified extraction, and `conformance/PASS-1/F-W8-CHECK.md` §1 is RETIRED as an operand.** Round 1 named that check file as a cross-read; it is a **bare-token** roster, so it carried the exact defect class this gate exists to catch — three homonym false-B marks and one outright omission (§1's four escapes) — and unit **e** run against it would have returned ∅ **falsely**. A gate may not be cross-read against an artefact carrying its own defect class; the PASS-1 roster survives here only as the **superseded reading** minuted at §1, never as an operand. ▲ **DETECTOR, STATED INLINE so this gate — not a later seat — catches the next format the corpus uses (R2-9's program-wide law):** **(shapes)** the roster is shape-agnostic — markdown table rows whose terminal cell routes **⊕** id-headed bullets routed in context (`→` · `**ADJUDICATED →**` · `⇒` · `rides` · `routed` · bold-at-disposition) **⊕** prose routings walked back to their enclosing row id; a shape-restricted operand is DEFECTIVE at authoring whatever its result, and **every one of the band's four round-2 escapes is a BULLET row** (⟨cmd⟩ §1's four `sed -n` pastes: each opens `- **M-13 …`, `- **M-10 …`, `- **L·M-4 …`, or sits in a `**Full-coverage note**:` prose sentence — not one is a table row), which is the measured proof that the shape decree is load-bearing here and not ceremony. **(spellings)** ▲ **ROUND-4 — THIS CLAUSE IS RETIRED AS A DENOMINATOR AND KEPT AS A READING DISCIPLINE, because PASS-4 D-1 proved it could not be anything else.** It read every routing token in **BOTH U+002D and U+2013** (`F\.W5[-–]W8`) and kept the en-dash arm permanently so it could never silently stop being non-∅ — sound as far as it reached, and it did not reach far enough: the corpus also spells the band `F.W5(-W8)`, and a fourth form costs the corpus one keystroke. **A gate that must enumerate its own future spellings is defective at authoring whatever its result** — the same sentence this cell already applies to shapes, now applied to itself. The roster supersedes the regex (R4-10); the spelling arms survive only as *how a reader recognises a band token on a line*, never as *what the band contains*. **(expansion)** every slash form expands to its member set (`F.W3/W4` → {F.W3, F.W4}); a span containing 8 routes the band — and note that this decree **contradicted the clause above it for two whole rounds**, since `F.W5(-W8)` is exactly such a span and the regex could not match it. That contradiction is what a denominator built from a probe looks like from the inside. **(qualification)** ids are matched as `(record, id)` pairs with boundary-aware matching (`re.escape(id) + "(?![0-9A-Za-z])"`), never bare. ▲ **Operand law (R-3): the LHS is the REGISTRY, by path; "the CARRY" is not vocabulary this gate may use, and no F.W8 carry is minted to supply one.** ▲ **Method law: the closure RE-DERIVES the homing; it does not re-hash any intake.** ▲ **Homing law, re-worded DISCHARGEABLE (R-5)**: *every band row has exactly ONE **STATING** home among F.W5/W6/W7/W8; F.W8's assertion citations are **not** homes.* The prior wording ("homed at exactly one of F.W5/W6/W7/W8 — no double-booking") was **undischargeable from this end** for as long as the keystone carried **no `F.W8` token at all** — it never named its consumer. ▲ **ROUND-3 (R3-3.10): round 2 warranted that clause with a banked `grep -c "F\.W8" F-W5.md` → 0, and a stale count of a live sibling inside a GATE CELL is the worst place this class can hide — the figure stopped reproducing the moment the reciprocal landed.** The condition is restated as the dated classification it always was; the operative fact is the next sentence, and it is a set-membership claim a seat can re-run. **Re-measured 2026-08-28: the reciprocal `F.W5 → F.W8` row HAS LANDED in `F-W5.md` §4** (§5c quotes it), so the leg is discharge*able*; it is **not discharged** — the gate is born-RED and closes only when unit **e** runs the difference and commits the output. FR-GIG-5 mirror holds: no wave credited with another's cure. ▲ **Runnability precondition: every id on both sides is RECORD-QUALIFIED** (R-5) — `fr-BasisCanvas C-17`, `fr-EquationView B-1`/`B-2`, `fr-ContourEditorCanvas C-2`, `fr-FunctionInput L-B1`/`L-M3`, `fr-CoefficientsSpectrum M-13`, `fr-BasisCanvas C-18` — the U-12 eight named identically at **F-W5 §4's `F.W5 → F.W8` reciprocal row**. A set difference over bare tokens cannot separate booked from escaped and is **not** a run of this gate. ▲ **The precondition is MET on this end at round 2 and was NOT met at round 1** (R2-7.2/R2-7.4): §3 P8's `C-3`/`C-4`/`D-13` are record-qualified with their homonyms named, §6a rows 37–40 carry the four escapes the bare operand hid, and the **NINTH** colliding token — `M-10` — is now named at BOTH ends (⟨cmd⟩ `grep -n "A NINTH collision" waves/F-W5.md`, this seat → F-W5 §2's ANTI-RENAME block, verbatim: *"**A NINTH collision is named here so the mechanism that hid it is disarmed rather than re-armed (R2-7.2's law applied at this end): `M-10` collides too**"*, resolving `fr-ContourSettings M-10` to clause **D12** and `fr-BasisSelector M-10` to **D10**'s WAVE-LOCK). **Met is not run**: the gate stays born-RED and closes only at unit `e`. ▲ **ROUND-3 (R3-7.2) — the operand was STILL defective at round 2's close, and is repaired here.** §6a's per-id table carried one **non-member** (`fr-EquationPanel D-3+C-5`, routed *"F.W4 + F.W2 rider"* by its own bytes) and omitted one **member** (`fr-EquationPanel D-10+D-L12+C-22`, home F-W5 clause **F9**) — **a non-empty difference in BOTH directions, which is this gate's own HALT condition met at authoring**, and it survived two passes only because the two errors cancelled in the total. Row 19 now carries the member; the non-member is a footnote outside the 40. **The HALT condition is LIFTED; the gate is NOT.** Its operand is runnable in both directions for the first time, and nothing in this cell claims unit `e` has run — the difference is still unrun and the row is still RED. **A non-empty difference after two closure passes HALTS the wave** | F.W8 (unit e) |
| **G16** | the admission horizon's only UI entry loads the saved entity | **GCM-1** (`:185` `image_slug` → `GalleryView:396` `router.push('/w/${slug}')` → the caller's IndexedDB draft, hard-reset to defaults, writing a draft row keyed on **another user's** `image_slug`; `loadVisualization` `workspace.ts:197-232` has **zero call sites**) + **VV-R2-B** (10 of 24 store exports unconsumed; `gallery.ts:168` bypasses `deleteVisualization`) | opening a published visualization loads the **saved entity** with slug + ETag captured, writes **no cross-owner draft row**, and the create → derive leg completes **through the product's real entry point**. **Data-dead today** | F.W4 (wiring) · F.W8 (assertion) |

---

## 5. Sequencing

### 5a. In-wave order (serial)

`a` walk map (§P, R-1..R-7 per-verb answerability + the consumed denominator) → `b` fixture register (§J, both directions, generated-from-model) → `c` C31 reproduction + A/B split → `d` `union-walk.mjs` **authored, not run** + the §M non-perturbation declaration + §R round-trip and §D liveness assertions → `e` rulings surfaced, FN-6 relay letter + INBOX row, **G15 carry closure**. Each unit commits before the next opens.

### 5b. Cure-integrity locks (bind the repair, not the finding)

▲ **K-1** 13/44, never zero · ▲ **K-3** **fr-BasisSelector m-7**'s 422-straddle is refuted from source; the fixtures may not cite it ⟨*record-qualified, REST-47*⟩ · ▲ **K9** do not delete the `?? item.slug` fallbacks · ▲ **K12** B-2's duplicate-row outcome closes STATICALLY; SS-13 spends no probe · ▲ **FR-GV-24** repair tests must not assert a re-open increment · ▲ **fr-FourierShapeExtractor C-3** no closure heuristic; the flag is carried, never inferred · ▲ **M-2** the tracked in-process generator exists for 5 of 7 assets · ▲ **DO-NOT-REGENERATE on `master`** — any regeneration revives L-B1 and L-B2/C-2 at BLOCKER · ▲ **M-CK-class** fix the abort key first (P8's per-call key) · ▲ **basisFilter WAVE-LOCK** — wire only with the banked M-10 normaliser · ▲ **MF-9** resolve or strike before quoting GCM-10's cure (**F-W5 gate G22, §3** — R-1f; never "§6c", which does not exist) · ▲ **BC-20** not re-booked · ▲ **`canonical_digest`** SKIPPED-sanctioned; do not silently re-open · ▲ **`UTF8_BYTEWISE_CODEPOINT`** ordering, never `localeCompare` · ▲ **X-9** one member-scope law before any percentage · ▲ **D9** the ruled visibility/lifecycle/moderation vocabulary · ▲ **D-19** re-resolve every fourier anchor after F.W0 · ▲ **FR-GIG-5 mirror** no wave credited with another's cure · ▲ **A-1/A-2/A-3** the three value-side amendments are recorded **at this amending record; the banked files are NEVER rewritten** · ▲ **RECORD-QUALIFICATION (R-5)** every short id is written with its record wherever it is booked, cited or excluded — `B-1`, `B-2`, `C-2`, `C-3`, `C-4`, `C-17`, `C-18`, `D-13`, `L-B1`, `L-M3`, `M-10`, `M-13`, **`m-7`** and **`m-15`** ⟨*added at repair round 5, REST-47*⟩ are each ≥2 distinct banked rows, so a bare token is not an identity. ▲ **Round 2 added `C-3`, `C-4`, `D-13` and `M-10` to this list by CONVICTION, not by caution**: those four tokens are precisely the ones the round-1 bare operand mis-resolved, hiding three band identities (§6a rows 37–39) — the list is now the measured collision set, and `M-10` is the **ninth** token, named at F-W5's end too (quoted at G15) · ▲ **NO PHANTOM INTAKE (R-3)** only `carry/F-W1-CARRY.md` and `carry/F-W4-CARRY.md` may be cited as carries, and only by path; F.W8 has none, mints none, and grounds every closure on the registry · ▲ **ANTI-RENAME** banked ids are original for life: an id is carried, folded or excluded under the name the record gave it — never re-minted, never silently merged.

### 5c. Cross-edges — declared from this end

| edge | direction | this end's declaration |
|---|---|---|
| **F.W0** substrate pre-gates | **DEPENDS — HARD** | ▲ **G-11 / G-12 QUOTATION (R-9.1): every corrected anchor and every corrected denominator this spec uses is an *input to / quotation of* F.W0's published tables**, cited by **GATE ID** and never by line (R2-2; the round-1 addresses `F-W0.md:199`/`:204`/`:63` had all drifted +12 under F.W0's own concurrent repair in this same round — PASS-2 D-9 — and are struck **as addresses**, their quoted bytes re-verified unchanged and re-pasted below). ⟨cmd⟩ `grep -n "^### G-1[123]" waves/F-W0.md` (re-run this sitting, 2026-08-28) → **F-W0 §4 gate G-11**, whose heading line reads verbatim, **as bytes, heading markup included**: *"`### G-11 — ONE corrected anchor table published; every later wave quotes it`"* · **F-W0 §4 gate G-12**: *"`### G-12 — ONE corrected-denominator table published; superseded figures FORBIDDEN downstream`"*. ▲ **ROUND-3 (R3-3.9 / PASS-3 D-4): round 2 rendered both spans with the `###` heading markup silently replaced by `**…**`.** Everything after the id was byte-true — which is exactly why the substitution was invisible — but a markup EDIT inside a span labelled *verbatim* is the same offence as an interpolation under R-1's law (an ellipsis marks elision; **nothing is added, nothing is re-styled**), and this file's own R2-1-LAW makes quotation form a defect class per se. The bytes are pasted as bytes. ▲ **The same probe returns a THIRD line, and it is disclosed rather than trimmed**: *"`### G-13 — The producer pin is re-derived at the ADOPTED commit; no F.W1 sizing quotes a 7.0.0 anchor`"* — F.W8 cites G-13 nowhere and needs nothing from it, but a pasted output that silently drops a returned line is PASS-3 D-3's class at a second site, cured here in the same breath as D-4. Both land in `SUBSTRATE-LEDGER.md`, whose path is declared at **F-W0 §2 (Bounds), the `SUBSTRATE-LEDGER.md` create row** — ⟨cmd⟩ `grep -n "SUBSTRATE-LEDGER.md" waves/F-W0.md` → the bounds row whose path cell reads `fourier/docs/tranches/F/SUBSTRATE-LEDGER.md` over an access cell reading verbatim *"**create** — the wave's single durable artefact: … the G-11 anchor table · the G-12 denominator table · the G-13 pin table + lattice …"* (ellipses mark elision only; §0's ERRATUM 2 minutes why the ruling's "§1a" candidate anchor does not hold — §1a is F.W0's four-verb table). **F.W8 re-performs neither re-resolution as its own act; divergence from those tables is a defect against G-11/G-12, not a rival act of this wave.** No line anchor, HEAD figure, or "LIVE at certified HEAD" citation enters this spec until F.W0 re-grounds (D-19). Named blockers: glass-ui 4.0.0's syntactically corrupt `dist/styles/index.css` — stated in R-8's **canonical form, `FR-NP-32 (≡ fr-PaperSidebar M1)`**, everywhere this spec states the gate (R2-7.5; the round-1 M1-alone spelling is the breach PASS-2 D-8 convicts, and this is the file's only statement of it — ⟨cmd⟩ `grep -n 'PaperSidebar' waves/F-W8.md` → this row alone). ⟨cmd⟩ `grep -n "FR-NP-32" waves/F-W0.md` → **F-W0 §3, carry row 1**, whose disposition cell reads verbatim: *"THE F.W0 RE-HOME IS PERFORMED HERE (R-8): the producer-owned emitter fix and the F.W1 sequencing gate both stand, and F.W0 additionally owns the pre-gate (G-4) + relay item 0 — cite-both, never substitute; canonical form **FR-NP-32 (≡ fr-PaperSidebar M1)**."* Further named blockers: the **dirty fourier worktree** (GAB-13 — 28 entries re-measured; the census pin `14d83356` **does not resolve**); `F8-REACH-01` (`InfoCard.vue`, `b2c2e718…`/1,516 B, unmounted) and `F8-REACH-02` (`CanvasOverlayButton.vue`, `35f3db91…`/595 B, unmounted) — for both, **"HOLD is RED"**. **If F.W0 fails to re-ground, F.W8 HALTS; it does not proceed on stale bytes** |
| **F.W1** atomic tri-package uplift | **DEPENDS — WITH A DECLARED CYCLE** | ▲ **CONFLICT DECLARED FROM THIS END, for adjudication at the spine, not at execution**: the DAG puts F.W8 after `F.W1.close`, but fr-AdminFlaggedPanel's **roster-wide ruling 5** requires *the F.W5-W8 producer-or-retire decision to precede F.W1's sizing where grades are load-bearing*. **F.W8 does not own that ruling — F-W5 G11 does — but the cycle passes through this wave's band and must be cut before either opens.** Otherwise independent both ways. ▲ **F.W1 is ONE atomic land-or-lose transaction — the TWELVE-limb roster chartered at F-W1 §4 (Sequencing), step 4 "The atomic transaction (ONE change, G6)" — cited whole, never restated here** (R-4b; the prior three-limb naming would have let a reader of F-W8 alone size the transaction at three, omitting the vaul-vue gate, the RE-PIN at the adopted commit hash, and the P0 CSS-class census). ▲ **The extent is re-quoted from the corrected charter, never re-derived here (R2-5): round 1 cited "ELEVEN"; F.W1's own round-2 cure raises it to TWELVE and this end quotes the cure rather than arithmetic of its own.** ⟨cmd⟩ `grep -n "The roster is" waves/F-W1.md` (this seat, 2026-08-28) → verbatim: *"**The roster is TWELVE limbs and stays twelve — the eleven chartered plus the FR-EQC-7 vaul-vue manifest gate returned INSIDE at repair round 1 per the `:294` pin (R-4a, cross-edge 1 at this writing) — it is the extent every sibling spec cites (R-4b); nothing is added to it by a later reading.**"* The `:294` pin the charter names is **F-W1 §4's cross-edge 1 (`F.W0 → F.W1`)**, cited here by that label and never by line (R2-2); F.W1's own coordinate is marked *"at this writing"* inside its bytes, which is exactly why this end does not copy it. The G5 landing cell stays **conditional, not a thirteenth limb**. The transaction — whose failure mode is four zero-console-error e2e gates going red (FR-EQR-3) — is untouched here, and ▲ **F.W1 must NOT be credited for the pagination-drain cure (FR-GIG-5)** — F.W8 asserts that separation in P8 and claims no credit for anything F.W1 owns |
| **F.W2** value.js consumption to spec | **DEPENDS** (ordering only) | F.W8 books **zero migration cost** and inherits F.W5's exported zero-cells so **no fixture row manufactures overlap**: App API-inert · MobileFloatingToc · CollapsibleSection · the PaperSearch trio (*"F.W5-W8 = no rows"*) · MorphShapePreview (structurally unreachable — R6-8 and R3-7b both) · ▲ **UserSlugBar FR-USB-38 — do NOT book this component into the F.W2 migration surface.** These measured negatives are **evidence, not work** (P-9) — and at round 4 they are **not a denominator of anything** (R4-3.2), which is the whole of the change. ▲ **ONE OF THEM IS CONTESTED BY THE CENSUS OF RECORD, and F.W8 records rather than resolves it**: the canonical homes `fr-PaperSearchDropdown` **`C:C-23`** in its **§2 `F.W5-W8`** roster, read off the very line whose own words are *"F.W5-W8 = no rows."*. **The canonical governs (R4-10)**; this row's zero-cell inheritance therefore stands for the *other* members of the trio and is **suspended for `C:C-23`, which is cited to F-W5 as the roster's holder** (§6a header). A wave with zero record-side rows may not re-grade a record-side reading — it may only refuse to build a denominator on top of one, which is what it does here |
| **F.W5** the shared-provenance contract | **DEPENDS — HARD; the reciprocal DECLARED at both ends (re-measured 2026-08-28)** | Required as **artifacts, not prose**: **(1)** `J-diff-shape-v2.md`'s clause set with each clause RULED / DECLINED-WITH-REASON / OWNER-GATED; **(2)** `operation-register.md` — the 45 = 30 + 13 + 1 + 1 enumeration with per-operation **authority class** (R3-7b) and **client-edge disposition** (R3-7c) **plus a joinable/unjoinable cell**, or G4 is unfalsifiable; **(3)** `OWNER-RULINGS-F.W5.md` — G4 (TA-4), G7 (trie-vs-KISS), **M-9** (off-state `[]`), G10 (like verb), G11 (flags producer-or-retire), FR-AFP-9 (hard-delete arm), **FR-AFP-71** (codegen vs twins); **(4)** the casing rule (TS-camel ↔ Python-snake, the ONLY allowed envelope difference) as a **mechanical check on each side**. ▲ **DEFECT DECLARED, THEN CURED AT THE KEYSTONE'S END — both readings dated, neither overwritten (E-3: addenda, not patches).** **Read 1 (2026-08-28, this seat):** `F-W5.md` **§4** — *"Cross-edges — declared from THIS end (one home, two citations)"* — declared edges to F.W0, F.W1, F.W2, F.W3/W4, F.W6, F.W9/W10, SS-4, the value.js API row, the glass BH relay, X·parse-that and SS-13, and **NO edge to F.W7 or F.W8** — the `F.W8` token was absent from the keystone altogether at that reading. ▲ **ROUND-3 (R3-3.8 / R3-3.10): round 2 banked this as `grep -c "F\.W8" F-W5.md` → 0, a receipt that CANNOT reproduce** — the reciprocal landed later the same session, so ⟨cmd⟩ `grep -o 'F\.W8' waves/F-W5.md` (this seat, 2026-08-28) now returns the token. **Read 1 stands as a dated reading and its figure is struck**, because a banked count of a live sibling is false the moment the sibling moves; Read 2 below is the operative fact and always was. The anchor is corrected per R-1f: `F-W5.md` has **no §6b and no §6c**, and the prior draft's "§6c" attestation is struck. **Read 2 (re-measure, same session, after R-5 was ruled):** the reciprocal **HAS LANDED** — **F-W5 §4** now carries the row whose edge cell is **`F.W5 → F.W8`** and whose direction cell reads **the RECIPROCAL, declared from this end**, its act cell verbatim: *"F.W5 STATES the clauses; F.W8 ASSERTS them (fixture/e2e direction). ONE home (F.W5), two citations — the shared ids are enumerated at `F-W8.md` §1's census. No F.W8 row re-books a clause; no F.W5 clause claims F.W8's assertion work. Reciprocal: `F-W8.md` §5c."* ▲ **This end now reciprocates by name**: §5c IS that reciprocal, §1's census IS the enumeration it points at, and **§6a** names the STATING home of every band identity F.W8 does not carry. ▲ **The set-difference precondition F.W5 attaches is adopted here verbatim**: the shared ids run in the **record-qualified** form on both ends — the U-12 eight (**M-13 · L-B1 · L-M3 · C-17 · C-18 · B-1 · B-2 · C-2**) are ambiguous bare and cannot be differenced; §3, §6a and §5b now carry that qualification throughout. ▲ **The split, stated from this end — as a LAW, with NO count word (R2-7.6 / PASS-2 D-13, the "64 ids" figure STRUCK).** Round 1 asserted *"**64** of the ids booked in §3 also occur in `F-W5.md`"* with no stated method, and the figure is **unreproducible**: ⟨cmd⟩ `sed -n '/^## 3\. Carry/,/^## 4\. Gates/p' waves/F-W8.md \| grep -E '^[\|] \*\*(P\|J\|R\|M\|D)[0-9]' \| awk -F'\|' '{print $3}' \| grep -oE '[A-Za-z]+[·]?[A-Za-z]*-[A-Za-z0-9]+' \| sort -u`, each token then tested with `grep -qF` against `waves/F-W5.md` (resumption sitting, 2026-08-28) → **118 tokens extracted**, and then, **twice within one minute, TWO DIFFERENT ANSWERS: 97 present / 21 absent, then 98 present / 20 absent** — over a **byte-identical** extracted roster (⟨cmd⟩ `diff -q` on the two runs' rosters → identical). The cause was measured, not guessed: ⟨cmd⟩ `stat -f '%Sm %N' waves/F-W5.md` → the keystone was **rewritten at 16:38:31, between the two runs**, by its own concurrent L-20 repair seat. The first sitting's extractor had returned **104 · 93 · 11**; the PASS-2 seat's independently-written extractor **91 · 90 · 1**. ▲ **Four honest runs, four answers, not one of them 64 — and the last two differ from each other with the same code over the same input, sixty seconds apart.** So the figure is not merely method-dependent, it is **time-dependent against a live sibling**, which is why no overlap figure is published here at all and why D-19's re-resolve discipline is not ceremony. The first sitting's figure additionally carried a **⟨cmd⟩ marker with no command beside it** — a defect per se under this file's own masthead law — and that marker is discharged here by a command any seat can re-run. **The divergence is not noise to be averaged; it IS the finding**, because **a bare-token overlap count is not an identity count** (R-5: a bare token is not an identity, so a number built on one measures spelling, not shared identity). **No overlap figure is published here.** The law it was standing in for is stated instead and is what actually binds: F.W5 **STATES** the clause, F.W8 **ASSERTS** it in the fixture/e2e direction; **no §3 row re-books an F.W5 clause**, and F.W8 claims no credit for any clause F.W5 states (FR-GIG-5 bar). The record-qualified overlap, if a later seat needs the number, is **G15's unit `e` output** — derived once, at closure, on qualified pairs, and committed to `F-W8/carry-closure.md`; it is not asserted here in advance of the run (R2-7.4: the stamp follows the operand). ▲ **Standing caveat (D-19)**: `F-W5.md` is under concurrent repair this round; the reciprocal is re-confirmed at open by **id** (the `F.W5 → F.W8` row in §4), never by line |
| **F.W6** provenance defect burn-down | **SIBLING — F.W8 ACCEPTS, F.W6 BURNS** (no gating either way) | F.W8 repairs nothing and claims credit for none of F.W6's cures. But the **history leg cannot pass while F-α stands** (every version `depth=0`; corroborated three ways at intake X-8), nor the **diff leg while F-β stands** (PATCH `$set`s atom 5 without recomputing `set_hash` or writing a version; `palette_slug` never clearable to null), nor the **provenance leg honestly while F-γ stands** (no redaction — `_readable_or_none` applied only to the entry row `:744`, so `slug`/`set_hash`/`author_slug`/`created_at` cross the wire for private and draft ancestors, while value.js collapses non-public hops to `{kind:"unavailable", ordinal}`). **Declared as ACCEPTANCE dependencies, booked once at F.W6.** ▲ Also declared: the **FSE pipeline DECISION (C-7) has NO wave owner** — F.W8 **nominates F.W6 and requests adjudication**; the DO-NOT-REGENERATE tripwire binds F.W8 wherever it lands |
| **F.W7** trie-like compression | **SIBLING — OWNER-GATED**, no gating either way | F.W8 designs no trie. If G7 **admits** it, the derive leg must exercise structural sharing; if G7 **sustains the incumbent KISS guardrail** (`atomdiff.py:12-14` verbatim: *"the atoms are a flat BAG (not a tree / Merkle / document); the diff is a whole-atom replace …; there is no three-way / DAG / merge"* — carried on BOTH sides), the prototype asserts **whole-snapshot duplication as the DECLARED shape and records its cost** (fourier re-persists all 5 atoms incl. the 12-field `ContourSettings` and 6-field `AnimationSettings` per version; value.js re-persists the whole `colors: PaletteColor[]`). **R-4 is the single union requirement with ZERO material on either side.** Unruled = F.W8 carries the documented default **and says so** |
| **F.W9 / F.W10** deploy spine, gates, close | **F.W8 EMITS evidence — DOUBLE-HOME RISK DECLARED** | **F.W8 LANDS the fixtures and the walk script; F.W9/W10 RUN the close-gate as evidence** — declared because **F-W5 §4's `F.W5 → F.W9/W10` edge row** (cited by row label, per §0's addressing law) routes the close-gate to F.W9/W10 while census §4.9 gives F.W8 the conformance fixtures. ▲ **QUOTE CONFORMED TO ITS BYTES AT ROUND 2 (R2-7.6 / PASS-2 D-10): the round-1 rendering *"v1 §6's close-gate … re-authored by v2 and executed there"* INTERPOLATED the words `'s close-gate` — an ellipsis marks elision, never addition (R-1's verbatim law).** ⟨cmd⟩ `grep -n 'v1 §6' waves/F-W5.md` (this seat, 2026-08-28) → the `F.W5 → F.W9/W10` row, verbatim: *"v1 §6 re-authored by v2 and **executed there**, each side's probe asserting against the DOC, never the sibling (inv-26)"*. The routing claim round 1 made was TRUE; only its quotation was not, and the quotation is what is cured. Consequence unchanged: **one home, two citations, no wave credited with another's cure.** Binding law carried: **each side's probe asserts against the DOCUMENT, never against the sibling (inv-26) — no cross-repo read, ever.** Coverage seams emitted: **AA-44** (zero frontend tests; nine e2e specs never touch the audit tab) · GAB-12's test seam · FR-AFP-66's orphan-flag test exercising only the reporter-keyed path · ▲ **FR-AUL-13's lesson adopted as this wave's own bar: a contract document without a probe REPEATS the defect that row convicts.** The walk's execution environment (a non-production stack) is **F.W9's**, not this wave's |
| **the value.js API row** (X·V W3/W9) | **CO-SIGNER; neither side's edits gate the other's waves** (COHESION §2) | Obligations created here, booked as **VALUE-SIDE rows, never re-booked as fourier defects**: **TA-4** (restore `lib/crud/atomdiff.ts` + `atomDiff` on `PaletteVersion` **+ the excised conformance fixture, A-1**, or be re-scoped out — G1) · **V-α** (revert writes no version row yet still `$inc`s `versionCount`, permanently drifted; no test asserts otherwise) · **V-β** (global `PaletteVersion._id` collides across palettes — fourier already solved it with the compound `{viz_slug}:{set_hash}`) · **V-γ** (nullable `userSlug` ⇒ an unattributable edit mutates the palette and writes no version row: `crud.ts:119`, `crud.ts:204`, `versions.ts:147`). Cross-checked against `api-gap-remeasure.md`'s nine classes and **X-1** (`computeContentHash` ≠ `payloadHash`), **X-2** (stored `forkCount` as public authority), **X-3** (PATCH TOCTOU / no CAS), **X-6** (`unlisted` still alive) — all VERIFIED-GAP — and against ruled **D9**. ▲ **X-4 (the `/versions` rail) is CHANGED–CONTESTED and needs an owner ruling; the value-side history leg must not assume its shape.** ▲ FN-5's currency invariant re-anchored by **A-3** |
| **SS-4** union synthesis | **F.W8 IS the union's ACCEPTANCE half** | F.W5 states the contract, F.W6 burns the defects, F.W7 rules the compression, **F.W8 proves the whole thing walks.** Owner rulings FLAGGED INLINE per SS-4's charter; F.W8 surfaces **only** those F.W5's block does not own — **(i)** the fixture-direction law's exception set (FR-IC-6) and **(ii)** the re-scope branch should G4 decline TA-4, now carrying A-1's third limb. Everything else routes to `OWNER-RULINGS-F.W5.md` and is **consumed here, never re-ruled** |
| **SS-13** live-probe lane | **DEFERS magnitude only** | FR-GSB-28's interleave symptom · VV-R2-A's observable phantom view · B-4's `contour_hash` instability probe · M-14's end-to-end contour-loss reproduction · L-M2's extraction-scope magnitude · L-B1's *"what `moon.json` WAS generated from"* (undetermined) · FR-AFP-33's aggregate cost. ▲ **K12 and FR-GV-24 hold. No F.W8 gate is discharged by an SS-13 probe; magnitude is not a gate** |
| **glass-ui BH relay (SS-6)** | **DECLARED EMPTY** | F.W8 carries **ZERO** producer rows — declared so that (a) no glass-producer row is smuggled in as a contract or fixture clause, and (b) no contract row is discharged as a frontend hack. Any producer-shaped finding leaves **as a letter** to the standing BH inbox (owner edict, standing formation invariant). **FR-GIG-1's pagination drain: named and routed, never hacked** |
| **X·parse-that** | **FORBIDDEN EDGE (declared)** | Direct parse-that→fourier routing is forbidden and has never existed (PLAW-BIND clean). No F.W8 row is discharged through the parser sub-tranche |
| **`/Users/mkbabb/Programming/fourier-analysis`** | **READ-ONLY, ALWAYS** | Zero fourier bytes. The register and the FN-6 ask travel as a **letter** for co-signature; the reader strategy is fourier's own. `J-diff-shape.md` v1 is **IMMUTABLE**; v2 supersedes by reference (E-3) |
| **execution gate** | **ABSOLUTE** | Status stays `planned`. Nothing opens, writes, builds, or runs product source in either repo — including `$V/api/test/conformance/**` — until the owner's begin-word. **Specifying is not doing** |

### 5d. Cadence, artefacts, commits

Docs-only wave. After each unit and before close: `git diff --check`; the markdown link check over the new `conformance/` paths; the **G15 spelling-agnostic set-difference re-run**. `npm run typecheck` / `test` / `lint` are **not this wave's gates** — no source byte moves, so a delta in any of them means the wave escaped its bounds and is itself the failure signal (run once at close as that tripwire, not as evidence). Committed at close (L-7 — an uncommitted witness does not exist): the five `conformance/` artifacts, `walk/union-walk.mjs`, `F-W8/carry-closure.md`, the relay letter + its INBOX row, and the F.W0 re-grounding receipt this wave's anchors were re-resolved against (cited, not copied). Pathspec commits only, one per unit; `scripts/dev/dev.sh` never staged.

---

## 6. Excluded — carried nowhere else, with reason

Nothing below is a silent drop; each is named so a later reader cannot mistake absence for oversight. **§6 excludes by CLASS; §6a excludes by ID** — the **40** band-routed identities §3 does not carry, one row each, with the STATING home of every one. Together they discharge the promise the class table alone could not: the prior draft's §6 named **none** of them (PASS-1 D-3), so the closure was deferred wholesale to G15's unit **e**. It is performed here. ▲ **Round 2 raises the table from 36 to 40** (R2-7.1): the round-1 operand was bare-token and reported four band identities as booked-or-absent that are neither — the promise *"nothing below is a silent drop"* was **false as written for exactly four rows**, and the cure is to name them, not to re-word the promise.

| excluded | reason |
|---|---|
| **The TA-4 restoration itself** (all three limbs) | ⊙ **G1 unruled.** F.W8 states the prerequisite and carries the honest re-scope default; the restoration is the **value.js API row's** act |
| **Re-booking A-1 as a fourier defect** | **VALUE-SIDE row.** The excision was value.js's own T.W1 act; fourier owes nothing for it |
| **F.W4's wiring unit** (route → `loadVisualization`; publish → `setVisibility`; delete → the store action) | F.W4's, as **one** unit (VV-R2-B's cure shape). F.W8 asserts adoption and claims **no credit** |
| **The `:disabled` / re-entry-guard cure** (FR-GV-1's client arm) | F.W3/W4's per FR-GV-1's own split law. F.W8 owns only the **server dedupe** assertion |
| **F.W1's tri-package transaction**; the pagination-drain cure | F.W1's; and ▲ **F.W1 may not be credited with FR-GIG-5's cure** — the true cure rides the SS-6 producer relay |
| **Re-deriving the 45/30/13 enumeration or the 0/45 security figure** | **Consumed from F.W5's `operation-register.md`** — one home, two citations. Re-deriving would double-book the keystone |
| **Re-booking the FSE tripwire** | Already banked at **F-W5 G1c/G16**. Carried here **as a binding lock only** |
| **The FSE pipeline DECISION (C-7)** | **No wave owner in the census taxonomy.** F.W8 nominates **F.W6** and requests adjudication at the spine — routed, not dropped |
| **Any regeneration of `moon.json` / `sun.json`** | **TRIPWIRE** — DO-NOT-REGENERATE on `master`; any attempt revives L-B1 and L-B2/C-2 at BLOCKER |
| **A post-hoc closure heuristic** | **KILLED (C-3)** — 44.08 closed vs 44.97 open overlap; the flag is carried, never inferred |
| **fr-BasisSelector m-7's straddle-a-422 scenario** ⟨*record-qualified, REST-47*⟩ | **REFUTED from source (K-3)**; the fixtures may not cite it |
| **Deleting the `?? item.slug` fallbacks** | **NOT PROVEN (K9)** — `admin.py` emits `doc.get("image_slug")` unvalidated; defensive |
| **A repair test asserting a re-open view increment** | **FR-GV-24 lock** — the session guard works; the defect is SCOPE, not absence |
| **A live probe of B-2's duplicate-row outcome** | **K12** — closed statically; SS-13 spends no probe |
| **`canonical_digest` parity** | **SKIPPED-SANCTIONED.** Adoption = a stored-hash migration, not parity. A fixture re-opening it **fails G14**; the three named twin divergences stay un-unified BY DESIGN |
| **A general two-point diff fixture** | fourier's `/diff` is **not** general — `on_chain` is only `{head_hash} ∪ {fork_of_hash}` (`:827`), reads the STORED `atom_diff`, 404s any other pair |
| **The FN-6 reader implementation** | **fourier-owned.** value.js neither dictates it nor reads from fourier's tree (dispatch-homes A.1) |
| **Reopening R.W6 §11-Q9** (fixture home) | Resolved to *"value.js lands, in-tree, read locally"* (M4). **Dissent recorded, not re-litigated** |
| **Trie / structural-sharing design** | ⊙ **G7 (F.W7) unruled.** F.W8 carries the documented KISS default and authors no design |
| **Tier↔flag SEMANTICS; the moderation band's rulings** | **F.W5/F.W6's.** F.W8 owns only FR-GV-9's batch-write state-preservation assertion |
| **F.W6's F-α / F-β / F-γ burn-down** | F.W6's. Declared here as **ACCEPTANCE dependencies**, booked once there |
| **Value-side fixture bytes in `api/test/conformance/**`** | **Execution gate + G1.** Specified in the register; landed by the value.js API row |
| **Any glass-ui producer row** | **SS-6 relay DECLARED EMPTY**; producer-shaped findings leave as letters |
| **Editing F.W5's contract artifacts, `COHESION.md`, or any evidence record** | E-1/E-3 immutable; the spine is root-authored. Editing one destroys the evidence a gate cites or creates a hidden cross-wave write conflict |
| **Negative-control rows in any denominator** | MorphShapePreview §3, the PaperSearch trio, MobileFloatingToc, CollapsibleSection are **EVIDENCE** (P-9); counting them manufactures overlap |
| **Any coverage percentage published before X-9** | **One member-scope law first.** A percentage over an unfrozen member scope is a fabrication |
| **All product source, both repos; all fourier bytes** | Execution gate + read-only law |

### 6a. Excluded by ID — the 40 band identities F.W8 does not carry, each with its STATING home

**Why this table exists**, restated at round 4 without the arithmetic that used to open it (R4-3.2): these **40 numbered rows are F.W8's own citation operand** — the band identities this wave *names* and *does not carry*, one row each, each with the F-W5 clause that STATES it. ▲ **What it is NOT, stated so no fifth pass reads it as one: it is not a census partition, not a roster, and not a denominator.** The sentence struck from this slot — *"§1 measures routedTotal = 124 band identities; §3 books 84; the other 40 …"* — made the table the residue of a subtraction over a check file's mark tally, which is the construction R3-4 voided and R4-3 forbids by name. The record-side roster of record is `CENSUS-CANONICAL.md` **§2 `F.W5-W8` (89 rows)** ⟨*re-based at errata round 5, **E5-15**: read 90; `C:C-23` → UNROUTED*⟩, held at **F-W5** (R4-10); this table neither reproduces it nor competes with it. ▲ **The two axes are different and the difference is load-bearing**: the canonical's **home** is where a record's routing token sends the row; this table's **STATING home** is which F-W5 clause states the contract rule the identity witnesses. Where the two diverge, **the canonical governs the routing and the clause citation governs the statement** — and F.W8 books neither, so nothing here can fabricate a home in R4-10's sense. Landing shape per R-2's law and R-5's one-home rule: these are **CITATIONS, never bookings** — a second wave that needs an id cites the adopting wave, and **booking any row below here would double-book the keystone**.

▲ **TWO CANONICAL IDS CITED HERE RATHER THAN ROWED, so the table's forty stay stable and machine-checkable (R4-10's book-or-cite duty, discharged by citation).** **(1)** `fr-ContourPreview` **`L:L-5`** — the identity PASS-4 **D-1** found escaping the two-dash detector at the `F.W5(-W8)` spelling — is homed by the canonical at **§2 `F.W5`**, not in the band, and is **held at F-W5**; F.W8 asserts nothing over it and books nothing. **(2)** `fr-PaperSearchDropdown` **`C:C-23`** — the sole canonical `F.W5-W8` row with **zero bytes anywhere else in this file**, found by this seat by testing all **89** canonical band rows against these bytes — is cited to the same holder, **with the §1 minute attached**: its record's own line declares *"F.W5-W8 = no rows"* while the canonical homes that line's token ⌧ **RESOLVED AT THE CENSUS, ERRATA ROUND 5 (E5-15), ON THIS SPEC'S EVIDENCE (REST-51).** The canonical now reads `` | `C:C-23` | `C:C-24` | — | **UNROUTED** <sub>*errata R5 E5-15*</sub> | ``: the only `F.W5-W8` on `fr-PaperSearchDropdown.md:77` was the record's own **`F.W5-W8 = no rows.`** — **a load-bearing negative read as a positive routing** — on an INFO row that FOLDS by reference into banked `fr-PaperSearch C-Z1/C-Z2`. **F-W8 minuted the divergence three times, booked nothing either way, and said in terms that _"a wave with zero record-side rows may not settle a record-side reading."_ That posture was correct and the repair was always the census's.** The band roster is **89**, not 90. in the band. **The canonical governs; the divergence is F-W5's and the census's to settle, never this wave's**, and citing it here is the difference between a disagreement that is *recorded* and one that is *hidden by silence*. Every id is **record-qualified** (R-5). Homes verified at the bytes by this seat, 2026-08-28, with boundary-aware matching. ▲ **Variables for every ⟨cmd⟩ in this section, declared here beside their first use (R4-2.3)**: `R='/Users/mkbabb/Programming/value.js/docs/tranches/V/megatranche/registry/adjudicated'` (frozen corpus — line cites permitted, §0 addressing law item 2) · `C='docs/tranches/X/fourier/conformance/CENSUS-CANONICAL.md'` (the sole census operand, hash-pinned **`3e0a9acb3381`**… at the masthead ⟨*re-stamped at errata round 5, REST-51*⟩ and cited by §-anchor, never by line).

▲ **ROUND-2 ADDITIONS — rows 37–40, the measured residue (R2-7.1).** Rows 1–36 are round 1's, unchanged in substance; **rows 37–40 are the four band-routed identities the round-1 bare-token operand could not see** (PASS-2 D-1..D-4). Three were marked BOOKED because a bare token matched a *different* record's row; the fourth was in **no** prior operand at all, so no amount of re-reading the round-1 register could have found it — only re-deriving from the corpus does. Each is proved at the **frozen** corpus by a `sed` paste at §1 (line cites permitted there, §0 addressing law item 2), each names ONE home, and **each is an exclusion-with-reason: F.W8 books none of them.** Their homes were re-confirmed at F.W5's **live** bytes by this seat this round and are cited by **clause id** only.

▲ **ROUND-3 RE-ATTACHMENT — row 19, and why it is not bookkeeping (R3-7.2; PASS-3 D-1/D-2).** Round 2's row 19 **recorded** that a band token on `fr-EquationPanel.md:48` — a fifteen-identity MAJOR line — had been attached to the WRONG id, and then **left the right id out of the table**: `D-3+C-5`, which that row's own cell proves is **not band-routed**, occupied the slot, while `D-10+D-L12+C-22`, which **is** band-routed and is homed at F-W5 clause **F9**, had no row at all. The correction stopped one step short of putting the correct id in the operand. ▲ **An operand carrying one non-member and missing one member yields a non-empty set difference in BOTH directions** — and because §6a's own closing sentence declares this table to be *"the per-id operand **G15** runs its set difference against"*, that is not a bookkeeping slip: it is **G15's stated HALT condition, met at authoring.** The total survived only by **CANCELLATION** — one non-member in, one member out — which is exactly the accident §1's round-2 minute warns against (*"the prior '17 rows' was arithmetically consistent only by accident"*), and it is the same operand-integrity class R2-7.1/R2-7.4 convicted at round 2, surviving into round 3 in a new shape: not a homonym collision this time, but a **row-identity mis-attachment on a multi-identity line**. **The cure is symmetric and small**: `D-10+D-L12+C-22` takes row 19 as a band row homed at F9; the `D-3+C-5` correction is preserved as a **footnote of that row, explicitly OUTSIDE the 40**, so a later reader still reproduces the correction rather than rediscovering it; and the header and the closure stamp are **re-derived after the rows move, never defended** (R2-7.4). ▲ **ROUND-4 addendum**: round 3 closed that minute by observing that *neither `bookedCount` nor `routedTotal` moved* — true at the time, and moot now, since **both figures are struck** (R4-3.2) and the row-19 repair is judged on what it always should have been judged on: **the right identity is in the operand, with its home verified at the keystone, and the non-member is a footnote outside the forty.** **Every one of the THIRTY-NINE numbered rows below is a band-routed identity by its own cited bytes** ⟨**re-derived at repair round 5 under R2-7.4, AFTER the row moved and not in defence of it (REST-41).** The fortieth — `fr-CanvasControlsDock C-28` — is **struck as a numbered identity**: the canonical banks no such row for that record, its only `C-28` row is `GLASS-RELAY` at *another* record, and at the corpus the token lives only in prose as a fold into `D-4`, which §3 P7 books and now carries the fold by name. **The header and this closure stamp are re-derived from the moved table, never argued back to the old figure.**⟩ ⌧ **AND THE 16-OF-40 DISCLOSURE, DISCHARGED (REST-48; P5-7).** Joined against this file's own declared roster of record (§2 `F.W5` ⊎ `F.W5-W8` = **117** post-E5-15), **17 of the 40 named an identity absent from it**: one was REST-41's fabrication, now struck; **sixteen are canonical rows homed at F.W4 or F.W3 carrying `<sub>legs: F.W5-W8</sub>`** — band-**routed**, not band-**homed** — at rows **2 · 8 · 9 · 13 · 14 · 15 · 16 · 17 · 22 · 23 · 25 · 26 · 30 · 32 · 38 · 39**. §6a's header pre-empts them in terms, **so they were disclosed, not hidden — and R4-10's actual duty was undischarged**: fifteen of the sixteen named an **F-W5 clause** where the canonical holder is **F-W3** or **F-W4**. **The canonical holder is now named in each of the sixteen cells, beside the STATING clause.** ⊘ The two axes are both real; the row carries both.

| # | identity (record-qualified) | routed at | STATING home | F.W8's relation |
|---|---|---|---|---|
| 1 | fr-AdminAuditLog **AA-5** | `:42` | **F-W5 clause D16** (§D) — the shared action taxonomy | cited, not booked; the admin leg's taxonomy is F.W5's to state |
| 2 | fr-AdminAuditLog **AA-10** | `:47` | **F-W5 clause E18** (§E) — attribution: the actor is a FIELD ⟨**canonical holder: **F.W4** <sub>legs: F.W5-W8</sub>** — band-**routed**, not band-**homed**; named at repair round 5, REST-48 (R4-10)⟩ | cited; M2's identity-mixing assertion rides E18's predicate, states none of it |
| 3 | fr-AdminAuditLog **AA-23** | `:142` | **F-W5 clause D15** (§D) — query semantics under the shipped indexes | cited; no F.W8 fixture asserts index shape |
| 4 | fr-AdminFlaggedPanel **FR-AFP-1** | `:44` | **F-W5 clause D3 ⊙** (§D) — moderation producer-or-retire, THE ADMISSION GATE (also cited INVERTED at clause **D1**) | cited; the §5c F.W1 cycle passes through this ruling and F.W8 declares the cycle without owning the ruling |
| 5 | fr-AdminFlaggedPanel **FR-AFP-7** | `:55` | **F-W5 clause D4** (§D) — flag identity keys the ENTITY | cited. ▲ Also the D-4 homonym trap: a bare `grep -c "FR-AFP-7"` is masked by `FR-AFP-70`/`-71`, both of which F.W8 **does** book at J7/M3 |
| 6 | fr-AdminFlaggedPanel **FR-AFP-8** | `:56` | **F-W5 clause D5** (§D) — which identifier a human-facing surface may render | cited |
| 7 | fr-AdminFlaggedPanel **FR-AFP-18** | `:65` | **F-W5 clause A2** (§A) — envelope + cardinality ruled once | cited |
| 8 | fr-AdminFlaggedPanel **FR-AFP-32** | `:86` | **F-W5 clause D17** (§D) — date serialization, serializer arm ⟨**canonical holder: **F.W3** <sub>legs: F.W5-W8, SS-13</sub>** — band-**routed**, not band-**homed**; named at repair round 5, REST-48 (R4-10)⟩ | cited |
| 9 | fr-AppHeader **FR-AH-6** | `:52` | **F-W5 clause C3** (§C) — session truth + the canonical admin predicate ⟨**canonical holder: **F.W4** <sub>legs: F.W5-W8</sub>** — band-**routed**, not band-**homed**; named at repair round 5, REST-48 (R4-10)⟩ | cited; M2 asserts the authenticated leg's honesty over C3's predicate, does not state it |
| 10 | fr-BasisCanvas **C-17** | `:102` | **F-W5 clause E15** (§E) — ONE transport clause | cited. ▲ **Not** fr-CoefficientsPanel C-17, which F.W8 **does** book at J6 (= FR-CP-16) — the D-4 collision, now unmasked by qualification |
| 11 | fr-BasisCanvas **C-18** | `:103` | **F-W5 clause E15** (§E) | cited |
| 12 | fr-ContourEditorCanvas **C-2** | `:48` | **F-W5 clause E17** (§E) **+ gate G15** (§3) — image bounds on write | cited. ▲ **Not** fr-FourierShapeExtractor's `L-B2 / C-2`, which sits in D2's block |
| 13 | fr-ContourPreview **row 28 / D:i-3 · L:L-8** | `:61` | **F-W5 clause A3** (§A) — boundary-validation evenness ⟨**canonical holder: **F.W4** <sub>legs: F.W5-W8</sub>** — band-**routed**, not band-**homed**; named at repair round 5, REST-48 (R4-10)⟩ | cited |
| 14 | fr-ContourSettings **M-15** | `:72` | **F-W5 clause F5** (§F) — *"one server identity, two witnesses"* (with C·D-02) ⟨**canonical holder: **F.W3** <sub>file-criterion → §5.d</sub> <sub>legs: F.W5-W8, SS-13</sub>** — band-**routed**, not band-**homed**; named at repair round 5, REST-48 (R4-10)⟩ | cited |
| 15 | fr-ContourSettings **m-18** | `:94` | **F-W5 clause E13** (§E) — cache identity ⊇ consumed fields ⟨**canonical holder: **F.W3** <sub>file-criterion → §5.d</sub> <sub>legs: F.W5-W8</sub>** — band-**routed**, not band-**homed**; named at repair round 5, REST-48 (R4-10)⟩ | cited; F.W8 books the same clause's **B-4 ⊕ i-7** legs at R4 and asserts them — E13 remains the single STATING home |
| 16 | fr-EasingPicker **L/M-3** | `:65` | **F-W5 clause E11** (§E) — easing domain hoisted to the operation (**merged with RESOLVER, both ids preserved**) ⟨**canonical holder: **F.W4** <sub>legs: F.W5-W8</sub>** — band-**routed**, not band-**homed**; named at repair round 5, REST-48 (R4-10)⟩ | ▲ **The sharpest of the 36** (D-3): R1 asserts *"only easing/speed/active_bases round-trip"* while the easing **domain** row is E11's. F.W8 **asserts over** E11's hoisted domain and **states none of it**; the RESOLVER half is already carried at §3 J1's fold list, so the mechanism is present and the id now is too |
| 17 | fr-EditorControlsDock **D-12 / C-4 / C-5** | `:59` | **F.W4** — the `:disabled` / pending-state / double-submit client arm; **no F-W5 clause books it by bytes** (re-measured 2026-08-28) ⟨**canonical holder: **F.W4** <sub>legs: F.W5-W8</sub>** — band-**routed**, not band-**homed**; named at repair round 5, REST-48 (R4-10)⟩ | excluded-with-reason, and already excluded by class at §6 row 4 (FR-GV-1's split law). This record's band leg that F.W8 **does** carry is **C-6 / C-7** (`:60`) at §3 P7, stated at **F-W5 clause C1** (§C). ▲ **ROUND-3 — THE HOME IS SHARPENED AGAINST THE FINAL TWIN PARTITION (R3-1a; the round-2 spelling was the vague `F.W3/W4`).** The row is a **sole `F.W4`** spelling at the bytes, ⟨cmd⟩ `sed -n '59p' $R/fr-EditorControlsDock.md \| grep -oE 'F\.W[0-9]+([-–/]W[0-9]+)?'` (this seat, 2026-08-28) → `F.W4` then `F.W5–W8` — the wave token is `F.W4` alone, and the band token beside it is a **rider**, verbatim *"**F.W5–W8 rider**: C-7's idempotency channel at the same seam"*, whose subject is the NEXT row's identity (`C-6 / C-7`, `:60`), which F.W8 books at P7. Under **F-W3 §X.1-v4**'s criterion (quoted in full at row 19's footnote) *"sole spellings home at their named wave"*, so this identity's one home is **F.W4** — no dual arm, no default arm, and nothing asked of F.W3. The row sits in this table because the band token on its line puts it in `routedTotal` under the shape-agnostic detector, not because F.W8 or the band owns the cure |
| 18 | fr-EqCoefficientsPanel **FR-EQC-8** | `:45` | **F-W5 clause A5** (§A) — no untyped operations (= C-11) | ▲ Material (D-3): *"F.W5–W8 owns the tuple seam … one adapter, both directions; rides the R6-8 contract lesson."* F.W8's J3 asserts the same law over `M-13`; the tuple-seam **typing decision is A5's**, and J3's cure clause — *typed at F.W5 or NAMED AND STRUCK in the register* — is exactly this row's disposition |
| 19 | fr-EquationPanel **D-10+D-L12+C-22** | `:48` | **F-W5 clause F9** (§F) — *A parameter that does not govern* | ▲ **ROUND-3 (R3-7.2) — the band identity round 2 named in prose and failed to row.** Banked routing at the FROZEN corpus, ⟨cmd⟩ `sed -n '48p' $R/fr-EquationPanel.md \| grep -oE "D-10\+D-L12\+C-22[^·]*F\.W5-W8"` (this seat, 2026-08-28) → verbatim: *"D-10+D-L12+C-22 (Terms names a quantity it stops governing; `budget` never forwarded — re-verified at latex_rendering.py:281 this session) — F.W5-W8"*. **Home confirmed at the live keystone**, ⟨cmd⟩ `grep -n '\*\*F9\*\*' waves/F-W5.md` → the clause row *"**F9** A parameter that does not govern"* over the witness cell *"D-10 + D-L12 + C-22 (EquationPanel)"* and the act cell *"Composes with F2's shared constant and B1's under-determined key: **the SAME `budget` field is unbounded at the client, unforwarded at the server, and absent from the operation's identity.**"* **cited, not booked**: F9's rule — a field the operation never forwards cannot belong to that operation's identity — is the same law J4 consumes for generated bounds and R4 for cache identity, and **F.W8 states none of it**; the assertion direction only. ▲ **FOOTNOTE — the census correction, DEMOTED AND EXPLICITLY OUTSIDE THE 40.** This slot previously held `fr-EquationPanel D-3+C-5`, which is **not band-routed**: ⟨cmd⟩ `sed -n '48p' $R/fr-EquationPanel.md \| grep -oE "D-3\+C-5[^·]*F\.W4 \+ F\.W2 rider"` → verbatim: *"D-3+C-5 (**AMENDED**: cure blast radius = 3 consumers / 2 routes — EquationView.vue:7/:70 + InfoCard.vue:5/:14 join the repair's regression surface; 6-literal budget per M-N1 unchanged) — F.W4 + F.W2 rider"*. **That is a SOLE `F.W4` spelling, so the twin partition now FINAL homes it by criterion rather than by default** — ⟨cmd⟩ `grep -o 'One question of a PATH[^!]*the other twin CITES, never books' waves/F-W3.md` (this seat, 2026-08-28) → **F-W3 §X.1-v4**, verbatim: *"One question of a PATH — **is the record's target file in a §5 unit file list (`.a`–`.e`)?** — dual `F.W3/W4` rows on `.a`–`.e` files home at **F.W3**; all others take the default arm to **F.W4**; sole spellings home at their named wave; **the other twin CITES, never books**"*. `D-3+C-5`'s home is therefore **F.W4**; it is **not** a band identity, **not** a member of `routedTotal`, holds **no row number**, and is counted in nothing here. The line's other band member is `D-L5+C-1` at row 20 — the two together are all of `:48`'s band routing, and F.W8 books neither |
| 20 | fr-EquationPanel **D-L5+C-1** | `:48` | **F-W5 clause C4** (§C) — retry / limiter posture | cited; M2's `retryOn429` assertion rides C4's posture |
| 21 | fr-EquationView **B-1** | `:47` | **F-W5 clause F1 ‡** (§F) — partial projection; `latex_sigma` joins `SimplifyResponse` | cited. ▲ **Not** fr-ContourSettings B-1, which F.W8 **does** book at J4 — the D-4 collision. F1's cure is itself **CURE-BOUND by M-CK** (fix the key first), and F.W8 carries M-CK-class as a §5b lock |
| 22 | fr-EquationView **B-2** | `:48` | **F-W5 clause F2 ‡** (§F) — one shared bound constant (`budget` walks past `le=50`) ⟨**canonical holder: **F.W4** <sub>legs: F.W5-W8</sub>** — band-**routed**, not band-**homed**; named at repair round 5, REST-48 (R4-10)⟩ | cited. ▲ **Not** fr-GalleryDraftsSection B-2, which F.W8 **does** book at P7/G10 |
| 23 | fr-EquationView **C·D-02** | `:56` | **F-W5 clause F5 ‡** (§F) — the error envelope is actually spoken ⟨**canonical holder: **F.W4** <sub>legs: F.W5-W8</sub>** — band-**routed**, not band-**homed**; named at repair round 5, REST-48 (R4-10)⟩ | cited |
| 24 | fr-EquationView **M-DV** | `:113` | **F-W5 clause F4** (§F) — server-side domain validator | cited; J4's generated-bounds act consumes F4's validator, authors none |
| 25 | fr-FunctionInput **L-B1 / C-1** | `:38` | **F-W5 clause F2** (§F; **FOLDS** there with fr-EquationView B-2 ⊕ C-8) ⟨**canonical holder: **F.W4** <sub>legs: F.W5-W8</sub>** — band-**routed**, not band-**homed**; named at repair round 5, REST-48 (R4-10)⟩ | cited. ▲ **Not** fr-FourierShapeExtractor L-B1, which sits in D2's block; F.W8 books this record's **C-8 / N-4** at J4 |
| 26 | fr-FunctionInput **L-M3** | `:53` | **F-W5 clause F6** (§F) — a response field states WHICH population it measures ⟨**canonical holder: **F.W4** <sub>legs: F.W5-W8</sub>** — band-**routed**, not band-**homed**; named at repair round 5, REST-48 (R4-10)⟩ | cited. ▲ **Not** fr-FourierShapeExtractor `L-M3 / C-6`, which sits in D2's block |
| 27 | fr-GalleryCard **C-1-as-corrected** | `:50` | **F-W5 clause D2 ‡** (§D) — the like verb (*four witnesses, ONE identity*) | cited; the like verb is not one of the walk's five |
| 28 | fr-GalleryCardModal **GCM-2** | `:43` | **F-W5 clause D2** (§D) — same four-witness identity | cited. ▲ Masked from a naive `grep -c` by `GCM-25`; F.W8 books GCM-1/-10/-52/-55 |
| 29 | fr-GalleryFeaturedCarousel **FR-GFC-3** | `:34` | **F-W5 clause D2** (§D) | cited |
| 30 | fr-GalleryFeaturedCarousel **FR-GFC-20** | `:54` | **F-W5 clause D8** (§D) — a blanket write is not a transition ⟨**canonical holder: **F.W4** <sub>legs: F.W5-W8</sub>** — band-**routed**, not band-**homed**; named at repair round 5, REST-48 (R4-10)⟩ | cited; M3 asserts state-preservation over D8's rule and states none of it |
| 31 | fr-GalleryMarquee **GM-M4** | `:70` | **F-W5 clause B4** (§B) — **the LIVENESS predicate (registry-novel)**, beside PP-DEADSEAM | ▲ Material (D-3): the record's only band row, a live dead CRUD surface. **The mechanism is already F.W8's R6 and G13** — F.W8 books PP-DEADSEAM there and **measures** the unconsumed set; **B4 STATES the predicate, R6 measures it**, and GM-M4's id is now visible as one of B4's witnesses rather than absent by byte |
| 32 | fr-GalleryView **FR-GV-7** | `:43` | **F-W5 clause A6** (§A) — credential transport, both ends (cited again at clause **C3**) ⟨**canonical holder: **F.W3** <sub>file-criterion → §5.d</sub> <sub>legs: F.W5-W8</sub>** — band-**routed**, not band-**homed**; named at repair round 5, REST-48 (R4-10)⟩ | ▲ Material (D-3): an admin bearer token accepted from the URL query. **M4 asserts, per operation, the authority class F.W5's register declares** — this row is one of the classes it asserts against; the transport **rule** is A6's and F.W8 authors none of it |
| 33 | fr-GalleryView **FR-GV-34** | `:73` | **F-W5 clause D6** (§D) — lifecycle truth and cascade (member-site rider on FR-AFP-9) | cited; F.W8's D9 lock carries the ruled lifecycle vocabulary, and D6 owns the cascade |
| 34 | fr-ImageUpload **row 26 / C:C-12** | `:60` | **F-W5 clause E15** (§E, transport half) **+ clause C2** (§C, security half) — cited again at clause **B1** (§B) **only** | ▲ Material (D-3): *"the incomplete **R6-8 join** — FOLD to the intake row."* The R6-8 join is F.W8's governing seam and is booked at **J1**; this instance is the record's own fold, and its **two halves are stated at two F.W5 clauses**. F.W8 asserts the join, re-books neither half. ▲ **RE-KEYED AT ROUND 2 — the `B5` limb is STRUCK (R2-7.2 / PASS-2 D-6): this cure table had itself committed the very homonym error it was authored to cure.** ⟨cmd⟩ `grep -n '\*\*B1\*\* The join relation' waves/F-W5.md` (this seat, 2026-08-28) → clause **B1**'s witness cell carries *"row 26 C:C-12 (ImageUpload)"* — correct, and the citation kept. ⟨cmd⟩ `grep -n '\*\*B5\*\* Negative controls' waves/F-W5.md` → clause **B5** *"Negative controls — **EVIDENCE, never a denominator**"*, witness cell *"HLG-23 (C:C-12) · C §3 negatives (MorphShapePreview) · P-9 (cited; home F.W4)"* — **that `C:C-12` is fr-HarmonicLevelGrid's, not fr-ImageUpload's**: ⟨cmd⟩ `grep -n 'HLG-23' $R/fr-HarmonicLevelGrid.md` → `:74`, whose row head reads *"**HLG-23 · C:C-12**"* over the witness *"`/morph` reaches zero of the 45 API operations …"*. Two records spell one token; B5 books the other one. **Cross-check that the strike is not itself a drop**: HLG-23 is a *negative control* (P-9 evidence, never a denominator) and fr-HarmonicLevelGrid is one of §1's five boilerplate-only records that route **nothing** — so it is outside routedTotal by measurement and owes this table no row |
| 35 | fr-SpeedSelect **SS-L-07 / SS-C-10** | `:59` | **F-W5 §5** (*"Carried by citation — the excluded-from-clause-tables set (G19's other operand)"*), **the row keyed `SS-L-07 / SS-C-10`** ≡ **`carry/F-W4-CARRY.md` §Rows D (the transport cluster), the `SS-L-07/SS-C-10` ⟨SpeedSelect⟩ bullet** — cited by **row label at both ends**, never by line (R2-2; the round-1 `:234` address is struck **as an address**, its bytes re-verified unchanged). ▲ **ANCHOR SPELLING CORRECTED AT THE RESUMPTION SITTING**: the earlier sitting wrote *"§D"*, which is not a heading this carry has — ⟨cmd⟩ `grep -oE '^## §[A-Za-z0-9]+' carry/F-W4-CARRY.md` (re-run this sitting, 2026-08-28) → the five level-2 §-anchors, **complete and in order**: `## §0` · `## §Rows` · `## §Gates` · `## §Bounds` · `## §CrossEdges`; and ⟨cmd⟩ `grep -n '^### D · ' carry/F-W4-CARRY.md` → `172:### D · fr-AnimationControls · fr-GlassTimeline · fr-SpeedSelect · fr-EasingPicker · fr-EasingCurvePreview (the transport cluster)` — **nested under `## §Rows`**. ▲ **ROUND-3 (R3-3.9 / PASS-3 D-3): round 2 printed that same list under the WIDER probe `grep -n '^#\{1,4\} '`, whose real return also carries the level-1 title, the trailing text on three of the five level-2 headings, and the level-3 headings `§0a` and `A`…`K` — an elision offered as the whole output, with no ellipsis, at the very site where the masthead one screen earlier convicts itself for this exact class at `F-W5.md`.** The cure is **not a bigger paste but a probe whose output IS what is rendered**: the narrowed command above returns exactly the five §-anchors and nothing else, the second returns exactly the cluster line, and the wider probe's extra returns are named here rather than dropped. The **substantive** claim was never in doubt — `§Rows D` is the right anchor, it exists, and the bullet quoted below is byte-identical — which is precisely why the form had to be fixed rather than argued. A stable anchor that does not exist is no better than a line number that has drifted, and the keystone spells the same anchor the same way — ⟨cmd⟩ `grep -n 'SS-L-07' waves/F-W5.md` → its §5 row cites *"`carry/F-W4-CARRY.md` **§Rows D — the transport cluster — its `SS-L-07/SS-C-10` bullet** (stable anchor, never by line)"*, so both ends now resolve by one spelling. ⟨cmd⟩ `grep -n 'SS-L-07' carry/F-W4-CARRY.md` (re-run this sitting, 2026-08-28) → *"- **SS-L-07/SS-C-10** ⟨SpeedSelect⟩ MINOR — the value domain exists only as ten template literals; the adjacent control's idiom (`lib/easings.ts` → `EASING_OPTIONS` + `AnimationEasingName`) is one file away; no `lib/speeds.ts`, no `SpeedName`."* ⟨cmd⟩ `grep -n 'SS-L-07' waves/F-W5.md` → F-W5 §5's row, which **splits the identity** verbatim: *"The **body is F.W4's** — the literal-union speed catalog … **F.W5's share is the type-reach only**"*, admitted under clauses **D12** and **E11** | ▲ **The only escape whose home is not an F-W5 clause** — at first read it had **zero** F-W5/F-W6 bytes; **re-measured 2026-08-28 it is homed at F-W5 §5 by citation**, with F.W5 claiming no credit for the body (FR-GIG-5 bar). This is the sole escape whose banked byte lives in a real in-tree carry, and the carry is cited **by path** (R-3) with a **label**, not a line — a carry is a live sibling and R2-2.2 admits line cites only for the frozen corpus. F.W8 books neither it nor a substitute: R1 asserts the **speed atom's round-trip**, while the **value-domain catalog** is F.W4's body and F.W5's type-reach |
| 36 | fr-UserSlugBar **FR-USB-15** | `:52` | **F-W5 clause C3** (§C) — session truth + the canonical admin predicate (with FR-AH-6, row 9) | ▲ Material (D-3): the MAJOR of the cluster whose two MINORs (**FR-USB-23 / -24**) F.W8 **does** book at M2. The asymmetry is now stated, not silent: **C3 states the session-truth cure** (401 handling + `getMe` revalidation at the store/api seam); **M2 asserts the authenticated leg's honesty over it.** ▲ The **FR-USB-23 ⊙ owner-gated ⊙ is kept on M2's routing row** (RULINGS R-1e), and **FR-USB-38 stays DO-NOT-BOOK** per §5c's F.W2 negative roster |
| 37 | fr-ContourSettings **M-13** (= LC-missed-2) | `:70` | **F-W5 clause E14** (§E) — *Contour provenance — an editor-saved contour is FIRST CLASS* ‡ | ▲ **ROUND-2 (R2-7.1.1) — the worst of the four: a PRIMARY band routing, banked MAJOR, in F.W8's own derive-and-history subject matter, with ZERO bytes in this file before this round.** Banked routing at the frozen corpus, ⟨cmd⟩ `sed -n '70p' $R/fr-ContourSettings.md \| tail -c 320` (this seat, 2026-08-28) → *"… **→ F.W5–W8** (contour provenance/CRUD union: an editor-saved contour must be a first-class input to compute, and extraction must not silently overwrite `source="editor"` assets)."* **How it hid**: every `M-13` byte in this file is **fr-CoefficientsSpectrum M-13** (J1's fold list, J3, G4) or a collision roster — the D-4 homonym, and the one this file *diagnosed at §5b and then fell into*. **Home confirmed at the live keystone**, ⟨cmd⟩ `grep -n '\*\*E14' waves/F-W5.md` → the clause row *"**E14** Contour provenance — an editor-saved contour is FIRST CLASS ‡"* over the witness cell *"**fr-ContourSettings M-13 ⊕ fr-BasisSelector M-14** *(NOT fr-CoefficientsSpectrum M-13 — the A5 homonym; U-12 qualification)*"* — F.W5 names the homonym in the clause itself. **Excluded-with-reason, not booked**: E14 is the single STATING home; §3 **R4** now CITES it beside `B-4 ⊕ i-7` (same record, same derive seam) and **books nothing** |
| 38 | fr-ContourSettings **M-10** (= L-M6 / C-19 / D-m9) | `:67` | **F-W5 clause D12** (§D) — *Closed domains at the boundary* ⟨**canonical holder: **F.W3** <sub>file-criterion → §5.d</sub> <sub>legs: F.W5-W8</sub>** — band-**routed**, not band-**homed**; named at repair round 5, REST-48 (R4-10)⟩ | ▲ **ROUND-2 (R2-7.1.2) — the shared-enum contract rider.** ⟨cmd⟩ `sed -n '67p' $R/fr-ContourSettings.md \| tail -c 260` → *"… **→ F.W3/W4** (one typed vocabulary; the shared-enum contract itself is an **F.W5–W8 rider**)."* The **row** routes F.W3/W4; the **rider named inside it** routes the band — which is why a row-terminal-cell reader misses it and the shape-agnostic detector (G15) does not. **How it hid**: this file's only `M-10` is **fr-BasisSelector M-10**, a *different* banked row (the `basisFilter` normaliser, itself routed F.W3/W4) carried at P8's WAVE-LOCK; the aliases `L-M6` / `C-19` have **0 bytes** here. ▲ **HOME CORRECTED AGAINST THE RULING, AT THE BYTES (§0 ERRATUM 1)**: R2-7.1 proposed *"the E13/A5 family"*; the measured home is **D12** — ⟨cmd⟩ `grep -n 'M-10' waves/F-W5.md` → the clause row *"**D12** Closed domains at the boundary"* over the witness cell *"FR-AUL-21 ⊕ FR-AUL-31 · **fr-ContourSettings M-10** (=L-M6/C-19/D-m9) · **fr-BasisSelector M-9** ⊙ (=L-M4/C-20) · FR-NP-30b (cited A5)"*. The ruling's **LAW** (exclusion-with-reason at the F-W5 clause home, homonym named in the row) is applied unchanged; only its coordinate is corrected, per R2-1-LAW.3 — a rulings file is a cure-shape, never quotable authority over the bank. **The homonym is named here so the collision that hid this row is disarmed**, and F.W5 names it too at its ANTI-RENAME block as the **ninth** colliding token (quoted at G15) |
| 39 | fr-GalleryCard **L·M-4 / D-13 / C-8(a) + L·D-2 / C-12** | `:64` | **F-W5 clause D17** (§D) — *Date serialization — **serializer arm only*** ⟨**canonical holder: **F.W3** <sub>legs: F.W5-W8, F.W3/W4, SS-13</sub>** — band-**routed**, not band-**homed**; named at repair round 5, REST-48 (R4-10)⟩ | ▲ **ROUND-2 (R2-7.1.3) — the serializer leg.** ⟨cmd⟩ `sed -n '64p' $R/fr-GalleryCard.md \| tail -c 300` → *"… GalleryCard:53-61; → F.W3; serializer → F.W5–W8; JSC → SS-13) …"* ⟨**PURGE-SEAT BYTE-CHECK, R3-3.9.** As banked the span read `serializer → **F.W5–W8**`; **the record carries no emphasis there** — the bold was this file's, added inside a span presented as command output. Re-quoted from the bytes; the emphasis moves outside the quotation marks, where the reading belongs.⟩ — a **three-way split inside one row**: the render arm to F.W3, the serializer arm to the band, the JSC magnitude arm to SS-13. **How it hid**: `L·M-4` and `L·D-2` have **0 bytes** in this file; the row matched only through the bare token `D-13`, which here spells **fr-GalleryInfiniteGrid**'s P8 fold — now record-qualified at §3 P8 with this very record named as the collision. **Home confirmed**, ⟨cmd⟩ `grep -n '\*\*D17' waves/F-W5.md` → D17's witness cell carries *"**`fr-GalleryCard L·M-4 / D-13 / C-8(a) + L·D-2 / C-12`** folds here — the FULL banked spelling"*, and its act cell scopes F.W5 to *"ONLY the serializer arm"*. **Anti-rename**: the identity is carried under the record's own full banked spelling, never shortened to `D-13`. F.W8 asserts no date-serialization fixture; the **JSC → SS-13** leg is magnitude only and discharges no gate here (§5c SS-13 row) |
| 40 | fr-GalleryDraftsSection **m-15** (= `r2-missed-3`) | `:80` | **F-W5 clause C2** | **CITED, not booked — ESCAPED UNTIL REPAIR ROUND 5 (REST-46; P5-1 at F-W8).** Canonical `` | `m-15` | `r2-missed-3` | `F.W5-W8` | **F.W5-W8** | `` at fr-GalleryDraftsSection; ⟨cmd⟩ `/usr/bin/grep -n 'fr-GalleryDraftsSection' waves/F-W5.md` → `320:| **fr-GalleryDraftsSection** (2) | \`F-4\` → C1 · \`m-15\` → C2 |`. The mechanism is the unversioned thumbnail URL + 24 h `public` cache; **the band identity is F-W5's and F.W8 books nothing.** ⊘ **Until this round it lived only inside the quoted F-W5 C2 span at `:40` and `:210` — bare, ambiguous, in no §3 cell and no §6a row. The escape and the instrument are one defect: a bare `grep -qF` on a six-record homonym cannot see it** (REST-45). |
| ⌧ | ~~fr-CanvasControlsDock **C-28**~~ — **STRUCK AS A NUMBERED IDENTITY AT REPAIR ROUND 5 (REST-41; P5-2, HIGH)** | `:102` | **NOT A CANONICAL ROW.** ⟨cmd⟩ `/usr/bin/awk 'NR>=806 && NR<876' "$C" | /usr/bin/grep -c 'C-28'` → **0**; the canonical's only `C-28` row is `` | `C-28` | — | `GLASS-RELAY` | **GLASS-RELAY** | `` at **fr-AnimationControls**, another record; ⟨cmd⟩ `/usr/bin/grep -nE '^\| \*\*C-28' $R/fr-CanvasControlsDock.md` → **∅, exit 1**. At the corpus `C-28` lives only in **prose**, as a fold **into `D-4`** — and `D-4` **is** a canonical row (`` | `D-4` | `L-1` · `C-3` | `F.W4` · `F.W5–W8` | **F.W4** <sub>legs: F.W5-W8</sub> | ``) which §3 **P7** already books. **The C-28 fold is carried where the record carries it — as a named fold on `D-4` inside P7's cell — and §6a's header and closure stamp are re-derived AFTER the row moves (R2-7.4), never defended.** | ⊘ **The row convicted itself in its own cell**: it read *"F.W8 books **D-4/L-1/C-3** … and **does not** book C-28"* and was numbered as an identity anyway. **G15's LHS therefore carried a member its canonical RHS cannot contain — a non-empty set difference at authoring, which is this wave's own stated HALT condition, met for the third round running in a new costume.** The round-2/round-3 provenance below is preserved as dated history with no roster weight: the escape was in no prior operand; the `:102` prose sentence carries no emphasis on the `C-28` clause and the bold was this file's; B1's witness cell at `waves/F-W5.md` carries *"**fr-CanvasControlsDock C-28** (`:150`, fold-by-reference — R2-3d.5)"*. |

**Closure statement.** **40 identities, 40 dispositions, zero bookings** — **38** cited to an F-W5 clause verified by clause id at this seat's 2026-08-28 read (**rows 1–16 ⊎ 18–34 ⊎ 36–40**), **1** excluded to the twin surface with its reason (row 17, homed at **F.W4** by §X.1-v4's criterion), and **1** homed at **F-W5 §5**'s carried-by-citation set (row 35). The ranges are contiguous and re-derive: `16 + 17 + 5 = 38`, with `17` and `35` the two named exceptions and `38 + 1 + 1 = 40`. ▲ **ROUND-4 — THE CLOSURE ARITHMETIC ENDS AT THE ROWS (R4-3.2).** Round 3 closed this stamp with *"84 booked + 40 named = 124 = routedTotal, so §3 ⊎ §6 ⊎ §6a partitions the band's routed identities with no residue"* — **struck whole**: F.W8 books nothing, so there is no `84`; F.W8 has no record-side denominator, so there is no `124`; and a **partition claim over the band belongs to the roster's holder** — F-W5, per R4-10 — not to the wave that cites it. What survives is the only claim this table was ever entitled to make and can still prove from its own visible bytes: **forty numbered rows, forty dispositions, zero bookings**, ranges contiguous and self-deriving. A partition that needed a figure to be believed was never a partition; it was a subtraction. ▲ **RE-DERIVED AT ROUND 3, AFTER THE ROWS MOVED — and the re-derivation, not the total, is what changed (R3-7.2; PASS-3 D-1/D-2).** Round 2 stamped *"37 cited … 1 excluded to F.W3/W4 (row 17), 1 recorded as a census correction (row 19)"* over a table whose row 19 held an identity its own cell proved **not band-routed**, while that line's true band identity sat in the same cell's prose with **no row**. **The sum survived by CANCELLATION — one non-member in, one member out** — so a table declared to be G15's per-id operand carried a non-empty difference in **both** directions: the wave's own HALT condition, met at authoring. Round 3 re-attached the band identity at row 19, demoted the census correction to that row's footnote outside the 40, and **only then** re-stated the stamp. ▲ **The same round-2 enumeration also omitted row 36 from its own list of clause-homed rows while counting it in the 37** — a list that did not reproduce its own figure, cured here by making the ranges contiguous so the figure is derivable from the ranges rather than asserted beside them. ▲ **The round-1 and round-2 stamps, and the whole chain of figures that produced them, are struck and MINUTED ONCE — at §1's strike, not twice here (R4-3.2; E-3).** The reading that matters survives without them: round 1's sum was internally consistent and **externally false**, because the operand beneath it was a bare-token roster and the residue was non-empty in both directions — **G15's HALT condition, met at authoring**. That lesson is the reason this table is now differenced against a **roster held by another wave** rather than against a number this wave computed for itself. ▲ **The stamp follows the operand, always: if G15's unit `e` closure run returns a different denominator, the closure run governs and this statement is amended, not defended.** ▲ Homes are keyed by **clause id + section**, never by line: `F-W5.md` is under concurrent repair this round and its lines moved between two reads by this seat in one session (§0's addressing law, generalised at R2-2 to every live sibling). ▲ This table is the per-id operand **G15** runs its set difference against, and at round 4 the **other** side of that difference is the **canonical roster** (`CENSUS-CANONICAL.md` §2), not a probe of this seat's own. The shape lesson still binds the *reading* of any record — **all four round-2 additions are bullet or prose rows**, and a table-only census manufactured exactly those four drops — but it no longer has to be the roster's guarantor, which is the one job R2-9's decree was never able to do (PASS-4 D-1 proved it at the spelling axis in the same breath). ▲ It creates **no** new obligation for F.W5 beyond the reciprocal R-5 already rules onto **F-W5 §4** — every one of rows 37–40 was found **already homed** at the keystone's live bytes, so this round adds citations, not asks. ▲ And per FR-GIG-5, **naming a row is not claiming its cure**.
