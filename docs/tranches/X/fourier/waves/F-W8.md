# X.F.W8 — The CRUD union prototype, end to end (the horizon probe)

**Sub-tranche**: X·F (`docs/tranches/X/fourier/waves/`), authority `docs/tranches/X/COHESION.md` §0/§1 **SS-4** (`COHESION.md:36`, `:69`) — *"CRUD/provenance union … union prototype → F.W5–W8, + the value-side counterparty riding X·V W3/W9 surfaces"*.

**Fold provenance (M-23 / M-12)**: this file is the fold-seat output, 2026-08-28, **repaired at X·F L-20 repair round 1 (2026-08-28)** under `docs/tranches/X/fourier/conformance/PASS-1/RULINGS.md` (R-1f · R-3 · R-4b · R-5 · R-9.1) and its local register `conformance/PASS-1/F-W8-CHECK.md`.

**Intake authority — stated once, and it is NOT a carry.** `ls docs/tranches/X/fourier/carry/` → `F-W1-CARRY.md`, `F-W4-CARRY.md`: the only two carries in tree, and neither is F.W8's. Per R-3.2 **no F.W8 carry is authored here**, and the vocabulary *"the CARRY"* is retired from this file. This wave's rows are derived from **the 66 adjudicated records at `docs/tranches/V/megatranche/registry/adjudicated/`**, re-checked against **`conformance/PASS-1/F-W8-CHECK.md` §1**'s id-keyed census; the §3 table agglomerates **28 rows** over the band's routed identities (FR-GV-8 ⊕ FR-GV-27 deduped by identity into one booking at §3 P5, both ids preserved). The three value-side amendments A-1/A-2/A-3 were **re-measured by this seat 2026-08-28** and every figure reproduced (the A-1 excision commit, the A-2 spelling census 120/26 · 72/24 · 192/49 · 23 en-dash-only · 0 direct, the A-3 `model.ts:84` anchor).

**The keystone section actually read** (R-1f; the prior draft attested to a section that does not exist): `F-W5.md` has **no §6b and no §6c**. Its headings are `## 4. Cross-edges — declared from THIS end (one home, two citations)`, `## 5. Carried by citation`, `## 6. Close`. This seat read **`F-W5.md` §4**, twice: at first reading it declared **no edge to F.W7 or F.W8** (`grep -c "F\.W8" F-W5.md` → **0**) — the defect §5c declares; **on re-measure later the same session the R-5 reciprocal had LANDED** and §5c below states the corrected fact. Neither authoring half is the wave; this fold is the wave's spec.

▲ **ADDRESSING LAW FOR EVERY `F-W5.md` CITATION IN THIS FILE (E-3 / D-19).** `F-W5.md` is **under concurrent repair in this same L-20 round**, and its line numbers moved *between two reads by this seat in one session* (§4 was at `:245`, then `:255`). A line number is therefore an **address, not an identity**: every citation below names the **CLAUSE or GATE ID and its section** — `F-W5` clause **E15** (§E), gate **G22** (§3) — which are banked and anti-rename-stable, and any line figure is a **dated MEASURE-AT-OPEN reading, re-resolved at open, never load-bearing**. Line readings in this file are this seat's, **2026-08-28**. Where a sibling's repair moves a clause, the citation still resolves; where it renames one, that is a defect against anti-rename, not against this file.

**Half**: **library half — Opus author seat (M-12 tri-fold)**. Stated honestly: F.W8 owns **no product source at all**, so the wave's substance does not divide along a library/demo axis; this half authors the complete spec and the Fable arbiter fold reconciles it against the sibling half. Neither half is the wave; the fold is.

**Status**: **planned**.

**EXECUTION IS NOT AUTHORIZED BY THIS FILE.** Nothing **writes, builds, or runs** product source in either repo until the owner's begin-word. `/Users/mkbabb/Programming/fourier-analysis` is **READ-ONLY, always** — this wave writes zero fourier bytes; the FN-6 ask travels as a letter. Specifying is not doing.

▲ **READ-ONLY MEASUREMENT CARVE-OUT (stated so the absolute is not self-contradicted).** The prior draft said *"nothing opens"* while the spec's strongest witnesses (A-1/A-3, G1, G16, §3 R3) are **reads**. The absolute is restated precisely: **inspection — `ls`, `grep`, `git log`, `git show`, reading a file — is permitted, must be DATED and ATTRIBUTED at its row, and produces no authorization**; **mutation — writing, building, running, installing — waits for the begin-word.** The fourier tree is inspected under this carve-out and **written never**, in any verb, at any time. A read that finds a corpus anchor stale is **recorded as a re-resolve obligation (D-19)**, never adopted as this file's anchor (§4, discipline 1).

---

## 1. State

**Name**: F.W8 — the union prototype walked end to end: create → derive → diff → remix → history, against **both** palette and fourier-viz objects, with conformance fixtures in **both directions**.
**Opens after**: **F.W0** (substrate pre-gates — hard) · **F.W1.close** (DAG; one cycle DECLARED, §5c) · **F.W2** (ordering) · **F.W5** (the ADMISSION KEYSTONE — its three artifacts are inputs, not prose).
**Agents**: 5 **serial** (units b/c/d share the fixture-register identity space; §5a forces sequencing).
**Hard gate**: 16 conditions in §4, **all born-RED**, **each keyed by id to a banked witness carried in a §3 row — none minted without a registry or in-tree authority** (the operand is the registry itself, never a carry; R-3). 4 are OWNER-GATED (G1, G7, G12, and G3's denominator freeze) and may not be authored unruled.

### Four-verb status (separate fields; this file moves exactly one)

| verb | value | evidence |
|---|---|---|
| AUDITED | **YES** | 26 of the 66 adjudicated `registry/adjudicated/fr-*.md` records · `formation/fourier/lane-crud.md` (R-1..R-7, F-α/β/γ, V-α/β/γ) · `INTAKE-ADJUDICATION-2026-08-03.md` §3 · `FOURIER-AUXILIARY-…-2026-08-03.md` §4/§5 · `api-gap-remeasure.md` · census §4.9 |
| SPECIFIED | **YES — fold-sealed 2026-08-28** | validated against `docs/precepts/` current at authoring (L-20) |
| IMPLEMENTED | **NO** | — fixture bytes land only after the begin-word |
| VERIFIED | **NO** | — stamped only at X·F's release close |

**ZERO registry rows, measured.** `grep -rho "F\.W8" registry/adjudicated/fr-*.md` → **0 hits across 66 records** (re-measured 2026-08-28). No adjudicated row pre-assigns F.W8; the entire band routes as the atom **`F.W5-W8`**. This wave is therefore built from **census §4.9 + the band's routed registry rows themselves** — never from a carry, which for F.W8 does not exist — and its homing obligation (G15) is *performed*, never *inherited*.

**The band's id-keyed denominator (the operand G15 and §6a run against).** Under the spelling-agnostic probe `grep -rhoE "F\.W5[-–]W8" $R/fr-*.md` (R-6's ruled idiom; F-W8 §0's census is its named conforming model), with boundary-aware id matching (`re.escape(id) + "(?![0-9A-Za-z])"` — a bare `grep -c` is unsound here: `FR-AFP-1` matches `FR-AFP-10`/`-71`, `GCM-2` matches `GCM-25`):

- **routedTotal = 123** distinct banked identities across **43 of 66** records, plus **7 records declaring measured band NEGATIVES** (evidence, never work — P-9): fr-App `:144` · fr-CollapsibleSection `:131` · fr-MobileFloatingToc `:97` · fr-MorphShapePreview `:110` · fr-PaperSearch `:90` · fr-PaperSearchDropdown `:77` · fr-PaperSearchInput `:125`.
- **123 = 87 booked in §3 + 36 not carried here**, the 36 enumerated one-per-row at **§6a** with the STATING home of each. ▲ **Arithmetic reconciliation, recorded rather than silently adopted**: `F-W8-CHECK.md` §1 states the split as *"bookedCount = 86 · escapedCount = 37"* while its own per-record mark table tallies **87 B / 36 E** (fr-FourierShapeExtractor's "all B" = its 18 band-routed lines) and its named escape roster enumerates **36** ids. The total 123 and the roster are unchanged and reproduce; the **split is corrected here to 87/36**, and §6a carries one row per named escape.

### 1a. Goal criterion

The wave succeeds if the five verbs of the union can be **walked against both object kinds**, and — for every leg either side cannot answer — the inability is **stated in the register with its reason** rather than discovered by a later repair. The walk is a diagnostic instrument, not a repair: it repairs nothing and claims credit for no other wave's cure.

### 1b. The TA-4 branch — stated, never silent

⊙ **G1 is unruled.** The honest default this spec carries absent the owner's word is **EXPLICIT RE-SCOPE**: *the prototype's diff leg is one-sided — fourier-only — and the union's diff verdict is a one-sided verdict, said out loud in the walk record and in every fixture row that touches it.* The alternative branch is TA-4 **restoration as a PREREQUISITE**, owned by the value.js API row, not by this wave.

▲ **AMENDMENT A-1 (measured 2026-08-28, value-side, READ-ONLY measurement per §0's carve-out — the F.W0 lock reaches fourier anchors, not these).** FN-6's value-side half — five wire-envelope shape rows in `api/test/conformance/diff.test.ts`, landed at **`7351297f`** (*"test(R · W6): the 5 wire-envelope shape-fixture rows in diff.test.ts"*) and merged at **`8a2a617e`** — is **true of history, false at HEAD.** ▲ The authority for that sentence is **git itself, re-run below**, and the record at §D D1 (FN-6/FN-5): the prior draft attributed it to a quoted "CARRY G14 cell" that is in no in-tree document, and that attribution is **struck, not paraphrased** (R-1 law / R-3).

```
find . -name "diff.test.ts" -not -path "./node_modules/*"      → (nothing)
git log --oneline --all -- api/test/conformance/diff.test.ts   → 7351297f (landed) … a8ff7792 (last)
git show --name-status --oneline a8ff7792                      → D api/src/lib/crud/atomdiff.ts
                                                                 D api/test/conformance/diff.test.ts   (371 lines)
```

`a8ff7792` is *"refactor(T.W1 · api · TA-4): excise the write-only atom-diff apparatus (the /remix+/diff fold)"* — **the same commit that excised atomdiff excised the conformance fixture with it.** Consequences, all VALUE-SIDE, never re-booked as fourier defects: **(i)** TA-4's restoration scope has a **third, previously unbooked limb** — the fixture file, not only `lib/crud/atomdiff.ts` and `atomDiff` on `PaletteVersion`; **(ii)** FN-6 asks fourier to wire a reader for a file value.js **no longer holds**, so the correspondence is not merely one-way, it is **zero-way at HEAD**; **(iii)** G14's born-RED reading corrects from *"1 direction landed, 0 reciprocated"* to **"0 extant · 1 landed-then-excised · 0 reciprocated"**. Survivors confirmed: `api/src/modules/palette/hash.ts:26` `computeAtomHash` / `:44` `computeAtomSetHash`; the compiled corpse `api/dist/lib/crud/atomdiff.{js,d.ts,+maps}` (untracked, 4 files); `ls api/src/lib` → *No such file or directory*.

▲ **AMENDMENT A-3 (anchor drift, value side).** FN-5's contract-currency invariant cites *"the `PaletteVersion` shape (`models.ts:153`)"*. At HEAD the file is `api/src/modules/palette/**model**.ts` (singular) and the interface is at **`:84`**; `atomDiff` appears nowhere in the module. Re-resolve before citation — the same D-19 discipline, applied on the side F.W0's lock does not reach.

### 1c. What this wave is NOT

It repairs nothing. It wires no client (F.W1/F.W3/F.W4), burns no defect (F.W6), designs no trie (F.W7), rules nothing F.W5's ruling block already owns, and runs no live probe (SS-13). **FR-GIG-5 mirror, adopted as a standing bar: no wave is credited with another's cure** — F.W8 claims none of F.W4's wiring unit, none of F.W1's tri-package transaction, and none of F.W1's *non*-cure of the pagination drain.

---

## 2. Bounds

### 2a. File bounds

| File | Access |
|---|---|
| `$V/docs/tranches/X/fourier/waves/F-W8.md` | create (this spec) |
| `$V/docs/tranches/X/fourier/conformance/union-prototype-walk.md` | create — the five-verb × two-object walk map and its acceptance record |
| `$V/docs/tranches/X/fourier/conformance/fixture-register.md` | create — one row per fixture, **both directions**, each naming the operation model it is generated FROM |
| `$V/docs/tranches/X/fourier/conformance/C31-two-sided-reproduction.md` | create — the reproduction, then the F8-C31A/F8-C31B split |
| `$V/docs/tranches/X/fourier/conformance/OWNER-RULINGS-F.W8.md` | create — **only** the rulings F.W5's block does not already own (§5d) |
| `$V/docs/tranches/X/fourier/conformance/walk/union-walk.mjs` | create — **authored here, EXECUTED at F.W9/W10.** No run under this wave |
| `$V/docs/tranches/X/coordination/value-to-fourier-2026-08-DD-fn6-reader-and-fixture-register.md` | create — the relay letter (the only vehicle by which fourier receives the register) |
| `$V/docs/tranches/V/coordination/INBOX.md` | **append-only** — one E13 ledger row; no rewrite of any existing row |
| `$V/docs/tranches/X/fourier/waves/F-W8/carry-closure.md` | create — G15's set-difference output, both directions |

**Bounds authority — the table above, and nothing behind it.** There is no F.W8 carry to reconcile against (R-3), so the nine rows are **this spec's own tree-derived enumeration**: every one is a path under `$V/docs/tranches/X/` (or the E13 INBOX) that this wave creates or appends to, and every one is named because a gate or an edge below binds it. Seven are the `conformance/` + coordination artefacts **census §4.9** assigns this wave; **two are added here and justified in the open**, each because a gate would otherwise be unfalsifiable prose: `walk/union-walk.mjs` (§5c's F.W9/W10 edge gives F.W8 the fixture artifacts **and** the prototype walk script, and F.W9/W10 the run), and `F-W8/carry-closure.md` (G15 is prose without its artifact — the `F-W5.md` G19 precedent). **No path here is adopted on the authority of a document not in tree**, and every path either exists or is marked `create` above.

**The value-side fixture bytes are NOT in these bounds.** `api/test/conformance/**` is product-test source under the execution gate, and A-1 proves its diff fixture was excised. F.W8 **specifies** the value-side fixture rows in `fixture-register.md`; **landing** them is the value.js API row's act, gated on the begin-word **and** on G1's ruling. Binding by **TRANSCRIPTION**, never cross-repo read (the api suite must run where fourier is not checked out).

### 2b. Do NOT touch — every path below, without exception

- `/Users/mkbabb/Programming/fourier-analysis/**` — whole tree, read-only law. **`J-diff-shape.md` v1 (271 lines) is IMMUTABLE**; v2 supersedes by reference (E-3: addenda, not patches).
- All product source both repos: `$V/api/src`, `$V/api/test`, `$V/src`, `$V/demo`, fourier `api/`, fourier `web/`.
- **F.W5's artifacts** — `contract/J-diff-shape-v2.md`, `contract/operation-register.md`, `contract/OWNER-RULINGS-F.W5.md`, `F-W5.md`. F.W8 **consumes** them; editing one would re-rule what F.W5 ruled.
- The evidence records: `registry/adjudicated/**` (all 66) · `formation/fourier/lane-crud.md` · `INTAKE-ADJUDICATION-2026-08-03.md` · `api-gap-remeasure.md` · `FOURIER-AUXILIARY-…-2026-08-03.md` · the census. **E-1/E-3 immutable beside the spec** — they hold the witnesses; rewriting one destroys the evidence a gate cites.
- `docs/tranches/X/COHESION.md` — root-authored at the boundary. F.W8 declares its half of every edge in §5c and **requests** the reciprocal; it does not write the spine.
- `scripts/dev/dev.sh` — unowned dirty row, never staged.

### 2c. Disjointness

Units **b**, **c**, **d** share the fixture-register identity space (`F8-CLIENT-*`, `F8-C31*`); per WAVE_SPEC §4a they are **sequenced**, not parallelised: `a → b → c → d → e`, each committing before the next opens. One writer at a time; a dirty tree at handoff halts the wave. No other X wave writes any path in §2a while F.W8 is open.

▲ **Read-only law, precisely scoped:** it binds fourier's **TREE (bytes)**. `union-walk.mjs`, when F.W9/W10 runs it, mutates fourier **DATA** in a non-production environment — that mutation is the instrument, is declared, and is subtracted (G9). **No fourier byte is written, ever.** The environment is F.W9's deploy spine, not this wave's.

---

## 3. Carry — 28 rows agglomerating the band's routed identities

**Operand**: the 66 adjudicated records (§1's id-keyed census), not a carry (R-3). **87 of the band's 123 routed identities are booked in the rows below**; the other 36 are enumerated with their STATING homes at §6a. Dedupe is by **identity**, not text: where records witness one mechanism the row carries **all ids** and books once. **Every short id is record-qualified** (R-5) — the corpus reuses `B-1`, `B-2`, `C-2`, `C-17`, `C-18`, `L-B1`, `L-M3` and `M-13` across records, so a bare token is not an identity and a closure keyed on one cannot run. `‡` BLOCKER-weight · `⊙` OWNER-GATED · `▲` a lock binding the **repair**, not the finding. Every anchor below is **carried, not re-asserted** — MEASURE-AT-OPEN after F.W0 (D-19), except the three value-side amendments dated 2026-08-28.

### §P — The walk (the five verbs, both object kinds)

| row | banked ids | witness (carried) | F.W8's act + locks | gate |
|---|---|---|---|---|
| **P1** The seam map | **R-1..R-7** (lane-crud §2/§4, census §3b) | Every verb is asymmetric: R-3 diff EXISTS-fourier / **ABSENT-value**; R-5 remix EXISTS-fourier (per-atom overrides, tri-state `palette_slug`, `urn:contract:remix-noop`, idempotency, deliberately non-transactional) / PARTIAL-value (fork only, transactional); R-6 history PARTIAL-fourier (no revert, no by-hash get) / EXISTS-value. Route gap: fourier has `/diff` + `/restore`; value.js has `/revert` + `/versions/:hash` + `/vote` + `/flag`; **neither has the other's** | The walk runs the same five verbs against both kinds and reports **per verb which side cannot answer**. Two divergences **STATED, never harmonised**: **(a)** a fourier remix child is born `draft`, a value.js fork child born `public` — opposite privacy defaults on one verb (`visualization.py:277` / `forks.ts:76`); **(b)** fourier runs **no transaction by deliberate choice** ("standalone-topology-honest", `:493`) while value.js's fork is transactional. ▲ **fourier's `/diff` is NOT a general two-point diff** — `on_chain` is only `{head_hash} ∪ {fork_of_hash}` (`visualizations.py:827`), it reads the STORED `atom_diff` and never recomputes, 404s any other pair: **the fixture must not assume a general diff.** ▲ **D9 lock**: visibility `private\|public` (`unlisted` DIES), lifecycle `active\|trashed`, moderation `clear\|withdrawn`. ▲ **X-4 (`/versions` rail) is CHANGED–CONTESTED** — the value-side history leg must not assume its shape | G2 |
| **P2** ⊙ ‡ Diff-leg participation | **TA-4** (census §5 risk 3 · lane-crud §0/§2 R-3/§7 · INTAKE §3 · F-W5 G4) | `lib/crud/atomdiff.ts` + `atomDiff` on `PaletteVersion` excised at T.W1; only the hashing half survives. `J-diff-shape.md §2.5` (line 75) **mandates a path the tree does not hold** — "no ruling" is not a neutral state | Prerequisite **or** explicit re-scope, §1b. ▲ **A-1 widens the restoration to a third limb (the fixture).** Ruling is **F.W5's G4**; F.W8 consumes it, never re-books it. VALUE-SIDE row | **G1** |
| **P3** ‡ The only UI entry | **GCM-1** (BLK-1 FOLDS · SS-C-1 read leg FOLDS) | The modal's only CTA emits `image_slug` → `GalleryView:396` `router.push('/w/${slug}')` → the **caller's IndexedDB draft**, hard-reset to defaults when absent, writing a draft row keyed on **another user's** `image_slug`; the correct loader `loadVisualization` (`workspace.ts:197-232`) has **zero call sites** | F.W4 wires (route → `loadVisualization`); **F.W8 asserts end-to-end**: opening a published visualization loads the **saved entity** with slug + ETag captured, never a draft, and writes no cross-owner draft row. ▲ **Identity guard: BLK-1 and SS-C-1's read leg FOLD here — never re-booked** (the fr-ImageUpload registry-motion event proves the guard must re-run *at* adjudication time). Sibling of F-α (F.W6): publishing twice yields two unrelated roots | **G16** |
| **P4** Adoption surface | **VV-R2-B** (owns the JOINT identity of BLK-1 · BLK-2 · VV-R2-A · VV-R2-E) | Ten of twenty-four workspace-store exports have zero consumers outside the store file; `gallery.ts:168` bypasses the store's own `deleteVisualization`; four of the ten are exactly the `/v/` shell's surface | Read as **ONE gap**: "a single wiring unit at this component, not scattered repairs." F.W4 owns the wiring; **F.W8 owns the acceptance assertion only** — *the CRUD-CONTRACT §1 adoption is the union tranche's own acceptance surface*. Does not re-book BLK-1/BLK-2 | G16 |
| **P5** The verb set is unclientted | **FR-GV-8** ⊕ **FR-GV-27** (rides) | Seven of thirteen `/api/visualizations` operations — remix · publish · unpublish · forks · provenance · diff · versions — have **zero client function**, and that set **is** the walk's verb set (`F8-CLIENT-03..09`). `publish` PATCHes `{visibility}` behind an extra GET (`gallery.ts:223`) that adds a phantom view, while `/publish` and `/unpublish` ship with no wrapper at all | Each gap operation gets a bound client method + consumer state **or an explicit `SERVER_ONLY` law** (36/45 → 45/45). ▲ **K-1: a `@router.` grep is blind to prefixed routers — `gallery.py`'s `@gallery_router.get("/cursor")` reads against 13/44, never zero.** Does **not** repair the 0/45 security descriptions nor the three absent/prune surfaces. Consumes F.W5's register — **F.W8 does not re-derive the enumeration**. Wiring seat = F.W4 (VV-R2-B's single unit); identity/provenance DISPLAY arms FOLD to banked fr-GalleryCard D-7, GCM-25, GCM-3 — do not re-book | G2 |
| **P6** The owning-actor enumeration | **FR-GV-13** (=MISS-LC-2) | `visibilityFilter` is a **producerless** store ref — three hits, all inside `stores/gallery.ts`, zero writers — so `ownerParam()` returns `undefined` unconditionally and the server's whole `owner=="me"` branch (`visualizations.py:305-309`) is dead from this client | Load-bearing here specifically: **the history walk needs `owner=me` to enumerate the actor's own chain**, so a dead branch means the walk cannot run as the owning actor at all. F.W3/W4 owns the control; **F.W8 owns the enumeration dependency** | G2 |
| **P7** ‡ The create leg | **D-4/L-1/C-3** (CanvasControlsDock) ⊕ **fr-GalleryDraftsSection B-2** (=C-C-1∘L-3, `:40`; **FR-GV-1 FOLDS**; record-qualified per R-5 — *not* fr-EquationView B-2, the `budget`-past-`le=50` row at `:48`, which F-W5 books at clause **F2**, §F's one-shared-bound-constant clause) ⊕ **C-6/C-7** (EditorControlsDock) | Re-entrant and idempotency-free on every hop: `saveVisualization` is an unconditional POST minting a fresh slug per call (`visualizationSlug` written `:358`, never read as a guard); `handlePublish` has no re-entry guard and no `:disabled` — **N clicks ⇒ N public gallery rows**; the drafts path reconciles against `savedSnapshots`, whose ONE writer is the literal `[]` (`workspace.ts:102`); the contour write **declines the `Idempotency-Key` channel its own client core declares** (`:98-99`/`:138-139` declared, `:316-324` unpassed) | **Idempotent-or-declared on both sides.** The union holds both halves in different repos on different verbs and **neither side has both**: fourier's remix carries `idempotency.replay_or_record(...)` (`visualizations.py:612`) with no transaction; value.js's fork is transactional with in-txn source re-read (`forks.ts:94-137`, `:99-105`) with no idempotency key. ▲ FR-GV-1's split law verbatim: *"split F.W3/W4 contract + F.W5-W8 server dedupe"* — **the `:disabled` cure is F.W3/W4's and F.W8 claims no credit for it.** Cross-checks `api-gap-remeasure.md` X.A3 (strong `If-Match` + required `Idempotency-Key` + `matchedCount === 1`) and **X-3 (PATCH TOCTOU / no CAS, VERIFIED-GAP)** | **G10** |
| **P8** The list contract | **FR-GFC-4** (home) ⊕ **FR-GSB-1** (FOLDS) ⊕ **FR-GSB-28** · C-4/D-13 FOLD, R-7 sustained | `entries` is featured **∩** the loaded 20-row cursor window with **no `tier` param at ANY layer** (`api.ts:397-402` · `visualizations.py:288-294` · `gallery.py:38-41` · `cursors.py:20-23`); `fetchNextPage` appends so the strip grows during scroll; `resetAndFetch()`/`fetchNextPage()` share one abort key (the literal `"listVisualizations"`, `api.ts:411`; `abortable()` `:54-57`; `:161` defaults every call's signal to it) | The walk **cannot enumerate a chain through a list endpoint that returns an intersection of a filter and a page window**: server-side `tier`/filter param + per-call abort key. ▲ **WAVE-LOCK (fr-GallerySearchBar C-2, verbatim): any F.W5-W8 wiring of `basisFilter` MUST use the banked normaliser (fr-BasisSelector M-10)** — without it the Fourier pill returns zero rows against `active_bases ∈ {fourier-epicycles, fourier-series}` (`GalleryCard.vue:39` has it; GallerySearchBar does not). ▲ **FR-GIG-5: the F.W1 tri-package bump does NOT cure the pagination drain and F.W1 must not be credited for it** — the true cure is a producer change riding the BH relay (SS-6). C-3/C-4/D-13 FOLD by reference — do not re-book. FR-GSB-28's interleave symptom → SS-13 (magnitude only). **Dissent preserved**: FR-GFC-1 carries two BLOCKER filings; adjudicated MAJOR (blocker-weight) | G2 |

### §J — The join seam and the fixture-direction law (R6-8, three directions)

| row | banked ids | witness (carried) | F.W8's act + locks | gate |
|---|---|---|---|---|
| **J1** ‡ The seam itself — **the wave's reason to exist** | **R6-8 / C31** (intake `lane-fourier-r3-r6.md:142`, adjudicated INTAKE §3 secondary; AUXILIARY §5) · folded-by-reference (all record-qualified, R-5): fr-AnimationControls C-30 · fr-BasisCanvas C-5 · fr-EasingCurvePreview RESOLVER · fr-ContourSettings i-7/C-25 · fr-FourierShapeExtractor C-7 · fr-MorphShapePreview §3 negatives · fr-EditorControlsDock S-7 (the POSITIVE) · fr-CoefficientsSpectrum M-13 · fr-InfoCard §R6-8 | `operation:PATCH:/api/visualizations/{slug}` embeds `"clients": ["client:updateVisualization"]` + `"clientDisposition": "CLIENT_MATCH_SOURCE_DERIVED"`, so C31's client-side edit at `web/src/lib/api.ts:420` (PATCH→PUT) breaks the client↔operation match **and mutates the operation leaf too** — the leaves are structurally non-isolable by construction | **REPRODUCE, then SPLIT.** First the exact two-sided failure — decoded stdout `{"code":"control.wrong-reason","errors":["client.method.visualization-update","operation.method.visualization-update"],"controlId":"R4.C31","verdict":"REJECT"}`, **exit 43**, against `web/src/lib/api.ts:420` + `api/routers/visualizations.py:350`. Then **F8-C31A** (sole mutable target `client.method.visualization-update`; owner result: exactly its client-method leaf fails; non-owner result: the operation-method leaf and all other predicates retain) and **F8-C31B** (the mirror). Verbatim constraint: *"Both must use raw receipts, the same production validator, an owner-only bypass, all-non-owner retention, and no caller-supplied expected code."* **Reproduction is the DIAGNOSTIC; isolation is the ACCEPTANCE.** ▲ Denominator: C31 terminal RED at **30/37**; **32/38 only if lawfully replaced by C31A/C31B**, and the later denominator **must be owner-frozen**. F.W5 STATES the contract clause (operation identity independent of client identity; the join lives in a SEPARATE RELATION); **F.W8 EXECUTES** — one home, two citations. Governance note carried: the R-series' authority is dead (OG-F1 owner-gated; R6 self-declared `NO_SUCCESSOR`) but **the MEASUREMENT enters by adjudication — "zero UNVERIFIED credit", not zero credit** (census ADDENDUM §6.7). Exactly one R-row was refuted corpus-wide (R4-4, a filename transcription) | **G3** |
| **J2** The direction law | **FR-IC-6** (FOLDS → L·m-7) | The client leaf invents an operation-value domain (`EquationTier`) the operation record never constrains (`tier: str`); the parse is throw-guarded but shape-unvalidated (`:36-41`) — R6-8's **mirror image** | **THE FIXTURE-DIRECTION LAW, this wave's own acceptance constraint: every fixture is generated FROM the operation model, never from the client union.** Verbatim consequence: *"a fixture generated from the client union will never exercise the `??` branch, so the relabel bug is invisible to exactly the tests meant to catch it."* Generalised: *the operation must own its value domains, or the client's narrowing is fiction.* Sharpening adopted: `sessionStorage` keys are schema-versioned `eq-tab-*-v2` (`useEquationCache.ts:7-8`), blunting the stale-schema limb — **the backend-adds-a-tier limb survives whole**. F.W4 owns the client guard; **F.W8 enforces the direction across the whole register** | **G4** |
| **J3** The companion clause | **fr-CoefficientsSpectrum M-13 = that record's own C-7** (`:65`; record-qualified per R-5 — *not* fr-BasisCanvas C-7, which F-W5 books at clause **E9** (§E, *"One shape, one name"*), nor fr-FourierShapeExtractor C-7, which sits in D2's block) | The epicycle response is `data: dict`; its only typed component model (`EpicycleComponentDTO`) is dead with one self-hit and disagrees with the wire; `BasisComponent` is hand-maintained against a Python dict literal — **no derivable join** | Verbatim: ***an operation's response model must be structurally typed, or the leaf is unjoinable in principle.*** Consequence owned here: *"the value.js↔fourier conformance fixtures (census §4 F.W8 / FN-6) cannot be generated for this operation as it stands"* — so each such operation is **typed at F.W5 or NAMED AND STRUCK in the register with its reason**. The equation route does it right in the same API — the in-tree exemplar. ▲ Cross-edge: **F.W5's `operation-register.md` must carry a per-operation joinable/unjoinable cell or G4 is unfalsifiable prose** | G4 |
| **J4** Bounds are generated, not invented | **m-7** (= C-6-as-rescoped); riders/folds: fr-SliderControl **R-16** (FOLDS, books nothing) · fr-ContourSettings **B-1** · fr-FrequencyGraph **FR-FG-21** · fr-EquationView **M-SB** · fr-FunctionInput **C-8/N-4** · fr-AdminUserList **FR-AUL-46** | The client's numeric window `[1,500]` is invented at the leaf and restated ~6× per control, matching **none** of the server's six divergent windows (256 / 4096×3 / 200-unbounded / 200-le) | Constructive cure verbatim: ***generate client bounds from the operation models (the R6-8 dual)*** — F.W5 states the RULE (D7: *every request field is bounded, or the exception is named*; *operation records must carry field DOMAINS*); **F.W8 GENERATES them inside the fixtures**. B-1's rider carried: *the typed range-checked descriptor table for all six fields rides an F.W5–W8 rider — the contract seam owes CLIENT-DERIVABLE BOUNDS*. FR-FG-21 rides (`maxBars` unclamped public prop → canvas dimension, over the family's only unbounded request model). ▲ **Preserve the enforcement SHAPE while fixing the numbers** — this control is the only thing between a hand-typed `-9e9` and an FFT call (`ComputeEpicyclesRequest`/`ComputeBasesRequest` carry no `Field`, no bounds, no validator). ▲ **K-3 lock, verbatim: m-7's straddle-a-422 scenario is REFUTED from source (`AnimationData` is a server-computed embedded document; the client-reachable bound 4096 ⊇ [1,500]) — v2 must not cite it, and neither may the fixtures.** M-SB/N-4 are F.W5's D7 clause: **F.W8 consumes the bound, does not author it** | G4 |
| **J5** Serialization bypass | **GCM-10 ⊕ GCM-55** | `json.dumps` bypasses the Pydantic model on `visualizations.py`: `tier` is absent from the model under `extra="forbid"` yet written by `admin.py:183/:432/:438` and passed raw by `_public_doc` (a full-strength Bookmark chip with an empty label over `undefined`); `likes` is served raw while the store's `likes ?? 0` and the template's bare `{{ entry.likes }}` assert **opposite nullabilities — one is wrong by construction** | Cure at the **model** (a `tier` field WITH A DEFAULT; GCM-55 rides the same fix), not at the render guard; F.W3/W4 owns the `v-else-if`. Acceptance: **a fixture generated from the model matches the wire byte-for-byte, or the bypass is NAMED in the register.** ▲ **REGISTRY-INTEGRITY LOCK — banked at **F-W5 gate G22** (§3, *"dangling-cite resolution (registry integrity)"*; `F-W5.md:251` at this seat's 2026-08-28 read), re-anchored per R-1f** (the prior "§6c" address does not exist in `F-W5.md`): *"GCM-10's cure cites a companion row **'MF-9'** that resolves to NOTHING in the adjudicated registry — `grep -rn "MF-9" $R/` → exactly ONE hit, the dangling citation inside `fr-GalleryCardModal.md:56` itself (re-measured 2026-08-28)"*; **MF-9 resolved or STRUCK before v2 quotes GCM-10's cure**, G22's GREEN owner being F.W5 itself (unit e). If struck, the cure is quoted without it | **G5** |
| **J6** Validate at the boundary | **FR-CP-16** (= **fr-CoefficientsPanel C-17** / L·A-3 — record-qualified per R-5; *not* fr-BasisCanvas C-17, the overlay double-fetch rider at `:102`, which F-W5 books at clause **E15**, §E's ONE-transport clause); folds: fr-FrequencyGraph **FR-FG-13** + C-8/L-m4 · **fr-FourierShapeExtractor C-15** | The descending-amplitude invariant is enforced only in Python (`epicycles.py:54`), expressed nowhere in TypeScript, and the whole **45-operation client surface lands as ONE unchecked `(await res.json()) as T`** (`api.ts:192`); IndexedDB draft rehydration (`workspace.ts:171`) replays older payloads through the same zero checking, so a violation surfaces as **`NaN%` bar widths, not an error** | The fixtures are the instrument that proves a boundary exists — *validate-at-boundary for the epicycle payload at minimum*. F.W4 owns the ordering brand and reading `n_components`. Sibling clause folded: **C-15** — the producer's output type unenforced end-to-end (anonymous literal → untyped JSON → independently-declared `FourierPathData` → `as any` at both consumers, **with `resolveJsonModule` already providing the structural type being discarded**). FR-FG-13's cross-ref names FR-CP-16 as the enforcement point — one home, two citations | **G11** |
| **J7** ⊙ One generated shape | **FR-AFP-71** (named cause) ⊕ **FR-AFP-36** (the drift) ⊕ **GAB-12** (same mechanism, gallery twin) | Hand-maintained god-module twins with no codegen — `api.ts` 672 L / `types.ts` 391 L, **three hand-maintained copies of the flagged shape (one already rotted)**, three dead server models encoding the retired contract (`FlagRequest` · `FlaggedEntryInfo` with rotted `user_slug` · offset `FlaggedListResponse`), **no `response_model` anywhere**; the gallery twin (`types.ts:110-118` ↔ `api/models/gallery.py`) is isomorphic today with `res.json()` unvalidated, and drift degrades into **six silent em-dashes with the tier tint dropped — indistinguishable from an empty database, no console error, no toast, no test** | *"One generated/checked shape"*; dead models delete; **module split is the union's call**. ▲ **K9 lock, verbatim: do NOT delete the `?? item.slug` fallbacks** — `admin.py` emits `doc.get("image_slug")` unvalidated, so they are defensive and the "five dead branches" limb is **NOT PROVEN**. F.W5 rules (its G11/FR-AFP-71); **F.W8 is the wave whose register either IS the generated shape or documents why it is not.** GAB-12's test seam is also an F.W9/W10 coverage emission — one home, two citations | **G12** |

### §R — Round-trip, persistence, cache identity (the derive and history legs)

| row | banked ids | witness (carried) | F.W8's act + locks | gate |
|---|---|---|---|---|
| **R1** Replay reproduces its own frame | **BC-9 / C-6 / D-20 ⊕ SS-C-2** | Half the persisted `AnimationSettings` contract is write-only and **three-way divergent INCLUDING UNITS** — server `fps 30 / duration 30.0 s / max_circles 80` (`shared.py:65-71`), client `60 / 5000 ms / 100` (`defaults.ts`), renderer `ref(80)` (`:47`) and `20000 ms` (`animation.ts:23`); only easing/speed/active_bases round-trip; `speed` agrees **by luck**, unconstrained on both sides (`shared.py:70`, no validator) | Verbatim: *"settings round-trip is a persistence-contract row; the CRUD/provenance union owns the three-way reconciliation"* — **with server-side units**. Acceptance instrument: **a gallery replay must reproduce its own frame.** ▲ The `duration` unit fork (20000 ms store / 5000 ms client / 30.0 s server — **a 1000× fork across three declarations**) must be reconciled in the contract *before* the fixture can assert anything — cross-edge to F.W5. ▲ Partial fold: `duration = ref(20000)` zero-writers stays banked at **BC-20** — not re-booked | **G6** |
| **R2** The update verb reaches every atom | **SS-C-1 (write leg)** | The persisted `speed` atom is settable **only at CREATE/FORK** — `VisualizationUpdate` (`visualization.py:198-210`) and `VisualizationPatch` (`types.ts:256-262`) are the same five fields with `extra="forbid"` — while `animation_settings` is **atom 4-of-5 of the version-identity set** (`atomdiff.py:38`): a preference the control claims to set is silently non-persistent | The update verb reaches **every atom of the version-identity set, or the atom set is re-declared to exclude what it cannot touch — stated either way, never left divergent.** The instrument is the derive→history leg: **mutate atom 4, walk the chain, assert a version row exists carrying it.** F.W5 states the atom set; F.W8 asserts reachability. **Read leg FOLDED to GCM-1.** Severity grades the CONTRACT, not the control (demoted from the challenge's BLOCKER at component altitude) | G6 |
| **R3** ⊙ Cardinality admission | **M-9** (BasisSelector) = L-M4 / C-20 | The deliberately-minted "off" state `[]` (`:103-104`, *"canvas handles it gracefully"*) is **unrepresentable end-to-end and laundered on every path** — server forbids it (`min_length=1`), the store substitutes (`workspace.ts:351-353`, `gallery.ts:252`), the loader refuses to restore it (`:53`) and force-prepends on first data (`:109-114`): honoured on screen, destroyed on save, **zero diagnostic** | Verbatim: ***"Needs an owner ruling: admit `[]` in the contract, or stop minting it — the silent rewrite must not survive either way."*** The **cardinality axis of the R6-8 seam**: the fixture asserts that a state the client can mint either **survives the round trip or is refused with a diagnostic, never laundered**. F.W5's `OWNER-RULINGS-F.W5.md` owns the ruling; **F.W8 owns the fixture that proves whichever branch lands — it may not author on a guessed branch.** ▲ **Anchor-drift WARNING recorded, not an assertion** (§4 discipline 1): the corpus cites `:184/:279`, a read-only seat inspection gives `:186/:280` — the divergence is booked as a **re-resolve obligation after F.W0 (D-19)**; this spec adopts neither pair as its anchor | **G7** |
| **R4** ‡ Cache identity | **B-4** (ContourSettings) = C-1 ∘ C-25/R6-8, ⊕ **i-7** (folds) | `extraction_cache_key` is a **CLOSED 10-field `json.dumps` literal** (`image_storage.py:250-266`) omitting `ml_threshold`/`ml_detail_threshold` — the two fields this control solely produces — while `images.py:219-226` short-circuits on the cache hit **BEFORE** `compute_contours`: the user drags 0.50→0.85, waits out the debounce, receives the 0.50 contour with **no signal**. `auto` is the shipped default, so the control is live on every fresh workspace | Contract constraint verbatim: ***an operation's cache identity must be a superset of the request fields the operation consumes*** — the R6-8 seam made product-visible. **F.W8's derive leg is where the cross-repo fixture lives**; the client-side regression fixture (assert `contour_hash` **instability** across an ML-threshold change) is F.W3/W4's + the SS-13 probe. **i-7/C-25 folds here — never re-booked.** *The derive step cannot be trusted while a cache key under-determines its own operation.* Magnitude → SS-13 | **G8** |
| **R5** The flush seam | **MISSED-E** (EasingPicker save-race) | The picker's write reaches persistence through a 500 ms `watchDebounced` (`VisualizationView:53-64`) with **no flush-on-save**, while `saveVisualization()` reads `toRaw(animationSettings.value)` synchronously (`workspace.ts:344-357`) — select-then-save inside the window persists the **previous** curve | Persistence semantics: ***the flush seam is the banked L-12/C-25 `setEasing` action*** — **do not invent a second seam.** The round-trip fixture asserts **the last user-visible value is the persisted value**. Narrow reachability (menu dismiss + save within 500 ms) **honestly stated, not inflated**. Composes with **SS-C-8** (`{once:true}` loader seeding + in-place `/w/:slug` nav + the same 500 ms writer ⇒ workspace B's persisted speed clobbered by workspace A's live value) — SS-C-8's repair seat is F.W4, its ordering witness SS-13; **F.W8 asserts only the round trip** | G6 |
| **R6** The liveness predicate | **M-β4** (BasisCanvas) ⊕ **L·m-6 = C·D-14** (EquationView) ⊕ **PP-DEADSEAM = C-12** (PathPreview) | Three envelope fields dead in one direction or both: `EpicycleData.trace` — a 3000-sample polyline computed per response — has **zero readers** yet rides `structuredClone` into every IndexedDB draft (`workspace.ts:95-104`); `reconstructed_points` (500+500 floats) is fetched, sessionStorage-persisted whole and read by **nothing** (`equations.py:114`/`:128`); `preview_path` is written `""` at all three server sites (`assets.py:85`, `image_storage.py:318`, `responses.py:22`) with the client leaf unreachable — **zero implementation on either side** | **THE LIVENESS PREDICATE — verbatim: "beside R6-8's identity/isolation predicate, genuinely new to the register." As contract law: every field in the envelope has a PRODUCER and a CONSUMER, or it is dropped.** Per-field disposition: *drop the field, or consume it*. **The walk is the instrument** — a create → derive → diff → remix → history traversal that touches every envelope field and **reports the unconsumed set**. M-β4 is *"the R6-8 seam's converse: server computes, nobody consumes"* — the third direction of one seam, deduped by mechanism with **all three ids preserved**. F.W5 states the predicate; F.W8 measures it | **G13** |

### §M — Measurement integrity (the probe's own honesty)

| row | banked ids | witness (carried) | F.W8's act + locks | gate |
|---|---|---|---|---|
| **M1** Non-perturbation | **FR-GV-12** (home) ⊕ **VV-R2-A** ⊕ **FR-GV-24** (rides) | `GET /api/visualizations/{slug}` is an unsafe read that **cannot observe its own side effect** — `find_one` `:256`, `$inc {"views":1}` `:268-270`, `_public_doc` serialising the **pre**-increment doc `:272` — and the same verb is the ETag-capture path (`gallery.ts:167`, `:223`), so every ETag cache-miss on publish or soft-delete **adds a phantom view to the row being mutated**; a freshly minted draft's ETag lives in a different store (`workspace.visualizationETag`, `workspace.ts:359`) so **the map ALWAYS misses**. No `viewed_ips` dedup exists while `liked_ips` does | ***PROBE NON-PERTURBATION: the union prototype's own READS must not mutate the entity under measurement*** — either the walk **declares and subtracts** its perturbation, or the read verb stops mutating (a mutating GET is unsafe against any proxy/prefetch, **RFC 9110 §9.2.1**). VV-R2-A's cheap arm is F.W4's (publish adopts `store.setVisibility("public")`, `workspace.ts:373-389` — the exact ETag-guarded PATCH with **zero callers repo-wide** — dropping one round trip and the phantom view; it rides BLK-2's repair unit). ▲ **Two probe-suppression locks: K12 and FR-GV-24 — SS-13 spends no probe re-deriving them.** ▲ **FR-GV-24 cure-integrity lock (banked GCM-3 R-7 precision): repair tests must NOT assert a re-open increment — the guard DOES work within a session; the defect is SCOPE, not absence** | **G9** |
| **M2** The authenticated leg's honesty | **FR-USB-24 ⊕ FR-USB-23** | The login path opts into the silent **≤60 s** 429 auto-retry (`retryOn429 ?? true`, two sleeps ≤30 s) against a 5/60 s budget with no feedback and no cancel (Escape doesn't abort — FR-USB-27); the dead session subsystem (~17 % of the store, zero external call sites) hides a **latent identity-mixing bootstrap** — `logout` never calls `clearSession`, and the `else if (sessionToken)` arm would re-attach an anonymous identity on reload if revived | Verbatim: ***auth mutations pass `retryOn429: false` or surface the wait*** — **a horizon probe that silently retries for 60 s manufactures false timings and false greens in the authenticated walk.** The session subsystem is **retired or wired, never left latent**, because R-7 (user provenance) is a union seam and an identity-mixing bootstrap **poisons every attribution the history walk records**. The value-side mirror is **V-γ** (nullable `userSlug` ⇒ an unattributable edit mutates the palette and writes **no** version row: `crud.ts:119`, `crud.ts:204`, `versions.ts:147`) — VALUE-SIDE row, never re-booked as a fourier defect. ▲ **Session-token-at-rest is the one place value.js is strictly ahead** (SHA-256 digest as `_id` vs fourier's plaintext UUIDv4) — **recorded so the union does not regress it** | G9 |
| **M3** Batch writes preserve orthogonal state | **FR-GV-9** (= C·M-8 second clause) | Batch Unfeature sets `{"$set": {"tier": "normal"}}` unconditionally via `update_many` (`admin.py:435-440`) — **unfeaturing a SAVED-tier entry silently erases the saved tier** — while the `:423-425` dialog copy is honest about the mechanism and the `:326-333` button label is not | *Tier is a state machine, not a scalar collapse.* **F.W8 owns only the batch-write state-preservation assertion**, recorded in the walk's admin leg (**no separate gate — L-19: a gate here would name no witness the walk does not already carry**). Composes with **FR-AFP-10** (tier↔flag: "Mark acceptable (save)" never dequeues — `set_tier` writes `{tier, updated_at}` only, the flagged listing has no tier predicate) and **FR-AFP-70** (`handleSetTier` hardwired to `'saved'`, no inverse — an unrecoverable one-way write). **Their SEMANTICS ruling is F.W5/F.W6's** — one home, two citations | G2 (walk record, admin leg) |
| **M4** Authority asserted per operation | **R3-7b** (INTAKE §3 primary, un-refuted); instantiated at GCM-52 · F-4 (= banked FR-AFP-4) · fr-EditorControlsDock C-6 | **OpenAPI security-description coverage is 0 of 45** — the admin arm inside the denominator — instantiated at live leaves: the modal's only API call is unauthenticated, visibility-unchecked and **publicly cacheable for a day** (a draft/unlisted viz's source image world-readable by slug); the drafts thumbnail GET carries the same posture; the contour write is an **unauthenticated write from a client that always authenticates** | *"Auth parity cannot be specified over a surface documenting none"* — **and it cannot be ACCEPTED without the walk: the authenticated leg asserts, per operation, the authority class F.W5's register declares.** F.W5 states; F.W8 asserts. **F.W8 does not re-derive the 0/45 figure; it consumes it.** ▲ **Lock — banked at **F-W5 clause C2** (§C, *"Image remediation contract — ONE unit"*; `F-W5.md:114` at this seat's 2026-08-28 read), re-anchored per R-1f** (the prior "§6b" address does not exist in `F-W5.md`), verbatim: *"**m-15 is CROSS-REFERENCED, NOT MERGED with F-4**: F-4 books *who can fetch*; m-15 books *what they see*."* FR-AFP-4's image-remediation contract is F.W5/F.W6's; the deployment auth-proxy question → SS-13 | G2 |
| **M5** The denominator of record | **R3-7c ⊕ X-3** (INTAKE §3 + census ADDENDUM adopted-fact 4 + AUXILIARY §4 `F8-CLIENT-01..09`) | 45 total decorators = **30 public-non-admin + 13 admin + 1 app + 1 gallery**; 36 client edges exist; **9 operations have none** — triangulating census risk 9 and fourier's own M.W10 "7 endpoints, 0 callers" | Cite the **45/30/13 triple** as the denominator of record; every gap operation receives an authenticated client edge **or an explicit `SERVER_ONLY` law** before the walk may claim coverage (36/45 → 45/45); per-gap results enumerated as `F8-CLIENT-01..09` (**-01** `GET /api/gallery/cursor`; **-02** `GET /api/health` *"without treating health as product readiness"*; **-03** diff UI/error receiver; **-04** forks empty/error; **-05** provenance; **-06** version-history; **-07** publish auth/success/error; **-08** remix new-resource/conflict; **-09** unpublish auth/success/error). ▲ **K-1: prefixed-router blindness — read against 13/44, never zero.** ▲ **Method law inherited: *"a registry audit that re-hashes bytes but never re-derives products proves the bytes unchanged, not the summary true"*** — the F.W4/F.W5 evidence standard, binding on G15's closure. ▲ **Identity ordering is `UTF8_BYTEWISE_CODEPOINT`, never `localeCompare`** (binds register row ordering and any digest). ▲ **X-9: publish ONE member-scope law before any percentage** — **no F.W8 coverage percentage is published without it.** Also carried: *"an operation record embedding derived client back-references cannot attribute a defect to one side of the seam"* (the R6-8 root) | G2 · G15 |

### §D — The deliverable, and the standing tripwire

| row | banked ids | witness (carried) | F.W8's act + locks | gate |
|---|---|---|---|---|
| **D1** ‡ Fixtures both directions | **FN-6 ⊕ FN-5** (`R.W6.md:65,:122` · `R/PROGRESS.md:124` · `dispatch-homes.md:20,:26` · census §3c) | FN-6 was BOOKED *"value.js lands the fixture; fourier wires the reader"* — but the correspondence is one-way in fact and law: **four unanswered value→fourier letters, zero inbound rows from fourier in the V INBOX**. ▲ **AMENDMENT A-1 (§1b): the value-side fixture was landed at `7351297f`/`8a2a617e` and then DELETED at `a8ff7792` with atomdiff — 0 extant, 1 landed-then-excised, 0 reciprocated** | F.W8 authors the register for **BOTH directions** and relays it. ▲ **FN-6's reader strategy stays fourier-owned: "value.js neither dictates it nor reads from fourier's tree"** (dispatch-homes A.1; vendor-a-copy recommended, sibling-read is fourier's alternative). Guarded by **FN-5**, the contract-currency invariant, sequencing rider **"BEFORE or WITH fourier M.W10"**. ▲ **`canonical_digest` remains SKIPPED-SANCTIONED** (fourier sorts keys, value.js hand-orders literals; adoption = a stored-hash migration, not parity) — **a fixture that re-opens it fails G14**; the three named twin divergences stay un-unified BY DESIGN. FN-5 names exactly what re-opens the twin question: `api/src/lib/crud/atomdiff.ts`, `api/src/hash.ts`, the `PaletteVersion` shape (**A-3: `model.ts:84`, not `models.ts:153`**), the URN catalog. ▲ **Test-isolation law**: the api suite must run where fourier is **not** checked out, so each side holds its own in-tree copy and the binding is by **TRANSCRIPTION**. E13: one append-only INBOX row. **Dissent recorded, not re-litigated**: R.W6's §11-Q9 fixture-home question was resolved to *"value.js lands, in-tree, read locally"* (M4) over the proto's fourier-tree recommendation — **F.W8 does not reopen it** | **G14** |
| **D2** The geometry tripwire — **a LOCK, not a gate** | **fr-FourierShapeExtractor pipeline block** — every id below is that record's, record-qualified per R-5 so none collides with the homonyms elsewhere in the corpus: **L-B1** (`:51`) · **L-B2 / C-2** (`:52`, one identity — *not* fr-ContourEditorCanvas C-2, which is F-W5 E17) · **L-M1** (`:53`) · **C-3** (`:54`) · **C-4** (`:55`) · **L-M3 / C-6** (`:56`, one identity — *not* fr-FunctionInput L-M3, which is F-W5 F6) · **L-M5 / C-5** (`:57`, ONE identity: the record books a single row and the prior draft listed its two halves as two rows) · **C-7** (`:59`) · **L-B3** (`:71`) · **L-M2** (`:72`) · **L-m3 / C-10** (`:81`) · **L-m5** (`:83`) · **M-7** (`:85`) · **M-8** (`:86`) · **M-9** (`:87`, *not* fr-BasisSelector M-9, which is R3/G7) · **M-11** (`:88`) · **C-14** (`:90`) · **C-15** (`:91`) — **18 rows, one identity block.** ▲ **Count corrected (D-5)**: the prior "17 rows" was arithmetically consistent only by accident — the `L-M5`/`C-5` double-listing cancelled the omission of **C-3**, which is now **admitted into the block** (it remains a standing lock in the act cell besides). 18 reconciles exactly to the record's 18 band-routed lines | The canonical-geometry pipeline has diverged from the artifact it generates and its only producer is **untracked, broken at import, parameter-diverged**: shipped `moon.json` is not derivable from the component's Moon SVG (proximity 0.037 / median 2.93 / p90 37.57 / max 44.83 against 1.23-unit spacing); `.gitignore:53 scripts/*` swallows both `precompute_svg_fourier.py` and `raw-contours.json`; `:40` imports `order_contours` which **no module defines**; `:139-142` hard-codes 100/12 against the shipped 50/10 | ▲ **TRIPWIRE, verbatim: "if any wave attempts regeneration before F.W5–W8 lands the pipeline, L-B1 and L-B2/C-2 REVIVE AT BLOCKER with the DO-NOT-REGENERATE rider as the tripwire. DO-NOT-REGENERATE on `master`."** **F-W5 G1c/G16 already banks it — F.W8 does NOT re-book it**; it is carried here solely as a **binding lock on the prototype: the union walk must not regenerate canonical geometry.** ▲ **fr-FourierShapeExtractor C-3 lock (`:54`): NO closure heuristic** (44.08 closed vs 44.97 open overlap — the closed/open flag must be **CARRIED, not inferred**); the id now sits **both** in the identity block above and here as a standing lock. ▲ **M-2 lock: the tracked in-process generator exists for 5 of 7 assets.** Cure shapes carried for the owning wave: the pipeline decision names which stage-0 survives (C-7); `raw-contours.json` TRACKED (L-B3 rides L-B2/C-2); one named interface end-to-end (C-15 + C-5); a uniform post-pass or contract doc for `samplesPerPath` (C-4 — *a parameter named "samples per path" produced a 4-point contour*); diagnostics on the three silent-drop gates (L-M3 — the mechanism that makes L-B1 structurally unobservable at the human seam). ▲ **The pipeline DECISION has NO wave owner in the census taxonomy — F.W8 declares it a cross-edge and requests adjudication at the spine (candidate home: F.W6).** "What `moon.json` WAS generated from" → SS-13 / undetermined. **Dissent PRESERVED WITH ITS REVIVAL CONDITION**: reader-1's BLOCKER positions on L-B1 and L-B2/C-2 were demoted to MAJOR on latency and M-2's repair-shrink; **they revive at BLOCKER the moment any wave attempts regeneration before the pipeline lands** | — (lock; banked at F-W5 G16) |

---

## 4. Gates — 16, all born-RED

`V=/Users/mkbabb/Programming/value.js` · `F=/Users/mkbabb/Programming/fourier-analysis` (read-only) · `R=$V/docs/tranches/V/megatranche/registry/adjudicated`.

**Two reading disciplines, binding.** **(1) MEASURE-AT-OPEN** — every fourier figure and anchor is re-confirmed by F.W0 before citation (D-19); **this file re-asserts none as fact.** Where a seat READ (permitted under §0's read-only-measurement carve-out) diverges from the corpus, the divergence is **RECORDED as a re-resolve obligation** — never adopted as this file's anchor and never re-booked as a fourier defect; §3 R3's `:184/:279` → `:186/:280` note is exactly that shape and nothing more. Value-side measurements dated **2026-08-28** are taken read-only and are **not under the F.W0 lock**. **(2) Split verdict** — a gate closes *for F.W8* when the fixture/assertion is **authored with its disposition**; it goes **GREEN** only when the named owner lands the change. **F.W8 never claims a GREEN it did not execute.**

**No gate is added.** 16, each keyed by id to a banked witness in the §3 rows; G14 absorbs amendment A-1 and G15 absorbs A-2 because both are **corrections to existing witnesses, not new obligations** (L-19). The repair round re-based G15's operand on the registry and re-worded its homing leg dischargeable (R-3 / R-5) — an operand correction, not a seventeenth gate.

| # | gate | born-RED witness — command / carried anchor | clears when | GREEN owner |
|---|---|---|---|---|
| **G1** ⊙ | TA-4 disposition declared | **measured 2026-08-28**: `grep -rn "atomdiff\|atomDiff\|AtomDiff" $V/api/src $V/src` → **1 hit**, a comment at `api/src/modules/palette/__tests__/palettes-forks.test.ts:9` recording the T.W1 excision · `ls $V/api/src/lib` → *No such file or directory* · `ls $V/api/dist/lib/crud/` → 4 untracked build artifacts (the corpse) · `git -C $V log --oneline --all -- api/test/conformance/diff.test.ts` → `7351297f` … **`a8ff7792`**, and `git show --name-status a8ff7792` → **`D` both `api/src/lib/crud/atomdiff.ts` and `api/test/conformance/diff.test.ts`**. `J-diff-shape.md §2.5` (line 75) mandates a path the tree does not hold | F.W5's **G4 ruling lands** AND this spec carries either **(a)** TA-4 restoration as a named PREREQUISITE (**all three limbs**, owner = the value.js API row) or **(b)** an EXPLICIT re-scope of the diff leg to fourier-only with the one-sidedness stated. **Silence is RED** | owner → value.js API row |
| **G2** | five-verb walk has a client edge on both sides | **FR-GV-8**: 7 of 13 `/api/visualizations` operations (remix · publish · unpublish · forks · provenance · diff · versions) have **zero client function** — independently enumerated `F8-CLIENT-03..09`; edges 36/45, 9 gaps. Probe: `union-prototype-walk.md` must carry **10 cells (5 legs × 2 object kinds)**, each with a bound client method + named consumer state, or `SERVER_ONLY` | all 10 cells carry a disposition **and an authority class** (M4). **0 of 5 legs clear on the fourier side today.** ▲ K-1: 13/44, never zero · ▲ X-9 before any percentage | F.W1/F.W4 (clients) · fourier API row (strikes) |
| **G3** ⊙ | two-sided C31 reproduced, then isolated | decoded stdout `{"code":"control.wrong-reason","errors":["client.method.visualization-update","operation.method.visualization-update"],"controlId":"R4.C31","verdict":"REJECT"}`, **exit 43**, against live `web/src/lib/api.ts:420` + `api/routers/visualizations.py:350`; fourier controls **terminal RED at 30/37** | the pair (i) **reproduces exactly** and (ii) `F8-C31A`/`F8-C31B` isolate each leaf under the stated constraints — raw receipts, same production validator, owner-only bypass, all-non-owner retention, **no caller-supplied expected code**. **0 of 2 controls exist. The later denominator must be OWNER-FROZEN** (32/38 only if lawfully replaced) | F.W8 (unit c) + owner (denominator) |
| **G4** | every fixture generated FROM the operation model | **FR-IC-6** (client-invented `EquationTier` over server `tier: str`) · **m-7** (`[1,500]` matching none of six windows) · **fr-CoefficientsSpectrum M-13** (`data: dict`; `EpicycleComponentDTO` dead with one self-hit; `BasisComponent` hand-maintained against a Python dict literal) | every register row names its **source operation model**; every structurally-untyped operation is **typed at F.W5 or NAMED AND STRUCK with its reason**. **A row sourced from a client type is a gate failure by construction.** ▲ Requires F.W5's per-operation joinable/unjoinable cell, or this gate is prose | F.W5 (typing) · F.W8 (register) |
| **G5** | model output byte-matches the wire | **GCM-10 + GCM-55**: `json.dumps` bypasses the model; `tier` absent under `extra="forbid"` yet written by `admin.py:183/:432/:438`; `likes` raw against `likes ?? 0` + bare `{{ entry.likes }}` — **two fields provably divergent** | a model-generated fixture matches the wire for every field, or **each bypass is named in the register with its reason**. ▲ **Blocked on resolving-or-striking the dangling "MF-9" cite before the cure is quoted** | fourier API row + F.W3/W4 (guard) |
| **G6** | round-trip replay reproduces its own frame | **BC-9/C-6/D-20 + SS-C-2** (server `30 / 30.0 s / 80` vs client `60 / 5000 ms / 100` vs renderer `ref(80)` / `20000 ms`) · **SS-C-1** (`speed` settable only at create/fork under `extra="forbid"`, while `animation_settings` is atom 4-of-5) | every atom of the version-identity set survives **create → update → reload with server-side units**, or the atom set is re-declared. **3 of 5 `AnimationSettings` fields fail today; a gallery replay cannot reproduce its own frame.** Includes R5's assertion: the last user-visible value is the persisted value | F.W5 (units) · fourier API row |
| **G7** ⊙ | off-state / cardinality admission ruled | **M-9**: `[]` minted `:103-104`, forbidden `min_length=1` (`visualization.py:186/:280`), substituted (`workspace.ts:351-353`, `gallery.ts:252`), refused (`:53`), force-prepended (`:109-114`) — honoured on screen, destroyed on save, **zero diagnostic** | the owner rules (admit or stop minting) and the fixture proves the ruled branch. **Unruled = RED; F.W8 may not author on a guessed branch.** The silent rewrite must not survive either way | owner → F.W5 ruling block |
| **G8** | cache identity ⊇ consumed request fields | **B-4**: closed 10-field `json.dumps` literal (`image_storage.py:250-266`) omitting `ml_threshold`/`ml_detail_threshold`; `images.py:219-226` short-circuits **before** `compute_contours`; `auto` is the shipped default (`defaults.ts:4`) | the derive-leg fixture asserts `contour_hash` **INSTABILITY** across an ML-threshold change. **Today it is stable — which is the defect** | fourier API row |
| **G9** | the probe does not perturb what it measures | **FR-GV-12** (`find_one` `:256` → `$inc {views:1}` `:268-270` → `_public_doc` serialises pre-increment `:272`) · **VV-R2-A** (the publish ETag map always misses; `workspace.ts:359`) · no `viewed_ips` while `liked_ips` exists · **FR-USB-24** (silent ≤60 s retry manufactures false timings in the authenticated leg) | the walk's **reads** are provably non-mutating, or the perturbation is **declared and subtracted** in the walk record; auth mutations pass `retryOn429: false` or surface the wait. ▲ **K12 and FR-GV-24 hold: SS-13 spends no probe here; repair tests must not assert a re-open increment** | fourier API row + F.W4 |
| **G10** | create leg idempotent-or-declared, both sides | **D-4/L-1/C-3** (`saveVisualization` unconditional POST, fresh slug per call, `:358` never read as a guard ⇒ **N clicks = N public rows**) · **fr-GalleryDraftsSection B-2** (idempotency-free publish POST over a write-only-empty `savedSnapshots`) · **C-6/C-7** (`Idempotency-Key` declared `:98-99`/`:138-139`, unpassed `:316-324`). Counter-witnesses: fourier `idempotency.replay_or_record(...)` (`visualizations.py:612`); value.js transactional fork (`forks.ts:94-137`) | each side's create verb **carries idempotency or declares its absence with the reason**, and the register records which half each repo holds. **Neither side holds both today.** ▲ F.W8 claims **no credit** for the `:disabled` cure | fourier API row · value.js API row |
| **G11** | validate-at-boundary exists somewhere on the client surface | **FR-CP-16**: the whole 45-operation surface lands as **ONE** unchecked `(await res.json()) as T` (`api.ts:192`); IndexedDB rehydration (`workspace.ts:171`) replays through the same zero checking; a violation surfaces as **`NaN%` bar widths** · **fr-FourierShapeExtractor C-15**: `as any` at both consumers with `resolveJsonModule` already providing the discarded type | at minimum the epicycle payload validates at the boundary and the fixture proves **the failure mode is an error, not a `NaN`**. **0 of 45 operations validate today** | F.W4 + fourier API row |
| **G12** ⊙ | one generated/checked contract shape | **FR-AFP-71** (672 L / 391 L, no codegen; three copies of the flagged shape, one rotted) · **FR-AFP-36** (three dead server models; **no `response_model` anywhere**) · **GAB-12** (`types.ts:110-118` ↔ `api/models/gallery.py` hand-mirrored, `res.json()` unvalidated; drift = six silent em-dashes) | the owner rules codegen-vs-hand-typed-twins and **the register either IS the generated shape or documents why it is not**. ▲ **K9 holds: the `?? item.slug` fallbacks are NOT deleted** | owner → F.W5 ruling block |
| **G13** | liveness — every envelope field has a producer AND a consumer | **M-β4** (`EpicycleData.trace`, 3000 samples/response, zero web-tree readers, `structuredClone`d into every IndexedDB draft, `workspace.ts:95-104`) · **L·m-6/C·D-14** (`reconstructed_points`, 500+500 floats, `equations.py:114`/`:128`, read by nothing) · **PP-DEADSEAM** (`preview_path` written `""` at `assets.py:85`, `image_storage.py:318`, `responses.py:22`; client leaf unreachable) | the walk **touches every envelope field** and the unconsumed set is empty **or each member carries a drop-or-consume disposition**. **3 fields already proven dead** | F.W5 (predicate) · F.W8 (measurement) |
| **G14** | fixtures in BOTH directions; FN-6 relayed | ▲ **AMENDED (A-1, measured 2026-08-28)**: the value-side half is **not extant** — landed `7351297f`, merged `8a2a617e`, **deleted `a8ff7792`** with atomdiff (`D`, 371 lines). Census §3c: **four unanswered value→fourier letters, zero inbound rows in the V INBOX**. Reading: **0 extant · 1 landed-then-excised · 0 reciprocated** | the register covers **both directions** and the relay letter is **sent + INBOX-ledgered (E13)**. ▲ The fourier reader stays **fourier-owned**; value.js neither dictates it nor reads fourier's tree. ▲ **`canonical_digest` stays SKIPPED-sanctioned — a fixture re-opening it FAILS this gate.** ▲ The value-side fixture bytes land via the value.js API row (execution gate + G1) | F.W8 (register + letter) · value.js API row (bytes) · fourier (reciprocal) |
| **G15** | band carry-closure — set difference empty both ways | ▲ **AMENDED (A-2, measured 2026-08-28; operand re-based on the registry per R-3 — this gate never had a carry to run against)**. The ASCII-spelling figure reproduces **exactly**: `grep -rho "F\.W5-W8" $R/fr-*.md \| wc -l` → **120**, across **26** records. But `grep -rho "F\.W5–W8"` (**en-dash**) → **72 more across 24 records**; the union is **192 mentions across 49 of 66** — **23 records use ONLY the en-dash and are invisible to the ASCII grep.** A closure run on one spelling **manufactures 23 silent drops** — precisely this gate's defect class. Plus: `grep -rho "F\.W8" $R/fr-*.md` → **0** (no row pre-assigns this wave) | **set-difference ∅ in BOTH directions** between **the band's routed registry identities** — the 123 of §1, re-derived at closure time from `$R/fr-*.md` with `grep -rhoE "F\.W5[-–]W8"` and boundary-aware id matching, cross-read against `conformance/PASS-1/F-W8-CHECK.md` §1 — and **the §3 rows + §6 Excluded + §6a's 36-row home table**. Output committed to `F-W8/carry-closure.md`. ▲ **Operand law (R-3): the LHS is the REGISTRY, by path; "the CARRY" is not vocabulary this gate may use, and no F.W8 carry is minted to supply one.** ▲ **Method law: the closure RE-DERIVES the homing; it does not re-hash any intake.** ▲ **Homing law, re-worded DISCHARGEABLE (R-5)**: *every band row has exactly ONE **STATING** home among F.W5/W6/W7/W8; F.W8's assertion citations are **not** homes.* The prior wording ("homed at exactly one of F.W5/W6/W7/W8 — no double-booking") was **undischargeable from this end** while `grep -c "F\.W8" F-W5.md` → **0** — the keystone never named its consumer. **Re-measured 2026-08-28: the reciprocal `F.W5 → F.W8` row HAS LANDED in `F-W5.md` §4** (§5c quotes it), so the leg is discharge*able*; it is **not discharged** — the gate is born-RED and closes only when unit **e** runs the difference and commits the output. FR-GIG-5 mirror holds: no wave credited with another's cure. ▲ **Runnability precondition: every id on both sides is RECORD-QUALIFIED** (R-5) — `fr-BasisCanvas C-17`, `fr-EquationView B-1`/`B-2`, `fr-ContourEditorCanvas C-2`, `fr-FunctionInput L-B1`/`L-M3`, `fr-CoefficientsSpectrum M-13`, `fr-BasisCanvas C-18` — the U-12 eight named identically at `F-W5.md` §4's reciprocal row. A set difference over bare tokens cannot separate booked from escaped and is **not** a run of this gate. **A non-empty difference after two closure passes HALTS the wave** | F.W8 (unit e) |
| **G16** | the admission horizon's only UI entry loads the saved entity | **GCM-1** (`:185` `image_slug` → `GalleryView:396` `router.push('/w/${slug}')` → the caller's IndexedDB draft, hard-reset to defaults, writing a draft row keyed on **another user's** `image_slug`; `loadVisualization` `workspace.ts:197-232` has **zero call sites**) + **VV-R2-B** (10 of 24 store exports unconsumed; `gallery.ts:168` bypasses `deleteVisualization`) | opening a published visualization loads the **saved entity** with slug + ETag captured, writes **no cross-owner draft row**, and the create → derive leg completes **through the product's real entry point**. **Data-dead today** | F.W4 (wiring) · F.W8 (assertion) |

---

## 5. Sequencing

### 5a. In-wave order (serial)

`a` walk map (§P, R-1..R-7 per-verb answerability + the consumed denominator) → `b` fixture register (§J, both directions, generated-from-model) → `c` C31 reproduction + A/B split → `d` `union-walk.mjs` **authored, not run** + the §M non-perturbation declaration + §R round-trip and §D liveness assertions → `e` rulings surfaced, FN-6 relay letter + INBOX row, **G15 carry closure**. Each unit commits before the next opens.

### 5b. Cure-integrity locks (bind the repair, not the finding)

▲ **K-1** 13/44, never zero · ▲ **K-3** m-7's 422-straddle is refuted from source; the fixtures may not cite it · ▲ **K9** do not delete the `?? item.slug` fallbacks · ▲ **K12** B-2's duplicate-row outcome closes STATICALLY; SS-13 spends no probe · ▲ **FR-GV-24** repair tests must not assert a re-open increment · ▲ **fr-FourierShapeExtractor C-3** no closure heuristic; the flag is carried, never inferred · ▲ **M-2** the tracked in-process generator exists for 5 of 7 assets · ▲ **DO-NOT-REGENERATE on `master`** — any regeneration revives L-B1 and L-B2/C-2 at BLOCKER · ▲ **M-CK-class** fix the abort key first (P8's per-call key) · ▲ **basisFilter WAVE-LOCK** — wire only with the banked M-10 normaliser · ▲ **MF-9** resolve or strike before quoting GCM-10's cure (**F-W5 gate G22, §3** — R-1f; never "§6c", which does not exist) · ▲ **BC-20** not re-booked · ▲ **`canonical_digest`** SKIPPED-sanctioned; do not silently re-open · ▲ **`UTF8_BYTEWISE_CODEPOINT`** ordering, never `localeCompare` · ▲ **X-9** one member-scope law before any percentage · ▲ **D9** the ruled visibility/lifecycle/moderation vocabulary · ▲ **D-19** re-resolve every fourier anchor after F.W0 · ▲ **FR-GIG-5 mirror** no wave credited with another's cure · ▲ **A-1/A-2/A-3** the three value-side amendments are recorded **at this amending record; the banked files are NEVER rewritten** · ▲ **RECORD-QUALIFICATION (R-5)** every short id is written with its record wherever it is booked, cited or excluded — `B-1`, `B-2`, `C-2`, `C-17`, `C-18`, `L-B1`, `L-M3`, `M-13` are each ≥2 distinct banked rows, so a bare token is not an identity · ▲ **NO PHANTOM INTAKE (R-3)** only `carry/F-W1-CARRY.md` and `carry/F-W4-CARRY.md` may be cited as carries, and only by path; F.W8 has none, mints none, and grounds every closure on the registry · ▲ **ANTI-RENAME** banked ids are original for life: an id is carried, folded or excluded under the name the record gave it — never re-minted, never silently merged.

### 5c. Cross-edges — declared from this end

| edge | direction | this end's declaration |
|---|---|---|
| **F.W0** substrate pre-gates | **DEPENDS — HARD** | ▲ **G-11 / G-12 QUOTATION (R-9.1): every corrected anchor and every corrected denominator this spec uses is an *input to / quotation of* F.W0's published tables — G-11 (`F-W0.md:199`, *"ONE corrected anchor table published; every later wave quotes it"*) and G-12 (`F-W0.md:204`, *"ONE corrected-denominator table published; superseded figures FORBIDDEN downstream"*), landing in `SUBSTRATE-LEDGER.md` (path declared at `F-W0.md:63`). F.W8 re-performs neither re-resolution as its own act; divergence from those tables is a defect against G-11/G-12, not a rival act of this wave.** No line anchor, HEAD figure, or "LIVE at certified HEAD" citation enters this spec until F.W0 re-grounds (D-19). Named blockers: glass-ui 4.0.0's syntactically corrupt `dist/styles/index.css` (fr-PaperSidebar M1); the **dirty fourier worktree** (GAB-13 — 28 entries re-measured; the census pin `14d83356` **does not resolve**); `F8-REACH-01` (`InfoCard.vue`, `b2c2e718…`/1,516 B, unmounted) and `F8-REACH-02` (`CanvasOverlayButton.vue`, `35f3db91…`/595 B, unmounted) — for both, **"HOLD is RED"**. **If F.W0 fails to re-ground, F.W8 HALTS; it does not proceed on stale bytes** |
| **F.W1** atomic tri-package uplift | **DEPENDS — WITH A DECLARED CYCLE** | ▲ **CONFLICT DECLARED FROM THIS END, for adjudication at the spine, not at execution**: the DAG puts F.W8 after `F.W1.close`, but fr-AdminFlaggedPanel's **roster-wide ruling 5** requires *the F.W5-W8 producer-or-retire decision to precede F.W1's sizing where grades are load-bearing*. **F.W8 does not own that ruling — F-W5 G11 does — but the cycle passes through this wave's band and must be cut before either opens.** Otherwise independent both ways. ▲ **F.W1 is ONE atomic land-or-lose transaction — the ELEVEN-limb roster chartered at `F-W1.md:276`, incl. the FR-EQC-7 vaul-vue manifest gate INSIDE per `:294` — cited whole, never restated here** (R-4b; the prior three-limb naming would have let a reader of F-W8 alone size the transaction at three, omitting the vaul-vue gate, the RE-PIN at the adopted commit hash, and the P0 CSS-class census). The transaction — whose failure mode is four zero-console-error e2e gates going red (FR-EQR-3) — is untouched here, and ▲ **F.W1 must NOT be credited for the pagination-drain cure (FR-GIG-5)** — F.W8 asserts that separation in P8 and claims no credit for anything F.W1 owns |
| **F.W2** value.js consumption to spec | **DEPENDS** (ordering only) | F.W8 books **zero migration cost** and inherits F.W5's exported zero-cells so **no fixture row manufactures overlap**: App API-inert · MobileFloatingToc · CollapsibleSection · the PaperSearch trio (*"F.W5-W8 = no rows"*) · MorphShapePreview (structurally unreachable — R6-8 and R3-7b both) · ▲ **UserSlugBar FR-USB-38 — do NOT book this component into the F.W2 migration surface.** These measured negatives are **denominators, not work** (P-9) |
| **F.W5** the shared-provenance contract | **DEPENDS — HARD; the reciprocal DECLARED at both ends (re-measured 2026-08-28)** | Required as **artifacts, not prose**: **(1)** `J-diff-shape-v2.md`'s clause set with each clause RULED / DECLINED-WITH-REASON / OWNER-GATED; **(2)** `operation-register.md` — the 45 = 30 + 13 + 1 + 1 enumeration with per-operation **authority class** (R3-7b) and **client-edge disposition** (R3-7c) **plus a joinable/unjoinable cell**, or G4 is unfalsifiable; **(3)** `OWNER-RULINGS-F.W5.md` — G4 (TA-4), G7 (trie-vs-KISS), **M-9** (off-state `[]`), G10 (like verb), G11 (flags producer-or-retire), FR-AFP-9 (hard-delete arm), **FR-AFP-71** (codegen vs twins); **(4)** the casing rule (TS-camel ↔ Python-snake, the ONLY allowed envelope difference) as a **mechanical check on each side**. ▲ **DEFECT DECLARED, THEN CURED AT THE KEYSTONE'S END — both readings dated, neither overwritten (E-3: addenda, not patches).** **Read 1 (2026-08-28, this seat):** `F-W5.md` **§4** — *"Cross-edges — declared from THIS end (one home, two citations)"* — declared edges to F.W0, F.W1, F.W2, F.W3/W4, F.W6, F.W9/W10, SS-4, the value.js API row, the glass BH relay, X·parse-that and SS-13, and **NO edge to F.W7 or F.W8** (`grep -c "F\.W8" F-W5.md` → **0**). The anchor is corrected per R-1f: `F-W5.md` has **no §6b and no §6c**, and the prior draft's "§6c" attestation is struck. **Read 2 (re-measure, same session, after R-5 was ruled):** the reciprocal **HAS LANDED** — `F-W5.md` §4 now carries the row **`F.W5 → F.W8` | the RECIPROCAL, declared from this end**, verbatim: *"F.W5 STATES the clauses; F.W8 ASSERTS them (fixture/e2e direction). ONE home (F.W5), two citations — the shared ids are enumerated at `F-W8.md` §1's census. No F.W8 row re-books a clause; no F.W5 clause claims F.W8's assertion work. Reciprocal: `F-W8.md` §5c."* ▲ **This end now reciprocates by name**: §5c IS that reciprocal, §1's census IS the enumeration it points at, and **§6a** names the STATING home of every band identity F.W8 does not carry. ▲ **The set-difference precondition F.W5 attaches is adopted here verbatim**: the shared ids run in the **record-qualified** form on both ends — the U-12 eight (**M-13 · L-B1 · L-M3 · C-17 · C-18 · B-1 · B-2 · C-2**) are ambiguous bare and cannot be differenced; §3, §6a and §5b now carry that qualification throughout. ▲ **The split, stated from this end**: **64** of the ids booked in §3 also occur in `F-W5.md` — F.W5 **STATES** the clause, F.W8 **ASSERTS** it in the fixture/e2e direction; **no §3 row re-books an F.W5 clause**, and F.W8 claims no credit for any clause F.W5 states (FR-GIG-5 bar). ▲ **Standing caveat (D-19)**: `F-W5.md` is under concurrent repair this round; the reciprocal is re-confirmed at open by **id** (the `F.W5 → F.W8` row in §4), never by line |
| **F.W6** provenance defect burn-down | **SIBLING — F.W8 ACCEPTS, F.W6 BURNS** (no gating either way) | F.W8 repairs nothing and claims credit for none of F.W6's cures. But the **history leg cannot pass while F-α stands** (every version `depth=0`; corroborated three ways at intake X-8), nor the **diff leg while F-β stands** (PATCH `$set`s atom 5 without recomputing `set_hash` or writing a version; `palette_slug` never clearable to null), nor the **provenance leg honestly while F-γ stands** (no redaction — `_readable_or_none` applied only to the entry row `:744`, so `slug`/`set_hash`/`author_slug`/`created_at` cross the wire for private and draft ancestors, while value.js collapses non-public hops to `{kind:"unavailable", ordinal}`). **Declared as ACCEPTANCE dependencies, booked once at F.W6.** ▲ Also declared: the **FSE pipeline DECISION (C-7) has NO wave owner** — F.W8 **nominates F.W6 and requests adjudication**; the DO-NOT-REGENERATE tripwire binds F.W8 wherever it lands |
| **F.W7** trie-like compression | **SIBLING — OWNER-GATED**, no gating either way | F.W8 designs no trie. If G7 **admits** it, the derive leg must exercise structural sharing; if G7 **sustains the incumbent KISS guardrail** (`atomdiff.py:12-14` verbatim: *"the atoms are a flat BAG (not a tree / Merkle / document); the diff is a whole-atom replace …; there is no three-way / DAG / merge"* — carried on BOTH sides), the prototype asserts **whole-snapshot duplication as the DECLARED shape and records its cost** (fourier re-persists all 5 atoms incl. the 12-field `ContourSettings` and 6-field `AnimationSettings` per version; value.js re-persists the whole `colors: PaletteColor[]`). **R-4 is the single union requirement with ZERO material on either side.** Unruled = F.W8 carries the documented default **and says so** |
| **F.W9 / F.W10** deploy spine, gates, close | **F.W8 EMITS evidence — DOUBLE-HOME RISK DECLARED** | **F.W8 LANDS the fixtures and the walk script; F.W9/W10 RUN the close-gate as evidence** — declared because **`F-W5.md` §4** (cited by section, per §0's addressing law) routes *"v1 §6's close-gate … re-authored by v2 and executed there"* to F.W9/W10 while census §4.9 gives F.W8 the conformance fixtures: **one home, two citations, no wave credited with another's cure.** Binding law carried: **each side's probe asserts against the DOCUMENT, never against the sibling (inv-26) — no cross-repo read, ever.** Coverage seams emitted: **AA-44** (zero frontend tests; nine e2e specs never touch the audit tab) · GAB-12's test seam · FR-AFP-66's orphan-flag test exercising only the reporter-keyed path · ▲ **FR-AUL-13's lesson adopted as this wave's own bar: a contract document without a probe REPEATS the defect that row convicts.** The walk's execution environment (a non-production stack) is **F.W9's**, not this wave's |
| **the value.js API row** (X·V W3/W9) | **CO-SIGNER; neither side's edits gate the other's waves** (COHESION §2) | Obligations created here, booked as **VALUE-SIDE rows, never re-booked as fourier defects**: **TA-4** (restore `lib/crud/atomdiff.ts` + `atomDiff` on `PaletteVersion` **+ the excised conformance fixture, A-1**, or be re-scoped out — G1) · **V-α** (revert writes no version row yet still `$inc`s `versionCount`, permanently drifted; no test asserts otherwise) · **V-β** (global `PaletteVersion._id` collides across palettes — fourier already solved it with the compound `{viz_slug}:{set_hash}`) · **V-γ** (nullable `userSlug` ⇒ an unattributable edit mutates the palette and writes no version row: `crud.ts:119`, `crud.ts:204`, `versions.ts:147`). Cross-checked against `api-gap-remeasure.md`'s nine classes and **X-1** (`computeContentHash` ≠ `payloadHash`), **X-2** (stored `forkCount` as public authority), **X-3** (PATCH TOCTOU / no CAS), **X-6** (`unlisted` still alive) — all VERIFIED-GAP — and against ruled **D9**. ▲ **X-4 (the `/versions` rail) is CHANGED–CONTESTED and needs an owner ruling; the value-side history leg must not assume its shape.** ▲ FN-5's currency invariant re-anchored by **A-3** |
| **SS-4** union synthesis | **F.W8 IS the union's ACCEPTANCE half** | F.W5 states the contract, F.W6 burns the defects, F.W7 rules the compression, **F.W8 proves the whole thing walks.** Owner rulings FLAGGED INLINE per SS-4's charter; F.W8 surfaces **only** those F.W5's block does not own — **(i)** the fixture-direction law's exception set (FR-IC-6) and **(ii)** the re-scope branch should G4 decline TA-4, now carrying A-1's third limb. Everything else routes to `OWNER-RULINGS-F.W5.md` and is **consumed here, never re-ruled** |
| **SS-13** live-probe lane | **DEFERS magnitude only** | FR-GSB-28's interleave symptom · VV-R2-A's observable phantom view · B-4's `contour_hash` instability probe · M-14's end-to-end contour-loss reproduction · L-M2's extraction-scope magnitude · L-B1's *"what `moon.json` WAS generated from"* (undetermined) · FR-AFP-33's aggregate cost. ▲ **K12 and FR-GV-24 hold. No F.W8 gate is discharged by an SS-13 probe; magnitude is not a gate** |
| **glass-ui BH relay (SS-6)** | **DECLARED EMPTY** | F.W8 carries **ZERO** producer rows — declared so that (a) no glass-producer row is smuggled in as a contract or fixture clause, and (b) no contract row is discharged as a frontend hack. Any producer-shaped finding leaves **as a letter** to the standing BH inbox (owner edict, standing formation invariant). **FR-GIG-1's pagination drain: named and routed, never hacked** |
| **X·parse-that** | **FORBIDDEN EDGE (declared)** | Direct parse-that→fourier routing is forbidden and has never existed (PLAW-BIND clean). No F.W8 row is discharged through the parser sub-tranche |
| **`/Users/mkbabb/Programming/fourier-analysis`** | **READ-ONLY, ALWAYS** | Zero fourier bytes. The register and the FN-6 ask travel as a **letter** for co-signature; the reader strategy is fourier's own. `J-diff-shape.md` v1 is **IMMUTABLE**; v2 supersedes by reference (E-3) |
| **execution gate** | **ABSOLUTE** | Status stays `planned`. Nothing opens, writes, builds, or runs product source in either repo — including `$V/api/test/conformance/**` — until the owner's begin-word. **Specifying is not doing** |

### 5d. Cadence, artefacts, commits

Docs-only wave. After each unit and before close: `git diff --check`; the markdown link check over the new `conformance/` paths; the **G15 spelling-agnostic set-difference re-run**. `npm run typecheck` / `test` / `lint` are **not this wave's gates** — no source byte moves, so a delta in any of them means the wave escaped its bounds and is itself the failure signal (run once at close as that tripwire, not as evidence). Committed at close (L-7 — an uncommitted witness does not exist): the five `conformance/` artifacts, `walk/union-walk.mjs`, `F-W8/carry-closure.md`, the relay letter + its INBOX row, and the F.W0 re-grounding receipt this wave's anchors were re-resolved against (cited, not copied). Pathspec commits only, one per unit; `scripts/dev/dev.sh` never staged.

---

## 6. Excluded — carried nowhere else, with reason

Nothing below is a silent drop; each is named so a later reader cannot mistake absence for oversight. **§6 excludes by CLASS; §6a excludes by ID** — the 36 band-routed identities §3 does not carry, one row each, with the STATING home of every one. Together they discharge the promise the class table alone could not: the prior draft's §6 named **none** of the 36 (F-W8-CHECK D-3), so the closure was deferred wholesale to G15's unit **e**. It is performed here.

| excluded | reason |
|---|---|
| **The TA-4 restoration itself** (all three limbs) | ⊙ **G1 unruled.** F.W8 states the prerequisite and carries the honest re-scope default; the restoration is the **value.js API row's** act |
| **Re-booking A-1 as a fourier defect** | **VALUE-SIDE row.** The excision was value.js's own T.W1 act; fourier owes nothing for it |
| **F.W4's wiring unit** (route → `loadVisualization`; publish → `setVisibility`; delete → the store action) | F.W4's, as **one** unit (VV-R2-B's cure shape). F.W8 asserts adoption and claims **no credit** |
| **The `:disabled` / re-entry-guard cure** (FR-GV-1's client arm) | F.W3/W4's per FR-GV-1's own split law. F.W8 owns only the **server dedupe** assertion |
| **F.W1's tri-package transaction**; the pagination-drain cure | F.W1's; and ▲ **F.W1 may not be credited with FR-GIG-5's cure** — the true cure rides the SS-6 producer relay |
| **Re-deriving the 45/30/13 enumeration or the 0/45 security figure** | **Consumed from F.W5's `operation-register.md`** — one home, two citations. Re-deriving would double-book the keystone |
| **Re-booking the FSE tripwire** | Already banked at **F-W5 G1c/G16**. Carried here **as a binding lock only** |
| **The FSE pipeline DECISION (C-7)** | **No wave owner in the census taxonomy.** F.W8 nominates **F.W6** and requests adjudication at the spine — routed, not dropped |
| **Any regeneration of `moon.json` / `sun.json`** | **TRIPWIRE** — DO-NOT-REGENERATE on `master`; any attempt revives L-B1 and L-B2/C-2 at BLOCKER |
| **A post-hoc closure heuristic** | **KILLED (C-3)** — 44.08 closed vs 44.97 open overlap; the flag is carried, never inferred |
| **m-7's straddle-a-422 scenario** | **REFUTED from source (K-3)**; the fixtures may not cite it |
| **Deleting the `?? item.slug` fallbacks** | **NOT PROVEN (K9)** — `admin.py` emits `doc.get("image_slug")` unvalidated; defensive |
| **A repair test asserting a re-open view increment** | **FR-GV-24 lock** — the session guard works; the defect is SCOPE, not absence |
| **A live probe of B-2's duplicate-row outcome** | **K12** — closed statically; SS-13 spends no probe |
| **`canonical_digest` parity** | **SKIPPED-SANCTIONED.** Adoption = a stored-hash migration, not parity. A fixture re-opening it **fails G14**; the three named twin divergences stay un-unified BY DESIGN |
| **A general two-point diff fixture** | fourier's `/diff` is **not** general — `on_chain` is only `{head_hash} ∪ {fork_of_hash}` (`:827`), reads the STORED `atom_diff`, 404s any other pair |
| **The FN-6 reader implementation** | **fourier-owned.** value.js neither dictates it nor reads from fourier's tree (dispatch-homes A.1) |
| **Reopening R.W6 §11-Q9** (fixture home) | Resolved to *"value.js lands, in-tree, read locally"* (M4). **Dissent recorded, not re-litigated** |
| **Trie / structural-sharing design** | ⊙ **G7 (F.W7) unruled.** F.W8 carries the documented KISS default and authors no design |
| **Tier↔flag SEMANTICS; the moderation band's rulings** | **F.W5/F.W6's.** F.W8 owns only FR-GV-9's batch-write state-preservation assertion |
| **F.W6's F-α / F-β / F-γ burn-down** | F.W6's. Declared here as **ACCEPTANCE dependencies**, booked once there |
| **Value-side fixture bytes in `api/test/conformance/**`** | **Execution gate + G1.** Specified in the register; landed by the value.js API row |
| **Any glass-ui producer row** | **SS-6 relay DECLARED EMPTY**; producer-shaped findings leave as letters |
| **Editing F.W5's contract artifacts, `COHESION.md`, or any evidence record** | E-1/E-3 immutable; the spine is root-authored. Editing one destroys the evidence a gate cites or creates a hidden cross-wave write conflict |
| **Negative-control rows in any denominator** | MorphShapePreview §3, the PaperSearch trio, MobileFloatingToc, CollapsibleSection are **EVIDENCE** (P-9); counting them manufactures overlap |
| **Any coverage percentage published before X-9** | **One member-scope law first.** A percentage over an unfrozen member scope is a fabrication |
| **All product source, both repos; all fourier bytes** | Execution gate + read-only law |

### 6a. Excluded by ID — the 36 band identities F.W8 does not carry, each with its STATING home

**Why this table exists**: §1 measures **routedTotal = 123** band identities; §3 books **87**. The other **36** are **not corpus-level drops** — every one is stated by a sibling band wave — but until they were named here, F.W8's *"nothing below is a silent drop"* covered them only as class prose, and **G15's set difference had no per-id operand to run against** (F-W8-CHECK D-3/D-4). Landing shape per R-2's law and R-5's one-home rule: these are **CITATIONS, never bookings** — a second wave that needs an id cites the adopting wave, and **booking any row below here would double-book the keystone**. Every id is **record-qualified** (R-5). Homes verified at the bytes by this seat, 2026-08-28, with boundary-aware matching.

| # | identity (record-qualified) | routed at | STATING home | F.W8's relation |
|---|---|---|---|---|
| 1 | fr-AdminAuditLog **AA-5** | `:42` | **F-W5 clause D16** (§D) — the shared action taxonomy | cited, not booked; the admin leg's taxonomy is F.W5's to state |
| 2 | fr-AdminAuditLog **AA-10** | `:47` | **F-W5 clause E18** (§E) — attribution: the actor is a FIELD | cited; M2's identity-mixing assertion rides E18's predicate, states none of it |
| 3 | fr-AdminAuditLog **AA-23** | `:142` | **F-W5 clause D15** (§D) — query semantics under the shipped indexes | cited; no F.W8 fixture asserts index shape |
| 4 | fr-AdminFlaggedPanel **FR-AFP-1** | `:44` | **F-W5 clause D3 ⊙** (§D) — moderation producer-or-retire, THE ADMISSION GATE (also cited INVERTED at clause **D1**) | cited; the §5c F.W1 cycle passes through this ruling and F.W8 declares the cycle without owning the ruling |
| 5 | fr-AdminFlaggedPanel **FR-AFP-7** | `:55` | **F-W5 clause D4** (§D) — flag identity keys the ENTITY | cited. ▲ Also the D-4 homonym trap: a bare `grep -c "FR-AFP-7"` is masked by `FR-AFP-70`/`-71`, both of which F.W8 **does** book at J7/M3 |
| 6 | fr-AdminFlaggedPanel **FR-AFP-8** | `:56` | **F-W5 clause D5** (§D) — which identifier a human-facing surface may render | cited |
| 7 | fr-AdminFlaggedPanel **FR-AFP-18** | `:65` | **F-W5 clause A2** (§A) — envelope + cardinality ruled once | cited |
| 8 | fr-AdminFlaggedPanel **FR-AFP-32** | `:86` | **F-W5 clause D17** (§D) — date serialization, serializer arm | cited |
| 9 | fr-AppHeader **FR-AH-6** | `:52` | **F-W5 clause C3** (§C) — session truth + the canonical admin predicate | cited; M2 asserts the authenticated leg's honesty over C3's predicate, does not state it |
| 10 | fr-BasisCanvas **C-17** | `:102` | **F-W5 clause E15** (§E) — ONE transport clause | cited. ▲ **Not** fr-CoefficientsPanel C-17, which F.W8 **does** book at J6 (= FR-CP-16) — the D-4 collision, now unmasked by qualification |
| 11 | fr-BasisCanvas **C-18** | `:103` | **F-W5 clause E15** (§E) | cited |
| 12 | fr-ContourEditorCanvas **C-2** | `:48` | **F-W5 clause E17** (§E) **+ gate G15** (§3) — image bounds on write | cited. ▲ **Not** fr-FourierShapeExtractor's `L-B2 / C-2`, which sits in D2's block |
| 13 | fr-ContourPreview **row 28 / D:i-3 · L:L-8** | `:61` | **F-W5 clause A3** (§A) — boundary-validation evenness | cited |
| 14 | fr-ContourSettings **M-15** | `:72` | **F-W5 clause F5** (§F) — *"one server identity, two witnesses"* (with C·D-02) | cited |
| 15 | fr-ContourSettings **m-18** | `:94` | **F-W5 clause E13** (§E) — cache identity ⊇ consumed fields | cited; F.W8 books the same clause's **B-4 ⊕ i-7** legs at R4 and asserts them — E13 remains the single STATING home |
| 16 | fr-EasingPicker **L/M-3** | `:65` | **F-W5 clause E11** (§E) — easing domain hoisted to the operation (**merged with RESOLVER, both ids preserved**) | ▲ **The sharpest of the 36** (D-3): R1 asserts *"only easing/speed/active_bases round-trip"* while the easing **domain** row is E11's. F.W8 **asserts over** E11's hoisted domain and **states none of it**; the RESOLVER half is already carried at §3 J1's fold list, so the mechanism is present and the id now is too |
| 17 | fr-EditorControlsDock **D-12 / C-4 / C-5** | `:59` | **F.W3/W4** — the `:disabled` / pending-state / double-submit client arm; **no F-W5 clause books it by bytes** (re-measured 2026-08-28) | excluded-with-reason, and already excluded by class at §6 row 4 (FR-GV-1's split law). This record's band leg that F.W8 **does** carry is **C-6 / C-7** (`:60`) at §3 P7, stated at **F-W5 clause C1** (§C) |
| 18 | fr-EqCoefficientsPanel **FR-EQC-8** | `:45` | **F-W5 clause A5** (§A) — no untyped operations (= C-11) | ▲ Material (D-3): *"F.W5–W8 owns the tuple seam … one adapter, both directions; rides the R6-8 contract lesson."* F.W8's J3 asserts the same law over `M-13`; the tuple-seam **typing decision is A5's**, and J3's cure clause — *typed at F.W5 or NAMED AND STRUCK in the register* — is exactly this row's disposition |
| 19 | fr-EquationPanel **D-3+C-5** | `:48` | ▲ **NOT A BAND ROW** — `fr-EquationPanel.md:48` routes this identity *"— F.W4 + F.W2 rider"* by bytes | **census correction, recorded not adopted**: the check's row-identity resolution attached the band token to the wrong id on a 15-identity MAJOR line; that line's true band rows are `D-10+D-L12+C-22` (F-W5 clause **F9**, §F) and `D-L5+C-1` (row 20). Kept in the table so a later reader can reproduce the correction rather than rediscover it |
| 20 | fr-EquationPanel **D-L5+C-1** | `:48` | **F-W5 clause C4** (§C) — retry / limiter posture | cited; M2's `retryOn429` assertion rides C4's posture |
| 21 | fr-EquationView **B-1** | `:47` | **F-W5 clause F1 ‡** (§F) — partial projection; `latex_sigma` joins `SimplifyResponse` | cited. ▲ **Not** fr-ContourSettings B-1, which F.W8 **does** book at J4 — the D-4 collision. F1's cure is itself **CURE-BOUND by M-CK** (fix the key first), and F.W8 carries M-CK-class as a §5b lock |
| 22 | fr-EquationView **B-2** | `:48` | **F-W5 clause F2 ‡** (§F) — one shared bound constant (`budget` walks past `le=50`) | cited. ▲ **Not** fr-GalleryDraftsSection B-2, which F.W8 **does** book at P7/G10 |
| 23 | fr-EquationView **C·D-02** | `:56` | **F-W5 clause F5 ‡** (§F) — the error envelope is actually spoken | cited |
| 24 | fr-EquationView **M-DV** | `:113` | **F-W5 clause F4** (§F) — server-side domain validator | cited; J4's generated-bounds act consumes F4's validator, authors none |
| 25 | fr-FunctionInput **L-B1 / C-1** | `:38` | **F-W5 clause F2** (§F; **FOLDS** there with fr-EquationView B-2 ⊕ C-8) | cited. ▲ **Not** fr-FourierShapeExtractor L-B1, which sits in D2's block; F.W8 books this record's **C-8 / N-4** at J4 |
| 26 | fr-FunctionInput **L-M3** | `:53` | **F-W5 clause F6** (§F) — a response field states WHICH population it measures | cited. ▲ **Not** fr-FourierShapeExtractor `L-M3 / C-6`, which sits in D2's block |
| 27 | fr-GalleryCard **C-1-as-corrected** | `:50` | **F-W5 clause D2 ‡** (§D) — the like verb (*four witnesses, ONE identity*) | cited; the like verb is not one of the walk's five |
| 28 | fr-GalleryCardModal **GCM-2** | `:43` | **F-W5 clause D2** (§D) — same four-witness identity | cited. ▲ Masked from a naive `grep -c` by `GCM-25`; F.W8 books GCM-1/-10/-52/-55 |
| 29 | fr-GalleryFeaturedCarousel **FR-GFC-3** | `:34` | **F-W5 clause D2** (§D) | cited |
| 30 | fr-GalleryFeaturedCarousel **FR-GFC-20** | `:54` | **F-W5 clause D8** (§D) — a blanket write is not a transition | cited; M3 asserts state-preservation over D8's rule and states none of it |
| 31 | fr-GalleryMarquee **GM-M4** | `:70` | **F-W5 clause B4** (§B) — **the LIVENESS predicate (registry-novel)**, beside PP-DEADSEAM | ▲ Material (D-3): the record's only band row, a live dead CRUD surface. **The mechanism is already F.W8's R6 and G13** — F.W8 books PP-DEADSEAM there and **measures** the unconsumed set; **B4 STATES the predicate, R6 measures it**, and GM-M4's id is now visible as one of B4's witnesses rather than absent by byte |
| 32 | fr-GalleryView **FR-GV-7** | `:43` | **F-W5 clause A6** (§A) — credential transport, both ends (cited again at clause **C3**) | ▲ Material (D-3): an admin bearer token accepted from the URL query. **M4 asserts, per operation, the authority class F.W5's register declares** — this row is one of the classes it asserts against; the transport **rule** is A6's and F.W8 authors none of it |
| 33 | fr-GalleryView **FR-GV-34** | `:73` | **F-W5 clause D6** (§D) — lifecycle truth and cascade (member-site rider on FR-AFP-9) | cited; F.W8's D9 lock carries the ruled lifecycle vocabulary, and D6 owns the cascade |
| 34 | fr-ImageUpload **row 26 / C:C-12** | `:60` | **F-W5 clause E15** (§E, transport half) **+ clause C2** (§C, security half) — cited again at clause **B1** (§B) and **B5** (§B) | ▲ Material (D-3): *"the incomplete **R6-8 join** — FOLD to the intake row."* The R6-8 join is F.W8's governing seam and is booked at **J1**; this instance is the record's own fold, and its **two halves are stated at two F.W5 clauses**. F.W8 asserts the join, re-books neither half |
| 35 | fr-SpeedSelect **SS-L-07 / SS-C-10** | `:59` | **`F-W5.md` §5** (*"Carried by citation — the excluded-from-clause-tables set"*), the row keyed `SS-L-07 / SS-C-10` ≡ **`carry/F-W4-CARRY.md:234`** — the banked byte, verified at that line by this seat. The row **splits the identity**: *"the **body is F.W4's**"* (the literal-union speed catalog); *"**F.W5's share is the type-reach only**"*, admitted under clauses **D12** and **E11** | ▲ **The only escape whose home is not an F-W5 clause** — at first read it had **zero** F-W5/F-W6 bytes; **re-measured 2026-08-28 it is homed at `F-W5.md` §5 by citation**, with F.W5 claiming no credit for the body (FR-GIG-5 bar). This is the sole escape whose banked byte lives in a real in-tree carry, and it is cited **by path**, per R-3. F.W8 books neither it nor a substitute: R1 asserts the **speed atom's round-trip**, while the **value-domain catalog** is F.W4's body and F.W5's type-reach |
| 36 | fr-UserSlugBar **FR-USB-15** | `:52` | **F-W5 clause C3** (§C) — session truth + the canonical admin predicate (with FR-AH-6, row 9) | ▲ Material (D-3): the MAJOR of the cluster whose two MINORs (**FR-USB-23 / -24**) F.W8 **does** book at M2. The asymmetry is now stated, not silent: **C3 states the session-truth cure** (401 handling + `getMe` revalidation at the store/api seam); **M2 asserts the authenticated leg's honesty over it.** ▲ The **FR-USB-23 ⊙ owner-gated ⊙ is kept on M2's routing row** (RULINGS R-1e), and **FR-USB-38 stays DO-NOT-BOOK** per §5c's F.W2 negative roster |

**Closure statement.** 36 identities, 36 dispositions, **zero bookings** — 34 cited to an F-W5 clause or section verified by clause id at this seat's 2026-08-28 read, 1 excluded to F.W3/W4 with its reason (row 17), 1 recorded as a census correction (row 19), and 1 homed at `F-W5.md` §5's carried-by-citation set (row 35). **87 booked + 36 named = 123 = routedTotal**, so §3 ⊎ §6 ⊎ §6a partitions the band's routed identities with no residue. ▲ Homes are keyed by **clause id + section**, never by line: `F-W5.md` is under concurrent repair this round and its lines moved between two reads by this seat in one session (§0's addressing law). ▲ This table is the per-id operand **G15** runs its set difference against; ▲ it creates **no** new obligation for F.W5 beyond the reciprocal R-5 already rules onto `F-W5.md` §4; ▲ and per FR-GIG-5, **naming a row is not claiming its cure**.
