# X.F.W0 — Re-ground the substrate (frontend half)

**Sub-tranche**: X·F (`docs/tranches/X/fourier/waves/`), authority `docs/tranches/X/COHESION.md` §0/§1 (SS-3/SS-4) + §0a BOUNDARY 2026-08-25, which names two of this wave's pre-gates verbatim ("the F.W0 substrate pre-gates (corrupt glass-ui 4.0.0 dist/styles/index.css; dirty-worktree settle)").

**Fold provenance (M-23)**: twice-authored (Fable ×2), agglomerated by the fresh-Fable fold seat 2026-08-28. Where the drafts disagreed on a measurable fact, the fold seat re-measured live and the winner is recorded (§1d). Dedupe law honored throughout: banked identities are cited at their banked home and never re-booked.

**Path reconciliation, re-measured at repair round 1, 2026-08-28.** **There is no `F-W0-CARRY.md`, and this spec cites none.** `ls docs/tranches/X/fourier/carry/` returns exactly two files — **`F-W1-CARRY.md`** (F.W1) and **`F-W4-CARRY.md`** (F.W4) — and per X·F repair ruling **R-3** those two are the ONLY carries any wave may cite, and only by path. This wave therefore takes its bounds from **its own tree-derived enumeration (§2a/§2a-i)** and takes its census, whole and underived, from **ONE operand: `docs/tranches/X/fourier/conformance/CENSUS-CANONICAL.md` §2's `### F.W0 — **57 rows**` ⟨*re-based at repair round 5: read 56; errata §0.4 **E5-1** homes `PP-GATE` here*⟩** ⟨**superseded at repair round 4 (R4-3.3 / R4-10)**: this clause read *"derives its census from ONE operand: the 66 … records directly, under the detector §3 publishes inline."* The operand was lawful when written and is not now — R4-3 adds *"a superseded census is never a denominator"*, R4-10 forbids a wave to derive or re-cut a roster from any source but the canonical, and the inline detector was convicted at PASS-4 D-5 for a first-position-only slash probe. **The corpus is still where the evidence lives and every §3 row still quotes it; what moved is the ROSTER, which this file no longer computes.** The roster map is §3b⟩. ⟨**Corrected at repair round 3 (R3-4.1; PASS-3 D-2)**: this sentence previously named *"two in-tree authorities"*, the second being the **82-row roster at `conformance/PASS-1/F-W0-CHECK.md` §1**. R3-4's standing law makes a check file a **prior run and never an operand** — *"a closure/census transcript whose LHS is a count copied from any check file FAILS BY CONSTRUCTION"* — so the second authority is retired, not merely re-numbered. The PASS-1 and PASS-2 checks remain citable as **prior runs** and as the record of what earlier seats measured; **the corpus is the only thing this file counts over**, and it answers **84**, not 82.⟩ "The CARRY" with no path is forbidden vocabulary in this file from this amendment forward (R-3.3), and **no new carry is authored here** (R-3.2). `ls docs/tranches/X/fourier/waves/` returns **11** files — `F-W0.md` · `F-W1.md` · `F-W2.md` · `F-W3.md` · `F-W4.md` · `F-W5.md` · `F-W6.md` · `F-W7.md` · `F-W8.md` · `F-W9.md` · `F-W10.md` — and this file's name is **`F-W0.md`**, the lane's live convention.

**Amendment minute — X·F L-20 repair round 1, 2026-08-28.** This file was amended under `conformance/PASS-1/RULINGS.md` (the six directives addressed to F-W0: **R-3** carry re-point · **R-4a** G-6 → precondition-check + F.W0.d re-cut · **R-9.3** G-13 dated addendum · **R-9.4** §2a denominator · **R-2c** the three escaped ids · **R-10.3/.5 + R-1f** path and anchor re-resolution) and `conformance/PASS-1/F-W0-CHECK.md` (14 defects, D-1..D-14). Every amended coordinate was re-read AT THE BYTES by the repair seat before it was kept; **one ruling coordinate was found stale and is minuted at row 10 rather than adopted** (`web/DESIGN.md` CVA row is at `:33`, not the ruling's `:34`). Re-validated against `docs/precepts/instructions/tranche/WAVE_SPEC.md` (present, 180 lines) per L-20. **Status stays `planned`; no gate was made green; no banked id was renamed; no record byte was touched.**

**Amendment minute — X·F L-20 repair round 2, 2026-08-28.** Amended under `conformance/PASS-2/RULINGS-2.md` (the two directives addressed to F-W0 — **R2-2.5**, the anchor-stability sentence at §4's head · **R2-10.8 / R2-8.2(iii)**, the F.W7 reciprocal cross-edge row — plus the **P2-10** path rider) and `conformance/PASS-2/F-W0-CHECK.md` (8 defects, D-1..D-8; **all eight cured**). Three round-wide laws bind this write and were applied to every byte it touches: **R2-1-LAW (QUOTE-BY-COMMAND)** — every quotation in this file is reproduced by a command run this round, and the register keying quote → home → command is **§3a**; **R2-2 (the anchor idiom)** — no citation written or rewritten here addresses a live sibling spec by line number; **R2-5** — the F.W1 roster's extent is quoted from its charter by anchor and never re-derived here. Running the quote law found and cured, across the round's two repair passes, **seven drifted or fabricated quotations no local attestation had caught — four against round 1's bytes, and three against this round's OWN first pass** (§3a's closing note keys them all). The first four — `fr-CanvasOverlayButton.md:52`/`:69` fused into one sentence under one attribution · `W3-button-ledger.md:93`'s quote form (single-quoted where the bytes are double-quoted) · `fr-PaperSearchInput.md:120` re-dated from the bank's *"at 7.0.0"* to *"at the adopted pin"* and elided with an em-dash rather than an ellipsis · `fr-MorphShapePreview`'s FR-MSP-12 coin-flip sentence published in a **third** form that matches neither of the two the record carries, with a bank figure replaced by a cross-reference inside the quotation marks. **All four are keyed in §3a with the command that convicted them.** **The three the second pass found are the harder class, because each was shipped INSIDE the cure for the class it belongs to**: a sentence attributed to **F-W7's gate G-F7-11 that F-W7 does not contain** — *a fabricated authority minted by the fabricated-authority repair, R2-1c one wave over*. ⟨**Round-3 correction, in place**: round 2 warranted that strike with a pasted `grep -c 'Genuinely owed' F-W7.md` → **0**. **That receipt was itself unrun** — pass 3 measured **1** — and it is struck here and re-warranted on live, count-free receipts at the round-3 minute below and at §3a Q20. The *conclusion* is unchanged and re-proved; only its warrant was forged.⟩ Plus **two ⟨cmd⟩ notes whose commands do not return the words printed beside them** (§3a Q1's quote spans a hard line break in COHESION; Q17's `[^.]*` halts at the period inside `F.W1`), shipped by the very pass that made the ⟨cmd⟩ note mandatory. **All three are struck or corrected, with the old command, its real output, and the reason stated at each site rather than swapped silently.**

▲ **This amendment was written in TWO sittings, and the minute says so** (E-3 honesty; the alternative is a masthead that misdescribes its own file). The first sitting was cut off mid-write by a wall and its partial writes were banked as such at `281a420a` (*"partial repair writes from wall-killed seats (resume pending; rulings cached)"*); this sitting resumed against the same two inputs and **verified the first sitting's claims at the bytes rather than inheriting them**. Two of the first sitting's assertions did not survive that check and are corrected here rather than left standing: it declared the `fr-PaperSearchInput.md:120` quotation cured when the drifted spelling was still in §6b, and it pointed at **§3a** as the quote register when no §3a existed in the file. **Both are now true statements** — the residue-6 quote is conformed to its bytes and §3a is authored — but they were false when first written, and a repair round whose whole subject is unreproduced attestation does not get to quietly true them up. **The convicted class of this pass is closed by measurement, including against this file's own prior repair seat.**

▲ **And then a SECOND repair pass ran, against the first pass's bytes — the minute says that too.** It re-executed **every** ⟨cmd⟩ in this file rather than inheriting any of them, which is how the three finds above surfaced: **all twenty §3a register commands were run**, seventeen reproduced EXACT, and the three that did not are cured at their sites. It also **overturned one of the first pass's judgement calls**: the five outbound `F-W1.md:276` cites the first pass consciously left for the Re-anchor seat are **converted here**, for the reason given in the hand-off note above (an outbound address in one's own file cannot be staled by another seat's line-shifts, and one file publishing two idioms for one address is not discipline). **The pattern across all three passes is now on the record and is this file's most useful output**: round 1 attested, round 2's first pass attested harder — and each time the *attestation* was the thing that failed, never the measurement. **A repair seat's own cures are the least-audited bytes in the file, because a seat that has just authored a rule reads its own compliance as given.** Only the third reading, which trusted nothing it had written, found the fabrication. **Status stays `planned` across all three passes; no gate was made green; no banked id was renamed; no record, fourier or sibling-spec byte was touched.**

**Amendment minute — X·F L-20 repair round 3, 2026-08-28.** Amended under `conformance/PASS-3/RULINGS-3.md` (the three directives addressed to F-W0: **R3-3.1** the flagship strike's three receipts re-derived honestly at all four sites · **R3-4.1** §3/§8 re-denominated over the corpus with the detector stated inline · **R3-3.9** the purge share) and `conformance/PASS-3/F-W0-CHECK.md` (12 defects, D-1..D-12, 2 HIGH). **`F-W3.md` §X.1-v4 landed FIRST this round (R3-1a) and is CONSUMED here**: it is cited, never re-derived, at the three rows of this file that touch the twin surface (§3 rows 11 · 15 · 33 — see each row's v4 cite).

▲ **The round-3 conviction, and why it is the worst of the four.** Rounds 1 and 2 each failed at *attestation* while measuring correctly. Round 2's second pass understood that, wrote it down, and then **forged the receipt that convicted the forgery**: the three commands it pasted to strike the fabricated G-F7-11 quotation (`grep -c 'Genuinely owed' F-W7.md` → 0 · `'each for its true reason'` → 0 · `grep -rln … waves/` → this file alone) return, live, the **opposite** — the phrase is in `F-W7.md` too, put there by F-W7's own round-2 write, which **preceded** this file's last write. The strike's conclusion survives and is re-proved below on honest receipts; its warrant did not. **Fourth pass, same class, one level deeper: the instrument built to detect fabrication became the fabrication surface.**

▲ **The structural cure, adopted whole: R3-3.10 — NO BANKED COUNTS.** *No ⟨cmd⟩ whose output is a COUNT of a live sibling spec, or of this file itself, may be banked here.* Sibling and self receipts state **classifications, set-membership, or `grep -o` word-output** — forms that survive the next edit of the file they describe. This retires, by law rather than by correction, the four receipts this file was proudest of: the three `Genuinely owed` counts, and the masthead's own `grep -c 'F-W1.md:276' F-W0.md` → 2 / `grep -o … | wc -l` → 3 counting-unit disclosure. **Every one of them was true when written and false when read**, which is the whole argument: a frozen count of a moving file is not a receipt, it is a timestamp wearing one. The corpus (`fr-*.md`, E-1 immutable) and this repo's committed history are the only operands a count may be taken over in this file, and every count below is over one of those.

**Amendment minute — X·F L-20 repair round 4, 2026-08-29. THE CENSUS FREEZE.** Amended under `conformance/PASS-4/RULINGS-4.md` (the directives addressed to F-W0: **R4-1.1** the §6b F.W7 reciprocal re-pasted from the true bytes · **R4-2.4** §7b's two-commands-one-base · **R4-3.3** §6a lock 11 re-based on the canonical · **R4-4.3** the round-3-minted reciprocal unwound · **R4-7.3** the "other 30" count-word · **R4-8.3** hash-pinned cross-file quotes · **R4-9.8** the canonical FR-NP-32 pair at four sites · **R4-10** the 56-row roster ⟨**57 at errata round 5, E5-1**⟩) and `conformance/PASS-4/F-W0-CHECK.md` (9 defects, D-1..D-9, 2 HIGH).

▲ **The structural change, and it is the largest this file has taken: THIS SPEC NO LONGER DERIVES A CENSUS.** Rounds 1–2 counted **82** over a check file; round 3 counted **84** over the corpus under a detector it published inline. Both are retired. **`conformance/CENSUS-CANONICAL.md` is the census of record** (R4-3's sharpened clause: *a check file is never an operand; now, additionally, a superseded census is never a denominator*), its **§2 F.W0 roster is 57 rows** ⟨*re-based at repair round 5: errata §0.4 **E5-1** homes `PP-GATE` here; read 56*⟩, and this file's duty under **R4-10** is to **book or cite each of the 57 — no more, and nothing derived**. The inline detector at §3's head is **struck whole**, not narrowed: PASS-4 D-5 convicted it for a missing second-position slash arm, and **R4-9.6 dissolves the question** by moving the atomiser into the canonical (**§0.1**, where `/` is protected exactly as `·` and position-restricted probes are abolished). A private detector inside a wave spec is now a **second census operand by construction**, which R4-10 forbids; the cure for a defective instrument this file should never have owned is to stop owning it. **§3b is the new roster map**, and it is the only census surface in this file.

▲ **The round-4 conviction.** Two HIGHs, and both are the same shape one layer past round 3's: **an identity that was never in the bank** (`fr-PaperSearchDropdown residue 12` — ⟨cmd⟩ `/usr/bin/grep -rn 'residue 12' fr-*.md` → ∅ across all 66; the real id is **`FR-PSD-CB`**, and the canonical homes it at **F.W3**, not here), and **a quotation of a moving sibling that drifted at the one word a standing law names** (§6b's bare `(M1)` where `F-W7.md:218` carries the canonical pair). Round 3 cleaned every corpus quotation and then minted a corpus *identity*; the instrument that would have caught it — resolve the (record, id) pair before counting it — is the one this file states and did not run on its own additions. **Under the canonical both die by construction**: an id absent from the canonical cannot be booked, and `residue 12` is absent from the canonical because it is absent from the corpus.

▲ **R4-8.3 — cross-file quotes carry the sibling's hash.** Every quotation this file takes from a live sibling now records that sibling's `shasum -a 256` first-12 **at quote time**, so a later rewrite is detectable rather than deceptive. ⟨cmd⟩ `shasum -a 256 F-W1.md F-W3.md F-W7.md | /usr/bin/awk '{print substr($1,1,12), $2}'` → **`F-W1.md` · `F-W3.md` · `F-W7.md` — pinned per PIN-PURGE-CERT.md §TABLE (2026-08-29); the three inline first-12 values are RETIRED by the R4-8.2 purge seat** ⟨**RE-RUN AND RE-PASTED BY THE PASS-4 TRULY-LAST PURGE SEAT, 2026-08-29 — AND THE RIDER FIRED ON ITS FIRST OUTING.** The round-4 write banked `83ffe83cb37f F-W1.md` · `a89c3386f3f8 F-W3.md` · `8b631aca0dc1 F-W7.md`. Re-run at the pass-4 settle, **two of the three no longer match**: F-W7 was re-written after F-W0 quoted it (`8b631aca0dc1` → `5cc3346db23e`), and F-W1 twice more (`83ffe83cb37f` → `a1302689aaa3`, the second time by this seat's own R4-2.4 and cross-wave cures at `F-W1.md:363/:372`). `F-W3.md` alone holds. **This is exactly the detection R4-8.3 was minted for, and it is the first mechanical proof the programme has that a same-round cross-file quotation went stale rather than merely being suspected of it.** All six quote sites in this file are re-pinned to the settled values above; the quoted WORDS at §6b, §7c and §8 were re-run against the new bytes by this seat and reproduce unchanged — only the fingerprints moved⟩, this seat, 2026-08-29. This is the fourth idiom the program has tried for cross-file receipts, and the first that is honest about the failure mode the other three shared: counts, classifications and word-output all describe a file that moves; **a hash describes the file that was read.** It goes stale too — but staleness is the *signal*, not the defect.

▲ **R4-2 — PORTABLE-COMMAND LAW.** Every ⟨cmd⟩ in this file executes on the pinned toolchain: `bash` + BSD `/usr/bin/grep` / `sed` / `awk`. No `-P`, no `\K`, no lookaround, no `.{n,m}` with m > 255, every shell variable defined in the block that consumes it, **one command one base**. §7b's archaeology bullet — the one site PASS-4 convicted (D-7) — is re-cut as two complete commands each carrying its own absolute base. **Two standing disclosures so a pass-5 seat need not guess.** ⟨1⟩ **Where a ⟨cmd⟩ writes bare `grep` / `sed` / `awk`, it means the BSD binaries at `/usr/bin/`**, and receipts added or re-run from round 4 forward spell the path explicitly. The hazard is an operator hazard the PASS-4 seat hit and disclosed: an interactive shell on this host may resolve `grep` to **ugrep**, which errors or returns false negatives on probes BSD grep answers correctly. ⟨2⟩ **Two `$`-bearing strings survive in this file and NEITHER is a command of this file's**: the `$F/**` row label, which is a **quotation of F-W5's own bounds table**, and `grep -c "F\.W7" $V/…/F-W0.md`, which is the **struck fabrication** §6b convicts by name. Both are quoted objects, not instructions; **this file publishes no ⟨cmd⟩ containing an undefined variable.**

**Status stays `planned` across all five passes; no gate was made green; no banked id was renamed; no record byte, no canonical byte, no fourier byte and no sibling-spec byte was touched by this seat.**

**Anchor-drift disclosure (round-2 D-7).** Repair round 1 shifted this file's §4 gate headings by **+12 lines**, staling the `F-W0.md:199` / `:204` / `:209` coordinates that `PASS-1/RULINGS.md` R-9 and the sibling specs published, and `F-W1.md`'s own F.W0 predecessor row still quotes one of them. Round 2 does **not** re-publish corrected line numbers — it **abolishes the address**: this file's gates are citable by **gate id only** (§4's head), and converting the sibling cites is the Re-anchor seat's act, which runs last in this round.

**Hand-off to the Re-anchor seat — narrowed to what F-W0 genuinely cannot fix (R2-2.3).** Round 2's first pass converted the four F.W1-roster sites it rewrote for other reasons and **deliberately left five more** (`F-W1.md:276`, twice paired with `:294`) for the Re-anchor seat, reasoning that an address-only pass over a file other seats are still editing is the failure mode round 1 shipped. **The second pass overturned that and converted all five** — the `web/package.json` bounds row at §2a, §3 row 14, §4 G-6's "GREEN (F.W1's half)" cell, the §6b *"gate fires inside the other wave"* row, and §7's `F.W0.d` unit row — each now reading **F-W1 §4 Sequencing, intra-wave step 4** ⊕ **§4's cross-edge 1**. **The reasoning that changed**: the round-1 hazard is seats *shifting each other's line numbers*, and an OUTBOUND cite in this file is immune to it, because abolishing an address cannot be staled by anything any seat does. Leaving five known-defective cites of a class already cured four times **in the same file** is not sequencing discipline — it is one file publishing two idioms for one address and inviting the reader to treat `F-W1.md:276` as live. Both anchors were verified at the bytes before conversion: ⟨cmd⟩ `awk 'NR>=250 && NR<=300 && /^## |^[0-9]\. \*\*/' F-W1.md` puts `:276` inside **§4 Sequencing** as **intra-wave step 4** and `:294` at **cross-edge 1**, and F-W1's own charter names the latter *"cross-edge 1 at this writing"*. **Stated as a CLASSIFICATION, not a count, per R3-3.10** ⟨round-3 correction: this cell previously banked `grep -c 'F-W1.md:276' F-W0.md` → 2 against `grep -o … | wc -l` → 3 as a counting-unit disclosure. Both figures were true when written; the `-c` figure is **false at these bytes** — this round's writes moved it. **A self-count is the one receipt guaranteed to be staled by the act of publishing it**, so the count is retired and the classification put in its place⟩: **every surviving occurrence of `F-W1.md:276` in this file is a masthead minute naming the address AS RETIRED — none is a citation of it.** The classifying receipt is ⟨cmd⟩ `grep -n 'F-W1\.md:276' F-W0.md`, whose every hit lands inside this masthead's amendment minutes and none in §2a, §3, §4, §6b, §7 or §8. **A disclosure of a dead address is not a citation of it**, and the governing unit here is CITATIONS, which is **∅** in the spec's body by every reading.

**What remains INBOUND is unreachable from here** ⟨round-3 re-cut, R3-3.10 + R3-9: this cell previously banked *"14 cites across 8 siblings"* under an **un-runnable command** (`F-W1.md F-W2.md … F-W10.md` — the `…` is not a path). Both halves failed: the command could not be executed as written, and the figure is **stale at the live bytes** — this round's sibling writes moved it. **RULINGS-3 dissolves the seat itself** (R3-10 row 2: *"there is no separate re-anchor seat this round"*; the purge seat certifies the class LAST, R3-3.8). So the count is retired and replaced by a **runnable set-membership receipt**, which is what R3-3.10 permits⟩: ⟨cmd⟩ `grep -lE 'F-W0\.md:[0-9]+' F-W1.md F-W2.md F-W3.md F-W4.md F-W5.md F-W6.md F-W7.md F-W8.md F-W9.md F-W10.md` → the sibling **SET** `F-W1.md` · `F-W3.md` · `F-W4.md` · `F-W6.md` · `F-W8.md` · `F-W10.md`, and ⟨cmd⟩ `grep -hoE 'F-W0\.md:[0-9]+' F-W1.md F-W2.md F-W3.md F-W4.md F-W5.md F-W6.md F-W7.md F-W8.md F-W9.md F-W10.md | sort -u` → the **address set** `F-W0.md:199` · `F-W0.md:204` · `F-W0.md:209` and no fourth — the +12 class this file's §4 head abolishes from the publisher's end. **Set-membership and word-output are stated because they survive the siblings' next edit; the cardinality is deliberately not stated, because it would not.** **F-W0 writes no sibling byte and does not convert them.** They take the gate ids §4 publishes. **Status stays `planned`; no gate was made green; no banked id was renamed; no record byte, no fourier byte and no sibling-spec byte was touched by this seat.**

**EXECUTION IS NOT AUTHORIZED BY THIS FILE.** Tranche X's execution gate stands: nothing opens, writes, builds, or runs product source until the owner's begin-word. Specifying is not doing. Status fields stay **planned**.

**Posture note (reconciled against F-W5 §1b — "Do NOT touch — every path below, without exception", the `$F/**` fourier-tree row).** ⟨Re-anchored at the round-2 close: the round-1 spelling *"F-W5.md §Preamble"* addresses a heading F-W5 does not have. ⟨**The warrant is re-cut at repair round 3 under R3-3.10**: round 2 banked `grep -c 'Preamble' F-W5.md` → **0**, a **count of a live sibling** — still true at these bytes, but true by luck, since F-W5's seat writes this round and any use of the word would flip it. **A negative that another seat can falsify by typing a word is not a receipt for a heading's absence.** The classifying form replaces it: the receipts below are `grep -o` word-output of the headings F-W5 **does** have, and they locate the law positively — an address that resolves needs no proof that a different address does not.⟩ The law lives in F-W5's §1b row for the fourier tree — ⟨cmd⟩ `grep -o 'Do NOT touch — every path below, without exception' F-W5.md` → *"Do NOT touch — every path below, without exception"* (the §1b heading) and ⟨cmd⟩ `grep -o 'the whole fourier tree, read-only law' F-W5.md` → *"the whole fourier tree, read-only law"* (that section's `$F/**` row). Both receipts are pasted prefix-free: F-W5 is a live sibling and a coordinate into it is not an address (R2-2). Cited by §-heading and row, never by line (R2-2).⟩ F-W5 declares `/Users/mkbabb/Programming/fourier-analysis` READ-ONLY *for itself* — correctly, because W5 is a doc-and-letter wave. **F.W0 is the wave that lifts that posture for X·F**, and only at the begin-word: this wave commits fourier bytes (the land-or-abandon minute's LAND set, a `git rm`, tsconfig flags, CLAUDE.md, INBOX, the substrate ledger). **It does NOT commit a manifest declare+lock transaction** — that limb moved to F.W1 at repair round 1 (R-4a); F.W0's `package.json`/lock act is G-1's per-path ruling and nothing else. Every later X·F wave inherits a *settled, committed* tree from here; none of them may write fourier bytes on the strength of F-W5's read-only law being "already lifted" — each wave carries its own authorization.

**Law**: M-25 (agglomerate, never transcribe — every adjudicated row lands as a gate, a bounds item, a named cure with its locks, or an explicit EXCLUDED line; silent drops are the defect class this program exists to kill). L-20 (validated against `docs/precepts/instructions/tranche/WAVE_SPEC.md`, present and current at authoring; re-validated at every amendment). L-19 (every gate names a real witness; no ornament). E-1/E-3 (adjudicated records immutable; corpus corrections land as addenda, never patches). Owner: LEAN, KISS, no contrivance, NO CRONS.

---

## 1. State

**Name**: W0 — Re-ground the substrate
**Half**: frontend (the CRUD/provenance half is SS-4 / F.W5–W8; §6b declares the non-dependency)
**Opens after**: tranche X's execution gate lifts (owner begin-word for the X·F lane). **No predecessor wave** — this is X·F's first *executing* file. Its inputs are complete: the adjudicated registry whole (66/66 fourier at `35fc8ebf`, COHESION §0a) + `docs/tranches/V/megatranche/audit/codex-provenance/INTAKE-ADJUDICATION-2026-08-03.md` + `docs/tranches/V/megatranche/audit/codex-provenance/intakes/lane-fourier-r3-r6.md` (**added at repair round 1, R-10.5** — the home file of the bare `R3-*`/`R4-*`/`R5-*`/`R6-*`/`X-*` rows G-14 must disposition and of row 4's bare `R3-7`; tree-verified, `:78`) + `docs/tranches/V/megatranche/formation/fourier/CENSUS-2026-08-03.md`.
**Agents**: 6 — **strictly serial** (§2b: unit *a* holds every path the other units need released).
**Hard gate**: 15 conditions, §4. **All 15 born-RED**, each with a named live witness — measured at the fold seat 2026-08-28 in `/Users/mkbabb/Programming/fourier-analysis` at branch `m/w1-bump-migration` @ `cd26c65`. **All fifteen gates were re-run RED by two independent adversarial seats at pass 1 and pass 2; both runs are named as PRIOR RUNS and neither is quoted as an operand here.** The gloss *"every clause EXACT"* stays STRUCK (round-2 D-3): three of the fifteen carried qualifiers at pass 1 — G-12's contradiction set was spot-checked, G-13's producer-pin clause was exact but for the hash, and G-15(d) had no witness of its own — and **the qualification is recorded, not measured, in this file.** ⟨**Re-cut at repair round 5 (P5-12 / R4-3).** Both receipts here took `conformance/PASS-1/F-W0-CHECK.md` as their operand and both reproduce; the defect is legal, not factual. **A check file is never an operand** (R3-4, restated program-wide by R4-3 **with no denominator qualifier**), and `CLOSE-CERT-2` §2 Class E cleared these two on a narrowing — *"attestation quotes, not rosters"* — **that the ruling's text does not contain**; R4-2.5 forbids the charitable reading. The two ⟨cmd⟩s are DELETED and the class claim stands in F.W0's own voice.⟩ A hostile register is cited for what it measured; it is never enlisted as blanket corroboration (porcelain 28 · CLAUDE.md absent · 3 worktrees · r4-sha evaporated · e2e = 8 · tsconfig 0-of-3 flags · dist delimiters 17/8 · five runtime devDeps · vaul-vue/@lucide/vue/embla in neither block and absent from the lock · installed glass-ui 4.0.0).
**Status**: **planned**

### 1a. Four-verb status

| verb | value | evidence |
|---|---|---|
| AUDITED | **YES** | **`X/fourier/conformance/CENSUS-CANONICAL.md` §2 (the stamped census of record — F.W0 → **57 rows** ⟨*re-based at repair round 5, errata §0.4 E5-1; read 56*⟩; added at repair round 4, R4-10)** · the 66 adjudicated `registry/adjudicated/fr-*.md` records (the EVIDENCE; no longer the roster) · `formation/fourier/{CENSUS,lane-frontend}.md` · `INTAKE-ADJUDICATION-2026-08-03.md` · **`coordination/`**`FOURIER-AUXILIARY-EIGHT-HOUR-SOURCE-DELTA-2026-08-03.md` (root corrected at repair round 2 — it does **not** live under `formation/fourier/`; see §7b) |
| SPECIFIED | **YES — this file, twice-authored + fold-agglomerated, 2026-08-28** | validated against `docs/precepts/instructions/tranche/WAVE_SPEC.md` (L-20, current at authoring) |
| IMPLEMENTED | **NO** | stamped at this wave's own close, when all 15 gates go green and the per-path minute exists |
| VERIFIED | **NO** | stamped only at X·F's sub-tranche release close; no wave stamps VERIFIED at its own close |

### 1b. Goal criterion

This wave succeeds if, when work ends, **every later X·F wave can quote a number, a line anchor, or a producer fact without re-deriving it — because the tree it points at is one a commit actually holds; no F.W1 budget rests on a byte no commit holds; no producer-owned defect is patched in the consumer; and every relay this lane owes has a seat it was sent from.** Today none of that is true: 27 modified paths and 1 untracked path carry the entire corpus's evidentiary base, so every `fr-*` record's "working tree at `cd26c65`" anchor names a tree no commit holds, and every per-file F.W1 budget rests on bytes `git stash` would erase. F.W0 settles the tree, publishes three tables (anchors, denominators, pins), lands the gates that would have caught an entire defect family, **AUTHORS the manifest gate and measures its born-RED facts for F.W1 to land** (R-4a — F.W0 does not band the manifest itself), and creates the mail seat every relay this program owes has been waiting on.

### 1c. What this wave is NOT

It does not perform the tri-package uplift (F.W1). It does not delete dead code (F.W3/W4) — it lands the *gate* that finds it. It does not decide the unit-test floor's scope (F.W9/W10) — it stands up the *seat*. It does not patch a producer defect in the consumer tree, ever (SS-6). It does not decide OG-F1. It claims credit for nothing another wave cures — the standing bar adopted from FR-GIG-5.

### 1d. Corrections to the wave brief (measured live, fold seat 2026-08-28; re-grounded at repair round 1 on in-tree authorities only, R-3)

1. **`web/dist` is NOT committed.** `git ls-files web/dist` → **0 files**; gitignored twice (`.gitignore:6` `dist/`, `:42` `web/dist/`); present on disk dated `Jun 12 18:13`. The brief's "stale *committed* web/dist artifacts" would send a `git rm` at nothing. Booked as an **on-disk stale-artifact trap** (§3 row 27).
2. **The e2e denominator is 8.** Stated positively, on in-tree authority only (repair round 1 — the earlier "retraction-of-a-retraction" framing rested on a ledger that does not exist and is struck): the **banked** figure is **8**, thrice-independently banked at `fr-AdminFlaggedPanel.md:110` ("no test script, no vitest, **8 e2e specs**"), `fr-CollapsibleSection.md:65` ("none of the **8 e2e specs** asserts any of the four section titles") and `fr-CanvasOverlayButton.md:74` ("`web/e2e` **8 specs**, zero references"); the live tree agrees — `web/e2e/` holds **8** `.spec.ts`: contour-extraction · gallery · paper-performance · settings-persistence · visual-baseline · visualization-crud · visualization-ux · workspace-flow. **Any 7-spec figure downstream is a superseded denominator and is FORBIDDEN** under G-12's own law. Booked as a G-12 denominator row (§4 G-12, G-8).
3. `/tmp/fourier-r4-files.sha256` **confirmed ABSENT** (evaporated since fold). `~/.codex/worktrees` confirmed **three**: `7e28`, `9167`, `d0be`.

---

## 2. Bounds

Paths: `value.js/` = `/Users/mkbabb/Programming/value.js` · `fourier/` = `/Users/mkbabb/Programming/fourier-analysis`.

### 2a. File bounds

| Path | Access |
|---|---|
| `value.js/docs/tranches/X/fourier/waves/F-W0.md` | **create** — this spec |
| `value.js/docs/tranches/X/COHESION.md` | **append-only addendum** (E-3) — §1 register row for the fourier mail-ledger surface + the §2 cross-edge, declared from this end |
| `fourier/CLAUDE.md` | **create** — measured ABSENT 2026-08-28 |
| `fourier/docs/tranches/F/coordination/INBOX.md` | **create** — the E13-analogue mail ledger. **Bounded at repair round 2 (D-8)**: the directory already holds **three letters** (`F-OPERATOR-WINDOW.md` · `F-T-N1-status-field-drop.md` · `F-VHOST-CORRECTNESS.md`, all 2026-05-29), which are **READ + TRIAGED INTO the ledger at creation** — see G-3. The letters themselves are **read-only**: the ledger records their state, it does not rewrite them. |
| `fourier/docs/tranches/F/SUBSTRATE-LEDGER.md` | **create** — the wave's single durable artefact: the 28-path minute · the STEP-2i divergence minute · the G-11 anchor table · the G-12 denominator table · the G-13 pin table + lattice · the stale-dist admissibility rule · the Codex dispositions · the four rulings (§4 G-15) · the recorded OPTIONs |
| `fourier/docs/tranches/N/valuejs-inbound-2026-07-27-facility19-migration-table.md` | **commit** — the O-14 letter, the sole `??` row |
| `fourier/docs/constellation/tri-tranche-run/RUN-BOARD.md` | ` M` — dispositioned; carries the J/K-deploy/M interlock + dead M.W1b glass-4.1.0 gate declaration |
| `fourier/web/package.json` · `package-lock.json` | ` M` — **the land-or-abandon bump hunk ONLY** (G-1, per path). **Re-cut at repair round 1 (R-4a): F.W0 does NOT land the manifest DECLARE+LOCK banding** — it AUTHORS G-6 and measures its born-RED facts; the declare+lock act lands inside F.W1's atomic transaction (**F-W1 §4 Sequencing, intra-wave step 4**, per **§4's cross-edge 1**). See §8. |
| `fourier/web/tsconfig.json` | **edit** — G-7 flags + G-8 scope |
| `fourier/.gitignore` | **read/rule** — `:6` `dist/`, `:42` `web/dist/` (the quarantine rule); `:43-44` tsbuildinfo (FR-IC-25's cold-CI mechanism) |
| `fourier/web/dist/` | **quarantine — AFTER G-4/G-5 green only** — on-disk 3.1.0-era residue dated Jun 12 18:13, UNTRACKED and gitignored (brief corrected: NOT committed; a `git rm` targets nothing) |
| `fourier/.github/workflows/ci.yml` (`:95`) · `deploy-pages.yml` (`:114`) | **edit** — the true enforcing gate (`npx vue-tsc -b --force`); anything F.W0 adds is wired here or it is an ornament (FR-IC-25) |
| `fourier/web/src/components/visualization/CanvasOverlayButton.vue` | **`git rm` target**, conditional on G-10's ruling |
| `fourier/web/src/components/equation/InfoCard.vue` | ` M` — F8-REACH-01 ruling target |
| `fourier/web/src/components/equation/EquationModeToggle.vue` | ` M` — **LAND** (FR-EMT-25, named exception) |
| `fourier/web/src/components/paper/search/PaperSearchDropdown.vue` | ` M` — **LAND** (C:S-2, named exception) |
| the **21** remaining ` M` SFCs (§2a-i) | **dispositioned by name; contents NOT edited by this wave** (execution gate). **Denominator corrected at repair round 1 (R-9.4) and published ONCE in G-12's own table**: 27 ` M` rows, of which **24 are SFCs** (RUN-BOARD.md · package.json · package-lock.json are not); minus the **three** named individually above this row (InfoCard · EquationModeToggle · PaperSearchDropdown) = **21 remaining**; minus the **two** named in the row below (EditorControlsDock, ConvergenceTimeline — lift sites) = **19 genuinely untouched**. |
| the 8 `:aria-pressed` lift sites (CanvasControlsDock ×5 · EditorControlsDock ×2 · ConvergenceTimeline ×1) | **edit — same commit as the `git rm`, conditional on G-10** |
| `fourier/web/e2e/` | **edit** — the axe keystone-route extension is *authored* here (not run); 8 `.spec.ts` live |
| `value.js/docs/tranches/V/megatranche/formation/fourier/CENSUS-2026-08-03.md` · `lane-frontend.md` | **ERRATA ADDENDUM ONLY** (E-3 — never patched in place) |
| `value.js/docs/tranches/X/fourier/conformance/CENSUS-CANONICAL.md` | **READ-ONLY — the census of record** (added at repair round 4, R4-10). §2's `### F.W0 — **57 rows**` is this wave's entire record-side roster; §3b maps it. **This wave writes no byte of it and derives no roster beside it.** A gate citing any other census operand is defective on sight (R4-3). |
| `~/.codex/worktrees/{d0be,7e28,9167}` | **READ-ONLY at spec time**; dispositioned at execution |

**§2a-i — the 28 paths, tree-derived and re-verified 2026-08-28** (`git status --porcelain` = 27 ` M` + 1 `??`; the enumeration below IS the bounds authority — it is derived from the tree, not adopted from any ledger, and it was reproduced item-for-item by the independent seat at `conformance/PASS-1/F-W0-CHECK.md` §3 G-1: *"EXACT — 28 rows, set identical to §2a-i, item for item"*): RUN-BOARD.md · web/package-lock.json · web/package.json · EquationModeToggle · EquationView · InfoCard · ConvergenceLegend · ConvergenceTimeline · HarmonicLevelGrid · MorphPhaseConfig · MobileFloatingToc · PaperView · PaperSearchDropdown · SliderControl · AnimationControls · BasisSelector · EditorControlsDock · EquationPanel · GalleryView · GlassTimeline · VisualizationView · AdminFlaggedPanel · AdminUserList · GalleryAdminBanner · GalleryCardModal · GalleryDraftsSection · GallerySearchBar · `?? docs/tranches/N/valuejs-inbound-…-facility19-migration-table.md`.

**Bounds derivation (repair round 1: tree-derived, R-3).** The bounds above are this spec's own enumeration of the live tree; **no ledger is their source**. Four rows carry corrections against the wave brief, each minuted: (1) the spec path is `F-W0.md`, the lane's live convention (`ls waves/` → 11 files); (2) `web/dist/` demoted from an act-target to a *quarantine* target with the not-committed correction (§1d.1: `git ls-files web/dist` → 0); (3) `web/e2e/` = **8** specs (§1d.2, banked ×3 + live `ls`); (4) `ci.yml`/`deploy-pages.yml` promoted from a fact-row to a **write** row, because G-7/G-8/G-9 are ornaments unwired (FR-IC-25). A fifth correction lands at repair round 1: (5) `web/package.json`/`package-lock.json` is a **G-1 land-or-abandon** row only — the manifest banding is authored here and lands at F.W1 (R-4a).

**Do NOT touch.** All product source outside the paths above — `fourier/api/**`, `fourier/web/src/**` except the named rows, `value.js/{src,api/src,demo,test}/**`. The 66 `registry/adjudicated/fr-*.md` records, `audit/codex-provenance/INTAKE-ADJUDICATION-2026-08-03.md` and `audit/codex-provenance/intakes/lane-fourier-r3-r6.md` (bounded here at repair round 1 so G-14 may disposition the bare `R3-*`/`R4-*`/`R5-*`/`R6-*`/`X-*` rows it names) — E-1/E-3 immutable beside the spec; they hold the witnesses, and rewriting one destroys the evidence a gate cites. **`fourier/docs/tranches/J/design/J-diff-shape.md`** — spelling corrected at repair round 1 (R-10.3/.5): the file exists ONLY at `/Users/mkbabb/Programming/fourier-analysis/docs/tranches/J/design/J-diff-shape.md` (tree-verified; the bare `docs/tranches/J/design/J-diff-shape.md` written here previously resolved nowhere under `value.js/`, and a negative bound must still name a real file). It is **F.W5's object — readable by this wave, never written by it.** `scripts/dev/dev.sh` in value.js — unowned dirty row, **never staged**. Producer trees (`glass-ui`, `latex-paper`, `parse-that`) — relay only; consumer patches are gate failures.

### 2b. Disjointness

Unit *a* holds all 28 paths — including `package.json`/`package-lock.json`, whose only F.W0 act is a's land-or-abandon ruling (R-4a moved the manifest LANDING to F.W1, so **unit *d* writes no manifest byte** and is now doc-only). Unit *f* (the rm + lift, incl. ` M` EditorControlsDock) writes paths *a* must release first, and *d*'s measurements must be taken after *a* rules the branch they are conditioned on. Therefore **serial, not parallel**: `a → b → e → c → d → f`. No other X wave writes any §2a path while W0 is open. Pathspec commits only; a `git add -A` is a wave abort.

### 2c. Worktree plan

**None — deliberately.** `git worktree add` from a 28-row dirty tree leaves the dirt in the original and produces a clean duplicate that is *not* the audited substrate — precisely the confusion G-1 exists to end. One writer, in place, on `m/w1-bump-migration`.

---

## 3. Carry

39 rows, agglomerated by identity, carrying the **CANONICAL F.W0 ROSTER — 57 rows, CITED, NEVER DERIVED HERE.** The census of record is `docs/tranches/X/fourier/conformance/CENSUS-CANONICAL.md`, **§2 PER-WAVE ROSTERS → `### F.W0 — **57 rows**`**, sha256 **`3e0a9acb3381`**, stamped by the pass-4 adjudication seat 2026-08-29 and re-based at errata round 5 (§0.4 **E5-1**); the roster map keying each of the 57 to its booking site is **§3b**, below. Under **R4-10** a wave's record-side roster *is* that section verbatim, and the wave's whole duty is to **BOOK each id or CITE its holder by name**; an id absent from both is this wave's escape, and **an id booked that the canonical does not home here is this wave's fabrication.** ⟨**Re-based at repair round 4 (R4-3.3 / R4-10; PASS-4 D-2, D-3, D-5).** Rounds 1–2 stamped **82** over a check file; round 3 stamped **84** over the corpus under a detector published inline. **Both are superseded.** The 84 was arithmetically defensible and structurally unlawful the moment a canonical existed: R4-3's clause is *"a check file is never an operand; now, additionally, a superseded census is never a denominator"*, and a per-wave re-derivation is exactly a rival census. It also carried a fabricated identity — `fr-PaperSearchDropdown residue 12`, ⟨cmd⟩ `/usr/bin/grep -rn 'residue 12' fr-*.md` → **∅ across all 66 records** — which the canonical rejects by construction because the corpus never held it. **The delta from 84 to 57 is not a correction of arithmetic; it is the withdrawal of an instrument.** ⟨*the round-4 figure was 56; errata round 5 E5-1 restored `PP-GATE` to this roster*⟩ The rows below are unchanged in substance: what changed is that their authority is now a stamped census rather than this file's own reading of the bank.⟩

**THE DETECTOR IS STRUCK — and the strike, not a narrowing, is the cure (R4-9.6; PASS-4 D-5).** Rounds 2–3 published a four-axis detector here so a hostile seat could re-run the census without this file's cooperation. PASS-4 convicted it: axis (ii) probed `F\.W0/(F\.)?W[0-9]+`, anchored to **first position**, and could not match `F.W1/F.W0` at `fr-ContourSettings.md:155` — the identical omission for which a sibling gate was re-cut, under a ruling whose own words are *"a gate stating anything narrower is DEFECTIVE at authoring, whatever it returns."* **The repair is not a fifth axis.** R4-9.6 moves the atomiser into the canonical — **`CENSUS-CANONICAL.md` §0.1**, where `/` is protected *"exactly as `·` already is"* and the position axis is abolished by rule rather than by probe — and R4-10 forbids a wave to derive, sample or re-cut a roster from any other source. **A private detector in a wave spec is a second census operand**, so the honest act is to delete it: this file's falsifier is no longer "re-run my four axes over the 66", it is **"difference §3b against `CENSUS-CANONICAL.md` §2's F.W0 roster"**, which a hostile seat can execute against a document this file cannot edit. ⟨The retired paragraph also carried the count-word **R4-7.3** names — *"the other 30 of the 66 contribute only preambles and tallies"*, where the measured split is **8** token-bearing-but-non-routing records and **22** carrying no `F.W0` token at all. It is not restated with corrected numbers; it is gone with the instrument that produced it.⟩

**0 silent drops** (§8 excludes LIMBS only, each with its home; every id the canonical homes elsewhere is CITED to its holder at §3b's second table, never dropped and never re-booked). **R** = routing; every row is a gate, a bounds item, a named cure with its locks, or an explicit EXCLUDED line. Line anchors quoted below are the records' own and are **re-resolved by G-11 before any of them is followed** (D-19: "a repair agent following D's anchors lands OUTSIDE the file"). Dedupe law: fr-AnimationControls C-1 ≡ fr-BasisCanvas C-8 (the `npm ls` ELSPROBLEMS identity, component-altitude INFO, reproduced ≥6×) and census risk-1 (the EBADPEER reproduction, banked ×3) are **cited, never re-opened**, by every row below that touches peers.

| # | Row (banked id) | Sev | Mechanism → cure, with locks | R |
|---|---|---|---|---|
| 1 | **FR-NP-32 ≡ fr-PaperSidebar M1** ⟨fr-NotationPills · fr-PaperSidebar (independently derived by postcss) · instruments at fr-HarmonicLevelGrid, fr-CanvasOverlayButton⟩ | BLOCKER | Adopted glass-ui 4.0.0 `dist/styles/index.css` is a **CSS syntax error**: the emitter injected the AN.W1 fold-block at the first *literal* `@source` occurrence — a prose mention inside a comment — so `:195-203` terminates the comment early and `:203-221` is un-commented prose whose apostrophes open an unterminated string. `web/src/style.css:3` carries the live `@import "@mkbabb/glass-ui/styles"` (re-read at fold). **Banked disposition, verbatim (`fr-NotationPills.md:35`): *"NO-WAVE-OWNER (producer dist emitter) + glass-ui BH relay at the TOP of the FR-COB-28 packet; F.W1 SEQUENCING GATE."* **THE R-8 RE-HOME IS WITHDRAWN AT REPAIR ROUND 4 (R4-10), AND THE ROW IS UNCHANGED BY THE WITHDRAWAL.** Rounds 1–3 declared that this file *performed* an F.W0 re-home of `FR-NP-32`. The canonical rules otherwise — `CENSUS-CANONICAL.md` §1 homes **`FR-NP-32` → F.W1** (legs `NO-WAVE-OWNER`), which is the record's own terminal cell read straight — and under R4-10 **an id booked at a wave the canonical does not home there is that wave's fabrication.** So `FR-NP-32` is **CITED to its holder F-W1**, not booked here. **What is booked here is `fr-PaperSidebar M1`**, which the canonical DOES home at F.W0 (§2, `fr-PaperSidebar (3): M1 · C-m9 · M7`) — one identity, two banked ids, and the F.W0 half was always the one this row owns: the pre-gate (G-4) + relay item 0. **The canonical form is unchanged and now mandatory at every site (R4-9.8): FR-NP-32 (≡ fr-PaperSidebar M1) — cite both, NEVER substitute either for the other.** Any later wave that needs "an F.W0 pre-gate" cites THIS row rather than asserting it bare.** Relay ask, quoted at the bytes ⟨**elision cured at repair round 3 (R3-3.9; PASS-3 D-4).** Rounds 1–2 closed the parenthetical at *"dist untracked at every tag)."* under a **verbatim** label; ⟨cmd⟩ `grep -cF 'dist untracked at every tag).' fr-NotationPills.md` → **0**. The bytes carry two more words. §3a's Q2 command does return the line, but the words displayed beside it were a **non-contiguous subset** of its output — the exact failure §3a's own convention (1) defines and claimed to have closed, occurring in mirror inside the register that closed it. ⟨cmd⟩ `grep -o 'NOT a version bump[^)]*)' fr-NotationPills.md` → *"NOT a version bump (no v8 dist exists to read: local dist is a 1,514-byte stub, dist untracked at every tag — my reads)"*⟩: *"the emitter fix AND a producer gate that parses its own published `exports[\"./styles\"]` — NOT a version bump (no v8 dist exists to read: local dist is a 1,514-byte stub, dist untracked at every tag — my reads)."* Reader-1's necessary-and-sufficient patch test adopted (repair ONLY the comment → 437,303-byte OK output). **Consumer must NOT patch locally** (FR-COB-8 S-4). Decides banked fr-CollapsibleSection M-7 negative; corrects banked K-5. | **G-4**, relay item 0 |
| 2 | **GAB-13** ⟨fr-GalleryAdminBanner (worker-LC missed; DU concurring at INFO; ruled MAJOR) · FR-COB-10 · fr-GalleryDraftsSection m-18 · FR-EQR-33(c)⟩ | MAJOR | **The substrate is uncommitted.** 28 dirty rows incl. `web/package.json` (HEAD pins glass-ui ^3.1.0, keyframes ^2.2.0, value.js ^0.10.0) — so every per-file budget and the "resolved producer is 4.0.0" premise rest on bytes `git stash` would erase. **Land-or-abandon BEFORE any budget commits; per-path, never wholesale** (27 ` M` + 1 `??`, each dispositioned by name and minuted) — a silent `git stash`/reset is forbidden because two dirty lines are the *correct repair* (rows 24, 25). Kills D §9's byte-identity certification (K-9). *Dissent carried verbatim (worker-DU)*: "the fleet has already elected to treat the working tree as the scope" — see §5's OG-F1 interaction. | **G-1** |
| 3 | **FR-COB-1 (= D-2/L-1/C-1 = F8-REACH-02)** ⟨fr-CanvasOverlayButton · `FOURIER-AUXILIARY-EIGHT-HOUR-SOURCE-DELTA-2026-08-03.md:31`, N.C39, HOLD is RED⟩ | BLOCKER | Dead module: zero consumers, unreachable by **all four** resolution channels (static grep 0 repo-wide excl. docs · dynamic = 3 string-literal `defineAsyncComponent` at GalleryView.vue:31-33 · router = 7 literal lazy records · no unplugin-vue-components), under **five verbatim DELETE orders never executed** (W3-button-ledger.md:93 · M.md:141 · M-design-language.md:70 · M-bump-migration.md:56/:199 STEP 2i · A8-no-legacy-sweep.md:29), skip self-reported at `partial-prior-run.json:207-211`, un-minuted. Orphaned at `2f53d5d` (2026-03-16) = 145 days, two maintenance passes. **Disposition is LIFT-THEN-DELETE, never a bare `git rm`** — "a bare `git rm` converts a paper defect into a live a11y regression." Byte identity `35f3db91…5efd` / 595. *Dissent preserved verbatim (L-1)*: "if F.W0 rules ABANDON on the M.W1a tree wholesale, the governance limb falls to MAJOR; the deadness does not." | **G-10** |
| 4 | **FR-IC-1 (= F8-REACH-01) ∘ FR-COB-10 ∘ intake R3-7** ⟨fr-InfoCard (r2-sustained) · fr-CanvasOverlayButton · intake **R3-7** at its real home `docs/tranches/V/megatranche/audit/codex-provenance/intakes/lane-fourier-r3-r6.md:78` — TRUE / **ADOPT-AS-FACT**, `sourceWorkflowTotal 66 / Reachable 64 / Unmounted 2`, the two unmounted workflows never named there. **Home bounded at repair round 1 (R-10.5)**: `INTAKE-ADJUDICATION-2026-08-03.md` carries only R3-7a/b/c, so the bare R3-7 row was cited to a file that does not hold it; the row lives in the intakes file now named in §1 and §2a⟩ | MAJOR (fact) / BLOCKER-adjacent by demand | Intake R3-7's two **anonymous** unmounted workflows are NAMED: `CanvasOverlayButton.vue` + `InfoCard.vue`. The 66-SFC zero-inbound sweep re-derived independently three times, closed a seventh way (untracked `web/dist` carries the twin's `"% energy"` in 1 file, InfoCard's unique `"energy captured"` in 0). **F8-REACH-01 already DEMANDS the InfoCard ruling one row above F8-REACH-02 in the same table — uncited by all five corpus documents and both readers (R-6).** Not "undisposed": disposed-in-demand and unexecuted. InfoCard accrues cost now (the uncommitted `:amount=`→`:value=` edit, ` M` re-verified at fold). **F.W1 rider**: the lane-frontend `./metric-badge` budget is **5 live files + 1 unmountable**, not "7 imports / 6 files" (FR-COB-10; FR-IC-8's restatement). FR-IC-23's one-edit law binds only KEEP/HARNESS (FR-IC-2 + FR-IC-3's amount limb cured as ONE edit); under DELETE, FR-IC-2 retires unmeasured — correctly. | **G-10**, F.W1 rider |
| 5 | **FR-NP-2 (= L·D-1)** ⟨fr-NotationPills⟩ | BLOCKER *(audit substrate, not the component)* | R6's `loops` row ships `cardinality: 18` for a **3-element array** under the label `FINITE_ARRAY_LITERAL_SOURCE_DERIVED`; the comma-counter reconstruction predicts 18/15/7 across all three array rows — a source-derived label over a fabricated number. **Binding rider: every F.W4 consumer treats array-literal `loops[].cardinality` as FABRICATED.** No later wave may quote a Codex/deriver cardinality without re-deriving it. | **G-12** |
| 6 | **FR-NP-4 (= L·D-2)** ⟨fr-NotationPills⟩ | MAJOR (substrate) | Deriver coverage is 89/89 on Button but **396/512 leaves unresolved** — the honest label is what bounds it at MAJOR. Publish the resolved/unresolved split beside the anchor and denominator tables so no wave quotes a 512-denominator percentage against a 396-resolved numerator. Rides intake X-9 ("publish ONE member-scope law before any percentage") — F.W0 publishes the law, F.W4 consumes it. | **G-12** |
| 7 | **MISS-A7 (+ ruling-6 registry-coherence rider)** ⟨fr-PaperSearch · banked at fr-PaperSearchInput K12 / residue 6 · **`fr-PaperSearchDropdown` `FR-PSD-CB` — CITED, not booked** ⟨**the phantom `residue 12` is STRUCK at repair round 4 (R4-10; PASS-4 D-2, HIGH).** Rounds 3's re-denomination minted it as one of two additions carrying 82 → 84 and PASS-3's check certified it as resolving at its cited home. It resolves nowhere: ⟨cmd⟩ `/usr/bin/grep -rn 'residue 12' fr-*.md` → **∅ across all 66 records**, and ⟨cmd⟩ `/usr/bin/sed -n '112,120p' fr-PaperSearchDropdown.md` shows that record's SS-13 residue section holding **exactly one** numbered item, `:114`, whose banked identity is **`FR-PSD-CB`**. **The routed row was always real; its id was invented.** The canonical homes `FR-PSD-CB` at **F.W3** (`file-criterion → §5.c`, legs `SS-13`) and gives this record exactly **one** F.W0 row — `C:S-2`, booked at row 25 — so under R4-10 the correct act is a citation to F-W3, not a booking here. The lesson is the one this file states and did not run on its own additions: **resolve the (record, id) pair before counting it.**⟩⟩ | MAJOR (substrate) | `npm run build` is unrunnable at the pin (vite-8/manualChunks) — **silently disabling every build-based falsifier in all three challenge files across the whole family**. Second, independent break from row 1. **Ruling-6 rider, booked verbatim**: *"when MISS-A7's build unblock lands, run the build, grep the emitted CSS for `class*=size-`, and correct whichever banked cell lost — booked here so the contradiction cannot outlive the evidence."* ⟨**E-3 RE-ROUTE, added at repair round 4 (PASS-4 D-6).** The rider's *"correct whichever banked cell lost"* is quoted forward from a frozen record and, taken literally, asks for an edit to a `registry/adjudicated/**` file — which §2a declares **E-1/E-3 immutable** and which §7a lists verbatim as a *"bounds expansion that invalidates the wave"*. Everywhere else this spec is scrupulous about that boundary (CENSUS and lane-frontend take **ERRATA ADDENDA, never in-place patches**, three times over); here the rider was carried through three rounds without the re-route it needs. **The correction lands as a DATED E-3 ADDENDUM naming the losing cell and its superseding measurement — never as a patch of the record.** The bank's instruction is honoured in substance and the immutability law is not bent to do it.⟩ The registries **disagree**: fr-PaperSearch K1 kills the guard ON EMISSION; fr-PaperSearchModal PSM-24/R-3 books it LATENT-on-next-build. Both cannot survive the first real 4.0.0 build. Method note (K12): npm ≥11 re-satisfies the peer on install, so **the correct manifest probe is `npm ci --omit=dev`, not `npm rm X && npm run build`**. | **G-5** (gated behind G-4), **G-15(d)** |
| 8 | **MISS-LC2** ⟨fr-PaperSearchInput, reader-LC; re-verified end-to-end by the seat's lock scan; lock re-grepped at fold: 0 `@lucide/vue` entries⟩ | MAJOR | **The audited tree is not reproducible from its own lockfile.** glass-ui 4.0.0 declares `@lucide/vue ^1.16.0` a REQUIRED peer; the lock *records the requirement* inside the glass-ui entry yet contains **no `node_modules/@lucide/vue` package entry**; on-disk sits 1.20.0 — unlocked, unmanifested, in neither dep block. `createLucideIcon` is reached from dock/select/configurator/dropdown-menu/collapsible/tabs — subpaths this app imports — so a faithful `npm ci` yields module-eval throws. "Third member of the C-3/C-8 misfiling class, and the worst: a required peer filed NOWHERE." | **G-6**; manifest lands with F.W1 |
| 9 | **FR-EQC-7 (= C-1, re-provenanced per R-4)** ⟨fr-EqCoefficientsPanel⟩ | MAJOR (F.W1-commit-conditional) | The uncommitted M.W1a lock regeneration **dropped `vaul-vue`** — a required glass-ui peer statically imported by the root barrel (`dist/glass-ui.js:60`) reachable through 7 barrel edges incl. `router/index.ts:2` (every route's eager graph) and `CollapsibleSection.vue:2` — while the **committed** lock carries `vaul-vue@0.4.1` and `web/node_modules` retains it. **Cure: declare + lock `vaul-vue`, OR land the barrel→subpath edge retirement first** (`./collapsible` pulls no vaul-vue — C's own contrast cell). **This gate does not exist standalone: it is F.W0-authored and FIRES inside F.W1's atomic transaction.** Riders: @lucide/vue non-edge (reader's kill, adopted); optional-peer scoping carried. *Dissent carried*: the BLOCKER framing was KILLED and EXTENDED — the defect is **working-tree-only**; "a clean CI checkout fails today" is FALSE; it fires the day the bump lands. | **G-6** → fires in **F.W1** |
| 10 | **FR-AH-33 (banked manifest-banding identity)** ⟨fr-AppHeader (= C-14, the banked home); folds by reference, never re-booked: fr-BasisSelector m-9 · fr-CollapsibleSection F-2 · FR-AFP-54 · FR-AUL-56 · fr-ConvergenceTimeline M-6 · FR-EQR-29 · fr-EquationView C·D-27 · FR-GFC-21 · FR-GSB-31 · fr-PaperSidebar C-m9 · fr-ContourSettings C-13 · FR-EQC C-3 · fr-PaperSearchInput C-3⟩ | MINOR (fatal-conditional: MAJOR under `--omit=dev`) | `lucide-vue-next`, `reka-ui`, `class-variance-authority`, `clsx`, `tailwind-merge` sit in **devDependencies** while reached by shipped source (dep blocks re-read at fold — unchanged) — and vite's production `vendor-ui` chunk (`vite.config.ts:52`, keyed on the package root) names lucide, so the manifest misdescribes the graph the build config asserts. All five are `"dev": true` in the lock ⇒ **`npm ci --omit=dev` fails TODAY.** **Mandatory same-act correction (ruling 6):** `cva`/`clsx`/`reka-ui` are LIVE runtime peers on the button-chunk→`cn`→clsx chain while **`lane-frontend.md:70` and `web/DESIGN.md:33` book them for deletion as "dead devDeps"** — correct the doc contradiction in the same act **or F.W1 deletes live peers**. ***Anchors re-resolved at repair round 1 (R-1f); the `:69` description CORRECTED at round 2 (D-2)***: ⟨cmd⟩ `grep -n 'lucide-vue-next' lane-frontend.md` → **`68:| `lucide-vue-next` | `latest` | `^1.0.0` | 1.0.0 | glass-ui 7 peers …`** — the `lucide-vue-next` table row is at **`:68`**, and ⟨cmd⟩ `sed -n '69p' lane-frontend.md | od -c` returns `0000000 \n` — **`:69` is a BLANK LINE**. The round-1 minute's sentence *"`lane-frontend.md:69` is a table row for `lucide-vue-next`"* is **STRUCK as false at the bytes**: it was published inside this file's own re-resolution minute, which is the model later waves are told to copy, so it is corrected here rather than quietly dropped. ⟨cmd⟩ `grep -n 'Dead devDeps' lane-frontend.md` → **`70:`** — the "**Dead devDeps (measured, not estimated)**" paragraph is at **`:70`**, unchanged and correct, and the gate condition resting on it never moved. `web/DESIGN.md:32` is *"Replace `.gallery-card`/`.modal-card` divs with glass-ui Card"*; the CVA row (*"Remove unused CVA dependency…"*) is at **`:33`** — `grep -n 'CVA dependency' web/DESIGN.md` → `33:`, this seat, live. **The ruling cited `:34`; the bytes say `:33`, so the ruling's LAW (re-resolve the anchor) is applied to the live coordinate and the divergence is minuted here rather than silently adopted.** The stale `:32` is *inherited* from `lane-frontend.md:70` and `:645`, which both quote it — so the errata addendum corrects three sites, not one; **this is a D-19 instance inside a gate condition, and G-11's re-resolution law reaches gate conditions too, not only §3 record quotes.** `tailwind-merge` **alone** is genuinely dead (doc-comment-only; C's precision sustained = C-5's narrowing of §9 carry #10). LC adopted: 4.0.0's `peerDependenciesMeta` does **not** mark `@lucide/vue` optional. The `@lucide/vue` rename itself is F.W1 (one of 35 censused sites; a CURRENT debt, contra lane-frontend §1 row 13). *Both readers OVERRULED on MAJOR grading (fr-ContourSettings R-7) — the banked altitude law wins; DU's "nine → eleven runtime deps" correction adopted.* | **G-6** |
| 11 | **FR-GFC-7 (manifest limb only)** ⟨fr-GalleryFeaturedCarousel = D-6 / C-8, ⊕ D-7 rescoped per R-2⟩ | MAJOR (row) / one-line at this limb | The Embla peer trio is already hoisted in `web/node_modules` while **`embla-carousel-vue` is undeclared** (re-verified at fold: absent from package.json). **Re-graded at repair round 1 (F-W0-CHECK D-13, re-measured by this seat at `web/node_modules/@mkbabb/glass-ui/package.json`): 4.0.0's `peerDependenciesMeta` marks `embla-carousel-vue` OPTIONAL** — alongside `@vueuse/core`, keyframes.js, pencil-boil, value.js, perfect-freehand, tw-animate-css — while the **required remainder is SEVEN, not six** ⟨**enumeration completed at repair round 3 (R3-3.9; PASS-3 D-11)**: rounds 1–2 listed six and dropped `vue` itself. Immaterial to the gate's outcome — `vue` is declared and installed — but the cell is **written as an enumeration**, and an enumeration that is one short is a defect at whatever altitude it sits, particularly one this row uses to argue which peers may lawfully be folded into G-6's land-together set⟩ — ⟨cmd⟩ this seat, `node -p` over `web/node_modules/@mkbabb/glass-ui/package.json`: **14 peers, 7 optional** (`@vueuse/core` · `embla-carousel-vue` · `@mkbabb/keyframes.js` · `@mkbabb/pencil-boil` · `@mkbabb/value.js` · `perfect-freehand` · `tw-animate-css`), **7 required** (`@lucide/vue` · `class-variance-authority` · `clsx` · `reka-ui` · `tailwindcss` · `vaul-vue` · **`vue`**). **Embla therefore cannot fail `npm ci --omit=dev`, and it is REMOVED from G-6's land-together set**: folding an optional peer into an all-or-nothing transaction with two required peers over-binds it, and F.W0 would be declaring a dependency with zero importers (row 11's own "do NOT pull the adoption forward" forbids the importer). The declaration is booked as a **one-line convenience limb riding the F.W3/W4 adoption**, not a G-6 operand. **Do NOT pull the adoption forward** — free-scroll+snap vs Embla's transform track is a behaviour change. (The row's body — four anonymous divs, no landmark/heading/carousel semantics against a shadowed `./carousel` shipping `aria-roledescription` + arrow handling + Prev/Next at both pins — routes F.W3/W4, **and the twin surface RESOLVES that dual at F.W3**: `F-W3.md` §X.1-v4, the definitive file-criterion partition landed FIRST this round under R3-1a, holds `fr-GalleryFeaturedCarousel` on its **F.W3 `.d`** row (**`F-W3.md` pinned per PIN-PURGE-CERT.md §TABLE (2026-08-29)**). **Cited, not re-derived** — F.W0 takes v4's answer and does not re-run the criterion.) | **G-6 measured fact, NOT a G-6 operand** (optional peer); declaration limb → **F.W3 `.d`** with the adoption, per `F-W3.md` §X.1-v4; body **EXCLUDED** → F.W3 `.d` |
| 12 | **FR-AFP-55** ⟨fr-AdminFlaggedPanel, superseded-draft carry; peers re-read at adjudication⟩ | MINOR (sizing-bearing) | Installed glass-ui 4.0.0 peer-declares `@mkbabb/value.js ^0.10 \|\| ^0.11` against the app's newer pin, and `@lucide/vue ^1.16` against the imported `lucide-vue-next` — **the "clean pin" premise of tri-bump sizing understates the start state.** Publish the true start state (peer mismatches enumerated) as an F.W1 sizing input. Must not re-book fr-AnimationControls C-1; the increment is the *sizing consequence*, not the fact. | **G-6** + **G-13** |
| 13 | **C·D-11** ⟨fr-EquationView⟩ | MAJOR (sequencing fact) | The glass-ui→value.js peer edge is unmet today (installed 0.13.0 vs peer `^0.10 \|\| ^0.11`, optional ⇒ EBADPEER warning not failure) — and **the 0.13.0 pin is held from BELOW**: keyframes.js@4.3.0 hard-deps `value.js ^0.13.0` + optionalDeps `glass-ui ~4.0.0` (all three package.json facts node-verified). **The backward pin is the genuine increment — it is what makes F.W2 un-landable alone.** F.W0 publishes the **pin lattice** (who holds whom, in which direction) as a named input to F.W1's atomicity argument. The EBADPEER reproduction folds to census risk-1 (banked ×3) — do not re-book. | **G-13** → F.W1/F.W2 |
| 14 | **FR-EQR-30 (= C-10, re-pinned by the seat)** ⟨fr-EquationResult⟩ | INFO | `@types/katex@^0.16.8` is inert against self-declaring `katex@0.17.0` under `tsconfig.json:5` `moduleResolution: "bundler"`. The FACT survives; **every `tsconfig.app.json` citation in the corpus is DEAD** — web/ holds exactly one `tsconfig.json` (13 compilerOptions), re-verified at fold. The corpus citation correction is **F.W0's** (a G-12 denominator row + the errata addendum); the devDependency removal is a **limb of F.W1's transaction** (**F-W1 §4 Sequencing, intra-wave step 4**), authored by G-6 here and landed there (R-4a). **F.W0 consumes THIS pin, not C's.** | **G-6** (authored) + **G-12** (landed here) |
| 15 | **MG-θ (banked toolchain-gate identity)** ⟨fr-App MG-θ (reader-LC miss, adopted — the banked home); folds by reference: fr-PathPreview **PP-DEAD** (`fr-PathPreview.md:30` — *"MINOR · FOLD → F.W3/W4 … **+ F.W0 `noUnusedLocals` rider**"*; **id restored at repair round 1, R-2c** — the F.W0 limb is the rider, its instance is the dead `PathPreview` import at `GalleryCard.vue:10` named in this row's list below; **the delete-or-complete fork is F.W3's — the dual RESOLVES there**, per `F-W3.md` **§X.1-v4** (the definitive file-criterion partition, landed FIRST this round under R3-1a; **`F-W3.md` pinned per PIN-PURGE-CERT.md §TABLE (2026-08-29)**), whose **F.W3 `.d`** row holds `fr-PathPreview`; **cited, never re-derived here** — F.W0 owns only the `noUnusedLocals` rider and takes v4's answer for the rest, per §8) · fr-PathPreview PP-GATE · FR-GFC-22 rider · fr-GalleryCard L·D-3 (VIZ_COLORS half) + K-13 · fr-GalleryMarquee GM-F6 · fr-GalleryInfiniteGrid L-8 · fr-GalleryView K7 · FR-CP-32 · fr-CollapsibleSection i-3 (=C-17) · FR-USB-21 · fr-PaperSidebar M7 · fr-EquationPanel M-N14 rider⟩ | MINOR (O(1) fix behind an entire defect family) | **The dead-code family has no gate that could ever catch it**: no `noUnusedLocals`/`noUnusedParameters` in `web/tsconfig.json` (0-of-3 flags, re-grepped at fold), no lint config, no `lint` script. Instances it would have caught: GalleryCard's two dead imports (`VIZ_COLORS` :9, `PathPreview` :10 — **the latter is PP-DEAD's F.W0 instance, id carried above**) · the unread `observer` · `workspaceStore` · 24 dead transition lines · the dead green write · SvgFilters whole · FR-CP-32's unused `onUnmounted` + guard-only local + unreachable `\|\| "#888"` · the wholly dead 4-line barrel `web/src/lib/equation/index.ts` · **34 `catch (e: any)` sites in web/src** (FR-USB-21, corrected UP from "≥20"; `vue-tsc` cannot see `: any`). Three axes reported instances; **none named the gate.** fr-CollapsibleSection i-3 rider, binding: formatting diverges intra-file and no formatter exists — **do NOT fix by hand; land the gate, then let it drive.** | **G-7**; deletions **EXCLUDED** → F.W3/W4 |
| 16 | **i-3 (fr-BasisSelector, = LC-missed-7)** ⟨folds: fr-GalleryCard L·D-6 · fr-ContourPreview 28 · PP-LEN rider · FR-GSB-6⟩ | INFO (gate-flag decision) | `noUncheckedIndexedAccess` is absent from the 13 compilerOptions — **that flag, not the loose `Record`, is what lets `basisDisplay["fourier-epicycles"]` type-check non-optional and keeps M-10's two-domain key space invisible to `vue-tsc`.** **Rider ON M-10, never landed alone**: M-10's repair unit is `BasisKey` + `normalizeBasisKey` + `satisfies` at F.W3/W4; the flag makes it enforceable. **WAVE-LOCK (fr-GallerySearchBar ruling 5): no `basisFilter` wiring without the normaliser** (the store repeats the widening at `gallery.ts:38`). Do not over-credit the flag: PP-LEN's asymmetric failure (X-longer → literal `NaN`; Y-longer → a syntactically perfect, silently mis-framed path) is catchable **only by a unit test**. | **G-7** (same tsconfig edit) |
| 17 | **PP-TSSCOPE** ⟨fr-PathPreview, reader-B missed find, adopted⟩ | MINOR | **The only type gate never checks its own test suite or build config.** `include` = `["src/**/*.ts","src/**/*.d.ts","src/**/*.vue","env.d.ts"]` (re-read at fold), exactly one tsconfig in `web/`; `vite.config.ts`, `playwright.config.ts` and every `e2e/*.spec.ts` sit **outside `vue-tsc -b` entirely** — a SCOPE gap all three axes missed while litigating the FLAGS gap. Cure: extend `include` (or add a second project), **or minute the exclusion as a ruling**. Instrument-only precedent disclosed honestly: fr-CanvasOverlayButton observed the glob at `tsconfig.json:19`; no banked row books the CONSEQUENCE — this one does. **Denominator: 8 specs — banked ×3 (`fr-AdminFlaggedPanel.md:110` · `fr-CollapsibleSection.md:65` · `fr-CanvasOverlayButton.md:74`) and live-measured at the fold seat; a 7-spec figure is a superseded denominator, FORBIDDEN downstream (§1d.2).** | **G-8**; count → **G-12** |
| 18 | **PP-NOTEST (= L-13) ≡ R-20 ≡ fr-App C-13 ≡ fr-PaperSidebar M7 (test limb)** ⟨+ fr-EasingPicker C/m-6 · fr-GalleryInfiniteGrid L-12⟩ | MINOR (F.W0 seat) / decision-bearing (F.W9/W10) | **No vitest/jest anywhere in `web/`** (`scripts` = dev/build/preview/test:e2e/test:e2e:ui; vitest in neither dep block — re-verified at fold). Probe-C1b-class defects (valid-and-wrong output, e.g. PP-LEN) are catchable by nothing else. **R-20's rider**: the only e2e touching SliderControl asserts `canvas.toBeVisible()` and its `input[type="range"]` fallback **can never match** (reka renders spans; `VisuallyHiddenInput` is gated on `name`, never passed) — and Playwright `fill()` dispatches ONE input event, so **even an assertive spec cannot reproduce R-2; the gate must be a component test.** fr-EasingPicker: the sole covering e2e is `test.fixme` at `:192` whose unblock condition is **stale by two majors** against `^4.0.0`. fr-GalleryInfiniteGrid: a 139-line spec with **zero paging-vocabulary hits**. **F.W0 owns the SEAT — nothing more.** | **G-9**; FLOOR **EXCLUDED** → F.W9/W10 |
| 19 | **R-3 axe-keystone rider (fr-SliderControl, = D-4) + D-i1 (fr-ContourSettings)** ⟨D-i1 folds to fr-BasisSelector B-3's fixme census⟩ | MINOR (rider on a MAJOR) | The axe keystone set **does not reach `/equations`** — the only route where SliderControl's `subtitle` renders (gap verified by both readers). Separately, the only a11y gate over ContourSettings is a `test.fixme` resting on a premise FALSE at the installed pin. **F.W0 lands ONLY the keystone-route extension + the corrected justification text** — the prose must be corrected to the **4.0.0 `inert` truth** when re-armed. Un-`fixme`-ing B-3 at `:133` is a **four-test operation routed F.W3/W4** — not landed on a tree F.W1 is about to move. Re-arming is also how B-6/M-1/M-2 would have been CI-caught (all three axes; ratified). | **G-9**; un-fixme **EXCLUDED** → F.W3/W4 |
| 20 | **M-10 / K-9 — THE PRODUCER PIN IS 8.0.0** ⟨fr-EditorControlsDock M-10 + L-7 + K-9, seat-verified, both readers · FR-EQC-16 · fr-EasingPicker MISSED-D · fr-DarkModeToggle SR-2 · fr-CoefficientsSpectrum B-4 · FR-EQR-33(a)⟩ | MAJOR (substrate correction) | Producer glass-ui is **8.0.0**, not 7.0.0 (installed consumer pin re-verified 4.0.0 at fold). **The corpus, the CENSUS, lane-frontend's break-surface rows, AND the fr-CanvasControlsDock apotheosis's producer pin are ALL stale.** All producer `file:line` cites verified in SUBSTANCE (DockControl tri-state; Popover coarse degradation; definition-absences: `dock/index.ts` has DockControl/DockSeparator, **no** DockIconButton; `src/components` has `metric/`+`popover/`, no metric-badge/hover-popover) — but **every anchor needs re-resolution at 8.0.0 before any F.W1 scheduling; the transaction re-costs against a 4→8 hop, not 4→7.** HEAD drift: `4ef35e8d`→`7d9abe08`→`4a86570b`→`558c3fa3`→`e73ec80c`. **Standing rule this wave mints (P-4): producer-side evidence carries the producer COMMIT HASH, never the version string.** L-7's budget correction rides: **the migration budget is CALLSITES, not imports — 12 `<DockIconButton` + 2 `<HoverPopover` + 1 `<MetricBadge` in 118 template lines.** Carries fr-ContourEditorCanvas MM-11 (cures rendering the dock elsewhere enlarge the transaction) + fr-CanvasControlsDock D-12. **The pin correction kills CURES, not FINDINGS — the table must say which** (FR-EQC-16 K-7: `<Card tier="opaque" surface="cartoon">` is *unspellable* at 8.0.0; the form is `<Card class="cartoon-surface">`). | **G-13** (blocks F.W1 *sizing*, not execution) |
| 21 | **MISSED-D — the producer answered mid-audit** ⟨fr-EasingPicker; both readers missed⟩ | MAJOR (substrate re-derivation) | glass-ui 8.0.0 `1bc09dde` (2026-08-08) **extracts `EasingCurve`** — zero-state, `strokes: EasingStroke[]` where `d` is a PATH not a callable (so the analytic catalogue drives it directly; the README's "declines analytic catalogues" objection binds only the math) — and its header names fourier's fork by exact line count (98+41, matching `wc -l`). **The census's 4→7 delta table is one major stale.** Consequences carried verbatim: *"D-3 dies by construction on adoption; EasingCurvePreview.vue becomes a DELETION not a re-style; MorphPhaseConfig's twin re-homes onto the same unit; the accent gets both arms free; BUT the line re-enters the tri-package deadlock (glass 4→8) whereas DU's ToggleChip route was landable at the old pin — that trade-off is the ruling the forming F.W3 spec actually owes."* **F.W0 re-derives the delta table (CENSUS ERRATA ADDENDUM) and STATES the trade-off; F.W3 owes the ruling — F.W0 must not pre-decide it.** | **G-13**; ruling **EXCLUDED** → F.W3 |
| 22 | **D-19 ANCHOR-RESOLUTION TABLE (P-4 packet)** ⟨fr-ContourPreview 31 · fr-EasingCurvePreview CITE (DU M-6 ≡ LC K-CITE, incl. **one citation past EOF inside its highest-severity carry**) · fr-DarkModeToggle I-3 · fr-CollapsibleSection K-11 · fr-GalleryDraftsSection m-18 · fr-EditorControlsDock K-9 · fr-App K-14 · fr-AdminAuditLog K-13 · fr-CoefficientsSpectrum K-11 · fr-CanvasOverlayButton K-10 · fr-CanvasControlsDock (all anchors re-resolved by that seat)⟩ | MAJOR (corpus hygiene; repair-misrouting hazard) | Four separately-proven drift classes: (1) **axis line-drift** — "systematic-but-inconsistent +1 drift in the script-block cites while other cites in the same document are exact"; C-lane list: stroke :45 not :44, cartoon-card :33 not :32, `preserveAspectRatio` :38 not :53, flip :41 not :55, vector-effect :47 not :48; (2) **cites past EOF**; (3) **dirty-tree attribution** — "every verdict is a working-tree verdict wearing a HEAD label — right for the tree that will be committed, unreproducible from the commit named"; (4) **version-string-instead-of-commit-hash provenance** (K-8/K-9/M-10). Consequence, stated identically across records: **"a repair agent following D's anchors lands OUTSIDE the file."** Accuracy benchmarks the table must name (L-18 runs both ways): the **L-axis cites byte-exact throughout** at fr-ContourPreview; every D/C `git show v7.0.0:` claim reproduced across two working-tree drifts while L's working-tree citations went **76 lines adrift** (fr-CanvasOverlayButton superlative 3) — **the pinned-authority law: cite the epoch or the cite rots.** Instrument-unit note (pt-leaf K7, restated): byte-offset and occurrence-vs-line differences between seats are **ANCHOR/UNIT differences, not errors — state the counting unit beside the pattern.** These rows are currently NO-WAVE-OWNER; **F.W0 gives them their terminal home** (COHESION §3.3). | **G-11** (possible only after G-1) |
| 23 | **REGISTRY-CURRENCY GATE (P-5 packet)** ⟨fr-ImageUpload R2-missed-1 · fr-DarkModeToggle (14 identity-guard folds; "the readers ran ZERO registry sweeps — the largest single correction this seat makes") · FR-EQC R-1 + DISSENT-1 (27 folds) · fr-PathPreview · FR-USB-21 · fr-EquationPanel · fr-VisualizationView K8 · fr-PaperSearch ruling 6 · fr-PaperSearchInput ruling 1 · fr-GalleryAdminBanner K-9 · fr-AdminAuditLog K-1/K-13 · fr-App MG-β/K-14⟩ | MAJOR (attacks SIZING) | REGISTRY-FIRST was treated as courtesy, not precondition. Measured cost: fr-ImageUpload — **zero `registry/adjudicated\|fr-*` citations across all three axes (grep → 0/0/0); ≥7 headline rows are re-books; two of six filed BLOCKERs were graded against banked DEMOTIONS.** fr-DarkModeToggle — 14 banked-row rediscoveries by two independent readers on ONE component. FR-EQC — "two BLOCKER confirmations re-argued from scratch for rows banked a day earlier, and a crown handed to a re-derivation of a banked hinge on dead evidence." Consequences: inflated ledgers, mis-graded severities, off-component bookings, cures pointing at dead lines, banked counts wrong **in both directions**. **Publish the corrected denominators ONCE; FORBID inheriting superseded figures.** Standing method rules minted: **"a re-reader's missed-find cell needs a registry sweep before it needs a proof"** (thrice-recorded); a reader return SHOULD carry a "registry collisions checked" cell. **Publish the DENOMINATOR DEFINITION, not just the integer** (fr-ContourPreview 27's honest both-right reading of 21-vs-25). | **G-12** |
| 24 | **FR-EMT-25 (= C S-3 / K-2)** ⟨fr-EquationModeToggle⟩ | INFO (immediate instruction) | The working-tree delta on `EquationModeToggle.vue` is exactly **one line**, `glass-subtle` → `glass-wash`; **`glass-subtle` resolves NOWHERE (0 hits, dist + src), so at HEAD the track carried a dead class and no material at all**; the wash rung is the documented correct tier for a detail tile and carries a real border (`ladder.css:36-42`) that makes `:39`'s transition leg reachable. **LAND THIS DIFF.** A **named exception** to any wholesale-abandon ruling. Independent of F.W1 (the SegmentedTabs fold routes F.W3, "no F.W1 dependency"). | **G-1** (LAND, exception) |
| 25 | **C:S-2** ⟨fr-PaperSearchDropdown⟩ | INFO (substrate provenance) | The sole working-tree change to `PaperSearchDropdown.vue` is the **correct repair** (`glass-elevated` → `glass-floating`, one line); **`glass-elevated` has ZERO occurrences in the installed dist — HEAD paints a class that does not exist.** MUST NOT be lost in any tree reset. **Second named exception**; the F.W0 minute must enumerate the correct-repair dirty lines **explicitly, before ruling on the rest**. | **G-1** (LAND, exception) |
| 26 | **FR-COB-9 + FR-COB-25 + FR-COB-3 + FR-COB-15 + FR-COB-2** ⟨fr-CanvasOverlayButton; **FR-COB-2 added at repair round 3 (D-12) — the family's fifth id, previously cited but never id-keyed here**; **the latter two ids restored at repair round 1 (R-2c); the composite quotation SPLIT INTO ITS TWO REAL SENTENCES at round 2 (D-4 — two homes, two quotes, never fused: R2-1-LAW.4)** — ⟨cmd⟩ `grep -noE 'dies with the file \(\*\*F\.W0\*\*[^)]*\)' fr-CanvasOverlayButton.md` → `52:dies with the file (**F.W0** rm)` · `58:dies with the file (**F.W0**)` · `69:dies with the file (**F.W0**)`, and they are **not the same sentence**: `:52` (FR-COB-3) carries the `rm`; `:69` (FR-COB-15) does not — its full clause is *"dies with the file (**F.W0**); the ledger ruling is the standing wrapper policy → **F.W3/W4**"* — and the round-1 header that attributed the `rm` form to both coordinates is struck. ⟨**Instrument corrected at repair round 3 (R3-3.9; PASS-3 D-6)**: rounds 1–2 warranted this split with the broad `grep -n 'dies with the file'` and the words *"returns both"*. **It returns nine lines**, and a **third** F.W0 limb sits among them — `:58`, this row's own FR-COB-9 — so the receipt both over-claimed its command and under-counted the family it was enumerating. The narrowed `-oE` probe above returns exactly the three F.W0 limbs and nothing else; **the paste is now the enumeration, so no count word can drift from it.**⟩ Both were previously carried as unattributed content only⟩ | MINOR / INFO | FR-COB-9: the subject's docblock is false on both halves, self-contradictory across two consecutive lines ("legacy" :6 vs "matching the glass-ui canon" :7), stale-stamped ("A.W3.b", predating the 3.1→4.0 hop), and imprecise — authored by `be24948` **in the same diff that falsified it**. **Dead code that ASSERTS a design rule is a teaching artifact aimed at the 9 live sites**, and `lane-frontend.md:101` carries this file as a **LIVE role**. FR-COB-25: five commits; the name earned until `2f53d5d`; `be24948` broke the class + added the correct ARIA + wrote the false canon; `262c3d0` the subpath re-point — **two maintenance passes on a corpse = the empirical price of five skipped DELETE orders.** **F.W0 act: the inventory register gains a DEADNESS COLUMN for design-asserting rows** (lane-frontend ERRATA ADDENDUM, E-3). **FR-COB-3** (`:52`): the `.is-active` inertia is the `btn-glass`/`glass-btn` transposition; the F.W0 limb is *"dies with the file (**F.W0** rm)"*, and the surviving limb — the fourier-wide vocabulary consolidation with its **sweep-law rider** — routes **F.W3/W4** and is attributed to this id at §6b. **FR-COB-15** (`:69`): the Goldilocks failure (unified **zero of 9** live sites; the one natural consumer hand-rolls it); the F.W0 limb is *"dies with the file (**F.W0**)"*, and the surviving limb is the **standing wrapper policy**, quoted at round 2 in the FORM THE BYTES CARRY (D-5) — ⟨cmd⟩ `sed -n '93p' /Users/mkbabb/Programming/fourier-analysis/docs/tranches/A/audit/W3-button-ledger.md` → *"naked wrapper component forwarding `active` as `aria-pressed`; `<Button variant="glass" size="icon">` IS the surface"* — **DOUBLE quotes; the single-quoted form this row and `fr-CanvasOverlayButton.md:69` both carried under a "verbatim" label is STRUCK, because a quote presented as at-the-coordinate must survive `grep -F`.** The policy routes **F.W3/W4** and is joined to that cite at §7b. **FR-COB-2 — the fifth id of this family, given its own id-keyed disposition at repair round 3 (R3-3.9; PASS-3 D-12).** ⟨Its F.W0 limb is banked at `fr-CanvasOverlayButton.md:41` — ⟨cmd⟩ `sed -n '41p' fr-CanvasOverlayButton.md \| grep -o '+ F\.W0[^)]*)'` → *"+ F.W0** (this file is the only member whose complete cure is deletion)"*. In rounds 1–2 the id appeared in this file **only as a citation** — a G-12 denominator operand and a row-38 premise correction — and was absent from row 3, this row and G-10's fold lists, **where every sibling FR-COB id is homed.** It was booked by absorption into G-10's deletion, which is a true routing and not a ledger row: M-25 requires *"a gate, a bounds item, a named cure with its locks, or an explicit EXCLUDED line"*, and absorption is none of the four. The asymmetry with its own family is the finding, and it is cured by naming it here rather than by re-opening it.⟩ **Disposition**: **FR-COB-2's F.W0 limb is DISCHARGED BY G-10's deletion and by nothing else.** Its component-instance identity is banked FOLDED at `fr-BasisSelector M-7` and is **never re-booked** (the record's own *"FLEET IDENTITY FOLDED → fr-BasisSelector M-7"*); its prop-level-diff re-derivation with the corrected `9 live variant="glass" sites / 7 files` and `36 real size="icon" sites (35 live)` budget is **ADJUDICATED → F.W1** and is not F.W0's. **The lock**: the `size="icon"`→`size="md"` sweep F.W1 inherits **silently drops the 44px floor on all 35 live sites** unless it is `size="md" iconOnly` — so if G-10 rules KEEP rather than DELETE, this id does not quietly vanish, it becomes an F.W1 sizing operand with that lock attached. **Under DELETE it retires unmeasured, correctly**, on the same law FR-IC-2 retires under at row 4. **All five die with the file at the `git rm`; only the REGISTER lesson, the sweep-law rider, the wrapper policy and FR-COB-2's F.W1 sizing lock survive it.** | **G-10** + lane-frontend errata |
| 27 | **STALE-DIST TRAP (`web/dist`)** ⟨FR-NP-32 "why no seat hit it before" · FR-IC-1 + FR-IC-25 · fr-PathPreview residue 6 · FR-MSP-12 admissibility cell · fr-CanvasOverlayButton residue 7⟩ | MAJOR (epistemic) | `web/dist` is a **3.1.0-era artifact dated 2026-06-12** — built BEFORE the 4.0.0 install and **unrebuildable today** (rows 1 + 7). **Every "what the installed 4.0.0 emits" argument in the corpus reasoned about it.** It is simultaneously the ONLY admissible emission evidence for git-clean files older than it (FR-MSP-12's byte-position extraction at 1955/1327 is sound *precisely because* the subject is Mar 30 and clean) and **INADMISSIBLE for anything the 4.0.0 install touched.** F.W0 publishes the admissibility rule (artifact date vs subject mtime vs install date), then quarantines. **Brief corrected (§1d.1): NOT committed** — `git ls-files web/dist` → 0; gitignored twice. **Quarantine AFTER G-4/G-5** — delete-before-rebuild destroys the only emission evidence the corpus has. | **G-5** + ledger rule |
| 28 | **INTAKE F.W0 CARRIES** — R3-9 · R3-16+R4-11+R5-9+R6-10 · R4-12 · R5-3 · X-7 · the Codex worktrees ⟨`INTAKE-ADJUDICATION-2026-08-03.md` §"F.W0 · re-ground" + `CENSUS-2026-08-03.md:360/:368`⟩ | MINOR (disposition) | Five Codex-provenance residues own no other wave. R3-9: an unverifiable non-execution assertion — **"record, NEVER cite as method guarantee."** R3-16+R4-11+R5-9+R6-10: **the 14-wave M board is the real object to disposition; the 72/158/P29/slot superstructure is Codex-internal and does not transfer.** R4-12: `/tmp/fourier-r4-files.sha256` — **measured ABSENT at fold, so the disposition is "evaporated, consume nothing", not "delete"** — record the evaporation date. R5-3: an unverified pin — **"re-hash or drop."** Register carry X-7. **Census §"wave-sketch adjustments forced": F.W0 gains the residue/worktree dispositions AND the pin-hygiene rule — unverified Codex pins are re-hashed at use or dropped.** Worktrees: **three** (`d0be`, `7e28`, `9167`), not one; `7e28` is named in the formation tree (`formation/codex-worktree-7e28/CENSUS.md`), so **its disposition is not purely mechanical.** Also disposes the brief's named objects: the J/K-deploy/M tri-tranche interlock (live object `docs/constellation/tri-tranche-run/RUN-BOARD.md`, currently ` M` — one of the land-or-abandon rows) and **the dead M.W1b glass-4.1.0 gate — "dead-by-supersession (glass shipped 7.0.0)" per CENSUS:152, now doubly dead at 8.0.0** (CENSUS §5 [P1]: "the dead M.W1b gate holds fourier's entire design surface"). | **G-14** |
| 29 | **OG-F1 — OWNER RULING** ⟨`INTAKE-ADJUDICATION-2026-08-03.md` open-governance register; rows R3-1, R3-4, R4-1, R4-13, R5-1, R6-1⟩ | OWNER RULING | *"Re-root fourier source auditing at F.W0, or leave the R-coordinates frozen and build from live-tree measurement alone?"* **FLAGGED INLINE; F.W0 does not decide** (§5). Lane evidence stated verbatim: *"favors **freeze-with-adoption** — the measurements are already live-reproduced; R6 itself declared `NO_SUCCESSOR`, making the ruling cheap."* Interacts with GAB-13's dissent and with OG-V2. | §5 (flagged) |
| 30 | **FR-COB-28 — the consolidated glass-ui BH relay letter** (+ FR-NP-32 at the TOP, + FR-MSP-12 as item 8) ⟨fr-CanvasOverlayButton FR-COB-28 (DU-miss-6; "this record IS the packet") · fr-NotationPills FR-NP-32 · fr-MorphShapePreview FR-MSP-12 · fr-PaperSidebar M1 + L-10/D-M4 · fr-GalleryAdminBanner ×2 · fr-AppHeader ×2⟩ | NO-WAVE-OWNER (producer) — **F.W0 owns ASSEMBLE + SEND** | The corpus's producer-owned findings carry ONE relay instruction where the standing owner edict (glass-ui BH/BI relay, a fond) requires all. The packet names its own trigger: *"one relay letter, seven items, sent at F.W0/F.W1 open by whoever holds the mail seat"* — grown to **nine entries (items 0–8)** by the two additions. **The list — SPEC-VOICED ROSTER, not a quotation** ⟨**label struck at repair round 3 (R3-3.9; PASS-3 D-3).** Rounds 1–2 headed this list *"The list, verbatim:"* with **no ⟨cmd⟩ anywhere** — inline or in §3a, whose Q10 covers only the trigger sentence and Q11 only the coin-flip pair — which R2-1-LAW.2 makes **a defect per se**. The label also did not hold at two items: ⟨cmd⟩ `grep -cF 'near-collision naming hazard (two utilities one letter-order apart in one file)' fr-CanvasOverlayButton.md` → **0** (the bytes carry *"…hazard (FR-COB-3's mechanism — two utilities one letter-order apart in one file)"*, and the un-marked elision deleted the very **FR-COB-3 attribution repair round 1 restored under R-2c**, from inside a quotation, in the file that books the id at row 26) and ⟨cmd⟩ `grep -cF 'record DOCK-ACTIVE SATISFIED upstream — do not re-send' fr-CanvasOverlayButton.md` → **0** (the bytes read *"record DOCK-ACTIVE satisfied upstream, do not re-send (FR-COB-11)"* — re-cased, comma swapped for an em-dash, and *"a NEGATIVE ask"* **added inside** a verbatim label, which §3a's convention (2) expressly forbids). **The whole packet is therefore re-labelled as this spec's roster** — the honest description of an assembly the wave is composing, in which items 0 and 8 are additions the bank never contained — **and the two drifted items are re-quoted at their bytes with the spec's own additions moved outside the quotation marks.** Items 1–5 reproduce and are unchanged⟩: (0) **FR-NP-32 (≡ fr-PaperSidebar M1) — the corrupt dist stylesheet, at the TOP, with a published-stylesheet PARSE-GATE ask; the emitter fix, NOT a version bump** ⟨canonical pair restored at repair round 4, R4-9.8 / PASS-4 D-9: rounds 1–3 carried a bare `FR-NP-32` here⟩; (1) FR-COB-7 — dead border arms fleet-wide + pressed-suppresses-feedback composition; (2) FR-COB-8 — the unlayered-P9-defeats-layered-a11y-resets CLASS (FR-COB-12 one instance); (3) FR-COB-6 — v7 pressed-paint removal: intentional?; (4) FR-COB-23 — the 1.94:1 focus ring; (5) FR-COB-26 — the generated color-mix fallback; (6) the `btn-glass`/`glass-btn` **near-collision naming hazard** — quoted at the bytes, ⟨cmd⟩ `grep -o 'near-collision naming hazard[^;]*' fr-CanvasOverlayButton.md` → *"near-collision naming hazard (FR-COB-3's mechanism — two utilities one letter-order apart in one file)"*, **the FR-COB-3 attribution restored inside the quotation where the bank puts it**; (7) the negative ask — ⟨cmd⟩ `grep -o 'record DOCK-ACTIVE[^)]*)' fr-CanvasOverlayButton.md` → *"record DOCK-ACTIVE satisfied upstream, do not re-send (FR-COB-11)"*, **the bank's casing and comma kept**; that it is a NEGATIVE ask is this spec's classification and stands outside the quotation; (8) **FR-MSP-12 — the manufactured accent-on-accent fallback (1.00:1) with the class's first PROVABLE counterfactual: strip the synthesized tint fallback and the legacy path lands on solid `--muted` at 4.43/3.76 light · 4.74/5.58 dark — the generator MANUFACTURES the failure.** Also riding: fr-PaperSidebar L-10/D-M4's ramp-headroom ask. **Honest tension carried — SPLIT INTO ITS TWO REAL SENTENCES at round 2 (two homes, two quotes, never fused: R2-1-LAW.4).** ⟨cmd⟩ `grep -n -o 'the synthesis is a coin-flip[^*]*fires' fr-MorphShapePreview.md` returns **two distinct sentences**, and the round-1 cell quoted neither: (i) the **FR-MSP-12 row's own** sentence, `:104` — *"the synthesis is a coin-flip: absent where it would help, harmful where it fires"* — **this is the one the id carries, and it takes a COLON and no parenthetical**; (ii) the **fold-admission** sentence — *"the synthesis is a coin-flip — absent where it would help (7 files, 19 unguarded declarations), harmful where it fires"* — whose home is `fr-MorphShapePreview` **§"The incredulity seat's own work — rulings and corrections", ruling 3 (the FR-MSP-12 fold admission), at `:22`**. ⟨**Address corrected at repair round 3 (R3-3.9; PASS-3 D-7).** Rounds 1–2 addressed it as *"the §22 fold-admission"* while §3a's Q11 keyed the identical quote to *"`:22` (the fold admission)"* — **one file publishing two idioms for one address, and the section idiom resolves nowhere**: ⟨cmd⟩ `grep -nE '^#{2,4} ' fr-MorphShapePreview.md` returns the record's headings — Scoreboard · The incredulity seat's own work — rulings and corrections · Adjudicated defect roster · A…F · Killed-claims register · Superlatives · UNPROVEN-NEEDS-LIVE residue · DISSENT · Closing verdict — **and there is no §22**; `:22` is ruling 3 of the second section's list. The corpus is frozen, so a line cite is lawful here (R2-2.2(i)) — **a cite to a section that does not exist never was**, and it was shipped inside the cure for the coin-flip fabrication, which is the same escalation round 3 convicts at Q20.⟩ Round 1 published a **third** form: sentence (ii)'s shape with *"(FR-NP-8's 19 unguarded declarations)"* substituted for *"(7 files, 19 unguarded declarations)"*, under a "verbatim" label. **The substitution is struck.** The cross-reference it was reaching for is real but is the SPEC's, not the bank's, and belongs outside the quotation marks: ⟨cmd⟩ `grep -n '19 unguarded' fr-NotationPills.md` → `46:` — **FR-NP-8** carries *"19 unguarded `color-mix` declarations across 7 SFCs"*, which is where the fold row's "7 files, 19 unguarded declarations" figure comes from. **Stated as a cross-reference; never smuggled into a quotation as an attribution the quoted file does not make.** | **G-3**; **F.W1 CONSUMES** |
| 31 | **THE FOURIER MAIL SEAT + CLAUDE.md (E13 analogue) — this wave's creating act** ⟨`CENSUS-2026-08-03.md` §4 item 1 · `COHESION.md` §1 register row "Mail-ledger surface … F.W0 wave item" · the O-14 letter⟩ | MAJOR (enabling) | fourier has **no mail ledger and no CLAUDE.md** (absence re-verified at fold); the O-14 letter sits UNTRACKED (the sole `??` row). Every relay this wave owes (P-1, P-6) needs a seat that does not exist. **F.W0 creates: (a) the fourier-side mail ledger (E13 analogue) at the fourier docs root; (b) a root `CLAUDE.md`; (c) commits the O-14 letter.** Owner law E13 binds the seat once it exists: **every session/wave OPENS with a 4-path coordination-mail sweep; NO WAVE CLOSES WITH UNREAD MAIL. Owner order: NO CRONS.** COHESION §2's reciprocity law binds: cross-repo edges are declared FROM BOTH ENDS in the spec files. | **G-2** + **G-3** |
| 32 | **P-6 LATEX-PAPER PRODUCER CARRIES** — PAW-32 · PAW-38 · PAW-39 · PAW-45 · PAW-46 · PAW-56 ⟨fr-PaperArticleWindow (all six, each "→ LATEX-PAPER carry; NO-WAVE-OWNER locally") + fr-PaperView's 6 LATEX-RELAY riders + fr-PaperSidebar L-11 (M2 rides: "one relay carries L-11 + M2")⟩ | MAJOR band (producer-owned) | **PAW-32** — `PaperSectionBlocks.vue.d.ts` ships NO `__VLS_Slots` (grep 0; sibling PaperSection has it), so `figure.*`/`callout.*` destructure implicit `any` — the file's largest unchecked surface: emit slot types. **PAW-38** — section ids (the `:key`, the DOM `id`, `elementMap`/`measuredHeights`, a never-cleared session cache) are all the same un-deduplicated title slug with no uniqueness registry: two identically-titled headings collide across four registries at once (**LATENT: all 93 slugs unique today**): uniquify at the emitter. **PAW-39** — every `depth > 0` section renders `<h3>` + `.section-header--sub`, so 51 `\section` and 31 `\subsection` nodes are indistinguishable to AT heading nav; the flattener emits `sourceLevel` (`:110`) the renderer never reads: read it. **PAW-45** — a SECOND module-level, unbounded, never-evicted cache: the KaTeX memo (dist `:387`) retains every equation ever typeset for the page-session lifetime, surviving unmount and route change (~1145 inline + ~263 display spans vs ≤18 mounted sections): **"cap, don't delete" — repairs must weigh superlative 13: the memo is also what makes the PAW-3/4 churn survivable.** **PAW-46** — the same memo keys on the TeX string alone and ignores `macros`; two `useKatex()` instances with different macro sets cross-serve: key on tex + macro identity, or move the Map inside `useKatex` (same repair as PAW-45). **PAW-56** — `--_section-color: var(--section-color-${sectionIndex})` with **no fallback and no modulo** (dist `:1404`); 13 stops vs 12 top-level entries ⇒ a thirteenth entry makes the var guaranteed-invalid → the fallback is **itself IACVT** (`--section-heading` is oklch) → all six heading-colour/gradient declarations drop at once and nothing signals: modulo at the producer bind, or a fallback that is NOT var-shaped, riding the PAW-1 rewrite. **fr-PaperSidebar L-11 rides the same letter**: latex-paper's damped rAF sidebar-follow has **NO PRM gate anywhere in `dist/vue.js` (0 hits)** and no motion option — "a third ungated clock missing from lane-frontend §8's two-clock inventory; amend lane-frontend §8" (ERRATA ADDENDUM). **PAW-38/46/56 are LATENT-today with margin-1 exposure — the letters must say so, or the producer sizes an emergency.** | **G-3** (SEND); **F.W10** reconciles + terminally disposes |
| 33 | **FR-AUL-58 ≡ FR-COB-24 (= D-28 / D-14) — the root-size inversion** ⟨fr-AdminUserList · fr-CanvasOverlayButton · instruments: fr-AdminAuditLog AA-15 · fr-CoefficientsSpectrum m-9 · fr-SliderControl R-3 · fr-App · fr-FourierMorphSvg FM-21⟩ | MINOR (app-wide) | fourier forks the root font at `style.css:40-50` — `html{font-size:1.125rem}` under 768px — **an inversion: 18px mobile / 16px desktop.** Consequences: the 10px admin-label rung renders SMALLEST on the DENSEST context; every rem-denominated glass-ui control token inflates 12.5% and compounds with coarse `--ui-scale: 1.5` → **67.5px, not the 60px the token author computed**; the two micro scales CROSS (13.5px mobile `text-xs` vs fixed 10px `text-admin-label`); PaperSearchInput's 40–67.5px controls sit beside a 20px hand-corrected twin. **SPLIT ROUTING, both halves carried: the FORK DECISION is F.W0 substrate; the TOKEN-PARITY re-tune rides F.W1** ("the 7.0 ladder is re-tuned against a 16px root"). FM-21 — ⟨cmd⟩ `sed -n '99p' fr-FourierMorphSvg.md` → *"which scaling system owns control sizes is an F.W0-adjacent question the F.W4 ticket should pose, not answer."* (**quoted at the bank's own casing from round 2; the round-1 "POSE" was this spec's emphasis wearing a verbatim label**) — **DIVERGENCE MINUTE, required by M-25/E-1 and authored at repair round 3 (R3-3.9; PASS-3 D-8).** ⟨Rounds 1–2 quoted that sentence and then, **in the very next clause**, assigned the POSE to F.W0 — *"F.W0 poses it and rules the fork"* — with no errata, no E-3 addendum and no divergence record, in a file that minutes such departures scrupulously everywhere else (`web/DESIGN.md:33` against the ruling's `:34`; the `lane-frontend.md:69` strike; the R4-12 evaporation). **An id may be re-routed; under M-25/E-1 it may not be re-routed silently one clause after being quoted against.** The minute is owed and is here.⟩ **The departure, and its reason.** The bank gives the POSE to the F.W4 ticket. **F.W0 takes it, knowingly**, on one ground: the question is *which scaling system owns control sizes*, and its answer is the **root-fork decision** — `html{font-size:1.125rem}` at `style.css:40-50` — which is this row's own substrate finding and which **F.W1's token re-tune consumes before F.W4 opens**. A pose deferred to F.W4 arrives after both consumers have already had to assume an answer. **What is NOT taken**: the bank's *"not answer"* half binds F.W4's per-component execution and is untouched — F.W0 poses and rules the FORK only; F.W1 re-tunes; **F.W4 executes per-component within the partition `F-W3.md` §X.1-v4 publishes** (the twin's definitive file-criterion partition, landed FIRST this round under R3-1a: *"is the record's target file in a §5 unit file list"*, `F.W3 CITES, F.W4 BOOKS`; **`F-W3.md` pinned per PIN-PURGE-CERT.md §TABLE (2026-08-29)**). **`fr-FourierMorphSvg` is NOT on v4's F.W4 default arm at the ruled left-token record-qualified detector** — v4 carries only its already-booked `FM-4..FM-16` band there — **precisely because FM-21 is homed HERE**, at this row. F.W4 inherits the ruled fork, not the question. **If a later seat judges the pose F.W4's after all, this minute is the record of the departure to reverse; it is not a silent fact.** One identity, first booked at FR-COB-24 — three instruments observed without booking. | **G-15(b)**; re-tune **EXCLUDED** → F.W1 |
| 34 | **FR-GV-28 (= L-25)** ⟨fr-GalleryView⟩ | MINOR | `Visualization` is imported from **two paths in one subtree** — `GalleryView.vue:10` from `@/lib/types` vs `gallery.ts:4` via an `@/lib/api` passthrough. One canonical type path; **the passthrough is the removable side.** One act with row 35. | **G-6** (import-hygiene arm) |
| 35 | **FR-EQR-24 ≡ FR-USB-22 (∘ fr-EquationView S-10 ∘ prior art A5-05 ∘ FR-COB-27)** ⟨fr-EquationResult (= L-m2 / C-4) · fr-UserSlugBar (= L-9 / C-m6)⟩ | MINOR | Root-BARREL `useClipboard` imports sit one line below SUBPATH imports in the same files (`:4` vs `:3`; `:4` subpath vs `:5` barrel); `./dom` is live in the 80-key exports map; prior art A5-05 names all three sites. **The bundle-weight arm is correctly DEAD** — and FR-COB-27 kills the cognate credit: **`vendor-ui` is keyed on the package ROOT (`vite.config.ts:52`), so the manualChunks split is INDIFFERENT to leaf-vs-barrel discipline; the actual barrel offender is `CollapsibleSection.vue:2`.** **The real stake is FR-EQC-7**: the root barrel is what drags `vaul-vue` through 7 edges incl. `router/index.ts:2` — the subpath sweep is the alternative cure row 9 names. Carries fr-UserSlugBar **RC-2** (the MPC-19 stale cell). **The bundle-cost cell is killed in the registry's own record and must not be re-argued.** | **G-6** (alternative-cure arm) |
| 36 | **FM-19** ⟨fr-FourierMorphSvg, r1 identity re-affirmed at r2⟩ | INFO | The morph assets are **FROZEN with no regeneration pipeline**, plus 5 orphan JSONs. The unguarded-drift exposure is also the reachability frame for **FM-23** (`lerpPoints` silently truncates on length mismatch — `Math.min(a.length,b.length)`; a length-mismatched-but-non-empty asset pair cross-fades only the common prefix, closed by `" Z"` into a **chord across the figure**, no throw, no warn) and the assumption FM-20's cure must not inherit silently. **F.W0 owns only the regeneration/freeze ruling + the golden-file baseline** they depend on. **Rider carried: FM-23's cure is a rider ON banked N-15 — a UNIFORM-LENGTH ASSERTION at `prepareFourierShape`, not an emptiness check — paired with FM-19's golden-file diff.** Latent today (512 points at every level of both assets, seat-swept). | **G-15(c)**; FM-20/23 cures **EXCLUDED** → F.W4 |
| 37 | **M-15 ⊗ PP-REDGATE — THE VUE-TSC RED CONTRADICTION** ⟨fr-AnimationControls M-15 (banked: "substrate green before F.W1 opens") · fr-PaperView (LC's fourth reproduction, folded → F.W0) · fr-PathPreview L-2/PP-REDGATE + K7 (**re-adjudicated: the F.W0 routing DIES**)⟩ | MAJOR (with an ERRATA against a banked routing) | Banked M-15: `vue-tsc` fails on `PaperView.vue(12,8) TS2882` (`@mkbabb/latex-paper/theme` side-effect import), measured independently **four times**; `build` = `vue-tsc -b && vite build`. **PP-REDGATE re-derives and re-routes it on four receipts:** (1) the **committed** lock resolves typescript **5.9.3** / vue-tsc **2.2.12**; (2) the **uncommitted** package.json hunk carries `typescript ^5.8→^6.0.3`, `vue-tsc ^2.0→^3.3.5` — **the same hunk M-15 quoted**; (3) `noUncheckedSideEffectImports` is UNSET in the 13-key tsconfig, so the compiler default governs — **false at 5.9.3, true at 6.0.3**; (4) a flag-isolation run with the flag forced off emits **ZERO** diagnostics. **PP-REDGATE's conclusion — *committed substrate + committed lock = GREEN gate; the RED is the UPLIFT'S* — is the banked PREDICTION this row carries, NOT a settled outcome: G-15(a) is born-RED and the falsifier that settles it has not run** (repair round 1: the previous phrasing pre-decided the very question G-15(a) exists to settle). The offending import is COMMITTED and four months old. **Disposition, verbatim: "The cure (ambient `declare module \"@mkbabb/latex-paper/theme\"` in env.d.ts, or a latex-paper types fix) lands WITH the toolchain bump, owned by F.W1/W2 — NOT as an F.W0 precondition."** ERRATA instruction **SPECIFIED here, performed at execution** (status planned; repair round 1 struck the current-voice "executed here"): banked `fr-AnimationControls.md:69` M-15's F.W0 routing **is to be RE-READ and SUPERSEDED by an addendum**, dated evidence unedited (E-3). **F.W0 RULES the contradiction; it does not carry both bookings.** *This row IS the live dissent in the bank — M-15 + fr-PaperView + reader-A route the RED to F.W0; fr-PathPreview K7 kills all three routings together. Both cannot stand; G-15(a) settles it with a falsifier.* | **G-15(a)**; cure **EXCLUDED** → F.W1/W2 |
| 38 | **FR-IC-25** ⟨fr-InfoCard, reader-B miss 2⟩ | INFO (evidence upgrade) | **The enforcing type gate is CI, and it runs cold by construction**: `ci.yml:95` and `deploy-pages.yml:114` both run `npx vue-tsc -b --force`; `--force` defeats the `-b` incremental skip, and the tsbuildinfo is gitignored twice (`.gitignore:43-44`) so CI always starts cold anyway. **Both original axes rested the gate claim on the PACKAGE SCRIPT; registry-swept — no `fr-*` row cites either workflow line.** F.W0 publishes the **true gate topology** in the substrate ledger. Consequence for F.W1: at the tri-package hop the TS2307 on `@mkbabb/glass-ui/metric-badge` (`InfoCard.vue:4`) fails **CI AND the Pages deploy**, not merely a local build. Corrects the premise under FR-COB-2's "the file gates the build anyway" and FR-IC-8's budget stake. | **G-7**/**G-8**/**G-9** wiring |
| 39 | **FR-AUL-23 (F.W0 OPTION limb only)** ⟨fr-AdminUserList, DU-miss, net-new⟩ | MAJOR (row) / **OPTION** at this limb | The search field's border renders in `currentColor` — a near-black hairline in light mode: Tailwind v4 preflight resets `border: 0 solid` with no color, `.border` carries no color, and **there is NO base `border-color: var(--border)` rule anywhere in the stack** (built-CSS count = 2, both element-scoped). The only text input on the panel draws a near-black edge while every surface around it draws the muted token — the classic v3→v4 migration casualty; **no axis caught it.** **Primary route F.W4** (cured for free by FR-AUL-6's `Input` swap). **"Independently, a base-layer border-color rule is an F.W0 substrate option." Book as an OPTION, not a cure** — landing a base-layer rule ahead of the uplift changes the paint of every bordered surface F.W1 is about to re-tier. **If F.W0 takes the option it must be re-measured after G-4/G-5** (the count of 2 was taken against the unbuildable pin). | **recorded OPTION** in the ledger; cure **EXCLUDED** → F.W4 |

### 3a. QUOTE REGISTER — every cross-file quotation in this file, keyed quote → home → command (R2-1-LAW)

**Why this section exists.** R2-1-LAW makes a quotation admissible only as the pasted output of a command run this round, with the command stated beside it, and makes a *"verbatim"* label without a ⟨cmd⟩ note **a defect per se**. ⟨**The basis count is STRUCK at repair round 3 (R3-3.9 ⊕ R3-3.10; PASS-3 D-10).** This sentence read *"This file carries **19 lines** bearing that label"*; the PASS-3 check ran `grep -c 'verbatim' F-W0.md` and got **24** — a figure this round's own writes have since moved again, which is exactly why it is reported as **that seat's dated measurement** and not re-banked here — no command in the register ever returned 19, and none was offered — **a bare count word inside the section authored to make counts reproducible.** It is not re-stated with a corrected number: a self-count is banned outright under R3-3.10, and this round's own writes moved it again. **The register's completeness claim never rested on the count anyway** — it rests on the pairing below, which is checkable row by row.⟩ **Every quotation in this file is keyed to a row of this table**, and every ⟨cmd⟩ label the file carries is either a row here or an inline note at its own site; rather than swell the §3 rows with inline transcripts, the commands live here, once, keyed to their sites — **the ⟨cmd⟩ note for every quotation below is this table's row for it.** Rows already carrying their own inline ⟨cmd⟩ (row 26's two `dies with the file` sentences, row 30's split coin-flip pair, row 33's FM-21, §4 G-3's mail inventory, §4 G-13's drift receipt, §6b's wrapper policy and residue-6, §7b's ledger line) keep it there and are cross-listed, not duplicated.

**Standing rule this register enforces**: a home is a FILE and a stable anchor — an id, a row, a §-heading. Line numbers appear below **only** for the frozen corpus (`fr-*.md`, adjudicated, E-1 immutable), the frozen intake/formation documents, and the **stamped canonical census**, exactly as R2-2.2 permits; **no row addresses a live sibling spec by line.**

**HASH-PINNED CROSS-QUOTES — the round-4 idiom (R4-8.3).** Every quotation this file takes from a **live sibling spec** now records that sibling's `shasum -a 256` **first-12 at quote time**, so a later rewrite is detectable rather than deceptive. ⟨cmd⟩ `shasum -a 256 F-W1.md F-W3.md F-W7.md | /usr/bin/awk '{print substr($1,1,12), $2}'`, this seat, 2026-08-29 → **`F-W1.md` · `F-W3.md` · `F-W7.md`, each pinned per PIN-PURGE-CERT.md §TABLE (2026-08-29) — the inline first-12 values RETIRED (R4-8.2 purge)**. These pin **Q17** (the F.W1 roster's extent, quoted at §6a lock 5 · §6b · §4 G-6 · §8), **Q20 and §6b's F.W7 reciprocal**, and the three `F-W3.md` **§X.1-v4** twin cites at §3 rows 11 · 15 · 33. ▲ **Why this idiom and not the four before it.** Rounds 1–4 tried, in order: bare assertion, **counts**, **classifications**, and **`grep -o` word-output — and every one went stale for the identical reason**, because each describes a file that keeps moving. R3-3.10's ranking (*set-membership ≫ classification ≫ word-output ≫ counts*) was right about the ordering and wrong about the ceiling: **the ceiling is not a better description of a moving file, it is a fingerprint of the file that was actually read.** A hash goes stale too — and that is the point: **staleness becomes the signal instead of the defect.** A later seat that finds a **PIN-PURGE-CERT.md §TABLE (2026-08-29)** row no longer matching has learned something true and actionable; a later seat that found `grep -c` returning 1 instead of 0 learned only that someone had typed a word.

**Two conventions, stated so a hostile reader need not guess** (added at the second repair pass, because an unstated convention is indistinguishable from a drift). **(1) The command must RETURN the quote.** A ⟨cmd⟩ whose output is a *subset* of the words displayed beside it is not a provenance note — it is an unverified quotation wearing one, and it fails R2-1-LAW.1 exactly as a typed-from-memory quote does. Three such notes were shipped by the round-2 first pass and are corrected in this register (Q1, Q17, and the G-6 site of Q17's second command); each correction states the old command, its actual output, and why it fell short, rather than swapping the command silently. **(2) Markdown emphasis is normalized, words are not.** Where a bank sentence carries `**bold**` inside the quoted span (Q6's *"that trade-off is the ruling…"*; Q2's severity head), this file reproduces the WORDS byte-for-byte and renders emphasis to suit its own prose. **No word, figure, tense, version or parenthetical is ever normalized** — those are the four drift classes this register convicted, and they are edits, not formatting.

| # | Quoted where | Home (id / anchor) | Command run this round | Result |
|---|---|---|---|---|
| Q1 | Masthead §Sub-tranche · §6a lock 1 | `X/COHESION.md` §0a BOUNDARY | `sed -n '151,152p' COHESION.md \| tr '\n' ' ' \| grep -o 'the F.W0 substrate pre-gates[^)]*)'` | *"the F.W0 substrate pre-gates (corrupt glass-ui 4.0.0 dist/styles/index.css; dirty-worktree settle)"* ✔ EXACT. ▲ **Command CORRECTED at the second repair pass**: the quoted sentence **spans a hard line break** in COHESION (`:151` ends at *"dirty-worktree"*; *"settle)"* opens `:152`), so the round-2 first pass's single-line `grep -n 'F.W0 substrate pre-gates' COHESION.md` returned only the first half while the cell displayed the whole — a note that did not return its own quote. The join is now performed by the command, and the wrap is disclosed rather than papered over |
| Q2 | Row 1 (banked disposition) | `fr-NotationPills` **FR-NP-32** | `sed -n '35p' fr-NotationPills.md` | *"BLOCKER — NO-WAVE-OWNER (producer dist emitter) + glass-ui BH relay at the TOP of the FR-COB-28 packet; F.W1 SEQUENCING GATE."* ✔ EXACT |
| Q3 | Row 2 · §5 (worker-DU's dissent) | `fr-GalleryAdminBanner:130`, the GAB-13 counter-position | `grep -n 'already elected to treat the working tree' fr-GalleryAdminBanner.md` | `130:` — *"the fleet has already elected to treat the working tree as the scope"* ✔ EXACT |
| Q4 | Row 3 · §5 (L-1's conditional) | `fr-CanvasOverlayButton:40`, **FR-COB-1**'s L-1 limb | `sed -n '40p' fr-CanvasOverlayButton.md \| grep -o 'if F.W0 rules ABANDON.*$'` | *"if F.W0 rules ABANDON on the M.W1a tree wholesale, the governance limb falls to MAJOR; the deadness does not."* ✔ EXACT |
| Q5 | Row 7 · §4 G-5 · §4 G-15(d) (ruling-6 rider) | `fr-PaperSearch`, ruling 6 | `grep -o 'when MISS-A7.*outlive the evidence.' fr-PaperSearch.md` | *"when MISS-A7's build unblock lands, run the build, grep the emitted CSS for `class*=size-`, and correct whichever banked cell lost — booked here so the contradiction cannot outlive the evidence."* ✔ EXACT |
| Q6 | Row 21 (MISSED-D consequences) | `fr-EasingPicker` **MISSED-D** | `grep -o 'D-3 dies by construction on adoption.*actually owes' fr-EasingPicker.md` | *"D-3 dies by construction on adoption; EasingCurvePreview.vue becomes a DELETION not a re-style; MorphPhaseConfig's twin re-homes onto the same unit; the accent gets both arms free; BUT the line re-enters the tri-package deadlock (glass 4→8) whereas DU's ToggleChip route was landable at the old pin — that trade-off is the ruling the forming F.W3 spec actually owes"* ✔ EXACT |
| Q7 | Row 26 · §6b · §7b (**FR-COB-3** / **FR-COB-15** F.W0 limbs) | `fr-CanvasOverlayButton:52` / `:69` | `grep -noE 'dies with the file \(\*\*F\.W0\*\*[^)]*\)' fr-CanvasOverlayButton.md` | **THREE F.W0 limbs in TWO forms**, pasted whole: `52:dies with the file (**F.W0** rm)` · `58:dies with the file (**F.W0**)` · `69:dies with the file (**F.W0**)` — `:52` = FR-COB-3 (**the only one carrying `rm`**), `:58` = FR-COB-9, `:69` = FR-COB-15; `:58` and `:69` are byte-identical in this span. ⟨**Command and count word both corrected at repair round 3 (R3-3.9; PASS-3 D-6).** Rounds 1–2 keyed this row to the broad probe `grep -n 'dies with the file' fr-CanvasOverlayButton.md` and printed the result as *"TWO sentences, not one"*. The probe returns **nine** lines — `:52 :53 :54 :58 :67 :68 :69 :74 :75` — so the register built to prove that commands return their quotes printed a count word **its own command refutes**, and it missed `:58` (FR-COB-9) entirely, a third F.W0 limb this row's own site books. The split was right about the ids it named and wrong about its instrument: **the fix is a command whose output IS the enumeration**, which is what the narrowed `-oE` probe above delivers — no count word survives it, because the paste is the count.⟩ Inline at row 26 |
| Q8 | Row 26 · §6b · §7b (the standing wrapper policy) | `fourier/docs/tranches/A/audit/W3-button-ledger.md:93`, the `CanvasOverlayButton.vue:8` row | `sed -n '93p' /Users/mkbabb/Programming/fourier-analysis/docs/tranches/A/audit/W3-button-ledger.md` | *"naked wrapper component forwarding `active` as `aria-pressed`; `<Button variant="glass" size="icon">` IS the surface"* — **DOUBLE quotes**; the single-quoted form is struck at all three sites (D-5, third site cured this round) |
| Q9 | Row 29 · §5 (OG-F1 lane evidence) | `INTAKE-ADJUDICATION-2026-08-03.md:233`, the **OG-F1** register row | `grep -n 'favors \*\*freeze-with-adoption\*\*' INTAKE-ADJUDICATION-2026-08-03.md` | `233:` — *"favors **freeze-with-adoption** — the measurements are already live-reproduced; R6 itself declared `NO_SUCCESSOR`, making the ruling cheap."* ✔ EXACT |
| Q10 | Row 30 (the relay packet's own trigger) | `fr-CanvasOverlayButton` **FR-COB-28** | `grep -o 'one relay letter, seven items, sent at F.W0/F.W1 open by whoever holds the mail seat' fr-CanvasOverlayButton.md` | ✔ EXACT — **and the "seven" is the BANK's word for a packet this wave grew to nine (items 0–8); the growth is the spec's act, stated outside the quotation, never edited into it** |
| Q11 | Row 30 (FR-MSP-12's honest tension) | `fr-MorphShapePreview:104` (the **FR-MSP-12** row) ⊕ `:22` (the fold admission) | `grep -n -o 'the synthesis is a coin-flip[^*]*fires' fr-MorphShapePreview.md` | **TWO forms**; round 1 published a **third** that is neither. Split at round 2 (R2-1-LAW.4); inline at row 30 |
| Q12 | Row 33 (FM-21's F.W0-adjacent question) | `fr-FourierMorphSvg:99` | `sed -n '99p' fr-FourierMorphSvg.md` | *"which scaling system owns control sizes is an F.W0-adjacent question the F.W4 ticket should pose, not answer."* ✔ EXACT — casing is the bank's; round 1's "POSE" was this spec's emphasis wearing a verbatim label |
| Q13 | Row 37 · §8 (PP-REDGATE's disposition) | `fr-PathPreview` **PP-REDGATE** | `grep -o 'The cure (ambient.*NOT as an F.W0 precondition.' fr-PathPreview.md` | *"The cure (ambient `declare module \"@mkbabb/latex-paper/theme\"` in env.d.ts, or a latex-paper types fix) lands WITH the toolchain bump, owned by F.W1/W2 — NOT as an F.W0 precondition."* ✔ EXACT |
| Q14 | §4 G-13 addendum (FR-EQR-33(b)) | `fr-EquationResult:74` | `sed -n '74p' fr-EquationResult.md` (PASS-2 §2 re-verified) | *"The `CopyFailureReason` narrowing + `timeoutMs` are **v7.0.0 facts** (K-12), so they bind at ANY ≥7 target"* ✔ EXACT |
| Q15 | §4 G-3 (the mail surface, D-8) | `fourier/docs/tranches/F/coordination/` — three letters, 2026-05-29 | `ls -la …/F/coordination/` + `head -1` ×3 | inline at G-3 — `F-OPERATOR-WINDOW.md` · `F-T-N1-status-field-drop.md` · `F-VHOST-CORRECTNESS.md` |
| Q16 | §4 G-13 drift receipt (D-6) | `/Users/mkbabb/Programming/glass-ui` HEAD | `git -C /Users/mkbabb/Programming/glass-ui rev-parse --short=8 HEAD` + `git -C /Users/mkbabb/Programming/glass-ui log -1 --format='%s'` | inline at G-13 — **two readings by this seat in one sitting, `87464122` then `5cd70d08`** (round 2's `eb2e9428` and round 1's `207bf174` are both far behind). **A dated reading, never a live fact** — and the one register row whose value is *expected* to fail the purge seat's re-run, which is G-13's argument, not a defect against it. The elided `…/glass-ui` path is completed to a literal one at round 3 (R3-9's class: a command a seat cannot execute as written is not a receipt) |
| Q17 | §6a lock 5 · §6b · §4 G-6 · §8 (the F.W1 roster's extent) | **F-W1 §4 Sequencing, intra-wave step 4** ⊕ **§4 cross-edge 1** | `grep -o 'The roster is [A-Z]* limbs and stays [a-z]*' F-W1.md` · `grep -o "FR-EQC-7's vaul-vue gate lands INSIDE the F\.W1 transaction, not before\." F-W1.md` | *"The roster is TWELVE limbs and stays twelve"* · *"FR-EQC-7's vaul-vue gate lands INSIDE the F.W1 transaction, not before."* — **R2-5: quoted from the charter, never re-derived here, at all four sites.** ▲ **Second command CORRECTED at the second repair pass**: as first written it read `…INSIDE[^.]*\.`, which returns *"…lands INSIDE the F."* — the class named at the foot of this register |
| Q18 | §6b SS-13 row (fr-PaperSearchInput residue 6) | `fr-PaperSearchInput:120` | `sed -n '120p' fr-PaperSearchInput.md` | inline at §6b — the round-1 re-dating (*"at the adopted pin"* for the bank's **"at 7.0.0"**) and the un-marked elision are **struck** |
| Q19 | §6b SS-13 row (the D-24 decider) | `fr-PaperSearch:86`, the **D-24 / K1** cell | `grep -n 'it needs the build, not a browser' fr-PaperSearch.md` | `86:` — *"(The D-24 emission decider is **F.W0**, not SS-13 — it needs the build, not a browser; ruling 6's rider owns it.)"* ✔ EXACT |
| Q20 | §6b **F.W7** row (the reciprocal) | **F-W7 §6 gate G-F7-7** ⊕ **§11's F.W0 row** — **G-F7-11 is cited by gate id and deliberately NOT quoted** | `grep -n 'F\.W7' F-W7.md` · `grep -rl 'Genuinely owed' waves/` · `grep -o 'the phrase has never existed here' F-W7.md` | inline at §6b — the halt condition and the dependency's content reproduce EXACT. ▲▲ **RE-WARRANTED IN REPAIR ROUND 3 (R3-3.1; PASS-3 D-1 convicted round 2's own strike-receipts).** Round 2 struck a G-F7-11 attribution — correctly — and warranted the strike with three pasted counts (`grep -c 'Genuinely owed' F-W7.md` → 0 · `'each for its true reason'` → 0 · `grep -rln … waves/` → this file alone). **All three return the opposite at the live bytes**, and they did when they were published: `F-W7.md`'s mtime **16:48:58** precedes this file's **16:58:39**, so F-W7 already held the phrase. **The receipts were never run.** They are retired — not re-pasted with new numbers, because R3-3.10 forbids banking any count of a live sibling — and replaced by two forms **claimed** to survive F-W7's next edit: ⟨cmd⟩ `grep -rl 'Genuinely owed' waves/` → the **SET** `F-W0.md` · `F-W7.md`, and ⟨cmd⟩ `grep -o 'the phrase has never existed here' F-W7.md`. **The SET receipt survived; the word-output receipt did not** — F-W7's round-3 write re-worded the passage and the quoted sentence returns ∅ today. Re-quoted by the purge seat from F-W7's live bytes: ⟨cmd⟩ `grep -o "an absence receipt that its own quotation falsified" F-W7.md` → *"an absence receipt that its own quotation falsified"* — **F-W7's own words about its own bytes, which is the whole strike**: F-W7's occurrence is F-W7 quoting this file's strike back at it, not G-F7-11 authoring the sentence. The conclusion stands, re-proved a third time; only its warrant was forged, and then twice re-forged by the cure. **The surviving generalisation: set-membership outlives prose; word-output does not** |

**SEVEN drifted or fabricated quotations were found and cured BY this register across round 2's two repair passes, none of them by attestation** (each pass attested that every kept coordinate had been re-read at the bytes; both attestations are what failed).

**Found by the first pass, against round 1's bytes** — the *paraphrase* class, where a quotation drifted while reading better than the bytes: **Q7** — two sentences from two homes fused under one attribution; **Q8** — a per-file "verbatim" fix that corrected two of three sites and left §6b carrying the struck single-quoted form; **Q11** — a parenthetical substituted inside quotation marks, converting a bank figure (*"7 files"*) into a cross-reference the quoted file does not make (*"FR-NP-8's"*); **Q18** — a version-bound claim silently re-dated from **7.0.0** to *"the adopted pin"*, with the elision worn as the bank's own punctuation.

**Found by the SECOND pass, against the FIRST pass's own bytes** — and these are the more instructive three, because every one was shipped *inside the cure for the class it belongs to*: **Q20** — a sentence attributed to F-W7's gate **G-F7-11** that F-W7 does not contain; **a fabricated authority minted by the fabricated-authority repair.** ⟨**Round-3 strike-within-the-strike (R3-3.1)**: this sentence originally warranted itself with `grep -c 'Genuinely owed' F-W7.md` → 0 and `grep -rln … waves/` → `F-W0.md` alone. **Both receipts were unrun and both are false**; they are struck here and Q20's row carries the count-free replacements. The class this note names is real and the strike stands — but the note *convicting* it was itself an unrun receipt, which is round 3's finding and this register's fourth consecutive failure at the same layer.⟩ **Q1** and **Q17** — two ⟨cmd⟩ notes whose commands do not return the words beside them (a line-break span; a `[^.]*` halting inside `F.W1`); **provenance notes that do not prove their own quote, shipped by the pass that made the ⟨cmd⟩ note mandatory.**

**The lesson this file publishes downstream, in its own voice, revised by what the second pass found.** The first pass named the class as *fluency* — a paraphrase that reads better than the bytes is the shape the eye passes over — and that is true but insufficient. The second pass found the harder class: **a repair seat trusts its own cures.** Q1, Q17 and Q20 were not sloppy readings of the bank; they were *written in the act of enforcing the quote law* and then never run, because a seat that has just authored a rule reads its own compliance as given. **So the check is not "did I quote from a command" — it is "did I RUN the command, this pass, and does its output contain these exact words".** A ⟨cmd⟩ that has never been executed since it was typed is decoration; a ⟨cmd⟩ whose output is a subset of its quote is worse, because it survives casual verification. **No later X·F wave may quote this spec, the corpus, or a sibling without its ⟨cmd⟩; a "verbatim" label with no command beside it or in a register like this one is a defect on its face, whatever the words say — and a command that does not reproduce its own quote is the same defect, one layer deeper.**

▲ **ROUND 4, AND THE SEQUENCE IS NOW COMPLETE ENOUGH TO NAME.** Round 4 found **two** more, and neither is a corpus quotation — the corpus quotations are clean, all twenty rows above reproduce, and every `grep -F` of a frozen-record span passes. The survivors migrated to the two surfaces the register never covered: **a quotation of a MOVING SIBLING** (§6b's bare `(M1)` against `F-W7.md:218`'s canonical pair — cured under R4-1.1 with a non-author `grep -Fc` and a hash pin) and **an IDENTITY IMPORTED FROM A RECORD'S OWN RESIDUE LIST** (`fr-PaperSearchDropdown residue 12` — which the corpus never contained, and which round 3 minted *while re-denominating the census* and PASS-3 then certified as resolving). **Five rounds, one class, five altitudes**: round 1 mis-attested a measurement; round 2 forged a quotation; round 2's second pass forged the receipt that convicted the quotation; round 3 forged the receipt that convicted the forged receipt; **round 4's predecessor invented an identity inside the instrument built to count identities.** The direction is consistent — **the defect always moves INTO the newest layer of verification machinery, because that layer is the least-audited bytes in the file.** The structural answer is not a sixth layer of self-published machinery: it is **`CENSUS-CANONICAL.md`** for identities and **`shasum`** for sibling bytes — two operands this file cannot edit and cannot forge. **Every previous cure was a better description written by the same hand that needed checking; these two are not.**

### 3b. CANONICAL ROSTER MAP — the 57, booked or cited, id for id (R4-10; authored at repair round 4, re-based at repair round 5)

**The operand, named once.** `docs/tranches/X/fourier/conformance/CENSUS-CANONICAL.md`, **§2 → `### F.W0 — 57 rows`**, sha256 **`3e0a9acb3381`**, stamped 2026-08-29, **errata round 5 §0.4 applied** ⟨*re-based at repair round 5: `CENSUS-CANONICAL.md` §0.4 **E5-1** homes `PP-GATE` here; §2's header now reads `### F.W0 — **57 rows**`*⟩. ⊘ *A stamped operand no wave may re-cut is only as good as its derivation; a six-record spot audit from this seat put two rows in the defect column and the amendment round sustained both.* **This table is a MAP, not a derivation**: every id below is copied from that roster, and where this file and the canonical disagreed, **the canonical won and the divergence is minuted at the row.** Under R4-10 the duty is binary — **BOOK the id, or CITE its holder by name** — and the two tables below are exhaustive over the roster. The falsifier is one command a hostile seat can run without this file's cooperation: **difference this map against the canonical's F.W0 roster.** No count in this section is taken over anything but the canonical.

| Record (canonical n) | Canonical F.W0 ids → booking site in this spec |
|---|---|
| **fr-AdminFlaggedPanel** (2) | `FR-AFP-54` → row 10 fold list · `FR-AFP-55` → **row 12**, G-6 + G-13 |
| **fr-AdminUserList** (2) | `FR-AUL-56` → row 10 fold list · `FR-AUL-58` → **row 33**, G-15(b) |
| **fr-AnimationControls** (1) | `M-15` → **row 37**, G-15(a) (with its ERRATA against the banked F.W0 routing) |
| **fr-App** (1) | `MG-θ` → **row 15**, G-7 |
| **fr-AppHeader** (1) | `FR-AH-33` → **row 10**, G-6 |
| **fr-BasisSelector** (1) | `i-3` (alias `LC-missed-7`) → **row 16**, G-7 |
| **fr-CanvasOverlayButton** (7) | `FR-COB-1` → **row 3**, G-10 · `FR-COB-3` → row 26 · `FR-COB-9` → row 26 · `FR-COB-10` → **row 4** · `FR-COB-15` → row 26 · `FR-COB-25` → row 26 · `FR-COB-28` → **row 30**, G-3 |
| **fr-CollapsibleSection** (2) | `F-2` (= C-5) → row 10 fold list · `i-3` (= C-17) → row 15 fold list + the do-not-hand-fix rider (§7b) |
| **fr-ContourSettings** (2) | `R-7` → **row 10** ⟨**id-keyed at repair round 4** — the row was cited in row 10's closing sentence (*"Both readers OVERRULED on MAJOR grading (fr-ContourSettings R-7)"*) but never named as a booking. It is the identity-fold ruling that sends this record's `C-13` to `FR-AH-33`'s manifest-hygiene home, which is why row 10 is its site⟩ · `C-13` → row 10 fold list |
| **fr-ConvergenceTimeline** (1) | `M-6` → row 10 fold list |
| **fr-EqCoefficientsPanel** (3) | `C-3` → row 10 fold list · `R-4` → **row 9**, G-6 ⟨**id-keyed at repair round 4** — row 9's head already reads *"FR-EQC-7 (= C-1, re-provenanced per R-4)"*, but under M-25 absorption is not a booking. `R-4` is the lockfile-forensics ruling that makes the vaul-vue break **working-tree-only and F.W1-commit-conditional**; it is the reason row 9's gate fires inside F.W1 rather than here⟩ · `FR-EQC-7` (= C-1) → **row 9**, G-6, §6b |
| **fr-EquationModeToggle** (1) | `FR-EMT-25` → **row 24**, G-1 (named LAND exception) |
| **fr-EquationResult** (3) | `FR-EQR-24` → **row 35** · `FR-EQR-29` → row 10 fold list · `FR-EQR-30` → **row 14**, G-6 (authored) + G-12 (landed) |
| **fr-EquationView** (1) | `C·D-27` → row 10 fold list |
| **fr-GalleryCard** (2) | `L·D-3` → row 15 fold list (VIZ_COLORS half + K-13) · `L·D-6` → row 16 fold list |
| **fr-GalleryFeaturedCarousel** (2) | `FR-GFC-7` → **row 11**, G-6 measured fact, §8 · `FR-GFC-21` → row 10 fold list |
| **fr-GalleryInfiniteGrid** (1) | `L-8` → row 15 fold list |
| **fr-GalleryMarquee** (1) | `GM-F6` (= L-19) → row 15 fold list |
| **fr-GallerySearchBar** (1) | `FR-GSB-31` → row 10 fold list |
| **fr-GalleryView** (2) | `FR-GV-28` (= L-25) → **row 34**, G-6 · `K7` (= L-13) → row 15 fold list |
| **fr-InfoCard** (3) | `FR-IC-1` (= F8-REACH-01) → **row 4**, G-10 · `FR-IC-2` → row 4 (retires unmeasured under DELETE, by FR-IC-23's one-edit law) · `FR-IC-25` → **row 38**, G-7/G-8/G-9 wiring |
| **fr-NotationPills** (2) | `FR-NP-2` (= L·D-1) → **row 5**, G-12 · `FR-NP-4` (= L·D-2) → **row 6**, G-12 |
| **fr-PaperSearch** (2) | `D-24` → §6b SS-13 row (the emission decider is F.W0, not SS-13) · `MISS-A7` → **row 7**, G-5, G-15(d) |
| **fr-PaperSearchDropdown** (1) | `C:S-2` → **row 25**, G-1 (second named LAND exception). **This is the record's ONLY F.W0 row** — the struck `residue 12` never was one (PASS-4 D-2) |
| **fr-PaperSearchInput** (1) | `MISS-LC2` → **row 8**, G-6 |
| **fr-PaperSidebar** (3) | `M1` → **row 1**, G-4 — the F.W0 half of the canonical pair **FR-NP-32 (≡ fr-PaperSidebar M1)**, and the reason row 1 books at all now that `FR-NP-32` cites to F-W1 · `C-m9` → row 10 fold list · `M7` → rows 15/18, G-9 |
| **fr-PathPreview** (5) | `L-2` → **row 37**, G-15(a) ⟨**id-keyed at repair round 4** — row 37's provenance bracket already read *"fr-PathPreview L-2/PP-REDGATE + K7"*. `L-2` is the reader's *typecheck-RED-at-HEAD* framing, **killed** at its record and re-routed as `PP-REDGATE`; the killed framing is itself the F.W0 row, and G-15(a) is the falsifier that settles it⟩ · `PP-TSSCOPE` → **row 17**, G-8 · `PP-NOTEST` → **row 18**, G-9 · `K7` → row 37, G-15(a) · `PP-GATE` (aliases `D-1` gate half · `C-10` · `L-3`) → **row 15**, G-7 ⟨**CITATION → BOOKING at repair round 5.** The canonical now reads `` \| `PP-GATE` \| `D-1` (gate half) · `C-10` · `L-3` \| `F.W0` \| **F.W0** <sub>*errata R5 E5-1*</sub> \| ``, and the record's own banked line is the warrant — ⟨cmd⟩ `/usr/bin/sed -n '49p' fr-PathPreview.md` → `14. **PP-GATE** (D-1 gate half · C-10 · L-3) — **MINOR · FOLD → F.W0** (banked: fr-GalleryFeaturedCarousel.md:56 FR-GFC-22 \`noUnusedLocals\` rider).` ⊘ **The unmeasured editorial gloss that shed this row is STRUCK**: every neighbouring cell in the citation table carried a home receipt and this one carried an argument — had the receipt been run at authoring, `D-1` would have surfaced then⟩ |
| **fr-SliderControl** (1) | `R-20` (= C-14) → **row 18**, G-9 |
| **fr-UserSlugBar** (2) | `FR-USB-21` → row 15 fold list (the 34 `catch (e: any)` correction) · `FR-USB-22` → **row 35** |

**57 ids, 29 records, 57 booked, 0 escaped.** ⟨*re-based at repair round 5: `PP-GATE` moved from the citation table to the booking table at errata **E5-1**, and `PP-DEAD` moved the other way at **E5-2** — the canonical homes it F.W3*⟩ Three were **id-keyed for the first time at repair round 4** — `R-7`, `R-4`, `L-2` — each already present in this file as *content* at the site now named, none previously carried as a booking. That is the M-25 defect this program exists to kill (*"a gate, a bounds item, a named cure with its locks, or an explicit EXCLUDED line"* — absorption is none of the four), and it is cured by naming, not by re-opening.

**CITED TO THEIR HOLDERS — ids this spec's rows touch that the canonical homes ELSEWHERE.** Under R4-10 these are **not F.W0 bookings and not escapes**: each is cited by holder, its F.W0 leg discharged inside a row that owns a different id. Any of them booked here would be **this wave's fabrication**.

| Id (record) | Canonical home | Where this spec cites it |
|---|---|---|
| `GAB-13` (fr-GalleryAdminBanner) | **F.W1** <sub>legs F.W3, F.W0</sub> | row 2, G-1 — the land-or-abandon minute is the F.W0 leg; the row is F-W1's |
| `FR-NP-32` (fr-NotationPills) | **F.W1** <sub>legs NO-WAVE-OWNER</sub> | row 1, G-4, G-5, row 30 item 0 — always as the canonical pair; the F.W0 half is `M1` |
| `FR-COB-2` (fr-CanvasOverlayButton) | **F.W1** <sub>legs F.W0</sub> | row 26 (F.W0 limb discharged by G-10's deletion; sizing lock is F-W1's) |
| `FR-COB-17` (fr-CanvasOverlayButton) | **F.W3** <sub>file-criterion §5.e</sub> | G-10's 8-site lift + §6b — sequenced in F.W0's commit, homed at F-W3 |
| `FR-IC-8` (fr-InfoCard) | **F.W1** <sub>legs F.W0</sub> | row 4's `./metric-badge` budget rider |
| `FR-IC-23` (fr-InfoCard) | **F.W3** <sub>file-criterion §5.e</sub> | row 4's one-edit law |
| `FR-EQR-33` (fr-EquationResult) | **F.W1** <sub>legs F.W0</sub> | rows 2/20 + G-13's addendum limb (b) |
| `C·D-11` (fr-EquationView) | **F.W2** <sub>legs F.W0</sub> | row 13 + §6b — F.W0 publishes the lattice, F.W2 inherits it |
| `L-7` · `M-10` (fr-EditorControlsDock) | **F.W1** <sub>legs F.W0</sub> | row 20, G-13 — the 8.0.0 pin correction and the callsites budget |
| `MISSED-D` (fr-EasingPicker) | **F.W3** <sub>legs F.W0</sub> | row 21, G-13, §8 — F.W0 states the trade-off, F-W3 owes the ruling |
| `C/m-6` (fr-EasingPicker) | **F.W4** <sub>legs F.W0</sub> | row 18, G-9 — the stale `test.fixme` |
| `FR-CP-32` (fr-CoefficientsPanel) | **F.W3** <sub>file-criterion §5.d</sub> | row 15's dead-code instance list |
| `M-N14` (fr-EquationPanel) | **F.W3** <sub>file-criterion §5.e</sub> | row 15's fold list (rider) |
| `D-i1` (fr-ContourSettings) | **F.W3** <sub>file-criterion §5.d</sub> | row 19, G-9 — the `test.fixme` on a false premise |
| `R-3` (fr-SliderControl) | **F.W3** <sub>file-criterion §5.a</sub> | row 19, G-9 — the axe keystone-route rider |
| `FR-GSB-6` (fr-GallerySearchBar) | **F.W3** <sub>file-criterion §5.e</sub> | row 16's fold list |
| `FR-GFC-22` (fr-GalleryFeaturedCarousel) | **F.W4** <sub>legs F.W0</sub> | row 15's rider |
| `L-12` (fr-GalleryInfiniteGrid) | **F.W4** <sub>legs F.W0</sub> | row 18, G-9 — the zero-paging-vocabulary spec |
| `FR-AUL-23` (fr-AdminUserList) | **F.W4** <sub>legs F.W0</sub> | row 39 + §8 — recorded **OPTION**, explicitly not taken |
| `FM-17..FM-19` band (fr-FourierMorphSvg) | **F.W4** <sub>legs F.W0</sub> <sub>band = 3 ids</sub> | row 36, G-15(c) — F.W0 owns only the freeze ruling + golden-file baseline |
| `FM-21` (fr-FourierMorphSvg) | **F.W4** <sub>legs SS-13</sub> | row 33's divergence minute ⟨**corrected at repair round 4**: round 3's re-denomination added `FM-21` as an F.W0 row on the strength of its *"F.W0-adjacent"* prose. The canonical homes it at **F.W4**, and *adjacent* is not *routed* — the minute recording what F.W0 takes and what it leaves stands unchanged, and is now correctly a CITATION⟩ |
| `C-3` · `D-M3` (fr-PaperSearchInput) | **F.W1** <sub>legs F.W0</sub> | row 10's fold list · G-12's 87-site denominator operand |
| `FR-PSD-CB` (fr-PaperSearchDropdown) | **F.W3** <sub>file-criterion §5.c</sub> <sub>legs: SS-13</sub> | row 7 + §6b's SS-13 row — the real identity behind the struck `residue 12` |
| `PP-LEN` (fr-PathPreview) | **F.W3** <sub>file-criterion §5.d</sub> | rows 16/18 — the asymmetric-failure rider |
| `PP-REDGATE` (fr-PathPreview) | **F.W1** <sub>legs F.W0, F.W1, F.W1/W2</sub> | row 37, G-15(a), §8 — the re-route OUT of F.W0 is the finding |
| `PP-DEAD` (fr-PathPreview) | **F.W3** <sub>legs: F.W0</sub> | **CITED to the holder, F-W3's register (R4-10)** — and the `noUnusedLocals` rider is F.W0's leg, carried at row 15. ⟨*errata R5 E5-2: the routing column read `*DEAD*`, a token that occurs nowhere on `fr-PathPreview.md:30`; this file quoted the falsifying bytes in its own §3 row 15 for two rounds and filed nothing. §0's disagree-or-amend clause is exercised at last — by the census, on this file's evidence.*⟩ |
| `FR-COB-24` (fr-CanvasOverlayButton) | **F.W1** | carried in §3 row 33's HEAD (`FR-AUL-58 ≡ FR-COB-24 (= D-28 / D-14)`); **CITED to F-W1** — discharged until now only by §8's exclusion row, which names the wave but not the holder |
| `AA-23` (fr-AdminAuditLog) | **F.W5** | not cited by this wave — recorded here only because R4-6.4 names its host row, which is F-W5's, never F.W0's |

**What this table replaces.** Round 3's 84 counted twenty-plus of these as F.W0 rows because their records mention `F.W0` in a leg, a rider or an adjacency. **The canonical's first-arrow rule homes an id ONCE**, and legs ride the home. Booking a leg as a row is how a program double-counts itself into a denominator no two seats can reproduce — which is exactly the pair of irreconcilable denominators R4-5 struck at two sibling waves. **F.W0's number is 56 because the canonical says 56.**

---

## 4. Gates — all born-RED

Every gate below is RED at authoring with its witness named. Commands are literal and run from `/Users/mkbabb/Programming/fourier-analysis` (or `web/` where stated). Witnesses stamped "fold" were re-measured 2026-08-28.

**ANCHOR STABILITY — binding on this file's consumers (R2-2.5).** These gates' citable identities are their **gate ids**: `G-1` … `G-15`, with `G-15(a)`/`(b)`/`(c)`/`(d)` for the four rulings. The artefacts they publish are cited by gate id plus artefact name — *"F-W0 §4 **G-11**, the corrected anchor table"* · *"F-W0 §4 **G-12**, the corrected-denominator table"* · *"F-W0 §4 **G-13**, the pin table + lattice"* — and this file's other stable anchors are its §-headings and its §3 row numbers. **A line cite into this file is FORBIDDEN downstream from this amendment forward**, and is a defect at authoring even if it resolves on the day it is written. The reason is measured, not theoretical: repair round 1 moved every heading in this section by **+12 lines**, staling the `F-W0.md:199` / `:204` / `:209` addresses that `conformance/PASS-1/RULINGS.md` R-9 and five sibling specs had published — every one of them then addressed a blank line, an "Owning rows" cell or the wrong gate under a masthead attesting it had been read at the bytes. **Round 2 does not re-publish corrected numbers; it abolishes the address.** Converting the sibling cites already written is the Re-anchor seat's act (R2-2.3), which runs last in this round; nothing downstream waits on it, because the gate id has always resolved and always will. F-W1's **§4 cross-edge 1 (`F.W0 → F.W1`)** already models the receiving end, and did so before this sentence was written — ⟨cmd⟩ `grep -o 'cited by gate id[^|]*' F-W1.md` returns two cells, the second of them the reciprocal: *"cited by gate id, never by line into a live sibling (R2-2; the round-1 `:199`/`:204` cites are struck as stale-by-construction); every later wave QUOTES them and no wave re-performs the re-resolution as its own act — F.W1's G1 is a quotation, superseded figures are forbidden downstream, and divergence from those tables is a defect against G-11."* **That is the idiom every consumer of this file uses.**

### G-1 — The 28-path M.W1a tree is SETTLED, per path, minuted
**RED witness** (fold): `git rev-parse --abbrev-ref HEAD` → `m/w1-bump-migration`; `git rev-parse --short HEAD` → `cd26c65`; `git status --porcelain | wc -l` → **28** (27 ` M` + 1 `??`; the full listing is enumerated at §2a-i, which IS the bounds authority, and was reproduced item-for-item by the independent seat at `conformance/PASS-1/F-W0-CHECK.md` §3).
**GREEN**: every one of the 28 paths carries a named disposition (**LAND / ABANDON / LAND-WITH-CORRECTION**) in `SUBSTRATE-LEDGER.md`, and `git status --porcelain` is empty **or** its residue is exactly the ruled-ABANDON set. **Rows 24 (FR-EMT-25) and 25 (C:S-2) are NAMED EXCEPTIONS that must LAND. A wholesale reset is a gate FAILURE, not a gate pass.**
**Owning rows**: 2, 24, 25, 3, 4 · FR-EQR-33(c).

### G-2 — The O-14 letter is COMMITTED
**RED witness** (fold): the sole `??` row is `docs/tranches/N/valuejs-inbound-2026-07-27-facility19-migration-table.md`.
**GREEN**: `git ls-files docs/tranches/N/valuejs-inbound-2026-07-27-facility19-migration-table.md` returns the path.
**Owning rows**: 31.

### G-3 — The fourier mail seat exists (E13 analogue) + a root CLAUDE.md, and both letters are logged SENT
**RED witness** (fold; **the surface INVENTORIED at repair round 2 — D-8**): `ls /Users/mkbabb/Programming/fourier-analysis/CLAUDE.md` → *No such file or directory* (re-run this round, unchanged). No mail-**ledger** file under the fourier docs root — ⟨cmd⟩ `ls /Users/mkbabb/Programming/fourier-analysis/docs/tranches/F/coordination/INBOX.md` → *No such file or directory*. COHESION §1 lists the surface as an unstarted F.W0 wave item. **But the coordination DIRECTORY is not empty, and round 1 never said so**: ⟨cmd⟩ `ls /Users/mkbabb/Programming/fourier-analysis/docs/tranches/F/coordination/` → **three extant letters**, all dated **2026-05-29** — `F-OPERATOR-WINDOW.md` · `F-T-N1-status-field-drop.md` · `F-VHOST-CORRECTNESS.md`. Their subjects, ⟨cmd⟩ `head -1` on each: *"# F — operator-window runbook (F.W3 binding)"* · *"# F-T-N1 — coordination ASK: drop the legacy `status` field from `FormattedPalette`"* · *"# F — vhost-correctness spec (inv-22 binding; F.α first land)"*. **The ledger is absent; the mail surface is not.**
**GREEN**: `docs/tranches/F/coordination/INBOX.md` + `CLAUDE.md` exist; **the three extant letters above are TRIAGED INTO THE LEDGER AT CREATION, each with a status and a disposition, before any new letter is logged** (D-8 — the seat is stood up over an inventoried surface, never an assumed-empty one; the two F.W3-binding/F.α-binding letters carry their wave routing, the F-T-N1 ASK carries its answer-or-defer state); the **P-1** letter (row 30 — the nine-entry packet, items 0–8, FR-NP-32 first, FR-MSP-12 as item 8, item 7 a NEGATIVE ask recorded, not sent) and the **P-6** letter (row 32 — PAW-32/38/39/45/46/56 + fr-PaperView's 6 LATEX-RELAY riders + fr-PaperSidebar L-11/M2) are logged **SENT with dates**. E13 binds thereafter: **no wave closes with unread mail. NO CRONS.** ▲ **Why the triage is a gate clause and not a courtesy**: row 31 binds E13 to this seat the moment it exists, and E13's closing law is *"NO WAVE CLOSES WITH UNREAD MAIL"* — so a ledger created blind over three unlogged letters makes them unread mail on day one and **F.W0 cannot close its own gate**. The inventory is what makes G-3 satisfiable.
**Owning rows**: 31, 30, 32.

### G-4 — The adopted producer stylesheet PARSES (producer-owned; green upstream, never by consumer patch)
**RED witness** (fold): installed `@mkbabb/glass-ui` = **4.0.0**; `grep -o '/\*' web/node_modules/@mkbabb/glass-ui/dist/styles/index.css | wc -l` → **17** and `grep -o '\*/' … | wc -l` → **8**, vs **15 / 6** in `src/styles/index.css`. `web/src/style.css:3` = `@import "@mkbabb/glass-ui/styles";` (lines 1–2 are tailwindcss / tw-animate-css). **FR-NP-32 (≡ fr-PaperSidebar M1)**'s repro with the app's own toolchain: `postcss([@tailwindcss/postcss])` → `CssSyntaxError: Unterminated string: 's own'`. ⟨**Canonical pair restored at repair round 4 (R4-9.8; PASS-4 D-9).** G-4 **is** the corrupt-dist gate, so R3-8's standing clause — *"everywhere the corrupt-dist gate is stated"* — reaches this witness, and rounds 1–3 stated it with a bare left half. Row 1's law admits no abbreviation exemption; the mitigation it offered (consumers cite row 1 rather than assert bare) governs consumers, not this gate's own face.⟩
**GREEN**: `postcss` with `@tailwindcss/postcss` over `@import "tailwindcss"; @import "@mkbabb/glass-ui/styles";` exits 0 — because **the producer fixed the emitter (relay item 0)** or because F.W0/F.W1 adopted a pin whose dist parses. **A consumer-side patch is a GATE FAILURE** (S-4).
**Owning rows**: 1, 30.

### G-5 — `npm ci && npm run build` runs to completion in `web/`
**RED witness**: two independent, still-live breaks — G-4, and banked **MISS-A7** (vite-8/manualChunks, "silently disables every build-based falsifier in all three challenge files"). Corroborating artifact state (fold): `git ls-files web/dist` → **0**; `ls -ld web/dist` → present, dated **`Jun 12 18:13`**.
**GREEN**: build exits 0 **and** the post-build readbacks fire — `/usr/bin/grep -c animate-collapsible` on emitted CSS (**FR-NP-32 (≡ fr-PaperSidebar M1)** pre-gate — ⟨canonical pair restored at repair round 4, R4-9.8 / PASS-4 D-9: this clause carried the *other* bare half, which is a **substitution**, not an abbreviation, and the one act row 1's law names by name⟩; **prediction: build succeeds, grep 0**) · FR-NP-10's one-line rule-order readback · **ruling-6's `/usr/bin/grep 'class\*=size-'` on emitted CSS, with the losing banked cell superseded by a DATED E-3 ADDENDUM** (fr-PaperSearch K1 vs fr-PaperSearchModal PSM-24/R-3 — both cannot survive; = G-15(d); **never an in-place registry patch — E-1/E-3, per row 7's re-route**). Then, and only then, `web/dist` is quarantined under the ledger's admissibility rule.
**Owning rows**: 7, 1, 27.

### G-6 — the manifest gate is AUTHORED and its born-RED facts MEASURED (a PRECONDITION-CHECK; the declare+lock landing is F.W1's)

**Re-cut at repair round 1 (R-4a).** As previously written, G-6 landed the manifest DECLARE+LOCK act at F.W0 — the exact shape row 9 forbids (*"This gate does not exist standalone: it is F.W0-authored and FIRES inside F.W1's atomic transaction"*) and the exact shape **G-15(a) declares forbidden** (*"unsatisfiable-by-construction on the ABANDON branch and gated on a later wave on the LAND branch — that shape is forbidden"*). It was also unconditioned on G-1's branch, so on ABANDON it would have declared a **4.0.0** peer set against a **committed 3.1.0** manifest (`git show HEAD:web/package.json` — glass-ui `^3.1.0`, keyframes `^2.2.0`, value.js `^0.10.0`). Both faults are cured here:

- **F.W0's act is AUTHORING + MEASUREMENT**, nothing more: it writes the gate, records the born-RED facts below, and publishes them as F.W1 sizing inputs.
- **The act is CONDITIONED ON G-1's branch.** On **LAND**, the measurements stand against the landed 4.0.0 manifest. On **ABANDON**, the measurements are recorded against the **committed 3.1.0** manifest and **the gate transfers to F.W1 unchanged** — no F.W0 exit criterion moves either way.
- **The DECLARE+LOCK landing is F.W1's**, inside the atomic transaction chartered at **F-W1 §4 Sequencing, intra-wave step 4** — **TWELVE limbs, re-quoted from the corrected charter at round 2 (R2-5), never re-derived here**: ⟨cmd⟩ `/usr/bin/grep -o 'The roster is [A-Z]* limbs and stays [a-z]*' F-W1.md` → *"The roster is TWELVE limbs and stays twelve"*, re-run this seat 2026-08-29 at **`F-W1.md`, pinned per PIN-PURGE-CERT.md §TABLE (2026-08-29)**. Pinned by **F-W1 §4's cross-edge 1 (`F.W0 → F.W1`)** — ⟨cmd⟩ `grep -o "FR-EQC-7's vaul-vue gate lands INSIDE the F\.W1 transaction, not before\." F-W1.md` → *"FR-EQC-7's vaul-vue gate lands INSIDE the F.W1 transaction, not before."* (**command corrected at the second repair pass**: the round-2 first pass stated this ⟨cmd⟩ as `…INSIDE[^.]*\.`, whose `[^.]*` halts at the period *inside* `F.W1` and returns only *"…lands INSIDE the F."* — a note that did not return its own quote, which under R2-1-LAW.1 is not a provenance note at all) — **which is the warrant for the twelfth limb** (it was returned INSIDE the transaction at repair round 1, R-4a; round 1's "eleven" reached its count by fusing the charter's last two conjuncts, a grouping F-W1's own top-level separators do not mark).
- **The "graph evaluates without a module-eval throw" clause is F.W1-DISCHARGED**, not F.W0's: it requires a runnable build (G-5), which requires producer-owned G-4, which F.W0 cannot guarantee and for which §7a grants G-4/G-5 honest-RED-close relief that G-6 never had.

**RED witness** (fold): `web/package.json` — `dependencies` = @mkbabb/glass-ui · @mkbabb/keyframes.js · @mkbabb/latex-paper · @mkbabb/pencil-boil · @mkbabb/value.js · @vueuse/core · katex · pinia · tw-animate-css · vue · vue-router. `devDependencies` = @axe-core/playwright · @playwright/test · @tailwindcss/postcss · @types/katex · @types/node · @vitejs/plugin-vue · **class-variance-authority · clsx · lucide-vue-next · reka-ui · tailwind-merge** · tailwindcss · typescript · vite · vue-tsc. **`vaul-vue`, `@lucide/vue`, `embla-carousel-vue` are in NEITHER block**; the working lock holds **zero** `node_modules/vaul-vue` and **zero** `node_modules/@lucide/vue` entries (fold grep). All five runtime devDeps are `"dev": true` in the lock ⇒ **`npm ci --omit=dev` fails TODAY.**
**GREEN (F.W0's half — authoring + measurement)**: `SUBSTRATE-LEDGER.md` carries the manifest gate as written text with (i) the four born-RED facts above measured and dated, **stamped with G-1's ruled branch**; (ii) the **prescribed transaction** named for F.W1 to land — the five runtime packages moved into `dependencies`; **`tailwind-merge` alone deleted** (genuinely dead, doc-comment-only); `vaul-vue` declared+locked **or** the barrel→subpath retirement landed (rows 34/35) so no edge needs it; `@lucide/vue` declared and present in the lock; the dead `@types/katex` row removed; (iii) the **`lane-frontend.md:70` / `web/DESIGN.md:33` "dead devDeps" contradiction corrected as an ERRATA ADDENDUM in the SAME act as the authoring** — this half is doc-side, inside F.W0's own bounds, and F.W1 must not delete live peers on an uncorrected doc. **`embla-carousel-vue` is NOT in the set** (row 11: optional peer, cannot fail `npm ci --omit=dev`; its declaration rides the F.W3/W4 adoption).
**GREEN (F.W1's half — the landing, named here so no reader mistakes the split)**: the prescribed transaction lands as limbs of **F-W1 §4 Sequencing, intra-wave step 4**; `npm ci --omit=dev` exits 0; the entry graph evaluates without a module-eval throw. **F.W0 is not green-or-red on this half and claims no credit for it** (FR-GIG-5 bar).
**Owning rows**: 10, 8, 9, 11, 12, 14, 34, 35.

### G-7 — An unused-code gate + a lint floor exist and are WIRED
**RED witness** (fold): `web/tsconfig.json` = **13 compilerOptions** (target, module, moduleResolution, lib, jsx, strict, noEmit, skipLibCheck, esModuleInterop, resolveJsonModule, isolatedModules, verbatimModuleSyntax, paths) — grep for `noUnusedLocals|noUnusedParameters|noUncheckedIndexedAccess` → **0**. `scripts` = `dev / build / preview / test:e2e / test:e2e:ui` — **no `lint`**. No eslint/prettier in either dep block.
**GREEN**: `noUnusedLocals` + `noUnusedParameters` set; `noUncheckedIndexedAccess` **decided** (row 16 — set, or minuted as deferred *with M-10's `BasisKey` unit named as its consumer*); a lint floor runnable **and wired into `ci.yml`** (FR-IC-25: an unwired gate is an ornament). **Predicted first-run yield, twice-executed independently: 16 findings / 12 files** (PP-GATE = challenge-L probe T1 + reader B; labels.ts rows are destructured locals, not params — the honest count stays 16/12). **The DELETIONS route F.W3/W4; formatting drift is fixed BY the gate, never by hand.**
**Owning rows**: 15, 16, 38.

### G-8 — The type gate covers its own build config and specs — or the exclusion is a minuted ruling
**RED witness** (fold): `include` = `["src/**/*.ts","src/**/*.d.ts","src/**/*.vue","env.d.ts"]`; exactly one tsconfig in `web/`. `vite.config.ts`, `playwright.config.ts` and every `web/e2e/*.spec.ts` sit outside `vue-tsc -b`. **Spec count re-measured at fold: 8** (contour-extraction · gallery · paper-performance · settings-persistence · visual-baseline · visualization-crud · **visualization-ux** · workspace-flow) — matching the banked 8 at `fr-AdminFlaggedPanel.md:110` · `fr-CollapsibleSection.md:65` · `fr-CanvasOverlayButton.md:74`; **a 7-spec figure is a superseded denominator and is forbidden downstream** (→ G-12).
**GREEN**: `include` (or a second project) reaches config + specs and `vue-tsc -b` is green over the widened scope, **or** the exclusion is recorded as a ruling with its reason.
**Owning rows**: 17, 15.

### G-9 — A unit-runner SEAT exists (the FLOOR's scope belongs to F.W9/W10)
**RED witness** (fold): `scripts` carries no unit runner; `vitest` in neither dep block. Banked: PP-NOTEST · R-20 (the SliderControl e2e's `input[type="range"]` fallback **can never match**; `fill()` dispatches one input event so **even an assertive spec cannot reproduce R-2**) · fr-EasingPicker (`test.fixme` at :192 on a two-majors-stale condition) · fr-GalleryInfiniteGrid L-12 (139-line spec, zero paging vocabulary). Axe keystone set does not reach `/equations`.
**GREEN**: a runner is installed, **wired into `ci.yml`**, and inside G-8's type scope, with **ONE asserting spec** proving the seat live; **and** the axe keystone set is extended to `/equations` with its justification prose corrected to the **4.0.0 `inert`** truth (authored, not run). **Explicitly NOT green-by-coverage** — F.W0 must not pre-empt the floor's scope.
**Disjointness with F.W9 (one sentence, declared identically at both ends — R-5)**: **F.W0's G-9 owns the SEAT** (a runner installed, wired into `ci.yml`, inside G-8's type scope, with ONE asserting spec proving it live) and **F.W9's G-F9-1 owns the FLOOR** (the spec population and its thresholds); the seat is a precondition of the floor, never a down payment on it, and neither gate may be discharged by the other's evidence. F-W9's harness chain names the F.W0 seat leg as its predecessor.
**Owning rows**: 18, 17, 19.

### G-10 — F8-REACH-01 and F8-REACH-02 ruled IN ONE BREATH, with the lift in the same commit as any rm
**RED witness**: both HOLDs are RED in `FOURIER-AUXILIARY-EIGHT-HOUR-SOURCE-DELTA-2026-08-03.md` — **F8-REACH-01** (`N.C11`, `web/src/components/equation/InfoCard.vue`, sha `b2c2e718…`/1,516) sits **one row above** **F8-REACH-02** (`N.C39`, `CanvasOverlayButton.vue`, sha `35f3db91…`/595). `InfoCard.vue` is still ` M` in the fold porcelain. Five DELETE orders never executed; skip self-reported at `partial-prior-run.json:207-211`, un-minuted.
**GREEN**: both files carry an exact **KEEP_WITH_MOUNT / ISOLATED_HARNESS / DELETE** ruling (R-6: one breath, both rows); the **STEP-2i divergence is minuted**; and **if DELETE**, FR-COB-17's 8-site `:aria-pressed` lift lands in the **SAME commit** — sites: **CanvasControlsDock ×5 (:54/:59/:71/:77/:87) · EditorControlsDock ×2 (:143/:148) · ConvergenceTimeline ×1 (:61)**. **L's list, NOT C's (K-11)**: C's version adds `aria-pressed` to `FullscreenViewer.vue:110`, a one-shot close **ACTION**, manufacturing FR-COB-4's own defect, and miscounts its own list as "six" for seven. **Line numbers are re-resolved via G-11 before the lift is applied; the record's anchors are never followed raw.** A bare `git rm` is a gate FAILURE. FR-COB-9/FR-COB-25 fold into this minute; the register **deadness column** is the surviving act.
**Owning rows**: 3, 4, 26.

### G-11 — ONE corrected anchor table published; every later wave quotes it
**RED witness**: every `fr-*` record's anchors read "working tree at `cd26c65`" — a tree no commit holds (G-1: 28 dirty rows). Drift registers: fr-ContourPreview 31 (six enumerated C-lane cells) · fr-EasingCurvePreview CITE (**one citation past EOF inside its highest-severity carry**) · fr-CollapsibleSection K-11 (six +1 slips) · fr-DarkModeToggle I-3 (~10–11 lines) · fr-GalleryDraftsSection m-18 (26 files dirty under a HEAD label) · fr-App K-14 · fr-AdminAuditLog K-13 · fr-CoefficientsSpectrum K-11 · fr-CanvasOverlayButton K-10 · fr-EditorControlsDock K-9. All currently NO-WAVE-OWNER with no terminal home.
**GREEN**: the table exists, is dated, carries **COUNTING UNITS beside every pattern** (pt-leaf K7's law: byte-offset vs occurrence vs line are *anchor/unit differences, not errors*), names the accuracy benchmarks in both directions (L-axis byte-exact at fr-ContourPreview; L's working-tree cites 76 lines adrift at fr-CanvasOverlayButton superlative 3), and states the standing rule: **producer-side evidence carries the producer COMMIT HASH, never the version string.** **Impossible to satisfy before G-1.**
**Owning rows**: 22, 20, 2.

### G-12 — ONE corrected-denominator table published; superseded figures FORBIDDEN downstream
**RED witness — the live contradictions**: 9 live `variant="glass"` / 7 files + 36 real `size="icon"` (35 live) [FR-COB-2/K-12] **vs** 87 template `variant=` / 36 icon / 35 files [fr-PaperSearchInput D-M3] — **reconciled, not averaged** (+9/+2 comment-resident, +17 non-Button) · shim **21 cartoon-card sites / 14 files** [fr-AdminAuditLog K-13, re-confirmed FR-EQC-3] vs the shim's own committed comment at `style.css:98-112` self-reporting "14 sites (13 files)" (stale by 7) vs lane-frontend's raw **25** (both right under their own denominator, fr-ContourPreview 27) · `text-admin-label` **7 / 4 files** (corrects reader-DU's 8; fr-AdminAuditLog concurs) · **9 route records = 7 lazy + 2 redirects + 1 alias over 6 distinct view modules** (corrects the census's "8 routes, all lazy"; = intake X-2) · **2 Teleport sites** · **4 CollapsibleSection usages / 3 files** · **10 sites / 8 files for L§3c** · **`--viz-amber` = `#9d6515` light / `#e8b96d` dark** (kills D/L's `#a3702f` and C's `#9d6415`, the latter inherited unre-derived by a reader) · **13 Button variant members, not 14; `link` carries no aria-pressed leg** (K-5) · **8 `e2e/*.spec.ts`** (banked ×3 — `fr-AdminFlaggedPanel:110` · `fr-CollapsibleSection:65` · `fr-CanvasOverlayButton:74` — and live-measured at the fold seat 2026-08-28; **any 7-spec figure is superseded and forbidden**) · **the ` M` SFC denominator, published ONCE here (R-9.4): 27 ` M` rows ⇒ 24 SFCs (RUN-BOARD.md, package.json, package-lock.json are not SFCs) ⇒ 21 remaining after the three named individually in §2a above that row ⇒ 19 genuinely untouched after the two named below it — the "22 remaining" figure §2a formerly carried is SUPERSEDED and forbidden** · **array-literal `loops[].cardinality` is FABRICATED** (row 5) · **deriver coverage 89/89 Button but 396/512 leaves unresolved — publish the split** (row 6) · every `tsconfig.app.json` citation is DEAD (row 14).
**GREEN**: the table is published **with its denominator definitions** (application-occurrences vs raw-grep-lines vs callsites — fr-ContourPreview 27's both-right reading), and **REGISTRY-FIRST is bound as a PRECONDITION, not a courtesy.** Every later wave cites this table rather than a challenge file.
**Owning rows**: 23, 5, 6, 17, 14.

### G-13 — The producer pin is re-derived at the ADOPTED commit; no F.W1 sizing quotes a 7.0.0 anchor
**RED witness**: producer `package.json` reads **8.0.0** (fr-EditorControlsDock M-10/K-9, seat-verified, both readers) — corpus, CENSUS, lane-frontend break-surface rows and the fr-CanvasControlsDock apotheosis are all stale. Corroborated at FR-EQC-16, fr-DarkModeToggle SR-2, fr-CoefficientsSpectrum B-4, fr-EasingPicker MISSED-D. HEAD drift chain: `4ef35e8d`→`7d9abe08`→`4a86570b`→`558c3fa3`→`e73ec80c` (FR-EQR-33(a): "the fifth-plus occurrence of declared-pin drift"). Installed consumer pin re-verified 4.0.0 at fold.

> **G-13 addendum, 2026-08-28**: producer HEAD was `207bf174` (glass-ui) **as measured at repair round 1 — re-measure at wave open; this sentence is a dated reading, never a live fact** (D-6). It was a SIXTH drift beyond the chain published above, which is retained as the historical record. The chain re-anchors HERE; '8.0.0' remains a version label, and per P-4 the pin cell is the COMMIT HASH. F.W1 re-derives at the ADOPTED pin per FR-EQR-33(a); FR-EQR-33(b)'s ≥7-binding facts (CopyFailureReason narrowing + timeoutMs, K-12) are named as surviving at any ≥7 target.

*(Addendum authored under E-3 — added, never a silent rewrite of the chain above. **FR-EQR-33(b) verbatim from `fr-EquationResult.md:74`**: *"The `CopyFailureReason` narrowing + `timeoutMs` are **v7.0.0 facts** (K-12), so they bind at ANY ≥7 target"* — the limb G-13's asymmetry clause demands be named, previously uncarried.)*

**Drift receipt — the reason the sentence above is dated rather than asserted (round-2 D-6).** Round 1 wrote *"live producer HEAD is `207bf174` … this seat, live"* as a present-tense fact. It has since drifted **three more times, and the third is this round's own** ⟨**re-pasted at repair round 3 under R3-3.8** — the round-2 receipt `eb2e9428` **no longer reproduces**, and a receipt that does not reproduce is fixed from live output or struck⟩: the PASS-2 check seat measured `ebb58a0f`; repair round 2 measured `eb2e9428`; and repair round 3 measured it **TWICE, in one sitting, and got two different answers**. First reading, this seat: ⟨cmd⟩ `git -C /Users/mkbabb/Programming/glass-ui rev-parse --short=8 HEAD` → **`87464122`** / `log -1 --format='%s'` → *"fix(fourier,demo/chassis): cure the three δ-owned π routes — the shader constant that was never declared, the dark plate that lifted instead of covering, and the ToC rung that fell short of AA"*. Second reading, **same seat, same session, minutes later, taken only to verify the first before banking it**: → **`5cd70d08`** / *"fix(dock,search): cure the four α-owned π routes — the gap with two owners, the ring the port cut off, the third site of the one-property collision, and the combobox that was not one"*. **`207bf174` and `eb2e9428` are both far behind; the chain stands at TEN recorded drifts, and the tenth accrued inside the very act of writing this receipt.** ⟨**Neither value is banked as current.** They are pasted as **two dated readings by one seat**, which is what they are, and the second exists in this file for one reason: it is the cleanest proof the program has produced that **R3-3.10 is a law and not a caution.** A seat that re-measured a figure specifically to avoid publishing a stale one still published a stale one — because the interval between measuring and writing is never zero. **The PURGE SEAT re-ran the same command over the settled bytes and got an ELEVENTH value** — ⟨cmd⟩ `git -C /Users/mkbabb/Programming/glass-ui rev-parse --short=8 HEAD` → `ac204dca`, this seat, 2026-08-28 — **and does not bank that one either.** It is minuted, not pasted into the sentence above, precisely because the receipt above is already correct as written: it never claimed either hash was current, and the eleventh drift is further evidence for its thesis rather than a falsification of it. **This is the one receipt in the eleven specs that the purge sweep leaves non-reproducing ON PURPOSE, because reproducing it would be the defect.** **This is why G-13's satisfiability was never staked on a number**, and why the GREEN clause above now carries no hash at all.⟩ **This is P-4's own failure mode reproducing inside the addendum P-4 was minted to justify**, and it is why the gate's satisfiability was never staked on the number: **G-13's MEASURE-AT-OPEN clause is the cure, and the drift is its evidence, not its refutation.** No hash written into this spec is a live fact; the pin cell is filled at wave open and the ADOPTED cell is PROSPECTIVE. **A later seat that finds `5cd70d08` stale has confirmed the rule, not found a defect** — this seat already did, twenty minutes after measuring it — the defect would be a spec that claimed otherwise, and round 3 found exactly that defect in G-13's own GREEN clause (D-9), where a `207bf174` **this receipt had already retired three paragraphs earlier** was still asserted *"as of this authoring"*. **The receipt was right and the clause depending on it was not**, which is why the strike lands in the clause and no replacement hash is written there.

**GREEN**: a pin table exists whose producer cell is a **COMMIT HASH, never a version string** (P-4) — **MEASURE-AT-OPEN**: F.W0 records the hash live at wave open ⟨**the parenthetical *"(`207bf174` as of this authoring)"* is STRUCK at repair round 3 (R3-3.9; PASS-3 D-9).** It was false when round 2 wrote it: three paragraphs above, this same gate's drift receipt already recorded `207bf174` as `HEAD~2`. **A GREEN clause is the last place a stale hash may hide**, because it is the sentence a later seat reads to decide whether the gate is satisfiable, and it is the one sentence the D-6 drift cure did not reach. **No hash is written into this clause in its place** — under R3-3.10 a producer HEAD is the archetype of a figure that is false the moment it is banked, so the clause now states only the LAW: the cell is filled from a command run at wave open, and the value lives in `SUBSTRATE-LEDGER.md`, never here⟩ and marks the **ADOPTED-hash cell PROSPECTIVE, to be filled by F.W1 at adoption**, because §6b states that F.W0 does not execute the bump and FR-EQR-33(a) gives the re-derivation to F.W1. **F.W0 is never red for a hash only F.W1 can name**; it is red until the table, the interim rule and the prospective cell exist. The census's 4→7 delta table is re-derived as **4→8** and re-homed for `EasingCurve` (row 21); **every producer `file:line` in the F.W1 budget is re-resolved at the ADOPTED hash — an obligation F.W0 STATES and F.W1 discharges**; the **pin LATTICE** is published (row 13: keyframes.js@4.3.0 hard-deps `value.js ^0.13.0` + optionalDeps `glass-ui ~4.0.0` — the backward pin that makes F.W2 un-landable alone) together with the **true peer start state** (row 12). **The table must state the asymmetry: at 8.0.0 every load-bearing 7.0.0 FINDING survives, but CURES die — and say which** (K-7's Card form). Migration budget expressed in **CALLSITES** (12/2/1 in 118 template lines), not imports.
**Owning rows**: 20, 21, 13, 12, 14, 23.

### G-14 — Codex residues and worktrees DISPOSITIONED; the pin-hygiene rule published
**RED witness** (fold): `ls /tmp/fourier-r4-files.sha256` → *No such file or directory* — **evaporated**. Its last-recorded size, **74,507 B**, is attributed at repair round 1 to the two in-tree documents that actually record it: `docs/tranches/V/apotheosis/pi/HANDOFF.md:306` (*"`/tmp/fourier-r4-files.sha256` is 74,507 bytes / SHA `e7054390…` and grants no…"*) and `docs/tranches/V/megatranche/formation/fourier/CENSUS-2026-08-03.md:360` (the F.W0 carry row: *"R4-12 (the `/tmp/fourier-r4-files.sha256` residue, 74,507 B, still on disk at fold — disposition, consume nothing)"*), both re-read by this seat. `ls ~/.codex/worktrees` → **`7e28`, `9167`, `d0be`** — three, not one; `7e28` has its own formation census at `formation/codex-worktree-7e28/CENSUS.md`, so its disposition is **not purely mechanical**.
**GREEN**: all three worktrees dispositioned; **R3-9** recorded as an unverifiable assertion ("never cite as method guarantee"); **R3-16+R4-11+R5-9+R6-10** reduced to the **14-wave M board** with the 72/158/P29/slot superstructure **explicitly NOT transferred**; **R4-12** recorded as *evaporated, consume nothing* with its date; **R5-3** re-hashed or dropped; **X-7** registered; the **pin-hygiene rule** published ("unverified Codex pins are re-hashed at use or dropped"); and the **dead M.W1b glass-4.1.0 gate** + the **J/K-deploy/M interlock** (live object `docs/constellation/tri-tranche-run/RUN-BOARD.md`, currently ` M`) declared **dead-by-supersession** (CENSUS:152, doubly dead at 8.0.0).
**Owning rows**: 28, 29 (flag only).

### G-15 — The four contradictions F.W0 must RULE are ruled in writing, each with its falsifier
F.W0 does not inherit contradictory bookings. Four rulings, each written into `SUBSTRATE-LEDGER.md`:

| | Ruling owed | RED witness | Falsifier / green |
|---|---|---|---|
| **(a)** | **The vue-tsc RED** (row 37): banked M-15 + fr-PaperView route it to F.W0; fr-PathPreview K7 kills all three routings. | `noUncheckedSideEffectImports` **absent** from the 13 compilerOptions (fold); `web/package.json` is in the dirty set; `ci.yml:95` = `npx vue-tsc -b --force`. **Falsifier re-cut at repair round 1 — NON-DESTRUCTIVE, and SEQUENCED AFTER G-1** (§6a lock 10). The previous shape (`git stash && … ; restore, re-run`) was the one witness in this spec that **wrote the fourier tree**, and it stashed `EquationModeToggle.vue` and `PaperSearchDropdown.vue` — the tree's ONLY copies of the two correct repairs rows 24/25 exist to protect — plus `web/package.json` and the lock, with no `stash pop` failure mode specified, and unsequenced against the very gate (G-1) that decides those files' fate. Row 2 forbids exactly that act. The replacement: **(1) read-only, available at any time** — `git show HEAD:web/package-lock.json` resolves typescript **5.9.3** / vue-tsc **2.2.12**, and `git show HEAD:web/package.json` carries the pre-bump pins, so the committed compiler default for `noUncheckedSideEffectImports` is **false**; banked corroboration already exists in row 37 receipt (4) — a flag-isolation run with the flag forced off emits **ZERO** diagnostics — so the committed-substrate-GREEN half is discharged from the record and the commit object, with **no working-tree mutation whatsoever**. **(2) After G-1 is GREEN** the settled tree IS the committed substrate (residue = the ruled-ABANDON set only), so `(cd web && npx vue-tsc -b --force)` runs against it with no stash at all; the tsbuildinfo it writes is gitignored (`.gitignore:43-44`) and CI is cold by construction anyway (row 38). **(3) The RED half is NOT re-manufactured here**: it is F.W1's born-RED witness at the adopted toolchain (`typescript ^6.0.3` / `vue-tsc ^3.3.5`), where the flag default flips true — F.W0 does not re-dirty a tree it just settled to reproduce a diagnostic the bank already reproduced four times. **Green = the ledger records: committed substrate GREEN, the RED is the uplift's, cure owned by F.W1/W2 (ambient `declare module` in `env.d.ts` or a latex-paper types fix riding the P-6 relay).** An F.W0 exit criterion of "vue-tsc green" would be unsatisfiable-by-construction on the ABANDON branch and gated on a later wave on the LAND branch — that shape is forbidden. |
| **(b)** | **The root-size fork** (row 33): does `html{font-size:1.125rem}` under 768px stay? | `web/src/style.css:40-50`; the 12.5% inflation compounding with `--ui-scale: 1.5` → **67.5px vs the token author's 60px**. | Green = a written fork ruling. **F.W0 POSES and RULES the fork; the token-parity re-tune is F.W1's; per-component execution is F.W4's.** |
| **(c)** | **FM-19**: regeneration path, or explicit frozen-forever with a golden-file diff. | No regeneration pipeline; 5 orphan JSONs; `lerpPoints` truncates at `Math.min(a.length,b.length)`. | Green = the ruling + the golden-file baseline exists. **FM-20/FM-23 cures are F.W4's**; FM-23's cure is a rider ON banked N-15 (a **uniform-length assertion** at `prepareFourierShape`, not an emptiness check). |
| **(d)** | **The emission contradiction**: fr-PaperSearch K1 (guard dies ON EMISSION) vs fr-PaperSearchModal PSM-24/R-3 (LATENT-on-next-build). **Witness re-cut at repair round 1 (the previous cell stated the blocker, not a measurement).** The contradiction is witnessed AT THE BYTES, this seat, live: `fr-PaperSearch.md:28` books K1 SUSTAINED — *"the fleet's registries currently DISAGREE: fr-PaperSearchModal **PSM-24/R-3** books the same guard LATENT-on-next-build … while this registry's K1 kills it on emission. Both cannot survive the first real 4.0.0 build"* — against `fr-PaperSearchModal.md:63` — *"**PSM-24 = D-14 (LATENT-at-the-adopted-pin, R-3)** … both intended icon registers (:68/:77 vs Input's `h-3 w-3`) collapse to one output on the first 4.0.0 build."* ⟨**Re-quoted at repair round 3 (R3-3.9; PASS-3 D-5).** Rounds 1–2 published this span with a **second, un-marked elision inside it** — ⟨cmd⟩ `grep -cF 'both intended icon registers collapse to one output on the first 4.0.0 build' fr-PaperSearchModal.md` → **0** — while the visible ellipsis sat at the head, so the reader was told where the elision was and it was somewhere else. ⟨cmd⟩ `grep -o 'both intended icon registers[^)]*) collapse to one output on the first 4\.0\.0 build\.' fr-PaperSearchModal.md` → *"both intended icon registers (:68/:77 vs Input's `h-3 w-3`) collapse to one output on the first 4.0.0 build."* — the parenthetical the cell had dropped, restored above at the bank's own spelling and **not re-punctuated**: the bare `:68/:77` carries no backticks in the bytes and gains none here. The asymmetry is what convicts it: the companion `fr-PaperSearch.md:28` quote **in the same cell** is byte-exact with its ellipsis correctly placed, so this was a drift, not a convention — and this row's own law, stated three times in this file (row 26 · §6b · §7b), is *"a quote presented as at-the-coordinate must survive `grep -F`."*⟩ Two adjudicated registry cells, mutually exclusive, both present. What is missing is not evidence but the DECIDER: the only artifact that could settle it is `web/dist`, a 3.1.0-era build dated **Jun 12 18:13** which the STALE-DIST law (row 27) makes inadmissible for anything the 4.0.0 install touched, and no 4.0.0 build can run while G-4/G-5 are RED. **RED = the contradiction stands, measured and unresolvable today.** | **GREEN, RE-CUT AT REPAIR ROUND 4 (PASS-4 D-6) — the ruling, with its falsifier; NOT the build.** ⟨The round-1–3 clause read *"Green = after G-5, `grep 'class\*=size-'` on the emitted CSS, and the losing banked cell is corrected in the registry."* **Two independent faults, and G-15's own sibling names both as forbidden shapes.** (1) **Unreachable on the branch this spec declares likely.** That clause chains G-15(d) ⇒ G-5 ⇒ G-4, and G-4 is producer-owned; §7a granted honest-RED-close relief **by name to G-4 and G-5 only**, so on the honest-RED branch G-15(d) could never go green — precisely the shape G-15(a) forbids in its own words: *"unsatisfiable-by-construction on the ABANDON branch and gated on a later wave on the LAND branch — that shape is forbidden."* (2) **The other half was a bounds violation**: *"corrected in the registry"* asks for an edit to a `registry/adjudicated/**` record, which §2a declares E-1/E-3 immutable and §7a lists verbatim as wave-invalidating.⟩ **Green = `SUBSTRATE-LEDGER.md` carries the WRITTEN RULING that the two cells are mutually exclusive and undecidable on today's evidence, naming (i) the two coordinates measured above, (ii) the decider — a 4.0.0 emitted stylesheet — and (iii) why it is unavailable (G-4 RED; `web/dist` inadmissible under row 27's stale-dist law), together with the FALSIFIER that settles it whenever it becomes runnable**: after G-5, `/usr/bin/grep 'class\*=size-'` on the emitted CSS, with the losing cell superseded by a **DATED E-3 ADDENDUM** — never an in-place patch. **§7a's honest-RED relief is extended to G-15(d) BY NAME**, on the identical ground it was granted to G-4/G-5: the wave closes with the ruling written and the emission untested, and *says so*. **F.W0 rules the contradiction; it does not manufacture the build that would end it.** |

**Owning rows**: 37, 33, 36, 7.

---

## 5. Owner rulings — FLAGGED INLINE, never presumed

**OG-F1** (row 29). *"Re-root fourier source auditing at F.W0, or leave the R-coordinates frozen and build from live-tree measurement alone?"*
- Lane evidence, verbatim: **"favors freeze-with-adoption — the measurements are already live-reproduced; R6 itself declared `NO_SUCCESSOR`, making the ruling cheap."**
- Cost of each branch: **freeze-with-adoption** → G-11's scope shrinks to drift-correction and G-14's R-coordinate rows become records, not work. **Re-root** → every R-coordinate is re-derived at the settled tree before G-11 can publish, and F.W1 sizing waits on it.
- **Interaction, stated because it changes another gate**: OG-F1 composes with **GAB-13's carried dissent** — worker-DU's grading instinct, preserved as the counter-position: *"the fleet has already elected to treat the working tree as the scope"*; **if F.W0 formally adopts the worktree as baseline, GAB-13 discharges to a disclosure line and DU's INFO grading was right all along.** If the owner rules **freeze-with-adoption AND worktree-as-baseline**, G-1's minute becomes a disclosure + the LAND set, and G-11 narrows to drift-correction only.
- Also open, declared not decided: **OG-V2** — "Do Codex-authored prohibitions bind the Claude-owned formation after M-15?"

**L-1's conditional, preserved verbatim** (row 3): *"if F.W0 rules ABANDON on the M.W1a tree wholesale, the governance limb falls to MAJOR; the deadness does not."*

---

## 6. Sequencing

### 6a. Locks carried into this wave

1. **F.W0 substrate pre-gates FIRST** (COHESION §0a, verbatim). **G-4 · G-1 · G-13 · G-12 precede F.W1 SIZING.** The bank's *"F.W1 runs a build anyway"* clause is **STRUCK**: **F.W1 would open on a red build**, and no uplift delta computed against a sheet that does not parse is trustworthy.
2. **G-4 gates G-5 gates the quarantine.** No build can run while the stylesheet does not parse; `web/dist` is not touched until G-4 and G-5 are green, because delete-before-rebuild destroys the only emission evidence the corpus has.
3. **G-1 gates G-11 gates G-10's lift.** Anchors cannot be re-resolved against a tree no commit holds; the 8-site lift is applied at re-resolved anchors, never at the record's raw line numbers.
4. **G-3 gates the SENDs.** The P-1 and P-6 packets name *"whoever holds the mail seat"*; the seat is created here, so no letter leaves before G-3.
5. **G-6 is a PRECONDITION-CHECK, not a landing** (re-cut at repair round 1, R-4a). F.W0 AUTHORS the manifest gate and MEASURES its born-RED facts, **conditioned on G-1's ruled branch** (ABANDON ⇒ measured against the committed 3.1.0 manifest, gate transfers to F.W1 unchanged). **The declare+lock act lands inside F.W1's atomic transaction** — the **TWELVE-limb** roster chartered at **F-W1 §4 Sequencing, intra-wave step 4** (re-quoted at round 2 per R2-5; see G-6's bullet and §3a for the ⟨cmd⟩), FR-EQC-7's vaul-vue gate INSIDE it per **F-W1 §4's cross-edge 1** — where rows 8/9/10/11/12/14/34/35 are limbs — **keyed to their banked ids at repair round 5 (P5-15)**: row 8 `fr-PaperSearchInput MISS-LC2` · row 9 `fr-EqCoefficientsPanel FR-EQC-7` (= `C-1`) · row 10 `fr-AppHeader FR-AH-33` (= `C-14`) · row 11 `fr-GalleryFeaturedCarousel FR-GFC-7` (= `D-6`/`C-8`) · row 12 `fr-AdminFlaggedPanel FR-AFP-55` · row 14 `fr-EquationResult FR-EQR-30` (= `C-10`) · row 34 `fr-GalleryView FR-GV-28` (= `L-25`) · row 35 `fr-EquationResult FR-EQR-24` ≡ `fr-UserSlugBar FR-USB-22`. `embla-carousel-vue` is **not** in that set (optional peer, row 11). The doc half is F.W0's and lands in the **same act as the authoring**: the `lane-frontend.md:70` + `web/DESIGN.md:33` "dead devDeps" contradiction is corrected as an ERRATA ADDENDUM, or F.W1 deletes live peers.
6. **G-10 is one commit.** Lift-then-delete; a bare `git rm` is a failure.
7. **Row 16 — `fr-BasisSelector i-3` (= `LC-missed-7`) — is a rider on `M-10`, never landed alone**, and carries the **WAVE-LOCK**: no `basisFilter` wiring without `normalizeBasisKey` (the store repeats the widening at `gallery.ts:38`). ⊘ **Keyed to the banked id at repair round 5 (P5-15).** Row numbers resolve under R2-2.5 so nothing was broken, but **a lock that does not name its id is the shape that let `R-7`, `R-4` and `L-2` sit un-keyed for three rounds in this very file** (§3b's three round-4 id-keyings).
8. **E-3 everywhere**: CENSUS and lane-frontend corrections are **addenda, never patches**. E-1: the 66 adjudicated records and the intake adjudication are immutable beside this spec.
9. **Pathspec commits only.** `scripts/dev/dev.sh` (value.js) is never staged.
10. **G-1 gates G-15(a)** (added at repair round 1). The vue-tsc falsifier's tree-touching leg runs **only after G-1 is green**, on the settled tree; **no `git stash` anywhere in this wave** — a stash would erase rows 24/25, the tree's only copies of two correct repairs, and the manifest hunk G-1 is still ruling. Its read-only leg (`git show HEAD:web/package{.json,-lock.json}`) is available at any time.
11. **No new carry ledger is authored by this wave** (R-3.2). The only citable carries in X·F are `carry/F-W1-CARRY.md` and `carry/F-W4-CARRY.md`, by path; this wave cites neither, because neither is its authority. Its bounds authority is §2a/§2a-i; **its census authority is `conformance/CENSUS-CANONICAL.md` §2's `### F.W0 — **57 rows**` roster, and nothing else** ⟨**re-based at repair round 4 (R4-3.3; PASS-4 D-3, MEDIUM–HIGH).** Rounds 1–3 wrote *"its census authority is `conformance/PASS-1/F-W0-CHECK.md` §1 plus the 66 records"* — a **check file**, named as half this wave's census authority, in the same file whose masthead, §3 head and §8 all outlawed exactly that. R3-4's law is unconditional (*"check files and rulings are measurements and cure-shapes, never quotable operands"*), and R4-3 sharpens it (*"a superseded census is never a denominator"*), so the lock did not merely lag the cure — **a Sequencing lock that licenses the operand the Carry and Closure sections forbid moves a defect rather than curing it.** Both halves are struck: the check file because it is a prior run, and the bare "66 records" because R4-10 forbids a wave to re-derive a roster from any source but the canonical. The PASS-1/2/3/4 checks remain citable as **prior runs** and as the record of what earlier seats measured.⟩

### 6b. Cross-edges — declared from this end (COHESION §2 reciprocity; the far end declares its half)

| To | Edge |
|---|---|
| **F.W1** (the atomic tri-package uplift) | F.W0's per-path ruling **fixes the tree F.W1 lands on**. F.W0 does **not** execute the bump — it rules whether the M.W1a hunk in `package.json`+lock **LANDS** or is **ABANDONED and re-derived inside F.W1**. ▲ **F.W1 is ONE atomic land-or-lose transaction — the TWELVE-limb roster chartered at F-W1 §4 Sequencing, intra-wave step 4, incl. the FR-EQC-7 vaul-vue manifest gate INSIDE per F-W1 §4's cross-edge 1 — cited whole, never restated here.** (Repair round 1: the three-limb restatement this cell carried re-fractured the transaction; the extent is now stated only by citation, though individual limbs are still NAMED where a local gate binds one. **Round 2, R2-5**: the count word is re-quoted from the corrected charter — *"The roster is TWELVE limbs and stays twelve"* — and the address converted to the stable idiom in the same act, per R2-2.4. **This spec does not re-derive the count in either direction**; if the charter ever moves again, this cell re-quotes it and nothing here re-counts.) The limb this wave's G-6 authors and F.W1 lands is the manifest banding; the limb FR-EQR-3 makes born-RED is the prop rewrite — the producer's DEV-mode retired-prop watcher (`Button.vue:131-146`, `console.error("[glass-ui] <Button> was given …")`) fires per mounted Button while the specs' filter lists pass `[glass-ui]` through, on a matrix that runs against the DEV server (`e2e.sh:102`). F.W1's born-RED witness is named for it: one dev-server run of the four gated specs at the adopted pin, before-and-with the rewrite. **F.W1 must not be credited for anything F.W0 owns — and per FR-GIG-5, must NOT be credited for the pagination-drain cure.** |
| **F.W1** (gate fires *inside* the other wave) | **FR-EQC-7 is an F.W0-authored manifest gate that FIRES INSIDE F.W1's transaction**, not a standalone F.W0 act. **This is now true of G-6 whole** (R-4a): F.W0 authors + measures; the declare+lock landing is a limb of **F-W1 §4 Sequencing, intra-wave step 4** per **§4's cross-edge 1**. F.W0's G-6 is red until the gate text and its branch-conditioned measurements exist; it is never red for the landing. Today's CI is **GREEN** on this axis; the failure fires the day the M.W1a bump commit lands. Rows 12 and 13 feed the same seam as **sizing inputs**. |
| **glass-ui BH relay (SS-6), NO-WAVE-OWNER** | **F.W0 owns ASSEMBLE + SEND; F.W1 owns CONSUME** — the answers (portal tiers, the unlayered P9 sheet, the color-mix fallback, the chip/press rungs, whether the v7/v8 pressed-paint removal was intentional) change the uplift's **break surface** and therefore its sizing. **Glass-producer rows ride the relay and NEVER become frontend hacks** (FR-COB-8 S-4: *"consumer must NOT patch locally"*). Item 7 is a **negative** ask — DOCK-ACTIVE is satisfied upstream, do not re-send. |
| **F.W10** (ADOPTION-ASKS reconciliation) | The **P-6** letters SEND at F.W0 open on the seat this wave creates; **F.W10 owns the reconciliation both directions and the terminal disposition of every deferred row** (`CENSUS-2026-08-03.md` **§4 item 11** — §4 is a flat numbered list 1..11 with no `4.x` subsections; the `§4.11` notation this cell formerly used named a subsection that does not exist, corrected at repair round 1). Each row is "NO-WAVE-OWNER locally" with a named external producer. |
| **F.W3 / F.W4** | FR-COB-17's 8-site lift has its **wave home at F.W3/W4** but is **sequenced with F.W0's `git rm` — same commit**. Two sweep-law riders carry forward and must be quoted there: **(a) — banked at `fr-CanvasOverlayButton.md:52` as the surviving limb of `FR-COB-3`, attributed here at repair round 1 (R-2c); previously quoted verbatim in this cell with no id — never key the vocabulary-consolidation sweep on a bare `is-active` grep, it corrupts the PROP at `PaperView.vue:344`** (five spellings live: `.is-active` / `.is-active-sub` / `.is-playing` / `.liked` / the prop); **(b)** FR-COB-4's toggle-vs-action law governs which sites may receive `aria-pressed` at all. **`FR-COB-15`'s surviving limb rides the same edge**: the standing wrapper policy, quoted at round 2 in **the form the bytes carry** (D-5, second site — this cell still held the struck single-quoted form after §7b and row 26 were corrected) — ⟨cmd⟩ `sed -n '93p' /Users/mkbabb/Programming/fourier-analysis/docs/tranches/A/audit/W3-button-ledger.md` → *"naked wrapper component forwarding `active` as `aria-pressed`; `<Button variant="glass" size="icon">` IS the surface"*, the ledger's row for `visualization/CanvasOverlayButton.vue:8`. **DOUBLE quotes.** A quote presented as at-the-coordinate must survive `grep -F`, and a per-file cure that fixes two of three sites is the drift class continuing under a repair label — see §7b for the full cite and §3a for the register row. **F.W1 precedes F.W3/W4** (COHESION §2 law) — no fourier frontend spec may assume the uplifted tree before W1 lands it. MISSED-D's trade-off **is F.W3's ruling to make**. Row 16's WAVE-LOCK also crosses here. |
| **F.W1 / F.W2** (ERRATA against a banked F.W0 routing) | **PP-REDGATE re-routes M-15's vue-tsc RED OUT of F.W0** — see G-15(a). The cure lands **with** the toolchain bump, owned by F.W1/W2; the latex-paper types arm rides the P-6 relay. |
| **F.W2** (value.js consumption) | C·D-11's **backward pin** is what makes F.W2 un-landable alone. F.W0 publishes the lattice; **F.W2 inherits it rather than re-deriving.** Negative scope fact recorded so F.W2 is not over-scoped (FR-AUL-55): value.js/keyframes consumption is correctly **ZERO** at some components. |
| **F.W9 / F.W10** | F.W0 owns only the **seats**; the **decisions and CI wiring** land at W9/W10 — the unit-test floor's scope (fr-App C-13, explicitly *"with MG-θ's F.W0 substrate row"*; `CENSUS-2026-08-03.md` **§4 item 5** books the vitest-floor decision at F.W4 — flat list, no `4.x` subsections; the `§4.5` notation is corrected at repair round 1), the axe pass on `/paper`, the lint script as a CI gate (fr-PaperSidebar M7). **Runner-seat disjointness, declared identically at both ends (R-5): F.W0's G-9 owns the SEAT; F.W9's G-F9-1 owns the FLOOR** — see G-9. Per FR-IC-25, any gate F.W0 adds must be wired into `ci.yml`/`deploy-pages.yml` **or it is an ornament** — that wiring is the W9/W10 half. |
| **F.W7** (the owner-gated trie design) — **THE RECIPROCAL, written at repair round 2 (R2-10.8 / R2-8.2(iii)); this end was the one genuinely owed** | **F.W7 DEPENDS ON F.W0, HARD, and F.W0 depends on nothing of F.W7's.** The ask is named by F-W7's own §6 gate **G-F7-11** (cited by gate id; **not quoted** — see the ▲ note at the foot of this cell) and its content is stated by F-W7's **§7c Cross-edges, the `**F.W0** substrate pre-gates` row**, quoted here at its bytes: *"No line anchor, no HEAD figure, no \"LIVE at certified HEAD\" citation until F.W0 re-grounds: 28 dirty entries, pin `14d83356` unresolvable, corrupt glass-ui 4.0.0 `dist/styles/index.css` — **`FR-NP-32` (≡ `fr-PaperSidebar M1`)** — GAB-13, F8-REACH-01/02."*

⟨**RE-PASTED AND RE-ADDRESSED AT REPAIR ROUND 4 (R4-1.1 ⊕ R4-4.3; PASS-4 D-1 HIGH and D-4).** The round-3 cure minted here is **revoked whole** and replaced from F-W7's live bytes. **Two independent faults, in the one cell round 3 rewrote end-to-end.**

**(i) The quotation drifted at the exact word a standing law names.** F-W0 printed a bare **`(M1)`** inside quotation marks presented as the sibling's own words. The bytes carry the **canonical pair**. Receipts, both forms, run this seat on the pinned toolchain: ⟨cmd⟩ `/usr/bin/grep -Fc 'dist/styles/index.css` (M1), GAB-13, F8-REACH-01/02.' F-W7.md` → **0**; ⟨cmd⟩ `/usr/bin/grep -Fc 'corrupt glass-ui 4.0.0 `dist/styles/index.css` — **`FR-NP-32` (≡ `fr-PaperSidebar M1`)** — GAB-13, F8-REACH-01/02.' F-W7.md` → **1**. **Non-author warrant, as R4-1.1 requires**: the PASS-4 check seat — which authored none of these bytes — ran the same pair and published `0` / `1` with the coordinate `F-W7.md:218`. **R2-7.5, restated by R3-8 and now standing as R4-9.8, is *"cite both, NEVER substitute"*, and row 1 of THIS file declares that law binding on every later wave** — so the file broke its own canonical-form law inside a quotation of the file that obeys it. F-W7's §7c minutes the export from its end (*"a standing canonical form is not suspended because a sibling quoted the uncanonical bytes first"*) and routes the correction here, correctly, writing no F-W0 byte to do it. **This is that correction.**

**(ii) The address named a real section that holds a different F.W0 row** — the most deceptive shape a mis-cite can take, because it survives a reader who checks that the section exists. ⟨cmd⟩ `/usr/bin/grep -oE '^#{2,3} (7c\. Cross-edges|11\. Dependencies)' F-W7.md` returns **`### 7c. Cross-edges`** and **`## 11. Dependencies`** — both sections exist, and the quoted words live **inside §7c**, which ⟨cmd⟩ `/usr/bin/awk '/^### 7c\. Cross-edges/,/^## 8\./' F-W7.md | /usr/bin/grep -c 'FR-NP-32'` → **`1`** confirms by containment. ⟨**THE THREE LINE COORDINATES ARE STRUCK BY THE PASS-4 TRULY-LAST PURGE SEAT, 2026-08-29, AND THE PROBE RE-CUT TO AN ANCHOR PROBE.** Round 4 published *"§7c Cross-edges at `:214`", "§11 Dependencies at `:299`", "the quoted words live at `:218`"*. Re-run at the pass-4 settle the same command puts §7c at **`:267`** and §11 at **`:352`**, and the quoted words at **`:271`** — **all three false by 53 lines**, F-W7 having been re-written after this cell quoted it (the hash rider at (iv) below carries the same conviction independently). They are STRUCK rather than re-pasted: **R2-2's anchor idiom forbids addressing a live sibling by line number at all**, so re-pasting three fresh numerals would re-arm the identical defect for the next round. The claim this cell actually makes is STRUCTURAL — *§7c holds the source, §11 holds a different F.W0 row* — and it is now warranted by anchor-and-containment receipts that survive the next edit of the file they describe⟩ F-W7's §11 *does* carry an F.W0 row and it says something else — *"**Depends on**: **F.W0** (hard, G-F7-7) — specifically **F-W0 §4's gate G-11, the corrected anchor table**…"*. Both rows are real; only one is the source, and the citation now names it.

**(iii) The old ⟨cmd⟩ could not return its own quote.** Round 2–3 warranted this span with `grep -n 'F\.W7' F-W7.md`, which returns lines containing `F.W7` — not the quoted sentence. Under R2-1-LAW.1 that is not a provenance note; under **R4-2.5** it is convictable on sight. The `grep -Fc` pair above is the replacement, and it is the form R4-1.1 prescribes.

**(iv) Hash-pinned per R4-8.3**, because this is a quotation of a file under concurrent repair: `F-W7.md` `shasum -a 256` first-12 at quote time = **pinned per PIN-PURGE-CERT.md §TABLE (2026-08-29)** (⟨cmd⟩ `shasum -a 256 F-W7.md | /usr/bin/awk '{print substr($1,1,12)}'`, this seat, 2026-08-29). A later rewrite of F-W7 is now **detectable**, not deceptive — which is the whole of what four rounds of count-, classification- and word-output receipts failed to deliver.⟩

**Declared from this end, three clauses.** (1) **What F.W7 consumes**: F.W0's published re-grounding — **§4 G-11's corrected anchor table** and **§4 G-12's corrected-denominator table**, landing in `fourier/docs/tranches/F/SUBSTRATE-LEDGER.md`. F.W7 **QUOTES** them; it never re-performs the re-resolution as its own act, and a divergence from them is a defect against G-11, not a rival measurement. F-W7's **§6 gate G-F7-7** carries the failure branch verbatim — ⟨cmd⟩ `/usr/bin/grep -oF 'If F.W0 fails, the wave HALTS' F-W7.md | /usr/bin/sort -u` → *"If F.W0 fails, the wave HALTS"*, this seat, at `F-W7.md`, **pinned per PIN-PURGE-CERT.md §TABLE (2026-08-29)** ⟨the round-2/3 spelling *"Same-⟨cmd⟩"* pointed at the `grep -n 'F\.W7'` probe struck above, so this quote inherited a warrant that never returned it; it carries its own now⟩: *"**F.W0 re-grounds.** Until then no HEAD figure and no line anchor here is citable (D-19) — including R-4's own and this fold's `atomdiff.py` read. **If F.W0 fails, the wave HALTS**"* — **F.W0 acknowledges that halt condition from this end: an honest-RED G-11 close (§7a) halts F.W7; it does not license F.W7 to proceed on stale bytes.** (2) **What crosses back: NOTHING.** F.W0 books **no** F.W7 registry row, mints none into F.W7's empty column, and takes no position on the trie question — that ruling is owner-owed and lives at **F-W5 §2 clause E16** ⊕ **§3 gate G7 ⊙**, cited by clause and gate id, never by line, per the instruction F-W5's own `F.W5 → F.W7` row publishes. **F.W7's routing census is a measured ∅ and that ∅ is a finding, not a gap** (R2-8, ruled CLOSED: no wave re-litigates `routedTotal`, and no F.W0 act may add a row to it). (3) **Sequencing**: F.W7 opens after F.W0's substrate pre-gates, alongside F.W5's clause predecessors — F.W0 imposes no further order and owes F.W7 no artefact beyond the two tables and this declaration. ▲ **STRUCK, and why G-F7-11 is cited but never quoted (round 2, second repair pass).** This row as first written attributed to G-F7-11 the sentence *"Genuinely owed, each for its true reason: `grep -c "F\.W7" $V/…/F-W0.md` → 0"*. **It is a FABRICATION**, and the strike stands — **but the three receipts round 2 pasted to prove it were themselves never run, and round 3 strikes them in turn (R3-3.1; PASS-3 D-1).** Round 2 published `grep -c 'Genuinely owed' F-W7.md` → **0**, `grep -c 'each for its true reason' F-W7.md` → **0**, and `grep -rln 'Genuinely owed' waves/` → **`F-W0.md` alone**. **Live, every one returns the opposite**, and did at publication: `F-W7.md`'s mtime **16:48:58** precedes this file's **16:58:39**. The honest warrant, taken this seat in the forms R3-3.10 permits for a live sibling — **set-membership and word-output, never counts** — is: ⟨cmd⟩ `grep -rl 'Genuinely owed' waves/` → the **SET** `F-W0.md` · `F-W7.md`; ⟨cmd⟩ `grep -o "an absence receipt that its own quotation falsified" F-W7.md` → *"an absence receipt that its own quotation falsified"*; ⟨cmd⟩ `grep -o "stopped being true the instant this file quoted F-W0's strike" F-W7.md` → *"stopped being true the instant this file quoted F-W0's strike"*. **Read together they prove the strike better than the counts ever did**: F-W7 says in its own voice that its single occurrence of the phrase is F-W7 quoting *this file's* strike back at it — so G-F7-11 did not author the sentence, and every occurrence in the lane is downstream of this spec's own fabrication. ⟨**PURGE-SEAT CORRECTION, round 3, R3-3.8 — and it is the round's fourth-generation instance of its own headline class.** The two word-output receipts round 3 substituted for round 2's false counts — `grep -o 'the phrase has never existed here' F-W7.md` and `grep -o 'F\.W0 caught and minuted its own fabrication before this seat read it' F-W7.md` — **both return ∅ at the settled bytes**: F-W7's round-3 write (mtime **18:44:35**, after this file's **18:29:10**) re-worded the passage, and the two sentences quoted here never survived into it. Re-quoted above from F-W7's live bytes, which say the same thing in the sibling's current words. **The lesson is now complete and belongs in the record**: round 1 asserted, round 2 counted, round 3 quoted word-output *because counts go stale* — and the word-output went stale too, for the identical reason. **Word-output is not stale-proof; it is merely stale-slower.** The only receipt in this cell that has survived all three rounds unchanged is the SET-membership one, because a set named by filename does not move when the file's prose does.⟩ **The escalation is the finding.** Round 2 convicted a fabricated authority with a fabricated receipt; the class did not merely recur, it moved one level up, from the quotation to the instrument built to police quotations. It was minted BY the repair that claimed to be curing fabricated authority — the R2-1c failure mode exactly, reproduced one wave over — and it is struck whole: quotation, attribution and figure. The figure was false too: G-F7-11's live cell reads **1**, not 0. **The deeper reason the quote is not replaced with the sibling's current words**: G-F7-11's owed-status cell is a *measurement of this file*, which **this row changes**. Banking a sibling's count of F-W0's own bytes would freeze a sentence guaranteed stale the instant the row lands, and would make F.W0's reciprocity turn on a number only F.W7's seat may re-take. So the gate is cited by **id** for the ask, and the stable halves are quoted at their bytes. ⟨**Round-3 correction, R3-3.10.** Round 2 discharged F.W7's *"announced but unwritten"* receipt with two **self-counts** — `grep -c 'F.W7' F-W0.md` → 3 and an `awk`-scoped `grep -c` → 1 — and defended them on the ground that a sibling's count of this file would go stale. **The reasoning was right and stopped one step short**: this file's count of *itself* goes stale for exactly the same reason, and both figures were changed by the very round-3 write that is correcting them. The discharge is therefore stated as **existence, not cardinality** — the form that cannot be staled by an edit that does not delete the row.⟩ ⟨cmd⟩ `awk '/^### 6b\./,/^---$/' F-W0.md \| grep -o '^\| \*\*F\.W7\*\*'` → the row header `**F.W7**` — **the edge row EXISTS in §6b**, which is the whole of what F.W7's receipt asks, and word-output says so without banking a number. **F.W7's seat re-measures at its own seat; F.W0 neither writes nor quotes that cell.** |
| **SS-4 / F.W5–W8** (declaration only) | F.W0 declares **no dependency** on SS-4 beyond hosting the mail seat that carries the co-signature thread. Carried forward for the SS-4 authors: **SS-4 must FLAG its owner rulings INLINE** (trie-vs-KISS et al.) and must **name the TA-4 value-side atomdiff restoration as a prerequisite OR re-scope explicitly** — never presume. Neither side's edits gate the other's waves. |
| **X·P (parse-that)** | **A declared NON-EDGE. Direct parse-that→fourier is FORBIDDEN** (standing routing law). Routing is PLAW-BIND: parser → value (X·V L1/L5 surfaces) → packed release → consumers. Declared here so no F.W0 act — mail, pin, or manifest — opens one. |
| **SS-13** (visual audit) | Unblocking, not scheduling. SS-13 residues that are **BLOCKED, not merely unrun**, are gated on G-4/G-5 and **must not be queued as independent visual work**: FR-NP-8's fresh-build reproduction · the vue-tsc-diagnostic-count-at-7.0.0 residue · FR-NP-10's rule-order readback · fr-PathPreview residue 6 · fr-PaperSearchInput residue 6 — **quote conformed to its bytes at round 2**: ⟨cmd⟩ `sed -n '120p' fr-PaperSearchInput.md` → *"`variant="ghost"` **at 7.0.0**: hard vue-tsc error vs silent fallthrough … the 87-site budget is typecheck-shaped only if the error fires"* (**an ellipsis marks the elision of *"(D-M3 limb 1 asserts both) — unmeasurable until F.W0 clears the banked MISS-A7 build break;"***). The round-1 spelling read *"at the adopted pin"* where the bank reads **"at 7.0.0"**, and elided the middle clause with an em-dash that read as the bank's own punctuation — **a re-dated quotation is a new assertion wearing a citation, and it is struck**. The re-dating was not even neutral here: the bank pins the fact to 7.0.0 while the adopted pin is 4.0.0 and the producer is at 8.0.0 (G-13), so the spec's paraphrase silently moved a version-bound claim onto a different version. Conversely, **fr-PaperSearch's D-24 emission decider is F.W0, NOT SS-13** — verbatim: *"it needs the build, not a browser."* **`fr-PaperSearchDropdown`'s SS-13 residue item 1 rides this row under its REAL banked id, `FR-PSD-CB`** — ⟨cmd⟩ `/usr/bin/sed -n '114p' fr-PaperSearchDropdown.md` — **CITED to its canonical holder F-W3** (`CENSUS-CANONICAL.md` §2, `F.W3` roster; `file-criterion → §5.c`, legs `SS-13`) and **booked nowhere here**: its `:114` prose does route *"F.W0 re-checks before any SS-13 pass is scheduled"*, and that re-check is discharged by G-4/G-5's gating of this whole row, but the ID is F-W3's and this wave neither books nor re-homes it. The phantom `residue 12` that stood in its place through round 3 is struck at row 7 (PASS-4 D-2). |

---

## 7. Agent units (serial: a → b → e → c → d → f)

| Unit | Owns | Gates |
|---|---|---|
| **F.W0.a** — substrate settle | the 28 paths; the per-path minute; the two named LAND exceptions; the STEP-2i divergence minute | G-1 |
| **F.W0.b** — the mail seat | `CLAUDE.md` · `INBOX.md` · the O-14 commit · assemble+send P-1 (nine entries, 0–8) and P-6 (6+6+2) | G-2, G-3 |
| **F.W0.e** — the three tables + rulings | anchor table · denominator table · pin table + lattice · stale-dist admissibility rule · Codex/worktree dispositions · the four G-15 rulings · the recorded OPTIONs · the CENSUS + lane-frontend **addenda** · the COHESION addendum | G-11, G-12, G-13, G-14, G-15 |
| **F.W0.c** — toolchain gates | `tsconfig.json` flags + `include` scope · lint floor · unit-runner seat · axe keystone-route extension · **`ci.yml` / `deploy-pages.yml` wiring** | G-7, G-8, G-9 |
| **F.W0.d** — manifest gate **AUTHORING** (re-cut at repair round 1, R-4a) | writes the manifest gate + its branch-conditioned born-RED measurements into `SUBSTRATE-LEDGER.md`, and the `lane-frontend`/`DESIGN.md` dead-devDeps ERRATA ADDENDUM. **Writes no `package.json`/`package-lock.json` byte** — the declare+lock landing is a limb of **F-W1 §4 Sequencing, intra-wave step 4**. | G-6 |
| **F.W0.f** — the REACH ruling | `git rm` + the 8-site lift, ONE commit | G-10 |
| *(producer-owned, no unit)* | G-4 goes green **upstream**; G-5 goes green when G-4 and MISS-A7 clear | G-4, G-5 |

### 7a. Triumvirate dispatch (mandatory: research + plan augment + redress)

- **Bounds expansion that invalidates the wave**: any product-source write outside §2a; any edit to a `registry/adjudicated/**` record, `lane-crud.md`, or the intake adjudication; any in-place patch of CENSUS/lane-frontend; any consumer-side patch of the producer stylesheet; any `git add -A`.
- **Gate failures not local-edit-recoverable**: **G-4 unanswered by the producer** (then G-5, the quarantine, and F.W1 sizing all stay blocked — **the wave closes with G-4, G-5 and G-15(d) RED and *says so***, it does not proceed on a stale dist). ⟨**G-15(d) added to this relief BY NAME at repair round 4 (PASS-4 D-6).** It was omitted through three rounds while its GREEN chained through G-5 to producer-owned G-4, which made it **unreachable on the branch this spec declares likely** — the shape G-15(a) forbids in its own words. The relief is granted on the identical ground the other two hold it: an exit criterion no act of this wave can satisfy is not a gate, it is a trap. **G-15(d)'s honest-RED close is the written ruling plus its falsifier** (see the gate's own cell), never the emission.⟩ Also: **G-1 cannot be ruled per-path** because a disposition is genuinely unknown (escalate, do not reset); **OG-F1 returns no ruling** and the honest default cannot be stated without contradicting a standing law.
- **Diagnostic loops whose third iteration halts**: the denominator table failing to reconcile the 21-vs-25 / 9-vs-87 pairs a third time; the anchor re-resolution failing on the same file three times; **the G-6 authoring failing to produce a branch-conditioned measurement set three times** (the `npm ci --omit=dev` run itself is F.W1's, per R-4a, and cannot halt this wave).

### 7b. Format, lint, verification, commits

- **Format/lint cadence**: no hand-formatting anywhere (fr-CollapsibleSection i-3). Once G-7's floor lands, it drives; before it lands, touched files are left as found.
- **Verification artefacts** (stored beside the ledger): the G-1 porcelain before/after · the G-4 postcss transcript · the G-5 build log + the three readbacks · **the G-6 gate text + its branch-conditioned measurements (no `npm ci --omit=dev` transcript is F.W0's — that run is F.W1's, per R-4a)** · the G-7 first-run finding list (measured against the **16/12** prediction) · the widened `vue-tsc -b` output · **the G-15(a) read-only pair (`git show HEAD:web/package.json` / `…package-lock.json`) plus the post-G-1 `vue-tsc -b --force` run — no stash artefact exists, by lock 10** · the INBOX SENT rows.
- **Commit plan** (pathspec only, in unit order): `docs(F.W0): substrate ledger + 28-path land-or-abandon minute` → the LAND set (grouped by disposition, the two exceptions named in the message) → `chore(F.W0): commit the O-14 letter` → `docs(F.W0): CLAUDE.md + INBOX — the E13 mail seat` → `docs(F.W0): anchor, denominator and pin tables; Codex dispositions; four rulings` → `chore(F.W0): toolchain gates + lint floor + unit-runner seat + CI wiring` → `docs(F.W0): manifest gate authored + born-RED measurements (landing is F.W1's)` → `refactor(F.W0): F8-REACH ruled — aria-pressed lift + rm CanvasOverlayButton` → value.js side: `docs(X·F): CENSUS + lane-frontend errata addenda; COHESION §1/§2`.
- **Archaeology** (every path rooted and `ls`-resolved at repair round 1, R-10; **one root CORRECTED at round 2 — D-1/P2-10**): in `value.js/docs/tranches/V/megatranche/` — `audit/codex-provenance/INTAKE-ADJUDICATION-2026-08-03.md` · `audit/codex-provenance/intakes/lane-fourier-r3-r6.md` (the bare `R3-7` home, `:78`) · `formation/fourier/CENSUS-2026-08-03.md` · **`coordination/FOURIER-AUXILIARY-EIGHT-HOUR-SOURCE-DELTA-2026-08-03.md`** — **root corrected: the round-1 spelling `formation/fourier/…` names a directory that does not hold the file.** ⟨**BOTH RECEIPTS RE-CUT AT REPAIR ROUND 4 — one command, one base, each complete (R4-2.4; PASS-4 D-7).** Rounds 2–3 declared ONE base for the bullet (*"in `value.js/docs/tranches/V/megatranche/`"*) and then ran two commands under it, of which **the second cannot execute from it**: there is no `docs/` beneath `megatranche/`, so `find docs -iname '*EIGHT-HOUR*'` returns *No such file or directory* from the declared base and resolves only from the repo root, which the bullet never states. **The bound this receipt establishes is load-bearing** — this bullet's own sentence says so — so a reader who cannot run the command cannot check the bound. Each command now carries its own absolute base and needs no prose to locate it⟩ ⟨cmd⟩ `ls /Users/mkbabb/Programming/value.js/docs/tranches/V/megatranche/formation/fourier/` → `CENSUS-2026-08-03.md`, `lane-crud.md`, `lane-docs.md`, `lane-frontend.md` — four files, **none of them the delta**; ⟨cmd⟩ `find /Users/mkbabb/Programming/value.js/docs/tranches/V/megatranche -iname '*EIGHT-HOUR*'` → six coordination files, the delta among them at `/Users/mkbabb/Programming/value.js/docs/tranches/V/megatranche/coordination/FOURIER-AUXILIARY-EIGHT-HOUR-SOURCE-DELTA-2026-08-03.md` (the other five: `KEYFRAMES-…-CURRENT-SOURCE-DELTA` · `VALUE-…-SOURCE-EVIDENCE-DELTA` · `CONSTELLATION-…-READINESS-RECONCILIATION` · `CONSTELLATION-…-CONVERGENCE-LAW` · `CONSTELLATION-…-CHECKSUMS.sha256`). **This is load-bearing, not decorative: row 3 and G-10 name that file as the RED-witness holder for F8-REACH-01/02, and R-10's LAW is that a bounds-keyed halt condition may not rest on a path that does not exist — the bound is the FILE, and the spelling must be the tree's.** The round-1 attestation that every path here was `ls`-resolved was therefore false for this row and is struck; this round re-ran the `ls` for all of them · `formation/codex-worktree-7e28/CENSUS.md`; in `fourier/` — `docs/audits/runs/2026-06-17-M-critique-audit/partial-prior-run.json` and the five DELETE orders: **`docs/tranches/A/audit/W3-button-ledger.md:93`** (⟨cmd⟩ `sed -n '93p' /Users/mkbabb/Programming/fourier-analysis/docs/tranches/A/audit/W3-button-ledger.md` → *"`<Button variant="glass" size="icon">` IS the surface"* — **DOUBLE quotes, D-5; the single-quoted "verbatim" is struck. This is `FR-COB-15`'s surviving limb, the standing wrapper policy; the id is joined to the cite here per R-2c, and the limb routes F.W3/W4**) · `docs/tranches/M/M.md:141` · `docs/tranches/M/design/M-design-language.md:70` · `docs/tranches/M/design/M-bump-migration.md:56` + `:199` · `docs/audits/runs/2026-06-16-M-deep-audit/A8-no-legacy-sweep.md:29`.

---

## 8. Excluded — with reason

**Closure is certified against THE 66 ADJUDICATED RECORDS — never against a check file, never against a carry** ⟨**re-based at repair round 3 (R3-4.1; PASS-3 D-2)**, superseding the round-1 R-3.1 basing. Rounds 1–2 stamped **82/82** over `conformance/PASS-1/F-W0-CHECK.md` §1. **Three faults compounded**: the number was two short of the corpus; the operand was a **check file**, which R3-4's standing law makes *FAIL BY CONSTRUCTION* — *"a gate whose operand is a superseded seat's enumeration can only ever re-find that seat's misses; it is structurally incapable of greening against the corpus"* — and no detector was stated, so the stamp could not be re-run by anyone. All three were cured at round 3⟩ ⟨**AND RE-BASED WHOLE AT REPAIR ROUND 4 (R4-3.3 / R4-10; PASS-4 D-3, D-5).** Round 3's cure — *"the 84 routed (record, id) pairs derived at §3's head under the detector published there"* — fixed the operand fault and left a subtler one standing: **a wave that derives its own roster is running a rival census**, and R4-3 now says so outright (*"a superseded census is never a denominator"*). Its detector was also convicted independently (PASS-4 D-5: first-position-only slash probe, blind to `F.W1/F.W0`), and R4-9.6 dissolves that whole question by moving the atomiser into the canonical's **§0.1**. **The closure therefore no longer counts anything.**⟩

**THE OPERAND IS `docs/tranches/X/fourier/conformance/CENSUS-CANONICAL.md`, §2's `### F.W0 — **57 rows**`, AND NOTHING ELSE** — never a check file, never a carry, never this file's own reading of the bank. **The roster lands WHOLE: 57 booked, 0 escaped, 0 fabricated**, mapped id-for-id at **§3b**, across §3's 39 agglomerated rows, §4's gate operands and Owning-rows entries, §6b's cross-edges and this section's EXCLUDED lines — the four booking shapes §3's head names. Every id the canonical homes at another wave is **CITED to that holder** at §3b's second table and booked nowhere here; every id the canonical rules `TERMINAL` or `UNROUTED` is a **census fact, not a wave debt** (R4-10). **The closure's self-test is one difference, not a detector**: set-difference §3b against the canonical's F.W0 roster. It is executable by a hostile seat against a document **this file cannot edit**, which is the property four rounds of self-published instruments never had. Each line below is a LIMB excluded from this wave, with its home.

| Excluded from F.W0 | Reason / home |
|---|---|
| The tri-package **bump execution** (glass-ui 4→8, keyframes, value.js) | **F.W1**, atomic land-or-lose. F.W0 rules only whether the M.W1a hunk lands or is re-derived there. |
| The **manifest DECLARE+LOCK landing** — the five runtime devDeps moved to `dependencies`, `tailwind-merge` deleted, `vaul-vue` + `@lucide/vue` declared and locked, the dead `@types/katex` row removed, `npm ci --omit=dev` exiting 0, the entry graph evaluating | **F.W1** — a limb of the **TWELVE-limb** transaction chartered at **F-W1 §4 Sequencing, intra-wave step 4** (R2-5; ⟨cmd⟩ at §3a), with FR-EQC-7's vaul-vue gate INSIDE it per **F-W1 §4's cross-edge 1**. **Excluded from F.W0 at repair round 1 (R-4a)**: landing it here is the shape row 9 and G-15(a) both forbid, and it is unconditioned-on-G-1 by construction. F.W0 keeps only the AUTHORING + branch-conditioned MEASUREMENT (G-6) and the doc-side errata. |
| The **`embla-carousel-vue` declaration** | **F.W3/W4**, riding the carousel adoption. It is an **optional** peer at glass-ui 4.0.0 (`peerDependenciesMeta`, seat-verified), so it cannot fail `npm ci --omit=dev` and does not belong in an all-or-nothing manifest transaction (row 11). |
| The **162-site prop rewrite** + the copied→status triple | **F.W1**, same change as the bump (FR-EQR-3). |
| The **`@lucide/vue` rename** (35 censused sites) | **F.W1** — current debt, rides the uplift (row 10). |
| The **`<Card class="cartoon-surface">` cure re-derivations** | **F.W1** — cures re-derive at the adopted pin (G-13's asymmetry). |
| The **token-parity re-tune** against a 16px root (FR-COB-24) | **F.W1** — F.W0 rules the fork only (G-15(b)); per-component execution F.W4. |
| The **vue-tsc TS2882 cure** (ambient `declare module`, or the latex-paper types fix) | **F.W1/W2** (PP-REDGATE's explicit disposition); the types arm rides the P-6 relay. |
| **MG-θ's dead-code deletions** (16 findings / 12 files, incl. the 34 `catch (e: any)` sites, `web/src/lib/equation/index.ts`, SvgFilters) | **F.W3/W4.** F.W0 lands the gate, not the cleanup. |
| The **unit-test FLOOR's scope** | **F.W9/W10** "with MG-θ's F.W0 substrate row"; `CENSUS-2026-08-03.md` **§4 item 5** books the vitest-floor decision at F.W4. F.W0 owns the SEAT only (G-9); **F.W9's G-F9-1 owns the FLOOR** (R-5). |
| **Un-`fixme`-ing B-3's keystone at `:133`** | **F.W3/W4** — a four-test operation on a tree F.W1 is about to move. F.W0 lands only the route extension + corrected justification. |
| The **axe pass on `/paper`** and the lint script **as CI gates** | **F.W9/W10** (fr-PaperSidebar M7). |
| The **Embla carousel adoption** (FR-GFC-7's body) | **F.W3/W4** — free-scroll+snap vs a transform track is a behaviour change; F.W0 declares the dependency only. |
| The **SegmentedTabs fold** (fr-EquationModeToggle closing) | **F.W3** — "no F.W1 dependency", routed by its own record. |
| **FR-AUL-23's base-layer `border-color` rule** | **OPTION recorded, not taken** — repaints every bordered surface F.W1 is about to re-tier. Primary cure is FR-AUL-6's `Input` swap at **F.W4**; if taken, re-measure after G-4/G-5. |
| **FM-20 / FM-23 cures** | **F.W4.** F.W0 owns only the freeze-or-regenerate ruling + the golden-file baseline (G-15(c)). FM-23's cure is a rider on banked N-15. |
| **M-10's `BasisKey` + `normalizeBasisKey` + `satisfies` unit** | **F.W3/W4.** F.W0 lands the enabling flag decision only (row 16), under the `basisFilter` WAVE-LOCK. |
| The **MISSED-D trade-off ruling** (EasingCurve adoption vs DU's ToggleChip route) | **F.W3 owes it.** F.W0 re-derives the delta table and **states** the trade-off; it does not pre-decide. |
| The **vocabulary-consolidation sweep** and the toggle-vs-action classification | **F.W3/W4**, quoted forward with both sweep-law riders (§6b). |
| **P-6 reconciliation** + terminal disposition of deferred rows | **F.W10** (`CENSUS-2026-08-03.md` **§4 item 11**). |
| **Consumer-side patching of the glass-ui stylesheet** | **FORBIDDEN** (SS-6 / FR-COB-8 S-4). A consumer patch is a G-4 failure, not a G-4 pass. |
| **OG-F1** and **OG-V2** | **Owner's.** Flagged inline (§5), never presumed. |
| Codex's **72 / 158 / P29 / slot superstructure** | **NOT TRANSFERRED** — Codex-internal. Only the 14-wave M board is dispositioned (G-14). |
| `/tmp/fourier-r4-files.sha256` **rows** | Never consumed; the artifact itself has **evaporated** — recorded with its date, not deleted (G-14). |
| A **`git rm` of `web/dist`** | Brief corrected (§1d.1): NOT committed — the act is quarantine-after-G-4/G-5, never rm, never before (FR-MSP-12's admissibility). |
| A **direct parse-that→fourier** act of any kind | **FORBIDDEN** standing routing law; declared as a non-edge (§6b). |
| **SS-4 / F.W5–W8 CRUD rows** | The other half. F.W0 hosts the mail seat their co-signature thread rides; it declares no other dependency. |
| The **EBADPEER reproduction** (fr-AnimationControls C-1 ≡ fr-BasisCanvas C-8 `npm ls` ELSPROBLEMS identity) | **Not re-booked** — ruled component-altitude INFO, reproduced ≥6× across the corpus; folds to census risk-1. |
| The **bundle-weight arm** of FR-EQR-24/FR-USB-22 | **Killed in the registry's own record** (`sideEffects` + root-keyed `vendor-ui`); must not be re-argued. |
| Relay **item 7 re-send** (DOCK-ACTIVE) | Negative ask — satisfied upstream (FR-COB-11); recorded in the packet, not sent as a defect. |
| Deleting **`cva` / `clsx` / `reka-ui`** as "dead devDeps" | **FORBIDDEN** — live glass-ui runtime peers on the button-chunk→`cn`→clsx chain. Only `tailwind-merge` is genuinely dead. The doc contradiction is corrected in the same act as G-6. |
| Any edit to the **21 remaining ` M` SFCs** (denominator per G-12: 24 SFCs − 3 named = 21; − 2 lift sites = 19 untouched) or any product source | Execution gate — disposition objects only; the specs await the owner's begin-word. |
| **Crons** on the mail seat | Owner order: NO CRONS — E13 sweeps are session-open acts. |

---

*L-18 rider (one line, reference not ceremony)*: landing the fifteen gates green makes this wave IMPLEMENTED, not ACCEPTED — two skeptic passes + a fresh-instance apotheosis sign it off; VERIFIED is the X·F release close's to stamp. L-20: this file re-validates against `docs/precepts/instructions/tranche/WAVE_SPEC.md` at every amendment.
