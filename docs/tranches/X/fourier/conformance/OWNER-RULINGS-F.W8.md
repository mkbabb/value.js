SERVED MODEL: claude-opus-5[1m]

# OWNER-RULINGS — X.F.W8

**Authored by**: X.F.W8 unit **e**, 2026-09-19 (sitting of record **2026-09-17**, the owner's
begin-word, COHESION **§0j**). **Spec**: `docs/tranches/X/fourier/waves/F-W8.md` §2a (this file's
create row) · §5c's **SS-4** row (its scope) · §1b (item ii's honest default) · §3 **J2/J3** (item i's
subject). **Status**: this file **surfaces**; it rules nothing. A seat does not rule.

---

## §0 What this file is, and — more importantly — what it is NOT

**Its scope is one sentence of the spec, and that sentence is the whole of its mandate.** ⟨cmd⟩ (base
`docs/tranches/X/fourier`) `sed -n '273p' waves/F-W8.md`, the §5c **SS-4** row's act cell, the clause
verbatim:

> *"Owner rulings FLAGGED INLINE per SS-4's charter; F.W8 surfaces **only** those F.W5's block does not
> own — **(i)** the fixture-direction law's exception set (FR-IC-6) and **(ii)** the re-scope branch
> should G4 decline TA-4, now carrying A-1's third limb. Everything else routes to
> `OWNER-RULINGS-F.W5.md` and is **consumed here, never re-ruled**."*

So: **two items, and nothing else.** §2 below carries item (i); §1 carries item (ii). §3 enumerates
what is consumed from elsewhere and is deliberately **not** re-stated as a ruling here, because
re-stating a ruling is how a second home is minted for a decision that has one (**R-5**'s one-home
rule; **FR-GIG-5**'s bar: no wave is credited with another's cure).

**This file books no canonical row.** F.W8's record-side roster is empty by the census of record
(`CENSUS-CANONICAL.md` §4.1), so every identity named below is a **citation to its holder** — never a
booking. A booked row here would be a fabrication under **R4-10**'s own word for it.

**E-3.** The spec, `OWNER-RULINGS-F.W5.md`, the contract artifacts, the 66 adjudicated records and the
frozen canonical are **immutable operands**. Where this file's measurement diverges from a banked
sentence, the divergence is recorded **beside** it, dated, and the banked bytes are left alone.

---

## §1 ITEM (ii) — the TA-4 re-scope branch: **RULED, AND RECORDED AS CONSUMED**

### 1.1 The ruling, quoted at its own bytes

⟨cmd⟩ (base `docs/tranches/X`) `sed -n '992,994p' COHESION.md` → **§0j.D**, verbatim, three lines
whole:

> **F-SS4REST** — **R1 (TA-4)**: **RE-SCOPE value.js out of the diff clause** (a one-sided §6 verdict,
> stated explicitly) — `atomdiff.ts` is wholly excised from value.js and restoring it is value-side
> authoring that would couple value.js's release train to the fourier contract (the §0i.1 logic).

⟨cmd⟩ `grep -n 'F\.W8' COHESION.md | grep -v 'KF\.W8'` → **one line, `:987`** — the **F-PRODRET** home
sentence, and nothing else. Re-run at this seat's own clock, reproducing the open seat's and units
`a`/`b`/`d`'s readings: **no addendum §0k–§0v re-opens, moves or adds an F.W8 ruling.**

### 1.2 What the ruling does to this wave, stated once

| the spec's branch (§1b `:105`) | disposition under F-SS4REST R1 |
|---|---|
| **(b)** EXPLICIT RE-SCOPE — *"the prototype's diff leg is one-sided — fourier-only — and the union's diff verdict is a one-sided verdict, said out loud in the walk record and in every fixture row that touches it"* | **THE RULED BRANCH.** The spec's honest default and the owner's word coincide; the default is no longer a default, it is the ruling |
| **(a)** TA-4 **restoration as a PREREQUISITE** (all three limbs, owner = the value.js API row) | **DEAD.** Not a deferred branch, not an open option, not a residual: the owner ruled the opposite and the rationale is recorded in the ruling's own words |

**A-1's third limb, disposed rather than left hanging.** The spec's amendment A-1 widened TA-4's
restoration scope to a **third** limb — the excised conformance fixture `api/test/conformance/diff.test.ts`,
deleted in the same commit as `lib/crud/atomdiff.ts` (⟨cmd⟩ `git show --name-status --oneline a8ff7792`
→ `D` on both; re-run at this seat's baseline, record §B.3). Under branch (a) that limb would have been
a value-side obligation. **Under the ruled branch (b) it is not an obligation at all**: there is no
restoration, so there is no third limb to restore. The measured reading stands unchanged and is
**evidence, not a work item** — **0 extant · 1 landed-then-excised · 0 reciprocated**.

### 1.3 Where the branch is already declared aloud — and therefore not re-declared here

- `conformance/union-prototype-walk.md` **§1** (unit `a`) — G1's branch declared aloud, carried into
  both diff cells (**X-F**, **X-V**) and the §4 per-verb row.
- `conformance/fixture-register.md` — the value-direction rows carry the one-sidedness at every row
  that touches diff.
- `coordination/value-to-fourier-2026-09-DD-fn6-reader-and-fixture-register.md` **§3** (this unit) —
  the relay says it to fourier in terms, so the counterparty is not left to discover a one-sided
  correspondence by running into it.

**CONSUMED, NEVER RE-RULED.** This section re-opens nothing, adds no condition to the owner's word, and
asks nothing further of the owner on TA-4.

---

## §2 ITEM (i) — the fixture-direction law's exception set (FR-IC-6): **OWNER-OWED and UNRULED**

*(authored on the declared default only — §2.4 branch (A), and no other)*

### 2.1 The measurement that makes this an ask rather than an assumption

⟨cmd⟩ (base `docs/tranches/X`) `grep -n 'FR-IC-6\|fixture-direction' COHESION.md` → **no output**
(double-run). The exception set is named in **no** §0j.D row and in **no** later addendum: it is not
ruled, it is not implicitly ruled, and no seat may read the absence as consent. It is surfaced here
because SS-4's charter routes it here and to nowhere else.

### 2.2 The law it is an exception to, and the law's own generalisation

The spec's **§3 J2** act cell, the constraint verbatim:

> **THE FIXTURE-DIRECTION LAW, this wave's own acceptance constraint: every fixture is generated FROM
> the operation model, never from the client union.**

and its generalisation at the same cell: ***the operation must own its value domains, or the client's
narrowing is fiction.*** **G4** makes it mechanical: *"A row sourced from a client type is a gate
failure by construction."*

### 2.3 The exception set, as MEASURED at the register (three members, closed)

Consumed from `conformance/fixture-register.md` **§1.3** (unit `b`, 2026-09-19) — quoted by section
label, never re-derived here:

| # | exception | why it is an exception | the register's disposition today |
|---|---|---|---|
| **X-1** | `fr-InfoCard` **FR-IC-6** — the client leaf invents `EquationTier` (`web/src/lib/equation/types.ts:2`) over the operation's unconstrained `tier: str` | the **client** narrows a domain the operation never constrains; a client-sourced fixture would assert the narrowing and never exercise the backend-adds-a-tier limb | **the LAW's own subject, not an exemption from it** — no client-sourced row is admitted |
| **X-2** | the three **URL-builder** edges (`thumbnailUrl` · `overlayUrl` · `imageUrl`) | they never traverse `coreFetch`, so they carry **no envelope** — there is no wire body to generate a fixture against | **NAMED, no fixture**; the boundary posture is asserted in the walk, not in the register |
| **X-3** | `fr-BasisSelector` **m-7**'s 422-straddle | **K-3 KILL LOCK — REFUTED FROM SOURCE** (`AnimationData` is server-computed; the client-reachable bound 4096 ⊇ [1,500]) | **NO ROW CITES IT** |

**The set is closed at three, and silence is not a fourth.** Every id is record-qualified (**R-5**);
`m-7` and `FR-IC-6` are cited to their holders and booked nowhere.

### 2.4 The question, stated as a closed question with its branches

**Q.** Does the exception set stay a **STRIKE list** — named, reasoned, fixture-free until the
operation owns its domain — or may a member be admitted as a **client-sourced fixture row** with its
one-sidedness declared?

| branch | what it means | what it costs | falsifier |
|---|---|---|---|
| **(A) STRIKE — the honest default this wave carries** | each member is NAMED AND STRUCK in the register with its reason (J3's idiom); the fixture arrives only when the operation constrains the domain (F.W5's **D7** on the operation side; the client guard is **F.W4**'s) | the relabel limb stays **unasserted** until the operation model moves — the defect remains detectable only by reading, not by a test | a register row whose `source operation model` cell resolves under `web/src/` or `demo/`: ⟨cmd⟩ `awk -F'|' '/^\| \*\*F8-FIX-/ {print $5}' fixture-register.md \| grep -n 'web/src/\|demo/'` → **no output** at these bytes |
| **(B) ADMIT a bounded exception** | a member may carry a client-sourced fixture, marked as such, provided the set stays closed and each member states what it **cannot** exercise | **G4 must be amended by name** — today it reads *"a row sourced from a client type is a gate failure by construction"*, so branch (B) is not available to a seat: it is available only to the owner, in writing | the same probe returns a hit, and the hit is lawful only if this ruling exists |

**This wave authors branch (A) and no other**, exactly as it authored G7's ruled branch and no other.
Branch (A) is the spec's own default, so the register is conformant under it today; if the owner rules
**(B)**, the register's §4 strike rows are re-opened **by dated addendum-beside**, never by rewriting a
landed row.

### 2.5 What this ask does NOT hold up

**No F.W8 gate is gated on it.** G4 is unit `b`'s and closed for F.W8 under the default with its split
verdict; **G14** and **G15** — this unit's two gates — do not read the exception set at all. The ask is
surfaced because it is owed, not because anything of this wave waits on it. A seat that finds this file
and reads *"unruled"* as *"blocked"* has read it wrong; a seat that reads it as *"settled"* has read it
worse.

---

## §3 Everything else — CONSUMED from its own home, and NOT re-ruled here

**From `OWNER-RULINGS-F.W5.md` §1 (the nine), consumed by id, quoted nowhere here to avoid a second
home**: **R1** (TA-4 → §1 above, the one item SS-4 routes to this file) · **R2/F-TRIE** (NO TRIE) ·
**R3/F-PRODRET** (PRODUCER, as a port homed at F.W8 — discharged **in the register**, `fixture-register.md`
§8, because F.W8 owns no product source: the port is *specified* with its operation model and its
authority class, and the **bytes are the fourier API row's act**) · **R4** (REMOVE the affordance) ·
**R5** (STOP MINTING `[]` — the branch unit `d` proved, and the only branch it authored) · **R6** (KEEP
the hard-delete arm) · **R7** (CODEGEN — the register either IS the generated shape or documents why it
is not) · **R8** (REMIX + BORN-PRIVATE) · **R9** (DELETE the dead session subsystem).

**From §0j.D directly**: **OG-F1** — *FREEZE-WITH-ADOPTION AND WORKTREE-AS-BASELINE*; C31's denominator
stays **30/37 terminal**, **32/38 only if lawfully replaced**, and unit `c` left it exactly where the
owner froze it. No re-cut denominator is published by this wave on any seat's authority.

**One standing consequence worth writing down once**: of the spec's four ⊙ owner-gated conditions (G1,
G7, G12, and G3's denominator freeze), **all four were ruled before this wave opened**. The spec's
*"Silence is RED"* clause at G1 therefore never had to be exercised — and the reason it never had to be
is that the rulings were consumed by id at open, not presumed at close.

---

## §4 Receipts

- **Quote-by-command**: every quotation above is the pasted output of a `sed`/`grep` run **by this seat,
  this sitting**, with the command beside it. A *"verbatim"* label with no ⟨cmd⟩ is a defect per se in
  this sub-tranche, and there is none here.
- **Anchors**: COHESION is cited by **§-anchor and ruling id** (`§0j.D`, `F-SS4REST R1`); the spec by
  **section and row label**; `F-W5.md`'s rulings by **ruling number**. The two line figures that appear
  (`:992-994`, `:987`, `:105`, `:273`) sit **inside pasted command output** as this seat's dated
  reading, never as the citation (**R2-2**).
- **Self-count, read from the settled bytes and double-run**: ⟨cmd⟩ `grep -c '^## §' OWNER-RULINGS-F.W8.md`
  → **5** top-level sections (§0–§4); ⟨cmd⟩ `grep -c '^| \*\*X-[123]\*\*' …` → **3** exception rows;
  rulings **surfaced** here: **1** (item i, unruled) ⊕ **1** recorded as consumed (item ii, ruled
  elsewhere) — **0 ruled by this seat**.
- **Bounds**: this file is one of the four paths in unit `e`'s writable set. Zero fourier bytes in any
  verb; zero product bytes; `scripts/dev/dev.sh` never staged.
