# F-W2 — FRESH ADVERSARIAL SPEC CHECK (L-18 / L-20, PASS 2)

**Spec under trial**: `/Users/mkbabb/Programming/value.js/docs/tranches/X/fourier/waves/F-W2.md` (376 lines, `Status: planned`, repaired 2026-08-28 13:44:45)
**Corpus authority**: the 66 `fr-*.md` in `/Users/mkbabb/Programming/value.js/docs/tranches/V/megatranche/registry/adjudicated/` (+ `library-band.md`). In-tree carries = `carry/F-W1-CARRY.md`, `carry/F-W4-CARRY.md` ONLY (`ls` this seat).
**Seat posture**: FRESH. Every figure below was re-derived from the bytes this session; PASS-1's roster was read only AFTER the independent derivation closed, and is used as a cross-check, never as an input.

**CENSUS DETECTOR (both-dash law, R-6)**: `grep -nE 'F[.·–-]?W2'` over all 66 records — U+002D and U+2013 both admitted.
Result: **68 occurrences / 57 lines / 25 records**, and the *sole surviving form is `F.W2`* (`grep -ohE 'F[.·–-]?W2' fr-*.md | sort | uniq -c` → `68 F.W2`; zero `F-W2`, zero `F–W2`). The en-dash leg is empty **here** — recorded as a measured negative, not skipped.

**VERDICT: DEFECTIVE.** routedTotal **54** · booked **54** · escaped **0** · 2 BLOCKER-class + 6 MAJOR + 4 MINOR.

The census axis — the axis that convicted PASS 1 — is now **CLEAN**: all twelve PASS-1 escapes land by banked id, and this seat's independent re-derivation finds **no thirteenth**. The wave fails on a different axis: **AUTHORITY REALITY**. Seven cited coordinates do not hold their quoted content at the bytes, three seat measurements are refuted by their own witness commands, and the wave's hardest sequencing lock (the same-commit `hexToRgba` rider) is sized at a count its own enumeration and its own gate command both contradict.

---

## 1. ID-KEYED CENSUS — the routed set, re-derived

### 1a. Literal-detector members (43 ids / 25 records)

Every row below was walked back from the `F.W2` hit to its enclosing banked id (S-8 enumeration law), and each id was then located in `F-W2.md`.

| # | banked id (record-qualified) | receipt line | lands in F-W2 |
|---|---|---|---|
| 1 | `fr-AdminAuditLog AA-20` | :57 *"F.W2 must widen or exclude with reasons"* | C1 · G7 · R-i |
| 2 | `fr-AdminUserList FR-AUL-55` | :103 *"Scope fact for **F.W2**"* | A1 · D3 |
| 3 | `fr-App C-2` | :47 *"the census's F.W1-before-F.W2 sequencing is wrong for this leg"* | A1 dissent · G10 · §6a |
| 4 | `fr-CanvasControlsDock C-25` | :96 *"this file's F.W2 migration cost is zero"* | D1 · G11 · D3 |
| 5 | `fr-CoefficientsSpectrum B-3` | :41 | B5 |
| 6 | `fr-CoefficientsSpectrum M-10` | :62 | B5 ⟨provenance⟩ |
| 7 | `fr-ContourPreview row 1` | :34 ADJUDICATED → F.W2 | B1 |
| 8 | `fr-ContourPreview row 14` | :47 ADJUDICATED → F.W2 | B2 |
| 9 | `fr-ContourPreview row 22` | :55 ADJUDICATED → F.W2 | B3 |
| 10 | `fr-ConvergencePlot RD-5` | :26 | B5 |
| 11 | `fr-ConvergencePlot D-1` | :48 | B7 · §6b · §7 (cross-ref) |
| 12 | `fr-ConvergencePlot L-M6+C-1` | :70 *"→ the banked census identity … **F.W2**, latent at HEAD"* | A1 |
| 13 | `fr-ConvergencePlot C-19` | :88 *"**F.W2** rider on the specifier edit"* | A3 · §2b item 7 |
| 14 | `fr-ConvergencePlot C-22` | :109 *"**F.W2** budget note"* | A4 |
| 15 | `fr-ConvergencePlot K-11` | :127 *"AMENDS the banked ramp identity's F.W2 angle"* | B6 |
| 16 | `fr-ConvergenceTimeline S-9` | :123 *"no purchase here"* | D3 |
| 17 | `fr-ConvergenceLegend C-2` | :54 | B7 · G5 |
| 18 | `fr-ConvergenceLegend C-15` | :83 | B5 · B6 |
| 19 | `fr-ConvergenceLegend C-1 + U-2` | :104 | A1 |
| 20 | `fr-ConvergenceLegend K-7` | :124 | A1 · §7 |
| 21 | `fr-EquationPanel D-3+C-5` | :48 *"F.W4 + F.W2 rider"* | C2 |
| 22 | `fr-EquationModeToggle FR-EMT-24` | :54 | D3 |
| 23 | `fr-EquationView SC-4` | :36 | A3 |
| 24 | `fr-EquationView C·D-11` | :69 | E1 · G10 |
| 25 | `fr-EquationView M-RTC` | :72 | B9 · G8 · R-ii |
| 26 | `fr-EquationView I-5` | :127 | A4 · §7 |
| 27 | `fr-EquationView K-5` | :138 | A1 |
| 28 | `fr-EquationView K-6` | :139 | A4 |
| 29 | `fr-FunctionInput C·S-1/S-2 item 6` | :112 *"already at the F.W2 target posture"* | D3 (preservation) |
| 30 | `fr-GalleryInfiniteGrid C-5` | :49 *"FOLDS BY REFERENCE to banked FR-GFC-8 (**→ F.W2**)"* | B4 · G4 |
| 31 | `fr-GalleryInfiniteGrid FR-GIG-17` | :78 *"**→ F.W2 rider**"* | A5 |
| 32 | `fr-GalleryFeaturedCarousel FR-GFC-8` | :39 | B4 · G4 |
| 33 | `fr-ImageUpload row 13` | :47 *"invisible to any import-specifier-keyed F.W2 inventory"* | B12 · G11 |
| 34 | `fr-HarmonicLevelGrid HLG-4` | :50 | B10 |
| 35 | `fr-HarmonicLevelGrid superlative 3` | :113 *"the F.W2 remedy hierarchy … adopted"* | B1 · B10 · §6a |
| 36 | `fr-MorphShapePreview C·S-2` | :138 *"Preserve verbatim through F.W2"* | D3 (preservation) |
| 37 | `fr-MobileFloatingToc item 9` | :126 | D3 |
| 38 | `fr-NotationPills FR-NP-9` | :47 *"F.W3/W4 cure, **F.W2 tie**"* | B11 · G11 |
| 39 | `fr-PaperView C-08` | :72 | D3 |
| 40 | `fr-PaperArticleWindow K-24` | :303 *"the inventory must be exact"* | D2 · §2b |
| 41 | `fr-UserSlugBar FR-USB-38` | :81 *"do NOT book this component into the F.W2 migration surface"* | D3 |
| 42 | `fr-Tooltip FR-TT-22` | :56 *"A zero-cost row on the F.W2 ledger"* | D3 |
| 43 | `fr-GalleryView superlative 5 (C·C-1)` | :118 | E2 named fold · G9 |

**Non-members with `F.W2` in their bytes, correctly excluded from the routed set** (their TERMINAL disposition is another wave; the token appears only as a cross-reference): `fr-GalleryInfiniteGrid C-6` (:48 — *"→ F.W1"*, *"Rides banked FR-GFC-8's F.W2 wave-lock"*) · the seven per-record verdict/taxonomy lines (`fr-ContourPreview:30,:137` · `fr-ConvergencePlot:42,:177` · `fr-ConvergenceLegend:42,:174` · `fr-EquationView:41,:205` · `fr-CoefficientsSpectrum:157` · `fr-GalleryInfiniteGrid:148` · `fr-AdminAuditLog:142` · `fr-EquationView:74`), each of which restates a routing already booked above.

### 1b. Non-literal members (11 ids) — W.L5 ACT routings ⊎ the P-9 export

The W.L5 detector (`grep -nE 'W\.L5' fr-*.md`) returns exactly **7 records**: fr-AnimationControls (:120, :192) · fr-CanvasControlsDock (:96) · fr-ConvergencePlot (:48) · fr-ConvergenceLegend (:54) · fr-GalleryFeaturedCarousel (:39) · fr-InfoCard (:50) — six of them already literal members; the seventh contribution is `fr-AnimationControls C-18`. The easing half of ACT(1) is banked in five records that carry no `F.W2` token; the P-9 export (`F-W5.md:263`) names four further zero-cell records.

| # | banked id | receipt | lands in F-W2 |
|---|---|---|---|
| 44 | `fr-EasingPicker L/B-1 + C/B-1` | `:52` — *"no `\".\"` export … `timingFunctions` deleted … **14 of 22 silently drift analytic→CubicBezier-approximation** (8 stay analytic incl. ease-out-cubic/-expo)"* | **A6** (cite; home F.W1) · A1 · A2 · G15 · G20 |
| 45 | `fr-EasingPicker MISSED-F` | `:89` — *"the F.W1 '5 sites / 6 symbols' costing double-counts"* | **A6** · A1 site-vs-cost |
| 46 | `fr-EasingCurvePreview CENSUS-MC` | `:58` — *"the value.js-specifier half … is NOT blocked on the uplift"* | **A6** · G10 · R-iii |
| 47 | `fr-MorphPhaseConfig MPC-2` | `:46` — *"FOLD → fr-EasingPicker L/B-1 + C/B-1 · F.W1 (… **never a specifier rewrite**)"* | **A6** (verbatim) · R-iii |
| 48 | `fr-FourierMorphSvg r2 easings/value.js fold` | `:46` — the `as EasingFn` rider | **A6** · A2 Unwrap lock |
| 49 | `fr-InfoCard FR-IC-20` | `:50` — *"fires at the census-adopted `value 0.13→4.0`"* | **A7** |
| 50 | `fr-AnimationControls C-18` | `:120` + `:192` — *"the colors.ts deletion identity stays with census W.L5"* | **D4** · D3 · G11 |
| 51 | `fr-CollapsibleSection` zero-cell | `:27` — *"One identity, one booking, forever"* (no component-level value.js id minted) | **D3** (by record) |
| 52 | `fr-PaperSearch C-Z1/C-Z2` | `:57` — *"load-bearing zeros re-run by δ"* | **D3** |
| 53 | `fr-PaperSearchInput C-13/C-14` | `:107` — *"each with its own falsifier"* | **D3** |
| 54 | `fr-PaperSearchDropdown C:C-23/C-24/L:§5` | `:77` — *"Load-bearing zeros: sole producer import `glass-ui/button`"* | **D3** |

### 1c. Closure

**routedTotal = 54 · booked = 54 · escaped = 0.** Grep receipts that the twelve PASS-1 escapes landed (over `F-W2.md`): `L/B-1` **12** · `MISSED-F` **7** · `CENSUS-MC` **10** · `MPC-2` **8** · `FourierMorphSvg` **6** · `FR-IC-20` **3** · `C-18` **8** · `C·C-1` **4** · `CollapsibleSection` **3** · `PaperSearch C-Z1` **4** · `PaperSearchInput` **3** · `PaperSearchDropdown` **3** (all were **0** at the fold seat).

**No thirteenth escape found.** Adjacent-surface sweeps for terminal routings onto F.W2's own acts returned nothing new: `facility` → 1 record (fr-App, already a member) · `bare.root|bare specifier` → 12 records, **all** already members · `cssVarToHex|hexToRgba|hexToRgb\(|hslToHex|rgbToHex` → 22 records, the twelve non-members carrying the symbols only as F.W1/F.W3/W4-homed evidence.

---

## 2. AUTHORITY REALITY — where the wave fails

### 2a. VERIFIED AT THE BYTES (the large majority)

Registry: `fr-EasingPicker:30/:52/:89` · `fr-MorphPhaseConfig:46` · `fr-EasingCurvePreview:58` · `fr-FourierMorphSvg:46` · `fr-InfoCard:50` · `fr-AnimationControls:87/:120/:123/:192` · `fr-CollapsibleSection:27` · `fr-PaperSearch:57` · `fr-PaperSearchInput:107` · `fr-PaperSearchDropdown:77` · `fr-HarmonicLevelGrid:50/:113` · `fr-NotationPills:35` · `fr-GalleryView:118` · `fr-PaperArticleWindow:303` · `fr-App:47/:90` — **all resolve, all quotations reproduce verbatim.** 39 of 39 distinctive quoted strings matched by exact substring over the corpus (the two apparent misses were markdown emphasis inside the quoted span and a source soft-wrap, both confirmed present).

Sibling specs: **`F-W1.md:161` · `:227` (ESC-4, *"14/22 analytic→CubicBezier; 8/22 material, max Δ 0.192"*) · `:258` (G12) · `:276` (the ELEVEN-limb roster) · `:294` (vaul-vue INSIDE)** — all four resolve exactly; §6a/§6b carry R-4b's cure text **verbatim**, twice.
`X/waves/W9.md:366` (G24 drift gate) · `:375` (G33) · `:48` (Scope item 8, SCI-1) · `:363` (G21) — all resolve.
`library-band.md:294-313` is §W.L5; `:295` carries *"8/22 curves drift, max 0.192"*; RD-6/RD-7/RD-8/RD-11 at `:103/:113-116/:120/:147` all resolve, RD-8's one-way law and RD-7's *"deferral shape"* both verbatim.

Live witnesses (all re-run read-only this seat): `fourier-value-import-drift.mjs` **3/3 RED**, leg 3 returning the *exact* eight names and Δ values G15 lists (`ease-out-circ 1.923e-1 … ease-out-quad 2.520e-2`) · `fourier-vizcolor-oklch.mjs` **5/6**, amber the sole survivor "via fourier style.css:120 local override" — G3's cell reproduces word-for-word · `G16` `easing('ease-in-out').value === easing('ease-in-out').value` → **false** · `G6` 3-digit hex → `rgba(171, 12, NaN, 1)` · `G17` `hexToRgb` **0** call sites outside `colors.ts:111` · `colors.ts` **exactly 117 lines** · `G18`/`G14` glass dist **13,949 B / 13,833 chars / sha256 `ce97a135…`**, `parseStylesheet(string)` **does not throw**, returns `ok:false` + **ONE** `css_syntax` diagnostic `start 12396 / end 13833`, `actual` beginning ``"@source` line, Tailwind scanned an empty dir…"``; Buffer input throws `e.startsWith is not a function` — **G18 and G14 reproduce exactly, including the reshaping** · fourier `cd26c65`, **28** dirty, `14d83356` not a valid object · `energyColor` 3 consumers / 2 routes at the named lines · `actionTone` `AdminAuditLog.vue:67`/`:135` · `spectrumColor` **4 declarations** at the four named coordinates · `resolveVizColors()` assigns exactly the five named keys, `golden: STATIC.golden` frozen at `:83` · `ConvergencePlot.vue:11` **does** import `lerp` (§2b item 7 correct) · value.js exports = the 7 subpaths, no `"."`; `parseCssColor` at `src/subpaths/css.ts:47`; `easing()` at `src/easing.ts:166`; `Result` union at `src/foundation/result.ts:1-3`; `./math` and `./color` export exactly the symbol lists A3/B5 name; `.gitignore:34` = `*.png`; **16** named exports at `./easing`, `bezierPresets` **30** keys; `EASING_LABELS` **22** keys.

### 2b. DRIFTED — seven coordinates that do not hold their quoted content

All three cited spec files were finalized **before** `F-W2.md` (`stat`: F-W5 13:40:26 · F-W0 13:43:42 · **F-W2 13:44:45**), so every one of these was measurable at authoring time. `RULINGS.md`'s own binding-force clause is explicit: *"a repair seat that finds a ruling's coordinate stale re-measures and applies the ruling's LAW to the live bytes."*

| cited | spec's claim | at the bytes NOW | true home |
|---|---|---|---|
| **`F-W5.md:253`** (×9) | "the export row" / G19's LHS / §8's instrument | `---` (a horizontal rule) | **`F-W5.md:263`** |
| **`F-W5.md:272`** (×2) | "P-9's home row" | the `F.W5 → value.js API row` cell | **`F-W5.md:284`** |
| **`F-W0.md:199`** (×2) | G-11 *"ONE corrected anchor table published"* | blank line | **`F-W0.md:211`** |
| **`F-W0.md:204`** (×2) | G-12 corrected denominators | `**Owning rows**: 18, 17, 19.` | **`F-W0.md:216`** |
| **`F-W0.md:209`** (×2) | G-13 pin table (P-4) | `**Owning rows**: 3, 4, 26.` | **`F-W0.md:221`** |
| **`F-W0.md:103`** (×3) | row 1, FR-NP-32 re-home, gate G-4 | the table header `\| # \| Row (banked id) \| Sev \| …` | **`F-W0.md:105`** |
| **`F-W0.md:63`** | SUBSTRATE-LEDGER **create** row | `\| fourier/CLAUDE.md \| create — measured ABSENT \|` | **`F-W0.md:65`** |

Aggravation: §6b's own *correction* note ("the fold seat cited `F-W5.md:234` twice, **which is F.W5's gate row G15 (image bounds on write)**") is itself stale — `F-W5.md:234` now holds **G5, born-visibility of the derived variant**.

### 2c. REFUTED — three seat measurements their own witnesses contradict

1. **`hexToRgba` = 5 call sites, not 4.** G5's cell enumerates five coordinates under the label "**4 call sites / 3 files**"; §2b item 6 calls `BasisCanvas.vue:261` "a **fourth** call site"; §6a/§6b repeat "four call sites, three files". G5's own command returns five: `golden-shimmer.ts:55`, `:58` · `epicycles.ts:258`, `:283` · `BasisCanvas.vue:261`. Receipt: `grep -rn 'hexToRgba(' /Users/mkbabb/Programming/fourier-analysis/web/src/ | grep -v 'export function' | wc -l` → **5**.
2. **glass-ui exports keyset = 80, not 78.** B9 and G8 both say "78 subpaths". Receipt: `node -p "Object.keys(require('…/@mkbabb/glass-ui/package.json').exports).length"` → **80** (79 excluding `./package.json`). The **bank** says 80 too (`fr-ConvergenceTimeline.md:123`: *"real keys in the 80-key exports map"*) — the spec contradicts tree **and** bank.
3. **`DIRECT_EASINGS` = 19 keys, not 20.** Stated twice (A2 §3, G2 §5) as a seat re-measurement. The `Object.freeze({…})` block at `src/easing.ts:94-115` holds nineteen entries.

### 2d. WRONG ANCHOR CORRECTIONS inside §2b — the G-11 contribution

§2b is declared (R-9.1 block, `:75`) to be F.W2's **contribution to F.W0's ONE corrected anchor table**, where *"superseded figures are forbidden."* Two of the eight items are themselves wrong:

- **item 3**: *"only `ConvergenceLegend.vue` lives there [`equation/convergence/`]"* — `ls` returns **`ConvergenceLegend.vue` AND `ConvergenceTimeline.vue`**.
- **item 6**: "a **fourth** `hexToRgba` call site" — it is the **fifth** (§2c.1).

---

## 3. NO INVENTION / M-25 DEPTH — PASSES

**Anti-rename: CLEAN.** Every §3 row and all 54 §8a roster entries trace to a real banked id at a real record. The PASS-1 BLOCKER class is fully cured: A6 opens all five easing-cluster records **by banked id with their adopting home named** (`F-W1.md:161` / ESC-4 `:227` / G12 `:258`), and every "seat-minted" framing over banked substance is withdrawn. `⭑` now marks G16–G20 only; **G15's `⭑` is struck** and its figure is the bank's — *14/22 drift, of which 8/22 material, max Δ 0.192; ease-out-cubic and ease-out-expo among the 8 that STAY analytic* — with F.W1's ESC-4 named as accepting authority and FR-GIG-5 credit ceded. R-1d is discharged exactly.

**Named locks — where their rows land:**

| lock | binds F.W2? | verdict |
|---|---|---|
| **`FR-NP-32` by id** (`fr-NotationPills:35`) | yes (B11 · G14 · §6b) | **CARRIED, and the R-8 re-home note is present**: banked *"NO-WAVE-OWNER (producer dist emitter) + BH relay at the TOP of the FR-COB-28 packet; F.W1 SEQUENCING GATE"* quoted verbatim, the F.W0 re-home attributed to F-W0 (**coordinate stale, §2b above**), the F.W1 sequencing arm left undisturbed, and *"≡ fr-PaperSidebar M1"* used as the canonical form. The `vite dev`/`vite build` RED repro is quoted at its bytes |
| **`PAW-44` / `LAW-3`** | no (`fr-PaperArticleWindow` sticky-stack → F.W4/SS-13) | n/a. The record's F.W2-binding row `K-24` is **carried** (D2) and §2b is authored as its discharge — correct in shape, defective in two of its eight items |
| **`MPC-31`** one-cut law | no (F.W1/F.W4) | n/a. `MPC-2` from the same record **does** bind and is now **carried verbatim and contested without pre-emption** (A6 · R-iii · G20) |
| **`FR-MSP-6`** | no (F.W4 token edit) | n/a. `C·S-2`'s preservation clause carried verbatim (D3, §7) |
| **the same-commit riders** | yes (§6a) | **CARRIED** — A2's mapping with A1's edit · A3's `lerp` re-point · ACT(2) only with the B7 join and the validated residual · B1⊕B2⊕B3 as ONE cut. **Sized wrong** (§2c.1, and "ten amber sites" vs a measured nine) |
| **F.W3's four anti-cures** | yes (§7) | **CARRIED** — "one import edge" KILLED (K13) · derive-from-`VIZ_COLORS` structurally impossible (K4) · tokenisation-is-not-the-cure (RD-5-i) · ImageUpload theming arm KILLED (ruling 6). All four reproduce at the bank |
| **F.W4's preamble + NEGATIVE ROSTER** | yes (D3/D4) | **CARRIED WHOLE** — sixteen zero-cells named by id, the four P-9 members and C-18 restored, both PRESERVATION clauses kept (MorphShapePreview verbatim; FunctionInput's target posture), and the *"evidence, never a denominator"* law stated. The roster's **address** is stale (§2b) |

**Dissents carried, spot-checked at the bytes**: fr-ConvergenceLegend C-1 BLOCKER (A1) · R-D/R-L on RD-5 with R-L's inversion argument binding (B5) · LC's BLOCKER on FR-CP-2 (B5) · O's bidirectional argument on RD-8 (F1, §7) · DU's BLOCKER on fr-App C-2 (E2) · O's post-4.1 sequencing on RD-7 (B8). All **PASS**.

---

## 4. GATES — born-RED and witness realness

**All 20 rows are stated born RED**, and the header, §1 and the closing line agree on twenty. No gate is a `scripts/**/proof-*.mjs` (L-19 satisfied). **R-10.6 is discharged**: G5/G8/G11/G13/G17 are now absolutely rooted at `/Users/mkbabb/Programming/fourier-analysis/…`, which resolves; every one executes as written from the declared cwd. Every fourier-side witness is a read (grep / `ls` / node module read) — **no witness writes the fourier tree**.

**Witness existence — verified**: both spine probes exist and both are RED as claimed · `src/subpaths/{easing,math,color,css}.ts` exist · `dist/subpaths/css.js` exports `parseStylesheet` · `API-FACILITY-ISOMORPHISM.json` + schema exist · `formation/fourier/{CENSUS-2026-08-03.md,lane-frontend.md,lane-docs.md}` exist · `.gitignore:34` = `*.png` · the four to-be-created paths and the four PNGs each carry an explicit **create**/absence marker, and both absences are re-confirmed this seat (`facility-19.md` and `RULINGS-F.W2.md` → *No such file or directory*) · G8's `useTokenColor.d.ts` exists and `./dom` is in the keyset.

**FAILS:**
- **G5's count** (§2c.1) — a born-RED gate whose RED figure its own command refutes.
- **G8/B9's "78 subpaths"** (§2c.2).
- **G2/A2's "DIRECT_EASINGS 20 keys"** (§2c.3).
- **G13's "7 hits in ONE fourier file"** does not reproduce. The gate's own command returns **one line in one file**; the occurrence count (`-o`) is **two**. The RED *state* is real and re-verified (facility-19.md absent; `docs/tranches/N/valuejs-inbound-2026-07-27-facility19-migration-table.md` is `??` in git status; FN-1..FN-7 unanswered; `web/package.json:18` still `^0.13.0`) — only the quantified witness is unreproducible.
- **G15's `src/easing.ts:113-132`** mis-anchors `DIRECT_EASINGS`, which lives at `:94-115`; `:116-129` is `CubicBezier` and `:131+` is `steppedEase`. The figure is transcribed from the probe's own stale comment — the M-25 transcription class the wave polices elsewhere (and correctly kills at E2/G9 for `colorUnit2`/`color2`).
- **G19 closes against a stale address** (§2b) — the instrument is real and re-derivable, but its cited coordinate is not the export row.

---

## 5. ATOMICITY + POSTURE

- **F.W1's 11-limb transaction — CITED, NEVER RESTATED.** R-4b's exact sentence appears twice (§6a lock 2, §6b row 2), naming `F-W1.md:276` and `:294`; the fold seat's three-limb restatement is struck **and the strike is disclosed**. Both previously-omitted limbs (vaul-vue manifest gate; RE-PIN at the ADOPTED commit hash under P-4) are now named at the cross-edge. `grep -c 'vaul-vue' F-W2.md` → **2** (was 0). **PASS.**
- **F.W0 pre-gates honored.** G14 is stated as an opens-after F.W2 never closes and explicitly claims none of its GREEN; §6a puts F.W0 first; §6b declares G3/G4/G12 cannot execute on a non-compiling build; §7 excludes "Closing G14". §2b carries the R-9.1 quotation block declaring itself an input to G-11, with divergence named a defect against G-11 rather than a rival act. **PASS in law; DEFECTIVE in address and in two of its eight items** (§2b, §2d).
- **SS-4 owner flags inline.** FN-5 → *"SS-4's OWNER-GATED G4 … F.W2 answers the letter and names the gate, never pre-empts the ruling"*, repeated at §6b and §7, with FN-5's "before or with fourier M.W10" rider travelling; FN-1/2/3/4/6 excluded on the merits; FN-7 left to F.W5 both-ends; *"Owner rulings SS-4 flags inline (trie-vs-KISS et al.) are not re-litigated here."* **PASS.**
- **fourier tree READ-ONLY.** `:7` — *"EXECUTION IS NOT AUTHORIZED BY THIS FILE … READ-ONLY at spec time … Specifying is not doing"*; `:231` — *"Every fourier-side witness is a read … no witness writes the fourier tree, at spec time or after."* No execution verb in current voice; `scripts/dev/dev.sh` explicitly never staged. **PASS.**
- **Status posture.** `Status: planned` (`:3`); AUDITED YES · SPECIFIED YES · IMPLEMENTED NO · **VERIFIED NO**. `grep -noE 'VERIFIED[^|]{0,40}'` → **one** hit, line 26, the verb-table cell. **Zero VERIFIED stamps. PASS.**
- **Ruling gates honest.** G7/G8/G20 green on *recording* R-i/R-ii/R-iii in `RULINGS-F.W2.md`, not on curing; G14 is declared a precondition F.W2 "states and never closes"; G3/G4's GREENs are ceded to F.W1 by name under FR-GIG-5. R-iii routes MPC-2 and CENSUS-MC to F.W1 without overruling either and puts G1's 5/5 `ERR_PACKAGE_PATH_NOT_EXPORTED` on the record as the bearing measurement. **PASS.**
- **Rulings addressed to F-W2 — 8 directives, spot-checked**: R-1d **discharged exactly** · R-1f **applied to a coordinate that had already moved** (§2b) · R-2c **discharged** (C-18 at D4; four P-9 zero-cells at D3) · R-3 **discharged** ("the CARRY" survives only as forbidden-vocabulary self-reference, twice; both real carries cited by path) · R-4b **discharged verbatim** · R-8 **discharged in substance, stale in address** · R-9.1 **discharged in law, stale in address, wrong in two items** · R-10.6 **discharged**.

**Arithmetic residue.** §3 opens *"26 rows, agglomerated by mechanism"* (`:92`) while §8b declares the ledger *"(29 rows …)"* and enumerates 29 (A1–A7 · B1–B13 · C1–C2 · D1–D4 · E1–E2 · F1). The "26" is fold-seat residue the repair did not sweep — the same stale-figure class §8b itself demotes to a historical note. Separately, B3/§6a say "cure **ten** sites … as ONE cut" while §2a and B3's own measurement say **9 sites / 2 files**; the live grep returns **9** (B1's `ContourPreview.vue:45` is already one of the nine).

---

## 6. DEFECT REGISTER (worst first)

| # | severity | claim | receipt |
|---|---|---|---|
| 1 | **BLOCKER** | G5's born-RED figure — "**4 call sites / 3 files**" — is refuted by its own enumeration (five coordinates) and by its own gate command. The wave calls this "the hardest sequencing lock in the wave"; the same-commit rider law is sized against a false count, and §2b item 6 pushes "a fourth call site" into F.W0's G-11 anchor table | `grep -rn 'hexToRgba(' /Users/mkbabb/Programming/fourier-analysis/web/src/ \| grep -v 'export function' \| wc -l` → **5** (`golden-shimmer.ts:55,:58` · `epicycles.ts:258,:283` · `BasisCanvas.vue:261`) |
| 2 | **BLOCKER** | G19's census instrument, D3's roster authority, §6b's INBOUND edge, §7's exception row and §8's instrument all cite **`F-W5.md:253`** (×9) and **`:272`**; neither holds the quoted content. This re-commits the phantom-coordinate class R-1f cured one round earlier, against a file finalized 4 minutes before this spec | `python3 -c "print(open('F-W5.md').read().split(chr(10))[252])"` → `---`; the export row (*"exports zero-cells (P-9) … copies into F.W2's denominator"*) is at **`:263`**; P-9's home row at **`:284`**. `stat`: F-W5 **13:40:26** vs F-W2 **13:44:45** |
| 3 | MAJOR | Five F.W0 coordinates drifted — `:199` (G-11), `:204` (G-12), `:209` (G-13), `:103` (FR-NP-32/G-4 re-home), `:63` (SUBSTRATE-LEDGER). §2b's R-9.1 block, §6a, §6b (×2), B11 and G14 rest on them; the block's whole point is that F.W0's table is the *single owned authority* | `grep -n '^### G-1[0-9]' F-W0.md` → **211** G-11 · **216** G-12 · **221** G-13. `F-W0.md:103` is the table header; row 1 (FR-NP-32 ≡ M1) is **:105**. SUBSTRATE-LEDGER's create row is **:65**. `stat`: F-W0 **13:43:42** |
| 4 | MAJOR | "`./dom` … 4.0.0 exports keyset (**78 subpaths**)" (B9) / "one of **78** published subpaths" (G8) is false, and contradicts the bank's own figure | `node -p "Object.keys(require('…/@mkbabb/glass-ui/package.json').exports).length"` → **80** (79 sans `./package.json`); `fr-ConvergenceTimeline.md:123` — *"real keys in the **80-key** exports map"* |
| 5 | MAJOR | G13's quantified witness — "**7 hits** in ONE fourier file" — does not reproduce. The RED state is real; the number is not | `grep -rniE 'megatranche\|facility-19\|library-band' /Users/mkbabb/Programming/fourier-analysis/docs/` → **1 line, 1 file**; `-o` count → **2** |
| 6 | MAJOR | "`easing(name)` over DIRECT_EASINGS (**20 keys**)" (A2) / "DIRECT_EASINGS **20 keys**" (G2) is false | `src/easing.ts:94-115` `Object.freeze({…})` holds **19** entries |
| 7 | MAJOR | §2b item 3 — a *correction* contributed to F.W0's G-11 table — is itself wrong: *"only `ConvergenceLegend.vue` lives there"* | `ls …/web/src/components/equation/convergence/` → **`ConvergenceLegend.vue`, `ConvergenceTimeline.vue`** |
| 8 | MAJOR | §3's masthead says "**26 rows**"; §8b says the same ledger is "**29 rows**" and lists 29. Stale fold-seat figure inside the repaired file, of the class §8b itself demotes | `F-W2.md:92` vs `:367`; count of A1–A7 · B1–B13 · C1–C2 · D1–D4 · E1–E2 · F1 = **29** |
| 9 | MINOR | G15 anchors `DIRECT_EASINGS` at `src/easing.ts:113-132`; that range is the table's last three entries plus the whole `CubicBezier` function. The anchor is transcribed from the probe's own stale comment | `sed -n '94,132p' src/easing.ts` — table `:94-115`, `CubicBezier` `:116-129`, `steppedEase` `:131+` |
| 10 | MINOR | `basis-display.ts:1-6` (§2a, B4, G4) drifts from the banked anchor `:1-7` | `fr-GalleryFeaturedCarousel.md:39` and `fr-GalleryInfiniteGrid.md:49` (*"read whole"*) both say `:1-7`; the snapshot literal closes at `:7` |
| 11 | MINOR | "Ten amber sites land together" (§6a) / "cure ten sites … as ONE cut" (B3) vs the measured and separately-stated **9 sites / 2 files** — B1's site is already one of the nine | `grep -n 'hsl(40' ContourPreview.vue ContourEditorCanvas.vue` → **9** lines (`:45` + `:261,:307,:308,:315,:319,:328,:329,:333`) |
| 12 | MINOR | §6b's own correction note mis-describes the coordinate it corrects: "`F-W5.md:234` … is F.W5's gate row G15 (image bounds on write)" | `F-W5.md:234` now holds **G5**, born-visibility of the derived variant |

---

## 7. LOCAL VERDICT

**DEFECTIVE.**

What the repair fixed, it fixed cleanly. The census axis that convicted PASS 1 is closed: **54 routed ids, 54 landed, zero escapes**, re-derived independently under the both-dash detector, with the whole easing cluster opened by banked id and credit ceded to F.W1 at `:161`/`:227`/`:258`; G15 restated in the bank's own figures with its `⭑` struck; the P-9 roster carried at sixteen with both preservation clauses; R-4b's atomicity sentence quoted verbatim twice; the five phantom gate commands rooted; "the CARRY" reduced to forbidden vocabulary. Posture is clean throughout — planned, zero VERIFIED, fourier read-only, SS-4 unpre-empted, ruling gates honest.

It fails on the axis the same repair round was convened to fix, one level down. **`F-W5.md:253` and five F.W0 coordinates are the phantom-anchor defect re-committed** — this time not against a hallucination but against sibling files the seat could have `sed`-ed, two of which were saved minutes earlier, and in defiance of the ruling's explicit re-measure clause. Layered on that: three seat measurements that their own commands refute (5-not-4 call sites, 80-not-78 subpaths, 19-not-20 keys), one of which sizes the wave's self-declared hardest lock; two of the eight §2b "corrections" contributed to F.W0's single owned anchor table are themselves wrong; and the file still carries its fold-seat row count.

None of these is a scope failure and none re-opens the census. All twelve are cheap to cure — six are `sed`-able coordinates, four are re-counts, two are one-word arithmetic. But under L-18/L-20 a drifted citation convicts, and **G19 cannot certify closure against an address that resolves to a horizontal rule**.

*Pass 2 · fresh adversarial · read-only · no file outside this path written.*
