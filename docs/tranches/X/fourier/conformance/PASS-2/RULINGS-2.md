# X·F REPAIR ROUND 2 — CROSS-WAVE RULINGS (RULINGS-2)

**Seat**: cross-wave rulings, round 2, 2026-08-28. **Inputs**: `PASS-2/UNION.md` (whole) + the 11 PASS-2 check files beside it + `PASS-1/RULINGS.md` + `waves/F-W0..F-W10.md` + the 66 `fr-*.md` at `docs/tranches/V/megatranche/registry/adjudicated/`. Every load-bearing figure below was re-verified AT THE BYTES by this seat this session (read-only; this file is the seat's only write). Where a command grounds a ruling it is pasted beside the finding as a ⟨cmd⟩ note — this file obeys the law it rules (R2-1-LAW) and the anchor idiom it rules (R2-2): coordinates given as `:N` carry the qualifier **at this writing** and are never the citation a repair seat copies; the stable anchor beside them is.

**Binding force**: each ruling names ONE owning cure per cross-wave defect class. Repair seats execute these rulings' LAW, not their line numbers; a seat that finds any coordinate stale re-measures by the pasted command and applies the LAW to the live bytes. Round-1's binding-force clause failed because it was a re-measure *permission*; round 2 makes it a re-measure *prohibition* — no seat may copy a coordinate from this file, PASS-1/RULINGS.md, or any sibling into a spec at all (R2-2).

**Sequencing (binding on the round)**: content repairs first (R2-1, R2-3..R2-8, R2-10, R2-11), the paired W9/W10 splice with them (R2-6); the **Re-anchor seat runs LAST** (R2-2), converting every cross-spec citation to the stable idiom after all line-shifting writes have landed. No other ordering survives the round-1 failure mode (ten seats shifting lines under one another's citations).

---

## R2-1 · FABRICATED AUTHORITY — the four survivors, cured at the bytes; the QUOTE-BY-COMMAND LAW

R-1's LAW stands unchanged: an authority that cannot be produced at its cited coordinate is DELETED or replaced with the banked text VERBATIM at its true home — never paraphrased into survival. The four survivors, each re-verified by this seat:

### R2-1a — F-W6's phantom "F.W5 §12" + the invented blocking quotation · owner F.W6 · FABRICATED, both halves

⟨cmd⟩ `grep -c '§12' waves/F-W5.md` → **0**. ⟨cmd⟩ `grep -rn "burn-down needs" waves/` → `F-W6.md:301` only (echo at `:76`). F-W5 has no §12, no blocking declaration, and the quoted sentence *"the burn-down needs the chain clause"* exists nowhere in the tree outside F-W6 and the check files.

**Cure**: at F-W6's §5 F.W5 dependency cell (`:301` at this writing) and its `:76` echo, STRIKE the clause *"F.W5 §12 declares it blocks F.W6 ('the burn-down needs the chain clause')"* — quotation, attribution, and the dependent "declares it blocks" framing together. The dependency is restated in F.W6's own voice on the real edge: **F-W5 §4's `F.W5 → F.W6` row** ("emits burn-down": F-α's deepen-or-retire execution (G3); the G16 pipeline WITH G5c; DO-NOT-REGENERATE until the pipeline lands — ⟨cmd⟩ `grep -n 'F.W5 → F.W6' waves/F-W5.md` → `:265` at this writing). The sequencing fact F-W6 needs — F.W5 states the clause before F.W6 burns it down — is carried by that edge row and by clause E2/gate G3 (R2-1b); nothing about it requires a phantom section. FW6-G16's operand list drops the phantom leg and re-states over the real edge.

### R2-1b — F-W6's "F-W5 §6c verbatim" composite ×5 · owner F.W6 · FABRICATED attribution, real substance at two true homes

⟨cmd⟩ `grep -n '§6c' waves/F-W6.md | cut -d: -f1` → **29 76 143 268 286** (at this writing). ⟨cmd⟩ `grep -c '§6c' waves/F-W5.md` → **0** (the only `6c` in F-W5 is clause-gate `G6c` at `:190`). The composite sentence *"F.W5 owns the CLAUSE, F.W6 the burn-down — NO DOUBLE-BOOKING"* is attributed "verbatim" to a section that does not exist. The REAL banked bytes, both produced by ⟨cmd⟩ `grep -n "owns the CLAUSE" waves/F-W5.md`:

- `F-W5.md:147` (at this writing) — **§2 clause E2, the ▲ sentence**: *"F.W5 owns the CLAUSE; F.W6 owns the burn-down — do NOT double-book."*
- `F-W5.md:232` (at this writing) — **§3 gate G3, green-owner cell**: *"F.W5 owns the CLAUSE only — no double-booking"*

**Cure, all five sites**: the composite is replaced by ONE of the two real sentences quoted verbatim with a ⟨cmd⟩ note, cited by **clause id E2 ▲ / gate id G3** (stable idiom, R2-2) — never as "§6c", never fused.

**The obligation-list half** (`:29` bounds row 3, `:143` V-α, `:268` FW6-G16 — "citing F.W5 §6c's itemized obligation list"): the real itemized value-side obligation list is **F-W5 §4's `F.W5 → value.js API row` edge row** (⟨cmd⟩ `grep -n 'value.js API row' waves/F-W5.md` → `:272` at this writing), which itemizes **V-β (+ two-histories test, G2) · V-γ (E18) · TA-4 (per G4's ruling) · fork born-visibility + create-visibility test (G5) · G18's value-side probe · D9 reconciliation**. Correction of record bound into the cure: **that list does NOT itemize V-α.** FW6-G16's V-α leg therefore stands on **lane-crud §V-α** (an F-W5 §0 bounds authority — ⟨cmd⟩ `grep -n 'V-α/β/γ' waves/F-W5.md` → `:26`) plus F-W6's own §2 booking, and the receipt artefact (bounds #3) cites BOTH: the F-W5 §4 edge row for V-β/V-γ/TA-4, lane-crud §V-α for V-α — stated as such, no invented list membership.

### R2-1c — F-W9's struck-bytes G-12 quote asserted as the file's current bytes · owner F.W9 · FABRICATED (minted BY the repair)

F-W9 §2.8 E-1 (`:191` at this writing) quotes as G-12's own RED-witness bytes: *"8 `e2e/*.spec.ts` — the CARRY's '7' is RETRACTED at the fold seat, 2026-08-28"*. ⟨cmd⟩ `grep -c "RETRACTED at the fold seat" waves/F-W0.md` → **0** — the same round's F-W0 repair STRUCK that framing. The live bytes, ⟨cmd⟩ `grep -n "any 7-spec figure is superseded" waves/F-W0.md` → `:217` (inside **§4 G-12's RED witness**, at this writing):

> **8 `e2e/*.spec.ts`** (banked ×3 — `fr-AdminFlaggedPanel:110` · `fr-CollapsibleSection:65` · `fr-CanvasOverlayButton:74` — and live-measured at the fold seat 2026-08-28; **any 7-spec figure is superseded and forbidden**)

and **F-W0 §1d item 2** states the positive form and expressly strikes the retraction framing (*"the earlier 'retraction-of-a-retraction' framing rested on a ledger that does not exist and is struck"*).

**Cure**: E-1's settling-authority cell replaces the struck-sentence quote with the `:217` bytes verbatim (⟨cmd⟩ note beside), cited by **gate id (F-W0 §4 G-12, the RED-witness e2e row) + §1d.2** — not by the stale `F-W0.md:204`. The pathless *"the CARRY"* vocabulary dies WITH the quote — this single edit also discharges the R-3.3 breach (R2-10 item 6). The AA-44 "9" in the shared §2.2 table remains the separately-disclosed PAIRED edit E-1 already declares; this ruling changes only F-W9's own erratum cell.

### R2-1d — F-W7 §5a's C-14 "verbatim" label · owner F.W7 · ruling-inherited misquote over a MOVING sibling

F-W7's F-MAIL-∅ row (`:124` at this writing) labels *"a wave with no registry rows gets no manufactured ones — the ∅ is a finding, not a gap"* as **"verbatim (KF.W1 C-14, `KF-W1.md:256`)"**. Both halves of the label fail: `KF-W1.md` is a LIVE sibling under its own repair round (mtime 14:21, AFTER the UNION's read) and `:256` now holds an unrelated O-8/O-11 row. The truth at the bytes, this seat:

- ⟨cmd⟩ `grep -n "registry rows gets no manufactured ones" ../../keyframes/waves/KF-W1.md` → `:140` (at this writing) — the **§5 carry row C-14 (KF-MAIL-∅), LOCK (L-19) clause**: *"a wave with no registry rows gets no manufactured ones"* — this half IS verbatim.
- ⟨cmd⟩ `grep -n "The ∅ is a finding" ../../keyframes/waves/KF-W1.md` → `:262` (at this writing) — the **§8 exclusion row ⟨C-14⟩**: *"A wave with no adjudicated rows gets no invented ones. The ∅ is a finding ⟨C-14⟩"*. The tail *"— the ∅ is a finding, not a gap"* as quoted is a FUSION of this row with C-14's *"The empty column is a finding, not a gap."*

**Cure**: split the quote into its two real sentences, each with its ⟨cmd⟩ note, cited by STABLE anchor only — *"KF-W1 §5 carry row C-14, the LOCK (L-19) clause"* and *"KF-W1 §8, the exclusion row ⟨C-14⟩"* — never by line into a moving sibling. **Root cause minuted in the spec**: PASS-1 R-11.1 itself prescribed the drifted words; the erratum states that a RULING's text is a cure-shape, never a quotable authority — a "verbatim" label may point only at the cited file's own bytes (this is R2-1-LAW's second clause, applied).

### R2-1-LAW — QUOTE-BY-COMMAND (binding on EVERY seat this round, and standing)

1. **A cross-wave or corpus quotation may enter a spec ONLY as the pasted output of a `grep`/`sed` command run this round.** The executing seat states the command beside the quote as a **⟨cmd⟩ provenance note**. Typed-from-memory quotes are the convicted class of this pass — two of the four §3 survivors were typed from memory of a ruling, not read from a file.
2. **A "verbatim" label without a ⟨cmd⟩ note is a defect per se**, whatever the words say.
3. **Rulings files are not quotable authority.** RULINGS.md / RULINGS-2.md prescribe cure-shapes; a spec quoting "the banked lock" quotes the BANK, by command, at the bank's live bytes — never the ruling's paraphrase of it (the R2-1d failure mode, closed).
4. Composites are labelled composites: two sentences from two homes are quoted as two quotes with two ⟨cmd⟩ notes, never fused under one attribution.

---

## R2-2 · THE ANCHOR IDIOM — stable anchors, never line numbers; ONE Re-anchor seat, run LAST

The repair round's own +12/+8/+2/+10 line-shifts staled ~60 cross-wave citations in 10 waves (F-W0 `:199`/`:204`/`:209`/`:63` cited ~30×; F-W7's founding `F-W5.md:153`/`:226` ×14; F-W2's `:253` ×9; F-W10 `:114`→`:116`; F-W3 `:289`→`:299`) — every one now addressing a blank line, an "Owning rows" cell, a rule, or the wrong row, under mastheads attesting "re-read at the bytes". The class METASTASIZED because the cure was per-coordinate. The cure is now per-IDIOM:

**LAW (program-wide, standing)**:
1. **Cross-spec receipts cite STABLE anchors** — §-headings, gate ids (G-11, G-F10-6, g19), clause ids (E16 ▲, G7 ⊙), banked row ids, and row labels (the SS-4-PREREQ row; the `F.W5 → F.W6` edge row) — **never line numbers.** F-W5's §4 `F.W5 → F.W7` row already models it (*"cite E16 and G7 by clause and gate id, never by line"*); what one row did, every citation does.
2. Line numbers are permitted ONLY for: (i) the frozen corpus (`fr-*.md`, adjudicated, immutable) and files cited WITH a commit hash; (ii) a spec's own intra-file coordinates, marked *"at this writing"*. A line number into any live sibling (`waves/`, the carries, `KF-W1.md`, COHESION) is a defect from this ruling forward.
3. **The Re-anchor seat** converts ALL staled citations across the 11 specs **after every content repair of this round lands** (see Sequencing): the F-W0 +12 class → *"F-W0 §4 G-11 / G-12 / G-13"* and *"F-W0 §1a, the SUBSTRATE-LEDGER create row"*; F-W7's founding pair → *"F-W5 §2 clause E16 ▲ / §3 gate G7 ⊙"*; F-W2's `:253` class → F-W5 §5's export row by label; `F-W10.md:114` ×all → *"F-W10 §2.3, the SS-4-PREREQ row"* (with a one-line erratum minute at F-W10 §2.8 recording the un-minuted :114→:116 drift, closing R-1b's dangling end); `F-W3.md:289` → *"F-W3 §X, the S-6 row's declared-not-carried tail"*. Substance is verbatim-correct at the true homes in every audited case — the conversions are address-only; NO quoted words change under this seat's hand (word changes belong to R2-1 and the per-wave seats, which run first).
4. **Every seat writing NEW citations this round writes them in the stable idiom from the first byte.** A new line-number cite into a sibling is a round-2 defect even if it resolves today.
5. F-W0 (the shift-source) adds one anchor-stability sentence at §4's head: its gates' citable identities are the gate ids; line cites into this file are forbidden downstream — making the idiom enforceable from the publisher's end.

---

## R2-3 · THE 123 PROGRAM ESCAPES — the F.W3/F.W4 TWIN LAW, and every escape homed

### R2-3a — THE TWIN LAW (decreed once, decisively)

Measured basis (both seats, reproduced): sole-`F.W3` rows **164**; literal-`F.W4` row heads **1,011/54 records**; dual `F.W3/W4` **1,010 occurrences / 898 row heads / 844 dual-routed rows**; route-marked **bullet** rows on the W3 axis **691/36 records**; en-dash exists in this corpus ONLY on `F.W5–W8` (no `F.W3–W4` in either spelling — a measured negative).

1. **ROW-SHAPE DECREE — a route mark in ANY format books the row.** A *row* is a markdown table row whose terminal cell routes, **or** an id-headed bullet routed in context (`→` · `**ADJUDICATED →**` · `⇒` · `rides` · `routed` · bold-at-disposition), **or** a prose routing walked back to its enclosing row id (S-8). The 691 bullet rows ARE rows. Row-shape blindness is cured by decree, program-wide — no gate or census may state a shape-restricted operand again.
2. **SPELLING PARTITION — one home per id**:
   - sole `F.W3` → **F.W3 books**;
   - sole `F.W4` → **F.W4 books**;
   - dual `F.W3/W4` → **F.W4 books by default** — F-W3 §X.1's own column-3 declaration (*"the rows are held by the adopting wave named in column 3"*) generalized to the whole dual population — **EXCEPT** rows whose target file lies in F-W3's §5 unit file lists (the `.a`–`.e` roster): those home at **F.W3**, and §X.1's column 3 names them per id. The criterion is mechanical (file ∈ unit list), decidable today, and matches the check's own finding that five sampled escapes sit in files F.W3 owns.
   - The other twin CITES; a citation is never a booking (unchanged R-2 law).
3. **DETECTOR**: the twin gates' token set is **{`F.W3`, `F.W4`, `F.W3/W4`} in BOTH dash spellings** (U+002D and U+2013 — the en-dash arm is ∅ today and stays in the probe so it can never silently stop being ∅), over the shape-agnostic roster of item 1. F-W3 g19's LHS = the 1,008-row/62-record roster; F-W4 `G-F4-CARRY-CLOSURE` leg (b) = the {F.W4, F.W3/W4} set. (Execution mechanics at R2-9.)

### R2-3b — the 103 F.W3-axis zero-homed rows · owners F.W3 + F.W4 jointly, split by the LAW

The F-W3 seat runs the partition mechanically (its own `c1.py`/`c3.py` reproduce the roster) and publishes the split as **§X.1-v2**: column 3 per id over the ESCAPES (the already-booked 905 stand; only escapes book — no wholesale re-enumeration of 844). Each of the 103 lands in its LAW-assigned wave as a §2 row, a named fold, or an exclusion-with-reason — **by banked id, record-qualified**. The nine spot-verified escapes bind as worked examples: `MG-α`/`MG-ε`/`MG-η` (fr-App) · `FR-PS-Z`/`FR-PS-HL` (fr-PaperSearch, `.c` files → F.W3) · `MISS-DU1`/`MISS-DU3`/`MISS-DU4` (fr-PaperSearchInput, `paper/search/**` = `.c` → F.W3) · `PSM-40` · `MAJ-5`/`MAJ-7` (VisualizationView = `.e` → F.W3) · `FR-IC-16` (InfoCard = `.e` → F.W3) · `FR-GSB-30` (GallerySearchBar = `.e` → F.W3); the App-level and PSM rows split per the file criterion at execution. `L-26` stays the control (escapes F-W3, held at F-W4 + carry — correct handoff shape). §X's *"KEPT PER ID"* and §Z's closure re-denominate over 1,008/844, not 317/59.

### R2-3c — the 15 §X.1 citation-orphans · owner **F.W4** (one home each, per §X.1's own column 3)

The declaration is KEPT; the booking is PERFORMED. All fifteen land in F-W4:

| ids | landing shape in F-W4 |
|---|---|
| `GCM-6` · `GCM-12` · `GCM-35` · `GCM-36` · `GCM-37` | rows beside the booked GCM family (GCM-6 carries its F.W1-target contingency note) |
| `GCM-28` · `GCM-30` · `GCM-48` | named folds in the GCM family with their banked kill/dedup triggers verbatim (GCM-28 dies inside GCM-30's dedup; GCM-30 dies with GCM-52's wrapper deletion; GCM-48's delete kills GCM-30 — the cycle stated in one block so no seat executes them separately) |
| `GCM-31` | row with its GLASS-RELAY leg routed to the standing BH inbox (SS-6; producer `cn`-alias ask never a frontend hack) |
| `PAW-6` · `PAW-18` · `PAW-26` · `PAW-29` | rows beside the booked PAW set (PAW-6 carries the K-17 constraint verbatim; PAW-29 rides the PAW-12 generator note) |
| `fr-CanvasControlsDock L-18` | named fold by reference → `fr-BasisCanvas BC-10/C-9` (the record's own fold instruction) |

### R2-3d — the 5 zero-byte singles · owners named, one each

1. **`fr-ExportModal D-M3`** → **F.W1** — a DISCHARGED-BY-UPLIFT ledger row (banked: *"free at F.W1 (producer 44×44 verified at `558c3fa3`)"*, `fr-ExportModal.md:51`), carrying K-16's killed-WCAG-headline rider. **Record-qualification MANDATORY** — every spec-side `D-M3` today is WU-C's Button-budget id; the homonym is the record's own named hazard.
2. **`fr-ContourPreview` row 29** → **F.W1** — joins the G1 RE-PIN / pin-law family by id (banked: *"ADJUDICATED → F.W1 (pin before the transaction)"*, `fr-ContourPreview.md:29` — the one §7 uplift-pin row F-W1's set omitted); spec spelling `CP-ROW-29` per F-W4's established row-N idiom, banked identity cited.
3. **`fr-BasisCanvas D-corpus-C-1`** → **F.W1** — epistemic-carry row (cartoon-surface survives the 4→7 uplift; the shim is a design ask, not a build break — `fr-BasisCanvas.md:105`), beside the cartoon-card denominator rows.
4. **`fr-ConvergenceTimeline D·D-5 + C·C-5`** → **F.W4** — the banked id JOINS its already-carried mechanism: F-W4 §SP-9's `FR-CT-D5b/C-5` cell (`size="icon-sm"` under the pin; F.W1 rider) gains the banked spelling `fr-ConvergenceTimeline D·D-5 + C·C-5` (anti-rename; the spec-local alias keeps pointing at it); the F.W1 post-uplift rider (`iconOnly`/`data-control-target`) and the SS-13 rendered-px leg stay cited, not booked.
5. **`fr-CanvasControlsDock C-28`** → **F.W5** — fold-by-reference row beside the R6-8 seam carry (the B1/C-30 row): C-28 is the D-4 server-arm dedupe rider on the non-isolable client↔operation seam (`fr-CanvasControlsDock.md:102`/`:150`); F-W4's D-4 row cites it; F-W8 names it in §6a (R2-7).

---

## R2-4 · F-W2's MIS-SIZED HEAD LOCK — corrected by its own commands

1. **G5**: the born-RED figure becomes **5 call sites / 3 files** — the gate's own command, re-run this seat: ⟨cmd⟩ `grep -rn 'hexToRgba(' /Users/mkbabb/Programming/fourier-analysis/web/src/ | grep -v 'export function'` → **5**: `golden-shimmer.ts:55`,`:58` · `epicycles.ts:258`,`:283` · `BasisCanvas.vue:261`. Every "4 call sites / 3 files" and §2b item 6's "a fourth call site" re-worded to the five-coordinate enumeration the cell already carries; the same-commit rider law (the wave's "hardest sequencing lock") re-sized over five. The G-11 contribution row re-stated with the corrected figure.
2. **DIRECT_EASINGS = 19**, re-derived at the CURRENT bytes this seat: ⟨cmd⟩ `sed -n '95,113p' /Users/mkbabb/Programming/value.js/src/easing.ts | grep -c ','` → **19** (the `Object.freeze` block opens `:94`, closes `:114` at this writing — 9 camelCase entries + 10 hyphenated aliases; `linear` has no alias). A2 §3 and G2 §5 corrected 20→19.
3. Riders in the same class, same seat: **G15's anchor** corrected — the table lives at `src/easing.ts:94-114`, `CubicBezier` `:116-129`, `steppedEase` `:131+`; never transcribe a probe's stale comment (M-25). **G13's witness** re-stated as its command actually returns (**1 line / 1 file; `-o` → 2**) — the RED state is real and keeps its gate. **§6b's correction note** re-keyed to the stable idiom (F-W5 §5's export row / P-9's home row by label — its `:234` cite has now drifted onto G5, proving the idiom).

---

## R2-5 · F-W1's ROSTER COUNT — TWELVE, corrected once, re-quoted by command

Re-derived from the banked transaction, this seat: ⟨cmd⟩ `sed -n '276p' waves/F-W1.md | sed 's/\*\*The roster.*//; s/([^)]*)//g' | grep -o ' + ' | wc -l` → **11 top-level separators ⇒ TWELVE conjuncts** (producer bump · 162-attr Button rewrite · `copied`→`status` · lucide rename · G10 deletions · **FR-EQC-7 vaul-vue manifest gate** · G13 manifest/lock moves · PP-REDGATE · FR-CP-13 · M-γ deletion · GCM-22 `p-0` · pencil-boil floor). The history is exactly as the coordinate's own `:294` pin records: the roster was ELEVEN as chartered; **R-4a returned the FR-EQC-7 vaul-vue gate INSIDE the transaction at repair round 1 — the twelfth limb.** RULINGS R-4 reached eleven only by fusing the last two conjuncts, a grouping the spec's separators do not mark.

**Cure — F.W1, ONCE, at the pinned coordinate** (§4 step 4, `:276` at this writing): *"The roster is **TWELVE** limbs and stays twelve — the eleven chartered plus the FR-EQC-7 vaul-vue manifest gate returned INSIDE at repair round 1 per the `:294` pin (R-4a) — it is the extent every sibling spec cites (R-4b); nothing is added to it by a later reading."* The G5 landing cell stays *"conditional, not a **thirteenth** limb."*

**Cure — the siblings**: every spec citing *"the ELEVEN-limb roster"* (⟨cmd⟩ `grep -c 'ELEVEN-limb' waves/F-W*.md`, this seat → F-W2 ×2 · F-W4 ×2 · F-W5 ×1 · F-W6 ×1 · F-W7 ×1 · F-W8 ×1 · F-W9 ×1 · F-W10 ×2) **re-quotes AFTER the F-W1 edit lands, via ⟨cmd⟩** (`grep -n "The roster is" waves/F-W1.md`), in the stable idiom: *"the TWELVE-limb roster chartered at F-W1 §4 step 4"*. No sibling re-derives the count; every sibling quotes the corrected charter. **F-W3** (R-4b's one gap) adds the citation sentence ONCE at its first "atomic tri-package uplift" mention — the tri-package prose may stand as color, but the extent is stated only by citation.

---

## R2-6 · F-W10 — R-2b LANDED IN THE RULED DIRECTION; the drain RE-DERIVED from the corpus

### R2-6a — the paired splice, direction restored

R-2b's direction was and remains: **F.W9 books (home/author); F.W10 receives the IDENTICAL bytes by paired splice.** The pass-2 measurement: F-W9 has already landed the ruled 17-line checkpoint table (*"THE F.W1 VISUAL-REGRESSION CHECKPOINT SET — 8 items, HOMED HERE (R-2b)"*); F-W10 instead authored a rival prose paragraph and its E-3 narrates the outstanding act as *"the twin's identical splice"* — inverted; a reader following E-3 would overwrite the ruled-in table with the paragraph.

**Cure (ONE paired commit, two files)**: F-W10's §2.2 prose paragraph is **DELETED** and replaced by F-W9's table bytes, spliced never retyped; E-3 re-written: *the outstanding act was F.W10's receipt of F.W9's bytes — performed this round*; the two region-head sentences conformed to one shared spelling in the same commit; G-F9-21 ≡ G-F10-6 then witness Δ=∅. F-W9's E-2 story updated in the same commit (the transient asymmetry is closed; the one-directional narration corrected). The AA-44 "9"→8 paired fix may ride the same commit (both specs, one commit, per E-1's own declaration).

### R2-6b — R-7.2 executed against the 66 THIS time

The round-1 drain complied with the ruling's letter (PASS-1's 36-strict roster) and inherited its blind spot. **Re-ruled with the corrected operand**: the drain roster is re-derived from the corpus — pass-2 set C measures **81 strict NO-WAVE-OWNER row-level lines**, of which 65 are held and **16 escape**. The 16 (F-W10-CHECK PASS-2 §2.2, anchors verbatim there) each get a TERMINAL verb in §2.5/§2.5a:

- **14 SS-3/SS-4-bound rows** — `fr-ImageUpload 11` + `29` · `fr-GalleryInfiniteGrid C-3` · `fr-BasisSelector i-1/L-INFO-2` + `i-2/L-INFO-1` · `fr-VisualizationView §4 method row` · `fr-MobileFloatingToc i-5/L-18` · `fr-CoefficientsSpectrum i-5` · `fr-ContourPreview 31` + `39` + `41` · `fr-CollapsibleSection i-1/D·i-4` · `fr-EditorControlsDock FOLD-L-14` · `fr-FunctionInput C §0 row 7` — carried to the census-input/methodology register **per id** (excluded-with-reason: SS-3/SS-4 spec-authoring input, the banked disposition's own words).
- **`fr-SvgFilters M-7`** — excluded-with-reason: fleet provenance note (provenance register).
- **`fr-PaperSearchDropdown L:L-16`** — named fold → banked `fr-PaperSearch L-19` (the record's own fold).

§5's header re-stated over the corrected roster (it can no longer read "zero adjudicated registry rows among them"); the class-level verb over the loose 16 stands (correctly disposed). **Count word**: the reconciliation is **59 distinct ids** — FR-NP-32 heads §2.5 and is never added twice; "SIXTY" struck; G-F10-7 re-denominated and its falsifier kept (*"a roster that homes a subset and calls it zero-left-open"* — now satisfied rather than self-describing).

---

## R2-7 · F-W8 — the residue named, the tokens qualified, the out-of-band booking dissolved

1. **§6a gains four rows** (the measured residue, each record-qualified, each naming its one home):
   - `fr-ContourSettings M-13` (= LC-missed-2, PRIMARY band MAJOR) — **held at F-W5 clause E14** (contour provenance); F-W8's row is an exclusion-with-reason citing E14; F.W8's assertion seat may cite it at R4 beside `B-4 ⊕ i-7`, booking nothing.
   - `fr-ContourSettings M-10` (+ aliases `L-M6`/`C-19`) — the shared-enum contract rider; excluded-with-reason citing its F-W5 clause home (the E13/A5 family); the `fr-BasisSelector M-10` homonym named in the row so the collision that hid it is disarmed.
   - `fr-GalleryCard L·M-4 / D-13 / C-8(a) + L·D-2 / C-12` — the serializer leg; **held at F-W5 D17** (date serialization); exclusion row cites D17; the JSC → SS-13 leg noted.
   - `fr-CanvasControlsDock C-28` — cites its R2-3d home (F.W5's R6-8 seam fold row).
2. **P8's bare `C-3`/`C-4`/`D-13` record-qualified** (`fr-GalleryInfiniteGrid C-3/C-4`; `D-13` likewise) per R-5's own law the spec already declares (*"a bare token is not an identity"* — the file diagnosed the trap at `:206` and fell into it); §6a row 34's `B5` citation re-keyed to `B1` (B5's `C:C-12` is HLG-23). This is the mechanism that made rows 1–3 invisible; qualifying it is what makes G15 runnable.
3. **`fr-AdminUserList FR-AUL-46` OUT of J4's banked-ids cell** — see R2-11.
4. **§6a's closure statement re-computed** over the corrected partition (the "87 + 36 = 123 … no residue" arithmetic is re-stated only after rows 1–4 land; if the sum changes, the sum changes — the stamp follows the operand, never the reverse). **G15's LHS** = the record-qualified census both sides (its own stated runnability precondition, now met).
5. **R-8 at F.W8** (R2-10 item 5): the corrupt-dist gate takes the canonical form **`FR-NP-32 (≡ fr-PaperSidebar M1)`** everywhere it is stated (the `:212` M1-alone site and §5c) — the LAW's scope clause is "everywhere the gate is stated"; absence from a directive index lifts nothing.
6. Same-class riders, same seat: the F-W5 §4 quotation conformed to its bytes (*"v1 §6 re-authored by v2 and executed there"* — the interpolated *"'s close-gate"* struck; an ellipsis marks elision, never addition); the two-candidate `raw-findings.json` cite path-qualified (the 06-16 run); the "64 ids" and 50-vs-49 count words re-derived or struck.

---

## R2-8 · F-W7 — THE ∅ POSTURE SETTLED, NOW AND FINALLY

**RULED: the wave genuinely owns ZERO registry rows; the routing token is NOT mis-keyed.** Two independent seats, two passes, one number: PASS-1 R-11 measured `routedTotal = 0`; the PASS-2 fresh seat re-derived it with the full spelling-agnostic token distribution (⟨cmd⟩ receipts in F-W7-CHECK §1: token-bounded `F\.W7` → **0** across 66 records; bare-substring hits = 2, both `KF.W7` in `fr-Tooltip`; no span containing 7 exists in either dash spelling — the only spans in the corpus are `F.W5-W8`/`F.W5–W8` and the slash forms). The spec already declares the posture **exactly the way a zero-row wave must**: the F-MAIL-∅ row in the C-14 form, `routedTotal = 0; bookedCount = 0` stated so the grep it invites cannot falsify it, the band denominator disowned to F.W5's row, the `fr-ContourEditorCanvas L-5` exclusion-with-reason at §8, and N-2's 147 = 146 artefacts + 1 true hit — all byte-exact at pass 2.

**Consequences, binding**:
1. The posture question is **CLOSED**. No pass-3 seat re-litigates `routedTotal`; a future re-derivation is a defect against this ruling unless the corpus itself changes (a new `fr-*` routing to F.W7).
2. The wave's 12 local defects are ALL of the stale-anchor/label class and are cured **on the spec's own terms** (its §12 lesson — *"a citation is a measurement, and an unreproduced measurement is an invention"*): (i) the 22-row drift table converts to the stable idiom under the Re-anchor seat (R2-2) — F-W5 cites by clause/gate id, F-W0 by gate id, F-W10 by the SS-4-PREREQ row label, F-W3 by the §X S-6 row, F-W6/F-W8 by their §-headings; (ii) the C-14 label takes R2-1d's split-quote cure; (iii) **G-F7-11 re-measured** (R2-10 item 8): F-W5 §4 DOES declare its F.W7 edge (the `F.W5 → F.W7 | states, never books` row) AND the `F.W5 → F.W8` row — the born-RED witness is re-worded to the truth: **only F.W0's reciprocal is owed**; (iv) the §1 "9 records cited" count word → 12, and the find-dirs enumeration corrected.
3. F-W7 remains the program's model for the honest ∅ — nothing is minted to fill the empty column, and nothing in this round adds a row to it.

---

## R2-9 · ROW-SHAPE-TOLERANT CLOSURE GATES — g19 and every sibling, re-based by decree

**LAW (program-wide, standing)**: every closure/census gate's operand is the **shape- and spelling-agnostic roster** of R2-3a item 1: table rows ⊕ id-headed route-marked bullets ⊕ prose routings walked to their enclosing row id (S-8) — with every routing token probed in **BOTH U+002D and U+2013** and every slash form expanded to its member set. A gate that states a shape- or spelling-restricted operand is DEFECTIVE at authoring, whatever its result. R-6 cured an instance; this rules the idiom.

Applied by name:
- **F-W3 g19** — LHS = the 1,008-row/62-record shape-agnostic roster (317 table ⊕ 691 bullet); the set-difference re-run; escapes homed per R2-3b.
- **F-W4 `G-F4-CARRY-CLOSURE` leg (b)** — token set `{F.W4, F.W3/W4}`, both dash spellings; re-run; the 15 + 2 escapes homed per R2-3c/§B.2 (the `PS D-M4 extended` witness joins `PV-★MF-10 (= PS L-10)`'s row; the id-less `fr-ContourPreview :76 D:m-6` row homed by anchor since the record wrote `—` where the number goes).
- **F-W8 G15** — both sides record-qualified (R2-7.2); the band probe `F\.W5[-–]W8` stays both-dash.
- **F-W10 G-F10-7** — strict-roster basis = the corpus-derived 81 (R2-6b), not any check file.
- **F-W6 FW6-G17** — the 192/49 en-dash-corrected roster (already conformant) PLUS the shape decree; the 7-record negative roster stays the lower operand.
- **F-W2 G19 · F-W5 G19** — operands re-stated with the detector law inline.
- **Every gate cell states its detector INLINE** (shapes + spellings + the expansion set), so the gate — not a later seat — catches the next format the corpus uses.

---

## R2-10 · THE 8 UNAPPLIED ROUND-1 DIRECTIVES — re-directed, each to a sharper owner

| # | round-1 directive | owner | round-2 re-direction |
|---|---|---|---|
| 1 | binding-force re-measure clause (class) | all seats | **Superseded by R2-2**: coordinates are no longer re-measured — they are abolished from cross-spec citations. Re-anchor seat runs LAST; new cites stable-idiom from birth. |
| 2 | R-2b paired splice at F.W10 | F.W10 (+F.W9) | **R2-6a**: F.W10 deletes its rival paragraph, receives F-W9's 17-line table by splice; E-3 direction corrected; one paired commit; Δ=∅ re-witnessed. |
| 3 | R-7.2 drain re-derived | F.W10 | **R2-6b**: derived from the 66 (81 strict), the 16 named escapes homed with terminal verbs; SIXTY→59; §5 header restated. |
| 4 | R-5 record-qualification at F.W8 §3 | F.W8 | **R2-7.2**: P8's `C-3`/`C-4`/`D-13` qualified; B5→B1; §6a residue rows added; G15's LHS qualified — the gate becomes runnable, then it runs. |
| 5 | R-8 canonical form at F.W8 | F.W8 | **R2-7.5**: `FR-NP-32 (≡ fr-PaperSidebar M1)` at every statement of the gate, `:212` and §5c included. |
| 6 | R-3.3 vocabulary at F.W9 | F.W9 | **R2-1c**: the pathless "the CARRY" dies with the fabricated quote it lives inside — one edit, two defects closed. |
| 7 | R-1b dissent anchor at F.W7 | F.W7 + F.W10 | **R2-2.3**: the dissent cited as *"F-W5 §2 clause E16 ▲ ≡ F-W10 §2.3 SS-4-PREREQ row"* — no line number at either end; F-W10 §2.8 gains the one-line erratum minuting the :114→:116 drift. |
| 8 | R-11.5 / G-F7-11 re-measure | F.W7 | **R2-8.2(iii)**: the born-RED witness re-worded — F-W5 §4 declares BOTH the F.W7 and F.W8 edges; only F.W0's reciprocal is owed; F.W0 adds it (one edge row, its §5/cross-edge register). |

---

## R2-11 · THE ONE DOUBLE-HOME — `fr-AdminUserList FR-AUL-46`, resolved

Banked disposition routes **F.W5** (⟨cmd⟩ this seat: `grep -n 'F\.W5' fr-AdminUserList.md` → the `:89` row tail *"| **F.W5** — surface the cap in the shared contract; client-side chunk or block |"*; ⟨cmd⟩ `grep -cE 'F\.W5[-–]W8' fr-AdminUserList.md` → **1**, the `:28` boilerplate line only — the row is NOT band-routed). F-W5 D7 states it; F-W8 §3 J4's banked-ids cell **books** it — an out-of-band re-book against F-W8's own *"no §3 row re-books an F.W5 clause"* and R-5's one-home law.

**RULED**: the ONE home is **F.W5 (D7)**. F-W8 J4 REMOVES `fr-AdminUserList FR-AUL-46` from its banked-ids cell and replaces it with a citation — *"cap bound consumed from F.W5 D7 (cited, not booked)"* — which is exactly what J4's own act cell (*"F.W8 consumes the bound, does not author it"*) already claims to be doing. **doubleHomed = 0** at close of round.

---

## PER-WAVE DIRECTIVE INDEX

| wave | directives (by ruling) | n |
|---|---|---|
| **F-W0** | R2-2.5 (anchor-stability sentence at §4; line-cites into this file forbidden downstream) · R2-10.8 (add the F.W7 reciprocal edge row) — rider: §7b's EIGHT-HOUR delta re-rooted `formation/fourier/`→`coordination/` (P2-10, same seat) | **2** |
| **F-W1** | R2-5 (TWELVE at the §4 step-4 charter, once; `:294` pin named as the twelfth limb's warrant) · R2-3d.1–3 (adopt `fr-ExportModal D-M3` ledger row, record-qualified · `fr-ContourPreview` row-29 pin row · `fr-BasisCanvas D-corpus-C-1` epistemic row) · R2-2/R2-1-LAW riders (its cross-cites to stable idiom; census "§4.2" re-spelling; PIN-LAW pin string anchored where it lives) | **3** |
| **F-W2** | R2-4 (G5 → 5/3 by its own command · DIRECT_EASINGS → 19 · G13 witness 1/1/2 · G15 anchor `:94-114` · §6b note re-keyed) · R2-5 (re-quote TWELVE ×2 via ⟨cmd⟩) · R2-9 (G19 detector inline) · R2-1-LAW (every "verbatim"/seat-measured label gains its ⟨cmd⟩ or loses its label) | **4** |
| **F-W3** | R2-3a (§X.1-v2 split roster; column 3 per id over the escapes; §X/§Z re-denominated 1,008/844) · R2-3b (book its LAW-assigned share of the 103) · R2-9 (g19 re-based on the shape-agnostic 1,008) · R2-2 (F-W0 cites → gate ids; NotationPills quote re-anchored `:23`→`:9`'s row) · R2-5 (add the TWELVE-limb citation once) — riders: PD-1's struck `v7.0.0` conformed to g6's cure; §5a disjointness re-cut over the real shared files (per-wave seat, named here so it is not dropped) | **5** |
| **F-W4** | R2-3c (book the 15 §X.1 orphans, shapes as tabled) · R2-3b/§B.2 (its share of the 103 · `PS D-M4 extended` witness joined · `D:m-6` anchor-row homed) · R2-9 (leg (b) token set `{F.W4, F.W3/W4}`, re-run) · R2-2 (four F-W0 cites → gate ids; pin string → its true home; `raw-findings.json` path-qualified) · R2-5 (re-quote ×2) · R2-1-LAW ("verbatim" at §2.C → "record-qualified") | **6** |
| **F-W5** | R2-3d.5 (C-28 fold row beside B1/C-30's R6-8 carry) · R2-7 reciprocity (confirm-by-command the M-13 id at E14 and the GalleryCard serializer id at D17; add the ID where only the mechanism sits) · R2-5 (re-quote at the F.W5→F.W1 edge) · R2-2 (its X-1 G-11/G-12 cites → gate ids; "Two tail rows" count word) · R2-9 (G19 detector inline) | **5** |
| **F-W6** | R2-1a (phantom §12 + invented quotation struck; real edge cited) · R2-1b (§6c ×5 → E2 ▲ / G3 verbatim by ⟨cmd⟩; obligation list → the `F.W5 → value.js API row` edge row; V-α's authority corrected to lane-crud §V-α) · R2-5 (re-quote) · R2-9 (FW6-G17 shape decree) · R2-2 (census-§7/COHESION-§1 mis-sections; the nonexistent homonym-file claim at §1 r4) · P2-9 cluster (branch order §2.11b conformed to its 24 bookings; FR-GFC-20 band rider + M-10 innerPoly rider restored; GalleryCard D17-fold named) | **6** |
| **F-W7** | R2-8 (posture CLOSED; G-F7-11 re-measured; count words 9→12; find-dirs) · R2-1d (C-14 split-quote, stable anchors into KF-W1) · R2-2 (the 22-row drift table converted; dissent cited by clause/gate id both ends) · R2-5 (re-quote) | **4** |
| **F-W8** | R2-7.1 (§6a four residue rows) · R2-7.2 (P8 record-qualified; B5→B1) · R2-11 (FR-AUL-46 out of J4 → citation of F-W5 D7) · R2-7.5 (R-8 canonical form everywhere stated) · R2-9 (G15 on qualified operands, then run) · R2-7.6 (quote conformed; `raw-findings` path; count words) · R2-5 (re-quote) | **7** |
| **F-W9** | R2-1c (struck-bytes quote → G-12's live bytes by ⟨cmd⟩; "the CARRY" dies with it) · R2-6a (author side of the paired splice commit; E-2 story conformed) · R2-2 (ALL 14 F-W0 cites → gate ids; screenshot/Dockerfile ±1 anchors; `--shadow-cartoon` 2→1 files; the "cartoon-surface" respelling re-keyed to G-12's published `cartoon-card` token) · R2-5 (re-quote) | **4** |
| **F-W10** | R2-6a (receive F-W9's table by splice; delete the rival paragraph; E-3 direction corrected) · R2-6b (drain from the 66: 81 strict, 16 homed, 59 not SIXTY, §5 header) · R2-9 (G-F10-7 corpus basis) · R2-2 (eight F-W0 cites; §2.8 erratum minuting :114→:116) · R2-5 (re-quote ×2) | **5** |

**Program seats**: the **Re-anchor seat** (R2-2.3, runs LAST, address-only) · the **twin-partition run** (R2-3b, F-W3 seat executes, F-W4 seat consumes).

*Rulings complete: 11 (R2-1..R2-11). Read-only everywhere except this file.*
