# F-W3 — FRESH ADVERSARIAL SPEC CHECK (L-18 / L-20, PASS 4)

**Subject**: `/Users/mkbabb/Programming/value.js/docs/tranches/X/fourier/waves/F-W3.md` (**779 lines**, 20 gates, 6 units, §X = 3 rows · §X.1 = 59 · §X.1-v2 · §X.1-v4 = 48 records / 427 id-instances · §Y errata · §Z close)
**Corpus authority**: the 66 `fr-*.md` adjudicated records at `/Users/mkbabb/Programming/value.js/docs/tranches/V/megatranche/registry/adjudicated/`. Real carries = `carry/F-W1-CARRY.md` + `carry/F-W4-CARRY.md` ONLY (⟨cmd⟩ `ls docs/tranches/X/fourier/carry/`, this seat 2026-08-29).
**Seat**: FRESH, pass 4. **No roster inherited.** The detector below was written from the task decree alone (rows in ANY format · both dash spellings · record-qualified ids · slash/range expansion **at every position**); PASS-1/2/3 counts and the RULINGS' figures were re-derived, never copied. Read-only against `/Users/mkbabb/Programming/fourier-analysis`. Sole write = this file.
**Verdict**: **DEFECTIVE** — six MAJOR. The round-3 repair's substantive work is large and, where audited, real: **all 31 per-id hand-off coordinates reproduce byte-exactly, every runnable gate witness reproduces at the live tree, the purge-seat's own corrected probes reproduce exactly, §7's rulings-file quotation is properly re-sourced, §5a's disjointness lie is truthfully re-authored with split (5) landed, and both new census blocks balance.** What fails is that **the atomisation rule g19 rules is itself lossy** — it splits the `D/D-nn` prefix-axis family, hiding 30 zero-homed banked ids and one whole record from the partition that calls itself definitive — and that three of the round's own flagship instruments (the record-qualification classification, the `L-26` control, the §Y ledger) are falsified at the settled bytes.

---

## §0 Method (fresh; id-keyed, shape-agnostic, dash-agnostic, position-agnostic, record-qualified)

1. **Routing detector.** Every `F[.·–-]?W<n>` with slash/range tails expanded to member sets, in **both** U+002D and U+2013, **and** the full-`F` compound form `F.W<a>/F.W<b>` — **W3 counts whether written first or second**.
2. **Row shape.** Table row (terminal cell routes ⊕ non-terminal cell routes) ⊕ id-headed bullet routed in context (`→` · `ADJUDICATED` · `⇒` · `rides` · `routed` · `FOLD` · `held` · `banked`) ⊕ prose line carrying a routing.
3. **Atomisation.** Ids from cell 1 / bullet head, LEFT of `=`; `·` preserved; hyphen-runs preserved; grade tails and parentheticals stripped; split on `/ ≡ ⊕ ∘ , +` **as g19 rules it** — and separately **without** the `/` split, which is what exposes D-1.
4. **Booked** = boundary-exact `(?<![A-Za-z0-9\-·])ID(?![A-Za-z0-9\-·])` against `F-W3.md`, **record-qualified**: an id occurring in ≥2 records books only where the holding line names the record.
5. Script: `…/scratchpad/w3p4/census.py`.

---

## §1 THE CENSUS (fifth independent derivation)

| measure | this seat | the spec's §0 ▲▲▲▲ / g19 | prior seats |
|---|---|---|---|
| routed rows — table **terminal** cell | **321** | 321 | 317 ×4 |
| routed rows — table **non-terminal** | **6** | 6 | — |
| routed rows — **route-marked bullets** | **747** | 830 | 681 · 730 · 731 · 742 · 764 |
| routed rows — **prose** | **127** | 42 | — |
| **routedTotal (all shapes)** | **1201** / **66 records** | 1199 / 66 | 1008 (struck) · 1048 · 1059 |
| dual `F.W3/W4` ⊕ sole `F.W3` | **1031 + 170 = 1201** ✅ balances | 1032 + 167 = 1199 ✅ | — |
| distinct `(record, id)` W3 pairs | **1021** | — | — |
| distinct head-ids / **homonym** head-ids | **613 / 180** | 137 homonyms | 140 |
| **booked** (record-qualified) | **517** | — | — |
| **escaped** | **504** / 47 records | 427 / 48 records | 256 · 264 |
| escaped **and homed in NO X·F document** | **141** / 23 records | — | 144 · 155 · 172 |

**Both table arms reproduce the spec EXACTLY (321 / 6) — a fifth exact reproduction of the arm the file says is reproducible.** The total clears the floor (1201 ≥ 1,008), so g19's floor clause is satisfied by this seat's run, and the bullet/prose spread is atomisation and route-context, as the spec says. **The partition's record set is not**: see D-2.

---

## §2 DEFECTS (ranked)

### D-1 — MAJOR · THE RULED ATOMISER DESTROYS THE PREFIX-AXIS ID FAMILY: 30 zero-homed banked ids invisible to g19, and the fifth blindness axis is unnamed after four were cured

g19 axis (iii) rules: *"then split on `/ ≡ ⊕ ∘ , +`, **`·` preserved** … boundary-exact thereafter."* The corpus heads a large family of banked ids **containing** a slash — the reader-axis-prefixed form `D/D-18`, `C/M-2`, `L/m-5` — and the spec **itself cites them in that spelling**: §C.C books `D/D-1 + D/D-4` and `fr-EasingPicker D/D-2`. Under the ruled split, `D/D-18` atomises to `D-18`, which then books on **another record's** `D-18` and reports present.

**Worked receipt, by isolation — the sibling test that proves omission rather than artefact:**

| banked id | coordinate | routing (verbatim cell) | in `F-W3.md` | anywhere in `waves/` + `carry/` |
|---|---|---|---|---|
| `D/D-11` | `fr-EasingPicker.md:76` | `**F.W3** — …` | **1** | held |
| `D/D-12` | `fr-EasingPicker.md:76` — `\| **D/D-11 + D/D-12 + D/D-13** \| Three sibling-rhythm breaks in one popup column …` | same row | **0** | **∅** |
| `D/D-13` | `fr-EasingPicker.md:76` | same row | **0** | **∅** |
| `D/D-17` | `fr-EasingPicker.md:79` | `**F.W3/F.W4** — sa…` | **1** | held |
| `D/D-18` | `fr-EasingPicker.md:79` — `\| **D/D-17 + D/D-18** \| Decorative SVG with no \`aria-hidden\`/\`role\`/\`<title>\` (preview :18-24); \`aria-label="Easing"\` duplicating the visible heading …` | same row | **0** | **∅** |

⟨cmd⟩ this seat, 2026-08-29: `for t in D/D-11 D/D-12 D/D-13 D/D-17 D/D-18; do grep -c -- "$t" waves/F-W3.md; done` → `1 · 0 · 0 · 1 · 0`; `grep -rl -- 'D/D-12' waves/ carry/` → **∅** (identically `D/D-13`, `D/D-18`). **Two ids of a three-id row are booked and the third is not; two ids of a two-id row split one-and-one.** That pattern is unreachable by a detector artefact and is the signature of a hand roster.

**The class, measured.** ⟨cmd⟩ this seat, over the 66 records, prefix-axis ids (`X/Y-nn`) appearing on a W3-routed row and absent from `F-W3.md`: **81**, of which **30 are zero-homed PROGRAM-WIDE across 7 records** — `fr-EasingPicker` (9: `L/M-2` · `D/D-9` · `C/M-1` · `L/m-5` · `C/M-2` · `L/M-1` · `D/D-12` · `D/D-13` · `D/D-18`) · `fr-CollapsibleSection` (7) · `fr-GalleryDraftsSection` (5) · `fr-EqCoefficientsPanel` (3) · `fr-ContourEditorCanvas` (3) · `fr-CoefficientsPanel` (1) · `fr-EditorControlsDock` (1). The corpus carries **85 distinct** prefix-axis ids in bold heads.

**Why this is a gate-design defect and not a counting quibble.** The wave's own §0 names the blindness axes and closes the list: *"Atomisation is the third blindness axis, after row shape (this ruling) and dash spelling (R-6)"*, with record-qualification added as the fourth. **The fifth — prefix-axis ids, where the id CONTAINS the separator the rule splits on — is never named**, and it is worse than the others because it does not merely hide a row: it **manufactures a booking**. `D-18` greps, so g19 reports the `(record, id)` pair present while the banked id `D/D-18` is nowhere in the program. That is exactly the failure mode §0 attributes to the `·` case and cures by preserving `·` (*"`L·D-1` (U+00B7) atomises to `L-D-1` and then greps nowhere"*) — the identical hazard on `/`, cured in one direction and left open in the other, inside the same sentence.

### D-2 — MAJOR · §X.1-v4's *"48 records … every one to a NAMED holder"* is FALSE — and the spec's OWN file-criterion probe convicts it

§X.1-v4 item 5 is titled *"THE PARTITION — all 427 record-qualified residue id-instances, 48 records, every one to a NAMED holder"*; §Z re-states *"v4 partitions all 48 residue-bearing records to a named holder by the file criterion."*

**`fr-EasingPicker` is not one of the 48** (⟨cmd⟩ this seat: `sed -n '653,660p' waves/F-W3.md | grep -c 'EasingPicker'` → **0**), yet it carries the D-1 residue — **9 banked ids, zero-homed program-wide**. The criterion the section calls *"one question of a PATH"* answers unambiguously against the spec here: the record's target file is **`EasingPicker.vue`**, and it is a member of **§5b's Files list** — measured by **the spec's own structural probe**, the one §X.1-v4 item 1 uses to certify the F.W4 negatives (⟨cmd⟩ this seat, 2026-08-29: `grep -h '^- \*\*Files\*\*' F-W3.md | grep -o 'AppHeader\|CanvasControlsDock\|EasingPicker\|PaperSidebar' | sort -u` → `AppHeader` · `CanvasControlsDock` · `EasingPicker` · `PaperSidebar` — **all four return**). So the dual spelling homes at **F.W3**, and the nine ids have no holder in any wave, any carry, §2, §X, §X.1, §X.1-v2 or §X.1-v4.

The record is not unknown to the file: `fr-EasingPicker` is named **four times** in `F-W3.md` (the `L30` row, the `MISSED-D` ruling, `L/M-4 + C/m-8`, `D/D-2`) — which is what makes the nine legible as omissions rather than as an unexamined record.

**Three further records** sit in this seat's residue and in no v4 row — `fr-CanvasControlsDock` (the banked head `**D-1 / L-3 / C-1**` at `:43`, routed `F.W3`/`F.W4`, six controls with no accessible name), `fr-AppHeader`, `fr-PaperSidebar` — all three with their files in §5 Files lists by the same probe. v4's hedge (*"If a later run admits a **49th** record, its home is decided by the same question about a path"*) is written for one record and is met by **four**, one of which carries nine program-wide orphans. **This is PASS-3 D-2's defect — v3's *"every"* falsified by a record the partition never admitted — recurring at the partition that supersedes it and calls itself DEFINITIVE.**

### D-3 — MAJOR · g19's RECORD-QUALIFICATION CLASSIFICATION IS FALSIFIED AT THE BYTES — the R3-3.10 remedy fails on its debut instance

Axis (iv) is round 3's headline cure, and its F-W3-side statement is deliberately a **classification** rather than a count, on R3-3.10's instruction (*"round 2's habit of banking a self-count is what let this defect hide"*). The classification reads:

> *this file carries an `M-2` record-qualified in **exactly three places** — `fr-ConvergenceTimeline M-2` (§C.A/§C.B, the 23/7 slider-scrub amendment), `fr-GalleryDraftsSection M-2` (§C.H, the primitive-not-the-wrapper cure lock), and `fr-PaperSearchDropdown D:M-2` cited at §C.D — and **not one of the three is `fr-App`'s**.*

There is a **FOURTH**. ⟨cmd⟩ this seat, 2026-08-29: `grep -noE '[A-Za-z]+ (D:)?M-2[^0-9]' waves/F-W3.md` → `219:ConvergenceTimeline M-2` · `240:PaperSearchDropdown D:M-2` · **`284:ContourSettings M-2`** (×2 on the line) · `302:GalleryDraftsSection M-2` · `386:` (the g19 cell's own four). The `:284` hit is a substantive citation, not a mention: *"…`valueCommit` DESTROYED — precisely why the consumer's busy-state defect (**fr-ContourSettings M-2**) is uncurable at the callsite."* And `fr-ContourSettings` **is** a member of the nine records the same cell's own probe returns as heading an `M-2` row (⟨cmd⟩ `grep -lE '^- [*]{2}M-2[^0-9A-Za-z-]' fr-*.md` → nine, `fr-ContourSettings.md` among them; the banked row is `fr-ContourSettings.md:59` — `- **M-2 = D-M3-as-rescoped (R-4) — MAJOR.** No busy affordance anywhere…`).

A classification stated as exhaustive (*"exactly three"*) and falsified by a fourth is not safer than the count it replaced — it is a count with the arithmetic hidden. **R3-3.10's structural cure fails in the first cell that applies it, and that cell is the one carrying the axis the whole round turns on.**

### D-4 — MAJOR · THE `L-26` CONTROL RECEIPT IS FALSIFIED AT THE SETTLED BYTES — the third generation of the same control to die, in the file that names its death as the lesson

§X.1-v4's `L-26` row (`:590`) pastes, explicitly labelled as re-run this round:

> ⟨cmd⟩ this seat, **re-run AFTER this round's writes**, 2026-08-28: `grep -rln 'L-26' waves/ carry/` → `waves/F-W4.md` · **`waves/F-W3.md`** · `carry/F-W4-CARRY.md` … A control whose command no longer returns what it prints is not a control (R2-1-LAW 1: the pasted output is the output of a command run THIS round, self-reference included)

⟨cmd⟩ this seat, 2026-08-29, same command: **SEVEN files** — `F-W4.md` · `F-W5.md` · **`F-W1.md`** · `F-W3.md` · **`F-W10.md`** · **`F-W9.md`** · `carry/F-W4-CARRY.md`. The four additions are genuine `L-26` mentions, every one consuming this very ruling (`F-W9.md:339` *"positive rulings — `fr-VisualizationView L-26` → **F.W3**"*; `F-W10.md:247` *"partition RULES **`fr-VisualizationView L-26` to F.W3**"*; `F-W1.md:406` cites *"the `GAB-25`/`L-26` idiom §X.1-v4 uses"*).

The file diagnoses this failure mode by name at §Y (`:762`): *"the failure mode where a receipt invalidates itself the instant it is written down, **which is how round 2's own `L-26` control died**"* — and offers set-membership as the cure (*"Sibling and self receipts state classifications, set-membership, or `grep -o` word-output — forms that survive the next edit"*). **This receipt IS in the blessed set-membership form and died anyway**, because R3-1's serial sequencing guarantees the siblings write *after* F-W3 publishes. The remedy is structurally insufficient for exactly the row it is showcased on, and no re-run since has caught it.

### D-5 — MAJOR · g19's SPELLING AXIS ENUMERATES A TOKEN SET NARROWER THAN THE CORPUS, WITH NO POSITION AXIS — the defect R3-5.1 re-cut F-W2's G19 for, uncured here

g19 axis (ii): *"**spellings** — the token set `{F.W3, F.W4, F.W3/W4}` in **BOTH** U+002D and U+2013, every slash form expanded to its member set."* Measured against the corpus, that enumeration is short by two live spellings:

| spelling | occurrences | records |
|---|---|---|
| `F.W3/F.W4` (full-`F` second member) | **9** | fr-ConvergenceTimeline · fr-ContourSettings · fr-EasingPicker · fr-EasingCurvePreview |
| `F.W1/F.W3` (**W3 in second position**) | **5** | — |
| `F.W1/W3` (**W3 in second position**) | **1** | fr-AdminFlaggedPanel `:87` (FR-AFP-33's client limb) |

⟨cmd⟩ this seat, 2026-08-29: `grep -rhoE 'F\.W[0-9]+/F\.W[0-9]+' fr-*.md | sort | uniq -c` → `9 F.W3/F.W4` · `5 F.W1/F.W3` · … ; `grep -n 'F\.W1/W3' fr-*.md` → `fr-AdminFlaggedPanel.md:87`.

**No POSITION axis is stated anywhere in g19** — the standard detector R3-5 makes program-wide names four axes, the fourth being *"**positions** — a member matches whether written first or second in a compound"*, and R3-5.1 re-cut **F-W2's G19** on precisely this (*"the probe `F[.·–-]?W2` cannot match a slash form whose SECOND member is W2"*). The sibling's cure was not generalised to the wave that publishes the partition every other wave consumes.

**The two defects have one root.** `fr-EasingPicker`'s `D/D-18` row (D-1, D-2) routes in the unenumerated `F.W3/F.W4` spelling — ⟨cmd⟩ `sed -n '79p' fr-EasingPicker.md | grep -oE 'F\.W[0-9]+/F\.W[0-9]+'` → `F.W3/F.W4`. A detector reading only the three enumerated tokens never sees the row, so the record never enters the residue to be homed. Under R2-9 as this file restates it, **a gate stating a restricted operand is DEFECTIVE at authoring, whatever it returns.**

### D-6 — MAJOR · §Y's DISPOSITION LEDGER DOES NOT BALANCE, in the section that discharges R2-1-LAW and under a header asserting completeness

§Y's header: **"THE DISPOSITION, BY CLASS — 47 of 47, no residue."** Its table: **A 18 · B 20 · C 1 · D 1 · E 8** — ⟨cmd⟩ this seat: `18+20+1+1+8 = ` **48**. And its own measurement block partitions the pre-round bytes as `REPRODUCES 18 · DRIFT 21 · no quoted span 8` (= 47 ✅), against which the drift-derived dispositions are `B 20 + C 1 + D 1 = ` **22**, one more than the 21 measured.

This is the class §0 ▲▲▲▲(1) convicts round 2 for, in this file's own words — *"a spelling partition that does not exhaust its roster is not a partition"* — recurring inside the ledger whose entire purpose is mechanical completeness over a label class. The section correctly disclaims gate weight (*"never an operand of any gate"*), which bounds the damage; it does not repair a completeness claim falsified by its own table.

**Mitigation of record, so this is not read as fabrication**: §Y's *live* probe reproduces exactly. ⟨cmd⟩ this seat: `awk '/verbatim/ && !/⟨cmd⟩/' waves/F-W3.md | wc -l` → **28** whole-file; on §Y's **declared** operand boundary (the spec minus §Y, `:731`–`:764`) → **26**, the banked figure, exactly; and `grep -c 'paraphrase — drift noted' F-W3.md` → **20**, matching class B. The declared self-exclusion is honest and it works.

### D-7 — MINOR · §0 ▲▲'s census block was NOT re-pasted from a balancing run, as R3-4.3 expressly directed

R3-4.3 directs: *"the pasted block whose partition sums 903+151=1054 against a 998/1081 roster is **re-pasted from a balancing run**."* It was not. §0 ▲▲ still carries, under a `⟨cmd⟩ this seat … 2026-08-28` attribution and with no marking at its own site:

```
W3-axis table rows  : 317   …   W3-axis bullet rows : 681 / 764
dual F.W3/W4 rows   : 903        sole F.W3 rows : 151
```

`903 + 151 = 1054`, which exhausts neither `317+681 = 998` nor `317+764 = 1081`. The disclosure exists — two blocks below at ▲▲▲▲(1), which states the arithmetic plainly — so nothing is concealed; but R3-3.8 allows a non-reproducing receipt only *"fixed from the live output or struck together with every claim that depends on it — **no third disposition**"*, and retention-with-remote-disclosure is a third disposition. A reader arriving at ▲▲ is handed a this-seat-dated receipt whose partition cannot close.

### D-8 — MINOR · g7's witness token cannot match half the sites it counts

g7's RED baseline: *"`.easing-preview` = 4 grep hits across `EasingCurvePreview.vue` + `MorphPhaseConfig.vue`"*; its GREEN: *"`.easing-preview` collapses to one home or disappears."* ⟨cmd⟩ this seat, 2026-08-29: `grep -rn '\.easing-preview' web/src` → **2** (`EasingCurvePreview.vue:37`, `MorphPhaseConfig.vue:198`); `grep -rn 'easing-preview' web/src` → **4** — the other two are template attributes carrying **no dot** (`EasingCurvePreview.vue:23` `class="easing-preview"`, `MorphPhaseConfig.vue:47` `<svg class="easing-preview" …>`).

The count 4 is right; the printed token is not the probe that returns it. A green run keyed on the cell's own spelling reports the class collapsed while two live sites survive — the narrow-detector class this wave convicted twice in its own gates (R-6's dash blindness; g17's separator-agnostic forbid-list, re-cut precisely because *"a detector that cannot match the bytes it forbids is a defect at authoring, whatever its result"*).

### D-9 — MINOR · §X.1-v4 item 6's probe output is a partial return presented as whole, and it is the premise of the seam argument

Item 6: *"`FR-EMT-23` · `D·D-M12` · `m-15` · `i-6` already grep in this file (⟨cmd⟩ `probe.py FR-EMT-23 D·D-M12 m-15 i-6` → **`F-W3.md` for all four**) and are **held here**, so an F.W4 booking of them would be the out-of-band re-book R3-6's tie-breaker exists to prevent."* ⟨cmd⟩ this seat, boundary-exact over `waves/` + `carry/`: `FR-EMT-23` → **F-W3 · F-W4**; `D·D-M12` → **F-W3 · F-W4**; `i-6` → **F-W3 · F-W4**; `m-15` → **F-W3 · F-W4 · F-W5 · F-W6 · F-W7 · F-W8**. The load-bearing half of the claim (*"already grep in this file"*) is TRUE for all four; what is not disclosed is that all four already grep in **F-W4** too — which is the exact condition the sentence argues does not obtain. PASS-3 D-7's elision class, at the row where the elision changes the argument.

### D-10 — MINOR · §1 ▲▲'s `live at` receipt prints one of two returned lines without disclosing the elision

§1 ▲▲ pastes `⟨cmd⟩ … grep -n 'live at' fr-NotationPills.md:` followed by a fenced block containing only `9:…`. ⟨cmd⟩ this seat: the command returns **two** lines — `9:` and `124:`. Line 124 IS quoted immediately below by a separate `sed -n '124p'`, so nothing is hidden and the substance is correct (line 9 does carry both fragments — verified fragment-by-fragment this seat: `EquationPanel.vue (live at` ✅ · `imports NotationPills :9, mounts :96` ✅ · `--pill-c` ✅ · `GallerySearchBar.vue:121/:214-221` ✅ · `GalleryCard.vue:121/:271-273` ✅ · `GalleryCardModal` ✅). The defect is the discipline: the file applies it correctly at §0 ▲▲▲ (`C-C-2` → *"5 lines … of which line 62 is quoted and the other four are elided, disclosed rather than presented as the whole return"*) and drops it here — PASS-3 D-7 cured at one site and not the other.

---

## §3 WHAT VERIFIES (recorded so it is not re-litigated)

**The per-id hand-off table — 31 of 31 coordinates byte-exact.** Every id/coordinate pair in §X.1-v4 item 2 reproduces at the frozen corpus, this seat, 2026-08-29: `fr-BasisCanvas` `BC-3`:46 · `BC-7`:53 · `BC-12`:81 · `BC-14`:82 · `BC-15`:83 · `BC-16`:84 · `BC-18`:85 · `BC-22`:99 · `BC-23`:100 · `BC-24`:101 · `D-10-remnant`:88 · `M-α1`:60 · `M-α4`:89 · `M-α7`:91 · `M-α8`:92 · `M-β2`:62 · `M-β7`:108 · `M-β8`:109 · `fr-ContourEditorCanvas` `MM-1`:55 · `MM-2`:68 · `MM-3`:69 · `MM-4`:70 · `MM-7`:89 · `MM-8`:90 · `MM-9`:91 · `MM-12`:98 · `fr-EquationResult FR-EQR-21`:59 · `fr-GalleryAdminBanner GAB-25`:67 (incl. *"rides FR-COB-18's F.W3/W4 …"*) · `fr-VisualizationView L-26`:91 · `fr-CoefficientsSpectrum m-19`:90 · `m-22`:93. **F.W4's born-RED zero-run has exact tokens to be written against — the round-2 count-only hand-off is genuinely dead.**

**The purge-seat's own corrections reproduce EXACTLY.** ⟨cmd⟩ boundary-exact over `waves/` + `carry/`: `BC-20` → `F-W2 · F-W3 · F-W4 · F-W5 · F-W7 · F-W8` (banked: identical) · `M-β1` → `F-W2 · F-W3 · F-W4` (identical) · `M-β3` → `F-W2 · F-W3 · F-W4` (identical). `for t in BC-20 M-β1 M-β3; do grep -c -- "$t" F-W4.md; done` → **4 · 6 · 4**, as banked. The struck-as-satisfied disposition is correct at the bytes.

**PASS-3's nine defects, re-tested:** D-1 (the `9 → 0` alias cell) **CURED** — re-pasted as `9 → 2` with `m-19`/`m-22` named and homed · D-2 (§X.1-v3's *"every"*) **CURED at fr-CoefficientsSpectrum, re-opened elsewhere** — the record is now `.d`'s (`grep -ho 'shared/CoefficientsSpectrum.vue' F-W3.md | sort -u` reproduces), but see D-2 above · D-3 (record-blind g19) **CURED IN TERMS, defective in application** — axis (iv) is stated inline with a runnable rule; see D-3 · D-4 (§5a disjointness) **CURED** — re-titled *"Conflict declaration and sequencing"*, the false sentence struck at both sites, and **split (5) landed** for the §C.I batch toolbar with a probe that reproduces (`grep -h '^- \*\*Files\*\*' F-W3.md | grep -o 'Admin[A-Za-z]*' | sort -u` → `AdminAuditLog` · `AdminFlaggedPanel` · `AdminUserList`) · D-5 (R2-1-LAW 2/3) **CURED** — §Y is a real mechanical sweep (26 reproduces on its declared boundary; 20 labels demoted to `paraphrase — drift noted`), and §7's rulings-file quotation is **struck and re-sourced** onto the banked `fr-NotationPills.md:35` fact with a set-membership receipt that reproduces (`grep -rl 'RED BY CONSTRUCTION at the installed' ../conformance/ …/adjudicated/` → `PASS-3/F-W3-CHECK.md` · `PASS-1/RULINGS.md`, and ∅ over the 66 records) · D-6 (check-file denominator) **CURED** — the `691/62/1,008/844` quadruple is struck as an operand, `1,008` kept as floor only, and both replacement blocks BALANCE (`321+6+830+42 = 1199 = 1032+167`; `317+742 = 1059 = 885+174`); the ▲▲ block is the residue, D-7 · D-7 (undisclosed elision) **CURED at §0 ▲▲▲** (`C-C-2` → 5 lines, 10/62/90/106/137, reproduced exactly this seat; four elided *and disclosed*), uncured at §1 ▲▲ (D-10) · D-8 (bracketed `h[onour]`) **CURED** — `grep -c 'structurally cannot honour' fr-ContourSettings.md` → **1** · D-9 (`MG-λ`) **CARRIED as disclosure**, unchanged.

**Gate witnesses — every command re-run against the live trees, all REAL, all born-RED:**

| gate | claim | measured this seat |
|---|---|---|
| g1 | 8 e2e specs / 0 `paper-search*` hits; `PaperSearch.vue:41` = `<style scoped>` | **8** · **0** · `<style scoped>` ✅ |
| g2 | 23 declarations / 7 files; glass-ui → EMPTY | **23 / 7 / 0** ✅ |
| g3 | `GlassTimeline.vue` 127 · `ConvergenceTimeline.vue` 146; `glass-ui/timeline` → 0 | **127 / 146 / 0** ✅ |
| g7 | `.easing-preview` = 4 across 2 files | **4** on the bare token; **2** on the printed one — D-8 |
| g12 | forms / search / fading-scroll → 0 / 0 / 0 | **0 / 0 / 0** ✅ |
| g15 | `function timeAgo` = 5; `startsWith("fourier")` = 7 | **5 / 7** ✅ |
| g17 | `lane-frontend.md:446` headline + separator forensics | ✅ byte-exact; `1990`→0 · `2079`→0 · `1 990\|2 079`→1 · U+2009→0 · `1 315`→1 |
| g18 | `git status --porcelain \| wc -l` → 28 | **28** ✅ |
| §5a-v2 | `is-active` → 11 files; `<Tooltip` → 11 files; `PaperView.vue:344` | **11 / 11 file-for-file identical to the printed lists and to their §5-owner mapping**; `:344` = `                        :is-active="isActive"` byte-exact ✅ |
| §X.1-v4 §1 | four negatives resolve at the tree, absent from every §5 Files line | `find` returns all four ✅; structural probe → **no output** ✅ |

**Cross-spec receipts — every one reproduces by STABLE anchor (R2-2 landed):** `grep -h '^### G-1[123] ' F-W0.md` → all three headings byte-exact ✅ · `grep -h '^### 2a' F-W0.md` → `### 2a. File bounds` ✅ · `grep -h '^## ' F-W0.md` → `## 4. Gates — all born-RED` ✅ · F.W1's TWELVE-limb roster, `grep -ho 'The roster is TWELVE limbs[^*]*' F-W1.md` → byte-exact, the F.W1 atomic transaction cited whole at its pinned §-anchor ✅ · **zero** `F-W0.md:<digits>` coordinates remain ✅.

**Corpus quotations — every audited quote byte-exact at its coordinate:** `DU-missed-2` `fr-CoefficientsPanel.md:71` ✅ · `D-24-as-corrected` `:42` ✅ · `r1-missed-2` `fr-GalleryDraftsSection.md:82` ✅ · `C-C-2` 5 lines ✅ · `D-m10`/`N-10` `fr-DarkModeToggle.md:96` ✅ · `v7.0.0:Slider.vue` 3 matches at `:11`/`:46`/`:106` ✅ · `MG-α` `fr-App.md:58` · `MG-ε` `:78` · `MG-η` `:80` · `MG-λ` `:95` ✅ · `M-2` `fr-App.md:49` ✅ · `MAJ-5` `fr-VisualizationView.md:59` · `MAJ-7` `:61` · `FR-IC-16` `fr-InfoCard.md:48` · `FR-GSB-30` `fr-GallerySearchBar.md:70` ✅ · `FR-NP-32` `fr-NotationPills.md:35` ✅ · `W3-button-ledger.md:93` = the `A.W3.b.1` ledger row, `grep -ic 'policy'` → **0** ✅ · `fr-ContourSettings M-16` held in `F-W3.md` and neither carry ✅.

**M-25 depth — the banked-id locks are present and correctly spelled:** `FR-NP-32 (≡ fr-PaperSidebar M1)` **canonical at 6 of its sites**, both ids cited and neither substituted (R3-8's form, honoured though F-W3 was not directed under it) · `MPC-31`'s ONE-CUT LAW carried, now correctly labelled `(paraphrase — drift noted, §Y)` rather than verbatim · `FR-MSP-6` the void-cure lock, carried with its row and named so it cannot read as dropped · `PAW-44/LAW-3` restore-only-with-background-cure, carried at three sites incl. §X · the **four anti-cures** all present and numbered · the TWIN LAW stated once with its direction (*"where v4 assigns an id to F.W3, F.W4 cites; where v4 assigns to F.W4, F.W3 cites. The converse never occurs."*).

**Posture:** `Status: planned` at `:82` and `:770`; **zero** VERIFIED stamps (`VERIFIED | **NO**` `:91`; `VERIFIED NO` `:770`); `EXECUTION IS NOT AUTHORIZED` and fourier tree READ-ONLY at `:7`; **20 gates all born-RED**; owner flags inline (S-6 a/b/c, OP-1/OP-2/OP-5); SS-4's obligations correctly disowned to SS-4 with the `basisFilter` WAVE-LOCK the only imposition; **F.W7 zero-row posture intact** — three mentions, all the declared-not-carried KISS-guardrail edge, no re-derivation of F.W7's roster and no position on R2-8; **no `F-W3-CARRY.md`** (R-3 item 2 honoured); *"the CARRY"* with no path absent; `registry/adjudicated/**` out of bounds.

**RULINGS-3's six F-W3 directives:** R3-1a ✅ (§X.1-v4 published FIRST; the 28 named per id with the 18-vs-19 slack settled at the bytes and `BC-14`/`BC-15` restored; `fr-CoefficientsSpectrum` in; `L-26` ruled to F.W3 with its departure record — subject to D-2/D-4) · R3-3.3 ✅ (the alias cell re-pasted `9 → 2`, §Z's dependent claim re-worded to *"dissolves MOST of"*) · R3-4.3 ✅ (`691/62` struck; fresh balancing denominators — subject to D-7) · R3-5.3 ✅ **in terms** (axis (iv) inline and runnable — subject to D-3/D-5) · R3-9.4 ✅ (§5a truthfully re-authored; split (5) landed) · R3-3.9 ✅ (§Y sweep real — subject to D-6).

---

## §4 VERDICT

**DEFECTIVE.** Six MAJOR (D-1 the ruled atomiser destroys the prefix-axis family, 30 orphans · D-2 §X.1-v4's *"every"* false by four records · D-3 the record-qualification classification falsified · D-4 the `L-26` control dead a third time · D-5 the spelling axis short by two live forms, no position axis · D-6 §Y's ledger does not balance), four MINOR.

The distance travelled since pass 3 is the largest of the programme: the hand-off is real per id and byte-exact at all 31 coordinates, the disjointness lie is gone with a fifth split declared, the rulings-file quotation is struck and re-sourced, the alias cell is corrected against its own falsification, §Y is a genuine mechanical sweep whose live probe reproduces, and both census blocks balance. What has not travelled is the **class**. Four blindness axes were named and cured — shape, dash, alias, record. The fifth was never named, and it is the one that does the most damage: **an id that CONTAINS the separator the atomiser splits on does not merely vanish from the roster, it books on a stranger's bytes**. `D/D-18` greps as `D-18` and reports home while the banked id exists nowhere in eleven specs and two carries. The partition that calls itself definitive is derived through that atomiser, so the record carrying nine such orphans was never eligible to enter it — the identical mechanism by which v3 lost `fr-CoefficientsSpectrum`, one axis further down.

*Seat's own writes: this file only. Everything else read-only.*
