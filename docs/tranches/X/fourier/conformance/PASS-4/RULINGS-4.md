# X·F CONFORMANCE PASS 4 — RULINGS-4 (repair round 4)

**Seat**: BLESS + RULINGS-4 seat (Fable), 2026-08-29. **Input**: `PASS-4/UNION.md` (12 union defects, 2 BLOCKER · 4 HIGH · 4 MEDIUM · 2 LOW; §0 overturn + 3 adjudications; §4's nine unapplied RULINGS-3 directives) · `../CENSUS-CANONICAL.md` **as re-derived and STAMPED by this seat this session** (72 divergences fixed in place, §0.3; totals re-based 4278 → 4269; §1 = §2 = §3 machine-verified) · the product bytes at `fourier-analysis/web/src` for the G10 twin (read this session).

**Standing order, restated once**: the bank, the twin, the drain and the double-home law HELD at pass 4 — nothing below re-opens an adjudicated record, a carry, or a banked id. Every directive is a repair to RECEIPTS, OPERANDS, SCOPES and BOOKINGS. **Ten rulings, R4-1..R4-10.** Every cure lands under R4-8's sequencing (the purge seat runs TRULY LAST) and R4-2's portability law, and **no cure lands without its receipt re-run by a seat that did not author it** (UNION §5's falsifier, adopted as standing law here).

---

## R4-1 — THE TWELVE FALSE-AT-WRITE RECEIPTS, NAMED WITH CURES, AND THE CLOSE-CERT ERRATUM (cures P4-2)

Law first: **a receipt may only be published beside the command that produced it, run on the pinned toolchain (R4-2), by-or-re-run-by a non-author seat.** A cure below that requires a re-run states the command SHAPE; the repair seat pastes the true output — this file mints no receipts of its own.

| # | site | cure |
|---|---|---|
| 1 | **F-W0 §6b** (drifted F.W7 reciprocal; bare `(M1)`; §11 mis-address) | Re-paste the TRUE span from `F-W7.md` bytes — the canonical pair `FR-NP-32 (≡ fr-PaperSidebar M1)` — and re-address §11 → §7c. The words *"quoted here at its bytes"* may return only above a fresh `grep -Fc` receipt run by a non-author seat. |
| 2 | **F-W2 B11** (banked FR-NP-9 quotation returns ∅; printed receipt returns a different id) | Strike the pasted span; re-quote from `fr-NotationPills.md` with the em-dash preserved and no added period, beside the `grep -Fn` that returns exactly it. |
| 3 | **F-W3 g19 axis-(iv)** ("exactly three places") | Correct to the measured FIVE (`:219 · :240 · :284 · :302` + the cell's own `:386`); the debut classification is re-written from the corrected count. |
| 4 | **F-W3 §X.1-v4 L-26 control** ("re-run AFTER this round's writes → 3 files") | Re-run at the ROUND-4 settle and publish the live figure (7 files at the pass-4 settle), or STRIKE the control; a control that has died three generations may not be re-pasted un-run. |
| 5 | **F-W4 §2.X.1(E)** (pasted output is a `sort -u` the command does not perform) | Either append `\| sort -u` to the printed command, or paste the raw five-line output with `EqCoefficientsPanel ×2` disclosed. One or the other; not the current chimera. |
| 6 | **F-W5 B4** (minted coinage in the banked-quotation idiom) | Strike the ***-marked span or source it (file:line + `grep -Fc` → 1). The triple-star idiom is RESERVED for verified quotes; a coinage wears plain text. |
| 7 | **F-W6 §2.6 Q-4** (the cure broke its own command — paste unreachable at any `-o` bound) | See R4-4.2 — the round-3 cure is unwound there; the re-cut command and its true paste land together. |
| 8 | **F-W7 §2a** ("FIVE directories" — live eight, 94 minutes before the write) | Re-run `find . -type d` and paste; the load-bearing-negative framing survives only over the true count. |
| 9 | **F-W7 §0 item 7** (universal "every ⟨cmd⟩ re-run, every one reproduced, no third case") | Strike the universal; replace with the enumerated exceptions (items 8/10 of the register + the two `.{0,262}` non-executables). An attestation-of-sweep must list its residue or not exist. |
| 10 | **F-W7 G-F7-1** (receipt 11 chars longer than the command's output, cut undisclosed) | Truncate the published receipt to the command's true 134 chars ending `…the recorded shipped` WITH the cut disclosed, or widen the command so it prints the published span — then re-run. |
| 11 | **F-W9 §2.8a** ("the one that failed is E-10(b)" — false by one: FR-EMT-11's with-period span → 0) | Correct to TWO failures; G-F9-21's face gains its FOURTH paired obligation (R4-7). |
| 12 | **F-W10 §2.5a** (band-correction receipt cannot produce its printed 44 at any reading) | The FIGURE stands; the RECEIPT is re-cut: drop `-h` so `cut -d: -f1` receives filenames, add both boilerplate filters, re-run, paste. If the honest pipeline prints something other than 44, the figure follows the pipeline — never the reverse. |

**R4-1.E-3 · THE CLOSE-CERT ERRATUM (dated row, not a rewrite).** `PASS-3/CLOSE-CERT.md` is FALSIFIED on four counts (UNION §2). Per E-3 (addenda-not-patch) the cert body is NOT edited; the repair seat APPENDS one dated erratum row:

> **ERRATUM 2026-08-29 (pass-4 union, verified at the bytes):** (i) §2/§9's "F-W7 … no command-level defect" is false — three command-level defects (§2 items 8–10) plus two receipts non-executable on the declared engine; (ii) §5's "no third disposition survives anywhere in the eleven" is false by one (§2 item 12); (iii) §6.1's "724 label sites … 0 word-level drifts" is false — ≥25 surviving/new drifts under verbatim labels (P4-9); (iv) §3d's "Rooted to `$R` and re-run" is false — `$R` is defined nowhere in F-W5 (3 uses, 0 definitions). **This certificate may not be cited as an operand anywhere without this row.** No certificate issues for round 4 until its own claims are re-run by its successor seat (UNION verdict, clause 3).

---

## R4-2 — THE PORTABLE-COMMAND LAW (cures P4-3; standing, program-wide)

**Every ⟨cmd⟩ published in any wave, check, union, or certificate MUST execute on the pinned toolchain: `bash` + BSD `/usr/bin/grep` / `sed` / `awk` on the recorded host.** Concretely and forever:

1. **No `-P`, no `-coP`, no `\K`, no lookaround** — PCRE does not exist on the pinned binary (exit 2). Re-cut in `-E`.
2. **No `.{n,m}` with m > 255** — the BSD bound is 255; `.{0,262}` is incurable at the legal maximum (the F-W7 lesson: at 255 the output stops 7 chars short of its paste). Windowed extraction uses two stages (`grep -n` to locate, `sed -n 'A,Bp'` or `awk 'NR>=A && NR<=B'` to print) — never a giant bounded wildcard.
3. **Every shell variable a ⟨cmd⟩ consumes is DEFINED in the same published block** (`R=…` on the line above its first use). An undefined `$R`/`$V` is a phantom witness — the purge seat itself minted one (P4-2, cert count iv).
4. **One command, one base**: a receipt may not splice two commands over one undisclosed base (F-W0 §7b).
5. **The check gate**: any pass-5 seat that meets a ⟨cmd⟩ failing 1–4 convicts it ON SIGHT as unrunnable machinery — no charitable reading, no "intent was clear".

**The named unrunnable clusters, re-cut in `-E` form** (the wave seat runs each and pastes the output; templates only here):

| wave | dead form | portable re-cut |
|---|---|---|
| **F-W4** (flagship witness + 6 siblings) | `grep -coP '…\K…'` | `grep -Ec '<same alternation, anchors made explicit>'` — where `\K` trimmed a prefix, match the full span and count lines; where a capture was extracted, pipe `grep -Eo '…'` → `sed -E 's/^<prefix>//'`. |
| **F-W7** (two `.{0,262}` receipts) | `grep -Eo '^.{0,262}anchor'` | `L=$(grep -nF '<anchor>' FILE \| cut -d: -f1); sed -n "${L}p" FILE \| cut -c1-262` — the window moves to `cut`, which has no 255 bound. |
| **F-W5** (`$R` ×2, purge-minted) | `grep … "$R"/…` with `$R` undefined | Prepend `R='<the intended root, spelled>'` in the same block; re-run both. |
| **F-W2** (undeclared `$V`) | `… "$V" …` | Same: `V='<value>'` declared in-block; re-run. |
| **F-W0 §7b** (two-cmds-one-base) | two piped fragments, base stated once | Publish as ONE pipeline over ONE stated base, or two complete commands each with its own base. |
| **F-W3** (the L-26 control, dead three generations) | prose count with no runnable form | `grep -rlF 'L-26' waves/ carry/ \| sort` published WITH its file list — the control becomes self-verifying or it is struck (R4-1.4). |

---

## R4-3 — CHECK-FILE OPERANDS RE-BASED ON THE CANONICAL AT FOUR WAVES (cures P4-4; enforces R3-4)

R3-4 stands program-wide: **a check file is never an operand; now, additionally, a superseded census is never a denominator.** The four convicted sites re-base on `../CENSUS-CANONICAL.md` (the stamped census of record — a gate citing any other census operand is DEFECTIVE on sight):

1. **F-W9 masthead** — strike PASS-1 as closure co-operand; the wave's id-level denominator is the canonical **F.W9 roster (17)**, enumerated from §2, not from any pass index.
2. **F-W8 §-arithmetic (84/124)** — the check-descended figures are struck; F-W8 carries **ZERO record-side routings** (canonical §4.1 — no record names F.W8), so its census is gate-machinery only, enumerated from its own fold/operand lists and NEVER from a check file.
3. **F-W0 §6a lock 11** — the lock re-derives from the canonical F.W0 roster (**56**) and the record bytes; the check-file-derived 11 is struck.
4. **F-W1 §6·R1 pasted 42-roster** — replaced by the canonical **F.W1 roster (362)** filtered to the surface under test, enumerated by ⟨cmd⟩ over §2 — the claim-under-test may not supply its own witness roster.

---

## R4-4 — THE FIVE ROUND-3-MANUFACTURED CURES, UNWOUND (cures P4-6)

1. **G10's twin-scope over-correction — FIRST, and verified at the product bytes by THIS seat (2026-08-29).** The bytes: `CollapsibleSection.vue` carries `.collapsible-content { overflow: hidden }` (≈:54-56), the A.W3.d comment, the two `[data-state]` `animation: collapsible-open/close` shorthands (≈:60-65), and the PRM `animation: none` arm (≈:66-71); `ContourSettings.vue` mirrors it exactly — `.advanced-content { overflow: hidden }` (≈:362-364), the B.W2.d comment, the two `.advanced-content[data-state]` shorthands (≈:365-370), the PRM arm (≈:371-377). **THE CORRECT TWIN SCOPE: the deletion pair is the two `[data-state]` animation shorthands + the PRM arm on EACH file, SYMMETRICALLY — and `overflow: hidden` is EXCLUDED on BOTH files** (it is load-bearing clipping; its retirement is fr-CollapsibleSection m-16's separate question). The round-3 range — swallowing `.advanced-content { overflow: hidden }` + the PRM arm on ContourSettings while leaving both on CollapsibleSection — is REVOKED: executing it strips load-bearing product CSS asymmetrically (the first product-breaking defect of the programme). G10 re-states its ranges per-file against these bytes, re-resolved at cure time (D-19).
2. **F-W6 §2.6's Q-4 command** — the round-3 paste/command pair is REVOKED; the repair seat publishes a NEW pair under R4-2 (windowed two-stage extraction), pasting only what the command prints, cut disclosed.
3. **F-W0 §6b's R3-3.1-minted reciprocal** — revoked and re-pasted per R4-1.1.
4. **F-W10 §2.5a's band-correction pipeline** — revoked and re-cut per R4-1.12.
5. **The purge seat's `$R` rooting (×2 in F-W5)** — revoked; re-rooted per R4-2.3 with `$R` defined in-block; and the CLOSE-CERT clause that certified it fixed is covered by the R4-1 erratum.

**Standing falsifier (from UNION §5, now law): no cure lands without its receipt re-run by a seat that did not author it.** Round 3 proved the cure apparatus is itself a defect source; round 4's cures are two-key.

---

## R4-5 — THE TWO IRRECONCILABLE DENOMINATORS, REPLACED BY CANONICAL FIGURES (cures P4-7)

- **F-W5's 170 is STRUCK** (its four shape arms false-at-two by cancelling ±40; two constructions of 170 disagreeing at three members). The record-side denominator for the F.W5 band is the CANONICAL: **F.W5 = 28 rows · F.W5-W8 = 90 rows** (§2/§3 of the stamped census; 118 record-side rows in the band, per-record enumeration in §2). F-W5's own booked-row count is whatever its §-registers enumerate — it may DERIVE nothing arithmetically; it counts lines and cites the canonical for the record side. The halt operand keyed to the dead 170 is re-keyed to the enumerated register count.
- **F-W6's 132 is STRUCK** (unproducible from its own §2.11c rule — 111 or 153, never 132). The record-side figure for F-W6 is **ZERO** (canonical §4.1: no record routes F.W6; four passes measured "escapes" against record-side rows that do not exist). F-W6's census is re-scoped to its actual population — folds and gate operands — enumerated by register, with the canonical cited for the record-side zero.

---

## R4-6 — THE LEG/RIDER DROPS, BOOKED AT THEIR HOLDERS (cures P4-8)

Each leg books as ONE explicit row at the wave that HOLDS the host id — a leg is never a new identity (§0.1 of the canonical):

1. **PP-NOSHADOW's F.W1 rider (holder: F-W3)** — F-W3 adds the rider row, and its G12 target gains the qualification: **`./easing` is a LIVE cross-package homonym — value.js AND glass-ui@7 both export the subpath** (verified at the union seat). Any re-point gate on `./easing` must name WHICH package's subpath it means; unqualified, the gate is ambiguous-by-construction.
2. **fr-SpeedSelect D-03's terminal F.W1 DECISION leg (holder: F-W4)** — F-W4 adds the leg row with the DECISION verb spelled (the terminal leg is a ruling obligation, not a routing).
3. **GM-6's enabling F.W1 rider (holder: F-W4)** — F-W4 adds the enabler row; the enabled work cites it.
4. **AA-23's host row (holder: F-W5/F-W8)** — the host row is booked at F-W5 (its canonical home is F.W5 per §2); F-W6 keeps only the killed-cure citation, marked as such.
5. **FR-EQR-14's F.W3/W4 half** — resolved BY THE CANONICAL: the id is booked whole at its record's canonical home; F-W4 books it or cites the canonical row (its prior sole-citation-of-the-F.W1-leg is an escape, cured by the booking).

---

## R4-7 — VERBATIM-LABEL LAW, FIFTH GENERATION (cures P4-9) + G-F9-21's FOURTH PAIRED OBLIGATION

1. The ≥25 surviving/new drift sites under verbatim labels (F-W2's six-site emphasis set · F-W9's D2 span · F-W10's four punctuation drifts + elided-emphasis label · F-W6's four unmarked truncations + row-21 prepend · F-W5's undisclosed `rev|cut` half-paste · F-W7's D-8/D-10 subset pastes · F-W4's two subset prints) are cured at their sites: **re-paste byte-true, or re-label** (`paraphrase`, `subset — cut disclosed`). The verbatim label means bytes; there is no third meaning.
2. **G-F9-21 gains its FOURTH paired obligation**: the FR-EMT-11 span (with-period → 0, without → 1) joins E-10(b) and the two prior pairs ON THE GATE'S FACE — the gate's own text enumerates four, and §2.8a's count corrects per R4-1.11. The same face has under-counted twice in two rounds; the gate now carries its own census command (`grep -Ec` over its enumerated spans) so the count is machine-checkable.
3. Count-words (P4-11) ride the same law: every "N ids/files/records" adjacent to an enumeration must equal the enumeration; where the prose and the list disagree, the LIST is authoritative and the prose is corrected — all seven named sites (F-W4 "23"/24 · F-W6 "FIVE"/eleven · F-W0 "other 30"/8 · F-W10 11-vs-12 + 26-vs-22 · F-W7 "(both passes)"/4 · F-W9's E-1 absolute · F-W5's occurrences-vs-lines) are corrected in place.

---

## R4-8 — THE SEAM-STALENESS CURE: THE PURGE SEAT RUNS TRULY LAST, WITH THE SHA256 CERT (cures P4-10; LAW E imported from X·KF)

The F-W8-masthead-vs-F-W5-rewrite breach (19:15 vs 19:19) proves ordering by intention fails. Round 4 adopts **LAW E**:

1. **Strict order**: every repair seat lands → the UNION/adjudication seat re-reads and re-hashes → **the purge seat runs LAST, touching every file AFTER every other seat's final byte.**
2. **The sha256 certificate**: the purge seat's closing act is a table — `shasum -a 256` over every wave, carry, check, union and census file — computed AFTER its final edit. Any seat that writes after the cert is issued VOIDS the cert by construction (the hashes prove it mechanically).
3. **Cross-file pastes carry the hash**: any masthead or cell that quotes a SIBLING file's count/span records the sibling's sha256-at-quote-time (first 12 hex suffice). A later rewrite of the sibling is then detectable, not deceptive — the F-W8 167 class dies here.
4. The round-4 cert issues only after its own claims are re-run by the successor seat (R4-1.E-3's closing clause).

---

## R4-9 — THE NINE UNAPPLIED RULINGS-3 DIRECTIVES, RE-DIRECTED (cures UNION §4)

| # | directive | round-4 re-direction |
|---|---|---|
| 1 | **R3-3.8** (purge transcript per-spec) | The centralised CLOSE-CERT transcript is void with the cert (R4-1.E-3). The round-4 purge appends its transcript to EACH spec's errata section — F-W4 first CREATES an errata section (three inline PURGE-SEAT notes migrate into it). |
| 2 | **R3-3.9** (verbatim labels byte-checked) | Executed as R4-7.1 — with the two-key rule: the checker is not the author. |
| 3 | **R3-3.10** (no banked count of a live sibling) | Executed via R4-8.3 (hash-pinned cross-quotes); the four named breaches (F-W8 masthead 167 · F-W10 §4b 26/22 · F-W6 D9 · F-W0 §6a) are corrected in place. |
| 4 | **R3-4** (check files never operands) | Executed as R4-3, now with the sharper canonical-only clause. |
| 5 | **R3-5.2** (G17 four spellings) | The `comm -13`-scoped fourth arm is re-cut UNSCOPED: the bare-`F.W5` spelling matches all 18 bare records; the 15 wave-level escapes book at F-W6 as gate-defect rows citing their F-W5 holder (per R4-10's roster law). |
| 6 | **R3-5.3** (g19 record-qualified axis) | The atomiser amendment is IN THE CANONICAL (§0.1: `/` protected exactly as `·`; position axis abolished by rule 4 — "no position-restricted probes"). g19 re-cuts against §0.1 VERBATIM; any private separator table in a gate is defective on sight. |
| 7 | **R3-5.4** (G-F7-8 shape-agnostic) | Re-cut to match every spelling its own axis (ii) declares — the probe is derived FROM the declared spelling list by `-E` alternation, not from any delimiter (bold, table, or otherwise). |
| 8 | **R3-8 standing clause** (canonical form everywhere) | F-W0's four sites (§6b · G-4 · G-5 · row 30) re-state the full pair `FR-NP-32 (≡ fr-PaperSidebar M1)`; lands with R4-1.1. |
| 9 | **R3-9** (every gate executable) | Executed as R4-2 — the law is now toolchain-pinned, variable-scoped, and convictable on sight. |

---

## R4-10 — PER-WAVE ESCAPE ROSTERS, FROM THE CANONICAL AND NOWHERE ELSE (cures P4-1's residual frame; closes the partition)

**The law**: a wave's record-side roster is the CANONICAL §2 roster for its home, verbatim. A wave BOOKS each canonical id, or CITES the holder by name (`held at F-W3 §…` / `held at carry/F-W4-CARRY.md §…`), or cites the canonical packet row (TERMINAL/UNROUTED/relay ids are NOT wave debts). **No wave derives, samples, or re-cuts a roster from any other source** — not a check file, not a prior pass, not its own §-arithmetic. An id absent from both booking and citation is that wave's escape; an id booked that the canonical does not home there is that wave's fabrication. The 76-row program-level zero-homed tier of P4-1 is DISSOLVED BY CONSTRUCTION: the canonical homes every one of the 4269 rows (the 30 atomiser-manufactured ids died with §0.1's `/`-protection; the 14 VV ids, the 27 fold-sources, PAW-4, FR-EQR-14, L·S-1, FV-24, GIG R-7 are all §1 rows with named homes — F-W4 additionally rules the PAW-4 twin question flagged at canonical §4.5, by amendment, not by hand-pick).

| wave | canonical roster (home → rows) | wave duty |
|---|---|---|
| F-W0 | F.W0 → **56** | book/cite all 56; §6a lock re-derived (R4-3.3) |
| F-W1 | F.W1 → **362** | book/cite all 362 incl. the R4-6 legs at their holders; §6·R1 re-based (R4-3.4) |
| F-W2 | F.W2 → **31** | book/cite all 31; `$V` declared (R4-2); C·D-16 cited at its carry holder |
| F-W3 | F.W3 → **926** | book/cite all 926 (the twin's F.W3 arm, 85 criterion + 841 dual-resolved); g19 re-cut per R4-9.6 |
| F-W4 | F.W4 → **1014** | book/cite all 1014 (14 VV ids, 27 fold-sources, FR-EQR-14 now IN the roster); rule PAW-4 per canonical §4.5; errata section created (R4-9.1) |
| F-W5 | F.W5 → **28** (+ band F.W5-W8 → **90**) | denominators per R4-5; `$R` defined; AA-23 host booked |
| F-W6 | **0** record-side (§4.1) | census re-scoped to folds/operands (R4-5); the 15 F-W5-held ids cited, never counted |
| F-W7 | **0** — ∅ posture TRUE at the corpus (§4.1) | receipts cured per R4-1.8–10; nothing routes here |
| F-W8 | **0** record-side (§4.1) | 84/124 struck (R4-3.2); masthead re-pasted under R4-8.3 |
| F-W9 | F.W9 → **17** | masthead re-based (R4-3.1); G-F9-21 fourth obligation (R4-7.2) |
| F-W10 | (close wave; canonical carries no F.W10 record-side home — §4.1) | band-correction receipt re-cut (R4-1.12); §4b counts corrected (R4-7.3) |
| (packets) | GLASS-RELAY 22 · NWO 29 · NWO→SS-3 25 · NWO→SS-13 3 · NWO→SS-5 3 · SS-\* 178 · TERMINAL 1190 · UNROUTED 307 | packet/terminal rows are census facts, not wave escapes; the UNROUTED 307 is the routing frontier, owned by the forming SS-3/SS-4 specs |

*(Roster arithmetic: 56+362+31+926+1014+28+90+17 wave-side = 2524; packets/terminal/unrouted = 1745; total 4269 — the canonical's §3 TOTAL, exactly.)*

---

**Register disposition**: P4-1 → R4-10 (+ the canonical itself); P4-2 → R4-1; P4-3 → R4-2; P4-4 → R4-3; P4-5 → R4-9.5–7; P4-6 → R4-4; P4-7 → R4-5; P4-8 → R4-6; P4-9 → R4-7; P4-10 → R4-8; P4-11 → R4-7.3; P4-12 → R4-1.1 (the §6b re-address) + the D-19 re-resolve standing law. Twelve of twelve union defects carry a ruling; ten rulings; the purge seat closes under LAW E; no certificate before the successor's re-run.

*— end of RULINGS-4. This file directs; it measures nothing it did not run. The two receipts it DOES carry (the census re-derivation of §0.3 and the G10 product-byte scope) were run by this seat this session and are re-runnable from the commands named at their sites.*
