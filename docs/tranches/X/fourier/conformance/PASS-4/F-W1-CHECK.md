# F-W1-CHECK — PASS 4 · FRESH ADVERSARIAL SPEC CHECK (L-18/L-20)

**Wave**: F.W1 (X·F) · **Spec under trial**: `docs/tranches/X/fourier/waves/F-W1.md` (post-repair-round-3 + fabrication purge) · **Date**: 2026-08-29 · **Seat**: FRESH adversarial checker, pass 4. **No roster inherited.** `PASS-3/F-W1-CHECK.md`, `PASS-3/RULINGS-3.md` and `PASS-3/CLOSE-CERT.md` were read as *inputs to the trial* — never as findings adopted. Every figure below was re-derived at the live bytes by this seat this session.

**Corpus authority**: the 66 `fr-*.md` at `docs/tranches/V/megatranche/registry/adjudicated/` (frozen `35fc8ebf`). **Carries in tree** (`ls`): `carry/F-W1-CARRY.md` + `carry/F-W4-CARRY.md` — the only two; the spec cites only F-W1-CARRY, by path (R-3 conformant).

**Verdict**: **DEFECTIVE** — **4 id-keyed escapes** (1 MAJOR · 2 MINOR · 1 INFO) + **1 MAJOR structural defect introduced BY the round-3 cure** (G10's twin ranges now span non-corresponding scopes) + 2 MINOR + 3 INFO. **The round-3 repair is otherwise the strongest of the four**: all five PASS-3 escapes are cured by bytes, G20 leg (b)'s check-file operand is genuinely re-based on the corpus and its pasted arm-(i) run reproduces to the digit, **every one of the ~55 ⟨cmd⟩ receipts this seat re-ran reproduces byte-exact**, and every born-RED witness is live and RED.

---

## §0 Method (id-keyed; DETECTOR LAW honoured; every command re-run this seat)

1. **SPELLING PROBE** — rows in ANY format, both dash spellings (U+002D · U+2013 · `·`), record-qualified ids, slash/range expansion at **every** position (`F.W0/W1` as well as `F.W1/W2`, `F.W1/W3`). **Measured, this seat, cwd `$R`:**
   - `grep -lE 'F[.·–-]W[0-9]*/?W?1([^0-9]|$)' fr-*.md | wc -l` → **66** (of 66 records)
   - `grep -hE 'F[.·–-]W[0-9]*/?W?1([^0-9]|$)' fr-*.md | wc -l` → **681** mention lines
   - per-arm: `F.W1` **845** occurrences · `F·W1` **0** · `F–W1` **0** · `F-W1` **0** (measured negatives, kept) · second-position `F.W0/W1` **2** · first-position `F.W1/W`*n* **6**
   **All eight figures reproduce G20's pasted arm (i) exactly.** Independent token census (own regex, range-expanding): `F.W4` 1128 · `F.W3/W4` 1010 · `F.W1` 839 bare + 5 `F.W1/W2` + 1 `F.W1/W3` = 845 ✓.
2. **ROW-SHAPE**: table rows ⊕ id-headed route-marked bullets ⊕ prose routings walked back to the enclosing row id (S-8). No shape restriction anywhere in this census. Numeric row ids (`fr-ContourPreview` 29/30) admitted as ids.
3. **ID-KEYING**: head-id extraction over all 681 mention lines returned **448 id-bearing lines → 435 distinct (record, id) pairs**; **5 are parse artefacts, not identities** (`Blocker-weight` ×3 — a prose lead-in at `fr-AdminFlaggedPanel:17`, `fr-CoefficientsSpectrum:17`, `fr-CollapsibleSection:17`; `NEEDS-BUILD`; `KEEP-AND-REPARAMETERISE` — a disposition verb, not a row). **ROUTED = 430.** The remaining 233 mention lines carry no row id: verdict/fold-summary paragraphs, routing-law preambles, numbered scope-notes, kill-cluster prose. Each of the **88** whose terminal segment nonetheless carries an F.W1 token was walked back to its subject ids; **83 resolve to identities the spec holds**, and the 5 residue lines are adjudicated in §2/§3 below — none is a silent drop.
4. **MEMBERSHIP by bytes**, record-qualified, against `F-W1.md`, then `F-W1-CARRY.md`, then a declared FOLD/KILL to an identity the spec carries. Every miss hand-adjudicated against all eleven wave specs (one-home law, R-2/R-5).
5. **A NOTE ON THE DENOMINATOR (G20's FAIL clause, honoured).** This seat's 430 is *above* PASS-3's 404 and PASS-2's 357 because the id-keyed pair set is built by over-collection first and adjudicated second — the leg/rider/benefit class PASS-3 admitted by hand is admitted here mechanically, at every position. A narrower terminal-position-only reading returns **341 routed pairs**; it is disclosed, not used, because it is exactly the restriction R2-9/R3-5 forbid.

## §1 Arithmetic

| quantity | value |
|---|---|
| records carrying an F.W1 routing token | **66 / 66** |
| F.W1 mention lines (both dash spellings + all slash forms, both positions) | **681** |
| id-bearing lines → distinct (record, id) pairs | 448 → **435** |
| parse artefacts struck from the denominator (not identities) | **5** |
| **ROUTED to F.W1 — id-keyed, deduped, shape-agnostic, all positions** | **430** |
| booked — id present by bytes in `F-W1.md` | **374** |
| booked — id present only in `carry/F-W1-CARRY.md`, under an identity §2 carries | **14** |
| id absent from both — hand-adjudicated (declared FOLD/KILL to a carried identity · measured-negative or self-declared non-routing · verdict-paragraph subject already carried · ruled-disagreement row whose subject is carried) | **38** of 42 |
| **BOOKED total** | **426** |
| **ESCAPED** | **4** |

**ALL FIVE PASS-3 ESCAPES ARE CURED BY BYTES, verified this seat**: `fr-PaperArticleWindow K-20` **BOOKED** (§2·R3a, seated beside G8 and re-gating §2·R2a.1's `fr-ExportModal D-M3` on G8 + ESC-1/G1) · `fr-PaperArticleWindow K-24`'s F.W1 leg **ANSWERED** (§2·R3b, citing F-W2 D2/§2b) · `fr-UserSlugBar FR-USB-10`'s leg **CITED** (§2·R3c; home F.W4) · `fr-SliderControl R-14` and `fr-ConvergencePlot SC-3` **on the BENEFIT LEDGER** (§2·R3d, SC-3 hard-gated ESC-1/8.0.0-only). The PASS-2 eleven and PASS-1 fifteen remain present.

---

## §2 ESCAPES (named by bytes) — 4

| # | record:line | id (banked) | grade | what escaped | receipt (this seat) |
|---|---|---|---|---|---|
| 1 | `fr-PathPreview.md:61` | **PP-NOSHADOW** | **MAJOR** | The row routes `→ F.W3/W4` and banks, in terminal position, *"**F.W1 rider real**: `./easing` exists at v7.0.0, not at the installed 4.0.0."* A **new producer member arriving at the F.W1 target** is WU-G's subject by definition; WU-G carries no such row. Worse, it is a **live cross-package homonym at the target**: F-W1's WU-K row (`:161`, a RULINGS-pinned coordinate) reads *"picker's five functions survive at `./easing`"* — **unqualified** — and G12 (`EASING RE-POINT VERIFIED BY EXECUTION`) is gated on that re-point. At the F.W1 target **both packages expose `./easing`**, so the spec's single unqualified spelling names two homes and record-qualifies neither. | `grep -c 'PP-NOSHADOW' F-W1.md carry/F-W1-CARRY.md` → **0 / 0**; homed `waves/F-W3.md:1` only. `grep -c 'easing' F-W1.md` → 13, **every hit value.js-side or the G12 witness**; `FadingScroll`-class producer-member rows absent. **Producer, this seat**: `git show v7.0.0:package.json` → exports include **`./easing`**, 74 keys; installed `@mkbabb/glass-ui@4.0.0` → **80 keys, no `./easing`** — the record's rider is TRUE at the bytes. And `value.js@4.0.0` exports `['./color','./value','./css','./easing','./math','./transform','./quantize']` — the homonym is live. §5's *"PathPreview.vue — certified nil on every F.W1 census row (PP-#7), the one file the wave need not open"* is the wave's ONLY mention of the record and does not reach a subpath-availability rider. |
| 2 | `fr-SpeedSelect.md:41` | **D-03** (merged `D-11/SS-L-08/SS-C-4/SS-C-11`) | MINOR | The **terminal cell** reads `**F.W4** (the CSS is re-authored against the 8.0.0 single register) **+ F.W1** (the design decision: the producer has RULED against invisible pickers)` — an explicit two-wave routing whose F.W1 half is a **decision**, the class §2 exists to hold. Structurally identical to `FR-USB-10`, which R3-2.5/R3-7.4 cured this round — and **stronger**, because this leg sits in TERMINAL position rather than in a claim cell. | `grep -c 'D-03' F-W1.md carry/F-W1-CARRY.md` → **0 / 0**; `grep -c 'D-03' waves/F-W*.md` → **`F-W4.md:4` alone**. `grep -c 'SpeedSelect' F-W1.md` → 2, and **both are homonym-disambiguation examples** (`fr-SpeedSelect D §5 U-2` at `:321`/`:493`), not this row. `grep -c 'invisible picker' F-W1.md carry/…` → 0/0. F.W4 adopts the CSS; the design decision is never taken. |
| 3 | `fr-GalleryMarquee.md:51` | **GM-6** (`= D-11`) | MINOR | Terminal disposition `→ **F.W4** with F.W1 rider`, and the rider is named in the row's own words: *"`FadingScroll` region-naming props are 7.0.0-only — an accessible name has **NO producer vehicle until F.W1**."* F.W1 is the enabling condition for the cure; the wave holds no row. | `grep -c 'GM-6' F-W1.md carry/F-W1-CARRY.md` → **0 / 0**; `grep -c 'GM-6' waves/F-W*.md` → **`F-W4.md:1` alone**. `grep -c 'FadingScroll' F-W1.md carry/…` → **0 / 0** (and `fading-scroll` 0/0). The wave DOES carry two other rows of this record (`GM-19` §2·R2b.4, `GM-F7` §6·R1) — so the record is in scope and this row alone is missing. §5's `GalleryMarquee ./scrolling-text` exclusion is a different member and does not reach it. |
| 4 | `fr-FourierShapeExtractor.md:125` | **`L·S-1 / D·S-1 / C·S-1`** (the seed-42 canon) | INFO | *"The strongest positive result in the folder; it is what demotes L-B3 and **lets F.W1 cite instead of re-baseline**."* A **scope certification routed to F.W1** — the exact class **WU-Q** ("Non-credit, negatives, scope certifications") was built to hold — and WU-Q's measured-zeros row holds `D:U-2 / PSM-36 / PP-#7 / S-3 / PAW-37` and not this. | `grep -c 'seed' F-W1.md` → **0** (carry 3, none the canon); `grep -c 'raw-contours' F-W1.md carry/…` → 0/0; `grep -c 'L·S-1' waves/F-W*.md` → **0 in all eleven**. The four `re-baseline`/`REBASELINE` sites in F-W1 (`:64` producer-window, `:118` tooltip, `:145` K-8/SR-1 slider bank, `:224` ESC-1) are all other objects — read at the bytes this seat. The one register built to hold a "F.W1 need not re-baseline" certification does not hold it: the anti-rename defect **M-25** names, recurring in the register. |

**Adjudicated NOT escapes (receipts kept, 38 of 42)** — declared FOLD/KILL to a carried identity: `fr-AppHeader FR-AH-27` (→ `fr-BasisCanvas M-α6`, spec 3/carry 1) · `fr-GallerySearchBar FR-GSB-2` (→ M-α6) · `fr-GalleryCardModal GCM-51` (→ `fr-App R-6`, 1/1) · `fr-MorphShapePreview L-05` (→ `FR-COB-13` 2/1 + `FR-MSP-5` 2/1) · `fr-MorphShapePreview FR-MSP-4` (→ `fr-InfoCard FR-IC-10`, 1/1) · `fr-SvgFilters C-M4` (→ `L-i3/C-18.1`, 4/2) · `fr-SvgFilters C-i2` (→ `fr-AnimationControls C-1` @ WU-L) · `fr-ExportModal R2-1` (→ `D-M3` 13/3 + `K-16`) · `fr-FullscreenViewer K-7` (→ `AA-2`, 8/7) · `fr-ImageUpload K-15` (→ `CP-ROW-29`, booked §2·R2a.2) · `fr-PaperSearchInput D-B2` (*"rides D-M3's F.W1 row"*) · `fr-AdminFlaggedPanel FR-AFP-55` (→ F.W0; the value.js/lucide peer fact is the same object WU-L carries via `C-1=C-8`, and WU-B `:70` carries the `^0.10.0` start-state pin) · `fr-FullscreenViewer FV-24` (→ `FB-4`, carried at G8) · `fr-GalleryView NEEDS-BUILD` (`cartoon-surface` 5/5 at G5). **Self-declared measured-negatives**: `fr-EquationModeToggle FR-EMT-4` (*"NOT cured by F.W1"*) · `FR-EMT-7` (*"no F.W1 dependency"*) · `K-7` (*"zero F.W1 dependency"*) · `fr-EquationModeToggle:25` (*"No F.W1 dependency exists for the fold"*) · `fr-GalleryFeaturedCarousel FR-GFC-10` (*"F.W1-independent"*) · `fr-AdminUserList D-32` (*"recorded as the F.W1→W4 option, **not booked as a cure**"* — an SS-3/SS-4 design ruling) · `fr-GalleryInfiniteGrid FR-GIG-3` and `FR-GIG-6` (persistence notes; both terminal `→ F.W3/W4`). **Terminal routes elsewhere**: `fr-ContourEditorCanvas B-5`, `fr-EditorControlsDock D-9`, `fr-FourierMorphDemo FMD-9`, `fr-CoefficientsPanel FR-CP-2`, `fr-BasisSelector M-12`, `fr-FourierMorphSvg FM-4..FM-16`. **Verdict/summary paragraphs whose subjects are carried**: `fr-CoefficientsPanel m-22`, `fr-CoefficientsSpectrum` B-4 (spec 8/carry 7, G11), `fr-CollapsibleSection` B-1 (G10), `fr-EqCoefficientsPanel FR-EQC-1`, `fr-FrequencyGraph FR-CP-2`, `fr-GalleryInfiniteGrid FR-GIG-1` (`FR-GIG-5` 5/2, WU-Q + G15), `fr-Tooltip` (`FR-TT-5/-6/-9` all carried, §6 E), `fr-AdminFlaggedPanel:33` pin-provenance. **Second-position slash forms homed at F.W0**: `fr-EquationResult FR-EQR-24` (`F-W0.md:2`) · `fr-UserSlugBar FR-USB-22` (`F-W0.md:2`) · `fr-PaperSidebar C-m9` (`F-W0.md:1`; the rename arm is an unnamed instance of WU-N's 35-site identity, lawful under the anti-rename law). **Record-internal mis-citation, not F.W1's**: `fr-EditorControlsDock:158`'s *"booked at MAJ-11"* — `MAJ-11` occurs nowhere in its own record; the row's F.W1 siblings (`D-12+L-7`, `MM-11`) are carried at WU-G.

---

## §3 DEFECTS BEYOND THE CENSUS

### D-1 · MAJOR — the PASS-3 D-5 range cure OVER-corrected: G10's two twins now span non-corresponding scopes, and the longer one swallows a load-bearing rule

G10 now reads: *"`CollapsibleSection.vue:60-65` AND its **`ContourSettings.vue:361-375`** twin die IN THE UPLIFT COMMIT."* The round-3 erratum derives the new range explicitly: *".advanced-content `:361-363` · `[data-state="open"]` `:364-366` · `[data-state="closed"]` `:367-369` · the `@media (prefers-reduced-motion: reduce)` arm `:370-375`."*

⟨read, this seat, `$F`, read-only⟩ `ContourSettings.vue` `:361-363` = `.advanced-content { overflow: hidden; }` · `:364-369` = the animation pair · `:370-375` = the PRM arm. `CollapsibleSection.vue` `:54-56` = `.collapsible-content { overflow: hidden; }` · `:60-65` = the animation pair · `:66-71` = the PRM arm.

Two consequences, both measurable:

1. **The ranges are no longer twins.** `:361-375` = overflow ⊕ pair ⊕ PRM. `:60-65` = pair only. The corresponding span on the CollapsibleSection surface is `:54-71`. One gate, one twin pair, **two different scopes** — the second half of exactly the defect PASS-3 D-5 convicted (*"one spec giving one range two spellings"*), displaced from within-file to across-twin rather than closed.
2. **The longer range deletes a rule that must survive.** B-1's defect is the `animation` shorthand naming keyframes deleted at ≥7. `.advanced-content { overflow: hidden; }` names no keyframe and is **load-bearing for the collapsible height animation**; the PRM arm sets `animation: none` and names no keyframe either. A seat executing G10's declared deletion literally strips `overflow: hidden` and the PRM arm from ContourSettings and leaves both standing on CollapsibleSection — an asymmetric regression **manufactured by the cure**.

**Cure**: state the two twins at corresponding scope and at the DEFECT's scope — the animation-shorthand pairs `CollapsibleSection.vue:60-65` ⊕ `ContourSettings.vue:364-369` — and, if the fork-wide span is wanted, spell BOTH fork-wide (`:54-71` ⊕ `:361-375`) with the surviving rules named. WU-F's `B-1` LOCK and §2·R1c's `m-7` cell take the same spelling.

### D-2 · MINOR — §6·R1's roster is sourced from a CHECK FILE, inside the surface G20 tests

§6·R1 closes: *"**Source of the enumeration**: `conformance/PASS-1/F-W1-CHECK.md` §2 (2026-08-28), re-checked against the records."* R3-4's standing law is unambiguous — *"check files and rulings are measurements and cure-shapes, never quotable operands … may be named as prior runs, never as the operand"* — and G20's own falsifier names *"§6 + §6·R1 + §2·R1 + §2·R2 + §2·R3"* as **the claim the run tests**. A claim surface whose 42-row roster is sourced from a superseded seat's §2 inherits that seat's coverage boundary in the one place leg (a) is supposed to become runnable. The *"re-checked against the records"* clause is the right instinct and is not a receipt: no ⟨cmd⟩ accompanies it, and the 42 coordinates are pasted, not derived. **Cure**: derive the roster from the CARRY ⟷ corpus directly and paste the derivation, naming PASS-1 §2 as a prior run only. (The `:479` round-1 erratum still names `PASS-1/F-W1-CHECK.md §3` as a G20 operand; it is a dated row and is superseded by `:511`, so it is recorded, not convicted.)

### D-3 · MINOR — a ⟨cmd⟩ marker with no executable operand banks a sweep result

§E-3·R2's F.W0 `create`-row erratum closes: *"**Sweep: ⟨cmd⟩ a per-table pipe-count pass over every table in this file returns 0 mismatched rows.**"* There is **no command**. The ⟨cmd⟩ marker is the provenance instrument this whole round exists to defend, and R3-3.9 states the rule in terms — *"a label without a ⟨cmd⟩ either gains one or loses the label"*. Here the marker is present and the operand is not, which the CLOSE-CERT's own enumeration (§1.1) would classify as one of the 177 markers *"carrying no operand to run"* — i.e. it is invisible to the purge sweep by construction.

⟨run, this seat — the sweep the row asserts, written out⟩ a per-table cell-count pass over `F-W1.md`: **37 tables, 0 mismatched rows.** **The claim is TRUE** — which is precisely the condition under which the class is invisible, and the spec's own words for it (§E-3·R2: *"every one of these five happened to be TRUE at the bytes — which is exactly the condition under which the class is invisible"*). **Cure**: paste the pass as a runnable command, or drop the ⟨cmd⟩ marker and state it as a classification.

### D-4 · INFO — three residues

(i) **§2·R2a.2's printed command still ends `| sort | uniq -c`** — a form whose output *is* a count of a live sibling. The cell handles it correctly in substance (*"the cardinality is deliberately not stated"*, the ×4→×6→×8 history minuted, the word-output set given), but R3-3.10 bans the *shape*, and the shape is what is banked; `grep -o … | sort -u` would state the same set without producing a number. (ii) **`fr-FullscreenViewer FV-24`** is unhomed program-wide (`grep -c 'FV-24' waves/F-W*.md` → 0 in all eleven) and lands here only by fold to `FB-4`; its product — `variant="glass"` ≡ `default`, byte-identical cva arms at 4.0.0 — bears directly on FR-COB-2's *"Corrected budget 9 `variant="glass"`/7 files"* and is absent from that cell. (iii) **`fr-GalleryView K11`**'s *"the F.W1 visual-diff budget is one regression, not two"* corrects a figure cross-edge 9's nine-item checkpoint set (`:302`) does not carry; recorded because the checkpoint set is the register that would hold it.

---

## §4 WHAT VERIFIED CLEAN (adversarial checks that failed to convict)

**RECEIPT REALITY — ~55 ⟨cmd⟩ receipts re-run by this seat; 55 reproduce byte-exact; ZERO failures.** (Sampled well past the ≥15 floor, weighted to the round-3 additions and to every receipt PASS-3 or the CLOSE-CERT touched.)

- **G20 arm (i), all eight figures** — `66` · `681` · `F.W1` 845 · `F·W1` 0 · `F–W1` 0 · `F-W1` 0 · `F.W0/W1` 2 · `F.W1/W`*n* 6. **Every one reproduces**, and this seat's independently-written detector returns the same 681 over the same 66 records. The re-based operand is real.
- **Corpus (frozen `35fc8ebf`), 20 receipts, all byte-exact**: MPC-31's ONE-CUT LAW (whole banked sentence) · **LAW-3** *"PAW-44 restoration lands WITH-or-AFTER PAW-1 + PAW-30, never before."* (the round-2 un-tightening holds) · `fr-PaperSidebar` M3's one-command discriminator · both spellings of 162 (`-attribute` / `-site`) · `fr-CanvasControlsDock` K-9 · `fr-SvgFilters` "disgusting metallic" · `fr-PaperSearch` D-14/D-15/D-17 · **K-20's three** (4.0.0 three-selector floor · v7 *"no floor rule exists at all"* · the `fr-GalleryCardModal F.W1 ledger` correction) · **K-24's two** (4 distinct / 6 sites · the blast-radius sentence) · **FR-USB-10's `:23` clause** · **R-14's benefit rider** · **SC-3's amendment and its shipped-at-8.0.0 pair** · `fr-BasisSelector` "variant re-selection rides F.W1" + the `outline`-no-successor sentence · `fr-ConvergenceTimeline`'s `**F.W1 rider**` sentence.
- **Live siblings, set-membership and word-output (the R3-3.10 idiom honoured), 16 receipts, all exact**: `grep -l 'K-24' waves/*.md carry/*.md` → **`waves/F-W1.md` · `waves/F-W2.md`** (as pasted) · `grep -l 'K-20' …` → **`F-W1.md` · `F-W3.md` · `carry/F-W4-CARRY.md`** (as pasted, disclosed not trimmed) · `grep -l 'D §3 U-2' …` → **`waves/F-W1.md` alone**, and its dependent classification is TRUE at the bytes (three sites: `:321` the cell, `:493` and `:512` the errata that minute it) · `grep -l 'D-corpus' …` → **`F-W1.md`** alone · F-W2's `home **F.W1** costing + **F.W4** hygiene`, its `CITES.` sentence, `D2 · K-24 (fr-PaperArticleWindow)` and the `§2b … pre-discharge` sentence · F-W3's `THE DEFINITIVE FILE-CRITERION PARTITION`, `F.W3 CITES, F.W4 BOOKS`, `` fr-PaperArticleWindow` (20) `` · F-W4's `GM-19 is never certified` and the FR-USB-10 extraction row · F-W10's bypass-half and `C-06's ComputedRef<any> rider` · F-W0's pipe-free `SUBSTRATE-LEDGER.md` **create** row.
- **Product tree (`$F`, READ-ONLY), 9 receipts**: `scripts/e2e.sh:102` pasted whole and byte-exact · `variant="default"` → **4** and the four files match one-for-one · `variant="solid"` → **0** · the emphasis/`iconOnly` probe → **0** under both readings of its printed `\|` · `variant="outline"` 30 sites / 16 files · `variant="ghost"` 49 · `btn-pill` in dist → **8** artefacts (the round-2 "seven"→"eight" correction is the right number).
- **Producer (`$P`, `git show` only), the round-3 K-20 cell's own five**: `git show v7.0.0:…/a11y-overrides.css | grep -c 'pointer: coarse'` → **2** · `sed -n '135,138p'` → `@utility touch-hit-area {` / `    position: relative;` / *(empty)* / `    @media (pointer: coarse) {` — **exactly as pasted**, and the disposition (`:132` comment prose, `:138` the sole RULE, nested inside the opt-in utility) is correct at the bytes · `v7.0.0:responsive.css:1-8` `[data-control-target]` ✓ · `v7.0.0:Button.vue:92` `:data-control-target="iconOnly ? '' : undefined"` ✓ · installed `4.0.0:a11y-overrides.css:115-118` = three selectors, none Button ✓. **§2·R3a's narrowing of PASS-3's own claim is correct and PASS-3 overshot** — recorded as a seat correction against the prior check.

**GATES BORN-RED, WITNESSES REAL** (live read-only measurement, `/Users/mkbabb/Programming/fourier-analysis` + `/Users/mkbabb/Programming/glass-ui`): `--slider-scrub` → **23 declarations / 7 files** (G9 ✓) · `text-admin-label` → **7 / 4** (G5 ✓) · `.cartoon-card` → **25/15 raw**, i.e. the 21/14 application census net of the four `style.css` shim lines (G5 ✓) · `.btn-pill` emitted from **8** producer artefacts, 0 in `web/src` as a class literal (G5's LAW ✓) · `CoefficientsSpectrum.vue:19` = `import { AnimatedDigit } from "@mkbabb/glass-ui/animated-digit";` (G11 ✓) · `easings.ts:58` `as EasingFn` (G12 ✓) · `GlassTimeline.vue:73` `@pointerdown="onPointerDown"` (G18 ✓) · `CollapsibleSection.vue:60-65` the animation pair (G10 ✓ **as a pair**; see D-1 for the range) · `a11y-overrides.css:115-118` three selectors none Button (G8 ✓) · `v7.0.0:responsive.css` + `Button.vue:92` (G8's ≥7 half ✓) · installed producer **4.0.0** with **80 export keys**, `v7.0.0` **74** (the PP-NOSHADOW rider's own operands ✓).

**M-25 DEPTH — locks, riders, dissents, anti-cures**: `FR-NP-32 (≡ fr-PaperSidebar M1)` in canonical R-8 form ✓ · **PAW-44/LAW-3** carried as its bytes, `WITH-or-AFTER` restored, PAW-30 restored as second named predecessor ✓ · **MPC-31** ONE-CUT LAW un-compressed, whole sentence by command ✓ · **FR-MSP-6**'s two-channel lock and the PaperSearchModal same-commit triple excluded at §5 with the correct reason (rows land F.W3/W4; PSM-36 certifies the sole F.W1 exposure) ✓ · same-commit riders (MPC-13 · MPC-22 · D-14 · G10's HOST-imports-first ordering · MPC-10's contrast leg) carried ✓ · **anti-cures carried as kills with receipts**: KILL-6 (`<Card tier surface="cartoon">` unspellable, 21/14) · K-5 · MG-ι · C-M7 · GAB-29 (MetricPill barrel-only, no source at v8) · K-12 · K-9 · FMD-21 ✓ · **dissents preserved without conversion to budget** (reader-δ's MAJOR revives *iff* the variant row is priced as a rename; FR-GIG-5's three BLOCKER dissents ride NWO-1; LC's FR-CP-33 MAJOR carried as dissent) ✓ · **the twin law** (R2-1-LAW quote-by-command ⊕ R2-2 stable anchors ⊕ **R3-3.10 no banked counts of moving files**) declared in the masthead, applied from §2·R3's first byte, and self-audited at §E-3·R3 ✓.

**GATES / OPERANDS**: **G20 leg (b) is genuinely cured** — LHS is the 66 records, detector stated INLINE in four axes (spellings both dashes + both slash positions · shapes · positions · record-qualification), PASS-2's 357 and PASS-3's 404 demoted to *named prior runs*, and the leg's own FAIL condition (*"a run reporting materially below the highest honest prior run has mis-stated its detector"*) is satisfied by this pass's **430** ✓ · G5's operand is the WHOLE emitted roster at the adopted pin ✓ · G7 states six figures with the counting unit beside each and refuses averaging ✓ · G20 leg (a)'s LHS is the CARRY's 105 row ids, never the unresolved `fr-NAME:LINE` anchor family ✓.

**POSTURE**: `Status: planned` ✓ · four-verb table `VERIFIED = NO`, stamped only at sub-tranche close ✓ · **four** uppercase `VERIFIED` tokens, all lawful (`:21` the table row · `:258` G12's gate name · `:481`/`:501` errata) — **no verification verb stamped anywhere** ✓ · the transaction cited whole at **`:276`** with the correct **TWELVE**-limb count — this seat counted the top-level `+` conjuncts and got twelve — and the `:294` warrant named ✓ · all four RULINGS-pinned coordinates intact and correct at the bytes (`:161` WU-K `L/B-1 + C/B-1` · `:227` ESC-4 · `:276` the roster · `:294` the vaul-vue-INSIDE pin); the round-3 within-line discipline held ✓ · the four surviving `361-374` tokens are all inside self-disclosing correction sentences, and the three `ELEVEN-limb` tokens all sit inside the strike-note at `:309` and the erratum at `:516` beside a live TWELVE ✓ · **W7 ZERO-ROW POSTURE**: `grep -o 'F\.W7' F-W1.md` → **0 occurrences in the whole file** — F.W1 declares no F.W7 edge and does not re-litigate R2-8.1 ✓ · **SS-4 flags INLINE** at ten sites, incl. cross-edge 10's declared NON-edge (TA-4 atomdiff, §5) and the SS-3/SS-4 design rulings left at their own specs ✓ · **fourier + glass-ui + value.js trees READ-ONLY throughout** (`grep`/`sed`/`git show` only; zero files opened for write; this register is this seat's sole write) ✓.

**RULINGS-3 APPLIED — the per-wave directive index, checked line by line**: R3-2.2 (K-20 booked, gated on G8 + ESC-1/G1 by name; K-24's leg answered citing F-W2 D2/§2b) ✓ · R3-2.5 + R3-7.4 (FR-USB-10 citation row; R-14 and SC-3 benefit-ledger rows, SC-3 hard-gated 8.0.0-only with both producer probes pasted) ✓ · R3-3.2 (§2·R1a receipt → classification + `grep -l` set) ✓ · R3-4.2 (G20 leg (b) → corpus operand, detector inline) ✓ · R3-10.5 residue (§2·R1's sibling claim restated as the DIRECTIVE, certification handed to the PURGE SEAT) ✓ · the D-3 enumeration rider ✓ · **the D-5 range rider is applied and OVER-applied — D-1**. The CLOSE-CERT's F-W1 line (100 markers / 75 run / 72 repro / 3 fixed / 1 struck) is consistent with this seat's own extraction of **73 fenced ⟨cmd⟩ spans**, and all three "fixed" sites (`CP-ROW-`, `D-corpus`, the `×6` strike) verify at the settled bytes.

---

**Seat verdict: DEFECTIVE** — routed **430** · booked **426** · **escaped 4** (1 MAJOR · 2 MINOR · 1 INFO) · plus **1 MAJOR** structural defect **introduced by the round-3 cure** (G10's non-corresponding twin ranges, the longer of which deletes a load-bearing rule), **2 MINOR** (§6·R1's check-file roster source; a ⟨cmd⟩ marker with no operand) and **3 INFO**. **The receipt surface is the cleanest of the four passes: 55 of 55 re-run receipts reproduce byte-exact, including all five of the round's new producer probes and all eight of G20's arm-(i) figures — and the round's own narrowing of PASS-3's overshoot at v7's `a11y-overrides.css` is correct.** The escape class has narrowed again (5 → 4) and has moved: **three of this pass's four escapes are named F.W1 legs/riders on rows terminally routed to another wave, and the fourth is a scope certification missing from the register built to hold it** — the class RULINGS-3 cured by name for `FR-USB-10` and did not sweep. The one MAJOR structural defect is the first in four passes to be *manufactured by a cure* rather than survive one. Read-only everywhere except this file.
