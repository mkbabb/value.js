# F-W4 — FRESH ADVERSARIAL SPEC CHECK, PASS 4 (L-18/L-20)

**Seat**: fresh, 2026-08-29. **Roster inherited: NONE.** Every figure below was re-derived this session from the frozen corpus (66 `fr-*.md` at `docs/tranches/V/megatranche/registry/adjudicated/`), the live `waves/F-W3.md` §X.1-v4, the two real carries (`carry/F-W1-CARRY.md`, `carry/F-W4-CARRY.md`), and the fourier tree at HEAD `cd26c65` (read-only). `PASS-3/F-W4-CHECK.md`, `PASS-3/RULINGS-3.md` and `PASS-3/CLOSE-CERT.md` were read for the cure-shapes they prescribe; **no number in this file is copied from any of them**, and no check file is used as an operand (R3-4). This file is the seat's only write.

**Verdict: DEFECTIVE.**

---

## §A — DETECTOR AND METHOD (stated inline so the run is reproducible)

**Shapes** — a routed row is a table row whose terminal cell routes, **or** an id-headed bullet routed in context (`→` · `⇒` · `**ADJUDICATED →**` · bold-at-disposition), **or** a prose routing walked to its enclosing row id.
**Spellings** — token set `{F.W4, F.W3/W4}`, both dash spellings, every slash form expanded **at every position** (a member matches whether written first or second), every span expanded to its member set.
**Atomisation** — left-token rule on `=`/`/`-joined heads; `·` preserved when unspaced (`L·D-1` ≠ `L-D-1`); hyphen-runs preserved; grade tails and parentheticals stripped; `fr-ContourPreview`'s `| N |` scheme → `CP-N`.
**Record-qualification (axis iv)** — the booking test is the **(record, id) PAIR**. A record-unique id books on boundary-exact bytes; a homonym books only where the holding line carries the record marker (component name, `fr-` spelling, **or the record's own spec prefix**, e.g. `AC-`, `FR-CL-`, `MSP `, `VV `).
**Booking test, alias-aware** — a booking counts if the spec holds the id bare, or under its record prefix, or inside one of the spec's own declared elision idioms, all of which this seat implemented and ran: dotted chains (`FR-AH-19 · -23 · -25 …`), ranges (`-39..-43`), slash-elisions (`FR-EQR-9/-11`), `⊕`-chains (`FSE-D-3 ⊕ D-4 ⊕ D-5 …`, `FR-GFC-12 ⊕ -13 ⊕ …`), prefixed continuation chains (`FR-CL-D3 · D-4 · D-11 …`), and the local aliases the spec declares (`CP-33 → CP-ROW-40`, `FR-CT-D14/D-19/C-15` for banked `L·D-14 + D·D-19 + C·C-15`). **Every survivor was hand-adjudicated at the bytes.**

**Detector exhaustiveness, tested rather than asserted** — ⟨cmd⟩ this seat over the 66:
`grep -oh 'F·W[0-9/–-]*' fr-*.md` → **∅** (no middot spelling) · `grep -ohE 'F[–-]W4' fr-*.md` → **∅** · `grep -ohE 'F\.W[0-9]+(/W[0-9]+)+' fr-*.md | sort | uniq -c` → `2 F.W0/W1 · 5 F.W1/W2 · 1 F.W1/W3 · 1010 F.W3/W4 · 54 F.W9/W10` · `grep -ohE 'F\.W[0-9]+[–-]W[0-9]+' fr-*.md | sort | uniq -c` → `120 F.W5-W8 · 72 F.W5–W8` (**no span contains W4**) · `grep -oh 'F\.W4' fr-*.md | wc -l` → **1128** / `grep -l` → **56 records** · `grep -oh 'F\.W3/W4' fr-*.md | wc -l` → **1010** / `grep -l` → **55**. The gate's stated token set is **exhaustive for this corpus** — recorded because that is the one axis of `G-F4-CARRY-CLOSURE` leg (b) that verifies clean.

**Home assignment (§X.1-v4's criterion, run mechanically against F-W3 §5's own `Files` lines):** sole `F.W4` → F.W4 · dual on a file in an `.a`–`.e` `Files` line → F.W3 · all other duals → F.W4 · **plus** the override the gate's own step (iii) mandates: any id F-W3 §X.1 / §X.1-v4 names with `F.W4` as holder is F.W4's regardless of file.

### Census result

| figure | value |
|---|---|
| routed `(record, id)` pairs, F.W4-token-bearing, **65 records** | **1694** (row instances before pair-dedup: 937 bullet · 766 table; spelling: 890 sole-`F.W4` · 804 dual) |
| — LAW-assigned **F.W3** (correctly cited, not booked here) | 683 |
| — LAW-assigned **F.W4** | **1011** |
| booked in `F-W4.md` (record-qualified, after the full alias/elision expansion, hand-verified) | **968** |
| **ESCAPED** — LAW-assigned to F.W4, zero record-qualified bytes | **43** |

**Leg (a) — CARRY→spec — is GENUINELY CLEAN and is confirmed independently.** ⟨cmd⟩ this seat: `sed -n '52,512p' carry/F-W4-CARRY.md | grep -c '^- \*\*'` → **421**, matching the masthead's `421 rows` at the bytes; every head resolves in `F-W4.md`. The spec's own diagnosis — that leg (a) *"is incapable of seeing the wave's actual defect"* — remains correct: **all 43 escapes below are registry→spec, and 41 of them are invisible to the carry.**

---

## §B — THE ESCAPES (43 · LAW-assigned to F.W4 · zero record-qualified bytes)

### §B.1 — `fr-VisualizationView` — the sole-`F.W4` MINOR sweep (14)

`D-11` · `D-14` · `D-17` · `D-20` · `D-21` · `D-24` · `D-25` · `L-11` · `L-12` · `L-16` · `L-17` · `L-21` · `L-22` · `L-25` — all r1-carried rows routing **sole `F.W4`** (`fr-VisualizationView.md:79-101`), so they home here by the criterion's *first* arm, before any twin question arises. Record-qualified probes, ⟨cmd⟩ this seat, **all 0**: `VV D-11` · `VV D-14` · `VV D-17` · `VV D-20` · `VV D-21` · `VV D-24` · `VV D-25` · `VV L-11` · `VV L-12` · `VV L-16` · `VV L-17` · `VV L-21` · `VV L-22` · `VV L-25` · `VV-L-12` · `VV-D-20` · `VisualizationView D-20`. The file's only `L-21` bytes are `CCD-D-20/L-21-addendum` — **fr-CanvasControlsDock's** id, which is exactly the cross-record homonym axis (iv) exists to refuse. The record IS in bounds (`views/VisualizationView.vue`, §1 *Visualize* row) and the spec holds `VV L-4`, `VV-L-15`, `VV-BLK-1`, `VV-R2-A/B/D/E`, `VV §4`, `VV dead-CSS census` — so the record is not missing, only fourteen of its rows are.

### §B.2 — `fr-MorphShapePreview` — twenty fold-source identities (20)

`C-1` · `C-11` · `D-1` · `D-2` · `D-4` · `D-5` · `D-7` · `D-9` · `D-12` · `D-14` · `D-16` · `D-17` · `D-19` · `D-21` · `D-22` · `D-24` · `L-10` · `L-11` · `L-16` · `L-17` — every one a `**FOLD → …** · **F.W4**` row (`fr-MorphShapePreview.md:39-76`). **Every target is booked** (`FM-1`, `FMD-2`, `FMD-1`, `FM-20`, the `FM-4..FM-16` batch, `FMD-9`, `FMD-N3`, `FMD-31`, `FM-10`, `FMD-19`, `FMD-6`, `FMD-27`, `FMD-30`, `FMD-34`, `FM-21`, `FMD-29`, `FM-6`, `FMD-33`, `FMD-N4`) — **not one source id is.** ⟨cmd⟩ this seat: `MSP D-1` · `MSP D-4` · `MSP D-5` · `MSP D-7` · `MSP D-9` · `MSP D-12` · `MSP D-17` · `MSP L-10` · `MSP L-11` · `MSP L-17` · `MorphShapePreview D-` → **0 each**; the file's only MSP spellings are `MSP D-20`, `MSP D-6/L-12`, `MSP-D-15`, `MSP PRM-residue` and the `FR-MSP-*` series.

### §B.3 — `fr-DarkModeToggle` — seven routing rows (7)

`C-15` · `D-m13` · `L-1` · `L-2` · `L-5` · `L-15` · `L-16` — the record's `→ fr-AppHeader FR-AH-*` table (`:54-68`), each routed `(F.W4)`. Targets booked (`FR-AH-1`, `FR-AH-7`, `FR-AH-8`, `FR-AH-9`, `FR-AH-23`, `FR-AH-53`, intake `R5-7`); sources absent. `D-m13` is absent **program-wide** — ⟨cmd⟩ `grep -rl 'D-m13' waves/ carry/` → **∅**.

### §B.4 — the two singles the twin seam itself owes (2)

| banked id | record / routing | status in `F-W4.md` |
|---|---|---|
| `PAW-4` | `fr-PaperArticleWindow.md:50`, `**→ F.W3/W4**, sequenced strictly AFTER PAW-2` — and named in F-W3 **§X.1's column-3 table** with holder **F.W4** | **0 bytes.** ⟨cmd⟩ `grep -cE '(^\|[^A-Za-z0-9-])PAW-4([^A-Za-z0-9-]\|$)' F-W4.md` → 0, against `PAW-6=4 · PAW-18=3 · PAW-26=3 · PAW-29=7` from the same table |
| `FR-EQR-14` | `fr-EquationResult.md:52`, `**→ F.W3/W4**` (rides FR-COB-18) — on a record §X.1-v4 assigns **wholly** to F.W4 | **0 bytes.** Program-wide only `waves/F-W1.md` + `carry/F-W1-CARRY.md` (the F.W1-leg citation, not a booking of the dual row) |

---

## §C — DEFECTS

**D-1 · BLOCKER · the flagship born-RED witness is written in a flag this same file strikes as unrunnable, and it dies on the binary the program pins.** §2.X.1's hash-pinned 32-id zero-run — the measurement R3-4.4 made `G-F4-CARRY-CLOSURE` leg (b)'s operand, and the only proof that the receive happened — is a literal `sh` block built on **`grep -coP`**. §0's own PURGE-SEAT FIX strikes a receipt for exactly that flag: *"The flag was `-ohP`; BSD `grep` has no `-P` (**invalid option -- P**), so the receipt was unrunnable as written."* ⟨cmd⟩ this seat: `/usr/bin/grep --version` → `grep (BSD grep, GNU compatible) 2.6.0-FreeBSD`; `/usr/bin/grep -coP '(?<![A-Za-z0-9-])BC-3(?![A-Za-z0-9-])' /tmp/fw4-r2.md` → `grep: invalid option -- P` (exit 2). F-W3 §X.1-v4 item 3 **pins the idiom to `/usr/bin/grep`** *"so the answer cannot turn on whose shell ran it."* ⟨cmd⟩ `grep -o 'grep -c\?o\?h\?P' F-W4.md | sort | uniq -c` → `2 grep -P · 5 grep -coP` — **seven receipts, all dead on the pinned binary**, including the §2.X born-RED, the §2.X.1 receive-witness and the §4 gate cell that quotes it. Under a shell routing `grep` to ugrep the block does return the thirty-two zeros (this seat reproduced them there) — which is the two-answers-for-one-command hazard F-W3 minuted in terms and this file shipped anyway. §2.X.1 asserts of this very block: *"The command is written LITERALLY … R3-9's runnability law — an elided command is a receipt no seat can re-run, and the purge sweep re-runs every one."* §0 and §2.X.1 cannot both be right, and the wave's whole leg-(b) claim rests on the one that is not.

**D-2 · BLOCKER · the receive drops an id the twin's own per-id hand-off table assigns to F.W4 — and the gate's own step (iii) mandates.** §B.4. `PAW-4` is in F-W3 §X.1's column-3 table with holder **F.W4**; `G-F4-CARRY-CLOSURE`'s runnability clause says in terms *"(iii) **add back** every id F-W3 §X.1 / §X.1-v4 names with `F.W4` as holder … (iii) is where the fourteen live, and a gate that runs (ii) alone is the gate that reported them out of scope."* The gate was repaired to see exactly this class and the spec still does not hold the row. Four of its table-siblings (`PAW-6/-18/-26/-29`) were booked in round 2 with a pasted before/after pairing; `PAW-4` was not, and nothing discloses the omission.

**D-3 · BLOCKER · §2.X.1(C)'s "1 id" is falsified by the corpus, in the section written to end count-word hand-offs.** §B.4. `FR-EQR-14` routes `**→ F.W3/W4**` on `equation/EquationResult.vue`, a file the section's own receipt proves is in **no** `.a`–`.e` `Files` line (⟨cmd⟩ `grep -h '^- \*\*Files\*\*' F-W3.md | grep -oE 'BasisCanvas|ContourEditorCanvas|EquationResult|GalleryAdminBanner'` → no output, re-run and reproduced this seat), so the default arm sends it here. The record therefore routes **at least three** ids to F.W4 — `FR-EQR-11` (booked at §2.H via `EV-D·D-B1 (=FR-EQR-9/-11)`), `FR-EQR-21` (booked at §2.X.1(C)), `FR-EQR-14` (nowhere). §2.X.1(C) spends a paragraph adjudicating *"`PASS-3/F-W4-CHECK.md` §A reports … 24 routed / 24 booked / 0 escaped, while §X.1-v4's ruled detector returns this id zero-homed — and the bytes agree with v4"*. **The bytes agree with neither**: v4's residue derivation counted `FR-EQR-14` "homed" because it greps in a sibling, and the sibling hit is an F.W1-leg citation. A record-blind grep is what the fourth axis was added to abolish; it decided this record's count anyway.

**D-4 · MAJOR · a whole record's sole-`F.W4` sweep is unbooked at the identity level — and it is the record the spec spends five paragraphs on.** §B.1. Fourteen `fr-VisualizationView` ids home here by the criterion's *first* arm (sole spelling — no twin question arises), and none has a record-qualified byte. Meanwhile §2.X(2)/§2.X.1 devote five dense paragraphs, four ⟨cmd⟩ receipts and a toolchain minute to `L-26`, a VV row the wave **converts to a citation** and does not hold. A partition that can adjudicate one row it does not own to three decimal places, while fourteen rows it does own have no id anywhere, is measuring the seam and not the corpus.

**D-5 · MAJOR · §6.4's fold register is short by a record — the exact class it was re-denominated twice to close.** §B.2. Twenty `fr-MorphShapePreview` FOLD rows route `F.W4`; every target is booked and no source id is. §6.4 states the law that convicts this — *"a fold whose source id is unwritten is a silent drop wearing a cure's clothes"* — and registers 12 `HLG-*` and 7 `MPC-*` folds **to the same FMD spine**; there is no MSP sub-register. Add §B.3's seven `fr-DarkModeToggle` routing rows in the same state and the unregistered fold-source class is **27**, against a register that re-denominated itself for `4` (round 2, sub-register (f)) and then for `1` (round 3, sub-register (g)).

**D-6 · MAJOR · the receive section reintroduces the count word §2.X(2) retired, and its own enumeration falsifies it.** §2.X.1(A) heads its list *"— **23 ids**, routed to the spine by mechanism"*, then enumerates **24** bolded banked ids. The 24th is `BC-10 / C-9`, which §X.1-v4 expressly places outside the residue and which the section itself books as an *extra* (*"the fold target, booked so §2.E's fold has a home"*). §2.X(2) retired *"booked (2)"* on the ground that *"the default arm's output is a SET decided by a question about a path … never a numeral this section chooses"*; §6.4 states *"the count follows the operand, never the reverse."* The section written to enforce both breaks both in its first sentence.

**D-7 · MAJOR · the purge sweep left no transcript, though R3-3.8 makes one mandatory and three inline notes claim it ran.** R3-3.8: *"The sweep's transcript (per-spec: receipts run / reproduced / re-pasted / struck) is appended to each spec's errata section, one row per spec."* ⟨cmd⟩ this seat: `grep -ci 'errata' F-W4.md` → **1** (the §0 phrase *"struck-address erratum"* — there is no errata section); `grep -c 'receipts run' F-W4.md` → **0**; `grep -c 'PURGE-SEAT' F-W4.md` → **3**. The twin carries `## §Y Errata` with its ledger. A sweep asserted three times with no transcript is a sweep no reader can audit — and D-1, D-8 and D-9 are what it missed.

**D-8 · MINOR · two `grep -n` receipts print a strict subset of their command's output, unmarked.** (i) §2.E: ⟨cmd⟩ `grep -n 'C-28' fr-CanvasControlsDock.md` is published as `:102` · `:150`; live it returns **three** lines — `:45` · `:102` · `:150`. (ii) §2.0 SP-2: ⟨cmd⟩ `grep -n 'MG-λ' fr-App.md` is published as `:95` alone; live it returns **two** — `:95` and `:140` (`M-4 (+ MG-λ)`), the second being the line F-W3's own §X.1-v4 discloses as the row's other reading. R2-1-LAW 1 requires the pasted output to be the command's output; this file marks elisions elsewhere (§3 D4 does), so the omission reads as fact, not convention.

**D-9 · MINOR · a receipt whose printed output the command cannot produce.** §2.X.1(E): ⟨cmd⟩ `grep -h '^- \*\*Files\*\*' F-W3.md | grep -oE 'EquationModeToggle|EqCoefficientsPanel|EquationView|CollapsibleSection'` is published as four values. Live, this seat: **five** — `EqCoefficientsPanel` · `EqCoefficientsPanel` · `EquationModeToggle` · `EquationView` · `CollapsibleSection`; `.d`'s `Files` line names the file twice. The published form is a `sort -u` the pasted command does not perform — the same shape as §2.X's *"a `-rln` receipt reports two filenames"* conviction, one level smaller.

**D-10 · MINOR · a booking that mints a token the bank never minted.** `fr-GalleryAdminBanner GAB-11` (*"member site F.W1; register note F.W4"*) is discharged in §2.J as **`GAB-11-note`** inside a `⊕`-chain; boundary-exact `GAB-11` → **0**. R-8/R-5 make the banked id original for life and forbid a spelling that is not the id; a `-note` suffix is a token no reader greps for. The disposition is right and the identity is not citable — the inverse of the D-5 class.

**D-11 · MINOR · §2.X.1(E) attributes to the twin a ground the twin does not take for four of six ids.** F-W4: *"§X.1-v4 item 6 re-reads six of them against the criterion and homes them at F.W3 `.d`."* v4 item 6 homes only `FR-EMT-19` and `FR-EQC-14` on the **file criterion**; `FR-EMT-23`, `D·D-M12`, `m-15`, `i-6` it homes because they *"already grep in this file"* — a bare-token ground the very next F-W4 bullet then corrects for `i-6`. The paths do decide all six the same way, so the outcome survives; the attribution does not, and it is a quotation of reasoning under R2-1-LAW.

---

## §D — WHAT HOLDS (verified at the bytes, so the verdict is not read as blanket)

- **Leg (a) 421/421** — independently reproduced (§A). The carry direction is closed; 41 of the 43 escapes are invisible to it.
- **The detector's token set is exhaustive for this corpus** — the five spelling probes in §A all reproduce, including the en-dash ∅ the gate keeps in the probe on purpose.
- **The twin partition is applied faithfully where it was published.** The 28-id hand-off is received per id at §2.X.1(A)/(B) (BC-*, M-α*, M-β*, MM-* all present and non-zero today); `GAB-25` and `FR-EQR-21` land; `L-26` converts to a citation exactly as R3-1a.3 rules; `fr-Tooltip`'s residue is correctly left to `.e` (`ui/tooltip/**`); the six `.d` residue ids are cited, not booked. **Round 3's central defect — the unreceived hand-off — is genuinely cured.**
- **§2.X.1(B)'s arithmetic is sound**: nine `MM-*` rows ⊕ thirteen heads carrying the seventeen homonym-only tokens = 22 rows / 26 id-instances, and all seventeen (`C-8 · C-9 · C-12 · C-13 · C-16 · C-20 · C-22 · C-23 · L-4 · L-6 · L-11 · L-12 · L-13 · L-19 · L-20 · i-2 · m-12`) are carried beside their banked heads rather than minted as rows. ⟨cmd⟩ `grep -cF '**L-4 · C-10' fr-ContourEditorCanvas.md` → **1**, as published.
- **Thirty-one ⟨cmd⟩ receipts re-run by this seat reproduce exactly at their stated coordinates** — §0's three-dead-address set (under the repaired `-ohE`) · `### G-11` / `### G-12` · F-W1's `**Name**` pin string and `ESC-1 ruled (G1)` · the §1 evaluate-scope structural probe · `fr-App:95` MG-λ · `fr-ConvergenceTimeline:56` `D·D-5 + C·C-5` · `fr-BasisCanvas:56` `BC-10 / C-9` (1 hit) and its `export-truth work item, with BC-3/D-11/D-22/D-24/D-25/BC-12/BC-22` · `fr-CanvasControlsDock:98`/`:116` · `fr-PaperSidebar:51` `D-M4 extended` · `fr-FourierMorphSvg:39` `FM-4..FM-16` · `fr-ContourPreview:76` `D:m-6` · `fr-GalleryAdminBanner:67` `GAB-25 · missed` · `fr-SpeedSelect:72` `SS-C-1 (read leg` · `fr-PaperArticleWindow:220` `LAW-3` and **both** split `overflow-x: clip.\{0,250\}` / `The two-TIER stack additionally needs.\{0,90\}` spans (the purge seat's `{0,300}` fix is correct and runnable) · the `BC-10` self-count (`grep -oE "\bBC-10\b" F-W4.md | wc -l` → **11**, exactly as published, the `\b` correctly excluding the printed command itself) · the `PAW-6/-18/-26/-29` → `4 · 3 · 3 · 7` and the ten-`GCM` re-run pairings → `5 · 3 · 5 · 12 · 3 · 3 · 3 · 3 · 4 · 5` · `showImageOverlay`/`shape only` → `1` and `1` · the four F-W3 quotations at §2.X · the `WAS THE CONTROL` supersession quote · both hash-pinned `git show` receipts (`ce2622d1`, `281a420a` both resolve; the 32-id run returns thirty-two zeros and the `L-26` run returns `0` against `3`/`3` substring hits) · `F-W1`'s `TWELVE limbs` and `4. **The atomic transaction (ONE change, G6):**`. **Only the four in D-8/D-9 and the seven in D-1 fail.**
- **Gate witnesses re-measured live at fourier HEAD `cd26c65`**: `G-F4-ADMIN-AXE` (`grep -rln "admin" web/e2e/` → ∅) ✔ · `G-F4-VITEST` (scripts **exactly** `build/dev/preview/test:e2e/test:e2e:ui`; 0 `*.test.ts|*.spec.ts` under `web/src`) ✔ · `G-F4-NO-UNUSED` (**13** `compilerOptions` keys, no `noUnusedLocals`; no ESLint config anywhere in `web/`) ✔ · `G-F4-A11Y-ROUTE` (**7** `checkA11y` **call** sites — `:114/:146/:163/:201` and `:529/:636/:659`; `AxeBuilder` ×2) ✔ · `G-F4-OCCLUSION` (`App.vue:24` the `overflow-hidden` shell, `:26` the sole `<main>` scroller) ✔ · `G-F4-VUE-TSC-CLEAN` (`glass-scrubber` live at `BasisSelector.vue:315`, `EditorControlsDock.vue:48/:222`) ✔ · `G-F4-DEAD-DEP` (`components/paper/search/index.ts` present, zero external consumers) ✔ · `G-F4-CENSUS-CELLS` path-qualification (two `raw-findings.json`, one `DELTA.md`) ✔.
- **M-25 depth** — `FR-NP-32 (≡ fr-PaperSidebar M1)` canonical at §5.1(1) with the R-8 note, and **no bare `(M1)` survives anywhere** (⟨cmd⟩ `grep -c '(M1)'` → 0) ✔ · PAW-44/LAW-3 quoted as two sentences from two homes, each with a runnable ⟨cmd⟩, trap carried ✔ · MPC-31 ONE-CUT with the witnessability precondition and the MPC-13 ordering ✔ · FR-MSP-6 TWO-CHANNEL, FR-MSP-10 NAME-CURE, EQP-M-N11, the ECD-B-2 ⊕ CCD-M-2 inversion lock, PSM-1's three same-commit riders, HLG-41's ordering edict, the M-10 RNG rider, DO-NOT-REGENERATE — all present and sequenced at §5.1(3) ✔ · anti-cures at §6.1 = **19**, counted ✔ · §6.2 **7** ✔ · §6.3 **7** ✔ · §6.4 sub-registers **12+12+7+4+1+4+1 = 41**, each counted at its own bullet ✔ · dissents preserved (FR-AFP-5 α, GV-L-5, FR-AUL-33/-35, ECD-D-3 DU, CCD-D-17 LC, MPC-12 reader-2, HLG `:disabled`) ✔.
- **POSTURE** — F.W1 cited **whole as TWELVE limbs** at both sites, by stable anchor, with two reproducing receipts, and never re-derived ✔ · **zero** `F.W7` tokens (the ruled ∅ posture; no edge asserted, none owed from this end) ✔ · SS-4 owner rulings flagged **INLINE** at §5.2 ✔ · tree declared READ-ONLY ×3 incl. the closing execution gate ✔ · `planned` on the masthead, on §2's heading, on §3 and at the close ✔ · **zero** occurrences of `VERIFIED` ✔ · RULINGS-3's F-W4 directives applied in substance: R3-1b.1 (28 received) ✔ · R3-1b.2 (§1 evaluate-scope) ✔ · R3-1b.3 (count word retired at §2.X) ✔ · R3-1b.4 (BC-10/C-9 booked, fold lands, `L-26` cited) ✔ · R3-3.4 (§2.E receipt struck, not re-pointed) ✔ · R3-4.4 (gate witness is the wave's own run; column-3 override disclosed in the operand sentence) ✔ · R3-10.5 (stale ELEVEN disclosure retired; §0 PIN LAW re-denominated to a roster with no count word) ✔.

---

## §E — DISPOSITION

`routedTotal = 1694` · `LAW-assigned F.W4 = 1011` · `bookedCount = 968` · `escapedCount = 43` · **verdictLocal = DEFECTIVE**.

The wave is materially better than it was: the hand-off that failed twice is received per id, the count word at §2.X is properly retired, the twin's ruled conversion of `L-26` is honoured at both ends, and every gate witness re-measures true at the live tree. It fails pass 4 on three things.

**First, the receipt instrument itself.** The one measurement the whole leg-(b) claim rests on is written in a flag the same file, three sections earlier, strikes as unrunnable on the program's own binary — and six more receipts share the defect. A wave that convicted two prior rounds for banking receipts nobody re-ran has banked its flagship witness in a dialect the pinned `grep` rejects.

**Second, the census still leaks — but no longer at the seam.** Round 3 fixed the twin surface and only the twin surface. The forty-three rows that escape now are ordinary registry→spec drops on records nobody was arguing about: fourteen sole-`F.W4` rows on a record the spec discusses at length, twenty fold-source identities on a record §6.4's own law names as the class it exists to catch, seven more of the same on a third, and two singles the twin's own per-id table and the gate's own step (iii) both hand over by name.

**Third, the cured sections re-commit the cured defects at smaller scale** — a numeral header its own list falsifies, a "1 id" the corpus reads as three, a purge sweep with no transcript. Each is small; together they are the same failure the last three rounds diagnosed, which is that **the wave keeps measuring the previous measurement.** This seat measured the corpus.
