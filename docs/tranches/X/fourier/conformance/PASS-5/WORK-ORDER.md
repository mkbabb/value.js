# X·F CONFORMANCE PASS 5 — SURGICAL WORK ORDER (round 5 repair, post-errata)

**Seat**: surgical work-order seat, 2026-08-29. **This file is the seat's only write.** It directs; it repairs nothing.
**Inputs, read whole this seat**: `conformance/CENSUS-CANONICAL.md` **re-read FRESH after the §0.4 errata cure** · `PASS-5/UNION.md` (P5-1…P5-18) · all eleven `PASS-5/F-W*-CHECK.md` · `waves/F-W0..F-W10.md` at their settled bytes · `PASS-4/RULINGS-4.md` · the 66 frozen `fr-*.md`.
**Toolchain**: `PATH=/usr/bin:/bin:/usr/sbin:/sbin`, BSD `/usr/bin/grep` · `sed` · `awk` · `shasum`. Never the interactive `grep` (a shell function wrapping `ugrep 7.8.4`). Every "current bytes" span below was **grepped by this seat this session** from the live file named beside it.

## §0 THE TWO HASH FACTS THIS ORDER RESTS ON

⟨cmd⟩ (cwd `docs/tranches/X/fourier`) `shasum -a 256 waves/F-W*.md carry/*.md conformance/CENSUS-CANONICAL.md | awk '{print substr($1,1,12), $2}'`:

| file | digest[0:12] | state |
|---|---|---|
| `waves/F-W0.md` | `282f0c120cd4` | **unmoved** since CLOSE-CERT-2 §8 |
| `waves/F-W1.md` | `a1302689aaa3` | unmoved |
| `waves/F-W2.md` | `578dba1fb42e` | unmoved |
| `waves/F-W3.md` | `a89c3386f3f8` | unmoved |
| `waves/F-W4.md` | `39e1a60b3fc9` | unmoved |
| `waves/F-W5.md` | `132c03192176` | unmoved |
| `waves/F-W6.md` | `c8c6d1d7f136` | unmoved |
| `waves/F-W7.md` | `5cc3346db23e` | unmoved |
| `waves/F-W8.md` | `1e3698adf4ff` | unmoved |
| `waves/F-W9.md` | `e9a9c3016f4c` | unmoved |
| `waves/F-W10.md` | `bb58dc400cff` | unmoved |
| `carry/F-W1-CARRY.md` | `0d0b091e86ca` | unmoved |
| `carry/F-W4-CARRY.md` | `bf1da6fe1e7e` | unmoved |
| `conformance/CENSUS-CANONICAL.md` | **`3e0a9acb3381`** | **MOVED — was `a450b8e9f80e`; §0.4 errata round 5 landed** |

**Consequence, and it governs every edit below.** All eleven specs still hold the exact bytes the pass-5 checks convicted, so every "current bytes" span in this order is live and greppable. **The census of record has moved and no wave knows it.** Every `a450b8e9f80e` pin in every spec is now stale by construction, and eleven of the eighteen errata entries move a row a wave has already booked. **`3e0a9acb3381` is the new pin; it appears in every re-pin edit below.**

## §A THE ERRATA DELTA — what the cured canonical does to each wave's duty

Read from `CENSUS-CANONICAL.md` §0.4 (E5-1…E5-18) and §2, re-derived at the bytes this seat.

| wave | roster BEFORE | roster AFTER | what moved | duty change |
|---|---|---|---|---|
| F.W0 | 56 | **57** | `PP-GATE` UNROUTED → **F.W0** (E5-1) | one CITATION becomes a BOOKING |
| F.W1 | 362 | **358** | `L-9` struck (E5-3) · `D:M-3` → F.W3 (E5-5) · `vue-tsc` struck (E5-6) · `D-i2` → NWO→SS-3 (E5-17) | four rows leave; **23 of the 24 escapes stand** |
| F.W2 | 31 | **27** | `C-15` → UNROUTED (E5-9) · `RD-5` → TERMINAL (E5-7/9) · `FR-TT-22` → NWO (E5-13) · `FR-USB-38` → NWO (E5-17) | **four §8a bookings become citations** — a clean census axis goes defective unless repaired |
| F.W3 | 926 | **928** | `PP-DEAD` → F.W3 leg F.W0 (E5-2) · `D:M-3` → F.W3 (E5-5) | the byte-identical register paste is now short by two |
| F.W4 | 1014 | **1012** | `PP-AGGLOM` → NWO→SS-3 · `PP-SEVLAW` → NWO (E5-17) | two rows leave; the 3 escapes stand; `FR-AH-27` stays F.W1 |
| F.W5 | 28 | 28 | — | unchanged |
| F.W5-W8 | 90 | **89** | `C:C-23` → UNROUTED (E5-15) | F-W5's sole citation and F-W8's whole band operand move |
| F.W9 | 17 | **16** | `R2-7` → TERMINAL (E5-16) | **§2.0 row 14 loses its operand** — the canonical says so in terms |
| NWO packets | 60 | **65** | +`FR-TT-22` +`PP-SEVLAW` +`FR-USB-38` (NWO 29→32) · +`PP-AGGLOM` +`D-i2` (SS-3 25→27) | F-W10's drain operand rises; its verbs were already right |
| escapes | 28 | **27** | `fr-EquationView vue-tsc` retired by E5-6 | the other 27 are the waves' to book or cite |

**Standing instruction for every batch**: where a spec's roster figure, its pasted roster block, or its `a450b8e9f80e` pin appears, it is re-based in the SAME edit that touches the row — never in a separate pass, and never by a wave re-cutting a detector (R4-3).

---

## §B BATCH-W1 — `waves/F-W1.md` (52 edits)

Anchors are line numbers at `a1302689aaa3`; all were `sed -n`'d by this seat. Where an edit inserts, the anchor is the line the insert follows.

### B.1 · P5-1 — the 23 surviving F.W1 escapes, BOOKED (25 edits)

**W1-01 · scaffold.** Anchor: after `:601` (the `**Closure claim on the canonical direction…**` paragraph that closes §6·R4c).
*Current bytes*: `**Closure claim on the canonical direction, stated as a CLAIM and tested elsewhere**: every one of the 362 canonical F.W1 rows is now either present in this spec by bytes, BOOKED by a §2·R4 row, or CITED above by carried identity or named holder.`
*Replacement*: same sentence with `362` → `358`, ⊕ a new heading and preamble appended below it:
`#### §6·R5 — THE PASS-5 ESCAPE CLOSURE (23 canonical F.W1 rows booked by name, record-qualified)` / `**Why this section exists.** PASS-5 §1.4 named 24 canonical F.W1 rows with no attributable booking or citation. One (`fr-EquationView vue-tsc`) was struck from the census at errata E5-6 and is disposed at W1-25. The other twenty-three are booked below, each with its canonical §1 row verbatim and the record's own banked-head line by ⟨cmd⟩ over the FROZEN corpus (`R=docs/tranches/V/megatranche/registry/adjudicated`, `35fc8ebf`). **Every row is record-qualified: an unqualified short token is not an identity (R-5), and eleven of these twenty-three were greened for four passes only by a RULINGS-namespace homonym.**`

**W1-02 … W1-24 · the twenty-three rows.** Anchor: inside §6·R5, in canonical §2 order. *Current bytes*: **∅ — no record-qualified occurrence exists** (each verified this seat by the check's own probe). *Replacement*: one table row apiece, shape `| <record> **<id>** | <canonical §1 row, verbatim> | <record's banked line, by ⟨cmd⟩> | <landing> |`.

| # | edit | record · id | canonical §1 row (verbatim, `3e0a9acb3381`) | banked text to paste (⟨cmd⟩ `sed -n 'Np' $R/<record>.md`) | landing |
|---|---|---|---|---|---|
| 1 | **W1-02** | `fr-AdminAuditLog` **S-4** | `` \| `S-4` \| — \| `F.W1` \| **F.W1** \| `` | `:122` *"`./pagination` absent from 4.0.0's 80 keys AND 7.0.0's 66-at-re-measure (my checks) — the retirement has held three majors"* | WU-G (Census FE), beside `AA-11`; carries D-20's figure reconciliation (W1-51) |
| 2 | **W1-03** | `fr-AdminFlaggedPanel` **FR-AFP-2** | `` \| `FR-AFP-2` \| — \| `F.W1` \| **F.W1** \| `` | `:45` *"All six `<Button>` callsites break at glass 4→7: three `size="icon"` … six `variant=` … silent fallthroughs"* | WU-C, as a named member of the Button re-grammar spine — **not** as prose inside an ellipsis |
| 3 | **W1-04** | `fr-AdminFlaggedPanel` **K1** | `` \| `K1` \| — \| `F.W1` \| **F.W1** \| `` | `:124` *"nine breaking prop sites; the corpus's most consequential error (would under-size F.W1); C's §3 table contradicts its own §1"* | WU-C, record-qualified against `fr-PaperSearch K1` (G4's) |
| 4 | **W1-05** | `fr-MobileFloatingToc` **F-1** | `` \| `F-1` \| `D-i2` \| `F.W1` \| **F.W1** \| `` | `:45` — the stale-`web/dist` emission hinge, *"→ F.W1 pre-gate"* | WU-B (substrate land-or-lose), where G4 already carries the mechanism unnamed |
| 5 | **W1-06** | `fr-MobileFloatingToc` **i-4** | `` \| `i-4` \| `D-i2` \| `GLASS-RELAY` · `F.W1` \| **F.W1** <sub>legs: GLASS-RELAY</sub> \| `` | `:92` *"→ **F.W1 pre-gate rider (F-1)**"* | WU-B beside F-1, with the GLASS-RELAY leg cited to the BH inbox |
| 6 | **W1-07** | `fr-EquationPanel` **D-14** | `` \| `D-14` \| `D-L18` \| `F.W1` \| **F.W1** \| `` | `:48` *"D-14+D-L18 (Metric drops `color` + the pill plate at 8.0.0) — F.W1"* | WU-H (the metric family), beside `D·D-M10` |
| 7 | **W1-08** | `fr-EditorControlsDock` **R-8** | `` \| `R-8` \| — \| `F.W1` \| **F.W1** \| `` | `:29` — the `VIZ_COLORS → #888888` fold | WU-E (`--viz-*` palette collapse); **record-qualified against ruling `R-8`** |
| 8 | **W1-09** | `fr-PaperArticleWindow` **R-9** | `` \| `R-9` \| — \| `F.W1` \| **F.W1** \| `` | `:32` *"**Seat-checked all three.** D-i-4 … D-i-5 …"* (the reader-coverage ruling row; its `C-i-1` limb folds to the census F.W1 lucide P0 at `:99`) | WU-N, as the lucide fold's ruling authority; qualified against `R-9.1` |
| 9 | **W1-10** | `fr-BasisSelector` **R-6** | `` \| `R-6` \| — \| `F.W1` · `SS-13` \| **F.W1** <sub>legs: SS-13</sub> \| `` | `:30` *"**MINOR ×4.** D-M6: direction proven by construction …"* | WU-P (re-ink/tone/geometry), SS-13 leg cited not booked |
| 10 | **W1-11** | `fr-GalleryView` **R-6** | `` \| `R-6` \| — \| `F.W1` \| **F.W1** \| `` | `:25` *"**MINOR · MINOR · MINOR-provisional.**"* (D-7/D-15/D-16 grades) | WU-P; qualified against `fr-BasisSelector R-6` (W1-10) |
| 11 | **W1-12** | `fr-App` **R-4** | `` \| `R-4` \| — \| `F.W1` \| **F.W1** \| `` | `:28` *"**MAJOR.** Fact CONFIRMED at both ends by this seat (installed `dist/styles/cards.css` rule present; producer 7.0.0 `.paper-texture` rule over src/+dist absent)"* | WU-D (P0 CSS-class census), the `.paper-texture` severity ruling; qualified against `R-4a`/`R-4b`/`R-4 sibling` |
| 12 | **W1-13** | `fr-GalleryDraftsSection` **R-4** | `` \| `R-4` \| — \| `GLASS-RELAY` · `F.W1` \| **F.W1** <sub>legs: GLASS-RELAY</sub> \| `` | `:28` *"**reader-1 wins.** fr-CollapsibleSection F-3 (my re-read, :44) already banks this exact identity"* | WU-O (token parity), with the GLASS-RELAY leg cited |
| 13 | **W1-14** | `fr-BasisCanvas` **R-2** | `` \| `R-2` \| — \| `F.W1` \| **F.W1** \| `` | `:26` *"**KILLED — DU wins.** LC confirmed a row whose API it admits it could not read"* (`D-26`, `grid.ts` vs glass-ui `./axes`) | WU-G, as a KILL disposition; qualified against ruling `R-2a`/`R-2c` |
| 14 | **W1-15** | `fr-GalleryInfiniteGrid` **R-2** | `` \| `R-2` \| — \| `F.W1` · `SS-1` \| **F.W1** <sub>legs: SS-1</sub> \| `` | `:27` *"**Carry KILLED; the missed find is re-proven by this seat at the tag and booked FR-GIG-5**"* | WU-Q (non-credit/negatives), beside the FR-GIG-5 NON-CREDIT LOCK; SS-1 leg cited |
| 15 | **W1-16** | `fr-AppHeader` **S-8** | `` \| `S-8` \| — \| `F.W1` \| **F.W1** \| `` | `:153` *"challenge-C §4's 18-row typed reconciliation … and the negatives that stop F.W1 over-budgeting this component"* | WU-Q; **not** G20's detector-axis label of the same spelling |
| 16 | **W1-17** | `fr-DarkModeToggle` **C-3** | `` \| `C-3` \| — \| `F.W1` \| **F.W1** \| `` | `:60` *"**fr-AnimationControls C-1** (F.W1 · G33 `npm ls` gate) — Fifth independent confirmation"* | WU-L (peer graph/manifest); qualified against `fr-PaperSearchInput C-3` |
| 17 | **W1-18** | `fr-EditorControlsDock` **L-2** | `` \| `L-2` \| `C-2` \| `F.W1` \| **F.W1** \| `` | `:151` *"**L-2/C-2 → fr-BasisCanvas D-1/BC-1/C-1** (BLOCKER; the `:123` rider pre-banked at fr-ContourEditorCanvas)"* | WU-E; qualified against `fr-SvgFilters L-2` (WU-M's pencil-boil row) |
| 18 | **W1-19** | `fr-CoefficientsSpectrum` **i-4** | `` \| `i-4` \| `DU-missed-5` \| `F.W1` \| **F.W1** \| `` | `:100` *"glass-ui MIGRATION.md **:738-739** … still homes `AnimatedDigitMode`/`AnimatedDigitProps` at `/animated-digit` with no removal marker"* | WU-G, BH-relay leg named; qualified against `fr-BasisSelector i-4` and `fr-MobileFloatingToc i-4` |
| 19 | **W1-20** | `fr-PaperSearchInput` **C-8** | `` \| `C-8` \| — \| `F.W1` \| **F.W1** \| `` | `:74` *"MINOR, FOLD → banked PSM-33 … rides the F.W1 rename ×35"* | WU-N (the lucide rename); the `PSM-33` pointer re-based off struck §6·R1 → §6·R4 (W1-46) |
| 20 | **W1-21** | `fr-AdminUserList` **FR-AUL-53** | `` \| `FR-AUL-53` \| — \| `F.W1` \| **F.W1** \| `` | `:96` — eight `lucide-vue-next` imports, home **F.W1** | WU-N, as a named member of the 35-site budget — **not** prose inside an ellipsis |
| 21 | **W1-22** | `fr-ImageUpload` **F8** | `` \| `F8` \| `D:U-3` \| `F.W1` \| **F.W1** \| `` | `:76` *"F8 · D:U-3 residual \| **fr-ContourPreview row 23** (MINOR, banked → F.W1) \| The instant-1px-translate-under-PRM residual — banked verbatim; ruling 2."* | WU-F, beside the already-carried fold target; **disarms `F8-REACH-01+02`** (W1-50) |
| 22 | **W1-23** | `fr-AnimationControls` **R-14** | `` \| `R-14` \| — \| `F.W1` \| **F.W1** \| `` | `:36` *"**Compatible — both demotions from the axis BLOCKER ratified; fold**"* (C-1 altitude) | WU-L beside `C-1`; qualified against `fr-SliderControl R-14`, whose F.W3 home is CITED at W1-34 |
| 23 | **W1-24** | `fr-GlassTimeline` **C-18** | `` \| `C-18` \| `C-20` \| `F.W1` \| **F.W1** \| `` | `:81` *"Zero direct value.js/keyframes consumption; census pin reading corrected at r1. FOLD: banked C-1/R-14 + census §1 + G33 — **F.W1**"* | WU-L; **word-boundary-qualified against `C-18.2`** (`fr-FourierShapeExtractor`), which is the byte-match that hid it |

**W1-25 · the 24th, disposed as a canonical amendment and not as a booking.** Anchor: §6·R5 tail.
*Current bytes*: ∅.
*Replacement*: `| `fr-EquationView` **`vue-tsc`** | **NOT AN IDENTITY — STRUCK FROM THE CENSUS.** `CENSUS-CANONICAL.md` §0.4 **E5-6** (2026-08-29): the token was minted from the first word of a prose head (`fr-EquationView.md:179`) and is not id-shaped under §0.1's separator semantics; the three sibling rows at `fr-GalleryView:131`, `fr-NotationPills:105`, `fr-PaperArticleWindow:145` were struck with it. This wave books nothing and cites nothing: **there is no row.** |`

### B.2 · P5-2 — the four F-W1 fabrications, BOOKED → CITED (3 edits)

**W1-26 · `fr-InfoCard FR-IC-17`.** Anchor `:318`, §2·R1a.
*Current bytes*: `| **FR-IC-17** | `fr-InfoCard.md:49` | **DISCHARGE REGISTER row (R-2a) — INFO, DISCHARGED-BY-UPLIFT; no work, one line.**`
*Replacement*: `| **FR-IC-17** — **CITED, NOT BOOKED (R4-10; PASS-5 P5-2 #1)** | `fr-InfoCard.md:49` | The canonical routes it `*DISCHARGED*`, home **TERMINAL (∅)** — ⟨cmd⟩ (`$X/conformance`) `grep -F 'fr-InfoCard	| \`FR-IC-17\`' …` → `` \| `FR-IC-17` \| — \| *DISCHARGED* \| **TERMINAL (∅)** \| ``. **TERMINAL ids are NOT wave debts.** The benefit fact is preserved as a ledger note under `fr-ExportModal D-M3` at §2·R2a.1; the DISCHARGE REGISTER row is struck. |`

**W1-27 · `fr-PaperSearch D-14 · D-15 · D-17`.** Anchor `:322`, §2·R1a.
*Current bytes*: `| **fr-PaperSearch D-14 · D-15 · D-17** | `fr-PaperSearch.md:55` | **Three token limbs of the F.W1 re-ink (R-2c)** — the cell at WU-P names only `D:B-3 · D-M1 · MISS-A6`; these three are the rest of it.`
*Replacement*: `| **fr-PaperSearch D-14 · D-15 · D-17** — **CITED, NOT BOOKED (R4-10; PASS-5 P5-2 #2–4)** | `fr-PaperSearch.md:55` | All three are canonical **UNROUTED** (`` \| `D-14` \| — \| — \| **UNROUTED** \| `` · `` \| `D-15` \| `D-18` \| — \| **UNROUTED** \| `` · `` \| `D-17` \| — \| — \| **UNROUTED** \| ``). The UNROUTED tier is the routing frontier the forming SS-3/SS-4 specs own; **it is not an F.W1 obligation and this wave takes no credit for it.** The re-ink MECHANISM stays at WU-P under its own banked ids `D:B-3 · D-M1 · MISS-A6`; the three token limbs are named here as a citation only.` **This one edit discharges three register entries.**

**W1-28 · the discharge-register denominator that FR-IC-17 anchors.** Anchor `:422`, §2·R3d.
*Current bytes*: `the wave keeps an explicit benefit ledger — **M-TL** · `fr-EquationView D·D-M3` · **MPC-32** · **FR-IC-17** · **FR-CP-5** · `fr-ExportModal D-M3` —`
*Replacement*: `the wave keeps an explicit benefit ledger — **M-TL** ⟨cited, F.W4⟩ · `fr-EquationView D·D-M3` ⟨cited, F.W4⟩ · **MPC-32** · ~~**FR-IC-17**~~ ⟨**STRUCK at repair round 5 — canonical TERMINAL (∅); a benefit is not a booking**⟩ · **FR-CP-5** · `fr-ExportModal D-M3` —` ⊕ the same strike carried at `:625`'s §E-3·R2 errata row, which asserts the register *"grew from two rows to three"*: append `⟨**re-corrected round 5: two, not three — FR-IC-17 is TERMINAL and never was a register row**⟩`.

### B.3 · P5-9 consequence — the roster re-based on the cured canonical (5 edits)

**W1-29 · the section heading.** Anchor `:510`.
*Current bytes*: `### §6·R4 — THE CANONICAL F.W1 ROSTER (362 rows, 65 records), BOOKED OR CITED — the operand of record for G20 leg (b)`
*Replacement*: `### §6·R4 — THE CANONICAL F.W1 ROSTER (**358 rows, 64 records** — re-based at repair round 5 on `CENSUS-CANONICAL.md` §0.4 errata E5-3 · E5-5 · E5-6 · E5-17), BOOKED OR CITED — the operand of record for G20 leg (b)`

**W1-30 · the reproduction receipt.** Anchor `:514`.
*Current bytes*: `**Reproduction (cwd `docs/tranches/X/fourier/conformance/`; operand sha256 `a450b8e9f80e` at quote time, R4-8.3).**` … `→ **362**, and the roster command piped to `tr -cd '\140'` then `wc -c` → **724** delimiters = 362 quoted ids.`
*Replacement*: same paragraph with `a450b8e9f80e` → **`3e0a9acb3381`**, `362` → **358** (both occurrences), `724` → **716**. The two `awk` commands are unchanged and re-run to the new figures; **re-run them, do not compute them.**

**W1-31 · the pasted roster block.** Anchor `:516-580` (65 lines).
*Current bytes*: the 65-line paste beginning `- **fr-AdminAuditLog** (3): `AA-2` · `AA-11` · `S-4`` and ending at the `fr-VisualizationView` line.
*Replacement*: re-run the §6·R4 `awk` against `3e0a9acb3381` and paste the **64** lines it returns byte-identical. The four deltas to expect: `fr-CanvasOverlayButton` 8→**7** (`L-9` gone) · `fr-EquationView` 6→**5** (`vue-tsc` gone) · `fr-ImageUpload` 5→**4** (`D:M-3` gone) · `fr-PaperSearchInput` 5→**4** (`D-i2` gone). **Verify by `diff`, never by eye** — the paste's byte-identity is G20 leg (b)'s LHS.

**W1-32 · `fr-ImageUpload D:M-3`, the citation that was right and obedient to a wrong operand.** Anchor: §6·R4c's `fr-ImageUpload` clause.
*Current bytes*: the cell quoting *"the record's own verdict is 'NOT an F.W1 item (K-12) → F.W3/W4'"* and citing it anyway because the canonical homed it here.
*Replacement*: `**`fr-ImageUpload D:M-3` — THE CANONICAL NOW AGREES (E5-5, 2026-08-29).** Round 4 quoted the record correctly (*"NOT an F.W1 item (K-12) → F.W3/W4"*) and cited the row anyway, because the census homed it at F.W1 and a wave may not re-cut the census. The errata round re-derived it to **F.W3** <sub>file-criterion → §5.d</sub>. The row leaves this roster; the citation is retained as the record of a correct reading under a wrong operand, which is the shape R4-3's amendment clause exists for.`

**W1-33 · the other three departures, disposed once.** Anchor: §6·R4c tail.
*Current bytes*: ∅.
*Replacement*: `**THREE FURTHER ROWS LEFT THIS ROSTER AT ERRATA ROUND 5, AND NONE IS THIS WAVE'S TO BOOK.** `fr-CanvasOverlayButton L-9` — **STRUCK** (E5-3): the record's `:69` alias cell claims it for `FR-COB-15`, and §0.1's ONE HOME PER ID forbids an alias booking a row of its own. `fr-EquationView vue-tsc` — **STRUCK** (E5-6): not id-shaped; see §6·R5's tail row. `fr-PaperSearchInput D-i2` — **RE-HOMED NWO→SS-3** (E5-17): the `F.W1` token sat inside a parenthetical citing *another* banked id's routing (`banked MISS-A6, F.W1 token re-ink`). **F.W1 358 = 362 − 4, and the arithmetic is the census's, not this file's.**`

### B.4 · P5-7 — legs BOOKED as rows, converted to cite-the-holder (10 edits)

R4-6: *"each leg books as ONE row at the wave that holds the host id."* R4-10: *"or CITES the holder by name."* §2·R4a applied both to the four PASS-4 escapes with hashes and never swept back. Each edit below converts one booking to a citation **at its existing cell** — no row moves, no mechanism is lost.

| edit | id (record-qualified) | current bytes (the booking site) | canonical home | replacement |
|---|---|---|---|---|
| **W1-34** | `fr-Tooltip` **FR-TT-24** | booked inside WU-F's `FR-TT-5/-6/-9/-14/-24` chain **and** in §6's roster | **F.W3** <sub>legs: F.W1</sub> | drop `-24` from the WU-F chain; append `⟨**FR-TT-24 — CITED, held at F-W3 §2 (canonical F.W3, leg F.W1); the break-surface note rides here, the identity does not**⟩` |
| **W1-35** | `fr-SliderControl` **R-14** | the WU-I slider cell carrying `R-14` bare | **F.W3** | `⟨**R-14 — CITED, held at F-W3; this cell carries the F.W1 GATE only (execution F.W3/W4, §2 WU-I's own words)**⟩`; qualified against `fr-AnimationControls R-14` (W1-23) |
| **W1-36** | `fr-EquationView` **M-TL** | §2·R3d's benefit-ledger head | **F.W4** | `⟨**M-TL — CITED, held at F-W4 §2.H**⟩` beside the ledger entry; the benefit stays, the identity leaves |
| **W1-37** | `fr-EquationView` **D·D-M3** | same ledger, same line | **F.W4** | `⟨**D·D-M3 — CITED, held at F-W4 §2.H**⟩` |
| **W1-38** | `fr-CanvasOverlayButton` **FR-COB-28** | WU-C's Button-spine roster | **F.W0** | `⟨**FR-COB-28 — CITED, held at F-W0 §3 row 10's fold list**⟩` |
| **W1-39** | `fr-CoefficientsPanel` **FR-CP-33** | WU-P's re-ink roster | **F.W3** | `⟨**FR-CP-33 — CITED, held at F-W3 §X.1-v5 item 4's register**⟩` |
| **W1-40** | `fr-BasisSelector` **m-6** | WU-P | **F.W3** | `⟨**m-6 — CITED, held at F-W3**⟩`; qualified against `fr-ContourSettings m-6` |
| **W1-41** | `fr-CollapsibleSection` **m-7** | WU-O token-parity roster | **F.W3** | `⟨**m-7 — CITED, held at F-W3**⟩`; qualified against `fr-BasisSelector m-7` (F.W5-W8, F-W8's) |
| **W1-42** | `fr-NotationPills` **FR-NP-6** | WU-D's CSS-class census roster | **F.W3** | `⟨**FR-NP-6 — CITED, held at F-W3**⟩` |
| **W1-43** | *the law, swept backward* | §2·R4a's opening: `**The disposition rule, stated once (R4-10).** An escape is answered by BOOKING when the canonical homes the id at F.W1, and by CITING ITS HOLDER when it does not` | — | append: `**AND THE RULE IS RETROACTIVE (repair round 5).** It was written for the four PASS-4 escapes and never run over §2·R1, §2·R2 or §2·R3. It is now run over all of them: W1-34…W1-42 convert nine legs, W1-26/W1-27 convert four non-debts, and §6·R5 books the twenty-three the sweep had missed in the other direction. **A law applied forward and never backward is a law that has not landed.**` |

### B.5 · P5-4 / P5-5 — the two receipts that do not reproduce (2 edits)

**W1-44 · `:188`, the lucide budget of record (R4-2.4 — one command, one base).** *Current bytes*: `Figure of record: **35 import sites / 35 files** (`grep -rn "lucide-vue-next" src/ \| wc -l` → 35)`.
*Replacement*: `Figure of record: **35 import sites / 35 files** (⟨cmd⟩ base `$F` = `/Users/mkbabb/Programming/fourier-analysis`: `/usr/bin/grep -rn "lucide-vue-next" web/src | wc -l` → **35**)` ⊕ the disclosure `⊘ **The round-1→4 spelling took `src/`, which is a REAL BUT DIFFERENT tree under `$F` and returns 0 with exit 1 — a receipt that resolves silently to a wrong-but-plausible answer, which is worse than one that errors. Re-run before quoting.**` This is the site the round-4 purge seat cured at §2·R2a.3 for a sibling receipt and left standing here.

**W1-45 · `:400`, the K-24 set that its own round made two-membered.** *Current bytes*: the §2·R3 opening note publishing `grep -l 'K-24' waves/*.md carry/*.md` → *"`waves/F-W2.md` **alone**"*.
*Replacement*: `⟨cmd⟩ (`$X`) `/usr/bin/grep -l 'K-24' waves/*.md carry/*.md` → **`waves/F-W1.md` · `waves/F-W2.md`** — a two-member set, and `waves/F-W1.md` **is this file** (§2·R3b, thirteen lines below, already discloses it: *"disclosed not trimmed: `waves/F-W1.md` is THIS ROW"*). ⊘ **The word "alone" was the defect, not the command**: R3-3.10 outlawed the count and not the count word, and a prose gloss that freezes a set's cardinality is the PASS-3 D-2 class in one adjective. **The gloss is struck; the set is printed.**

### B.6 · P5-6 — the pin block, re-stamped at final bytes (1 edit)

**W1-46 · `:437`.** *Current bytes*: `**Sibling hashes at quote time (R4-8.3, first 12 hex of `shasum -a 256`, run this seat this session).** `waves/F-W0.md` `69b05ccbb320` · `waves/F-W2.md` `6fc843820033` · `waves/F-W3.md` `a89c3386f3f8` · `waves/F-W4.md` `71f3b640d51e` · `carry/F-W1-CARRY.md` `0d0b091e86ca` · `conformance/CENSUS-CANONICAL.md` `a450b8e9f80e`.` … `**Every cross-file quotation this spec carries — §2·R3a/b/c's F-W2 and F-W3 spans, §2·R4a's F-W3 and F-W4 holders, G1's and cross-edge 1's F-W0 gate-id citations — is pinned to these hashes.**`
*Replacement*: the same block re-stamped by the **purge seat that runs LAST** (R4-8.2), with **three stale pins corrected and F-W10 added**: `waves/F-W0.md` **`282f0c120cd4`** · `waves/F-W2.md` **`578dba1fb42e`** · `waves/F-W3.md` `a89c3386f3f8` · `waves/F-W4.md` **`39e1a60b3fc9`** · **`waves/F-W10.md` `bb58dc400cff`** · `carry/F-W1-CARRY.md` `0d0b091e86ca` · `conformance/CENSUS-CANONICAL.md` **`3e0a9acb3381`**; and the universal extended to name §2·R2b.5's and §2·R2b.7's two F-W10 quotations, which the round-4 list omitted while quoting the file twice. ⊘ **Record the mitigation the pass-5 seat proved: all eleven quotations pinned to the three stale rows still reproduce — the mechanism worked; the instrument lied.**

### B.7 · the unapplied rulings' surviving substance (6 edits)

**W1-47 · R4-3.4 — five in-body pointers still aimed at the struck §6·R1.** Anchors `:75` (WU-C), `:95` (WU-E), `:141` (WU-I), `:171` (WU-L), `:188` (WU-N).
*Current bytes* (each): the clause *"the elided ids are NAMED … see §6·R1"* — e.g. WU-C's `ids named at **§6·R1** (`FR-AFP-2 … fr-EasingCurvePreview` is prose, never the roster)`.
*Replacement* (each): `ids named at **§6·R4 / §6·R5** ⟨*§6·R1 was struck as an operand at R4-3.4 and names none of the six endpoints these cells spell; the pointers are re-based at repair round 5*⟩`, and the named endpoint (`FR-AFP-2`, `FR-AUL-53`, `fr-App B-1`, `fr-GalleryView C·C-1`, `fr-ImageUpload`, `fr-EasingCurvePreview`) is written as a booked row at §6·R5 rather than as prose inside an ellipsis. **Five sites; one edit each is acceptable, but they must land together or the class survives.**

**W1-48 · R4-3 — G20's claim surface, `:266`.** *Current bytes*: `§6 + §6·R1 + §2·R1 + §2·R2 + **§2·R3** are the claim the run tests`.
*Replacement*: `§6 + §6·R4 + §6·R5 + §2·R1 + §2·R2 + §2·R3 + **§2·R4** + **§2·R5** are the claim the run tests` ⟨*repair round 5: the round-4 and round-5 bookings were outside the surface of the one gate that could catch an escape in either of them — the gate was pointed away from the work that would have caught D-1*⟩. **W1-49** applies the same correction to `:500`'s closing sentence, whose *"repair rounds 1, 2 and 3"* becomes *"repair rounds 1 through 5"*.

**W1-50 · P5-11 — the `R-n` collision class, disarmed by roster.** Anchor `:508` (the homonym line under §6·R1).
*Current bytes*: `**Homonyms carried record-qualified, never merged**: `M-5` … `C-11`, `C-13`, `C-2`, `M-13`, `m-6` across §2 generally. An unqualified short token is not an id.`
*Replacement*: the same line ⊕ `**AND THE `R-n` NAMESPACE, WHICH IS THE ONE THAT COST TWENTY-THREE ROWS.** This spec's own ruling ids occupy `R-1..R-10`; the canonical homes **thirteen banked `R-n` rows across eleven records at F.W1** — `fr-App R-4` · `fr-AnimationControls R-14` · `fr-BasisCanvas R-2` · `fr-BasisSelector R-1`/`R-6` · `fr-CoefficientsSpectrum R-5` · `fr-CollapsibleSection R-1`/`R-5` · `fr-EditorControlsDock R-5`/`R-8` · `fr-GalleryDraftsSection R-4` · `fr-GalleryInfiniteGrid R-2` · `fr-GalleryView R-6` · `fr-PaperArticleWindow R-9` · `fr-SliderControl R-1`/`R-5`. **Every `R-n` in this file is either a RULINGS id (written `R-n` with a ruling verb) or a banked identity (written `fr-<Record> R-n`). There is no third reading.** Eleven of PASS-5's twenty-four escapes were greened by this collision alone.`

**W1-51 · M-25 — the budget of record under an unqualified token.** Anchor `:77`, WU-C.
*Current bytes*: the `D-M3` cell (*the Button budget of record, 87 / 36 / 35*) whose HOMONYM-DISARMED note names only `fr-ExportModal D-M3`.
*Replacement*: `**`fr-PaperSearchInput D-M3`** — the record spelled at the head ⟨registry `:51`: *"Adjudicated blast radius: 87 template `variant=` sites · 36 `size="icon"` · 35 files … Route F.W1"*⟩, with the existing disarm against `fr-ExportModal D-M3` retained beside it. ⊘ **The wave's single most-cited figure sat under a bare token in the file that legislates against bare tokens.**

**W1-52 · P5-5 successor — the probe that cannot fail, retired.** Anchor `:586`, §6·R4a.
*Current bytes*: the paragraph publishing the `grep -Fq` partition `SPEC 315 · CARRY 11 · NEITHER 36` at `83ffe83cb37f` with its honest-limit disclosure.
*Replacement*: retain the disclosure verbatim, and append `⊘ **RETIRED AS A CLOSURE INSTRUMENT AT REPAIR ROUND 5.** Re-run at the settled bytes with the pasted roster excluded (⟨cmd⟩ `awk 'NR<516 || NR>580' waves/F-W1.md > /tmp/specnr.txt`), the probe returns **SPEC 358 · CARRY 0 · NEITHER 0** — it greens every id, including ids with zero word-boundary occurrences in the file, because §6·R4b/c and §6·R5 now name them all. **A test that cannot fail is not a test.** The only admissible instrument is G20 leg (b)'s record-qualified join, run by a non-author seat, and its LHS is §6·R4's paste. **The ~15-pair homonym tail PASS-5 declared but did not count (`R-1` ×3 · `R-5` ×4 · `K-8` ×4 · `K-9` ×2 · `C-4` ×4 · `C-6` ×2 · `C-11` ×4 · `K-5` ×3 · `F-1` ×2 · `F-3` ×2) is that join's first work, and resolving it re-grades no record.**`

**Batch W1 total: 52 edits.** None opens product source; none re-books, re-grades or renames an id; every one lands inside `waves/F-W1.md`.

---

## §C BATCH-MID — `waves/F-W2.md` · `waves/F-W4.md` · `waves/F-W5.md` (51 edits)

### C.1 · `waves/F-W2.md` @ `578dba1fb42e` (19 edits)

**The premise.** PASS-5 found this wave's census axis CLEAN — 31 booked, 0 escaped, 0 fabricated — and it is now DEFECTIVE **through no act of its own**: errata E5-7 · E5-9 · E5-13 · E5-17 moved four of the thirty-one. The first four edits are not corrections of a mistake; they are the wave following its operand, in the order R4-3 requires (the census moves, the wave follows, never the reverse).

| edit | anchor | current bytes | replacement |
|---|---|---|---|
| **MID-01** | §8a row 9, `fr-ConvergenceLegend C-15` | the §8a booking row with column-2 cell `` `F.W2` … **F.W2** `` | **BOOKED → CITED.** Canonical now: `` \| `C-15` \| — \| — \| **UNROUTED** <sub>*errata R5 E5-9*</sub> \| ``. Replace the booking with `**CITED — canonical UNROUTED (E5-9, 2026-08-29).** The row's own arrow is `**FOLD → the ramp identity (M-β1/FR-CP-2)**` (`fr-ConvergenceLegend.md:83`) and its lone `F.W2` token modifies *that* identity's already-adopted angle. The legend-site instance stays in §2's cure text under `M-β1`; the identity leaves this roster.` |
| **MID-02** | §8a row 12, `fr-ConvergencePlot RD-5` | the booking row, aliases `⟨D-2 · C-14⟩` | **BOOKED → CITED.** Canonical now: `` \| `RD-5` \| `D-2` · `C-14(b)` \| — \| **TERMINAL (∅)** <sub>*errata R5 E5-7 / E5-9*</sub> \| ``. Replace with `**CITED — canonical TERMINAL (∅).** A ruled-disagreement head whose six register siblings are all TERMINAL; its lone `F.W2` token sits inside *"the banked identity's F.W2 … adoption angle must re-route"*. **Alias spelling corrected to the record's own `C-14(b)`.**` |
| **MID-03** | §8a row 30, `fr-Tooltip FR-TT-22` | the booking row, `` **F.W2** <sub>legs: NWO</sub> `` | **BOOKED → CITED.** Canonical now **NWO (packet)** (E5-13, filed by three independent seats). Replace with `**CITED — canonical NWO (packet).** `fr-Tooltip.md:56`'s arrow is the sole terminal `**NO-WAVE-OWNER.**`; *"a zero-cost row on the F.W2 ledger"* is the prose of a **negative budget statement** — the sentence's whole point is that the row costs F.W2 nothing. Disposed at F-W10's drain.` |
| **MID-04** | §8a row 31, `fr-UserSlugBar FR-USB-38` | the booking row, aliases `⟨L-16 · L-17 · D-i2 · C-i3⟩` | **BOOKED → CITED.** Canonical now **NWO (packet)** (E5-17). Replace with `**CITED — canonical NWO (packet).** The record says it in bold: *"**do NOT book this component into the F.W2 migration surface**"* (`fr-UserSlugBar.md:81`). **This wave obeyed a census that had captured a prohibition as a routing; the census is amended and the booking goes with it.**` |
| **MID-05** | §8a header + `fr-ContourPreview` rows 5/6 | `rosterSize 31 · booked 31 · escaped 0 · fabricated 0` and rows spelled `D:B-1` / `C:D-1` | `rosterSize **27** · booked **27** · cited **31** · escaped 0 · fabricated 0` ⊕ the `(a)`/`(b)` disambiguators **restored from the record** (E5-8): row 5 `` `D:B-1` ⟨aliases `C:D-1(a)` · `L:L-9`⟩ `` · row 6 `` `C:D-1(b)` ``. ⊘ This retires §8a's absolute *"An alias is never re-booked as a row, here or anywhere"* being falsified by its own rows 5 and 6 — **the stripped disambiguator was the canonical's defect and it is cured; the absolute now holds.** |
| **MID-06** | `:19` and every `31 rows` operand statement (§1a, §8a head, G19's LHS) | `4851:### F.W2 — **31 rows**` and the derived `31` | re-run ⟨cmd⟩ `/usr/bin/grep -n '^### F\.W2' …/CENSUS-CANONICAL.md` → `### F.W2 — **27 rows**` and re-base every consuming figure. **Operand sha `a450b8e9f80e` → `3e0a9acb3381` at all five citing sites.** |
| **MID-07** | `:184`, B11 — **P5-5, third generation at one paragraph** | `⟨cmd⟩ `grep -o 'F.W0 additionally owns the pre-gate (G-4) + relay item 0' F-W0.md` **returns that sentence**` ⊕ the quoted span | **STRIKE the quotation and its attestation.** Re-run: ⟨cmd⟩ `/usr/bin/grep -c 'additionally owns' F-W0.md` → **0** — the word *additionally* is at no byte of `F-W0.md`. Replace with the live bytes and their own command: ⟨cmd⟩ (`$X/waves`) `/usr/bin/grep -oF 'the F.W0 half was always the one this row owns: the pre-gate (G-4) + relay item 0' F-W0.md` → *"the F.W0 half was always the one this row owns: the pre-gate (G-4) + relay item 0"*. ⊘ **Record why it is the third generation**: round 3 struck *"NO-WAVE-OWNER (producer emitter)"* here; round 5 struck the `:35`/FR-NP-9 mis-receipt here; the round-3 cure's own replacement — *"the two real strings above, each by command"* — is now one real string and one that returns nothing. **A cure that re-quotes a moving sibling is a defect with a delay fuse.** |
| **MID-08** | `:109`, §2b's anchor-authority receipt | `⟨cmd⟩ `grep -n '^### G-1[0-9]' F-W0.md` → G-11 … · G-12 … · G-13 …` (three outputs, `NNN:` prefixes and `### ` markers stripped) | **Narrow the command to the three gates it means, or publish six.** Preferred: `⟨cmd⟩ (`$X/waves`) `/usr/bin/grep -n '^### G-1[123] — ' F-W0.md` → `364:### G-11 — …` · `369:### G-12 — …` · `374:### G-13 — …` — **prefixes and heading markers retained as bytes.** ⊘ It is not staleness: ⟨cmd⟩ `git show fffb9685:…/F-W0.md \| /usr/bin/grep -c '^### G-1[0-9]'` → **6**, live → **6**. **The command has returned six since before round 5, on the receipt that establishes this spec's entire stable-anchor authority.** |
| **MID-09** | `:445-453`, §8d hash table | eight rows pinned `cd64d6580098` (F-W0) · `8507ec30f0bd` (F-W1) · `71f3b640d51e` (F-W4, cell reads **"no"**) · `8047404bb6fc` (F-W5) · `a450b8e9f80e` (census) | **Re-stamped by the purge seat AFTER every other seat's final byte (R4-8.2 LAW E).** F-W0 → **`282f0c120cd4`** · F-W1 → **`a1302689aaa3`** · F-W4 → **`39e1a60b3fc9`** and the *"moved: no"* cell → **"YES"** · F-W5 → **`132c03192176`** · census → **`3e0a9acb3381`** ⊕ a row for it reading **"YES — errata round 5, §0.4"**. ⊘ Carry the pass-5 re-walk verbatim: **the F-W1, F-W4 and F-W5 quotations all still resolve; the F-W0 set does not** (MID-07, MID-08). *A hash table is a smoke detector, not a fire door.* |
| **MID-10** | `:248`, §5's base universal | `**Every ⟨cmd⟩ in this file therefore carries a LITERAL ABSOLUTE PATH and consumes no shell variable at all** — the only form that cannot fail R4-2.1–4` | **Restate as what it can prove.** `**No ⟨cmd⟩ in this file consumes a shell variable** — verified by a reader finding a `$` between backticks in a command, never by counting dollars. **Sixty-eight of the eighty-two distinct commands carry no absolute path and run from a DECLARED base instead**; the four bases are named below and every command names the one it runs from.` ⟨cmd⟩ this seat: `… \| sort -u \| wc -l` → **82**; `… \| grep -c '/Users/'` → **14**. |
| **MID-11** | `:248`, the three-roots sentence | `The three roots, stated once as prose and written out at every use: sibling specs at `…/waves/`; the frozen corpus at `…/registry/adjudicated/`; the fourier tree at `/Users/mkbabb/Programming/fourier-analysis/`, absolute and never relative (R-10.6).` | **FOUR roots.** Append `; and the **value.js repository root** at `/Users/mkbabb/Programming/value.js/` — **undeclared until repair round 5 and in use at nine distinct commands**: `grep -n 'Object.freeze' src/easing.ts` · `grep -n '^});' src/easing.ts` · the fused `Object.freeze\|^});` form · `sed -n '92,135p' src/easing.ts` · `sed -n '95,113p' src/easing.ts \| grep -c ','` (two spellings) · `grep -rn 'sampleColorRamp\|mixColorsN' src/ dist/ \| wc -l` · `sed -n '34p' .gitignore` · `ls docs/tranches/V/megatranche/audit/probes/app-wave/`.` ⊘ **This is the very defect the round-4 minute claims to have retired — three bases mixed in one file — reproduced with a fourth under a universal asserting there are none.** |
| **MID-12** | `:115`, §2b item 3's two fourier-tree commands | `⟨cmd⟩ `ls web/src/components/equation/convergence/`` and `⟨cmd⟩ `find web/src -name 'Convergence*.vue'`` | Absolutise both against the paragraph's own *"absolute and never relative (R-10.6)"*: `⟨cmd⟩ `ls /Users/mkbabb/Programming/fourier-analysis/web/src/components/equation/convergence/`` → `ConvergenceLegend.vue` · `ConvergenceTimeline.vue`; `⟨cmd⟩ `find /Users/mkbabb/Programming/fourier-analysis/web/src -name 'Convergence*.vue'`` → the three paths. ⊘ **These are phantom witnesses of exactly the R-10.6 class the round-4 cure enumerated and closed at five sites — two were never counted, and they carry §2b's *"two SFCs, not one"* correction into F.W0's owned G-11 anchor table.** |
| **MID-13** | `:334`, §7's zero-cell row | `— **six** of them are canonical `F.W2` rows and are booked at §8a (`FR-AUL-55` · `C-25` · `S-9` · `FR-EMT-24` · `C-08` · `FR-TT-22` · `FR-USB-38` · `C·S-2`)` | **R4-7.3: the LIST is authoritative.** The enumeration is EIGHT, and after E5-13/E5-17 two of the eight (`FR-TT-22` · `FR-USB-38`) are no longer F.W2 rows. Replace with `— **six** of them are canonical `F.W2` rows and are booked at §8a (`FR-AUL-55` · `C-25` · `S-9` · `FR-EMT-24` · `C-08` · `C·S-2`), and **two more** — `FR-TT-22` · `FR-USB-38` — were F.W2 rows until errata round 5 re-homed both to **NWO (packet)**; they are CITED at §8a and counted in the DENOMINATOR at G11 either way. **The count word now equals its list because the list moved to meet it.**` |
| **MID-14** | G11's `SIX F.W2 measured itself` | `(Tooltip FR-TT-22 · EquationModeToggle FR-EMT-24 · **FunctionInput item 6** · PaperView C-08 · ConvergenceTimeline S-9 · AdminUserList FR-AUL-55)` | `FunctionInput item 6` is **not** an §8a booking — §8b row 22 homes it *"out of the census's domain (§0.2)"*. Replace with the reconciled set and its class label: `**the six F.W2 measured itself** — five canonical F.W2 rows (`FR-EMT-24` · `C-08` · `S-9` · `FR-AUL-55` · `C·S-2`) ⊕ **`fr-FunctionInput` item 6, an out-of-domain prose head that is a DENOMINATOR member and not a census row (§0.2)**. ⊘ The `16 = TEN ⊕ SIX` decomposition stands only under this reading; the round-4 claim that D-7 was *"separated at D3, G11 and §7"* was true at two of the three. |
| **MID-15** | G15, the `0.192` span | `…`timingFunctions` deleted; **8/22 curves drift, max 0.192**` | **Re-paste plain, against the bank.** ⟨cmd⟩ `/usr/bin/sed -n '296p' library-band.md` → `` `timingFunctions` deleted; 8/22 curves drift, max 0.192); `fourier-vizcolor-oklch.mjs` 5/6 `` — **the bank sets no emphasis**, and this same file renders the same span plain at three sibling sites. R4-7.1: *"nothing is added, nothing is re-styled."* |
| **MID-16** | G15, the `fr-EasingPicker.md:52` span | `**14 of 22 silently drift analytic→CubicBezier-approximation** (8 stay analytic incl. ease-out-cubic/-expo)` | The bank opens the bold one word earlier: `…**but 14 of 22 silently drift analytic→CubicBezier-approximation**…` (⟨cmd⟩ `sed -n '52p' fr-EasingPicker.md`), so the elided *but* sits **inside** the emphasised span. Re-cut the quotation to open at `but`, or drop the emphasis and label the span a paraphrase. **A third rendering at `:148` closes the bold elsewhere and must be reconciled in the same edit.** |
| **MID-17** | masthead, the FR-NP-9 cure note | `Re-quoted at `:47` beside the `grep -Fn` that returns exactly it.` | `Re-quoted at `:47`; the exact witness is ⟨cmd⟩ `/usr/bin/grep -Fc "(nine of nine channels, proven with value.js's own dist — the very declared-and-bypassed dependency this row books)" fr-NotationPills.md` → **1**. ⊘ The `grep -Fn` returns the whole ~400-character line 47, not the quoted parenthetical; **the cure landed and only its description of its own witness was loose** (R4-1: a receipt is published beside the command that produced it). |
| **MID-18** | §6a's anti-rename lock line | `Anti-rename law, B5. ONE ramp identity, never re-booked per surface.` | `Anti-rename law, B5. **ONE ramp identity, `M-β1`, original for life** — never re-booked per surface.` ⊘ M-25 wants the lock keyed to the banked id at the lock site; B5 carried it and the lock did not. |
| **MID-19** | the masthead anchor self-receipt ⊕ G13's cell | `grep -noE 'F-W[0-9]+\.md:[0-9-]+\|W9\.md:[0-9]+' F-W2.md` → ∅ (a **literal pipe** under `-E`) ⊕ G13's `grep -rniE 'megatranche\|facility-19\|library-band' …` | Re-cut both to true ERE alternation with the table-cell escape resolved: `⟨cmd⟩ /usr/bin/grep -noE 'F-W[0-9]+\.md:[0-9-]+|W9\.md:[0-9]+' F-W2.md` → **∅ (exit 1)** — *the claim survives; only its witness did not witness it* — and G13's ERE with a rendered `|`, output **1**. ⊘ Record the engine fact so no seat re-derives it: **BSD `grep` honours `\|` in BRE (`printf 'alpha\nbeta\n' \| grep -c 'alpha\|beta'` → 2), so the BRE siblings G2/G9 are unaffected; only the `-E` forms are ambiguous**, and R4-2.5 admits no charitable reading. |

### C.2 · `waves/F-W4.md` @ `39e1a60b3fc9` (16 edits)

| edit | anchor | current bytes | replacement |
|---|---|---|---|
| **MID-20** | `:418`, §Y's founding receipt — **P5-3, BLOCKER** | `⟨cmd⟩ this seat, 2026-08-29 over the round-3 bytes: `git show ce2622d1:docs/tranches/X/fourier/waves/F-W4.md > /tmp/fw4-r2.md; /usr/bin/grep -ci 'errata' /tmp/fw4-r2.md` → **1** … `/usr/bin/grep -c 'receipts run' /tmp/fw4-r2.md` → **0** · `/usr/bin/grep -c 'PURGE-SEAT' /tmp/fw4-r2.md` → **3**` | **RE-POINT THE OPERAND TO THE ROUND-3 SETTLE AND RE-RUN ALL THREE.** `ce2622d1` is *"repair r2 complete … + L-20 pass 3"* and returns **1 · 0 · 0**; the published `3` is **`fffb9685`'s figure pasted beside a `ce2622d1` command**. Replacement: `⟨cmd⟩ repo root, re-run by a seat that did not author §Y: `git show fffb9685:docs/tranches/X/fourier/waves/F-W4.md > /tmp/fw4-r3.md; /usr/bin/grep -ci 'errata' /tmp/fw4-r3.md; /usr/bin/grep -c 'receipts run' /tmp/fw4-r3.md; /usr/bin/grep -c 'PURGE-SEAT' /tmp/fw4-r3.md` → **1 · 0 · 3**.` ⊘ **The prose *"asserted a purge sweep three times inline"* survives only over the commit that holds three.** R2-1-LAW 1 breached in the section R4-9.1 created to make the sweep auditable, against the wrong round of the file it audits. |
| **MID-21** | `:439`, §Y.2 item 3 — **P5-3, BLOCKER** | `**§2.I's `PAW-6/-18/-26/-29` re-run pairing.** Correct: `4 · 3 · 3 · 7` reproduces over the settled bytes.` | `**§2.I's `PAW-6/-18/-26/-29` re-run pairing.** **Corrected in round 4 and mis-transcribed here**: §2.I of this same file already struck `4 · 3 · 3 · 7`. ⟨cmd⟩ (`$W`) `for t in PAW-6 PAW-18 PAW-26 PAW-29; do /usr/bin/grep -oE "\b$t\b" F-W4.md \| wc -l; done` → **7 · 3 · 3 · 7** — the FIRST member is **7**, not 4.` ⊘ **The verb changes from *Correct* to *corrected-in-round-4*.** Under R4-4's two-key rule the transcript is exactly what a successor re-runs; this row failed on first contact. |
| **MID-22** | `:409`, §6's fold register — **P5-4** | `⟨cmd⟩ this seat, 2026-08-29, `/usr/bin/grep -rl 'D-m13' waves/ ../carry/` → **∅** before this write.` | **No base resolves both operands** — from `…/fourier` the command exits 2 on `../carry/`; from `…/fourier/waves` it exits 2 on `waves/`; `docs/tranches/X/carry` does not exist. Replace with one base and both halves of R3-3.8's pairing: `⟨cmd⟩ (base `$W` = `docs/tranches/X/fourier/waves`) `/usr/bin/grep -rl 'D-m13' . ../carry/` → **∅ before this write** · **`./F-W4.md` after it**.` ⊘ Same failure mode as the `-P` class R4-2 abolished, published inside the round-4 repair. |
| **MID-23** | §2.X.2, escape 1 — **P5-1** | ∅ — no record-qualified booking; all four `K-10` tokens in the file are foreign (`FR-CL K-10` L26 · an fr-AnimationControls disclaimer L106 · `fr-CL K-10` L364 · an `AA-1` sub-arm L397) | **BOOK `fr-EquationResult K-10`**, canonical `` \| `K-10` \| — \| `F.W4` \| **F.W4** \| ``, at §2.X.1(C)'s send-whole cell where its substance already lives: the `c654824e` vs `cd26c65` provenance kill at L26/L364 **is** this row's content, carried without its banked identity. §6.4: *"a fold whose source id is unwritten is a silent drop wearing a cure's clothes."* |
| **MID-24** | §2.G / §2.X.2, escape 2 — **P5-1** | ∅ — ⟨cmd⟩ `/usr/bin/grep -oE 'FR-FG-[0-9A-Za-z-]+' F-W4.md \| sort -u` → `FR-FG-12 · FR-FG-13 · FR-FG-21 · FR-FG-22 · FR-FG-C-8`, **no `R-7`**; the file's only `R-7`-bearing tokens are `FR-CP-R-7` | **BOOK `fr-FrequencyGraph R-7`**, canonical `` \| `R-7` \| — \| `F.W4` \| **F.W4** \| ``, record-qualified beside `FR-FG-12` in the `/equation` instrument unit, and **disarmed against `fr-CoefficientsPanel FR-CP-R-7`** in the same cell. |
| **MID-25** | §2.J / §2.X.2, escape 3 — **P5-1** | ∅ — every `R-5` in the spec is the programme's own short-id-law ruling (*"R-5: a bare token is not an identity"*, L103/L112/L137/L144) | **BOOK `fr-GalleryView R-5`**, canonical `` \| `R-5` \| — \| `F.W4` \| **F.W4** \| ``, record-qualified in the `/gallery` unit. ⊘ **The sharpest of the three: a live cross-namespace homonym with the spec's own short-id-law ruling, cited four times while the record row it collides with went unbooked.** Add the `R-n` disarm line the file lacks. |
| **MID-26** | `:321`, §2.X.2(f)2's expansion — **P5-2** | `-19 · -23 · **-25 · -26 · -27** · -28 · -32 · -34 …` (the chain **books** `FR-AH-27`) | **DROP `-27` from the booking chain and CITE it.** Canonical: `` \| `FR-AH-27` \| `D-m1` · `L-11` · `C-8` · `C-9` \| `F.W1` · `F.W4` \| **F.W1** <sub>legs: F.W4</sub> \|`` — an F.W1 row with an F.W4 leg. Replace with `… -25 · -26 · -28 · -32 …` ⊕ a leg row at §2.X.2(d) in the form (d) already uses for `fr-SpeedSelect D-03` and `GM-6`: `**`fr-AppHeader FR-AH-27` — LEG BOOKED AS ONE ROW AT THE HOLDER (R4-6); the identity is F.W1's, the 24 dead `--ease-apple-spring` lines delete here.**` ⊕ **enumerate the fabrication class at §2.X.2, even at zero** — the duty sentence promises both classes are *"enumerated below rather than asserted away"* and the fabrication class simply does not appear. |
| **MID-27** | `:296`, the rename crosswalk — **P5-8** | `**`fr-ConvergencePlot` — the `FR-CP-LMn` spelling drops the banked hyphen (7).**` crosswalking seven | ⟨cmd⟩ (`$W`) `/usr/bin/grep -oE 'FR-CP-L[A-Z][0-9]+' F-W4.md \| sort -u` → **nine**: `FR-CP-LB2 · FR-CP-LB3 · FR-CP-LM1 · FR-CP-LM2 · FR-CP-LM3 · FR-CP-LM4 · FR-CP-LM5 · FR-CP-LM7 · FR-CP-LM8`. **Retire the numeral to the enumeration** and add the two missing crosswalks — `L-B2` ⟨FR-CP-LB2⟩ · `L-M4` ⟨FR-CP-LM4⟩ — both canonical fr-ConvergencePlot **F.W4** rows, so the un-crosswalked renames sat on live roster rows. ⊘ The bare `L-B2` bytes at L94/L183/L377 are unqualified and cannot discharge them: **the record banks no `L-B1`.** |
| **MID-28** | §2.X.2(f)'s BANDS clause — **P5-8** | `**`FM-4..FM-16` and `FM-17..FM-19` are ONE canonical row each and their members are enumerated at §2.F and §2.X.2(b) so no member is lost to the shorthand.**` | ⟨cmd⟩ `/usr/bin/grep -c 'FM-18' F-W4.md` → **0**. Either enumerate the three (`FM-17 · FM-18 · FM-19`) at §2.F, or strike *"no member is lost"* and say the band is carried whole. ⊘ **The canonical half of this defect is already cured**: E5-10 re-spelled the row `` `FM-17..FM-19` `` <sub>band = 3 ids</sub> with the multiplicity outside the backtick span, so a boundary-exact probe now matches the roster's own spelling. |
| **MID-29** | `:441`, §Y.3's residual-`-P` site list — **P5-8 / R4-7.3** | `the sites are **§0's PURGE-SEAT FIX · §2.X's re-written toolchain minute · §2.X.1's `SS-C-1` re-cut · §2.X.1's born-RED re-cut preamble · §4's predicate-hygiene clause · this declaration** — *a site that spells the dead flag joins this list, or the list is wrong.*` | ⟨cmd⟩ (`$W`) `/usr/bin/grep -nE 'grep -[a-z]*P\|-coP\|-ohP\|`-P`' F-W4.md \| cut -d: -f1 \| sort -un` → **L9 · L163 · L209 · L218 · L351 · L424 · L437 · L441** — **eight**. Add **§Y.1's own table row (L424)** and **§Y.2 item 1 (L437)**, and re-address `L163` to **§2.J** — the declaration currently names §2.X.1, and the spec's own §2.X.1 bullet says *"`SS-C-1` — **BOOKED at §2.J**"*. **The list's own falsifier fires on it.** |
| **MID-30** | `:9`, §0's residual-address declaration — **P5-17** | `every occurrence sits inside a STRUCK-address erratum at its own cure site (**§0 ×3, `G-F4-ANCHORS`, §5.1(1)**) or inside this declaration.` | ⟨cmd⟩ `/usr/bin/grep -nE 'F-W0\.md:199\|F-W0\.md:204\|F-W1\.md:7' F-W4.md \| cut -d: -f1 \| sort -un` → **L9 · L24 · L26 · L27 · L364 · L375 · L441** — **seven**, and L441 (§Y.3's closing sentence) is outside the enumeration. Add **§Y.3** to the site list. ⊘ Same class as MID-29 one level smaller: **§Y was added in round 4 and §0's declaration was never re-swept over it.** |
| **MID-31** | §2.X.2(a), fr-ConvergenceTimeline — **M-25 / P5-14** | `**The banked pair-spellings are restored beside every label**; the labels stay so the cure text does not fracture.` | ⟨cmd⟩ `/usr/bin/grep -noE 'CT-D[0-9]+[a-z]?' F-W4.md \| cut -d: -f1 \| sort -un` → **L81 · L82 · L84 · L122 · L297**; only L297 (the crosswalk) and L84 carry a banked spelling. **Restore the alias beside the head at L81 (`FR-CT-D4b⊕D-9`), L82 (`FR-CT-D3/C-2/D-1`, `FR-CT-D4/C-3/D-3`) and L122 (`CT-D14`)** — `L·D-3` · `L·D-4` · `L·D-14` respectively. **The alias rides the head at every cure site, not only in the crosswalk table.** |
| **MID-32** | §2.X.2(f)1, the record-prefix idiom | `**The prefix is what makes axis (iv) satisfiable — it is the record qualification, written into the token.**` | `FR-CP-` is **fr-CoefficientsPanel's BANKED prefix** — ⟨cmd⟩ (`$R`) `/usr/bin/grep -o 'FR-CP-' fr-CoefficientsPanel.md \| wc -l` → **92**, against `fr-ConvergencePlot.md` → **3** (cross-references only; that record banks `RD-*`, `SC-*`, `L-M*`, `L-B*`). Re-cut `FR-CP-LM1` · `FR-CP-LB3` · `FR-CP-D1` · `FR-CP-D9` · `FR-CP-C7` · `FR-CP-§5.1` to **fr-ConvergencePlot's banked bare ids with the record named in words**, and add the clause `⊘ **One prefix, two records: a boundary-exact probe over `FR-CP-*` cannot partition them, and the qualification named the wrong one.**` |
| **MID-33** | §2.X.2's masthead figure | `**F.W4 = 1014 rows across 54 records**` | **`F.W4 = 1012 rows across 54 records`** (errata E5-17: `PP-AGGLOM` → **NWO→SS-3**, `PP-SEVLAW` → **NWO (packet)**), ⊕ a citation row: `**`fr-PathPreview PP-AGGLOM` · `PP-SEVLAW` — CITED, NOT BOOKED.** Both were homed here on wave tokens their own arrows never name — `PP-SEVLAW`'s record says *"must not be inherited by **F.W4** as present grades"*, `PP-AGGLOM`'s says *"Intake MUST strike … or **F.W4** double-books"*. **The census captured a negation and a warning as routings; E5-17 made §0.1's arrow clause operative and both rows left.** F.W4 books neither and takes credit for neither. |
| **MID-34** | §2.X.2's pin triple | `⟨cmd⟩ `$W`: `shasum -a 256 ../conformance/CENSUS-CANONICAL.md ../conformance/PASS-4/RULINGS-4.md F-W3.md` → `a450b8e9f80e…` · `c899cdc2baad…` · `a89c3386f3f8…`` | Re-run and re-stamp: census → **`3e0a9acb3381`** (moved at errata round 5, §0.4), RULINGS-4 → `c899cdc2baad` (unmoved), F-W3 → `a89c3386f3f8` (unmoved). ⊘ **The pin did its job**: the operand moved and the mechanism says so instead of the wave inheriting it silently. |
| **MID-35** | `:366`, `G-F4-CONV-STUDY` | the gate rests **twice** on `FR-CP-LB3`, and §2.0's SP-16/SP-18 on `FR-CP-LM1`/`FR-CP-LM8` | Carry the banked ids beside them — `L-B3` · `L-M1` · `L-M8` — at the gate face and at SP-16/SP-18. ⊘ **A gate whose witness is a spelling the same spec convicts as un-citable at §2.X.2(a) cannot close**; MID-27's crosswalk supplies the ids and this edit lands them where the gate reads. |

### C.3 · `waves/F-W5.md` @ `132c03192176` (16 edits)

**The premise.** PASS-5 found this wave's census axis the strongest of the five passes — **118/118, booked 117 · cited 1 · escaped 0**, every landing real. Both HIGHs are machinery: six receipts consuming variables that are never assigned, and a "four sites" universal over twenty.

**MID-36 · declare `V` and `X` in-block (R4-2.3).** Anchor `:14` (where `R=` already lives) and `:352`.
*Current bytes*: `R=docs/tranches/V/megatranche/registry/adjudicated` (a real assignment — the R4-4.5 cure, correctly landed) and, at `:21`/`:352`, the PROSE `` `$V` = `/Users/mkbabb/Programming/value.js` ``.
*Replacement*: an executable block beside the first consumer, exactly as `R=` is: `V='/Users/mkbabb/Programming/value.js'` and `X='/Users/mkbabb/Programming/value.js/docs/tranches/X/fourier'` ⊕ `F='/Users/mkbabb/Programming/fourier-analysis'  # READ-ONLY, always`. ⟨cmd⟩ the seat's own falsifier: `/usr/bin/grep -nE '(^|[^A-Za-z0-9_])V=' F-W5.md` must return the assignment line — it currently returns **no output**. ⊘ **R4-2's cure was applied to one variable of three.**

| edit | anchor | current bytes | replacement |
|---|---|---|---|
| **MID-37** | `:66` — **G21's born-RED witness, a phantom in R4-2.3's exact sense** | `⟨cmd⟩ `ls $V/docs/tranches/X/fourier/contract/` → *No such file or directory*` | With `$V` unset the command prints `ls: /docs/tranches/X/fourier/contract/: No such file or directory` — **textually indistinguishable from the published paste, for the wrong reason.** Re-cut to the absolute form and re-run: `⟨cmd⟩ `ls /Users/mkbabb/Programming/value.js/docs/tranches/X/fourier/contract/` → *No such file or directory*` — **covers three**: `J-diff-shape-v2.md` · `operation-register.md` · `OWNER-RULINGS-F.W5.md`. |
| **MID-38** | `:67` — **G20's born-RED witness, which does NOT reproduce as written** | `⟨cmd⟩ `ls $V/docs/tranches/X/coordination/` → exactly one file, `ATLAS-TO-VALUE-2026-08-03-RULINGS.md`` | `⟨cmd⟩ `ls /Users/mkbabb/Programming/value.js/docs/tranches/X/coordination/` → exactly one file, `ATLAS-TO-VALUE-2026-08-03-RULINGS.md`` — re-run at the settle. ⊘ The file's own §1a calls this *"G20's own witness"*; **as published it fails on the pinned toolchain.** |
| **MID-39** | `:128` | `⟨cmd⟩ `sed -n '61p' $V/api/src/modules/palette/model.ts`` | `⟨cmd⟩ `/usr/bin/sed -n '61p' /Users/mkbabb/Programming/value.js/api/src/modules/palette/model.ts`` → `/** I.W1 canonical visibility (3-state): \`public\`/\`unlisted\`/\`private\`. */` |
| **MID-40** | `:205` (two commands) | `grep -rniE "merkle\|flat bag\|…" $V/api/src $V/src` and `ls $V/api/src/lib` | both absolutised to `/Users/mkbabb/Programming/value.js/api/src` · `/Users/mkbabb/Programming/value.js/src` and `/Users/mkbabb/Programming/value.js/api/src/lib`; outputs re-run and re-pasted (**one hit, `api/src/modules/palette/hash.ts:6`**; `api/src/lib` **absent**). |
| **MID-41** | `:341` | `⟨cmd⟩ `grep -cw 'P-9' "$X"/conformance/CENSUS-CANONICAL.md` → **0**` | `⟨cmd⟩ `/usr/bin/grep -cw 'P-9' /Users/mkbabb/Programming/value.js/docs/tranches/X/fourier/conformance/CENSUS-CANONICAL.md` → **0**` — **re-run against `3e0a9acb3381`, not inherited from `a450b8e9f80e`.** |
| **MID-42** | §2c's universal — **P5-7, HIGH** | `Four sites in this file carried a leg in a booking voice, and each is re-marked at its clause` | **Twenty, not four.** The four re-marked sites (D7 `fr-ContourSettings B-1`, D12 `M-10`, F5 `M-15`, D10 `fr-GalleryInfiniteGrid C-4/D-13`) are correct; seventeen further clause sites carry **twenty** canonically-elsewhere-held ids in the rows-folded (operand) column with **no LEG-CITED / HELD-AT mark**: A3 `fr-InfoCard FR-IC-6` (F.W4) · B5 `fr-HarmonicLevelGrid HLG-23` (F.W4) · C3 `fr-AppHeader FR-AH-6` (F.W4) · D1 `fr-GalleryDraftsSection F-6` (NWO→SS-3) · D10 `fr-GallerySearchBar FR-GSB-1` (F.W4) · D11 `fr-GalleryAdminBanner GAB-17` (F.W4) · D14 `fr-VisualizationView VV-R2-B` (F.W4) · D17 `fr-AdminFlaggedPanel FR-AFP-32` (F.W3) · E5 `fr-CanvasControlsDock D-4` (F.W4) · E5 `fr-GalleryDraftsSection B-2` (F.W3) · E6 `fr-VisualizationView VV-R2-A` (F.W4) · E13 `fr-ContourSettings m-18` (F.W3) · E18 `fr-AdminAuditLog AA-10` (F.W4) · F2 `fr-EquationView B-2` · `fr-FunctionInput L-B1` (F.W4 ×2) · F6 `fr-FunctionInput L-M3` (F.W4) · F7 `fr-ConvergencePlot L-M7 / C-8` (F.W4). **Each takes a `⟨LEG — held at F-W<n>⟩` mark at its own clause; the universal is restated as twenty.** ⊘ Not a roster escape — axis 1 still holds at 118/118 — but **twenty ids sitting on two waves' operand lists at once is the mirror hazard R4-6 was written to kill.** |
| **MID-43** | A6 and D16 — **P5-2 successor, the sharper class** | A6 books `fr-UserSlugBar FR-USB-37`; D16 books `fr-AdminAuditLog AA-24` | **Both are fabrications under R4-10's own clause**: canonical `` \| `FR-USB-37` \| `C-i2` \| `NO-WAVE-OWNER` \| **NWO (packet)** \| `` — **no F.W5 route at all**; `` \| `AA-24` \| `C·D-8` \| `F.W4` \| **F.W4** \| `` — **routes F.W4 only**. Convert both to citations naming the holder (F-W10's drain · F-W4 §2.A) and re-state §2c's disposition as `booked 115 · cited 3 · escaped 0`. |
| **MID-44** | `:111`, the collider block — **P5-8** | `⊕ **`R-7`** (FOUR identities, not three: `lane-crud §R-7` at C3/E18 · `GCM-3 R-7` at E6's repair-test lock · **`fr-GalleryInfiniteGrid R-7`**, now booked at D10 — pass-4 D-3's escape …)` | The list holds **three**. R4-7.3: the LIST is authoritative and the prose is corrected in place → `⊕ **`R-7`** (**three identities**: `lane-crud §R-7` at C3/E18 · `GCM-3 R-7` at E6's repair-test lock · **`fr-GalleryInfiniteGrid R-7`**, booked at D10 …)`. ⊘ **The same file contradicts the "four" at `:177`** (*"the token `R-7` is present three times in this file"*), and the emphatic *"not three"* makes it an assertion rather than a slip — **the P4-11 class re-committed inside the round-4 text that cures it two paragraphs above.** |
| **MID-45** | §2c's landing table — **P5-14** | `fr-ContourSettings i-7` → **B1**; `fr-ConvergencePlot K-13` → **F7** | Both registers refuse the landing. `i-7` appears in B1 only inside its **LOCK cell's** fold-by-reference list; its actual booking is **E13** (*"fr-ContourSettings B-4 = C-1 ∘ C-25 / R6-8 ⊕ **i-7** ⊕ m-18"*). F7's rows-folded column books `L-M7 + C-8 (ConvergencePlot)` — a canonically **F.W4** row (MID-42) — while `K-13`, the record's one canonical F.W5 row F7 is supposed to carry, appears only in F7's lock cell. **Re-key `i-7` → E13 and give `K-13` a rows-folded entry at F7.** ⊘ §2c calls itself *"the answer sheet"* to G19's *"question"*; an answer sheet pointing at a cell that does not book is unanswerable by the set-difference G19 states. |
| **MID-46** | `:111`, the collider roster — **P5-11** | `M-13 · L-B1 · L-M3 · C-17 · C-18 · B-1 · B-2 · C-2` ⊕ `M-10` ⊕ `M-9` ⊕ `C-7` ⊕ `C-3` ⊕ `R-7` ⊕ `R6-8` ⊕ record-local `K-n` | Add the **five further tokens that collide inside this wave's own 118** — the block's own stated ground is *"it escaped **because** this roster under-declared"*: `i-7` (`fr-ContourSettings` F.W5-W8 vs `fr-CollapsibleSection` NWO→SS-3) · `C:C-12` (`fr-ImageUpload` head vs `fr-HarmonicLevelGrid HLG-23`'s alias — **B5 prints exactly the fused form `HLG-23 (C:C-12)`**) · `C-6` (`fr-EditorControlsDock` head vs `L-M3`'s / `FR-AH-6`'s aliases vs `fr-BasisCanvas BC-9`'s companion) · `C-5` (`fr-BasisCanvas` head vs `L-M5`'s / `D-12`'s aliases) · `L-B2` (`fr-FourierShapeExtractor` head vs `FR-EMT-1`'s alias). |
| **MID-47** | `:266` — **R4-1** | `token-bounded `F.W5` = **245 occurrences on 240 lines across 54 records** (sole-`F.W5` = 245 − 192 = **53**).` — **no ⟨cmd⟩**, in the one paragraph whose siblings all carry theirs | Publish the instrument beside the figure: `⟨cmd⟩ (base `$R`) `/usr/bin/grep -row 'F\.W5' fr-*.md \| wc -l` → **245** · `-rw` → **240** · `-rlw` → **54**`. ⊘ **The figure is TRUE — a pass-5 seat reconstructed the instrument and got it exactly — so the defect is form, not fact**, and R4-1's opening law admits no exception for true figures. |
| **MID-48** | `:36`, §0's AUDITED cell | `**THE THREE RECEIPT BASES ARE DECLARED HERE, FILE-WIDE (repair round 4, R4-2.4 …)**` | **R4-2.4 does not carry that licence.** R4-2.4 is *"One command, one base"*; the variables item is **R4-2.3**, and it says the opposite of file-wide: *"Every shell variable a ⟨cmd⟩ consumes is DEFINED in the same published block."* Replace with `**THE RECEIPT BASES ARE ASSIGNED IN THE BLOCK THAT CONSUMES THEM (R4-2.3); the file-wide declaration is STRUCK — it was invented under a ruling number that does not carry it, and MID-36…MID-41 are what followed.**` |
| **MID-49** | `:260`, §2c's RHS definition — **P5-14** | `**RHS = this file's registers**: the §A–§G clause tables ⊕ §4's cross-edge citations ⊕ **§5's carried-by-citation table**.` | `:409` declares **ALL FOUR** §5 rows OUTSIDE the LHS, so §5 contributes nothing to covering the 118 and naming it as an RHS operand re-introduces the both-sides shape R3-4.5 killed. Replace with `**RHS = this file's registers**: the §A–§G clause tables ⊕ §4's cross-edge citations. **§5's carried-by-citation table is NOT an RHS operand — all four of its rows are outside the LHS by §5's own declaration**, and a register with no LHS member cannot cover one.` |
| **MID-50** | the `C:C-23` citation ⊕ every `F.W5-W8 = 90` figure | `§2's `F.W5` (28 rows) ⊕ `F.W5-W8` (90 rows) = 118 record-side rows` and the one CITED row `fr-PaperSearchDropdown C:C-23` cited to §4's F.W5→F.W2 edge | **Band 90 → 89; the citation re-based.** Canonical now `` \| `C:C-23` \| `C:C-24` \| — \| **UNROUTED** <sub>*errata R5 E5-15*</sub> \| `` — the only `F.W5-W8` on `fr-PaperSearchDropdown.md:77` is the record's own **`F.W5-W8 = no rows.`**, a load-bearing negative read as a positive routing. Replace with `§2's `F.W5` (28 rows) ⊕ `F.W5-W8` (**89 rows**) = **117** record-side rows` and re-cut the citation to `**CITED — canonical UNROUTED (E5-15).** An INFO row that FOLDS by reference into banked `fr-PaperSearch C-Z1/C-Z2`; the frontier is SS-3/SS-4's, not F.W5's.` **Disposition becomes booked 114 · cited 3 · escaped 0** after MID-43. |
| **MID-51** | A6 ⊕ C3, `fr-GalleryView FR-GV-7` — **P5-14** | A6: `FR-GV-7 (=M-9/C·M-9; cited at C3)` · C3: `FR-GV-7 (cited A6)` — a closed two-clause loop | Canonical `` \| `FR-GV-7` \| `M-9` · `C·M-9` \| `F.W3/W4` · `F.W5-W8` \| **F.W3** <sub>file-criterion → §5.d</sub> \| ``. R4-10's citation form requires the holder **by name**: replace both ends with `FR-GV-7 (=M-9/C·M-9) — **held at F-W3 §X.1-v5's register (canonical F.W3, leg F.W5-W8)**`. ⊘ **A citation whose referent is the other clause of the same spec resolves to nothing outside the file and is unfalsifiable by an id-keyed differ — pass-4's D-3 escape one level up.** |

**Batch MID total: 51 edits** (F-W2 19 · F-W4 16 · F-W5 16).

---

## §D BATCH-REST — `waves/F-W0 · F-W3 · F-W6 · F-W7 · F-W8 · F-W9 · F-W10.md` (72 edits)

### D.1 · `waves/F-W0.md` @ `282f0c120cd4` (8 edits)

**The premise.** F-W0's only spec-side HIGH was **D-4**: the file quoted, in its own §3 row 15, the record bytes that falsify the canonical row it deferred to in its own §3b, and filed neither an amendment nor a divergence minute. **The errata round has now ruled both rows in F-W0's favour** (E5-1, E5-2). The repair is to take the duty the file shed.

| edit | anchor | current bytes | replacement |
|---|---|---|---|
| **REST-01** | `:291`, §3b's citation table, `PP-GATE` — **P5-1 / P5-7 / E5-1** | `\| `PP-GATE` (fr-PathPreview) \| **UNROUTED** \| row 15's fold list ⟨likewise: the UNROUTED tier is the routing frontier owned by the forming SS-3/SS-4 specs, not an F.W0 obligation⟩ \|` | **CITATION → BOOKING.** Canonical now `` \| `PP-GATE` \| `D-1` (gate half) · `C-10` · `L-3` \| `F.W0` \| **F.W0** <sub>*errata R5 E5-1*</sub> \| ``. Move the row **out of the citation table and into §3b's booking table**, keyed to §3 row 15, with the record's own banked line by ⟨cmd⟩: `/usr/bin/sed -n '49p' fr-PathPreview.md` → `14. **PP-GATE** (D-1 gate half · C-10 · L-3) — **MINOR · FOLD → F.W0** (banked: fr-GalleryFeaturedCarousel.md:56 FR-GFC-22 \`noUnusedLocals\` rider).` ⊘ **The unmeasured editorial gloss that shed it is struck**: every neighbouring cell in that table carried a home receipt and this one carried an argument — had the receipt been run at authoring, D-1 would have surfaced then. |
| **REST-02** | `:290`, §3b's citation table, `PP-DEAD` — **E5-2** | `\| `PP-DEAD` (fr-PathPreview) \| **TERMINAL (∅)** \| row 15's fold list ⟨round 3 counted this as an F.W0 escape recovered; the canonical rules it TERMINAL, so it is a census fact, not a wave debt⟩ \|` | Canonical now `` \| `PP-DEAD` \| — \| `F.W3/W4` · `F.W0` \| **F.W3** <sub>file-criterion → §5.d</sub> <sub>legs: F.W0</sub> \| ``. Replace with `\| `PP-DEAD` (fr-PathPreview) \| **F.W3** <sub>legs: F.W0</sub> \| **CITED to the holder, F-W3's register (R4-10)** — and the `noUnusedLocals` rider is F.W0's leg, carried at row 15. ⟨*errata R5 E5-2: the routing column read `*DEAD*`, a token that occurs nowhere on `fr-PathPreview.md:30`; this file quoted the falsifying bytes in its own §3 row 15 for two rounds and filed nothing. §0's disagree-or-amend clause is exercised at last — by the census, on this file's evidence.*⟩ \|` |
| **REST-03** | six roster-figure sites | `56 rows` / `F.W0 → 56 rows` at `:62` · `:111` · `:132` · `:225` · `:495`, and `**The roster lands WHOLE: 56 booked, 0 escaped, 0 fabricated**` at `:495` | **`57` at all six**, with the errata cited once at `:225`'s operand paragraph: `⟨*re-based at repair round 5: `CENSUS-CANONICAL.md` §0.4 **E5-1** homes `PP-GATE` here; §2's header now reads `### F.W0 — **57 rows**`*⟩`, and `:495` → `**The roster lands WHOLE: 57 booked, 0 escaped, 0 fabricated**`. |
| **REST-04** | `:55`, §1's hard-gate paragraph — **P5-12 / R4-3** | two ⟨cmd⟩s taking `conformance/PASS-1/F-W0-CHECK.md` as their operand: `grep -n '15 of 15' conformance/PASS-1/F-W0-CHECK.md` and the three-row qualifier grep | **A check file is never an operand (R3-4, restated program-wide by R4-3 with no denominator qualifier).** Both receipts reproduce; the defect is legal, not factual, and `CLOSE-CERT-2` §2 Class E cleared them on a narrowing (*"attestation quotes, not rosters"*) **the ruling's text does not contain** — R4-2.5 forbids the charitable reading. Replace with the class claim in F.W0's own voice: `**All fifteen gates were re-run RED by two independent adversarial seats at pass 1 and pass 2; both runs are named as PRIOR RUNS and neither is quoted as an operand here.** The gloss *"every clause EXACT"* stays STRUCK (round-2 D-3): three of the fifteen carried qualifiers at pass 1, and the qualification is recorded, not measured, in this file.` **Delete both ⟨cmd⟩s.** |
| **REST-05** | `:265-292`, §3b's second table — **D-8** | the citation table, exhaustive over *"ids this spec's rows touch that the canonical homes ELSEWHERE"* | Add the missing row: `\| `FR-COB-24` (fr-CanvasOverlayButton) \| **F.W1** \| carried in §3 row 33's HEAD (`FR-AUL-58 ≡ FR-COB-24 (= D-28 / D-14)`); **CITED to F-W1** — discharged until now only by §8's exclusion row, which names the wave but not the holder \|` ⊕ re-verify the table's exhaustiveness claim against the **cured** canonical, striking `fr-CanvasOverlayButton C-3` and `L-9` if either appears (both **STRUCK as rows** at E5-3: the record's own alias cells claim them for `FR-COB-3` and `FR-COB-15`). |
| **REST-06** | `:425` and `:427`, §6a locks 5 and 7 — **P5-15** | lock 5 keys on *"rows 8/9/10/11/12/14/34/35"*; lock 7 reads `**Row 16 is a rider on M-10, never landed alone**` | Key both to banked ids. Lock 7 → `**Row 16 — `fr-BasisSelector i-3` — is a rider on `M-10`, never landed alone**`, carrying the WAVE-LOCK unchanged. Lock 5 → the eight rows' banked ids spelled beside their numbers. ⊘ Row numbers resolve under R2-2.5 so nothing breaks, but **a lock that does not name its id is the shape that let `R-7`, `R-4` and `L-2` sit un-keyed for three rounds** in this very file. |
| **REST-07** | `:225`, §3b's operand paragraph | `**The operand, named once.** `docs/tranches/X/fourier/conformance/CENSUS-CANONICAL.md`, **§2 → `### F.W0 — 56 rows`**, stamped 2026-08-29.` | Re-stamp with the moved digest and the errata pointer: `**§2 → `### F.W0 — 57 rows`**, sha256 **`3e0a9acb3381`**, stamped 2026-08-29, **errata round 5 §0.4 applied**. ⊘ *A stamped operand no wave may re-cut is only as good as its derivation; a six-record spot audit from this seat put two rows in the defect column and the amendment round sustained both.* |
| **REST-08** | §3b's 30-citation table, whole | thirty citations each reproducing the canonical's home **and legs** | **Re-difference all thirty against `3e0a9acb3381`** and correct the rows the errata moved. Expect at minimum: `PP-DEAD` (REST-02) · `FR-COB-24` (REST-05) · any row citing `fr-CanvasOverlayButton C-3`/`L-9` (struck) · any row citing `fr-ImageUpload D:M-3` (F.W1 → F.W3). **Verify by join, never by eye**; the table is §3b's whole warrant. |

### D.2 · `waves/F-W3.md` @ `a89c3386f3f8` (6 edits)

**The premise.** 926/926 booked, 0 cited, 0 escaped, 0 fabricated, byte-identical to the operand — and **the operand moved by two.** Both spec-side convictions are stale-or-false receipts over live siblings, the class this wave itself diagnosed at `:618` and then left standing four sections earlier.

| edit | anchor | current bytes | replacement |
|---|---|---|---|
| **REST-09** | `:783`, §X.1-v5 item 5 — **P5-6, HIGH** | `**F.W4's 1014 rows are held at `waves/F-W4.md` + `carry/F-W4-CARRY.md`** (sha256-at-quote-time **`9a6cf2b11fc6`** and `bf1da6fe1e7e`)` | ⟨cmd⟩ `shasum -a 256 waves/F-W4.md carry/F-W4-CARRY.md \| awk '{print substr($1,1,12), $2}'` → **`39e1a60b3fc9`** · `bf1da6fe1e7e`. **The published pin is false at the certified settle** — and `39e1a60b3fc9` is also CLOSE-CERT-2 §8's own line for the file, so two certified artefacts contradicted each other inside one round. Cause is dated and mechanical: `F-W3.md` last written 16:49, `F-W4.md` 18:07 — **the sibling was re-written 78 minutes after F-W3 fingerprinted it, the R4-8.3 rider fired, and no seat re-pinned it.** ⊘ **Preferred cure, because F-W4 will move again**: replace the fingerprint with the word-output anchor probe this file already uses successfully — `⟨cmd⟩ `/usr/bin/grep -c '^### §2.X.2' waves/F-W4.md` → **1**` — which survives the next edit of the file it describes. **This wave struck its own `L-26` control at `:618` for exactly this reason and wrote the finding down.** |
| **REST-10** | `:783`, the same sentence's sibling roster | `` `F.W0`'s 56, `F.W1`'s 362, `F.W2`'s 31, `F.W5`'s 28, the `F.W5-W8` band's 90 and `F.W9`'s 17 are held at their own specs `` | Re-base on `3e0a9acb3381`: `` `F.W0`'s **57**, `F.W1`'s **358**, `F.W2`'s **27**, `F.W5`'s 28, the `F.W5-W8` band's **89** and `F.W9`'s **16** `` ⊕ `F.W4`'s **1012** at the head of the same sentence ⟨*errata round 5, §0.4 — E5-1 · E5-3 · E5-5 · E5-6 · E5-7 · E5-9 · E5-13 · E5-15 · E5-16 · E5-17*⟩, and the canonical's own pin re-stamped `a450b8e9f80e` → **`3e0a9acb3381`** at all five citing sites. |
| **REST-11** | `:559`, §7's set-receipt — **P5-5, false at write** | `→ `conformance/PASS-3/F-W3-CHECK.md` · `conformance/PASS-1/RULINGS.md` — **a check file and a rulings file, and NOTHING ELSE; the sentence is absent from all 66 corpus records**` | Re-run from the stated base (`waves/`) returns **three**: `../conformance/PASS-3/F-W3-CHECK.md` · **`../conformance/PASS-4/F-W3-CHECK.md`** · `../conformance/PASS-1/RULINGS.md`. **Not stale — false when written**: `PASS-4/F-W3-CHECK.md` was created 15:21 and `F-W3.md` last written 16:49, 88 minutes later. **Strike the enumeration and the universal**; restate as the class claim the cell actually needs: `**Every home of this sentence is a check file or a rulings file, both non-quotable by name (R2-1-LAW 3 / R3-4); the sentence is absent from all 66 corpus records** — ⟨cmd⟩ (base `$R`) `/usr/bin/grep -rl 'RED BY CONSTRUCTION at the installed' .` → **∅**, the one operand here that is FROZEN.` ⊘ R4-1.9 verbatim: *"an attestation-of-sweep must list its residue or not exist"* — and **a `-rl` over `conformance/` is a sweep of a directory that grows by one file per wave per round, so "NOTHING ELSE" is unwarrantable there by construction.** Secondary drift in the same paste: the `../` prefix the stated base produces is dropped and the members are reordered. |
| **REST-12** | `:378`, §2's compound cure head — **D-3** | `- **fr-PathPreview PP-DEAD / PP-SHAPE / PP-CLOSE / PP-STROKE / PP-LEN / PP-A11Y / PP-NOSHADOW** — MAJOR/MINOR.` | Read through canonical §0.1's ALIAS clause that head banks `PP-DEAD` with six aliases, four of which (`PP-SHAPE` · `PP-LEN` · `PP-A11Y` · `PP-NOSHADOW`) are standalone F.W3 identities in this file's own register. **Add one clause at §2's head** — `⊘ **In cure-cluster heads the `/` is a ROW SEPARATOR, never the ALIAS separator canonical §0.1 gives it** (§0.1 already licenses the third reading for top-level ` · `); a pass-6 seat atomising §2 with §0.1 in hand must not read these as aliases.` — **or** re-spell the compound heads with the ` · ` form. **No booking error exists**; the defect is legibility under the operand's own rule, in the one file that declares that operand supreme. |
| **REST-13** | `:412`, the g19 cell — **D-4 / P5-18** | `boundary-exact `(?<![A-Za-z0-9-])ID(?![A-Za-z0-9-])` thereafter` | Add one parenthetical: `⟨*Python-regex form; the detector was `python3`, never the pinned shell — the BSD toolchain never sees this string, and it sits inside the ⌧ struck-history region with no gate weight*⟩`. ⊘ Filed because **R4-2.5 is written to be applied without charity**; the next seat scanning for `(?<` hits this and must otherwise re-derive the exemption from scratch, as the pass-5 seat did. **One parenthetical closes the axis permanently.** |
| **REST-14** | `:708-775`, §X.1-v5 item 4's register, and `:17`/`:23`/`:26`'s duty quotes | the 59-line register, **byte-identical** to the old canonical's §2 `F.W3` roster (⟨cmd⟩ `diff canon.txt register.txt` → exit 0), summing 926 | **Re-paste from `3e0a9acb3381`: 928 rows across 59 records.** Two rows arrive: `fr-PathPreview` gains **`PP-DEAD`** (E5-2, home F.W3 <sub>§5.d</sub> leg F.W0 — its `noUnusedLocals` rider is F-W0's, cited not booked) and `fr-ImageUpload` gains **`D:M-3`** (E5-5, home F.W3 <sub>§5.d</sub> — the record's only arrow is `→ **F.W3/W4**` and its lone `F.W1` token sat inside the express negation *"NOT an F.W1 item (K-12)"*). Re-run the self-check `awk` → `records 59 declared 928 enumerated 928`; re-base `:17`'s quoted duty row and `:23`/`:26`'s figures with a dated errata note. ⊘ **g19's `comm -3` must return 0 rows in both columns after the re-paste, and that run is the gate — not this instruction.** |

### D.3 · `waves/F-W6.md` @ `c8c6d1d7f136` (17 edits)

**The premise.** Zero fabricated quotations were found — every quoted word reproduced at its source, and the born-RED witness set re-ran green-as-RED against the live fourier tree. What convicts is three bookings the canonical homes elsewhere, and eleven receipts that publish a paste their own command does not print.

| edit | anchor | current bytes | replacement |
|---|---|---|---|
| **REST-15** | `:221`, §2.7 — **P5-2, the worst of the three** | `\| **fr-EquationModeToggle FR-EMT-20** — **BOOKED THIS ROUND (R3-7.1); census escape E-3** \| MAJOR (dead wire fields) \| …` ⊕ its rows at `:67` · `:152` · `:376` · `:395` · `:587` · `:616` | **BOOKED → CITED at all six sites.** Canonical `` \| `FR-EMT-20` \| `R2` · `missed-5` \| `F.W5` \| **F.W5** \| `` — **F.W5 with NO band leg at all**; the record routes `F.W5` alone (`fr-EquationModeToggle.md:50`). Replace the verb with `**CITED — held at F-W5 §2's `E10` clause (canonical F.W5, no band leg).** The E10 family's server strike that §2.7 executes is the MECHANISM; the identity is F.W5's.` ⊘ **The file convicts itself in its own hand**: §2.11d states *"Every one of the fifteen is **held at F.W5 by the canonical**, so under R4-10's roster law F.W6's duty is to **CITE THE HOLDER BY NAME** — never to book, never to count, and never to route"*, and a sixteenth F.W5-held id is booked four sections above the cell that forbids it. **The booking was ordered by round 3's own R3-7.1 before the canonical existed; R4-10 postdates and governs, so the cure is a one-line conversion and not a substantive error.** |
| **REST-16** | `:206`, §2.5 — **P5-2** | `\| **AA-10** (⊕ **AA-5** server arm; AA-6/AA-24 cited) \| MAJOR ×2 \| …` ⊕ `:393` · `:404` · `:558` · `:576` | **BOOKED → CITED.** Canonical `` \| `AA-10` \| `D-M2` · `L-13` \| `F.W4` · `F.W5–W8` \| **F.W4** <sub>legs: F.W5-W8</sub> \| ``; the record names the primary itself — `:47` *"Display cure (sentinel-aware branch + legend) **→ F.W4**; the actor-field contract question **→ F.W5–W8**."* R4-6.1: *a leg books as ONE row at the wave that HOLDS the host id.* Replace with `**CITED — held at F-W4 §2.A; the F.W5-W8 leg (the actor-field contract question) is what this wave carries, and it carries it under the holder's id.**` |
| **REST-17** | `:189`, §2.3 — **P5-2, compounded** | `\| **fr-GalleryDraftsSection B-2** ‡ (⊕ **FR-GV-1** FOLD; …) — **record-qualified per R-5** \| BLOCKER \| …` ⊕ `:391` · `:425` · `:452` | **BOOKED → CITED, at BLOCKER weight.** Canonical `` \| `B-2` \| `C-C-1` · `L-3` \| `F.W3/W4` · `F.W5-W8` \| **F.W3** <sub>file-criterion → §5.d</sub> <sub>legs: F.W5-W8</sub> \| ``. **Compounded**: `fr-GalleryView FR-GV-1`, also canonical **F.W3**, is folded INTO it at BLOCK C. Replace with `**CITED — both identities held at F-W3 (canonical F.W3, file-criterion §5.d, legs F.W5-W8).** F.W6 executes the counter/identity/idempotency cure at the band and books neither id; the BLOCKER severity is preserved as the banked grade, cited not re-graded.` |
| **REST-18** | `:67` and `:108`, the wave's own count | `**25 identities cut to this wave** at §2.1–§2.10 (24 at round 1–2 ⊕ `fr-EquationModeToggle FR-EMT-20`, booked at §2.7 this round under R3-7.1)` and `## §2 Carry — 25 identities, agglomerated by mechanism` | **22 booked ⊕ 3 cited.** After REST-15/16/17: `**22 identities BOOKED** at §2.1–§2.10 ⊕ **three CITED to their canonical holders** (`FR-EMT-20` → F-W5 · `AA-10` → F-W4 · `B-2` ⊕ `FR-GV-1` → F-W3) ⊕ four non-record commissioned acts (`F-α` · `F-β` · `F-γ` ⊕ the value-side quartet routed OUT at branch 1), which are outside the roster law.` **The arithmetic 3+2+2+6+1+3+2+1+1+4 = 25 is re-summed in the same edit.** |
| **REST-19** | `:468` / FW6-G17's falsifier (a) — **P5-13 / R4-10** | the gate cell holds the R4-10 addition (*"a read of the **canonical §2 rosters**"*) **and** the retained four-spelling corpus detector (*"**forward** — walk every span line in the 66 frozen records under the four-spelling detector back to its enclosing row id (S-8)…"*) side by side, declaring **UPPER = "the ENUMERATION this file publishes"** | **Retire the detector as a co-equal falsifier.** R4-10: *"No wave derives, samples, or re-cuts a roster from any other source — not a check file, not a prior pass, not its own §-arithmetic."* Replacement: `**UPPER = `CENSUS-CANONICAL.md` §2's rosters, and nothing else.** The four-spelling corpus detector is retained **as dated round-3 provenance, struck as an operand** ⟨*it is the machinery that produced the `FR-EMT-20` booking REST-15 converts; round 4 retired this file's derived census and left the derivation installed in the gate that was supposed to retire it*⟩.` |
| **REST-20** | `:63` and `:549`, the R-5 collision list — **P5-4/P5-11, HIGH** | `Eight banked tokens collide across records (UNION U-12): **`M-13` · `L-B1` · `L-M3` · `C-17` · `C-18` · `B-1` · `B-2` · `C-2`**` | **At least ten.** Add **`M-10`** — 18 hits in this file across `fr-ContourSettings M-10` (F.W3) · `fr-BasisSelector M-10` (F.W3) · the `fr-FourierShapeExtractor` innerPoly **rider** inside banked head `D-16` (F.W4) — and **`M-9`** — 10 hits across `fr-BasisSelector M-9` ⊙ (row 7) · `fr-FourierShapeExtractor M-9` (row 36) · `fr-GalleryView FR-GV-7`'s `M-9` alias (row 51). ⊘ **The file documents both collisions and declines to act on them** (row 36 carries an explicit collision guard), and FW6-G17's reverse falsifier — *"grep **its banked id token** … the token, not the record-qualified spelling"* — **is unrunnable at both**: a reverse probe on either returns a set spanning three identities across two or three canonical waves. |
| **REST-21** | `:96`-region, §2.6's M-10 anchor — **P5-5, HIGH** | `⟨cmd⟩ `grep -nF '**M-10 bound in, seat-verified by read of the RNG interleave' fr-FourierShapeExtractor.md` → **one hit, `:96`**` followed by the M-10 sentence alone | The command prints line 96 **entire** (~200 chars), opening `96:- **D-16 / C-9 / L-m2-arm (+ M-10 rider, reader-2's find — REPAIR-CRITICAL)** — dead seams and stale prose …` — **including a different banked head, `D-16 / C-9 / L-m2-arm`, which the canonical homes at F.W4**, dropped before the paste with no cut mark. **Paste the line whole, or mark the cut and name the head.** ⊕ name the banked head at all five carrying sites: `**`fr-FourierShapeExtractor D-16 (≡ C-9 · L-m2-arm)` — canonical F.W4** — the innerPoly anti-cure rider `M-10` rides it and is not an identity of its own.` ⊘ **Round 4's own P4-D7 cure created this**: it replaced a 3-hit `grep -n "innerPoly"` with a `-F` anchor returning one line and then quoted a substring of that line. *A single-hit anchor is not a single-quote anchor.* |
| **REST-22** | `:` §2.11 row 19 — **P5-10** | `⟨cmd⟩ `grep -n "Server half" fr-EasingPicker.md`` published with a mid-line substring paste | The command returns line 65 whole — a ~700-character table row opening `65:\| **L/M-3 (decides C/i-1)** \| No unknown-value posture over a persisted domain…` — of which ~600 characters are dropped undisclosed. **Narrow the command to the span, or disclose the cut with an ellipsis and the line's opening head.** The words are the record's; the receipt is not the command's output. |
| **REST-23** | rows 46 · 52 · 50/§4 lock 9 · §2.3 — **P5-10** | four `-o` bounds whose output over-runs the paste into the following table cell: `.{0,80}` → `…see FR-GFC-4). \|` · `.{0,90}` → `…is canonical) \|` · `.{0,190}` → `…fourier-series}\`. \| Both re` · `.{0,120}` → `…(CRUD/provenance union). Riders: m-13 (` | Re-cut each bound so the printed window ends where the paste ends, **or** paste the full window including the cell boundary. ⊘ **This is the exact class R4-7.1 reported cured at six sites, unfixed at four others** — each paste stops one cell-boundary short, unmarked. |
| **REST-24** | §2.11a's attestation — **P5-8 / P5-10** | `all seven reproduce, in order, with the cells below` | **False at three of seven.** The loop prints a ~90-character window; `fr-App`'s cell exceeds it at **both** ends, `fr-MorphShapePreview`'s cell **prepends** the `preser` the `.{0,10}` prefix cut and drops the closing `.`, `fr-MobileFloatingToc`'s continues past the window after its ellipsis. Replace with `**all seven cells are verbatim in their records** — ⟨cmd⟩ `grep -cF '<cell>' <record>` → **1** at each — **and the loop's window does not coincide with three of them; the cells are the claim, the loop is a locator.**` ⊘ Receipt-vs-paste, never fabrication. **Round 3's Q-7 cure completed one cell of seven and declared the label exact.** |
| **REST-25** | `:40-56`, §0.3's hash-pin table — **P5-6** | `waves/F-W5.md` `d4f47eb3ec4f` · `waves/F-W0.md` `cd64d6580098` · `waves/F-W1.md` `839d9964010b` · `waves/F-W9.md` `eda1fab4e865` · `../COHESION.md` `91c6d974db9b` · `../conformance/CENSUS-CANONICAL.md` `a450b8e9f80e` | **Five of eight stale; re-stamp by the purge seat that runs LAST.** F-W5 → **`132c03192176`** · F-W0 → **`282f0c120cd4`** · F-W1 → **`a1302689aaa3`** · F-W9 → **`e9a9c3016f4c`** · COHESION → re-measure · census → **`3e0a9acb3381`** with the errata noted. ⊕ **issue the R4-8.2 certificate that does not exist**: no purge-seat sha256 cert has ever been written for this wave, so LAW E's authoritative stamp is still owed. ⊘ Record the mitigation: **thirteen live-sibling receipts reproduced despite five pins having moved — the pin did its job; movement was detected, not silent.** |
| **REST-26** | BLOCK C's four fold-cell landings — **P5-11 / P5-14** | `FR-AFP-32` → §2.4's D17 · `GCM-10` → *"→ F.W5 A3 + D17"* (row 45) · `fr-ContourSettings m-18` → *"rider at §2.6 B-4"* · `FR-GFC-20` → *"burned WITH FW6-G10"* | Each cell assigns a landing the canonical contradicts: `FR-AFP-32` → **F.W3** (legs F.W5-W8, SS-13) · `GCM-10` → **F.W3** (file-criterion §5.e) · `m-18` → **F.W3** (legs F.W5-W8) · `FR-GFC-20` → **F.W4**. **Add the canonical home to each cell beside the landing** — the landing is where the cure runs, the home is where the identity lives, and R4-10 requires the second to be named. |
| **REST-27** | BLOCK C's identity equation — **P5-13** | `FR-AFP-49 = AA-44 = FR-AUL-16` as one identity | The canonical homes them at **three different places**: `` \| `FR-AFP-49` … \| **F.W9** <sub>legs: SS-3, SS-4, F.W9/W10</sub> \| `` · `AA-44` → **F.W9** · `` \| `FR-AUL-16` \| — \| `NO-WAVE-OWNER` · `SS-3` · `SS-4` \| **NWO→SS-3** \| ``. Replace the equation with `**one MECHANISM, three banked identities, two homes** — `FR-AFP-49` ⊕ `AA-44` held at F-W9; `FR-AUL-16` at the NWO→SS-3 packet, disposed at F-W10's drain. **Never collapsed into one token.** |
| **REST-28** | row 54 — **P5-19** | `fr-VisualizationView BLK-1` landed *"→ F.W5 E4 / F.W4"* | Canonical `` \| `BLK-1` \| — \| `F.W4` · `F.W5–W8` \| **F.W4** <sub>legs: F.W5-W8</sub> \| `` — **the canonical knows no F.W5 home for it.** Re-cut to `→ **F.W4** (held) / the F.W5-W8 band leg`. |
| **REST-29** | §2.1's census-dissent receipt — **P5-10** | `sed -n '71,72p' CENSUS-2026-08-03.md` published with ~25 chars dropped before the paste and a whole trailing sentence after it, both unmarked | Paste both lines whole — `into a stalled M — i.e. the half-built chain is *known* unfinished work on their side, not an` / `overlooked bug. Strengthens the finding and re-homes it as formation work, not blame.` — **in the cell whose own round-2b erratum convicts the narrower `71p` bound.** |
| **REST-30** | §4's F.W7 edge — **P5-17** | the round-4 preservation list claims *"**F.W7's ∅ posture**"* preserved; ⟨cmd⟩ `grep -noE '.{0,45}∅.{0,45}' F-W6.md` → **3 hits, none a live statement of it** (`:379` a grep exit code, `:611`/`:624` the two preservation lists) | **State it once, live**, at §4's F.W7 edge: `**F.W7's routing census is a measured ∅ and that ∅ is a finding, not a gap** — corroborated at the canonical's §4.1 (`F.W6`, `F.W8` and `F.W10` are likewise named by **no record**). F.W6 asserts nothing against it and adds no row to it.` ⊘ **A posture preserved that was never stated is a preservation of nothing.** |
| **REST-31** | §2.0's `comm -13` paste · §2.5's Q-1 receipt · three `grep -n` pastes — **P5-14/15/16** | the `comm -13` cell drops the `.md` suffix the command prints on five filenames; Q-1's paste omits the path prefix its own `-rn` form prints; three `grep -n` receipts drop the `NN:- ` list-marker prefix | **Restore what each command prints, byte for byte**: five filenames with `.md`; `<path>:197:booking); strengthens the census routing, no new work.`; `73:- **m-7 = C-6-as-rescoped…`, `64:- **L·M-4 / D-13 / C-8(a)…`, `54:- **FR-GFC-20 = C-9 — MINOR.**…`. ⊘ One edit, five sites — **the class is "the paste is not the command's output", and it does not close site by site.** |

### D.4 · `waves/F-W7.md` @ `5cc3346db23e` (9 edits)

**The premise.** Roster 0 / booked 0 / cited 0 / escaped 0 — **the ∅ posture is sound at the operand and the census axis passes outright.** Fifty-nine of sixty receipts reproduce. What convicts is three cures declared complete that are complete at some of their sites and not all.

| edit | anchor | current bytes | replacement |
|---|---|---|---|
| **REST-32** | §1b · §5d · §6 `G-F7-8`'s operand list · §8's exclusion row (`:301`) — **P5-2 successor, HIGH, four sites** | `plus F.W4's ImageUpload roster 12/23` · `homes **F.W5 §C2** (m-15) / **F.W4** (12, 23)` · `+ F.W4's ImageUpload roster 12/23` · `Homes are **F.W4** and **F.W5 §C2**` | **The census of record homes both ids at F.W3.** ⟨cmd⟩ `/usr/bin/awk '/^### F\.W3 —/,/^### F\.W4 —/' CENSUS-CANONICAL.md \| /usr/bin/grep '^- \*\*fr-ImageUpload\*\*'` → the 19-id F.W3 line carrying **`L:L-M3`** and **`R2-missed-3`**; per-record `` \| `L:L-M3` \| `C:C-6` \| `F.W3/W4` \| **F.W3** <sub>file-criterion → §5.d</sub> \| `` and `` \| `R2-missed-3` \| — \| `F.W3/W4` \| **F.W3** <sub>file-criterion → §5.d</sub> \| ``. **Replace `F.W4` with `F.W3` at all four sites**; `K-8` is `` **TERMINAL (∅)** `` and is named as such. ⊘ **Nothing books either way — F.W7's roster is zero and the wave takes no credit — so no count moves. What moves is where a reader is sent, in the table whose whole declared function is that absence cannot be mistaken for oversight.** The sharpest form: **§8 is the same table in which round 4's D-11 re-based `L-5` onto the canonical — the cure was applied to the row above and not to the row below it.** |
| **REST-33** | `:365`-region, §12's fourth failure mode — **P5-5, HIGH** | `**Every receipt in this spec is now one of the surviving forms, and the one place a numeral remains — the frozen 66-record corpus — is the one operand in the program that cannot move.**` | **False in the round that wrote it.** §8's `L-5` erratum banks, in the same round, `grep -n 'ContourEditorCanvas' waves/F-W4.md \| grep -c 'L-5'` → **0** and the same against `F-W3.md` → **0** — **two numerals over two LIVE wave specs**, precisely the R3-3.10 form §12 declares extinct. Both re-run to **0** at this seat: *the measurement reproduces; the attestation does not.* Replace with `**Every receipt in this spec is one of the surviving forms with TWO disclosed residues — §8's `L-5` erratum banks two `grep -c` counts over live siblings (`waves/F-W4.md`, `waves/F-W3.md`), both re-run 0 at the pass-5 settle and both re-cut to set-membership at repair round 5.**` ⊘ R4-1.9 / PASS-4 D-4 — *"an attestation of sweep must publish its residue or not exist"* — **recommitted one section later in the same file, in the same round, by the same cure.** |
| **REST-34** | §0 item 6 (`:16`) · §4's erratum · §5a's P-10 row — **P5-4, HIGH, three sites** | three receipts taking a bare `docs/` operand: `grep -rl "trie ruling stays inline" docs/` · `grep -rn … docs/` ×2 | §0(B) declares the base map *"binding on every ⟨cmd⟩ in this file"* and enumerates **five cwds, none of which is the repository root**; `docs/` resolves only from `$V`. From the map's own base the command prints `grep: docs/: No such file or directory` and **exits 2**. **Re-cut all three to a declared base**: `⟨cmd⟩ (base `$V` = `/Users/mkbabb/Programming/value.js`, **added to §0(B)'s map in this same edit**) `/usr/bin/grep -rl "trie ruling stays inline" docs/` → `docs/tranches/X/fourier/waves/F-W7.md` **and no other file in the tree**`. ⊕ **Rider**: two of the three still carry the `grep -rn` address form **§0 item 6's own round-3 cure retired** (*"stated as a file-list and a classification rather than as `grep -rn`'s addresses, which move whenever a strike note is edited"*) — re-cut those two to the file-list form. ⊘ **The exactness of the recurrence is the point**: §0(B)'s own ⟨CC⟩ paragraph is titled *"The path that resolved nowhere"* and cures `../keyframes/…` while three receipts on the facing page resolve nowhere under the map that paragraph publishes. `CLOSE-CERT-2` Class A's `F-W7 \| 182 \| 0 \| — \| CLEAN` is falsified here. |
| **REST-35** | §8's `L-5` row — **P5-8** | `` `ContourEditorCanvas.vue` is one of the partition's **four negatives** `` | The canonical enumerates **five** — ⟨cmd⟩ `/usr/bin/grep -c 'criterion negative → F.W4' CENSUS-CANONICAL.md` → **5**; `… \| grep -oE 'fr-[A-Za-z]+'` → `fr-BasisCanvas` · `fr-ContourEditorCanvas` · `fr-EquationResult` · `fr-FourierMorphSvg` · `fr-GalleryAdminBanner`. Replace with `**one of the partition's five criterion negatives** (⟨cmd⟩ above, naming all five)`. ⊘ **Two defects in one**: a count word against the census of record, **and a retirement announced and not executed** — §12 of this same file, same round, declares *"the pass-3 count word 'four negatives' is **retired for membership**"*. |
| **REST-36** | `:134`-region, §2a's `find . -type d` — **P5-5** | an eight-line block published as *"The live output, pasted whole"* | Re-run at the pass-5 settle returns **nine** lines — `./conformance/PASS-5` is absent from the paste. **Take the cure §2a already names and then declines**: publish the CLASSIFICATION and drop the enumeration — `⟨cmd⟩ (base `$X`) `find . -type d \| /usr/bin/grep 'contract\|design'` → **no output, exit 1** — the absence probe every gate actually rests on, over a tree that grows one directory per pass.` ⊘ **Third generation of one receipt**: round 2 struck it for understating, round 3 replaced it with a differently-wrong list, round 4 re-listed it — **against the file's own argument written two sentences below the block.** |
| **REST-37** | §5d's banked-id cell ⊕ §8's exclusion row — **P5-2 successor** | `homes **F.W5 §C2** (m-15)` | Canonical `` \| `m-15` \| `r2-missed-3` \| `F.W5-W8` \| **F.W5-W8** \| `` and §2's band roster carries `- **fr-GalleryDraftsSection** (2): \`F-4\` · \`m-15\``. **R4-5 makes F.W5 (28) and F.W5-W8 (89) two distinct canonical denominators.** Replace with `held at the **`F.W5-W8` band** (canonical), whose wave-side duty R4-10 assigns to **F-W5**, where the cure sits at clause **§C2**` — naming both, in that order. ⊘ Softer than REST-32 only because F-W5 §2's `§C2` genuinely holds the cure and this file states the band correctly elsewhere (*"the band denominator `F.W5–W8` is **F.W5's** row, not F.W7's"*); **the two sentences did not agree with each other.** |
| **REST-38** | `:48-60`, §0(C)'s hash pin — **P5-6** | `waves/F-W0.md` `cd64d6580098` · `waves/F-W1.md` `0bfd6c8cd369` · `waves/F-W5.md` `40c1a5f5a576` · `waves/F-W6.md` `b11f08894b9d` · `../keyframes/waves/KF-W1.md` `ee33bf25cb2a` **("moved mid-round? no")** · `conformance/CENSUS-CANONICAL.md` `a450b8e9f80e` | **Five stale, one "no" now false.** Re-stamp: F-W0 → **`282f0c120cd4`** · F-W1 → **`a1302689aaa3`** · F-W5 → **`132c03192176`** · F-W6 → **`c8c6d1d7f136`** · KF-W1 → **`919e484b8a60`** with its cell **"no" → "YES"** · census → **`3e0a9acb3381`** with **"YES — errata round 5"**. ⊘ §0(C) explicitly disclaims settled-tree scope (*"the settled-tree hash table is the PURGE SEAT's closing act"*) and **every span re-run from all five moved files reproduced** — so the mechanism did what R4-8.3 built it for. **Two residues remain: the table cannot verify anything without a re-run, and one row's "no" is now a false statement.** |
| **REST-39** | `G-F7-5`'s close condition ⊕ §9's `X.F.W7.b` — **P5-18** | G-F7-5's F.W7-close: *"Any F.W7 key spec names its consumed-field set and proves the superset relation. **RED by absence: no key spec exists today**"*, GREEN owner *"F.W7 (FOR branch)"*; §9 asserts *"**G-F7-3/4/5/6** close for F.W7"* for **both** branches | §2a row 3 says `R4-variant-storage.md` is *"**never created**"* if the ruling goes against, so **on AGAINST there is no key spec and no stated act that closes G-F7-5**; §1a's XOR does not cover it. Add one clause to the gate face: `**On the AGAINST branch G-F7-5 closes VACUOUSLY**, with the kill rationale recording that no key was designed and no superset relation was owed.` |
| **REST-40** | §7b's `C-25` lock ⊕ `G-F7-5`'s operand — **P5-15 / E5-14** | `▲ **C-25 (home: F-W5 §2 clause E13; bytes: `fr-ContourSettings.md:43`)**` leading on `C-25` | `C-25` is an **alias**, never a banked head. **Lead with the banked id**: `▲ **`fr-ContourSettings B-4` (= `C-1`, ∘ `C-25` / `R6-8` fold) — home: F-W5 §2 clause E13; bytes: `fr-ContourSettings.md:43`**`. ⊘ **The canonical's own attachment is now cured in this file's favour**: E5-14 (2026-08-29) moved `C-25`/`R6-8` off `i-7` (`:104`) and back onto `B-4` (`:43`), the **first** banked head that claims them inside its own head — *"`F-W7.md`'s own keying already followed the record."* **The spec was right and the operand was not; say so once, here.** |

### D.5 · `waves/F-W8.md` @ `1e3698adf4ff` (11 edits)

**The premise.** F.W8's canonical roster is **zero** and that reproduces at both ends. The convictions are one fabricated roster row and one dead receipt inside a gate cell — *sound commands answering the wrong question.*

| edit | anchor | current bytes | replacement |
|---|---|---|---|
| **REST-41** | `:371`, §6a row 40 — **P5-2, HIGH** | `\| 40 \| fr-CanvasControlsDock **C-28** \| `:102` \| **F-W5 clause B1** (§B) … C-28 admitted **fold-by-reference** \| …` | **STRIKE the row as a numbered identity.** ⟨cmd⟩ `/usr/bin/awk 'NR>=806 && NR<876' "$C" \| /usr/bin/grep -c 'C-28'` → **0**; the canonical's only `C-28` row is `:455` `` \| `C-28` \| — \| `GLASS-RELAY` \| **GLASS-RELAY** \| `` at **fr-AnimationControls**, another record; ⟨cmd⟩ `/usr/bin/grep -nE '^\| \*\*C-28' $R/fr-CanvasControlsDock.md` → **∅, exit 1**. At the corpus `C-28` lives only in prose, as a fold **into `D-4`** — and `D-4` **is** a canonical row (`` \| `D-4` \| `L-1` · `C-3` \| `F.W4` · `F.W5–W8` \| **F.W4** <sub>legs: F.W5-W8</sub> \| ``) which §3 **P7** already books. **Carry `C-28` where the record carries it — as a named fold on `D-4` inside P7's cell — and re-derive §6a's header and closure stamp AFTER the row moves (R2-7.4), never defend them.** ⊘ **The row convicts itself in its own cell** (*"F.W8 books **D-4/L-1/C-3** … and **does not** book C-28"*) and is numbered as an identity anyway; **G15's LHS therefore carried a member its canonical RHS cannot contain — a non-empty set difference at authoring, which is this wave's own stated HALT condition, met for the third round running in a new costume.** |
| **REST-42** | `:246`, §4's `G15` cell — **P5-5, HIGH** | `**the same probe returns `L:L-5` a second time inside the canonical's §2 `F.W3` roster, at `fr-PaperSearchDropdown`**`, warranted by `grep -nF 'fr-ContourPreview** (1): ' "$C"` | **The fact is true; the receipt is dead.** Re-run, the fixed-string filter returns three lines (`4996` · `5044` · `5309`) and `\| grep -c 'L:L-5'` → **1**; the second `L:L-5` is at `:4920`, an `fr-PaperSearchDropdown` line the filter **excludes by construction, at any reading, on any base**. **Publish the two-probe pair — one command, one claim**: `⟨cmd⟩ `/usr/bin/grep -nF 'fr-ContourPreview** (1): ' "$C"` → three lines, the homing, residue disclosed` ⊕ `⟨cmd⟩ `/usr/bin/grep -nF 'L:L-5' "$C"` → `:4920` fr-PaperSearchDropdown (§2 F.W3) · `:4996` fr-ContourPreview (§2 F.W5) — **the tenth colliding token**`. ⊘ R4-2.5 forbids the charitable reading that would save it, **and it sits inside a gate cell — the site this file itself calls "the worst place this class can hide" when it struck round 2's dead probe from the identical position.** |
| **REST-43** | `:78`-region, §1's replacement-for-the-arithmetic sentence — **P5-8** | `**13 declared cross-edges** (§5c)` inside the countable-list warrant | ⟨cmd⟩ `/usr/bin/awk 'NR>=265 && NR<=278 && /^\| /' waves/F-W8.md \| wc -l` → **14** — F.W0 · F.W1 · F.W2 · F.W5 · F.W6 · F.W7 · F.W9/W10 · the value.js API row · SS-4 · SS-13 · the glass-ui BH relay · X·parse-that · the fourier tree · the execution gate. **`13` → `14`.** R4-7.3: the LIST is authoritative. ⊘ **Four of five figures (9 · 28 · 16 · 40) survive their own instruction; the fifth is the shape R4-3.2 was written to end, surviving one sentence past the strike that produced it.** |
| **REST-44** | `:3`, the masthead's FIRST receipt — **P5-4 + P5-5 + P5-10, three defects in one cell** | `⟨cmd⟩ `grep -n 'SS-4' COHESION.md`` with a two-line paste labelled **verbatim**, ending `· TA-4 atomdiff-excision record · X-W3 P0 rows` | **(a) BASE**: from `docs/tranches/X/fourier` — the base at which every other operand in this file resolves — the file does not exist; it resolves only from `docs/tranches/X`. Re-cut to `⟨cmd⟩ (base `docs/tranches/X`) `/usr/bin/grep -n 'SS-4' COHESION.md``. **(b) RESIDUE**: the command returns **seven** lines (`36 57 69 135 146 147 165`); the paste discloses two and states no residue — R4-1.9. Publish the seven line numbers and name the two quoted. **(c) VERBATIM OVER A TRUNCATION**: ⟨cmd⟩ `/usr/bin/sed -n '36p' COHESION.md` continues `· owner rulings owed (trie-vs-KISS et al.)` **plus four further cells and a trailing clause**, cut with no ellipsis under a label that means bytes — R4-7.1: *re-paste byte-true, or re-label.* ⊘ The routing claim is unaffected, **which is exactly why the form has to be fixed rather than argued, in the words this file uses on its own siblings.** |
| **REST-45** | `:69`, §0's coverage sweep — **P5-5, the instrument** | `every one of its 90 rows was tested for bytes in this file (⟨cmd⟩ per row `grep -qF '<id>' waves/F-W8.md`) … **no row is absent**` | The receipt is **true as printed and is the wrong instrument by this file's own law** — `grep -qF` over a bare token is what R-5 forbids as an identity test, and this file says so at §3, §5b, G15 and §6a, then warrants its only coverage claim with it. **Re-cut record-qualified**: an id counts as identified when it is corpus-unique **or** its record's name co-occurs on the line carrying it. Re-run: **40 of the 89 are corpus-AMBIGUOUS; 38 qualify; two do not** — `fr-BasisSelector m-7` and `fr-GalleryDraftsSection m-15`. **Publish that partition, not the 90/90.** |
| **REST-46** | §6a / §3, `fr-GalleryDraftsSection m-15` — **P5-1 at F-W8** | present only inside the quoted F-W5 C2 span at `:40` and `:210`, **bare, ambiguous, in no §3 cell and no §6a row** | **ESCAPED — cite it.** Canonical `` \| `m-15` \| `r2-missed-3` \| `F.W5-W8` \| **F.W5-W8** \| `` at fr-GalleryDraftsSection; ⟨cmd⟩ `/usr/bin/grep -n 'fr-GalleryDraftsSection' waves/F-W5.md` → `315:\| **fr-GalleryDraftsSection** (2) \| \`F-4\` → C1 · \`m-15\` → C2 \|`. Add a §6a row: `\| fr-GalleryDraftsSection **m-15** (= `r2-missed-3`) \| `:80` \| **F-W5 clause C2** \| **CITED, not booked** — the unversioned thumbnail URL + 24 h `public` cache; the band identity is F-W5's \|`. ⊘ **The escape and the instrument are one defect**: a bare `grep -qF` on a six-record homonym cannot see it. |
| **REST-47** | §3 J4 · G4 · §5b · §6, `m-7` — **P5-11** | `**m-7** (= C-6-as-rescoped)` **bare at all four sites**, in a file that books `C-6/C-7 (EditorControlsDock)` at §3 P7 — two different `C-6`s one screen apart | **Record-qualify at all four**: `**fr-BasisSelector m-7** (= `C-6-as-rescoped`)`. `m-7` is held by **7** records in §2 and **17** files in the frozen corpus. ⊕ **Add `m-7` and `m-15` to §5b's declared collider roster** (`B-1 B-2 C-2 C-3 C-4 C-17 C-18 D-13 L-B1 L-M3 M-10 M-13`) — a roster round 2 built *"by CONVICTION, not by caution"* from tokens that had already failed, **which is precisely why it does not contain the two that were failing silently.** |
| **REST-48** | §6a's forty rows — **P5-7, the 17-of-40** | §6a declares itself *"the per-id operand **G15** runs its set difference against"* and closes *"**Every one of the forty rows below is now a band-routed identity by its own cited bytes.**"* | Joined against this file's own declared roster of record (§2 `F.W5` ⊎ `F.W5-W8` = **117** post-E5-15), **17 of 40 name an identity absent from it**: one is REST-41's fabrication; **sixteen are canonical rows homed at F.W4 or F.W3 carrying `<sub>legs: F.W5-W8</sub>`** — band-**routed**, not band-**homed** (rows 2 · 8 · 9 · 13 · 14 · 15 · 16 · 17 · 22 · 23 · 25 · 26 · 30 · 32 · 38 · 39). §6a's header pre-empts them in terms, **so they are disclosed, not hidden — and R4-10's actual duty is undischarged**: fifteen of the sixteen name an **F-W5 clause** where the canonical holder is **F-W3** or **F-W4**. **Add the canonical holder by name to each of the sixteen cells, beside the STATING clause.** ⊘ The two axes are both real; the row must carry both. |
| **REST-49** | §3 M4 — **P5-14** | `**R3-7b** … instantiated at GCM-52 · **F-4 (= banked FR-AFP-4)** · fr-EditorControlsDock C-6` | **Two distinct canonical band rows collapsed into one unqualified token**: `` \| `F-4` \| `C-C-8` · `FR-AFP-4` · `R3-7b` \| `F.W5-W8` \| **F.W5-W8** \| `` at **fr-GalleryDraftsSection**, and `` \| `FR-AFP-4` \| — \| `F.W5-W8` \| **F.W5-W8** \| `` at **fr-AdminFlaggedPanel** — and F-W5 homes them at **two different clauses** (C1 and C2). Replace with `**fr-GalleryDraftsSection F-4** (= `C-C-8` · `FR-AFP-4`-as-alias · `R3-7b`) ⊕ **fr-AdminFlaggedPanel FR-AFP-4**, distinct rows, F-W5 clauses **C1** and its own`. ⊘ **R-5's exact prohibition, at a token the keystone had already separated — and the same cell is where `m-15` enters bare (REST-46), so both halves of fr-GalleryDraftsSection's band pair were unqualified at one site.** |
| **REST-50** | `:80`, §1's band negatives — **P5-10** | fr-App `:144` quoted as *"Nothing routes → F.W5–W8"* under a **verbatim** label; `fr-CollapsibleSection :131` a mid-sentence subset with no ellipsis | ⟨cmd⟩ `/usr/bin/sed -n '144p' $R/fr-App.md` → `- **Nothing routes → F.W5–W8**: App's coupling budget is spent entirely on the CSS-token↔parser seam and the mount seam…` — **emphasis stripped inside a verbatim span**, while the sibling paste one clause later preserves it byte-true (`fr-MobileFloatingToc :97`). **Restore the `**`**; add the ellipsis to `fr-CollapsibleSection` (the bytes continue *", and that negative is recorded;"*). ⊘ **PASS-3 D-4's convicted class inverted** — round 3 convicted markup *added*, this is markup *removed*, and R4-7.1 covers both: *"nothing is added, nothing is re-styled."* |
| **REST-51** | `:44`, §1a's operand ⊕ every `90 rows` figure ⊕ the `C:C-23` minute | `§2 `F.W5-W8` (**90 rows**)` and the three sites minuting the `C:C-23` divergence | **Band 90 → 89 (E5-15), and the divergence this wave minuted three times is RESOLVED IN ITS FAVOUR.** Canonical now `` \| `C:C-23` \| `C:C-24` \| — \| **UNROUTED** <sub>*errata R5 E5-15*</sub> \| ``: the only `F.W5-W8` on `fr-PaperSearchDropdown.md:77` was the record's own `**F.W5-W8 = no rows.**` — a load-bearing negative read as a positive routing, on an INFO row that FOLDS by reference into banked `fr-PaperSearch C-Z1/C-Z2`. Replace the minutes with `**RESOLVED at the census, errata round 5 (E5-15), on this spec's evidence** — F-W8 minuted the divergence three times, booked nothing either way, and said in terms that *"a wave with zero record-side rows may not settle a record-side reading."* **That posture was correct and the repair was always the census's.**` ⊕ re-stamp the census pin `a450b8e9f80e` → **`3e0a9acb3381`** and re-base §1b's 90 / 32-bullet extraction. |

### D.6 · `waves/F-W9.md` @ `e9a9c3016f4c` (13 edits)

**The premise.** Roster axis was CLEAN at 17/17 — **and errata E5-16 struck `R2-7`, so the roster is 16 and §2.0 row 14 loses its operand.** The canonical says so in terms: *"F-W9 §2.0 row 14 falls with it — a wave-side consequence, recorded here and not cured here."* This batch cures it.

| edit | anchor | current bytes | replacement |
|---|---|---|---|
| **REST-52** | `:124` (§2.0 row 14) ⊕ `:244` ⊕ every `17 rows` figure ⊕ `G-F9-21`'s census operand — **E5-16** | `\| 14 \| fr-PaperArticleWindow \| **`R2-7`** \| **NEWLY BOOKED AT ROUND 4** … **the canonical books it as a row of its own, homed F.W9** \|` | **The row falls.** Canonical now `` \| `R2-7` \| — \| — \| **TERMINAL (∅)** <sub>*errata R5 E5-16*</sub> \| ``: `fr-PaperArticleWindow.md:186` is a **Ruled-items** row whose own disposition column reads `**ADMITTED → PAW-50 (INFO).**` (siblings R2-1..R2-6/R2-8 read identically), and its lone `F.W9/W10` token grammatically qualifies **PAW-12's** rider — **`R2-7` and `PAW-50` were one finding banked twice.** Replace row 14 with `\| 14 \| fr-PaperArticleWindow \| **`R2-7`** \| **STRUCK AT ERRATA ROUND 5 (E5-16) — canonical TERMINAL (∅).** Round 4 booked it because the census homed it here; the census re-derived it and the booking goes with it. **The no-unit-harness finding itself is not lost: it is `PAW-50`, booked whole at row 15.** \|` ⊕ **`17` → `16`** at the masthead, §2.0's three receipts, and `G-F9-21`'s face. ⊘ **The pass-5 seat filed this against the canonical and not against the spec, and said so: *"if the canonical sustains the reading, nothing moves in F-W9; if it does not, F.W9's roster is 16 and §2.0 row 14 falls with it."* It did not sustain it.** |
| **REST-53** | `:297` (§2.8 E-11(f)) ⊕ `:460` (P-9's cell on G-F9-21's face) ⊕ E-10(a)'s residue probe — **P5-5, HIGH** | E-10(a) publishes the stem-alternation probe as *"**exactly two distinct forms, `PROGRESS.md:15` and `lane-frontend.md:568`**"* and its residue probe as *"**∅ (exit 1)**"*; E-11(b) re-certifies *"**∅ unlicensed**"* | Re-run (cwd `$X/waves`): the probe returns **THREE** forms — `F-W1.md:272` · `PROGRESS.md:15` · `lane-frontend.md:568` — and the residue probe **exits 0**, printing `F-W1.md:272`. **`F-W1.md:272` is a live line number into a LIVE sibling, in this spec's own voice, at two sites — `:297` and `:460` — and BOTH were minted by round 4's own repair, in the two cells that announce the anchor cure.** **Re-cut both to the stable anchor**: `while `F-W1.md` §4's **Intra-wave order (binding)** head reads `**Intra-wave order (binding):**`` — no numeral — and **re-run the residue probe after the edit, not before.** ⊘ *`:272` happens to still resolve — luck, not method,* and F-W1 has moved since the pin. **Fifth consecutive round in which a certification of a CLASS is published over an enumeration short by one, and the first in which the certifying round supplied the counter-example itself.** |
| **REST-54** | §4b's F.W3 row — **P5-5, HIGH** | `⟨cmd⟩ `grep -oE 'F\.W9(/(F\.)?W10)?' waves/F-W3.md \| sort -u` → **one distinct form, `F.W9/W10`**` | Re-run at a **matching** hash (`a89c3386f3f8`, identical to the PIN TABLE row — the sibling has **not** moved) returns **TWO**: `F.W9` and `F.W9/W10`, the second at `F-W3.md:783` inside a round-4 R4-10 clause. **Publish two.** The CONCLUSION survives — that occurrence is a census-roster mention, not a cross-edge, so F.W3's reciprocal is still genuinely owed — **but the published receipt is not the output of the published command**, which R4-1's opening law forbids outright. ⊘ **The standing lesson, banked here in one line: *a hash proves the bytes did not move; it does not prove the paste was produced by the command.*** |
| **REST-55** | `:462`, `G-F9-21`'s face, twice — **P5-8** | `**All seven** land in ONE commit with the twin.` and `The gate stays RED on **all seven** outstanding paired obligations` | The face's own census ⟨cmd⟩ returns **8 OUTSTANDING** (`grep -Ec '^\| \*\*P-[0-9]+\*\* \|.*OUTSTANDING' "$W"`). **`seven` → `eight` at both.** R4-7.3: *the LIST is authoritative and the prose is corrected in place* — **the list was installed at R4-7.2 and the prose beside it was never corrected.** ⊘ **Fourth consecutive round the same gate face has under-counted itself, now inside the round that installed the cure**; until it is fixed **G-F9-21's GREEN condition is indeterminate by one obligation.** |
| **REST-56** | `:297`, §2.8 E-11's disposition — **P5-8** | `**Five** paired obligations stand outstanding and none was fixed unilaterally** — P-1 · P-2 · P-4 · P-5 · P-6 · P-7 · P-8` | The word says **five**, its own list names **seven**, and the census says **eight**. Replace the word with the census figure and the list with the census's members: `**Eight** paired obligations stand outstanding and none was fixed unilaterally — P-1 · P-2 · P-4 · P-5 · P-6 · P-7 · P-8 · **P-9**, with P-3 closed at the bytes at round 3.` |
| **REST-57** | `:488`, §4a-17's PAIRED-BYTES LAW — **P5-8** | `Outstanding: **P-1** … **P-8**` — enumerates seven and **omits P-9 entirely** | Add **P-9** (the F-W1 anchor's CASE at the two checkpoint-block sites). ⊘ **P-9 is OUTSTANDING by the list and invisible to every prose statement of what must close** — which is precisely what makes REST-55's gate indeterminate. |
| **REST-58** | `:74`, §2's disposition arithmetic — **P5-8** | `**36 + 8 = 44 dispositions in all**, now **46** with round 4's two canonical additions (`(fr-PaperArticleWindow, R2-7)` ⊕ `(fr-PaperArticleWindow, PAW-50)` booked whole)` | **PAW-50 is already inside the 26, therefore already inside the 44** — ⟨cmd⟩ `awk '/^\| record \| identity/,/^\*\*By-mechanism entrants/' "$W" \| /usr/bin/grep -c '^\| fr-'` → **22** rows carrying 26 identities, and row 16 is `\| fr-PaperArticleWindow:200 \| **PAW-50** \| **F.W4** ⟨re-cut⟩ \|`. Round 4 moved its HOME; it did not add a disposition. **Only `R2-7` was a genuinely new disposition, and E5-16 has now struck it** — so `now **46**` → `**44**, unchanged: `PAW-50` was already counted at §2.2 row 16 (round 4 moved its home, not its membership), and `R2-7` was struck from the census at errata round 5 (E5-16)`. **Count the sections, which is the falsifier §2 installs for itself at E-10(c).** |
| **REST-59** | `:388`, §2.8a's span count — **P5-8** | `over **ELEVEN** labelled spans` and `the **eleven-span** transcript` | ⟨cmd⟩ `awk '/^\| # \| labelled span/,/^\*\*Three failures/' "$W" \| /usr/bin/grep -oE '^\| [0-9]+.? \|'` → **13 rows**, of which **ten** are distinct labelled spans (`#1..#10`), the three primed rows being the records' own spans rather than labels. **`ELEVEN` → `ten labelled spans over thirteen transcript rows (three primed)`** at both sites — **neither 10 nor 13 was eleven**, and this is R4-7.3 inside the register that publishes the count law. |
| **REST-60** | §2.0's base block ⊕ 44 inline ⟨cmd⟩s — **P5-4, HIGH** | `Every ⟨cmd⟩ in this section and at §2.8a runs from the **value.js repo root** after this exact block.` — while **44** commands take bare `waves/…` (33) · `formation/fourier/…` (7) · `registry/adjudicated/…` (2) · `carry/…` (2) operands | Those 44 resolve only from `$X` or `$V` — **at least three incompatible cwds**, which is verbatim the defect §2.8 E-10(e) records and declares cured. ⟨cmd⟩ from the repo root: `/usr/bin/grep -o 'No unit-test runner — vitest is ABSENT' formation/fourier/lane-frontend.md` → `No such file or directory`, **exit 2**. **Re-root all 44 to the assigned bases in the block that consumes them** — `"$X"/waves/F-W0.md`, `"$V"/formation/fourier/lane-frontend.md`, `"$R"/fr-*.md`, `"$X"/carry/F-W4-CARRY.md` — **and fix §2.8a's own section-B table, which carries bare `waves/F-W0.md` operands inside the register that publishes the repo-root rule.** ⊘ Round 3 answered this with a *convention*; R4-2.3 answered it with an *assignment*; **the assignment block was written and the 44 consuming commands were not re-rooted to it.** R4-2.5 convicts on sight. |
| **REST-61** | `:236` · `:440` · `:475` (§4a-4) · `:479` (§4a-8) · `:499` — **P5-15** | row numbers standing as identities in the wave's own voice: `ContourPreview 38` · `ImageUpload 27`, **two of them ordering LOCKS** (`§4a-4` HARNESS-BEFORE-RIDER, `§4a-8` MATRIX-BEFORE-COARSE) | **Key every seat-local site to the banked id** §2.0 rows 4/12 already restore: `ContourPreview 38` → **`fr-ContourPreview C:D-11`** (aliases `L:L-12`); `ImageUpload 27` → **`fr-ImageUpload L:L-i2`**. The two sites inside the shared §2.2 bytes (`:193`, `:199`) are **licensed and stay** — P-6/P-7 disclose them and neither is fixed unilaterally. ⊘ **§4a-17 asserts the seat-local corrections *"were taken in full"* — an absolute falsified at five sites, one of which (§2.3 S5) is itself one of them.** A lock keyed on a row number survives no re-numbering of the record, which is what §0.1 rule 1 exists to prevent. |
| **REST-62** | `:295` · `:297`, E-11(b) — **P5-10** | E-11(b) certifies the plain spelling `§1 :48` is now bracket-classed at both sites, while the clauses that ANNOUNCE the bracketing **re-spell it plain** — two new plain occurrences minted by the cell declaring the form extinct | Bracket them: `§1 :[4]8` at both announcing clauses. ⊕ E-11(b)'s companion claim *"**∅ unlicensed**"* is **false on this and on REST-53's `F-W1.md:272`** — restate it as the enumerated residue after both edits land, and **re-run the probe afterwards, not before.** ⊘ *A strike rendered at one of its occurrences and certified over all of them* — E-11(b)'s own named lesson, repeating one round later on the very ids E-11(b) struck. |
| **REST-63** | `:282`, E-1's re-scoped absolute — **P5-17** | E-1's enumeration of where the `9 e2e never enter the audit tab` span lives — *"the shared cell's, plus **this row's** quotation of it"* | ⟨cmd⟩ `/usr/bin/grep -n '9 e2e never enter the audit tab' F-W9.md` → **`:178`** (shared bytes, licensed) · **`:282`** (E-1's own cell) · **`:452`** (P-1's cell on G-F9-21's face, **minted at round 4**). The enumeration accounts for two of three and **predates P-1's cell by one round.** Add the third. ⊘ *Same shape as REST-53 and REST-59: axis right, enumeration short.* |
| **REST-64** | `:332-343`, the PIN TABLE — **P5-6** | `$X/waves/F-W0.md` `cd64d6580098` ⟨moved⟩ · `$X/waves/F-W1.md` `0bfd6c8cd369` ⟨moved⟩ · **`$X/waves/F-W4.md` `71f3b640d51e`** ⟨**unmarked**⟩ · `$C` `a450b8e9f80e` | Re-stamp: F-W0 → **`282f0c120cd4`** · F-W1 → **`a1302689aaa3`** · **F-W4 → `39e1a60b3fc9`, and mark the row ⟨moved⟩ — it moved and was not marked** · census → **`3e0a9acb3381`** ⟨moved — errata round 5⟩. ⊘ Record the asymmetry the pass-5 seat proved: **all eighteen quotations F-W9 takes from the moved files still reproduce, and the pin caught nothing at F-W3, where the hash MATCHED and the receipt was false anyway (REST-54).** |

### D.7 · `waves/F-W10.md` @ `bb58dc400cff` (8 edits)

**The premise.** 41 of 41 receipts reproduce, 15 of 15 anchors resolve, the 165-line partition closes — *the first F-W10 round in which a hostile seat could not break a single published receipt.* Both HIGHs are **sound commands answering the wrong question**, and the canonical defect this wave filed has now been ruled in its favour.

| edit | anchor | current bytes | replacement |
|---|---|---|---|
| **REST-65** | `:338`, §2.5b's escape test ⊕ `:296`, §2.5a-iii(c) row 13 — **P5-1, HIGH** | `the 60 ids extracted from `CENSUS-CANONICAL.md` §2's four NWO rosters and each tested `grep -qF` against `F-W10.md`: **60 present · 0 missing**` | **The result is an artefact of the probe.** ⟨cmd⟩ `/usr/bin/grep -cF 'ExportModal' waves/F-W10.md` → **0** — the record is named **nowhere**; the boundary-aware probe `(^\|[^A-Za-z0-9])L-16([^A-Za-z0-9]\|$)` → **9 hits, every one another record's** (`fr-UserSlugBar FR-USB-38`'s alias · `fr-PaperSearchDropdown L:L-16`). **Re-cut to a record-qualified, boundary-anchored probe over the 65** and publish its true output. ⊕ **land the fold sentence** at §2.5a-iii(c) row 13 (`fr-BasisCanvas L §R5-7`, same anchor `fr-BasisCanvas:106`): `**`fr-ExportModal L-16` folds here** — ⟨cmd⟩ `/usr/bin/sed -n '49p' fr-ExportModal.md \| tr '·' '\n' \| /usr/bin/grep -n 'L-16'` → `10:  L-16 → fr-BasisCanvas :106, NO-WAVE-OWNER` — **one home, no re-book**; the canonical's `NWO (packet)` row for it is answered by name.` ⊘ **The file's own convicted class re-created inside the cure written to end it**: §2.5b's closing paragraph condemns *"a booking under a bare roster number"* one clause before warranting itself with a bare `grep -qF` on a four-character id. |
| **REST-66** | `:471`, §5's round-4 exclusion row — **P5-2, HIGH** | `**⟨ROUND 4⟩ The three §2.2 canonical divergences** — `FR-TT-1 → F.W10` vs canonical **F.W4** · the 26-vs-17 F.W9 roster spread · `PAW-49 → F.W0` and `PAW-12 → F.W9` vs canonical **F.W3** for both` | **Short by at least one.** `PAW-50 → F.W4 ⟨re-cut⟩` (carried F.W4 at §2.7) stands against canonical `` \| `PAW-50` \| — \| `F.W9/W10` \| **F.W9** <sub>legs: F.W9/W10</sub> \| `` — **the canonical's PRIMARY is F.W9 and §2.2 cuts away from it**, which is different in kind from the eight cuts operating on the leg column (`FR-AFP-42`→F.W3 · `FR-EQR-6`→F.W4 · `D·D-B2`→F.W4 · PaperSidebar `D-B2`→F.W4 · `C-M4`→F.W4 · `M7`→F.W0), none of which this order convicts. Replace `three` with **four** and add the row: `**`PAW-50` → F.W4 ⟨re-cut⟩ vs canonical **F.W9**** — undisclosed until repair round 5; the harness-before-rider row §4a.5 makes load-bearing for five other bookings. |
| **REST-67** | `:444`, §4a.13 — **P5-8** | `the NO-WAVE-OWNER drain is **89 rows / 89 distinct ids / 89 terminal verbs** — §2.5's 23 ⊕ §2.5a's 36 (= **59** distinct ids) ⊕ §2.5a-ii's 16 (= 75) ⊕ **§2.5a-iii's 13**` | **`13` → `14`. One character.** `23 + 36 + 16 + 13 = 88` under a label reading **89**; the section-extraction instrument returns **23 · 36 · 16 · 14** and `23 + 36 + 16 + 14 = 89`. ⟨cmd⟩ `/usr/bin/grep -o "§2.5a-iii's 1[34]" waves/F-W10.md` → `14` · `14` · `13` — **the round-4 13→14 landing reached §2.5a-iii, §5 and G-F10-7 and not the clause headed *"Corrected denominators, binding on this file against itself"*.** ⊘ Filed at MEDIUM because it is the **third generation** of the E-13/E-22 shape and it sits in the clause built to stop it. |
| **REST-68** | `:327`, §2.5b's packet claim ⊕ §1.2's operand — **P5-3-adjacent / E5-17** | `its rows are **terminal dispositions over rows the canonical homes at a PACKET**, which is exactly R4-10's "cites the canonical packet row"` — false for **five of §2.5a-iii(c)'s fourteen** (`PP-AGGLOM`/`PP-SEVLAW`→F.W4 · `FR-TT-22`/`FR-USB-38`→F.W2 · `D-i2`→F.W1) | **THE CLAIM IS NOW TRUE AND THE OPERAND MOVES.** Errata **E5-17** (2026-08-29) made §0.1's arrow clause operative and re-derived all five to the packets their dispositions state: `PP-SEVLAW` → **NWO (packet)** · `PP-AGGLOM` → **NWO→SS-3** · `FR-TT-22` → **NWO (packet)** (E5-13) · `FR-USB-38` → **NWO (packet)** · `D-i2` → **NWO→SS-3**. Replace §1.2's extraction with `NWO (packet) **32** ⊕ NWO→SS-3 **27** ⊕ NWO→SS-13 **3** ⊕ NWO→SS-5 **3** = **65**`, re-run the four `awk` extractions against `3e0a9acb3381`, and add the minute: `⊘ **The spec was right at the record bytes and the census was wrong on all five; the amendment landed at errata round 5 and F-W10's drain verbs needed no change.** The drain's canonical operand rises 60 → 65 and the five rows this file already disposed correctly are now disposed against a census that agrees with them.` ⊕ **the disclosure §2.5b owed and did not carry is discharged by the amendment, not by prose.** |
| **REST-69** | §2.5a-iii(c) / the homonym guards — **P5-11** | `L-16` carries **no guard**, while `D-i2` (7 records), `m-4` (17), `L-19` (16), `MISSED-2` (3), `M-16`, `i-5`, `i-1` all do | ⟨cmd⟩ `/usr/bin/grep -n '^\| \`L-16\`' CENSUS-CANONICAL.md` → **six records bank `L-16` as a HEAD id**; `/usr/bin/grep -l 'L-16' fr-*.md \| wc -l` → **25 records carry the token**. **Add the guard**: `▲ **`L-16` is a SIX-record banked head** — one of them, `fr-ExportModal`'s, inside this wave's own NWO roster (REST-65). In this file `L-16` appears only as **`fr-UserSlugBar FR-USB-38`'s alias** and as **`fr-PaperSearchDropdown L:L-16`**, a distinct token; **neither is fr-ExportModal's.** ⊘ **The unguarded token is the mechanism of REST-65** — the one six-way collision never disclosed, and the one that hid the escape. |
| **REST-70** | §2.5a-iii(d), `MISS-5` — **LOW** | `MISS-5` carried only inside the weak enumeration as *"(restatement of FR-GIG-20)"* and counted among the 26 cited-elsewhere | `MISS-5` is a canonical **`NWO→SS-5` head id** (`- **fr-GalleryInfiniteGrid** (2): \`FR-GIG-20\` · \`MISS-5\``). **A packet-homed head id disposed only as another id's restatement is a citation by demotion**: either disclose the fold relation explicitly (`fr-GalleryInfiniteGrid MISS-5` restates `FR-GIG-20`; one home, no re-book) or give it its own drain row with a terminal verb. |
| **REST-71** | §2.5b's fabrication test — **P5-2** | `Since F.W10's canonical roster is empty, **every id-bearing cell of this file must be a citation, and one is not**` — a universal **measured over §2.2 alone** | **State the scope or widen the test.** Replace with `Run over **§2.2's re-cut table**: every id-bearing cell there must be a citation, and **two are not** — `FR-TT-1` (canonical F.W4) and `PAW-50` (canonical F.W9, REST-66).` **or** widen the test to the whole file and publish its output. ⊘ *A universal that was measured over one section is the "erratum written to prove that checking happened" shape this file convicts three times at E-4.* |
| **REST-72** | the census pin, everywhere it appears | `CENSUS-CANONICAL.md` quoted at `a450b8e9f80e` | Re-stamp **`3e0a9acb3381`** at every citing site, with the note `⟨*errata round 5, §0.4 — eighteen entries; F-side NWO packet population 60 → 65 (E5-13 · E5-15 · E5-17), and CANON-1 as this file filed it is SUSTAINED*⟩`. ⊘ **C3's caveat stands against interest and must be carried forward**: the arrow-clause class was found in a five-record spot audit of sixty-six and **is not proven confined to five** — §0.4's own "WHAT THIS ROUND DID NOT CURE" item 1 owes a corpus-wide re-derivation, and **that is a canonical act, never a wave's.** |

**Batch REST total: 72 edits** (F-W0 8 · F-W3 6 · F-W6 17 · F-W7 9 · F-W8 11 · F-W9 13 · F-W10 8).

---

## §E COVERAGE — every union register entry, mapped to its edits

**175 edits · BATCH-W1 52 · BATCH-MID 51 · BATCH-REST 72.** No entry is discharged by prose; every one has at least one anchored edit.

| union id | claim | edits | note |
|---|---|---|---|
| **P5-1** (BLOCKER) | roster not closed — 28 escapes | **W1-01…W1-25** (F-W1's 24: 23 booked, 1 retired by E5-6) · **MID-23 · MID-24 · MID-25** (F-W4's 3) · **REST-65** (F-W10's `L-16`) ⊕ **REST-46** (F-W8's `m-15`, the check's own escape) | **28 → 27 by errata, then 0 by these edits.** `fr-EquationView vue-tsc` was an escape only because the census minted a non-identity from a prose head; E5-6 struck it and its three siblings. |
| **P5-2** (BLOCKER) | fabrication 11 | **W1-26 · W1-27** (4) · **MID-26** (`FR-AH-27`) · **REST-15 · REST-16 · REST-17** (F-W6's 3) · **REST-41** (`C-28`, the one STRIKE) · **REST-66** (`PAW-50`) · **REST-71** (`FR-TT-1` + scope) | ⊕ the successor class the errata created: **MID-01…MID-04** (F-W2 ×4) · **MID-43** (F-W5 ×2) · **MID-50** · **REST-52** (F-W9 ×1) · **REST-32 · REST-37** (F-W7 holder mis-cites). **Cures are BOOKED→CITED conversions; exactly one row is struck.** |
| **P5-3** (BLOCKER) | F-W4 §Y false at both founding receipts | **MID-20** (`ce2622d1` → `fffb9685`, `1·0·0` → `1·0·3`) · **MID-21** (`4·3·3·7` → `7·3·3·7`) | Both must be re-run **by a seat that did not author §Y** (R4-4 two-key). |
| **P5-4** (HIGH) | portable-command law breached at five waves | **F-W9 44**: REST-60 · **F-W2 68/82 + the undeclared 4th base**: MID-10 · MID-11 · MID-12 · **F-W5 `$V`/`$X` ×6**: MID-36…MID-41 · **F-W7 ×3**: REST-34 · **F-W4 ×1**: MID-22 ⊕ **F-W1 ×1** (R4-2.4, wrong tree): W1-44 · **F-W8 ×1** (masthead base): REST-44 · **F-W0** (check-file operand): REST-04 | **~130 command sites, and not one changes a finding.** Every command is re-cut absolute-or-based **with the base DECLARED IN THE BLOCK THAT CONSUMES IT** (R4-2.3), never file-wide. |
| **P5-5** (HIGH) | dead and false receipts inside gate cells and mastheads | **MID-07** (F-W2 B11, third generation — locate the true bytes: they exist at `F-W0.md:140`, so **restate, do not strike**; no dependent falls) · **MID-08** (§2b 3-of-6) · **REST-42** (F-W8 G15's impossible probe — fact true, receipt dead, split into two) · **REST-54** (F-W9 D-2, receipt-not-its-command at a MATCHING hash) · **REST-11** (F-W3 §7 "NOTHING ELSE", false at write) · **REST-33** (F-W7 §12) · **REST-36** (F-W7 `find`) · **REST-45** (F-W8's 90/90) · **W1-45** (F-W1 K-24 "alone") · **W1-52** (the probe that cannot fail) · **MID-17** · **MID-47** | **No dependent is struck anywhere**: in every case the underlying fact survives and only the instrument fails. B11's quotation exists in four committed generations and at live `F-W0.md:140` in a rewritten sentence — **the true bytes are located, so the strike-with-dependents branch does not fire.** |
| **P5-6** (HIGH) | ~21 stale R4-8.3 pins; two false "moved: no"; no R4-8.2 purge certificate | **W1-46** (F-W1 3/6 + F-W10 omission) · **MID-09** (F-W2 4/8, **one false "no"**) · **REST-09** (F-W3's F-W4 pin, the one that contradicts CLOSE-CERT-2 §8) · **REST-25** (F-W6 5/8 ⊕ **the missing purge certificate**) · **REST-38** (F-W7 5/10, **the second false "no"** at KF-W1) · **REST-64** (F-W9 3, **one unmarked**) ⊕ the census re-stamp `a450b8e9f80e` → `3e0a9acb3381` at **every** citing site (W1-30 · MID-06 · MID-34 · MID-41 · REST-07 · REST-10 · REST-51 · REST-72) | **All twenty-one, plus the two false rows, plus the certificate.** Every quoted span still reproduces: *the mechanism worked, the instrument lies.* |
| **P5-7** (HIGH) | elsewhere-held ids BOOKED against cite-the-holder | **W1-34…W1-43** (F-W1's ≥11 legs ⊕ the retroactive law) · **MID-42** (F-W5's 20 unmarked legs under a false "four sites") · **REST-48** (F-W8 §6a's 16 legs cited to a clause instead of a holder) · **REST-01** (F-W0's PP-GATE shed on an unreceipted gloss — now a BOOKING) ⊕ **REST-15/16/17** (F-W6's 3 + the 18 band rows, filed for ruling at REST-18) | |
| **P5-8** (HIGH) | ≥10 count-words contradicting their own lists | **MID-13 · MID-14** (F-W2 "six"/eight ⊕ the differently-composed six) · **MID-27** (F-W4 "(7)"/nine) · **MID-28** (`FM-18`) · **MID-29** (§Y.3 six/eight) · **MID-30** (§0 six/seven) · **MID-44** (F-W5 "FOUR"/three) · **REST-35** (F-W7 "four negatives"/five) · **REST-43** (F-W8 13/14) · **REST-55 · REST-56 · REST-57 · REST-58 · REST-59** (F-W9's five/seven-vs-8, 46-vs-45, ELEVEN-vs-ten) · **REST-67** (F-W10 88-vs-89) · **REST-18** (F-W6 25→22⊕3) · **REST-24** ("all seven reproduce") | **Sixteen sites. R4-7.3 in every one: the LIST is authoritative and the prose is corrected in place.** |
| **P5-9** (HIGH) | the canonical's own ≥14 defects | **DISCHARGED AT THE CANONICAL, NOT HERE** — `CENSUS-CANONICAL.md` §0.4 E5-1…E5-18, verified at the record bytes, `3e0a9acb3381`. This order carries the **wave-side consequences**, which §0.4 item 4 expressly declines to make: **W1-29…W1-33 · MID-01…MID-06 · MID-33 · MID-50 · REST-01 · REST-02 · REST-03 · REST-10 · REST-14 · REST-51 · REST-52 · REST-68** | **The sequencing ruling was honoured: the amendment landed FIRST.** Its residue is named at §F item 4. |
| **P5-10** (MEDIUM) | verbatim/emphasis/truncation drift, 6th generation | **MID-15 · MID-16** (F-W2 G15's bold `0.192` ⊕ the fr-EasingPicker boundary) · **REST-21 · REST-22 · REST-23 · REST-24 · REST-29 · REST-31** (F-W6's four `-o` over-runs, two mid-line truncations, "all seven", `comm -13`/prefix/marker drops) · **REST-50** (F-W8's stripped emphasis ⊕ un-ellipsised subset) · **REST-44(c)** (F-W8 masthead truncation under a *verbatim* label) · **REST-62** (F-W9's re-created plain spelling) | |
| **P5-11** (MEDIUM) | homonym/collider rosters under-declared | **W1-50** (the `R-n` class — the mechanism of eleven of F-W1's escapes) · **W1-52** (the ~15-pair tail) · **MID-46** (F-W5's five undeclared colliders) · **REST-20** (F-W6's `M-9`/`M-10`) · **REST-47** (F-W8's `m-7`/`m-15`) · **REST-69** (F-W10's `L-16`, the one that hid the escape) ⊕ **MID-24 · MID-25** (F-W4's `R-7`/`R-5` disarms) | |
| **P5-12** (MEDIUM) | the F-W0 cluster | **REST-04** (PASS-1 check as ⟨cmd⟩ operand ×2) · **REST-02** (PP-DEAD's falsifier, now ruled) · **REST-05** (FR-COB-24) | |
| **P5-13** (MEDIUM) | claim surfaces pointed away from their own round | **W1-48 · W1-49** (G20's surface + the closing sentence) · **REST-19** (FW6-G17's retained detector) · **REST-55/57** (G-F9-21's indeterminate GREEN via the un-enumerated P-9) | |
| **P5-14** (MEDIUM) | landing/keying errors short of fabrication | **MID-45** (F-W5's mis-keys) · **MID-51** (the FR-GV-7 loop) · **REST-26 · REST-27 · REST-28** (F-W6's four contradicted homings, the triple identity, BLK-1) · **MID-32** (F-W4's `FR-CP-` prefix) · **REST-49** (F-W8's `F-4` merge) · **REST-58** (F-W9's PAW-50 double-count) | |
| **P5-15** (LOW) | M-25 lock-depth gaps | **REST-06** (F-W0 locks 5/7) · **MID-18** (F-W2 B5 without `M-β1`) · **REST-61** (F-W9's `38`/`27` at five sites, two of them LOCKS) · **REST-40** (F-W7's two locks heading on the alias `C-25`) | |
| **P5-16** (LOW) | dead-regex self-receipts | **MID-19** (both `\|`-under-`-E` sites, with the BRE/ERE engine fact banked once) | |
| **P5-17** (LOW) | residual-address enumerations short at their own new sections | **MID-30** (F-W4 §0's six/seven ⊕ §Y.3's mis-address of L163 at MID-29) · **REST-63** (F-W9 D-9's two-of-three) · **REST-30** (F-W6's unstated ∅ posture) | |
| **P5-18** (LOW) | disclosed history / legibility residue | **REST-13** (F-W3's lookaround parenthetical) · **REST-12** (§2's compound-head `/`) · **REST-39** (G-F7-5's AGAINST branch) ⊕ F-W7's four minted measurement-ids **recorded, not convicted** (no edit — the pass-5 seat and this seat concur) | |

### §E.1 · the eight unapplied RULINGS-4 families, as concrete edits

| ruling | unapplied at | edits |
|---|---|---|
| **R4-1** (a receipt is published beside the command that produced it) | F-W9 D-2 · F-W3 §7's two-member universal · F-W5 `:266`'s receipt with no ⟨cmd⟩ | **REST-54 · REST-11 · MID-47** ⊕ MID-07 · MID-08 · MID-17 · REST-36 · REST-42 · REST-45 |
| **R4-2** (portable commands; base in the consuming block) | five waves | **REST-60 · MID-10/11/12 · MID-36…41 · REST-34 · MID-22** ⊕ W1-44 · REST-44 · MID-48 (the R4-2.4/R4-2.3 mis-cite that licensed F-W5's whole class) |
| **R4-3** (canonical-only operands; no check file) | F-W0 §1 · F-W1's five struck-section pointers · G20's claim surface | **REST-04 · W1-47 · W1-48 · W1-49** ⊕ REST-19 |
| **R4-6** (a leg books as ONE row at the holder) | F-W1 · F-W5 · F-W6 · F-W8 | **W1-34…W1-43 · MID-42 · REST-15/16/17 · REST-48** |
| **R4-7** (.1 verbatim; .3 list-over-prose) | F-W2 · F-W6 · F-W8 at .1; ≥10 sites at .3 | **MID-15/16 · REST-21…24 · REST-29 · REST-31 · REST-50** ⊕ the sixteen count-word edits at P5-8 |
| **R4-8** (.2 purge certificate; .3 pins) | no certificate exists; ~21 pins stale | **REST-25** (the certificate) ⊕ the eight pin edits at P5-6 |
| **R4-9.1** (§Y auditable) | F-W4's §Y false at both founding receipts | **MID-20 · MID-21** ⊕ MID-29 · MID-30 |
| **R4-10** (book, or cite the holder by name; zero fabrication) | 28 escapes · 11 fabrications · cite-the-holder | the whole of P5-1, P5-2 and P5-7 above — **74 edits** |

**R4-5** (F.W5 28 vs band 90 held distinct) and **R4-10's adoption arm** (the canonical as sole operand at all eleven) were executed whole and are **preserved, not touched** — except that the band figure is now **89** (E5-15) and the distinction itself is re-affirmed at MID-50 and REST-37.

---

## §F SEQUENCING, AND WHAT THIS ORDER DOES NOT DO

1. **The canonical amendment landed FIRST and is DONE.** `CENSUS-CANONICAL.md` moved `a450b8e9f80e` → `3e0a9acb3381`; §0.4 carries eighteen entries, each verified at the record bytes before it was cured, each with a dated `<sub>*errata R5 …*</sub>` note at its own §1 cell, with §2 and §3 re-based in the same act. **No wave repair may re-cut a roster; every batch below consumes the amendment and none re-derives it** (R4-3).
2. **Batch order is BATCH-REST(F-W0, F-W3) → BATCH-W1 → BATCH-MID → BATCH-REST(remainder) → PURGE SEAT.** F-W0 and F-W3 first because REST-01/REST-14 change what F-W1 and F-W4 may cite; F-W1 next because nine of its leg conversions name F-W3 and F-W4 as holders; the rest in any order. **The purge seat runs LAST, touches every file after every other seat's final byte, re-pins every R4-8.3 table at the true settle, and issues the R4-8.2 certificate that has never existed** — LAW E, which four rounds have written and none has executed.
3. **No certificate issues until its own claims are re-run by its successor** (R4-4's two-key rule). In particular **MID-20 · MID-21 must be re-run by a seat that did not author F-W4 §Y**, and **REST-41 · REST-42's re-derived §6a header and closure stamp must be re-run, never defended** (R2-7.4).
4. **Three canonical residues are NOT cured and are named so their absence is not read as a clean bill** (§0.4's own disclosure, carried here verbatim in substance): **(i)** the arrow-clause class is **not proven confined to five rows** — it was found in a five-record spot audit of sixty-six, and a corpus-wide re-derivation under the newly operative clause is the **next canonical act**; **(ii)** the alias-orphan sweep is partial — `fr-PathPreview` carries a standalone `C-10` row with no banked head of its own, **flagged rather than struck because no seat filed it**; **(iii)** `fr-PathPreview K7` homes F.W0 on a token inside the description of *the claim being killed* — rule 2's verbatim reading reaches F.W0, its own killed-claims clause reaches TERMINAL, and **no seat filed it, so it stands.**
5. **What no edit here touches.** No product byte in either tree. No `fr-*.md` in the frozen corpus (`35fc8ebf`, immutable). No gate's born-RED state, no witness, no cure text, no sequencing lock, no dissent, no severity grade. `status: planned` stays; `VERIFIED **NO**` stays; the F.W1 TWELVE-limb citation stays whole and un-restated at all seven citing waves; the F.W7 ∅ posture stays (and is *stated* for the first time at REST-30); the fourier tree stays READ-ONLY; `scripts/dev/dev.sh` is never staged. **No id is re-booked, re-graded or renamed by any edit in this order** — except the one row R4-10 requires to be struck (`fr-CanvasControlsDock C-28`, REST-41), which was never a canonical row at all.
6. **The arithmetic pass 6 opens against.** Escapes **27 → 0**. Fabrications **11 → 0** (plus the eleven successor mis-homes the errata created, all converted). Count words **16 → 0**. Stale pins **21 → 0**, with a purge certificate for the first time. Portable-command sites **~130 re-cut, 0 findings changed**. **Roster totals after the batches: F.W0 57 · F.W1 358 · F.W2 27 · F.W3 928 · F.W4 1012 · F.W5 28 · F.W5-W8 89 · F.W9 16 · packets 65 · TOTAL 4262.**

*— PASS-5 surgical work-order seat, X·F, 2026-08-29. Every "current bytes" span above was grepped by this seat this session from the live file named beside it, on the pinned BSD toolchain, at the digests tabled at §0. This file directs; it repairs nothing, certifies nothing, and is an operand of nothing.*
