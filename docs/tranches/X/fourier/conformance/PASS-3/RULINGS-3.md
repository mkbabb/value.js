# X·F REPAIR ROUND 3 — CROSS-WAVE RULINGS (RULINGS-3)

**Seat**: cross-wave rulings, round 3, 2026-08-28. **Inputs**: `PASS-3/UNION.md` (whole) + the 11 PASS-3 check files beside it + `PASS-2/RULINGS-2.md` + `waves/F-W0..F-W10.md` + both carries + the 66 `fr-*.md` at `docs/tranches/V/megatranche/registry/adjudicated/`. Every load-bearing figure below was **re-verified at the live bytes by this seat this session** (read-only; this file is the seat's only write), and — because pass 3's headline conviction is that the ⟨cmd⟩ receipt itself became the forgery surface — every receipt in this file is pasted from a command this seat ran, with the four places where the UNION's own figures have gone stale against the resumed run **corrected here rather than inherited** (R3-2.2 K-24 · R3-10 row 5 · noted in place).

**Binding force**: each ruling names ONE owning cure per cross-wave defect class. Repair seats execute the LAW, never the line numbers; RULINGS files remain non-quotable authority (R2-1-LAW.3 stands). No seat copies a coordinate from this file into a spec (R2-2 stands). A seat finding any figure here stale re-measures by the pasted command and applies the LAW to the live bytes.

**Sequencing (BINDING on the round — the round-2 failure was an unsequenced twin):**

1. **F-W3 repairs FIRST** and publishes **§X.1-v4** (R3-1a). No other seat opens until v4 is landed bytes.
2. **F-W4 repairs SECOND**, consuming §X.1-v4 (R3-1b). Steps 1–2 are strictly serial; **no other wave touches the twin surface** (the partition, its holders, or the 28+7 ids) at any point in the round.
3. All remaining wave seats run after step 2, in parallel (R3-2 through R3-9 as indexed per wave).
4. **The PURGE SEAT runs LAST** (R3-3.8), over the settled bytes of all 11 specs, after every content write of the round — including the twins'. Its own output obeys R3-3.7's no-self-invalidating-receipt law, so nothing it writes re-stales what it certifies.
5. Any residual address conversions (R3-10 row 2) ride the purge sitting — there is no separate re-anchor seat this round; the classes left are receipt-shaped, not address-shaped.

---

## R3-1 · TWIN SEQUENCING — the hand-off received, in the only order that can receive it (cures P3-1)

### R3-1a — F-W3 publishes §X.1-v4, the definitive partition, FIRST

The **file-criterion LAW is STABLE and is KEPT**: one question of a PATH — *is the record's target file in a §5 unit file list?* — dual `F.W3/W4` rows on `.a`–`.e` files home at F.W3; all others take the default arm to F.W4; sole spellings home at their named wave; the other twin CITES, never books. §X.1-v4 changes no criterion. It changes four things:

1. **The 28 F.W4-assigned ids are NAMED PER ID, with their records** — `fr-BasisCanvas` (`BC-3/C-2b` · `BC-7` · `BC-20` · `M-α1` · `M-β1` · `M-β2` · `M-β3` · `BC-12` · `BC-16` · `BC-18` · `D-10-remnant` · `M-α4` · `M-α7` · `M-α8` · `BC-22` · `BC-23` · `BC-24` · `M-β7` · `M-β8`) · `fr-ContourEditorCanvas` (`MM-1` · `MM-2` · `MM-3` · `MM-4` · `MM-5` · `MM-7` · `MM-8` · `MM-9` · `MM-12`) · `fr-EquationResult` (its one row, already booked at F-W4 per that seat's 24/24/0 — named so the count closes). A record-count-only hand-off is what round 2 proved undeliverable: F-W4 declared the side "booked" while holding zero of the ids. The v3 18-vs-19 BasisCanvas slack is settled at the bytes in v4, per id, whichever way the atomiser lands.
2. **`fr-CoefficientsSpectrum` ENTERS the record partition.** ⟨cmd⟩ this seat: `grep -rlE '(^|[^A-Za-z0-9·-])m-19([^0-9]|$)' waves/ carry/` → **∅**; the record's `:90`/`:93` route `m-19 = C-22` and `m-22 = D-m7/D-i7` **→ F.W3/W4**; `grep -c 'shared/CoefficientsSpectrum.vue' F-W3.md` → **2** (§1 bounds + §5d's file list). The file criterion answers **F.W3** — v4 lists the record under the unit holding `shared/CoefficientsSpectrum.vue` with `m-19` and `m-22` as its residue ids. The v3 partition's *"every"* was false by exactly this record (F-W3-CHECK D-2); v4's closing sentence is restated only after the record is in.
3. **`fr-VisualizationView L-26` is RULED to F.W3 — the criterion admits NO exceptions.** `L-26` is dual (`fr-VisualizationView.md:91` → F.W3/W4) on `VisualizationView.vue` ∈ `.e` — the same file, same roster fragment §2.X(1) uses to home `MAJ-5`/`MAJ-7` at F.W3. R2-3b's designation of L-26 as "the control … held at F-W4 + carry" **is SUPERSEDED on this one id** by the generalized criterion (a ruling is a cure-shape; the LAW and the bank govern), minuted in v4 as a one-line departure record. F-W4 converts its L-26 booking to a citation (R3-1b.4). This dissolves F-W4-CHECK D-2's same-record-same-file-opposite-homings contradiction at the root instead of papering it.
4. **§X/§Z re-denominated over fresh figures, never a check file** (jointly with R3-4.3): the `691/62` dies; the atomiser spread is DISCLOSED (this round's measured residue: **144 permissive / 155 strict / 172 left-token**, one detector chosen and stated inline); the close transcript prints the per-id residue against **v4**.

### R3-1b — F-W4 writes ONLY THEN, consuming §X.1-v4

1. **BOOKS every id v4 assigns to it**: the 28 (per R3-1a.1, minus any v4 re-counts, per id) **plus the 7 §B.2 residue ids** — `SS-C-1` (the identity, beside its already-booked mechanism at §2.J, and registered at §6.4) · `FR-EMT-19` · `FR-EMT-23` · `FR-EQC-14` (a named row, not the `FR-EQC-residue` bucket) · `D·D-M12` · `m-15` (fr-CollapsibleSection, distinct from CP-25's timer row) · `i-6`. Born-RED witness of the receive: the ⟨cmd⟩ boundary-exact zero-run F-W4-CHECK §B.1 pasted (`BC-3=0 … MM-12=0`), re-run and non-zero after landing.
2. **§1 Bounds gains `BasisCanvas.vue`, `ContourEditorCanvas.vue`, `EquationResult.vue` as evaluate-scope rows** — the wave holds rows on files it must be allowed to read and judge; today neither file appears in any Bounds row while 28 of its rows target them (F-W4-CHECK D-4(ii)).
3. **§2.X's reciprocal declaration corrected**: ⟨cmd⟩ this seat `grep -on 'booked (2)' waves/F-W4.md` → `:202`. The count word follows the operand (F-W4's own §6.4 law): the default arm delivers the v4-assigned set, stated as a set-receipt, not "(2)".
4. **§2.E's fold gets a real home**: `fr-BasisCanvas BC-10/C-9` is BOOKED (it arrives with the 28), so `fr-CanvasControlsDock L-18`'s fold — R2-3c's fifteenth orphan — finally lands on a row the wave holds. The false receipt warranting the old arrangement dies under R3-3 item 4. L-26 converts to a citation of F.W3 per R3-1a.3.

---

## R3-2 · THE 149 ZERO-HOMED — every routing homed, excluded-with-reason, or killed at a named owner (cures P3-2)

1. **The 144 twin-axis rows resolve MECHANICALLY under §X.1-v4.** Expected split, stated so the close can be checked against it: **F.W4 books ≈35–36** (the 28 hand-off ⊕ the 7 residue ids); **F.W3 holds the remainder ≈108** (the v3 unit shares — `.a` 12 · `.c` 14 · `.d` 60 · `.e` 58 at the spec's own left-token atomiser, re-counted at v4 — ⊕ `m-19`/`m-22` at the `shared/CoefficientsSpectrum.vue` unit), landing as unit rows at the unit's open per v3's landing-shape law, which v4 keeps. If the chosen detector lands the residue at 144 or 155 instead of 172, the SPLIT moves with it and the partition table says so — the shares are derived, never inherited from this paragraph.
2. **The 2 F.W1 singles**, both `fr-PaperArticleWindow`, both record-qualified against the live homonym field (bare `K-20` collides across ≥3 identities: F-W3's `:5` "K-20 corollary" law-name, `:330`'s `fr-ExportModal K-20`, and the `rK-20`/`rK-24` fr-GlassTimeline family):
   - **`K-20` BOOKS at F.W1** — ⟨cmd⟩ this seat: `grep -c 'K-20' waves/F-W1.md carry/F-W1-CARRY.md` → **0 / 0**; unhomed program-wide once homonyms are disposed. It lands beside the `fr-ExportModal D-M3` ledger row whose unconditional discharge it corrects: at v7.0.0 **no coarse floor exists** — the coarse path is opt-in `[data-control-target]`/`touch-hit-area` — so the D-M3 cell is simultaneously **gated on G8 (`iconOnly` sweep) and ESC-1/G1 by name** (F-W1-CHECK D-4's cure, bound into this booking).
   - **`K-24`'s F.W1 LEG is ANSWERED at F.W1.** Correction of the UNION's record, this seat at the live bytes: `grep -c 'K-24' waves/F-W2.md` → **4** — K-24 IS booked at F-W2 (D2 · the §2b pre-discharge · G11's method cell · §8a row 40); the resumed run landed it. What remains owed is exactly the leg the banked sentence names — *"the **F.W1**/F.W2 blast-radius argument rests on it"* — one F-W1 row (or exclusion-with-reason) citing F-W2 D2/§2b for the discharged F.W2 half and carrying the inventory correction into the `/paper` sizing premise F.W1 inherits.
3. **`m-19` / `m-22` ASSIGNED** — F.W3, per R3-1a.2. Two ids, one record, one unit; booked at v4's partition today and at the unit's rows at execution.
4. **The 3 strict drain rows** — `PP-CENSUS` (fr-PathPreview:31) · `PP-AGGLOM` (:69) · `PP-SEVLAW` (:70) — **carried at F-W10, the drain wave, before its own COHESION §3.3 deadline clause.** ⟨cmd⟩ this seat: `grep -c 'PathPreview\|PP-CENSUS\|PP-AGGLOM\|PP-SEVLAW' waves/F-W10.md` → **0**; F-W4:274 expressly disclaims settlement. Each gets a per-id terminal verb in §2.5/§2.5a-ii (their banked dispositions are SS-3/SS-4 spec-authoring input — CENSUS-INPUT / NEXT-BOUNDARY-LEDGER verbs fit the bank) **or** an exclusion-with-reason; either way by id, before the deadline the wave itself declares. This landing is performed INSIDE R3-10 row 6's fresh derivation, not as three patch rows — the fresh run is what proves no fourth record survives the way fr-PathPreview did.
5. **The named-not-counted annex, homed cheaply where the checks already priced it**: `FR-USB-10`'s F.W1 leg (⟨cmd⟩ `grep -c 'FR-USB-10' waves/F-W1.md carry/F-W1-CARRY.md waves/F-W4.md` → 0/0/**2** — F.W4 is the adopting home; F-W1 adds ONE citation row naming the leg beside its carried `FR-USB-39`/`FR-EQR-1` identities) · `fr-SliderControl R-14`'s benefit rider and `fr-ConvergencePlot SC-3`'s shipped-relay adoption (one benefit-ledger row each at F-W1 — the register built to hold them) · `fr-GalleryInfiniteGrid C-6` (one F-W2 §7 exclusion line, R3-5.1) · `fr-PaperSearchDropdown C:C-23/C-24` (F-W5 §4's export row names the ids, citing F.W2 as adopting wave — the id-keyed citation D-17 asked for).

---

## R3-3 · FABRICATION PURGE — the seven named, then the class swept (cures P3-3 and P3-8)

The class MUTATED at pass 3: five false pasted ⟨cmd⟩ receipts and two minted quotations — the forgery now wears the provenance instrument itself. All seven re-verified by this seat at the live bytes before ruling. Named cures first, then the mandate.

**The five false ⟨cmd⟩ receipts:**

1. **F-W0, the flagship strike — FOUR sites** (masthead ▲ minute · §3a Q20 · §3a close · §6b F.W7 row ▲). Published: `grep -c 'Genuinely owed' F-W7.md → 0` · `'each for its true reason' → 0` · `grep -rln → F-W0.md alone`. ⟨cmd⟩ this seat, live: **1 · 1 · TWO files** (`F-W7.md:207` holds the phrase; F-W7's 16:48 mtime precedes F-W0's 16:58). **Cure**: all three receipts re-pasted from live output at all four sites; the strike's surviving conclusion (G-F7-11 never authored the sentence — F-W7 quotes F-W0's strike back at it) restated over the honest receipts; the lesson minute re-worded to include its own failure ("the conviction receipt was itself unrun").
2. **F-W1 §2·R1a** — published `grep -c 'D §3 U-2' F-W1.md …` → *"1 (this citation itself) / 0"*; live **2 / 0** (the second hit is the round's own §E-3·R2 erratum row). **Cure** (the check's own idiom, already used by the file's anchor-sweep row): **state the classification, not the count** — a self-descriptive count in a moving file is banned outright by item 7 below.
3. **F-W3 §0 ▲▲▲, the alias cell** — *"`fr-CoefficientsSpectrum` 9 → **0**"*, the sentence §Z calls "the alias rule that makes the gate closable at all". Live: `m-19`/`m-22` → 0 in F-W3.md, ∅ program-wide. **Cure**: the cell re-pasted from a live run — the honest figure is **9 → 2**, with the two named and homed per R3-1a.2 — and §Z's dependent claim re-worded (the alias rule dissolves MOST of the residue; the residue it does not dissolve is homed, not denied).
4. **F-W4 §2.E** — published `grep -c 'BasisCanvas.vue' waves/F-W3.md` → **0**; live **1** (F-W3:573, §X.1-v3's own row), and the receipt breaks §2.X's own never-count-a-live-sibling prohibition three paragraphs above. **Cure**: the receipt is STRUCK, not re-pasted — the fold's warrant becomes §X.1-v4's partition row read at the roster's own spelling (the lawful method §2.X(1) already states), and the fold target is a booked row per R3-1b.4.
5. **F-W9 §2.8 E-9(g)** — certifies *"the `:568` form survives only inside FR-GIG-15's banked words"* over a probe matching only `file.md:N`; live: `F-W9.md:354` carries `lane-frontend :568` in the wave's own voice (⟨cmd⟩ this seat: `grep -onE 'lane-[a-z]+ :[0-9]+' waves/F-W9.md` → `354:lane-frontend :568`). **Cure**: `:354` converts to the row label the file already uses at §4b (*"lane-frontend §6's INP / long-task discipline row"*); E-9(g) re-certified over a **form-agnostic probe pasted beside it** — `NAME[ .]:[0-9]+` over every external authority the file names, not only `.md`-spelled ones; the masthead absolute re-stated only after the probe returns ∅.

**The two minted quotations:**

6. **F-W7 §0 item 1** — attributes to F-W5's `F.W5 → F.W7` row the string *"cite E16 and G7 by clause and gate id, never by line"*, whose only homes are F-W7 itself and **RULINGS-2 §R2-2** (a rulings file; LAW.3 non-quotable), with no ⟨cmd⟩. **Cure**: quotation marks and the "instructs its consumer in this exact idiom" framing STRUCK; either state the idiom in F.W7's own voice, or quote F-W5's actual sentence (*"cited by clause id at this end and by §-anchor + row label at the other, with NO line number at either end"*) with its ⟨cmd⟩. R2-1d's class, recommitted inside the paragraph announcing it closed — the erratum says so.
7. **F-W9 §2.2's D/i-1 cell** — *"Coupled: rides the B-1/B-2 cure wave, never scheduled independently (verbatim lock)"*; the bank reads *"coverage rides the B-1/B-2 cure wave."* and `grep -rc 'never scheduled independently' fr-*.md` → **0**. **Cure**: quote marks re-scoped to the banked bytes with the spec's coupling clause OUTSIDE them (§2.3 S3 already models the correct form). The cell sits inside the SHARED paired-splice bytes: the edit lands **in BOTH F-W9 and F-W10 in ONE commit**, and G-F9-21's face carries it as the third paired obligation until that commit lands (then all three discharge together).

**The PURGE SEAT mandate (runs LAST, all 11 specs):**

8. **EVERY ⟨cmd⟩ receipt in all 11 specs is RE-RUN against the settled bytes.** A receipt that does not reproduce is **fixed from the live output** or **struck together with every claim that depends on it** — no third disposition. The sweep's transcript (per-spec: receipts run / reproduced / re-pasted / struck) is appended to each spec's errata section, one row per spec.
9. **Every 'verbatim' / 'exact' / 'byte-for-byte' label is byte-checked** (`grep -F` of the full labelled span at its named home). Drifted ones are **re-labeled `paraphrase (drift noted)`** or **re-quoted from the bytes** — the ~30-instance elision/casing/emphasis penumbra (F-W0 D-3/D-4/D-5 · F-W2 D-2/D-6/D-8/D-10 · F-W3 D-7/D-8 · F-W5 D-8/D-9/D-10 · F-W6 Q-1..Q-5/Q-7 · F-W7 D-4/D-5/D-6/D-7/D-8 · F-W8 D-3/D-4) plus F-W3's 47 unreceipted labels all fall under this sweep; a label without a ⟨cmd⟩ either gains one or loses the label (R2-1-LAW.2 enforced mechanically, once, program-wide).
10. **Standing amendment to R2-1-LAW (the structural cause killed, not the instance)**: **no ⟨cmd⟩ whose output is a COUNT of a live sibling or of the citing file itself may be banked in a spec.** Sibling receipts state classifications, set-membership, or `grep -o` word-output — forms that survive the sibling's next edit. A banked frozen count of a moving file is false the moment the file moves; four of this round's seven fabrications are exactly that (items 1, 2, 4, and F-W4's D-7 self-count). F-W0's check reasoned its way to this law; this ruling makes it binding.

---

## R3-4 · CHECK-FILE OPERANDS — five closure denominators re-based on the corpus (cures P3-4)

**The standing law, restated from g19's own FAIL clause and made program-wide**: *a closure/census transcript whose LHS is a count copied from any check file **FAILS BY CONSTRUCTION** — check files and rulings are measurements and cure-shapes, never quotable operands.* A gate whose operand is a superseded seat's enumeration can only ever re-find that seat's misses; it is structurally incapable of greening against the corpus. Check files may be named as *prior runs*, never as the operand. Applied by name, five denominators:

1. **F-W0 §3 header + §8 closure** — stamp **82/82** over `PASS-1/F-W0-CHECK.md` (live: `82-row` at `:7`/`:113`/`:381`, this seat). Re-based to the corpus-derived **84** (two independent seats, passes 2 and 3, one number: 84/84/0), **with the detector stated inline** (both dash spellings · slash forms expanded · table ⊕ bullet ⊕ prose-walked shapes) — the closure currently states none.
2. **F-W1 G20 leg (b)** — LHS is `PASS-2/F-W1-CHECK.md §4`'s **357** (live at `:266`), a roster that mislabels `GCM-26`/`HLG-12` in opposite directions and excludes the 47-row leg/rider class where three of this pass's five escapes live. LHS becomes **the 66 records directly**, detector inline; the pass-3 fresh figure (404 routed under the shape decree) is the shape of the answer, derived not copied.
3. **F-W3 §X / §Z / g19's operand statement** — the never-reproduced **691/62** (live: `691` ×5) dies; re-denominated per R3-1a.4 with the atomiser spread disclosed and the record count from a run that balances (the pasted block whose partition sums 903+151=1054 against a 998/1081 roster is re-pasted from a balancing run; 65 records, not 62).
4. **F-W4 `G-F4-CARRY-CLOSURE`** — the born-RED cell rests on PASS-1/PASS-2 figures quoted as witness, against the spec's own §0/§2.X statements. The witness becomes **the wave's own leg-(b) measurement, printed fresh** (the receive-run of R3-1b.1 IS that measurement), and the R2-3c column-3 override is **disclosed in the operand sentence** so the gate's enumerated bookings are inside its own declared scope (D-3 and D-10 cured in one edit).
5. **F-W5 census + G19 LHS** — pinned five times to `PASS-2/F-W5-CHECK.md §1`'s landing table (**167**, live ×14), which that table's own "zero-routing records" row falsifies: `fr-EquationPanel:48` carries two routings the spec itself books at C4/F9. Re-derived fresh from the 66 → **169** (or 170 — the `fr-AdminUserList:161` R6-8 carry is admitted or excluded EXPLICITLY, reconciling the record's own "F.W5 ×12" tally; D-16 resolved one way, stated). G19's LHS restated so `P-9` sits on exactly one side and `SS-L-07/SS-C-10` is counted exactly once (D-3's contradiction dissolved in the same cell that houses it).

---

## R3-5 · RESTRICTED DETECTORS — five gates re-cut on the standard detector (cures P3-5)

The standard detector (R2-3a.1 ⊕ R2-9, now with pass 3's two new axes): **shapes** (table ⊕ id-headed route-marked bullet ⊕ prose walked to the enclosing row id) · **spellings** (both dashes; every slash/span/parenthesised form; every token the corpus actually uses) · **positions** (a member matches whether written first or second in a compound) · **record-qualification** (the booking test is the (record, id) PAIR — a bare token is not an identity). A gate stating anything narrower is DEFECTIVE at authoring, whatever it returns. Applied by name:

1. **F-W2 G19** — the probe `F[.·–-]?W2` cannot match a slash form whose SECOND member is W2; `fr-PathPreview`'s two `F.W1/W2` rows are invisible to all three operands. Re-cut with the second-position arm (`(F[.·–-]?W[0-9]+[/–-])?W2`, both dashes); the set-difference re-run; **PP-REDGATE + roster item 7 homed as an §7 exclusion row** citing F-W1 §4 step 4's *"PP-REDGATE ambient declaration"* limb (F.W1 owns both inside the atomic transaction — the id F-W2 quotes twice without noticing); **`fr-GalleryInfiniteGrid C-6`** gets its one-line §7 row (folds by reference to the F.W1 identity; rides B4's wave-lock; no F.W2 booking).
2. **F-W6 FW6-G17** — the operand probes one token in two dashes; the corpus routes in **FOUR band spellings**: `F.W5-W8` · `F.W5–W8` · `F.W5(-W8` (fr-ContourPreview:137, whose second span row is `L:L-5` at `:46`) · **bare-`F.W5`-as-union-name** in the five records whose routing taxonomy spells the union that way (ConvergenceLegend · ConvergencePlot · EquationModeToggle · EquationResult · NotationPills). Re-cut over all four; re-run; the **ten escaped identities** (L:L-5 · FR-EMT-1 · FR-EMT-20 · FR-EQR-4 · FR-EQR-32 · FR-NP-30b · D-L4/C-7 · L-M7+C-8 · C-7 · L-m13) each land as book / cite / exclusion-with-reason under the wave's own branch law — all ten are program-held (F.W4/F.W5/F.W8/carry), so most land as citations; **FR-EMT-20 lands as a BOOKING per R3-7.1**. The gate's UPPER operand is enumerated at the same sitting (D-7: rows 57–122 listed by id, or the falsifier re-worded to an operand a seat can actually run).
3. **F-W3 g19** — the booking test is record-blind: 140 head-ids collide across records; 421 routed rows satisfy (→) through a cross-record homonym (`fr-App M-2` the worked receipt — 8 hits in F-W3, none its row). **Record-qualification is added as the fourth inline axis**; the residue derivation of §X.1-v4 re-runs record-qualified, which is what lets colliding-id records (fr-App · fr-MobileFloatingToc · fr-SvgFilters · fr-CollapsibleSection) enter the residue at all.
4. **F-W7 G-F7-8** — both operands anchored to markdown-table shape (`^\| \*\*E…` / `awk` over `§E`). Re-stated shape-agnostically with the detector INLINE per R2-9's closing clause; the result (8/8, §E = E1…E20) is expected unchanged — which is precisely the condition that does not cure the operand.
5. **F-W10 §2.5a** — the loose-class predicate is FALSE of ten of its sixteen members (all strict at the bytes: FR-TT-21 · FR-TT-22 · FR-USB-34 · FR-USB-37 · FR-USB-38 · PAW-56 · FR-FG-23 · m-4 · D-i2 · fr-BasisCanvas L §R5-7). The ten receive **per-id terminal verbs** (the strict roster's requirement, today, not on re-grading); the predicate is re-run honestly — the true weak residue is ~36 lines including records the spec never names (fr-EquationPanel:52) — and the class is re-enumerated so predicate and roster reproduce each other in both directions. Rides R3-10 row 6's fresh derivation. The band disclosure in G-F10-7's cell is corrected in the same edit (the 81/92 sets CROSS, not nest; the strict-92 spans **44** records, not 56).

---

## R3-6 · DOUBLE-HOMES — one owner for AA-45/46/47; a standing tie-breaker for the seam (cures P3-6)

1. **RULED: the ONE home for `AA-45` · `AA-46` · `AA-47` is F-W10 §2.5 — the NO-WAVE-OWNER drain.** The banked terminal cells (fr-AdminAuditLog:88–90) are `**NO-WAVE-OWNER**` with **no F.W5 token** (the three are not members of F-W5's 169); the drain is the COHESION §3.3 deadline authority built to hold exactly this class, and its rows already carry terminal verbs. F-W5's clause-bank booking is the out-of-band re-book — the same shape R2-11 dissolved at J4, except here the record does not even route F.W5.
2. **F-W5 A1/A2/A3 keep the MECHANISM and convert the ids to citations**, on the model A1 already applies to `AA-48` (*"same latent class — recorded, NOT re-booked"*): each clause cites F-W10 §2.5's row by id; the three ids leave the rows-folded cells; §2c gains the exclusion receipt it gave AA-48 and denied these three (the NO-WAVE-OWNER criterion, now applied evenly), citing F.W10.
3. **AA-46's contradiction is resolved AT THE BYTES, in F-W10's favour.** The record's own terminal cell reads *"Redundancy records. **NO-WAVE-OWNER**."* — F-W10's **KILL-WITH-RATIONALE** ("a redundancy record; no defect, no work") is the byte-faithful disposition and **STANDS**. F-W5's A2 clause loses nothing: the envelope decision stands on its other authorities (FR-AFP-18 · D10); AA-46 may be cited as envelope *evidence* with the kill noted, never as an operand of the clause.
4. **`FR-AUL-46` STAYS CURED** — verified at both ends this pass (F-W8 §3b: band count 1 = boilerplate; one home = F-W5 D7; J4 carries the citation form). No seat re-books it; its check row demands nothing further.
5. **STANDING TIE-BREAKER for the clause-bank/drain seam** (the class needs a law, not a fourth per-id ruling): **a banked NO-WAVE-OWNER row's ONE home is the drain (F-W10 §2.5)**; an F.W5 clause that needs the row's mechanism CITES the drain row by id and stands on its F.W5-routed authorities. The converse never occurs — the drain does not cite the clause bank as a home. A future row claimed by both surfaces resolves by this sentence without a ruling.

---

## R3-7 · M-25 LEG DROPS — the executing clauses made whole (cures P3-7)

1. **`FR-EMT-20` INTO F-W6's booking.** F-W5 clause E10's carried-rows cell names it (*"… · **FR-EMT-20** (cited F1)"*); F-W6 §2.7 books PP-DEADSEAM against E10/B4 and omits it; ⟨cmd⟩ this seat `grep -c 'FR-EMT-20' waves/F-W6.md` → **0**. Booked at §2.7 beside the E10 rows — it is simultaneously census escape E-3 under R3-5.2, cured once by this booking.
2. **F-W8 row 19's band re-attached.** `fr-EquationPanel D-10+D-L12+C-22` (a band row, home F-W5 clause **F9**) gets its own §6a row citing F9; row 19's occupant `D-3+C-5` (proven NOT band-routed by its own cell) demotes to a footnote of that row or an explicitly-excluded census correction; the §6a header and the closure stamp are **re-derived after the rows move** (the wave's own stamp-follows-operand law) — G15's stated HALT condition, currently met at authoring, is thereby lifted.
3. **F-W9's N-2 attribution clause.** The banked cure routes **F.W4** (`fr-DarkModeToggle:88`); the string `F.W4` never appears beside N-2 in F-W9. One clause at §4a-12 — *"N-2's cure is F.W4's (banked); F.W9 owns only the spec-side half of the coupling"* — plus one row in §5's cure-exclusion table.
4. **`FR-USB-10`'s F.W1 leg** — answered per R3-2.5 (one F-W1 citation row; the id, not new work — FR-USB-39/FR-EQR-1 are already carried).

---

## R3-8 · FR-NP-32 CANONICAL FORM — the last two waves brought under R2-7.5 (cures P3-10)

The canonical form is **`FR-NP-32 (≡ fr-PaperSidebar M1)`** — cite both, never substitute — **everywhere the corrupt-dist gate is stated**; absence from a directive index lifts nothing (the loophole R2-7.5 closed in terms, which is exactly how these two waves escaped it).

1. **F-W7** (⟨cmd⟩ this seat: `grep -c 'FR-NP-32' waves/F-W7.md` → **0**, the only X·F spec without it): the canonical form lands at all **three** statements — §5e's OG-F1 row (bare `(M1)`), §6 G-F7-7's witness (`fr-PaperSidebar M1`, record-qualified but not canonical), §7c's F.W0 row (bare `(M1)`). Two of the three are additionally bare-token against a live `M1` homonym class — the canonical form cures both defects at once.
2. **F-W6**: `FR-NP-32` appears once (§4's F.W0 edge, `:312`, correctly). **FW6-G19's born-RED witness** — the gate's own witness cell, the one site that drops the id — takes the canonical form.

---

## R3-9 · UNRUNNABLE MACHINERY — every gate executable as written (cures P3-9)

1. **F-W2**: the two `…`-elided commands (G4's `sed -n '1,9p' …/lib/basis-display.ts` · G8's `node -p …/@mkbabb/glass-ui/package.json`) are completed to literal paths from the declared cwd. **The four-PNG phantom bounds cured**: G12's artefacts either enter §2a's File Bounds as explicit `create` rows at a path that resolves (the live probe directory is `docs/tranches/V/megatranche/audit/probes/app-wave/`) or G12 re-points to bounds the wave holds; §5's Denominator note is restated truthfully in the same edit (both its current halves are false — no create markers exist and the path as written does not resolve).
2. **F-W5 §1c**: the triumvirate halt names *"a census row (§1's PASS-1 roster)"* — §1 is Bounds, and the PASS-1 roster is declared forbidden at four of the file's own sites. The operand is renamed lawfully: the halt keys on **the fresh corpus-derived census of R3-4.5** (a routed id of the §2c roster failing to home three times).
3. **F-W6 §2.11b**: the re-authored derivation law contradicts its own landings in both directions (booked FR-AFP-33 on a CITES clause; seven cited rows on BOOK clauses). The seat either **amends the discriminator** to the rule the 24 landings actually obey (the plausible true rule: F.W6 books only rows whose burn-down EXECUTION it owns; a row that merely instantiates a clause is cited even where F.W6 owns the clause's green) **or re-lands the eight mismatches** — then RUNS the procedure over all 122 and prints the branch each took. The closing sentence ("a procedure whose branches cannot be run in order is itself that failure") is re-stated only after the run exists.
4. **F-W3 §5a**: the disjointness sentence is FALSE and §5a-v2 re-affirms it as TRUE over its own three declared shared paths, while a fourth (the §C.I shared toolbar: `AdminUserList.vue` ∈ `.e` ⊕ `AdminFlaggedPanel.vue` ∈ `.d`, one repair unit) is not declared at all. Re-authored truthfully: the units are **conflict-declared and sequenced**, not file-disjoint; split (5) added for the toolbar row; §5's preamble spelling corrected to match.
5. **F-W9**: three incompatible ⟨cmd⟩ path bases. One line — `$X = docs/tranches/X/fourier/` — declared beside §2.8a's `$R`/`$M`, and every inline note rooted against its base.

---

## R3-10 · THE 8 UNAPPLIED RULINGS-2 DIRECTIVES — verified against the resumed run, then re-directed

Per the UNION's §4, with each row **re-verified at the live bytes this session** before re-direction — two of the eight turn out substantially landed by the resumed run, and their re-directions shrink accordingly.

| # | directive | resumed-run verification (this seat, live) | round-3 re-direction |
|---|---|---|---|
| 1 | **R2-1-LAW** (quote-by-command, standing) | Breached at scale in the round that adopted it: five false receipts + two mints (§R3-3), 47 unreceipted labels at F-W3, ~30 drifting labels program-wide | **R3-3 whole**: the seven named cures + the PURGE SEAT sweep (items 8–9) + the standing no-banked-counts amendment (item 10), which removes the structural cause rather than the instance |
| 2 | **R2-2** (line-cite abolition; re-anchor LAST) | Near-total: F-W0's gate ids cited program-wide; ~60-site class → ~6, all inside errata/receipts. Survivors: F-W9's `:354` space-form (live, re-verified) · F-W10 E-4's two `W9.md` addresses under a "corrected throughout" disposition · fourth-generation drift at F-W5 X-1 / F-W6 §4 against F-W0's 16:58 last write (the re-anchor seat did not run last relative to it) | F-W9 seat converts `:354` (R3-3.5); F-W10 seat discharges E-4 (convert the two addresses, resolve the E-4/E-17 self-contradiction); F-W5 X-1 and F-W6 §4's stale erratum FACTS are re-pasted from the live F-W0 or restated as classifications — all during the round's writes, then the PURGE SEAT (which runs LAST, R3-3.8) certifies the class over the form-agnostic probe. No separate re-anchor seat: the survivors are receipt-shaped |
| 3 | **R2-3b** (twin split; every escape homed by id) | Publish side ran (§X.1-v3 names holders); receive side did not: 28 ids ZERO-received (BC-3/MM-1 → 0 re-verified; `booked (2)` at F-W4:202 live) | **R3-1** — the serial twin sequence is the re-direction; the blocking cause (two seats writing the seam unordered, each citing the other's unlanded state) is removed by making the order BINDING on the round |
| 4 | **R2-3c** (the 15 §X.1 orphans at F-W4) | 14 of 15 landed (verified by the F-W4 seat); the 15th (`L-18` → `BC-10/C-9`) folds onto a home booked nowhere, warranted by the false §2.E receipt; the column-3 override undisclosed in the gate operand | **R3-1b.4** (BC-10/C-9 booked with the 28; the fold lands) + **R3-4.4** (the override disclosed in `G-F4-CARRY-CLOSURE`'s operand sentence) |
| 5 | **R2-5, the sibling half** (no sibling re-derives; every sibling re-quotes) | **SUBSTANTIALLY APPLIED in the resumed run — the UNION's row is stale here.** ⟨cmd⟩ this seat: every surviving `ELEVEN-limb` token (F-W1:309 · F-W5:299 · F-W6:301 · F-W7:14/:217 · F-W9:199 · F-W10:293/:316) sits INSIDE a self-disclosing strike-note or erratum, beside a live TWELVE re-quote — lawful mentions, not citations | Residue only, two edits: **F-W4** retires the stale ⊘ disclosure at G-F4-ZERO-CONSOLE (`:246` — *"at this writing F-W1's charter sentence still reads eleven"* is FALSE at the live bytes; the cell's own follow-the-charter clause licenses the retirement) · **F-W1** restates §2·R1's sibling claim as the DIRECTIVE R2-5 made it, not a description of files it cannot write |
| 6 | **R2-6b** (drain re-derived from the 66, fresh) | The sixteen PASS-2-named rows were homed; the fresh derivation NEVER RAN — fr-PathPreview's three strict rows escape (0 hits in F-W10, re-verified live), and the loose predicate is false of 10/16 | **F-W10 RUNS THE DERIVATION** this round — the strict detector over the 66, fresh, then: the PP triple homed per R3-2.4; per-id verbs for the ten strict "loose" members and the predicate/roster made mutually reproducing per R3-5.5; the 81/92/44-vs-56 band disclosure corrected. The blocking cause (honouring the ruling for named rows while skipping its derivation clause) is addressed by making the DERIVATION TRANSCRIPT itself the deliverable §2.5a cites |
| 7 | **R2-7.5** (FR-NP-32 canonical everywhere the gate is stated) | F-W7 → 0 (live); F-W6's G19 witness bare — the two waves the pass-2 directive index did not name, the exact loophole the ruling closed in terms | **R3-8** — both waves, all four sites, canonical form |
| 8 | **R2-9** (shape/spelling-agnostic operands; detector inline; never a check file) | Declared everywhere, executed unevenly: five check-file denominators live (82 · 357 · 691 · PASS-1/2 witness · 167, all re-verified at the bytes) and five restricted detectors survive | **R3-4** (the five denominators + the FAIL-by-construction standing law) + **R3-5** (the five gates re-cut on the standard detector, now four axes incl. record-qualification and second-position expansion) |

---

## PER-WAVE DIRECTIVE INDEX

| wave | directives (by ruling) | n |
|---|---|---|
| **F-W0** | R3-3.1 (the flagship's three receipts re-derived honestly at all four sites) · R3-4.1 (§3/§8 → 84 with inline detector, corpus operand) · R3-3.9 purge share (D-3/D-4/D-5 label drifts · D-6 count-vs-command · D-7 phantom §22 · D-8's unminuted FM-21 re-route gains its divergence minute · D-9/D-10/D-11/D-12 riders) | **3** |
| **F-W1** | R3-2.2+R3-2.5+R3-7.4 (book K-20 gated on G8/ESC-1 · answer K-24's F.W1 leg citing F-W2 D2/§2b · FR-USB-10 leg citation · R-14/SC-3 benefit-ledger rows) · R3-3.2 (R1a receipt → classification) · R3-4.2 (G20 leg (b) → corpus operand, detector inline) · R3-10.5 residue (§2·R1 restated as directive; D-3 enumeration · D-5 range spelling riders) | **4** |
| **F-W2** | R3-5.1 (G19 second-position arm; PP-REDGATE pair + C-6 homed at §7) · R3-9.1 (two elided commands completed; PNG bounds/create markers; §5 Denominator note truthful) · R3-3.9 purge share (D-2 C·S-2 · D-6 RD-8 · D-8/D-10 casing/elision · D-5 span self-contradiction · D-7 sixteen-vs-ten attribution) | **3** |
| **F-W3** | R3-1a (§X.1-v4 FIRST: 28 named per id · fr-CoefficientsSpectrum in · L-26 ruled to F.W3 · "every" restated true) · R3-3.3 (the alias cell re-pasted: 9 → 2, homed) · R3-4.3 (691/62 dies; fresh denominators; balancing block) · R3-5.3 (g19 record-qualified, fourth axis) · R3-9.4 (§5a conflict-declared; split (5) toolbar) · R3-3.9 purge share (47 unreceipted labels · the R-8 rulings-file quote re-sourced · D-7/D-8) | **6** |
| **F-W4** | R3-1b (SECOND: book the 28+7 · §Bounds evaluate-scope · §2.X "(2)" corrected · BC-10/C-9 fold landed · L-26 → citation) · R3-3.4 (§2.E receipt struck; fold re-warranted by the roster's bytes) · R3-4.4 (gate witness = own measurement; column-3 override disclosed) · R3-10.5 residue (stale ELEVEN disclosure retired · D-6 PIN-LAW count re-denominated · D-7 set-receipt idiom) | **4** |
| **F-W5** | R3-6.2 (AA-45/46/47 → citations of the drain on the AA-48 model; §2c exclusion receipts; AA-46 killed per the bytes) · R3-4.5 (census 169 fresh; D-16 resolved; G19's P-9/SS-L-07 operand contradiction dissolved) · R3-9.2 (§1c halt operand lawful) · R3-3.9 purge share (D-2's false 2-of-7 universal corrected · D-5's "nine e2e specs" → 8 both sites · D-8/D-9/D-10 labels · D-6 K-13 qualified · D-11 rooted paths · D-12 X-1 receipt refreshed · D-14 whole outputs · D-19 reciprocal minute · D-20 count rendering) | **4** |
| **F-W6** | R3-5.2 (G17 four band spellings; the ten escapes landed per branch law; upper operand enumerated) · R3-7.1 (FR-EMT-20 booked at §2.7) · R3-8.2 (G19 witness canonical) · R3-9.3 (§2.11b discriminator squared with the landings, then run and printed) · R3-3.9 purge share (Q-1..Q-5 · Q-6 stale erratum fact re-pasted · Q-7) | **5** |
| **F-W7** | R3-3.6 (the minted attribution struck/re-sourced) · R3-8.1 (canonical form at the three statement sites) · R3-5.4 (G-F7-8 shape-agnostic, detector inline) · R3-3.9 purge share (D-2's one-command-two-quotes split into two real commands · D-3 five→four · D-4/D-5/D-6/D-7/D-8 drift set · D-11 §8 names both stale-premise consumers) | **4** |
| **F-W8** | R3-7.2 (row 19 re-attached: D-10+D-L12+C-22 rowed at F9; D-3+C-5 demoted; header + stamp re-derived) · R3-3.9 purge share (D-3 carry heading paste completed or elided honestly · D-4 markup-in-verbatim) | **2** |
| **F-W9** | R3-3.5+R3-3.7 (E-9(g) cured: `:354` converted + form-agnostic probe pasted; the D/i-1 quote re-scoped as a PAIRED edit with F-W10, G-F9-21's face updated) · R3-9.5 (`$X` base declared, notes rooted) · R3-10.5-adjacent residue (D3's 36/44 decomposition stated inline or struck · D4's N-2 clause per R3-7.3 · D6/D7/D8 riders) | **3** |
| **F-W10** | R3-10.6 (the fresh drain derivation RUN; PP triple homed with terminal verbs per R3-2.4; ten strict members per-id verbed; predicate/roster reproducing; band disclosure corrected — with R3-5.5) · R3-6.1 (drain rows stand as the ONE home for AA-45/46/47; the F-W5 citation direction noted at this end) · R3-10.2 (E-4 discharged; E-4/E-17 self-contradiction resolved) · R3-3.9 purge share (MISSED-2 record-qualified · the paired D/i-1 edit lands here in the same commit as F-W9's) | **4** |

**Program seats**: the **TWIN SEQUENCE** (F-W3 then F-W4, serial, exclusive — R3-1) · the **PURGE SEAT** (R3-3.8–10, runs LAST over all 11 settled specs, publishes its per-spec transcript).

*Rulings complete: 10 (R3-1..R3-10). Read-only everywhere except this file.*
