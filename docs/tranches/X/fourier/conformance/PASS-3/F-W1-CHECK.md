# F-W1-CHECK — PASS 3 · FRESH ADVERSARIAL SPEC CHECK (L-18/L-20)

**Wave**: F.W1 (X·F) · **Spec under trial**: `docs/tranches/X/fourier/waves/F-W1.md` (post-repair-round-2 + re-anchor sweep) · **Date**: 2026-08-28 · **Seat**: FRESH adversarial checker, pass 3 — every figure below re-derived from the corpus and the trees by this seat this session. **No roster inherited**: PASS-1/PASS-2 rosters and RULINGS/RULINGS-2 were read as *inputs to the trial*, never as findings adopted.

**Corpus authority**: the 66 `fr-*.md` at `docs/tranches/V/megatranche/registry/adjudicated/` (frozen `35fc8ebf`). **Carries in tree** (`ls`): `carry/F-W1-CARRY.md` + `carry/F-W4-CARRY.md` — the only two; the spec cites only F-W1-CARRY, by path (R-3 conformant).

**Verdict**: **DEFECTIVE** — 5 id-keyed escapes + 2 MAJOR structural defects (G20 leg (b)'s LHS is a check file, not the corpus, contra R2-9; a round-2 ⟨cmd⟩ receipt that no longer reproduces at its own bytes) + 4 MINOR + 3 INFO. **The repair is otherwise real**: all 11 PASS-2 escapes are cured by bytes, every ⟨cmd⟩ note in the file reproduces except one, every born-RED witness is live and RED, and the R2-5 TWELVE-limb cure is exact at its pinned coordinate.

---

## §0 Method (id-keyed; DETECTOR LAW honoured; every command re-run this seat)

1. **SPELLING PROBE** — `F[.·–-]\s?W1(?![0-9])` in **both** U+002D and U+2013, plus every slash form containing `W1`. **Measured, this seat**: `F.W1` → **845 occurrences / 679 lines / 66 records**; **en-dash forms of the W1 token → 0** (a measured negative, kept in the probe per R2-3a.3); `F·W1` → 0; `F.W0/W1` → **2 lines** (`fr-EquationResult.md:62`, `fr-UserSlugBar.md:62`); `F.W1/W2` → 5, `F.W1/W3` → 1 (both slash forms expanded to their member sets). Non-routing substrings disposed inline: `M.W1` (26 — the `M.W1a` working-tree branch), `AN.W1` (2 — a producer comment inside a dist sheet). **Total mention lines = 681.**
2. **ROW-SHAPE DECREE (R2-3a.1) applied**: table rows ⊕ id-headed route-marked bullets ⊕ prose routings walked back to the enclosing row id (S-8). No shape restriction anywhere in this census.
3. **ROUTING position**: the F.W1 token sits in the row's **terminal-disposition** segment (last table cell; or the bullet's disposition tail after `→` / `⇒` / `ADJUDICATED →` / a bolded disposition clause). **Excluded and counted**: 222 lines where the token is not in terminal position; 19 routing-law preambles (`Routes:` / `Dispositions:` / `Routing:`); 19 verdict / fold-summary paragraphs; 11 numbered scope-notes; 6 non-row prose lines. **= 277 excluded, 404 routed.**
4. **DEDUPE** by (record, banked row id) — the id-keyed denominator. **Membership by bytes** against `F-W1.md`, then `F-W1-CARRY.md`, then a declared FOLD/KILL to an identity the spec carries. Every miss hand-adjudicated against all eleven wave specs (one-home law, R-2/R-5).

## §1 Arithmetic

| quantity | value |
|---|---|
| F.W1 mention lines, 66 records (both dash spellings + all slash forms) | **681** |
| non-terminal / preamble / verdict / scope-note lines excluded | **277** |
| **ROUTED to F.W1 — id-keyed, deduped, shape-agnostic** | **404** |
| booked — id present by bytes in `F-W1.md` | **362** |
| booked — id present only in `carry/F-W1-CARRY.md`, under an identity §2 carries | **8** |
| id absent from both — hand-adjudicated (declared FOLD / KILL / measured-zero to a carried identity · record-qualified numeric rows · ruled-disagreement rows whose subject is carried) | **29** of 34 |
| **BOOKED total** | **399** |
| **ESCAPED** | **5** |

**PASS-2's 11 escapes are ALL CURED, verified by bytes this seat**: `fr-ExportModal D-M3` (§2·R2a.1, record-qualified against WU-C's homonym) · `CP-ROW-29` (§2·R2a.2) · `fr-BasisCanvas D-corpus-C-1` (§2·R2a.3) · `fr-FunctionInput N-1` (§2·R2b.1) · `fr-EasingPicker MISSED-F` (b.2) · `fr-PaperSidebar M3` (b.3, landed as G5's PRE-GATE LAW) · `fr-GalleryMarquee GM-19` (b.4, landed as G5's RE-READ OBLIGATION) · `fr-PaperView C-06` (b.5) · `fr-NotationPills FR-NP-30` (b.6) · `fr-VisualizationView §4` bypass (b.7) · `fr-ConvergenceTimeline D·D-5 + C·C-5` (§2·R2c, cited-not-booked per R2-3d.4). PASS-1's 15 remain present.

**Note on the denominator's growth (357 → 404)**: PASS-2's routing classifier admitted only rows whose terminal cell *assigns* F.W1; this seat, under the R2-3a shape decree and the "rows in ANY format" law, also admits rows whose banked disposition names an **F.W1 leg/rider/gate/benefit** beside another wave's assignment (the shape §2·R1c and §2·R2b already treat as routed). The 47-row delta is entirely that class. **This is the operand G20 leg (b) does not state — see D-1.**

---

## §2 ESCAPES (named by bytes) — 5

| # | record:line | id (banked) | grade | what escaped | receipt (this seat) |
|---|---|---|---|---|---|
| 1 | `fr-PaperArticleWindow.md:208` | **K-20** | **MAJOR** | The kill row's surviving product is expressly *"a correction to a supporting cell of the **fr-GalleryCardModal F.W1 ledger**"*: at 4.0.0 the coarse floor is scoped to exactly three selectors, none Button; **at v7.0.0 no floor rule exists at all** — the coarse treatment is the opt-in `touch-hit-area` / `[data-control-target]` path. | `grep -c 'K-20' waves/F-W*.md` → **0 everywhere except F-W3 (5), and all five F-W3 hits are `rK-20`** (fr-GlassTimeline's homonym, verified by context). Unhomed program-wide. **Confirmed at the producer bytes**: `git show v7.0.0:src/styles/utilities/a11y-overrides.css` carries **no** `@media (pointer: coarse)` floor block (its own header calls it *"the size-agnostic touch-hit-area utility"*), while `v7.0.0:src/styles/utilities/responsive.css:3-8` keys `[data-control-target]` only. The correction is load-bearing on §2·R2a.1 (see D-4). |
| 2 | `fr-UserSlugBar.md:47` | **FR-USB-10** | MINOR | Banked *"→ **F.W4** (consume the result or pass `onCopyError` → toast) **+ F.W1** (the same `:23` destructure breaks at 7.0.0 — FR-USB-39/FR-EQR-1)"* — an explicit two-wave routing with a named F.W1 leg. | `grep -c 'FR-USB-10'` → `F-W1.md:0` / `F-W1-CARRY.md:0`; **`F-W4.md:2`** (booked inside the SP-8/SP-7/SP-12 extraction row). F.W4 is the adopting home and F.W1 never answers the leg — the mirror image of the `MISSED-F` partition hole PASS-2 convicted, uncured because it was never in a check's roster. FR-USB-39 and FR-EQR-1 are both carried; only the id is missing, so the reciprocal cannot be run id-keyed. |
| 3 | `fr-SliderControl.md:43` | **R-14 = D-16** (its F.W1 half) | MINOR | Routed *"→ F.W3/W4 (+ **F.W1 benefit rider**: `controlLabelable`/`describedBy` at v7.0.0 labeled-field, DU-verified)"*. | `grep -c 'controlLabelable'` → 0/0; `grep -c 'describedBy'` → 0/0. The wave keeps an explicit **benefit ledger** (M-TL · `fr-EquationView D·D-M3` · MPC-32 · FR-IC-17 · FR-CP-5 · `fr-ExportModal D-M3`) and this banked benefit is not in it — the anti-rename defect M-25 names, in the one register built to hold it. |
| 4 | `fr-PaperArticleWindow.md:303` | **K-24** | INFO | *"the **F.W1/F.W2 blast-radius argument rests on it** — the inventory must be exact"*: 4 distinct glass-ui tokens across 6 sites, `:205`'s hover box-shadow omitted entirely from §0's self-declared ground truth. | `grep -c 'K-24' waves/F-W*.md` → **0 in all eleven**. The blast-radius premise F.W1's `/paper` sizing inherits is corrected nowhere. |
| 5 | `fr-ConvergencePlot.md:159` (⊕ `:70`) | **SC-3 `valueText` amendment** | INFO | *"the SC-3 `valueText` relay amendment (ask → **adopt-at-F.W1**)"*; `:70` records the paired fact *"the relay ask is now shipped at 8.0.0"*. | `grep -c 'valueText' / 'aria-valuetext'` → 0/0 in spec and carry. The only `SC-3` hits in `F-W1.md` are substrings of `ESC-3` (5, all verified). A relay ask that SHIPPED converts to a benefit-ledger adoption; the ledger does not hold it. |

**Adjudicated NOT escapes (receipts kept)**: `FR-EQR-24` + `FR-USB-22` (the two `F.W0/W1` rows) — homed at F.W0, `grep -c` → `F-W0.md:2` each · `fr-PaperSidebar C-m9` (`F.W0 dep placement + F.W1 rename`) — F.W0-homed; the rename arm is an instance witness inside WU-N's 35-site identity (anti-rename law) · `fr-ImageUpload D:U-2` (row 28, *"→ F.W1 input"*) — booked at WU-Q's measured-zeros cell · `fr-ContourPreview` rows 4/15/23/27/30/37 — record-qualified in G10, §6 and §6·R1 · `fr-MorphShapePreview L-05`, `fr-SvgFilters C-M4`/`C-i2`, `fr-GlassTimeline rK-24`, `fr-FullscreenViewer K-7`, `fr-PathPreview K7`, `fr-AnimationControls R-14`, `fr-ExportModal R2-1` — declared folds/kills to identities the spec carries (`FR-COB-13` · `L-i3/C-18.1` · `C-1` · `MPC-21/BR-1` · `AA-2` · `PP-REDGATE` · `C-8` · `D-M3/K-16`) · `fr-GalleryCardModal GCM-26` and `fr-HarmonicLevelGrid HLG-12` — **route F.W3/W4 and F.W3 respectively** (HLG-12 expressly *"Today-fixable, not F.W1-gated"*), so neither is F.W1's to book; both are mislabelled BOOKED in `PASS-2/F-W1-CHECK.md` §4, in opposite directions · `fr-ImageUpload` rows 2/8, `fr-PaperSearchInput` rows 1/6/10, `fr-PaperSearchDropdown` row 2 — **ruled-disagreement table rows, not verdict-register rows**; their subjects (`D:U-3`→`fr-ContourPreview 23`, the G7 budget, `C-3`, `D:I-2`) are all carried.

---

## §3 DEFECTS BEYOND THE CENSUS

### D-1 · MAJOR — G20 leg (b) states a CHECK FILE as its operand, which R2-9 struck by name for the sibling gate

G20's leg (b) reads: *"the routed id-keyed census of `conformance/PASS-2/F-W1-CHECK.md` §4 (**357 routed rows**; the operand re-based from the PASS-1 §3 census at repair round 2 …) ⟷ this spec"*.

RULINGS-2 **R2-9** ruled the operand class program-wide — *"every closure/census gate's operand is the shape- and spelling-agnostic roster … A gate that states a shape- or spelling-restricted operand is DEFECTIVE at authoring, whatever its result"* — and applied it by name to the structurally identical sibling: *"**F-W10 G-F10-7** — strict-roster basis = the **corpus-derived** 81 (R2-6b), **not any check file**."* G20 does exactly what G-F10-7 was forbidden to do: it pins its LHS to one seat's enumeration inside a superseded pass.

The consequence is measurable, not theoretical. This seat's fresh corpus-derived roster is **404** id-keyed routed rows under the R2-3a shape decree — **47 rows outside G20's declared LHS**, and **three of this pass's five escapes live in that 47** (`fr-PaperArticleWindow K-20` · `K-24` · `fr-ConvergencePlot SC-3`). A gate whose operand is a check file can only ever re-find that check file's misses; it is structurally incapable of greening against the corpus, which is the thing G20 claims to test. **Cure**: leg (b)'s LHS becomes the 66 records directly, with the detector stated INLINE (both dash spellings · all slash forms expanded · table ⊕ bullet ⊕ prose-walked-to-row-id), per R2-9's closing clause. The check files may be named as *prior runs*, never as the operand.

*(Aggravation, disclosed: G20's own falsifier language says the gate "stays RED … a THIRD independent run against the repaired bytes is the only thing that can green it". This IS that run, and it convicts the operand rather than the repair.)*

### D-2 · MAJOR — a round-2 ⟨cmd⟩ receipt no longer reproduces, and the repair that broke it is in the same file

§2·R1a's `fr-SvgFilters D §3 U-3` cell (the PASS-2 D-4 in-place cure) pastes:

> ⟨cmd⟩ `grep -c 'D §3 U-2' F-W1.md carry/F-W1-CARRY.md` → `F-W1.md:1` **(this citation itself)** / `carry/F-W1-CARRY.md:0`

Re-run at the live bytes, this seat: **`waves/F-W1.md:2`** / `carry/F-W1-CARRY.md:0`. The second hit is **§E-3·R2's own erratum row** (*"It claimed `D §3 U-2` is 'already carried' …"*), written by the SAME round that pasted the receipt. Both the figure and the parenthetical *"(this citation itself)"* are false at the bytes.

This is R2-1-LAW's failure mode one layer past the one §E-3·R2 already minutes (*"a ⟨cmd⟩ note that does not run is a 'verbatim' label wearing a costume"*): the command runs, and returns a different answer than the one pasted beside it. A self-descriptive count inside a file that is still being edited is brittle by construction — the file's own anchor-sweep row diagnoses exactly this hazard (*"any count word here would be self-invalidating the moment the row is edited"*) and then leaves this one standing two hundred lines above. **Cure**: state the classification, not the count (the idiom the anchor-sweep row already adopted for itself), or re-run the receipt last.

### D-3 · MINOR — the anchor-sweep row's own enumeration is wrong by one site

§E-3·R2's last-line-cite row asserts the sweep's hits fall in *"exactly two non-address classes, and no third"*, class (i) being the struck coordinates *"which occur **ONLY** inside the erratum sentences that strike them **(this row and the F.W0 row above)**"*.

⟨run, this seat⟩ `grep -noE '(F-W[0-9]+|KF-W[0-9]+|COHESION|CENSUS-2026-08-03|F-W1-CARRY|F-W4-CARRY)\.md:[0-9]+' F-W1.md` returns `F-W1-CARRY.md:246` at **three** sites: `:348` (§2·R1d's checkpoint-set row), `:461` (this row), and `:453`/`:461` for `F-W0.md:199`. `:348` is neither *"this row"* nor *"the F.W0 row above"*. The **class** characterisation survives (348's sentence does strike the coordinate); the **enumeration** inside the parenthesis does not, and it is the enumeration a later seat will check. Same shape as D-2: a self-descriptive claim written before the file stopped moving.

### D-4 · MINOR — §2·R2a.1's D-M3 discharge is asserted unconditionally over a floor that is OPT-IN at ≥7

The adopted row says: *"**no work, one line**: the producer's ≥7 control floor makes the ✕ 44×44, so the geometry MAJOR discharges at the bump and is **never budgeted** and never credited as a cure."*

At the producer bytes, this seat: the ≥7 floor is **not automatic**. `v7.0.0:src/styles/utilities/responsive.css:3-8` = `@media (pointer: coarse) { [data-control-target] { min-block-size: var(--touch-target, 2.75rem); … } }`, and `[data-control-target]` is emitted **only when `iconOnly`** — `v7.0.0:src/components/button/Button.vue:92` = `:data-control-target="iconOnly ? '' : undefined"`. (Both anchors are exactly as G8 cites them; G8 is correct and this row is what disagrees with it.) The 4.0.0 automatic block (`[data-size="icon"]` + two others, `a11y-overrides.css:115-121`, this seat's read of the installed dist) is **gone at v7.0.0** — the fact `fr-PaperArticleWindow K-20` banks and escape #1 shows is homed nowhere.

So the discharge is a **consequence of G8's `iconOnly` sweep inside limb 2**, not of the bump; and it further presumes the ✕ survives `fr-ExportModal M-α`'s `dismiss="deliberate"` re-spelling (WU-F, §5). Neither dependency is named, and the banked producer verification the row quotes (`558c3fa3`) is a post-v7-tag HEAD — an 8.0.0-era artefact under an **unruled ESC-1**. R-4 sibling (iii)'s law — *"no cure-shape presumes the unruled branch"* — is not honoured here. **Cure**: gate the cell on **G8** and on **ESC-1/G1** by name, and carry K-20's correction beside it.

### D-5 · MINOR — G10's twin range stops one line short, and the spec spells the same range two ways

G10: *"`CollapsibleSection.vue:60-65` AND its **`ContourSettings.vue:361-374`** twin die IN THE UPLIFT COMMIT."* §2·R1c's `fr-CollapsibleSection m-7` cell: *"The `ContourSettings` fork (`:255-265` + **`:361-375`**)"*.

At the tree, this seat: `.advanced-content { overflow: hidden; }` = `:361-363` · `[data-state="open"]` pair `:364-366` · `[data-state="closed"]` pair `:367-369` · `@media (prefers-reduced-motion: reduce)` arm `:370-375`, whose media block closes at **`:375`**. `:361-374` under-describes the block by its closing brace — **byte-for-byte the class PASS-2 D-7(ii) convicted** (`style.css:40-49` → `:40-50`) and that this round cured at `style.css` and at WU-O only. Two cells of one spec giving one range two spellings is the second half of the defect: G10's five-surface close witness is scoped by the shorter one.

### D-6 · MINOR — the R2-5 charter is correct here and half-applied in the siblings the note speaks for

`:276` is exact: twelve top-level `+` conjuncts (producer bump · 162-attribute rewrite · `copied`→`status` · lucide rename · G10 deletions · FR-EQC-7 vaul-vue gate · G13 manifest/lock · PP-REDGATE ambient · FR-CP-13 gap · M-γ deletion · GCM-22 `p-0` · pencil-boil floor), count word **TWELVE**, warrant named, G5's disclaimer re-denominated to *"not a THIRTEENTH limb"* — R2-5 executed verbatim. ✓

But §2·R1's placement note asserts as fact that *"siblings re-quote the corrected charter and **no sibling re-derives the count**"*. ⟨run⟩ `grep -c 'ELEVEN-limb' F-W*.md` → **F-W10 2 · F-W7 2 · F-W5 1 · F-W6 1 · F-W9 1** (and `TWELVE-limb` present in all of them too). Five siblings carry **both** count words for a roster nine specs cite *whole*. The rows are outside F.W1's write scope; the **assertion** is F.W1's, and it is false at the bytes. Restate it as the directive R2-5 made it, not as a description.

### D-7 · INFO — three census-shape residues

(i) `grep -c 'D-corpus' F-W1.md carry/F-W1-CARRY.md` now returns `2 / 0`, not the `0 / 0` §2·R2a.3 pastes — **correctly disclosed** in the cell as *"at the pre-repair bytes"*, so no conviction; recorded only because it is the one instance of the D-2 shape that the round handled right, and the contrast is the lesson. (ii) `PASS-2/F-W1-CHECK.md` §4 mislabels `GCM-26` and `HLG-12` as booked while both route away from F.W1 and neither id is in the spec — a check-file error G20 leg (b) currently inherits as its operand (see D-1). (iii) `fr-PaperSidebar C-m9`'s F.W1 rename leg is carried only as an unnamed instance of WU-N's 35-site identity; lawful under the anti-rename law, unverifiable id-keyed.

---

## §4 WHAT VERIFIED CLEAN (adversarial checks that failed to convict)

**QUOTE REALITY — every ⟨cmd⟩ note in the file re-run by this seat.** 50 provenance notes; **49 reproduce byte-exact**, one fails (D-2). Reproduced verbatim, this session:

- **Corpus (frozen `35fc8ebf`)**: `fr-MorphPhaseConfig` MPC-31 ONE-CUT LAW (the whole banked sentence, `MPC-8's re-domain` — the `.` in the printed pattern is the necessary shell-quoting wildcard and the pasted OUTPUT is the true bytes) · `fr-PaperArticleWindow` **LAW-3** = *"PAW-44 restoration lands **WITH-or-AFTER** PAW-1 + PAW-30, never before."* (the round-2 un-tightening is real and correct — PASS-2 §3 declined to convict this and was wrong to) · `fr-PaperSidebar` M3's one-command discriminator · `fr-EquationResult`'s **both** spellings of 162 (`-attribute` and `-site`) · `fr-CanvasControlsDock` K-9 · `fr-SvgFilters` "disgusting metallic" + `:36`'s whole row · `fr-PaperSearch` D-14/D-15/D-17 · `fr-ExportModal:51` + K-16 · `fr-ContourPreview:62` · `fr-BasisCanvas:105` · `fr-FunctionInput:54` · `fr-EasingPicker:89` · `fr-GalleryMarquee:192` · `fr-PaperView:70` · `fr-NotationPills:74` · `fr-BasisSelector:95` + M-1 ×2 · `fr-VisualizationView:103` · `fr-ConvergenceTimeline:56`.
- **Live siblings, by `grep -o` (no coordinate in the output — the R2-2 idiom honoured)**: F-W2's `home **F.W1** costing + **F.W4** hygiene` and its `CITES.` sentence · F-W4's `GM-19 is never certified` and the `CP-ROW-40` ×6 usage count (`CP-ROW-` total 7 = 6 + the bare template token, exactly as stated) · F-W10's bypass-half sentence and `C-06's ComputedRef<any> rider` · F-W0's pipe-free `SUBSTRATE-LEDGER.md` **create** row (**the fix works**: the pattern carries no pipe, the row renders, and the output is the ledger row).
- **Carry**: cross-edge 9's row label and NWO-1's roster label both reproduce.
- **§6·R1's 42 carry-fold ids**: script-verified — **all 42 coordinates resolve, all 42 carry their id AT that line, and all 42 lines carry the `F.W1` token. 0 mismatches.**

**GATES BORN-RED, WITNESSES REAL** (live read-only measurement, this seat, `/Users/mkbabb/Programming/fourier-analysis` + `/Users/mkbabb/Programming/glass-ui`):
`--slider-scrub` → **23 declarations / 7 files**, and the seven are byte-for-byte §1's re-resolved list (G9 ✓) · `text-admin-label` → **7 / 4** (G5 ✓) · `.cartoon-card` → 25/15 raw, minus the 4 shim lines in `style.css` ⇒ **21 application occurrences / 14 files** — the census of record is exactly right (G5 ✓) · `.btn-pill` → the two named sheets **0/0**, the class emitted from **EIGHT** producer artefacts, and the eight names match the pasted list one-for-one (G5's LAW ✓, and the round-2 "seven"→"eight" correction is the right number) · `CoefficientsSpectrum.vue:19` animated-digit · `InfoCard.vue:4` metric-badge (G11 ✓) · `easings.ts:58` `as EasingFn` (G12 ✓) · `GlassTimeline.vue:73` `@pointerdown="onPointerDown"` (G18 ✓) · `CollapsibleSection.vue:60-65` animation pair (G10 ✓) · `a11y-overrides.css:115-121` = the 4.0.0 `[data-size="icon"]` coarse block, **scoped to exactly three selectors, none Button** (G8 ✓ — and K-20's 4.0.0 half confirmed at the same read) · `v7.0.0:responsive.css:3-8` + `Button.vue:92` (G8's ≥7 half ✓) · installed producer = **4.0.0** · `dist/styles/index.css` carries the fold-block and the `@source`-in-prose that yields `Unterminated string: 's own'` (G2 ✓) · `scripts/e2e.sh:102` pasted whole and byte-exact (G6 ✓) · `docs/tranches/X/coordination/` holds exactly ONE file, `docs/tranches/X/fourier/evidence/` does not exist (G15 born-RED ✓) · ESC-7's live measure **30 sites / 16 files** against `ghost`'s **49** (G19 ✓) · `variant="default"` → **4** sites and the four files match exactly; `variant="solid"` → **0**; the emphasis/iconOnly probe → **0 under BOTH readings** of its printed `\|` (§2·R2b.1 ✓ — the ambiguity that broke G1's ⟨cmd⟩ does not bite here).

**M-25 DEPTH — locks, riders, dissents, anti-cures**: `FR-NP-32 (≡ fr-PaperSidebar M1)` carried in the canonical R-8 form at NWO-1's top, in G2's premise, and at §2·R1d ✓ · **PAW-44/LAW-3 now carried as its bytes**, with the round-1 tightening (`only WITH`) struck and **PAW-30 restored as the second named predecessor** ✓ · **MPC-31**'s ONE-CUT LAW un-compressed and un-fused ✓ · **FR-MSP-6**'s two-channel lock and the PaperSearchModal same-commit triple excluded at §5 with the correct reason (their rows land F.W3/W4; PSM-36 certifies the sole F.W1 exposure) ✓ · same-commit riders (MPC-13 · MPC-22 · D-14 · G10's HOST-imports-first ordering · MPC-10's contrast leg in the breaking direction) all carried ✓ · **anti-cures** carried as kills with receipts: KILL-6 (`<Card tier surface="cartoon">` unspellable, 21/14) · K-5 (`color` getter worsens the surface) · MG-ι (`color2` both forms) · C-M7 (`./confirm-dialog` manufactures a break) · GAB-29 (MetricPill barrel-only, no source at v8) · K-12 · K-9 · FMD-21 (CVA type-level hard edge) ✓ · dissents preserved without being converted to budget (reader-δ's MAJOR revives *iff* the variant row is priced as a rename; FR-GIG-5's three BLOCKER dissents ride NWO-1; LC's FR-CP-33 MAJOR carried as dissent) ✓ · **the twin law of RULINGS-2** (R2-1-LAW quote-by-command + R2-2 stable anchors) is declared in the masthead, applied from §2·R2's first byte, and self-audited at §E-3·R2 — the second sitting genuinely caught six residues the PASS-2 check did not ✓.

**CLOSURE OPERANDS, ROW-SHAPE TOLERANCE**: G5's operand is explicitly *"the WHOLE emitted roster at the adopted pin, never a two-file subset — a shape-restricted operand is defective at authoring whatever it returns (R2-9)"* ✓ · G7 states SIX figures with the counting unit beside each and refuses averaging ✓ · G20 leg (a)'s LHS is the CARRY's 105 row ids, never the unresolved `fr-NAME:LINE` anchor family ✓. **Leg (b) is the exception and is D-1.**

**POSTURE**: `Status: planned` ✓ · four-verb table `VERIFIED = NO`, stamped only at sub-tranche close ✓ · the five uppercase `VERIFIED` tokens are G12's gate name, the four-verb row, and three quotations of PASS-2 §3's heading — **no verification verb stamped anywhere** ✓ · the transaction cited whole at `:276` with the **correct TWELVE-limb count** and the `:294` warrant named ✓ · all four RULINGS-pinned coordinates intact and correct at the bytes (`:161` WU-K `L/B-1 + C/B-1` · `:227` ESC-4 · `:276` the roster · `:294` the vaul-vue-INSIDE pin) — the append-below-`:294` discipline worked across two rounds ✓ · **F.W0 pre-gates honoured**: §4 step 2 RECORDS F.W0's discharges and does not perform them; the DECLARE+LOCK landing is F.W1's per R-4a; G-11/G-12/G-13 cited **by gate id only** ✓ · **F.W9/W10 splice direction per R-2b is correct** — §2·R1d: F.W9 home, F.W10 terminal-verify, F.W1 mints the set at cross-edge 9 and books no checkpoint row ✓ · **SS-4 flags inline**: cross-edge 10 declares the NON-edge and expressly leaves the trie-vs-KISS escalation at SS-4's own spec ✓ · **fourier + glass-ui trees READ-ONLY throughout this check** (grep/`git show` only; zero files opened for write; this register is this seat's sole write) ✓ · **RULINGS-2 faithfully applied**: R2-5 ✓ (D-6 is the sibling half) · R2-3d.1–3 all three adopted with record-qualification ✓ · R2-2 ✓ (no load-bearing live-sibling coordinate survives; the two struck ones are subjects of errata) · R2-1-LAW ✓ except D-2 · R2-9 **partially** — declared and applied at G5, not at G20 leg (b) (D-1).

*(F.W7's ruled posture is not in scope here: F-W1 declares no F.W7 edge, and R2-8.1 closes the question against re-litigation. This seat did not re-derive it.)*

---

**Seat verdict: DEFECTIVE** — routed **404** · booked **399** · **escaped 5** (1 MAJOR · 2 MINOR · 2 INFO) · plus **2 MAJOR** structural defects (G20 leg (b)'s check-file operand contra R2-9; a ⟨cmd⟩ receipt broken by its own round) · **4 MINOR** · **3 INFO**. All 11 PASS-2 escapes and all 15 PASS-1 escapes are cured by bytes; 49 of 50 ⟨cmd⟩ receipts reproduce; every born-RED witness is live and RED; the TWELVE-limb charter is exact. **The escape class survived a second repair at a lower rate and in a narrower band — the three deepest escapes all sit outside the roster G20 declares as its operand, which is the finding that matters.** Read-only everywhere except this file.
