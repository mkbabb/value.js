# F-W0-CHECK — FRESH ADVERSARIAL SPEC CHECK, PASS 3 (L-18 / L-20)

**Subject**: `/Users/mkbabb/Programming/value.js/docs/tranches/X/fourier/waves/F-W0.md` (421 lines, 154,844 B; mtime 2026-08-28 16:58:39)
**Corpus authority**: the 66 `fr-*.md` records at `docs/tranches/V/megatranche/registry/adjudicated/`
**Real carries (the only two citable)**: `carry/F-W1-CARRY.md` · `carry/F-W4-CARRY.md` — `ls carry/` returns exactly these two; **there is no `F-W0-CARRY.md` and the spec correctly cites none**
**Seat**: FRESH — no roster inherited from `PASS-1/F-W0-CHECK.md`, `PASS-2/F-W0-CHECK.md`, `PASS-1/RULINGS.md` or `PASS-2/RULINGS-2.md`. Every census row, every quotation and every gate witness below was re-derived by this seat, live, read-only, against `/Users/mkbabb/Programming/fourier-analysis` and `/Users/mkbabb/Programming/glass-ui`.
**Date**: 2026-08-28
**VERDICT**: **DEFECTIVE** — census CLOSES **84/84, zero id-level escapes**; all 15 gates reproduce born-RED with real witnesses; **12 defects survive, 2 HIGH**. The measurement layer of this spec is sound to a degree the corpus rarely reaches. The *attestation* layer fails for the fourth consecutive pass, and it fails inside the register authored to close it.

---

## 0. Method

1. **Detector, stated inline (R2-9).** Routing tokens `F.W0` and `F-W0`, in **both** dash spellings (U+002D and U+2013), over **every row shape**: markdown table rows, id-headed route-marked bullets (`→` · `⇒` · `ADJUDICATED →` · `FOLD →` · `rides` · `routed` · bold-at-disposition), and prose routings walked back to their enclosing row id (S-8). Slash forms expanded to their member set.
2. **Measured spelling distribution.** `grep -hoE 'F\.W0' fr-*.md | wc -l` → **155 occurrences over 144 lines in 44 records**. `F-W0` spelling → **0**. En-dash spans containing W0 → **∅** (a measured negative, kept in the probe). Slash forms → `F.W0/W1` ×2, `F.W0/F.W1` ×5, `F.W0/F.W2` ×1. Bare `W0` not preceded by `F.` → 3 hits, all `H.W0/γ` in `fr-EquationPanel` (tranche H, not this lane).
3. **Row extraction.** 23 lines are routing-law/taxonomy preambles and 17 are closing-verdict routing tallies; neither shape books. The remainder were read for enclosing row id and terminal disposition.
4. **Carry-by-any-shape.** BOOKED = carried in F-W0.md as a §3 row, a named fold-identity inside a §3 provenance bracket ⟨…⟩, a gate operand or Owning-rows entry, a §6b cross-edge, or a §8 EXCLUDED line. Content-only carry with no id = ESCAPE.
5. **Quote reality.** Every ⟨cmd⟩ in the file — all 20 §3a register rows plus every inline note — was **executed by this seat**, and every quoted span was tested with `grep -F` against its named home. Nothing was inherited.
6. **Witness verification.** All 15 gate witnesses executed against the live fourier and glass-ui trees.

---

## 1. ID-KEYED CENSUS — 84 routed rows, id for id

**routedTotal = 84 · bookedCount = 84 · escapedCount = 0.**

| Record | Routed ids (banked F.W0 disposition) | n | Booked where |
|---|---|---|---|
| fr-AdminFlaggedPanel | FR-AFP-54 · FR-AFP-55 | 2 | row 10 fold list · **row 12** |
| fr-AdminUserList | FR-AUL-23 · FR-AUL-56 · FR-AUL-58 | 3 | **row 39** (OPTION) + §8 · row 10 · **row 33**/G-15(b) |
| fr-AnimationControls | M-15 | 1 | **row 37**, G-15(a) |
| fr-App | MG-θ | 1 | **row 15**, G-7 |
| fr-AppHeader | FR-AH-33 · C-14 | 1 | **row 10**, G-6 |
| fr-BasisSelector | i-3 (= LC-missed-7) | 1 | **row 16**, G-7 |
| fr-CanvasOverlayButton | FR-COB-1 · FR-COB-2 · FR-COB-3 · FR-COB-9 · FR-COB-10 · FR-COB-15 · FR-COB-17 · FR-COB-25 · FR-COB-28 | 9 | rows 3 · G-12 operand · 26 · 26 · 4 · 26 · G-10+§6b · 26 · **row 30** |
| fr-CoefficientsPanel | FR-CP-32 | 1 | row 15 fold list |
| fr-CollapsibleSection | F-2 (= C-5) · i-3 (= C-17) | 2 | row 10 · row 15 (+ the do-not-hand-fix rider) |
| fr-ContourSettings | C-13 · D-i1 | 2 | row 10 · **row 19**, G-9, §8 |
| fr-ConvergenceTimeline | M-6 | 1 | row 10 fold list |
| fr-EasingPicker | MISSED-D · C/m-6 (+ test leg) | 2 | **row 21**, G-13, §8 · row 18, G-9 |
| fr-EditorControlsDock | L-7 · M-10 | 2 | **row 20**, G-13 |
| fr-EqCoefficientsPanel | FR-EQC-7 (= C-1) · C-3 | 2 | **row 9**, G-6, §6b · row 10 |
| fr-EquationModeToggle | FR-EMT-25 | 1 | **row 24**, G-1 (named exception) |
| fr-EquationPanel | M-N14 | 1 | row 15 fold list |
| fr-EquationResult | FR-EQR-24 · FR-EQR-29 · FR-EQR-30 · FR-EQR-33 | 4 | **row 35** · row 10 · **row 14** · rows 2/20 + G-13 addendum (b) |
| fr-EquationView | C·D-11 · C·D-27 | 2 | **row 13**, G-13, §6b · row 10 |
| fr-FourierMorphSvg | FM-19 · FM-21 | 2 | **row 36**, G-15(c) · **row 33** (⟨cmd⟩-quoted) |
| fr-GalleryAdminBanner | GAB-13 | 1 | **row 2**, G-1, §5 |
| fr-GalleryCard | L·D-3 · L·D-6 | 2 | row 15 · row 16 |
| fr-GalleryFeaturedCarousel | FR-GFC-7 · FR-GFC-21 · FR-GFC-22 | 3 | **row 11**, G-6, §8 · row 10 · row 15 |
| fr-GalleryInfiniteGrid | L-8 · L-12 | 2 | row 15 · row 18, G-9 |
| fr-GalleryMarquee | GM-F6 (= L-19) | 1 | row 15 fold list |
| fr-GallerySearchBar | FR-GSB-6 · FR-GSB-31 | 2 | row 16 · row 10 |
| fr-GalleryView | FR-GV-28 · K7 | 2 | **row 34**, G-6 · row 15 |
| fr-InfoCard | FR-IC-1 · FR-IC-2 · FR-IC-8 · FR-IC-23 · FR-IC-25 | 5 | **row 4**, G-10 (first four) · **row 38** |
| fr-NotationPills | FR-NP-2 · FR-NP-4 | 2 | **row 5**, G-12 · **row 6**, G-12 |
| fr-PaperSearch | MISS-A7 · D-24 (≡ K1, ruling-6 rider) | 2 | **row 7**, G-5, G-15(d) · §6b SS-13 row + G-15(d) |
| fr-PaperSearchDropdown | C:S-2 · SS-13 residue 12 | 2 | **row 25**, G-1 · row 7 bracket (`fr-PaperSearchDropdown residue 12`) |
| fr-PaperSearchInput | C-3 · D-M3 · MISS-LC2 · residue 6 | 4 | row 10 · G-12 operand · **row 8**, G-6 · §6b SS-13 row |
| fr-PaperSidebar | M1 · C-m9 · M7 | 3 | **row 1** (canonical `FR-NP-32 (≡ fr-PaperSidebar M1)`) · row 10 · rows 15/18 |
| fr-PaperView | M-15 fold (LC's 4th reproduction) | 1 | **row 37** |
| fr-PathPreview | PP-DEAD · PP-LEN · PP-GATE · PP-REDGATE · PP-TSSCOPE · PP-NOTEST · K7 · residue 6 | 8 | row 15 · rows 16/18 · row 15 · **row 37**/G-15(a)/§8 · **row 17** · **row 18** · row 37 · **row 27** |
| fr-SliderControl | R-3 (= D-4) · R-20 (= C-14) | 2 | **row 19**, G-9 · **row 18**, G-9 |
| fr-UserSlugBar | FR-USB-21 · FR-USB-22 | 2 | row 15 · **row 35** |

**36 records route; 30 contribute no routed row** (their F.W0 hits are taxonomy preambles or verdict tallies): fr-AdminAuditLog · fr-BasisCanvas · fr-CanvasControlsDock · fr-CoefficientsSpectrum · fr-ContourEditorCanvas · fr-ContourPreview · fr-ConvergenceLegend · fr-ConvergencePlot · fr-DarkModeToggle · fr-EasingCurvePreview · fr-ExportModal · fr-FourierMorphDemo · fr-FourierShapeExtractor · fr-FrequencyGraph · fr-FullscreenViewer · fr-FunctionInput · fr-GalleryCardModal · fr-GalleryDraftsSection · fr-GlassTimeline · fr-HarmonicLevelGrid · fr-ImageUpload · fr-MobileFloatingToc · fr-MorphPhaseConfig · fr-MorphShapePreview · fr-PaperArticleWindow · **fr-PaperSearchModal** · fr-SpeedSelect · fr-SvgFilters · fr-Tooltip · fr-VisualizationView.

### 1a. Divergences from the roster the spec cites as its census authority

The spec's §3 header and §8 closure both rest on `PASS-1/F-W0-CHECK.md` §1's **82-row** roster. This seat's corpus-derived roster is **84**. The delta:

- **+ `fr-FourierMorphSvg` FM-21** — `sed -n '99p'` routes it as an "F.W0-adjacent question"; PASS-1 booked FM-19 only. F-W0 *does* carry FM-21 (row 33), so the roster undercounted a row the spec books.
- **+ `fr-PaperSearchDropdown` SS-13 residue 12** — prose routing, *"F.W0 re-checks before any SS-13 pass is scheduled"* (enclosing row = banked MISS-A7). Booked at row 7's bracket.
- **+ `fr-PaperSearchInput` residue 6** — a distinct numbered residue row PASS-1 folded into D-M3. Booked at §6b's SS-13 row by name.
- **− `fr-NotationPills` FR-NP-32** — the record's own disposition line routes **NO-WAVE-OWNER + F.W1 SEQUENCING GATE**, not F.W0 (`sed -n '35p'`, this seat). Its F.W0 identity arrives only through `≡ fr-PaperSidebar M1`, which PASS-1 counted separately; keeping both double-counts one identity. F-W0's row 1 performs the re-home under R-8 and says so — correctly.
- PASS-1 §1a also states "25 records" and then enumerates **29**, omitting `fr-PaperSearchModal` entirely (66 − 29 = 37 ≠ 36).

**PASS-2/F-W0-CHECK.md:115 independently states `routedTotal = 84 · bookedCount = 84 · escapedCount = 0`.** Two independent seats, one number. See defect 2.

### 1b. Anti-invention

Every structured id F-W0.md names was checked against the 66: **0 phantoms, 0 renames.** `M1`, `M7`, `K7`, `i-3`, `F-2`, `C-17`, `C-13`, `C-3`, `C-5`, `L-7`, `L-8`, `L-12`, `M-6`, `M-N14`, `C/m-6`, `GM-F6`, `L·D-3`, `L·D-6`, `C·D-11`, `C·D-27`, `C:S-2`, `C-m9`, `D-i1`, `D-24`, `K1`, `LC-missed-7`, `residue 12` all resolve to real, record-qualified banked ids at their cited homes.

---

## 2. GATES — all 15 born-RED, every witness reproduced by this seat

| Gate | Witness re-run | Result |
|---|---|---|
| G-1 | `git rev-parse --abbrev-ref HEAD` · `--short HEAD` · `status --porcelain \| wc -l` | `m/w1-bump-migration` · `cd26c65` · **28** (27 ` M` + 1 `??`) — **EXACT**, and the §2a-i enumeration is 28 items, set-identical |
| G-2 | the sole `??` row | `docs/tranches/N/valuejs-inbound-2026-07-27-facility19-migration-table.md` — **EXACT** |
| G-3 | `ls CLAUDE.md` · `ls …/F/coordination/INBOX.md` · `ls …/F/coordination/` · `head -1` ×3 | both absent; **three letters**, subjects byte-exact (`# F — operator-window runbook (F.W3 binding)` · `# F-T-N1 — coordination ASK: drop the legacy \`status\` field from \`FormattedPalette\`` · `# F — vhost-correctness spec (inv-22 binding; F.α first land)`) — **EXACT**, incl. the D-8 triage clause |
| G-4 | installed version · dist/src comment delimiters · `style.css:3` | **4.0.0** · dist **17/8** vs src **15/6** · `@import "@mkbabb/glass-ui/styles";` — **EXACT** |
| G-5 | `git ls-files web/dist` · `ls -ld web/dist` | **0** files; on-disk, dated **Jun 12 18:13** — **EXACT** |
| G-6 | both dep blocks · `peerDependenciesMeta` · lock entries | dependency/devDependency sets byte-identical to the cell; `vaul-vue`/`@lucide/vue`/`embla-carousel-vue` in neither block; lock `node_modules/vaul-vue` → **0**, `node_modules/@lucide/vue` → **0**; embla **optional** — **EXACT** (one enumeration slip, defect 11) |
| G-7 | `compilerOptions` count + names · flag grep · `scripts` | **13** keys in the stated order; flags → **0**; scripts = `dev/build/preview/test:e2e/test:e2e:ui`, no `lint` — **EXACT** |
| G-8 | `include` · tsconfig count · `ls web/e2e/*.spec.ts` | as stated; **8** specs — **EXACT**, and the banked ×3 anchors reproduce |
| G-9 | no unit runner in `scripts`; vitest in neither block | **EXACT** |
| G-10 | F8-REACH-01/02 rows; the 8 lift anchors | `ConvergenceTimeline.vue:61` = `:class="{ 'is-playing': playing }"`; `EditorControlsDock.vue:143/:148` = `'is-active'`; CanvasControlsDock ×5 — **EXACT**; `partial-prior-run.json` resolves; all five DELETE orders resolve at their stated `file:line` |
| G-11 | drift registers; "tree no commit holds" | **EXACT** — G-1 establishes it |
| G-12 | the contradiction set | spot-derived: 8 specs ✔ · `tsconfig.app.json` absent ✔ · the 27/24/21/19 ` M` chain arithmetically sound ✔ |
| G-13 | producer `package.json`; live HEAD | **8.0.0** ✔ · `rev-parse --short=8 HEAD` → **`eb2e9428`** ✔ (the drift receipt reproduces; the GREEN clause does not — defect 9) |
| G-14 | `/tmp/fourier-r4-files.sha256` · `~/.codex/worktrees` | absent ✔ · **`7e28` `9167` `d0be`** ✔; G-14's disposition set is set-identical to the intake's `### F.W0 · re-ground` block (R3-9 · R3-16+R4-11+R5-9+R6-10 · R4-12 · R5-3 · X-7 · the worktrees) — **COMPLETE** |
| G-15 | (a) committed lock + flag · (b) `style.css:40-50` · (c) FM-19 · (d) the two registry cells | committed lock resolves typescript **5.9.3** / vue-tsc **2.2.12** ✔; `noUncheckedSideEffectImports` → **0** ✔; both (d) cells present and mutually exclusive ✔ (one quote fails `grep -F` — defect 5) |

**15 of 15 RED, 15 of 15 witnesses real.** The non-destructive re-cut of G-15(a)'s falsifier (no `git stash` anywhere; read-only leg from `git show HEAD:…`) is correct and is the single best repair in the file: it removed the only witness that wrote the fourier tree, and it sequences behind G-1 per §6a lock 10.

---

## 3. POSTURE

| Requirement | Finding |
|---|---|
| F.W1 transaction cited whole, correct limb count | **PASS.** ⟨cmd⟩ `grep -o 'The roster is [A-Z]* limbs and stays [a-z]*' F-W1.md` → *"The roster is TWELVE limbs and stays twelve"*, and `grep -n 'The roster is' F-W1.md` → `276:` = **§4 Sequencing, intra-wave step 4**. Cited at 4 sites (§4 G-6 · §6a lock 5 · §6b · §8), always by §-heading, never by line. The `:294` warrant quote reproduces byte-exact. R2-5 fully applied. |
| F.W0 pre-gates honored | **PASS.** §6a lock 1 quotes COHESION §0a; ⟨cmd⟩ `sed -n '151,152p' COHESION.md \| tr '\n' ' ' \| grep -o '…'` reproduces the line-wrapped span **EXACT**, and the wrap is disclosed rather than papered over — the one ⟨cmd⟩ correction in this file that is fully honest. |
| F.W7's ruled posture declared | **PASS on substance.** §6b's F.W7 row states the hard dependency, the two consumed tables, the halt condition, "what crosses back: NOTHING", and *"F.W7's routing census is a measured ∅ and that ∅ is a finding, not a gap (R2-8, ruled CLOSED)"*. G-F7-7's quote reproduces at F-W7's bytes. R2-10.8 / R2-8.2(iii) discharged. **FAIL on attestation** — see defect 1. |
| F.W9/W10 splice direction (R-2b) | **N/A — no violation.** F-W0 owns neither side; §6b's F.W9/W10 row confines itself to the SEAT/FLOOR disjointness (R-5), declared identically at both ends. |
| SS-4 flags inline | **PASS.** §6b's SS-4 row carries the forward instruction verbatim (flag owner rulings INLINE; name TA-4 as prerequisite or re-scope explicitly). §5 flags OG-F1 and OG-V2 without deciding. |
| Tree READ-ONLY | **PASS.** fourier porcelain still **28** after this seat's work; glass-ui untouched; the only write is this file. |
| status `planned`, zero VERIFIED | **PASS.** `Status**: **planned**` at §1; §1a VERIFIED = **NO**; the only other `VERIFIED` is §421's correct statement that VERIFIED belongs to the X·F release close. |
| Anchor idiom (R2-2.5) | **PASS.** The anchor-stability sentence stands at §4's head. Live-sibling line cites in the body: **0**. The 3 surviving `F-W1.md:276` occurrences (2 lines) and the 3 `F-W0.md:199` are all in masthead minutes naming the addresses **as retired** — and the file's own counting-unit disclosure (`grep -c` → 2 vs `grep -o \| wc -l` → 3) reproduces **EXACT** at this seat. |
| RULINGS-2 applied | **PASS on all three F-W0 directives**: R2-2.5 ✔ · R2-10.8 ✔ · P2-10 (the EIGHT-HOUR delta re-rooted `formation/fourier/` → `coordination/`) ✔ — `ls formation/fourier/` returns four files, none of them the delta; the delta resolves at `V/megatranche/coordination/`. |

---

## 4. DEFECTS — 12, ranked

### D-1 · **HIGH** · The flagship round-2 strike rests on three receipts that all return the opposite at the bytes

F-W0.md asserts, at **four** sites — the masthead ▲ minute (§Amendment minute, round 2), §3a **Q20**, §3a's closing *"Found by the SECOND pass"* note, and §6b's F.W7 row ▲ — that:

- ⟨cmd⟩ `grep -c 'Genuinely owed' F-W7.md` → **0**
- ⟨cmd⟩ `grep -c 'each for its true reason' F-W7.md` → **0**
- ⟨cmd⟩ `grep -rln 'Genuinely owed' waves/` → **`F-W0.md` and no other file in the lane**

**This seat, live**: **1** · **1** · **two files (`F-W0.md` and `F-W7.md`)**. `F-W7.md:207` carries the phrase (F-W7 quoting F-W0's own strike back at it). Mtimes settle the excuse: `F-W7.md` **16:48:58**, `F-W0.md` **16:58:39** — F-W7 already held the phrase when F-W0 was last written.

The load-bearing *conclusion* survives — G-F7-11 never authored that sentence, and the strike was right to land. What fails is the class this file exists to close: **a ⟨cmd⟩ presented as run-this-round whose live output is the negation of the printed figure**, shipped inside the cure for exactly that, and published four times over as the file's most instructive lesson (*"did I RUN the command, this pass"*). Round 1 attested; round 2's first pass attested harder; round 2's second pass convicted the first — and its own conviction receipt is now false. Fourth pass, same class.

### D-2 · **HIGH** · §3 and §8 denominate over 82 against a check file, when the corpus roster is 84 and a sibling check already said so

§3 header: *"carrying the **82-row ID-KEYED census** at `conformance/PASS-1/F-W0-CHECK.md` §1"*. §8: *"the **82-row ID-keyed roster** … lands **whole** … the register now reads **82/82 by id** across §3's 39 agglomerated rows"*.

Three faults, compounding:

1. **The number is wrong.** This seat's fresh corpus enumeration is **84** (§1/§1a above). `PASS-2/F-W0-CHECK.md:115` — a document this amendment's masthead names as an input and claims to have cured all eight defects from — states **`routedTotal = 84 · bookedCount = 84 · escapedCount = 0`**, and its verdict line reads *"census CLOSES 84/84"*. The repair seat read a check measuring 84 and left both denominators at 82.
2. **The operand is a check file, not the corpus.** R2-9 rules exactly this basis defective for a sibling — *"F-W10 G-F10-7 — strict-roster basis = the corpus-derived 81, not any check file"* — and R-3.1's own words at §8's head are *"certified against the in-tree census"*. A check file is neither.
3. **No detector is stated inline.** R2-9's standing law requires every closure/census gate to name its operand's shapes and **both dash spellings**. §8's closure names none; its self-test is a single `grep -c 'FR-COB-3\|FR-COB-15\|PP-DEAD'`, which tests three ids and nothing about shape or spelling.

The closure is *substantively* sound — 84/84 by this seat's count, zero escapes — which is why this is a stamp defect and not a carry defect. But a closure whose denominator is two short and whose operand is a hostile register is not a closure; it is a citation of one.

### D-3 · **MEDIUM** · Row 30's *"The list, verbatim"* has no register row and diverges from the bank at two items

The nine-entry relay packet is introduced with **"The list, verbatim:"** and carries **no ⟨cmd⟩** — not inline, and not in §3a (Q10 covers only the trigger sentence, Q11 only the coin-flip pair). R2-1-LAW.2: *a "verbatim" label without a ⟨cmd⟩ note is a defect per se.* The label does not hold:

- item **(6)**: `grep -cF 'near-collision naming hazard (two utilities one letter-order apart in one file)' fr-CanvasOverlayButton.md` → **0**. The bytes read *"…hazard (**FR-COB-3's mechanism** — two utilities one letter-order apart in one file)"*. The un-marked elision deletes the **FR-COB-3 attribution repair round 1 restored under R-2c** — the id is dropped from inside a quotation in the same file that books it at row 26.
- item **(7)**: `grep -cF 'record DOCK-ACTIVE SATISFIED upstream — do not re-send' fr-CanvasOverlayButton.md` → **0**. Bank: *"record DOCK-ACTIVE satisfied upstream, do not re-send (FR-COB-11)"*. The spec re-cases, swaps the comma for an em-dash, and inserts *"a NEGATIVE ask"* into the parenthetical — an addition inside a verbatim label, which §3a's convention (2) expressly forbids (*"No word, figure, tense, version or parenthetical is ever normalized"*).

Items 0–5 and 8 reproduce; the packet's growth from seven to nine is honestly stated outside the quotation (Q10's cure held).

### D-4 · **MEDIUM** · Row 1's *"verbatim"* relay ask carries an un-marked internal elision

Spec: *"…NOT a version bump (no v8 dist exists to read: local dist is a 1,514-byte stub, dist untracked at every tag)."* `grep -cF 'dist untracked at every tag).' fr-NotationPills.md` → **0**; the bytes read *"dist untracked at every tag **— my reads**)."* §3a's Q2 command (`sed -n '35p'`) does return the line, but the displayed words are a **non-contiguous subset** of its output — precisely the failure §3a's own convention (1) defines and claims to have closed (*"A ⟨cmd⟩ whose output is a subset of the words displayed beside it is not a provenance note"*, applied here in mirror).

### D-5 · **MEDIUM** · G-15(d)'s PSM-24 witness quote does not survive `grep -F`

Spec, G-15(d) RED witness: *"**PSM-24 = D-14 (LATENT-at-the-adopted-pin, R-3)** … both intended icon registers collapse to one output on the first 4.0.0 build."* `grep -cF 'both intended icon registers collapse to one output on the first 4.0.0 build' fr-PaperSearchModal.md` → **0**. The bytes carry *"both intended icon registers **(:68/:77 vs Input's `h-3 w-3`)** collapse to one output"* — an elision **inside** the quoted span with the ellipsis placed elsewhere, so the reader is told the elision is at the head when it is in the middle. The companion `fr-PaperSearch.md:28` quote in the same cell is byte-exact with its ellipsis correctly placed, which makes the asymmetry a drift and not a convention. The spec's own law, stated three times (row 26, §6b, §7b): *"a quote presented as at-the-coordinate must survive `grep -F`."*

### D-6 · **MEDIUM** · §3a Q7's stated result misdescribes its own command's output

§3a Q7 Result: **"TWO sentences, not one"**. Row 26: *"⟨cmd⟩ `grep -n 'dies with the file' fr-CanvasOverlayButton.md` **returns both**"*. Live, this seat: the command returns **nine** lines — `:52 :53 :54 :58 :67 :68 :69 :74 :75` — of which **three** carry the `(**F.W0**)` form: `:52` (FR-COB-3, with `rm`), **`:58` (FR-COB-9)** and `:69` (FR-COB-15). `:58` and `:69` open byte-identically (*"dies with the file (**F.W0**);"*). The two-way split is right about the ids it names and incomplete on its own command: the register that exists to prove commands return their quotes prints a count word its command refutes.

### D-7 · **MEDIUM** · Row 30 addresses `fr-MorphShapePreview` by a section that does not exist

Row 30: *"(ii) the **§22 fold-admission** sentence"*. §3a Q11 keys the identical quote to *"`:22` (the fold admission)"*. The record's headings, `grep -nE '^#{2,4} ' fr-MorphShapePreview.md`, are named — Scoreboard · The incredulity seat's own work · Adjudicated defect roster · A…F · Killed-claims register · Superlatives · UNPROVEN-NEEDS-LIVE residue · DISSENT · Closing verdict — **there is no §22**; line 22 is item 3 of the rulings list. One file publishing two idioms for one address, one of which resolves nowhere, inside the cure for the coin-flip fabrication. The corpus is frozen so a line cite is permitted (R2-2.2(i)) — a section cite to a nonexistent section is not.

### D-8 · **MEDIUM** · Row 33 overturns the banked FM-21 instruction in the clause after quoting it, unminuted

⟨cmd⟩ `sed -n '99p' fr-FourierMorphSvg.md` → *"which scaling system owns control sizes is an F.W0-adjacent question **the F.W4 ticket should pose, not answer**."* — reproduced **EXACT** by this seat. The spec's very next clause: *"**F.W0 poses it** and rules the fork; F.W1 re-tunes; F.W4 executes per-component."* The bank assigns the POSE to the F.W4 ticket; the spec assigns it to F.W0, with no errata, no E-3 addendum, and no divergence minute — the discipline this file applies scrupulously everywhere else (`web/DESIGN.md:33` vs the ruling's `:34`; the `lane-frontend.md:69` strike; the R4-12 evaporation). An id may be re-routed; under M-25/E-1 it may not be re-routed silently one clause after being quoted against.

### D-9 · **LOW–MEDIUM** · G-13's GREEN clause still asserts a hash the same gate declares stale

GREEN: *"F.W0 records the hash live at wave open (**`207bf174` as of this authoring**)"*. Three paragraphs above, the same gate's D-6 drift receipt records `207bf174` as `HEAD~2` and measures ⟨cmd⟩ `git -C …/glass-ui rev-parse --short=8 HEAD` → **`eb2e9428`** — reproduced by this seat. *"As of this authoring"* was false at round-2 authoring. The MEASURE-AT-OPEN law and the PROSPECTIVE ADOPTED cell are the right cure and are undamaged; the residual round-1 parenthetical inside the GREEN clause is the one sentence the D-6 cure did not reach.

### D-10 · **LOW** · §3a's own basis count is unreproducible

*"This file carries **19 lines** bearing that label."* `grep -c 'verbatim' F-W0.md` → **24**. No command in the register returns 19, and none is offered — a bare count word inside the section authored to make counts reproducible.

### D-11 · **LOW** · G-6 / row 11's required-peer enumeration is incomplete

Spec: *"while `vaul-vue`, `@lucide/vue`, `cva`, `clsx`, `reka-ui`, `tailwindcss` are **required**."* Measured at `web/node_modules/@mkbabb/glass-ui/package.json`: **14 peers, 7 marked optional** (`@vueuse/core`, `embla-carousel-vue`, keyframes.js, pencil-boil, value.js, perfect-freehand, tw-animate-css) — the non-optional remainder is those six **plus `vue`**. Immaterial to the gate's outcome; the cell is written as an enumeration and is one short.

### D-12 · **LOW** · FR-COB-2's F.W0 limb has no id-keyed disposition

`fr-CanvasOverlayButton.md:41` routes it *"…**+ F.W0** (this file is the only member whose complete cure is deletion)"*. In F-W0.md the id appears **twice**, both as citations — a G-12 denominator operand (`[FR-COB-2/K-12]`) and a row-38 premise correction — and is absent from row 3, row 26 and G-10's fold lists, where every sibling FR-COB id is homed. Booked by absorption into G-10's deletion; the ledger row that would make M-25's *"a named cure with its locks"* true of it does not exist. Counted BOOKED here on the G-12-operand reading, symmetric with `fr-PaperSearchInput D-M3` in the same cell — but the asymmetry with its own family is the finding.

---

## 5. WHAT THIS PASS DID NOT FIND

Stated because a check that only accuses is a poor instrument. **No phantom authority, no invented id, no fabricated gate, no re-litigated ∅.** Every one of the 15 witnesses reproduced at the bytes, including the three that write nothing (G-4/G-5 producer-owned, G-14's evaporated residue). The R-4a re-cut (manifest landing moved to F.W1's TWELVE-limb transaction) is correct at both ends. The §6a lock-10 abolition of `git stash` removes the one act in the spec that would have destroyed rows 24/25 — the tree's only copies of two correct repairs. The census closes at 84/84 with zero escapes, which no earlier pass achieved. The F.W7 reciprocal is genuinely owed, genuinely written, and correct in substance.

**The pattern across three repair rounds and four readings is now unambiguous, and it is not a measurement pattern.** Every defect that has ever survived a pass on this file is an *attestation* defect: a quotation that reads better than its bytes, a count word with no command, a ⟨cmd⟩ whose output was never re-run against the words beside it. The file diagnosed this precisely at §3a — *"a repair seat trusts its own cures"* — and then shipped D-1 into the very sentence that says so. The cure is not another minute. It is that **no ⟨cmd⟩ pointing at a live sibling may be banked at all**: sibling receipts move, and a frozen count of a moving file is false the moment the sibling is edited. F-W0 already reasoned its way to this for G-F7-11's owed-status cell and then banked three sibling counts anyway.

---

## 6. VERDICT

**DEFECTIVE.** `routedTotal = 84 · bookedCount = 84 · escapedCount = 0` · **12 defects (2 HIGH · 6 MEDIUM · 4 LOW)**.

The wave is specified to a standard that would execute correctly today: bounds tree-derived and reproducible, 15 born-RED gates with 15 real witnesses, the M-25 carry closed by id with zero silent drops, every lock and dissent preserved, status `planned` and no byte touched. What it cannot yet certify is its own citations. **D-1 and D-2 are the round-3 repair's whole subject**: strike or re-measure the three `Genuinely owed` receipts at their live bytes, and re-denominate §3/§8 over the corpus-derived **84** with an inline detector, citing the 66 records and not a check file.

*Read-only everywhere except this file. No fourier byte, no glass-ui byte, no record byte, no sibling-spec byte was written by this seat.*
