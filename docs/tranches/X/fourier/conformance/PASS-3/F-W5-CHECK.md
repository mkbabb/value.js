# F-W5-CHECK — FRESH ADVERSARIAL SPEC CHECK (L-18/L-20, PASS 3)

**Subject**: `/Users/mkbabb/Programming/value.js/docs/tranches/X/fourier/waves/F-W5.md` (337 lines, 148,833 B, mtime 2026-08-28 16:43:21).
**Corpus**: the 66 `fr-*.md` at `/Users/mkbabb/Programming/value.js/docs/tranches/V/megatranche/registry/adjudicated/` (⟨cmd⟩ `ls fr-*.md | wc -l` → **66**, this seat). Real carries: `X/fourier/carry/F-W1-CARRY.md` + `X/fourier/carry/F-W4-CARRY.md` ONLY (⟨cmd⟩ `ls X/fourier/carry/` → exactly those two).
**Seat**: FRESH. PASS-1/PASS-2 registers and both RULINGS files were read for *what was ordered*, never for *what was found*. Every figure below is this seat's own run. This file is the seat's only writable path; `$F` was never opened.
**Detector (binding)**: token-bounded `F\.W5(?![0-9])` across all 66 records, BOTH `F.W5-W8` (U+002D) and `F.W5–W8` (U+2013), then S-8 walk-back to the enclosing roster row — **table rows ⊕ id-headed route-marked bullets ⊕ id-headed prose routings ⊕ bare-first-cell rows**; no shape in which a routing does not count (R2-3a.1 / R2-9). Set-difference by BYTES against `F-W5.md`, record-qualified where the token collides.

**VERDICT: DEFECTIVE.**
**routedTotal = 169 · booked = 169 · escaped = 0 · defects = 20 (0 BLOCKER · 7 MAJOR · 13 MINOR).**

The escape axis is CLEAN a third time — no adjudicated row routed to F.W5 is missing from this spec by bytes. What convicts at pass 3 is **(a) one-home law** (three banked NO-WAVE-OWNER rows double-homed against F.W10's drain), **(b) the census figure and the closure gate's own operand**, and **(c) a masthead source-coordinate defence that is false at 2 of the 7 ids it enumerates**. Two of the three are self-contradictions internal to the file; none is inherited from the prior passes.

---

## 1. ID-KEYED CENSUS — routedTotal 169 (the spec's 167 is understated by 2)

### 1.1 Detector reproduction (this seat's own run)

```
grep -rhoE 'F\.W5'        fr-*.md | wc -l  → 245 occurrences
grep -rho  'F\.W5-W8'     fr-*.md | wc -l  → 120   files → 26
grep -rho  'F\.W5–W8'     fr-*.md | wc -l  →  72   files → 24
union band → 192 occurrences / 49 records / 23 en-dash-ONLY
sole F.W5 (245 − 192)                      →  53
F.W5-bearing LINES                         → 240   across 54 of 66 records
```

The spec's §2 BAND-PROBE block reproduces **to the digit** (192/49/23). The only other `W5`-shaped tokens in the corpus are non-F (`P.W5`, `AX.W51/52`, `A.W5.c`, `C.W5-retired`) and one slash form `F.W2/F.W5`; no `F.W3–W4` exists in either dash spelling (measured negative, as decreed).

### 1.2 Roster derivation

This seat's own shape classification of the 240 lines (leading token of each): **70 markdown table rows · 98 id-headed bullets · 72 non-row lines** (lane-taxonomy mastheads, closing-verdict paragraphs, fold-notes and roster tallies). Of the 72 non-row lines, exactly **six** are the `Routing law:` boilerplate and the rest divide into per-record "Routes: …" taxonomies, closing verdicts, and fold/coverage notes.

| class | n | provenance |
|---|---|---|
| F.W5-bearing lines | **240** | this seat |
| enclosing contexts | 216 | reproduced from the spec's cited operand (PASS-2 §1.2); this seat did not re-derive the merge rule |
| — non-row prose with no enclosing roster row | 46 | 47 in the cited operand, **less `fr-EquationPanel.md:48`** (D-4) |
| **candidate routings** (`:48` carries **two**) | **171** | 169 in the cited operand |
| — mis-attributions (token belongs to an adjacent fold-note, not the row) | −2 | reproduced, both correct |
| **ROUTED TOTAL** | **169** | **a floor, not a ceiling** — see D-16 |

The 169 is stated as a **floor**: it is the cited operand's own arithmetic with the one demonstrable under-count corrected. D-16 names a second, unresolved candidate (`fr-AdminUserList.md:161`) whose admission would make it 170 and would reconcile that record's own tally of *"F.W5 ×12"*.

The two exclusions are the spec's own and reproduce: `fr-AnimationControls.md:127` **M-13 (LC)** (disposition *"fold-rider on **fr-BasisCanvas D-9/BC-8** … → F.W3/W4 with that row"*; the `:129` token opens the *reader-misses-folded* note, whose `BC-9/C-6/D-20` is booked at E8) and `fr-CanvasControlsDock.md:100` **M-10** (terminal cell `**NO-WAVE-OWNER**`; the `:102` token sits in the *Full-coverage note*, whose C-28 fold is booked at B1).

**The delta from the spec's 167 is `fr-EquationPanel.md:48`** — see D-4. Both of its routings ARE booked (C4, F9), so the delta moves routedTotal and bookedCount together and creates no escape.

### 1.3 Set-difference result

Every routed id was tested against `F-W5.md` **by bytes**, in the record-qualified form where the token collides. **ABSENT: 0. ESCAPED: 0.**

Spot receipts across the shapes the detector admits: `fr-PathPreview` PP-DEADSEAM (`:56`, prose-numbered row) → B4 · `fr-FrequencyGraph:70` (bullet, `→ FR-CP-16`) → A3 · `fr-GalleryInfiniteGrid:32` R-7 (bare-first-cell table row, substance FR-GFC-1/-4) → D10/D2 · `fr-EquationPanel:48` ×2 (prose routings) → C4/F9 · `fr-SpeedSelect:59` SS-L-07/SS-C-10 → §5 · `fr-GalleryDraftsSection:49` F-6 → D1.

Spelling variance is real but resolves: `BC-9 / C-6 / D-20` → `BC-9/C-6/D-20` (E8); `D-L4 / C-7` → `D-L4+C-7` (B1); `C-4 / D-13` → `GIG C-4/D-13` (D10). Three bookings drop banked alias limbs — see D-18.

---

## 2. ESCAPES

**NONE.** `{169 routed ids} \ {F-W5.md bytes}` = ∅.

Two landings are *record-level only* and would not survive an id-keyed difference: `fr-PaperSearchDropdown:77` (`C:C-23 / C:C-24 / L:§5` — *"**F.W5-W8 = no rows.**"*) is carried only as the component name in §4's F.W2 zero-cell roster (D-17), and the three FSE ids at D-2 are not carried at all — but the latter route to **F.W4**, so they are not F.W5 escapes.

---

## 3. DEFECT REGISTER

### D-1 · MAJOR — three banked NO-WAVE-OWNER rows are DOUBLE-HOMED against F.W10's drain

F-W5 books `AA-45` (clause **A1**, rows-folded), `AA-46` (**A2**), `AA-47` (**A3**). All three are banked **NO-WAVE-OWNER** at the record — ⟨cmd⟩ `sed -n '88p;89p;90p' fr-AdminAuditLog.md`, terminal cells: *"Latent contract gap. **NO-WAVE-OWNER** (recorded for SS-3/SS-4)."* · *"Redundancy records. **NO-WAVE-OWNER**."* · *"…the *uneven* defensiveness… **NO-WAVE-OWNER**."* — and none carries an `F.W5` token anywhere (they are not members of the 169).

All three are ALREADY homed, with terminal verbs, in `F-W10.md` **§2.5 — the NO-WAVE-OWNER drain (G-F10-7; COHESION §3.3 deadline)** — ⟨cmd⟩ `sed -n '160,162p' F-W10.md`:

> `| **AA-45** (fr-AdminAuditLog:88) | … | **NEXT-BOUNDARY LEDGER** — API-contract row (identity on the wire), SS-3/SS-4 input |`
> `| **AA-46** (:89) | … | **KILL-WITH-RATIONALE** — a redundancy record; no defect, no work |`
> `| **AA-47** (:90) | … | **NEXT-BOUNDARY LEDGER** — SS-4 contract input |`

`AA-46`'s two dispositions are directly contradictory: F.W10 rules it *"no defect, no work"*; F.W5 makes it an operand of clause A2 (*"Envelope + cardinality ruled once"*). This breaks R-5's one-home law (*"No id lands in two waves; a second wave that needs it cites the adopting wave"*) — the same law F-W5's D7 invokes to certify `doubleHomed = 0` for FR-AUL-46. §2c excludes `AA-48` from the census on precisely the NO-WAVE-OWNER criterion and gives AA-45/-46/-47 no exclusion receipt, no citation of F.W10, and no disclosure at all.

**Cure shape**: A1/A2/A3 keep the *mechanism* and cite F-W10 §2.5's rows by id; the three ids leave F-W5's rows-folded cells, or F-W10's drain rows convert to citations of F-W5's clauses — one direction, declared at both ends.

### D-2 · MAJOR — the masthead's FOUR-SOURCE-COORDINATE defence is FALSE at 2 of its 7 ids

`F-W5.md:9`:

> `…/fr-FourierShapeExtractor.md:81-87` (⟨cmd⟩ `sed -n '81,87p'` → the ids `L-m3/C-10 · L-m4/C-12 · L-m5 · M-5 · M-7 · M-8 · M-9`, **every one of which is homed in a §G clause row, not a tail**)

Token-bounded search of the WHOLE spec (`(?<![A-Za-z0-9-])L-m4(?![0-9])` and `…M-5(?![0-9])`) returns **exactly one hit each — line 9 itself**. Neither `fr-FourierShapeExtractor L-m4/C-12` nor `M-5` is homed in §G or anywhere else. Worse, both route elsewhere — ⟨cmd⟩ `sed -n '82p;84p' fr-FourierShapeExtractor.md`, terminal cells: *"`id` rides as Button fallthrough at every pin. → **F.W4**."* and *"…the most load-bearing missing sentence in the file. → **F.W4**."* — so they could not be homed in an F.W5 §G clause without a double-booking.

The universal claim was manufactured by enumerating a raw line range without testing membership. (`L-m3/C-10`, `L-m5`, `M-7`, `M-8`, `M-9` do land, at G10c/G10c/G9c/G3c/G9c.) The range's structural claim — *"not a tail"* — does hold: `:81-87` sits inside the record's `### MINOR (21)` band.

### D-3 · MAJOR — G19's own cell places `P-9` on BOTH sides of the LHS, and double-counts `SS-L-07/SS-C-10`

`F-W5.md:285`, one gate cell, two sentences apart:

> **LHS**: the **167** id-keyed routings … ⊕ the **two `F-W4-CARRY.md` rows carried by citation … : §0's `CORRECTED DENOMINATORS` bullet (P-9) and §Rows D — the transport cluster — its `SS-L-07/SS-C-10` bullet**.

> **The three §5 ids that are NOT members of the 167 (AA-48 · P-9 · AA-44) are excluded from the LHS** by §2c's partition and are asserted on the RHS side only.

`P-9` is admitted to the LHS as a carry-row operand and excluded from the LHS in the same cell. Independently, `SS-L-07/SS-C-10` is declared a MEMBER of the 167 at both §2c (*"the one §5 row that IS inside the 167"*) and §5, then added AGAIN as a distinct LHS operand — a double count. §6 item 1 repeats the identical shape. The set-difference "∅ in BOTH directions" is not computable from this operand as written; this is the *"two sections asserting opposite things about the same ids"* class §5's ▲ D-4-cure block claims to have closed, now compressed into one cell.

### D-4 · MAJOR — routedTotal 167 is understated, and the cited reproduction is falsified by the spec's own bookings

⟨cmd⟩ `grep -nE 'F\.W5' fr-EquationPanel.md` → **one line, `:48`**, which carries TWO id-headed route-marked routings:

> `… · **D-10+D-L12+C-22** (Terms names a quantity it stops governing; \`budget\` never forwarded — re-verified at latex_rendering.py:281 this session) — **F.W5-W8** · …`
> `… · **D-L5+C-1** (full-payload re-upload vs the 5/60s compute limiter; two-slot hardening) — **F.W5-W8** · …`

F-W5 **books both**: `D-10 + D-L12 + C-22 (EquationPanel)` at clause **F9**, `D-L5+C-1 (EquationPanel)` at clause **C4**, and quotes `fr-EquationPanel.md:69` as C4's ONE-CONTROLLER LOCK home.

The operand F-W5 pins its census to — `conformance/PASS-2/F-W5-CHECK.md` §1's landing table, cited five times (masthead, §2, §2c, G19, §6) — lists fr-EquationPanel under **"Records contributing ZERO routings (19 of 66) — correctly absent, no clause claims a row from any of them."** That is falsified by F-W5's own clause tables. Fresh routedTotal = **169**.

The mechanism is the one R2-9 decreed against: the reproduction's own class table buckets 47 lines as *"prose (no enclosing roster row)"* and `:48` fell in — a **shape-restricted operand**, DEFECTIVE at authoring whatever its result, under the detector G19 itself states inline (*"prose routings walked back to their enclosing row id (S-8) — no shape in which a routing does not count"*).

### D-5 · MAJOR — a superseded denominator restated in F.W5's own voice, twice

⟨cmd⟩ `grep -on 'nine e2e specs' F-W5.md` → `:251`, `:305`, `:325`. `:251` is inside the AA-44 quotation and is legitimate (the record's bytes). The other two are **F.W5's own voice, unquoted, unqualified**:

- `:305` (§4 · F.W5 → F.W9/W10): *"**AA-44**'s one-sided test seam (zero frontend tests; nine e2e specs never touch the audit tab)"*
- `:325` (§5 · AA-44 row): *"zero frontend tests; nine e2e specs never exercise the audit tab"*

⟨cmd⟩ `grep -n 'Spec count re-measured at fold' F-W0.md` → `:249`, inside F-W0 §4's RED witness: **"Spec count re-measured at fold: 8"**. F-W9 §2.8 **E-1** and F-W10 §2.8 **E-5** both declare the paired 9→8 correction and quote the standing rule *"Quote 8 specs, never 9 (K11)"*. F-W5's own X-1 cell states that *"divergence from those two tables is a defect against G-11"*; G-12's heading is *"superseded figures FORBIDDEN downstream"*. Two live divergences.

### D-6 · MAJOR — bare `K-13` carries two different records' identities inside one spec

- **F2**: *"▲ **KILL LOCK (K-13)**: fr-FunctionInput L-B1's 'without ever touching the offending slider' headline rides the ratio-1 state proven UNREACHABLE from defaults"* — that is `fr-EquationView.md:146` / `fr-FunctionInput.md:100`.
- **F7**: *"▲ **K-13 BINDS THE CURE**: `latex` is the budget-TRUNCATED series render…"* — that is `fr-ConvergencePlot.md:129`.

⟨cmd⟩ `grep -rln '\bK-13\b' fr-*.md | wc -l` → **35 of 66**. `K-n` is a record-local kill-register index; it is the corpus's widest collision, wider than every member of the U-12 eight. §2's ANTI-RENAME block names a *ninth* collision (`M-10`) as a seat-measured addition and rules that *"a bare `M-10` is not an identity and may not enter a set-difference at either end"* — then leaves `K-13` (and bare `K-1` §1c/D1, `K-3` D7/D16, `K-5` D10, `K-6` D16, `K-12` E5/§4, `K9` A4, `K10` D5, `K5` D10) unqualified throughout. The consequence is already visible in the operand F-W5 cites: PASS-2 §1's landing row reads `ConvergencePlot | 5 | … K-13 F2/F7 …`, attributing one record's K-13 to a clause that carries a different record's.

### D-7 · MAJOR — §1c's third-iteration halt names an operand the same file forbids

`F-W5.md:82`: *"Third-iteration halts (triumvirate): … a **census row** (§1's PASS-1 roster) failing to home three times; …"*

Two failures in one clause: (i) F-W5's **§1 is *Bounds*** — it contains no roster of any kind; (ii) the **PASS-1 roster is declared superseded and forbidden** at four separate sites of this file (masthead `:7` *"The PASS-1 figure 162 is SUPERSEDED and forbidden downstream"*; §2c `:236`; G19 `:285` *"may not be quoted as this gate's operand"*; §6 `:331` *"NOT an admissible operand"*). The halt condition is unrunnable as written and cites the one operand the wave bans.

### D-8 · MINOR — a "verbatim"-labelled quote that fails a `grep -F` of itself

**B3**: *"▲ **FR-AUL-13 verbatim** (⟨cmd⟩ `grep -n -F 'either the router populates' fr-AdminUserList.md` … → one hit): "either the router populates `errors`/`matched_count` or the contract drops them; the client compares `affected` vs `slugs.length` either way.""*

⟨cmd⟩ this seat: `grep -qF 'either the router populates \`errors\`/\`matched_count\` or the contract drops them; the client compares \`affected\` vs \`slugs.length\` either way.' fr-AdminUserList.md` → **no match**. ⟨cmd⟩ `sed -n '51p' fr-AdminUserList.md | tail -c 160` → *"| **F.W5** — either the router populates `errors`/`matched_count` or the contract drops them; the client compares `affected` vs `slugs.length` either way |"* — the cell ends `either way |`, with **no period**. The spec adds terminal punctuation inside a *verbatim* marker. This is the exact class F-W5 struck at B1 (*"dropped the terminal period, so the 'verbatim' label failed a `grep -F` of its own sentence"*), in the other direction.

### D-9 · MINOR — A5's "whose routing cell reads" rendering moves the record's emphasis

F-W5 `:116` prints the cell as *"→ **F.W5–W8** (the R6-8 companion clause: …)"*. ⟨cmd⟩ `sed -n '65p' fr-CoefficientsSpectrum.md | tail -c 200` → *"… **→ F.W5–W8** (the R6-8 companion clause: an operation's response model must be structurally typed or the leaf is unjoinable in principle)."* — the arrow is INSIDE the bold and a terminal period follows. `grep -F` of F-W5's rendering fails. Same D-6 class the file struck at B1 (C-30, *"STRIPPED the record's bold markers from inside the quotation"*) and D7 (ContourSettings B-1). The inner core quote (*"an operation's response model must be structurally typed or the leaf is unjoinable in principle"*) does byte-match.

### D-10 · MINOR — E16's lane-crud quote exceeds its stated command's output and is silently re-flowed

⟨cmd⟩ `grep -n -F 'flat BAG' lane-crud.md` → **one line, `:240`**, whose bytes END at *"…; the diff is a"*. The quoted sentence continues on `:241` (*"whole-atom replace …; there is no three-way / DAG / merge."*). Under R2-1-LAW.1 a quotation enters only as the pasted output of the stated command; this one exceeds it. E3 discloses the identical mid-sentence wrap in `COHESION.md` with explicit `/` markers (*"the source wraps mid-sentence, disclosed rather than silently re-flowed"*); E16 performs the same operation without disclosure. Substance is correct at the bytes and the second hit (`:399`) is verbatim-exact.

### D-11 · MINOR — unrooted / unrunnable ⟨cmd⟩ paths against the file's own adopted law

§0's AUDITED cell adopts R-10.5 wave-locally: *"unrooted spellings are forbidden vocabulary"*. Yet:

- **A4**: ⟨cmd⟩ `grep -n "inv-16" …/lane-crud.md` — an **ellipsis** in the path; the command cannot execute as written.
- **E16**: ⟨cmd⟩ `grep -n -F 'flat BAG' lane-crud.md` and ⟨cmd⟩ `grep -n 'atomdiff.py:12-14' lane-crud.md` — bare filenames. The file's true path is `docs/tranches/V/megatranche/formation/fourier/lane-crud.md`; neither resolves from the corpus cwd every other ⟨cmd⟩ in the file assumes.

R-10.6's law is the governing one: *"a gate command that cannot execute as written is a phantom witness."* (Both quoted spans verify at the true path.)

### D-12 · MINOR — the X-1 erratum's pasted receipt no longer reproduces

`F-W5.md:298` prints ⟨cmd⟩ `sed -n '63p;199p;204p' F-W0.md` (this seat) as returning *"F-W0's bounds-table **header row** (`Path` / `Access`) · `**Owning rows**: 15, 16, 38.` · `**Owning rows**: 17, 15.`"*. Re-run by this seat it returns `---` · (blank line) · `### G-1 — The 28-path M.W1a tree is SETTLED, per path, minuted`. `F-W0.md` mtime is 16:58, later than F-W5's 16:43. The cell's LAW (abolish the coordinate; cite gate ids) is correct and is applied — but the receipt is a line-number probe into a live sibling used as evidence, and it has now drifted a third time, unmarked.

### D-13 · MINOR — §3's preamble contradicts its own gate table

`F-W5.md:263`: *"Every command in this table is non-mutating (`ls`, `grep`, `sed`); **no witness in this file writes a byte in either tree, and none reads `$F` at all** — the fourier column is MEASURE-AT-OPEN precisely so this file never needs to."*

The table's own commands: **G1** `grep -rn "class AuditEntry" $F/api --include='*.py'` · **G3** `grep -rn "_write_root_version" $F/api` · **G7** `grep -rniE "trie|…" $F/api $F/web/src $V/api/src $V/src`, the last asserting a measured result (*"**ZERO true hits on BOTH trees**"*). The read-only/no-write posture holds; the "**none reads `$F` at all**" clause does not, and three born-RED witnesses state $F facts as measured.

### D-14 · MINOR — partial ⟨cmd⟩ outputs presented as the command's return

- ⟨cmd⟩ `grep -rn "D9" …/VALUE-FORMATION-PACKET-2026-07-29.md` → shown as `:55`; this seat's run returns **2** hits (`:55`, `:531`).
- ⟨cmd⟩ `grep -rn "inv-26" docs/tranches/R/audit/pass1/R4-FOURIER.md` → shown as `:332`; this seat's run returns **5** (`:169`, `:188`, `:207`, `:319`, `:332`).

Both quoted spans are byte-exact at the cited lines; the receipts are incomplete, not false. Contrast C4, which explicitly narrows its fragment *because* the broader one returns two hits — the correct idiom, applied unevenly.

### D-15 · MINOR — the census DEFINITION does not match the roster it counts

Masthead `:7`: the 167 are *"id-keyed routings **whose terminal disposition names** `F.W5` / `F.W5-W8` / `F.W5–W8`"*. §2c then admits into the same total: measured NEGATIVES (⟨cmd⟩ `sed -n '144p' fr-App.md` → *"**Nothing routes → F.W5–W8**…"*; `sed -n '97p' fr-MobileFloatingToc.md` → *"**F.W5–W8 gets nothing**…"*) and intake fold-carries whose terminal cell names another wave (`fr-AnimationControls:187`, `fr-CanvasControlsDock:150`, `fr-EasingCurvePreview:135`, `fr-ConvergencePlot:164`, all `R6-8 … CARRY→F.W5`). Both classes are correctly *landed*; the definitional sentence is wrong for them and should read "whose row carries an `F.W5` routing token", which is what the detector actually does.

### D-16 · MINOR — the detector is applied inconsistently to R6-8 intake carries

`fr-AdminUserList.md:161` — *"Intake identities folded by reference, never re-booked: … **R6-8** (FR-AUL-3/11/12 are instances; **carry → F.W5**) …"* — is not counted, while four identically-shaped carries in sibling records are (D-15's list). The record's own tally at `:165` reads *"F.W5 ×12"* against the **11** AdminUserList ids the cited roster books. Either the tally is one over or the roster is one under; the spec resolves neither.

### D-17 · MINOR — `fr-PaperSearchDropdown` lands by record name only

⟨cmd⟩ `sed -n '77p' fr-PaperSearchDropdown.md` → *"**C:C-23 / C:C-24 / L:§5 — INFO · FOLDS into banked fr-PaperSearch C-Z1/C-Z2.** … **F.W5-W8 = no rows.**"* §4's F.W5 → F.W2 export names only the component (*"PaperSearch/PaperSearchInput/PaperSearchDropdown"*); the three ids appear nowhere. An id-keyed set-difference (G19's stated form) cannot resolve them. R-2c already assigns `C:C-23/C-24` to F.W2 — the citation owed here is of that adopting wave, by id.

### D-18 · MINOR — banked alias limbs dropped from record-qualified bookings

- `fr-CanvasControlsDock.md:45` heads **`D-4 / L-1 / C-3`**; E5 books `fr-CanvasControlsDock D-4` alone.
- `fr-EditorControlsDock.md:59` heads **`D-12 / C-4 / C-5`**; C1 carries `D-12`'s rider alone.
- `fr-GalleryCard.md:52` heads **`D-7 / C·I-2`**; D1/E4 carry `GalleryCard D-7` alone.

G19's difference runs on the qualified id form; a dropped limb is invisible to it in both directions. (Contrast D17, which carries `fr-GalleryCard L·M-4 / D-13 / C-8(a) + L·D-2 / C-12` — *"the FULL banked spelling"* — correctly.)

### D-19 · MINOR — an unminuted divergence from RULINGS-2 R2-7.1's stated clause home

R2-7.1 directs F-W8's §6a to cite `fr-ContourSettings M-10`'s *"F-W5 clause home (the E13/A5 family)"*. F-W5 books it at **D12** (§2's ninth-collision block: *"**`fr-ContourSettings M-10`** is the closed-domain/shared-enum row at **D12**"*). The divergence is correct on the merits and is minuted — but only at the OTHER end: `F-W8.md:10` (*"R2-7.1's candidate home … does not hold at the bytes … booked at **clause D12**"*). F-W5 carries no reciprocal note, so a reader of the keystone alone cannot see that a ruling was departed from.

### D-20 · MINOR — `grep -c` output rendered in the `path:N` shape the same block forbids

§2's ninth-collision block: ⟨cmd⟩ `grep -c 'M-10' fr-ContourSettings.md fr-BasisSelector.md` → *"`fr-ContourSettings.md:2` · `fr-BasisSelector.md:3`"*. Those are **counts**, printed in a form indistinguishable from the line-anchor idiom R2-2 abolishes — inside the very block that rules on identity discipline. (The counts reproduce.)

---

## 4. AXES THAT PASSED

**(2) QUOTE REALITY — 23 of 26 corpus/cross-spec quotations byte-match, run by this seat.** Verified by `grep -qF` of the full quoted span: fr-EditorControlsDock `:60` · fr-AdminFlaggedPanel `:47`, `:25` · fr-EquationPanel `:69` (incl. the deliberate two-hit narrowing: bare *"signal is hoisted outside"* → `:48`, `:69`) · fr-UserSlugBar `:64` · fr-GalleryFeaturedCarousel `:34` · fr-ContourSettings `:40`, `:43`, `:70` · fr-GallerySearchBar `:24` · fr-BasisSelector `:58` · fr-AdminAuditLog `:60`, `:87`, `:88`, `:91` · fr-ContourPreview `:46` · fr-EquationView `:47`, `:71`, `:112` · fr-FunctionInput `:53` · fr-EquationResult `:39` · fr-FourierShapeExtractor `:51`, `:149` · fr-GalleryCard `:64` · fr-CanvasControlsDock `:45`, `:150`, `:102` · fr-AdminUserList `:89` · fr-AnimationControls `:123`, `:127/:129` · fr-MorphShapePreview `:110` · fr-App `:144` · fr-MobileFloatingToc `:97` · fr-SpeedSelect `:59`. Measured negatives reproduce: `grep -rn -iF 'join to get wrong' fr-*.md` → **0**; unbounded `P-9` → **6 records**, token-bounded → **0**; `MF-9` across the registry → **exactly 1**, the dangling cite at `fr-GalleryCardModal.md:56`. Cross-spec: `COHESION.md` §2 (both quotes, `:70` and `:77`, inside §2's 64–78 span) · `F-W1.md:276` TWELVE-limb charter (full sentence `grep -F` → 1, under `## §4 Sequencing`, item 4) · `F-W10.md:133` dissent (full sentence → 1, under `### §2.3`, the `SS-4-PREREQ` row) · `F-W0.md` G-11/G-12 headings and the SUBSTRATE-LEDGER create row (`:77`, under `### 2a. File bounds` — the spec's own address correction is right) · `F-W4-CARRY.md:18` P-9 export (full sentence → 1, under `## §0`) and its `§Rows D` SS-L-07/SS-C-10 bullet (`:234`, under `### D · … the transport cluster`). Value-side born-RED witnesses re-measured read-only: `hash.ts computeContentHash(name, colors)` · `paletteVersion.findByHash(hash)` / `insertIfAbsent` (G2) · `atomdiff|atomDiff` over `$V/api/src $V/src` → **ONE** hit, `__tests__/palettes-forks.test.ts:9` + `ls $V/api/src/lib` → absent (G4/E3) · `forks.ts:76` = `visibility: "public"` (G5) · the merkle/flat-bag/structural-sharing sweep → **exactly 1**, `hash.ts:6` (E16) · `model.ts:61` 3-state visibility (D9 reconciliation) · `DECISIONS.md` `grep -c "D9"` → **1**, `:36`, bytes exact. Three quotation defects survive (D-8, D-9, D-10); the fabricated-authority class of passes 1–2 is **absent**.

**(3) M-25 DEPTH — locks, riders and dissents carried.** `FR-NP-32 (≡ fr-PaperSidebar M1)` in canonical cite-both form at X-1, with `fr-NotationPills.md:35`'s BLOCKER disposition verbatim ✓ (R-8 discharged). Same-commit riders named by BANKED id: `PSM-1` `:34` (= D-1·L-1·C-1) ✓ · `PSM-4` `:37` (= D-3; *"Must land in the same wave as PSM-1 or the cure ships a WCAG regression"* — `grep -F` → 1) ✓ · `PSM-13` `:49` (*"MANDATORY rider inside the PSM-1 commit"* — `grep -F` → 1) ✓ · the record's `:140` verdict does name the pair *"(PSM-4, PSM-13)"* ✓. `PAW-44/LAW-3` ✓ (`fr-PaperArticleWindow.md:220` — *"LAW-3: PAW-44 restoration lands WITH-or-AFTER PAW-1 + PAW-30, never before"*; F-W5's compression is unquoted and faithful). `MPC-31/MPC-3⊕10⊕13⊕8⊕22` ✓ (all five ids live: `:82`, `:47`, `:59`, `:67`, `:57`, `:76`). `FR-MSP-6 two-channel lock` ✓ (`:98`, `:16`). Anti-cures carried with their kills: K-3 (m-7's 422-straddle refuted from source, D7) · K-5 (D-1 skeleton, D10) · K-13 ×2 (F2/F7 — but see D-6) · K12 (E5 probe-suppression) · K9 (`?? item.slug` fallbacks, A4) · K-1 (13/44, D1/G9) · FR-GV-9's first clause killed at R-3/K3 (D8) · β's 428-escalation kill preserved (D8) · GAB-16 refuting D-M7 (D11) · M-2's repair-shaping correction (G2c). Dissents preserved with revival conditions: §2b's nine, incl. `L-B1`/`L-B2/C-2` *"demoted **with revival conditions** = the DO-NOT-REGENERATE tripwire"* ✓ and the E16/R-4 trie dissent quoted ONCE, at the F.W7 edge, from `F-W10.md`'s bytes ✓ (RULINGS-2's twin law honoured: the quotation has one home, one command, one ⟨cmd⟩ note).

**(4) GATES — 22, all born-RED, witnesses real.** `grep -c '^| \*\*G[0-9]' F-W5.md` → 32 = 22 gates (G1–G22) ⊕ 10 §G clause rows (G1c–G10c); §0's *"22 conditions"* and §3's heading agree. Exactly four ⊙ OWNER-GATED (G4, G7, G10, G11) as §0 declares. Every RHS-existence witness re-run and RED: `ls $V/docs/tranches/X/fourier/contract/` → *No such file or directory* (G19, G21) · `ls $V/docs/tranches/X/coordination/` → exactly one file, `ATLAS-TO-VALUE-2026-08-03-RULINGS.md` (G20) · `grep -rn "MF-9" …/adjudicated/` → 1 (G22). G19's detector IS stated inline with shapes, both dash spellings, and token-bounded matching (R2-9 discharged in form) — the operand it then names is the defect (D-3, D-4). §1a's negative-existence quantifier is correctly narrowed to **FOUR** to-be-created artifacts with per-receipt coverage, and the fifth path (this spec) is expressly excluded — pass 2's `:54` defect is cured. All six bounds paths `ls`-resolve; `INTAKE-ADJUDICATION-2026-08-03.md` §3 does carry an `### F.W5` block and §2 does carry `C-3` (`:111`, *"both right, different denominators"*), so §0's "opens after" authority is real.

**(5) POSTURE.** F.W1 cited **whole** at its pinned coordinate with the CORRECT limb count — *"the TWELVE-limb roster chartered at `F-W1.md` §4 step 4"*, stable anchor, re-quoted by ⟨cmd⟩ AFTER F-W1's own R2-5 edit landed; the single surviving `ELEVEN-limb` string is the self-disclosing strike note, not a citation ✓ (R2-5 discharged). F.W0 pre-gates HARD at X-1 with `FR-NP-32 (≡ M1)` ✓. W7's ruled ∅ posture declared from the keystone's end (*"its routing census is a measured ∅"*; F.W7 CITES, F.W5 STATES) ✓ (R2-8 honoured; not re-litigated here). SS-4 rulings flagged INLINE — §2a's R1–R9 with honest defaults and costs, re-enumerated at the §4 SS-4 edge ✓. `$F` READ-ONLY declared and no fourier byte written ✓. `Status: planned` at masthead and §6; `VERIFIED = NO`; **zero VERIFIED stamps** ✓. **R2-2 fully discharged at this end**: `grep -oE 'F-W[0-9]+\.md:[0-9]+|…-CARRY\.md:[0-9]+|COHESION\.md:[0-9]+|KF-W[0-9]+\.md:[0-9]+'` over the whole spec → **zero hits**. The line coordinates that remain address the frozen corpus (permitted, R2-2.2(i)), product source in either tree (fourier-side ones all marked MEASURE-AT-OPEN under D-19), or are F-W1's own intra-file `:294` pin carried inside a quotation and marked *at this writing*. One in-repo doc cite sits outside R2-2.2's enumerated scope and resolves correctly (`R4-FOURIER.md:332`, A4). R2-3d.5 (C-28 fold at B1, banked bytes quoted from `:150` and `:102`) ✓ · R2-7 reciprocity (M-13 confirmed by command at E14, GalleryCard serializer id added at D17 where only the mechanism sat) ✓ · R2-11 (FR-AUL-46's one home at D7, F-W8's J4 confirmed clean of it at the other end) ✓ · the *"Two tail rows"* count word replaced by the FOUR-coordinate block ✓ *in form* — see D-2 for its content.

---

## 5. WHAT PASS 3 CHANGES

Pass 1 convicted **fabrication** (`P-8`, the phantom carry). Pass 2 convicted **authority drift** (three unresolving coordinates, the pathless D9). Both classes are **GONE**: this seat could not find a single quotation in F-W5 that names an authority which does not exist, and not one line-number citation into a live sibling.

What survives is a different failure: **the spec is now more disciplined than its own operands**. Its detector is correct and its census is wrong (D-4); its anti-rename law is correct and its own locks break it (D-6); its one-home law is correct and it double-books three rows against the wave that exists to drain them (D-1); its closure gate states its detector inline and then names an operand that contradicts itself in the same cell (D-3). The masthead block written to cure pass 2's "tail rows" finding asserts a universal that is false at two of seven ids (D-2), and one of the corrected denominators it is bound to quote is restated in the superseded form (D-5).

None of the seven MAJORs requires new material. Six are edits inside `F-W5.md`; D-1 needs one direction chosen with F.W10 and declared at both ends.

*Read-only everywhere except this file. No spec, carry, record or sibling was written.*
