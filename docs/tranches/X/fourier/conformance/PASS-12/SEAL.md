# X·F ROUND 12 — THE SEAL. **VERIFY-ONLY. ZERO EDITS.**

**2026-08-30 · the seal seat · base = repo root `/Users/mkbabb/Programming/value.js` · pinned BSD toolchain.**

⊙ **INSTRUMENT DECLARED FIRST, because this seat nearly mis-convicted a receipt on it.** The interactive shell in which this seat runs shims bare `grep` to a **`ugrep` 7.8.4** function, not `/usr/bin/grep`. The two disagree on ERE alternation anchored inside a group: `grep -nE '(^|[^A-Za-z0-9])L-16([^A-Za-z0-9]|$)' F-W10.md` returns **0** under the shim and **11** under BSD. **Every receipt below was re-run under the DECLARED binary — absolute `/usr/bin/grep`, `/usr/bin/awk`, `/usr/bin/wc` — and the corpus reproduces exactly.** ▲ **This is a finding about the reader, not the corpus, and it is recorded so the next seat does not file a false negative**: the specs' own idiom of spelling `/usr/bin/grep` absolutely (F-W2:248's four census arms, and R4-2.1–2's portability law) is the thing that makes them reproducible at all. A seat that runs bare `grep` will convict lawful bytes.

⊘ **`grep -oE '.{0,260}…'` aborts on the pinned binary** — *maximum repetition exceeds 255* — the exact BSD ceiling `F-W2:248`'s bounded-wildcard probe exists to keep the corpus away from. The law is live and was demonstrated, not merely asserted.

---

## §1 — THE RE-HASH. **12 / 12 BYTE-IDENTICAL. CANONICAL FROZEN, FOURTH ROUND.**

⟨cmd⟩ `shasum -a 256 waves/F-W*.md conformance/CENSUS-CANONICAL.md`, re-run against **§ERRATA-R12**'s table (`PIN-PURGE-CERT.md:666`–`:679`) row by row.

| # | file | §ERRATA-R12 pin | this seat | |
|---|---|---|---|---|
| 1 | `waves/F-W0.md` | `1f5a93c90ed7` | `1f5a93c90ed7` | ✓ |
| 2 | `waves/F-W1.md` | `ab2437af09e0` | `ab2437af09e0` | ✓ |
| 3 | `waves/F-W2.md` | `0b967619bb35` | `0b967619bb35` | ✓ **moved** |
| 4 | `waves/F-W3.md` | `be86ae410118` | `be86ae410118` | ✓ |
| 5 | `waves/F-W4.md` | `2fb6e6d637bb` | `2fb6e6d637bb` | ✓ |
| 6 | `waves/F-W5.md` | `40bcad59cd2b` | `40bcad59cd2b` | ✓ |
| 7 | `waves/F-W6.md` | `e83dcad051f5` | `e83dcad051f5` | ✓ |
| 8 | `waves/F-W7.md` | `ba9be3e37613` | `ba9be3e37613` | ✓ **moved** |
| 9 | `waves/F-W8.md` | `d8237cf6b11e` | `d8237cf6b11e` | ✓ |
| 10 | `waves/F-W9.md` | `b265bf677869` | `b265bf677869` | ✓ |
| 11 | `waves/F-W10.md` | `8ae0ad8fa623` | `8ae0ad8fa623` | ✓ **moved** |
| 12 | `conformance/CENSUS-CANONICAL.md` | **`f44362757458`** | **`f44362757458`** | ✓ **FROZEN** |

**THREE moved · NINE unmoved · 3 + 9 = 12.** The census pin is byte-identical to §ERRATA-R11's, §ERRATA-R10's, §ERRATA-R9's and §TABLE-R7's row 12 — **`f44362757458`, a fourth consecutive round.** The cert's own §ERRATA-R12·b enumeration ("Eight unchanged … and row 12, the canonical") closes at **8 + 1 = 9**, agreeing with its table's "NINE are unmoved".

⊙ **No stale pin propagated.** ⟨cmd⟩ `/usr/bin/grep -n '77854a06b2ee\|75a08eeee03a\|9415e4c93e57' waves/F-W*.md` → **∅ (exit 1)**. Not one of the three superseded digests is cited anywhere in the eleven.

⊙ **Live pin census, glob order, re-measured file by file:** **4 · 13 · 2 · 6 · 9 · 4 · 5 · 4 · 2 · 6 · 3** — reproducing §ERRATA-R12's published sequence exactly, `F-W10` sorting after `F-W1` as the shell produced it.

⊙ **Observation, not a finding.** `PASS-12/CHECK.md:48` pins `PIN-PURGE-CERT.md` at `024943b69d11`; it now reads `6c1dfad4103c`. **This is lawful and necessary** — the check ran before §ERRATA-R12 was appended, and a certificate cannot pin its own post-append bytes. The cert's table does not include itself, so nothing is stale.

---

## §2 — THE RECEIPT BATTERY. **48 RECEIPTS RE-RUN VERBATIM. 48 REPRODUCE BYTE-EXACTLY. `fabricated = 0`.**

This is the write-then-measure law's own proof: **every figure round 12 published is a reading of the settled bytes.**

### §2.1 `F-W2:248` — both arms, the cured line. **REPRODUCES IN ALL SIX FIGURES.**

| ⟨cmd⟩ (BSD, verbatim) | published R12 | this seat |
|---|---|---|
| `/usr/bin/grep -oE '⟨cmd⟩ \`[^\`]*\`' F-W2.md \| sort -u \| /usr/bin/wc -l` | **86** | **86** ✓ |
| `… \| /usr/bin/grep -c '/Users/'` | **16** | **16** ✓ |
| `/usr/bin/awk 'NR!=248' F-W2.md \| /usr/bin/grep -oE … \| sort -u \| /usr/bin/wc -l` | **81** | **81** ✓ |
| `… \| /usr/bin/grep -c '/Users/'` | **16** | **16** ✓ |
| line's own contribution (delta) | **5** distinct · **0** absolute | **86 − 81 = 5** · **16 − 16 = 0** ✓ |
| derived no-absolute-path readings | verbatim **70** · self-excl **65** | **86 − 16 = 70** · **81 − 16 = 65** ✓ |

▲ **The round-10 → round-11 → round-12 recurrence is CLOSED at the bytes.** Round 10 read `83`/*sixty-seven*; round 11 read `84`/*sixty-eight*; **the settled bytes read 86 / seventy**, and the count word tracks the verbatim form. The self-excluding arm was unmoved and exact across all three rounds. **The substantive claim — most commands run from a declared base, not an absolute path — holds at 70/86 and at 65/81 alike**, exactly as the cure states.

### §2.2 `F-W2` portability and shell-variable probes — **∅ · ∅ · ∅, all three**

| ⟨cmd⟩ | expected | this seat |
|---|---|---|
| `/usr/bin/grep -oE '⟨cmd⟩ \`[^\`]*\$[^\`]*\`' F-W2.md` | ∅ | **∅ (exit 1)** ✓ |
| `grep -n 'grep -[a-z]*P' F-W2.md` | ∅ | **∅ (exit 1)** ✓ |
| `grep -no '[.]{0,[0-9]*}' F-W2.md` | ∅ | **∅ (exit 1)** ✓ |

### §2.3 `F-W10` — the chase and every invariant class held across the write

| ⟨cmd⟩ | published | this seat |
|---|---|---|
| `grep -c 'F.W9 roster is 17 rows' F-W10.md` | 1 before → **0** after | **0** ✓ |
| `grep -c '16 rows' F-W10.md` | 0 before → **1** after | **1** ✓ |
| `grep -nE '(^\|[^A-Za-z0-9])L-16([^A-Za-z0-9]\|$)' F-W10.md` | eleven, ending `442` | **`191 193 244 276 291 296 297 301 303 338 442`** ✓ |
| `awk 'NR!=338' \| grep -c` (self-excluding) | 10 | **10** ✓ |
| the `+23` re-base arithmetic | `419 + 23 = 442` | **442** ✓ |
| `grep -n "§2.5a-iii's 1[34]" F-W10.md` | `314 442 467`, **14 · 14 · 14** | **identical** ✓ |
| `awk 'NR!=467' \| grep -c` (self-excluding) | 2 | **2** ✓ |
| `grep -cF '(≡ M1' F-W10.md` | 1 | **1** ✓ |
| `awk 'NR!=446' \| grep -cF '(≡ M1'` | 0 | **0** ✓ |
| `grep -c '35 callsites / 9 consumers' F-W10.md` | 2 | **2** ✓ |
| `grep -c '35/9' F-W10.md` | 1 | **1** ✓ |

### §2.4 `F-W7` — the §5 scope proof the attribution cure rests on

| ⟨cmd⟩ | published | this seat |
|---|---|---|
| §5 carry census, `## 5. Carry` … `## 6. Gates` | **15** | **15** ✓ (25 pipe-lines − 5 headers − 5 separators) |
| the heading's own decomposition | §5a 5 + §5b 1 + §5c 4 + §5d 3 + §5e 2 | **= 15** ✓ |
| `:101` inside the §5 range? | **outside** | `## 5. Carry` opens at **:181**; `:101` sits in `## 1. State` (:80–:105) — **outside** ✓ |
| `grep -rl "trie ruling stays inline" …/waves/` | `F-W7.md` **alone** | **alone** ✓ |
| §ERRATA-R12·b row 8's zero-hit claim: `F-W7:101` cited anywhere in the eleven, this cert, or `PASS-11/CHECK.md` | ∅ | **∅ (exit 1)** ✓ — the finding was **adjudicated, not rediscovered** |

### §2.5 Corpus-wide invariants the three writes could have perturbed — **NONE MOVED**

| ⟨cmd⟩ | published | this seat |
|---|---|---|
| `/usr/bin/wc -l waves/F-W*.md` | 596 · 735 · 465 · 912 · 470 · 433 · 634 · 359 · 374 · 557 · 515, **total 6050** | **identical, total 6050** ✓ |
| `grep -ho '### F\.W… — \*\*[0-9]* rows\*\*'` over the eleven | **24** | **24** ✓ |
| `grep -hoE '[0-9]+ records'` over the eleven | **157** | **157** ✓ |
| shared §2.2 splice **leg 1** — `diff <(awk '/^\| record \| identity/,/^\*\*By-mechanism entrants/' F-W9.md) <(… F-W10.md)` | EMPTY | **EMPTY** ✓ (26 lines) |
| shared §2.2 splice **leg 2** — `diff <(awk '/^\*\*THE F.W1 VISUAL-REGRESSION/,/^\*\*Born-RED witness/' …)` | EMPTY | **EMPTY** ✓ (18 lines) |

▲ **`G-F9-21 ≡ G-F10-6`'s `Δ=∅` is INTACT.** The whole of §2.2 differs between the twins at exactly **two** lines — the mirrored heading and the scope note that declares the mirroring — and at no shared byte. **The `:363` cure was lawfully unilateral**: it is `F-W10`'s own prose at §R10-LAW, outside both legs.

▲ **The `F-W0` §R11-4 enumerated sets survive untouched** — 24 quoted canonical row headings (four bearing superseded spellings, **all four chased**: `F.W0 57 rows` at `F-W0:225` · `F.W4 1014 rows` at `F-W7:291` · `F.W5 28 rows` at `F-W6:476` and `F-W8:20`) and 157 `N records` claims.

---

## §3 — `F-W10:363` AT THE FROZEN CANONICAL. **RE-DERIVED FROM THE OPERAND, NOT QUOTED.**

**This seat did not take the cure's word for the roster. It re-derived it three independent ways.**

⟨cmd⟩ `awk '/^### F\.W9 —/,/^### GLASS-RELAY/' CENSUS-CANONICAL.md`, at `:5126`:

```
### F.W9 — **16 rows** *(errata round 5, 2026-08-29: read 17; `R2-7` → TERMINAL at E5-16)*
```

| derivation | method | result |
|---|---|---|
| **records** | count of `- **fr-…**` bullets in the roster body | **15** ✓ |
| **rows, arm A** | sum of the parenthetical multiplicities `(n)` | 14 × 1 + 1 × 2 = **16** ✓ |
| **rows, arm B** *(independent of arm A)* | count of backticked ids in the roster body | **16** ✓ |

**The roster, summed by hand:** AdminAuditLog 1 · AdminFlaggedPanel 1 · App 1 · ContourPreview 1 · EquationModeToggle 1 · EquationResult 1 · FunctionInput 1 · GalleryAdminBanner 1 · GalleryCardModal 1 · GallerySearchBar 1 · GalleryView 1 · ImageUpload 1 · **PaperArticleWindow 2** · PaperView 1 · UserSlugBar 1 = **15 records, 16 rows.** ✓

⊙ **`R2-7` is TERMINAL, and the two halves were one defect.**

- `grep -c 'R2-7'` **inside the F.W9 roster body** → **0**. The body carries `- **fr-PaperArticleWindow** (2): `PAW-34` · `PAW-50`` and no `R2-7` at all. ✓
- `CENSUS-CANONICAL.md:3956` → `| `R2-7` | — | — | **TERMINAL (∅)** <sub>*errata R5 **E5-16**…*</sub> |` ✓
- `CENSUS-CANONICAL.md:5371` → the TERMINAL bullet, `fr-PaperArticleWindow` **56 ids** (re-counted: **56** ✓), `R2-7` among them (**1** ✓).

▲ **The erratum that took 17 → 16 IS the erratum that moved `R2-7` — E5-16 — so the magnitude and the homing were never two findings.** The cure treats them as one act, and that is correct at the operand. **`R2-7`'s withdrawal from the divergence list is right**: the canonical homes it TERMINAL and §2.2 cuts it to no wave, which is **agreement**, not divergence.

⊙ **R4-10's quotation is byte-true history and its inside is untouched** — *"F-W9 | F.W9 → 17 | masthead re-based"* — with the chase standing **outside** its marks (E-3). What R4-10 assigns is the **DUTY**; the duty is unchanged by the magnitude.

### §3.1 Coherence with `:494` — **CONFIRMED, and the distance is exact**

`:494` carries `the 26-vs-17 F.W9 roster spread ⟨*16 at E5-16*⟩` — the identical spread, the identical erratum, the identical live figure. **`494 − 363 = 131`**, reproducing the cert's "131 lines below" exactly. **This file carried the correct reading all along on one line and the false one on the other; after round 12 the two agree.** The cut count's denominator was re-based on the same line (*"26 cut identities against a **16**-row canonical roster"*), so `:363` no longer contradicts itself either.

⊘ **The substantive conclusion survives untouched**: the two sets are not nested, and the reconciliation remains **F-W9's duty under R4-10**, as do the `C:D-11` and `L:L-i2` homings.

### §3.2 The §3 aggregates §ERRATA-R12·a re-derives — **RE-DERIVED AGAIN HERE, INDEPENDENTLY**

- **Wave-duty**, summed from the canonical's own §3 home table (`:5462`–`:5470`): `55 + 330 + 23 + 919 + 1007 + 27 + 89 + 16` = **2466** ✓
- **Residual**: `4242 − 2466 − 1220 − 307` = **249** ✓
- **And the residual closed from the other direction** — the relay/packet/SS band summed from its own rows: `22 + 32 + 3 + 27 + 3 + 5 + 134 + 7 + 9 + 3 + 2 + 1 + 1` = **249** ✓. **Two independent paths to the same number.**
- §ERRATA-R12·a's arithmetic: class-sum `8 + 8 + 8 + 3 + 2` = **29** ✓; readable enumeration `8 + 8 + 7 + 3 + 2` = **28** ✓. **The defect is PLACEMENT, exactly as ruled** — the eighth aggregate exists, is real, and was verified.

### §3.3 `F-W7:101`'s quoted figures, checked against the canonical's own era record

| figure | the cure's claim | the operand |
|---|---|---|
| `F.W5` **28** | R4-5-era | `:5443`'s errata-round-6 note lists the pre-round-6 reading as **`F.W5 28`** ✓ |
| `F.W5` **27** | frozen, moved at **E6-3** | §3 table reads **27**; E6-3's delta line carries **`F.W5 −1`** ✓ |
| `F.W5-W8` **90** | R4-5-era | `:5446`'s errata-round-5 note lists the round-4 reading as **`F.W5-W8 90`** ✓ |
| `F.W5-W8` **89** | frozen, moved at **E5-15** | `:5092` — `### F.W5-W8 — **89 rows** *(errata round 5: read 90; `C:C-23` → UNROUTED at E5-15)*` ✓ |

▲ **The mixed pair `28 ⊕ 89` that rounds 5–11 published was true of no state of the operand**, precisely as ruled. **Either `28 ⊕ 90` (R4-5) or `27 ⊕ 89` (frozen), never one of each.** The distinctness is R4-5's ruling; the figures belong to their own eras. **The adjudication is correct at the bytes.**

---

## §4 — THE STANDING CLASS PROBES

### §4.1 Self-count. **`selfCountLiveMembers = 0`.**

⟨cmd⟩ an independent python sweep over all eleven for receipts of the form `grep -c '<pat>' <own file>` whose `<pat>` occurs literally on the publishing line with no `NR!=` exclusion → **13 candidates** (this seat's regex is narrower than `PASS-12/CHECK.md` §4.1's, which found 15; the populations overlap and the adjudication is identical). **Every one was run and read at the bytes. All are lawfully declared:**

| candidate | measured | published | disposition |
|---|---|---|---|
| `F-W0:457` | `Genuinely owed` in F-W7 → **2**; `each for its true reason` → **1**; `grep -rln` → **`F-W0.md` `F-W7.md`** | round 2's `0` / `0` / *"F-W0.md alone"* | **LAWFUL** — the cell expressly convicts its own quoted receipts: *"Live, every one returns the opposite."* Verified: every one does. |
| `F-W4:143` | 2 | 1 | **LAWFUL** — *"RECEIPT CONVERTED … the figure was false the instant it was written down"* (R3-10.5). Struck to a classification. |
| `F-W7:359` | legs re-run → **0** and **0** | 0 / 0 | **LAWFUL** — names the R3-3.10 form extinct and re-runs correctly against the live operands. |
| `F-W0:21` `:37` `:182` · `F-W1:363` · `F-W2:24` `:271` · `F-W3:359` `:885` · `F-W10:287` ×2 | — | — | **LAWFUL** — self-excluding, self-enumerated, or figure-expressly-not-banked, per the round-10 law's enumerated list. |

▲ **The one live member `PASS-12/CHECK.md` §4.1 recorded — `F-W2:248` — is CURED.** Its verbatim arm now reproduces (§2.1). **Post-cure the class is empty.**

### §4.2 Numeric context-probe over `F-W2` / `F-W7` / `F-W10`. **ZERO STALE UNCHASED MAGNITUDES.**

Probing **by context, never by value** (R11-5 law 2): every superseded canonical magnitude across fifteen axes (`4269` `4262` · `4329` `4322` · `2768` · `56` `57` · `362` `358` · `31` `27` · `926` `928` · `1014` `1012` · `28` · `90` · `17` · `29` · `138` `135` · `1190` · `2515`) swept against the three files, context-bound to `rows|records|roster|census|canonical|denominator|F.W|TERMINAL`. **79 context hits. 68 carry an on-line chase. The eleven without one were each read at the bytes and every one is a FALSE POSITIVE, adjudicated individually:**

| line | token | why it is not a canonical magnitude |
|---|---|---|
| `F-W2:34` | `57` | the file's own detector arithmetic (*"25 records / 68 occurrences / **57 lines**"*) — lines, not F.W0 rows |
| `F-W2:271` | `28` | the date `2026-08-28` |
| `F-W2:340` | `31` | the identity `C·C-31` |
| `F-W2:346` | `29` | a **verbatim quotation** of `fr-PathPreview.md:114`'s own record-level text, produced by a command |
| `F-W2:430` | `27` | a roster **index** in the table's first column |
| `F-W2:436` | `29` | §8c, the wave's own §3 analysis ledger — **re-counted: 29 labels** (A1–A7 ∥ B1–B13 ∥ C1–C2 ∥ D1–D4 ∥ E1–E2 ∥ F1 = **29**) ✓, and the line itself declares *"This ledger indexes the wave's ANALYSIS and has never been a census"* |
| `F-W7:261` | `17` | §7c, the file's own cross-edge table — **re-counted: 17 data rows**, decomposing exactly as the heading states (**15** declared edges + the F.W1 sequencing-lock row + the F.W9/W10 row) ✓ |
| `F-W7:330` | `17` | the clause id `E17` |
| `F-W10:129` | `28` | dirty-worktree modified-row count |
| `F-W10:189` | `56` | a live corpus receipt — **re-run: `grep -l 'NO-WAVE-OWNER' fr-*.md \| wc -l` → 56 records against `ls fr-*.md \| wc -l` → 66** ✓ |
| `F-W10:384` | `27` | the file coordinates `:27/:32` |

**Not one stale canonical magnitude survives unchased in the three files round 12 wrote.**

### §4.3 Coordinate spot-run. **16 ADDRESSES — ≥6 REQUIRED. ZERO DEAD.**

| coordinate | resolves to |
|---|---|
| `F-W0:199` | the Q8 row ✓ |
| `F-W0:204` | Q13, `PP-REDGATE`'s disposition ✓ |
| `F-W0:209` | Q18, the SS-13 residue row ✓ |
| `F-W1:276` | the atomic-transaction clause (G6) ✓ |
| `F-W2:149` | `MISSED-F` / `fr-EasingPicker.md:89` ✓ |
| `F-W3:783` | **BLANK — correctly so**, the round-11 `+32` drift's vacated address ✓ |
| `F-W3:815` | the `fr-AdminFlaggedPanel FR-AFP-42` row bearing `F.W9/W10`; `grep -n 'F\.W9/W10' F-W3.md` → **`815`**, sole hit ✓ |
| `F-W5:114` | the four `M-10`s clause ✓ |
| `F-W7:218` | the `B-2 … ≡ E5` §5d carry row ✓ |
| `F-W9:185` `:289` `:461` | round-11's uniform `+7` re-base; `grep -n '9 e2e never enter the audit tab' F-W9.md` → **`185 289 461`** ✓ |
| `F-W10:114` | the tooltip re-proportion row ✓ |
| `CENSUS-CANONICAL.md:393` | the `K1` E6-3 row ✓ |
| `CENSUS-CANONICAL.md:3956` `:5371` | `R2-7`'s TERMINAL row and the TERMINAL bullet ✓ |

**Every address is within its target file's length and resolves to the content its citing sentence names.**

---

## §5 — LANDED-WRONG. **ONE FINDING. MINOR. NOT ROUND 12's, AND RECORDED NOT APPLIED.**

### LW-R12-a — `F-W0:588` publishes a self-referentially false line-count receipt, unchased. **MINOR.**

**The bytes.** `F-W0` §R11-4 (`:588`) reads:

> *"**Every cure this round was an in-place, single-line replacement.** All eleven line counts are byte-identical before and after — F-W0 **535** · F-W1 735 · … · F-W10 515, **total 5989 both times**."*

**The operand.** `/usr/bin/wc -l` reads **F-W0 596**, **total 6050**.

**Why it is a defect, and it is exactly the class round 12 legislated.** The §R11 note was **appended at end-of-file**, and that append is **61 lines** (`:536`–`:596`). `596 − 535 = 61` and `6050 − 5989 = 61` — **the delta is the note's own body.** The line therefore publishes an unqualified count of its own file that **was false the instant it landed**, because the act of writing it moved the number it certifies. This is precisely §ERRATA-R12·b row 3's ruling — *"a published count is a reading of the SETTLED bytes, never of the bytes that existed while the cure was being drafted"* — with `F-W0:588` as a second member of the convicted class. ⟨cmd⟩ `awk 'NR==588' F-W0.md | grep -c 'chase\|repair round 12\|596\|6050'` → **0**: **no on-line chase.**

**Attribution, stated so it is not misfiled.**

- ⊘ **Round 12 did NOT land this.** `F-W0`'s digest is `1f5a93c90ed7`, **unmoved for four consecutive rounds**. `F-W0` is not one of round 12's three writable files. **This is a ROUND-11 residue that round 12 inherited.**
- ⊘ **The certificate is CLEAN on this axis and must not be convicted with the spec.** §ERRATA-R11·d states the same ten figures and then **expressly qualifies the eleventh**: *"with `F-W0` **535** growing **only** by its EOF append."* The cert never published the false universal. **The spec did.**
- ⊘ **`PASS-12/CHECK.md` §1 also reconciled it** — *"`6050 − 61 = 5989`, the pre-append figure `F-W0` §R11-4 publishes"* — characterising `:588`'s figure as pre-append. **But `:588` does not say "pre-append". It says "both times".** The reconciliation lives in two external authorities and in neither case on the line itself.
- ⊙ **§ERRATA-R12·c is CORRECT.** Its own sentence publishes **F-W0 596 · total 6050** at the settled bytes, and its claim to reproduce §ERRATA-R11·d holds exactly for the ten unqualified files and consistently for `F-W0` (535 + 61 = 596).

**Severity MINOR, and the reason.** Nothing downstream depends on the figure; the arithmetic closes exactly and in one step; both the cert and the check carry the correct reconciliation. **No conclusion moves.** What is wrong is that a reader re-running `wc -l` against `:588` gets a mismatch with no on-line explanation — the same reader-experience defect the corpus convicts everywhere else.

⊘ **Recorded here and applied nowhere.** This seat is VERIFY-ONLY and made **zero edits**. ▲ **The disposition this finding implies for the next seat**: round 12 wrote the write-then-measure law at `F-W2:248` but **did not sweep the corpus for other members of the class outside its three writable files.** The law's own settle order — *"re-run every touched receipt ONCE MORE"* — governs touched receipts; it does not reach a receipt no round has touched since it was minted false. **`F-W0:588` is that case, and it is the only one this seat found.**

---

## §6 — VERDICT

| axis | result |
|---|---|
| §ERRATA-R12 hash table vs the bytes | **12 / 12 byte-identical** |
| canonical pin | **`f44362757458`, FROZEN a fourth round** |
| receipts re-run verbatim | **48** |
| receipts reproducing byte-exactly | **48** · `fabricated = 0` |
| `F-W10:363` at the frozen canonical | **16 rows / 15 records / `R2-7` TERMINAL — re-derived three ways** |
| `:363` ↔ `:494` coherence | **CONFIRMED**, distance `131` exact |
| §3 aggregate re-derivation | **2466 ✓ · 249 ✓ (two independent paths)** |
| `F-W7:101` era figures | **`28 ⊕ 90` R4-5 · `27 ⊕ 89` frozen — both confirmed at the operand** |
| self-count live members | **0** |
| stale unchased magnitudes in `F-W2`/`F-W7`/`F-W10` | **0** (79 context hits · 68 chased · 11 adjudicated false positives) |
| coordinates spot-run | **16 · zero dead** |
| §2.2 splice `Δ` | **∅ — both legs** |
| landed-wrong | **1 (MINOR, `F-W0:588`, round-11 residue, recorded not applied)** |

⊙ **ROUND 12 SEALS.** The three cures are true at the bytes they shipped on, the canonical is untouched for a fourth consecutive round, and the write-then-measure law round 12 wrote is **proven by its own receipts reproducing** — which is the only proof that law can have.

---

⟨cmd⟩ this seat, 2026-08-30, base = repo root `/Users/mkbabb/Programming/value.js`, on the pinned BSD toolchain, taken **as the absolute last act of this write, after every verification closed and with ZERO edits made to any file in the corpus**:

```
shasum -a 256 /Users/mkbabb/Programming/value.js/docs/tranches/X/fourier/waves/F-W*.md \
              /Users/mkbabb/Programming/value.js/docs/tranches/X/fourier/conformance/CENSUS-CANONICAL.md \
              /Users/mkbabb/Programming/value.js/docs/tranches/X/fourier/conformance/PIN-PURGE-CERT.md
```

**Output, pasted whole and verbatim — glob order, `F-W10` sorting after `F-W1` exactly as the shell produced it:**

```
1f5a93c90ed7e9f4463d859d104b76377bbd190f5b38c8a971a2cc989f734411  /Users/mkbabb/Programming/value.js/docs/tranches/X/fourier/waves/F-W0.md
ab2437af09e0d0646260afdd024541539ad588d492b8de48aee25a8f82adff3e  /Users/mkbabb/Programming/value.js/docs/tranches/X/fourier/waves/F-W1.md
8ae0ad8fa623de2201a6184ec32dfcb2a6bd25e2443b69ad4377e24746eeddc9  /Users/mkbabb/Programming/value.js/docs/tranches/X/fourier/waves/F-W10.md
0b967619bb35763b6f1b09529db080fa2f9a37db2fa3e5e1eac70ce2aef7b7a6  /Users/mkbabb/Programming/value.js/docs/tranches/X/fourier/waves/F-W2.md
be86ae4101184d8d7278879f5ab5a8abc9102fbcfdfb554d0523affde576bfcf  /Users/mkbabb/Programming/value.js/docs/tranches/X/fourier/waves/F-W3.md
2fb6e6d637bb6d0fad710716fb1dfa103d39fc41f67d8ff0fb856e4c0126cdb6  /Users/mkbabb/Programming/value.js/docs/tranches/X/fourier/waves/F-W4.md
40bcad59cd2beed4e3e8453011b605bdcc9eab67082c9dc64bde131caea124ef  /Users/mkbabb/Programming/value.js/docs/tranches/X/fourier/waves/F-W5.md
e83dcad051f560329ade30dbb0877732650ac05071e8b2ee0eab22f56abf7e11  /Users/mkbabb/Programming/value.js/docs/tranches/X/fourier/waves/F-W6.md
ba9be3e376139204a17ad7b5ebc858513bc3880e26f63f7340ebce14145547d7  /Users/mkbabb/Programming/value.js/docs/tranches/X/fourier/waves/F-W7.md
d8237cf6b11ef248e4080b8d805a2ee7477139b8d4cf9cf3564922096e29bd45  /Users/mkbabb/Programming/value.js/docs/tranches/X/fourier/waves/F-W8.md
b265bf677869b0ec6f3c02c54b93272f657111e55498cf05bef14578d6c4b53b  /Users/mkbabb/Programming/value.js/docs/tranches/X/fourier/waves/F-W9.md
f443627574581ec2a4138e46d927b5fc431a7a6657cae766f7ffafa01e770968  /Users/mkbabb/Programming/value.js/docs/tranches/X/fourier/conformance/CENSUS-CANONICAL.md
6c1dfad4103ce8f03df89dc4c606f87bb3bf3df432ca8495ad5df1e1ea8bd7aa  /Users/mkbabb/Programming/value.js/docs/tranches/X/fourier/conformance/PIN-PURGE-CERT.md
```
