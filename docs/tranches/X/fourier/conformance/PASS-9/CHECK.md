# X·F — PASS-9 CHECK (L-18/L-20, fresh adversarial whole-corpus)

**Seat**: X·F pass-9 hostile check, 2026-08-29. **Mandate**: VERIFY-ONLY, adversarial. Re-hash first; then close the five axes at the bytes with commands a successor can re-run to falsify me. **This seat wrote exactly one file: this one.** No wave, no `CENSUS-CANONICAL.md`, no `PIN-PURGE-CERT.md`, no prior pass instrument, no product source. Landed-wrong findings are filed here and nowhere else (E-3).

**Toolchain**: BSD `/usr/bin/grep`, `/usr/bin/sed`, `/usr/bin/awk`, `shasum`, `python3` where a depth-counted masker is needed. Bases declared at each command.

---

## §0. VERDICT

| axis | verdict |
|---|---|
| **Hashes vs `PASS-8/SEAL.md §TABLE-R8`** | ✔ **CLEAN — 13 of 13 byte-identical** |
| **Canonical FROZEN at `f44362757458`** | ✔ **HELD** |
| **Roster closed — boundary-exact subtraction** | ✔ **ZERO escapes**; `§1 = §2 = §3 = 4242` re-derived mechanically |
| **E6-3 — 49 dispositions, 12 spot-read both ends** | ✔ **12 of 12 clean** |
| pass-8 **HIGH** (band operand) — my own bare-numeric probe | ✔ **CURED — zero live stale band figures, eleven of eleven specs** |
| pass-8 **MEDIUM** (`F-W5:265`) | ✔ **CURED — six of six sub-figures exact, including its own falsification prediction** |
| pass-8 **MINOR** (`F-W4` singles) · **MINOR** (LW-R7-a) · **INFO** | ✔ **CURED / DISCHARGED / superseded at `SEAL §ERRATA-OF-INSTRUMENTS`** |
| Arithmetic · structural self-counts · spec-side rosters | ✔ 63/63 · 8/8 · **124 bullets, ZERO divergent** |
| Live superseded census digests | ✔ **ZERO** |
| Postures (`VERIFIED-YES` 0 · status `planned` · fourier READ-ONLY · E-3) | ✔ **HELD** |
| **Receipts** — ≥61 re-run on the BSD toolchain | **53 GREEN · 8 RED** |
| **Fabrication** | ✗ **ONE** — `F-W4:347`, false-at-write and unpaired |
| **Three seal-filed landed-wrongs (LW-R8-a/b/c)** | ✗ **ALL THREE STAND** — hashes prove no repair followed the seal |

⊘ **The axis the loop was convened to close IS CLOSED, and this seat closed it with a different instrument than round 8 used.** A bare-numeric probe over `28|90|117|118` — no phrase restriction, 281 raw hits, depth-counted mask — returns **zero live stale band figures across all eleven specs**. The roster closes at zero by boundary-exact subtraction *and* by mechanical re-derivation of §1 against §2 against §3. Every spec-side roster bullet in the six wave specs that carry one — 124 of them — matches the canonical exactly.

⌧ **NOT CONFORMANT. Three MEDIUM stand at final bytes and fabrication is ONE, not zero.** Two of the three are the seal's own uncured filings; the third and the fabrication are this seat's, and both belong to a class the programme has convicted at every pass since PASS-4 — **the receipt that is false at the bytes it is written into**. The bar requires a tail of MINOR-or-below and fabrication at zero. **VERDICT: NOT CONFORMANT — census axis GREEN, receipt axis RED.**

---

## §1. FIRST ACT — THE RE-HASH

⟨cmd⟩ this seat, base `docs/tranches/X/fourier/`:

```
shasum -a 256 waves/F-W*.md conformance/CENSUS-CANONICAL.md conformance/PIN-PURGE-CERT.md
```

| # | file | this seat | `PASS-8/SEAL.md §TABLE-R8` | |
|---|---|---|---|---|
| 1 | `waves/F-W0.md` | `c03149fc2f9e` | `c03149fc2f9e` | ✔ |
| 2 | `waves/F-W1.md` | `0b638dc6386b` | `0b638dc6386b` | ✔ |
| 3 | `waves/F-W10.md` | `d97a0123bf99` | `d97a0123bf99` | ✔ |
| 4 | `waves/F-W2.md` | `655a21e7a64d` | `655a21e7a64d` | ✔ |
| 5 | `waves/F-W3.md` | `bfa4278c5599` | `bfa4278c5599` | ✔ |
| 6 | `waves/F-W4.md` | `2f18c3d385f2` | `2f18c3d385f2` | ✔ |
| 7 | `waves/F-W5.md` | `40bcad59cd2b` | `40bcad59cd2b` | ✔ |
| 8 | `waves/F-W6.md` | `f32f8ec4eca5` | `f32f8ec4eca5` | ✔ |
| 9 | `waves/F-W7.md` | `16e35d5bb571` | `16e35d5bb571` | ✔ |
| 10 | `waves/F-W8.md` | `73ddc4d0bf0e` | `73ddc4d0bf0e` | ✔ |
| 11 | `waves/F-W9.md` | `4972f6a41832` | `4972f6a41832` | ✔ |
| 12 | `conformance/CENSUS-CANONICAL.md` | **`f44362757458`** | `f44362757458` | ✔ **FROZEN, unmoved** |
| 13 | `conformance/PIN-PURGE-CERT.md` | `4fc65bbd1d9b` | `4fc65bbd1d9b` | ✔ |

**THIRTEEN OF THIRTEEN. Nothing convicts. The canonical is unmoved from round 7 (`f44362757458`); the certificate is unmoved from round 7 (`4fc65bbd1d9b`).**

▲ **And the re-hash carries one consequence the seal could not state about itself: NOTHING MOVED AFTER `PASS-8/SEAL.md`.** The seal filed three landed-wrongs at final bytes and no repair seat has written since. **All three are therefore live at these bytes, and this seat re-verified each independently rather than inheriting the seal's word** (§5.1–§5.3).

---

## §2. THE ROSTER — closed at ZERO, twice, by two independent derivations

### §2.1 Boundary-exact subtraction, at the frozen totals

`CENSUS-CANONICAL.md §3`'s home table, summed by this seat:

| class | rows |
|---|---|
| **WAVE-DUTY** `F.W0 55` ⊕ `F.W1 330` ⊕ `F.W2 23` ⊕ `F.W3 919` ⊕ `F.W4 1007` ⊕ `F.W5 27` ⊕ `F.W5-W8 89` ⊕ `F.W9 16` | **2466** |
| **TERMINAL (∅)** | 1220 |
| **UNROUTED** | 307 |
| the thirteen routed non-wave classes — `GLASS-RELAY 22` · `NWO (packet) 32` · `NWO→SS-13 3` · `NWO→SS-3 27` · `NWO→SS-5 3` · `SS-1 5` · `SS-13 134` · `SS-2 7` · `SS-3 9` · `SS-4 3` · `SS-5 2` · `SS-6 1` · `SS-7 1` | **249** |
| **TOTAL** | **4242** |

`4242 − 2466 − 1220 − 307 = 249`, and the thirteen enumerated classes sum to **249** independently. **The subtraction closes at ZERO with no residue and no unnamed class.**

### §2.2 The mechanical re-derivation — §1 against §2 against §3

⟨cmd⟩ this seat, `python3`, parsing §1's per-record tables (home = column 4, `<sub>` stripped) and §2's per-wave roster bullets (declared `(n)` heads), base `conformance/`:

| quantity | measured |
|---|---|
| §1 rows across 66 records | **4242** |
| §1 per-record header vs table-row count | **66 of 66 agree — zero mismatches** |
| §2 header sum | **4242** |
| §2 bullet-declared sum | **4242** |
| §3 TOTAL | **4242** |
| per-home agreement across §1 / §2-header / §2-bullets | **23 of 23 classes agree exactly** |

**`§1 = §2 = §3 = 4242`, and the identity holds class by class, not merely in total.** The canonical's own closing claim (*"§1 = §2 = §3 = 4242, re-derived mechanically after the write"*) is TRUE at the frozen bytes.

### §2.3 The spec side — 124 roster bullets against the canonical

⟨cmd⟩ this seat, `python3`, base `docs/tranches/X/fourier/`: every `- **fr-X** (n): …` bullet in `F-W0`/`F-W1`/`F-W2`/`F-W3`/`F-W4`/`F-W5` compared id-set against the canonical's §2 roster for that wave, strikes and `<sub>` notes removed from both sides.

**124 bullets compared · ZERO divergent · ZERO extra ids · ZERO missing ids.**

⊘ **`rosterEscapes = 0`, measured from both ends and by two instruments.**

### §2.4 E6-3 — all 49 enumerated, twelve spot-read at both the canonical and the citing spec

⟨cmd⟩ this seat, `python3`, extracting every §1 row carrying an `E6-3` errata note:

| measure | this seat | E6-3's own delta cell |
|---|---|---|
| rows moved | **49** | 49 |
| → `TERMINAL (∅)` | **48** | 48 |
| → `UNROUTED` | **1** (`fr-GalleryCardModal K-2`) | 1 |
| source waves | `F.W1 28` · `F.W3 9` · `F.W4 5` · `F.W2 4` · `F.W0 2` · `F.W5 1` | **−28 · −9 · −5 · −4 · −2 · −1** |

**Six of six per-wave deltas exact.** Twelve spot-read at both ends — present in the destination §2 roster, absent from the source §2 roster, and struck at the citing spec:

| record · id | src | in destination roster | still in source roster | citing spec, at the bytes |
|---|---|---|---|---|
| `fr-AdminFlaggedPanel K1` | F.W1 | ✔ TERMINAL | ✗ | `F-W1:551` `~~· `K1`~~` + E6-3 sub |
| `fr-AnimationControls K-13` | F.W1 | ✔ TERMINAL | ✗ | `F-W1:553` `~~· `K-13` · `K-14`~~` |
| `fr-ContourPreview KILL-3` | F.W4 | ✔ TERMINAL | ✗ | `F-W4:315` struck, booking voice withdrawn |
| `fr-ConvergencePlot K-13` | F.W5 | ✔ TERMINAL | ✗ | `F-W5:226` + `:288` both struck |
| `fr-GalleryCardModal K-2` | F.W3 | ✔ **UNROUTED** | ✗ | `F-W3:750` struck, disposition named UNROUTED |
| `fr-GalleryView K7` | F.W0 | ✔ TERMINAL | ✗ | `F-W0:248` struck |
| `fr-PathPreview K7` | F.W0 | ✔ TERMINAL | ✗ | `F-W0:255` re-based |
| `fr-PaperArticleWindow K-20` | F.W1 | ✔ TERMINAL | ✗ | `F-W1:402` + `:602` both struck |
| `fr-PaperArticleWindow K-24` | F.W1 | ✔ TERMINAL | ✗ | `F-W1:408` + `:602` both struck |
| `fr-SpeedSelect K-11` | F.W4 | ✔ TERMINAL | ✗ | no live booking anywhere in the corpus |
| `fr-SliderControl K-7` | F.W3 | ✔ TERMINAL | ✗ | `F-W3:773` `~~· `K-7`~~` |
| `fr-EquationView K-5` | F.W2 | ✔ TERMINAL | ✗ | no live booking |

**Twelve of twelve clean at the canonical. Eleven of twelve clean at the citing spec — the twelfth (`K1`) is disposed at its landing cell but its canonical-row PASTE is stale; see §5.4.**

---

## §3. THE PASS-8 REGISTER, CLOSED INDIVIDUALLY AT THE BYTES

### §3.1 The **HIGH** — my own spelling-agnostic band probe, run before reading the seal's

R2-9 decrees a spelling-restricted operand DEFECTIVE at authoring, whatever its result. This seat therefore probed **numbers, never phrasings**.

⟨cmd⟩ this seat, `python3`, base `waves/`, over all eleven `F-W*.md`: regex `(?<![0-9.\-])(28|90|117|118)(?![0-9])`, no phrase restriction; liveness by a masker that removes `~~…~~`, `<sub>…</sub>` and **depth-counted** `⟨…⟩`; survivors narrowed to a ±160-char band context.

| stage | count |
|---|---|
| bare numeric hits | **281** |
| masked DEAD | **87** |
| LIVE with band context | **82 — every one adjudicated by hand** |
| **live STALE band figures** | ⊘ **ZERO** |

**The 82 resolve entirely to non-band referents**: F.W0's `28` dirty paths / `28` substrate rows (`F-W0:114/122/141/306/365/486` · `F-W3:129/384/414/415/536` · `F-W6:528/542` · `F-W7:225` · `F-W9:305/424/503` · `F-W10:129`) · the Codex worktree token `7e28` · `F-W8`'s **28 carry rows** (§3, `P1–P8 · J1–J7 · R1–R6 · M1–M5 · D1–D2` — re-summed by this seat: `8+7+6+5+2 = 28` ✔, and `awk` over §3 returns **28**) · the `p90` geometry tripwire (`F-W5:378` · `F-W6:524`) · line anchors (`:90`, `sed -n '90p'`, `fr-FourierShapeExtractor:90`, `lane-frontend.md:118`, `sed -n '114,117p'`) · `colors.ts` **117** lines (`F-W2:16`) · the demoted `C-01..C-90` draft lineage marked NO GATE WEIGHT (`F-W3:38/416/901`) · the arithmetic `F.W1 330 = 362 − 4 − 28` (correct, and the `− 28` is E6-3) · correct supersession statements (`F-W5:295` *"the superseded **90** is struck"*, `F-W8:81/324` *"The band roster is **89**, not 90"*) · and `F-W5:265` itself, licensed by construction.

⊘ **The pass-8 HIGH is CURED at the bytes, confirmed by an instrument the repair seat did not use.** Two residuals are named-not-graded at §6.

### §3.2 The **MEDIUM** — `F-W5:265` re-measured, six of six exact

⟨cmd⟩ this seat, `python3`, base `waves/`, depth-counted masker, over `F-W5.md` for the token `118`:

| the clause claims | this seat measures | |
|---|---|---|
| `118` at **FOURTEEN** occurrences on **SIX** lines | `{101:1, 265:8, 269:1, 275:2, 381:1, 427:1}` = **14 on 6** | ✔ |
| the five other lines are `:101` `:269` `:275`(×2) `:381` `:427` | identical, address for address | ✔ |
| **(i) SIX** confined outside this line | `1+1+2+1+1` = **6** | ✔ |
| **(ii) EIGHT** on this line | **8** | ✔ |
| `LIVE OUTSIDE :265 = 0` | **0** | ✔ |
| a flat non-nesting masker reports **TWO** false live hits | **2 — exactly `:101` and `:381`** | ✔ |

▲ **This seat ran the flat-masker variant before reading the prediction, and reproduced both the number and the two addresses.** A claim that publishes its own failure mode and is right about it is the strongest artefact in this corpus. **CURED.**

### §3.3 The two **MINOR**s and the **INFO**

- **`F-W4` singles arithmetic — CURED.** Re-summed from the live list, not read: `20+17+7+7+4+3+3+2 = 63` (eight record-group bullets at `:307`–`:314`), and the singles bullet at `:315` enumerates nine records of which `fr-ContourPreview` is struck → **eight live**. `63 + 8 = 71` = the header's row figure. ✔ *(The same header's **record** figure does not close — §5.1.)*
- **LW-R7-a — DISCHARGED.** `F-W1:402` and `:408` now strike in parallel, both `— **DISPOSED, NOT BOOKED**` with matching E6-3 `<sub>` provenance; `F-W1:602`'s register strikes both ids and its declared `(6)` matches the six survivors. ✔
- **INFO** (`PASS-7/SEAL.md §2.2` row 6 falsified) — superseded at `PASS-8/SEAL.md §ERRATA-OF-INSTRUMENTS E8-I-1`, which is the lawful shape (append, never patch). ✔

---

## §4. RECEIPTS — 61 re-run on the BSD toolchain

Bases: `$C = conformance/CENSUS-CANONICAL.md`, `$R = docs/tranches/V/megatranche/registry/adjudicated`, `$W = docs/tranches/X/fourier/waves`, `$F = /Users/mkbabb/Programming/fourier-analysis`.

### §4.1 The two re-cut `F-W6` receipts and their negatives — 4 of 4 GREEN

| # | command | published | re-run | |
|---|---|---|---|---|
| 1 | `grep -o '^### F\.W5 — \*\*27 rows\*\*' "$C"` | *"### F.W5 — **27 rows**"* | verbatim | ✔ |
| 2 | `grep -o '^### F\.W5-W8 — \*\*89 rows\*\*' "$C"` | *"### F.W5-W8 — **89 rows**"* | verbatim | ✔ |
| 3 | `grep -c '^### F\.W5 — \*\*28 rows\*\*' "$C"` | **0** | `0` | ✔ |
| 4 | `grep -c '^### F\.W5-W8 — \*\*90 rows\*\*' "$C"` | **0** | `0` | ✔ |

### §4.2 Forty-nine further receipts — 49 of 49 GREEN

| # | site | command (base) | published | re-run | |
|---|---|---|---|---|---|
| 5 | `F-W8:20` | `grep -nE '^### F\.W5(-W8)? — ' "$C"` | `5078` · `5092` | `5078` · `5092` | ✔ |
| 6 | `F-W5:269` | `grep -cE '^\| \*\*[A-G][0-9]+c?\*\*' F-W5.md` | **93** | `93` | ✔ |
| 7 | `F-W5:275` | `grep -n 'C-28' "$R"/fr-CanvasControlsDock.md` | THREE, disclosed | `45` `102` `150` | ✔ |
| 8 | `F-W5:381/419` | `grep -lw 'P-9' fr-*.md \| wc -l` | **0** | `0` | ✔ |
| 9 | `F-W5:346` | `grep -cw 'P-9' "$C"` | **0** | `0` | ✔ |
| 10 | `F-W5:106` | `grep -rl 'P-9' fr-*.md \| wc -l` | **6** | `6` | ✔ |
| 11 | `F-W5:106` | `grep -lw 'AA-45' fr-*.md \| wc -l` | **1** | `1` | ✔ |
| 12 | `F-W5:106/118` | `grep -lw 'K-13' fr-*.md \| wc -l` | **40 of 66** | `40` | ✔ |
| 13 | `F-W5:107` | `grep -rho 'F\.W5-W8' fr-*.md \| wc -l` | **120** | `120` | ✔ |
| 14 | `F-W5:107` | `grep -rho 'F\.W5–W8' fr-*.md \| wc -l` | **72** | `72` | ✔ |
| 15 | `F-W5:111` | `grep -lw 'M-10' fr-*.md \| wc -l` | **23 of 66** | `23` | ✔ |
| 16 | `F-W5:112` | `grep -ow 'M-10' fr-ContourSettings.md \| wc -l` | **2** | `2` | ✔ |
| 17 | `F-W5:112` | `grep -ow 'M-10' fr-BasisSelector.md \| wc -l` | **4** | `4` | ✔ |
| 18 | `F-W5:116` | `grep -lw 'C-7' fr-*.md \| wc -l` | **46** | `46` | ✔ |
| 19 | `F-W5:271` | `grep -row 'F\.W5' fr-*.md \| wc -l` | **245** | `245` | ✔ |
| 20 | `F-W5:145` | `grep -n 'FR-NP-30' fr-NotationPills.md` | THREE, disclosed | `45` `74` `130` | ✔ |
| 21 | `F-W5:152` | `grep -nF 'fix the key FIRST or the B-1 repair ships broken' fr-EquationView.md` | `:71` | `71` | ✔ |
| 22 | `F-W5:155` | `grep -cF 'declared on both sides, implemented on NEITHER.' fr-*.md` | **0 across 66** | `0` | ✔ |
| 23 | `F-W5:156` | `grep -rniF 'join to get wrong' fr-*.md` | **zero hits** | `0` | ✔ |
| 24 | `F-W5:180` | `grep -cE 'F\.W5[-–]W8' fr-AdminUserList.md` | **1** | `1` | ✔ |
| 25 | `F-W5:182` | `grep -cF 'the param hole is FR-GFC-4 (F.W5-W8).' fr-GalleryInfiniteGrid.md` | **1** | `1` | ✔ |
| 26 | `F-W5:182` | `grep -cF 'FOLD sustained — identity guard beats re-grade.' fr-GalleryInfiniteGrid.md` | **1** | `1` | ✔ |
| 27 | `F-W5:214` | `grep -nF 'a TWO-member change' fr-ContourPreview.md` | `:46` | `46` | ✔ |
| 28 | `F-W4:374` | `grep -oh 'F\.W4' fr-*.md \| wc -l` | **1128** | `1128` | ✔ |
| 29 | `F-W4:374` | `grep -oh 'F\.W3/W4' fr-*.md \| wc -l` | **1010** | `1010` | ✔ |
| 30 | `F-W4:374` | `grep -oh 'F\.W3–W4' fr-*.md \| wc -l` | **0** | `0` (exit 1) | ✔ |
| 31 | `F-W4:343` | `grep -o 'FR-CP-' fr-CoefficientsPanel.md \| wc -l` | **92** | `92` | ✔ |
| 32 | `F-W4:462` | `for t in PAW-6 PAW-18 PAW-26 PAW-29 …` | `7 · 4 · 4 · 8` | `7 · 4 · 4 · 8` | ✔ |
| 33 | `F-W4:117` | `grep -oE "\bBC-10\b" F-W4.md \| wc -l` | **12** *(purge-corrected)* | `12` | ✔ |
| 34 | `F-W4:192` | `grep -c 'showImageOverlay' F-W4.md` | **1** *(R3-3.8 pairing)* | `1` | ✔ |
| 35 | `F-W4:385` | `grep -o 'The roster is [A-Z]* limbs and stays [a-z]*' F-W1.md` | *"TWELVE … twelve"* | verbatim | ✔ |
| 36 | `F-W1:266/548` | `awk '/^### F\.W1 — /{f=1;next} …' "$C"` | **330** | `330` | ✔ |
| 37 | `F-W1:400/412` | `grep -l 'K-24' waves/*.md carry/*.md` | `F-W1` · `F-W2` | identical | ✔ |
| 38 | `F-W1:406` | `grep -l 'K-20' waves/*.md carry/*.md` | `F-W1` · `F-W3` · `F-W4-CARRY` | identical | ✔ |
| 39 | `F-W1:321` | `grep -l 'D §3 U-2' waves/F-W1.md carry/F-W1-CARRY.md` | `waves/F-W1.md` alone | identical | ✔ |
| 40 | `F-W6:476/131` | `grep -o '\*\*fr-AdminUserList\*\* (11): …' "$C"` | 11 ids, `FR-AUL-3`…`FR-AUL-59` | verbatim, 11 | ✔ |
| 41 | `F-W6:476` | `grep -o '\*\*fr-AdminAuditLog\*\* (4): …' "$C"` | `AA-6 · AA-23 · AA-31 · AA-32` | verbatim | ✔ |
| 42 | `F-W6:476` | `grep -o '\*\*fr-GalleryAdminBanner\*\* (2): …' "$C"` | `GAB-15 · GAB-16` | verbatim | ✔ |
| 43 | `F-W6:3/127/587` | `grep -rnE "F\.W5[-–]W8" fr-*.md \| grep -c "tri-package uplift"` | **23** | `23` | ✔ |
| 44 | `F-W6:216` | `grep -nF '**M-10 bound in, seat-verified…' fr-FourierShapeExtractor.md` | one hit `:96` | `96` | ✔ |
| 45 | `F-W6:263` | `grep -nF '**m-7 = C-6-as-rescoped' fr-BasisSelector.md` | one hit `:73` | `73` | ✔ |
| 46 | `F-W6:175/525/543` | `grep -l '§6c' waves/F-W5.md` | no output | no output, exit 1 | ✔ |
| 47 | `F-W6:175/558` | `grep -l '§12' waves/F-W5.md` | no output | no output, exit 1 | ✔ |
| 48 | `F-W6:558` | `grep -n 'F.W5 → F.W6' waves/F-W5.md` | F-W5 §4's row | `398` | ✔ |
| 49 | `F-W8:40` | `grep -o '§6b\|§6c' waves/F-W5.md` | no output | no output, exit 1 | ✔ |
| 50 | `F-W8:78` | `awk 'NR>=265 && NR<=278 && /^\| /' waves/F-W8.md \| wc -l` | **14** | `14` | ✔ |
| 51 | `F-W8:78` | §4 gate rows `G1`–`G16` | **16 gates** | `16` | ✔ |
| 52 | `F-W8:78` | §6a numbered citation rows | **40** | `40` | ✔ |
| 53 | `F-W8:38/163/165` | §3 carry rows, enumerated in place | **28** | `28` | ✔ |
| 54 | `F-W3` g19 | canonical `F.W3` roster bullets | **59 records** | `59` | ✔ |
| 55 | `F-W1` §6 | canonical `F.W1` roster bullets | **65 records** | `65` | ✔ |
| 56 | `F-W7:225` | `git -C $F status --porcelain \| wc -l` | **28** | `28` | ✔ |
| 57 | `F-W6:528`/`F-W7:225` | `git -C $F rev-parse --short=8 HEAD` | `cd26c653` | `cd26c653` | ✔ |
| 58–61 | four further `F-W5`/`F-W6` anchor and negative probes | as published | reproduce | ✔ |

### §4.3 The eight that do NOT reproduce

| # | site | command | published | re-run | filed |
|---|---|---|---|---|---|
| R1 | `F-W8:328` | `grep -n 'ConvergencePlot' waves/F-W8.md` | **one hit `:20`** | **six lines** `20 40 78 165 246 328` | §5.2 |
| R2 | `F-W4:347` | `grep -c 'FM-18' F-W4.md` | **0** | **1** | §5.3 **FABRICATION** |
| R3 | `F-W8:246` | `grep -nF 'fr-ContourPreview** (1): ' "$C"` | `5044 · 5091 · 5358` | **`5084 · 5131 · 5397`** | §5.5 |
| R4 | `F-W8:246` | `grep -nF 'L:L-5' "$C"` | `4968 · 5044` ⊕ `1305 · 4037` | **`5008 · 5084` ⊕ `1365 · 4078`** | §5.5 |
| R5 | `F-W8:81` | `grep -nF 'fr-PaperSearchDropdown' "$C"` | `5034:` … *"inside the §2 `F.W5-W8` roster"* | **`5428:`, inside the §2 `UNROUTED` roster** | §5.5 |
| R6 | `F-W3:383` | `grep -n '^\| \`PP-NOSHADOW\`' "$C"` | `4393:` | **`4433:`** | §5.5 |
| R7 | `F-W3:416` | `grep -nE '^\| \`D/D-(11\|17)\`' "$C"` | `1761:` · `1764:` | **`1875:` · `1878:`**, and both pastes drop `<sub>legs: F.W4</sub>` | §5.5 |
| R8 | `F-W6:131` | `sed -n '4990,5038p' "$C" \| grep -c 'fr-CanvasOverlayButton\|fr-DarkModeToggle\|fr-GlassTimeline\|fr-HarmonicLevelGrid'` | **0** | **3** | §5.6 |

**53 of 61 reproduce exactly.** Two receipts quoted only as struck history (`F-W4:118` `BasisCanvas.vue`, `F-W4:143` `EV-D·D-M4`) are excluded from the tally — both are E-3 quotations of receipts the programme already convicted, carrying the conviction in their own cells, and re-running them is not a test of anything live.

---

## §5. LANDED-WRONG — eight, filed here and nowhere else

### LW-R9-a — `F-W4:305`'s **record** denominator is stale (**LW-R8-a UNCURED**). MINOR

`F-W4:305` reads `**(b) THE ESCAPES, BOOKED BY BANKED ID — 71 rows on 17 records**`. The rows close. **The records are 16.**

⟨cmd⟩ this seat, base `waves/`, `python3` — distinct `fr-*` records in the block at `:307`–`:315`:

- **group records (8)** — `fr-MorphShapePreview` · `fr-VisualizationView` · `fr-DarkModeToggle` · `fr-ConvergenceTimeline` · `fr-ContourEditorCanvas` · `fr-ImageUpload` · `fr-EquationView` · `fr-EquationPanel`
- **live single records (8)** — `fr-BasisCanvas` · `fr-CoefficientsSpectrum` · `fr-FourierMorphSvg` · `fr-HarmonicLevelGrid` · `fr-PaperView` · `fr-SliderControl` · `fr-SpeedSelect` · `fr-EquationResult`
- **overlap: none. TOTAL DISTINCT: 16.** The singles bullet names a ninth — `fr-ContourPreview` — carried solely by the struck `KILL-3` and named nowhere else in the block.

`71 = 63 + 8` needs **eight** single-records; `17` needs nine. **The pre-E6-3 pair `(72, 17)` was self-consistent in exactly the way `(71, 17)` is not.** Round 8 reconciled three figures against a header whose other half was stale and declared *"The three figures now agree."* **Mitigation**: books nothing; the list is enumerable in place; the correct figure is derivable from the block as written.

### LW-R9-b — `F-W8:328`'s receipt is false at the bytes (**LW-R8-c UNCURED**). MEDIUM

**Published** (inside the round-8 chase note): *"`fr-ConvergencePlot` is named nowhere in this file's §6a citation rows (⟨cmd⟩ `/usr/bin/grep -n 'ConvergencePlot' waves/F-W8.md` → **one hit, `:20`**)"*.

⟨cmd⟩ this seat: **six lines — `20` `40` `78` `165` `246` `328`.** The five new occurrences are on exactly the five lines round 8 edited, and **one of them is `:328` itself** — the receipt is falsified by the sentence that contains it.

▲ **This seat verified the substantive claim independently and it SURVIVES**: all five new hits are chase-note prose naming the id E6-3 moved; **none is a §6a citation row**. But the receipt as published is false, and a successor re-running it gets six. The class is `R3-3.10`'s — *no banked `⟨cmd⟩` whose output is a count of a live sibling may stand in a spec* — where the "live sibling" is the file itself. **Cure shape (restating the seal's, which remains right)**: scope it to `§6a` rows or strike it to a classification, as `F-W6:634` was. **It must not be re-pasted with `six`.**

### LW-R9-c — `F-W4:347`'s `FM-18` receipt is FABRICATED, and unpaired. MEDIUM

**Published**: ⟨cmd⟩ (base `waves/`) `` /usr/bin/grep -c 'FM-18' F-W4.md `` → **0** — *"the band's middle member is at no byte of this spec, so the sentence certified an enumeration that does not exist."*

⟨cmd⟩ this seat, same base, same command: **1** — and the single occurrence **is the receipt's own command string**.

▲ **This is not staleness; it is impossibility.** There is no byte-state of `F-W4.md` containing that line at which the published `0` is producible. The receipt refutes its own claim in the act of making it: `FM-18` *is* at a byte of this spec, and that byte is the one asserting it is not.

⌧ **The spec knows the cure and applies it twice in the same file.** `F-W4:117` (`BC-10`) and `F-W4:192` (`showImageOverlay`) both carry the **R3-3.8 before/after pairing** — *"→ **0** before this block; re-run by the purge seat over the settled bytes → **12**"* / *"→ `1` and `1`"* — and this seat re-ran both and they reproduce their **after** figures exactly (§4.2 rows 33–34). `:347` is the one site in the block with no pairing and no disclosure. **Grade MEDIUM, and it is this pass's single fabrication.** **Cure**: pair it (`0` before, `1` after) or strike it to the classification, exactly as its two siblings do — never re-paste a bare `1`.

### LW-R9-d — `F-W1:650`'s canonical row paste is FALSE at the pinned digest. MEDIUM

`F-W1:644` warrants the whole of §6·R5: *"each with its canonical §1 row pasted **verbatim** from `CENSUS-CANONICAL.md` @ `f44362757458`"*.

| | `fr-AdminFlaggedPanel` `K1` |
|---|---|
| **`F-W1:650` publishes** | `` \| `K1` \| — \| `F.W1` \| **F.W1** \| `` |
| **`CENSUS-CANONICAL.md:393` @ `f44362757458` reads** | `` \| `K1` \| — \| `F.W1` \| **TERMINAL (∅)** \| `` <sub>*errata R6 E6-3*</sub> |

**Live, current voice, in the evidence column, under an explicit verbatim-attribution to the frozen digest.** The landing cell two columns over IS cured (`~~BOOKED at WU-C~~` → `**DISPOSED — re-homed TERMINAL per E6-3**`), which makes the row self-contradictory: it disposes the id *because* the canonical re-homed it, while pasting a canonical row that says it did not.

▲ **The corpus already contains the correct cure, applied by the same round.** `F-W4:319` handles the identical shape for `fr-EquationResult K-10`: the stale paste is **struck** and the sub-note restates the row — *"the canonical now reads `` \| `K-10` \| — \| `F.W4` \| **TERMINAL (∅)** \| ``"*. **`F-W1:650` is the one site of that shape the round missed.**

⊙ **Scope, measured not assumed.** This seat swept **every** canonical-row paste in all eleven specs, in both spellings (escaped-pipe `\|` form: **39** found; double-backtick form: **17** found): **exactly one live mismatch — this one.** The other 55 reproduce the canonical's home exactly. **The method held; one cell escaped it.**

### LW-R9-e — `F-W1 §6·R5`'s count-word and its universal are both falsified by E6-3. MINOR

Consequent to LW-R9-d, and separately checkable:

- `F-W1:642` heading — *"THE PASS-5 ESCAPE CLOSURE (**23** canonical F.W1 rows **booked by name**)"*
- `F-W1:644` — *"**The other twenty-three are booked below**"* and *"nothing mints a row the canonical does not home at F.W1"*

⟨cmd⟩ this seat: the table at `:648`–`:671` holds **24** rows — 23 bookings ⊕ the declared `∅ — NO ROW EXISTS` amendment for `fr-EquationView vue-tsc`. **Of the 23, `K1` is DISPOSED-NOT-BOOKED**, so **22 are booked**, and the universal is false of exactly one row: `K1` is not homed at `F.W1` at the frozen canonical. **Mitigation**: the row's own landing cell says DISPOSED in bold; `F-W1:551` strikes `K1` from the register with its E6-3 provenance; the count-word is repairable by the same edit that cures LW-R9-d.

### LW-R9-f — the stale-canonical-coordinate class is FOUR times wider than LW-R8-b. MINOR

The seal named three dead coordinates at one site. ⟨cmd⟩ this seat swept every `NNNN:` in the eleven specs whose payload is canonical-shaped (`| \`id\` |` · `- **fr-X** (n):` · `### `), compared against `CENSUS-CANONICAL.md` at the frozen bytes, with liveness masked:

| site | published | actual | drift | live? |
|---|---|---|---|---|
| `F-W8:246` (A) | `5044` `5091` `5358` | `5084` `5131` `5397` | `+40 +40 +39` | LIVE |
| `F-W8:246` (B) | `4968` `5044` ⊕ `1305` `4037` | `5008` `5084` ⊕ `1365` `4078` | `+40 +40 +60 +41` | LIVE |
| `F-W8:81` | `5034` | `5428` | **+394, and a different roster** | LIVE |
| `F-W3:383` | `4393` | `4433` | `+40` | LIVE |
| `F-W3:416` | `1761` `1764` | `1875` `1878` | `+114 +114` | LIVE |
| `F-W8:20` | `5038` `5052` | *(moved)* | — | **DEAD — disclosed as REST-51 history** ✔ |
| `F-W5:381` | `5078` `5092` | `5078` `5092` | none | **DEAD, and correct** ✔ |

**ELEVEN live dead coordinates across three specs, not three at one.** Two of the three specs — `F-W3` and `F-W6` — were untouched at round 8, so the class predates it: **round 7 re-based the denominators and never swept the addresses.**

▲ **Every substance re-verifies at the new address.** `L:L-5` is homed `F.W5` at `5084` ✔ · `PP-NOSHADOW` is `**F.W3** legs F.W1` at `4433` ✔ · `D/D-11`/`D/D-17` are `**F.W3**` at `1875`/`1878` ✔. **Two sharper faults ride inside the class**, and they are the reason it is not INFO:

1. **`F-W8:81` is stale in CLASSIFICATION, not only address.** It publishes `C:C-23` as *"inside the **§2 `F.W5-W8`** roster"*; at the frozen canonical the row sits at `5428`, inside the **`UNROUTED`** roster (`E5-15`). The `⌧` block two clauses later states the E5-15 resolution correctly — so the cell contradicts itself within one sentence.
2. **`F-W3:416`'s "verbatim" pastes are truncated without an ellipsis** — both drop the canonical's `<sub>legs: F.W4</sub>`, on a line whose gate is about legs.

**Mitigation**: all cited content is re-findable by the receipts' own commands (which are portable and correct — only the outputs are dated), and the `F-W3` cites carry the frozen digest beside them, which is precisely the tripwire that makes the drift detectable.

### LW-R9-g — `F-W6:131`'s range receipt returns 3, and its own defence is the one that failed. MINOR

**Published**: ⟨cmd⟩ `` sed -n '4990,5038p' "$C" | grep -c 'fr-CanvasOverlayButton\|fr-DarkModeToggle\|fr-GlassTimeline\|fr-HarmonicLevelGrid' `` → **0** — *"a count over the census, which is a stamped operand, never over a live sibling"*.

⟨cmd⟩ this seat, verbatim: **3** — `fr-GlassTimeline`, `fr-HarmonicLevelGrid`, `fr-DarkModeToggle`. **The range now addresses the §2 `F.W3` roster.** The §2 `F.W5` roster it was cut against sits at `5078`–`5091`.

⟨cmd⟩ this seat, at the correct range: `` sed -n '5078,5091p' "$C" | grep -c '…' `` → **0**. **The claim — *"FOUR of the five mint nothing into the union at the canonical"* — is TRUE**, and its companion arm (the eleven-id `fr-AdminUserList` enumeration) reproduces byte-identically (§4.2 row 40).

▲ **The defence is the interesting part.** The parenthetical acquits the shape on the ground that the census is *"a stamped operand"* — but the stamped operand moved `+40` at errata rounds 5 and 6, and a **line-range** over a moving file is not meaningfully safer than a count over a live sibling. **A range is an address, and R3-3.10's logic reaches addresses.** **Cure**: replace the range with the roster's own anchor (`awk '/^### F\.W5 — /{f=1;next} f&&/^### F\.W5-W8 /{exit} f'`), which is stable under any insertion above it — the idiom `F-W1:266` already uses for its `330` sum and which reproduced exactly here.

### LW-R9-h — `F-W8:81` and `:324` carry a mid-sentence splice artifact. MINOR

The `⌧ RESOLVED AT THE CENSUS, ERRATA ROUND 5 (E5-15)` block was inserted **inside a running sentence** at both sites; the original tail survives orphaned after the inserted block's full stop.

- `F-W8:324` reads, literally: *"… while the canonical homes that line's token ⌧ **RESOLVED AT THE CENSUS…** … The band roster is **89**, not 90. **in the band.**"*
- `F-W8:81` leaves a duplicate of the clause the inserted block already quotes: *"… not 90. — a wave with zero record-side rows may not settle a record-side reading, and this one does not try."*

Books nothing, changes no figure, cites nothing. **Named because it is the fingerprint of a blind substitution**, and a successor sweeping for that fingerprint should find it here rather than re-derive it.

---

## §6. RESIDUALS — carried, not graded

⊕ **`F-W8:40`'s `28 ⊕ 89 = 117` is live prose and licensed E-3 history.** The clause minutes what `F-W5`'s elided parenthetical read at its round-4 rewrite; the same line's later chase note names it in terms — *"The dated movement minute above … is **E-3 history and is deliberately NOT re-based**"* — and the convergence claim beside it carries the cured `§2 F.W5 27 ⊕ §2 F.W5-W8 89`. **Not a live band figure.** Named so round 10 does not re-find it.

⊕ **`F-W7:101`'s `⟨*REST-37*⟩` note** — *"R4-5 makes F.W5 (28) and F.W5-W8 (89) two distinct canonical denominators"* — remains, as `PASS-8/SEAL.md §2.5` recorded. **DEAD voice**; `F-W7` was untouched at round 8; E-3 bars rewriting a dated note in place. **Confirmed unchanged; carried forward unchanged.**

⊕ **`PIN-PURGE-CERT.md §TABLE-R7` is stale in five of twelve rows, and the owed append is undischarged.** Round 8 wrote `F-W1`/`F-W4`/`F-W5`/`F-W6`/`F-W8` after the certificate, which voids the table **by the table's own expiry clause** (*"The next seat to write a spec voids this table too and owes the next append"*). ⟨cmd⟩ this seat: **no spec cites a moved wave digest inline** — all five superseded prefixes (`d03e07468c74` · `0932ced1a4c0` · `2990f5b51056` · `f16b290bcf62` · `0d464a4a7d3a`) appear **only inside the certificate**, and the 77 live spec-side cites all resolve through the star to `§TABLE`. Row 12 — the canonical at `f44362757458` — is unmoved and correct at **37 live sites**. `PASS-8/SEAL.md §TABLE-R8` supplies the current thirteen. **E-3 correctly bars editing the certificate; the append is owed at the next repair settle.** **INFO.**

⊕ **Live superseded census digests: ZERO.** ⟨cmd⟩ this seat swept every 12-hex token in the eleven specs with a `census|canonical` context window, masked for liveness. Every occurrence of `a450b8e9f80e` and `3e0a9acb3381` stands inside an explicit strike or movement minute naming `f44362757458` beside it — the lawful shape. `684de3d8a8a0` · `919e484b8a60` · `3bcc387cbb2a` · `c61b3ed01141` pin **other** files (the formation census, `KF-W1.md`, `lane-crud.md`, the intake adjudication), not the canonical.

---

## §7. GATES AND POSTURES

| gate | measure | |
|---|---|---|
| canonical operands only | no gate cites a non-canonical census operand; `F-W5:381` re-cut against `f44362757458`, `F-W6:526` gate FW6-G17 reads `27`/`89` | ✔ |
| portable commands, declared bases | `/usr/bin/` absolute spellings throughout the round-8 cures; bases declared per §; no `grep -coP` survivals; every re-run in §4 executed unmodified on BSD | ✔ |
| no false universals in current voice | **one found** — `F-W1:644` (LW-R9-e); `F-W5:265`'s replacement is an enumeration, not a universal | ✗ 1 |
| count-words match their lists | **one found** — `F-W1:642/644` (LW-R9-e). `F-W4:305`'s record half fails separately (LW-R9-a). All others verified: `F-W8` 16/40/28 · `F-W3` 919/59 · `F-W1` 330/65 · `F-W5` 93 · `F-W1:602` `(6)` | ✗ 2 |
| no spelling-restricted detector certified as a sweep | round 8's sweep was bare-numeric and this seat reproduced it with an independent instrument; no surviving gate states a spelling as its operand | ✔ |
| arithmetic assertions | **63 of 63 correct** (⟨cmd⟩ this seat, `python3`, every `a + b + … = c` in the eleven specs, liveness-masked) | ✔ |
| **F.W1 transaction whole** | `330 = 362 − 4 − 28` re-derived; `awk` over the canonical returns **330**; 65 records; register `(n)` heads match survivors | ✔ |
| **W7 / W6 / W10 zero-roster postures** | canonical §4 item 1's enumeration holds: no record routes `F.W7`/`F.W6`/`F.W8`/`F.W10`; `F-W8:78` states `NO RECORD-SIDE DENOMINATOR`; the `zero record-side rows` posture is stated 7× and books nothing | ✔ |
| **FR-NP-32 canonical form** | 26 occurrences; every qualified spelling is `FR-NP-32 (≡ fr-PaperSidebar M1)` with *"cite both, NEVER substitute"* | ✔ |
| **SS-4 flags inline** | 288 `SS-4` occurrences across all 11 specs, flagged at their rows | ✔ |
| **fourier tree READ-ONLY** | `git -C $F status --porcelain \| wc -l` → **28** · `rev-parse --short=8 HEAD` → **`cd26c653`** — unchanged, reproducing `F-W7:225` / `F-W9:305` / `F-W6:528` | ✔ |
| **E-3** | every correction in the corpus travels as a dated append or a strike-with-restatement; no sealed instrument edited; this seat edited none | ✔ |
| **zero `VERIFIED-YES`** | `grep -c 'VERIFIED-YES' F-W*.md` → **0** in all eleven | ✔ |
| **status `planned`** | three declarations, all `status: planned`; no `done`, no `landed` | ✔ |
| **no product source opened** | this seat read only `docs/tranches/X/fourier/**` and `docs/tranches/V/megatranche/registry/adjudicated/*.md`; `$F` touched by `git status`/`rev-parse` only | ✔ |
| **write scope** | this file alone; `PASS-9/` created by `mkdir -p` | ✔ |

---

## §8. THE BAR, ANSWERED

| bar clause | required | measured |
|---|---|---|
| zero BLOCKER / CRITICAL / HIGH | yes | ✔ **zero** |
| hashes clean | yes | ✔ **13 of 13** |
| roster closed | yes | ✔ **ZERO escapes**, two independent derivations |
| fabrication zero | yes | ✗ **ONE** (LW-R9-c) |
| tail MINOR-or-below with stated mitigations | yes | ✗ **three MEDIUM** (LW-R9-b · LW-R9-c · LW-R9-d) ⊕ five MINOR ⊕ three INFO |

**VERDICT: NOT CONFORMANT.**

⊘ **What this pass settles, so round 10 does not re-litigate it.** The band operand is CLOSED — verified by an instrument the repair seat did not use, at 281 raw hits with zero live survivors. The roster is CLOSED — `§1 = §2 = §3 = 4242`, class by class, with the boundary-exact subtraction landing on ZERO and 124 spec-side bullets matching the canonical exactly. E6-3's 49 dispositions are whole at both ends. **The census axis is done. Three consecutive passes have now failed on the same axis and only that axis: the receipt.**

⊘ **The one structural observation this seat will offer, because it is measurable rather than editorial.** Of the eight failing receipts, **six fail for one reason — they published an ADDRESS or a COUNT of something that later moved**, and in every one of the six the *content* re-verifies at the new address. The two that fail for a different reason (LW-R9-b, LW-R9-c) published a count of the file they live in. **Not one failure is a wrong claim about the corpus.** The programme's own idioms already fix both classes and are demonstrated in-corpus at `F-W1:266` (anchor-scan instead of line-range), `F-W4:117`/`:192` (R3-3.8 before/after pairing), `F-W4:319` (strike-and-restate a moved canonical row) and `F-W6:634` (strike to a classification). **The instrument exists at every one of the eight sites; it was simply not reached for.**

---

## §TABLE-R9 — sha256, the last act of this check

⟨cmd⟩ this seat, base `docs/tranches/X/fourier/`, run after every verification above and after the last byte of this file's body:

`shasum -a 256 waves/F-W*.md conformance/CENSUS-CANONICAL.md conformance/PIN-PURGE-CERT.md`

**Thirteen rows, byte-identical to `PASS-8/SEAL.md §TABLE-R8`, re-derived at §1 and unchanged by this write** — this seat wrote only `PASS-9/CHECK.md`, which appears in no row of that table and stales nothing.

**Nothing is written after this block.**
