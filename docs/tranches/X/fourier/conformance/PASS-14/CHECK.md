# X·F PASS 14 — FRESH ADVERSARIAL WHOLE-CORPUS CHECK (L-18 / L-20)

**2026-08-30 · check seat · base = repo root `/Users/mkbabb/Programming/value.js` · pinned BSD toolchain (`/usr/bin/grep`, `/usr/bin/sed`, `/usr/bin/awk`, `/usr/bin/wc`), spelled absolutely at every receipt; `python3` used only for structural parsing, declared at each site.** **ZERO edits to any file in the corpus, the canonical, the certificate, the carries, the waves or the registry.** No product source was opened. The only file this seat wrote is this one, and it is an operand for no gate.

---

## VERDICT — **NOT CONFORMANT**, and by exactly one carried MEDIUM.

**Hashes clean (13/13). Roster closed at ZERO escapes, ZERO phantoms, ZERO home-mismatches. Fabrication ZERO across 56 re-run receipts. Zero BLOCKER, zero CRITICAL, zero HIGH.** Four of the five axes HOLD outright.

**The bar is missed on the tail clause alone.** `F-W3:780` — the surviving member `§ERRATA-R13·c` and `PASS-13/SEAL.md` §4 both RECORD and deliberately leave uncured — is reproduced independently by this seat's own instrument, in the present indicative, attributed expressly to the canonical, **carrying not one chase token**. It is the identical shape and the identical class this programme graded **MEDIUM** at pass 13 (`D13-1`, `F-W5:347`) and cured at round 13. Grading it MINOR at pass 14 to clear a bar it did not clear at pass 13 would be the softening the charge forbids, so it is graded **MEDIUM** and it blocks.

⊙ **The briefing this seat was handed is FALSE against the instruments of record and this seat says so.** The charge states the pass-13 class was *"cured at round 13 with the class re-measured at zero corpus-wide by two disjoint instruments."* `PIN-PURGE-CERT.md:705`–`:712` (`§ERRATA-R13·c`) says the opposite in its own heading — ***"THE CLASS IS NOT CLOSED, and this certificate says so rather than certifying a universal its own instrument falsifies"*** — and names `F-W3:780` as the surviving member. `PASS-13/SEAL.md` §4 endorses it. This seat reproduces it from a comparator built from the frozen canonical without reading either instrument's finding first. **Three independent instruments, one answer.**

⊘ **This is one numeral plus a chase from CONFORMANT.** With `waves/F-W3.md` writable, round 14 cures it in the idiom `F-W5:347`, `F-W9:131` and `F-W10:363` now share four times over, and the fifteenth pass should find nothing material — a sentence this seat writes knowing pass 13 wrote it and was falsified, and which is therefore stated as a prediction with its own falsifier named at §7.

---

## §1 — FIRST ACT: THE RE-HASH AGAINST `PASS-13/SEAL.md` §8. **13 / 13 BYTE-IDENTICAL.**

⟨cmd⟩ this seat, before any other probe, base = repo root:

```
shasum -a 256 docs/tranches/X/fourier/waves/F-W*.md \
              docs/tranches/X/fourier/conformance/CENSUS-CANONICAL.md \
              docs/tranches/X/fourier/conformance/PIN-PURGE-CERT.md
```

| # | file | `PASS-13/SEAL.md` §8 | this seat | verdict |
|---|---|---|---|---|
| 1 | `waves/F-W0.md` | `3d62e5f4e78b` | `3d62e5f4e78b` | **MATCH** |
| 2 | `waves/F-W1.md` | `ab2437af09e0` | `ab2437af09e0` | **MATCH** |
| 3 | `waves/F-W10.md` | `8ae0ad8fa623` | `8ae0ad8fa623` | **MATCH** |
| 4 | `waves/F-W2.md` | `0b967619bb35` | `0b967619bb35` | **MATCH** |
| 5 | `waves/F-W3.md` | `be86ae410118` | `be86ae410118` | **MATCH** |
| 6 | `waves/F-W4.md` | `2fb6e6d637bb` | `2fb6e6d637bb` | **MATCH** |
| 7 | `waves/F-W5.md` | `26aebcdc7bac` | `26aebcdc7bac` | **MATCH** *(the round-13 cure, settled)* |
| 8 | `waves/F-W6.md` | `e83dcad051f5` | `e83dcad051f5` | **MATCH** |
| 9 | `waves/F-W7.md` | `ba9be3e37613` | `ba9be3e37613` | **MATCH** |
| 10 | `waves/F-W8.md` | `d8237cf6b11e` | `d8237cf6b11e` | **MATCH** |
| 11 | `waves/F-W9.md` | `b265bf677869` | `b265bf677869` | **MATCH** |
| 12 | `conformance/CENSUS-CANONICAL.md` | **`f44362757458`** | **`f44362757458`** | **MATCH — FROZEN, SIXTH consecutive round** |
| 13 | `conformance/PIN-PURGE-CERT.md` | `a3861e158e49` | `a3861e158e49` | **MATCH — the seal read the certificate this seat read** |

⊙ **No mismatch. No conviction on this axis.** The canonical is FROZEN at `f44362757458` — identical to `§TABLE-R7`, `§ERRATA-R9`, `-R10`, `-R11`, `-R12`, `-R13` and `PASS-13/SEAL.md` row 12.

⊙ **The tree is unmoved and READ-ONLY.** ⟨cmd⟩ `git status --porcelain docs/tranches/X/fourier/` → the same four rows before and after this seat's work: `M PIN-PURGE-CERT.md` · `M F-W0.md` · `M F-W5.md` · `?? PASS-13/SEAL.md`. **The canonical is not among them; no file this seat touched is among them.**

⊙ **No stale pin propagated.** ⟨cmd⟩ `/usr/bin/grep -rn '77854a06b2ee\|75a08eeee03a\|9415e4c93e57' waves/` → **∅ (exit 1)**.

---

## §2 — THE ROSTER, AT THE FROZEN TOTALS. **CLOSES AT ZERO IN FOUR DIRECTIONS.**

### §2.1 Boundary-exact subtraction

`ROWS 4242 − WAVE-DUTY 2466 − TERMINAL 1220 − UNROUTED 307 = **249**`, and **249 is enumerated, not a residue**: the thirteen relay/packet rosters, summed from §3's own table — `GLASS-RELAY 22 ⊕ NWO 32 ⊕ NWO→SS-13 3 ⊕ NWO→SS-3 27 ⊕ NWO→SS-5 3 ⊕ SS-1 5 ⊕ SS-13 134 ⊕ SS-2 7 ⊕ SS-3 9 ⊕ SS-4 3 ⊕ SS-5 2 ⊕ SS-6 1 ⊕ SS-7 1 = **249**`. **The subtraction closes at ZERO.**

`WAVE-DUTY` re-derived from §2's own headings: `55 ⊕ 330 ⊕ 23 ⊕ 919 ⊕ 1007 ⊕ 27 ⊕ 89 ⊕ 16 = **2466**` ✓.

### §2.2 The three totals, parsed mechanically rather than read

⟨cmd⟩ this seat, `python3`, over the frozen canonical — §1 headers, §2 bullets and §3's table parsed independently and compared:

| operand | derivation | value |
|---|---|---|
| **§1** | 66 `### fr-* — … **N rows**` headers, summed | **4242** |
| **§1** | 4242 `\| \`id\` \| aliases \| routing \| home \|` table rows, counted one at a time | **4242** |
| **§2** | 23 roster headings' declared totals, summed | **4242** |
| **§2** | 4242 `(record, id)` entries extracted from the bullets, struck spans removed | **4242** |
| **§3** | the TOTAL row | **4242** |

**§1 = §2 = §3 = 4242.** Duplicate `(record, id)` pairs: **0 in §1 · 0 in §2.**

### §2.3 The canonical-row paste sweep — the real escape test, run at the id

⟨cmd⟩ this seat, `python3`: both `(record, id) → home` maps built independently and diffed in both directions.

| test | result |
|---|---|
| ids in §1 absent from §2 (**ESCAPES**) | **0** |
| ids in §2 absent from §1 (**PHANTOMS**) | **0** |
| home disagreements between §1 and §2 | **0** |
| per-record §1 total vs §2 aggregation, all 66 | **0 divergences** |

⊙ **`rosterEscapes = 0`, proved at the id and not at the total.** A partition can close on totals while losing an id; this closes on the set.

### §2.4 The band arithmetic, independently reproduced

Four §2 bullets carry band spellings with their multiplicity **outside** the backtick span (the `E6-2` cure, verified landed): `FM-4..FM-16` <sub>13</sub> · `FM-17..FM-19` <sub>3</sub> · `K-1..K-16` <sub>16</sub> · `K-1..K-17` <sub>17</sub>. Extra ids over the four band rows = `12 ⊕ 2 ⊕ 15 ⊕ 16 = **45**`, and **`4242 ⊕ 45 = 4287`** — §3's `ids after band expansion` **reproduces exactly**, from a derivation §3 does not publish.

### §2.5 Spot-reads — **eleven dispositions plus three negative controls**, both ends

Head end (`fr-AdminAuditLog` → `fr-App`) and tail end (`fr-SpeedSelect` → `fr-VisualizationView`), each read at the canonical **and** at the frozen record:

| id | canonical §1 home | record bytes | verdict |
|---|---|---|---|
| `fr-AdminAuditLog AA-1` | **F.W4** legs F.W1 | `:35` ADJUDICATED BLOCKER, routes `F.W4`·`F.W1` | ✓ |
| `fr-AdminAuditLog AA-44` | **F.W9** leg `F.W9/W10` | `:87` *"→ **F.W9/W10**"* | ✓ |
| `fr-AdminFlaggedPanel FR-AFP-54` | **F.W0** | `:114` *"**F.W0** manifest banding"* | ✓ |
| `fr-AnimationControls M-15` | **F.W0** leg F.W1 | `:69` build-gate RED row | ✓ |
| `fr-App MG-θ` | **F.W0** | `:91` *"MG-θ's F.W0 substrate"* | ✓ |
| `fr-SpeedSelect SS-C-9` | **F.W3** | `:71` *"→ intake **R3-7a** \| F.W3"* | ✓ |
| `fr-SvgFilters C-m4` | **F.W3** <sub>criterion §5.e</sub> | `:63` *"→ **F.W3/W4**"* | ✓ |
| `fr-Tooltip FR-TT-24` | **F.W3** leg F.W1 | `:58` INFO, missed-find 5 | ✓ |
| `fr-UserSlugBar FR-USB-26` | **F.W3** | `:66` *"→ **F.W3** (structure/shadow sweep)"* | ✓ |
| `fr-VisualizationView M9` | **F.W3** <sub>criterion §5.e</sub> | `:117` r1-carried M1–M10 | ✓ |
| `fr-GalleryCardModal K-2` | **UNROUTED** <sub>errata R6 E6-3</sub> | killed-claims row, arrow names no wave | ✓ |

**Negative controls — the three ids the errata STRUCK are ABSENT from §1, as they must be:** `fr-CanvasOverlayButton C-3` (E5-3) · `fr-NotationPills K-2` (E6-1) · `fr-PathPreview C-10` (E6-4). **Three for three.** The strikes landed; they did not merely change a total.

**AXIS 1 — HOLDS.**

---

## §3 — THE PASS-13 REGISTER, CLOSED AT THE BYTES

### §3.1 `D13-1` / `F-W5:347` — **CURED. VERIFIED AT THE BYTES, NOT AT THE SEAL.**

⟨cmd⟩ `/usr/bin/awk 'NR==347' waves/F-W5.md` returns, at the settled bytes:

> `… the canonical homes it **F.W9** (leg `F.W9/W10`) and lists it in the F.W9 roster of 16 ⟨*chased at repair round 13, 2026-08-30: read 17; re-based on `CENSUS-CANONICAL.md` §0.5 errata **E5-16** — `R2-7` → TERMINAL*⟩ …`

Operand re-derived four ways by this seat: canonical `:5126` = `### F.W9 — **16 rows**` · `:148` = erratum `E5-16` (*"F.W9 −1 (17 → 16)"*) · `:289` = `AA-44` homed **F.W9** · `:5128` = the F.W9 roster lists it. ⟨cmd⟩ `/usr/bin/grep -rn 'roster of 17' waves/` → **∅ (exit 1)**. **The cure holds and returns under neither of this seat's two comparators.**

### §3.2 The three `PASS-13/SEAL.md` §5 landed-wrong findings — **all three PERSIST** (the certificate's hash is unmoved), all three re-verified, **none blocks**

- **`LW-13-1` (LOW) reproduces and is sharper than filed.** `PIN-PURGE-CERT.md:707` publishes ⟨cmd⟩ `grep -rn '289 rows' waves/ conformance/` → *"the line itself and the canonical's E6-5 row, and nothing else."* Re-run at the settled bytes: **eight lines** — `waves/F-W0.md:598` · `waves/F-W3.md:780` · `CENSUS-CANONICAL.md:182` · `PIN-PURGE-CERT.md:707` · and `PASS-13/SEAL.md` `:120`/`:132`/`:134`/`:143`. Every unaccounted match is a self-match of the receipt's own quoted pattern; `F-W0:598` carries a declared-first self-exclusion clause and is lawful; `:707` carries none. **The substantive claim is TRUE and unmoved.** Carried, not re-filed as new.
- **`LW-13-2` (INFO)** — the *"108 → 108 (distinct 101 → 101)"* figures publish no instrument and reproduce under none this seat built. Figure-only; the invariance the figures were cited for does reproduce.
- **`LW-13-3` (INFO)** — `F-W0:598`'s *"48 · 41 · 7"* against `PIN-PURGE-CERT.md:705`'s *"49 · 42 · 7"* for the same instrument in the same round. **The adjudicated outcome is identical in every run including this seat's: six acquit, ONE convicts, and the one is `F-W3:780`.** Scope-disclosure, not arithmetic.

**AXIS 2 — HOLDS.** The register is closed; the one live item is the class the certificate itself declined to certify.

---

## §4 — THIS SEAT'S OWN INSTRUMENTS, BUILT FROM THE FROZEN CANONICAL FIRST

**Declared method.** The authority list was derived **before any spec was opened**: LIVE = the canonical's own §1 record headers ⊕ §2 roster headings ⊕ §3's table (68 distinct values). SUPERSEDED = extracted **mechanically** from the canonical's own `read **N**` / `N → N` / *"figures this table carried immediately before"* prose. `SUPERSEDED-ONLY = SUPERSEDED − LIVE`, so a magnitude that is live anywhere is never convicted on a value collision:

> `17 · 25 · 28 · 29 · 33 · 56 · 57 · 63 · 87 · 90 · 97 · 119 · 135 · 138 · 152 · 252 · 283 · 289 · 358 · 362 · 776 · 862 · 926 · 928 · 1012 · 1014 · 1190 · 2515 · 2767 · 2768 · 4262 · 4269 · 4278 · 4322 · 4329 · 4338`

### §4.1 Instrument A — census-context binding over all eleven specs

**54 hits · 39 carrying an on-line chase · 15 read whole.** ⊘ **The first cut of this instrument returned 16, and the sixteenth was a FALSE ACQUITTAL of this seat's own making** — the chase detector matched the token `R3-7a` on `F-W3:780` under a loose `R\d+-\d` rule. **The detector was tightened to explicit supersession vocabulary only and re-run**, which is how `F-W3:780` entered the read-whole bucket. Disclosed because a successor building the same instrument will hit the same trap.

The fifteen, each adjudicated at the bytes:

| site | token | adjudication |
|---|---|---|
| `F-W0:114` | `28` | **ACQUITS** — 28 dirty **tree** paths (`27 M ⊕ 1 ??`), enumerated item-for-item; a tree count, no census magnitude |
| `F-W0:581` | `1014` | **ACQUITS** — the dated R11-3 repair minute that convicts `F-W7:291`; lawful E-3 |
| `F-W2:16` | `25` | **ACQUITS** — a dated round-3 detector receipt; **and it reproduces LIVE**: `F.W2` → 68 occ / 57 lines / 25 records |
| `F-W2:346` | `29` | **ACQUITS** — byte-true quotation of `fr-PathPreview.md:114`, re-run whole and reproducing character for character |
| `F-W2:436` | `29` | **ACQUITS** — self-derived on its own next line (`A1–A7 ∥ B1–B13 ∥ C1–C2 ∥ D1–D4 ∥ E1–E2 ∥ F1` = **29**) and self-declared *"index, no gate weight … has never been a census"* |
| `F-W3:648` | `57` | **ACQUITS** — a detector-configuration record count inside a fenced spread table; value collision |
| `F-W3:649` | `63` | **ACQUITS** — same table, same class |
| **`F-W3:780`** | **`289`** | ▲ **CONVICTS. §5.** |
| `F-W4:299` | `29` | **ACQUITS** — the wave's own anti-rename crosswalk count, not a canonical magnitude |
| `F-W7:261` | `17` | **ACQUITS** — self-deriving in the same parenthetical (`15 declared edges ⊕ 1 ⊕ 1 = 17`) |
| `F-W8:163` | `28` | **ACQUITS** — the wave's own carry-table heading; re-counted at `:167`–`:221`: **38 pipe-lines − 5 headers − 5 separators = 28** ✓ |
| `F-W9:173` | `25` | **ACQUITS** — a live corpus receipt; **reproduces** (`F.W9/W10` → 54 / 25 records / 66 files) |
| `F-W9:305` | `28` | **ACQUITS** — a `git status --porcelain` tree count |
| `F-W10:53` | `25` | **ACQUITS** — the same live receipt, same reproduction |
| `F-W10:189` | `56` | **ACQUITS** — live: `NO-WAVE-OWNER` → **167 occ / 165 lines / 56 records / 66 files**, all four reproduce |

### §4.2 Instrument B — the wave-magnitude comparator, **six** denominated forms

Built on six spellings (`F.W<k> (N rows)` · `F.W<k> … roster is/of N` · `N-row F.W<k>` · `the N F.W<k> rows` · `F.W<k>'s roster/register/population … N rows` · `F.W<k> — N rows`) and compared against the canonical's own §2 headings.

**43 pairs captured · 38 match · 5 mismatch, ALL FIVE CHASED · ZERO UNCHASED MISMATCHES.** The chased five, each read whole:

| site | spec | canonical | the chase |
|---|---|---|---|
| `F-W0:225` | 57 | 55 | *"⟨chased at round-7 reconcile … read 57; re-based on §0.5 errata **E6-3**⟩"*, and the live cell reads `### F.W0 — 55 rows` |
| `F-W6:476` | 28 | 27 | *"⟨re-based at repair round 8 … a heading the canonical does not carry — `grep -c` → **0**⟩"* |
| `F-W7:291` | 1014 | 1007 | *"⟨**chased at the R11 numeric reconciliation, 2026-08-30** … at the frozen canonical that command returns ZERO⟩"*; the live receipt reads `1007` |
| `F-W8:20` | 28 / 90 | 27 / 89 | a **byte-true paste of the canonical's own §2 headings**, errata parentheticals included — ⟨cmd⟩ `/usr/bin/grep -nE '^### F\.W5(-W8)? — '` reproduces `5078:` and `5092:` **exactly** |
| `F-W9:131` | — | 16 | an explicit `E5-16` STRUCK row |

⊙ **`F-W5:347` captures under form 1 as `F.W9 = 16` and MATCHES.** Both instruments confirm the round-13 cure independently.

### §4.3 Self-count probe — **zero unlawful live members**

`R3-3.10` forbids banking a ⟨cmd⟩ whose output is a count of a live sibling or of the citing file. Every self-referential count in the corpus is either declared an **instrument, not an operand** (`F-W10:309`), stated as **set-membership** (`F-W8:36`), or falls under `F-W5:269`'s express carve-out (*"A count of what a spec wrote is not a census"*). The carve-out's arithmetic was re-derived rather than trusted:

| receipt | published | this seat | verdict |
|---|---|---|---|
| `grep -cE '^\| \*\*[A-G][0-9]+c?\*\*' F-W5.md` | **93** | **93** | ✓ |
| §3 gate rows `G1`–`G22` | **22** | **22** (of 32 `G`-prefixed) | ✓ |
| clause rows `93 − 22` | **71** | **71** = 61 `[A-F]` ⊕ 10 `G1c`–`G10c` | ✓ |
| the §-decomposition | 6·5·5·16·20·9·10 | **sums to 71** | ✓ |
| *"no clause is numbered D9"* | 16 D-clauses | `D9` absent, **16** | ✓ |
| `grep -Ec '^\| \*\*P-[0-9]+\*\* \|' F-W9.md` | **9** | **9** | ✓ |
| the same, `OUTSTANDING` | **8** | **8** | ✓ |
| `F-W7`'s §5 record denominator | **15** | **15** | ✓ |
| `grep -n 'raw-findings' waves/F-W8.md` | sole occurrence | **1** | ✓ |
| line-cites into live siblings, `F-W2` | **0** | **0** | ✓ |

⊙ **`F-W8:29`'s own portability self-probe reproduces**: ⟨cmd⟩ `/usr/bin/grep -nE -- '-P |\\K|\.\{[0-9]*,[0-9]*\}' waves/F-W8.md` → **exactly one line, line 29 itself.**

### §4.4 A probe no prior pass ran — **the digest-pin reality sweep**

**185** twelve-hex tokens across the eleven specs; **66** match a live corpus file; the remaining **119** were tested against a chase detector, leaving **21** non-live-and-unchased. **Every one of the 21 pins a file OUTSIDE the corpus**, and this seat measured all of them:

| pinned operand | spec pin | measured | verdict |
|---|---|---|---|
| `carry/F-W1-CARRY.md` | `0d0b091e86ca` | `0d0b091e86ca` | ✓ **EXACT** |
| `carry/F-W4-CARRY.md` | `bf1da6fe1e7e` | `bf1da6fe1e7e` | ✓ **EXACT** |
| `X/waves/W9.md` | `820bd500999f` | `820bd500999f` | ✓ **EXACT** |
| `$M/lane-docs.md` | `1a5caf437d54` | `1a5caf437d54` | ✓ **EXACT** |
| `$M/lane-frontend.md` | `4433d809fac7` | `4433d809fac7` | ✓ **EXACT** |
| `$M/CENSUS-2026-08-03.md` | `684de3d8a8a0` | `684de3d8a8a0` | ✓ **EXACT** |
| `../COHESION.md` (per `F-W6:54`) | `956e71a83e32` | `956e71a83e32` | ✓ **EXACT** |
| **`../COHESION.md` (per `F-W7:58`)** | **`91c6d974db9b`**, cell reads **"no"** | `956e71a83e32` | ▲ **CONVICTS — LOW. §6.** |

⊙ **The corpus's external-operand pinning is otherwise perfect: seven of eight pins reproduce byte-exact against files no prior pass re-measured.** The eighth is filed at §6 and its blast radius is measured, not assumed.

**AXIS 3 — HOLDS, with the two convictions carried to §5 and §6.**

---

## §5 — THE CONVICTION. **`F-W3:780`. MEDIUM. CARRIED, NOT NEW — AND IT BLOCKS.**

### The bytes

⟨cmd⟩ `/usr/bin/awk 'NR==780' waves/F-W3.md`, the operative clause:

> *"Five are the criterion's negatives (canonical §4.2 — their target files are in no `.a`–`.e` list, and the five carry **289** rows between them)"*

### The operand contradicts it, and names the very figure as an error

⟨cmd⟩ `/usr/bin/awk 'NR==182' conformance/CENSUS-CANONICAL.md` → erratum **`E6-5`**, ruling in terms: *"§4.2 published … carry **289 rows** between them … Summing those five records' own §1 headers gives **283** before this round … and **282** after E6-1 … a **six-row overstatement** … Corrected to **282** with both readings disclosed."*

### Re-summed independently, from the five records' own §1 headers at the frozen canonical

⟨cmd⟩ `/usr/bin/awk 'NR==768||NR==1265||NR==2133||NR==2462||NR==2753' conformance/CENSUS-CANONICAL.md | /usr/bin/grep -oE '\*\*[0-9]+ rows\*\*'` → `**79 rows**` · `**73 rows**` · `**46 rows**` · `**32 rows**` · `**52 rows**`. **`79 ⊕ 73 ⊕ 46 ⊕ 32 ⊕ 52 = 282`.**

### Not one chase token

⟨cmd⟩ `/usr/bin/awk 'NR==780' waves/F-W3.md | /usr/bin/grep -oE 'errata|chased|superseded|E6-5|282|repair round|~~'` → **∅ (exit 1).**

### Why MEDIUM and not MINOR — stated so the grade is auditable

The charge instructs that a MINOR with a stated mitigation does not block and that materiality must not be manufactured. It equally instructs that a real finding must not be softened to reach the bar. **The programme graded this exact class MEDIUM eight hours ago.** `§ERRATA-R13·b` books `D13-1` — *"a superseded canonical magnitude in the present indicative, attributed expressly to the canonical, unchased"* — as **MEDIUM**, and spent a repair round curing it. `F-W3:780` is the same class, the same shape, the same attribution and the same absence of a chase; it differs only in which file it lives in. **A defect does not change grade because the round that found it could not write the file it lives in.** It is graded MEDIUM, and under a bar that requires a MINOR-or-below tail, it blocks.

### The mitigations, stated in full and weighed honestly

1. **It is RECORDED in two instruments of record**, not latent: `PIN-PURGE-CERT.md §ERRATA-R13·c` and `PASS-13/SEAL.md` §4. The certificate expressly refused to certify the class closed.
2. **Its function is unimpaired.** The sentence's job — enumerating the seven records that book nothing at F.W3 so their absence is a fact and not a gap — is correct, and this seat verified it: 66 records, 59 in the register, the seven named. Only the decorative magnitude is stale.
3. **No gate reads it.** It is prose inside §4a, not a roster row, not a gate operand, not a denominator.
4. **The cure is one numeral plus a chase**, in an idiom the corpus now carries four times.

⊘ **These mitigate the severity of the consequence; they do not convert a MEDIUM into a MINOR.** The bar as written is not met, and this seat will not restate the bar to fit the finding.

---

## §6 — A SECOND, GENUINELY NEW FINDING. **LOW. IT DOES NOT BLOCK.**

### `F-W7:58` — a stale external pin under a **"moved mid-round? no"** cell that is FALSE

`F-W7`'s quote-time pin table publishes:

```
| `../COHESION.md` | `91c6d974db9b` | no |
```

- **The file moved.** ⟨cmd⟩ `shasum -a 256 docs/tranches/X/COHESION.md` → **`956e71a83e32`**.
- **The corpus already knows.** `F-W6:54`, the sibling spec, same file, same `../COHESION.md` spelling: `` ~~`91c6d974db9b`~~ → **`956e71a83e32`** ⟨moved; re-measured this seat⟩ ``.
- **`F-W7`'s own minute names this exact class and stops one short.** `:60` publishes ⌧ *"FIVE STALE, **ONE "no" NOW FALSE**"* and cures `KF-W1.md`'s identical cell. **Two "no"s are false, not one** — `COHESION.md`'s is the second, and it is the one row in the table that is neither a wave sibling nor the census, which is exactly why the round-5 sweep passed over it.

### The blast radius, MEASURED and not assumed — **this is why it is LOW**

`F-W7` carries four live receipts against `COHESION.md`. **All three quotable ones were re-run by this seat against the moved file, and all three reproduce byte-for-byte:**

| # | ⟨cmd⟩ against the LIVE `COHESION.md` | result |
|---|---|---|
| 1 | `grep -o 'Cross-repo edges are declared FROM BOTH ENDS in the spec files (the X·V W4-D2 lesson, now law)'` (G-F7-11) | **reproduces exactly, exit 0** |
| 2 | `grep -A1 'the value-side atomdiff restoration'` (§2 TA-4, `:207`) | **reproduces, both lines, the wrap included** |
| 3 | `grep -A1 'Every census wave-sketch id'` (§3 item 2, `:245`) | **reproduces, both lines** |

⊘ **Nothing substantive rests on the stale pin.** The digest sits under a column header that scopes it by its own words to *"sha256-**at-quote-time**"*, which places the digest itself inside the licensed E-3 class. **The defect is the third column's affirmative present-tense `no`**, and it is falsified by a sibling spec inside the same corpus — so the corpus is self-correcting on the point and a reader who consults both ends is never misled.

**Grade LOW. Mitigations stated: (a) `F-W6:54` carries the correct re-measured value in the same corpus; (b) all three dependent quotations re-run GREEN at the moved bytes this pass; (c) the digest column is quote-time-scoped by its own header; (d) `F-W7:60` supplies the cure idiom verbatim. It does not block.**

⊙ **Rider, INFO:** `F-W7:60`'s count-word *"ONE 'no' NOW FALSE"* is short by one against its own table. Enumeration-only; no gate reads it; cured by the same one-row edit.

---

## §7 — GATES AND POSTURE. **ALL FOURTEEN HOLD.**

| gate | probe | verdict |
|---|---|---|
| **canonical operands only** | every wave magnitude in every spec traced to `CENSUS-CANONICAL.md` §2/§3; the detector-era figures at `F-W2:16`/`:34` sit under a declared supersession clause | ✓ |
| **portable commands** | 23 `-P`/lookaround/`\K` tokens swept; **every one** sits inside an `R4-2` re-cut narration, a `§Y.3` residual declaration, a declared Python-regex form, or a self-declaring probe. `F-W8:29`'s probe returns **exactly itself** | ✓ |
| **no false universals in current voice** | five live universals tested at the bytes: `NO-WAVE-OWNER (producer emitter)` in `F-W0` → **∅** · `burn-down needs` → **`F-W6` only, 3 lines** · line-numbers into `F-W10` from `F-W7` → **0** · `F-W5` §12 → **0** · `RED BY CONSTRUCTION at the installed` across all 66 records → **∅, exit 1** | ✓ |
| **count-words match lists** | 12 count-word ⊕ enumerated-list pairs captured; 4 flagged, **all four acquit at the bytes** (`F-W6:69`'s fourth member is prose-spelled; `F-W10:406`'s *"three files across the two trees"* binds correctly to 3 paths under 2 roots; two are regex mis-bindings) | ✓ |
| **line-cites into live siblings** | **61** across 8 specs; ⟨cmd⟩ `python3` — **0** sit on a line without a retirement/errata/narration marker. `F-W0:37`'s declared universal (*"every surviving occurrence … is a masthead minute naming the address AS RETIRED"*) **holds by enumeration** | ✓ |
| **F.W1 transaction whole** | the atomic tri-package uplift is spelled whole at `F-W0` `:73`/`:160`/`:177`/`:197`/`:437`; never split, never partially named | ✓ |
| **W7 zero-roster** | ⟨cmd⟩ over 66 records: `F.W7` → **2**, both inside `KF.W7`; the ∅ posture is the corpus's own reading | ✓ |
| **W6 / W8 / W10 zero-roster** | `F.W6` → **0** · `F.W8` → **0** · bare `F.W10` → **0** (against 54 `F.W9/W10`). `F-W6:526` and `F-W10:53`/`:441` state the posture in the canonical's own terms | ✓ |
| **FR-NP-32 canonical form** | every occurrence carries the pair `FR-NP-32 (≡ fr-PaperSidebar M1)`; **cite-both-never-substitute holds corpus-wide** | ✓ |
| **SS-4 flags inline** | 288 `SS-4` references, each flagged at its site (prerequisite rows, D-8 minutes, purge-seat notes); none deferred to a footnote | ✓ |
| **fourier tree READ-ONLY** | `/Users/mkbabb/Programming/fourier-analysis/` never opened; `git status --porcelain docs/tranches/X/fourier/` identical before and after | ✓ |
| **E-3** | dated evidence nowhere rewritten; the six licensed history classes re-verified in place and **not re-filed** | ✓ |
| **zero VERIFIED-YES** | ⟨cmd⟩ `/usr/bin/grep -rn 'VERIFIED-YES' waves/` → **∅ (exit 1)** | ✓ |
| **status planned** | 16 `Status:` declarations across the eleven, **every one `planned`**; no `executing`, no `complete`, no `closed` | ✓ |
| **no product source opened** | this seat read the eleven specs, the canonical, the certificate, `PASS-13/SEAL.md`, `COHESION.md`, and frozen `fr-*.md` records. `src/`, `demo/`, `api/` and the fourier product tree were never opened; the four external operands were **hashed, not read** | ✓ |

⊘ **The licensed E-3 history was re-verified and deliberately NOT re-filed:** `F-W8:40`'s `28 ⊕ 89 = 117` · `F-W10:363`'s R4-10 byte-true quotation · quote-time digests in dated errata rows with chased sub-notes · strike-quoted retired spellings under the self-excluding probe · `F-W5:243`'s shader-provenance `4242` · `F-W0:588` under `:541`'s declared-first scope clause.

**AXIS 4 — HOLDS.**

---

## §8 — RECEIPT REALITY. **56 RECEIPTS RE-RUN. `fabricated = 0`.**

Every published figure this seat re-ran reproduced at the settled bytes. Selected, with the round-13 cure's own receipts included as ordered:

| # | ⟨cmd⟩ | published | measured |
|---|---|---|---|
| 1–13 | the thirteen-row `shasum -a 256` block | `PASS-13/SEAL.md` §8 | **13/13 identical** |
| 14 | `grep -o 'F\.W9/W10' fr-*.md \| wc -l` | 54 | **54** |
| 15 | `grep -l 'F\.W9/W10' fr-*.md \| wc -l` | 25 | **25** |
| 16 | `ls fr-*.md \| wc -l` | 66 | **66** |
| 17 | `grep -o 'NO-WAVE-OWNER' fr-*.md \| wc -l` | 167 | **167** |
| 18 | the same, lines | 165 | **165** |
| 19 | the same, records | 56 | **56** |
| 20 | bare `F.W10` routing | 0 | **0** |
| 21 | `grep -o 'F\.W7' fr-*.md \| wc -l` | 2 | **2**, both `KF.W7` |
| 22 | `F.W6` / `F.W8` / `F.W10` in records | ∅ | **0 · 0 · 0** |
| 23 | `F.W2` occurrences / lines / records | 68 / 57 / 25 | **68 / 57 / 25** |
| 24 | `F.W5-W8` hyphen arm | 120 | **120** |
| 25 | `F.W5–W8` en-dash arm | 72 | **72** |
| 26 | union records | 49 | **49** |
| 27 | `grep -row 'F\.W5' \| wc -l` | 245 | **245** |
| 28 | `grep -nE '^### F\.W5(-W8)? — '` | `5078` = 27 · `5092` = 89 | **identical, 27 ⊕ 89 = 116** |
| 29 | `### F.W0 — **55 rows**` at `:4841` | 55 | **55** |
| 30 | `### F.W9 — **16 rows**` at `:5126` | 16 | **16** |
| 31 | `:148` erratum `E5-16` | `F.W9 −1 (17 → 16)` | **identical** |
| 32 | `:289` / `:5128` — `AA-44` home + roster | F.W9 | **both resolve** |
| 33 | `sed -n '87p' fr-AdminAuditLog.md` | the `:347` quotation | **byte-true, head and tail** |
| 34 | `grep -rn 'roster of 17' waves/` | ∅ | **∅ (exit 1)** |
| 35 | the three-digest stale-pin sweep | ∅ | **∅ (exit 1)** |
| 36 | `sed -n '114p' fr-PathPreview.md` | *"29 rows routed (F.W0 ×4 · F.W1/W2 ×2 · F.W3/W4 ×15 …)"* | **byte-true with the elision** |
| 37 | `sed -n '107p' fr-PathPreview.md` | the `F-W2:346` head quotation | **byte-true** |
| 38 | `F-W8:163` carry table, `38 − 5 − 5` | 28 | **28** |
| 39 | `F-W2:436` §3 ledger labels | 29 | **29** (`7+13+2+4+2+1`) |
| 40 | the five criterion-negatives' §1 headers | 282 | **79⊕73⊕46⊕32⊕52 = 282** |
| 41 | `grep -cE '^\| \*\*[A-G][0-9]+c?\*\*' F-W5.md` | 93 | **93** |
| 42 | the `22 ⊕ 71` decomposition | 93 | **93** |
| 43 | `grep -Ec '^\| \*\*P-[0-9]+\*\* \|' F-W9.md` | 9 | **9** |
| 44 | the same, `OUTSTANDING` | 8 | **8** |
| 45 | `F-W7` §5 record denominator | 15 | **15** |
| 46 | `grep -c 'raw-findings' F-W8.md` | 1 | **1** |
| 47 | `F-W8:29`'s portability self-probe | exactly itself | **1 line, line 29** |
| 48 | `ls carry/` | two entries | `F-W1-CARRY.md` · `F-W4-CARRY.md` |
| 49 | `### GLASS-RELAY — **22 rows**` | 22 | **22** |
| 50 | the G-F7-11 `COHESION.md` sentence | verbatim | **reproduces, exit 0** |
| 51 | `grep -A1 'the value-side atomdiff restoration'` | the wrapped pair | **reproduces** |
| 52 | `grep -A1 'Every census wave-sketch id'` | the wrapped item | **reproduces** |
| 53 | the seven external-operand digests | seven pins | **7/7 EXACT** |
| 54 | `grep -rn 'VERIFIED-YES' waves/` | ∅ | **∅ (exit 1)** |
| 55 | `sed -n 'Np' fr-*.md` coordinates | 130 distinct | **130 LIVE, 0 DEAD** |
| 56 | §1 / §2 / §3 totals | 4242 | **4242 = 4242 = 4242** |

⊙ **COORDINATES — 130 re-run, ZERO dead.** Every `sed -n 'Np' fr-X.md` receipt in all eleven specs was extracted mechanically and resolved against the frozen record: **every file exists, every line exists, no line is empty.** Far past the ≥8 ordered.

⊙ **`fabricated = 0`.** The only figures that did not reproduce belong to **instruments** — `PIN-PURGE-CERT.md:707`'s sweep enumeration (`LW-13-1`, carried) and `PASS-13/SEAL.md` §5's two INFO figures. **Not one figure in the corpus of eleven specs failed to reproduce**, save the two this check convicts as stale rather than fabricated: `F-W3:780`'s `289` and `F-W7:58`'s `91c6d974db9b`, both of which name a real prior state of a real operand.

⊙ **One INFO against the seal, recorded not filed:** `PASS-13/SEAL.md` §2.1 renders `F-W5:347`'s cured numeral as `**16**` where the bytes carry an unbolded `16`. A bold-emphasis addition inside a quoted span, in a file that declares itself an operand for no gate. Named so a successor does not re-derive it.

**AXIS 5 — HOLDS.**

---

## §9 — THE REGISTER

| # | severity | finding | blocks? |
|---|---|---|---|
| **D14-1** | **MEDIUM** | `F-W3:780` publishes *"the five carry **289** rows between them"* attributed expressly to canonical §4.2, in the present indicative, with **zero chase tokens**. §4.2 reads **282**; erratum `E6-5` rules 289 a six-row overstatement; independently re-summed `79⊕73⊕46⊕32⊕52 = 282`. Same class and shape as pass 13's `D13-1`, graded MEDIUM then and MEDIUM now. **Carried** — recorded at `§ERRATA-R13·c` and `PASS-13/SEAL.md` §4, deliberately uncured because `F-W3` was not writable. | **YES** |
| **D14-2** | **LOW** | `F-W7:58` pins `../COHESION.md` at `91c6d974db9b` under a **"moved mid-round? no"** cell; the live digest is `956e71a83e32` and `F-W6:54` records the move. Mitigated: the column is quote-time-scoped by its own header, the sibling carries the truth, and **all three dependent quotations re-run GREEN**. | no |
| **D14-3** | **LOW** | `PIN-PURGE-CERT.md:707`'s `'289 rows'` sweep enumeration is falsified at the settled bytes (returns 8 lines, claims 2). **Carried** from `LW-13-1`; substantive claim TRUE; instrument only. | no |
| **D14-4** | **INFO** | `F-W7:60`'s *"ONE 'no' NOW FALSE"* is short by one against its own table (D14-2 is the second). Enumeration-only. | no |
| **D14-5** | **INFO** | `LW-13-2` — the *"108 → 108 / 101 → 101"* figures publish no instrument and reproduce under none. Carried; the invariance they were cited for does reproduce. | no |
| **D14-6** | **INFO** | `LW-13-3` — `48/41/7` against `49/42/7` for one instrument in one round. Carried; **the adjudicated outcome is identical in all three runs including this seat's**. | no |
| **D14-7** | **INFO** | `PASS-13/SEAL.md` §2.1 adds bold emphasis inside a quoted span (`**16**` for an unbolded `16`). Instrument, not corpus; operand for no gate. | no |

**Zero BLOCKER · zero CRITICAL · zero HIGH.**

---

## §10 — THE BAR, CHECKED CLAUSE BY CLAUSE

| clause | state |
|---|---|
| zero BLOCKER / CRITICAL / HIGH | ✓ **MET** |
| hashes clean | ✓ **MET** — 13/13 byte-identical; canonical FROZEN sixth round |
| roster closed | ✓ **MET** — §1=§2=§3=4242; escapes 0, phantoms 0, home-mismatches 0, duplicates 0; band expansion 4287 reproduced |
| fabrication zero | ✓ **MET** — 56 receipts, `fabricated = 0` |
| **tail MINOR-or-below with stated mitigations** | ✗ **NOT MET** — one **MEDIUM** (`D14-1`) |

**Four of five. `verdict = NOT CONFORMANT`.**

---

## §11 — WHAT ROUND 14 OWES, AND THE PREDICTION WITH ITS FALSIFIER NAMED

1. **`waves/F-W3.md:780`** — one numeral `289 → 282` plus a chase in the corpus's established idiom (`⟨*chased at repair round 14, 2026-08-30: read 289; re-based on `CENSUS-CANONICAL.md` §0.5 erratum **E6-5** — a six-row overstatement inherited from a pre-errata-round-5 sum*⟩`). **This alone converts the verdict.**
2. **`waves/F-W7.md:58`** — re-pin `../COHESION.md` to `956e71a83e32` and flip the `no`; amend `:60`'s count-word to TWO. One row, one word.
3. **`PIN-PURGE-CERT.md:707`** — one self-exclusion clause on the sweep enumeration, in the form `F-W0:598` already carries.

⊘ **The prediction, and it is falsifiable by construction.** Pass 13 wrote *"the next hostile pass should find nothing"* and was falsified within the round. This seat states the narrower claim its own instruments support: **the two comparator classes are now measured to ONE and ONE member respectively, both named with their file, line and cure; a round that lands the three edits above closes both classes, and the fifteenth pass's job is to re-run this file's §4 instruments against the moved bytes and say whether they return empty.** If they do not, this seat was wrong in the same way pass 13 was, and the instrument specifications at §4 are published in full precisely so a successor can convict this file rather than inherit it.

⊘ **What this seat did NOT do:** it wrote no file in `waves/`, `conformance/` (other than this one), `carry/` or the registry; it opened no product source; it re-filed no licensed E-3 history; it did not soften `D14-1` to reach a verdict, and it did not manufacture materiality from the six INFO/LOW dispositions to avoid one.

---

## §12 — CLOSING HASH. **TAKEN AS THE ABSOLUTE LAST ACT OF THIS SEAT.**

⟨cmd⟩ this seat, 2026-08-30, base = repo root, pinned BSD toolchain, taken after every probe had settled:

```
3d62e5f4e78b99d5f3552a8b4d1ed377981854ca47db80b8471b8f4690651645  waves/F-W0.md
ab2437af09e0d0646260afdd024541539ad588d492b8de48aee25a8f82adff3e  waves/F-W1.md
8ae0ad8fa623de2201a6184ec32dfcb2a6bd25e2443b69ad4377e24746eeddc9  waves/F-W10.md
0b967619bb35763b6f1b09529db080fa2f9a37db2fa3e5e1eac70ce2aef7b7a6  waves/F-W2.md
be86ae4101184d8d7278879f5ab5a8abc9102fbcfdfb554d0523affde576bfcf  waves/F-W3.md
2fb6e6d637bb6d0fad710716fb1dfa103d39fc41f67d8ff0fb856e4c0126cdb6  waves/F-W4.md
26aebcdc7bacb6bc85e31b3bf1205097bacb4311855ade0fe1c581784ff2ad85  waves/F-W5.md
e83dcad051f560329ade30dbb0877732650ac05071e8b2ee0eab22f56abf7e11  waves/F-W6.md
ba9be3e376139204a17ad7b5ebc858513bc3880e26f63f7340ebce14145547d7  waves/F-W7.md
d8237cf6b11ef248e4080b8d805a2ee7477139b8d4cf9cf3564922096e29bd45  waves/F-W8.md
b265bf677869b0ec6f3c02c54b93272f657111e55498cf05bef14578d6c4b53b  waves/F-W9.md
f443627574581ec2a4138e46d927b5fc431a7a6657cae766f7ffafa01e770968  conformance/CENSUS-CANONICAL.md
a3861e158e4983976b6b9910395a507fc85b62c5671dd37e0b0cb97a195d18f8  conformance/PIN-PURGE-CERT.md
```

**Thirteen rows, unmoved from `PASS-13/SEAL.md` §8's thirteen. The canonical stands at `f44362757458` — FROZEN for a SIXTH consecutive round. Every file this check adjudicated is pinned here, so a successor can prove whether it read the same corpus.**
