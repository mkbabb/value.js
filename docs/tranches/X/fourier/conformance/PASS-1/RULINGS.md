# X·F REPAIR ROUND 1 — CROSS-WAVE RULINGS

**Seat**: cross-wave rulings, 2026-08-28. **Inputs**: `UNION.md` (whole) + the 11 check files beside it + `waves/F-W0..F-W10.md` + `carry/F-W1-CARRY.md` + `carry/F-W4-CARRY.md` + the 66 `fr-*.md` at `docs/tranches/V/megatranche/registry/adjudicated/`. Every ruling below was re-verified AT THE BYTES by this seat this session (read-only; this file is the seat's only write).

**Binding force**: each ruling names ONE owning cure per cross-wave defect class. Repair seats execute these rulings verbatim; a repair seat that finds a ruling's coordinate stale re-measures and applies the ruling's LAW to the live bytes, never a different cure. Per-wave defects the checks booked outside these classes (arithmetic residue M-1, write-adjacent M-4, unrunnable-gate M-3, voice leaks L-1/L-2) stay with the per-wave seats under their check files.

**Amendments at re-measure** (two UNION/check receipts this seat REFUTED at the bytes — the rulings below carry the corrected facts):

- **A-α**: the trie-vs-KISS dissent is NOT invented. It is banked at `F-W5.md:153` (§2 clause E16's ▲ block: *"Dissent recorded (F-W10): the trie requirement collides head-on with a standing KISS guardrail present in BOTH trees — `atomdiff.py:12-14` is the INCUMBENT — and the owner ruling PRECEDES design"*) and at `F-W10.md:114` (§2.3 SS-4-PREREQ row, same substance). Only F-W7's coordinate (`F-W10.md:106`, a blank line) is false. F-W7-CHECK's receipt (*"grep 'collides head-on' → F-W7.md:115 only"*) does not reproduce: this seat's grep returns `F-W7.md:115`, `F-W5.md:153`, `F-W10.md:114`. Cure = RE-ANCHOR (R-1b), not strike.
- **A-β**: five fr-EquationView span ids the UNION's §1b cluster lists as zero-byte are PRESENT in `F-W5.md` by bytes — `M-CK` (×2), `M-SB`, `M-DV`, `C·D-02`, `L·m-6` — and `B-1`/`B-2` (EquationView) are present but homonym-unqualified. The F-W5-CHECK census was right; the union's re-measure was wrong for these seven. The genuinely zero-byte cluster members are **M-TL · D·D-M3 · M-ZM · I-3 · I-4**. R-2 homes those five; the other seven are cured by record-qualification + reciprocal citation (R-2 note, R-5).

---

## R-1 · FABRICATED / MIS-KEYED AUTHORITY (the gravest class — ruled first)

**LAW**: an authority that cannot be produced at its cited coordinate is DELETED, never paraphrased. Where the banked text exists elsewhere, the citation is replaced with the banked text VERBATIM at its true coordinate. Where it exists nowhere, the quote AND every clause that depended on it are struck; surviving substance stands only on real authorities named in the same breath.

### R-1a — F-W7's invented F-W5 trie prohibition · owner F.W7 · verified fabricated

`grep -rn "No trie design" docs/tranches/X/` → `F-W7.md:73` and `:160` only. `F-W5.md` contains no such sentence and its §3 is `## 3. Gates — 22, all born-RED` (`F-W5.md:212`), not a Prohibitions section.

**Cure (exact shape)**: at `F-W7.md:73` and `:160`, strike the quotation marks, the word "verbatim", and the attribution "(F-W5 §3 …)". Replace with:

> **No design byte absent G-F7-1's ruling** — F.W7's own prohibition, grounded in F-W5.md:153 (§2 clause E16 ⊙: *"The honest default v2 MUST carry absent a ruling: no trie; whole-snapshot duplication is the recorded shipped behaviour"*) and F-W5.md:226 (G7 ⊙, recorded UNRULED).

The prohibition survives in F-W7's own voice; it is never again presented as F-W5's sentence.

### R-1b — F-W7's dissent at blank F-W10:106 · owner F.W7 · RE-ANCHORED per A-α

**Cure**: everywhere F-W7 cites `F-W10.md:106` / "F-W10:106" (§5a R-4 row, §5c TA-4 source list at `:130`, §8), replace the coordinate with **`F-W5.md:153` (the banked home) ≡ `F-W10.md:114`**, and conform the quoted words to `F-W5.md:153`'s bytes exactly (including "`atomdiff.py:12-14` is the INCUMBENT"). The "intake CARRY" provenance tag on the dissent is struck (R-3).

### R-1c — F-W5's invented "P-8 disposition verbatim" · owner F.W5 · verified fabricated

`P-8` occurs in ZERO of the 66 records and ZERO carry lines (`grep -E '(^|[^A-Za-z0-9-])P-8([^0-9A-Za-z-]|$)'` → 0/0); "identity-less envelope" occurs ONCE in the tree — `F-W5.md:84` itself.

**Cure**: at `F-W5.md:84` clause A1, DELETE the sentence `P-8 disposition verbatim: "leaving them NO-WAVE-OWNER guarantees the new contract reproduces the same identity-less envelope that forced the positional key."` in full — no paraphrase, no re-attribution. A1 stands on its real authorities already in the cell: AA-45 (`fr-AdminAuditLog.md:88`) + AA-48 (`:91`).

### R-1d — F-W2's seat-minted G15 vs the banked drift · owner F.W2 · verified inverted

Banked: `fr-EasingPicker.md:52` — *"**14 of 22 silently drift analytic→CubicBezier-approximation** (8 stay analytic incl. ease-out-cubic/-expo)"*; `F-W1.md:227` ESC-4 — *"14/22 analytic→CubicBezier; 8/22 material, max Δ 0.192"*; `library-band.md:295` — the 3-leg probe (8/22 curves drift materially, max 0.192). F-W2's G15 states "8 of 22 change SHAPE" with a member list that names among the eight two curves the bank says STAY analytic.

**Cure**: G15 is re-written with the banked figures verbatim — **14/22 drift analytic→CubicBezier, of which 8/22 are material (max Δ 0.192); ease-out-cubic and ease-out-expo are among the 8 that STAY analytic**. The `⭑ seat-minted 2026-08-28` stamp is STRUCK; provenance re-attributed to **fr-EasingPicker L/B-1 + C/B-1 (CC-2 ratification)** and `library-band.md:295`'s probe (`fourier-value-import-drift.mjs`, 3/3 legs). F.W1's ESC-4 is named as the accepting authority; F-W2 claims no credit (FR-GIG-5 bar). A1/A2's "seat-measured" framings on the 22-key resolution claim carry the same re-attribution (banked at `fr-EasingPicker:52`).

### R-1e — the W6/W7 systematic F-W5 clause re-keying · owners F.W6 + F.W7 · verified against live `F-W5.md:138-157`

Live register (this seat's read): **E7** = PATCH atom coverage + `set_hash` (F-β ⊕ SS-C-1 write leg) · **E8** = AnimationSettings three-way reconciliation (BC-9/C-6/D-20 ⊕ SS-C-2) · **E9** = One shape one name (C-7) · **E11** = Easing domain hoisted (RESOLVER ⊕ L/M-3) · **E12** = Debounced mirror (MISSED-E) · **E13** = Cache identity ⊇ consumed fields (B-4 …) · **E14** = Contour provenance · **E17** = Image bounds on write (C-2) · **E18** = Attribution/actor (AA-10) · **D5** = identifier a human surface may render · **D6** = Lifecycle/cascade (R6) · **D17** = Date serialization. **F-W5 has NO clause D9** (`grep '\*\*D9\*\*'` → 0). Gates run **G1–G22 + G1c–G10c** (`F-W5.md:212`).

**Cure — F.W6** (one mechanical re-key, every occurrence): "E8"→**E7** (×4: §2.1 F-β, §2.1 SS-C-1, FW6-G2, §4); "E12"→**E13** (§2.6 B-4); "E11"→**E14 + E17** (§2.6 M-13); "D12"→**D5** (§2.4 FR-AFP-7); "D10"→**D17** (§2.4 serializer + FW6-G12); "E7"→**D6 / owner ruling R6** (§2.4 FR-AFP-66); invented "D9" (×3: §2.5 AA-10, §2.10 V-γ, §4)→**E18**. The gate-gap claim re-stated: *"F.W5's G1–G22 (+G1c–G10c) carry no gate for clause **E7**"*. FW6-G18's owner roster becomes **R1–R9 complete**: restore **R5** (off-state `[]` admission — M-9/D12) and **R9** (delete-or-wire dead session subsystem — FR-USB-23/C3, its ⊙ KEPT on the F.W8 routing row); relabel born-visibility **R8/E4** (not "G5") and the hard-delete arm **R6/D6** (not "E7").

**Cure — F.W7** (same re-key): C-25 ≡ **E13** (not E12); C-2 ⊕ K-3 ≡ **E17** (not E11); SS-C-1 ≡ **E7** (not E8); SS-C-2 rides **inside E8** (not E9; and the "E10/B4" pairing drops "B4" per F-W5:147's own separation). G-F7-8's eight-identity roster becomes **E1 · E3 · E5 · E7 · E8 · E10 · E13 · E17**. The six wrong section pointers (D-11) re-keyed to F-W5's real headings (§2 for all clauses; FR-GIG-5 bar at **§0b**; the "C-25 verbatim" quote conformed to F-W5.md:150's actual bytes: *"SUPERSET of the request fields the operation consumes"*).

### R-1f — mis-keyed coordinates on real authorities (re-anchor, never delete; the facts hold, the addresses do not)

- **F.W8**: `F-W5 §6c` (×3: `:5`, `:122`, `:202`) → **`F-W5.md §4` (`:245`)**; `F-W5 §6b` (`:144`) → **`F-W5.md` clause C2 (`:106`)**; the MF-9 lock → **F-W5 G22 (`:241`)**; `:5`'s "re-read" attestation restated against the section actually read.
- **F.W2**: phantom `F-W5.md:234` (×2, §3 D3 + §6b) → the export row **`F-W5.md:253`** and P-9's home row **`:272`**.
- **F.W9**: G-F9-14's `PaperArticleWindow:52` → **`web/src/lib/figureDimensions.ts:52`** (the registry's bare `:52` stays bare; the spec-added filename is corrected, per the FR-CP-45 law F-W9 itself adopts).
- **F.W7**: G-F7-11's "F-W0 and F-W6 do not exist" is STRUCK — `waves/` holds 11 files (this seat's `ls`); the two "owed reciprocal" cells re-measured (F-W6.md:182 already DECLARES its F.W7 edge; F-W0's reciprocal is genuinely owed, for the true reason).
- **F.W0**: G-6's two gate-condition anchors re-resolved (`lane-frontend.md:70` not `:69`; `DESIGN.md:34` not `:32`); "census §4.11/§4.5" re-spelled as §4 items 11 and 5.

---

## R-2 · THE 60 PARTITION ESCAPES — one adopting wave per id

**LAW**: every id below lands in its adopting wave as a §2/§3 row, a named fold, or an exclusion-with-reason — by its BANKED id, record-qualified where the token is short. No id lands in two waves; a second wave that needs it cites the adopting wave.

### R-2a — Tier 1 (the UNION §1b roster, amended per A-β)

| ids | adopting wave | landing shape |
|---|---|---|
| GCM-24 · GCM-44 · GCM-45 · GCM-46 · GCM-49 | **F.W4** | rows in the GCM restore family (GCM-44 cross-cites banked fr-ExportModal M-δ as pattern-class, per the record) |
| PAW-9 · PAW-13 · PAW-14 · PAW-19 · PAW-21 · PAW-23 · PAW-24 · PAW-55 · PAW-57 (+ annex PAW-7 · PAW-11 · PAW-15) | **F.W4** | rows beside the booked PAW set; LAW-3/PAW-44 sequencing untouched |
| FR-COB-5 · FR-COB-22 | **F.W3** | rows riding FR-COB-17's lift (the 36-site `size="icon"` naming sweep + register note), sequenced with F.W0's `rm` |
| **LF-1** (fr-GlassTimeline:65) | **F.W3** | BOTH a §2 carry row (inline `left`% per frame vs producer "NEVER `style.left`") AND an S-5 SS-6 letter item — it was dropped in both directions; both restored in one edit |
| RB-1 (fr-GlassTimeline:84) | **F.W1** | charter-note row (K-14/SR-1 route authorities mis-baselined) |
| FM-4 FM-5 FM-6 FM-7 FM-8 FM-9 FM-10 FM-11 FM-15 (+ annex FM-13 · FM-14) | **F.W4** | the whole FM-4..FM-16 band booked as rows; **FM-14's F.W3/W4 split restored with a named F.W3 citation** |
| FR-EQR-18 · FR-EQR-20 · FR-EQR-22 · FR-EQR-23 · FR-EQR-28 | **F.W4** | rows (FR-EQR-18 folds → fr-EquationView I-3, which F.W4 also adopts below; FR-EQR-28's fold names ECD C-23) |
| fr-EquationView **M-TL** · **D·D-M3** | **F.W1** | benefit-ledger rows (≥7 ships useTabRovingFocus; coarse-pointer tap-toggle) |
| fr-EquationView **M-ZM** · **I-3** · **I-4** | **F.W4** | rows by id (M-ZM's mechanism already lives in SP-6 — the id joins it, anti-rename cured; I-3's `trust: true` LOAD-BEARING constraint carried with its `:248` extension; I-4 rides intake R5-7's standing F.W4 carry, id named at §0 BS-1) |
| fr-EquationView M-CK · M-SB · M-DV · B-1 · B-2 (+ C·D-02, L·m-6) | **F.W5 (already home — per A-β)** | NOT re-adopted. Cure = record-qualify (`fr-EquationView B-1`, `fr-EquationView B-2`) at their F1/F2/B1/F3/F4 clauses; F.W6/F.W8 cite F.W5's clauses (R-5, R-6) |
| ★MF-4 · ★MF-5 · ★MF-6 (fr-PaperView) | **F.W4** | rows (ℱ glyph text-alternative; iOS auto-zoom; `.sidebar-link` font-weight transition) |
| INFO-3 (fr-EditorControlsDock:97) | **F.W4** | row — the separator cure takes the whole DockLayerGroup/DockLayer family |
| FR-IC-17 (fr-InfoCard:49) | **F.W1** | a DISCHARGED-BY-UPLIFT ledger row (the wave's discharge register names it; no work, one line) |
| CP-33 (fr-ContourPreview:66) | **F.W4** | named fold → CP-ROW-40 (target already booked; the id joins it) |
| MPC-29 (fr-MorphPhaseConfig:91) | **F.W1** | row — /morph's one screenshot gate catches MPC-5 drift iff baseline regeneration straddles the bump; bears on ESC-4 acceptance |
| C-S3 (fr-SvgFilters:64) | **F.W1** | context row (pencil-boil the only @mkbabb dep in exact peer-compliance) |
| M-δ (fr-ExportModal:41) | **F.W3** | row (`@open-auto-focus.prevent` + explicit target); F.W4's GCM-44 cites it as pattern-class |
| fr-EasingPicker L30 ("Producer-arrival severity", :30) | **F.W3** | formation-input row (the arrival re-homes an entire F.W3 disposition, kills two prescribed cures) |
| C-30 (fr-AnimationControls:123) | **F.W5** | fold-by-reference row at B1 with R6-8's carry — the join-ambiguity leg named |
| F-6 (= C-C-9 = L-15, fr-GalleryDraftsSection:49) | **F.W5** | the D1 register's SECOND blindness lock: the template-`src`-binding edge is invisible to a function-keyed model — booked beside COUNTING LOCK K-1 |

### R-2b — Tier 2: the 8-item `F-W1-CARRY.md:246` visual-regression checkpoint set · adopting wave **F.W9** (home), F.W10 terminal-verify

The set (verbatim from the carry): `--shadow-cartoon` sign flip · `cartoon-surface` hover loss (21 sites) · `.disclosure-content` body register · tooltip re-proportion · GCM-22 `p-0` insets · Badge rim · FR-CP-13 fused-card gap · `text-admin-label` reversion.

**Cure**: F.W9 books the 8 as one checkpoint table inside the §2.2 SHARED region, citing `carry/F-W1-CARRY.md:246` by path; F.W10 receives the IDENTICAL bytes by paired splice (the twins' shared-bytes law: spliced, never retyped; G-F9-21 ≡ G-F10-6 witnesses byte-identity after the edit). F.W9 owns execution; F.W10 owns terminal disposition. One paired edit, two files, one home.

### R-2c — annex limbs (UNION §1c, named so they stop being unhomed)

**F.W0** adds id rows for **FR-COB-3**, **FR-COB-15**, **PP-DEAD** (all terminally "dies with the file (F.W0)"; currently held at F-W3 only — the F.W0 limbs get their ids: FR-COB-3's sweep-law rider attributed at §6b, FR-COB-15's wrapper-policy line joined to §7b's `W3-button-ledger.md:93` cite, PP-DEAD's `noUnusedLocals` instance in row 15 gains its id). **F.W1** adds: fr-SvgFilters **D §3 U-3** (the twice-rejected feTurbulence register must not ride the uplift — negative-carry row; disambiguated from F-W3.md:154's homonym axis id), fr-PaperSearch **D-14/D-15/D-17** (the three re-ink token limbs beside the carry's re-ink cell), **AA-11** (constraint row beside G15's FR-GIG-5 bar: never plan the pagination cure around `./pagination`), **FR-COB-28** (the seven-item relay-packet id over the NWO-1 roster that already lists its members). **F.W4** adds **FR-USB-18**'s F.W4 leg (logout busy affordance; the F.W1 half is carried) and restores its three carry drops (**FR-AH-24** F.W4 leg id · **SS-C-7** corroborating witness on AC-C-2 · **FR-USB-19** in FR-AUL-41's cohort list). **F.W3** adds **FR-AFP-33** (client flag-list collapse limb, routed F.W1/W3 — F.W3 is the adopting wave; F.W1 cites). **F.W2** adds **C-18** (fr-AnimationControls:120 — the colors.ts-deletion denominator twin of D1's C-25) and the four P-9 zero-cells its D3 roster omits (**fr-CollapsibleSection · fr-PaperSearch C-Z1/C-Z2 · fr-PaperSearchInput C-13/C-14 · fr-PaperSearchDropdown C:C-23/C-24**), closing G19 against the real `F-W5.md:253` export.

**Sibling correction**: F-W3 names its 48 dual-routed F.W3/W4 handoffs per id in §X (one line each, per its own "named at its row with its trigger" promise) — the rows are held by F.W4; F.W3's lines are citations, not bookings.

---

## R-3 · PHANTOM CARRY LEDGERS — one idiom, program-wide

**Verified**: `ls docs/tranches/X/fourier/carry/` → `F-W1-CARRY.md`, `F-W4-CARRY.md` — the ONLY carries. Nine specs (W0, W2, W3, W5, W6, W7, W8, W9, W10) certify closure against per-wave carries that do not exist.

**RULED IDIOM**: **only `carry/F-W1-CARRY.md` (F.W1) and `carry/F-W4-CARRY.md` (F.W4) may be cited as carries, and only by path.** Every other wave's carry-closure gate re-points its operands at authorities that exist in tree:

1. **The LHS of every id-keyed closure gate becomes the wave's PASS-1 census roster** in `conformance/PASS-1/F-Wn-CHECK.md` (path-cited) **and/or the 66-record registry directly**. Specifically: **W5 G19** re-based on the registry census (routedTotal 162, F-W5-CHECK §1) — every "128 CARRY" reference DELETED (`:5`, `:39`, `:74`, `:208`, `:238`, `:279`); the P-9 tail row keeps its real cite (`F-W4-CARRY.md:18`). **W3 g19** re-based on F-W3-CHECK §3's 317-row roster; §0/§Z's "90 rows / two drafts" provenance demoted to a historical session note with no gate weight. **W2 G19** re-based on F-W2-CHECK's 54-roster; "26 CARRY rows" language struck. **W6 FW6-G17** re-based on the en-dash-corrected 122-identity roster (R-6) + the 7-record negative roster as the lower operand; line 3's *"verified row-for-row against the F.W6 CARRY, consumed whole … zero silent drops"* header STRUCK (it is a false verification receipt over a nonexistent artefact). **W8 G15** re-based on its own §0 spelling-agnostic census (which is already registry-derived and reproduces); its 8 pathless "the CARRY" cites re-pointed. **W0**'s 12 "the CARRY" cites re-pointed: the bounds authority becomes §2a's own tree-derived enumeration; the "74,507 B" figure re-attributed to its real homes (`V/apotheosis/pi/HANDOFF.md:306`, `CENSUS-2026-08-03.md:360`); the §1d.2 "retraction-of-a-retraction" G-12 row re-grounded on the banked 8 (`fr-AdminFlaggedPanel:110` et al.). **W7**'s CARRY language (fold provenance, §2a, §5, §6, §7c) re-pointed at the registry + F-W7-CHECK. **W9/W10** masthead provenance ("intake CARRY consumed whole: 36 rows…", "…77 rows…") re-written as historical notes; every §2.8 correction re-grounded on the in-tree authority it corrects toward (the registry, F-W0's tables, the live tree).
2. **NO wave authors a new `F-Wn-CARRY.md` in repair round 1.** Minting nine new authorities mid-repair multiplies the defect class; the two real carries stay the only carries.
3. **Every gate witness names a path that exists** (or an explicit `create` marker with its path). "The CARRY" with no path is forbidden vocabulary from this ruling forward.

---

## R-4 · F.W1 ATOMICITY — `F-W1.md:276` restored as the single authority

**Verified**: `F-W1.md:276` charters the atomic transaction as **eleven limbs in ONE change** (producer bump lockstep · 162-attribute Button rewrite · `copied`→`status` triple · lucide rename · G10 disclosure deletions · **FR-EQC-7 vaul-vue manifest gate** · G13 manifest/lock moves · PP-REDGATE ambient · FR-CP-13 gap · M-γ deletion · GCM-22 `p-0` + pencil-boil floor); `:294` pins *"FR-EQC-7's vaul-vue gate lands INSIDE the F.W1 transaction, not before."*

### R-4a — F-W0 G-6 corrected to a PRECONDITION-CHECK, not a limb-landing · owner F.W0

G-6, §6a lock 5, and unit F.W0.d currently land the manifest banding at F.W0 — the exact shape F-W0's own row 9 and G-15(a) forbid. **Cure**: F.W0.d becomes **gate-AUTHORING**: F.W0 authors the manifest gate and measures its born-RED facts (vaul-vue undeclared + 0 lock entries; `@lucide/vue` undeclared; the five runtime devDeps), **conditioned on G-1's branch** (ABANDON ⇒ the measurements are recorded against the committed 3.1.0 manifest and the gate transfers to F.W1 unchanged). The DECLARE+LOCK landing is F.W1's, inside the `:276` transaction per `:294`. G-6's "graph evaluates without a module-eval throw" clause is re-stated as F.W1-discharged (it needs G-5/G-4, which F.W0 cannot guarantee). `embla-carousel-vue` (optional peer at 4.0.0 — F-W0-CHECK D-13) is removed from the land-together set.

### R-4b — the siblings CITE, never restate · owners F.W2, F.W4, F.W5, F.W6, F.W7, F.W8, F.W9, F.W10

A sibling that restates the transaction re-fractures it. **Cure (exact text, replacing every 3-limb restatement)**:

> ▲ F.W1 is ONE atomic land-or-lose transaction — **the ELEVEN-limb roster chartered at `F-W1.md:276`, incl. the FR-EQC-7 vaul-vue manifest gate INSIDE per `:294` — cited whole, never restated here.**

Applied at: F-W4 §5.1(2) + G-F4-ZERO-CONSOLE (×2) · F-W5:252 · F-W6 §4 lock 5 · F-W7 §7c · F-W8 §5c (:200) · F-W9:216/§4a-3 · F-W10 §4a.3 + §2.7 FR-EQR-3 row · F-W2 §6b (adds the citation where its cross-edge omitted vaul-vue and RE-PIN). Waves may still NAME individual limbs when a local gate binds one (e.g. F-W2's G3/G4 ceding) — but the transaction's extent is stated only by citation.

**Sibling corrections riding this ruling**: (i) **F-W1** commit #3 (the `--viz-*` palette cure) gains explicit failure semantics — if commit #4 aborts, #3 reverts, OR #3 is declared severable with cross-edge 3's rationale stated AS its abort clause; (ii) **F-W1** G5's P0 CSS-class census gains a landing cell: a conditional limb inside §4.4 — "if the census returns consumer re-targets, those edits land inside commit #4"; (iii) **F-W4** gates every "8.0.0-as-settled" substrate assertion (×8) on **ESC-1/G1 by name** — the pin is `4→7(→8, G1-gated)` and no cure-shape presumes the unruled branch.

---

## R-5 · THE W5↔W8 64-ID SPLIT — one home per id, the reciprocal DECLARED

**Verified**: `grep -c 'F\.W8' F-W5.md` → **0**; F-W8 books 64 ids F-W5 also carries, under an asserted-unilaterally "F.W5 states / F.W8 asserts" split; W8's G15 ("homed at exactly ONE of F.W5/W6/W7/W8") is undischargeable from the keystone's end.

**Cure**: **F-W5 §4 adds the reciprocal edge row** (exact shape):

> | **F.W5 → F.W8** | F.W5 STATES the clauses; F.W8 ASSERTS them (fixture/e2e direction). ONE home (F.W5), two citations — the shared ids are enumerated at `F-W8.md` §1's census. No F.W8 row re-books a clause; no F.W5 clause claims F.W8's assertion work. Reciprocal: `F-W8.md` §5c. |

and a **F.W5 → F.W7** row (E16/G7 cited by F.W7; F.W7 books nothing — closing the D-16/M-6 gap from the keystone's end). With the reciprocal published, **W8's G15 is re-worded dischargeable**: "every band row has exactly ONE STATING home at F.W5/W6/W7/W8; F.W8's assertion citations are not homes." **F.W8 record-qualifies every short id** it books (`fr-BasisCanvas C-17`, `fr-EquationView B-1/B-2`, `fr-ContourEditorCanvas C-2`, `fr-FunctionInput L-B1/L-M3`, …) so the set-difference can run — the same qualification law binds F.W5 and F.W6 for the eight colliding tokens (UNION U-12: M-13 · L-B1 · L-M3 · C-17 · C-18 · B-1 · B-2 · C-2). **F.W8 adds the 37-id exclusion table** (one row per §1 escape naming its F-W5/F-W6 home — converting its deferred closure into a performed one) and fixes the D2 FSE block (strike the duplicate `L-M5 / C-5` listing, admit `C-3`, restate the count).

**Same-family collisions cured here**: the **F-W2 easing credit collision** — F-W2's A1/A2 acts cite `fr-EasingPicker L/B-1 + C/B-1` as the banked identity and `F-W1.md:161` as the carrying home (per R-1d); the **W0↔W9 runner-seat collision** — one disjointness sentence both ends: F.W0 G-9 owns the SEAT (runner installed, wired into ci.yml, ONE asserting spec); F.W9 G-F9-1 owns the FLOOR (the spec population and thresholds), and F-W9's harness chain adds the F.W0 seat leg it omitted.

---

## R-6 · W6 EN-DASH BLINDNESS — the census idiom ruled en-dash-tolerant

**Verified**: the routing token `F.W5–W8` uses U+2013 in 24 records (72 occurrences); the hyphen form in 26 (120); union **192 occurrences / 49 records**, 23 records en-dash-ONLY. F-W6's census (line 3: "120 / 26") is structurally blind to 23 records and 56 of its 122 span identities escaped.

**RULED IDIOM (program-wide)**: the canonical span probe is spelling-agnostic — `grep -rhoE "F\.W5[-–]W8" fr-*.md` (BOTH U+002D and U+2013), with the S-8 enumeration law (walk back to the enclosing row id; never trust a single literal). F-W8 §0's census is named the conforming model.

**Cure — F.W6**: (1) line 3's census re-measured and re-stated: 192/49 (120/26 ⊕ 72/24; 23 en-dash-only); (2) **F-W6 re-books its 56 escaped span identities** under the corrected detector — each of the F-W6-CHECK §2.2 rows lands as a §2 row, a named fold, or a §5 exclusion; where the id's STATING home is an F.W5 clause (most of the 56), the landing is a CITATION of that clause under its R-1e-corrected id (booking would violate R-5's one-home law); (3) the 7-record NEGATIVE roster (fr-App:144 · fr-CollapsibleSection:131 · fr-MobileFloatingToc:97 · fr-PaperSearch:90 · fr-PaperSearchDropdown:77 · fr-PaperSearchInput:125 · fr-MorphShapePreview:110) is carried as FW6-G17's lower-bound operand; (4) the fr-GallerySearchBar **C-2 WAVE-LOCK** (binding "any F.W5-W8 wiring" by name) and **m-7's live arm** are restored (locks are CARRIED, not dropped). The same both-dash probe law binds F-W5's and F-W7's band prose wherever the span is measured.

---

## R-7 · W10's NO-WAVE-OWNER DRAIN — homed at its declared deadline

**Verified**: F-W10 declares itself COHESION §3.3's deadline ("closes HERE"), homes 23 rows, stamps "zero left open" — while ≥36 strict-dispositioned NO-WAVE-OWNER rows (94 row-level / 167 occurrences / 56 records) are neither carried, folded, nor excluded. `grep -c 'FR-NP-32' F-W10.md` → 0 while the producer packet **O-20** carries the id.

**Cure — F.W10 (the sole drain; F-W9.md:243 refuses it correctly)**:
1. **FR-NP-32 FIRST** — the BLOCKER heads the §2.5 drain table, disposition verbatim from `fr-NotationPills:35` (NO-WAVE-OWNER producer dist emitter + glass-ui BH relay at the TOP of the FR-COB-28 packet + F.W1 SEQUENCING GATE), with the O-20 receipt attached (D1 below). The close-gate carries the id the producer packet carries.
2. The drain roster becomes **23 homed + the 36-strict roster of F-W10-CHECK §2.1**, each with a TERMINAL verb (carried / folded-with-target / excluded-with-reason — producer-owned rows route to the BH relay register; SS-3/SS-4 methodology rows to the census-input register; F.W2/F.W5 input rows cite their consuming wave). The 25 loose candidates get one class-level disposition line naming them.
3. **Count words reconciled**: "Twenty-three rows, twenty-three verbs, zero left open" and G-F10-7's "23" are re-denominated to the homed total; the falsifier reads "every row of the reconciled roster bears a TERMINAL verb".
4. **D1/D2 receipts**: §1b bounds and G-F10-12 add `glass-ui/docs/tranches/BK/coordination/` (the live inbox — 11 packets, not 10); G-F10-12's F-half re-measured: the `@source`/FR-NP-32 arm is ALREADY receipted in O-20 (`valuejs-outbound-2026-08-28-o20-authoring-block-batch.md` row A-1) — the arm asserted RED is GREEN-by-receipt, and the gate cell says so.
5. **D3**: G-F10-1/E-1's denominator corrected: the live ADOPTION-ASKS table holds **17 id-rows, 11 OPEN** (6 closed, enumerated), value.js targeted on 6 (4 OPEN).

---

## R-8 · FR-NP-32 EROSION — the corrupt-dist sequencing gate restored by id

**LAW**: banked ids are original for life. `FR-NP-32` (fr-NotationPills:35, BLOCKER, reproduced with the app's own toolchain) and `fr-PaperSidebar M1` are two banked witnesses of ONE fact; the cure is cite-both, never substitute. Canonical citation form everywhere the gate is stated: **"FR-NP-32 (≡ fr-PaperSidebar M1)"**.

- **F.W3**: adds the id (currently 0 hits) — an OP-row beside OP-2/g18 citing FR-NP-32 (≡ M1) with its BLOCKER grade, AND one disclosure sentence in §7: *"the per-batch `npx vite build` cadence is RED BY CONSTRUCTION at the installed ^4.0.0 pin until FR-NP-32 clears (F.W0 G-4); a batch's build gate reads against that standing RED, not against the batch."*
- **F.W5**: the §4 X-1 edge (F.W0 → F.W5) adds FR-NP-32 by id beside fr-PaperSidebar M1 — the corpus's SEQUENCING-GATED witness joins the parse-census witness.
- **F.W10**: per R-7 item 1.
- **F.W4**: §5.1(1)'s attribution becomes "FR-NP-32 (≡ fr-PaperSidebar M1)" (the id already lives at §2.H; the gate row now carries it too).
- **F.W9**: same conformance wherever the mechanism is carried under M1 alone.
- **F.W2**: G14/§6b's "an F.W0 pre-gate" gains the re-home note — banked disposition is *NO-WAVE-OWNER (producer dist emitter) + F.W1 SEQUENCING GATE*; `F-W0.md:103` performs the F.W0 re-home; F-W2 cites that re-home rather than asserting the pre-gate bare.

---

## R-9 · F.W0's PUBLISHED-ONCE LAWS — owned once, quoted everywhere

**Verified**: `F-W0.md:199` G-11 ("ONE corrected anchor table published; every later wave quotes it", landing in `SUBSTRATE-LEDGER.md`), `:204` G-12 (corrected denominators; superseded figures forbidden), `:209` G-13 (producer pin re-derived at the ADOPTED commit; P-4: hash, never version string). Live glass-ui HEAD this session: **`207bf174`** (2026-08-28) — a sixth drift beyond F-W0's published 5-hash chain.

1. **G-11/G-12 owned once.** The five re-ownings become QUOTATIONS: **F-W1 G1**, **F-W2 §2b**, **F-W3 g17**, **F-W4 §0/G-F4-ANCHORS**, **F-W9 §2.8 C-4 + E-1** are each re-labelled *"input to / quotation of F.W0's G-11 anchor table (G-12 denominators) in `SUBSTRATE-LEDGER.md`; divergence from that table is a defect against G-11, not a rival act"*. No wave re-performs the re-resolution as its own act. **All ten later waves add the one-line G-11/G-12 quotation** at their F.W0 edge (W5, W6, W7, W8, W10 currently name neither; W7's D-15 cell names `F-W0.md:199` + the ledger path).
2. **P-4 cures.** **F-W3 g6**: the `v7.0.0:Slider.vue:69-78` RED baseline (×20 `7.0.0` cites) is re-anchored at the producer's ADOPTED commit hash via G-13's pin table; until that table exists, the cell is marked MEASURE-AT-OPEN — never a version string. §C.H's blanket re-read rule stays but is subordinated to the re-anchor.
3. **F-W0's own G-13**: cured with a **DATED ADDENDUM ROW** (E-3 addenda-not-patch — never a silent rewrite):

   > **G-13 addendum, 2026-08-28**: live producer HEAD is `207bf174` (glass-ui, 2026-08-28) — a SIXTH drift beyond the chain published above, which is retained as the historical record. The chain re-anchors HERE; '8.0.0' remains a version label, and per P-4 the pin cell is the COMMIT HASH. F.W1 re-derives at the ADOPTED pin per FR-EQR-33(a); FR-EQR-33(b)'s ≥7-binding facts (CopyFailureReason narrowing + timeoutMs, K-12) are named as surviving at any ≥7 target.

4. **F-W0 §2a's denominator** corrected ONCE, in G-12's own table: 24 ` M` SFCs; 21 remaining after the three named above the row; 19 genuinely untouched after the two named below it.

---

## R-10 · PHANTOM PATHS IN BOUNDS — verified at the tree, corrected or struck

**LAW**: a bounds-keyed halt condition may not rest on a path that does not exist. Every §1 bounds path in every wave must `ls`-resolve at spec time or carry an explicit `create` marker with its path. The bound is the FILE; the spelling must be the tree's.

1. **F-W1 §1** — the 8 phantoms corrected to the live coordinates (F-W1-CHECK §7, tree-verified): `src/lib/basis-display.ts`→`web/src/components/visualization/lib/basis-display.ts` · `src/env.d.ts`→`web/env.d.ts` · CoefficientsSpectrum→`components/shared/` · UserSlugBar→`components/visualization/gallery/` · CanvasControlsDock, EditorControlsDock, AnimationControls→`components/visualization/` · AppHeader→`components/layout/`. The four doubling as gate witnesses (G6, G10's sibling, G11, the metric-badge roster) re-anchor identically.
2. **F-W3 §1** — the 20 phantoms corrected per F-W3-CHECK D-4's live-path table (ConvergenceTimeline→`equation/convergence/` · EqCoefficientsPanel→`equation/` · HarmonicLevelGrid→`morph/` · SvgFilters→`decorative/` · EquationPanel→`visualization/` · the paper-search set→`paper/search/` · the admin trio→`visualization/gallery/` · `lib/notation.ts`→`lib/equation/` · labels.ts→`visualization/lib/canvas-drawing/` · the composables to their live homes); bare `types.ts` disambiguated by naming the intended file(s) among the three candidates. **The root `index.html` does not exist** — the three mutually contradictory cites collapse to the ONE real file `web/index.html` (fix `:114` "repo root" and `:419`'s absolute root path; `:329` is already correct). §5a's `.d`/`.e` worktree split re-cut so it partitions the REAL tree (SvgFilters in `decorative/`, EquationPanel in `visualization/`, admin trio in `visualization/gallery/`).
3. **J-diff-shape.md** (F-W6:33/:190, F-W7:54, F-W0 §2a) — the file exists ONLY at `/Users/mkbabb/Programming/fourier-analysis/docs/tranches/J/design/J-diff-shape.md`. Every cite re-pointed there, labelled *"in the READ-ONLY fourier tree — readable, never writable"* (resolving F-W6's self-contradiction: read-only-in-bounds, not out-of-bounds).
4. **F-W7's `contract/**` + `OWNER-RULINGS-F.W5.md`** (`:54`, §4, §5a, §7c, §8) — re-marked **to-be-created by F.W5** (F-W5 §1a creates them): "cited-WHEN-created; until then the obligation is prospective". Never again listed among existing immutable witness-holders. F-W6:33's identical list gains the same marker.
5. **Unrooted `formation/` / `audit/` / `intakes/` paths** in F-W4 (§Authority), F-W6 (§1), F-W7 (:54) — rooted at `docs/tranches/V/megatranche/…` (tree-verified homes). F-W0's D-8 bare `docs/tranches/J/design/J-diff-shape.md` row takes cure 3's spelling. F-W0's un-bounded intake home for bare R3-7 (`audit/codex-provenance/intakes/lane-fourier-r3-r6.md`) is added to its §1 inputs so G-14 can disposition the rows it names.
6. **F-W2's five gate commands** (G5/G8/G11/G13/G17) — rooted at `/Users/mkbabb/Programming/fourier-analysis/…` (the declared cwd has no `fourier-analysis/`; a gate command that cannot execute as written is a phantom witness).
7. **F-W6's stale bounds reconciliation** — the 7-file `waves/` enumeration re-measured (11 files, this seat's `ls`).

---

## R-11 · W7's ZERO-CENSUS — the registry surface ruled

**RULED**: F-W7's routing measurement is **CORRECT** — `routedTotal = 0`. The only `W7` hits in the 66 records are `KF.W7` (a keyframes cross-repo note) and non-F tokens; no adjudicated row carries a terminal disposition to F.W7. The spec's routing token is NOT mis-keyed; **the wave genuinely owns no registry rows**, existing solely as SS-4's owner-gated trie design (E16/G7).

**Cure — F-W7 states the ∅ the way KF.W1 does** (the C-14 form, `KF-W1.md:136` + the `:256` lock):
1. §5a gains a minted **`F-MAIL-∅`-form row**: *"Zero adjudicated registry rows target this wave"*, verified with the TOKEN-not-substring probe stated so the grep it invites cannot falsify it (the two `KF.W7` substring hits disposed inline), plus the lock verbatim: **"a wave with no registry rows gets no manufactured ones — the ∅ is a finding, not a gap."** The wave's specced work is exactly the owner-gated design question and its declarations, nothing else.
2. §8 gains the **fr-ContourEditorCanvas L-5** exclusion-with-reason row (`:56` — the corpus's ONE true hit for R-4's six-term probe, naming structural sharing as an adjudicated F.W3/W4 cure candidate over a flat coordinate bag): routed F.W3/W4, NOT F.W7's to book — named so a later reader cannot mistake absence for oversight, and so §12's "no material on either tree" framing is qualified by the registry's own candidate.
3. **N-2's figures corrected** to this seat's/the check's reproduction: the six-term probe returns **147 lines — 146 substring artefacts + 1 true hit (L-5)** — never "60+, every one an artefact".
4. The §5 census count corrected: the table holds **14 rows**, not "11 of 11"; the "zero drops" claim re-stated over the counted table.
5. G-F7-11 and the two "owed reciprocal" cells re-measured per R-1f (11 wave files exist; F-W6 already declares its edge).

---

## PER-WAVE DIRECTIVE INDEX

| wave | directives (by ruling) | n |
|---|---|---|
| **F-W0** | R-3 (carry re-point ×12 cites) · R-4a (G-6 → precondition-check; F.W0.d re-cut) · R-9.3 (G-13 dated addendum @ `207bf174`) · R-9.4 (§2a denominator 24/21/19) · R-2c (FR-COB-3 · FR-COB-15 · PP-DEAD id rows) · R-10.3/.5 (J-diff-shape spelling; intake home bounded) + R-1f anchors (lane-frontend:70, DESIGN.md:34, §4 items) | **6** |
| **F-W1** | R-2a/c (adopt RB-1 · M-TL · D·D-M3 · FR-IC-17 · MPC-29 · C-S3 + U-3 · D-14/D-15/D-17 · AA-11 · FR-COB-28) · R-4 sibling (commit-3 abort semantics + G5 landing cell; G15/G20 born-RED restated) · R-9.1 (G1 → quotation of G-11) · R-10.1 (8 bounds paths corrected) | **4** |
| **F-W2** | R-1d (G15 re-written banked-verbatim; easing identity credited L/B-1+C/B-1 @ F-W1:161/ESC-4) · R-1f (`F-W5.md:234` → :253/:272) · R-2c (adopt C-18; four P-9 zero-cells; G19 closes on the real export) · R-3 (G19 re-based; "the CARRY" re-pointed) · R-4b (§6b cites F-W1:276 whole) · R-8 (FR-NP-32 re-home note) · R-9.1 (§2b declared a G-11 quotation) · R-10.6 (five gate commands rooted) | **8** |
| **F-W3** | R-2a (adopt LF-1 row+letter · FR-COB-5 · FR-COB-22 · M-δ · EasingPicker L30) + R-2c (FR-AFP-33; the 48 dual-routes named per id in §X) · R-3 (g19 re-based) · R-8 (FR-NP-32 id row + §7 RED-by-construction disclosure) · R-9.1 (g17 → G-12 quotation) · R-9.2 (g6 v7.0.0 → adopted-hash / MEASURE-AT-OPEN) · R-10.2 (20 paths + index.html tri-cite + .d/.e re-cut) | **7** |
| **F-W4** | R-2a (adopt GCM ×5 · PAW ×9(+3) · FM band ×11 · FR-EQR ×5 · ★MF ×3 · INFO-3 · CP-33 · M-ZM · I-3 · I-4) · R-2c (FR-USB-18 leg; 3 carry drops restored; registry→spec direction added to G-F4-CARRY-CLOSURE) · R-4b (cite F-W1:276, both sites) · R-4 sibling (8.0.0 gated on ESC-1/G1 by name, ×8) · R-8 (FR-NP-32 ≡ M1 at §5.1(1)) · R-9.1 (G-F4-ANCHORS → G-11 quotation) · R-10.5 (two authority paths rooted) | **7** |
| **F-W5** | R-1c (P-8 quotation deleted) · R-2a (adopt F-6 · C-30; record-qualify fr-EquationView B-1/B-2; SS-L-07/SS-C-10 booked by citation of F-W4-CARRY:234) · R-3 (G19 re-based; "128 CARRY" deleted ×6) · R-5 (reciprocal F.W8 edge + F.W7 edge row) · R-8 (X-1 edge adds FR-NP-32 by id) · R-4b (:252 → citation of F-W1:276) · R-9.1 (G-11 quotation) | **7** |
| **F-W6** | R-6 (en-dash census 192/49; 56 identities re-booked/cited; negative roster as G17 operand; C-2 WAVE-LOCK + m-7 live arm restored) · R-1e (six clause re-keys + D9→E18; G1–G22) · R-1e (FW6-G18 roster R1–R9 complete; R5+R9 restored; labels R8/E4 + R6/D6; FR-USB-23 ⊙ kept) · R-3 (line-3 header struck; FW6-G17 re-based) · R-4b (§4 lock 5 cites F-W1:276) · R-9.1 (G-11 quotation) · R-10.3/.5/.7 (J-diff-shape; unrooted paths; 11-file waves/) | **7** |
| **F-W7** | R-1a (trie quotation struck/re-sourced) · R-1b (dissent re-anchored → F-W5:153 ≡ F-W10:114) · R-1e (four clause ids + G-F7-8 roster + six section pointers + C-25 verbatim conformed) · R-11 (∅-census declared KF.W1-form; L-5 exclusion row; N-2 = 147/146+1; count 14; G-F7-11 re-measured) · R-3 (CARRY re-pointed) · R-10.4/.3/.5 (contract/** + OWNER-RULINGS → create-markers; J-diff-shape; unrooted paths) · R-9.1 (G-11/SUBSTRATE-LEDGER named at the F.W0 edge) · R-4b (§7c cites F-W1:276) | **8** |
| **F-W8** | R-1f (§6c/§6b/MF-9 re-anchored; :5 attestation restated) · R-3 (8 CARRY cites re-pointed — NO new carry authored) · R-5 (G15 dischargeable via F-W5 reciprocal; short ids record-qualified) · R-5 (37-id exclusion table + D2 FSE block fixed) · R-4b (§5c cites F-W1:276) · R-9.1 (G-11 quotation) | **6** |
| **F-W9** | R-2b (checkpoint set booked in shared §2.2 — paired splice with W10) · R-3 (phantom "intake CARRY" demoted; §2.8 re-grounded) · R-9.1 (G-11/G-12 quoted; C-4/E-1 re-labelled inputs to F.W0's tables) · R-8 (FR-NP-32 id beside M1) · R-4b (:216 cites F-W1:276) · R-1f (G-F9-14 → figureDimensions.ts:52) · R-5 (runner-seat disjointness sentence; F.W0 G-9 seat leg named) | **7** |
| **F-W10** | R-7.2/.3 (drain re-cut: +36 strict roster; count words + G-F10-7 re-denominated) · R-7.1/R-8 (FR-NP-32 FIRST; id in §2.3/G-F10-11) · R-2b (paired splice of the checkpoint set) · R-3 (77-row masthead demoted) · R-4b (§4a.3 cites F-W1:276 eleven limbs) · R-7.4 (BK inbox bounded; O-20 receipt; G-F10-12 F-half GREEN-by-receipt) · R-7.5 (ADOPTION-ASKS 17/11/6 denominator) · R-9.1 (G-11 quotation) | **8** |

**Residuals expressly left with per-wave seats** (not cross-wave classes): UNION M-1 arithmetic cluster except the items ruled above (W9's ci.yml `e2e-tests:`/`:100` and 4-vs-5 fixmes; W0 G-15(d) witness) · M-3 unrunnable-GREEN shapes (W3 g12/g17 OR-escapes; W1 G15/G20 born-RED — assigned under R-4 sibling for W1) · M-4 write-adjacent breaches (W0 G-15(a) stash sequencing; W3 `.worktrees/` ignore row; W5/W6/W8 read-absolute sentence reconciliation) · L-1/L-2 voice and hygiene rows. Each stays convicted per its check file; repair seats cure them wave-locally without cross-wave coordination.

*Rulings complete: 11. Read-only everywhere except this file.*
