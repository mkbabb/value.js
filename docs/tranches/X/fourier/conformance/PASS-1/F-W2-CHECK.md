# F-W2 — FRESH ADVERSARIAL SPEC CHECK (L-18 / L-20, PASS 1)

**Spec under trial**: `/Users/mkbabb/Programming/value.js/docs/tranches/X/fourier/waves/F-W2.md` (301 lines, `Status: planned`, fold-seat 2026-08-28)
**Corpus authority**: 66 `fr-*.md` in `/Users/mkbabb/Programming/value.js/docs/tranches/V/megatranche/registry/adjudicated/` (+ `library-band.md`; in-tree carries = `carry/F-W1-CARRY.md`, `carry/F-W4-CARRY.md` only)
**Method**: ID-keyed census (X·P terminal method). Routing detection = `grep -nE 'F[.·-]?W2'` over all 66 records (68 hits, sole form `F.W2`, 25 files) + non-literal terminal routings onto F.W2's own acts (W.L5 ACT1/ACT2/ACT3 — the easing/specifier surface and the `colors.ts` deletion) + the declared inbound P-9 export at `F-W5.md:253`.

**VERDICT: DEFECTIVE.** routedTotal **54** · booked **42** · escaped **12** · 3 BLOCKER-class + 9 MAJOR + 6 MINOR defects.

---

## 1. ID-keyed census — BOOKED (42)

| # | banked id | record | banked terminal disposition (receipt) | lands in F-W2 |
|---|---|---|---|---|
| 1 | `AA-20` (C·D-4 / L-14) | fr-AdminAuditLog:57 | "F.W2 must widen or exclude with reasons → F.W4 (cure) + F.W2 (charter note)" | C1 · G7 · R-i |
| 2 | `FR-AUL-55` | fr-AdminUserList:103 | "Scope fact for **F.W2** — the migration surface does not reach this file" | A1 · D3 |
| 3 | `C-2` (R-6) | fr-App:47 | routes **F.W1** (`:139`); carries the F.W2 sequencing correction | A1 dissent · G10 · §6a |
| 4 | `C-25` | fr-CanvasControlsDock:96 | "this file's F.W2 migration cost is zero … joins the F.W2 colours denominator"; NO-WAVE-OWNER | D1 · G11 |
| 5 | `B-3` (D:B-3/L-B1/C-1) | fr-CoefficientsSpectrum:41 | "→ F.W3/W4 … the value.js `sampleColorRamp` angle rides F.W2" | B5 |
| 6 | `M-10` (L-M4/D-m11/C-2) | fr-CoefficientsSpectrum:62 | "**→ F.W2 + F.W3/W4**" | B5 ⟨provenance⟩ |
| 7 | row 1 (D:B-1·C:D-1(a)·L:L-9) | fr-ContourPreview:34 | ADJUDICATED → **F.W2** | B1 |
| 8 | row 14 (C:D-1(b)) | fr-ContourPreview:47 | ADJUDICATED → **F.W2** (with row 1) | B2 |
| 9 | row 22 (D:m-8·L:L-9) | fr-ContourPreview:55 | ADJUDICATED → **F.W2** (mint `--contour-stroke`) | B3 |
| 10 | `RD-5` | fr-ConvergencePlot:26 | "the banked identity's F.W2 `sampleColorRamp` adoption angle must re-route" | B5 |
| 11 | `D-1` | fr-ConvergencePlot:48 | "→ **F.W4** … the join … rides the fr-CL C-2 disposition (F.W2/W.L5 + F.W4)" | B7 · §6b · §7 |
| 12 | `L-M6+C-1` | fr-ConvergencePlot:70 | fold → census identity, "**F.W2**, latent at HEAD" | A1 |
| 13 | `C-19` | fr-ConvergencePlot:88 | "**F.W2** rider on the specifier edit" | A3 |
| 14 | `C-22` | fr-ConvergencePlot:109 | "**F.W2** budget note" | A4 |
| 15 | `K-11` | fr-ConvergencePlot:127 | "AMENDS the banked ramp identity's F.W2 angle" | B6 |
| 16 | `S-9` (C·S-3) | fr-ConvergenceTimeline:123 | "the F.W2 value.js cut … ha[s] no purchase here" | D3 |
| 17 | `C-2` | fr-ConvergenceLegend:54 | one token two jobs; join, W.L5 ACT(2) collision | B7 · G5 |
| 18 | `C-15` | fr-ConvergenceLegend:83 | "FOLD → the ramp identity … which already adopted the F.W2 `sampleColorRamp` angle" | B5 · B6 |
| 19 | `C-1 + U-2` | fr-ConvergenceLegend:104 | "FOLD → CENSUS-2026-08-03:38 + lane-frontend:480" | A1 |
| 20 | `K-7` | fr-ConvergenceLegend:124 | kills "a specifier-grep F.W2 inventory UNDERCOUNTS" | A1 |
| 21 | `D-3+C-5` | fr-EquationPanel:48 | "F.W4 + **F.W2** rider" | C2 |
| 22 | `FR-EMT-24` | fr-EquationModeToggle:54 | "off the F.W2 surface — a load-bearing negative the F.W2 sweep must not violate" | D3 |
| 23 | `SC-4` | fr-EquationView:36 | fold → fr-CP C-19 (F.W2 rider) | A3 |
| 24 | `C·D-11` | fr-EquationView:69 | "F.W0/F.W2 — sequencing fact for the tri-package transaction" | E1 · G10 |
| 25 | `M-RTC` | fr-EquationView:72 | seat-ratified NEW; cure re-homing | B9 · G8 · R-ii |
| 26 | `I-5` (C·C-31) | fr-EquationView:127 | fold → fr-CP C-22 ("F.W2 scope evidence, not a defect") | A4 |
| 27 | `K-5` | fr-EquationView:138 | "A post-F.W2 cure mis-scheduled as landable" | A1 |
| 28 | `K-6` | fr-EquationView:139 | "fr-CP C-22 = F.W2 scope evidence, INFO" | A4 |
| 29 | `C·S-1/S-2` item 6 | fr-FunctionInput:112 | "already at the F.W2 target posture" | D3 |
| 30 | `C-5` | fr-GalleryInfiniteGrid:49 | "FOLDS BY REFERENCE to banked FR-GFC-8 (**→ F.W2**)" | B4 · G4 |
| 31 | `FR-GIG-17` (= C-18) | fr-GalleryInfiniteGrid:78 | "**→ F.W2 rider**" | A5 |
| 32 | `FR-GFC-8` (= C-5 R-3) | fr-GalleryFeaturedCarousel:39 | the snapshot wave-lock | B4 · G4 |
| 33 | row 13 (C:C-11) | fr-ImageUpload:47 | "→ **F.W2 rider** (inventory-blindness note)" | B12 · G11 |
| 34 | `HLG-4` (D:D-4⊕C:C-2) | fr-HarmonicLevelGrid:50 | FOLD → FMD-17 → F.W4/F.W2 | B10 |
| 35 | superlative 3 (D:S-2⊕C:S-1) | fr-HarmonicLevelGrid:113 | "the F.W2 remedy hierarchy … adopted" | B1 · B10 · §6a |
| 36 | `C·S-2` | fr-MorphShapePreview:138 | "**Preserve verbatim through F.W2**" | D3 (preservation) |
| 37 | item 9 (method) | fr-MobileFloatingToc:126 | "correctly scoped the component out of F.W2/F.W5" | D3 |
| 38 | `FR-NP-9` (C·D-4∘L·L-4) | fr-NotationPills:47 | "F.W3/W4 cure, **F.W2 tie**" | B11 · G11 |
| 39 | `C-08` | fr-PaperView:72 | "F.W2's colors.ts work provably does not reach this subtree → F.W4" | D3 |
| 40 | `K-24` | fr-PaperArticleWindow:303 | "the F.W1/F.W2 blast-radius argument rests on it — the inventory must be exact" | D2 · §2b |
| 41 | `FR-USB-38` | fr-UserSlugBar:81 | "**do NOT book this component into the F.W2 migration surface**" | D3 |
| 42 | `FR-TT-22` (= C-10) | fr-Tooltip:56 | "A zero-cost row on the F.W2 ledger — a negative budget statement" | D3 |

Booked-by-exclusion-with-reason (§7) counts as carried per G19; rows 11, 30, 32 are carried as locks/cross-refs with credit ceded to F.W1 — correct under FR-GIG-5.

---

## 2. ID-keyed census — ESCAPED (12). Named by bytes.

**None of the twelve appears anywhere in `F-W2.md`.** Receipts: `grep -c 'EasingPicker\|L/B-1\|ESC-4\|EasingCurvePreview\|CENSUS-MC\|MISSED-F' F-W2.md` → **0**; `grep -c 'AnimationControls' F-W2.md` → **0**; `grep -c 'InfoCard\|GalleryView\|MorphPhaseConfig\|FourierMorphSvg\|CollapsibleSection\|PaperSearch' F-W2.md` → 1 (the single hit is `InfoCard.vue`, a fourier consumer file in C2's blast radius, **not** the record `fr-InfoCard`).

### E-1 · `fr-EasingPicker` **L/B-1 + C/B-1** — the banked home of F.W2's own act (BLOCKER-class escape)
`fr-EasingPicker.md:52`: *"The import closure fails at value.js 4.0.0: no `\".\"` export (7 subpaths only; main/module/types absent) AND `timingFunctions` deleted (src grep = 0). The picker's own five functions survive at `./easing`… **LC's sharpenings RATIFIED with CC-2**: all 22 morph keys DO resolve through `easing(): Result` at 4.0.0 … **but 14 of 22 silently drift analytic→CubicBezier-approximation** (8 stay analytic incl. ease-out-cubic/-expo) — the morph curves change shape across the bump, **unbooked anywhere else**."*
This is the corpus's banked identity for exactly what F-W2 A1 + A2 + G2 + G15 book. F-W2 re-books it under `CENSUS-2026-08-03:38 / lane-frontend:480` + `W.L5-ACT1` with **no citation of the banked component-level id** — an anti-rename violation (M-25), and a credit collision with `F-W1.md:161`, which already carries `L/B-1 + C/B-1` (BLOCKER-fold) including the *"`Result`-shape rewrite for `EASING_PRESETS`"* — i.e. A2's act.

### E-2 · `fr-EasingPicker` **MISSED-F**
`:89`: *"easings.ts:9 and :10-16 are two import statements from the byte-identical specifier, and `ANIMATION_EASINGS` is a strict content-subset of `EASING_PRESETS` by object identity (seat node probe: 5/5 `timingFunctions[k] === easeInOut*` true at installed 0.13.0)"* → disposition **F.W1 (costing correction) + F.W4 (no-duplicate-imports hygiene)**. A1 books those two statements as 2 of its 5 sites without the banked id or the costing correction.

### E-3 · `fr-EasingCurvePreview` **CENSUS-MC** (C-D-5) — refutes G10's premise
`:58`: *"`@mkbabb/glass-ui/motion-curves` exists at the PINNED 4.0.0 (seat-read exports map) and value-re-exports every easing symbol `lib/easings.ts` imports bare-root (both readers verified the dist module; exception `timingFunctions` holds) — **the value.js-specifier half of the scheduled migration is NOT blocked on the uplift**"* → **F.W1** — census §1/§3 rows AMENDED. F-W2's G10 ("F.W2's specifier arm does not attempt to land alone") and A1's exports-map framing never cite or refute this banked amendment.

### E-4 · `fr-MorphPhaseConfig` **MPC-2** — an inverted cure-shape lock
`:46`: *"Easing catalog cannot resolve at the value.js bump: bare-root specifiers, no `\".\"` export at 4.0.0, `timingFunctions` absent from every entry point."* → *"**FOLD → fr-EasingPicker L/B-1 + C/B-1 · F.W1** (lockstep tri-package bump per MPC-14, **never a specifier rewrite**)."*
F-W2 §2a rows 1–4 prescribe precisely a specifier rewrite (`specifier → /easing` ×4 files). The banked lock is neither carried nor refuted.

### E-5 · `fr-FourierMorphSvg` r2 easings/value.js fold
`:46`: *"**NEW fold (r2) — easings/value.js seam → banked fr-EasingPicker L/B-1 + C/B-1** (F.W1; + lane-docs O-14/I-2): no `\".\"` export at value.js 4.0.0, `timingFunctions` deleted, **14/22 presets drift analytic→CubicBezier under a CORRECT rewrite** — all re-verified in-repo by this seat. **The one unbanked arm, booked as a rider on that F.W1 ticket** … `easings.ts:58`'s `as EasingFn` cast …"*
F-W2 A2's "Unwrap lock" is this rider, presented as a fold-seat find "*reached by a different mechanism than the CARRY states*"; it is also already gated at `F-W1.md:258` **G12**.

### E-6 · `fr-InfoCard` **FR-IC-20**
`:50`: *"INFO (consumption posture) — AMENDED by ruling R2-2: the bare-root break row now carries its target: **fires at the census-adopted `value 0.13→4.0`** … Reader-B's registry context adopted: **the W.L5 library-band row (timingFunctions deleted; 8/22 curves drift)** and `safeAccentColor` surviving at v4 `./color` ride their banked homes."* The row is on F.W2's own surface and is neither carried nor excluded.

### E-7 · `fr-AnimationControls` **C-18** — the colors.ts-deletion denominator twin of D1
`:120`: *"Zero value.js colour consumption in the tree's most colour-dense transport surface … | **NO-WAVE-OWNER** observation; the colors.ts deletion identity stays with census W.L5."* · `:192`: *"**Census W.L5 item 2**: colors.ts deletion — C-18 scoped out, no claim on this graph."*
Identical shape to `fr-CanvasControlsDock C-25`, which F-W2 books as **D1** and cites at G11. C-18 escapes.

### E-8 · `fr-GalleryView` superlative 5 (`C·C-1`)
`:118`: *"**C·C-1's value.js seam discipline** — traced `lib/colors.ts` to the installed `@mkbabb/value.js` surface and refused to book the cure blind, gating it on the parser-proof R1 oklch crash. The single most action-relevant sentence in the corpus; inverts the census's 'cheapest leg' framing, and the bank already adopted the wave-lock it implies (FR-GFC-8's F.W2 rider)."* G9 carries the substance; the id is never named.

### E-9..E-12 · P-9 export members omitted from D3's roster
The spec's own declared inbound (§3 D3, §6b) is `F-W5.md` — actual export row `F-W5.md:253`:
> `| **F.W5 → F.W2** | exports zero-cells (P-9) | The measured negative roster copies into F.W2's denominator: App API-inert · MobileFloatingToc · **CollapsibleSection** · **PaperSearch/PaperSearchInput/PaperSearchDropdown** · MorphShapePreview (structurally unreachable) · CanvasControlsDock/**AnimationControls** zero migration cost · **UserSlugBar FR-USB-38 — do NOT book into the F.W2 migration surface**. One home (F.W4), two citations |`

F-W2 D3's "eleven" omits, with no exclusion-with-reason:
- **E-9** `fr-CollapsibleSection` zero-cell
- **E-10** `fr-PaperSearch` `C-Z1/C-Z2` (+ `PSM-36/S-11`) — the banked load-bearing-zeros home
- **E-11** `fr-PaperSearchInput` superlative 5 (`C-13/C-14`: *"value.js 0 · keyframes 0 · fourier-API 0, each with its own falsifier"*)
- **E-12** `fr-PaperSearchDropdown` `C:C-23 / C:C-24 / L:§5` (*"Load-bearing zeros … zero value.js/keyframes/canvas/WebGL/API contact"*)
(`fr-AnimationControls` is also named in the export — counted once at E-7.)

Consequence: **G19's "∅ both ways / 26 of 26 / zero silent drops" is falsified** against the very export the spec says it carries whole, and D3's "F.W2 is a citing end and re-books none of them" is true only of the subset the spec happened to list.

---

## 3. NO INVENTION / M-25 DEPTH

**Anti-rename PASSES for the 42 booked rows** — every §3 row and every §8 roster entry traces to a real banked id at a real record; no invented ids found. §8's 26 landing points all resolve.

**FAILS on three counts:**
1. **Re-mint of a banked identity** (E-1/E-2/E-5): the easing/specifier/timingFunctions identity is banked at `fr-EasingPicker L/B-1 + C/B-1` and already carried at F-W1; F-W2 re-books it under census/W.L5 ids only.
2. **Novelty overstatement**: A2's *"Seat-measured surviving surface … All 22 of fourier's `EASING_LABELS` resolve — 0 unresolvable"* is banked verbatim at `fr-EasingPicker:52` (CC-2 ratification). G15 is stamped **⭑ seat-minted 2026-08-28** although the same claim is the **BORN witness of the authority the wave executes**: `library-band.md:295` — *"`fourier-value-import-drift.mjs` 3/3 legs (5 × ERR_PACKAGE_PATH_NOT_EXPORTED; `timingFunctions` deleted; **8/22 curves drift, max 0.192**)"*.
3. **Silent drop of a banked arm**: the **14/22 analytic→CubicBezier** figure (fr-EasingPicker:52, fr-FourierMorphSvg:46, and `F-W1.md:227` **ESC-4** — *"14/22 analytic→CubicBezier; 8/22 material, max Δ 0.192"*) is absent from F-W2. G15 states only "8 of 22 … change SHAPE" and its named eight include `ease-in-cubic`/`ease-in-expo`, while the bank names `ease-out-cubic`/`-expo` among the **eight that stay analytic**.

**Named locks — where their rows land:**

| lock | binds F.W2? | verdict |
|---|---|---|
| `fr-PaperSearchModal` same-commit riders | no (PSM-1 blocker family → F.W3/W4) | n/a; but the PaperSearch **zero-cells** do bind and escape (E-10..E-12) |
| `PAW-44` / `LAW-3` | no (`fr-PaperArticleWindow:220`, sticky-stack sequencing → F.W4/SS-13) | n/a. The record's F.W2-binding row is `K-24`, **carried** (D2, §2b discharge) — correct |
| `MPC-31` one-cut law | no (`fr-MorphPhaseConfig:82`, MPC-3/10/8 conjunction → F.W1/F.W4) | n/a. But **MPC-2 from the same record DOES bind and escapes** (E-4) |
| `FR-MSP-6` two-channel / void-cure lock | no (F.W4 token edit) | n/a. The record's F.W2-binding clause `C·S-2` is **carried verbatim** (D3) — correct |
| `FR-NP-32` corrupt-dist sequencing gate | yes (F-W2 G14/§6b/B11) | **mis-homed** — banked disposition (`fr-NotationPills:35`) is *"NO-WAVE-OWNER (producer dist emitter) + glass-ui BH relay … **F.W1 SEQUENCING GATE**"*; F-W2 states it as "an F.W0 pre-gate" with no note of the re-home (F-W0.md:103 does re-home it, so the fact is defensible; the **citation** is not) |
| F.W3's four named anti-cures | partially (the ramp anti-cures) | **carried** — §7 rows: "one import edge" KILLED (K13), derive-from-`VIZ_COLORS` structurally impossible (K4), tokenisation-is-not-the-cure (RD-5-i), ImageUpload theming arm KILLED (ruling 6). Correct |
| F.W4's derivation-law preamble + NEGATIVE ROSTER | yes (D3) | **partially carried** — the preservation clauses (MorphShapePreview verbatim; FunctionInput posture) are carried; the roster membership is short by four records (E-9..E-12) |

**Dissents CARRIED (not merely cited)** — spot-checks all PASS: fr-ConvergenceLegend C-1 BLOCKER dissent (A1); R-D vs R-L on RD-5 with R-L's inversion argument as the binding cure-shape constraint (B5); LC's BLOCKER on FR-CP-2 (B5); O's bidirectional argument on RD-8 (F1, §7); DU's BLOCKER on fr-App C-2 (E2); MPC/O sequencing dissent on RD-7 (B8).

---

## 4. GATES — born-RED and witness realness

All 19 gates are stated **born RED** (§5 header + every row). No gate is a `scripts/**/proof-*.mjs` — L-19 satisfied in form.

**Real, verified witnesses**: `docs/tranches/V/megatranche/audit/probes/fourier-value-import-drift.mjs` **EXISTS** and genuinely has three legs (`:23` leg 1 bare specifier, `:40` leg 2 `timingFunctions`, `:49` leg 3 curve drift; `:66` prints `${drift.length}/22 … change SHAPE`) — G1/G2/G15's legs are real. `fourier-vizcolor-oklch.mjs` **EXISTS** (G3). `src/subpaths/{easing,math,color,css}.ts` **EXIST** (§2a). `dist/subpaths/css.js` exists and contains `parseStylesheet` (G18). `docs/tranches/V/vnext/API-FACILITY-ISOMORPHISM.json` + schema **EXIST**. `formation/fourier/{CENSUS-2026-08-03.md,lane-frontend.md,lane-docs.md}` **EXIST**. `library-band.md:294-313` is the real §W.L5. `.gitignore:34` is exactly `*.png` (G12's force-add anchor is **correct**). To-be-created paths (`facility-19.md`, `RULINGS-F.W2.md`, the two letters, the four PNGs under `audit/probes/app-wave/`) are each given a path — admissible.

**FAILS — five gate commands cannot execute as written.** §5 preamble: *"Commands are literal and run from `/Users/mkbabb/Programming/value.js` unless rooted."*
`ls /Users/mkbabb/Programming/value.js/fourier-analysis` → **No such file or directory**. Therefore, verbatim:
- **G5** `grep -rn 'hexToRgba(' fourier-analysis/web/src/` — unrunnable
- **G8** `ls fourier-analysis/web/node_modules/@mkbabb/glass-ui/dist/composables/dom/useTokenColor.d.ts` — unrunnable
- **G11** "a colour-VALUE census over `fourier-analysis/web/src/`" — unrunnable
- **G13** `grep -rniE 'megatranche|facility-19|library-band' fourier-analysis/docs/` — unrunnable
- **G17** `grep -rn 'hexToRgb(' fourier-analysis/web/src/ | grep -v hexToRgba` — unrunnable
The sibling repo is at `/Users/mkbabb/Programming/fourier-analysis/` (correctly rooted only in §2a's prose, never in the gate table).

**FAILS — G19's instrument names an authority with no path.** §1/§5/§8 cite "the CARRY" (26 rows, 14 gates) with no file reference; `ls docs/tranches/X/fourier/carry/` → `F-W1-CARRY.md`, `F-W4-CARRY.md` **only**. There is no F-W2 carry ledger in tree, so the G19 set-difference has no resolvable witness and `grep -cE 'carry/F-W|F-W2-CARRY' F-W2.md` → **0**.

**FAILS — phantom line anchor, cited twice.** §3 D3 and §6b assert *"`F-W5.md:234` already EXPORTS these to F.W2 and F.W4"*. `F-W5.md:234` is gate row **G15 (image bounds on write)**. The export lives at `F-W5.md:253`; P-9's home row at `F-W5.md:272`.

---

## 5. E-3 + STATUS — PASS

- `grep -noE 'VERIFIED[^|]{0,40}'` → **one** hit, line 26, inside the §1 verb table as `| VERIFIED | **NO** |`. **Zero VERIFIED stamps.**
- `Status: planned` (`:3`); AUDITED/SPECIFIED YES, IMPLEMENTED NO, VERIFIED NO — correct posture.
- `:7` — "EXECUTION IS NOT AUTHORIZED BY THIS FILE … `/Users/mkbabb/Programming/fourier-analysis` is **READ-ONLY at spec time** … Specifying is not doing."
- No execution verbs in current voice; all fourier acts are future/conditional ("lands", "must", "the wave does"). §2b's measurements are declared read-only and past-tense.
- The spec opens **no** product source for write; value.js `src/subpaths/**` and the 66 records are declared READ ONLY; `scripts/dev/dev.sh` is explicitly never staged (`:71`).
- Every witness keeps the fourier tree read-only (greps, `ls`, node reads); G12's PNGs are force-added into **value.js**'s `audit/probes/app-wave/`, past value.js's own `.gitignore:34`.

---

## 6. POSTURE AXES

- **F.W1 atomicity** — HOLDS in substance: §2a "EXPLICITLY NOT IN BOUNDS" fences `web/package.json`, `App.vue:11,:13-17`, `main.ts`; §6a states FR-EQR-3; §6b cedes G3/G4 to F.W1 and lists **B-2's P0 CSS-class census** ✓. **Incomplete at the ceding end**: the **vaul-vue manifest gate** is never named (`grep -c vaul F-W2.md` → 0; `F-W1.md` → 4), and the **RE-PIN act at the adopted commit hash** appears only as the generic "Re-pin at wave open (SR-1/K-8)" in B6, not as an F.W1 limb. Aggravated by E-1/E-5: two acts F-W1 already owns (`L/B-1`'s `Result`-shape rewrite at `F-W1.md:161`; the `as EasingFn` gate at `F-W1.md:258` G12) are authored here as F.W2's own without a cross-edge — a credit collision under the spec's own FR-GIG-5 bar.
- **F.W0 precedence** — HOLDS: G14 is stated as an opens-after F.W2 never closes; §6a puts F.W0 first; §6b declares G3/G4/G12 cannot execute on a non-compiling build. **Partial on the anchor table**: F.W0 publishes the D-19 anchor table (`F-W0.md:199` G-11: *"ONE corrected anchor table published; every later wave quotes it"*). F-W2 §2b authors its **own** eight-item reconciliation and never declares it as an input to, or a quotation of, F.W0's G-11 table — the two must not diverge.
- **SS-4 owner rulings** — HOLDS: FN-5 is routed to "SS-4's OWNER-GATED G4" with the explicit *"F.W2 answers the letter and names the gate, never pre-empts the ruling"*; §6b repeats it; §7 excludes FN-1/2/3/4/6 on the merits. No ruling presumed.
- **F.W10 SPLIT gate** — not F-W2's axis; no encroachment found.
- **Ruling gates honest** — G7/G8 are declared RULING gates that green on *recording* R-i/R-ii in `RULINGS-F.W2.md`, not on curing. Correct.

---

## 7. DEFECT REGISTER (worst first)

| # | severity | claim |
|---|---|---|
| 1 | **BLOCKER** | G15 ⭑ contradicts the banked drift cell and is falsely stamped seat-minted (8/22 vs banked 14/22 + 8/22-material; named 8 conflict with the bank's "8 stay analytic incl. ease-out-cubic/-expo") |
| 2 | **BLOCKER** | The banked easing/specifier identity `fr-EasingPicker L/B-1 + C/B-1` escapes wholly; A1/A2/G2/G15 re-book it under census/W.L5 ids; already carried at `F-W1.md:161` (credit collision) |
| 3 | **BLOCKER** | G19's "∅ both ways, 26 of 26, zero silent drops" is falsified — 12 routed ids escape, four of them members of the inbound P-9 export the spec claims to carry whole |
| 4 | MAJOR | `fr-MorphPhaseConfig MPC-2`'s "**never a specifier rewrite**" lock is inverted by §2a rows 1–4 without citation or refutation |
| 5 | MAJOR | `fr-EasingCurvePreview CENSUS-MC` ("the value.js-specifier half … is NOT blocked on the uplift") refutes G10's premise; uncited |
| 6 | MAJOR | P-9 roster short by four records vs the declared export (`F-W5.md:253`): CollapsibleSection · PaperSearch · PaperSearchInput · PaperSearchDropdown (+ AnimationControls) |
| 7 | MAJOR | Phantom anchor `F-W5.md:234`, cited twice; the export is `:253`, the home row `:272` |
| 8 | MAJOR | Five gate commands (G5/G8/G11/G13/G17) reference `fourier-analysis/…` relative to a cwd where it does not exist |
| 9 | MAJOR | `fr-AnimationControls C-18` escapes — the exact colors.ts-deletion denominator twin of D1's C-25 |
| 10 | MAJOR | A2's "Unwrap lock" (`easings.ts:58 as EasingFn`) is banked at `fr-FourierMorphSvg:46` and gated at `F-W1.md:258` G12; presented as a fold-seat find |
| 11 | MAJOR | `fr-InfoCard FR-IC-20` escapes (bare-root break row + the W.L5 8/22 library-band cell) |
| 12 | MAJOR | FR-NP-32's banked disposition is "F.W1 SEQUENCING GATE"; F-W2 states "an F.W0 pre-gate" with no re-home note |
| 13 | MINOR | `fr-EasingPicker MISSED-F` escapes (the banked mechanism for two of A1's five sites; F.W1 costing correction) |
| 14 | MINOR | `fr-GalleryView` superlative 5 (`C·C-1`) escapes — substance carried at G9, id never named |
| 15 | MINOR | A2's "all 22 resolve, 0 unresolvable" presented as seat-measured; banked at `fr-EasingPicker:52` (CC-2) |
| 16 | MINOR | "The CARRY" (26 rows / 14 gates) is cited throughout with no path; no F-W2 carry ledger exists in tree — G19's instrument is unresolvable |
| 17 | MINOR | A2/G15's `?? EASING_PRESETS.linear.fn` silent-linear claim overlaps banked `fr-AnimationControls C-17` (→ F.W4), uncited |
| 18 | MINOR | §6b's F.W1 cross-edge omits the vaul-vue manifest gate and the RE-PIN-at-adopted-commit act |
| 19 | MINOR | §2b's anchor reconciliation is not declared as a quotation of / input to F.W0's G-11 anchor table (`F-W0.md:199`) |

---

## 8. LOCAL VERDICT

**DEFECTIVE.** The wave is strong where it was measured — 42 of 54 routed ids booked with live receipts, correct credit-ceding to F.W1 on G3/G4, honest ruling gates, clean E-3/status posture, real probes for its two spine witnesses. It fails on one axis and fails hard: **the easing half of its own act is banked in a cluster of five records the spec never opens** (`fr-EasingPicker`, `fr-EasingCurvePreview`, `fr-MorphPhaseConfig`, `fr-FourierMorphSvg`, `fr-InfoCard`), and the one number it re-mints from that cluster — the curve drift — it states in a form the bank contradicts and that its sibling F.W1 already accepted as **ESC-4**. Add the four missing P-9 zero-cells and five unrunnable gate commands, and G19 cannot close.

*Pass 1 · fresh adversarial · read-only · no file outside this path written.*
