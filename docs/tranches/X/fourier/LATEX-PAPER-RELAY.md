SERVED MODEL: claude-opus-5[1m]

# LATEX-PAPER-RELAY — the latex-paper producer register (X·F)

⟨Created 2026-09-20 by **X.F.W10.d** under the owner's 2026-09-17 begin-word (COHESION §0j).
Authority: `docs/tranches/X/fourier/waves/F-W10.md` §1a row 2 `:27` · **§2.6** `:367–388` ·
§3 **G-F10-5** `:440` + **G-F10-12**'s latex arm `:447` · §4a.6 `:460` · §4a.10 `:464` ·
§4b's **LATEX-PAPER-RELAY** row `:480` · §5 `:508`/`:509`. Wave record:
`docs/tranches/X/execution/C/F-W10.md`. **This register is the SS-6 analogue for the latex-paper
producer** — the receiving surface ≥18 named carries route to and which did not exist. It is
**ASK-ONLY**: it rows, it reconciles, it names a delivery point. **It mints no third channel, and
it re-sends nothing.**⟩

---

## §0 What this register is, and what it is not

| | |
|---|---|
| **IS** | the terminal home of every latex-paper-owned carry the X·F sub-tranche banked; the reconciliation surface against the letters already dispatched; the place PAW-1's F.W1 fourth-package question is **answered in the ledger** |
| **IS NOT** | a letter. No byte here is addressed to the producer. Letters are `docs/tranches/X/coordination/…` and ride the X formation mail seat (see §1) |
| **IS NOT** | a work order against any consumer tree. **Producer rows never become frontend hacks** (standing owner edict; F-W10 §Binding law 2). Every cure below is the *producer's* act |
| **IS NOT** | a second register. COHESION §4a is **glass-only** (SS-6); latex rows live **here and only here** — the one-register invariant G-F10-9 measures |
| **READ-ONLY throughout** | `/Users/mkbabb/Programming/latex-paper/**` and `…/fourier-analysis/web/node_modules/@mkbabb/latex-paper/**` were read and never written. No `node_modules` patch, no pin move, no consumer shim minted to mask a producer defect |

**ANCHOR LAW (SC-3), binding on every row below**: installed **0.2.1** ships no composable source
— ⟨cmd⟩ `ls web/node_modules/@mkbabb/latex-paper/src/vue` → **`theme.css`** and nothing else;
`files` is ⟨cmd⟩ `node -e "…"` → `["dist","grammar","src/vue/theme.css"]`. Every anchor below
therefore cites **`dist/…`** (the bytes the consumer actually runs) **or the record** — a
producer-src coordinate would cite bytes no consumer executes, which is G-F10-5's own falsifier.
**Measured over the roster's own anchor column** (the scope the falsifier speaks about), ⟨cmd⟩
`grep -E '^\| \*\*[0-9]+\*\* \|' … \| awk -F'\|' '{print $5}' \| grep -c 'Programming/latex-paper'`
→ **0**: not one of the fifteen anchors reaches into the producer's working tree. The sole mention
of that tree's `src/` in this file is **this sentence and §5's drift note**, which are prose about
the law, not anchors under it — stated here so the probe and the claim have the same scope, which
is the one shape this programme convicts repeatedly.

**K-9 KILL, restated before the roster so no reader reaches it late**: PAW-1's `:root` triplet-alias
limb is **KILLED**. The literal snippet is **not a prescribed repair anywhere in this register**,
and a repair reasoning from it ships a no-op (it breaks PaperView `:524`, `style.css` `:110-112`,
`:182/:184` and the glass recipes). O-33 confirmed the kill **at the bytes**: there is no alias to
author, and authoring one would invent a dependency in order to patch it.

---

## §1 The delivery point, NAMED (E-7 — the producer tree has no inbox path)

**Measured at this seat, 2026-09-20, read-only:**

| probe | ⟨cmd⟩ | reading |
|---|---|---|
| a coordination dir in the producer tree | `find /Users/mkbabb/Programming/latex-paper/docs -type d -name coordination \| wc -l` | **0** |
| an inbox anywhere in it | `find /Users/mkbabb/Programming/latex-paper -iname 'INBOX.md' -not -path '*/node_modules/*' \| wc -l` | **0** |
| what the tree does carry | `ls /Users/mkbabb/Programming/latex-paper/docs/tranches/` | **`VIRT`** (one tranche dir, no coordination surface under it) |

**E-7 is INTACT at the bytes**: unlike glass-ui (`docs/tranches/BK/coordination/`), keyframes.js
(`docs/tranches/V/coordination/`), the atlas (`docs/tranches/P/coordination/`) and fourier
(`docs/tranches/F/coordination/INBOX.md`, live since F.W0 — COHESION §0k), **latex-paper has no
mail surface at all.** That is why this register lives in *our* tree.

**THE DELIVERY POINT, named in one line and not hedged:**

> **`/Users/mkbabb/Programming/latex-paper/docs/tranches/VIRT/coordination/valuejs-inbound-<YYYY-MM-DD>-<slug>.md`** — the producer's own tranche dir `VIRT/` plus the `coordination/` leaf every other producer in the constellation already carries, with the `valuejs-inbound-…` filename idiom measured from the two producers that use it (keyframes `VALUEJS-INBOUND-2026-09-17-…`, atlas `valuejs-inbound-2026-07-27-…`). **The hop is the X formation mail seat's act, and no other seat's**: sibling trees are READ-ONLY to every wave seat, so the letters are AUTHORED and ROWED here and DELIVERED there by that seat alone.

**Authoring surface (ours, and already used twice)**: `docs/tranches/X/coordination/` —
`fourier-to-latexpaper-2026-09-17-nwo5-latex-relay.md` (**161** lines) and
`value-to-latexpaper-2026-09-DD-fw4-relay.md` (**98** lines), rowed at
`docs/tranches/V/coordination/INBOX.md` as **O-24** and **O-33**. **No third channel is invented by
this register** (F-W10 §5 `:509`): the two paths above are the whole of the correspondence, and
this file is a register, not a courier.

---

## §2 Reconciliation against the TWO DISPATCHED letters — cite them, re-send nothing

The spec's baseline read the register's absence against an INBOX with **0/1** `latex-paper` hits
(F-W10 §3 G-F10-5, §2.8 E-14). **The baseline moved again, and this seat publishes the live
reading rather than the flattering one**: ⟨cmd⟩ `grep -c 'latex-paper'
docs/tranches/V/coordination/INBOX.md` → **4**, of which **two are real relay rows** — the I-29
repo-list mention is not one, and neither is the I-29 body reference.

| id | date | path | what it actually carries (ids enumerated from its own bytes) |
|---|---|---|---|
| **O-24** | 2026-09-17 | `docs/tranches/X/coordination/fourier-to-latexpaper-2026-09-17-nwo5-latex-relay.md` (161 lines) | **NWO-5**, X·F · **F.W1 unit `b`** — G16's second arm. ⟨cmd⟩ `grep -oE '(PAW\|WU\|C)-[A-Za-z0-9]+' \| sort \| uniq -c` → `6 C-06` · `2 WU-S` · `1 PAW-1`. §1 = PAW-1's 42-site ask with the theorem+definition+aside witness · §2 = `fr-PaperView` **C-06** at `dist/vue/composables/useVirtualSectionWindow.d.ts` · §3 = the **katex band** (peer `^0.16` optional vs installed `0.17.0`) · §4 `:131-138` = the four WU-S gaps **carried by row label**: *"**slot-type** · **slug-uniqueness** · **sourceLevel** · **ref**"* |
| **O-33** | 2026-09-18 | `docs/tranches/X/coordination/value-to-latexpaper-2026-09-DD-fw4-relay.md` (98 lines) | **X.F.W4's `/paper` producer relay**, read at the installed 0.2.1. Same probe → `6 PAW-38` · `1` each of `PAW-54 PAW-46 PAW-45 PAW-39 PAW-32 PAW-30 PAW-28 PAW-1 K-9`. §1 = PAW-1/PAW-30 (sheet inert twice over; **K-9 confirmed at the bytes**) · §2 = PAW-38 slug uniquification (**the one row with a schedule consequence**, LAW-5, D5 blocked behind it) · §3 `:52-59` = the **`parentId`** defect, both producers, same shape · §4 = cap-don't-delete: PAW-45/-46 · PAW-28 · PAW-32 · PAW-39 · PAW-54 · §5 = the producer-internal loop census we owe them |

**The reconciliation verb for every row in §4 is one of three, and never a fourth:**

- **DISPATCHED (id spelled)** — the letter names the banked id in its own bytes.
- **DISPATCHED (by row label)** — the letter carries the row under the WU-S label the F.W1 spec
  gave it, id unspelled. This is a real dispatch, recorded honestly as what it is, so that a later
  seat re-reading the letter for the id does not conclude the ask never left.
- **UNDISPATCHED** — no letter carries it in any form. **The cure is the NEXT letter the mail seat
  sends, not a new one minted here** (§8 residual 1).

**The tally, read from §4's settled rows and not asserted ahead of them**: of **15** roster rows,
**9** are DISPATCHED whole, **1** is SPLIT (C-19 ⊕ PAW-33 — the katex half left on O-24, the vite
half never did), and **5** are UNDISPATCHED. By banked id the roster carries **20**, of which
**12** have left in some form and **8** have not. §4 §4a re-states both figures against the table's
own bytes.

⚠ **A row's dispatch is not its discharge.** Every dispatched arm is
**AUTHORED + ROWED**, and the INBOX cells say so in their own words — *"delivery is the X formation
mail seat's act"* (both rows). **Zero latex-paper packets exist at the producer end**: ⟨cmd⟩
`find /Users/mkbabb/Programming/latex-paper/docs -type f -name '*valuejs*'` → **no output**. That
is G-F10-12's latex arm, and §7 reads it RED rather than arguing it green.

---

## §3 PAW-1's F.W1 fourth-package question — **ANSWERED IN THE LEDGER**

**The question, quoted byte-true from the bank** (F-W10 §2.6's PAW-1 cell, *"F.W1 LEDGER RIDER
VERBATIM"*):

> *"the transaction as chartered is INSUFFICIENT for the paper route — latex-paper is a FOURTH
> package the constellation must reconcile — **add the LATEX-PAPER carry to the transaction or seat
> it explicitly**."*

**THE ANSWER: SEATED EXPLICITLY — arm two. The fourth package was NOT added to the transaction.**

Read from what F.W1 *actually ruled*, at F.W1's own gate rather than from this wave's expectation:

| leg | receipt (F.W1's record, `docs/tranches/X/execution/C/F-W1.md`) |
|---|---|
| the gate that poses the disjunction | **G16** *"latex-paper sufficiency"*, born **RED — and its first arm measured UNEXECUTABLE in bounds** |
| why arm one died | the 42 `hsl(var(` declarations are **published producer bytes**: ⟨cmd⟩ `grep -c "hsl(var(" web/node_modules/@mkbabb/latex-paper/src/vue/theme.css` → **42**, `grep -rn "hsl(var(" web/src \| wc -l` → **0**, and `files: ["dist","grammar","src/vue/theme.css"]` makes the file a **shipped artifact, not a stray source** — so an in-place rewrite is a `node_modules` patch, **a HIGH defect under standing law**, not a judgement call |
| the ruling | *"**G16 therefore resolves to arm two, and the named owner is the latex-paper producer**"* — stamped **GREEN on the second arm, owner named**, seated by **NWO-5** whose own header reads `**To**: the **latex-paper** producer` |
| what the transaction stayed | **THREE packages, atomically**: `@mkbabb/glass-ui ^8.0.0` · `@mkbabb/keyframes.js ^6.0.0` · `@mkbabb/value.js ^4.0.0` (verified live at `fourier/web/package.json`). **latex-paper is not in it** and stays at **0.2.1** — ⟨cmd⟩ `npm view @mkbabb/latex-paper@latest` era-reading at F.W1: *"0.2.1 is the latest published version, so no producer fix exists to adopt"* |
| what moved instead | **consumer-side pin truth**, named in F.W1's own commit body and residual 8.2: `katex ^0.17.0 → ^0.16.47` and `vite ^8.0.16 → ^7.3.6`, because *"with either left as it stood, `npm install` cannot resolve at all"*. **Both are reversible the day latex-paper widens** — and that widening is NWO-5's ask, *"not waited on"* |

**Therefore, in the ledger's terms**: the constellation reconciled latex-paper **by seating, not by
uplift**. The seat is **G16 arm 2**; the seat's instrument is **O-24 (NWO-5)**; the seat's owner is
**the latex-paper producer**; the seat's receiving surface is **this register**; and the seat's
delivery point is **§1's path**. **PAW-1's rider is discharged as a question and stays open as an
ask** — the 42 sites are still 42 (§4 row 1), which is exactly what "seated, not cured" means, and
nothing in this answer credits F.W1 with a cure it did not make.

---

## §4 The roster — every §2.6 carry, banked ids kept for life, **nothing re-booked**

**Reading law for this table.** The **carry** column is the banked identity and never changes. The
**ask** column is the *producer's* cure, stated as the bank states it. The **anchor** column cites
`dist/…` or the record (SC-3) and is **verified at the true bytes at this seat, 2026-09-20** —
where a banked coordinate has drifted, the row carries **INTENT at the true bytes** and the drift
is recorded in §5 rather than silently corrected. The **status** column is §2's three-verb
vocabulary. **Every cell's locks travel with its ask**; a reader who takes an ask without its lock
ships the no-op the bank already convicted.

| # | carry (banked ids) | the ask (producer's act) | anchor, verified at this seat | status |
|---|---|---|---|---|
| **1** | **PAW-1** (fr-PaperArticleWindow:42 · BLOCKER, sustained R-2 on **seven** arms) | rewrite all **42** `hsl(var(--x))` declarations to `var(--x)` — **the SOUND limb**. **KILLED (K-9): the `:root` triplet-alias limb — the repair MUST NOT implement the literal snippet.** **PAW-30 lands in the SAME REPAIR.** **LAW-3**: PAW-44's `overflow-x: clip` restoration lands **WITH-or-AFTER** PAW-1 + PAW-30, **never before** (else the K-18-killed see-through-header catastrophe becomes real) | ⟨cmd⟩ `grep -c 'hsl(var(' …/src/vue/theme.css` → **42 · 42** (double-run); `wc -c` → **15764**. The file is a **published** artifact (`files[2]`), which is why an in-place rewrite is the producer's act and a `node_modules` patch is a HIGH defect | **DISPATCHED** — O-24 §1 (id spelled) ⊕ O-33 §1 (id spelled; kill confirmed at the bytes) |
| **2** | **PAW-2** (:48 · MAJOR, demoted BLOCKER→MAJOR at R-1; absorbs D-i-5) | never-painted `.deferred-section` wrappers report the 1200px estimate and `syncMeasuredHeight` writes it **ungated** into the never-invalidated session cache. Cure: gate on the CV-state event, **or** skip when `offsetHeight` equals the estimate, **or** move `.deferred-section` off the measured element. **COUPLING LAW VERBATIM: "the per-patch ref re-read is currently the only repair mechanism — fix this BEFORE memoising the ref (PAW-4)."** K-16: co-landing rhythm cures MEASUREMENT-SAFE only | **K-19's "EXACT repair line" HOLDS at the true bytes**: ⟨cmd⟩ `awk 'NR==869' dist/vue.js` → `if (measuredHeights.get(id) === normalized) return;`; `syncMeasuredHeight` at **:867**, its two call sites at **:885/:892** (`el.offsetHeight`), `SESSION_HEIGHT_CACHE` declared **:790**, written **:871**, read **:822/:941** | **UNDISPATCHED** |
| **3** | **PAW-11** (:57 · premise corrected K-4) | `\ref`/`\eqref` render as plain text **and** are href-less `<a>` (**5 emitters, NOT spans**) — invisible-but-live, unreachable to keyboard and AT. Visuals ride PAW-1; **the focusability arm needs real `href`/tabindex semantics AT THE EMITTER and cannot be shimmed consumer-side** | ⟨cmd⟩ `grep -c '<a class="paper-ref' dist/chunk-A7GY23HR.js` → **5 · 5** (double-run), against `grep -c '<a href='` → **2** (a different pair) — the href-less fact holds at the emitter; the consumer-side selector is `dist/vue.js:457` `selector: ".paper-ref"` | **DISPATCHED (by row label)** — O-24 §4 `:131-138`, the WU-S gap **"ref"**; id unspelled |
| **4** | **PAW-28 ⊕ PAW-45 ⊕ PAW-46** (≡ PaperView **L/D21**) (:79/:195/:196 · fr-PaperView:130 — three module-scoped-cache rows, **ONE letter**) | PAW-28: instance-scope or namespace the height cache (deliberately excluded from teardown). PAW-45: bound or instance-scope the KaTeX memo — **CONSTRAINT TRAVELS WITH THE ASK: the memo makes PAW-3/PAW-4 churn survivable — CAP, DON'T DELETE.** PAW-46: key on tex **+ macro identity**, or move the Map inside `useKatex`. **PAW-46 ≡ L/D21 — ONE mechanism banked twice; carried ONCE, both ids cited, NEITHER re-booked**; L/D21's surviving increment is the docblock promising an `inject` fallback the code lacks | the memo is module-scoped at ⟨cmd⟩ `grep -n 'new Map()' dist/vue.js` → **:387** `var cache = new Map();`, immediately above `function useKatex(macros)` at **:388** — so the key is tex-only while the macros are a parameter, which is PAW-46's mechanism at the bytes. Height cache: **:790** (PAW-28). **45/46 LATENT** at one `useKatex` call site today (record) | **DISPATCHED** — O-33 §4 names **PAW-45 · PAW-46 · PAW-28** (ids spelled), under its own "cap-don't-delete" heading, which is PAW-45's constraint carried, not paraphrased |
| **5** | **PAW-32** (:88) | emit the slot types (`__VLS_Slots`). P-6 member; **NO-WAVE-OWNER locally** — no consumer wave can cure a producer's emitted types | ⟨cmd⟩ `grep -rl '__VLS_Slots' dist` → **2** files (`dist/vue/components/PaperSection.vue.d.ts` · `Theorem.vue.d.ts`); the remaining component d.ts files carry **0** `slots` entries — the gap is the emitted surface, at dist | **DISPATCHED** — O-33 §4 (id spelled) ⊕ O-24 §4's **"slot-type"** label |
| **6** | **PAW-38** (:94) | **uniquify slugs at the emitter.** Joint P-6/P-12 row. **THE ONE ROW WITH A SCHEDULE CONSEQUENCE**: O-33 §2 records `D5` as *posed and blocked* behind it, **LAW-5** requiring PAW-38 FIRST — *"or the deferral renews itself silently at the next wave"* | ⟨cmd⟩ `grep -n 'slugify' dist/chunk-A7GY23HR.js` → **:92** `function slugify(text)`, called at **:773** `const id = slugify(range.title);` — **no uniquifier at either end**; duplicate titles collide by construction | **DISPATCHED** — O-33 §2 (id spelled, 6 occurrences) ⊕ O-24 §4's **"slug-uniqueness"** label |
| **7** | **PAW-39** (:95 · census seat-corrected **SC-2**) | read `sourceLevel`; **restore the second outline level**. **Census law: quote 31, never 35** | ⟨cmd⟩ `grep -n 'sourceLevel' dist/vue.js` → **exactly 1 hit, :1050** — `sourceLevel: section.sourceLevel ?? depth` inside `flattenPaperSections`'s walk. The field **is emitted**; the outline that consumes it collapses to one level, which is why the ask is "read it", not "add it" | **DISPATCHED** — O-33 §4 (id spelled) ⊕ O-24 §4's **"sourceLevel"** label (2 occurrences) |
| **8** | **K-11-FLATTENER-RIDER** (:115, re-graded **K-18** :206 — MAJOR **cure-integrity**, not a defect row) | flat-peers nesting is the **SECOND** of two gates on the two-tier sticky stack; **THE SCROLLPORT IS THE FIRST (PAW-44) — reworking the flattener alone ships a no-op.** Whether the stack returns is a **formation design decision**; SS-13 witnesses first. Bound to **LAW-3** and to **LAW-4 (ONE CLEARANCE AUTHORITY)**: PAW-47's clearance constant is a **DEPENDENT** of the PAW-44 decision (stack restored → clearance ≥ stack height; retired → clear the 32px scrim **and** record theme's three dead `scroll-margin-top` rules retired-with-rationale) | `flattenPaperSections` at ⟨cmd⟩ `grep -n 'flattenPaperSections' dist/vue.js` → **:987** (its source banner), **:1040** (the function), **:1851** (the export); its walk at **:1042** passes `parentId` through correctly — the contrast row 12 depends on | **UNDISPATCHED** — and **correctly so today**: it is a decision-gated rider, not a ripe ask. **K-18 re-grades banked K-11: a repair reading the banked cell alone ships a no-op** |
| **9** | **C-06** (fr-PaperView:70 · MAJOR) | 0.2.1 ships `activeId`/`activeRootId` as `ComputedRef<any>` — **six typed contracts vacuous, invisible to `vue-tsc -b`**. Relay: give the composable a real generic and resolve both refs at **`ref<T \| null>`**. **+ the F.W1 rider** — this row is *why* PAW-1's fourth-package ask was not optional: a consumer cannot type around a producer's `any` without minting the cast the standing edict forbids | ⟨cmd⟩ `awk 'NR>=19 && NR<=20' dist/vue/composables/useVirtualSectionWindow.d.ts` → `activeId: import("vue").ComputedRef<any>;` · `activeRootId: import("vue").ComputedRef<any>;` — **the banked `d.ts:19` anchor holds exactly**, `grep -c` → **2** | **DISPATCHED** — O-24 §2 (id spelled, **6** occurrences), the ask stated as `ref<T\|null>` with **no consumer cast minted** |
| **10** | **L/D5 + L/D30** (fr-PaperView:75 · MAJOR — **one row, two ids**) | two **GUARANTEED no-op teardowns**: `onUnmounted` re-reads a template ref Vue has already nulled. Cure = **`onBeforeUnmount`** (LC's one-word fix, canonical) **or** the cached-element idiom **that ships in the same package at S9**. **The producer twin is RELAYED WITH the consumer half (→ F.W4) so neither end re-introduces the other** | ⟨cmd⟩ `grep -c 'onUnmounted' dist/vue.js` → **10** — the hook family is live throughout the virtualiser, so the fix is a lifecycle swap at named sites, not a rewrite | **UNDISPATCHED** |
| **11** | **L/D8** (fr-PaperView:77 · MAJOR) | `:ref` is re-invoked per patch and **`el.offsetHeight` is evaluated AS THE ARGUMENT, before the dedupe** — one forced layout per visible section per patch, while **the guarded twin sits 300 lines away**. Relay: hoist the read behind the guard at the producer (consumer guard → F.W4). **SAME FAMILY as PAW-4(b) — cite both, book once.** PAW-2's coupling law binds this row too | ⟨cmd⟩ `grep -n 'offsetHeight' dist/vue.js` → **:524** (an unrelated centring read), **:885** `syncMeasuredHeight(id, el.offsetHeight)` and **:892** `if (target) syncMeasuredHeight(id, target.offsetHeight)` — **the read is the argument at both call sites**, and the dedupe it should sit behind is at **:869**. The "300 lines away" guarded twin is that `:869` identity guard, **16** lines above the first call | **UNDISPATCHED** |
| **12** | **L/D29** (fr-PaperView:92 · MINOR) | `useTreeIndex` records the **ROOT** id as `parentId` for depth ≥ 2 — latent, correctly filed; **the same package's other walk gets it right**. Relay: pass the true parent | ⟨cmd⟩ `grep -n 'parentId' dist/vue.js` → **:85** `parentId: depth === 0 ? node.id : parentId` and **:90** `walk(children, depth + 1, depth === 0 ? node.id : parentId, …)`, inside `useTreeIndex` (**:73–119**), masked by `isDescendant` (**:103/:108**). The correct walk is `flattenPaperSections`' at **:1042/:1052** (`parentId,` passed through) — **the contrast is in one file, 950 lines apart**. ⚠ the banked `:27/:32` are **producer-src coordinates**; INTENT preserved at the dist bytes (§5 drift 1) | **DISPATCHED (by mechanism)** — O-33 §3 `:52-59` carries the defect verbatim in both producers' shapes; **the id `L/D29` is unspelled in the letter** |
| **13** | **C-19 ⊕ PAW-33** (fr-PaperView:125 · fr-PaperArticleWindow:89 — **ONE fact, two banked ids**) | declared peers silently violated at the installed pin: **katex `^0.16` vs 0.17.0**; **vite `^6\|\|^7` vs 8.0.16**; all `optional: true`, **zero npm signal**. Relay: **widen both ranges at the producer**. **BOTH CITED, NEITHER RE-BOOKED** (PAW-33 carries the vite half C-19 omits). **DISTINCT from the glass/keyframes/value peer deadlock — never folded.** Companion ★MF-12 (dual katex ambient type sources, skipLibCheck masking) → F.W1's manifest pass | O-24 §3's table, at the producer's own manifest: `"katex": "^0.16"` in `peerDependencies` · `"katex": { "optional": true }` in `peerDependenciesMeta`, against ⟨cmd⟩ `grep -m1 '"version"' web/node_modules/katex/package.json` → `0.17.0`. **`0.17.0` does not satisfy `^0.16`** | **SPLIT — and the split is this register's most consequential honest RED.** **katex half DISPATCHED** (O-24 §3). **vite half UNDISPATCHED**: ⟨cmd⟩ `grep -c vite` → **0** in O-24, and O-33's single `vite` hit is a command path (`grep -rn "theme.css" src/ vite.config.ts`), not the ask. F.W1's residual 6 re-states *both* ranges to NWO-5 **in our record**; the letter's bytes carry only one |
| **14** | **C-20** (fr-PaperView:126; headroom cell excised **K-6**) | `hsl(var(--section-heading))` IACVT at **SIX** theme sites — **a NAMED SUBSET of PAW-1's 42. It DISCHARGES INSIDE PAW-1's carry and is carried separately ONLY so the 6-site cell survives the 42-count quote.** The consumer sibling (PaperSidebar **L-10 ≡ ★MF-10**, fallback-less `var(--section-color-${si})`, cure `var(--section-color-${si % 13}, var(--foreground))` with a root-count assertion) is **F.W4's, not the producer's**. A GLASS-RELAY rider applies **only** if more stops are wanted upstream — the ramp is **exactly saturated 13/13** | ⟨cmd⟩ `grep -c 'hsl(var(--section-heading' src/vue/theme.css` → **6**, inside the same 42 (`grep -c 'hsl(var('` → 42). **The two counts are one population; publishing 42 + 6 as 48 would be the double-count this cell exists to prevent** | **DISPATCHED INSIDE PAW-1** — no separate ask leaves, by design (row 1's letter is its carrier); **K-6 killed both headroom cells** and no repair is inherited from them |
| **15** | **L-11 + M2** (fr-PaperSidebar:40/:62 — **ONE relay carries both**) | **L-11**: add a **PRM gate / motion option** to the damped-rAF follow — a **THIRD ungated clock** (amend `lane-frontend` §8's inventory). **M2 (MAJOR)**: replace the hard-coded `.sidebar-top-btn` in the producer's pointer-exemption with a **DATA-ATTRIBUTE hook**. **HARD LOCK VERBATIM: "F.W4 must not rename the class before the relay lands"** — renaming silently converts scroll-to-top clicks into sticky manual-override suspensions | the follow loop and the exemption both live in `dist/vue.js`'s sidebar surface (the `.paper-ref`/sidebar selector neighbourhood at **:457** and the centring read at **:524**); the class is the producer's literal, which is precisely why a consumer rename is a silent breakage and not a refactor | **UNDISPATCHED** — **and the HARD LOCK is live because of it**: see §6 lock 4 |

### §4a Self-count, read from the settled table above (SELF-COUNT LAW)

⟨cmd⟩ `grep -cE '^\| \*\*[0-9]+\*\* \|' LATEX-PAPER-RELAY.md` → **15 · 15** (double-run, after the
bytes settled) — **15 rows**, numbered 1..15 with no gap and no duplicate.
**The banked ids, published as the probe returns them and then partitioned** — a raw token sweep of
the carry column, ⟨cmd⟩ `grep -E '^\| \*\*[0-9]+\*\* \|' … \| awk -F'\|' '{print $3}' \| grep -oE
'PAW-[0-9]+\|L/D[0-9]+\|C-[0-9]+\|K-11\|L-11\|M2' \| sort -u \| wc -l` → **22**, and the two that
are **not** roster ids are named rather than quietly dropped: **`L/D21`** (cited inside row 4 as
PAW-46's twin — *ONE mechanism banked twice, carried ONCE*, so counting it again would be the
double-book this register forbids) and **`C-2`**, which is **not an id at all** but the probe's own
false positive over row 7's *"census seat-corrected **SC-2**"*. **22 − 2 = 20 roster ids**:
`PAW-1` · `PAW-2` · `PAW-11` · `PAW-28` · `PAW-45` · `PAW-46` · `PAW-32` · `PAW-38` · `PAW-39` ·
`K-11` · `C-06` · `L/D5` · `L/D30` · `L/D8` · `L/D29` · `C-19` · `PAW-33` · `C-20` · `L-11` · `M2`.
**20 ≥ 18**, which is the *"≥18 named carries"* the spec says route here (F-W10 §2.6 head).
**Status tally, measured not asserted** — ⟨cmd⟩ `grep -oE '\| \*\*(UN)?DISPATCHED\|\| \*\*SPLIT'
LATEX-PAPER-RELAY.md \| sort \| uniq -c` → **9 DISPATCHED · 1 SPLIT · 5 UNDISPATCHED**, identical
on both runs (the probe matches only a cell-opening verb, so row 13's mid-cell *"vite half
UNDISPATCHED"* is correctly not counted): DISPATCHED (rows 1,3,4,5,6,7,9,12,14) · SPLIT (row 13) ·
UNDISPATCHED (rows 2,8,10,11,15) — **9+1+5 = 15**, the arithmetic closing against the row
count rather than beside it. **Nothing is re-booked, re-graded or renamed**: every id above appears
under its banked spelling, and the three folds (`PAW-46 ≡ L/D21`, `C-20 ⊂ PAW-1`, `C-19 ⊕ PAW-33`)
are **cited, never double-counted**.

---

## §5 Anchor conformance (SC-3) — verified at the true bytes, with the drift recorded, not corrected

**The §2.6 head's own witness, re-measured here rather than inherited**: ⟨cmd⟩ `ls
web/node_modules/@mkbabb/latex-paper/src/vue` → **`theme.css`**, alone. The installed **0.2.1**
ships **no composable source**, so the virtualiser's bytes exist for a consumer only as `dist/…`.

| anchor the bank states | measured at this seat | verdict |
|---|---|---|
| PAW-2 / K-19 — *"the identity guard at dist `:869` is the EXACT repair line"* | `dist/vue.js:869` → `if (measuredHeights.get(id) === normalized) return;` | **HOLDS exactly** |
| C-06 — `d.ts:19` `ComputedRef<any>` | `dist/vue/composables/useVirtualSectionWindow.d.ts:19` (`activeId`) and `:20` (`activeRootId`) | **HOLDS exactly** |
| PAW-1's **42** · C-20's **6** | `src/vue/theme.css` (a **published** file, `files[2]`, read at the installed path): **42 · 42** and **6** | **HOLDS exactly** |
| PAW-11's **5 emitters, not spans** | `dist/chunk-A7GY23HR.js`: **5 · 5** `<a class="paper-ref` | **HOLDS exactly** |
| PAW-39's `sourceLevel` | `dist/vue.js:1050`, the sole occurrence | **HOLDS** (the field is emitted; the outline is what drops it) |
| **L/D29's `:27/:32`** | those coordinates address **`src/vue/tracking/useTreeIndex.ts`**, a path this package does not ship | ⚠ **DRIFT — producer-src coordinates.** **INTENT PRESERVED AT THE TRUE BYTES**: `dist/vue.js:85` and `:90`, inside `useTreeIndex` `:73–119`. **The banked id is untouched and the finding is unchanged**; only the address moves, and it moves *into* SC-3's lawful space rather than out of it |

**Two figures are deliberately NOT re-derived at this seat**, and saying so is the honest form:
**PAW-39's outline census — quote 31, never 35** (SC-2's seat correction; the corpus census is the
operand and a fresh derivation here would mint a rival number), and **PAW-45/46's *"one `useKatex`
call site today, PaperView `:38`"***, which is a **consumer** coordinate carried from the record.
Neither is an anchor under SC-3; both are citations, and they are labelled as such.

---

## §6 The locks, carried whole — an ask taken without its lock ships the no-op the bank convicted

1. **K-9 KILL** — PAW-1's `:root` triplet-alias limb. Not prescribed anywhere here (§0).
2. **PAW-30 SAME-REPAIR** (F-W10 §4a.6) — PAW-30 lands **in the same repair as PAW-1**, never after.
3. **LAW-3, verbatim** — *"PAW-44 restoration lands **WITH-or-AFTER** PAW-1 + PAW-30, never before."* The disjunction is carried as its bytes: the round-1 tightening to *"only WITH"* was struck at F.W1's §E-3·R2 and is not re-introduced here. Restoring the scrollport **before** the background cure makes the K-18-killed see-through-header catastrophe **real**.
4. **LAW-4 — ONE CLEARANCE AUTHORITY.** PAW-47's clearance constant is a **dependent** of the PAW-44 decision, never an independent constant: stack restored → clearance ≥ stack height; stack retired → clear the 32px scrim **and** retire theme's three dead `scroll-margin-top` rules **with rationale**. Row 8's flattener rider is bound to both this and LAW-3.
5. **L-11 + M2 HARD LOCK, verbatim** — *"F.W4 must not rename the class before the relay lands"* (F-W10 §4a.10). **The lock is LIVE, because row 15 is UNDISPATCHED — and it has been HONOURED**: ⟨cmd⟩ `grep -rn 'sidebar-top-btn' fourier-analysis/web/src` → **4** hits, the class **unchanged** at `PaperSidebar.vue:121/:277/:309` with the lock written into the file's own comment at `:110` (*"⊘ `PS-M2` RELAY-BEFORE-RENAME: the CLASS NAME is unchanged"*), against the producer's hard-coded exemption at `dist/vue.js:602` — `if (target?.closest("[data-toc-id], .sidebar-top-btn")) return;`. **A rename today would silently convert scroll-to-top clicks into sticky manual-override suspensions**, and the lock stays live until the relay lands.
6. **PAW-2's COUPLING LAW, verbatim** — *"the per-patch ref re-read is currently the only repair mechanism — fix this BEFORE memoising the ref (PAW-4)."* It binds row 11 (L/D8) as well as row 2.
7. **PAW-45's CAP, DON'T DELETE** — the constraint travels with the ask, because the memo is what makes PAW-3/PAW-4 churn survivable.
8. **LAW-5 / PAW-38 FIRST** — `D5` is *posed and blocked* behind slug uniquification; O-33 §2 already says why it rides a letter: *"or the deferral renews itself silently at the next wave."*
9. **C-20 DISCHARGES INSIDE PAW-1** — the 6 are a named subset of the 42. **Never 48.**
10. **SC-3** — §5, and no anchor in the roster's anchor column reaches the producer's working tree.

---

## §7 Gate readings — BEFORE → AFTER, and the RED is not argued away

### G-F10-5 · *"The LATEX-PAPER carry has a receiving surface"*

**BEFORE (this wave's baseline, 2026-09-19)**: **RED** — `docs/tranches/X/fourier/LATEX-PAPER-RELAY.md`
**ABSENT**; the spec's own 0-then-1 INBOX reading had moved again to **4**, two of them real relay
rows. **AFTER**: **GREEN**, on all three of the gate's own acceptance conjuncts, each measured:

| conjunct | receipt |
|---|---|
| the register **exists** | ⟨cmd⟩ `ls docs/tranches/X/fourier/LATEX-PAPER-RELAY.md` → present, this seat's CREATE |
| **every §2.6 carry rowed** | **15 rows** against §2.6's **15** table rows, one-to-one, **20 banked ids**, none re-booked (§4a, double-run) |
| **PAW-1's F.W1 question answered in the ledger** | **§3 — SEATED EXPLICITLY (G16 arm 2), not added to the transaction**, read from F.W1's own gate rather than from expectation |
| **with a named delivery point** | **§1**, one path, with the hop's owner named |
| falsifier — a producer-src anchor | ⟨cmd⟩ over the roster's anchor column → **0** |
| falsifier — the K-9 `:root` snippet prescribed | **absent**; the kill is restated at §0 and again at §6 lock 1 |

### G-F10-12 · the **latex arm** only (the gate is SPLIT and this unit turns one arm)

**BEFORE**: **RED** on the latex arm — `latex-paper` → **0** across the sent glass packets.
**AFTER**: **RED, unchanged and honestly so**, re-measured at this seat and double-checked in the
one direction that matters — the arm's own inbox path:

- ⟨cmd⟩ `find glass-ui/docs -name 'valuejs-outbound-*.md' \| wc -l` → **12**; of those, ⟨cmd⟩
  `… -exec grep -l 'latex-paper' {} \;` → **0**. (These are the *glass* packets; a latex receipt was
  never going to live there, and counting them as the arm's denominator is the instrument error
  E-2 corrected for `@source`.)
- ⟨cmd⟩ `find /Users/mkbabb/Programming/latex-paper/docs -type f -name '*valuejs*' \| wc -l` → **0**
  — **the arm's true denominator, and it is zero because the path itself does not exist** (§1).

**The gate's language is *"a packet file exists under the producer's inbox path CONTAINING THAT
ARM'S IDENTIFYING TERM"*, and a receipt is READ, never asserted. There is nothing to read.** This
unit therefore **does not stamp G-F10-12**, and records the arm's state precisely:

> **AUTHORED + ROWED (×2) · UNDELIVERED (×2) · and five roster rows plus one half-row NOT YET
> AUTHORED IN ANY LETTER.** The gate's *"relay-declared-never-sent enters DISEASE-REGISTRY
> regardless"* clause bites on the **delivery** limb, and this register is the structural cure for
> the half it can cure: **the arms are now rowed where a gate can find them, and the delivery point
> is named so the mail seat's act has an address.** The RED belongs to the hop, and the hop is not
> this seat's to make.

---

## §8 Residuals, disclosed rather than discovered later

1. **Five UNDISPATCHED rows and one undispatched half** — **PAW-2** · the **K-11 flattener rider** ·
   **L/D5 + L/D30** · **L/D8** · **L-11 + M2**, plus **C-19 ⊕ PAW-33's vite limb**. Their cure is
   **the NEXT letter the X formation mail seat sends**, carrying them by id. **This register mints
   no letter** — it is ASK-ONLY, and a third channel is exactly what F-W10 §5 `:509` forbids. Two
   of the six carry live consequences: **M2 holds the `.sidebar-top-btn` rename lock shut**, and
   **the vite limb is masked consumer-side** by F.W1's `^8.0.16 → ^7.3.6` conforming, which is
   reversible the day the producer widens and must not be mistaken for a producer fix.
2. **Delivery is owed, not done.** Zero latex-paper packets exist at the producer end. **G-F10-12's
   latex arm stays RED until the hop**, and the close seat (`g`) should read it as such rather than
   as a register defect.
3. **L/D29's anchor drift** (§5) is **recorded, not corrected in the bank** — E-3 governs: the
   dated record keeps its bytes and this register carries the true-byte address beside it.
4. **PAW-39's 31 and PAW-45/46's `PaperView :38`** are carried as citations, not re-derived (§5).
   A successor widening either must say so and re-run, not quietly publish a rival figure.
5. **`PAW-54` rides O-33 but is not a §2.6 carry** — it appears in the letter's §4 cap-don't-delete
   list and is therefore **cited here and NOT rowed**, because rowing it would book an id this
   wave's §2.6 roster does not carry. Named so a later reader does not read its absence as a miss.
6. **The one-register invariant holds**: latex rows live here, glass rows live at COHESION §4a
   (unit `c`'s accretion), and ⟨cmd⟩ over this file finds **no glass arm rowed** and no INBOX row
   minted by this seat.
