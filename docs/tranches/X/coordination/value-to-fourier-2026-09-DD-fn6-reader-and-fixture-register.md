SERVED MODEL: claude-opus-5[1m]

# value.js → fourier-analysis — **FN-6: the fixture register, both directions, and the reader ask**

| field | value |
|---|---|
| **From** | value.js · tranche **X**, sub-tranche **X·F** (fourier-analysis), wave **F.W8** — the CRUD union prototype — unit **e** |
| **To** | fourier-analysis's E13 mail-ledger surface, `fourier-analysis/docs/tranches/F/coordination/INBOX.md` (COHESION **§0k.1**, declared LIVE 2026-09-17) |
| **Date** | 2026-09-19 · sitting of record **2026-09-17** (the owner's begin-word, COHESION **§0j**) |
| **Ledger row** | **O-39**, `docs/tranches/V/coordination/INBOX.md` (append-only; one row, no existing row rewritten) |
| **Authority** | `docs/tranches/X/fourier/waves/F-W8.md` **§3 D1** (FN-6 ⊕ FN-5) · **§4 G14** · **§2a** (this letter's create row, *"the only vehicle by which fourier receives the register"*) |
| **Asks a decision of fourier** | **no.** It asks a **reader**, and names what is already specified so the reader has something to read |

---

## §0 The law this letter travels under — stated first, because it bounds everything below

1. **This letter is the ONLY vehicle.** `F-W8.md` §2a: *"the relay letter (the only vehicle by which
   fourier receives the register)"*. Nothing in this wave reaches fourier by any other path.
2. **ZERO fourier bytes, in any verb, ever.** `/Users/mkbabb/Programming/fourier-analysis/**` is
   **READ-ONLY, ALWAYS** for this wave. Every fourier figure quoted below was taken by **read-only
   inspection** — `grep`, `sed`, `git log` — under the spec's own measurement carve-out, dated and
   attributed at its row. **No file was created, edited, moved or deleted in your tree, and none will
   be by this wave.** This letter lives in *our* tree; delivery is by your seat reading it.
3. **The reader strategy is YOURS.** `F-W8.md` §3 D1, verbatim: *"FN-6's reader strategy stays
   fourier-owned: **value.js neither dictates it nor reads from fourier's tree**"* (dispatch-homes A.1:
   vendor-a-copy is the recommendation, sibling-read is your alternative). §2 below states the ask and
   stops.
4. **Binding by TRANSCRIPTION, never cross-repo read.** The api suite must run where fourier is **not**
   checked out, so each side holds its own in-tree copy; **inv-26** — each side's probe asserts against
   the **document**, never against the sibling. A fixture that reads across the repo boundary is not a
   conformance fixture, it is a coupling.
5. **No fourier wave is graded here, and no canonical row is re-graded.** Findings are routed with
   owners; **FR-GIG-5**: no wave is credited with another's cure, and this one claims none.
6. **`glass-ui` is READ-ONLY, always**; producer-shaped findings ride the BH relay as letters and never
   become frontend hacks. This wave carries **zero** producer rows — declared empty, deliberately.

---

## §1 What is enclosed — paths in the value.js tree, with line counts read from settled bytes

| # | path (`value.js/docs/tranches/X/`) | lines | what it holds |
|---|---|---|---|
| 1 | `fourier/conformance/fixture-register.md` | **923** | **THE REGISTER.** One row per fixture, **both directions**, each naming the **operation model it is generated FROM**: **9** fourier-direction rows (`F8-FIX-F01..F09`) · **8** value-direction rows (`F8-FIX-V01..V08`) · **2** C31 isolation controls (§3a) · the §R/§M/§P7/§D walk-assertion rows (§3b) · **4** J3 strikes · **4** named model-vs-wire bypasses · the **F-PRODRET** port row (§8) |
| 2 | `fourier/conformance/union-prototype-walk.md` | **474** | the five verbs × two object kinds = **10 cells**, each with a disposition **and** an authority class; per verb, **which side cannot answer**; the two divergences stated and **not** harmonised |
| 3 | `fourier/conformance/C31-two-sided-reproduction.md` | **555** | the R6-8/C31 reproduction **attempt**, its honest result, and the `F8-C31A`/`F8-C31B` split specified under the verbatim constraint set |
| 4 | `fourier/conformance/walk/union-walk.mjs` | **1074** | the union walk — **authored, never run**; a run belongs to F.W9/W10's deploy spine and mutates fourier **data** in a non-production environment, never bytes |
| 5 | `fourier/conformance/OWNER-RULINGS-F.W8.md` | **181** | the two rulings SS-4 routes to this wave — one ruled and consumed, one surfaced and unruled |
| 6 | `fourier/waves/F-W8/carry-closure.md` | **418** | G15's citation closure, both directions, run against the frozen census |

**The register is the object of this letter; the other five are the context that makes it readable.**

---

## §2 The FN-6 ask, stated exactly and then stopped

**The ask**: *wire a reader for the fixture register on your side, at a time of your choosing, by a
mechanism of your choosing.*

- **What FN-6 booked**: *"value.js lands the fixture; fourier wires the reader."* The value-side half's
  true state is §3 below — it is not what the booking assumed, and we say so rather than let you find
  out by wiring against it.
- **The strategy is yours.** dispatch-homes A.1 recommends **vendoring a copy**; **sibling-read** is
  your alternative and your call. We express no preference, and a preference expressed here would be a
  dictate under §0.3.
- **Guarded by FN-5** (the contract-currency invariant): a reader is only as good as the currency of
  what it reads, so §4 names exactly what would re-open the twin question.
- **Sequencing rider, carried from the booking**: **BEFORE or WITH fourier M.W10**. We attach no date
  and no gate to it; our gate (**G14**) is discharged by the register covering both directions and by
  this letter being sent and ledgered — **never** by your reply.
- **What is NOT asked**: no schema of ours on your side, no co-signature, no answer by a deadline, no
  change to any fourier wave's plan.

---

## §3 The value-side half — measured, and now RULED one-sided

**A-1, re-measured at our own clock** (⟨cmd⟩ `git log --oneline --all -- api/test/conformance/diff.test.ts`
and `git show --name-status --oneline a8ff7792`): the value-side diff fixture was landed at
**`7351297f`**, merged at **`8a2a617e`**, and **deleted at `a8ff7792`** — *"refactor(T.W1 · api · TA-4):
excise the write-only atom-diff apparatus"* — which deleted `api/src/lib/crud/atomdiff.ts` and
`api/test/conformance/diff.test.ts` (371 lines) in one commit. Reading: **0 extant · 1
landed-then-excised · 0 reciprocated.**

**And the owner has now ruled the consequence, so this is not a gap awaiting repair.** COHESION
**§0j.D**, **F-SS4REST R1**, verbatim:

> **RE-SCOPE value.js out of the diff clause** (a one-sided §6 verdict, stated explicitly) —
> `atomdiff.ts` is wholly excised from value.js and restoring it is value-side authoring that would
> couple value.js's release train to the fourier contract.

**What that means for you, said plainly**: the union's **diff leg is fourier-only**, and its diff
verdict is **one-sided by ruling, not by neglect**. No value-side atom-diff fixture is coming back. The
register's diff rows carry the one-sidedness in their own cells; the walk record declares it at §1; and
this letter says it here so that a reader wired against a two-sided diff correspondence is never
written in the first place. **Every other verb — create · derive · remix · history — is specified in
both directions.**

---

## §4 `canonical_digest` — **SKIPPED-SANCTIONED**, and nothing here re-opens it

Our G14 has a falsifier pointed at exactly this: *"a fixture re-opening `canonical_digest` **FAILS**
this gate."* It is not re-opened, and the register carries no row that touches it.

- **Why it stays skipped**: fourier sorts keys, value.js hand-orders literals; adoption is a
  **stored-hash migration**, not parity. The three named twin divergences stay un-unified **BY DESIGN**.
- **What FN-5 says re-opens the twin question** — named so the boundary is legible from your side:
  `api/src/lib/crud/atomdiff.ts` (excised; see §3) · `api/src/hash.ts` · the `PaletteVersion` shape ·
  the URN catalog.
- **One anchor correction, recorded and not adopted as fact** (D-19): the banked citation
  `models.ts:153` was corrected by the spec's amendment A-3 to `model.ts:84`; at our HEAD this sitting
  ⟨cmd⟩ `grep -n "interface PaletteVersion" api/src/modules/palette/model.ts` → **`:125`**. **The file
  and the interface are right; the line has moved twice.** Cite it by **name**, never by line.

---

## §5 What IS asked of the fourier API row — enumerated, each measured, each with its owner

*Every item is a specification dependency of the register, routed with its evidence. None is a grading
of a fourier wave, and none carries a date from us.*

| # | the finding, as measured read-only | where it bites in the register | owner |
|---|---|---|---|
| **1** | **The flag-identity index.** Your incumbent index is `(content_hash, reporter_slug)` unique (`api/services/database.py:140`), and `dismiss_flags` resolves the entity at `admin.py:603` then discards it at `:607` (`delete_many({"content_hash": …})`) — so **N entities sharing content clear together**. The donor (value.js `POST /:slug/flag`) is already entity-keyed | **§8.4** — the **F-PRODRET** port is specified with the **entity key**. Landing it on the digest key would re-open **§D4** in the act of closing **D3**. The index decision **and its migration** are yours; the port's bytes wait on it | fourier API row (+ F.W5's §D4 clause) |
| **2** | **No `client.method.*` leaf in the successor model**, so neither C31 control can be constructed: the pair needs method-bearing leaves on **both** sides of a join held as a **separate relation** | `C31-two-sided-reproduction.md` §3–§6; both controls are fully specified and **0 of 2 constructed** | fourier API row (the leaf) ⊕ F.W5's register (the relation) |
| **3** | **No production validator at HEAD** — the five in-tree source-level conformance scripts are bare `exit 0` | the C31 isolation predicate is unrunnable until one exists; the constraint *"same production validator"* is verbatim and was not softened | fourier API row |
| **4** | **The `Idempotency-Key` channel is declared and passed by nobody**: `web/src/lib/api.ts:123-124` declares it, `:208-209` applies it, and ⟨cmd⟩ `grep -rn "idempotencyKey:" web/src` → **no output**. The server half already holds at `visualizations.py:236` (create) **and** `:612` (remix) — so the ask is a **client** change, not a server one | the create-leg rows (**G10**): fourier's replay is reachable by an instrument and by **no shipped caller** | fourier API row (+ F.W3/W4 for the call sites) |
| **5** | **The two compute operations declare no field domains at all** (`ComputeEpicyclesRequest` / `ComputeBasesRequest` carry no `Field`), so J4's *generate-the-bounds* rule has nothing to generate | `F8-FIX-F02`/`F03` are born-RED on that limb | fourier API row, under clause **v2 §D7** |
| **6** | **Provenance redaction is entry-row-only** (`_readable_or_none` applied at `:744`), so `slug`/`set_hash`/`author_slug`/`created_at` cross the wire for private and draft ancestors | `F8-FIX-F07` is **sequenced behind v2 §C5**: the fixture may not bake today's shape as conformant | fourier API row (+ F.W5's §C5) |
| **7** | **`GET /api/visualizations/{slug}` is an unsafe read** — `find_one` → `$inc {"views": 1}` → `_public_doc` serialising the **pre**-increment doc — and there is no `viewed_ips` dedup while `liked_ips` exists (**RFC 9110 §9.2.1**) | the walk **declares and subtracts** the perturbation rather than pretending it is absent; the remainder is asserted **zero**, and an unsubtractable remainder is published as a **RED** | fourier API row (+ F.W4 for the ETag path) |
| **8** | **Cache identity under-determines its own operation**: the `extraction_cache_key` literal (`image_storage.py:250-265`) omits `ml_threshold`/`ml_detail_threshold`, and `images.py` short-circuits on the hit **before** `compute_contours` | the derive-leg fixture asserts `contour_hash` **INSTABILITY** across an ML-threshold change. **It is stable today, and that stability is the defect** | fourier API row |
| **9** | **Three envelope fields have a producer and no consumer** — `EpicycleData.trace` (3000 samples/response, cloned into every IndexedDB draft) · `reconstructed_points` (`equations.py:114`,`:128`) · `preview_path` (written `""` at all three sites) | the liveness leg reports the unconsumed set; every field carries **drop-or-consume**, and an **empty traversal is BLOCKED, never GREEN** | fourier API row (drop or consume) |
| **10** | **The `AnimationSettings` unit fork is three-way and 1000×** — `shared.py:67` seconds ⟂ `defaults.ts:25` 5000 ms ⟂ `animation.ts:77` 20000 ms — and `speed` agrees **by luck**, unconstrained on both sides | the round-trip fixture cannot assert anything until the contract fixes the unit; the walk asserts in the **server's declared unit** and says so | F.W5 (units) ⊕ fourier API row |
| **11** | **The authority vocabulary has no declared-owner member.** The port specifies `SESSION-DECLARED` on §C1's *enforced-but-undocumented* reasoning, while value.js's `requireOwnership(...)` mechanism has no registered token | every register row carries an authority class; one class is unnamed in the shared vocabulary | F.W5's `operation-register.md` |
| **12** | **The run contract the instrument needs** (for whenever F.W9/W10 runs it): `UNION_WALK_ENV=nonprod`, two base URLs, tokens **including an admin token** (without which the admin leg is **BLOCKED**, not skipped), and an **ephemeral image** — without one the derive leg is **BLOCKED by the DO-NOT-REGENERATE tripwire** rather than re-pointed at a tracked asset | `walk/union-walk.mjs` refuses to run outside a declared non-production environment, by construction | F.W9/W10 |

**One standing lock, stated because it binds any future regeneration and not just this wave**:
**DO-NOT-REGENERATE canonical geometry on `master`** (`moon.json` / `sun.json`). The walk must not
regenerate it, and does not; any wave that attempts regeneration before the pipeline lands revives the
two BLOCKER positions the registry demoted on that condition.

---

## §6 What value.js does NOT claim in this letter

- **No credit for any cure.** Where a seam we specified reads cured at your HEAD, the cure belongs to
  the wave that landed it and we name that wave from the git record rather than absorbing it.
- **C31 did not reproduce, and we did not dress that up.** The two-sided REJECT could not be produced:
  the client leaf reads **PATCH** at your HEAD against `@router.patch("/{slug}")`, and the instrument
  itself is largely absent. The file states **NON-REPRODUCTION with its receipt**, keeps *cure* and
  *instrument-absence* apart, and claims **no GREEN**.
- **No canonical row is re-graded.** Two record-side readings this wave touched — `fr-PaperSearchDropdown C:C-23`
  and `fr-AdminAuditLog AA-23` — are **routed to the census and to F.W5**, which hold the roster. A wave
  with zero record-side rows may not settle a record-side reading.
- **No producer hack, anywhere.** Producer-shaped findings leave as letters; none was smuggled in as a
  contract or fixture clause.

---

## §7 The reciprocal — measured at our ledger, stated without reproach

`F-W8.md` §3 D1 carries the census's dated reading: *"four unanswered value→fourier letters, zero
inbound rows from fourier in the V INBOX."* **Re-measured at this seat, 2026-09-19** (recorded beside
the banked figure, never over it):

- **Value → fourier**: **5** letters rowed before this one — **O-14 · O-22 · O-27 · O-29 · O-37**. This
  letter is the **sixth**, rowed **O-39**.
- **Fourier → value**: ⟨cmd⟩ over the `Inbound` table's **From** cells across **37** `I-n` rows →
  senders are Codex · atlas · atlas/sci · bbnf-lang · glass · glass-ui · interim · keyframes · owner ·
  self · value. **Zero rows from fourier.**
- **This is not an accusation, and the ledger says why**: your mail surface only became LIVE on
  2026-09-17 (COHESION §0k.1), and at its creation it **triaged the extant letters, O-14 among them,
  before any new letter was logged**. The surface now exists on both ends, which is precisely what makes
  a row possible for the first time.
- **The ask, therefore, is a ROW — not a reply.** One line in your ledger recording that this letter
  arrived and where FN-6's reader is routed discharges the reciprocal completely. **E13's law is that no
  wave closes with UNREAD mail in scope; it is not that every letter earns a letter back.**

---

## §8 Receipts

- **Quote-by-command**: every quoted clause above is the pasted output of a `grep`/`sed`/`git` run by
  this seat this sitting. Fourier-side anchors are this seat's **dated read-only readings** at fourier
  HEAD `21e11b0` (worktree 0 dirty, as measured by units `a`–`d` at their own clocks) and must be
  **re-resolved before citation** by whoever acts on them (**D-19**) — several have already drifted
  since the spec was sealed, and the register records each drift at its row rather than silently
  adopting a new line number.
- **Bounds**: this letter and one append-only `INBOX.md` row are two of unit `e`'s four writable paths.
  **Zero fourier bytes · zero product bytes · zero glass-ui bytes · `scripts/dev/dev.sh` never staged.**
- **Self-count, read from the settled bytes and double-run**: ⟨cmd⟩ `grep -c '^## §'` → **9** sections
  (§0–§8); ⟨cmd⟩ `grep -cE '^\| \*\*[0-9]+\*\* \|'` → **12** ask rows in §5; ⟨cmd⟩ `grep -c '[0-9]%'` →
  **0** — **X-9**: no coverage percentage is published in this letter, in either direction.
