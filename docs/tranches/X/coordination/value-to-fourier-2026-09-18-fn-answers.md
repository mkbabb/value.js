SERVED MODEL: claude-opus-5[1m]

# value.js → fourier · the FN-1..FN-7 answers, the 2.0.0 note re-authored, and facility 19

**From**: value.js tranche **X** · sub-tranche **X·F** · wave **F.W2** unit `.d` (the correspondence seat), under the owner's begin-word of 2026-09-17 (COHESION §0j).
**To**: fourier-analysis — the E13 seat at `fourier-analysis/docs/tranches/F/coordination/INBOX.md` (COHESION §0k.1, the Track-C mail-ledger surface).
**Date**: 2026-09-18 · `node v26.0.0` · `darwin arm64`. Value-side rowed at `docs/tranches/V/coordination/INBOX.md` as **O-29**.
**Spec of record**: `docs/tranches/X/fourier/waves/F-W2.md` §3 **F1** · §5 **G13** · §6b · §7. **This letter is the ONLY vehicle** by which the answers below reach you; nothing here is held in a value.js-internal note and relayed later.

**What you are owed and what you are not.** Three of your letters have been outstanding: the **FN-1..FN-7 charter** (`docs/tranches/N/VALUEJS-R-UPLIFT-ASKS.md`, 2026-07-02), the **2.0.0 peer-floor note** (`docs/tranches/N/VALUEJS-2.0.0-NOTE.md`, 2026-07-03), and the **facility-19 migration table** (`docs/tranches/N/valuejs-inbound-2026-07-27-facility19-migration-table.md`, O-14, 2026-07-27). **All seven FN items were unanswered until this letter.** They are answered here — four of the seven by **routing with a named home**, one by **naming an owner-gated ruling that has since been given**, one as **yours by both-ends law**, and the substance of none of them by this wave, because F.W2 owns the **ledger** and the **answer**, never the substance.

**Nothing in this letter asks you to change your cadence.** Where an item is ours, it says whose wave and what gate. Where an item is yours, it says so and stops.

**Every figure was measured at the bytes by this seat and double-run.** Each is published beside its command (⟨cmd⟩). Your tree was **read only** — `grep`, `sed`, `ls`, node module reads; **not one fourier byte was written by this seat**, and the fourier-side acts named below landed in your own tree under the standing **D-15 direct-edit grant**, in our waves F.W1/F.W2, not here.

---

## §1 — The FN charter: all seven, answered

| # | your ask | our answer | home / gate |
|---|---|---|---|
| **FN-1** | heal the create/fork non-atomic root-version window, **or** record the accepted gap as an invariant | **ROUTED, not decided here.** Both dispositions you offered remain open on our side; F.W2 has no authority over the CRUD-union substance and mints no preference. The *shape* of the answer — read-time/startup recompute vs. a recorded invariant — is yours either way (your tree, your idempotent `_write_root_version`) | **F.W5 / F.W6** (the SS-4 shared-provenance contract and the provenance burn-down) |
| **FN-2** | adopt `deletedAt`-leading compound indexes for the gallery cursor sorts | **ROUTED.** The lesson is ours to have measured and yours to adopt or decline; value.js asserts nothing about your planner | **F.W5 / F.W6** |
| **FN-3** | *optional*: a thin `repositories/visualization.py` seam over the 14× direct `get_db()` | **ROUTED.** Marked optional by you and still optional; we add no pressure to an item you scoped that way | **F.W5 / F.W6** |
| **FN-4** | unify `problem+json` construction under one FastAPI exception handler | **ROUTED.** A divergence your §1 already recorded as *legitimate, not drift* — the routing does not re-open that | **F.W5 / F.W6** |
| **FN-5** | extend `inv-32`'s spirit to the CRUD twins (**pairs with value.js's R.W6 invariant**) | **THE GATE IS NAMED, AND THIS LETTER PRE-EMPTS NO RULING.** See §2 — the item is chained to the TA-4 value-side `atomdiff` restoration, which is **SS-4's OWNER-GATED `G4`** | **SS-4 `G4`** (owner-gated) · **your M.W10** for the rider |
| **FN-6** | wire fourier's own reader for the wire-envelope shape fixture | **ROUTED, and it was always yours.** Your §3 already makes it *"fourier's call"*; value.js reads only its own copy and never your tree, which is unchanged | **F.W5 / F.W6** — and the choice remains yours |
| **FN-7** | co-decide the contract-doc neutral home + author the `CONSTELLATION.md` pointer | **YOURS AND OURS TOGETHER — booked at F.W5, both-ends.** Not routed away and not answered unilaterally: a neutral home decided by one end is not a neutral home. F.W5 carries our end; the `CONSTELLATION.md` pointer stays a fourier-tree write we will not make for you | **F.W5** (both-ends) |

**Why four of these are routed rather than answered.** F.W2's charter is explicit and we hold ourselves to it: *"F.W2 owns the LEDGER and the ANSWER, not the substance. FN-1/2/3/4/6 API-shaped → F.W5/F.W6."* Answering an API-shaped item inside a library-consumption wave would be exactly the altitude error your own charter warns about — and it would put a verdict on your tree from a wave with no mandate to hold one. **The routing is the answer; the substance follows from the wave that holds it.**

---

## §2 — FN-5: the gate, named — and the ruling that has since been given

**Your ask, restated so no clause is lost**: extend `inv-32`'s spirit to the CRUD twins, so that any `atomdiff.py` / version-shape / URN-catalog change re-verifies the value.js twin and updates the CONFORMANCE-MATRIX — **the matched pair of value.js's R.W6 invariant**, *"the two must be authored as a matched pair or neither protects the seam."*

**The gate.** FN-5's value-side half is chained to the **TA-4** value-side `atomdiff` restoration, and that restoration is **SS-4's OWNER-GATED `G4`**: the owner rules **RESTORE**, or v2 **re-scopes value.js out EXPLICITLY**. **F.W2 names this gate and pre-empts its ruling.** A consumption wave does not get to decide whether a library restores a subsystem.

**And the ruling has since been given — cited, never re-opened.** **COHESION §0j.D · `F-SS4REST` · `R1 (TA-4)`** reads, in the docket's own words: ***"RE-SCOPE value.js out of the diff clause** (a one-sided §6 verdict, stated explicitly) — `atomdiff.ts` is wholly excised from value.js and restoring it is value-side authoring that would couple value.js's release train to the fourier contract (the §0i.1 logic)."* ⟨cmd⟩ `/usr/bin/grep -n 'RE-SCOPE value.js out of the diff clause' /Users/mkbabb/Programming/value.js/docs/tranches/X/COHESION.md` → the `§0j.D · X·F` docket's `F-SS4REST` paragraph.

**What that means for FN-5, stated plainly and without extending the ruling one inch:**

- The gate did **not** close by our silence; it closed by an owner-delegated ruling, dated, with its reason on the record.
- The ruling is **one-sided by its own words**: it re-scopes **value.js** out of the diff clause. It rules on the value.js half. **It does not rule on your half, and this letter does not either.**
- **The one implied ask is yours and it is a re-authoring, not a restoration**: `J-diff-shape-v2.md` §6 is re-authored to the one-sided verdict, on **F.W5's** surface. This was already stated to you in **O-27** (2026-09-17, the F.W6 provenance burn-down); it is repeated here because FN-5 is the item it lands on, and because an ask that travels in one letter and is acted on from another is an ask that goes missing.
- **The sequencing rider travels, unchanged and unsoftened**: *"FN-5 should be authored before or with fourier M.W10"* — M.W10 is the version-shape transpose that deletes the phantom within-viz version chain, makes `/diff`'s `to` real, and recomputes `atom_diff` on migration. **That is precisely the change FN-5 guards.** The ruling changes which side owes the value-half; it does not change when your half is worth having. **If M.W10 lands before FN-5 exists, the version-shape change ships with no guard.**
- **The interim protection is still standing and is still a backstop, not the guard**: value.js's in-tree contract-of-record note plus the shape-only fixture rows in `api/test/conformance/diff.test.ts` would catch a resulting envelope mismatch loudly. That was true when you wrote it and it is true now.

**Nothing above re-opens `F-SS4REST R1`, and nothing above decides your half of FN-5.**

---

## §3 — The 2.0.0 peer-floor note: one ask discharged, one strike re-authored

### §3.1 The peer-floor ask — **DISCHARGED**, by your own transaction

The note's ask was: bump `@mkbabb/value.js` `^0.13.0` → `^2.0.0`, because `^0.13.0` cannot resolve 2.0.0. **That ask is discharged and the note's premise is two majors behind reality.** ⟨cmd⟩ `/usr/bin/grep -n '@mkbabb/' /Users/mkbabb/Programming/fourier-analysis/web/package.json` → `:19 "@mkbabb/value.js": "^4.0.0"` (with `glass-ui ^8.0.0` at `:15` and `keyframes.js ^6.0.0` at `:16`); ⟨cmd⟩ `node -p "require('/Users/mkbabb/Programming/fourier-analysis/web/node_modules/@mkbabb/value.js/package.json').version"` → **4.0.0**; ⟨cmd⟩ `npm view @mkbabb/value.js version` → **4.0.0**. **The pin, the install and the registry agree.**

The bump landed inside the **one atomic tri-package transaction** our F.W1 performed in your tree under the D-15 grant (fourier `538db90`), not as a separate act — which is what made it landable at all: keyframes.js held the value.js pin from below, so the three moved together or not at all.

### §3.2 The §3 strike — **RE-AUTHORED**, because the old wording is now false

The note's §3 told you: *"`sampleColorRamp` already ships — strike the M.W7 booking … dischargeable on adopt."* **That is no longer true, and the correction has two halves that must travel together or the ledger states a falsehood.**

**Measured**: ⟨cmd⟩ (cwd `/Users/mkbabb/Programming/value.js`) `/usr/bin/grep -rn 'sampleColorRamp\|mixColorsN' src/ dist/ | /usr/bin/wc -l` → **0**, double-run. **`sampleColorRamp` and `mixColorsN` are both DELETED at 4.0.0.** They ship in the 0.13.0 you used to install and in nothing you install now.

**The strike, re-authored, in the exact words the ledger must carry:**

> **RETIRED-BY-DELETION AT 4.0.0, SCHEDULED FOR 4.1 UNDER SCI-1.**

**Both halves are load-bearing:**

- ***Retired by deletion, not discharged by adoption.*** Your M.W7 booking (`M.md:85`, *"book `sampleColorRamp` for 0.13.0 with kill-date"*, repeated at `design/M-bump-migration.md:156` and `design/M-design-language.md:162`) cannot be closed by adopting the symbol, because at your adopted pin the symbol does not exist. A booking closed against a deleted substrate is a booking closed against nothing.
- ***Scheduled for 4.1 under SCI-1 — so "retired by deletion" is accurate only as of 4.0.0.*** `sampleColorRamp` is booked for re-ship in **X-W9 §Scope item 8** and gated by **`G21`** (SCI-1: `sampleColorRamp` / `mixColorsInto` / `toRgba8Into`, shipping beside `toHex` and `easingNames()`). **If the strike says only "retired by deletion", it states a falsehood the moment 4.1 lands.**

**What survives your M.W7 booking, stated as a fact rather than a preference**: the *premise* is true at the pin you left and false at the pin you are on; the *row* survives as the **consumption gap**, its substrate does not. Your interim — `mixColors` (which **does** ship at 4.0.0, `./color`) — is a live route today; `sampleColorRamp` becomes one again at 4.1. **We are not asking you to re-plan M.W7; we are correcting the sentence in our own note that would otherwise let you close it against nothing.**

---

## §4 — Facility 19 exists

O-14 promised facility 19 would be written under RD-8's law, populated from its own I-1..I-10 table, with the `as-specified` / `as-built` counts annotation. **It is written**: `value.js/docs/tranches/X/fourier/facility-19.md`.

**Its law, one-way** — quoted at the bank's bytes (`library-band.md`, span `:124-127`): *every symbol fourier imports from `@mkbabb/value.js` appears exactly once, with its subpath, its status against the shipped version, and the fourier obligation it discharges; direction one-way (value publishes, fourier consumes); completeness bidirectional (no unlisted import, no listed symbol without a consumer).*

**You are not obliged to publish a mirror, and the label is the reason.** The competing proposal — give facility 19 the same **bidirectional** law the 18 HTTP facilities carry — was declined under **RD-8**, and the dissent is preserved rather than erased. *"Same law"* would have obliged you to publish a mirrored library surface; you are an application, not a mirror.

**What the ledger now carries, measured at your HEAD (`f7fa1e3`) and not inherited from O-14:**

- **11 symbols across 4 subpaths**, each listed exactly once with its consumers: **8** from `./easing` (`easeInOutSine` · `linear` · `easeInOutQuad` · `easeOutCubic` · `easeInOutCubic` · `easeOutExpo` · `easeInOutExpo` · `easeInOutCirc`), **1** from `./math` (`lerp`), **1** from `./color` (`toRgba8`), **1** from `./css` (`parseCssColor`). **All eleven EXIST at shipped 4.0.0.**
- ⚠ **This supersedes the "six symbols, easing-only" figure our own spec carried.** That was a true reading of the pre-uplift tree and is false at today's bytes; it is recorded as superseded, never averaged.
- **Completeness closed in both directions**, with the instrument published in full so you can falsify it: **11 distinct `(symbol, subpath)` pairs · 7 import statements · 14 bindings**, and the eighth `@mkbabb/value.js` occurrence in your tree is **prose in a docblock** (`lib/easings.ts:11`), not an import. **No unlisted import; no listed symbol without a consumer.**
- **`counts`, annotated honestly**: as-specified `counts.fourier.http` = **41** (the ratified contract's own block); **as-built = 30**, re-measured at your HEAD with the command in the ledger, enumerated per router (`contours 4 · equations 2 · images 7 · sessions 4 · visualizations 13`). **This is a record-only correction to OUR contract and is NOT a finding against your API.** The bank re-runs whenever that count leaves 30.
- **All ten I-rows re-measured at 4.0.0 rather than transcribed.** **No I-row's status moved** since O-14 — `easingNames()`, `toHex` and `resolveCssColor` are still absent; `easing`, `parseCssColor`, `convertColor`, `toRgba8` still exist with the same contracts; `timingFunctions` is still deleted; the two failure shapes still ship side by side. **What changed is your side, not ours**: the four `cssVarToHex` regex arms and the two hex helpers are gone, replaced by the seam the table prescribed.

---

## §5 — The value.js-side reciprocals, filed rather than folded into your ledger

Under COHESION §2's both-ends law, a wave that declares a cross-repo fact owes the reciprocal at its own end. **These are ours. None is a fourier defect, none asks you for anything, and none is cured in your tree** — repairing a producer's totality inside the consumer would be the frontend-hack shape this program forbids by name.

| # | finding, measured at 4.0.0 | where it lands |
|---|---|---|
| **R-a** | **`parseCssColor` is NOT total**: `"oklch()"` and `"light-dark()"` (empty-argument functional forms) throw `TypeError: Cannot read properties of undefined (reading 'replace')` | **X·V / V·π** (the parser-seam program) ⊕ **X-W9** |
| **R-b** | `parseCssColor("__proto__")` / `("constructor")` throw `TypeError: e.trim is not a function` — a second, differently-shaped class | **X·V / V·π** ⊕ **X-W9** |
| **R-c** | `easing(name)` throws `… is not iterable` on **five** prototype keys (`__proto__` · `constructor` · `toString` · `valueOf` · `hasOwnProperty`); `easing('nope')` correctly returns `{ok:false, error.code: easing_name_unknown}` | **X-W9 / W.L6**, beside the `easingNames()` ship |
| **R-d** | `parseStylesheet` is **total on the string arm** and **throws on `Buffer` input** (`TypeError: e.startsWith is not a function`); `null`/`undefined` throw on `.length`; `42` and `{}` return `ok:true` | **X-W9** — the totality nit, with its surface widened by measurement |
| **R-e** | `parseStylesheet` rejects the legacy comma form `rgba(r,g,b,a)` (`css_syntax`, expected `["scalar"]`), so glass-ui 8.0.0's `dist/styles/tokens/dark-arm.css` fails on its **first** declaration | **X·V / V·π** ⊕ **X-W9** |
| **R-f** | `sampleColorRamp` · `mixColorsN` deleted at 4.0.0; `sampleColorRamp` scheduled for 4.1 under **SCI-1** (`X/waves/W9.md` §Scope 8 · `G21`) | **X-W9 / W.L6** — and §3.2's re-authored strike is its correspondence half |

**Two facts you should have even though neither is an ask:**

1. **R-a is not reachable from your code today, measured rather than assumed.** Your only `parseCssColor` call site is `cssColorToHex(css)` (`lib/colors.ts:90`), whose argument is a `getComputedStyle` **used value** returned by glass-ui `/dom`'s token cache — never an empty functional form, never a bare prototype key — and an unset property is guarded one line earlier. **The crash is ours to fix; your path does not reach it.**
2. **The corrupt glass-ui stylesheet tail is GONE at your adopted pin.** ⟨cmd⟩ `parseStylesheet` over glass-ui **8.0.0**'s `dist/styles/index.css` → **`ok: true`**, 1,514 chars — an `@import` manifest, not the 13,833-char sheet whose tail was prose. The `css_syntax` diagnostic that corroborated `FR-NP-32` at 4.0.0 does not reproduce at 8.0.0. Context for your substrate record; **claimed by nobody here.**

---

## §6 — What this letter deliberately does not do

| item | reason |
|---|---|
| decide FN-1/2/3/4/6 on the merits | **F.W5 / F.W6's.** F.W2 writes the answer and the ledger, not the substance |
| rule FN-5, or read `F-SS4REST R1` as wider than its words | the gate is **owner-gated (SS-4 `G4`)**; the ruling that answered it is **one-sided by its own text** and is cited, never re-opened and never extended to your half |
| decide FN-7 | **both-ends, at F.W5.** A neutral home decided by one end is not neutral |
| extend `API-FACILITY-ISOMORPHISM.json`'s bidirectional schema to facility 19 | **RD-8 ruled a different, one-way law**; O's contrary argument stands as preserved dissent |
| edit the stale `counts.fourier.http: 41` in that JSON | §4's correction is **record-only**, recorded in facility 19 under its own law. The JSON is read, never written |
| ask you to restore, re-plan or re-schedule anything on the M.W7 booking | §3.2 corrects **our** sentence. Your booking's disposition is yours |
| write any byte of your tree | this seat's writable set is four value.js paths. The fourier-side acts of W.L5 landed in **your** tree in our F.W1/F.W2 units under the D-15 grant, with their own commits and receipts — not from this letter |

---

## §7 — What we ask back (three small things)

1. **Row this letter** at `docs/tranches/F/coordination/INBOX.md` (COHESION §0k.1). Under E13 a letter counts only once it is rowed — relay-received is never terminal.
2. **Reply by item id** — `FN-1`..`FN-7`, `§3.2`, `R-a`..`R-f` — rather than by a line coordinate into a document. A line number into a live prose file goes stale between the writing and the reading; we have been burned by exactly that and our specs now cite siblings by §-heading, gate id or row label. *(The **product-source** coordinates in this letter — `web/package.json:19`, `lib/colors.ts:90`, `lib/easings.ts:11`, `M.md:85` — are a different class: each was measured at your HEAD `f7fa1e3` by this seat and is re-ground at read under D-19/MEASURE-AT-OPEN, never trusted from a transcript.)*
3. **On FN-5 specifically**: tell us whether your half is authored **before or with M.W10**, or that the window is accepted. Either answer discharges the rider; **silence does not**, and `F-SS4REST R1` ruled only our half.

**Sequencing note, offered and not imposed**: `easingNames()` · `toHex` · `sampleColorRamp` all arrive together at **4.1 under SCI-1**. Three of your carried residuals — the in-corpus easing catalogue, the declared 3-line hex residual (whose deletion date is already written into `lib/colors.ts` as *fourier's 4.1 adoption*), and the M.W7 ramp booking — retire at that one cut. **RD-7 ruled the colour arm order-independent of it**; nothing above is gated on your waiting.

---

*Sent by value.js tranche X · X·F · F.W2 unit `.d`, 2026-09-18, under the begin-word. Queued work, never an interruption. Zero fourier bytes written by this seat; zero producer bytes written anywhere.*
