SERVED MODEL: claude-opus-5-5

# KF.W13X.x — A2-KE-X-n rows (minted 2026-09-24, KF-W13.md addendum (b))

Instrument: private `vite --force` dev server on :5194 with its own dep cache (scratchpad), serving glass-ui 10.1.0 (`actionsWhen` present in the served `@mkbabb_glass-ui.js` prebundle). kf `574642be`. Headed Chromium, DPR 2, `isMobile`+`hasTouch` below 1024. Widths 360x780 · 390x844 · 430x932 · 844x390 · 768x1024 · 1024x768, light + dark. Every figure below reproduces in two runs (`views-*-run1.log` vs `views-run2.log`, `timeline-1024-run{1,2}.log`, `overlays-*-run1.log` vs `overlays-run2.log`). Frames: `frames/<view>-<vp>-<theme>.jpg`.

Each row is routed to the family unit that owns its file (Unit plan G1–G21).

| id | view | finding (measured) | widths × themes | route |
|---|---|---|---|---|
| A2-KE-X-1 | expanded timeline | At 1024x768 the expanded timeline card sits at x −289..77, so 289 of its 366 px are off-screen to the left. The position is the same 1.1 s and 4.1 s after the press, so the card is at rest, not mid-transition. At 1440x900 the card sits at 60..501. | 1024x768 · both | `.timeline` |
| A2-KE-X-2 | expanded timeline | The expanded card's preview cube is not clipped to its preview box. It paints, blurred, over the 0–100% ruler and the track. At 1440 it stays inside the box. | 360/390/430/768x1024 · both | `.timeline` |
| A2-KE-X-3 | expanded timeline | At 844x390 the expanded card shows only its 130 px toolbar strip. The ruler, track and empty-state copy are under the transport sheet (docSH 594/390). At 390 the empty-state line is cut by the sheet. | 844x390, 360–430 · both | `.timeline` |
| A2-KE-X-4 | KeyframesAddDialog | It cannot be reached at runtime. Its only mount is `KeyframesEditor.vue:203`, and no demo file imports `KeyframesEditor` (⟨`git grep -n KeyframesEditor -- demo`⟩: only its own composable is imported elsewhere). Nothing to read at any width. | all | `.keyframes` (same identity as KFE-ORPHAN) |
| A2-KE-X-5 | CSS paste dialog (Add / Import) | At 844x390 the dialog is 358 px tall in a 390 px viewport and scrolls, so the primary Add action is below the fold inside the dialog. In light the description line is low-contrast. At phone portrait it fits (gutters 20/20). | 844x390 · both (contrast: light) | `.timeline` (`CSSPasteDialog.vue` is in `timeline/**`) |
| A2-KE-X-6 | timing-function detail editor | The editor opens in the sheet's peek. At 360/390/430 the Bezier/Steps plot is cut by the viewport bottom (docSH 1148/844 at 390). At 844x390 the editor is entirely below the fold. tinyText is 3 at phone and at 1024, and 1 at 844/768. | phone + 844 · both | `.controls` |
| A2-KE-X-7 | Matrix Controls facet | The facet's cell row sits on the sheet's bottom edge, so only the first row of four circles is visible, cut. The cell labels (Sx/y/z/w) overlap the values. smallTargets is 6 at every width below 1024 and 9 at 1024. | all · both | `.matrix` |
| A2-KE-X-8 | spring discrete Entry (`StartingStyleTarget.vue`) | The "Hello, spring." discrete card overlaps the header's "emitted by springLinearStops()" line, and at 844x390 it covers the `@starting-style` header card. The ζ/ms meta column wraps to four lines. With Controls open, docSH is 1367/844 at 390 and smallTargets is 6. | 360/390/430/844 · both | `.springd` |
| A2-KE-X-9 | toasts | At 360/390/430 the toast sits at top 16 px and is 82.6 px tall, so it covers the top dock (y 31–91). Its close button hangs outside the toast's top-left corner. pastEdge is 4, against 1 on every other view. At 844x390, 768 and 1024 the bottom-right toast lies over the transport, with its close button on Play. | all · both | `.home` (critic gap: toasts) |
| A2-KE-X-10 | scene-loading skeleton (`App.skeleton.vue`) | In dark the skeleton block is near-invisible against the page. It is visible in light. docSH equals innerHeight during load. | all · dark | `.home` |
| A2-KE-X-11 | Share popover (overlay, every scene) | The popover opens flush to the viewport's left edge (gutL 0, 288 px wide) while the @mbabb menu is still open beneath it. The URL input placeholder truncates ("Paste share UR"). | 360/390/430/844 on all 7 routes; 768 on home · both | `.overlays` |
| A2-KE-X-12 | @mbabb menu (overlay) | On the home route the menu opens flush left (gutL 0 at 360 and 390). On every scene route it opens at x 82.5. | 360/390 home · both | `.dock` |
| A2-KE-X-13 | @mbabb menu + scene Select (overlays, landscape) | At 844x390 the menu is 234 px tall and scrolls, with 3 items past the viewport, on all 7 routes. The scene listbox has 1 option clipped on 5 routes. | 844x390 · both | `.dock` |

Overlays not flagged (every scene, every width, both themes): the keyboard-shortcuts dialog, the Clear-all confirm, and the transport channel Select where one exists. The channel Select exists on cube, amiga, spring and home. It is ABSENT on square, easing and sequence (1 channel), which is 18 = 3 routes × 6 widths per theme; those are not defects.

## Gates banked BEFORE (no product byte)

- **A2-KE-L2-2 consumer gate, `document.scrollingElement.scrollHeight == innerHeight`** (`docsh.mjs`, runs 1 and 2 byte-identical): 84 cells (7 routes × 6 widths × 2 themes). At rest **60 RED / 24 GREEN**, and with the first surface open **60 RED**. GREEN = home at every width, plus every scene at 1024x768. RED = every scene route below 1024, in both themes. The excess at rest is 171 px at 360/390/430, 204 px at 844x390 and 172 px at 768x1024; it grows with the open surface's in-flow sheet (sheet `position: relative`: at 390 the sheet is at top 716, height 304 → docSH 1148; easing/spring/sequence height 523 → 1367). Runs 1 and 2 hit the pressed-by-default Controls item at 1024, so their 1024 "open" cells measured a toggled-off pane. The fixed probe re-ran 1024x768 twice (`docsh-run3/4.log`): 14 cells, 0 RED.
- **A2-KE-L2-15 safe-area** (`safearea.mjs`, CDP `Emulation.setSafeAreaInsetsOverride`, runs 1 and 2 identical): the viewport meta is `width=device-width, initial-scale=1.0` with no `viewport-fit=cover`. Under the override, Chromium resolves `env()` to the injected insets anyway (p390 47/0/34/0; l844 0/47/21/47), and the top dock moves 31 → 49 px, so the `env()` terms are live in emulation. On iOS without `viewport-fit=cover` they read 0, which is the source finding A2-KE-L2-15 rests on. Controls in an inset band:
  - p390 (top 47, bottom 34): 1 control on cube, amiga, square, easing and sequence, all below the fold. This is the in-flow sheet, the same excess as the L2-2 gate.
  - l844 (left/right 47, bottom 21): 4 on cube, amiga and square; 8 on easing; 2 on spring; 3 on sequence; 1 on home. The sheet's "Sheet position" handle spans 21..823 and its Close button spans 779..823, so both sit in the 47 px notch bands.
  - p390 with no inset: 0 on every route.

## Evidence retention
- Committed: `frames/` holds every view frame (`v*`, 12 configs × 9 views plus the settle/probe frames), every safe-area frame (`sa-*`, 21), and the overlay frames behind A2-KE-X-11/-12/-13 (72). The other 162 overlay frames were unflagged (shortcuts, Clear-all, channel Select, and scene/menu at widths that passed). They stay local in the seat's scratchpad; their metrics are in `overlays-*-run1.json` and `overlays-run2.json`.
- Overlays: 504 states per run (7 routes × 6 overlays × 6 widths × 2 themes), 36 ABSENT (single-channel routes) and 86 flagged. Run 1 vs run 2: 0 differences.
- Probes, each committed and READ-ONLY: `discover.mjs` · `docsh.mjs` · `safearea.mjs` · `views.mjs` · `overlays.mjs` · `timeline-1024.mjs` · `timing-1024.mjs`. `timing-1024.mjs` shows why the first v4 reading at 1024 errored: Controls is pressed by default at ≥1024, so the probe's click toggled the pane off. That was a probe defect, not a product one. `views.mjs` and `docsh.mjs` now open a surface only when it is not already pressed.
