SERVED MODEL: claude-opus-5[1m]

# AFTER witness — HANDED BACK to KF.W9 / SS-13, by row id

**From**: X.KF.W13 `.c` (repair round 1), 2026-09-20.
**To, by row id**: **KF.W9 · `G-KFW9-9`** (the before/after pair) and **KF.W9 · `D-3 + DU-M-1`**;
the forced-colors capture itself is **SS-13's one shared shot** (spec `KF-W13.md:57`, *"named not
taken here"*).
**Why this file exists**: KF-W9 `:238` holds the AFTER witness open — *"(delete, assert the Highlight
outline returns) is the packet's outcome, **re-shot here once KF.W13's act lands** (S-9 discipline);
until then it reads UNMEASURED, never inherited."* **The act has landed. The condition is met.**
This seat does not shoot it, does not proxy it and does not mark the gate green from this end
(S-9): it hands over the sha, the bytes and the exact assertion the shot must now carry.

## 1 · The act

| | |
|---|---|
| sha | keyframes.js **`9ceea648`** (`master`, pushed: local == remote at `9d814f6c`) |
| files | `demo/styles/playback-idiom.css` **and** `demo/styles/design-idioms.css` — ONE sha (K-5) |
| ground | re-derived **before** the act, per selector: `evidence/W13/c-op5-rederivation.md` (value.js `dc7866c2`) |
| runtime clause | `test/demo/instrument/playback-ribbon-contract.test.ts` (keyframes.js `9d814f6c`), born-RED 2 failed / 13 passed ×2 → GREEN 15/15 ×2 |

## 2 · What the BEFORE witness measured, and what the AFTER shot must now show

**BEFORE (KF.W9's, held there)**: under forced-colors, a focused glass `Button` wearing
`.btn-playback` shows **NO indicator** — the demo's unlayered `.btn-playback:focus-visible
{ outline: none }` cascades later than, and at equal (0,2,0) specificity to, glass-ui's unlayered
`a11y-overrides.css` restoration, and `box-shadow` does not paint in forced-colors.

**AFTER (owed to KF.W9 / SS-13)**: the demo rule is gone, so the producer's own
`.focus-ring:focus-visible { outline: 2px solid Highlight; outline-offset: 2px }` is the last word
on the same element — every glass `Button` renders `focus-ring` in its own class list (measured:
`dist/button.js` → its chunk's class expression). **The shot must show the Highlight outline
returning on a focused transport button.**

**Subjects for the shot** (this act's census, LAW A — five class applications, all producer
`Button`s): `PlaybackRibbon.vue:70` (Play/Pause, accent) · `PlaybackRibbon.vue:80` (Reverse) ·
`SpringScene.vue:225` · `SpringScene.vue:254` · `StartingStyleTarget.vue:58`. The ribbon pair is
the cell KF.W9's before-witness used.

**Cells**: as KF-W9 `:238` names them — the safari-app cell and the real-HCM cell, per-shot
`sha256` + cell + substrate ref.

## 3 · The half that was NOT deleted, so the shot is not mis-scoped

`design-idioms.css`'s `.kf-focus-ring:focus-visible` **stays** (re-anchored `:106`, its
forced-colors arm `:127`). It is not a copy of the deleted rule: its eleven hosts are bespoke
non-`Button` surfaces that no producer selector matches, and it supplies the Highlight outline
itself. **A shot that expects those hosts to change has the wrong subject** — they were already
compliant after KF.W6 `a6418729`, and this act left them byte-identical apart from the dated ledger
written beside them.

## 4 · What is NOT claimed here

No screenshot, no sha256, no cell reading, no green on KF.W9's gate. KF.W9 is CLOSED
(honest-RED); this witness is owed **to** it, blocks nothing **from** it, and is delivered as an
artefact keyed by row id so the re-shoot can be scheduled by whoever owns SS-13's shared capture.
