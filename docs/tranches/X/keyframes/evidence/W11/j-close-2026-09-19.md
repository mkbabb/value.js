SERVED MODEL: claude-opus-5[1m]

# KF.W11.j — the close seat's verification artefacts (2026-09-19)

Spec: `docs/tranches/X/keyframes/waves/KF-W11.md` §Agent Units `:273-277` · §Gates G-KFW11-10 `:307`
· §Commit plan 7 `:382` · §Format and lint cadence / §Verification artefacts `:384-386` · the
ADDENDUM 2026-09-19 (`:408-417`) part 3, the ratchet's close clause. Authority for the ratchet:
`COHESION.md` §0u (2026-09-19, `085b2121`).

**This seat wrote ZERO keyframes.js bytes.** Every reading below is a read-only measurement taken at
this seat's own clock, at keyframes.js `bf4a9a9c`, each figure double-run.

---

## §1 Substrate, named never assumed

| reading | ⟨cmd⟩ | value |
|---|---|---|
| kf HEAD | `git -C ../keyframes.js rev-parse --short HEAD` | **`bf4a9a9c`** |
| kf `origin/master` | `git -C ../keyframes.js rev-parse --short origin/master` | **`2b649a1d`** |
| unpushed | `git log --oneline origin/master..HEAD \| wc -l` | **67** (residual j-R1) |
| kf porcelain | `git -C ../keyframes.js status --porcelain` | **2 untracked** `docs/tranches/V/coordination/VALUEJS-INBOUND-*` mail packets — value.js-delivered, outside every KF.W11 path, not touched |
| value porcelain, this seat's set | `git status --porcelain -- execution/B/KF-W11.md execution/LEDGER.md V/coordination/INBOX.md keyframes/evidence/W11` | **empty** — zero inherited hunks |
| value porcelain, whole tree at open | `git status --porcelain` | **2 modified**: `CARRY-LEDGER.md` (a sibling seat's) · `scripts/dev/dev.sh` (unowned, never staged) |
| wave open sha | seat 0's banked substrate | **`dd28da55`** |
| wave span | `git log --oneline dd28da55..HEAD \| wc -l` | **69** commits |

---

## §2 The ten gates, re-run at this seat's own commands, double-run

All commands run from `/Users/mkbabb/Programming/keyframes.js`. **SELF-COUNT**: ⟨cmd⟩
`grep -c '^\*\*G-KFW11-' ../value.js/docs/tranches/X/keyframes/waves/KF-W11.md` → **10**; ten rows
follow.

| gate | ⟨cmd⟩ (the spec's own) | born-RED (seat 0, `dd28da55`) | this seat, run 1 · run 2 | verdict |
|---|---|---|---|---|
| **G-KFW11-1** | `npx vitest run --project demo test/demo/scenes/cube-roll-and-prestart.test.ts` | `No test files found` ×2 | **17 passed (17)** · **17 passed (17)** | **GREEN** |
| **G-KFW11-2** | `… test/demo/scenes/sequence-instrument-truth.test.ts` | `No test files found` ×2 | **5 passed (5)** · **5 passed (5)** | **GREEN** |
| **G-KFW11-3** | `… test/demo/scenes/spring-derby-truth.test.ts` | `No test files found` ×2 | **9 passed (9)** · **9 passed (9)** | **GREEN** |
| **G-KFW11-4** | `… test/demo/scenes/spring-trace-truth.test.ts` | `No test files found` ×2 | **1 failed \| 11 passed (12)** · same | **RED** — one case, KF11-E(f1) |
| **G-KFW11-5** | `… test/demo/scenes/spring-heatmap-reversibility.test.ts` | `No test files found` ×2 | **18 passed (18)** · **18 passed (18)** | **GREEN** |
| **G-KFW11-6** | `… test/demo/scenes/starting-style-artifact.test.ts` | `No test files found` ×2 | **12 passed (12)** · **12 passed (12)** | **GREEN** |
| **G-KFW11-7** | `… test/demo/scenes/square-editor-seam.test.ts` | `No test files found` ×2 | **7 passed (7)** · **7 passed (7)** | **GREEN** |
| **G-KFW11-8** | `… test/demo/scenes/amiga-paused-pose.test.ts` | `No test files found` ×2 | **11 passed (11)** · **11 passed (11)** | **GREEN** |
| **G-KFW11-9** | `… test/demo/instrument/drag-scrub-reentrancy.test.ts` | `No test files found` ×2 | **9 passed (9)** · **9 passed (9)** | **GREEN** |
| **G-KFW11-10** | `npm run test:demo` + the vue-tsc clause + the self-trip grep + the skip/only grep | 39 files / 286 tests; vue-tsc 54; RED by construction | **1 failed \| 47 passed (48)** files · **1 failed \| 414 passed (415)** tests, both passes; vue-tsc **24 · 24**; self-trip **0 · 0**; skip/only grep **1 · 1** (prose) | **RED** — §2.3 |

**18 vitest invocations, one output form per gate across both passes.**

### §2.1 The one failing case, quoted whole

```
$ npx vitest run --project demo test/demo/scenes/spring-trace-truth.test.ts
 × (4b) the floor the heatmap declares is not below the one the plot pins 2ms

 FAIL  |demo| test/demo/scenes/spring-trace-truth.test.ts > SpringTrace — the ceiling's
   coupling to the ζ floor (L-14, documented AND enforced) > (4b) the floor the heatmap
   declares is not below the one the plot pins
Error: SpringHeatmap.vue no longer declares `DAMPING_MIN = <number>` — L-14's coupling anchor
   moved; re-bind the plot's PLOT_DAMPING_FLOOR witness to the new declaration
 ❯ test/demo/scenes/spring-trace-truth.test.ts:276:19

 Test Files  1 failed (1)
      Tests  1 failed | 11 passed (12)
```

**The red is exactly what `.e` predicted (residual e-R3) and `.f` returned (KF11-E(f1)).** `.e`'s
test (4b) reads `SpringHeatmap.vue` for a `DAMPING_MIN = <number>` declaration by regex; `.f`'s
contract commit re-homed that constant as the exported `DAMPING_AXIS.min`, so the witness's anchor
moved and the test failed **explicitly, by design, with the re-bind instruction in its own message**.
This is the enforcement L-14 asked for working, not a broken invariant: the coupling itself still
holds. **`.f` kept no byte in `SpringHeatmap.vue` to satisfy a sibling's regex — that would have been
a workaround aimed at a test.**

**Why this seat did not cure it.** The cure is a write to
`test/demo/scenes/spring-trace-truth.test.ts` — a keyframes.js byte, `.e`'s §Bounds `:131` create
row, and outside this unit's writable set, which is value.js paths only. The standing law is
explicit: *"any write outside it is an ESCALATION — stop and return it."* **Returned as KF11-E(j1),
with the exact cure shape `.f` already wrote down**, which is stronger than the regex it replaces
because it reads the shipped export rather than a byte pattern:

```ts
import { DAMPING_AXIS } from "../../../demo/scenes/spring/SpringHeatmap.vue";
expect(DAMPING_AXIS.min).toBeGreaterThanOrEqual(PLOT_DAMPING_FLOOR);
```

**No `test.skip` was written, no timeout widened, no assertion weakened, no allowlist added.**
Turning G-KFW11-10 green by any of those is L-18 base (iv), the masking class, and the gate's own
text forbids it.

### §2.2 The skip/only clause reads 1, and what the 1 is

⟨cmd⟩ `git diff dd28da55..HEAD -- test | grep -c 'test.skip\|it.skip\|\.only('` → **1 · 1**.

The single hit, located: ⟨cmd⟩ `git diff dd28da55..HEAD -- test | grep -n 'test.skip\|it.skip\|\.only('` →

```
1794:+ *       sections of the spec, and a `test.skip` standing in for a cure that was
```

It is **a docblock sentence inside `test/demo/scenes/spring-derby-truth.test.ts:21` (`.c`'s file)
that names the masking class in order to forbid it** — the same class of self-trip `.b` recorded
three times against its own prose. The semantic form of the clause is **0**:

- ⟨cmd⟩ `grep -rnE '\b(it|test|describe)\.(skip|only)\(' test/` → **no match** — zero skipped or
  exclusive tests anywhere in the tree.
- ⟨cmd⟩ `npx vitest run --project demo 2>&1 | grep -E 'skipped|todo'` → **no line** — vitest reports
  no skipped or todo test.

**Recorded as a literal-RED with the semantic GREEN beside it, not rounded to zero and not cured**:
the rewording would be a keyframes.js byte in `.c`'s file, outside this seat's set.

### §2.3 G-KFW11-10, clause by clause

| clause (spec `:307`) | reading | verdict |
|---|---|---|
| every `test/demo/**` file passes | **1 failed \| 47 passed (48)** files · **1 failed \| 414 passed (415)** tests, both passes | **RED** — the one case of §2.1 |
| no `test.skip`/`.only` in the wave's diff | literal grep **1 · 1** (a docblock forbidding the practice); semantic **0** (no skipped/exclusive test exists) | **literal RED · semantic GREEN**, §2.2 |
| every id in §Carry reads LANDED / KILLED-with-rationale / carried | every unit receipt carries its roster; the four BLOCKER cores all LANDED (§4) | **GREEN** |
| **zero** SWAP-discharge receipt strings in the record | §5 | **GREEN — 0 · 0** |
| the head is unmoved (vue-tsc → 0) | **24 · 24** | superseded by §0u as a RATCHET — §3 |

---

## §3 The ratchet's close reading (§0u part 3 / the ADDENDUM's close clause)

⟨cmd⟩ `npx vue-tsc --noEmit -p tsconfig.json 2>&1 | grep -c 'error TS'` → **24 · 24** (double-run,
byte-identical file histograms on both passes).

| datum | figure | delta to 24 |
|---|---|---|
| the spec's banked OP-0 count (§State, §0u, seat 0 at `dd28da55`) | **54** | **−30** |
| `.r`'s CI-faithful floor at `d8eb43ff`, the reproducible figure (KF11-E5) | **31** | **−7** |
| `.r`'s close | 29 | −5 |
| **close, this seat** | **24** | — |

**THE COUNT DID NOT RISE. The ratchet holds against both readings.** §0u part (2) — *"each wave
banks the count at its open and may not raise it"* — is MET. §0u part (3) — *"the count reads 0 at
KF.W13's close"* — is **not this wave's to assert** and is not asserted here.

**KF11-E5 stands, restated at this seat**: the banked **54** is not reproducible; the tree measures
**24** with the lockfile's pins settled, and the 24-diagnostic `OrbitalDrag.vue` block that was the
largest single item of the banked figure **does not exist** under `npm ci`. Every unit that could
have spent a cure against a diagnostic that is not there re-derived its own figure first and did not.

### §3.1 Every diagnostic still standing, homed by wave — all 24, none unowned-and-unnamed

⟨cmd⟩ `npx vue-tsc --noEmit -p tsconfig.json 2>&1 | grep 'error TS' | sed -E 's/: error TS([0-9]+).*/ TS\1/' | sort`

| home | rows | n |
|---|---|---|
| **KF.W12 §B.2** | `useKeyframeOps.ts(80,13)` TS2322 *(outside W11 `.b`'s `:58-69` carve)* · `KeyframesEditor.vue(76,73)`/`(81,41)` TS2339 · `KeyframesStringControls.vue(54,9)`/`(75,5)` TS6133 · `useTimingFunctionEditor.ts(136,13)`/`(157,13)` TS2367 · `(177,9)` TS2322 · `ChannelControls.vue(329,7)` TS6133 · `ChannelOptions.vue(272,34)` TS2379 · `LayerConfigPanel.vue(87,10)` TS2345 · `TimingFunctionPanel.vue(32,14)` TS2379 · `EasingSidebar.vue(27,14)` TS2379 | **13** |
| **KF.W13 §B.2** | `MbabbMenu.vue(208,12)`/`(208,36)` TS2339 · `TransportDock.vue(95,34)` TS2322 · `(276,7)`/`(366,9)` TS6133 | **5** |
| **UNOWNED — KF11-E3** | `easing/useEasingDemo.ts(294,13)` TS2322 · `(310,43)` TS2345 | **2** |
| **UNOWNED — KF11-E4** | `instrument/shell/EditorShell.vue(175,10)` TS2379 | **1** |
| **UNOWNED AND UNWRITABLE — KF11-E2, `src/**`** | `compositor.ts(79,11)` · `waapi.ts(9,1)` · `smooth.ts(194,13)`, all TS6133 | **3** |
| | **TOTAL** | **24** ✓ |

**KF.W11's own §Bounds rows contribute ZERO.** Every unit zeroed its own rows with the cure that
owned them: `.r` 2 · `.a` 4 → 0 · `.b` 0 throughout · `.c` 1 → 0 · `.d` 0 in `scenes/sequence` ·
`.e`/`.f`/`.g`/`.h`/`.i` 0 at close. **No cast, no `@ts-expect-error`, no `// eslint-disable` was
written as a cure anywhere in the wave** (§6).

`.r`'s original enumeration named 5 unowned demo rows; 2 (`EasingScene.vue`) were cured at
`2b649a1d` and the remaining 3 are KF11-E3/E4 above. The `TransportDock.vue(95,34)` row is present
at this close and homed to KF.W13; it is recorded here because the close reading homes what it
measures, not what a prior histogram listed.

---

## §4 Commit roster, audited against §Bounds by `git show --stat`

**SELF-COUNT**: ⟨cmd⟩ `git log --oneline dd28da55..HEAD | wc -l` → **69** commits · ⟨cmd⟩ the
per-sha `--name-only` union `| sort -u | wc -l` → **59** distinct paths.

### §4.1 The aggregate audit — every path the wave touched, against §Bounds

Method: ⟨cmd⟩ `git log --format='%H' dd28da55..HEAD | while read s; do git show --stat --format='' --name-only $s; done | sort | uniq -c | sort -rn` — the union of every sha's stat, so nothing hides inside a commit.

**All 59 paths fall inside the union of §Bounds `:96-137` and the §0u `.r` grant. ZERO out-of-bounds writes.**

| unit | §Bounds rows | paths measured in the wave |
|---|---|---|
| `.r` (§0u) | `package-lock.json` + the enumeration's own files | `package-lock.json` · `demo/scenes/easing/EasingScene.vue` |
| `.a` | `:96-103` | `CubeTarget.vue` · `CubeTarget.css` · `CubeScene.vue` · `matrix-editor/{MatrixEditor.vue,transformMath.ts,useTransformState.ts}` · `orbital-drag/OrbitalDrag.vue` · `orbital-drag/composables/useOrbitalPointer.ts` · `useCubeDemo.ts` · `useCubeRelit.ts` · the `SpringScene.vue` two-line carve |
| `.b` | `:121-124` | `SquareScene.vue` · `SquareScene.css` · `SquareInstrument.vue` · `useSquareDemo.ts` · `useSquareKeyboard.ts` · `useSquareTumble.ts` · `squareKeys.ts` — **`useKeyframeOps.ts` NOT touched** (L-2 booked LANDED-BY `d7f68225`) |
| `.d` | `:104-110` | `SequenceScene.vue` · `SequenceTarget.vue` · `SequenceTarget.css` · `SequenceAxis.vue` · `SequencePlayhead.vue` · `useSequenceDemo.ts` · `useSequenceInstrument.ts` · `SequenceScrubber.vue` |
| `.c` | `:115-116` | `SpringScene.vue` · `SpringTarget.vue` · `useSpringDemo.ts` · `useSpringDerby.ts` · `useSpringLinearStops.ts` · `useCompiledEntry.ts` |
| `.e` | `:118` | `SpringTrace.vue` |
| `.f` | `:119` | `SpringHeatmap.vue` · `SpringPhysicsFacet.vue` |
| `.g` | `:120` | `StartingStyleTarget.vue` — **`useSpringDemo.ts` never written** |
| `.h` | `:125-126` | `AmigaScene.vue` · `useAmigaDemo.ts` · `useAmigaThree.ts` · `useSphereSpin.ts` · `utils.ts` |
| `.i` | `:110-114` | `useDragScrub.ts` · `useDragCapture.ts` · `AnimationVisualizer.vue` (the KF-AV-16 carve) — **`gestureSelectSuppression.ts` NOT exercised**, with the reason stated |
| tests | `:128-137` | the nine create rows + the five modify rows (`cube-scene` · `square-scene` · `sequence-scene` · `amiga-sphere-spin` · `sequence-scrubber-mount`) |

### §4.2 The "Do NOT touch" list, measured EMPTY

⟨cmd⟩ `git diff --stat dd28da55..HEAD -- src node_modules package.json vitest.config.ts demo/scenes/cube/CubeAxisLines.vue demo/components/playback/PlaybackRibbon.vue demo/components/instrument/timeline demo/components/instrument/keyframes/composables/useKeyframeOps.ts demo/utils/gestureSelectSuppression.ts demo/DESIGN.md` → **0 lines of output.**

**Zero `src/**` bytes · zero `node_modules` bytes · `package.json` unmoved · `vitest.config.ts`
unmoved · `CubeAxisLines.vue`, `PlaybackRibbon.vue`, `TimelineTrack.vue`, `KeyframeTimeline.vue`,
`useKeyframeOps.ts`, `gestureSelectSuppression.ts` and `DESIGN.md` all byte-unchanged.**
Glass-ui: ⟨cmd⟩ `git -C ../glass-ui status --porcelain` — **not written by any seat of this wave**;
every producer row rode mail (§7).

⟨cmd⟩ the per-sha `--name-only` union `| grep -c 'dev.sh'` → **0**. `scripts/dev/dev.sh` appears in
**0 of 69** commits.

### §4.3 The MUST-NOT-SPLIT families, each verified as ONE sha

| family | sha | `git show --stat` names |
|---|---|---|
| the four phantom `variant:` sites (`.a`, §Commit plan 1) | **`dc3f0900`** | `CubeScene.vue` **AND** `SpringScene.vue` — 2 files, one sha ✓ |
| ST-1 one word + one deletion (`.d`, L-9) | **`f4249cb0`** | `SequenceTarget.css` **AND** `SequenceTarget.vue` ✓ |
| the ONE guard family in BOTH composables (`.i`, L-18 base (iii)) | **`4ab4c4db`** | `useDragCapture.ts` **AND** `useDragScrub.ts` ✓ |
| tether-truth D-1+D-6+D-16+N-SQ-4 (`.b`) | **`b9b5476c`** | `SquareInstrument.vue` + `SquareScene.vue` + the witness ✓ |
| MISS-3's settle-paced tour **and** its assertion (`.b`) | **`8c0122e1`** | `useSquareKeyboard.ts` + `SquareScene.vue` + **both** test files ✓ |
| the spring-plot core D-1+N-1+N-2+C-2/L-3 (`.e`) | **`e683d9a1`** | `SpringTrace.vue`, one file, one sha ✓ |

**L-18 base (iii) cannot be raised against this wave**: the drag-seam guard landed in
`useDragScrub.ts` **and** `useDragCapture.ts` in the same sha, and `git show --stat 4ab4c4db` names
both.

### §4.4 The four §Goal-criterion BLOCKER cores — all LANDED

| BLOCKER | sha | gate |
|---|---|---|
| **CubeTarget #53** — the die's only pre-start orientation writer | `bc5cc128` | G-KFW11-1 GREEN |
| **SquareScene L-2** — the editor seam keeps the square's renderer | booked **LANDED-BY `d7f68225`** (R.2 GREEN-BEFORE-CURE, the library arm landed first), born witness `1233e42e` passing | G-KFW11-7 GREEN |
| **AmigaScene D-1** — the paused stage shows the scrubbed pose | `73b424da` | G-KFW11-8 GREEN |
| **SpringTarget C-1** — who dispatches play-intent | `8dcf5882` | G-KFW11-3 GREEN |

**The §Goal criterion's own failure mode — *"lands the hygiene tails and leaves a BLOCKER core
un-cured"* — did NOT occur.** MISS-3's docblock-as-assertion, the criterion's named example, landed
with its assertion in one sha (`8c0122e1`) and the pacing byte clause reads **0 · 0**.

### §4.5 The shas other waves open on, printed for them

| consumer | sha | what it is |
|---|---|---|
| KF.W12 AXISLINE-UNIT | **`fff7232c`** | `.a`'s OD latch family → the registry (KF-AX-1's upstream half) |
| KF.W12 KFED-UNIT | `useKeyframeOps.ts` **byte-unchanged**; L-2 booked **LANDED-BY `d7f68225`** | the `:58-69` carve was NOT spent; `useKeyframeOps.ts(80,13)` is outside it and left untouched |
| KF.W13 | `useDragCapture`'s **exported surface unchanged** (`{onStart?,onMove?,onEnd?}` → `{isDragging,onPointerDown}`, 0 changed lines); `PlaybackRibbon.vue` never edited | two internal changes W13 re-measures: window-scoped listener registration; `onMove` delivered once per frame with the terminal sample flushed at release |
| KF.W11 `.i`'s own open point | `ba12b2ba` (`.d`'s last) | recorded by `.d` |

---

## §5 The KF-AV-28 discharge alphabet — stated positively, in words

**ZERO discharge receipts of the KF.W7 SWAP-verdict kind exist in this record. The set is EMPTY and
always was.** KF.W7's verdict was **KEEP-BESPOKE on all six surfaces, ZERO SWAP** (`4c03ceda`,
2026-09-18), so no surface of this wave was ever eligible to carry such a receipt; `.i` stated the
KEEP verdict before its first byte and emitted none, and no other seat wrote one either.

The gate's measurement, run with the pattern deliberately broken by a character class so the command
itself cannot self-trip the file it greps:

⟨cmd⟩ `grep -cE 'DISCHARGED by KF[.]W7 SW[A]P verdict' docs/tranches/X/execution/B/KF-W11.md` →
**0 · 0**.

**This clause of G-KFW11-10 is GREEN.**

---

## §6 §Format and lint cadence (spec `:384-386`), at close

| ⟨cmd⟩ | reading |
|---|---|
| `npm run check` | **exit 2** — leg 1 (`vue-tsc`) on the 24 rows of §3.1; legs 2 and 3 unreached. **KF.W11's own rows contribute zero**; the 24 belong to KF.W12 (13), KF.W13 (5), KF11-E3/E4 (3) and KF11-E2's `src/**` (3) |
| `npm run test:demo` | **1 failed \| 47 passed (48)** files · **1 failed \| 414 passed (415)** tests, both passes — the one RED of §2.1 |
| `npx eslint demo/scenes demo/composables demo/components/playback demo/components/instrument/transport/composables` | **exit 0, no output** — the §Format scope is CLEAN |
| masking constructs added in the wave diff: `@ts-expect-error` | ⟨cmd⟩ `git diff dd28da55..HEAD -- demo test \| grep -c '^+.*@ts-expect-error'` → **0** |
| masking constructs added: `eslint-disable` | ⟨cmd⟩ same form → **0** |
| casts added in **product** bytes | **0** — the one `grep` hit in `demo/composables/useDragScrub.ts` is the prose *"what was absent was any"*, a substring false positive, verified in context |
| casts added in **test** bytes | **9**, all in `test/demo/scenes/spring-derby-truth.test.ts`: `(window as any).requestAnimationFrame`/`cancelAnimationFrame` (×6, the deterministic rAF harness `.c` used to reproduce the M-5 race) and three spring-state stubs. **Recorded, not rounded to zero.** None cures or suppresses a diagnostic — `tsconfig.test.json` is a different leg from `vue-tsc`, and the §0u refusal of `as` is a refusal of casts used AS cures |

---

## §7 The BH relay — every producer row this wave surfaced (lock 4, glass-producer → SS-6)

Collected from the nine unit receipts. **Every row is producer-side; not one was worked around
demo-side** — a demo-side cure for a producer defect is a HIGH defect under the wave's own law, and
no seat spent one. **Glass-ui was READ-ONLY at every seat.** Eight rows, from four units; five units
(`.b` · `.c` · `.d` · `.e` · `.h`) surfaced none, and `.c` and `.h` said so positively so this seat
would not go looking.

| # | row | source | ask |
|---|---|---|---|
| **W11-P1** | `registerShortcut`'s combo chip renders a `code` spelling verbatim — `formatCombo("KeyX")` → `"KeyX"`. Registering SPATIAL bindings by `event.code` is what discharges OD-4's layout limb, so the consumer cannot avoid `code` | `.a`, **KF11-E6** | a display override on `ShortcutOptions`, **or** `formatComboParts` learning the `Key*`/`Digit*` family |
| **W11-P2** | the Chip's selectable filter strips `role` / `aria-pressed` — the banked SPF-4 constraint, re-measured live | `.f` finding 1(a) | restore the state attributes on the selectable filter, or document the supported toggle seat |
| **W11-P3** | ToggleGroup single-mode items ship `aria-pressed` buttons inside a `role="group"`; four mutually exclusive presets are a radio group, and **the radio half needs the producer** | `.f` finding 1(b) | a single-select mode that emits `role="radiogroup"`/`role="radio"` |
| **W11-P4** | **the Boolean-cast `default: undefined` class the close brief names**: `ToggleGroupProps.modelValue` is typed **without** `undefined` while the runtime accepts it — the controlled-empty case is unrepresentable under `exactOptionalPropertyTypes` | `.f` finding 1(c) | widen the prop type to admit `undefined`, or publish the sanctioned empty sentinel |
| **W11-P5** | the ToggleGroup ROOT reaches the DOM through reka's `as-child` **without the consumer's scope attribute**, so no consumer's scoped rule can reach its track plate | `.f` finding 1(d) | the track opt-out ask KF.W7's easing port already mailed — one cure, two consumers |
| **W11-P6** | LabeledSlider ships **no `valueCommit` seam** (N-SH-3's banked datum) — a consumer cannot distinguish a drag sample from a committed value | `.f` finding 1(e) | a `valueCommit` emit beside `update:modelValue` |
| **W11-P7** | the window shortcut registry checks **neither `defaultPrevented` nor composite-widget ownership** (N-SH-2, carrier #2) — a global binding fires inside a widget that already handled the key | `.f` finding 1(f) | honour `defaultPrevented`; give composite widgets an ownership claim |
| **W11-P8** | glass `accessibility.css` keys `forced-colors: active` / `prefers-contrast: more` state indication on `[aria-current]`, `[aria-selected]`, `[aria-pressed]`, `[aria-checked]`, `[data-state="checked"]`, `[data-state="on"]` — and on **nothing** for `[aria-expanded]`. A correctly-authored disclosure button gets no vendor state indication, pushing consumers to `data-state` workarounds or to misusing `aria-pressed` | `.g` | **one selector-list addition** fixes it for every consumer |

**CubeScene D-2/C-3's `variant`, which the close brief names: measured and resolved CONSUMER-SIDE,
with NO producer ask.** `.a` read installed glass-ui 7's `ButtonProps` — `emphasis` / `tone` / `size`
/ `iconOnly` / `loading` / `type` / `disabled` / `class`, and **no `variant`** — so all four
`variant: "outline"` sites were phantom props falling through to the DOM, not a producer gap. The
cure was their deletion (`dc3f0900`, one sha, both files), the byte clause reads **nothing** twice,
and **no producer row is owed**. Recorded so a later seat does not mint one.

### §7.1 Non-producer findings routed with their owning waves (not SS-6)

`EasingTarget.css`'s `.specimen-grid` track reset is INERT at the bytes for W11-P5's reason, measured
live on `#/easing` → **KF.W12** (`.f` finding 2) · KF-CO-47's decision is what SPF-13 / KF-SS-24 wait
on → **KF.W12 OPTIONS-UNIT** (`.f` finding 3) · D-B3's full decision, SPF-3/-5's delivered-pixel
cells, D-M5's touch outcome, C-m-6's clipping re-measure → **KF.W9 / SS-13** (`.f` finding 4) ·
MISSED-F's house-wide `touch-action: pinch-zoom` idiom at five further scene sites → the owning units
(`.h`).

---

## §8 E13 Step-0 — the four-path mail sweep at this seat's clock

Swept **read-only**. Classification taken from each row's **Status cell by position** and from that
cell's **leading verb**, never from a bare `grep -i unread` — the token `UNREAD` appears in four
rows' Status prose (O-20's sweep note, I-31's *"Prior status, kept"*, I-32's *"The advance from
UNREAD"*, O-39's quotation of the E13 law) and is the leading verb of **none**. `INBOX.md`
self-excluded (SELF-COUNT law).

1. **`docs/tranches/V/` + `V/coordination/`** — ⟨cmd⟩ `/bin/ls -t docs/tranches/V/coordination/ | head -6` → `INBOX.md` (self) + the five 2026-09-18 `*-inbox-2026-09-18-value-4.1-*` letters, **ours and rowed** (`evidence-addendum-2` → 3 citations in the ledger).
2. **`../glass-ui/docs/tranches/BK/coordination/`** — BK re-confirmed newest: ⟨cmd⟩ `/bin/ls -dt ../glass-ui/docs/tranches/*/ | head -4` → `BK/` · `BJ/` · `BI/` · `IOS27-MICRO/`. Newest letter = `glass-outbound-2026-09-18-valuejs-o26-reply.md` = **I-35, rowed**. **UNMOVED** since seat 0.
3. **`../keyframes.js/docs/tranches/V/coordination/`** — newest inbound-grammar file = `VALUEJS-INBOUND-2026-09-17-o8-o11-amendment-addendum.md` = **O-21, ours, delivered**.
4. **`../sci-report/atlas/docs/tranches/P/coordination/`** — newest = `valuejs-inbound-2026-07-27-library-band-export-delta.md` = **O-12, ours**. Path **UNMOVED**.

⟨cmd⟩ `grep -cE '^\| [IO]-[0-9]+[a-z]? \|' INBOX.md` → **79** rows. ⟨cmd⟩ the positional
leading-verb Status scan → **0 · 0** UNREAD (double-run).

**Result: ZERO unrowed letters addressed to value.js · ZERO UNREAD Status cells · ZERO new `I-n`
minted.** Register tail **I-35 / O-39** — O-39 (2026-09-19, X.F.W8 unit `e`, outbound to
fourier-analysis) was rowed by a sibling track since seat 0's sweep and is **SENT**, ours, and
carries no obligation on this wave. **No wave closes with UNREAD mail in scope; none is.**

---

## §9 Residuals and escalations, the whole wave

### §9.1 Escalations returned — 11 open, 2 discharged

| id | substance | owner |
|---|---|---|
| **KF11-E1** | which reading of *"G-KFW4-1 lands"* governs OP-0 | **DISCHARGED** — ruled at COHESION §0u, disposition (b), the RATCHET |
| **KF11-E5** | the banked OP-0 count **54** is not reproducible; the CI-faithful floor is **31** | **DISCHARGED as a measurement** — consumed by every unit's own re-derivation and by §3 |
| **KF11-E2** | the three `src/**` TS6133 rows (`compositor.ts` · `waapi.ts` · `smooth.ts`). §Excluded makes a `src/**` write wave-invalidating and §0u does not lift it. **Sole cause of CI's `library gates` job failure** at run `35428272041`. **§0u part (3)'s "0 at KF.W13's close" cannot be met by KF.W11–W13 alone** | **KF.W5 / KF.W8** (or an owner grant extending a seat's set to `src/**` for TS6133-only cures) |
| **KF11-E3** | `useEasingDemo.ts(294,13)` / `(310,43)` — needs a `@mkbabb/value.js` declaration fix and/or the easing-name contract. Forcing it in-set measured **28 → 42** with +4 rows outside the set (measured, reverted) | **KF.W12 OPTIONS-UNIT** |
| **KF11-E4** | `EditorShell.vue(175,10)` — the failing property is Vue's `VNodeProps['key']`; every available cure is behavioural | the shell seam's owning wave |
| **KF11-E6** | `registerShortcut`'s `code` spelling — producer | **SS-6**, relayed as W11-P1 |
| **KF11-E7** | **the wave's largest coverage cap**: the demo vitest lane cannot import any SFC that transitively loads glass-ui's spring chunk — `Cannot find package '@mkbabb/keyframes.js' imported from node_modules/@mkbabb/glass-ui/dist/useSpring-*.js`. Raised **independently by `.a`, `.b`, `.d` and `.g`**. `vitest.config.ts` is a §Excluded Do-NOT-touch row; **no seat patched it, mocked the producer, patched `node_modules` or skipped a case** | whichever unit owns `vitest.config.ts` — **every scene-level mount witness in this wave and the next is blocked on it** |
| **KF11-E(c1)** | M-4→M-3's cure bytes are `SpringTrace.vue:50-86`, `.e`'s §Bounds row, not `.c`'s; the swap is indivisible at its home record. `.c` did not write the file and did not land half | a ruling seat, with `.e`'s 0.41 px `.fn`-deviation measurement as a dated input |
| **KF11-E(d1)** | the loop-seam family L-3+L-10+SC-5+SC-6 needs `SweepSceneOptions.loop` in `demo/composables/scene-runtime/useSweepScene.ts` — out of bounds; **nothing of it landed, the family not split** | the unit holding `demo/composables/scene-runtime/**` (KF.W12) |
| **KF11-E(f1)** | `.e`'s test (4b) reads `.f`'s file by regex; `.f` re-homed `DAMPING_MIN` as `DAMPING_AXIS.min`. **This is the one demo RED** | re-raised as **KF11-E(j1)** below |
| **KF11-E(i1)** | KF-SCR-5's dead-API deletion needs `SquareScene.vue:425` in the SAME sha as the composable half; landing the seam half alone creates a NEW TS2353 — a rise the ratchet refuses. Census run (`releasePolicy` 1 consumer, `onRelease` 0). **Nothing landed; not substituted** | whichever seat holds `SquareScene.vue` |
| **KF11-E(i2)** | C·C-1's sequence half — `useSequenceDemo.scrub()` dispatches per admitted sample at `:310`/`:368`; the landing byte is `.d`'s §Bounds row. Cure shape written out, ready for one commit | whichever seat holds `demo/scenes/sequence/**` |
| **`.h` C-20** | `demo/scenes/amiga/checkerboard.jpg` (103,890 B) is dead — census clean (3 prose hits, 0 code refs; the board is procedural). The path is not in `.h`'s set and a delete is a write | a seat owning the path, or a bounds grant |
| **KF11-E(j1)** *(new, this seat)* | **the (4b) re-bind.** The cure is a write to `test/demo/scenes/spring-trace-truth.test.ts` — `.e`'s §Bounds `:131` row, a keyframes.js byte, outside this seat's value.js-only set. Cure shape at §2.1, stronger than the regex it replaces. **No skip, no widened timeout, no weakened assertion** | `.e` re-opened, or a seat granted that path |

### §9.2 Residuals of this seat

- **j-R1 — 67 keyframes.js commits are unpushed.** ⟨cmd⟩ `git log --oneline origin/master..HEAD | wc -l` → **67**; HEAD `bf4a9a9c`, `origin/master` `2b649a1d` (only `.r`'s two are published). The owner's begin-word authorises push, but **push is not among this unit's enumerated acts and this seat's lock is value.js paths only**, so it is named rather than taken. **Routed to the orchestrator** with the exact command it wants: `git -C /Users/mkbabb/Programming/keyframes.js push origin master`.
- **j-R2 — the COHESION §4a mirror.** §Bounds `:141` names the SS-6 accretion register among `.j`'s modify rows, but **`COHESION.md` is not in this seat's hard-bound writable set**, and the standing law makes a write outside that set an escalation returned. The **substance is discharged in full** — the eight producer rows are written whole into `INBOX.md` (§7, the durable coordination ledger E13 names), addressed, and ready to paste as §4a rows. **The mirror itself is routed to the orchestrator**, exactly as KF.W0's close routed the same mirror (`LEDGER.md:45`, *"COHESION §4a mirror → orchestrator"*).
- **j-R3 — the skip/only literal.** §2.2: the clause reads 1 on a docblock in `.c`'s test file that forbids the practice; semantic 0. The rewording is a keyframes.js byte and was not taken.
- **j-R4 — `TransportDock.vue(95,34)` TS2322** is present at this close and homed to KF.W13; seat 0's open histogram listed only `(276,7)`/`(366,9)` for that file. Recorded as a homing observation, not a rise: the whole-tree count **fell** (54/31 → 24) and KF.W13's own rows are that wave's to zero.

### §9.3 What did NOT happen — stated positively, because L-18 will look for it

- **No `src/**` byte was written by any seat** (§4.2) — L-18 base (v) cannot be raised.
- **No geometry cure was written off a banked figure**: `.b` (tether, 225.5 / 201.7 / 187.5 px), `.d` (the grid placement), `.h` (`hypot(1.5, 13.8)` = 13.8813 → 1.719:1 → 1.624:1 against φ) each re-derived at their own open sha **before** the cure — L-18 base (ii) cannot be raised.
- **The drag-seam guard landed in BOTH composables in ONE sha** (`4ab4c4db`) — L-18 base (iii) cannot be raised.
- **The three decisions were written before their patches** and are in the record above their families: `.d`'s canonical time domain (`532f8985`), `.e`'s N-1 fork (`ec4b7eff`), `.f`'s field encoding (`f6d05a5c`) — three value.js commits that **precede** their keyframes.js families — L-18 base (vi) cannot be raised.
- **No producer defect was cured demo-side** (§7) — L-18 base (viii) cannot be raised.
- **No BLOCKER core slipped to a residual** (§4.4) — L-18 base (i) cannot be raised.
- **No `test.skip`, no `.only`, no widened timeout, no allowlist, no copied producer selector, no `node_modules` patch** anywhere in the wave (§2.2, §6) — L-18 base (iv) cannot be raised; the one demo RED is **carried honestly**, not masked.
- **E-3 held**: no dated spec, adjudicated registry row, conformance artefact or prior evidence file was edited by this seat or any unit — every value.js act was an append or a new file.

---

## §10 SELF-COUNT

**10** gates re-run, each **twice** (18 vitest invocations + the suite, the ratchet and the clauses) ·
**8 GREEN · 2 RED** (G-KFW11-4 · G-KFW11-10), both RED tracing to **one** case · **17** byte clauses
re-measured, each twice · **69** commits audited, **59** distinct paths, **0** outside §Bounds ·
**6** MUST-NOT-SPLIT families verified unsplit · **4** BLOCKER cores LANDED · **24** diagnostics
standing, **all 24 homed by wave**, **0** in KF.W11's own rows · **8** producer rows relayed, from
**4** units · **14** escalations tabled (**2** discharged, **12** open, **1** minted here) · **4**
residuals of this seat · **79** INBOX rows swept, **0** UNREAD · **0** SWAP-discharge receipts ·
**0** figures published that were not read from settled bytes.
