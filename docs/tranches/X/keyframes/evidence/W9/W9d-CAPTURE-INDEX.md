SERVED MODEL: claude-opus-5[1m]

# X.KF.W9 `.d` — CAPTURE INDEX (the contrast / forced-colors / AT cell)

**Date** 2026-09-17 · **Gates** G-KFW9-8 · G-KFW9-9 + shares of -1/-2/-4/-14 ·
**Ruling** COHESION §0m.2 (shape (b)) · §0j.C KF-AT / KF-ODV3 / KF-ODV5.

The two evidence records are the payload; this file is the index the §Bounds
`evidence/W9/**` row asks each seat to publish.

| artifact | what it carries |
|---|---|
| `…/audit/visual/safari-real/hcm-CELL-RECORD-2026-09-17.md` | the substrate build, OP-4 per cell, the forced-colors reachability measurement **with its control**, the cascade re-derived at the bytes, the G-KFW9-9 BEFORE witness + falsifier, G-KFW9-8's re-recorded 0 and the twelve-file producer enumeration, the WHC bound, the mid-seat cell closure |
| `…/audit/visual/safari-real/at-CELL-RECORD-2026-09-17.md` | the AT-precondition inventory over 6 routes, thirteen §F rows dispositioned, the three AT cells' bounds, S-8 family (vi) declared **unspent** |
| `…/audit/visual/safari-real/hcm-at-cell-ledger-2026-09-17.json` | the machine-readable ten-cell ledger + the seven captures with digests |
| `W9d-raw-safari-app-desktop.json` · `W9d-raw-chromium-cells.json` | the unedited measurement output the two records quote |
| `W9d-cell-capture-safari.mjs` · `W9d-cell-capture-chromium-webkit.mjs` · `W9d-webdriver-client.mjs` | the apparatus, banked so every reading is re-runnable |

## Substrate stamp carried on every row

```
repo          keyframes.js
substrateRef  55e9bf0d              (COHESION §0m.2 HOLD; frontier 3e81f500, pin is its ancestor)
substrateSha  55e9bf0d2391bbc6d9871bb3f0555a6225daae92
bundleSha256  1ba994574a250fcc1ffe4655fdee0249a235c2824a2692a6dcfe785d2afda448   MEASURED, double-run
clone         /Users/mkbabb/Programming/keyframes-w9-capture   (§0m.2 grant; clean at the pin;
                                                                no commit, no push, not the sacred checkout)
origin        http://127.0.0.1:9123
```

## The seven captures — self-counted from the settled bytes

⟨`ls -1 …/safari-real/ | grep -cE '^(hcm|at)-.*\.png$'`⟩ → **7** ·
⟨each PNG re-hashed and compared to its `.sha256` sidecar⟩ → **7 of 7 agree**.

| # | shot | cell |
|---|---|---|
| 1 | `hcm-safari-app-desktop-easing-ribbon-default.png` | `safari-app/desktop` |
| 2 | `hcm-safari-app-desktop-easing-focusring-default.png` | `safari-app/desktop` |
| 3 | `hcm-safari-app-desktop-sequence-slider-default.png` | `safari-app/desktop` |
| 4 | `hcm-chromium-emulated-forced-colors-easing-ribbon.png` | `chromium/emulated-forced-colors` |
| 5 | `hcm-chromium-emulated-forced-colors-sequence-slider.png` | `chromium/emulated-forced-colors` |
| 6 | `at-chromium-spring-heatmap-application.png` | `chromium` |
| 7 | `at-chromium-amiga-bare-canvas-subject.png` | `chromium` |

**No capture is labelled `windows/real-HCM` and none is labelled `webkit-engine`.** Both cells read
UNREACHABLE-IN-CELL / UNMEASURED with their measured bounds, because a label a run did not earn is the
I-20 failure, not a shortcut.

## The four things this seat found that the registry does not yet say

Handed to `.e` as addendum material under **original ids** (E-3); **no record byte written here**.

1. **`playback-idiom.css`'s redundancy ground is FALSE as worded** — glass 7.0.0 ships **no
   `.btn-playback` rule at any coordinate**. The row survives by a *different* path: the ribbon
   buttons carry **both** `.btn-playback` and `.focus-ring`, so the producer reaches them once both
   demo copies are gone. **This also makes K-5 measured rather than argued.** → KF.W13, before the
   deletion lands.
2. **kf-SpringHeatmap D-m8's label is 23 words, not 33** (counting rule published with the figure).
   The no-`describedby`-split half holds; only the numeral moves, and downward.
3. **`role="application"` appears TWICE** on the spring route with an identical 23-word label —
   the bank's *"sole such role"* is a role-**kind** claim and is untouched; the duplication is new.
4. **The copy button ships in two divergent forms on one route** — `"Copy curve literal"` has **no**
   nested `role="status"` at all, `"Copy easing literal"` has one. KF-CB-9's probe therefore has a
   third possible outcome its two-branch framing does not enumerate.

Plus a fifth, booked as a dated measurement rather than a re-grade: the producer's forced-colors rules
now live in **twelve** dist files, not four. kf-SequenceAxis ruling 7's four are **all still present**
and its conclusion — *no "sole forced-colors rule is X" cell survives* — is **reinforced**, not
disturbed.
