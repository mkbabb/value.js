SERVED MODEL: claude-opus-5[1m]

# X.KF.W9 `.a` — THE SCOPED SURFACE LIST · 590 probes, published

**Gate**: G-KFW9-3 (the wave's own scoped surface list) · G-KFW9-4's denominator, re-derived at open.
**Seat**: `.a`, 2026-09-17. **Machine form**: `SURFACE-LIST.json` (590 addressable probe rows).
**Authority**: `docs/tranches/X/keyframes/waves/KF-W9.md` §Surface-list protocol **1–7** · §Gates
G-KFW9-3 / G-KFW9-4 / G-KFW9-12 / G-KFW9-2 / G-KFW9-14 · §H *B18-26* · *KF-AX-26* · *SS-13 residue*.

> **A fraction quoted before this list is published FAILS G-KFW9-3.** It is published here, first, and
> every fraction this wave ever quotes is against **590** and nothing else.

---

## 1 · Protocol 1 — THE REJECTED DENOMINATOR (B18-26, adjudicated verbatim)

`152 surfaces × 11 states × 4 origins × 11 profiles = **73,568**` — and the handoff's split
**46,816 + 26,752 = 73,568** exactly. **A Kronecker self-cross-product with no product consumer.**

Re-read at the bytes this seat:

- ⟨`sed -n '151p' …/codex-provenance/intakes/lane-keyframes-b10-b21.md`⟩ →
  *"Real iOS Simulator Mobile Safari remains **0/73,568** chronology-only cells | **TRUE as arithmetic;
  UNPROVEN as coverage (manufactured denominator)** … It is a Kronecker self-cross-product with no
  product consumer … **CARRY-TO-WAVE KF.W9**: the *numerator* is honest (zero real-Safari execution …).
  **The denominator must be replaced by KF.W9's own scoped surface list, not inherited**"*

**RULED VERBATIM: inherit the NUMERATOR (zero real-Safari execution). REJECT the DENOMINATOR.**
73,568 appears in this wave exactly once more — as the thing that was rejected.

---

## 2 · Protocol 2 — THE LIST **IS** THE BANKED RESIDUE: 565 + 25 = **590**

### 2.1 The enumerated 565 — re-derived at this seat with the **AMENDED** command

**Counting rule (amended form, repair round 3 / RULINGS-3 D3-6):** *per record, count `- ` / `N. `
**top-level** items inside every `##` **OR `###`** heading matching `UNPROVEN` / `SS-13`, excluding
identity-guard sections.*

Both variants executed side by side by one script, printed as its whole output, **twice**:

```
h2-only: 563 · h2+h3: 565 · records with items: 57 · files: 58
```

**The published figure reproduces to the digit — including the struck 563.** The delta is located, not
assumed:

```
DELTA +2   kf-ChannelOptions.md
```

and it is exactly the pair the spec names: **`### G. SS-13 additions (append to the standing list)`**
(`:223`) — an **h3 nested inside `## ADDENDUM R2`** (`:174`) — carrying item **11** (`:225`, dropdown
WIDTH overflow) and item **12** (`:226`, focus-ring 2px bleed sufficiency). **563 + 2 = 565.**

**THE RETIRED COMMAND IS A TRAP, NAMED** — S-13 arms a triumvirate on *"a material divergence from
565+25"*, and the **unamended `##`-only form returns 563 at the current bytes**. *A seat that measures
563 has run the retired command, not found a corpus change.* The divergence is measured with the
**amended form only**.

**`kf-CubeAxisLines.md` is the one record of 58 with ZERO enumerated items** (⟨`--per-record`⟩ → `0 0
kf-CubeAxisLines.md`) — its whole residue is the prose-carried r1 queue below. That is why the figure is
**57 records with items over 58 files**.

### 2.2 The prose-carried 25 — three records, each re-read at its byte

| record | anchor | count | what is carried |
|---|---|---|---|
| `kf-CubeAxisLines.md` | `:104` | **9** | *"r1's **nine-item queue** carries with three sharpenings"* — #1 (the Z triptych: settled attitude / mount frame / PRM) now decides **three** rows and **must be shot BEFORE AND AFTER the filter gate** (S-9); #3 gains α-4's cross-surface framing; #8 gains α-1's methodology (pin `--axis-active`). #2 #4 #5 #6 #7 #9 carry unchanged |
| `kf-TransportDock.md` | `:122` | **8** | *"**1–8 carried from r1**"*: TD-30 strand probe (~3.6 s boundary) · TD-7 rendered boxes · TD-8 vivid-arm per-pixel · TD-3 cost profile · TD-13 rendered consequence at 390×844 · TD-21 cross-mirror live twin · TD-2 Space single-toggle twin · the AT-activation path of `usePlayActuation` |
| `kf-CubeTarget.md` | `:83` | **8** | *"r1 items **1–4 and 6–8** unchanged"* (7) **+ r1 item 5 AMENDED** (1) — its *"3D tumble under a pre-start drag"* clause died with #53; the amended probe captures the spinner at z=0 inside the 0.8-α die, **static** |

**9 + 8 + 8 = 25.** **565 + 25 = 590.**

**All 25 are made ADDRESSABLE** in `SURFACE-LIST.json` (ids `CubeAxisLines#r1-1`…, `TransportDock#r1-1`…,
`CubeTarget#r1-1`…`#r1-5-amended`…`#r1-8`) — *a queue that is counted but not itemised is not a list,
and cannot reach a terminal state per probe.* **`SURFACE-LIST.json.probes.length` = 590**, self-counted
from the settled bytes.

### 2.3 The seeds — **SEEDS, NEVER DENOMINATORS**

⟨`sed -n '215p' …/INTAKE-ADJUDICATION-2026-08-03.md`⟩ →
*"### VISUAL-AUDIT-INPUT (marker; **10 rows — enumeration seeds, never denominators**)"*

**V6-18** (88 physical Vue SFCs) · **V6-19** (717 static mounts) · **V6-21** (2 Teleports) · **V6-22**
(15 routes) · **V6-23** (11 panes) · **V6-28** (137 state axes — **catalog-only** per §0's demotion) ·
**V6-33** · **V6-34** · **CM-15** (the responsive/a11y/motion predicate classes) · **XF-6** (zero Safari
on every surface, whole chain).

They **seed** enumeration — *route × pane × state-axis is the visual-audit lane's own surface.* **None
of them is multiplied into anything.** Manufacturing `15 × 11 × 137` is how 73,568 was born.

---

## 3 · Protocol 3 — KF-AX-26, THE BOUNDS LAW (honoured, and measured)

> *"The list must **NOT** inherit the demo gate's own named exclusions — **the excluded surface is
> exactly the one no instrument has ever seen**; `e2e/` does not exist."*

- **Exclusions inherited from the demo gate: `0`.** `SURFACE-LIST.json.boundsLaw.excludedSurfacesInherited = 0`.
- **`e2e/` does not exist** — `boundsLaw.e2eExists = false`. No probe is filtered on its account.
- The **axis-lock reveal** (a NAMED EXCLUSION from the demo's only visual gate) is **IN** this list, as
  KF-AX-26 requires. Its test row rides KF-AX-16.

**The temptation this law forecloses**: a surface list built by subtracting the demo gate's exclusions
would be exactly the set that has already been looked at, and would photograph nothing new.

---

## 4 · Protocol 4 — THE TEN CELLS

`safari-app/desktop` · `safari-app/ios-device` · `safari-app/ios-simulator` · `webkit-engine` ·
`chromium` · `chromium/emulated-forced-colors` · `windows/real-HCM` · `at/voiceover-safari` · `at/nvda`
· `at/jaws`.

Full roster, column law, unrun vocabulary and the per-cell OP-4 capability record:
**`CELL-ROSTER.md`** (published with this list; mechanized in `capture.mjs`/`states.mjs`).

**A `webkit-engine` verdict copied into a SAFARI column is a gate failure, not a shortcut.**
**Green without a per-shot `sha256` FAILS.**

---

## 5 · Protocol 5 — DISCRIMINATOR + FALSIFIER, on every probe

*A probe satisfied by both hypotheses is a **spec defect**, not a measurement* (exemplar:
kf-ChromeDock D-19's *"a Curve-labelled node proves NOTHING"*).

**Mechanized rather than requested.** Every probe row carries `discriminator` and `falsifier` fields,
`null` at publication. **`assertProbeTerminal()` in `capture.mjs` THROWS** on any attempt to move a row
to `EXECUTED` while either is null:

```
executedRequires: ["capture", "sha256", "substrateSha", "cell", "discriminator", "falsifier"]
```

The **fifteen** rows whose discriminators are already authored in the bank ship filled, in
**`NEGATIVE-REGISTER.json`** — the **4 traps** (each with its own discriminator and falsifier) and the
**11 retired probes** (which may never re-enter this list at all).

---

## 6 · Protocol 6 — SHARED-CAPTURE FAMILIES (S-8): no probe double-spent

Carried into the list so a probe is not photographed twice from two seats:

| family | members | seat |
|---|---|---|
| (i) | the three-rect probe, **ONE mount** (SS-13 #1; K-29 stands against ruling it statically) for kf-SequenceAxis / Playhead / Scene / Target | `.b` |
| (ii) | the composited `Card cartoon tier="quiet"` plate **ONCE** for KF-AV-19/D-8 + kf-KeyframeTimeline D-6/D-7 + kf-TimelineTrack D-5/D-6/D-m3 + kf-TimelineCaret D·M-7 | `.b` |
| (iii) | the **390×844 + 375×667** mobile pass for KF-APP-6/-8, EditorShell D-25/D-6, TD-36, KF-EST-3/4 | `.c` |
| **(iii-a)** | **THE OD-V3 CAPTURE PACKET** — **both** transport homes (in-panel transport card **and** bottom-center floating pill) on **all four** duplicating scenes (`cube` · `amiga` · `square` · `easing`) at **390 AND 1280**, against real Glass 7. **DISCRIMINATOR: one home, or either home at one viewport only, does NOT satisfy the packet — the ask is a comparison. FALSIFIER: any scene shot at 390 alone, or against a pre-Glass-7 build, VOIDS that scene's cell.** The 1280 arm is **not optional** (OD-V3's own ruling text names *"390 AND 1280"*). The storyboard scenes (spring, sequence) carry **only** the pill — which is why the packet is four scenes, not six | `.c` (390) + `.b` (1280) |
| **(iii-b)** | **THE OD-V5 390 AT-REST OBSERVATION** — recorded **either way**. OD-V5 is **DEFERRED by ruling** (COHESION §0j.C **KF-ODV5**, pending glass's dock mark). **This wave produces the packet and RULES NOTHING** | `.c` |
| (iv) | the **WHC pass** for RB-1, ST-1, KF-CE-13's folds | `.d` |
| (v) | **ONE iOS device session** for R-9, SP-4, KAD-F4, KF-KC-16/25, KF-SCR-1, i-13/C-3, K-13, TimelineCaret probe 10 | `.c` |
| (vi) | **ONE AT pass** for KF-CB-9, KF-APP-33, KF-KC-2/3/26, MM-2, D-B3, D-1/L-i1, ST-1 | `.d` |
| + | LP-1 + KF-CO-1 + KF-CO-8 (**one capture, three rows**) · the SpringHeatmap Physics-facet light-mode shot (D-B2 + D-M4 + D-M3's branch) · KF-ET-14 + SPF-5 (one plate) · #58 + D·B-1 (one arm) · the ChromeDock touch/menu session (S-6) · KF-APP-16's trace (P-4 + P-8) · the stacking capture (D-8/C-17 + SharePopover probe 5) | per S-8 |

---

## 7 · Protocol 7 — CONTRAST NUMERALS RE-DERIVE **AT CAPTURE**

**KF-SKEL-22: no corpus numeral is the figure of record.** Every contrast figure this wave publishes is
computed from the captured pixels, in the cell that captured them. Frames are **stamped** (KF-AX-4).
Every capture names **substrate ref + sha + cell**. **One pass, one pinned commit, re-anchored lines**
(KF-AT-28).

---

## 8 · TERMINAL STATES — what "discharged" means for a probe

| state | required beside it |
|---|---|
| **EXECUTED** | capture path **+ per-shot `sha256` + substrate ref/sha + cell + discriminator + falsifier** |
| **RETIRED** | the **ruling**, named |
| **UNREACHABLE-IN-CELL** | the **UA capability** (or missing host) that forecloses it, named — *by capability, not by omission* |

Anything else is one of `UNMEASURED` / `UNREACHABLE-IN-CELL` / `UNVERIFIABLE-HERE`. **Silence is not a
state, and green without a hash is not a state.**

---

## 9 · THE READING AT PUBLICATION

| | |
|---|---|
| denominator | **590** (565 enumerated + 25 prose-carried), over **58** records |
| executed | **0** |
| substrate | keyframes.js `master` == `origin/master` == **`55e9bf0d2391bbc6d9871bb3f0555a6225daae92`** |
| rejected denominator | 73,568 — **never quoted again except as the thing rejected** |

**G-KFW9-3 closes at this seat** (the list is published, derived from the banked ≈590, seeds as seeds,
KF-AX-26 honoured). **G-KFW9-4 does NOT**: its denominator re-derives — which is the gate's
*precondition* holding — while **0 of 590** probes have reached a terminal state. That distinction is
kept explicit because recording a precondition as a closure is the failure class this wave exists to
convict.
