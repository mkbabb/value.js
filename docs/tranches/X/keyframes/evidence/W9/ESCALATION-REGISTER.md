SERVED MODEL: claude-opus-5[1m]

# X.KF.W9 `.a` — THE ESCALATION-TRIGGER REGISTER · 13 armed triggers over 12 records

**Gate**: G-KFW9-12 (published WITH the surface list) · feeds **G-KFW9-13** (`.e`'s close).
**Seat**: `.a`, 2026-09-17.
**Authority**: `docs/tranches/X/keyframes/waves/KF-W9.md` §H *the escalation-trigger register* (the
enumeration and its arithmetic correction, R2-15) · §Gates G-KFW9-13 · §Bounds (the adjudicated-record
**CELL SPLIT**: existing bytes immutable, **append-only dated addenda under ORIGINAL ids, by `.e`
alone**).

---

## 0 · The figures, re-derived — not inherited

**Thirteen armed triggers over twelve records.** These are *the only registry rows whose grade is not
final.* §H states the figures at its own enumeration after the round-2 correction (R2-15 / D-5: two
dependent cells asserted **fourteen** and were restated at thirteen; the fourteenth was the
kf-TimelineTrack item, which binds **two banked ids at one record** — M-7/missed-4's `deltaY: 0` joint
MAJOR **and** D-4's label-collision — as **ONE armed row: one trigger, one measurement, two ids**).

**The record count is DERIVED HERE, by homing each trigger at the bank** (⟨`grep -l`⟩ over
`registry/adjudicated/kf-*.md`, then the decisive line read) — because a figure asserted beside an
enumeration that does not produce it is this wave's convicted failure class:

| # | armed trigger | banked home (measured) |
|---|---|---|
| 1 | KC-25 | `kf-KeyframeCard.md:67` (banked `KF-KC-25`) |
| 2 | KF-ES-3 | `kf-EasingScene.md:42` — **not** `kf-EasingSidebar.md:41`, which banks a *different* KF-ES-3 limb (the smooth-step-3 polarity row). The trigger's own words (*`jump-none` faults render*) select the EasingScene home |
| 3 | D-M3 branch (a) | `kf-SpringHeatmap.md:22` |
| 4 | TimelineTrack M-7/missed-4 **+** D-4 | `kf-TimelineTrack.md` |
| 5 | KAD-7 / D-2 | `kf-KeyframesAddDialog.md` |
| 6 | KF-SST-15/-16/-17 | `kf-StartingStyleTarget.md` |
| 7 | KF-TFP-7 (via KF-CO-5) | `kf-TimingFunctionPanel.md` |
| 8 | KF-TD-8 | `kf-TypingDots.md` — **TD = TypingDots, NOT TransportDock** |
| 9 | KF-AV-41 | `kf-AnimationVisualizer.md` |
| 10 | KAD-F4 | `kf-KeyframesAddDialog.md` — **same record as (5)** |
| 11 | KF-ET-8's contested dark arm | `kf-EasingTarget.md` |
| 12 | N-5 | `kf-SpringTrace.md:29` (*"the cheapest probe in this record"* — residue #10) |
| 13 | L-M-10 / N-3 **+** KF-TFP-19 (INP hooks) | **two records**: `kf-KeyframesStringControls.md:159` (L-M-10/N-3) and `kf-TimingFunctionPanel.md:128` (KF-TFP-19) |

**Distinct records**: KeyframeCard · EasingScene · SpringHeatmap · TimelineTrack · KeyframesAddDialog ·
StartingStyleTarget · TimingFunctionPanel · TypingDots · AnimationVisualizer · EasingTarget ·
SpringTrace · KeyframesStringControls = **TWELVE**. **Thirteen triggers over twelve records — §H's
figures reproduce at the bank.** (Two collisions carry the arithmetic: (5)+(10) share
KeyframesAddDialog; (7)+(13)'s KF-TFP-19 limb share TimingFunctionPanel; (13)'s other limb supplies the
twelfth record.)

**THREE CAN REACH BLOCKER**: (2) KF-ES-3 · (3) D-M3 branch (a) · (5) KAD-7/D-2 (*toward* BLOCKER).

---

## 1 · THE THIRTEEN — each with its measurement, discriminator and falsifier

> **Law on every row**: *no trigger closes by inference; each names its measurement; an unrun trigger's
> row is reported **PROVISIONAL at close**, never silently inherited* (G-KFW9-13). The write-back is an
> **ADDENDUM under the ORIGINAL id**, appended, **by `.e` alone** — E-1 (existing bytes immutable) /
> E-3 (addenda-not-patch). *An addendum that edits an existing line, or mints a new id, or lands from
> any seat but `.e`, is a bounds expansion → **S-13 TRIUMVIRATE**.*

### E-1 · `KC-25` → MAJOR (z-modal punch-through)
- **Current grade**: provisional. **Ceiling**: MAJOR.
- **Measurement**: the modal punch-through, observed, on the z-modal surface.
- **DISCRIMINATOR**: the contenteditable at `kf-KeyframeCard.md:67` sets **no** `spellcheck="false"` /
  `autocorrect="off"` / `autocapitalize="off"` (`:41-50` verified). On **iOS**, autocorrect and
  autocapitalize **rewrite the text before `@input` reads it** — so the tell is a *mutated value*, not
  a red squiggle. A desktop squiggle does not discriminate; the iOS mutation does.
- **FALSIFIER**: text that survives an iOS entry byte-identical kills the compounding claim against
  KF-KC-16/17.
- **Cell**: `safari-app/ios-device` (`.c`, S-8 family (v)).

### E-2 · `KF-ES-3` → **BLOCKER** (`jump-none` faults render; decides L-i2's ordering)
- **Current grade**: MAJOR at the consumer surface, **BLOCKER dissent** banked. **Ceiling**: BLOCKER.
- **Measurement**: `steps(1, jump-none)` reached through the shipped `step-start` seed, inside the
  glass-ui EasingPicker the scene mounts by default.
- **DISCRIMINATOR**: the throw must land in the **RENDER path** (the `easingFn` computed feeding both
  the picker's `value` computed **and** its emit watcher — *render and emit both fault*), not merely in
  an event handler. A caught/handled error, or one confined to the emit path, does not discriminate.
- **FALSIFIER**: a rendered picker that degrades (no throw) — e.g. a producer-side domain constraint
  already shipped — kills the BLOCKER dissent and collapses the row to its banked MAJOR.
- **Ownership note**: **GLASS-OWNED** (BH relay) — the cure is the producer's; *value.js is
  CSS-spec-correct and needs no change*. **This wave measures; it authors no producer byte.**

### E-3 · `D-M3` branch (a) → **BLOCKER**
- **Current grade**: MAJOR (confirmed core). **Ceiling**: BLOCKER **if branch (a)**.
- **Measurement**: `kf-SpringHeatmap.md:22` — *"The SS-13 probe (residue #1) must log
  `getPropertyValue` AND `ctx.fillStyle` to pick the branch; **branch (a) escalates the row to
  BLOCKER**."*
- **DISCRIMINATOR**: **both** values logged together. (a) classical `getPropertyValue` + a canvas
  parser that **rejects** `light-dark()` ⇒ every assignment a silent no-op, the field is 400 opaque
  black rects ⇒ **BLOCKER**. (b) a parser that **resolves** `light-dark()` against the canvas's used
  `color-scheme` ⇒ the `:270` repaint is exactly what re-tints. (c) css-values-5 arbitrary-substitution
  semantics ⇒ `getPropertyValue` returns the **resolved arm** and D's byte-identity premise itself dies.
  **Logging one of the two values cannot pick a branch** — and a third such pass HALTS under S-13.
- **FALSIFIER**: any branch other than (a) forecloses the BLOCKER.
- **Independent of branch, already CONFIRMED** (not re-probed): zero validation/read-back/fallback on a
  silently-ignorable assignment; the producer naming the hazard in a shipped docblock
  (`resolveCanvasColor.d.ts`); `amiga/utils.ts:3-13`; the theme-blind surface operand under ruling 1's
  race.

### E-4 · `kf-TimelineTrack` **M-7 / missed-4** (`deltaY: 0` ⇒ joint MAJOR) **+ D-4** (label collision)
- **ONE armed row, ONE measurement, TWO banked ids** — the composition that made §H's *"fourteen"*
  false. Both ids receive the addendum; neither is renamed, merged or dropped.
- **DISCRIMINATOR**: a wheel event with **`deltaY: 0`** (not merely a small deltaY) reaching the track
  handler; and, separately observable in the same capture, the **label collision** at the rendered
  overlap.
- **FALSIFIER**: a handler that filters `deltaY === 0` before acting kills the joint MAJOR; labels that
  reflow rather than collide kill D-4.

### E-5 · `KAD-7 / D-2` → toward **BLOCKER** (`pre[contenteditable].tabIndex`, PER ENGINE)
- **DISCRIMINATOR**: the **computed `tabIndex`** of the `pre[contenteditable]` **read inside each
  engine cell separately** — I-20 applies in full: a `webkit-engine` reading may **not** be written into
  a `safari-app` column. The row is per-engine by construction.
- **FALSIFIER**: a `tabIndex` that is reachable and consistent across the three engine cells forecloses
  the escalation.
- **Cells**: `safari-app/desktop` · `webkit-engine` · `chromium` — **three separate rows.**

### E-6 · `KF-SST-15 / -16 / -17` (kf-StartingStyleTarget)
- **DISCRIMINATOR**: the `@starting-style` entry transition's **rendered first frame**, stamped
  (KF-AX-4) — an un-stamped frame cannot be placed in the entry sequence and does not discriminate.
- **FALSIFIER**: an entry that renders its start value (rather than skipping to the end) forecloses all
  three.
- **Rider**: the `data-allow-motion` producer-inert policy is decided **ONCE demo-wide** (KF-SST-13,
  third witness; S-3) — not re-decided at this row.

### E-7 · `KF-TFP-7` (via `KF-CO-5`)
- **DISCRIMINATOR**: the observation must run **through KF-CO-5's path**, not the panel's own; a
  reading taken off the direct path does not carry the escalation.
- **FALSIFIER**: the path measured inert forecloses the promotion.

### E-8 · `KF-TD-8` (kf-**TypingDots**) — INFO-forever **or** promote
- **Measurement**: **one `performance.measure`.** The cheapest trigger in the register; the whole row
  turns on a single number.
- **DISCRIMINATOR**: the measure must be taken **on the lane the substrate actually runs**. The
  **TypingDots INVERSION** is live here (§H's witness-substrate law): at the disqualified `8281638c`
  the dots stop (rAF lane); at `origin/master` they keep pulsing (WAAPI lane). **A measure taken on the
  wrong lane answers a different question.** Substrate stamp `55e9bf0d` is mandatory on the row.
- **FALSIFIER**: a cost below the felt threshold pins the row **INFO-forever**; above it, the row
  promotes.

### E-9 · `KF-AV-41` — a live counterexample promotes
- **DISCRIMINATOR**: an actual **counterexample instance**, captured, not an argument that one could
  exist.
- **FALSIFIER**: absence of a counterexample across the captured band leaves the grade where it stands
  — **absence is recorded as absence, never as a green.**

### E-10 · `KAD-F4` — R-A's MAJOR is **revivable**
- **DISCRIMINATOR**: the **autocapitalize / autocorrect mutation inside `pre[contenteditable]`**, on a
  **real iOS device** (G-KFW9-11's own close condition). A desktop cell cannot revive this MAJOR.
- **FALSIFIER**: no mutation on real iOS keeps R-A's MAJOR dead.
- **Cell**: `safari-app/ios-device` (`.c`); **governed by the KF-AV-28 rider** (§H) — the witness is
  lawful now; a *cure* is not this wave's to spend.

### E-11 · `KF-ET-8`'s contested **dark arm**
- **DISCRIMINATOR**: the **dark** arm specifically, re-derived **at capture** (KF-SKEL-22 — *no corpus
  numeral is the figure of record*). A light-arm reading does not decide it.
- **FALSIFIER**: a dark-arm contrast numeral above the threshold forecloses the contest.
- **Trap adjacency**: `kf-EasingTarget`'s **KF-ET-24** is a *separate* row and a **named trap** — see
  NEGATIVE-REGISTER T-3. Do not conflate them.

### E-12 · `N-5` (kf-SpringTrace) — decides MINOR-vs-MAJOR; **the cheapest probe in that record**
- **Measurement**: `kf-SpringTrace.md:29` — the rail wrapper at `:55` carries **no `shrink-0`** while
  every sibling does (`:33`, `:134`, root `:9`), so the column **can** compress on that one member down
  to its min-content; below it, `justify-center` + `overflow-hidden` (**not `auto`**) clip both ends
  symmetrically with **no scroll affordance and no recovery**.
- **DISCRIMINATOR**: the **bite point** — the viewport height at which clipping begins. *"Threshold
  pushed further out than A claimed; bite point UNPROVEN."* A reading that only confirms "it clips
  eventually" does not discriminate MINOR from MAJOR.
- **FALSIFIER**: a bite point outside any reachable viewport keeps the row MINOR.

### E-13 · `L-M-10 / N-3` **+** `KF-TFP-19` — the INP hooks (two records, one class)
- **Measurement**: a **felt INP cost**, measured — not the existence of the waste, which is already
  banked.
  - `kf-KeyframesStringControls.md:159` — *"the N+1 prettier formats and the per-pause localStorage
    serialize; the MINOR grades carry an escalation hook if this measures material."*
  - `kf-TimingFunctionPanel.md:128` — *"the per-pointermove whole-bucket serialize; the MINOR grade
    carries the escalation hook."*
- **DISCRIMINATOR**: **INP** (the interaction's own latency), not total work. Both rows are graded on
  the **kf-ChannelControls precedent** — *"runs and does nothing" ≠ breakage; recurring waste on a
  debounced path books MINOR until SS-13 measures a **felt** cost.* A CPU-time figure alone does not
  promote either row.
- **FALSIFIER**: an INP inside the good threshold pins both at MINOR and **retires the hooks**.

---

## 2 · THE WRITE-BACK PROTOCOL (G-KFW9-13; `.e` alone)

For each of the thirteen, at close, `.e` appends to the record that banks it:

```
## ADDENDUM 2026-09-17 (X.KF.W9 · G-KFW9-13) — <ORIGINAL ID>
measured:     <the value>                     cell: <roster cell>
substrate:    55e9bf0d…   bundle: <sha256>    capture: <path>  shot-sha256: <sha256>
resulting severity: <grade>   |   PROVISIONAL (unrun) — precondition: <exact precondition>
```

**Hard rules.** Under the **ORIGINAL banked id**, never a new one · **appended**, never an edit of an
existing line · **by `.e` alone** · a trigger that did not run is written **PROVISIONAL with its exact
unmet precondition**, never inherited and never silently dropped. **Row (4) writes to BOTH of its ids
from ONE measurement.**

---

## 3 · READING AT THIS SEAT

**All thirteen stand UNMEASURED.** ⟨`grep -rl 'PROVISIONAL' …/registry/adjudicated/kf-*.md | wc -l`⟩ →
**0** records carry a provisional-severity resolution; ⟨`grep -rl 'ADDENDUM.*2026-09-17' …/kf-*.md`⟩ →
**0** W9 addenda banked. `.a` publishes the register; it measures no trigger and closes none.
