SERVED MODEL: claude-opus-5-5

# KF.W13U.x — cause derivations for the four e2e reds (read-only probes over kf `dist/gh-pages`, headless Chromium via value.js Playwright)

Probes: `p-cube.mjs` (cube autoplay, pause, nested targets) · `p-ring3.mjs` (focus-ring paint across glass Button emphasis) · `p-m1.mjs` (mobile sheet scroll chain). Run: `KF_PLAYWRIGHT_DIR=<value.js> node <probe>` from the keyframes.js root.

| red | cause | owner | disposition |
|---|---|---|---|
| usability | KF-EST-3 inlined the hero title as a template literal, so `usability.mjs:91`'s declared-title read (`/\btitle:\s*"…"/`) found nothing and threw | demo (`EditorStartScreen.vue`) | CURED at kf `c1ce06e8` (declared once as `hero.title`, and the template renders it) |
| subject-animates `[real-cube]` | the oracle assumed the playhead reads 0 before Play. `.w` made `#/cube` auto-play, so the read raced the autoplay. Its corroborator read `.graph > div`, which carries no engine write since `.w` (it read 0 distinct values while passing) | oracle | RE-SEATED at kf `68c80e79` |
| live-session S4 `enterToggled`/`spaceToggled` | the same autoplay premise: the first Enter PAUSES | oracle | RE-SEATED at kf `d2bc0f76` (reads a toggle) |
| live-session S4 `ringPainted:false` | glass 7.0.0 `components/button` `.button[data-emphasis="quiet"] { box-shadow: none }` ties `.focus-ring:focus-visible { box-shadow: var(--focus-ring-shadow) }` at (0,2,0) in `@layer components` and wins on source order. The transport Play (both faces, `TransportDock.vue:48`/`:204`) is `emphasis="quiet"`. `p-ring3`: quiet → `none`, text → `none`, primary/secondary/tertiary/unset → the ring paints. Every quiet glass Button in the demo (Scene, Edit easing curve, Back to controls, Decrease/Increase) has no ring | **producer (glass 7.0.0)** | ESCALATED. glass `v10.0.1:src/styles/utilities/base.css:144` paints the ring as an `outline`, which the quiet subtraction (`box-shadow: none`) no longer touches → re-read at KF.W13R |
| live-session S5 spring INTERACT | T.G4 `562ced31` moved every spring painter to `style.transform = translateX(<cqw>)`; the oracle read `style.left` | oracle | RE-SEATED at kf `3b1dbd8f` |
| live-session-mobile M1 SCROLL | glass 7.0.0's snap-point Drawer is a full-height sheet (`.glass-drawer[data-glass-drawer-snap-points="true"] { height: 100% }`) translated to the detent. At the demo's maximum detent (0.36) the sheet runs to y=1228 on an 844 px viewport: `.controls-pane` = [524, 1228], 704 px tall, 765 px content. The oracle's swipe at the pane's rect centre (y=876) lands BELOW the viewport. A swipe at a visible point scrolls the pane its full 61 px overflow (`p-m1`: scrollTop 0 → 61), but ~384 px of the pane never enter the viewport at any scroll position | **producer (glass 7.0.0 Drawer geometry; the "UPSTREAM GLASS 5 OVERLAP" row the oracle already reports)** | ESCALATED. glass `336dacf9` "W-DIALOG-DETENT — the detent is a size, the drawer folds in whole" (an ancestor of `v10.0.1`, not of `v7.0.0`) replaces the translated Drawer with a Sheet whose detent is a size → re-read at KF.W13R. Moving the oracle's touch point into the visible band would green M1 while the content stays unreachable, so it is NOT done |
