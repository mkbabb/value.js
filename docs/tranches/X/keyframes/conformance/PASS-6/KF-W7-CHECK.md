# KF.W7 — FRESH ADVERSARIAL SPEC CHECK (L-18/L-20, pass 6)

**Subject**: `/Users/mkbabb/Programming/value.js/docs/tranches/X/keyframes/waves/KF-W7.md` (443 L, **249,757 B**, mtime **2026-08-29T15:18:37**, `sha256 ed5f0851…6f21`) — five repair rounds deep.
**Seat**: FRESH. Nothing is inherited. `PASS-5/KF-W7-CHECK.md`, `PASS-5/RULINGS-5.md` and `PASS-5/CLOSE-CERT-2.md` are read as **claims to be tested**, never as findings. The census below is re-derived line-by-line from the 58 `kf-*.md` records' own bytes at `docs/tranches/V/megatranche/registry/adjudicated/`. Sole X·KF carry consulted: `carry/KF-W6-CARRY.md`.
**Tree law**: every keyframes witness re-executed READ-ONLY at `origin/master 81a56990736ced5b5edde0b84c527680ac7689b1` (`git -C /Users/mkbabb/Programming/keyframes.js rev-parse origin/master`, re-verified this seat). Local `8281638c` disqualified; the dirty worktree never read as substrate.
**Sole write**: this file. No product source opened; no gate stamped; no registry byte touched; no PASS-1..PASS-5 artifact edited; no spec repaired.
**Verdict**: **DEFECTIVE** — carriage is **CLEAN by bytes (0 escaped under every admissible reading)**, the frontier axis is **exemplary** (18 of 18 §Bounds counts and 12 of 12 gate-witness commands reproduce EXACTLY), but **14 defects** stand, headed by **a census figure whose arithmetic contains a phantom operand and whose two cells contradict each other** (seventh consecutive generation), **a receipt about `KF-W4.md` that has been false at that sibling's bytes since round 3**, and **LAW E(4) applied to one of its two receipt classes**.

---

## §0 · LAW E(2) — THE FIRST ACT: RE-HASH ALL ELEVEN

`CLOSE-CERT-2.md` §8(4) makes this the pass-6 seat's first act and §9 the challenge. Run this seat, `shasum -a 256 docs/tranches/X/keyframes/waves/KF-W*.md`:

| spec | closing sha256 ⟨cert §9⟩ | this seat | match |
|---|---|---|:--:|
| KF-W0 · W1 · W2 · W3 · W4 · W5 · W6 · W7 · W8 · W9 · W10 | `fd226ecb…` `f6986de7…` `5f3656e3…` `4b68baf3…` `4028327c…` `8baba077…` `b41e5576…` `ed5f0851…` `c7c11445…` `bea2d9a6…` `1c107329…` | identical, all eleven | **✓ 11/11** |

**NO MISMATCH. The round-5 write-set is provable and no spec moved after the certificate.** LAW E(1)/(2) discharged — the round closed lawfully and its repairs bank. *This is the first round in six whose close instrument survives its own successor's test, and it is recorded first because five predecessors did not.*

---

## §1 The five axes — verdicts

| # | axis | verdict | one-line basis |
|---|---|---|---|
| **1** | **ID-KEYED CENSUS by record** | **AMBER** | **134 routed · 134 booked · 0 escaped** by this seat's independent re-enumeration (127 roster + 7 non-roster). Every roster identity resolves to a carried row, an §Excluded bullet with a named owner, or a named precondition. **Carriage is CLEAN.** The AMBER is the denominator: the cited **135** does not re-derive — its `− 2 + 1` carries a **phantom +1** (**D1**), and the spec's own two cells describe the delta as −2 and as −1 (**D1**). |
| **2** | **RECEIPT REALITY / ANCHOR STABILITY** | **RED** | **The drift class is not dead; it moved siblings.** `KF-W4.md` *"says 'ten'"* is **false at W4's bytes and has been since round 3** (**D2**). The §LAW-C **BACKWARD** block is unrepaired round-4 text: four stale per-seat stamps, four false coordinates, under *"Verified this round"* (**D3**), and hard-ordering item 9's two quoted coordinates resolve at **neither** number (**D4**). One pasted grep prints 1 of its 2 lines (**D9**). Offsetting: **12 of 12** frontier commands and **8 of 8** registry quotations re-run EXACT, and the file's two internal scope-receipt greps reproduce **hit-for-hit**. |
| **3** | **M-25 DEPTH + MECHANISM S (LAW F)** | **AMBER** | **The shadow discipline is genuinely applied**: the round's struck set is enumerated with per-item dispositions and its four notified termini named, and **no row moved home** — verified by carriage, not by assertion. But the SHADOW LINE's own set-size (*"all 8 non-roster identities"*) inherits D1's phantom (**D12**), and the fold-dedupe enumeration the rule discharges against prints **18** coordinates under a **17** heading with no counting rule at the receipt (**D10**) — PASS-4's *"16"* listed 17, so the off-by-one was inherited, not cured. |
| **4** | **GATES · reachable GREEN · LAW-A · corpus operands · LAWFUL LANDING SURFACES** | **AMBER** | 15 gates, **all born-RED, none stamped** (45 `RED` tokens, `GREEN` **0**), every one with reachable green; **18 of 18** §Bounds line counts EXACT; **both LAW-A censuses reproduce command-for-command**; the access column enumerates **24 of 24** rows path-qualified. But **G6's fourth leaf is not path-qualified and names an 80-line file for a `:152` coordinate** while the SUBJECT-IDENTITY receipt certifies 4 of 4 (**D7**), and **three P0 rows spend on surfaces with no §Bounds row and no out-of-bounds declaration** (**D8**). |
| **5** | **POSTURE** | **GREEN-WITH-ONE** | W4 head ✓ (`KF-W4.md:55`/`:59` live; W8 §State `Opens after` names KF.W4) · W3 gated ✓ (`KF-W3.md:1` = *"# KF.W3 — Parser Consumption (GATED, never scheduled)"*) · **O-21 → 0 hits, correctly not this wave's surface** ✓ (it is KF.W1↔KF.W10's mail-cure instrument, `KF-W10.md:533`) · **§6.D SUCCESSOR-FORMATION REGISTER resolves at `KF-W10.md:573` with all three rows** — KF.W11 `:588` (sequence · square cargo), KF.W12 (CARD-UNIT · KFED-UNIT · EDITOR-UNIT), KF.W13 (dock-menu · transport/ribbon) — **exactly the three addressees this file re-points to** ✓ · **§G-2's Acceptance alphabet at `:407` byte-identical** to the adopted OUTPUT FORM ✓ · **EH-4/5/8**: 0 hits, correctly — the restoration is KF.W6's and this wave is no terminus of it ✓. **COHESION §0d is not cited here (0 hits) and is not owed here** — R5-12 homes that citation at KF.W10, and it is landed there. The single posture blemish is D2's stale W4 characterization. |

---

## §2 Census — id-keyed, re-derived at the registry bytes

**Counting rule, restated so a later seat re-runs it rather than inherits it** (identical in substance to the rule the spec now writes out, with the seam that produced the delta made explicit):

1. **RECORD-SET** — the 58 `kf-*.md` adjudicated records. Nothing else is a source.
2. **MEMBERSHIP — the OWNER test, governing**: a row is ROUTED iff **KF.W7 is the owner named in its own terminal disposition**, bare or in a `KF.Wn/W7` dual-home. A `KF.W7` appearing only as a cross-ref, an evaluation-input pointer, or a rider-governance clause on a `NO-WAVE-OWNER`/other-wave row is **not** a routing.
3. **CONTEXT IS NEVER A ROUTING** — routing-law preambles, method paragraphs, ruled-table cells, closing tallies, prose mentions.
4. **GRANULARITY** — a banked MERGED multi-id header counts ONCE at its banked identity.
5. **IDENTITY DEDUPE (M-25)** — a FOLDS/identity-guard line discharging to an already-counted banked id is fold-carried, not a new routing; a clause riding INSIDE an already-counted row is not a second identity.
6. **BOUNDARY (the tail discriminator the spec now states, applied)** — a routing-summary tail is a non-roster routing **iff it carries an obligation not otherwise booked**.
7. **`KF.W7-TOKENS`** is OUT by mechanism; **`KF.W6-TIMELINE`** dispositions are IN as named preconditions.

*Note on the sweep pattern, stated because it is the difference between 127 and 126:* `grep -E 'KF\.W7'` **misses the dual-home spelling `KF.W6/W7`** (four live rows). The enumeration below is derived from `grep -nE 'W7'` over all 58 records, then classified.

### §2a · Roster-owned routings — enumerated by line, verified this seat

| record | routed | lines (this seat's enumeration) | carried | excluded-with-owner | escaped |
|---|--:|---|--:|--:|--:|
| `kf-KeyframeTimeline.md` | **40** | `:42 :43 :44 :45 :46 :47 :48 :49 :55 :56 :57 :59 :60 :61 :62 :63 :64 :65 :66 :71 :72 :73 :74 :75 :76 :79 :80 :81 :82 :84 :86 :87 :91 :95 :96 :97 :98 :99 :100 :102` ⟨`:16` `:18` `:170` = header/preamble/tally⟩ | 40 | 0 | **0** |
| `kf-TimelineTrack.md` | **30** | `:40 :41 :42 :43 :45 :46 :51 :52 :53 :54 :55 :56 :57 :59 :60 :61 :62 :63 :64 :65 :66 :67 :68 :73 :74 :75 :77 :78 :79 :80` ⟨`:25` `:29` = **ruled-table cells**, rule (3) context, both already counted at `:41`/`:73`⟩ | 30 | 0 | **0** |
| `kf-TimelineHoverPreview.md` | **17** | `:42 :43 :49 :50 :51 :52 :54 :57 :58 :59 :61 :62 :63 :69 :70 :71 :73` (`:61` = MISSED-3, `KF.W6/W7` dual-home — invisible to a `KF\.W7` grep) | 17 | 0 | **0** |
| `kf-TimelineCaret.md` | **14** | `:40 :41 :50 :51 :52 :53 :54 :55 :60 :62 :66 :67 :68 :70` | 14 | 0 | **0** |
| `kf-CSSPasteDialog.md` | **8** | `:41 :42 :45 :59 :61 :63 :64 :65` | 8 | 0 | **0** |
| `kf-KeyframesAddDialog.md` | **8** | `:50 :53 :54 :57 :68 :69 :73 :74` (`:68/:69/:73` = `KF.W6/W7` dual-homes; `:63` KAD-F3 fold-deduped to R-7) | 5 | 3 (KAD-15 · KAD-16 · KAD-20 → **KF.W6**) | **0** |
| `kf-AnimationVisualizer.md` | **2** | `:51` KF-AV-10 · `:72` KF-AV-28 ⟨`:54`/`:60` owner `NO-WAVE-OWNER`, `:68` owner `KF.W8` — rider-governed non-routings; `KF-AV-17` at `:61`/`:105` carries no `W7` token at all⟩ | 2 | 0 | **0** |
| `kf-KeyframeCard.md` | **2** | `:53` KF-KC-11 · `:100` KF-KC-52 | 2 | 0 | **0** |
| `kf-SpringTarget.md` | **2** | `:64` C-4 · `:69` m-8 | 2 | 0 | **0** |
| `kf-CSSCodeEditor.md` | **1** | `:34` KF-CE-2 (parent half, BLOCKER) | 1 | 0 | **0** |
| `kf-RibbonBar.md` | **1** | `:77` RB-7 | 1 | 0 | **0** |
| `kf-SequenceScrubber.md` | **1** | `:58` C·C-4 ⟨`:29` `:87` `:104` = ruled-table / kill-entry / method context⟩ | 1 | 0 | **0** |
| `kf-SpringTrace.md` | **1** | `:62` C-3 ⟨`:48` C-2 = L-3 is `NO-WAVE-OWNER` — re-verified this seat⟩ | 1 | 0 | **0** |
| **subtotal** | **127** | | **124** | **3** | **0** |

**Every one of the 127 was matched to a carried row in `KF-W7.md` by banked id.** Independent spot-verification (rows different from the ones PASS-5 sampled, so the two passes do not corroborate each other by construction): KT `:91` D-22 (P9 coherence group), KT `:95` L-18/C-11 + `:96` L-20/L-21 + `:99` D-25/D-26/D-28 (P9 INFO group), TT `:57` D-14/i-1 + `:68` D-m2 (P4), TT `:79` i-3 (P9 TT INFO group), THP `:69` L-D14 + `:70` L-D15 + `:71` MISSED-6 + `:73` P2-DEAD (P7 hygiene group), TC `:52` L-9/m-8 + `:62` MISS-α4 + `:70` MISS-β4, CPD `:63 :64 :65` R-21/R-22/R-23 (P8 dialog MINOR group) — **all present, at their banked rungs**.

### §2b · Non-roster identities — the boundary applied, not practised

| coordinate | identity | disposition in `KF-W7.md` | counted |
|---|---|---|:--:|
| `kf-AnimationControlsGroup.md:42` | **D-1** — the document-singleton portal BLOCKER (`KF.W6-TIMELINE` precondition) | **OP-0** ✓ | ✔ |
| `kf-ChannelControls.md:40` | **L-2/C-2** — the `:key` remount BLOCKER | **OP-1** ✓ | ✔ |
| `kf-ChannelControls.md:52` | **C-8/D-6** — the empty timeline tabpanel | §Sequencing **item 8** ✓ | ✔ |
| `kf-RibbonBar.md:131` | routing-summary tail carrying the expanded-timeline settlement obligation | §Sequencing 8 + P5 RB-7 ✓ | ✔ |
| `kf-SequenceScrubber.md:134` | routing-summary tail carrying **D-5/D-6/D-11**'s verdict-gated arms | → KF.W11 arm (d) + P0 C·C-4 ✓ | ✔ |
| `kf-CSSPasteDialog.md:115` | residue **#9** — L-4's open falsifier, *"owned by the R-7 fold in KF.W7"* | **G15** assertion + P8 R-7 ✓ | ✔ |
| `kf-PlaybackRibbon.md:157` | the NO-WAVE-OWNER transport/ribbon packet the KF-AV-28 rider governs | §Sequencing packet homing → **KF.W13** ✓ | ✔ |
| `kf-PlaybackRibbon.md:36` | **PR-CAUTION**'s host paragraph | header lock, quoted byte-identical ✓ | **✘ — measured this seat: `Routing law: **KF.W0/W4/W6/W7/W8/W9** per CENSUS-2026-08-03 …` — a routing-law PREAMBLE, the class rule (3) excludes by name.** Carried either way |
| `kf-SpringTarget.md:69` | the cure-caution clause riding m-8 | P0 KF-AV-10 cure-caution lock ✓ | **✘ — this IS §2a's m-8 line.** Carried either way |
| **subtotal counted** | | | **7** |

**Explicit ZERO declarations, recorded and NOT counted** (re-verified): `kf-SequenceScene.md:153` — *"**KF.W7**: none (no export-surface rows in this corpus)"*; `kf-SequenceAxis.md:130` — *"**KF.W6/W7**: no axis-owned rows"*.
**Routing-summary tails NOT counted, per rule (6)**: `kf-SpringTrace.md:139` · `kf-SpringTarget.md:146` · `kf-SequencePlayhead.md:151` — each restates an identity already counted at §2a or fold-carried at §2c and adds no obligation. **No tail is newly admissible under the round-5 discriminator; both admissible tails were already among the nine PASS-4 booked.**

### §2c · Fold-carried lines resolved by identity — **18** coordinates, not 17

`kf-KeyframeTimeline:106` (D-2/L-2/C-2 ≡ **R-3**) · `kf-KeyframeTimeline:114` (Cross-refs; the OP-1 identity) · `kf-TimelineTrack:86` (C-2 ≡ **C-1**) · `kf-TimelineCaret:74 :75 :77 :78` · `kf-TimelineHoverPreview:77 :80 :81 :82 :84 :86 :88` · `kf-SequencePlayhead:92 :143` · `kf-SequenceScrubber:128` (≡ KF-AV-28) · `kf-SpringTarget:135` (m-8 ≡ KF-AV-10). **18 coordinates → 0 new routings** (every target carried). All eighteen re-read at the bytes this seat. See **D10**.

### §2d · Totals

| | this seat | the spec's cited figure |
|---|---|---|
| **routed** | **134** (127 roster + 7 non-roster) | 135 |
| **booked** (124 carried + 3 excluded-with-named-owner + 7 non-roster) | **134** | 135 |
| **escaped by bytes** | **0** | 0 |

*Under the spec's own printed rule the figure is **134**. **Escaped is 0 under every reading** — 134, 135 or 136. The delta is a denominator and never a drop, and both contested identities are carried by the spec regardless. But the spec's own arithmetic does not close: see **D1**.*

---

## §3 Witness re-execution — every command run by THIS seat

`git -C /Users/mkbabb/Programming/keyframes.js rev-parse origin/master` → `81a56990736ced5b5edde0b84c527680ac7689b1` ✓ · `HEAD` → `8281638c` (disqualified; worktree dirty, never read).

### §3.A · §Bounds line counts — **18 of 18 EXHAUSTIVE, all EXACT**

`KeyframeTimeline.vue` **312** · `TimelineTrack.vue` **246** · `TimelineCaret.vue` **70** · `TimelineHoverPreview.vue` **38** · `useTimeline.ts` **129** · `useTimelineBuild.ts` **200** · `useTimelineOps.ts` **88** · `useZoomPan.ts` **110** · `timelineTypes.ts` **41** · `index.ts` **8** · `timelineEngine.ts` **102** · `snapshotCapture.ts` **66** · `flattenVars.ts` **33** · `CSSPasteDialog.vue` **80** · `KeyframesAddDialog.vue` **161** · `SequenceScrubber.vue` **162** · `AnimationVisualizer.vue` **256** · `keyframeSelector.ts` **31**. **Not a sample — the whole column.** The `timelineEngine` 102-at-origin correction holds.

### §3.B · Gate witnesses — **12 of 12 sampled reproduce, most to the line**

| gate | command | result |
|---|---|---|
| **G1** | `wc -l …/glass-ui/dist/components/timeline/index.d.ts` · `ls` · `grep SliderVariant` | **2 lines**; **nine** files, the **seven** un-re-exported exactly as enumerated; `SliderVariant = "standard" \| "spectrum"` at `types.d.ts:4` ✓ |
| **G2** | `git show 81a56990:…/KeyframeTimeline.vue \| grep -n scrub` | **78 · 83 · 197 · 202 · 224**; bare `scrub` among NONE ✓ |
| **G3** | `TimelineTrack.vue:174-178` / `:192-196` | `onMarkerPointerDown` records **no** offset; `onTrackPointerMove` emits the absolute percent ✓ |
| **G4** | `useTimelineOps.ts:56-62` | `moveKeyframe` → `rebuild()` unconditional, **no early-out** ✓ |
| **G5** | `timelineEngine.ts \| nl -ba` | merge loop **`:37-47`**, key **`:38`**, merge comment **`:40`**, property loop **`:42`**, build **`:49-51`** — **EXACT, all five** ✓ |
| **G6** | four leaves | `importCSSToTimeline` decl `timelineEngine.ts:76`, import `useTimelineBuild.ts:12`, await `:146` ✓ · `easing?` `timelineTypes.ts:10` ✓ · `captureSnapshot` decl `snapshotCapture.ts:8`, call `useTimelineOps.ts:29` ✓ · **the fourth leaf resolves at `useTimelineBuild.ts:152`, NOT at the file the gate names — D7** |
| **G7** | `TimelineCaret.vue:22` · `:58-65` | `@blur="commitEdit"`; `commitEdit` compares nothing ✓ |
| **G8** | `git grep -n onMarkerKeydown 81a56990 -- demo/` | `TimelineTrack.vue:82` (binding) + **`:203`** (decl) ✓ |
| **G9** | `TimelineTrack.vue:86` | `<TooltipContent side="top" :side-offset="8" class="p-2 max-w-56">` — `ariaLabel` unpassed ✓ |
| **G10** | `previewCache` census · `grep -c delete` | `:81` passed · `:217` decl · `:221` read · `:226` write; `delete` → **0** ✓ |
| **G11** | leg (i) over `test/` · leg (ii) over `demo/` | **0 hits** / **13 hits**, and **all thirteen coordinates match the spec's printed enumeration exactly** ✓ |
| **G13 · G14 · G15 · OP-4** | `useZoomPan \| grep -n delta` · `useTimelineBuild \| grep -n 'toast\|console.error'` · `initialText` · `package.json` | `:50` + `:59` only, `deltaX` **0** ✓ · `console.error` **`:48`**, toasts **`:121 :133 :137 :148 :155 :157`** ✓ · `initialText` four hits `:49 :53 :59 :67`, `:initial-text` at **neither** mount ✓ · `package.json:81` `"@vitejs/plugin-vue": "^6.0.7"`, `test-utils` → **0**, `vitest.config.ts` `plugins` → **none** ✓ |

### §3.C · LAW A — both censuses re-executed independently, **COMMAND-FOR-COMMAND**

**(1) `flattenVars.ts`**: `git grep -nF '../utils/flattenVars' 81a56990 -- demo/ test/ src/ scripts/` → **1** (`useTimelineBuild.ts:14`). `git grep -n '\bflattenVars\b' …` → **4** — `useTimelineBuild.ts:14` (import) · `:170` (call) · `flattenVars.ts:10` (decl) · `:26` (self-recursion). **CONSUMER SET = exactly one module** ✓.
**(2) `KeyframesAddDialog.vue`**: `git grep -nF 'components/KeyframesAddDialog' …` → **exactly one live import**, `KeyframesEditor.vue:118`; symbol census → mount `KeyframesEditor.vue:75` + one docblock (`useToolbarKeyboard.ts:24`). **CONSUMER SET = one module, and it is outside §Bounds** ✓.

**LAW A remains the strongest material in this spec.** Neither census is inherited; both reproduce exactly; the `loadPreset` re-export consequence and the out-of-bounds repoint consequence are both real and both correctly binding. The access column re-enumerates at **24 rows**, every one path-qualified, exactly one carrying a delete/repoint verb, its census present at the row.

### §3.D · Registry- and sibling-side receipts

- **Taxonomy pair, both re-run**: `grep -rn 'KF.W6-TIMELINE' adjudicated/*.md` → **8 hits / 4 records** ✓; `grep -rniE 'disposition:.*KF\.W6-TIMELINE' …` → **exactly three rows in two records** (`kf-AnimationControlsGroup:42` · `kf-ChannelControls:40` · `kf-ChannelControls:52`) ✓.
- **LAW D quotations, byte-diffed this seat, all four**: `kf-EasingTarget.md:109` (P-2, whole) ✓ · `kf-SpringTarget.md:69` (the gloss, whole, labelled as the gloss; **`wc -w` → 75, `wc -c` → 590** — the spec's corrected figure is exact) ✓ · `kf-TimelineCaret.md:74` (MISS-β3 with its scoping parenthetical) ✓ · `kf-PlaybackRibbon.md:36` (PR-CAUTION's host) ✓.
- **Rung integrity**: `grep -n '^### ' kf-KeyframeCard.md` → `:38 BLOCKER · :47 MAJOR · :71 MINOR · :95 INFO`; `KF-KC-52` at **`:100`**, inside INFO ✓ — the round-4 restoration holds at a second independent reading.
- **Stable-anchor labels**: `grep -c 'kill register'` → **0**; `grep -n 'Killed-claims register'` → **`:116`**; `K-8 · L-15` → **`:125`** ✓.
- **KF.W6 §Excluded, both ends, by banked row header**: `S-10 · KF-KE-21 (+KAD-15)` now `KF-W6.md:394` (PASS-5 read `:360`; round 4 read `:299`) and `KF-W6-CARRY.md:178`; the KAD group row now `KF-W6.md:480` (PASS-5 read `:438`) and `KF-W6-CARRY.md:248`. **The numbers moved a FIFTH time; the banked row headers held.** The spec carries no number here — the idiom is earning its keep.
- **KF.W5 reciprocity**: `grep -c` for each of the four fixtures in `KF-W5.md` → **2 · 2 · 2 · 2** ✓; `highlight-css-roundtrip` → 6 ✓.
- **KF.W10 forward anchors, all four, at W10's FINAL bytes**: `grep -n 'DISCHARGED by KF.W7 SWAP verdict' KF-W10.md` → **`:276` `:407` `:530`** ✓ · §G-2's Acceptance clause at `:407` carries the adopted OUTPUT FORM **byte-identical** ✓ · §3.2's `KF-AV-28 · STANDING SUPERSESSION RIDER` bullet at `:274` ✓ · **§6.D · SUCCESSOR-FORMATION REGISTER at `:573` with rows 1/2/3 = KF.W11 · KF.W12 · KF.W13**, cargo matching the three edges this file re-points ✓.
- **KF.W8 anchors**: §Bounds `package.json` row `:96` ✓ · `vitest.config.ts` row `:97` ✓ · the nine-tracked row `:99` ✓ · `resize-tracks.test.ts` row `:100` ✓ · create rows `:133` `:134` `:135` — **THREE rows, four filenames** (see **D6**).
- **The file's own scope-receipt greps, re-run**: `grep -n 'KF-W8\.md\|KF\.W8' KF-W7.md` → **29 hits at exactly the 29 lines the receipt prints** ✓ · `grep -n 'PASS-4/KF-W7-CHECK' KF-W7.md` → **4** (`:402 :417 :431 :437`), matching the receipt's *"2 history + this ledger's two records"* ✓. **Both internal receipts reproduce hit-for-hit — the R5-11 instrument works where it was applied.**
- **Gate/status posture**: 15 gate headers, **45 `RED` tokens, `GREEN` 0** — all born-RED, none stamped; `Status: planned` ✓.

---

## §4 Defects — 14 (8 MAJOR · 6 MINOR), ranked

### **D1 · MAJOR — the census figure still does not re-derive from the rule the spec writes out. Seventh consecutive generation, and this time the arithmetic contains a PHANTOM OPERAND and the file's two census cells contradict each other.**
`KF-W7.md:13` prints the derivation R5-11 mandates: *"**Nine non-roster entries − 2 + 1** (the routing-summary-tail seam that rule (1) now states outright, below) **= 8**; **127 + 8 = 135**."*
- **The `+1` has no referent.** Rule (1)'s new discriminator names exactly two COUNTED tails — `kf-RibbonBar.md:131` and `kf-SequenceScrubber.md:134` — and **both are already inside the nine** (verified this seat at `PASS-4/KF-W7-CHECK.md` §1b rows 4 and 5). The three NOT-COUNTED tails stay uncounted and the two zero declarations stay recorded. **No entry is newly admissible.** The correct derivation under the spec's own rule is **9 − 2 = 7**, and **127 + 7 = 134**.
- **The cited artifact is itself contradictory and the contradiction was re-issued untested.** `PASS-5/KF-W7-CHECK.md` §1b lists nine rows, marks **exactly two ✘**, and then prints *"subtotal counted **8**"*; its §1d footnote concedes *"under a strict reading of rule (3) that also drops `kf-PlaybackRibbon.md:36`, the figure is **134**"* — i.e. its **135 requires counting a row its own table struck**. R5-11(2) orders the checks to *"test receipt arithmetic before testing scope"*; the spec's round-5 seat re-issued the figure and re-derived a different phantom to reach it.
- **The file's two census cells disagree.** `:13` computes −2 +1 = **−1**; `:366` (§Excluded) states *"The **−1** against PASS-4 is the roster/non-roster BOUNDARY question — **one double-counted line** (`kf-SpringTarget.md:69`) and **one routing-law preamble** (`kf-PlaybackRibbon.md:36`)"* — **two removals labelled −1**, with no `+1` anywhere.

**Carriage is unaffected — 0 escaped under every reading, and both contested identities are carried regardless.** But the round-5 cure's own claim was that the boundary rule ends the drift; the boundary rule is now written and the figure still does not follow from it.
**Receipt**: `KF-W7.md:13` and `:366` · `PASS-5/KF-W7-CHECK.md` §1b (9 rows / 2 ✘ / subtotal 8) and §1d · `PASS-4/KF-W7-CHECK.md` §1b rows 4–5 (both tails already booked) · this seat's §2b (7).

### **D2 · MAJOR — a live present-tense claim about `KF-W4.md` that has been FALSE at that sibling's bytes since repair round 3. The D1 class, retired "as a class", recurring at a different sibling in the same file.**
`KF-W7.md:79` asserts: *"⟨`KF-W4.md` §Disjointness says **"ten"** while enumerating nine names and listing `resize-tracks.test.ts` twice; RULINGS-3 §END DH-3 routes that correction to the KF.W4 seat, and it is named here rather than silently reconciled⟩"*.
Re-run at `KF-W4.md`'s FINAL bytes, `sed -n '93p'`: *"At `81a56990` it holds **NINE tracked files — cardinality corrected at round 3 (D-3/DH-3)**, `git ls-tree -r --name-only origin/master -- test/demo/instrument | wc -l` → **9**, enumerated: …. **The round-2 spelling said "ten" by listing `resize-tracks.test.ts` twice** … **The double-count is struck**."*
**W4 discharged the routed correction at round 3.** This file has carried it as an open, present-tense finding for **three rounds**, and it survived round 5 for a mechanical reason the round itself names: the D1/D12 conversion enumerated its class with `grep -n 'KF-W8\.md\|KF\.W8' KF-W7.md` — **keyed on the named sibling, not on the stated scope**. This is the file's own quoted lesson (*"a repair that cures the named instance and not the stated scope leaves the law green over its own counterexample"*) firing one sibling over, in the round that quoted it twice.
**Receipt**: `KF-W7.md:79` vs `sed -n '93p' KF-W4.md`, both read this seat at final bytes.

### **D3 · MAJOR — LAW E(4) is applied to ONE of the file's two receipt classes. The BACKWARD block is unrepaired round-4 text: four stale per-seat substrate stamps and four false coordinates, under the words "Verified this round".**
`KF-W7.md:423` opens the round-5 ledger: *"**LAW E(4) governs every cross-wave receipt below — anchor-only, and per-seat substrate stamps are retired**."* LAW E(4) is unrestricted: *"**Every** same-round cross-wave receipt written by a per-wave seat is anchor-only — no quoted sibling prose, no line numbers, **no per-seat mtime stamps**."*
`KF-W7.md:343` — the BACKWARD half of the LAW-C partition — is untouched round-4 prose and reads: *"**Verified this round**: **KF-W2** (`:710`/`:753`, **mtime 17:02:55**) · **KF-W4** (`:54`/`:58` + §Commits commit 2, **mtime 16:59:44**) · **KF-W5** (**mtime 16:55:55**) · **KF-W6** + **KF-W6-CARRY** (**mtimes 16:59:25 / 10:49:45**)"*.
Measured this seat: **all four stage-1 siblings wrote again in round 5** (`stat` → `KF-W2/W4/W5/W6.md` all **2026-08-29 15:17:38**), and **all four coordinates are false** — W2's two sentences are at `:782`/`:825`, W4's two rows at `:55`/`:59` (a correction **this same file makes at `:31`**, so the contradiction is internal). The FORWARD half was re-cut with a four-part test and a scope receipt; the BACKWARD half received nothing.
**Receipt**: `KF-W7.md:343` vs `:31` (which prints `:55 :59 :166 :212`) · `stat -f '%Sm %z' KF-W2.md KF-W4.md KF-W5.md KF-W6.md` · `grep -n 'KF.W7 may not decide' KF-W2.md` → `:782` · `sed -n '55p;59p' KF-W4.md`.

### **D4 · MAJOR — hard-ordering item 9's quoted-by-command receipt resolves at NEITHER of its two coordinates, and it is the one hard ordering the file added because a sibling believed it was reciprocal.**
`KF-W7.md:335` prints, under a LAW C(2) label with a named substrate and mtime: *"`grep -n 'KF.W7 may not decide' KF-W2.md` → **`:710`** … `grep -n 'Both directions are now declared at both ends' KF-W2.md` → **`:753`**"*.
Re-run this seat at `KF-W2.md`'s final bytes: **`:782`** and **`:825`**. Both sentences survive verbatim — *"**KF.W7 may not decide a failure posture before KF.W2 publishes the registry**"* and *"Both directions are now declared at both ends"* — so **the ordering is sound and nothing downstream weakens**. What fails is the instrument: the cell's own parenthetical says *"the drift is why LAW C(2) requires the substrate, the mtime and the command, and why the anchor of record is the **sentence**, not the number"* — and it then re-issues the numbers anyway, one round after the law retiring them.
**Receipt**: `KF-W7.md:335` vs `grep -n 'KF.W7 may not decide' KF-W2.md` (→ `:782`) and `grep -n 'Both directions are now declared at both ends' KF-W2.md` (→ `:825`).

### **D5 · MAJOR — the round-5 ledger contradicts itself about its own transcripts, and its second cell re-issues a figure its first cell struck in the same round.**
`KF-W7.md:427` (the D4+D9+D11 entry): *"**both `sed -n 'Np'` sites are now printed WHOLE rather than marked as fragments** — the ruling asked for fragment marks, and whole-printing is the cheaper discharge … `sed -n '150p' kf-TimelineTrack.md` (D5's site) and `sed -n '69p' kf-SpringTarget.md` both fit: the latter measures **75 words / 590 bytes** at this seat, where the check described *"a full 400-word m-8 row"* — **a figure corrected because this seat measured it**."*
`KF-W7.md:438` (the R5-11 TRANSCRIPT ARM, 11 lines later): *"**The two `sed -n 'Np'` excerpts that CANNOT be printed whole without quoting a 400-word registry row are marked as fragments with their elisions named** — the honest third option, and the one D11 asks for."*
The two are mutually exclusive, and **the file's bytes agree with `:427`**: `:119` prints `sed -n '69p' kf-SpringTarget.md` whole and `:164` prints `sed -n '150p' kf-TimelineTrack.md` whole (both byte-diffed EXACT this seat — 590 B / 75 w and 77 w respectively); **there is no fragment-marked `sed` excerpt anywhere in the live file**. So `:438` is false twice over: it describes a discharge the file did not take, and it re-issues the *"400-word"* figure `:427` measured at 75 words in the same round. **The R5-11 scope-receipt block — the round's own arithmetic-first instrument — is the cell that fails.**
**Receipt**: `KF-W7.md:427` vs `:438` vs `:119`/`:164`; `sed -n '69p' kf-SpringTarget.md | wc -w` → 75; `sed -n '150p' kf-TimelineTrack.md | wc -w` → 77.

### **D6 · MAJOR — the FORWARD four-part test ("no verb, no glob, no COUNT, no prose") is violated at the very cell that certifies it passed, and the count is false at KF.W8's final bytes.**
`KF-W7.md:344` mints the test and then enumerates the members *"every one of them now passing the four-part test above"* — including *"**the two characterization-pair create rows**"*. `KF-W7.md:77` declares *"**ANCHOR-ONLY — no verb, no glob, no count is restated at this end**"* and, in the same cell, restates one: *"⟨`KF-W8.md` §Bounds, **the four create rows**; a filename IS an anchor …⟩"*. `KF-W7.md:352` repeats it: *"the … **create rows**"* (plural, for the pair).
Measured at `KF-W8.md`'s FINAL bytes: its `test/demo/instrument/` creates are **THREE rows** — `:133` (`sfc-load.probe.test.ts`) · `:134` (`groupShortcuts.test.ts`) · `:135`, a **single row carrying BOTH** `CSSPasteDialog.test.ts` and `KeyframesAddDialog.test.ts`. So *"the four create rows"* and *"the two characterization-pair create rows"* are both false, and both are the class the test forbids. **The four filenames are correct and the disjointness assertion is unaffected** — which is precisely what makes the count gratuitous, and its survival diagnostic: the round's own enumeration command found the cells and the conversion left a count inside them.
**Receipt**: `KF-W7.md:77`, `:344`, `:352` vs `grep -n 'test/demo/instrument' KF-W8.md` → `:133 :134 :135` (three rows).

### **D7 · MAJOR — G6's fourth witness leaf is NOT path-qualified and names a file that cannot hold its coordinate, while the SUBJECT-IDENTITY receipt certifies four of four.**
`KF-W7.md:414` (the R4-10(1) receipt, still live as this file's only statement of the class): *"found **G6 carrying four symbols without their modules** (`importCSSToTimeline`, `TimelineKeyframe.easing?`, `captureSnapshot`, **the `imported` assignment**); **all four are now file+line qualified at `81a56990`**."*
`KF-W7.md:279` (G6's live witness) qualifies three: `timelineEngine.ts:76` · `timelineTypes.ts:10` · `snapshotCapture.ts:8`. The fourth reads: *"`state.value.keyframes = imported` (**`CSSPasteDialog` consumer path, R-3's `:152`**)"*. Measured this seat — `git grep -n 'keyframes = imported' 81a56990 -- demo/` → **`demo/components/instrument/timeline/composables/useTimelineBuild.ts:152`**, and **`CSSPasteDialog.vue` is 80 lines**, so the named file cannot hold `:152` at all. P8's R-3 row (`:226`) carries the same bare `:152` with no file. An execution seat resolving the gate's own witness gets nothing; **L-19 forbids a gate resting on an unnamed witness**, and this is the gate that locks the whole round-trip family.
**Receipt**: `KF-W7.md:279` and `:414` and `:226` vs `git grep -n 'keyframes = imported' 81a56990 -- demo/` and `git show 81a56990:…/CSSPasteDialog.vue | wc -l` → 80.

### **D8 · MAJOR — three P0 rows name concrete spend sites with no §Bounds row and no out-of-bounds declaration. An act needs its bounds row; this file proves it knows so three times and misses the fourth family.**
The access column enumerates **24 rows**, and **none** of the following is among them: `demo/scenes/spring/SpringTarget.vue` · `demo/scenes/spring/SpringPhysicsFacet.vue` · `demo/scenes/sequence/SequenceTarget.vue` · `demo/styles/design-idioms.css` (the file the spec itself calls *"outside §Bounds"* at §Excluded). Yet:
- **KF-AV-10** (`:119`) books *"adopt the idiom"* against `design-idioms.css:161-187` and names the live retint seam *"SequenceTarget `:80`, SpringTarget `:113`"* — both verified live at `81a56990` this seat.
- **m-8**, bundled onto that row, is *"the T.G4 anchor triplet re-authored four times (`.spring-ball`/`.sampler-ball`/`.derby-lane-ball`/`.preset-ball`)"* — `git grep -ln` at the frontier resolves those to **`demo/scenes/spring/SpringTarget.vue`** and **`demo/scenes/spring/SpringPhysicsFacet.vue`**, neither in bounds.
- **C-4 (SpringTarget)** (`:120`) books *"repo-wide prose sweep + the S-9 evaluate — adopt `/metric` or DELETE THE SENTENCE"* — a repo-wide act with no bounds row of any kind.

The spec declares the out-of-bounds landing correctly for **R-7** (`KeyframesEditor.vue`, G15 made a *ruling* gate), for **KF-KC-11** (`KeyframeCard.vue` → CARD-UNIT), for **C-3 (SpringTrace)** (library-side → KF.W5/KF.W8) and for **KF-AV-13** (cure lands NO-WAVE-OWNER) — four declarations, and none for the spring-idiom trio, which §Sequencing instead says is *"HOMED HERE — decided in G1's table, cures verdict-gated"*. If G1 rules KEEP-BESPOKE, those cures have nowhere lawful to land. This is exactly the class **R5-5/R5-11** booked against KF.W8 (*"acts booked at §Sequencing/§Rows are swept against §Bounds coverage — the access column and the symbol column never see this class"*), and the sweep was never run here.
**Receipt**: `KF-W7.md:119`, `:120`, `:339` vs the 24-row access column (`sed -n '/^## §Bounds/,/^## §Verdict/p' KF-W7.md | awk -F'|' …`) and `git grep -ln 'spring-ball\|sampler-ball\|derby-lane-ball\|preset-ball' 81a56990 -- demo/`.

### **D9 · MINOR — a pasted command's output trimmed at a live cell, at the fifth command the round-5 TRANSCRIPT ARM's enumeration excluded.**
`KF-W7.md:31` (OP-4 residue (a)) prints: *"command pasted: `grep -n 'plugin-vue REGISTRATION' KF-W4.md` → **the §Commits line**"*.
Re-run this seat at `KF-W4.md`'s final bytes: **TWO** hits — **`:269`** (W4's `→ KF.W8` cross-edge row, which books the same registration act from the other side) and **`:322`** (the §Commits line quoted). The extra hit **corroborates** and the conclusion is untouched. What fails is the transcript, and it fails because `:438`'s transcript arm enumerated **four** commands (`vitejs/plugin-vue` · `KF-AV-17` · `sed 150p` · `§1c` heading) and this is a fifth pasted command in a live cell — the class re-cut by scope at the W8 receipts and re-cut by instance here.
**Receipt**: `KF-W7.md:31` vs `grep -n 'plugin-vue REGISTRATION' KF-W4.md` (2 lines).

### **D10 · MINOR — the fold-dedupe enumeration the counting rule discharges against prints EIGHTEEN coordinates under a "17" heading, with no counting rule at the receipt. PASS-4's "16" listed 17; the off-by-one was inherited, not cured.**
`KF-W7.md:13` rule (5) points at the one enumeration and prints its heading as the receipt: *"`grep -n '^### §1c' PASS-5/KF-W7-CHECK.md` → `:75 ### §1c · Fold-carried lines resolved by identity — **17**, not 16`"*. The command reproduces (verified, `:75`). The **number does not**: the enumeration beneath it lists **18** coordinates — `KT:106 :114` · `TT:86` · `TC:74 :75 :77 :78` · `THP:77 :80 :81 :82 :84 :86 :88` · `SP:92 :143` · `SS:128` · `ST:135` — every one re-read at the bytes this seat. The predecessor made the same error in the same direction: PASS-4's *"16 lines"* enumerated **17**. A defensible reading (excluding `KT:114`, annotated *"counted once at §1b"*) yields 17 — **but that counting rule is stated nowhere, and R5-11(1) requires the counting rule AT the receipt**. D7's cure corrected the enumeration's membership and re-inherited its arithmetic.
**Receipt**: `grep -n '^### §1c' PASS-5/KF-W7-CHECK.md` → `:75`; `sed -n '75,78p'` (18 coordinates); `PASS-4/KF-W7-CHECK.md` §1c ("16 lines", 17 coordinates).

### **D11 · MINOR — an "elision marked" that marks no elision and substitutes a character, inside a re-quote labelled LAW D(1).**
`KF-W7.md:31` quotes W4's §Commits commit 2 as *"…only `vitest.config.ts` changes **… and this commit is the witness…**"* under the label *"**RE-QUOTED AT REPAIR ROUND 4 UNDER LAW C(2) + LAW D(1)** … The elision is marked and the addition disclosed"*. `sed -n '322p' KF-W4.md` returns *"only `vitest.config.ts` changes **—** and this commit is the witness…"* — **an em-dash, and nothing elided**. The `…` therefore reports an elision that did not occur and replaces a character of the source. Harmless in conclusion; it is recorded because a quotation whose ellipsis is decorative teaches a reader that this file's ellipses are decorative.
**Receipt**: `KF-W7.md:31` vs `sed -n '322p' KF-W4.md`.

### **D12 · MINOR — the SHADOW LINE carries D1's phantom denominator.**
`KF-W7.md:433`: *"**SHADOW: struck/minted ROUTING set = EMPTY.** … **all 127 roster identities and all 8 non-roster identities remain carried at the rows that carried them before**."* The **substance is verified true by this seat** — no row moved home, and both contested identities are carried (`m-8` at P0's KF-AV-10 row, `PR-CAUTION` at the header lock and §Carry's disclosure). But LAW F(1) makes the shadow line an **act-completeness record**, and a completeness record whose set-size does not reproduce from the file's own rule (7, not 8 — §2b) cannot discharge the law it satisfies in substance.
**Receipt**: `KF-W7.md:433` vs `KF-W7.md:13`'s rule (1) and this seat's §2b.

### **D13 · MINOR — "ELEVEN of the twelve cured here" over-claims D7 and D11.**
`KF-W7.md:440` closes: *"**ELEVEN of the twelve cured here**"*, with D2 booked-and-declined to RECONCILE (correctly — the cert landed it, §4 · R5-R2, verified this seat). **D7's cure re-points to an enumeration whose own arithmetic does not reproduce** (D10 — the class D7 named was *"an enumeration that misses a member cannot discharge the rule that cites it"*, and 18-under-17 is that class), and **D11's discharge is described two ways in the same ledger** (D5). Ten of twelve is the defensible figure.
**Receipt**: `KF-W7.md:440` vs this seat's D5 and D10.

### **D14 · MINOR — same-round backward receipts still restate a sibling's verbs, counts and prose; they happen to be true, which is why they survived.**
`KF-W7.md:76` books KF.W4's footprint as *"**2 creates** — `typing-dots-engine-seam.test.ts` · `aurora-opacity-ceiling.test.ts` — **plus one cure site, `resize-tracks.test.ts`** (`modify-carve`, the `fs.readFileSync` + two `toMatch` source-text pins)"*, and `:75` books KF.W5's as *"**1 create**"* with its §Disjointness paragraph named. Both re-verified TRUE at those siblings' final bytes this seat (`KF-W4.md:75 :83 :84`; `KF-W5.md`, four fixtures ×2). They are nonetheless same-round sibling content claims of exactly the shape LAW E(4) retires, left standing because the conversion enumerated `KF.W8` rather than the class. **D2 is what this looks like when the sibling moves.**
**Receipt**: `KF-W7.md:75`, `:76` vs `KF-W4.md:75/:83/:84` and `KF-W5.md`; RULINGS-5 LAW E(4).

---

## §5 What HOLDS — recorded so the repair does not regress it

- **LAW E(2)'s challenge is MET: 11 of 11 sha256 match the cert's closing table.** The round-5 write-set is provable; nothing moved after `CLOSE-CERT-2.md`. Six rounds in, the close instrument finally survives its successor.
- **Census carriage: 0 escaped**, under all three denominators. All **127** roster identities matched to a carried row by banked id — verified on a sample disjoint from PASS-5's, so the two passes do not corroborate each other by construction. The three §Excluded-with-named-owner rows verify at **both** KF.W6 ends by banked row header for a **fifth** consecutive round while their line numbers moved again (`:360`→`:394`, `:438`→`:480`).
- **The frontier axis is exemplary and now EXHAUSTIVE, not sampled**: **18 of 18** §Bounds line counts EXACT; **12 of 12** gate-witness commands reproduce, most to the line (G5's five anchors, G6's three qualified leaves, G11's 0/13 split with all thirteen coordinates matching the printed enumeration, G13's two delta reads, G14's six toast lines, G15's four `initialText` hits and the two mounts, OP-4 exact).
- **LAW A remains the best-founded material in the file**: both censuses reproduce **command-for-command** from an independent execution; the `loadPreset` re-export consequence and the out-of-bounds repoint consequence are both real and both correctly binding.
- **The file's own scope receipts reproduce hit-for-hit**: `grep -n 'KF-W8\.md\|KF\.W8'` → **29 hits at exactly the 29 lines printed**, partitioned 8 + 11 + 10; `grep -n 'PASS-4/KF-W7-CHECK'` → **4**, matching the receipt's own accounting. **Where R5-11 was applied, it works** — which is what makes D2/D3/D14 a scope failure rather than a method failure.
- **All four LAW-D registry quotations byte-diff clean**, including the two `sed` sites printed whole; the *"400-word"* correction to **75 words / 590 bytes** is exact at the bytes.
- **Rung integrity holds**: `KF-KC-52` at INFO (`kf-KeyframeCard.md:95`/`:100`), the M9 rung lock intact, the five-member TT dissent roster carried whole and each member shown at the row that carries it — re-verified against `sed -n '150p'`'s full 77-word line.
- **Gates**: fifteen, **all born-RED, none stamped, every one with reachable green**; G2's AnimationControlsGroup arm honestly conditioned on OP-0 while the detached-target arm is unconditioned; G11's cost is one in-bounds devDep plus a named inbound act; G15 correctly a *ruling* gate because its repoint site is out of bounds.
- **Posture is the strongest axis**: W4 head, W3 gated, **O-21 zero hits (correctly)**, **all four KF.W10 forward anchors resolve at final bytes** — §G-2's alphabet at `:407` byte-identical across two further W10 writes and a whole repair round — and **§6.D's three rows are exactly the three addressees this file re-points to**. The item this wave flagged rather than assumed is, for the third round running, the item that needed no repair.

---

## §6 Local verdict

**DEFECTIVE.** The wave's substance keeps improving and its measurement layer is now genuinely excellent: carriage is clean, the frontier axis re-executes exhaustively rather than by sample, LAW A is exemplary, the shadow discipline is real, the hash-proven close survives, and both of the file's own scope receipts reproduce hit-for-hit. The failures are, again, entirely in the **citation layer**, and they cluster on a single mechanism this round named and then did not apply: **a class enumerated by its named sibling instead of by its stated scope**. That mechanism produces **D2** (a claim about `KF-W4.md` false since round 3), **D3** (LAW E(4) applied to the FORWARD half only, leaving four stale stamps and four false coordinates under *"Verified this round"*), **D4** (item 9's two coordinates false), **D6** (a count inside the cell that certifies no count), **D9** (a fifth trimmed transcript outside the four the arm swept) and **D14** (the backward characterizations that happen to still be true). Beside them sit **D1** — the census figure, seventh generation, now with a phantom operand and two cells disagreeing about its sign — **D7**, a gate witness naming an 80-line file for line 152, and **D8**, three P0 rows with no lawful landing surface. **D1, D2, D3, D5, D6, D7, D8 are the repair round's work; D4 and D9–D14 are one-line corrections.**

*Sole write = this file. Read-only at both trees. `origin/master 81a56990` for every keyframes witness; the value.js registry and the ten sibling specs at their final round-5 bytes. No gate stamped, no ruling declined, no prior-pass artifact edited, nothing inherited from PASS-5's prose — every figure, receipt and quotation above was re-executed here before it was written, beginning with the eleven hashes LAW E(2) makes this seat's first act.*
