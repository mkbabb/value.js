# KF-W8 — FRESH ADVERSARIAL SPEC CHECK (L-18/L-20, pass 6)

**Subject**: `/Users/mkbabb/Programming/value.js/docs/tranches/X/keyframes/waves/KF-W8.md` — 476 L / 249 660 B, mtime **2026-08-29 15:17:38**, repaired **five** times (rounds 1–5).
**Corpus authority**: the **58** `kf-*.md` records at `/Users/mkbabb/Programming/V/…` → `docs/tranches/V/megatranche/registry/adjudicated/` (`ls kf-*.md | wc -l` → **58**, re-run this seat). Sole in-tree carry `carry/KF-W6-CARRY.md`.
**Census instrument**: an id-keyed harvest from the 58 records' own bytes — token sweep, alternate-spelling sweep, by-name sweep, whole-sentence resolution of every routed line, fold-transitivity sweep of every W8-terminal identity across all 58. PASS-5's roster was opened **only after** this seat's enumeration was complete, and then only as a diff target.
**Product tree (TREE LAW)**: `/Users/mkbabb/Programming/keyframes.js`, read-only. `git rev-parse origin/master` → **`81a56990736ced5b5edde0b84c527680ac7689b1`**, re-resolved this seat. Local `HEAD`/`master` = **`8281638c`** (DISQUALIFIED; `git status --porcelain | wc -l` → **252**). Every product measurement below was taken at `origin/master`.
**LAW E(2) FIRST ACT — the eleven specs re-hashed.** `shasum -a 256 waves/KF-W*.md` reproduces **CLOSE-CERT-2 §9's closing table for all eleven, byte-for-byte**. KF-W8.md → `c7c1144599f8c8cfbe2533c5a4bb65d36357e4e8fb45918e9c09a9c056fba1c9` = the cert's closing hash. **No file moved after the cert. Round 5 closed lawfully; its repairs bank.**

**Verdict: DEFECTIVE.** The **ID-keyed census is CLEAN** — **32 routed / 32 booked / 0 escaped**, re-enumerated by record, id-for-id identical to PASS-3/PASS-4/PASS-5 (a fourth independent derivation). **All fifteen gates reproduce their stated frontier numbers**, falsifiers 15/15, and **all ten LAW-A censuses re-derive exactly** (A-9's seven, A-5's four, A-10's corrected four). The five pass-5 HIGHs are genuinely cured at the bytes: **G3's seven repoints all have landing surfaces**, **G14's SOURCE is granted**, **R-1's five MUST-CARRY edits are bounded**, **the AUDITED cell's numerals are converted to a pointer and the pointer's figures still reproduce (15 · 26 · 8, re-measured here)**, and **G7's ordering is declared at this end twice**. What fails is one class carried forward and one manufactured by the round's own hand: **the G14 SOURCE row's §Bounds coordinate column grants six lines of a ten-line body** — the identical extent defect the same round cured at G11 by re-cutting its coordinate column — and **G7's third ordering declaration does not exist at KF.W6's final bytes**, while this file asserts three times that the template is "applied whole, three ways." **2 HIGH · 4 MEDIUM · 4 LOW.**

---

## 1 · ID-KEYED CENSUS — enumerated BY RECORD, nothing inherited

### 1.1 Method

1. **Token sweep**: `grep -c 'KF\.W8' kf-*.md` over all 58 → **47** hit lines across **18** records (`grep -l` → 18). All 47 read whole and classified: routing-law boilerplate **4** (`kf-KeyframeTimeline:18` · `kf-KeyframesStringControls:17` · `kf-TimelineCaret:17` · `kf-TimelineTrack:18`) · closing-verdict / fold-summary **11** · **row-terminal routing 32**.
2. **Alternate-spelling sweep**: `grep -nE '\bW8\b|W\.8|[Ww]ave[ -]8' kf-*.md | grep -v 'KF\.W8'` → routing-law taxonomy lines only, plus the single non-KF token `L.W8 ED-3` (`kf-OrbitalDrag.md:82`, a **value.js** tranche-ledger cite inside OD-34 — NO-WAVE-OWNER; the same token recurs inside the routed `KF-CE-42`). Every summary line re-opened: **zero new ids**.
3. **By-name sweep**: `grep -niE 'structure (&|and) colocation|colocation settle|structure-colocation'` with `KF.W8` lines removed → **boilerplate only**.
4. **Whole-sentence resolution**: every routed line read to its terminal period — merged groups (`L-19/C-14`), fold clauses (`KF-KC-45` → KAD-24; `SPF-22` ≡ KAD-24; `C-13` ≡ KF-AV-8/-9/-18; `m-15`/`L-8` = banked N-11; `KF-AV-35` folds into KF-AV-8/KF-CE-12), split dispositions (`C-8` = KF.W5 surface + KF.W8 colocation at `kf-KeyframesStringControls.md:81`; `C-12` = KF.W8 + KF.W4 depcruise arm), rider restatements (`kf-HeroAurora.md:27`(d) · `:120` · `kf-AnimatedText.md:137` — all restate KF-AT-26(c), no new id).
5. **Fold-transitivity**: every W8-terminal identity `grep -ln`'d across all 58 (§1.3).

### 1.2 The routed roster — 32 ids, per record

| record | ids routed → KF.W8 | n |
|---|---|--:|
| kf-AnimationVisualizer | KF-AV-8 (`:49`) · KF-AV-9 (`:50`) · KF-AV-18 W8 arm (`:62`) · KF-AV-24 (`:68`) · KF-AV-26 (`:70`) · KF-AV-35 (`:82`) | 6 |
| kf-CSSCodeEditor | KF-CE-12 (`:46`) · KF-CE-42 (`:80`) | 2 |
| kf-KeyframeTimeline | L-19/C-14 (`:77`) · C-12 (`:78`) · N-11 (`:101`) | 3 |
| kf-KeyframesAddDialog | KAD-24 (`:77`) · the zero-coverage fold → the twin's C-10 (`:89`) · L-17 colocation (`:92`) | 3 |
| kf-TimelineCaret | MISS-β2 (`:46`) · m-7 (`:59`) · L-8 (`:81`) | 3 |
| kf-KeyframeCard | KF-KC-42 (`:87`) · KF-KC-45 (`:90`) | 2 |
| kf-RibbonBar | M-4 (`:66`/`:132`) · m-4 (`:132`) | 2 |
| kf-AnimatedText | KF-AT-26(c) (`:71`) | 1 |
| kf-HeroAurora | KF-HA-14 (`:56`) | 1 |
| kf-CSSPasteDialog | C-10 (`:75`) | 1 |
| kf-CopyButton | KF-CB-37 (`:85`) | 1 |
| kf-KeyboardShortcutsModal | R-10 (`:46`) | 1 |
| kf-KeyframeCardList | KC-30 (`:66`) | 1 |
| kf-KeyframesStringControls | C-8 (`:81`) | 1 |
| kf-KeyframesEditor | KF-KE-15 (`:57`) | 1 |
| kf-PlaybackRibbon | C-13 (`:154`) | 1 |
| kf-SpringPhysicsFacet | SPF-22 (`:62`) | 1 |
| kf-TimelineTrack | m-15 (`:104`) | 1 |
| **TOTAL** | | **32** |

**Booked, id-for-id — 32 of 32.** `grep -oF <id> KF-W8.md | wc -l` ≥ 1 for every one, re-run this seat: KF-AV-8 11 · KF-AV-9 11 · KF-AV-18 8 · KF-AV-24 11 · KF-AV-26 7 · KF-AV-35 2 · KF-CE-12 7 · KF-CE-42 2 · L-19/C-14 1 · C-12 6 · N-11 13 · KAD-24 8 · L-17 6 · MISS-β2 7 · m-7 7 · L-8 3 · KF-KC-42 3 · KF-KC-45 5 · M-4 8 · m-4 4 · KF-AT-26 8 · KF-HA-14 6 · C-10 2 · KF-CB-37 18 · R-10 15 · KC-30 11 · C-8 3 · KF-KE-15 4 · C-13 2 · SPF-22 3 · m-15 3 · C-3 (inbound) present. The three census-derived rows and the three booked inbound edges (R-19c ii–iv) are present; the fourth is declined with its owner named.

### 1.3 Escapes — ZERO

No id anywhere in the 58-record corpus carries a terminal disposition to KF.W8 and is absent from `KF-W8.md`. Escapes are closed **by construction**: any record routing a row here carries the token (sweep 1), the name (sweep 3) or an alternate spelling (sweep 2), and all three are exhausted.

**Fold-transitivity, re-run whole — and the figure the spec now CITES is itself a double-count (→ D-3).** Off-record occurrences of the distinctive W8-terminal identities, by command (`grep -l <id> kf-*.md`, home record removed):

| id (home) | off-record records | lines |
|---|---|--:|
| `KF-CE-12` (kf-CSSCodeEditor) | kf-AnimationVisualizer · kf-KeyframeTimeline · kf-KeyframesEditor · kf-PlaybackRibbon · kf-TimelineHoverPreview | 13 |
| `N-11` (kf-KeyframeTimeline) | kf-ChannelOptions · kf-SequencePlayhead · kf-TimelineCaret · kf-TimelineTrack | 8 |
| `KF-AV-8` (kf-AnimationVisualizer) | kf-PlaybackRibbon | 6 |
| `KAD-24` (kf-KeyframesAddDialog) | kf-SpringPhysicsFacet | 3 |
| `KC-30` (kf-KeyframeCardList) | kf-KeyframeCard | 3 |
| `KF-CE-42` (kf-CSSCodeEditor) | kf-KeyframesStringControls | 1 |

**34 lines ✓ — but TWELVE distinct records, not thirteen**: `kf-PlaybackRibbon` appears in two rows (once under `KF-CE-12`, once under `KF-AV-8`) and the "13" is the row-sum, not the record count. **All thirty-four resolve as non-escapes** on this seat's own read — the on-roster routings (`C-13`, `C-8`, `L-8`, `m-15`, `SPF-22`, `KF-KE-15`, `C-12`) are booked fold clauses; the genuinely independent five (`KF-CE-12` @ `kf-TimelineHoverPreview` the negative comparator · `KC-30` @ `kf-KeyframeCard` = the different id `KF-KC-30` · `KAD-24` @ `kf-SpringPhysicsFacet` a provenance note · `N-11` @ `kf-SequencePlayhead` a different N-11 carrying a KF.W0 fold · `N-11` @ `kf-ChannelOptions` the substring inside `L·N-11/N-12`) re-verified non-escapes here. **`0 escaped` is independently reconfirmed a fourth time.**

### 1.4 Diff against PASS-5

Roster **identical, id-for-id**. One PASS-5 statement does not survive: its §1.3 headline *"Thirteen records, ≥34 lines"* double-counts `kf-PlaybackRibbon` (→ **D-3**), and KF-W8 now reprints that figure at three sites under a ruling whose whole instruction was *cite, don't count*.

### 1.5 §State's remaining commanded figures — ALL FOUR REPRODUCE

`ls kf-*.md | wc -l` → **58** ✓ · `grep -l 'KF\.W8' kf-*.md | wc -l` → **18** ✓ · and the two figures the round-5 cell **converted to a pointer** re-measure true at the file's current bytes: `grep -oE 'kf-[A-Za-z0-9]+\.md' KF-W8.md | sort -u | wc -l` → **15** ✓ (cited 15) · union of routed ∪ named-by-path → **26** ✓ · named-not-routed → **8** ✓, and it is exactly the enumerated set (`kf-App` · `kf-ChannelOptions` · `kf-DemoGlobalChrome` · `kf-EditorShell` · `kf-EditorStartScreen` · `kf-SequencePlayhead` · `kf-SharePopover` · `kf-SpringTrace`), including the two D-15-introduced names the cell names. **D-5 is CURED, and cured twice over — by conversion and by the figures happening to hold.**

---

## 2 · ANCHOR + RECEIPT REALITY — every citation re-resolved at the siblings' FINAL bytes

Sibling state at this seat: all eleven specs at `2026-08-29 15:17:38` except `KF-W10.md` 15:18:25 and `KF-W7.md` 15:18:37. **Hashes match CLOSE-CERT-2 §9 for all eleven.**

| receipt as written in KF-W8 | anchor resolves? | this seat's re-derivation at FINAL bytes |
|---|---|---|
| `KF-W5 §Carry · Arm D · row D-6` | ✅ | `grep -n '^\| \*\*D-6' KF-W5.md` → **`:378`** (spec's `:364` is a dated reading, declared non-load-bearing). The R-4 verbatim *"The do-not-split lock is SATISFIED by decline-whole…"* and the `DENOMINATOR CORRECTED` rider both **byte-exact in that row** |
| `KF-W5 §Sequencing · "→ KF.W8" cross-edge` legs (a)(b)(c)(d) | ✅ | `grep -n 'Structure & Colocation' KF-W5.md` → **`:444`**; leg (c) *"`ingest/cssom.ts`'s split disposition defers to KF.W2's façade decision; KF.W8 owns its colocation."* **byte-exact**; leg (d) *"If OP-4 resolves demo-side, B-16 becomes a **shadow-name** row for KF.W8."* **byte-exact** |
| `KF-W5 · B-16 / OP-4 · LOCUS DECLARED` | ✅ | `grep -n 'OP-4 · LOCUS DECLARED' KF-W5.md` → **`:353`**; both pre-stated dispositions present |
| `KF-W6 §Sequencing · "→ KF.W8"` (KF-CB-37, MOVE-vs-RESHELL GUARD) | ✅ | `grep -c 'KF-CB-37' KF-W6.md` → **1**, at **`:558`** (`:410`→`:417`→`:445`→`:514`→**`:558`** — a fifth spelling; the anchor never moved). Guard **byte-exact**, and the *"No W6 commit carries the move"* clause present. **⚠ this is also where D-2 lives** |
| `KF-W6 §Bounds` barrel row / EditorHeader row | ✅ | `grep -n 'shell/index\.ts' KF-W6.md` → **`:72`** (sole-write-owner row) · EditorHeader delete at **`:69`** |
| `KF-W6 §Carry` KF-APP-41 + `THE ACT: ONE ATOMIC COMMIT` | ✅ | `grep -n 'KF-APP-41' KF-W6.md` → **ELEVEN** hits (`:69 :448 :457 :458 :503 :528 :546 :553 :560 :584 :616`); both quotations byte-exact |
| `KF-W6 §Carry · W6-C · PROSE-WITH-CODE` · `W6-I` honest scoping | ✅ | **`:256`** · **`:404`** (*"graph width, NOT crash exposure"* byte-exact) |
| `KF-W7 §Bounds` `package.json` row (DH-2) | ✅ | `grep -c 'devDependencies only' KF-W7.md` → **0** (phantom still dead); `:61` carries the quoted row **byte-identical** at W7's 15:18:37 bytes |
| `KF-W7 §Bounds` shared-directory declaration (DH-3) | ✅ | `grep -n 'never globbed' KF-W7.md` → **`:68`**, *"…never globbed **by shared prefix**"* — cure holds. **And the far end is now ANCHOR-ONLY** (`:77`: *"no verb, no glob, no count is restated at this end"*), so the four-party declaration is consistent at both ends — **D-12 of pass 5 is genuinely cured** |
| `KF-W7 §Carry · P0` SpringTrace C-3 route dissent | ✅ | `KF-W7.md:123` — *"route-by-mechanism: a value.js LIBRARY export-surface decision…"* **byte-exact** |
| `KF-W4 §Bounds` `vitest.config.ts` row · item 3 · commit 2 | ✅ | item 3 (ME-32 ≡ R-10) at **`:166`**; commit 2 *"demo vitest project + **plugin-vue REGISTRATION**"* at **`:322`**; the reciprocal `→ KF.W8` cross-edge at **`:269`** carries *"DECLARED FROM THIS END, the reciprocal of KF.W8's re-cut G10"* — **G10's template IS three-way, which is exactly what makes D-2 visible** |
| `KF-W3` title · `package.json (:70 only)` row | ✅ | `sed -n '1p'` → *"# KF.W3 — Parser Consumption (GATED, never scheduled)"*; the row at **`:77`** |
| `KF-W2 §Carry · F4` §4.4 lock · `§0 · OP-6` | ✅ | `grep -c 'KF.W8 precedes' KF-W2.md` → **4**; `ORDERED: KF.W8 precedes` at **`:63`** |
| `KF-W10` FOLD-FORWARD `KF-W8-R-4-STRUCT-PAIR` · `§6.C-0` · **`§6.D`** | ✅ | `grep -c 'KF-W8-R-4-STRUCT-PAIR' KF-W10.md` → **11**, the record at `:100`; **`§6.D · SUCCESSOR-FORMATION REGISTER` at `:573`** with all three rows (`:588`/`:589`/`:590`), cargo **9 + 6 + 2**, terminal verb `MINTED-UNAUTHORED`. `grep -oE 'KF-W10\.md:[0-9]+' KF-W8.md` → **∅** — the anchor-only posture holds |
| `COHESION §0d` (via W10 §6.C-E) | ✅ | `COHESION.md:185` = **§0d ADDENDUM 2026-08-28**, cited as landed at `KF-W10.md:555`; R4-8 clause 4 discharged |
| precepts `README.md:127-130` / `:132-135` · census `CENSUS-2026-08-03.md` · `lane-library.md §7.3` | ✅ | all resolve in-tree (`docs/precepts/instructions/README.md`; `docs/tranches/V/megatranche/formation/keyframes/`) |
| `R1-05-demo-colocation.md:118` | ✅ as declared | present in the **keyframes.js** tree, absent from value.js — the spec says so and says *"cite, never re-mint"*. Correct posture |

**M-25 depth — every load-bearing registry quotation byte-diffed at the bank.** All verify **byte-exact**: KF-AV-9's *"the eviction API is global-by-construction (**library-side** — no scoped bump exists)…"* and *"`:79` is the only wire in demo/ (census 2 hits, both this file)"* (`kf-AnimationVisualizer.md:50`) ✓ · C-22 whole with its elision marked (`kf-EditorShell.md:95`) ✓ · MISS-β2 **DISSENT 1** (`kf-TimelineCaret.md:136`, inside the `## DISSENT — preserved, not ruled away` block at `:134`) ✓ · SpringTrace **C-3** (`kf-SpringTrace.md:62`) ✓ · KF-CB-37 (`kf-CopyButton.md:85`) ✓ · KF-APP-41 (`kf-App.md:91`) ✓ · M-L3 (`kf-DemoGlobalChrome.md:70`) ✓ · L-16-RESCOPED (`kf-EditorShell.md:79`) ✓ · KF-KC-37 / SP-18 / KF-KE-53 (`kf-KeyframeCard.md:82` · `kf-SharePopover.md:54` · `kf-KeyframesEditor.md:98`) ✓. **LAW D's verbatim arm produces ZERO convictions for a second consecutive pass.**

**What does NOT hold at the receipt layer**: eight *"FINAL stage-1 bytes"* assertions and twenty substrate-stamp occurrences survive in the body (→ **D-4**), and the one reciprocal this file asserts a sibling carries does not exist there (→ **D-2**).

---

## 3 · GATES — re-executed whole at `origin/master 81a56990`

**All fifteen reproduce their stated frontier numbers. Falsifiers re-counted: 15/15. All fifteen still born RED.**

| gate | this seat's re-run | |
|---|---|---|
| G1 | cl.1 → **7 statements / 6 files**, anchors byte-identical (`useKeyframeOps:1` · `parseAnimationCSS:7` · `timelineEngine:1`,`:17` · `AnimationVisualizer.vue:45` · `helpers.ts:9` · `keyframeSelector.ts:5`); cl.2 → four greps over the four published entries, **four empty results**; `export *` in `src/animation/index.ts` → **0** | ✅ |
| G2 | eight symbols over four entries → **exit 1**; exports map exactly `.` + `./engine`; `export *` → 0 | ✅ |
| G3 | `-- src demo` → exactly **3** (`format/format.ts:20` · `css-text.ts:58` · `keyframeSelector.ts:7`); fourth homonym `test/engine/nan-frame.test.ts:39` real and out of scope. **All seven repoints now have landing surfaces — PASS-5 D-1 CURED** | ✅ |
| G4 | five `index.ts` under `demo/components/`; **all four clause greps empty**, all four barrels present; `transport/index.ts` **14 L**, 3 async + 1 type export, docblock at `:1-5` | ✅ |
| G5 | exactly **3** up-imports at `:31`, `:116`, `:229` | ✅ |
| G6 | `demo/components/` non-recursive → `CopyButton.vue` + `instrument` + `playback` → **1** loose `.vue`. **R-1's five MUST-CARRY edits all in-grant — PASS-5 D-3/D-14 CURED** | ✅ |
| G7 | (i) **8** SFCs; (ii) **4** exports; (iii) per-export counts **1 / 2 / 3 / 0** reproduce exactly; (iv) external = `HeroAurora` (`App.vue:147`), internal-only = `AnimatedText`/`KeyboardShortcutsModal`/`TypingDots`, zero-importer = `EditorHeader` | ⚠ **D-2** (the third ordering declaration is absent at KF.W6) |
| G8 | leg (i) → **2**, both byte-exact; leg (ii) → raw **5** for the SFC (`:46 :70 :72 :76 :77`), **6** in the test (`:4/:5/:13/:35/:105/:127`); leg (iii) → **4 files**, `transport/index.ts` returns **`:3` only** | ✅ |
| G9 | `git ls-tree -r --name-only origin/master \| grep -c '\.test\.ts$'` → **132** = `-- test` **130** + `-- bench` **2** (`d3-changed-keys` · `sync-step`) | ✅ |
| G10 | `git show origin/master:vitest.config.ts \| grep -c plugins` → **0**; two jsdom projects (`library` `test/**/*.test.ts`, `demo` `test/demo/**/*.test.ts`); benchmark include `bench/*.bench.ts`; `@vue/test-utils` **absent**; 9 files under `test/demo/instrument`, names matching §Bounds exactly | ✅ |
| G11 | `groupedShortcuts` body measured `:57` `const groupedShortcuts = computed(() => {` … `:67` `});`, `:68` blank, `:69` `</script>`; template consumer `:11`; `-- test` → empty. **The carve is now `:57-67` = the falsifier's extent — PASS-5 D-11 CURED** | ✅ |
| G12 | `-- test` → empty for both dialogs | ✅ |
| G13 | `src/` → one entry ✅ · `basename==dirname` → **exactly the 8** ✅ · broader token sweep → **12**, and they are D-6's twelve path-for-path ✅; subordination cites `G-STRUCT` by gate id and the SET, never a numeral — W5's R2-5(a) reciprocal directive satisfied | ✅ |
| G14 | **11 lines / 10 files**, host list exact; `useDragScrub` → **exit 1, zero**; AV carries `:104` + `:199`. Projector body measured `:101`→**`:110`** (`};`), `:112` opens `applyProgress` | ⚠ **D-1** (the §Bounds coordinate column grants `:101-106`) |
| G15 | round-2 command returns **exactly the 9 stated lines** (`:5 :7 :8 :13 :59 :69 :108 :113 :138`); leg 1 → **7** convicted; leg 2 → **exactly the pad's two**; 151 L | ✅ |

**LAW A censuses, re-derived from the import graph — 10 of 10 reproduce.** A-1..A-4 → **0 consumers** for all four barrels, by both sweeps ✓ · **A-5 → exactly the four R-1 names** (`KeyframeCard.vue:59` · `KeyframesEditor.vue:116` · `EasingTarget.vue:143` · `StartingStyleTarget.vue:87`), the `w1-rehearsed.patch:69` hit correctly non-import ✓ · A-6 **1** ✓ · A-7 **1** ✓ · A-8 **1** ✓ (patch hit excluded) · **A-9 → 7**, id-for-id (`useKeyframeOps.ts:9` · `useTimelineBuild.ts:8` · `useTimelineOps.ts:7` · `snapshotCapture.ts:3` · `timelineEngine.ts:16` · `timeline-undo.test.ts:6` · `value4-editor-boundary.test.ts:6`), with the four docs-prose lines correctly non-import ✓ · **A-10** → `compile/emit/css-text`'s library consumers are **FOUR** (`engine/css/metadata.ts:35` · `resolve/browser.ts:4` · `resolve/conditional.ts:2` · `resolve/function.ts:6`) and test consumers **five** ✓.

**MECHANISM F.** `grep -coE 'KF\.W1[1-9]' KF-W8.md` → **0**. This wave mints no successor-wave token; **no mint escapes**. All three registers exist at `KF-W10.md:573 §6.D`. ✓

**RECEIPT ARITHMETIC FIRST (R5-11).** The **L-18 blanket** reproduces under its own stated rule: COMMANDED **12** (G1·G2·G3·G4·G5·G6·G7·G9·G10·G11·G12·G15) + DECLARED-IN-PART **3** (G8 leg (ii) · G13 · G14) = **15** ✓. The **ACCESS-COLUMN** receipt's arithmetic reproduces (8+8 = 16 rows; 14+9 = 23 paths) ✓ **but its scope is short by three rows** (→ D-5). The **SUBJECT-IDENTITY** receipt's first two blocks reproduce (14 + 5, all present at the frontier) ✓ **but its third block prints no arithmetic** (→ D-7).

---

## 4 · POSTURE

| axis | finding |
|---|---|
| **W4 head honored** | ✅ §State opens after KF.W0 · KF.W4 · KF.W5; G10 is an inbound-dependency cite on W4's commit 2, **reciprocated at W4's own `:269` cross-edge**; `vitest.config.ts` carved to R-5's `measure` project alone; `package.json` to `+@vue/test-utils` |
| **W3 gated-unscheduled** | ✅ the → KF.W2/W3 cross-edge quotes KF-W3's title byte-exact and names **KF.W2** as the schedulable receiver |
| **O-21 (not O-20)** | ✅ vacuous and correctly so — `grep -c 'O-20'` → 0, `grep -c 'O-21'` → 0, and the disclosure is written **by description**, so it cannot falsify its own zero |
| **§6.D successor register + COHESION §0d** | ✅ both cited **anchor-only**; `§6.D` exists at `KF-W10.md:573` with all three rows and their cargo; `COHESION.md:185` carries §0d; **zero W10 line numbers ride any W8 receipt** |
| **EH-4/5/8 restoration with TRUE anchors** | ✅ **verified at KF-W6's final bytes**: `:460`/`:461`/`:462` restore EH-4/EH-5/EH-8 **LIVE, one row per banked anchor** (`EditorShell.vue:45` · `:46` · the live shell ribbon cluster), each re-read at `81a56990`. **W8 is correctly unaffected**: it books **zero** EH ids (`grep -c 'EH-4\|EH-5\|EH-8' KF-W8.md` → **0**), its §Excluded `KF-APP-41` / `C-22` cell keeps `D-27` and `RR-2 M7` NO-WAVE-OWNER, and R5-1 expressly leaves the R4-1 census and delete intact — so G7's inbound dependency is unharmed by the BLOCKER's cure |
| **MECHANISM S (LAW F) — moved/dropped rows carry their shadows** | ✅ mostly: **7 SHADOW lines** land at the row-level acts (the nine-tracked row's narrowing, the two carve re-anchors, the three G3 mints, the G14 SOURCE mint, the five R-1 mints, the G11 widening, the ledger's struck stamps), the ROUND-5 SHADOW LEDGER enumerates minted/widened/re-pointed/struck with **DROPPED: NOTHING**, and each mint states its banked anchor was re-read first (LAW F(3)). ⚠ **one terminus list is short** — D-6 |
| **KF-AV-28 rider** | ✅ both banked spellings byte-exact; applied by mechanism to all governed rows incl. the fourth AV row minted this round |
| **LAW B (no self-voiced closure)** | ✅ every closure claim cites `PASS-5/KF-W8-CHECK.md` by path and date (12 citations across §1/§1.2/§1.3/§2/§3/§5); PASS-4 and PASS-3 named as superseded, never dropped |
| **LAW E(2) hash gate** | ✅ **all eleven specs re-hash to CLOSE-CERT-2 §9's closing table** |
| **§Bounds bounds the wave's acts** | ⚠ **the act-coverage class is 4-of-5 cured**: G3's seven, R-1's five, R-10's extent and G14's destination all land in-grant; **G14's SOURCE is granted at an extent four lines short of its own body — D-1** |

---

## 5 · DEFECTS

### D-1 · HIGH — the G14 SOURCE row's §Bounds coordinate column grants six lines of a ten-line body, in the round that cured the identical defect at G11

R5-4 granted the projector. The row's **path cell** reads:

```
| **`demo/components/playback/AnimationVisualizer.vue` (`:101-106` + `:199` + `:210`/`:215`)** | **modify-carve** |
```

and the same row's prose then states the opposite: *"**The grant is therefore `:101-110`, the projector WHOLE**, and `:101-106` is recorded as the ruling's spelling of its first six lines rather than silently widened away."* Re-measured at `81a56990` — `git show origin/master:demo/.../AnimationVisualizer.vue | sed -n '101,112p'` → `:101` `const progressFromPointerX = (clientX: number): number => {` … `:109` `return x / maxX;` · **`:110` `};`** · `:112` `const applyProgress`. **The body is ten lines; the coordinate column grants six.** G14's cure requires **deleting** the projector and repointing `:210`/`:215`; a seat executing from the coordinate column leaves `:107-110` behind — four orphan lines of a deleted function, exactly what the row's own prose says must not happen. **This is D-11's class, and D-11 was cured in this same round by re-cutting its coordinate column** (`:57` → **`:57-67`**, with `:57` struck by name). The two identical defects received two different cures inside one edit, and the one that kept the short numeral is the one whose gate is the wave's hardest. The SUBJECT-IDENTITY receipt carries the short extent too (*"`progressFromPointerX` ⟨`AnimationVisualizer.vue:101`, its call sites `:210`/`:215`, the ball rect read `:199`⟩"*), so `:110` appears nowhere in the receipt that certifies the wave's subjects. E-3 does not require this: E-3 preserves the *ruling's* spelling as a dated record — which the prose already does — it does not require the operative grant to be issued at the wrong extent.

### D-2 · HIGH — the G7 ordering template is asserted "applied whole, three ways"; the third way does not exist at KF.W6's final bytes

R5-6 ruled the G10 template must be applied in three places. This file declares it at **three sites** — §State (*"The template is now applied whole: here, at the `→ KF.W6` cross-edge, and at KF.W6's own G7-facts row"*), §Gates G7 (*"The template is now applied whole, three ways: … **(3)** the reciprocal at KF.W6's own G7-facts row is that seat's to state (R5-6(3))"*), and the `→ KF.W6` cross-edge (*"It is now declared at three places"*). Measured at KF-W6's **final bytes** (`15:17:38`, hash `b41e5576…daaa`, matching the cert):

```
$ grep -n 'R5-6' KF-W6.md                                                    → (empty)
$ grep -nE 'lands first|G7 measures after|atomic commit lands|execution order' KF-W6.md → (empty)
$ sed -n '558p' KF-W6.md   # the → KF.W8 cross-edge, the G7-facts row
… "Anchor-only per LAW C(3) — that seat writes later this round." …
```

**The sibling still orders only the repair round's writes** — the exact sentence PASS-5 D-4 convicted (*"states the write-order of the repair round … never the execution order of the waves"*). The reciprocal was never commissioned: RULINGS-5 §END's KF-W6 row lists **seven** directives (R5-1 · D-P5-2 · D-P5-3 · D-P5-4 · D-P5-5 · D-P5-6 · D-P5-7/8) and **R5-6(3) is not among them**. The comparison the gate itself draws makes the gap sharp: **G10's W4 dependency genuinely is three-way** — `KF-W4.md:269` reads *"DECLARED FROM THIS END, the reciprocal of KF.W8's re-cut G10"*. G7's is two-way. **This is the R-1 phantom-reciprocal class one generation on, in the wave that names R-1's phantom by name**: a spec asserting, in the present tense and in its own voice, that a sibling carries a declaration the sibling does not carry. Two of the three declarations are real and this seat verifies them; the assertion of the third is false at the bytes.

### D-3 · MEDIUM — the fold-transitivity figure the round converted to a citation is itself a double-count, and is reprinted at three sites

R5-7/D-6 ordered *"cite, don't count"*, and the file both cites **and** counts: §State, §Rows and §Excluded each print **"13 records / ≥34 lines, per `PASS-5/KF-W8-CHECK.md §1.3`"**. Re-run whole (§1.3 above): the **34 lines reproduce exactly** (13+8+6+3+3+1), and the record count is **12 distinct records, not 13** — `kf-PlaybackRibbon` appears in two rows of the cited table (once as a `KF-CE-12` off-record host, once as a `KF-AV-8` one) and the "13" is the row-sum. PASS-5's own headline carries the double-count; this spec inherited it verbatim at three sites. **The verdict is unmoved** — all thirty-four resolve as non-escapes and `0 escaped` is reconfirmed here independently — but the class is precisely the one D-6 convicted: a figure reprinted from an artifact that does not support it, now one generation on and inside the citation that was supposed to end the practice.

### D-4 · MEDIUM — LAW E(4) retires per-seat substrate stamping; the cure reached the LEDGER only, and twenty stamps survive in the body, eight of them asserting "FINAL stage-1 bytes"

The round-5 header states the stamps are *"struck to a pointer at the LEDGER below"*, and the LEDGER duly carries its strike and its SHADOW line. Outside it the mechanism is untouched. Re-grepped this seat: `16:55:55` ×**10** · `16:59:25` ×**5** · `15:39:04` ×**4** · `15:51:10` ×**1** · `17:02:55` ×2 · plus `16:58:59`/`16:56:52`/`16:59:44`, and the phrase *"final stage-1 bytes"* appears **8** times — at §Bounds' `engine/animation.ts` row, its `ingest/cssom.ts` row, the `package.json` DH-2 cell, the shared-directory DH-3 cell, the struck `shell/index.ts` row, G7's reachability clause, R-1's third re-measurement and R-4's verbatim cell. Every one of those substrates has moved twice since: KF-W5/KF-W6 are `2026-08-29 15:17:38` and KF-W7 is `15:18:37`, all hash-pinned at CLOSE-CERT-2 §9. **Nothing substantive moves** — this seat re-resolved every anchor those stamps ride and all resolve (§2) — but LAW E(4) is program-wide (*"per-seat substrate stamping … is retired"*), and the file that supplied D-7's own diagnosis (*"seats stamp clocks they cannot keep"*) still keeps twenty of them, eight declaring "FINAL" of bytes that are not.

### D-5 · MEDIUM — the ACCESS-COLUMN receipt's arithmetic reproduces and its scope is short by three rows: G3's two library-side serializer bodies and the export list it amends

The receipt was re-cut this round *with its counting rule stated* and invites the test (*"A pass-6 seat tests this by SCOPE: re-grep the access column, re-count under the rule"*). Its arithmetic **reproduces** (round-4 8 rows / 14 paths + round-5 8 rows / 9 paths = **16 / 23**) ✓. Its scope does not: `src/animation/compile/emit/format/format.ts` (*"Serializer copy 1 (`selectorText:20`)"*), `src/animation/compile/emit/css-text.ts` (*"copy 2 (`serializeSelector:58`)"*) and `src/animation/compile/emit/index.ts` (*"the export list G3 amends"*) are **`modify-carve` rows carrying G3's delete-and-repoint act** — the wave's unit-d core — and they appear in **neither** the 16/23 enumeration **nor** the "rows that changed access and owe no census" list. Measured at the frontier, the `0 owed` verdict **survives**: both bodies are file-local — `git grep -n 'serializeSelector' origin/master -- src test demo` → `css-text.ts:58` (decl) + `:75` (sole use); `git grep -n 'selectorText' origin/master -- src` → `format.ts:20` (decl) + `:132/:201/:280/:313` — so no consumer outside either file is repointed and no census is owed. The defect is the receipt's, not the rows': a sweep re-cut to be exhaustive over the access column omits the three rows the wave's own headline act operates on.

### D-6 · MEDIUM — the R5-5 SHADOW's terminus list omits KF.W10 while two of its five mints reach into a registered successor packet's surface

LAW F(1) requires *"citing/terminus waves notified = {list}"* at every mint. The R5-5 SHADOW names **KF.W6 · KF.W7 · KF.W4** and stops. Two of the five rows it mints are `demo/scenes/spring/StartingStyleTarget.vue:87` and `demo/scenes/easing/EasingTarget.vue:143`, and the first record says, at the bank: `kf-StartingStyleTarget.md:35` — *"**NO-WAVE-OWNER** = SS-1/SS-2 spec-authoring intake (**the spring artifact-truth packet**)"* — a packet enumerated as **KF.W11 cargo at `KF-W10.md:588 §6.D` row 1** (*"spring-artifact-truth"*), and again in the canonical 17-packet roster at `:612`. The sibling mint on the same round — the G14 SOURCE row — **does** notify KF.W10 (⟨§6.D — unchanged; the eight out-of-bounds G14 hosts keep their enumeration⟩) for a weaker reason. **The substance is sound and declared at the row itself** (*"`demo/scenes/**` is otherwise untouched by this wave … this row is one import line and reaches nothing else"*), so nothing is annexed; what is short is the notification list LAW F(2) hands the reconcile seat to re-sweep.

### D-7 · LOW — the SUBJECT-IDENTITY receipt states a counting rule and then prints no count for the block that rule was written for

The receipt declares, at the receipt: *"one subject = one ⟨symbol, path:line⟩ pair; a §Bounds row naming N symbol sites contributes N subjects."* Blocks 1 and 2 close with arithmetic (**14 present, 0 absent** · **5 present, 0 absent**), and this seat re-verified all nineteen at `81a56990`. Block 3 — *"the nine rows minted this round add their own subjects, each re-read at the same ref by the same rule"* — closes with **no figure at all**, while collapsing four `CopyButton` sites into one bullet and four `AnimationVisualizer` sites into another, i.e. exactly the N-sites-to-one-bullet shape the rule was written to forbid. Under R5-11 (*"the checks test receipt arithmetic before its scope"*) this is the one block with no arithmetic to test.

### D-8 · LOW — the act-coverage sweep, the round's new third enumeration class, is short of four booked acts

Its stated source is *"every ruling at §Sequencing and every `lands as` cell at §Rows"*, and it enumerates ten. Not swept: **R-5's `measure`-project adoption** (`§Rows`, *"unit g · G9 — adopt in place"*), **G10's `+@vue/test-utils` residual manifest act**, the **four `create` test files**, and the **two `create` doc paths**. Every one of them **is** covered by a §Bounds grant, so the sweep's verdict is true and no act is unbounded — but a receipt that claims *"Swept whole"* over an enumerable source and enumerates a subset is the receipt-arithmetic class R5-11 named, in the very class R5-11 commissioned.

### D-9 · LOW — the `useToolbarKeyboard.ts` carve grants the docblock's closing token

§Bounds carves `:25-28` for *"the docblock prose only"*. Re-read at the frontier — `git show origin/master:…/useToolbarKeyboard.ts | sed -n '23,30p'` — `:25` carries the CopyButton name, `:26`/`:27` are its continuation, and **`:28` is ` */`**, the comment terminator. A prose carve that includes the block's closing token is the mirror of the extent class this round cured elsewhere: it is not short, it is one line long. Recorded rather than graded higher because the correction target (`:25`) is unambiguous and no gate turns on the extent.

### D-10 · LOW — KF-W6's KF-CB-37 coordinate has now died a FIFTH time, and the file records four

R-1's paragraph is this file's own monument to coordinate mortality: it records `:410` → `:417` → `:445` → `:514`, with `:514` explicitly *"recorded as a fourth measurement, not as an anchor."* Re-run at W6's final bytes this seat: `grep -n 'KF-CB-37' KF-W6.md` → **`:558`**. The receipt survives (the §-heading + cross-edge label are the anchor, and the guard is byte-exact at `:558`), which is the paragraph's whole point — but the enumeration inside it now describes four of five deaths, and the same pattern recurs at every other W6/W5/W7 coordinate this file records. An INFO-weight observation, booked so the next round's re-anchor seat has the current reading and does not re-derive it.

---

## 6 · WHAT HOLDS

Recorded so no later seat re-litigates settled ground. **The census is CLEAN by record** — 32 routed / 32 booked / **0 escaped**, a **fourth** independent derivation agreeing id-for-id with PASS-3, PASS-4 and PASS-5. **All fifteen gates reproduce at the frontier**, falsifiers 15/15, all fifteen still born RED. **All ten LAW-A censuses re-derive from the import graph**, A-9's seven and A-5's four id-for-id, A-10's round-4 correction (3 → 4) holding. **LAW E(2)'s hash gate passes for all eleven specs** — the round closed lawfully and every repair banks. **Four of the five pass-5 HIGHs are cured at the bytes and not merely asserted**: G3's seven repoints have grants (three new rows, two carve re-anchors verified at `useKeyframeOps.ts:9` / `timelineEngine.ts:16`, two declared exceptions); R-1's five MUST-CARRY edits have grants, and the move/carve split on `KeyframeCard.vue` is stated with its two-commit discipline; G11's carve is `:57-67` = its falsifier's measured extent; the AUDITED cell is a pointer **and** its pointed-to figures still reproduce (15 · 26 · 8, with the named-not-routed set enumerated correctly). **The DH-3 four-party declaration is now consistent at both ends** — W7's `:77` is anchor-only and restates no verb, no glob and no count of W8's, which is the cleanest cure in the round. **LAW D's verbatim arm produces ZERO convictions for a second consecutive pass**: every registry and sibling quotation byte-diffed here is exact, and the drift class that dominated passes 3–5 is dead at the words. **MECHANISM F runs clean** — this wave mints no successor token. **The EH-4/5/8 restoration is verified at KF-W6's own bytes with the banked anchors true at the frontier, and it costs this wave nothing**: W8 books no EH id, R4-1's census and delete survive R5-1 intact, and G7's inbound dependency is unharmed. **The residue is two coordinate/declaration defects and four receipt-scope defects — not measurement.** The file measures the tree accurately, bounds every act it books, and the one act it bounds at the wrong extent is the one whose ruling handed it a short numeral.

---

*Instruments (all read-only, zero writes to product source): `git rev-parse` · `git grep` · `git ls-tree` · `git show` · `git cat-file` · `git status --porcelain` at `/Users/mkbabb/Programming/keyframes.js`; `grep`/`sed`/`awk`/`sort`/`comm`/`uniq`/`stat`/`shasum` over the value.js registry, conformance, carry, formation, precepts and wave trees. Frontier ref `origin/master 81a56990736ced5b5edde0b84c527680ac7689b1`, re-resolved at the head of this pass; local `8281638c` opened only to confirm its disqualification (252 dirty rows). No product source was opened for writing; no registry record, no wave spec and no prior-pass artifact was edited — the only file written by this seat is this register.*
