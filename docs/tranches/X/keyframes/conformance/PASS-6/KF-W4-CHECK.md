# PASS-6 · KF-W4 — FRESH ADVERSARIAL SPEC CHECK (L-18 / L-20)

**Subject**: `docs/tranches/X/keyframes/waves/KF-W4.md` — 334 lines, 232,774 B, its round-5 final bytes.
**Seat**: FRESH. `PASS-5/KF-W4-CHECK.md`, `PASS-5/RULINGS-5.md` and `PASS-5/CLOSE-CERT-2.md` were read **only** to learn what the round claims it cured and which laws now bind. No denominator, count, anchor or partition was inherited from any of them; §1 below was harvested from the 58 records' bytes before any prior figure was consulted.
**Substrate law**: every keyframes-side witness in this register was re-executed read-only at **`origin/master 81a56990736ced5b5edde0b84c527680ac7689b1`** (`git rev-parse origin/master`, re-resolved this seat) in the sibling checkout `/Users/mkbabb/Programming/keyframes.js`. That checkout's local HEAD is `8281638c` with a dirty worktree; every command below names `origin/master` explicitly, so no working-tree byte enters this register. Zero product writes; the sole write of this seat is this file.
**Census law**: §1 is a per-record, id-keyed harvest across all **58** `kf-*.md` at `docs/tranches/V/megatranche/registry/adjudicated/`. Sole carry consulted: `docs/tranches/X/keyframes/carry/KF-W6-CARRY.md`.
**Date**: 2026-08-29.

---

## §0 · THE LAW E(2) HASH GATE — RUN FIRST, AS ORDERED

RULINGS-5 LAW E(2): *"The pass-6 union seat re-hashes all 11 as its FIRST act; any mismatch is the round's own conviction."* Executed at this seat before any other read:

```
$ shasum -a 256 docs/tranches/X/keyframes/waves/KF-W*.md
fd226ecb…d09a  KF-W0.md      f6986de7…12f0  KF-W1.md      1c107329…1c50  KF-W10.md
5f3656e3…11a4  KF-W2.md      4b68baf3…86f7  KF-W3.md      4028327c…fe68  KF-W4.md
8baba077…404d  KF-W5.md      b41e5576…daaa  KF-W6.md      ed5f0851…6f21  KF-W7.md
c7c11445…a1c9  KF-W8.md      bea2d9a6…c744  KF-W9.md
```

**All eleven match `PASS-5/CLOSE-CERT-2.md` §9's closing column exactly. ZERO mismatches. The round-5 write-set is proven; no spec moved after the cert; the round closed lawfully under LAW E(1)/(2) and its repairs bank.** LAW E's first hash-bracketed round is clean, and the instrument works — this is the one thing in the program that is now provable rather than asserted.

---

## §1 · HEADLINE

| axis | verdict |
|---|---|
| **1 · id-keyed census, by record** | **HOLDS — 111 routed · 111 booked · 0 escaped.** A per-id `grep -oF` loop over 107 distinct id tokens returned **no zero**. Zero escape for a second consecutive pass, on a denominator built by record. |
| **2 · receipt reality (the drift class)** | **HOLDS — the class is DEAD at this wave.** All three PASS-5 receipts are cured: `KF-W0.md:588` and `KF-W6.md:145-146` are retired to anchors that resolve **uniquely** at the siblings' current bytes, and the → KF.W8 characterization is restated and now **true at W8's final bytes on both counts**. Zero live sibling coordinates survive. |
| **3 · M-25 depth + mechanism S (LAW F)** | **HOLDS.** Twenty-three pasted commands re-run; **twenty-three reproduce to the digit** — the first clean numeric sweep this wave has produced. D-1/D-7/D-8/D-9 all cured at the bytes. The KF-ES-16 strike carries its SHADOW, and this seat re-read **all 26 surviving row-1 chain members' banks**: not one folds to row 2's head. The cure ran the stated scope. |
| **4 · gates: reachable GREEN, LAW A, lawful landings** | **FAILS on one of fourteen.** G-KFW4-11 is convicted a **third** consecutive pass, at a fourth address: its artefact is defined two incompatible ways and the gate is defective under both readings, and its oracle detects a narrower set than its falsifier asserts. LAW A passes BY SCOPE (11 censuses + 1 declared). |
| **5 · posture** | **HOLDS at every obligation, each verified at the far end.** W4 head (8 of 8 records re-read, AmigaScene strike correct at 0), W3 gated, O-21, §6.D, W8 reciprocity, KF-AV-28, the W11/W12/W13 disposition. |

**VERDICT: DEFECTIVE. 111 routed · 111 booked · 0 escaped · 12 defects (0 CRITICAL · 4 MAJOR · 5 MINOR · 3 INFO).**

**The shape of the failure has changed again, and the change is the finding.** Round 5 cured the drift class outright and cured every arithmetic defect at its enumeration — the two axes that carried pass 5's centre of gravity now hold. What it did not do is re-run **its own scope receipts** over the round it was executing: the numeric arm still enumerates round 4's three exceptions while the file's own cure cells record four more, and the citation-of-record was never swept, so the file publishes **two contradictory census readings** and certifies a gate cure the next pass overturned. The one gate it re-cut, it re-cut into a different defect.

---

## §2 · AXIS 1 — ID-KEYED CENSUS, BY RECORD

### 2.1 The record-level sweep

`grep -cE 'KF\.W4|KF-W4|\bW4\b'` run per file over all 58 records, this seat:

| class | n |
|---|--:|
| carry ≥1 W4 byte | **45** |
| carry ZERO W4 bytes | **13** — kf-App.skeleton · kf-CSSPasteDialog · kf-CubeTarget · kf-KeyboardShortcutsModal · kf-KeyframesAddDialog · kf-SequenceAxis · kf-SequencePlayhead · kf-SequenceScene · kf-SequenceScrubber · kf-SharePopover · kf-SpringHeatmap · kf-SpringTarget · kf-SquareInstrument |

45 + 13 = **58**. Of the 45 byte-carriers, **40 supply ≥1 W4-routed id** and **five supply none** — kf-CubeAxisLines **3** · kf-DemoGlobalChrome **1** · kf-EditorHeader **1** · kf-KeyframeCard **1** · kf-PlaybackRibbon **2** (each re-measured; all five outputs reproduce the AUDITED cell exactly). Three zero-byte records are nevertheless carried by the spec and counted as routed rows because it books them.

**The AUDITED cell verifies by enumeration, not by assertion.** Extracted and de-duplicated this seat, its roster is **exactly 49 distinct names, every one a real registry file**, and the complement is exactly the nine W4-silent non-carriers (kf-App.skeleton · kf-CubeTarget · kf-KeyframesAddDialog · kf-SequenceAxis · kf-SequencePlayhead · kf-SharePopover · kf-SpringHeatmap · kf-SpringTarget · kf-SquareInstrument). **49 + 9 = 58**, and the cell's own composition **40 + 5 + 4 = 49** sums with every member re-measured. The balance's four zero-byte members are 4 of the 13; the other 9 are the unconsulted set. The arithmetic closes in both directions.

### 2.2 Per-record routed ids (the denominator)

Convention (this seat's, stated so it is falsifiable): one row per **banked id or explicitly-banked rider** a record routes to `KF.W4` / `KF.W4-PROSE`, including witness-only and rider-only routings; merged groups counted at their member ids; the three zero-byte records the spec carries counted at the rows it books.

| # | record | routed ids | n | booked |
|--:|---|---|--:|:--:|
| 1 | kf-AmigaScene | C-19/L-m1 | 1 | ✓ |
| 2 | kf-AnimatedText | KF-AT-4 · -7 · -10 · -12 · -24 | 5 | ✓ |
| 3 | kf-AnimationControlsGroup | C-4 | 1 | ✓ |
| 4 | kf-AnimationVisualizer | KF-AV-8 · KF-AV-9 · KF-AV-18 · KF-AV-28 (rider, `:35`) | 4 | ✓ |
| 5 | kf-App | KF-APP-4 | 1 | ✓ |
| 6 | kf-CSSCodeEditor | KF-CE-12 (gate arm) · KF-CE-16 · KF-CE-30 | 3 | ✓ |
| 7 | kf-ChannelControls | C-6 · D-7/L-4 · C-13 · C-15 · L-18 · L-19 · N-7 | 7 | ✓ |
| 8 | kf-ChromeDock | C-2/B-2 · `:128` · C-8 · m-8 · m-1/C-7 · m-9 | 6 | ✓ |
| 9 | kf-ChannelOptions | KF-CO-7 · KF-CO-33 | 2 | ✓ |
| 10 | kf-CopyButton | KF-CB-8 · -18 · -24 · -29 | 4 | ✓ |
| 11 | kf-CubeScene | L-8/C-9 · L-18/C-12a · L-10/L-11 | 3 | ✓ |
| 12 | kf-ControlsPaneWrapper | N-1/C-5 · M-5/C-9 | 2 | ✓ |
| 13 | kf-EasingSidebar | KF-ES-3 · KF-ES-34 | 2 | ✓ |
| 14 | kf-EasingScene | KF-ES-14 · KF-ES-16 | 2 | ✓ |
| 15 | kf-EasingTarget | KF-ET-6 · KF-ET-11 | 2 | ✓ |
| 16 | kf-EditorStartScreen | L-EST-16 · L-EST-17 · KF-EST-1 · KF-EST-6 | 4 | ✓ |
| 17 | kf-EditorShell | L-4/C-6 · L-6/C-18 · C-21 · D-19 | 4 | ✓ |
| 18 | kf-HeroAurora | KF-HA-4 · KF-HA-5 | 2 | ✓ |
| 19 | kf-KeyframeCardList | KC-13 · KC-17 · KC-37 | 3 | ✓ |
| 20 | kf-KeyframeTimeline | C-12 · RR-B missed-1 · L-14 (K3) | 3 | ✓ |
| 21 | kf-KeyframesStringControls | L-BL-4 | 1 | ✓ |
| 22 | kf-KeyframesEditor | KF-KE-14 | 1 | ✓ |
| 23 | kf-LayerConfigPanel | LP-7 (+ the `--project demo` rider) | 1 | ✓ |
| 24 | kf-KfPillTabs | C:C-1/L:D-3/D:D-10 · D:D-8 · L:D-8 · header mangle | 4 | ✓ |
| 25 | kf-MbabbMenu | MM-9 · MM-13 · MM-36 | 3 | ✓ |
| 26 | kf-MatrixEditor | ME-5 · ME-32 · ME-29 | 3 | ✓ |
| 27 | kf-OrbitalDrag | OD-29 | 1 | ✓ |
| 28 | kf-RibbonBar | B-1 | 1 | ✓ |
| 29 | kf-SequenceTarget | L-4 · ST-5 | 2 | ✓ |
| 30 | kf-SpringScene | KF-SS-34 | 1 | ✓ |
| 31 | kf-SpringTrace | L-4/C-8 · L-7/C-6 · L-13/C-7 · D-1 (`:48-49`) | 4 | ✓ |
| 32 | kf-SpringPhysicsFacet | SPF-26 · SPF-27 | 2 | ✓ |
| 33 | kf-SquareScene | L-14/C-13.1 (+C-13.4) · MISS-6 · the closing verdict `:166`/L-19 | 3 | ✓ |
| 34 | kf-StartingStyleTarget | KF-SST-37 | 1 | ✓ |
| 35 | kf-TimelineCaret | L-4 · L-7 (witness only) | 2 | ✓ |
| 36 | kf-TimelineTrack | C-10 · C-11 · D-m6 · m-1 · D-m5 · M-5/C-5/L-M-5 | 6 | ✓ |
| 37 | kf-TimingFunctionPanel | KF-TFP-17 | 1 | ✓ |
| 38 | kf-TimelineHoverPreview | L-D2 · L-D3 · L-D7 · REGISTER-LAUNDER · C-S+1 | 5 | ✓ |
| 39 | kf-TransportDock | TD-19 (typecheck half) | 1 | ✓ |
| 40 | kf-TypingDots | KF-EST-1 · KF-EST-6 · L:D-2 · L:D-15 | 4 | ✓ |
| — | **subtotal (40 suppliers)** | | **108** | |
| 41 | kf-KeyboardShortcutsModal (0 bytes) | R-9 | 1 | ✓ |
| 42 | kf-SequenceScene (0 bytes) | D25/L-5/C-7 | 1 | ✓ |
| 43 | kf-SequenceScrubber (0 bytes) | ruling 12 | 1 | ✓ |

**Non-suppliers re-checked by subject, not by count**: kf-CubeAxisLines' three bytes are AXISLINE-UNIT sequencing prose (`:34`, `:53`) and its verdict; KF-AX-31 routes to KF.W6 at §Excluded 17. kf-KeyframeCard's one byte is its routing law; KF-KC-35 routes to KF.W12 at §Excluded 18. kf-EditorHeader's one byte is its routing law — the EH-4/5/8 family restored by R5-1 is **KF.W6's**, verified at that wave (24 `EH-4` · 32 `EH-5` · 24 `EH-8` tokens, `EditorShell.vue:45` ×5, `:46` ×3, `MbabbMenu.vue:20` ×3, five SHADOW lines), and W4's only EH obligation is EH-14, booked at §Excluded 6 and declared out of G-KFW4-1's reach in the falsifier.

### 2.3 Verdict on axis 1

| figure | value |
|---|--:|
| **routed total (this seat's own by-record denominator)** | **111** |
| **booked** | **111** |
| **escaped** | **0** |

**Method, so it is reproducible**: 107 distinct id tokens (the merged groups' members and the multi-id chains resolve to shared tokens) were passed through `grep -oF -- '<id>' KF-W4.md | wc -l`; **the loop printed no zero**. `m-9` → 11. `KF-ES-16` → 12 (the strike narrative plus rows 2/4/25). `KC-17` → 3 · `m-8` → 3 · `L-6/C-18` → 4 · `m-1/C-7` → 3 · `D-m5` → 4 — round 3's five and round 4's one all verify booked at the current bytes.

**Carry-table arithmetic re-derived**: `31` rows counted by pattern, matching the file's claim; `14` distinct `G-KFW4-n` gates; §Excluded 15 numbered routings + the 6-row residue table. Every structural figure the file states about itself reproduces.

**One granularity note, carried rather than smoothed** (it is D-7 of this register): the 111 counts three rows no record routes to W4 — see §6 D-7.

---

## §3 · AXIS 2 — RECEIPT REALITY: THE DRIFT CLASS, TESTED AT THE SIBLINGS' CURRENT BYTES

### 3.1 LAW E(4) compliance, tested by regex

| probe | result |
|---|---|
| live sibling coordinates `KF-W<n>.md:NNN` | **4 tokens, and every one sits inside an explicitly-declared RETIREMENT block** — `KF-W0.md:447` and `:588` (D-2's own three-spelling history), `KF-W2.md:270` (struck at round-2 close, U-2/R2-7), `KF-W6.md:145-146` (D-6's retired coordinate). **Zero live coordinates ride any receipt.** |
| transcript spelling, regex A (`grep -n '…' KF-W<n>.md → :NNN`) | **0 hits.** The U5-B class is absent here. |
| CARRY class, regex B | **4 tokens, all bare `KF-W6-CARRY.md`, zero coordinates.** |
| per-seat substrate stamps | **0.** The three `FINAL bytes` occurrences all say verification at final bytes is `PASS-5/CLOSE-CERT-2.md`'s act, not this seat's — LAW E(4) executed to the letter. |
| §-anchors into siblings | 9, all stable-anchor form: `KF-W0 §Sequencing` ×2 · `KF-W2 §Excluded` · `KF-W6 §W6-C` · `KF-W8 §Bounds` · `KF-W8 §Gates` ×2 · `KF-W10 §6.D` · `KF.W0 §B-12` ×2 |

### 3.2 Every anchor resolved at the sibling's current bytes

| # | anchor | resolution this seat | verdict |
|--:|---|---|:--:|
| A1 | `KF-W0 §Sequencing`, the OUTBOUND row *"the NO-WAVE-OWNER packets — the canonical 17"* | `grep -c` → **1**, unique | ✓ |
| A2 | `KF-W6 §W6-C`'s *"R-6 MANIFEST RECEIPT (the KF.W4-PROSE limb intake)"* | `grep -c` → **1**, unique, and reciprocal — the row books the limbs id-for-id with *"the id-anchor stays at W4 (rows 29/30)"* | ✓ |
| A3 | `KF-W2 §Excluded`, the row *"The cube · sequence · spring · dock-menu · CARD-UNIT packets"* | `grep -c` → **1**, unique, routing restated on the canonical 17 | ✓ |
| A4 | `KF-W10 §6.D · SUCCESSOR-FORMATION REGISTER` | present, with the KF.W12 row live, and **reciprocal**: W10 names *"KF-W4 §Sequencing, the R-15 homing block — the minting authority"* | ✓ |
| A5 | `KF-W1`'s O-21 mint | minted under the `max+1` MINT LAW, booked at C-10, gated at G-KF1-7 | ✓ |
| A6 | `KF-W8 §Bounds` / `KF-W8 §Gates` | W8 carries `## Gates`; both anchors resolve — **not** the `§Rulings` phantom class CLOSE-CERT-2 R5-R4 convicted at W10 | ✓ |

**Six of six resolve, uniquely, with no coordinate transcribed. The drift class the two-stage law was built to kill is dead at this wave** — the first pass at which that can be said.

### 3.3 The → KF.W8 characterization (PASS-5 D-3) — CURED, and cured *better* than the ruling asked

The struck sentence claimed W8's G10 was born RED *"solely"* on the absent `plugins` array with G11/G12 *"gated on G10"*. Verified this seat at **W8's final round-5 bytes**:

- **G10 GREEN has two legs**, in W8's own words: *"(a) W4's commit 2 landed … (b) this wave's own residual act: `+@vue/test-utils` devDep — the ONE manifest change this wave needs."* W4's restatement is exact.
- **G11** — *"Gated on the KF.W4 commit-2 dependency-cite (G10)"*. **G12** — *"Gated on the KF.W4 commit-2 dependency-cite (G10) and KF.W7's R-7 verdict."* W4's restatement — *"neither is gated on G10 the sibling gate … both are gated on the KF.W4 commit-2 dependency-cite"* — is **exactly right**.
- **The in-cell dissent against PASS-5's own reading is correct and is the register's most creditable line this round.** PASS-5 D-3 probed for the bare phrasings `gated on G10 | behind G10 | after G10 | G10 first`, found none, and read that as *"neither is gated on G10 anywhere in the file"*; the relation is present under a different spelling, and W4 found it, recorded the difference rather than absorbing it, and cured the convicted half anyway. **A repair seat that corrects the check that convicted it, and still executes the cure, is the discipline this program has been trying to install for six passes.**
- The one frontier fact kept: `git show origin/master:vitest.config.ts | grep -c plugins` → **0**, re-run and reproduced.

---

## §4 · AXIS 3 — M-25 DEPTH, MECHANISM S, AND THE NUMERIC ARM AT THE FRONTIER

### 4.1 Twenty-three commands re-executed. **Twenty-three reproduce.**

`@src/` in `demo/` → **9 lines** (7 specifiers / 6 files + 2 declared prose) · **the G-KFW4-11 pipeline run verbatim → exactly the seven pairs the gate prints, in the gate's own sorted order** · `serializeEasing` → **49 lines**, partitioning exhaustively as **17 calls + 1 declaration + 2 published re-exports + 8 import specifiers + 9 prose + 12 in-test = 49** (**D-1 CURED — the seventeen sum 2+3+6+2+4 by file and every claimed coordinate is a call**) · `seedFor|syncGap` in `demo/` → **6**, and `sed -n '112p'` → `    if (name in bezierPresets) {` under its own command (**D-9 CURED**) · `.dependency-cruiser.cjs:84-86` → the three lines **with `.map((m) => m.replace(/[-]/g, "\\$&"))` restored** (**D-8 CURED**) · `useKfPillTabs` → **12** (**D-7's re-sum CURED: 1+3+2+1+3+2 = 12 under one stated rule**) · `INERTIA_FACTOR` in `test/` → **9** (declaration + eight uses) · the eight citation targets → **13·5·3·3·3·3·2·1 = 33 lines / 19 distinct names**, and the 19 match R-7's partition **member-for-member** across cells (a) 17 / (b) `brittleness` / (c) `publish` — **the partition is TOTAL at the bytes** · whole-tree `proof:` → **273 lines / 106 names** · `package.json:50-52` → the three runnable scripts, byte-exact · `test/demo` → **27**, and **all 27 are `.test.ts`** (the collect glob's subject verified, not assumed) · `test/demo/instrument` → **9** · `scripts/gates/` → **9** · `_boundTimeline` → **3 hits / 1 file / 0 reads** · `TimingFunctionNames` → **12 hits / 6 files** · `LIGHT_BARREL_MODULES` → **24 entries, exactly five dead**, each verified by `git cat-file -e` per entry · `Stylesheet` in `load-engine.ts` → **1** · `declare module "*.vue"` → 1 declaration + 1 prose, and `tsconfig.test.json`'s `"include": ["test/","bench/","demo/env.d.ts"]` with its `:12-15` comment **byte-exact** · `.vue` specifiers **62** in `demo/`, **0** in `test/`, over **58** tracked SFCs · `const props` in `…/timeline/` → **4**, the row's three among them at their stated lines · `Teleport` in ChannelOptions → **3** (`:377` open · `:401` close · `:437` import) · `proof:idioms` → **3** incl. `layout.css:10` · `proof:bezier` scoped → **1** · `EditorShell.vue` `proof:` → **0** · `vitest.config.ts` `plugins` → **0**, projects at `:38-56`, demo include at `:52` · every `package.json` / `ci.yml` / `tsconfig.json` anchor in §Bounds (`:36-38`, `:44-46`, `:70`, `:77`, `:81`, `:92`; `ci.yml:41-42`, `:50-55`; `tsconfig.json:8/:9/:47`) resolves byte-exact.

**This is the first pass at which the numeric arm produces no exception.** All four of PASS-5's arithmetic defects are cured at their enumerations with their counting rules stated. The arm's *receipt* is a different matter — D-3 below.

### 4.2 Mechanism S (LAW F) — the round's one moving act, tested over the stated scope

The round performs one act with a moving verb (the `KF-ES-16` head strike) and one routing-scope correction (`KF-CO-33`); both carry SHADOW lines, and line 9 carries the round ledger LAW F(2) requires. The bank was read **per row at its own anchor** (LAW F(3)): `sed -n '55p' kf-EasingScene.md` folds the id *"by reference to banked KF-CB-8 / KF-ET-11"* — row 2's head. The strike is correct and its quote is byte-faithful with elisions marked and no emphasis added (R5-13 satisfied).

**And this seat ran the cure's own predicate over the WHOLE surviving chain, which is what R4-10(1) and R5-11 demand and what pass 5 convicted round 4 for not doing.** All 26 remaining row-1 chain members were resolved at their banks: KF-CE-16 · KF-CO-7 · KC-13 (`= KF-CE-16`) · L-14/C-13.1 (`≡ KF-APP-4`) · KF-AT-12 · KF-HA-5 (`FOLDS KF-AT-12`) · ME-5 (`FOLD ≡ KF-APP-4`) · MM-36 (`FOLD ≡ KF-APP-4`) · LP-7 (`≡ KF-CO-7 ≡ KF-APP-4`) · SPF-26 · B-1 · L-4 (SequenceTarget) · L-8/C-9 · L-4/C-6 · M-5/C-9 · C-2/B-2 · M-5/C-5/L-M-5 (`= KF-CE-16`) · L-4 (Caret, `= KF-CE-16`) · L-D3 (`= KF-CE-16`) · L-BL-4 · KF-KE-14 (`= KF-CE-16`) · L-EST-16 · L:D-2 · TD-19 · R-9 · RR-B missed-1 (`= KF-CE-16`). **Not one folds to `KF-CB-8` as its head identity. The scope is clean; the third strike was the last one owed.** Carriage is unaffected: the id sits at three rows before the act and three after.

### 4.3 Depth, consumed by mechanism

Sampled hard and **sound**: row 1 carries kf-KeyboardShortcutsModal's R-9 dissent *against its own founding premise* and answers it; it carries KC-17's honest disclaimer and KC-13's ordering dissent; row 3 carries LP-7's rider identity at all three legs; row 4 carries the kf-TimelineTrack merged group at its full five ids with both dispositions; row 27 refuses to claim banked `L-19`'s cure and lifts only the closing verdict's LAW; rows 30/31 preserve three KILLED sub-claims by name; the `kf-ChromeDock:41` rider sentence is quoted **entire** and matches the bank word-for-word, with `:90`'s count-integrity clause beside it. The R-1 manifest is id-for-id and reciprocal at W6's live receipt.

---

## §5 · AXIS 4 — GATES, LAW A, LAWFUL LANDINGS

### 5.1 Gate-by-gate

| gate | witness re-derives at `81a56990`? | GREEN reachable in-bounds? |
|---|---|---|
| G-KFW4-1 | ✓ — every day-one diagnostic's file/flag verified; five ChromeDock riders booked with the banked sentence entire | ✓ (falsifier scope: see D-8) |
| G-KFW4-2 | ✓ (`ci.yml:53-55`; `vitest.config.ts:48-55`, no `plugins`; 27 tracked specs, all `.test.ts`) | ✓ |
| G-KFW4-3 | ✓ (ME-29's keyless `v-for`; `lint = depcruise src`) | ✓ |
| **G-KFW4-11** | ✓ — the seven-specifier inventory and the new pipeline both reproduce exactly | **✗ — see D-1 and D-4** |
| G-KFW4-12 | ✓ (`monaco-themes` at `:92`; consumer set ∅ — vendored-sibling trap named) | ✓ |
| G-KFW4-4 | ✓ (every in-bounds K3 site verified by subject; four `const props` in `…/timeline/`, the row's three among them) | ✓ — L-7's cross-wave exception declared |
| G-KFW4-5 | ✓ (`registry.ts:36`/`:43`; `easing-serialize.ts:69-73`) | ✓ |
| G-KFW4-6 | ✓ (`leaves.ts:17-19` one sentence, both halves false; `:28` exact) | ✓ |
| G-KFW4-7 | ✓ (`backward.ts:30/:32/:47`) | ✓ |
| G-KFW4-8 | ✓ (33 lines / 19 names verified; `brittleness` carve declared; `publish` resolves to `package.json:51`) | ✓ — partition TOTAL at the bytes |
| G-KFW4-9 | ✓ (`usability.mjs:239`; `orchestration-api.test.ts:142-148`; the eight `INERTIA_FACTOR` uses) | ✓ |
| G-KFW4-10 | ✓ (`font-roles.json` spans) | ✓ |
| G-KFW4-13 | ✓ (`types.ts:25/:27/:195`; the two prose phantoms path-qualified) | ✓ |
| G-KFW4-14 | ✓ — `seedFor` at `EasingSidebar.vue:99` reaching `bezierPresets` at `:112`, and **absent from `useEasingDemo.ts`** | ✓ — the round-3 cure holds at a third clock |

**13 of 14 re-derive with reachable GREEN. G-KFW4-11 is convicted a third consecutive pass, at a fourth address inside itself.**

### 5.2 LAW A — tested BY SCOPE, and its arithmetic tested first (R5-11(2))

**Receipt arithmetic**: *"TWELVE rows — eleven carry a census HERE, and the twelfth's is deferred"* → 5 pre-existing + 6 written at round 4 = 11, + 1 deferred = 12. **Sums.** **Scope**: the access column was re-read this seat and every delete / symbol-removal / repoint / shim-retirement / move row is in the twelve; the declared non-members (prose-only removals; test-body and config-value edits carrying no graph edge) hold on inspection — `registry.ts`'s memoisation removes no key (the fence forbids it), `tsconfig*.json` and `ci.yml` are config values, the `lint` redefinition moves no edge. **Eleven censuses present and sound at re-run, the twelfth declared. LAW A passes BY SCOPE for a second pass**, and every one of its eleven outputs reproduced at the frontier above.

### 5.3 Lawful landing surfaces

Every act traced to a §Bounds row. The four-party `package.json` carve and the four-party `test/demo/instrument/` disjointness both hold and are declared from this end; the nine tracked files enumerate to 9 and the created filenames are disjoint across all four claimants. **One act lands nowhere lawful — the gate command authored this round: see D-9.**

---

## §6 · DEFECT REGISTER (12)

### D-1 · MAJOR — G-KFW4-11's artefact is defined two incompatible ways, and the gate is defective under **both** readings

**Claim.** Command column, part (1): *"`npx depcruise --config .dependency-cruiser.cjs src demo --output-type json > evidence/KF-W4/depcruise-inventory.json` … The run must REACH `demo/`; a config still scoped to `src/` reports **zero** demo modules, and an empty reported set is a **RED by part (2)** — which is how blindness fails this gate instead of passing it."* Part (2): *"`git grep -n '@src/' origin/master -- demo/ | … | sort > actual-specifiers.txt` then `diff actual-specifiers.txt evidence/KF-W4/pinned-seven.txt` — **exit 0 is the gate, and nothing else is**."* §Artefacts: *"`depcruise-inventory.json` — **the pinned SEVEN specifiers / SIX files, hashed** … plus the five dead LIGHT-allowlist entries with their live twins and the config's own literal path · `pinned-seven.txt` … written by `depcruise-inventory.json`'s own writer"*, and the falsifier: *"the artefact is the diff's right-hand side and is written **before** the first cure."*

**Receipt.** Part (1) makes `depcruise` the writer of `depcruise-inventory.json`; §Artefacts makes that same file a curated inventory containing three things `depcruise --output-type json` cannot emit (a hash of the pinned pairs, the five dead LIGHT entries with their live twins, and the config's literal path). One path, two writers, two contents — and the gate's only oracle takes its right-hand side from it. **Both readings convict:**

1. **RHS is the frozen pinned list** (as §Artefacts and the falsifier's *"written before the first cure"* say). Then part (2) never reads the depcruise report at all — its left side is `git grep` over the repo, its right side is a static file. Re-run this seat: the `git grep` pipeline returns the seven pairs **whether or not `depcruise` ever reaches `demo/`**, so `diff` exits 0 over a config still scoped to `src/`. *"An empty reported set is a RED by part (2)"* is **false**, and the reach arm — the entire subject of the gate — has **no falsifier at all**.
2. **RHS is regenerated from the run.** Then the gate derives its own oracle at run time from the very run it is checking — **G-KFW4-9 rule (e)**, *"a gate may not re-derive its own oracle"*, which this file authors two units away — and *"written before the first cure"* is false.

**Why it matters.** §Gates opens by convicting *"a gate whose GREEN cannot be reached inside its own bounds."* Round 4 cured this gate's denominator; round 5 cured its two-oracle split and, in the same edit, replaced the unreachable-GREEN defect with an **undetectable-RED** one: the blindness the gate exists to see now passes it silently. This is G-KFW4-11's third consecutive conviction and its fourth distinct address (three-site inventory → two oracles → reach arm unfalsifiable → artefact identity). The remedy is one clause: make part (2)'s left side the **depcruise report's** demo-module set, or state plainly that the reach arm is a declared, unfalsified prerequisite and stop claiming part (2) catches it.

### D-2 · MAJOR — twelve live `PASS-4` citations republish a superseded census; the file states its own carriage two contradictory ways

**Claim.** Line 9 publishes the round's authority: *"`conformance/PASS-5/KF-W4-CHECK.md` … whose census reading this file cites and does not re-assert (LAW B): **111 routed · 111 booked · 0 escaped · 10 defects (0 CRITICAL · 5 MAJOR · 5 MINOR)**."*

**Receipt**, this seat:
- `grep -oh 'PASS-[0-9]/KF-W4-CHECK\.md' KF-W4.md | sort | uniq -c` → **PASS-4 ×12**, PASS-5 ×2.
- `grep -c '110 booked' KF-W4.md` → **4**. The **AUDITED** cell, the **SPECIFIED** cell, the **carry-table preamble** and the **§Excluded head** each cite `PASS-4/KF-W4-CHECK.md §1` and publish *"111 routed · **110 booked · 1 escaped**"* — the reading line 9 supersedes, in the same file, in cells whose whole purpose is to be the file's citation of record under LAW B.
- The §Excluded head additionally publishes PASS-4's register: *"whose §6 registers **eight** defects (1 CRITICAL · 3 MAJOR · 4 MINOR)"* — against PASS-5's **ten** (0 CRITICAL · 5 MAJOR · 5 MINOR), which line 9 prints thirty lines earlier.
- **The sharpest instance is §Gates' head**, still citing `PASS-4 §4` and certifying *"one gate — G-KFW4-11 — born-RED with UNREACHABLE GREEN … **That defect is cured this round (R4-2 …)**"* — a cure PASS-5 convicted as incomplete and round 5 itself re-cut one section below.
- §Sequencing carries the same class at three cells (the eight-record enumeration, the 17-packet homing, the posture axis), all citing `PASS-4 §5`.

**Why it matters.** The AUDITED cell states the governing rule in its own voice — *"re-pointed from the PASS-3 reading **under this file's own standing instruction that a superseded citation is re-pointed, never re-asserted**"* — and round 5 did not execute it. RULINGS-5 **R5-9 row 1** made this exact class a named, owned cure, and `PASS-5/CLOSE-CERT-2.md` §5 records it **CURED at W1 (20 sites), W3 (16) and W9 (18)**. It was never assigned to W4 and never swept here, so the wave that publishes the program's first zero-escape census publishes, four cells later, the census that had one escape. Nothing load-bearing moves — both readings are the same 111 denominator — but a file that states its carriage two ways cannot be the citation of record for either.

### D-3 · MAJOR — the NUMERIC ARM scope receipt was not re-run by the round that cured four of its counterexamples

**Claim** (§Bounds, the third scope receipt): *"**NUMERIC ARM (LAW D(3)) · scope receipt — every pasted command in this file was RE-RUN at this seat's write time**, not only the two a ruling named. … **Three figures did NOT reproduce and are corrected at their rows with both readings printed**: carry row 29's symbol census (**9 → 12**), `.dependency-cruiser.cjs`'s dead LIGHT paths (**4 → 5**), and ChannelOptions' Teleport."*

**Receipt.** That is round 4's receipt, unedited. Round 5's own cure cells, inside this same file, record **four further figures that did not reproduce at round 4** and correct them here: the `easing-serialize` census's **16 over a seventeen-member list** (*"the arithmetic, not the enumeration, was wrong, and it was wrong in the largest-radius act in this wave"*); row 29's **twelve summed by two incompatible rules to thirteen**; the `.dependency-cruiser.cjs` transcription's **silently dropped `.map()`** — *"a transcription that drops the matching rule cannot prove the matching rule"*; and the `seedFor|syncGap` transcript's **seventh line printed from a six-line command**. The receipt names none of the four, and still asserts, in the present tense, that the sweep reached every pasted command.

**Why it matters.** R5-11(1) binds a scope receipt to carry *its* enumeration and *its* counting rule **at the receipt**, and R5-11(2) makes receipt arithmetic the pass-6 checks' first act — which is how this was found. The numeric arm is the one sweep whose scope was declared and not executed at round 4 (PASS-5's own diagnosis), and round 5 cured all four of its products while leaving the receipt that certified their absence exactly as it was. **The scope law's sixth issue fires at the seventh receipt: a repair that cures every named instance and never touches the receipt that missed them leaves the law green over its own counterexamples.** The remedy is one sentence — the exception list restated at seven with the round-5 four named, or the receipt re-pointed at the round-5 cure cells as the one enumeration.

### D-4 · MAJOR — G-KFW4-11's oracle detects a narrower set than its falsifier asserts

**Claim** (falsifier): *"the set of `demo/ → @src/…` deep-import specifiers must equal the pinned **SEVEN-specifier / SIX-file** inventory … **it fails on an addition AND on a silent removal**"*, and *"satisfiable by DETECTION alone (mandatory)."*

**Receipt.** The oracle's key extraction is `grep -E ':[0-9]+:[[:space:]]*import '`. Re-run at `81a56990`, the pipeline is exact today — 9 raw `@src/` lines in `demo/`, 7 kept, and the 2 dropped are precisely the prose witnesses the gate declares (`demo/kf-engine.ts:5`, `useOrbitalInertia.ts:12`). But the filter keys on a line *beginning* with `import`, so three shapes of **addition** are invisible to it: a re-export (`export { x } from "@src/…"`), a multi-line import whose `from "@src/…"` clause sits on its own line, and a dynamic `await import("@src/…")`. Each is a live `demo/ → @src/` deep edge; none reds the gate.

**Why it matters.** This wave detects and KF.W8 decides (§Excluded 4), so detection completeness *is* this gate's entire contribution to the program. The hole is latent rather than live — the frontier carries none of the three shapes today — which is exactly why it will not announce itself: the gate goes green on a correct inventory now and stays green through an addition written in a shape its grep cannot see, in the wave whose goal criterion is that *"no gate in the tree computes its own oracle."*

### D-5 · MINOR — the largest-radius act's consumer figure counts two specs that assert nothing

**Claim** (§Bounds, `easing-serialize.ts`): *"**and asserted by FIVE test files** … **Consumer set = 17 call sites + 2 published re-exports + 5 asserting specs; none may change behaviour.**"*

**Receipt.** `git grep -n '\bserializeEasing\b' origin/master -- src/ demo/ test/ scripts/` → 49 lines, and the row's partition is **exhaustive and exact** (D-1 of pass 5 is fully cured). But of the five named specs, only three import and call the symbol (`roundtrip-easing.test.ts`, `value4-easing-contract.test.ts:42`, `w0-crashes.test.ts:39`/`:73`). `test/engine/replay-equality.test.ts:9` is prose — the row says so in the same breath. And `test/compile/compile-roundtrip.test.ts:570` is `describe("S.B3 EN-a — serializeEasing CSS-twin …")`, a **title string**, and it is that file's **sole hit**: the file neither imports nor calls `serializeEasing`. The row also books `:570` inside its *"12 in-test code lines"* bucket, so one line is counted once as an in-test code line and once as an asserting spec.

**Why it matters.** R5-11: *"A receipt whose own arithmetic does not reproduce is a defect REGARDLESS of the scope's truth."* The operative sentence is the authorization for R-2, the wave's largest blast radius, and it is the same two-rules-in-one-sentence shape D-7 convicted at row 29 — cured there, surviving eleven rows above it. The direction is safe (the figure overstates consumers, and *"none may change behaviour"* binds either way); the receipt still does not reproduce.

### D-6 · MINOR — §Excluded 21's three-limb enumeration hangs on a coordinate whose own words deny it

**Claim** (the round-5 clause): *"`KF-CO-33` banks **THREE** citation limbs, not one: **`kf-ChannelOptions.md:207`** books, beside the `proof:bezier-*` trio, '`design-idioms.css:3`'s `proof:idioms` authority claim … and `DESIGN.md:236`'s `proof:style-file-ceiling` …'"*

**Receipt.** `sed -n '207p' kf-ChannelOptions.md` → *"this record adds **only the two** citations load-bearing HERE to KF-CO-33: `design-idioms.css:3` … and `DESIGN.md:236` …"*. The `proof:bezier-*` trio is banked at the id's **own row**, `kf-ChannelOptions.md:88` (*"citing three `proof:bezier-*` gates that do not exist"*), verified this seat. The quoted half is byte-faithful; the *"THREE limbs at `:207`"* frame is the spec's, and `:207` says the opposite of it.

**Why it matters.** The substance is right at every end and this seat verifies it independently: three limbs exist, the bezier trio's sole site is `ChannelOptions.vue:168` (`git grep -n 'proof:bezier' origin/master -- demo/ src/ scripts/` → **1**), and both surviving limbs are inside the eight targets, inside §Bounds, and inside G-KFW4-8's denominator (`proof:idioms` ×3 and `proof:style-file-ceiling` at `DESIGN.md:236` all verified). **The routing correction is correct and the cure is right.** What fails is R5-11(1): the enumeration is stated at a receipt that does not carry it, with no command pasted for the three. One coordinate — `:88` beside `:207` — is the whole remedy.

### D-7 · MINOR — "routed" carries two incompatible definitions in one file

**Claim.** The AUDITED cell's supplier criterion is *"**supply ≥1 W4-routed id**"*, and on it kf-KeyboardShortcutsModal, kf-SequenceScene and kf-SequenceScrubber supply **none** — the cell says so at length, and quotes kf-SequenceScrubber's own routing law (*"**KF.W0/W6/W7/W9** … **never W4**"*) to prove it: *"being carried is not supplying."* Line 9 simultaneously publishes the census figure **111 routed**.

**Receipt.** Re-measured this seat: those three records supply **0** routed ids by the AUDITED cell's criterion and contribute **3** rows to the 111 by the census's (which counts them *"because the spec books them"*). By the cell's own criterion the routed total is **108**; by the census's it is **111**. Both figures ride this file, thirty lines apart, with no counting rule reconciling them.

**Why it matters.** R5-11's whole instrument is that a figure asserted over an enumeration states its counting rule **at** the enumeration. Nothing is lost either way — the three rows are booked and the escape count is 0 under both readings — but the file publishes its headline denominator without saying which of its two definitions of *routed* produced it, in the round that minted the rule.

### D-8 · MINOR — the NO-SILENT-DELETION falsifier asserts "four scripts" over a five-name enumeration, with no counting rule

**Claim** (G-KFW4-1's falsifier): *"fails if ANY of **the four scripts** this wave's `package.json` row opens — `check` (`:37`), `check:lib` (`:38`), `lint` (`:44`), `test`/`test:lib` (`:45`/`:46`) — lacks, at execution, ANY invocation present in its `origin/master` form."*

**Receipt.** Five names at five coordinates, and all five reproduce byte-exact this seat (`"check"`, `"check:lib"`, `"lint"`, `"test"`, `"test:lib"` at `:37`/`:38`/`:44`/`:45`/`:46`). Whether `test`/`test:lib` is one script or two decides whether the falsifier's scope is four or five, and the file uses both conventions — §Bounds says *"the same §Bounds row opens three more"* (implying four) and enumerates five line numbers.

**Why it matters.** The falsifier's own closing sentence is *"a falsifier's scope is the only thing that makes it executable: a law scoped to one script while its row opens four is a law that cannot fail on three of them."* The law is now scoped to a number its own enumeration does not fix. LAW D(3) asks for one clause: the rule stated where the count is.

### D-9 · MINOR — the one command this round authored writes to a path no §Bounds row grants and no §Artefacts location matches

**Claim.** Part (1), tagged *"**literal, no placeholder, R-8.4**"*: `npx depcruise --config .dependency-cruiser.cjs src demo --output-type json > **evidence/KF-W4/depcruise-inventory.json**`.

**Receipt.** §Artefacts locates the wave's evidence at `docs/tranches/X/keyframes/waves/evidence/KF-W4/` — a **value.js** path. The command runs in the keyframes.js clone at `origin/master`, where `evidence/` exists at no coordinate (`git ls-tree` returns nothing under it), and where §Bounds' *Do NOT touch* line forbids `docs/tranches/**` outright. No §Bounds row grants a write anywhere named `evidence/`.

**Why it matters.** R-8.4 exists because *"the config is never referenced by placeholder in a command"*; the same discipline binds a redirect target. An act needs its bounds row, and the round's one new command has neither a bounds row nor a resolvable destination — in the gate whose artefact identity is already the subject of D-1.

### D-10 · INFO — the §Excluded 21 SHADOW line declares an empty set beside a narrowing verb

LAW F(1) lists *"access NARROWED"* and *"re-pointed"* among the verbs that compel a SHADOW line; the clause performs a routing-scope narrowing and then states *"**SHADOW (LAW F(1)): struck/minted set = { }** — nothing is struck, minted, dropped or re-keyed by this correction; the ROUTING SCOPE of one row is narrowed."* The disposition-per-limb and notified-rows halves are present, correct and verified at both ends, so the line does its work; the set declaration contradicts the act's own verb, and line 9's round ledger handles the same act correctly under a third heading (*"routing-scope CORRECTED"*). Three vocabularies for one act.

### D-11 · INFO — the provenance block runs out of chronological order

Rounds appear 1 (`:7`) → 2 (`:8`) → **5** (`:9`) → **4** (`:10`) → 3 (`:11`). A reader taking the block in order meets round 5's cures before the round-4 text they supersede, and meets *"Repaired a fourth time"* after *"Repaired a fifth time."* No claim is wrong; the ordering is.

### D-12 · INFO — the SUBJECT-IDENTITY scope receipt speaks in round-4 voice inside a round-5 file

*"All three are path-qualified **this round** at every site that names them, gate-witness columns included (D-7)"* names round 4's act and round 4's D-7. The receipt was not re-dated or re-run at round 5, and it cites `PASS-4/KF-W4-CHECK.md §2` as its completeness authority (D-2's class). The sweep's substance re-verifies at the frontier — `easing.ts:44`, `TimelineCaret.vue:35` and `KeyframeTimeline.vue:179` all resolve with their subjects, and the `…/timeline/` vs `…/timeline/components/` near-miss is stated where it bites.

---

## §7 · VERDICT

**DEFECTIVE — and for the first time the defects are all in the file's *receipts about itself*, not in its carriage, its anchors or its measurements.**

**Three axes hold outright.** **Axis 1** holds for a second consecutive pass and holds harder: 111 routed · 111 booked · 0 escaped, on a denominator rebuilt by record from the 58 records' bytes, with a per-id loop over 107 tokens returning no zero, and with the AUDITED cell's roster verified by *enumeration* — 49 distinct real records whose complement is exactly the nine W4-silent non-carriers, 49 + 9 = 58 in both directions. **Axis 2 holds outright, and the drift class is dead at this wave**: zero live sibling coordinates, zero transcript-form receipts, zero per-seat stamps, and six of six stable anchors resolving uniquely at the siblings' current bytes. The → KF.W8 cure is the round's best work — it restates the sibling's gate topology correctly on both counts, and it does so while *correcting the check that convicted it*, recording the difference instead of absorbing it. **Axis 3 holds**: twenty-three pasted commands re-run, twenty-three reproduce, all four of pass 5's arithmetic defects cured at their enumerations with their counting rules stated — the first clean numeric sweep this wave has produced — and the `KF-ES-16` strike executed over the **stated scope**, verified by this seat against all twenty-six surviving chain members' banks. **Axis 5 holds at every obligation**, each checked at the far end. **The LAW E(2) hash gate passes: all eleven specs match CLOSE-CERT-2 §9 exactly. Round 5 closed lawfully and its repairs bank.**

**What fails is the file's account of its own reach.**

**Axis 4** convicts G-KFW4-11 for the **third consecutive pass**, at a fourth address. Round 4 cured its denominator; round 5 cured its two-oracle split — and in the same edit created a worse defect than the one it closed. The gate now names one artefact path as both the redirect target of a `depcruise` dump and the curated, hashed, pinned inventory its only oracle diffs against; under the reading §Artefacts states, the reach arm has **no falsifier at all** and a config still scoped to `src/` passes the gate silently, which is the exact blindness the gate was extended past `src/` to see; under the other reading, the gate re-derives its own oracle, which G-KFW4-9 rule (e) — this file's own law — forbids. And its detector, run verbatim and exact at the frontier, keys on a shape narrower than the set its falsifier asserts, so an addition written as a re-export, a multi-line import or a dynamic import leaves it green.

**And the receipts.** The wave that publishes the program's first zero-escape census also publishes, in the four cells that are its LAW B citation of record, the *previous* census with its one escape — twelve live `PASS-4` citations against two `PASS-5`, four printings of *"110 booked · 1 escaped"*, and a §Gates head still certifying that G-KFW4-11's defect *"is cured this round (R4-2)"* thirty lines before the same file re-cuts that gate again. R5-9 row 1 made this a named cure and CLOSE-CERT-2 records it discharged at W1, W3 and W9; W4 was never assigned it. The **numeric arm's** receipt is the same failure in the other direction: it still enumerates round 4's three non-reproducing figures while the round-5 cure cells, in the same file, correct four more — the receipt that certified the sweep reached everywhere is the one artifact the sweep did not reach, for the third consecutive round.

**Stated as the file itself would state it.** Round 5 executed every directive it was given, and executed two of them better than they were written. What it did not do is turn its own instruments on the round it was performing: the scope receipts still describe round 4, and the citation of record still points at the pass that has been superseded twice. *A repair that cures every named instance and never re-runs the receipt that missed them leaves the law green over its own counterexamples* — R4-10(1)'s sentence, turned once more on the file that keeps quoting it, and this time not at an id or an anchor but at the paragraph that certifies the sweep.

**111 routed · 111 booked · 0 escaped · 12 defects (0 CRITICAL · 4 MAJOR · 5 MINOR · 3 INFO).**

---

*PASS-6 KF-W4 check seat, 2026-08-29. Sole write = this file. Frontier of record: `origin/master 81a56990736ced5b5edde0b84c527680ac7689b1`; the sibling checkout's local `8281638c` and its dirty worktree are disqualified as witness substrate and were never read. LAW E(2)'s hash gate was run as this seat's first act and passed 11/11. No product byte was written, no spec was edited, and nothing here stamps any wave.*
