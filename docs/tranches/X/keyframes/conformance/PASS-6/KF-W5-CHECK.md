# KF-W5 — PASS-6 FRESH ADVERSARIAL SPEC CHECK (L-18/L-20)

**Subject**: `/Users/mkbabb/Programming/value.js/docs/tranches/X/keyframes/waves/KF-W5.md` — **530 L / 292,137 B, mtime 2026-08-29 15:17:38**, read WHOLE at this seat's clock (six paged reads, nothing skipped).
**Seat**: FRESH. Nothing inherited from PASS-1..PASS-5, from `PASS-5/RULINGS-5.md`, from `PASS-5/CLOSE-CERT-2.md`, or from any prior text at this path. Every census row below is re-harvested **by record** from the 58 adjudicated `kf-*.md` bytes; the sole carry consulted is `carry/KF-W6-CARRY.md`.
**Tree law**: keyframes witnesses verify at the frontier, `origin/master` **`81a56990736ced5b5edde0b84c527680ac7689b1`** — re-derived this seat (`git rev-parse origin/master`, exact). Local HEAD `8281638c` (**41 behind / 252 dirty**, both re-run) is historical context only, never a witness substrate. Every keyframes probe is a read-only `git show` / `git ls-tree` / `git grep` / `git cat-file`. **No product source was opened for writing.**
**FIRST ACT, per CLOSE-CERT-5 §8(4)**: `shasum -a 256 waves/KF-W*.md` re-run at this seat — **all eleven hashes reproduce the cert's closing column byte for byte** (`KF-W5.md` = `8baba077…404d`). **The round-5 write-set is intact; no spec moved after the certificate; write-last holds by construction.** The cert itself is `2026-08-29 15:24:27`, after the last spec edit (`KF-W7.md`, 15:18:37) — proof-of-order re-verified, and R5-2's void-ing of `PASS-4/CLOSE-CERT.md` is correctly succeeded.
**Sole write of this seat**: this file.

**VERDICT: DEFECTIVE.**
Axis 1 (by-record census) **CLEAN** · Axis 2 (receipt reality) **FAIL — two MAJOR** · Axis 3 (M-25 depth + frontier censuses) **CLEAN** · Axis 4 (gates / LAW-A / lawful landing surfaces) **FAIL — four MAJOR** · Axis 5 (posture) **PASS with one stale descriptor**.

Round 5 is a genuinely good round. **All thirteen of pass-5's register ids are addressed in the bytes**, and the four hardest are cured on their merits: the sibling **access word is gone** and the WRITE-seam derivation re-keyed to row anchors (D-1); the **fifteen receiving-end cells are re-keyed to CLOSE-CERT-5 rather than re-stamped**, and the cert discharged all fifteen (`KAD-1` spec-side measured **23** at this seat — the refusal to mint a successor numeral was right); **both short transcripts are printed whole and both reproduce exactly** at the frontier (D-3/D-10); the **G-CSSIDENT oracle re-cut promised at round 3 finally lands**; the bare `34` is struck and **nine negative witnesses are declared**, all nine re-measured `0/0/0` here. Every keyframes-side figure in the file re-executes exactly.

What fails is, again, the level above the enumerated set — and this round it fails inside the receipts that certify the round. **Two of the four receipts the ⟨RECONCILE SEAT⟩ block narrowed itself to, and then declared "all resolving", do not resolve at final bytes** — and round 5 wrote down the drift in prose one section away and left the coordinates. **The LAW F(2) notification probe minted this round returns two files, not one**, so a citing sibling went unnotified. **The LAW E(4) compliance receipt — "stated as a measurement" — is falsified by a one-line grep over this file's own clocks.** And **G-CSSIDENT's re-cut was declared, in writing, to reach the oracle and nothing else**, leaving the §Bounds row that *grants* the act and the §Sequencing lock that *orders* it both naming the subject set the cure removed.

---

## §1 · Axis 1 — ID-KEYED CENSUS, ENUMERATED BY RECORD

### 1.1 Method (partition-free, nothing inherited)

`for f in kf-*.md; do grep -c 'W5' "$f"; done` over all 58 records → **22 files carry the bare token**; `grep -l 'KF\.W5\|KF-W5'` → **19**. Every hit in all 22 was read in whole sentence and resolved:

- **16 records route at least one row** (§1.2).
- **3 carry only a `KF.W5-PARTIALS` taxonomy header** — kf-AnimationControlsGroup `:17` · kf-ControlsPaneWrapper `:6` · kf-DemoGlobalChrome `:17`. Each is the *"Status verbs per M-25 … Wave targets"* header naming the superseded `lane-frontend.md §10` sketch; **0 row hits**, reproduced by direct grep (§4.1, G-TAX born state).
- **3 are false positives on the bare token** — kf-SquareInstrument `:7`/`:21` (`R.W5`) · kf-OrbitalDrag `:82` (`P.W5.S3`) · kf-HeroAurora `:32` (routing-law recital; routes nothing).
- **kf-SquareScene and kf-ChannelOptions return `grep -c 'W5'` = 0** for the engine-seam / engine-lane rows the wave carries at C-1..C-3 and B-14/B-15/B-16 — homed by **§0 R-1**, not by token, and the file says so in its own AUDITED cell.
- **No alternate routing spelling exists.** The 36 zero-token records were swept for `tri-fold|trifold|D/L/C|library (letter|roster|arm)|engine seam|engine lane`; every hit is a routing-law recital, an adjudication footer, or kf-ChannelOptions' §0 R-1 arms already counted.

### 1.2 The routed ids, by record, and their booking

| record | routed ids (whole sentences resolved) | n | booked in `KF-W5.md` |
|---|---|--:|---|
| kf-AnimatedText | KF-AT-8 `:43` · KF-AT-9 `:44` · KF-AT-12 `:52` · KF-AT-23 `:63` | 4 | B-1 · B-2 · Arm A (PREMISE) · B-3 |
| kf-App.skeleton | KF-SKEL-9 `:44` · KF-SKEL-16 `:56` · KF-SKEL-20 `:60` | 3 | §Excl 2 · 3 · 4 |
| kf-CopyButton | KF-CB-30 `:31`/`:78` · KF-CB-35 `:83` · KF-CB-36 `:84` | 3 | B-19 · B-20 · B-21 |
| kf-EasingScene | KF-ES-36 `:78` | 1 | §Excl 1 |
| kf-EasingSidebar | KF-ES-20 `:61` | 1 | Arm A |
| kf-EasingTarget | KF-ET-1 `:37` · KF-ET-27 `:69` · KF-ET-32 `:77` · KF-ET-33 `:78` · KF-ET-35 `:80` | 5 | B-22 · Arm A · B-12 · §Excl 1 · §Excl 1 |
| kf-EditorStartScreen | KF-EST-5 `:62` · KF-EST-17 `:79` · P-8 `:136` | 3 | B-4 · B-17 · B-4 leg 2 |
| kf-KeyframeCard | KF-KC-27 `:69` | 1 | B-7 |
| kf-KeyframesAddDialog | KAD-1 `:42` · KAD-2 `:46` · KAD-3 `:47` · KAD-5 `:49` · KAD-13 `:57` · KAD-14 `:67` · KAD-17 `:70` | 7 | Arm 0 ×5 · §Excl 5 · §Excl 6 |
| kf-KeyframesEditor | KF-KE-10 `:52` | 1 | B-9 |
| kf-KeyframesStringControls | N-8 `:55` · C-8 `:81` | 2 | B-10 · B-11 |
| kf-SpringPhysicsFacet | SPF-20 `:60` | 1 | fold at B-9 |
| kf-SpringScene | KF-SS-4 `:43` · KF-SS-6 `:45` · KF-SS-31 `:77` · KF-SS-38 ⟨the `:151` KF.W5 roster line names all four⟩ | 4 | §Excl 1 (all four) |
| kf-TimelineHoverPreview | C-8 `:85` ⟨≡ KF-SKEL-16⟩ | 1 | §Excl 3 (folded) |
| kf-TimingFunctionPanel | KF-TFP-21 `:64` · KF-TFP-27 `:73` | 2 | B-13 (one row, two ids) |
| kf-TypingDots | KF-TD-1 `:57` · KF-TD-5 `:61` · KF-TD-8 `:64` · S★-2 `:88` | 4 | B-6 · B-8 · B-18 · B-5 |

**routedTotal = 43** (4+3+3+1+1+5+3+1+7+1+2+1+4+1+2+4).

**Booking verified by bytes, id by id** — `grep -o '<id>' waves/KF-W5.md | wc -l`, run for all 43: **every one returns ≥1**. Singletons: KF-CB-36 · KF-KC-27 · KF-TD-5 · KF-TD-8 · S★-2. Densest: KAD-1 **36** · C-8 **21** · KAD-14 **16** · KF-ES-20 **11** · KF-SS-31 **11** · P-8 **9** · KAD-3 **9**. **bookedCount = 43 · escapedCount = 0.**

**Whole-sentence resolution added nothing the spec dropped.** kf-TypingDots `:39`/`:46` are *witness ×2* restatements of KF-EST-5 / KF-EST-17 (carried at B-4/B-17, not re-booked); `:21`/`:73`/`:116` are the ruling and the routing recital for rows already counted; kf-EditorStartScreen `:24` is the L-EST-5 mechanism ruling; kf-CopyButton `:31` is the C-11/M-4 severity cell whose row is KF-CB-30 at `:78`; KAD-14's `(a)(b)(d)(e)` / `(c)` split is carried whole (a/b/d/e at Arm 0, c at §Excl 6).

### 1.3 The ledger arithmetic, re-derived mechanically

Rows counted by machine off the tables (`^| \*\*` per arm section): **Arm 0 = 7** · **Arm A = 8** · **Arm B = 22** · **Arm C = 3** · **Arm D = 6** → **46**. §Excluded = item 1's **7** prose sub-rows + items 2–6 → **12 rows / 13 ids** (item 3 carries KF-SKEL-16 *and* kf-TimelineHoverPreview C-8). **46 + 12 = 58** ✓. The 30/13 split reproduces: 43 − 13 = 30 across 27 §Carry rows (B-4 = KF-EST-5+P-8, B-9 = KF-KE-10+SPF-20, B-13 = KF-TFP-21+KF-TFP-27). §Gates holds exactly **20** gate rows (`grep -cE '^\| \*\*G-[A-Z-]+\*\*'` → 20).

**AXIS 1 — CLEAN.** No escape by bytes. No row inside a table the tally omits. No double-home. No id renamed or re-homed away from its bank. **Four fresh by-record enumerations at four passes have now returned the same three figures.**

---

## §2 · Axis 2 — RECEIPT REALITY AT FINAL BYTES

### 2.0 Substrate, re-derived this seat

`stat -f '%Sm %z %N'` — all eleven specs at **2026-08-29 15:17:38** except `KF-W10.md` **15:18:25** and `KF-W7.md` **15:18:37** (the two the reconcile seat edited); `CLOSE-CERT-2.md` **15:24:27**, i.e. **after** the last spec edit. `carry/KF-W6-CARRY.md` **2026-08-28 10:49:45 / 199,055 B**, `shasum -a 256` → **`87f28c36bed06c78ce11b2203e0f0e2895cc74b5abacbe02a6333f7caa72b8af`** — **exactly the hash the file prints**; the freeze is a hash, not a clock, and it holds.

### 2.1 What reproduces (the large majority)

- **The CARRY column of the fifteen-cell block: all thirteen re-measured here, all exact.** KF-SS-4 · KF-SS-6 · KF-SS-31 · KF-SS-38 · KF-ET-33 · KF-ET-35 · KF-ES-36 · KF-SKEL-9 · KF-SKEL-20 · KAD-17 = **ten zeros**; controls KAD-1 **12** · KAD-3 **2** · KF-AT-8 **2**. The **0/10** reciprocity obligation stands owed by KF.W6 on a measurement.
- **The re-key to ⟨CLOSE-CERT-5⟩ was the right instrument.** `grep -o 'KAD-1' waves/KF-W6.md | wc -l` → **23** at this seat (16 printed at round 4, 20 at pass 5). Had round 5 minted a numeral it would be stale a third time. **CLOSE-CERT-5 §7.1 discharges all fifteen** and its table reproduces cell-for-cell at this seat.
- **The six CARRY line-cites all resolve** at the frozen bytes: `:9` (the TAXONOMY RECONCILIATION head, the `KF.W5-PARTIALS ⇒ THIS WAVE` bullet at `:13` inside the cited `:9-13` range) · `:170` ×3 (the FIVE-CONSTRAINT MUST-CARRY set — **constraint 1 = *"KF-AT-8 — a NAMED BLOCKER-OF-THE-MIGRATION"***, constraint 4 = KF-AT-9, both read in full here) · `:248` (KAD-3's KF.W6 component-side disposition) · `:319` (the RD-4 bounds row, *"(KAD-1 is KF.W5's)"*). `grep -o 'KF-W6-CARRY\.md:[0-9]*'` → **6**, exactly the declared set; `:12` and `:356` resolve unchanged.
- **Every sibling reciprocation holds at final bytes.** `KF-W7.md:57` the fold-SOURCE row (*"its `innerHTML` BLOCKER is KF.W5's, front-loaded, NOT gated here"*) ✓ · W7's **four** creates `:63-66` ✓ · `KF-W4.md` row 3 **ME-32** `:166` and **G-KFW4-2** `:212` (`npx vitest run --project demo` **and** `npm run demo:correctness`, both blocking) ✓ · W4's two `test/demo/instrument/` creates `:83`/`:84` and its `resize-tracks.test.ts` cure-site row `:75` ✓ · `KF-W8.md:99` the nine-tracked-files row + `:100` `resize-tracks.test.ts` + `:133`/`:134`/`:135` the three create rows / four filenames keyed to G10·G11·G12 ✓ — **W4 2 + W7 4 + W8 4 = TEN**, ELEVEN four-party, both correct · `KF-W9.md` `G-KFW9-6` live (13 hits) with 17 KF.W5 edges ✓ · `KF-W10.md §6.D · SUCCESSOR-FORMATION REGISTER` present at `:573` with `KF-W8-R-4-STRUCT-PAIR ⟨origin KF.W5 D-6⟩` at `:100`/`:237`/`:637`, terminal verb **`DECLINED-FOR-X·KF, carried forward`**, three re-open triggers carried ✓.
- **The nine tracked files** in `test/demo/instrument/` re-derive exactly, `highlight-css-roundtrip.test.ts` absent, so the `create` verb is real ✓. **No created filename collides with a tracked one** — all thirteen checked ✓.

### 2.2 The failures

**D-1 · MAJOR — the ⟨RECONCILE SEAT⟩ block's round-5-narrowed claim — *"the four receipts enumerated above, re-verified at final bytes, all resolving"* — is false: TWO of the four do not resolve, and the same two coordinates are printed a second time as a live *"Command, run this seat"* at §0 R-1.4.**

Both sites print, for `waves/KF-W4.md`, `grep -n 'The ten rulings this spec owes'` → **`:98`** and the Census-S-2 sentence live at **`:123`**. Measured this seat at final bytes:

```
grep -n 'The ten rulings this spec owes' waves/KF-W4.md   → 99
grep -n 'Census S-2'                     waves/KF-W4.md   → 124   (one hit; the sentence is verbatim there)
```

Both are off by one. The *quotation* is byte-exact at `:124` and the **§-heading + ruling-id anchor is sound**, so the ruling this wave relies on is unharmed — but three things are not:

1. **§0 R-1.4 frames it live**: *"**Command, run this seat**: `grep -n 'Census S-2' waves/KF-W4.md` → `:123: …` … **Byte-diffed against that output at write time**"*. That is the exact LAW D(3) shape this round cured at Act 1 and Act 6 — a pasted command beside an output it does not produce.
2. **Round 5 recorded the drift and left it.** The strike note two clauses earlier reads: *"the sentence's coordinate had **already moved off the round-4 reading by one line** when this seat looked, inside round 5's own writing window."* One line is exactly the delta measured here. The seat saw it, wrote it down, struck the **mtime** — and did not touch the two numerals the drift was about.
3. **The reconcile block's own narrowed universal is refuted.** Round 5 struck *"NOT ONE ANCHOR IN THIS FILE FAILED"* and replaced it with *"**What this block may state is its four** — the four receipts enumerated above, re-verified at final bytes, all resolving."* Two of the four are these coordinates. **The successor claim is falsified at the same clock as its predecessor, one narrowing later.**

**And the declared certifying end did not catch it.** `CLOSE-CERT-5 §2` books `KF-W5.md` at **145 receipts checked · 1 fixed** (cert-path only), and `grep -n 'Census S-2\|:123\|:98' conformance/PASS-5/CLOSE-CERT-2.md` → **zero hits**. The cert cured this class at two siblings in the same document — drift-tail rows 7 and 8, both *"coordinate **retired**, not re-issued"* — and did not run it over this file.

**D-2 · MAJOR — the LAW F(2) notification probe minted this round returns TWO files; the citing sibling it misses is `KF-W2.md`, which cites this very gate cell and wrote 47 seconds before this file.**
G-CSSIDENT's D-4 SHADOW closes: *"**Citing/terminus waves notified**: KF.W8 …; **no other spec cites this cell — `grep -l 'G-CSSIDENT' waves/KF-W*.md` returns this file alone, run this seat**."* Measured:
```
grep -l 'G-CSSIDENT' waves/KF-W*.md
  waves/KF-W2.md
  waves/KF-W5.md
```
`KF-W2.md` names it **twice**, and not in passing — at `:276` and `:894` it books `KF-ET-32` as an **R1-boundary VERIFIED NEGATIVE** on the express ground that *"the KF.W5 leg is booked at **`KF-W5 §Carry Arm B` row `B-12`**, **which is `G-CSSIDENT`'s third leg**"*, entered *"in the census of record (§Bounds) at repair round 5"*. The probe was false **at write time**, not merely at final bytes: `CLOSE-CERT-5 §6.1` prints the round-5 per-wave order as **… W2 18:54:59 → W5 18:55:46 …**, so W2's citation predates this seat's grep by 47 seconds. **Consequence**: both the act-level SHADOW and the consolidated ROUND-5 SHADOW LEDGER list the notified set as *"KF.W8 · KF.W4 · KF.W6 · the RECONCILE seat"* — **KF.W2 is absent from both**, in the round that legislated LAW F(2)'s notify-every-terminus rule, and in the one act of the round whose registry shadow is non-empty.

**AXIS 2 — FAIL** (D-1, D-2). The CARRY column, the six declared cites, the cert delegation and every sibling reciprocation hold; two receipts minted or left standing this round do not.

---

## §3 · Axis 3 — M-25 DEPTH, MECHANISM S, AND THE FRONTIER CENSUSES

### 3.1 Every sampled command reproduces — 20 of 20 re-run at `81a56990`

- **Structure**: `git ls-tree -r 81a56990 --name-only -- src | grep -c '\.ts$'` → **153** (B0) · `.d.ts` → **0** ✓. God-module sweep at the ≥437 floor over all 153 → **exactly eight, the identical list and identical counts** (`progress.ts` 484 · `engine/animation.ts` 478 · `draggable.ts` 470 · `ingest/cssom.ts` 466 · `compile/frame/compiler.ts` 461 · `compile/emit/entry.ts` 459 · `classic-data.ts` 458 · `group/group.ts` 437), **no new entrant**, `engine/play-lifecycle.ts` MISSING ✓. Stutter predicate run **verbatim as printed** → **12**, the identical twelve paths in the identical order ✓. `git rev-list --count 8281638c..origin/master` → **41** · `git status --porcelain | wc -l` → **252** ✓.
- **Act 1 (D-3's cure, re-run whole)**: `git grep -nF 'orchestration/view-transition' origin/master -- src test` → **exactly the SIX lines printed, in the printed order**, three of them prose/docblock; `git show origin/master:src/animation/orchestration/index.ts | sed -n '52p;57p'` → `export { viewTransition } from "./view-transition";` / `} from "./view-transition";` ✓. **3 + 2 = 5 by resolution, with both commands printed** — the round-4 defect is genuinely closed.
- **Act 2 (the four dead allowlist names)**: raw `git grep -nF` returns 2 / 5 / 3 / 8, and **with the declared `docs/` exclusion the printed figures are exact** — `duration` **1** (`.cjs:58`) · `linear-stops` **1** (`:60`) · `timing-function` **1** (`:61`) · `reseat` **2** (`:59` + `bench/spring-tick.bench.ts:215`, recorded non-import) ✓. Zero live importers ✓.
- **Act 3**: `git grep -n '\./scheduler' origin/master -- src/` → **exactly `entries.ts:16` · `group.ts:170` · `group.ts:264`, all comments** ✓; the real `internal/scheduler.ts` and its four consumers verified ✓.
- **Act 6 (D-10's cure, both transcripts)**: `git grep -n 'return {' … useHighlightCSS.ts` → **3 lines** (`:20` · `:63` · `:147`) ✓; `git grep -n 'useCodeHighlight' origin/master -- demo/ test/` → **5 lines**, incl. the declaration `:78` ✓. **`highlight` destructured by neither consumer — the delete is census-true** ✓.
- **SUBJECT-IDENTITY**: all four declared zeros re-measured **0** ✓; the **nine further zeros** re-measured `0/0/0` across `index.ts`/`public.ts`/`load-engine.ts` for `cssIdent` · `reverseCSSTime` · `serializeTimingFunction` ✓; `setTargets(...targets: HTMLElement[])` → **exactly three declarations** (`engine/animation.ts:465` · `group/group.ts:195` · `sequence/sequence.ts:310`) ✓. The receipt's arithmetic re-derives from the cell alone: **4 + 9 + 1 = 14** ✓.
- **Gate born-states**: G-XSS **two** `innerHTML` lines, property form **1** ✓ RED-and-REACHABLE · G-PRM-FLIP **7 lines / 3 files** with `strategies.ts:76` and `frame.ts:137` exact ✓ · G-TAX **3 headers / 0 SUPERSEDED / 0 row hits** ✓ · G-SHIM presets = `catalog.ts`,`classic-data.ts`,`index.ts` and `split by kind` → **0** ✓ · G-DEPCRUISE: file **253 L**, `sed -n '55,78p' | grep -c '"'` → **24**, `knownViolations` → **0**, `known-violations` → **3**, the baseline file **ABSENT**, `lint` = bare `depcruise src` (`package.json:44`) ✓ **RED, unmoved by 41 commits**.
- **OP-6**: `package.json:37/:38/:44/:50` all exact, `:77` glass-ui `7.0.0` ✓; `vitest.config.ts:26/:38/:52` exact ✓. All six "specs it extends" PRESENT ✓.

**Not one keyframes-side figure in this file failed at this seat.**

### 3.2 M-25 depth and mechanism S (the shadows on moved rows)

- **Shadows are carried at the act, not only in the ledger.** Fourteen `SHADOW (LAW F)` act lines, one consolidated ledger; the four self-count probes the file prints **all reproduce exactly at this seat**: `SHADOW (LAW F)` → **15** · `struck set = ` → **18** · the ∅ form → **13** · the `{C-8` form → **2**, with **14 acts − 1 non-empty = 13 empty**, exactly as declared. `grep -o 'repair round 5' | wc -l` → **22**, and the file correctly says that probe *locates* rather than counts. `grep -c '^| \*\*D-' PASS-5/KF-W5-CHECK.md` → **13**, matching the declared directive register.
- **The one non-empty shadow is booked at its own banked anchor** (LAW F(3)): `kf-KeyframesStringControls.md:80` is C-8's row heading and `:81` its verification body naming all four names — **both re-read here, both exact** — and **C-8 keeps all four names verbatim at §Carry B-11**, unchanged, with the two demo-local names still routed to KF.W8. Nothing was dropped, re-booked or re-homed by the strike.
- **Preserved dissents** all intact: §0 R-3 (KF-KE-10 MAJOR vs SPF-20 BLOCKER-at-bank; bank governs, dissent preserved, stated as why B-9 heads the ruling queue) · B-19 (*"Reader-2's MINOR as dissent"*, verbatim at kf-CopyButton `:31`) · B-14 (RR-A whole-at-BLOCKER / RR-B WAAPI-half → RESCOPED-BY-SUBSTRATE) · B-4 (kf-TypingDots ruling 3 **overruling** reader-2's demotion) · B-6 (C's S★-4 second bullet STAYS KILLED; L's S-3 SURVIVES SCOPED).
- **Cure-locks carried, not paraphrased**: B-22's KF-ET-2 lock (never *"bind `cssValue`"*, never a silent approximation) with the row itself NOT taken · §Excl 1 KF-SS-38's *"must not resurrect the killed claim"* · §Excl 6's dead BLOCKER arm · §Excl 2's C·D-4 premise · KAD-14(a)'s same-commit trap · D-6's R-14 addendum carried **verbatim** with its correcting sentence beside it (E-3, never a patch).
- **Fold identities never re-booked**: SPF-20 ≡ B-9 · THP C-8 ≡ KF-SKEL-16 · KF-CB-15 ≡ m-4 ≡ C-7 folded at C-3 with its bytes named · KF-CB-6 at B-7 · KF-CE-42 at B-11 · KF-ET-34 at §Excl 1 · KAD-14(d)'s T `lane-18` #6 identity.
- **Transcription-only**: absent. Every arm carries a mechanism, a lock or a ruling.

**AXIS 3 — CLEAN.**

---

## §4 · Axis 4 — GATES, LAW-A SCOPE, AND LAWFUL LANDING SURFACES

### 4.1 What the round genuinely closed

**G-CSSIDENT's oracle is fixed.** The GREEN now reads *"`cssIdent` (and **C-8's TWO LIBRARY names** — `reverseCSSTime` · `serializeTimingFunction` — or their declared relocations, and KF-ET-32's registry) resolve from `loadAnimationEngine()` or the exports map"*, and the two demo-local names are struck from the RED witness with `git grep -c 'debounce' 81a56990 -- src` → **0** re-verified here. **GREEN is now reachable by a lawful seat** — pass-5's D-4 is closed at the cell it names. **The SUBJECT-IDENTITY receipt is re-cut correctly**: the bare denominator is struck and not re-issued, the enumeration source is a pointer to the §Bounds rows, the counting rule (k×m) is stated at the receipt, and the fourteen findings are printed. **The LAW-A access-column receipt now measures rather than asserts**: `awk` clause (i) → **`modify-carve` 14 · `modify` 9 · `read` 5 · `create` 2 = 30**, exact at this seat; clause (ii) → **exactly ONE line, Act 5's own row, its verb marked STRUCK** ✓. Every born-state verb in the ledger re-checks.

### 4.2 The failures

**D-3 · MAJOR — §END's LAW E(4) compliance receipt, *"stated as a measurement"*, is a probe-free negative universal falsified by a one-line grep over this file's own bytes.**
The receipt reads: *"**LAW E(4) COMPLIANCE, stated as a measurement**: … no per-seat substrate mtime is minted; **the three that existed are struck (D-11), and the only clocks this file now prints are the frozen CARRY's and its own.**"* Measured — `grep -o '[0-9][0-9]:[0-9][0-9]\(:[0-9][0-9]\)\?' waves/KF-W5.md`:

| clock | subject | where | struck? | true at final bytes? |
|---|---|---|:--:|:--:|
| **16:59:44** | `KF-W4.md` | ⟨RECONCILE SEAT⟩ | **no** | **no** (15:17:38) |
| **16:59:25** | `KF-W6.md` | ⟨RECONCILE SEAT⟩ | **no** | **no** |
| **16:58:59** | `KF-W0.md` | ⟨RECONCILE SEAT⟩ | **no** | **no** |
| **17:17:02** | `KF-W7.md` | ⟨RECONCILE SEAT⟩ | **no** | **no** (15:18:37) |
| 15:32 · 14:06 · 14:22 | `KF-W6.md` ×3 | §Excluded round-3 note | no | no |
| 15:49 · 15:50 · 15:39 | W4 · W6 · W7 | §0 R-1.4 / §Excluded | **yes** | — |
| 10:49:45 ×3 · 10:49 ×2 | CARRY | passim | — | **yes** ✓ |

**Seven live sibling clocks, four of them in the reconcile block and none of them struck.** *"The three that existed"* is a count of the round-4 stamps only; the class is at least seven. D-11 cured the named instances and not the stated scope — **R4-10's own shape, sixth generation** — and it is minted in a sentence that says *"stated as a measurement"* while printing no probe, one paragraph after the round struck a self-voiced closure claim (D-7) for being exactly this.

**D-4 · MAJOR — the G-CSSIDENT re-cut was declared, in writing, to reach the gate's oracle and nothing else; the §Bounds row that GRANTS the act and the §Sequencing lock that ORDERS it still name the subject set the cure removed.**
The D-4 SHADOW says so itself: *"what is struck is their appearance **in this gate's oracle**, nothing else."* Two live sites carry the superseded set:

- **§Bounds, the grant row**: `` `src/animation/public.ts` · `index.ts` · `load-engine.ts` | modify | the **publication decision** surface (`cssIdent` + **C-8's four** + KF-ET-32's registry) ``. This is the row a seat reads to know what it may write. *"C-8's four"* includes `debounce` and `convertPixelsToCh`, which **Act 4 and B-11 rule are not library symbols and which this wave may not publish** — *"not something this wave may publish out of a library that does not own them"*. A row granting a publication decision over symbols with **zero declarations under `src/`** (re-verified `0`/`0` here) is the pass-5 D-4 shape moved from the oracle to the bounds.
- **§Sequencing S-3**: *"The publication decision (G-CSSIDENT) precedes APPLY-UNIT entirely and is a **single ruling over five names**"* — flatly contradicted by B-11 in the same file (*"**ONE publish-or-relocate ruling over THREE library names**, with the two demo-local helpers separated out"*). The → KF.W8 edge repeats it: *"**B-11's five names** split publish (here) from relocate (KF.W8…)"*.

The SUBJECT-IDENTITY block **quotes the §Bounds row verbatim** while deriving the nine zeros, so the round-5 seat had the row open and corrected the sweep around it rather than the row itself.

**D-5 · MAJOR — the §Gates observability partition (*"nineteen of the twenty run under the instruments that exist today … the twentieth is G-XSS"*) is falsified by G-TAX's own round-4 cell, and its *"nineteen `src/`-side gates"* description is wrong for at least three of them.**
§Gates: *"**Nineteen of the twenty run under the instruments that exist today** (`check`, `check:lib`, `vitest`, `depcruise`) … and **none of those nineteen waits on KF.W4** … **The twentieth is G-XSS, and its exception is DECLARED, not glossed**."* OP-6 states it the same way: *"the **nineteen `src/`-side gates** run under today's `check`/`check:lib`/`vitest`/`depcruise`"*. But **G-TAX's own cell**, re-cut at round 4, reads: *"this gate's second leg is therefore **authored RED and stays unobservable until the SS-1/SS-2 authoring block acts**, exactly as G-XSS's round-trip half waits on KF.W4's wiring."* **There are two dependency-blocked gates, not one**, and the partition that counts them 19/1 was never updated when the second was created. Separately, **G-TAX greps `docs/tranches/V/megatranche/registry/adjudicated/`**, **G-ID greps this spec**, and **G-SCOPE is an enumeration of this file** — none is `src/`-side, so *"the nineteen `src/`-side gates"* is wrong by at least three. The claim is a self-voiced partition over an enumerable set the file itself enumerates twenty rows of.

**D-6 · MAJOR — the census-carriage and gate-status claims are homed at superseded conformance artifacts, the exact class RULINGS-5/R5-9 retired program-wide and CLOSE-CERT-5 books CURED at three siblings.**
Measured over this file: `PASS-3/KF-W5-CHECK` **13 sites** · `PASS-4/KF-W5-CHECK` **4** · `PASS-2/KF-W5-CHECK` **1** · `PASS-5/KF-W5-CHECK` **5**. The load-bearing ones are the stale ones — the **SPECIFIED verb's census carriage** (live citation `PASS-4 §1`), **G-SCOPE**, **G-ID's status**, **§Gates' witness re-verification**, **§Excluded's closure conversion**, and **"Close of the ledger"** (all `PASS-3`) — while `PASS-5/KF-W5-CHECK.md` exists, is fresher, and graded the file **DEFECTIVE**. The SPECIFIED cell carries its own rule — *"When a newer pass supersedes this one the citation is re-pointed again"* — and round 5 did not run it. **CLOSE-CERT-5 §5 books this class CURED at KF-W1 (row 1, *"the artifact of record at 20 sites"*), KF-W3 (row 6) and KF-W9 (row 16)**; `KF-W1.md` now cites PASS-5 at **20** sites against this file's **5**. A reader following this file's own citations reaches the pass that certified the census and not the pass that convicted the file.

**D-7 · MINOR — the count-oracle sweep's closing tally mis-partitions the twenty it prints.** *"Tally: 20 gates swept · 1 INVERTED · **8 count-or-set oracles verified sound** · **11 non-count oracles**"* sums to 20, but the block's own prose calls at least **five** of the eight non-count: G-CSSIDENT (*"a **RED witness, not an oracle**"*), G-ID (*"a property over every hit … **not a count**"*), and G-STRUCT/G-BASIS/G-RING (*"carry figures **with their probes**; **none is a drive-to-zero**"*). The partition is arithmetically consistent with 9 − 1 and semantically inconsistent with the enumeration it summarizes — un-ruled arithmetic of the kind R5-11 exists to forbid, inside the sweep R4-D-8 widened to twenty. The **sweep itself is complete** and its verdicts are sound; only the summary's categories are wrong.

**D-8 · MINOR — §Spec-home's printed probe does not return what it prints.** *"the spelling appears in no in-tree authority (`grep -rl 'X/kf/waves/KF.W5'` → **this file only**)"*. Measured over `docs/`: **FOUR files** — `PASS-1/KF-W5-CHECK.md`, `PASS-2/KF-W5-CHECK.md`, `PASS-1/RULINGS.md`, and this file. **The conclusion survives** (the other three are conformance artifacts quoting the spelling in order to strike it, not authorities), but the numeral beside the paste is not the paste's output — the D-12 class this same round corrected at `withReducedMotion`.

**D-9 · MINOR — the LAW-A receipt's contrast figure names no command.** *"a **naïve line-grep** over the same range returns **31**, one more than the row census"*. It **does** reproduce under `grep -cE 'modify'` → **31** (and, coincidentally, `grep -cE 'read'` → 31), but **not** under the natural four-verb form `grep -cE 'modify|modify-carve|create|read'` → **55**, nor the delete-verb form → **17**, nor any `awk` variant tried (36 · 37 · 68 · 3). A figure whose probe is described rather than printed, in the one receipt whose whole subject is that a measurement carries its command; and *"one more than the row census"* reads as derived arithmetic (30+1) rather than a reading. **Not booked as false** — it reproduces under one reading — booked as under-specified.

**D-10 · MINOR — the successor-formation descriptor is one round stale.** §Sequencing still reads *"cube + square at **the proposed** KF.W11; dock-menu at **the proposed** KF.W13"*, while `KF-W10.md §6.D · SUCCESSOR-FORMATION REGISTER` (verified present at `:573`) and **COHESION `§0d`** (landed at round 5 per R5-12, cert §6.2) record all three as **`MINTED-UNAUTHORED`** with a ruled **9 + 6 + 2 = 17** cargo. Nothing routes wrongly — this wave *"neither mints nor owns either packet"*, correctly — but the word contradicts the register this file's own → KF.W10 edge depends on.

### 4.3 Born-state ledger, re-checked

| declared | re-checked at `81a56990` this seat |
|---|---|
| G-XSS born-RED, GREEN reachable | ✓ **RED and REACHABLE** — 2 writes; property form 1 → 0 after the cure |
| G-TAX born-RED, INBOUND-DEPENDENCY | ✓ **RED (3/0/0)**, relay leg in-grant — but its unobservable second leg breaks the §Gates 19/1 partition, **D-5** |
| G-PRM-FLIP born-RED | ✓ RED; `strategies.ts:66-75`/`:76` and `frame.ts:121`/`:137` exact; 7 lines / 3 files |
| G-DEPCRUISE L-1 RED at the ref of record | ✓ four dead paths, unmoved by 41 commits; the three-clause oracle sound |
| G-DEPCRUISE L-2 CURED-AT-FRONTIER | ✓ denial `:125-129`; 0 / bare `depcruise src` / file absent |
| G-SHIM declared regression floor, cure `7e9ddf49` | ✓ honest; three files gone, docblock true |
| G-STRUCT / G-RING / G-BASIS MEASURE-AT-OPEN | ✓ 153 · 8 · 12 all reproduce; rings honestly unmeasured |
| G-CSSIDENT | ✓ oracle re-cut, GREEN reachable — but the §Bounds grant and S-3 still name the struck set, **D-4** |
| the other twelve arm-B/C gates | ✓ every witness re-resolves at the frontier |

**AXIS 4 — FAIL** (D-3/D-4/D-5/D-6 MAJOR; D-7..D-10 MINOR). **No BLOCKER**: no surviving oracle can be driven GREEN by deleting something true, and the two pass-5 gate MAJORs (G-CSSIDENT's unsatisfiable GREEN, the short SUBJECT-IDENTITY sweep) are genuinely closed.

---

## §5 · Axis 5 — POSTURE

| item | finding |
|---|---|
| **W4 head** | ✓ `→ KF.W4` declared **BLOCKING for the re-homed rows, not for this wave's bounds**; KF-AT-12 stated as PREMISE; all **three** riders named and reciprocated at final bytes (`KF-W4.md:166` row 3 ME-32 ≡ LP-7 rider; `:212` **G-KFW4-2**, both blocking merge steps). OP-6's frontier restatement is exact at `package.json:37/:38/:44/:50` |
| **W3 gated** | ✓ `→ KF.W2/KF.W3 · Parser Consumption (GATED)` — *"must NOT pre-empt the 4.0.0→4.1.x repin"*; `compile/emit/css-text.ts` reserved; B-11's addendum correctly derives publication as a re-export in `public.ts`/`index.ts` only. B-16's OP-4 both-dispositions pre-declaration keeps the row alive either way |
| **KF-AV-28 · O-20 · O-21** | ✓ all three correctly **absent** (`grep -c` → 0/0/0). O-21 is live at `KF-W10.md` (**7** hits) where it belongs; KF-AV-28 lives in nine specs and is *"absent only from `KF-W5.md`"* by the cert's own §4·R5-R1 finding — correct, its surfaces are not this wave's |
| **§6.D successor register** | ✓ **present and coherent at the receiving end** — `KF-W10.md:573`, three rows, ruled cargo **KF.W11 ×9 · KF.W12 ×6 · KF.W13 ×2 = 17**, terminal verb `MINTED-UNAUTHORED`. The one W5-origin obligation is verified fulfilled from that end: **KF-W8-R-4-STRUCT-PAIR ⟨origin KF.W5 D-6⟩**, `DECLINED-FOR-X·KF, carried forward`, RECORD-ONLY, three re-open triggers verbatim. **This file's descriptor is stale — D-10** |
| **COHESION §0d** | ✓ landed and discharged at the upward end (`KF-W10.md` §0-vehicle; cert §6.2 resolves `COHESION.md` at `:185`). **This file cites COHESION nowhere** (`grep -c` → 0) — correct for a wave with no COHESION-owed line; the minted-wave boundary is W10's declaration, not W5's |
| **EH-4/5/8 restoration** | ✓ the R5-1 BLOCKER cure that *restores* three rows is `KF-W6.md`'s (EH-4 ×18 · EH-5 ×20 · EH-8 ×17 · R5-1 ×21, all live with true anchors). **This file's only stake is the KAD-1 control cell**, which correctly refuses a successor numeral *because* that cure restores rows — and the cert's §7.1 measurement (**23**) proves the refusal right |
| **DH-3 four-party carriage** | ✓ **fully repaired**: the access word is gone, the KF.W8 bullet carries **row identity only**, the WRITE-seam sentence is re-keyed to two row anchors with its extent handed to CLOSE-CERT-5 (§7.4 discharges it). Counts TEN / ELEVEN re-derived at all three siblings' final bytes ✓ |

**AXIS 5 — PASS**, with D-10 recorded.

---

## §6 · Register (worst first)

| # | sev | claim | receipt |
|---|---|---|---|
| **D-1** | MAJOR | the ⟨RECONCILE SEAT⟩ block's round-5-narrowed claim *"the four receipts … re-verified at final bytes, **all resolving**"* is false — **two of the four are off by one** — and the same coordinates are printed again at §0 R-1.4 as a live *"Command, run this seat"* | printed `:98` / `:123`; measured `grep -n 'The ten rulings this spec owes' waves/KF-W4.md` → **99**, `grep -n 'Census S-2' waves/KF-W4.md` → **124** (one hit, quotation byte-exact). Round 5 recorded the drift in prose — *"had already moved off the round-4 reading by one line when this seat looked"* — struck the mtime and left the numerals. `CLOSE-CERT-5 §2` books W5 at **145 checked · 1 fixed**; `grep -n ':123\|:98\|Census S-2'` over the cert → **0 hits**, while cert §5 rows 7 and 8 retire the identical class at two siblings |
| **D-2** | MAJOR | the LAW F(2) notification probe **minted this round** returns **two** files, not one; the unnotified citer is `KF-W2.md`, which names this exact gate cell | printed: *"no other spec cites this cell — `grep -l 'G-CSSIDENT' waves/KF-W*.md` returns **this file alone**, run this seat."* Measured → **`waves/KF-W2.md` + `waves/KF-W5.md`**. W2 `:276`/`:894`: *"the KF.W5 leg is booked at **`KF-W5 §Carry Arm B` row `B-12`**, which is **`G-CSSIDENT`'s third leg**"*, entered *"at repair round 5"*. Cert §6.1's order: **W2 18:54:59 → W5 18:55:46** — false at write time. **KF.W2 absent from both the act SHADOW and the consolidated LEDGER** |
| **D-3** | MAJOR | §END's *"LAW E(4) COMPLIANCE, **stated as a measurement**"* asserts *"the only clocks this file now prints are the frozen CARRY's and its own"* — a probe-free negative universal, falsified by one grep over the file | `grep -o '[0-9][0-9]:[0-9][0-9]\(:[0-9][0-9]\)\?' waves/KF-W5.md` → **seven live sibling clocks**, four of them unstruck in the ⟨RECONCILE SEAT⟩ block (`KF-W4` **16:59:44** · `KF-W6` **16:59:25** · `KF-W0` **16:58:59** · `KF-W7` **17:17:02**), three more for `KF-W6` at §Excluded (14:06 · 14:22 · 15:32). All false at final bytes (all eleven specs 15:17:38+). *"The three that existed"* counts the round-4 stamps only — instance cured, class left, one paragraph after D-7 struck the same shape |
| **D-4** | MAJOR | the G-CSSIDENT re-cut is **declared** to reach the oracle *"and nothing else"*, leaving the §Bounds row that GRANTS the act and S-3 that ORDERS it naming the struck subject set | §Bounds: *"`public.ts` · `index.ts` · `load-engine.ts` \| modify \| the **publication decision** surface (`cssIdent` + **C-8's four** + KF-ET-32's registry)"* — includes `debounce`/`convertPixelsToCh`, `git grep -c … 81a56990 -- src` → **0** both, which Act 4/B-11 forbid publishing. §Sequencing **S-3**: *"a **single ruling over five names**"* vs B-11's *"ONE publish-or-relocate ruling over **THREE library names**"*; the → KF.W8 edge repeats *"B-11's five names"*. The SUBJECT-IDENTITY block quotes that same §Bounds row verbatim while curing D-5 |
| **D-5** | MAJOR | the §Gates / OP-6 observability partition — *"**nineteen** of the twenty run under the instruments that exist today … **the twentieth is G-XSS**"* — is falsified by **G-TAX's own cell**, and *"the nineteen `src/`-side gates"* is wrong for at least three | G-TAX (re-cut round 4): *"this gate's second leg is therefore **authored RED and stays unobservable until the SS-1/SS-2 authoring block acts**, exactly as G-XSS's round-trip half waits on KF.W4's wiring."* Two dependency-blocked gates, counted as one. G-TAX greps `registry/adjudicated/`, G-ID greps this spec, G-SCOPE enumerates this file — none `src/`-side |
| **D-6** | MAJOR | the census-carriage and gate-status claims are homed at **superseded** conformance artifacts — the class R5-9 retired program-wide and CLOSE-CERT-5 books **CURED at three siblings** | measured: `PASS-3/KF-W5-CHECK` **13** · `PASS-4` **4** · `PASS-2` **1** · `PASS-5` **5**. Stale at the load-bearing sites: the SPECIFIED verb's census carriage (PASS-4 §1, under its own rule *"when a newer pass supersedes this one the citation is re-pointed again"*), G-SCOPE, G-ID, §Gates' witness re-verification, §Excluded's closure, "Close of the ledger" (all PASS-3) — while `PASS-5/KF-W5-CHECK.md` exists and graded the file DEFECTIVE. Cert §5 rows 1/6/16: CURED at KF-W1 (**20 sites**), KF-W3, KF-W9 |
| **D-7** | MINOR | the count-oracle sweep's tally mis-partitions the twenty verdicts it prints | *"1 INVERTED · **8 count-or-set oracles** · **11 non-count**"* = 20 arithmetically, but the block itself calls **five** of the eight non-count: G-CSSIDENT *"a RED witness, not an oracle"* · G-ID *"a property … not a count"* · G-STRUCT/G-BASIS/G-RING *"none is a drive-to-zero"*. Sweep complete and sound; the summary's categories are not |
| **D-8** | MINOR | §Spec-home's printed probe does not return what it prints | *"`grep -rl 'X/kf/waves/KF.W5'` → **this file only**"*; measured over `docs/` → **4 files** (`PASS-1/KF-W5-CHECK.md` · `PASS-2/KF-W5-CHECK.md` · `PASS-1/RULINGS.md` · this file). Conclusion survives — the three are conformance artifacts striking the spelling — the numeral beside the paste does not |
| **D-9** | MINOR | the LAW-A receipt's contrast figure **31** names no command | reproduces under `grep -cE 'modify'` → **31** (and coincidentally `grep -cE 'read'` → 31), **not** under `grep -cE 'modify\|modify-carve\|create\|read'` → **55**, the delete-verb form → **17**, or four `awk` variants (36 · 37 · 68 · 3). *"One more than the row census"* reads as 30+1, not as a reading. Booked under-specified, not false |
| **D-10** | MINOR | the successor-formation descriptor is one round stale | §Sequencing: *"cube + square at **the proposed** KF.W11; dock-menu at **the proposed** KF.W13"*, against `KF-W10.md:573` §6.D's `MINTED-UNAUTHORED` verb and COHESION `§0d` (both verified). Routing unchanged and correct; the word is not |

**routedTotal 43 · bookedCount 43 · escapedCount 0 · verdictLocal DEFECTIVE.**

---

## §7 · What this pass does not dispute

**The by-record census is whole and honest in both directions for the fourth pass running**, and this pass re-derived it from the 58 records with the token partition swept for alternate spellings — it did not move by one id. **Every keyframes-side measurement in this file re-executes exactly at the frontier**: B0 153 · `.d.ts` 0 · god modules 8 with the identical list · stutters 12 with the identical list · 41 behind / 252 dirty · depcruise 253 L / 24 entries / 0 and 3 / the baseline absent · the two-site `cssIdent` chain · the `innerHTML` pair · PRM 7-lines-3-files · the three `setTargets` declarations · Act 1's six lines and its second resolving command · Act 2's four names under their declared exclusion · Act 3's three comments and the four-consumer sibling trap · Act 6's two whole transcripts · G-TAX 3/0/0 · the nine tracked files · zero create-name collisions.

**Pass-5's four MAJORs are genuinely closed.** The sibling access word is gone and its derivation re-keyed to row anchors; the fifteen cells were re-keyed rather than re-stamped and the cert discharged all fifteen — **and the refusal to mint a successor numeral was vindicated by measurement** (`KAD-1` spec-side is **23** now, a third value in three clocks); both short transcripts print whole; the promised G-CSSIDENT oracle re-cut landed; the bare `34` is struck without a replacement numeral and nine negative witnesses are declared, all nine re-measured here. **The LAW F apparatus works**: fourteen act-level shadows, one ledger, four self-count probes that all reproduce, and the one non-empty shadow booked at its own banked anchor with C-8's four names intact at B-11. **The hash discipline works**: eleven of eleven reproduce, and the cert is the last write.

**The pattern, stated once.** Five rounds have each cured an enumerated set and left the class one level up — pass 2 the arm-D substrate, pass 3 the arm-0 oracle, pass 4 the anchor-content and false-universal classes, pass 5 the certification, and **pass 6 the receipts that certify the certification**. Round 5 struck a self-voiced closure sentence (D-7), struck a whole-file universal from the reconcile block (D-6), and then **minted three more in their place**: a narrowed *"all four resolving"* that two coordinates falsify, a *"returns this file alone"* that a sibling falsifies, and a *"the only clocks this file now prints"* that the file falsifies. Each is smaller than the one it replaced. **The shape that survives every narrowing is the shape that has no probe beside it** — and the one place in this file where a probe *is* beside the claim (the four LAW-F self-counts, the two `awk` clauses, the fourteen printed findings) is the one place nothing failed.
