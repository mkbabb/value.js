# KF-W1 — FRESH ADVERSARIAL SPEC CHECK, PASS 6 (L-18/L-20)

**Target**: `docs/tranches/X/keyframes/waves/KF-W1.md` — **626 L / 250,457 B**, mtime **2026-08-29 15:17:38**, sha256 `f6986de7…12f0`.
**Seat**: FRESH. Every list, count, anchor and verdict below is this seat's own command output at the current bytes. `PASS-5/KF-W1-CHECK.md`, `PASS-5/RULINGS-5.md` and `PASS-5/CLOSE-CERT-2.md` were read **only** to know what was ruled, what was claimed cured, and what LAW E / LAW F commission — never as a source of a measurement. The census below is derived **BY RECORD** from the 58 `kf-*.md` files themselves (the nine-block partition stays retired, RULINGS-3 R3-5).
**Corpus**: `docs/tranches/V/megatranche/registry/adjudicated/`, `ls kf-*.md | wc -l` → **58**; sole carry `docs/tranches/X/keyframes/carry/KF-W6-CARRY.md` (`find docs/tranches/X/keyframes -iname '*CARRY*'` → the dir plus exactly one file, mtime **2026-08-28 10:49:45 / 199,055 B**, unmoved for a seventh round).
**TREE LAW**: keyframes witnesses verified at **`origin/master` = `81a56990736ced5b5edde0b84c527680ac7689b1`** in `/Users/mkbabb/Programming/keyframes.js` (read-only; zero writes, zero git mutations, zero npm). `keyframes-v-exec` HEAD re-verified `81a56990`, porcelain **0**. Local `8281638c` opened **only** as declared historical context, per R3-2.
**First act, per RULINGS-5 LAW E(2)**: all eleven specs re-hashed — see **§0**.

**VERDICT: DEFECTIVE — 0 BLOCKER · 5 MAJOR · 3 MINOR · 2 INFO.**
**Census: CLEAN — 6 routed · 6 booked · 0 escaped · 0 inventions.**

**The severity head stays MONOTONE-flat for a third round (pass 4 = 0·5·3·2 → pass 5 = 0·5·3·2 → pass 6 = 0·5·3·2), and all ten PASS-5 local defects are verified CURED at the bytes.** What pass 6 finds is that **LAW E's hash arm WORKED — 11 of 11 reproduce, the write-SET is provable — and LAW E's order arm did not**: the certification that proves what was written cannot derive what it certifies about the order in which it was written, it disagrees with itself on its own headline census, and the per-seat stamping LAW E(4) retired is still standing in ten rows at `KF-W10.md`. Locally, the file's own D5-1 cure declared the per-sibling numerals **retired** and then **restated** them, and one of the restated numerals is false at final bytes with no correction beside it.

---

## §0 · FIRST ACT — THE LAW E(2) RE-HASH OF ALL ELEVEN SPECS

RULINGS-5 LAW E(2): *"The pass-6 union seat re-hashes all 11 as its FIRST act; any mismatch is the round's own conviction — the moved file is named, the round did not close, no repair banks."* CLOSE-CERT-2 §8(4) repeats it.

`shasum -a 256 docs/tranches/X/keyframes/waves/KF-W*.md`, run this seat:

| spec | CLOSE-CERT-2 §9 closing sha256 | measured this seat | Δ |
|---|---|---|:--:|
| `KF-W0.md` | `fd226ecb…d09a` | `fd226ecb26ea68e36724fb14ee359a974b3d7d1579ca3398483217f3ec06d09a` | — |
| `KF-W1.md` | `f6986de7…12f0` | `f6986de7b90c2f009c0d0cfdbea6e59a73e3a56c10cbb9223e93a1ef6f6112f0` | — |
| `KF-W2.md` | `5f3656e3…11a4` | `5f3656e32363e49d4fed431facc32c7a458f45d20dc6a69f427b9d0aa2bd11a4` | — |
| `KF-W3.md` | `4b68baf3…86f7` | `4b68baf35a63357d9af857c2c96e73022ecbdd7a3cecfd2a5374be70cde786f7` | — |
| `KF-W4.md` | `4028327c…fe68` | `4028327cb221b6b65f29f6fa7e5e232f1581eb73e6bfca5e2e80c254d5aafe68` | — |
| `KF-W5.md` | `8baba077…404d` | `8baba077d0e6612f7f44888a91d855ebeb2fa7857baf720ed968326bdd4a404d` | — |
| `KF-W6.md` | `b41e5576…daaa` | `b41e55769f9cea837f8ba579b5e59e0bf2cc94abf46f35e53abb648bd993daaa` | — |
| `KF-W7.md` | `ed5f0851…6f21` | `ed5f0851378cde7175b3523cb8f33d2ebc606cfa36103f6a6d07b811d9986f21` | — |
| `KF-W8.md` | `c7c11445…a1c9` | `c7c1144599f8c8cfbe2533c5a4bb65d36357e4e8fb45918e9c09a9c056fba1c9` | — |
| `KF-W9.md` | `bea2d9a6…c744` | `bea2d9a6c519bf975393c54c2b9b23ccad325afb2ef8721b4157eb02143cc744` | — |
| `KF-W10.md` | `1c107329…1c50` | `1c107329458e0ba1ff6762923a5a684391ac32824dc7d7fac42d8e33f4851c50` | — |

**ELEVEN OF ELEVEN REPRODUCE. NO MISMATCH. The round-5 write-set is PROVEN and every repair BANKS.** This is the first close in six rounds whose write-set survives an independent seat, and it is the direct product of LAW E(2) — *an mtime table is a claim about the past; a hash table is a challenge to the future*, and the challenge was met.

**Two consequences follow immediately and are used throughout this check.** (1) Nothing below can be excused as post-cert drift: every figure in `CLOSE-CERT-2.md` was derived over exactly the bytes this seat measured. (2) A cert figure that does not reproduce here is therefore a **defect at the certificate**, not a stale reading — which is what **D6-3**, **D6-4** and **D6-8** are.

---

## §1 · AXIS 1 — ID-KEYED CENSUS, BY RECORD (escapes measured by bytes)

### 1.1 The routing alphabet, closed four ways — every figure re-run this seat

| probe | command | result |
|---|---|---|
| corpus size | `ls kf-*.md \| wc -l` | **58** |
| `KF.W1-DEP` occurrences | `grep -o 'KF\.W1-DEP' kf-*.md \| wc -l` | **18** |
| records carrying them | `grep -c` per file | **5** — ACG **4** · CC **4** · CPW **3** · DGC **4** · KPT **3** |
| literal `KF.W1` substring | `grep -o 'KF\.W1' kf-*.md \| wc -l` | **33** |
| `KF.W10` substring | `grep -o 'KF\.W10' kf-*.md \| wc -l` | **15** |
| **bare `KF.W1` token** | `grep -oE 'KF\.W1([^0-9A-Za-z-]\|$)' kf-*.md \| wc -l` | **0** |
| terminal-disposition alphabet | `grep -ohE '→ \*\*[^*]*W1[^*]*\*\*' kf-*.md \| sort -u` | **`→ **KF.W10**` only** |

33 = 18 + 15 exactly. **The spec's alias-block census reproduces at the bytes, per-file counts included.**

### 1.2 The routed rows, id for id — every hit read and classified this seat

Six adjudicated rows carry a `KF.W1`-form target. All six are **one identity** — census **F-1**, the phantom `@mkbabb/glass-ui` dependency, collided into this namespace by `lane-frontend.md §10`'s superseded taxonomy.

| # | record : line | routing text (read) | booked as | state |
|---|---|---|---|---|
| R-1 | `kf-AnimationControlsGroup:28` | *"FOLD to census F-1 (KF.W1-DEP) at MAJOR — identity guard, not re-booked"* — **verbatim** | C-13 + §8 row 5 | **BOOKED** |
| R-2 | `kf-AnimationControlsGroup:47` | *"L-3 / C-1 — MAJOR · FOLD → census F-1 (KF.W1-DEP)"*; rider read whole this seat — *"three specifiers (`.vue:124`, `.vue:126`, `useControlsKeyboardShortcuts.ts:1`), the third being the sole binding site of all 18 shortcuts"* — **verbatim** | C-13 + §8 row 5 | **BOOKED** |
| R-3 | `kf-ChannelControls:56` | *"FOLD → census F-1 (KF.W1-DEP). Identity guard"*; rider *"`ChannelControls.vue:219` (root-barrel `TooltipProvider, Button`) is the first import that fails"* — **verbatim** | C-13 + §8 row 5 | **BOOKED** |
| R-4 | `kf-ControlsPaneWrapper:60` | *"FOLD → census F-1 (KF.W1-DEP), MAJOR at the bank"*; rider *"`.vue:166` is the demo's SOLE `/drawer` consumer"* — **verbatim** | C-13 + §8 row 5 | **BOOKED** |
| R-5 | `kf-DemoGlobalChrome:44` | *"L-8 (F-1 exposure) — FOLD → census F-1 (KF.W1-DEP) — identity guard, not re-booked"*; the four toast utility classes **verbatim**; **`a59d3a22` dissent carried, not resolved** | C-13 + §8 row 5 | **BOOKED** |
| R-6 | `kf-KfPillTabs:46` | *"F-1 — MAJOR · FOLD → census F-1 (KF.W1-DEP)"*; rider *"one class + eight custom properties, seven glass-ui-owned"* — **verbatim** | C-13 + §8 row 5 | **BOOKED** |

**Anchor decomposition, re-derived from the 18 printed hits**: **5** taxonomy-preamble (`ACG:17` · `CC:17` · `CPW:6` · `DGC:17` · `KPT:19`) + **6** routing rows + **5** ADJUDICATED-summary (`ACG:135` · `CC:155` · `CPW:149` · `DGC:130` · `KPT:122`) + **2** riders (`CC:142` · `DGC:43` — the latter routes to **S-9**, not to F-1, and is correctly classified a rider rather than a seventh routing) = **18**. Closed, and identical to the spec's alias-block enumeration id for id.

**Six rows across five records — ACG carries two.** D3-5's cure at §8 row 5 holds at `:395` (all six enumerated by anchor), and C-13's *"five"* is correctly labelled a RECORD count.

### 1.3 Escape sweep — every other channel, by bytes

- `Mail Cure` → **7 records** (`kf-CopyButton:36` · `kf-EasingScene:33` · `kf-EasingTarget:33` · `kf-EasingSidebar:32` · `kf-KeyframeCard:36` · `kf-KeyframesEditor:36` · `kf-TimingFunctionPanel:34`). Every one read: all seven are the Routing-law preamble reciting the census wave sketch (*"W0 Substrate Settle · **W1 Mail Cure** · W2 Parse Façade …"*) — a declaration of the alphabet, **not a routing**. No row in any of the seven disposes to W1.
- `O-8` → **2 records** (`kf-ChannelOptions` · `kf-LayerConfigPanel`); `O-11` → **1 record** (`kf-ChannelOptions`). **Every hit printed with its preceding characters and read this seat**: all are `KF-CO-8` / `KF-CO-1` / `KF-CO-11` substrings; **none is a letter citation**. ⟨This reproduces the spec's C-14 `D5-9` correction exactly — the PASS-4 register's *"`O-11` → 2 records"* remains non-reproducing, and the correction landed at the right place.⟩
- **ZERO** hits corpus-wide for `X.KF.W1` · `KF-W1` · `I-26` · `INBOX` · `VALUEJS-INBOUND` · `sampleBezier` · `D-GAP-6` · `parseStylesheet` · `lerpArray`.
- **Third namespace re-verified**: keyframes' own `BUILD W1` is live at **exactly six anchors** — `kf-CopyButton:15` · `:40` · `kf-KeyframesEditor:10` (spelled **`BUILD-W1`**, hyphenated) · `:40` · `:51` · `:140`. The spec's six-anchor claim is **EXACT**.
- **The carry** (`KF-W6-CARRY.md`, 199,055 B, mtime **10:49:45** — the one substrate that has never moved): bare `KF.W1` token **0** · `KF.W1-DEP` **1** (the same booked F-1 identity) · literal `KF.W1` **3** · `Mail Cure` **0** · `O-8`/`O-11` **0**. **Nothing escapes to this wave through the carry.**

### 1.4 Census tally

| quantity | value |
|---|---|
| rows routed to census-taxonomy **KF.W1 (Mail Cure)** | **0** |
| rows routed to the collided token **`KF.W1-DEP`** | **6** |
| **ROUTED (denominator)** | **6** |
| **BOOKED** (fold-identity C-13 + §8 row 5 exclusion-with-reason, all six by anchor) | **6** |
| **ESCAPED** | **0** |
| **INVENTIONS** | **0** |

**The ∅-column finding is TRUE and independently re-derived from the records. C-14's L-19 lock holds** — no gate is a script, no row or bounds item was manufactured, and the mail tooling a lesser spec would mint is excluded by name (§8). **The six-row booking is sound at both ends**: all five component riders are verbatim at their records (above) **and** present at `KF-W0.md`'s final bytes (§2.1).

---

## §2 · AXIS 2 — RECEIPT REALITY AT **FINAL** SIBLING BYTES (≥10 ⟨cmd⟩ samples re-run)

### 2.1 What HOLDS — every receipt re-executed at the hash-locked bytes

| # | receipt | measured this seat | verdict |
|--:|---|---|---|
| 1 | **KF-W0 §Carry `C-1.F` row `F-1` — the D4-3 rider landing** | `useControlsKeyboardShortcuts.ts:1` **2** · `ChannelControls.vue:219` **2** · `ControlsPaneWrapper.vue:166` **1** · `toast utility classes` **2** · `eight-custom-property` **1`; `C-1.F` resolves | ✓ **FIVE OF FIVE, at final bytes** |
| 2 | **KF-W0 §Sequencing OUTBOUND row `KF.W1 · Mail Cure`** | `:687` | ✓ |
| 3 | **KF-W0 §Excluded row `KF.W1's mail re-delivery`** | `:728` — *"Depends on this wave, is not performed by it."* | ✓ |
| 4 | **KF-W0 §Sequencing packet-roster, the canonical 17** | `:695`, self-declaring *"re-cut this round from this file's stale 15"* | ✓ |
| 5 | **KF-W4 §Preconditions `OP-5`** | `:38` — *"OPEN — KF.W1…"* | ✓ |
| 6 | **KF-W4 §Sequencing row `KF.W1 · Mail Cure`** | `:264` — *"PRECONDITION on anchor citability"* | ✓ |
| 7 | **KF-W3 §Preconditions `OP-5`** · **`L-4`** · **§Cross-edges row** | `:43` · `:282` · `:309` (*"DEPENDS ON"*) | ✓ all three |
| 8 | **KF-W9 §Carry `S-10.1`** (the declared non-edge) | `:285` | ✓ |
| 9 | **KF-W7 §Excluded `KF-CO-1/KF-CO-8`** · **`N-10`** · **§LAW C SUBSTRATE DISCLOSURE `BACKWARD` bullet** | `:378` · `:127` · `:343` | ✓ all three |
| 10 | **KF-W10 §1 `Opens after`** · **`OP-3`** · **§Sequencing `B. → KF.W1`** · **§6.D SUCCESSOR-FORMATION REGISTER** | `:22` · `:67` (still **UNLANDED**) · `:543` · `:573` — three rows, cargo **9 + 6 + 2 = 17**, six travelling locks, verb `MINTED-UNAUTHORED` | ✓ all four **anchor-only, zero quotations** |
| 11 | **The stage-2 receipt class enumerated at §14** | W7 **3** · W8 **0** · W9 **1** · W10 **4** = **EIGHT anchors**; `grep -oE 'KF-W[0-9]+\.md\`?:[0-9]+' KF-W1.md` → only `KF-W0.md:276` ×2, **both inside dated records that declare it dead** | ✓ **0 quotations · 0 live coordinates · 0 stamps** |
| 12 | **`INBOX.md`** | **97 L / 56,702 B**, mtime 12:33:59, clean. Sweep law `:15-16` verbatim ✓ · narrowed grep **1** at `:15` ✓ · broad grep **5** (`:15 :66 :68 :72 :75`) ✓ · **I-26 at `:92`** ✓ · **I-10 at `:47`** ✓ · **max outbound `O-20` at `:95`** ✓ | ✓ **`O-21` is still `max+1`** |
| 13 | **C-9's precedent quotation** | `INBOX.md:13-14` — *"glass moved BI→BJ→BK … The stale BI pin here cost 19 days on the 08-09 letters → I-28/I-29"* verbatim; `7a7dc6ef` = 2026-08-28 **12:33:59** ✓ · `48003630` = **12:32:16** ✓ · `acff6265` dated 2026-08-28 ✓ | ✓ EXACT |
| 14 | **G-KF1-5 legs (a)+(b)** | `4\.0\.1` → **4 hits** (`INBOX:71`/`:72`/`:80` + O-8 `:101`), **ZERO in the exec-visible tree** ✓ · `withdraw` → **4 hits** (`INBOX:80`, `CONSTELLATION:9`/`:69`, `vnext/skeptic-H1-r2.md:65`), **none a withdrawal of the vehicle question** ✓ · O-8 `:101-103` quoted whole, verbatim | ✓ born-RED, re-executable |
| 15 | **G-KF1-9** | `find … -name '*parser-totality-exposure*' -o -name '*library-band-r1-widened*'` → **0** | ✓ born-RED |
| 16 | **`library-band.md:205-206`** + `:173` | `:205` ends *"DECLINE — `sampleBezier`"*, `:206` opens *"(permanently, measured zero demand)"*; `:173` corroborates | ✓ **the span cure is EXACT** |
| 17 | **`CARRY-CUT-LEDGER.md:186` / `:255`** · **`COHESION.md:76`** · **census `(c)5`/`(c)6`** (`:260`/`:264`) · **`lane-docs:407`** | all verbatim at their sources | ✓ |
| 18 | **`INTAKE-ADJUDICATION-2026-08-03.md` §3** | `:146` heading; the KF.W4 block enumerates `B10-17 · B10-18 · B10-19 · B10-20 · B10-22 · B10-23 · B10-27 · B18-19 · B21-17` — **gap-preserving; `B10-21` correctly absent and homed to KF.W5 at `:156`** | ✓ |
| 19 | **`EXECUTION-HANDOFF.md:13-18`** | quoted whole this seat: the expiry clause *"This invariant survives until W2 completes."* and the `a59d3a22` / *"~254 dirty paths"* record are **both present** | ✓ EXACT |
| 20 | **The cert-path re-point (R5-R6)** | `PASS-5/CLOSE-CERT.md` → **0** in all eleven ✓ · KF-W1 cites `PASS-5/CLOSE-CERT-2.md` **17 lines / 27 occurrences** ✓ · `PASS-4/CLOSE-CERT.md` → **53**, untouched ✓ | ✓ the dead path is dead |

### 2.2 The four sibling properties, re-measured at final bytes

| property | measured this seat | spec/cert says | verdict |
|---|---|---|:--:|
| bare `KF.W1` token over the six non-consumers | **W2 7 · W5 0 · W6 0 · W8 0 · W7 1 · W9 1** | cert R5-R1: same vector | ✓ cert holds |
| `grep -c 'O-11'` over the six | **0 · 0 · 0 · 0 · 0 · 0** | 0 in all six | ✓ |
| every `O-8` hit a `KF-CO-8`/`KF-CO-1` substring | **15 occurrences: W2 3 · W5 1 · W6 4 · W7 2 · W8 0 · W9 5**, every one printed and read | cert: *"all fifteen … 3+1+4+2+0+5 = 15"* | ✓ cert holds — **and it convicts the spec, D6-1** |
| `KF-AV-28` over the ten siblings | W0 4 · W2 12 · W3 8 · W4 5 · **W5 0** · W6 12 · W7 14 · W8 4 · W9 12 · W10 11 | *"live in nine, absent only from KF-W5"* | ✓ |

**The nine bare-token hits were read and classified individually**: W2's seven (`:29 :308 :418 :836 :982`) are all *"`O-21` — KF.W1's outbound row"* relay-vehicle **attributions** minted by W2's own round-5 `D-13` cure; W7's is its stage-1-sibling roster listing; W9's is `S-10.1`'s declared non-edge. **Nine of nine are NON-EDGES. The six-carry-no-KF.W1-edge claim HOLDS.**

### 2.3 What FAILS

**D6-1** (the surviving W2 `O-8` numeral, uncorrected) · **D6-2** (the retired form not retired, at three sites) · **D6-3** (the cert disagrees with itself on its own headline census) · **D6-4** (the cert's write-order enumeration cannot derive its own stage-order certification) · **D6-5** (LAW E(4) applied asymmetrically — ten stamps still standing at KF-W10).

---

## §3 · AXIS 3 — M-25 DEPTH + MECHANISM S (LAW F) + THE LAW-A CENSUSES

### 3.1 Every keyframes-side witness reproduces byte-exact at `origin/master 81a56990`

Re-executed this seat, read-only:

- three ABSENT packet anchors (`compile/value-ast.ts` · `compile/easing/easing-registry.ts` · `compile/emit/backward.ts`) ✓
- `resolve/browser.ts:162` = `const parsed = parseCssScalar(source);` (import at `:3`) ✓ · `compile/value/compile.ts:32` = `const parsed = parseCssValues(value);` ✓ · `easing/registry.ts:36` = *"Stable identities let the serializer distinguish named curves from closures."* ✓ · `easing-serialize.ts:71` = `const registryName = timingFunctionEntries.find(` ✓
- `emit/backward/` = exactly `backward.ts, color.ts, index.ts, walk.ts` ✓ · `color.ts` **385 L** ✓ · **§5a rows 6–8 EXACT**: `:171` = `const sampleRamp = (` · `:250` = the `stopCount` call · `:263` = the `1024` call ✓
- `package.json:70` value.js `"4.0.0"` ✓ · **`:77` glass-ui `"7.0.0"` — SCH-1's falsifier, F-1 FALSIFIED-AT-HEAD** ✓
- `options.ts:31` ✓ · `leaves.ts:28` ✓ · `load-engine.ts:65` ✓ · `css-text.ts:41` ✓ · `test/internal/leaves-parity.test.ts` **PRESENT** ✓
- `parseStylesheet` raw output = **nine** lines: call sites `adapter.ts:222` · `scroll/grammar.ts:109` · `validate.ts:182`; imports `:7`/`:37`/`:47`; prose `adapter.ts:202`/`validate.ts:41`/`validate.ts:130` ✓ **exactly as G-KF1-3 states**
- frontier import census **62** = `/css` 29 · `/value` 16 · `/color` 7 · `/math` 5 · `/easing` 3 · `/transform` 2 ✓
- exec clone: coordination dir = **9 entries + `vnext/`**, no addendum/O-8/O-11 ✓; `INBOUND-LEDGER` = **9 rows** (IN-ATLAS-1..5 · IN-GLASS-1..2 · IN-VALUE-1..2), no IN-VALUE-3/4 ✓

**§5b's three-substrate receipt reproduces EXACTLY**: worktree census **61** (`/css` 29 · `/value` **15** · `/color` 7 · `/math` 5 · `/easing` 3 · `/transform` 2) · bare `8281638c` **81** over a **disjoint alphabet** (`/parsing` 24 · `/units` 26 · root 10 · `/color` 9; `/css` and `/value` absent) · frontier **62**. Glass-ui triple: worktree **absent from the manifest** · `8281638c:package.json:71` = `"6.0.0"` · `origin/master:package.json:77` = `"7.0.0"`. **Sacred checkout**: HEAD `8281638c` · `41  1` ✓ · **252** dirty ✓ · both bodies **7,064 B** (Jul 24 16:41) / **15,633 B** (Jul 27 12:27) ✓. **Zero writes, zero git mutations, zero npm.**

### 3.2 LAW A — §4c's census REPRODUCES, SUMS, AND ITS SELF-REFERENCE ARM FIRED EXACTLY AS DECLARED

Every leg re-run this seat at the current tree bytes:

| leg | spec's round-5 re-measurement says | measured this seat |
|---|---|---|
| (1) `grep -rnF 'keyframes.js/docs/tranches/V/coordination' docs/ --include='*.md'` | **27 hits / 19 files** | **27 / 19** ✓ |
| (1b) `grep -rnF '../keyframes.js/docs/tranches/V/' docs/ --include='*.md'` | **16 hits / 9 files** | **16 / 9** ✓ |
| (2) the narrowed subject grep | **1**, at `INBOX.md:15` | **1**, `:15` ✓ |

**The nine-class partition is exhaustive, disjoint and sums by construction — every member enumerated and verified this seat**: A `INBOX.md:15` · B `:66 :68 :72 :75` · C `value-inbox-2026-07-20-residual-repairs:33`, `keyframes-inbox-2026-07-18-…:456` · D **seven** apotheosis files (`armA/edict-matrix-draft:140` · `armA/edict-matrix:143` · `armB/final-PA-parse:73` · `armB/final-PB-color:224` · `armB/program-PA-parse:72` · `armB/program-PB-color:151` · `probe/coordination/…handoff:456`) · E `lane-docs:290` · F `harvest/packet-seat-O11-O15-receipts:11` · G `DISPOSITIONS:51`, `W40:136`, `WL:63` · H **three** (`PASS-1:125`, `PASS-4:189`, **`PASS-5:165`**) · I this spec at **`:74 :136 :198 :322 :337`** — **1+4+2+7+1+1+3+3+5 = 27.** Census (1b) likewise: `5 + 4 + 3 + 4 = 16`, this spec's four at **`:139 :159 :198 :337`**.

⟨**All nine of the spec's own intra-file class-I coordinates re-resolve EXACTLY at the current bytes** — `:74 :136 :198 :322 :337` for census (1) and `:139 :159 :198 :337` for (1b) — after a **+49,776 B** round-5 growth. That is not luck: intra-spec coordinates move with the file and the seat re-ran the censuses **last**, as §4c's disclosure requires. **This is the cleanest instrument in the file.**⟩

⟨**The self-reference disclosure fired and was booked, not hidden**: class H took its third member when `PASS-5/KF-W1-CHECK.md` landed, exactly as the class's stated rule predicts, and §14 row 10 records it as *the disclosure working* rather than as a defect — correctly. **This file enters class H as its fourth member at its next reading**, and the STABLE DENOMINATOR is unchanged at **TWO**: `INBOX.md:15-16` in bounds, and `~/.claude/…/memory/feedback-mail-inbox-law.md:16` **re-verified present this seat**, still naming `../keyframes.js/docs/tranches/V/` among its four landing paths, still out of bounds and routed to the OWNER. **There is no third.**⟩

### 3.3 M-25 depth + mechanism S

**All ten locks present and verbatim** (C-1's *"Amend, do not re-send"* + ANTI-STALENESS · C-3 · C-6's LAW-B substrate lock · C-7 · C-8 · C-9's SIZING LOCK · C-10's MINT LAW · C-11 · C-12's HARD NON-GATING · C-14's L-19). **All three dissents carried, not resolved**: the `a59d3a22` rider (routed to KF.W0's re-measurement) · the KF-W0 reverse-edge conflict (recorded for COHESION §3 and the owner) · C-4's recorded asymmetry. Ids anti-rename; `SCH-4` untouched; **C-1..C-14 net delta 0**; gate set `G-KF1-1..12` intact, **twelve headings measured, tally `10 born-RED · 1 DECLARED-SATISFIED · 1 stay-GREEN INVARIANT` stated identically at five sites**. **Agglomeration by identity is correct — one identity, six rows, booked once.**

**MECHANISM S (LAW F), tested for the line's presence at every qualifying verb.** Five in-place **SHADOW** blocks (§1's KF-W10 bullet · §1's SIX-non-consumers bullet · §9 item 5 · §13's substrate table · §13's KF-AV-28 cell) plus §14's round-5 **SHADOW LEDGER** enumerating the whole set: struck quotations (5 acts / 3 ids, each banked anchor re-read first per LAW F(3) and **KEPT**) · struck numerals (6 acts, all **retired to properties**) · retired coordinates (1) · re-pointed citations (10) · **MINTED: NOTHING**, with termini notified {KF-W10 · KF-W7 · KF-W9 · KF-W0 · RECONCILE}. **No row, routing, edge, direction, force, bound or gate moved this round** — verified independently: `C-1..C-14` and `G-KF1-1..12` are byte-identical in id and count to pass 5. **The shadow discipline is executed to the letter here; where it is not executed is at the far end (D6-5).**

**LAW F(3) at the restoration this wave's bank feeds.** The R5-1 BLOCKER cure is verified end-to-end at the frontier, per-row and per-anchor: `EditorShell.vue:45` = `title="Toggle dark mode"` · `:46` = `class="aspect-square w-8 scale-on-hover"` · `:36` = the shortcuts `Button`'s sizing utility · `MbabbMenu.vue:20-21` = `title` + `class="aspect-square w-5"` — **all four read at `81a56990` this seat**, and all three banked anchors (`kf-EditorHeader.md:43`/`:44`/`:47`) carry exactly the bytes the rows convict. `KF-W6.md` books **EH-4 · EH-5 · EH-8 LIVE, one row per banked anchor** (`:460`/`:461`/`:462`), grants them at `§Bounds` `EditorShell.vue` (`:68`) and `MbabbMenu.vue` (`:76`), strikes the round-4 premise at `:451`/`:459`, and carries **five SHADOW lines**; `KF-W0 §Excluded` carries the reciprocal at `:270`. **The restoration is real, per-anchor, and two-ended.**

---

## §4 · AXIS 4 — GATES: REACHABLE GREEN · LAW-A CENSUSES · CORPUS-DERIVED OPERANDS · LAWFUL LANDING SURFACES

**R5-11(2) executed first — receipt arithmetic before scope.**

| receipt | its own stated arithmetic | measured this seat | verdict |
|---|---|---|:--:|
| §14 CARRIAGE-CITATION | 26 lines · 45 citations · class (a) 8 · (b) 15 · (c) 2 · (d) 1 = 26 · `PASS-5` named **20** | **26 lines · 45 citations · 20** — and the (a) set verified site by site: **all eight cite PASS-5** | ✓ arithmetic · ✗ **scope, D6-6** |
| §14 LAW-A / access column | *"SIX rows — `modify` ×2 · `create` ×4"* | §Bounds table has **FIVE rows / SIX files**; `**create**` greps 4 only by counting row 1's **SPENT** grant | ✗ **D6-7** |
| §14 SUBJECT-IDENTITY | symbol-bearing bounds rows **0** · gate-witness symbols **11**, each with file AND line at a named substrate · bare leaves **0** | reproduces: all bounds rows are markdown; the eleven symbols carried at §5a rows 1–14 and G-KF1-2/-3/-4 | ✓ |
| §14 NUMERIC ARM (re-cut as a rule) | *"numerals over stage-2 siblings: 0 · per-seat mtime stamps: 0 · line numbers on cross-wave receipts: 0"* | **0 · 0 · 0** — the only `KF-Wn.md:NNN` tokens are `KF-W0.md:276` ×2, both declared dead in dated records | ✓ **the rule-not-a-list form works** |
| §14 STAGE-2 RECEIPT class | W7 3 · W8 0 · W9 1 · W10 4 = **8 anchors, 0 quotations, 0 line numbers, 0 stamps** | reproduces exactly (§2.1 row 11) | ✓ |

**Tally agrees at every altitude**: §1's header, §6, §11, §12, §13 and §14 all read **10 born-RED · 1 DECLARED-SATISFIED · 1 stay-GREEN INVARIANT**. G-KF1-1..10 are each genuinely RED right now, each with a re-executable witness this seat watched fail (§2.1 rows 14–15, §3.1). G-KF1-11 and G-KF1-12 are correctly relabelled and correctly excluded from the count.

**Reachable GREEN, gate by gate.** G-KF1-4 keeps the D4-4 cure intact — the **SENTENCE TEMPLATE** plus the four-part **SUBSTRATE-NAMING RULE**, with *"every numeral in (i)–(iv) FILLED AT AUTHORING by re-running §5b's three commands"* and the round-3 sentence preserved as a dated **exemplar**; its falsifier still carries the two legs that make the pattern enforceable. **All five of §5b's exemplar numerals re-measure identical this seat** (`8281638c` · 252 · 61 · 81 · 62), so the exemplar is honest and the anti-copy lock is still the operative order. G-KF1-7's GREEN is `max+1` **measured at open** under C-10's MINT LAW, not the literal — and `INBOX.md`'s maximum is still `O-20` at `:95`, so **`O-21` is still `max+1` at this clock**. G-KF1-10's counterparty half stays explicitly NON-GATING (C-12's HARD lock), so the wave is completable by construction. G-KF1-12 is quoted with its expiry clause and its independence declaration.

**Corpus-derived operands.** Every gate operand traces to a command over the corpus, the two trees or the ledger — `ls` (G1), `git cat-file`/`git grep` at `origin/master` (G2/G3/G4/G10), absolute-path `grep`/`sed` over the three coordination trees (G5), `INBOX.md` row ids (G6/G7/G8), `find` on the value.js root (G9), `git status --porcelain` (G12). **Nothing is invented; nothing is a script.**

**Lawful landing surfaces — every act has its bounds row.** The wave's acts are: two back-fills (`create`), the addendum (`create`, sibling tree), its retained copy (`create`), three `INBOX.md` edits (`modify`), and repair of this spec (`modify`). **Every GREEN lands inside those rows.** The one repoint act (C-9/G-KF1-8's mail-path law) is a repoint of a **law**, not a module, and it carries a LAW-A-shaped census anyway. The out-of-bounds second limb (the memory file) is **routed to the OWNER and edited by nobody** — the correct disposition, re-verified.

---

## §5 · AXIS 5 — POSTURE

| item | verdict |
|---|---|
| **W4 head honored** | ✓ No conflict. KF-W1 declares PRECEDES-on-anchor-citability; KF-W4 reciprocates at `OP-5` (`:38`) and its §Sequencing row (`:264`). W4's vue-tsc sequencing head is untouched: **W1 opens no source and no `package.json` in either repo** — all six bounds files are markdown. |
| **W3 gated-unscheduled** | ✓ KF-W1 names it *"KF.W3 (Parser Consumption, **GATED**)"* at cross-edge 1 and schedules nothing. KF-W3 reciprocates at `OP-5` (`:43`) · `L-4` (`:282`) · §Cross-edges (`:309`). |
| **O-21 at every mint site** | ✓ **CLEAN.** Thirteen `O-21` sites in KF-W1, all five load-bearing mint sites plus the trailing citations; `INBOX.md`'s maximum is still **`O-20`** at `:95`, so the literal is still `max+1` and the MINT LAW governs over it. KF-W4 (**2**) and KF-W10 (**7**) both name `O-21` by id; `KF-W10 OP-3` (`:67`) and `§Sequencing B.` (`:543`) carry it explicitly. Surviving `O-20` tokens are all *measurements* of the ledger maximum. |
| **§6.D successor register cited** | ✓ **PRESENT AND COHERENT AT BOTH ENDS.** `KF-W10 §6.D` (`:573`) exists with three rows, cargo **9 + 6 + 2 = 17** matching R-15 and KF-W1 §8 id-for-id, six travelling locks, two minting citations, verb `MINTED-UNAUTHORED`, MECHANISM F declared. KF-W1's cross-edge 9 carries the second end **anchor-only** and records the flag **REGISTERED**. **This wave homes ZERO of the seventeen and §8 says so.** W10's quotation of KF-W1's cross-edge 9 (`:584`) **byte-diffs clean at KF-W1's final bytes** — but the stamp beside it does not (D6-5). |
| **COHESION §0d cited** | ✓ `COHESION.md:185` **is** §0d, carrying the KF.W11/W12/W13 boundary line at `:188`; R5-12 clause 4 is **DISCHARGED** and W10 cites it as landed historical fact. The **ownership** half is correctly re-cut at W10's five sites to *"a dated COHESION addendum mirroring §0b — §0e or a successor letter, because §0c and §0d are occupied"* (`:5`, `:7`), so no resolving seat can read a landed-but-unrelated section as satisfying the precondition. **R5-12's premise is verified true.** |
| **EH-4/5/8 restoration with true anchors** | ✓ **VERIFIED AT THE FRONTIER, PER ROW** (§3.3). `EditorShell.vue:45`/`:46`/`:36` and `MbabbMenu.vue:20-21` all carry the exact bytes the three rows convict; the bank (`kf-EditorHeader.md:43`/`:44`/`:47`) says exactly what the rows say; W6 books all three LIVE one row per anchor inside surviving §Bounds grants, with five SHADOW lines; W0's §Excluded reciprocal is landed. **The BLOCKER is closed on the record's own terms and nothing of the restoration is stranded.** |
| **W10's obligations closed by carriage not omission** | ✓ (not this wave's). The carry routes **nothing** here; W1 mints no new obligation and hands W10 **the row**, not a frozen id. |

---

## §6 · DEFECTS

### D6-1 · **MAJOR** — §1's SIX-non-consumers bullet still carries `KF-W2's at 2 occurrences / 2 lines` as a live, uncorrected numeral; it is **3 / 3** at final bytes, and the certificate that swept this very sentence says so three clauses away

`KF-W1.md:60`, in the operative voice, immediately after the `O-8`-substring property:

> *"…every `O-8` hit is the `KF-CO-8`/`KF-CO-1` substring, **KF-W2's at 2 occurrences / 2 lines** (not the *"1 … 1"* an earlier reading carried — W2 was written **ten minutes after** this file at round 3, and its second hit arrived with that write)…"*

Measured this seat at `KF-W2.md`'s hash-locked final bytes: `grep -o 'O-8' KF-W2.md | wc -l` → **3**; `grep -c 'O-8' KF-W2.md` → **3** (`:452` · `:553` · `:916`, each read, each a `KF-CO-1 / KF-CO-8` substring). **The stated 2/2 is false under both metrics.**

**Two aggravations make this a defect rather than a dated reading.** **(a)** The **RECONCILE SEAT's own insertion sits inside the same sentence** and re-verified the neighbouring properties (the token vector, `O-11` = 0, *"every one of the fifteen `O-8` hits"*) while leaving this numeral untouched — and **`PASS-5/CLOSE-CERT-2.md` §4 R5-R1's arithmetic is `3+1+4+2+0+5 = 15`, which names W2 at 3.** The certificate contradicts, by its own pasted sum, the numeral it swept past. **(b)** The numeral carries **no substrate, no stamp and no retirement marker**, unlike its twelve siblings at §9 item 5, which the round-5 note explicitly retires (*"dated round-4 readings, retired as receipts"*). It reads as current because nothing says otherwise.

**Nothing load-bearing moves**: every one of the three hits is a substring, so *"zero real O-8/O-11 letter citations across the non-consumer set"* — the property the COHESION §3.1 mark actually turns on — **HOLDS and is re-verified**. **The cure is subtraction, one edit**: strike the parenthetical clause and let the property carry, exactly as the file's own D5-4/D5-5 arm does everywhere else. Owner **KF-W1**.

### D6-2 · **MAJOR** — the retired form was not retired: `0 in FOUR` survives as the lead clause at all three live sites and is FALSE at final bytes

§14 row 1 states the cure: *"the per-sibling zeros are **RETIRED as the load-bearing form** … what now carries is the property no sibling write can falsify."* Measured this seat, the retired form is still the **lead clause** at all three sites:

| site | text carried | true at final bytes |
|---|---|---|
| `:60` §1's SIX-non-consumers bullet | *"is **0 in FOUR** (KF-W2 · KF-W5 · KF-W6 · KF-W8; W2's literal `KF.W1` greps resolve to KF.W11/W13 prefixes)"* | **KF-W2 = 7**; and the parenthetical is false too — `grep -oE 'KF\.W1[0-9]' KF-W2.md` returns the prefixes **separately** |
| `:368` cross-edge 1 | *"bare-`KF.W1` token **0 in FOUR**"* | same |
| `:415` §9 item 5 | *"**The bare-`KF.W1` token is 0 in FOUR**, and 1 each in KF-W7 and KF-W9"* | same |

**This is D5-1's class firing a FOURTH consecutive round, on the axis that defines this wave's consumer set** — round 4 corrected W9 and left the universal, round 5 corrected W7 and left the count, and the count broke on a **stage-1 peer** whose D-13 cure landed at **18:54:59**, *before* this file's own **18:56:54** round-5 write. Under **R2-5** (*"a witness a seat leaves standing without re-running is that seat's defect"*) it was re-measurable at write time and was not re-measured.

**Strongly mitigated, and the mitigation is why this is MAJOR and not BLOCKER**: the RECONCILE insertion at every one of the three sites names the figure **FALSE at final bytes, loudly, with its command and its cause**, and the load-bearing property is re-verified at all four legs. No reader is misled and no act, row, gate or edge rides the numeral. **The cure is the one the file already chose for D5-4 and D5-5 and declined to apply here: strike the numeral, keep the property.** A count that has been falsified in four consecutive rounds does not get a fifth restatement. Owner **KF-W1**.

### D6-3 · **MAJOR** — `PASS-5/CLOSE-CERT-2.md` disagrees with itself on its own headline census: the head says **73**, §4 R5-R6's pasted transcript says **70**, and the bytes say **73**

The certificate's head (`:7`) states: *"The bare-name spelling **`CLOSE-CERT-5`** (63 sites) and the path spelling **`PASS-5/CLOSE-CERT-2.md`** (**73 sites**) both resolve HERE."*

§4 R5-R6 pastes the transcript that produced the cure:

```
$ grep -oh 'PASS-5/CLOSE-CERT-2\.md' waves/KF-W*.md | wc -l    → 70   (after; 6 were already correct)
```

Re-run by this seat, the identical command over the identical bytes: **73** (per file: W0 3 · W1 27 · W2 5 · W3 1 · W4 4 · W5 1 · W6 5 · W7 2 · W8 8 · W9 8 · W10 9 = 73). The other three lines of that transcript reproduce exactly (`PASS-5/CLOSE-CERT.md` → **0** before-and-after ✓; `PASS-4/CLOSE-CERT.md` → **53** untouched ✓).

**This cannot be drift, and that is the point.** §9's hash table — **re-verified 11 of 11 by this seat as its first act (§0)** — proves no spec moved after the certificate. So the certificate's own pasted command does not reproduce over the bytes the certificate itself sealed, and its own head states the number the bytes give. **One instrument, two figures, one quantity — the D3-4 class (*one file, two verdicts*) at the round's closing certificate**, on the very census R5-R6 exists to certify.

**Nothing banks falsely**: the cure is real (the dead path is at **0**, verified in all eleven), the count is *under*-stated rather than over-stated, and the head carries the true figure. **The cure is one re-run of the transcript line, or a `⟨corrected⟩` marker beside it.** Owner **RECONCILE**. Recorded here because prior-pass artifacts are never edited (E-3), so the correction of record must live in a check.

### D6-4 · **MAJOR** — the certificate's write-order enumeration lists **nine** entries for eleven specs, substitutes reconcile clocks for three of them, and as printed places a stage-1 spec AFTER two stage-2 specs — under a sentence certifying that the stage order held

`CLOSE-CERT-2.md:204`:

> *"The per-wave round-5 write order, from the pass's own clocks, was **W0 18:49:33 → W4 18:50:12 → W3 18:50:14 → W6 18:52:46 → W2 18:54:59 → W5 18:55:46 → W8 19:11:06 → W9 19:17:10 → W1/W7 19:28 (predecessor reconcile) → W10 19:30:41**. **LAW C's stage order held**: every stage-1 spec wrote before every stage-2 spec, W0 first, W10 last of the per-wave seats."*

Three substitutions, measured against the certificate's own §3: **`W1/W7 19:28`** is labelled *"(predecessor reconcile)"* — §3 dates that pass's insertions at `KF-W1.md` **19:28:19** and `KF-W7.md` **19:28:45** — and **`W10 19:30:41`** is, to the second, the same predecessor pass's third insertion (§3: *"and `KF-W10.md` (19:30:41)"*). **So the enumeration carries no per-wave write clock for W1, W7 or W10** — the three specs whose own round-5 seats each wrote a full repair section (KF-W1 §14 with ten rows; W7 and W10 likewise).

**The true clock for this wave exists and is in the tree**: `KF-W1.md:60` dates its own round-5 write at **18:56:54** — which slots between W5 (18:55:46) and W8 (19:11:06) and **satisfies the stage order perfectly**. The certificate does not carry it. As printed, stage-1 **W1 at 19:28** sits *after* stage-2 **W8 19:11:06** and **W9 19:17:10**, so the sentence *"every stage-1 spec wrote before every stage-2 spec"* is **not derivable from the table that is offered as its proof**, and *"W10 last of the per-wave seats"* rests on a timestamp the certificate itself attributes to a reconcile pass.

**The underlying order is almost certainly fine** — this is a receipt defect, not an ordering defect, and **LAW E's hash arm is untouched: the write-SET is proven (§0).** But LAW E exists precisely because *"an mtime table is a claim about the past"*, and this is the **third consecutive round** in which the close certificate's proof-of-order does not survive an independent read (round 4: the twelfth write; round 5 predecessor: the falsified byte-stamp; round 5 final: this). **The cure is to print eleven per-wave clocks, or to state plainly that the order is proven only for the eight specs whose per-wave clocks were captured.** Owner **RECONCILE**.

### D6-5 · **MAJOR** — LAW E(4) was applied to this wave and not to its hardest downstream consumer: `KF-W10.md` still carries a ten-row per-seat substrate stamp table, two rows of which stamp THIS file at bytes it no longer has

**LAW E(4)**, minted this round: *"Per-seat substrate stamping — the mechanism behind 9 of the drift tail's 16 rows — is **retired**."* KF-W1 executed it: §13's table is struck **as authority** and preserved as record (§14 row 6), round 5 adds no table, and the file's per-seat mtime stamps written this round measure **0**.

Measured this seat at `KF-W10.md`'s final bytes: **a complete ten-row per-seat substrate table survives**, every row at a round-4 clock —

```
`KF-W0.md` | 2026-08-28 16:58:59   `KF-W1.md` | 2026-08-28 17:00:23 | 1 (final)
`KF-W2.md` | 17:02:55   `KF-W3.md` | 16:56:52   `KF-W4.md` | 16:59:44   `KF-W5.md` | 16:55:55
`KF-W6.md` | 16:59:25   `KF-W7.md` | 15:39:04   `KF-W8.md` | 15:50:14   `KF-W9.md` | 15:39:31
```

— plus a second stamp on the **§6.D minting-citation block** (`:581`): *"quoted by command at both sources' post-round-4 STAGE-1 bytes ⟨`stat` → `KF-W4.md` **16:59:44** · `KF-W1.md` **17:00:23**⟩."* **Both KF-W1 stamps are false at final bytes** (`KF-W1.md` = 2026-08-29 **15:17:38** / **250,457 B**), and one of them is labelled ***"1 (final)"*** of bytes that were superseded twice over.

**The asymmetry is the finding.** CLOSE-CERT-2 struck exactly **one** stale stamp this round (R5-R2, inside `KF-W7.md`) and certified the sweep *"COMPLETE across all five classes"* with **866 receipts checked** — while ten stamps of the retired class sat in the file the sweep swept last and fixed most (20 sites). The class LAW E(4) was written to kill survives in the largest single instance in the tree.

**Nothing load-bearing moves**: the quotations those stamps vouch for still reproduce — this seat byte-diffed W10's quotation of KF-W1's cross-edge 9 (`:584`) against `KF-W1.md:376` and it is **exact** — and all four W10 anchors resolve (§2.1 row 10). **The cure is the one KF-W1 already performed**: strike the table as authority, preserve it as a dated record, re-point the stamp authority to the cert's §6. Owner **KF-W10** (cure) + **RECONCILE** (the sweep that missed it).

### D6-6 · **MINOR** — §14's CARRIAGE-CITATION receipt passes its arithmetic and fails its own stated locus: class (b) says "inside §10–§13" and 7 of its 15 members are outside

**The arithmetic reproduces exactly**, and R5-11(2) is satisfied: `grep -n 'PASS-[0-9]/KF-W1-CHECK' KF-W1.md` → **26 lines** carrying **45** citations, `grep -oE 'PASS-5/KF-W1-CHECK' | wc -l` → **20**, and `8 + 15 + 2 + 1 = 26`. The class-(a) rule holds too: **all eight** carriage sites (`:7 :13 :15 :186 :233 :381 :461 :478`) cite **PASS-5**, verified site by site — the D5-2 cure is complete, and the receipt's own defence of enumerating **eight** where §12 row 6 enumerated six is correct and well-argued.

What does not reproduce is class (b)'s **locus**. The receipt reads: *"(b) **dated-record references** inside **§10–§13**…"*. Measured, the 15 members are `:369 :430 :451 :484 :493 :495 :521 :534 :568 :580 :588 :589 :597 :604 :622`. §-boundaries at the current bytes: §10 `:428` · §11 `:449` · §12 `:482` · §13 `:519` · §14 `:578`. So **8** members lie in §10–§13 and **7** do not: **`:369`** (§7, cross-edge 2's round-3 correction block) and **`:580 :588 :589 :597 :604 :622`** (all six in **§14**, the round-5 record itself).

A seat re-running the receipt from its stated rule gets **8**, not 15, and the partition stops summing. **The scope claim is unharmed** — every one of the seven was read this seat and every one is genuinely a dated-record reference that must never be re-pointed — so the closing rule (*"every hit in (a) cites the freshest artifact; no hit in (b), (c) or (d) is ever re-pointed"*) is **TRUE**. **The cure is three words**: *"dated-record references **anywhere in this file**"*, or the addition of §7 and §14 to the named range. Owner **KF-W1**.

### D6-7 · **MINOR** — the LAW-A / access-column receipt says "SIX rows"; the §Bounds table has FIVE, and the counting rule R5-11 requires is not stated

§14's second scope receipt: *"**§Bounds access column enumerated whole: 6 rows — `modify` ×2 · `create` ×4**; rows carrying `delete` / `modify-delete` / `repoint` / `shim` / `move`: **0**."* §4c(i) repeats it: *"This wave's **six bounds rows** are `modify` ×2 … and `create` ×4."*

Measured this seat: the §Bounds table (`:98-105`) carries **FIVE** data rows. The sixth exists only by splitting row 5 — `…parser-totality-exposure.md` **·** `…library-band-r1-widened-k1-k4.md`, two files on one row — into two. And a bare `grep -oE '\*\*(modify|create)\*\*'` over the table returns `create` ×4 only because it counts **row 1's explicitly SPENT grant** (*"The superseded **create** grant is **SPENT**"*), which is a struck grant, not a live access word. Under either reading the stated "6 rows" is not what the access column contains: it is a **file** count wearing the word *rows*.

**R5-11(1) is explicit**: *"Every scope receipt states, at the receipt: the enumeration (or a pointer to the one enumeration), the **counting rule**, and the command."* This receipt states a command and a total and **no counting rule**, and it is the one receipt in the file whose arithmetic a re-running seat cannot reproduce. **The substance is untouched and re-verified**: zero rows carry `delete`/`repoint`/`shim`/`move`, LAW-A censuses owed **0** and present **0**, all six files are markdown documents, and the module clause is genuinely vacuous. **The cure is one clause**: *"counting rule: one bounds FILE = one row; row 5 names two files; row 1's superseded `create` is SPENT and is not counted."* Owner **KF-W1**.

### D6-8 · **INFO** — the certificate states the bare-name census as "63 sites" at both of its sites; it is 64 occurrences / 37 lines

`CLOSE-CERT-2.md:7` and §4 R5-R6 both read *"The bare-name spelling `CLOSE-CERT-5` (**63 sites**)"*. Measured this seat over the hash-locked bytes: `grep -oh 'CLOSE-CERT-5' waves/KF-W*.md | wc -l` → **64**; `grep -c` summed per file → **37 lines**. **Neither reading yields 63.**

Recorded as INFO because nothing rides it: the bare name is explicitly *"a round-name, not a path"*, is declared equivalent to the file at the head, and resolves for every reader regardless of cardinality. It is booked only because it is the **same shape as D6-3 in the same document** — a census figure stated without its counting rule (sites? occurrences? lines?) at an instrument whose whole authority is that its figures reproduce. Owner **RECONCILE**.

### D6-9 · **INFO** — the dated intervals are one day stale at this reading, exactly as their own law predicts

`G-KF1-10` and §9 item 4 record silence at **41 days** (2026-08-28), O-8 unanswered **35**, O-11 **32**. At this seat's clock they are **42 · 36 · 33**.

**Lawful in form and recorded, not booked as drift**: every one is explicitly declared *"dated and non-inheritable — the executing seat re-measures again"*, and C-1's ANTI-STALENESS lock is the reason they are written that way. It is noted only so the executing seat re-runs them at open rather than reading them as current, and because it is the one figure class in the file that goes stale **without any seat writing anything** — the pure case of why the ANTI-STALENESS lock exists.

---

## §7 · WHAT STANDS

- **The census is CLEAN and re-derived BY RECORD from the 58 files' bytes**: **6 routed / 6 booked / 0 escaped / 0 inventions**, closed four ways (bare token **0** · substring decomposition **33 = 18 + 15** · terminal-disposition alphabet · carry sweep), all six routing rows read and classified individually, all five component riders verbatim at the records **and** present 5-of-5 at KF-W0's final bytes. **Zero rows, gates or bounds items were manufactured to fill the ∅ column.**
- **THE LAW E(2) HASH GATE PASSES 11 OF 11.** The round-5 write-set is proven against an independent seat — the first close in six rounds to survive that test — and every round-5 repair **BANKS**. The instrument that was minted to end the drift class did end the part of it a hash can reach.
- **All ten PASS-5 local defects are CURED at the bytes**, three of them beyond the letter of the ruling:
  - **D5-2** — the carriage class is swept **whole** (eight sites, not the six §12 row 6 listed), made **greppable**, and given a receipt whose arithmetic reproduces exactly (26 · 45 · 20). The receipt even declares **itself** a member of the class it enumerates.
  - **D5-5** — the three stage-2 quotations are gone, the anchors kept, `OP-3` and `B.` retained as **labelled paraphrases**, and the last line coordinate retired: the anchor-cleanliness axis now holds at **zero live coordinates by construction**, measured, not promised.
  - **D5-6/D5-7** — the substrate table and the KF-AV-28 numerals are struck **as authority** and preserved **as record**, with the authority re-pointed to the cert. §14's NUMERIC ARM is re-cut from a *list* to a *rule*, and the rule measures **0 · 0 · 0**.
- **§4c's LAW-A census reproduces, sums, and predicted its own instability correctly**: **27 / 19** and **16 / 9**, nine classes summing by construction, class H taking its third member exactly by its stated rule, and **all nine intra-file class-I coordinates re-resolving EXACTLY** after a +49,776 B round. The **STABLE DENOMINATOR is TWO**, a fifth round running.
- **Every keyframes-side witness reproduces byte-exact at `origin/master 81a56990`** — §5a row for row, §5b's three censuses (**61 / 81 / 62**) and the glass-ui triple (absent / `6.0.0`@`:71` / `7.0.0`@`:77`), the nine-line `parseStylesheet` output, the 9-row `INBOUND-LEDGER`, the 9-entry coordination listing, the sacred tuple (`8281638c` · `41 1` · **252** · 7,064 B / 15,633 B) — and the sacred checkout was read and never written.
- **Every value-side `INBOX.md` anchor HOLDS unchanged** (97 L / 56,702 B · `:15-16` · `:47` · `:92` · max `O-20` at `:95`) — **`O-21` is still `max+1`**, named by id at KF-W4 and at all three KF-W10 ends.
- **Mechanism S is executed to the letter here**: five in-place SHADOW blocks plus a round ledger enumerating the whole struck/retired/re-pointed set with per-id dispositions, banked anchors re-read first (LAW F(3)), **MINTED: NOTHING**, and termini notified. **C-1..C-14 net delta 0; G-KF1-1..12 intact; the gate tally 10 · 1 · 1 identical at five sites.**
- **The R5-1 BLOCKER chain is verified end-to-end at the frontier** — `EditorShell.vue:45`/`:46`/`:36` and `MbabbMenu.vue:20-21` carry the convicted bytes, the bank says what the rows say, W6 books EH-4/5/8 LIVE one row per anchor inside surviving grants, and W0's reciprocal is landed. **The record was right all along and the specs now say what it said.**

**Local verdict: DEFECTIVE — 0 BLOCKER · 5 MAJOR · 3 MINOR · 2 INFO. Census CLEAN (6 / 6 / 0). Zero inventions.**

**The head is flat for a third round, and the shape of what remains has changed.** Pass 5 found nine of ten defects in one class — *a figure true when written and false when the round ended*. Pass 6 finds that class **structurally beaten and locally unfinished**: the hash gate proves the write-set, the anchors-only posture measures zero coordinates, the numeric arm measures zero stamps, and the one instrument that could not be hashed — the **order** of the writes — is where the certificate fails again (**D6-4**), together with a headline census it disagrees with itself about (**D6-3**). Locally, **D6-1** and **D6-2** are the same one-inch gap: the file **declared** the per-sibling numerals retired and then **restated** them, and restatement is what has failed in four consecutive rounds. Subtraction is what has worked — it worked at D5-4, at D5-5, at D5-6 and at D5-7, in this very round, in this very file.

**Nothing here touches a row, a gate, a count, a bound or a disposition.** Every cure is editorial; four of nine land inside this file (**D6-1 · D6-2 · D6-6 · D6-7**), four at the RECONCILE seat and KF-W10 (**D6-3 · D6-4 · D6-5 · D6-8**), and one is a clock (**D6-9**). The census, the carry-row set, the gate tally, the ∅-column finding, every lock and every dissent are unchanged and re-verified.

---

*Pass-6 conformance seat, KF-W1. Sole write = this file. Nothing here stamps a verb, authorizes execution, or opens product source. The LAW E(2) re-hash of all eleven specs was this seat's first act and returned 11 of 11 matching. Every keyframes-side figure was re-derived read-only at `origin/master 81a56990` in `/Users/mkbabb/Programming/keyframes.js` and `/Users/mkbabb/Programming/keyframes-v-exec` before it was written; the sacred checkout was read (`git rev-parse`, `git rev-list`, `git status`, `git grep`, `git show`, `grep`, `sed`, `ls`) and never written, and no npm or mutating git ran there. `8281638c` was opened only as declared historical context, per R3-2. Prior-pass artifacts were read and never edited (E-3), which is why the corrections of record for `PASS-5/CLOSE-CERT-2.md` live here. This file enters §4c's census class H as its fourth member at its next reading, per that census's own self-reference disclosure.*
