# KF-W3 — FRESH ADVERSARIAL SPEC CHECK (L-18/L-20, PASS 6)

**Spec under trial**: `docs/tranches/X/keyframes/waves/KF-W3.md` — **187 109 B, 418 lines**, mtime **2026-08-29 15:17:38**,
sha256 `4b68baf35a63357d9af857c2c96e73022ecbdd7a3cecfd2a5374be70cde786f7`
(pass 1: 57 164 B / 232 · pass 2: 86 041 / 261 · pass 3: 101 473 / 281 · pass 4: 138 765 / 358 · pass 5: 168 078 / 409).
**Corpus authority**: the **58** `kf-*.md` records in `docs/tranches/V/megatranche/registry/adjudicated/`
(`ls kf-*.md | wc -l` → 58, re-run this seat). Sole in-tree carry: `docs/tranches/X/keyframes/carry/KF-W6-CARRY.md`.
**Method — BY RECORD, from zero.** No denominator, id list, partition or verdict was inherited from PASS-1/2/3/4/5.
Every command the spec prints beside a figure was **re-executed at this seat against final bytes** and the outputs
compared digit for digit. Where a receipt rides a *sibling's* bytes, the sibling's **final** bytes were the substrate.
**Seat instruments (read-only)**: `shasum`/`grep`/`sed`/`comm`/`stat` over the 58 records and the eleven wave specs;
`git show`/`git grep`/`git ls-tree`/`git cat-file` over keyframes.js at **`origin/master` `81a56990`**; `node` against
the pinned `node_modules/@mkbabb/value.js@4.0.0` packed artifact.
**Four executions**: the R-E probe unmodified · the census-1b packed-surface enumeration · the E-14.5 reconciliation
transcript · the G-KF3-3 arm-(c) roster re-derivation. **This file is the only write.**

**VERDICT: DEFECTIVE** — 4 MAJOR · 1 MINOR · 1 INFO. **Not census-fatal** (`escapedCount = 0`; carriage of the routed
24 is complete and id-for-id verifiable, re-derived here without inheritance). **Axes 1, 3 and 5 PASS unqualified.**
**Axis 4 PASSES.** **Axis 2 fails, and it fails in the same place it failed at PASS 5, one instance to the left**: the
round-5 seat cured the receipt a ruling *named* and left its twin (**D-1**); the sibling that writes LAST pasted that
twin's stale numeral *and* a stale sibling mtime into its own cell (**D-2**); R5-12(2)'s vehicle re-cut was applied to
the ten sites inside W10 and to none of W3's three (**D-3**); and LAW E(4)'s retirement of per-seat substrate stamping
— certified "16 of 16 disposed, the class is retired" — is falsified at the bytes in 7 of 11 specs (**D-4**).
**All five PASS-5 defects (D-1..D-5) are independently re-derived as CURED.**

---

## §0 LAW E(2) — THE HASH RE-CERTIFICATION, RUN AS THIS SEAT'S FIRST ACT

RULINGS-5 **LAW E(2)**: *"The pass-6 union seat re-hashes all 11 as its FIRST act; any mismatch is the round's own
conviction."* Run before anything else at this seat:

```
$ shasum -a 256 waves/KF-W*.md
fd226ecb…d09a  KF-W0.md     f6986de7…12f0  KF-W1.md     1c107329…1c50  KF-W10.md
5f3656e3…11a4  KF-W2.md     4b68baf3…86f7  KF-W3.md     4028327c…fe68  KF-W4.md
8baba077…404d  KF-W5.md     b41e5576…daaa  KF-W6.md     ed5f0851…6f21  KF-W7.md
c7c11445…a1c9  KF-W8.md     bea2d9a6…c744  KF-W9.md
```

**ELEVEN OF ELEVEN MATCH `PASS-5/CLOSE-CERT-2.md` §9's closing column, digit for digit. NO MISMATCH. The round's
write-order claim stands and CLOSE-CERT-5 is not convicted by its own instrument.** The mtimes at §6.1 reproduce too
(`KF-W3.md` 15:17:38 / 187 109 B; `KF-W7.md` 15:18:37 last-edited; `KF-W10.md` 15:18:25). **LAW E(1)/(2)/(3) is the
one instrument in this program that worked exactly as designed this round, and it is recorded first for that reason.**

**§0b TREE LAW.** `git rev-parse 81a56990` → commit, exact; `git log` names it *"docs(V·vnext): trim tape to
out-of-scope pointer per owner context ruling"*. Local HEAD `8281638c` with a **252-row dirty worktree** —
**DISQUALIFIED** as witness substrate and cited by this spec only in its lawful historical forms (§Header ref-of-record
*"1 ahead / 41 behind"*; C-7's `345/12/57 @ 8281638c` citation cap; E-10's X-1 routing-away). Every gate-load-bearing
anchor below was resolved at `81a56990` via `git show`/`git grep`/`git ls-tree`, never by opening a working-tree file.
**No conviction.**

---

## §1 CENSUS TOTALS — derived by record, from zero

| bucket | count |
|---|---|
| **routedTotal** (distinct registry row-ids routing to this wave) | **24** |
| **BOOKED** (§Carry fold-identity · §Excluded E-1 disposal · §Excluded E-15 exclusion-with-named-owner) | **24** |
| **ESCAPED** (no row, no fold-identity, no exclusion line) | **0** |

Carriage **24 / 24 = 100 %**.

**The four census figures the spec prints all reproduce exactly at this seat, 2026-08-29:**

```
ls kf-*.md | wc -l                                                  → 58
markup union, case-INSENSITIVE, 5 markups                           → 26
markup union, case-SENSITIVE                                        → 23
payload sweep  grep -l '\bR1\b' kf-*.md | wc -l                     → 41
```

**The three-record case delta is exactly what the spec names** — `comm -13` over the two unions returns
`kf-EasingScene.md` · `kf-EasingSidebar.md` · `kf-TimingFunctionPanel.md`, the three header routing-law sentences whose
capitals a case-sensitive grep misses. **The wider (26) reading is declared and taken.**

**The five markups re-derived from the law's own text** (E-15 (R-i) clause (2)): `KF\.W3` · `KF\.W2/W3` ·
`KF\.W3-SHIM` · `parser lane|parser wave|parser ingress|parser consumption` · `megatranche R1`. Per-markup record
counts at this seat: **14 · 7 · 5 · 17 · 8**, union **26**.

**Routed ids re-enumerated id-for-id (19 canonical + 5 SHIM = 24), every anchor opened by `sed -n`.**
*Nine fold-identities* — `kf-AmigaScene:150` · `kf-CSSCodeEditor:46` · `kf-KeyframeTimeline:48` ·
`kf-KeyframesStringControls:63` · `kf-KeyframeCardList:122` · `kf-CopyButton:81` · `kf-StartingStyleTarget:80` ·
`kf-KeyframeCard:101` · `kf-EasingTarget:77` — **1 + 4 + 3 + 1 = 9**, matching §Carry's own per-row arithmetic
(the round-5 D-4 cure: the struck range *"C-1..C-5"* is replaced by the enumeration, and `grep -c 'C-1\.\.C-5'
KF-W3.md` → **2**, both naming it as the struck historical form; `grep -l` across the eleven specs → this file only).
*Ten E-15 exclusions-with-named-owner* — `kf-App:111` · `kf-EditorShell:77` · `kf-CubeAxisLines:108` ·
`kf-KeyframesEditor:99` · `kf-TimelineTrack:130` · `kf-ChannelOptions:126` · `kf-ChannelControls:126` ·
`kf-CubeTarget:76` · `kf-DemoGlobalChrome:77` · `kf-SquareScene:132`.
*Five SHIM rows* — `kf-ChannelControls:79` · `kf-AnimationControlsGroup:74` · `kf-KfPillTabs:58` · `kf-KfPillTabs:70` ·
`kf-DemoGlobalChrome:70`. **19 + 5 = 24.**

**The SHIM cardinality re-derived independently.** `grep -n 'KF\.W3-SHIM' kf-*.md` → **12 hits in 5 records**;
subtract the five header taxonomy sentences (`kf-AnimationControlsGroup:17` · `kf-ChannelControls:17` ·
`kf-ControlsPaneWrapper:6` · `kf-DemoGlobalChrome:17` · `kf-KfPillTabs:19`), the one in-row cross-reference to the law
(`kf-AnimationControlsGroup:39`) and the one terminal-manifest restatement (`kf-KfPillTabs:122`) → **12 − 7 = 5 rows
across 4 records**. `kf-ControlsPaneWrapper` books no shim row at all, exactly as C-6 and E-1 state.

**Escape hunt, run independently of the spec's tables, over all 26 markup-positive records.** Every markup-positive
line was enumerated and classified. The unbooked residue is **15 header/routing-law taxonomy sentences · 7 terminal
ADJUDICATED manifests · 1 in-row cross-reference (`kf-AnimationControlsGroup:39`) · 1 retired-residue budget register
(`kf-KeyframesStringControls:161`) · 4 fold-restatements inside already-booked records (`kf-CSSCodeEditor:134` — its
own bytes read *"no new row"* · `kf-KeyframeTimeline:112` · `kf-SquareScene:125` · `kf-StartingStyleTarget:139`) ·
2 §Carry C-5 witness lines (`kf-SquareScene:53`/`:125`, carried-but-not-counted and declared so) · the 5
discarded-by-reading lines (§7 D-6)**. **Not one is an adjudicated row routing here that lacks a booking.
ESCAPED = 0, confirmed by construction rather than by trusting the spec's arithmetic.**

**The E-15 arithmetic closes, counted by command.** `grep -c '^| \*\*X-[0-9][0-9]\*\*'` → **12** (X-06…X-17, printed in
order); sub-line I → **8**; sub-line II → **6**. **12 + 8 + 6 = 26** ids booked at E-15, plus `KF-HA-19` at E-14.5 →
**denominator 27**, against RULINGS-4's ruled floor of ≥ 26. The map's resolution re-adds: 21 UNREACHABLE (7 + 8 + 6) ·
3 REACHABLE (X-07 · X-09 · X-11) · 1 REACHABLE-but-correct (X-15) · 1 posture (X-17) · 1 DECLINED-FRAMING (KF-HA-19)
= **27**. **All 26 E-15 anchors opened and verified at their cited lines this seat; both arithmetics close.**

---

## §2 AXES

| axis | result |
|---|---|
| **1 · ID-KEYED CENSUS** | **PASS.** 24 routed / 24 booked / 0 escaped, re-derived by record from zero for the second pass running. The two-clause membership law is consistent and honest about its reading step; the five discards are named; both surplus sub-lines are on the page with every anchor resolving. |
| **2 · RECEIPT REALITY** | **FAIL (MAJOR ×4).** Every *frontier* anchor holds exactly and every *structural* cross-wave anchor resolves. But a receipt this file takes over its **own** bytes is stale at its own final bytes (**D-1**); the sibling that writes LAST pasted that stale numeral plus a stale sibling mtime (**D-2**); R5-12(2)'s vehicle re-cut skipped this file entirely (**D-3**); and LAW E(4)'s retired stamp class is alive in 7 of 11 specs (**D-4**). |
| **3 · M-25 DEPTH + MECHANISM S + LAW-A CENSUSES** | **PASS — unqualified.** All five LAW-A censuses, census 1b, the R-E probe, the E-14.5 reconciliation and the G-KF3-3 roster reproduce exactly. Mechanism S is satisfied *within this file*: every round-5 act carrying a strike verb carries its SHADOW line, and the shadows are true (§4.3). |
| **4 · GATES** | **PASS.** Seven close gates born-RED with executed baselines; both previously-unpassable oracles reachable by declared inbound ask; LAW B honoured at the gates; no gate re-derives its own oracle. |
| **5 · POSTURE** | **PASS.** W4 head · W3 gated · O-21 clean · §6.D successor register two-ended · the EH-4/5/8 restoration verified at TRUE frontier anchors. |

---

## §3 AXIS 2 — anchors (PASS) and receipts (FAIL)

### 3.1 Frontier anchors — every one re-measured at `81a56990`, all hold

`css-text.ts` `:17 export const reverseAnimationShorthand = (` · `:41 export const serializeCssValue = (value:
CssValue): string => {` · `:54 if (!serialized.ok) throw new TypeError("Value returned an unserializable CSS color.");`
— **exact**.
`package.json` `:70 "@mkbabb/value.js": "4.0.0"` · `:77 "@mkbabb/glass-ui": "7.0.0",` — **exact**.
`src/animation/constants/types.ts` `:193-196` = `Easing` · `TimingFunctionNames` · **`| string`** · `undefined`;
**`:195` is `| string`** — the round-3 D-4 path pin holds.
`easing/registry.ts` `:26 "smoothStep3",` · `:27 "easeInBounce",` · `:31 ...Object.keys(bezierPresets),` ·
`:131 const parsed = parseTimingFunction(timingFunction);` — **exact**; `git ls-tree` over
`src/animation/compile/easing/` returns **`index.ts` · `option.ts` · `registry.ts` only — no `easing-registry.ts`**,
confirming §B.1's *"ALL THREE DEAD"* row.
`demo/components/CopyButton.vue:42 timingFunction: "easeInBounce",` — **exact**.
`format/options.ts` `:20 reverseAnimationShorthand,` · **`:80`** *"K.W10 CC-1 — the per-child `animation` SHORTHAND
emit via value.js's"* · `:121 return reverseAnimationShorthand(cssOptions);` — the D-4 partition's load-bearing
justification confirmed to the byte.

### 3.2 Structural cross-wave anchors — all resolve, and the seam is two-ended

`grep -o 'KF-W[0-9]*\.md[:0-9]*' KF-W3.md` → **12 occurrences, all `KF-W3.md`** — self-references only; **this file
emits ZERO sibling coordinates**, so §B.6's LAW-C claim is true at final bytes and the file prints no numeral for it
(LAW E(4)-compliant). All three forward references are anchor-only per LAW C(3) and all three resolve at `KF-W10.md`'s
CURRENT bytes: `§6.C-I` (live at `:569`), `§6.D · SUCCESSOR-FORMATION REGISTER` (live at `:573`), and the reciprocal —
`KF-W10.md:529` names ***"KF-W3 §Excluded, `E-14.5`"*** as inbound row 2.
`grep -c 'PASS-5/CLOSE-CERT-2.md' KF-W3.md` → **1**; `grep -c 'PASS-5/CLOSE-CERT.md'` → **0** — R5-R6's cert-path
re-point landed here exactly as CLOSE-CERT-2 §3 reports.
**The class-(iii) receipt a sibling takes over THIS file's bytes and gets RIGHT**: G-4's never-cite field —
`grep -ohE '357/414|86\.23|0/5 slots'` → W0 **2** · W3 **2** · W10 **23** = **27 occurrences / 18 lines**, reproducing
W10 `:439` digit for digit. **This is the control that proves the two failures below are not noise: the same seat, in
the same file, re-ran one cross-spec command correctly and pasted another from memory.**

### 3.3 Where it fails

**This file's forward receipts survive because they carry no bytes and no numbers.** The one receipt it carries with a
number over its *own* bytes is stale (**D-1**), and the one sibling cell that quotes this file *with* figures pasted
the pre-repair pair (**D-2**). Both are the identical mechanism PASS-5 D-1 convicted; the cure was applied at the
instance RULINGS-5 §END named and nowhere else.

---

## §4 AXIS 3 — M-25 depth, mechanism S, and the LAW-A censuses (PASS, unqualified)

### 4.1 The five LAW-A censuses

**Census 1 · `serializeCssValue`.** Re-run at `81a56990`: **15 files carry the symbol, 14 of them importers** —
`src/` **8** (`emit/densify.ts:13` · `emit/entry.ts:37` · `emit/format/format.ts:1` · `frame/interp-slot.ts:19` ·
`engine/css/metadata.ts:33` · `resolve/browser.ts:4` · `resolve/conditional.ts:2` · `resolve/function.ts:6`) ·
`demo/` **1** (`timelineEngine.ts:17`, `@src/…/css-text`, called `:88` — the alias-root consumer invisible to any
`src/`-scoped grep) · `test/` **5**. Plus the five in-file uses at `css-text.ts` (`:43 :48` recursive · `:66 :83 :89`
callers). **Exact, path for path.**

**Census 1b · the routing target, EXECUTED.** Against `node_modules/@mkbabb/value.js` → `4.0.0`:
`color 23 · value 1 · css 19 · easing 16 · math 9 · transform 9 · quantize 2` = **79 exports**, and
`serializeCssValue`/`reverseAnimationShorthand` are on **none**. **The routing target genuinely does not exist at the
pinned coordinate** — G-KF3-4's re-cut into a declared inbound ask is the correct cure.

**Census 2 · `reverseAnimationShorthand`.** One implementation (`css-text.ts:17`), **exactly one live import
specifier** (`format/options.ts:20` ← `../css-text`), call `:121`; everything else prose at `backward.ts:20 :46 :252
:285` · `options.ts:10 :81` · `index.ts:232` · `compile-roundtrip.test.ts:23` — **eight prose sites, exact**. The
**5 provider-naming / 3 provider-silent** partition is verified at the bytes (§3.1, `:80`). **The gate states no
numeral and points at the enumeration.**

**Census 3 · `cssIdent`.** Two re-export sites — `emit/index.ts:53` (`from "./backward"`) **and**
`emit/backward/index.ts:29` (`from "./walk"`) — definition `walk.ts:145`. **Exact.**

**Censuses 4 + 5** (the two carried shims, LAW A binding the carrier) are present at §B.5 and their acts are routed to
KF.W6, not re-homed — verified against E-1's riders.

**The D-5 relocation rests on a re-run command, not on prose.**
`git show 81a56990:src/animation/compile/emit/index.ts | grep -nE
'serializeCssValue|reverseAnimationShorthand|css-text'` → **no output, exit 1**, this seat. The barrel re-exports
neither forked symbol, so neither delete forces an edit there.

### 4.2 The executed baselines

**G-KF3-2 — RED BASELINE EXECUTED.** `node docs/tranches/V/megatranche/audit/probes/r1-published-totality.mjs`
(3 476 B, unmodified — R-E):

```
RED  parseCssColor  102/172   RED  parseCssScalar 102/172
RED  parseCssValue   60/172   RED  parseCssValues  60/172
ok   parseKeyframeSelector · parseStylesheet · parseTimingFunction
ok   parseAnimationTimeline · parseAnimationRange     (0/172 each)
TOTAL 324 throws / 1548 calls · DISTINCT FAILURE MODES: 1
  324x  TypeError: Cannot read properties of undefined (reading 'replace')
```

**Exact to the digit**, one mode, matching `src/css/grammar.ts:181`.

**E-14.5's reconciliation — re-executed against the packed artifact, line for line:**

```
parseCssColor("oklch()")                          THROW TypeError: …(reading 'replace')
parseCssColor("oklch(0.7 0.1 200)")               ok=true
parseCssColor("oklch(70% 0.1 200 / 50%)")         ok=true
parseCssColor("rgb()"/"lab()"/"color()"/"foo()")  THROW (same single mode)
parseCssColor("var(--x)")                         ok=false
parseCssColor("color-mix(in srgb, red, blue)")    ok=false
```

**Reproduces exactly.** Both records are true and they do not conflict, precisely as the spec argues.

**G-KF3-3 arm (c) — the roster RE-DERIVED by `git cat-file -e`**: all **12 enumerated paths exist at the frontier**, no
more, no fewer. Arm (a): `test/fixtures/keyframes/` → **15 objects = 14 `.css` + `manifest.json`** ✓. Arm (b): both
`scroll-driven.css` and `multi-color-scroll.css` present ✓.

**KF-AV-28 · corpus depth.** The id occurs in exactly **3 of 58** records (`kf-AnimationVisualizer` ·
`kf-PlaybackRibbon` · `kf-SequenceScrubber`) — the spec's 3-of-58 reading holds.
**The six sub-line-II negatives — every claim tested.** All six banked quotations verify at their cited lines
(`kf-MbabbMenu:93` · `kf-SequenceScene:119` · `kf-SpringHeatmap:101` · `kf-MatrixEditor:139` · `kf-EditorHeader:95` ·
`kf-AnimationVisualizer:124`), and **all six measure 0 on all five routing markups, per record, this seat** — so
`routedTotal` is genuinely untouched. All eight sub-line-I anchors likewise resolve.

### 4.3 Mechanism S (LAW F) — the shadows this file books are TRUE

Four strikes are declared this round and **each carries its SHADOW line in the same edit**, as LAW F(1) requires:
(1) the (R-i) five-position figure; (2) its §B.6 NUMERIC-ARM mirror; (3) the fold-identity provenance range
*"C-1..C-5"*; (4) the *"freshest artifact"* superlative. **Every shadow's disposition claim was tested and holds**:
`grep -c 'C-1\.\.C-5' KF-W3.md` → **2**, both the struck historical form; `grep -l 'C-1\.\.C-5' waves/KF-W*.md` → this
file only, so *"citing/terminus waves notified = none"* is TRUE; **C-4 and C-5 keep their §Carry rows, verbs,
severities and riders untouched** (verified at `:206`/`:207`); *"Minted this round: nothing"* is TRUE — routedTotal
**24**, `9 + 5 + 10`, the five SHIM rows, E-15's `26 + KF-HA-19 = 27`, every §Bounds row and access word and every
gate's RED baseline are unchanged in substance from their round-4 state. **LAW F(3) — per-row anchors, never
per-carrier — is honoured: no strike here rests on a co-id twin or a host file.** **Mechanism S PASSES inside this
file.** *(Its failure is at the round's boundary, not this file's: see D-3, where a sibling's strike carried no
citing-sibling sweep and this file is the un-notified citer.)*

---

## §5 AXIS 4 — gates (PASS)

**G-KF3-1 — RED, all six conjuncts FALSE, re-measured this seat**: `package.json` → `4.0.0` ·
`RELEASE-CONDITION.md`/`RELEASE-PACKET.md` → *No such file* · `grep -c 'RC-P' INBOX.md` → **0** ·
`grep -rn 'RC-P' docs/tranches/X/fourier/` → **0** over **eleven** specs (`ls waves/` → `F-W0 … F-W10` — the round-2
off-by-one repair confirmed) · `ls docs/tranches/X/keyframes/evidence/` → *No such file or directory*.
**G-KF3-2 / G-KF3-3** — executed at §4.2; both RED baselines reproduce.
**G-KF3-4 / G-KF3-5** — both previously-unpassable oracles are reachable: each names its unpublished symbol as a
**declared conjunct** carried on the X-W9 cross-edge (measured absent at 4.0.0 by census 1b), so each can now fail for
its intended reason. **No gate re-derives its own oracle (C-7 clause d)** — verified by reading all seven falsifiers.
**G-KF3-6** — `lane-docs.md:373`'s *"DECLARED; no keyframes acknowledgement on record"* is the live RED, cited at
`:249` with its verbatim cell.
**G-KF3-8 / G-KF3-9** — correctly re-classed **FLOOR**, numbers retained for citation stability; G-KF3-9's
*"This wave may NOT author that gate"* occurs **3×**, so the W4 head is stated where it binds.
**LAW B at the gates — honoured.** G-KF3-7's completeness is routed to the conformance artifact, not claimed; E-15's
carriage sentence likewise. **The artifact-of-record class is now swept by SCOPE**: `grep -on
'PASS-[0-9]/KF-W3-CHECK\.md' KF-W3.md` → **16 hits on 8 lines — 9 `PASS-5` · 5 `PASS-3` · 2 `PASS-4`**, reproducing
§B.6's printed figure exactly, with every `PASS-3`/`PASS-4` hit a class-(ii)/(iii) history cite as the discriminator
requires. **PASS-5 D-3 is CURED.**

---

## §6 AXIS 5 — posture (PASS)

| clause | result |
|---|---|
| **W3 gated-unscheduled** | **PASS.** §-head *"(GATED, never scheduled)"*; Status **planned**; opening condition verbatim and by predicate name; **L-1** *"gate-keyed and NEVER scheduled"*. |
| **W4 head honored** | **PASS.** E-3/E-4 route the easing commit and the gate chassis to KF.W4 (3 `→ KF.W4` routings); **L-3 · KF.W4 BEFORE THE REPIN** is stated as *"Ordering, not advice"*; G-KF3-9 forbids authoring the chassis gate. |
| **O-21** | **PASS.** W3's O-references measured at final bytes: `O-11` ×7, `O-8` ×2 — **it mints no O-row**; `grep -c 'O-21'` → **0**. INBOX's live ceiling is **O-20** and `grep -c 'O-21' INBOX.md` → **0**. W3 makes no claim and there is no collision. *(The ×7/×2 readings differ from PASS-5's ×9/×1 because the file grew 19 KB this round — and the spec prints no numeral for them, which is LAW E(4) working.)* |
| **§6.D successor register, both ends** | **PASS.** W3's **E-8** records `KF.W11 · KF.W12 · KF.W13` as **MINTED — deliberately, by KF-W4's R-15 homing block** with verb **MINTED-UNAUTHORED** and terminal disposition `KF-W10 §6.D`, **anchor-only under LAW C(3)**. At the far end `KF-W10.md:573` carries **§6.D · SUCCESSOR-FORMATION REGISTER** with all three rows. `ls waves/KF-W1[123].md` → *no matches*: no spec exists at any of the three coordinates. |
| **COHESION §0d** | **LANDED and verified** — `COHESION.md:185` is *"§0d ADDENDUM 2026-08-28 (later still) — THE MINTED-UNAUTHORED SUCCESSOR WAVES, DECLARED AT THE BOUNDARY"*, carrying the KF.W11/W12/W13 boundary line at `:188`. R5-12 clause 4 is genuinely DISCHARGED. **W10 cites it; W3's own §0c vehicle did not travel — booked at D-3, not here.** |
| **EH-4 / EH-5 / EH-8 restoration** | **PASS — the anchors are TRUE, re-derived at `81a56990` by this seat.** `EditorShell.vue:45` = `title="Toggle dark mode"` (EH-4); `:46` = `class="aspect-square w-8 scale-on-hover"` (EH-5); the shortcuts `Button`'s sizing utility at `:36` and the DarkModeToggle cluster `:44-47` (EH-8); the third instance at **`demo/app/dock/MbabbMenu.vue:20`/`:21`** (`w-5` variant), path confirmed by `git ls-tree`. `KF-W6.md` carries **18 `EH-4` tokens** across §Excluded, §Bounds and gate rows and **5 SHADOW lines**. **R5-1's central act — making the spec say what the record said — is real at the bytes.** |

---

## §7 DEFECTS

### D-1 · MAJOR · The anti-drift note's own figure is stale at this file's own final bytes — the receipt written to prevent this exact drift is the one that drifted

§Cross-edges' KF.W10 row (`:310`) closes with:

> *"**Anti-drift note for the RECONCILE seat**: adding this row raises `grep -c 'KF-HA-19' KF-W3.md` from the **4**
> that RULINGS-4 **R4-7 D-2** pre-printed for W10's edge-2 cell — a figure measured *before* this repair — to **10** at
> this file's stage-1 bytes (**re-run at write time, 2026-08-28**)."*

Re-run at this file's final bytes, this seat:

```
$ grep -c 'KF-HA-19' KF-W3.md          → 12      (asserted: 10)
$ grep -o 'KF-HA-19' KF-W3.md | wc -l  → 18
```

**The mechanism is this round's own repair.** The `10` is the **round-4** stage-1 reading (it is exactly what PASS-5
§3.2 measured on the 168 078-byte round-4 file). The round-5 stage-1 write at **18:50:14** added two further
`KF-HA-19`-bearing lines — **`:193`, the LAW F round-5 shadow ledger** (*"E-15's `26 + KF-HA-19 = 27` denominator"*)
and **`:338`, the R5-13 emphasis disclosure** — and the figure beside the command was not re-run. Lines carrying the id
at final bytes: `:193 :256 :310 :333 :335 :337 :338 :339 :349 :409 :413 :415`.

**Three things make this the round's sharpest miss.** **(a)** It is **LAW D(3)'s numeric arm** failing under an
explicit *"re-run at write time"* label — the identical shape PASS-5 **D-1** convicted at E-15's (R-i) clause.
**(b)** It violates **this file's own standing rule, minted this round, three sections earlier**: §B.6's R5-11 bullet
(`:189`) reads *"a figure over this file's own bytes is re-run at the seat's last edit or it is not printed."*
**(c)** It is **R5-11 / R4-10(1)'s instance-not-scope class, sixth consecutive round**: the seat cured the five-position
receipt a ruling named and did not sweep the class the ruling defined — and there were only **two** in-file
self-receipts to sweep.

**The substance survives**: E-14.5 is still the booking, the seam is still two-ended, `routedTotal` is untouched.
**The receipt does not.** The cure is one re-run: state **12 matching lines / 18 occurrences** and name the two
round-5 lines that moved it — or, per LAW E(4), drop the numeral and keep the anchor.

*Receipt*: `grep -c 'KF-HA-19' KF-W3.md` → `12`; `grep -o … | wc -l` → `18`; `sed -n '193p;338p'` shows both lines
citing RULINGS-5 / R5-13, i.e. round-5 additions.

### D-2 · MAJOR · W10's edge-2 cell pastes this file's PRE-REPAIR figures and a PRE-REPAIR mtime — a cross-wave numeral LAW E(4) forbids outright, written by the seat that writes LAST

`KF-W10.md:529` — INBOUND-DEPENDENCY row 2, the reciprocal of the seam W3 declares — carries:

> *"mtime **16:56:52**; `grep -c 'KF-HA-19' KF-W3.md` → **10 matching lines / 15 occurrences**"*

Measured against `KF-W3.md`'s FINAL bytes, this seat:

```
KF-W3.md  mtime 2026-08-29 15:17:38   187,109 B   (asserted: 16:56:52 = the ROUND-4 mtime, 168,078 B)
grep -c 'KF-HA-19' KF-W3.md          → 12         (asserted: 10)
grep -o 'KF-HA-19' KF-W3.md | wc -l  → 18         (asserted: 15)
```

**All three are the round-4 state.** Per CLOSE-CERT-2 §6.1's own write-order, **W3 wrote at 18:50:14 and W10 at
19:30:41 — forty minutes later**, and W10 writes LAST of the per-wave seats *precisely so it can measure final bytes*.

**This is a compound violation.** **(i)** LAW E(4) retires per-seat cross-wave receipts to *anchor-only*: *"no quoted
sibling prose, no line numbers, **no per-seat mtime stamps**"* — this cell carries a sibling mtime, two sibling
figures and a quoted sibling heading. **(ii)** All three are false at the target's final bytes. **(iii)** The
falsification is **causally downstream of D-1**: W3's anti-drift note exists to hand W10 the post-repair figure, and
it handed it the pre-repair one.

**And the sweep that was built to catch this looked straight at it.** CLOSE-CERT-2 §1 books class (iii) —
*stage-2 → stage-1, backward* — as **418 tokens "swept whole; 1 defect found"**, and §2 records W10 as **238 receipts
checked / 20 fixed**, the most-worked file in the round, edited twice at this very seat (R5-R4, R5-R5). **A pasted
sibling numeral in an INBOUND table row is the most legible member of that class, and it survived the sweep.**

*Receipt*: `sed -n '529p' KF-W10.md` carries the quoted string; `stat -f '%Sm %z' waves/KF-W3.md` →
`2026-08-29 15:17:38 187109`; RULINGS-5 LAW E(3)'s opening table pins `16:56:52 / 168,078` as W3's **round-4** state.

### D-3 · MAJOR · R5-12(2)'s COHESION-vehicle re-cut was applied to W10's ten sites and to none of W3's three — and the failure mode the ruling names is live here

R5-12(2) ruled the ownership vehicle re-cut because *"a resolving seat can no longer find a landed-but-unrelated
section and read the precondition satisfied"* — `§0c` and `§0d` are **occupied**. It enumerated **five sites, every one
inside `KF-W10.md`**. W10 executed it thoroughly: `grep -c 'mirroring §0b\|§0e or a successor letter'` → **7**, the
strike declared at `:5`, `:7`, `:70`, `:553`, `:639`, `:646`, with the reason spelled out.

**`KF-W3.md` carries three `§0c` sites and ZERO instances of the ruled replacement form:**

- **`:311`** — the cross-edge **row header** reads ***"COHESION §0b (this wave) / §0c (KF.W10)"***, and its body:
  *"RULINGS **R-18** recommends **SS-2** and names the cure a **COHESION §0c addendum — the root session's act,
  flagged upward**"*.
- **`:41`** — OP-3: *"…§0c addendum — the root session's act**. This row cites §0c and books nothing."*
- **`:310`** — *"The COHESION §0b/§0c row below is NOT this edge"*.

At final bytes `COHESION.md:170` **is** `§0c`, carrying ***"X·V REFINEMENT FOLD CONFORMANT; THE VALUE.JS SPEC LAYER IS
DEVELOP-COMPLETE"*** — a landed section with an unrelated subject. **A seat resolving KF.W10's ownership gap from W3's
routing opens §0c, finds it landed, and reads the precondition satisfied — which is verbatim the outcome R5-12(2)
exists to prevent.**

**The owning failure is LAW F(2), not W3's seat.** W3 had no §END directive for this. LAW F(2) puts the duty on
RECONCILE: *"re-sweeps **every named terminus and every citing sibling** for every listed id before close."* W10's own
round-5 SHADOW ledger (`:639`) books the item as ***"RE-POINTED: the ownership vehicle at **ten** sites"*** — ten being
its own — and names **no citing sibling** for it. CLOSE-CERT-2 §2 records W3's entire round-5 fix set as
*"cert-path ×1"*. **The class was never swept across the file boundary.** One edit at W3 `:311` (+ `:41`) cures it.

*Receipt*: `grep -c '§0c' KF-W3.md` → **3**; `grep -c 'mirroring §0b\|§0e or a successor letter\|§0c and §0d are
occupied' KF-W3.md` → **0** vs `KF-W10.md` → **7**; `grep -n '^## §0' COHESION.md` → `:170` is §0c with the X·V subject.

### D-4 · MINOR-to-MAJOR (booked MAJOR) · LAW E(4)'s retired class is alive in 7 of 11 specs, and CLOSE-CERT-2 certifies it dead

LAW E(4): *"Per-seat substrate stamping — the mechanism behind 9 of the drift tail's 16 rows — **is retired**."*
CLOSE-CERT-2 §5 closes: *"**16 of 16 disposed. The class is retired**: per-seat substrate stamping ends with this
round (LAW E(4)), and **these are its terminal members**."*

Measured at final bytes, this seat — surviving `HH:MM:SS` per-seat stamps:

```
$ grep -lE '\b1[5-9]:[0-5][0-9]:[0-5][0-9]\b' waves/KF-W*.md
  KF-W1.md  KF-W2.md  KF-W5.md  KF-W7.md  KF-W8.md  KF-W9.md  KF-W10.md      ← 7 of 11
$ grep -n '16:56:52' waves/KF-W*.md                      ← THIS file's ROUND-4 mtime
  KF-W10.md:145 · KF-W10.md:529 · KF-W2.md:23 · KF-W8.md:24                  ← 4 sibling sites
```

**The distinction the cert should have drawn, and did not.** `KF-W8.md:24` is **correctly cured**: its ledger header
reads *"⟨**dated round-3/4 reading; struck as a live claim at round 5 — stamps live at `PASS-5/CLOSE-CERT-2.md`**⟩"* —
R5-9 row 15 landed, no conviction. But:

- **`KF-W10.md:140-146`** is a live table headed ***"mtime at this seat's read"***, every row stamped *"1 (final)"*,
  the policy column reading ***"quoted by command, prose + figures"*** — no struck label, no dated qualifier. Its
  `KF-W3.md` row asserts **`2026-08-28 16:56:52` · 1 (final)**; the true final is **2026-08-29 15:17:38**. Five sibling
  stamps in one table, all falsified.
- **`KF-W2.md:23`** — *"substrate `KF-W3.md`, mtime **2026-08-28 15:43:45**"* — a live stamp with no qualifier, and W2
  *was* directed at D-7 to convert *"the two RE-VERIFIED stamps → anchors; stamps live at CLOSE-CERT-5."* It converted
  two and left this one. **Instance-not-scope, again.**

**This is the round's central mechanism failing a fourth consecutive time** — round 4's twelfth write (PASS-5 D-2),
round 5's predecessor reconcile stamp (CLOSE-CERT-2 §3, *"a third time"*), and now the retirement itself, certified
complete while 7 of 11 specs carry the class. **The stamps' subjects are unaffected** — every anchor they accompany
resolves (§3.2) — which is exactly LAW E(4)'s point and exactly why the cure is deletion, not refreshment.

### D-5 · MINOR · CLOSE-CERT-2's drift row 6 reports the artifact-of-record class at the wrong denominator

CLOSE-CERT-2 §5 row 6 books W3's D-3 as *"**CURED** — one artifact (`PASS-5/KF-W3-CHECK`, **16 sites**)."*
Re-run this seat: `grep -on 'PASS-[0-9]/KF-W3-CHECK\.md' KF-W3.md` → **16 hits on 8 lines, of which 9 are `PASS-5`,
5 are `PASS-3` and 2 are `PASS-4`.** The **16** is the size of the whole greppable class, not the count of
artifact-of-record cites; the seven non-PASS-5 hits are precisely the class-(ii)/(iii) history cites that §B.6's
discriminator says must **never** be re-pointed. **The cure itself is sound and W3's own §B.6 states the split
correctly** — the cert's summary is the only place the two numbers are conflated. Cite-level; no claim moves.

### D-6 · INFO · The five markup-positive discards are separated by a LINE-level reading under a RECORD-level clause, and the discriminator is not stated at the receipt

E-15's clause (2) is explicitly a **record**-level test (*"run per record against the 58 records, returns ≥ 1"*), and
the spec itself says clause (2) *"cannot enumerate"*. The five discards are then argued at the **line** level. Two of
them deserve the discriminator in writing: `kf-EditorStartScreen.md:113` and `kf-TypingDots.md:89` each **open** with a
component-exposure clause — *"the one string that crosses into value.js was **EXECUTED** against the installed
4.0.0"* / *"the one string crossing the package boundary checked against the installed `@mkbabb/value.js@4.0.0` dist"*
— and each sits in a **`## Superlatives`** section, which is where **five of sub-line II's six** bookings live
(`kf-SequenceScene:119` · `kf-SpringHeatmap:101` · `kf-MatrixEditor:139` · `kf-EditorHeader:95` ·
`kf-AnimationVisualizer:124`).

**This seat sustains the discard on the merits**: each line's *payload* is a **WAAPI/CSS-twin** finding — the R1 clause
is the read-only-budget justification, not the finding — whereas all six sub-line-II members name R1 *as the subject*
(*"R1-clean"* · *"R1 is structurally unreachable"* · *"DECLINED an R1 parser-crash claim"*). **No escape; routedTotal
stays 24.** Recorded because the reading is load-bearing, is performed at a register the law does not name, and is one
sentence away from being checkable: the receipt should state *"a superlative whose payload is not an R1 finding — the
R1 clause is budget justification"* rather than leaving the pair to be told apart by ear.

---

## §8 PASS-5 CARRY-FORWARD — all five re-derived as CURED

| PASS-5 | status at PASS 6 | evidence |
|---|---|---|
| **D-1** ((R-i)'s five-position receipt falsified in all five positions) | **CURED** | `for n in EasingSidebar EasingScene TimingFunctionPanel EditorStartScreen TypingDots; do grep -c "$n" KF-W3.md; done` → **3 · 1 · 3 · 1 · 1**, exactly as re-stated; the by-content enumeration resolves — `:355` (this sentence, all five) · `:105` (§B.3 tail) · `:250` (G-KF3-6's fence), and (b)/(c) each name the `TimingFunctionPanel`/`EasingSidebar` blast-radius **pair**, curing the class error too. SHADOW line present and true. |
| **D-2** (closure conditioned on the falsified CLOSE-CERT-4) | **CURED** | §B.6's LAW E block re-points to `PASS-5/CLOSE-CERT-2.md` + R5-2; `grep -c 'PASS-5/CLOSE-CERT-2.md'` → **1**, `grep -c 'PASS-5/CLOSE-CERT.md'` → **0**. **And the successor instrument was re-run: all eleven sha256 match (§0).** |
| **D-3** (two artifacts of record for one carriage claim) | **CURED, and swept by SCOPE** | `grep -on 'PASS-[0-9]/KF-W3-CHECK\.md'` → 16/8 with the **9 · 5 · 2** split; six cites re-pointed with the discriminator stated once and applied to the class; the greppable class named at §B.6. *(The cert's summary of it is D-5 — a defect against the report, not the cure.)* |
| **D-4** (fold-identity provenance range `C-1..C-5`) | **CURED** | §Provenance now enumerates **1 + 4 + 3 + 1 = 9** id for id, identical to §Carry's arithmetic; the range is struck with a SHADOW; `grep -c 'C-1\.\.C-5'` → **2**, both the struck historical form; C-4/C-5 keep their rows untouched. |
| **D-5** (undisclosed emphasis in a *verbatim* quotation) | **CURED** | §B.6's R5-13 bullet discloses over the class and states the **measured** basis: `sed -n '66p' kf-HeroAurora.md \| cut -d'\|' -f4 \| grep -o '\*\*' \| wc -l` → **0**, re-run here and reproducing; the three sites are itemised at E-14.5. |

**New at this pass**: **D-1** and **D-2** are the same receipt seen from its two ends, **D-3** and **D-4** are the same
sweep failing at the file boundary, and all four are one class — **a cure applied to the instances a ruling names
rather than to the scope it defines.** That class has now been the round's principal finding for **six consecutive
passes** (R4-10(1) → R5-11's *"fifth consecutive partial"* → here). Round 5 minted **LAW E**, **LAW F** and
**R5-11** against it, wrote them into every seat's directive, and then reproduced it four times — twice inside the very
file that states the rule three sections earlier, and twice across a boundary the new laws explicitly cover
(LAW E(4)'s stamp retirement, LAW F(2)'s citing-sibling re-sweep). **The laws are correct; the enumeration step that
applies them is what is missing, and it is missing at the RECONCILE seat, whose 866-receipt sweep is scoped by
*artifact* and not by *class*.**

**What did work, recorded so the next round keeps it**: the **hash bracket** (LAW E(1)/(2)/(3)) is the one instrument
that closed clean — eleven of eleven, no mismatch, no argument. Every failure above is a figure or a stamp that a
hash would have made unnecessary.

---

*Seat: fresh L-18/L-20 adversarial, PASS 6, 2026-08-29. LAW E(2) hash re-certification run as the first act: 11/11
MATCH. Frontier `81a56990`, re-verified; local `8281638c` (252 dirty rows) disqualified. Four executions (R-E probe
unmodified · census-1b packed surface · the E-14.5 reconciliation · the G-KF3-3 roster). Census derived by record from
the 58 adjudicated bank files; no partition, denominator or verdict inherited from any prior pass. No product source
opened for writing. This file is the only write.*
