# X·KF CONFORMANCE PASS 3 — UNION ADJUDICATION (post-repair-2)

**Seat**: union adjudication (cross-wave axes the per-wave seats cannot see) · 2026-08-28
**Inputs**: the 11 PASS-3 per-wave check files in this directory (all read whole) · `PASS-2/UNION.md` + `PASS-2/RULINGS-2.md` (the baseline and the 17-ruling repair contract) · the 11 wave specs at `docs/tranches/X/keyframes/waves/` · `carry/KF-W6-CARRY.md` · seat receipts KF-W0..KF-W10.
**Instruments**: the checks' own receipts, plus union-level greps re-executed this seat — the escaped-id sweep across all 11 specs with word-boundary discipline (`KAD-12`→W6 · `LP-8`/`KF-CO-35`→W6 · `KF-ES-3`→W9 trigger register + W4 fold row · `KF-HA-13`→W6+W10 · `KF-CO-48`→W5 B-15/G-OPTSET, fully booked · `KC-17`/`L-EST-12`/`SUP-D`/`countable-cell`/`useAmigaDemo` → **0 program-wide** · the `m-8` hits at W6/W7 verified OTHER records' ids — TimelineCaret L-9/m-8, SpringTarget m-8, TimelineTrack m-7/m-8 — never ChromeDock's · W0's sole `C-18` hit is its own cell, not kf-EditorShell's) · the W0 escape roster read at its own §1 (E-1..E-10). Sole write = this file.

**UNION VERDICT: DEFECTIVE — 11 of 11 wave specs DEFECTIVE. Pass 2's sole CLEAN (KF-W3) did not survive a fresh seat.**
**114 deduped defects** (3 BLOCKER · 2 CRITICAL · 6 HIGH · 45+2 MAJOR incl. union-level · 8 MEDIUM · 38 MINOR · 6 LOW · 4 INFO; 112 raw − 1 cross-filed dedupe + 3 union-level) · **6 hard program partition escapes** (vs pass 2's 9 — all nine verified CLOSED; the six are fresh) · **4 double-homings** (vs pass 2's 3 — all three CURED; these four are fresh) · **2 rulings partially unapplied** (R2-5 program-wide · R2-7's close certification falsified) · all four pass-2 BLOCKER/CRITICAL items verified CLOSED — and two of the four **classes** re-fired at new members.

**The pass-3 shape in one sentence**: repair round 2 executed its 17 rulings nearly whole — every enumerated BLOCKER/CRITICAL cure verified landed, the census axes narrowed again (escapes 30→25 per-wave, 9→6 hard, carriage 97.2%→97.8%) — but the severity head GREW (6→11 top-tier), because fresh seats going deeper found three faults the enumerated repairs never touched (a delete authorized on a false consumer census · an inverted gate oracle in a sequencing head · a cure-lock whose §Bounds anchors resolve silently onto unrelated bytes) **plus one BLOCKER manufactured BY a round-2 cure itself** (W1's letter ordered to certify a tree the census never matched) — the S-1 mechanism's fourth consecutive firing, now operating above the census layer.

---

## §0 · Per-wave census roll-up (from the 11 checks)

| wave | routed | booked | escaped | local verdict | defect register |
|---|--:|--:|--:|---|---|
| KF-W0 | 128 | 118 | 10 | DEFECTIVE | 9 MAJ · 2 MIN |
| KF-W1 | 6 | 6 | 0 | DEFECTIVE | **1 BLOCKER** · 5 MAJ · 3 MIN · 2 INFO |
| KF-W2 | 70 | 62 | 8 | DEFECTIVE | 6 MAJ · 9 MIN |
| KF-W3 | 24 | 24 | 0 | DEFECTIVE | 2 MAJ · 3 MIN · 1 INFO |
| KF-W4 | 109 | 104 | 5 | DEFECTIVE | **2 CRITICAL** · 4 MAJ · 7 MIN |
| KF-W5 | 43 | 43 | 0 | DEFECTIVE | **1 BLOCKER** · 4 MAJ · 5 MIN |
| KF-W6 | 421 | 420 | 1 | DEFECTIVE | **1 BLOCKER** · 2 MAJ · 3 MIN |
| KF-W7 | 138 | 138 | 0 | DEFECTIVE | 7 MAJ · 3 MIN |
| KF-W8 | 32 | 32 | 0 | DEFECTIVE | **3 HIGH** · 5 MED · 4 LOW |
| KF-W9 | 139 | 139 | 0 | DEFECTIVE | **3 HIGH** · 3 MED · 2 LOW |
| KF-W10 | 22 | 21 | 1 | DEFECTIVE | 6 MAJ · 3 MIN · 1 INFO |
| **Σ** | **1132** | **1107** | **25** | | **112 raw filings** |

**Carriage 97.8%** (pass 2: 1051/1081 = 97.2%; pass 1: 91.0%). Denominator moves are re-derivations with stated provenance, not drift: W0 112→128 (the pair-unit law applied to the mint's input set and the sibling-edge surface), W2 62→70 (posture cells swept by reading, not token-matching — the delta IS W2's headline defect), W4 104→109 (id-keyed census vs the inherited nine-block partition — the reuse is W4's CRITICAL 2), W6 392→421 (the spec's own stated method re-run; 392 reproduces under NO reading — W6 MINOR), W7 150→138 (the 150 sits between two rules and matches neither — W7 MAJOR; 138 is the reproducible enumeration), W9 147→139 (granularity variant, provenance stated, axis CLEAN), W10 9→22 (the census's three mechanisms extended to the five real routing surfaces; the unenumerated surface D is where the escape lives). W1/W3/W5/W8 reproduce exactly.

Note on KF-W3: its pass-2 CLEAN verdict is **not contradicted on the axes pass 2 measured** — the pass-3 convictions (routed/surplus membership law self-contradiction; KF-HA-19 carriage omission) are on the membership-law and W10-carriage axes fresh seats opened this round.

---

## §1 · PARTITION — rebuilt fresh (vs pass 2's 9 hard / 3 double-homed)

**Law** (unchanged): a row routed to any wave must land in ≥1 wave as a row, a spelled-out fold identity, or an exclusion-with-named-owner. Escaped-by-ALL-waves = program defect. Classification held like-for-like with PASS-2 §1 (hard / sibling-landed / register / id-keyed-trail) so the 9→6 comparison is honest.

### 1.1 Pass 2's NINE hard escapes: ALL CLOSED (verified in the pass-3 checks)

D·D-7 → W6 booked + declared-exception verified at 0 CARRY hits · ACG D-1 → W7 **OP-0** present (the pass-3 check leans on it by name) · AmigaScene D-2 + MISSED-A + KF-HA-10 → W9 §F/§G bookings verified verbatim at the bank, "the class's last members" · KF-ET-28 → W2 registry row 16, adopted · W10 C-3/C-4/C-5 → carried as record-only rows, "reproduce and are correct" (W10 check §5). **9/9 dead.**

### 1.2 The SIX fresh hard escapes — id for id (content/obligation carried by NO wave, verified by union grep this seat)

1. **KF-W8 R-4's three re-open triggers** (W10-routed by `KF-W8.md:298`'s own words: *"the FOLD-FORWARD record at KF.W10 carries all three with the ruling"*) — `grep -in 'trigger' KF-W10.md` → 0; W10 books the STRUCT-PAIR key 8 times and carries none of the three conditions, while writing the opposite posture ("a successor re-opens it against KF.W8 R-4"). The successor's evaluation instrument exists only in the source ruling; the ruled carrier is empty. [W10 D-4; routed by the one mechanism its census does not enumerate, D-9]
2. **kf-SequenceScene countable-cell inheritance datum** (W0 E-2; routed at `:153`) — `countable-cell` → 0 hits in all 11 specs.
3. **kf-AmigaScene per-file re-anchor datum** (W0 E-4; *"comment-only drift, useAmigaDemo −2 below :73"* — the corpus's only measured per-file offset, handed to the wave whose G-0.9 GREEN requires the pass "demonstrably ran per-file") — `useAmigaDemo` → 0 hits in all 11 specs.
4. **kf-SpringTrace D-15 no-re-book guard** (W0 E-6; *"KF.W0 fold-adjacent; no re-book"*) — W0's sole D-15 hit is kf-EditorHeader's family; the guard datum lands nowhere.
5. **kf-ChromeDock m-8 rider** (W4-routed as a rider the component "contributes to the banked born-RED": the `String(id)` AcceptableValue laundering at `:239`/`:289`) — 3 of 4 riders booked at W4; the W6/W7 `m-8` hits are TimelineCaret's, SpringTarget's, and TimelineTrack's ids, never ChromeDock's. Live content, carried nowhere.
6. **kf-EditorShell L-6/C-18** (W4-routed in the same sentence whose mate C-21 IS booked) — 0 hits program-wide (W0's `C-18` is its own §Carry cell).

**All six are NEW members** — none is a pass-2 escape surviving. Pass 1: 32 → pass 2: 9 → pass 3: 6, and in every generation the entire hard set is fresh. That is the S-1 mechanism stated as a curve: **the enumerated set is cured perfectly; the class is never closed by enumeration** (KF-W6's own pass-3 check says it in one line: "pass 1: five escapes; pass 2: D·D-7; pass 3: L-EST-12. The class is not closed by enumeration"). The six are also strictly smaller in mass than pass 2's nine (three measurement datums, two rider/limb rows, one carriage record — no adjudicated-BLOCKER row escapes this round).

### 1.3 The other nineteen per-wave escapes, decomposed (partition holds; local specs short)

- **Sibling-landed (8)**: W0 E-3 (PlaybackRibbon §7-caution/NON-bespoke rider limbs — dropped at W0's own locks-that-travel enumeration `:447`, carried at `KF-W4.md:244` for drag-seam) · E-7 **KAD-12** + E-8 **LP-8 ≡ KF-CO-35** (both live at W6 + the CARRY; the escape is the MINT's input set — C-17.R declares itself "the mint's complete input set" and is short) · W2 **KF-ES-3** (W9's escalation-trigger register arms it: *"jump-none faults render → BLOCKER"*; W4 row 8 carries the fold) · W2 **KF-HA-13** (W6 + W10's §6.C-H rider list) · W2 **KF-CO-48** (fully booked at W5: §Bounds row, B-15, G-OPTSET third leg) · W4 **m-1** + **D-m5** (W7 carries the TimelineTrack merged-group cure end; W4's group row books 3 of 5 ids).
- **Register escapes (4)** — W2's posture census-of-record still short after R2-11 completed it over the 58: **S-5/C-S5** (the corpus's ONLY positive posture over a typed ParseIssue, quoted in passing at `:256`, no row — the exact reasoning that admitted X15) · **SUP-D** (the positive refusal-surfaced-verbatim twin, 0 hits program-wide) · **KC-2's frozen-parse-boundary contract fact** (G-W2-6's published contract omits that façade parse results are deep-frozen — the very mechanism that makes KC-2 a loud TypeError; W9 carries only KC-2's AT arm) · **kf-SpringTrace C-2's kill qualifier** (the ruling-12 kill of "the parser is forced" cited twice without the kill — an M-25 dropped qualifier that *strengthens* the spec's own thesis).
- **Id-keyed trail escapes (7)**: W0 E-1 (SequenceScene F-1 family row `L-1 · C-11 · D §6` — fold content in C-1's mechanism, id in neither C-1.F nor C-1.G) · E-5 (AmigaScene C-6's "owned by KF.W6" limb dropped from a booked mint row) · E-9 (**F1(c)** — a claim booked under a COINED id with 0 registry hits, in a table whose preamble forbids coining) · E-10 (the W10 mint-consumption edge, declared at W10, unanswered at W0 — W0's own C-22 precedent unapplied to itself) · W4 **KC-17** (witness to the booked KC-37 row) · W6 **L-EST-12** (CARRY `:171` carries it; the ID-ALIASES block restores its two siblings from the same record and drops it) · W2 **KF-CB-1** (the executed easing-name break generalized by the twice-carried KF-CB-33 organ lock, absent from the census the lock widens).

### 1.4 DOUBLE-HOMING — fresh count (pass 2's three ALL CURED)

R2-12 verified at the ordered pair (W5:152 names all four W7 fixtures reciprocally — "that pair is clean") · R2-13 (proof:brittleness) and R2-14 (KfPillTabs limbs) draw no pass-3 conviction — **3/3 dead**. Four fresh:

1. **The `@vitejs/plugin-vue` registration act — one edit, two waves' booked cure** (W8 HIGH 3). KF-W4 §Bounds claims the absent `plugins` array, item 3 books it, commit 2 is *"plugin-vue REGISTRATION (only vitest.config.ts changes)"* — and KF-W8's G10 is born RED **solely** on that absence, with G11/G12 gated on G10 and Rows 24's "one vitest.config.ts edit" being W4's edit. W4 lands first; G10 is discharged before W8 opens; neither spec declares the collision. Compounded by W7's cost-model outlier (OP-4/G11 cost an ADD of a plugin that is already a devDependency at `:81` — W4 struck exactly that add as a no-op; W7 is the outlier of three).
2. **`package.json` — four waves' bounds rows, no cross-wave declaration** (W7 MAJ 6 + W8 MED 8, one program defect filed at both ends, deduped): claimed by W3/W4/W7/W8; W8's modify-carve over `exports · sideEffects · scripts · devDeps` overreaches W5's decision authority (its own G2: "The gate MEASURES; KF.W5 DECIDES") and W4's scripts claim; W7's KF.W4 edge is scoped "for VERIFICATION not authoring", false the moment W7 writes vitest.config.ts.
3. **`test/demo/instrument/` — the R2-12 law applied to one neighbour pair of a four-party surface** (W7 MAJ 3 + W4 MAJ 3, distinct claims kept): W8 claims the 9 tracked files modify-carve PLUS resize-tracks.test.ts PLUS four creates; W4 creates two more; W7's §Disjointness asserts "Neither wave owns the directory" — a two-party statement; W4's own row count for the directory is self-contradictory (9 tracked; its list reaches ten by double-counting resize-tracks.test.ts).
4. **`scripts/observe/demo/usability.mjs` — three postures, one entry** (W6 MIN): "read-only witnesses (never modified)" AND "written by the KF.W4∥KF.W6 atomic bundle" AND "never written concurrently by the two waves" — a seat cannot determine whether the path is writable in W6.

---

## §2 · RULINGS-2 EXECUTED — the 17-ruling audit

| ruling | verdict at pass 3 |
|---|---|
| R2-1 (W1 · O-21 mint) | **APPLIED** — O-21 at all five load-bearing sites, verified independently by the W9 seat (`KF-W1.md:79/:136/:191/:194/:315`) and the W10 seat (INBOX max O-20 ⇒ mint = O-21); no stale O-20 commissioning survives. *But the same wave's C-6 cure leg commissions an UNORDERED invention that is now the pass's #2 defect (below).* |
| R2-2 (W10 · C-3/C-4/C-5) | **APPLIED** — carried as record-only rows; W10's check: "reproduce and are correct." |
| R2-3 (W4 · G-KFW4-1 composes) | **APPLIED** — composition with `proof:structure` verified at four sibling seats (W0/W5/W9/W10). Residue: the no-silent-deletion law reaches only `check`, not the sibling scripts the same §Bounds row opens (W4 MIN). |
| R2-4 (W4 · phantom cite struck, EIGHT targets) | **APPLIED** — EditorShell struck, no re-conviction. Residues: the R-7 cure inventory names 2 of 3 `proof:idioms` sites (misses `layout.css:10`, itself a target) so G-KFW4-8 still cannot go green; 107≠106 names; §Excluded 21's printed command is not the command that produces its figure (W4 MAJ+MIN×2). |
| **R2-5 (witness re-derivation law)** | **PARTIALLY UNAPPLIED — the pass's dominant conviction.** Breached in substance at seven-plus seats: W1 (the two tree-identity gates run bare HEAD = the DISQUALIFIED `8281638c`; the census-cardinality schism), W4 (row 78's anchors resolve SILENTLY onto unrelated bytes — the phantom class in its check-surviving form), W5 (G-PRM-FLIP's witness cells never re-anchored — `8281638c` coordinates into a file that does not exist at the frontier; the `:3` headline quotes three struck denominators probeless), W6 (line counts re-run, the import graph never — under a DELETE authorization), W7 (the census of record transcribed from R2-9's prose, non-rederivable: 150 sits between two rules), W8 (G9's `git ls-files` reads the disqualified local index; G13's named-11 check reproduces under no reading), W9 (D3-1's five-site witness family resolves only at `8281638c`; D3-4's universal is false for the un-enumerated family), W0 (C-1.G's 200/48 is a third basis — the SCH-3 class inside the anti-SCH-3 table). |
| R2-6 (seven gate clauses) | **5/7 legs whole; 3 partial**: 6.1 G-W2-2 ✓ · 6.2 G-BASIS partial (OP-6's premise understates the live 3-leg script; the reconciliation's rival still owed) · 6.3 G-SHIM ✓ · 6.4 G-DEPCRUISE outcome ✓, strike-reason false (lane-library.md:154 sources the `:233-248` anchor the strike calls provenance-free) · 6.5 G11 ✓ (new sibling G10 placeholder, MIN) · 6.6 G4 ✓ · 6.7 G1.2 ✓ with the L-18 blanket still over-claiming (13/15 commands, not 15/15). |
| **R2-7 (re-anchor seat writes LAST)** | **PARTIALLY UNAPPLIED — its own close certification is falsified.** W1 §11 row 7 certifies "NO line number anywhere on this file's cross-spec receipts"; five survive on cross-edges 2 and 11, four DEAD at current sibling bytes — and §1 of the same file strikes the same four coordinates it then re-asserts (one file, two verdicts). W8 cites `:410` as live and retired in the same file, and re-uses `:269` after striking it. W9's `⟨X-W6 §MATRIX⟩` is not a §-heading, gate id, or banked id. W7 keeps two raw-line labels beside the idiom it retired them for (disclosed). |
| R2-8 (D·D-7 → W6) | **APPLIED** — booked; declared-exception family verified at 0 CARRY hits. |
| R2-9 (ACG D-1 → W7, OP-0) | **APPLIED** — OP-0 present and load-bearing. The ordered census restatement (150/144+6/0) was transcribed from the ruling and is non-rederivable — an R2-5 breach filed there, not a non-execution here. |
| R2-10 (D-2 · MISSED-A · KF-HA-10 → W9) | **APPLIED, verbatim** — the pass-3 W9 census axis is CLEAN; the conditional W6 sibling-half verified NOT triggered (booked at `KF-W6.md:314` + CARRY `:220`). |
| R2-11 (KF-ET-28; F0 over the 58; W3 E-15 line) | **APPLIED** — row 16 adopted; X1..X15 entered; the W3 sub-line present. Residues: the completed register still misses four posture cells swept only by token-matching (W2's Y-escapes); E-15's surplus discriminator now contradicts the criterion that admits X-13 (W3 MAJ 1). |
| R2-12 (the glob dies; both ends enumerate) | **APPLIED at the ordered pair** (W5↔W7 clean, reciprocal) — the law was not extended to the surface's other two parties (§1.4 item 3). |
| R2-13 (proof:brittleness → W6) | **APPLIED** — no re-conviction. |
| R2-14 (KfPillTabs limbs from the bank) | **APPLIED** — no re-conviction. |
| R2-15 (W9 headline + counting family) | **APPLIED** — the dated-correction block present; the whole counting family reproduces at the bytes (79 · 13 gates · 16 · 13 triggers · 6/11/4 · 17/1/1 · 17 · 565). One residue: the 565's *published command* yields 563 (the h3-nested addendum section), D3-6. |
| R2-16 (R-8.4's rows 59/64/65) | **APPLIED** — literals carried. New anchor defects in adjacent rows 64/68/69 (W4 MIN/MAJ). |
| R2-17 (W0 G-0.7 re-cut, 53-name roster) | **APPLIED** — no re-conviction of G-0.7; W0's pass-3 register is elsewhere (C-14/C-17.R/C-1.G/C-19). |

**rulingsUnapplied = 2** (R2-5 · R2-7 — both partial, both program-wide, both the round's OWN new laws; every numbered per-wave directive verified executed).

---

## §3 · THE U-2 / U-3 CLASSES — not at zero; named survivors

**U-2 (intra-round anchor rot) — SURVIVES at 4 named families**: (1) KF-W1 cross-edges 2+11 — five line-number receipts under a falsified "no line number" close stamp, four dead (`:393`/`:431` vs KF-W0's `:439`/`:477`; `:54`/`:332` vs KF-W10's `:59`/`:382`), with §1 of the same file striking the same coordinates §7 asserts; (2) KF-W8's `:410` cited-live-and-retired + `:269` struck-then-reused, both intra-file; (3) KF-W9's `⟨X-W6 §MATRIX⟩` (an inline bold label, not an anchor); (4) KF-W7's two raw-line spec-local labels (resolving today, disclosed — the mildest member). Materially narrowed from pass 2's ~14 families; not at zero, and survivor (1) sits inside R2-7's own certification.

**U-3 (repaired-against-the-record-never-against-the-tree) — SURVIVES at 5 named families, including the pass's worst defects**: (1) KF-W1 whole — the wave's thesis is inverted at the bytes (the packets measured the live worktree accurately and named no sha; every anchor §5a marks ABSENT resolves in the sacred checkout), its two tree-identity gates run bare HEAD at the disqualified `8281638c`, and its C-6 cure orders `8281638c` inscribed as "the tree it matched" when the census matches that sha on ZERO of six lines — **a new false certification commissioned by the cure of a false certification** (BLOCKER); (2) KF-W9 D3-1 — the `play-lifecycle.ts` five-site witness family (two born-RED gates) resolves only at `8281638c`; the frontier module is a directory; (3) KF-W5 — G-PRM-FLIP's un-re-anchored cells + the `:3` probe-free denominators; (4) KF-W8 G9 — the set-comparison oracle reads the disqualified local index (122 vs 132; a wrong ref is a wrong verdict, not a wrong number); (5) KF-W4 row 78 — the WORSE form: anchors that resolve silently onto unrelated bytes and therefore survive every anchor-existence check R2-5 installed.

---

## §4 · BLOCKER/CRITICAL CLOSURE — the four pass-2 items

| item | closure |
|---|---|
| **O-21 mint** (pass-2 #1, W1) | **CLOSED** — verified at all five sites by two independent sibling seats; no spec names a spent id. |
| **W10 carriage C-3/C-4/C-5** (pass-2 #2) | **CLOSED as enumerated** — all three carried, correct. **The CLASS re-fired at five new one-ended W10 edges** (§5): W10 D-4 (the R-4 triggers, this pass's one hard-escape-by-census-blindness), W0 E-10 (the C-17-mint consumption edge unanswered), W3 (KF-HA-19 — the R1-framing triple's third member, the one adverse to the wave's headline, absent while E-14 preserves four other dissents), W7 (ZERO KF.W10 references against W10's three, including the acceptance form its terminal table needs), W9 D3-3 (the OD-V3/OD-V5 capture packet — W10's hard opening precondition, closed by silence in the section headed "declared from this end"). |
| **W4 head G-KFW4-1** (pass-2 #3) | **CLOSED** — the composition verified independently at four sibling seats; the head law honored in all 11. |
| **W4 phantom cite** (pass-2 #4) | **CLOSED as enumerated** (EditorShell struck; DESIGN.md:251 in). **The CLASS reproduced in worse form**: row 78's silently-mis-resolving anchors (CRITICAL — the check-surviving variant) and W9's stale-ref witness family (HIGH). |

---

## §5 · MONOTONE NARROWING — the verdict is SPLIT, and the head is non-monotone

**Monotone (genuinely, third consecutive pass)**: hard escapes 32→9→6, with each generation's enumerated set fully cured and the surviving mass strictly smaller in kind (adjudicated-BLOCKER rows → records/rulings → datums and limbs) · per-wave escapes 30→25 · carriage 91.0→97.2→97.8% · double-homings 6→3→4 but the four are surface-declaration collisions, not contradictory whole-row claims · phantom coordinates that resolve to NOTHING: extinct (the survivors resolve to the wrong bytes, which is the mutated form) · the counting families of W9 and W10 now reproduce nearly whole (pass 2's S-3 signature) · zero fabricated registry citations, third pass running — every failure remains staleness or scope, never invention.

**Non-monotone at the severity head**: top-tier defects 6 → 11 (2 BLOCKER + 2 CRITICAL + 2 HIGH → 3 BLOCKER + 2 CRITICAL + 6 HIGH), total 108 → 114, and **all eleven top-tier defects are NEW**. Three are pre-existing faults the first two passes never reached (W6's false consumer census under a delete authorization · W5's inverted G-XSS oracle in its own sequencing head · W4's row-78 silent mis-resolution); one was **manufactured by the round-2 repair itself** (W1's C-6). The S-1 mechanism has climbed a layer: rounds 1–2 closed enumerated ESCAPES while un-enumerated escapes survived; round 2 closed enumerated DEFECTS while un-enumerated defects of higher severity survived — and one cure minted a defect of the exact class it was curing (SCH-4 inside SCH-4's cure). **A repair contract for round 3 that enumerates these eleven will, on the observed curve, be executed perfectly and leave the next stratum standing; the missing instrument is not a better list but the two unapplied laws (R2-5, R2-7) actually run to completion — plus one new law: no cure text may itself assert a certification the cure's own receipts do not reproduce.**

The W10-seam class (§4) is the one genuinely program-structural survivor: five one-ended edges, all pointing at the close wave, discovered independently by five seats. The close wave cannot assemble its terminal table from what the siblings oblige their seats to write.

---

## §6 · UNION DEFECT REGISTER — fold + dedupe

**Arithmetic**: 112 raw filings across 11 checks → **−1 cross-filed dedupe** (P3-1 · the package.json multi-wave bounds collision, filed at W7 MAJ 6 + W8 MED 8 — one program defect, two ends) → 111 → **+3 union-level** → **114**.

Union-level additions:
- **U3-A · MAJOR · the W10-seam carriage class** — five one-ended edges (W0 E-10 · W3 KF-HA-19 · W7 zero-refs · W9 D3-3 · W10 D-4), each individually filed, jointly making KF-W10's terminal product unassemblable; no single wave can cure it.
- **U3-B · MAJOR · the S-1 mechanism, fourth firing** — six fresh hard escapes, all members no prior list named; three consecutive proofs that this program's escape class cannot be closed by enumeration, now stated as a standing program fact requiring a mechanism change (full-surface re-derivation per pass, never differential).
- **U3-C · MAJOR · the residual stale-substrate family** (§3's U-3 survivors as one program class) — five families across five waves still carrying `8281638c` or worktree/index measurements under frontier certifications, after a round whose binding law was exactly this.

### Ranked worst-10 (the register's head)

| # | sev | wave | defect |
|---|---|---|---|
| 1 | BLOCKER | KF-W6 | **The F-5 shim delete is authorized on a FALSE consumer census.** The named "repoint" target (`test/demo/instrument/useAnimationGroupPlayback.test.ts`) imports the REAL module — nothing to repoint, G-W6-3 unfalsifiable against it; the shim's sole consumer is `test/demo/state/no-shadow-playback-authority.test.ts:21`, with ZERO hits across the entire wave tree + CARRY. Executing the authorized delete breaks a suite the program cannot see. The bank named a line, the CARRY substituted the wrong filename, and R2-5's re-run covered line counts but never the import graph the delete depends on. |
| 2 | BLOCKER | KF-W1 | **G-KF1-4/C-6 commissions a NEW false certification into the cure letter**: it orders O-11's withdrawn "matches your tree exactly" replaced by "naming the tree it matched (`8281638c`)" — but the census matched the 252-dirty UNCOMMITTED WORKTREE byte-exact and matches `8281638c` on ZERO of six lines (81 imports vs 61; `/css` and `/value` do not exist at that sha). SCH-4 reproduced inside its own cure. The wave's whole thesis is inverted at the bytes: the packets measured a live tree precisely and only named no sha. |
| 3 | BLOCKER | KF-W5 | **G-XSS's "unconditionally observable half" is an INVERTED ORACLE** in the wave's sequencing head: `grep -n 'innerHTML' useHighlightCSS.ts → 0` can never go GREEN without deleting the CORRECT hljs-escaped write at `:123`; the ruled KAD-3/KAD-14(a) cure redesigns the marker, not the highlighter, so post-cure the grep returns 1 permanently. Same class as the round-2 G-DEPCRUISE inversion, surviving in the one gate the wave calls unconditional. |
| 4 | CRITICAL | KF-W4 | **§Bounds row 78 mis-files G-KFW4-14's cure-lock subject**: `seedFor`/`syncGap` live in `EasingSidebar.vue` (`:99`/`:112`/`:132-137`), not `useEasingDemo.ts`; the row is modify-carve "these lines only", so the verbatim CURE-LOCK ("gate `seedFor` on `NAMED_EASING_BEZIER`") is unsatisfiable inside its own §Bounds and its falsifier unreachable. The anchors resolve SILENTLY onto unrelated bytes — the phantom-target class in the form that survives every anchor-existence check R2-5 installed. |
| 5 | CRITICAL | KF-W4 | **Five banked W4-routed ids booked nowhere** (KC-17 · ChromeDock m-8 · EditorShell L-6/C-18 · TimelineTrack m-1 · D-m5): both prior passes re-used one nine-block partition and declared it an independent re-derivation; four of the five sit in the same sentences whose siblings the spec books — a partial harvest of a banked list. Two of the five (m-8, L-6/C-18) are carried by NO wave (§1.2). |
| 6 | HIGH | KF-W8 | **G8 leg (i) understates the frontier by one site, and the site is in no bounds row**: the command returns TWO hits; the second (`EditorStartScreen.vue:102`, a phantom-directory citation in a comment — exactly G8's subject) is un-booked, so the gate stays RED with no booked act or forces an out-of-bounds edit. Survived two repair rounds and PASS-2's per-gate check. |
| 7 | HIGH | KF-W9 | **The `play-lifecycle.ts` witness family (5 sites, incl. born-RED G-KFW9-6/G-KFW9-7 and the SUBSTRATE-INVERSION row) resolves ONLY at the DISQUALIFIED `8281638c`** — at the frontier the module is a directory (`engine/play-lifecycle/{frame,strategies,…}.ts`, carved at `53b907c5`). The exact fault the wave exists to police, convicting under its own TREE LAW. |
| 8 | HIGH | KF-W8 | **G10's entire born-RED condition is another wave's booked commit**: KF-W4 owns the absent `plugins` array, books it (ME-32 ≡ R-10), and commits it ("plugin-vue REGISTRATION"); W4 lands first, so G10 is discharged before KF.W8 opens; G11/G12 are gated on it. Neither spec declares the collision (§1.4 item 1). |
| 9 | HIGH | KF-W9 | **G-KFW9-6's witness "`reducedMotionScale` … used by nothing" is false at BOTH baselines** — three live consumers (`physics/spring/progress.ts:153/:232/:385`, `amplitudeScale`); the wave's central adjudicated cure (the intensity form) has a shipped in-tree precedent the spec does not name; the RED overstates the gap it measures. An unbanked amplification of the bank's "exported". |
| 10 | HIGH | KF-W9 | **KF.W10's hard inbound dependency closed by omission**: W10 declares W9/SS-13's OD-V3 capture packet + OD-V5 observation a hard opening precondition at five sites; W9 contains zero W10/OD-V3/OD-V5 bytes, no 1280 arm, no duplicating-scene enumeration, no bounds row for the receipt W10 says lives in "both waves' bounds blocks" — W10's OP-4 is unfulfillable by construction. Head member of the five-edge W10-seam class (U3-A). |

**Register tail (11–114, by cluster, all retained from the per-wave checks)**: W8's G13 (the named-11 check reproduces under no reading; twelve resolve at the frontier — the eleventh top-tier defect, HIGH) · W0's provenance-gate inversion (the headerribbon letter EXISTS at keyframes origin/master; the wave strikes it inside the gate whose content is provenance truth, against KF-W10's declared-back correction) + the C-17.R mint defects (coined `F1(c)` · anonymous row 13 · short input set) + C-1.G's third-basis 200/48 + C-19's 32-vs-30 (third consecutive round on one cell) · W1's one-file-two-verdicts on four coordinates + the 6-vs-"all five" census schism + the bare-HEAD tree-identity gates · W2's token-matching exclusion basis (the instrument whose blindness produced the round-2 escapes, re-used to close 13 records) + the refuted BH-relay DECLARED NEGATIVE (two banked postures name the relay; the sentence instructs later seats not to raise one — an ask suppressed under the standing BH/BI edict, flagged upward) + the repin `4.1.x` literal against W3's RC-P(V) predicate law · W3's routed/surplus membership-law self-contradiction (routedTotal 24 not auditable; the criterion that admits X-13 admits the eight surplus rows) + the unlocatable "424" · W4's nine-vs-ten directory self-contradiction + the 47/41/6 AUDITED arithmetic (SequenceScrubber the mis-classed seventh) + the proof:idioms 2-of-3 cure inventory · W5's `:3` probe-free struck denominators + the ten-vs-nine created-spec enumeration + the false KF-SS-31 0/1 cell (false at write time, mtime-proven) · W6's SPF-10 (one occurrence, no disposition, outside the declared-exception law) + the 392-vs-421/473 method-figure gap + CubeTarget #8/#9 adjectival-only · W7's non-rederivable 150 + the receipt that returns different rows (R2-7's own law: "a receipt that returns a different row is not a receipt") + the paraphrase labelled verbatim · W9's D3-4/D3-5/D3-6 (the universal, the un-enumerated 13 cross-edges, the 563-command) · W10's D-1/D-2/D-3 (a FOLDED-TO that resolves nowhere · evidence coordinates two waves have never heard of · 7 of 15 rows unwordable in a closed vocabulary with re-derivation forbidden) + D-5/D-6 (cite-the-row-miss-the-row-underneath, twice — a method finding) · plus the MEDIUM/MINOR/LOW/INFO tail as filed.

---

## §7 · What HOLDS program-wide (recorded so pass 4 does not re-litigate)

1. **The rulings mechanism executed again**: all 15 enumerated per-wave rulings verified applied (several verbatim, independently confirmed by sibling seats); all four pass-2 BLOCKER/CRITICAL items closed; all nine pass-2 hard escapes dead; all three pass-2 double-homings dead. What failed is, for the second consecutive round, exactly the two things a directive list cannot do: re-measure everything (R2-5) and re-anchor last (R2-7).
2. **Census carriage 97.8%**, six waves at 0 escapes (W1 · W3 · W5 · W7 · W8 · W9 — W9's axis formally CLEAN after two repair rounds, closed by re-enumeration at 139/139); W6 at 99.8%.
3. **W4 head + W3 gate posture hold in all 11**, third pass running; the `npm run check` re-cuts compose end-to-end (verified at four seats); KF-AV-28 is present where governed in 10 of 11 (W0's `:447` limb drop is the residue, E-3).
4. **Zero fabricated citations, three passes running** — every conviction is staleness, scope, inheritance, or inversion; never invention. NO-INVENTION/M-25 holds at the row level in all 11.
5. **The countable instruments now reproduce**: W9's whole counting family, W10's §0 table (~20 rows byte-exact), W6's corpus preamble, W0's five sub-table denominators (73), W5's per-cell §Excluded counts (14 of 15). The remaining figure defects are enumerable on one hand per wave.
6. **What round 3 must be, on the observed curve**: not a bigger list. (i) Run R2-5 to completion as a FULL-SURFACE sweep (every witness, every anchor, both baselines checked for silent mis-resolution, import graphs for every delete); (ii) run R2-7's seat again, LAST, with its certification itself audited; (iii) cure the eleven top-tier defects with the new law that **no cure may assert a certification its own receipts do not reproduce** (the W1 lesson); (iv) close the W10 seam bidirectionally in one motion across the five waves (U3-A); (v) adopt full re-derivation (never differential) as the standing census posture (U3-B).

---

## §8 · Structured summary (mirrors the StructuredOutput call)

- **verdict**: DEFECTIVE — 11/11 wave specs DEFECTIVE; pass-2's four BLOCKER/CRITICAL items all CLOSED and two of their classes re-fired at new members; hard escapes 9→6 (all fresh — S-1's fourth firing); double-homings 3→4 (all pass-2 cured); U-2/U-3 both survive with named members; monotone on every census axis, NON-MONOTONE at the severity head (6→11 top-tier, all new, one manufactured by a round-2 cure)
- **defectCount**: 114 (112 raw − 1 dedupe + 3 union)
- **blockers**: 3 (W6 false-census delete authorization · W1 cure-commissioned false certification · W5 inverted G-XSS oracle)
- **high**: 8 (2 CRITICAL at W4 + 6 HIGH at W8/W9)
- **partitionEscapes**: 6 (§1.2, id-for-id; classification law held like-for-like with PASS-2 §1)
- **doubleHomed**: 4 (§1.4; pass 2's three all cured)
- **rulingsUnapplied**: 2 (R2-5 partial program-wide · R2-7 partial, its close certification falsified)

*Union seat, PASS 3. This file is the pass's only union write; nothing here stamps any wave. E-3: a check record, not a spec amendment.*
