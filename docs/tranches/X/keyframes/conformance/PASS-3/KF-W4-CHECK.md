# KF-W4 — FRESH ADVERSARIAL SPEC CHECK (L-18/L-20, PASS 3)

**Spec under trial**: `/Users/mkbabb/Programming/value.js/docs/tranches/X/keyframes/waves/KF-W4.md` (315 lines, repaired twice)
**Corpus authority**: the 58 `kf-*.md` records in `docs/tranches/V/megatranche/registry/adjudicated/`
**Sole in-tree carry**: `docs/tranches/X/keyframes/carry/KF-W6-CARRY.md` (KF.W6's, 201 rows — cited by this spec at its W6 cross-edge only, never as provenance)
**Prior registers** (read for what they CLAIM, never for a measurement): `PASS-1/KF-W4-CHECK.md` + `PASS-1/RULINGS.md`; `PASS-2/KF-W4-CHECK.md` + `PASS-2/RULINGS-2.md`
**Seat**: FRESH adversarial reader. **Nothing inherited** — census re-derived from the 58 records' bytes; every anchor, count, command and witness re-executed read-only at the keyframes frontier. Zero product-source writes; sole write = this file.
**TREE LAW observed**: product source read-only at the sibling checkout `/Users/mkbabb/Programming/keyframes.js`, **at `origin/master` `81a56990`** (confirmed: `git rev-parse origin/master` → `81a56990736ced5b5edde0b84c527680ac7689b1`; `git rev-list --count 8281638c..origin/master` → **41**). The local worktree is dirty and pinned at the DISQUALIFIED `8281638c`; no witness below is taken from it.

**Verdict**: **DEFECTIVE** — **109 routed · 104 booked · 5 escaped** · 12 defects (**2 CRITICAL · 4 MAJOR · 6 MINOR**).

**The shape of this pass.** Round 2's binding law (R2-5, the WITNESS RE-DERIVATION LAW) was largely *executed*, and the difference from round 1 is enormous: every CRITICAL and every MAJOR of PASS-2 is cured **at the bytes** — the `check` re-cut now composes with the live `proof:structure`; `EditorShell.vue` is struck (0 `proof:` bytes, confirmed); both script rows are re-pathed to real files; the demo lane's 27 specs are counted; the three created fixtures carry the only extension either vitest project collects; `SequenceScene.vue`'s dead `computed` is enumerated; `_boundTimeline`'s three hits are stated. Of the 60-odd frontier anchors this seat re-executed, **the great majority re-derive exactly** — including the entire eight-target citation census (33 lines / 19 names, every per-file count and every line number), the `NAMED_EASING_BEZIER` 29⊂30 `smooth-step-3` identity (re-derived by executing the installed 4.0.0), and every cross-spec receipt in the program.

The failures that remain are of two kinds, and both are kinds round 2 believed it had closed:

1. **The census was never re-derived; it was inherited.** Both prior passes report 104/104/0 over the same nine-block partition. An id-keyed re-enumeration from the bytes finds **five banked W4-routed ids that partition never contained** — so it could not, and did not, escape them. Four of the five sit in the *same sentences* whose siblings the spec books.
2. **One anchor class survived R2-5 because it is silently resolvable.** Round 2 hunted anchors that resolve to *nothing*. It did not hunt anchors that resolve to *something else*. §Bounds row 78 files `seedFor` and `syncGap` — G-KFW4-14's own cure-lock subject and the caption's driver — under `useEasingDemo.ts`, where neither name exists at any coordinate; the line numbers are `EasingSidebar.vue`'s and land, in the file named, on an SVG-path branch and a comment block.

---

## §0 Method

1. **Census.** Mechanically extracted every line in all 58 records matching `KF\.W4|KF-W4|\bW4\b` → **195 lines across 45 records**; 13 records are W4-byte-silent. Separately extracted the 171 lines matching `KF\.W4` across 43 records.
2. For each hit resolved the **terminal disposition** — the last cell of a roster row, the record's `## Wave routing summary` `**KF.W4**:` bucket, or the prose `FOLD → … KF.W4` / `→ **KF.W4**` / `KF.W4 rider` / `KF.W4-PROSE` / *"Riders this component contributes to the banked born-RED"* forms. Discarded non-routings: routing-law recitals (`kf-App:35`, `kf-ChromeDock:7`, `kf-EditorHeader:7`, `kf-EditorShell:7`, `kf-KeyframesEditor:36`, and 20 more), sequencing references (*"sequenced after the KF.W4 vue-tsc gate"* — `kf-CubeAxisLines:34`, `KF-AX-16`; *"AXISLINE-UNIT after KF.W4"*; `LP-21`→OPTIONS-UNIT), adjudication summaries, and the cross-taxonomy trap at `kf-StartingStyleTarget.md:112` (**K**.W4, tranche K — correctly uncarried).
3. **Escape hunt by mechanism, not by list.** Extracted every id token co-occurring on a `KF.W4` line (245 distinct), tested each against `KF-W4.md` by `grep -F`, then resolved the 116 non-matches by reading their source line's terminal disposition. Accounted for the spec's compressed-list idiom (`KF-APP-1/-12/-30/-34/-45` books `KF-APP-30`, which a literal grep misses). **Five survive as genuine escapes** (§2 D-2).
4. Cross-swept all 13 W4-silent records for family-routings landing in W4 by reference (`KF-APP-4|KF-CE-16|noUnusedLocals|vue-tsc|KF-CO-7|typecheck`). Ten return **zero**; the three that matter — `kf-SequenceScene`, `kf-KeyboardShortcutsModal`, `kf-SequenceScrubber` — are all three now carried by the spec (rows 63, 1, 12 / §Sequencing). **No new cross-sweep escape.**
5. Matched every survivor against the spec's 31-row carry table, 14 gates, §Bounds' 36 file rows, §Sequencing, §Disjointness, §Excluded and the §Excluded 16–21 residue table.
6. **Executed every cited command and path at `origin/master 81a56990`** — read-only `git show` / `git grep` / `git ls-tree` against the sibling checkout, plus one `node --input-type=module` import of the *installed* `@mkbabb/value.js@4.0.0` to re-derive the `bezierPresets` key set. No write, no install, no browser.
7. Re-resolved every cross-spec receipt in the program (`KF-W0.md`, `KF-W1.md`, `KF-W2.md`, `KF-W6.md`, `KF-W9.md`) by its declared **label** anchor, never by an inherited line number.

---

## §1 ID-KEYED CENSUS — 109 routed · 104 booked · **5 escaped**

### 1.1 The denominator, re-derived

| block | mechanism | n | running |
|---|---|--:|--:|
| **A** | the vue-tsc / demo-typecheck fold identity — row 1's 30-member `≡` chain plus the day-one diagnostic witnesses attached to G-KFW4-1 | 33 | 33 |
| **B** | gate topology + merge-path lanes (KF-CB-8 · ME-32/ME-29 · KF-SST-37 · C-21) | 4 | 37 |
| **C** | K-quartet (K1–K4 · C-19/L-m1 · KF-SS-34 · L-10/L-11 · C-10/C-11/D-m6 · L·N-16 · KF-AV-18 · L-18/C-12a · MISS-6) | 12 | 49 |
| **D** | easing honesty (KF-ET-6 · KF-ES-3 · KF-ES-14 · KF-CB-18 · KF-CB-24 · KF-CB-29 · KF-TFP-17) | 7 | 56 |
| **E** | citation/provenance census (KF-EST-1 · L:D-3/C:C-5 · KF-HA-4 · D-19 · KF-CO-33 · KF-CO-37 · SPF-27 · MM-9 · MM-13 · KF-EST-6) | 10 | 66 |
| **F** | AnimatedText gate family (KF-AT-4 · -10 · -24 · L-EST-17 · KF-AT-7 cascade leg) | 5 | 71 |
| **G** | gate-authoring audit + structure (OD-29 · KF-CE-12 · KF-AV-8 · C-12 · KF-CE-30 · REGISTER-LAUNDER · C-S+1 · L-D7 · L:D-3 zero-test · SquareScene `:166` · B10-27 · B21-17) | 12 | 83 |
| **H** | the KF.W4-PROSE class, R-6 split (ChannelControls ×7 · KfPillTabs ×4 · SpringTrace ×3 · C-4 · N-1/C-5 · B18-19) | 17 | 100 |
| **I** | the four PASS-1 escapes, carried at round 1 and verified carried here | 4 | 104 |
| **J** | **ids with a terminal KF.W4 disposition that no prior partition contained** (§1.2) | **5** | **109** |

**Blocks A–I reproduce member-for-member.** Block J is this pass's finding, and it is the census axis's defect.

### 1.2 The five escapes, by bytes

| # | id | banked at | the banked routing, verbatim | in `KF-W4.md` |
|---|---|---|---|---|
| **E1** | **KC-17** | `kf-KeyframeCardList.md:121` | *"**KC-13 = KF-CE-16** (the vue-tsc gate hole) — one identity, folded to **KF.W4**; this corpus adds the **KC-37 and KC-17** witnesses to that row"* | `grep -c 'KC-17'` → **0**. KC-37 is booked in G-KFW4-1's witness column; its named twin is not |
| **E2** | **m-8** | `kf-ChromeDock.md:41` | *"**FOLD → kf-App KF-APP-4 (KF.W4 gates lane)** … **Riders this component contributes to the banked born-RED**: the concealed TS2322 at `:128` …; **C-8** …; **m-8** (`String(id)` at `:239`/`:289` launders `AcceptableValue` into `"[object Object]"` scene ids); **m-1/C-7**"* | three of the four riders booked (`:128` ✓ C-8 ✓ m-1/C-7 ✓); **m-8** → **0** |
| **E3** | **L-6/C-18** | `kf-EditorShell.md:40` | *"**FOLD → kf-App KF-APP-4 (KF.W4 gates lane)** … **Riders this component contributes**: the **L-6/C-18** unread `const props` and the `object\|null` annotation-laundering … are members of the class this gate would catch; **C-21** … rides as the test-coverage cell"* | **C-21 booked** (row 3); its sentence-mate → **0** |
| **E4** | **m-1** ⟨TimelineTrack's⟩ | `kf-TimelineTrack.md:59` | *"**C-10/C-11/D-m6/m-1/D-m5 merged — the contract-hygiene group** … **→ KF.W7** (cure), **KF.W4** (enforcement, by fold)"* | carry row 4 books *"C-10/C-11/D-m6"* — a **five**-id merged group under **three** ids |
| **E5** | **D-m5** ⟨TimelineTrack's⟩ | `kf-TimelineTrack.md:59` | (same row; `D-m5` is the emits/`defineModel`-law arm, *"adopted at this seat's read of `:129-134`"*) | → **0** |

E2/E3 are the sharpest: both sit inside a *"Riders this component contributes"* sentence, both name a specific type-surface mechanism, and in both the spec books the sentence's other members and drops one. E4/E5 are an id-keyed truncation of a banked merged group — the exact discipline this spec legislates for itself (*"the bare id `D:D-8` is record-scoped and resolves to four different rows across the corpus; it is never carried unqualified"*).

### 1.3 What the census axis gets RIGHT

- **All four PASS-1 escapes remain carried with receipts** — the ~40-name magnitude (row 29 + R-7 + §Excluded 20), `proof:sequence-rows-draggable` (row 12 header + OP-3 RESOLVED), SpringTrace D-1's `:48-49` sequencing rider (*"rides the fix"*, verbatim), the KF-AV-28 standing rider (§Sequencing, verified byte-for-byte against all three bank sites below).
- **PASS-2's D-9 cure verified**: `kf-SequenceScene` **D25 / L-5 / C-7** is now a named G-KFW4-4 demo-arm site and its measurement re-derives exactly (`git show origin/master:demo/scenes/sequence/SequenceScene.vue | wc -l` → **43**; `sed -n '8p'` → `import { computed, provide, ref } from "vue";`; `grep -c 'computed('` → **0**).
- **The 13 W4-silent records hold no further escape**: ten return zero on a family-routing sweep; the three that carry cross-edge weight are all carried.
- **The cross-taxonomy trap is still correctly avoided** — `kf-StartingStyleTarget.md:112`'s *"K.W4 S5 de-duplication"* is tranche K and is not imported.
- **Sequencing references are correctly not claimed**: `KF-AX-16` (*"AXISLINE-UNIT after KF.W4"*), `LP-21` (→OPTIONS-UNIT), `KF-KE-17`, `SPF-1`, `KF-CB-33` — all verified as non-routings.

---

## §2 DEFECTS (12) — every figure re-executed at `81a56990`

### D-1 · **CRITICAL** — §Bounds row 78 mis-files G-KFW4-14's cure-lock subject: `seedFor` and `syncGap` are `EasingSidebar.vue`'s, not `useEasingDemo.ts`'s

Row 78 is a `modify-carve — **these lines only**` row. It reads:

> `EasingSidebar.vue` `:42-49` caption / `:86-89` comment · **`useEasingDemo.ts` `:112` `seedFor`, `:132-137` `syncGap`, `:255-257` the `[0,0,1,1]` reset**

At the Ref of record:

```
$ git grep -n 'seedFor\|syncGap' origin/master -- demo/scenes/easing/useEasingDemo.ts
(no output — ZERO hits, both names)

$ git grep -n 'seedFor\|syncGap' origin/master -- demo/
demo/scenes/easing/EasingSidebar.vue:99:  const seedFor = (name: string): PickerSeed | null => {
demo/scenes/easing/EasingSidebar.vue:112:      if (name in bezierPresets) {
demo/scenes/easing/EasingSidebar.vue:122:      seedFor(demo.currentEasingName.value) ?? {
demo/scenes/easing/EasingSidebar.vue:132:  const syncGap = (name: string) => {
demo/scenes/easing/EasingSidebar.vue:138:  syncGap(demo.currentEasingName.value);
demo/scenes/easing/EasingSidebar.vue:162:      syncGap(name);
demo/scenes/easing/EasingSidebar.vue:163:      const seed = seedFor(name);
```

The **line numbers are right and the file is wrong.** `seedFor`'s reach into `bezierPresets` is `EasingSidebar.vue:112`; `syncGap` is `EasingSidebar.vue:132-137`. In the file the row actually names:

```
$ git show origin/master:demo/scenes/easing/useEasingDemo.ts | sed -n '112p'
        if (name === "step-end") return "M 0,1 L 1,1 L 1,0";
$ git show origin/master:demo/scenes/easing/useEasingDemo.ts | sed -n '132,137p'
    // ── Preview sweep: NumericAnimation (direction: "alternate") ────
    // The 0→1→0 ping-pong is the keyframe sequence itself — a normalized phase
    …
```

An SVG-path return and a six-line comment. Only `:255-257` genuinely belongs to `useEasingDemo.ts` — and that one is **exact** (`} else {` / `// Non-bezier curve: reset to linear approximation` / `bezierControlPoints.value = [0, 0, 1, 1];`), which is what makes the other two so easy to miss.

**Why this is CRITICAL, not clerical.** G-KFW4-14's CURE-LOCK is quoted verbatim in three places: *"gate `seedFor` on `NAMED_EASING_BEZIER`, never merge the catalogues."* Its falsifier reads *"fails if the caption is corrected while `seedFor` still reaches `bezierPresets`."* Under row 78's own `these lines only` bound, the wave is authorized to carve the caption (`:42-49` ✓ exact) and the defending comment (`:86-89` ✓ exact) — and **not one byte of `seedFor`**, whose file/line pair the row never states. The gate is unsatisfiable inside its own §Bounds, and the falsifier's failure condition is the one thing the row makes unreachable.

This is PASS-2's D-2 class at a new row, in its worse form: D-2's `EditorShell.vue:235-237` resolved to *nothing* and was findable by a `grep -c`. This resolves to *something*, silently, and survives every anchor-existence check R2-5 installed. **Round 2 hunted dead anchors; it did not hunt anchors that resolve to the wrong bytes.**

### D-2 · **CRITICAL** — the ID-KEYED census escapes: five banked W4-routed ids booked nowhere

Enumerated with receipts at **§1.2**. `routedTotal 109 · bookedCount 104 · escapedCount 5`.

The structural fault is not any one drop; it is that **both prior passes re-used one nine-block partition and called the result an independent re-derivation.** PASS-1 built it; PASS-2 declared *"my re-derivation reproduces PASS-1's denominator member-for-member"* — which is true, and is exactly the problem: a partition cannot escape an id it never contained. Four of the five escapes (E1–E3, and the E4/E5 pair) live in sentences whose *siblings the spec books* — `KC-37` beside `KC-17`; `:128`/C-8/m-1-C-7 beside `m-8`; `C-21` beside `L-6/C-18`; `C-10/C-11/D-m6` beside `m-1/D-m5`. Every one is a partial harvest of a banked list, and a partial harvest is the failure mode an id-keyed census exists to catch.

M-25 is explicit that adjudicated rows are *"consumed by mechanism, deduped by identity, never transcribed."* Four of these five are deduped-away members of an identity the spec does book; the fifth (**KC-17**) is a distinct `noUncheckedIndexedAccess` witness — *"`frames: any[]` … **disables `noUncheckedIndexedAccess`** in the one file whose job is projecting the union"* — of exactly the flag pair rows 1 and 4 stand on, in the record that supplies the row's own ordering dissent.

### D-3 · **MAJOR** — `test/demo/instrument/` holds NINE tracked files, not ten; the spec's enumeration double-counts `resize-tracks.test.ts`

§Disjointness:

> At `81a56990` it holds **ten tracked files** — `KfPillTabs.test.ts`, `ios-text-entry.test.ts`, `kf-toolbar-keyboard.test.ts`, **`resize-tracks.test.ts`**, `timeline-undo.test.ts`, `transport-play-actuation.test.ts`, `useAnimationGroupPlayback.test.ts`, `useThrottledReadout.test.ts`, `value4-editor-boundary.test.ts` and (this wave's cure site) **`resize-tracks.test.ts`**

```
$ git ls-tree -r --name-only origin/master -- test/demo/instrument | wc -l
9
```

Nine distinct names, listed as ten by naming `resize-tracks.test.ts` twice. §Bounds row 67 compounds it independently — *"the **nine** other tracked files there are read-only witnesses of this wave"* (true count: **eight**) — while §Disjointness's own tail says *"the remaining **eight**"*, which is right and contradicts its own "ten".

The wave's AUDITED cell struck precisely this error one paragraph earlier (*"kf-CopyButton counted ONCE — the prior double-listing is struck"*). R2-12's shared-directory law is declared from this end on a count the declaration itself gets wrong, and a shared-directory declaration whose cardinality is inflated is not a disjointness proof.

### D-4 · **MAJOR** — the AUDITED basis arithmetic 47 / 41 / 6 does not reproduce; a seventh balance record is classified as a supplier

The AUDITED cell states **47 records consulted**, **41 supplying ≥1 W4-routed id**, and — the round-2 cure to D-14/D-9 — *"**The balance is SIX and its composition is stated exactly**"*: four carrying W4 bytes that resolve to no routed row (kf-CubeAxisLines 3 · kf-DemoGlobalChrome 1 · kf-EditorHeader 1 · kf-KeyframeCard 1) and two carrying **zero** W4 bytes (kf-KeyboardShortcutsModal, kf-SequenceScene).

All six of those measurements re-derive **exactly**. The problem is the seventh:

```
$ grep -cE 'KF\.W4|KF-W4|\bW4\b' kf-SequenceScrubber.md
0
$ sed -n '36p' kf-SequenceScrubber.md
Routing law: **KF.W0/W6/W7/W9** where a forming X·KF wave owns the surface; **NO-WAVE-OWNER** = SS-1/SS-2 spec-authoring intake …
```

kf-SequenceScrubber is one of the enumerated 47, carries **zero** W4 bytes, and routes to W0/W6/W7/W9 + NO-WAVE-OWNER — never to W4. It is structurally **identical** to kf-KeyboardShortcutsModal, which the same cell places in the balance while noting its row *is* carried (*"its **R-9** dissent is carried at row 1"*). kf-SequenceScrubber's ruling 12 is likewise carried (row 12) and its `:36` rider likewise carried (§Sequencing) — yet it is counted among the 41 suppliers. **Same shape, opposite classification, one cell.** The honest figures are **40 of 47, balance seven**, and the cell's own criterion (*"supply ≥1 W4-routed id"*) is what decides it.

### D-5 · **MAJOR** — §Bounds row 68's `(:56)` anchor belongs to a file the row does not name and §Bounds never declares

Row 68 (files: `test/demo/scenes/orbital-inertia-parity.test.ts` + `test/demo/scenes/orbital-rotate3d.test.ts`) reads *"`INERTIA_FACTOR = 0.92` vs the shipped `0.95` (`:56`, no consumer override)"*; carry row 19 repeats it verbatim.

```
$ git grep -n 'INERTIA_FACTOR' origin/master -- test/
…orbital-inertia-parity.test.ts:29:const INERTIA_FACTOR = 0.92; // the orbital-drag default friction-per-frame
$ git show origin/master:test/demo/scenes/orbital-inertia-parity.test.ts | sed -n '56p'
        const FRAMES = 120; // a ~2s window — long enough to span the whole glide
$ git grep -n '0\.95' origin/master -- demo/scenes/cube/
demo/scenes/cube/orbital-drag/OrbitalDrag.vue:56:const inertiaFactor = props.inertiaFactor ?? 0.95;
```

`:56` is `OrbitalDrag.vue`'s — a file named in **neither** the row nor §Bounds anywhere — while the pinned constant's own coordinate (`:29`) is never given. The mechanism is real and the recalibration is sound; the address is not.

This is the bare-leaf disease round 2 legislated against **in this same file**, at row 64: *"the bare leaf `types.ts` is struck (R2-5) … a pathless anchor here resolved to nothing checkable."* The same seat that struck a pathless `types.ts` left a file-less `:56` standing in the row carrying G-KFW4-9's central rule-(c)/(d) witness.

### D-6 · **MAJOR** — R-7's cure inventory understates `proof:idioms` by a site, and the missed site sits inside G-KFW4-8's own denominator

R-7's cell **(a) CURE INVENTORY** lists *"`idioms` **×2** (`design-idioms.css:3`/`:43`)"*. At the frontier:

```
$ git grep -n 'proof:idioms' origin/master -- demo/
demo/styles/layout.css:10:   / --mask-fade live in design-idioms.css (proof:idioms definition-anchors them
demo/styles/design-idioms.css:3:   so these are the AUTHORITATIVE demo copy (proof:idioms asserts each referenced idiom
demo/styles/design-idioms.css:43:    /* Three DRY layout tokens the demo owns HERE (proof:idioms anchors these; the rest
```

**Three** sites, not two. The isolation is what convicts: the cell's two sibling glosses re-derive exactly — `cursor-light-subtle` ×3 (`App.vue:49`, `HeroAurora:18`, `HeroAurora:44` ✓) and `typing-dots` ×3 (`TypingDots :45`/`:51`/`:59` ✓) — so this is a measurement miss, not a notation convention.

The name-level partition survives and is *sound*: **17 + 1 + 1 = 19**, total, and it re-derives at the bytes (the 19 distinct names extracted independently match the cell's roster member-for-member). But G-KFW4-8 asserts *"every `proof:*` / baseline / allowlist citation **at the EIGHT citation targets** resolves to an executable"* — that is a claim over all **33 lines**, and `layout.css` is both an enumerated target and a §Bounds file. A cure inventory naming two of three sites for a dead name leaves a dead citation standing inside the gate's own denominator, and the gate cannot go green. R-7's arithmetic is over names; the gate's oracle is over lines; the file needs both, and states only one.

### D-7 · **MINOR** — the whole-tree magnitude is 106 distinct `proof:*` names, not 107

§Excluded's closing paragraph, stating the sentence's own bound:

> `git grep 'proof:' origin/master -- demo/ src/ scripts/` finds a far larger surface — **273 citation-bearing lines carrying 107 distinct `proof:*` names**, re-measured at this round's clock

```
$ git grep -h 'proof:' origin/master -- demo/ src/ scripts/ | wc -l
273
$ git grep -h 'proof:' origin/master -- demo/ src/ scripts/ | grep -oE 'proof:[A-Za-z0-9_-]+' | sort -u | wc -l
106
```

Lines exact ✓; names off by one. The runnable-scripts tail re-derives exactly (`proof:structure` · `proof:publish` · `proof:owner-golden` at `package.json:50-52` ✓), which is the load-bearing half — the magnitude is decorative, and R2-5 still requires it to be the number the command returns.

### D-8 · **MINOR** — the fourth `types.ts` is `demo/scenes/cube/orbital-drag/types.ts`, not "the group twin"

§Bounds row 64: *"**four files of that name exist at the frontier** (`animation/constants/`, `animation/group/`, `animation/physics/spring/`, **and the group twin**)"*.

```
$ git ls-tree -r --name-only origin/master | grep -E '(^|/)types\.ts$'
demo/scenes/cube/orbital-drag/types.ts
src/animation/constants/types.ts
src/animation/group/types.ts
src/animation/physics/spring/types.ts
```

The cardinality **4** is right tree-wide; the enumeration names `animation/group/` twice and never reaches the real fourth, which is in `demo/`. The row's conclusion — that a bare `types.ts` leaf resolves to nothing checkable — survives on the corrected basis, and is if anything stronger for the fourth file living outside `src/` entirely.

### D-9 · **MINOR** — `usability.mjs:8` is not the header line the spec quotes; it is `:9`

§Bounds row 69, in the round-2 re-pathing cure, asserts what a line it does *not* touch contains:

> the roster docblock at `:20-31` (**NOT `:8`, which is the header line *"Three falsifiable clauses, each BITING on the exact defect it forbids"***)

```
$ git show origin/master:scripts/observe/demo/usability.mjs | sed -n '8p'
 *
$ git show origin/master:scripts/observe/demo/usability.mjs | grep -n 'falsifiable'
9: * Three falsifiable clauses, each BITING on the exact defect it forbids:
```

`:8` is a bare comment continuation; the quoted header is `:9`. Every other anchor in that row re-derives **exactly** — 410 L ✓, `:20-31` the `.wave-word`/`.wave-char` contract ✓, `:160-172` the `heroReady` 30×300 ms poll ✓ with `:164` the predicate and `:172` the FAIL text ✓, `:239` the `glyphCount` oracle ✓, `:188`/`:218` the live queries ✓, `:259` the failure text ✓, `:278-285` the `charCount === glyphCount` equality ✓. One off-by-one in a nine-anchor row, on the only anchor whose content the spec quotes.

### D-10 · **MINOR** — §Excluded 21 prints an unscoped command whose stated output is false

> **sole site** `demo/…/ChannelOptions.vue:168`, re-measured at `81a56990` (`git grep -n 'proof:bezier'` → **exactly one line**)

Run as printed, that command returns roughly ninety lines — the whole `docs/tranches/{H,I,J,L,M,S,T}` gate lineage. The **substantive claim is TRUE** under this file's own living-source scoping:

```
$ git grep -n 'proof:bezier' origin/master -- demo/ src/ scripts/
demo/components/instrument/transport/channel-controls/ChannelOptions.vue:168:  pencil hook proof:bezier-{no-scroll,single-card,grown}
```

One line, carrying all three names in brace form ✓. R2-5's own words are *"every anchor is re-executed at write time **and its command printed beside the figure**"* — the printed command is not the command that produces the figure, in the item that exists because round 1 named a witness the gate could not reach.

### D-11 · **MINOR** — the NO-SILENT-DELETION law reaches only `check`, while §Bounds opens `check:lib`, `lint` and `test:*`

The round-2 CRITICAL cure installed a wave-level law in G-KFW4-1's falsifier — *"fails if **the redefined `check`** at execution lacks ANY invocation present in `origin/master`'s `check` without a declared §Bounds removal row"* — with its three-part removal protocol.

§Bounds row 47 opens `package.json` for *"scripts (`check`, **`check:lib`**, **`lint`**, **`test:*`**)"*. `lint` is redefined by G-KFW4-11 (`origin/master:package.json:44` → `"lint": "depcruise src"`, verified) with no before/after quotation and no falsifier of its own; `check:lib` (`:38`, `tsc --noEmit -p tsconfig.lib.json`) is redefined by implication in G-1's CI-merge-step clause; `test:*` receives **no cure at all** across the nine §Commits lines. The law that exists because one live invocation vanished from one script does not reach the three sibling scripts the same row opens — and the falsifier's scope is the only thing that makes it executable.

### D-12 · **MINOR** — OP-5's discharging instrument is named generically after KF.W1 minted it by id

W4's OP-5 and its KF.W1 cross-edge call the O-11 §C cure *"the amendment-addendum"* and *"the mail cure"*. At the sibling spec that mint is now a **named outbound row**:

```
$ grep -n 'O-21' KF-W1.md   → :32, :79, :93, :136, :191, :194, :243, :284, :315 …
KF-W1.md:79:  mint outbound row **O-21** — the measured ledger maximum **O-20** (at `:95`) **+ 1** …
KF-W1.md:194: GREEN: … **O-21** minted naming (a)–(d); O-8/O-11's rows keep their text plus an **amended-by-O-21** pointer
```

**The posture axis itself is satisfied**: `grep -oE 'O-2[01]' KF-W4.md` → **zero hits**, so the retired `O-20` appears nowhere in this file, and W1's five load-bearing sites all carry `O-21` (R2-1 items 1–3, executed). The defect is one of carriage, not of staleness: W4 cites `O-11` seven times and its amendment zero times by id, in a file that otherwise names every instrument it depends on by id and absolute path (`KF-W6-CARRY.md`, §W6-C's receipt bullet, KF-W0's roster line, KF-W2's sibling-packet row).

---

## §3 AXIS-BY-AXIS

| axis | verdict | basis |
|---|---|---|
| **(1) ID-KEYED CENSUS** | **DEFECTIVE** | 109 / 104 / **5**. Blocks A–I reproduce; block J is new. Both prior passes re-used one partition and could not escape what it never held (D-2) |
| **(2) AUTHORITY REALITY** | **DEFECTIVE (tree side) · CLEAN (cross-spec side)** | Cross-spec: every receipt resolves by its declared label — see §4. Tree: D-1 (mis-filed cure-lock subject), D-5 (file-less `:56`), D-6 (missed `proof:idioms` site), D-7/D-8/D-9/D-10 |
| **(3) M-25 DEPTH** | **CLEAN, with the census caveat** | Every lock, rider and dissent this seat could test is present and correct against the bank — §4.3. The five escapes are census misses, not M-25 violations of a booked row |
| **(4) GATES born-RED, real witnesses AT THE FRONTIER** | **DEFECTIVE at one gate of fourteen** | Thirteen gates' witnesses re-derive at `81a56990` — §4.2. **G-KFW4-14 alone cannot close inside its own §Bounds** (D-1) |
| **(5) POSTURE** | **CLEAN** | Head honored on eight independently-verified witnesses (kf-AmigaScene correctly struck); the `check` re-cut **COMPOSES** with the live scripts; KF.W3 GATED and condition-bound, never scheduled; KF-AV-28 verbatim and correctly declared HOMING AUTHORITY; **`O-20` absent**; W10's obligations closed by carriage. One soft carriage MINOR (D-12) |

---

## §4 WHAT VERIFIES — the frontier, executed

### 4.1 Every PASS-2 CRITICAL and MAJOR is CURED at the bytes

| PASS-2 | cure claimed | re-executed here |
|---|---|---|
| **D-1** the head gate deleted a live gate | `check` re-cut preserves the tail | `git show origin/master:package.json \| sed -n '37p'` → `"check": "tsc --noEmit && tsc --noEmit -p tsconfig.test.json && npm run proof:structure"`. G-KFW4-1's after-form keeps `&& npm run proof:structure` verbatim, invoking `scripts/gates/structure/index.mjs` (`:50` ✓). **COMPOSES** ✓ |
| **D-2** `EditorShell.vue:235-237` phantom | struck; real site is `DESIGN.md:251` | `grep -c 'proof:' EditorShell.vue` → **0** ✓; `DESIGN.md:251` carries `proof:appearance-suffusion` ✓; R-4's three live sites (`DESIGN.md:251`, `EditorStartScreen.vue:15`, `layout.css:31`) all exact ✓ |
| **D-3/D-15** two phantom script paths | re-pathed to `scripts/observe/demo/` | both present; `usability.mjs` **410 L** ✓, `live-session.mjs` **1612 L** ✓; `scripts/usability.mjs` at no ref ✓ |
| **D-8** "structurally a green empty set" | struck; 27 tracked specs | `git ls-tree -r --name-only origin/master -- test/demo \| wc -l` → **27** ✓ |
| **D-9** SequenceScene omitted | added as a G-KFW4-4 site | 43 L ✓ · `:8` imports `computed` ✓ · `grep -c 'computed('` → 0 ✓ |
| **D-11** `_boundTimeline` arithmetic | three hits, one file | `:5` docblock · `:54` declaration · `:82` **live write** ✓ exact |
| — | `.spec.ts` → `.test.ts` | `vitest.config.ts:43-44` (`library`) and `:52` (`demo`) collect `*.test.ts` **only** ✓; all 27 demo specs use `.test.ts` ✓ |
| — | `test/animation/` → `test/compile/` | `test/animation/` at no ref ✓; `test/compile/` holds `roundtrip-easing.test.ts`, `value4-easing-contract.test.ts`, `default-easing-css-twin.test.ts` ✓ |
| — | `vitest.config.ts` re-anchor | `projects` `:38-56` ✓ · `library` `:39-47` ✓ · `demo` `:48-55` ✓ · glob `:52` ✓ · **no `plugins` array** ✓ |
| — | `@vitejs/plugin-vue` add STRUCK | already a devDep at `:81` (`^6.0.7`) ✓ — the no-op strike is correct |
| — | `tsconfig.json` include already satisfied | `:47` → `"include": ["src/", "demo/"]` ✓; `:8`/`:9` witness flags intact ✓ |

### 4.2 The gate witnesses, at the frontier

- **G-KFW4-1** — `vue-tsc` and `eslint` absent from the manifest ✓ (`grep -nE 'vue-tsc|eslint' package.json` → none); `demo/env.d.ts:3-7` = the `DefineComponent<{}, {}, any>` shim, exact ✓; `sceneExposedApi.ts:33` = `isStarted?: boolean;` exact ✓.
- **G-KFW4-2** — `ci.yml:50-52` the comment *"does not block library merges"* ✓; `:53-55` the `schedule || workflow_dispatch` gate ✓; the merge job's **four** lib steps verified (`check:lib`, `build:lib`, `test:lib`, `proof:publish`) ✓.
- **G-KFW4-3** — `MatrixEditor.vue:11` = `v-for="(value, i) in matrix3dEnd.args"`, keyless ✓; `lint` = `depcruise src` at `:44` ✓.
- **G-KFW4-4** — `load-engine.ts:65` = `import type { Stylesheet } from "@mkbabb/value.js/css";` exact ✓.
- **G-KFW4-5** — `registry.ts:30-34` `registryNames` ✓, `:36` the false stable-identity docstring ✓, `:43` the module-evaluation `throw` fence ✓; `easing-serialize.ts:71-73` the `.find(([_name, func]) => func === easing.fn)` reverse-map ✓.
- **G-KFW4-6** — `leaves.ts:19` *"match the value.js barrel"* ✓, `:28` the re-export ✓; `test/internal/leaves-parity.test.ts` present ✓.
- **G-KFW4-8** — **the full eight-target census re-derives exactly**: `DESIGN.md` 13 (`:29` · `:224` · `:235-240` · `:243` · `:250-253`) · `EditorStartScreen.vue` 5 (`:15`/`:56`/`:134`/`:159`/`:180`) · `App.vue` 3 (`:49`/`:117`/`:322`) · `HeroAurora.vue` 3 (`:5`/`:18`/`:44`) · `TypingDots.vue` 3 (`:45`/`:51`/`:59`) · `layout.css` 3 (`:10`/`:31`/`:152`) · `design-idioms.css` 2 (`:3`/`:43`) · `SpringPhysicsFacet.vue` 1 (`:118`) = **33 lines / 19 distinct names** ✓. The `App.vue:44`→`:49` and `HeroAurora:46`→`:44` re-anchors are correct ✓; `DESIGN.md:29` sits inside the `:24-30` Mono-as-data clause ✓; `DESIGN.md:236` carries `proof:style-file-ceiling` ✓.
- **G-KFW4-9** — `orchestration-api.test.ts:142-148` is the `it("BITE: …")` block, with `:146` the `cssTwinFor("bounceInEase")` assertion and **`:147` the `"not-a-real-easing"` in-file falsifier** ✓ exact; `resize-tracks.test.ts:137-139` = `fs.readFileSync` + the two `toMatch` source-text pins ✓; `scripts/gates/` = **nine** files ✓, the roster verbatim as listed.
- **G-KFW4-10** — `font-roles.json` `:16-26` the two `filing-tab` rows on `.tab-trigger-base[data-state=…]` ✓, `:34-38` the `h1.hero-display .wave-char` pin ✓, `:68` `kbd` ✓, `:82` `_monoContract` with clause (d) ✓.
- **G-KFW4-13** — `src/animation/constants/types.ts` `:25` the type opens ✓, `:27` `| "steps"` ✓, `:195` `| string` ✓; `easing.ts:44` and `waapi/eligibility.ts:169` the two surviving `bounceInEase` prose phantoms ✓ (and no live binding).
- **G-KFW4-14** — the central witness is **TRUE and re-derived by execution**, not by citation:
  ```
  $ node --input-type=module -e "import {bezierPresets} from '…/@mkbabb/value.js/dist/subpaths/easing.js'; …"
  bezierPresets count: 30 · NAMED_EASING_BEZIER count: 29
  presets \ named: [ 'smooth-step-3' ] · named \ presets: []
  ```
  A byte-exact strict 29-of-30 subset with sole delta `smooth-step-3` ✓. The caption at `EasingSidebar.vue:42-49` (*"is engine-native — editing here authors a custom cubic-bezier"*) ✓ exact; the defending comment at `:86-89` (*"wider (quart/quint) and differs on some quads (sine)"*) ✓ exact — and **false on both halves**, as the row claims. `test/demo/reference-data/easing-catalog.test.ts` exists ✓, so the not-a-duplicate argument holds. **Only the cure's address is wrong (D-1).**
- **R-5** — `git grep -c 'TypingDots' origin/master -- test/` → **0** ✓, so *"exactly one demo-lane spec for the TypingDots engine seam"* stands and the *"zero coverage for the LCP hero"* headline is correctly overruled.
- **§Excluded 17** — `proof:brittleness`'s three measured frontier sites re-derive **exactly**: `layout.css:152` · `style.css:38` · `scripts/lib/demo-driver.mjs:69` ✓. The declared carve at G-KFW4-8 is sound.

### 4.3 M-25 depth — locks, riders, dissents

- **KF-AV-28**, verified byte-for-byte at all three bank sites — `kf-AnimationVisualizer.md:35` (*"KF.W7's S-9 evaluation (KF-AV-28) may supersede any behavioral cure here"*), `kf-PlaybackRibbon.md:36` (with the C-axis §7 caution riding), `kf-SequenceScrubber.md:36`. The spec's §Sequencing clause matches in substance, correctly declares **this wave's governed rows = none** (it is the library half and spends no behavioural cure on an evaluated surface), and correctly re-casts the rider's weight here as **HOMING AUTHORITY**, travelling with the drag-seam and transport/ribbon packets. ✓
- **kf-SpringTrace's KF.W4-PROSE bucket carried WHOLE** — all four items at `:139`, with D-1's `:48-49` comment truth kept as a *sequencing rider* (*"rides the fix"*) rather than promoted or dropped ✓.
- **kf-KeyboardShortcutsModal R-9's denial of the row's own founding premise** carried at row 1, with its carve-out quoted whole and its supersession argued rather than assumed ✓.
- **Dissents preserved**: KC-13's ordering dissent (kf-KeyframeCardList §4, DU's a11y triad vs LC's type-gate head); C-13's vendor-DOM exemption; KF-ET-6's reader-1 BLOCKER against the banked MAJOR; C-4's MAJOR→MINOR demotion on RR-L's correction ✓.
- **MUST-CARRY riders in transit**: kf-ChromeDock M-4's MbabbMenu lock (glass-ui's bare `{ type: Boolean }` ⇒ the cure as worded ships an unopenable menu), handed to KF.W13; the F-5 shim repoint (`ChannelControls.vue:230` → `../KfPillTabs/useKfPillTabs`, **not** the `.vue`), handed to KF.W6 ✓.
- **The five dead sub-claims are still not re-imported** — MM-13's modality hazard (ruling 3: non-modal), C-4's running cost (`_withCtx` lazy closure), KF-CB-8's leg 2, D-4's WCAG rationale, *"zero coverage for the LCP hero"* ✓. This remains the file's single strongest behaviour.
- **Anti-rename holds**: no banked id was renumbered or re-homed away from its bank; the carry table stands at 31 rows; status stays `planned`. ✓

### 4.4 Cross-spec authority — every receipt resolves by its label

| receipt | claimed | resolved |
|---|---|---|
| KF.W6's **"R-6 MANIFEST RECEIPT (the KF.W4-PROSE limb intake)"** | *"sat at `:145-146` at this writing, non-load-bearing per R2-7"* | **present at `KF-W6.md:142`** ✓ — the heading is the anchor and it holds; the coordinate's declared non-load-bearing status is exactly why the drift is harmless |
| KF-W0's **canonical packet-roster line** | *"read at `:276` … at `:401` … and at **neither** by round-2 close — all three coordinates retired; a line number is not a receipt"* | **present at `KF-W0.md:447`**, and its 17 members match W4's list **member-for-member**; W0 states *"**Homing is discharged by KF.W4's lists**, not by this wave"* — reciprocal from both ends ✓ |
| KF-W2's **§Excluded sibling-packet row** | round-1/2 coordinate `:270` **struck**; §-heading + row label are the anchor | **present at `KF-W2.md:499`** ✓ |
| KF-W1 **cross-edge 9** | routes dock-menu to KF.W13 | `KF-W1.md:236-241` mints KF.W13 (dock-menu · transport/ribbon) and cites KF-W2's row reciprocally ✓ |
| KF-W9 **S-11's adds** | {amiga, EDITOR-UNIT} → 17 | `KF-W9.md:35` carries the 17 canonical packets; W0 confirms `17 = W0's 15 ∪ {amiga, EDITOR-UNIT}` and that *"the number 16 was never true"* ✓ |
| **W10's carry-routed obligations** | `home.json:9` · kf-AnimatedText's two stale doc authorities · the `345/12/57 @ 8281638c` boundary (B18-23/B19-14/B20-14) | `docs/tranches/T/stage-manifests/home.json` present at `origin/master` ✓; `KF-AT-21` (`r-animation-sota.md:109`) at the bank ✓; B18-23/B19-14/B20-14 and `345/12/57` resolve in `audit/codex-provenance/INTAKE-ADJUDICATION-2026-08-03.md` + the coordination set — **the basis this spec's AUDITED cell names** ✓. **Closed by carriage, not omission** ✓ |
| **§Sequencing's EIGHT head-witnesses** | kf-AmigaScene struck under R-8.6 | all eight independently state it (kf-CSSCodeEditor · kf-ChannelOptions `:150` · kf-KeyframeCardList · kf-KeyframesEditor · kf-CubeAxisLines `:34` · kf-LayerConfigPanel `:126` · kf-KeyframeTimeline `:111` · kf-TimelineHoverPreview `:87`) ✓; kf-AmigaScene's only *"sequence"* hits are D-1's headline sequence and the paused-stage row — **the strike is correct** ✓ |

---

## §5 THE ROUND-3 GUARDRAIL THIS FILE OWES ITSELF

Round 1's failure was named plainly and correctly: *repaired against the rulings, never against the tree.* Round 2 reversed it and the reversal is real. Round 3's failure is narrower and has two names:

1. **A denominator is not a re-derivation.** *"My re-derivation reproduces PASS-1's denominator member-for-member"* is a consistency check, not a census. The census axis must be re-built **from the bytes to the ids**, and it must resolve **whole sentences** — every *"Riders this component contributes"* list, every merged group's full id set, every *"this corpus adds the X and Y witnesses to that row"* — because a partial harvest of a banked list is invisible to any check that starts from the previous harvest.

2. **R2-5 must hunt anchors that resolve to the WRONG bytes, not merely to none.** Every dead anchor round 2 found announced itself with an empty `grep`. D-1's does the opposite: it resolves, cleanly, onto an SVG-path branch and a comment block, and it takes the wave's own cure-lock subject out of the wave's own bounds. The executable form of the guardrail is a **file+symbol** assertion, not a file+line one: *for every §Bounds row naming a symbol, `git grep -n '<symbol>' origin/master -- '<the row's own file>'` must return the stated line.* Run over row 78, that one command prints nothing — and would have caught this before the file was written.

**Carried forward for the repair seat**: D-1 and D-2 are blocking. D-3 through D-6 are corrections of measured figures whose conclusions survive. D-7 through D-12 are receipts to re-cut. Nothing in §4 needs re-litigating — it is verified at `81a56990` and may be relied on.
