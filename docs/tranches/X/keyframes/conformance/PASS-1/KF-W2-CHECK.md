# KF-W2 — FRESH ADVERSARIAL SPEC CHECK (L-18 / L-20, PASS 1)

**Spec under trial**: `/Users/mkbabb/Programming/value.js/docs/tranches/X/keyframes/waves/KF-W2.md` (278 L, X.KF.W2 · Parse Façade)
**Corpus authority**: the 58 `kf-*.md` records in `docs/tranches/V/megatranche/registry/adjudicated/`
**Substrate re-checked read-only**: keyframes.js `origin/master` `81a56990736ced5b5edde0b84c527680ac7689b1`
**Seat**: fresh adversarial checker. Zero writes outside this file. Zero product source opened for writing.
**Verdict**: **DEFECTIVE** — 47 routed · 28 booked · 19 escaped · 10 defects (6 MAJOR).

---

## §0 Method

"Routes to KF-W2" is read at the **terminal disposition**, in any markup — table cells, prose routings, dotted `KF.W2/W3` forms, `cross-ref KF.W2's registry` posture edges, and R1 census cells recorded *"so parser waves skip this file"*. Two namespaces collide on the glyph `KF.W2` and are separated before counting (see §4).

Instruments (all read-only): `grep -nE 'KF\.?W2|KF-W2'` over the 58 (70 hit lines); a widened `\bW2\b` pass (10 further files, all routing-law preambles); a semantic pass on `parse fa[çc]ade|parser lane|parser wave`; a symbol pass on `parseCssColor|parseCssScalar|parseCssValues?|parseTimingFunction|parseStylesheet|parseKeyframeSelector|serializeCssValue|coerceToSyntax` (22 files); an R1-identity pass (26 files); and `git show origin/master:<path>` / `git grep <rev>` byte checks in keyframes.js.

---

## §1 ID-KEYED CENSUS — BOOKED (28)

Each row: banked id ⟨record⟩ — banked terminal disposition — where the spec carries it.

| # | id | record | banked routing (bytes) | carried at |
|---|---|---|---|---|
| B1 | **KF-APP-56** | kf-App:111 | *"fold by reference → megatranche R1 / census risk-3 (KF.W2/W3 own the class; App books nothing)"* | §Carry F0 + negatives |
| B2 | **KF-CE-12** (R1 ingress arm only) | kf-CSSCodeEditor:46,:134 | *"the `:116` fuzz-entry note folds to the R1 identity in the parser lanes (KF.W2/KF.W3); no new row"* | F0, arm-split lock quoted verbatim |
| B3 | **R-4** | kf-CSSPasteDialog:42 | *"→ KF.W7 … cross-referenced to KF.W2's posture registry so the façade counts it"* | F3 (sixth posture) |
| B4 | **C-4** | kf-CSSPasteDialog | R1 negative, *"Cleared — do not re-file"* | F0 verified negatives |
| B5 | **KAD-9** | kf-KeyframesAddDialog:53 | *"→ KF.W7 + cross-ref KF.W2's failure-posture registry (as the twin's R-4 did)"* | F3 (seventh posture) |
| B6 | **K-5** (axis C) | kf-KeyframesAddDialog | *"CONTAINED … Cleared — do not re-file"* | F0 verified negatives |
| B7 | **MISS-β2** | kf-TimelineCaret:46 | three serializer copies, none exported; §4.4 lockstep | F4 + G-W2-8 (conditioned) |
| B8 | **C·§4** | kf-CubeAxisLines:108 | *"C·§4's R1 negative → megatranche R1 (KF.W2/W3)"* | F0 verified negatives |
| B9 | **C-19** | kf-EditorShell:77 | *"FOLD (parser class) → kf-App KF-APP-56 ≡ megatranche R1 (KF.W2/W3 own the class)"* | F0 + G-W2-6 |
| B10 | **KF-KE-54** | kf-KeyframesEditor:99 | *"the ingress note folds by reference … → KF.W2/W3 scoping input"* | F1 |
| B11 | **KF-KE-3** | kf-KeyframesEditor | BLOCKER → KFED-UNIT (`requireKeyframeSelector` at the field) | F1 + G-W2-6 contract |
| B12 | **KF-KE-46** | kf-KeyframesEditor:91 | → KFED-UNIT, *"the third skipped-helper site"* | G-W2-6 corroboration |
| B13 | **C-7** | kf-KeyframeTimeline:48,:112 | split lock: crash FOLDS to R1; *"the fuzz-entry note rides KF.W2/KF.W3"*; posture → KF.W7 *"cross-ref KF.W2's posture registry"* | F0 + F3 (eighth posture) |
| B14 | **L-6/C-4** | kf-KeyframeTimeline:47 | *"→ KF.W7 (delegate), cross-ref KF.W2 (the parse façade this bypass would have used)"* | F4 + G-W2-7 |
| B15 | **Axis C's S-C R1 note** | kf-KeyframeCardList:122 | *"folds to the megatranche R1 identity in the parser lanes (KF.W2/KF.W3)"* | F0 verified negatives |
| B16 | **SUP-3** | kf-TimelineTrack:130 | *"value.js consumption is minimal, granular, and R1-contained"* | F0 verified negatives |
| B17 | **C·S-4** | kf-ChannelOptions:126,:155 | *"the banked R1 class … FOLDED by reference to the value.js parser lane"* | F0 (non-string arm) |
| B18 | **KF-CB-33** | kf-CopyButton:81 | negative UPHELD + PREDICTIVE → **KF.W3** scoping input; organ lock | F0 organ lock, quoted verbatim |
| B19 | **KF-KC-53** | kf-KeyframeCard:101 | *"Same posture as banked KF-CB-33 → recorded as a KF.W3 scoping input"* | F0 verified negatives |
| B20 | **LP-24** | kf-LayerConfigPanel:79,:98 | *"folded by reference onto the banked R1 identity so no later lane re-opens it"* | F0 verified negatives |
| B21 | **MM-37** | kf-MbabbMenu:93,:131 | *"exposure-not-reachability, ≡ the census R1 line"* | F0 verified negatives |
| B22 | **#11** (superlative) | kf-CubeTarget:76 | *"The R1-parser-unreachability negative proof … sustained through two hostile re-reads"* | F0 verified negatives |
| B23 | **C's L-1** | kf-ChannelControls:126 | *"the value.js R1 blast radius through this subtree is ZERO"* | F0 verified negatives |
| B24 | **KF-HA-19** | kf-HeroAurora:66 | **NO-WAVE-OWNER** — *"a value.js-side registry carry … never a KF wave row"* | F0 (measurement confirmed / conclusion retired) |
| B25 | **K-6** | kf-SquareInstrument:24,:100 | KILLED-BY-EXECUTION cell; residue → useSquareTumble's lane | F0 — **but MISSTATED, see D-2** |
| B26 | **C-2 ≡ L-3** | kf-SpringTrace:48 | NO-WAVE-OWNER; serialize→regex-reparse one layer out | F2 evidence + §Excluded |
| B27 | **KC-1** | kf-KeyframeCardList:33 | → CARD-UNIT; cure *"`selectorText(frames[i].start)` — one line, one import"* | F4 citation correction (cite verified EXACT) |
| B28 | **KF-KE-1** | kf-KeyframesEditor:40 | census FE-3 identity, never re-booked | F4 (*"KF-KE-1's framing survives"*) |

**Fold-identity carries also verified present**: lane-library §4.1 (Tier-A), §4.2 (cssom/Tier-B), §4.3 (Tier-C six), §4.4 (Tier-D lockstep), §4.6 (R1 crash-surface roster), §7.5 (five library postures) — all named and consumed. KF-CE-12's arm-split, C-7's split lock, R-4's/KAD-9's cross-ref locks, MISS-β2's §4.4 lockstep and the KAD dissent (`kf-KeyframesAddDialog.md:133` §DISSENT ¶1, *"The XSS stays BLOCKER (vs the twin's demoted pair)"*) are **carried, not merely cited** — M-25 satisfied on these 28.

---

## §2 ID-KEYED CENSUS — ESCAPED (19), named by bytes

None of the following appears anywhere in `KF-W2.md`. Each is a banked cell whose terminal disposition lands in the parse/R1 class this wave declares itself the census of record for.

### 2a · Boundary-set escapes with explicit do-not-re-probe locks (2) — the sharpest

| # | id ⟨record:line⟩ | banked bytes |
|---|---|---|
| **E1** | **value.js / R1 unreachability** ⟨kf-KeyboardShortcutsModal:78⟩ | *"(C-§1, R-B independently re-traced 62/11/1): CLEARED — the R1 crash class is not on this component's graph; same containment posture as the banked kf-CSSPasteDialog row. **Do not re-file.**"* |
| **E2** | **C-10** ⟨kf-DemoGlobalChrome:77⟩ | *"zero value.js/engine edges; R1 reachability CLEAN … Identity guard: respects value.js's banked R1 row; **recorded so parser waves skip this file**. … No wave."* |

E2 states, in the bank's own words, exactly the function G-W2-5's assertion claims (*"the boundary set is recorded as negatives so no later seat re-probes it"*). Both escape.

### 2b · Failure-posture escapes (3) — G-W2-1's count is short

| # | id ⟨record:line⟩ | banked bytes |
|---|---|---|
| **E3** | **D-12 = L-6 = C-4** ⟨kf-SpringTrace:56, packet at :140⟩ | *"silent-swallow error posture: an unparseable stop becomes `{v: 0}` — indistinguishable from a legitimate baseline datum — while the header still counts it as plotted … violates the engine's own fail-explicit law"*; the packet names it **"the parser posture (D-12/L-6/C-4 + the filter-cell)"** |
| **E4** | **C-2** ⟨kf-KeyframesStringControls:63⟩ MAJOR | *"the pane's only content-production path is unguarded over a throwing, value.js-bearing serializer … the chain ends at `css-text.ts:54` `throw new TypeError("Value returned an unserializable CSS color.")` … **only the mount path is bare**. … Disposition: absence-of-posture → NO-WAVE-OWNER; reachability → KF.W3"* |
| **E5** | **D-6 / L-BL-3 / C-1** ⟨kf-KeyframesStringControls:60⟩ MAJOR | *"`formatEditor` **has no error posture**: a prettier rejection latches `isFormatting` true for the life of the never-unmounted instance, permanently muting the success channel, with both callsites bare."* |

### 2c · Square-packet escapes (2) — the packet the spec HOMES here

| # | id ⟨record:line⟩ | banked bytes |
|---|---|---|
| **E6** | **D-27 / L-7 / C-9** ⟨kf-SquareScene:53, identity at :125⟩ MAJOR | *"Five `throw` sites execute inside the rAF frame (`num()` ×2, `colorAt` ×3) with no engine guard, and a throw does not drop a frame — it BRICKS the loop for the mount's lifetime"*; :125 — *"The R1 parser crash-class note inside D-27/L-7/C-9 ≡ the megatranche R1 identity (V·π parser program) — annotated, never re-booked."* |
| **E7** | **superlative 3 / C S-C** ⟨kf-SquareScene:132⟩ | *"`parseCssScalar` at the two-writer boundary … the demo's best use of value.js and the counter-example to the library's own internal regex re-parsing; **worth citing upward when the parser wave lands**."* |

### 2d · Easing/CSS-organ escapes (2) — against KF-CB-33's carried organ lock

| # | id ⟨record:line⟩ | banked bytes |
|---|---|---|
| **E8** | **KF-ET-2** ⟨kf-EasingTarget:41⟩ MAJOR | *"My probes against value.js's own grammar: `parseTimingFunction("ease-in-bounce")` and `("smooth-step-3")` **FAIL**"*; the corpus-unanimous cure would put **20 of 28** specimens on invalid CSS |
| **E9** | **KF-TFP-20** ⟨kf-TimingFunctionPanel:63⟩ MINOR, NO-WAVE-OWNER | *"`kind` runs value.js's full timing-function grammar once per pointermove … a parser recovering a two-valued tag the handler already holds verbatim as `v.mode`"* |

### 2e · Open parser-lane question routed upward (1)

| # | id ⟨record:line⟩ | banked bytes |
|---|---|---|
| **E10** | **C-L-2 residual** ⟨kf-CubeScene:170⟩ | *"whether value.js `parseCssValue`/`parseCssScalar` internally dispatch into the colour grammar for non-colour declarations (the C-L-2 residual, **untraced by all five seats**) — routed as a census annotation to the V·π parser program"* — answerable by this spec's own F0 execution; neither answered nor recorded |

### 2f · R1 verified negatives / boundary cells (9)

| # | id ⟨record:line⟩ | banked bytes (abridged) |
|---|---|---|
| **E11** | **SUP-1/2/3** ⟨kf-AnimationControlsGroup:106⟩ | *"the direct cause of the R1-unreachable verified negative — value.js's banked R1 row respected, not re-booked"* |
| **E12** | **S+2 / superlative 6** ⟨kf-AmigaScene:150⟩ | *"≡ census R1 crash-surface row (lane-library §4.6) — amiga is the *negative* exemplar (zero exposure); the R1 row itself stays owned by KF.W3"* |
| **E13** | **C-10** ⟨kf-AnimatedText:105⟩ | *"the R1 crash-class exposure delta of adopting `splitText` is exactly zero"* |
| **E14** | **SUP-7** ⟨kf-AnimationVisualizer:124⟩ | *"parser-free subpath (no R1 crash class in this call graph)"* |
| **E15** | **★ S-7** ⟨kf-SpringPhysicsFacet:107⟩ | *"the R1 crash class does not reach this subtree … every user-driven parse below the facet runs through `withErrorToastAsync` with Retry"* (also a posture datum) |
| **E16** | **superlative 3 / C S★1** ⟨kf-OrbitalDrag:132⟩ | *"a 1,110-byte zero-import leaf making the R1 crash class unreachable by module topology while a sibling scene took the exposed edge"* |
| **E17** | **S+3 / S-d** ⟨kf-TimelineHoverPreview:112⟩ | *"structural immunity to the R1 crash class by construction"* |
| **E18** | **S-C2** ⟨kf-EditorStartScreen:113⟩ | *"the one string that crosses into value.js was EXECUTED against the installed 4.0.0, not reasoned about … Given the parser lane's R[1]…"* |
| **E19** | **C's S★-3** ⟨kf-TypingDots:89⟩ | *"the one string crossing the package boundary checked against the installed `@mkbabb/value.js@4.0.0` dist"* |

**Escaped total: 19.**

---

## §3 NO INVENTION (M-25) — one fabricated authority, everything else traced

Every substantive cite in the spec was re-checked against the bank. **All of these verified EXACT**: KF-APP-56, KF-CE-12's arm-split verbatim, C-7's split lock verbatim, R-4's precision correction, KAD-9's cross-ref, KF-KE-3/54/46, KF-KC-53, LP-24, MM-37, kf-CubeTarget #11 (record superlative 11), kf-ChannelControls C's L-1, KF-CB-33's organ-lock sentence, C·S-4, KF-HA-19's NO-WAVE-OWNER routing, MISS-β2's three limbs and the 8-integer corruption set, **KC-1's prescription** (`kf-KeyframeCardList:33` — *"The correct serializer is used by the library one function away (`selectorText`, format.ts:131) … Cure: `selectorText(frames[i].start)` — one line, one import"*, cited exactly), the KAD dissent (§DISSENT ¶1). The spec's *"11 records fold to R1"* enumeration is faithful.

**DEFECT — invented authority.** The spec invokes **"the kf-SquareScene law (comment-stated invariants are test obligations)"** twice — §Carry F2's cssom bullet and **G-W2-4's assertion** (*"Under the kf-SquareScene law the `:28-33` comment becomes a **test obligation** — either the fixture covers the stated robustness bound or the comment is corrected in the same commit"*) — and a §Sequencing commit-family lock is written on it. **No such law exists in `kf-SquareScene.md`.** The record's nearest cells are MISS-3 (`:88`, MINOR — a docblock asserting the opposite of its own code), MISS-9 (`:103`, INFO) and L-16/C-10 (`:82`); the only thing the record calls a "law" is `animationGroup.pause()`'s repo-named playback law (`:25`, `:46`). A gate assertion and a commit-family lock rest on a named authority that is not in the bank.

**DEFECT — bank misstated (K-6).** §Carry F0 states: *"kf-HeroAurora KF-HA-19 and kf-SquareInstrument K-6 are CONFIRMED IN MEASUREMENT and REFUTED IN CONCLUSION … but **neither probed the empty-argument form**."* False for K-6 by bytes — `kf-SquareInstrument.md:24`: *"Bonus: `parseCssColor("oklch()")` — **the true R1 degenerate form** — throws an uncaught TypeError in my run, confirming R1 is real AND unreachable from any token (`getPropertyValue` yields `""` for undefined tokens…)"*; and `:100`: *"R1's true input is empty-args `oklch()` (throws in my run) **which no token can produce** (`getPropertyValue` → `""` → the `if (value)` guard). Safe regardless of cascade order."* K-6 probed the exact form the spec says nobody probed, reached the same measurement, and added a **reachability guard finding** the spec does not carry. §Sequencing then asserts `useSquareTumble.ts:22` is *"lane-library §4.6's named R1 crash surface, now measured: it throws on `oklch()`/`calc()`"* — stated without K-6's guard, and without kf-SquareScene C-1's finding (`:44`) that the colour write is dead paint. The spec's *"sharpest single argument"* is built on a false novelty claim.

---

## §4 NAMESPACE COLLISION — undeclared

Two taxonomies collide on the glyph `KF.W2`:

- **X·KF / CENSUS-2026-08-03 §(a)** — `W0 Substrate Settle · W1 Mail Cure · **W2 Parse Façade** · W3 Parser Consumption (GATED) · W4 K-Quartet Hygiene · W5 D/L/C Library Audit · W6 Glass Suffusion · W7 Timeline Evaluate · W8 Structure & Colocation · W9 Safari Visual · W10` (quoted verbatim in kf-CopyButton:36, kf-EasingScene:33, kf-EasingTarget:33, kf-KeyframeCard:36, kf-KeyframesEditor:36, kf-EasingSidebar:32, kf-TimingFunctionPanel:34). **This is the spec under trial.**
- **lane-frontend §10** — `KF.W1-DEP · **KF.W2-TABS** · KF.W3-SHIM · KF.W4-PROSE · KF.W5-PARTIALS · KF.W6-TIMELINE · KF.W7-TOKENS` (kf-ChannelControls:17, kf-AnimationControlsGroup:17, kf-KfPillTabs:19, kf-ControlsPaneWrapper:6, kf-DemoGlobalChrome:17).

**~25 banked rows** route to the string `KF.W2-TABS` — kf-ChannelControls §0/C-1/D-3/L-5, D-9, C-13, L-6 (the T.G9 re-home obligation), D-11, SUP-4; kf-KfPillTabs D:D-4, C:C-3.2, D:D-16/C:C-5/L:D-12, and the RR-2 grade-reinstatement rider at :115; kf-AnimationControlsGroup RR-D-NEW; kf-EditorShell **L-15/C-9**; and the header preambles. A binding ruling homes them elsewhere — `KF-W6.md:10`: *"lane-frontend §10's **KF.W2-TABS** / W3-SHIM / W5-PARTIALS / W7-TOKENS all **COLLAPSE INTO THIS WAVE**; (binding, adjudicated KF-W4 §11 ¶1)"* — so no content is orphaned. **But `KF-W2.md` never states the disambiguation**, in a spec that otherwise records the Codex intake and five whole packets *"so the grep trail shows them considered"*. Any seat grepping `KF.W2` over the registry gets 70 hit lines of which ~25 are a different wave, with nothing in this file to separate them.

Recorded here as **EXCLUDED-BY-RULING-BUT-UNDECLARED** (not counted in the 19 escapes).

---

## §5 GATES — born-RED with real witnesses (L-19): **PASS**

Every witness anchor re-resolved read-only at `origin/master 81a56990` by this seat. **All EXACT:**

| assertion | verified |
|---|---|
| `src/animation/compile/parse-facade.ts` **absent** (the façade is DECLARED, not created) | `git cat-file -e` → ABSENT ✓ |
| `ingest/cssom.ts:214-216` unescaped identifier interpolation | ``const nameRe = new RegExp(`\\banimation(?:-name)?\\s*:[^;}]*\\b${name}\\b`,);`` ✓ verbatim |
| `presets/catalog.ts:16` Tier-C | `/^\s*@keyframes\s+[^\s{]+\s*\{([\s\S]*)\}\s*$/.exec(css)?.[1] ?? css;` ✓ |
| `engine/composition.ts:175` (spec's drift correction from banked `:176`) | `const nums = raw.match(/-?\d*\.?\d+(?:e[+-]?\d+)?/gi);` ✓ |
| `svg/draw-svg.ts:89` | `if (!/^\s*\d*\.?\d+\s*%\s*$/.test(v)) {` ✓ |
| `emit/view-transition.ts:136` `CQ_UNIT_RE` (drift from banked `:159`) / `:146` reparse | `const CQ_UNIT_RE = …` / `for (const m of body.matchAll(/([\w-]+)\s*:\s*([^;]+);/g))` ✓ |
| `emit/format/format.ts:341` candidate 7th | `return \`${keyframes.replace(/\(\s*\{/g, "{").replace(/\}\s*\)/g, "}")}\n\`;` ✓ |
| `emit/format/format.ts:20` copy 1 · `emit/css-text.ts:58` copy 2 · `selector.ts:42` exported `namedSelectorToFraction` | `const selectorText = …` / `const serializeSelector = …` / `export const namedSelectorToFraction = …` ✓ (the E-3 precision correction to MISS-β2 is **right**: the symbol IS exported) |
| `emit/index.ts` exports no keyframe-selector serializer | read whole ✓ |
| `test/fixtures/keyframes/` = 14 `.css` + `manifest.json` | `git ls-tree` → 15 entries ✓ |
| `test/ingest/` exists with 3 tests | ✓ |
| `test/compile/valuejs-contract.test.ts` · `grammar-fuzz.test.ts` · `roundtrip-fidelity.test.ts` exist | ✓ |
| `GATE-VERDICT.md:42` *"no known consumer feeds the crash shape"* | ✓ live at `:42-43` |
| **G-W2-2's denominator = 21** | reproduced exactly by independent `git grep`: **11** grammar-entry sites (`adapter.ts:222`, `easing/registry.ts:131`, `emit/format/options.ts:104`, `selector.ts:24`, `value/compile.ts:32`, `engine/options.ts:31`, `resolve/browser.ts:162`, `scroll/grammar.ts:77/:85/:109`, `validate.ts:182`) + **10** collector sites (`adapter.ts:205/:241/:341/:374/:377`, `metadata.ts:42/:102`, `scroll/grammar.ts:111/:112`, `validate.ts:186`) ✓ |
| 27 `src/` files name `@mkbabb/value.js/css`; 15 carry a runtime statement | ✓ both reproduced |
| six `boundsFiles` path-drift corrections | all six re-resolved paths EXIST at `origin/master`, all six CARRY spellings ABSENT ✓ |

No proof-script contrivance: every gate's falsifier is a file:line, a fixture, or an executed matrix. **G-W2-8 is correctly conditioned on OP-6** rather than asserted. **This axis holds.**

**One count-gate caveat**: G-W2-1's assertion *"**8 postures** enumerated in one place, scope spanning `src/` and `demo/`"* makes the count **the gate**. E3/E4/E5 above put at least three more banked postures in scope, so the gate is born-RED at a number the registry already refutes — the identical failure mode the spec convicts the charter of (*"the charter's '5 postures' is provably 8"*).

---

## §6 E-3 + STATUS — **CLEAN**

- `VERIFIED` appears twice: the verb table (`| VERIFIED | **NO** |`) and *"VERIFIED NEGATIVES"* as a census-cell label. **Zero VERIFIED stamps.** ✓
- `**Status**: **planned**` at `:5`, restated at `:277`; `EXECUTION IS NOT AUTHORIZED BY THIS FILE` at `:17`. ✓
- No execution verbs in current voice; all product-facing acts are future/declarative (`create`, `modify`, `DECLARED, not created by this file`). ✓
- Opens no product source: the only declared write is the spec itself; §Bounds' *Do NOT touch* list names `scripts/dev/dev.sh` (NEVER) and the pinned `GATE-VERDICT.md` with the addendum-not-rewrite rule. ✓
- E-3 honoured throughout: the MISS-β2 `namedSelectorToFraction` correction, the `keyframe_selector_invalid` code precision, the `parseCssScalar("from") → ok:true` correction and the §F-2 falsification are all recorded **as corrections beside** the dated records, never as rewrites. ✓

---

## §7 POSTURE AXES

| axis | binding? | finding |
|---|---|---|
| **KF.W4 is the DECLARED SEQUENCING HEAD** | YES | HONOURED. OP-4 states KF-CE-16 ≡ KF-APP-4 as *"the sequencing head of the whole record"*, scopes it to the demo-side arms only with `check:lib` as the `src/` carrier, §Sequencing ¶5 defers the KFED-UNIT call-site edit behind it, and the cross-edge owes W4 the corrected 21-site/15-importer denominator. ✓ |
| **KF.W3 GATED via PLAW-BIND, never scheduled** | YES | HONOURED. OP-5 *"KF.W3 owns the repin … **KF.W2 MUST NOT REPIN**"*; §Sequencing ¶6 *"No repin"*; the cross-edge is labelled `KF.W3 · Parser Consumption (GATED)`; the F0 matrix is handed forward as W3's before-table. No W3 act is scheduled here. ✓ |
| **KF.W7 carries the KF-AV-28 supersession rider** | YES, and **NOT SQUARED** | `KF-W7.md:7` — *"**Standing supersession rider (KF-AV-28), the wave's defining lock**: this wave's S-9 evaluation may supersede any behavioral cure — a per-surface SWAP verdict discharges the bespoke rows banked under that surface with the component"*, with `KF-W7.md:234` putting **KeyframeTimeline (312 L)** and **TimelineHoverPreview (38 L)** in the verdict table. `KF-W2.md` contains **zero** occurrences of `AV-28`. G-W2-7's assertion — *"The façade publishes both halves L-6/C-4 hand-rolled … so KF.W7's delegation has a target"* — and the KF.W7 cross-edge are written as if the delegation target is unconditional; under KF-AV-28 a SWAP verdict on the timeline cluster discharges L-6/C-4 with the component and the emit-half obligation is moot. The edge is declared from W7's end and unacknowledged from this one. |
| **KF.W6 / 424-row CARRY** | NO | Binds W6 only. The spec's one W6 edge (KF-APP-15 → KF.W6) matches `kf-HeroAurora:55`'s routing of KF-HA-13 to KF.W6. ✓ |

---

## §8 PROVENANCE — the CARRY denominator is unciteable

The verb table asserts *"the CARRY consumed whole (**11 rows, 11 carried, 0 dropped**)"* and §Carry repeats *"11 rows in the CARRY, 11 landed below, 0 dropped"*. `docs/tranches/X/keyframes/carry/` contains exactly one file — **`KF-W6-CARRY.md`**. There is no KF-W2 CARRY artifact in the tree, and the spec itself notes the CARRY names a spec home (`docs/tranches/X/kf/waves/KF.W2.md`) that does not exist either. The fold-verification claim is therefore **unauditable**, and its denominator (11) is not the registry's denominator (47 routed rows by this census). Under M-25 the CARRY is agglomeration input, not the authority; the authority is the 58 records, against which the spec books 28 and drops 19.

---

## §9 DEFECT REGISTER

| # | severity | claim | receipt |
|---|---|---|---|
| D-1 | **MAJOR** | Invented authority: "the kf-SquareScene law (comment-stated invariants are test obligations)" is not in the bank; G-W2-4's assertion and a §Sequencing commit-family lock rest on it | `KF-W2.md:159,:200,:236` vs `kf-SquareScene.md` (nearest cells MISS-3 `:88` / MISS-9 `:103`; the only named "law" is the playback law at `:25/:46`) |
| D-2 | **MAJOR** | The bank is misstated on K-6: the spec claims *"neither probed the empty-argument form"* to retire K-6's conclusion; K-6 probed `parseCssColor("oklch()")`, reported the throw, and added an unreachability guard finding the spec drops | `KF-W2.md:124` vs `kf-SquareInstrument.md:24` (*"the true R1 degenerate form — throws … confirming R1 is real AND unreachable from any token"*) and `:100` |
| D-3 | **MAJOR** | 19 registry-routed cells escape by bytes; the "boundary set" G-W2-5 claims is short by ≥11 verified negatives, two carrying explicit locks addressed to parser waves | `kf-KeyboardShortcutsModal.md:78` (*"Do not re-file"*); `kf-DemoGlobalChrome.md:77` (*"recorded so parser waves skip this file"*); §2 above |
| D-4 | **MAJOR** | G-W2-1's count-as-gate is born-RED at a false number: ≥3 further banked postures exist beyond the declared 8 | `kf-SpringTrace.md:56/:140` (*"silent-swallow error posture"*, *"the parser posture"*); `kf-KeyframesStringControls.md:63` (*"only the mount path is bare"*), `:60` (*"`formatEditor` has no error posture"*) |
| D-5 | **MAJOR** | §Excluded asserts the spring packet carries "no parse row" — refuted by the packet's own text | `KF-W2.md:270` vs `kf-SpringTrace.md:140` (*"with the parser posture (D-12/L-6/C-4 + the filter-cell)"*) |
| D-6 | **MAJOR** | §Sequencing homes the square packet's parse arm here but drops the square record's own parse rows and asserts a reachability the bank guards | `KF-W2.md:238` vs `kf-SquareScene.md:53` (rAF-brick MAJOR), `:125` (R1 identity), `:132`, `:44` (dead-paint C-1), `kf-SquareInstrument.md:100` (`if (value)` guard) |
| D-7 | **MINOR** | KF-AV-28's supersession rider — KF.W7's "defining lock", which can discharge L-6/C-4 with the component — is unacknowledged; G-W2-7's delegation-target obligation is written as unconditional | `grep -c 'AV-28' KF-W2.md` → **0**; `KF-W7.md:7,:234` |
| D-8 | **MINOR** | The `KF.W2-TABS` namespace collision (~25 banked rows) is nowhere declared, breaking the grep trail the spec is otherwise meticulous about | `kf-ChannelControls.md:17`, `kf-KfPillTabs.md:19`, `kf-EditorShell.md:55`; binding collapse at `KF-W6.md:10` (*"adjudicated KF-W4 §11 ¶1"*) — absent from `KF-W2.md` |
| D-9 | **MINOR** | Provenance unauditable: the "11 rows, 11 carried, 0 dropped" fold-verification cites a CARRY artifact that does not exist in-tree | `ls docs/tranches/X/keyframes/carry/` → `KF-W6-CARRY.md` only; `KF-W2.md:13,:103` |
| D-10 | **MINOR** | The easing/CSS-organ census is incomplete despite carrying KF-CB-33's organ lock: the corpus's only executed `parseTimingFunction` conformance measurement and its only live per-move grammar consumer are absent | `kf-EasingTarget.md:41` (*"`parseTimingFunction("ease-in-bounce")` … **FAIL**"*); `kf-TimingFunctionPanel.md:63` |

---

## §10 WHAT SURVIVES

The spec's measurement work is sound and independently reproducible: the 21-site denominator, the 27/15 importer split, the six-path drift table, every Tier-C/Tier-D/cssom byte anchor, the fixture inventory, and the `parse-facade.ts` absence all reproduce exactly at `origin/master 81a56990`. The E-3/status axis is clean. Gate construction is honest — G-W2-8's conditioning on OP-6 is exactly the L-19 discipline. The three declared fold repairs (G-W2-2 symbol-scoping, the `coerceToSyntax` cell, the `keyframe_selector_invalid` code) are real corrections, and the MISS-β2 `namedSelectorToFraction` E-3 correction is verified right at the bytes.

**What fails is the census, not the craft**: an ID-keyed pass finds **47** routed rows against the spec's self-declared **11**, with **19** escaping by bytes, one **fabricated authority** carrying a gate assertion, and one **misstated bank row** carrying the wave's headline argument.

**verdictLocal: DEFECTIVE.**
