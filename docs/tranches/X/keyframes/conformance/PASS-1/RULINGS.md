# X·KF REPAIR ROUND 1 — CROSS-WAVE RULINGS

**Seat**: cross-wave rulings, repair round 1 · 2026-08-28
**Authority**: `PASS-1/UNION.md` (read whole) · the 11 per-wave check files · the 11 wave specs at `docs/tranches/X/keyframes/waves/` · the 58-record corpus at `docs/tranches/V/megatranche/registry/adjudicated/` · `docs/tranches/X/COHESION.md` §0b/§1 · `carry/KF-W6-CARRY.md` (the ONLY in-tree carry: **201 rows / 54 records** — 424 was the literal `KF.W6` token count, never a row count).
**Law**: every CROSS-WAVE defect gets exactly ONE owning cure that a single wave can apply. Per-wave repair seats execute these rulings verbatim. E-3 everywhere: registry records are amended by ADDENDA under original ids only; wave specs are repaired in place (they are `planned`, unstamped). Nothing here authorizes execution or opens product source.

**19 rulings.** Ruling → per-wave directive map at §END.

---

## R-1 · THE 32 PROGRAM ESCAPES — one adopting wave each (UNION §1.2, U-1)

Partition law: every one of the 32 rows lands in exactly ONE wave below, by id, with its banked rider verbatim. No other wave may book any of them except as an exclusion-with-named-owner pointing at the adopter.

### R-1a · ownerWave **KF-W0** — the fold-rider family of 12 (UNION §1.2 #1–12)

**Cure shape**: KF-W0 §Carry C-1's fold-identity mechanism gains an id-keyed table booking each of the 12, each with its travel-rider quoted verbatim from the bank; C-1's blast-surface rider list (currently six records) widens accordingly, and C-9's hard order ("must land BEFORE the F-1 remediation scopes its blast radius") is thereby satisfied on a full count. The 12, id for id (receipts at KF-W0-CHECK §2.B/§2.A):

1. kf-DemoGlobalChrome **D-5/L-3/C-2** (the `./toast` S-9 fold; the swap verdict is census-owned — book with the identity guard; the mint receives it under R-5)
2. kf-EasingTarget **KF-ET-3** — rider: *"4 specifiers here + 3 in the sidebar; sole consumer of `/fading-scroll` + `/toggle-group`"*
3. kf-EasingSidebar **KF-ES-33** — rider: this component's 3 subpath edges
4. kf-EasingScene **KF-ES-15** — rider: the scene's exposure census (7 entry points)
5. kf-KeyframesEditor **KF-KE-16** — rider: the `/dark`+`/forms` subpath-import blast radius
6. kf-TimingFunctionPanel **KF-TFP-29** — rider: the 4.0.1-era prose `:25-31` = **KF-CO-17**, not a dependency defect
7. kf-AnimatedText **KF-AT-7**, declaration leg (three-way-drift evidence attached)
8. kf-ChannelOptions **L·B-3 / C·B-2** discharge half (the spec already carries the three-file disk↔master reconciliation half; book the L·B-3 discharge half beside it so the record's `:149` packet is whole)
9. kf-ChromeDock **L/B-1**
10. kf-CSSPasteDialog **F-1 row C-11/L-I1** — rider: *"All six of this file's component imports ride it"*
11. kf-KeyframesAddDialog **F-1 row L-5/C-11** — rider: *"All six glass imports ride it"*
12. kf-KeyframeCardList **L-9 = C-D-7 = C-D-9** (identity owned by lane-frontend §10 step 1, per the KF-CE-32 precedent)

**Sibling corrections**: KF-W6 does NOT adopt #10/#11 (their W6-precondition arm is the fold's, not a W6 row). No other wave changes.

### R-1b · ownerWave **KF-W2** — the W2↔W3 mutual-assumption hole of 4 (UNION §1.2 #13–16)

W2 declared itself census-of-record for the R1 class (UNION §4); the negatives land there. **Cure shape**: book all four in §Carry F0's verified-negative register / posture registry, by id, with the do-not-re-probe locks quoted:

13. kf-DemoGlobalChrome **C-10** — *"zero value.js/engine edges; R1 reachability CLEAN … recorded so parser waves skip this file"* → F0 verified negative (the `/math`-is-not-the-parse-surface mechanism named)
14. kf-SquareScene **C S-C** — *"`parseCssScalar` at the two-writer boundary … worth citing upward when the parser wave lands"* → F0 positive exemplar, cited upward as the bank instructs (unit-blindness = L-10 noted)
15. kf-KeyboardShortcutsModal **C-§1/R-B R1-negative** — *"CLEARED … Do not re-file"* → F0 verified negative
16. kf-SpringTrace **D-12 = L-6 = C-4** — the silent-swallow parser posture → F0 posture registry, AND added to G-W2-1's posture count. **G-W2-1's denominator moves from 8 to ≥11** (this row + KSC C-2 + KSC D-6/L-BL-3/C-1, per KF-W2-CHECK D-4/E3-E5); the gate's count cell is re-stated at the true number.

**Sibling corrections**: KF-W3 cross-declares all four as exclusions-with-named-owner (owner = KF.W2) inside its G-KF3-7 call-map denominator (see R-7b). Neither parser wave may again assume the other holds the negatives: each spec's cross-edge to the other states *"the R1 negative/posture census of record is KF.W2's F0"* in one sentence.

### R-1c · ownerWave **KF-W4** — kf-SpringTrace D-1's `:48-49` comment truth (UNION §1.2 #17)

**Cure**: row 29's SpringTrace clause (currently three of the record's four KF.W4-PROSE items) gains the fourth item verbatim: **"D-1's `:48-49` comment truth (rides the fix)"** — a sequencing rider; M-25 forbids dropping it. No sibling correction.

### R-1d · ownerWave **KF-W6** — the six W6-routed escapes (UNION §1.2 #18–23)

**Cure shape**, per KF-W6-CHECK §3:

- 18–20. kf-CubeTarget **#21 · #30 · #39** — booked as rows or as named members of the cube-packet homing at §Sequencing L368, which currently enumerates only "CubeTarget #58/#60". The bolded terminal manifest at `kf-CubeTarget.md:100` (*"KF.W6: r1 set (#8 · #9 · #21 · #30 · #39) + #58 + #60"*) is carried whole; the r1 bodies ride the `:38` carried-whole-from-r1 clause.
- 21. kf-SquareInstrument **D-20** — booked (the eight-selector token re-point producer-truth correction), AND the **BG-6 BH-relay ask added to G-W6-14's relay inventory (L349) and the §Sequencing relay list (L381)**.
- 22. kf-CubeScene **D-20** — booked under W6-C's stale-vendor-premise class ("rides any touching wave"; W6 touches `CubeScene.vue` at §Bounds L72 and owns the class).
- 23. kf-SequencePlayhead **flat-namespace datum** — booked beside the material-register row at L165 (the record's `:151` routes three W6 items; the spec carries two).
- Alias-drop repair rides this directive (INFO, one line each): `C-7 / L-m7` · `D-M3 / L-M-3 / C-B-1` · `L-EST-6 / D-C6 / ND-10` alias lines added so the id-keyed trail closes.

**Sibling corrections**: none — no other wave may book these.

### R-1e · ownerWave **KF-W9** — the uncarried AT/announcement arm + two locks (UNION §1.2 #24–32)

**Cure shape**, per KF-W9-CHECK §3 (S-12's *"≥8 records"* becomes true by carriage, not assertion):

- 24–27. kf-SpringTarget **D-5 (aria-hidden half) · D-6 · D-14 · N-4** — booked as §F rows. **D-14's cure lock is restored verbatim: "one binding, with N-4" (M-25)** — D-14 and N-4 are ONE cure, never split.
- 28. kf-SequenceAxis **D-12** + the **kf-SequenceTarget relay** — booked; the cross-record relay obligation is RECEIVED (named at the row: the `text-mono-caption` `@800MS` datum + the sliders' missing `aria-valuetext`, relayed to the SequenceTarget rows W9 already carries).
- 29. kf-SpringHeatmap **D-m8** — booked beside the carried D-m9 RTL limb (the 33-word `aria-label` / missing `aria-describedby` split).
- 30. kf-PlaybackRibbon **D-12's demo-side stopgap** — booked as MEASUREMENT (witness held here); the stopgap's cure bytes ride the transport/ribbon packet homed at R-15 (KF.W13), consistent with R-9a's measure-only posture.
- 31. kf-SequenceScene **D17/D18-unit fold** — booked, and its sequencing lock **"SEQUENCED after banked N-1"** entered in §Sequencing (S-1..S-13 gains the lock; a sequencing lock routed to a wave must live in that wave's §Sequencing).
- 32. kf-CSSCodeEditor **KF-CE-47 witness arm** — booked in §H's SS-13 list (witnesses to SS-13/KF.W9; the EDITOR-UNIT theme-repair cure half stays with unauthored KF.W12 — noted, not booked).

**Sibling corrections**: none.

---

## R-2 · DOUBLE-HOMING 1+2 — KF-KE-24 and KF-KE-32 · ownerWave **KF-W6** (UNION §2.2.1–2)

One reading: **both rows are carried exactly as §Carry L235 books them** — the unlayered runtime hljs plate's **TOKEN decision is KF.W6's rider; the layer/re-tokenize pipeline is KFED-UNIT's** (matching the registry: `kf-KeyframesEditor.md:66` KF-KE-24 → KFED-UNIT *with a KF.W6 rider*; `:74` KF-KE-32 → KF.W6).

**Cure (exact)**: §Bounds L54's carve list is amended from
`KF-KE-21/-23/-30/-31/-36/-39/-40/-42/-45/-53/-59 only` to
`KF-KE-21/-23/-24 (TOKEN-decision rider only)/-30/-31/-32/-36/-39/-40/-42/-45/-53/-59; six blockers and the hljs layer/re-tokenize pipeline are KFED-UNIT's`.
The word "only" no longer forecloses two rows §Carry books; a seat can execute both sections without breach.

**Sibling corrections**: none (KFED-UNIT receives its pipeline half via R-15's KF.W12 homing, which already carries the KFED-UNIT locks).

---

## R-3 · DOUBLE-HOMING 3 — TimelineCaret L-7 · ownerWave **KF-W7** (UNION §2.2.3)

The banked routing wins: `kf-TimelineCaret.md:51` — *"L-7 … root cause is the banked KF-CE-16 gate hole (KF.W4 fold) → **KF.W7**"* — and KF-W7 already books it (P6, "L-7 (TimelineCaret)"). **The flag is W4's; the site is W7's.**

**Cure at KF-W4** (the sibling whose text is corrected): §Bounds :61 drops `TimelineCaret L-7's dangling const props` as a unit-`.c` cure site; G-KFW4-4's demo-arm witness list keeps L-7 **as witness only**, annotated: *"site cured at KF.W7 P6 (banked routing); the K3 flag is this wave's, the cure site is KF.W7's — declared cross-wave edge, no in-wave write."* §Disjointness gains the file under the declared-cross-wave-re-open list.

**KF-W7**: no change.

---

## R-4 · DOUBLE-HOMING 4 — ST-1's cure bytes · owner = the sequence packet (**KF.W11**, homed by KF-W4); KF-W9 is measure-only (UNION §2.2.4; joint with R-9a)

One reading: W9's own S-11 ("this wave DECLINES their behaviour, HOLDS their witnesses", sequence packet → proposed KF.W11 *"sequenced after the KF.W4 gate"*) and S-13 (behaviour edit in a packet-owned component = mandatory triumvirate trigger) are the law; G-KFW9-10's landing of the edit was the breach.

**Cure**: the `SequenceTarget.vue:97` one-word + `SequenceTarget.css:163-167` deletion moves WITH the sequence packet to KF.W11 (R-15 roster), sequenced after G-KFW4-1. KF-W9 keeps ST-1 as measurement + held witness. Execution text at R-9a.

---

## R-5 · DOUBLE-HOMING 5 — the S-10 census slot triple-claim · ownerWave **KF-W0** (UNION §2.2.5)

The single-motion mint (C-17) is the ONLY census-slot authority; claims are inputs to the mint, never slots. Resolved without minting a new collision:

**Cure (exact shape)**: C-17's roster is extended to the full claimant set (KF-W0-CHECK D-1/§2.A): S-9 claimants ≥8 — the four already enumerated **plus** kf-AnimationVisualizer:72 (**KF-AV-28**, ScrubberTimeline), kf-CSSPasteDialog:43 (R-5, Textarea), kf-DemoGlobalChrome:43 (`./toast`), kf-SpringTrace:89 (KILLED-AS-STATED — enumerated as a dead occupant, not minted), kf-SharePopover:90 (REFUSED — enumerated, not minted); S-10 claimants ≥4 — **kf-KeyboardShortcutsModal C-9's seat-ruled renumber, kf-KeyframesEditor KF-KE-21's ask, kf-SquareInstrument C-12**, + the fifth record's original request. The mint assigns **fresh non-colliding ids in ONE motion, each citing its originating record** (C-17's own cure-shape). The KSM seat-ruled renumber-to-S-10 is DEMOTED to a claim-input: an E-3 addendum under the original id at `kf-KeyboardShortcutsModal.md` records *"the renumber is superseded by the KF.W0 C-17 single-motion mint; the slot id is whatever the mint assigns"*. C-17's fourth-subject description is corrected per D-9 (the AmigaScene C-6 S-9 = `Surface`/`PaperBackdrop`/`Card` bespoke-CSS shadow extending S-1..S-8 as a block, not "an extension of S-1").

**Sibling corrections**: KF-W6's KSM C-9→S-10 booking gains one clause: *"census id pending the KF.W0 C-17 mint"*. No wave other than W0 mints or renumbers a census slot.

---

## R-6 · DOUBLE-HOMING 6 — the KF.W4-PROSE split rule gets ONE reading · rule owner **KF-W4**; manifest receiver **KF-W6** (UNION §2.2.6)

**The one reading (binding on both specs)**: *the KF.W4-PROSE class splits per LIMB, by citation-mechanism: a limb whose defect is a `proof:*` / gate / baseline citation to a phantom AUTHORITY belongs to KF.W4 (G-KFW4-8, as re-scoped by R-8); every other limb — phantom consumers, false invariant names, stale vendor premises, impossible type claims, broken text / counterfactual rationale — belongs to KF.W6. The row ID stays at the wave holding the id-anchor (W4 row 29/30); the non-proof limbs travel to W6 by an id-for-id manifest, declared at W4 and BOOKED at W6.*

**Cure at KF-W4**: R-1's split sentence is replaced by the reading above, plus an explicit manifest of the non-`proof:*` limbs routed to W6 — headed by **kf-ChannelControls D-7/L-4's 20-line counterfactual rationale block** (the named faller). The manifest replaces the unreciprocated "all other arms → KF.W6" gesture.

**Cure at KF-W6**: the manifest's ids are BOOKED (rows or fold lines) — D-7/L-4's rationale limb first (this simultaneously cures W6's E-2 escape; the NO-WAVE-OWNER branch-delete half stays with the bank's disposition). W6's taxonomy line L10 is annotated: *"per the R-6 ruling: limbs travel by manifest, not by class-claim."*

---

## R-7 · THE KF-W3 BLOCKER PAIR · ownerWave **KF-W3** (UNION worst-10 #1–2)

### R-7a · E-12 vs C-5 — one consistent reading (L-19 restored)

**Ruling**: E-12 as written is STRUCK and re-founded on the bytes. What K-6 killed (`kf-SquareInstrument.md:24/:100`) is exactly one claim: **R1-through-a-token** — empty-args `oklch()` is unreachable via `getPropertyValue` → `""` → the `if (value)` guard. K-6 does NOT kill the `useSquareTumble.ts:21-25` `!ok → throw` crash surface, which the frontier bytes, the spec's own carried C-5 (five throw sites in the rAF frame — `num()` ×2, `colorAt` ×3 — BRICK the loop), and the uncarried-now-carried kf-EditorShell C-19 (RR-2 reproduced R1 live against installed 4.0.0) all establish.

**Replacement E-12 text (shape)**: *"E-12 · Scope of the K-6 kill. K-6 kills only the claim that a TOKEN can produce the empty-args R1 degenerate. Reachability of the `useSquareTumble.ts:22` call site via authored CSS and the `!ok` throw arm is ESTABLISHED (C-5 / D-27 / kf-EditorShell C-19) and MAY be asserted RED. Read as a pair: C-4 governs the token→R1 path; C-5 governs the authored-CSS/throw path."* The gate that can fail for its intended reason is thereby permitted to.

Companion repairs in the same directive: **C-5's second reachability conjunct restored verbatim** (D-03: *"and via any `--rainbow-*` re-author in a syntax value.js 4.0.0 rejects"*), with §Excluded E-13's reason amended to the bank's "both" (token-namespace defect AND parse-adjacent); **E-8's reason re-founded** (D-04: the false "no ChannelOptions… row lands" replaced by the true routing — C·S-4 is parser-lane-forwarded, census-of-record = KF.W2 F0 per R-1b); **G-KF3-1 re-founded with a discharge condition** (D-06); **G-KF3-8/G-KF3-9 re-classed as declared floors** — §Gates counts **7 real gates, not 9** (D-07).

### R-7b · The 10/19 escape mass — denominator cure, per R-1

**Ruling**: G-KF3-7's call-map denominator must contain ALL ten (six are the UNREACHABLE-with-mechanism rows that ARE the map). Carriage split:

- The **8 that land at KF-W2** (KF-APP-56 = W2 B1 · EditorShell C-19 = B9 · CubeAxisLines C·§4 = B8 · KF-KE-54 = B10 · TimelineTrack SUP-3 = B16 · ChannelOptions C·S-4 = B17 · ChannelControls C-L-1 = B23 · CubeTarget #11 = B22): W3 books each as an **exclusion-with-named-owner (KF.W2)** while entering it in G-KF3-7's map denominator. The dotted `KF.W2/W3` class is parked on the W2 side; W3's map still counts it.
- The **2 that escaped both** (kf-DemoGlobalChrome C-10 · kf-SquareScene S-C): adopted at KF-W2 per **R-1b**; W3 cross-declares identically.

Also in this directive: **OP-3 and the COHESION §1 cross-edge re-anchored on COHESION §0b** (D-05: KF.W3 → SS-1 by dated assignment, GATED posture unchanged; ONE owed ruling remains — the write hand); OP-3's "(nor is KF.W10)" clause re-anchored per R-18.

---

## R-8 · THE KF-W4 CRITICAL — G-KFW4-8 re-cut so the sequencing head is satisfiable in-wave · ownerWave **KF-W4** (UNION worst-10 #3)

**Ruling: the census is RE-SCOPED to the named nine, with the ≥6 further families ROUTED** (§Bounds is NOT widened — every residue cure site is outside §Bounds and any write trips the triumvirate; the staged-RED disease dies by scoping, not by scope-creep).

**Cure (exact shape)**:

1. **G-KFW4-8's command** is re-cut from "every `proof:*`/baseline/allowlist citation in `src/`, `demo/`, `scripts/`, `*.css` and the living docs" to an enumerated-site assertion over **R-7's nine citation targets** (§Bounds row 74) — the set whose cures are in-bounds. R-7's satisfiability argument becomes true as arithmetic.
2. **A residue routing table** is appended to §Excluded (each line a routing, not a drop), covering the bank-enumerated families the spec never named (KF-W4-CHECK D-1):
   - kf-AnimationControlsGroup **D-13** (7 `proof:*` names, NO-WAVE-OWNER) **+ kf-DemoGlobalChrome L-6** (`proof:demo-no-oversize`, **16 hits across 13 files** — "the fold owns the sweep") → **KF-W0's dead-gate roster sweep (G-0.7)**, whose roster widens to receive them (reciprocal edge declared here; W0 executes under R-17's roster directive).
   - kf-CubeAxisLines **KF-AX-31** (`proof:brittleness` at `style.css:38-39` + 3 stale prose sites) → **KF.W6** (as banked: "KF.W6 + KF.W0 rider"; W6 already books KF-AX-31).
   - kf-KeyframeCard **KF-KC-35** / kf-KeyframesEditor **KF-KE-62** (`proof:accent-census`, one site, two records) → the **KFED/CARD packets at KF.W12** (banked NO-WAVE-OWNER / KFED-UNIT).
   - kf-ControlsPaneWrapper **D-M3/mi-5/C-14** (`proof:stage-visible`) → the **OPTIONS/controls surface at KF.W12**.
   - the **~40-name `proof:*` prose-rot class** (kf-ChannelOptions:207 ≡ ChannelControls L-19): row 29 states the **magnitude (~40) explicitly** (D-8 cured — a class booked without its cardinality cannot be checked for closure); limbs split per **R-6**'s one reading.
3. **Five gate fixtures get §Bounds create rows** (D-2): `test/animation/easing-identity.test.ts` · `test/animation/timing-function-names.test.ts` · `test/demo/easing-catalogue.spec.ts` · R-5's one TypingDots demo-lane spec (pathed) · KF-HA-4's aurora-ceiling demo-lane spec (pathed). The wave may author its own born-RED witnesses without tripping itself.
4. **Pathless rows get paths; the `<config>` placeholder is spelled** (D-3): G-KFW4-11's command names `.dependency-cruiser.cjs` (or the re-anchored config by literal path), and §Bounds rows 51/59/64/65/66/71 carry literal paths.
5. **The `scripts/gates/` inventory is corrected to the frontier** (D-7): origin/master carries **9** (incl. `structure/index.mjs` + `agent-surface.mjs`), not "surface, visual" — G-KFW4-9's subject set restated (joint with R-17).
6. §Sequencing's head-witness list is corrected (D-10): "six banked records" → the true count, with kf-AmigaScene struck from the list (it states no such thing).

**Sibling corrections**: KF-W0 receives the G-0.7 roster-widening edge (executed under R-17); KF-W6/KF.W12 receive the routed families above (W6: no new booking needed beyond KF-AX-31 already carried; KF.W12: rides R-15's packet locks).

---

## R-9 · THE HEAD BREACH + THE UNOBSERVABLE GATE (UNION §3.1)

### R-9a · KF-W9's G-KFW9-10 breach — ownerWave **KF-W9**: the wave MEASURES; every cure arm leaves

One reading (the check's own fork, taken): **measure**. Cures:

1. **G-KFW9-10 is struck as a gate** and re-cut as a **sequencing DECLARATION handed to KF.W11**: *"ST-1's affordance edit (`SequenceTarget.vue:97` one word + `SequenceTarget.css:163-167` deletion) is the sequence packet's, sequenced after G-KFW4-1; this wave holds the before/after witnesses."* (= R-4.) Thirteen gates remain; the preamble's count is restated.
2. **G-KFW9-9 closes on the born-RED witness pair only** (focus a ribbon button under forced-colors, assert NO indicator; the Highlight-outline-returns half becomes the packet's after-witness). The **two-deletion cure (K-5 lock intact: one deletion fixes nothing — the pair is ONE act)** routes to the **transport/ribbon packet at KF.W13** (R-15), which owns `playback-idiom.css`; `design-idioms.css`'s copy rides the same single act. W9 re-shoots after it lands (S-9 discipline).
3. **§Bounds' five EXECUTION-TIME-ONLY kf write grants are struck** (`design-idioms.css:76-79`, `playback-idiom.css:72-75`, `SequenceTarget.vue:97`, `SequenceTarget.css:160-167`, `app/index.html:6`) — OP-2's "write authority UNRESOLVED for any cure" is restored to true (D-5 cured).
4. **G-KFW9-8's title and §Goal are re-worded to the true outcome** (D-9): the producer rule restoration is measured; the demo-side `forced-colors` count is expected to stay 0 and that IS the recorded result — no demo-side hack is authored (the standing edict and KF-ET-4's lock hold).
5. **OP-6 stands, now true**: with every cure arm gone, W9 is a pure measurement wave and the KF.W4 head lawfully does not bind it. The §Scope-3/§Goal cure phrasing is re-worded to match.

**Sibling corrections**: KF.W13 (per R-15) carries the two-deletion act + the D-12 stopgap bytes with the K-5/KF-AV-28 riders; KF.W11 carries ST-1's edit (R-4).

### R-9b · KF-W5's G-XSS observability — ownerWave **KF-W5**

**Ruling** (the check's option (b)): the front-load survives; the false independence claim goes.

1. **LP-7's rider and ME-32 are CARRIED** as fold-identity rows in §Carry (*"a `--project demo` CI step (= V.md's MR4) is a prerequisite of the cure, not an optional extra"*; *"wire `test:demo` into CI"*).
2. §Sequencing's *"riders KF.W4 owes"* gains the third rider: **the `--project demo` / `test:demo` CI wiring** (KF-W4 already reciprocates at its row 3 / G-KFW4-2+3 — the edge is now declared both ends).
3. **OP-6 is amended**: *"G-XSS's gate OBSERVABILITY — not its cure — waits on KF.W4's one wiring step (G-KFW4-2/-3); the typecheck half of the front-load exemption is unconditional."*
4. G-XSS's fixture gains the `(**create**)` marker; `test/demo/instrument/` (exact path) is enumerated in §Disjointness; the shared-prefix `test/**` glob in §Bounds is narrowed (D-10 cured).

**Sibling corrections**: none beyond KF-W4's already-standing reciprocation.

---

## R-10 · KF-AV-28 — the standing supersession rider propagated as a STANDING CLAUSE (UNION P-1, worst-10 #8)

**Owner of the lock**: KF-W7 (unchanged, its defining lock, ×7). KF-W8 already carries it (×1) — no change. **The six specs that carry governed rows and drop the rider — KF-W2, KF-W3, KF-W4, KF-W6, KF-W9, KF-W10 — each add the following clause VERBATIM**, plus their own governed-row enumeration:

> **KF-AV-28 · STANDING SUPERSESSION RIDER (KF.W7's defining lock) — carried, not cited.** Banked at `kf-AnimationVisualizer.md:35`, `kf-PlaybackRibbon.md:36`, `kf-SequenceScrubber.md:36`: KF.W7's S-9 evaluation (KF-AV-28) may supersede any behavioral cure on a NO-WAVE-OWNER row banked under an evaluated surface; a per-surface SWAP verdict discharges the bespoke rows banked under that surface with the component. Rows in this spec governed by the rider are spent CONDITIONALLY: no cure on a governed row lands before KF.W7's verdict for its surface, or the executing seat accepts the throwaway explicitly; at execution the seat STATES which governed rows a SWAP verdict has discharged BEFORE spending any cure (KF-W7 G15's discharge discipline).

Per-wave governed-row enumerations (the clause names them where the spec spends or routes cures):

- **KF-W2**: L-6/C-4 (G-W2-7's delegation-target obligation becomes conditional — a SWAP on the timeline cluster moots the emit-half).
- **KF-W3**: the rows routed to KF.W7 at §Excluded E-5 + cross-edge :206 (KeyframeTimeline C-7's posture; L-6/C-4's hand-rolled parser).
- **KF-W4**: at §Sequencing's packet-homing head — the rider travels intact with every homed NO-WAVE-OWNER packet (KF.W11/W12/W13), named beside "their internal ordering edicts are theirs and travel intact".
- **KF-W6**: the three exposed surfaces spending cures — `AnimationVisualizer.vue` (KF-AV-11), `PlaybackRibbon.vue` (the `--color-progress` half), `SequenceScrubber.vue` (KF-SCR-2) — each gains the guard the spec already gives N-2 and KAD-6; the five declared W7 bindings at L376 name the rider by id.
- **KF-W9**: the carried KF-AV-19/D-8 · KF-SCR-1 · K-13 · D·D-8 rows; S-10's KF.W7 edge names the rider by id (paraphrase is not carry).
- **KF-W10**: §3.2's carried-locks list gains the rider as its seventh bullet; G-2's sweep declares it a sweep input (a governed row's lawful terminal words include "DISCHARGED by KF.W7 SWAP verdict").

---

## R-11 · THE PHANTOM SCRIPT — `build:gh-pages` exists at no coordinate · owners **KF-W0** + **KF-W9** (UNION P-2, worst-10 #6)

The real script is `package.json:43` → `"gh-pages": "vite build --mode gh-pages"`; the real invocation is **`npm run gh-pages`** (the `KF_ANALYZE=1 vite build --mode gh-pages` form in W9's §Excluded is correct and untouched).

- **KF-W0 — 7 sites corrected**: `:48` (item 10) · `:106` (C-13's companion caveat) · `:241` (G-0.10 GREEN) · `:257` (unit KF.W0.e) · `:272` (the KF.W6 OUTBOUND edge's shared precondition) · `:311` (§Cadence's sole carve-out) · `:313` (artefacts list, "the fresh `gh-pages` hash").
- **KF-W9 — 3 sites corrected**: `:41` (OP-5) · `:175` (the witness-substrate law's WITNESS REFRESH) · `:194` (G-KFW9-14's witness).

Every corrected sentence reads `npm run gh-pages`. The four records whose byte-offset trust hangs on the refresh (kf-CSSPasteDialog #8 · kf-KeyframeTimeline #13 · kf-TimelineCaret #12 · kf-TimelineTrack #11) are unchanged — the command they wait on now exists.

---

## R-12 · THE 5 NONEXISTENT CARRY-LEDGER CITATIONS (UNION P-3) — each struck or re-pointed; owners **KF-W2 · KF-W3 · KF-W4 · KF-W5 · KF-W7**

The ONLY in-tree carry is `docs/tranches/X/keyframes/carry/KF-W6-CARRY.md` — **201 rows / 54 records** (424 = the `KF.W6` token count). No other wave may cite a per-wave CARRY as provenance. Cure per spec — strike the phantom citation and restate provenance as *"authored from the 58-record registry directly; the routed denominator is the registry's (PASS-1 census)"*, striking every "N rows carried, 0 dropped" claim keyed to a ledger that does not exist:

1. **KF-W2** (`:13`,`:103`): "11 rows, 11 carried, 0 dropped" struck; registry denominator = **47 routed** (per its check).
2. **KF-W3** (`:17`): "7/7 rows carried, 0 dropped" struck; registry denominator = **19 canonical (24 with the SHIM family)**.
3. **KF-W4** (line 11, AUDITED row): *"authored against the CARRY ledger whole"* struck; the AUDITED basis restated over the true 41-record source set (its D-15 duplicate/omissions fixed in the same edit).
4. **KF-W5** (§Provenance + §5): the "SS-1 CARRY ledger" and the `docs/tranches/X/kf/waves/KF.W5.md` bounds-correction target struck; the 54/42/12 accounting re-keyed to the registry census (its D-1's 44+12=56 recount lands in the same edit).
5. **KF-W7** (`:13`,`:38`,`:327`,`:343`): all four CARRY bindings struck; **and the failure the phantom ledger hid is cured in the same directive** — §Excluded gains the three dual-homed ids **KAD-15 · KAD-16 · KAD-20 as exclusions-with-named-owner (KF.W6, where all three are booked in spec AND CARRY, re-verified)**, so "No CARRY row is dropped" is replaced by a true sentence: "no registry-routed row is unaccounted."

---

## R-13 · ZERO-HOMED ACT 1 — KF-CB-37, the only banked KF.W8 move · ownerWave **KF-W8** (UNION §1.5, worst-10 #5)

**Ruling: KF.W8 owns the act.** Grounds: the bank routes it there (`kf-CopyButton.md:85` → KF.W8 Structure & Colocation Settle); the W6 CARRY bounds `CopyButton.vue` as **modify** (113 L, S-7 reshell), not move; W8's own fallback clause ("the bare move falls back to KF.W8 unit f") fires NOW — not on decline — because **W6 never received the row**.

**Cure at KF-W8 (exact shape)**:
- **R-1 is rewritten**: the sentence *"the file moves inside KF.W6's reshell commit; KF.W6 carries the reciprocal declaration"* is struck and **the phantom reciprocal declaration is NAMED AS SUCH**: *"the previously asserted W6 reciprocal declaration does not exist (0 hits in KF-W6.md and the 201-row CARRY); the assertion was this spec's error."*
- New R-1 ruling text: *"KF.W8 unit f executes the bare move (`git mv demo/components/CopyButton.vue` → its colocation target + import repoints). If KF.W6's KF-CB-7 reshell (a modify) has landed first, the move follows it; the two acts never share a commit. G6 is the ACTING gate (born RED at 1 loose `.vue`, turned by this wave), no longer verify-only."*
- `:117`'s completed-act voice ("executed in W6") is corrected to the future/conditional form used at `:136`.

**Sibling correction at KF-W6** (one line, its §Sequencing → KF.W8 edge): *"CopyButton.vue's MOVE is KF.W8's (KF-CB-37); this wave's KF-CB-7 reshell modifies in place and must not relocate the file."*

---

## R-14 · ZERO-HOMED ACT 2 — `engine/animation.ts` and the do-not-split lock · ownerWave **KF-W8** (rules), touchpoints **KF-W5** + **KF-W10** (UNION §1.5, worst-10 #7)

**Ruling — the lock is RE-RULED with reasons, restoring ONE programme**: W5's rename programme (the 16 named stutters, G-STRUCT) is one wave's, W5's — no split possible there. The **two structural instances** (`src/` single-child; `src/animation/engine/animation.ts`) are tree-structure decisions W5's D-6 handed to W8, and W8's R-4 substantively DECLINED the flatten "for the duration of X·KF". That decline is lawful — but it must cover BOTH instances in ONE ruling, and its terminus must be a RECORD, not a deferred act (W10 "consumes; it does not home and it does not cure").

**Cure at KF-W8**: R-4 is amended to name **both** structural instances explicitly — *"the `src/` single-child flatten AND the `engine/animation.ts` stutter-instance are DECLINED together, one ruling, for the duration of X·KF (reasons as stated); the pair travels as ONE record to KF.W10's FOLD-FORWARD ledger — a terminal RECORD, not a receiving surface for the act."* The "Receiving surface: KF.W10" phrasing is corrected accordingly. G13's 8-count predicate gains one sentence: *"KF.W5's G-STRUCT owns the 16-stutter denominator (broader predicate); G13's 8 is a narrower structural count and never re-measures W5's rows"* (the 16-vs-8 schism closed by declared subordination).

**Touchpoint at KF-W5**: D-6 gains an addendum sentence: *"The do-not-split lock is SATISFIED by decline-whole: both structural instances terminate together in KF.W8 R-4's one ruling, recorded at KF.W10; the rename programme (16 stutters) executes here, whole."*

**Touchpoint at KF-W10**: §2b/§3.1 books the FOLD-FORWARD record row (record-only, terminal verb `DECLINED-FOR-X·KF, carried forward`), consistent with its charter.

---

## R-15 · THE NO-WAVE-OWNER PACKET PARTITION — CLOSED · homing authority **KF-W4** (UNION §1.5, worst-10 #10)

**The canonical roster is 17 packets** — the reconciliation of W0's 15 (`KF-W0.md:276`), W9's 14 (S-11), and W1's unreceipted "16": W0's 15 ∪ {amiga, EDITOR-UNIT} (W9's adds) = 17. No enumeration in any spec was complete; the number 16 was never true.

> **cube · sequence · spring · spring-plot · spring-physics-facet · spring-artifact-truth · square · amiga · drag-seam · CARD-UNIT · OPTIONS-UNIT · KFED-UNIT · EDITOR-UNIT · APPLY-UNIT · AXISLINE-UNIT · dock-menu · transport/ribbon**

**Cure at KF-W4 (the homing authority)** — §Sequencing's homing lists are re-cut to cover the roster whole, minting KF.W13 explicitly (per W1 cross-edge 9's cohesion flag: waves beyond the census sketch are MINTED, declared):

- **KF.W11 · Demo Scene Repair** (9): cube · sequence · spring · spring-plot · spring-physics-facet · spring-artifact-truth · square · amiga · **drag-seam** (kf-SequenceScrubber:36 — sequence-family drag seams; carries the KF-AV-28 rider per R-10: the scrubber surface is W7-evaluated, the packet sequences after that verdict). Existing locks travel intact; ST-1's edit rides here (R-4).
- **KF.W12 · Authoring-Surface Repair** (6): CARD-UNIT · OPTIONS-UNIT · KFED-UNIT · EDITOR-UNIT · APPLY-UNIT · AXISLINE-UNIT. All existing verbatim locks (KC-34 · the OPTIONS-UNIT fixed order · LP-1 · TD-1/TD-2 — see below · M-4 rider) travel; the R-8 residue families (accent-census, stage-visible) ride their units.
- **KF.W13 · Chrome, Dock & Transport Repair** (2, MINTED): **dock-menu** (moved here from W4's KF.W12 list — reconciling `KF-W2.md:270` and W1 cross-edge 9, which both route it to W13; the kf-ChromeDock M-4 MUST-CARRY MbabbMenu rider travels) · **transport/ribbon** (kf-PlaybackRibbon:36 — HOMED for the first time; carries the KF-AV-28 rider, the TD-1/TD-2 bundle, G-KFW9-9's two-deletion act and PlaybackRibbon D-12's stopgap bytes per R-9a).

**Sibling corrections**:
- **KF-W1**: the cardinality **"16" → "17 (canonical roster, per RULINGS R-15)"** at `:126` (C-14 leg iii), `:219` (cross-edge 9), `:236` (§8), each now RECEIPTED against the roster above (its D-3 cured); cross-edge 9's KF.W11/W12/W13 membership restated canonically.
- **KF-W0**: `:276`'s 15-packet line re-cut to the canonical 17 (homing stays "SS-1/SS-2's act, this wave supplies their ground" — now discharged by W4's lists).
- **KF-W9**: S-11 re-pointed at the canonical roster (its own enumeration gains spring-physics-facet · spring-artifact-truth · drag-seam; witnesses stay held here).
- **KF-W10**: NWO-TERMINAL-SWEEP's BLOCKER text re-anchored: the amiga packet IS claimed (W4 :198); the "orientation/a11y cluster" = the AT arm, now carried at W9 per R-1e; the sweep's denominator = the 17-packet roster + the 1,216-occurrence census.

---

## R-16 · THE W2↔W8 SERIALIZER CIRCLE — an orderer is named · ownerWave **KF-W8** precedes (UNION §2.2 parenthetical; KF-W8-CHECK D-6)

**Ruling**: the banked routing (`kf-TimelineCaret.md:46` — *"→ KF.W8 (publish-or-relocate, joint with banked C-12)"*) puts the ACT at W8. **KF.W8's row 10 / G3 is the publication act and PRECEDES; G-W2-8 gates the façade-side contract only and arms if-and-only-if W8 has not preceded** (exactly W2's own `:221` alternative, now the declared order rather than a mutual conditional).

**Cure at KF-W8**: `:254`'s "possible publication vehicle — if W2 mints it" is replaced by the order above (W8 acts; W2's façade contract follows). **Cure at KF-W2** (sibling): OP-6's "UNORDERED" is replaced by *"ORDERED: KF.W8 precedes (RULINGS R-16)"*; G-W2-8's condition cites it.

---

## R-17 · THE W0↔W4 PROOF-ROSTER DENOMINATOR — the dispute is RESOLVED on the fourth measurement · ownerWave **KF-W4**; roster owner **KF-W0** (UNION §2.2 parenthetical; KF-W4-CHECK D-6/D-7/E2; KF-W0-CHECK D-3)

**Ruling**: `kf-SequenceScrubber.md:119` decides it — *"`proof:structure` present in origin/master's `check`, deleted in the worktree edit"* — corroborated by kf-TypingDots K8 (SUSTAINED: origin/master carries **9** under `scripts/gates/`). The frontier roster is the denominator; the 2-script count is stale-worktree-only.

**Cure at KF-W4**: OP-3 re-cut from "DISPUTED — KF.W0 owns the recount" to **RESOLVED** (grounds above), and the deciding row **E2 (`proof:sequence-rows-draggable` liveness, KF-EST-1 family) is CARRIED at row 12**; §Bounds row 69's "today: `surface`, `visual`" corrected to the frontier 9 (executes with R-8 items 2 and 5).

**Cure at KF-W0**: G-0.7's dead-gate roster widened by its three missing sites (`demo/DESIGN.md:237` · `EditorStartScreen.vue:159` · `SequenceTarget.css:212` — its D-3), and the roster RECEIVES the R-8 residue families (D-13's 7 names + `proof:demo-no-oversize`'s 16×13 sweep) as declared inbound edges. One roster, one owner, one sweep.

---

## R-18 · KF.W10's OWNERSHIP GAP — recommended owner: **SS-2** (UNION §3.2 tail; KF-W10-CHECK D-9; COHESION §0b symmetry)

**Recommendation (the ruling this seat can make; COHESION amendment is the root session's act)**: **KF.W10 belongs to SS-2** (X·KF frontend/coordination seat). Grounds: COHESION §1 splits X·KF authoring SS-1 = library half (KF.W0/W2/W4/W5/W8, + W3 by §0b) and SS-2 = frontend + **mail-cure** half (KF.W1/W6/W7/W9); KF.W10 is the Fold-Discharge/coordination-terminal wave whose hardest dependency is KF.W1 (SS-2's Mail Cure — O-8/O-11 exec-visible: W10 OP-3, G-6, `:296` "W10 depends"), whose content is ledger/mail terminalization under the owner's E13 law, and which no library artifact gates.

**Cure shape**: a **COHESION §0c addendum** mirroring §0b: *"KF.W10 ownership gap: CURED by assignment — KF.W10 is a coordination/close wave; it belongs to SS-2. Boundary record: this assignment is the SPECIFIED precondition W10's OP-6/§6.C-E names."* Then, at **KF-W10**: D-9's self-stamp tension dissolves (the precondition is met by the addendum, and the file states the file-scope vs sub-tranche-scope distinction in one sentence); OP-6/§6.C-E re-anchored on §0c. At **KF-W3**: OP-3's "(nor is KF.W10)" clause re-anchored on §0c (rides R-7b's OP-3 edit).

---

## R-19 · DECLARED-BUT-UNRECIPROCATED EDGES — the receiving end books or declines, by id (UNION §2.3)

Each sub-item has ONE owner: the declared destination.

- **R-19a · ownerWave KF-W6** — the ten W5 §Excluded re-homes with 0 hits at the destination (KF-SS-4 · KF-SS-6 · KF-SS-31 · KF-SS-38 · KF-ET-33 · KF-ET-35 · KF-ES-36 · KF-SKEL-9 · KF-SKEL-20 · KAD-17): booked at KF-W6 (rows or fold lines; the seven-row prose sweep lands as seven ids, not an ownership rule), and mirrored into the CARRY as E-3 addendum rows. The W5→W6 seam closes at the receiving end (W5's declarations were sound).
- **R-19b · ownerWave KF-W0** — the W6-CARRY §CrossEdges corrections (`App.vue:176` not the stale `:169` · `--dock-margin` = 9 live unfallbacked sites + 1 prose · KF-SKEL-7's cascade-coupling arm beside the import census): booked as G-0.9 appended-correction obligations by id (its D-6; KF-SKEL-7 additionally rides R-1a's fold table).
- **R-19c · ownerWave KF-W8** — the four inbound edges neither booked nor declined: (i) the import-granularity structural half (**KF-KC-37 + SP-18 + KF-KE-53**, from `KF-W6.md:378`) — book-or-decline by id; (ii) **SpringTrace C-3's implementation edge** (from KF-W7) — book-or-decline with the route dissent noted; (iii) **`ingest/cssom.ts` colocation** (from `KF-W5.md:269(c)`) — book, deferring the split disposition to KF.W2's façade decision as W5 declared; (iv) **B-16's conditional shadow-name** (from `KF-W5.md:269(d)`) — book as conditional. Fifteen §Excluded declines exist; these four join one list or the other, never neither.
- **R-19d · ownerWave KF-W1** — the reciprocity paragraph (`:39`) and §9 item 5 gain **KF-W10** (the sibling that hard-blocks on this wave three ways: `KF-W10.md:16/:50/:296`); the measured set becomes the authored set (W0·W2·W3·W4·W7·W8·W10).
- **R-19e · ownerWave KF-W6** — **KF-EST-4**: the spec books it (§2 census) but the CARRY has 0 hits; add the CARRY row as an E-3 addendum so W9's S-9 before/after hazard has its counterparty (W9's D-14 needs no W9-side edit).

---

## §END · PER-WAVE DIRECTIVE MAP (what each repair seat executes, verbatim, from the rulings above)

| wave | n | directives |
|---|--:|---|
| **KF-W0** | 6 | R-1a (book the 12 + riders) · R-5 (C-17 single-motion mint, full roster, KSM demotion) · R-11 (7 × `npm run gh-pages`) · R-15 (canonical 17 roster at :276) · R-17 (G-0.7 roster widened +3 sites, receives R-8 residue families) · R-19b (three W6-CARRY §CrossEdges corrections booked) |
| **KF-W1** | 2 | R-15 ("16"→17 receipted at :126/:219/:236; W11/W12/W13 membership canonical) · R-19d (reciprocity gains KF-W10) |
| **KF-W2** | 4 | R-1b (adopt the 4 boundary escapes; G-W2-1 → ≥11) · R-10 (KF-AV-28 clause; L-6/C-4 conditional) · R-12 (phantom CARRY struck; denominator 47) · R-16 (OP-6 → ORDERED, W8 precedes) |
| **KF-W3** | 7 | R-7a (E-12 re-founded; C-5 conjunct restored; C-4/C-5 pair reading) · R-7a companions (E-8 reason; G-KF3-1 discharge; G-KF3-8/9 → floors, 7 gates) · R-7b (10 escapes as denominator rows/exclusions-with-owner-W2) · R-7b (OP-3 → COHESION §0b) · R-10 (KF-AV-28 clause on the W7-routed rows) · R-12 (phantom CARRY struck; denominator 19/24) · R-18 (OP-3's KF.W10 clause → §0c) |
| **KF-W4** | 8 | R-1c (SpringTrace D-1 rider at row 29) · R-3 (L-7 → witness-only; Bounds :61 re-cut) · R-6 (PROSE split rule one reading + limb manifest → W6) · R-8 (G-KFW4-8 re-scope + residue table + fixture create-rows + paths + head-witness count) · R-10 (KF-AV-28 clause at §Sequencing head) · R-12 (phantom "CARRY ledger whole" struck; AUDITED basis restated) · R-15 (canonical homing: W11 ×9 · W12 ×6 · W13 minted ×2) · R-17 (OP-3 RESOLVED; E2 carried; gates inventory → frontier 9) |
| **KF-W5** | 3 | R-9b (G-XSS observability: LP-7+ME-32 carried; third rider; OP-6 amended; fixture/disjointness) · R-12 (phantom SS-1 ledger struck; 44+12=56 recount lands) · R-14 (D-6 addendum: lock satisfied by decline-whole; G-STRUCT owns the 16) |
| **KF-W6** | 8 | R-1d (adopt the 6: #21/#30/#39 · SquareInstrument D-20+relay · CubeScene D-20 · flat-namespace · alias lines) · R-2 (Bounds L54 carve list amended; -24 rider/-32 in) · R-5 (KSM C-9→S-10 booking: "pending the W0 mint") · R-6 (book the non-proof limb manifest, D-7/L-4 first) · R-10 (KF-AV-28 clause + guards on AV-11/PlaybackRibbon/KF-SCR-2) · R-13 (one-line move-vs-reshell guard on the →W8 edge) · R-19a (book the ten W5 re-homes, spec+CARRY addenda) · R-19e (KF-EST-4 CARRY addendum row) |
| **KF-W7** | 2 | R-12 (four phantom-CARRY bindings struck; provenance restated over the registry) · R-12 (KAD-15/16/20 → §Excluded exclusions-with-owner-KF.W6) |
| **KF-W8** | 4 | R-13 (own the KF-CB-37 move; R-1 rewritten, phantom NAMED; G6 acting; :117 voice) · R-14 (R-4 covers both structural instances; record → W10; G13 subordinated to G-STRUCT's 16) · R-16 (:254 → W8-precedes order) · R-19c (four inbound edges booked-or-declined by id) |
| **KF-W9** | 5 | R-1e (AT arm carried: D-5/D-6/D-14+N-4 lock/N-4 · SeqAxis D-12+relay · D-m8 · PR D-12 measurement · D17/D18 lock in §Sequencing · KF-CE-47) · R-9a (G-KFW9-10 → declaration; G-KFW9-9 witness-pair only, cure → W13; Bounds write-grants struck; G-KFW9-8 re-worded; 13 gates) · R-10 (KF-AV-28 clause by id) · R-11 (3 × `npm run gh-pages`) · R-15 (S-11 → canonical roster) |
| **KF-W10** | 4 | R-10 (rider = §3.2's seventh bullet; G-2 sweep input) · R-14 (FOLD-FORWARD record row for the declined pair) · R-15 (NWO-TERMINAL-SWEEP re-anchored: amiga claimed, AT cluster at W9, 17-packet denominator) · R-18 (OP-6/§6.C-E → COHESION §0c; D-9 scope sentence) |

**Owner-of-record for the KF.W10 boundary record: SS-2** (R-18; COHESION §0c addendum is the root session's act, flagged upward).

*Cross-wave rulings seat, repair round 1. Sole write = this file. Nothing here stamps any wave; E-3 holds — registry corrections land as addenda under original ids; spec repairs execute at the per-wave seats.*
