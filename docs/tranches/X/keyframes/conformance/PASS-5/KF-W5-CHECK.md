# KF-W5 — PASS-5 FRESH ADVERSARIAL SPEC CHECK (L-18/L-20)

**Subject**: `/Users/mkbabb/Programming/value.js/docs/tranches/X/keyframes/waves/KF-W5.md` — **504 L / 252,821 B, mtime 2026-08-28 17:32:57**, read WHOLE at this seat's clock (five paged reads, nothing skipped).
**Seat**: FRESH. Nothing inherited from `PASS-1`, `PASS-2`, `PASS-3`, `PASS-4/KF-W5-CHECK.md`, `PASS-4/RULINGS-4.md`, `PASS-4/CLOSE-CERT.md`, or from any prior text at this path. Every census row below is re-harvested **by record** from the 58 adjudicated `kf-*.md` bytes; the sole carry consulted is `carry/KF-W6-CARRY.md`.
**Tree law**: keyframes witnesses verify at the frontier, `origin/master` **`81a56990736ced5b5edde0b84c527680ac7689b1`** — re-derived this seat (`git rev-parse origin/master`, exact). Local HEAD `8281638c` (**41 behind / 252 dirty**, both re-run) is historical context only, never a witness substrate. Every keyframes probe below is a read-only `git show` / `git ls-tree` / `git grep` / `git cat-file`. **No product source was opened for writing.**
**Value-side substrate, named because siblings moved after the round closed**: the eleven specs at their current mtimes (§2.0 table), `PASS-4/CLOSE-CERT.md` at **17:37:59**.
**Sole write of this seat**: this file.

**VERDICT: DEFECTIVE.**
Axis 1 (by-record census) **CLEAN** · Axis 2 (receipt reality — the drift class) **FAIL — three MAJOR** · Axis 3 (M-25 depth + LAW-A censuses at the frontier) **CLEAN** · Axis 4 (gates born-RED / reachable GREEN / in-grant closes / no self-voiced closure) **FAIL — three MAJOR** · Axis 5 (posture) **PASS with one recorded program-level wrinkle**.

The round-4 repair is real and the two pass-4 MAJORs it was built for are genuinely closed: **G-TAX is no longer unpassable** (re-cut as an inbound-dependency gate, its act reduced to the RD-2 relay, born-state re-measured 3/0/0 here), and **the SUBJECT-IDENTITY law was finally run over this wave's own §Bounds** — its three live defects cured and its one negative witness declared. Every arm-D denominator, every LAW-A census of record, and every symbol anchor re-executes exactly at the frontier; **not one keyframes-side figure in this file failed at this seat.**

What fails is the axis the round itself named. **LAW C was written to kill same-round sibling drift and this file is where it did not reach**: the wave wrote FIRST of eleven (16:55:55, ahead of KF-W0), handed fifteen cross-wave cells and one four-party access table to the RECONCILE seat by name, and the RECONCILE seat re-measured **one of sixteen** — then stamped, inside this file's own bytes, *"NOT ONE ANCHOR IN THIS FILE FAILED."* Two of the unswept receipts are false at the siblings' final bytes. Beside them sit a cure whose pasted command does not produce its pasted output, a round-3 gate re-cut that was promised and never landed, and — minted this round, in the paragraph that certifies the round — the sentence *"its cross-wave receipts survive a sibling's next keystroke."*

---

## §1 · Axis 1 — ID-KEYED CENSUS, ENUMERATED BY RECORD

### 1.1 Method (partition-free, nothing inherited)

`for f in kf-*.md; do grep -c 'W5' "$f"; done` over all 58 records → **22 files carry the bare token**; `grep -l 'KF\.W5\|KF-W5'` → **19**. Every hit in all 22 was read in full sentence and resolved:

- **16 records route at least one row** (§1.2).
- **3 carry only a `KF.W5-PARTIALS` taxonomy header** — kf-AnimationControlsGroup `:17` · kf-ControlsPaneWrapper `:6` · kf-DemoGlobalChrome `:17`. Each is the *"Status verbs per M-25 … Wave targets"* header naming the superseded `lane-frontend.md §10` sketch; **0 row hits**, reproduced by direct grep (§4.1, G-TAX born state).
- **3 are false positives on the bare token** — kf-SquareInstrument `:7`/`:21` (`R.W5`, tranche R) · kf-OrbitalDrag `:82` (`P.W5.S3`, tranche P) · kf-HeroAurora `:32` (routing-law recital; routes nothing).
- **kf-SquareScene and kf-ChannelOptions return `grep -c 'W5'` = 0.** Their engine-seam / engine-lane rows are homed here by **§0 R-1**, not by token — and the record side of that homing reproduces: kf-ChannelOptions `:154` *"Engine lane (keyframes.js src): the `applyTimingFunction` frame-propagation gap … + the `parseAnimationCSS.ts` per-stop timing-function drop"* and `:205` KF-CO-48 are the three arms the wave carries at B-14/B-15/B-16.
- **No alternate routing spelling exists.** The 36 zero-token records were swept for `tri-fold|trifold|D/L/C|library (letter|roster|arm)|engine seam|engine lane`; every hit is a routing-law recital, an adjudication footer, or kf-ChannelOptions' §0 R-1 arms already counted. **No record routes to KF.W5 by any spelling the token census missed.**

### 1.2 The routed ids, by record, and their booking

| record | routed ids (whole sentences resolved) | n | booked in `KF-W5.md` |
|---|---|--:|---|
| kf-AnimatedText | KF-AT-8 `:43` · KF-AT-9 `:44` · KF-AT-12 `:52` · KF-AT-23 `:63` | 4 | B-1 · B-2 · Arm A (PREMISE) · B-3 |
| kf-App.skeleton | KF-SKEL-9 `:44` · KF-SKEL-16 `:56` · KF-SKEL-20 `:60` | 3 | §Excl 2 · 3 · 4 |
| kf-CopyButton | KF-CB-30 `:31`/`:78` · KF-CB-35 `:83` · KF-CB-36 `:84` | 3 | B-19 · B-20 · B-21 |
| kf-EasingScene | KF-ES-36 `:78` | 1 | §Excl 1 |
| kf-EasingSidebar | KF-ES-20 `:61` | 1 | Arm A |
| kf-EasingTarget | KF-ET-1 `:37` · KF-ET-27 `:69` · KF-ET-32 `:77` · KF-ET-33 `:78` · KF-ET-35 `:80` | 5 | B-22 · Arm A · B-12 · §Excl 1 · §Excl 1 |
| kf-EditorStartScreen | KF-EST-5 `:62` · KF-EST-17 `:79` · P-8 `:136` | 3 | B-4 · B-17 · B-4 leg 2 |
| kf-KeyframeCard | KF-KC-27 `:69` | 1 | B-7 |
| kf-KeyframesAddDialog | KAD-1 `:42` · KAD-2 `:46` · KAD-3 `:47` · KAD-5 `:49` · KAD-13 `:57` · KAD-14 `:67` · KAD-17 `:70` | 7 | Arm 0 ×5 · §Excl 5 · §Excl 6 |
| kf-KeyframesEditor | KF-KE-10 `:52` | 1 | B-9 |
| kf-KeyframesStringControls | N-8 `:55` · C-8 `:81` | 2 | B-10 · B-11 |
| kf-SpringPhysicsFacet | SPF-20 `:60` | 1 | fold at B-9 |
| kf-SpringScene | KF-SS-4 · KF-SS-6 · KF-SS-31 · KF-SS-38 ⟨the `:151` KF.W5 roster line names all four⟩ | 4 | §Excl 1 (all four) |
| kf-TimelineHoverPreview | C-8 `:85` ⟨≡ KF-SKEL-16⟩ | 1 | §Excl 3 (folded) |
| kf-TimingFunctionPanel | KF-TFP-21 `:64` · KF-TFP-27 `:73` | 2 | B-13 (one row, two ids) |
| kf-TypingDots | KF-TD-1 `:57` · KF-TD-5 `:61` · KF-TD-8 `:64` · S★-2 `:88` | 4 | B-6 · B-8 · B-18 · B-5 |

**routedTotal = 43** (4+3+3+1+1+5+3+1+7+1+2+1+4+1+2+4).

**Booking verified by bytes, id by id** — `grep -o '<id>' KF-W5.md | wc -l`, run for all 43: every one returns ≥1. Singletons: KF-CB-36 · KF-KC-27 · KF-TD-5 · KF-TD-8 · S★-2. Densest: KAD-1 **33** · KAD-14 **14** · KF-ES-20 **11** · C-8 **11** · KF-SS-31 **10** · P-8 **9** · KAD-3 **9**. **bookedCount = 43 · escapedCount = 0.**

**Whole-sentence resolution added nothing the spec dropped.** kf-TypingDots `:39`/`:46` are *witness ×2* restatements of KF-EST-5 / KF-EST-17 (carried at B-4/B-17 as `witness ×2`, not re-booked); kf-EditorStartScreen `:24` is the L-EST-5 mechanism ruling, not a routing; kf-CopyButton `:31` is the C-11/M-4 severity cell whose row is KF-CB-30 at `:78`; kf-EasingTarget KF-ET-1's *"+ D-15 and C-2-as-rescoped folded"* travels with B-22's rider carriage; KAD-14's `(a)(b)(d)(e)` / `(c)` split is carried whole (a/b/d/e at Arm 0, c at §Excl 6).

### 1.3 The ledger arithmetic, re-derived not inherited

Rows counted mechanically off the tables: **Arm 0 = 7** (KAD-1 · KAD-2 · KAD-3 · KAD-14 · KAD-5 · LP-7's rider · ME-32) · **Arm A = 8** (X-4/C-6 · B10-21 · the D/L/C rubric · KF-ET-27 · KF-ES-20 · G-ID · G-TAX · KF-AT-12) · **Arm B = 22** (B-1…B-22) · **Arm C = 3** (C-1…C-3) · **Arm D = 6** (D-1…D-6) → **46**. §Excluded = item 1's seven prose rows + items 2–6 → **12 rows / 13 ids** (item 3 carries KF-SKEL-16 *and* kf-TimelineHoverPreview C-8). **46 + 12 = 58** ✓. The 30/13 split reproduces: 43 − 13 = 30 across 27 §Carry rows (B-4 = KF-EST-5+P-8, B-9 = KF-KE-10+SPF-20, B-13 = KF-TFP-21+KF-TFP-27). §Gates holds exactly **20** gate rows (`grep -cE '^\| \*\*G-[A-Z-]+\*\*'` → 20; the twenty ids enumerated and unique).

**AXIS 1 — CLEAN.** No escape by bytes. No row inside a table the tally omits. No double-home. No id renamed or re-homed away from its bank. Three fresh by-record enumerations at three passes have now returned the same three figures.

---

## §2 · Axis 2 — RECEIPT REALITY: DOES EVERY CROSS-WAVE RECEIPT MATCH **FINAL** SIBLING BYTES?

### 2.0 The substrate table this axis is judged against

`stat -f '%Sm %z %N'`, run this seat:

| spec | mtime | bytes | stamped in `KF-W5.md` as |
|---|---|--:|---|
| `KF-W4.md` | **16:59:44** | 209,984 | *"mtime 2026-08-28 **15:49**"* (§0 R-1.4) |
| `KF-W6.md` | **16:59:25** | 240,976 | *"mtime 2026-08-28 **15:50**"* (§Excluded) |
| `KF-W7.md` | **17:33:23** | 210,113 | *"mtime 2026-08-28 **15:39**"* (§Excluded) |
| `KF-W8.md` | **17:34:06** | 195,418 | anchor-only (§Disjointness) |
| `KF-W9.md` | **17:32:08** | 221,965 | anchor-only (B-6) |
| `KF-W10.md` | **17:38:21** | 209,923 | anchor-only (§Sequencing) |
| `carry/KF-W6-CARRY.md` | **10:49:45** | — | *"mtime 10:49"* ✓ genuinely frozen |
| `KF-W5.md` (this subject) | **17:32:57** | 252,821 | — |

**Three stale stamps** (D-11). The file discloses that it may be superseded and names the RECONCILE seat as the certifying end — which is exactly why the sweep's completeness is this axis's subject.

### 2.1 What holds at FINAL bytes (the large majority)

Re-executed here, all exact: `grep -n 'The ten rulings this spec owes' KF-W4.md` → **`:98`**, and R-1's operative sentence live at **`:123`** — *"**Census S-2's owner is hereby named: KF.W6 (Glass Suffusion), with KF.W10 taking any doc-authority addenda.**"* — **byte-identical to the quotation at all three of this file's sites** (§0 R-1.4, RD-3, §Excluded item 1) · `grep -n 'KF.W4-PROSE SPLITS BY MECHANISM' KF-W6-CARRY.md` → **`:12`**, unchanged · CARRY `:9`/`:13` the KF.W5-PARTIALS reconciliation (*"KF.W5-PARTIALS ⇒ THIS WAVE (S-5/S-6/S-7, each re-homed by its bank)"*) ✓ · CARRY `:170` constraint 1 = *"KF-AT-8 — a NAMED BLOCKER-OF-THE-MIGRATION"* ✓ · CARRY `:248` KAD-3 ✓ · CARRY `:319` *"(KAD-1 is KF.W5's)"* ✓ · CARRY `:356` carrying **verbatim** *"KAD-1's `textContent` sink fix is **FRONT-LOADED at W5**"* ✓.
`KF-W7.md:57` the fold-SOURCE row — *"fold SOURCE; its `innerHTML` BLOCKER is KF.W5's, front-loaded, NOT gated here"* — live and reciprocated at `:351` ✓ · W7's **four** creates at `:63-66` ✓ · W7 `:68` *"never globbed by shared prefix"* ✓ · W7 `:79` uses **eleven** as the four-party total, exactly the sense this file attributes to it ✓.
`KF-W4.md` reciprocates the third rider at **row 3 (`:165`, ME-32)** and at **`G-KFW4-2` (`:211`, `npx vitest run --project demo` **and** `npm run demo:correctness`, both blocking merge steps)** ✓; W4's two `test/demo/instrument/` creates at `:82`/`:83` and its `resize-tracks.test.ts` cure site at `:74` ✓ — **W4 2 + W7 4 + W8 4 = TEN**, so the round-4 *eleven → TEN* correction is right at final bytes.
`KF-W9.md` books the R4-3 reciprocal from its end (`:229`, quote-by-command against this file; `:225` the re-cut CLOSES; `:250`/`:264` the KF.W5 cross-edges) and this file's B-6 carries the inbound ✓.
`KF-W10.md` carries **KF-W8-R-4-STRUCT-PAIR ⟨origin KF.W5 D-6⟩** at `:98`/`:213`/`:320`/`:504`/`:606` with terminal verb **`DECLINED-FOR-X·KF, carried forward`**, RECORD-ONLY, **plus the three re-open triggers carried verbatim** (`:232`) ✓; KF-AT-21 (`:89`), the `345/12/57 @ 8281638c` citation law (`:411`/`:423`) and the slot-number falsifier are all W10's, as this file states ✓.
**Fourteen of the fifteen receiving-end cells reproduce exactly** at KF-W6's final bytes: KF-SS-4 0/2 · KF-SS-6 0/1 · KF-SS-31 0/4 · KF-SS-38 0/1 · KF-ET-33 0/2 · KF-ET-35 0/3 · KF-ES-36 0/2 · KF-SKEL-9 0/4 · KF-SKEL-20 0/1 · KAD-17 0/1 · KAD-3 2/1 · KF-AT-8 2/5; KAD-13 **2** and KF-SKEL-16 **2** at KF-W7 ✓. **The CARRY side is confirmed 0/10, still owed, as the file states.**

### 2.2 The failures

**D-1 · MAJOR — the DH-3 four-party table states KF.W8's access as a grant KF.W8 STRUCK this same round, and the wave draws a conclusion from it.**
§Disjointness (`:277`) reads: *"**KF.W8** — **modify-carve over the nine tracked files** (`KF-W8 §Bounds`, the `test/demo/instrument/` — the 9 tracked files, ENUMERATED BY NAME` row)"*, and `:279` derives: *"**The one genuine WRITE overlap is W8's modify-carve over the nine tracked files against W4's `resize-tracks.test.ts` cure site**."* Measured at KF-W8's final bytes:
```
sed -n '93p' waves/KF-W8.md
| test/demo/instrument/ — the 9 tracked files, ENUMERATED BY NAME … | **READ-ONLY** (the nine) —
  ***access NARROWED at repair round 4*** | … The round-3 grant of **modify-carve over all nine** is **STRUCK** …
  this row's own DH-3 text names exactly ONE cure site, `resize-tracks.test.ts` …
```
KF.W8 holds **no** modify-carve over the nine; it holds one, on `resize-tracks.test.ts` (`KF-W8.md:94`). The overlap still exists — but it is W4's cure site against W8's **single** row, not against a nine-file carve, and the sentence that a third end is supposed to read is false about the sibling's access column. **This is a stage-1 → stage-2 forward reference carrying substantive prose about a sibling's grant, which LAW C(3) reduces to anchor-only precisely so this cannot happen**; the row identity is anchored correctly and the *access* rode along uncited. The RULINGS-4 directive that struck W8's row (§END KF-W8 D-8) and the directive that re-ran this file's DH-3 count (§END KF-W5 D-7) were issued in the same document, to the same round, and neither end reconciled the access column.

**D-2 · MAJOR — the receiving-end control cell `KAD-1 12 / 16` is stale; at KF-W6's final bytes it is `12 / 20`.**
```
grep -o 'KAD-1' carry/KF-W6-CARRY.md | wc -l   → 12   ✓
grep -o 'KAD-1' waves/KF-W6.md      | wc -l   → 20   (printed 16)
```
The block that prints it (`:455`) is the file's own instrument for *"a frozen reading of a moving tree is a defect"*, it names its substrate honestly (`KF-W6.md` at 15:50), and it hands the whole block forward by name: *"**KF.W6 is a STAGE-1 sibling and may write again this round** … the RECONCILE seat re-verifies every cell after the last per-wave write (LAW C(4))."* **The RECONCILE seat re-verified ONE cell of the fifteen** — KF-SS-31 — and certified the file whole. The ten routed cells and two of the three controls survived; the third did not. **The class the round was built to kill is alive at one cell, and the instrument built to catch it did not run over the block that asked for it.**

**D-3 · MAJOR — Act 1's view-transition row: the pasted command does not produce the pasted output, inside this round's own cure for a count defect.**
The round-4 correction (`:116`) writes: *"Re-run this seat: `git grep -nF 'orchestration/view-transition' origin/master -- src test` → the five above"*, where **the five** are `index.ts:123`, `:128`, `orchestration/index.ts:52`, `:57`, `test/orchestration/view-transition.test.ts:10`. Measured this seat, that command returns **SIX** lines and **neither of the two `orchestration/index.ts` hits is among them**:
```
src/animation/compile/emit/view-transition.ts:38          (prose — "its LIGHT twin is `orchestration/view-transition`")
src/animation/index.ts:123 · :128
src/animation/orchestration/view-transition/index.ts:2     (the module's own docblock)
test/orchestration/view-transition.test.ts:2 · :10
```
`orchestration/index.ts:52`/`:57` spell **`from "./view-transition"`** (verified: `export { viewTransition } from "./view-transition";` / `} from "./view-transition";`) and cannot be reached by an `-F 'orchestration/view-transition'` grep. The **barrel figure 5 is defensible by resolution** and the 32-site migration surface is unaffected; what is not defensible is that a command was pasted as the producer of an output it does not produce — **LAW D(3), in the one row this file singles out as *"the R3-1 shape, found here before it could authorize anything"*, and in the cure written for pass-4's D-11, which was itself a count defect in this same row.**

**AXIS 2 — FAIL** (D-1, D-2, D-3). Every anchor in this file resolves; three receipts do not match the bytes they are about.

---

## §3 · Axis 3 — M-25 DEPTH AND THE LAW-A CENSUSES, RE-EXECUTED AT THE FRONTIER

### 3.1 Every sampled command reproduces

Run at `81a56990` this seat, nothing inherited:

- **Structure**: `git ls-tree -r 81a56990 --name-only -- src | grep -c '\.ts$'` → **153** (B0) · `.d.ts` → **0** ✓. God-module sweep at the ≥437 floor over all 153 → **exactly eight, the identical list and identical counts** (`progress.ts` 484 · `engine/animation.ts` 478 · `draggable.ts` 470 · `ingest/cssom.ts` 466 · `compile/frame/compiler.ts` 461 · `compile/emit/entry.ts` 459 · `classic-data.ts` 458 · `group/group.ts` 437), **no new entrant**, `engine/play-lifecycle.ts` MISSING ✓. Stutter predicate run **verbatim as printed** → **12**, the identical twelve paths in the identical order ✓. `git rev-list --count 8281638c..origin/master` → **41** · `git status --porcelain | wc -l` → **252** ✓.
- **§0 R-2's reconciliation, arithmetic re-derived**: `find src -type f -name '*.ts' | wc -l` → **145** · `git ls-files -- 'src/**/*.ts' | wc -l` → **139** · `git status --porcelain -- src` → **84 M · 2 D · 8 ??**. **139 − 2 + 8 = 145**, exact — the two figures the reconciliation exists to settle still differ by exactly the uncommitted WIP.
- **Depcruise**: file **253 L** ✓ · `LIGHT_BARREL_MODULES` opens `:54` (`const LIGHT_BARREL_MODULES = [`), closes `:79` (`];`), `sed -n '55,78p' | grep -c '"'` → **24** ✓ · `LIGHT_FROM` `:84-86` ✓ · `no-cycle` `:113` ✓ · the honest denial spanning **`:125-129`** verbatim (*"There is NO known-violations baseline … not wired (`lint` is a bare `depcruise src`, no `--known-violations` flag)"*) ✓ · `viaOnly.dependencyTypesNot` **`:148-149`** ✓ · rule 2 `leaf-no-engine-no-valuejs` `:163` ✓ · rule 3 `light-barrel-no-engine` `:203` ✓ · `grep -c 'knownViolations'` → **0** · `grep -c 'known-violations'` → **3** ✓. **The three-clause property oracle is sound and its round-1 count-inversion stays dead.**
- **SUBJECT-IDENTITY, all four declared zeros re-measured**: `snapToReducedMotion` in `internal/reduced-motion.ts` → **0**, while `ReducedMotionPolicy` resolves at **`:107 :110 :125 :144 :154`** ✓ · `setTimingFunction` in `engine/option-setters.ts` → **0**, sole declaration `engine/animation.ts:260` ✓, `applyTimingFunction` `:32` decl / `:149` call ✓ · `steps` in `waapi/emission.ts` → **0** ✓ · `withReducedMotion` in `waapi/delegation.ts` → **0** ✓. **`setTargets(...targets: HTMLElement[])` → exactly three declarations** — `engine/animation.ts:465` · `group/group.ts:195` · `sequence/sequence.ts:310` ✓.
- **Act 4 / B-10 / G-CSSIDENT's shared census**: the `cssIdent` chain is exactly two re-exports — `backward/walk.ts:145` (decl; applications `:89`/`:110`/`:131`, docblock `:9` — five hits, one file) → `backward/index.ts:29` → `emit/index.ts:53` ✓; **0 hits across `index.ts`, `public.ts`, `load-engine.ts`** ✓.
- **Act 6 (added this round)**: `useCodeHighlight`'s two call sites are `KeyframesEditor.vue:176` (`{ highlightAll }`) and `KeyframesAddDialog.vue:92` (`{ setHighlightingString, highlightAll }`); the return object at `:147` is `{ setHighlightingString, highlight, highlightAll }`. **Neither destructures `highlight` — the delete is census-true** ✓. The named non-subject reproduces: `useHighlightCSS` declared `:27`, live consumer `useApplyCSS.ts:25` (`{ setContent, clear: clearStyle }`) ✓.
- **LAW-A Act 1 spot-checks**: `group/group.ts`'s five consumers all exact (`group/index.ts:15` · `layer-api.ts:21` · `lifecycle.ts:30` · `waapi.ts:14` · `composite/compositor.ts:33` via `../group`) ✓; the barrel four (`index.ts:175/:287` · `public.ts:55/:60`) ✓; the two named same-basename NON-consumers verified at the bytes — `demo/app/scene/scenes.ts:20` = `SequenceIcon from "@assets/icons/sequence.svg?component"` (an asset) and `demo/components/instrument/index.ts:26` = `export * from "./timeline"` (the demo's own dir) ✓; the sibling module `compile/emit/view-transition.ts`'s seven consumers all resolve (`compile/index.ts:57/:64` · `index.ts:255` · `public.ts:143/:150` · both `test/compile/` specs) ✓.
- **`@src/animation` in `demo/`**: **9 hits / 8 files**, decomposing to the **SEVEN import specifiers across SIX files** plus the two prose-only comments (`demo/kf-engine.ts:5` · `useOrbitalInertia.ts:12`) — **the honest divergence from RULINGS-4's ordered *"9 hits / 7 files"* is correct as written, and the import-level decomposition matches R4-2's own census exactly.**
- **Gate born-states**: G-XSS — `innerHTML` in `useHighlightCSS.ts` → **two lines**, `:111 el.innerHTML = s;` and `:123 el.innerHTML = h.value;`; property form **1 today → 0 after the cure** ✓ **RED and REACHABLE**. G-PRM-FLIP — `snapToReducedMotion` under `src/` → **7 lines / 3 files** exactly as printed, with `strategies.ts:76` (decl, under the `:66-75` docblock) and `frame.ts:121` (`playFrame`) / `:137` (the call) ✓. G-TAX — **3 header hits · 0 SUPERSEDED (0/0/0) · 0 row hits** ✓ **genuinely born-RED**. G-SHIM — presets holds `catalog.ts`, `classic-data.ts`, `index.ts` only; `grep -c 'split by kind'` → **0** ✓ honest floor. G-DEPCRUISE L-1 — the four allowlist names still resolve to nothing while `solver/{duration,reseat}.ts` and `css/{linear-stops,timing-function}.ts` sit un-named; all seven paths PRESENT ✓ **RED, unmoved by 41 commits**.
- **Anchor sample, re-read at the bytes**: `group/types.ts:27-29` (`AnimationGroupInput`, three lines, no `options` field) ✓ · `orchestration/stagger.ts:15-22` with `options: { delay: … }` at `:20` ✓ · `group/group.ts:159-161` the `animations.every(…)` derivation and `:195` `setTargets` ✓ · `compile-bridge.ts:88` `adoptCompiled` ✓ · `engine/css/animation.ts:108` *"reference comparison (`usesDefaultRenderer`)"* and `:110` `resolveTransform` ✓ · `physics/playback.ts:113` `_run` / `:139` `const result = step(now)` ✓ · `load-engine.ts:123-124` the `??=` memoization ✓ · `constants/types.ts:182` `Partial<{` / `:195` `| string` ✓ · `easing.ts:50` `cssTwinFor` / `:79` the lazy registry import ✓ · `backward/backward.ts:253-257` the unguarded `staticBlock ?? … ` chain and `:352` `compileToCSS` ✓ · `split-text.test.ts:72-76` exactly the **explicit**-`role="heading"` test ✓ · `test/demo/instrument/` holds **exactly the nine** named files with `highlight-css-roundtrip.test.ts` **absent** ✓ · all six "specs it extends" PRESENT ✓ · **none of the 13 created filenames collides with a tracked one** ✓ · `package.json:37/:38/:44/:50/:81` ✓ · `vitest.config.ts:26` `@kf-engine → demo/kf-engine.ts`, `:38` `projects`, `:52` `include: ["test/demo/**/*.test.ts"]` ✓.

**Not one keyframes-side figure in this file failed at this seat.**

### 3.2 M-25 depth

- **Preserved dissents**: §0 R-3 (KF-KE-10 MAJOR vs SPF-20 BLOCKER-at-its-bank; bank governs, dissent preserved, stated as the reason B-9 heads the ruling queue) ✓ · B-19 (*"Reader-2's MINOR as dissent"*, matching kf-CopyButton `:31` verbatim) ✓ · B-14 (RR-A whole-at-BLOCKER / RR-B WAAPI-half → RESCOPED-BY-SUBSTRATE) ✓ · B-4 (kf-TypingDots ruling 3 **overruling** reader-2's demotion — *"the kill demotes the CURE only, never the defect"*) ✓ · B-6 (C's S★-4 second bullet STAYS KILLED; L's S-3 SURVIVES SCOPED) ✓.
- **Cure-locks carried, not paraphrased**: B-22's KF-ET-2 lock (never *"bind `cssValue`"*, never a silent approximation; `easing.ts:41-50`'s doctrine as the authority) with the row itself NOT taken ✓ · §Excl 1 KF-SS-38's *"the sweep must not resurrect the killed claim"* ✓ · §Excl 6's *"the killed BLOCKER arm (mangled parse) is DEAD and must stay dead"* ✓ · §Excl 2's *"the cure may not be authored in a way that resurrects C·D-4's premise"* ✓ · KAD-14(a) as a same-commit trap ✓ · D-6's R-14 addendum carried **verbatim** with its correcting sentence beside it (E-3, never a patch) ✓.
- **Riders as rows**: LP-7's rider and ME-32 are Arm 0 **rows** (which is why Arm 0 is 7) precisely because G-XSS's observability depends on them ✓ · kf-ChromeDock M-4's MbabbMenu MUST-CARRY rider travels with the packet ✓ · kf-SquareScene's *"treat every comment-stated invariant as a test obligation (L-19)"* carried verbatim ✓.
- **Fold identities never re-booked**: SPF-20 ≡ B-9 · THP C-8 ≡ KF-SKEL-16 · KF-CB-15 ≡ m-4 ≡ C-7 folded as C-3's witness with its bytes named · KF-CB-6 folded at B-7 · KF-CE-42 folded at B-11 · KF-ET-34 folded at §Excl 1 · KAD-14(d)'s T `lane-18` #6 identity ✓.
- **Transcription-only**: absent. Every arm carries a mechanism, a lock, or a ruling; the four RULINGS each precede their fixes with a stated reason.
- **The R4-3 reciprocal** is booked at both ends with the constraint quoted from the dated authority rather than from a moving sibling ✓ — the round-1 D-3 class is retired at its third gate.

**AXIS 3 — CLEAN.**

---

## §4 · Axis 4 — GATES: BORN-RED WITH REACHABLE GREEN · IN-GRANT CLOSES · NO SELF-VOICED CLOSURE

### 4.1 What the round genuinely closed

**G-TAX's pass-4 MAJOR is CLOSED.** The gate is re-cut on the R3-8 inbound-dependency template: **this wave's act is the RD-2 relay row** (present, naming all three records, the superseded authority `lane-frontend.md §10` and the binding `CENSUS-2026-08-03:177-206`), and the SUPERSEDED annotations are declared **the receiving owner's act, as E-3 dated addenda under original ids** — with an observability rider on the G-XSS form. The falsifier now names the failure the old gate invited (*"fails if this wave annotates a `registry/adjudicated/` file itself"*). Born-state re-measured here: **3 / 0 / 0.** The relay leg is checkable today under any shell. **An unpassable oracle became an in-grant act plus a declared dependency — the cure is exactly the one the shape needed.**

**G-XSS remains RED with a reachable GREEN**, re-executed at the bytes (§3.1); **G-DEPCRUISE**'s three-clause property oracle, **G-SHIM**'s declared floor, **G-STRUCT/G-BASIS/G-RING**'s measure-at-open probes, and the eleven non-count oracles all reproduce. The twenty-gate inversion sweep now prints twenty verdicts, not nine (pass-4 D-8, closed).

### 4.2 The failures

**D-4 · MAJOR — G-CSSIDENT's promised re-cut never landed; the gate as written asks for the publication of two symbols its own wave ruled the library does not own.**
The round-3 dated correction (`:164`) closes: *"**G-CSSIDENT's five-name assertion is re-cut accordingly at its own cell**"* — the correction being that at `81a56990` only three of C-8's names are library symbols, `debounce` and `convertPixelsToCh` being declared in `demo/utils/helpers.ts` (`:16`, `:28`) and reached through the demo alias `@utils/*`, with **zero declarations under `src/`** (re-verified this seat: `git grep -c 'debounce' 81a56990 -- src` → 0; same for `convertPixelsToCh`). **Two repair rounds later the gate cell is unchanged.** `sed -n '394p' KF-W5.md`:
```
GREEN: "`cssIdent` (and the C-8 four, or their declared relocations, and KF-ET-32's name→fn registry)
        resolve from `loadAnimationEngine()` or the exports map"
RED  : "Same greps clear `reverseCSSTime`, `serializeTimingFunction`, `debounce`, `convertPixelsToCh`."
```
Read literally, GREEN requires two demo-local helpers to resolve from a library entry — the act Act 4 and B-11 both forbid in terms (*"the two demo-local helpers are a colocation question for KF.W8, not something this wave may publish out of a library that does not own them"*). The *"or their declared relocations"* clause is the only thing keeping the gate passable, and it was written for a different purpose. **This is the pass-4 D-4 shape one gate over** — a GREEN condition a lawful seat cannot satisfy inside the wave's own rulings — surviving because the correction was declared at the census and never carried to the cell the census promised to fix.

**D-5 · MAJOR — the SUBJECT-IDENTITY sweep, this round's flagship, prints a bare denominator and undercounts its own zeros.**
The sweep asserts *"**Enumerated and executed this seat: 34 symbol × file pairs, one command each. 31 resolve. FOUR zeros**"* and closes *"**Scope receipt: 34 symbol-bearing pairs swept + 1 pathless row · 5 findings · 5 cured or declared · 0 outstanding.**"* Two defects:
1. **The 34 has no enumeration and no probe.** Only the four zeros are printed; the 34 pairs are never listed and no command produces the figure — against this file's own **G-BASIS** falsifier (*"Fails on … any figure quoted without its probe"*) and against LAW D(3)'s S-10.1 pattern (*"a count asserted over an enumeration is stated AT the enumeration with its counting rule"*). It is the one figure in the round-4 additions that a reader cannot re-run.
2. **The zero-set is short.** The §Bounds row `public.ts · index.ts · load-engine.ts | modify | the publication decision surface (`cssIdent` + C-8's four + KF-ET-32's registry)` names symbols with **zero hits in all three files** — measured this seat, `cssIdent` **0/0/0**, `reverseCSSTime` **0/0/0**, `serializeTimingFunction` **0/0/0**: **nine symbol × file zeros**, none of which appears in the four-zero table. They are lawful **negative witnesses** — the zero *is* the RED G-CSSIDENT measures — which is precisely why the sweep declared `withReducedMotion`'s zero *"so no later sweep re-files it"* and should have declared these the same way. **The round whose whole subject is that the enumeration source is the SCOPE and not the example list published a scope receipt reading "0 outstanding" over a sweep that is short by at least three pairs.**

**D-6 · MAJOR — the ⟨RECONCILE SEAT⟩ block inserted into this file's own bytes certifies a whole-file verification it did not perform.**
`KF-W5.md:499` states: *"**Re-verified by this seat at the true final bytes, and every anchor resolves** … **NOT ONE ANCHOR IN THIS FILE FAILED; the stale stamps cost it nothing**."* The block names four verifications (W4's `:98`/`:123`, the CARRY bullet `:12`, W9's B-6 reciprocal, and KF-SS-31's count). It does **not** touch the other fourteen receiving-end cells the §Excluded block hands it by name, nor the §Disjointness four-party access table. **Both unswept surfaces contain a receipt that is false at final bytes (D-2, D-1).** `PASS-4/CLOSE-CERT.md` §3 records this file at *"6 same-round receipts · anchors all hold · fixed 1"* and §9 item 2 certifies *"EVERY same-round cross-wave receipt … 47 of 47"*. The certification is inside the subject file; the claim is falsifiable from the subject file; it is false.

**D-7 · MAJOR — a self-voiced closure claim, minted this round, refuted by this pass.**
§END's closing sentence (`:503`): *"What changed is that its rows now name the symbols they carve, **its gates can be turned by the seats they bind**, and **its cross-wave receipts survive a sibling's next keystroke**."* The third clause is refuted by **D-1** and **D-2**; the second by **D-4**. Both are completeness claims in the file's own voice over enumerable sets — **the exact LAW B item-1 shape converted at D-13 one round earlier**, re-minted in the paragraph that certifies the conversion. Fifth consecutive generation of the class, and the first time it appears in a sentence written *about* the class.

**D-8 · MINOR — `PASS-4/CLOSE-CERT.md`'s "wrote last" proof is false at the tree.** §1 tables `KF-W10.md` at **17:31:54 / 209,902 B** and §9 item 1 certifies a nine-minute clear margin. Measured: `KF-W10.md` is **17:38:21 / 209,923 B** — written **22 seconds after CLOSE-CERT.md itself (17:37:59)** and four minutes after the reconcile seat's last spec edit (`KF-W8.md`, 17:34:06). The round's closing artifact was superseded by a per-wave write. This file's §END makes CLOSE-CERT the certifying end (*"without it the round did not close"*), so the defect reaches this subject even though every W10 receipt here is anchor-only and unaffected.

**D-9 · MINOR — CLOSE-CERT §7's *"Live line-numbered cross-cites: ZERO"* is false of this file, and this file is the counterexample.** The sweep's regex is `KF-W[0-9]+\.md:[0-9]+`, which cannot match `KF-W6-CARRY.md:NNN`. KF-W5 carries **six live CARRY line-cites with no strike marker** — `:9` ×1, `:170` ×3, `:248`, `:319` (plus `:12` and `:356` cited without the filename prefix). **All six resolve** at the genuinely frozen CARRY (10:49:45), so nothing is at risk; the universal is what fails, in the section that declares the idiom verified.

**D-10 · MINOR — Act 6's two pasted commands are short transcripts.** `git grep -n 'return {' … useHighlightCSS.ts` returns **three** lines (`:20`, `:63` `{ keyframesStyle, setContent, clear }`, `:147`); the census prints one. `git grep -n 'useCodeHighlight' origin/master -- demo/ test/` returns **five** lines (the four call/import sites **plus the declaration `useHighlightCSS.ts:78`**); the census prints four. **Both conclusions hold** — the consumer set is exactly two call sites and neither destructures `highlight`. This is the *"short transcript"* class `CLOSE-CERT` §8 names as having fired at three seats *in the round that named it*; it fired at a fourth, in a census **added this round**, and the reconcile sweep does not cover own-frontier commands.

**D-11 · MINOR — three stale substrate stamps, and they are the mechanism behind D-1/D-2.** §0 R-1.4 names `waves/KF-W4.md` at **15:49** (final 16:59:44); §Excluded names `waves/KF-W6.md` at **15:50** (final 16:59:25) and `waves/KF-W7.md` at **15:39** (final 17:33:23). LAW C(2) is satisfied — the substrate is named — but LAW C(1)'s *"stage 1 … in any order"* let this wave write **first of eleven** (16:55:55, ahead of KF-W0's 16:58:59), so every stage-1 peer it quotes wrote after it. The outputs were verified here and hold **except the one at D-2**.

**D-12 · MINOR — the `withReducedMotion` cell prints a figure its own pasted command does not return.** The cell reads *"live in **10** other `src/` files — `git grep -l 'withReducedMotion' origin/master -- src/`"*. That command returns **11** files, `internal/reduced-motion.ts` among them. *"10 other"* is derivable by subtracting the declaring file, so the prose is defensible; **the numeral beside the paste is still not the paste's output**, which is what LAW D(3) legislates.

**D-13 · MINOR — the LAW-A access-column scope receipt closes with the banned shape.** `:186`: *"**6 rows enumerated · 6 censuses present.** **No other §Bounds row carries a delete, repoint, move or shim verb** — every remaining row is `modify`, `modify-carve`, `create`, or `read`."* Verified true at this seat's reading of the column — and it is a negative universal over an enumerable set in the file's own voice, minted this round **inside the receipt whose whole purpose is to prove the law was applied by scope rather than by instance**.

### 4.3 Born-state ledger, re-checked

| declared | re-checked at `81a56990` this seat |
|---|---|
| G-XSS born-RED, GREEN reachable after the R3-3 re-cut | ✓ **RED and REACHABLE** — 2 writes; property form 1 → 0 after the cure |
| G-TAX born-RED, re-cut as INBOUND-DEPENDENCY | ✓ **RED (3/0/0) and now REACHABLE** — relay leg in-grant; annotation leg dependency-declared. **Pass-4 D-4 CLOSED** |
| G-PRM-FLIP born-RED, receipts re-anchored | ✓ RED; `strategies.ts:66-75`/`:76` and `frame.ts:121`/`:137` exact; 7 lines / 3 files |
| G-DEPCRUISE L-1 RED at the ref of record | ✓ four dead paths, unmoved by 41 commits |
| G-DEPCRUISE L-2 CURED-AT-FRONTIER; three-clause oracle | ✓ denial `:125-129`; 0 / bare `depcruise src` / file absent |
| G-SHIM declared regression floor, cure `7e9ddf49` | ✓ honest; three files gone, docblock true |
| G-STRUCT / G-RING / G-BASIS MEASURE-AT-OPEN with probes | ✓ 153 · 8 · 12 all reproduce; rings honestly unmeasured |
| G-CSSIDENT | ✓ RED witness exact (0/0/0 across the three published files) — but **GREEN names two symbols the wave's own ruling forbids publishing, D-4** |
| the other twelve arm-B/C gates | ✓ every witness re-resolves at the frontier |

**AXIS 4 — FAIL** (D-4/D-5/D-6/D-7 MAJOR; D-8..D-13 MINOR). **No BLOCKER**: no surviving oracle can be driven GREEN by deleting something true, and pass-4's unpassable oracle is closed.

---

## §5 · Axis 5 — POSTURE

| item | finding |
|---|---|
| **W4 head** | ✓ `→ KF.W4` declared **BLOCKING for the re-homed rows, not for this wave's bounds**; KF-AT-12 stated as PREMISE not cure; all **three** riders named (`noUnusedLocals` · depcruise-over-demo · `--project demo`/`test:demo`) and **reciprocated at both ends at final bytes** — `KF-W4.md:165` (row 3, ME-32 ≡ LP-7 rider, booking kf-EditorShell L-6/C-18) and **`KF-W4.md:211` G-KFW4-2** (`npx vitest run --project demo` **and** `npm run demo:correctness`, both blocking merge steps). OP-6's frontier restatement is exact at `package.json:37/:38/:44/:50`, leg 3 `proof:structure` naming the script G-BASIS's fifth rival is owed to (`scripts/gates/structure/index.mjs` present) |
| **W3 gated** | ✓ `→ KF.W2/KF.W3 · Parser Consumption (GATED)` — *"This wave must NOT pre-empt the 4.0.0→4.1.x repin"*; `compile/emit/css-text.ts` reserved, and B-11's addendum correctly derives that **publication is a re-export in `public.ts`/`index.ts` only**, never an edit inside the reserved file. B-16's OP-4 both-dispositions pre-declaration keeps the row alive under either resolution |
| **KF-AV-28 · O-20 · O-21** | ✓ all three correctly **absent** — `grep -c` → **0 / 0 / 0**. The AV-28 rider governs surfaces this wave's §Bounds does not carry; the mint law is W1's, and O-21 is live at KF-W10 (6 hits) where it belongs |
| **W11 / W12 / W13 per RULINGS-4 R4-8** | ✓ **present and coherent at the receiving end.** `KF-W10.md` §6.D · SUCCESSOR-FORMATION REGISTER exists with the three rows and the ruled cargo — **KF.W11 ×9 · KF.W12 ×6 · KF.W13 ×2** — the minting citations by §-heading anchor, and the boundary line at `:527`. This file's own W11/W13 sentence (§Sequencing, the square-packet edge) is the **round-2 dated correction** that struck *"SS-1/SS-2 must mint W11 and W13"* and homed the minting at KF-W4 §Sequencing per R-15 — **consistent with R4-8's ruling (a)**, and this wave neither mints nor owns either packet |
| **W10's greens conditioned on artifacts that exist** | ✓ OP-5 / G-2's right-hand union is *"the union of AUTHORED X·KF wave bounds ∪ §6.D's cargo enumeration"*, and §6.D **exists** (`:543`–`:560`). The one W5-origin obligation is verified fulfilled from that end: **KF-W8-R-4-STRUCT-PAIR ⟨origin KF.W5 D-6⟩** at `:98`/`:213`/`:320`/`:504`/`:606`, terminal verb **`DECLINED-FOR-X·KF, carried forward`**, RECORD-ONLY, with the **three re-open triggers carried verbatim** (`:232`). This wave's *"nothing here may be re-booked at W10"* and *"this wave's counting-basis row must not absorb them"* both hold; W10's three named subjects (KF-AT-21 `:89`, the `345/12/57 @ 8281638c` citation law `:411`/`:423`, the slot-number falsifier) are all W10's |
| **DH-3 four-party carriage** | ✓ in its **count** (W4 2 + W7 4 + W8 4 = **TEN**, re-derived at all three siblings' final bytes; the round-4 *eleven → TEN* correction is right, and W7:79's four-party *"eleven"* is used exactly as this file attributes it) — **defective in its statement of W8's access, D-1** |
| **program-level wrinkle, recorded not charged to this wave** | `KF-W10.md` was written **after** `PASS-4/CLOSE-CERT.md` (D-8). Every W10 receipt in this file is anchor-only, so **this wave's posture is unaffected**; the pass-5 union seat inherits it |

**AXIS 5 — PASS.**

---

## §6 · Register (worst first)

| # | sev | claim | receipt |
|---|---|---|---|
| **D-1** | MAJOR | §Disjointness states KF.W8's access as **"modify-carve over the nine tracked files"** — a grant KF.W8 **STRUCK this same round** — and derives *"the one genuine WRITE overlap"* from it | `sed -n '93p' waves/KF-W8.md` → *"\| … the 9 tracked files … \| **READ-ONLY** (the nine) — ***access NARROWED at repair round 4*** \| … The round-3 grant of **modify-carve over all nine** is **STRUCK** … names exactly **ONE** cure site, `resize-tracks.test.ts`"* (its own row, `KF-W8.md:94`). W5 `:277`/`:279`. A stage-1 → stage-2 forward reference carrying substantive prose, which LAW C(3) reduces to anchor-only for exactly this reason |
| **D-2** | MAJOR | the receiving-end control cell **`KAD-1 12 / 16`** is stale — at KF-W6's FINAL bytes it is **`12 / 20`**; the block hands all fifteen cells to the RECONCILE seat by name and the seat re-measured **one** | `grep -o 'KAD-1' waves/KF-W6.md \| wc -l` → **20** (printed 16); CARRY side **12** ✓. W5 `:455` (*"the RECONCILE seat re-verifies every cell after the last per-wave write (LAW C(4))"*) vs `PASS-4/CLOSE-CERT.md` §R-11, which re-measures **KF-SS-31 only**. The other fourteen cells reproduce exactly |
| **D-3** | MAJOR | Act 1's view-transition row: **the pasted command does not produce the pasted output**, inside this round's own cure for a count defect in that same row | printed: *"`git grep -nF 'orchestration/view-transition' origin/master -- src test` → the five above"*. Measured: **6 lines**, and **neither `orchestration/index.ts:52` nor `:57` is among them** — both spell `from "./view-transition"` (verified at the bytes). The barrel figure **5** survives by resolution; the command beside it does not produce it |
| **D-4** | MAJOR | **G-CSSIDENT's round-3-promised re-cut never landed**: GREEN still demands *"the C-8 four"* and RED still lists `debounce`/`convertPixelsToCh`, which this wave's own Act 4 / B-11 rule are **not library symbols** and must not be published | `:164` promises *"G-CSSIDENT's five-name assertion is **re-cut accordingly at its own cell**"*; `sed -n '394p' KF-W5.md` still reads *"(and the C-8 four, or their declared relocations, …) resolve from `loadAnimationEngine()` or the exports map"* and *"Same greps clear `reverseCSSTime`, `serializeTimingFunction`, `debounce`, `convertPixelsToCh`"*. `git grep -c 'debounce' 81a56990 -- src` → **0**; same for `convertPixelsToCh`. The pass-4 D-4 shape, one gate over |
| **D-5** | MAJOR | the SUBJECT-IDENTITY sweep prints **"34 symbol × file pairs"** with no enumeration and no probe, and its zero-set omits **nine** symbol × file zeros its own §Bounds row names — while the scope receipt asserts **"0 outstanding"** | G-BASIS's own falsifier: *"Fails on … any figure quoted without its probe."* Measured at `81a56990`: across `index.ts` / `public.ts` / `load-engine.ts`, `cssIdent` **0/0/0** · `reverseCSSTime` **0/0/0** · `serializeTimingFunction` **0/0/0** — lawful negative witnesses, none declared, where `withReducedMotion`'s identical zero **was** declared *"so no later sweep re-files it"* |
| **D-6** | MAJOR | the ⟨RECONCILE SEAT⟩ block **inside this file** certifies *"every anchor resolves"* / *"NOT ONE ANCHOR IN THIS FILE FAILED"* over a sweep of four receipts, leaving the fifteen-cell block and the four-party access table unswept — both of which contain a receipt false at final bytes | `KF-W5.md:499`; `PASS-4/CLOSE-CERT.md` §3 (*"KF-W5 … 6 … anchors all hold … fixed 1"*) and §9 item 2 (*"EVERY same-round cross-wave receipt … 47 of 47"*). Falsified by D-1 and D-2 |
| **D-7** | MAJOR | a **self-voiced closure claim minted this round** — *"its gates can be turned by the seats they bind, and its cross-wave receipts survive a sibling's next keystroke"* — refuted by this pass | `:503`. Third clause refuted by D-1/D-2; second by D-4. The LAW-B item-1 shape converted at D-13 one round earlier, re-minted in the paragraph certifying the conversion. Fifth generation |
| **D-8** | MINOR | `PASS-4/CLOSE-CERT.md`'s *"proof this seat wrote last"* is **false at the tree** — `KF-W10.md` was written after the certification | CLOSE-CERT §1 tables `KF-W10.md` **17:31:54 / 209,902 B**; `stat` measures **17:38:21 / 209,923 B**, i.e. **22 s after CLOSE-CERT.md itself (17:37:59)**. This file's §END makes CLOSE-CERT the certifying end; its own W10 receipts are anchor-only and unaffected |
| **D-9** | MINOR | CLOSE-CERT §7's *"Live line-numbered cross-cites: ZERO"* is **false of this file** | its sweep regex `KF-W[0-9]+\.md:[0-9]+` cannot match the CARRY filename. `grep -o 'KF-W6-CARRY\.md:[0-9]*' KF-W5.md` → **6 live cites** (`:9` ×1 · `:170` ×3 · `:248` · `:319`), none carrying a strike marker. **All six resolve** at the frozen CARRY (10:49:45) |
| **D-10** | MINOR | Act 6's two pasted commands are **short transcripts** | `git grep -n 'return {' … useHighlightCSS.ts` → **3** lines (`:20` · `:63` · `:147`), one printed; `git grep -n 'useCodeHighlight' -- demo/ test/` → **5** lines (incl. the declaration `useHighlightCSS.ts:78`), four printed. Both conclusions hold. The class `CLOSE-CERT` §8 names as firing at three seats in the round that named it — a fourth, in a census added this round |
| **D-11** | MINOR | three **stale substrate stamps**, and they are the mechanism behind D-1/D-2 | stamped 15:49 / 15:50 / 15:39 for `KF-W4.md` / `KF-W6.md` / `KF-W7.md`; finals **16:59:44 / 16:59:25 / 17:33:23**. LAW C(1)'s stage-1 *"any order"* let this wave write **first of eleven** (16:55:55, ahead of KF-W0's 16:58:59). Outputs verified and holding **except D-2** |
| **D-12** | MINOR | the `withReducedMotion` cell prints **10** beside a command that returns **11** | `git grep -l 'withReducedMotion' origin/master -- src/` → **11 files**, `internal/reduced-motion.ts` among them. *"10 **other**"* is derivable; the numeral beside the paste is not the paste's output (LAW D(3)) |
| **D-13** | MINOR | the LAW-A access-column scope receipt closes with a **spec-voiced negative universal** | `:186` — *"**No other §Bounds row carries a delete, repoint, move or shim verb** — every remaining row is `modify`, `modify-carve`, `create`, or `read`."* True at this seat's reading; the banned LAW-B shape, minted inside the receipt that exists to prove the law was applied by scope |

**routedTotal 43 · bookedCount 43 · escapedCount 0 · verdictLocal DEFECTIVE.**

---

## §7 · What this pass does not dispute

The by-record census is whole and honest in both directions for the third pass running, and this pass retired the token partition entirely — sweeping all 36 zero-token records for alternate routing spellings — without moving it by one id. **Pass-4's two MAJOR gate defects are genuinely closed**: G-TAX went from an oracle no seat inside the bounds could turn to an in-grant relay plus a declared inbound dependency, with a falsifier that names the failure the old gate invited; and the SUBJECT-IDENTITY law, ruled at round 3 and pointed at a sibling, was finally run over this wave's own §Bounds, curing three live mis-anchorings (`snapToReducedMotion` · `setTimingFunction` · `steps`), pathing a `modify` row that had no path at all, and — the best single act of the round — **declaring the fourth zero as a negative witness rather than absorbing it.** The two false universals inside the LAW-A block are struck on their own measurements, the `cssIdent` chain is stated as the two-site chain it is at all three of its sites, and the divergence from RULINGS-4's ordered `@src/` figure is recorded rather than smoothed — the honest form, and correct at the bytes.

**Every keyframes-side measurement in this file re-executes exactly at the frontier.** B0 153 · `.d.ts` 0 · god modules 8 with the identical list · stutters 12 with the identical list · 41 behind / 252 dirty · depcruise 253 L / 24 entries / every rule anchor / 0 and 3 · the `cssIdent` chain · the `innerHTML` pair · PRM 7-lines-3-files · the three `setTargets` declarations · 9 hits / 8 files decomposing to 7 specifiers / 6 files · G-TAX 3/0/0 · the nine tracked files · zero create-name collisions · `139 − 2 + 8 = 145`. Every dissent, cure-lock, rider and fold identity survives.

**The pattern, stated once.** Four rounds have each cured an enumerated set perfectly and left the class open one level up — pass 2 the arm-D substrate, pass 3 the arm-0 oracle, pass 4 the anchor-content and false-universal classes, and **pass 5 the certification itself**. Round 4 built LAW C to kill same-round sibling drift, wrote a RECONCILE seat to certify it, and produced a certification that swept four of this file's receipts, missed the sixteen the file handed it by name, and stamped *"NOT ONE ANCHOR IN THIS FILE FAILED"* into the file's own bytes — two lines above a sentence claiming its receipts *"survive a sibling's next keystroke."* Two of them did not. **The instrument was not wrong; it was not run over the surface that asked for it** — which is, verbatim, the lesson R4-10 wrote down when it fixed the enumeration source as the SCOPE and not the instance.
