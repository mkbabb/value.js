# KF.W7 — FRESH ADVERSARIAL SPEC CHECK (L-18/L-20, pass 5)

**Subject**: `/Users/mkbabb/Programming/value.js/docs/tranches/X/keyframes/waves/KF-W7.md` (419 L, 210,113 B, mtime **2026-08-28T17:33:23**), four repair rounds deep.
**Seat**: FRESH. Nothing is inherited from `PASS-4/KF-W7-CHECK.md`, `PASS-4/RULINGS-4.md` or `PASS-4/CLOSE-CERT.md` — those are read as *claims to be tested*, never as findings. The census below is re-derived line-by-line from the 58 `kf-*.md` records' own bytes at `docs/tranches/V/megatranche/registry/adjudicated/`. Sole X·KF carry consulted: `carry/KF-W6-CARRY.md`.
**Tree law**: every keyframes witness re-executed READ-ONLY at `origin/master 81a56990736ced5b5edde0b84c527680ac7689b1` (`git -C /Users/mkbabb/Programming/keyframes.js rev-parse origin/master`, re-verified this seat). Local `8281638c` disqualified; the dirty worktree never read as substrate.
**Sole write**: this file. No product source opened; no gate stamped; no registry byte touched; no PASS-1..PASS-4 artifact edited; no spec repaired.
**Verdict**: **DEFECTIVE** — carriage is **CLEAN by bytes (0 escaped under every admissible reading)** and the frontier/LAW-A axes are exemplary, but **12 defects** stand, headed by **a same-round cross-wave receipt falsified at KF-W8's final round-4 bytes** (the U4-C class the round existed to kill, surviving both W7's own LAW-C disclosure and the RECONCILE seat) and **a census figure that still does not re-derive from the rule the spec writes out — the sixth consecutive generation of that class**.

---

## §0 The five axes — verdicts

| # | axis | verdict | one-line basis |
|---|---|---|---|
| **1** | **ID-KEYED CENSUS by record** | **AMBER** | **135 routed · 135 booked · 0 escaped** by this seat's independent re-enumeration. Every one of the 127 roster identities resolves to a carried row; every non-roster identity to a precondition, a settlement, or an §Excluded bullet with a named owner. **Carriage is CLEAN.** The AMBER is the *denominator*: the cited **136** contains one double-counted line and one line rule (1) excludes (**D3**, **D8**). |
| **2** | **RECEIPT REALITY / ANCHOR STABILITY** | **RED** | **The drift class is NOT dead.** W7 asserts KF.W8 holds `test/demo/instrument/**` (all nine tracked files) `modify-carve`; W8's round-4 write **STRUCK** that grant (**D1**). The RECONCILE certification's "wrote last" proof is falsified at the bytes — `KF-W10.md` moved *after* `CLOSE-CERT.md` (**D2**). One pasted grep prints 2 of its 4 lines (**D4**), one prints 1 of 2 (**D9**). Offsetting: W2/W4/W5/W6/W10 anchors **all resolve at current bytes**, and the W6 §Excluded ends hold by banked row header for a fourth round while their numbers moved again. |
| **3** | **M-25 depth + LAW-A censuses** | **AMBER** | **LAW A is exemplary** — both censuses re-executed by this seat at `81a56990` reproduce **COMMAND-FOR-COMMAND** (§2.C). D2's rung restoration (KF-KC-52 → INFO) verifies at the bank. But the D4 dissent cure's own "byte-exact" quotation is a **truncation without ellipsis** that drops a fifth preserved dissent from the item it quotes (**D5**), and §1c's fold enumeration — the artifact the rule points at — is short by one line (**D7**). |
| **4** | **GATES born-RED / REACHABLE GREEN · in-grant · LAW B** | **AMBER** | 15 gates, **all born-RED, none stamped, all with reachable green**; every witness this seat re-ran reproduces EXACTLY (§2.B); G6's four leaves are now path-qualified and all four verify; G15's out-of-bounds fold is honestly a *ruling* gate. But the round-4 ledger asserts a **universal closure claim in the spec's own voice** — *"No figure in the cells this round touched was carried over unre-run"* — and **D4 falsifies it** (**D6**). |
| **5** | **POSTURE** | **AMBER** | W4 head ✓ (`KF-W4.md:54`/`:58` + §Commits commit 2 at `:321`, W4 lands first) · W3 gated ✓ (`# KF.W3 — Parser Consumption (GATED, never scheduled)`; its `package.json (:70 only)` row live at `:77`) · **O-20/O-21 → 0 hits, correctly not this wave's surface** ✓ · W11/W12/W13 per RULINGS-4 R4-8 present and resolving (§6.D at `KF-W10.md:543`, KF.W12 row `:559`) ✓ · W10's greens rest on artifacts that **exist** (alphabet `:383` byte-identical, §3.2 rider `:250`) ✓. But the D9 by-scope cure stopped two edges short (**D10**), and the LAW-C disclosure mislabels the vehicle of **D1** as an anchor (**D12**). |

---

## §1 Census — id-keyed, re-derived at the registry bytes

**Counting rule, restated so a later seat re-runs it rather than inherits it.** Identical in substance to the rule the spec now writes out, with the two seams that produced the delta made explicit:

1. **RECORD-SET** — the 58 `kf-*.md` adjudicated records. Nothing else is a source.
2. **MEMBERSHIP — the OWNER test, governing**: a row is ROUTED iff **KF.W7 is the owner named in its own terminal disposition**, bare or in a `KF.Wn/W7` dual-home. A `KF.W7` appearing only as a cross-ref, an evaluation-input pointer, or a rider-governance clause on a `NO-WAVE-OWNER`/other-wave row is **not** a routing.
3. **CONTEXT IS NEVER A ROUTING** — routing-law preambles, method paragraphs, closing tallies and prose mentions. *(This is rule (1)'s own sentence; the spec's cited artifact breaks it once — D3.)*
4. **GRANULARITY** — a banked MERGED multi-id header counts ONCE at its banked identity.
5. **IDENTITY DEDUPE (M-25)** — a FOLDS/identity-guard line discharging to an already-counted banked id is fold-carried, not a new routing. **Corollary this seat states because the cited artifact needed it: a clause riding INSIDE an already-counted row is not a second identity.**
6. **DUAL-HOMES count once**, carried here if the mechanism is this wave's, excluded-with-named-owner if not.
7. **`KF.W7-TOKENS`** (the retired lane-frontend §10 namespace) is OUT by mechanism; **`KF.W6-TIMELINE`** dispositions are IN as named preconditions.

### §1a · Roster-owned routings — enumerated by line, verified this seat

| record | routed | lines (this seat's enumeration) | carried | excluded-with-owner | escaped |
|---|--:|---|--:|--:|--:|
| `kf-KeyframeTimeline.md` | **40** | `:42 :43 :44 :45 :46 :47 :48 :49 :55 :56 :57 :59 :60 :61 :62 :63 :64 :65 :66 :71 :72 :73 :74 :75 :76 :79 :80 :81 :82 :84 :86 :87 :91 :95 :96 :97 :98 :99 :100 :102` | 40 | 0 | **0** |
| `kf-TimelineTrack.md` | **30** | `:40 :41 :42 :43 :45 :46 :51 :52 :53 :54 :55 :56 :57 :59 :60 :61 :62 :63 :64 :65 :66 :67 :68 :73 :74 :75 :77 :78 :79 :80` | 30 | 0 | **0** |
| `kf-TimelineHoverPreview.md` | **17** | `:42 :43 :49 :50 :51 :52 :54 :57 :58 :59 :61 :62 :63 :69 :70 :71 :73` (`:61` = MISSED-3, `KF.W6/W7` dual-home) | 17 | 0 | **0** |
| `kf-TimelineCaret.md` | **14** | `:40 :41 :50 :51 :52 :53 :54 :55 :60 :62 :66 :67 :68 :70` | 14 | 0 | **0** |
| `kf-CSSPasteDialog.md` | **8** | `:41 :42 :45 :59 :61 :63 :64 :65` | 8 | 0 | **0** |
| `kf-KeyframesAddDialog.md` | **8** | `:50 :53 :54 :57 :68 :69 :73 :74` (`:68/:69/:73` = `KF.W6/W7` dual-homes; `:63` KAD-F3 fold-deduped to R-7) | 5 | 3 (KAD-15 · KAD-16 · KAD-20 → **KF.W6**) | **0** |
| `kf-AnimationVisualizer.md` | **2** | `:51` KF-AV-10 · `:72` KF-AV-28 ⟨`:54`/`:60` owner `NO-WAVE-OWNER`, `:68` owner `KF.W8` — rider-governed non-routings⟩ | 2 | 0 | **0** |
| `kf-KeyframeCard.md` | **2** | `:53` KF-KC-11 · `:100` KF-KC-52 | 2 | 0 | **0** |
| `kf-SpringTarget.md` | **2** | `:64` C-4 · `:69` m-8 | 2 | 0 | **0** |
| `kf-CSSCodeEditor.md` | **1** | `:34` KF-CE-2 (parent half, BLOCKER) | 1 | 0 | **0** |
| `kf-RibbonBar.md` | **1** | `:77` RB-7 | 1 | 0 | **0** |
| `kf-SequenceScrubber.md` | **1** | `:58` C·C-4 | 1 | 0 | **0** |
| `kf-SpringTrace.md` | **1** | `:62` C-3 ⟨`:48` C-2 = L-3 is `NO-WAVE-OWNER` — re-verified this seat; its `→ KF.W7` belongs to the sibling C-3⟩ | 1 | 0 | **0** |
| **subtotal** | **127** | | **124** | **3** | **0** |

**Every one of the 127 was matched to a carried row in `KF-W7.md` by banked id.** Spot-verification of the rows most likely to be lost — TT `RR-A missed-4` (folded at P7 MISSED-1's description arm), TT `D-13 net arm` (folded at P7 `D-3/L-D6/C-6`), TT `m-3`/`i-3` (P9 INFO group), KT `C-17`/`N-12`/`C-13` (P9 INFO group), KAD-21 (P8 dialog MINOR group), THP `P2-DEAD` (P7 hygiene group) — **all present**.

### §1b · Non-roster identities

| coordinate | identity | disposition in `KF-W7.md` | counted |
|---|---|---|:--:|
| `kf-AnimationControlsGroup.md:42` | **D-1** — the document-singleton portal BLOCKER (`KF.W6-TIMELINE` precondition) | **OP-0** ✓ | ✔ |
| `kf-ChannelControls.md:40` | **L-2/C-2** — the `:key` remount BLOCKER | **OP-1** ✓ | ✔ |
| `kf-ChannelControls.md:52` | **C-8/D-6** — the empty timeline tabpanel | §Sequencing **item 8** ✓ | ✔ |
| `kf-RibbonBar.md:131` | routing-summary tail carrying the *expanded-timeline settlement* obligation | §Sequencing 8 + P5 RB-7 ✓ | ✔ |
| `kf-SequenceScrubber.md:134` | routing-summary tail carrying **D-5/D-6/D-11**'s verdict-gated arms | §Sequencing → KF.W11 arm (d) + P0 C·C-4 ✓ | ✔ |
| `kf-CSSPasteDialog.md:115` | residue **#9** — L-4's open falsifier, *"owned by the R-7 fold in KF.W7"* | **G15** assertion + P8 R-7 ✓ | ✔ |
| `kf-PlaybackRibbon.md:157` | the NO-WAVE-OWNER transport/ribbon packet the KF-AV-28 rider governs | §Sequencing packet homing → **KF.W13** ✓ | ✔ |
| `kf-PlaybackRibbon.md:36` | **PR-CAUTION** — the counter-evidence clause | header lock, quoted **byte-identical** (§2.D) ✓ | **✘ — rule (3): this line is the record's *Routing law* preamble.** Carried either way; see **D3** |
| `kf-SpringTarget.md:69` | *"the kf-EasingTarget P-2 cure caution riding m-8"* | P0 KF-AV-10 cure-caution lock ✓ | **✘ — this IS §1a's m-8 line.** Double count; see **D3** |
| **subtotal counted** | | | **8** |

**Explicit ZERO declarations, recorded and NOT counted** (re-verified this seat): `kf-SequenceScene.md:153` — *"**KF.W7**: none (no export-surface rows in this corpus)"*; `kf-SequenceAxis.md:130` — *"**KF.W6/W7**: no axis-owned rows"*.

**Routing-summary tails NOT counted, and why** (the discriminator the cited artifact never states — see **D8**): `kf-SpringTrace.md:139` · `kf-SpringTarget.md:146` · `kf-SequencePlayhead.md:151` each restate an identity already counted at §1a or §1c and add no obligation.

### §1c · Fold-carried lines resolved by identity — **17**, not 16

`kf-KeyframeTimeline:106` (D-2/L-2/C-2 ≡ **R-3**) · `kf-KeyframeTimeline:114` (Cross-refs → counted once at §1b as OP-1) · `kf-TimelineTrack:86` (C-2 ≡ **C-1**) · `kf-TimelineCaret:74 :75 :77 :78` · `kf-TimelineHoverPreview:77 :80 :81 :82 :84 :86 :88` · **`kf-SequencePlayhead:92`** · `kf-SequencePlayhead:143` · `kf-SequenceScrubber:128` (≡ KF-AV-28) · `kf-SpringTarget:135` (m-8 ≡ KF-AV-10). **17 lines → 0 new routings.** `kf-SequencePlayhead:92` — *"Folded-by-identity (carried, not rows): (1) the **C-§4 export-surface correction** ≡ banked kf-KeyframeTimeline C-15 (→ KF.W7)"* — is the line the cited enumeration omits (**D7**). Its target C-15 is carried at P0, so nothing escapes.

### §1d · Totals

| | this seat | the spec's cited figure |
|---|---|---|
| **routed** | **135** (127 roster + 8 non-roster) | 136 |
| **booked** (124 carried + 3 excluded-with-named-owner + 8 non-roster) | **135** | 136 |
| **escaped by bytes** | **0** | 0 |

*Under a strict reading of rule (3) that also drops `kf-PlaybackRibbon.md:36`, the figure is **134**. Under the cited artifact's arithmetic it is **136**. **Escaped is 0 under all three.** The delta is a denominator, never a drop — which is exactly what the D1 cure claimed to have ended, and did not.*

---

## §2 Witness re-execution — every command run by THIS seat at `origin/master 81a56990`

`git -C /Users/mkbabb/Programming/keyframes.js rev-parse origin/master` → `81a56990736ced5b5edde0b84c527680ac7689b1` ✓ · `HEAD` → `8281638c` (disqualified; worktree dirty, never read).

### §2.A · §Bounds line counts — **11 of 11 sampled EXACT**

`KeyframeTimeline.vue` **312** · `components/TimelineTrack.vue` **246** · `TimelineCaret.vue` **70** · `components/TimelineHoverPreview.vue` **38** · `utils/timelineEngine.ts` **102** · `utils/flattenVars.ts` **33** · `CSSPasteDialog.vue` **80** · `keyframes/components/KeyframesAddDialog.vue` **161** · `scenes/sequence/SequenceScrubber.vue` **162** · `playback/AnimationVisualizer.vue` **256** · `demo/utils/keyframeSelector.ts` **31**. The `timelineEngine` 102-at-origin correction holds.

### §2.B · Gate witnesses — **9 of 9 sampled reproduce, most to the line**

| gate | command | result |
|---|---|---|
| **G1** | `wc -l …/glass-ui/dist/components/timeline/index.d.ts` · `ls` | **2 lines**; **nine** files, the seven un-re-exported exactly as enumerated ✓ |
| **G2** | `git show origin/master:…/KeyframeTimeline.vue \| grep -n scrub` | **78 · 83 · 197 · 202 · 224**; bare `scrub` among NONE (`:197 scrubT`, `:202 scrubAndCapture`) ✓ |
| **G5** | `timelineEngine.ts \| nl -ba` | merge loop **`:37-47`**, key **`:38`**, merge comment **`:40`**, property loop **`:42`**, build **`:49-51`** — **EXACT, all five** ✓ |
| **G6** | four leaves | `importCSSToTimeline` decl `timelineEngine.ts:76`, import `useTimelineBuild.ts:12`, await `:146` · `easing?` `timelineTypes.ts:10` · `captureSnapshot` decl `snapshotCapture.ts:8`, call `useTimelineOps.ts:29`, filter `:16-21` — **all four path-qualifications verify** ✓ |
| **G8** | `git grep -n onMarkerKeydown origin/master -- demo/` | `TimelineTrack.vue:82` (binding) + **`:203`** (decl) ✓ |
| **G9** | `TimelineTrack.vue:86` | `<TooltipContent side="top" :side-offset="8" class="p-2 max-w-56">` — `ariaLabel` unpassed ✓ |
| **G10** | `previewCache` census · `grep -c delete` | `:81` passed · `:217` decl · `:221` read · `:226` write; `delete` → **0** ✓ |
| **G11** | leg (i) over `test/` · leg (ii) over `demo/` | **0 hits** / **13 hits** ✓ |
| **G13 · G14** | `useZoomPan.ts \| grep -n delta` · `useTimelineBuild.ts \| grep -n 'toast\|console.error'` | `:50` + `:59` only, `deltaX` nowhere ✓ · `console.error` **`:48`**, toasts **`:121 :133 :137 :148 :155 :157`** — EXACT ✓ |

`OP-4` re-measured: `git show origin/master:package.json \| sed -n '81p'` → `"@vitejs/plugin-vue": "^6.0.7",` ✓; `grep -c 'test-utils'` → **0** ✓.

### §2.C · LAW A — both censuses re-executed independently, **COMMAND-FOR-COMMAND**

**(1) `flattenVars.ts`**: `git grep -nF '../utils/flattenVars' origin/master -- demo/ test/ src/ scripts/` → **1** (`useTimelineBuild.ts:14`). `git grep -n '\bflattenVars\b' …` → **4** — `useTimelineBuild.ts:14` (import) · `:170` (call) · `flattenVars.ts:10` (decl) · `:26` (self-recursion). **CONSUMER SET = exactly one module** ✓.
**(2) `KeyframesAddDialog.vue`**: `git grep -nF 'components/KeyframesAddDialog' origin/master -- demo/ test/ src/ scripts/` → **exactly one live import**, `KeyframesEditor.vue:118`; symbol census → mount `KeyframesEditor.vue:75` + one docblock (`useToolbarKeyboard.ts:24`). **CONSUMER SET = one module, and it is outside §Bounds** ✓.

**LAW A remains the strongest part of this spec.** Neither census is inherited; both reproduce exactly; the `loadPreset` re-export consequence and the out-of-bounds repoint consequence are both real and both correctly binding.

### §2.D · Registry- and sibling-side receipts

- **Taxonomy pair, both re-run**: `grep -rn 'KF.W6-TIMELINE' adjudicated/*.md` → **8 hits / 4 records** ✓; `grep -rniE 'disposition:.*KF\.W6-TIMELINE' …` → **exactly three rows in two records** (`kf-AnimationControlsGroup:42` · `kf-ChannelControls:40` · `kf-ChannelControls:52`) ✓. The D7 headline now follows its receipt.
- **LAW D quotations, byte-diffed this seat**: `kf-EasingTarget.md:109` (P-2, whole) ✓ · `kf-SpringTarget.md:69` (the gloss, labelled as the gloss) ✓ · `kf-TimelineCaret.md:74` (MISS-β3 **with the scoping parenthetical restored**) ✓ · `kf-PlaybackRibbon.md:36` (PR-CAUTION) ✓. **The D3 and D6 cures are real.**
- **D2 cure**: `grep -n '^### ' kf-KeyframeCard.md` → `:38 BLOCKER · :47 MAJOR · :71 MINOR · :95 INFO`; `KF-KC-52` at **`:100`** ✓ — the spec now carries INFO. **The silent promotion is reversed.**
- **D8 cure**: `grep -c 'kill register' kf-KeyframeTimeline.md` → **0**; `grep -n 'Killed-claims register' …` → **`:116`**; `K-8 · L-15` → **`:125`** ✓.
- **D5 cure**: `KF-W2.md` `:710` / `:753` reproduce verbatim at current bytes ✓; hard-ordering **item 9** present and its falsifier is stated in both directions.
- **KF.W6 §Excluded, both ends**: `S-10 · KF-KE-21 (+KAD-15)` now `KF-W6.md:360` (was `:299`) and `KF-W6-CARRY.md:178`; the KAD group row now `KF-W6.md:438` (was `:370`) and `KF-W6-CARRY.md:248`. **The numbers moved AGAIN; the banked row headers held.** Fourth round running — the stable-anchor idiom earning its keep.
- **KF.W5**: `grep -c` for each of the four fixtures → **2 each** ✓ — the reciprocal naming is real.
- **KF.W10 forward anchors, all four, at W10's CURRENT bytes**: §6.D `:543` ✓ · KF.W12 row `:559` ✓ · §G-2 Acceptance alphabet `:383` **byte-identical** ✓ · §3.2 `KF-AV-28 · STANDING SUPERSESSION RIDER` `:250` ✓. `grep -c 'KF.W10' KF-W7.md` → **7**.
- **KF.W4**: `:54` (`package.json` row) and `:58` (`vitest.config.ts` row) live ✓; §Commits commit 2 at `:321` carries the extended parenthetical the round-4 requote discloses ✓.

---

## §3 Defects — 12 (6 MAJOR · 6 MINOR), ranked

### **D1 · MAJOR — a same-round cross-wave receipt FALSIFIED at KF.W8's final round-4 bytes. The drift class is not dead; it survived W7's own LAW-C disclosure AND the RECONCILE seat.**
`KF-W7.md:77` (§Bounds four-party table) books **KF.W8**'s footprint as *"`test/demo/instrument/**` (all nine tracked files) `modify-carve` · `resize-tracks.test.ts` `modify-carve` … ⟨`KF-W8.md` §Bounds⟩"*; `:79` builds an invariant on it (*"`KF-W8.md` §Bounds asserts `modify-carve` over exactly the nine files this wave reads as witnesses"*); `:352` repeats it (*"W8 carves all nine tracked files + `resize-tracks.test.ts` and creates four"*).
`KF-W8.md:93`, at its **final** round-4 bytes (mtime 17:34:06, 41 s after W7's write), reads: *"| `test/demo/instrument/` — **the 9 tracked files, ENUMERATED BY NAME, never by shared-prefix glob** … | **READ-ONLY** (the nine) — ***access NARROWED at repair round 4*** | … **The round-3 grant of modify-carve over all nine is STRUCK**"* (RULINGS-4 §END KF-W8 D-8). **W8's only write in that directory is `resize-tracks.test.ts` (`:94`) plus its four creates.**
Three harms, compounding: (a) the premise of W7's outward invariant — *"no KF.W7 gate may be read against `timeline-undo.test.ts` … at bytes a neighbour has since changed"* — is dead for eight of the nine files; (b) W7 attributes to W8 a `**` **shared-prefix glob** on a row that explicitly forswears one, re-importing the exact idiom R2-12 struck from W7's own §Bounds; (c) W7's four-party disjointness assertion is now computed against a footprint W8 does not hold. **Nothing escapes** — the four W7 creates still collide with nothing — but the receipt is false, and it is the load-bearing kind.
**Receipt**: `KF-W7.md:77`, `:79`, `:352` vs `KF-W8.md:93` (+ `:94`), re-read by this seat at both files' current bytes.

### **D2 · MAJOR — the RECONCILE certification's "proof this seat wrote last" is falsified at the bytes, and the ⟨RECONCILE SEAT⟩ receipt carried INSIDE this spec names a substrate that is not W10's final bytes.**
RULINGS-4 §END directs the pass-5 seat to audit `CLOSE-CERT.md` against the bytes **as its first act**. Measured this seat:

```
CLOSE-CERT.md   2026-08-28 17:37:59   27,004 B
KF-W10.md       2026-08-28 17:38:21  209,923 B
```

`CLOSE-CERT.md` §1 tables `KF-W10.md` at **17:31:54 / 209,902 B** and §9 claim 1 certifies *"It ran AFTER the last per-wave write … nine-minute margin."* **`KF-W10.md` was written 22 seconds AFTER the certification and is 21 bytes larger than the table it is certified in.** The consequence reaches this file directly: `KF-W7.md:344` carries the reconcile seat's inserted answer to the one item W7 flagged — *"Re-run at KF-W10's post-round-4 bytes (**mtime 17:21:25**, the last per-wave write of the round)"* — and 17:21:25 is now three writes stale. **Substance holds** (this seat re-verified all four W10 anchors at the current bytes, §2.D), so the flagged alphabet is genuinely safe; what fails is the certification's own instrument, in the round that made the certification a closing condition.
**Receipt**: `stat -f '%Sm %z %N'` on both files; `PASS-4/CLOSE-CERT.md` §1 row 6 and §9 claim 1 vs `KF-W7.md:344`.

### **D3 · MAJOR — the census figure still does not re-derive from the rule the spec writes out. Sixth consecutive generation, and this time the arithmetic is inside the artifact the spec cites as authority.**
The verb table and §Excluded both cite `PASS-4/KF-W7-CHECK.md` §1 at **136 · 136 · 0** and print a rule "so the figure reproduces". Two of the cited artifact's nine non-roster entries do not survive that rule:
- **`kf-SpringTarget.md:69` is counted twice.** §1a books `kf-SpringTarget` at **2 (C-4 · m-8)** — and m-8's routing line *is* `:69`. §1b then books `:69` again as *"the kf-EasingTarget P-2 cure caution riding m-8/KF-AV-10"*. A cure-caution clause riding **inside** an already-counted row is not a second identity — rules (3)/(5), the granularity and dedupe rules the spec itself restates.
- **`kf-PlaybackRibbon.md:36` is a routing-law preamble**, the class rule (1) excludes by name (*"Routing-law preambles and prose mentions are context, never routings"*). §1b counts it.

This seat reaches **135** (or **134** under the strict rule (1) reading). **Carriage is unaffected — 0 escaped under every reading, and both identities are carried regardless.** But the D1 cure's claim was precisely that the OWNER-test reconciliation ended the drift; the delta never turned on the owner test at all. It turns on the **roster/non-roster boundary**, which rounds 1–4 never touched. The dated log now reads 148 → 150 → 138 → 136 → (135).
**Receipt**: `PASS-4/KF-W7-CHECK.md` §1a row `kf-SpringTarget.md` vs §1b rows 7 and 9; `kf-SpringTarget.md:69` (one line, both cells); `kf-PlaybackRibbon.md:36` (`Routing law: **KF.W0/W4/W6/W7/W8/W9** per CENSUS-2026-08-03 …`); `KF-W7.md:13` and `:366` (the cites).

### **D4 · MAJOR — LAW D(3) numeric-arm violation at a receipt the round-4 ledger names among those it re-ran: a grep printed at 2 of its 4 lines.**
`KF-W7.md:31` (OP-4) prints: *"re-derived by this seat AFTER that write, `grep -n 'vitejs/plugin-vue' KF-W4.md` (substrate `waves/KF-W4.md`, mtime **2026-08-28T16:59:44**) → **`:54`** (the `package.json` row) and **`:58`** (the `vitest.config.ts` row)"*.
Re-run by this seat at that file: **`grep -c 'vitejs/plugin-vue' KF-W4.md` → 4**; the hits are **`:54` `:58` `:165` `:211`**. Two of four. The conclusion (the plugin is a devDep at `package.json:81`, unregistered, registration is W4's) is **true and unaffected** — `:165` is W4's carry row 3 (`ME-32 ≡ R-10`) and `:211` its `G-KFW4-2` row, both corroborating. What fails is the transcript, and it fails at the exact site the round-4 LAW-C block advertises as its flagship re-derivation (*"`KF-W4.md:46` had MOVED … and is re-anchored"*). This is the **short-transcript** shape `CLOSE-CERT.md` §8 named as one of only two residual classes, firing again in the file that names it.
**Receipt**: `KF-W7.md:31` vs `grep -n 'vitejs/plugin-vue' /Users/mkbabb/Programming/value.js/docs/tranches/X/keyframes/waves/KF-W4.md` (4 lines, run this seat).

### **D5 · MAJOR — the D4 dissent cure's own "quoted byte-exact by command" receipt is a truncation with no ellipsis, and the elided sentence carries a FIFTH preserved dissent.**
`KF-W7.md:164` prints: *"`kf-TimelineTrack.md` §DISSENT item 1, quoted byte-exact by command — `sed -n '150p' kf-TimelineTrack.md` → **"RR-A's MAJOR rungs on D-4, M-7, D-12, and D-13's net arm** — preserved with their reopening conditions: residue rows 1, 2, and 6 are each a single live probe away from restoring the higher rung; the demotions here are burden-of-proof rulings, not mechanism refutations.""*
`sed -n '150p'` run this seat returns that text **plus a final sentence the spec drops**: *"Axis D's BLOCKER on D-1 is preserved at the banked C-1 dissent's terms (if the W7 spec rules scene-scrubbing the instrument's core promise, the whole seam inherits blocker weight as one cluster)."*
So the item preserves **five** dissents, not four, and the spec's own disclosure — *"the item names FOUR members and THREE probes"* — under-counts the roster it was cured to carry whole. **Nothing is dropped in substance** (the fifth is carried at P1's C-1 dissent and at the `D-1 (TimelineTrack)` row's *"preserved at C-1's dissent terms"*), but this is D3/D6's convicted class — a truncation under a byte-exact label, unmarked — committed by the cure written to end the *dissent*-truncation class, in the same sentence.
**Receipt**: `KF-W7.md:164` vs `sed -n '150p' kf-TimelineTrack.md` (full line, run this seat).

### **D6 · MAJOR — a universal closure claim in the spec's own voice, falsified by its own receipt. Third generation of the LAW-B-corollary class.**
`KF-W7.md:415` (the R4-10(1) scope receipts) asserts: *"**No figure in the cells this round touched was carried over unre-run**, and the sibling anchors this file exposes to KF.W8's R4-5(b) receipt (`:31` · `:61` · `:302`) were verified unmoved before close."*
That is a completeness claim over an enumerable set, asserted here rather than cited — the shape LAW B exists to forbid and the shape this file has now had struck twice (round 2's *"Nothing was inherited…"*, round 3's scoped restatement). **D4 falsifies it directly**: the `vitejs/plugin-vue` grep is one of the figures the same paragraph lists as re-run, and its pasted output is not the command's output. *(The second clause survives: `grep -n 'test-utils' KF-W7.md` → `:31 :61 :302 :348 :352 :394`, the three named anchors unmoved — verified this seat.)*
**Receipt**: `KF-W7.md:415` vs `KF-W7.md:31` and the 4-line grep of D4.

### **D7 · MINOR — the fold-dedupe enumeration the rule points at is short by one line, in the cell that promises it is "auditable, never silent".**
Rule (5) discharges FOLDS lines by pointing at the cited artifact's §1c, which enumerates **16** lines *"so the dedupe is auditable, never silent"*. This seat's sweep finds **17**: `kf-SequencePlayhead.md:92` — *"**Folded-by-identity (carried, not rows):** (1) the **C-§4 export-surface correction** ≡ banked kf-KeyframeTimeline C-15 (→ KF.W7)"* — is a third `KF.W7` fold line in that record, beside the enumerated `:143` and `:151`. **No escape** (C-15 is the wave's evaluation input, carried at P0), but an enumeration that misses a member cannot discharge the rule that cites it.
**Receipt**: `PASS-4/KF-W7-CHECK.md` §1c (16 lines) vs `grep -nE 'KF\.W7' kf-SequencePlayhead.md` → `:92 :143 :151`.

### **D8 · MINOR — the routing-summary-tail rule is applied without a stated discriminator: two counted, three silently omitted, one recorded as a zero.**
Rule (1) admits *"routing-summary tails"* into the non-roster set unqualified. Six records carry one naming KF.W7: `kf-RibbonBar:131` and `kf-SequenceScrubber:134` are **counted**; `kf-SpringTrace:139`, `kf-SpringTarget:146` and `kf-SequencePlayhead:151` are **omitted**; `kf-SequenceScene:153` is **recorded as an explicit zero**. The practice is defensible (the two counted carry an obligation not otherwise booked; the three omitted restate already-counted identities) — **and the rule nowhere says so**. This is the residual half of the D1 class: the reconciliation fixed the *membership* test and left the *boundary* test unwritten.
**Receipt**: `KF-W7.md:13` rule (1) vs `kf-RibbonBar.md:131` · `kf-SequenceScrubber.md:134` · `kf-SpringTrace.md:139` · `kf-SpringTarget.md:146` · `kf-SequencePlayhead.md:151` · `kf-SequenceScene.md:153`.

### **D9 · MINOR — a second short transcript, at the D10 cure: a grep printed at 1 of its 2 lines.**
`KF-W7.md:122` prints *"**KF-AV-17**'s registry disposition, quoted by command — `grep -n 'KF-AV-17' kf-AnimationVisualizer.md` → `:61` …"*. Re-run this seat: **two** hits — `:61` (the disposition) and **`:105`** (`12. **K-12 (reader-2) · D-3's cited cure portability** … Carried in KF-AV-17.`). `:105` is the *kill entry that supplies the very cure-shape lock the row carries* — corroborating, not contradicting, which is what makes the omission cheap and the class expensive.
**Receipt**: `KF-W7.md:122` vs `grep -n 'KF-AV-17' kf-AnimationVisualizer.md` (2 lines).

### **D10 · MINOR — the D9 by-scope cure stopped two edges short: the same section still addresses two MINTED-UNAUTHORED waves as though their specs exist.**
The spec applies D9 *"by SCOPE, not by named instance"* and re-points **KFED-UNIT**, **CARD-UNIT** and **EDITOR-UNIT** to `KF-W10.md` §6.D → the KF.W12 row — correctly and well. Two addressees of identical shape in the same section are left unconverted: `:339` — *"**cube** and **square** packets are KF.W11's …, **dock-menu** is KF.W13's — **homed by their owning specs**, edges declared from this end"* — and the cross-edges at `:354` (*"**KF.W11 arm (d)** (this wave GATES)"*) and `:355` (*"**KF.W13** (this wave GATES)"*). Per RULINGS-4 R4-8 those three waves are **MINTED-UNAUTHORED**; there are no owning specs, and §6.D rows 1 and 3 are their terminal disposition exactly as row 2 is KF.W12's. The register **exists** (verified, `KF-W10.md:558`/`:559`), so the fix is a citation, not a finding — but *"a repair that cures the named instance and not the stated scope leaves the law green over its own counterexample"* is this spec's own quoted lesson, and it applies one row over.
**Receipt**: `KF-W7.md:339`, `:354`, `:355` vs `KF-W7.md:228`/`:233`/`:356` (the three converted ends) and `KF-W10.md:558`-`:559`.

### **D11 · MINOR — two `sed -n 'Np'` receipts print sentence-fragments of the returned line as the command's output, unmarked.**
`KF-W7.md:119`: *"`sed -n '69p' kf-SpringTarget.md` → m-8's clause **"Cure cautioned by banked kf-EasingTarget P-2 (…)."**"* — the line returned is a full 400-word m-8 row of which the quoted clause is the tail-but-one; the leading two-thirds and the trailing `**KF.W7 (with KF-AV-10).**` are elided with no ellipsis. Same at `:164` (D5). The *quotations themselves are byte-exact* — this seat diffed both — and the labels ("m-8's clause", "§DISSENT item 1") disclose that an excerpt is intended, so the harm is small. It is recorded because LAW D(1) makes the pasted command the guarantor of the quote, and a command whose output is silently trimmed guarantees nothing.
**Receipt**: `KF-W7.md:119` and `:164` vs `sed -n '69p' kf-SpringTarget.md` / `sed -n '150p' kf-TimelineTrack.md`.

### **D12 · MINOR — the LAW-C substrate disclosure mislabels D1's vehicle as an anchor, which is why the drift went unseen.**
`KF-W7.md:344` partitions every cross-wave receipt into BACKWARD (quoted-by-command) and FORWARD (*"stable anchor ALONE — §-heading + row/gate id, no quoted prose, no line numbers"*), and enumerates the forward members *"so none hides"*: *"**KF.W8** — §Bounds' four-party footprint row and the two characterization-pair create rows"*. The two create rows **are** anchors (filenames) and both verify (`KF-W8.md:120`). The four-party footprint row is **not** an anchor: it asserts W8's access verbs, its glob spelling and its file count — a content claim about a sibling's round-4 bytes, filed under the exemption for claims that make none. Had it been classified honestly it would have been a forward *quotation*, forbidden by LAW C(3) and caught by the RECONCILE seat. **The taxonomy failure is the mechanism of D1.**
**Receipt**: `KF-W7.md:344` (the FORWARD enumeration) vs `KF-W7.md:77` (the row it exempts).

---

## §4 What HOLDS — recorded so the repair does not regress it

- **Census carriage**: **0 escaped**, under all three admissible denominators. All **127** roster identities matched to a carried row by banked id; the three §Excluded-with-named-owner rows verify at **both** KF.W6 ends by banked row header for the fourth consecutive round while their line numbers moved again.
- **LAW A**: both delete/repoint censuses re-executed independently and reproduce **command-for-command**; the `loadPreset` re-export consequence and the out-of-bounds repoint consequence are both real and both correctly binding. This remains the best-founded material in the file.
- **Frontier witnesses**: 11 of 11 sampled `§Bounds` line counts EXACT; 9 of 9 sampled gate witnesses reproduce, several to the line (G5's five anchors, G11's 0/13 split, G13's two delta reads, G14's six toast lines); OP-4 exact. G6's four newly path-qualified leaves **all verify**, and the round-4 note that two of them resolve in more than one module is correct.
- **The four PASS-4 quotation cures are genuine.** P-2 re-attributed and quoted whole from `kf-EasingTarget.md:109`; the gloss labelled as the gloss; MISS-β3 re-quoted **with its scoping parenthetical restored**; PR-CAUTION byte-identical.
- **D2's rung restoration is real**: `### INFO` at `kf-KeyframeCard.md:95`, `KF-KC-52` at `:100`, carried at INFO — the promotion the M9 lock forbids is reversed with no re-adjudication offered.
- **D5's sixth inbound edge is genuinely two-ended**: `KF-W2.md:710`/`:753` reproduce at current bytes, hard-ordering **item 9** is present with a falsifier in both directions, and the no-deadlock argument is taken from W2's own sentence.
- **D7 and D8 are closed**: the taxonomy headline now says *"all THREE rows in TWO records"* and both commands re-run to the same outputs; `L-15-PROTECTED` is bound to the record's literal heading at **both** sites (`grep -c 'kill register'` → 0 confirms the old binding was dead).
- **Gates**: fifteen, **all born-RED, none stamped, every one with reachable green**. G2's AnimationControlsGroup arm is honestly conditioned on OP-0 while the detached-target arm is unconditioned, so the gate is not hostage to a NO-WAVE-OWNER settlement; G11's cost is one devDep plus a named inbound act; G15 is correctly a *ruling* gate because its repoint site is out of bounds; G14's registration clause and its ordering are no longer conflated.
- **Posture**: W4 head, W3 gated, **O-20/O-21 zero hits (correctly)**, the W11/W12/W13 disposition per R4-8 present, and **every W10 green this wave depends on rests on an artifact that exists at W10's current bytes**.

---

## §5 Local verdict

**DEFECTIVE.** The wave's substance keeps improving — carriage is clean, LAW A is exemplary, every frontier witness this seat re-ran reproduces, and four of the five PASS-4 MAJOR cures (D2, D3, D5, D6, D8) verify at the bytes. The failures are all in the **citation layer**, and they cluster exactly where four rounds of rulings predicted they would: **a sibling's bytes moved under a receipt** (D1 — and it is the round's own convicted class, now with a falsified certification behind it, D2), **a figure that outruns its stated rule for the sixth time** (D3, D8), **transcripts shorter than their commands** (D4, D9, D11), **a quotation truncated inside the cure written against truncation** (D5), and **a universal asserted in the spec's own voice that its own receipt refutes** (D6). D1, D2, D3, D4, D5 and D6 are the repair round's work; D7–D12 are one-line corrections.

*Sole write = this file. Read-only at both trees. `origin/master 81a56990` for every keyframes witness; the value.js registry and the ten sibling specs at this seat's clock. No gate stamped, no ruling declined, no prior-pass artifact edited, nothing inherited from PASS-4's prose — every figure, receipt and quotation above was re-executed here before it was written.*
