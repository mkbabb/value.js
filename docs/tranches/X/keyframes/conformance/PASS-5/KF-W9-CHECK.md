# KF-W9-CHECK — PASS-5 (fresh adversarial spec check, L-18/L-20)

**Subject** `docs/tranches/X/keyframes/waves/KF-W9.md` (544 L, 221,965 B, mtime **2026-08-28T17:32:08**) — X.KF.W9, Safari Visual Audit, frontend half — **four times repaired** (rounds 1/2/3/4).
**Seat** FRESH. Nothing inherited. **PASS-4's 168 is NOT inherited**, exactly as that artifact instructs (*"No pass-5 seat should inherit 168 either"*). The census below is re-enumerated **by record** from the 58 `kf-*.md` with this seat's own stated rule and its own scripts. PASS-1's 167 · PASS-2's 147 · PASS-3's 139 · PASS-4's 168 were read only to know what has been ruled, never to inherit a figure.
**Substrate** keyframes.js `origin/master` **`81a56990736ced5b5edde0b84c527680ac7689b1`** (`git rev-parse origin/master`, exact, this seat). Local HEAD `8281638c` is **DISQUALIFIED** and appears here only as declared historical context; the working tree is dirty (252 paths) and **every keyframes measurement below is `git grep`/`git show` against `origin/master`, never the worktree**. value.js at current tree bytes, mtimes stamped where load-bearing.
**Corpus** the 58 `registry/adjudicated/kf-*.md` (`ls kf-*.md | wc -l` → **58**). Sole carry `carry/KF-W6-CARRY.md`.
**Verdict** **DEFECTIVE** — 8 defects (2 HIGH · 5 MEDIUM · 1 LOW). **Axis 1 (census) is CLEAN at 181/181/0.** **Axis 3 (M-25 depth + LAW-A reproduction) is CLEAN — every pasted command re-runs to its printed output at the frontier, including all four cells R4-4 corrected.** The convictions are on **axis 2 (receipt reality — the round's own close certification is falsified at the bytes and never verified this wave's forward receipts)** and on **the numeric arm inside round 4's own cures**.

---

## §1 · Axis 1 — ID-KEYED CENSUS BY RECORD (own rule, own scripts, escapes by bytes)

### The rule, stated so pass 6 can disagree with it precisely

A **routed (record, id) pair** is an id that a record's **own bytes** route to KF.W9, by either shape:

1. **Disposition/roster row** — a line that opens a roster row (`- **…**` bullet **or** `| **…** |` table cell) **and contains the token `KF.W9`**, in a section not named killed-claims / identity-guard / superlatives / scoreboard / verdict / dissent / routing-summary / corrections / ruled-disagreements / provenance. The ids are those in the row's **leading bold run**, split on `/ + · , ≡ &` and truncated at the first em-dash or paren.
2. **Routing-summary segment** — the ids named inside that record's `**KF.W9**:` segment of `## Routing summary`, differenced against (1).

Both shapes are required: the corpus mixes bullet- and table-shaped rosters, and **23 records** carry a `**KF.W9**` routing segment naming ids their own rows do not all reproduce.

### Measurement (script: `p5w9census.py`, this seat)

| cell | measured |
|---|---|
| records in corpus | **58** |
| records mentioning `KF.W9` at all | **52** (6 zero-mention: kf-KfPillTabs · kf-KeyboardShortcutsModal · kf-DemoGlobalChrome · kf-ControlsPaneWrapper · kf-ChannelControls · kf-AnimationControlsGroup) |
| raw rule-1 row→id hits | **149** |
| records with a `**KF.W9**` routing segment | **23** |
| **routedTotal — distinct (record, id) pairs (rules 1 ∪ 2)** | **181 over 39 records** |
| **bookedCount** | **181** |
| **escapedCount** | **0** |

*On the denominator's shape.* Five seats, five numbers — **167 · 147 · 139 · 168 · 181** — one corpus. They are **granularity variants** (limb splitting, alias treatment, table-row inclusion, routing-segment differencing), not rival truths. **181 is this pass's enumeration and the discharge below is keyed to it. No pass-6 seat should inherit 181 either.**

### Discharge

A machine sweep over all 181 pairs, testing (i) presence of the id token in `KF-W9.md` and (ii) presence of the id in that record's **own** `UNPROVEN-NEEDS-LIVE` / `SS-13` residue (route (d), record attribution enforced), returned **4 candidates with neither**. All four adjudicated at the bytes:

| candidate | resolution (read this seat) |
|---|---|
| kf-CSSCodeEditor `corpus-missed` | **not an id** — extractor artifact from `**KF-CE-33 · corpus-missed (reader-A) …**` (`:69`). KF-CE-33 is carried at §H's SS-13 aggregate by name. DISCARD. |
| kf-SequenceTarget `HCM-family` | **not an id** — artifact from the routing segment's *"ST-1's HCM-family datum"* (`:164`). ST-1 is carried whole at §B. DISCARD. |
| kf-ChromeDock `m-6` | **discharged, route (d)** — the row is `D-6 / m-6` (`:76`, *"live overflow → KF.W9"*); that record's residue item **#8** carries *"**D-6's label overflow** (longest live string 'Matrix Controls')"* (`:137`). The alias `m-6` is unspelled in the spec — booked as **D5-8**, not as an escape. |
| kf-EasingTarget `D-M-6` | **discharged by identity** — the row is `KF-ET-14 · D-M-6` (`:53`) and §E carries it under **KF-ET-14** with the bank's contingency and the SS-13 arm intact. The alias is unspelled — **D5-8**. |

Nine further pairs resolve by route (d) alone (absent from the spec, present in their own record's residue): kf-App **KF-APP-2/-3/-7/-8/-11** · kf-ChromeDock **C-10** · kf-EditorHeader **D-20** · kf-StartingStyleTarget **KF-SST-17** (also carried at §B as the `KF-SST-14/-17` fold) · kf-TypingDots **KF-TD-4**. Each was read at its record's residue this seat.

**AXIS 1 VERDICT: CLEAN — 181 routed, 181 discharged, 0 escapes**, at a granularity larger again than PASS-4's. Five independent enumerations, five denominators, **zero hard escapes for the third consecutive pass**. The class booked at PASS-2 (R2-10's three) remains the last one found.

### Counting family, re-derived at the post-round-4 bytes (never inherited)

| cell | spec claims | this seat measures | |
|---|---|---|---|
| §Carry bullets | 79 (A 18 · B 5 · C 1 · D 8 · E 7 · F 13 · G 19 · H 8) | **79**, same per-section split | ✅ |
| live gates | 13 (`-1..-9`, `-11..-14`); `-10` struck-in-place | **14 rows, 13 live**, `G-KFW9-10` struck, id retained | ✅ |
| §Bounds data rows | 16 | **18 pipe rows − header − separator = 16** | ✅ |
| escalation triggers | 13 armed over 12 records | **13** ` · `-separated armed items | ✅ |
| negative register | 6 records · 11 probes · 4 traps | **6 · 11 · 4** | ✅ |
| dissents | 11 row-level + 1 wave-level = 12 | **11** `Dissent preserved` inside `- **` §Carry bullets (14 file-wide; the stated scope rule is correct) **+ 1** | ✅ |
| KF-CE-13 fold families | **sixteen** by the row's ` · ` separation | **16**, script-split at the bytes | ✅ **R4-6's cure is CORRECT** |
| cross-edges | enumerated at S-10.1, numeral stated only there | **13** numbered edges present | ✅ |
| enumerated residue | 565 by the AMENDED (`##` **or `###`**) command | **h2-only 563 · h2+h3 565 · records-with-items 57 · files 58**; sole `+2` = kf-ChannelOptions (`### G. SS-13 additions` `:223`, items 11 `:225` / 12 `:226`); sole zero-item record = kf-CubeAxisLines | ✅ **D3-6's cure reproduces exactly** |
| prose-carried residue | 9 + 8 + 8 = 25 | anchors verbatim: `kf-CubeAxisLines.md:104` · `kf-TransportDock.md:122` · `kf-CubeTarget.md:83` | ✅ |
| `KF.W3` occurrences | **4** file-wide · 1 body · 3 ledger | **3** file-wide (`:251` `:505` `:520`) · 1 body · **2** ledger | ❌ **D5-4** |

---

## §2 · Axis 2 — RECEIPT REALITY (every cross-wave receipt at FINAL sibling bytes; the drift class must be dead)

### The stage-1 receipts — HOLD, byte-exact, at the substrates named

Re-executed this seat against the siblings' current bytes:

- **`KF-W5.md`** (mtime **17:32:57**; the spec stamps its per-wave final at **16:55:55**, and the 17:32:57 delta is the RECONCILE seat's own `⟨RECONCILE SEAT⟩` insertion, not a per-wave write — the stamp is lawful). `grep -n '66-75' KF-W5.md` → **six** lines `:216 :218 :329 :372 :391 :485`, exactly as the reconcile insertion at §A's KF-TD-1 row corrects. **Both named cells verify byte-exact**: §Bounds `play-lifecycle` row at `:216` (*"`strategies.ts:66-75` the **false** live-PRM docblock (declaration `snapToReducedMotion` at `:76`)"*) and §Carry **B-6 · KF-TD-1** at `:329`. ✅
- **The R4-3 reciprocal at KF-W5 `:329`** — `grep -c 'hands the unification constraint here as a DECLARED SEQUENCING OBLIGATION'` → **1**; `grep -c "This wave's act discharges the obligation and KF.W9 records the discharge"` → **1**. ✅
- **The R4-3 reciprocal at `KF-W6.md`** (mtime **16:59:25** = the stamped substrate, exact) — `grep -n 'G-KFW9-6' KF-W6.md` → `:387`, the **KAD-11 — THE PRM ROW**, carrying *"← `KF.W9` `G-KFW9-6` hands the unification constraint here as a DECLARED SEQUENCING OBLIGATION (RULINGS-4 R4-3); this wave's act discharges it and…"*, plus its directive at `:575`. ✅
- **`KF-W6.md:404` KF-HA-10** — quoted **whole** by the spec, and the whole is what is there: *"**KF-HA-10** (MIN) — drift never parks outside PRM: **AN OWNER RULING QUESTION, not a fix order — is the drift register blessed?** Frame cost → KF.W9."* Round 2's unmarked ellipsis is genuinely closed. Sibling half `KF-W6-CARRY.md:220` ✅ (mtime 10:49:45, frozen).
- **`KF-W0.md` 16:58:59 · `KF-W4.md` 16:59:44** — consumed by §-heading anchor only; both stamps exact. ✅
- **Line-numbered sibling cites in this file**: `grep -on 'KF-W[0-9]\+\.md:[0-9]\+'` → **4** (`:50` `:252` `:322` `:323`), all `KF-W4.md:193` / `KF-W0.md:276`, **all four riding an explicit strike marker** (*"dead"*, *"the round-1 coordinate died"*). **Live line-numbered cross-cites: ZERO.** ✅

### The forward (W10) receipts — the anchors HOLD; **their certification does not exist, and the certifying artifact is stale**

W9 reduces its W10 receipts to anchors under LAW C(3) and delegates their verification twice — §Bounds `:74` (*"The **RECONCILE seat** verifies these five anchors against W10's final bytes after the last per-wave write and certifies them at `PASS-4/CLOSE-CERT.md`"*) and S-10 `:250` (same undertaking).

**Verified by this seat instead, at `KF-W10.md`'s current bytes** — all six resolve:

| anchor | resolves |
|---|---|
| §State — *Opens after* | `:20`, names **X.KF.W9/SS-13** ✅ |
| **OP-4** | `:66`, *"KF.W9/SS-13's capture packet exists — both transport homes … 390 AND 1280, real Glass 7"* ✅ |
| the **PACKET-FIRST** law | `:485`, *"KF.W9/SS-13 produces the 390+1280 captures against real Glass 7 **before** the ask is assembled. Never proxied."* ✅ |
| §D — the → KF.W9 cross-edge row | `:519`, *"**D. → KF.W9 (Safari Visual Audit) / SS-13 — W10 depends.**"* ✅ |
| §B-1 | `:182`, the V-A95 aurora row, *"surface verify **KF.W9**"* ✅ |
| §B-3 | `:184`, the Glass §4 dock-contract re-verify subsuming CH2-02 ×4 ✅ |

Also verified: **§6.D · SUCCESSOR-FORMATION REGISTER exists** (`:543`) with the **KF.W11** and **KF.W13** rows carrying W9's R4-4 blast-radius receipts by name (the `btn-playback`/`.focus-ring` radius at the W13 row; the retired-gate prose radius at the W11 row) — so the R4-4 hand-off addresses a reader that exists. ✅

**But the delegation is unfulfilled and the artifact it names is falsified at the bytes** — **D5-1** and **D5-2**.

---

## §3 · Axis 3 — M-25 DEPTH + LAW-A / SUBJECT-IDENTITY REPRODUCTION AT THE FRONTIER

### The three LAW-A censuses, re-run this seat at `81a56990` — **every corrected cell reproduces**

```
git grep -n  'focus-ring'   origin/master -- demo/ test/ src/ scripts/ | wc -l   → 11
git grep -ln 'focus-ring'   origin/master -- demo/ test/ src/ scripts/ | wc -l   →  7
git grep -n  'class=.*focus-ring' origin/master -- demo/                         →  4  (KeyframeCard.vue:45 ·
        SpringHeatmap.vue:30 · SpringTarget.vue:63 · SquareScene.vue:46)
git grep -n  'btn-playback' origin/master -- demo/ test/ src/ scripts/ | wc -l   → 24
git grep -ln 'btn-playback' origin/master -- demo/ test/ src/ scripts/ | wc -l   →  7  (incl. EasingScene.vue)
git grep -n  'seq-handle'   origin/master -- demo/ test/ src/ scripts/ | wc -l   → 11  / 2 files
git grep -l  'proof:sequence-rows-draggable' origin/master -- .                  → 20 files
        by tranche: H 13 · J 3 · K 1 · S 1 · U 1  (=19 doc files) + the SequenceTarget.css comment
git grep -n  'proof:sequence-rows-draggable' origin/master -- .    | wc -l       → 32  (31 doc + 1 CSS)
git grep -c  'proof:sequence-rows-draggable' origin/master -- package.json       → exit 1
git grep -n  'proof:sequence-rows-draggable' origin/master -- .github/ scripts/  → exit 1
```
**All four cells R4-4 corrected (7 files · 4 class-consumers · the widened census-1 scope · the five-tranche prose field) reproduce exactly.** D4-1 is genuinely cured; the numeric arm of the LAW-A block is sound. *One characterisation inside the widened scope is not — see **D5-5**.*

### SUBJECT-IDENTITY sweep, re-executed member by member

`index.ts:50` (`export { reducedMotionScale } …`) ✅ · `frame.ts:121` `export function playFrame` ✅ / `:131` `const flipped = withReducedMotion(` ✅ / `:137` `snapToReducedMotion(anim);` ✅ · `strategies.ts:66` docblock open / `:75` close / `:76` `export function snapToReducedMotion` ✅ / `:109` `return beginPlay(anim._playback, () => withReducedMotion(` ✅ · `group/group.ts:57` `respectReducedMotion = false;` ✅ · `constants/defaults.ts:87` `respectReducedMotion: false,` ✅ · **`delegation.ts:53`** `const shadowTick = (now: number)…` ✅ and `:54` = `if (animation.done) return false;` — **D4-4's `:53` cure is CORRECT at both citing sites and in the §RECEIPT row** · `git grep -c 'withReducedMotion' … delegation.ts` → **exit 1** ✅ · `src/animation/physics/numeric.ts:82` constructor / `:89` the flag assignment — **D4-7's full path is CORRECT** ✅ · `reducedMotionScale` → **6 hits**, demo → exit 1, spring lane `:2 :153 :232 :385` ✅ · `package.json:43` `"gh-pages": "vite build --mode gh-pages"` ✅ · `SequenceTarget.vue:97` `class="seq-handle"` ✅ · `clampIOSNoZoomFontSize` decl `iosTextEntry.ts:10`, consumed `CSSCodeEditor.vue:39`/`:137`, tested `:88`/`:89`/`:97`; `initIOSPlatformClass` decl `:14`, called `EditorShell.vue:133` ✅ · **§Bounds PRM surface: 15 of 15 paths resolve** at the frontier, incl. `demo/scenes/cube/orbital-drag/OrbitalDrag.vue` (D3-8's cure). ✅

### M-25 depth (locks, riders, dissents)

- **D-14 + N-4 one-binding lock** (`kf-SpringTarget.md:63`/`:76`) carried verbatim with the *never split* gloss ✅ · the **a11y one-edit family** (`:148`) ✅ · **D-B3 lattice lock** (`kf-SpringHeatmap.md:37`) carried twice as the bank requires ✅ · **S-14's N-1 time-domain lock** entered in §Sequencing, not merely mentioned, with its NOT-governed scope stated ✅ · the **amiga shared-idiom cure lock** carried, not cited ✅ · **KF-AV-28** carried whole at §H with seven governed rows + one identity-governed twin + D-24 named as a not-yet-governed datum ✅ · **all twelve dissents** present.
- Non-registry authorities re-read: `lane-frontend.md:462` = `### 6.5 prefers-reduced-motion — 13 enforcement sites across 12 files` ✅ · `INTAKE-ADJUDICATION-2026-08-03.md:138` = `## §3 — CARRY table` ✅ and `:161` carries *"73,568 Kronecker denominator; KF.W9 scopes its own surface list"* ✅ · `intakes/lane-keyframes-b10-b21.md:151` is the B18-26 row ✅ · arithmetic re-executed: `152×11×4×11 = 73568`, `46816+26752 = 73568` ✅.

**AXIS 3 VERDICT: CLEAN.** Fifth consecutive pass with no conviction on depth, and the first at which the whole LAW-A block reproduces cell-for-cell.

---

## §4 · Axis 4 — GATES born-RED with REACHABLE GREEN · in-grant closes · LAW B

**Born-RED witnesses re-executed at `origin/master 81a56990` / the value tree:**

- **G-KFW9-1** — `git ls-files …/audit/visual/safari-real/` → **4** tracked vs **31** on disk; `shots/` **0 tracked / 11 on disk** ✅ exact.
- **G-KFW9-8** — `git grep -c forced-colors origin/master -- demo/` → **exit 1, zero** ✅.
- **G-KFW9-9** — `design-idioms.css:76` `.focus-ring:focus-visible` (rule `:76-79`, doc comment `:73-74`) **and** `playback-idiom.css:72` `.btn-playback:focus-visible` ✅ — two selectors, both unlayered.
- **G-KFW9-10's surviving measurement** — `SequenceTarget.vue:97` ✅; the fork's justification **void** (both falsifier greps exit 1) ✅.
- **G-KFW9-11** — the whole `clampIOSNoZoomFontSize` / `initIOSPlatformClass` family resolves ✅.
- **G-KFW9-14** — both shas exact; `package.json:43` live ✅.

**REACHABLE GREEN.** **All thirteen live gates now close on acts inside this wave's grant.** **G-KFW9-6's R4-3 re-cut is correct and complete**: the CLOSES is three own-wave measurements (flag-state-and-layer register · the KAD-11 rest-state capture · the `reducedMotionScale` census), the unification constraint is carried **verbatim** and handed to KF.W5/KF.W6, **both reciprocals verify byte-exact at the siblings' bytes (§2)**, and the falsifier forbids a green on the constraint's behalf. **PASS-4's D4-2 — the wave's one unpassable oracle, the round-1 D-3 class at its third gate — is CLOSED.** **G-KFW9-13 is likewise reachable**: the §Bounds registry cell is split *read-only for existing bytes (E-1) / APPEND-ONLY for dated addenda under original ids (E-3), by `.e` alone*, which is what S-13's carve-out and the gate's CLOSES already assumed — **D4-5 is CLOSED**, and the declined alternative is recorded on the ledger rather than silently dropped.

**LAW A scope.** The §SCOPE receipt greps the **§Bounds access column** (its stated enumeration source, not a ruling's example list): **16 data rows, 0 carrying `delete`/`repoint`/`shim`/`move`** — re-derived true this seat (`create` ×3, `create/write`, `modify` ×2, `modify-carve`, `EXECUTE NO WRITE`, `read-only`-family ×7, the append-only split). The wave authors no LAW-A act; the three it carries have 3 of 3 censuses, all reproducing (§3).

**LAW B.** No closure claim is asserted in this spec's own voice. The census claim is homed in the artifact with its defects; S-10.1's thirteen and §B's sixteen are stated **at their enumerations** with counting rules and alternative granularities disclosed. Two residual citation faults — a stale artifact at §F (**D5-6**) and an over-claimed one at §RECEIPT (**D5-7**) — are LAW-B-adjacent bookkeeping, not self-voiced closure.

**Convicted on this axis:** none. *(Axis 4 is CLEAN for the first time in five passes.)*

---

## §5 · Axis 5 — POSTURE

| clause | finding |
|---|---|
| **W4 head honored** | ✅ **by construction and by measurement**: §Bounds carries **zero** kf write grants (all five EXECUTION-TIME-ONLY grants struck, each re-marked READ-ONLY), the access column is grepped in the file's own receipt, and S-13's triumvirate fence is never armed by this wave's own gates. **The residue PASS-4 qualified this row with — G-KFW9-6's cure-naming CLOSES — is gone.** |
| **W3 gated-unscheduled** | ✅ `KF-W3.md:1` = *"# KF.W3 — Parser Consumption (GATED, never scheduled)"*; W9 schedules nothing against it and names it as a deliberate non-edge. ❌ only in the **count** attached to the disclosure — **D5-4**. |
| **O-21 (not O-20) at W1's mint sites** | ✅ correctly **not this wave's surface**: `grep -c 'O-20\|O-21' KF-W9.md` → **0**. The mint lives at KF-W1 and its consumption at `KF-W10.md:20` (*"carried by KF.W1's outbound row `O-21`, named by id at repair round 4 — §END KF-W10 D-8"*), re-read this seat. No stale mint anywhere in W9. |
| **W11/W12/W13 disposition per RULINGS-4 R4-8** | ✅ **present and coherent from this end.** S-11 states the canonical **17-packet roster** and its homing **KF.W11 ×9 · KF.W12 ×6 · KF.W13 ×2**; S-10.1 carries edges 7/8/9 to the three; the two LAW-A hand-offs are addressed to **`KF-W10 §6.D`'s KF.W11 and KF.W13 rows** with the express reason (*"KF.W11/KF.W12/KF.W13 are MINTED-UNAUTHORED per R4-8, so a census handed to a filename would have no reader"*). Verified at the far end: §6.D exists at `KF-W10.md:543`, the partition sums **9+6+2 = 17**, and both W9 censuses are quoted inside the W11/W13 rows. |
| **W10's greens conditioned on artifacts that exist** | ✅ **for this wave's edge.** `KF-W10 §6.C-0` row **4** (`:503`) conditions on `KF-W9 §Sequencing S-10` (`:250`) · `§Sequencing S-8` (`:248`) · `KF-W9 §Bounds` capture-receipt row (`:74`) — **all three present at W9's bytes**, and the receipt is a declared output (`SS-13-CAPTURE-RECEIPT.md`, create `.e` serial) rather than a promise. OP-4/PACKET-FIRST/§B-1/§B-3 likewise resolve (§2). |
| **KF-AV-28 present where governed** | ✅ carried whole at §H, governed set enumerated by id from all three banked coordinates, with the ribbon's §7 caution riding the same line. |

---

## §6 · DEFECT REGISTER — 8 booked

### D5-1 · HIGH — `PASS-4/CLOSE-CERT.md` is falsified at the bytes: `KF-W10.md` moved **after** the certification that proves it wrote last, and this wave conditions its own closure on that artifact

`CLOSE-CERT.md` §1 is the LAW C(4)(i) **mtime table — "proof this seat wrote last"**. Its row 6 reads `KF-W10.md | 17:31:54 | 209,902 B`. Measured this seat:

```
stat -f '%N %Sm %z' -t '%Y-%m-%dT%H:%M:%S' KF-W10.md          → 2026-08-28T17:38:21  209923
stat -f '%N %Sm %z' -t '%Y-%m-%dT%H:%M:%S' PASS-4/CLOSE-CERT.md → 2026-08-28T17:37:59   27004
```

**`KF-W10.md` is 21 bytes larger and 6 minutes 27 seconds later than the state the certification tabled — and later than the certification itself.** The nine-minute margin §9(1) certifies (*"this seat's first write at 17:30:52 … nine-minute margin"*) is therefore not a margin at all at the file that matters most: the close wave, which LAW C(1) makes the **last** per-wave seat and which is the one spec whose post-certification movement can invalidate every forward anchor pointed at it.

This is **the stale-stamp class the certification itself convicts** — §8 item 1 books twelve of its sixteen repairs under *"a seat reads a sibling, then the sibling writes again before the reader does"* — reproduced **in the instrument's own proof**, one level up, exactly as U4-A's cure was supposed to end it.

**Why this is W9's defect and not only the reconcile seat's.** This spec's round-4 ledger closes on it in terms: *"the cross-wave receipts written this round are the **RECONCILE seat's** to certify at `PASS-4/CLOSE-CERT.md`, and **without that certification this round did not close**."* W9 rests its closure and both of its forward-receipt cells on an artifact whose §1 proof a two-command `stat` refutes.

**Cure**: re-run the reconcile sweep's §1 table (or have this wave's cells cite `CLOSE-CERT §R-8`/`§7` **by finding**, not by the table), and re-verify anything `KF-W10.md` carries that W9 depends on at its **current** bytes — which this check has done (§2) and records so the next seat need not.

### D5-2 · HIGH — the RECONCILE seat never verified this wave's forward receipts; the delegation W9 states twice does not appear in the artifact it names

W9 delegates, at both cells:
- §Bounds `:74` — *"The **RECONCILE seat** verifies these five anchors against W10's final bytes after the last per-wave write and certifies them at `PASS-4/CLOSE-CERT.md`."*
- S-10 `:250` — *"the **RECONCILE seat** re-verifies all five anchors against W10's final bytes after the last per-wave write and certifies them at `PASS-4/CLOSE-CERT.md`."*

Measured against that artifact:
- **§3's `KF-W9` row** counts *"5 same-round receipts · 4 HOLD (both R4-3 reciprocals byte-exact) · 1 NUMERIC"* — and the five it enumerates are **all backward**: KF-W5 ×2, KF-W6 ×2, `KF-W6-CARRY.md`. **The W10 anchors are not among them.**
- **§4 R-8** is the **reverse direction** — `KF-W10 → KF-W9`, verifying W10's three anchors *into* W9 (S-8 `:248`, S-10 `:250`, §Bounds `:74`). It does not verify W9's anchors into W10.
- **§7's forward-reference roll-call** lists `KF-W0→KF-W1` · `KF-W3→KF-W10 §6.C-I` · `§6.D` · `KF-W6→KF-W10 §5 G-2` · `ADOPTED-BY-KF.W6` · `KF-W6→KF-W8 G7` · `KF-W7→KF-W10 §6.D KF.W12 row`. **No `KF-W9 → KF-W10` entry exists**, and §7's scope sentence is *"All **stage-1 → stage-2** forward references"* — W9→W10 is stage-2→stage-2 and falls in no swept class.

So the **one seam this wave was convicted for closing by silence at PASS-3 (D3-3)**, and cured at round 3 by declaring it, is now carried under a warrant that was never executed. **This check executed it instead: all six anchors resolve (§2), so the substance stands and no ruling re-opens.** What fails is the receipt's warrant — the same shape as D3-3, one layer out.

**Cure**: replace the delegation sentence at both cells with **this pass's verification, by command** (the six coordinates in §2 above, each with its `grep -n`), and state that the RECONCILE certification covers this wave's **backward** receipts only.

### D5-3 · MEDIUM — "five anchors" is asserted at two cells whose enumerations differ, and the union is six

The two cells that delegate the verification also state its denominator:

- **§Bounds `:74`** — *"the anchors, and nothing but the anchors: **KF-W10 §State (Opens after)** · **KF-W10 OP-4** · **KF-W10 §D (the → KF.W9 cross-edge row)** · **KF-W10 §B-1** and **§B-3**"* → State · OP-4 · §D · §B-1 · §B-3 = **five, PACKET-FIRST absent**.
- **S-10 `:250`** — *"declares this wave a hard opening precondition at **five** sites … **§State — the Opens after row** · **OP-4** · **the PACKET-FIRST law** · **§D — the → KF.W9 cross-edge row** · **§B-1 and §B-3 — the two coordination rows**"* → five bullets but **six anchors**, with **PACKET-FIRST present** and §B-1/§B-3 merged into one.

**Union = six** (all six verified live, §2). One numeral, two enumerations, and a third party asked to verify *"these five anchors"* with no single list to verify. This is the **exact fault this file has now cured four times** — the negative register (round 1 D-10), the escalation register (round 2 D-5), the cross-edge set (round 3 D3-5), the KF-CE-13 folds (round 4 D4-3) — each by the same remedy the file wrote for itself: **state the count at its enumeration and have dependent cells point there.**

**Cure**: enumerate the anchors **once** (six, with PACKET-FIRST in and §B-1/§B-3 counted separately), state the counting rule, and have the §Bounds cell point at S-10's enumeration rather than re-issue a numeral.

### D5-4 · MEDIUM — the D4-6 cure's own numeric arm is wrong at three cells: the token count is 3, printed as 4, as "3 in this ledger", and as "×1"

D4-6 cured a **self-falsifying count**. Its cure prints a new one — measured this seat with the command the spec pastes:

```
grep -on 'KF\.W3\b' KF-W9.md
  251:KF.W3     ← the S-10.1 disclosure (the spec body)
  505:KF.W3     ← §SCOPE, the numeric-arm receipt
  520:KF.W3     ← §RECEIPT, the counting-family row
grep -c 'KF\.W3\b' KF-W9.md → 3
```

Against which the file states, at three cells:
- `:505` §SCOPE — *"**`KF.W3` occurrences: 4 file-wide** (`grep -on 'KF\.W3\b' KF-W9.md`, post-repair) — **1 in the spec body** … **plus 3 in this round-4 ledger**"* → **file-wide is 3, not 4; ledger occurrences are 2, not 3.**
- `:535` the D4-6 register row — *"Measured at the post-repair bytes: **4 file-wide, 1 in the spec body, 3 in this ledger** — the scope rule is load-bearing … because recording the cure printed the token **three further times**"* → same two errors.
- `:520` §RECEIPT's counting-family row — *"`KF.W3` **×1**"*, in a list whose every other member (79 bullets · 16 §Bounds rows · 14 gate rows · 11+1 dissents) is a **file-wide** count. **A third figure, disagreeing with both.**

**The posture is untouched and correct** — W3 is gated-unscheduled, the spec schedules nothing against it, the body occurrence is exactly 1, and the carried scope rule (ledger and provenance prose are not spec-body references) makes the cured sentence true and keeps it true. What fails is the **numeric arm inside the cure of a numeric defect**, printed beside its own pasted command, in the round whose LAW D(3) exists to forbid precisely that. Its own §SCOPE header claims *"Every figure printed beside a command in this file's LAW-A block was re-run at write time"*; this figure sits in the same block and was not.

**Cure**: `3 file-wide · 1 in the spec body · 2 in this ledger`, and restate `:520`'s cell as `KF.W3 ×3 file-wide (1 body)` so the three cells cannot disagree again.

### D5-5 · MEDIUM — census 1's widened scope calls four docblocks stale prose; its own pasted transcript shows none of them is

R4-4 widened census 1's non-import scope to *"**FOUR SIBLING-MODULE DOCBLOCKS OUTSIDE THE MODULE**, each naming the pre-carve `engine/play-lifecycle.ts` **as if it still existed** … exactly the stale prose a carve leaves behind and the kind a census exists to surface"*. Read at the frontier this seat:

```
git show origin/master:src/animation/engine/playback-state.ts | sed -n '1,6p'
  :2-3  "the `PlaybackState` run-state STORE, CARVED OFF `engine/play-lifecycle.ts` at S.B2 …"
  :4    "the play machine's free functions live in `./play-lifecycle`; the state they mutate lives HERE"
git show origin/master:src/animation/group/lifecycle.ts | sed -n '1,8p'
  :4-5  "lifted off the `AnimationGroup` class as FREE FUNCTIONS (S.B5 / a19 F1 — the group ceiling
         carve, MIRRORING the engine's `engine/play-lifecycle.ts`)"
git show origin/master:src/animation/orchestration/sequence/lifecycle.ts | sed -n '1,8p'
  :5-6  "(S.B5 — the sequence ceiling carve, MIRRORING `group/lifecycle.ts` + the engine's
         `engine/play-lifecycle.ts`)"
```

- `playback-state.ts:3` and `group/lifecycle.ts:5` and `sequence/lifecycle.ts:6` name the flat path as the **object of a carve** — *"carved off X"*, *"mirroring X"* — which are **correct historical provenance statements**, not assertions that X exists.
- `playback-state.ts:4` names **`./play-lifecycle`**, the **live specifier** that resolves to the directory barrel — the very fact census 1's own Finding turns on (*"the specifier is unchanged … no consumer repoint is owed"*). It is not a pre-carve path at all.

**The spec prints both texts and then characterises them as their opposite.** The census's *finding* is unharmed and correct; what is false is the one sentence a receiving seat would act on — it advertises four stale-prose cleanups that do not exist, in the block whose stated purpose is that the receiving seat *"inherits a census rather than a filename"*. This is D4-1's shape (a scope statement its own pasted output refutes) surviving inside D4-1's cure, and it entered by executing RULINGS-4 R4-4 verbatim without re-reading the four lines the ruling named.

**Cure**: restate as *"three sibling-module docblocks name the pre-carve path as the **origin of the carve** (correct as written), and one (`playback-state.ts:4`) names the live `./play-lifecycle` specifier — **no stale reference exists outside the module**; the carve left no prose debt."*

### D5-6 · MEDIUM — §F's preamble still homes "the class's standing verdict" at PASS-3, while every sibling cell was re-pointed to PASS-4

`:161`, §F's class-sweep paragraph: *"a dated measurement, not a closure of the class (LAW B, repair round 3): **the class's standing verdict lives in `PASS-3/KF-W9-CHECK.md §1` (2026-08-28)**, whose independent two-shape re-enumeration recorded **139 routed pairs, 0 escapes** and named PASS-2's R2-10 bookings as 'the class's last members'."*

Round 4 re-pointed this identical claim at **three** other cells — the header (*"citation re-pointed at repair round 4"*), §Carry head `:95`, the round-2 correction block `:97`, and the R2-5 receipt `:99` — each now naming `PASS-4/KF-W9-CHECK.md §1` (168/168/0). **§F was not swept**, so the file now names **two different artifacts** as the home of one standing verdict, with two different figures (139 and 168), and the stale one is the cell that governs the AT arm's class sweep. The file's own instruction is unambiguous: *"When a later pass supersedes that check, the citation is re-pointed, never the claim re-written here."*

This is R4-10's **scope-not-instance** lesson in miniature — a repair that cured the named cells and not the stated class.

**Cure**: re-point `:161` to the freshest census artifact (this file, §1 — 181/181/0), and add the class to the round's scope receipt so *citation freshness* is swept by grep (`grep -n 'PASS-[0-9]/KF-W9-CHECK'`) rather than by memory.

### D5-7 · MEDIUM — the §RECEIPT "NOT re-derived this round" row over-claims the artifact it inherits by citation

`:521`, the round-4 §RECEIPT table's last row: *"**NOT re-derived this round** … **the thirteen gate witnesses other than -6** (PASS-4 §4 re-executed them and found them TRUE at the frontier)."* That is a claim about **twelve** gate witnesses, inherited by citation under LAW B.

Measured against the cited artifact: **`PASS-4/KF-W9-CHECK.md §4` names seven gate ids in total** (`grep -o 'G-KFW9-[0-9]\+' | sort | uniq -c` over the §4 span → `-1`, `-6`, `-8`, `-9`, `-10`, `-13`, `-14`) and re-executes born-RED witnesses for **five**: `G-KFW9-1` · `-8` · `-9` · `-10`'s surviving measurement · `-14` (plus the library-reference family, the R3-9 census and the §Bounds PRM surface). **The witnesses of `-2`, `-3`, `-4`, `-5`, `-7`, `-11`, `-12` are re-executed nowhere in §4.**

Inheriting a broader claim than the cited artifact makes is the **citation-inheritance failure this spec convicts twice in its own body** (the kf-ControlsPaneWrapper `D-3+M-1` dangling cite at round 1; the PASS-1 KF-HA-10 mis-booking at round 2). Consequence is bounded — this pass re-executed `-1`, `-8`, `-9`, `-10`, `-11`, `-14` independently and all hold (§4) — but the row as written tells a pass-6 seat that seven witnesses have a warrant they do not have.

**Cure**: name the five (plus this pass's six) explicitly, and state that gates `-2`/`-3`/`-4`/`-5`/`-7`/`-12`'s witnesses are **documentary** (I-20's two firings, the Kronecker arithmetic, the residue denominator, the census sentence, the two-direction pair, the trap/retired register) and re-verify at wave-open per the gates preamble.

### D5-8 · LOW — two banked aliases are carried by identity and never spelled, the round-2 D-9 class at its last two survivors

- **kf-ChromeDock `D-6 / m-6`** (`:76`, MINOR — the `[&>span]:line-clamp-none` no-op plus the absent truncation contract; *"NO-WAVE-OWNER; live overflow → KF.W9"*). The spec's §G carries five ChromeDock rows (M-5/C-6 · M-2/C-4 · D-4 · D-22+RR-1 · D-23/C-5) and **neither `D-6` nor `m-6`**; the pair discharges only through that record's residue item **#8** (*"D-6's label overflow"*), route (d). A reader resolving a bare `D-6` in this spec lands on **kf-SpringTarget's D-6** at §F — a different record's id at the same token, which is precisely the collision the round-2 D-9 repair existed to kill for the two `D-15`s.
- **kf-EasingTarget `KF-ET-14 · D-M-6`** (`:53`). §E carries the row under `KF-ET-14` alone; the banked reader-D alias is never written.

Neither is a census escape — both rows are discharged (§1) — and both are LOW because the substance travels. But the file's own anti-rename doctrine is *"folds cite their bank"* and its round-2 cure wrote the banked id in full rather than relying on identity.

**Cure**: spell both aliases at their carrying rows (`§G … ⟨kf-ChromeDock D-6 / m-6⟩` as a new residue-routed datum line or inside the existing ChromeDock cluster; `§E **KF-ET-14 / D-M-6**`), with the standing note that a bare `D-6`/`D-15`-class token in this spec is record-qualified.

---

## §7 · WHAT THIS PASS DECLINES TO BOOK

Recorded so pass 6 does not manufacture them:

1. **No census escape.** 181/181/0 at a larger granularity again. A pass booking a hard escape must first defeat the four adjudications in §1 and the nine route-(d) resolutions beside them.
2. **G-KFW9-6 is CURED.** The R4-3 re-cut is in-grant, its falsifier is right, and **both reciprocals verify byte-exact** at KF-W5 `:329` and KF-W6 `:387`. Do not re-open D4-2 or the round-1 D-3 class — it is closed at its third and last gate.
3. **D4-5 is CURED.** The §Bounds append-only split is the minimum that makes G-KFW9-13 reachable without granting a kf byte, and the declined alternative is on the ledger. Do not re-file the immutability contradiction.
4. **D4-1's four cells are CURED and reproduce exactly** — 7 files · 4 class-consumers · the five-tranche prose field · both falsifier exits. Only the *characterisation* of census 1's widened scope fails (D5-5); the counts are right.
5. **D4-3 is CURED.** Sixteen is stated at §B's enumeration with its counting rule and its granularity disclosures, and G-KFW9-8's GREEN and S-2 both point there. Script-verified 16.
6. **D4-4 and D4-7 are CURED.** `delegation.ts:53` at all three sites; `src/animation/physics/numeric.ts:82-89` in full with both offsets verified.
7. **The 565 / ≈590 denominator and its amended command are BOTH correct** — independently reproduced here (`h2-only 563 · h2+h3 565 · 57 records · 58 files`, kf-ChannelOptions the sole +2, kf-CubeAxisLines the sole zero). **Do not re-open.**
8. **The W10 seam is real and two-ended** — all six anchors resolve and W10's §6.C-0 row 4 records it MEASURED. What fails is the *certification* of that receipt (D5-1/D5-2), never its substance.
9. **The round-1 decline (D-14, KF-EST-4's missing KF.W6 counterparty) stands** on R-19e for the fifth pass.
10. **The two `D-15`s are distinct banked ids** (kf-RibbonBar forced-colors fold vs kf-SequencePlayhead RTL posture). Never merge them.
11. **kf-TimelineTrack D-8 is not a W9 escape** — it routes → KF.W7; only its forced-colors arm folds to D-21 at §B. Verified for the third consecutive pass.
12. **S-10.1's coincidence disclosure must not be trimmed**, and its thirteen is not a LAW-B violation — it is stated at its enumeration, which is the cured shape.
13. **O-20/O-21 is correctly not this wave's surface** (`grep -c` → 0). Do not import W1's mint here.

---

## §8 · VERDICT

**DEFECTIVE** — and for the first time on a frontier that is not the spec's own content.

**Three of the five axes are CLEAN.** The census closes at **181 routed / 181 discharged / 0 escapes** under a fifth independent enumeration at the largest granularity yet attempted. Axis 3 is clean for the fifth consecutive pass **and** now reproduces the entire LAW-A block cell-for-cell — the first pass at which no pasted command in this file returns something other than what is printed beside it. **Axis 4 is clean for the first time**: round 4's two structural cures both land — G-KFW9-6 closes on acts this wave can perform, with reciprocals verified at both ends, and G-KFW9-13's write is made lawful by a cell split rather than by a fiction — so **all thirteen live gates now have reachable green inside a wave that holds zero write grants in the kf tree**. Posture is intact on every clause, including the R4-8 successor disposition, which resolves at both ends.

What survives four repair rounds is a **single, newly-exposed shape: the receipts are now sound, but their certification is not.** The round's own instrument — `CLOSE-CERT.md`, the artifact LAW C(4) makes the round's closing condition — carries the stale-stamp class in its own proof (`KF-W10.md` is 21 bytes and six minutes past the table that proves the seat wrote last, **D5-1**), and it never swept this wave's only forward receipts at all: §3 counts five backward receipts, §7's roll-call has no `KF-W9 → KF-W10` row, and R-8 runs the other way (**D5-2**). This check verified all six anchors itself and they hold — so **nothing in the wave's substance moves** — but the wave's W10 seam, the very thing D3-3 convicted and round 3 cured, is currently carried on a warrant that was never executed.

The remaining five are the numeric arm, still leaking at the seams of its own cures: a "five anchors" delegation stated over two different enumerations of six (**D5-3**); the D4-6 cure printing 4 / "3 in this ledger" / "×1" for a token that occurs 3 times (**D5-4**); census 1's widened scope calling four correct docblocks stale prose its own transcript refutes (**D5-5**); §F still naming PASS-3 as the standing verdict every sibling cell re-pointed to PASS-4 (**D5-6**); and a §RECEIPT row claiming twelve gate witnesses were re-executed in an artifact that names five (**D5-7**). Plus one LOW: two banked aliases carried by identity and never spelled (**D5-8**).

**None of the eight touches a row, a gate, a lock, a dissent or the posture.** The wave still measures: zero write grants in the kf tree, zero cures spent, status `planned`, gate ids un-renumbered, §Carry unmoved at 79, every cure-shape lock and all twelve dissents carried. **All eight are repairable inside this spec except D5-1, whose cure belongs to the reconcile seat and whose W9-side remedy is to cite this check's §2 verification instead of a delegation that did not happen.**

*Fresh L-18/L-20 seat, PASS-5, 2026-08-28. Sole write: this file. Nothing here stamps a wave, opens product source, or edits a prior PASS file; keyframes.js was read read-only at `origin/master 81a56990` throughout.*
