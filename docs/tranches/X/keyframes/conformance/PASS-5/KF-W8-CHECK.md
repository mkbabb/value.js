# KF-W8 — FRESH ADVERSARIAL SPEC CHECK (L-18/L-20, pass 5)

**Subject**: `/Users/mkbabb/Programming/value.js/docs/tranches/X/keyframes/waves/KF-W8.md` — 454 L / 195 418 B, mtime **2026-08-28 17:34:06**, repaired **four** times (rounds 1–4) and swept by the round-4 RECONCILE seat, which wrote this file **last** of the eleven.
**Corpus authority**: the **58** `kf-*.md` records at `/Users/mkbabb/Programming/value.js/docs/tranches/V/megatranche/registry/adjudicated/` (`ls kf-*.md | wc -l` → 58). Sole in-tree carry `carry/KF-W6-CARRY.md`, consulted only for the KF.W6 reciprocity axis.
**Census instrument**: an **id-keyed harvest from the 58 records' own bytes** — token sweep, alternate-spelling sweep, by-name sweep, whole-sentence resolution of every routed line, fold-transitivity sweep of every W8-terminal identity across all 58. No prior pass's roster or denominator was opened until this seat's own enumeration was complete; PASS-4's roster was then read **only as a diff target** (§1.4).
**Product tree (TREE LAW)**: `/Users/mkbabb/Programming/keyframes.js`, read-only. `git rev-parse origin/master` → **`81a56990736ced5b5edde0b84c527680ac7689b1`**, re-resolved this seat. Local `HEAD`/`master` = **`8281638c`** (DISQUALIFIED; `git status --porcelain | wc -l` → **252** at this seat too). **Every product measurement below was taken at `origin/master`.**
**Seat posture**: FRESH. Every gate re-executed, every cross-spec receipt re-resolved at the sibling specs' **current** bytes (not at the substrates the spec names), every registry verbatim re-read at the record. PASS-4, RULINGS-4 and CLOSE-CERT were read to learn what was claimed applied — **nothing was inherited**, and three PASS-4/CLOSE-CERT certifications do not survive this seat's re-read.

**Verdict: DEFECTIVE.** The **ID-keyed census is CLEAN** — **32 routed / 32 booked / 0 escaped**, re-enumerated by record, id-for-id identical to PASS-4's independent derivation. **All fifteen gates reproduce their stated frontier numbers**, every round-4 quotation cure (KF-AV-9's `library-side`, the C-22 parenthesis, the DH-3 `by shared prefix` tail, G8's `transport/index.ts:3`) verifies **byte-exact**, the DH-2 phantom is genuinely dead, G7's predicate is unified and commanded at leg (iv), and both LAW-B closure sentences PASS-4 convicted are struck. What fails is the same class one generation on: **§Bounds still does not bound the wave's own acts** — round 4 cured the three `AnimationVisualizer` rows it was told to and left **G3's seven repoints, G14's projector, and R-1's four MUST-CARRY edits** with no lawful landing surface, two of them made unlawful **by round 4's own D-8 narrowing**. **G7 was made an inbound-dependency gate on a wave this spec does not sequence itself after.** **§State's four commanded figures were falsified by this round's own edits, for the third consecutive round.** **5 HIGH · 6 MEDIUM · 3 LOW.**

---

## 1 · ID-KEYED CENSUS — enumerated BY RECORD, nothing inherited

### 1.1 Method

1. **Token sweep**: `grep -c 'KF\.W8' kf-*.md` over all 58 → **47** hit lines across **18** records (`grep -l` → 18). All 47 read whole and classified: routing-law boilerplate (4) · closing-verdict summary (11) · row-terminal routing (32).
2. **Alternate-spelling sweep**: `grep -nE '\bW8\b|W\.8|[Ww]ave[ -]8' kf-*.md | grep -v 'KF\.W8'` → routing-law taxonomy lines only (14 records) plus the single non-KF token `L.W8 ED-3` (`kf-OrbitalDrag.md:82`, a **value.js** tranche-ledger cite inside OD-34 — NO-WAVE-OWNER; the same token recurs inside the routed `KF-CE-42`). Closing-summary `W8` mentions (`kf-AnimationVisualizer:147` · `kf-CopyButton:142` · `kf-HeroAurora:129` · `kf-KeyframeCard:157`) each re-opened: **fold-summaries, zero new ids.**
3. **By-name sweep**: `grep -niE 'structure (&|and) colocation|colocation settle|structure-colocation|structure/colocation'` with `KF.W8` lines removed → **boilerplate only**. No record routes by name without the token.
4. **Whole-sentence resolution**: every routed line read to its terminal period — merged groups (`L-19/C-14`), fold clauses (`KF-KC-45` → KAD-24; `SPF-22` ≡ KAD-24; `C-13` ≡ KF-AV-8/-9/-18; `m-15`/`L-8` = banked N-11; `KF-AV-35` folds into KF-AV-8/KF-CE-12), split dispositions (`C-8` = KF.W5 surface + KF.W8 colocation, `kf-KeyframesStringControls.md:81`; `C-12` = KF.W8 + KF.W4 depcruise arm), rider restatements (`kf-HeroAurora.md:27` limb (d) · `:120`; `kf-AnimatedText.md:137` *"**KF.W8** the colocation carry"* — all three restate KF-AT-26(c), no new id).
5. **Fold-transitivity**: every W8-terminal identity `grep -ln`'d across all 58; every off-record occurrence opened (§1.3).

### 1.2 The routed roster — 32 ids, per record

| record | ids routed → KF.W8 | n |
|---|---|--:|
| kf-AnimationVisualizer | KF-AV-8 (`:49`) · KF-AV-9 (`:50`) · KF-AV-18 W8 arm (`:62`) · KF-AV-24 (`:68`) · KF-AV-26 (`:70`) · KF-AV-35 (`:82`) | 6 |
| kf-CSSCodeEditor | KF-CE-12 (`:46`) · KF-CE-42 (`:80`) | 2 |
| kf-KeyframeTimeline | L-19/C-14 (`:77`) · C-12 (`:78`) · N-11 (`:101`) | 3 |
| kf-KeyframesAddDialog | KAD-24 (`:77`) · the zero-coverage fold → the twin's C-10 (`:89`) · L-17 colocation (`:92`) | 3 |
| kf-TimelineCaret | MISS-β2 (`:46`) · m-7 (`:59`) · L-8 (`:81`) | 3 |
| kf-KeyframeCard | KF-KC-42 (`:87`, rides the R-7 consolidation) · KF-KC-45 (`:90`) | 2 |
| kf-RibbonBar | M-4 (`:66`/`:132`) · m-4 (`:132`) | 2 |
| kf-AnimatedText | KF-AT-26(c) (`:71`; restated `:137`, `kf-HeroAurora.md:27`(d)/`:120`) | 1 |
| kf-HeroAurora | KF-HA-14 (`:56`) | 1 |
| kf-CSSPasteDialog | C-10 (`:75`) | 1 |
| kf-CopyButton | KF-CB-37 (`:85`) | 1 |
| kf-KeyboardShortcutsModal | R-10 (`:46`, restated `:127`) | 1 |
| kf-KeyframeCardList | KC-30 (`:66`) | 1 |
| kf-KeyframesStringControls | C-8 (`:81`) | 1 |
| kf-KeyframesEditor | KF-KE-15 (`:57`) | 1 |
| kf-PlaybackRibbon | C-13 (`:154`) | 1 |
| kf-SpringPhysicsFacet | SPF-22 (`:62`) | 1 |
| kf-TimelineTrack | m-15 (`:104`) | 1 |
| **TOTAL** | | **32** |

**Booked, id-for-id — 32 of 32.** `grep -c <id> KF-W8.md` returns ≥1 for every one (KF-CE-12 7 · KF-CE-42 2 · KF-AV-8 10 · KF-AV-9 8 · KF-AV-18 8 · KF-AV-24 6 · KF-AV-26 7 · KF-AV-35 2 · C-10 2 · KF-HA-14 5 · KF-AT-26 8 · L-19/C-14 1 · C-12 5 · N-11 8 · MISS-β2 5 · m-7 6 · L-8 3 · m-15 3 · R-10 11 · KAD-24 5 · L-17 5 · KF-KC-42 2 · KF-KC-45 2 · KC-30 6 · KF-KE-15 3 · C-8 3 · C-13 2 · M-4 6 · m-4 4 · KF-CB-37 8 · SPF-22 3 · C-3 14). The three census-derived rows and the three booked inbound edges (R-19c ii–iv) are present; the fourth inbound edge is declined with its owner named.

### 1.3 Escapes — ZERO

No id anywhere in the 58-record corpus carries a terminal disposition to KF.W8 and is absent from `KF-W8.md`. Escapes are closed **by construction**: any record routing a row to this wave carries the token (sweep 1), the name (sweep 3) or an alternate spelling (sweep 2), and all three sweeps are exhausted above.

**Fold-transitivity, re-run whole — and the figure the spec carries is short (→ D-6).** Off-record occurrences of the distinctive W8-terminal identities, by command:

| id (home) | off-record records | off-record lines |
|---|---|--:|
| `KF-CE-12` (kf-CSSCodeEditor) | kf-AnimationVisualizer · kf-KeyframeTimeline · kf-KeyframesEditor · kf-PlaybackRibbon · kf-TimelineHoverPreview | 13 |
| `N-11` (kf-KeyframeTimeline) | kf-ChannelOptions · kf-SequencePlayhead · kf-TimelineCaret · kf-TimelineTrack | 8 |
| `KF-AV-8` (kf-AnimationVisualizer) | kf-PlaybackRibbon | 6 |
| `KAD-24` (kf-KeyframesAddDialog) | kf-SpringPhysicsFacet | 3 |
| `KC-30` (kf-KeyframeCardList) | kf-KeyframeCard | 3 |
| `KF-CE-42` (kf-CSSCodeEditor) | kf-KeyframesStringControls | 1 |

**Thirteen records, ≥34 lines** — not the **five** the spec prints. **All resolve as non-escapes** on this seat's own read: the on-roster routings (`C-13`, `C-8`, `L-8`, `m-15`, `SPF-22`, `KF-KE-15`, `C-12`) are the fold clauses already booked; the genuinely-independent five PASS-4 named (`KF-CE-12` @ `kf-TimelineHoverPreview:112` the negative comparator · `KC-30` @ `kf-KeyframeCard:75` = a **different** id `KF-KC-30`, NO-WAVE-OWNER · `KAD-24` @ `kf-SpringPhysicsFacet:125` a provenance note · `N-11` @ `kf-SequencePlayhead:83` a different N-11 carrying a **KF.W0 fold** · `N-11` @ `kf-ChannelOptions:88` the substring inside `L·N-11/N-12`) re-verified as non-escapes here; the remainder are cross-record confirmations inside records already routed. **The verdict `0 escaped` is independently reconfirmed; the enumeration the spec cites for it is not.**

### 1.4 Diff against PASS-4

Roster: **identical**, id-for-id — a third independent derivation agrees. Two PASS-4 statements do not survive: (i) §1.3's *"five off-record occurrences … exist"* (→ **D-6**, and the spec now carries the figure three times); (ii) §1.5's certification of the four §State figures, true at PASS-4's clock and **falsified by the round-4 write** (→ **D-5**).

### 1.5 §State's four commanded figures — TWO NO LONGER REPRODUCE

`ls kf-*.md | wc -l` → **58** ✓ · `grep -l 'KF\.W8' kf-*.md | wc -l` → **18** ✓ · `grep -oE 'kf-[A-Za-z0-9]+\.md' KF-W8.md | sort -u | wc -l` → **15** ✗ (cell says **13**) · union of routed-source ∪ named-by-path → **26** ✗ (cell says **24**); the named-not-routed set is **8**, not the 6 printed. See **D-5**.

---

## 2 · ANCHOR + SUBJECT-IDENTITY — every citation re-resolved at the siblings' CURRENT bytes

Sibling mtimes at this seat: `KF-W0` 16:58:59 · `KF-W3` 16:56:52 · `KF-W6` 16:59:25 · `KF-W4` 16:59:44 · `KF-W2` **17:30:52** · `KF-W1` 17:32:20 · `KF-W9` 17:32:08 · `KF-W5` **17:32:57** · `KF-W7` **17:33:23** · `KF-W8` 17:34:06 · `KF-W10` **17:38:21**.

| receipt as written in KF-W8 | resolves? | subject? | this seat's re-derivation |
|---|---|---|---|
| `KF-W5 §Carry · Arm D · row D-6` (substrate stamped 16:55:55 "final stage-1") | ✅ | ✅ | `grep -n '^\| \*\*D-6' KF-W5.md` → **`:364`**, still the stutter row. **Substrate stamp stale — W5's final bytes are 17:32:57 (→ D-7)**; the coordinate survived anyway |
| `KF-W5 §Sequencing · "→ KF.W8" cross-edge` legs (a)(b)(c)(d) | ✅ | ✅ | `grep -n 'Structure & Colocation' KF-W5.md` → **`:430`**; leg (c) quotation byte-exact; leg (d) present |
| `KF-W5 §Carry · Arm B · row B-16` / `OP-4 · LOCUS DECLARED` | ✅ | ✅ | `grep -n 'OP-4 · LOCUS DECLARED' KF-W5.md` → **`:339`** |
| `KF-W6 §Sequencing · "→ KF.W8"` (KF-CB-37, MOVE-vs-RESHELL GUARD) | ✅ | ✅ | `grep -c 'KF-CB-37' KF-W6.md` → **1**, at **`:514`**; guard verbatim, and the added *"No W6 commit carries the move"* clause present |
| `KF-W6 §Bounds` barrel row / EditorHeader row | ✅ | ✅ | `grep -n 'shell/index\.ts' KF-W6.md` → `:70` (sole-write-owner row) · EditorHeader delete at **`:67`** |
| `KF-W6 §Carry` KF-APP-41 adoption + `THE ACT: ONE ATOMIC COMMIT` | ✅ | ✅ | `grep -n 'KF-APP-41' KF-W6.md` → **TEN** hits (`:67 :414 :418 :419 :461 :484 :502 :516 :540 :572`) — **the RECONCILE seat's §R-4 correction reproduces exactly**; both quotations byte-exact at `:414`/`:416` |
| `KF-W6 §Carry · W6-C · PROSE-WITH-CODE` · `W6-I` honest-scoping | ✅ | ✅ | `:222` · `:370` (*"graph width, NOT crash exposure; consistency, not bundle size"* byte-exact) |
| `KF-W7 §Bounds` `package.json` row (DH-2, quote-by-command) | ✅ | ✅ | `grep -c 'devDependencies only' KF-W7.md` → **0** (phantom confirmed dead); `:61` carries the quoted row **byte-identical** at W7's **current** 17:33:23 bytes |
| `KF-W7 §Bounds` shared-directory declaration (DH-3) | ✅ | ✅ | `grep -n 'never globbed' KF-W7.md` → **`:68`**, *"…never globbed **by shared prefix**"* — the D-13 truncation cure holds |
| `KF-W7 §Carry · P0` SpringTrace C-3 route dissent | ✅ | ✅ | `KF-W7.md:123` — *"route-by-mechanism: a value.js LIBRARY export-surface decision…"* **byte-exact** |
| `KF-W4 §Bounds` `vitest.config.ts` row · item 3 · commit 2 · the → KF.W8 reciprocal | ✅ | ✅ | present; W4 unmoved since 16:59:44 |
| `KF-W3` title heading · `package.json (:70 only)` row | ✅ | ✅ | `sed -n '1p'` → *"# KF.W3 — Parser Consumption (GATED, never scheduled)"*; the row at **`:77`** |
| `KF-W2 §Carry · F4` §4.4 lock · `§0 · OP-6` | ✅ | ✅ | `grep -c 'KF.W8 precedes' KF-W2.md` → **4**; `grep -n 'ORDERED: KF.W8 precedes'` → **`:59`** — both survive W2's 17:30:52 write. **Substrate stamp 17:02:55 stale (→ D-7)** |
| `KF-W10` FOLD-FORWARD `KF-W8-R-4-STRUCT-PAIR` + 3 triggers · `§6.C-0` · **`§6.D`** | ✅ | ✅ | all anchor-only and all resolve at W10's **current** 17:38:21 bytes: `:98`/`:213` (terminal verb + triggers) · `:232` · `:494` (§6.C-0) · **`:543` §6.D SUCCESSOR-FORMATION REGISTER**. `grep -oE 'KF-W10\.md:[0-9]+' KF-W8.md` → **∅** ✓ |
| precepts `README.md:127-130` / `:132-135`; census `CENSUS-2026-08-03.md` | ✅ | ✅ | resolve as cited |
| `R1-05-demo-colocation.md:118` | ✅ as declared | — | not in the value.js tree; the spec says so and says *"cite, never re-mint"*. Correct posture |

**M-25 depth — every round-4 quotation cure re-read at the bank, byte-diffed.** **All four verify byte-exact**: KF-AV-9's bounding note *"the eviction API is global-by-construction (**library-side — no scoped bump exists**) and the wire mirrors…"* (`kf-AnimationVisualizer.md:50`) ✓ · the same row's *"`:79` is the only wire in demo/ (census 2 hits, both this file)"* ✓ · C-22 whole, with the elision now marked — *"it does NOT ship (rolldown shakes the unused barrel member; **markers absent from every built chunk**), so deletion is source-hygiene, while the orphan `--header-items-max-w: 500px` DOES survive into the shipped cascade"* (`kf-EditorShell.md:95`) ✓ · MISS-β2 **DISSENT 1** (`kf-TimelineCaret.md:136`) ✓. SpringTrace **C-3** (`kf-SpringTrace.md:62`) ✓ · KF-AV-28 at both banks ✓ · M-L3, L-16-RESCOPED + RR-2 M4, KF-APP-41 ✓. **The drift class PASS-4 convicted at the quotation seam is DEAD.** What survives is at the *substrate-stamp* layer (D-7), not at the words.

---

## 3 · GATES — re-executed whole at `origin/master 81a56990`

**All fifteen reproduce their stated frontier numbers. Falsifiers re-counted: 15/15.**

| gate | this seat's re-run | |
|---|---|---|
| G1 | cl.1 → **7 statements / 6 files**, anchors byte-identical (`useKeyframeOps:1` · `parseAnimationCSS:7` · `timelineEngine:1`,`:17` · `AnimationVisualizer.vue:45` · `helpers.ts:9` · `keyframeSelector.ts:5`); cl.2 → four greps over the four published entries, four empty results; `export *` → 0 | ✅ |
| G2 | eight symbols over four entries → exit 1; exports map `.` + `./engine`; `export *` → 0 | ✅ |
| G3 | `-- src demo` → exactly **3** (`format/format.ts:20` · `css-text.ts:58` · `keyframeSelector.ts:7`); fourth homonym `test/engine/nan-frame.test.ts:39` real and out of scope; `compile/selector.ts:42` exported, re-exported `compile/index.ts:24` — the *"unpublished"* re-word correct | ⚠ **D-1** (its seven repoints have no host) |
| G4 | five `index.ts`; **all four clause greps exit 1**, all four barrels present; `transport/index.ts` 14 L, 3 async + 1 type export | ✅ |
| G5 | exactly **3** up-imports at `:31`, `:116`, `:229` | ✅ |
| G6 | `demo/components/` non-recursive → `CopyButton.vue` + `instrument` + `playback` → **1** loose `.vue` | ⚠ **D-3** (its four repoints unbounded) |
| G7 | (i) **8** SFCs; (ii) **4** exports (`EditorShell` · `EditorHeader` · `EditorStartScreen` · `SharePopover`); (iii) per-export counts 1 / 0 / 2 / 3 reproduce; **(iv) the new externality leg reproduces exactly** — external = `HeroAurora` (`App.vue:147`), internal-only = `AnimatedText`/`KeyboardShortcutsModal`/`TypingDots` (`EditorStartScreen.vue:62`/`:63`, `EditorShell.vue:119`), zero-importer = `EditorHeader`. **PASS-4 D-3 CURED at the predicate** | ⚠ **D-4** (undeclared cross-wave ordering) |
| G8 | leg (i) → **2**, both lines byte-exact; leg (ii) → raw **5** for the SFC (`:46 :70 :72 :76 :77`) with the semantic conjunct now declared, **6** in the test at `:4/:5/:13/:35/:105/:127`; leg (iii) → **4 files**, and `transport/index.ts` returns **`:3` only** — the D-14 spelling unification holds | ✅ |
| G9 | `git ls-tree -r --name-only origin/master \| grep -c '\.test\.ts$'` → **132** = `-- test` **130** + `-- bench` **2**; no `git ls-files` survives | ✅ |
| G10 | `git show origin/master:vitest.config.ts \| grep -c plugins` → **0**; `@vue/test-utils` absent (**0**); 9 files under `test/demo/instrument`, names matching §Bounds' enumeration exactly | ✅ |
| G11 | `groupedShortcuts` defined `KeyboardShortcutsModal.vue:57`, consumed `:11`; `-- test` → exit 1 | ⚠ **D-11** (`:57` carve vs `:57-67` body) |
| G12 | `-- test` → exit 1 for both dialogs | ✅ |
| G13 | `src/` → one entry ✅ · `basename==dirname` → **exactly the 8** ✅; D-6's twelve resolve 12/0; the lane's 9-live/7-gone and the six-plus-one commit attribution stand | ✅ |
| G14 | **11 lines / 10 files**, host list exact; `useDragScrub` → exit 1, zero | ⚠ **D-2** (destination bounded, source not) |
| G15 | the command returns **exactly the 9 stated lines** (`:5 :7 :8 :13 :59 :69 :108 :113 :138`); leg 1 → 7 convicted, leg 2 → the 2-line pad allowance; 151 L | ✅ |

**LAW A censuses, re-derived from the import graph.** A-1..A-4 → **0 consumers** for all four barrels, by both sweeps ✓ · A-5 → **exactly the four R-1 names**, patch hit correctly non-import ✓ · A-6 **1** ✓ · A-7 **1** ✓ · A-8 **1** ✓ · A-9 → **7**, id-for-id at `useKeyframeOps.ts:9` · `useTimelineBuild.ts:8` · `useTimelineOps.ts:7` · `snapshotCapture.ts:3` · `timelineEngine.ts:16` · `timeline-undo.test.ts:6` · `value4-editor-boundary.test.ts:6` ✓ · A-10 → the round-4 correction reproduces: `compile/emit/css-text`'s library consumers are **FOUR** (`engine/css/metadata.ts:35` · `resolve/browser.ts:4` · `resolve/conditional.ts:2` · `resolve/function.ts:6`) and its test consumers **five** ✓.

**MECHANISM F (RULINGS-4 R4-8.5), run as commanded.** `grep -nE 'KF\.W1[1-9]' waves/*.md` → KF.W11 ×78 · KF.W12 ×55 · KF.W13 ×82, and all three carry register rows at **`KF-W10.md:543` §6.D · SUCCESSOR-FORMATION REGISTER** (`:558`/`:559`/`:560`) with cargo 9+6+2 and the terminal verb `MINTED-UNAUTHORED`. **No mint escapes.** ✓

---

## 4 · POSTURE

| axis | finding |
|---|---|
| **W4 head honored** | ✅ §State opens after KF.W4; G10 is an inbound-dependency cite on W4's commit 2, reciprocated at W4's own → KF.W8 cross-edge; G11/G12 re-gated on the cite; `vitest.config.ts` carved to R-5's `measure` project alone; `package.json` to `+@vue/test-utils` |
| **W3 gated-unscheduled** | ✅ the → KF.W2/W3 cross-edge quotes KF-W3's title verbatim and names **KF.W2** as the schedulable receiver |
| **O-21 (not O-20)** | ✅ vacuous and correctly so — `grep -c 'O-20'` → 0, `grep -c 'O-21'` → 0, and the disclosure is written by description rather than by token so it cannot falsify its own zero |
| **KF-AV-28 present where governed** | ✅ both banked spellings byte-exact; the scope-word-vs-mechanism distinction applied to all seven governed rows, and the three round-4 §Bounds rows explicitly ride it unchanged |
| **W11/W12/W13 disposition (RULINGS-4 R4-8)** | ✅ **present and coherent from this end**: this wave routes the eight out-of-bounds G14 hosts to the scene-repair packets and cites `KF-W10 §6.D` **anchor-only per LAW C(3)**; §6.D exists at W10's current bytes with all three rows |
| **W10's greens conditioned on artifacts that exist** | ✅ at W10's end (§6.D is authored; G-2/OP-5 re-conditioned on the register). ⚠ **but the round's own close certification is falsified at the tree — D-10** |
| **LAW B (no self-voiced closure)** | ✅ for the two sentences PASS-4 convicted — both **STRUCK** and replaced by the lawful cited form. ⚠ two ruling-commanded scope receipts remain in spec voice and **both are short by their own stated scope** (D-8, D-9) |
| **LAW A (import-graph census for every delete/repoint)** | ✅ all ten censuses re-derive; A-10's corrected figure holds |
| **LAW C / LAW D (substrate + verbatim)** | ✅ at the words (every quotation byte-exact). ⚠ **three of eight substrate stamps stale, two labelled "final" — D-7** |
| **§Bounds bounds the wave's acts** | ❌ **D-1 (G3's seven) · D-2 (G14's projector) · D-3 (R-1's four) · D-11 (G11's block)** — the D-1 class of pass 4 recurs at a different row set, and two of its members were made unlawful by round 4's own D-8 narrowing |

---

## 5 · DEFECTS

### D-1 · HIGH — the serializer unification's SEVEN repoints have no lawful landing surface, and round 4's own D-8 cure took two of them away
G3's census paragraph and LAW A's **A-9** both state the act: *"The unification repoints **seven** consumers of `demo/utils/keyframeSelector.ts`,"* and the gate's own text makes it binding — *"A 'one body' landing that leaves any of the seven on a deleted module is a broken tree, not a green gate."* Re-derived at `81a56990`, the seven are:

```
demo/components/instrument/keyframes/composables/useKeyframeOps.ts:9
demo/components/instrument/timeline/composables/useTimelineBuild.ts:8
demo/components/instrument/timeline/composables/useTimelineOps.ts:7
demo/components/instrument/timeline/utils/snapshotCapture.ts:3
demo/components/instrument/timeline/utils/timelineEngine.ts:16
test/demo/instrument/timeline-undo.test.ts:6
test/demo/instrument/value4-editor-boundary.test.ts:6
```

Measured against §Bounds (lines 79–121): **`useTimelineBuild.ts` · `useTimelineOps.ts` · `snapshotCapture.ts` appear in NO §Bounds row** — `grep -c` over the §Bounds block returns **0** for each, and their only occurrences in the whole file are A-9 (`:163`) and G3's census paragraph (`:263`). **`timeline-undo.test.ts` and `value4-editor-boundary.test.ts` are two of the nine files this round narrowed to READ-ONLY** (§Bounds, *"access NARROWED at repair round 4"*). The two bounded consumers are carved away from the import that must move: `useKeyframeOps.ts` is carved at **`:1`** while its keyframeSelector import is at **`:9`**; `timelineEngine.ts` is carved at **`:1`/`:17`** while its import is at **`:16`**. **Five of seven repoints are unlawful and two more are out-of-carve** — the round-4 D-8 narrowing removed the write access two of them needed without re-reading the census that had booked them at round 3.

### D-2 · HIGH — G14's D-9 cure supplied the destination and never bounded the source; the projector still has no writable host
Round 4 granted `demo/composables/useDragScrub.ts | modify-carve` (the seam) and named `AnimationVisualizer.vue` *"the **only** projector host this wave writes … bounded at three carve rows this round (`:70-78` · `:79` · `:122-191`)."* Re-measured this seat:

```
origin/master:demo/components/playback/AnimationVisualizer.vue:101:const progressFromPointerX = (clientX: number): number => {
origin/master:demo/components/playback/AnimationVisualizer.vue:104:    const rect = track.getBoundingClientRect();
origin/master:demo/components/playback/AnimationVisualizer.vue:199:        const ballRect = ball.getBoundingClientRect();
origin/master:demo/components/playback/AnimationVisualizer.vue:210:        const p = progressFromPointerX(e.clientX);
origin/master:demo/components/playback/AnimationVisualizer.vue:215:        const p = progressFromPointerX(e.clientX);
```

**KF-AV-24's subject is `progressFromPointerX` at `:101-106`; its call sites are `:210`/`:215`; the file's two rect reads are `:104` and `:199`. NONE of the five lies inside ANY of the four AV carve rows** (`:45` · `:70-78` · `:79` · `:122-191`; the spec itself fixes the third row's close at `:191` and opens *"Drag capture"* at `:193`). The consolidation the falsifier demands (*"moving a bare rect read into the seam … leaves the projector count unchanged and fails"*) requires deleting `:101-106` and repointing `:210`/`:215` — all out of bounds. The cure moved the wall, not the door: **a destination was granted, the source was not**, and the gate's green remains unreachable inside its own bounds. This is the identical shape as the PASS-4 D-9 it was written to cure, and the spec's own §Bounds precision rule (*"the block ends at `:78`; `:79` is the wire and is the NEXT row's subject"*) is what makes the carve strict.

### D-3 · HIGH — R-1's four MUST-CARRY edits are booked at §Sequencing and bounded nowhere
R-1 rules, verbatim: *"KF.W8 **unit f** executes the bare move … **with the four import repoints** (`instrument/keyframes/KeyframeCard.vue:59` · `KeyframesEditor.vue:116` · `scenes/easing/EasingTarget.vue:143` · `scenes/spring/StartingStyleTarget.vue:87`) and **the `useToolbarKeyboard.ts` docblock at `:25-28`** in the **same commit** (S-2's roving-tabindex MUST-CARRY)."* Over the §Bounds block, `grep -c` returns **0** for `useToolbarKeyboard`, **0** for `EasingTarget`, **0** for `StartingStyleTarget` and **0** for `KeyframesEditor`. Only `KeyframeCard.vue` appears, and it appears as a **`move`** row, not as a carve at `:59`. The docblock is real at the frontier — `git show origin/master:demo/components/instrument/keyframes/composables/useToolbarKeyboard.ts | sed -n '25p'` → *" \* DialogTrigger, CopyButton, the Apply-CSS button — with no per-item"* — and it is the one edit S-2 makes a MUST-CARRY. G6 is the **acting** gate and its falsifier binds the landing to *"R-1's destination law"*; four of the five edits that landing requires are outside every grant. R4-10 changed the sweep's mechanism to **by SCOPE**, but the two scope receipts written this round enumerate the **access column** and the **symbol column** — never the acts booked at §Sequencing and §Rows against §Bounds coverage, which is where this class actually lives.

### D-4 · HIGH — G7 was made an inbound-dependency gate on KF.W6 while KF.W6 is not a declared predecessor of this wave
R4-5(c) re-cut G7 *"on the R3-8/G10 template"*, and GREEN now reads: *"after **(1)** KF.W6's R4-1 atomic commit lands … and **(2)** this wave's own booked HeroAurora move lands …"*. The G10 template has two halves, and only one was applied. **§State's `Opens after` names KF.W0 · KF.W4 · KF.W5 — not KF.W6** (`sed -n '32p'`), and the → KF.W6 cross-edge (`:410`) declares routing and the R-1 guard only; it contains no ordering word. From the other end, `KF-W6.md:514` supplies the *facts* G7 turns on and states the write-order of the **repair round** (*"Anchor-only per LAW C(3) — that seat writes later this round"*), never the **execution** order of the waves. Compare G10, whose W4 dependency is declared **three** ways (§State Opens-after · §Sequencing *"W4 lands first by this wave's §State"* · W4's own reciprocal). A gate whose green is conditioned on an event with no declared ordering is either a stuck gate or an unpoliced race — the same hole R3-8.1 was written to close for W4.

### D-5 · HIGH — §State's commanded figures were falsified by this round's own edits, for the third consecutive round
The AUDITED cell prints *"the spec names **13** records by path (`grep -oE 'kf-[A-Za-z0-9]+\.md' KF-W8.md | sort -u | wc -l` → **13**)"* and *"the union … is **24**"* with the **6** named-not-routed enumerated. Re-run at the file's current bytes:

```
$ grep -oE 'kf-[A-Za-z0-9]+\.md' KF-W8.md | sort -u | wc -l
15
$ union(routed 18, named-by-path 15) = 26
```

The two new names are **`kf-ChannelOptions.md`** and **`kf-SequencePlayhead.md`**, both introduced **by this round's own D-15 citation re-point** (§State, §Rows and §Excluded each name the two `N-11` collisions by path). The named-not-routed set is therefore **8**, not 6. This is the cell that was re-cut at round 2 (its `21` struck), re-cut again at round 3 (its `10` struck, `12` recorded at open and `13` stated *"measured after the round-3 edits"*), and that closes with the sentence *"**A cell that states its figure at open and is written at close would be false at close**, which is the exact defect this cell has now been re-cut for twice."* It is false at close a third time, and the same round's cure is what falsified it.

### D-6 · MEDIUM — the "FIVE off-record occurrences" figure carried three times does not reproduce
§State, §Rows and §Excluded each state that the PASS-4 artifact *"closed the fold-transitivity sweep over **FIVE** off-record occurrences."* Re-run whole (§1.3): the distinctive W8-terminal identities occur off-record in **13 records across ≥34 lines** — `KF-CE-12` alone in **five** records (13 lines), `N-11` in **four** (8 lines), `KF-AV-8` in one (6 lines). PASS-4's own convictions read *"its fold-transitivity sweep reports 'three off-record occurrences'; there are **five**"* — the identical class, one generation on, now load-bearing inside the spec's LAW-B citation. **The verdict is unmoved**: all thirty-four resolve as non-escapes and `0 escaped` is reconfirmed here independently. What fails is that a spec cites an artifact *for* an enumeration and reprints a number that enumeration does not support.

### D-7 · MEDIUM — three of the eight substrate stamps in the LAW C(2) ledger are stale, two labelled "final", in the file the RECONCILE seat wrote last
The CROSS-WAVE RECEIPT LEDGER exists so the reconcile seat *"can re-verify every receipt this file wrote without re-deriving the map."* Measured at the tree: `KF-W5.md` is stamped **16:55:55**, labelled *"1 (final)"* — its actual final round-4 bytes are **17:32:57**; `KF-W2.md` is stamped **17:02:55**, labelled *"1 (final)"* — actual **17:30:52**; `KF-W7.md` is stamped **15:39:04** and its reconcile insertion corrects to **17:17:02** — actual **17:33:23**. All three sibling files were rewritten by the RECONCILE seat, **which then wrote this file at 17:34:06 and updated the ledger's rows for none of them**. Every quotation riding those stamps still verifies (§2), so nothing substantive moved; but the manifest built expressly to survive same-round drift is the one artifact that did not get re-run, and two of its rows now assert "final" of bytes that are not.

### D-8 · MEDIUM — the ACCESS-COLUMN scope receipt is short by one row and its path arithmetic does not reproduce
The receipt states, in the spec's own voice: *"**access column: 8 rows / 11 paths carrying `delete-or-gate` · `move` · repoint; 10 censuses present (A-1 … A-10), one per path-family; 0 owed.**"* Re-grepped by scope: the enumerated classes carry **14 paths**, not 11 — 4 barrels (A-1..A-4) + 4 moves (A-5..A-8) + the **4-path** deep-import row + `helpers.ts` + `keyframeSelector.ts`; the 8-row figure only reconciles with 11 if the deep-import row counts as one path in the second column and as four in the census column, a mixed rule the receipt does not state. Worse, the receipt's own escape clause — *"**Two rows changed access this round and neither owes a census**: `shell/index.ts` is STRUCK … and the three new `AnimationVisualizer.vue` rows are …"* — **omits `demo/composables/useDragScrub.ts`**, the round's other new write grant, whose consolidation act repoints `AnimationVisualizer.vue:210`/`:215` into the seam. The receipt invites exactly this test (*"A pass-5 seat tests this by SCOPE: re-grep the access column, re-count"*); re-grepped, it is short.

### D-9 · MEDIUM — the SUBJECT-IDENTITY scope receipt is short by its own stated scope
The receipt declares its enumeration source as *"**every symbol-bearing §Bounds row and every gate-witness symbol, whole file**"* and certifies *"Fourteen subjects enumerated … **14 present, 0 absent**."* The §Bounds deep-import row — the wave's core act and G1's whole subject — is symbol-bearing at **five** sites and **none of the five is among the fourteen**: `reverseCSSTime` (`useKeyframeOps.ts:1`) · `serializeTimingFunction` (`parseAnimationCSS.ts:7`) · `camelCaseToHyphen` (`timelineEngine.ts:1`) · `serializeCssValue` (`timelineEngine.ts:17`) · `bumpLayoutEpoch` (`AnimationVisualizer.vue:45`). All five resolve at the frontier, so nothing is wrong at the tree; what is wrong is that a receipt written under R4-10's *"test BY SCOPE, not by instance"* rule enumerates a subset of its declared scope and closes over it in the spec's own voice.

### D-10 · MEDIUM — the round-4 CLOSE-CERT's "proof this seat wrote last" is falsified at the tree
`PASS-4/CLOSE-CERT.md` (mtime **17:37:59**) tables `KF-W10.md` at **17:31:54 / 209 902 B** and certifies at §9(1) *"It ran **AFTER the last per-wave write** … nine-minute margin."* Measured this seat: `KF-W10.md` is **17:38:21 / 209 923 B** — **written after the certification**, 21 bytes larger. LAW C(4)(i)'s mtime table and §9 items 1–2 therefore do not hold at the bytes, and RULINGS-4 §END makes that certification the round's close condition (*"Without it the round did not close"*) and orders the pass-5 seat to audit it first. The substance is intact — every W10 anchor this spec rides resolves at the **current** W10 bytes (§2), and §6.D exists — so no receipt of KF-W8's is harmed; the defect is that the instrument certifying the round's write order is itself out of date about the last write.

### D-11 · MEDIUM — G11's §Bounds carve is one line against an eleven-line body
§Bounds grants `demo/components/instrument/shell/KeyboardShortcutsModal.vue` (**`:57`**) `modify-carve`. G11's falsifier requires *"the inline body **gone**"*. Measured at the frontier, the body is `:57-67`:

```
57: const groupedShortcuts = computed(() => {
…
67: });
```

A one-line carve cannot delete an eleven-line block, and this file has just been convicted (PASS-4 D-1, cured at R4-5(a)) for exactly the coordinate-vs-subject mismatch — `:45` the import versus `:79` the wire. The subject-identity sweep lists `KeyboardShortcutsModal.vue:57 const groupedShortcuts` as **present**, which is true of the declaration and silent about the extent the gate's falsifier binds.

### D-12 · LOW — the DH-3 four-party declaration still states this wave's claim in the access word round 4 struck
The row's access column now reads **`READ-ONLY` (the nine) — *access NARROWED at repair round 4*** with a full paragraph explaining why the round-3 `modify-carve` grant is struck. Twelve lines later, in the **same cell**, the four-party declaration still reads *"**KF.W8** (here) — the 9 tracked as **modify-carve**, of which `resize-tracks.test.ts` is the one named cure site, **+ 4 creates**."* A both-ends declaration whose entire purpose is to state each claimant's claim accurately now states this claimant's claim in the word the same cell struck — the class the file convicts by name at DH-2 (*"a phantom verbatim inside the declaration whose entire purpose is to state each claimant's claim accurately"*).

### D-13 · LOW — G14's limb 2 asserts a bounding relation that does not hold
*"`AnimationVisualizer.vue` carries **2 of the 11** measured lines **and is bounded at three carve rows this round** (`:70-78` · `:79` · `:122-191`, R4-5(a)). It is the **only** projector host this wave writes."* The two measured lines are `:104` and `:199`; neither is inside any of the three rows. The conjunction reads as though the bounding covers the measured lines, and it does not — this is D-2's premise, recorded separately because the sentence would remain false even if D-2 were cured by a fourth carve row.

### D-14 · LOW — `KeyframeCard.vue:59` is carried as one of R-1's four repoints and bounded only as a `move`
A-5's four consumer names include `instrument/keyframes/KeyframeCard.vue:59`. The file's only §Bounds row is `demo/components/instrument/keyframes/KeyframeCard.vue → keyframes/components/ | move | KC-30`. The CopyButton repoint at `:59` is a byte edit inside a file whose grant is a relocation for a different id, and the two acts land in **different commits** by R-1's own *"the two acts never share a commit"* discipline elsewhere. Enumerated so that the fourth repoint is not read as covered by a move row that never names it.

### D-15 · LOW — the L-18 blanket's "commands 13/15" no longer describes the suite it counts
The round-4 restatement keeps **13/15** with **G13** and **G14** as the two exceptions, then declares G7's conjunct **cured** by supplying leg (iv) — a literal command. G7 was never one of the two exceptions, so the figure is unchanged for a suite that gained a command; and G14's honest-scoped GREEN now binds to *"unit a's enumeration"* plus a §Bounds grant, which is a third shape again. The number is defensible under one reading and undefended under the text as written — the same *"describes a suite the file does not have"* diagnosis the blanket has now carried through three restatements.

---

## 6 · WHAT HOLDS

Recorded so no later seat re-litigates settled ground. **The census is CLEAN by record** — 32 routed / 32 booked / **0 escaped**, a third independent derivation agreeing id-for-id with PASS-3 and PASS-4. **All fifteen gates reproduce at the frontier**, falsifiers 15/15, and every PASS-4 gate conviction is genuinely cured at the bytes: **G7's predicate is unified as EXPORTS-WITH-EXTERNAL-IMPORTER, `external` is defined, and leg (iv) is commanded and reproduces exactly**; **G8's `transport/index.ts` spelling is unified at `:3`** and leg (ii)'s semantic conjunct is declared rather than counted; **A-10's library figure is 4 and re-runs**; **the DH-2 phantom is dead and the replacement quote-by-command verifies byte-exact at KF-W7's post-reconcile bytes**; **the DH-3 truncation is completed with `by shared prefix` restored**; **the KF-AV-9 quotation is whole with `library-side` restored and the wire re-anchored to `:79`**; **the C-22 parenthesis elision is marked**; **both spec-voiced closure sentences are struck** and replaced by the lawful cited form. **LAW D's verbatim arm produced ZERO convictions this pass** — every one of the eleven registry and sibling quotations byte-diffed here is exact, and the drift class that dominated passes 3 and 4 is dead at the words. **MECHANISM F runs clean**: KF.W11/W12/W13 all carry §6.D register rows; no mint escapes. **The barrel double-home is resolved at both ends** — one writer (KF.W6), one measurer (G7), each spec naming the other's claim, this file's own barrel row struck. **The residue is bounds and arithmetic, not measurement**: the file measures the tree accurately and still does not bound four of the acts it has decided to perform — and this round's own two cures (the `test/demo/instrument/` narrowing, the D-15 citation re-point) are what created two of the five HIGHs.

---

*Instruments (all read-only, zero writes to product source): `git rev-parse` · `git grep` · `git ls-tree` · `git show` · `git log` · `git status --porcelain` at `/Users/mkbabb/Programming/keyframes.js`; `grep`/`sed`/`awk`/`sort`/`uniq`/`stat` over the value.js registry, conformance, carry and wave trees. Frontier ref `origin/master 81a56990736ced5b5edde0b84c527680ac7689b1`, re-resolved at the head of this pass; local `8281638c` opened only to confirm its disqualification (252 dirty rows). No product source was opened for writing; no registry record, no wave spec and no prior-pass artifact was edited — the only file written by this seat is this register.*
