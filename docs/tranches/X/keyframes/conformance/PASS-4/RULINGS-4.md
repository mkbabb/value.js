# X·KF REPAIR ROUND 4 — CROSS-WAVE RULINGS

**Seat**: cross-wave rulings, repair round 4 · 2026-08-28
**Authority**: `PASS-4/UNION.md` (read whole) · the 11 PASS-4 per-wave check files (all read whole) · `PASS-3/RULINGS-3.md` + the standing laws of rounds 1–3 (binding except where amended here) · the 11 wave specs at `docs/tranches/X/keyframes/waves/` · the 58-record corpus at `docs/tranches/V/megatranche/registry/adjudicated/` · READ-ONLY probes of keyframes.js at **`origin/master 81a56990736ced5b5edde0b84c527680ac7689b1`** (re-verified this seat: `git rev-parse origin/master` exact; local `8281638c` remains **DISQUALIFIED** — a named historical measurement context only, per R3-2).
**Pass-4 verdict being repaired**: DEFECTIVE 11/11 · 116 defects · **1 BLOCKER · 1 CRITICAL · 6 HIGH** · 3 fresh hard escapes (S-1's fifth firing, incl. the program-structural KF.W11/W12/W13 wave-set omission) · 1 fresh double-homing (the `shell/` barrel) · RE-ANCHOR certification ABSENT · R3-11(e) partial at seven seats. **The head is MONOTONE for the first time (11 → 8)**; all eleven pass-3 top-tier defects verified CLOSED; carriage 98.2%.
**Law** (unchanged, restated): every cross-wave defect gets exactly ONE owning cure a single wave can apply; per-wave repair seats execute these rulings verbatim; E-3 everywhere (specs are `planned`, repaired in place; registry corrections land as dated addenda under original ids, never edits); nothing here authorizes execution or opens product source. LAW A and LAW B remain in force program-wide. **Every frontier fact cited in a case ruling below was re-derived by this seat at `81a56990` before it was written**; the census receipts are pasted where the cures land.

**13 rulings** (2 laws + 11 cases). Ruling → per-wave directive map at §END.

---

## LAW C · THE TWO-STAGE / RECONCILE LAW — same-round sibling drift is killed by write order, quote-by-command, and a mandatory close certification

**Why now**: U4-C is the modal cross-wave failure of pass 4 — every spec rewritten inside one 14-minute window, FIVE waves shipping receipts falsified by same-round sibling writes (W10's seven-minutes-stale roster + half-run seam table · W2's struck-sentence-quoted-as-verified · W8's six dead KF-W5 coordinates + the DH-2 phantom · W5's stale KF-W6 coordinates · W1's falsified sibling-zeros). The R2-7 re-anchor seat ran mid-window and certified nothing (U4-A) — the third consecutive round the class survived its instrument.

**Law, binding on repair round 4 and every subsequent repair round**:

1. **Two stages, ordered.** STAGE 1 = **KF-W0 first** (it is the roster source three siblings consume), then KF-W1..KF-W6 in any order. STAGE 2 = KF-W7, KF-W8, KF-W9, then **KF-W10 LAST** of the per-wave seats. A stage-2 seat quotes stage-1 siblings only at their **FINAL stage-1 bytes**.
2. **Quote-by-command.** Every same-round cross-wave receipt names its substrate — the sibling file and its mtime (or commit) at quote time — and every quoted sibling sentence rides beside the pasted command that produces it (`grep -n '<fragment>' <sibling>.md` → the line). A receipt without its command is not a receipt.
3. **Forward references are anchor-only.** A receipt whose source wave writes LATER in the stage order carries the stable anchor alone (§-heading + row/gate id — no quoted prose, no line numbers); the Reconcile seat verifies it after the last write.
4. **The RECONCILE seat** (successor of R2-7's re-anchor seat) runs **AFTER the last per-wave write**, re-verifies **EVERY cross-wave receipt written this round** (both stages, both directions), and **WRITES `docs/tranches/X/keyframes/conformance/PASS-4/CLOSE-CERT.md`**: (i) the mtime table of all 11 specs proving it wrote last; (ii) the receipt-by-receipt verification, commands pasted; (iii) a LAW-B-form certification citing this file and the PASS-4 checks by path and date. Its round-3 absence is the convicted defect **U4-A**. **In round 4 the certification's absence means the round DID NOT CLOSE** — no repair is bankable without it — and the pass-5 union seat audits `CLOSE-CERT.md` against the bytes as its first act.

---

## LAW D · THE VERBATIM LAW — "verbatim" is a checkable claim; a figure beside a pasted command is the command's output

**Why now**: U4-E — six MAJOR-band members this pass: quotations labelled *verbatim* that are paraphrases, truncations, or phantoms (W7's P-2 lock attributed to the wrong record + the MISS-β3 elision · W8's DH-2 phantom + the KF-AV-9 reduction + the DH-3 truncation · W2's struck-sentence-as-verified). Zero fabricated registry citations for the fourth pass running — the quotation seam is where invention now leaks in.

**Law, binding program-wide**:

1. A quotation labelled **verbatim** is **byte-diffed against its source at write time** — the quote is produced by a pasted command (`grep -n` / `sed -n`) or the diff is pasted beside it. Anything else is labelled a **paraphrase** with elisions marked by ellipsis and altered words disclosed.
2. A quotation of a **sibling spec** additionally satisfies LAW C(2) — the substrate is named, because siblings move inside a round.
3. **Numeric arm**: every figure printed beside a pasted command is produced by **re-running the command at write time**. A count asserted over an enumeration is stated AT the enumeration with its counting rule (the S-10.1 pattern); dependent cells point at the enumeration, never re-issue the numeral.

Convicted members, all cured this round at their seats: KF-W7 D3/D6 · KF-W8 #2/D-5/D-12/D-13 · KF-W2 D-1/D-2 · KF-W10 D-7 · KF-W9 D4-3 (the numeric arm's head case).

---

## R4-1 · BLOCKER (KF-W6) — the EditorHeader.vue delete: the LAW-A census run by THIS seat; the NO-WAVE-OWNER row HOMED at KF.W6 by declared adoption FIRST; the delete re-granted as one atomic act

**LAW A census, executed by this seat at `81a56990`** (commands + outputs, the census of record):

```
$ git grep -n 'EditorHeader' origin/master -- demo/ test/ src/ scripts/
demo/components/instrument/shell/index.ts:2:export { default as EditorHeader } from "./EditorHeader.vue";
demo/styles/layout.css:15:    --header-items-max-w: 500px; /* the EditorHeader expanded items-wrapper cap … */
```

- **Specifier census**: exactly **ONE live import** — `demo/components/instrument/shell/index.ts:2`, the barrel re-export. The `layout.css:15` hit is prose/token context (the orphan custom property), not an import.
- **Symbol census** (consumers of the barrel's `EditorHeader` named export): **ZERO**. `App.vue:143` imports `{ EditorShell, EditorStartScreen }`; `MbabbMenu.vue:81` imports `{ SharePopover }`; the level-2 barrel `demo/components/instrument/index.ts:27` (`export * from "./shell"`) has no `EditorHeader` consumer — the specifier grep is corpus-total and returns nothing else. **Mount census: 0** — the component renders nowhere at the frontier.
- **Named non-consumer**: `scripts/observe/demo/usability.mjs:122-133` reads the shell **directory** by `readdirSync` (the 2d `depth-text` probe) — it enumerates whatever files exist and asserts nothing per-file; a delete changes no assertion. Recorded, not counted.
- **The bank is internally honest; the spec's paraphrase is where the error entered.** `kf-App.md:91` reads *"EditorHeader: 0 consumers, **still barrel-exported on App's import path**"* — zero MOUNT consumers AND the live barrel edge, in one clause. `KF-W6.md:50`'s note *"(0 consumers)"* dropped the second half — the same truncated-paraphrase mechanism R3-1 convicted in the CARRY's filename substitution, one row over, exactly as the pass-3 curve predicted.

**True consumer set = { `shell/index.ts:2` } (one importer, the barrel) · exported-symbol consumers = ∅ · mounts = 0.** The delete is sound; the authorization was not.

**Cure (exact), owner KF-W6 — the POSTURE FIRST, then the grant.** A NO-WAVE-OWNER row cannot carry a delete grant in any wave: the row is **HOMED before it is granted**.

1. **HOMING (the ruled posture): `KF-APP-41` is ADOPTED BY KF.W6**, by declaration, in this order:
   - a dated **E-3 addendum** appended under `KF-APP-41` at `kf-App.md` (never an edit): *"ADOPTED BY KF.W6 — RULINGS-4 R4-1, 2026-08-28; the delete is spent at KF-W6 §Bounds with the LAW-A census of record."*
   - the **kf-EditorShell C-22 orphan-token limb** (`kf-EditorShell.md:95` — the `--header-items-max-w: 500px` at `layout.css:15` that *"DOES survive into the shipped cascade"*) is adopted in the **same declaration** with its own E-3 addendum; C-22's remaining limbs (D-27 · RR-2 M7) **stay NO-WAVE-OWNER, untouched**.
   - W6 §Carry gains the adoption row (census pasted); §Excluded's NWO line updated — the rows leave the NWO set **by adoption, never by omission**.
   - the adoption is **declared to KF.W10's G-2 NWO-TERMINAL-SWEEP** (terminal verb: `ADOPTED-BY-KF.W6 (RULINGS-4 R4-1)`) and noted at the R4-8 successor register's KF.W12 row (anti-re-book: no successor packet re-books either row).
2. **§Bounds row `:50` re-cut**: *"`EditorHeader.vue` | **delete** (KF-APP-41, ADOPTED R4-1) | banked `kf-App.md:91` — '0 consumers, still barrel-exported on App's import path'; LAW-A census at `81a56990` (RULINGS-4 R4-1): ONE live importer = `shell/index.ts:2` (the barrel), 0 symbol consumers, 0 mounts."* The **evaluate fork is RESOLVED to delete on the census** (0 mounts); the *"live escapes EH-4/-5/-8/-9/-10 are the real cures"* clause is re-cut: those rows are **MOOT-ON-DELETE**, dispositioned by the same dated addendum — no cure is spent inside a file the same row deletes.
3. **ONE ATOMIC ACT, declared at §Commits**: delete `EditorHeader.vue` **+** strike barrel line `shell/index.ts:2` **+** strike the `layout.css:15` orphan token — one commit. (The barrel edit is lawful here and only here: R4-5(c) rules KF.W6 the barrel's ONE write owner.)
4. **Gate restated falsifiably** (the R3-1 template): GREEN = the file is deleted AND `git grep -n 'EditorHeader' <tree-at-execution> -- demo/ test/ src/` → **0** AND `git grep -n 'header-items-max-w' <tree> -- demo/` → **0** AND the barrel exports exactly `{EditorShell, EditorStartScreen, SharePopover}` AND `App.vue:143`/`MbabbMenu.vue:81` still resolve. **Falsifier**: fails if the delete lands in a different commit from the barrel/token strikes; fails if any green here is read as discharging C-22's non-token limbs; fails if the barrel is edited by any seat but KF.W6.
5. **The same LAW-A discipline reaches the wave's whole delete surface in the same motion** (the R3-11(e) scope lesson): `tab-idiom.css` gets its census row — sole edge `demo/styles/design-idioms.css:7` (`@import "./tab-idiom.css";`, measured at the pass-4 check) — and the `@import` removal is **booked** in the S-1 commit family (§END KF-W6 D-P4-6).

---

## R4-2 · CRITICAL (KF-W4) — G-KFW4-11's inventory re-derived at the frontier: SEVEN specifiers / SIX files; the equality oracle restated over the true set; the pinned-hash artefact propagated

**The inventory of record, executed by this seat at `81a56990`** (`git grep -n '@src/' origin/master -- demo/`):

```
demo/components/instrument/keyframes/composables/useKeyframeOps.ts:1    @src/animation/compile/emit/css-text
demo/components/instrument/keyframes/utils/parseAnimationCSS.ts:7       @src/animation/compile/emit/css-text
demo/components/instrument/timeline/utils/timelineEngine.ts:1           @src/animation/internal/helpers
demo/components/instrument/timeline/utils/timelineEngine.ts:17          @src/animation/compile/emit/css-text
demo/components/playback/AnimationVisualizer.vue:45                     @src/animation/resolve/browser
demo/utils/helpers.ts:9                                                 @src/animation/resolve/browser
demo/utils/keyframeSelector.ts:5                                        @src/animation/compile/selector
  (+2 prose-only comments, non-import context: demo/kf-engine.ts:5 · useOrbitalInertia.ts:12)
```

**Seven import specifiers across six files.** Zero of the pinned three resolve as written: `CSSCodeEditor.vue` carries **no** `@src/` import (its sole import of interest is `@utils/helpers` at `:40`); `useKeyframesState.ts:1` likewise imports `@utils/helpers` — the deep hop **moved one file away**, into `demo/utils/helpers.ts:9`; `AnimationVisualizer` is `:45` not `:79`; `timelineEngine` is `:1`/`:17` not `:4`/`:19`. The bank's anchors (`kf-CSSCodeEditor.md:46`) were measured at the DISQUALIFIED worktree and never re-derived — the exact class R2-5 exists to kill, untouched by rounds 2 AND 3.

**Cure (exact), owner KF-W4**:

1. **The born-RED column re-cut**: the three-site inventory is **struck**; the seven specifier lines above are written in, pasted with the command, each with file + line + module. The two prose-only comments are recorded as non-import context so a future census does not count them.
2. **The equality oracle restated over the true set**: *"the set of `demo/ → @src/…` deep-import specifiers must equal the pinned SEVEN-specifier / SIX-file inventory, re-read and re-hashed at use; it fails on an addition AND on a silent removal."* The rule survives; only its denominator was wrong. GREEN is now reachable: the gate **measures** (its cure sites remain KF.W8's per §Excluded 4 — stated as the standing dependency-cite).
3. **The witness set re-anchored at the frontier**: `KF-CE-12`'s witness is the timeline/keyframes utils pair (not CSSCodeEditor, which is @src-clean at the frontier — stated as a dated correction); `KF-AV-8`'s at `AnimationVisualizer.vue:45`; `C-12`'s at `timelineEngine.ts:1`/`:17`.
4. **§Artefacts propagated**: `depcruise-inventory.json` re-specified as *"the pinned SEVEN specifiers / SIX files, hashed"* — never the three.

---

## R4-3 · HIGH (KF-W9) — G-KFW9-6 re-cut on the G-KFW9-9 template: it closes on THIS wave's measurements; the unification constraint becomes a declared sequencing obligation to KF.W5/KF.W6, booked at both ends

**The chosen arm** (of the two lawful cures): the CLOSES clause is **re-cut to in-grant acts** — the acts cannot move to W9 (its own §Bounds rules every PRM cure KF.W5's/KF.W6's/a packet's; a write grant here would re-open the OP-2 breach).

**Cure (exact), owner KF-W9** — G-KFW9-6's CLOSES becomes, verbatim:

> **G-KFW9-6 · CLOSES (re-cut, RULINGS-4 R4-3, on the G-KFW9-9 template).** This wave closes on its own measurements only: (i) the **flag-state-and-layer enumeration** — the group arm (`g.respectReducedMotion`, `group/group.ts:57`) and both standalone-arm options bags, published as a register row; (ii) the **KAD-11 pair's rest-state capture** — the idle-indistinguishability witness at 100 % width; (iii) the `reducedMotionScale` consumer census (R3-9's, re-cited). The unification constraint — *"no unification lands that snaps a progress animation to a rest state its own banked defect makes idle-indistinguishable; group arm via `g.respectReducedMotion`, standalone arms via options bags; the KAD-11 pair lock holds"* — is HANDED to **KF.W5** (engine half) and **KF.W6** (tokenization half) as a **DECLARED SEQUENCING OBLIGATION** (the S-2/S-15 idiom), read **UNMEASURED here** until their acts land. **Falsifier**: fails if a green is recorded on the constraint's behalf rather than on the three measurements; fails if the register omits an arm.

**Reciprocals, owners KF-W5 and KF-W6** (one line each, booked at both ends): each gains, at its PRM row, *"← KF.W9 G-KFW9-6 hands the unification constraint here as a declared sequencing obligation (RULINGS-4 R4-3); this wave's act discharges it and W9 records the discharge."* This retires the round-1 D-3 class at its third and last gate.

---

## R4-4 · HIGH (KF-W9) — the three LAW-A censuses re-run by THIS seat; the four wrong cells corrected; the KF.W13/KF.W11 blast-radius hand-off re-issued to the R4-8 register

**Re-measured this seat at `81a56990`** — the check's findings verified cell-for-cell:

- **Census 2, `btn-playback`**: demo-scoped file count = **7** (`demo/DESIGN.md` · `PlaybackRibbon.vue` · **`EasingScene.vue`** · `SpringScene.vue` · `StartingStyleTarget.vue` · `font-roles.json` · `playback-idiom.css`). The "8" is struck; **`EasingScene.vue:61`** enters the list as non-import comment context — the seventh file whose omission is why the count and the list disagreed.
- **Census 2, `.focus-ring` consumers**: exactly **4** class applications / 4 files — `KeyframeCard.vue:45` · `SpringHeatmap.vue:30` · `SpringTarget.vue:63` · `SquareScene.vue:46`. *"Five"* struck at BOTH sites (header + Finding).
- **Census 1, non-import scope**: widened to name the **four sibling-module docblocks outside the module** — `engine/playback-state.ts:3`/`:4` · `group/lifecycle.ts:5` · `orchestration/sequence/lifecycle.ts:6` — each naming pre-carve `engine/play-lifecycle.ts`: exactly the stale prose a carve census exists to surface.
- **Census 3, prose scope**: *"H prose ONLY"* struck. The true field, measured this seat: **five tranches — H ×13 files · J ×3 · K ×1 · S ×1 · U ×1** (19 files; line-hits 22/6/1/1/1). The K row is quoted by command (LAW D): `docs/tranches/K/audit/ci-cd-k.md:73` = `| proof:sequence-rows-draggable | H.W12 | ~1m (drag + re-sort wait) | ci.yml:1031-1034 |` — the historical CI-gating evidence a deleting seat must meet. The falsifier's substance (no `package.json` entry, no `.github/`/`scripts/` hit — the fork's justification void) is re-confirmed and stands.

**Cure, owner KF-W9**: paste the four true outputs with their commands (LAW D numeric arm); restate the two consumer counts at **7 files / 4 class-consumers**; widen the two scope sentences as above. **The blast-radius hand-off is RE-ISSUED**: the corrected censuses are addressed to the **R4-8 successor-formation register rows** (KF.W13 for the `btn-playback`/`.focus-ring` radius, KF.W11 for the retired-gate prose radius) — never to unauthored specs.

---

## R4-5 · HIGH ×3 (KF-W8) — the AnimationVisualizer bounds rows; the DH-2 phantom replaced by quote-by-command; G7's predicate unified and the barrel given ONE owner

**(a) The three unbacked acts get §Bounds rows; KF-AV-9's subject re-anchored to `:79`.** §Bounds gains three modify-carve rows for `demo/components/playback/AnimationVisualizer.vue`:
- **`:70-78`** — KF-AV-18's W8 arm (the ≥4 value.js-attribution prose sweep; G8 leg (ii) measures it);
- **`:79`** — KF-AV-9's demo half: **THE WIRE** (`useResizeObserver(containerEl, () => bumpLayoutEpoch())`), relocation + debounce → unit h. The cell's *"`:45` is the only wire"* is **struck**: `:45` is the **import**; the bank's own census says so (`kf-AnimationVisualizer.md:50`: *"`:79` is the only wire in demo/ (census 2 hits, both this file)"*);
- **`:122-191`** — KF-AV-26's DOM-free extraction to a colocated `use*.ts`.
All three ride the existing KF-AV-28 swap rider unchanged. The `:45` carve row stays (the deep-import statement, G1's subject).

**(b) The DH-2 declaration re-written against W7's CURRENT bytes, quote-by-command (LAW C(2)/LAW D).** The phantom *"devDependencies only"* is **struck**. The row's W7 leg becomes: *"KF.W7 — quoted at its current bytes (`grep -n 'test-utils' KF-W7.md` → `:61`): 'modify-carve — **ONE line: `+@vue/test-utils` in `devDependencies`** ⟨narrowed at this repair round, RULINGS-3 R3-8.3 + §END DH-2⟩' — and its OP-4 claims the same `+@vue/test-utils` add (`KF-W7.md:31`/`:302`, true and retained)."*

**(c) G7's predicate unified: EXPORTS-WITH-EXTERNAL-IMPORTER; the tri-party surface gets ONE owner — KF.W6.**

*The chosen predicate* (the only one whose GREEN is reachable from booked acts): **a shell barrel exports exactly the directory's SFCs that have an EXTERNAL importer**, where *external* = an importing module **outside `demo/components/instrument/shell/`**. The frontier truth, measured this seat: 8 SFCs; barrel exports 4; external importers — EditorShell (`App.vue:143`, via barrel) · EditorStartScreen (`App.vue:143`) · SharePopover (`MbabbMenu.vue:81`) · **HeroAurora (`App.vue:147`, direct)** · EditorHeader **0** · AnimatedText/KeyboardShortcutsModal/TypingDots **internal-only** (`EditorStartScreen.vue:62`/`:63`, `EditorShell.vue:119`).

1. **The headline and the ratified idiom KF.W8-I are re-worded to the predicate**: *"a shell barrel exports exactly its directory's EXTERNALLY-CONSUMED SFCs, and every export has ≥1 importer."* The unreachable *"exports exactly its own directory"* reading is struck.
2. **G7 becomes an INBOUND-DEPENDENCY GATE** (the R3-8/G10 template): GREEN = **after** (i) KF.W6's R4-1 atomic commit lands (EditorHeader deleted, barrel line `:2` struck) and (ii) this wave's own booked **HeroAurora move** lands (its §Bounds move row — the SFC leaves the directory), the barrel exports exactly `{EditorShell, EditorStartScreen, SharePopover}` = exactly the directory's externally-imported SFCs, every export with ≥1 importer. **G7 performs no barrel edit.** Falsifier: fails if green is reached by adding exports for internal-only SFCs, by deleting an externally-consumed export, or by any barrel edit outside KF.W6's atomic commit.
3. **ONE owner (kills U4-D, the pass's double-homing)**: the barrel's write owner is **KF.W6, alone** — its `:53` row (*"barrel truth after the deletes"*) is the one live grant, exercised only inside the R4-1 atomic commit. **W8's §Bounds `shell/index.ts | modify-carve | "4-of-8 barrel"` row is STRUCK** (its own dispositions require no barrel edit — pass-4 check D-8). **G7's NO-WAVE-OWNER reservation dissolves**: KF-APP-41 left the NWO set by R4-1's adoption; the ownership-guard paragraph is re-cut to cite the adoption and W6's sole write grant. Each spec names the other's claim: W6's row gains *"measured by KF.W8 G7 (inbound-dependency)"*; W8's G7 gains *"written by KF.W6 alone (R4-1 atomic commit)"*.

---

## R4-6 · HIGH (KF-W9) — "fourteen folds" corrected at the enumeration and at all three dependent cells, including the gate GREEN

**The count of record**: §B's KF-CE-13 row enumerates **SIXTEEN** fold families by its own ` · ` separation (script-counted at the pass-4 check; the counting rule is the separator unit; by banked ids the count is larger — D-M6/D-M7 are two, KF-SST-14/-17 are two, the D-21 row folds three records — granularities disclosed, the S-10.1 pattern).

**Cure (exact), owner KF-W9** (LAW D numeric arm — the count lives AT the enumeration):

1. **§B's KF-CE-13 row** gains, beside its list: *"SIXTEEN fold families by this row's ` · ` separation (counting rule stated; id-level and record-level granularities disclosed)."*
2. **`:93` (§Carry head, route (b))**: *"fourteen"* → *"the sixteen (per the §B enumeration)"*.
3. **`:223` G-KFW9-8 CLOSES (the gate GREEN)**: *"…lands as a measurement with its folds — **the §B enumeration, sixteen ` · ` families** — discharged by reference, zero re-bookings."* The gate points at the enumeration, never re-issues a numeral.
4. **`:234` S-2**: same re-point. An executing seat now discharges the enumeration, not a figure two families short.

---

## R4-7 · THE STAGE-2 RE-CONSUMPTION (KF-W10) — the close wave re-consumes FINAL sibling rosters under LAW C; the stale C-17.R roster and the two seam edges recorded UNCURED are cured

**Cure (exact), owner KF-W10, executed in stage 2 AFTER W0's stage-1 write**:

1. **D-1 — the CARRY-C-3 claimant roster re-consumed at KF-W0's POST-ROUND-4 bytes, quote-by-command.** The roster W10 consumes is W0's final one: **TEN S-9 claimants + FOUR S-10 claimants** (the fourth = **`KF-ET-27`**, adopted this round at W0's D-3 cure with `KF-ES-20` as its second provenance leg — §END KF-W0) **+ row 13 retained as the enumerated strike**. Specifically: **`KAD-12` IN** (adopted round 3, `KF-W0.md:374`); **`F1(c)` OUT** (retired — the strike printed); the `/number-field` claim re-keyed **`LP-8 ≡ KF-CO-35`**; *"the fifth record's original request"* OUT (struck as the KF-KE-21 duplicate). The *"11 of 13"* verb split is re-derived over the live roster at W0's final bytes and pasted with its command. W0's second falsifier (`:495` — a mint over the round-2 thirteen collides) is quoted as the reason this cell can never again be consumed at a prior round's bytes.
2. **D-2 — the §6.C-0 seam table's edges 1 and 2 re-measured and their supersession RECORDED IN THE CELL**, exactly as edges 3/4 record theirs: edge 1 → `KF-W0.md` §Sequencing's KF.W10 OUTBOUND row carries the mint-order answer (*"THE C-17-MINT CONSUMPTION EDGE, answered from this end"* — present at the bytes, R3-10.1 landed); edge 2 → `grep -n 'KF-HA-19' KF-W3.md` → **4 hits**, E-14.5 the booking (R3-10.2 landed). Both cells move from *"directive-backed, not measured"* to **MEASURED — CURED AT THE SOURCE**, command pasted.
3. **D-7 — the two `ENUMERATED-NOT-MINTED` receipts re-anchored** per LAW C(3)/LAW D: §-heading + the quoted cell text (*"ENUMERATED AS A DEAD OCCUPANT, NOT MINTED"* / *"ENUMERATED AS REFUSED, NOT MINTED"*), produced by command at W0's final bytes; the dead `:234`/`:235` dropped.

(The rest of W10's register — D-4/D-5/D-6/D-8/D-9 — is at §END.)

---

## R4-8 · THE KF.W11 / KF.W12 / KF.W13 DISPOSITION — ruled at the minting bytes: **(a) REGISTERED SUCCESSOR FORMATIONS**; the mints stand

**The bytes, read by this seat.** `KF-W4.md:249`: *"**KF.W13 · Chrome, Dock & Transport Repair (2) — MINTED here, declared** (a wave beyond the census sketch is minted explicitly, per KF-W1 cross-edge 9's cohesion flag)"* — inside the homing block that partitions **the canonical SEVENTEEN NO-WAVE-OWNER packets 9 + 6 + 2** across the three (KF.W11 Demo Scene Repair · KF.W12 Authoring-Surface Repair · KF.W13 Chrome/Dock/Transport), each with named cargo and travelling locks. `KF-W1.md:308-311` (cross-edge 9) lists all three, raises the **COHESION FLAG** (*"the X·KF spec set must MINT them explicitly"*) and records it *"**DISCHARGED at the homing authority** — R-15 has KF.W4 mint KF.W13 explicitly … The flag is recorded as answered, not deleted."*

**The mints are deliberate, load-bearing, and flagged-and-answered — striking them as accidental (arm (b)) is untenable at the bytes.** Seventeen packets, six travelling locks and the KF-AV-28 rider have no other home; the program's own successor-authoring mechanism (the SS-1/SS-2 authoring block — the standing NEXT of the megatranche record, the surface W5's G-TAX relay and W7's D9 already name) is the seat that authors them. **RULED: arm (a).**

**Cure (exact)**:

1. **Owner KF-W10 — a new section, §6.D · SUCCESSOR-FORMATION REGISTER**, one row per wave, each carrying: the name · the inbound cargo (**KF.W11**: cube · sequence · spring · spring-plot · spring-physics-facet · spring-artifact-truth · square · amiga · drag-seam — 9; **KF.W12**: CARD-UNIT · OPTIONS-UNIT · KFED-UNIT · EDITOR-UNIT · APPLY-UNIT · AXISLINE-UNIT — 6; **KF.W13**: dock-menu · transport/ribbon — 2) · the travelling locks by name (the KF-AV-28 rider on W7-evaluated surfaces · kf-ChromeDock M-4's MUST-CARRY MbabbMenu rider · the TD-1/TD-2 bundle WHOLE · LP-1's write→render ordering · comment-stated-invariants-are-test-obligations · glass-producer rows → SS-6) · **the minting citations** (`KF-W4 §Sequencing`, the R-15 homing block; `KF-W1 §Cross-edges` edge 9 + its DISCHARGED flag — §-heading anchors) · the inbound blast-radius receipts (W9's R4-4 corrected censuses, indexed at the KF.W13 and KF.W11 rows) · the anti-re-book note (KF-APP-41 `ADOPTED-BY-KF.W6`, R4-1) · **verb: `MINTED-UNAUTHORED — AUTHORING = the SS-1/SS-2 authoring block`** (ownership assignment stays the root session's act, the R-18 pattern — the register RECORDS the boundary, it does not assign).
2. **R-A re-cut (the VERIFIED stamp's scope made honest)**: *"X.KF.W10 advances its own four-verb row and **every AUTHORED sibling X·KF wave's** (the eleven specs on disk, W0–W9 + this file) from IMPLEMENTED to VERIFIED in one act at close. The three minted-unauthored successor formations take **no verb from this close** — their §6.D register row IS their terminal disposition here."* §1's Opens-after is unchanged (the three never gated the close).
3. **G-2 / OP-5 re-conditioned on artifacts that EXIST**: OP-5's GREEN leg re-cut — *"every packet home either (i) has an authored spec, or (ii) is named in §6.D with its cargo and minting citations; the register is the artifact — the unauthored specs are the authoring block's deliverable, never this close's precondition."* G-2's right-hand union becomes *"the union of AUTHORED X·KF wave bounds ∪ §6.D's cargo enumeration."* L-2's vacuous-gate rule no longer fires; the close stamps nothing it never named.
4. **COHESION owed the boundary line** (dated addendum, §0c or successor): *"KF.W11 · KF.W12 · KF.W13 — MINTED (KF-W4 R-15 homing; KF-W1 edge 9, flag DISCHARGED), UNAUTHORED; cargo = the 17-packet partition 9+6+2 with the six travelling locks; authoring seat = the SS-1/SS-2 authoring block; X·KF W10's close stamps the AUTHORED ELEVEN only."*
5. **MECHANISM F adopted program-wide** (the pass-5 instrument U4-B names): every conformance census includes the **minted-wave roster** — `grep -nE 'KF\.W1[1-9]|MINTED' waves/*.md`, diffed against the authored spec set ∪ the §6.D register. **A mint with neither an authored spec nor a register row is a hard escape on sight.** No census mechanism enumerates waves until this one; the largest object ever to escape the partition cannot escape it again.

---

## R4-9 · THE THREE HARD ESCAPES + THE DOUBLE-HOME — owners assigned

1. **kf-KeyframeCard `KF-KC-17`** (⟨`:59`⟩ — the corpus's only banked non-CSS-character ingress into the grammar adapter: the next real keystroke hands 4×NBSP, not CSS whitespace, to `parseAnimationCSS` via ops:111) → **owner KF-W2**: booked by id, in record context, at **G-W2-5's fuzz surface** — the second member of the malformed class the gate's extension clause exists for (the fuzz corpus is *"structurally incapable"* of producing it; this datum is its in-tree instance). The record's honest disclaimer travels; the verified negative KF-KC-53 stays booked beside it, never merged.
2. **kf-ChromeDock `m-9`** (⟨`:41` + `:90`⟩ — the fifth rider of the sentence R3-5.2 cured at four) → **owner KF-W4**: the rider sentence re-cut at **FIVE** — `:128` TS2322 · C-8 · m-1/C-7 · m-8 · **m-9** — with m-9 booked beside m-1/C-7 under the bank's own clause (*"listed here for count integrity: the unguarded trigger lookup + the inline `find`; one repair"*). No new repair mass; the count-integrity row is restored. The round that existed to end sentence-mate truncation does not get a third try: the cure quotes the WHOLE banked sentence per LAW D.
3. **KF.W11 · KF.W12 · KF.W13** → **R4-8** (owner KF-W10 + the COHESION line).
4. **The double-home U4-D** (the `shell/index.ts` + EditorHeader surface: W6 modify · W8 modify-carve · G7's NWO reservation · the bank's NWO delete) → **resolved at R4-1 + R4-5(c)**: ONE write owner (KF.W6), one measurer (KF.W8 G7, inbound-dependency), the NWO rows adopted or untouched by name, both specs naming the other's claim.

---

## R4-10 · THE TWO PARTIALLY-UNAPPLIED RULINGS-3 DIRECTIVES — re-directed with changed mechanisms

**(1) R3-11(e), the R2-5/LAW-A full-surface completion — FOURTH consecutive partial round. The mechanism changes: the sweep's enumeration source is the SCOPE, never the example list.** The pass-4 lesson (KF-W6 check, verbatim): *"a repair that cures the named instance and not the stated scope leaves the law green over its own counterexample."* Binding on every round-4 repair seat:

- **LAW A's enumeration source is the §Bounds ACCESS COLUMN itself**: every row whose access includes `delete`, `modify/delete`, `repoint`, `shim`, or `move` carries a census — the column is grepped, the rows enumerated, each census present or written now. Known members land this round: W6 `EditorHeader` + `tab-idiom.css` (R4-1, D-P4-6) · W5 `KAD-14(e)` (symbol-level, the Act-4 reading applied consistently) · W8 A-10's library-consumer figure (3 → **4**: `engine/css/metadata.ts:35` · `resolve/browser.ts:4` · `resolve/conditional.ts:2` · `resolve/function.ts:6`).
- **SUBJECT-IDENTITY's enumeration source is every symbol-bearing §Bounds row and every gate-witness symbol**, per wave, whole file — not the rows a ruling named. Known members: W5 D-1/D-2/D-3 (+D-12) · W8's KF-AV-9 `:79` (R4-5(a)) · W9 D4-4 (`shadowTick` decl **`:53`**, both sites, or the R2-7 file+symbol idiom) · W4 D-7's three bare leaves.
- **The numeric arm is LAW D(3)**: every pasted command re-run at write time. Known members: W9 D4-1 (R4-4) · W4 D-4 (row 68's **eight** uses: `:52 :58 :76 :99 :106 :123 :132 :138`) + D-6 (row 29's nine hits each resolved) · W0 E-5/D-5 (the two symbol hits: `test/compile/compile-roundtrip.test.ts:42-44` · `src/animation/load-engine.ts:47`) · W10 D-5 (the `test/physics/sync-step.test.ts:24` hit added; leg-1's blanket corrected).
- Each repair seat writes a one-line **scope receipt** per class into its round-4 ledger (*"access column: N rows enumerated, N censuses present"*), and **the pass-5 checks test BY SCOPE, not by instance**.

**(2) The RE-ANCHOR seat directive — superseded by LAW C's RECONCILE seat.** It runs AFTER the last per-wave write (not mid-window), re-verifies every cross-wave receipt written this round, and its certification is **`PASS-4/CLOSE-CERT.md`** — a LAW-B artifact with the mtime table and per-receipt commands. **Without it the round did not close.** The pass-5 union seat audits it against the bytes as its first act.

---

## R4-11 · THE REMAINDER — per-wave residue directives (executed verbatim from each check's register)

**KF-W0** — D-1 (`D-19` indexed: a C-1.R carry-not-cure guard row — *"the re-anchor/re-resolve-before-cure law: CONTENT carried whole at C-2 · C-21 · G-0.9; NO RE-BOOK; record-qualified because `kf-KeyframesAddDialog:69` overloads the id"* — with the 35-record receipt pasted and **G-0.9's stamp keyed to it**) · D-2 (G-0.1's falsifier + the L-18 rider carry the measured **225** with `comm -13` pasted and the non-nesting stated — 152 status rows outside the frontier diff, 124 untracked; GREEN requires both enumerations, never one plus a difference) · D-3 (the S-10 receipt re-cut: the **37 hits / 15 records** reading pasted with the substring-discard rule `KF-ES-10`/`MISS-10`/`KF-SS-10` ≠ `S-10`; **`KF-ET-27` ADOPTED as the FOURTH S-10 claimant** with `KF-ES-20` as its second provenance leg and *"the design-idioms census arm is KF.W5's"* as its disposition; *"no fourth or fifth claimant"* replaced by the LAW-B form) · D-4 (the four `:447` landing cells re-anchored on *"§Sequencing, the NO-WAVE-OWNER packets row"*, `:588` parenthetical non-load-bearing) · D-5 (the two symbol hits resolved; *"10 flat siblings"* → **7 flat + `format/` ×3**; the docs-prose clause qualified for the two in-source hits) · D-6 (`KF-EST-1` named as the THIRD R-8 residue family with its eight sites listed, `TypingDots.vue:45/:51/:59` entered in the roster) · **F-1 rider landing** (W1 D4-3's receiving end): the §Carry F-1/`KF.W1-DEP` row gains the five component riders verbatim (`useControlsKeyboardShortcuts.ts:1` sole-binding-site · `ChannelControls.vue:219` first-failing import · `ControlsPaneWrapper.vue:166` sole `/drawer` consumer · the four toast utility classes · the one-class/eight-custom-property inventory).

**KF-W1** — D4-1 (the head reciprocity paragraph + cross-edge 1 + `:58` re-measured over the **TEN** siblings on disk; the two-verdicts-one-file split ended at the head) · D4-2 (the LAW-A census re-run: **25/17 + 14/7**, the five in-spec occurrences enumerated, the non-consumer enumeration made to sum, *"six apotheosis files"* → **seven**; the two-executing-consumer finding retained) · D4-3 (C-13/cross-edge 3 re-worded to cite W0's post-landing bytes — paired with W0's landing above, LAW C stage order) · D4-4 (G-KF1-4 re-cut on the **C-10 MINT-LAW pattern**: the gate commissions the sentence TEMPLATE + the substrate-naming rule, the five dated measurements filled at authoring — never inscribed verbatim from this spec) · D4-5 (the bare-token zeros corrected at `:347`/`:396`: `KF-W9.md:243` named as a declared non-edge — a stronger reciprocation than silence; KF-W2's `O-8` count restated at 2) · tails: D4-6 (the KF-AV-28 parenthetical re-run at current bytes or dropped, citation kept) + D4-7 (the SACRED-CHECKOUT quotation carries *"until W2 completes"* + the `a59d3a22`/~254 record) + D4-8 (`:205-206`) + D4-10 (*"every gate leg"* → *"every leg whose reading turns on tree identity"*).

**KF-W2** — D-1 (the three KF-W10 relay sites re-pointed to the **successor SS-6 batch — COHESION §4a's re-opened accretion register**; W10's live sentence quoted by command; the O-20 departure carried as the qualifier) · D-2 (the frozen-parse paste completed at **three** assertions `:43`/`:67`/**`:84`**; prose + falsifier updated) · D-3 (§Excluded's *"only 15 carry a runtime statement"* corrected to the F1 table's **13** runtime importers / 25 specifiers; the **owed-to-KF.W4 denominator restated at the operative figures** so W4's graph gate is authored against a real number) · D-4 (the base **28 enumerated by banked id** — the routed denominator made falsifiable) · D-5 + D-6 (the *"only executed easing measurement"* superlative retired against Y1 and `kf-EasingTarget:119`; the BH-relay deliverable's `steppedEase`/`step_count_invalid` reading **attributed to KF-ES-3's bank** — or run as one node probe and owned) · Z1/Z2/Z3 (the KF-KE-2 fold stated by id or booked; N-9's per-stop timing-function drop carried as the qualifier beside the `parseAnimationCSS` affirmative cite, cross-cited to `KF-W5 §Carry B-16`'s open locus with this wave's 58-line read named as the settling measurement; **KF-KC-17 booked at G-W2-5's fuzz surface per R4-9.1**) · Z4–Z7 (the `requireEasing` Result-API funnel entered in the positive register beside S-5/SUP-D; the three executed entry measurements — `easing_name_unknown`, `parseTimingFunction("step-start")→steps`, `parseCssScalar("500m")` payload-shape with its do-not-chase lock — entered in the census-of-record) · minors D-12..D-17 (the ledger's phantom `KF-W4 §Rows` swept; the frozen-site count **23**; G-W2-2b relabelled MONITOR per L-19 with the gates head at eight-armed; `:135-144` → `:136-144`; §Provenance's denominator voice converted).

**KF-W3** — D-1 (the payload sweep run over ALL 58 independent of markup; the **six banked negatives** — MM-37 · kf-SequenceScene:119 · kf-SpringHeatmap:101 · kf-MatrixEditor:139 · kf-EditorHeader:95 S-A · SUP-7 — entered on the SURPLUS side; G-KF3-7's arithmetic restated **≥26**) · D-2 ((R-i) restated *payload-admissible AND markup-positive, the payload test read off the row's bytes*; *"no hand-partition anywhere"* → *"no UNDECLARED partition"*; the five discarded-by-reading records named) · D-3 (the **→ KF.W10 cross-edge row added** — the verb-table VERIFIED dependency + the E-14.5 pass-through, matching W10's inbound table row 2) · D-4 (G-KF3-5: state why `:252`/`:285` are outside the six or drop the count and keep the predicate) · D-5 (`emit/index.ts`'s no-act modify → read-only witness, or the concrete edit named) · D-6 (E-14.5's `:398`/`:419` re-pointed to §6.C-I at current bytes, *"at this writing"* kept).

**KF-W4** — R4-2 · R4-9.2 (m-9, the five-rider sentence) · D-3 (the AUDITED basis **47 → 49**: kf-PlaybackRibbon + kf-CSSPasteDialog entered; the balance restated at the true composition) · D-4 (row 68: the command's **nine** lines pasted — declaration `:29` + **eight** uses `:52 :58 :76 :99 :106 :123 :132 :138`) · D-5 (the KF-ET-11/KF-ES-34 double-booking removed from row 1; both stay at row 2 per their banks' KF-CB-8-alone routing) · D-6 (row 29's symbol census: all **nine** hits resolved to their specifiers, per LAW A's own method sentence) · D-7 + D-8 (the three bare leaves path-qualified — `src/animation/easing.ts:44` · `demo/components/instrument/timeline/TimelineCaret.vue:35` · `…/timeline/KeyframeTimeline.vue:179`; LP-7's rider identity restored: *"≡ kf-MatrixEditor K1 + FAM-03/MR4"*).

**KF-W5** — D-1 (the `internal/reduced-motion.ts` row re-cut: `ReducedMotionPolicy` stays; *"`snapToReducedMotion`'s single caller"* re-anchored to its true homes — decl `play-lifecycle/strategies.ts:76`, caller `frame.ts:121` calling `:137` — in their own rows) · D-2 (the `option-setters.ts` row splits its subjects: `applyTimingFunction` `:32`/`:149` here; `setTimingFunction` at **`engine/animation.ts:260`** named at ITS row, so G-OPTSET leg 1 has a bounds row naming its symbol) · D-3 (the pathless `setTargets` row gains its subject(s): the three `HTMLElement[]` declarations enumerated — `engine/animation.ts:465` · `group/group.ts:195` · `sequence/sequence.ts:310` — the target chosen or all three carved, with the two-organs-disagree consequence stated) · D-4 (**G-TAX re-cut as an INBOUND-DEPENDENCY GATE** on the SS-1/SS-2 relay per the R3-8 idiom: this wave's act = the RD-2 relay row + the declared observability rider; the SUPERSEDED annotations are the receiving owner's act, E-3 addenda under original ids) · D-5 (the false root-barrel universal struck; the narrow twelve-subject clause kept; the **9 hits / 7 files** pasted) · D-6 (*"only `cssIdent` re-export"* → the TWO-site chain `walk.ts:145` → `backward/index.ts:29` → `emit/index.ts:53`, at all three sites incl. G-CSSIDENT's RED witness; the zero-in-published-surface half kept) · D-7 (*"eleven"* → **ten** for the three-sibling set, or the four-party **11** attributed as KF-W7 uses it) · tail D-8..D-14 (the twenty-gate sweep prints all twenty verdicts or scopes its claim to the nine count-oracles; the KF-SS-31 cross-spec line numbers dropped for count-only per the file's own RD-3 rule; the KAD-14(e) symbol census added per R4-10; the view-transition barrel count **5**; the `emission.ts` row's `steps()` subject corrected to `densify.ts:45`/`:57`; D-13's negative-universal converted to LAW-B form; G-CSSIDENT's resolution offered from `loadAnimationEngine()`/the exports map, the demo-only `kfEngine` dropped).

**KF-W6** — R4-1 · D-P4-2 (four §Carry rows: THP `D-5` two-tone · `D-11` four-tier/6px · `D-17` alpha one-offs · TT `m-17` `max-w-56`/1.272 — each with severity, mechanism, bank pointer) · D-P4-3 (the census composition re-cut: **eight** records carry no pure token; the true route `(50 − 2) + 6 = 54` stated; the `KF\.W6`-matches-`KF.W6-TIMELINE` trap named) · D-P4-4 (the dock-contract seam two-ended: W6's TransportDock surface gains the CH2-02 ×4 evidence row — BG-5 · GU-1 · GU-2 · subject-legible — or the bounded exclusion is declared WITH the obligation routed by name; booked at both ends with W10 §B-3) · D-P4-5 (`ND-7`/`D-C2` entered on the ID-ALIASES trail beside their hosts KF-AT-3/KF-AT-6; *"all three fold rows"* corrected to FOUR by dated note) · D-P4-6 (the `tab-idiom.css` LAW-A census row + the `design-idioms.css:7` `@import` removal **booked** in the S-1 commit family) · D-P4-7 (`C-14` homed: a §Excluded line naming its NO-WAVE-OWNER home and the decision's owner, so the S-1 family's completeness no longer hangs on an unhomed decision).

**KF-W7** — D1 (the census rule reconciled: the OWNER test (rule 5) GOVERNS, rule (2) restated as its corollary; the figure restated at **136** with the four adjudicated rows named — KF-AV-13/-14/-24 rider-governed non-routings, KAD-F3 fold-deduped; the LAW-B cite re-pointed to `PASS-4/KF-W7-CHECK.md §1`) · D2 (**KF-KC-52 restored to INFO** — its bank's rung — per the spec's own carried lock; no silent promotion) · D3 + D6 (LAW D: the P-2 cure-caution re-attributed — quoted byte-exact from `kf-SpringTarget.md:69` AS the gloss it is, or `kf-EasingTarget.md:109` quoted whole; MISS-β3 re-quoted with the scoping parenthetical or labelled paraphrase) · D4 (TT DISSENT-1's **all four** members carried: D-12 and the D-13 net arm gain their preserved-dissent clauses beside D-4 and M-7) · D5 (the KF.W2 registry-before-posture ordering entered as **hard-ordering item 9** + the cross-edge IN leg, matching `KF-W2.md:571`'s both-ends assertion) · D7 + D8 + D10 (*"BOTH rows"* → three; `L-15-PROTECTED` re-bound to the literal heading *"Killed-claims register — 10 rows, refutations re-executed"*; the P0 riders header re-verbed so KF-AV-17 reads as the CURE-SHAPE LOCK it is, its fold home stated) · D9 (the KFED-UNIT *"from both ends"* re-worded: the second end is the **R4-8 §6.D register row** until the successor spec exists — cite it, promise nothing).

**KF-W8** — R4-5(a)(b)(c) · D-4 (the six dead KF-W5 coordinates re-pointed under LAW C: stable anchors — `KF-W5 §Carry Arm D row D-6` = `:310`, the → KF.W8 cross-edge = `:374` — parenthetical, non-load-bearing; *"at this writing"* retired for quote-by-command) · D-6 (the two surviving spec-voiced closure sentences — the inbound-edge table heading and the → KF.W5 *"No leg is left neither…"* — converted to LAW-B form) · D-7 (the L-18 blanket restated: falsifiers 15/15, commands 13/15, **plus two gates carrying uncommanded semantic conjuncts named — G7 (cured at R4-5(c)) and G8 leg (ii)** — the suite described as it is) · D-8 + D-9 (the no-act write grants narrowed: `test/demo/instrument/` nine tracked files → read-only + the ONE cure site `resize-tracks.test.ts`; the barrel row struck per R4-5(c); **G14 given a lawful landing surface**: §Bounds gains `demo/composables/useDragScrub.ts | modify-carve | the consolidation seam (G14's falsifier's own destination)` plus the named booked hosts, or G14 is re-cut to measure-and-declare with the consolidation handed to the R4-8 register) · tail D-10..D-15 (R-4's requote carries D-6's dated 12-correction rider beside the verbatim; A-10's library figure **4**; the C-22 parenthesis elision marked; the DH-3 truncation completed *"…never globbed by shared prefix"*; G8's transport spelling unified at `:3`; the closure citation re-pointed to `PASS-4/KF-W8-CHECK.md §1` whose fold-transitivity enumeration is complete at five).

**KF-W9** — R4-3 · R4-4 · R4-6 · D4-4 (`shadowTick` decl **`:53`** at both sites, or the R2-7 file+symbol idiom) · D4-5 (the §Bounds cell split: *"read-only for the EXISTING bytes (E-1); APPEND-ONLY for dated addenda under original ids (E-3), by `.e` alone"* — matching what S-13 and G-KFW9-13 already assume) · D4-6 (the self-falsifying zero re-worded on the file's own dissent-roster pattern: *"this spec schedules nothing against it; the only occurrence of the token is this disclosure"*) · D4-7 (`src/animation/physics/numeric.ts:82-89` written in full; §Carry anchors carry full paths).

**KF-W10** — R4-7 · R4-8 · D-4 (G-4's conformance-class table gains `PASS-3/KF-W10-CHECK.md` AND `PASS-4/KF-W10-CHECK.md`; the field re-grepped at write time per the gate's own standing instruction, the 25/80 figure superseding 24/56) · D-5 (the LAW-A exemplar census completed: `test/physics/sync-step.test.ts:24` entered as the third disqualified reference; leg (1)'s *"prose in dated docs/ records"* blanket corrected; *"none was missed"* retired for the LAW-B form) · D-6 (the §B-2 `FOLDED-TO … SC-1` key collision cured under the file's own ROW-KEY NOTE: the target re-keyed **`COHESION-SC-1`** as a declared positional key, disambiguated from kf-SequenceScene's banked SC-1..SC-8; §3.1's alphabet gains the **register-row evidence form by declaration** — the third lawful `FOLDED-TO` target — so row 2 stops taking an undeclared form) · D-8 (**O-21 named by id** at OP-3, G-6's RED and §6.C-B — *"the O-8/O-11 amendment-addendum, discharged by KF.W1's outbound row O-21 (minted max+1 at open)"* — and §6.C-0 gains the **SIXTH inbound-dependency row**: the KF.W1 edge, PRECEDES-hard, its three W10-side anchors listed) · D-9 (§B rows 1/4/9's evidence cells re-pointed at the named vehicle that satisfies `STANDING-CARRIED`'s form — COHESION §4a's re-opened accretion register — or the alphabet's evidence column widened by declaration; row 9's gate-receipt form declared if kept).

---

## §END · PER-WAVE DIRECTIVE MAP (what each round-4 repair seat executes, verbatim, from the rulings above)

| seat | n | directives |
|---|--:|---|
| **KF-W0** | 7 | D-1 (`D-19` guard row + G-0.9 keyed) · D-2 (the 225/`comm -13` falsifier) · D-3 (S-10 37/15 + KF-ET-27 the fourth claimant) · D-4 (`:447` → §-heading) · D-5 (symbol arm + flat count) · D-6 (KF-EST-1 third family) · the F-1 five-rider landing (W1 D4-3's receiving end). **Writes FIRST (LAW C stage 1).** |
| **KF-W1** | 6 | D4-1 (ten siblings) · D4-2 (census re-run) · D4-3 (C-13 cites W0's post-landing bytes) · D4-4 (G-KF1-4 → MINT-LAW pattern) · D4-5 (the falsified zeros) · tails D4-6/7/8/10 |
| **KF-W2** | 8 | D-1 (W10 relay → COHESION §4a successor) · D-2 (three-assertion paste) · D-3 (13-importer denominator, W4-owed) · D-4 (the 28 enumerated) · D-5+D-6 (superlative + relay attribution) · Z1/Z2/Z3 (incl. R4-9.1 KF-KC-17 → G-W2-5) · Z4–Z7 (positive register + three measurements) · D-12..D-17 |
| **KF-W3** | 6 | D-1 (payload sweep, six negatives) · D-2 ((R-i) restated) · D-3 (→ KF.W10 edge row) · D-4 (six-vs-eight) · D-5 (no-act modify) · D-6 (E-14.5 anchors) |
| **KF-W4** | 7 | R4-2 (G-KFW4-11 seven-site oracle + artefact) · R4-9.2 (m-9, five riders) · D-3 (AUDITED 49) · D-4 (row 68 ×9) · D-5 (double-booking) · D-6 (row 29 ×9 resolved) · D-7+D-8 (bare leaves; LP-7 identity) |
| **KF-W5** | 8 | D-1 (reduced-motion row) · D-2 (option-setters split) · D-3 (setTargets pathed) · D-4 (G-TAX → inbound-dependency) · D-5 (root-barrel universal) · D-6 (cssIdent chain ×3 sites) · D-7 (ten/eleven) · tail D-8..D-14 + R4-3 reciprocal |
| **KF-W6** | 7 | R4-1 (census + ADOPTION + atomic grant + gate) · D-P4-2 (four M-25 rows) · D-P4-3 (census composition) · D-P4-4 (dock seam two-ended) · D-P4-5 (ND-7/D-C2 trail) · D-P4-6 (tab-idiom census + booked @import removal) · D-P4-7 (C-14 homed) + R4-3 reciprocal |
| **KF-W7** | 7 | D1 (owner-test rule, 136) · D2 (KF-KC-52 → INFO) · D3+D6 (LAW D relabels) · D4 (four dissents) · D5 (W2 ordering, item 9) · D7+D8+D10 (one-liners) · D9 (second end = the §6.D register) |
| **KF-W8** | 8 | R4-5(a) (three AV rows + `:79`) · R4-5(b) (DH-2 quote-by-command) · R4-5(c) (G7 predicate + barrel owner; own row struck) · D-4 (W5 anchors per LAW C) · D-6 (two closure sentences) · D-7 (blanket + two semantic conjuncts) · D-8+D-9 (no-act grants; G14 landing surface) · D-10..D-15 tail |
| **KF-W9** | 7 | R4-3 (G-KFW9-6 re-cut) · R4-4 (three censuses, four cells, hand-off → §6.D rows) · R4-6 (sixteen at the enumeration) · D4-4 (`:53`) · D4-5 (cell split) · D4-6 (self-zero) · D4-7 (full path) |
| **KF-W10** | 8 | R4-8 (§6.D register + R-A scope + G-2/OP-5 re-condition + COHESION line + mechanism F) · R4-7 D-1 (final roster: 10+4+strike) · R4-7 D-2 (edges 1/2 MEASURED) · R4-7 D-7 (quoted cells) · D-4 (G-4 class table) · D-5 (census third hit) · D-6 (COHESION-SC-1 re-key + evidence form) · D-8+D-9 (O-21 by id + sixth edge; evidence cells). **Writes LAST of the per-wave seats (LAW C stage 2).** |
| **RECONCILE seat** | 1 | LAW C(4): run AFTER the last per-wave write; re-verify EVERY cross-wave receipt written this round (both stages); WRITE `PASS-4/CLOSE-CERT.md` (mtime table + per-receipt commands + LAW-B certification). **The round does not close without it**; the pass-5 union seat audits it as its first act. |

**Frontier of record for every keyframes-side re-measurement: `origin/master 81a56990736ced5b5edde0b84c527680ac7689b1`** (re-verified this seat). Local `8281638c` remains DISQUALIFIED (R3-2 governs its sole lawful citation form). Value-side re-measurements bind to the current tree bytes at the executing seat's clock, under LAW C's stage order.

*Cross-wave rulings seat, repair round 4. Sole write = this file. Nothing here stamps any wave; E-3 holds — registry corrections land as dated addenda under original ids (the R4-1 adoption addenda included); spec repairs execute at the per-wave seats; the PASS-1..PASS-4 check files are never edited. The EditorHeader census (R4-1), the seven-specifier inventory (R4-2), the shell-barrel surface (R4-5c), the W9 census cells (R4-4), and the W11/W12/W13 minting bytes (R4-8) were each re-derived or re-read by this seat at `81a56990` / the current spec bytes before the rulings were written, and the receipts are pasted where the cures land.*
