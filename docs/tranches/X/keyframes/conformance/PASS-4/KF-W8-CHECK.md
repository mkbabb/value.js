# KF-W8 — FRESH ADVERSARIAL SPEC CHECK (L-18/L-20, pass 4)

**Subject**: `/Users/mkbabb/Programming/value.js/docs/tranches/X/keyframes/waves/KF-W8.md` — 401 L / 144 190 B, repaired **three** times (round 1 after PASS-1, round 2 after PASS-2, round 3 after PASS-3/RULINGS-3) and swept by the round-3 re-anchor seat.
**Corpus authority**: the **58** `kf-*.md` records at `/Users/mkbabb/Programming/value.js/docs/tranches/V/megatranche/registry/adjudicated/` (`ls kf-*.md | wc -l` → 58). Sole in-tree carry `carry/KF-W6-CARRY.md`, consulted only for the KF.W6 reciprocity axis.
**Census instrument (R3-5, binding)**: the nine-block partition is **RETIRED**. This seat's census is an **id-keyed harvest from the 58 records' own bytes** — token sweep, alternate-spelling sweep, by-name sweep, whole-sentence resolution of every routed row (merged groups, fold clauses, rider lists), and a fold-transitivity sweep of every W8-terminal identity across all 58 records. No prior pass's denominator, roster or partition was consulted before this seat's own enumeration was complete; the PASS-3 roster was opened afterwards **only as a diff target**, and the diff is reported at §1.4.
**Product tree (TREE LAW)**: `/Users/mkbabb/Programming/keyframes.js`, read-only. `git rev-parse origin/master` → **`81a56990736ced5b5edde0b84c527680ac7689b1`**, re-resolved this seat. Local `HEAD`/`master` = **`8281638c`** (DISQUALIFIED — cited only as declared historical context; `git status --porcelain | wc -l` → 252 at this seat as well; merge-base `a59d3a22`, neither ref an ancestor of the other). **Every product measurement below was taken at `origin/master`.**
**Seat posture**: FRESH. Every gate re-executed, every cross-spec receipt re-resolved at the sibling specs' **current** bytes, every registry verbatim re-read at the record. PASS-3's register and RULINGS-3 were read to learn which directives were claimed applied — **nothing was inherited**, and two PASS-3 certifications do not survive this seat's re-read (§1.4, D-5).

**Verdict: DEFECTIVE.** The **ID-keyed census is CLEAN** — **32 routed / 32 booked / 0 escaped**, re-enumerated by record — and the round-3 gate repairs are, measurement-for-measurement, the strongest yet: **all fifteen gates now reproduce their stated numbers at the frontier**, including every figure PASS-3 convicted (G8's two sites, G13's 12/12 + the lane's 9-live/7-gone + the six-plus-one commit attribution, G9's 132 = 130 + 2, G15's 9 = 2 pad + 7 convicted, G7's per-export importer counts, G10's absent `plugins` key), and the KF.W4 plugin-vue collision is now **declared and reciprocated at both ends**. What fails is elsewhere. **§Bounds does not bound the wave's own acts**: the file carves `AnimationVisualizer.vue` at `:45` while booking three further edits inside it — and mis-names the wire it must relocate. A **both-ends declaration quotes a sibling sentence that does not exist**. **G7's GREEN and the ratified idiom KF.W8-I state incompatible predicates over the same file**, neither commanded. Six KF-W5 coordinates were dead at round-3 close. One "verbatim" is not one. **3 HIGH · 7 MEDIUM · 5 LOW.**

---

## 1 · ID-KEYED CENSUS — enumerated BY RECORD, nothing inherited

### 1.1 Method

1. **Token sweep**: `grep -c 'KF\.W8' kf-*.md` over all 58 → **47** hit lines across **18** records (`grep -l` → 18). Every one of the 47 read whole and classified: routing-law boilerplate · closing-verdict summary · row-terminal routing.
2. **Alternate-spelling sweep**: `grep -nE '\bW8\b|W\.8|[Ww]ave[ -]8' kf-*.md | grep -v 'KF\.W8'` → routing-law boilerplate only (13 records carry the taxonomy line naming *W8 structure & colocation* without routing a row), plus the single non-KF token `L.W8 ED-3` (`kf-OrbitalDrag.md:82`, a **value.js** tranche-ledger cite inside OD-34 — NO-WAVE-OWNER, correctly not a KF.W8 routing; the same token recurs inside the routed `KF-CE-42` at `kf-CSSCodeEditor.md:80`).
3. **By-name sweep**: `grep -niE 'structure (&|and) colocation|colocation settle'` with `KF.W8` lines removed → boilerplate only. No record routes to this wave by name without the token.
4. **Whole-sentence resolution** (R3-5): every routed line read to its terminal period, including merged groups (`L-19/C-14`), fold clauses (`KF-KC-45` → KAD-24; `SPF-22` ≡ KAD-24; `C-13` ≡ KF-AV-8/-9/-18; `m-15`/`L-8` = banked N-11; `KF-AV-35` folds into KF-AV-8/KF-CE-12), split dispositions (`C-8` = KF.W5 surface + KF.W8 colocation; `C-12` = KF.W8 + KF.W4 depcruise arm) and rider lists (`kf-HeroAurora.md:27` limb (d); `kf-AnimatedText.md:137` *"**KF.W8** the colocation carry"* — both restatements of KF-AT-26(c), no new id).
5. **Fold-transitivity**: every W8-terminal identity `grep -ln`'d across all 58 records; every off-record occurrence opened.

### 1.2 The routed roster — 32 ids, per record, with the sentence that routes each

| record | ids routed → KF.W8 | n |
|---|---|--:|
| kf-AnimationVisualizer | KF-AV-8 (`:49`) · KF-AV-9 (`:50`) · KF-AV-18 W8 arm (`:62`) · KF-AV-24 (`:68`) · KF-AV-26 (`:70`) · KF-AV-35 (`:82`) | 6 |
| kf-CSSCodeEditor | KF-CE-12 (`:46`) · KF-CE-42 (`:80`) | 2 |
| kf-KeyframeTimeline | L-19/C-14 (`:77`) · C-12 (`:78`) · N-11 (`:101`) | 3 |
| kf-KeyframesAddDialog | KAD-24 (`:77`) · KAD zero-coverage fold → the twin's C-10 (`:89`) · L-17 colocation (`:92`) | 3 |
| kf-TimelineCaret | MISS-β2 (`:46`) · m-7 (`:59`) · L-8 (`:81`) | 3 |
| kf-KeyframeCard | KF-KC-42 (`:87`, rides the R-7 consolidation) · KF-KC-45 (`:90`) | 2 |
| kf-RibbonBar | M-4 (`:66`/`:132`) · m-4 (`:132`) | 2 |
| kf-AnimatedText | KF-AT-26(c) (`:71`; restated `kf-HeroAurora.md:120`/`:27`(d), `kf-AnimatedText.md:137`) | 1 |
| kf-HeroAurora | KF-HA-14 (`:56`) | 1 |
| kf-CSSPasteDialog | C-10 (`:75`) | 1 |
| kf-CopyButton | KF-CB-37 (`:85`) | 1 |
| kf-KeyboardShortcutsModal | R-10 (`:46`, restated `:127`) | 1 |
| kf-KeyframeCardList | KC-30 (`:66`) | 1 |
| kf-KeyframesStringControls | C-8 (`:81`) | 1 |
| kf-KeyframesEditor | KF-KE-15 (`:57`) | 1 |
| kf-PlaybackRibbon | C-13 (`:154`) | 1 |
| kf-SpringPhysicsFacet | SPF-22 (`:62`) | 1 |
| kf-TimelineTrack | m-15 (`:104`) | 1 |
| **TOTAL** | | **32** |

**Booked, id-for-id — 32 of 32.** `grep -c` over `KF-W8.md` for each id returns ≥1 for all thirty-two (KF-CE-12 7 · KF-CE-42 2 · KF-AV-8 9 · KF-AV-9 5 · KF-AV-18 7 · KF-AV-24 4 · KF-AV-26 6 · KF-AV-35 2 · C-10 2 · KF-HA-14 5 · KF-AT-26 8 · L-19/C-14 1 · C-12 5 · N-11 6 · MISS-β2 5 · m-7 6 · L-8 3 · m-15 3 · R-10 9 · KAD-24 5 · L-17 5 · KF-KC-42 2 · KF-KC-45 2 · KC-30 6 · KF-KE-15 3 · C-8 3 · C-13 2 · M-4 6 · m-4 4 · KF-CB-37 7 · SPF-22 3; the KAD zero-coverage fold rides `C-10`/`R-10`'s rows by its banked fold identity). The three census-derived rows and the three booked inbound edges (R-19c ii–iv) are additionally present, and the fourth inbound edge is declined with its owner named.

### 1.3 Escapes — ZERO

No id anywhere in the 58-record corpus carries a terminal disposition to KF.W8 and is absent from `KF-W8.md`. **Fold-transitivity closed independently**: five off-record occurrences of W8-terminal identities exist, and all five resolve as non-escapes on this seat's own read —
- `KF-CE-12` @ `kf-TimelineHoverPreview.md:112` — the **negative comparator** (*"a cluster banked for reaching into library-private `src/` (C-12/KF-CE-12 family)"*);
- `KC-30` @ `kf-KeyframeCard.md:75` — a **different id**, `KF-KC-30` (two dead glass `<Label>`s → NO-WAVE-OWNER);
- `KAD-24` @ `kf-SpringPhysicsFacet.md:125` — a provenance note about RR-2's contribution; the routing itself is `:62`;
- **`N-11` @ `kf-SequencePlayhead.md:83`** — a **different N-11** (*"the manifest triangulation … **KF.W0 fold**"*);
- **`N-11` @ `kf-ChannelOptions.md:88`** — the substring of `L·N-11/N-12` inside KF-CO-33 (dead hooks/dead layout, not a colocation row).

### 1.4 Diff against the retired instrument (reported per R3-5)

Roster: **identical** to PASS-3's 32, id-for-id — two independent derivations agree. Two PASS-3 statements do **not** survive re-derivation: (i) its fold-transitivity sweep reports *"three off-record occurrences"*; there are **five** (the two `N-11` collisions above are new to this seat — both non-escapes, so the verdict is unmoved but the enumeration was short, and this spec now leans on that artifact by LAW B citation → **D-15**); (ii) its M-25 table certifies KF-AV-9's bounding note *"byte-exact"*; it is not (→ **D-5**).

### 1.5 §State's four commanded figures — all reproduce

`ls kf-*.md | wc -l` → **58** ✓ · `grep -l 'KF\.W8' kf-*.md | wc -l` → **18** ✓ · `grep -oE 'kf-[A-Za-z0-9]+\.md' KF-W8.md | sort -u | wc -l` → **13** ✓ (the round-3 cell's own figure, measured after its edits) · union of routed-source ∪ named-by-path → **24** ✓, with the **6** named-not-routed reproducing by name (`kf-App` · `kf-DemoGlobalChrome` · `kf-EditorShell` · `kf-EditorStartScreen` · `kf-SharePopover` · `kf-SpringTrace`). PASS-3's D-4 is **CURED**.

---

## 2 · ANCHOR + SUBJECT-IDENTITY — every citation re-resolved, and its target read for its subject

| receipt as written in KF-W8 | resolves? | subject present? | this seat's re-derivation |
|---|---|---|---|
| `KF-W5 §Carry · Arm D · row D-6` | ✅ | ✅ | `KF-W5.md:310`. **D-6 does enumerate twelve paths by name** — the round-3 re-cut's premise is sound. Addendum quoted by R-4 reproduces **byte-exact** (see D-10 on what rides beside it) |
| `KF-W5 §Gates · G-STRUCT` (leg 2) | ✅ | ✅ | `KF-W5.md:345`; states **12 stutters × rename-or-keep** and names KF.W8's G13 8-count as the narrower predicate. KF-W8 binds by gate id, asserts no number — **compliant** |
| `KF-W5 §Sequencing · "→ KF.W8" cross-edge`, legs (a)(b)(c)(d) | ✅ | ✅ | `KF-W5.md:374`. Leg (c) — *"`ingest/cssom.ts`'s split disposition defers to KF.W2's façade decision; KF.W8 owns its colocation."* — **byte-exact**. Leg (d) — *"If OP-4 resolves demo-side, B-16 becomes a **shadow-name** row for KF.W8."* — **byte-exact**. Leg (b) ✅. **But the six `:273`/`:219` coordinates riding these receipts are dead — D-4** |
| `KF-W6 §Sequencing · the "→ KF.W8" cross-edge` | ✅ | ✅ | `KF-W6.md:445`; `:417` **is blank**, exactly as the spec says. MOVE-vs-RESHELL GUARD present verbatim. `grep -c 'KF-CB-37' KF-W6-CARRY.md` → **0** ✓; CARRY `:317` = `\| demo/components/CopyButton.vue \| 113 \| modify \| S-7 reshell \|` ✓ |
| `KF-W6 §Carry · W6-C · PROSE-WITH-CODE` | ✅ | ✅ | `KF-W6.md:164`; both quoted family-law strings present (2 occurrences each) |
| `KF-W6 §Carry · W6-I`, the KF-KC-37+SP-18+KF-KE-53 row | ✅ | ✅ | `KF-W6.md:308`; honest-scoping verbatim *"graph width, NOT crash exposure; consistency, not bundle size"* **byte-exact** |
| `KF-W7 §Carry · P0 · Evaluate intake`, the SpringTrace C-3 row | ✅ | ✅ | `KF-W7.md:123`; route dissent *"route-by-mechanism: a value.js LIBRARY export-surface decision…"* **byte-exact** |
| `KF-W7 §Bounds`, the `package.json (keyframes.js)` row | ✅ | ❌ | **D-2** — the row exists; the sentence KF-W8 quotes from it does not |
| `KF-W7 §Bounds`, `test/demo/instrument/` shared-directory declaration | ✅ | ✅ | `KF-W7.md:68`; W7's four creates at `:63-66` ✓ (quotation truncated — **D-13**) |
| `KF-W4 §Bounds` `vitest.config.ts` row · item 3 (ME-32 ≡ R-10) · **commit 2** | ✅ | ✅ | `KF-W4.md:51` · `:158` · `:314`. Commit 2 reads *"demo vitest project + **plugin-vue REGISTRATION** … only `vitest.config.ts` changes — **and this commit is the witness KF.W8's G10 cites to discharge its RED condition**"* |
| `KF-W4 → KF.W8` cross-edge (the R3-8.2 reciprocal) | ✅ | ✅ | `KF-W4.md:261`; declares the discharge **by gate id** from W4's end. **The collision PASS-3 D-3 convicted is now declared at both ends** |
| `KF-W4 §Bounds` `package.json` row (DH-2 claim) | ✅ | ✅ | `KF-W4.md:47`: scripts (`check`, `check:lib`, `lint`, `test:*`) + devDeps ✓ |
| `KF-W3 §Bounds`, `package.json (:70 only)` (DH-2 claim) | ✅ | ✅ | `KF-W3.md:77` ✓ |
| KF-W3's title heading | ✅ | ✅ | `KF-W3.md:1` = *"# KF.W3 — Parser Consumption (GATED, never scheduled)"* byte-exact |
| `KF-W2 §Carry · F4 · The emit mirror (Tier D)`, the §4.4 lock | ✅ | ✅ | `KF-W2.md:434`; the `G-W2-8 … KF.W8 precedes` quotation **byte-exact** (`grep -c` → 1) |
| `KF-W2 §0 · OP-6` | ✅ | ✅ | `KF-W2.md:42`; *"**ORDERED: KF.W8 precedes (RULINGS R-16).**"* byte-exact |
| `KF-W10` FOLD-FORWARD `KF-W8-R-4-STRUCT-PAIR` + the three triggers | ✅ | ✅ | `KF-W10.md:92`/`:175`/`:177`/`:505`; W10 carries the triggers verbatim and **re-points to `KF-W8 §Sequencing · R-4`'s *"TRIGGER, named"* clause by §-heading, not by line** (`grep -o 'KF-W8\.md:[0-9]*' KF-W10.md` → ∅). KF-AT-26 limb (c) → KF.W8 at `:478` ✓ |
| census `CENSUS-2026-08-03.md` §(a)9 · `:64` · `:107` · `:142-144` · `:201` | ✅ | ✅ | all four re-read and **EXACT** |
| precepts `README.md:127-130` / `:132-135` | ✅ | ✅ | resolve as cited |
| `R1-05-demo-colocation.md:118` | ✅ as declared | — | not in the value.js tree; the spec says so and says *"cite, never re-mint"*. Correct posture |
| `KF-W5.md:219` · `KF-W5.md:273` (six sites, *"at this writing"*) | ❌ | ❌ | **D-4** — `:219` blank, `:273` = the unrelated `B-4 · KF-EST-5` row |

**M-25 depth — locks, riders, dissents, re-read at the registry.** Byte-exact: MISS-β2 **DISSENT 1** (`kf-TimelineCaret.md:136`) ✓ · SpringTrace **C-3** (`kf-SpringTrace.md:62`) ✓ incl. *"(my dist grep → 0)"* and the *"MINOR → KF.W7"* tail · the **KF-AV-28 standing rider** at both banks (`kf-AnimationVisualizer.md:35` ✓, `kf-PlaybackRibbon.md:36` ✓ with its *"'swap onto the primitive' is never sufficient as a cure"* caution) · **M-L3** (`kf-DemoGlobalChrome.md:70`, incl. *"have zero consumers repo-wide"*) ✓ · **L-16-RESCOPED + RR-2 M4** (`kf-EditorShell.md:79`, incl. *"the 'route through the barrel' remedy is REFUTED"*) ✓ · **KF-APP-41** (`kf-App.md:91`) ✓ · the C-22 rider's substance ✓ (its parenthesis — **D-12**). **NOT byte-exact**: KF-AV-9's bounding note (**D-5**). Anti-rename holds throughout; the kf-RibbonBar-vs-kf-ChromeDock `M-4` collision guard, the KF-AT-26 identity guard (legs (a)(b)(d)→W6, (e)→W10, matching `kf-AnimatedText.md:71` and `KF-W10.md:477-478`), the C-15 closure on m-7 and the KC-1 E-3 posture all hold.

---

## 3 · GATES — re-executed whole at `origin/master 81a56990`

**All fifteen reproduce their stated frontier numbers. Nothing was inherited.**

| gate | this seat's re-run | |
|---|---|---|
| G1 | cl.1 → **7 statements / 6 files**, byte-identical anchors (`useKeyframeOps:1` · `parseAnimationCSS:7` · `timelineEngine:1`,`:17` · `AnimationVisualizer.vue:45` · `helpers.ts:9` · `keyframeSelector.ts:5`); cl.2 → four greps over the four published entries, **four empty results**; `index.ts` `export *` → **0** | ✅ |
| G2 | eight symbols over four entries → **exit 1**; exports map = `.` + `./engine` exactly; `export *` → 0 | ✅ |
| G3 | `-- src demo` → exactly **3** (`format/format.ts:20` · `css-text.ts:58` · `keyframeSelector.ts:7`); the fourth homonym `test/engine/nan-frame.test.ts:39` real and out of scope; `emit/index.ts:53` exports `cssIdent`, **no serializer**; `compile/selector.ts:42` `export const namedSelectorToFraction`, re-exported `compile/index.ts:24` — the *"unpublished"* re-word is correct and the sibling's E-3 strike is named. PASS-3 D-9 **CURED** | ✅ |
| G4 | five `index.ts`; **all four clause greps exit 1**, all four barrels present; `transport/index.ts` 14 L, 3 `defineAsyncComponent` + 1 type export | ✅ |
| G5 | exactly **3** up-imports at `:31`, `:116`, `:229` (the third, `ChannelControls.vue:229 → ../KfPillTabs.vue`, is real) | ✅ |
| G6 | `demo/components/` non-recursive → `CopyButton.vue`, `instrument`, `playback` → **1** loose `.vue`; the owner-named idiom exists (`transport/{AnimationControlsGroup,TransportDock,KfPillTabs,ControlsPaneWrapper}/`) | ✅ |
| G7 | legs (i) **8** SFCs, (ii) **4** exports; the **re-cut import census** reproduces exactly — EditorShell **1** (barrel `App.vue:143`) · EditorStartScreen **2** (barrel + `EditorShell.vue:118`) · SharePopover **3** (barrel `MbabbMenu.vue:81` + `EditorHeader.vue:45`, `EditorShell.vue:117`) · **EditorHeader 0**; the CSS-comment false positive (`layout.css:15`) is real and now excluded. PASS-3 D-5's *command* half **CURED** | ⚠ **D-3** (the uncommanded conjunct) |
| G8 | leg (i) → **2**, both printed lines byte-exact; leg (ii) → 4 attribution lines in `AV:70-78` + 6 in the test at `:4/:5/:13/:35/:105/:127`; leg (iii) → **4 files** (`instrument:10`+`:15` · `keyframes:1/:3/:4` · `timeline:2-3` · `transport:3`), `shell/index.ts` no hit. PASS-3 D-1 **CURED** | ✅ (D-14 minor) |
| G9 | `git ls-tree -r --name-only origin/master \| grep -c '\.test\.ts$'` → **132** = `-- test` **130** + `-- bench` **2**; both orphans confirmed; `vitest.config.ts` re-read whole (library `test/**/*.test.ts` less `test/demo/**`; demo `test/demo/**/*.test.ts`; `benchmark.include` `bench/*.bench.ts`); **no `git ls-files` survives**. PASS-3 D-7 **CURED** | ✅ |
| G10 | `git show origin/master:vitest.config.ts \| grep -c plugins` → **0**; `@vitejs/plugin-vue ^6.0.7` present; **`@vue/test-utils` absent**; vitest `^4.1.8`; vue `^3.5.35`; 9 files under `test/demo/instrument`. The dependency-cite is reciprocated at `KF-W4.md:261`/`:314`. PASS-3 D-3 **CURED** | ✅ |
| G11 | `groupedShortcuts` defined `KeyboardShortcutsModal.vue:57`, consumed `:11`; `-- test` → exit 1 | ✅ |
| G12 | `-- test` → exit 1 for both dialogs | ✅ |
| G13 | `src/` → one entry ✅ · `basename==dirname` → **exactly the 8** ✅ · token sweep → **exactly 12**, D-6's twelve path-for-path ✅ · **leg (i) re-cut verified whole**: D-6's twelve resolve **12/0**; `lane-library.md §7.3` names **sixteen** and stands at **9 LIVE / 7 GONE**, the gone set exactly as printed; `1412ed8e` carries **six** of the seven (`easing-option→option` · `easing-registry→registry` · `css-animation→animation` · `resolve-if→conditional` · `resolve-function→function` · `waapi-options→options`) and `e7fcff3f` the seventh (`drag-2d→drag/2d.ts`). PASS-3 D-2 **CURED, and its replacement is correct in every limb** | ✅ |
| G14 | **11 lines / 10 files**, host list exact incl. `useMenubarMeasure.ts` DO-NOT-TOUCH; `useDragScrub` → exit 1 | ⚠ **D-9** (no writable host) |
| G15 | the struck round-2 command returns **exactly the 9 stated lines** (`:5 :7 :8 :13 :59 :69 :108 :113 :138`); leg 1 (coupling) → **7**, the exact convicted set; leg 2 (pad) → **exactly 2**, both the allowance; 151 L; `ChannelControls:186` Teleport w/ `defer`, `:189` `:key=…`, `:253` hand-rolled `defineAsyncComponent`. **GREEN is now reachable under the gate's own allowance.** PASS-3 D-6 **CURED** | ✅ |

**LAW A censuses (A-1…A-10), re-derived from the import graph at the frontier.** A-1..A-4 → **0 consumers** for all four barrels, and the resolution hazard the censuses print is real (`test/**` hits on `orchestration/timeline` / `sequence/transport` resolve to **library siblings**, not to `P`) ✓ · A-5 → **exactly the four R-1 names** (`KeyframeCard.vue:59` · `KeyframesEditor.vue:116` · `EasingTarget.vue:143` · `StartingStyleTarget.vue:87`), the `w1-rehearsed.patch:69` hit correctly non-import ✓ · A-6 **1** ✓ · A-7 **1** ✓ (and a whole-tree sweep confirms `TimelineCaret` has **no** test or comment cite for m-7's rename to correct) · A-8 **1** ✓ · A-9 **7** — five demo + two demo-lane tests, id-for-id, plus the two non-consumer homonyms ✓ · A-10 the demo half exactly G1 clause 1's seven, and the named non-member `resize-tracks.test.ts:31` real ✓ (**one library figure short — D-11**).

---

## 4 · POSTURE

| axis | finding |
|---|---|
| **W4 head honored** | ✅ and now **consistent with W4's bytes**. §State opens after KF.W4; G10 is an inbound-dependency cite on W4's commit 2; G11/G12 re-gated on that cite; `vitest.config.ts` carved to R-5's `measure` project alone; `package.json` narrowed to `+@vue/test-utils`. W4 declares the reciprocal at `KF-W4.md:261` and again at `:314`. The `npm run check` re-cut still composes (live `check` = `tsc --noEmit && tsc --noEmit -p tsconfig.test.json && npm run proof:structure`; KF-W8 authors no script) |
| **W3 gated-unscheduled** | ✅ the → KF.W2/W3 cross-edge quotes KF-W3's title verbatim and names **KF.W2** as the schedulable receiver, keeping the pair-name because the bank writes it |
| **KF-AV-28 present where governed** | ✅ both banked spellings byte-exact; the scope-word-vs-mechanism distinction stated and applied to all seven governed rows (KF-AV-8 · -9 · -18 · -35 · C-13 · KF-AV-24 · KF-AV-26) |
| **O-21 (not O-20) at W1's mint sites** | ✅ vacuous and correctly so — `grep -c 'O-20'` → 0, `grep -c 'O-21'` → 0. No stale mint leaks into this wave |
| **W10-seam five-edge table fulfilled at source waves** | ✅ for this wave's two limbs. Edge 5 (R-4's three re-open triggers) is **carried verbatim at `KF-W10.md:177`** and W10's inbound-dependency table exists (`§6.C-0`, `:429`), listing the W8 row at `:439` **anchored by §-heading, not by line** — the round-3 cure holds from both ends. KF-AT-26 limb (c) is routed to KF.W8 at `KF-W10.md:478` and carried here with its identity guard; the `KF-W8-R-4-STRUCT-PAIR` record carries the terminal verb `DECLINED-FOR-X·KF, carried forward` at `:175`/`:505` |
| **LAW B (no self-voiced closure)** | ⚠ **partially applied** — §Rows', §Excluded's and §Provenance's closure sentences are lawfully re-cut to cite the PASS-3 artifact *with its defects*; **two survive in spec voice — D-6** |
| **LAW A (import-graph census for every delete/repoint)** | ✅ present and, on re-derivation, materially correct — the strongest single addition of round 3 |
| **§Bounds bounds the wave's acts** | ❌ **D-1** (AnimationVisualizer), **D-9** (the G14 seam), **D-8** (grants without acts) |

---

## 5 · DEFECTS

### D-1 · HIGH — §Bounds carves `AnimationVisualizer.vue` at `:45`; the wave books three further acts inside it, and mis-names the one it must relocate
§Bounds' only row for the file is `… playback/AnimationVisualizer.vue` (**`:45`**) | **modify-carve** | *"the 7 deep-import statements"*. The wave nonetheless books, in that same file:
- **KF-AV-18 (KF.W8 arm)** — *"AV comment block `:70-:78` (≥4 value.js attributions)"*, a prose sweep in the same commit as the extirpation, and G8 leg (ii) measures it;
- **KF-AV-9 demo half** — *"wire relocation + debounce → unit h"*;
- **KF-AV-26** — *"extract the DOM-free block to a colocated `use*.ts`"* (the bank's `:122-191` inertia block).

None of `:70-78`, the wire, or `:122-191` appears in any §Bounds row. Worse, the KF-AV-9 cell states *"**`AnimationVisualizer.vue:45` is the only wire in demo/**"*. Re-measured this seat:

```
origin/master:demo/components/playback/AnimationVisualizer.vue:45:import { bumpLayoutEpoch } from "@src/animation/resolve/browser";
origin/master:demo/components/playback/AnimationVisualizer.vue:79:useResizeObserver(containerEl, () => bumpLayoutEpoch());
```

`:45` is the **import**; the **wire** is `:79` — and the bank says so in the very row this cell carries (`kf-AnimationVisualizer.md:50`: *"`:79` is the only wire in demo/ (census 2 hits, both this file)"*). So the act named "wire relocation" has its subject mis-anchored **and** out of bounds. This is precisely the R3-4 SUBJECT-IDENTITY check — *an anchor's content must mention its subject* — which round 3 certifies it ran *"over every symbol-bearing §Bounds row"*; run against this row the check fails, and the file's own G8 falsifier treats an edit outside an enumeration as a gate failure (*"a zero obtained by touching any third file is an out-of-bounds green"*).

### D-2 · HIGH — the DH-2 both-ends declaration quotes a KF.W7 sentence that does not exist
§Bounds' `package.json` row states: *"**KF.W7** — **"devDependencies only"**, and its **OP-4 claims the SAME `@vue/test-utils` add** (`KF-W7 §Bounds`, its `package.json (keyframes.js)` row · `§0 OP-4`)"*. Re-run at KF-W7's current bytes: `grep -c 'devDependencies only' KF-W7.md` → **0**. What that row actually reads (`KF-W7.md:61`) is *"modify-carve — **ONE line: `+@vue/test-utils` in `devDependencies`** ⟨narrowed at this repair round, RULINGS-3 R3-8.3 + §END DH-2⟩"*, and W7 struck the `+@vitejs/plugin-vue` half as a no-op in the same motion. KF-W7.md was written **15:39**, KF-W8.md **15:50** — the sentence was already gone when this file quoted it. The OP-4 half of the claim is true (`KF-W7.md:31`/`:302`); the quoted half is a **phantom verbatim inside the declaration whose entire purpose is to state each claimant's claim accurately** — the class this file convicts twice by name (R-1's phantom reciprocal; R-4's PHANTOM VERBATIM STRUCK).

### D-3 · HIGH — G7's GREEN and the ratified idiom state incompatible predicates over the same file, and neither is commanded
G7's headline and **KF.W8-I** agree: *"a barrel exports exactly its own directory"* / *"`shell/index.ts` **exports exactly its directory**, and every export has ≥1 importer."* G7's GREEN says something else: *"leg (iii) holds for every export **and the exported set is exactly the directory's SFCs that have an external importer**."* Measured at the frontier: the directory holds **8** SFCs, the barrel exports **4**, and the four non-exported SFCs all have importers —

```
AnimatedText          ← demo/components/instrument/shell/EditorStartScreen.vue:62
KeyboardShortcutsModal← demo/components/instrument/shell/EditorShell.vue:119
TypingDots            ← demo/components/instrument/shell/EditorStartScreen.vue:63
HeroAurora            ← demo/app/App.vue:147
```

Under the headline/idiom predicate GREEN requires three **unbooked** export additions (the wave books none; *"`AnimatedText` is discharged by staying"* answers the **move** question, not the export one), so the gate can never turn. Under the GREEN clause it can — but "external importer" is defined nowhere, and **the conjunct carries no command** while legs (i)–(iii) all do. A wave whose §Goal criterion is *"the colocation idiom is written down once and every banked colocation row is re-derived against it"* cannot leave its own idiom and its enforcing gate saying different things about the file both name; and the L-18 rider itself lists *"a 'ratified idiom' that blessed both directions"* as a likely base against this wave. PASS-3's D-5 cured leg (iii)'s command and left the conjunct exactly where it found it.

### D-4 · MEDIUM — six KF-W5 coordinates declared "at this writing" were dead at round-3 close
`:67` (×2, the `engine/animation.ts` ruling-subject row: *"at `:219` and `:273` at this writing"*), `:68` (the `ingest/cssom.ts` row), `:198`, `:349`, `:350`, `:357` all ride *"at `:273` at this writing"* for `KF-W5 §Sequencing · "→ KF.W8"`, and `:67` additionally rides `:219` for row D-6. Re-resolved at KF-W5's current bytes: **`:219` is blank**; **`:273` is the unrelated `B-4 · KF-EST-5` row**; D-6 lives at **`:310`** and the cross-edge at **`:374`**. `KF-W5.md` was written **15:49** and `KF-W8.md` **15:50**, so *"at this writing"* was false when written. The same repair round struck `:410` **and** its successor `:417` for KF-W6 by name, loudly, at five sites — and left six live KF-W5 spellings of the identical class untouched. (The receipts themselves survive, because the row/leg anchors are load-bearing; what fails is the measurement statement attached to them.)

### D-5 · MEDIUM — the §Rows cell labelled "bounding note verbatim" is not verbatim
KF-W8 prints, as KF-AV-9's *"bounding note verbatim"*: *"the eviction API is global-by-construction (**no scoped bump exists**) and the wire mirrors the library's own undebounced window-resize policy…"*. The bank (`kf-AnimationVisualizer.md:50`) reads: *"the eviction API is global-by-construction (**library-side — no scoped bump exists**) and the wire mirrors…"*. Two words are dropped with no ellipsis, and the dropped words are the ones that carry the cell's own conclusion (*"half of this row is a library-surface design question"* — i.e. why the library half routes to KF.W5). M-25 requires a quotation be verbatim at its source **or** say it is a paraphrase; R-4 in this same file applies that rule to itself explicitly. PASS-3 certified this quotation byte-exact, so the error has now survived four seats.

### D-6 · MEDIUM — two spec-voiced closure claims survive LAW B
LAW B bans, program-wide, any completeness/closure/zero-escape claim over an enumerable set asserted in a spec's own voice. Round 3 lawfully re-cut §Rows (*"; none dropped"*) and §Excluded (*"No registry-routed row is dropped — all 32 land above"*), and §Excluded even explains the principle: *"R-19c's law is satisfied **by those four rows**, not by a sentence claiming it."* Two sentences of the same shape stand: §Sequencing's inbound-edge table heading — *"**Inbound edges received (RULINGS R-19c) — each booked or declined, by id; none left neither.**"* — and the → KF.W5 cross-edge's *"**No leg is left neither-booked-nor-declined.**"* Both are closure-by-enumeration in spec voice over an enumerable set.

### D-7 · MEDIUM — the re-scoped L-18 blanket is still an over-claim
Round 3 struck *"15 gates · 15 literal commands · 15 falsifiers"* and replaced it with *"falsifiers are 15/15, commands are **13/15**"*, declaring **G13** and **G14** as the two exceptions. Falsifiers re-counted this seat: **15** ✓. But at least two more gates carry a GREEN conjunct with no command: **G7**'s *"the exported set is exactly the directory's SFCs that have an external importer"* (D-3), and **G8** leg (ii), whose green condition — *"every surviving line names a symbol value.js actually exports"* — is a semantic judgment the `git grep` cannot decide (the command's raw output for `AnimationVisualizer.vue` is five lines, one of which, `:46` `clamp` from `@mkbabb/value.js/math`, is a genuine import that passes the predicate for a reason no command states). The blanket was re-cut precisely to stop describing a suite the file does not have; the new figure describes a different suite it does not have.

### D-8 · MEDIUM — write access granted where no act is booked, the class round 3 just cured for `package.json`
Two rows survive the narrowing that killed the `exports · sideEffects · scripts` over-reach:
- **`test/demo/instrument/`** — the **nine** tracked files are granted **modify-carve**, while the row's own DH-3 text names exactly **one** cure site (`resize-tracks.test.ts`) and declares that **KF.W7 holds the same nine read-only**. G10's falsifier needs only that *"the demo project's 9 existing test files stop being collected"* — a read, not a write. Eight files carry write access with no booked act, in a directory the row itself says no wave owns.
- **`demo/components/instrument/shell/index.ts`** — granted **modify-carve** (*"4-of-8 barrel"*), while G7's ownership guard reserves every edit to that barrel to NO-WAVE-OWNER (*"This gate **measures** the barrel's export/importer symmetry and **may not spend their cure**"*), and the wave's own dispositions (AnimatedText stays; HeroAurora leaves the directory) require no barrel edit at all.

### D-9 · MEDIUM — G14's cure has no writable host, and the seam it must write is bounded read-only
G14 convicts hand-rolled rect-ratio projectors outside `useDragScrub`, and its falsifier states the cure is **consolidation into the seam** (*"a green obtained by driving the raw `getBoundingClientRect()` count down rather than by consolidating projection — moving a bare rect read into the seam, or into a new helper, leaves the projector count unchanged and fails"*). Of the ten measured hosts, **zero** appear in §Bounds with write access (`AnimationVisualizer.vue` is carved at `:45` — D-1; `useMenubarMeasure.ts` is DO-NOT-TOUCH; the six scene hosts and `useZoomPan.ts` are absent entirely), and the destination — `demo/composables/useDragScrub.ts` — is bounded **read-only** (*"seam charter"*). Unit h's KF-AV-24 act therefore has no lawful landing surface inside this wave's own bounds.

### D-10 · MEDIUM — R-4 re-quotes D-6's "16 stutters" as the verbatim of record without the correction that sits in the same cell
R-4 prints, as *"**The verbatim of record**, re-read at `KF-W5 §Carry · Arm D · row D-6` … and reproducing byte-exact"*: *"…; the rename programme (**16 stutters**) executes here, whole."* The quotation is byte-exact ✓ — and the very cell it says it re-read carries, immediately beside it, the dated round-2 rider that corrects the figure: *"**Re-measured at the ref of record the denominator is 12** … the correct statement of the ruling is: *the rename programme executes here WHOLE, over the stutter set as measured at open* — the **set**, not the number, is what R-14 subordinated."* KF-W8 kills the same `16` twice elsewhere (D-19 row 5: *"16 is UNREPRODUCED"*; G13: *"The banked 16 does not reproduce at `81a56990`"*) and reprints it unqualified here, in the ruling a later seat will read for the decline's grounds. Carrying a lock verbatim is right (E-3); carrying it without the correction its own authority attached is the M-25 half left undone.

### D-11 · LOW — LAW A census A-10 under-counts one module's library consumers
A-10 records `compile/emit/css-text` as *"3 library + 5 test consumers"*. Re-derived this seat, the library consumers are **four**: `engine/css/metadata.ts:35` · `resolve/browser.ts:4` · `resolve/conditional.ts:2` · `resolve/function.ts:6` (the five test consumers reproduce exactly, as do `internal/helpers`' six library consumers, `resolve/browser`'s one, and `compile/selector`'s one). The demo half — the census's actual subject — is correct, so no act moves; but the figure is pasted as a derived output under a law whose whole point is that pasted outputs are re-runnable.

### D-12 · LOW — the C-22 rider quotation closes a parenthesis its source does not close there
KF-W8 (twice, at G7 and §Excluded): *"it does NOT ship (rolldown shakes the unused barrel member**)** … while the orphan `--header-items-max-w: 500px` DOES survive into the shipped cascade"*. The bank (`kf-EditorShell.md:95`) reads *"(rolldown shakes the unused barrel member**; markers absent from every built chunk**), so deletion is source-hygiene, while the orphan…"*. The elision manufactures a closing paren the source does not have at that position. PASS-3 saw this and recorded it *"noted, not booked"*; it is still an unmarked elision inside a rider the file says it carries verbatim.

### D-13 · LOW — the DH-3 quotation of KF-W7 is truncated without an ellipsis
KF-W8 quotes W7 as declaring *"a SHARED directory this wave does NOT own — paths are enumerated, never globbed"*. `KF-W7.md:68` reads *"… paths are enumerated, never globbed **by shared prefix**"*. The dropped words are the operative qualifier of the law being invoked.

### D-14 · LOW — G8's two enumerations of the transport chunk-comment disagree with each other and with the command
The *RED today* paragraph prints the four surviving chunk-rationale comments as *"`instrument/index.ts:15` · `keyframes/index.ts:4` · `timeline/index.ts:3` · **`transport/index.ts:2-3`**"*, while leg (iii)'s own enumeration in the same gate gives *"transport/index.ts:3"*. Re-run, the command returns `transport/index.ts:3` only (the `Monaco/highlight.js` token is on `:3`; `:2` carries `defineAsyncComponent`). One of the two spellings is wrong in a gate whose falsifier turns on *"the surviving copy convicts"*.

### D-15 · LOW — the artifact this spec now cites for its closure claim has a short fold-transitivity enumeration
Under LAW B the file's carriage claim lives at `PASS-3/KF-W8-CHECK.md §1.2–§1.3`, cited three times (§Provenance, §Rows, §Excluded). That artifact states its fold-transitivity sweep opened *"three off-record occurrences"*. A fresh sweep finds **five** — adding `N-11` at `kf-SequencePlayhead.md:83` (a different N-11, *"**KF.W0 fold**"*) and `N-11` inside `L·N-11/N-12` at `kf-ChannelOptions.md:88` (KF-CO-33, dead hooks/dead layout). Both are non-escapes, so the cited verdict (**0 escaped**) is unchanged and is independently reconfirmed here — but the citation should be re-pointed to this pass, whose enumeration is complete.

---

## 6 · WHAT HOLDS

Recorded so no later seat re-litigates settled ground. **The census is CLEAN by record** — 32 routed / 32 booked / **0 escaped**, derived without the retired partition and agreeing id-for-id with the independent PASS-3 roster, with the fold-transitivity class closed over five occurrences rather than three. **All fifteen gates reproduce at the frontier**, and every one of PASS-3's four convicted gates and two flagged gates is genuinely cured at the bytes: G8 now enumerates both `editor-shell/` sites and bounds both; G13's replacement leg is correct in **every** limb (D-6's twelve resolve 12/0; the lane's §7.3 names sixteen and stands at 9 live / 7 gone with the exact gone set; `1412ed8e` carries six renames and `e7fcff3f` the seventh); G9's oracle reads `origin/master` and its 132 reconciles as 130 + 2; G15's re-cut makes GREEN reachable under its own pad allowance with the born count (9 = 2 + 7) stated and true; G7's leg (iii) is a real import census whose per-export counts are exact; G10 is an inbound-dependency cite **reciprocated at `KF-W4.md:261` and `:314`** — the collision PASS-3 called undeclared is now declared identically at both ends. **LAW A is the round's strongest addition**: ten censuses, all of them re-derivable, two of them (A-9's seven consumers, A-10's relative-spelling non-member) net-new findings against the file's own earlier text. **Every cross-spec §-heading, gate-id, row-id and OP-id anchor resolves** at the siblings' current bytes, and every registry verbatim but one is byte-exact. **Ten of PASS-3's thirteen local defects are fully cured, three partially** (D-5 → this pass's D-3; D-11 → D-7; D-8's class → D-8). **The residue is bounds and quotation, not measurement** — the file now measures the tree accurately and does not yet bound what it has decided to do to it.

---

*Instruments (all read-only, zero writes to product source): `git rev-parse` · `git grep` · `git ls-tree` · `git show` · `git cat-file -e` · `git log` · `git show --name-status -M` · `git status --porcelain` at `/Users/mkbabb/Programming/keyframes.js`; `grep`/`sed`/`awk`/`comm`/`sort` over the value.js registry, formation, carry and wave trees. Frontier ref `origin/master 81a56990736ced5b5edde0b84c527680ac7689b1`, re-resolved at the head of this pass; local `8281638c` opened only to confirm MISS-α5's one-sided file. No product source was opened for writing; no registry record and no wave spec was edited; the only file written by this seat is this register.*
