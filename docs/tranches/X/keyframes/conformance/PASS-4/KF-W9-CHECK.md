# KF-W9-CHECK — PASS-4 (fresh adversarial spec check, L-18/L-20)

**Subject** `docs/tranches/X/keyframes/waves/KF-W9.md` (429 L, X.KF.W9 — Safari Visual Audit, frontend half), **thrice repaired** (rounds 1/2/3).
**Seat** FRESH. Nothing inherited. **The inherited nine-block partition is RETIRED**; the census below derives from the 58 records themselves. Not PASS-1's 167, not PASS-2's 147, not PASS-3's 139 — this seat re-enumerated by record with its own stated rule and its own scripts. PASS-3's register and RULINGS-3 were read only to know what has already been ruled, never to inherit a figure.
**Substrate** keyframes.js `origin/master` **`81a56990`** (`git rev-parse origin/master` → `81a56990736ced5b5edde0b84c527680ac7689b1`, exact). Local HEAD `8281638c` cited ONLY as declared historical context. **TREE LAW: a witness that resolves only at `8281638c` convicts.** value.js at current tree bytes.
**Corpus** the 58 `registry/adjudicated/kf-*.md` records (`ls kf-*.md | wc -l` → **58**). Sole carry `carry/KF-W6-CARRY.md`.
**Verdict** **DEFECTIVE** — 7 defects (3 HIGH · 2 MEDIUM · 2 LOW). **Axis 1 (census) is CLEAN at 168/168.** Axis 3 (M-25 depth) is CLEAN. The convictions are on axes 2, 4 and 5, and **all three HIGHs land inside round 3's own repair work**.

---

## §1 · Axis 1 — ID-KEYED CENSUS BY RECORD (partition retired; rule stated; escapes by bytes)

### The rule, stated so pass 5 can disagree with it precisely

A **routed (record, id) pair** is either:

1. a **roster-level row** — bullet (`- **id** …`) *or* table row (`| **id** | … |`) — in a record's defect-roster surface, **whose text names `KF.W9`** (routing a cure, a witness, a datum or a posture to this wave); sections excluded by name: routing-summary, killed-claims, identity-guards, dissent, verdict, superlatives, scoreboard, corrections/rulings registers; **or**
2. an **id named inside that record's `**KF.W9**:` routing-summary segment** (all shapes: standalone bullet, and multi-wave `— **KF.W9**: … —` runs on one line), differenced against (1).

Both extractions are required: **the corpus mixes bullet-shaped and table-shaped rosters** (kf-App, kf-TypingDots, kf-HeroAurora, kf-EditorStartScreen, kf-CubeAxisLines, kf-MatrixEditor and kf-SequenceAxis are table-shaped in whole or part), and **21 records** carry a routing segment whose ids are not all reproduced on their own rows.

### Measurement

| cell | measured this seat |
|---|---|
| records in corpus | **58** |
| records mentioning `KF.W9` at all | **52** (`grep -c` per file; 260 lines) |
| records with a `**KF.W9**` routing segment | **21** |
| roster rows naming KF.W9 (rule 1) | **145** over 40 records |
| **routedTotal — distinct (record, id) pairs (rules 1 ∪ 2)** | **168 over 40 records** |
| **bookedCount** | **168** |
| **escapedCount** | **0** |

*On the denominator's shape.* PASS-1 reported **167** roster-level ids, PASS-2 **147** record-scoped pairs, PASS-3 **139**, this seat **168**. Four seats, four numbers, one corpus — they are **granularity variants** (fold-limb splitting, combined-id treatment, table-row inclusion, routing-segment differencing), not rival truths. **168 is this pass's enumeration of record and the discharge below is keyed to it.** The spec's own §Carry-head statement — *"79 is the bullet count, never a claim that 147 = 79 or that 167 = 79"* — survives this pass unchanged and is CORRECT as stated. **No pass-5 seat should inherit 168 either.**

### Discharge — the four lawful mechanisms

| route | mechanism | verified |
|---|---|---|
| (a) | §Carry bullet by id | **79** bullets, re-derived by script this seat: **A 18 · B 5 · C 1 · D 8 · E 7 · F 13 · G 19 · H 8 = 79** — the spec's figure reproduces exactly |
| (b) | named fold identity inside a §Carry bullet | KF-CE-13's fold list · D-15's RTL fold list · D-16's sequence-family list · D-21's timeline folds · MISSED-F's witness list · the KF-APP-5/-6 folds · the D-8+M-7 MatrixEditor folds |
| (c) | §Excluded row with a named receiving owner | 9 routes |
| (d) | line-item inside the record's OWN `UNPROVEN-NEEDS-LIVE` / `SS-13` residue, **record attribution enforced** | the balance |

**A machine sweep over all 168 pairs found 0 with no discharge under any of the four.** A stricter sweep — requiring, for routes (a)/(b), a spec LINE carrying *both* the id and the record name — flagged **19** candidates. All nineteen discharge on manual adjudication at the bytes. Recorded so pass 5 does not re-book them:

| candidate | resolution (read this seat) |
|---|---|
| kf-AnimationVisualizer **KF-AV-20** | (b) — `KF-AV-20/D-9` in §B's KF-CE-13 fold list |
| kf-CopyButton **KF-CB-32** | (b) — `KF-CB-32/D-19`, same list |
| kf-KeyframesEditor **KF-KE-43** | (b) — `KF-KE-43/D-31`, same list |
| kf-SpringPhysicsFacet **SPF-28** | (b) — `SPF-28`, same list |
| kf-SpringScene **KF-SS-36** | (b) — `KF-SS-36/D-25` in §C's RTL fold list |
| kf-CubeScene **MISSED-F** | (b) — §D's MISSED-F ⟨kf-AmigaScene⟩ names *"cube (B's UNPROVEN folds by reference)"* |
| kf-OrbitalDrag **OD-35** | (b) — *"OD-35's stage-wide trap"* in §D MISSED-F's witness list |
| kf-SquareScene **RB-1** · kf-PlaybackRibbon/**kf-SequenceTarget/kf-StartingStyleTarget KF-CE-13** | (b) — the §B anchors, cited by the routing segments as x-refs |
| kf-PlaybackRibbon **M-1** | (a) — the bank writes the row `D-3 + DU-M-1` at `kf-PlaybackRibbon.md:47` and `D-3+M-1` at `:142`/`:155`; the spec carries the **:47 spelling**, which is the roster-row id. No rename. |
| kf-SpringTarget **D-8** | (d) — that record's residue **#4** (*"Contrast reads over the live glass plate … `.spring-target-marker` (50%) and `.sampler-ball` (65%), the two rows near the 3:1 floor"*), and the bank itself routes it there (*"Near-threshold rows → SS-13 #4"*, `:57`) |
| kf-SpringHeatmap **#8** | (d) — the routing segment's own *"witness list (SS-13 #1-#8)"*, that record's 8-item residue |
| kf-StartingStyleTarget **KF-SST-30** | (a-adjacent) — carried at **S-10**'s SS-6 relay enumeration |
| kf-OrbitalDrag · kf-SequencePlayhead · kf-SpringTrace · kf-SquareInstrument · kf-SquareScene **SS-13** | (d) — §H's SS-13 aggregate; each record's own residue |

A second machine pass (token-absent-from-spec) flagged 28 rows; all 28 resolve, the interesting five being **kf-App KF-APP-2/-3/-7/-8/-11** (route (d) — that record's residue items **2 · 3 · 6 · 7 · 8**, each naming the id), **kf-ChromeDock D-2-RESCOPED** (residue **#7**, *"D-2-residue"*) and **C-10** (residue **#8**), and **kf-TypingDots KF-TD-4** (residue **U-4**, *"KF-TD-4's confirmatory pair"*).

**AXIS 1 VERDICT: CLEAN — 168 routed, 168 discharged, 0 escapes.** Three repair rounds have closed this axis: the class sweeps of rounds 2 and 3 (PASS-2's R2-10 trio) were its last members, and a fourth independent enumeration — at a *larger* granularity than any prior pass, deliberately, to give escapes more room to appear — surfaces none.

### Counting family, re-derived at the bytes (never inherited)

| cell | spec claims | this seat measures | |
|---|---|---|---|
| §Carry bullets | 79 (18·5·1·8·7·13·19·8) | **79**, same per-section split | ✅ |
| live gates | 13 (`-1..-9`, `-11..-14`); `-10` struck-in-place | **14 rows present, 13 live**, `G-KFW9-10` struck, id retained | ✅ |
| §Bounds data rows | 16 (rule: rows less header less separator) | **17 pipe rows − 1 header = 16** | ✅ |
| escalation triggers | 13 armed over 12 records | **13** ` · `-separated armed items | ✅ |
| negative register | 6 records · 11 probes · 4 traps | **6 · 11 (1+4+2+2+1+1) · 4** | ✅ |
| dissents | 11 row-level + 1 wave-level = 12 | **11** `Dissent preserved` inside `- **` §Carry bullets (14 occurrences file-wide; the scope rule the spec states is correct) **+ 1** | ✅ |
| enumerated residue | 565 by the AMENDED command | **565** — and the amended command **reproduces**: one script, both variants, over all 58 records → `h2-only 563 · h2+h3 565 · records-with-items 57 · files 58`; the delta is **kf-ChannelOptions +2**, exactly as named; **kf-CubeAxisLines is the sole zero-item record**, exactly as claimed | ✅ **D3-6's cure is CORRECT and independently reproduced** |
| prose-carried residue | 9 + 8 + 8 = 25 | anchors resolve verbatim: `kf-CubeAxisLines.md:104` *"r1's nine-item queue carries with three sharpenings"* · `kf-TransportDock.md:122` *"1–8 **carried from r1**"* · `kf-CubeTarget.md:83` *"Carried: r1 items 1–4 and 6–8 unchanged"* | ✅ |
| cross-edges | *"the set is enumerated at S-10 and its numeral stated ONLY there"* | **S-10.1 enumerates 13, numbered**; the two former assertion sites now point here | ✅ **D3-5's cure is CORRECT** |
| **KF-CE-13 folds** | **fourteen**, at three sites | **16** by the row's own ` · ` enumeration | ❌ **D4-3** |

---

## §2 · Axis 2 — ANCHOR + SUBJECT-IDENTITY (every citation resolves AND its target mentions its subject)

This axis is stricter than PASS-3's: a citation passes only if the coordinate resolves **and** the destination text is about the subject the citing sentence claims. Re-executed this session:

**Sibling specs — RESOLVE, and the target names the subject:**

- **KF-W4 §Sequencing head-law** — present; the §-anchor holds and no numeric convenience rides it ✅
- **KF-W0 §Sequencing packet-roster row** — `KF-W0.md:588`; the "verbatim" quotation resolves **exactly**, word for word: *"this file previously enumerated **15**; the canonical roster is **17** = W0's 15 ∪ {**amiga**, **EDITOR-UNIT**} (KF.W9 S-11's adds)"*, and the row's own cell adds *"Homing is discharged by KF.W4's lists, not by this wave"* — **agreeing** with W9's S-11 ✅
- **KF-W6 §Carry KF-HA-10** — `KF-W6.md:342`, subject-exact: *"AN OWNER RULING QUESTION, not a fix order … Frame cost → KF.W9"* ✅; sibling half at `KF-W6-CARRY.md:220` ✅. R2-10's conditional sibling correction correctly reported NOT triggered.
- **KF-W5 B-6 cross-citation (round 3's new claim)** — **TRUE and symmetric**: `KF-W5.md:275` anchors B-6's caller at *"declared `engine/play-lifecycle/frame.ts:121`, the call at `:137`, the rAF lane"*; `KF-W5.md:162` (§Bounds) and `:335` (G-PRM-FLIP) both anchor the docblock at **`strategies.ts:66-75`** over the declaration at **`:76`**. W9's *"the two specs now name the same file and the same lines"* is **measured true**, both directions ✅
- **KF-W10 — the W10-seam edge (see §5)** — `KF-W10.md` §6.C-0 row **4** consumes exactly what W9 §S-10 / §S-8(iii-a)(iii-b) / §Bounds now declare, and records it **MEASURED — CURED AT THE SOURCE THIS ROUND** ✅
- **KF-W3** — `KF-W3.md:1` = *"KF.W3 — Parser Consumption (GATED, never scheduled)"* ✅
- **`X/waves/W6.md`** — `:354` = `## 8. Verification Artefacts`; **MATRIX** is a bolded inline label at `:367` inside that section's π-obligations paragraph, and the spec now cites it as exactly that ✅ **D3-7's cure is CORRECT**
- **`X/waves/W11.md`** — `:290` = `### G8 — Real-Safari evidence cell, held separate · **BORN-RED**` ✅; the shared-apparatus/separate-cells reading holds at `:26`/`:70`/`:95` ✅

**Registry banks — RESOLVE verbatim, subject-exact:** `kf-AmigaScene.md:42` (D-2) · `:47` (D-7's *"`reducedMotionScale` numeric-intensity resolver exported on the LIGHT barrel — index.ts:36–52 read by this seat"* — the bank's narrower wording the spec now honours) · `:54` (MISSED-A) · `:158` · `kf-HeroAurora.md:52` · `kf-RibbonBar.md:57`/`:65`/`:133` (the anti-rename trail; the two `D-15`s are genuinely distinct banked ids) · `kf-SpringTarget.md:63`/`:76` (the one-binding lock, verbatim to the closing `**`) · `:147` · `:148` (the *"D-6 heading · D-14 status region · N-4 name+valuetext"* one-edit family — resolves inside that line) · `kf-SpringHeatmap.md:37`/`:130` · `kf-ControlsPaneWrapper.md:77`/`:94` · `kf-CSSCodeEditor.md:85` · `kf-SequenceScene.md:153` · `kf-EditorShell.md:70` · `kf-ChannelOptions.md:174`/`:223`/`:225`/`:226` (the h3 section and both item coordinates, exact).

**The KF-AV-28 rider at all three banked coordinates — RESOLVES VERBATIM, each carrying its own reach** (a claim worth re-checking because the round-2 widening rests on it): `kf-AnimationVisualizer.md:35` *"A standing sequencing rider on every NO-WAVE-OWNER row below: **KF.W7's S-9 evaluation (KF-AV-28) may supersede any behavioral cure here**"* · `kf-PlaybackRibbon.md:36` *"Standing rider: **KF.W7's timeline-evaluate verdict may supersede any NO-WAVE-OWNER cure below** (the KF-AV-28 rider)"* — **and its §7 caution rides on the same line**, exactly as the spec carries it · `kf-SequenceScrubber.md:36` *"KF-AV-28's standing rider applies: the KF.W7 ScrubberTimeline evaluation **gates the component-shape rows**"* ✅ The widening's warrant is sound.

**Non-registry authorities:** `lane-frontend.md:462` = `### 6.5 prefers-reduced-motion — 13 enforcement sites across 12 files`, verbatim, and it IS §6.5's heading ✅ · `INTAKE-ADJUDICATION-2026-08-03.md:138` = `## §3 — CARRY table` ✅ and `:161` carries *"73,568 Kronecker denominator**; KF.W9 scopes its own surface list"* ✅ · `intakes/lane-keyframes-b10-b21.md:151` is exactly the B18-26 row ✅ · arithmetic re-executed: `152×11×4×11 = 73568` ✅, `46816+26752 = 73568` ✅.

**keyframes.js docs:** `OWNER-DECISIONS.md:7` (OD-V3 — *"both transport homes — in-panel card and floating pill — on all four duplicating scenes at 390 AND 1280, real Glass 7, both options mocked or annotated"*, verbatim) ✅ · `:9` (OD-V5 deferred pending glass's dock mark) ✅ · `FOLD-FORWARD.md:34` (*"subsumes the folded CH2-02 ×4 (BG-5, GU-1, GU-2, subject-legible)"*) ✅ · `docs/published-surface.md:35` (*"manifest-only: K.W11 PHYS-E"*) ✅ — the spec's reading of this as the likely inheritance source for *"used by nothing"* is fair: the row goes on to say *"the consumer can read it to scale its own non-spring motion"*, which is a **manifest** statement, not a usage claim.

**Convicted on this axis:** **D4-4** (the `shadowTick` pinpoint).

---

## §3 · Axis 3 — M-25 DEPTH (locks, riders, dissents)

Spot-audited at the banks, verbatim, this seat:

- **D-14 + N-4 one-binding lock** — `kf-SpringTarget.md:63` closes *"**NO-WAVE-OWNER + KF.W9 (one binding, with N-4).**"* and `:76` closes *"**NO-WAVE-OWNER + KF.W9 (with D-14).**"* ✅ — verbatim to the trailing emphasis; the "never split" gloss is a faithful reading.
- **The a11y one-edit family** — `:148` carries *"the **a11y one-edit family** (D-6 heading · D-14 status region · N-4 name+valuetext)"* ✅ inside the NO-WAVE-OWNER packet line, exactly as cited.
- **D-B3 lattice lock** — `kf-SpringHeatmap.md:37` ✅, carried twice (D-B3 and D-m8) as the bank requires.
- **S-14's N-1 time-domain lock** — routed at `kf-SequenceScene.md:153` and **entered in §Sequencing**, not merely mentioned in §Carry ✅; its scope statement (D-12's uppercase limb and SpringTarget N-4 explicitly NOT governed) is correct against both banks.
- **The amiga cure-shape lock** — *"a **shared subject-a11y idiom for cube AND amiga**"* carried, not cited ✅; route-(d) unavailability correctly demonstrated against that record's own twelve-item residue.
- **KF-AV-28** carried whole with its governed set enumerated by id, and D-24 named as a not-yet-governed datum ✅.
- **Dissents** — 11 row-level instances inside §Carry bullets, matching the roster's own scoped rule; the three R-1e additions present and named; the three round-2 rows correctly carry none.

**AXIS 3 VERDICT: CLEAN.** No lock paraphrased, no rider cited-instead-of-carried, no dissent dropped. Three passes have not moved this axis and this one does not either.

---

## §4 · Axis 4 — GATES born-RED with REACHABLE GREEN · LAW A · LAW B

### Born-RED witnesses — re-executed and TRUE at `origin/master 81a56990`

- **G-KFW9-1** — `git ls-files …/audit/visual/safari-real/` → **4** tracked (`MATRIX-SAFARI.md`, `ROUTE-MATRIX.json`, `STATE-MATRIX.json`, `picker-safari26.4-light.png`) vs **31** on disk; `shots/` **0 tracked / 11 on disk** ✅ exact, this seat.
- **G-KFW9-8** — `git grep -c forced-colors origin/master -- demo/` → **no hits (exit 1)** ✅; PRM sites in `demo/` → **19 files** (≥ the claimed 13+) ✅.
- **G-KFW9-9** — `design-idioms.css:76` `.focus-ring:focus-visible` (`:77` its box-shadow, `:76-79` the rule) **and** `playback-idiom.css:72` `.btn-playback:focus-visible` ✅ — two different selectors, both unlayered, both `outline: none`.
- **G-KFW9-10's surviving measurement** — `SequenceTarget.vue:97` = `class="seq-handle"` ✅; `SequenceTarget.css:163-167` = the forked `.seq-handle:focus-visible { outline: none; box-shadow: var(--focus-ring-shadow); … }` ✅; **the fork's justification is confirmed void** — `proof:sequence-rows-draggable` at the frontier has **no `package.json` entry** (`git grep -c … -- package.json` exit 1) and **no script or workflow** (`… -- .github/ scripts/` exit 1) ✅.
- **G-KFW9-14** — both shas re-derived exact ✅.
- **Library reference (the R3-7 re-anchor) — CORRECT and complete except one pinpoint**: `frame.ts` `playFrame` decl `:121` ✅, `withReducedMotion` flip `:131` ✅, `snapToReducedMotion(anim)` `:137` ✅ · `strategies.ts` docblock `:66-75` ✅ (closing sentence verbatim), decl `:76` ✅, final-frame `beginPlay(anim._playback, () => withReducedMotion(` `:109` ✅ · `index.ts:50` ✅ · `group/group.ts:57` ✅ · `constants/defaults.ts:87` ✅ · `waapi/delegation.ts` loop `:64` ✅, **decl `:54` ✗ — see D4-4**. `withReducedMotion` is **absent from `delegation.ts` entirely** (exit 1) ✅, so KF-TD-1's mechanism claim is measured true.
- **The R3-9 consumer census — EXACT**: `git grep -n '\breducedMotionScale\b' origin/master -- src/ demo/ test/ scripts/` → **6 hits** (export `index.ts:50`; definition `internal/reduced-motion.ts:125`; `physics/spring/progress.ts` import `:2` + assignments `:153`/`:232`/`:385`); `-- demo/` → **no hits**; the spring precedent verified whole (`amplitudeScale` field `:100`, applications `:323`/`:352`) ✅. **D3-2's cure is CORRECT.**
- **§Bounds PRM surface** — every member re-resolved at the frontier by `git cat-file -e`, **15 of 15 OK**, including the D3-8 repair `demo/scenes/cube/orbital-drag/OrbitalDrag.vue` ✅. **D3-8's path cures are CORRECT.**

### REACHABLE GREEN — the new clause

Twelve of the thirteen live gates close on acts inside this wave's grant (a capture, a published list, a register, a recorded grep, an addendum). **One does not: G-KFW9-6 — see D4-2.** G-KFW9-9 is the control case: round 1 re-cut it to close on *"the born-RED witness pair only"* with the AFTER witness deferred (*"re-shot here once KF.W13's act lands"*), and it is reachable. G-KFW9-13 is reachable **only if §Bounds grants the write it needs — see D4-5.**

### LAW A — import-graph censuses for every delete/repoint

The scope statement is **correct and well-drawn**: this wave authors no such act and censuses the three it carries as declarations. Census 1's headline finding (*the specifier is unchanged; no consumer repoint is owed; `snapToReducedMotion` is barrel-EXCLUDED so KF.W5's cure lands in `strategies.ts`*) is **measured true** — 2 import consumers, `animation.ts:37` and `option-setters.ts:27`, exactly ✅, and the barrel docblock names it a module-internal cross-file export ✅. Census 3's headline (`seq-handle` **11 hits / 2 files**, one template consumer, eight further style obligations, the fork's justification void) is **exact** ✅. Census 2's `focus-ring` headline (**11 hits / 7 files**) is **exact** ✅.

**But three of the three censuses carry a count or a scope statement their own pasted command refutes — see D4-1.**

### LAW B — no self-voiced closure claims

**Compliant, and conspicuously so.** The round-3 re-homing is real work, not a re-wording: the header, the §Carry-head correction block, §F's class-sweep paragraph and the §Bounds PRM row all now cite `PASS-3/KF-W9-CHECK.md §1` **with its defects** rather than asserting closure. The one place the file still states a completeness figure in its own voice — S-10.1's thirteen — states it **at its own enumeration with its counting rule and two alternative granularities disclosed**, which is the cured idiom. The self-deprecating disclosure that *"that the enumeration lands on the same numeral the un-enumerated cells asserted is a coincidence at this granularity, not a confirmation of them"* is exemplary and should not be edited away.

**Convicted on this axis:** **D4-1** (LAW A arithmetic) · **D4-2** (unpassable oracle) · **D4-3** (fourteen-vs-sixteen) · **D4-5** (§Bounds forbids G-KFW9-13's write).

---

## §5 · Axis 5 — POSTURE

| clause | finding |
|---|---|
| **W4 head honored** | ✅ **by construction, and re-verified**: §Bounds carries **zero** kf write grants (all five EXECUTION-TIME-ONLY grants struck and each marked READ-ONLY); S-13's triumvirate fence is never armed by this wave's own gates. *Qualified by D4-2*: G-KFW9-6's close condition still names cure acts, which is a residue of the pre-R-9a posture in the one gate the three repair rounds never re-cut. |
| **W4's `npm run check` re-cut composes** | ✅ W4 re-cuts `npm run check`; W9 invokes **`npm run gh-pages`** only — a disjoint script, present at `package.json:43` = `"gh-pages": "vite build --mode gh-pages"`, re-read at the frontier this seat. **R-11's repair is CORRECT**; `build:gh-pages` exists at no coordinate. No collision. |
| **W3 gated-unscheduled** | ✅ in substance — `KF-W3.md:1` is *"(GATED, never scheduled)"* and W9 schedules nothing against it. ❌ in bookkeeping — **D4-6**: S-10.1's *"this spec holds 0 references to it"* is falsified by the sentence that states it (measured: 1). |
| **KF-AV-28 present where governed** | ✅ carried whole at §H with the governed set re-derived from all three banked coordinates, each of which resolves verbatim (§2). Seven governed rows + one identity-governed twin, with D-24 named as a datum that is *not* governed until a cure is spent — a disclosure, not an omission. |
| **O-21 (not O-20) at W1's mint sites** | ✅ **correctly not this wave's surface**: `grep -c 'O-20\|O-21' KF-W9.md` → **0**. Verified at the sibling instead: `KF-W1.md` carries the O-21 mint at its gate (`:263`/`:266`), its repair directive (`:387`) and its cross-edge (`:315`); the surviving `O-20` occurrences there are the *measured ledger maximum* datum, which is the correct use. No stale mint. |
| **The W10-seam five-edge table fulfilled at source waves** | ✅ **for this wave's edge, and measurably.** `KF-W10.md` §6.C-0 is the index; **edge 4** is W9's, and W10's own condition cell reads *"MEASURED — CURED AT THE SOURCE THIS ROUND: `S-10` and `S-8` are present at KF-W9's bytes, **and the §Bounds capture-receipt row now exists**"*. Verified from this end: **S-10** carries the `KF.W10` cross-edge typed **PRECEDES — W10 DEPENDS**; **S-8** family (iii-a)/(iii-b) carries the OD-V3 packet with the **1280 arm**, **both transport homes** and the **four duplicating scenes** enumerated from DP2-06's own words, plus the OD-V5 390 at-rest observation *"either way"*; **§Bounds** carries `SS-13-CAPTURE-RECEIPT.md`, create (`.e`, serial), whose four indexed limbs (a)–(d) each resolve — including (d), the **V-A95 aurora surface verify**, which `KF-W10.md:144` independently routes as *"surface verify **KF.W9**"* with evidence *"KF.W9's capture-receipt §Bounds row"*. **Both ends now name each other. D3-3 is genuinely cured, and cured as a declared output rather than a promise.** Edges 1, 2 and 5 are W0's, W3's and W10's own; edge 3 is W7's and W10 records it cured. |

---

## §6 · DEFECT REGISTER — 7 booked

### D4-1 · HIGH — the LAW A census block's counts and scope statements are refuted by its own pasted commands, in all three censuses

LAW A's own text: the census is *"derived from the import graph at the frontier, **pasted with its outputs** — never from line counts, never from a CARRY's or a spec's own text."* The three censuses at §Repair ledger — round 3 §LAW-A paste commands whose outputs do not match the prose beside them. Re-executed this seat at `origin/master 81a56990`:

1. **Census 2, `btn-playback` file count.** Spec: *"`git grep -n 'btn-playback' … ` → **24 hits / 8 files**"*. Measured: `git grep -n … | wc -l` → **24** ✅; `git grep -ln … | wc -l` → **7**, not 8. The seven are `demo/DESIGN.md` · `PlaybackRibbon.vue` · **`demo/scenes/easing/EasingScene.vue`** · `SpringScene.vue` · `StartingStyleTarget.vue` · `demo/styles/font-roles.json` · `demo/styles/playback-idiom.css`. The spec's enumeration names **six** of them and **omits `EasingScene.vue:61`** entirely — a comment site, so non-import context, but it is the seventh file and its absence is why the count and the list disagree.
2. **Census 2, the `.focus-ring` consumer count.** Spec header: *"CONSUMERS (**5** template/style sites, the class applied)"*, and the Finding repeats *"**five** class-consumers for `.focus-ring`"*. The block then lists exactly **four** application sites — `KeyframeCard.vue:45` · `SpringHeatmap.vue:30` · `SpringTarget.vue:63` · `SquareScene.vue:46` — plus two comment lines it correctly labels as comments and two it correctly segregates as TOKEN-ONLY. Measured: **4** class applications across 4 files. The 11-hits/7-files headline is exact; the consumer count is one high.
3. **Census 1, the non-import scope.** Spec: *"all other hits are docblock/comment prose **inside the module's own five files** + `scripts/gates/structure/index.mjs:259`"*. The same pasted command returns four further src prose hits **outside** the module: `engine/playback-state.ts:3` and `:4`, `group/lifecycle.ts:5`, `orchestration/sequence/lifecycle.ts:6` — each naming the pre-carve `engine/play-lifecycle.ts` in a docblock, which is precisely the kind of stale prose a carve leaves behind and the kind a census exists to surface.
4. **Census 3, the prose scope.** Spec: *"`demo/scenes/sequence/SequenceTarget.css:162` (a COMMENT) + **`docs/tranches/H/**` prose ONLY**"*. The pasted command (`git grep -n 'proof:sequence-rows-draggable' origin/master -- .`) returns doc hits across **five** tranches: H ×13, **J ×3, K ×1, S ×1, U ×1** — including `docs/tranches/K/audit/ci-cd-k.md:73`, which files the gate against **`ci.yml:1031-1034`**, and `docs/tranches/J/waves/J.W7c-impl.md:177`, *"gated GREEN by `proof:sequence-rows-draggable`"*. The **substance is unharmed** — I re-ran the falsifier: no `package.json` entry (exit 1) and no hit under `.github/` or `scripts/` (exit 1), so the fork's justification **is** void at the frontier. But "H prose only" is false, and the J/K rows are exactly the historical evidence a KF.W11 seat would want before deleting a rule that once gated CI.

**Why this is HIGH and not bookkeeping.** LAW A is round 3's own new law, and its stated purpose is that the receiving seat *"inherits a census rather than a filename"*. The census handed to **KF.W13** over-states the deletion's file radius by one and mis-states its class-consumer count by one; the census handed to **KF.W11** mis-states where the retired gate's prose lives. A blast radius is the one thing a delete-act consumer must not have to re-derive — and this file's own K-5 doctrine (*one deletion fixes nothing*) is an argument **from** radius. This is also the same fault the file convicts elsewhere three times over: a number asserted beside an enumeration that does not produce it.

**Cure**: re-run all four commands, paste the true outputs, add `EasingScene.vue:61` to Census 2's file list as non-import context, restate the two consumer counts at **7 files / 4 class-consumers**, widen Census 1's non-import scope to name the four sibling-module docblocks, and widen Census 3's to *"H ×13 · J ×3 · K ×1 · S ×1 · U ×1"* with `ci-cd-k.md:73`'s `ci.yml` reference named, since a KF.W11 seat will meet it.

### D4-2 · HIGH — G-KFW9-6 is an unpassable oracle: it closes on cure acts in a tree where this wave holds zero write grants

`KF-W9.md:221`, **G-KFW9-6**, CLOSES clause, verbatim: *"no unification lands that snaps a progress animation to a rest state its own banked defect makes idle-indistinguishable; **group arm via `g.respectReducedMotion` (`group/group.ts:57`), standalone arms via options bags**; the KAD-11 pair lock holds."*

Those are edits to keyframes.js `src/animation/` — the PRM unification, whose engine half is **KF.W5's** and whose demo half is **KF.W6's or a packet's** (this spec says so itself at §Bounds' PRM row: *"every PRM cure is KF.W5's (engine), KF.W6's (tokenization), or a packet's — a standing write grant here would re-open the OP-2 breach the ruling closed"*). After R-9a this wave *"holds **ZERO write grants in the kf tree**"* and *"spends **no cure**"*. **No act available to this wave can turn this gate green.** Its RED — restated at round 3 as *"the DEMO half does not yet consume the resolver the LIBRARY half already ships"* — is a condition only another wave can clear, and the gate names no witness whose recording would discharge it.

This is **round-1 check D-3**, HIGH (*"preamble forswears cures two gates close on"*), surviving at the third gate. That check cured **two** of the three: G-KFW9-9 was re-cut to close *"on the born-RED witness pair only"* with the AFTER witness explicitly deferred (*"re-shot here once KF.W13's act lands"*, S-9 discipline), and G-KFW9-10 was struck outright. G-KFW9-6 was left, and round 3 (R3-9) rewrote its **witness** — correctly and well — without touching the close condition standing underneath it. The result is that the wave's central adjudicated deliverable is gated on an oracle it cannot reach.

**Cure**: re-cut G-KFW9-6 on the G-KFW9-9 pattern — close on the **measurement** this wave can make (the flag-state-and-layer enumeration for the group arm and both standalone arms, the KAD-11 pair's rest-state capture, the final-frame/idle-indistinguishability witness at 100 % width), and hand the *"no unification lands that…"* constraint to KF.W5/KF.W6 as a **declared sequencing obligation** (the S-2/S-15 idiom), read UNMEASURED here until their act lands.

### D4-3 · HIGH — "KF-CE-13's fourteen folds" is asserted at three sites over an enumeration of sixteen, and one site is a gate's close condition

§B's KF-CE-13 row publishes its fold list after *"ONE IDENTITY, ZERO RE-BOOKINGS — folds by reference:"*. Split on the row's own ` · ` separator, script-counted this seat: **16 items** — KF-AV-20/D-9 · KF-CB-32/D-19 · KF-ET-16/D-M-8 · D-21 (kf-KeyframeTimeline ≡ kf-TimelineTrack D-9 ≡ kf-TimelineHoverPreview D-10) · KF-KE-43/D-31 · SPF-28 · KF-SS-40/D-19 · D-14 (kf-SpringTrace) · D-21 (kf-SquareInstrument) · KF-SST-14/-17 · KF-APP-24 ≡ kf-TypingDots D:D-8 · D-15 (kf-AmigaScene) · kf-SquareScene's demo-wide row · kf-CubeAxisLines' keying correction · kf-SpringHeatmap D-M6/D-M7 · kf-TimingFunctionPanel D-14 residual.

**"Fourteen" is asserted at three dependent cells and enumerated at none:**
- `:93` §Carry head, route (b): *"KF-CE-13's **fourteen** folds"*
- `:223` **G-KFW9-8**, CLOSES: *"KF-CE-13's demo-wide row lands as a measurement with its **fourteen** folds discharged by reference, zero re-bookings"*
- `:234` **S-2**: *"the KF-CE-13 sweep's **fourteen** folds discharge by reference against the pre-deletion cascade"*

No counting rule reconciles them: by ` · ` units it is 16; by banked ids it is more (D-M6 and D-M7 are two, KF-SST-14 and -17 are two, and the D-21 row folds three records). Round 3 states *"§Carry bullets: **79**, unmoved this round — no row was added, removed, renamed or re-graded"*, so the list has not changed under the figure; the figure was simply never counted.

**This is live, not cosmetic.** G-KFW9-8's green is defined as *"its fourteen folds discharged by reference"*. An executing seat that discharges fourteen leaves two fold families unshot and closes the gate anyway; one that discharges sixteen faces a two-count divergence with the gate's own text. It is the identical fault the file has now cured three times — the negative register (round-1 D-10), the escalation register (round-2 D-5), the cross-edge count (round-3 D3-5) — each time by the same remedy the file wrote for itself: *state the count at its enumeration*.

**Cure**: restate the count at the §B enumeration with its counting rule (` · `-separated fold families), and re-point G-KFW9-8 and S-2 at that enumeration rather than re-issuing a numeral — the S-10.1 pattern this file already built.

### D4-4 · MEDIUM — the `shadowTick` declaration anchor is one line late at BOTH refs, inside the receipt block that certifies it re-executed

Two sites: §Bounds' library row — *"**`src/animation/waapi/delegation.ts`** (`shadowTick` decl **`:54`**; `animation.playback.loop(shadowTick)` `:64`)"* — and §A's KF-TD-1 row — *"`shadowTick` (**`:54`**, looped at `:64`) still never consults it."*

Measured this seat: `git grep -n "shadowTick" origin/master -- src/animation/waapi/delegation.ts` → **`:53`** `const shadowTick = (now: number): boolean | Promise<boolean> => {` and `:64` `animation.playback.loop(shadowTick);`. Line `:54` is `if (animation.done) return false;` — the body's first statement, not the declaration. **Identical at `8281638c`** (`:53`/`:64`), so this is not stale-ref drift: it was never right.

Two aggravators put this above cosmetic:
- The **§RECEIPT** table's first row — the D3-4 cure, the block whose entire reason for existing is to replace a false universal with an *enumerated* re-derivation — lists this family as re-executed at `origin/master 81a56990` and prints *"`src/animation/waapi/delegation.ts` `shadowTick` `:54`/loop `:64`"*. A receipt that certifies a re-execution must not carry a coordinate a one-line `git grep` refutes.
- The round-3 defect table records D3-8 as **CURED**, with *"the authored `delegation.ts:50` struck as resolving to no named symbol while `:54`/`:64` are kept **as measured**."* The pair was half-measured: `:64` is right, `:53` was read as `:54`.

The **substance is sound and independently verified**: `withReducedMotion` is absent from `delegation.ts` entirely (`git grep -c` exit 1), so KF-TD-1's *"`shadowTick` never consults it"* is true, and `snapToReducedMotion`'s sole call site is `frame.ts:137` inside `playFrame`. Only the pinpoint fails — the same class as round-2 check D-10 (`.focus-ring:focus-visible` pinned at its declaration line rather than its selector line), cured once and reproduced.

**Cure**: `:53` at both sites, or drop to the R2-7 idiom already used for the rest of the row (file + symbol, no offset).

### D4-5 · MEDIUM — §Bounds marks the 58 adjudicated records `read-only, IMMUTABLE` while G-KFW9-13 can close only by writing into them

§Bounds, `:64`: *"`…/megatranche/registry/adjudicated/kf-*.md` (58 records) | **read-only, IMMUTABLE** (E-1/E-3) | G-KFW9-13 write-backs land as ADDENDA under original ids, never edits"*.

**G-KFW9-13** CLOSES: *"each trigger's measured value + resulting severity **written back as an ADDENDUM under the ORIGINAL id** (E-3 addenda-not-patch / E-1 immutable); unrun triggers reported PROVISIONAL at close."* **S-13** arms its triumvirate on *"any adjudicated-record edit **that is not an addendum under its original id**"* — a carve-out that only makes sense if addenda under original ids **are** writes this wave may perform. **G-KFW9-5** has the same shape at a different path (*"the census row **amends**"*), but there §Bounds says `read-only` of `lane-frontend.md §6.5` specifically and routes the amendment elsewhere, so it is consistent; here the Access column and the gate contradict each other on one path.

Either reading breaks something: a seat that writes the addendum breaches §Bounds (and, on a strict reading of S-13's first clause, arms a triumvirate); a seat that does not cannot close the gate, and the thirteen provisional severities can only be reported PROVISIONAL — which the goal criterion permits as an outcome but which the gate does not accept as green. This is round-1 check **D-5** (*"OP-2 forbids the writes §Bounds grants"*) inverted and moved to the value.js side, at the one path the three prior passes checked only for read-integrity.

**Cure**: split the cell — `read-only for the EXISTING bytes (E-1); APPEND-ONLY for dated addenda under original ids (E-3), by `.e` alone` — which is what S-13 and G-KFW9-13 already assume, or route the addenda into `evidence/W9/**` and re-cut G-KFW9-13 to close there with the record-side application declared as a later wave's act.

### D4-6 · LOW — S-10.1's "this spec holds 0 references to it" is falsified by the sentence that states it

**S-10.1**, closing: *"KF.W1, KF.W2, KF.W3 and KF.W8 are **not** edges — W3 deliberately so (gated-unscheduled; **this spec holds 0 references to it**)."* Measured: `grep -c 'KF\.W3\b' KF-W9.md` → **1**, and the sole occurrence is that clause. PASS-3 §5 verified zero — correctly, at *pre-round-3* bytes; round 3 authored S-10.1 and made the count 1 in the act of asserting it is 0.

The posture itself is **sound** and should not be disturbed: W3 is gated-unscheduled, nothing here schedules against it, and naming it as a deliberate non-edge is exactly the right disclosure. What fails is the self-referential zero — and the file demonstrates two paragraphs of its own that it knows the fix, in the dissent roster's scope rule: *"occurrences in ledger and provenance prose, **including this sentence**, are not roster members."*

**Cure**: *"this spec schedules nothing against it; the only occurrence of the token is this disclosure"* — a statement that stays true as the file grows.

### D4-7 · LOW — `numeric.ts:82-89` is a bare filename under no declared base — the D3-8 class, swept in §Bounds and not in §Carry

§A's **D-8 + M-7** row: *"Cure: intensity form per the anchor; `NumericAnimation` accepts the flag (**numeric.ts:82-89**)."* No base is declared anywhere in §Carry, and the file is `src/animation/**physics/**numeric.ts` (`git ls-tree -r --name-only origin/master | grep numeric.ts` → one hit, this seat). The offsets are **sound**: `:82` is `constructor(keyframes: T[], options?: NumericAnimationOptions)` and `:89` is `this._respectReducedMotion = options?.respectReducedMotion ?? false;` — the constructor accepting the flag, exactly as claimed, verified this seat.

Round 3 cured this exact shorthand-base class for **two §Bounds paths** under D3-8 (`cube/OrbitalDrag.vue` → `demo/scenes/cube/orbital-drag/OrbitalDrag.vue`; `delegation.ts` → `src/animation/waapi/delegation.ts`), each with its `ls-tree` receipt. The §Carry body was not swept, and the same shape survives there. LOW because a reader who greps finds it in one hop and the offsets hold.

**Cure**: write it `src/animation/physics/numeric.ts:82-89`, and state that §Carry source anchors carry full paths (§Bounds' bases do not reach into §Carry).

---

## §7 · WHAT THIS PASS DECLINES TO BOOK

Recorded so pass 5 does not manufacture them:

1. **No census escape.** Axis 1 is clean at **168/168**, at a deliberately *larger* granularity than any prior pass. Any pass that books a hard escape must first defeat the nineteen strict-mode adjudications and the twenty-eight token-absent adjudications in §1.
2. **The 565 / ≈590 denominator and its amended command are BOTH correct.** This seat reproduced `h2-only 563 · h2+h3 565 · 57 records · 58 files` independently, and confirmed kf-ChannelOptions as the sole +2 and kf-CubeAxisLines as the sole zero-item record. **D3-6 is fully cured. Do not re-open.**
3. **D3-1's re-anchor is correct** — the whole `play-lifecycle` family now resolves at the frontier (`frame.ts:121/:131/:137`, `strategies.ts:66-75/:76/:109`), the carve `53b907c5` is real, the barrel exclusion is real, and **KF.W5's B-6 and G-PRM-FLIP name the same file and lines** — verified from the W5 end this seat. Only `delegation.ts:54` survives (D4-4), and that is a different file and a different fault.
4. **D3-2's cure is correct and its census is exact** — 6 hits, zero demo consumers, three library consumers, and the `SpringProgress.amplitudeScale` precedent verified whole (`:100`, `:323`, `:352`). The gate's restated RED is honest.
5. **D3-3's cure is real, and it is a declared output, not a promise** — both ends now name each other, and `KF-W10.md`'s own §6.C-0 row 4 records it MEASURED. Do not re-file the W10 seam.
6. **D3-5's cure is exemplary** — S-10.1 enumerates thirteen, states its counting rule, discloses two alternative granularities (16 / 7), and **discloses that agreement with the prior un-derived figure is a coincidence**. That last sentence is the single best paragraph in the file; it must not be trimmed.
7. **D3-7 and D3-8's cures are correct** — `W6.md:354`/`:367`, `W11.md:290`, and all fifteen §Bounds PRM paths resolve at the frontier.
8. **R-11's `npm run gh-pages` is correct** — `package.json:43`, re-read at the frontier. Do not re-file D-1.
9. **The round-1 decline (D-14, KF-EST-4's missing KF.W6 counterparty) stands** on R-19e; nothing in this pass disturbs it.
10. **The two `D-15`s are distinct banked ids** (kf-RibbonBar forced-colors fold vs kf-SequencePlayhead RTL posture). Never merge them.
11. **kf-TimelineTrack D-8 is not a W9 escape** — it routes → KF.W7; only its forced-colors arm folds to D-21, carried at §B. PASS-3's finding re-verified.
12. **LAW B compliance is genuine.** The closure claims are homed in the artifact and cited with their defects. Do not book a LAW B violation on S-10.1's thirteen — it is stated at its enumeration, which is the cured shape.

---

## §8 · VERDICT

**DEFECTIVE** — narrowly, and on a different frontier again.

The census axis is **closed and stays closed** under a fourth independent enumeration at a larger granularity: **168 routed pairs, 168 discharged, zero escapes**, with the whole counting family reproducing at the bytes. Axis 3 is clean for the fourth consecutive pass. Round 3's four headline cures — the play-lifecycle re-anchor, the `reducedMotionScale` truth-restatement, the W10 seam, and the residue command — are all **verified correct at the frontier by this seat's own scripts**, and two of them (the S-10.1 enumeration with its coincidence disclosure; the W10 seam cured as a declared output) are the best work in the file.

What survives thrice-repairing is a single recognisable shape: **the round-3 repairs did not audit their own arithmetic.** All three HIGHs sit inside round 3's own product — a LAW A census block whose four pasted commands return numbers and scopes the prose beside them contradicts (**D4-1**); a gate whose witness round 3 rewrote beautifully while leaving underneath it a close condition naming another wave's bytes, so the wave's central deliverable is gated on an oracle it cannot reach (**D4-2**); and a fold count asserted at three cells — one of them a gate's green — over an enumeration of sixteen, which is the exact *"three numbers, one enumeration"* fault this file has now cured three separate times for other registers (**D4-3**). The two MEDIUMs are the same shape at lower stakes: a receipt block that certifies a re-execution it did not perform for one coordinate (**D4-4**), and a §Bounds cell that forbids the write its own gate closes on (**D4-5**).

None of the seven touches the wave's posture. It still **measures**: zero write grants in the kf tree, zero cures spent, status `planned`, gate ids un-renumbered, every cure-shape lock carried, every rider carried, all twelve dissents present, and the W4 head lawfully unbound. **All seven are repairable inside this spec, none re-opens a ruling, and none disturbs the census.**
