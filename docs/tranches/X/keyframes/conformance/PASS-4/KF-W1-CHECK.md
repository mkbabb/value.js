# KF-W1 — FRESH ADVERSARIAL SPEC CHECK, PASS 4 (L-18/L-20)

**Target**: `docs/tranches/X/keyframes/waves/KF-W1.md` — **445 L / 142,952 B**, mtime **2026-08-28 15:42**.
**Seat**: FRESH. Every list, count, anchor and verdict below is this seat's own command output at the current bytes. `PASS-1/`, `PASS-2/`, `PASS-3/KF-W1-CHECK.md` and `PASS-3/RULINGS-3.md` were read **only** to know what was claimed cured and what §END commissioned — never as a source of a measurement. **The inherited nine-block partition is RETIRED** (RULINGS-3 R3-5): the census below is derived **BY RECORD** from the 58 `kf-*.md` files themselves.
**Corpus**: `docs/tranches/V/megatranche/registry/adjudicated/`, `ls kf-*.md | wc -l` → **58**; sole carry `docs/tranches/X/keyframes/carry/KF-W6-CARRY.md` (`find docs/tranches/X/keyframes -iname '*CARRY*'` → exactly one file).
**TREE LAW**: keyframes witnesses verified at **`origin/master` = `81a56990736ced5b5edde0b84c527680ac7689b1`** in `/Users/mkbabb/Programming/keyframes.js` (read-only; zero writes, zero git mutations, zero npm). `keyframes-v-exec` HEAD re-verified `81a56990`, porcelain **0**. Local `8281638c` opened **only** as declared historical context, per R3-2.

**VERDICT: DEFECTIVE — 0 BLOCKER · 5 MAJOR · 3 MINOR · 2 INFO.**
**Census: CLEAN — 6 routed · 6 booked · 0 escaped · 0 inventions.**

**Severity head is MONOTONE for the first time in this loop**: pass 3's BLOCKER is CLOSED and no new BLOCKER replaces it (pass 3 = 1·5·3·2 → pass 4 = 0·5·3·2). **All eleven PASS-3 local defects verified CURED at the bytes**, including both letter-affecting ones. What pass 4 finds is a single coherent class the round-3 seat did not run: **it closed at 15:42 and seven of its ten siblings were rewritten after it** (15:49–15:52), and **no re-anchor seat ever swept this file** — so three of its own present-tense measurements are now false, and its LAW-A census does not reproduce or even sum to its own stated total.

---

## §1 · AXIS 1 — ID-KEYED CENSUS, BY RECORD (the partition retired; escapes by bytes)

### 1.1 The routing alphabet, closed four ways

| probe | command | result |
|---|---|---|
| corpus size | `ls kf-*.md \| wc -l` | **58** |
| `KF.W1-DEP` occurrences | `grep -o 'KF\.W1-DEP' kf-*.md \| wc -l` | **18** |
| records carrying them | per-file, occurrences **and** lines (identical) | **5** — ACG **4** · CC **4** · CPW **3** · DGC **4** · KPT **3** |
| literal `KF.W1` substring | `grep -o 'KF\.W1' kf-*.md \| wc -l` | **33** |
| `KF.W10` substring | `grep -o 'KF\.W10' kf-*.md \| wc -l` | **15** |
| **bare `KF.W1` token** | `grep -oE 'KF\.W1([^0-9A-Za-z-]\|$)' kf-*.md \| wc -l` | **0** |
| terminal-disposition alphabet | `grep -ohE '→ \*\*[^*]*W1[^*]*\*\*' kf-*.md \| sort -u` | **`→ **KF.W10**` only** |

33 = 18 + 15 exactly. **The spec's alias-block census reproduces at the bytes, per-file counts included.**

### 1.2 The routed rows, id for id — every hit read and classified this seat

Six adjudicated rows carry a `KF.W1`-form target. All six are **one identity** — census **F-1**, the phantom `@mkbabb/glass-ui` dependency, collided into this namespace by `lane-frontend.md §10`'s superseded taxonomy.

| # | record : line | routing text (read) | booked as | state |
|---|---|---|---|---|
| R-1 | `kf-AnimationControlsGroup:28` | *"FOLD to census F-1 (KF.W1-DEP) at MAJOR — identity guard, not re-booked"* | C-13 + §8 row 5 | **BOOKED** |
| R-2 | `kf-AnimationControlsGroup:47` | *"L-3 / C-1 — MAJOR · FOLD → census F-1 (KF.W1-DEP)"*; rider `useControlsKeyboardShortcuts.ts:1` *"the sole binding site of all 18 shortcuts"* — **verbatim at the record** | C-13 + §8 row 5 | **BOOKED** |
| R-3 | `kf-ChannelControls:56` | *"FOLD → census F-1 (KF.W1-DEP). Identity guard"*; rider `ChannelControls.vue:219` *"(root-barrel `TooltipProvider, Button`) is the first import that fails"* — **verbatim** | C-13 + §8 row 5 | **BOOKED** |
| R-4 | `kf-ControlsPaneWrapper:60` | *"FOLD → census F-1 (KF.W1-DEP), MAJOR at the bank"*; rider *"`.vue:166` is the demo's SOLE `/drawer` consumer"* — **verbatim** | C-13 + §8 row 5 | **BOOKED** |
| R-5 | `kf-DemoGlobalChrome:44` | *"L-8 (F-1 exposure) — FOLD → census F-1 (KF.W1-DEP)"*; four toast classes `text-body`/`text-small`/`bg-foreground`/`text-background` **verbatim**; **`a59d3a22` dissent carried, not resolved** | C-13 + §8 row 5 | **BOOKED** |
| R-6 | `kf-KfPillTabs:46` | *"F-1 — MAJOR · FOLD → census F-1 (KF.W1-DEP)"*; rider *"one class and eight custom properties, seven glass-ui-owned"* — **verbatim** | C-13 + §8 row 5 | **BOOKED** |

**Anchor decomposition, re-derived**: **5** taxonomy-preamble (`ACG:17` · `CC:17` · `CPW:6` · `DGC:17` · `KPT:19`) + **6** routing rows + **5** ADJUDICATED-summary (`ACG:135` · `CC:155` · `CPW:149` · `DGC:130` · `KPT:122`) + **2** riders (`CC:142` · `DGC:43`) = **18**. Closed.

**Six rows across five records** — ACG carries two. D3-5's cure at §8 row 5 and the alias block is **verified applied and correct**.

### 1.3 Escape sweep — every other channel, by bytes

- `Mail Cure` → **7 records** (`kf-CopyButton:36` · `kf-EasingTarget:33` · `kf-EasingScene:33` · `kf-EasingSidebar:32` · `kf-KeyframeCard:36` · `kf-KeyframesEditor:36` · `kf-TimingFunctionPanel:34`). Every one read this seat: all are the **Routing-law preamble** reciting the census wave sketch (*"W0 Substrate Settle · **W1 Mail Cure** · W2 Parse Façade …"*) — a declaration of the alphabet, **not a routing**. No row in any of the seven disposes to W1.
- `O-8` → **2 records** (`kf-ChannelOptions` · `kf-LayerConfigPanel`); every occurrence inspected individually: **all** `KF-CO-8` substrings. `O-11` → **2 records**; all `KF-CO-11`/`KF-CO-12` substrings.
- **ZERO** hits corpus-wide for `X.KF.W1` · `KF-W1` · `I-26` · `INBOX` · `VALUEJS-INBOUND` · `sampleBezier` · `D-GAP-6` · `parseStylesheet` · `lerpArray`.
- **Third namespace re-verified**: keyframes' own `BUILD W1` is live at **exactly six anchors** — `kf-CopyButton:15` · `:40` · `kf-KeyframesEditor:10` (spelled **`BUILD-W1`**, hyphenated — *"FE-3's BUILD-W1 evidently executed upstream"*) · `:40` · `:51` · `:140`. The spec's six-anchor claim is **EXACT**; the hyphen variant is the reason a bare `grep -o 'BUILD W1'` returns five, and the spec's enumeration is right where a naive count would be wrong.
- **The carry** (`KF-W6-CARRY.md`, 366 L): bare `KF.W1` token **0** · `KF.W1-DEP` **1** (`:9`, the superseded-taxonomy reconciliation, the same booked F-1 identity) · literal `KF.W1` **3** (`:9` + `KF.W10` at `:225`/`:361`) · `Mail Cure` **0** · `O-8`/`O-11` **0**. **Nothing escapes to this wave through the carry.**

### 1.4 Census tally

| quantity | value |
|---|---|
| rows routed to census-taxonomy **KF.W1 (Mail Cure)** | **0** |
| rows routed to the collided token **`KF.W1-DEP`** | **6** |
| **ROUTED (denominator)** | **6** |
| **BOOKED** (fold-identity C-13 + §8 exclusion-with-reason, all six by anchor) | **6** |
| **ESCAPED** | **0** |
| **INVENTIONS** | **0** |

**The ∅-column finding is TRUE and independently re-derived from the records. C-14's L-19 lock holds.** — but see **D4-3**: the *booking mechanism* is routing to KF.W0, and KF.W0 does not carry the riders C-13 says it receives.

---

## §2 · AXIS 2 — ANCHOR **AND SUBJECT-IDENTITY** (every citation resolves AND its target names its subject)

This axis is sharpened this pass per R3-4: a citation passes only if the destination both **resolves** and **mentions the subject the citation attributes to it**.

### 2.1 Cross-spec receipts — ALL RESOLVE, ALL CARRY THEIR SUBJECT

| receipt | measured this seat | subject present? |
|---|---|---|
| **KF-W4 §Preconditions `OP-5`** | `:36` — *"**OPEN — KF.W1's.** … The mail cure precedes everything kf-side"* | ✓ names KF.W1 |
| **KF-W4 §Sequencing row `KF.W1 · Mail Cure`** | `:256` — *"**PRECONDITION on anchor citability**"* | ✓ |
| **KF-W3 §Preconditions `OP-5`** | `:43` — *"**KF.W1 Mail Cure** … UNLANDED"* | ✓ |
| **KF-W3 §Cross-edges row `KF.W1 · Mail Cure (O-8/O-11) + E13`** | `:279` — *"**DEPENDS ON** \| L-4"*; L-4 itself at `:252` | ✓ both |
| **KF-W10 §1 `Opens after`** | **`:20`** — *"**X.KF.W1** (Mail Cure — the O-8/O-11 amendment-addendum on an exec-visible path)"* | ✓ **the one carried coordinate is ALIVE** |
| **KF-W10 §Preconditions `OP-3`** | `:59` — *"**KF.W1's O-8/O-11 amendment-addendum is exec-visible** … **UNLANDED**"* | ✓ |
| **KF-W10 §Sequencing `B. → KF.W1 (Mail Cure, SS-2)`** | `:447` — *"**W10 depends.** No coordination terminalization (G-6) before …"* | ✓ |
| **KF-W0 §Sequencing OUTBOUND row `KF.W1 · Mail Cure`** | `:580`, inside §Sequencing (heading `:570`) | ✓ reverse edge, conflict correctly recorded |
| **KF-W0 §Excluded row `KF.W1's mail re-delivery`** | `:618`, inside §Excluded (heading `:592`) | ✓ |
| **KF-W0 §Sequencing packet-roster `the canonical 17`** | `:588`, inside §Sequencing; self-declares *"re-cut this round from this file's stale 15"*; roster id-for-id matches KF-W1's | ✓ |
| **KF-W0 §Sequencing OUTBOUND `KF.W4 · K-Quartet Hygiene`** | `:581` | ✓ |
| **KF-W2 §Excluded sibling-packet row** (dock-menu) | `:673` — *"The cube · sequence · spring · **dock-menu** · CARD-UNIT packets — routing restated on the canonical 17-packet roster (RULINGS R-15)"* | ✓ verbatim |
| **KF-W2 §Excluded row `KF-CO-1 / KF-CO-8`** | `:670` | ✓ |
| **KF-W7 §Excluded row + N-10** | `:372` / `:127` | ✓ both |
| **`INTAKE-ADJUDICATION-2026-08-03.md` §3 `### KF.W4 · K-Quartet Hygiene …`** | `:146`; enumeration `:147-149` = `B10-17 · B10-18 · B10-19 · B10-20 · B10-22 · B10-23 · B10-27 · B18-19 · B21-17` — **gap-preserving; `B10-21` correctly absent** | ✓ |
| **`CARRY-CUT-LEDGER.md:186`** | CC-084 row; contains **verbatim** *"(no emergency 4.0.1 — ruled)"* | ✓ |
| **`CARRY-CUT-LEDGER.md:255`** | the X-W9 row | ✓ |
| **`COHESION.md` §2** | `:76` — *"**I-26 mail cure (KF.W1) precedes any kf wave consuming O-8/O-11 obligations.**"* verbatim | ✓ |
| **census (c)5 / (c)6** | `:260` *"keyframes.js is sibling-read-only under value.js law"* · `:264` *"The mail cure precedes everything kf-side."* | ✓ |
| **`lane-docs.md §7.2`** | `:407` — *"**Amend, do not re-send.**"* verbatim | ✓ |
| **`kf-LayerConfigPanel.md:12`** | tail of the line: *"plus census **F-1/SCH-1 (→ KF.W0 §B-12**, FALSIFIED-AT-HEAD …)"* | ✓ **subject present** — the citation survives the sharpened test |

**Two citations fail the sharpened test — D4-7 (the sacred-checkout law's expiry clause dropped) and D4-8 (a "re-read verbatim" quotation spanning an unnamed line).** One further citation is a claim *about* a sibling's content that the sibling does not carry — **D4-3**.

### 2.2 Value-side `INBOX.md` — UNCHANGED, every re-key HOLDS, the mint still correct

97 L / **56,702 B**, mtime **12:33**, clean in git, last written by `7a7dc6ef` ✓. Sweep-path law at **`:15-16`** ✓ (narrowed grep → exactly **1** hit; broad grep → **5**: `:15 :66 :68 :72 :75` — the spec's declared departure from RULINGS-2 R2-1 item 4 is **correct**). **I-26 at `:92`** ✓, text *"ROWED 2026-08-03"* verbatim. **I-10 at `:47`** ✓. **Max outbound `O-20` at `:95`** ✓ — so **`O-21` is still `max+1`** and C-10's MINT LAW is satisfied at this clock. `KF-W4.md` cites it by id per RULINGS-3 §END R3-11(f) D-12 (*"THE DISCHARGING INSTRUMENT IS NAMED BY ID AT ROUND 3 (D-12): it is KF.W1's outbound row **`O-21`**"*), with the amended-by pointer clause carried — **the O-21 posture is CLEAN at both ends.**

---

## §3 · AXIS 3 — M-25 DEPTH (locks · riders · dissents)

**All ten locks present and verbatim**: C-1 *"Amend, do not re-send"* + ANTI-STALENESS · C-3 HARD NEGATIVE (never re-asked, no cut date) · C-6's new LAW-B substrate lock (*"No certification cites a sha as 'the tree measured' unless the census reproduces at that sha."*) · C-7 no-re-ask/no-re-scope/no-severity-change + anti-rename on `K1–K4`/`G-L7a–d` · C-8 HARD NEGATIVE + DESIGN LOCK · C-9 SIZING LOCK · C-10 ORDERING + MINT LAW · C-11 SEQ-LOCK + verbatim-retention · C-12 HARD NON-GATING · C-14 L-19.

**Riders**: all **five** F-1 component riders verified **verbatim at the records** (§1.2) — this is real M-25 depth, not paraphrase.

**Dissents carried, not resolved**: the `a59d3a22` rider (routed to KF.W0's re-measurement) · the KF-W0 reverse-edge **conflict** (recorded for COHESION §3 and the owner; neither spec rewrites the other) · C-4's recorded asymmetry. **Agglomeration by identity is correct** — one identity, six rows, booked once, never re-booked per component. Ids are anti-rename; C-6's banked `SCH-4` survived round 3 with only its false descriptive tail struck, which is the right surgery.

**Depth defect**: **D4-3** — the riders are verbatim *here* and absent *at the routing target*.

---

## §4 · AXIS 4 — GATES BORN-RED WITH REACHABLE GREEN · LAW A · LAW B

### 4.1 Frontier witnesses — every one reproduces byte-exact at `origin/master 81a56990`

Verified this seat in `/Users/mkbabb/Programming/keyframes.js` (read-only), per TREE LAW:

- three ABSENT packet anchors (`compile/value-ast.ts` · `compile/easing/easing-registry.ts` · `compile/emit/backward.ts`) ✓
- `resolve/browser.ts:3` import + **`:162`** `const parsed = parseCssScalar(source);` ✓ · `package.json:70` value.js `4.0.0` ✓ · **`:77` glass-ui `"7.0.0"` — SCH-1's falsifier, F-1 FALSIFIED-AT-HEAD** ✓
- `compile/value/compile.ts:32` ✓ · `easing/registry.ts:36` docstring ✓ · `easing-serialize.ts:71-73` reverse-map ✓ · `emit/backward/` = exactly `backward.ts, color.ts, index.ts, walk.ts` ✓ · `color.ts` **385 L** ✓
- **§5a rows 6–8, the D3-11 cure, measured**: `color.ts:171` = `const sampleRamp = (` · `:250` = the `stopCount` call · `:263` = the `1024` call ✓ **all three EXACT**
- `options.ts:31` ✓ · `leaves.ts:28` ✓ · `load-engine.ts:65` ✓ · `css-text.ts:41` ✓ · `test/internal/leaves-parity.test.ts` **PRESENT** ✓
- `parseStylesheet` raw output = **nine** lines: call sites `adapter.ts:222` · `scroll/grammar.ts:109` · `validate.ts:182`; imports `:7`/`:37`/`:47`; prose `adapter.ts:202`/`validate.ts:41`/`validate.ts:130` ✓ **exactly as G-KF1-3 states**
- import census **62** = `/css` 29 · `/value` 16 · `/color` 7 · `/math` 5 · `/easing` 3 · `/transform` 2 ✓
- exec clone: coordination dir = **9 entries + `vnext/`**, no addendum/O-8/O-11 ✓; `INBOUND-LEDGER` = **9 rows** (IN-ATLAS-1..5 · IN-GLASS-1..2 · IN-VALUE-1..2), no IN-VALUE-3/4 ✓

**§5b's three-substrate receipt — the round-3 BLOCKER cure — reproduces EXACTLY**: worktree census **61** (`/css` 29 · `/value` **15** · `/color` 7 · `/math` 5 · `/easing` 3 · `/transform` 2, byte-identical to O-11 `:52`) · bare `8281638c` **81** over a **disjoint alphabet** (`/parsing` 24 · `/units` 26; `/css` and `/value` absent) · frontier **62**. The glass-ui triple likewise: worktree **absent from the manifest** · `8281638c:package.json:71` = `"6.0.0"` · `origin/master:package.json:77` = `"7.0.0"`. **Sacred checkout**: `41  1` ✓ · **252** dirty ✓ · both bodies **7,064 B** / **15,633 B** at the stated mtimes ✓ · O-8 `:101-103` verbatim (`:100` blank) ✓ · O-11 `:34`/`:40`/`:52`/`:82` verbatim ✓.

**G-KF1-5's new literal legs (the D3-10 cure) re-executed and both reproduce**: `4\.0\.1` over all three coordination trees → **4 hits** (`INBOX.md:71`/`:72`/`:80` + sacred `…07-24-parser-totality-exposure.md:101`), **ZERO under `keyframes-v-exec`** ✓; `withdraw` → **4 hits** (`INBOX.md:80` · `CONSTELLATION.md:9`/`:69` · exec `vnext/skeptic-H1-r2.md:65`), **none a withdrawal of O-8's vehicle question** ✓.

### 4.2 Born-RED honesty and reachable GREEN

G-KF1-1..10 are each genuinely RED right now, each with a re-executable witness this seat watched fail. G-KF1-11 (DECLARED-SATISFIED) and G-KF1-12 (stay-GREEN INVARIANT) are correctly relabelled and correctly excluded — **the D3-7 header cure is applied**: §1 now reads *"TEN hard conditions … plus one DECLARED-SATISFIED and one stay-GREEN INVARIANT"*, agreeing with §6 and §11. **L-19 holds affirmatively**: no gate is a script, no row was manufactured to fill the ∅ column, and the mail tooling a lesser spec would mint is excluded by name.

**Reachable-green defect**: **D4-4** (G-KF1-4's "commissioned sentence verbatim" against C-6's own anti-copy lock).

### 4.3 LAW A — the import-graph law

**The module clause is genuinely VACUOUS**, and by enumeration rather than assertion: the six bounds rows are `modify` ×2 + `create` ×4, **all markdown**; every `src/` in both repos is out of bounds by §4's Do-NOT-touch row and by the execution gate. This wave deletes, repoints or shims **no module**. Correct.

**The one repoint act (C-9's mail-path law) does get a LAW-A-shaped census — and the census does not reproduce**: **D4-2**. The *finding* it produced survives and is the most valuable thing in §4c: **two** executing consumers, one in bounds (`INBOX.md:15-16`) and one **out of bounds and routed to the OWNER** (`~/.claude/projects/-Users-mkbabb-Programming-value-js/memory/feedback-mail-inbox-law.md`, verified this seat — its *"How to apply"* paragraph names the four landing paths including the frozen keyframes tree). All **25** current hits were inspected this seat: **no third executing site exists**, so the consumer set is right even though its arithmetic is not.

### 4.4 LAW B — no self-voiced closure claims

**Discharged and verifiable.** The header PROVENANCE, the alias block, §5's preamble, C-14, §11's close stamp and its *"Still standing"* paragraph all cite `conformance/PASS-3/KF-W1-CHECK.md` **by path and date**, state what that artifact found, **and state that it returned DEFECTIVE on four axes**. The round-2 stamp's *"NO line number anywhere"* clause is struck **as a spec-voiced completeness claim** with the falsification named. This is the cleanest axis in the file.

---

## §5 · AXIS 5 — POSTURE

| item | verdict |
|---|---|
| **W4 head honored** | ✓ No conflict. KF-W1 declares PRECEDES-on-anchor-citability; KF-W4 reciprocates at `OP-5` (`:36`) and at its §Sequencing row (`:256`), and now **names `O-21` by id** as its discharging instrument. W4's vue-tsc sequencing head is untouched: W1 opens no source and no `package.json`. Live `check` at the frontier (`package.json:37`) = `tsc --noEmit && tsc --noEmit -p tsconfig.test.json && npm run proof:structure`; `tsconfig.json`, `tsconfig.test.json` and `proof:structure` all exist — W4's re-cut still **COMPOSES**. |
| **W3 gated-unscheduled** | ✓ KF-W1 names it *"KF.W3 (Parser Consumption, **GATED**)"* at cross-edge 1 and schedules nothing. KF-W3 reciprocates at `OP-5`/`L-4`/§Cross-edges. |
| **KF-AV-28 present where governed** | ✓ **D3-8 CURED** — cross-edge 9's transit roster now enumerates **six** and includes the KF-AV-28 supersession rider, agreeing with §8's 17-packet row. ⚠ its parenthetical per-sibling census is stale — **D4-6**. |
| **O-21 (not O-20) at every mint site** | ✓ **CLEAN.** All five load-bearing sites plus the cross-edge 11 trailing citation commission **O-21**; `INBOX.md`'s maximum is still **O-20**, so the literal is still `max+1`; surviving `O-20` tokens are all *measurements* of the ledger maximum. |
| **The W10-seam five-edge table fulfilled at source waves** | ✓ **CORRECT BY ABSENCE.** `KF-W10.md` §6.C-0 (`:429`) carries the R3-10 INBOUND-DEPENDENCY table with exactly **five** rows — KF-W0 · KF-W3 · KF-W7 · KF-W9 · KF-W8. **KF.W1 is rightly not among them**: its edge was never one-ended. It is declared at both ends — here at cross-edge 11, and at W10's `§1 Opens after` (`:20`) · `OP-3` (`:59`) · `§Sequencing B.` (`:447`), all three verified live this seat with direction and force unchanged. |
| **W10's obligations closed by carriage not omission** | ✓ (not this wave's). The carry routes **nothing** here (§1.3); W1 mints no new obligation and hands W10 **the row**, not a frozen id. |

---

## §6 · DEFECTS

### D4-1 · **MAJOR** — the reciprocity head-claim is stale by three siblings, at two sites, and the sentence convicts itself

§1 (`:50`): *"**Reciprocity, measured 2026-08-28 over the WHOLE authored sibling set — all seven (`KF-W0` · `KF-W2` · `KF-W3` · `KF-W4` · `KF-W7` · `KF-W8` · `KF-W10`), per RULINGS R-19d.** An earlier draft measured four and enumerated seven; **the measured set and the enumerated-as-authored set are now the same set**, because a mail head that measures a subset of its consumers is measuring the wrong thing."*
Cross-edge 1 (`:300`): *"**Reciprocity, measured 2026-08-28 over all seven authored siblings** (§1, per RULINGS R-19d)."*
§1 (`:58`) likewise names only three non-consumers: *"KF-W2 · KF-W7 · KF-W8 — no KF.W1 edge."*

Measured this seat: `ls docs/tranches/X/keyframes/waves/` → **eleven** specs, **ten** siblings. `KF-W5`, `KF-W6` and `KF-W9` are on disk and are omitted from all three sentences. The file already knows this — **§9 item 5** (`:347`) reads *"KF-W5 · KF-W6 · KF-W9 as well — **ten siblings on disk** … **the measured set is the authored set, now ten**"* and **§11 row 10** (`:396`) reads *"the mark discharges across all **SIX** non-consumers."*

So the file carries **two verdicts on the same quantity**: seven at its head, ten in its records. This is the **D3-4 class** (*one file, two verdicts*) reproduced one level up — on the sibling cardinality rather than on four coordinates — and it sits in the wave's **head sentence**, whose own justification is that *"a mail head that measures a subset of its consumers is measuring the wrong thing."* Round 3 cured D3-4 at cross-edges 2 and 11 and never swept §1's own reciprocity paragraph.

### D4-2 · **MAJOR** — the LAW-A census, the wave's sole repoint-act discharge, neither reproduces nor sums

§4c (`:130-149`) pastes three commands *"with their outputs"*:

| leg | spec | measured this seat |
|---|---|---|
| (1) `grep -rnF 'keyframes.js/docs/tranches/V/coordination' docs/ --include='*.md'` | **23 hits / 17 files** | **25 hits / 17 files** |
| (1b) `grep -rnF '../keyframes.js/docs/tranches/V/' docs/ --include='*.md'` | **12 hits / 7 files** | **14 hits / 7 files** |
| (2) subject grep on the sweep law | **1** | **1** ✓ |

The 2-hit delta is **entirely inside `KF-W1.md` itself**. The file now carries **five** occurrences — `:72` (OP-1) · `:134` (the census command) · `:167` (C-9) · **`:255`** · **`:269`** — not the *"**3** in this spec"* its own enumeration claims. `:255` is the **D3-10 cure** (G-KF1-5's new three-tree literal grep) and `:269` is G-KF1-8's witness: both were written *after* §4c's census was taken, and the census was never re-run. **A wave whose LAW-A census is invalidated by its own later edits in the same round is the exact failure LAW A exists to force into view.**

Two further arithmetic failures, independent of the drift:
- The declared *"Non-consumer context — **22** of the 23 hits"* enumerates 4 (`INBOX` outbound cells) + 1 (`lane-docs:290`) + 1 (`DISPOSITIONS:51`) + 1 (`W40:136`) + 1 (`WL:63`) + 2 (the two letters) + 6 (apotheosis) + 1 (harvest) + 1 (PASS-1 check) + 3 (in-spec) = **21**. The enumeration is one short of its own stated total even at the spec's own figures.
- *"six `V/apotheosis/**` analysis files"* is **seven** at the bytes: `armA/edict-matrix-draft:140` · `armA/edict-matrix:143` · `armB/final-PA-parse:73` · `armB/final-PB-color:224` · `armB/program-PA-parse:72` · `armB/program-PB-color:151` · `probe/coordination/keyframes-inbox-2026-07-18-vnext-formation-handoff.md:456`.

**The finding survives intact**: all 25 hits were inspected this seat; the executing-consumer set is still exactly **two** (`INBOX.md:15-16` in bounds; the memory limb out of bounds and routed to the OWNER, verified at `~/.claude/…/memory/feedback-mail-inbox-law.md`), and there is no third. It is the census's arithmetic and reproducibility — the two properties LAW A names — that fail.

### D4-3 · **MAJOR** — C-13 says KF.W0 "receives F-1 plus its five component riders"; KF-W0 carries none of the five

C-13 (`:171`): *"Reciprocal declaration owed at KF.W0, **which receives F-1 plus its five component riders** (the `useControlsKeyboardShortcuts.ts:1` sole-binding-site blast radius · `ChannelControls.vue:219`'s first-failing import · `ControlsPaneWrapper.vue:166`'s sole `/drawer` consumer · kf-DemoGlobalChrome's four toast utility classes · kf-KfPillTabs' one-class/eight-custom-property inventory)."* Cross-edge 3 (`:302`) repeats it: *"KF.W0 receives the identity plus its five component riders."*

Measured in `KF-W0.md` this seat:

```
useControlsKeyboardShortcuts → 0     .vue:219      → 0     .vue:166 → 0
drawer                       → 0     eight custom  → 0     text-body → 0
18 shortcuts                 → 0     first import that fails → 0
```

KF-W0's nine `KeyboardShortcuts` hits are all `kf-KeyboardShortcutsModal` — **a different component**. Its single `KF.W1-DEP` mention is at `:295`, and the rider it carries there is `kf-DemoGlobalChrome:43`'s **S-9 toast-family** sequencing note — **not** the `:44` four-utility-class rider C-13 names. F-1 itself is carried (C-1 = *"F-1 ≡ SCH-1"* at `:266`, plus the fold-index rows at `:295`/`:321-323`), so the *identity* travels; the **riders do not**.

This is load-bearing on the census axis, not merely on prose: **the booking mechanism for all six routed rows is "routed away to KF.W0"**, so a routing target that carries none of the five named riders means the rows are booked into a wave that cannot execute what the booking says it received. It is the **D3-8 class** (*a rider omitted from the operative transit declaration is a rider lost in transit*) at one seam further out — and D3-8 was found and cured **inside** this file last round while this instance, which crosses a wave boundary, was not swept. C-13's *"owed at KF.W0"* framing is honest about the declaration; the present-tense *"which receives … its five component riders"* is not true at KF-W0's bytes.

### D4-4 · **MAJOR** — G-KF1-4's GREEN commissions "verbatim" what C-6's own lock forbids copying

G-KF1-4 GREEN (`:249`): *"the certification is explicitly withdrawn by naming a **WORKTREE-STATE RECORD, not a sha** — **C-6's commissioned sentence verbatim**, with §5b's measurement standing beside it."*
C-6 (`:164`) closes: *"**Re-measure all three censuses at authoring; copy neither 61 nor 62 nor 81 from here**."*

The commissioned sentence contains **five dated measurements**: `8281638c` · **252** dirty · **61** with its six-line split · **81** · **62**. Under **OP-1** the sacred worktree is declared *"the wave's single largest liveness risk"* — the file explicitly expects that tree to be able to change between now and open. If it changes, *"verbatim"* and *"re-measure, copy nothing"* cannot both be satisfied and the gate has **no reachable GREEN**; if it cannot change, the anti-copy lock is decorative and the C-1 ANTI-STALENESS lock it derives from is void here.

The file already contains the correct pattern and did not apply it: **C-10's MINT LAW** solves exactly this shape for the outbound id by commissioning the **rule** (`max+1` measured at open) rather than the literal, precisely so *"a spec-frozen outbound literal is never trusted over the measured ledger."* The same treatment is owed to C-6 — commission the **sentence template** and the substrate-naming rule, with the numbers filled at authoring. As written, the gate whose entire subject is *"do not inscribe a measurement you cannot reproduce"* commissions five measurements to be inscribed verbatim.

### D4-5 · **MAJOR** — "bare-`KF.W1` token 0 in all six non-consumers" is false at the bytes

§9 item 5 (`:347`) and §11 row 10 (`:396`): *"the bare-`KF.W1` token is **0** in all six."*

Measured this seat: `grep -oE 'KF\.W1([^0-9A-Za-z-]|$)' KF-W9.md` → **1**, at **`KF-W9.md:243`**:

> *"…counting only the rows physically inside S-10 yields **seven**. **KF.W1**, KF.W2, KF.W3 and KF.W8 are **not** edges — W3 deliberately so (gated-unscheduled)…"*

That is W9's own round-3 **S-10.1 cross-edge enumeration** (its D3-5 cure), written at **15:39** — three minutes before this file closed at 15:42. Substantively the hit **confirms** the non-consumer finding: W9 names KF.W1 in order to declare it a non-edge, which is a stronger reciprocation than silence. But the stated measurement is now false, at both sites, on the axis that defines this wave's consumer set.

Stale in the same paragraph, same cause: **KF-W2's `O-8` count is 2 occurrences / 2 lines**, not the *"KF-W2 **1** … lines — KF-W2 **1**"* stated (KF-W2 was written at **15:52**, ten minutes after this file). Both hits verified `KF-CO-8` substrings this seat (`:414` and `:670`), as are all hits in W5/W6/W7/W9 — so the **load-bearing claim (zero real O-8/O-11 letter citations across the six) still HOLDS**, and `grep -c 'O-11'` is still **0** in all six.

### D4-6 · **MINOR** — cross-edge 9's KF-AV-28 census is stale on nine of nine siblings

Cross-edge 9 (`:313`): *"KF-AV-28 is live across the sibling set (**W9 ×11 · W6 ×10 · W7 ×7 · W2/W10 ×5 · W4/W3 ×4 · W8 ×2 · W0 ×1**, per `PASS-3/KF-W1-CHECK.md` §6 D3-8)."*

Measured this seat — **occurrences**: W0 **6** · W2 **10** · W3 **5** · W4 **6** · W5 **0** · W6 **13** · W7 **13** · W8 **4** · W9 **15** · W10 **6**. **Lines**: 3 · 7 · 5 · 5 · 0 · 12 · 12 · 2 · 12 · 5. **Not one sibling matches under either metric.**

The provenance is lawful — the figures are attributed to the PASS-3 artifact by path and date, which is exactly what LAW B asks. But every sibling was rewritten in the same repair round *after* that artifact was cut, and C-1's ANTI-STALENESS lock (*"no measurement is inherited"*) plus R2-5 (*"a witness a seat leaves standing without re-running is that seat's defect"*) both bite. **The load-bearing claim is TRUE and strengthens on re-measurement**: KF-AV-28 is live in **nine of ten** siblings, absent only from KF-W5 — so the honest cure is to re-run the two greps or to drop the parenthetical and keep the citation.

### D4-7 · **MINOR** — the SACRED-CHECKOUT LAW is quoted with its own expiry clause removed

The header (`:27`) and G-KF1-12 (`:287`) both cite `keyframes.js/docs/tranches/V/EXECUTION-HANDOFF.md:13-18` — *"no mutating git, no npm operations there, ever"* — as the standing authority. Read verbatim at `:13-18` this seat, the same block continues:

> *"…It is SACRED: no mutating git, no npm operations there, ever — W2 reads its FILES (the 65-path slice export) and nothing else. **This invariant survives until W2 completes.**"*

The cited authority is **conditional and self-terminating**, and the citation carries the absolute half while dropping the condition — in a file whose C-5 lock is *"anchors are re-resolved at authoring, never copied"* and whose M-25 idiom requires quotations be verbatim or declared paraphrase. The same block is dated in two further respects the citation does not carry: it records the checkout at **`a59d3a22`** with **"~254 dirty paths"**, against this file's measured `8281638c` / **252**.

Nothing is actually at risk — the wave performs zero writes, and value.js's own prohibition stands independently — but a spec that convicts sibling waves for citing a target without its qualifier owes the same discipline to its own most-cited external authority. The cure is one clause: carry the *"until W2 completes"* condition and note that value.js's read-only posture does not depend on it.

### D4-8 · **MINOR** — a "re-read verbatim" quotation spans a line the citation does not name

G-KF1-6 (`:260`) and C-4 (`:162`): *"`registry/adjudicated/library-band.md:205` reads **'DECLINE — `sampleBezier`'** (permanently, measured zero demand), **re-read verbatim this seat**."*

Measured: **`:205`** ends *"…`strictTypes: 62` deletion. DECLINE — `sampleBezier`"*. The parenthetical *"(permanently, measured zero demand)"* is at **`:206`**. The quoted string as a whole does not exist at the cited coordinate.

The substance is unimpeachable and independently corroborated — `library-band.md:173` reads *"D-GAP-6 retired permanently on measured zero demand"* — but *"re-read verbatim this seat"* is a receipt promise, and the receipt is one line short. The cure is `:205-206` or a stable-anchor citation.

### D4-9 · **INFO** — no round-3 re-anchor certification exists on this file, though RULINGS-3 §END commissions one and makes this seat its auditor

RULINGS-3 §END: *"**RE-ANCHOR seat** | 1 | R2-7 re-run: **LAST writer under `waves/`, over every touched file**; its close certification written per LAW B and **audited by the pass-4 union seat as its first act**."*

Measured: `KF-W1.md` mtime **15:42**. Seven siblings were written **after** it — `KF-W4` 15:49 · `KF-W5` 15:49 · `KF-W6` 15:50 · `KF-W8` 15:50 · `KF-W10` 15:51 · `KF-W2` 15:52 (and `KF-W0`/`KF-W3` at 15:43). §12 carries **no re-anchor stamp**. **There is therefore no certification for this seat to audit**, and D4-1, D4-5 and D4-6 are exactly the same-round sibling drift a LAST-writer sweep exists to catch.

The residue is genuinely small — the single coordinate this file still carries, KF-W10 §1 `Opens after` at **`:20`**, is **ALIVE**, and every one of the file's other cross-spec receipts rides a §-heading + row label that resolves (§2.1). **The missing instrument, not the surviving residue, is the finding.**

### D4-10 · **INFO** — §5b's consequence (2) overstates its own reach

`:223`: *"(2) **every gate leg names both its repo and its substrate** (§6, re-keyed at round 3)."*

Measured across §6: **G-KF1-2 · G-KF1-3 · G-KF1-4 · G-KF1-10** name repo and substrate ✓ (the D3-6 cure, correctly applied, and correctly extended to the third member — the alias block's SCH-1 falsifier at `:19`, whose three-substrate demonstration this seat reproduced exactly). But **G-KF1-1** (`ls` on a path, no ref), **G-KF1-5** (absolute-path `sed`/`grep`, no ref), **G-KF1-7/G-KF1-8** (value-side, relative path only), **G-KF1-9** (`find` on the value.js root) and **G-KF1-12** (`git status --porcelain`, its tree named in prose only) do not.

None is a defect on its own — a filesystem listing has no ref, and the value-side legs bind to *"the current tree bytes"* by declaration. The overstatement is: **"every gate leg"** is false as a universal, where *"every leg whose reading turns on tree identity"* is both true and exactly sufficient. In a file that struck a round-2 stamp for certifying zero coordinates while carrying five, a universal that its own §6 does not meet is the same shape at smaller scale.

---

## §7 · WHAT STANDS

- **The census is CLEAN and re-derived BY RECORD from the 58 files' bytes**: **6 routed / 6 booked / 0 escaped / 0 inventions**, closed four ways (bare token · substring decomposition 33 = 18 + 15 · terminal-disposition alphabet · carry sweep), with all six routing rows read and classified individually and the ∅-column finding independently confirmed. **Zero rows, gates or bounds items were manufactured to fill it.**
- **All eleven PASS-3 local defects are CURED at the bytes**, including both letter-affecting ones. The round-3 BLOCKER cure is real and reproduces: §5b's three censuses (61 / 81 / 62) and the glass-ui triple (absent / `6.0.0`@`:71` / `7.0.0`@`:77`) are byte-exact this seat, and the ANCHOR DUALITY is declared verbatim and honoured throughout.
- **Every keyframes-side witness reproduces byte-exact at `origin/master 81a56990`**, including the §5a table row for row — and the D3-11 cure (`color.ts:171`/`:250`/`:263`) is **EXACT**, path drift only, zero line drift, both files 385 L.
- **Every value-side `INBOX.md` anchor HOLDS unchanged** (97 L / 56,702 B, `:15-16`, `:47`, `:92`, max `O-20` at `:95`), so **`O-21` is still `max+1`** and the O-21 posture is clean at both ends — `KF-W4` names it by id per §END R3-11(f) D-12.
- **Every cross-spec receipt resolves AND carries its subject** under the sharpened R3-4 test (§2.1), including the sole surviving coordinate (`KF-W10:20`, ALIVE) and the three registry/formation citations a naive reader would expect to fail (`kf-LayerConfigPanel:12` does route census F-1/SCH-1 → KF.W0 §B-12; `CARRY-CUT-LEDGER:186` does contain *"no emergency 4.0.1 — ruled"*; the `BUILD W1` six-anchor census is exact once the hyphenated `BUILD-W1` at `kf-KeyframesEditor:10` is counted).
- **M-25 depth is real**: all five F-1 component riders verbatim at the records, all three dissents carried rather than resolved, all ten locks intact, one identity booked once across a namespace boundary.
- **LAW B is discharged and verifiable** — every closure sentence cites the PASS-3 artifact by path and date and states that it returned DEFECTIVE. **LAW A's module clause is vacuous by enumeration**, and its one repoint-act census produced the finding that matters (the out-of-bounds memory limb, routed to the OWNER) even though its arithmetic fails.
- **The W10-seam axis is correct by absence**: KF.W1 is rightly not in `KF-W10.md` §6.C-0's five-row inbound table, because its edge was declared at both ends from round 1 and all three of W10's declarations are live.

**Local verdict: DEFECTIVE — 0 BLOCKER · 5 MAJOR · 3 MINOR · 2 INFO. Census CLEAN (6 / 6 / 0). Zero inventions.**

**The severity head is monotone for the first time in this loop.** Eight of the ten cures are editorial and land inside this file. **D4-3 does not** — it names a rider set that must land in `KF-W0.md`, at KF-W0's seat, or the six-row booking is a routing into a void. **D4-4 does not** — it changes what G-KF1-4 orders the executing seat to write into the cure letter, and it must be settled before `.b` authors a line, for the same reason pass 3's BLOCKER had to be. Everything else is the cost of a repair round that closed at 15:42 with no re-anchor seat behind it (**D4-9**).

---

*Pass-4 conformance seat, KF-W1. Sole write = this file. Nothing here stamps a verb, authorizes execution, or opens product source. Every keyframes-side figure was re-derived read-only at `origin/master 81a56990` in `/Users/mkbabb/Programming/keyframes.js` before it was written; the sacred checkout was read (`git rev-parse`, `git rev-list`, `git status`, `git grep`, `git show`, `grep`, `sed`, `ls`) and never written, and no npm or mutating git ran there. `8281638c` was opened only as declared historical context, per R3-2.*
