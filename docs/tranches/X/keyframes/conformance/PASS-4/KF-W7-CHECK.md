# KF.W7 — FRESH ADVERSARIAL SPEC CHECK (L-18/L-20, pass 4)

**Subject**: `/Users/mkbabb/Programming/value.js/docs/tranches/X/keyframes/waves/KF-W7.md` (394 L, thrice repaired).
**Seat**: FRESH. The inherited nine-block partition is RETIRED; this census derives from the 58 `kf-*.md` records' own bytes at `/Users/mkbabb/Programming/value.js/docs/tranches/V/megatranche/registry/adjudicated/`. Sole X·KF carry consulted: `carry/KF-W6-CARRY.md` (for the §Excluded W6-end receipts only).
**Tree law**: every product witness re-executed at the keyframes frontier `origin/master 81a56990` (`/Users/mkbabb/Programming/keyframes.js`, read-only). Local `8281638c` used only as declared historical context.
**Sole write**: this file. No product source opened for writing; no gate stamped; no registry byte touched; the PASS-1/2/3 artifacts are not edited.
**Verdict**: **DEFECTIVE** — census carriage is CLEAN by bytes (0 escaped under both admissible readings), but **10 defects** stand across axes 2, 3, 4 and 5, headed by a census figure that still does not reproduce from the rule the spec writes out (fifth consecutive generation of that class) and a silent rung promotion.

---

## §0 The five axes — verdicts

| # | axis | verdict | one-line basis |
|---|---|---|---|
| **1** | **ID-KEYED CENSUS by record** | **HOLD** | 136 routed · 136 accounted · **0 escaped**. Independent re-enumeration from the 58 records; every routed identity resolves to a §Carry row, an OP precondition, a §Sequencing settlement, or an §Excluded bullet with a named owner whose both ends this seat re-verified. |
| **2** | **ANCHOR + SUBJECT-IDENTITY** | **AMBER** | 31 of 31 frontier anchors re-executed and reproduce EXACTLY (§2). Registry-side: one "verbatim" quotation attributed to the wrong record (**D3**), one paraphrase-under-a-verbatim-label (**D6**), one spec-local-label section name that returns 0 by grep (**D8**). |
| **3** | **M-25 depth** | **RED** | A banked **INFO** row is carried at **MINOR** (**D2**) — against this spec's own carried rung lock; two of four preserved dissents from one DISSENT item silently dropped (**D4**); one row carried whose registry home is another row's cure (**D10**). |
| **4** | **GATES born-RED / REACHABLE GREEN · LAW A · LAW B** | **AMBER** | 15 gates, all born-RED, all with reachable green; **LAW A is exemplary** — both delete/repoint censuses (`flattenVars.ts`, `KeyframesAddDialog.vue`) re-executed by this seat at `81a56990` and reproduce COMMAND-FOR-COMMAND (§2.C). LAW B holds in the spec's voice. But the cited census figure is not re-derivable from the stated rule (**D1**), and one reciprocity claim has no second end (**D9**). |
| **5** | **POSTURE** | **AMBER** | W4 head ✓ · W3 gated ✓ (no W3-landing dependency; only the `package.json:70` collision cite) · KF-AV-28 rider carried and its discharge alphabet adopted from W10 verbatim ✓ · O-20/O-21 not this wave's surface (0 hits, correctly) ✓ · **W10-seam edge 3 fulfilled at its SOURCE end** ✓ (`grep -c 'KF.W10' KF-W7.md` → **4**). But a **sixth inbound edge** — KF.W2's registry-before-posture hard ordering — is declared at ONE end only (**D5**), and one internal contradiction survives the round-3 receipt (**D7**). |

---

## §1 Census — id-keyed, re-derived at the registry bytes

**Counting rule, stated so a later seat re-runs it rather than inherits it.**

1. **RECORD-SET** — the 58 `kf-*.md` adjudicated records. Nothing else is a source.
2. **MEMBERSHIP (owner test, not mention test)** — a row is ROUTED to this wave iff **KF.W7 is the OWNER named in its own terminal disposition**, bare or in a `KF.Wn/W7` dual-home. A `KF.W7` appearing only as a *cross-ref*, an *evaluation-input pointer*, or a *rider-governance clause* on a row whose owner is `NO-WAVE-OWNER` or another wave is **NOT** a routing.
3. **NAMING SCHISM** — `KF.W7-TOKENS` (retired `lane-frontend.md §10` taxonomy = the token-namespace audit) is held OUT by mechanism. `KF.W6-TIMELINE` dispositions ARE this wave's (the timeline-cluster wave under that taxonomy) and are held IN as **named preconditions**, not as cure rows.
4. **GRANULARITY** — a banked MERGED multi-id header counts ONCE at its banked identity (`m-9/m-11/m-12` = 1; `C-10/C-11/D-m6/m-1/D-m5` = 1). A merged group is never truncated to shrink a denominator nor expanded to inflate one.
5. **IDENTITY DEDUPE (M-25)** — a FOLDS/identity-guard line that discharges to an already-counted banked id is **fold-carried, not a new routing**. 16 such lines were resolved this way and are enumerated at §1c so the dedupe is auditable, never silent.
6. **DUAL-HOMES count once**, carried here if the mechanism is this wave's, excluded-with-named-owner if it is not.

### §1a · Roster-owned routings (KF.W7 named as owner)

| record | routed | carried in `KF-W7.md` | excluded-with-named-owner | escaped |
|---|---|---|---|---|
| `kf-KeyframeTimeline.md` | **40** (19 MAJOR · 14 MINOR · 7 INFO) | 40 | 0 | **0** |
| `kf-TimelineTrack.md` | **30** (6 MAJOR · 17 MINOR · 7 INFO) | 30 | 0 | **0** |
| `kf-TimelineHoverPreview.md` | **17** (2 MAJOR · 11 MINOR · 4 INFO) | 17 | 0 | **0** |
| `kf-TimelineCaret.md` | **14** (2 MAJOR · 8 MINOR · 4 INFO) | 14 | 0 | **0** |
| `kf-CSSPasteDialog.md` | **8** (3 MAJOR · 5 MINOR) | 8 | 0 | **0** |
| `kf-KeyframesAddDialog.md` | **8** (4 MAJOR · 4 MINOR; 3 are `KF.W6/W7` dual-homes) | 5 | 3 (KAD-15 · KAD-16 · KAD-20 → **KF.W6**) | **0** |
| `kf-AnimationVisualizer.md` | **2** (KF-AV-10 MAJOR · KF-AV-28 spec-input) | 2 | 0 | **0** |
| `kf-KeyframeCard.md` | **2** (KF-KC-11 arm · KF-KC-52) | 2 | 0 | **0** |
| `kf-SpringTarget.md` | **2** (C-4 · m-8) | 2 | 0 | **0** |
| `kf-CSSCodeEditor.md` | **1** (KF-CE-2 parent half, BLOCKER) | 1 | 0 | **0** |
| `kf-RibbonBar.md` | **1** (RB-7) | 1 | 0 | **0** |
| `kf-SequenceScrubber.md` | **1** (C·C-4 contract arm) | 1 | 0 | **0** |
| `kf-SpringTrace.md` | **1** (C-3) | 1 | 0 | **0** |
| **subtotal** | **127** | **124** | **3** | **0** |

*Two roster-adjacent false positives, resolved and recorded so a later seat does not re-file them*: `kf-SpringTrace.md:48` **C-2 = L-3** is `NO-WAVE-OWNER` — its `→ KF.W7` belongs to the sibling **C-3**, which IS carried; `kf-KeyframeTimeline.md:114` "Cross-refs, not folds" points at `kf-ChannelControls` L-2/C-2, counted once at §1b as OP-1.

### §1b · Non-roster routings (folds-with-obligation, routing-summary tails, §BLOCKER preconditions, riders)

| coordinate | identity | disposition in `KF-W7.md` |
|---|---|---|
| `kf-AnimationControlsGroup.md:40-42` | **D-1** — the document-singleton portal BLOCKER (`KF.W6-TIMELINE` precondition) | **OP-0** ✓ (opens §Sequencing's hard ordering) |
| `kf-ChannelControls.md:40` | **L-2/C-2** — the `:key` remount BLOCKER | **OP-1** ✓ |
| `kf-ChannelControls.md:52` | **C-8/D-6** — the empty timeline tabpanel | §Sequencing **item 8**, the expanded-timeline settlement ✓ |
| `kf-RibbonBar.md:131` | routing-summary tail: *"RB-7 + the expanded-timeline family … one settlement"* | §Sequencing **item 8** + P5 RB-7 ✓ |
| `kf-SequenceScrubber.md:134` | routing-summary tail: **D-5/D-6/D-11**'s *"the primitive already solves it"* arms, verdict-gated | §Sequencing → **KF.W11 arm (d)** + P0 C·C-4 ✓ |
| `kf-CSSPasteDialog.md:115` | residue **#9** — L-4's open falsifier, *"owned by the R-7 fold in KF.W7"* | **G15** assertion (*"L-4's falsifier answered"*) + P8 R-7 ✓ |
| `kf-PlaybackRibbon.md:36` | the **PR-CAUTION** clause riding the KF-AV-28 standing rider | header lock, quoted **byte-identical** (re-verified, §2.D) ✓ |
| `kf-PlaybackRibbon.md:157` | the NO-WAVE-OWNER transport/ribbon packet the rider governs | §Sequencing packet homing → **KF.W13** ✓ |
| `kf-SpringTarget.md:69` | the **kf-EasingTarget P-2** cure caution riding m-8/KF-AV-10 | P0 KF-AV-10 "Cure caution lock" ✓ *(carried — but mis-attributed, see **D3**)* |
| **subtotal** | **9** | **9 booked** |

**Explicit ZERO declarations, recorded and NOT counted** (they route nothing; a later seat must not read the absence as an escape): `kf-SequenceScene.md:153` — *"**KF.W7**: none (no export-surface rows in this corpus)"*; `kf-SequenceAxis.md:130` — *"**KF.W6/W7**: no axis-owned rows"*.

### §1c · Fold-carried lines resolved by identity (rule 5) — enumerated, not silent

`kf-KeyframeTimeline:106` (D-2/L-2/C-2 ≡ **R-3**) · `kf-TimelineTrack:86` (C-2 ≡ **C-1**) · `kf-TimelineCaret:74` (≡ **M2**), `:75` (≡ **D-3**+M8), `:77` (≡ **M1**), `:78` (≡ **D-17**+D-28) · `kf-TimelineHoverPreview:77` (≡ **M7**), `:80` (≡ **D-4/L-4/C-5**), `:81` (≡ **L-5**), `:82` (≡ **D-10**), `:84` (≡ **C-15**), `:86` (≡ **N-2**+N-9), `:88` (≡ **D-22**) · `kf-SequencePlayhead:143`/`:151` (C-§4 ≡ **C-15**) · `kf-SequenceScrubber:128` (≡ **KF-AV-28**) · `kf-SpringTarget:135` (m-8 ≡ **KF-AV-10**). **16 lines → 0 new routings.** Each target is carried; the spec cites the fold at the carried row's `⟨…⟩` provenance cell in every case (spot-checked at all 16).

### §1d · Totals

| | |
|---|---|
| **routed (re-derived by this seat)** | **136** (127 roster + 9 non-roster) |
| **booked** (124 carried + 3 excluded-with-named-owner + 9 non-roster) | **136** |
| **escaped by bytes** | **0** |

### §1e · Relationship to the figure the spec cites

The spec cites `PASS-3/KF-W7-CHECK.md` §1 for **138 routed · 138 booked · 0 escaped**. This seat reaches **136 · 136 · 0**. The 2-row delta is **entirely a rule question, not an escape** — carriage is complete under both readings — and it is the substance of **D1**:

| row | PASS-3 | this seat | why |
|---|---|---|---|
| **KF-AV-13** | counted (1a) | not counted | disposition owner = `NO-WAVE-OWNER`; `KF.W7` appears as *"decide press-to-seek with the KF.W7 evaluation"* — rider-governance, excluded by the spec's own rule (5) |
| **KF-AV-14** | counted (1a) | not counted | owner = `NO-WAVE-OWNER`; `KF.W7` appears as *"cross-ref KF.W7"* — same class |
| **KF-AV-24** | counted (1a) | not counted | owner = **KF.W8** (*"seam consolidation decision"*); `KF.W7` is a cross-ref. §Excluded itself books KF-AV-24 to KF.W8 |
| **KAD-F3** | counted (1a, KAD=9) | not counted | *"**FOLD to `kf-CSSPasteDialog` R-7**"* — an identity fold to a counted row; rule (5)/M-25 dedupe |
| `kf-SequenceScene` / `kf-SequenceAxis` zeros | not in 1a/1b | recorded, not counted | explicit non-routings |

All four are nevertheless **carried** by the spec (KF-AV-13/14/24 at P0's riders row; KAD-F3 at P8's `R-7 ≡ KAD-F3`), so **no reading produces an escape**.

---

## §2 Witness re-execution at the frontier — every command run by THIS seat at `origin/master 81a56990`

`git -C /Users/mkbabb/Programming/keyframes.js rev-parse origin/master` → `81a56990736ced5b5edde0b84c527680ac7689b1` · `HEAD` → `8281638c…` (disqualified substrate, context only).

### §2.A · §Bounds line counts — **18 of 18 EXACT**

`KeyframeTimeline.vue` **312** · `components/TimelineTrack.vue` **246** · `TimelineCaret.vue` **70** · `components/TimelineHoverPreview.vue` **38** · `composables/useTimeline.ts` **129** · `useTimelineBuild.ts` **200** · `useTimelineOps.ts` **88** · `useZoomPan.ts` **110** · `timelineTypes.ts` **41** · `index.ts` **8** · `utils/timelineEngine.ts` **102** · `utils/snapshotCapture.ts` **66** · `utils/flattenVars.ts` **33** · `CSSPasteDialog.vue` **80** · `keyframes/components/KeyframesAddDialog.vue` **161** · `scenes/sequence/SequenceScrubber.vue` **162** · `playback/AnimationVisualizer.vue` **256** · `demo/utils/keyframeSelector.ts` **31**.
The `timelineEngine.ts` correction holds: **102** at `origin/master`, **104** in the dirty worktree (`wc -l` → 104) — the `+4/−2` edit the spec calls the C-12-widening breach.

### §2.B · Gate witnesses — **13 of 13 reproduce**

| gate | command | result |
|---|---|---|
| **G1** | `cat …/glass-ui/dist/components/timeline/index.d.ts` · `ls` that dir | **2 lines** (`GlassTimeline` + `TimelineSegment`/`…Gradient`/`…State`); **nine** files ✓ |
| **G2** | `git show origin/master:…/KeyframeTimeline.vue \| grep -n scrub` | **78 · 83 · 197 · 202 · 224**; bare `scrub` among NONE (`:197 scrubT`, `:202 scrubAndCapture`) ✓ |
| **G3** | `TimelineTrack.vue` `:192-196` / `:174-178` | `onMarkerPointerDown` records no offset; `onTrackPointerMove` emits the ABSOLUTE `getPercentFromPointer(event)` ✓ |
| **G4** | `useTimelineOps.ts` `moveKeyframe` | `:56-63`; `rebuild()` at `:61` unconditional, no early-out ✓ ⟨the spec's `:56-62` is the bank's span, one line short of the closing brace — cosmetic, not booked⟩ |
| **G5** | `timelineEngine.ts \| nl -ba` | merge loop **`:37-47`**, key **`:38`**, merge comment **`:40`**, property loop **`:42`**, build **`:49-51`**, `exportTimelineToCSS` **`:60-70`** inheriting the merge at **`:66`** — **EXACT, all six** ✓ |
| **G7** | `TimelineCaret.vue` `:22`, `:58-65`; `useTimelineOps.ts:60` | `@blur="commitEdit"`; `commitEdit` `:58-65` compares nothing; `kf.selector = percentSelector(kf.percent)` at `:60` ✓ |
| **G8** | `git grep -n onMarkerKeydown origin/master -- demo/` | `TimelineTrack.vue:82` (binding) + **`:203`** (decl, body `:203-214`) — the spec's file attribution is CORRECT and silently repairs the bank's file-ambiguous anchor ✓ |
| **G9** | `TimelineTrack.vue:86` | `<TooltipContent side="top" :side-offset="8" class="p-2 max-w-56">` — **`ariaLabel` unpassed** ✓ |
| **G10** | `KeyframeTimeline.vue \| grep -n previewCache` · `grep -c delete` | `:81` passed · `:217` decl · `:221` read · `:226` write; `delete` → **0** ✓ |
| **G11 (i)** | `git grep -n 'TimelineHoverPreview\|previewCache\|SequenceScrubber' origin/master -- test/` | **0 hits** ✓ |
| **G11 (ii)** | same over `demo/` | **13 hits**, at exactly the 13 coordinates the spec prints (`KeyframeTimeline:81/217/221/226` · `TimelineTrack:87/89/117/125` · `SequenceTarget:125/127/146/205` · `useSequenceInstrument:5`) ✓ |
| **G13** | `useZoomPan.ts \| grep -n delta` | **`:50`** (zoom factor) and **`:59`** (`event.deltaY * 0.1`) — the only two; `deltaX` nowhere ✓ |
| **G14** | `useTimelineBuild.ts \| grep -n 'toast\|console.error'` | `console.error` at **`:48`** inside the `:47-50` catch (sole non-toasting failure); toasts at **`:121 · :133 · :137 · :148 · :155 · :157`** — **EXACT** ✓ |
| **G15** | `wc -l` both dialogs; `initialText` census | 80 / 161; `initialText` = **4 in-file hits** (`CSSPasteDialog.vue:49/53/59/67`) and **0 mount bindings** — "dead AT THE MOUNT" is exactly right ✓ |

Additional anchors re-executed and reproducing: `OP-4` — `package.json:81` `"@vitejs/plugin-vue": "^6.0.7"`, installed; `grep -c test-utils` → **0**; `vitest.config.ts` declares no `plugins` key (rc=1) ✓. `timelineTypes.ts:5-6` `selector: KeyframeSelector` present at `origin/master` ✓. `design-idioms.css:161-187` is the `.progress-rail`/`.progress-ball` block (`:161` opens the docblock, `:166` the rule) ✓. `design-idioms.css:47` `--rail-width: clamp(25rem, 33svi, 32rem)` = **400–512px** — the D-12/C-12/i-2 citation correction is sound and the "800px/~600px rail" fictions are correctly killed ✓. `TimelineTrack.vue:224-227` transitions `transform` + `border-color` only ✓. `TimelineCaret.vue:36` `keyframeId: string` sole declaration ✓.

### §2.C · LAW A — both delete/repoint censuses re-executed independently

**(1) `flattenVars.ts`** (§Bounds delete-candidate): `git grep -nF '../utils/flattenVars' origin/master -- demo/ test/ src/ scripts/` → **1** (`useTimelineBuild.ts:14`). `git grep -n '\bflattenVars\b' …` → **4** — `useTimelineBuild.ts:14` (import) · `:170` (call) · `flattenVars.ts:10` (decl) · `:26` (self-recursion). **CONSUMER SET = exactly one module** ✓. `loadPreset` census: declared `useTimelineBuild.ts:163`, returned `:197`, re-exported `useTimeline.ts:44`/`:121` — the binding consequence the spec states (a delete of `flattenVars.ts` alone leaves `loadPreset` uncompilable) **verifies** ✓.

**(2) `KeyframesAddDialog.vue`** (P8's R-7 fold SOURCE): `git grep -n 'KeyframesAddDialog' origin/master -- demo/ test/ src/ scripts/` → **exactly one live import** `KeyframesEditor.vue:118`, its single mount `KeyframesEditor.vue:75`, and one docblock prose mention (`useToolbarKeyboard.ts:24`) — **CONSUMER SET = one module**, and it is **outside §Bounds** ✓.

**LAW A is the strongest part of this spec.** Both censuses were sourced from no CARRY row and reproduce command-for-command. The KF.W6 F-5 failure class is genuinely checked before it could be committed.

### §2.D · Registry-side receipts re-executed at the value.js bytes

- `grep -rn 'KF.W6-TIMELINE' adjudicated/*.md` → **8 hits / 4 records** = 4 routing-law preambles (`kf-ChannelControls:17` · `kf-AnimationControlsGroup:17` · `kf-ControlsPaneWrapper:6` · `kf-DemoGlobalChrome:17`) + 1 closing tally (`kf-ChannelControls:155`) + **3 dispositions** ✓ — reproduces the spec's round-3 replacement receipt **exactly**.
- `grep -rniE 'disposition:.*KF\.W6-TIMELINE' adjudicated/*.md` → **exactly three rows in two records**: `kf-AnimationControlsGroup:42` (D-1) · `kf-ChannelControls:40` (L-2/C-2) · `kf-ChannelControls:52` (C-8/D-6) ✓. All three booked (OP-0 / OP-1 / §Sequencing 8).
- The struck round-2 command returns **three hits in ONE record** — the spec's diagnosis of why it is not a receipt is correct ✓.
- **PR-CAUTION**: the header quotation is **byte-identical** to `kf-PlaybackRibbon.md:36` (verified by exact string comparison). RULINGS-3 §END D8 is genuinely cured **at that site**.
- **§Excluded W6 ends**: `S-10 · KF-KE-21 (+KAD-15)` present at `KF-W6.md:299` and `S-10 · KF-KE-21` at `KF-W6-CARRY.md:178`; the `KAD-3 + KAD-7 + KAD-19 + KAD-20 + KAD-25 + KAD-16 + KAD-F1 + KAD-F4 + KAD-F5` group row present at `KF-W6.md:370` and `KF-W6-CARRY.md:248`. **Both ends of all three exclusions verify by banked row header** ✓.
- **Sibling declarations**: `KF-W8.md:97` authors the `CSSPasteDialog.test.ts` / `KeyframesAddDialog.test.ts` pair *"against whichever shape KF.W7 rules"* ✓ (the G15 dependency the spec declares from this end is real). `KF-W5.md:204/220/224-225` names its one create and asserts filename disjointness across all four claimants ✓. `KF-W10.md:437` records seam edge 3 as **MEASURED — CURED AT THE SOURCE**, and `grep -c 'KF.W10' KF-W7.md` → **4** ✓.

---

## §3 Defects — 10 (6 MAJOR · 4 MINOR), ranked

### **D1 · MAJOR — the census figure the spec cites still does not reproduce from the rule the spec writes out. Fifth generation of the same class.**
The verb table and §Excluded both cite `PASS-3/KF-W7-CHECK.md` **138** and then print a five-part rule "so the figure reproduces". Rules (2) and (5) **conflict** on four rows:
- rule (2) is a **mention** test — *"a row counts iff its own disposition text names `KF.W7` (bare, or in a `KF.Wn/W7` dual-home)"*;
- rule (5) is an **owner** test — *"NO-WAVE-OWNER rows the KF-AV-28 rider merely GOVERNS are not routings and are not counted."*

`kf-AnimationVisualizer` **KF-AV-13** (`→ NO-WAVE-OWNER … decide press-to-seek with the KF.W7 evaluation`), **KF-AV-14** (`→ NO-WAVE-OWNER … cross-ref KF.W7`) and **KF-AV-24** (`→ KF.W8 …, cross-ref KF.W7`) satisfy (2) and are excluded by (5). Neither rule addresses folds-by-reference, so **KAD-F3** (`FOLD to kf-CSSPasteDialog R-7 … → KF.W7`) is counted at PASS-3 and de-duped here. **Mention-reading = 138; owner+dedupe reading = 136.** The dated log already records 148 → 150 → 138; the rule that was supposed to end the drift admits both readings. *(Carriage is unaffected: 0 escaped either way.)*
**Receipt**: `KF-W7.md:15` and `:360` (the rule); `kf-AnimationVisualizer.md:54/60/68`; `kf-KeyframesAddDialog.md:63`; `PASS-3/KF-W7-CHECK.md` §1a subtotal **131** vs this seat's **127**.

### **D2 · MAJOR — a banked INFO row is carried at MINOR, against this spec's own rung lock.**
`KF-W7.md:233` books *"**KF-KC-11 (undo-trail unification) + KF-KC-52** MAJOR arm (KC-11) / **MINOR** (KC-52)"*. `kf-KeyframeCard.md` heads its severity blocks at `:38` BLOCKER · `:47` MAJOR · `:71` MINOR · **`:95` INFO**, and **KF-KC-52 sits at `:100`, inside INFO**. The spec's own P7 M9 row carries the governing lock verbatim: *"an identity fold cannot carry a different severity than its bank without re-adjudicating the bank"*. This is a silent promotion of exactly the kind that lock forbids, on the only row from that record whose rung the spec restates.
**Receipt**: `kf-KeyframeCard.md:95` (`### INFO`) + `:100` (KF-KC-52) vs `KF-W7.md:233`; lock at `KF-W7.md:209`.

### **D3 · MAJOR — a "verbatim" cure-caution lock attributed to the wrong record; the quoted sentence exists only as another record's gloss.**
`KF-W7.md:119` prints **"Cure caution lock (kf-EasingTarget P-2, verbatim)"**: *"the idiom leaves `transform` unclaimed BY DESIGN; a careless consolidation drops 28 balls out of their rails."*
`kf-EasingTarget.md:109` — the actual P-2 — reads: *"**P-2 · the `.progress-ball` idiom leaves `transform` unclaimed BY DESIGN** — `margin-top` centring on the ball, `translateY(-50%)` on the rail that nothing paints; the asymmetry is the proof of intent, and **one tidy-up would drop 28 balls out of their rails**."*
The quoted sentence — subject elided to "the idiom", *"a careless consolidation"* for *"one tidy-up"*, semicolon-joined — appears **nowhere in `kf-EasingTarget.md`**. It is `kf-SpringTarget.md:69`'s parenthetical gloss of P-2, lifted with a terminal period substituted for the closing paren. This is precisely the class **RULINGS-3 §END D8** convicted for PR-CAUTION *at this same repair round*: a paraphrase carrying a "(verbatim)" label. D8's cure swept one site; the sibling site four lines earlier in the same section was not swept.
**Receipt**: `KF-W7.md:119` vs `kf-EasingTarget.md:109` vs `kf-SpringTarget.md:69` (exact string comparison run by this seat: the quote matches neither record byte-for-byte; it matches `kf-SpringTarget.md:69` modulo the final `)` → `.`).

### **D4 · MAJOR — two of four preserved dissents from one DISSENT item silently dropped, while the other two are carried.**
`kf-TimelineTrack.md` DISSENT item 1: *"**RR-A's MAJOR rungs on D-4, M-7, D-12, and D-13's net arm** — preserved with their reopening conditions."* The spec carries **D-4** (`:163` *"RR-A MAJOR in dissent; reopening = photographed collision"*) and **M-7** (`:149` *"RR-A's MAJOR on M-7 in dissent"*) — and carries **nothing** for the other two: `:164` (`D-12/C-12/i-2 (TimelineTrack, merged)`) records no dissent, and `D-13 net arm` is folded into `:211`'s THP `D-3/L-D6/C-6` row, whose "(RR-A MAJOR preserved with the M3 reopening)" is **THP's own** dissent, not TimelineTrack's. `grep -on 'dissent' KF-W7.md` returns 11 hits; none is TT D-12's or TT D-13's. The asymmetry is the harm: two carried dissents make the pair read as complete.
**Receipt**: `kf-TimelineTrack.md` §DISSENT item 1 vs `KF-W7.md:164` and `:211`.

### **D5 · MAJOR — a sixth inbound edge, and it is declared at ONE end. `KF-W2.md` asserts reciprocity that `KF-W7.md` does not carry.**
`KF-W2.md:571`: *"**KF.W7 may not decide a failure posture before KF.W2 publishes the registry** (declared at both ends — KF-W7.md G14 resolves against this registry, verified)"*, restated at `:608` as *"Both directions are now declared at both ends."*
At this end: §Sequencing's **"Hard ordering (blocking, in order)"** block enumerates **8** items — none of them this one. The `KF.W2/W3` cross-edge (`:344`) lists the posture-registry rows as **OUT** only; its **IN** leg names solely the parse façade (OP-5, a different artifact). G14's assertion (`:316`) requires *registration* in W2's registry — a completion condition, not an ordering. So a hard, blocking, inbound ordering that W2 believes is bilateral exists at W7 only as a registration clause. This is the exact W10-seam class R3-10 cured five times and a sixth member of that table.
**Receipt**: `KF-W2.md:571`, `:608` vs `KF-W7.md:326-334` (8 hard-ordering items), `:344`, `:316`.

### **D6 · MAJOR — a second paraphrase under a "verbatim" label, this one eliding a qualifier with no ellipsis.**
`KF-W7.md:136` prints **"MISS-β3 cure-shape lock verbatim"**: *"the guard the author DID write is INERT — `@click` listeners on the track = 0, so :12/:25 `@click.stop` stop nothing — the fix REPLACES the inert guards with `@pointerdown.stop`, it does not stack a third modifier."*
`kf-TimelineCaret.md:74` reads: *"…the guard the author DID write is INERT**:** `@click` listeners on the track = **0** **(my grep; KeyframeTimeline's five `@click`s are button-local)**, so `:12`/`:25` `@click.stop` stop nothing — the fix REPLACES…"*. The colon becomes an em-dash, the scoping parenthetical — the cell that says *why* the count is 0 and where the exception lives — is dropped **without an ellipsis**, and the anchor backticks are stripped. Same class as D3 and as the struck round-2 PR-CAUTION.
**Receipt**: `KF-W7.md:136` vs `kf-TimelineCaret.md:74`.

### **D7 · MINOR — internal contradiction survives the round-3 receipt: "BOTH rows" in the headline, three rows in the receipt.**
`KF-W7.md:354` reads *"the rule applied EXHAUSTIVELY, over **BOTH rows** that carry the disposition, not one"* — and then, inside the same bullet, the round-3 replacement receipt correctly enumerates **three dispositions in two records** (D-1 · L-2/C-2 · C-8/D-6) and re-states *"**three rows, two records**"*. The round-2 headline ("BOTH") was written when the count was two; the receipt moved to three and the headline did not. A seat reading the bold clause and stopping there under-counts the very set OP-0 exists to protect.
**Receipt**: `KF-W7.md:354`, headline clause vs the `⟨…⟩` receipt in the same line; this seat's re-execution at §2.D returns three.

### **D8 · MINOR — one of the two spec-local labels is bound to a section name that returns 0 by grep, breaking the round-3 (c)(4) stable-anchor idiom asymmetrically.**
`KF-W7.md:111` binds **`L-15-PROTECTED` ≡ `kf-KeyframeTimeline.md` § *the adjudicator's kill register* → entry `K-8 · L-15`**. The record's literal heading is **`## Killed-claims register — 10 rows, refutations re-executed`** (`:116`); `grep -n 'kill register' kf-KeyframeTimeline.md` → **0**. Its sibling label one line above (`PR-CAUTION`, `:110`) uses the section's **literal** title and resolves. The banked id `K-8 · L-15` does resolve (`:125`, exactly as the parenthetical says), so the anchor is recoverable — but the (c)(4) repair claimed the re-cut at "record + section + banked id", and half of it is a description, not a section.
**Receipt**: `KF-W7.md:111` vs `kf-KeyframeTimeline.md:116` (heading) and `:125` (entry).

### **D9 · MINOR — a "from both ends" declaration whose second end does not exist.**
`KF-W7.md:228` (the R-7 row, LAW A consequence): *"any landing that deletes or re-points `KeyframesAddDialog.vue` must carry `KeyframesEditor.vue:75`/`:118` in the SAME atomic act, and that act **is declared to the KFED-UNIT owner from both ends** exactly as the CARD-UNIT cure site is."* **KFED-UNIT is NO-WAVE-OWNER** (`kf-KeyframesEditor.md:36`) — an SS-1/SS-2 authoring packet with **no spec file**: `ls waves/` returns no KFED/EDITOR document. There is no second end at this clock, so the reciprocity is unverifiable and the fold's one repoint site has a declared constraint but no declared owner-document. The finding itself (the fold's SOURCE census, the out-of-bounds consequence, G15 as a *ruling* gate) is excellent and correctly reasoned; only the bilateral claim over-reaches.
**Receipt**: `KF-W7.md:228`; `kf-KeyframesEditor.md:36`; directory listing of `docs/tranches/X/keyframes/waves/`.

### **D10 · MINOR — a row carried under a header verb its registry home contradicts.**
`KF-W7.md:122` heads the P0 riders row *"**KF-AV-13 / KF-AV-14 / KF-AV-17 / KF-AV-24 (evaluate-input riders)** MINOR / spec-input, **decisions owned here**"*. **KF-AV-17**'s registry disposition (`kf-AnimationVisualizer.md:61`) is *"→ **folds into KF-AV-1's cure**"* — KF-AV-1 is NO-WAVE-OWNER and is not routed to this wave. The row's body does treat KF-AV-17 correctly, as a **CURE-SHAPE LOCK** rather than a decision, and the lock's content (K-12's `translateX(calc(var(--p) * (100cqw − 100%)))`) is sound and worth carrying — but the shared header verb makes a fold-target read as a W7-owned decision, and the row never states where KF-AV-17 actually lives. *(No escape: nothing is lost; the disposition is mis-labelled, not dropped.)*
**Receipt**: `KF-W7.md:122` vs `kf-AnimationVisualizer.md:61`.

---

## §4 What HOLDS — recorded so the repair does not regress it

- **Census carriage**: **0 escaped**, and every one of the 16 fold-carried lines lands at a carried banked id. The three §Excluded-with-named-owner rows verify **at both W6 ends** by banked row header (the round-2 numeric coordinates having been correctly retired).
- **Frontier witnesses**: 31 of 31 re-executed reproduce, several *exactly* to the line (G5's six anchors, G11's thirteen coordinates, G13's two delta reads, G14's six toast lines). The G8 file attribution silently corrects the bank's ambiguous `:203-214`. The `--rail-width` and `timelineEngine` 102-vs-104 corrections both verify.
- **LAW A**: both delete/repoint censuses reproduce command-for-command from an independent execution; the `loadPreset` re-export consequence and the out-of-bounds repoint consequence are both real and both correctly binding.
- **LAW B**: no self-voiced closure claim survives in the spec's own voice — the verb table, §Carry's anchor-cleanliness sentence, §Gates' witness blanket and §Excluded's opener are all converted to cited form, and the struck predecessors are preserved as dated log rather than rewritten (E-3 honoured).
- **Taxonomy reconciliation**: the round-3 replacement receipts are the real thing — both commands re-executed here return exactly what the spec pastes, and the diagnosis of why the round-2 command was not a receipt is correct.
- **Gate reachability**: all 15 born-RED, none stamped, none resting on an unpassable oracle. G15's ruling-gate framing is the right resolution of the out-of-bounds fold, not an evasion.

---

## §5 Local verdict

**DEFECTIVE.** The census axis is clean and the frontier-witness and LAW A axes are the strongest this file has been. The defects cluster where the previous three rounds also cluster: **quotation discipline** (D3, D6 — the D8 class recurring at unswept sibling sites), **rung/dissent fidelity** (D2, D4), **one-ended edges** (D5, D9), and **a census figure that outruns its stated rule** (D1). D1, D2, D3, D4, D5 and D6 are the repair round's work; D7–D10 are one-line corrections.

*Sole write = this file. Read-only at both trees. `origin/master 81a56990` for every keyframes witness; the value.js registry at this seat's clock. No gate stamped, no ruling declined, no prior-pass artifact edited, nothing inherited from PASS-3's prose — every figure, receipt and quotation above was re-executed here before it was written.*
