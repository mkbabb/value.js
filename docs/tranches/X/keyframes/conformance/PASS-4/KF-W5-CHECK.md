# KF-W5 — PASS-4 FRESH ADVERSARIAL SPEC CHECK (L-18/L-20)

**Subject**: `/Users/mkbabb/Programming/value.js/docs/tranches/X/keyframes/waves/KF-W5.md` — 431 L, mtime 15:49, read WHOLE at this seat's clock.
**Seat**: FRESH. Nothing inherited from `PASS-1`, `PASS-2`, `PASS-3/KF-W5-CHECK.md`, `PASS-3/RULINGS-3.md`, or from any prior text at this path. **The inherited nine-block partition is RETIRED (RULINGS-3 R3-5)**: every census row below is re-harvested **by record** from the 58 adjudicated `kf-*.md` files' bytes, whole sentences resolved. Sole carry consulted: `carry/KF-W6-CARRY.md`.
**Tree law**: witnesses verify at the keyframes frontier, `origin/master` **`81a56990736ced5b5edde0b84c527680ac7689b1`** (`git rev-parse origin/master`, this seat). Local HEAD `8281638c` (**252 dirty**, `git status --porcelain | wc -l`) is **historical context only**, never a witness substrate. Every keyframes probe below is a read-only `git show` / `git ls-tree` / `git grep` / `git cat-file`. **No product source was opened for writing.**
**Sole write of this seat**: this file.

**VERDICT: DEFECTIVE.**
Axis 1 (by-record census) **CLEAN** · Axis 2 (anchor + SUBJECT-IDENTITY) **FAIL — three MAJOR** · Axis 3 (M-25 depth) **CLEAN** · Axis 4 (gates born-RED / reachable GREEN · LAW A · LAW B) **FAIL — one unpassable oracle, two false census universals** · Axis 5 (posture) **PASS**.

The round-3 repair is **real and large**: G-XSS's inverted oracle is genuinely cured and its GREEN is now reachable (re-executed here); G-PRM-FLIP is genuinely re-anchored at the carve's leaves; every struck denominator now reproduces with its probe; the four LAW-A censuses are substantively correct where they are the authorizing instrument. **What survives is a fourth generation of the same class the program has now convicted three times: a perfectly cured enumerated set, and the class not closed one level up** — this round it is the *anchor-content* class (R3-4's own SUBJECT-IDENTITY check, ruled at KF.W4 and **never run over this wave's own §Bounds**) and the *false-universal* class (two closure sentences inside the LAW-A census block, the one block whose subject is that assertions are not censuses).

---

## §1 · Axis 1 — ID-KEYED CENSUS, ENUMERATED BY RECORD

### 1.1 Method (partition-free)

`for f in kf-*.md; do grep -c 'W5' "$f"; done` over all 58 records → **22 files carry the bare token**; `grep -l 'KF\.W5\|KF-W5'` → **19**. Every hit in all 22 was then **read in full sentence** and resolved:

- **16 records route at least one row** (below).
- **3 carry only a `KF.W5-PARTIALS` taxonomy header** — kf-AnimationControlsGroup `:17` · kf-ControlsPaneWrapper `:6` · kf-DemoGlobalChrome `:17`. Each is a *"Wave targets … (`lane-frontend.md §10`)"* header line; **0 row hits**. Reproduced by direct grep.
- **3 are false positives on the bare token** — kf-SquareInstrument `:7`/`:21` (`R.W5`, tranche R) · kf-OrbitalDrag `:82` (`P.W5.S3`, tranche P ledger register) · kf-HeroAurora `:32` (routing-law prose naming the taxonomy; routes nothing).
- **kf-SquareScene and kf-ChannelOptions return `grep -c 'W5'` = 0** at the bytes. Their engine-seam / engine-lane rows are homed here by **§0 R-1**, not by token — the spec's own correction reproduces.
- **No alternate routing spelling exists**: `tri-fold`/`trifold` and `D/L/C` hits across the corpus were resolved individually; every one is either a routing-law recital or a member of the 16 already counted. No 0-W5 record routes by "library letter", "library roster", "library arm", or "engine seam".

### 1.2 The routed ids, by record, and their booking

| record | routed ids (whole sentences resolved) | n | booked in `KF-W5.md` |
|---|---|--:|---|
| kf-AnimatedText | KF-AT-8 `:43` · KF-AT-9 `:44` · KF-AT-12 `:52` · KF-AT-23 `:63` | 4 | B-1 · B-2 · Arm A (PREMISE) · B-3 |
| kf-App.skeleton | KF-SKEL-9 `:44` · KF-SKEL-16 `:56` · KF-SKEL-20 `:60` | 3 | §Excl 2 · 3 · 4 |
| kf-CopyButton | KF-CB-30 `:31`/`:78` · KF-CB-35 `:83` · KF-CB-36 `:84` | 3 | B-19 · B-20 · B-21 |
| kf-EasingScene | KF-ES-36 `:78` | 1 | §Excl 1 |
| kf-EasingSidebar | KF-ES-20 `:61` | 1 | Arm A |
| kf-EasingTarget | KF-ET-1 `:37` · KF-ET-27 `:69` · KF-ET-32 `:77` · KF-ET-33 `:78` · KF-ET-35 `:80` | 5 | B-22 · Arm A · B-12 · §Excl 1 · §Excl 1 |
| kf-EditorStartScreen | KF-EST-5 `:24`/`:62` · KF-EST-17 `:79` · P-8 `:136` | 3 | B-4 · B-17 · B-4 leg 2 |
| kf-KeyframeCard | KF-KC-27 `:69` | 1 | B-7 |
| kf-KeyframesAddDialog | KAD-1 · KAD-2 · KAD-3 · KAD-5 · KAD-13 · KAD-14 · KAD-17 | 7 | Arm 0 ×5 · §Excl 5 · §Excl 6 |
| kf-KeyframesEditor | KF-KE-10 `:52` | 1 | B-9 |
| kf-KeyframesStringControls | N-8 `:55` · C-8 `:81` | 2 | B-10 · B-11 |
| kf-SpringPhysicsFacet | SPF-20 `:60` | 1 | fold at B-9 |
| kf-SpringScene | KF-SS-4 · KF-SS-6 · KF-SS-31 · KF-SS-38 ⟨the `:151` KF.W5 roster line names all four⟩ | 4 | §Excl 1 (all four) |
| kf-TimelineHoverPreview | C-8 `:85` ⟨≡ KF-SKEL-16⟩ | 1 | §Excl 3 (folded) |
| kf-TimingFunctionPanel | KF-TFP-21 `:64` · KF-TFP-27 `:73` | 2 | B-13 (one row, two ids) |
| kf-TypingDots | KF-TD-1 `:57` · KF-TD-5 `:61` · KF-TD-8 `:64` · S★-2 `:88` | 4 | B-6 · B-8 · B-18 · B-5 |

**routedTotal = 43** (4+3+3+1+1+5+3+1+7+1+2+1+4+1+2+4).

**Booking verified by bytes, id by id** (`grep -o '<id>' KF-W5.md | wc -l`, run for all 43): every one returns ≥1 — KF-CB-36 1 · KF-KC-27 1 · KF-TD-1 1 · KF-TD-5 1 · KF-TD-8 1 · S★-2 1 are the singletons; KAD-1 29 · KF-ES-20 11 · C-8 11 · KAD-14 11 the densest. **bookedCount = 43 · escapedCount = 0.**

**Whole-sentence resolution added nothing the spec dropped.** kf-TypingDots' `:39`/`:46` W5 hits are *witness ×2* re-statements of kf-EditorStartScreen's KF-EST-5/KF-EST-17 (correctly carried at B-4/B-17 as `witness ×2`, not re-booked); kf-EasingTarget KF-ET-1's *"+ D-15 and C-2-as-rescoped folded"* travels with B-22's rider carriage; kf-KeyframesAddDialog KAD-14's `(a)(b)(d)(e)` / `(c)` split is carried whole (a/b/d/e at Arm 0, c at §Excl 6); kf-CopyButton KF-CB-30's `C-11 = L·M-4` identity is carried at B-19 with its dissent.

### 1.3 The ledger arithmetic, re-derived not inherited

Table rows counted mechanically: **Arm 0 = 7** · **Arm A = 8** · **Arm B = 22** (B-1…B-22) · **Arm C = 3** · **Arm D = 6** → **46**. §Excluded = item 1's seven prose rows + items 2–6 → **12**. **46 + 12 = 58** ✓. The 30/13 split reproduces (§Excluded's 12 rows carry 13 ids — item 3 carries KF-SKEL-16 *and* kf-TimelineHoverPreview C-8; 43 − 13 = 30 across 27 §Carry rows, since B-4 = KF-EST-5+P-8, B-9 = KF-KE-10+SPF-20, B-13 = KF-TFP-21+KF-TFP-27). §Gates holds exactly **20** gate rows (`grep -o '^| \*\*G-[A-Z-]*\*\*'` → 20).

**AXIS 1 — CLEAN.** No escape by bytes. No row inside a table the tally omits. No double-home. No id renamed or re-homed away from its bank. The retirement of the nine-block partition changed nothing about the answer, which is the point of ordering it.

---

## §2 · Axis 2 — ANCHOR RESOLUTION **AND SUBJECT IDENTITY**

R3-4.3 ruled the SUBJECT-IDENTITY CHECK for the silent-mis-resolution class: *for every §Bounds row naming a symbol, `git grep -n '<symbol>' origin/master -- '<the row's own file>'` must return the row's stated lines — an anchor's CONTENT must mention its SUBJECT.* The ruling was addressed to KF.W4 and **swept over KF.W4's rows only**. Run here over KF.W5's §Bounds for the first time.

### 2.1 What resolves (the large majority)

Re-executed at `81a56990`, all exact: `group/types.ts` **`:27-29`** `AnimationGroupInput` (the round-3 correction of the bank's `:27-30` is right — `:27` decl, `:28`/`:29` the two arms, no `options` field) · `group/group.ts` `:159-161` the `animations.every(a => a.targets[0] === animations[0]?.targets[0])` derivation and `:195-204` the `setTargets` recompute (both verbatim) · `group/waapi.ts` `:31-33` the multi-target refusal inside `isGroupWAAPIEligible` (`:27`) · `stagger.ts` `:15-22` the docblock example passing `options: { delay }` at `:20` · `play-lifecycle/strategies.ts` `:66-75` docblock over `snapToReducedMotion` `:76`, the quoted sentence byte-exact · `frame.ts` `:36-38` the `delay > 0` sleep, `:85` `startTime = t + anim.options.delay`, `playFrame` declared `:121`, the `snapToReducedMotion` call `:137` · `delegation.ts` `:53` `shadowTick` / `:64` `playback.loop(shadowTick)` · `view-transition/view-transition.ts` `:197` `respectReducedMotion = true` · `engine/animation.ts` `:219` `get frames()` · `load-engine.ts` `:123-124` the `??=` memoization · `emit/index.ts:53` · `backward/walk.ts` cssIdent **5 hits** (`:9` doc, `:89`/`:110`/`:131` applications, `:145` declaration) · `backward/backward.ts` `:253-257` the unguarded chain, `:259-266` the try/catch whose comment names the `serializeEasing` throw, `compileToCSS` `:352` · `constants/types.ts` `:182` `Partial<{…}>` and `:195` `| string` · `easing.ts` `:50` `cssTwinFor` (its `:41-50` docblock) and `:79` the lazy `import("./compile/easing/registry")` · `compile-bridge.ts` `adoptCompiled` `:88` · `engine/css/animation.ts` `:108` *"reference comparison (`usesDefaultRenderer`)"*, `resolveTransform` `:110` called `:118`/`:135`/`:170` · `physics/playback.ts` `_run` `:113` inside the cited `:110-152`, `const result = step(now)` `:139` · `split-text.test.ts:72-76` exactly the explicit-`role="heading"` test · `split-a11y-oracle.test.ts` `PRE_SPLIT` `:51`/`:97`/`:173` · `test/demo/instrument/` holding **exactly the nine** named files, `highlight-css-roundtrip.test.ts` absent · **none of the 13 created filenames collides with a tracked one** · `package.json` `:37` `check` (three legs), `:38` `check:lib`, `:44` `lint`, `:50` `proof:structure`, `:81` `@vitejs/plugin-vue ^6.0.7` · `tsconfig.lib.json` include `["src/"]`, `tsconfig.test.json` include `["test/","bench/","demo/env.d.ts"]` · `vitest.config.ts` `projects` `:38-56`, `library` excluding `test/demo/**` · `useHighlightCSS.ts` `:104` bare `setCodeTheme()`, `:110`/`:117`/`:124` the marker triple, `:111`/`:123` the two writes.

Cross-spec receipts re-resolved by stable anchor: `KF-W4.md` §Carry → *"The ten rulings this spec owes"* (`:91`) → **R-1** (`:92`) carries the sentence at **`:116`** verbatim ✓ (the round-2 note that it drifts is again proved — it has moved from `:115` to `:116` since pass 3, and the stable anchor still holds). `KF-W6-CARRY.md` → *"KF.W4-PROSE SPLITS BY MECHANISM"* ✓ · `:170` KF-AT-6/S-5 ✓ · `:248` KAD-3 ✓ · `:319` *"(KAD-1 is KF.W5's)"* ✓ · `:356` the → KF.W5 edge carrying **verbatim** *"KAD-1's `textContent` sink fix is **FRONT-LOADED at W5**"* ✓ · `:9-13` the KF.W5-PARTIALS reconciliation ✓. `KF-W7.md:57` *"its `innerHTML` BLOCKER is KF.W5's, front-loaded, NOT gated here"* ✓, reciprocated at `:345`/`:365`. `KF-W4.md:156`/`:198` *"126 demo files, 0 `.vue`"* ✓. `KF-W4` reciprocates the third rider at **row 3 (`:158` ME-32)** and **G-KFW4-2 (`:204`, `npx vitest run --project demo` as a blocking merge step)** ✓.

### 2.2 The failures — three §Bounds rows whose CONTENT does not mention their SUBJECT

**D-1 · MAJOR — `src/animation/internal/reduced-motion.ts` is granted `modify-carve` for a symbol that is not in it.**
The row reads: *"`ReducedMotionPolicy`; **`snapToReducedMotion`'s single caller**"*. Run this seat:
```
git grep -n 'snapToReducedMotion' origin/master -- src/animation/internal/reduced-motion.ts   → 0 hits
git grep -n 'ReducedMotionPolicy'  origin/master -- src/animation/internal/reduced-motion.ts   → :107 :110 :125 :144 :154
```
`snapToReducedMotion` is declared at `play-lifecycle/strategies.ts:76` and its **single caller** is `playFrame` at `frame.ts:121`, calling at `:137` — both in a *different* §Bounds row. R3-4.3's command, run over this row as written, prints nothing for half its subject. This is the row whose cure B-6 depends on, and the seat that opens `reduced-motion.ts` looking for the caller finds no caller there.

**D-2 · MAJOR — `src/animation/engine/option-setters.ts` is granted `modify` for `setTimingFunction`, which lives in another file.**
```
git grep -n 'setTimingFunction'  origin/master -- src/animation/engine/option-setters.ts  → 0 hits
git grep -n 'applyTimingFunction' origin/master -- src/animation/engine/option-setters.ts  → :32 (decl) :149 (call)
git grep -n 'setTimingFunction'  origin/master -- src/                                     → engine/animation.ts:260 (sole declaration)
```
The row's two subjects are **split across two files** and only one is named — the exact shape R3-4 convicted at KF.W4 row 78 (`seedFor`/`syncGap` filed at `useEasingDemo.ts` while living in `EasingSidebar.vue`). `engine/animation.ts` **is** in `.c`'s bounds under its own row, so the act is reachable; the *row* is not anchored to its subject, and **G-OPTSET's leg 1** (*"`setTimingFunction` normalizes but never reaches compiled frames"*) has no bounds row that mentions its symbol.

**D-3 · MAJOR — a §Bounds `modify` row with NO PATH AT ALL.**
`| the `setTargets` element-contract declaration (re-anchored) | modify | KF-CB-30 — the contract should be `Element`; the demo animates SVG |`. §Bounds' own opening law is *"All paths keyframes.js repo-relative under `/Users/mkbabb/Programming/keyframes.js/`"* and its CITATION LAW requires every anchor to be **banked with its record** or **re-derived by a printed read-only `git` command**. This row has neither, and the parenthetical *"(re-anchored)"* names no anchor. Measured this seat, the subject is not even singular:
```
git grep -n 'setTargets(\.\.\.targets: HTMLElement\[\])' origin/master -- src/
  engine/animation.ts:465 · group/group.ts:195 · orchestration/sequence/sequence.ts:310
```
Three declarations carry the `HTMLElement[]` contract; the wave's own `.c` ownership reaches all three, so a seat cannot infer which "the declaration" is, and a contract widened at one of the three leaves the other two disagreeing with it — the same "two organs of one engine disagree" defect B-7 exists to rule on.

**AXIS 2 — FAIL** (D-1, D-2, D-3). Every *coordinate* in the file resolves; three *subjects* do not live where their rows say they do, and the check that finds this class was ruled this very round and pointed at a sibling wave.

---

## §3 · Axis 3 — M-25 DEPTH (locks, riders, dissents, folds)

Re-read against the banks, not against the prior passes:

- **Preserved dissents**: §0 R-3 (KF-KE-10 MAJOR vs SPF-20 BLOCKER-at-its-bank — bank governs, dissent preserved, and stated as the reason B-9 heads the ruling queue) ✓ · B-19 (*"Reader-2's MINOR as dissent"* against the ruled INFO, matching kf-CopyButton `:31` verbatim) ✓ · B-14 (RR-A whole-at-BLOCKER / RR-B WAAPI-half-BLOCKER → RESCOPED-BY-SUBSTRATE) ✓ · B-4 (kf-TypingDots ruling 3 **overruling** reader-2's demotion — *"the kill demotes the CURE only, never the defect"*) ✓ · B-6 (C's S★-4 second bullet STAYS KILLED; L's S-3 SURVIVES SCOPED) ✓.
- **Cure-locks carried, not paraphrased**: B-22's KF-ET-2 lock (never *"bind `cssValue`"*, never a silent approximation; `easing.ts:41-50`'s doctrine as the authority) with the row itself NOT taken ✓ · §Excl 1 KF-SS-38's *"the sweep must not resurrect the killed claim"* ✓ · §Excl 6's *"the killed BLOCKER arm (mangled parse) is DEAD and must stay dead"* ✓ · §Excl 2's *"the cure may not be authored in a way that resurrects C·D-4's premise"* ✓ · KAD-14(a) as a MUST-CARRY into KF.W6 ✓ · D-6's R-14 addendum carried **verbatim** with its correcting sentence beside it (E-3, never a patch) ✓.
- **Riders carried as rows, not merely cited**: LP-7's rider and ME-32 are Arm 0 **rows** (which is why Arm 0 is 7, not 5) precisely because G-XSS's observability depends on them ✓ · kf-ChromeDock M-4's MbabbMenu MUST-CARRY rider travels with the packet ✓ · kf-SquareScene's *"treat every comment-stated invariant as a test obligation (L-19)"* carried verbatim ✓.
- **Fold identities never re-booked**: SPF-20 ≡ B-9 · THP C-8 ≡ KF-SKEL-16 · KF-CB-15 ≡ m-4 ≡ C-7 folded as C-3's witness with its bytes named · KF-CB-6 folded at B-7 · KF-CE-42 folded at B-11 · KF-ET-34 folded at §Excl 1 · KF-SKEL-14 kin at §Excl 4 · KAD-14(d)'s T `lane-18` #6 identity ✓.
- **Transcription-only**: absent. Every arm carries a mechanism, a lock, or a ruling; the four RULINGS (`fromString` · `delay` · PRM default · `singleTarget`) each precede their fixes with a stated reason.

**AXIS 3 — CLEAN.**

---

## §4 · Axis 4 — GATES: BORN-RED WITH **REACHABLE** GREEN · LAW A · LAW B

### 4.1 What re-executes exactly

**Structure, re-measured whole at `81a56990` this seat**: `git ls-tree -r origin/master --name-only -- src | grep -c '\.ts$'` → **153** (B0) · `.d.ts` → **0** ✓. God-module sweep at the ≥437 floor over all 153 → **exactly eight**, `progress.ts` 484 · `engine/animation.ts` 478 · `draggable.ts` 470 · `ingest/cssom.ts` 466 · `compile/frame/compiler.ts` 461 · `compile/emit/entry.ts` 459 · `classic-data.ts` 458 · `group/group.ts` 437, **no new entrant**, `engine/play-lifecycle.ts` MISSING ✓. Stutter predicate run **verbatim as printed** → **12**, the identical twelve paths ✓.

**Depcruise, re-anchored line by line**: file **253 L** ✓ · `LIGHT_BARREL_MODULES` opens `:54`, closes `:79`, **24 entries** (`sed -n '55,78p' | grep -c '"'` → 24) ✓ — the round-3 correction from 25 is right · `LIGHT_FROM` `:84-86` ✓ · `no-cycle` `:113`, comment `:116-131` with the honest denial spanning **`:125-129`** verbatim ✓ · `viaOnly.dependencyTypesNot` **`:148-149`** ✓ · rule 2 `:163` ✓ · rule 3 `:203` ✓ · `grep -c 'knownViolations'` → **0** ✓ · `grep -c 'known-violations'` → **3** (the denial) ✓. **The three-clause property oracle is sound** and its round-1 count-inversion is genuinely dead.

**D-1's RED at the frontier**: `physics/spring/{duration,reseat,linear-stops,timing-function}` name **zero files**; the real modules are `solver/{duration,reseat}.ts` and `css/{linear-stops,timing-function}.ts`; all seven §Bounds paths PRESENT ✓. Presets → `catalog.ts`, `classic-data.ts`, `index.ts` only; `grep -c 'split by kind'` → **0** ✓ (G-SHIM honestly a floor).

**G-XSS's re-cut is verified sound and its GREEN is reachable.** `git grep -n 'innerHTML' origin/master -- …/useHighlightCSS.ts` → **two lines**: `:111 el.innerHTML = s;` inside `setHighlightingString` (`:108`, sole caller `KeyframesAddDialog.vue:103`) and `:123 el.innerHTML = h.value;` inside `highlight()` (`:116`), immediately after `const h = hljs.highlight(el.innerText, { language: "css" });` at **`:122`** — read at the bytes. The property form `| grep -v 'h\.value'` returns **1 today**, **0 after the cure**. The identifying predicate is the `h.value` write, exactly as R3-3 ordered. **This was the pass-3 BLOCKER and it is CLOSED.**

**G-PRM-FLIP's re-anchor is verified.** `snapToReducedMotion` under `src/` → **7 lines / 3 files** (import `frame.ts:14`, call `frame.ts:137`, decl `strategies.ts:76`, docblocks `strategies.ts:7`/`:10`, `index.ts:11`/`:23`) — the spec's honest divergence from the ruling's ordered *"4 files"* is correct and correctly disclosed. **This was the pass-3 D-3 and it is CLOSED.**

**G-TAX is genuinely born-RED**: `grep -c 'SUPERSEDED\|superseded'` over the three PARTIALS records → **0 / 0 / 0**.

### 4.2 The failures

**D-4 · MAJOR — G-TAX's GREEN is UNREACHABLE INSIDE THE WAVE'S OWN BOUNDS, and no inbound dependency is declared.**
The gate's pass condition is: `grep -rn 'KF.W5-PARTIALS' docs/tranches/V/megatranche/registry/adjudicated/` → *"exactly **3** header hits, **each annotated SUPERSEDED**; **0** row hits"*, and its falsifier is *"Fails if the registry note is not carried."* Turning it GREEN requires **writing three files in `registry/adjudicated/`**. §Bounds' *"### Read-only witnesses this wave cites and must not rewrite"* names *"the value.js authorities: `registry/adjudicated/` (**all 58 kf-*.md**)"*. The wave therefore commands an edit to a surface it forbids itself to edit, and the disposition it actually carries is a **relay** (RD-2, *"Relayed to SS-1/SS-2"*) — an act by a different owner. Measured born-state: 3 header hits, 0 SUPERSEDED annotations, 0 row hits.
This round created the exact instrument for this shape — **R3-8's INBOUND-DEPENDENCY GATE** (KF.W8's G10 re-cut from a born-RED act to a dependency-cite on KF.W4's commit 2) — and the wave applied it to none of its own gates. It also has the local precedent in its own file: G-XSS's **declared observability rider**. G-TAX has neither. **An oracle no seat inside the bounds can turn is not born-RED; it is unpassable.**

**D-5 · MAJOR — the LAW A census block asserts a false universal about the alias graph, refuted by the wave's own B-11.**
§Bounds Act 1 closes: *"Zero `@src/*` spellings reach any of the twelve … ; **the demo reaches the library only through `@mkbabb/keyframes.js`, the root barrel**, which no rename touches."* Measured this seat:
```
git grep -nE '@src/animation' origin/master -- demo/   → 9 hits, 7 files
  useKeyframeOps.ts:1 @src/animation/compile/emit/css-text
  parseAnimationCSS.ts:7 @src/animation/compile/emit/css-text
  timelineEngine.ts:1 @src/animation/internal/helpers · :17 …/compile/emit/css-text
  AnimationVisualizer.vue:45 @src/animation/resolve/browser
  demo/utils/helpers.ts:9 @src/animation/resolve/browser
  demo/utils/keyframeSelector.ts:5 @src/animation/compile/selector
  demo/kf-engine.ts:5 · useOrbitalInertia.ts:12 (prose)
```
The **narrow** clause holds (none of the nine reaches any of the twelve rename subjects). The **broad** clause is false, and it is false *by the file's own contents*: **B-11 / Act 4 exist entirely to rule on `reverseCSSTime` (`useKeyframeOps.ts:1`) and `serializeTimingFunction` (`parseAnimationCSS.ts:7`) as demo `@src/` deep reaches**, and kf-KeyframeTimeline C-12's `@src/animation/internal/helpers` reach is cited at B-11's kin clause. LAW A's own text requires *"EVERY alias root that can reach P"* be run **as its own pattern**; the block runs the alias census and then over-states its result into a universal the same page refutes. This is the LAW-B voice defect landing inside the LAW-A instrument.

**D-6 · MAJOR — *"the only `cssIdent` re-export"* is false at the frontier, and Act 4's own census is the refutation.**
Stated at three sites — §Bounds (`compile/emit/index.ts` … *"`:53` — the **only** `cssIdent` re-export"*), **B-10** (*"re-exported **only** at `compile/emit/index.ts:53`"*), and **G-CSSIDENT's RED-witness cell** (same words). Measured:
```
git grep -n 'cssIdent' origin/master -- src/animation/compile/emit/backward/index.ts
  :29  export { cssIdent } from "./walk";
git grep -n 'cssIdent' origin/master -- src/animation/compile/emit/index.ts
  :53  export { cssIdent } from "./backward";
```
**Two** re-exports, in a chain (`walk.ts:145` decl → `backward/index.ts:29` → `emit/index.ts:53`). Act 4's own consumer column **lists `backward/index.ts:29`** — so the file censuses the second re-export and then, three rows later, denies it exists. The load-bearing half (*zero* hits in `index.ts`/`public.ts`/`load-engine.ts` — verified) is TRUE and the publication decision is unaffected; the word *"only"* is not, and it sits in the RED witness of the gate the wave calls the **head of three BLOCKERs**.

**D-7 · MAJOR — the four-party disjointness statement mis-counts its own enumeration: "eleven" over a list of TEN.**
§Disjointness, DH-3 cure text: *"`highlight-css-roundtrip.test.ts` appears in none of W4's, W7's or W8's **eleven** created names."* The same block enumerates **W4 2** (`typing-dots-engine-seam.test.ts` · `aurora-opacity-ceiling.test.ts`) + **W7 4** + **W8 4** = **10**. Verified at the siblings' current bytes: `KF-W4.md:75`/`:76` (2 creates) · `KF-W7.md:63-66` (4 creates) · `KF-W8.md:95`/`:96`/`:97` (4 creates across 3 rows, keyed G10/G11/G12 — the spec's "three create rows / four files" reading is correct). **Eleven is the FOUR-party total** (10 + this wave's own one), which is how `KF-W7.md:79` uses it; W5 attributes it to the three *other* waves. In a wave whose ownership is **by filename**, and which was convicted at pass 3 (its D-4) for exactly *"a count that does not match its own list"*, the same class recurs inside this round's cure for a different ruling.

**D-8 · MINOR — the COUNT-ORACLE INVERSION SWEEP claims a verdict it does not print for eleven of twenty gates.**
The block opens *"run over the **twenty** gates enumerated below, **verdict printed per gate**"* and prints verdicts for **nine**: G-XSS (INVERTED, re-cut) · G-DEPCRUISE (sound) · G-SHIM (sound as a floor) · G-TAX (sound, deliberately non-zero) · G-CSSIDENT (witness, not oracle) · G-ID (property, not count) · G-STRUCT / G-BASIS / G-RING (figures with probes). **No verdict** for G-ROLE · G-REFUSE · G-REVERT · G-STAGGER-DOC · G-PRM-FLIP · G-DELAY · G-FROMSTRING · G-OPTSET · G-RAF · G-RENDERER · G-SCOPE. The substance is defensible (those eleven carry vitest/tsc/enumeration oracles, not counts); the printed claim is not.

**D-9 · MINOR — the round-3 cure for pass-3's D-5 re-introduced cross-spec line coordinates, and they died within one minute.**
§Excluded prints KF-SS-31's four sites as *"`waves/KF-W6.md:152`, `:158`, `:356`, `:384`"*. Re-run this seat: KF-SS-31 occurs at **`:178`, `:184`, `:384`, `:412`** — **one of four survives**. The **count reproduces (0 / 4)**; the coordinates do not. `KF-W6.md`'s mtime (15:50) is one minute **after** `KF-W5.md`'s (15:49), so this is lawful drift, not write-time falsity — but it is the file's own condemned shape: §0 R-1.4 and RD-3 retired line numbers on cross-spec receipts on the rule that *"a coordinate that has to be re-measured every round is not a receipt"*, and the sentence that says so sits 200 lines above the sentence that breaks it.
*(Drift confirmation, recorded not convicted: at this clock the receiving-end counts read KF-ET-35 **0/3** and KF-SKEL-9 **0/4** against the printed 0/2 and 0/3; the other thirteen reproduce exactly, KAD-1 12/16 · KAD-3 2/1 · KF-AT-8 2/5 among them, and the CARRY side is confirmed **0/10** as the spec states it owed.)*

**D-10 · MINOR — LAW A carries no consumer census for the KAD-14(e) dead-export delete.**
Arm 0's §Bounds row grants *"the unused `highlight` export (KAD-14(e))"* and the §Carry row asserts *"used by no consumer"* — inherited from the bank's prose, which LAW A forbids as a source. Re-derived this seat, **the claim is TRUE**: `useCodeHighlight` returns `{ setHighlightingString, highlight, highlightAll }` at `:147`, and its only two consumers destructure `{ highlightAll }` (`KeyframesEditor.vue:176`) and `{ setHighlightingString, highlightAll }` (`KeyframesAddDialog.vue:92`). LAW A's literal subject is a *module* at a path, so the omission is arguable — but the file's own **Act 4** applies LAW A at symbol level to five names, which is the reading it must then apply consistently.

**D-11 · MINOR — Act 1's view-transition row undercounts its own non-consumer barrel set by one.**
The row prints *"| **2** … | 4 barrel **+ 7 CONSUMERS OF A SIBLING MODULE**"*. Measured: barrel hits are **5** — `index.ts:123`, `:128`, `orchestration/index.ts:52`, `:57`, and **`test/orchestration/view-transition.test.ts:10`** (`from "../../src/animation/orchestration/view-transition"`). Test barrel hits **are** counted on the sibling rows (`backward` counts `test/compile/value4-color-emit.test.ts:9`; `split-text` counts `test/orchestration/split-text.test.ts:15`), so the omission is inconsistent, not definitional. **The consumers (2) and the seven sibling-module hits reproduce exactly**, so the **32-site migration surface is unaffected** — the error is in the row the file singles out as *"the R3-1 shape, found here before it could authorize anything."*

**D-12 · MINOR — SUBJECT-IDENTITY on the read-only `waapi/emission.ts` row.** `git grep -n 'steps' -- src/animation/waapi/emission.ts` → **0**, for a row reading *"`:35-36` / `:45`,`:57` the `steps()` densify cost"*. `:35-36` are `multiSegment` / `bakeCurve` — the densify **gate**, not `steps()` — and `densify.ts:45`/`:57` reproduce exactly (`WAAPI_MAX_SUBSEGMENT_STOPS = 16`, `WAAPI_CHORD_TOLERANCE = 0.005`). Read-only row, no byte at risk.

**D-13 · MINOR — a spec-voiced completeness claim survives the LAW B sweep.** §Gates' BORN-STATE AUDIT closes *"**No gate in this file now claims a RED the frontier has already turned**, and no cure is authorised over bytes that do not exist."* That is a negative-universal over an enumerable set (the twenty gates) in the file's own voice — the shape LAW B item 1 bans and for which seven other sentences were converted this round. Verified **true** this seat (every born-state verb reproduces); the voice is the defect, and it is the voice that has been falsified at each of the last three passes.

**D-14 · MINOR — G-CSSIDENT routes a library spec through a demo module.** The gate's command runs `test/_root/public-surface.test.ts` (a `library`-project spec) and offers resolution *"from `kfEngine()` / `loadAnimationEngine()` / the exports map"*. At the frontier `kfEngine` is **demo-only** — `demo/kf-engine.ts`, reached by the `@kf-engine` alias (`vitest.config.ts:26`) — so the first alternative points a library spec at `demo/`. The other two are library-correct, and the gate's GREEN is reachable through them.

### 4.3 Born-state ledger, re-checked

| declared | re-checked at `81a56990` |
|---|---|
| G-XSS born-RED, observable half re-cut per R3-3 | ✓ **RED and now REACHABLE** — 2 writes today, property form 1 → 0 after the cure |
| G-PRM-FLIP born-RED, receipts re-anchored | ✓ RED; `strategies.ts:66-75`/`:76` and `frame.ts:121`/`:137` both exact; 7 lines / 3 files |
| G-DEPCRUISE L-1 RED at the ref of record | ✓ four dead paths, unmoved by 41 commits |
| G-DEPCRUISE L-2 CURED-AT-FRONTIER; three-clause oracle | ✓ denial `:125-129`; 0 / bare-`depcruise src` / file absent |
| G-SHIM declared regression floor, cure `7e9ddf49` | ✓ honest; three files gone, docblock true |
| G-STRUCT / G-RING / G-BASIS MEASURE-AT-OPEN with probes | ✓ 153 · 8 · 12 all reproduce; rings honestly unmeasured |
| G-TAX born-RED | ✓ RED (0 SUPERSEDED ×3) — but **GREEN unreachable in bounds, D-4** |
| the other twelve arm-B/C gates | ✓ every witness re-resolves at the frontier |

**AXIS 4 — FAIL** (D-4/D-5/D-6/D-7 MAJOR; D-8..D-14 MINOR). No BLOCKER: pass-3's BLOCKER is closed, and no surviving oracle can be driven GREEN by deleting something true.

---

## §5 · Axis 5 — POSTURE

| item | finding |
|---|---|
| **W4 head** | ✓ `→ KF.W4` declared **BLOCKING for the re-homed rows, not for this wave's bounds**; KF-AT-12 stated as PREMISE not cure; all **three** riders named (`noUnusedLocals` · depcruise-over-demo · `--project demo`/`test:demo`); **reciprocated at both ends** — `KF-W4.md:158` (row 3, ME-32 ≡ LP-7 rider, now also booking kf-EditorShell L-6/C-18 at R3-5.3) and **`KF-W4.md:204` G-KFW4-2** (`npx vitest run --project demo` **and** `npm run demo:correctness`, both blocking merge steps). OP-6's frontier restatement is exact: `package.json:37`'s three legs, leg 2 the `tsconfig.test.json` pass G-STAGGER-DOC leg 1 rides, leg 3 `proof:structure` — the script G-BASIS's fifth rival is owed to (`scripts/gates/structure/index.mjs` verified present) |
| **W3 gated** | ✓ `→ KF.W2/KF.W3 · Parser Consumption (GATED)` — *"This wave must NOT pre-empt the 4.0.0→4.1.x repin"*; `compile/emit/css-text.ts` reserved, and B-11's round-3 addendum correctly derives the consequence (**publication is a re-export in `public.ts`/`index.ts` only**, never an edit inside the reserved file). B-16's OP-4 both-dispositions pre-declaration keeps the row alive under either resolution |
| **KF-AV-28** | ✓ correctly **absent** — `grep -c 'KF-AV-28'` → **0**; the rider governs AnimationVisualizer / SequenceScrubber / PlaybackRibbon rows and this wave's §Bounds carries none of those components |
| **O-21 (not O-20)** | ✓ not this wave's surface — `grep -c` → **0** for both. The mint law is W1's |
| **the W10-seam five-edge table** | ✓ **KF.W5 is the source wave of none of R3-10's five edges** (W0 · W3 · W7 · W9 · W10), so nothing is owed from this end. The one W5-origin obligation in W10 is verified fulfilled at the receiving end: `KF-W10.md` carries **KF-W8-R-4-STRUCT-PAIR ⟨origin KF.W5 D-6⟩** at `:92`/`:175`/`:505` with terminal verb **`DECLINED-FOR-X·KF, carried forward`**, RECORD-ONLY, **plus the three re-open triggers carried verbatim** (`:177`, the R3-10.5 cure). W5's *"nothing here may be re-booked at W10"* and *"this wave's counting-basis row must not absorb them"* both hold from that end |
| **DH-3 four-party carriage** | ✓ in substance (all three siblings' files named by name, the W8×W4 overlap declared from a third end, the nine tracked files re-derived and exact) — **defective in its count, D-7** |

**AXIS 5 — PASS.**

---

## §6 · Register (worst first)

| # | sev | claim | receipt |
|---|---|---|---|
| **D-1** | MAJOR | SUBJECT-IDENTITY: `internal/reduced-motion.ts` is granted modify-carve for *"`snapToReducedMotion`'s single caller"* — a symbol with **zero hits** in that file | `git grep -n 'snapToReducedMotion' origin/master -- src/animation/internal/reduced-motion.ts` → **0**; decl `strategies.ts:76`, sole caller `playFrame` `frame.ts:121` calling `:137`. `ReducedMotionPolicy` does resolve (`:107/:110/:125/:144/:154`) |
| **D-2** | MAJOR | SUBJECT-IDENTITY: `engine/option-setters.ts` is granted modify for `setTimingFunction`, which is declared in another file — G-OPTSET leg 1 has no bounds row naming its symbol | `git grep -n 'setTimingFunction' -- …/option-setters.ts` → **0**; sole declaration `engine/animation.ts:260`. `applyTimingFunction` resolves at `:32`/`:149`. The exact R3-4 row-78 shape |
| **D-3** | MAJOR | a §Bounds **modify** row with **no path**: *"the `setTargets` element-contract declaration (re-anchored)"* — and the subject is not singular | §Bounds law: *"All paths keyframes.js repo-relative"* + every anchor banked-or-re-derived. Measured: `setTargets(...targets: HTMLElement[])` at **`engine/animation.ts:465` · `group/group.ts:195` · `sequence/sequence.ts:310`** |
| **D-4** | MAJOR | **G-TAX's GREEN is unreachable inside bounds** and no inbound dependency is declared — an unpassable oracle | GREEN requires *"each annotated **SUPERSEDED**"* inside `registry/adjudicated/`, which §Bounds lists under *"Read-only witnesses … must not rewrite"* (all 58 kf-*.md). Born-state measured: 3 header hits, **0** SUPERSEDED, 0 row hits. R3-8's INBOUND-DEPENDENCY idiom and the file's own G-XSS observability-rider precedent are both unused here |
| **D-5** | MAJOR | the LAW-A census block asserts *"the demo reaches the library **only** through `@mkbabb/keyframes.js`, the root barrel"* — false, and refuted by the wave's own B-11 | `git grep -nE '@src/animation' origin/master -- demo/` → **9 hits / 7 files**, incl. the two B-11 is built on (`useKeyframeOps.ts:1`, `parseAnimationCSS.ts:7`) and C-12's `internal/helpers` reach. The narrow clause (none reaches the twelve rename subjects) holds |
| **D-6** | MAJOR | *"the **only** `cssIdent` re-export"* (§Bounds · B-10 · **G-CSSIDENT's RED witness**) is false — and Act 4's own census lists the second one | `backward/index.ts:29` `export { cssIdent } from "./walk";` **and** `emit/index.ts:53` `export { cssIdent } from "./backward";`. The load-bearing half (0 in `index.ts`/`public.ts`/`load-engine.ts`) is TRUE |
| **D-7** | MAJOR | the DH-3 four-party disjointness sentence counts *"W4's, W7's or W8's **eleven** created names"* over an enumeration of **ten** | W4 2 (`KF-W4.md:75`/`:76`) + W7 4 (`KF-W7.md:63-66`) + W8 4 (`KF-W8.md:95`/`:96`/`:97`) = 10. Eleven is the four-party total including this wave's own create — the sense `KF-W7.md:79` uses. Same class as pass-3 D-4, inside this round's own cure |
| **D-8** | MINOR | the COUNT-ORACLE INVERSION SWEEP claims *"run over the twenty gates … verdict printed per gate"* and prints **nine** | no verdict for G-ROLE · G-REFUSE · G-REVERT · G-STAGGER-DOC · G-PRM-FLIP · G-DELAY · G-FROMSTRING · G-OPTSET · G-RAF · G-RENDERER · G-SCOPE |
| **D-9** | MINOR | the round-3 D-5 cure printed cross-spec line coordinates into a moving sibling; three of four are already stale | printed `KF-W6.md:152/:158/:356/:384`; measured **`:178`/`:184`/`:384`/`:412`**. Count (4) reproduces. The file retires exactly this receipt shape at §0 R-1.4 / RD-3 |
| **D-10** | MINOR | LAW A carries no consumer census for the KAD-14(e) dead-export delete; the *"used by no consumer"* claim is inherited bank prose | re-derived and **TRUE**: `useCodeHighlight` returns `{setHighlightingString, highlight, highlightAll}` `:147`; consumers destructure `{highlightAll}` (`KeyframesEditor.vue:176`) and `{setHighlightingString, highlightAll}` (`KeyframesAddDialog.vue:92`). Act 4 applies LAW A at symbol level; this row does not |
| **D-11** | MINOR | Act 1's view-transition row prints *"4 barrel"* for a barrel set of **5** | the uncounted hit is `test/orchestration/view-transition.test.ts:10`; test barrel hits are counted on the `backward`/`format`/`split-text` rows. Consumers (2) and the 7 sibling-module hits are exact — the 32-site migration surface stands |
| **D-12** | MINOR | SUBJECT-IDENTITY on the read-only `waapi/emission.ts` row: `steps()` is not in the file | `git grep -n 'steps' -- src/animation/waapi/emission.ts` → **0**; `:35-36` are `multiSegment`/`bakeCurve`. `densify.ts:45`/`:57` reproduce exactly |
| **D-13** | MINOR | a spec-voiced completeness claim survives the LAW B sweep: *"No gate in this file now claims a RED the frontier has already turned"* | negative-universal over the twenty gates in the file's own voice; **true** at this seat, but the shape LAW B item 1 bans and for which seven sentences were converted this round |
| **D-14** | MINOR | G-CSSIDENT points a `library`-project spec at a demo module | `kfEngine` is `demo/kf-engine.ts` via the `@kf-engine` alias (`vitest.config.ts:26`); `loadAnimationEngine()` and the exports map are the library-correct alternatives the gate also offers |

**routedTotal 43 · bookedCount 43 · escapedCount 0 · verdictLocal DEFECTIVE.**

---

## §7 · What this pass does not dispute

The by-record census is whole and honest in both directions, and retiring the nine-block partition did not move it by one id. The pass-3 **BLOCKER is closed**: G-XSS's observable half is re-cut verbatim, its escape mechanism named, and its GREEN reachable without deleting the hljs-escaped write — re-executed here at the bytes. G-PRM-FLIP is genuinely re-anchored and the two waves' anchors now agree. Every struck denominator reproduces with the probe beside it (rings MEASURE-AT-OPEN · god modules **8** · stutters **12** · allowlist **24** · `AnimationGroupInput` **`:27-29`**), and the strike reason refuted by `lane-library.md:154` is correctly restated on the measurement. The four LAW-A censuses are, where they authorize an act, correct at the frontier: the 32-site migration surface, the three same-basename non-consumers, the four dead allowlist names with zero live importers, the `./scheduler` twin with four consumers that must not be repointed, and the publish-or-relocate set correctly shrunk from five names to three. Every dissent, cure-lock, rider and fold-identity survives.

**The pattern, stated once**: three rounds have each cured an enumerated set perfectly and left the class open one level up — pass 2 the arm-D substrate, pass 3 the arm-0 oracle, pass 4 the *anchor-content* class and the *false-universal* class. The instrument that would have caught D-1/D-2/D-3 was **ruled this round** (R3-4.3) and pointed at a sibling wave; the instrument that would have caught D-5/D-6 is **this wave's own LAW-A census**, run correctly and then over-stated in the sentence beneath it.
