# KF-W8 — FRESH ADVERSARIAL SPEC CHECK (L-18/L-20, pass 2)

**Subject**: `/Users/mkbabb/Programming/value.js/docs/tranches/X/keyframes/waves/KF-W8.md` (328 L, 71 223 B) — repaired after a DEFECTIVE pass 1.
**Corpus authority**: the **58** `kf-*.md` records in `/Users/mkbabb/Programming/value.js/docs/tranches/V/megatranche/registry/adjudicated/`. Sole in-tree carry `carry/KF-W6-CARRY.md` (201 rows per RULINGS R-12) consulted for the W6 reciprocity axis only.
**Product tree** (read-only, zero writes): `/Users/mkbabb/Programming/keyframes.js` at `origin/master 81a56990736ced5b5edde0b84c527680ac7689b1` (re-resolved this session).
**Seat posture**: FRESH — the census, every gate measurement, and every load-bearing citation were **re-derived from source**, not inherited. `PASS-1/KF-W8-CHECK.md` and `PASS-1/RULINGS.md` were read only to spot-check that the four KF-W8 directives (R-13 · R-14 · R-16 · R-19c) were applied.
**Instruments**: `grep -n 'W8' kf-*.md` (**67** hit lines, all classified) · alternate-spelling sweep `grep -nEi 'W\.8|wave 8|wave-8|W-8'` · per-id `grep -ln` fold-transitivity sweep · **15 of 15** gate measurements re-executed with `git grep` / `git ls-tree` / `git show` at `81a56990` · anchor resolution of every cited sibling-spec, registry, census and precept line.

**Verdict**: **DEFECTIVE — but at a materially lower grade than pass 1.** The **ID-keyed census is CLEAN (32 routed / 32 booked / 0 escaped)**, **all 15 gate measurements reproduce EXACTLY**, E-3/STATUS holds, and **all four pass-1 rulings are faithfully applied**. Every pass-1 HIGH (D-1 barrel family · D-2 KF-CB-37 phantom · D-3 half-received W5 handoff) and MEDIUM (D-4 inbound edges · D-5 G7 guard · D-6 W2 order) is **CURED**. What survives is **anchor drift into sibling specs** (5 stale anchor families, one of them a phantom verbatim) and **three gate-authoring defects**, two of which would let a gate close wrongly. **Zero HIGH.**

---

## 1 · ID-KEYED CENSUS (re-derived, not inherited)

### 1.1 Method

`grep -n 'W8' kf-*.md` over all 58 records → **67 lines**, each classified as **routing-law boilerplate** (the KF.W0–W10 taxonomy paragraph: kf-AnimatedText:30 · kf-CopyButton:36 · kf-KeyframeCard:36 · kf-KeyframesEditor:36 · kf-KeyframesStringControls:17 · kf-KeyframesAddDialog:38 · kf-KeyframeTimeline:18 · kf-TimelineTrack:18 · kf-TimelineCaret:17 · kf-HeroAurora:32 · kf-AnimationVisualizer:35 · kf-EditorStartScreen:32 · kf-TypingDots:31 · kf-EasingScene:33 · kf-EasingSidebar:32 · kf-EasingTarget:33 · kf-TimingFunctionPanel:34), **closing-verdict summary** (kf-AnimationVisualizer:147 · kf-CSSCodeEditor:147 · kf-CopyButton:142 · kf-AnimatedText:137 · kf-KeyframeCard:157 · kf-KeyframeCardList:135 · kf-KeyframeTimeline:170 · kf-TimelineCaret:145 · kf-TimelineTrack:159 · kf-KeyboardShortcutsModal:127 · kf-KeyframesStringControls:174 · kf-PlaybackRibbon:9/:57 · kf-HeroAurora:27/:129), or a **row-terminal routing**.

Alternate spellings swept: `grep -nEi 'W\.8|wave 8|wave-8|W-8'` returns **only** Tailwind `w-8` class hits (kf-EditorShell :50/:61/:109 · kf-EditorHeader :44/:47/:50 · kf-SharePopover :44 · kf-DemoGlobalChrome :58). The lone non-`KF.W8` `W8` token is `kf-OrbitalDrag.md:82` — **`L.W8 ED-3`**, a *value.js* tranche-ledger cite inside OD-34 (NO-WAVE-OWNER), correctly not a KF.W8 routing. (The same `L.W8 ED-3` token appears at `kf-CSSCodeEditor.md:80` inside KF-CE-42, which *is* routed.)

Fold-transitivity closed: every W8-terminal identity was `grep -ln`'d across all 58 records to catch rows folded to a W8 identity without typing `W8`. Three candidate leaks opened, all resolved as non-escapes on independent grounds:

| candidate | record | this seat's resolution |
|---|---|---|
| `KF-CE-12` | `kf-TimelineHoverPreview.md:112` | superlative S+3/S-d — the C-12/KF-CE-12 breach family cited as the **negative comparator** for a clean boundary. No defect, no routing. |
| `KC-30` | `kf-KeyframeCard.md:75` | a **different id** — `KF-KC-30` (two dead glass `<Label>`s, `→ NO-WAVE-OWNER`), not kf-KeyframeCardList's `KC-30`. Not an escape. |
| `KF-CE-12` | `kf-KeyframeTimeline.md:48` · `kf-PlaybackRibbon.md:57` | C-7's R1 ingress folds to the megatranche R1 identity *"per the KF-CE-12 precedent"*; PlaybackRibbon's `KF-AV-8/-9/-18` fold line already routed. Both carried (spec :134, :144). |

### 1.2 Routed roster — 32 registry ids, id-for-id, each with its terminal locus

| # | id | source : line | terminal routing | booked? | where |
|---|---|---|---|--:|---|
| 1 | **KF-CE-12** | kf-CSSCodeEditor:46 | "extirpation → **KF.W8**" | ✅ | Rows 1 (unit c · G1+G2) |
| 2 | **KF-CE-42** | kf-CSSCodeEditor:80 | "Body folded into KF-CE-12's routing. → **KF.W8**" | ✅ | Rows 2 (identity retained for life) |
| 3 | **KF-AV-8** | kf-AnimationVisualizer:49 | "→ **KF.W8**" | ✅ | Rows 3 (unit c) |
| 4 | **KF-AV-9** | kf-AnimationVisualizer:50 | "→ **KF.W8** with KF-AV-8" | ✅ | Rows 5 — bounding note **verbatim** |
| 5 | **KF-AV-18** (W8 arm) | kf-AnimationVisualizer:62 | "prose sweep rides KF-AV-8's KF.W8 decision" | ✅ | Rows 6 — regex pin refused to KF.W4 |
| 6 | **KF-AV-24** | kf-AnimationVisualizer:68 | "→ **KF.W8**" | ✅ | Rows 27 · G14 (a11y rider carried) |
| 7 | **KF-AV-26** | kf-AnimationVisualizer:70 | "→ **KF.W8**" | ✅ | Rows 26 · unit h |
| 8 | **KF-AV-35** | kf-AnimationVisualizer:82 | "Folds into KF-AV-8/KF-CE-12 → KF.W8" | ✅ | Rows 4 (K-10 read-down carried) |
| 9 | **C-10** ⟨CSSPasteDialog⟩ | kf-CSSPasteDialog:75 | "**→ KF.W8** (test-tree isomorphism)" | ✅ | Rows 25 · unit g · G12 |
| 10 | **KF-HA-14** | kf-HeroAurora:56 | "Fold-by-reference → KF-AT-26(c) → **KF.W8**" | ✅ | Rows 14 |
| 11 | **KF-AT-26(c)** | kf-AnimatedText:71 · kf-HeroAurora:120 | "(c) **KF.W8** (colocation settle)" | ✅ | Rows 13 + IDENTITY GUARD (a)(b)(d)→W6, (e)→W10 |
| 12 | **L-19/C-14** | kf-KeyframeTimeline:77 | "**→ KF.W8**" | ✅ | Rows 18 |
| 13 | **C-12** ⟨KeyframeTimeline⟩ | kf-KeyframeTimeline:78 | "**→ KF.W8** (publish-or-relocate) + KF.W4" | ✅ | Rows 9 — *"not a consumer proof"* carried |
| 14 | **N-11** | kf-KeyframeTimeline:101 | "**→ KF.W8**" | ✅ | Rows 19 · unit f · G5 |
| 15 | **MISS-β2** | kf-TimelineCaret:46 | "**→ KF.W8** (publish-or-relocate, joint with banked C-12)" | ✅ | Rows 10 · unit d · G3 — **DISSENT 1 now carried verbatim** |
| 16 | **m-7** | kf-TimelineCaret:59 | "→ **KF.W8** (rename with the colocation move)" | ✅ | Rows 21 (C-15 closure honored) |
| 17 | **L-8** | kf-TimelineCaret:81 | "= banked N-11 (INFO, → KF.W8)" | ✅ | Rows 20 |
| 18 | **m-15** | kf-TimelineTrack:104 | "= banked N-11 (**→ KF.W8**)" | ✅ | Rows 20 (same line) |
| 19 | **R-10** ⟨KeyboardShortcutsModal⟩ | kf-KeyboardShortcutsModal:46/:127 | "**→ KF.W8**" | ✅ | Rows 24 · G10/G11 — **R-9 explicitly not absorbed** |
| 20 | **KAD-24** | kf-KeyframesAddDialog:77 | "**→ KF.W8**" | ✅ | Rows 15 · unit e · G4 — family identity |
| 21 | **KAD zero-coverage** | kf-KeyframesAddDialog:89 | "Fold identity → the twin's C-10. **→ KF.W8**" | ✅ | inside Rows 24 (one `vitest.config.ts` edit) |
| 22 | **L-17 colocation** | kf-KeyframesAddDialog:92 | "**→ KF.W8**" | ✅ | Rows 23 · unit b — the ratification anchor |
| 23 | **KF-KC-42** | kf-KeyframeCard:87 | "rides the R-7 consolidation (**KF.W8**)" | ✅ | Rows 30 + R-3 rules the W7/W8 boundary |
| 24 | **KF-KC-45** | kf-KeyframeCard:90 | "**→ KF.W8** (adopt-or-delete)" | ✅ | Rows 17 — folded into KAD-24 by R-2 |
| 25 | **KC-30** | kf-KeyframeCardList:66 | "→ **KF.W8** (colocation settle)" | ✅ | Rows 22 · unit f · pure-move |
| 26 | **KF-KE-15** | kf-KeyframesEditor:57 | "**FOLD to KF-CE-12 → KF.W8**" | ✅ | Rows 7 (census re-derived, D-19-3) |
| 27 | **C-8** ⟨KeyframesStringControls⟩ | kf-KeyframesStringControls:81 | "**KF.W5** (surface) + **KF.W8** (colocation)" | ✅ | Rows 8 + split lock |
| 28 | **C-13** ⟨PlaybackRibbon⟩ | kf-PlaybackRibbon:154 | "**KF.W8**: C-13 ≡ KF-AV-8/-9" | ✅ | Rows 11 — fold only |
| 29 | **M-4** ⟨RibbonBar⟩ | kf-RibbonBar:66/:132 | "**KF.W8** … RibbonBar-side spec input" | ✅ | Rows 28 · G15 + ChromeDock name-collision guard |
| 30 | **m-4** ⟨RibbonBar⟩ | kf-RibbonBar:59/:132 | "+ the m-4 fold's directory settle" | ✅ | Rows 29 — booked MEASURED-DIVERGENT |
| 31 | **KF-CB-37** | kf-CopyButton:85 | "→ **KF.W8** … same wave as the KF-CB-7 reshell decision" | ✅ | Rows 12 · **unit f · G6 ACTING** (R-13 applied) |
| 32 | **SPF-22** | kf-SpringPhysicsFacet:62 | "FOLD ≡ KAD-24 … → **KF.W8**" | ✅ | Rows 16 |

Mechanical carriage check — `grep -c` for each of the 32 ids over `KF-W8.md` returns **≥1 for all 32** (KF-AV-35 and L-19/C-14 and C-13 at exactly 1; KF-AV-8 at 8, KF-CB-37 at 7).

**Non-registry rows additionally carried** (declared as census-derived, not `kf-*` ids): `census §(a)9 + §2-flags · src/animation single-child` (unit e · G13) · `census §1 tests-row (LIB §5) · two orphaned *.measure.test.ts` (unit g · G9) · `census §3 FE §7 · colocation ratification` (unit b). Census anchors re-resolved and **EXACT**: `CENSUS-2026-08-03.md:64` (*"131 files, 1 051 cases, 0 colocated … two bench `*.measure.test.ts` picked up by NO vitest project (LIB §5)"*), `:107` (*"16 dir/name stutters + the `src/animation` single-child"*), `:142-144` (FE §7 colocation ratification), `:201` (§(a) item 9 = KF.W8).

### 1.3 Escapes

**ZERO.** Named by bytes: no id anywhere in the 2 942 KB / 58-record corpus carries a terminal disposition to KF.W8 and is absent from `KF-W8.md`. The four inbound sibling edges (R-19c) are separately booked (ii, iii, iv) or declined-with-owner (i) — none left neither.

### 1.4 Excluded-with-reason, audited

All 20 §Excluded rows name a real owner and a real reason. Five spot-verified against the bank at the bytes, **all EXACT**:

- **M-L3** = `kf-DemoGlobalChrome.md:70` ✅ verbatim (*"the `transport/` and `instrument/` barrels — including three `defineAsyncComponent` wrappers with a load-bearing-sounding hazard rationale (T.F5) — have zero consumers repo-wide"*), NO-WAVE-OWNER.
- **L-16-RESCOPED + RR-2 M4** = `kf-EditorShell.md:79` ✅ verbatim incl. *"the 'route through the barrel' remedy is REFUTED"*.
- **KF-APP-41** = `kf-App.md:91` ✅ verbatim, `NO-WAVE-OWNER (delete)`; the fold at `kf-EditorStartScreen.md:52` ✅.
- **C-22 + D-27 + RR-2 M7** = `kf-EditorShell.md:95` ✅ — the binding rider is carried **byte-exact**: *"it does NOT ship (rolldown shakes the unused barrel member) … while the orphan `--header-items-max-w: 500px` DOES survive into the shipped cascade."* (The token is live at `demo/styles/layout.css:15`, re-read.)
- **KF-KC-37 · SP-18 · KF-KE-53** — declined with owner KF.W6 ✅; the inbound declaration is real at `KF-W6.md:410` (see D-2 for the anchor).

---

## 2 · NO INVENTION / M-25 DEPTH

**HOLDS on the registry axis — every verbatim I re-read reproduces byte-exact.** Checked at the bytes, not at a sibling's transcription:

| carried material | source | result |
|---|---|---|
| MISS-β2 **DISSENT 1** (pass-1 D-8's cure) | `kf-TimelineCaret.md:136` | ✅ **verbatim** — *"the caret remains the chief INTERACTIVE integer feeder, and if a wave ever establishes an integers-only invariant at the UI, the serializer row (MISS-β2) inherits the full user-visible harm C-1 described."* |
| SpringTrace **C-3** banked text | `kf-SpringTrace.md:62` | ✅ **verbatim**, incl. *"(my dist grep → 0)"* and the *"MINOR → KF.W7 (export-surface wave; kf-KeyframeTimeline C-15 precedent)"* tail |
| C-3 **route dissent** | `KF-W7.md:101` | ✅ **verbatim** — *"route-by-mechanism: a value.js LIBRARY export-surface decision, natural home KF.W8 / the export settle; the record's KF.W7 routing preserved verbatim, nothing silently re-homed"* |
| `ingest/cssom.ts` colocation | `KF-W5.md:273(c)` | ✅ **verbatim** |
| B-16 shadow-name + both pre-stated dispositions | `KF-W5.md:273(d)` + `:194` | ✅ **verbatim**; `:194` is genuinely B-16's OP-4 row |
| KF-AV-9 bounding note | `kf-AnimationVisualizer.md:50` | ✅ carried verbatim |
| C-22's shipped-token rider | `kf-EditorShell.md:95` | ✅ carried verbatim |
| KF-AV-28 standing rider | `kf-AnimationVisualizer.md:35` / `kf-PlaybackRibbon.md:36` | ✅ carried (×2 — see D-10 for depth) |

**One phantom verbatim, and it is a sibling-spec quote, not a registry quote** — R-4's *"One rename programme, two waves: do not split mid-cure"* attributed to `KF-W5.md:215` exists **nowhere** in the current `KF-W5.md` (D-1 below). Its *substance* is alive at `KF-W5.md:219` under the R-14 discharge, so the ruling is sound; the quotation is not.

Anti-rename holds throughout: KF-CE-42, KF-KC-45, C-13, SPF-22, m-15/L-8, KF-AT-26's legs and the kf-RibbonBar-vs-kf-ChromeDock `M-4` collision guard all keep their banked identities and are explicitly marked *"identity retained for life, carries no independent cure."*

---

## 3 · GATES — L-19 (born-RED, real witness, phantom scripts convict)

### 3.1 All fifteen measurements re-executed at `origin/master 81a56990`. **15 of 15 reproduce EXACTLY.**

| gate | spec claims | this seat's re-run | |
|---|---|---|---|
| **G1** | 7 statements / 6 files / 4 library modules, enumerated | `git grep -n 'from "@src/' origin/master -- demo` → **7 lines / 6 files**, byte-identical anchors (`useKeyframeOps:1` · `parseAnimationCSS:7` · `timelineEngine:1` & `:17` · `AnimationVisualizer.vue:45` · `helpers.ts:9` · `keyframeSelector.ts:5`) | ✅ |
| **G2** | zero hits for all eight symbols; map = 2 entries; `index.ts` has 0 `export *` | grep over `index.ts`/`public.ts`/`load-engine.ts` → **exit 1, no hits**; `export *` count → **0**; `package.json` exports = `.` + `./engine` | ✅ |
| **G3** | 3 bodies at `format/format.ts:20`, `css-text.ts:58`, `keyframeSelector.ts:7` | exactly 3, exactly those anchors; `emit/index.ts:53` exports `cssIdent` (not a serializer); `compile/selector.ts:42` + `compile/index.ts:24` re-export confirmed | ✅ |
| **G4** | **five** `index.ts`, **four** `export *` at `:24-:27`, `transport/index.ts` 14 L / 3 async + 1 type / 0 importers | `git ls-tree -r … \| grep 'index\.ts$'` → **5**; umbrella `:24-27` = transport/keyframes/timeline/shell; transport barrel = **14 lines**, exactly 3 `defineAsyncComponent` + `export type { TransportChannel }`; `git grep -c 'instrument/transport"' … demo test` → **0**; `'instrument/timeline"'`, `'instrument/keyframes"'`, `from "@components/instrument"` → **0 each** | ✅ **D-19-12 repair holds** |
| **G5** | exactly **3** up-imports (`:31`, `:116`, `:229`) | 3 hits, exactly those files/lines — **the third site (`ChannelControls.vue:229 → ../KfPillTabs.vue`) is REAL** | ✅ |
| **G6** | 1 loose `.vue` = `CopyButton.vue` | `git ls-tree --name-only … demo/components/` → `CopyButton.vue`, `instrument`, `playback` | ✅ |
| **G7** | 4 of 8 SFCs exported; `EditorHeader` 0 component importers | shell dir holds exactly **8** `.vue`; `index.ts` exports exactly EditorShell/EditorHeader/EditorStartScreen/SharePopover; `EditorHeader` appears only at `index.ts:2` + `layout.css:15` (a CSS comment) | ✅ |
| **G8** | `App.vue:144` `editor-shell/` · AV `:70-78` ≥4 value.js attributions · `resize-tracks.test.ts` ×6 · **four** barrel comments | `App.vue:144` = *"colocated in editor-shell/ beside"* (no such dir; `shell/` is real) ✅ · AV `:70-78` = 4 value.js mentions with `:45` importing from keyframes' own `src/` ✅ · resize-tracks value.js cites at **exactly** `:4/:5/:13/:35/:105/:127` ✅ · chunk-rationale comments at `instrument/index.ts:15`, `keyframes/index.ts:4`, `timeline/index.ts:3`, `transport/index.ts:2-3`; `shell/index.ts` carries none ✅ | ✅ |
| **G9** | 2 orphans; library/demo/benchmark globs as stated | `bench/` holds exactly `d3-changed-keys.measure.test.ts` + `sync-step.measure.test.ts`; `vitest.config.ts:43-44` library include/exclude, `:52` demo include, `:35` `bench/*.bench.ts` — nothing matches `bench/*.measure.test.ts` | ✅ |
| **G10** | no `@vitejs/plugin-vue` in `vitest.config.ts`; 9 demo-lane files; plugin **is** a devDep; `@vue/test-utils` absent | config has **no `plugins` key at all**; `test/demo/instrument/` = **9** `.test.ts`; `package.json:81` `@vitejs/plugin-vue ^6.0.7`; `@vue/test-utils` **absent**; vitest `^4.1.8` `:110`; vue `^3.5.35` `:111` | ✅ |
| **G11** | `groupShortcuts` 0 hits; `groupedShortcuts` locked at KSM `:57`; 0 test hits | `git grep -l groupShortcuts … demo test` → exit 1; `KeyboardShortcutsModal.vue:57` = `const groupedShortcuts = computed(…)`; `grep -l "KeyboardShortcuts\|groupedShortcuts" … test` → exit 1 | ✅ |
| **G12** | 0 test hits for the dialog pair | `git grep -l "CSSPasteDialog\|KeyframesAddDialog" origin/master -- test` → exit 1 | ✅ |
| **G13** | `src/` → one entry; **8** `basename == dirname` stutters | `git ls-tree --name-only origin/master -- src/` → `src/animation`; the awk predicate → exactly the **8** named (`backward/backward` · `format/format` · `group/group` · `sequence` · `split-text` · `timeline` · `view-transition` · `solver/solver`) | ✅ |
| **G14** | 11 lines / 10 files; `useDragScrub` zero | `git grep -c 'getBoundingClientRect()' … demo` → 10 files, `AnimationVisualizer.vue` ×2 = **11 lines**; host list **identical** incl. `useMenubarMeasure.ts` (DO-NOT-TOUCH) | ✅ |
| **G15** | RibbonBar 151 L; `ChannelControls:186` teleports with `defer`; `:189` `:key` remount; `:253` hand-rolled async | `wc -l` → **151**; `:186` `<Teleport … defer>`; `:189` `:key="storedControls.selectedControl"`; `:253` `defineAsyncComponent(() => import("../../timeline/KeyframeTimeline.vue"))` | ✅ |

**Every product path the spec names EXISTS.** Additionally re-verified: `demo/utils/keyframeSelector.ts` present at `origin/master`, **absent at local `8281638c`**, merge-base `a59d3a22` (MISS-α5 ✅ exact); `demo/utils/helpers.ts:1-8` docblock *"the remaining deep-import retirement belongs to the folded-forward demo settlement wave"* ✅; `resolve/browser.ts:1-3` value.js subpaths + `:23` module-eval resize listener ✅; `SpringPhysicsFacet.vue:135` direct import ✅; `font-roles.json:32` *"T.H gated-on-publish excision"* ✅; the four CopyButton importers at `KeyframeCard.vue:59` · `KeyframesEditor.vue:116` · `EasingTarget.vue:143` · `StartingStyleTarget.vue:87` ✅ exactly four; AnimatedText's **one** importer `EditorStartScreen.vue:62`, `TypingDots.vue:4` a comment ✅ (D-19-10 correct); `controls-pane/` = `ControlsPaneWrapper.{vue,css}` + `RibbonBar.vue` ✅ (D-19-7's MEASURED-DIVERGENT booking correct); `"lint": "depcruise src"` at `package.json:44` ✅; `docs/precepts/instructions/README.md:127-130` (tests outside `src/`, *"verify glob behaviour before choosing the shape"*) and `:132-135` (*"Doc drift is uncommitted work"*) ✅ both exact.

**No proof-script contrivance.** No gate here invents a script; the depcruise config and the source-regex pin are explicitly refused to KF.W4, and the two uncommanded census figures (`16`, `1 051`) are refused by name (L-9).

### 3.2 The three gate-authoring defects that survive

D-3 (three gates carry no command, seven no falsifier, against a blanket claim that all fifteen do), D-4 (G1 clause 2 unmeasured and, as worded, unsatisfiable), D-5 (G4's command still greps the umbrella specifier its own falsifier forbids). Registered in §6.

---

## 4 · E-3 + STATUS

**CLEAN.** `**Status**: planned` (`:11`). Verb table: `IMPLEMENTED NO — status planned; awaits begin-word`, **`VERIFIED | NO`** (`:17-18`). `grep -n 'VERIFIED' KF-W8.md` → **exactly one line, `:18`, and it is the NO cell**. Provenance closes: *"This is a specification: status fields stay `planned`; nothing opens product source; execution awaits the owner's begin-word."* (`:3`). Every lowercase "verified" is a read-only fold-seat measurement receipt.

E-3 honored by name **twice**: KC-1's dated record left unrewritten with only its citation corrected here (`:143`, `:316`), and the `sideEffects:false` framing killed by K-10's read-down but carried so no spec re-argues it (`:134`, `:317`). No registry record was edited; this check wrote nothing outside `PASS-2/`.

---

## 5 · POSTURE AXES

| axis | finding |
|---|---|
| **KF.W4 = sequencing head, honored** | **HOLDS, exemplary and now reciprocated.** `Opens after: … KF.W4` (`:8`); *"**KF.W8 authors no type gate**"* (`:284`); G10's *"KF.W4's vue-tsc edit to the same files lands first"* (`:227`); §Bounds marks `package.json` and `vitest.config.ts` **modify-carve after KF.W4** (`:66-67`). Verified at the other end: `KF-W4.md:223` *"This wave is the declared sequencing head of X·KF"*; `KF-W4.md:247` the SPLIT ROW *"gate arm ours, cure theirs"*; `:264` *"The deep-import cure sites and `convertPixelsToCh` — KF.W8 (publish-or-relocate). We detect; they decide."*; `:81` Do-NOT-touch *"the deep-import **cure** sites (KF.W8 owns publish-or-relocate)"*. |
| **KF.W3 gated-unscheduled** | **HOLDS — pass-1 D-11 CURED.** `:288` states it explicitly and names KF.W2 as the scheduled receiver (F0, per RULINGS R-1b). Verified: `KF-W3.md:1` = `# KF.W3 — Parser Consumption (GATED, never scheduled)`. |
| **KF-AV-28 rider where governed rows are cured** | **PARTIAL.** Carried verbatim as a STANDING RIDER on KF-AV-26 (`:159`) and KF-AV-24 (`:160`), matching `kf-AnimationVisualizer.md:35` / `kf-PlaybackRibbon.md:36`. **Not** carried on KF-AV-8/-9/-18/-35 or C-13 — rows banked on the same S-9-evaluated surfaces, where unit c spends bytes (`AnimationVisualizer.vue:45` + the `:70-78` prose). RULINGS R-10 expressly exempted W8 (*"already carries it (×1) — no change"*), so this is a fresh-seat depth finding, not a ruling breach → **D-10**. |
| **KF.W5 reciprocity** | **HOLDS — pass-1 D-3 CURED at both ends.** `KF-W5.md:273` now reads *"the 16 measured stutters are this wave's, and the programme executes here WHOLE … the do-not-split lock is thereby SATISFIED BY DECLINE-WHOLE … G-STRUCT's 16 is the owning denominator against KF.W8 G13's narrower 8"*, and D-6 at `:219` carries the R-14 addendum verbatim. KF-W8's R-4 (`:266-269`) declines **both** instances in one ruling and terminalizes as a RECORD at W10. |
| **KF.W6 reciprocity** | **HOLDS — pass-1 D-2 CURED.** `KF-W6.md:410` now carries *"**MOVE-vs-RESHELL GUARD (R-13): `CopyButton.vue`'s MOVE is KF.W8's (KF-CB-37); this wave's KF-CB-7 reshell MODIFIES IN PLACE and must not relocate the file.**"* — the reciprocity R-1 once falsely asserted now genuinely exists, in the right direction. CARRY bounds re-verified: `KF-W6-CARRY.md:317` = `| demo/components/CopyButton.vue | 113 | modify | S-7 reshell |`; `:173` = the S-7 · KF-CB-7 reshell row with the S-2 MUST-CARRY. |

### 5.1 Pass-1 rulings addressed to KF-W8 — spot-checked, all four APPLIED

- **R-13** (own the KF-CB-37 move) — ✅ R-1 rewritten (`:258-263`), the phantom **named as such**, `git mv` + four repoints ruled, **G6 re-cut as ACTING** (`:204-207`), destination law stated (`demo/components/CopyButton/CopyButton.vue`, owner-named, generic buckets refused twice over), sibling guard declared. The completed-act voice is gone: `:124` now reads *"the move executes here; R-1 as re-ruled at RULINGS R-13"* and explicitly strikes *"executed in W6"*.
- **R-14** (both structural instances; record → W10; G13 subordinated) — ✅ R-4 (`:266-269`) names both, declines together, terminates as **one FOLD-FORWARD record** with the verb `DECLINED-FOR-X·KF, carried forward`, and corrects *"Receiving surface: KF.W10"*. G13 (`:243`) carries the declared denominator subordination verbatim.
- **R-16** (W8 precedes on the serializer) — ✅ `:289` replaces the mutual conditional with the order; reciprocated at `KF-W2.md:40` (OP-6 *"ORDERED: KF.W8 precedes (RULINGS R-16)"*), `:228`, `:284`, `:320`.
- **R-19c** (four inbound edges booked-or-declined by id) — ✅ table at `:274-279`: (i) KF-KC-37 · SP-18 · KF-KE-53 **DECLINED, owner KF.W6** (§Excluded row 1, with the bank's own scoping carried verbatim); (ii) SpringTrace C-3 **BOOKED conditional**; (iii) `ingest/cssom.ts` **BOOKED**; (iv) B-16 **BOOKED conditional**. None left neither.
- **R-12** (phantom CARRY citations struck) — ✅ though not in W8's directive list, the spec strikes the *"34 CARRY rows"* figure by name at `:3`, `:130` and `:302`, restates provenance over the 58-record registry, and re-keys the denominator. Independently confirmed: the only in-tree carry is `KF-W6-CARRY.md`.

---

## 6 · DEFECT REGISTER (12; 0 HIGH · 5 MEDIUM · 7 LOW)

### D-1 · **MEDIUM** — R-4's load-bearing verbatim is a phantom: the quoted lock exists nowhere in `KF-W5.md`, and its anchor points at an unrelated row

`KF-W8.md:266` presents, in quotation marks and as the reason R-4 must cover both instances: *"One rename programme, two waves: do not split mid-cure"* (`KF-W5.md:215`, repeated `:269(a)`).

Receipts, re-run this session:
```
grep -c 'do not split mid-cure' KF-W5.md   → 0
grep -c 'One rename programme'  KF-W5.md   → 0
sed -n '215p' KF-W5.md → "| **D-2 · lane-library L-2** (no-cycle baseline) ⟨§3.5; SCH-7⟩ | …"
grep -n 'D-6 · the 16 dir/name stutters' KF-W5.md → 219
```
`KF-W5.md:215` is the depcruise no-cycle baseline row, not D-6. The real D-6 is at **:219** and now reads *"BOUNDARY WITH KF.W8, DECLARED — and now DISCHARGED (RULINGS R-14)"*, carrying the decline-whole addendum verbatim. The string KF-W8 quotes was true at pass-1 time and was rewritten by KF-W5's own round-1 repair. **The ruling's substance is sound; the quotation and its anchor are not** — and M-25's carry obligation is discharged by verbatim, so a verbatim that no longer resolves is a live defect. The same phantom `:215` is cited **four times**: `:62`, `:94`, `:243`, `:266`.

### D-2 · **MEDIUM** — five stale sibling-spec anchor families, and the file cites two different lines for the same target

| cited in KF-W8 | resolves today to | the real anchor |
|---|---|---|
| `KF-W5.md:269` — at `:63`, `:278`, `:279`, `:285` (the four legs) | the **→ KF.W0 · Substrate Settle** bullet | **`KF-W5.md:273`** — and `:167`/`:168`/`:169`/`:243` already use **:273** |
| `KF-W5.md:240` — at `:94`, `:243` ("G-STRUCT leg 2") | **G-RENDERER** (`adopt-compiled-renderer.test.ts`) | **`KF-W5.md:244`** (G-STRUCT) |
| `KF-W6.md:378` — at `:259`, `:276`, `:286` (W6's → KF.W8 edge) | **G-W6-11** canvas/theme colour discipline | **`KF-W6.md:410`** — and `:306` already uses **:410** |
| `KF-W7.md:313` / `:335` — at `:287` | not the cited material | **`KF-W7.md:317`** (KF.W5 feeds/defers) / **`:340`** (implementation edge declared) |
| `KF-W2.md:221` — at `:289` ("exactly `KF-W2.md:221`'s own alternative") | not the alternative | **`KF-W2.md:228`** (the Tier-D lock) / **`:40`** (OP-6) |

Two of these are **internal contradictions**, not mere drift: the file cites `KF-W5.md:269` *and* `:273` for one bullet, and `KF-W6.md:378` *and* `:410` for one edge — proving the seat had the repaired siblings in hand and updated only some sites. The wave whose G8 is *"zero directory or module citations … that resolve to nothing"* and whose D-19 exists to correct anchors cannot ship five anchor families that resolve elsewhere.

### D-3 · **MEDIUM** — the L-18 rider asserts a property of its own gate suite that 3 (command) and 7 (falsifier) gates do not have

`:328`: *"**L-19** governs every gate above — each names a literal command and a falsifier."*

Re-derived by scanning the §Gates block:
- **No literal command anywhere in the gate body**: **G7** (`:209-212`), **G8** (`:214-216`), **G15** (`:250-252`). Each states measured facts (4-of-8 exports; the four comment sites; 151 lines) but names no reproducible invocation — a later seat cannot re-run them as written.
- **No falsifier**: **G7, G8, G10, G11, G12, G14, G15** (G14 substitutes a "RIDER (binding)"; G15 substitutes "cannot go green independently").

The measurements themselves all reproduce (§3.1) — so this is a false self-description, not a fabricated gate. But L-19 is the wave's own gate-authoring law and the file claims compliance it does not have.

### D-4 · **MEDIUM** — G1's clause 2 has no command and, as worded, can never go green

G1 (`:175`): *"zero `@src/` specifiers in `demo/`, **and no demo module transitively imports a value.js-bearing library module**."* The only command measures clause 1; clause 2 is supported by one file-specific sentence (*"`demo/utils/helpers.ts` no longer reaches `src/animation/resolve/browser`"*).

Read literally, clause 2 is unsatisfiable at any tree state:
```
git grep -l '@mkbabb/value' origin/master -- src        → 47 files
git grep -n '@mkbabb/value' … src/animation/load-engine.ts → :65 import type { Stylesheet } from "@mkbabb/value.js/css"
git grep -n '@mkbabb/keyframes.js' origin/master -- demo   → demo/app/transition/useSceneTransition.ts:2, … (lawful, published-surface)
```
The published surface is itself value.js-bearing, so **every** lawful demo consumer transitively imports a value.js-bearing library module. The falsifier reveals the intent (*"a demo re-export shim that forwards the same module passes clause 1 and fails clause 2"*) — i.e. *library-private* module, reached by any path — but the gate text does not say that and no command tests it. A gate that stays RED for a reason its cure cannot reach is the contrivance L-19 forbids.

### D-5 · **MEDIUM** — G4's stated command still greps the umbrella specifier that its own D-1 falsifier forbids

Command (`:193`): `git grep -l 'from "@components/instrument"' origin/master -- demo test` **non-empty or the barrels absent**.
Second falsifier (`:195`): *"a `git grep -l 'from "@components/instrument"'` that sees only the umbrella specifier goes green with `transport/index.ts` and its T.F5 rationale prose alive — **the gate therefore enumerates all four barrel paths by path, never by umbrella specifier**."*

The gate line was annotated but **not re-cut**: the operative command is still the umbrella grep. This seat re-confirmed the hole is live — `git grep -c 'instrument/transport"' origin/master -- demo test` → **0**, and every live `instrument/transport` hit is a deeper subpath, so the umbrella grep is structurally blind to the fourth barrel the enumeration repair added. The D-19-12 repair is correct in prose and incomplete in the executable clause.

### D-6 · **LOW** — the KAD-24 minting anchor (pass-1 D-10's cure) resolves to a blank line

`:148` attributes the cure-shape lock to *"the label `kf-SquareScene law` … minted at **`KF-W6.md:128`**"*. `sed -n '128p' KF-W6.md` → **empty**. The mint is at **`KF-W6.md:136`**: *"**Family law (KF-CE-41, verbatim): the comment is part of the defect surface — the cure commit carries the comment.** Generalisation: **comment-stated invariants are test obligations** (kf-SquareScene law)."* (also restated at `KF-W6.md:377` in G-W6-10). The cure's content is right; the anchor D-10 asked for is off by 8.

### D-7 · **LOW** — R-1's grep receipt no longer reproduces

`:259`: *"`grep -n 'KF-CB-37'` → **0 hits in `KF-W6.md`**, **0 hits in the 201-row `KF-W6-CARRY.md`**"*. Re-run this session: `grep -c 'KF-CB-37' KF-W6.md` → **1** (`:410`, the R-13 sibling guard); CARRY → **0** ✅. The single hit *confirms* W8 owns the move, so the conclusion strengthens — but the file states a present-tense measurement that is now false, in the one paragraph whose whole point is that an asserted measurement was never checked.

### D-8 · **LOW** — the AUDITED evidence row's "21 adjudicated records" is unreceipted and reconciles with nothing

`:15`: *"21 adjudicated `registry/adjudicated/kf-*.md` records + `formation/keyframes/CENSUS-2026-08-03.md`"*. This seat's enumeration: the 32 routed ids come from **18** records; the spec cites **10** distinct `kf-*.md` paths by name (`grep -oE 'kf-[A-Za-z]+\.md' | sort -u`); the union of routed + guard + excluded source records is **24**. No command is recorded for 21 — the same L-9 defect the file refuses twice by name elsewhere (`census 16`, `1 051`).

### D-9 · **LOW** — `useToolbarKeyboard.ts:41-49` is cited as "the docblock"; the docblock naming CopyButton is at `:25`

R-1 (`:261`) orders *"the `useToolbarKeyboard.ts:41-49` docblock"* corrected in the move commit; `:145` cites the same span for S-2's roving-tabindex binding. Re-read at `origin/master`: `:41-49` is the `items()` **membership predicate** (`container.querySelectorAll<HTMLElement>("button")` filtered on `disabled`/`aria-hidden`) — which does earn S-2 its mechanism, so the *binding* citation at `:145` is sound. The **docblock** naming CopyButton ends at `:28`, with the name at **`:25`** (*"KeyframesAddDialog's DialogTrigger, CopyButton, the Apply-CSS button"*). The prose-correction target R-1 names is therefore mis-anchored.

### D-10 · **LOW** — KF-AV-28's standing rider is carried on 2 of the ~7 rows it governs, and the unrided rows are the ones that spend bytes

Carried on KF-AV-26 (`:159`) and KF-AV-24 (`:160`). **Absent** from KF-AV-8, KF-AV-9, KF-AV-18, KF-AV-35 and C-13 — all banked on `AnimationVisualizer.vue` / `PlaybackRibbon.vue`, the surfaces `kf-AnimationVisualizer.md:35` and `kf-PlaybackRibbon.md:36` place under KF.W7's S-9 evaluation. Unit c edits `AnimationVisualizer.vue:45` and its `:70-78` comment block; a SWAP verdict for that surface makes both edits throwaways, and the rider's own discipline (*"the seat STATES which governed rows a SWAP verdict has discharged BEFORE spending any cure"*) is not attached to them. **RULINGS R-10 exempted KF-W8** (*"already carries it (×1) — no change"*), so this is recorded as a fresh-seat depth finding, not a ruling breach.

### D-11 · **LOW** — G13 requires the ruling to name a trigger; R-4 names none

G13 (`:240`): *"A committed ruling names the chain, its disposition, and **its trigger**"*; falsifier (`:244`): *"a ruling with no trigger."* R-4 (`:266-269`) supplies the chain, the disposition (DECLINE both), reasons, and a terminus (one FOLD-FORWARD record at KF.W10) — but no re-open condition beyond the duration phrase *"for the duration of X·KF"*. A seat executing G13 must either read the duration as the trigger or find its own wave's deliverable failing its own falsifier.

### D-12 · **LOW** — G2's "narrowness clause" is asserted only inside the falsifier

G2's gate statement (`:181`) is *"every demo-consumed library symbol resolves through the published surface"* and its command returns a hit per consumed symbol. The falsifier (`:184`) then convicts *"a symbol published by widening a barrel to `export *` … fails the **narrowness clause**"* — a clause the gate never states. The measurement (`export *` count = 0 in `src/animation/index.ts`) is real and reproduces; the predicate is under-specified.

---

## 7 · WHAT THE REPAIR GOT RIGHT (L-18 runs both ways)

1. **Every pass-1 HIGH and MEDIUM is genuinely cured, at the bytes, at both ends.** The barrel family is FIVE sites with the two NO-WAVE-OWNER ids cited-not-annexed; KF-CB-37's act is owned here with the phantom named as this spec's own error and G6 re-cut as ACTING; the KF.W5 handoff is received whole and reciprocated at `KF-W5.md:273`; all four R-19c edges are booked-or-declined; G7 carries the three-fold ownership guard with C-22's shipped-token rider verbatim; the W2↔W8 circle is ordered at both ends.
2. **15 of 15 gate measurements reproduce EXACTLY** — including every fold-seat repair (D-19-9's third up-import, D-19-10's single AnimatedText importer, D-19-11's ten-file `getBoundingClientRect` host list, D-19-12's fifth barrel site, D-19-7's non-reproducing `m-4`).
3. **Every registry verbatim reproduces byte-exact**, including the two the pass-1 seat convicted as missing (MISS-β2's DISSENT 1; the SquareScene-law provenance chain, whose *content* is now correct).
4. **L-9 is honored three times** — `16`, `1 051`, and the received arm's up-import `2` are all refused or corrected with a recorded command.
5. **Fixture paths are now literal** (pass-1 D-7 cured): `sfc-load.probe.test.ts`, `groupShortcuts.test.ts`, `CSSPasteDialog.test.ts`, `KeyframesAddDialog.test.ts`, and the census/close docs are spelled in full.
6. **The declines are declines, not drops** — KF-KC-37/SP-18/KF-KE-53 carry the bank's own honest scoping verbatim and the reason KF.W8-I cannot receive them (*"rules where a module lives, never which specifier a consumer types"*).

---

## 8 · TALLY

| metric | value |
|---|--:|
| corpus records swept | **58** |
| `W8` hit lines classified | **67** |
| alternate-spelling sweep | clean (only Tailwind `w-8` + the `L.W8 ED-3` value.js ledger token) |
| registry rows with terminal disposition → KF.W8 | **32** |
| booked by the spec | **32** |
| **escaped** | **0** |
| non-registry census rows carried | 3 |
| inbound edges booked / declined-with-owner | 3 / 1 |
| gates re-executed by this seat | **15 of 15 — all EXACT** |
| defects | **12** (0 HIGH · 5 MEDIUM · 7 LOW) |
| **verdictLocal** | **DEFECTIVE** |

The census is airtight and the measurements are the strongest in the packet. The file fails on **provenance hygiene into sibling specs** (one phantom verbatim, five stale anchor families, two of them self-contradicting) and on **three gate-authoring defects** — a clause that can never go green (G1.2), a command that still contains the hole its own falsifier names (G4), and a blanket L-19 compliance claim that three commandless and seven falsifier-less gates do not support. All twelve are one-line repairs; none reopens the census, the rulings, or any measurement.

— end of pass-2 check. Written read-only against the 58-record registry, the ten sibling wave specs, `KF-W6-CARRY.md`, `docs/precepts/`, and `keyframes.js origin/master 81a56990`; **no product source opened for write; sole write = this file.**
