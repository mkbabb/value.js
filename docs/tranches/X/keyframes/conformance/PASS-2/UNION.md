# X·KF CONFORMANCE PASS 2 — UNION ADJUDICATION (post-repair-1)

**Seat**: union adjudication (cross-wave axes the per-wave seats cannot see) · 2026-08-28
**Inputs**: the 11 PASS-2 per-wave check files in this directory (all read whole) · the 11 repaired wave specs at `docs/tranches/X/keyframes/waves/` · `PASS-1/UNION.md` + `PASS-1/RULINGS.md` (the baseline and the 53-directive repair contract) · `carry/KF-W6-CARRY.md` · seat receipts for KF-W0..KF-W10.
**Instruments**: the checks' own receipts, plus union-level greps re-executed this seat — the nine hard-escape absences (D·D-7 0/0 at spec+CARRY · ACG D-1's two hits both taxonomy/exclusion lines · KF-HA-10/MISSED-A 0 at W9 · `.tap-floor`+`/command` 0 at W10), the five sibling landings (KF-SST-36 ×2 at W3 · L-15/L-11/KAD-10 booked at W7), the head law at `KF-W4.md:223` vs the dead `:193`, `build:gh-pages` live at `KF-W7.md:323`, O-20 as the measured INBOX maximum, `§0c` 0 hits in COHESION.md, KF-ET-28's sole cross-hit (a passing W4 fixture-rationale cite, not a booking). Sole write = this file.

**UNION VERDICT: DEFECTIVE — 10 of 11 wave specs DEFECTIVE; KF-W3 is the program's FIRST CLEAN local verdict.**
**108 deduped defects** (2 BLOCKER · 2 CRITICAL · 2 HIGH · 40 MAJOR · 10 MEDIUM · 36 MINOR · 10 LOW · 6 INFO) · **9 hard program partition escapes** (vs pass 1's 32) · **3 double-homings** (vs pass 1's 6 — all six pass-1 instances CURED; these three are fresh) · **1 ruling partially unapplied** (R-8.4 at KF-W4) · KF.W4 head now holds in all 11 (pass 1's one hard breach, KF-W9, is CURED by R-9a) · KF.W3 stays unscheduled everywhere and its own spec is CLEAN.

**The pass-2 shape in one sentence**: the repair round executed the rulings faithfully — 52 of 53 §END directives verified applied, several byte-verbatim — and the census disease collapsed (carriage 91.0% → 97.2%, hard escapes 32 → 9, double-homings 6 → 0 old + 3 new); what the round did **not** do is re-measure, and the surviving defect mass is almost entirely of two new mechanical classes: **repaired-against-the-rulings-never-against-the-tree** (phantom §Bounds paths, a disqualified-substrate arm, two born-GREEN gates, an id spent nine minutes before the repair write) and **intra-round anchor rot** (11 specs repaired serially, no closing re-anchor pass, ~14 dead cross-spec receipt families).

---

## §0 · Per-wave census roll-up (from the 11 checks)

| wave | routed | booked | escaped | local verdict | defect register |
|---|--:|--:|--:|---|---|
| KF-W0 | 112 | 105 | 7 | DEFECTIVE | 4 MAJ · 4 MIN |
| KF-W1 | 6 | 6 | 0 | DEFECTIVE | **1 BLOCKER** · 4 MAJ · 3 MIN · 1 INFO |
| KF-W2 | 62 | 47 | 15 | DEFECTIVE | 6 MAJ · 4 MIN |
| KF-W3 | 24 | 24 | 0 | **CLEAN** | 2 MAJ · 2 MIN · 1 INFO (none census-fatal) |
| KF-W4 | 104 | 104 | 0 | DEFECTIVE | **2 CRITICAL** · 7 MAJ · 6 MIN |
| KF-W5 | 43 | 43 | 0 | DEFECTIVE | 6 MAJ · 6 MIN · 1 INFO |
| KF-W6 | 392 | 391 | 1 | DEFECTIVE | 3 MAJ · 4 MIN · 1 INFO |
| KF-W7 | 150 | 149 | 1 | DEFECTIVE | 2 MAJ · 5 MIN · 2 INFO |
| KF-W8 | 32 | 32 | 0 | DEFECTIVE | 5 MED · 7 LOW (0 HIGH) |
| KF-W9 | 147 | 144 | 3 | DEFECTIVE | 2 HIGH · 6 MED · 4 LOW |
| KF-W10 | 9 | 6 | 3 | DEFECTIVE | **1 BLOCKER** · 4 MAJ · 3 MIN |
| **Σ** | **1081** | **1051** | **30** | | **109 raw filings** |

**Carriage 97.2%** (pass 1: 989/1087 = 91.0%). Denominator moves are re-derivations, not drift, and each check explains its own: W0 95→112 (the fold-rider families swept whole), W2 47→62 (the 58-record sweep vs pass 1's 37-record sample — the delta IS the defect, W2 D-8), W9 167→147 (record-scoped pair granularity, provenance stated), W10 31→9 (registry+carry unit vs pass 1's five-tier obligation ledger; the 27 non-registry tier-obligations are carried in-spec and were not re-counted as census units). W1/W3/W5/W8 reproduce exactly; W6 390→392 (pass 1's own miss at kf-SequenceScrubber); W7 148→150 (ACG D-1 + THP MISSED-3 admitted under the spec's own counting rules).

---

## §1 · PARTITION — rebuilt fresh from the pass-2 censuses

**Law** (same as PASS-1 §1): a row routed to any wave must land in ≥1 wave as a row, a spelled-out fold-identity, or an exclusion-with-named-owner. Escaped-by-ALL-waves = program defect.

### 1.1 Decomposition of the 30 per-wave escapes

| class | count | content |
|---|--:|---|
| **HARD PROGRAM ESCAPES** (live rows/obligations carried by NO wave) | **9** | §1.2 |
| landed at a sibling (partition holds; local spec still short) | 5 | §1.3 |
| register escapes (banked verified-negatives / posture exemplars; census-of-record incomplete, no live defect content lost) | 9 | §1.4 |
| id-keyed trail escapes (content in KF-W0 C-1's fold mechanism; ids absent by bytes from the index minted to hold them) | 7 | §1.5 |

### 1.2 The NINE hard program escapes — id for id (each is a program defect; all nine re-verified at the bytes by this seat)

1. **kf-SequenceScrubber D·D-7** (W6-routed by bolded terminal manifest `:133`) — the dark-substrate phosphor `text-shadow` at ~4.6:1, MAJOR at the bank. `grep 'D·D-7|phosphor'` → **0 in KF-W6.md AND the 201-row CARRY**. Pass 1 recorded this record as routing zero W6 ids, so no ruling covered it — **the repair closed the escape class by enumeration and the class was not closed by enumeration.** Whoever books it inherits the KF-AV-28 rider (governed surface).
2. **kf-AnimationControlsGroup D-1** (W7-routed by the spec's own taxonomy rule `:325`) — the **ADJUDICATED BLOCKER** portal-sink row, *"must be settled before KF.W6-TIMELINE."* Exactly two such rows exist in the corpus; W7 books one (ChannelControls, OP-1) and not this one — while routing **C-6's cure through AnimationControlsGroup itself** (`:108`, `:242`). The wave's headline seam cure is designed through an architecture an unbooked BLOCKER says must settle first.
3. **kf-AmigaScene D-2** (W9-routed twice: `:42` + the `:158` witness list) — bare-`<canvas>` a11y (no role/name/tabindex/keydown/fallback), the exact AT class R-1e swept. Not in the record's 12-item residue; 0 hits in KF-W9.md.
4. **kf-AmigaScene MISSED-A** (W9-routed twice) — no touch affordance at all for the scene's primary interaction; with D-2, *"zero discoverable interaction on a phone-filling canvas."* The single most on-thesis escape in the program (the wave's named condition is a real mobile Safari). 0 hits.
5. **kf-HeroAurora KF-HA-10** (`frame cost → KF.W9`) — no residue home (P-1..P-8 excludes it), 0 hits in KF-W9.md — **and PASS-1 affirmatively mis-booked it** ("all have residue homes. BOOKED."). The one pass-1 booking refuted by name.
6. **kf-EasingTarget KF-ET-28** (routed into W2's posture registry by the census-of-record rule) — *"value.js's Result API reaches the render path through a throwing adapter with no boundary… the `?? ""` decoy guarantees the throw it appears to prevent"*, NO-WAVE-OWNER degrade-don't-detonate posture. Absent from W2's registry; sole cross-hit is a passing fixture-rationale citation at KF-W4.md:72 (not a row, fold, or exclusion).
7. **W10 carry item C-3** — *"the census S-9/S-10 extensions terminalized"* (`KF-W6-CARRY.md:361`). Partial mitigation: W0's C-17 single-motion mint covers the slot minting; the terminalization RECORD routed to the fold-discharge wave escapes whole.
8. **W10 carry item C-4** — *"the `.tap-floor` adopt-or-delete ruling recorded"*. W6's G-W6-4 holds the measurement (0 adopters); the RULING RECORD escapes — `grep tap-floor KF-W10.md` → 0, re-verified.
9. **W10 carry item C-5** — *"the `/command` decision record filed so the census stops re-raising it"* — a row whose source calls it *"a wave product, not a code change"*, i.e. definitionally W10's. Claim-input booked at W6 ("pending the W0 mint") and W0 (C-17); the DECISION RECORD escapes — 0 hits, re-verified.

**Pass-1 comparison: 32 → 9.** Every one of pass 1's 32 is verified landed (W0's twelve in C-1.F riders-verbatim · the W2↔W3 four at W2 F0 per R-1b · SpringTrace D-1 at W4 row 29 · the W6 six incl. CubeTarget #21/#30/#39 · the W9 nine incl. the D-14+N-4 one-binding lock restored verbatim). The nine above are all NEW discoveries of the fresh 58-record sweeps — none is a pass-1 escape surviving; all are members pass 1's samples never enumerated. That is the narrowing's exact character: **the enumerated set was cured perfectly; the un-enumerated remainder was not re-derived** (KF-W9's D-1 states the mechanism as the spec's own defect).

### 1.3 Landed at a sibling (5) — the partition works where the local spec is short

- **kf-StartingStyleTarget KF-SST-36 · C-13** (W2 escape X8) → booked at KF-W3 §Carry C-2 (B-07), verified 2 hits this seat.
- **TimelineCaret L-15 · KeyframeTimeline L-11 · KeyframeTimeline D-15 · KAD-10** (W2 escapes X10–X13) → all four booked as W7 rows (P6/P8, verified by grep this seat). The defect is one-ended: `KF-W7.md:288` G14 says these register *"in KF.W2's posture registry (cross-referenced both ends)"* and the W2 end is short — the W2 registry defect stands (W2 D-3); no bytes are lost program-wide.

### 1.4 Register escapes (9) — the R1-negative/posture census of record is incomplete, two waves deep

W2's eight R1 verified negatives (X1–X7, X9: ControlsPaneWrapper S-9 · EditorHeader C S-A · MatrixEditor sup-7 · PlaybackRibbon C-neg · SequencePlayhead sup-7 · SequenceScene sup-4 · SpringHeatmap sup-7 · SpringTrace S-A) + the corpus's only §7.5-measured positive exemplar (ChannelControls SUP-3 `trySetOption`, 0 hits anywhere in waves/, re-verified). Union with KF-W3 D-02's eight same-shape absentees (CSSPasteDialog C-4 · LP-24 · KAD K-5 · OrbitalDrag #3 · SPF ★S-7 · THP S+3/S-d · AnimatedText C-10 · SpringTrace S-A; overlap = S-A) ⇒ **the F0 register that R-1b made census-of-record is short ~16 banked negatives**. No live defect content is lost (all are negatives/exemplars); what is lost is the register's stated purpose — *"recorded as negatives so no later seat re-probes it"* (G-W2-5) is false as written. One program defect, filed at both parser waves (deduped, §6).

### 1.5 Id-keyed trail escapes (7) — KF-W0's C-1.G index short against its own law

kf-SequencePlayhead **L-13/C-9** and **N-11** · kf-CopyButton **K3** · kf-EditorHeader **F2** · kf-EditorShell **L-2/C-2** · kf-MbabbMenu **K-1** · kf-SpringTrace **L-2** — all census-F-1 folds routed `→ KF.W0 §B-12`, content covered by C-1's fold mechanism, ids absent by bytes (re-verified: L-13 0 · N-11 0 · `MbabbMenu K-1` 0 · F2 0). C-1.G was minted this round *specifically* to make the census closable — and populated from PASS-1 §2.C's thirteen instead of from the bank, so its declared 65-row denominator is wrong (W0 D-3). The record that supplies C-19's founding dirty-manifest law (kf-SequencePlayhead) has both of its own W0 folds unindexed. Pass-1 classed this family "degraded landings"; pass 2 counts them escapes under **the spec's own minted law** (*"a row whose content is carried but whose id is absent by bytes is an escape"*). Law versioning stated so the 32→9 comparison stays like-for-like: under pass-1's classification these 7 (and §1.4's 9) would sit outside the hard count both times.

---

## §2 · DOUBLE-HOMING — fresh count

### 2.1 Pass 1's six: ALL CURED (verified in the checks, several verbatim)

R-2 (KF-KE-24/-32: §Bounds L54 carve list amended exactly as prescribed) · R-3 (TimelineCaret L-7 witness-only at W4, all three sites) · R-4/R-9a (ST-1's bytes → KF.W11; W9 measure-only, §Bounds write grants struck ×5) · R-5 (C-17 single-motion mint, 13 claimants, KSM demoted by E-3 addendum; W6's "pending the mint" clause) · R-6 (the limb manifest present at KF-W4:95-109, booked at W6 with D-7/L-4 first) — **6/6 dead**.

### 2.2 Fresh double-homings (3)

1. **`test/demo/instrument/` — one directory, two waves' write claims, undeclared at both ends** (W7 D6). KF-W7 §Bounds grants a `test/demo/instrument/**` **create-glob**; KF-W5 (as re-cut by R-9b item 4, under the program's own *"paths are enumerated, never globbed by shared prefix"* law) claims the same directory as its `.a` fixture path; the directory already holds 9 tracked tests including W7's own G11 witness (`timeline-undo.test.ts`). Neither §Disjointness names the collision.
2. **The `proof:brittleness` residue family — routed whole to KF.W6 while one live site sits inside KF-W4's own gate denominator** (W4 D-5). §Excluded 17 sends KF-AX-31 to W6 naming only `style.css:38-39`; the tree has `demo/styles/layout.css:152` — an enumerated G-KFW4-8 target and a §Bounds file. G-KFW4-8 must either red on a family the spec routed away or cure a byte assigned to W6: the R-8.2 partition does not hold at the bytes.
3. **KfPillTabs limb-labels — W4's R-6 manifest and W6's booking assign C:C-1 and L:D-8 to opposite limbs** (W6 §2.3, INFO-grade). All three ids are booked at W6, so R-6's id-for-id holds; its limb-for-limb does not, and a MAJOR fork-rationale row is re-booked at INFO under a limb the manifest assigns elsewhere.

**Pass-1 comparison: 6 → 3**, and the three are strictly smaller (one surface collision, one residue-family leak, one limb-label crossing — no whole-row contradictory claims survive).

---

## §3 · RULINGS EXECUTED — the 53-directive audit

Per-wave verification (each check's §Posture/repair-verification section, spot-receipts re-read this seat):

| wave | directives | verdict |
|---|---|---|
| KF-W0 | R-1a · R-5 · R-11 · R-15 · R-17 · R-19b | **6/6 APPLIED** (R-1a's twelve in ruling order, riders verbatim; R-11's seven sites all `npm run gh-pages`; R-17 executed as written — the roster remains short at the true 53-name altitude, a new defect, not a non-execution) |
| KF-W1 | R-15 · R-19d | **2/2 APPLIED** (R-15 receipted 17 at all three sites; R-19d cured in kind — its six receipts died to intra-round drift, D2-4) |
| KF-W2 | R-1b · R-10 · R-12 · R-16 | **4/4 APPLIED** (R-10 byte-verbatim; R-16 reciprocated at W8 both ends) |
| KF-W3 | R-7a(+companions) · R-7b(×2) · R-10 · R-12 · R-18 | **7/7 APPLIED** (both pass-1 BLOCKERs dead; §0c correctly cited as prospective, not phantom) |
| KF-W4 | R-1c · R-3 · R-6 · R-8 · R-10 · R-12 · R-15 · R-17 | **7/8 APPLIED + R-8 PARTIAL**: **R-8 item 4 UNEXECUTED on 3 of its 6 named rows** — §Bounds rows 59/64/65 still pathless (W4 D-7); each resolves in one read-only command the same file uses elsewhere. **The program's sole verified ruling non-execution.** |
| KF-W5 | R-9b · R-12 · R-14 | **3/3 APPLIED** (R-9b all four items, two verbatim, reciprocated at W4 both ends; R-14's subordination now rests on a stale 16-count — a substrate defect, not a breach) |
| KF-W6 | R-1d · R-2 · R-5 · R-6 · R-10 · R-13 · R-19a · R-19e | **8/8 APPLIED** (R-2/R-10 byte-verbatim, diffed by the seat) |
| KF-W7 | R-12 ×2 | **2/2 APPLIED** (+ a fifth phantom-CARRY binding found and struck beyond the ruling's four) |
| KF-W8 | R-13 · R-14 · R-16 · R-19c | **4/4 APPLIED** (G6 ACTING; both structural instances declined in ONE ruling; all four inbound edges booked-or-declined) |
| KF-W9 | R-1e · R-9a · R-10 · R-11 · R-15 | **5/5 APPLIED, verbatim** (the D-14+N-4 one-binding lock restored word-for-word; every cure arm gone; §Bounds zero kf write grants) |
| KF-W10 | R-10 · R-14 · R-15 · R-18 | **4/4 APPLIED** (R-10's 746-char clause byte-identical; R-14's record kept out of G-1's denominator; R-18's scope split held) |

**rulingsUnapplied = 1** (R-8.4, partial, owner KF-W4 — filed there as D-7).

**Two lawful-but-fatal enumeration gaps in the rulings themselves** (executed as enumerated; the class survived at the unenumerated member — these are RULINGS-authoring lessons, not wave breaches):
- **R-11** enumerated 10 `build:gh-pages` sites (W0 ×7, W9 ×3) and cured all 10; the phantom survives at **KF-W7.md:323** (re-verified live this seat), the witness-refresh precondition for every byte-offset receipt in the wave (W7 D2, MAJOR).
- **R-12**'s general rule (*"no other wave may cite a per-wave CARRY as provenance"*) was routed to five waves and never run at **KF-W1**, whose SPECIFIED verb rests wholly on a phantom "SS-2 CARRY" — the exact *"N rows carried, 0 dropped"* claim R-12 orders struck (W1 D2-3, MAJOR).
- Related standing item: **R-18's COHESION §0c addendum** (the root session's act, outside any wave's §END row) remains unwritten — `grep '§0c' COHESION.md` → 0, re-verified — and KF-W10 correctly withholds its sub-tranche-scope SPECIFIED pending it. Not a breach; flagged upward again.

---

## §4 · HEAD + GATE — the two program-wide posture axes

### 4.1 KF.W4 sequencing head (`KF-W4.md:223` — re-verified this seat; the law moved from `:193` in the repair)

**HOLDS in all 11 specs.** Pass 1's one hard breach (KF-W9's G-KFW9-10) is **CURED by R-9a**: the wave is now purely a measurement wave, its five kf write grants struck, OP-6 true by construction. Pass 1's partial strain (KF-W5's G-XSS observability) is **CURED by R-9b** with the CI-wiring edge declared and reciprocated at both ends (verified at `KF-W4.md:144`/`:190`).

Two residues, both coordinate-grade, neither a breach: **KF-W9 (:44, :208) and KF-W10 (:324, :334) cite the head law at the dead `:193`** — the same repair round moved it to `:223` (G-KFW4-12's gate row now sits at `:193`); four load-bearing citations across two specs point at the wrong row (deduped as one program defect, §6). And the head wave itself is again the least-closable in the program: **its own head gate G-KFW4-1, as redefined, silently deletes the live `proof:structure` gate** that OP-3/R-17 were re-litigated to prove present (W4 D-1, CRITICAL) — pass 1's pattern (the head everyone defers to carrying the worst defect) reproduces with a new instance.

### 4.2 KF.W3 GATED via PLAW-BIND, never scheduled

**HOLDS in all 11 — and the W3 spec itself is now CLEAN**, the program's first. No spec schedules W3 work or creates a parse-that→keyframes edge; W6 and W9 carry 0 W3 tokens; W1 declares the X·P NON-EDGE; W5 pre-declares both OP-4 dispositions; COHESION §0b (KF.W3 → SS-1) is consumed correctly at W3 and W10. One blemish: **KF-W10 §1's "Opens after KF.W2..W9 IMPLEMENTED" sweeps the GATED wave in with no carve-out** (W10 D-7, MINOR; partially inoculated by its own "dependency is on the artefacts, not the labels" sentence, and contradicted by OP-5's weaker restatement of the same precondition).

### 4.3 KF-AV-28 (the standing supersession rider)

**Presence: CURED program-wide** — R-10's clause verified byte-verbatim at W2/W3/W4/W6/W9/W10, held ×7 at W7 and ×1-plus at W8, correctly absent at W1/W5 (no governed rows). **Depth: two under-enumerations survive** — W8 carries the rider on 2 of ~7 governed rows and spends bytes on unrided ones (D-10); W9's §H enumeration is R-10's list, not the wave's own, and names no PlaybackRibbon row while its own S-11 says the ribbon packet is governed (D-8). One program defect (deduped): the per-wave governed-row enumerations were transcribed from the ruling instead of re-derived from the bank.

---

## §5 · MONOTONE NARROWING — what survived pass 1, what died, what is new

### 5.1 Classes DEAD at pass 2 (cured and verified)

1. **The phantom per-wave CARRY class** (pass-1 P-3) — R-12 executed at all five owners; sole survivor is W1's un-routed SS-2 CARRY (§3). 2. **The invented-authority class** (P-4, "kf-SquareScene law") — struck and re-founded at W2; the mint anchored at W6 (one anchor off by 8 lines, W8 D-6, LOW). 3. **All six pass-1 double-homings** (§2.1). 4. **The head breach** (R-9a). 5. **The minted-id class** (KF-SP-3 re-keyed; W7's two spec-local labels lawfully disclosed). 6. **`build:gh-pages` at its ten enumerated sites** (survivor: the unenumerated eleventh, W7). 7. **The 34-escape W0 census hole, the 19-escape W2 hole, the 10/19 W3 BLOCKER pair, the W9 AT arm, the W6 manifest trio** — every pass-1-enumerated escape verified landed.

### 5.2 Classes SURVIVING pass 2 — named as classes

- **S-1 · Closure-by-list, not by re-derivation** (the escape disease's surviving mechanism; direct descendant of pass-1's U-1). Every pass-2 escape is a member no pass-1 list named: W0's C-1.G adopted PASS-1 §2.C's thirteen unchanged (D-3); W2 closed over a 37-of-58 sample (D-8); W9 cured the ten and wrote *"no routed id now escapes"* without re-enumerating (D-1, HIGH); W10 scoped the carry to W6 because pass 1 did (D-1, BLOCKER). Four independent seats, one mechanism.
- **S-2 · The phantom-coordinate class — MUTATED, not narrowed.** Pass 1's phantom scripts are dead; pass 2 finds phantom **§Bounds paths and citation sites**: W4's `scripts/usability.mjs` + `scripts/observe/live-session.mjs` (exist at no ref) and the EditorShell phantom proof-site (CRITICAL); W2's `resolve/resolve-if.ts` (HEAD-only artefact); W5's five missing §Bounds paths + `internal/yield-batch.ts` (exists at NO ref — a spec-invented coordinate). Root cause below (S-5).
- **S-3 · The self-miscount class** (pass-1's "counting waves' self-miscounts") — reduced in magnitude, alive in kind: W9's 14≠13 / 18≠17 / 9≠11 (the round's signature act was curing an off-by-one; its own ledger reproduces one), W7's 145+3 ≠ 142+6, W10's 51≠53 grep field, W3's five-records≠four, W5's 7-of-11 directory listing.
- **S-4 · Denominator short-counts on gates** — W0's G-0.7 sweeps ≤11 names against a measured 53 (and its GREEN is unsatisfiable inside its own §Bounds — the one bounds-contradiction defect, W0 D-1); W4's G-KFW4-8 still conflates sites with citations (19 names / 33 lines vs ~10 inventoried); W3's G-KF3-3 names an unenumerable 15.
- **S-5 · NEW CLASS — repaired-against-the-rulings-never-against-the-tree** (the pass's dominant root cause; U-3). The repair seats verified ruling text and never re-opened the substrate: KF-W5's Arm D authored wholly on `8281638c`, the ref its own line 8 DISQUALIFIES (5 of 6 rows stale; **G-SHIM born GREEN**; **G-DEPCRUISE's oracle inverted** — driving it green deletes a true sentence; **G-BASIS's flagship probe returns 0** — `tr -d '\0'` collapses the list); KF-W1 authored on the pre-12:32 INBOX (the O-20 BLOCKER + three dead gate anchors stamped "re-measured"); KF-W10's OP-5 calls a dirty tree clean and G-6 measures the 08-04 file under an 08-28 stamp; KF-W4's nine unresolvable anchors under a §Bounds preamble promising re-resolution.
- **S-6 · NEW CLASS — intra-round anchor rot** (U-2). The round repaired 11 specs serially (mtimes 12:39→13:15) with no closing re-anchor pass; ~14 cross-spec receipt families died: KF-W4:193 (cited by W9 ×2 + W10 ×2), KF-W0:276 (W9 + W1), KF-W5:215 (a **phantom verbatim** — the quoted lock exists nowhere — cited ×4 by W8), W8's five stale families (two internally self-contradicting), W7's two dead W6 anchors, W1's six dead sibling anchors, W2's three, W5's inherited-not-re-derived `:86-88`. In every audited case the SUBSTANCE survives at a nearby line; the receipts do not — and a fresh seat bounces off all of them, which is precisely the I-26 failure mode this program exists to cure.
- **S-7 · NEW DEPTH — executable gate clauses that do not execute.** Pass 2's seats ran the commands (pass 1 mostly verified witnesses): G-W2-2 inert (POSIX `-E` has no `\b`; repaired, blind to 6 of 10 multi-line importers; false-matches kf's own wrapper) · W7's G11 command returns the opposite of its stated reading · W8's G4 still greps the umbrella its own falsifier forbids, and G1 clause 2 is unsatisfiable at any tree state · W6's two false born-RED witness cells (G-W6-9 "all at zero mounts" vs FadingScroll consumed on both axes in a §Bounds file; G-W6-10's "3 live files" vs the 12/8 the same spec declares 28 lines up) · W4's "green empty set" lane that holds 27 specs.

**Net monotonicity verdict**: genuinely monotone on the axes the rulings addressed (census carriage, double-homing, rider presence, head/gate posture, phantom ledgers, phantom scripts-as-enumerated) — and **non-monotone on measurement freshness**, where the repair round introduced two new program-wide classes (S-5, S-6) that now hold most of the defect mass. Repair round 2's contract is therefore different in kind from round 1's: not "book the escapes" but **"re-measure at the pinned ref/time, then re-anchor the round in one closing pass."**

---

## §6 · UNION DEFECT REGISTER — fold + dedupe

**Arithmetic**: 109 raw filings across 11 checks → **−4 cross-filed dedupes** → 105 → **+3 union-level defects** → **108**.

Dedupes (one program defect each, filed twice):
- **P2-1 · the dead head-law anchor `KF-W4.md:193`** — filed at W9 D-3 + W10 D-3; four citations across two specs, one moved sentence. (−1)
- **P2-2 · the stale `KF-W0.md:276` packet-roster cite** — filed at W9 D-4 + W1 D2-7; both cite a line that now holds an N-3 witness and a superseded 15-count. (−1)
- **P2-3 · the R1-negative register of record short ~16 banked members** — filed at W2 D-5 + W3 D-02 (overlap kf-SpringTrace S-A); one register, two ends (§1.4). (−1)
- **P2-4 · KF-AV-28 governed-row enumerations transcribed, not re-derived** — filed at W8 D-10 + W9 D-8. (−1)

Union-level additions:
- **U-1 · MAJOR · 9 rows/obligations routed and carried by NO wave** (§1.2) — headed by an ADJUDICATED BLOCKER (ACG D-1) absent from the wave that routes its headline cure through the same component.
- **U-2 · MAJOR · intra-round anchor rot, program-wide** (S-6) — ~14 dead cross-spec receipt families incl. one phantom verbatim; no closing re-anchor pass existed in round 1's protocol; the round's own carried law (*"anchors are re-resolved at authoring, never copied"*) is breached by its execution order.
- **U-3 · MAJOR · the repair executed against the rulings, never against the tree** (S-5) — five specs carry measurements from a state (ref, file, or clock) their own text disqualifies; two gates are born GREEN, one oracle inverted, one flagship probe returns 0, one ledger id double-mintable.

### Ranked worst-10 (the register's head)

| # | sev | wave | defect |
|---|---|---|---|
| 1 | BLOCKER | KF-W1 | The wave commissions outbound id **O-20** at five load-bearing sites — an id commit `48003630` spent **nine minutes before the repair write** (INBOX.md:95, re-verified the measured maximum this seat). Executing as written mints a duplicate live id into the E13 ledger and back-points O-8/O-11 at it, reproducing I-26 in the ledger layer under the spec whose thesis is I-26. Correct mint = O-21. |
| 2 | BLOCKER | KF-W10 | Three of five obligations routed by the sole in-tree carry (`KF-W6-CARRY.md:361`) **close by omission** — the census S-9/S-10 terminalization, the `.tap-floor` adopt-or-delete ruling record, the `/command` decision record (*"a wave product, not a code change"*). §7's 13 exclusions name none; the carry predates the repair by two hours. The exact failure §2 declares fatal. |
| 3 | CRITICAL | KF-W4 | **G-KFW4-1 — the declared sequencing head — redefines `npm run check` in a way that silently deletes the live `proof:structure` gate**, the very script OP-3 and R-17 were re-litigated to prove present at `origin/master`. No §Bounds row, commit line, or falsifier declares the removal. The wave that exists to extinguish phantom gates extinguishes a real one at hop one. |
| 4 | CRITICAL | KF-W4 | **`EditorShell.vue:235-237` is a phantom citation site** — zero `proof:` bytes at either ref — while it anchors a §Bounds carve row, a G-KFW4-8 born-RED witness, and one of the NINE targets carrying R-8.1's entire satisfiability arithmetic; the real third `proof:appearance-suffusion` site (`DESIGN.md:251`) is unenumerated. |
| 5 | HIGH | KF-W9 | The repair's headline closure claim — *"no routed id now escapes all four mechanisms"* — is **false at the bytes**: kf-AmigaScene D-2, kf-AmigaScene MISSED-A, kf-HeroAurora KF-HA-10 escape (two of them AT/discoverability rows on the mobile-Safari cell the wave exists to open; one affirmatively mis-booked by pass 1). Closure was inferred from pass 1's list, never re-enumerated. |
| 6 | MAJOR | PROGRAM (U-3) | **Repaired against the rulings, never against the tree**: KF-W5's Arm D authored wholly on the ref its own header disqualifies (G-SHIM born GREEN over three nonexistent files · G-DEPCRUISE's green deletes a true sentence · G-BASIS's probe returns 0) · KF-W4's nine unresolvable anchors incl. two phantom script paths · KF-W2's §Bounds owns `resolve-if.ts`, absent at the ref of record. |
| 7 | MAJOR | PROGRAM (U-1) | **Nine hard partition escapes** (§1.2) — headed by kf-AnimationControlsGroup **D-1**, an ADJUDICATED BLOCKER (*"must be settled before KF.W6-TIMELINE"*) absent from KF-W7 while that spec routes C-6's cure **through AnimationControlsGroup itself**; plus the W6 manifest escape (D·D-7) from the record pass 1 recorded as routing nothing. |
| 8 | MAJOR | PROGRAM (U-2) | **Intra-round anchor rot**: ~14 dead cross-spec receipt families across 8 specs — the head law cited at `:193` by two specs, `KF-W0.md:276` by two, KF-W5:215 as a **phantom verbatim** cited ×4 by W8 (two of W8's five families internally self-contradicting), W1's six dead sibling anchors, W7's two dead W6 anchors — no closing re-anchor pass existed. |
| 9 | MAJOR | KF-W6 | **Two born-RED gate witnesses false at the tree they name**: G-W6-9's *"all at zero mounts"* (FadingScroll consumed on BOTH axes with the real subpath import in `EasingTarget.vue`, a §Bounds file — the wave-wide "ONE FadingScroll decision" register is authored against the wrong tree; Card ×24; ToggleGroup mounted and self-contradicted in-cell) and G-W6-10's *"SpringSidebar in 3 live files"* (measured 12/8 — a cell the spec's own W6-N declares stale and leaves standing 28 lines away). |
| 10 | MAJOR | KF-W2 | **G-W2-2 is inert at the ref of record** (verbatim: 0 hits — POSIX `-E` has no `\b`; repaired to `-P`: blind to 6 of 10 multi-line importers; false-matches kf's own `parseKeyframeSelector`, the exact conflation F1 corrects) **and the census of record closed over 37 of 58 records** — a fresh 58-record sweep finds 15 further routed cells and the demo's principal CSS→AST adapter (`parseAnimationCSS.ts`) absent from the census entirely. |

**Register tail (11–108, by cluster, all retained from the per-wave checks)**: W0's bounds-contradiction (G-0.7's GREEN unsatisfiable inside its own §Bounds — the repair round must answer it first) + 53-name roster + §6.5 mis-homed to CENSUS at three altitudes (the F-R6 class, second instance) + the seven-names-nobody-wrote-down GREEN key · W1's three dead INBOX gate anchors stamped "re-verified" + the un-run E13 sweep that would have handed C-9 its own 19-day precedent + the SS-2 CARRY phantom · W2's demo-side gate hole (KF-KE-54's binding vs the `src/`-scoped G-W2-2) + the posture-registry short-count + `parseAnimationCSS.ts` · W3's unenumerable 15-module subject set + the five-rows-four-records cardinality (pass-1 D-10's error moved, not killed) · W4's phantom script paths + the layout.css residue leak + the un-routed `proof:bezier-*` witness + the 27-spec "empty" lane + the missing SequenceScene K3 site + R-8.4's three unexecuted rows · W5's stale G-STRUCT denominators (3 of 11 stutters renamed, 2 of 9 god modules gone — and R-14's subordination now rests on the stale 16) + five missing §Bounds paths + the no-ref `internal/yield-batch.ts` + its own G-ID failure (6 bare ids) · W6's surviving escape + three id-keyed trail gaps (KF-CE-32 · LP-2 · M7, disposed by class, unresolvable by id) · W7's `build:gh-pages` survivor + the 145+3 composition error + G11's inverted witness + the create-glob collision + the hardened paraphrase-as-quotation · W8's five stale anchor families + three gate-authoring defects (G1.2 unsatisfiable · G4's umbrella hole · the false L-19 blanket over 3 commandless/7 falsifier-less gates) · W9's three-numbers family (14/13 · 18/17 · 9/11) + the 563-vs-565 cell · W10's paraphrase-presented-as-quotation (the exact defect it books as never-cite entry 7) + OP-5's false clean-tree + G-6's 08-04 measurement + G-4's misreported output · plus the MINOR/LOW/INFO tail as filed.

---

## §7 · What HOLDS program-wide (recorded so pass 3 does not re-litigate)

1. **The repair contract was honored: 52 of 53 §END directives verified applied** — many byte-verbatim (R-10's clause diffed identical at four waves; R-2's carve list; R-1e's one-binding lock; R-15's 17-packet roster identical at five sites). The rulings mechanism WORKS; what it cannot do is re-measure, and that is the whole of pass 2's residue.
2. **Census carriage is near-airtight**: 1051/1081 (97.2%); five waves at 0 escapes (W1 · W3 · W4 · W5 · W8); W6 at 99.7%. The 30 residual escapes decompose cleanly (§1.1) with only 9 hard.
3. **E-3 + STATUS is clean in all 11, both passes**: zero VERIFIED stamps, `planned` everywhere, zero product-source writes, addenda-not-patch honored (W6 now enumerates six owed bank writes by id; W10's sidecar-JSON discipline exemplary).
4. **KF.W3's PLAW-BIND fence held under 22 adversarial reads across two passes**, and its spec is the program's first CLEAN.
5. **Measurement discipline where it was applied is superb**: W8's 15/15 gates exact · W6's 46/46 paths + 30/30 line counts + eleven new-prose anchors all byte-exact · W2's F0 matrix re-executed cell-for-cell on the installed dist · W9's every-runnable-witness-true · W0's thirty-plus baselines byte-exact · W10's ~30 commands across two repos. **Zero fabricated registry citations were found in any spec, either pass** — every failure is staleness or scope, never invention. NO-INVENTION/M-25 holds at the row level in all 11.
6. **The instruments now exist to close pass 3 mechanically**: C-1.G (once re-derived), the F0 register (once completed), the 17-packet roster (stable across five specs), the per-wave escape lists above — every surviving defect has a named one-command receipt.

### The repair-round-2 contract this adjudication implies (advisory, one line per class)

(i) Re-measure the five stale-substrate surfaces at the pinned ref/clock (W5 Arm D · W1 INBOX+O-21 · W10 OP-5/G-6 · W4's nine anchors · W2's resolve path); (ii) one closing re-anchor pass over all 11 specs AFTER the last edit (kills U-2 whole); (iii) book the nine §1.2 escapes at their named owners (W6 ×1 · W7 ×1 · W9 ×3 · W2 ×1 · W10 ×3); (iv) complete the two registers (W0's 53-name roster with a loud out-of-bounds disposition; W2's F0 over the full 58) and re-derive C-1.G from the bank; (v) execute R-8.4's three rows and re-cut the seven defective executable gate clauses (G-W2-2 · G-BASIS · G-SHIM · G-DEPCRUISE · G11(W7) · G4/G1.2(W8)); (vi) the root session writes COHESION §0c.

---

## §8 · Structured summary (mirrors the StructuredOutput call)

- **verdict**: DEFECTIVE — 10 of 11 DEFECTIVE; KF-W3 first CLEAN; carriage 91.0%→97.2%; hard escapes 32→9; double-homings 6→3 (all pass-1 cured); 52/53 rulings applied; dominant residue = stale-measurement classes (U-2/U-3); 2 BLOCKERs (W1 O-20 id collision · W10 carry close-by-omission)
- **defectCount**: 108 (109 raw − 4 dedupes + 3 union)
- **blockers**: 2 · **high**: 4 (2 CRITICAL + 2 HIGH)
- **partitionEscapes**: 9 (§1.2, id-for-id; law-versioning note at §1.5)
- **doubleHomed**: 3 (§2.2; pass 1's six all cured)
- **rulingsUnapplied**: 1 (R-8.4 at KF-W4, partial — rows 59/64/65)

*Union seat, PASS 2. This file is the pass's only union write; nothing here stamps any wave. E-3: a check record, not a spec amendment.*
