# X·KF REPAIR ROUND 2 — CROSS-WAVE RULINGS

**Seat**: cross-wave rulings, repair round 2 · 2026-08-28
**Authority**: `PASS-2/UNION.md` (read whole) · the 11 PASS-2 per-wave check files · `PASS-1/RULINGS.md` (the round-1 baseline; its laws stand except where amended here) · the 11 wave specs at `docs/tranches/X/keyframes/waves/` · the 58-record corpus at `docs/tranches/V/megatranche/registry/adjudicated/` · `carry/KF-W6-CARRY.md` (the only in-tree X·KF carry, 201 rows / 54 records) · READ-ONLY probes of keyframes.js at **`origin/master 81a56990736ced5b5edde0b84c527680ac7689b1`** (the ref of record; re-verified this seat) and of value.js `docs/tranches/V/coordination/INBOX.md` at the current bytes.
**Pass-2 verdict being repaired**: DEFECTIVE 10/11 · 108 defects · 2 BLOCKER · 2 CRITICAL · 9 hard partition escapes · 3 fresh double-homings · 1 round-1 ruling partially unapplied. KF-W3 is CLEAN — **no seat touches KF-W3 except where a ruling below names it explicitly (R2-11's one sub-line) and the R2-7 re-anchor pass (mechanical)**.
**Law** (unchanged from round 1, restated): every cross-wave defect gets exactly ONE owning cure a single wave can apply; per-wave repair seats execute these rulings verbatim; E-3 everywhere (registry amended by ADDENDA under original ids; specs repaired in place — they are `planned`, unstamped); nothing here authorizes execution or opens product source. **New this round, binding on every seat: R2-5 (measure at the tree, never at the rulings) and R2-7 (stable anchors; the Re-anchor seat writes LAST).**

**17 rulings.** Ruling → per-wave directive map at §END. Frontier of record: keyframes.js `origin/master` = `81a56990`.

---

## R2-1 · BLOCKER (KF-W1) — the wave commissions O-20, an id SPENT nine minutes before the repair write; the mint is **O-21**

**Measured by this seat at the current bytes**: `INBOX.md:95` is the **O-20** row — the SS-6 batched communique to glass BK, **SENT 2026-08-28**, minted by commit `48003630` (12:32:16); `7a7dc6ef` (12:33:59) then rowed I-28/I-29 at `:96-:97` and performed the BI→BK sweep-path cure. The measured outbound maximum is **O-20**. KF-W1.md was repaired at 12:41 and commissions the mint of O-20 at five load-bearing sites. Executing as written writes a duplicate live id into the E13 ledger and back-points O-8/O-11 at it — I-26 reproduced in the ledger layer under the spec whose thesis is I-26.

**Cure (exact), owner KF-W1**:

1. **Every commissioned `O-20` becomes `O-21`** — the five load-bearing sites (`:30` §1 hard-gate condition 7 · `:75` §3 item 6 · `:89` §4 Bounds INBOX row · `:132` C-10(ii) + its ANTI-RENAME pointer clause · `:187`/`:189` G-KF1-7 title + GREEN) **plus every trailing citation** (`:237` cross-edge 11's "I-26/O-20 ledger receipt" and any other token — the repair seat closes by `grep -n 'O-20' KF-W1.md` and re-keys every hit, then re-runs the grep to prove 0 commissioning hits remain).
2. **The mint law is written into C-10**: *"the commissioned id is O-21 (= the measured ledger maximum O-20 + 1, measured 2026-08-28 at `INBOX.md:95`/commit `48003630`); the executing seat re-runs the max-grep at open and, if the ledger has moved again, mints max+1 and records the delta as a dated addendum before executing — a spec-frozen outbound literal is never trusted over the measured ledger."*
3. **G-KF1-7's witness is re-cut**: the max-grep's stated output becomes **O-20** (not O-19); GREEN = *"I-26 reads CURED with its receipt; **O-21** minted naming (a)–(d); O-8/O-11's rows keep their text plus an **amended-by-O-21** pointer."*
4. **The three dead INBOX anchors are re-keyed at every citing site** (C-1 · C-4 · C-9 · §3.5 · §4 Bounds · G-KF1-6 · G-KF1-7 · G-KF1-8 · §9.6): the sweep-path law lives at **`:15-16`** (not `:13-14`) · I-10 lives at **`:47`** (not `:45`) · I-26 lives at **`:92`** (not `:90`). §4 Bounds' three-edit grant is re-aimed accordingly: *"the sweep-path law at `:15-16` · I-26 at `:92` ROWED→CURED · a new outbound row **O-21**."* G-KF1-8's witness is narrowed per its measured output: the grep returns **4 lines** (`:15` the law + `:66`/`:72`/`:75` the O-2/O-8/O-11 path cells) — state "4 hits, the law at the first," or narrow the pattern.
5. **C-9 gains its own precedent, one sentence** (check D2-5): commit `7a7dc6ef` cured the identical sweep-path defect class in the same file minutes before this spec's repair, with the cost measured in-row (*"the stale BI pin here cost 19 days on the 08-09 letters → I-28/I-29"*) — cited as corroboration of C-9's thesis and as the template for its two-line cure. No new obligation, no scope change.
6. **R-12's general rule is finally run here** (check D2-3): the phantom **"SS-2 CARRY"** provenance is struck at `:7`, `:39` (the SPECIFIED evidence cell), `:87`, `:119`, and §9's framing; provenance restated as *"authored from the 58-record registry directly; the routed denominator is the registry's (PASS-2 census: **6 routed / 6 booked / 0 escaped**)"*; §9's six corrections are re-labelled dated authoring-time findings (content preserved, E-3) rather than "corrections to the CARRY" — no ledger that is not in the tree is cited as a source again.

**Sibling corrections**: none. (KF-W10's OP-3 language names the addendum + ledger receipt generically; no W10 text names O-20.)

---

## R2-2 · BLOCKER (KF-W10) — the three obligations routed by `KF-W6-CARRY.md:361` close by omission; all three are CARRIED as records

The carry's W10 edge (re-read verbatim this seat) routes five items; A-1/A-4 are booked; the other three appear nowhere in KF-W10.md and §7's 13 exclusions name none. All three are, in the carry's own words, wave products — **W10 CARRIES all three as RECORD-ONLY rows** (the R-14 record/act discipline, already exemplary in this spec, is the template). §7's exclusion roster is thereby **unchanged in membership (13)** — the correction is that the three enter §2b as rows, not §7 as exclusions.

**Cure (exact), owner KF-W10** — three new §2b record rows:

1. **C-3 · "the census S-9/S-10 extensions terminalized"** (`KF-W6-CARRY.md:361`; the S-9 quartet `:174-:177`, S-10 `:179`) — RECORD-ONLY: W10 records the terminal verb of each claimant row by **consuming the output of KF.W0's C-17 single-motion mint** (round-1 R-5: C-17 is the ONLY census-slot authority; W10 mints and renumbers nothing). Sequenced after C-17. Terminal verb per claimant recorded; the record enters **no gate denominator** (G-1 stays 15; G-5 stays 4 addenda).
2. **C-4 · "the `.tap-floor` adopt-or-delete ruling recorded"** (`KF-W6-CARRY.md:83` the roster · `:268` G-W6-4's measurement, **0 adopters**) — RECORD-ONLY: the RULING RECORD files here at fold-discharge, citing W6's G-W6-4 as the measurement of record; **the act's bytes are KF.W6's** (its §Bounds carries `demo/styles/design-idioms.css`, where `.tap-floor` lives at `:81-82`) — W10 records the ruling's terminal verb, never the deletion.
3. **C-5 · "the `/command` decision record filed so the census stops re-raising it"** (`KF-W6-CARRY.md:179` — *"A DECISION RECORD, NOT A SWAP … A wave product, not a code change"*) — CARRIED: definitionally W10's. The record row carries the R-5 clause verbatim from W6's booking idiom: *"census id pending the KF.W0 C-17 mint"* — the slot id is whatever C-17 assigns; W10 files the decision record under it.

**Tally correction**: §1's census restates **9 routed · 9 booked · 0 escaped**. The three record rows are declared OUT of G-1's 15 and OUT of G-5's addendum set, in the same sentence pattern the R-14 record already uses. This ruling also discharges UNION §1.2 items 7–9 (the W10 third of the nine hard escapes).

---

## R2-3 · CRITICAL (KF-W4) — G-KFW4-1 re-cut so `npm run check` COMPOSES with the live `proof:structure` gate; no silent deletion of a live gate, ever

**Verified at the frontier by this seat** (`git show origin/master:package.json`, sed 37p): the live script is
`"check": "tsc --noEmit && tsc --noEmit -p tsconfig.test.json && npm run proof:structure"` — and `scripts/gates/` holds **9 files** (`structure/index.mjs` among them), with three runnable `proof:*` npm scripts (`proof:structure` · `proof:publish` · `proof:owner-golden`). OP-3/R-17's finding is TRUE at the tree. G-KFW4-1's redefinition (`vue-tsc --noEmit -p tsconfig.json && tsc --noEmit -p tsconfig.test.json`) silently drops the live `proof:structure` invocation.

**Cure (exact), owner KF-W4**:

1. **G-KFW4-1's redefined command becomes**: `vue-tsc --noEmit -p tsconfig.json && tsc --noEmit -p tsconfig.test.json && npm run proof:structure` — the `vue-tsc`-for-`tsc` substitution is the wave's cure (SFCs can finally fail a build); **the `&& npm run proof:structure` tail SURVIVES verbatim**. The gate row states this composition explicitly: *"the redefinition replaces the type-check legs and preserves every other invocation present in origin/master's `check`."*
2. **The no-silent-deletion law is written into the gate**: any removal of a live npm-script invocation from a redefined script requires (i) a §Bounds row quoting the before/after script lines verbatim, (ii) a §Commits line declaring the removal, and (iii) a falsifier. G-KFW4-1 gains that falsifier now: *"fails if the redefined `check` at execution lacks any invocation present in `origin/master`'s `check` without a declared §Bounds removal row."*
3. **No removal is authorized this round.** The wave that exists to extinguish phantom gates does not retire a real one at hop one.

---

## R2-4 · CRITICAL (KF-W4) — the phantom ninth citation target replaced by the real third `proof:appearance-suffusion` site; R-8.1's satisfiability arithmetic re-based on the measured EIGHT

**Verified at the frontier by this seat**: `EditorShell.vue` carries **0** `proof:` bytes at `origin/master` (`grep -c` = 0; `:234-237` is the clause-g legibility comment). The demo-scoped `proof:appearance-suffusion` sites are exactly **`demo/DESIGN.md:251`** and **`demo/styles/layout.css:31`**; `EditorStartScreen.vue:15` is `proof:hero-two-focal`.

**Cure (exact), owner KF-W4**:

1. **`EditorShell.vue:235-237` is STRUCK at all three homes** — R-4's three-live-sites sentence, the §Bounds Citation-targets row, and G-KFW4-8's born-RED witness column. R-4's sentence is restated: *"All three live sites retire (`demo/DESIGN.md:251` [appearance-suffusion], `EditorStartScreen.vue:15` [hero-two-focal], and the token authority at `layout.css:29-31`)."*
2. **The enumerated citation-target set is restated at EIGHT**, with the measured per-target `proof:` line counts printed so the arithmetic is checkable: `DESIGN.md` 13 · `EditorStartScreen.vue` 5 · `App.vue` 3 · `HeroAurora.vue` 3 · `TypingDots.vue` 3 · `layout.css` 3 · `design-idioms.css` 2 · `SpringPhysicsFacet.vue` 1 = **33 citation lines · 19 distinct names** (of which one, `proof:publish`, resolves to an executable).
3. **R-8.1's satisfiability is restated as a PARTITION over the 19 names at the eight targets**: every name is (a) in the cure inventory (token + clause, ≤1 line per site, with the `DESIGN.md:235-243` Proof-column table surgery declared as its own bounded act), (b) routed by NAME on an §Excluded 16–20 residue line, or (c) declared out with a named owner. G-KFW4-8's GREEN claim survives only when (a)+(b)+(c) partition the 19 — the sites-vs-citations conflation (check D-4) dies with this restatement.
4. **The unreachable witness is struck and routed** (check D-6): KF-CO-33's three `proof:bezier-*` at `ChannelOptions.vue:168` leave G-KFW4-8's witness column (the sole site is outside the eight targets and outside §Bounds); §Excluded gains one residue line: *"KF-CO-33's `proof:bezier-*` (ChannelOptions.vue:168) → the OPTIONS-UNIT packet at KF.W12"* — R-8.2's routing idiom, no drop.

---

## R2-5 · THE U-3 CLASS — the WITNESS RE-DERIVATION LAW (binding on every repair seat this round)

**Law**: every measurement a repair seat writes, carries, or leaves standing this round is **RE-EXECUTED at the substrate of record at write time** — keyframes.js at **`origin/master 81a56990`** (the ref KF.W0 declares of record; local `8281638c` is DISQUALIFIED and may be consulted only to date drift), value.js at the current tree bytes at the seat's clock — and **never inherited from a ruling's prose, a lane doc, a CARRY, a pass-1 register, or the spec's own prior text**. A ruling orders a cure; it is never a source of measurements. Any figure stamped *"re-measured ⟨date⟩"* must carry a command that reproduces it at that date's bytes. A gate/witness/anchor a seat touches and does not re-verify is that seat's defect at pass 3, at the toucher's severity.

**Named instances, with owners** (each is a directive):

- **(a) KF-W5 — Arm D whole** (its check D-1..D-6): re-run all six rows and G-RING's graph claims at `81a56990` (41 commits past the lane's substrate; the structural three: `7e9ddf49` presets-shim delete · `1412ed8e` stutter renames · `53b907c5` play-lifecycle carve). Book the upstream-cured rows **CURED-AT-FRONTIER with commit shas** (the spec's own B-14 idiom); D-1's four dead LIGHT paths survive RED — say so with the receipt. Re-path the five dead §Bounds/§Disjointness paths (`engine/play-lifecycle/**` · `compile/emit/backward/{backward,walk}.ts` · `engine/css/animation.ts` · `group/yield-batch.ts`); **`internal/yield-batch.ts` exists at NO ref — a spec-invented coordinate, struck**; `.d`'s "exactly three files" restated. Arm B's shared-prefix glob → the enumerated `test/orchestration/{split-text,split-a11y-oracle}.test.ts`; the nine created specs homed in the zone directories the tree actually uses (no `test/animation/`). **G-STRUCT's denominators re-measured** (8 of 11 stutters live · 7 of 9 god modules live at the frontier) and the new figure **RELAYED to KF-W8's G13 subordination sentence in the same motion** (R-14's clause updated at both ends — KF-W8 receives this as its reciprocal directive).
- **(b) KF-W2 — the resolve path** (its check D-2): `src/animation/resolve/resolve-if.ts` is a HEAD-only artefact; the real module is **`src/animation/resolve/conditional.ts`** (`legacyClauses` at `:27`/`:52`). Re-pathed at §Bounds, G-W2-3, and §Excluded; entered as the drift table's seventh member.
- **(c) KF-W4 — the nine anchors** (its check D-3/D-8/D-9/D-10/D-11/D-15): the two phantom script rows re-pathed to **`scripts/observe/demo/usability.mjs`** (410 L) and **`scripts/observe/demo/live-session.mjs`** (1612 L); `usability.mjs`'s row anchors re-resolved (`glyphCount` at `:239` · `heroReady` at `:160-172` · the roster docblock `:20-31`); G-KFW4-9's subject set re-pathed. Row 47's include-extension restated **already satisfied at the frontier**. The *"structurally a green empty set"* witness struck and restated on the true banked claim (ME-32's *"referenced by NO script and NO workflow"*; the lane holds **27 specs** whose pass state is unmeasured — G-KFW4-2's falsifier re-worded accordingly). Row 59's *"single hit"* restated (**three hits, one file**; the declaration is single, `:82` is a live write — the fact R-9's total rule must adjudicate). G-KFW4-4's demo-arm inventory gains the bank-measured in-bounds site `SequenceScene.vue` (D25/L-5/C-7 — `computed` imported never used, 43 L), and kf-SequenceScene enters the AUDITED basis.
- **(d) KF-W10 — OP-5 and G-6** (its check D-4/D-5): OP-5 is restated as a **re-measure-at-open** precondition with the current truth recorded (the `docs/tranches/X/keyframes/` tree carries 11 modified specs at this round's clock; sibling sizes read from the worktree, never from `git show` of an older commit). G-6's witness re-measured at the 2026-08-28 INBOX (**56,702 B**; `UNREAD` at `:4/:5/:19/:29/:95` — the fifth is prose inside O-20's disposition cell); the substance restated true (zero UNREAD status rows). §5's blanket "measured read-only 2026-08-28" becomes true of every gate or is scoped.
- **(e) KF-W1** — the INBOX substrate: = R2-1.
- **(f) KF-W7 — the R-11 class's eleventh site** (its check D2): `KF-W7.md:323`'s `build:gh-pages` → **`npm run gh-pages`** (R-11's standing correction text applies verbatim; its enumeration is hereby extended to this site). Round close proves the class dead: `grep -rn 'build:gh-pages' docs/tranches/X/keyframes/waves/` returns correction-prose mentions only.
- **(g) The census-roster companion (the S-1 mechanism)**: every roster or index minted or corrected this round is **re-derived from the BANK**, never adopted from a prior pass's register. Named instances: **KF-W0's C-1.G** — the seven pairs absent by bytes are indexed (kf-SequencePlayhead **L-13/C-9** and **N-11** · kf-CopyButton **K3** · kf-EditorHeader **F2** · kf-EditorShell **L-2/C-2** · kf-MbabbMenu **K-1** · kf-SpringTrace **L-2**), and the declared 65-row denominator is restated at the re-derived count (expected 72 — but the count is re-derived, not computed by arithmetic here); **KF-W0's G-0.7 roster** at the true 53-name altitude (rides R2-17); **KF-W2's census over the full 58** (rides R2-11); **KF-W9's re-enumeration** (rides R2-15).

---

## R2-6 · THE SEVEN DEFECTIVE EXECUTABLE GATE CLAUSES — each re-cut so its command produces its stated reading

Every clause below was executed by a pass-2 seat and failed. Cures are exact; each re-cut clause is re-executed by its repair seat at the frontier before the spec is written (R2-5 binds).

1. **G-W2-2 (KF-W2)** — inert (`-E` has no `\b` → 0 hits), multi-line-blind (6 of 10 importer modules unmatched even under `-P`), and self-conflating (matches kf's own `parseKeyframeSelector` at `frame/compiler.ts:40`). **Re-cut as a two-command witness**: (i) module enumeration — `git grep -n 'from "@mkbabb/value.js/css"' 81a56990 -- src/` with the runtime/type split stated (`import type` filtered); (ii) per-symbol sweep scoped to those files, tolerant of multi-line imports (`git grep -nP` with `(?<![A-Za-z0-9_])symbol(?![A-Za-z0-9_])`, or a per-file read), with **kf's own `parseKeyframeSelector` excluded by name** (the F1 conflation-correction carried into the gate). **Scope**: per F1's own binding (KF-KE-54 — the façade gate must not be `src/`-scoped), the gate gains a demo clause (or a paired G-W2-2b) enumerating the **six demo-side runtime grammar/collector modules** (`KeyframesEditor.vue:123` · `parseAnimationCSS.ts:1-5` · `useSquareDemo.ts` · `useSquareTumble.ts` · `keyframeSelector.ts` · `animationDescriptions.ts:128`); the falsifier is re-armed against both scopes.
2. **G-BASIS (KF-W5)** — the B0 probe returns 0 (`tr -d '\0'` collapses the list). **Re-cut**: `git ls-files -z -- 'src/**/*.ts' | tr '\0' '\n' | grep -c .` → **139** tracked; `find src -type f -name '*.ts' | wc -l` → **145**; the five-basis reconciliation re-run against the corrected B0 with the tracked-vs-untracked delta (145 − 139) named as the thing the table exists to explain.
3. **G-SHIM (KF-W5)** — born GREEN over three nonexistent files. **Struck, or re-cut as a declared regression floor** whose born-state is stated **CURED-AT-FRONTIER (`7e9ddf49`)**; §Bounds' `delete ×3` over `presets/{classic,spring,taxonomy}.ts` is struck (no bytes to delete).
4. **G-DEPCRUISE (KF-W5)** — oracle inverted: the frontier comment honestly *names* `known-violations` to deny it, so `grep -c → 0` deletes a true sentence. **GREEN re-cut to the real property**: (i) no `knownViolations` key in `.dependency-cruiser.cjs`; (ii) no `--known-violations` flag in `package.json`'s `lint` (`depcruise src`, bare — verified); (iii) the honest comment stands (its grep hits are the denial, lawful). The count-oracle is struck. D-9's unsourced `:216-230`/`:233-248` anchors are sourced or struck; `viaOnly` re-anchored at `:148-149`.
5. **G11 (KF-W7)** — the named command returns 13 render-site hits; the stated reading ("no mount") is carried by the `test/` leg alone. **Re-cut**: the witness command becomes `grep -rn "TimelineHoverPreview\|previewCache\|SequenceScrubber" test/` → **0**; the `demo/` leg is struck, or restated separately as the render-site enumeration with its expected 13 hits printed.
6. **G4 (KF-W8)** — the operative command still greps only the umbrella specifier its own falsifier forbids. **Re-cut**: the command enumerates **all four barrel paths by path** (`instrument/transport"` · `instrument/timeline"` · `instrument/keyframes"` · `from "@components/instrument"`), each a clause; the umbrella appears among them, never alone.
7. **G1 clause 2 (KF-W8)** — unsatisfiable as worded (the published surface is itself value.js-bearing). **Restated on the intended predicate**: *"no demo module imports, by any specifier, a LIBRARY-PRIVATE module (one not re-exported by the published surface)"* — with a command (the `@src/` grep of clause 1 plus an enumerated private-module check, or a depcruise reachability clause) and the existing shim-falsifier kept. **Rider (same owner)**: W8's blanket L-19 sentence (`:328`) is made true — G7/G8/G15 gain literal commands, the seven falsifier-less gates gain falsifiers, or the blanket is scoped to name its documentary exceptions.

---

## R2-7 · THE U-2 CLASS — the CITATION IDIOM, and the RE-ANCHOR SEAT that writes LAST

**Idiom (binding program-wide, from this round forward)**: a **cross-spec receipt cites a STABLE ANCHOR** — the destination spec's §-heading, gate id, OP id, or the row's banked id — never a bare line number. A line number may ride parenthetically as a convenience, declared non-load-bearing (e.g. *"KF-W4 §Sequencing, the head-law paragraph (at `:223` at this writing — the heading is the anchor)"*). Intra-spec line citations may stand (they move with their file). A quotation is verbatim at its source **at write time** or it is a paraphrase and says so (M-25).

**The seat**: ONE Re-anchor seat runs **AFTER the last per-wave repair write of round 2** — the round-1 protocol's missing step (11 specs repaired serially, mtimes 12:39→13:15, no closing pass; ~14 receipt families died intra-round). It converts every cross-spec citation in **all 11 specs** to the idiom, re-resolving each at the then-current bytes. **Ordering law: the Re-anchor seat is the round's LAST writer under `waves/`; any later write re-triggers it over the touched files.** (This is the one lawful touch of KF-W3 beyond R2-11.)

**Known dead families to convert** (from the checks; the seat sweeps beyond them):

- `KF-W4.md:193` → the §Sequencing head-law paragraph (at `:223`) — cited by KF-W9 (`:44` OP-6, `:208`) and KF-W10 (`:324`, `:334`). [= UNION dedupe P2-1]
- `KF-W0.md:276` → the canonical-17 packet-roster line (at `:401`) — cited by KF-W9 S-11 (`:226`) and KF-W1 C-14(iii). [= P2-2]
- **KF-W5:215 — a PHANTOM VERBATIM** (*"One rename programme, two waves: do not split mid-cure"*), cited ×4 by KF-W8 (`:62`/`:94`/`:243`/`:266`): re-quoted verbatim from the live KF-W5 D-6 row (at `:219`, the R-14 discharge text) or restated as paraphrase-said-so.
- KF-W8's other four families: `KF-W5.md:269`→`:273` (self-contradicting — `:273` already used at four sites) · `KF-W5.md:240`→`:244` (G-STRUCT) · `KF-W6.md:378`→`:410` (self-contradicting) · `KF-W7.md:313`/`:335`→`:317`/`:340` · `KF-W2.md:221`→`:228`/`:40`; plus D-6's `KF-W6.md:128`→`:136` (the kf-SquareScene-law mint) and D-9's `useToolbarKeyboard.ts:41-49`-as-docblock → the docblock at `:25-28` (the `:41-49` membership-predicate citation at `:145` stands).
- KF-W7's two W6 anchors: `KF-W6.md:250`→`:265` (KAD-15) · `:320`→`:335` (KAD-16/20).
- KF-W1's sibling set: `KF-W4.md:206`→`:242` · `KF-W3.md:209`→`:220` · `KF-W10.md:16`→`:20`, `:50`→`:54`, `:296`→`:332` · `KF-W0.md:269`/`:306` re-located · `INTAKE-ADJUDICATION:144-145`→`:146-149`.
- KF-W2's three: `KF-W7.md:234`→`:238` · `:284`→`:288` (G14) · `KF-W8.md:254`→`:143`/`:289`.
- KF-W5's inherited citation: `KF-W4.md:86-88`→ R-1 heading `:91`, sentence at `:114` (three sites).
- KF-W9's pinpoint: `design-idioms.css:78`-as-selector → `:76` (the `:76-79` range stands).
- KF-W10's two: `KF-W4.md:198`→ the §Sequencing KF.W11 roster bullet (at `:233`), with the **paraphrase-presented-as-quotation restated** under the R2-15 dated-correction idiom (quote the real bulleted roster or mark the paraphrase); `KF-W4.md:193`→`:223` (above).
- KF-W7 D7's hardened quotation (*"MUST be settled BEFORE KF.W7"*): restated as paraphrase, or the two true quotes given (`kf-ChannelControls.md:40` *"before KF.W6-TIMELINE"* · `kf-KeyframeTimeline.md:114` *"before/within KF.W7"*).

---

## R2-8 · HARD ESCAPE 1 — kf-SequenceScrubber **D·D-7** · ownerWave **KF-W6**

Routed by the record's bolded terminal manifest (`kf-SequenceScrubber.md:133`); 0 hits in spec AND CARRY; pass 1 recorded the record as routing nothing, so no ruling existed. **Cure**: booked as a **W6-H material-register row** (or beside KF-SCR-2 at W6-G — same surface), quoting the banked body (`:52` — the dark-substrate phosphor `text-shadow` ungated into a light-default theme, 12–16px tabular figures at ~4.6:1, chain verified to `--accent-kf`'s `light-dark()`; *"Percept → SS-13"* tail carried). **The row inherits the KF-AV-28 standing rider by id** (`kf-SequenceScrubber.md:36` — governed surface; the clause the spec already carries verbatim at L90 governs it). The CARRY gains an E-3 addendum row — the spec's owed-addendum list (L358) gains item **(vii)**. §1's census restates (expected 392 routed / 392 booked / 0 escaped — re-derived, not computed). `SequenceScrubber.vue` is already in §Bounds; no bounds motion.

---

## R2-9 · HARD ESCAPE 2 — kf-AnimationControlsGroup **D-1**, an ADJUDICATED BLOCKER · ownerWave **KF-W7**

`kf-AnimationControlsGroup.md:42`: the document-singleton portal sink, *"NO-WAVE-OWNER, must be settled **before** KF.W6-TIMELINE"* — and KF-W7's own taxonomy rule (`:325`) makes KF.W6-TIMELINE references **this wave's hard-ordering locks**. Exactly two such rows exist in the corpus; W7 books one (ChannelControls, OP-1) and routes **C-6's cure through AnimationControlsGroup itself** (`:108`, restated in G2's assertion at `:242`) while the other stays unbooked.

**Cure (exact), owner KF-W7**:

1. A new **hard precondition row beside OP-1** (OP-0 or OP-1a): *"kf-AnimationControlsGroup **D-1** (ADJUDICATED BLOCKER, restored): the document-singleton portal sink — one sink, N Teleport sources; expanding the timeline stacks every painting channel's KeyframeTimeline into one clipped box — must be **SETTLED before this wave's timeline-cluster work**. The cure is NO-WAVE-OWNER; the SETTLEMENT (a design ruling: keep or replace the portal architecture, with its banked scope enumeration — cube 3 · amiga 3 · spring 2, one shared `superKey` bucket flipping all instances) is this wave's intake, exactly as OP-1's `:key` row is."*
2. §Sequencing's *"Hard ordering (blocking, in order)"* list **opens with it** (it is one rung above OP-1's peers at the bank).
3. **C-6's cure is CONDITIONED**: the *"drive the scene's single engine through AnimationControlsGroup"* arm at `:108` and G2's `:242` restatement gain the clause *"conditioned on the D-1 settlement — no cure routed through AnimationControlsGroup lands before the portal ruling."*
4. §1's census row Q flips E→B; the taxonomy sentence at `:325` extends its own rule to `:42`; the tally restates **150 routed · 144 carried + 6 excluded · 0 escaped** (and the check's D3 composition error — "145 + 3" — is corrected in the same motion).

---

## R2-10 · HARD ESCAPES 3–5 — kf-AmigaScene **D-2** + **MISSED-A**, kf-HeroAurora **KF-HA-10** · ownerWave **KF-W9**

All three are witness/measurement rows on the wave's own thesis (a real mobile Safari); none touches W9's measure-only posture (R-9a stands — no cure arm returns).

**Cure (exact), owner KF-W9**:

1. **§F gains kf-AmigaScene D-2** — the bare-`<canvas>` AT row (no role, name, tabindex, keydown, or fallback content; banked at `:42`, routed twice incl. the `:158` witness list) — quoted from the bank, witness-held, cure NO-WAVE-OWNER (scene-repair packet).
2. **§F gains kf-AmigaScene MISSED-A** — no touch affordance for the scene's primary interaction (`cursor: grab` the whole discoverability story; the gesture legend deleted at T.A10); with D-2, *"zero discoverable interaction on a phone-filling canvas."* Same treatment.
3. **§G/§H gains kf-HeroAurora KF-HA-10** — the frame-cost measurement row (the `motion: "drifting"` nonzero drift quadruple; a whole-session full-viewport GPU loop under the least-visible layer). The **measurement is W9's; the owner ruling (is the drift register blessed?) is KF.W6's** — the cross-edge named at the row. **Conditional sibling correction (KF-W6)**: the repair seat verifies the W6 ruling-half is booked (`grep 'KF-HA-10'` over KF-W6.md + CARRY); if absent, W6 books the owner-ruling half in the same motion.
4. §F/§H counts, the S-12 *"≥8 records"* claim, and the 76-bullet ledger are **re-enumerated, not incremented** (R2-5(g)); the pass-1 mis-booking of KF-HA-10 (*"all have residue homes. BOOKED."* — refuted by the record's own P-1..P-8) is recorded in the R2-15 correction block, E-3, without editing the PASS-1 file.

---

## R2-11 · HARD ESCAPE 6 — kf-EasingTarget **KF-ET-28** · ownerWave **KF-W2**, and the F0 register COMPLETED over the 58 (UNION P2-3)

**Cure (exact), owner KF-W2**:

1. **KF-ET-28 booked** in §Carry F0's posture registry (banked `kf-EasingTarget.md:70`: *"value.js's Result API reaches the render path through a throwing adapter with no boundary … the `?? ""` decoy at `:288` guarantees the throw it appears to prevent"* — NO-WAVE-OWNER, degrade-don't-detonate posture; skip-not-throw on missing attribute), with the do-not-re-probe lock.
2. **The register that R-1b made census-of-record is COMPLETED over the full 58** — the mechanism behind the escape (its check D-5/D-8, deduped at UNION P2-3): the fifteen further routed cells enter by id — the nine R1 verified negatives (**X1** CPW S-9/RR-2 · **X2** EditorHeader C S-A · **X3** MatrixEditor sup-7 · **X4** PlaybackRibbon C-neg · **X5** SequencePlayhead sup-7 · **X6** SequenceScene sup-4 · **X7** SpringHeatmap sup-7 · **X8** KF-SST-36/C-13 · **X9** SpringTrace S-A), the four consumer-declared posture cells with **KAD-10's busy-state limb restored** beside KAD-9 (**X10** TimelineCaret L-15 · **X11** KeyframeTimeline L-11 · **X12** KeyframeTimeline D-15 · **X13** KAD-10 — closing the one-ended G14 edge `KF-W7 §Gates G14` declares), and the positive exemplar (**X15** ChannelControls SUP-3 `trySetOption`, the corpus's only §7.5-measured cell). G-W2-5's *"recorded as negatives so no later seat re-probes it"* becomes true as written; G-W2-1's registry enumeration restates its floor.
3. **The AUDITED basis is reconciled to 58** (the 21 previously-unaudited records named; the census restated — expected **62 routed / 62 booked / 0 escaped**, re-derived per R2-5(g)). **`parseAnimationCSS.ts` enters the ingress census of record** as a READ-ONLY census cell (two collector entries `:36`/`:41` · the deep `@src/` import `:7` · the `:32-34` THROW posture · the `:22-24` single-grammar-authority docblock), with the deep-import limb cross-reffed to KF.W8's G1 (already in its seven-statement enumeration — a declared edge, not a new owner); the "8 demo-side seams" figure reconciled to the measured six modules.

**Sibling correction (KF-W3 — the ONE lawful W3 touch)**: §Excluded E-15 gains **one surplus-negatives sub-line** booking the eight banked R1 negatives its own D-02 names (CSSPasteDialog **C-4** · LayerConfigPanel **LP-24** · KAD **K-5** · OrbitalDrag **#3** · SPF **★S-7** · THP **S+3/S-d** · AnimatedText **C-10** · SpringTrace **S-A**) as *surplus, owner KF.W2 (F0)* — the exact shape X-16/X-17 already use. Nothing else in KF-W3 moves; its remaining pass-2 notes stay at the spec's own discretion under its CLEAN verdict.

---

## R2-12 · DOUBLE-HOMING 1 — `test/demo/instrument/` · the glob dies; PATHS are enumerated at both ends (owners **KF-W7** + **KF-W5**)

The program's own law (*"paths are enumerated, never globbed by shared prefix"* — the X.P.W3 §4a lesson, applied to W5 by R-9b item 4) governs. **Cure at KF-W7**: the §Bounds `test/demo/instrument/**` create-glob (`:62`) is **struck and replaced by literal created-file paths** — the W7 seat names its G11 mount-test fixtures by full filename at authoring (the KF-W5 `:229` pathed-fixture idiom is the template); the **nine tracked files in the directory are declared READ-ONLY witnesses by name** (incl. `timeline-undo.test.ts`, this spec's own G11 witness); §Disjointness names KF-W5's `test/demo/instrument/highlight-css-roundtrip.test.ts` and asserts filename disjointness; §Sequencing's KF.W5 cross-edge declares the shared directory. **Cure at KF-W5** (reciprocal): its §Disjointness names KF-W7's enumerated fixture files and restates the shared-directory/disjoint-filenames contract. Neither wave owns the directory; each owns exactly its named files.

---

## R2-13 · DOUBLE-HOMING 2 — the `proof:brittleness` residue family · the banked routing WINS: family owner **KF.W6**; KF-W4's gate carves it out BY NAME

Measured at the frontier: `proof:brittleness` lives at `demo/styles/style.css:38` · `demo/styles/layout.css:152` · `scripts/lib/demo-driver.mjs:69` (a comment mirror). `layout.css` is one of KF-W4's eight targets and a §Bounds file — R-8.2's partition leaks at the bytes.

**Cure at KF-W4**: §Excluded 17 is re-cut to enumerate the **measured three frontier sites** (not the bank's `style.css:38-39` alone), family → **KF.W6** whole (the banked routing: *"KF.W6 + KF.W0 rider"*). **G-KFW4-8's denominator carves the `layout.css:152` brittleness line OUT by name**, and the gate's falsifier is re-worded so the carve is declared, not silent: *"the `proof:brittleness` citations at the enumerated targets are OUT of this gate's denominator, owned by KF.W6 per §Excluded 17; the gate fails if it cures a brittleness byte or if the carve is undeclared."* **Cure at KF-W6** (sibling): its KF-AX-31 booking gains the three-site enumeration (spec edit; CARRY by addendum if touched). **KF-W0 rider**: G-0.7's roster records `proof:brittleness` among the dead names with disposition *"cure → KF.W6"* (rides R2-17).

---

## R2-14 · DOUBLE-HOMING 3 — the KfPillTabs limb labels · the BANK wins; the R-6 manifest is corrected and W6's attribution follows (owners **KF-W4** + **KF-W6**)

`kf-KfPillTabs.md:43` is **C:C-1** — the MAJOR fork-rationale-void row (folds to CC `C-6`, booked at W6-A L102); the header mangle is its **own banked id at `:71`**; the `:4-5` phantom consumers are **L:D-8**'s arm. The R-6 manifest (KF-W4 rows 7–8) labels C:C-1 as the header mangle; W6 attributes the phantom-consumer riders to C:C-1. Both ends contradict the bank.

**Cure, one motion, both files**: KF-W4's R-6 manifest rows 7–8 are **re-labelled from the bank** — each limb keyed to its banked id AND source line (the header mangle under its own id ⟨`:71`⟩ · the phantom consumers under **L:D-8** ⟨`:4-5` arm⟩ · **C:C-1** = the fork-rationale-void ⟨`:43`⟩, its **MAJOR grade stated**); KF-W6's `:145-146` attributions follow the corrected manifest; the MAJOR-row-re-booked-at-INFO-under-the-wrong-limb is corrected at its booking. The manifest remains the routing authority (R-6's law); the bank remains the label authority — that sentence is added to the manifest head.

---

## R2-15 · KF-W9's FALSE HEADLINE CLOSURE CLAIM — struck and restated under the DATED-CORRECTION IDIOM · ownerWave **KF-W9**

The claim at `KF-W9.md:88` — *"no routed id now escapes all four mechanisms"* — was authored **this round** (repair round 1) and is **false at the bytes** (three escapes, one of them affirmatively mis-booked by pass 1). It is **STRUCK and restated with a dated correction block** (the idiom W10's §2b re-anchors already model — the superseded spelling quoted, struck by name, the true sentence beside it, dated):

> **CORRECTED 2026-08-28 (repair round 2)**: the round-1 sentence *"no routed id now escapes all four mechanisms"* was an inference from PASS-1's ten-id list, never a re-enumeration, and is struck. A fresh 58-record enumeration (PASS-2) found three escapes — kf-AmigaScene **D-2**, kf-AmigaScene **MISSED-A**, kf-HeroAurora **KF-HA-10** — now booked (RULINGS-2 R2-10). PASS-1's booking of KF-HA-10 (*"all have residue homes"*) is refuted on the record: the residue is P-1..P-8 and names no KF-HA-10. Restated true: every routed (record, id) pair in the PASS-2 enumeration (147) is discharged by one of the four mechanisms, **re-derived at this correction's date**.

**Same directive, the counting family** (its check D-5/D-6/D-7/D-11/D-12 — the S-3 class): the escalation-register dependents restate **thirteen** (`:24`, `:211`); the repair ledger restates **"17 cured · 1 declined · 1 no-defect = 19"**; the dissent roster is **re-enumerated** (the body now carries eleven-plus-one — count, don't assert); the 563 cell is re-derived (expected 565); §F's "ten records" restated as its true composition. Every figure re-counted at the bytes, per R2-5.

---

## R2-16 · THE ONE UNAPPLIED ROUND-1 RULING — **R-8.4 (partial)** at KF-W4 · re-directed with the literal paths supplied

R-8.4 ordered §Bounds rows 51/59/64/65/66/71 to carry literal paths; the repair executed three and refused three on the ground *"no record banks its directory."* **The refusal is OVERRULED** — the same file resolves frontier coordinates by exactly this class of read-only command elsewhere (the nine `scripts/gates/` scripts, `ci.yml`, `env.d.ts`, `tsconfig.lib.json`, `package.json:77`), and round 1's law stands: a ruling outranks any local finding it meets. **Cure (exact), owner KF-W4** — the three rows carry these literals, each verified at `81a56990` by the pass-2 seat:

- row **59** → `src/animation/engine/css/animation.ts` (decl `:54`, assign `:82`) — with the row's arithmetic corrected per R2-5(c): the resolving command returns **three hits, one file**; the declaration is single; `:82` is a live write, the exact fact R-9's total rule adjudicates;
- row **64** → `test/orchestration/orchestration-api.test.ts`;
- row **65** → `test/demo/instrument/resize-tracks.test.ts` (the row's own three-directories inference, now written as its conclusion; the path also enters R2-12's read-only witness enumeration at W7's end).

---

## R2-17 · KF-W0's BOUNDS CONTRADICTION — G-0.7's GREEN re-cut so it is satisfiable inside the wave's own §Bounds; the roster restated at the true 53-name altitude · ownerWave **KF-W0**

G-0.7's GREEN demands a strike over sites in keyframes `demo/**` — a surface W0's §Bounds forbids and its Triumvirate Dispatch calls wave-invalidating. The wave never chooses between the readings (its check D-1); and the roster's denominator is ≤11 names against a measured **54 distinct `proof:` names in `demo/` at the frontier, 1 runnable (`proof:publish`), 53 dead, 116 prose hits / 51 files** (its check D-2).

**Cure (exact), owner KF-W0**:

1. **G-0.7's GREEN is re-cut to two clauses, both docs-only**: (i) the authoritative dated roster artifact (`GATE-ROSTER.md`, already in unit KF.W0.e's Files list) enumerates the **measured 53 dead names** (derived at `81a56990` by the check's own grep, re-run at authoring per R2-5), each with a per-name disposition; (ii) **every name is DISPOSED — struck-in-owner-wave or routed-with-named-owner — and W0 executes NO strike**: sites in KF.W6-bounded style files route to KF.W6 (incl. `proof:brittleness` per R2-13); the R-8 residue families ride their packets per R-8.2's table; `proof:styling-idioms`/`proof:font-census`-class prose sites route to the wave or packet whose §Bounds carries the file; any name with no owner gets a **loud NO-WAVE-OWNER roster line** (routed, never dropped). Unit KF.W0.e's charge is re-worded from *"strike"* to *"roster + route."* G-0.7's GREEN is thereby reachable by writing `docs/**` only.
2. **D-13's seven names are ENUMERATED in the roster** (the record states only the count; the roster derives the seven from the 53-name sweep by prefix and record attribution — a GREEN keyed to an unenumerated set is undecidable and dies here). [check D-5]
3. **The `proof:demo-no-oversize` figure gains its scope word** — 16×13 is `demo/`-scoped; the repo-wide surface (121 hits / 70 files, 57 under `docs/`) is declared **loudly out-of-scope** (keyframes `docs/**` is outside every X·KF wave's bounds), so the record's "the fold owns the sweep" lock does not reproduce one directory up unanswered. [check D-6]

---

## §END · PER-WAVE DIRECTIVE MAP (what each round-2 repair seat executes, verbatim, from the rulings above)

| seat | n | directives |
|---|--:|---|
| **KF-W0** | 3 | R2-5(g) (C-1.G's seven pairs indexed from the bank; denominator re-derived) · R2-13 (roster rider: `proof:brittleness` → KF.W6) · R2-17 (G-0.7 GREEN re-cut, 53-name roster + route-not-strike, D-13's seven enumerated, demo-scope word) |
| **KF-W1** | 1 | R2-1 (O-21 mint at every site + mint law + G-KF1-7/G-KF1-8 witnesses re-cut + the three INBOX anchors re-keyed + the C-9 precedent sentence + the SS-2-CARRY strike per R-12) |
| **KF-W2** | 3 | R2-5(b) (`resolve/conditional.ts` re-path, seventh drift row) · R2-6.1 (G-W2-2 two-command re-cut + demo clause) · R2-11 (KF-ET-28 booked; F0 completed over the 58; AUDITED = 58; `parseAnimationCSS.ts` in the census of record) |
| **KF-W3** | 1 | R2-11 sibling line ONLY (the E-15 surplus-negatives sub-line, owner KF.W2/F0). Nothing else moves; the spec is CLEAN. |
| **KF-W4** | 6 | R2-3 (G-KFW4-1 composes with `proof:structure`; no-silent-deletion falsifier) · R2-4 (EditorShell struck; `DESIGN.md:251` in; EIGHT targets; R-8.1 partition over 19 names; KF-CO-33 routed) · R2-5(c) (the nine anchors re-resolved; empty-set witness restated; SequenceScene site + AUDITED) · R2-13 (§Excluded 17 three-site enumeration; the G-KFW4-8 declared carve) · R2-14 (R-6 manifest rows 7–8 re-labelled from the bank) · R2-16 (rows 59/64/65 literal paths) |
| **KF-W5** | 3 | R2-5(a) (Arm D re-measured at `81a56990`; CURED-AT-FRONTIER bookings; five paths re-pathed; `internal/yield-batch.ts` struck; G-STRUCT re-measured + relayed to W8 G13) · R2-6.2–4 (G-BASIS probe fixed at 139/145; G-SHIM struck-or-floor; G-DEPCRUISE oracle re-cut + `viaOnly` re-anchored) · R2-12 (reciprocal §Disjointness naming W7's enumerated fixtures) |
| **KF-W6** | 4 | R2-8 (D·D-7 booked at W6-H with the KF-AV-28 rider; CARRY addendum (vii); census re-derived) · R2-10 (verify-and-book-if-absent the KF-HA-10 owner-ruling half) · R2-13 (KF-AX-31 booking gains the three-site enumeration) · R2-14 (`:145-146` attributions follow the corrected manifest) — plus the standing G-W6-9/G-W6-10 false-witness cures its own check orders (FadingScroll two live mounts; SpringSidebar 12/8), which R2-5's law binds it to re-measure |
| **KF-W7** | 4 | R2-5(f) (`:323` → `npm run gh-pages`) · R2-6.5 (G11 re-cut to the `test/` leg) · R2-9 (ACG D-1 as the opening hard precondition; C-6 conditioned; census 150/144+6/0) · R2-12 (create-glob → literal fixture paths; nine tracked files read-only by name; disjointness both ends) |
| **KF-W8** | 2 | R2-5(a) receipt (G13's subordination sentence updated to G-STRUCT's re-measured figure — reciprocal, one motion with W5) · R2-6.6–7 (G4 enumerates the four barrel paths; G1.2 restated on library-private + command; the L-19 blanket made true or scoped) |
| **KF-W9** | 2 | R2-10 (D-2 + MISSED-A into §F; KF-HA-10 into §G/§H; counts re-enumerated) · R2-15 (the headline struck + dated correction; the counting family recounted at the bytes) |
| **KF-W10** | 2 | R2-2 (C-3/C-4/C-5 carried as record-only rows; tally 9/9/0; G-1 stays 15) · R2-5(d) (OP-5 re-measure-at-open; G-6 measured at the 08-28 INBOX) |
| **RE-ANCHOR seat** | 1 | R2-7 — runs LAST, after every seat above; converts every cross-spec citation in all 11 specs to the stable-anchor idiom; the ~14 enumerated dead families are the floor, not the ceiling |

**Frontier of record for every keyframes-side re-measurement: `origin/master 81a56990736ced5b5edde0b84c527680ac7689b1`.** Local `8281638c` remains DISQUALIFIED. Value-side re-measurements bind to the current tree bytes at the executing seat's clock.

*Cross-wave rulings seat, repair round 2. Sole write = this file. Nothing here stamps any wave; E-3 holds — registry corrections land as addenda under original ids; spec repairs execute at the per-wave seats; the PASS-1/PASS-2 check files are never edited.*
