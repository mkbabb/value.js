# KF-W3 — FRESH ADVERSARIAL SPEC CHECK (L-18/L-20, PASS 2)

**Spec under trial**: `docs/tranches/X/keyframes/waves/KF-W3.md` (86 041 B, 261 lines — grown from
pass 1's 57 164 B / 232 lines).
**Corpus authority**: the 58 `kf-*.md` records in `docs/tranches/V/megatranche/registry/adjudicated/`
(count confirmed: `ls kf-*.md | wc -l` → **58**). Sole in-tree carry:
`docs/tranches/X/keyframes/carry/KF-W6-CARRY.md` (199 055 B) — `ls` over `carry/` returns **that file only**.
**Method**: ID-KEYED CENSUS, re-derived from zero. This seat did **not** inherit pass 1's denominator:
the routed set was rebuilt by independent grep over all 58 records in every markup — dotted
`KF.W2/W3` forms, bare `KF.W3`, the `KF.W3-SHIM` spelling, and parser-lane prose
(`parser lane` · `parser wave(s)` · `parser ingress` · `parser consumption` · the `megatranche R1`
fold forms) — then each id tested for carriage by `grep -cF` against the spec.
**Seat instruments (all read-only)**: `grep`/`sed` over the 58 records, `COHESION.md`,
`lane-docs.md`, `INBOX.md`, `package.json`, `src/css/grammar.ts`; `git show origin/master:<path>`,
`git grep`, `git ls-tree`, `git cat-file -e`, `git rev-list` in `/Users/mkbabb/Programming/keyframes.js`
(ref `81a56990`). **No product source was opened for writing. This file is the only write.**

**VERDICT: CLEAN** (2 MAJOR + 2 MINOR + 1 INFO, none census-fatal; both pass-1 BLOCKERs CURED).

---

## §1 CENSUS TOTALS

| bucket | count |
|---|---|
| **routedTotal** (distinct registry row-ids routing to this wave, all markups) | **24** |
| **BOOKED** (§Carry fold-identity · §Excluded E-1 disposal · §Excluded E-15 exclusion-with-named-owner) | **24** |
| **ESCAPED** (no row, no fold-identity, no exclusion line) | **0** |

Carriage **24 / 24 = 100 %** (pass 1: 9 / 19 = 47.4 %). Plus **2 surplus** cross-declarations
(X-16 · X-17) correctly booked *outside* the routed arithmetic per R-1b.

**Independent derivation of the denominator** (this seat, not inherited). The 19 canonical routed ids:
kf-AmigaScene:150 · kf-App:111 · kf-CopyButton:81 · kf-CSSCodeEditor:46/:134 · kf-KeyframeCardList:122 ·
kf-KeyframeTimeline:48/:112 · kf-KeyframesStringControls:63 · kf-StartingStyleTarget:80/:139 ·
kf-KeyframeCard:101 · kf-EasingTarget:77 · kf-CubeAxisLines:108 · kf-EditorShell:77 ·
kf-KeyframesEditor:99 · kf-TimelineTrack:18/:130 · kf-ChannelOptions:126/:155 · kf-DemoGlobalChrome:77 ·
kf-ChannelControls:126 · kf-CubeTarget:76 · kf-SquareScene:132. The 5 `KF.W3-SHIM` rows:
kf-ChannelControls:79 · kf-AnimationControlsGroup:74 · kf-KfPillTabs:58 · kf-KfPillTabs:70 ·
kf-DemoGlobalChrome:70. **19 + 5 = 24** — the same total pass 1 reached, reached by a different route.

---

## §2 BOOKED — id for id (24)

### §2.A §Carry fold-identities (9)

| # | record : line | row id | spec site |
|---|---|---|---|
| B-01 | `kf-AmigaScene.md:150` | **S+2 / superlative 6** | §Carry **C-1** ownership cell, quoted verbatim (*"the R1 row itself stays owned by KF.W3"*) |
| B-02 | `kf-CSSCodeEditor.md:46, :134` | **KF-CE-12 · C-7** | §Carry **C-1** rider **(a)**; §B.3 row 6; G-KF3-7 falsifier (with the `:116`-anchor re-resolution) |
| B-03 | `kf-KeyframeTimeline.md:48, :112` | **C-7** | §Carry **C-1** rider **(b)**, KF.W7 posture split preserved; §B.3 row 8; §Excluded E-5 + L-9 |
| B-04 | `kf-KeyframeCardList.md:122` | **Axis C · S-C** (R1 note) | §Carry **C-1** rider **(d)** |
| B-05 | `kf-KeyframesStringControls.md:63` | **C-2** | §Carry **C-1** rider **(c)**; §B.3 row 7; G-KF3-4 falsifier; §Excluded E-5 + E-14 |
| B-06 | `kf-CopyButton.md:81` | **KF-CB-33 · C-14** | §Carry **C-2** whole, with EE-01/DP2-02 and the two-channel edict |
| B-07 | `kf-StartingStyleTarget.md:80, :139` | **KF-SST-36 · C-13** | §Carry **C-2** provenance, *"Not re-booked"* fold law preserved |
| B-08 | `kf-KeyframeCard.md:101` | **KF-KC-53 · C-7** | §Carry **C-2** provenance |
| B-09 | `kf-EasingTarget.md:77` | **KF-ET-32** (KF.W3 leg) | §Carry **C-3**, METHOD-BINDING → G-KF3-7's call-graph predicate |

### §2.B §Excluded E-1 — the `KF.W3-SHIM` taxonomy disposal (5)

`kf-ChannelControls:79` L-15/C-10 · `kf-AnimationControlsGroup:74` L-5 (evidence-correction rider intact) ·
`kf-KfPillTabs:58` N-KPT-2 · `kf-KfPillTabs:70` L:D-8/C:C-6 (repoint lock intact) ·
`kf-DemoGlobalChrome:70` M-L3 — the last now carried with its **terminal-disposition correction**
(NO-WAVE-OWNER, `KF.W3-SHIM` read as a cross-reference to a law, not a home). Pass-1 **D-09 CURED**.
Squaring re-checked: `KF-W6-CARRY.md` receives the family (`F-5`, `KF.W3-SHIM`, `N-KPT-2`, `M-L3`,
`L-15`, `rainbow` all present). No orphaning.

### §2.C §Excluded E-15 — exclusions-with-named-owner (owner KF.W2), counted in G-KF3-7's denominator (10 + 2 surplus)

X-06 `kf-App:111` KF-APP-56 · X-07 `kf-EditorShell:77` C-19 · X-08 `kf-CubeAxisLines:108` C·§4 ·
X-09 `kf-KeyframesEditor:99` KF-KE-54 · X-10 `kf-TimelineTrack:18/:130` SUP-3 ·
X-11 `kf-ChannelOptions:126/:155` C·S-4 · X-12 `kf-ChannelControls:126` C's L-1 ·
X-13 `kf-CubeTarget:76` challenge-C S-1 · X-14 `kf-DemoGlobalChrome:77` C-10 ·
X-15 `kf-SquareScene:132` C·S-C — **all ten pass-1 escapes now on the page**, each with its banked
routing bytes quoted and its REACHABLE/UNREACHABLE role stated. **Surplus (not in the 24)**:
X-16 `kf-KeyboardShortcutsModal:78` · X-17 `kf-SpringTrace:56` — exactly R-1b's #15/#16, cross-declared
so neither parser wave assumes the other holds the negatives. Verbatim fidelity spot-checked at the
bank for X-11, X-12, X-13, X-15, X-17: **byte-faithful**, including X-13's near-miss clause
(`"ease-out-back"` → `parseTimingFunction` rejects → registry resolves) that pass 1 quoted short.

**Arithmetic closes**: 9 + 5 + 10 = 24 = 19 canonical + 5 SHIM. **0 unaccounted.** The spec states the
routed arithmetic and the surplus arithmetic separately, so neither can be read off the other — correct.

---

## §3 AXIS FINDINGS

### Axis 2 — NO INVENTION / M-25 depth: **HOLDS**
Severities are the bank's throughout; no re-grading. **C-5's second reachability conjunct is RESTORED
verbatim** — diffed against `kf-SquareScene.md:53` character for character, including
*"and via any `--rainbow-*` re-author in a syntax value.js 4.0.0 rejects"* (pass-1 **D-03 CURED**).
Cure-shape locks travel: D-27's NO-WAVE-OWNER + `RAFPlayback._run` engine-seam rider; L-5's repoint;
L:D-8's *"NOT at the `.vue`"*; K-6's kill-of-a-confirm. Dissents preserved at E-14 (KSC C-2 BLOCKER→MAJOR,
KF-CB-3, kf-ACG L-5 as evidence-quality). **KF-AV-28 carried by id** — `grep -cF 'KF-AV-28'` → **4**
(L-9 verbatim per R-10, §Excluded E-5, cross-edge KF.W7) and the clause's three banked anchors
(`kf-AnimationVisualizer.md:35` · `kf-PlaybackRibbon.md:36` · `kf-SequenceScrubber.md:36`) each
**verified to carry the standing rider** — pass-1 **D-13 CURED**.

### Axis 3 — GATES born-RED with real witnesses: **HOLDS; no phantom script, no phantom path**
Every cited command and path re-executed by this seat:

| spec claim | measurement | result |
|---|---|---|
| probe on disk, 3476 B, header self-states packed-artifact/L-12 + *"RED today: 324 throws / 1548 calls … src/css/grammar.ts:181"* | `ls -l` + `head -14` | **EXACT** |
| `RELEASE-CONDITION.md` / `RELEASE-PACKET.md` absent | `ls` | **both *No such file*** |
| `grep -c 'RC-P' INBOX.md` → 0 · `grep -rn 'RC-P' docs/tranches/X/fourier/` → 0 | re-run | **0 / 0** |
| `docs/tranches/X/keyframes/evidence/` absent | `ls` | ***No such file*** |
| value.js `package.json` = `4.0.0` | `grep` | **EXACT** |
| `COHESION.md §0b ADDENDUM 2026-08-28` cures KF.W3 → SS-1, names `KF-W3.md`, banked `b6e09ed4` | `grep -n` + `git log -1 b6e09ed4` | **EXISTS; commit real** |
| keyframes.js `origin/master` `81a56990`; local HEAD `8281638c` 1 ahead / 41 behind | `git rev-parse` + `git rev-list --left-right --count` | **EXACT** |
| `package.json:70` `"@mkbabb/value.js": "4.0.0"`; `:77` `"@mkbabb/glass-ui": "7.0.0"` | `git show \| sed -n '69,71p;76,78p'` | **EXACT** |
| `css-text.ts` `:17` `:41` `:54`, recursion `:43 :48`, callers `:66 :83 :89` | `sed` at all eight | **EXACT, all eight** |
| the three PATH corrections (`emit/format/options.ts`, `emit/backward/backward.ts`, `easing/registry.ts`) | `git cat-file -e` on both spellings | **corrected paths OK; all three stale paths MISS** |
| easing fence: `DIRECT_NAMES :18-28` (`smoothStep3` `:26`, `easeInBounce` `:27`), `registryNames :30-34` w/ `:31 ...Object.keys(bezierPresets),`, module-top-level `timingFunctionEntries :37-48`, throw `:43-46`, `parseTimingFunction` `:131` | `sed -n '18,48p;131p'` | **EXACT, line for line; boot-evaluation confirmed** |
| `orchestration-api.test.ts:142-148`, TWO phantoms under ONE false comment | `sed -n '142,148p'` | **EXACT — `:145 easeOutCubic`, `:146 bounceInEase`, `:147 not-a-real-easing`** (pass-1 **D-12 CURED**) |
| `CopyButton.vue:42 timingFunction: "easeInBounce",` | `sed -n '42p'` | **EXACT** |
| `emit/index.ts:53 export { cssIdent } from "./backward";` · `scroll/grammar.ts` doc `:138-142` + `:143` | `sed` | **EXACT** |
| fixtures = 14 `.css` + `manifest.json`, all 14 names | `git ls-tree` → 15 entries | **EXACT, all 14 names match** |
| `test/fixtures/compile/{scroll-driven,multi-color-scroll}.css` | `git ls-tree` | **both present** |
| `compile-roundtrip.test.ts:424-428` *"Authored GATE-FIRST"* | `sed` | **EXACT** |
| six-hit demo grep = 3 imports + 3 calls | `git grep -n` | **EXACT — six hits, three files** |
| `keyframeSelector.ts` is `parseKeyframeSelector`; `requireKeyframeSelector :14-21` throws at `:18`; callers `useKeyframeOps.ts:174`, `timelineEngine.ts:83` | `sed` + `git grep` | **EXACT (mis-classification correction CONFIRMED)** |
| `animationDescriptions.ts:76` is `parseTimingFunction` (wrong organ) | `sed` | **EXACT** |
| channel-(ii) seed set = `registry.ts:131` · `format/options.ts:104` · `animationDescriptions.ts:76` | `git grep -n parseTimingFunction origin/master` | **EXHAUSTIVE — exactly three call sites in the tree**; kf-ChannelOptions C·S-4 / kf-TFP-20's `timingFunctionState` resolves to `animationDescriptions.ts:68`, whose `:76` call is already booked. **No fourth site escapes.** |
| `ci.yml:53-55` `demo-correctness` nightly/dispatch-only | `sed -n '50,58p'` | **EXACT** |
| prose phantoms `easing.ts:44`, `waapi/eligibility.ts:169` | `sed` | **both EXACT** |
| value.js `src/css/grammar.ts:181` verbatim | `sed -n '179,183p'` | **EXACT** |
| `lane-docs.md:373` cell = *"DECLARED; no keyframes acknowledgement on record"* | `sed -n '373p'` | **EXACT** |
| `test/compile/frame-compiler.test.ts` unrostered 17th sibling | `git cat-file -e` | **EXISTS** |

**L-19 contrivance presumption: REBUTTED again.** No proof-script authored; every gate is a command
against a pinned artifact or a physical coordinate; the R-E EXECUTE-NO-WRITE discipline on the probe
holds. **G-KF3-1 is now genuinely born-RED** — its three conjuncts include one dischargeable only by
another seat's file (the fourier half) and one only by a pasted transcript, so it can no longer be
green at the instant of authoring (pass-1 **D-06 CURED**). **G-KF3-8/G-KF3-9 are re-labelled DECLARED
FLOOR in place, numbers retained; §Gates counts SEVEN close gates** (pass-1 **D-07 CURED**).

### Axis 4 — E-3 + STATUS: **CLEAN**
`grep -n 'VERIFIED' KF-W3.md` → **one hit, line 26**, reading `| VERIFIED | NO | stamped only at X·KF
close (KF.W10); no wave stamps VERIFIED at its own close |`. **Zero VERIFIED stamps.**
`**Status**: **planned**` at `:5`. `IMPLEMENTED | NO`. Every keyframes.js anchor is declared
re-resolved read-only via `git show origin/master:<path>` — corroborated by the byte-exactness above,
all of which this seat reproduced **from git alone, with no working-tree file opened**. No product
byte is written. §B.2's KF-W3.md row now reads **modify** with the staleness named (pass-1 **D-11 CURED**).

### Axis 5 — POSTURE: **4 of 4 HOLD**
- **KF.W4 is the declared sequencing head** — HELD: OP-6 hard, L-3 (*"Ordering, not advice"*),
  E-3, E-4, cross-edge with three blocking legs, and G-KF3-9's fail-honest declaration.
- **KF.W3 GATED via PLAW-BIND, never scheduled** — HELD: title `(GATED, never scheduled)`; the
  opening condition verbatim and **by predicate name** at `:9`; L-1; the X·V cross-edge
  (*"There is no direct parse-that → keyframes edge, and none may be created"*). Corroborated by
  `COHESION.md §0b`: *"The wave's GATED posture is unchanged."*
- **KF.W6 squares** — HELD (§2.B).
- **KF.W7 carries the KF-AV-28 rider** — **NOW HELD** (Axis 2). Pass-1's only NOT-HELD posture is cured.

### Pass-1 rulings addressed to KF-W3 — spot-check (7 directives, §END map)
**R-7a** APPLIED: E-12 struck and re-founded on the bytes, *"K-6 kills only the claim that a TOKEN can
produce the empty-args R1 degenerate"*, the `!ok` throw arm *"MAY be asserted RED"*, C-4/C-5 read as a
pair — the pair reading is stated in **four** places (C-4, C-5, E-12, G-KF3-7) and they agree. Pass-1
**D-01 (BLOCKER) CURED**. Companions: E-8's false reason **struck and replaced** with the true routing
(D-04 CURED); E-13 amended to the bank's *"both"* (D-03's substrate); G-KF3-1 re-founded (D-06);
G-KF3-8/9 → floors, 7 gates (D-07).
**R-7b** APPLIED: all ten escapes booked at E-15 with owner KF.W2 and entered in G-KF3-7's denominator
without moving a cure — pass-1 **D-02 (BLOCKER) CURED**; OP-3 re-anchored on `COHESION §0b` (D-05 CURED).
**R-10** APPLIED verbatim (Axis 2). **R-12** APPLIED: the phantom 7-row CARRY struck at §Provenance,
the AUDITED/SPECIFIED basis restated over the registry, denominator stated as 19/24 (D-08 CURED).
**R-18** APPLIED: OP-3's KF.W10 clause routed to a `COHESION §0c` addendum named as *the root session's
act*; this seat confirms **§0c does not yet exist** and the spec's phrasing is prospective
(*"routes the cure to"*, *"cites §0c and books nothing"*) — honest, not a phantom-authority citation.

---

## §4 DEFECT REGISTER

### D-01 · **MAJOR** — G-KF3-3 cites an unenumerated module count as its subject set and its own falsifier denies doing so.
Gate body (`:147`): *"…the **15 parser-facing modules**. Each RED is attributed value-side or kf-side
before the wave closes."* Gate falsifier two lines later (`:149`): *"**No denominator here cites a
module count** until KF.W5's X-4 reconciles 80 / 139 / 153 / '159' (and this seat's own 15-vs-16-vs-17
finding, §B.1)."* §B.1 (`:68`) records the count as **unsettled**: 15 confirmed present, **+
`roundtrip-fidelity.test.ts` = 16**, and a 17th unrostered sibling
`test/compile/frame-compiler.test.ts` (**verified present at `origin/master` by this seat**).
The fifteen modules are **never enumerated anywhere in the spec** — `grep` finds no roster.
**Failure mode**: at execution the gate's subject set is unresolvable; a seat can close G-KF3-3 over
any 15 of 17 files and no reader can convict it. Either enumerate the modules by path, or strike the
clause and let the two named test files carry the gate (which is what the falsifier already assumes).

### D-02 · **MAJOR** — G-KF3-7's negative denominator is selectively complete: three banked R1-class negatives are admitted, ≥6 structurally identical ones are absent, and one of the absentees is named inside an admitted row's own quoted bytes.
The spec's law (`:169`): *"a map that omits its own negatives cannot know what completeness is, and
cannot be audited for it."* E-15 admits negatives that route to **no wave at all** — X-13
(`kf-CubeTarget:76`, a *superlative-register* entry), X-14 (*"recorded so parser waves skip this file"*),
X-16 (*"Do not re-file"*). By that admission rule these banked R1-class negatives belong in the same
denominator and are absent (`grep -cF` in `KF-W3.md`, this seat):

| banked negative | bytes | count in spec |
|---|---|---|
| `kf-CSSPasteDialog.md:76` **C-4** | *"the value.js R1 throw class is CONTAINED on this path … Cleared — do not re-file."* | **0** — and X-16's own admitted quote names it: *"same containment posture as the banked kf-CSSPasteDialog row"* |
| `kf-LayerConfigPanel.md:79/:98` **LP-24** | *"an explicit verified NEGATIVE, folded by reference onto the banked R1 identity **so no later lane re-opens it**"* | **0** (`LayerConfigPanel` 0 · `LP-24` 0) |
| `kf-KeyframesAddDialog.md:91` **C's K-5** | *"CONTAINED … Cleared — do not re-file."* | **0** |
| `kf-OrbitalDrag.md:132` **#3** | *"a 1,110-byte zero-import leaf making the R1 crash class unreachable by module topology"* | **0** |
| `kf-SpringPhysicsFacet.md:107` **★S-7** | *"the R1 crash class does not reach this subtree"* | **0** |
| `kf-TimelineHoverPreview.md:112` **S+3/S-d** | *"structural immunity to the R1 crash class by construction"* | **0** |
| `kf-AnimatedText.md:105` **C-10** | *"the R1 crash-class exposure delta … is exactly zero"* | **0** |
| `kf-SpringTrace.md:108` **S-A (C)** | *"the R1 crash class is provably unreachable … **the adopting spec must state this trade**"* — an obligation on a receiving spec | **0** (`S-A` 0) |

**Not a census escape** — none of these routes to KF.W3 in any markup, so `routedTotal` is unaffected
and this is not a repeat of pass-1 D-02. It is a **denominator** defect in the wave's central gate:
the map's UNREACHABLE side is built from an admission rule the spec applies to three rows and not to
eight more of the same shape. Cheapest lawful cure: one E-15 sub-line booking them as **surplus
negatives, owner KF.W2 (F0)** — the shape X-16/X-17 already use — or a stated rule that confines the
surplus set to the four R-1b names and says why the others are out.

### D-03 · **MINOR** — the `KF.W3-SHIM` family's record cardinality is wrong, in a spec that convicts wrong cardinalities.
C-6 (`:125`): *"**FIVE adjudicated ROWS across five records** name a wave literally spelled
'KF.W3-SHIM' … the family is **5 rows / 5 records**, and a class booked at the wrong cardinality
cannot be checked for closure."* E-1 (`:229`) repeats: *"FIVE rows across five records."*
Measured (`grep -n 'KF\.W3-SHIM' kf-*.md`, header lines `:6/:17/:19` excluded as taxonomy sentences):
the five rows sit in **FOUR** records — `kf-KfPillTabs.md` holds **two** of them (**:58** N-KPT-2 and
**:70** L:D-8/C:C-6). C-6's own ⟨provenance⟩ enumerates four record names and then asserts five.
The **row** count (5) is right, so §Carry's 9 + 5 + 10 = 24 arithmetic is unaffected; the **record**
count is not. Pass-1 D-10 corrected "six records" to "five records" and moved the error rather than
killing it. Cure: *"five rows across four records (kf-KfPillTabs books two), plus one non-row header
citation at kf-ControlsPaneWrapper:6."*

### D-04 · **MINOR** — G-KF3-1's conjunct (2) states a measurement denominator that is off by one.
`:136`: *"`grep -rn 'RC-P' docs/tranches/X/fourier/` returns **0** today (re-measured by the repair
seat, 2026-08-28, **over the ten existing `F-W*.md` specs**)."* `ls docs/tranches/X/fourier/waves/`
returns **eleven**: `F-W0 … F-W10`. The 0-hit *result* is correct (this seat re-ran it: 0), so the
gate's RED is not affected — but a gate that states the size of the set it swept must state it right,
and the same file elsewhere insists a class booked at the wrong cardinality cannot be checked.

### D-05 · **INFO** — E-15's local-label prefix collision is mitigated by disclosure, not removed.
`X-06 … X-17` are declared a *"local row label for this table only"*, zero-padded specifically so they
cannot be confused with the unpadded intake ids `X-1`/`X-4`/`X-5` or the `X-W9` cross-edge. The
reasoning (a renumber to express a naming preference is what the alias law forbids) is sound and the
disclosure is explicit. Recorded so no later seat re-litigates it as a defect.

---

## §5 WHAT HOLDS — recorded so no seat re-spends the budget

- **Both pass-1 BLOCKERs are dead.** D-01 (E-12 vs C-5) is cured by a struck-and-re-founded E-12 whose
  new text tracks the bank's bytes exactly and is echoed consistently in four places. D-02 (10/19
  escape) is cured at 24/24 by a mechanism — exclusion-with-named-owner counted in the denominator —
  that adds no cure to this wave and moves no id off KF.W2.
- **All eleven pass-1 MAJOR/MINOR defects are addressed**; ten are fully cured. Only D-10 was cured
  imperfectly and it re-emerges here, reduced, as D-03.
- **The §B.1 anchor-correction table is the spec's best work and it re-verifies whole**: three dead
  paths (`emit/format-options.ts`, `emit/backward.ts`, `easing/easing-registry.ts` — all three MISS at
  `origin/master`, all three corrected targets OK) and two imprecise spans. Two of the corrections
  would otherwise have sent a cure to a file that does not exist.
- **The channel-(ii) blast radius is exhaustive**, not merely asserted: `git grep parseTimingFunction`
  over the whole tree returns exactly the three call sites the seed set names.
- **The spec opens no product source and stamps nothing.** Every anchor in this check was reproduced
  from `git show`/`git grep` alone.

---

## §6 VERDICT

**verdictLocal = CLEAN.**

24 of 24 routed registry ids carried or excluded-with-reason-and-owner; **0 escapes named by bytes**;
every cited command, script and path verified to exist at the coordinate it is cited at; zero VERIFIED
stamps; the KF.W4 head, the GATED-never-scheduled posture, the KF.W6 squaring and the KF-AV-28 rider
all hold; all seven KF-W3 directives in `RULINGS.md §END` executed, several verbatim.

The two MAJORs are **completeness-of-statement** defects inside otherwise-honest gates, not
census failures and not contrivance: G-KF3-3 names a module count it cannot resolve (and denies naming
one), and G-KF3-7 admits three no-wave R1 negatives into its denominator while leaving eight of the
same shape in the corpus. Both are cured by one added line each. Neither is grounds to withhold
CLEAN at this pass.

Minimum repair set for a hypothetical pass 3: enumerate (or strike) G-KF3-3's fifteen modules;
add an E-15 surplus-negatives sub-line for the eight banked R1 negatives (or state the confining rule);
correct the SHIM family to *five rows across four records*; correct *ten* → *eleven* fourier specs.
