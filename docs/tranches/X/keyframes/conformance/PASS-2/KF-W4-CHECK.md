# KF-W4 — FRESH ADVERSARIAL SPEC CHECK (L-18/L-20, PASS 2)

**Spec under trial**: `/Users/mkbabb/Programming/value.js/docs/tranches/X/keyframes/waves/KF-W4.md` (301 lines, repaired)
**Corpus authority**: the 58 `kf-*.md` records in `docs/tranches/V/megatranche/registry/adjudicated/`
**Sole in-tree carry**: `docs/tranches/X/keyframes/carry/KF-W6-CARRY.md` (KF.W6's — cited by the spec at its W6 cross-edge only, never as provenance)
**Rulings applied against**: `conformance/PASS-1/RULINGS.md` — the eight directives addressed to KF-W4 (R-1c · R-3 · R-6 · R-8 · R-10 · R-12 · R-15 · R-17)
**Seat**: FRESH adversarial reader. Census re-derived from the bytes, not inherited from PASS-1. Zero product-source writes. **Product source READ-ONLY at the sibling checkout `/Users/mkbabb/Programming/keyframes.js` at the spec's own Ref of record `origin/master 81a56990`** — the one instrument PASS-1 never used, and the one that convicts.
**Verdict**: **DEFECTIVE** — 104 routed · 104 booked · **0 escaped** · 15 defects (**2 CRITICAL · 7 MAJOR · 6 MINOR**).

**The shape of the repair**: the census axis is now CLEAN — all four PASS-1 escapes are carried with receipts, and an independent re-derivation of the 58 records reproduces the same 104-row denominator with nothing new escaping. Every ruling was executed in *form*. The failures are all of one kind and they are new: **the spec was repaired against the rulings and never against the tree.** Its Ref of record is `origin/master 81a56990`; its §Bounds preamble says *"all re-resolve at `origin/master` before any cure"*; its §Archaeology claims the guardrail *"every anchor in this file is corrected in §Bounds and re-resolves at `origin/master`"*. Nine anchors do not resolve there at all.

---

## §0 Method

1. Mechanically extracted every line in all 58 records matching `KF\.W4|KF-W4|\bW4\b` → **195 lines across 45 records**; 13 records are W4-silent.
2. For each hit resolved the **terminal disposition** — the last cell of a roster row, the record's `## Wave routing summary` `- **KF.W4**:` bucket, or the prose `→ KF.W4` / `FOLD → … → KF.W4` / `KF.W4 rider` / `KF.W4-PROSE` form. Discarded non-routings: taxonomy recitals (`kf-App:35`, `kf-EditorHeader:7`, `kf-CopyButton:36`, `kf-PlaybackRibbon:31`, and 20 more), sequencing references (*"sequenced after KF.W4"*, *"trivial under the KF.W4 gate"*), kill-register rows, and **one cross-taxonomy trap: `kf-StartingStyleTarget.md:112`'s "the K.W4 S5 de-duplication" is tranche K, not KF.W4** — correctly not carried by the spec.
3. Cross-swept the 13 W4-silent records for family-routings landing in W4 by reference. This recovers `kf-SequenceScrubber.md:119` (PASS-1's E2) and surfaces **two further fold-identity instances the spec never names** — `kf-SequenceScene.md:74` and `kf-KeyboardShortcutsModal.md:45` — both banked NO-WAVE-OWNER, so **not escapes by terminal routing**, but both booked below as defects (D-9, D-12).
4. Matched each survivor against the spec's 31-row carry table, 14 gates, §Bounds, §Sequencing, §Excluded and the §Excluded 16–20 residue table.
5. **Executed the gates' cited commands and paths at `origin/master 81a56990`** (read-only `git show` / `git grep` / `git ls-tree` against the sibling checkout — no write, no install, no browser). This is §2's evidence base.

---

## §1 ID-KEYED CENSUS — 104 routed · 104 booked · 0 escaped

### 1.1 The four PASS-1 escapes are CARRIED, with receipts

| PASS-1 | banked at | ruling | where it lands now | receipt |
|---|---|---|---|---|
| **E1** ~40-name `proof:*` prose-rot magnitude | `kf-ChannelOptions.md:207` | R-8.2 | **row 29** (*"Class magnitude, stated: ~40 `proof:*` names"*) + **R-7** (*"carries **~40 names**"*) + **§Excluded 20** | 3 independent statements; `grep "~40" KF-W4.md` → 3 |
| **E2** `proof:sequence-rows-draggable` liveness | `kf-SequenceScrubber.md:119` | R-17 | **row 12 header** (*"the KF-EST-1 family's fourth measurement"*) + **OP-3 RESOLVED** | `grep "sequence-rows-draggable"` → 2; OP-3 re-cut from DISPUTED |
| **E3** SpringTrace D-1's `:48-49` comment truth | `kf-SpringTrace.md:139` | R-1c | **row 29** (*"carried WHOLE — all four items … a **sequencing rider** … M-25 forbids dropping it"*) | verbatim, with the *"(rides the fix)"* qualifier intact |
| **E4** KF-AV-28 standing supersession rider | `kf-AnimationVisualizer.md:35`, `kf-PlaybackRibbon.md:36`, `kf-SequenceScrubber.md:36` | R-10 | **§Sequencing :227**, clause **byte-identical to RULINGS R-10** | diffed against `RULINGS.md:201` — verbatim, plus the governed-row enumeration (*"none of its own cures … the rider's weight here is as HOMING AUTHORITY"*) |

### 1.2 Independent re-derivation — the denominator

| block | n | running |
|---|--:|--:|
| **A** · the vue-tsc/demo-typecheck fold identity (KF-APP-4 ≡ KF-CE-16 ≡ KF-CO-7 ≡ KC-13 ≡ …) | 33 | 33 |
| **B** · gate topology + merge-path lanes (ME-32 · ME-29 · KF-SST-37 · C-21) | 4 | 37 |
| **C** · K-quartet (K1–K4 · C-19/L-m1 · KF-SS-34 · L-10/L-11 · C-10/C-11/D-m6 · L·N-16 · KF-AV-18 · L-18/C-12a · MISS-6) | 12 | 49 |
| **D** · easing honesty (KF-ET-6 · KF-ES-3 · KF-ES-14 · KF-CB-18 · KF-CB-24 · KF-CB-29 · KF-TFP-17) | 7 | 56 |
| **E** · citation/provenance census (KF-EST-1 · L:D-3/C:C-5 · KF-HA-4 · D-19 · KF-CO-33 · KF-CO-37 · SPF-27 · MM-9 · MM-13 · KF-EST-6) | 10 | 66 |
| **F** · AnimatedText gate family (KF-AT-4 · -10 · -24 · L-EST-17 · KF-AT-7 cascade leg) | 5 | 71 |
| **G** · gate-authoring audit + structure (OD-29 · KF-CE-12 · KF-AV-8 · C-12 · KF-CE-30 · REGISTER-LAUNDER · C-S+1 · L-D7 · L:D-3 zero-test · SquareScene :166 · B10-27 · B21-17) | 12 | 83 |
| **H** · the KF.W4-PROSE class, R-6 split (ChannelControls ×7 · KfPillTabs ×4 · SpringTrace ×3 · C-4 · N-1/C-5 · B18-19) | 17 | 100 |
| **I** · the four PASS-1 escapes, now carried (§1.1) | 4 | **104** |

**routedTotal = 104 · bookedCount = 104 · escapedCount = 0.** My re-derivation reproduces PASS-1's denominator member-for-member; the cross-sweep of §0.3 found no fifth escape.

### 1.3 Corpus coverage — the AUDITED cell checks out arithmetically

The spec's `AUDITED` cell claims **46 distinct records consulted** (kf-CopyButton de-duplicated, nine previously-undeclared sources added) and **41 supplying ≥1 W4-routed id**. Both verify: the enumerated list is exactly 46 names, and of those, five supply no routed row — kf-CubeAxisLines, kf-DemoGlobalChrome, kf-EditorHeader, kf-KeyframeCard, kf-KeyboardShortcutsModal. 46 − 5 = 41. **R-12's basis restatement is executed correctly**, and the phantom *"authored against the CARRY ledger whole"* is struck and replaced by *"authored from the 58-record registry directly."*

### 1.4 Correctly excluded / correctly not claimed (spot-verified against the bank)

- **KF-CB-1 / EE-01** → §Excluded 7, with the frontier-cure reason. ✔ Verified at the tree: `git grep bounceInEase origin/master -- src/` → exactly the two prose phantoms the spec claims survive (`easing.ts:44`, `waapi/eligibility.ts:169`), and no live `bounceInEase` binding. **The spec's frontier-durability claim on row 9/row 11 is TRUE at the bytes.**
- **TimelineCaret L-7** → witness-only, site at KF.W7 (R-3 applied at §Bounds :61, G-KFW4-4's witness list, and §Disjointness). ✔ Matches `kf-TimelineCaret.md:51` verbatim.
- **KC-17 · LP-21 · KF-AX-16 · KF-CE-11 · KF-KE-17 · SPF-1** — all *"after the KF.W4 gate"* sequencing references, none claimed. ✔
- **Five dead sub-claims not re-imported** (MM-13 modality · C-4 running cost · KF-CB-8 leg 2 · D-4's WCAG rationale · *"zero coverage for the LCP hero"*). ✔ Still the spec's strongest single behaviour.
- **§Sequencing's head-witness count, corrected under R-8.6 to EIGHT with kf-AmigaScene struck** — I verified all eight independently state it (`kf-CSSCodeEditor:50/:66` · `kf-ChannelOptions:150` · `kf-KeyframeCardList:30` · `kf-KeyframesEditor:36` · `kf-CubeAxisLines:34/:53` · `kf-LayerConfigPanel:35/:126` · `kf-KeyframeTimeline:170` · `kf-TimelineHoverPreview:142`), and that `grep -i sequenc kf-AmigaScene.md` yields no such claim. **R-8.6 executed correctly.**
- **R-15's 17-packet roster** — all 17 homed, 9 + 6 + 2, none double-homed; KF.W13 minted explicitly; dock-menu moved to W13. ✔
- **R-17's frontier gate inventory** — **executed and TRUE**: `git ls-tree -r origin/master -- scripts/gates` → **9 files** (`structure/index.mjs`, `surface/agent-surface.mjs`, `surface/{boundary,consume-bundle,index,published-surface,readme-runs,verify-diff}.mjs`, `visual/index.mjs`), and `origin/master:package.json:37` carries `npm run proof:structure` inside `check`. The stale *"surface, visual"* reading is correctly struck.

---

## §2 DEFECTS (15) — the tree, executed

### D-1 · **CRITICAL** — the sequencing head's own command silently DELETES a live gate

`G-KFW4-1` (:184) redefines the wave's central script:

> `npm run check` (**redefined**: `vue-tsc --noEmit -p tsconfig.json && tsc --noEmit -p tsconfig.test.json`)

At the declared Ref of record:

```
$ git show origin/master:package.json | sed -n '37p'
"check": "tsc --noEmit && tsc --noEmit -p tsconfig.test.json && npm run proof:structure",
```

The redefinition **drops `&& npm run proof:structure`** — a *live, executable* gate (`scripts/gates/structure/index.mjs`, verified present) — and neither §Bounds row 45 (`package.json` modify — scripts) nor §Commits 1 nor any falsifier declares the removal.

This is not a typo; it is the wave's thesis turned on itself. OP-3 and RULINGS R-17 were just re-litigated **specifically to establish that `proof:structure` is present in `origin/master`'s `check`** and that the 2-script roster was stale-worktree-only. The spec wins that ruling, states it in three places (OP-3, §Bounds row 69, G-KFW4-8's denominator cell), and then, in the born-RED command of the gate it calls *"the sequencing head"*, ships the stale worktree's `check` — the very deletion `kf-SequenceScrubber.md:119` and `kf-TypingDots` K8 convict. A wave whose goal criterion is *"no gate in the tree computes its own oracle"* would extinguish a real one at hop one.

### D-2 · **CRITICAL** — `EditorShell.vue:235-237` is a PHANTOM citation site; the ninth target carries no `proof:` byte

R-4's ruling, §Bounds' **Citation targets** row, and G-KFW4-8's born-RED witness column all name `EditorShell.vue:235-237` as a live `proof:appearance-suffusion` / `proof:hero-two-focal` site — R-4 states it as fact: *"All three live sites retire (`EditorShell.vue:235-237`, `EditorStartScreen.vue:15`, and the token authority at `layout.css:29-31`)."*

```
$ git show origin/master:demo/components/instrument/shell/EditorShell.vue | grep -c 'proof:'
0
$ git grep -c 'proof:' HEAD -- '*EditorShell.vue'
(no output — 0 at the stale coordinate too)
$ git show origin/master:demo/components/instrument/shell/EditorShell.vue | sed -n '234,235p'
   §Hard-gate clause-g legibility assertion — W6-3 exits on a runtime clause,
   never deferred again). */
```

`:235-237` is a clause-g legibility comment that carries **no `proof:` token at either coordinate**. Meanwhile the real third site of `proof:appearance-suffusion` is **`demo/DESIGN.md:251`** — a file the spec *does* enumerate, at an anchor it does not.

The damage is structural, not cosmetic. `EditorShell.vue` is one of the **NINE** targets on which R-8.1's entire satisfiability arithmetic rests (*"nine enumerated sites, all nine in-bounds"* — §Excluded's load-bearing consequence). One of the nine is a phantom; a tenth real site is unenumerated. The spec's §Archaeology guardrail — *"every anchor in this file is corrected in §Bounds and re-resolves at `origin/master` before a cure"* — was never run on the row that most needed it.

### D-3 · **MAJOR** — two phantom script paths in §Bounds; one is a gate's named subject

| §Bounds row | spec path | at `origin/master` | the real path |
|---|---|---|---|
| 67 (**modify-carve**) | `scripts/usability.mjs` | **ABSENT** | `scripts/observe/demo/usability.mjs` (410 L) |
| 68 (read, no write) | `scripts/observe/live-session.mjs` | **ABSENT** | `scripts/observe/demo/live-session.mjs` (1612 L) |

```
$ git ls-tree -r --name-only origin/master | grep -E 'usability|live-session'
scripts/observe/demo/live-session-mobile.mjs
scripts/observe/demo/live-session.mjs
scripts/observe/demo/usability.mjs
```

Neither path exists at `origin/master` **or** at HEAD `8281638c`. `G-KFW4-9`'s command names *"every gate in `scripts/gates/`, `scripts/observe/`, **`scripts/usability.mjs`** and `test/`"* — a gate whose subject set names a file at no coordinate. The §Gates preamble asserts *"Commands are literal; paths re-resolve at `origin/master` first."*

The witnesses themselves are REAL at the real path — `glyphCount: mirrorText.replace(/\s+/g, "").length` at `usability.mjs:239`, the `heroReady` 30×300 ms `h1 .wave-char` poll at `:160-172`. That makes this a pure address failure, and it is the exact failure the spec elsewhere legislates against: rows 64/65 refuse to *"invent"* a directory for `orchestration-api.test.ts` and `resize-tracks.test.ts`, while rows 67/68 invent one for `usability.mjs` and `live-session.mjs`. Same discipline, opposite outcomes, one file.

### D-4 · **MAJOR** — G-KFW4-8's re-scoped denominator is still wrong: **19 distinct names / 33 citation lines at its own nine targets**, of which the spec inventories ~10

Measured at `origin/master`, `proof:` lines per enumerated target: `DESIGN.md` **13** · `EditorStartScreen.vue` **5** · `App.vue` **3** · `HeroAurora.vue` **3** · `TypingDots.vue` **3** · `layout.css` **3** · `design-idioms.css` **2** · `SpringPhysicsFacet.vue` **1** · `EditorShell.vue` **0** (D-2). Distinct names:

> appearance-suffusion · colocation · crayon-preserved · font-census · publish · style-file-ceiling · styling-idioms · zone-cohesion · app-is-shell · cursor-light-subtle · icon-paint-live · design-refinement · hero-deck-voice · hero-two-focal · no-hand-rolled-cursor-tracker · typing-dots · easing-sidebar-normalized · idioms · brittleness

**Nine of these the spec never names**: `colocation` · `crayon-preserved` · `styling-idioms` (two files) · `zone-cohesion` · `app-is-shell` · `icon-paint-live` · `design-refinement` · `hero-deck-voice` · `brittleness`. Only **one** of the nineteen (`proof:publish`) resolves to an executable. So G-KFW4-8 as commanded — *"every `proof:*` / baseline / allowlist citation at the NINE citation targets … resolves to an executable script or gate"* — reds on ~18 names while the spec plans ~10 cures.

R-8.1's arithmetic conflates **sites (files)** with **citations**. The residue is at least in-bounds here — but not freely: R-7 bounds the cure to *"the citation token and its clause (≤1 line per site)"*, and `DESIGN.md:235-243` is a seven-row §10 rules table whose entire **Proof** column is citations (`proof:colocation`, `proof:styling-idioms`, `proof:zone-cohesion`, `proof:style-file-ceiling`, `proof:publish`). Retiring those is table surgery, not a token deletion. §Excluded's closing sentence — *"G-KFW4-8 can now go GREEN in-wave for its stated reason"* — is not established by the spec's own numbers.

### D-5 · **MAJOR** — a routed residue family sits **inside** the gate's own in-bounds denominator (self-tripping falsifier)

§Excluded 17 routes kf-CubeAxisLines **KF-AX-31** — *"`proof:brittleness` at `style.css:38-39` + 3 stale prose sites"* — **wholly to KF.W6**. But:

```
$ git grep -n 'proof:brittleness' origin/master -- demo/ src/ scripts/
demo/styles/layout.css:152:   dock bands, NOT a :has() test on a generic .z-dock utility class (proof:brittleness
demo/styles/style.css:38:   value (proof:brittleness gates against drift). ...
scripts/lib/demo-driver.mjs:69:// A STALE-KEY GUARD (mirrors the proof:brittleness LISTENER_ALLOWLIST stale
```

`demo/styles/layout.css` **is one of the nine enumerated targets** and is in §Bounds. G-KFW4-8's own falsifier reads: *"**fails if a residue family is silently pulled in** (that is scope-creep wearing a green, and every such cure site is outside §Bounds where a write trips §Triumvirate)."* Here the premise is false — the cure site is *inside* §Bounds — so the gate must either red on a family the spec routed away, or cure a byte §Excluded 17 assigned to KF.W6. The partition R-8.2 was written to create does not hold at the bytes.

### D-6 · **MAJOR** — a G-KFW4-8 born-RED witness whose only site is outside the gate's denominator **and** outside §Bounds, routed nowhere

G-KFW4-8's witness column lists *"**KF-CO-33's three `proof:bezier-*`**"*.

```
$ git grep -n 'proof:bezier' origin/master -- demo/ src/ scripts/
demo/components/instrument/transport/channel-controls/ChannelOptions.vue:168:  ... pencil hook proof:bezier-{no-scroll,single-card,grown}
```

The sole site is `ChannelOptions.vue:168` — not one of the nine targets, not in §Bounds, and **not routed by §Excluded 16–20** (item 20 disposes of the ~40-name class *"with their files"*, naming no destination for this one). A born-RED witness the gate names but may not lawfully cure, and whose cure site the residue table forgets. L-19's own standard: a gate that names a witness it cannot reach names no witness.

### D-7 · **MAJOR** — RULINGS R-8.4 not executed on three of its six named rows

R-8.4, verbatim: *"**Pathless rows get paths**; the `<config>` placeholder is spelled (D-3): G-KFW4-11's command names `.dependency-cruiser.cjs` …, and **§Bounds rows 51/59/64/65/66/71 carry literal paths**."*

| row | status | the literal, resolvable read-only in one command |
|---|---|---|
| 51 | ✔ `.dependency-cruiser.cjs` (verified present) | — |
| **59** | ✘ still *"identifier-literal, path command-resolved"* | `src/animation/engine/css/animation.ts` (`:54` decl, `:82` assign) |
| **64** | ✘ still leaf-only + a resolving command | `test/orchestration/orchestration-api.test.ts` |
| **65** | ✘ still leaf-only + a banked-receipt inference | `test/demo/instrument/resize-tracks.test.ts` |
| 66 | ✔ `test/demo/scenes/orbital-{inertia-parity,rotate3d}.test.ts` (both verified present) | — |
| 71→76 | ✔ `demo/utils/reference-data/animationDescriptions.ts` (verified present) | — |

The spec's ground for the refusal — *"**no record banks its directory, and this file opens no product source to invent one**"* — is inconsistent with its own practice: the same file resolves the frontier's nine `scripts/gates/` scripts, `ci.yml:41-42`/`:53-55`, `env.d.ts:3-7`, `tsconfig.lib.json:1-11` and `package.json:77` by exactly this class of read-only command. A ruling *"outranking any local finding it meets"* was met by a local finding and lost. (Row 65's banked-receipt inference — *"three directories below the repo root"* — is nonetheless **correct**: `test/demo/instrument/`. Good reasoning, unexecuted conclusion.)

### D-8 · **MAJOR** — false witness: *"the lane is structurally a green empty set"*

§Bounds row 49 and G-KFW4-2's witness cell both assert the demo vitest project is *"structurally a green empty set."*

```
$ git ls-tree -r --name-only origin/master -- test/demo | wc -l
27
```

Twenty-seven live specs — `scene-machine-reducer`, `control-surface-dfa`, `cube-scene`, `square-scene`, `sequence-scene`, `KfPillTabs`, `easing-catalog`, the two orbital gates, and eighteen more. The **true** banked claim, which the spec also states, is ME-32's *"referenced by NO script and NO workflow"* — that one verifies. The "empty set" gloss is spec-authored, falsifiable by one `ls-tree`, and load-bearing: it underwrites G-KFW4-2's falsifier (*"fails if it 'passes' with zero mounted SFCs"*) and the row-3 mechanism cell. Wiring `--project demo` into the merge job will not surface an empty lane; it will surface 27 specs whose pass/fail state this wave has not measured.

### D-9 · **MAJOR** — G-KFW4-4's demo-arm inventory omits a bank-measured, in-bounds K3 site; its record is absent from the AUDITED basis

`kf-SequenceScene.md:74` — **D25 / L-5 / C-7**, verbatim: *"`computed` imported, never used (the 43-line file); the byte is this file's own; the detector hole (no ESLint, tsc never parses SFCs, no `noUnusedLocals`, CI = `check:lib` over src/) **folds ≡ banked KF-APP-4 ≡ KF-ES-16**."* Re-derived at the tree:

```
$ git show origin/master:demo/scenes/sequence/SequenceScene.vue | wc -l          →  43
$ ... | sed -n '8p'   →  import { computed, provide, ref } from "vue";
$ ... | grep -c 'computed('   →  0
```

A live, in-bounds `noUnusedLocals` demo-arm site, mechanically identical to the AmigaScene `:21` and SquareScene sites the spec *does* enumerate at §Bounds row 61 and G-KFW4-4's witness list. It appears in neither. G-KFW4-4's falsifier — *"**fails if the flag lands while any in-bounds site survives**"* — makes the omission operative, not cosmetic: the demo arm cannot close.

`kf-SequenceScene` is absent from the 46-record AUDITED basis entirely (it is W4-byte-silent; only the §0.3 cross-sweep reaches it). Its terminal disposition is NO-WAVE-OWNER, so it is **not** a census escape — it is an under-enumerated gate subject.

### D-10 · **MINOR** — a §Bounds "scope extension" already satisfied at the Ref of record

§Bounds row 47: *"`tsconfig.json` | modify — `"noUnusedLocals": true`; **the shared `include: ["src/","demo/"]` scope extension** (kf-AmigaScene C-19)."*

```
$ git show origin/master:tsconfig.json | sed -n '47p'
    "include": ["src/", "demo/"]
```

Already present. (`:8 noUncheckedIndexedAccess` and `:9 exactOptionalPropertyTypes` verify exactly as the spec states — those anchors are correct.) The remaining real change is `noUnusedLocals` plus a checker that parses SFCs; the "scope extension" is a no-op carried forward from a bank measurement the frontier already satisfies.

### D-11 · **MINOR** — R-9's decision rule rests on a miscounted measurement

§Bounds row 59: *"`git grep -n "_boundTimeline" origin/master -- src/` is the resolving command; **its single hit** is written to `k3-decision.md`."*

```
$ git grep -n "_boundTimeline" origin/master -- src/
src/animation/engine/css/animation.ts:5:  ... (`propertyRegistry`, `scrollOptions`, `_boundTimeline`)
src/animation/engine/css/animation.ts:54:    private _boundTimeline?: Timeline;
src/animation/engine/css/animation.ts:82:        this._boundTimeline = timeline;
```

**Three hits, one file.** The *declaration* is single; the command's output is not, and `:82` is a live write — which is exactly the fact R-9's total rule (*"if it has a named consumer … wire the guard; else delete the field and its prose together"*) must adjudicate. The bound holds (`src/animation/**` — no triumvirate trigger) and the rule survives; the row's arithmetic does not.

### D-12 · **MINOR** — an uncarried banked dissent against the wave's own founding premise

`kf-KeyboardShortcutsModal.md:45` — **R-9**, verbatim: *"nothing automated verifies this file … no `vue-tsc` anywhere … env.d.ts shims every `*.vue` to `DefineComponent<{}, {}, any>` … CI runs only `check:lib` over `src/`; no ESLint exists. … **→ NO-WAVE-OWNER** (repo-scope substrate; **no forming KF wave names demo typechecking — SS-1/SS-2 must seat it, post-F-1**)."*

That is the row-1 identity, measured a thirty-first time, and **banked as unhomed with an explicit denial that any KF wave owns it** — plus a carve-out the spec never carries (*"the whole-project `check`'s demo half is documented as staged until the Glass dependency lands … but the vue-tsc absence, the any-shim, and the lint absence all **survive** F-1's restoration"*). Row 1 claims the identity is *"booked once for the program"* over 30 aliases and never meets the record that says otherwise. M-25 wants the dissent carried, as the KC-13 ordering dissent and C-13's vendor-DOM exemption are.

### D-13 · **MINOR** — the residue table's closure claim over-states its own reach

§Excluded's closing: *"the residue is **not lost but owned elsewhere by name**."* Measured at `origin/master` across `demo/ src/ scripts/`: **273 `proof:`-bearing lines, ~100 distinct `proof:*` names**, of which three are executable npm scripts and a handful more map to `scripts/gates/surface/*.mjs` files. The residue table routes **five families**. The sentence is true of *the bank-enumerated residue* (which is what R-8.2 commanded) and false as written of the tree. One qualifier — *"every bank-enumerated residue family"* — would make it true.

### D-14 · **MINOR** — the AUDITED cell's balance sentence is false for one of its five

*"the balance carry W4 bytes that resolve to no routed row and are read as verified negatives / cross-edge sources."* Of the five balance records (kf-CubeAxisLines, kf-DemoGlobalChrome, kf-EditorHeader, kf-KeyframeCard, kf-KeyboardShortcutsModal), **kf-KeyboardShortcutsModal carries zero W4 bytes** (`grep -cE 'KF\.W4|KF-W4|\bW4\b'` → 0). It is a cross-edge source (its dead-gate arm rides the KF.W0 edge — correctly declared) but it does not "carry W4 bytes."

### D-15 · **MINOR** — `usability.mjs`'s anchors are bank-transcribed, not re-resolved

§Bounds row 67 carries *"roster `:8`"*. At the real file, `:8` is a line of the header docstring (*"Three falsifiable clauses, each BITING on the exact defect it forbids"*); the `glyphCount` oracle is at **`:239`**, the `.wave-word` enumeration in the `:20-31` docblock and the clause-2 body, and `heroReady`'s `h1 .wave-char` 30×300 ms poll at **`:160-172`**. Every mechanism the row names is real; every anchor is the bank's, unresolved at the Ref of record — the same failure mode as D-2 and D-3, one severity down because the row is a carve list rather than a gate command.

---

## §3 AXIS VERDICTS

| axis | verdict | receipt |
|---|---|---|
| **1 · ID-keyed census** | **CLEAN** | 104 routed / 104 booked / **0 escaped**, re-derived independently over all 58 records + a cross-sweep of the 13 W4-silent ones. All four PASS-1 escapes carried with receipts (§1.1). AUDITED basis arithmetic verifies (46 / 41 / 5). R-12's provenance restatement executed; no phantom CARRY citation survives (`grep -c "CARRY" KF-W4.md` → the two W6-ledger references, both correctly attributed to KF.W6) |
| **2 · No invention (M-25) / depth** | **NEAR-CLEAN** | Locks carried verbatim (KF-ET-6's *"gate `seedFor` on `NAMED_EASING_BEZIER`, never merge the catalogues"*; KF-HA-4's *"the un-writable import form must not enter a spec"*; KC-34; the OPTIONS-UNIT fixed order; M-4's MbabbMenu rider; the TD-1/TD-2 one-commit locks; K-5's two-deletion pair). Dissents preserved (KC-13 ordering, KF-ET-6 reader-1's BLOCKER, C-13's vendor-DOM exemption, KF-AV-18 reader-1's MAJOR, C-4's RR-L demotion, RR-1's `layout.css` find, RR-2's one-not-three). PASS-1's D-11 (row 27) and D-12 (`D:D-8`) are both **cured**: row 27 now quotes the closing verdict at `:166` with the count corrected to **eight**, names MISS-3/MISS-5 as witnesses-not-rows, and states the record's true W4 bucket; row 17 now qualifies `D:D-8` as record-scoped (*"resolves to four different rows across the corpus; never carried unqualified"*) and books only the KfPillTabs font-roles rider. PASS-1's D-13 cured — R-2's *"dated end"* is re-cut to *"**CONDITION-bound, not calendar-dated**"* with the not-a-shim argument re-founded on the terminator, not the date. Residual: **D-12** (uncarried KSM R-9 dissent) |
| **3 · Gates born-RED with REAL witnesses (L-19)** | **DEFECTIVE** | All 14 gates carry measured witnesses cited to banked ids, and I verified the load-bearing ones **at the tree**: `registry.ts:30-34/:36/:43` exact · `easing-serialize.ts:71-73` exact · `leaves.ts:19/:28` exact · `load-engine.ts:65` exact · `types.ts:27` `"steps"` and `:195` `\| string` exact · `env.d.ts:3-7` exact · `ci.yml:41-42`/`:50-55` exact · `INERTIA_FACTOR = 0.92` at `orbital-inertia-parity.test.ts:29` exact · `monaco-themes` at `package.json:92` exact · `.dependency-cruiser.cjs` present · all five R-8.3 fixture create-rows present · `test/demo/instrument/` present · `easing-catalog.test.ts` present and correctly distinguished from the new fixture. Against that: **D-1** (the head gate deletes `proof:structure`), **D-2** (a phantom ninth target), **D-3** (two phantom script paths — *a phantom script convicts*), **D-4/D-5/D-6** (denominator, partition and witness reach), **D-8** (false lane witness), **D-9** (under-enumerated K3 subject) |
| **4 · E-3 + STATUS** | **CLEAN** | Exactly one `VERIFIED` byte (`:14`, reading **NO** / *"stamped only at the X·KF sub-tranche close"*); `Status: **planned**` with *"No product byte is written by this file"*; current-voice completion-verb scan → **0 hits**; OP-1 declares write authority UNRESOLVED and decides nothing; OP-6 left to the owner; no product source opened for writing. My own reads were read-only `git show`/`git grep` at a sibling checkout — the instrument the spec is entitled to use and did not |
| **5 · Posture** | **NEAR-CLEAN** | KF.W4 head **declared and now correctly counted at eight verified witnesses** with kf-AmigaScene struck (R-8.6 ✔). KF.W3 **GATED** stated correctly and R-2's terminus re-founded on the condition (PASS-1 D-13 ✔). **KF-AV-28 rider present verbatim** at §Sequencing's homing head, travelling with every homed NO-WAVE-OWNER packet, and named again at the drag-seam and transport/ribbon packets (R-10 ✔). R-15's 17-packet roster homed whole, W13 minted (✔). R-6's one reading + the 14-entry id-for-id limb manifest present, headed by D-7/L-4's rationale block (✔). R-1c ✔. R-3 ✔ (§Bounds, gate witness, §Disjointness all three). R-12 ✔. R-17 ✔ and **true at the tree**. Residual: **D-5**'s partition breach is a posture failure as much as a gate failure |

### Premises independently verified TRUE at the tree (adversarial checks that failed to break the spec)

- **The frontier gate inventory is NINE.** `git ls-tree -r origin/master -- scripts/gates` → 9 files including `structure/index.mjs` and `surface/agent-surface.mjs`. R-17's correction is not merely ruled — it is measured.
- **`proof:structure` really is in `origin/master`'s `check`.** OP-3's RESOLVED grounds hold at the bytes (which is what makes D-1 a defect rather than a disagreement).
- **The `bounceInEase` phantom trail is exactly two prose sites and frontier-durable**, precisely as rows 9/11 and §Excluded 7 state.
- **The `.vue` shim is exactly `DefineComponent<{}, {}, any>` at `env.d.ts:3-7`** and `tsconfig.lib.json` is exactly `include: ["src/"]` under the staged-Glass comment — G-KFW4-1's no-op trap is real and correctly identified.
- **`easing-catalog.test.ts` is a live data gate over 29 `EASING_GROUPS` items** — row 72's careful non-duplication argument is exact, and the 29 matches `NAMED_EASING_BEZIER`'s banked key count.
- **`test/demo/instrument/` is the banked demo-lane home** (10 files) — R-8.3's two new fixture paths land in a real directory.
- **Row 65's banked-receipt directory inference is right** (`test/demo/instrument/resize-tracks.test.ts`, three levels deep) even though R-8.4's literal was not written.

---

## §4 VERDICT

**verdictLocal = DEFECTIVE.**

The repair round did what it was told. Every one of the eight rulings addressed to this wave — R-1c, R-3, R-6, R-8 (all six items in form), R-10, R-12, R-15, R-17 — is present in the text, several of them executed with real care: the KF-AV-28 clause is byte-verbatim, the 17-packet roster is homed whole with W13 minted, the head-witness count is corrected to a number I independently verified over eight records, row 27's transcription defect is cured with the count fixed at eight and the mis-homed ids returned to their tails, and the census closes at **104 / 104 / 0**. On the axis that failed hardest in PASS 1 — id-keyed carriage — this spec is now clean, and R-12's provenance restatement removes the last phantom ledger.

It fails on a new axis, and the failure is coherent: **the spec was repaired against the rulings and never against the tree it governs.** Its Ref of record is `origin/master 81a56990`, its §Bounds preamble promises re-resolution there, its §Archaeology names anchor-correction as the guardrail it learned from O-11 — and one read-only pass over that checkout finds a §Bounds Citation-target with no `proof:` byte in it at either coordinate, two §Bounds script rows at paths that exist nowhere, a demo lane called empty that holds twenty-seven specs, a K3 flag whose demo arm cannot close because a live in-bounds site is not in its inventory, a residue partition that leaks through `layout.css`, and — most seriously — a sequencing-head gate whose redefinition of `check` deletes the live `proof:structure` invocation that OP-3 and R-17 had just finished proving was there.

That last one is the wave's own thesis inverted. A spec that exists to extinguish phantom authorities and to make gates real cannot ship a head gate that silently retires a real one, nor §Bounds rows addressed to files that do not exist. The rulings are discharged; the tree is not. **One further repair round, executed with a read-only checkout at `81a56990` open beside the file, closes it.**

*Nothing in this file stamps a wave. ADJUDICATED-style findings only; the spec's own status stays `planned`. Sole write = this file.*
