# KF.W6 — CARRY (Glass Suffusion · demo)

**Seat**: CARRY seat, sub-tranche X·KF, wave KF.W6. **Law**: M-25 (agglomerate — dedupe by identity, carry cure-shape locks and sequencing edicts, route by mechanism; transcription-only is DEFECTIVE) · L-20 (validate against `docs/precepts/` latest) · L-19 (no gate without a named live witness) · born-RED. **Status**: `planned`. No product byte written; nothing opened product source for writing; execution awaits the owner's begin-word.

**Ref of record**: keyframes.js `origin/master` `81a56990`. Local HEAD `8281638c` is 41 behind and DISQUALIFIED as witness substrate. Every anchor below re-resolves at origin/master before cure (D-19). **KF.W0 §B-12 manifest schism carried**: origin/master `package.json:77` pins `@mkbabb/glass-ui` `7.0.0` EXACT devDep = installed; the census F-1 "phantom dependency" is FALSIFIED at HEAD and folds to KF.W0, never re-booked in this wave. **Every wave re-baselines against origin/master.**

**Sources consumed**: all 54 registry records under `docs/tranches/V/megatranche/registry/adjudicated/` carrying a `KF.W6` routing — the 50 assigned slugs plus `kf-AnimationControlsGroup`, `kf-ChannelControls`, `kf-ControlsPaneWrapper`, `kf-DemoGlobalChrome`, `kf-KfPillTabs` caught by the grep sweep.

**TAXONOMY RECONCILIATION (binding, adjudicated at KF-W4 §11 ¶1 — not inherited).** The `lane-frontend.md §10` seven-wave sketch (KF.W1-DEP · KF.W2-TABS · KF.W3-SHIM · KF.W4-PROSE · KF.W5-PARTIALS · KF.W6-TIMELINE · KF.W7-TOKENS) is **SUPERSEDED** by `CENSUS-2026-08-03 §(a)`. Consequences carried whole:
- **KF.W2-TABS ⇒ THIS WAVE** (S-1, census-corrected from REPLACE to **DELETE** by kf-ChannelControls §0).
- **KF.W3-SHIM ⇒ THIS WAVE** (F-5 shim + dead-barrel deletes).
- **KF.W4-PROSE SPLITS BY MECHANISM**: the phantom-**AUTHORITY** limb (counterfactual `proof:*` cites) rides KF.W4's gate; every other limb — phantom CONSUMERS, false INVARIANT NAMES, stale VENDOR premises, impossible TYPE claims, broken TEXT — is **THIS WAVE's**, because K2's commit discipline requires the delete and the correction to be **one commit** and the deletes are this wave's. **Census S-2's owner is therefore named: KF.W6 for prose-with-code, KF.W4 for the citation gate.**
- **KF.W5-PARTIALS ⇒ THIS WAVE** (S-5 / S-6 / S-7, each re-homed by its bank).
- **KF.W7-TOKENS ⇒ THIS WAVE**, **RE-CUT AS A PHANTOM-TOKEN AUDIT** (kf-App D-15's recommendation, ratified at the bank).
- **"KF.W6-TIMELINE" grep hits are NOT this wave** — they are KF.W7 · Timeline Evaluate under the old numbering (kf-ChannelControls L-2/C-2, kf-AnimationControlsGroup D-1, kf-KeyframeTimeline cluster). Carried here only as declared cross-edges.

---

## §Rows

Format per line-block: **id** · source(s) ⟨banked witnesses⟩ · severity · mechanism · **cure-shape + locks** · sequencing · dissent. Ids keep their banked names for life. A row banked in N records appears ONCE citing all witnesses. Severities: **B**=BLOCKER · **MAJ**=MAJOR · **MIN**=MINOR · **INFO** · **SPEC**=spec-input/law.

### A · S-1 THE TABS DELETE (census-corrected: DELETE, not replace)

- **CC-§0 / C-1 / D-3 / L-5** ⟨kf-ChannelControls; kf-KfPillTabs D:D-1 fold; kf-KeyframesStringControls RR-D missed-M2 rescope⟩ · **MAJ** (C-1's BLOCKER killed: unreachable code is not breakage) · the entire tab-strip half is unreachable in the shipped app — `injectionKeys.ts:4` decl, `App.vue` unconditional `provide(TABS_EXTERNALLY_MANAGED_KEY, true)` **(re-measured this seat at origin/master: `App.vue:176`; the bank's `:169` is a stale-HEAD anchor)**, `ChannelControls.vue:277` inject ⇒ `:56 v-if="!tabsExternallyManaged"` permanently false ⇒ KfPillTabs (:74-82), `stripOptions` (:313-324), the `extraTabs` pipeline, `useTabStripScroll` (:398, executes forever effect-free), `.tabs-overflow-*` (:441-455) and the T.G9 warm (:79-80) never paint · **cure = DELETE the branch whole; census S-1's "It is live, not dead" is CORRECTED — the render SITE is live, the rendered NODE is not.** **LOCK (a)**: carry RR-D's runs-vs-renders split — "never renders" and "runs and does nothing" are different dispositions and the deletion inherits the second, larger saving. **LOCK (b)**: **re-home the T.G9 interaction warm onto ChromeDock's control `<Select>` BEFORE deletion, else the perf mechanism dies with the branch (L-6).** · sequencing: heads family A; every A-row discharges with it · corroboration riders banked at kf-KfPillTabs: `SpringPhysicsFacet.vue:7` + `useSpringDemo.ts:61` in-tree prose ("the KfPillTabs strip is gone"); `font-roles.json:28-33` records the component as "a T.H gated-on-publish excision".
- **CC-C-6 ≡ KPT C:C-1 / L:D-3 / D:D-10** ⟨kf-ChannelControls C-6 + arbiter rider; kf-KfPillTabs fold⟩ · **MAJ** · the fork's whole rationale is void against installed 7.0.0: `SegmentedTabsSemantics = "toggle" | "tabs"` + `semantics?:` decouples material from ARIA role; `dist/tabs.js:232` emits conditional `aria-orientation`, killing the 4.0.1 complaint; the file contradicts itself (pill :57-65 vs `variant="underline"` :220-228) · **cure = adopt `SegmentedTabs semantics="tabs" variant="pill"` + `useTabRovingFocus`.** **LOCK (arbiter rider, verbatim)**: `KfPillTabs.vue:45-46` — *"This local tablist remains until the published Glass component can provide pill material with tablist semantics and roving focus"* — **that condition is MET by installed 7.0.0; even the fork's newest justification is stale.** · sequencing: with CC-§0 · emitted DOM → SS-13, non-gating (§0 makes it delete-not-port either way).
- **CC-D-2 / C-3 (+N-4) ≡ KPT D:D-6 / C:C-2 / L:D-5 + both readers' missed-M2** ⟨kf-ChannelControls; kf-KfPillTabs⟩ · **MAJ** (D's BLOCKER killed) · orphan `role="tabpanel"` regions with no tablist, no `role=tab`, no id/`aria-labelledby` anywhere in the shipping document — **four sites, one exposed at a time**, and `ChannelControls.vue:29-31` names the rule it breaks · **cure = the panels' naming story is DECIDED WITH the strip's fate; NEVER un-hide the strip to "fix" it.** **LOCK**: the fourth panel is grep-invisible — `CubeScene.vue:171-180` renders it through `h()`, raw-store-gated, no `tabindex`; *attribute greps do not see `h()` prop objects and this repo uses render functions for scene bodies.* **Method note is part of the row.** · sequencing: a11y decision lands in the same commit family as CC-§0; pairs with banked ACG D-5/M-6 on the teleport-destination side · killed: C:C-2's APG-as-MUST inflation (the APG is non-normative; `aria-controls` is SUPPORTED, not required, on `role=tab`).
- **CC-D-8 ≡ KPT D:D-9 / C:C-4 + RR-2 missed-M5** · **MIN** · the overflow probe measures a box that can never scroll (`.kf-pill-tabs` is `inline-flex`; `overflowStart = scrollLeft > threshold` pinned 0 at `useScrollFade.ts:96`) while `scrollActiveTabIntoView` scrolls the header; `.tabs-overflow-left/-both` unreachable; indicator desynchronised by construction · **cure = delete the composable + the three mask rules + the ref, WITH the branch.** **LOCK (RR-2's sharper cause)**: `tabsHeaderEl` is permanently null in the shipped app (the ref lives only inside the dead `v-if`) — the seam is **UNREACHABLE**, not merely mis-pointed. Citation corrections banked: **4** mask declarations not 8; the x-branch is `:96` not `:97`.
- **CC-N-2** ⟨kf-ChannelControls, RR-D net-new⟩ · **MIN** · glass-ui 7.0.0 ships `<FadingScroll>` — the exact primitive `useTabStripScroll` + `useScrollFade` + the 15-line mask block hand-roll — and `ChannelControls.vue:51-54` **names it while hand-rolling anyway** (`axis`/`fadeStart`/`fadeEnd`/`ariaLabel`/`ariaLabelledby` all published) · **cure = delete-the-dead-fade AND adopt-the-vendor-primitive wherever a fade is actually wanted** (a second demo consumer of `useScrollFade` exists at ControlsPaneWrapper) · a clean glass-ui-first precept row **none of the three axes raised** · sequencing: the adoption half joins family I with KSM R-2/R-4's FadingScroll composition — ONE FadingScroll decision for the wave.
- **CC-D-9 ≡ KPT D:D-5 / L:D-4 / C:C-3.1** · **MIN** · `.glass-wash` nested in `.glass-wash` — the same ladder rung twice (plate, blur, grain, rim, 1px border, `contain: paint`, specular `::before`, each doubled), equal 12px radii at 8px/2px insets, non-concentric by construction; glass-ui's own pill strip composes `.glass-capsule-track` instead (no border, no contain) · **cure = one rung.** Rider: no de-duplication rule exists in `glass/*.css` and the component offers no material opt-out prop — *"no way to be right"* → the opt-out ask rides the BH relay · composited magnitude → SS-13.
- **CC-N-1 ≡ KPT C:C-3.2 (+D:D-14 horizontal half)** · **MIN** · the `.kf-pill-tab` keyboard focus ring is clipped ~1px at the tablist's **own** `contain: paint` padding box (track padding 2px vs outline 2px + offset 1px = 3px) · **unified geometry (adjudicated)**: perimeter clip top/bottom of EVERY tab + outer edges of first/last only; interior ring edges **overlap the sibling** across the 2px gap and are overpainted — two failure modes, not one · **cure = `outline-offset: -1px` + widened ring, an inset ring, or drop `glass-wash` from the tablist (which D-9 wants anyway) — never a geometry change.** · killed: D:D-14's vertical half ("2+1+2 = 5px of room — the ring survives") totalled slack to the WRAPPER's boundary and never accounted for the strip's own containment — **the same wrong-boundary error the D axis committed twice across two corpora in one formation.**
- **CC-N-3 + CC-C-15 ≡ KPT D:D-8** · **MIN** · the `.kf-pill-tab` skin is a **second authored copy** of the DEAD `.tab-trigger-base`/`.tab-trigger-pill` rules (identical `color-mix` 5%/8% fills, identical `--radius-lg`, identical three-property transition incl. font-weight, identical 500→600; differing only in padding/size-token/line-height) — 48 lines of `.tab-trigger-*` with **zero consumers**, both copies unreachable · **cure = ONE pill skin deleted across two files** (`ChannelControls.vue` scoped block + `tab-idiom.css`). **Token rider (load-bearing)**: glass-ui ships semantic `--radius-strip: 0.75rem` and `--radius-tab: var(--radius-pill)` for exactly this object; the strip reaches for the generic panel/lg pair — *right arithmetic, wrong register*; **the INNER pick (`--radius-lg`) is off-ladder too — both rungs wrong.** **Prose rider (rides the same commit, KF-CE-41)**: `ChannelControls.vue:213-214` "survive for the scene tab triggers" is false; `tab-idiom.css`'s own header ("imported by AnimationControls.vue … loads whenever AnimationControls mounts") is false — it loads globally via `design-idioms.css`. **Registry rider**: `font-roles.json:16-26`'s `filing-tab`/`filing-tab-active` rows select `.tab-trigger-base[data-state=…]` — selectors that match nothing, **a green registry gate over an empty set**; dies with the delete · killed: "every future edit to the house pill skin must now be made twice" — there is no second painter; both sides are dead code.
- **CC-D-10 ≡ KPT D:D-7 / L:D-7 / C:C-9 / C:C-10** · **MIN** · `transition: font-weight` on a **variable** face (`dist/styles/fonts.css` `font-weight: 200 800`) animates the strip's own metrics — every pill right of the active one slides on each selection change — under a comment claiming "only the activation channels change", which lists the one metric-affecting channel · **PRM users spared by the substrate blanket; nobody else.** **KILL LOCK (kill-of-a-joint-confirm — do NOT re-derive)**: the "no PRM guard reaches this component" cell that BOTH hostile re-readers certified is **FALSE** — installed `dist/styles/utilities/a11y-overrides.css` is a universal `*:not([data-allow-motion])` blanket (transitions restricted to opacity/color/background-color/border-color/box-shadow at 0.1s; animations clamped 0.01ms×1), imported as the FIRST line of `accessibility.css`, which `index.css` imports last, reached via demo `style.css:3`. Font-weight is EXCLUDED from the allow-list ⇒ the 500→600 step SNAPS. Any PRM census scoped to demo/ is *an exact census of the wrong scope*.
- **CC-L-17 / C-11 ≡ KPT D:D-16 / C:C-5 / L:D-12** · **MIN** · icon metadata resolved from SURFACE_META, carried on `builtInTabs`, **silently dropped** (`KfPillTabOption = {label,value,disabled?}`; extras pre-stripped :318-321, builtins spread through untouched) — two treatments of one non-fact, and a capability regression vs `SegmentedTabOption`'s `icon?`/`tooltip?`; the sibling dock host renders it · **cure = the census S-1 capability record: the successor MUST carry icon/tooltip or the capability is declared retired in writing.**
- **CC-D-11 ≡ KPT D:D-11 / D:D-12** · **INFO** · fluid `--type-small` in a fixed-padding chip; half the token pair consumed (leading inherits body); the `line-height: 1.75rem` pin was dropped from the fork · **cure = glass-ui's `text-small` utility, which binds the pair.**
- **CC-L-6** · **MIN** (L's MAJOR killed on RR-L's bounds) · the T.G9 interaction-warm contract is void in the shipped shell — the warm hooks ride only the dead strip (two sites) · **cure = the re-home obligation on CC-§0 LOCK (b); damage bounded by the rIC(4000)/1500ms fallback** · latency delta → SS-13.
- **CC-C-13** · **MIN** · a "single DOCUMENTED vendor-DOM contract" exemption is claimed for a raw `querySelector` into a **first-party** 124-line SFC — the docblock names `<SegmentedTabs>`/sonner; the strip is KfPillTabs, one `defineExpose` from a real handle · **cure = dies with the delete; the prose half rides family C.**
- **KPT D:D-2** ⟨kf-KfPillTabs⟩ · **MIN** (BLOCKER killed twice — by the axis's own §5 and by RR-1) · the active chip is an 8% same-family tint, **structurally incapable of 3:1 against its own track at ANY backdrop** (ceiling ≈1.2:1, backdrop-independent, derived three times) · **cure = design-quality spec input IF any pill idiom is reinstated; NOT a WCAG 1.4.11 failure** (1.4.11 excludes text; the state is text-carried; the 5%/8% recipe is house idiom per `tab-idiom.css:56-61`, not this file's invention) · **TOKEN CORRECTION LOCK**: the track is `var(--glass-plate-tinted)` under two washes, **not `--card`** — **no specific ratio in this corpus is bankable** (the S-B lesson: a reproducible table can still be a table about the wrong stack) · *dissent: RR-2 holds MAJOR — "certain arithmetic deserves the grade regardless of latency"; if this wave ever REINSTATES a pill idiom instead of deleting, D-2 and D-3 revert to MAJOR inputs at that spec's open.*
- **KPT D:D-3** · **MIN** (demoted; RR-1+RR-2 both MAJOR, preserved) · `orientation="vertical"` ships correct ARIA + correctly remapped keyboard (`useKfPillTabs.ts:67-69`) over a layout that stays horizontal (`.kf-pill-tabs` declares no flex-direction; **zero** CSS anywhere consumes `aria-orientation`), with a green test (:186) certifying the working half · latent-squared (dead strip AND the sole call site never binds `:orientation`) · **cure = the test MIGRATES (see KPT-SUP-4), the half-feature dies.**
- **KPT D:D-4** · **MIN** · hover-inactive vs active differ only by a ~3% tint delta (≈1.09:1) plus the 500→600 weight step · **cure = compounds D-2; dies with the delete.** *Internal-coherence note adjudicated as part of the row: the corpus cannot hold the weight step imperceptible (D-4) and simultaneously a perceptible jitter defect (D-10) — if either is true the other weakens.*
- **KPT D:D-21** · **MIN** · `border: 0` (:91) reserves no space for the producer's `prefers-contrast: more` / `forced-colors: active` twins (`[aria-selected="true"] { border: 2px solid !important }`, both verified in `accessibility.css`) — selecting a tab grows the content-sized pill **4px per axis** inside a 2px-padded strip · **cure recorded: `border: 2px solid transparent` at rest — a MUST-CARRY acceptance criterion on the successor rung.**
- **KPT L:D-9 / C:C-11 + RR-2 missed-M6** · **MIN** · the selection guard exists **three times**; the SFC's own `select()` (:63-65) — the ONLY copy not backed by the composable — has **zero** test coverage (the harness never renders the SFC; `onClick` = 0) — *the duplicated branch is precisely the untested branch* · **cure = one guarded `select` passed to both the click path and the composable; closing this hole is part of what the test migration must deliver.**
- **KPT L:D-14** · **MIN** · `focusTab` hard-codes an undocumented host shape (direct-children crawl :52-62; the handler must sit one level below the tablist :89) into a composable exported at **two** public paths; neither the params type nor the 17-line docblock states it; **the test harness reproduces the flat shape by hand, RATIFYING the assumption rather than checking it** — any wrapping host (tooltip trigger, icon span, fade track) silently reopens the a12-F1 regression class by markup alone · **cure = an ACCEPTANCE CRITERION on the `useTabRovingFocus` swap, not a fix to dead code.**
- **KPT L:D-13** · **MIN** · the disabled branch is unreachable in-tree (no supplier sets `disabled`), and the permitted selected∧disabled state is internally inconsistent and untested: `aria-selected="true"` on a native-disabled (unfocusable) button while the roving stop sits on an `aria-selected="false"` sibling · **cure = dies with the delete; the state question rides the successor's contract.**
- **KPT D-minor cluster (D:D-15 · D:D-19 · D:D-20 · D:D-22 · D:D-24)** · **MIN** (cluster row) · (D-15) `:80-123` exposes zero component-scoped theming custom property while `font-roles.json` selects on the `.kf-pill-tab` **vanity class** — the de-vanitising is a registry migration; (D-19) `ariaLabel?` has no default and no `aria-labelledby` alternative — an unlabeled tablist is one omitted prop away; (D-20) arrow mapping is hardcoded physical-LTR with no `dir` read (`dir=` grep = 0 app-wide, latent); (D-22) the `v-for` renders empty/single-option strips unguarded; (D-24) `@container style(--glass-backdrop: light)` re-points `--muted-foreground` inside wash rungs — unreachable today, **a real repaint hazard the moment any `glass-floating`/`glass-overlay` ancestor appears** · **cure = all die with the delete; D-15's registry migration and D-24's hazard are carried into family D's audit.**
- **KPT RR-2 missed-M7** · **INFO** · `Math.max(0, findIndex(...))` (:70-74) coerces a contract-violating `-1` to `0`, converting an unmatched `value` into a plausible-but-wrong traversal instead of a detectable miss; the `:70` comment **ASSERTS** the invariant rather than enforcing it · **cure = early-return on -1** (same silent-degradation shape as L:D-14) · spec note for the successor.
- **KPT-SUP-4 / Killed #7 — THE PRESERVATION SPEC (MUST-CARRY, not optional)** ⟨kf-KfPillTabs superlatives + Killed #7; kf-ChannelControls SUP-4⟩ · **SPEC** · `test/demo/instrument/KfPillTabs.test.ts` is **a real interaction gate with a recorded bite**: `document.body` mount for live `activeElement`, genuine bubbling KeyboardEvents from the focused element, focus AND selection asserted together across **7 cases** (:130/:138/:153/:162/:173/:186/:195), kill condition documented at :20-22 — a component that got MORE rigorous after shipping a HIGH regression (a12 F1). The axis clause "…maintained for a branch that cannot execute" is **KILLED**: the test drives the COMPOSABLE through its own `createApp` host and never touches the SFC or the dead branch · **CURE-SHAPE LOCK, verbatim: MIGRATE the gate onto whatever occupies the seam (`useTabRovingFocus`) — NEVER delete-with-the-rest.** Carry into the migration: (i) `rovingValue`'s four-line Tab-unreachable-proof contract (selected-enabled → first-enabled → undefined only on an empty enabled set, gated by :195); (ii) L:D-14's host-shape criterion; (iii) D:D-21's forced-colors reservation; (iv) missed-M6's untested click path — **the gate's blind spot is part of what migration must close**; (v) the getter-thunk composable-params idiom (`KfPillTabs.vue:68-73` passes options/orientation/modelValue as thunks invoked per-call — the correct Vue 3.5 reactive-destructure cure, zero `toRef` ceremony, named by value.js's own census as the demo's reference implementation) — **preserve or replace deliberately, never lose by accident.**
- **ACG M-1 (tabs half) ≡ KPT L:D-1 / L:D-2 mechanism** ⟨kf-AnimationControlsGroup, RR-D net-new; kf-KfPillTabs fold; live carrier at kf-KfPillTabs N-KPT-1⟩ · **MAJ-latent** · the demo's global scrub shortcuts collide head-on with the tablist: `useControlsKeyboardShortcuts.ts:53-58` registers Arrow/Home/End **window-level with `preventDefault: true`**; glass-ui `dist/keyboard.js` ignores `defaultPrevented` (grep = 0) and its skip predicate covers only INPUT/TEXTAREA/SELECT/contentEditable/`.monaco-editor` — a `<button>` is not skipped; `useKfPillTabs` preventDefaults but **never stops propagation** · **cure = SPEC INPUT, NOT a fix here: any successor tablist — including glass-ui's own `useTabRovingFocus` — MUST be audited against the same registry, because the collision survives every tablist implementation.** The demo-side scoping cure and the producer seam are **NO-WAVE-OWNER + BH relay**, not this wave's code · **live carrier proof (do not re-derive)**: the identical leak is CONFIRMED-LIVE one component over at the KeyframesEditor toolbar (`useToolbarKeyboard.ts:80-101`, zero `stopPropagation`) · killed: the live BLOCKER/MAJOR framings against the strip itself — **no pill can hold focus in the shipped app.**
- **KSC N-9** ⟨kf-KeyframesStringControls, RR-D missed-M2 rescoped⟩ · **MIN-latent** · two composables own competing `focus()` calls on one activation — `useKfPillTabs`'s roving-focus restore vs `useKeyframesPaneReveal`'s panel focus (`:123-136`) — one of which is dead by construction · **cure = the S-1 decision must pick ONE focus owner and say so** · ordering witness → SS-13 only if a strip is ever restored.

### B · F-5 SHIMS + DEAD BARRELS (the no-backwards-compat deletes)

- **CC-L-15 / C-10 ≡ census F-5 ≡ KPT L:D-11 (+D:D-17 shim half)** · **MIN** · the 4-line `transport/composables/useKfPillTabs.ts` back-compat shim, **sole consumer `ChannelControls.vue:230`**; direct `feedback_no_backwards_compat` violation; all 4 shim files present at origin/master · **cure = delete the triple together under §0.** **MUST-CARRY RIDER (RR-2, load-bearing — the deletion is WRONG without it, verbatim)**: *"because the advertised path was never implemented, the F-5 shim is the only working type path — its deletion must repoint `ChannelControls.vue:230` at `../KfPillTabs/useKfPillTabs`, **NOT** at the `.vue`."* **Second rider**: the duplicate-name hazard (`KfPillTabs.vue` **beside** `KfPillTabs/`) rides the same row (census §7.4) · sequencing: after CC-§0's delete decides the consumer's fate.
- **KPT N-KPT-2** ⟨kf-KfPillTabs, RR-2 missed-M4 refiled from its own kill; kf-DemoGlobalChrome M-L3 independent confirmation⟩ · **MIN** · **the ENTIRE instrument barrel chain is dead** — `demo/components/instrument/index.ts` (four `export *`s under a 22-line laziness docblock) has ZERO importers, and `transport/index.ts` (the `defineAsyncComponent` wrappers for AnimationControlsGroup/TransportDock/KfPillTabs at :8-12) is referenced **only by that dead file**; the code-split both docblocks promise is collected by nobody — the Monaco-laziness guarantee is actually delivered by ChannelControls' own `defineAsyncComponent` panes · **cure = one deletion family with the F-5 shim.** Subsumes L:D-10/C:C-7 (the KfPillTabs row alone) · *a kill that produced a sharper row: L/D-10's "`index.ts:24` keeps the dead factory reachable from the wider barrel" is FALSE — the wider barrel is itself importer-less, so nothing is kept reachable.* **Note (M-L3)**: these dead exports wear a load-bearing-sounding T.F5 rationale comment — *dead exports wearing a rationale comment are the shape that survives audits* — and are also the only guard on a duplicate-mount hazard; the deletion states what replaces that guard.
- **ACG L-5** ⟨kf-AnimationControlsGroup⟩ · **MIN** · **census F-5's "zero consumers" evidence for the `useAnimationGroupPlayback` shim is WRONG** — the one consumer is `test/demo/instrument/useAnimationGroupPlayback.test.ts` (the lane's scan root excluded `test/`) · **cure = correction of EVIDENCE, not of law: the no-backwards-compat charge stands; the deletion wave must repoint the test import — and per ACG L-1 that test must MOVE TO THE COMPONENT SEAM in the same stroke, not merely repoint** (a mounted-component or wrapper-exported test that REDs on the current tree) · sequencing: the seam move is ACG's NO-WAVE-OWNER obligation; the repoint is this wave's — they land together or the gate goes green over the defect.
- **KF-AV-27** ⟨kf-AnimationVisualizer C-12⟩ · **MIN** · `useRafLoop` is a six-line pass-through whose docblock describes the engine's `RAFPlayback.loop` driver while the real contract is `useDemoTicker`'s shared module singleton + per-subscriber gating + visibility reconcile — **the hiding is what let KF-AV-1's guard look sufficient**; census closed: 2 consumers, both on the singleton (the third filename hit is a comment) · **cure = extends F-5's shim class: inline it or re-document it** — never leave a shim whose docblock names a different mechanism than the one it forwards to.

### C · S-2 · PROSE-WITH-CODE (the KF.W4-PROSE limbs ruled here; K2 commit discipline: delete + correction = ONE commit)

**Family law (KF-CE-41, verbatim, governs every row in this file that touches a comment): the comment is part of the defect surface — the cure commit carries the comment.** Its generalisation is the kf-SquareScene class: **comment-stated invariants are test obligations.**

- **CC-L-19 — the phantom-authority class, non-gate limbs** ⟨kf-ChannelControls L-19 + C-6 riders + N-7; ≡ kf-KfPillTabs C:C-1/D:D-8/L:D-8 ≡ kf-SpringTrace L-4/C-8, L-7/C-6, L-13/C-7 ≡ SPF-27 ≡ KF-CO-33/-37⟩ · **INFO/MIN each, MAJOR BY VOLUME** · ~45% of `ChannelControls.vue` is provenance prose with counterfactual blocks: the `.tab-trigger-*` "HERE" claim (:207-214) against a scoped-only style block; `<SegmentedTabs>` ownership prose across five sites; "no scroller is wired here" (:41-55) against the scroller at :398; all three colocated composables self-documenting as `AnimationControls.vue`'s · **cure = delete-and-correct in ONE commit with the code.** **SPLIT LOCK (adjudicated at KF-W4 §11 ¶1): the phantom-AUTHORITY limb — every counterfactual `proof:*` cite (`design-idioms.css:3`, `DESIGN.md:236`, SPF-27's `:118`, KF-CO-33's three `proof:bezier-*`, KF-CO-37) — rides KF.W4's gate G-KFW4-8, NOT this wave. Every other limb is this wave's.**
- **CC-L-18** · **INFO** · "THE ONE WRITER" is **N writers** (per-host watch instances over one shared bucket; `!==`-guarded so the effect is benign — **the invariant NAME is false**, not the behaviour) · **cure = correct the name or make it true; a false invariant name is what a future gating edit trusts.**
- **CC-N-7 ≡ KPT L:D-8 / C:C-6** · **INFO** · `KfPillTabs.vue:41-44` asserts a type re-export `<script setup>` **cannot perform**, above a plain `import type`; the claimed import path has zero consumers (the sole consumer routes through the F-5 shim) · **cure = joined to the B-family repoint rider — the prose and the shim delete are one act.**
- **KPT header mangle (RR-1 missed-M5)** · **INFO** · `KfPillTabs.vue:2-5` is **broken text** — an unterminated code span opening at `` `<SegmentedTabs `` and a dangling `:aria-orientation=` split across lines — *"the first thing any reader meets, parseable only by reconstruction,"* on a file whose comment-to-code ratio the axes measured at ~36%. **Seen by neither axis, neither reader, nor the bank.** · **cure = dies with the file; recorded because it is the corpus's proof that prose goes unread even by prose auditors.**
- **KPT C:C-1 phantom-consumer riders** · **INFO** · `KfPillTabs.vue:4-5` names **two consumer files that DO NOT EXIST** (`*SpringSidebar*` / `AnimationControls.vue`, `find` = 0) while never naming the one real (dead) consumer; the header argues against "glass-ui 4.0.1" with 7.0.0 installed · **cure = dies with the file; the CLASS is family-wide (below).**
- **SpringTrace L-4/C-8 · L-7/C-6 · L-13/C-7** ⟨kf-SpringTrace; class-precedent kf-KfPillTabs⟩ · **MIN/MIN/INFO** · (L-4) the header's reason-for-existing — "the curve drawn BESIDE its string", claimed **three times** — is architecturally impossible: `SpringScene.vue:10-11` forks the trace and the string into mutually exclusive `v-if`/`v-else`, and the named sidebar does not exist; (L-7) the style header claims "the scene's red identity" while `--color-progress` → `--accent-kf` = **violet** (oklch hue 295/305) and the retiring ruling ("red exits the chrome entirely") sits in the same token block; (L-13) `useSpringLinearStops.ts:9/:12` cites the deleted `SpringSidebar.vue` and a false present-tense census (the "ONE surface" invariant itself HOLDS — 2 consumers) · **cure = retire or re-home the claim; booked at CLASS weight citing the phantom-consumer precedent, not re-derived.**
- **SPF-27** ⟨kf-SpringPhysicsFacet⟩ · **INFO** · prose-drift bundle: **41% of the template is tranche changelog naming dissolved files**; the dead `SpringSidebar` name is still load-bearing in the consumed primitive's own `framed` doc (`KeyframesEditor.vue:5-6`); the `SpringPreset`/`SpringTrack` re-export hop's rationale names "the sidebar" · **cure = one prose motion with the family; the `:118 proof:easing-sidebar-normalized` cite is the AUTHORITY limb → KF.W4.**
- **KF-CO-33 / KF-CO-37** ⟨kf-ChannelOptions D-20 = L·N-11/N-12 + RR-A's three further inert declarations; C·m-1⟩ · **MIN** · dead hooks and dead layout: `.panel-stack` styled nowhere; `easing-edit-btn` a "NAMED BEHAVIORAL SEAM" with zero consumers; outer grid `gap-4 items-center` with one in-flow child; CardContent `gap-2` (one child); both `relative`s with zero absolutely-positioned descendants; and `LayerConfigPanel`'s header comment cites a host `:deep` rule ChannelOptions **deleted** (tombstone :594-603) · **cure = hygiene sweep riding whichever wave touches the pair; the R3 609-line ceiling breach rides the same row.** *(The behavioural body of ChannelOptions is OPTIONS-UNIT's, NOT this wave's — see §CrossEdges.)*
- **CC-C-4 (extraTabs) ≡ ACG C-4 ≡ CPW N-1/C-5 ≡ EditorShell L-15/C-9 ≡ KF-APP-12 — THE DEAD-TABS PLUMBING, DELETED END-TO-END** ⟨four records, one identity; census S-2's sharp instance⟩ · **MIN/MAJ** · `extraTabs` is a **3-hop dead prop** typed against a renderer that drops two of five fields (`SegmentedTabOption`'s `icon`/`tooltip` have no renderer in `KfPillTabs`; defaulted `() => []`, nothing overrides); the `#tabs-trigger` scoped-slot chain is forwarded through **FOUR** ancestors (App → EditorShell → AnimationControlsGroup → ControlsPaneWrapper) into a child declaring only two `tabs-content` slots (:32, :180) — **dead at both ends**; `EditorShell.vue:92-94` additionally severs its scoped slot props (`#tabs-content` bare) while both siblings forward with `v-bind`, so the barrel-exported public hop is the one that drops `selectedAnimation`/`isPlaying`; the file's own :47-51 "retire" claim contradicts the live chain · **cure = the chain deletes end-to-end in this wave; a partial delete leaves a contract nobody can honour.** **KILL LOCK**: C-4's running-cost sub-claim was KILLED at the bank by a `compileTemplate` run (`_withCtx` lazy closure, never invoked) — **do not re-import it as a cost argument.** · sequencing: with family A's delete; only the three born-RED **type** riders (four `any` props, the field-loss bridge, the `activeSnapPoint` variance) go to KF.W4.
- **KF-EST-1 — in-file comment cures (AUTHORITY limb excluded)** ⟨kf-EditorStartScreen, both readers concordant⟩ · **MIN** · eight `proof:*` citations across two files name gates deleted by `70b32501`/`92746148` **as binding authority** — `:15` styled as an OWNER lock, plus `:56`, `:134`, `:159`, `:180`, and `TypingDots :45`, `:51`, `:59`; `package.json` retains exactly `proof:publish`/`proof:owner-golden` · **cure = the GATE law is KF.W4's; the in-file comment cures ride THIS wave's file-touch — with the deletion commit's "direct product checks" posture read FIRST.** **No new grep-idiom `proof:*` mark is minted by this wave** (value.js house law, owner 2026-06-02: the grep-based invariant idiom is "overfit junk").
- **KF-AX-11 / KF-AX-12 / KF-AX-31** ⟨kf-CubeAxisLines⟩ · **MIN** · (AX-11) the `--z-behind` demo re-declaration vs the vendor token is **not a stray to delete but a POLICY CONTRADICTION to RESOLVE** — `layout.css:8-11` documents the shadow as deliberate override policy ("OUTSIDE @layer … authoritative for the demo") while `style.css` self-contradicts; **two written laws disagree**; (AX-12) the census is wrong about this file at its own HEAD twice (`raw z-index:-10` ×2), inherited from stale prose — the census re-anchor is KF.W0's, the **prose sites** are this wave's; (AX-31) the z-contract's cited enforcement is a **dissolved gate** — `style.css:38-39` says "`proof:brittleness` gates against drift"; `scripts/gates/` holds only `surface`/`visual`/`structure`; the same three-line block also carries AX-12's stale sentence — *a comment chain pointing to a contract pointing to a deleted enforcement, two falsehoods deep* · **cure = recut the z-contract block in ONE motion covering all three; the gate-inventory truth rides KF.W0.**

### D · THE PHANTOM-TOKEN AUDIT (the `--kf-*` namespace audit RE-CUT; kf-App D-15's recommendation, ratified at the bank)

**Family opening lock (AmigaScene MISSED-H, MUST FOLD BEFORE THE TOKEN LAYER IS TOUCHED, verbatim):** *"The demo DELIBERATELY shadows glass-ui's namespace — `design-idioms.css:9-11` states the override policy in its own words; `--amiga-red`/`--rainbow-red` are unprefixed BY DESIGN."* This **nuances, does not refute**, census FE §6.3's 98-unprefixed/zero-`--kf-*` row: prefixing `--amiga-red` would break the stated intent and — because Canvas2D silently ignores an invalid `fillStyle` — **ship a solid-white ball with no error.** **KF.W6 MUST fold this as a guard row before touching the token layer. The audit's product is a per-property disposition (RETAINED-BY-POLICY / PREFIXED / RETIRED), never a blanket rename.**

- **KF-APP-6 ≡ ChromeDock D-1 ≡ TD-13 ≡ EST ND-1** ⟨kf-App M-B/K-2 (BLOCKER at its bank); kf-ChromeDock D-1 + provenance riders; kf-TransportDock TD-13 (fourth refiling); kf-EditorStartScreen ND-1 (both readers, BLOCKER)⟩ · **B** · **`--dock-margin` is a phantom token at all three substrates** — zero definitions at worktree, HEAD, `origin/master`, and the producer; zero `@property` · **RE-MEASURED THIS SEAT at `origin/master`: `git grep -- '--dock-margin:'` → 0 definitions; `var(--dock-margin)` → 10 occurrences of which one (`TransportDock.vue:379`) is inside a prose comment ⇒ 9 LIVE UNFALLBACKED SITES, exactly as banked** (`layout.css:80,92,117,135,161,166,204` + `TransportDock.vue:389,395`) · the whole dock-anchor/work-area chain is invalid-at-computed-value-time: `ChromeDock.vue:216`'s `top` computes to `auto` (the φ-capped anchor is inert), CubeScene's recede lower-guard computes to 0, TransportDock's safe-area padding computes to 0; **fallback-carrying sites (the hero seat) resolve deterministically and MASK it** — *"the demo's entire layout authority chain runs through a token that does not exist at any substrate, masked by exactly the fallbacks that made the visible page look intended"* · **cure = define it or delete the chain; a fallback added per-site is the masking that hid it.** **Provenance rider (kf-ChromeDock, `git log -S`)**: the deleting commit is `c7f7c96d` "refactor(demo): modernize CSS tokens and remove redundant styles", body "Remove redundant color token declarations now in glass-ui defaults" — **the token died on a false belief about producer ownership; glass-ui 7.0.0 ships zero `--dock-margin`.** Second rider: the `@supports (anchor-name)` arm (`layout.css:159-170`) carries the same dead term and loses to the inline style regardless. **Comment-truth rider (EST)**: `EditorStartScreen.vue:82-86`'s "Derived ENTIRELY … No raw vh/px" is false below `lg` · **IDENTITY-GUARD EVENT (recorded, four times over)**: both ChromeDock re-readers filed this as a fresh confirmed BLOCKER eleven days after it was banked; TD-13 is its fourth refiling; ND-1 its third registry event — **cite the bank, do not re-derive** · rendered dock offset at 390×844 → KF.W9.
- **KF-APP-25 ≡ KF-SKEL-2** ⟨kf-App D-14; kf-App.skeleton D-3/L-3.1/C·D-1⟩ · **MIN/MAJ** · `--shadow-glass` is a **dead token** — zero declarations in installed glass-ui dist, demo styles, and the shipped bundle (this seat re-verified: exactly one consumer, `App.skeleton.vue:64`, fallback-carrying) — so the "glass plate" always paints the hardcoded 4%-black literal, **invisible on the dark arm** · **CURE RE-PRICED (the bank's own find, load-bearing — the obvious fix does not work)**: the suffixed bridge family `--shadow-glass-*` is **ALSO absent from the shipped bundle** (the `@theme inline` bridge emits only on utility reference — a scoped `var()` read does not trigger it), while the **backing family `--glass-shadow-{wash,quiet,resting,…}` IS emitted** — *"fix the name to the bridge spelling" would STILL paint the fallback* · **cure = delegate to a surface that owns its elevation; if any interim local fix ships it reads `--glass-shadow-quiet`/`-resting`, NEVER the bridge spelling.** **The bridge-emission trap itself is a first-class input to this audit.**
- **KF-APP-26 ≡ ChromeDock D-10 / m-11 / C-9** ⟨kf-App D-15 + K-1⟩ · **MIN** · `--dock-label-padding-inline` is a phantom (this seat: one consumer, `ChromeDock.vue:381`, fallback-carrying; zero definitions) — the real producer seam is `--dock-trigger-padding-inline` per density rung (0.4375/0.5/0.625/1rem × `--dock-scale`) · **cure = repoint to the real seam.** Riders: `DockSelectTrigger` **does not exist in 7.0.0** (the `:375` rationale names a retired primitive); the namespace-collision fear that motivated the local name is **refuted ∅** (triply derived) — *and that refutation is exactly why the audit was re-cut from "namespace collision" to "phantom token."*
- **SquareScene D-23 (+ SquareInstrument K-6 residue)** ⟨kf-SquareScene, reader-A's sharper instance; kf-SquareInstrument's execution attachment⟩ · **MIN, the sharpest §6.3 witness in the corpus** · **`--rainbow-{red,orange,yellow,green,blue,violet}` are declared by BOTH installed glass-ui 7.0.0 (`dist/styles/tokens/scale-paper.css`, oklch) AND the demo (`design-idioms.css:15-21`, hsl, unlayered-and-later)** — a live same-name `:root` collision held **only by import order** — feeding `useSquareTumble`'s `getComputedStyle` read straight into `parseCssColor`. Second instance in the same row: `--size` (`.demo-box` vs the global `.icon`) · **cure = the audit's flagship decision; the MISSED-H guard applies to `--amiga-red`'s alias (`design-idioms.css:24 --amiga-red: var(--rainbow-red)`).** **EXECUTION ATTACHMENT (do not re-open — kf-SquareInstrument K-6, reproduced by that seat against installed value.js 4.0.0)**: `parseCssColor("oklch(0.684 0.250 327.9)")` → `{ok:true}`; the mix and serialize both ok; **R1's true degenerate input is empty-args `oklch()`, which no CSS token can produce** (`getPropertyValue` yields `""` for an undefined token; `useSquareTumble.ts:17`'s `if (value)` skips empties) — *"safe only by cascade order" is FALSE: safe regardless.* The **namespace hygiene** is the surviving row, not a crash.
- **CubeScene D-11 ≡ RibbonBar D-3/m-1/C-2 ≡ SequenceScene D4 ≡ SequenceTarget D-16/L-13/C-6 ≡ SPF-6 ≡ PlaybackRibbon D-14/C-10/L-m1 ≡ CPW D-M11 — the `.btn-interactive` PHANTOM UTILITY** ⟨six records, one census-pinned figure⟩ · **MIN (CubeScene) / MAJ (CPW's deletion arm)** · `.btn-interactive` has **zero rules in the 571 KB shipped artifact, zero in glass-ui, and 8 consumer sites across 6 files**; provenance traced to the glass-ui `b0debec` retirement under a "false zero-site verdict" where only the two `.rainbow-*` were restored · **cure = the 8-site sweep + delete `RIBBON_BUTTON_CLASS` (CPW's deletion arm).** **Downgrade already applied and load-bearing**: no focus contract is lost at 7 of 8 sites because glass `Button` applies `.focus-ring` itself — **EXCEPT SPF-6's net-new rider: `SpringPhysicsFacet.vue:105` is a bare `<button>`, the ONE site the downgrade does not cover** (carried by SPF-8) · **gate rider (RibbonBar)**: `scripts/proof-phantom-classes.mjs` is named in `LESSONS-LEARNED.md:603` and **ABSENT from `scripts/`** — the phantom-class audit is the §6.3 widening (class space, not just custom-property space) · **class law (banked, binding)**: the value.js-side `btn-interactive` family (`ColorInput.md` D-2) stays **cross-referenced, NEVER merged**; the residue of what it once painted is unrecoverable from the tree and rides the sweep.
- **`.tap-floor` — the DEAD 44px UTILITY** ⟨kf-CopyButton KF-CB-25 (the roster's pinned count); kf-CubeScene MISS-3; kf-KeyframesStringControls D-20 + RR-D missed-M4; kf-KeyframesEditor KF-KE-45; kf-KeyframeCardList KC-23; kf-KeyframeCard KF-KC-33 premise correction; kf-SharePopover SP-11⟩ · **MIN** · `.tap-floor` (`design-idioms.css:81-85`) is a documented WCAG 2.5.5 utility with **ZERO adopters** — the only two grep hits are its own comment and selector · **cure = ADOPT-OR-DELETE, decided once for the whole demo.** **PREMISE-CORRECTION LOCK (binding on every row that cites it)**: rows arguing "against the demo's own 44px house practice" are **CORRECTED** — there is no house practice; what survives is (i) the SC 2.5.8 boundary math on gapless destructive/reversible pairs and (ii) **the in-house 32px answer that actually ships (`SharePopover.vue:31-39`)**.
- **KF-SST-30 — THE TOUCH-TARGET CORRECTION (annotation rider on two banked rows, E-3)** ⟨kf-StartingStyleTarget, reader-A MISS-2⟩ · **MIN here, PRODUCER-OWNED** · **glass-ui 7.0.0's `@utility touch-hit-area` CANNOT expand a tap target: its expander carries `pointer-events: none`** (verified verbatim; the dock's `::after` twin omits it and works) · **cure = kills the prescribed cure of kf-ChannelOptions D-23/KF-CO-24 and kf-CubeScene MISS-3 — both count the decorative twin among their "sanctioned cures" and are hereby ANNOTATED, not rewritten (E-3).** After the correction, **`.tap-floor` (a real box, zero adopters) is the only working in-reach mechanism** · producer half → BH relay (*one utility, two behaviours, the general-purpose name on the decorative one*).
- **RibbonBar RB-2** ⟨net-new, reader-DU MISS-2, sheet extraction by the seat⟩ · **MIN** · `.text-gold` is an **unlayered demo shadow of a LIVE glass utility of the same name** — BOTH rules ship (`.text-gold{color:var(--gold)}` glass, in utilities; `.text-gold{color:var(--color-gold)}` demo, unlayered and later, permanently winning) — and sharper than filed: **glass itself declares `--color-gold: var(--gold)`, so the demo shadows the CLASS *and* the alias TOKEN**, then re-points the token at `hsl(43 74% 49%)` under a comment claiming it "matches glass-ui's `--gold` base" (honest today, within 1/255) — *which is exactly why a producer retune will silently not arrive* · **cure = the token-namespace lane; corrects RibbonBar D-8's contrasting praise of `text-gold` as "the discipline the demo does own."** Third instance of the §6.3 hazard, **first at the class level.**
- **RibbonBar RB-5 ≡ EditorHeader EH-10** · **INFO/MIN** · `--scale-hover: 1.08` re-declared in the demo's unlayered `:root` (`design-idioms.css:41`) with a comment admitting the mirror; the producer's `scale-paper.css` value is **identical today** · **adjudication note the reader under-stated (carry verbatim): this is a ZERO-DELTA shadow whose entire cost is the future DS-retune no-op** — *a literal instance of lane-frontend §6.3's abstract hazard* · **cure = delete the shadow or document the pin; unbanked elsewhere (checked against kf-CopyButton/kf-CubeScene).**
- **DGC M-4** ⟨kf-DemoGlobalChrome, RR-D net-new, bundle-verified⟩ · **MIN** · **THREE definitions of the `shadow-lg` name coexist** in the shipped bundle — Tailwind's theme default, glass-ui's theme-aware `:root{--shadow-lg: 0 4px 20px var(--shadow-color)}`, and the emitted `.shadow-lg` **utility which inlined Tailwind's value at build time and reads NEITHER custom property at runtime**; glass bridges only the five `--shadow-glass-*` tokens into `@theme inline` · the demo's sole `shadow-lg` (this file) is the sole tripwire · **cure = the fold carries the component instance; the AUDIT owns the class.**
- **CPW D-m3 ≡ ACG D-12** ⟨kf-ControlsPaneWrapper; kf-AnimationControlsGroup⟩ · **MIN/INFO** · `--controls-idle-opacity` — a **WCAG-relevant magnitude** — is published unprefixed to the flat global namespace (`layout.css:41`), **restated as a hardcoded fallback at its sole consumer** (`.css:115`, the portalled-body divergence path), and mis-attributed to `design-idioms.css` by its own docblock (`usePaneHover.ts:22`); `@property --rail-width` is registered **document-globally**; sibling `--rail-track` instantiates the same hazard on an inheriting root with a measured collision surface of **zero** · **cure = disposition each per the audit's three-way rule; the docblock correction rides the same commit.**
- **KF-SKEL-17** ⟨kf-App.skeleton D-10⟩ · **MIN** · the `--color-*` triplet is a **1-of-1 deviation** (exactly one file) against 45 raw-form occurrences in 16 files — not a live bug (all three bridges emitted) but **a live tripwire on `@theme inline` emission**, the same mechanism as KF-SKEL-2's bridge trap · **cure = normalise to the raw form; the emission rule is the audit's standing instrument.**
- **KF-SKEL-7** ⟨kf-App.skeleton L-3.3, rescoped⟩ · **MIN** · **the census's import-line coupling instrument UNDERCOUNTS cascade coupling** — witness: `App.skeleton.vue` has zero imports and zero `@import`, sits inside lane-frontend's "21 `.vue` with no glass-ui import", **yet 4 of its 5 custom-property reads resolve ONLY through glass-ui's cascade** (nothing in `demo/styles/**` defines them) · **cure = the audit is the natural home of a cascade-level coupling probe beside the import census** (the re-count law itself is KF.W0's) · the phantom-dependency premise this row originally rode is dead at HEAD (SCH-1) — **the instrument critique stands on its own.**
- **KF-EST-16** ⟨kf-EditorStartScreen, L:L-EST-8's token leg⟩ · **MIN** · `--start-hero-band: 34dvh` (`layout.css:26`) — the token whose only job is clearing THIS hero — is a **raw-dvh hand-fit that cannot track the derived, ResizeObserver-fed band it approximates** · **cure = derive it or retire it; joins KF-APP-6/-25/-26 in the raw/phantom-token pass.**
- **KSM R-21** ⟨kf-KeyboardShortcutsModal; relocated out of a killed row — reader-A missed[0] + reader-B M6, merged⟩ · **MIN** · **the real `--panel-max-h` defect is at its DEFINITION**: `design-idioms.css:46` says "caps mobile panels"; consumer 1 is the expanded-timeline cell placed into the **desktop** rail/bottom grid cell (`AnimationControlsGroup.css:219-222`, with the mobile `position:fixed` variant at :179-184); consumer 2 is a **centred modal**; and `DESIGN.md:132-138` describes a U.B geometry move out of `design-idioms.css` that `layout.css:9-11` re-asserts **never landed** · **a token whose every call site contradicts its charter, in two documents that disagree about where it lives** · **cure = the audit's sharpest worked example — a SEMANTIC collision, not a name collision.**
- **KF-KE-30** ⟨kf-KeyframesEditor D-12 + D-13⟩ · **MAJ** · `.focus-ring` is a **REALIZED class-name collision**: glass-ui's layered copy (with `border-radius: var(--radius-pill)`) vs the demo's **deliberately un-layered** copy (without it) — the demo silently reshapes the vendor primitive's focus geometry **repo-wide**, and `KeyframeCard.vue:45` consumes the ambiguous name · **cure = resolve the collision; this EXTENDS census §6.3 from custom-property space into class space and is booked as a §6.3 annotation (E-3 addendum, never a rewrite).** · sequencing: heads family F (the focus contract cannot be specified while the class is ambiguous).
- **KF-CE-22** ⟨kf-CSSCodeEditor D-14⟩ · **MIN** · all geometry is JS-prop pixels; **zero CSS surface**; `--panel-max-h: 60dvh` unused · **cure = sharpens census §6.3 in the OPPOSITE direction — here the problem is the ABSENCE of a token surface, not a collision.** The audit's disposition table must have a third column for it.
- **KF-AX-25** ⟨kf-CubeAxisLines, both readers, compiler-proof re-run independently⟩ · **INFO** · scoped `@property` registers **document-wide**; the `--color` flat-namespace pair; an `initial-value` that **poisons future fallbacks app-wide** · **cure = the namespace audit owns it; second mechanical confirmation on record.**
- **SPF-32** ⟨kf-SpringPhysicsFacet, RR-1's census sharpening⟩ · **INFO, a PRICING row** · the flat-namespace hazard is **LATENT at this site, not live** — `--ball-size`/`--ball-glow`/`--rail-tint` are shipped by glass-ui **nowhere**; the remaining reads are intentional token reads · **cure = none. Recorded so KF.W6 prices this file correctly: NO RENAME SPEND HERE.**
- **KF-SP-3 (SequencePlayhead C-3) + the D-7 residue** ⟨kf-SequencePlayhead⟩ · **MIN** (pending the DESIGN.md-bindingness owner ruling; both r4 readers concordant) · `DESIGN.md:53-57` names the playhead cap a `--specular` consumer; **the token exists nowhere** (the prose is the sole tree-wide hit; glass-ui's `--specular-x/y/intensity/angle` are positional registers, not a colour mix) and `:63-64` ship the raw `white` the same paragraph forbids · **cure = define `--specular`/`--shade` as `light-dark()` pairs and repoint `:63-64`; ONE cure with the D-7 residue; cross-repo relay if the register is producer-owned.**
- **SequencePlayhead R.F8 annotation rider** · **INFO** · **"the discharged remedy IS the defect"** — F8's accepted remedy (a comment naming the stacking semantics) now exists at `css:20` **and is false**: `.seq-storyboard :21-25` creates no stacking context (no position/z/transform/containment) and the handles' `z:2` is trapped by `.seq-track`'s `container-type :114` · **cure = the annotation rides this wave's file-touch; the CONCLUSION survives on re-derived reasoning, the AUTHORITY does not.** Booking-kills already fired three times against re-files of this row — **do not re-open.**

### E · TAP-FLOOR / TARGET-SIZE / COARSE-POINTER

- **KF-CO-24** ⟨kf-ChannelOptions D-10⟩ · **MAJ** · the easing pencil is a **24×24 CSS-px** target and glass-ui's coarse-pointer floor **EXPLICITLY excludes `compact`** (`:not(.dock-icon-button--compact)`; compact padding default `0.25rem`; icon-sm 16px); Back is 28×28 on the same class · **cure = one of the sanctioned mechanisms — BUT SEE KF-SST-30: `touch-hit-area` is `pointer-events: none` and CANNOT expand a target, so this row's second "sanctioned cure" is void and the row is annotated accordingly (E-3).** Rendered boxes → SS-13.
- **KF-CB-5 / KF-CB-14 ≡ KF-ET-17 ≡ KF-KC-33 ≡ KC-23** ⟨kf-CopyButton D-4/D-5; kf-EasingTarget D-M-9; kf-KeyframeCard D-24; kf-KeyframeCardList D-13⟩ · **MAJ/MIN** · CopyButton has **zero intrinsic box** — every child is out of flow (both glyphs `position:absolute`; `.sr-only` absolute per glass `components.css`) — so the sizing contract lives in **a consumer's CSS comment** (`EasingTarget.css:68-74`) and the abspos `width:100%`-against-padding-box algebra makes *"a 16px glyph in a 32px target"* **structurally inexpressible**; four sites, four dialects: 16px targets at 2 of 4, 24px at the boundary at the other two; the destructive 24px sibling sits **gapless** against the copy target · **cure = the reshell owns the box (family I, KF-CB-7); a default `w-4 h-4` is the interim floor; the SC 2.5.8 boundary math is what survives after the `.tap-floor` premise correction.** 0×0 painted geometry → SS-13.
- **SP-8 / SP-9 / SP-11 ≡ EH-11 / EH-8** ⟨kf-SharePopover (SP-9 and SP-11's fold net-new); kf-EditorHeader⟩ · **MIN each, SP-9 is the row's largest deficit** · (SP-8) `h-8 w-8 p-0` hand-rolls `iconOnly`, forfeits the coarse floor, and **never yields the 32px it names** — `min-block-size: var(--control-h-sm)` is a *different property* from `height`, so the buttons render 32×36 fine / 32×54 coarse, and `data-control-target` (the coarse floor's SOLE hook) is emitted only under the unpassed `iconOnly`; (SP-9) the Input's `h-8` defeats the coarse floor **OUTRIGHT** — `.field-control[data-kind="input"]{block-size: var(--field-control-height)}` is **THE SAME property** as `height`, so there is **no `max()` rescue** of the kind the buttons get: coarse `--control-h-md` = max(60px,44px) = 60 **collapses to 32 — a 28px deficit on the one control the user must hit to paste**; (SP-11) the trigger is a **24×24** raw `<button>` with `p-0` and an `icon-lg` sole child, reachable by none of the declared floors · **cure = drop the height overrides and let `size` drive; swap the trigger to glass `Button emphasis="quiet" icon-only` (collapses SP-11/SP-26/SP-27 in one edit).** Bank-correction rider carried: the installed radius is `var(--radius-pill)`, not `calc(var(--button-size)/2)` — the D axis's producer-evidence base was the wrong version.
- **TimelineTrack RR-A missed-5** ⟨kf-TimelineTrack⟩ · **MIN** · the instrument's geometry ignores `pointer: coarse` **while every glass control beside it grows 1.5×**: `h-12`/`h-32`, `w-4`/`w-6`, the literal 24px pad and `--caret-offset: 14px` are all frozen; no `[data-control-target]`, no `.tap-floor` · **cure = the NET row is the composition — ONE CARD, TWO SCALING REGIMES**; the hard-px pad arm folds to banked S-1 BOUNDED and is not re-booked.
- **CubeScene MISS-3 (trigger half)** · **MIN, target half latent** · the cube's only bespoke pointer target (the 32px ppmycota trigger, `m-0 p-0` zeroing any rescue) uses **none of the three** available mechanisms · **cure = the trigger's disposition is decided by the KF-APP-17 sweep; the `.tap-floor` liveness question is THIS wave's** · annotated by KF-SST-30.

### F · THE FOCUS CONTRACT (one decision; KF-KE-30 must resolve first)

- **KF-KC-4 + KF-KC-13** ⟨kf-KeyframeCard D-4=C-3; D-14⟩ · **MAJ** · three `@layer utilities` classes (`shadow-none`, `border-none`, `focus:*`) **cancel the whole `@layer components` focus affordance** while `outline: none` from `field-control` survives uncontested — cascade decided by layer order (utilities last, `style.css:1`), landing site verified (`cn("field-control glass-defined", h.class)` on the `<input>`); and every plate token is stripped while `backdrop-filter` and the pill radius survive — **a filtered, edgeless, padding-less smudge with no editable affordance**; `glass-defined` occurs **once** in glass-ui.css as the custom property `--glass-defined-plate: var(--control-surface-bg)`, set inside the very rule whose `background` shorthand resets `background-image`, then killed by `bg-transparent` — **the plate is doubly dead** · **cure = ONE cure for both: stop erasing the control, or REQUEST THE BARE VARIANT FROM GLASS rather than erasing `field-control` (the root-styling law: style at the shadcn/producer root, never per-instance overrides).** The same file pairs `outline-none focus-ring` correctly on the `<pre>` 40 lines away — **omission, not house style.** With KF-KC-3/4 the field is undiscoverable by sight, keyboard, and AT simultaneously · forced-colors-only survivor arm folds to KF-CE-13.
- **KC-5** ⟨kf-KeyframeCardList D-4⟩ · **MAJ** · the same mechanism on the offset field (`focus:border-transparent focus:shadow-none` over `.field-control:focus-visible{border-color;box-shadow;outline:none}`) — and because that rule's own `outline:none` forecloses the native fallback, **keyboard focus changes literally nothing outside forced-colors** · **cure = ALREADY CURED UPSTREAM: glass-ui 8.0.0's `control.css:221-223` paints focus as an `outline`, which these utilities cannot defeat — the cheapest correct repair is the glass-ui 8 adoption riding F-1; else strip the `focus:*` overrides now.** **This is a VERSION-BUMP NOTE, and it is the only row in the carry that argues for a producer major bump — it must be weighed against KF.W0's 7.0.0 pin before the wave commits to either arm.**
- **KF-CB-12** ⟨kf-CopyButton D-7 = C-15, count rescoped⟩ · **MAJ** · no focus affordance; opts out of the demo's declared **SINGLE** `:focus-visible` contract (4 real adopters, not 11); a **third dialect** on the sibling button in the same toolbar (`:91 focus-visible:ring-2 focus-visible:ring-accent`, itself ~1.37:1); **no glass rule reaches bare buttons in ANY mode** · the UA ring survives (no `outline-none`) — **MAJOR not BLOCKER** · **cure = `.focus-ring` for both controls, or the reshell's ring.**
- **TimelineCaret C-7 + D·M-5** ⟨kf-TimelineCaret⟩ · **MAJ/MIN** · the ring is hand-authored against the demo's own documented single-affordance law (`:19 outline-none focus:ring-1 focus:ring-primary` vs `design-idioms.css:73-79` + 4 `.focus-ring` consumers incl. the same instrument's KeyframeCard); and under forced-colors there is **zero** indicator — `outline-none` genuinely removes (Tailwind v4 `.outline-none{outline-style:none}`), `ring-1` is an erased box-shadow, and the producer's restoration list enumerates classes this input joins none of; the bridged `--shadow-focus-ring` token is additionally unconsumed · SC level corrected to **2.4.7 AA** · **CURE-ORDER LOCK (K-9, and it INVERTS the primitive-swap order): `.focus-ring` FIRST — it discharges D·M-5 immediately and does NOT wait on the NumberField evaluation; the primitive swap is second.** · sequencing: declared to KF.W7 from this end.
- **KeyframeTimeline D-20** ⟨kf-KeyframeTimeline⟩ · **MIN** · the bespoke `role="slider"` markers get the **UA focus ring**, not `.focus-ring` (the class is absent from the marker list; **no global `:focus-visible` rule ships**) — beside glass Buttons that carry it · **cure = one focus vocabulary across the instrument.**
- **KF-CE-46** ⟨kf-CSSCodeEditor D-18⟩ · **MIN** · `.cartoon-surface:has(:focus-visible)` **fires on pointer focus here** (Monaco focuses a hidden textarea; text-entry matches `:focus-visible` when focused), contradicting `design-idioms.css`'s own written *"pointer focus stays quiet"* contract; sole raw-class consumer · **cure = the contract TEXT or the RULE must move — the contradiction is closed on source and one of the two is wrong.** Pixel half → SS-13.
- **CPD R-6 + R-27 ≡ KAD-F2 + KAD-22** ⟨kf-CSSPasteDialog D-M2 + both readers' convergent missed rows; kf-KeyframesAddDialog D-1 / D-23a⟩ · **MAJ/MIN** · `:54` ships `outline-none` **with nothing put back**; `:13-18` carries none of role / `aria-multiline` / `aria-label`; it is the dialog's only un-ringed focusable (the shipped close X carries `focus-ring`); opening focus lands (statically) on that un-ringed empty editing host — **the shipped primary flow (open → Cmd+V, no click) is aimed at exactly R-1's sibling-insertion trigger, and the dialog's opening focus is invisible**; across the three contenteditable surfaces there are **three focus idioms and one absence** · **KILL-SHOT EVIDENCE (in-repo, verbatim): `KeyframeCard.vue:45-49` applies `focus-ring` + `role="textbox"` + `aria-multiline="true"` + `:aria-label` to the IDENTICAL idiom, and `design-idioms.css:73-79` declares `.focus-ring` "the SINGLE keyboard-focus affordance." No "no established pattern" defence survives.** · **cure = the S-9 Textarea swap makes it a labelled control by construction; the non-swap fallback is four attributes + one class.** · escalation: a positive focus-landing witness at SS-13 promotes R-27 and feeds R-1's escalation.
- **SP-20** ⟨kf-SharePopover D·m-6⟩ · **MIN** · the field is **not focused on open** — `openAutoFocus` exists precisely for this and nothing binds it; the placeholder is an imperative · **cure = bind it.**

### G · TYPE REGISTER / TYPOGRAPHY (one register decision per role)

**Family opening lock — MISSED-3, THE CURE-COLLISION LAW (SPEC, binds this wave AND KF.W7, verbatim from kf-TimelineHoverPreview RR-A, arithmetic re-derived at the bank):** *the corpus's two headline cures COLLIDE* — swapping to `text-mono-small` makes rows **19.6 → 28px** (`--type-small` 14→20px × leading 1.4), cutting the 96px scroller from ~9 visible rows to **4.9→3.4** — **D-4's cure materially WORSENS D-3, and the two rows are adjacent in the same axis document.** **LAW: a repair resizes or drops `max-h-24` in the SAME COMMIT; `normal-case` ALWAYS pairs `tracking-normal` (MM-29 — `--type-tracking-caps: 0.1em` rides onto the literal otherwise). No wave ships the regression.** This is the typography twin of the N-10 cure-collision law.

- **KeyframeTimeline D-9 (+ TimelineCaret L-5 as second site)** ⟨kf-KeyframeTimeline; kf-TimelineCaret L-5 folds here — the rung lives at the bank, both readers' local rungs inapplicable, both positions preserved in DISSENT⟩ · **MAJ** · `text-admin-label` on **the user's own label input**: force-uppercases typed content, fixed 10px at every viewport (`--type-admin-label: .625rem`, no clamp), **trips iOS focus-zoom while the repo's own cure sits unused** (`clampIOSNoZoomFontSize`, `iosTextEntry.ts:10-11`, applied to Monaco at `CSSCodeEditor.vue:137`, unit-tested asserting 14→16), and on coarse pointers `padding-inline: calc(1rem × 1.5)` eats 48px of the 128px box · **cure = a text register that does not uppercase, at a size that does not zoom.** Letter-correction carried: *"the ONE type token with no clamp()" is false* (micro/subheading/heading/title are also fixed) — **the substance ("stays 10px at every viewport") is untouched.** Compounds N-2: the control this typography wrongs is ALSO inert · sequencing: **N-2's wire-or-delete is ruled at KF.W7 BEFORE this wave spends D-9's MAJOR on an inert control.**
- **CPD R-9 ≡ SP-4** ⟨kf-CSSPasteDialog reader-B missed, adopted; kf-SharePopover reader-1 MISS-1 net-new⟩ · **MAJ** · **iOS: text-entry surfaces below the repo's OWN ruled and unit-tested 16px no-zoom floor.** CPD: `text-small` at a 390px viewport = the 14px clamp floor, and the R-8 `--control-text` bypass is exactly why the coarse-pointer 1.5× never rescues it. SP-4: `text-mono-caption` binds `--type-caption` = `clamp(0.75rem, 0.71rem + 0.21vw, 1rem)` → **~12.2px at 390px**, and it **WINS over `field-control.css`'s `--field-control-font`** (components layer < utilities layer) — **the DS default it overrides was zoom-safe** (`--control-text` ≈ 21px on coarse via `--ui-scale`); `EditorShell.vue:133` calls `initIOSPlatformClass()` on the very shell that mounts this popover · **cure = `text-mono-small` / size-driven (one edit with SP-7); or the ruled `clampIOSNoZoomFontSize`.** *All three axes stopped one property short in the same declaration.* Live witness is hardware-only → KF.W9.
- **KeyframeTimeline D-18 ≡ TimelineCaret m-3/L-13/C-6(b) ≡ THP D-14/C-1 rider** · **MIN** · `font-mono` stacked redundantly on `text-admin-label` at **4 sites** (3 axis + `TimelineHoverPreview:20`) — the utility already sets the family · **cure = one deletion pass; the bank already counts all four sites — do not re-book per file.**
- **KeyframeTimeline M8 + M10** ⟨RR-A net-new⟩ · **MAJ/MIN** · neither text input has an accessible name (the label Input is placeholder-only, uppercased by D-9; the caret `<input type="number">` has **NO name of any kind**) while glass publishes `./label` and `./labeled-field`; and the rail carries a **typographic hierarchy inversion** — decorative ticks at fluid `text-small` (14-20px) vs the **EDITABLE** percent at fixed 10px, 14px apart · **cure = D-9's register error at the composition level; one naming + one register decision.**
- **THP D-4** ⟨kf-TimelineHoverPreview⟩ · **MAJ** · the component overrides the tooltip's **own declared body size DOWNWARD** to fixed 10px at `line-height: 1` for a ~10-row scrolling list — the surface declares `--tooltip-text: var(--type-caption)` = clamp 12→16px; `text-admin-label` forces 0.625rem fixed, zero leading, uppercase, 0.1em tracking: **−16.7% at the clamp floor, −37.5% at the ceiling, and the gap WIDENS on large viewports because the override is not fluid** · **cure = `text-mono-small` (non-uppercasing) — UNDER THE MISSED-3 LOCK ABOVE: never without the box resize in the same commit.**
- **THP L-D7** · **MIN** · `"Capturing..."` at `:17` is a **demo-authored mono UI label**, a ruled T.D4 breach, and a **SIBLING** of the `data-register="code"` div so no allowlist selector reaches it (`font-roles.json`'s 15 selectors match none of it; `[data-register='code'] *` cannot reach a sibling; `_monoContract` closes *"A new demo-authored mono UI label reds the census"*) · **cure = a text-face status line — the voice fix; or the Skeleton fold absorbs the string entirely.** The dead-witness datum (`proof:font-census` unwired; the census probe cannot sample a closed tooltip) rides KSM R-7's NO-WAVE-OWNER rider and KF.W4 — **not re-booked here.** **REGISTER-LAUNDER note (carried from KF.W4's G-KFW4-10 design brief): the SELECTOR contract is satisfied while the REGISTER SEMANTICS it encodes is violated by the inherited uppercase — the census gate cannot see `text-transform`.**
- **THP MISSED-2** ⟨RR-A net-new, decider re-executed at the bank⟩ · **MIN** · **`italic` at `:24` paints UPRIGHT** — zero italic faces in the installed producer (4-face enumeration) and demo `font-synthesis: none` at `:root` — so the empty state's ONLY differentiator is dead: NO PROPERTIES renders as 10px uppercase 0.1em-tracked text, **indistinguishable from the declaration rows it stands in for** · **cure = a real differentiator; kills RR-B's S-b arm and refutes axis-D's S-2 differentiator clause at its stated ground.**
- **KF-SCR-2 ≡ KF-EST-2 (mechanism), PERMANENT arm** ⟨kf-SequenceScrubber A-M-1 net-new; kf-EditorStartScreen D-3/C:D-C3, the L-§4 kill REVERSED⟩ · **MAJ** · `font-synthesis: none` (`style.css:100`) **permanently voids glass-ui's `text-caption` italic register across the WHOLE demo** (8 consumer sites; no italic face shipped; synthesis forbidden; glass declares `font-synthesis` nowhere) — **the mechanism is the same as KF-EST-2's but TRANSIENT there and PERMANENT here**; at EST both italic runs (`:138`, `:149`) are above the fold, the single preload is the **upright** woff2, the ital sheet is deliberately non-render-blocking, and the metric-matched fallback family is **upright-only** under `font-synthesis: none` → deck + hint paint upright for the whole swap window, while `:32-33`'s "the ital@1 face is already loaded; zero new payload" is **false in both halves** and `index.html:47-49`'s preload policy predates T.D11 moving italic into the hero · **cure = ONE font decision, named: declare the synthesis dependency / ship an italic face / retire italic from the register (+ preload the ital woff2 or an italic-declared fallback face).** + glass-ui BH relay (owner edict) · swap-window filmstrip → KF.W9.
- **KF-AT-22 ≡ L-EST-2 / D-C1** ⟨kf-AnimatedText reader-LC miss-6; kf-EditorStartScreen both readers⟩ · **MIN** · **the BG-6 blocker rationale is STALE**: glass-ui 7.0.0 **SHIPS** `--font-display-weight` (`scale.css`: `--font-display-weight: 600; --type-weight-display: var(--font-display-weight)`), so `style.css:48`'s "there is no `--font-display-weight` token" is false — setting it to 400 retires the weight arm of `@layer demo-typography` **plus** the scoped belt-and-braces in one line (the tracking arm survives) · **CURE-DISCRIMINATION RIDER (load-bearing, verbatim): `:root`, NEVER `@theme` — the unlayered producer beats a layered theme emission; the L-remediation's step 3 is structurally unsound.** EditorStartScreen adds a **THIRD** in-file redundancy (`:108 font-synthesis: none` scoped, beside `:root`'s) · **cure = one line + correct BOTH stale comments in the same commit.**
- **MM-8 / MM-9 / MM-29 / MM-31** ⟨kf-MbabbMenu⟩ · **MIN band** · the same file is both the font manifest's **named exception** and its **five-fold violator**; **MM-29 is the family's transferable law: `normal-case` cancels the case but NOT the caps tracking — it must always pair `tracking-normal`** · **cure = one voice pass; MM-29's pairing is a MUST-CARRY on every `normal-case` edit in this wave (see SP-7, MISSED-3).**
- **KSM R-6 + R-7** ⟨kf-KeyboardShortcutsModal D-3; D-4 strengthened by reader-A⟩ · **MAJ each** · (R-6) typographic hierarchy **inverted** and the title demoted below the shipped default — `--type-admin-label` 0.625rem **FIXED** vs vw-clamped `--type-small` rows = **0.61× at 1440px and widening with viewport**; `:5` overrides `DialogTitle`'s `text-subheading` down to `text-body`; the sole downward outlier of three sibling dialogs; rider: `cn` has **no line-height/tracking group**, so `leading-none tracking-tight` survive the demotion and `text-body`'s own leading competes on cascade order; (R-7) demo-authored **mono UI headings** against the demo's own written Mono-as-data law (`DESIGN.md:24-30`), with `h3`/`text-admin-label` in neither `monoAllowedSelectors` nor clause (d) · **KILL-SHOT (verbatim, in-repo): `design-idioms.css:222-231`'s `.status-badge` re-voices to `var(--font-text)` with a comment ruling exactly this case — "a status WORD is a UI label, not data" — and this file declines the fix the tree already wrote.** · **cure = class/voice fix; the dead `proof:font-census` witness is a repo-process gap → NO-WAVE-OWNER rider, not this wave's gate.**
- **KF-CO-30 + KF-CO-25** ⟨kf-ChannelOptions D-18; D-13 sharpened by RR-A⟩ · **MIN** · mono-as-data applied **three ways in one card** (dropdown name mono+register, trigger neither, CSS-literal fields none, sibling numeric Input mono); and the SelectLabel override resolves determinately — family headings render at **10px** (`--type-admin-label`, the admin-chrome rung) and `px-2` is inert against the surviving `pl-8` (vendor default `py-1.5 pl-8 pr-2 text-dropdown-secondary font-semibold`; the hand-rolled `cn` keys font-size and text-color as separate groups, padding-x/padding-l separate) · **cure = one register pass for the card.** 10px-in-a-dropdown taste → SS-13.
- **KAD-4 + KAD-8** ⟨kf-KeyframesAddDialog reader-A MISSED (upheld); D-5⟩ · **MAJ each** · the code surface renders in the **PROPORTIONAL UI face**, not mono: `text-small` binds `font-family: var(--font-text)` (`@layer utilities`) and beats preflight `pre{…mono}` (`@layer base`), and because `highlight()` **destroys the `<code>` wrapper** the visible glyphs sit directly in the `<pre>` and inherit the proportional face — *a tab-indented, syntax-"highlighted" CSS editor rendering in Plus Jakarta Sans*; and the description is typeset at a **heading-family rung** (`text-subheading` 1.272rem/600 on the supporting line vs `text-heading` 1.618rem/700 title) · **cure = the mono fix that IS present at `KeyframeCard.vue:45` and MISSING here (2-of-3 sites); one rung correction.** Exact face → SS-13.
- **KF-KE-23** ⟨kf-KeyframesEditor D-8⟩ · **MAJ** · `.text-small` binds the proportional body face onto **both** contenteditable CSS surfaces; the 4-nbsp fake tab presupposes monospace; code size is viewport-fluid; **the mono rung (`text-mono-small`) is used two lines up** · **cure = same register decision as KAD-4; verified in the installed cascade by both readers.**
- **TimelineTrack D-3** · **MIN** · both numeric readouts break the Mono-as-data law: `:17 {{ zoomLevel.toFixed(1) }}x` and `:45-48 {{ tick }}%` ride `text-small` — the proportional stack — with neither `font-mono` nor `tabular-nums`, **while every sibling readout obeys** (caret, hover preview, header) · **cure = the family register is the net half; the SIZE-register half folds to banked M10 and is not re-booked.** Micro-note carried: `toFixed(1)` renders "1.0x" for zoom 1.0-1.049 beside a mini-bar whose `v-if="zoomLevel > 1"` says otherwise.
- **TimelineCaret m-2 / C-6(a)** · **MIN** · a numeric datum wears the **full label register** while the demo's numeric idiom — **and the SAME datum 90 lines away** — wears `text-mono-caption` (`KeyframeTimeline:102-104` renders `{{ Math.round(selectedKeyframe.percent) }}%` at `text-mono-caption font-semibold tabular-nums`; 21-site idiom census; the governing rule at `design-idioms.css:224-226`) · **cure = the same-datum-two-registers cell is this row's own; the register family cross-refs banked D-9/M10.**
- **KF-SS-9** ⟨kf-SpringScene D-4⟩ · **MAJ** · the two faces of one scene wear **two different type systems in the identical structural slot** (`text-display` vs `text-heading` on the face titles: different family, rung, weight, one fluid one not; `gap-8` vs `gap-6`), and the scene carries **four registers for code identifiers** · **cure = ONE register decision per role** (NO-WAVE-OWNER at its bank; it RIDES this wave's typography discipline and must not be decided twice).
- **KF-EST-8 + KF-EST-10** ⟨kf-EditorStartScreen reader-DU misses + ND-3 re-rated⟩ · **MIN each** · `line-height: 0.92` at the mega rung sets consecutive h1 lines to **−0.04..+0.01em ink clearance** (touch/overlap on any descender-bearing non-final line; `title` is public; the default escapes by letter-luck) and the leading is the **THIRD un-tokenized display-rung override** (`--type-leading-display: 1.05` is published, in `scheme-motion.css`) that the KF-AT-22 cure leaves behind; and `<List>` inherits Lucide's `strokeWidth = 2` → ≈2.19px strokes at the 0.8em/26.3px box vs ≈1.8px serif stems — **the icon out-weighs the prose inside a file whose stated law is ":133-134 No weight above 400"** · **cure = the leading decision closes all three; `absolute-stroke-width` is the one-attribute icon cure.** Collision render with a descender title → KF.W9.
- **SP-7** ⟨kf-SharePopover, ruled R-4⟩ · **MIN** · `normal-case` cancels the case **but not the 0.1em caps tracking** on a long, case-significant, pasted string (`text-mono-caption` carries `letter-spacing: var(--type-tracking-caps)`; built-sheet order puts `.normal-case` after it, so case dies and tracking survives) · **cure = `text-mono-small`, which `cn`-merges cleanly; SAME EDIT as SP-4.** Mechanism ≡ MM-29 at a different site — **a new-site booking citing the mechanism, not a re-book.**
- **KF-EST-14 — A SEQUENCING CONSTRAINT, NOT A DEFECT TO FIX ALONE** ⟨both readers; "the subtlest correct finding"⟩ · **MIN** · the mobile subtitle rung (`:185`) beats the base deck rung (`:140`) **by source order alone**, at equal scoped specificity, **under a DIFFERENT class name** — so **a naive media-query consolidation silently deletes the 1.5rem serif floor**; `.hero-hint` shares one selector and is not exposed · **cure = ANY sheet-consolidation pass in this wave MUST re-key the override to `.hero-deck` in the same motion.** Carried as a hard constraint on the hygiene pass.
- **SpringHeatmap D-m4 residue** · **INFO** · `text-caption`'s italic register on the legend is a **glass-ui utility-taxonomy question, not this component's choice**; the readout pairing is the house idiom (16 lines across 15 files incl. `SpringPhysicsFacet.vue:79`, twenty-one lines from this mount) · **cure = the system-level question routes here as a producer-side datum; the component does NOT answer for a taxonomy it consumed correctly.** The `text-mono-caption` half is KILLED.

### H · CONTRAST / THEME-BLIND TOKENS / CANVAS COLOR

- **AmigaScene D-4 · D-5 · D-9 · D-10 · MISSED-B · MISSED-D · C-6/D-12 (S-9) · C-7 · L-i5 — ONE STAGE-SURFACE + CANVAS-THEME PACKET** ⟨kf-AmigaScene⟩ · **MAJ ×4, MIN ×5** · (D-4) the contact shadow — the load-bearing height cue after T.A10 — computes to **1.016:1 dark** from a theme-blind `rgba(0,0,0,0.55)` literal (`:69-71`) with **zero theme reads in the scene** and a 1.9× scale-up animating an invisible sprite; (D-5) `#b9b9c6` = hsl(240 10% 75%) — **hue 240 in a system whose every neutral is hue 24-40** — 1.22:1 light / 2.15:1 dark, **light 1.76× worse**, no token edge, and the T.A10 comment assigns the grid the deleted box's load-bearing job, **foreclosing the decorative escape**; (D-9) **one colour of seven is tokenized** — six literals (checker white *whiter than the system's own paper white*, grid, hemisphere pair, spot, specular at `utils.ts:70`, shadow stops) **invisible to any CSS token audit**; (D-10) the "sky/ground wash" is **1.064:1** light — a flat field — **and polarity INVERTS between themes: one authored intent, two opposite renderings**; (MISSED-B) the scene's "themed paper-grid backdrop" is an **opaque** two-stop gradient (`--muted`→`--background`, `:259-263` — *there is NO grid in it; the comment misnames its own CSS*) painted by a positioned descendant **ABOVE** the shell's fixed `.grid-background` graph paper — the substrate the J.W7a rationale guarantees "PRESENT and legible behind the glass plate"; every register sibling uses a translucent `<Card surface="glass">` so the substrate shows through — **amiga erases it across the stage (desktop) / viewport (mobile)**; (MISSED-D) the J.W7a hairline D-12 calls load-bearing was **never measured by the axis that measured everything else**: 1.94:1 light / 2.82:1 dark, the same light-worse asymmetry; (C-6/D-12, **census shadow row S-9, minted here**) two shipped glass primitives (`Surface`, `PaperBackdrop`; `Card` per the sibling register) shadowed by 8 lines of bespoke CSS — **AmigaScene is the only file in the tree hand-applying `rounded-card` to a raw element**, and the full-bleed mobile layer wears card chrome (the category error); (C-7) fallback asymmetry — two of three `var()`s fallbacked, `--border` bare, fallbacks achromatic-gray and **light-only**, the only such idiom in the tree, with the clean-install trigger dead at HEAD ⇒ dead-but-misleading off-palette fallbacks; (L-i5) the checker texture is baked once at mount and theme-blind — inert today only because `--amiga-red` → `--rainbow-red` has no `.dark` override · **cure = ONE decision: `resolveColor` + `useGlobalDark` re-bake (the SpringHeatmap/CSSCodeEditor precedent) + a shared stage-plate/Surface recipe.** **BOUND (adopted): the `:6-12` no-DOM-layer ruling may reduce the C-6/D-12 cure to a shared `.stage-plate` recipe rather than a primitive swap — EVALUATE, NOT SWAP.** Filed as *design* defects, **not WCAG 1.4.11** (scenery — the conformance framing is killed at the bank; do not re-import it) · perceptual verdicts → KF.W9.
- **SpringHeatmap D-M2 + D-M3 + C-M-1 — ONE CANVAS-COLOR-DISCIPLINE DECISION** ⟨kf-SpringHeatmap; the component-side booking of the precedent kf-AmigaScene D-4 already cites⟩ · **MAJ ×3** · (D-M2) **the theme repaint RACES the class flip**: `:269-270`'s default pre-flush `watch(isDark, paint)` reads computed style **before** vueuse applies `.dark` at `flush:"post"`; **fatal direction is dark→light** (the `.dark`-arm literal `hsl(24 9% 4%)` is theme-blind and the rasterised canvas keeps it until a resize) — **the producer ships the cure on the already-imported subpath (`onFlipSettled`)**; witness signature = *cured-by-resize-not-by-retoggle*; (D-M3) an **unresolved token stream is interpolated into `ctx.fillStyle`** with zero validation, read-back, `CSS.supports` probe, or canvas-valid fallback — staking the entire render on undeclared UA behaviour with a silent failure whose outcome is one of **three** branches (solid-black field / classical-resolve / ASF-resolve); **the producer names the failure in advance** (`resolveCanvasColor.d.ts` "→ … NOT black", exported from `./canvas`, never imported) **and the demo states the same law itself** (`amiga/utils.ts:3-13`); the `:119` green and `:124` `#fff` last-resorts are unreachable AND wrong-identity; (C-M-1) shadow of glass-ui's `./canvas` subpath — `useCanvas2D` + `resolveCanvasColor` cover the DPR policy (byte-equivalent default), resize/mount lifecycle, PRM parking, offscreen suspend, dispose, **and the colour resolution this file reimplements wrongly** · **cure = ONE decision, three admissible shapes: `resolveCanvasColor`/`useCanvas2D` adoption · `@property <color>` registration · or C-M-4's CSS-gradient dissolution.** **ESCALATION ARMED: D-M3 becomes a BLOCKER iff SS-13 #1 lands branch (a) — the solid-black field.** Census S-9 extension to KF.W0's shadow inventory. Provenance nit adopted: the falsifier bytes live in `dist/resolveCanvasColor-*.js`; `dist/canvas.js` is a re-export shim.
- **SpringHeatmap L-m-4 / D-m2** · **MIN** · the dead-fallback family's **two lies**: `var(--duration-fast, 160ms)` misstates the real 0.2s by **25%**, and the tone/surface last-resorts are an ad-hoc GREEN `hsl(142 71% 45%)` + `"#fff"` **inside the function whose header forswears ad-hoc hex**, against the ruled violet authority · **cure = token hygiene rider; family ≡ CubeTarget #60's duration-hygiene class, fallback-divergence variant.**
- **CubeScene D-7** ⟨kf-CubeScene⟩ · **MAJ** · **three of six face numerals fall to 1.36-1.72:1 in dark mode** against the 3:1 large-text floor — the face palette is six **theme-invariant `:root` literals** (single occurrence in the 571 KB build) while the numeral ink inherits theme-reactive `text-foreground`; face-4 arithmetic reproduced independently by both readers (1.368/1.356) · **cure = a dark arm or `light-dark()` for the face palette, per the AmigaScene D-4/D-5 precedent.** Correction adopted: the numeral is **weight-400** via `demo-typography`, not `font-bold` — the 3:1 large-text threshold holds on size alone.
- **CubeTarget #58 ≡ KF-AX-3** ⟨kf-CubeTarget α-missed #4, re-derived; kf-CubeAxisLines KF-AX-3, 3rd+4th independent reproductions⟩ · **MIN/MAJ** · the `--axis-*` palette is theme-blind and the **LOCKED** state fails 3:1 in each theme on a **different** axis: `--axis-y` = hsl(120 47% 47%) → **≈2.7:1** light (α computed 2.53 against a tinted stage; either figure is below 3:1) while `--axis-x` ≈ 4.2:1 and `--axis-z` ≈ 6:1 pass and the dark arm passes ≈7:1 · **at rest (0.45 opacity) all three are decorative furniture; LOCKED at opacity 1 + solid stroke THE LINE IS THE STATE SIGNAL** and carries meaning below the floor · **cure = theme-aware axis tokens with the #8/#9 theme packet; MAJOR sustained over β's demotion.** Exact backdrop pixel → SS-13.
- **KF-CO-19 + KF-CO-20** ⟨kf-ChannelOptions D-5; D-6; both RRs re-derived to 4 decimals⟩ · **MAJ each** · the gold pencil computes **≈2.08:1 on the light card** (bounded [2.08, 2.16] across the glass composite; dark passes 7.01:1), and the 'advanced' chevron computes **≈2.01:1 light / ≈2.46:1 dark** (`opacity-50` on an already-muted role) — **the only navigability mark fails 1.4.11 in BOTH arms** · **cure = the STY-4 consolidation means the pencil cure is ONE token in `design-idioms.css:36` (a light-arm gold); the chevron needs a real muted rung, not an opacity.** Exact composites → SS-13.
- **KeyframeTimeline D-6 + D-7** · **MAJ each** · quiet-emphasis pane icons composite to **~2.0:1 light / ~2.46:1 dark** — the token is fine (`--muted-foreground` at full opacity ≈5:1), **`opacity-50` breaks it**, and Clear/Expand are never disabled so no exemption applies; and **the scrub rail has no perceivable boundary in either theme** — fill@50% vs card ≈1.01/1.11:1, border ≈1.86/2.00:1, border-vs-fill ≈1.84/2.22:1 — **on the PRIMARY interactive surface**, with expanded mode stripping the Card plate entirely (`:3 border-0 shadow-none bg-transparent`) · **cure = opacity is not a contrast mechanism; give both a real rung.** Both readers reproduced the arithmetic independently to 2 d.p.; painted plate → SS-13 (**and D-7's SS-13 row is the single measurement site — TimelineTrack D-5 rides it, do NOT re-measure separately**).
- **TimelineTrack D-5** · **MIN** · the zoom viewport indicator fails SC 1.4.11 in **both** arms (`bg-primary/40` fill ≈1.72:1 light; rail outline ≈1.19:1) **and the mini-bar is the ONLY rendering of pan position** · **cure = net row (banked D-7 is the rail's own boundary; this is the zoom bar); ratios ride D-7's residue row.**
- **KC-12** ⟨kf-KeyframeCardList D-8⟩ · **MAJ-band** · the row-metadata block paints at a **1.84:1 theoretical ceiling** (`opacity-25`, arithmetic re-derived independently by both readers to agreement), **is exposed to AT**, holds the row's only visible identity, and `pointer-events-none` makes that identity **unselectable** · **cure = a real muted token, coordinated here; the behavioural half is CARD-UNIT's.** Token-independent.
- **KF-TD-4** ⟨kf-TypingDots D:D-2, its 1.52 slip killed⟩ · **MIN** · the legibility floor is denominated in **ALPHA** (the retired gate's ≥0.15) rather than **CONTRAST**: no 20%-alpha ink composite can exceed **1.662:1** against its own backdrop (backdrop-independent), **unreachable by ≈1.8× against the 3:1 large-text floor — a green gate coexisting with a ghost**; the animated ladder's bottom rung holds 300 of every 1200ms · **cure = ONE design decision WITH banked KF-EST-11's polarity question — rest-at-1 cures both.** Correctly scoped by the axis itself as design-legibility, not conformance (`aria-hidden`).
- **ME-13** ⟨kf-MatrixEditor D-4; r3 rider M-7⟩ · **MAJ** · sole per-cell identification at `opacity-20` light / `opacity-75` dark fails the 3:1 large-text bar; the light arm is **theory-proof** (α=0.2 ceiling 1.662:1); both r3 readers re-derived independently (1.201 light-y; 1.326 light-x) · **cure = theme-aware ink, NOT alpha.** **RIDER (upgrades the row from "found" to "found, ruled-in-tree, unlanded"): `cube.md:129` diagnoses this verbatim ("nearly invisible in light mode") and `:239-244` prescribes ~`opacity-45`, scheduled as work item 8 and never landed — the repo's own design doc is the floor.**
- **MM-12** ⟨kf-MbabbMenu⟩ · **MIN** · the brand mark and its label **part ways in the dark** · **cure = one brand-token decision; lands in the MM-4 motion.**
- **CPD R-12** ⟨kf-CSSPasteDialog D-M3, authority half dead⟩ · **MIN** · perimeter ink measures **sub-3:1** · **cure = available in the shipped dist (`--control-surface-border`).** Composited figure → SS-13.
- **TimelineCaret D·M-3** · **MAJ** · **in the light theme `--primary` and `--foreground` are BYTE-IDENTICAL**, so a hovered unselected caret is indistinguishable from the selected one (**1.00:1**): `:10 isSelected ? 'text-primary font-semibold' : 'text-muted-foreground hover:text-foreground'`; the light arms of `tokens/color-radius.css` + `tokens/light-dark.css` are identical, dark arms distinct; bridges bind `--color-primary`/`--color-foreground` straight through — **the surviving signal is the 500→600 weight step at 10px: exactly one signal, one weight notch, at the smallest type in the system** · **cure = selection needs a NON-COLOUR channel; joint with the D·M-5/C-7 cure.** **CAUTION LOCK: `aria-selected` is NOT the vehicle — it is INVALID off its seven admitted roles; "the cure ships invalid ARIA to harvest a stylesheet" is a killed shape.** Registry-swept: no banked row carries this mechanism. Deep arm (the `ladder.css` `@container style(--glass-backdrop: light)` rebind collapsing all three states on `.glass-quiet`) → SS-13.
- **TimelineCaret D·M-7 + MISS-α3** · **MAJ/MIN** · the edit field has **no perceivable boundary against the surface it replaces** (SC 1.4.11): `:19 bg-background border border-border rounded` over TimelineTrack's `bg-muted/50` on `<Card cartoon tier="quiet">`; `--background` = `--neutral-0` = **the PAGE ground** · **CURE SHARPENED (MISS-α3): glass ships the on-glass field surface — `.field-control` binds `--control-surface-bg: var(--input-on-glass)` (light `hsl(36 40% 92%)` / dark `hsl(26 12% 22%)`) — a ONE-LINE surface correction that does NOT wait on the NumberField evaluation.** **Correction to the reader carried: the token is NOT registry-virgin — banked kf-CSSPasteDialog R-24 already names `--input-on-glass` as the editable-well surface; this is a second site of that doctrine, cross-referenced not re-derived.**
- **CPD R-24** ⟨kf-CSSPasteDialog, reader-A missed⟩ · **MIN** · `bg-muted/50` is **~2.8× the system's READ-ONLY muting, on an EDITABLE surface** — the system reserves the `--muted` mix (18%) for `[data-state="readonly"]`; the editable well is `--input-on-glass` · **cure = affordance semantics are INVERTED, not merely off-token; the doctrine row of the family above.**
- **KAD-18 + KF-KE-24 + KF-KE-32** ⟨kf-KeyframesAddDialog D-13; kf-KeyframesEditor D-9; D-18⟩ · **MIN/MAJ** · `bg-transparent` on the `<pre>` is **dead** — the runtime-injected **unlayered** hljs `.hljs{background:#0d1117}` wins (and its death is what keeps the contrast decidable, 12.3:1/6.2:1); the same injected theme is a hard `#ffffff`/`#0d1117` plate **inside the warm `--card` surface** (a 1.42:1 tonal step in dark) and is the opacity that powers KF-KE-5; and `bg-background` is painted **over the `--card` surface the component just dropped a Card to honour** (a 1.42:1 near-black rectangle on warm brown in dark) · **cure = layer the injected sheet / re-tokenize the theme (KFED-UNIT owns the pipeline; the TOKEN decision is this wave's rider).** Fold identity: KAD-18 ≡ the twin's D-13 (`bg-muted/50` is the honest version).
- **RibbonBar D-8 / C-9** · **MIN** · `text-emerald-500` is **the demo's sole raw-Tailwind-palette colour** (1 hit) and the four-icon colour scheme carries **no information** (inherited / gold / emerald / rainbow-stroke on one strip; zero coloured icons on the next) · **cure = token/theme discipline, the CubeScene D-7 precedent lane.** D-8's contrasting praise of `text-gold` is **CORRECTED by RB-2** — that "discipline the demo does own" is itself a shadow.

### I · GLASS PRIMITIVE ADOPTION (the S-partials, S-9/S-10 census extensions, evaluate-not-swap)

**Family law (from KF.W7's counter-evidence lock, verbatim, weighed before ANY swap verdict): kf-PlaybackRibbon is the NON-bespoke case — it consumes the real `Slider` — and carries BLOCKERs anyway. "Swap onto the primitive" is NEVER SUFFICIENT as a cure.**

- **S-5 · KF-AT-6 — RE-HOMED** ⟨kf-AnimatedText, census S-5 CONTRADICTED and re-homed with its identity preserved; kf-EditorStartScreen ND-7/D-C2 independent concordance⟩ · **MAJ** · `splitText` — extracted from this component **by name** (`split-text.ts:9` "the AnimatedText.vue precedent"; `segment.ts:13` "the AnimatedText X-5 lesson"), barrel-exported LIGHT, gated by a browser a11y oracle **pinned to THIS component's default string** — is consumed **ZERO** times by the demo; and census S-5's proposed counterpart `TypewriterText` is a **category error** (a human-typing SIMULATOR — `TypoState`/`errorRate`/`deletingSpeed`, reveals text over time — where AnimatedText **LIFTS** text already present). **The census hedge "verify per-char granularity" is answered NO.** · **cure = the S-5 slot's subject is CORRECTED to the kf-dogfood decision: adopt `splitText` OR rule the hero template-only — WRITTEN EITHER WAY.** **FIVE-CONSTRAINT SET (MUST-CARRY; the migration is NOT a two-line swap and EVERY axis's repair order is unexecutable as written):** (1) **KF-AT-8 — a NAMED BLOCKER-OF-THE-MIGRATION**: `applyA11y` tests only `hasAttribute("role")` and silently stamps `role="img"` over any IMPLICIT role, so aiming `splitText` at the hero **deletes the page's only `<h1>` from the heading hierarchy** (WCAG 1.3.1/2.4.6, axe `page-has-heading-one`); the hole is untested; **the library cure (implicit-role awareness or a `role: null` opt-out) PRECEDES any h1 adoption** → KF.W5; (2) **KF-AT-11**: `splitText`'s `createElement` fragments carry **no `data-v-*`**, so `.wave-char[data-v-hash]` cannot match them and the scope-renamed `charLift-hash` keyframe is unreachable — **the prescribed repair ships a motionless, un-inline-blocked hero**; and `replaceChildren` on a Vue-rendered container desynchronizes the vdom; (3) **KF-AT-10**: a faithful migration **REDs the repo's only live gate BY SELECTOR** — `heroReady` hard-polls `h1 .wave-char` (30×300ms then FAIL) and clause (2a) enumerates `.wave-word`, while `splitText` emits `kf-split` fragments and **NO word tier** (`buildUnits` returns one flat run; `SplitBy` offers no grouping under grapheme) — **gate migration is spec'd WITH the component migration, ONE landing order, handed here by KF.W4**; (4) **KF-AT-9**: `splitText` **mutates before it can refuse** (`buildLines` does `el.replaceChildren(...)` BEFORE the `isLaidOut` check throws, and the throw propagates before the result object holding `revert` exists) → KF.W5; (5) **the zero-teardown price**: the component today has one `computed`, no lifecycle, no listener, no rAF — *the property the prescribed migration would destroy.* · dissent: L's BLOCKER grade recorded; C's MAJOR adopted (nothing fails at runtime — the sole call site binds only `:text` with a static ASCII string).
- **KF-AT-1 · -2 · -3 · -5 · -13 · -14 · -15 · -16 · -17 · -18 · -19 · -20 · -25 · -26(a)(b)(d)** ⟨kf-AnimatedText; kf-EditorStartScreen folds L-EST-6/L-EST-12/reader-DU 2.2.2/reader-LC `.hero-dots`; kf-TypingDots folds⟩ · **MAJ ×5, MIN ×7, INFO ×3** · the S-5 slot's component-repair roster: (AT-1) `const { offsetMs, cycleMs } = props` is **NOT** Vue 3.5 reactive-props-destructure (the initializer is the identifier `props`, not the macro) so both JSDoc'd public knobs **freeze at setup** while `text` stays live — compiler receipt on record; (AT-2) `v-bind="$attrs"` inside the char `v-for` replicates every fallthrough attribute onto **N glyphs inside the `aria-hidden` subtree** (`id`→N duplicates, listeners→N dead, `aria-*` unreachable) while the `.sr-only` mirror — **the accessible name** — receives nothing, and imposes a permanent `17 /* TEXT, FULL_PROPS */` patch though no consumer passes one attribute; (AT-3) `w.split("")` is a **UTF-16 code-unit split the library's own segmenter names as the anti-pattern** ("shreds into mojibake"), with a 5-code-unit ZWJ sequence already shipping one binding away (`App.vue:50`) — **the grapheme decision IS the S-5 slot's work and MUST land in the same wave as KF-AT-4's oracle repair (the ATOMIC BUNDLE)**; (AT-5) the in-file X-5 invariant is falsified in every non-rendered text path (the visual layer's textContent IS `Selectananimation`; the h1's full textContent is the doubled mirror+visual run plus the dots' `"..."`); (AT-13) the `:88-89` comment ("inline-block keeps each word unbreakable") is **false** per CSS Text 3 §5.1 and enforced by no declaration — **CURE SEVERED: `white-space: nowrap` on `.wave-word` is REJECTED (with `overflow-wrap` unset anywhere in the chain it converts a graceful mid-word wrap into HORIZONTAL OVERFLOW ON THE LCP NODE; the `.hero-dots` precedent is a false analogy — three fixed one-char dots vs an arbitrary word at up to 177px/glyph)**; (AT-14) last `.wave-word` and `.hero-dots` are two adjacent atomic inlines with a soft wrap opportunity between them that interior-only `nowrap` does not suppress — the "dots never wrap apart" contract is **prose, not a declaration** (and reader-LC's stronger form: the rule is a **TOTAL no-op** over an `inline-flex` child with no interior text nodes; the real guarantee is Vue's whitespace-condense — *the comment credits the wrong mechanism*); (AT-15) `cycleMs` reaches the cascade unvalidated → invalid `<time>` → IACVT → `animation-name: none`, **a silently dead poster the `3.6s` fallback cannot rescue** (the property IS set, to garbage) against `stagger.ts`'s fail-explicit throw; (AT-16) **five raw motion constants** (`55`, `3600`, two beziers, `-0.09em`) bypass glass-ui's shipped registers (`--motion-stagger-default: 80ms`, `--motion-weight: 0.618`) **in the one hero whose geometry is fanatically tokenized**, and the PRM guard is a hardcoded **binary** mirror (`animation: none`) of two **NON-binary** authorities (`ReducedMotionPolicy = boolean | number`; `--motion-weight`); (AT-17) WCAG 2.2.2 — infinite auto-starting motion on the LCP element with no in-content pause/stop/hide, only OS-level PRM relief, **and `:18 pointer-events-none` + zero interactive elements means the one place a control belongs is the one place the design forbids one**; (AT-18) empty/whitespace `text` → an `<h1>` with **zero accessible name**, AND the prescribed cure does NOT close it (`applyA11y(el,"","img")` yields `role="img"` with NO name — **a HARDER violation**); (AT-19) find-in-page on the LCP heading is degraded (multi-word queries can only match the 1×1 clipped mirror; the visible 177px poster matches no spaced query at all); (AT-20) the word gap is computed in JS and emitted as N inline styles where `.wave-word:not(:last-child){margin-inline-end:0.25em}` is exactly equivalent — **the cheapest item on the list**; (AT-25) the ":96-97 holds still ~2.7s" comment is ~23% high (true rest = 2216ms) in a header otherwise 13-for-14; (AT-26) `--wave-cycle` unprefixed (folds census §6.3 **by reference**, never re-booked), its `var(--wave-cycle, 3.6s)` fallback is dead code, and **three `.sr-only` mechanisms are in force repo-wide** (glass-ui unlayered + Tailwind layered + `.sr-only-slider`) · **cure = ONE contract motion: delete, validate, or react — NEVER two of the three half-done** (the law that also governs KF-TD-3/KF-EST-18) · **`--motion-weight` is the ready-made app-drivable mechanism for AT-17.** · dissents: AT-25's MINOR (reader-DU); AT-17's MAJOR (reader-DU) against the bank's MINOR-with-stated-contest.
- **S-6 · KF-SKEL-3 / -6 / -8 / -18 (+ KF-SKEL-4 as a CONSTRAINT, not a cure)** ⟨kf-App.skeleton; kf-App KF-APP-40 ≡ census S-6 fold⟩ · **MAJ/MIN** · the skeleton reimplements an installed primitive **minus five arms** (compositable transform sweep · forced-colors · reduced-transparency · motion-opt-in polarity · `--duration-shimmer` token) while the primitive's compiled CSS **ships in the demo bundle as dead payload**; and `SceneSkeleton`'s `label` prop is dead API whose glass `Skeleton` swap is **root-barrel-only** (73 subpaths, no `./skeleton`) · **CURE INVERSION — THE ROW THAT SHAPES THE WHOLE S-6 PARTIAL (KF-SKEL-6, verbatim in effect): the skeleton echoes the WRONG PLATE. The demo owns an explicit stage-card register (`<Card :shadow="false">` + `rounded-card` = `--radius-card` = 1rem, live in square/amiga/spring×2) which IS "the scene's stage panel" the docblock claims to echo; the skeleton paints 0.5rem flat veil, and the bare glass `Skeleton` all three axes prescribed paints `--radius-input` = 0.25rem with no border/shadow — EVERY PRESCRIBED CURE INCREASES THE SWAP-TIME SILHOUETTE DISCONTINUITY IT IS MEANT TO REMOVE. "Delegate the plate" means DELEGATE TO THE STAGE-CARD REGISTER; primitive-as-sheen only.** (KF-SKEL-8) `42rem × 24rem` are magic numbers appearing nowhere else in the tree and the plate **UNDER-fills** the stage on both layout forks (desktop ≈54% of the stage cell at 1920px; mobile severe — `.stage-cell{position:fixed;inset:0}` full-bleeds the viewport while the 24rem cap leaves the plate ≈half the stage height) — **cure = geometry derives from the stage box (cq units / the Card register), never viewport literals**; (KF-SKEL-18) `clamp(1rem, 4vw, 3rem)` reads the **viewport** inside the layout this repo deliberately promoted to a container (`container-type: inline-size`, the cqi idiom documented in-file with two named precedents) — **cq units with the geometry cure** · **CONSTRAINT (KF-SKEL-4, GLASS-OWNED): glass-ui's own packaged `components.css` DEFEATS ITS OWN `@theme` radius scale through cascade layers** (`index.css` ends `@import "./components.css" layer(components)` and that file's unlayered `:root` re-declares Tailwind defaults; `@layer components` sorts after `@layer theme`, so app-wide `--radius` = **0.25rem** not 0.625rem and `--radius-lg` = **0.5rem**) — re-derived from the shipped bundle by byte offset AND from source — **KF.W6 MUST NOT cure this component against the detuned values; the spec PINS intended-vs-shipped radius explicitly.** The producer packaging defect rides the BH relay; downstream blast-radius enumeration belongs to the relay, not this file.
- **S-7 · KF-CB-7** ⟨kf-CopyButton D-9 = census S-7, escalated "evaluate"→"decided"⟩ · **MAJ** · bespoke shell where `SharePopover.vue:31-39` ships **the identical affordance correctly** (glass `Button`, 32px target, `icon-md` 20px glyph, house focus/hover, visible `title`) · **F-1 gate RESCOPED by the substrate ruling — cured at the frontier (7.0.0 exact devDep); the swap is NO LONGER dependency-blocked.** **RESHELL CONSTRAINT (S-2, MUST-CARRY): a real `<button>` must remain in the DOM or CopyButton silently drops out of the toolbar's roving-tabindex cohort (`useToolbarKeyboard.ts:41-49` collects `button` descendants; its docblock names CopyButton).** · **STANDING RIDER: the S-7 reshell MAY ABSORB the shell-family rows (KF-CB-5/-12/-13/-14/-20/-21/-26) — if the glass `Button` swap lands they DISCHARGE with it; the execution seat STATES which rows discharge BEFORE spending a cure.** Companion rows: (KF-CB-13) no hover/active/disabled — hover improvised per site in three dialects while the house idiom (`--scale-hover: 1.08`, 13 sites) is declined.
- **S-9 · CPD R-5 — THE TEXTAREA SWAP (the wave's densest single cure)** ⟨kf-CSSPasteDialog D-M1/L-5/C-2; kf-KeyframeTimeline M6 fold⟩ · **MAJ** · `Textarea` is real, exported on `/forms` — **the subpath the sole consumer already imports** (`KeyframeTimeline.vue:171` imports `Input` from `@mkbabb/glass-ui/forms`) — with **zero mounts in the demo**; nine ruled field behaviours forfeited; census row 80 was classified `G` **on import-list evidence alone** · **S-9 is a corpus-MINTED extension of S-1..S-8, sized at 1 site** · **cure = the swap KILLS R-1 / R-6 / R-8 / R-9 / R-12 / R-14 / R-20 IN ONE MOVE.** **DISCHARGE DISCIPLINE (a gate condition): the execution seat STATES which rows the swap already discharges BEFORE spending a cure on any of them.** Companion rows the swap carries: (R-1) `<pre contenteditable>` whose text is also `{{ text }}` puts browser and VDOM in contention — **a browser-inserted sibling is invisible to Vue forever while its text is simultaneously mirrored** (the swap deletes the defect CLASS: the `value` channel means VDOM never touches the caret's node); (R-2) no scroll owner, no height bound — `DialogContent` mounted without `scroll` on **all three** demo mounts, centred base class carries no height bound, `preClass` has no max-h/overflow, **and every escape route destroys the paste** (the Textarea recipe carries `overflow: auto` natively); (R-8) `text-small` bypasses `--control-text` so on a coarse pointer every sibling control scales 1.5× while the code box stays at the clamp floor (`field-control` fonts ride `--control-text`); (R-26) the dialog keeps the **PROSE measure** for a source-code surface (`max-w-lg` ≈ 440px ≈ ~52 monospace columns at the 14px floor; `white-space: pre` never overridden; the `<pre>` is a grid item with `min-width:auto` so the track overflows the plate) — **horizontal overflow is the DEFAULT case for real `@keyframes` bodies, not the edge case all three axes filed** — cure = one-token widening (`sm:max-w-2xl`) plus an overflow owner; (R-10) `p-3`/`gap-2`/`min-h-[20vh]` are `--ui-scale`-blind and `vh` sizes a text box where the system rules line-height (**identity fold: the T-tranche `min-h-[Nvh]` carry — never re-booked**; the twin site is KAD-F5 at 25vh); (R-11) `text-body` on the description compresses hierarchy at all widths, inverting ≥2086px; (R-13) no `DialogHeader` — four peer grid children at uniform `gap-4`, responsive header alignment forfeited; (R-14) `rounded-lg` (10px) is the **button** rung; the multi-line-field rung is `--radius-field` (16px); (R-15) `text-subheading`/`text-muted-foreground` re-passed to primitives that already apply them (deterministic no-ops); (R-16) **`icon-md` namespace collision** (demo 20px vs glass `--icon-md` 16px) and the only 20px button icon in the instrument — **the audit names exactly this class**; (R-18) no Cancel paired with a destructive primary, `DialogFooter`'s ≥2-child machinery inert; (R-20) no `spellcheck="false"` / autocorrect / autocapitalize / `dir` on a source-code region (**iOS half live today**; the twin is KAD-F4 — `grep spellcheck|autocorrect|autocapitalize demo/` → **0 repo-wide**, all three contenteditable surfaces share it); (R-25) **one widget, three looks, three a11y postures, three copies of the innerText-mirror** (20vh / 25vh / min-h-32; filled-and-bordered vs transparent-borderless ×2; full / none / none; `KeyframeCard.vue:43` is the third copy) — **any fold that touches only the two dialogs leaves the third.** · escalations: R-1 → BLOCKER on SS-13 probe #1; R-2/R-27 on their probes.
- **S-9 · LP-8 ≡ KF-CO-35 + ME-18 — THE `/number-field` EVALUATION** ⟨kf-LayerConfigPanel D-5/C-7; kf-ChannelOptions RR-B net-new; kf-MatrixEditor C-8; kf-CubeScene L-5/C-6⟩ · **MIN/MAJ** · the z-index row hand-rolls a number control (`type="number"` outside the declared union; unbounded domain; `parseInt(…) || 0` **inventing data**; blur-gated `@change` against live siblings) where 7.0.0 ships `/number-field` with a number-payload emit — **reached zero times demo-wide** — and `NumberField` rides the root barrel the file already imports · **cure = EVALUATE, NOT MECHANICAL SWAP (the ME-18 precedent: at MatrixEditor the cell doubles as the selection click-target).** **S-9 ID-COLLISION RULING: D's "new" proposal converges on the existing S-9 row; C's distinct class goes to SS-1/SS-2 under a FRESH id.** The commit-contract/coercion limb ≡ KF-CO-35 and is OPTIONS-UNIT's. **TimelineCaret C-4/D·M-9 is the same evaluation's third site** (a hand-rolled `<input type=number>` + 9 utility classes while the grandparent already imports glass `Input` and `KeyframeTimeline.vue:105-109` renders the SIBLING field of the same keyframe through it — *one cluster, two field idioms*; NumberField's default slot is optional so the swap is landable; the bespoke one additionally carries the name gap, D·M-5, D·M-7 and MISS-α6 — **MISS-α6: the `%` unit DISAPPEARS on edit** (a literal in the label branch, absent from the input branch, no suffix/`aria-describedby`/title, in a 40px box further eaten by the native spinner) — **discharges free with the swap**).
- **S-9 · KAD-12 — REGISTRY ADOPTION** ⟨kf-KeyframesAddDialog C-3/D-23b⟩ · **MAJ** · the reformat hotkey hand-rolls **a second global keyboard mechanism** beside the house `/keyboard` registry: the demo's sole `useMagicKeys` site, while the registry has **19** `registerShortcut` calls surfaced by `KeyboardShortcutsModal`; this binding enters none of them, is **layout-dependent** (macOS `Ï` / US-literal disjunct), and leaks a window listener for the component's life · **cure (exact): `registerShortcut("Shift+Alt+KeyF", reformat, {allowInInput:true, label, group})` — glass ships `allowInInput` + an `e.code` fallback — WHICH DISSOLVES THE ENTIRE `Ï` CLUSTER** (`Ï` untypeable in a CSS editor, the literal triplicated with no rationale, the bare-`Ï` watch arm asserting no modifier). New census row S-9.
- **S-9 · DGC toast swap** ⟨kf-DemoGlobalChrome⟩ · **SPEC** · the toast-swap verdict is **CENSUS-OWNED** (shadow row S-9, minted by that corpus and already routed to the S-census by the kf-AnimationControlsGroup adjudication) · **cure = this wave records the verdict; it does not re-adjudicate it.**
- **S-10 · KF-KE-21** ⟨kf-KeyframesEditor C-M6 + reader-1 miss M-4, merged⟩ · **MAJ** · the progress bar is a **shadow of glass `./progress`** with **no progress semantics**, completes as the debounced work starts, **and has no rest state**: `.progress-bar` declares no width (`design-idioms.css:131-144`) so as a grid item it paints **100% at idle**, and `fillMode: "forwards"` leaves the sweep at 100% — *a permanently-full rainbow rule that collapses-and-refills per keystroke* · **cure = census row S-10: glass `Progress`, EVALUATE — and bind it to something true, or delete it (KFED-UNIT owns the binding).** Companion (KAD-15): the twin animator animates `width` (layout) not `transform: scaleX()` — **the compositor-safe form ALSO dissolves the RTL and rest-state rows** — and it is placed **inside** `DialogFooter`, which is *why* `grid` overrides the footer's flex; hoisting the bar out lets the footer be bare.
- **S-10 · KSM C-9 — A DECISION RECORD, NOT A SWAP** ⟨kf-KeyboardShortcutsModal, renumbered by the seat against the banked registry⟩ · **INFO** · the `/command` shadow: **evaluate-not-replace CONFIRMED**, including the combobox-semantics and second-stylesheet reasons · **cure = RECORD THE DECISION so the census stops re-raising it.** A wave product, not a code change.
- **KSM R-2 / R-3 / R-4 / R-22 — THE FADINGSCROLL COMPOSITION (one import retires four rows)** ⟨kf-KeyboardShortcutsModal⟩ · **MAJ ×3, MIN** · (R-2) the guaranteed-overflowing scroll port is **keyboard-unreachable in every engine — proven by the focus trap, not by scroller policy**: `:10` ships no tabindex/role/name; rows are div/h3/span/kbd only; content ≈864px vs a `60dvh` port ⇒ overflow on every viewport under 1440 CSS px; arrows scroll nothing (the port is not an ancestor of the focused close-X, DialogContent is not a scroller, body is scroll-locked) and Space/Home/End are swallowed by R-1; (R-3) `scroll` declined and the dialog has no height bound of its own — below ~313 CSS px the centred box clips off both edges with no scroll owner (fails the 400%-zoom SC 1.4.10 posture; passes 1.4.4 @200%) — **and `scroll` ALONE would scroll the DialogHeader away AND the close-X with it (`:285` is `absolute` inside the same box) — the C-axis remedy is KILLED**; (R-4) no scroll-boundary affordance — `pr-1` is a quarter-gutter, the always-overflowing list just stops mid-row on overlay-scrollbar platforms, while `.scroll-gutter-stable` and `.fading-scroll--y` ship in the consumed stylesheet with **zero demo hits** and the demo owns `--mask-fade` and applies the same feather idiom at `ControlsPaneWrapper.css`; (R-22) the keyboard journey through a 19-row reference is **Close → Tab → Close** · **CURE (verified sound end to end, ONE composition): `FadingScroll axis="y"` as the named, tabbable scroll port PLUS `scroll` on DialogContent — `FadingScroll` renders `tabindex="0"` unconditionally (a real candidate for reka's `tabIndex >= 0` walker) + `role="region"` when named, already consumed at `EasingTarget.vue`; its port precedes the close-X in DOM order so `focusFirst` lands on the content.** Retires R-2/R-3/R-4/R-22 **and** the RTL/gutter residue. Same shape as census S-3/S-4/S-7 at utility granularity · sequencing: ONE FadingScroll decision for the wave, shared with CC-N-2.
- **KF-CE-17 + KF-CE-23** ⟨kf-CSSCodeEditor D-3; D-15⟩ · **MIN** · `cartoon-surface` on a bare `<div>` paints its 2px border in **`currentColor`** — the only raw-utility site in the demo, where the intended glass hairline is a ~4% foreground tint; and `rounded-lg` is the demo's only cartoon-stamped surface **off the `--radius-card` ladder**, nested inside a `rounded-card` Card · **cure = glass-ui `Surface` (published `./surface`) or `<Card cartoon>`; one radius vocabulary.** The 14% provenance table is dead (killed).
- **KF-ET-10 — THE CURE MUST LAND PRODUCER-SIDE** ⟨kf-EasingTarget C-4 = D-M-3(b), RESTORED by the arbiter (neither reader adjudicated it — a corpus-coverage correction); kf-EasingScene KF-ES-28 fold, STRENGTHENED⟩ · **MAJ** · **28 independent `Chip mode="selectable"` toggles + a hand-rolled single-select invariant** where `ToggleGroup type="single"` is used **30 lines up in the same file**; 28 tab stops vs 1 + arrows, atop a ~32-stop corrected total · **CURE-SHAPE LOCK (KF-ES-28's strengthening, binding): the `Chip` STRIPS `role|aria-pressed|data-state` from fallthrough attrs, so the consumer-side ARIA remap is STRUCTURALLY UNSPELLABLE — the cure MUST land producer-side (a selectable chip-group shape) or as the ToggleGroup reshell. A repair order listing this as a scene-side change is UNEXECUTABLE.** → the chip-group pairing rides this wave's decision **+ a BH letter for a first-class selectable chip group**; interim note: **any reshell must keep real `<button>`s (the KF-CB-7/S-2 constraint class).** The ARIA-conformance arm stays dead (reka renders the same `role="group"` + `aria-pressed` shape); the **keyboard burden** survives.
- **KF-ET-21 + KF-ES-21** ⟨kf-EasingTarget reader-1 miss M4; kf-EasingSidebar D-16⟩ · **MIN each** · **28 concurrent `backdrop-filter` glass plates inside a `mask-image` scroller** — every tile carries `.glass-capsule` (`--glass-blur-floating` + 3-part shadow), a **floating-tier** choice for a 28-cell grid when `glass-quiet`/`glass-resting` exist and `Chip` exposes `surface`/`tone` seams (materially heavier than the 28 `will-change` balls all three axes flagged; bounded, not removed, by `content-visibility`); and **three translucent surfaces stacked on one axis** (`Card cartoon tier="quiet"` + the picker's two own `glass-card`s) where the demo's two hosts of one primitive **disagree about wrapping** · **cure = ONE tier decision for the wave (a Glass Suffusion call).** Forced-colors is no longer part of the sidebar question (every tier gets a CanvasText hairline for free).
- **KF-SST-28** ⟨kf-StartingStyleTarget C-8 + reader-A MISS-4, one identity; family ≡ KF-ES-28⟩ · **MIN** · consumes the `Card` shell but **hand-rolls the header glass-ui ships** (`CardHeader`/`CardTitle`/`CardContent` all exported; the sibling consumes the family) — **and the consequence C-8 never drew: the title is a `<span>` at heading scale with no heading semantics and the plate is no named region, so the specimen is invisible to heading/landmark navigation** · **cure = family adoption decision here; a heading/landmark one-liner NO-WAVE-OWNER if the family is declined.** `.card-title`'s `overflow-wrap: anywhere` is simultaneously the KF-SST-16 cure.
- **KF-KE-31 + KF-KE-36** ⟨kf-KeyframesEditor D-14/D-16/D-17 merged; D-15 carried as filed, unchallenged⟩ · **MAJ/MIN** · naked bespoke buttons where `Button`/`.interactive-item` are reachable (the S-7 pattern), an **icon-only toolbar with zero tooltips** (`title=`/`Tooltip` grep → 0 while the parent uses `title=`), and the `opacity-70` wand reading as **disabled** inside a `justify-evenly w-full` anti-cluster; and `useToolbarKeyboard` (111 L) **shadows glass `useTabRovingFocus`** · **cure = KF-KE-36 is EVALUATE, not mechanical-swap, with the honest toolbar-vs-tablist reservation intact: READ THE PARAM TYPE; if tab-coupled, the row DROPS TO INFO.** Its correctness is a superlative, and it is **the complete a11y cure source RibbonBar D-7 needs** (`KeyframesEditor.vue:63-95`'s `role="toolbar"` + `aria-label` + roving set + `aria-hidden` decorative glyph, adjudicated correct at the bank).
- **KF-KC-37 + SP-18 + KF-KE-53** ⟨kf-KeyframeCard L-12; kf-SharePopover L·D-11/C-6; kf-KeyframesEditor C-m1⟩ · **MIN** · **import granularity**: the 24 KB root barrel for `Label` beside a subpath import for `Input`, with `./label` at 69 B in the 73-entry exports map; four symbols from `.` (a 64-chunk closure reaching `@mkbabb/value.js/css`) beside `Input` from `./forms` when `./popover` (6 chunks) and `./button` (11) are published; root-barrel + subpath mixed in one closure · **cure = a subpath-rewrite sweep. HONEST SCOPING ADOPTED VERBATIM: graph width, NOT crash exposure (`sideEffects: ["*.css"]`); C-6's refusal to inflate is adopted — this is consistency, not bundle size.**
- **KAD-6** ⟨kf-KeyframesAddDialog L-3/C-2/D-4⟩ · **MAJ** · `DialogDescription` **nested inside** `DialogTitle` → `<h2><h3>…</h3><p>…</p></h2>`; `DialogHeader` unused though in the same barrel and used correctly at `KeyboardShortcutsModal.vue:4-8`; content-model violation **plus accessible-name pollution** (reka wires `aria-labelledby` → the title subtree, which now contains the description); **the twin `CSSPasteDialog.vue:11-12` keeps them siblings — the banked twin's superlative #4 names THIS nesting as its negative** · **cure = adopt the twin's sibling composition. SEQUENCING (declared both ends): the row DISCHARGES with KF.W7's R-7 twin fold, or STANDS ALONE HERE if the fold is declined.**
- **KAD-23 + CPD R-18** · **MIN** · the one confirming action ships at **default (secondary) emphasis** — a one-action dialog never declares its primary, while glass `Button` documents a distinct `primary` rung; 2-site omission · **cure = declare it.**
- **SP-5 + SP-6** ⟨kf-SharePopover D·M-5/C-8; D·M-6⟩ · **MAJ each** · the share-URL field has **no programmatic label** and none of the input semantics the primitive exposes (only `v-model`/`placeholder`/`class`/`@keydown.enter` pass; no `id`, no label, no `inputmode`/`enterkeyhint`/`autocomplete`) while glass exports `./labeled-field` first-class; and **zero state coverage** — empty input silently returns (`Button.disabled` shipped unused), `shareState` is async with no re-entry guard (`Button.loading` shipped **and documented** unused), both error paths toast elsewhere while the field keeps painting valid (`field-control` ships `:user-invalid`/`data-state="invalid"` skin unused), and `loadHashInput` is **never reset anywhere** so the placeholder carrying the only instruction is permanently hidden after first use · **cure = adopt the primitives that already ship the states.** **FALSIFIER HONOURED: `type="url"` is genuinely WRONG (the field deliberately accepts a bare base64 param) — `inputmode`, `enterkeyhint="go"` (the `@keydown.enter` IS the submit) and `autocomplete="off"` have no such downside.**
- **SP-1 + SP-19 + SP-21** · **MAJ/MIN/MIN** · (SP-1) `p-2` on `PopoverContent` is **DEAD** — `cn()`'s three disjoint padding buckets never collide it with the base's longhands and Tailwind emits `.p-2` before `.px-(--overlay-pad-inline)`/`.py-(--overlay-pad-block)` in the same layer, so the glass overlay rung (1rem/1.272rem) ships while the class persists in the DOM **looking operative** — cure = delete it; if compression is truly wanted, `[--overlay-pad-inline:0.5rem]` rides the primitive's own √φ calc; (SP-19) no `isInsideToaster` guard **on the one surface in the pair where a toast and an open popover provably coexist** (both `loadFromInput` error paths toast AND leave the popover open; both sibling overlays install the demo's own guard; `PopoverContent` emits the hooks unbound); (SP-21, **net-new**) `align="start"` is **wrong at BOTH live mounts** — the ribbon host is `placement="right"` → `inset-inline-end: 0` **AND** `__band{flex-direction: row-reverse}`, making the FIRST `#items` child render **RIGHTMOST with ~22px of room for a start-aligned 288px surface** (structurally unreachable alignment; the collision middleware, not the author, decides placement), and at MbabbMenu the surface overhangs a 272px `align="end"` panel — **correct value is `align="end"`, which the demo uses at `MbabbMenu.vue:6` and the bank praises for exactly this geometry**; the only axis contact was a superlative clause **praising** the wrong value (killed) · **cure = three one-line corrections.**
- **KF-KC-10 + MM-4 — GLASS-OWNED CASCADE DEFECTS (no local `:deep` patching)** ⟨kf-KeyframeCard D-9, byte-scan reproduced; kf-MbabbMenu D-1 + D-18, brace-matched by the arbiter⟩ · **MAJ each** · glass-ui **emits scoped component styles UNLAYERED**, silently outranking every consumer utility: `.glass-label[data-v-87831917]` at byte 21050 sits outside the single `@layer components{` block (2531-18827) so `--font-text` beats `text-mono-small`, 500 beats `font-light`, 1.4 beats `leading-none` — **only `tabular-nums` survives, on a proportional face**; and `dropdown-menu__item` at byte 29522 is outside the same block, so `cursor-pointer` and `text-destructive` **never paint** and the vendor's own layered `.glass-menu-row:hover` colour loses to its own unlayered `color:inherit` · **cure = GLASS-OWNED CORE → BH relay (wrap `dropdown-menu/styles.css` + the `_shared/menu.css` tail in `layer(components)` — `styles/index.css` already does it for `components.css`, so the omission is NOT a build limitation); DEMO-SIDE REPAIR here (interim unlayered override or post-relay adoption). NO LOCAL `:deep` PATCHING MEANWHILE.** **Blast-radius scope (MM-44): MbabbMenu is the demo's ONLY `DropdownMenu*` consumer — which kills every "precedent exists in-tree" argument for this primitive and makes a demo-local `.dropdown-menu__*` override regression-free.** **The destructive row must be repaired to the DEMO's OWN red (MM-30 `text-accent-red`), not the vendor's.** MM-15's z correction lands in the same motion. **This is the cascade-layer face of the census token-collision lane.** · MM-4's `min-w` table row is mechanism-confirmed / effect-unproven; the D axis's BLOCKER demoted (the confirmed-guarded command still functions; the loss is affordance).

### J · MOTION TOKENS / PRM / FORCED-COLORS

- **KF-CB-27** ⟨kf-CopyButton reader-1 miss #8, verified at the tokens⟩ · **MIN** · the motion constants sit **outside both scales**: `duration: 200` vs glass `--duration-fast: 0.2s` (same value, unnamed) and `scale(1.25)` ×3 vs the house lift `--scale-hover: 1.08` / `--scale-press: 0.96` — **~3× the house magnitude, invisible to any style-block-only falsifier** · **cure = the `--kf-*`/motion-token decision MUST INCLUDE SCRIPT-SIDE CONSTANTS.** This row is why the audit is not a CSS-only sweep.
- **KF-SKEL-10 + KF-SKEL-11** ⟨kf-App.skeleton D-8/L-4/C·D-2; D-9⟩ · **MIN each** · a paint-only `background-position` sweep whose `will-change` **promotes nothing and SURVIVES the PRM arm forever** (the arm resets only `animation` + `background`) — **and the producer measured and rejected exactly this** (the K.WP P1-4 receipt, verified at the bytes); and `1.6s` is hardcoded at ≈3.1× the installed house clock (`--duration-shimmer: 5s`), **unreachable by `.motion-calm`/PRM `--motion-weight`**, a third local `background-position` shimmer `@keyframes` in one cascade · **cure = the transform sweep arrives WITH the S-6 delegation; tokenize or delegate the clock.** Era annotation mandatory (at authoring it was 7% slower than the then-hardcoded 1.5s). The **U-24.2 fan-out escalator** is named on KF-SKEL-10. The motion-register **reachability** arm is KF.W9's (PRM-mechanism unification).
- **KF-SKEL-12** ⟨kf-App.skeleton D-11/D-12/L-10⟩ · **MIN** · **no `forced-colors` and no `prefers-reduced-transparency` arm ANYWHERE in the demo** (repo-wide grep = 0) while the installed primitive ships both; and glass-ui's `accessibility.css` forced-colors block **targets state selectors only and cannot reach a generic translucent plate** (reader-1's falsifier check, adopted) · **cure = both arms arrive with the S-6 delegation; the repo-wide absence is the wave's standing datum.** Render witness → KF.W9.
- **KF-AX-15 + CubeTarget #60 + SpringHeatmap L-m-4** · **MIN/INFO** · raw motion durations beside tokenised easing **in one unit**: `180ms` ×3 off the vendor duration ladder (the same declaration consumes the ease token); `transition: --lit 160ms linear` + three 180ms literals hard-code time while the same declarations route the curve through `var(--ease-standard, ease)` — the sibling scene tokenises both halves and the unit's one duration-token reach (`duration-panel`) is the wrong register AND dead · **cure = one duration-hygiene rider across the family.** β enumerated the full ladder; C's two-member-pattern caveat is carried.
- **KF-TD-6** ⟨kf-TypingDots L:D-6/D:D-5⟩ · **MIN** · `REST_OPACITY = 0.2` (script) and `opacity: 0.2` (scoped CSS) are **two unbound authorities for one load-bearing number**; a desync is invisible (both paths still render dots) · **cure = bind them (`:style` or one custom property).** **CONSTRAINT LOCK: the cure may NEVER DELETE THE CSS FLOOR — the resting paint as a designed frame is a banked superlative (KF-EST #7); the INTENT is the superlative, the unbound duplication is the defect, and both are true at once.**
- **KF-TD-3** ⟨kf-TypingDots reader-1 missed-2; class-linked to KF-AT-15⟩ · **MIN-latent** · `count` is an **unvalidated public numeric prop whose ONLY guard is Vue's `__DEV__`-gated `renderList` branch**: in the production bundle a fractional/negative/NaN/Infinity `count` throws `RangeError: Invalid array length` **inside the LCP `<h1>`'s render**, with no `app.config.errorHandler` and no `onErrorCaptured` ancestor · **cure = the SAME ONE CONTRACT MOTION as KF-EST-18: delete, validate, or react — never two of the three half-done; `stagger`'s own fail-explicit posture is the house standard.** Same defect CLASS as KF-AT-15, **distinct mechanism** (render-throw vs silent IACVT) — **a new witness in the banked class, not a new identity class.**
- **KF-TD-7 + KF-TD-9** · **INFO each** · `.typing-dot { display: inline-block }` is **dead** (the dots are flex items of the `inline-flex` container and flex items are blockified) — false-signal cost only; and the two ornaments in the LCP `<h1>` are **phase-locked 3:1 by unguarded coincidence** (`cycleMs` 3600 / `CYCLE_MS` 1200), documented in neither file — **any edit to either literal, or any consumer binding `:cycle-ms`, silently unlocks the beat** · **cure = hygiene pass; record the relationship in the motion-clock/token decision (one comment or one derived constant).** Perceptibility explicitly NOT claimed.
- **EH-9 ≡ SP-10 — MUST-CARRY CURE RIDER** ⟨kf-EditorHeader RR-1 missed #5, third-derived at shipped-sheet offsets; kf-SharePopover D·M-2/C-5, uniqueness re-verified⟩ · **MIN** · `transition-all duration-fast` on SharePopover's trigger (`:7`) **clobbers `scale-on-hover`'s design-system spring**: built-sheet order is `.scale-on-hover`@194614 < `.transition-all`@231036 < `.duration-fast`@232395, **all inside `@layer utilities`, so document order decides** — the spring curve/duration are replaced by default-ease @ 0.2s and **every** property becomes transitionable; uniqueness confirmed (13 sites / 10 files; only `SharePopover.vue:7` carries both on ONE element — TimelineTrack's pair sits on different nodes) · **cure = ONE SharePopover motion batch. MUST-CARRY RIDER: the axes' prescribed BARE DELETION ships a snapping `hover:opacity-50` — the `sharePopoverOpen ? 'opacity-100' : 'hover:opacity-50'` recede-affordance books WITH it.** Novel (`transition-all` = 0 across all three challenge files). C-5's UNPROVEN marker is discharged statically.
- **KAD-11** ⟨kf-KeyframesAddDialog D-8⟩ · **MAJ** · `prefers-reduced-motion` **not honoured on the 1000ms width sweep**; the engine gate is opt-in (`respectReducedMotion` defaults `false`) and this is **not delegable to CSS** (a WAAPI/rAF inline `width` write) · **cure = the fix is a PAIR — the twin lives at `KeyframesEditor.vue:254-258`.** PRM mechanism unification is KF.W9's; the component pair is this wave's.
- **KeyframeTimeline D-1 / L-1 / C-3** ⟨kf-KeyframeTimeline; RR-A's BLOCKER demoted⟩ · **MAJ** · `<Transition name="fade-slide">` binds **a class set glass-ui 7.0.0 does not ship**, and two comment blocks assert a false, line-precise provenance (`transitions.css:23-37` — a range that cannot resolve in the installed single-line artifact); whole-package grep 0; the producer source carries the tombstone *"RETIRED, census-dead, clean break, no alias"*; the 3 demo hits are self-references · **cure = adopt a shipped transition set OR author one WITH THE PRM BRACKET THE COMMENT FALSELY CLAIMS; the prose correction rides the same commit (KF-CE-41).** The phantom-dependency corroboration folds to census F-1 → KF.W0.
- **KF-KE-39 / -40 / -42 / -59** ⟨kf-KeyframesEditor D-25; D-26/27/28; D-30; reader-1 miss M-6⟩ · **MIN** · `overflow-x-scroll` on the 40px toolbar reserves the gutter unconditionally while glass ships `.scrollbar-*`; three spacing no-ops + a 32/24px asymmetric footer rhythm + `sticky bottom-0` + `m-4` floating the footer 16px above the scrollport floor; physical-only offsets (`top-2 right-4`); **mixed radius vocabulary** (`rounded-panel` vs `rounded-xl` resolve identically **today** — 12px — and **desynchronize on any `--radius-panel` retune**; `rounded-lg` = 10px beside them) · **cure = one polish pass; the radius vocabulary decision belongs to the token audit.** RTL is correctly scoped as a repo-level posture row.
- **SquareScene D-18** · **MIN** · off-scale spacing literals (`gap: 0.15rem 0.45rem`, `0.15rem`) against a 0.25rem grid **in a demo with a documented geometry-token layer** · **cure = rides the token discipline** (NO-WAVE-OWNER at its bank; carried here so the sweep does not miss it).

### K · HERO AURORA — ONE CONFIG MOTION (KF-HA-1/-2/-3/-7/-8/-18 + KF-APP-15/-16/-48)

**PRECONDITION (KF-HA P-7, MUST resolve BEFORE this cure set executes): a version probe of KF-HA-1 against glass-ui 6.0.0 (what `npm ci` at stale HEAD installs) — if 6.0.0's `light` carry was unconditional, the defect is a SILENT CROSS-MAJOR REGRESSION, not an authoring error, which changes the cure AND makes the gate gap the enabling cause twice. One install; forbidden at the bank; note the legacy-peer-deps constraint.**

- **KF-HA-1** ⟨kf-HeroAurora D:D-1(a/b/c) / L:D-1 / C:D-3+D-4; WGSL leg rescoped⟩ · **MAJ** (reader-LC's BLOCKER = recorded dissent) · the cursor-as-light axis is **dead three ways**: (a) `interactivity: { light: true }` (`:70`) is **dropped by `resolveAtoms`** — the medium arrives one layer below the door via `...PAPER_WASH_GROUND` (`:72`) and the door assigns `interactivity` wholesale; (b) the same literal is a hard **TS2345** against installed 7.0.0 (`AuroraSmoothInteractivityAtom { light?: never }`; the light-bearing arm REQUIRES `medium`) — **the defect is exactly one missing key**; (c) **even cured, `light` is INERT on this ground** (`mediumCrayon` never calls `relightImpasto` and `impasto: 0` makes both lit terms the identity) — **so the shipped-vs-cured visible delta is exactly ZERO** · leg (b′) `uLightDir` absent from the WGSL struct is **engine-conditional** (WebGPU cohort only; the WebGL2 net has the uniform), **never a universal kill** · **CURE IS A DESIGN DECISION, NOT A PATCH (axis C's framing adopted): `medium:{kind:"crayon"}` restores type+carry but no light; the honest options are a stroke medium (abandons paper-wash) or DELETE the atom + the `:57-59` prose — the owner's T-row wording was "done right OR removed." DECIDED WITH SS-13's P-1.** The crayon-ground/light-axis incompatibility is producer-domain → BH relay.
- **KF-HA-2** ⟨kf-HeroAurora, reader-DU MISSED #1, verified whole by the arbiter; missed by reader-LC and all three axes⟩ · **MAJ** · **the delivered wash is structurally ~0.19 — roughly DOUBLE the authored 0.1 bound and ABOVE the P-HERO 0.15 the amendment cut below**: placeholder and armed canvas are stacked `grid-area:1/1` siblings BOTH at `var(--aurora-opacity-ceiling)`, **no rule hides the placeholder when the canvas arms**, the compiled render fn emits the placeholder unconditionally, and the fragment epilogue `fragColor = vec4(col * uAlpha, uAlpha)` with probed `alpha = 1` means **the canvas is opaque wherever it draws**; composite = 0.81·page + 0.09·placeholder-ground + 0.10·canvas ≈ **0.19 presence** — and `.aurora-canvas` transitions 0→ceiling over `--duration-slow`, so **the home route BRIGHTENS from 0.10 to 0.19 shortly after first paint, at the LCP moment under the eager arm** · **cure = MECHANISM IS PRODUCER-OWNED → BH relay (placeholder-hide-on-arm, or a documented compose-mode). THE CONSUMER DECISION IS THIS WAVE'S: "was 0.1 or 0.19 the number the owner blessed?" — with an interim halved ceiling as one admissible answer.** **All three axes and reader-LC computed on the false 0.1-presence premise — the D axis built a WCAG table on it; that table is dead on BOTH its threshold and its compositing premise.**
- **KF-HA-3** ⟨L:D-6/D:D-3 + C:S-4's "both themes" scope-kill folded⟩ · **MAJ** · **the dark leg is unaddressed while `:55-56` claims "both themes"**: `config` is a plain setup-time literal — no `computed`, no token read, no `lightnessScheme` — seeded `#7c5ce6` (15.6° off-hue, ΔL −0.16, +54% chroma vs the dark token); the derived ramp pins at L **0.42-0.74**, **entirely the light-pastel band glass-ui's own doc names "the O-26 dark-leg defect"**, while the page carries a live dark toggle and the sibling grid ink DOES retint · **cure = thread the theme (`lightnessScheme` + reactive seed) — AND THE `OklchStop` SEED FORM KILLS KF-HA-13's PARSE SEAM IN THE SAME EDIT — or state in prose that the light band is deliberate at this ceiling.** The hue half of C:S-4 survives; **bracketing a hue is not previewing a theme** (hue is one of three coordinates).
- **KF-APP-16 ≡ KF-HA-7** ⟨kf-App C-M3; kf-HeroAurora, one identity, folded⟩ · **MAJ** (axis C's BLOCKER killed) · `initStrategy: 'eager'` arms the GPU **inside the mount commit** — skipping the idle defer AND the intersection gate (**inert here: `fixed inset-0` never leaves the viewport**) — against the file's own `:8` "lazy WebGL arm past first paint"; the WebGL2 leg is a synchronous compile-link; **the arm re-runs on EVERY return to home** (`v-if="isHome"`) · **cure = DELETE THE PROP — the library's own deferred-path comment refutes the only defence and makes prop-deletion LIBRARY-ENDORSED.** LCP/TBT magnitude, >50ms escalation bar, per-navigation re-arm → KF.W9.
- **KF-APP-15** ⟨kf-App C-M6⟩ · **MAJ** · HeroAurora's **static** import puts glass-ui's 202KB aurora module + value.js's colour graph in the **boot chunk on every route** — the STATIC value.js exposure the kf-edge boundary proof cannot see (`v-if="isHome"` gates render, **not the module edge**) · **cure = dynamic-import the hero layer; co-rider of the one config motion.** **Scope-CORRECTS, does not delete, superlative S-G.**
- **KF-APP-48 ≡ KF-HA-18** ⟨kf-App C-m8 + β-kill; kf-HeroAurora L:D-8/C:D-12, third independent confirmation⟩ · **MIN** · `PAPER_WASH_GROUND` spread **AFTER** the atoms silently overrides authored knobs (`medium` smooth→crayon; `saturation` hard-overwrite 0.913→0.92, Δ0.007 inert); "ONLY the opacity ceiling moved" is **false** · **cure = the durable trap is a future `colorEnergy` retune moving 3 of 4 limbs. ARITHMETIC LOCK: EXACTLY 7 of 12 keys change, 5 already equal — the "clobbers 12 fields" claim is killed THREE times over; do not re-derive.**
- **KF-HA-8** ⟨D:D-5/L:D-10/C:D-9; authorship arm killed⟩ · **MIN** · the one interaction dial runs at the **library maximum**: `setCursor(x, y, 1)` against the compiled `0.8` default (~120° max domain-warp rotation at strength 1 vs ~96° at 0.8), **unblessed by any comment or verdict** — and `setCursorRadius` is exposed, never called, leaving the default **0.25 quarter-viewport radius** unexamined beside it · **cure = ONE retune decision under the amendment: strength AND radius together.** The swirl axis is the DOCUMENTED consequence of authoring `light` (coupled default) — **the "unauthored/collateral" framing is dead.**
- **KF-HA-9 + KF-HA-11** ⟨L:D-3/D:D-6 + L-addendum C-1 + reader-LC's degenerate-states miss; L:D-9+L:D-11 + reader-LC's pointer-asymmetry miss⟩ · **MIN each** · under PRM **every forwarded mousemove executes one synchronous, provably-identical full-viewport draw inside the input task** (wake→tick inline; PRM gates the reschedule, not the suspend set), and the handler runs its two **forced-layout reads** (`innerWidth`/`innerHeight`) uselessly in the cssOnly cohort and the pre-arm window, while `:80-82`'s "no style/layout flush … collapses to one write per frame" overstates on both counts; and pointer policy is internally inconsistent **three ways**: (a) `pointerType !== "mouse"` drops **pen HOVER** though the comment justifies excluding touch only, (b) `onPointerLeave` **takes no event and cannot gate**, so touch/pen exits clear-and-wake an engagement only a mouse can create — **under PRM that is a synchronous draw triggered by an input class the file excludes**, (c) no `visibilitychange`/`blur` release, so alt-tab leaves the field engaged at a stale attractor · **cure = PRM early-out + `useWindowSize()` (an already-imported dep) covers all states; ONE pointer-policy pass: `=== "touch"` exclusion form, gate the leave handler, one `visibilitychange` release line.** The `wake()` PRM seam itself is producer-side → BH relay with HA-1/-2.
- **KF-HA-10** ⟨L:D-12⟩ · **MIN** · `motion: "drifting"` resolves to a nonzero drift quadruple (0.015/0.015/0.008/0.05) so **`needsAnimation()` never parks outside PRM** — a whole-session full-viewport GPU loop under the least-visible layer on the page; **the amendment cut presence, not cost** · **cure = AN OWNER RULING QUESTION, not a fix order (the L axis's own calibration, adopted): is the drift register blessed?** Frame cost → KF.W9.
- **KF-HA-12** ⟨D:D-7 strengthened past its own falsifier + reader-DU MISSED #2⟩ · **MIN** · **neither `forced-colors: active` nor `prefers-reduced-transparency: reduce` reaches the aurora** — and BOTH are established house idioms (the three global a11y sheets each carry forced-colors with 0 aurora selectors; `a11y-fallback`/`paper.css` carry reduced-transparency with 0 in demo/) — **the system swept and the aurora was omitted from the sweep: a producer conformance gap, not a consumer invention** · **cure = PRIMARY → BH relay (the aurora joins the a11y sweep); two-line consumer stopgaps (`display: none` under both queries) land here.** Visual outcome **at the true ~0.19 presence** → KF.W9.
- **KF-HA-13** ⟨C:D-8, both readers re-executed⟩ · **MIN** · `resolveAtoms` runs **unguarded at `<script setup>` top level** and glass-ui's bridge converts value.js DIAGNOSTICS to **throws**: `var(--accent-kf)` / non-opaque hex / `color-mix()` seeds all **THREW in execution** — **one token-ising edit (the obvious next one; `:54-56` names `--accent-kf` as the tracked hue) from a white-screened home route with no error handler anywhere** · **cure = the `OklchStop` seed form removes the parse seam AND is the same edit as KF-HA-3's theme lever; else a guard.** The bridge's throw posture is producer-domain → BH note.
- **KF-HA-15 + KF-HA-16 + KF-HA-17 + KF-HA-20** · **INFO/MIN** · neither `onInitError` nor `@rendererStatus` bound and no app `errorHandler` — a GPU-init failure is **silent everywhere** (correctly INFO: the documented degrade is good UX; **worth one console breadcrumb given the eager arm raises boot-time failure probability**); the scoped sizing block AND its `@supports` fallback are **wholly dead** (`fixed inset-0` is viewport-sized pre-`dvh`) and the wrapper div itself is removable (single-root Aurora; attribute fallthrough carries class + aria-hidden) — **−1 DOM node, −15 lines, −the S-6 argument**; `:84 "Aurora 5.x"` is **two majors stale** and breaks the file's ~78-col wrap mid-sentence (**the edit-scar and KF-HA-1 are the same event** — the 5.x→7 window is when the medium-discriminated arms landed), with surviving false lines at `:8` (lazy arm), `:6-7` (PRM-safe CSS substrate — `resolveRenderMode` has NO PRM branch; PRM is honoured by the frame-loop freeze, **a different mechanism at a different layer**), `:57-59` (axis ON), `:59-61` (verbatim/ONLY); and three explicitly-passed **library defaults** (`harmony`, `zones.arrangement`, `render-mode="auto"`) read as overrides but restate defaults while `{ passive: true }` on `pointerleave` is inert (non-cancelable event) · **cure = the composite KISS reduction adopted whole + prose reconciliation (intent stays; LIBRARY-INTERNALS NARRATION DIES — the D axis's own law) + one optional breadcrumb.**
- **KF-APP-49** ⟨kf-App C-m10⟩ · **MIN, UNSETTLED** · hand-rolled pointer wiring beside the producer's `useCursorInteraction` **on the same subpath** — **UNSETTLED by three seats** (the stage-element/nuclei falsifier is credible) · **cure = EVALUATE-NOT-MECHANICAL-SWAP; settle at the bytes in this wave.** Carried as an open question, not a prescribed fix.
- **KF-HA-4 (prose limb only)** · **MAJ at its bank; the PROSE limb is this wave's** · both cited oracles (`proof:cursor-light-subtle` ×3 in-file + `App.vue:44`; `proof:no-hand-rolled-cursor-tracker` `:5`) were dissolved at `70b32501`; the owner-ruled 0.1 bound has **zero** executable coverage · **the GATE rebuild is KF.W4's** (and the corpus's prescribed replacement is **unwritable as specified**: `HERO_AURORA_OPACITY_CEILING` is a `<script setup>` compile-local — nothing to import; the assertable surface is the rendered `--aurora-opacity-ceiling` on `.aurora-root`, or an `export const`) · **cure here = the in-file prose cites ride this wave's file-touch.** The stage-manifest row → KF.W10.

### L · APP-SHELL CHROME + DOCK

- **KF-APP-5 ≡ EditorShell D-1/L-1/C-1 (+L-3, D-15 riders) ≡ EditorHeader F3** ⟨kf-App M-A; kf-EditorShell; kf-EditorHeader — the bank won THREE TIMES against fresh re-filings⟩ · **B at its bank** · the header ribbon's **three chrome controls (Share, shortcuts trigger, dark-mode toggle) are `inert` + `aria-hidden` + zero-inline-size at rest with NO focusable affordance** — keyboard and AT lockout **source-decided from the installed producer** (`inert: !w.value`, `"aria-hidden": !w.value`, hover-opener `pointerType !== "touch"`, anchor = a non-focusable div); EditorShell supplies `#items` only, **no `#anchor`** · frontier: the phantom `mode="persistent"` is **already deleted at origin/master — the lockout is NOT** (still no `#anchor`) · **cure = CONSUMER HALF HERE: supply `#anchor` content / adopt the producer's real API. PRODUCER HALF → BH relay (a collapsed-at-rest toolbar whose empty-anchor state is unreachable-by-construction).** **COMPLETING 2.1.1 LEMMA (EditorShell rider (a), makes the case complete against the strongest objection): the dock's MbabbMenu copies are keyboard-INOPERABLE by reka construction (`MenuItem` SELECTION_KEYS click-on-the-item + `@select.prevent`; Tab preventDefaulted in content), frontier-durable — so with banked kf-ChromeDock D-18 (collapsed dock = zero tab stops) NO KEYBOARD ROUTE TO SHARE OR THEME-TOGGLE EXISTS ANYWHERE IN THE APP.** **TOUCH CONSEQUENCE CENSUS (EditorHeader rider (a)): the F.W15.S3 shortcuts trigger exists at exactly two sites (`EditorShell.vue:32-41` + `registerShortcut("?")` at `:190`), so ON A TOUCH DEVICE THE SHORTCUTS MODAL IS WHOLLY UNREACHABLE** (the bank noted only the keyboard `?` survivor); Share/dark-mode survive **solely** via the MbabbMenu dock duplication · **GEOMETRY REFINED (EditorShell rider (b)): the rest sliver is ≈12 × 52px, not 12 × 40 (band padding + the uncollapsed 40px cross-size)** — refines the bank's "undecidable statically" cell; the affordance-perception half stays KF.W9 · L-3 is frontier-CURED and reframes as a fossil, not author error.
- **EH-4 + EH-5 + EH-8** ⟨kf-EditorHeader D-10; D-20 (cascade third-derived); RR-1 missed #4⟩ · **MAJ/MAJ/MIN** · `title="Toggle dark mode"` **collides with DarkModeToggle's own state-aware accessible name** (only `class`/`type` are stripped, so `title` lands on the same `<button>` as the conditional `aria-label` + `aria-pressed`) — **accessible name ≠ visible tooltip, WCAG 2.5.3**; `aspect-square w-8` is **inert on the aspect** and yields a 32×36 box (`dark-mode-toggle.css` `@layer components` base 2.25rem with size arms for sm/lg/control/dock and **no `md`** — the default; `w-8` in utilities wins WIDTH only; both dimensions definite ⇒ `aspect-ratio:1` no-ops); and the live cluster is **incoherent three ways at once** — boxes 24×24 / 32×40 / 32×36 and glyphs 24 / 16 / 20 on one centred band, with **two inert `aspect-square`s** (SharePopover trigger `p-0` + `icon-lg` 24px; shortcuts Button `icon-only` = `--control-h-md` 40px **defeated on the inline axis only by `w-8`**; DarkModeToggle per EH-5) · **cure = drop the `title` (the producer already names itself); ONE sizing decision for the band.** *D-20 caught one third; the `icon-only`-invoked-then-broken instance is the unflagged one.* Rendered ladder → KF.W9.
- **KF-APP-21** ⟨kf-App D-10/L-21/C-M8⟩ · **MIN** · **three nested TooltipProviders, 700ms vs 100ms (7×) in one chrome**; glass-ui forwards no defaults; the frontier keeps the root provider **deliberately** (DP-02/CC-03) · **cure = ONE PROVIDER POLICY — the root provider is LOAD-BEARING at the frontier, so the cure is CONFIG UNIFICATION, NOT DELETION.** Identity: the descendant re-provides are named at kf-AnimationControlsGroup D-10/C-5/L-9 (the group row already names `ChannelControls.vue:2` as the copy to delete); one identity, no re-booking.
- **ChromeDock D-5 ≡ EditorHeader F4** · **MAJ** · **four glyph rungs in one dock row, none the dock's own**: the demo `icon-*` utilities **shadow glass-ui's `--icon-*` tokens by a uniform +1 rung under identical stems** (demo 14/16/20/24 vs producer 12/14/16/20), and `icon-lg` (24px, utilities layer) **defeats `--dock-icon-glyph`'s invariant 0.5 ratio**, drifting it to 0.60/0.545 with pointer type; **the identity glyph is the SMALLEST in the row and the nav toggle the LARGEST — the reverse of the file's own `:226-232` grammar** · **cure = token-alignment family with KF-APP-25/-26; the +1-rung shadow is a §6.3 class instance at utility granularity.**
- **ChromeDock D-22 + RR-1 MISSED #1 (merged)** · **MAJ** · the `gap-2` wrapper div at `:225` is **the single child of GlassDock's default slot**: it substitutes 8px for the token'd 6px **AND SILENTLY DEFEATS THE PRODUCER'S ENTIRE STAGGERED MORPH CHOREOGRAPHY** (the reveal cascade rides `.glass-dock[data-morphing] .dock-layer.is-active > *` with `nth-child(2 of *)`/`nth-last-child(2 of *)` onset steps — **with ONE direct child every arm is a no-op and the whole row fades/scales as a single block**; `data-morphing` is live), and latently defeats `overflow="wrap"` · **cure = DELETE THE WRAPPER (children directly into the slot); retune `--dock-layer-gap` if 8px is wanted.** *Same one-line root cause as the 2px, strictly more consequential: the primitive's signature expand animation is lost on the app's most-seen chrome.* Magnitude → KF.W9.
- **ChromeDock D-23 / C-5** · **MAJ** · the dock floats over the animated aurora `<canvas>` and **never tells the legibility observer** — the sampler runs (both defaults ON) and resolves `null`, falling to the static stack-walk on the landing route · **CURE LAW (RR-1's missed caveat, adopted — this is the row's most important cell): the d.ts advertises "element, getter, or CSS selector" but THE INSTALLED RESOLVER RETURNS `null` FOR A STRING — the remedy MUST pass a getter or an element; A SELECTOR-STRING DIFF WOULD SHIP GREEN AND CHANGE NOTHING.** The doc cell joins D-13's drift ledger. Contrast delta + observer cost → KF.W9.
- **ChromeDock D-24** · **MIN** · a **health-status** primitive carries the **selection** signal (`online` green / dashed `unknown` ring per non-current row) against the dock's own *"never a saturated brand hue"* register, **while `--dock-selected-accent` — built for the job — sits unused** · **cure = consume the producer's selection vocabulary.** The filled/hollow shape contrast is the mitigating half.
- **MbabbMenu MUST-CARRY RIDER on banked kf-ChromeDock M-4** ⟨kf-MbabbMenu, reader-DU's pair of misses, confirmed at the bytes; kills reader-LC's modal-composition cells⟩ · **SPEC** · **glass-ui's bare `{ type: Boolean }` declarations make the menu non-modal BY ACCIDENT and the primitive PERMANENTLY CONTROLLED** — the same Boolean-cast mechanism as KF-CO-1 · **CONSEQUENCE (the reason this is MUST-CARRY): kf-ChromeDock M-4's cure AS WORDED WOULD SHIP AN UNOPENABLE MENU. The rider travels with M-4 wherever M-4 lands.** Producer half → BH relay (`default: undefined`).
- **KF-APP-40 ≡ census S-6** ⟨kf-App C-m4⟩ · **MIN** · `SceneSkeleton`'s `label` prop is dead API and the glass `Skeleton` swap is **root-barrel-only** (73 subpaths, no `./skeleton`) · **cure = folds by reference to census S-6 ("keep stage geometry, delegate the plate") — and is SHARPENED IN PLACE by KF-SKEL-6's cure inversion (delegate to the STAGE-CARD register). One identity; never re-booked.**
- **TD-24 + TD-36 (sequencing only — the cures are NO-WAVE-OWNER)** ⟨kf-TransportDock⟩ · **INFO/MAJ** · the menubar peak destroyed per unmount is **moot today** (its consuming chain is IACVT under TD-13/KF-APP-6) and **becomes a live MINOR AFTER this wave's token cure**; and the transport host is a **full-viewport-width pointer trap** (`fixed left-0 right-0 z-dock`, ~90px tall, no `pointer-events: none` and no re-enabling wrapper — every pixel of the band outside the centred pill swallows pointer events over the stage/timeline beneath, while the sibling dock declares the exact guard it omits at `ChromeDock.vue:213-218` **and is narrower**) · **cure = TD-36's cure is NO-WAVE-OWNER (adopt the ChromeDock pair, two classes); DECLARED HERE BECAUSE ITS CONSEQUENCE INTERACTS: the band-reserve cure does NOT remove the trap, only the content beneath it — and TD-24 must be re-graded INFO→MINOR after the token cure lands.**

### M · A11Y WRAPPER / SHELL / RESIDUE

- **KF-SKEL-1** ⟨kf-App.skeleton D-2/L-1/C·D-3 + r1-miss M5/M6⟩ · **MAJ** · the announced a11y contract is inert **FOUR independent ways** — `aria-busy="true"` suppresses and never clears (a static literal; the node is destroyed on resolve); the `role="status"` region's only content is `aria-hidden` + an empty span; **the region is BORN INSIDE THE SUSPENSION AND DIES WITH IT** (a transient live region presents no announcement window, so *the corpus's own "smallest cure" of putting text in the region is INSUFFICIENT*); and the swap's documented focus destination `.scene-host` is a **nameless bare `div`** (`tabindex="-1"`, zero `aria-`/`role` in all of App.vue) · **FAN-OUT (what makes this MAJOR): `U.G.md:253-254` + `lane-24:77-79` canonize these exact attributes as "the house skeleton spec" / "already-correct."** · **cure = a PERSISTENT announcer at/above `.scene-host` (the in-tree correct idiom is `CopyButton.vue:15` — an sr-only `aria-live="polite"` region that OUTLIVES its updates) + a name/role on the focus target + the house-spec citations corrected wherever quoted.** SR speech capture → KF.W9; the structure is source/spec-proven.
- **KF-SKEL-13 + KF-SKEL-14** ⟨D-14/L-7/C·D-4-prop-half + r2-miss 3; D-15/L-8.1/L-8.3/C·D-7⟩ · **MIN** · the `label` prop is dead API (3 references repo-wide) **and could not be heard if passed** (KF-SKEL-1), **and the grammar is the banned legacy form** — `DESIGN.md:202-206` §9.2 rules `withDefaults` legacy (7 sites repo-wide); **L-7's "the macro form is fine" cell is CORRECTED: fine as Vue, WRONG AS HOUSE GRAMMAR**; and the docblock is false on **four counts** ("THE shared" — one consumer; "the skeletons tier" — deleted by `969990f6`; "a static dimmed plate" — **NOTHING DIMS**, the PRM arm touches only the sheen while plate background/border/shadow are untouched; "matching the stage geometry" — KF-SKEL-8), with five tranche ids unresolvable from inside keyframes.js · **cure = any wave touching the file migrates to reactive-destructure defaults and rewrites the docblock to the tree's truth; the prop LIVES OR DIES WITH KF-SKEL-1's cure.**
- **KF-SKEL-22** · **INFO** · the challenge's contrast numerals **drift from their own token sources** (border 1.676/2.204 vs stated 1.63/2.46; sheen-peak 1.172 vs 1.08); thresholds and directions unaffected · **LAW (carry into every contrast cure in this wave): contrast figures are RE-DERIVED AT USE, NEVER QUOTED.**
- **KSM R-1 — GLASS-OWNED with a consumer tail** ⟨D-2/L-2/C-1/C-3, one identity; + reader-B's M3 folded as cure-shape law⟩ · **MAJ (escalatable)** · **the whole 19-binding registry stays LIVE behind the open modal**: Escape rewinds and halts the animation then dismisses (reka's DismissableLayer neither stops nor prevents); Space toggles playback and, `preventDefault:true`, **swallows the last scroll affordance**; `Delete` destroys the selected keyframe **and fires on Backspace too** via the alias table; Mod+Z/Mod+Shift+Z mutate the timeline; `[ ] 1 2 3 R Home End` all act behind the scrim; Mod+S raises the toast that feeds R-5. **The registry's published surface has no suspend/scope/priority (five exports).** · **CURE-SHAPE LAW (M3, the cell no axis saw — and it CONSTRAINS THE RELAY'S API DESIGN): the dispatcher is LIFO FOR ESCAPE ONLY — a demo-side later-registered guard can pre-empt Escape but can NEVER pre-empt Space/Delete/Mod+Z, which hold FIFO priority from AnimationControlsGroup setup forever. The destructive half is therefore PRODUCER-GATED.** → **GLASS-OWNED (BH relay: a registry suspend/modality affordance; the relay MUST carry the LIFO/FIFO asymmetry so the API is designed against it) + a KF.W6 consumer-wiring tail on discharge.** Rung follows the banked kf-CSSPasteDialog R-3 precedent (a silent but **reversible** destruction is MAJOR — `Delete` routes through the registry-bound Mod+Z; Escape's `reset()` destroys transient playhead position, not persisted data) · **escalation: an SS-13 witness of ANY destruction path behind the modal with no live undo route re-promotes to BLOCKER.**
- **KSM R-5 + R-8 + R-11 + R-12 + R-13 + R-14 + R-15 + R-16 + R-17 + R-18 + R-19 + R-20** · **MAJ ×3, MIN ×9** · (R-5) missing the `@interact-outside` toaster guard **both sibling dialogs carry — and the modal's own live Mod+S is what summons the toast**; the Toaster is an `<html>`-teleported singleton so tapping the toast is interact-outside ⇒ **the reference self-dismisses**; one attribute, copied verbatim; (R-8) keycaps are **bare platform glyphs with no accessible text** (`⌘ ⇧ ⌥ ⌃ ␣ ⌫` on Mac, arrows/`↵` everywhere), each the sole text node of a `<kbd>` with no aria-label/title/sr-only twin — **U+2318 is "place of interest sign"**; the demo rightly owns zero platform knowledge, **so the durable cure is a PRODUCER accessible-name companion to `formatComboParts`** (interim: an sr-only twin per row) → GLASS-OWNED rider on the R-1 relay; (R-11) label↔keys pairing carries no semantics (no `<dl>`, no list roles, no association; heading levels correct); (R-12) the **hover plate promises interactivity the rows do not have** (`hover:bg-muted/50 transition-colors rounded-md` with no click/role/tabindex/cursor) — reading-position tracking is a defensible reading, which is what holds it under MAJOR: **add the genuinely-good affordance (click-to-run) or strip to an honest treatment**; (R-13) group headings hang 8px left of their rows, four times down one column; (R-14) multi-key combos **lose their `+` off-Mac** (`h(e)` encodes the platform join rule; demo usage of `formatCombo`/`isMac` = 0) so `Ctrl Shift Z` renders as three floating caps **on the platforms where `+` IS the convention**; (R-15) the `?` binding renders **twice in one dialog**, the literal bypasses the imported formatter, "panel" is the wrong noun, and the copy is unactionable on touch — **agreement between the two paths is the accident of `e.trim()` fall-through**; one copy edit cures all four faces; (R-16) `{{ shortcut.options.label }}` rests on an undocumented runtime filter while **the sibling optional field IS guarded — the asymmetry is the tell** (durable cure = producer type-narrowing → GLASS-OWNED rider; local guard meanwhile); (R-17) **the Delete row UNDER-REPORTS ITS OWN BINDING** — Backspace also destroys, the panel shows one chip — *a keyboard reference that under-reports a destructive binding is squarely this component's purpose*; (R-18) `text-muted-foreground` on DialogDescription restates the primitive's default and silently pins the call site against a future producer re-tone; (R-19) **the demo's only `DialogHeader` consumer**: below 640px the title/description centre while all 19 rows stay left (`text-center sm:text-left`) — an alignment-axis break exactly where the dialog is most cramped; (R-20) a 4-group port that **always** overflows ships **no sticky group headings**, and once a heading scrolls off proximity is the only grouping signal left — **and R-6 establishes the headings are already the weakest signal in the panel** · **cure = one composition pass on top of the FadingScroll import.**
- **KSM C-7/L-4 + M7** · **INFO** · `:key="shortcut.raw"` latent (composite-key one-liner noted for any wave touching the file); and **the dialog graph pays for the engine and never runs it** (`useSpring-*.js:3` imports `SpringProgress`; `springPreset` never passed, so the spring mount is gated off) — *the dogfooding irony named*; a `springPreset` pass is a one-token evaluation · **cure = riders on whichever commit touches the file.**
- **KAD-3 + KAD-7 + KAD-19 + KAD-20 + KAD-25 + KAD-16 + KAD-F1 + KAD-F4 + KAD-F5** ⟨kf-KeyframesAddDialog⟩ · **MAJ ×3, MIN ×6** · (KAD-3) **syntax highlighting is INERT for the entire authoring session** — the always-empty first open makes `highlight()` write `innerHTML=""` and set the marker, and every later `highlightAll()` short-circuits; the only recolour path is the undiscoverable Shift+Alt+F — **the component's core advertised affordance does not function during typing** (**the well-swap MOOTS it**); (KAD-7) **Tab is trapped forward** (`key==="Tab"` `preventDefault()` unconditional, no Esc-arm/F6, no `<form>` for Enter; Shift+Tab escapes via FocusScope `loop`, forward Tab does not) — WCAG 2.1.1 — **the well-swap restores native Tab; else a documented keyboard exit**; (KAD-19) long lines escape the panel (no `overflow-x`; hljs's `pre code.hljs{overflow-x:auto}` never matches because `hljs` is on the `<pre>` and the `<code>` is destroyed) — **same overflow class as CPD R-26**; (KAD-20) **no empty state** — the always-blank state after success/first open renders a 25vh slab with no placeholder/prompt/example and no accessible name to fall back on; (KAD-25) rich contenteditable with **no paste handling and no `plaintext-only`** — a paste from any highlighted page inserts foreign `<span style>` markup and the marker-gated highlight **never re-normalises**, so pasted colours sit over the theme for the dialog's life; (KAD-16) dead-token cluster — `:41 class="sticky bottom-0 class grid"` (a **literal `class`** defined by nothing; `grid` defeats DialogFooter's flex; `sticky bottom-0` inert with no scrolling ancestor), `:50` bare `bottom`, `:37` `css` + an inert `relative`, `:43 type="submit"` **with no `<form>` in the portalled tree** (which also removes the Enter workaround for KAD-7); (KAD-F1) no scroll owner → **FOLD to CPD R-2** (co-site #2; **this site's tell: the inert `sticky bottom-0` footer is the pin-the-actions idiom the author intended, DEFEATED BECAUSE `scroll` WAS NEVER TURNED ON**); (KAD-F4) → FOLD to CPD R-20 (**on iOS Safari the platform MUTATES the typed CSS — auto-capitalise after `;`/`{`, red-squiggle idents — and `onInput` emits the mutated `innerText` into the model**; folded at MINOR for consistency, *reader-A's MAJOR preserved in DISSENT*); (KAD-F5) `min-h-[25vh]` → FOLD to CPD R-10 · **cure = most die in the S-9 swap; the seat STATES which before spending a cure.** **KAD-1's `innerHTML` DOM-XSS sink is KF.W5's FRONT-LOADED one-line fix (`el.textContent = s`) — NOT this wave's — but the well-swap removes the whole `innerHTML` path structurally.**
- **KF-CO-18** ⟨kf-ChannelOptions D-4⟩ · **MAJ** · **`.gold-shimmer` cannot paint at this call site** — the whole vendor sheet is `@layer components` and `.gold-shimmer` carries `color: transparent` **inside it**, while `text-muted-foreground` is a real utility in the last layer and the call site keeps it unconditional; **the only rendered acknowledgment of a detail easing never appears — which is what makes KF-CO-23 a *silent* dead end** · **cure = drop the colour utility when shimmering, or a vendor variant.**
- **KF-CE-20** ⟨kf-CSSCodeEditor D-7 = C-13⟩ · **MIN** · `fontFamily: "Fira Code"` hardcoded with **no generic fallback**, bypassing `--font-mono`; latent (style.css:9 loads the payload) · **cure = the token-read cure COORDINATES with this wave; the pipeline row is EDITOR-UNIT's.** C's MINOR beats D's MAJOR.
- **KF-KC-4/-13 companions · KC-21 · KC-26 · KF-KE-45** ⟨kf-KeyframeCardList D-11; reader-DU M-5; kf-KeyframesEditor D-33 rescoped⟩ · **MIN** · the list rhythm is **double-encoded** (2rem void + hairline) at **4.1× its 8px frame**, and by choosing `display:contents` the list owns **no layout authority over its own dividers** (the sharper half); **two orphan glass-ui `<Label>`s (real `<label>` elements) wrap nothing and are `pointer-events-none` while the `<Input>` twelve lines up is NAMELESS** — *the glass-conformance inversion running backwards: a primitive conscripted for a job it does not do*; and 24px targets at the demo's own written floor with a gapless X↔CopyButton pair **that is OCCLUDED OUTRIGHT today** (KF-KE-5), so the touch-target question **goes live only after the un-occlusion** · **cure = the token/rhythm/Label-conformance rows land here; the behavioural half is CARD-UNIT's.** Rendered pixels → KF.W9.
- **KF-EST-3 · -4 · -5(component arm) · -9 · -12 · -13 · -15 · -17 · -18 · -19 · -20 · -21 · -22** ⟨kf-EditorStartScreen⟩ · **MAJ ×2, MIN ×8, INFO ×3** · (EST-3) the deck's public contract is an **icon-sandwich** (`{{ subtitle }} <List/> {{ subtitleSuffix }}` from three separately-defaulted props) with no way to suppress/reorder the glyph; `subtitleSuffix` has zero external consumers; **of the five props across the hero's two children, FOUR have never been passed by any consumer in the tree** — cure = decide reusable-or-inline for the copy contract; (EST-4) `.hero-band` declares `top` + `padding-inline` only — **no bottom/height/max-block-size** — inside `html,body{overflow:hidden}` + `.editor-shell` `overflow-hidden`, so on 720-768px-tall desktop viewports the derived seat + the mega rung's wrapped height exceed the remaining viewport and **the hint CLIPS (never scrolls)** (419.9px into 397.1px at 1280×720, two independent derivations concordant to the decimal) — cure = bound the band (`max-block-size` or a bottom seat); (EST-5) N per-instance perpetual rAF callbacks for a 3-glyph ellipsis at BOTH substrates where the library's group owns one driver — **CURE SEVERED at the bank (multi-target refusal, managed-`play()` throw, no per-child delay/iteration evidence; stagger's own docblock example does not typecheck against `AnimationGroupInput`)**: the component arm is **EVALUATE-NOT-MECHANICAL-SWAP and A GROUP VIABILITY TEST PRECEDES ANY REWRITE — not a browser probe**; the library arm is KF.W5's; (EST-9) `padding-inline: clamp(2rem, 5vw, 4.5rem)` is the seat's **ONE un-derived axis** — a repo-hapax raw-vw clamp misaligned with the work-area card's inline edge by 14-26px at common widths, **sign-flipping ≈2400px, under a comment claiming full derivation**; (EST-12) `vertical-align: baseline` **doesn't omit a fix — it OVERRIDES preflight's `vertical-align: middle`** (unlayered scoped beats `@layer base`), riding the glyph ≈0.145em high, and **the comment misdescribes the mechanism twice**; (EST-13) `6.2cqi`/`5.4cqi` **have no query container** (the sole demo `container-type` is an opt-in utility applied nowhere in this chain; glass-ui dist ships none) → they resolve against the small viewport — **a viewport unit wearing a container unit's name inside the file's own vh ban**, with a latent flip if any ancestor ever gains containment; (EST-15) **the `var()` fallback posture is INVERTED** — fallbacks on the two `:root`-guaranteed demo tokens, **none on eight bare glass-ui reads**, two of which (`--type-display-4`, `--muted-foreground`) are absent from glass-ui's published override manifest; (EST-17) TypingDots' async `onMounted` has **no failure arm** — `loadAnimationEngine()` **memoizes a REJECTED promise** (`??=`) and `main.ts` already swallows the boot failure, so the one visible surface for a failed engine chunk is **an unhandled rejection on the LCP node**; (EST-18) `count` is reactive in the template, **mount-once in the engine wiring** (delays materialized at setup, anims constructed once, no watch in the 88-line script) — the half-reactive contract KF-AT-1 books for the sibling, here in the **engine** dimension; (EST-19) `EditorShell.vue:62`'s `<EditorStartScreen />` default-slot fallback is **unreachable** (App always supplies the slot) **and diverges from the live call by dropping `hint`** — dead code that would silently change the page if it ever fired; (EST-20) the seam/no-op cluster (`p-0` and `w-full` ×2 inert; EditorShell's flex-centering + doubled `pointer-events-none`/`z-controls` inert over an absolutely-positioned child; `stagger(count,…).delays(count)` twice-redundant; `hint: undefined` in withDefaults documents nothing; the 9-line T.D12 tombstone; the two-media-block duplication **constrained by EST-14**); (EST-21) comment-truth cluster (`:165` names ☰ while the tree ships `List`; `index.html:38` names `text-display-4` while the tree ships `text-display-mega`; TypingDots `:25-26` names a `steppedEase` import **the frontier still ships as an orphan — scoping INVERTED, the comment-truth cure is REQUIRED, not a worktree artifact**; the deck→hint "one ladder" is **two mechanisms**, alpha vs token, which is why the measured step is 2.06:1 light / 1.49:1 dark); (EST-22) **composition latencies on public props** — titles ≥66 glyphs put the per-char delay past one cycle (3600/55 = 65.45) → **concurrent ripples vs the documented single sweep, UNCOMPENSATABLE because `cycleMs` is frozen (KF-AT-1)**; and TypingDots pairs `delays[i]` with `els[i]` on an **undocumented `v-for` ref-array order** under an unverified `HTMLElement[]` generic · **cure = one hero pass under the named constraints (EST-14's re-key; EST-5's severed cure; the `:root`-not-`@theme` discrimination).**
- **KF-ES-45** ⟨kf-EasingScene M-4, reader-1's miss, ADMITTED⟩ · **INFO** · the focus ring's **8px soft halo clips against the grid's 2px padding on the leading row/column** — the residue of the sustained D-i7 kill (the hard 2px ring clears **exactly, with zero tolerance against any producer `--focus-ring-width` bump**) · **cure = observation; rides this wave's focus-contract pass.**
- **KF-SST-39 fold + SequenceScene D22 height-floor fold** · **MIN** · carried as folds so the sweeps do not miss them: the StartingStyleTarget family arm rides the Card-family decision; SequenceScene's D22 height mechanism ≡ kf-KeyframeTimeline D-8 / kf-ControlsPaneWrapper D-m12 (the `h-*` overrides height but not `min-block-size` class) · **cure = one min-block-size policy across the demo, decided with D-8.**
- **KeyframeTimeline D-5 + D-8 + D-14** ⟨kf-KeyframeTimeline; D-8's machinery re-verified in the INSTALLED dist, immune to witness-staleness⟩ · **MIN/MIN/MAJ** · **disabled Undo/Redo are PIXEL-IDENTICAL to enabled** — `opacity-50` ≡ `--opacity-disabled: 0.5` (cascade-order-independent because equal) **and NOTHING visual survives**: `pointer-events:none` means the `cursor:not-allowed` cue never paints and `:hover` never engages, while `:7-10`'s own comment calls these "the discoverable affordance… bounded by canUndo/canRedo" — *boundedness real in the DOM, invisible on screen*; `h-7 w-7`/`h-6 w-6` **override `height` but not `min-block-size`** → 28×36 fine / 28×54 coarse pill-radius buttons, and the Input half is harder (`.field-control` carries NO min-block-size and Input is one element with caller classes merged, **so the `h-6` label Input genuinely renders 24px — 33% under the smallest published control — even on coarse**); and **Clear-all and Remove ship undifferentiated** — same size/emphasis/geometry/opacity as Undo/Redo/Expand, **no `tone="destructive"` (published and styled), no confirmation** — while `clear()` empties the array and nulls the engine · **cure = a real disabled treatment; one `min-block-size` policy (with the D22 fold above); `tone="destructive"` on the destructive pair.** Undo mitigates D-14's rung to MAJOR, not the row.

---

## §Gates

Born-RED candidates. Each names a live witness measured at `origin/master` `81a56990` by this seat or carried verbatim from a bank that executed it (L-19: a gate without a real witness is contrivance). **No gate here mints a grep-idiom `proof:*` mark** (value.js house law, owner 2026-06-02). Status at authoring: **ALL RED.**

| id | asserts | born-RED witness (measured / banked-executed) | anti-contrivance bound |
|---|---|---|---|
| **G-W6-1** · phantom-token extinction | every `var(--x)` in `demo/**` resolves to a definition at `origin/master`, or carries a deliberate documented fallback | **RED, executed this seat**: `git grep -- '--dock-margin:' origin/master` → **0 definitions**; `var(--dock-margin)` → **10 occurrences, 9 live + 1 in a `TransportDock.vue:379` prose comment** (`layout.css:80,92,117,135,161,166,204` + `TransportDock.vue:389,395`) — the banked "nine unfallbacked sites" reconciles exactly. Also `--shadow-glass` (1 consumer `App.skeleton.vue:64`, 0 defs) and `--dock-label-padding-inline` (1 consumer `ChromeDock.vue:381`, 0 defs) | fails if a site is "fixed" by adding a per-site fallback (that is the masking KF-APP-6 convicts); fails if the bridge-family spelling `--shadow-glass-*` is used (KF-SKEL-2: it does not emit) |
| **G-W6-2** · KfPillTabs extinction + preservation | zero `KfPillTabs` references under `demo/`; the 7-case keyboard gate runs GREEN against `useTabRovingFocus` | **RED, executed this seat**: `git grep -l KfPillTabs origin/master -- demo/ test/` → **11 files** (`KfPillTabs.vue` 124 L, `KfPillTabs/useKfPillTabs.ts` 93 L, `composables/useKfPillTabs.ts` 4 L, `ChannelControls.vue`, `transport/index.ts`, `SpringPhysicsFacet.vue`, `useSpringDemo.ts`, `font-roles.json`, + 3 tests) | **fails if the test file is DELETED rather than migrated** (Killed #7); fails if `font-roles.json`'s `filing-tab*` selectors survive the `.tab-trigger-*` delete; fails if `ChannelControls.vue:230` is repointed at the `.vue` instead of `../KfPillTabs/useKfPillTabs` |
| **G-W6-3** · shim + dead-barrel deletion | the F-5 shims and importer-less barrels are gone and every consumer is repointed | **RED, executed this seat**: `composables/useKfPillTabs.ts` (**4 L**) and `transport/index.ts` (**14 L**) and `instrument/index.ts` (**27 L**) all present at origin/master; `useRafLoop.ts` (**19 L**) present; `composables/useAnimationGroupPlayback.ts` present with its test consumer | fails if `useAnimationGroupPlayback`'s test is merely repointed rather than **moved to the component seam** (ACG L-1); fails if a barrel delete removes the only guard on the duplicate-mount hazard without naming the replacement (M-L3) |
| **G-W6-4** · phantom utilities | zero references to class names with no rule in the shipped sheet | **RED, banked-executed**: `.btn-interactive` → **8 lines / 6 files**, 0 rules in the 571 KB artifact, 0 in glass-ui (three independent seat censuses agree); `.tap-floor` → **0 adopters** (its 2 grep hits are its own comment + selector) | fails if `.tap-floor` is left undecided — **ADOPT-OR-DELETE, one ruling**; fails if `scripts/proof-phantom-classes.mjs` (named at `LESSONS-LEARNED.md:603`, absent from `scripts/`) is re-minted as a grep gate |
| **G-W6-5** · same-name shadow census | every demo `:root`/utility that shadows a producer name is dispositioned RETAINED-BY-POLICY / PREFIXED / RETIRED | **RED, banked-executed**: `--rainbow-{red…violet}` declared by BOTH glass-ui `scale-paper.css` (oklch) and `design-idioms.css:15-21` (hsl, unlayered-and-later) — **verified present this seat**; `.text-gold` class **and** `--color-gold` alias both shadowed; `--scale-hover` zero-delta shadow; `.focus-ring` shadowed **without** `border-radius: var(--radius-pill)`; `shadow-lg` triply defined; demo `icon-*` shadows producer `--icon-*` by +1 rung | **fails on a blanket rename** — the MISSED-H guard (`design-idioms.css:9-11` states the override policy in its own words; prefixing `--amiga-red` ships a solid-white ball with no error because Canvas2D ignores an invalid `fillStyle`) must be folded FIRST; fails if the disposition table has no third column for KF-CE-22's *absence* of a token surface |
| **G-W6-6** · focus contract | one `.focus-ring` affordance; no `@layer utilities` class cancels `field-control`'s focus paint; every interactive surface has a visible indicator in ordinary AND forced-colors modes | **RED, banked-executed**: KF-KC-4 (`shadow-none`/`border-none`/`focus:*` cancel the components-layer affordance while `outline:none` survives), KC-5 (same on the offset field), KF-CB-12 (no affordance, third dialect), KF-KE-30 (the `.focus-ring` name is ambiguous repo-wide), TimelineCaret C-7 + D·M-5 (`outline-none` + erased `ring-1`), KeyframeTimeline D-20 (UA ring on `role="slider"`), CPD R-6/R-27, KAD-F2/-22 | **the class collision (KF-KE-30) must resolve BEFORE the contract can be asserted**; fails if `field-control` is erased rather than a bare variant requested from the producer (root-styling law); **`aria-selected` is not a permitted vehicle** (invalid off its seven admitted roles) |
| **G-W6-7** · target size | every interactive target meets the ruled floor or carries a mechanism that actually works | **RED, banked-executed**: SP-9's **28px deficit** (coarse `--control-h-md` 60px collapsed to 32 by `h-8`, **same property, no `max()` rescue**), SP-8/SP-11 (32×36 / 24×24), KF-CO-24 (24×24 pencil, 28×28 Back), KF-CB-5/-14 (16px at 2 of 4 sites), KF-KC-33/KC-23 (24px destructive, gapless) | **MUST consume KF-SST-30's correction: `@utility touch-hit-area`'s expander is `pointer-events: none` and CANNOT expand a target — the annotated rows (KF-CO-24, CubeScene MISS-3) count it among their "sanctioned cures" and are wrong**; fails if a row still cites "the demo's own 44px house practice" (there is none — `.tap-floor` has zero adopters) |
| **G-W6-8** · typography-cure collision guard | no `text-mono-small` swap ships without its box resize in the SAME COMMIT; no `normal-case` ships without `tracking-normal` | **RED by construction, arithmetic re-derived at the bank**: THP D-4's cure makes rows **19.6→28px** and cuts the 96px scroller from ~9 rows to **4.9→3.4**, materially worsening D-3 — **and the two rows are adjacent in the same axis document**; MM-29's caps-tracking survival is confirmed at SP-7 and MM-31 | fails if the two edits land in separate commits; **binds KF.W7 identically** (declared both ends) |
| **G-W6-9** · glass-first suffusion ledger | each named producer surface is either CONSUMED or its decline is WRITTEN | **RED, banked-executed — all currently at zero mounts**: `Textarea` (`/forms`, 0), `FadingScroll` (0 while `ChannelControls.vue:51-54` names it), `NumberField` (`/number-field`, 0), `Progress` (0), `Skeleton` (root-barrel only, 0), `Surface`/`PaperBackdrop`/`Card`-family (declined at 4 sites), `./canvas` `useCanvas2D`+`resolveCanvasColor` (0), `ToggleGroup` for the 28 chips (used 30 lines up, declined below), glass `/timeline` (0 — **KF.W7's, not this wave's**) | **EVALUATE-NOT-SWAP is the default verdict shape**; fails on any mechanical swap that ignores the counter-evidence lock (**PlaybackRibbon consumes the real `Slider` and carries BLOCKERs anyway — "swap onto the primitive" is NEVER SUFFICIENT**); fails if KF-ET-10 is written as a scene-side change (**structurally unspellable — the `Chip` strips `role|aria-pressed|data-state`**) |
| **G-W6-10** · prose-with-code truth | every phantom consumer / false invariant name / stale vendor premise / impossible type claim / broken text named in §Rows resolves or dies **in the same commit as its code** | **RED, banked-executed**: `KfPillTabs.vue:2-5` unterminated code span + dangling `:aria-orientation=`; `:4-5` names two non-existent files; `:41-44` asserts an impossible `<script setup>` type re-export; `:45-46`'s contingency is MET by 7.0.0; `SpringSidebar` named in 3 live files after deletion; "THE ONE WRITER" is N writers; `tab-idiom.css`'s own header false; `KeyframeTimeline`'s `transitions.css:23-37` cite cannot resolve in a single-line artifact | **KF-CE-41 law**: a code cure that leaves its comment is an incomplete cure; **the phantom-AUTHORITY (`proof:*`) limb is EXCLUDED — it rides KF.W4's gate**, and this wave mints no new `proof:*` mark |
| **G-W6-11** · canvas/theme colour discipline | no unresolved token stream reaches a canvas `fillStyle`; every canvas re-bakes after the theme flip settles | **RED, banked-executed**: SpringHeatmap D-M3 (unvalidated interpolation, **three possible branches**, producer names the failure in `resolveCanvasColor.d.ts` "→ … NOT black", never imported) + D-M2 (pre-flush `watch(isDark, paint)` reads computed style **before** vueuse's `flush:"post"` class flip; **fatal direction dark→light**; `onFlipSettled` ships on the already-imported subpath); AmigaScene D-4/D-5/D-9 (6 of 7 colours are literals, zero theme reads); CubeScene D-7 (six theme-invariant `:root` face literals) | **ESCALATION ARMED: D-M3 becomes BLOCKER iff the SS-13 probe lands branch (a), the solid-black field**; fails if the row is cured at one site and left at the others (**one canvas-colour-discipline decision, not two**) |
| **G-W6-12** · aurora presence + config coherence | the delivered hero presence equals the number the owner blessed, and the config's dead atoms are decided | **RED, banked-executed**: structural composite ≈**0.19** vs the authored **0.1** bound and above the P-HERO **0.15** the amendment cut below (placeholder never hidden on arm; canvas opaque, probed `alpha = 1`; the route **brightens 0.10→0.19 shortly after first paint**); `interactivity:{light:true}` is a hard **TS2345** and inert even if cured; `initStrategy:'eager'` contradicts the file's own `:8` | **PRECONDITION P-7 must resolve first** (the 6.0.0 version probe: is KF-HA-1 a silent cross-major regression?); the assertable surface is the **rendered `--aurora-opacity-ceiling` on `.aurora-root`** — *the un-writable `HERO_AURORA_OPACITY_CEILING` import form must not enter a spec*; the **gate rebuild is KF.W4's and needs its demo lane to exist first** |
| **G-W6-13** · `--kf-*` namespace audit completion | every one of the demo's unprefixed custom properties carries a disposition | **RED, banked**: census §6.3 — **98 unprefixed demo custom properties, zero `--kf-*`** | **the product is a DISPOSITION TABLE, never a blanket rename** (MISSED-H guard); the audit is **re-cut as a PHANTOM-token audit** — the collision framing is refuted ∅ at KF-APP-26 and survives only where a real two-producer collision is shown (G-W6-5) |
| **G-W6-14** · producer relay dispatch | every GLASS-OWNED row is dispatched to the BH relay and **none** is patched demo-side | **RED, no relay sent**: MM-4 (unlayered `dropdown-menu` sheet, brace-matched), KF-KC-10 (unlayered `.glass-label` scoped rule), KF-SKEL-4 (`components.css` defeats its own `@theme` radius scale — byte-offset proof), the LabeledSelect/LabeledSwitch Boolean cast (`default: undefined`), KF-HA-2 (placeholder-not-hidden-on-arm), KF-HA-12 (aurora omitted from the a11y sweep), KF-SST-30 (`touch-hit-area` `pointer-events:none`), KSM R-1 (registry modality, **carrying the LIFO/FIFO asymmetry**), R-8/R-16/R-17 (keycap names, label narrowing, alias surfacing), KF-ET-10 (a first-class selectable chip group), CC-D-9 (a material opt-out prop), KF-SCR-2 (font-synthesis/italic register) | **a demo-side patch of a producer seam is a defect** (SS-6); fails if the relay omits the **cure-shape constraints** the banks derived (LIFO/FIFO; the getter-not-selector law; `default: undefined`) |

---

## §Bounds

Files this wave owns. All under `/Users/mkbabb/Programming/keyframes.js/`; **line counts are `git show origin/master:<f> | wc -l`, measured this seat.** Access: *modify* · *delete* · *evaluate* (verdict may or may not touch it) · *read-only witness*.

| File | L | Access | Note |
|---|---|---|---|
| `demo/components/instrument/transport/KfPillTabs.vue` | 124 | **delete** | S-1 DELETE head; the broken header text dies with it |
| `demo/components/instrument/transport/KfPillTabs/useKfPillTabs.ts` | 93 | **delete** | after the gate migrates (G-W6-2); preserve the getter-thunk idiom deliberately |
| `demo/components/instrument/transport/composables/useKfPillTabs.ts` | 4 | **delete** | F-5 shim; **repoint `ChannelControls.vue:230` at `../KfPillTabs/useKfPillTabs`, NOT the `.vue`** |
| `demo/components/instrument/transport/index.ts` | 14 | **delete** | dead barrel (referenced only by the other dead barrel) |
| `demo/components/instrument/index.ts` | 27 | **delete** | dead barrel, zero importers |
| `demo/components/instrument/transport/composables/useAnimationGroupPlayback.ts` | — | **delete** | F-5; its ONE consumer is a test (ACG L-5) |
| `demo/components/instrument/transport/composables/useRafLoop.ts` | 19 | **delete-or-document** | KF-AV-27; a shim whose docblock names a different mechanism |
| `demo/components/instrument/transport/composables/useScrollFade.ts` | 129 | modify | delete the dead fade; second consumer at ControlsPaneWrapper survives |
| `demo/components/instrument/transport/channel-controls/composables/useTabStripScroll.ts` | 80 | **delete** | unreachable seam (`tabsHeaderEl` permanently null) |
| `demo/components/instrument/transport/channel-controls/ChannelControls.vue` | 456 | modify | the strip half + the `.kf-pill-tab` scoped skin + prose; **NOT the `:key`/timeline structure (KF.W7)** |
| `demo/styles/tab-idiom.css` | 79 | modify/**delete** | the 48-line `.tab-trigger-*` corpse; the panel-enter animation stays until C-14 is decided |
| `demo/styles/design-idioms.css` | 300 | modify | `--rainbow-*`, `--amiga-red`, `.tap-floor`, `.text-gold`, `--scale-hover`, `.focus-ring`, `icon-*`, `--panel-max-h`, gold token, `.status-badge` precedent, `.progress-bar` |
| `demo/styles/layout.css` | 210 | modify | `--dock-margin` chain (:80,:92,:117,:135,:161,:166,:204), `--start-hero-band`, `--controls-idle-opacity`, the `@supports (anchor-name)` arm, the override-policy block |
| `demo/styles/style.css` | 294 | modify | `--font-display-weight`, `font-synthesis`, `@layer demo-typography`, `.focus-ring`, the z-contract block, `--z-behind` |
| `demo/styles/font-roles.json` | — | modify | `filing-tab*` empty-set selectors; the `.kf-pill-tab` vanity row; the `hero-display` selector contract |
| `demo/styles/playback-idiom.css` | — | modify | `--color-progress` wiring (KF-AV-11) |
| `demo/components/instrument/shell/AnimatedText.vue` | 126 | modify | S-5 slot |
| `demo/components/instrument/shell/TypingDots.vue` | 125 | modify | the `count` contract motion + REST_OPACITY binding |
| `demo/components/instrument/shell/EditorStartScreen.vue` | 191 | modify | hero band, gutter, leading, cqi, fallback posture, comment truth |
| `demo/components/instrument/shell/HeroAurora.vue` | 128 | modify | the one config motion + the KISS reduction |
| `demo/components/instrument/shell/EditorShell.vue` | — | modify | `#anchor` supply; the dead default-slot fallback; `#tabs-content`/`#tabs-trigger` chain deletes |
| `demo/components/instrument/shell/EditorHeader.vue` | — | **evaluate/delete** | banked as a DELETE at kf-App KF-APP-41 (0 consumers); the live escapes (EH-4/-5/-8/-9/-10) are the real cures |
| `demo/components/instrument/shell/SharePopover.vue` | — | modify | SP-1/-4/-5/-6/-7/-8/-9/-10/-11/-18/-19/-20/-21 |
| `demo/components/instrument/shell/KeyboardShortcutsModal.vue` | — | modify | the FadingScroll composition + the R-cluster |
| `demo/components/instrument/shell/index.ts` | — | modify | barrel truth after the deletes (4 of 8 exported today) |
| `demo/app/App.skeleton.vue` | 101 | modify | S-6 partial (delegate to the stage-card register) |
| `demo/app/App.vue` | — | modify-carve | provider policy (KF-APP-21); the `editor-shell/` misdirection comment; **the `provide(TABS_EXTERNALLY_MANAGED_KEY, true)` site at `:176` is READ-ONLY EVIDENCE — its removal belongs to the S-1 delete, not to App** |
| `demo/app/dock/ChromeDock.vue` | — | modify | D-5/D-22/D-23/D-24 + the `--dock-label-padding-inline` repoint; **M-4's cure carries the MbabbMenu rider** |
| `demo/app/dock/MbabbMenu.vue` | — | modify | MM-4 demo-side repair + MM-15 z + MM-30 red + MM-8/-9/-29/-31 |
| `demo/components/CopyButton.vue` | 113 | modify | S-7 reshell |
| `demo/components/instrument/timeline/CSSPasteDialog.vue` | 80 | modify | the S-9 Textarea swap (**shared with KF.W7's fold — see CrossEdges**) |
| `demo/components/instrument/keyframes/components/KeyframesAddDialog.vue` | 161 | modify | the well swap + registry adoption + the dead-token cluster (**KAD-1 is KF.W5's**) |
| `demo/components/instrument/keyframes/KeyframeCard.vue` | — | modify-carve | **focus/token/tap-floor rows ONLY — the behavioural body is CARD-UNIT's** |
| `demo/components/instrument/keyframes/components/KeyframeCardList.vue` | — | modify-carve | KC-5/-12/-21/-23/-26 only |
| `demo/components/instrument/keyframes/KeyframesEditor.vue` | — | modify-carve | KF-KE-21/-23/-30/-31/-36/-39/-40/-42/-45/-53/-59 only; **the six blockers are KFED-UNIT's** |
| `demo/components/instrument/keyframes/KeyframesStringControls.vue` | — | modify-carve | D-20, N-9 only |
| `demo/components/instrument/keyframes/CSSCodeEditor.vue` | — | modify-carve | KF-CE-17/-20/-22/-23/-46 only; **the pipeline is EDITOR-UNIT's** |
| `demo/components/instrument/timeline/KeyframeTimeline.vue` | 312 | modify-carve | **glass/token/contrast/name rows ONLY (D-1/D-5/D-6/D-7/D-8/D-9/D-14/D-18/D-20/M8/M10) — the scrub seam is KF.W7's** |
| `demo/components/instrument/timeline/components/TimelineTrack.vue` | 246 | modify-carve | D-5, D-3, m-17, RR-A missed-5 only |
| `demo/components/instrument/timeline/TimelineCaret.vue` | 70 | modify-carve | D·M-3, D·M-7, C-7, D·M-5, m-2, MISS-α3, MISS-α6, C-4/D·M-9 only |
| `demo/components/instrument/timeline/components/TimelineHoverPreview.vue` | 38 | modify-carve | D-4, L-D7, MISSED-2, D-5, D-11, D-17 only — **under G-W6-8** |
| `demo/components/playback/AnimationVisualizer.vue` | 256 | modify-carve | KF-AV-11's slider-var wiring + token truth only; **the shape verdict is KF.W7's** |
| `demo/components/playback/PlaybackRibbon.vue` | — | modify-carve | the `--color-progress` half of KF-AV-11 + the phantom-utility arm |
| `demo/components/instrument/transport/channel-controls/ChannelOptions.vue` | 609 | modify-carve | **KF-CO-18/-19/-20/-24/-25/-30 ONLY — everything else is OPTIONS-UNIT's** |
| `demo/components/instrument/transport/channel-controls/LayerConfigPanel.vue` | 92 | modify-carve | LP-8's `/number-field` evaluation only |
| `demo/components/instrument/transport/controls-pane/ControlsPaneWrapper.vue` + `.css` | — | modify-carve | D-m3 token + the dead `#tabs-trigger` chain only |
| `demo/components/instrument/transport/controls-pane/RibbonBar.vue` | — | modify-carve | D-3/m-1/C-2, RB-2, RB-5, D-8/C-9 |
| `demo/components/instrument/transport/components/DemoGlobalChrome.vue` | — | modify-carve | M-4 token instance only; the toast verdict is census-owned |
| `demo/components/instrument/transport/TransportDock.vue` | — | modify-carve | `--dock-margin` sites `:389`/`:395` only |
| `demo/scenes/amiga/AmigaScene.vue` + `utils.ts` | — | modify | the stage-surface + canvas-theme packet |
| `demo/scenes/spring/SpringHeatmap.vue` | — | modify | the canvas-colour + theme-flip decision |
| `demo/scenes/spring/SpringPhysicsFacet.vue` · `SpringScene.vue` · `SpringTrace.vue` · `StartingStyleTarget.vue` | — | modify-carve | SPF-6/-10, KF-SS-9 register, prose rows, KF-SST-28/-30 consumer half |
| `demo/scenes/cube/CubeScene.vue` · `CubeTarget.vue`/`.css` · `CubeAxisLines.vue` · `matrix-editor/MatrixEditor.vue` | — | modify-carve | D-7 face palette · #58/#60 · KF-AX-3/-11/-12/-15/-25/-31 · ME-13/ME-18; **the cube scene-repair packet is NO-WAVE-OWNER** |
| `demo/scenes/easing/EasingTarget.vue`/`.css` · `EasingSidebar.vue` · `EasingScene.vue` | — | modify-carve | KF-ET-10/-17/-21, KF-ES-21, KF-ES-45 |
| `demo/scenes/sequence/SequenceScrubber.vue` · `SequenceScene.vue` · `SequenceTarget.css` · `SequencePlayhead.vue` | — | modify-carve | KF-SCR-2, D4/D22 folds, btn-interactive fold, C-3 material register + the F8 annotation |
| `demo/scenes/square/SquareScene.vue`/`.css` · `SquareInstrument.vue` | — | modify-carve | D-23 collision, D-18 |
| `test/demo/instrument/KfPillTabs.test.ts` | — | **modify (migrate)** | the 7-case gate moves onto `useTabRovingFocus`; **never deleted** |
| `test/demo/instrument/useAnimationGroupPlayback.test.ts` | — | modify | repoint + **move to the component seam** (ACG L-1) |
| `demo/DESIGN.md` · `docs/**` in-file cites | — | modify-carve | **prose-with-code only**; the citation GATE is KF.W4's |

**Read-only witnesses (never modified by this wave)**: `node_modules/@mkbabb/glass-ui/**` (dist styles, d.ts, compiled bodies), `node_modules/reka-ui/**`, `scripts/observe/demo/usability.mjs` (**written by the KF.W4∥KF.W6 atomic bundle, not by this wave alone**), the shipped bundle. **Not touched by any seat here**: keyframes.js `src/**` (KF.W5), `demo/components/instrument/timeline/composables/**` + `utils/timelineEngine.ts` (KF.W7), `demo/state/**` behavioural stores, value.js `src/**`, and `scripts/dev/dev.sh` (unowned, either repo, **NEVER touch**).

---

## §CrossEdges (declared from this end)

- **→ KF.W0 · Substrate Settle.** Every anchor in this file re-resolves at `origin/master` before cure (D-19). Carried corrections this seat measured: the `TABS_EXTERNALLY_MANAGED_KEY` provide site is **`App.vue:176`**, not the banked stale-HEAD `:169`; `--dock-margin` has **9 live unfallbacked sites + 1 prose mention**. §B-12 owes: the three-file disk↔master reconciliation (`package.json` devDep · both EE-02 css-twin sites) so a stale-WIP commit cannot clobber a landed cure; the Codex v8-contract disposition; the cascade-level coupling arm beside the import census (KF-SKEL-7). **Census F-1 is FALSIFIED at HEAD and folds there — never re-booked in this wave.**
- **→ KF.W4 · K-Quartet Hygiene (the SEQUENCING HEAD).** (1) **The vue-tsc gate precedes verification of every typed cure here** — `check` at origin/master is still bare `tsc`, and `demo/env.d.ts:3-7` maps `*.vue` → `DefineComponent<{},{},any>`; until it lands, KF-HA-1's TS2345, KF-AT-1's freeze, and the `extraTabs` type riders are authorable but unverifiable. (2) **ATOMIC BUNDLE — KF-AT-4 ∥ KF-AT-3: the gate's counter cure and the component's counter cure land TOGETHER; fixing either alone re-creates the by-construction pass or manufactures a false RED.** (3) **ATOMIC BUNDLE — KF-AT-10: gate migration is spec'd WITH the component migration, ONE landing order; W4 authors both halves and hands this wave the order, not the file.** (4) The citation census's in-file prose cures ride this wave's file-touch, **with the deletion commit's "direct product checks" posture read first**; the phantom-AUTHORITY `proof:*` limb stays at W4's G-KFW4-8. (5) KF-HA-4's rebuilt mount-and-read oracle needs W4's demo lane to exist first. (6) `scripts/observe/demo/usability.mjs` is read-only here and written by the bundle — **the two waves never write it concurrently.**
- **→ KF.W5 · Tri-Fold Library Audit.** **KF-AT-8 is a NAMED BLOCKER-OF-THE-MIGRATION: the `role="img"` implicit-role override must be cured before any `<h1>` adoption of `splitText`.** Also owed before the S-5 slot can land: KF-AT-9 (mutate-before-refuse) and KF-AT-23 (`revert()` deletes a pre-existing author `aria-label`). KAD-1's `textContent` sink fix is **FRONT-LOADED at W5** (it blocks nothing and kills a BLOCKER plus its data-corruption twin). KF-EST-5's group-viability question, KF-EST-17's memoized-rejection design, KF-TD-1/-5, and SpringTrace C-3's export ask are W5's.
- **→ KF.W7 · Timeline Evaluate (bidirectional; five hard bindings).** (1) **G-W6-8 / MISSED-3 binds BOTH waves** — no `text-mono-small` swap without the box resize in the SAME commit; `normal-case` always pairs `tracking-normal`. (2) **N-2's wire-or-delete is ruled at W7 BEFORE this wave spends D-9's typography MAJOR on an inert control.** (3) **TimelineCaret C-7's `.focus-ring` one-class cure INVERTS the primitive-swap order — D·M-5 discharges here without waiting on the NumberField evaluation.** (4) **L-9/m-8 (TimelineCaret) discharges with the NumberField swap if it lands here** — declared both ends. (5) **KAD-6 stands alone here if W7's R-7 twin fold is declined** — declared both ends. Plus: **the S-9 Textarea swap's discharge list (R-1/R-6/R-8/R-9/R-12/R-14/R-20) must be STATED before W7 spends a cure on any dialog-residue row**; and **kf-KeyframeTimeline D-7's SS-13 residue row is the single contrast-measurement site — TimelineTrack D-5 rides it.**
- **→ KF.W7's OPEN PRECONDITION, not ours to cure.** kf-ChannelControls **L-2/C-2** (`:key="storedControls.selectedControl"` remounting `KeyframeTimeline` and silently destroying every authored keyframe and the undo trail) and kf-AnimationControlsGroup **D-1** (one singleton sink, N Teleport sources) are **BLOCKERs that must settle before the timeline cluster is touched** — *"any timeline-cluster swap that inherits the `:key` inherits the data loss."* This wave touches **no** timeline structure; it declares the ordering so no seat mistakes a token cure for permission.
- **→ KF.W8 · Structure & Colocation.** KF-AT-26(c) / U.B5's move-to-`app/` carry (and `App.vue:139-141`'s citation of an `editor-shell/` directory that does not exist — an **active misdirection** the move-wave corrects in the same edit); the import-granularity sweep's structural half; the barrel deletes coordinate with the colocation settle.
- **→ KF.W9 · Safari Visual Audit / SS-13.** Every rendered magnitude in §Rows: aurora presence + the 0.10→0.19 brightening ramp; the dock offset at 390×844; composited contrast for the gold pencil, chevron, pane icons, scrub rail, zoom bar, metadata block, axis lines, face numerals, caret selection; the SP-4/R-9 iOS zoom (hardware-only); doubled-wash and specular magnitudes; the SpringHeatmap three-branch probe (**which arms G-W6-11's BLOCKER escalation**); the KSM destruction-path witness (**which arms R-1's**); the CPD focus-landing witness (**which arms R-1/R-27's**); PRM-mechanism unification.
- **→ KF.W10 · Fold Discharge.** KF-AT-21's doc-truth re-open (`r-animation-sota.md:109`/`:253`); KF-HA-4's `stage-manifests/home.json:9`; the census S-9/S-10 extensions terminalized; the `.tap-floor` adopt-or-delete ruling recorded; the `/command` decision record filed so the census stops re-raising it.
- **→ glass-ui BH relay (SS-6, standing owner edict — a fond).** One communiqué carrying, with their cure-shape constraints intact: the unlayered `dropdown-menu`/`_shared/menu.css` emission (MM-4) and the unlayered scoped `.glass-label` (KF-KC-10); `components.css` defeating its own `@theme` radius scale (KF-SKEL-4, with the byte-offset proof); the LabeledSelect/LabeledSwitch Boolean cast (`default: undefined` — **every passive consumer is broken**); the aurora placeholder-not-hidden-on-arm (KF-HA-2) and the aurora's omission from the a11y sweep (KF-HA-12); `touch-hit-area`'s `pointer-events:none` expander (KF-SST-30 — **one utility, two behaviours, the general-purpose name on the decorative one**); the keyboard registry's missing modality/suspend affordance **with the LIFO/FIFO asymmetry stated so the API is designed against it** (KSM R-1), plus the keycap accessible-name companion (R-8), the label type-narrowing (R-16) and alias surfacing (R-17); a first-class selectable chip-group shape (KF-ET-10); a material opt-out prop for nested wash rungs (CC-D-9); the collapsed-at-rest toolbar whose empty-anchor state is unreachable-by-construction (KF-APP-5); the font-synthesis/italic register (KF-SCR-2); the `wake()` PRM seam and the diagnostics→throw bridge posture (KF-HA-9/-13). **A demo-side patch of any of these is a defect.**
- **→ NO-WAVE-OWNER packets this wave must NOT absorb** (declared so no seat mistakes a token cure for a behavioural one): **OPTIONS-UNIT** (kf-ChannelOptions' capability/persist/steps/a11y cluster — this wave takes only KF-CO-18/-19/-20/-24/-25/-30), **CARD-UNIT** (kf-KeyframeCard/CardList's offset loop), **EDITOR-UNIT** (kf-CSSCodeEditor's pipeline), **KFED-UNIT** (kf-KeyframesEditor's six blockers), **APPLY-UNIT** (kf-KeyframesStringControls' apply/name/feedback family), **AXISLINE-UNIT**, the **cube scene-repair packet** (kf-CubeTarget/MatrixEditor spine), the **dock-menu packet** (kf-MbabbMenu), the **spring/square/sequence scene packets**, and the **keyboard-scope row** (N-KPT-1 + ACG M-1/M-3/M-4). This wave supplies their token/register/glass inputs and takes none of their behaviour.



