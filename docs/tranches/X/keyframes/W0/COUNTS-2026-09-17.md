SERVED MODEL: claude-opus-5[1m]

# KF.W0.d — COUNTS, COLUMNS, AND FRESH SHADOW IDS — 2026-09-17

**Unit**: `KF.W0.d` (Track B · X·KF). **Sub-gate**: **G-0.6**.
**Charge**: spec `docs/tranches/X/keyframes/waves/KF-W0.md` §Agent Units `:675` · §Gates G-0.6
`:589-605` · §Scope 5, 6, 7 (`:309`–`:311`) · §Carry C-8 `:368` · C-9 `:369` · C-10 `:370` ·
C-11 `:371` · C-17 `:377` · **C-17.R `:457-482`** · §Sequencing KF.W5 `:691` + KF.W10 `:695` ·
§Excluded X-4 / SCH-7 / SCH-2 (`:707`–`:709`) · `OP-4` ⟨§Carry `:455`⟩.
**Substrate of record**: `/Users/mkbabb/Programming/keyframes.js`, **SETTLED** by OP-1 —
⟨`date "+%Y-%m-%d"`⟩ → **2026-09-17**; ⟨`git rev-parse HEAD`⟩ →
**`81a56990736ced5b5edde0b84c527680ac7689b1`** = ⟨`git rev-parse origin/master`⟩; branch `master`;
⟨`git status --short | wc -l`⟩ → **6**. Every figure below is read at `origin/master 81a56990`
unless a different ref is named **in the same line**, and every published figure was **double-run**
(both runs agreed on every count). Artefact of record, command-for-command:
`docs/tranches/X/keyframes/artefacts/W0/counts-2026-09-17.txt` (**412** lines, self-counted at the
settled bytes).

**This unit moved no byte in `keyframes.js`.** Every probe is `git` / `grep` / `sed` / `wc` /
`node -p`, read-only; the producer tree and `node_modules/@mkbabb/glass-ui/**` were read and hashed
only. No install, no build, no `depcruise`, no dev server.

---

## §1 — SCH-1..SCH-7 + X-2, each with its command, its literal output, and its date

All run 2026-09-17 at `origin/master 81a56990`. **A figure restated without its command is
indistinguishable from the stale one it replaces** (G-0.6's falsifier) — so the command is the row.

| row       | census claim (2026-08-03)                                              | command, run 2026-09-17                                                                                                        | literal output                                                                                                                                                                                      | verdict                                                                                                                                                                                                                                                                                                             |
| --------- | ---------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------ | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **SCH-1** | FE F-1: `@mkbabb/glass-ui` phantom — absent from `package.json` + lock | `git show origin/master:package.json \| grep -n 'glass-ui'` · `git show origin/master:package-lock.json \| grep -c 'glass-ui'` | `77:        "@mkbabb/glass-ui": "7.0.0",` · `3`                                                                                                                                                     | **FALSIFIED at the frontier, and now CURED ON DISK.** The declaration is an EXACT devDep at `:77` and the lock carries 3 entries. The count-claim is this row's; **the RULING on the worktree deletion and on `legacy-peer-deps` is `KF.W0.b`'s (G-0.2)** and is not restated here.                                 |
| **SCH-2** | KfPillTabs 217-line fork live, 4 files                                 | `git ls-tree -r --name-only origin/master \| grep -i pilltabs` + three `wc -l`                                                 | `KfPillTabs.vue` **124** · `KfPillTabs/useKfPillTabs.ts` **93** · `composables/useKfPillTabs.ts` **4** (the shim) · `test/demo/instrument/KfPillTabs.test.ts`                                       | **CONFIRMED at the frontier — RE-VERIFIED, NOT RE-BOOKED** (§Excluded `:709`). `124 + 93 = 217`, the banked figure reproduced by its operands and not inherited. Cure stays KF.W6's top suffusion target.                                                                                                           |
| **SCH-3** | 145 src `.ts`, 22 636 LOC                                              | `git ls-tree -r --name-only origin/master src/ \| grep -c '\.ts$'` · same list piped through `git show \| wc -l`               | **153** · **22 778**                                                                                                                                                                                | **DRIFTED.** +8 files / +142 LOC against the LIB roster. The LOC basis is stated with the count so it cannot become a fifth basis: **`git show` of every tracked `.ts` file under `src/` at `origin/master`, newline count.** **X-4 (80 / 139 / 153 / "159") is NOT reconciled here — KF.W5's** (§Excluded `:707`). |
| **SCH-4** | 61 value.js `from`-lines (`/value` 15) vs DOCS's 62 (16)               | **MEASURE-AT-OPEN — see §2**, where the predicate is stated before the readings                                                | **62 / 16** at the frontier under the stated predicate                                                                                                                                              | **DOCS's 62/16 REPRODUCES EXACTLY; LIB's 61/15 reproduces at no committed coordinate.** The whole delta is one `/value` line. §2 carries the predicate, the three-coordinate replay and the per-subpath split.                                                                                                      |
| **SCH-5** | 4+ Tier-A anchors dead                                                 | 10 `git cat-file -e` existence probes + one `git grep -n` per delegated call                                                   | 4 banked paths **ABSENT**, all 13 calls **re-anchored** (table, §3)                                                                                                                                 | **RE-ANCHORED, not merely restated.** Four files moved under the W5 carves; **every one of the 13 Tier-A sites resolves at the frontier**, 9 of 13 at their banked line number.                                                                                                                                     |
| **SCH-6** | 58 `.vue`                                                              | `git ls-tree -r --name-only origin/master demo/ \| grep -c '\.vue$'`                                                           | **58**                                                                                                                                                                                              | **HOLDS** (contents drifted; per-file claims still need re-verification at their own sites).                                                                                                                                                                                                                        |
| **SCH-7** | LIB L-1/L-2 depcruise defects, unverified at HEAD                      | four read-only probes; **`depcruise` NOT run**                                                                                 | allowlist names alive at `.dependency-cruiser.cjs:58-61`; the four real modules live under `solver/` + `css/`; `.dependency-cruiser-known-violations.json` **ABSENT**; `knownViolations` **0 hits** | **RESTATED UNVERIFIED-AT-HEAD, as ruled** (§Excluded `:708`). Both defects' _witnesses_ reproduce statically at the frontier; **the re-verification is KF.W5's input** and a green `depcruise` run here would be exactly the false refutation the row warns against.                                                |
| **X-2**   | 184 demo `.ts`/`.vue` (Codex **and** census agreed)                    | `git ls-tree -r --name-only origin/master demo/ \| grep -cE '\.(ts\|vue)$'`                                                    | **185**                                                                                                                                                                                             | **185 ADOPTED** — as ruled at COHESION §0j.C **KF-OGKF1**. The exact 184/184 agreement is the proof both censused the same stale worktree; the divergence is the tell, not the noise.                                                                                                                               |

### §1.1 — SCH-5, the Tier-A re-anchor table (13 sites, at `origin/master`)

Banked at `lane-library.md` §4.1. Each row re-resolved by `git grep -n <call> origin/master -- src`.

| #   | banked site (2026-08-03)                                      | site at `origin/master 81a56990`                                                                                                           | motion                                                 |
| --- | ------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------ | ------------------------------------------------------ |
| A1  | `compile/adapter.ts:222` `parseStylesheet(source)`            | `compile/adapter.ts:222`                                                                                                                   | **none**                                               |
| A2  | `compile/adapter.ts:266` `resolveKeyframes`                   | `compile/adapter.ts:266` (`export const resolveKeyframes = (`)                                                                             | **none**                                               |
| A3  | `compile/value-ast.ts:71` `parseCssValues`                    | **`compile/value/compile.ts:32`**                                                                                                          | file **carved into a module**; line −39                |
| A4  | `compile/selector.ts:24` `parseKeyframeSelector`              | `compile/selector.ts:23` (decl) / `:24` (the aliased `parseValueSelector` call); the one in-tree caller is `compile/frame/compiler.ts:146` | line −1 at the decl; **the caller is new information** |
| A5  | `compile/easing/easing-registry.ts:131` `parseTimingFunction` | **`compile/easing/registry.ts:131`**                                                                                                       | file renamed; **line identical**                       |
| A6  | `compile/emit/format-options.ts:104` `parseTimingFunction`    | **`compile/emit/format/options.ts:104`**                                                                                                   | file **into a sub-module**; line identical             |
| A7  | `engine/options.ts:31` `parseCssScalar`                       | `engine/options.ts:31`                                                                                                                     | **none**                                               |
| A8  | `resolve/browser.ts:165` `parseCssScalar`                     | `resolve/browser.ts:162`                                                                                                                   | line **−3**                                            |
| A9  | `scroll/grammar.ts:109` `parseStylesheet`                     | `scroll/grammar.ts:109`                                                                                                                    | **none**                                               |
| A10 | `scroll/grammar.ts:77` `parseAnimationTimeline`               | `scroll/grammar.ts:77`                                                                                                                     | **none**                                               |
| A11 | `scroll/grammar.ts:85` `parseAnimationRange`                  | `scroll/grammar.ts:85`                                                                                                                     | **none**                                               |
| A12 | `validate.ts:182` `parseStylesheet`                           | `validate.ts:182`                                                                                                                          | **none**                                               |
| A13 | `engine/css/css-animation.ts:176` `resolveKeyframes`          | **`engine/css/animation.ts:176`**                                                                                                          | file renamed; **line identical**                       |

**SCH-5's own four named dead anchors, dispositioned**: `value-ast.ts:71` → **`compile/value/compile.ts:32`** (A3) · `easing-registry.ts` → **`compile/easing/registry.ts`** (A5) · `emit/backward/backward.ts` → **PRESENT at the frontier** (it is the _frontier_ spelling; the flat HEAD triad is the dead one, removed by OP-1's reset and stated by path in `SUBSTRATE-SETTLE-2026-09-17.md`) · `emit/easing-serialize.ts:71-73` → **PRESENT**, and the three lines are the `timingFunctionEntries.find(([_name, func]) => func === easing.fn)` reverse-map, i.e. the K1 easing-reference site, unmoved.

**The 13-site inventory survives whole.** The census's _"4+ cited paths dead at HEAD"_ is true of the **paths** and false of the **seam**: no Tier-A site was deleted, merged or duplicated — four moved. That distinction is the one a parser-consumption wave needs, and it is why this row is a re-anchor and not a deletion.

---

## §2 — SCH-4, the MEASURE-AT-OPEN row: **the predicate first, then the readings**

The baseline (record §Baseline, seat 0) banked SCH-4 as _"does not reproduce at any spelling this
seat can state"_ over three readings (**148 / 30** at `-- src demo test scripts`, **207** repo-wide).
**Those three readings reproduce exactly at this seat** — and they are the wrong scope for the row.
**SCH-4 is a LIBRARY-lane row, and the library lane censused `src/`.** Its own command is banked, and
this seat inherited nothing: it read the predicate off the lane file and re-ran it.

**THE PREDICATE, stated so it reproduces** ⟨`lane-library.md:26`, verbatim: _"`grep -rn 'from
"@mkbabb/value.js' src/ | wc -l` → 61; `grep -rln … | wc -l` → 39"_⟩ — re-expressed as a
**ref-resolved** probe, because a `grep -r` over a working tree names no coordinate and that is the
SCH-3 drift class this wave exists to kill:

> **`git grep -h 'from "@mkbabb/value.js' <ref> -- src | wc -l`** — value.js `from`-clause lines,
> **scope `src/` ONLY**, at a **named ref**; and the same with `…/value` for the subpath arm.

**THE READINGS, all pasted, none subtracted:**

```
$ git grep -h 'from "@mkbabb/value.js'       origin/master -- src | wc -l   →  62
$ git grep -h 'from "@mkbabb/value.js/value' origin/master -- src | wc -l   →  16
$ git grep -l 'from "@mkbabb/value.js'       origin/master -- src | wc -l   →  40   (modules; LIB said 39)

the SAME predicate at the other two committed coordinates:
$ … 8281638c                     -- src | wc -l   →  81      /value →  0
$ … kf-sacred-snapshot-2026-09-17 -- src | wc -l   →  48      /value → 10

the three WIDER scopes seat 0 measured, reproduced exactly:
$ … origin/master -- src demo test scripts | wc -l → 148      /value → 30
$ … origin/master -- .                     | wc -l → 207
```

**The per-subpath split at the frontier, `src/` scope** — the shape of LIB's own table, so the delta
is visible per row rather than as a total:

```
$ git grep -ho 'from "@mkbabb/value\.js/[a-z]*' origin/master -- src | sed 's/.*value\.js//' | sort | uniq -c | sort -rn
  29 /css      16 /value      7 /color      5 /math      3 /easing      2 /transform     (= 62)
LIB's banked table (lane-library.md:25):
  29 /css      15 /value      7 /color      5 /math      3 /easing      2 /transform     (= 61)
```

**Verdict, stated at the altitude the evidence supports.**

1. **DOCS's `62 (/value 16)` reproduces BYTE-FOR-BYTE at `origin/master`, `src/` scope.** The census
   row's own verdict — _"LIB import census off-by-one at HEAD"_ — is **SUSTAINED**, and the
   off-by-one is now **located**: it is entirely inside `/value` (16 vs 15); every other subpath
   agrees exactly. An off-by-one whose limb is named is a different object from an off-by-one
   reported as a total.
2. **LIB's `61 (/value 15)` reproduces at NO committed coordinate** — not at the frontier (62/16),
   not at the stale pin `8281638c` (81/0), not at the snapshot of the sacred checkout's tracked dirt
   `kf-sacred-snapshot-2026-09-17` (48/10). LIB read a **working tree** (dirty, untracked files
   included, `grep -r`) that no ref preserves; the closest committed approximation of that tree is
   the snapshot, and it reads 48/10. **This is the tree-motion law's own case** ⟨C-2, DISSENT 3:
   _"any future seat re-running a corpus grep on this repo must date its run"_⟩ — LIB's run is
   undated and unrefed, so its figure is unreproducible **by construction**, not by drift.
3. **The `8281638c` reading is the sharp one and is stated rather than buried**: `/value` = **0** at
   the pin. The `/value` subpath spelling did not exist in `src/` at the stale pin at all; 16 of the
   62 frontier lines are consequences of V's own execution. A lane that censused the pin and a lane
   that censused the frontier were not counting the same object, which is §0's schism restated in
   one subpath.
4. **The seat-0 baseline's sentence is corrected, beside it, not rewritten** (E-3): _"Neither 61/15
   nor 62/16 is reachable from any of the three"_ is true **of the three scopes seat 0 stated** and
   false of the row's own scope. The correction is this section; the baseline's readings stand as
   dated evidence and are reproduced above.

**SCH-4 is therefore DISCHARGED as MEASURE-AT-OPEN**: a predicate is stated, both readings are
pasted, and the figure that goes forward is **62 / 16 at `origin/master`, `src/` scope, 2026-09-17**.
`40` modules supersedes LIB's `39` under the same predicate. **No basis is reconciled here** — the
counting-basis question is X-4's and X-4 is **KF.W5's** (§Excluded `:707`).

---

## §3 — `OP-4`: the `parseAnimationCSS` LOCUS probe. **MEASURED. NOT DECIDED.**

Commissioned at §Carry `:455` from two sibling ends (`KF-W5` §Open Questions, the
`parseAnimationCSS` LOCUS row; `KF-W8` §Carry row `B-16`, BOOKED CONDITIONAL). **The act, verbatim as
commissioned**, run 2026-09-17:

```
$ git grep -n 'parseAnimationCSS' 81a56990 -- src demo
81a56990:demo/components/instrument/keyframes/composables/useKeyframeOps.ts:7:import { parseAnimationCSS } from "../utils/parseAnimationCSS";
81a56990:demo/components/instrument/keyframes/composables/useKeyframeOps.ts:64:        const { options, keyframes } = await parseAnimationCSS(keyframesString);
81a56990:demo/components/instrument/keyframes/composables/useKeyframeOps.ts:116:                        await parseAnimationCSS(wrapped);
81a56990:demo/components/instrument/keyframes/composables/useKeyframeOps.ts:162:                    await parseAnimationCSS(keyframesString);
81a56990:demo/components/instrument/keyframes/utils/parseAnimationCSS.ts:26:export const parseAnimationCSS = async (input: string) => {
81a56990:demo/components/instrument/timeline/utils/timelineEngine.ts:11:import { parseAnimationCSS } from "../../keyframes/utils/parseAnimationCSS";
81a56990:demo/components/instrument/timeline/utils/timelineEngine.ts:79:    const { keyframes: parsed } = await parseAnimationCSS(css);
```

**The reading, confined to arithmetic over the output above**: 7 hits · 3 files · 1 declaration
(`demo/components/instrument/keyframes/utils/parseAnimationCSS.ts:26`) · 2 importing consumers · 4
call sites · **0 hits under `src`**.

**This seat states no home and arms nothing.** Both dispositions are pre-declared at the commissioning
end and the arming is **`KF-W8`'s reading of this output**, never this seat's — as the spec's own
words bind: _"this wave RUNS the probe and PRINTS the declaration sites; it does not decide the home"_
⟨§Carry `:455`⟩; _"KF.W8's `B-16` arms on the measurement, not on this seat's reading"_ ⟨§Agent Units
`:675`⟩. Recorded beside it, because it is the reason the row exists (PASS-6 D-1): §Sequencing's
engine-lane row `:699` asserted the probe's **answer** — _"Engine work, not substrate work"_ — and
that row **routes the CURE and does not resolve the LOCUS**; it is left standing, untouched, with the
output above now beside it.

---

## §4 — C-9: the CASCADE-COUPLED column (the measurement; the column lands at its two homes)

**Hard order honoured** (§Sequencing `:681`): this column was **measured and landed BEFORE** the F-1
row disposition was written into `lane-frontend.md`, so the F-1 remediation scopes its blast radius
against a surface that counts cascade coupling — and against **C-1.F's full thirteen**, never the six
record-level riders alone.

**The import census, re-run at `origin/master` 2026-09-17** (the census of record was 2026-08-03):

```
$ git grep -h '@mkbabb/glass-ui' origin/master -- demo       | wc -l →  83   (census: 82)
$ git grep -l '@mkbabb/glass-ui' origin/master -- demo       | wc -l →  43   (census: 42)
$ git grep -l '@mkbabb/glass-ui' origin/master -- 'demo/**/*.vue' | wc -l →  37   (census: 37, holds)
58 .vue total − 37 = 21 .vue with NO glass-ui import        (census: 21, holds)
```

**The column.** An import-graph census scores those **21** files CLEAN. Measured against the vendor's
own token/utility surface, they are not:

> **PREDICATE, stated so it reproduces**: a zero-import `.vue` is **cascade-coupled** when its bytes
> at `origin/master` contain at least one of the eight vendor-resolved spellings C-9 names —
> `text-mono-caption` · `text-caption` · `text-muted-foreground` · `border-border` · `--radius-pill`
> · `z-controls` · `--muted-foreground` · `--border` — **each of which is defined ZERO times anywhere
> under the demo tree and is resolvable only through the installed
> `node_modules/@mkbabb/glass-ui`.**

| zero-import `.vue`                                                   | cascade spellings it resolves through the vendor                                 |
| -------------------------------------------------------------------- | -------------------------------------------------------------------------------- |
| `components/instrument/shell/EditorStartScreen.vue`                  | `--muted-foreground` · `z-controls`                                              |
| `components/instrument/timeline/TimelineCaret.vue`                   | `border-border` · `text-muted-foreground`                                        |
| `components/instrument/timeline/components/TimelineHoverPreview.vue` | `border-border` · `text-mono-caption` · `text-muted-foreground`                  |
| `components/instrument/transport/KfPillTabs.vue`                     | `--muted-foreground`                                                             |
| `scenes/amiga/AmigaScene.vue`                                        | `--border`                                                                       |
| `scenes/sequence/SequenceAxis.vue`                                   | `text-mono-caption` · `text-muted-foreground`                                    |
| `scenes/sequence/SequencePlayhead.vue`                               | `--radius-pill`                                                                  |
| `scenes/sequence/SequenceScrubber.vue`                               | `border-border` · `text-caption` · `text-mono-caption` · `text-muted-foreground` |
| `scenes/spring/SpringTrace.vue`                                      | `--border` · `text-mono-caption` · `text-muted-foreground`                       |
| `scenes/square/SquareInstrument.vue`                                 | `--border` · `text-caption` · `text-mono-caption` · `text-muted-foreground`      |

**10 of the 21 are cascade-coupled under the stated predicate; the genuinely glass-uncoupled set is
10, not 21** — the other 11 minus the one named below. **BOTH readings, because the predicate is a
choice and a choice unstated is a choice unreproducible**: widening the predicate by
`text-foreground` — vendor-bridged exactly like the rest (⟨`bridges.css`⟩ `--color-foreground:
var(--foreground)`) — yields **11**, the added file being `components/CopyButton.vue`. **The roster
is a FLOOR and is used as one**; it asserts no completeness in its own voice: it is what the stated
eight-token predicate returns at `origin/master` on 2026-09-17, and a wider token set returns more.

**The four definers, each proved vendor-only at the bytes** (demo side ⟨`git grep -c '<tok>:'
origin/master -- demo`⟩ → **0** for every one):

| spelling                          | sole definer (installed 7.0.0)                     | form                                                                                                                                                                                                                                                                               |
| --------------------------------- | -------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `text-mono-caption`               | `dist/styles/typography/utilities.css`             | `@utility text-mono-caption { font-family: var(--font-mono); … }` — a **utility inside `node_modules`**                                                                                                                                                                            |
| `--radius-pill`                   | `dist/styles/theme/radius.css`                     | `@theme { … --radius-pill: 9999px; … }`; the demo has **5 `var(--radius-pill)` USE sites and 0 definitions**                                                                                                                                                                       |
| `--muted-foreground` · `--border` | `dist/styles/theme/bridges.css` + `components.css` | vendor-only; the demo consumes them through Tailwind's `text-muted-foreground` / `border-border`                                                                                                                                                                                   |
| `z-controls`                      | `dist/styles/theme/bridges.css`                    | `@theme inline { … --z-index-controls: var(--z-controls); … }` — the utility is **glass-GENERATED**; `--z-controls` itself is defined at `dist/styles/tokens/scheme-motion.css`. The demo's only `--z-controls` occurrences are **prose** (`styles/style.css:30`, `DESIGN.md:101`) |

**The reachability chain, by path, so the coupling is checkable and not asserted**:
`demo/styles/style.css:3` `@import "@mkbabb/glass-ui/styles"` → package `exports["./styles"]` =
`./dist/styles/index.css` → `@import "./accessibility.css"` → `utilities/a11y-overrides.css`, and in
parallel `index.css` → `./theme.css` / `./typography.css` for the token and utility definers above.
**Delete the declaration and the import survives; delete the `@import` and ten "clean" components
lose their type scale, their borders, their radii and their stacking order with no build error.**
That is C-9's whole content, now measured: _an import-graph census scores every one of these CLEAN
and is wrong._

**Where the column lands** (§Bounds): the **legend column** is appended to
`CENSUS-2026-08-03.md` (its `modify-append` row names it); the per-file roster and the corrected
"21 clean" arithmetic are appended to `lane-frontend.md` §3. The **token-namespace probe half is
KF.W6's** ⟨C-22 (iii), KF-SKEL-7⟩ and is not performed here.

---

## §5 — C-17: THE MINT. One motion, the whole C-17.R roster, fifteen rows.

**The mint is the only census-slot authority** (R-5 / §Scope 6). What follows was written in **ONE
motion** over **the whole of C-17.R as it stands at the spec's post-round-4 bytes** — fourteen live
rows plus the fifteenth retained as an enumerated strike — and landed in the S-block's home,
`lane-frontend.md` §5. **A mint run over a short roster re-collides on its first motion**, so the
roster was re-read at the spec bytes before the first id was assigned, never reconstructed from
memory or from a prior pass.

### §5.1 — the id-space rule this mint assigns under, stated before the assignment

Measured at this seat over the 58 adjudicated records (`grep -o 'S-<n>' kf-*.md`):

```
S-9  raw=127 recs=31   S-10 raw=40  recs=15   S-11 raw=19 recs=7    S-12 raw=17 recs=4
S-13 raw=782 recs=58   S-14 raw=6   recs=3    S-15 raw=5  recs=3    S-16 raw=18 recs=5
S-17 raw=8   recs=4    S-18 raw=13  recs=5    S-19 raw=6  recs=3    S-20 raw=48 recs=30
S-21 raw=6   recs=3    S-22 raw=15  recs=5
```

**Not one bare `S-n` spelling is free as a substring** — `S-13` reaches all 58 records because
`SS-13` contains it; `S-9`/`S-20` ride `KF-…-9` / `KF-ES-20`; `S-10` rides `KF-ES-10`, `MISS-10`,
`KF-SS-10`. **So "fresh non-colliding" cannot mean substring-free, and no numbering could make it so.**
It means what this file's own law makes it mean — **record-qualification** (C-1.G: _"a bare `K-1`
belonging to kf-SpringTarget does not book kf-MbabbMenu's `K-1`"_) — applied to the census:

> **A census slot's full spelling is `lane-frontend.md §5 S-n`.** Inside that block the namespace is
> exactly `S-1..S-8` today ⟨`grep -c '^### S-' lane-frontend.md` → **8**; `grep -oE '\bS-[0-9]+\b'`
> over the whole file → `S-1 … S-8`, nothing else; the same probe over `CENSUS-2026-08-03.md` → the
> same eight⟩, so **S-9..S-20 are free in the only namespace where a slot id resolves**. A bare
> `S-n` anywhere else is a **claim token**, another record's **local roster index**, or a **substring
> of a different id** — never a slot.

The mint therefore assigns **S-9 … S-20 contiguously** (extending the block rather than leaving holes
a later seat must explain) and publishes the **claim-token crosswalk** at §5.3, so that every banked
`S-9`/`S-10` _claim_ resolves to exactly one _slot_, which is the property whose absence manufactured
the collision in the first place.

**The block's instrument after the mint, read at the settled bytes rather than predicted.** The
twelve minted rows land as `^#### S-n` headings, subordinate to the appended note's section that
authorises them; promoting them to `###` would break them out of that section. So in
`lane-frontend.md`: `grep -c '^### S-'` → **8, unchanged** (the wave spec's quoted instrument keeps
measuring the original eight and is left true rather than silently redefined) · `grep -c '^#### S-'`
→ **12** · **`grep -cE '^#{3,4} S-[0-9]'` → 20**, which is the form a closure sweep over the whole
S-block uses. _(This paragraph replaced a predicted "8 → 20" reading of the bare `^### S-`
instrument, which did not reproduce once the bytes settled — write-then-measure, caught and corrected
inside this unit before publication rather than banked as a figure.)_

### §5.2 — the twelve MINTED slots (each citing its originating record)

| minted slot | claimed as | originating record ⟨banked id : line⟩                                                                                                                                           | subject                                                                                                                                                                                                                                                                                                                      | what the slot does NOT decide                                                                                                                                                                                                                                                                                                                                                                           |
| ----------- | ---------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **S-9**     | S-9        | `kf-SpringHeatmap` ⟨**C-M-1** : `:46`, `:128`⟩                                                                                                                                  | glass `./canvas` shadowed (present at installed 7.0.0; 73 subpath exports)                                                                                                                                                                                                                                                   | the swap/keep evaluation                                                                                                                                                                                                                                                                                                                                                                                |
| **S-10**    | S-9        | `kf-EditorHeader` ⟨**F1 ≡ D-1 / L-B1 / C-1 → kf-App KF-APP-41** : `:33`; the S-9 ask itself at the `## Verdict` DELETE line `:117`, **id-less at the bank and stated as such**⟩ | `EditorHeader → HeaderRibbon` — **DEAD, replace-by-deletion**. Re-measured: `EditorHeader.vue` PRESENT at `origin/master`, **108 lines** (the `:194` roster row's figure holds), exported from `shell/index.ts:2`, and ⟨`git grep -n '<EditorHeader' origin/master -- demo`⟩ → **no instantiation site anywhere in `demo/`** | the deletion itself, and the header-ribbon migration decision (NO-WAVE-OWNER, SS-1/SS-2's)                                                                                                                                                                                                                                                                                                              |
| **S-11**    | S-9        | ⟨**LP-8 ≡ KF-CO-35 + ME-18** : `KF-W6-CARRY.md:175`; provenance legs kf-LayerConfigPanel D-5/C-7 · kf-ChannelOptions RR-B · kf-MatrixEditor C-8 · kf-CubeScene L-5/C-6⟩         | the number-field row — the _"consumed-but-mis-called"_ class                                                                                                                                                                                                                                                                 | **the `/number-field` EVALUATION is KF.W6's** (ME-18 precedent); W0 fixes the identity only                                                                                                                                                                                                                                                                                                             |
| **S-12**    | S-9        | `kf-AmigaScene` ⟨**C-6** : `:61`, `:146`⟩                                                                                                                                       | `Surface`/`PaperBackdrop`/`Card` shadowed by 8 lines of bespoke CSS — _"extends S-1..S-8"_, **the block, not S-1**                                                                                                                                                                                                           | **owned by KF.W6** (evaluate-not-swap; the `:6–12` no-DOM-layer ruling may reduce the cure to a shared `.stage-plate` recipe)                                                                                                                                                                                                                                                                           |
| **S-13**    | S-9        | `kf-AnimationVisualizer` ⟨**KF-AV-28 · D-18 = C-13** : `:72`⟩                                                                                                                   | the larger of the two rail/ball shadows — `ScrubberTimeline` (256 L) against the censused 162-L `SequenceScrubber`                                                                                                                                                                                                           | **carries KF-AV-28's STANDING SUPERSESSION RIDER by name**: KF.W7's timeline-evaluate verdict may supersede any cure on a governed row; the row's own _"evaluate, not swap"_ verdict is **KF.W7's intake, never W0's**                                                                                                                                                                                  |
| **S-14**    | S-9        | `kf-CSSPasteDialog` ⟨**R-5** : `:43`⟩                                                                                                                                           | bespoke shadow of the shipped `Textarea`, census-invisible, 1 site; _"census row 80 classified `G` on import-list evidence alone"_                                                                                                                                                                                           | the suffusion cure (KF.W6's)                                                                                                                                                                                                                                                                                                                                                                            |
| **S-15**    | S-9        | `kf-DemoGlobalChrome` ⟨**D-5 / L-3 / C-2** : `:43`⟩                                                                                                                             | the `./toast` family (= C-1.F row **F-1**)                                                                                                                                                                                                                                                                                   | **the swap verdict is census-owned, not W0's**; the identity guard travels with the slot                                                                                                                                                                                                                                                                                                                |
| **S-16**    | S-9        | `kf-KeyframesAddDialog` ⟨**KAD-12** : `:56`, C-3/D-23b; the claimant row in this wave's sole carry, `KF-W6-CARRY.md:176`⟩                                                       | a **second global keyboard mechanism** beside the house `/keyboard` registry — the demo's sole `useMagicKeys` site, layout-dependent (`Ï` / US-literal), leaking a window listener for the component's life                                                                                                                  | **the cure is KF.W6's** (`registerShortcut(…, {allowInInput:true, …})`, which dissolves the entire `Ï` cluster); W0 fixes the identity only                                                                                                                                                                                                                                                             |
| **S-17**    | S-10       | `kf-KeyboardShortcutsModal` ⟨**C-9** : `:29`, `:68`, restated `:127`⟩                                                                                                           | the `/command` family shadow (`./command` PRESENT at installed 7.0.0)                                                                                                                                                                                                                                                        | **CLAIM-INPUT ONLY**: that seat's own _"COLLISION — renumber to S-10"_ ruling is **superseded by this mint**; the E-3 addendum under the original id is **`KF.W0.e`'s act** at the record. The seat's booked lesson stands verbatim: _"census extension ids must be allocated against the banked registry, not against the local corpus"_ — which this mint honours by allocating from the block's home |
| **S-18**    | S-10       | `kf-KeyframesEditor` ⟨**KF-KE-21** : `:63`, restated `:128`/`:172`⟩                                                                                                             | the progress bar as a shadow of glass `./progress` (PRESENT at 7.0.0)                                                                                                                                                                                                                                                        | the `Progress` **evaluation** is KF.W6's; binding it to something true is **KFED-UNIT's**                                                                                                                                                                                                                                                                                                               |
| **S-19**    | S-10       | `kf-SquareInstrument` ⟨**C-12** : `:78`, restated `:129`/`:140`/`:145`⟩                                                                                                         | the status pill against glass-ui `Chip` (`./chip` PRESENT), _"correctly evaluate"_                                                                                                                                                                                                                                           | an **intake row, not a re-derivation of S-1..S-8**; the shape completion is a `design-idioms.css` edit, NO-WAVE-OWNER                                                                                                                                                                                                                                                                                   |
| **S-20**    | S-10       | `kf-EasingTarget` ⟨**KF-ET-27** : `:69`⟩ — second provenance leg `kf-EasingSidebar` ⟨**KF-ES-20** : `:61`⟩                                                                      | the **`design-idioms.css` census arm** — glass overlap + contract truth                                                                                                                                                                                                                                                      | **THE ARM IS KF.W5's ACT** ⟨§Sequencing KF.W5 row `:691`⟩. W0 assigns the identity and **performs no part of the arm**; the split is the C-17 rule applied — _claims are inputs to the mint, and the mint is never the evaluation_                                                                                                                                                                      |

### §5.3 — the three rows that receive NO slot, enumerated (enumeration is what stops the next seat re-raising them)

| claim | banked id ⟨record : line⟩                           | disposition                                                                  | reason, at the bank                                                                                                                                                                                                                                                                                                                                                |
| ----- | --------------------------------------------------- | ---------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| S-9   | **K-6 / D-§S-9 = C-S-B** ⟨`kf-SpringTrace:89`⟩      | **DEAD OCCUPANT — ENUMERATED, NOT MINTED**                                   | _"S-9 · SpringTrace — JUSTIFIED BESPOKE"_ was **KILLED AS STATED** (`generateCurveSVGPath` _"was one `ls` away from a directory both axes cited"_). The row _"must be re-derived (0..1-range caveat included) before any census landing"_ — **that re-derivation is not this wave's**, and a slot minted over a killed claim would be a slot with no subject.      |
| S-9   | **C·C-2's census amendment** ⟨`kf-SharePopover:90`⟩ | **REFUSED — ENUMERATED, NOT MINTED**                                         | _"ALREADY CLOSED, TWICE … the S-9 slot is spoken for by an adjudicated booking (kf-EditorHeader.md:117). **Refused per anti-rename**"_. The refusal **is** the anti-rename law working; it is recorded so it is not re-litigated. The slot that booking became is **S-10** above.                                                                                  |
| S-10  | ~~the fifth record's original request~~             | **STRUCK AS A DUPLICATE — retained in the table so the strike is checkable** | The row is `kf-KeyframesEditor`'s own S-10 ask ⟨`:63`⟩, **already minted one row above as S-18 under `KF-KE-21`** — the same claim entered twice, the second time without its id. C-17's own falsifier convicts the row as written: _"a minted slot that does not cite its originating record fails too"_, and a row that does not know its record cannot cite it. |

**Roster arithmetic, counted at the table rows and not carried forward**: 10 S-9 claimants (8 minted ·
1 dead-and-enumerated · 1 refused-and-enumerated) + 4 S-10 claimants (4 minted) = **14 live rows**,
plus **1 enumerated strike** = **15 rows**, of which **12 receive a slot** (S-9..S-20) and **3 receive
a named disposition without one**. `8 + 4 = 12` minted; `12 + 3 = 15` dispositioned; **15 = C-17.R's
row count**. Every one of the fifteen resolves to a named outcome under a closure sweep.

**The claim-token crosswalk** (the property whose absence made the collision): banked **`S-9`** →
{S-9 · S-10 · S-11 · S-12 · S-13 · S-14 · S-15 · S-16} + {enumerated-dead `K-6`, enumerated-refused
`C·C-2`}; banked **`S-10`** → {S-17 · S-18 · S-19 · S-20} + {struck duplicate → S-18}. **Three records
also use `S-10` as their own local roster index** — `kf-App.skeleton.md:91` · `kf-ChromeDock.md:125` ·
`kf-ControlsPaneWrapper.md:122` — and **two more cross-reference another record's index**
(`kf-RibbonBar.md:7`, `:72`); **none of the five is a census claim and none receives a slot.** The
S-10 enumeration reproduces C-17.R row 13 exactly at this seat's own run: ⟨`grep -n 'S-10' kf-*.md |
wc -l`⟩ → **37** raw · ⟨`grep -l`⟩ → **15** records; substring discards `KF-ES-10` ×11 · `MISS-10` ×4
· `KF-SS-10` ×3; **post-discard 19 lines across 9 records**.

**Carriage claim, per LAW B**: this section asserts no completeness in its own voice. Its input set is
**C-17.R at the spec's post-round-4 bytes**, whose own carriage is measured at
`docs/tranches/X/keyframes/conformance/PASS-4/KF-W0-CHECK.md` §1 (2026-08-28) — the artifact that
found the round-3 roster short by `KF-ET-27` (E-2) and by its second leg `KF-ES-20` (E-3) and the
row-13 receipt unreproducible (D-3). A later pass re-points this citation rather than re-asserting
the claim.

**What the mint hands forward** ⟨§Sequencing KF.W10 `:695`⟩: **KF.W10 consumes; it never mints.**
`CARRY-C-3` and `CARRY-C-5` consume the ids assigned above, and they must be consumed **at these
bytes** — the twelve slots, the three enumerations, and the crosswalk — never at a prior round's
roster shape.

---

## §6 — C-10 / C-11: the PRM roster and the z-index cells, re-scoped in ONE motion

Both land in **`lane-frontend.md`** (§6.5 + its counts-table tally row + the two z cells), in one
appended motion, per C-10's re-aimed act (D-4) — **`CENSUS-2026-08-03.md` receives the pointer-cell
correction ONLY.** The measurements:

**PRM, re-run at `origin/master`.** All **13** banked sites reproduce **at their banked file:line,
byte-for-byte** — 10 `@media (prefers-reduced-motion: reduce)` blocks + 3 JS query sites. **The
roster's file denominator is wrong by one and its meaning is wrong altogether:**

1. **13 sites across 13 PATHS, not 12** — the 10 CSS blocks sit in 10 distinct files and the 3 JS
   sites in 3 more. The "12" is only reachable by counting `scenes/easing/EasingTarget` **once**
   across its `.css` block (`:48`) and its `.vue` query (`:234`), i.e. by **component**, not by file.
   Corrected as _13 sites · 13 paths · 12 components_, with the unit named.
2. **The roster is a roster of LOCAL GUARDS, and local guards are not coverage** (KF-AX-32). The
   actual floor is the vendor's **universal** rule, verified at the installed bytes:
   `node_modules/@mkbabb/glass-ui/dist/styles/utilities/a11y-overrides.css` opens with
   `@media (prefers-reduced-motion: reduce) { *:not([data-allow-motion]) { animation-duration:
0.01ms !important; animation-iteration-count: 1 !important; } … }` — **byte 0, unlayered,
   `!important`**, reaching **every demo element** through the chain proved by path at §4.
3. **Both "gap" rows therefore HOLD** — `TypingDots.vue:121` (PRM in prose, deferring to the engine's
   resting state) and `KeyframeTimeline.vue:94` (deferring to glass-ui) are **correct delegations**,
   not gaps. The census's _"unverified statically"_ is discharged: it is verifiable statically, and it
   verifies.
4. **`KeyframeTimeline.vue:94` cites the wrong vendor file, and the reason is now stated rather than
   asserted**: it names the _"`transitions.css` PRM block"_. That file EXISTS
   (`dist/styles/transitions.css`, pulled by `index.css`) and DOES carry a PRM block — but that block
   is **class-scoped** (`.fade-enter-active`, `.tab-fade-*`, `.pane-swap-*`, `.metric-swap-*`), so it
   covers none of the timeline's own motion. The rule that actually covers it is
   `utilities/a11y-overrides.css`. **The delegation holds; the citation does not** — and a correct
   conclusion resting on the wrong file is exactly the provenance class C-13 convicts.
5. **Consequence carried, not performed**: D·D-1's BLOCKER ground was manufactured by this
   mis-scoping, and **KF.W9's per-scene tallies re-derive from the corrected roster** (kf-CubeScene: 4
   engine motions + 1 gated; **kf-OrbitalDrag OD-11 corrects the cube tally to 5 ungated**). That
   re-derivation is KF.W9's act, named here, performed nowhere in this wave.

**The z-index cells (C-11 / KF-AX-12), both of them, measured at the frontier:**

```
$ git grep -n 'z-index' origin/master -- demo/scenes/cube/CubeAxisLines.vue
  demo/scenes/cube/CubeAxisLines.vue:67:    z-index: var(--z-behind);      ← a TOKEN, not a raw value
$ git grep -n 'z-index: *-\?[0-9]' origin/master -- demo
  demo/styles/style.css:38:   value (proof:brittleness gates against drift). The one raw `z-index: -10`
                              ← the ONLY remaining "-10" in demo/ is PROSE, inside a comment that
                                cites a dissolved gate (that dead-gate line is KF.W0.e's roster, not this seat's)
$ git grep -n 'z-\[[-0-9]' origin/master -- demo   → (no hits)   the bracket ban HOLDS
```

**The census is wrong about this file at its own frontier, twice** — `lane-frontend.md:253` (the
roster row _"axis lines (raw `z-index:-10`)"_) and `:442` (_"One acknowledged exception:
`CubeAxisLines.vue`'s raw `z-index:-10`"_). Both are corrected by appended note in the same motion.
**The z-contract PROSE RECUT is NOT performed here** — KF-AX-11/12/31 in one KF.W6 motion, together
with the `--z-behind` policy contradiction at `layout.css:8-11` (_a contradiction to RESOLVE, not a
stray to delete_) — §Excluded `:711`. **W0 owns only the census re-anchor.**

---

## §7 — What this seat did NOT do, loudly

| not done                                                                                                 | whose                                                                                                                                                                                       |
| -------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **X-4 basis reconciliation** (80 / 139 / 153 / "159")                                                    | **KF.W5's.** W0 re-runs the counts; restating four bases here would mint a fifth. The SCH-3 LOC basis is stated _with_ its command precisely so it does not become one.                     |
| **SCH-7's depcruise re-verification**                                                                    | **KF.W5's input.** `depcruise` was **not executed**. W0 restates L-1/L-2 as _unverified-at-HEAD_ with their static witnesses; a green run here would be the false refutation the row names. |
| **SCH-2 re-booking**                                                                                     | **Re-verified, not re-booked.** The cure is KF.W6's top suffusion target.                                                                                                                   |
| **The `/number-field` evaluation**                                                                       | **KF.W6's** (ME-18 precedent). W0 minted **S-11** and stopped.                                                                                                                              |
| **The z-contract prose recut**                                                                           | **KF.W6's**, in one motion with KF-AX-11/12/31.                                                                                                                                             |
| **The `design-idioms.css` S-10 census arm**                                                              | **KF.W5's act** ⟨§Sequencing `:691`⟩. W0 minted **S-20** and performed no part of the arm.                                                                                                  |
| **The E-3 addendum at `kf-KeyboardShortcutsModal`** recording that its self-ruled renumber is superseded | **`KF.W0.e`'s** — the 58 records are `.e`'s writable set, not this unit's. Named here so the obligation is not lost: the slot the mint assigned that claim is **S-17**.                     |
| **Any ruling on `package.json` / `.npmrc` / the worktree deletion**                                      | **`KF.W0.b`'s (G-0.2).** SCH-1 is a count-claim here and nothing more.                                                                                                                      |
| **Any product byte in `keyframes.js`**                                                                   | Out of bounds. This unit wrote **zero** bytes outside `value.js/docs/**`.                                                                                                                   |

---

## §8 — Gate G-0.6

| clause of the GREEN ⟨spec `:602`⟩                                                                      | BEFORE (baseline, seat 0)                                                   | AFTER                                                                                                                                                                                                                                       |
| ------------------------------------------------------------------------------------------------------ | --------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| every SCH row + X-2 carries a dated, re-derived figure with its command pasted                         | **RED** — 153/58/185 banked at the baseline; SCH-1/2/5/7 unrun; no artefact | **GREEN** — 8 rows, each with command + literal output + date, in this file and byte-for-byte in `artefacts/W0/counts-2026-09-17.txt`                                                                                                       |
| SCH-4's MEASURE-AT-OPEN row                                                                            | **RED** — _"does not reproduce at any spelling this seat can state"_        | **GREEN** — predicate stated ahead of the readings; **62/16** at `origin/master` `src/`, DOCS's probe reproduced exactly, LIB's 61/15 shown unreachable at all three committed coordinates, the delta localised to `/value`                 |
| the cascade-coupled column exists beside the import census                                             | **RED** — no column; 21 zero-import `.vue` scored clean                     | **GREEN** — the column is measured (10 of 21 cascade-coupled under a stated predicate; 11 under the widened one), landed at CENSUS (legend) and `lane-frontend.md` §3 (roster), **before** the F-1 disposition                              |
| §6.5's PRM roster + its tally row + the z cells re-scoped **in one motion**, at the roster's real home | **RED** — roster at 13/12 unexamined; z cells wrong at the frontier         | **GREEN** — one appended motion in `lane-frontend.md`: roster (13 sites · 13 paths · 12 components), tally row, both z cells; `CENSUS-2026-08-03.md` receives the **pointer-cell correction only** (D-4)                                    |
| every C-17.R claimant dispositioned in ONE motion                                                      | **RED** — no mint; three records claiming "S-10", four claiming "S-9"       | **GREEN** — 15 rows in one motion: **12 minted S-9..S-20**, each citing its originating record; **2 enumerated** (dead `K-6`, refused `C·C-2`); **1 enumerated strike**; plus the claim-token crosswalk and the stated id-space rule        |
| _second falsifier_: a mint over four-plus-one re-collides                                              | —                                                                           | **NOT FIRED** — the mint ran over the full fourteen-live/fifteen-row roster at the spec's post-round-4 bytes, including `KAD-12` (the sole carry's own claimant) and `KF-ET-27` (+`KF-ES-20`), the two claimants prior rounds were short by |
| _falsifier_: a figure restated without its command                                                     | —                                                                           | **NOT FIRED** — every figure in §1 is a command and its output                                                                                                                                                                              |
| _falsifier_: green-by-basis-reconciliation (X-4)                                                       | —                                                                           | **NOT FIRED** — X-4 is declared KF.W5's at §7 and no basis is reconciled                                                                                                                                                                    |

**G-0.6: RED-AS-EXPECTED → GREEN on every clause this unit owns.**
