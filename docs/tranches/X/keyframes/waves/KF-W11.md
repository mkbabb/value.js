SERVED MODEL: claude-fable-5-1

# KF.W11 — Demo Scene Repair

**Wave**: KF.W11 · Demo Scene Repair — the NINE NO-WAVE-OWNER scene packets of RULINGS R-15's canonical seventeen (`KF-W10 §6.D · SUCCESSOR-FORMATION REGISTER` row 1; homed at `KF-W4 §Sequencing`, the R-15 homing block; rostered at `KF-W1 §Cross-edges` edge 9): **cube · sequence · spring · spring-plot · spring-physics-facet · spring-artifact-truth · square · amiga · drag-seam**. Repair, not evaluate: every packet's cure shape is already ruled at its home record and this file HOMES, ORDERS and BOUNDS it — it re-derives no ruling, mints no id, and re-books nothing a sibling wave owns.
**Half**: frontend (keyframes.js demo — `demo/scenes/**` plus the three drag-seam composables and the one `AnimationVisualizer.vue` carve the drag-seam packet's own rows sit in). **Sub-tranche**: X·KF. **Track**: B.
**Authority**: `docs/tranches/X/COHESION.md` §0d (the mint, the 9+6+2 partition, the six travelling locks, the SS-1/SS-2 authoring seat); `EXECUTION-RUNBOOK.md §4.5`; ratified under `docs/tranches/V/megatranche/SCOPE.md` M-25. **Minting authority**: `KF-W4 §Sequencing`, the R-15 homing block, KF.W11 bullet. **Terminal disposition of record for the X·KF close**: `KF-W10 §6.D` row 1, cited by STABLE ANCHOR ALONE (LAW C(3)).
**Alias law (R-B idiom)**: `KF.W11` denotes `X.KF.W11`; a search alias, never a rename. The prose name *"Demo Scene Repair"* is KF-W1 edge 9's and KF-W4 §Sequencing's spelling, carried verbatim.
**Status**: planned. No product byte is written by this file; authoring it opened no product source for writing; execution awaits the sequencing head (§State).
**Ref of record**: keyframes.js `origin/master` **`69095552`** — ⟨cmd⟩ `git -C /Users/mkbabb/Programming/keyframes.js rev-parse --short origin/master` → `69095552`; `git rev-parse --short HEAD` → `69095552` (local `master` = the same); `git status --porcelain` → two untracked `docs/tranches/V/coordination/VALUEJS-INBOUND-*` letters, **zero modified product paths**. **The banked anchors are older than this ref and the drift is load-bearing**: the home records were adjudicated at worktree-on-`8281638c` and re-probed at `81a56990`, and KF.W6 (glass suffusion; the `kf-focus-ring` rename `a6418729`), KF.W7 (the timeline evaluate; `7225cdd1` · `43556828` · `8cfee94e`) and KF.W8 (structure & colocation; `34b051eb`) have since MOVED several of them. **Every move this seat measured is named at its packet as a dated observation, and every line anchor is re-resolved at open before any cure** (KF.W0 §B-12; D-19; the KF-AT-28 re-anchor law).
**Standing supersession rider (KF-AV-28), carried expressly on the drag-seam limb (register row 1, lock 1)**: no cure on a governed row lands before KF.W7's per-surface SWAP verdict. **The verdict is IN and it is KEEP**: `execution/B/KF-W7.md` `.a` G1 — value.js commit **`4c03ceda`** (2026-09-18; ⟨cmd⟩ `git -C /Users/mkbabb/Programming/value.js cat-file -t 4c03ceda` → `commit` — it is a value.js record commit, not a keyframes.js sha) — *"six surfaces, six KEEP-BESPOKE, ZERO SWAP"*; the `.f` verdict table row 4 `SequenceScrubber` = **KEEP-BESPOKE · discharge receipts NONE · CARRY — the shape rows PERSIST**; row 5 `AnimationVisualizer` = **KEEP-BESPOKE · NONE · CARRY its whole NO-WAVE-OWNER roster**. **The drag-seam packet is therefore UN-GATED and every one of its rows is live**; nothing under it is `DISCHARGED by KF.W7 SWAP verdict <surface>, <date>` (that alphabet has zero members — W7's own positive statement, *"the discharge set is EMPTY"*). The rider stays on the packet as the record of why it sequences where it does, and **no seat of this wave may write that discharge string on any row**.
**Provenance**: **AGGLOMERATED** by the fresh fold seat of the SS-1/SS-2 authoring block (M-23 §3) from the two BLIND drafts at `waves/drafts/fable/KF-W11.md` and `waves/drafts/opus/KF-W11.md`, the register of record `KF-W10 §6.D` row 1, the two minting citations, the home records in `docs/tranches/V/megatranche/registry/adjudicated/` — **kf-CubeTarget · kf-CubeScene · kf-MatrixEditor · kf-OrbitalDrag · kf-SequenceScene · kf-SequenceTarget · kf-SequenceAxis · kf-SequencePlayhead · kf-SequenceScrubber · kf-SpringScene · kf-SpringTarget · kf-SpringTrace · kf-SpringHeatmap · kf-SpringPhysicsFacet · kf-StartingStyleTarget · kf-SquareScene · kf-SquareInstrument · kf-AmigaScene · kf-AnimationVisualizer (the KF-AV-15/-16 twins only)** (19 records; their `NO-WAVE-OWNER — the … packet` terminal lines are the packet rosters, consumed **by mechanism, deduped by identity, never transcribed**, M-25); `execution/B/KF-W7.md` (the verdict table and *"What the two gated successors read here"*); and the live keyframes tree, READ-ONLY. Every disagreement between the arms was resolved **at the register's bytes and the live tree, never by preference** — the record is §Provenance at the foot of this file. Validated at authoring against `docs/precepts/instructions/tranche/WAVE_SPEC.md` (L-20). **No gate exists here without a named live witness (L-19).**

| verb | state | basis |
|---|---|---|
| AUDITED | YES | the nineteen home records named above, each ADJUDICATED-not-VERIFIED, each routing its behavioral remainder `NO-WAVE-OWNER` into one of this wave's nine packets by name — ⟨cmd⟩ `grep -c 'NO-WAVE-OWNER' registry/adjudicated/kf-{CubeTarget,CubeScene,MatrixEditor,OrbitalDrag,SequenceScene,SequenceTarget,SequenceAxis,SequencePlayhead,SequenceScrubber,SpringScene,SpringTarget,SpringTrace,SpringHeatmap,SpringPhysicsFacet,StartingStyleTarget,SquareScene,SquareInstrument,AmigaScene}.md` → **16 · 43 · 30 · 39 · 38 · 44 · 22 · 39 · 31 · 17 · 33 · 30 · 32 · 20 · 30 · 54 · 41 · 39** (eighteen non-zero counts; the nineteenth record, kf-AnimationVisualizer, enters by the drag-seam identity line at `kf-SequenceScrubber.md:125`) |
| SPECIFIED | YES — this file, the spec of record, written by the fold seat over both blind arms | **the packet denominator is the register's — 9 of 17 — and carriage over the packets' row-ids is NOT asserted in this file's voice (LAW B)**: the rows land by their home records' terminal lines, cited at §Carry by record + anchor; the carriage census is the successor conformance pass's artefact and is cited by path when it exists |
| IMPLEMENTED | NO | stays NO until the gates green after the sequencing head lands |
| VERIFIED | NO | stamped only at a successor close — **KF.W10's close stamps the AUTHORED ELEVEN only** (COHESION §0d); this wave takes no verb from that close |

**Goal criterion.** This wave succeeds if, when work ends, every one of the nine scene packets has landed **as its home record shaped it** — one commit family per packet, the family's internal ordering edicts intact — and the demo's six scenes are honest instruments: what a scene's docblock asserts is what its test asserts (the MISS-3 law), what its readout prints is what its engine computes (the instrument-truth cores), and what its gesture affords is what its machine does (the gesture specs). **A wave that lands the hygiene tails and leaves a BLOCKER core un-cured — CubeTarget #53, SquareScene L-2, AmigaScene D-1, SpringTarget C-1 — has failed this goal** whatever its gate count says: it closes `complete_with_misses`. And a wave that lands forty hygiene edits and leaves `useSquareKeyboard.ts`'s docblock asserting a spring-paced tour 46 lines above `setTimeout(step, 520)` has failed it too: **a docblock asserting an invariant is an assertion, and an unmet assertion is a defect whether or not a test names it.**

**Execution shape**: **10 seats across 4 phases** — phase 1: 3 parallel (`.a` cube ∥ `.b` square ∥ `.d` sequence); phase 2: 3 parallel (`.c` spring ∥ `.g` spring-artifact-truth ∥ `.h` amiga — `.h` after `.b`); phase 3: 3 parallel (`.e` spring-plot — after `.c`'s M-3 commit ∥ `.f` spring-physics-facet ∥ `.i` drag-seam — after `.d`); phase 4: 1 serial (`.j` close). Peak concurrency **3**, inside the owner's four-workflow cap (2026-07-24). Tiering per M-12 tri-fold is the orchestrator's at execution: the three decision-shaped units (`.d`'s canonical domain, `.e`'s N-1 fork, `.f`'s field encoding) are Fable-worker ∥ Opus-worker → fresh-Fable arbiter; the roster-and-precedent units are Opus solo.

---

## §State

**Name**: KF.W11 — Demo Scene Repair
**Opens after**: **G-KFW4-1 lands GREEN** (KF-W4 §Sequencing, the sequencing-head paragraph: *"No repair packet and no UNIT may open before G-KFW4-1 lands"*; R-15: *"All 17 sequence after G-KFW4-1"* — **measured RED at this seat, twice**: ⟨cmd⟩ `npx vue-tsc --noEmit -p tsconfig.json 2>&1 | grep -c 'error TS'` → **54** · **54**; the LEDGER's KF.W4 row reads `CLOSED 2026-09-17 (honest-RED: G-KFW4-1 …)`, so the head is CLOSED-honest-RED and **this wave cannot open until a repair seat turns that count to 0** — nothing in nine `.vue`-heavy packets is defensible until a `.vue` file can fail a build) · **KF.W7 CLOSED** with its verdict table LANDED (`4c03ceda`; the drag-seam limb's lock, §Sequencing 4) · **KF.W8 CLOSED** (its colocation moves are what re-anchored this wave's square and card seams; every anchor below is post-W8) · **KF.W6 CLOSED** (the `kf-focus-ring` rename is why two of RULINGS-4's four `.focus-ring` sites are now this wave's only bare consumers) · **KF.W0 §B-12** (the re-baseline law; inherited by every anchor here).
**Agents**: 10 (a→j); phases as the execution shape above; **no jury**.
**Hard gate**: **ten conditions, ALL RED**, measured at `69095552` and pasted at §Gates, each double-run.
**Status**: planned

## §0 Open preconditions (checked at wave-open; NOT close gates)

| # | precondition | state at authoring (this seat, 2026-09-18, at `69095552`) |
|---|---|---|
| **OP-0** | **G-KFW4-1 GREEN** — the sequencing head (§State). | **RED — 54 · 54 vue-tsc errors.** HARD. |
| **OP-1** | **KF.W7's per-surface SWAP verdict for `SequenceScrubber` and `AnimationVisualizer`** — the drag-seam limb's gate under KF-AV-28. | **MET**: KEEP-BESPOKE ×6, `4c03ceda`, 2026-09-18; discharge set EMPTY; the `.f` table's rows 4 and 5 say CARRY. The packet is un-gated (header rider). **This wave inherits work, not a deletion**, and `.i` STATES this before spending a cure (KF-W7 G15's discharge discipline). |
| **OP-2** | **The `grabDx` precedent** — SquareScene MISS-1 *"shares grabDx's class"* (KF-W7 §Sequencing → KF.W11 arm (d)). | **MET AS LANDED CODE**: keyframes.js `7225cdd1` (KF.W7 `.d`, G3 — grabDx + L-m-14, one family, `TimelineTrack.vue`; ⟨cmd⟩ `git cat-file -t 7225cdd1` → `commit`). Arm (c) inherits a worked precedent, not a design question — *read, never copied blindly* (a copied producer selector is a HIGH defect; a read precedent is engineering). |
| **OP-3** | **D-19 geometry re-derivation before any geometry cure** (KF.W7's OP-3, the same law): the SequenceAxis/Playhead/Target geometry commit, SquareInstrument's tether-truth commit and CubeTarget's latch/pointer seam each carry px figures banked at the stale fork. | **OPEN** — each geometry unit re-derives its figures at its open sha in its receipt before writing a cure. A cure computed off a border box is a cure to the wrong number. |
| **OP-4** | **The spring-plot draw route's export decision** — KF.W7 `.a` ruled *"C-3 DECISION: the export is WARRANTED"* with the implementation edge at KF.W5/KF.W8. | **UNLANDED AT THE ARTIFACT**: ⟨cmd⟩ `grep -c 'sampleNormalizedSpring\|resolveLinearStops' dist/keyframes.d.ts` → **0** · **0**. The packet's *"shared-builder route with a range parameter, preferred"* is NOT available at this clock and unit `.e` takes the **anchor-hoist minimum** route unless the export has landed by its open (re-measured there). No `src/**` byte is this wave's. |
| **OP-5** | **KF-ES-2's facility-honesty spec** (kf-EditorShell; owner: the shell's wave) — the spring packet is *"sequenced with … the KF-ES-2 facility-honesty spec"* (`kf-SpringScene.md:155`). | **CONSUMED BY REFERENCE**: KF-SS-10-as-witness and KF-SST-10 ride the spec's cure and never re-derive it; if it has not landed at `.c`'s/`.g`'s open, both rows land as **witness-only** (paint-or-declare-light) and say so. |
| **OP-6** | **The transport/ribbon packet (KF.W13)** — KF-SS-8 (*"the transport drives the selected channel"*) has a ribbon-side half; the spring packet is *"sequenced with the transport/ribbon packet"* (`kf-SpringScene.md:36`, `:155`). | **CROSS-EDGE, declared at §Sequencing**: KF-SS-8's scene-side cure lands here; its ribbon-side half is KF.W13's C-2 time-space contract. Neither blocks the other's open; the seam is named at both ends. |
| **OP-7** | **The owner-decision gate D-2** (kf-SquareInstrument) — the OD-5/T.M2 sign-off deciding which instrument layers live; D-18, D-10, N-SQ-5 ride it. | **OWNER-UNRULED.** `.b` states which of its rows the ruling gates **before** spending any cure; no layer is deleted before the sign-off; if unruled at `.b`'s close the layers question lands `complete_with_misses` and says so. Gates a named subset, not the wave. |

**OP-0 is hard.** OP-7 gates a named subset. The write authority for keyframes.js is the one every landed X·KF wave has used — `master` at `/Users/mkbabb/Programming/keyframes.js`, pathspec commits, the standing law's trailer (KF.W7's roster: `7225cdd1` · `43556828` · `8cfee94e` all landed there); no seat re-opens that question.

---

## §Scope

1. **cube** — the four-record packet (kf-CubeScene's routing block is the spine; kf-MatrixEditor's NO-WAVE-OWNER rows *"agglomerate into the cube scene-repair packet whose core is the matrix-editor family"*, its own routing law at `:42`) lands as ONE family per home record: CubeTarget's Roll stack + the **#53 + #7 paired seam** (the die's only pre-start orientation writer has never painted: `useTransformState.ts` hands `transformTargetsStyle` a `Matrix3dCall` object) + the latch/pointer seam + lighting-truth **#4/#16/#36/#56** (the graph attitude is a *contract gap* — `useCubeRelit` receives no graph attitude at all; `0.6 → −0.6` alone fixes the model's frame, not the screen's); CubeScene's matrix-editor family (**with kf-MatrixEditor ME-30/ME-31 ≡ L-4+M-6/L-3, ONE sync-topology spec**, and **ME-42's edit-representation limb** — the 2-dp display string *is* the write path, so the digit policy states the *edit* representation or `formatOptions` separates the two; a display-only cure is the defect) + state/identity + glass-conformance (**D-2/C-3 + D-3/C-4+D-25inv with the SpringScene rider: the four phantom `variant: "outline"` sites are ONE edit** — measured ⟨cmd⟩ `grep -rn 'variant:' demo` → **4**: `CubeScene.vue:193` · `:198` · `SpringScene.vue:202` · `:225`, all inside `h()` render functions, none a prop of installed glass-ui 7); OrbitalDrag's gesture-math + **latch family (= KF-AX-1's registry adoption + blur clear + expose reset)** + model-contract + presentation.
2. **sequence** — the five-record packet: the loop-seam commit, the transport-surface decision, the reel packet, the row-surface packet, **the instrument-truth core as ONE canonical-domain decision (M-10's frame: which of four domains is canonical — one spec, not four patches; K-25: N-1's domain cure and N-2's recompute cure land against each other; N-14's valuetext SEQUENCED AFTER N-1)**, the geometry commit (after OP-3), the a11y cluster **including ST-1's affordance edit** (one word in `SequenceTarget.vue` + the `.seq-handle:focus-visible` box-shadow deletion in `SequenceTarget.css` — measured at **`:177`** today; the banked `:163-167` MOVED), **ST-4's one-binding cure** (`:loading="demo.isReeling.value"`, closes three rows), the scrubber's gesture spec (one `applyScrub(p, {gesture})`), the prose-truth sweep, and the N-10 test obligation.
3. **spring** — SpringScene's interaction-truth + coordination + surface sweep; SpringTarget's contract head C-1, honest-instrument core, **M-4→M-3** (SpringTrace onto `.fn`; the parser, both dead guards and the regex die together — LAW A census pasted), M-5, N-1, the gesture spec, the a11y one-edit family, and the four born-RED tests (a)–(d).
4. **spring-plot** — SpringTrace's instrument-truth core as ONE commit family (the N-1 fork decided FIRST, in the receipt, then the draw route per OP-4), mark-design rows, type/register rows, N-5, L-8 (pending SS-13 #5), L-14.
5. **spring-physics-facet** — SpringHeatmap's instrument-truth core as ONE design decision (what the field encodes, on which axes, at what scale, with what legend — written before the family opens), the input-integrity core, the contract commit (two `defineModel`s), N-SH-3, boundary/hover/pips, prose, hygiene, **L-m-5's born-RED tests**; plus SpringPhysicsFacet's NO-WAVE-OWNER rows SPF-3 (the `clamp(values[i] ?? 0, 0, 1)` painter discards 100 % of the overshoot that is the only thing distinguishing the four presets — analytic peaks 1.005 / 1.068 / 1.205 / 1.000; L's cure #2 is KILLED (K-10); sound forms = remap `[-0.25,1.25]→[0,1]` or a stated-geometry headroom cap), SPF-4 (four exclusive presets as four `aria-pressed` toggles — the ToggleGroup port or the per-chip `aria-label` partial), SPF-5 (active-cell AA), SPF-7 (both inert `!important`s deleted). **SPF-13 is booked here and decided at KF.W12's OPTIONS-UNIT** (its decision is KF-CO-47's; this wave spends it on the facet's bytes after that decision exists).
6. **spring-artifact-truth** — StartingStyleTarget's artifact core KF-SST-3·4·5·9 (+11·12·26) as ONE spec, the verb/state pair KF-SST-1·6 (the survivor gets `aria-expanded`/`aria-pressed`; measured **0** today), KF-SST-2, -10 (with OP-5), -31, the prose sweep, the hygiene tail.
7. **square** — the **editor-seam packet (L-2 BLOCKER)**: `updateFromString` must re-supply the animation's own renderer, or `adoptCompiled` must preserve a receiver's non-default transform — **the demo half of that decision (`useKeyframeOps.ts`, the `updateFromString` hunk at `:58-69`, modify-carve) is this wave's per `kf-SquareScene.md:40`'s own disposition (*"NO-WAVE-OWNER (the square scene-repair wave's editor-seam packet)"*); the library half is NOT (§Excluded)**; + the `RAFPlayback` degrade-not-wedge rider; **the playback-authority packet (ONE playing-state authority — `facility.isPlaying()` exists; the sole `animationGroup.pause()` in any scene is `SquareScene.vue`'s, behind the machine)**; **the colour packet (the write at `useSquareDemo.ts:163` occluded by `.demo-box`'s opaque `background-image` at `SquareScene.css:52` — and the file's own `:29` docblock cites `:161` for a write that lives at `:163`, a comment-truth limb the MISS-3 law adds)**; the instrument packet; the input/a11y packet (MISS-1 grabDx via OP-2's precedent); geometry D-8; the hygiene tail; **and SquareInstrument's six commits** (owner-decision gate D-2 · tether-truth · instrument-truth · disclosure · field · chrome).
8. **amiga** — the twenty-two-row packet headed by **D-1 (BLOCKER: the paused stage is pinned to HOME — `rendered` is written only under `playing`; nothing reads `pose` outside that branch; the gate is doubled)**, L-B1, D-2's shared idiom (**a BORROWING from SquareScene — lands after `.b`'s a11y cure, never against the bank's description of it**), D-3+C-18+M-3, the L-M/C pairs, MISSED-A/C/E/F/G, C-16/-17/-20/-10/-5, L-m3/L-m6, the INFO rows.
9. **drag-seam** — **KF-SCR-1 + L·D-1 as ONE cure family** (re-entrancy guard + `pointerId` latch + scope-disposal release + the "Nesting-safe" docblock corrected) spec'd WITH **KF-AV-15** across both seams — **the two seams are two composables at the frontier**: `demo/composables/useDragScrub.ts` (the scrubber's, 150 L — `acquireSelectSuppression()` at `:116` on the pointerdown path, `setPointerCapture(e.pointerId)` at `:120`, release gated behind `dragging`) and `demo/components/instrument/transport/composables/useDragCapture.ts` (the AnimationVisualizer's AND PlaybackRibbon's, 70 L — `isDragging` gate at `:39`/`:44`, acquire at `:56`, release at `:47`, its docblock's `tryOnScopeDispose` guarantee at `:20`), both terminating in `demo/utils/gestureSelectSuppression.ts`'s one `activeGestureCount`; **C·C-1 ≡ KF-AV-16** (throttle-vs-decouple at the machine seam, ONE decision for pointer + coast); KF-SCR-5's dead-API/prose cleanup on the same seam edit.
10. **Close** — receipts, the per-packet commit roster audited against §Bounds, the BH relay of every producer row this wave surfaced (glass-producer → SS-6, lock 4), the E13 sweep, the LEDGER row.

### Triumvirate dispatch (WAVE_SPEC §3a)

- **Bounds whose expansion invalidates the wave**: any `src/**` write (the square L-2 library half, the spring-plot export, CubeTarget #7's library diagnostic) — ESCALATE, never expand; any write to `TimelineTrack.vue`, `KeyframeTimeline.vue`, or `AnimationVisualizer.vue` beyond the KF-AV-15/-16 twins (KF.W7's surfaces); any write to `CubeAxisLines.vue` (KF.W12's); any write to `PlaybackRibbon.vue` (KF.W13's — it consumes `useDragCapture` and is re-measured by that wave, never edited here).
- **Non-local gate failures**: G-KFW11-7 (square L-2) failing because the demo half cannot preserve the renderer without the library half → triumvirate, with KF.W5/KF.W8 named; G-KFW11-1 (cube #53) failing because `transformTargetsStyle`'s contract is the engine's → same; G-KFW11-9 failing because a single guard cannot serve both composables without changing `useDragCapture`'s exported surface (PlaybackRibbon's consumer) → triumvirate with KF.W13 named.
- **Diagnostic loops that halt at three**: the SpringTarget M-5 race (a double-tap at t≈1500 ms) reproduced three times without a deterministic harness → halt, escalate the harness shape.

---

## §Bounds

### B.1 Frontier re-anchor (this seat, read-only, `69095552`, 2026-09-18)

Every anchor this wave inherits is banked against `8281638c` or `81a56990`; ⟨cmd⟩ `git rev-list --count 81a56990..origin/master` → **203**. The rows below are the ones where the two arms disagreed or where a cure written from the bank would edit the wrong byte; each states the command that settled it. RULINGS-4's figures are **not amended** (E-3) — these are frontier readings beside them.

| # | inherited anchor | frontier truth | disposition |
|---|---|---|---|
| 1 | **kf-SquareScene MISS-3** — the docblock *"paced by the spring's own settle"* 46 lines above `setTimeout(step, 520)`, banked in `SquareScene.vue` | ⟨cmd⟩ `grep -n 'paced by the spring\|setTimeout(step, 520)' demo/scenes/square/useSquareKeyboard.ts` → `:20` · `:66`. **66 − 20 = 46, in the extracted composable** — the colocation settle carried the defect and its comment together. | RE-ANCHORED to `useSquareKeyboard.ts`; the lock's content is unchanged |
| 2 | **ST-1's CSS deletion** — banked `SequenceTarget.css:163-167` (KF-W4 §Sequencing's own words) | ⟨cmd⟩ `grep -n 'focus-ring' demo/scenes/sequence/SequenceTarget.vue demo/scenes/sequence/SequenceTarget.css` → **one line**: `SequenceTarget.css:177: box-shadow: var(--focus-ring-shadow);` (the `.seq-handle:focus-visible` rule spans `:175-178`); the `.vue` has **0** hits (the one word is absent). | RE-ANCHORED; the deletion and the one-word edit are ONE commit (L-9) |
| 3 | **kf-SquareScene C-1** — *"`useSquareDemo.ts:138`, the sole colour write"* | ⟨cmd⟩ `grep -n 'style.backgroundColor' demo/scenes/square/useSquareDemo.ts` → `:29` (a docblock **citing `:161`**) · `:163` (the write) · `:259`/`:260` (a settle-time clear). The occluder holds: `SquareScene.css:52` `background-image`. | CORRECTED; the packet gains a comment-truth limb (the `:29` docblock names the wrong line) |
| 4 | **The drag seam's shape** — the bank speaks of one `useDragScrub` seam and its AnimationVisualizer twin | ⟨cmd⟩ `grep -n 'useDragScrub' demo/scenes/sequence/SequenceScrubber.vue` → `:44` (import) · `:87` (`useDragScrub({`); ⟨cmd⟩ `grep -rln 'useDragCapture' demo` → `transport/composables/useDragCapture.ts` · `playback/AnimationVisualizer.vue` · `playback/PlaybackRibbon.vue`. **Two composables, three consumers; KF-AV-15's subject is `useDragCapture`** (`kf-AnimationVisualizer.md:55`). | RE-ANCHORED: the ONE seam-packet spec covers `useDragScrub.ts` AND `useDragCapture.ts`; `PlaybackRibbon.vue` is a consumer KF.W13 re-measures (§Sequencing) |
| 5 | **`.focus-ring` consumers = 4** (RULINGS-4 R4-4, quoted at §6.D row 3) | ⟨cmd⟩ `grep -rn 'focus-ring' demo | grep '\.vue:' | grep -v 'kf-focus-ring' | grep -v 'focus-ring-shadow'` → **2 class applications**: `SquareScene.vue:46` · `SpringTarget.vue:63` (+ a comment at `SquareScene.vue:43`). `KeyframeCard.vue` and `SpringHeatmap.vue` now carry `kf-focus-ring` (KF.W6, `a6418729`). | DECLARED DRIFT (E-3 — RULINGS-4 stands). **Both bare sites are this wave's files** (`.b` and `.c`); KF.W13 states the policy, this wave spends the edits (§Sequencing → KF.W13) |
| 6 | **kf-SpringTarget's clamp sites** (banked `:126-130` etc.) | measured `SpringTarget.vue:210` (`clamp(live.sampled, 0, 1)`) · `:222` (`clamp(trackValues[i] ?? 0, 0, 1.18)`) | RE-ANCHORED; re-resolved again at `.c`'s open |
| 7 | **kf-CubeScene D-2/C-3's four `variant:` sites** | ⟨cmd⟩ `grep -rn 'variant:' demo` → **4** (`CubeScene.vue:193`, `:198`, `SpringScene.vue:202`, `:225`), all `variant: "outline"` inside `h()`. | HOLDS EXACTLY; G-KFW11-1's byte denominator |
| 8 | **kf-SquareScene L-1/C-3** — *"the ONLY `animationGroup.pause()` in any scene"* | ⟨cmd⟩ `grep -rc 'animationGroup.pause()' demo | grep -v ':0'` → `SquareScene.vue:1`. | HOLDS; G-KFW11-7's byte clause |

### B.2 File bounds

All paths keyframes.js-relative at `69095552`; line counts ⟨cmd⟩ `wc -l` this seat, measured once and re-checked once. **Access** per WAVE_SPEC §4. Units in the last column.

| File | L | Access | Unit |
|---|---|---|---|
| `demo/scenes/cube/CubeTarget.vue` | 271 | modify | `.a` |
| `demo/scenes/cube/CubeTarget.css` | 210 | modify | `.a` |
| `demo/scenes/cube/CubeScene.vue` | 294 | modify | `.a` |
| `demo/scenes/cube/matrix-editor/MatrixEditor.vue` · `transformMath.ts` · `useTransformState.ts` | 187 · 186 · 227 | modify (the ME family + the sync-topology spec; the #53 seam at `useTransformState.ts` — `transformTargetsStyle({ transform: matrix3dEnd.value }, …)`) | `.a` |
| `demo/scenes/cube/orbital-drag/OrbitalDrag.vue` | 354 | modify (**the latch family's registry adoption — `useEventListener(window, "keydown"/"keyup")`, ⟨cmd⟩ `grep -c 'useEventListener(window, "keydown"' …/OrbitalDrag.vue` → **1** — is KF-AX-1's upstream half and is THIS unit's write; KF.W12's AXISLINE-UNIT consumes it by commit**) | `.a` |
| `demo/scenes/cube/orbital-drag/{composables/**,quaternionEuler.ts,types.ts,index.ts}` | — | modify-carve (OD model-contract rows only) | `.a` |
| `demo/scenes/cube/{useCubeDemo.ts,useCubeRelit.ts,cubeTransformStore.ts,cubeKeys.ts}` | 191 · 87 · 20 · — | modify-carve (lighting-truth #4/#16/#36/#56 — the graph attitude *parameter*; state/identity; the ppMode checklist) | `.a` |
| `demo/scenes/spring/SpringScene.vue` — **the two `variant:` lines `:202`/`:225` ONLY** | 272 | modify-carve — **`.a`'s glass-conformance commit carves these two lines in phase 1; `.c` (phase 2) never re-touches them** | `.a` → `.c` |
| `demo/scenes/sequence/SequenceScene.vue` | 43 | modify | `.d` |
| `demo/scenes/sequence/SequenceTarget.vue` | 252 | modify | `.d` |
| `demo/scenes/sequence/SequenceTarget.css` | 271 | modify (ST-1's deletion at `:175-178`, the `.seq-handle:focus-visible` rule) | `.d` |
| `demo/scenes/sequence/SequenceAxis.vue` | 49 | modify | `.d` |
| `demo/scenes/sequence/SequencePlayhead.vue` | 92 | modify | `.d` |
| `demo/scenes/sequence/{useSequenceDemo.ts,useSequenceInstrument.ts,useTypedTrigger.ts,sequenceKeys.ts}` | 482 · 45 · 31 · — | modify-carve (loop-seam, reel, store hygiene) | `.d` |
| `demo/scenes/sequence/SequenceScrubber.vue` | 193 | modify — **SHARED by `.d` (the scene rows, `kf-SequenceScrubber.md:137`) and `.i` (the seam rows, `:136`)**; `.i` opens only on `.d`'s commit sha (§Disjointness) | `.d` → `.i` |
| `demo/composables/useDragScrub.ts` | 150 | modify (KF-SCR-1 + L·D-1: the re-entrancy guard at the `:116` acquire, the `pointerId` latch, scope-disposal release, the docblock at `:32`) | `.i` |
| `demo/components/instrument/transport/composables/useDragCapture.ts` | 70 | modify (**the KF-AV-15 twin — the same guard family**; its `:20` docblock's `tryOnScopeDispose` claim is false against installed @vueuse 14 and is corrected; **its exported surface is unchanged — `PlaybackRibbon.vue` consumes it and is KF.W13's**) | `.i` |
| `demo/utils/gestureSelectSuppression.ts` | 23 | modify-carve (only if the guard belongs at the token, per the seam spec) | `.i` |
| `demo/components/playback/AnimationVisualizer.vue` | 305 | modify-carve — **KF-AV-16's machine-seam decision ONLY** (the coast's per-frame `scrub` emit); every other AV row is KF.W7-carried NO-WAVE-OWNER under KEEP and stays untouched | `.i` |
| `demo/scenes/spring/SpringScene.vue` (the rest) · `SpringTarget.vue` | 272 · 476 | modify | `.c` |
| `demo/scenes/spring/{useSpringDerby.ts,useSpringDemo.ts,useSpringHotPath.ts,useCompiledEntry.ts,useSpringLinearStops.ts,springPresets.ts,springKeys.ts}` | 115 · 499 · 146 · 84 · 34 · 42 · — | modify-carve (the rows that name them) | `.c` |
| `demo/scenes/spring/useSpringKeyframesEditor.ts` | 77 | **READ-ONLY WITNESS** — the KFED seam is KF.W12's; this wave measures the mount, never cures it | — |
| `demo/scenes/spring/SpringTrace.vue` | 148 | modify | `.e` |
| `demo/scenes/spring/SpringHeatmap.vue` · `SpringPhysicsFacet.vue` | 383 · 262 | modify | `.f` |
| `demo/scenes/spring/StartingStyleTarget.vue` | 297 | modify (a `useSpringDemo.ts` byte for KF-SST-31 is `.c`'s, landed in phase 2 before `.g` needs it, or `.g` names it as `complete_with_misses` — never a parallel write) | `.g` |
| `demo/scenes/square/SquareScene.vue` · `SquareScene.css` | 331 · 159 | modify | `.b` |
| `demo/scenes/square/SquareInstrument.vue` | 212 | modify (**OP-7-gated in part**) | `.b` |
| `demo/scenes/square/{useSquareDemo.ts,useSquareKeyboard.ts,useSquareTumble.ts,squareKeys.ts}` | 429 · 103 · 50 · — | modify | `.b` |
| `demo/components/instrument/keyframes/composables/useKeyframeOps.ts` | 219 | **modify-carve — the L-2 demo half ONLY** (the `updateFromString` hunk, `:58-69`: `updateFromString` at `:58`, `animation.adoptCompiled(compiled)` at `:69`); **shared with KF.W12's KFED-UNIT, which opens on this unit's L-2 sha and never re-touches the hunk** (§Sequencing 6) | `.b` |
| `demo/scenes/amiga/AmigaScene.vue` | 302 | modify | `.h` |
| `demo/scenes/amiga/{useAmigaDemo.ts,useAmigaThree.ts,useSphereSpin.ts,utils.ts,amigaKeys.ts}` | 156 · 272 · 245 · 103 · — | modify-carve | `.h` |
| `demo/DESIGN.md` | — | modify (**prose only** — corrections of false statements the packets carry, never new design) | owning unit |
| `test/demo/scenes/cube-roll-and-prestart.test.ts` | — | create | `.a` |
| `test/demo/scenes/sequence-instrument-truth.test.ts` | — | create | `.d` |
| `test/demo/scenes/spring-derby-truth.test.ts` | — | create | `.c` |
| `test/demo/scenes/spring-trace-truth.test.ts` | — | create | `.e` |
| `test/demo/scenes/spring-heatmap-reversibility.test.ts` | — | create | `.f` |
| `test/demo/scenes/starting-style-artifact.test.ts` | — | create | `.g` |
| `test/demo/scenes/square-editor-seam.test.ts` | — | create | `.b` |
| `test/demo/scenes/amiga-paused-pose.test.ts` | — | create | `.h` |
| `test/demo/instrument/drag-scrub-reentrancy.test.ts` | — | create | `.i` |
| `test/demo/scenes/{cube-scene,square-scene,sequence-scene,amiga-sphere-spin}.test.ts` · `test/demo/instrument/sequence-scrubber-mount.test.ts` | — | modify (existing witnesses extended, never weakened; `sequence-scrubber-mount.test.ts` is KF.W7 `.e`'s `8cfee94e` — ⟨cmd⟩ `grep -c 'it(' …/sequence-scrubber-mount.test.ts` → **6** — and already carries keyboard parity + the per-sample direction latch; **L·D-12's obligation is PARTLY DISCHARGED there** and this wave adds only the gesture-spec cases; `square-scene.test.ts:70` `'c' launches the envelope tour` does not assert pacing — the MISS-3 assertion is added there) | owning unit |
| value.js `docs/tranches/X/execution/B/KF-W11.md` | — | create (the execution record; unit receipts, append-only) | every unit |
| value.js `docs/tranches/X/keyframes/evidence/W11/**` | — | create (born-RED baselines, the D-19 re-derivation tables, the per-packet before/after witnesses keyed by apotheosis row id) | owning unit |
| value.js `docs/tranches/X/execution/LEDGER.md` | — | modify (this wave's row cells + appended event lines ONLY) | `.j` |
| value.js `docs/tranches/V/coordination/INBOX.md` + the SS-6 accretion register (COHESION §4a) | — | modify (append: the producer rows this wave surfaces) | `.j` |

**Do NOT touch**: `src/**` (the library — the square L-2 library half, CubeTarget #7's diagnostic, the spring-plot export and KF-SS-3's `.fn`-without-`.css` API signal are KF.W5/KF.W8's; `adoptCompiled`, `usesDefaultRenderer` (`src/animation/engine/animation.ts`) and `compile/frame/compiler.ts`'s own contract comment are read-only oracles) · `demo/components/instrument/timeline/**` and `demo/components/instrument/keyframes/**` except the one `useKeyframeOps.ts` carve (KF.W7 / KF.W12 surfaces; `TimelineTrack.vue`'s `grabDx` at `:480`/`:489`/`:557`/`:703` is read as precedent) · `demo/components/playback/PlaybackRibbon.vue`, `demo/components/instrument/transport/TransportDock*`, `demo/styles/playback-idiom.css`, `demo/styles/design-idioms.css`, `demo/app/**` (KF.W13's) · `demo/scenes/cube/CubeAxisLines.vue` (KF.W12's AXISLINE-UNIT — rows #31/#57 are cube-packet *findings* whose cure is that unit's; naming them without curing them is the anti-re-book law working) · `demo/scenes/easing/**` (KF.W12's OPTIONS-UNIT carves `EasingSidebar.vue`'s seat rows; no easing packet exists in this partition) · `node_modules/**` · `package.json` / `vitest.config.ts` (the harness is present: `@vue/test-utils`, `vue-tsc`, the `demo` vitest project with `test/demo/**/*.test.ts` — measured by the thirty existing demo test files; no manifest change is owed) · every glass-ui tree (READ-ONLY always) · `scripts/dev/dev.sh` (unowned, never staged) · every dated spec, the adjudicated registry, conformance artefacts (E-3).

### B.3 LAW A censuses — every delete this wave books

**LAW A (RULINGS-3): no delete is authorized on a line-count.** Each carries its consumer census, re-run at execution before the act is spent: (1) **ST-1's `SequenceTarget.css:175-178` deletion** — census = the `.seq-handle` selector's applications in `SequenceTarget.vue`; one commit with the one-word edit (L-9). (2) **the dead-declaration family** — kf-SequencePlayhead D-13/L-12; kf-SpringTarget's parser, both dead guards and the regex (M-4→M-3); each census is the symbol's reachable consumer set, and each row states whether its comment dies with it. (3) **the `useEventListener(window, …)` latch in `OrbitalDrag.vue`** — census = every reader of `pointer.updatePressedKeys`, pasted before the registry adoption replaces it. (4) **The `RAFPlayback` and `animationGroup.pause()` takeover edge** — census = the machine's PAUSE dispatchers; the count goes 1 → 0 by the same command.

### Disjointness (WAVE_SPEC §4a)

Every `modify`/`modify-carve` path above belongs to exactly one unit **except three, resolved by SERIAL order, never parallel**: (i) `SequenceScrubber.vue` — `.d` (phase 1) then `.i` (phase 3); `.i` opens on `.d`'s sha in the record; (ii) `SpringScene.vue` — `.a`'s two-line `variant:` carve (phase 1) then `.c` (phase 2); (iii) `useKeyframeOps.ts` — this wave's `.b` then KF.W12's KFED-UNIT (a cross-wave seam declared at both ends; §Sequencing 6). `AnimationVisualizer.vue`'s carve and `useDragCapture.ts` are `.i`'s alone; KF.W7 is CLOSED and wrote its last byte there. Phase 1 (`.a` ∥ `.b` ∥ `.d`) is directory-disjoint (`cube/` + the `SpringScene.vue` carve · `square/` + the `useKeyframeOps.ts` carve · `sequence/`); phase 2 (`.c` ∥ `.g` ∥ `.h`) is file-disjoint inside `spring/` (`.g` = `StartingStyleTarget.vue` alone) and directory-disjoint from `amiga/`; phase 3 (`.e` ∥ `.f` ∥ `.i`) shares nothing (`SpringTrace.vue` · `SpringHeatmap.vue`+`SpringPhysicsFacet.vue` · the three composables + the AV carve + the scrubber seam rows).

### Worktree plan (WAVE_SPEC §4b)

Parallel units run in sibling worktrees of keyframes.js named `keyframes-kfw11-<unit>` (absolute path stated in each dispatch), each committing on its own branch and merged to `master` by the orchestrator in phase order; serial dependents open on the merged sha named in the predecessor's receipt; `.j` runs on `master`. No Cargo; the column is n/a.

---

## §Carry — the nine packets, homed

Each packet's roster is its home record's terminal `NO-WAVE-OWNER — the … packet` line, cited by record + anchor at `69095552`'s registry bytes (the registry is immutable, E-3). **Ids are the records' own, for life; nothing is renumbered.** A packet's *internal ordering edicts are theirs and travel intact* (KF-W4 §Sequencing). **Carriage is not asserted in this file's voice (LAW B)**; the identity-dedupes this wave performs are stated where they occur.

### P1 · cube — `kf-CubeTarget.md:102` · `kf-CubeScene.md:178` · `kf-OrbitalDrag.md:158` · `kf-MatrixEditor.md:42` (routing law) — spine = kf-CubeScene's routing block

- **CubeTarget**: the Roll stack **#1·#2·#5·#15·#20·#38·#49** (one four-defect repair, its own born-RED gate) · **#53 + #7 as ONE paired seam repair** (demo serialization + library diagnostic; born-RED: pre-start drag → computed transform asserts) — **the library diagnostic half (#7) is a `src/**` byte and is NOT this wave's; the seam lands with the demo half and a declared edge to KF.W5** · #6 (transform ownership, re-derived post-#53) · lighting-truth **#4/#16/#36/#56** (the mount-time `rotate3d(-1,1,0,30deg)` frame that `litFor` never sees — a constant ~30° error at rest compounding #4's sign flip; the cure is a *parameter*, not arithmetic) · the latch/pointer seam **#13/#18/#19/#54/#55** · loading-state #12/#22 · #14 (latent under KF-APP-1) · #17 · axis-reveal **#31/#57** (the CubeTarget side only — **BOOKED here, CURED at KF.W12's AXISLINE-UNIT**) · hygiene #23–#28/#32/#33/#35/#37 · INFO #29/#40–#48/#52/#59.
- **CubeScene + MatrixEditor**: the matrix-editor family as core (**L-3 · L-4+M-6 · L-5/C-6/S-9 · C-7/L-13/L-14 · L-9 · C-16+D-10 · MISS-1 · M-5 · M-8**, with kf-MatrixEditor **ME-1·2·4·9·10·11·12·15·16·23·26·27·33·34·35·36·37·38·39·41·42·43·44·45**; **ME-30 · ME-31 ≡ L-4+M-6/L-3, ONE sync-topology spec** with ME-7's retain-and-retarget cure — fold by reference, zero re-bookings; **ME-42's edit-representation limb** is the packet's sharpest row and **ME-43 rides** — a `-1000` value readable on the *selected* cell, where ME-2 destroys the ellipsis) · state/identity (**L-2/C-5+C-20 · L-7 · L-19 · L-29** — L-2/C-5 settled WITH kf-SquareScene L-8/C-2: ONE playing-state authority) · glass-conformance (**D-2/C-3 + D-3/C-4+D-25inv with the SpringScene rider — the four `variant:` sites in ONE commit, `.a`'s, carving `SpringScene.vue:202`/`:225`**) · **the KF-APP-1/-17 motion under ARB-1's sequencing law — DELETE ARM ONLY until the glass-ui M-A relay lands; the `headerLeft` 'fill' arm is a TRAP (it lands inside an inert toolbar)** (the KF-APP-1/-17 same-motion lock is homed inside an authored wave and is NOT one of this wave's six travelling locks; this packet CONSUMES ARB-1's arm and re-derives nothing) + MISS-4's ppMode checklist + the C-14 bucket rider · the C-15 limb settled WITH kf-AnimationControlsGroup **D-1**'s portal-architecture cure (KF.W7's OP-0 — consumed as settled or the limb waits) · the hygiene tail **D-16 · D-17/C-10 · C-8/L-28 · C-11 · C-17 · C-18 · L-16 · L-26 · MISS-2 · MISS-5 · MISS-6 · INFO**.
- **OrbitalDrag**: gesture-math + hand-off **OD-1·7·12·13·21·22·27·33·36** (signed hand-offs, per-component angles, tracked deps, derived constants; born-RED gates named in-row) · **the latch family OD-3·4·5·6·25·31 — ONE cure = KF-AX-1's registry adoption + blur clear + expose reset** (riders ⌘Z + `event.code`) — **the identity dedupe, stated**: `kf-CubeAxisLines.md:38` disposes KF-AX-1 *"AXISLINE-UNIT (first; cure upstream in orbital-drag/)"* and `kf-OrbitalDrag.md:158` homes the latch family's ONE cure in the cube packet; **one byte, one home — the `orbital-drag/**` write is this unit's, and AXISLINE-UNIT lands KF-AX-1's consumer half (the reveal on the registry) on this unit's sha** (M-25; declared at both ends, §Sequencing → KF.W12) · model-contract **OD-2·8·14·16·18·19·20·23·24** · presentation **OD-10·32·35·43** · hygiene/INFO **OD-17·26·28·30·34·37·39·40·41·42·44**.
- **Locks riding**: glass-producer rows → SS-6 (D-2/C-3's `variant` — the consumer cure is ours; any producer ask rides mail) · comment-stated invariants are test obligations · ARB-1's delete-arm-only law (shared with KF.W13's dock-menu packet; both obey, neither re-books).

### P2 · sequence — `kf-SequenceScene.md:154` · `kf-SequenceTarget.md:165` (+ `:87` ST-4) · `kf-SequenceAxis.md:131` · `kf-SequencePlayhead.md:152` · `kf-SequenceScrubber.md:137`

- **Scene**: the loop-seam commit **L-3 + L-10 + SC-5 + SC-6** (one `SweepSceneOptions` parameter; one adapter, one name) · the transport-surface decision **SC-2 (+ D19(a)'s dead reverse arm · L-12/C-8's 10 unconsumed members)** — expose the verbs or delete them; the drag gesture gets its undo · the reel packet **D7 + SC-3 + L-8 + L-9 + SC-7 + D9/D22-residue** · the row-surface packet **D2 (+SA-1) · D3 · D10 · SC-8 (≡ N-12) · D16 · D15** · store hygiene **D23/L-4/C-3 + SC-1** · the prose-truth sweep **L-6/C-5 · L-11/C-4 (three instances fleet-wide) · SC-4 · the F.W9 docblock** · **D6 + D8** · hygiene **D21 · D24 · D25 · L-13 · D11** · C-9's census extension.
- **Target**: instrument-truth folds (D-1/N-2/N-4 + the seek-shape rider) · the geometry commit **D-6 + D-13 + D-22 + ST-9** (+ ruling-1's cure; **after OP-3**; landing WITH the head/label collision cure) · the state-signal spec **D-9 + D-15 + ST-2 + ST-4 + ST-10** — **ST-4 is the packet's cheapest cure**: `:loading="demo.isReeling.value"` on the shipped Button prop closes D-9.2, D-15 and makes `playReel`'s silent guard user-visible · the a11y cluster **D-5 + ST-1 + the N-14 riders** — **ST-1 (KF-W4 §Sequencing, carried verbatim): one word in `SequenceTarget.vue` + the css deletion**, measured today at `SequenceTarget.css:177` (§B.1 row 2) — **KF.W9 holds the before/after witnesses and lands no cure (R-4/R-9a)** · the gesture/ownership spec **L-5 + ST-7 + ST-8 + L-2 (seam-level with KF-AV-15 — coordinated with `.i`) + ST-6** · the consumption row **C-2 · C-3/D-26 · C-5 + ST-3 · C-8/C-10/C-12/C-13** · render-scope **L-3/C-9** (SS-13 #6 its kill switch) · the seam-hygiene tail **L-8 · L-9 · L-10/C-4 · C-11 · D-17/D-18 · D-19 · D-21 · D-23/D-24/L-12**.
- **Axis**: the ruler-legibility trio **D-3 · D-2 · D-4** (one raster item) · the geometry-commit rider (SS-13 #4 its gate) · seam **L-3/C-3/D-6b** · **L-2 + N-16** (`--tick-count`, one edit) · type **D-7 + SA-1** · **D-8a + L-10** · **D-9** (breakpoint tokens; T.F's born-RED gate) · **SA-2** (+N-12) · hygiene **D-5/L-6 · D-10/L-11 · D-13 · SA-3 · SA-4 · SA-5 (+N-17)** · **L-12 ≡ N-10**.
- **Playhead**: the instrument-truth core **N-1 · N-2 · D-1 · N-4** — ruling-1's cure (`gap: 0.5rem var(--col-gap)`; SS-13 #10), the K-25 note (N-1's domain cure and N-2's recompute cure land against each other), **M-10's frame: which of four domains is canonical — ONE spec, written in the receipt before any of the four patches** · interaction **N-12 · N-13 · N-3 (+N-18)** — one gesture spec · geometry **D-10/L-2 + D-2/L-4/C-5** (via N-8) · stacking **D-3/L-3** (the one deletion) · property-hygiene **D-11 + D-14 + L-9 + D-13/L-12** · **N-6 · N-7 · N-15 · N-5** · prose **D-4** (four sites, three names) · **D-5 · L-7 · N-19 · D-6 · D-12** · **N-14's valuetext cure, SEQUENCED AFTER N-1** · **N-16/N-17/N-20** · **L-10/L-11/N-9 · L-14/N-8** · **N-10 — the packet's test obligation, born-RED: the three-rect equality + the axis-terminal-label = duration gate** (G-KFW11-2).
- **Scrubber (the sequence half; `.d`)**: the gesture spec — one `applyScrub(p, {gesture})` landing **C-2/L-D-4 + KF-SCR-6 + the N-13 bloom rider + C·C-12's deadband** at once · geometry residue **D·D-1's N-18 cells · C-7 · KF-SCR-3** · typography **D·D-2 + KF-SCR-4** · the prose-truth sweep (two Fira-Code comments, two brightest-number claims, the `:584` citation, KF-SCR-5's three MotionPath paragraphs, the header's "editable CONTENT" claim) · hygiene **C-5/L-D-3 · L-D-8 (pending SS-13 #4) · L-D-9 · C-11 · D-12 · D-8** · a11y **L-D-10 + the N-14-sequenced valuetext + L-D-6 + the NaN-projector guard** · coverage **L·D-12** — partly discharged by `8cfee94e` (six cases at `test/demo/instrument/sequence-scrubber-mount.test.ts`, measured); the gesture-spec cases are what remain.
- **Also carried here per KF-W4 §Sequencing's KF.W11 line**: ST-5's one-guard fix (witness KF.W9's, cure ours) · MISS-6's scene cure · **the KF-ES-4/-5/-18 and L-M8 rows the unchecked `h(PlaybackRibbon, …)` bag enables** — the sequence scene's consumption side only; the ribbon's own rows are KF.W13's.
- **Locks riding**: KF-AV-28 (the scrubber surface: KEEP, un-gated) · comment-stated invariants are test obligations · glass-producer → SS-6 · **the decision precedes the patch** (the canonical-domain decision is written before the family opens).

### P3 · spring — `kf-SpringScene.md:155` · `kf-SpringTarget.md:148`

- **Scene**: the interaction-truth core **KF-SS-1** (reseat participates in the machine) · **KF-SS-2** (facility-aware Reset) · **KF-SS-8** (the transport drives the selected channel — scene half here, ribbon half KF.W13, OP-6) · **KF-SS-10-as-witness** (paint or declare light; OP-5) · the coordination core **KF-SS-3** (one debounced, cancellable recompile seam; the library `.fn`-without-`.css` API signal is RECORDED for KF.W5, never written here) · **KF-SS-16/17's banked-cure riders** · the surface sweep **KF-SS-4 · -5 · -7 · -18 · -19 · -21 · -22..30 · -33 · INFO**. **The two `variant:` lines are `.a`'s** (P1); `.c` inherits them landed.
- **Target**: the contract head **C-1** (who dispatches play-intent; rest-on-entry preserved) · the honest-instrument core **M-2 + D-2 + D-7/C-8 + the six prose corrections** (banked coordinates re-resolved at open; today the clamp sites sit at `SpringTarget.vue:210` and `:222`, §B.1 row 6) · **M-4→M-3** (the parser, both dead guards and the regex die together — LAW A census §B.3(2)) · **M-5** (clearTimeout + gate on `derbyActive`) · **N-1** · the gesture spec (**D-5 · m-11 · i-18 · N-3** — one spec) · the a11y one-edit family (**D-6 · D-14 · N-4**) · **D-9 + C-7-nit** · **D-3's three-site flag pass** · **N-2** · **N-6** · hygiene (m-6 · m-7 · m-9 · m-10 · m-12 · D-10 · D-11 · D-12-riders · D-13 · i-15 · i-16 · N-5 · N-7 · N-8) · **the bare `focus-ring` at `SpringTarget.vue:63`** (§B.1 row 5 — this wave's file; the policy is KF.W13's, the edit is this packet's a11y family's, or it is named `complete_with_misses` with KF.W13's policy cited).
- **Born-RED test obligation (the record's own four)**: (a) paused-entry rail-tap moves the ball (C-1) · (b) the trace's first/last plotted x = 0/100 (M-3) · (c) a double-tap at t≈1500 ms leaves the overlay mounted through race #2 (M-5) · (d) the settle pulse paints at `target`, not at 1 (D-2) → G-KFW11-3.
- **Locks riding**: glass-producer → SS-6 · comment-stated invariants (the DFA-test-vs-comment assertion, KF-SS's own born-RED candidate).

### P4 · spring-plot — `kf-SpringTrace.md:140`

The instrument-truth core **D-1 + N-1 + N-2 + C-2/L-3 as ONE commit family**: **the N-1 fork decided FIRST, in the receipt** (drop the prop, or label x in ms and discharge D-6 — `defineProps<{ response; dampingFraction }>` at `:41`), then the draw route (**anchor-hoist minimum** under OP-4's measured 0; the shared-builder route with a range parameter only if the export lands first, its value.js/easing trade stated), with the parser posture **D-12/L-6/C-4 + the filter cell**, L-5's bindings, D-11's label, **L-10's extraction/test obligation** inside it · mark-design **D-3 + D-4 + N-3** (neutral reference vocabulary, EasingTarget precedent; one motion) + **D-6 · D-7 · D-13** · type/register **D-2 + N-4 (+M-6) + D-10 + D-9** (one pass) · **N-5** · **L-8** (pending SS-13 #5) · **L-14** (document or clamp the coupling) · **D-16 recorded clean-by-construction so no sweep manufactures work**. **Identity note**: M-3's *"swap SpringTrace onto `.fn`"* is SpringTarget's row and lands in `.c`; `.e` consumes the new prop surface — `.e` opens after `.c`'s M-3 commit (§Sequencing 2). Measured: ⟨cmd⟩ `git grep -c "SpringTrace" origin/master -- test/` → **0** — no test names the component.

### P5 · spring-physics-facet — `kf-SpringHeatmap.md:132` + the kf-SpringPhysicsFacet NO-WAVE-OWNER rows (`:40` SPF-3 · `:41` SPF-4 · `:42` SPF-5 · `:47` SPF-7 · `:53` SPF-13; SPF-33 INFO recorded)

The instrument-truth core **D-B1 · D-B2 · D-M8 · D-M1(+rider)** — **ONE design decision, written before the family opens** (what the field encodes, on which axes, at what scale, with what legend) · the input-integrity core **L-M-1 · N-SH-1 · N-SH-2(site) · D-M5 · N-SH-6(site) · N-SH-4** — one lattice, one gesture/focus spec, one honest published tolerance · the contract commit **L-M-7/C-M-2 + C-M-3** (two `defineModel`s; the facet binds the heatmap exactly as its sliders do) · **N-SH-3** · **D-M4 + D-M7 + N-SH-5** (with the ramp redesign) · prose **L-M-2 · L-M-5 · L-M-6** · hygiene **L-m-1/L-m-2/L-m-3/C-m-6 + INFO** · **L-m-5's born-RED tests: reversibility property + N-SH-4 tolerance + the D-B2 mix-table snapshot** (G-KFW11-5). **Facet**: **SPF-3** (the `[0,1]` painter clamp — ⟨cmd⟩ `grep -c 'clamp(values\[i\] ?? 0, 0, 1)' demo/scenes/spring/SpringPhysicsFacet.vue` → **1**; remap `[-0.25,1.25]→[0,1]` or a stated-geometry headroom cap; pairs with SPF-15) · **SPF-4** (the `ToggleGroup` port at `@mkbabb/glass-ui/toggle-group`, or the per-chip `aria-label` partial; the producer constraint → SS-6 as a rider on KF-ET-4's letter) · **SPF-5** (active-cell AA: 4.464 FAIL → cure the mix) · **SPF-7** (delete both `!important`s) · **SPF-13** (booked here; **decided at KF.W12's OPTIONS-UNIT as KF-CO-47's decision** — `kf-SpringPhysicsFacet.md:53`: *"the SAME decision KF-CO-47 owns for SPF-1 — ride it together"*; this unit spends it on the facet's bytes only once that decision is in KF.W12's record, else names it `complete_with_misses`).

### P6 · spring-artifact-truth — `kf-StartingStyleTarget.md:140`

The artifact core **KF-SST-3 · 4 · 5 · 9 (+11 · 12 · 26)** — ONE spec: one source of truth for the endpoints, true polarity/duration, a case-preserving register (rendered text ≡ copied text), a readable+reachable viewport, one honest state model with a refusal surface · the verb/state pair **KF-SST-1 · 6** (which copy survives; the survivor gets `aria-expanded`/`aria-pressed` — ⟨cmd⟩ `grep -c 'aria-expanded\|aria-pressed' demo/scenes/spring/StartingStyleTarget.vue` → **0** today) · **KF-SST-2** (the response/duration decision, priced range caveat) · **KF-SST-10** (with OP-5) · **KF-SST-31** (view gate + house throttle, with 12's guard) · prose **KF-SST-20 · 21 · 22 (+13 · 25 · 33)** · hygiene **KF-SST-15 · 16 · 17 · 18 · 19 · 23 · 24 · 27 · 29**.

### P7 · square — `kf-SquareScene.md:162` (+ `:40` L-2) · `kf-SquareInstrument.md:145`

- **Scene**: the **editor-seam packet — L-2 BLOCKER** (`kf-SquareScene.md:40`, verbatim disposition: *"NO-WAVE-OWNER (the square scene-repair wave's editor-seam packet): `updateFromString` must re-supply the animation's own renderer, or `adoptCompiled` must preserve a receiver's non-default transform — an engine-seam decision (`usesDefaultRenderer` already exists as the reference test …)"*): **the demo half — `updateFromString` re-supplies the receiver's renderer — is `.b`'s carve of `useKeyframeOps.ts:58-69`; the library half (`adoptCompiled` preserving a non-default transform, `src/animation/engine/animation.ts`) is KF.W5/KF.W8's and is DECLARED, not written** (§Excluded); measured ⟨cmd⟩ `git grep -n "adoptCompiled" origin/master -- demo/ src/` → `useKeyframeOps.ts:69` · `useCubeDemo.ts:70` · `src/animation/engine/animation.ts:269`; `usesDefaultRenderer` at `animation.ts:159` — **the reference test exists and nothing in the demo path consults it**, ⟨cmd⟩ `grep -rl 'usesDefaultRenderer\|adoptCompiled' test/demo | wc -l` → **0** · + the **`RAFPlayback` degrade-not-wedge rider** (D-27/L-7/C-9) · the playback-authority packet **L-1/C-3 · L-8/C-2 (settled WITH kf-CubeScene L-2/C-5 — ONE playing-state authority; `facility.isPlaying()` exists) · C-4 · L-3 · L-5 + ARB-1 · D-14/L-9 · MISS-9** · the colour packet **C-1 · L-11/C-5 · MISS-4 · MISS-5 · L-22a** (one honest cure: sweep the gradient's own custom property, or reset `background-image` while an inline colour owns the paint; **plus the `:29` docblock's two-line drift**, §B.1 row 3) · the instrument packet **D-1 + MISS-2 (px-space geometry) · L-4/C-12 · D-9 · D-21 · D-15** · the input/a11y packet **MISS-1 grabDx (OP-2's landed precedent) · D-4 · D-7 · D-5 + L-17 · D-6/L-6 · D-19 · D-11 · D-12** (+ the bare `focus-ring` at `SquareScene.vue:46`, §B.1 row 5) · geometry **D-8** · hygiene **D-10 · D-13 · D-16 · D-17 · D-20 · D-22 · L-10 · L-13/C-11 · L-15 · L-16/C-10 · L-18 · L-19 · C-8 · MISS-3 · MISS-7 · MISS-8 · MISS-10 · D-24/D-25 · L-22b/c**.
- **MISS-3 — the comment-stated-invariant LOCK (travelling lock 3), measured RELOCATED** to `useSquareKeyboard.ts:20`/`:66` (§B.1 row 1). The lock's content is unchanged: the docblock's assertion is a test obligation, and `square-scene.test.ts:70`'s *"'c' launches the envelope tour"* case does not assert pacing — **the cure lands the settle-paced tour AND the assertion in one commit; a comment edit alone does not discharge it.**
- **Instrument**: the owner-decision gate **D-2** (OP-7) · the tether-truth commit **D-1 + D-6 + D-16 + N-SQ-4** (measured: ⟨cmd⟩ `git grep -c "viewBox" origin/master -- demo/scenes/square/SquareInstrument.vue` → **0**; `stroke-width: 0.8` at `:162` + `vector-effect: non-scaling-stroke` at `:165`, both no-ops at 1:1; **N-SQ-4 ORDERS the cure: derive-from-travel or px-space, NEVER `viewBox`+`none` — a cure that adds a `viewBox` fails G-KFW11-7 by construction**) · the instrument-truth commit **C-3/L-D3 + N-SQ-1 + L-D5 + N-SQ-8** (one clock question: what the strip reports, in which mode, under whose name) · the disclosure commit **D-4 + D-5 + C-6** · the field commit **D-7 + N-SQ-2 + N-SQ-3** · the chrome commit **D-8 + D-11 + N-SQ-5** · hygiene/prose **D-9/C-7 · D-14 · D-15 · D-17 (×4) · L-D8 · L-D10 · N-SQ-6 · N-SQ-7 · N-SQ-9 · C-9 · C-5's rider · D-19 · L-D6 · L-D9 · the S-9/S-10 intake**.

### P8 · amiga — `kf-AmigaScene.md:159`

**D-1 (BLOCKER)** — the paused stage is pinned to HOME: `rendered` is written only under `playing` or during the self-clearing re-seat; the scrub reaches `pose` but nothing reads it outside the `playing` branch, and the gate is doubled (`onFrame` false at rest; `setProgress` never marks dirty) · **L-B1 · D-2 (shared idiom — a BORROWING from SquareScene, after `.b`'s landed a11y cure, §Sequencing 7) · D-3+C-18+M-3 · D-6/D-11 · D-8+C-9 · L-M1/C-8 · L-M2/C-14 · L-M3/C-11 · L-M4/C-2 · L-M5/C-4 · MISSED-A · MISSED-C · MISSED-E · MISSED-F · MISSED-G · C-16 · C-17 · C-20 · C-10 · C-5 · L-m3 · L-m6 · INFO**. The existing `amiga-sphere-spin.test.ts` (⟨cmd⟩ `grep -c 'it(' …` → **5**, the grab/glide contract) is extended, never weakened.

### P9 · drag-seam — `kf-SequenceScrubber.md:136` (+ `:40` KF-SCR-1 · `:42` C·C-1 · `:125` the identity line) · `kf-AnimationVisualizer.md:55` (KF-AV-15) · `:56` (KF-AV-16)

**KF-SCR-1 + L·D-1 as ONE cure family** — the scrubber seam `useDragScrub.ts`: `acquireSelectSuppression()` unconditional per `pointerdown` (⟨cmd⟩ `grep -n 'acquireSelectSuppression\|pointerId\|dragging' demo/composables/useDragScrub.ts` → `:4` import · `:87`/`:98`/`:101`/`:113`/`:132` the `dragging` boolean · **`:116` the acquire** · **`:120` `setPointerCapture(e.pointerId)` — a `pointerId` mention that is NOT a latch**) vs release gated behind `dragging` → **acquire ×2 / release ×1 on a second pointer → `activeGestureCount` (`gestureSelectSuppression.ts`, 23 L) pinned ≥ 1 → `user-select: none` strands the page** — cure: re-entrancy guard + `pointerId` latch + scope-disposal release + the "Nesting-safe" docblock corrected; **spec'd WITH KF-AV-15** (`kf-SequenceScrubber.md:125`: *"KF-SCR-1's class ≡ KF-AV-15 — one seam-packet spec covers both seams"*) — **the AV twin is `useDragCapture.ts`** (`kf-AnimationVisualizer.md:55`: non-reentrant, no scope-bound cleanup, its `tryOnScopeDispose` docblock false against installed @vueuse 14; measured `isDragging` at `:30`/`:39`/`:44`/`:54`, acquire `:56`, release `:47`, the docblock at `:20`) — **the same guard family, landed once in each composable, one commit**; the grab-offset arm (`7225cdd1`) and C·C-4's provider guard (`43556828`) are inherited from KF.W7 as landed code · **C·C-1 ≡ banked KF-AV-16** — the scrub drives a `localStorage`-persisting machine at raw pointer-sample frequency (one `JSON.stringify` + `setItem` per admitted `pointermove`; the coast's per-frame `scrub` emit at `AnimationVisualizer.vue:185-190` → `:119`); **ONE decision at the machine seam for pointer + coast** (throttle-vs-decouple), landed once for both consumers · **KF-SCR-5**'s dead-API/prose cleanup on the same seam edit. **Sequenced AFTER `.d`** (same file) and, by the rider, after the verdict that has already landed. **`PlaybackRibbon.vue:93`/`:137` consumes `useDragCapture`** — the composable's exported surface is unchanged by this packet, and KF.W13 re-measures its consumer at its open (§Sequencing → KF.W13).

---

## §Agent Units

Each unit: Goal (forward) · Mechanism · Files (§Bounds rows) · Sub-gate (backward, evidence-bearing). Every unit's receipt is appended to `execution/B/KF-W11.md` with `SERVED MODEL: <id>` on its first line, the commands it ran and their outputs double-run, the SELF-COUNT of every figure it publishes, and the inherited-paths clause (CRASH-RECOVERY law). **No unit writes outside its §Bounds rows; an out-of-bounds need is an ESCALATION returned, never a write.**

### KF.W11.a — The Cube Packet (phase 1)
- **Goal**: the die's pre-start orientation writer paints, the Roll stack and the lighting frame are one repair each, the matrix editor has a model behind its field, the four phantom `variant:` props are gone in one edit, and the OD latch family adopts the demo's one keyboard registry.
- **Mechanism**: P1 in the home records' internal order — CubeTarget #53 demo half first (the `Matrix3dCall` object serialized before `transformTargetsStyle`), the Roll stack, the latch/pointer seam, lighting truth as a graph-attitude *parameter*; CubeScene's matrix-editor family with ME-30/31's sync-topology spec and ME-42's edit representation stated in the receipt, state/identity, the four-site glass-conformance commit (carving `SpringScene.vue:202`/`:225`), ARB-1's delete arm only; OrbitalDrag's four families with the latch family's window listeners replaced by registry adoption + blur clear + `reset` exposed (LAW A census §B.3(3)).
- **Files**: the `.a` rows of §Bounds; `test/demo/scenes/cube-roll-and-prestart.test.ts` (create); `cube-scene.test.ts` (extend).
- **Sub-gate**: G-KFW11-1 GREEN (runtime + both byte clauses); `git diff --stat` touches only `.a` rows; the receipt names #7's library half as DECLARED to KF.W5 with the edge's anchor, and prints the OD latch-family sha KF.W12's AXISLINE-UNIT opens on.

### KF.W11.b — The Square Packet (phase 1)
- **Goal**: a keyframes-panel edit no longer destroys the square's custom renderer, the playback authority is singular, the colour write paints, the tether is drawn in a frame that exists, and the tour is paced by the spring's settle as its docblock says.
- **Mechanism**: L-2's demo half at `useKeyframeOps.ts:58-69` (re-supply the receiver's renderer on `updateFromString`; `usesDefaultRenderer(frame.transform)` is the assertion's oracle); the `RAFPlayback` rider; the playback-authority packet (the sole `animationGroup.pause()` moved behind the machine, 1 → 0); the colour packet with the `:29` docblock corrected; the instrument packet with N-SQ-4's ordered cure after OP-3's re-derivation; the input/a11y packet (grabDx via `7225cdd1`'s shape, read not copied; the bare `focus-ring` at `:46`); geometry; hygiene; MISS-3's settle-paced tour with its assertion in the same commit; SquareInstrument's six commits in the record's order, D-2's owner ruling consumed or its absence stated BEFORE any layer is touched (OP-7).
- **Files**: the `.b` rows; `test/demo/scenes/square-editor-seam.test.ts` (create); `square-scene.test.ts` (extend).
- **Sub-gate**: G-KFW11-7 GREEN (all clauses); the `useKeyframeOps.ts` diff is the `updateFromString` hunk alone (its sha recorded for KF.W12's KFED-UNIT to open on); the receipt lists which SquareInstrument rows OP-7 gated.

### KF.W11.c — The Spring Packet (phase 2)
- **Goal**: the derby is an honest instrument — one dispatcher of play-intent, a marker that overshoots where the spring does, a trace on `.fn`, no race that unmounts the overlay.
- **Mechanism**: P3 in order — C-1 first (the contract head sizes the rest), M-2/D-2/D-7 with the prose corrections re-anchored, M-4→M-3 with its LAW A census, M-5, N-1, the gesture spec, the a11y family (+ `:63`'s bare `focus-ring`), the register/flag passes, hygiene; SpringScene's KF-SS-1/-2/-3/-8(scene half)/-10(witness) and the sweep; the two `variant:` lines inherited landed from `.a`.
- **Files**: the `.c` rows; `test/demo/scenes/spring-derby-truth.test.ts` (create — cases (a)–(d)).
- **Sub-gate**: G-KFW11-3 GREEN; the M-3 commit sha recorded (`.e` opens on it); KF-SS-8's ribbon-side half named to KF.W13 in the receipt; KF-SS-3's API signal named to KF.W5.

### KF.W11.d — The Sequence Packet (phase 1)
- **Goal**: the sequence scene has one canonical time domain, one gesture spec, and an affordance edit whose before/after KF.W9 already holds.
- **Mechanism**: P2 — **the canonical-domain decision written in the receipt before any of the four patches** (M-10; K-25), the loop-seam commit, SC-2's decision, the reel packet, the geometry commit after OP-3's re-derivation, ST-4's one binding, ST-1's one word + one deletion (`SequenceTarget.css:175-178`, LAW A census §B.3(1)), the N-14-sequenced valuetext, the scrubber's gesture spec (`applyScrub(p, {gesture})`), the prose sweeps, the store hygiene, the Axis trio and tokens.
- **Files**: the `.d` rows (including `SequenceScrubber.vue`, first writer); `test/demo/scenes/sequence-instrument-truth.test.ts` (create); `sequence-scene.test.ts` + `sequence-scrubber-mount.test.ts` (extend).
- **Sub-gate**: G-KFW11-2 GREEN (all clauses); the commit sha `.i` opens on is in the receipt.

### KF.W11.e — The Spring-Plot Packet (phase 3; after `.c`'s M-3 commit)
- **Goal**: the trace plots what the spring computes, in a unit the axis names, drawn by one route.
- **Mechanism**: P4 — the N-1 fork decided in the receipt before any byte, the draw route per OP-4 re-measured at open, the parser posture and L-10's extraction+test in the same family; mark-design; register pass; N-5; L-14.
- **Files**: `SpringTrace.vue`; `test/demo/scenes/spring-trace-truth.test.ts` (create).
- **Sub-gate**: G-KFW11-4 GREEN; the receipt prints OP-4's grep at its own clock.

### KF.W11.f — The Spring-Physics-Facet Packet (phase 3)
- **Goal**: the heatmap encodes one stated thing on stated axes with a legend, the facet's presets overshoot where the physics does, and the field round-trips.
- **Mechanism**: P5 — the instrument-truth design decision written in the receipt first; the input-integrity core; the two-`defineModel` contract; the ramp redesign; SPF-3's remap; SPF-4's port-or-partial with the producer rider to SS-6; SPF-5's mix; SPF-7's two deletions; SPF-13 spent only on KF.W12's KF-CO-47 decision; L-m-5's three tests.
- **Files**: `SpringHeatmap.vue` · `SpringPhysicsFacet.vue`; `test/demo/scenes/spring-heatmap-reversibility.test.ts` (create).
- **Sub-gate**: G-KFW11-5 GREEN (both clauses).

### KF.W11.g — The Spring-Artifact-Truth Packet (phase 2)
- **Goal**: the starting-style artifact tells the truth about its endpoints, polarity, duration and case, and has one copy verb with a state and a refusal surface.
- **Mechanism**: P6 — the artifact core as one spec, KF-SST-1·6's survivor with `aria-expanded`/`aria-pressed`, KF-SST-2, -10 (OP-5), -31, prose, hygiene.
- **Files**: `StartingStyleTarget.vue`; `test/demo/scenes/starting-style-artifact.test.ts` (create).
- **Sub-gate**: G-KFW11-6 GREEN (both clauses).

### KF.W11.h — The Amiga Packet (phase 2; after `.b`)
- **Goal**: a paused amiga stage shows the scrubbed pose, not HOME, and its shared-idiom borrow is taken from a cured exemplar.
- **Mechanism**: P8 — D-1's single read of `pose` outside the `playing` branch with the doubled gate undone (`onFrame`/`setProgress` marking dirty), then the packet in the record's order; D-2's borrow verified against `.b`'s landed SquareScene a11y cure, not against the bank.
- **Files**: the `.h` rows; `test/demo/scenes/amiga-paused-pose.test.ts` (create); `amiga-sphere-spin.test.ts` (extend).
- **Sub-gate**: G-KFW11-8 GREEN; every id in P8 LANDED or KILLED-with-rationale in the receipt.

### KF.W11.i — The Drag-Seam Packet (phase 3; after `.d`)
- **Goal**: two pointers on any drag seam never strand the page's select-suppression token, and the machine is written at a rate it owns.
- **Mechanism**: P9 — the KF-AV-28 KEEP verdict STATED before the first byte (zero discharge receipts); the ONE guard family in `useDragScrub.ts` (re-entrancy + `pointerId` latch + disposal release + docblock) and the same family in `useDragCapture.ts` (its docblock corrected), one commit; C·C-1/KF-AV-16's one throttle-vs-decouple decision at the machine seam (the AV carve); KF-SCR-5's cleanup; the born-RED two-pointer test over both composables.
- **Files**: the `.i` rows; `test/demo/instrument/drag-scrub-reentrancy.test.ts` (create).
- **Sub-gate**: G-KFW11-9 GREEN (both clauses); the receipt quotes the KEEP verdict record it sequences after and states `useDragCapture`'s exported surface unchanged for KF.W13.

### KF.W11.j — Close (phase 4; serial, last)
- **Goal**: the wave's record is whole, its commits are audited to §Bounds, its producer rows have sailed, and its row in the LEDGER moves.
- **Mechanism**: re-run all ten gates at this seat's own commands (double-run); audit every commit sha against §Bounds by `git show --stat`; write the BH relay entries for every producer row surfaced (CubeScene D-2/C-3's `variant`, SPF-4's `role`/`aria-pressed` filter, LP-class Boolean-cast defaults if met) to the SS-6 accretion register; the E13 four-path mail sweep; the LEDGER row cells + event line; the residuals and escalations; zero SWAP-discharge receipts stated positively.
- **Files**: the value.js rows of §Bounds.
- **Sub-gate**: G-KFW11-10 GREEN; the LEDGER row reads what the gates say and nothing more.

### Standing law for every unit

CRASH-RECOVERY first (`git status --porcelain` in every repo you may write, own-set paths only) · the spec GOVERNS: no quick fix, no workaround, no masking fallback (no `try/catch` around a defect, no `test.skip`, no allowlist, no copied producer selector, no `node_modules` patch — each a HIGH defect) · idiomatic root-cause cures · pathspec commits, one per meaning, families unsplit · E-3 · WRITE-THEN-MEASURE, double-run · SELF-COUNT · quote-by-command · sibling trees READ-ONLY, glass-ui READ-ONLY always (producer rows → SS-6) · no stash/reset --hard/force-push · E13 · probe parsimony (§5.2 — SS-13 shared captures keyed by apotheosis row id, never free-form) · `scripts/dev/dev.sh` never staged · LEDGER edited only by minimal in-place cell replacement or appended lines.

---

## §Gates — ten, all born RED at `69095552`, every command literal, every figure double-run

**SELF-COUNT (LAW D(3))**: ⟨cmd⟩ `grep -c '^\*\*G-KFW11-' KF-W11.md` → **10**. Runtime gates are `vitest` runs (WAVE_SPEC: *no grep-only runtime gates*); byte clauses ride beside them, never alone. `RED today` prints this seat's measurement; `GREEN when` is the acceptance form. All commands run from `/Users/mkbabb/Programming/keyframes.js`.

**G-KFW11-1 — cube: the pre-start orientation paints; the Roll stack, the lighting frame and the latch family are one repair each; the four phantom props are one edit.** ⟨cmd⟩ `npx vitest run --project demo test/demo/scenes/cube-roll-and-prestart.test.ts` → RED today: `No test files found` (⟨cmd⟩ `ls test/demo/scenes/cube-roll-and-prestart.test.ts` → `No such file or directory`, twice). GREEN when the file exists and passes with cases: a pre-start drag → the die's computed `transform` is a `matrix3d(...)` string (#53); the Roll stack's four defects each asserted (#1·#2·#5·#15·#20·#38·#49); `useCubeRelit` receives the graph attitude (#56 — `0.6 → −0.6` alone fails); a >2-dp cell round-trips through an edit with full precision (ME-42) and a `-1000` value is readable on the selected cell (ME-43); the OD latch clears on blur. Byte clauses: ⟨cmd⟩ `grep -c 'useEventListener(window, "keydown"' demo/scenes/cube/orbital-drag/OrbitalDrag.vue` → **1** · **1** (must read **0**); ⟨cmd⟩ `grep -rc 'variant:' demo | grep -v ':0'` → **`CubeScene.vue:2` · `SpringScene.vue:2`** (must read nothing — 4 → 0, **all four in ONE commit**, `git show --stat` naming both files).

**G-KFW11-2 — sequence: one canonical domain; ST-1 and ST-4 landed.** ⟨cmd⟩ `npx vitest run --project demo test/demo/scenes/sequence-instrument-truth.test.ts` → RED today: `No test files found`. GREEN when it passes the N-10 obligation (the three-rect equality; the axis-terminal-label = duration), N-14's valuetext asserted in the same run after N-1, and ST-4's binding (`aria-busy` present while `isReeling`); the canonical-domain decision is quoted in the receipt **before** the family's first commit. Byte clauses: ⟨cmd⟩ `grep -c 'focus-ring' demo/scenes/sequence/SequenceTarget.vue` → **0** · **0** (must read ≥ **1**); ⟨cmd⟩ `grep -c 'box-shadow: var(--focus-ring-shadow)' demo/scenes/sequence/SequenceTarget.css` → **1** · **1** (must read **0**) — **both in ONE sha** (L-9).

**G-KFW11-3 — spring: the four born-RED derby cases.** ⟨cmd⟩ `npx vitest run --project demo test/demo/scenes/spring-derby-truth.test.ts` → RED today: `No test files found`. GREEN when cases (a) paused-entry rail-tap moves the ball, (b) first/last plotted x = 0/100, (c) the overlay survives race #2 at t≈1500 ms, (d) the settle pulse paints at `target` all pass. Byte clause: ⟨cmd⟩ `grep -c 'class="spring-rail stage-field-x focus-ring' demo/scenes/spring/SpringTarget.vue` → **1** · **1** (recorded; the a11y family's disposition of the bare class is stated in the receipt).

**G-KFW11-4 — spring-plot: instrument truth.** ⟨cmd⟩ `npx vitest run --project demo test/demo/scenes/spring-trace-truth.test.ts` → RED today: `No test files found`. GREEN when the extracted builder (L-10) is unit-tested against the engine's own samples and the axis unit matches the N-1 decision quoted in the receipt before the family. Clock clause, printed at open and at close: ⟨cmd⟩ `grep -c 'sampleNormalizedSpring\|resolveLinearStops' dist/keyframes.d.ts` → **0** · **0** today (the route taken is stated against this figure).

**G-KFW11-5 — spring-physics-facet: reversibility, tolerance, the mix table; the overshoot painter.** ⟨cmd⟩ `npx vitest run --project demo test/demo/scenes/spring-heatmap-reversibility.test.ts` → RED today: `No test files found`. GREEN when L-m-5's three cases pass and the preset-ball painter expresses the four presets' analytic peaks (1.005 / 1.068 / 1.205 / 1.000) or states its headroom geometry. Byte clause: ⟨cmd⟩ `grep -c 'clamp(values\[i\] ?? 0, 0, 1)' demo/scenes/spring/SpringPhysicsFacet.vue` → **1** · **1** (must read **0**).

**G-KFW11-6 — spring-artifact-truth: one honest artifact, one stateful verb.** ⟨cmd⟩ `npx vitest run --project demo test/demo/scenes/starting-style-artifact.test.ts` → RED today: `No test files found`. GREEN when the endpoints/polarity/duration/case-fidelity assertions pass and the refusal path is reachable. Byte clause: ⟨cmd⟩ `grep -c 'aria-expanded\|aria-pressed' demo/scenes/spring/StartingStyleTarget.vue` → **0** · **0** (must read ≥ **1**).

**G-KFW11-7 — square: the renderer survives a panel edit; one playback authority; the colour paints; the tether has a frame; the tour is settle-paced.** ⟨cmd⟩ `npx vitest run --project demo test/demo/scenes/square-editor-seam.test.ts` → RED today: `No test files found`. GREEN when, after `updateFromString` on the square's animation, `usesDefaultRenderer(frame.transform)` is `false` for the nested leaf; the takeover edge dispatches PAUSE through the machine; the tour test asserts settle-pacing; and the instrument renders against a subject travelling ±110 px without a `viewBox` (N-SQ-4). Byte clauses: ⟨cmd⟩ `grep -c 'setTimeout(step, 520)' demo/scenes/square/useSquareKeyboard.ts` → **1** · **1** (must read **0**); ⟨cmd⟩ `grep -rc 'animationGroup.pause()' demo | grep -v ':0'` → `SquareScene.vue:1` · `SquareScene.vue:1` (must read nothing); ⟨cmd⟩ `sed -n '29p' demo/scenes/square/useSquareDemo.ts | grep -c ':161'` → **1** · **1** (must read **0** — the docblock names the line the write occupies); ⟨cmd⟩ `grep -c 'viewBox' demo/scenes/square/SquareInstrument.vue` → **0** · **0** (must STILL read **0**). Witness census: ⟨cmd⟩ `grep -rl 'usesDefaultRenderer\|adoptCompiled' test/demo | wc -l` → **0** · **0** today.

**G-KFW11-8 — amiga: a paused stage shows the scrubbed pose.** ⟨cmd⟩ `npx vitest run --project demo test/demo/scenes/amiga-paused-pose.test.ts` → RED today: `No test files found`. GREEN when a paused scene + `setProgress(t)` renders `pose`, not HOME, and D-2's borrow is asserted against `.b`'s landed cure.

**G-KFW11-9 — drag-seam: two pointers, one token, both composables; one machine-write policy.** ⟨cmd⟩ `npx vitest run --project demo test/demo/instrument/drag-scrub-reentrancy.test.ts` → RED today: `No test files found`. GREEN when a second `pointerdown` during a drag leaves `activeGestureCount` at exactly 1 and the last `pointerup` returns it to 0, **for `useDragScrub` AND `useDragCapture`**, an unmount mid-drag returns it to 0, and the machine sees ≤ 1 dispatch per animation frame under a scripted 240 Hz pointer stream. Byte clauses (RECORDED, not acceptance — the runtime clause decides): ⟨cmd⟩ `grep -c 'acquireSelectSuppression()' demo/composables/useDragScrub.ts` → **1** · **1**; ⟨cmd⟩ `grep -c 'acquireSelectSuppression()' demo/components/instrument/transport/composables/useDragCapture.ts` → **1** · **1**; ⟨cmd⟩ `grep -c 'pointerId' demo/composables/useDragScrub.ts` → **1** · **1** (the `setPointerCapture(e.pointerId)` at `:120` — a mention, NOT the latch; a byte count cannot stand in for the two-pointer run).

**G-KFW11-10 — close: the demo project is green and the head still holds.** ⟨cmd⟩ `npm run test:demo 2>&1 | tail -3` → RED today by construction (nine absent files are nine failing runs once they exist; the existing 30 demo files' verdict is the baseline, pasted at open). GREEN when every `test/demo/**` file passes with no `test.skip`/`.only` in the wave's diff (⟨cmd⟩ `git diff <open-sha>..HEAD -- test | grep -c 'test.skip\|it.skip\|\.only('` → must read **0**), every id in §Carry reads LANDED / KILLED-with-rationale / carried, **zero** `DISCHARGED by KF.W7 SWAP verdict` strings in the record (⟨cmd⟩ `grep -c 'DISCHARGED by KF.W7 SWAP verdict' execution/B/KF-W11.md` → **0**), and the head is unmoved: ⟨cmd⟩ `npx vue-tsc --noEmit -p tsconfig.json 2>&1 | grep -c 'error TS'` → must read **0** (OP-0's own figure, re-run at close).

**GREEN-BEFORE-CURE (R.2)**: a gate found GREEN at open without this wave's cure is recorded as such, its cause named (a sibling landed it), and the packet row it governs is booked `LANDED-BY <sha>` — never re-cured, never claimed.

---

## §Sequencing — locks and cross-edges

### Intra-wave order (binding)

1. **`.a` ∥ `.b` ∥ `.d` (phase 1) → `.c` ∥ `.g` ∥ `.h` (phase 2) → `.e` ∥ `.f` ∥ `.i` (phase 3) → `.j`.** Peak 3.
2. **`.e` after `.c`'s M-3 commit** — the trace's prop surface is SpringTarget's row.
3. **`.i` after `.d`** — same file (`SequenceScrubber.vue`); the drag-seam family lands on the gesture spec, not under it.
4. **`.i` after KF.W7's SWAP verdict** — the KF-AV-28 lock; satisfied (`4c03ceda`, 2026-09-18, KEEP ×6) and named regardless; `.i` states it before its first byte and emits zero discharge receipts.
5. **Every geometry commit after OP-3's re-derivation** at the unit's open sha, in its receipt.
6. **`.c` after `.a`'s glass-conformance commit** (the two `SpringScene.vue` lines) — phase order guarantees it; the sha is named in `.c`'s receipt.
7. **`.h` after `.b`** — kf-AmigaScene D-2 banks SquareScene as the exemplar; a borrow from an uncured exemplar is the defect the row exists to prevent.
8. **The decision precedes the patch, three times**: `.d`'s canonical domain, `.e`'s N-1 fork, `.f`'s field encoding — each written in the receipt before its commit family opens (*"one spec, not four patches"*, the banks' own words).

### The travelling locks (register row 1 — four of the six ride here; all six accounted)

- **KF-AV-28's standing supersession rider** — on the drag-seam limb; verdict KEEP; packet un-gated; rider retained as record; the discharge alphabet has zero members.
- **LP-1's write→render ordering** — KF-CO-1/KF-CO-8's cures are sequenced by it **in their owning wave (KF.W12's OPTIONS-UNIT)**; this wave's spring KF-SS-8 scene-half touches no ChannelOptions byte and inherits nothing from LP-1 except the knowledge that a ribbon-side `:max` is not live until KF.W12 lands the edge (OP-6).
- **Comment-stated invariants are test obligations** — the kf-SquareScene MISS-3 law, measured relocated to `useSquareKeyboard.ts:20`/`:66`; binds every docblock a unit touches (the `:29` docblock in P7; the DFA-test-vs-comment assertion in P3; the "Nesting-safe" docblock and `useDragCapture.ts:20`'s false `tryOnScopeDispose` claim in P9).
- **Glass-producer rows → SS-6, never demo-side hacks** — CubeScene D-2/C-3 (consumer half ours), SPF-4's producer filter, any Boolean-cast `default: undefined` ask: relayed at `.j`; a demo-side workaround for a producer defect is a HIGH defect.
- **MbabbMenu MUST-CARRY · TD-1/TD-2 bundling** — KF.W13's; not this wave's surfaces; named for the count.

### Cross-edges, declared from this end

| to | type | declaration |
|---|---|---|
| **KF.W4** | **DEPENDS ON (hard) — the sequencing head** | G-KFW4-1 GREEN before any unit opens; measured RED (54 · 54) at this seat. |
| **KF.W7** | **DEPENDS ON (met); INBOUND ONLY** | the SequenceScrubber and AnimationVisualizer KEEP verdicts (`4c03ceda`) un-gate P9; `7225cdd1` (grabDx) is P7 MISS-1's precedent; `43556828` (C·C-4) is P9's landed provider guard; `8cfee94e` holds L·D-12's six cases. W7 promised no reciprocation; none is needed — this end reads the verdict table and *"What the two gated successors read here"*. **KF-AV-17's resize-immune form** is a KF.W7-carried AV row (its residual 9), NOT in this packet — recorded as adjacent, never re-booked (§Excluded). |
| **KF.W8** | **DEPENDS ON (met)** | its colocation moves are the reason P7's MISS-3 lives in `useSquareKeyboard.ts` and the card seam sits where KF.W12 finds it; every anchor here is post-W8. |
| **KF.W6** | **DEPENDS ON (met)** | the `kf-focus-ring` rename (`a6418729`) is why exactly two bare `.focus-ring` sites remain and both are this wave's (§B.1 row 5). |
| **KF.W12** | **SUPPLIES (two commits) + SHARES ONE PATH SERIALLY + WAITS ON ONE DECISION** | (1) `.a`'s OD latch-family commit is KF-AX-1's upstream half — AXISLINE-UNIT opens on its sha; (2) `.b`'s `useKeyframeOps.ts` L-2 carve precedes KFED-UNIT's open on that file; (3) SPF-13 is decided at OPTIONS-UNIT (KF-CO-47) and spent here. Both shas are printed in this wave's record; W12 writes neither `orbital-drag/**` nor the `updateFromString` hunk; **CubeAxisLines #31/#57 are booked here and cured there.** |
| **KF.W13** | **SEAM (named both ends)** | KF-SS-8's scene half here, its ribbon half is W13's C-2 time-space contract; the spring packet is *"sequenced with the transport/ribbon packet"* (ordering declared, bytes W13's); **`useDragCapture.ts` is written by `.i` with its exported surface unchanged — `PlaybackRibbon.vue` (W13's) is a consumer W13 re-measures at its open**; the two bare `.focus-ring` sites are this wave's files and W13 states the policy. Neither blocks the other's open. |
| **KF.W5 / KF.W8 (library)** | **DECLARES (not owned)** | CubeTarget #7's library diagnostic; SquareScene L-2's `adoptCompiled` half; SpringTrace's `sampleNormalizedSpring`/`resolveLinearStops` export (W7 C-3: warranted); KF-SS-3's `.fn`-without-`.css` API signal. Each row lands its demo half here and names the library half by id. |
| **KF.W9 / SS-13** | **DEPENDS ON (witnesses)** | ST-1's before/after (KF.W9 holds them, lands no cure); SS-13 #4/#5/#6/#10 witnesses named in P2/P4; SPF-3/-5's delivered-pixel cells; every packet's visual residue rides by apotheosis row id under one shared capture each (§5.2). |
| **KF.W10** | **REGISTERED AT** | `§6.D` row 1 is this wave's terminal disposition for the X·KF close; this file changes that verb by nothing — the register records, the authoring block authors, a later close stamps. |
| **SS-6 (BH relay)** | **SUPPLIES** | the producer rows at `.j`. |

---

## §Excluded

| excluded | reason |
|---|---|
| **Any `src/**` byte** — CubeTarget #7, SquareScene L-2's `adoptCompiled` half, SpringTrace's export, KF-SS-3's API signal | library waves (KF.W5 / KF.W8); declared at §Sequencing, never written here; a demo-side shim for an engine-seam defect is a masking fallback. |
| **`CubeAxisLines.vue`** and every AXISLINE-UNIT row | KF.W12; this wave supplies KF-AX-1's upstream half only and books #31/#57 as findings. |
| **The eight NO-WAVE-OWNER packets not in this partition** — CARD · OPTIONS · KFED · EDITOR · APPLY · AXISLINE (KF.W12) · dock-menu · transport/ribbon (KF.W13) | R-15's partition; no re-booking (register: *"no successor packet re-books a row homed here"*). |
| **KF-APP-1/-17's same-motion lock** | homed inside an authored wave; P1 consumes ARB-1's delete arm only. |
| **KF-APP-41 / kf-EditorShell C-22's orphan-token limb** | `ADOPTED-BY-KF.W6` (RULINGS-4 R4-1; register row 2's ANTI-RE-BOOK). |
| **KF-AV-10/-13/-14/-17/-24/-32 and every AnimationVisualizer row other than the KF-AV-15/-16 twins** | KF.W7-carried NO-WAVE-OWNER under KEEP; the twins ride P9 by the record's own identity line; **KF-AV-17** (the overshooting `translateX(calc(p*100cqw))` at `SequenceScrubber.vue:34`, measured live) is W7's residual 9 and stays where W7 carried it. |
| **L·D-12's already-landed cases** | `8cfee94e` (KF.W7 `.e`); P2 adds only the gesture-spec cases. |
| **`PlaybackRibbon.vue` · `TransportDock*` · `playback-idiom.css` · `design-idioms.css` · `demo/app/**`** | KF.W13's — dock-menu and transport/ribbon. kf-PlaybackRibbon N-3's `playback-idiom.css` deletion is KF.W13's and is named here only so no sweeping seat adopts it. |
| **`demo/scenes/easing/**`** | no easing packet exists in the canonical seventeen; `EasingSidebar.vue`'s seat rows are KF.W12's OPTIONS-UNIT carve (KF-ES-12 → KF-CO-17). |
| **The KF-AV-28 rider's discharge alphabet** | KF.W10's; this wave emits **zero** `DISCHARGED by KF.W7 SWAP verdict <surface>, <date>` receipts because KF.W7 emitted zero and the set is empty. |
| **The retired-gate prose radius** (register row 1's blast-radius receipt) | measured at `69095552`, reproduced — ⟨cmd⟩ `git grep -l 'proof:sequence-rows-draggable' HEAD -- docs | cut -d/ -f3 | sort | uniq -c` → **13 H · 3 J · 1 K · 1 S · 1 U** (19 files); the K row: `docs/tranches/K/audit/ci-cd-k.md:73`; **a deleting seat must meet the K row's historical CI-gating evidence** — this wave deletes no `proof:*` prose (the `proof:` family is KF.W4/KF.W9's) and records the radius as re-measured. |
| **Amending RULINGS-4's `.focus-ring` / `btn-playback` censuses** | E-3 — dated authorities are immutable; §B.1 row 5 is a frontier reading beside the fixed figure, with its command. |
| **Owner rulings** (SquareInstrument D-2's layer sign-off, OP-7; OD-V5) | never proxied; `complete_with_misses` with the ruling named if unruled. |
| **Re-auditing any of the 19 records** | the registry's — dispositions are ADJUDICATED; ids are for life; folds are by reference at the banked id. |

---

## §Commit plan

One commit per meaning; every family the home record declares lands unsplit; pathspec on the commit itself; `Claude-Session:` trailer per the standing law. Expected roster (keyframes.js unless noted); **families marked MUST NOT SPLIT are one sha each**:

1. `.a`: `fix(kf/cube · X.KF.W11.a · #53+#7 demo half)` · `fix(kf/cube · X.KF.W11.a · roll stack + lighting frame contract)` (both born-RED assertions) · `feat(kf/cube · X.KF.W11.a · matrix-editor family + ME-30/31 sync topology; ME-42 edit representation stated)` · `fix(kf/glass-conformance · X.KF.W11.a · the four phantom variant: sites, one edit)` (**MUST NOT SPLIT** — CubeScene ×2 + SpringScene ×2) · `fix(kf/cube · X.KF.W11.a · OD latch family → registry)` (the sha KF.W12 `.f` opens on) · `chore(kf/cube · X.KF.W11.a · hygiene tail)` · `test(kf/cube · X.KF.W11.a · G-KFW11-1)`.
2. `.b`: `fix(kf/square · X.KF.W11.b · L-2 demo half — updateFromString re-supplies the renderer)` (the sha KF.W12 `.c` opens on) · `fix(kf/square · X.KF.W11.b · one playback authority)` · `fix(kf/square · X.KF.W11.b · colour packet + the :29 docblock)` · `fix(kf/square · X.KF.W11.b · tether in px-space — N-SQ-4)` (D-1 + D-6 + D-16 + N-SQ-4, **MUST NOT SPLIT**) · `fix(kf/square · X.KF.W11.b · MISS-3 settle-paced tour + assertion)` (**MUST NOT SPLIT**) · the remaining SquareInstrument commits by name · `test(kf/square · X.KF.W11.b · G-KFW11-7)`.
3. `.d`: `feat(kf/sequence · X.KF.W11.d · the canonical-domain decision + instrument-truth core)` (N-1 ∥ N-2 against each other; N-14 after N-1) · `fix(kf/sequence · X.KF.W11.d · loop seam)` · `fix(kf/sequence · X.KF.W11.d · geometry after D-19)` · `fix(kf/sequence · X.KF.W11.d · ST-1 one word + one deletion)` (**MUST NOT SPLIT**, L-9) · `fix(… ST-4)` · `feat(kf/sequence · X.KF.W11.d · applyScrub gesture spec)` · `test(kf/sequence · X.KF.W11.d · G-KFW11-2)` (the sha `.i` opens on).
4. `.c`: `fix(kf/spring · X.KF.W11.c · C-1 contract head)` · `fix(kf/spring · X.KF.W11.c · honest instrument M-2/D-2/D-7)` · `refactor(kf/spring · X.KF.W11.c · M-4→M-3 trace onto .fn — parser, guards, regex die together)` (the sha `.e` opens on) · `fix(kf/spring · X.KF.W11.c · M-5 race)` · `test(kf/spring · X.KF.W11.c · G-KFW11-3 (a)–(d))` · the sweep.
5. `.e` · `.f` · `.g` · `.h`: one family per packet core (`.e`'s D-1 + N-1 + N-2 + C-2/L-3 **MUST NOT SPLIT**, the fork quoted before the route) + `test(…)` each; `.h`'s roster commit names every id LANDED or KILLED.
6. `.i`: `fix(kf/drag-seam · X.KF.W11.i · KF-SCR-1+L·D-1+KF-AV-15 — one guard family, both composables)` (**MUST NOT SPLIT**) · `fix(kf/drag-seam · X.KF.W11.i · C·C-1≡KF-AV-16 one machine-write policy)` · `test(… G-KFW11-9)`.
7. value.js: `docs(x-kf-w11/<unit>): receipt` per unit; `docs(x-execution/ledger): KF.W11 row` at `.j`; the SS-6 accretion + INBOX append at `.j`; `evidence/W11/**` with each unit's receipt.

## §Format and lint cadence · §Verification artefacts

`npm run check` (vue-tsc + tsc test config + `proof:structure`) and `npm run test:demo` after each unit's last commit and at close; `npx eslint demo/scenes demo/composables demo/components/playback demo/components/instrument/transport/composables` at close; `git diff --check` on every commit. Artefacts saved at close in `execution/B/KF-W11.md` and `evidence/W11/**`: the ten gate transcripts (double-run), the commit roster with `git show --stat` per sha, the OP-4 grep at open and close, the D-19 re-derivation tables (P1 latch seam · P2 geometry · P7 tether), the three written-before-the-patch decisions, the BH relay entries' ids, and the E13 sweep line.

## §L-18 rider (reference, not ceremony)

Passing all ten gates makes X.KF.W11 **IMPLEMENTED**; it never makes it **ACCEPTED**. Per L-18 the implementation must first survive two challenging gestalt passes and a fresh-Fable agglomeration. Likely bases against it, named so they are looked for: (i) a packet whose hygiene tail landed while its BLOCKER core slipped to a residual (the §Goal criterion's own failure); (ii) a geometry cure written off the banked figure instead of the re-derived one (OP-3); (iii) the drag-seam guard landed in `useDragScrub.ts` and not in `useDragCapture.ts` — two guards, or one — contradicting the record's *"one seam-packet spec covers both seams"*; (iv) a `test.skip` or a widened timeout used to turn G-KFW11-10 green (the masking class); (v) a `src/**` write smuggled as "the only way" for L-2 or #53 — that is an ESCALATION, and a seat that writes it has failed the wave; (vi) a decision inferred from its own patches rather than written before them (§Sequencing 8); (vii) a comment left true-sounding beside a cured line (the MISS-3 law's class); (viii) a producer defect cured demo-side.

**Dissents recorded, not resolved by this seat**: (1) whether the amiga packet's 22 rows warrant a split into a core (D-1/L-B1) and a tail unit — this file keeps one unit per the register's one packet; (2) whether the spring quartet is better run as one seat (the Opus arm's shape) — this file keeps one seat per packet so each packet's commit family and gate are its own, and pays for it with phase order rather than concurrency.

---

## §Provenance — the agglomeration record (M-23 §3 / M-25)

**Fold seat**: a fresh Fable instance that authored neither draft; both drafts read whole; every disagreement resolved at the register's bytes and the live tree at `69095552`, commands pasted above.

**Structure taken from the Fable arm**: the header block and verb table, §State, §0 OP table, per-packet units (10 seats), the vitest-based gate form with byte clauses beside runtime runs (WAVE_SPEC's *no grep-only runtime gates*), the six-lock accounting, the cross-edge table, §Commit plan, §Format/lint, §L-18. **Taken from the Opus arm**: the §B.1 frontier re-anchor table as the first section of the bounds (rows 1, 3, 5, 7, 8 are its findings, re-measured here), the LAW A census block (§B.3), the amiga-after-square lock (§Sequencing 7), the decision-precedes-the-patch lock (§Sequencing 8), kf-MatrixEditor's rows folded into the cube packet (its routing law at `:42`), ME-42/ME-43, the graph-attitude parameter framing of #56, the four-site `variant:` family as ONE commit, the playback-authority and `:29`-docblock clauses of G-KFW11-7, the `viewBox` prohibition in gate form, `demo/DESIGN.md` prose-only bounds, the `evidence/W11/**` write path, the write-authority sentence (settled by the landed X·KF roster, not carried as an OP).

**Disagreements, and the byte that settled each**: (a) **the drag seam's composable** — the Opus arm read `SequenceScrubber.vue:28` alone and called the seam *"bare, no `pointerId`"*; the tree reads `useDragScrub` at `:44`/`:87` and `setPointerCapture(e.pointerId)` at `useDragScrub.ts:120` — the Fable arm's reading holds, and `kf-AnimationVisualizer.md:55` names `useDragCapture` as KF-AV-15's subject, so the twin is a second composable (§B.1 row 4), which neither arm had bounded. (b) **`useKeyframeOps.ts`** — the Opus arm held it read-only and called L-2's demo half a library act; `kf-SquareScene.md:40` disposes the row to *"the square scene-repair wave's editor-seam packet"* and `updateFromString` is a demo byte at `:58`, so the Fable arm's carve stands, serial with KF.W12. (c) **ST-1's CSS coordinate** — `:163-167` (banked, Opus) vs `:175-178` (Fable): the tree reads `:177`. (d) **KF-AV-17 as a gate** — the Opus arm minted G-KFW11-15 on it; the row is KF.W7-carried under KEEP and outside the R-15 packet, so it is excluded with the adjacency recorded. (e) **the four `variant:` sites** — one commit (Opus) vs a rider to `.c` (Fable): the record calls it one edit; this file gives the commit to `.a` with a two-line carve of `SpringScene.vue`, serial before `.c`. (f) **unit shape** — 10 seats (Fable) vs 6 (Opus): kept at 10 for one family and one gate per packet, with the Opus arm's amiga lock imported. (g) **`.focus-ring` drift** — both arms measured 2 bare sites; this file assigns the edits to the owning packets (P3, P7) and the policy to KF.W13. (h) **write authority (Opus OP-4)** — not carried as HARD: KF.W7's commits landed on keyframes.js `master` under the same law this wave inherits.

**Killed from the Opus arm**: G-KFW11-15 (KF-AV-17, re-booking); the "kf-SequenceScene N-3 in `PlaybackRibbon.vue`" census row (the row is kf-PlaybackRibbon's, its bytes `playback-idiom.css`, its wave KF.W13); `useSpringDerby.ts`+`DESIGN.md` as a separate bounds row (folded); the `EasingSidebar` mention. **Killed from the Fable arm**: the `AnimationVisualizer.vue` carve covering "KF-AV-15" (the twin is `useDragCapture.ts`; the AV carve is KF-AV-16's alone); the `TransportDock.vue`-side reading of TD-36 (a comment hit; W13's concern). **Blind convergences banked**: both arms produced the same nine packet names, the same KEEP reading of the KF-AV-28 rider, the same MISS-3 relocation arithmetic (66 − 20 = 46), the same two bare `.focus-ring` sites, the same `variant:` denominator, and the same `4c03ceda`/`7225cdd1`/`43556828` inheritance.

---

## ADDENDUM 2026-09-19 (dated, beside — E-3; COHESION §0u) — OP-0 read as a RATCHET; unit `.r` added FIRST

**OP-0.** The open check is G-KFW4-1's WIRED half (the chassis on the merge path). The count (54 · 54 at `dd28da55`) is banked at open, may not rise, falls inside each unit's own §Bounds rows with the cure that owns the diagnostic (no cast, no `@ts-expect-error`, no disable), and reads **0 at KF.W13's close**. KF.W12 and KF.W13 read their own OP-0 rows under the same ruling.

### KF.W11.r The unowned remainder and the lockfile (FIRST; Opus)
- **Acts, in order**: (1) **R-C3** — `npm install --package-lock-only` so `package-lock.json` takes `@vue/test-utils@^2.5.1`; `npm ci` exit 0; commit `chore(lock): the lockfile takes the manifest` (pathspec `package-lock.json`), push, record the CI run id and its two jobs' outcomes. (2) Enumerate every `vue-tsc` diagnostic OUTSIDE the KF.W11–W13 §Bounds rows, by file; cure each at the **type level only**, citing the diagnostic; ESCALATE any that needs a behavioural change, naming its owning wave.
- **Files**: `package-lock.json`; exactly the files act (2)'s enumeration names (seat 0 lists them in the unit plan; none may be a W11–W13 §Bounds row).
- **Sub-gate**: `npm ci` exit 0 · CI run observed · the count after `.r` = the count before minus the remainder, double-run.

**§Sequencing addendum**: `.r` → the banked plan unchanged (`[.a ∥ .b] → [.d] → [.c ∥ .h] → [.g] → [.e ∥ .f] → [.i] → [.j]`).
