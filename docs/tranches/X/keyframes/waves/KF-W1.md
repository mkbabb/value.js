# X.KF.W1 - Mail Cure (the O-8/O-11 amendment-addendum, and our own sweep-path defect)

**Sub-tranche**: X·KF, **frontend half (SS-2)** ⟨COHESION §1 SS-2 — "glass suffusion · timeline evaluate · Safari visual · **mail cure** → KF.W1/W6/W7/W9"⟩. Spec home `docs/tranches/X/keyframes/waves/KF-W1.md`; authority `docs/tranches/X/COHESION.md` §0/§2/§3, ratified under **M-25**. Validated at authoring against `docs/precepts/instructions/tranche/WAVE_SPEC.md` (**L-20**).

**Status**: **planned**

**PROVENANCE (restated at repair round 2, under RULINGS-2 R2-1 item 6 / RULINGS R-12).** This spec is authored **from the 58-record adjudicated registry directly** (`docs/tranches/V/megatranche/registry/adjudicated/`, `ls kf-*.md | wc -l` → **58**, re-derived this seat) and from the dated in-tree formation documents named in §4 (`CENSUS-2026-08-03.md`, `lane-docs.md`, `INTAKE-ADJUDICATION-2026-08-03.md`, `INBOX.md`). **The routed denominator is the registry's; the CARRIAGE CLAIM is the conformance artifact's, never this spec's** ⟨**LAW B**, `conformance/PASS-3/RULINGS-3.md` — no wave spec asserts a completeness/zero-escape claim in its own voice⟩: census carriage per **`docs/tranches/X/keyframes/conformance/PASS-3/KF-W1-CHECK.md` §1 (2026-08-28)**, which re-enumerated the routing alphabet from the 58 records' bytes and found **6 routed · 6 booked · 0 escaped · 0 inventions** — the six rows standing across **five** records, all six the census-F-1 identity under the collided `KF.W1-DEP` token (§5 C-13; §8 row 5, whose *"all five rows"* cardinality that same check convicted at **D3-5** and which is corrected at §8 this round). That artifact also returned **DEFECTIVE — 1 BLOCKER · 5 MAJOR · 3 MINOR · 2 INFO** on this file's other four axes; §12 records the cures. When a newer pass supersedes it, this citation is **re-pointed, never re-asserted here**. Every earlier citation of an "SS-2 CARRY" as this spec's source is **STRUCK**: `find docs -iname '*CARRY*'` returns exactly one carry under `X/keyframes/` — `carry/KF-W6-CARRY.md`, which is **W6's** and per RULINGS **R-12** the ONLY in-tree X·KF carry. **No ledger that is not in the tree is cited as a source in this file again**, and no `N rows carried, 0 dropped` claim is keyed to one.

**Filename reconciliation (declared so the filename never becomes a second namespace schism).** Re-measured **2026-08-28, repair round 2, this seat**: `ls docs/tranches/X/keyframes/waves/` → **eleven** specs, this one and **ten** siblings — `KF-W0.md` · `KF-W2.md` · `KF-W3.md` · `KF-W4.md` · `KF-W5.md` · `KF-W6.md` · `KF-W7.md` · `KF-W8.md` · `KF-W9.md` · `KF-W10.md` — every one self-naming `KF-W<N>.md`. The sibling convention governs: this spec's home is **`KF-W1.md`** (PRESENT, 65,138 B at this repair's open), and `W1.md` is **never** created (`test -e W1.md` → ABSENT, re-run this seat). The superseded `waves/W1.md` spelling and its "parent directory ABSENT at 2026-08-25" reading are recorded here **as the readings corrected, never as a source**.

**Alias law (R-B idiom, inherited from `KF-W4.md`).** The sibling spelling `KF.W1` denotes `X.KF.W1`. It is a search alias, never a rename; `X.KF.W1.<x>` is the WAVE_SPEC §5 form and is authoritative.

**Second alias — LOAD-BEARING, and the one omission that would corrupt other waves' tables.** `KF.W1-DEP`, as written in the superseded `formation/keyframes/lane-frontend.md §10` lane taxonomy, **does not denote this wave.** Five adjudicated records carry a `KF.W1`-form token and every one of them means `KF.W1-DEP` — **eighteen anchors across SIX routing rows**, re-measured 2026-08-28 at repair round 3 (`grep -c 'KF\.W1-DEP' kf-*.md` → 4 · 4 · 3 · 4 · 3 = **18**). **Anchor completeness is certified at `conformance/PASS-3/KF-W1-CHECK.md` §1.1–§1.2 (2026-08-28), not here** (LAW B): that seat re-derived the decomposition from the bytes — **5** taxonomy-preamble + **6** routing rows + **5** ADJUDICATED-summary + **2** riders = **18** — and found it exact, per-file counts included. The anchors: ⟨kf-AnimationControlsGroup.md:17/:28/:47/**:135** · kf-ChannelControls.md:17/:56/:142/**:155** · kf-ControlsPaneWrapper.md:6/:60/**:149** · kf-DemoGlobalChrome.md:17/:43/:44/**:130** · kf-KfPillTabs.md:19/:46/**:122**⟩. The five bolded anchors are each record's ADJUDICATED-summary line, which also names `KF.W1-DEP` as an owning fold; a block whose purpose is anchor completeness states all eighteen, not the thirteen an earlier draft cited.

**ROWS vs RECORDS, stated because they differ and the difference was mis-carried (D3-5, cured at repair round 3).** The **six** routing rows are `kf-AnimationControlsGroup.md:28` (the F-1 severity row) **and `:47`** (the L-3/C-1 row that owns the `useControlsKeyboardShortcuts.ts:1` sole-binding-site rider) · `kf-ChannelControls.md:56` · `kf-ControlsPaneWrapper.md:60` · `kf-DemoGlobalChrome.md:44` · `kf-KfPillTabs.md:46` — **six rows across five records, because ACG carries two.** Re-derived this seat (`grep -n 'KF\.W1-DEP' kf-*.md`, every hit read and classified); independently re-derived at `PASS-3/KF-W1-CHECK.md` §1.2, which prints the same six ids. **C-13's "five" is correct — it counts RECORDS**; §8 row 5's superseded *"All five `KF.W1-DEP` / census F-1 **rows**"* counted rows and under-booked by one, dropping `:47` and its rider. Corrected at §8 this round; recorded here, not silently applied. The mapping, declared once and for all:

> **KF.W1-DEP ≡ census F-1 (the phantom `@mkbabb/glass-ui` dependency) ≡ X.KF.W0 §B-12 — and FALSIFIED-AT-HEAD per SCH-1. ZERO `KF.W1-DEP` rows land in X.KF.W1 · Mail Cure.**

Falsifier, re-measured by this seat 2026-08-28, **repo and substrate named in the command** (repair round 3): **in `keyframes-v-exec`**, `git grep -n '"@mkbabb/glass-ui"' origin/master -- package.json` → **`package.json:77: "@mkbabb/glass-ui": "7.0.0",`** — an EXACT devDep, present.

⟨**ROUND-3 CORRECTION — the THIRD member of the D3-6 silent-mis-resolution class, and the worst-shaped one, found by this seat's own full-surface sweep (R2-5 §(e)); stated not silently applied (E-3).** This falsifier read `… HEAD -- package.json` with **no repo named**, declaring the substrate only in prose. Run in the sacred checkout — which §4 puts in bounds as a read target and where OP-1's `ls` runs — bare `HEAD` is the DISQUALIFIED `8281638c`, and the command **does not fail**: it returns **`package.json:71: "@mkbabb/glass-ui": "6.0.0"`**. A different line, a different version, and a hit that reads as *confirmation*. Unlike G-KF1-2's legs, which return nothing and are merely uninformative, **this one silently answers the wrong question with a plausible answer** — the exact failure mode the SUBJECT-IDENTITY / substrate-naming discipline exists to kill. **Three substrates, three answers**, all measured this seat: sacred **worktree** → `grep -n '"@mkbabb/glass-ui"' package.json` returns **nothing** (glass-ui absent from the manifest — F-1's phantom-dependency claim, RED, exactly as the bank states it) · bare **`8281638c`** → `6.0.0` at `:71` · **`origin/master`** → `7.0.0` at `:77`, which is the reading that makes **F-1 FALSIFIED-AT-HEAD**. The wave's SCH-1 disposition therefore turns entirely on the substrate, and the substrate is now written into the command instead of into the sentence beside it.⟩ F-1's RED is true only of the sacred checkout ⟨KF.W0 §B-12 manifest schism: origin/master pins = installed; local HEAD `8281638c` stale — **every wave re-baselines against `origin/master`**⟩. The lane taxonomy's names are **not** renamed (anti-rename; the five records' routing text is banked and must stay citable) — the collision is disambiguated by declaration, not by editing the bank. F-1 is **never re-booked**: the identity guard the five records already hold ("booked once at the census; never re-booked per component") extends across the namespace boundary. ⟨kf-LayerConfigPanel.md:12 routes F-1/SCH-1 → KF.W0 §B-12 explicitly⟩

**Third alias — the `W1` namespace that is neither of the two above, and the reason "once and for all" now means what it says.** keyframes' own `docs/tranches/V/DISPOSITIONS.md` uses **`BUILD W1`** for *their* build wave. It is live in this corpus at **six anchors** ⟨kf-CopyButton.md:15 · :40 (**EE-01**, `DISPOSITIONS.md:69`) · kf-KeyframesEditor.md:10 · :40 (**FE-3**) · :51 (**EE-03**) · :140⟩ and **denotes neither X.KF.W1 nor `KF.W1-DEP`.** Nothing escapes and nothing is owed here: all three identities route explicitly — *"→ **FOLD to EE-01 + KF.W0**"*, *"→ **FOLD to FE-3 + KF.W0**"*, *"→ **FOLD to EE-03 + KF.W0**"* — and the surviving obligation in each case is **KF.W0's re-regression guard** (the stale fork's in-flight migration must not re-land the dead bytes over the frontier's repair), not this wave's. Declared because a seat grepping `W1` across the corpus meets these six hits, and a mapping that admits two of three live meanings is exactly the corruption this block exists to prevent. **Three namespaces, closed: `X.KF.W1` (this wave) · `KF.W1-DEP` (census F-1 → KF.W0 §B-12) · `BUILD W1` (keyframes' own DISPOSITIONS → EE-01/FE-3/EE-03 → KF.W0).**

**EXECUTION IS NOT AUTHORIZED BY THIS FILE.** Tranche X's execution gate stands: nothing here opens, writes, builds, or runs against product source until the **owner's begin-word**. This wave opens **no product source in either repo** — not one `src/` file, not one `.vue`. Specifying is not doing.

**SACRED-CHECKOUT LAW.** ⟨keyframes' own `docs/tranches/V/EXECUTION-HANDOFF.md:13-18` — "no mutating git, no npm operations there, ever"⟩ X.KF.W1 writes **NOTHING** under `/Users/mkbabb/Programming/keyframes.js/`. Every access to that tree is a read or a copy **out**.

---

## 1. State

**Name**: W1 - Mail Cure (the O-8/O-11 amendment-addendum, and our own sweep-path defect)
**Opens after**: the owner's begin-word. **Not** after X.KF.W0 — see §7 cross-edge 2: this wave is deliberately **completable without §B-12**, because the exec-clone delivery path exists precisely so the mail cure never queues behind the owner's hand. (KF-W0's authored spec claims the reverse edge; the conflict is RECORDED, not silently resolved — §7 cross-edge 2.)
**Agents**: 3 Opus units, **serial** (`.a` → `.b` → `.c`). No Fable seat: nothing here is adjudicated or designed — it is a delivery act with three re-measurements. Owner cap (max 4 concurrent) respected by construction.
**Hard gate**: **TEN hard conditions in §6 (G-KF1-1..G-KF1-10), plus one DECLARED-SATISFIED and one stay-GREEN INVARIANT** — the addendum exists at an exec-visible path · every anchor in it resolves at `origin/master` · the three missed `parseStylesheet` sites named with their failure postures · the "matches your tree exactly" certification withdrawn **against a truthfully named substrate** (C-6 / G-KF1-4, re-cut at repair round 3) · the vehicle question closed honestly · the D-GAP-6 conditional resolved to *not adopted* · I-26 CURED + **O-21** minted (the mint is `max+1` **measured at open**, never this literal — C-10's MINT LAW) · our own sweep-path law repaired · both outbound bodies retained in value's tree · the ledger mark ASKED (and declared non-gating). **NOT hard conditions, and never counted as such**: the spec home populated (**G-KF1-11**, DECLARED-SATISFIED) · the sacred checkout unchanged (**G-KF1-12**, stay-GREEN INVARIANT).
⟨**Round-3 correction, stated not silently applied (E-3; check D3-7).** This line read *"**twelve** conditions in §6"* and enumerated all twelve as hard. **STRUCK**: §6 declares G-KF1-11 *"NOT a close gate this wave passes"* and G-KF1-12 *"INVARIANT, declared, **not** born-RED"*, and §11's tally already read **10 born-RED · 1 DECLARED-SATISFIED · 1 stay-GREEN INVARIANT** — a condition explicitly declared not to be a close gate cannot be one of twelve hard-gate conditions. The header now agrees with §6 and §11 instead of re-inflating by two what the next paragraph deflates by two.⟩

**Born-RED tally at WAVE-OPEN — 10 · 1 · 1, not eleven (L-19).** **Ten** are born-RED with a named, literal witness that a seat can re-execute and watch fail: G-KF1-1..G-KF1-10. **G-KF1-11 is DECLARED-SATISFIED, not born-RED** — its condition ("the spec home is populated") was RED at the instant of authoring and is discharged by the existence of this very file; at wave-open it is already GREEN and **no act inside `.a`/`.b`/`.c` can turn it**. **G-KF1-12 is a stay-GREEN INVARIANT.** Both are labelled as such rather than dressed as born-RED: counting a self-satisfying condition as born-RED inflates the tally by one and dresses a fact as a test — the same contrivance G-KF1-12 was relabelled to avoid, and the identical treatment was owed to G-KF1-11.

### Four-verb status (separate fields; this file moves exactly one)

| verb | value | evidence |
|---|---|---|
| AUDITED | **YES** | `CENSUS-2026-08-03.md` §0/§0.5/§4/(c)5/(c)6 · `formation/keyframes/lane-docs.md` §0/§4/§6/§7.1/§7.2 · `INBOX.md` row **I-26** (at `:92` at this writing — the **row id** is the anchor) · `INTAKE-ADJUDICATION-2026-08-03.md` §3 |
| SPECIFIED | **YES — by this file, 2026-08-28** | authored from the **58-record adjudicated registry directly** (header PROVENANCE); the routed denominator is the registry's — **6 routed / 6 booked / 0 escaped**, re-derived this seat; the fourteen carry rows C-1..C-14 each name a ground re-opened at the bytes (§5); §8 states every non-carry with its reason; L-20 validated; witnesses re-measured at authoring and again at repair round 2 |
| IMPLEMENTED | **NO** | — gates green + the addendum landed at the exec-visible path stamps this |
| VERIFIED | **NO** | — stamped only at X·KF's close (KF.W10); no wave stamps VERIFIED at its own close |

**This wave is the DECLARED MAIL HEAD of X·KF.** ⟨COHESION §2, verbatim: "**I-26 mail cure (KF.W1) precedes any kf wave consuming O-8/O-11 obligations.**"⟩ ⟨census (c)6, verbatim: "**The mail cure precedes everything kf-side.**"⟩ **Reciprocity, measured 2026-08-28 over the WHOLE authored sibling set — all seven (`KF-W0` · `KF-W2` · `KF-W3` · `KF-W4` · `KF-W7` · `KF-W8` · `KF-W10`), per RULINGS R-19d.** An earlier draft measured four and enumerated seven; **the measured set and the enumerated-as-authored set are now the same set**, because a mail head that measures a subset of its consumers is measuring the wrong thing.

**CITATION IDIOM (RULINGS-2 R2-7, binding program-wide from this round).** Every cross-spec receipt below cites a **STABLE ANCHOR** — the destination spec's §-heading, gate id, OP id, or table-row label. Any line number rides **parenthetically, declared non-load-bearing**, resolved at this repair's clock (2026-08-28, round 2). Intra-spec line citations stand (they move with this file). Quotations are verbatim at their source at write time or say they are paraphrase (M-25).

- **KF-W4 — RECIPROCATED.** **§Preconditions OP-5**: *"OPEN — KF.W1's … the amendment-addendum must land before they are honestly citable"*; and **KF-W4 §Sequencing, the cross-edge table row `KF.W1 · Mail Cure`** → **PRECONDITION on anchor citability** (**numeric convenience STRUCK at round-2 close (U-2/R2-7)** — the round-2 spelling `:242` had drifted onto W4's NO-WAVE-OWNER packet-roster paragraph; **the §-heading + row label `KF.W1 · Mail Cure` are the sole anchor and no line number rides this receipt**. Round-1's `:206` was already dead and stays struck).
- **KF-W3 — RECIPROCATED.** **§Preconditions OP-5** UNLANDED · **L-4** · **KF-W3 §Cross-edges, the row `KF.W1 · Mail Cure (O-8/O-11) + E13`** → **DEPENDS ON** (**numeric convenience STRUCK at round-2 close (U-2/R2-7)** — the round-2 spelling `:220` had drifted onto W3's SS-6 BH-relay row; **the §-heading + row label are the sole anchor and no line number rides this receipt**. Round-1's `:209` was already dead and stays struck).
- **KF-W10 — RECIPROCATED, and it hard-blocks on this wave THREE ways** (each re-resolved this seat; the round-1 `:16`/`:50`/`:296` receipts were all dead). ⟨**KF-W10 §1 State, the `Opens after` line** — *"**Opens after**: **X.KF.W0** …, **X.KF.W1** (Mail Cure — the O-8/O-11 amendment-addendum on an exec-visible path), **X.KF.W9**…"* — ⟨line coordinate retired at round-2 close (U-2/R2-7); the §-heading + the `Opens after` label are the anchor⟩⟩ ⟨**KF-W10 §Preconditions, row `OP-3`** — *"**KF.W1's O-8/O-11 amendment-addendum is exec-visible.** … **UNLANDED.** … Terminalizing a ledger whose packets were never delivered records a **false close** (G-6 depends on this, not the reverse)."* — ⟨**numeric convenience STRUCK at round-2 close (U-2/R2-7)**; `:54` was blank at the current bytes — **the §-heading + the row id `OP-3` are the sole anchor**⟩⟩ ⟨**KF-W10 §Sequencing, cross-edge `B. → KF.W1 (Mail Cure, SS-2)`** — *"**W10 depends.** No coordination terminalization (G-6) before the O-8/O-11 amendment-addendum lands on an exec-visible path."* — ⟨**numeric convenience STRUCK at round-2 close (U-2/R2-7)**; `:332` was blank at the current bytes — **the §-heading + the cross-edge label `B. → KF.W1 (Mail Cure, SS-2)` are the sole anchor**⟩⟩ Re-measured this seat: `grep -c 'O-8' KF-W10.md` → **8**, `grep -c 'O-11' KF-W10.md` → **3** — real letter citations, not `KF-CO-8` substrings. KF.W10 is therefore precisely *"a kf wave consuming O-8/O-11 obligations"*, the class COHESION §2 says this wave precedes, and it is the **hardest downstream consumer this wave has**. Cross-edge **11** carries the edge.
- **KF-W0 — declares the REVERSE edge**, at two stable anchors re-resolved this seat (the round-1 `:269`/`:306` receipts were dead): **§Sequencing, the OUTBOUND table row `KF.W1 · Mail Cure`** — *"W1's re-delivery depends on this wave making them visible and on the re-anchored counts replacing the stale ones."* — and **§Excluded, the row `KF.W1's mail re-delivery`** — *"Depends on this wave, is not performed by it."* ⟨**numeric convenience STRUCK at round-2 close (U-2/R2-7)**: the round-2 spellings `:393`/`:431` had both drifted (onto a §Gates falsifier and a §Sequencing lock respectively); **the two §-headings + the two row labels are the sole anchors and no line numbers ride these receipts**. Round-1's `:269`/`:306` were already dead and stay struck⟩. Conflict RECORDED, never silently reconciled (cross-edge 2).
- **KF-W2 · KF-W7 · KF-W8 — no KF.W1 edge, and the conditional mark DISCHARGES on the grep.** Bare-`KF.W1` token count **0** in each (W2's `KF.W1` greps resolve to KF.W11/W13 prefixes). The COHESION §3.1 cohesion mark was raised *conditionally* — owed from their end **if** they cite any O-8/O-11 obligation — and the condition is now **measured, not asserted: FALSE.** `grep -c 'O-11'` → **0** in all three; every `O-8` hit is the `KF-CO-8` substring ⟨**KF-W2 §Excluded, the row `KF-CO-1 / KF-CO-8`** · **KF-W7 §Excluded, the row `KF-CO-1/KF-CO-8`** and its **N-10 SPEC-INPUT** paragraph — **all three now cited by §-heading + row label alone, **numeric convenience STRUCK at round-2 close (U-2/R2-7)****: W7's `:355`/`:111` still resolved at this sweep but ride no receipt from here on, and W2's `:428` had drifted onto its §Sequencing OP-2 item. Round 1's `:267` / `:103`/`:338` are dead and stay struck; the substring finding is unchanged at the current bytes (`grep -c 'O-11'` → 0 in all three; W2's single `O-8` hit is the `KF-CO-8` substring in that §Excluded row)⟩. **The mark discharges; nothing is owed from those three ends**, and W2 is no longer singled out for a property W7 and W8 share.

### Goal criterion

This wave succeeds if, when work ends, **every obligation value has placed on keyframes is readable by the agent that will execute keyframes' work, at anchors that resolve in the tree that agent has checked out, with every question value has since answered itself withdrawn in writing** — and if **value's own sweep-path law can no longer produce the same defect again.** I-26's own words are the bar: *"a letter that lands untracked in a frozen checkout is NOT delivered — the path, not the content, was the defect."*

**The letters were not wrong, and they did not mismeasure.** They were *unreachable*, and their certification **named no substrate a recipient could re-derive**. This wave fixes the path and the certification; it re-scopes nothing, and it relays **no accusation of mismeasurement** to the addressee repo. The operational payload — re-anchor at `origin/master`, plus the §5a drift table — is **untouched, because it was correct.**

⟨**ROUND-3 CORRECTION, stated not silently applied (E-3) · authority RULINGS-3 `R3-2`.4 + check `D3-2`.** This paragraph read *"they certified a tree they had not measured"* — **STRUCK as false at the bytes.** Re-derived this seat, read-only, in the sacred checkout's **uncommitted worktree**: **every** anchor §5a marks ABSENT or DRIFTED resolves there, at the packets' own coordinates — `compile/value-ast.ts:71` = `const parsed = parseCssValues(value);` · `compile/easing/easing-registry.ts:36` = the exact docstring · `compile/emit/backward.ts:47` = the `sampleColorRamp`/`deltaEOK` symbol-list docblock line O-11 `:158` cites · `emit/backward-color.ts:171/:250/:263` = the `sampleRamp` declaration and its two call sites, i.e. the *"`count=1024`"* evidence tuple O-11 `:175` cites, byte-for-byte · `resolve/browser.ts:165` = `const parsed = parseCssScalar(source);` · `package.json:69` = the `"@mkbabb/value.js": "4.0.0"` pin · `emit/easing-serialize.ts:71` = the `timingFunctionEntries.find(` reverse-map head, which the packet's `:70-71` span brackets from the preceding early-return. Nine of nine drifted-or-absent rows resolve in that worktree; eight at the cited coordinate exactly, row 4 as the bracketing span just described. The packets **measured a live tree precisely and certified it accurately**; what they never did was **name** it — and the tree they left unnamed is an uncommitted worktree that **no sha addresses**. For a wave whose entire product is a letter telling an external repo what went wrong, relaying *"your letters measured a stale tree and were off by one"* would be a **false statement about their tree**, sent by the wave that exists to stop false certifications leaving this repo.⟩

## 2. Open preconditions (checked at wave-open; NOT close gates)

| # | precondition | state at authoring (2026-08-28, this seat) | check at open |
|---|---|---|---|
| **OP-1** | **The source bodies are still readable.** Their only copies on this machine are untracked files inside a frozen sibling checkout. | **PRESENT** — `VALUEJS-INBOUND-2026-07-24-parser-totality-exposure.md` **7,064 B** (mtime Jul 24 16:41) · `VALUEJS-INBOUND-2026-07-27-library-band-r1-widened-k1-k4.md` **15,633 B** (mtime Jul 27 12:27), in `/Users/mkbabb/Programming/keyframes.js/docs/tranches/V/coordination/`, alongside **252** dirty porcelain entries. | Re-`ls` before anything else. **This is the wave's single largest liveness risk** (the X.P.W1 OP-2 class): one `git clean` in that tree destroys the addendum's own source text. Unit `.a` runs first for exactly this reason. |
| **OP-2** | **The exec clone is the frontier.** | **TRUE** — `keyframes-v-exec` HEAD = **`81a56990`** = kf `origin/master`, re-verified 2026-08-28. | Re-verify; record the sha *in* the addendum (G-KF1-1 green condition). |
| **OP-3** | **Write authority for `keyframes-v-exec`.** ⟨census (c)5: "keyframes.js is sibling-read-only under value.js law … K1–K4 and every kf-side wave have no owning formation on the kf side today"⟩ | **UNRESOLVED** — the same OP-1 that `KF-W4.md` names. Note the asymmetry: this wave writes **one documentation file into a coordination directory**, which is what a mail path is *for*; it does not touch their source. | The owner's word if the begin-word does not already carry it. |
| **OP-4** | **§B-12 reconciliation** (the sacred checkout 41 behind). | **NOT DONE** — `git rev-list --left-right --count origin/master...master` → **`41  1`**, HEAD `8281638c`, re-measured 2026-08-28. | **Explicitly NOT required.** See §7 cross-edge 2. If it lands first, this wave is unaffected — it anchors on `origin/master` either way. |

---

## 3. Scope

1. Back-fill verbatim copies of O-8 and O-11 into value's tree under the live `value-inbox-<date>-<slug>.md` convention, each with a one-line provenance header (source path, original mtime, byte count). Originals are **never** corrected in place.
2. Re-resolve every anchor the two packets cite against kf `origin/master`, producing the site-drift correction table.
3. Author ONE amendment-addendum carrying exactly four payload items — (a) the HEAD-resolved anchors, (b) the three missing `parseStylesheet` sites with their failure postures, (c) the withdrawn delivery-vehicle question, (d) the D-GAP-6 `sampleBezier` resolution — plus the withdrawn import-census certification, the minimal ledger-mark ask, and the owner-facing §B-12 request.
4. Deliver it to `keyframes-v-exec/docs/tranches/V/coordination/` in their `<SENDER>-INBOUND-*` grammar; retain a copy in value's tree.
5. Repair the **keyframes sweep-path law** in `INBOX.md`'s four-path preamble — the bullet at **`:15-16`** (re-measured this seat, repair round 2; the round-1 `:13-14` anchor is dead — `:13-14` is now the glass BI→BJ→BK note that commit `7a7dc6ef` added) — so the path names the exec-visible tree and the sacred checkout is marked READ-ONLY / NEVER-DELIVER.
6. Move I-26 (at `:92`) ROWED → CURED with its delivery receipt; mint outbound row **O-21** — the measured ledger maximum **O-20** (at `:95`) **+ 1**, re-measured at open per C-10's MINT LAW, never taken from this literal; leave the kf-side mark tracked OPEN under I-26.

### 3a. Triumvirate dispatch

Triggered by: (i) OP-1 failing at open — the source bodies gone from the sacred checkout (bounds expansion: reconstruction from the census's quotations is a *different wave* and must not be improvised here); (ii) any anchor in the drift table failing to resolve to *any* live path at `origin/master`, which means the drift is deeper than a rename and O-11 §A3's scope claim needs re-derivation, not re-anchoring; (iii) a second consecutive failed delivery to the exec clone (write authority is then the root cause, not the path — halt, do not retry a third time).

---

## 4. Bounds

| File | Access |
|---|---|
| `/Users/mkbabb/Programming/value.js/docs/tranches/X/keyframes/waves/KF-W1.md` | **modify** (this spec; filename per the reconciliation in the header). The superseded **create** grant is **SPENT**: the file exists (2026-08-28, `acff6265`) and is repaired in place thereafter; `W1.md` is never created. Listing a file already on disk as `create` was the same error G-KF1-11 carried — corrected in the same motion |
| `/Users/mkbabb/Programming/keyframes-v-exec/docs/tranches/V/coordination/VALUEJS-INBOUND-<open-date>-o8-o11-amendment-addendum.md` | **create** — the wave's **SOLE sibling-tree write**; their `<SENDER>-INBOUND-*` grammar; the tranche root is NOT a mail path |
| `/Users/mkbabb/Programming/value.js/docs/tranches/V/coordination/INBOX.md` | **modify** — exactly three edits, **re-aimed at repair round 2** (all three round-1 coordinates were dead; re-measured this seat against the 97-line / 56,702 B file last written by `7a7dc6ef`): the **keyframes sweep-path law** at **`:15-16`** · **I-26** at **`:92`** ROWED→CURED · **a new outbound row `O-21`** (= the measured maximum **O-20** at `:95`, **+1**; re-measured at open per C-10's MINT LAW). The row ids (`I-26`, `O-20`) are the anchors; the line numbers ride as convenience and are re-resolved at open |
| `/Users/mkbabb/Programming/value.js/docs/tranches/V/coordination/value-inbox-<open-date>-o8-o11-amendment-addendum.md` | **create** (retained copy) |
| `…/value-inbox-2026-07-24-parser-totality-exposure.md` · `…/value-inbox-2026-07-27-library-band-r1-widened-k1-k4.md` | **create** (verbatim back-fill + provenance header) |

**READ-ONLY, SACRED — never written, never git-mutated**: `/Users/mkbabb/Programming/keyframes.js/**`, specifically the two untracked packet bodies. Reads and copy-out only.

**READ-ONLY (anchor re-resolution target)**, `keyframes-v-exec` at `81a56990`: `src/animation/compile/adapter.ts` · `compile/value/compile.ts` · `compile/easing/registry.ts` · `compile/emit/easing-serialize.ts` · `compile/emit/backward/backward.ts` · `compile/emit/backward/color.ts` · `compile/emit/css-text.ts` · `scroll/grammar.ts` · `validate.ts` · `internal/leaves.ts` · `load-engine.ts` · `resolve/browser.ts` · `engine/options.ts` · `package.json` · `docs/tranches/V/coordination/INBOUND-LEDGER.md`.

**READ-ONLY, NEVER REWRITTEN** (dated documents; amendments land as dated addenda per COHESION §0): `megatranche/formation/keyframes/CENSUS-2026-08-03.md` · `lane-docs.md` · `lane-frontend.md` · `lane-library.md` · `audit/codex-provenance/INTAKE-ADJUDICATION-2026-08-03.md`.

**READ-ONLY (citation sources)**: `registry/CARRY-CUT-LEDGER.md` (CC-084 at `:186`; X-W9 at `:255`) · `registry/adjudicated/library-band.md` (the `sampleBezier` DECLINE at `:205`) · `docs/tranches/X/COHESION.md` §2/§3.

**Do NOT touch**: any `src/` in either repo · the census file bodies · KF.W0's §B-12 act · glass-ui and the SS-6 communique · `scripts/dev/dev.sh` (unowned, either repo, **NEVER**).

### 4a. Disjointness

Three serial units; `.a` writes only the two back-fill files, `.b` only the addendum + its retained copy, `.c` only `INBOX.md`. No shared modify path. No parallel wave writes `INBOX.md` — E13 mail edits are single-writer by standing law. No sibling KF wave writes `coordination/**` or this spec file (re-checked against the seven authored siblings, 2026-08-28).

### 4b. Agent units

| unit | goal | mechanism | files | sub-gate |
|---|---|---|---|---|
| **X.KF.W1.a** Retention and Re-anchoring | The packet bodies survive in value's git, and every anchor they cite is resolved against the frontier | Copy **out** of the sacred checkout (read-only), add provenance headers, then `git cat-file -e` / line-content assertion per anchor at `81a56990` | the two `value-inbox-2026-07-{24,27}-*.md` | G-KF1-9, G-KF1-2 (table produced) |
| **X.KF.W1.b** The Addendum | One letter, exec-visible, carrying exactly the four payload items | Author in their inbound grammar; first payload section = the drift table; deliver; record the clone HEAD sha inside the letter | the addendum + its retained copy | G-KF1-1, G-KF1-2..6, G-KF1-10 (ask half) |
| **X.KF.W1.c** Ledger and Law | The ledger records what happened, and the law that produced I-26 stops pointing at the frozen tree | Three surgical edits to `INBOX.md`, performed **after** delivery | `INBOX.md` | G-KF1-7, G-KF1-8 |

### 4c. LAW A — the import-graph law, discharged ⟨`conformance/PASS-3/RULINGS-3.md` **LAW A**⟩

**(i) The module clause is VACUOUS here, by enumeration of the bounds table above — not by assertion.** This wave's six bounds rows are `modify` ×2 (`KF-W1.md`, `INBOX.md`) and `create` ×4 (the addendum, its retained copy, the two back-fills). **All six are markdown documents. This wave deletes, repoints or shims NO module in either repo** — every `src/` is out of bounds by §4's **Do NOT touch** row and by the execution gate. LAW A's census requirement has no subject at this wave; it is discharged by having none.

**(ii) This wave does contain ONE repoint act — of a mail-path law, not a module — and it gets a LAW-A-SHAPED CENSUS anyway** (C-9 / G-KF1-8, which repoints the keyframes landing path off the frozen checkout). Commands executed by this seat at the value.js tree bytes, **2026-08-28**, pasted with their outputs:

```
# (1) SPECIFIER census — the path as written:
grep -rnF 'keyframes.js/docs/tranches/V/coordination' docs/ --include='*.md'
#   → 23 hits across 17 files
# (1b) the law's own relative spelling (the memory limb's spelling too):
grep -rnF '../keyframes.js/docs/tranches/V/' docs/ --include='*.md'
#   → 12 hits across 7 files
# (2) SUBJECT census — the sweep law itself, by its four-path preamble bullet:
grep -n '^- `\.\./keyframes\.js' docs/tranches/V/coordination/INBOX.md
#   → exactly 1 — INBOX.md:15, the law spanning :15-16
```

**Resolved consumer set = the sites that EXECUTE the path (the sites that would reproduce I-26) — TWO, and only one of them is in bounds:**

1. `docs/tranches/V/coordination/INBOX.md` **`:15-16`** — the four-path sweep law. **IN BOUNDS; C-9's cure site; G-KF1-8's subject.**
2. `~/.claude/projects/-Users-mkbabb-Programming-value-js/memory/feedback-mail-inbox-law.md:16` — **the same law, codified a SECOND time** in the session memory that makes every future session open with the sweep, naming *"`../keyframes.js/docs/tranches/V/`"* among its four landing paths (measured this seat). **OUT OF BOUNDS: owner configuration, not this wave's and not any wave's to edit unasked.** **Recorded as an OPEN residual, routed to the OWNER**, and stated plainly in the addendum's own self-defect paragraph: **repairing `INBOX.md` alone does not close the class** — the memory limb re-injects the frozen path at the next session open. This is exactly the finding LAW A exists to force, and it is invisible to any count of `INBOX.md` lines or to the wave's own text. **No obligation is minted on keyframes by it** (it is value's defect, in value's own configuration).

**Non-consumer context — 22 of the 23 hits, recorded, never counted as consumers**: the four outbound path cells `INBOX.md:66`/`:68`/`:72`/`:75` (**O-2 · O-4 · O-8 · O-11** — dated ledger records of deliveries that really did land there; **never rewritten**, C-11's verbatim-retention logic applied to the ledger) · `megatranche/formation/keyframes/lane-docs.md:290`, which transcribes value's law and cites its now-dead `INBOX.md:13-14` coordinate — a dated formation document, **READ-ONLY NEVER REWRITTEN** per §4; the drift is recorded here, not repaired there · `reformation/DISPOSITIONS.md:51` · `reformation/waves/W40.md:136` · `reformation/waves/WL.md:63` · the two letters `coordination/keyframes-inbox-2026-07-18-vnext-formation-handoff.md:456` and `coordination/value-inbox-2026-07-20-residual-repairs.md:33` · six `V/apotheosis/**` analysis files · `registry/harvest/packet-seat-O11-O15-receipts.md` · `X/keyframes/conformance/PASS-1/KF-W1-CHECK.md` · and **3** in this spec. **Nothing above is inherited from a CARRY, a bank paraphrase, a lane doc or a prior pass** — every figure is this seat's own command output, re-measured at open like every other dated figure in this file.

---

## 5. Carry

**Fourteen rows, C-1..C-14**, agglomerated by identity (M-25: dedupe by identity, carry cure-shape locks, route by mechanism), each with its **banked id** and each traced to a ground re-opened at the bytes. **Eight** trace to banked ids in tree (`I-26` · `SCH-4` · `SCH-5` · `lane-docs` §6 rows 1 / 2–10 / 11 / 14 / 16 / 17); **six** are declared **minted** and each names its ground (C-9..C-14). **No ledger absent from the tree is cited as a source** (header PROVENANCE; RULINGS R-12), and no `N of N rows carried` claim is keyed to one. The routed denominator is the registry's, and **its carriage is certified at `conformance/PASS-3/KF-W1-CHECK.md` §1 (2026-08-28) — 6 routed · 6 booked · 0 escaped · 0 inventions — not in this spec's voice** (LAW B). §8 states the non-carries with their reasons; **whether that statement is complete is the conformance seat's finding, not this file's claim** — and at PASS-3 that seat convicted §8 row 5's cardinality (D3-5), cured this round. Ids are **anti-rename, original for life**.

| # | banked id | sev | lands as | binding lock (verbatim where quoted) |
|---|---|---|---|---|
| C-1 | **I-26** ⟨`INBOX.md` **row I-26** — the row id is the anchor, at `:92` at this writing (re-measured repair round 2; the round-1 `:90` coordinate is dead — `:90` is row **I-21a**) ≡ `CENSUS §0.5` ≡ `lane-docs §0/§6 row 15/§7.1/§7.2` — one identity, four witnesses⟩ | **BLOCKER** | the whole wave; gates G-KF1-1, G-KF1-7 | **"Amend, do not re-send."** ⟨lane-docs §7.2⟩ Second leg, verbatim: "**and** ask the owner to reconcile the sacred checkout (§B-12), which is theirs to do, not ours." · **ANTI-STALENESS**: no measurement is inherited into the addendum — every anchor, count and interval is re-measured at authoring against `origin/master` (the census's "silent 16 days" is a dated figure; silence measured **41 days** at this authoring — newest kf-authored letter in our tree 2026-07-18) |
| C-2 | **DOCS §6 row 17** — O-11 §A2/§A3 undercount ⟨`VALUEJS-INBOUND-2026-07-27-*.md:34,:40,:52-63`⟩ | MAJOR | payload item **(b)**; gate G-KF1-3 | O-11 §A2 raised `parseStylesheet` to a **NEW crash class** (measured `parseStylesheet("a{color:constructor}")` → THROW, `:40`) yet §A3's site table named **zero** of keyframes' own call sites — the exposure letter is INCOMPLETE by exactly the sites §A2 made reachable. The addendum names **all three** with their failure postures. It **re-derives** the §A3 end-state count; it does not correct the stale number by arithmetic. It names **only** the three missed sites — LIB §4.1's 13-site Tier-A inventory is **KF.W2's object, not this letter's** |
| C-3 | **DOCS §6 row 1** — the delivery-vehicle question ⟨`…2026-07-24-*.md:101-103` — re-measured at repair round 2 in the sacred checkout (read-only); round 1's `:100-102` was off by one, `:100` being blank⟩ | MAJOR | payload item **(c)**; gate G-KF1-5 | One sentence **WITHDRAWING** the question, citing **CC-084 at `CARRY-CUT-LEDGER.md:186`** ("*no emergency 4.0.1 — ruled*"), stating value ruled without their answer. **"The asked-for decision has been pre-empted by our own ruling; the letter must be closed honestly, not left dangling."** ⟨lane-docs §6 row 1⟩ **HARD NEGATIVE**: never re-asked, never restated as open, and the letter does not pretend keyframes' silence caused the ruling. **No cut date is promised** (X·P's release condition is OWNER-GATED — COHESION §2 PLAW-BIND) |
| C-4 | **DOCS §6 row 11** — I-10 / D-GAP-6 `sampleBezier` conditional ⟨`INBOX.md` **row I-10** — the row id is the anchor, at `:47` at this writing (re-measured repair round 2; the round-1 `:45` coordinate is dead — `:45` is row **I-7**) · `library-band.md:205`, re-verified this seat⟩ | MAJOR | payload item **(d)**; gate G-KF1-6 | **ONE line** resolving the conditional to **"not adopted"**, citing the permanence and its ground (measured zero demand). **"CONVERGED — the conditional resolves to 'not adopted'."** ⟨lane-docs §6 row 11⟩ Not a re-litigation: D-GAP-6 is terminal both sides (DOCS §6 row 12). Recorded asymmetry (not a dissent): keyframes marked it terminal on the strength of a conditional value has now foreclosed — the addendum states the foreclosure plainly rather than letting the two ledgers read as agreeing |
| C-5 | **SCH-5** — packet anchor drift + the site-drift correction table ⟨`CENSUS §0` · `lane-docs §0/§6`⟩ | MAJOR | payload item **(a)**, the addendum's **FIRST** section; gate G-KF1-2 | **The packets measured the sender-side working tree accurately; what drifted is the RECIPIENT's tree.** ⟨**Round-3 correction (R3-2.4 / D3-2), stated not silently applied (E-3)**: this cell read *"Both packets measured the stale tree"* — **STRUCK**. Re-derived this seat: every anchor §5a marks ABSENT or DRIFTED resolves in the sacred checkout's uncommitted worktree at the packets' own coordinates (§1 Goal criterion's round-3 block prints all nine). The drift is between **that worktree** and **`origin/master`** — a real, load-bearing drift that makes the letters unusable to a recipient at the frontier, and **not** a mismeasurement by their authors.⟩ V's W5 carves (`eb4379ca`, `6f6adfaa`, `94f2c3c9`, `b3b362d9`) renamed 4+ cited paths. **"The findings are real … but four cited paths were renamed by V's W5 module carves and any keyframes executor working from HEAD will bounce off them."** ⟨lane-docs §0⟩ **The findings survive; the anchors do not.** Anchors are re-resolved at authoring, **never copied** from the table below or from the census (the D-19 re-resolve edict) |
| C-6 | **SCH-4** — the import-census **CERTIFICATION** ⟨O-11 `:52`, `:82`⟩ ⟨**round-3**: the descriptive tail *"off-by-one"* is **STRUCK as false** (R3-2.4 / D3-1 — there is no arithmetic error; see §5b); the **banked id `SCH-4` is anti-rename and unchanged**⟩ | MINOR | rides C-5's section; gate G-KF1-4 | **The error is not the count, and there was never an off-by-one — it is the CERTIFICATION, which names no substrate.** The addendum restates the measured split and explicitly withdraws *"matches your tree exactly"* by naming a **WORKTREE-STATE RECORD, not a sha**, in these words ⟨**commissioned verbatim, RULINGS-3 `R3-2`.1**⟩: *"the census matched the keyframes.js UNCOMMITTED WORKTREE at the sacred checkout — HEAD `8281638c` + 252 dirty paths (`git status --porcelain \| wc -l` → 252, measured 2026-08-28) — a state no sha names and no recipient can re-derive; byte-identical on all six census lines (61 = /css 29 · /value 15 · /color 7 · /math 5 · /easing 3 · /transform 2). It does NOT match bare `8281638c` (81 imports, disjoint subpath alphabet) and does NOT match `81a56990` (62)."* **The measurement that makes that sentence true rides beside it at §5b**, pasted with its commands ⟨**LAW B corollary**: no cure commissions a certification its own receipts do not reproduce⟩. **LOCK (LAW B corollary, verbatim, binding on the executing seat): *"No certification cites a sha as 'the tree measured' unless the census reproduces at that sha."*** **Round-2 → round-3 correction, stated not silently applied (E-3)**: this row read *"naming the tree it matched (`8281638c`) and the tree it does not (`81a56990`)"* — **STRUCK**. That order withdrew one false certification by **inscribing another** (SCH-4 reproduced inside SCH-4's own cure, exactly as round 1's frozen ledger literal reproduced I-26 one layer up). **Re-measure all three censuses at authoring; copy neither 61 nor 62 nor 81 from here** |
| C-7 | **DOCS §6 rows 2–10 + 14** — the inherited payload ⟨lane-docs §6⟩ | MAJOR | re-anchored **by table reference only** | §E2 `lerpArray` length assertion (the single code ask; mismatched lengths silently yield `[2.5, 3.5, NaN]` in their FrameCompiler hot loop) · §C **K1** easing reference instability (21/40 unstable, 9 collisions, the `.find()` reverse-map at `emit/easing-serialize.ts:71-73`, the load-bearing false docstring at `compile/easing/registry.ts:36`) · §C **K2** tautology spec + false "barrel" docstrings (`internal/leaves.ts:28` exact) · §C **K3** nine `noUnusedLocals` errors incl. the dead `/css` type import on the light/heavy boundary (`load-engine.ts:65` exact) · §C **K4** provenance gate · §B the `serializeCssValue` fork (`emit/css-text.ts:41` exact) · §D1 the `bezierPresets` fence · §D2 the 8-curve shape change (max\|Δ\| 0.192 on `ease-out-circ`, unacknowledged) · §E1 the post-cut additive-symbol covenant · IN-ATLAS-3's `TimingFunction` fence. **LOCK: no re-ask, no re-scope, no severity change, no new obligation.** The addendum amends two letters; it is not a third — anything not already in O-8/O-11 or in payload (a)–(d) does not belong in it. Ids `K1–K4` / `G-L7a–d` are **anti-rename, original for life**. **Scope tension recorded**: §E2 is the only row asking keyframes to write code; this wave re-delivers the ask and must **not** schedule it |
| C-8 | **DOCS §6 row 16** — §B-12 owner-checkout reconciliation | MAJOR | **NOT OWNED HERE** — the addendum's second leg *asks* | The sacred checkout's 41-behind state produced I-26 and every SCH drift; its cure is one act (`git fetch && git reset --hard origin/master`, untracked V docs unharmed) that **"value.js must NOT perform"** ⟨DOCS §6 row 16, HARD NEGATIVE⟩. The addendum ASKS the owner ⟨lane-docs §7.2⟩. **DESIGN LOCK: this wave is COMPLETABLE WITHOUT §B-12** |
| C-9 | **KF-MAIL-PATH** (minted) — our own law names the wrong tree | MAJOR | gate G-KF1-8 | `INBOX.md`'s four-path sweep preamble, **the keyframes bullet at `:15-16`** (re-measured repair round 2; the round-1 `:13-14` coordinate is dead), declares the keyframes landing path as `../keyframes.js/docs/tranches/V/coordination/` — the frozen checkout. **I-26 is not a slip; it is our codified procedure executing correctly.** Unrepaired, the next letter reproduces it exactly. **PRECEDENT, in this same file and this same class, measured this seat**: commit **`7a7dc6ef`** (2026-08-28 12:33:59) cured the *glass* limb of the identical defect — a codified sweep path pinned to a dead tranche — by naming the live one, and recorded the cost in-row: *"glass moved BI→BJ→BK; sweep the LIVE tranche's coordination dir, not a pinned letter. **The stale BI pin here cost 19 days on the 08-09 letters → I-28/I-29**"* (now at `:13-14`, the very lines this row's superseded anchor named). Two independent limbs of one law-level defect, one already cured at a measured 19-day cost: that is the corroboration of this row's thesis and **the template for its two-line cure** — same shape, same file, keyframes limb instead of glass. No new obligation and no scope change follow from the precedent. Cure: name the exec-visible tree for delivery; mark the sacred checkout READ-ONLY / NEVER-DELIVER rather than deleting it (other senders' inbound does land there, and the sacred rule must stay visible); keep the grammar note verbatim. **SIZING LOCK (KISS, L-19): two lines plus one sentence of rationale — not a routing subsystem, not a script, not a linter** |
| C-10 | **KF-MAIL-LEDGER** (minted) | MINOR | gate G-KF1-7 | (i) I-26 ROWED → **CURED** with the receipt (absolute path + date + exec-clone HEAD sha); (ii) mint **O-21** naming payload items (a)–(d); (iii) the kf-side mark stays an OPEN sub-row under I-26. **MINT LAW (added at repair round 2, RULINGS-2 R2-1)**: *the commissioned id is **O-21** (= the measured ledger maximum **O-20** + 1, measured 2026-08-28 at `INBOX.md:95` / commit `48003630`); the executing seat **re-runs the max-grep at open** and, if the ledger has moved again, mints **max+1** and records the delta as a dated addendum before executing — **a spec-frozen outbound literal is never trusted over the measured ledger.*** The round-1 spelling commissioned **O-20**, an id spent nine minutes before that write on the SS-6 glass communique; executing it would have written a duplicate live id into the E13 ledger — I-26 reproduced in the ledger layer under the spec whose thesis is I-26. **ANTI-RENAME**: I-26 keeps its id for life; O-8/O-11's rows keep "SENT — *no reply on record*" **plus** an **amended-by-O-21** pointer (the pointer names whatever id the mint law yields at open), never a rewrite. **ORDERING**: valid only *after* G-KF1-1 — a ledger that marks CURED ahead of delivery reproduces I-26 in the ledger layer |
| C-11 | **KF-MAIL-COPY** (minted) | MAJOR | gate G-KF1-9 | Value's two most consequential outbound packets are **single-copy, outside value's git, on a tree value is forbidden to touch.** Any cleanup of untracked files there destroys them and the addendum's source text with them. Cure: verbatim back-fill under the live convention, + the addendum retained the same way. **LOCK: a read of the sacred checkout, never a write.** **LOCK: copies are verbatim — the retained originals are NOT corrected; the corrections live in the addendum (that is what "amend, do not re-send" means, and a silently corrected original destroys the evidence that I-26 happened).** **SEQ-LOCK: precedes addendum authoring** (OP-1) |
| C-12 | **KF-MAIL-MARK** (minted) | MINOR | gate G-KF1-10 (ask half only) | Explicit minimal ask for `IN-VALUE-3` (O-8) / `IN-VALUE-4` (O-11) in their grammar — their numbering, their call. **HARD NON-GATING LOCK: the marking may never come and MUST NOT gate this wave's close.** What we owe is delivery + the ask, both ours. Gating on a sibling repo's reply makes the wave un-completable by construction ⟨census (c)5⟩. Absence is re-measured at close and **recorded, not repaired** |
| C-13 | **KF-MAIL-NS** (minted) | MAJOR | the alias block at the head of this file | Declared **before** any X·KF spec is grepped for carry rows. Reciprocal declaration owed at KF.W0, which receives F-1 **plus its five component riders** (the `useControlsKeyboardShortcuts.ts:1` sole-binding-site blast radius · `ChannelControls.vue:219`'s first-failing import · `ControlsPaneWrapper.vue:166`'s sole `/drawer` consumer · kf-DemoGlobalChrome's four toast utility classes · kf-KfPillTabs' one-class/eight-custom-property inventory). **Dissent recorded, not resolved**: `kf-DemoGlobalChrome.md:44` banks a rider that survives SCH-1's falsification — commit `a59d3a22` ("fix(dependencies): consume Glass UI 6 from one registry core") exists in history "yet the declaration is still absent — F-1 is not merely open, it survived a commit that claimed to address the dependency." That rider was written against the stale tree; whether it survives at `origin/master` is **KF.W0's** re-measurement, not this wave's |
| C-14 | **KF-MAIL-∅** (minted) | INFO | §7 cross-edge 10; gate G-KF1-11 | **Zero adjudicated registry rows target this wave — and the CLOSURE of that set is the conformance seat's finding, cited, never asserted in this spec's voice** ⟨**LAW B**⟩: `conformance/PASS-3/KF-W1-CHECK.md` **§1 (2026-08-28)** re-enumerated the routing alphabet from the 58 records' bytes, closed it **four** ways (bare token · substring decomposition · terminal-disposition alphabet · carry sweep), and found the **∅ column TRUE with 0 inventions** — while convicting this file on four other axes (§12). The three legs below are **this seat's own measurements, offered as the material that check re-derived independently**, not as a completeness claim: **(i)** no `kf-*.md` routes a row to census-taxonomy KF.W1. **Stated so the grep it invites cannot falsify it**: a literal `grep -o 'KF\.W1' kf-*.md \| wc -l` returns **33** — **18** `KF.W1-DEP` anchors (C-13) **plus 15 `KF.W10` substrings**, since `KF.W10` contains `KF.W1`. The load-bearing measurement is the *token*, not the substring: `grep -oE 'KF\.W1([^0-9A-Za-z-]\|$)' kf-*.md` → **0 across all 58 records**, and `KF.W1` is absent from the terminal-disposition alphabet (`→ **…**`) entirely. An earlier draft's *"the only `KF.W1` hits are the five folds"* was TRUE in substance and falsifiable as written; this phrasing is neither. **(ii)** the intake CARRY table's kf targets are KF.W0 (X-1, B10-9, B19-10/11, B20-6, B21-7), KF.W4 (**B10-17 · B10-18 · B10-19 · B10-20 · B10-22 · B10-23 · B10-27** — the intake's own **enumeration with gaps**, never the range spelling `B10-17..27`, which would swallow `B10-21` and route it to two waves in one sentence ⟨**`INTAKE-ADJUDICATION-2026-08-03.md` §3, the heading `### KF.W4 · K-Quartet Hygiene (born-RED gate-authoring law, feeds G-L7a–d)` and the enumeration under it** — at `:146` / `:147-149` at this writing, re-resolved repair round 2; the round-1 `:144-145` coordinate is dead (it is the tail of the §3 **KF.W0** block); **`KF-W0.md` §Sequencing, the OUTBOUND row `KF.W4 · K-Quartet Hygiene`** transcribes it gap-preserving as `B10-17/18/19/20/22/23 + B10-27 + B18-19 + B21-17` ⟨**numeric convenience STRUCK at round-2 close (U-2/R2-7)**; `:394` had drifted onto a §Gates falsifier — **the §-heading + the OUTBOUND row label `KF.W4 · K-Quartet Hygiene` are the sole anchor**⟩⟩ · B18-19("derivation helpers" half) · B21-17(secondary)), KF.W5 (X-4, **B10-21**), KF.W9 (B18-26), KF.W10 (B18-12/13/14, B18-23+B19-14+B20-14, B18-27) — **none targets KF.W1**; **(iii)** none of the **17** NO-WAVE-OWNER packets proposes KF.W1 as a home. **The cardinality is now RECEIPTED, per RULINGS R-15**: 17 is the canonical roster — the reconciliation of **KF-W0's** then-stale 15 and **KF-W9 S-11's** 14, namely W0's 15 ∪ {**amiga**, **EDITOR-UNIT**} = **17**. **Re-resolved repair round 2**: the round-1 receipt `KF-W0.md:276` is dead; KF-W0 carries the canonical 17 at **§Sequencing, the packet-roster line `the NO-WAVE-OWNER packets — the canonical 17`** — self-declared *"re-cut this round from this file's stale 15"* (**numeric convenience STRUCK at round-2 close (U-2/R2-7)**; `:401` had drifted onto a §Carry carries-note — the **§-heading + roster line label** are the sole anchor and no line number rides this receipt). *The **16** this row previously carried was stated by no document in the tree and was never true; C-1's ANTI-STALENESS lock forbids exactly such an inherited count, and it was applied to fourteen other figures and skipped for this one.* The roster, id for id: **cube · sequence · spring · spring-plot · spring-physics-facet · spring-artifact-truth · square · amiga · drag-seam · CARD-UNIT · OPTIONS-UNIT · KFED-UNIT · EDITOR-UNIT · APPLY-UNIT · AXISLINE-UNIT · dock-menu · transport/ribbon**. Homing authority is **KF.W4** (R-15), whose §Sequencing lists partition them **KF.W11 ×9 · KF.W12 ×6 · KF.W13 ×2**; the set-difference against KF.W1 is ∅ by enumeration, not by assertion (cross-edge 9). **The empty column is a finding, not a gap.** NEXT-COMMUNIQUE for this wave: **EMPTY** ⟨intake §3: "Zero producer-facing asks survived adjudication in any lane"⟩ — nothing here rides the SS-6 BH letter; the standing edict is satisfied **by declaration**, not by minting a rider. **LOCK (L-19): a wave with no registry rows gets no manufactured ones** — the specced work is the census's four amendment items plus the three minted self-defect repairs, and nothing else |

### 5a. The site-drift correction table (payload item (a))

Re-measured **2026-08-28** at `keyframes-v-exec` HEAD `81a56990` = kf `origin/master`. **Count reconciliation, stated not silently applied**: `lane-docs §6` banks this as **12 rows**; re-measurement resolves to **14 anchor rows** (9 drifted, 5 exact) because `emit/backward-color.ts`'s three line anchors are three rows against one path rename. The executing seat re-derives this table again — it does not copy it.

| # | packet anchor | state at `81a56990` | measured |
|---|---|---|---|
| 1 | `src/animation/compile/value-ast.ts` | **ABSENT** → live `compile/value/compile.ts:32` (`const parsed = parseCssValues(value);`) | 2026-08-28 |
| 2 | `resolve/browser.ts:165` | → **`:162`** (`const parsed = parseCssScalar(source);`) | 2026-08-28 |
| 3 | `compile/easing/easing-registry.ts:36` | **ABSENT** → live `compile/easing/registry.ts:36` (same docstring: *"Stable identities let the serializer distinguish named curves from closures."*) | 2026-08-28 |
| 4 | `emit/easing-serialize.ts:70-71` | → **`:71-73`** (the `timingFunctionEntries.find(([_name, func]) => func === easing.fn)?.[0]` reverse-map) | 2026-08-28 |
| 5 | `emit/backward.ts:47` | **ABSENT** → live `emit/backward/backward.ts` (dir now holds `backward.ts`, `color.ts`, `index.ts`, `walk.ts`) | 2026-08-28 (path only) |
| 6–8 | `emit/backward-color.ts:171` / `:250` / `:263` — O-11 `:175`'s *"`count=1024`"* evidence tuple | path → `emit/backward/color.ts`; **all three LINE anchors now MEASURED, and all three are EXACT at the frontier**: `:171` = `const sampleRamp = (` · `:250` = `const ramp = sampleRamp(fromColor, toColor_, stopCount, space, hueOpt.hueMethod);` · `:263` = `const kfRefRamp = sampleRamp(fromColor, toColor_, 1024, space, hueOpt.hueMethod);` — byte-identical to the sacred worktree's `backward-color.ts` at the same three lines (both files **385 L**). **PATH drift only; ZERO line drift.** ⟨Round-2's *"line anchors NOT re-measured at spec-authoring — the executing seat resolves all three"* is **STRUCK (D3-11)**: it was this file's last unmeasured figure, sitting under a header whose own law is *"anchors are re-resolved at authoring, never copied"* and beside §11 row 8, which measured row 14 for exactly this reason. The executing seat re-derives it at open like every other row, but **inherits nothing**.⟩ | 2026-08-28 (round 3) |
| 9 | `package.json:69` (the `"@mkbabb/value.js": "4.0.0"` pin) | → **`:70`** (re-verified this seat) | 2026-08-28 |
| 10 | `engine/options.ts:31` | **EXACT** (`const parsed = parseCssScalar(raw);`) | 2026-08-28 |
| 11 | `internal/leaves.ts:28` | **EXACT** (`export { clamp, scale, lerp, lerpArray } from "@mkbabb/value.js/math";`) | 2026-08-28 |
| 12 | `load-engine.ts:65` | **EXACT** (`import type { Stylesheet } from "@mkbabb/value.js/css";` — the dead `/css` type import) | 2026-08-28 |
| 13 | `emit/css-text.ts:41` | **EXACT** (`export const serializeCssValue = (value: CssValue): string => {`) | 2026-08-28 |
| 14 | `test/internal/leaves-parity.test.ts` | **EXACT — re-measured at repair round 2** (`git cat-file -e 81a56990:test/internal/leaves-parity.test.ts` → **PRESENT**). Round 1 left this the file's one inherited figure under a header whose own law is *"anchors are re-resolved at authoring, never copied"*; it is now measured, so **the executing seat inherits nothing** — and re-measures it again at open, like every other row | 2026-08-28 (round 2) |

### 5b. The substrate receipt (C-6 · G-KF1-4) — the ANCHOR DUALITY, measured

**Why this block exists.** C-6 commissions a sentence about which tree O-11's census matched. Under **LAW B's corollary** no cure may commission a certification its own receipts do not reproduce, so the measurement rides here, beside the order, with its commands. All three were run **read-only** by this seat on **2026-08-28**; the sacred checkout was read (`git rev-parse`, `git status`, `grep`, `git grep`) and never written, per the SACRED-CHECKOUT LAW.

```
# (A) the sacred checkout's UNCOMMITTED WORKTREE — the tree O-11 actually measured
$ cd /Users/mkbabb/Programming/keyframes.js && git rev-parse HEAD
  8281638c0ac4ac8c54a67a018ca5bf6a9117174f
$ git rev-list --left-right --count origin/master...master   →  41   1
$ git status --porcelain | wc -l                             →  252
$ grep -h -o -E 'from "@mkbabb/value\.js[^"]*"' -r src | sort | uniq -c
   7 /color   29 /css   3 /easing   5 /math   2 /transform   15 /value      → 61
  # BYTE-IDENTICAL to O-11 :52's certified split — all six lines, and the total.

# (B) bare 8281638c — the sha an earlier cure ordered inscribed as "the tree it matched"
$ git grep -h -o -E 'from "@mkbabb/value\.js[^"]*"' 8281638c -- src | sort | uniq -c
  10 (root)  9 /color  5 /easing  5 /math  24 /parsing  2 /transform  26 /units  → 81
  # DISJOINT ALPHABET: /css and /value do not exist at that sha; /parsing and /units do.
  # ZERO of O-11's six lines reproduce. 81 ≠ 61.

# (C) the frontier of record
$ cd /Users/mkbabb/Programming/keyframes-v-exec
$ git grep -h -o -E 'from "@mkbabb/value\.js[^"]*"' origin/master -- src | sort | uniq -c
   7 /color  29 /css  3 /easing  5 /math  2 /transform  16 /value            → 62
```

**Corroborating receipt, same seat, same clock — the ANCHORS agree with the census.** The three anchors §5a rows 6–8 carry resolve **byte-exactly** in the worktree at the packet's own coordinates (`backward-color.ts:171/:250/:263` = the `sampleRamp` declaration and its two calls, the *"`count=1024`"* tuple O-11 `:175` cites) and **do not** resolve at bare `8281638c`, where those same three line numbers are docblock prose (`:171` *"The WORST midpoint ΔE this key drifts…"* · `:250` a `sampleColorRamp` docblock line · `:263` an EN-b docblock line). Two independent instruments — the import census and the line anchors — name the **same** substrate. The certification's fault is therefore **not** arithmetic.

> **THE ANCHOR DUALITY, declared once and binding on every measurement in this wave and on every sentence in the addendum** ⟨commissioned verbatim, RULINGS-3 `R3-2`.3⟩: *"Two substrates, each named at each use: cures and forward anchors bind to `origin/master 81a56990` (the frontier of record); the historical census is certified against the dirty-worktree record above. No certification cites a sha as 'the tree measured' unless the census reproduces at that sha."*

**Consequences, stated so no seat has to re-derive them**: (1) `8281638c` may appear in this wave **only** as a named historical measurement context inside the worktree-state record — never as a witness substrate, never as "the tree measured"; (2) every gate leg names **both its repo and its substrate** (§6, re-keyed at round 3); (3) the recipient cannot re-derive the worktree state, and the addendum **says so in that sentence** rather than handing them a sha that would fail under them — the whole point of withdrawing the certification.

---

## 6. Gates

All witnesses below are **literal commands with literal outputs**, and every one was **RE-EXECUTED at repair round 2 (2026-08-28)** — keyframes-side against the frontier of record `origin/master` **`81a56990`** (local `8281638c` is DISQUALIFIED and consulted only to date drift), value-side against the current tree bytes (`INBOX.md` = 97 L / 56,702 B, last written by `7a7dc6ef`). **Nothing below is inherited from prose, from a ruling, or from this file's own prior text** (RULINGS-2 **R2-5**, the witness re-derivation law). Dated figures are non-inheritable: they are re-measured again at open. **Where round 2's measurement differs from round 1's, the round-1 reading is quoted, struck by name, and the true one stated beside it** — never silently overwritten (E-3).

**G-KF1-1 · DELIVERY — the addendum exists at an exec-visible path** — *born-RED*
Witness: `ls /Users/mkbabb/Programming/keyframes-v-exec/docs/tranches/V/coordination/` → **9 entries + `vnext/`**: `ATLAS-INBOUND-2026-07-16-*`, `ATLAS-INBOUND-2026-07-17-*`, `GLASS-INBOUND-2026-07-16-*`, `GLASS-INBOUND-2026-07-17-*`, `INBOUND-LEDGER.md`, `SPEEDTEST-INBOUND-2026-07-17-*`, `VALUEJS-INBOUND-2026-07-17-{formation-exchange,formation-exchange-marks,wl-verdicts}.md`. **No addendum. No O-8. No O-11.** Clone HEAD `81a56990`, same session.
GREEN: the file exists at `…/VALUEJS-INBOUND-<open-date>-o8-o11-amendment-addendum.md` and the clone's HEAD sha is recorded **inside** it. Rows: C-1, C-10.

**G-KF1-2 · ANCHOR LIVENESS — every `file:line` in the addendum resolves at kf `origin/master`** — *born-RED*
**REPO AND SUBSTRATE ARE NAMED IN EVERY COMMAND** ⟨re-keyed at repair round 3 — RULINGS-3 `R3-2`.2, check **D3-6**; the idiom G-KF1-3 and G-KF1-10 already use⟩. **Frontier legs run `in keyframes-v-exec` against `origin/master`, never bare `HEAD`.**
Witness, **in `keyframes-v-exec`**: `git cat-file -e origin/master:src/animation/compile/value-ast.ts` → **ABSENT** (O-11 §A3's primary authored-input anchor); same for `compile/easing/easing-registry.ts` and `compile/emit/backward.ts`. `git grep -n 'parseCssScalar' origin/master -- src/animation/resolve/browser.ts` → **`:162`** (`const parsed = parseCssScalar(source);`; the import at `:3`), packet says `:165`. `git grep -n '"@mkbabb/value.js"' origin/master -- package.json` → **`:70`**, packet says `:69`. Five anchors verified EXACT (rows 10–13 above + row 14, measured at round 2) and rows 6–8 measured at round 3 — **so this is a per-anchor check, not a blanket one.**
**Historical legs — where the packets' own coordinates are resolved — run against the WORKTREE-STATE RECORD of §5b** (the sacred checkout's dirty worktree: HEAD `8281638c` + porcelain 252, dated), **never bare `HEAD`, never bare `8281638c`.**
⟨**Round-2 → round-3 correction, stated not silently applied (E-3).** This gate's legs read bare `HEAD:` and `HEAD --` with **no repo named**, while §4's SACRED-CHECKOUT LAW puts `/Users/mkbabb/Programming/keyframes.js/**` in bounds as a read target and OP-1's `ls` runs there — so bare `HEAD` most likely resolves to the **DISQUALIFIED** `8281638c`. Demonstrated by this seat: run in the sacred checkout, `git grep -n 'parseCssScalar' HEAD -- src/animation/resolve/browser.ts` returns **nothing (exit 1)** — the assertion `→ :162` cannot even fail informatively — and `git grep -c 'parseStylesheet' HEAD -- src` returns **0 files**. §6's blanket preamble declares the frontier in prose; **the commands are what a seat runs**, so the substrate is now written into each of them.⟩
GREEN: every anchor passes `git cat-file -e` + a line-content assertion at authoring time **at the substrate its own row names**, and the §5a table is the addendum's **first** payload section. Rows: C-5, C-7.

**G-KF1-3 · `parseStylesheet` COMPLETENESS — the three missed sites named** — *born-RED*
Witness, **in `keyframes-v-exec`, against `origin/master`** ⟨substrate written into the command at repair round 3, on the D3-6 rule: `HEAD` in that clone is `81a56990` with porcelain **0**, so the reading is unchanged — but the spelling no longer *depends* on that being true at the executing seat's clock⟩: `git grep -n 'parseStylesheet' origin/master -- src` → call sites **`compile/adapter.ts:222`** (`const result = parseStylesheet(source);`), **`scroll/grammar.ts:109`** (`? requireParsed(parseStylesheet(input), input)`), **`validate.ts:182`** (`const parsed = parseStylesheet(css);`); imports at `adapter.ts:7`, `scroll/grammar.ts:37`, `validate.ts:47` — all six re-verified at repair round 2 against `81a56990`. **The command's full output is NINE lines, stated so a re-running seat gets what the witness promises**: the six code lines above **plus three prose mentions** (`adapter.ts:202` *"Value's `parseStylesheet` directly if it needs the full set"* · `validate.ts:41` the `@mkbabb/value.js` edges docblock · `validate.ts:130` the numbered `parseStylesheet(css)` walk-through). The load-bearing set is the **six code lines**; narrow with `-- src` plus a `^\s*\(import\|.*= *parseStylesheet\|? requireParsed\)` filter, or read the nine and take the six. O-11 §A2 (`:34`) declares `parseStylesheet` a **NEW crash class** and `:40` records the measured THROW, while §A3's site table (`:52-63`) names **0 of 3**.
GREEN: all three named at re-resolved anchors, each with its LIB §7.5 failure posture (**absorb-to-diagnostics** at the adapter · **`requireParsed` THROW** at scroll/grammar · **swallow-to-`[]`** at validate), so the recipient sees that ONE value diagnostic-shape change touches three of their sites three different ways; the §A3 end-state count re-derived, not corrected by arithmetic. Row: C-2.

**G-KF1-4 · IMPORT CENSUS — the exactness certification withdrawn** — *born-RED*
**REPO AND SUBSTRATE ARE NAMED IN EVERY COMMAND** ⟨re-keyed at repair round 3 — RULINGS-3 `R3-2`.1/.2, checks **D3-1** and **D3-6**⟩. **Three censuses, three named substrates, all pasted with their outputs at §5b** — that block is this gate's receipt and is not restated here.
Witness (frontier leg), **in `keyframes-v-exec`**: `git grep -h -o -E 'from "@mkbabb/value\.js[^"]*"' origin/master -- src | sort | uniq -c` → **62** total: `/css` **29** · `/value` **16** · `/color` **7** · `/math` **5** · `/easing` **3** · `/transform` **2** (re-run 2026-08-28, exact). Witness (historical leg), **in the sacred checkout, against the §5b WORKTREE-STATE RECORD** — read-only, no `git` object needed: `grep -h -o -E 'from "@mkbabb/value\.js[^"]*"' -r src | sort | uniq -c` → **61**, split `/css` 29 · `/value` **15** · `/color` 7 · `/math` 5 · `/easing` 3 · `/transform` 2 — **byte-identical to O-11 `:52`, all six lines**. Contrast leg, same tree, the **DISQUALIFIED** sha: `git grep … 8281638c -- src` → **81**, over a **disjoint subpath alphabet** (`/parsing` 24 · `/units` 26; `/css` and `/value` absent). O-11 `:52` says *"61 import statements total"* and that it *"matches your tree exactly"*; `:82` repeats *"across 61 of your sites"*.
GREEN: the measured **frontier** split is stated, **and** the certification is explicitly withdrawn by naming a **WORKTREE-STATE RECORD, not a sha** — C-6's commissioned sentence verbatim, with §5b's measurement standing beside it. **RED remains reachable and the gate remains falsifiable**: it fails if the addendum inscribes any sha as *"the tree it matched"*, fails if it names 61 without naming the state that produced it, and fails if it calls the defect an off-by-one.
⟨**Round-2 → round-3 correction, stated not silently applied (E-3).** This GREEN read *"naming the tree it matched (`8281638c`) and the tree it does not (`81a56990`)"* — **STRUCK**. At `8281638c` the census is **81** over an alphabet sharing **zero of six lines** with O-11's certified split, so that GREEN condition commissioned a **NEW false certification into the cure letter**: the SCH-4 class reproduced inside SCH-4's own cure. The gate now orders the honest sentence, and the measurement that makes it true is pasted at §5b (**LAW B corollary**).⟩ Row: C-6.

**G-KF1-5 · VEHICLE QUESTION CLOSED HONESTLY** — *born-RED*
Witness: O-8 **`:101-103`** (re-measured at repair round 2 by read-only `sed` in the sacred checkout; round 1's `:100-102` was off by one — `:100` is blank — **struck**) still reads "**Decision we need from you:** do you want the fix as a deliberate `4.0.1` you take on your own schedule, or folded into the next coherent tuple … We will not cut a version into your dependency graph without your answer." The internal ruling exists at `CARRY-CUT-LEDGER.md:186` (CC-084 — "*no emergency 4.0.1 — ruled*", line re-verified 2026-08-28).
**Third leg, MADE LITERAL at repair round 3** ⟨check **D3-10**: this leg read *"Grep of both coordination trees for any withdrawal → **0**"* — no pattern, no path, no command, in a §6 whose own preamble promises *"literal commands with literal outputs"*. **STRUCK** and replaced by two commands over **all three** coordination trees, run by this seat 2026-08-28⟩:
(a) `grep -rn '4\.0\.1' /Users/mkbabb/Programming/value.js/docs/tranches/V/coordination/ /Users/mkbabb/Programming/keyframes.js/docs/tranches/V/coordination/ /Users/mkbabb/Programming/keyframes-v-exec/docs/tranches/V/coordination/` → **4 hits**, each read: value `INBOX.md:71` (the **O-7** glass-BJ row cell) · `:72` (the **O-8 row cell itself**) · `:80` (the **O-10a** glass-BJ row cell) · sacred-checkout `VALUEJS-INBOUND-2026-07-24-parser-totality-exposure.md:101` — **the question being asked**. **ZERO hits under `keyframes-v-exec/docs/tranches/V/coordination/`**, which is the exec-visible tree the addendum lands in.
(b) `grep -rniE 'withdraw' <the same three paths>` → **4 hits**, each read: value `INBOX.md:80` (*"withdrawn."* inside the **O-10a glass** row's summary) · `CONSTELLATION.md:9` and `:69` (the V-A136 / V-A168 configurator instructions) · exec `vnext/skeptic-H1-r2.md:65` (*"withdrawn on atlas evidence"*, a vnext skeptic table). **Not one is a withdrawal of O-8's vehicle question.** The question is asked in exactly one place and withdrawn in none — which is the fact this gate is born RED on, now re-executable and falsifiable by any seat.
GREEN: one sentence withdraws the question citing CC-084 at `:186`; the question appears nowhere else in the letter; **no cut date promised**. Row: C-3.

**G-KF1-6 · D-GAP-6 CONDITIONAL CLOSED** — *born-RED*
Witness: `INBOX.md` **row I-10** — at **`:47`**, re-measured at repair round 2 — records the acceptance "they adopt `sampleBezier` only if a future 4.1 ships it"; `registry/adjudicated/library-band.md:205` reads "DECLINE — `sampleBezier`" (permanently, measured zero demand), re-read verbatim this seat. **Round-1 correction, stated not silently applied**: this gate's anchor read *"`INBOX.md:45` (I-10)"* — **struck**; `:45` is row **I-7** (the 07-17 v-execution-open letter). The row id `I-10` is the anchor; the line rides as convenience and is re-resolved at open. **The two statements have never been joined in any letter** — the conditional is live in their ledger and foreclosed in ours.
GREEN: one line resolves it to "not adopted" with the permanence and its ground; no re-litigation. Row: C-4.

**G-KF1-7 · LEDGER VERBS — I-26 CURED + O-21 minted** — *born-RED*
Witness, **re-executed at repair round 2 against the current bytes**: `INBOX.md` **row I-26** — at **`:92`** — reads "**ROWED 2026-08-03**" with the cure described-but-unperformed (text re-read verbatim this seat); and `grep -oE '^\| O-[0-9]+' docs/tranches/V/coordination/INBOX.md | sort -t- -k2 -n | tail -1` → **`O-20`**. No outbound row exists for a letter that does not exist.
**Round-1 corrections, stated not silently applied** (E-3): (i) the anchor read *"`INBOX.md:90`"* — **struck**; `:90` is row **I-21a** (the D-2 card-condense ratification). (ii) the max-grep's stated output read *"**O-19**"* — **struck**; the measured maximum is **O-20**, the SS-6 batched communique to glass BK at `:95`, **SENT 2026-08-28**, minted by commit `48003630` (12:32:16) — nine minutes before round 1's write, which is why round 1 commissioned an id already spent.
GREEN: I-26 reads **CURED** with its receipt (absolute path + date + exec-clone HEAD sha); **O-21** minted naming (a)–(d); O-8/O-11's rows keep their text plus an **amended-by-O-21** pointer. The commissioned id is **`max+1` measured at open**, not this literal — C-10's MINT LAW governs, and a ledger that has moved again yields a different id plus a dated addendum recording the delta. **Ordered strictly after G-KF1-1.** Row: C-10.

**G-KF1-8 · SWEEP-PATH LAW REPAIRED — our own procedure stops pointing at the frozen tree** — *born-RED*
Witness, **narrowed at repair round 2 so the command produces its stated reading**: `grep -n '^- \`\.\./keyframes\.js' docs/tranches/V/coordination/INBOX.md` → **exactly 1 hit**, **`:15`** — "`../keyframes.js/docs/tranches/V/coordination/` — the keyframes exchange (their `<SENDER>-INBOUND-*` grammar; the tranche root is NOT a mail path)", the law spanning **`:15-16`**, re-read verbatim this seat. That tree measured this seat at `8281638c`, **41 behind / 1 ahead** (`git rev-list --left-right --count origin/master...master` → `41  1`), **252** dirty. The law that produced I-26 is unchanged.
**Round-1 corrections, stated not silently applied** (E-3): (i) the anchor read *"`:13-14`"* — **struck**; `:13-14` now carries the **glass** BI→BJ→BK sweep note that commit `7a7dc6ef` added, and the keyframes law moved to `:15-16`. (ii) the broad pattern `grep -n 'keyframes.js/docs/tranches' …` was presented as returning the law alone; **re-executed this seat it returns FIVE lines** — `:15` (the law) plus `:66` · `:68` · `:72` · `:75`, the **O-2 / O-4 / O-8 / O-11** outbound path cells, all in the ledger since 2026-07-17/17/24/27 and therefore already present at round-1 authoring. *(RULINGS-2 R2-1 item 4 enumerates four — `:15`/`:66`/`:72`/`:75` — omitting `:68`, the **O-4** WL-verdicts cell. Under **R2-5** the tree outranks the ruling's prose: the measured count is **five**, recorded here as a dated correction and relayed to the pass-3 seat.)* A seat may use either form: the narrowed pattern above (1 hit), or the broad one **with "5 hits, the law at the first"** stated.
GREEN: the block names the exec-visible tree for delivery, marks the sacred checkout READ-ONLY/NEVER-DELIVER, keeps the grammar note verbatim. Two lines plus one sentence. No tooling. Row: C-9.

**G-KF1-9 · OUTBOUND RETAINED — value holds copies of its own letters** — *born-RED*
Witness: `find /Users/mkbabb/Programming/value.js -name '*parser-totality-exposure*' -o -name '*library-band-r1-widened*'` → **0 hits** (re-run 2026-08-28). The `value-inbox-*` convention is live in `docs/tranches/V/coordination/`. **Dated authoring-time finding on the retained-directory census** (stated, not silently applied — the anti-staleness lock earning its keep): re-measured at repair round 2, the retained set is **7 files — 07-19 ×1 and 07-20 ×6** (`union-apotheosis-relay` · `bbnf-coordination` · `d23-ruled-mirror-primary` · `owner-rulings-relay` · `parser-proof-evidence` · `pi-minitranche-notice` · `residual-repairs`); there is **no `value-inbox-2026-07-21-*`**. The superseded *"07-19, 07-20 ×5, 07-21 present"* reading is recorded **as the reading corrected, never as a source** (header PROVENANCE). Either way the load-bearing fact holds: **07-24 and 07-27 are absent** — the sole copies are the two untracked sacred-checkout files (7,064 B / 15,633 B, mtimes re-verified today).
GREEN: both bodies retained verbatim with provenance headers, and the addendum retained the same way; read-only access to the sacred checkout; originals never corrected in place. Row: C-11.

**G-KF1-10 · MARK REQUESTED (our half) — and MARK RECEIVED declared NON-GATING (their half)** — *born-RED on our half only; the counterparty half is explicitly NOT a gate*
Witness, **in `keyframes-v-exec`, against `origin/master`** (substrate written into the command at repair round 3, same rule as G-KF1-2/G-KF1-3): `git show origin/master:docs/tranches/V/coordination/INBOUND-LEDGER.md | grep -oE 'IN-[A-Z]+-[0-9]+' | sort -u` → **9 rows** (IN-ATLAS-1..5, IN-GLASS-1..2, IN-VALUE-1..2; re-run 2026-08-28); no IN-VALUE-3/4, nothing dated after 2026-07-17. Value-side: the newest keyframes-authored letter in our tree is `keyframes-inbox-2026-07-18-*` → silence **41 days** at 2026-08-28 (the census's "16 days" and the CARRY's "38 days" are dated, **non-inheritable** figures). O-8 unanswered **35 days**; O-11 **32 days**.
GREEN: the addendum contains the explicit minimal ask for `IN-VALUE-3`/`IN-VALUE-4` in their grammar. **The actual marking is NOT a close condition** — it is theirs, may never come, and gating on it makes the wave un-completable ⟨census (c)5⟩. Its absence is re-measured at close and recorded under I-26. Row: C-12.

**G-KF1-11 · SPEC HOME POPULATED** — ***DECLARED-SATISFIED · NOT born-RED, NOT a close gate this wave passes*** (L-19; the same relabelling G-KF1-12 receives, owed here and now applied)
The condition was RED at the instant of authoring and is **discharged by the existence of this file**. At wave-open it is already GREEN, and **no act inside `.a`, `.b` or `.c` can turn it** — a gate that cannot fail for its intended reason is not a gate, and dressing one as born-RED inflates the tally L-19 exists to keep honest. It is retained as a **declared condition with a dated record**, not deleted, because the filename reconciliation it carries is load-bearing for every sibling's tables.
Dated authoring record, **re-measured at repair round 2**: `ls docs/tranches/X/keyframes/waves/` → **eleven specs — this one and ten siblings** (`KF-W0` · `KF-W2` · `KF-W3` · `KF-W4` · `KF-W5` · `KF-W6` · `KF-W7` · `KF-W8` · `KF-W9` · `KF-W10`), every one self-naming `KF-W<N>.md`. Round 1 measured **seven** and named `KF-W5`/`KF-W6`/`KF-W9` as absent — **struck**: the set is now the whole eleven, and the figure is re-measured again at open rather than inherited (this is the third consecutive round in which a frozen sibling count went stale, which is the argument for measuring it and not carrying it). Immediately before the first write: `test -e …/KF-W1.md` → **ABSENT**; `test -e …/W1.md` → **ABSENT**.
State now (re-measured 2026-08-28, this repair seat): `test -e …/KF-W1.md` → **PRESENT** (`acff6265`); `test -e …/W1.md` → **ABSENT**. **SATISFIED** — `KF-W1.md` exists under the sibling convention, `Status: planned`, the four-verb block reading SPECIFIED=YES / IMPLEMENTED=NO / VERIFIED=NO, validated against `docs/precepts/instructions/tranche/WAVE_SPEC.md` (L-20). Re-measured at open and **recorded, never gated**. Row: C-14.

**G-KF1-12 · SACRED CHECKOUT UNTOUCHED** — *INVARIANT, declared, **not** born-RED* (it is GREEN at open and must stay green; labelling a stay-green guard as born-RED is the contrivance L-19 forbids)
Witness: keyframes' `docs/tranches/V/EXECUTION-HANDOFF.md:13-18` — "no mutating git, no npm operations there, ever". Checkable quantity, **MEASURE-AT-OPEN**: `git status --porcelain | wc -l` in `/Users/mkbabb/Programming/keyframes.js` → **252** (2026-08-28; the docs lane's 2026-08-03 figure was also 252 — the agreement is coincidence and is not to be inherited as an expected value), plus the two packet mtimes (Jul 24 16:41 / Jul 27 12:27).
GREEN: unchanged across the wave; every access is a read (`ls`, `git show`, copy **out**). Zero writes, zero git mutations, zero npm. Rows: C-1, C-11.

---

## 7. Sequencing

**Internal order (hard).** `.a` before `.b` (the addendum is derived from bodies whose liveness is a wave-open precondition — OP-1; C-11's SEQ-LOCK). `.b` before `.c` (G-KF1-7 is void ahead of G-KF1-1: a ledger that marks CURED before delivery reproduces I-26 one layer up). C-9 and C-10 land in the **same motion** — repairing the ledger without repairing the law that broke it ships the cure and keeps the cause, the class the registry books elsewhere as "two writers for one persisted flag". Standing E13 law: the wave opens with the four-path mail sweep; the amended path takes effect at the very next wave-open program-wide.

**Substrate law (carried from KF.W0 §B-12, binding here as everywhere)**: origin/master pins glass-ui `7.0.0` exact devDep = installed; local `8281638c` is stale — **every measurement in this wave re-baselines against `origin/master`**, never the sacred checkout.

**Cross-edges, declared from this end.**

1. **→ KF.W2 (Parse Façade) · KF.W3 (Parser Consumption, GATED) · KF.W4 (K-Quartet Hygiene) · KF.W10 (Fold Discharge — see cross-edge 11) — and every future kf wave consuming O-8/O-11.** Type: **PRECEDES (hard)**. This wave must close before any of them may cite an O-8/O-11 obligation ⟨COHESION §2 · census (c)6⟩. **Reciprocity, measured 2026-08-28 over all seven authored siblings** (§1, per RULINGS R-19d): **KF-W4** declares it (**§Preconditions OP-5** "OPEN — KF.W1's"; **§Sequencing, cross-edge row `KF.W1 · Mail Cure`** → PRECONDITION on anchor citability, cited by §-heading + row label alone, **numeric convenience STRUCK at round-2 close (U-2/R2-7)**); **KF-W3** declares it (**§Preconditions OP-5** UNLANDED · **L-4** · **§Cross-edges, row `KF.W1 · Mail Cure (O-8/O-11) + E13`** → DEPENDS ON, cited by §-heading + row label alone, **numeric convenience STRUCK at round-2 close (U-2/R2-7)**); **KF-W10** declares it three ways and hard-blocks (cross-edge 11). *(All four receipts re-resolved at repair round 2 under R2-7's stable-anchor idiom; the round-1 line coordinates `:206`/`:209` were dead — the edges themselves were live at every one, so R-19d is cured in kind and only its receipts drifted.)* **KF-W2 · KF-W7 · KF-W8 carry no KF.W1 edge** (bare-`KF.W1` count **0** in each; W2's `KF.W1` greps resolve to KF.W11/W13 prefixes). The COHESION §3.1 **cohesion mark** raised against KF-W2 was *conditional* — owed **if** it cites any O-8/O-11 obligation — and the condition is **measured 2026-08-28: FALSE** (`grep -c 'O-11' KF-W2.md` → **0**; its single `O-8` hit is the `KF-CO-8` substring **in its §Excluded row `KF-CO-1 / KF-CO-8`** (**numeric convenience STRUCK at round-2 close (U-2/R2-7)** — `:267`/`:103`/`:338` all drifted; the §-heading + row label are the anchor); KF-W7 and KF-W8 measure identically, 0/0). **The mark DISCHARGES on the grep** — nothing is owed from those three ends, and W2 is no longer singled out for a property its two siblings share. This wave hands anchors, never a façade design (LIB §4.1's 13-site inventory is KF.W2's).
2. **→ KF.W0 (Substrate Settle · §B-12; + intake CARRY X-1, the Codex v8 contract pinned to `8281638c`).** Type: **ASKS, does NOT depend on — RECIPROCITY CONFLICT recorded.** The addendum asks the **owner** to perform §B-12 ⟨lane-docs §7.2⟩; **HARD NEGATIVE: value.js must NOT perform §B-12** ⟨DOCS §6 row 16⟩; this wave is deliberately completable without it — it anchors on `origin/master` and delivers to the exec clone. **Re-measured at repair round 2: KF-W0 declares the REVERSE edge at two stable anchors** — **§Sequencing, the OUTBOUND table row `KF.W1 · Mail Cure`** (*"W1's re-delivery depends on this wave making them visible and on the re-anchored counts replacing the stale ones."*) and **§Excluded, the row `KF.W1's mail re-delivery`** (*"Depends on this wave, is not performed by it."*). Both quotations verbatim at their source; **the edges ride the two §-headings + row labels alone**. ⟨**Round-3 correction (RULINGS-3 `(c)(1)`; checks **D3-3**/**D3-4**), stated not silently applied (E-3).** This cross-edge carried the coordinates *"at `:393`"* and *"at `:431`"* — **both dead and both STRUCK**: per `PASS-3/KF-W1-CHECK.md` §6 D3-3 the OUTBOUND row is at `:439` (`:393` is a §Gates falsifier) and the §Excluded row is at `:477` (`:431` is a §Sequencing lock). Worse than dead, they **contradicted §1's own KF-W0 bullet**, which had already struck the same two coordinates by name — one file, two verdicts on the same anchors (D3-4). The direction and force of the edge are unchanged at every reading; only the receipts drifted.⟩ That claim contradicts this CARRY's design lock (the exec-clone path exists precisely so the mail cure never queues behind the owner's hand). **The conflict is recorded for the COHESION §3 check and the owner's ruling — neither spec silently rewrites the other.** This spec stands on its CARRY: KF.W1 opens on the begin-word, not on KF.W0.
3. **→ KF.W0 (census F-1 / SCH-1 identity).** Type: **ROUTES-AWAY (namespace disambiguation).** See the alias block and C-13. Zero `KF.W1-DEP` rows land here; KF.W0 receives the identity plus its five component riders, and the `a59d3a22` dissent rider is KF.W0's re-measurement.
4. **→ KF.W4 (born-RED gates G-L7a/b/c/d).** Type: **SUPPLIES ANCHORS.** KF.W4's gates consume the addendum's HEAD-resolved table, never the packets' stale anchors ⟨census §0 formation law: "no wave spec may cite a `file:line` until re-anchored at `origin/master`"⟩. `G-L7a–d` and `K1–K4` are anti-rename. Reciprocated (`KF-W4.md` OP-5).
5. **→ X-W9 (value's dated 4.1 cut wave, `CARRY-CUT-LEDGER.md:255` — line re-verified 2026-08-28).** Type: **SHARED CLOSURE LINES.** The CC-084 vehicle-withdrawal and the D-GAP-6 "not adopted" line ride **both** this addendum and X-W9's cut packet, declared from both ends. The addendum does not replace the cut packet and promises no cut date.
6. **→ X·P (parse-that) release condition.** Type: **NON-EDGE, declared.** This wave carries no parser schedule. PLAW-BIND routing (parser → value → packed release → consumers) and the OWNER-GATED bench bar mean the letter states the ruling (CC-084) and nothing about timing. Direct parse-that→consumer promises are forbidden.
7. **→ SS-6 (glass-ui BH communique).** Type: **NON-EDGE, declared.** Zero glass-producer rows here; intake §3 NEXT-COMMUNIQUE is **EMPTY**. The standing BH-relay edict is satisfied by declaration; **no rider is minted.** The glass-producer rows found elsewhere in X·KF (LabeledSelect `open` / LabeledSwitch `modelValue` `default: undefined`; the `useTouchGate` styling contract; the standard-variant Slider thumb; the D-18 collapsed-summary a11y half) ride SS-6 **from their own waves**, never from here and **never as demo-side hacks**.
8. **→ keyframes.js (external; no owning formation on their side — census (c)5).** Type: **OPEN OBLIGATION, non-gating.** The `IN-VALUE-3`/`IN-VALUE-4` marks and any reply are theirs. We deliver and ask; we do not close on their answer. Tracked as an OPEN sub-row under I-26; silence re-measured at close (41 days at 2026-08-28), never inherited. **The addendum says plainly that the addressee repo's successor formation is this megatranche — its executor is us-adjacent** — rather than assuming a keyframes agent will act.
9. **→ KF.W11 (Demo Scene Repair) · KF.W12 (Authoring-Surface Repair) · KF.W13 (Chrome, Dock & Transport Repair) · KF.W6 · KF.W7.** Type: **HOMES NONE — routing declaration.** This wave homes **zero** of the **17** NO-WAVE-OWNER packets — the **canonical roster per RULINGS R-15**, receipted in full at C-14 leg (iii) and partitioned by the homing authority **KF.W4**:
   - **KF.W11 · Demo Scene Repair (9)** — cube · sequence · spring · spring-plot · spring-physics-facet · spring-artifact-truth · square · **amiga** · **drag-seam**.
   - **KF.W12 · Authoring-Surface Repair (6)** — CARD-UNIT · OPTIONS-UNIT · KFED-UNIT · **EDITOR-UNIT** · APPLY-UNIT · AXISLINE-UNIT.
   - **KF.W13 · Chrome, Dock & Transport Repair (2, MINTED)** — **dock-menu** · **transport/ribbon**.

   **dock-menu is KF.W13's, not KF.W12's** — R-15 reconciles **KF-W2's §Excluded sibling-packet row** (*"The cube · sequence · spring · **dock-menu** · CARD-UNIT packets — routing restated on the canonical 17-packet roster (RULINGS R-15)…"* — **numeric convenience STRUCK at round-2 close (U-2/R2-7)**: `:375` had drifted onto a §Carry census-corrections note and round-1's `:270` was already dead; **the §-heading + the row's opening label are the sole anchor**) and this cross-edge, which both route it there. `transport/ribbon` is homed for the first time by that ruling. Each routes to the waves above ⟨COHESION §3.3 — no silent drops⟩; the cardinality **16** an earlier draft carried was never true in any document and is struck (C-14 leg (iii)). **COHESION FLAG, raised because the sequencing locks arrive with this wave**: KF.W11/W12/W13 **do not exist in the census sketch, which stops at KF.W10** — the X·KF spec set must **MINT them explicitly**, and §3.2's "no silent drops, no re-booking" is symmetric: a wave minted beyond the sketch needs the same declaration a killed sketched wave needs. **FLAG STATUS, 2026-08-28: DISCHARGED at the homing authority** — R-15 has KF.W4 mint **KF.W13 explicitly** and re-cut its homing lists over the canonical 17, which is the declaration this flag asked for. The flag is recorded as answered, not deleted. The locks that must travel with those homings, restated so they are not lost in transit: **comment-stated invariants are test obligations** (kf-SquareScene law; KF.W4's MISS-6 keeps only its non-waiting green) · **KF-CO-1/KF-CO-8's cures are SEQUENCED by LP-1** (no write→render edge) in their owning wave · **kf-ChromeDock M-4's worded cure ships an unopenable menu without the MbabbMenu MUST-CARRY rider** (KF.W13) · **TD-1/TD-2's cures bundle, unsplit**, in their owning wave · **glass-producer rows go to the BH relay (SS-6), never demo-side hacks** · **the KF-AV-28 supersession rider travels on the W7-evaluated surfaces** ⟨**added at repair round 3 — check D3-8**, stated not silently applied (E-3): §8's 17-packet row already listed this rider among the locks that travel with the homings, and this list — the one introduced as *"the locks that must travel WITH those homings, restated so they are not lost in transit"* — **enumerated five and dropped it**. A rider omitted from the **operative transit declaration** is a rider lost in transit, which is the one thing this list exists to prevent. KF-AV-28 is live across the sibling set (W9 ×11 · W6 ×10 · W7 ×7 · W2/W10 ×5 · W4/W3 ×4 · W8 ×2 · W0 ×1, per `PASS-3/KF-W1-CHECK.md` §6 D3-8), so the omission was local to this list, not to the program. The two enumerations now agree at **six** members⟩.
10. **→ COHESION §3.1 / §3.3 cohesion checks.** Type: **SET-DIFFERENCE ∅ (affirmative).** Re-verifiable from C-14's three enumerations without re-deriving the greps. **§3.1's mark against KF-W2 is discharged by measurement** (cross-edge 1); **§3.3's no-silent-drops obligation is met against the canonical 17** (cross-edge 9), not against an inherited 16.
11. **→ KF.W10 (Fold Discharge & Coordination Terminal, SS-2 per RULINGS R-18).** Type: **PRECEDES (hard) — the hardest downstream dependency this wave has.** Declared here because it was **missing** from the earlier reciprocity measurement and from this cross-edge list; cured per **RULINGS R-19d**. KF-W10 was on disk at this spec's authoring (born 11:43:22, 44 s before this file; committed `b6e09ed4`) and declares the edge **three times**, at three stable anchors re-resolved at repair round 2 (the round-1 coordinates `:16`/`:50`/`:296` are all dead — **struck**; every edge survives at its new home, same direction, same force): ⟨**§1 State, the `Opens after` line** — "**X.KF.W1** (Mail Cure — the O-8/O-11 amendment-addendum on an exec-visible path)" — *(`:20` at 2026-08-28, **the one surviving coordinate on this file's cross-spec receipts**, carried parenthetically and **declared non-load-bearing** per R2-7; ALIVE at that reading per `PASS-3/KF-W1-CHECK.md` §6 D3-3, and re-resolved at open)*⟩ ⟨**§Preconditions, the row `OP-3`** — the addendum's exec-visibility is a named open precondition, **UNLANDED**, and terminalizing a ledger whose packets were never delivered *"records a **false close** (G-6 depends on this, not the reverse)"* — **§-heading + row id `OP-3` are the sole anchor**⟩ ⟨**§Sequencing, the cross-edge `B. → KF.W1 (Mail Cure, SS-2)`** — *"**W10 depends.** No coordination terminalization (G-6) before the O-8/O-11 amendment-addendum lands on an exec-visible path."* — **§-heading + cross-edge label are the sole anchor**⟩. ⟨**Round-3 correction (RULINGS-3 `(c)(1)`; checks **D3-3**/**D3-4**), stated not silently applied (E-3).** This cross-edge carried *"at `:54`"* and *"at `:332`"* — **both STRUCK as dead**: per the PASS-3 check, `OP-3` is at `:59` (`:54` is **blank**) and the `B. → KF.W1` cross-edge is at `:382` (`:332` is **blank**). Both had already been struck **by name** in §1's own KF-W10 bullet as *"blank at the current bytes"*, so this file asserted as live what it had elsewhere declared dead — the D3-4 contradiction, cured here. Each edge survives at its stable anchor, same direction, same force (§2.1 of the check).⟩ Re-measured this seat: 8 × `O-8`, 3 × `O-11`, all real letter citations. **Direction is unambiguous and uncontested — unlike cross-edge 2, there is no conflict to record**: W10 depends on this wave, this wave depends on nothing of W10's. What this wave owes W10 is exactly what it owes everyone — the addendum at an exec-visible path plus the **I-26/O-21** ledger receipt (G-KF1-1, G-KF1-7 — the outbound id is `max+1` measured at open per C-10's MINT LAW, so W10 consumes the **row**, never a frozen literal), which is W10's OP-3 artefact check verbatim. **No new obligation, no re-scope, no new row** — the edge was always true; only its measurement was missing. W10's own ownership assignment (SS-2) is **R-18's ruling and the root session's act**, not this wave's to make or to consume.

---

## 8. Excluded

| excluded | reason + route |
|---|---|
| **The 13-site Tier-A inventory** (LIB §4.1) and any façade design | **KF.W2's object.** This wave hands over anchors, never a façade design ⟨C-2 lock⟩ |
| **§E2's `lerpArray` length assertion — scheduling it** | The addendum re-delivers the ask; it must not schedule it ⟨DOCS §6 row 2: "the value-side cure (W.L3 `./math` failure protocol) cannot reach them until the pin moves"⟩ |
| **Any re-ask, re-scope, or severity change on DOCS §6 rows 2–10 + 14** | The addendum amends two letters; it is not a third ⟨C-7 lock⟩ |
| **§B-12 (the `git fetch && git reset --hard origin/master` on the sacred checkout)** | **HARD NEGATIVE — value.js must NOT perform it.** Asked, not done ⟨C-8⟩; owned by KF.W0 |
| **All SIX `KF.W1-DEP` / census F-1 rows — across FIVE records — and their component riders**: `kf-AnimationControlsGroup.md:28` **and `:47`** · `kf-ChannelControls.md:56` · `kf-ControlsPaneWrapper.md:60` · `kf-DemoGlobalChrome.md:44` · `kf-KfPillTabs.md:46` | Different namespace, different wave (KF.W0 §B-12), **and falsified at HEAD** by SCH-1 ⟨C-13⟩. ⟨**Round-3 correction (check `D3-5`), stated not silently applied (E-3)**: this cell read *"All **five** `KF.W1-DEP` / census F-1 **rows**"* — **STRUCK**. It counted **records as rows** and **under-booked by one**: ACG carries **two** routing rows, and the dropped `:47` is the row that owns the `useControlsKeyboardShortcuts.ts:1` sole-binding-site rider. The header PROVENANCE cites **this cell** as its booking receipt, so a seat booking from this cardinality would have dropped **R-2 and its rider** while the header claimed six booked. C-13's *"five"* is correct — **it counts RECORDS** — and the two now agree instead of colliding. This is exactly the unreceipted-cardinality class this spec struck at C-14 leg (iii) (*"**16** was stated by no document in the tree"*), applied to fourteen figures and skipped for this one.⟩ |
| **Correcting the retained O-8/O-11 originals in place** | Verbatim retention is the point: "amend, do not re-send" ⟨C-11⟩ |
| **The kf-side `INBOUND-LEDGER` mark as a close condition** | Theirs to perform; gating on it makes the wave un-completable by construction ⟨C-12⟩ |
| **A cut date for 4.1** | X·P's release condition is gate-keyed and OWNER-GATED (COHESION §2 PLAW-BIND) ⟨C-3⟩ |
| **The 17 NO-WAVE-OWNER scene/authoring/chrome packets** — the canonical roster **per RULINGS R-15**, whole: cube · sequence · spring · spring-plot · spring-physics-facet · spring-artifact-truth · square · amiga · drag-seam · CARD-UNIT · OPTIONS-UNIT · KFED-UNIT · EDITOR-UNIT · APPLY-UNIT · AXISLINE-UNIT · dock-menu · transport/ribbon | **Homing authority is KF.W4** (R-15), which partitions them **KF.W11 ×9 / KF.W12 ×6 / KF.W13 ×2 (minted)** per cross-edge 9; **dock-menu and transport/ribbon ride KF.W13**. Their riders travel with them (MbabbMenu MUST-CARRY at KF.W13 · LP-1 sequencing · TD-1/TD-2 bundle · kf-SquareScene test-obligation law · the KF-AV-28 supersession rider on the W7-evaluated surfaces). The earlier cardinality **16** is struck as unreceipted ⟨C-14 leg (iii)⟩ |
| **Any SS-6 rider, and any glass-ui touch** | Zero producer rows survived adjudication in any lane; the edict is discharged by declaration ⟨C-14, cross-edge 7⟩ |
| **Mail tooling — a routing script, a path linter, a delivery cron** | KISS / L-19. The cure is a two-line law edit ⟨C-9 sizing lock⟩. No cron: standing owner order |
| **Any registry row, gate, or bounds item manufactured to fill the empty column** | A wave with no adjudicated rows gets no invented ones. The ∅ is a finding ⟨C-14⟩ |
| **Any product source in either repo; the census bodies; `scripts/dev/dev.sh`** | Out of bounds by §4 and by the execution gate |

---

## 9. Dated authoring-time findings

**Framing restated at repair round 2 (RULINGS-2 R2-1 item 6, running RULINGS R-12's general rule here).** These six are **dated findings of this seat's own measurement**, not "corrections to a CARRY": the SS-2 carry ledger they were framed against **is not in the tree** (`find docs -iname '*CARRY*'` under `X/keyframes/` → one file, `carry/KF-W6-CARRY.md`, which is W6's), and R-12 forbids citing a ledger that does not exist as a source. **Content is preserved whole (E-3)** — only the provenance framing changes: each item now states what this seat **measured**, and the superseded reading is quoted **as the reading corrected, never as an authority**. Recorded rather than silently applied, because silent drops and silent re-counts are the defect class this program exists to kill. **All figures re-measured at repair round 2 (2026-08-28)** — keyframes-side at `origin/master` `81a56990`, value-side at the current tree bytes.

1. **Spec home** — measured: `docs/tranches/X/keyframes/waves/` holds **eleven** specs (this one + `KF-W0` · `KF-W2` · `KF-W3` · `KF-W4` · `KF-W5` · `KF-W6` · `KF-W7` · `KF-W8` · `KF-W9` · `KF-W10`), all self-naming `KF-W<N>.md`; `W1.md` **ABSENT**. Bounds reconciled to **`KF-W1.md`**; `W1.md` never created. Superseded readings, recorded as corrected: *"`waves/W1.md`, parent dir ABSENT (2026-08-25)"* · the two-sibling draft reading · round 1's **seven**. (G-KF1-11's record restated accordingly — and, at repair round 1, the gate **relabelled DECLARED-SATISFIED** and the Bounds grant moved **create → modify**, since the file now exists.) **Three rounds, three stale counts: the figure is measured at open, never carried.**
2. **Retention census** — measured: **7 files, 07-19 ×1 and 07-20 ×6; no 07-21.** Superseded reading, recorded as corrected: *"07-19, 07-20 ×5, 07-21 present"*. The load-bearing fact is unchanged: 07-24 and 07-27 absent.
3. **Drift-table row count** — `lane-docs §6` (in tree) banks **12 rows**. Re-measured: **14 anchor rows** (9 drifted + 5 exact); `emit/backward-color.ts`'s three line anchors are three rows against one path rename.
4. **Dated intervals re-measured** — silence since the newest keyframes-authored letter (`keyframes-inbox-2026-07-18-*`): **41 days** (`CENSUS-2026-08-03.md`, in tree, banks "16 days" — a dated figure). O-8 unanswered **35 days**; O-11 **32 days**. Superseded reading, recorded as corrected: *"38 days at 08-25"*. All figures are dated and non-inheritable; the executing seat re-measures again.
5. **Reciprocity measured over the WHOLE authored sibling set, seven not four** ⟨RULINGS **R-19d**⟩. The superseded cross-edge sketch named KF.W2/W3/W4 and KF.W0 as owing reciprocation once authored; all four are authored — **and so are KF-W7, KF-W8 and KF-W10** **and, at repair round 2, KF-W5 · KF-W6 · KF-W9 as well — ten siblings on disk.** Re-measured this seat over all ten: bare-`KF.W1` token counts `grep -oE 'KF\.W1([^0-9A-Za-z-]|$)'` → **KF-W0 4 · KF-W3 5 · KF-W4 2 · KF-W10 8** (the four declared consumers) and **0 for KF-W2 · KF-W5 · KF-W6 · KF-W7 · KF-W8 · KF-W9**. **The three new siblings add no KF.W1 edge and consume no O-8/O-11 obligation**: `grep -c 'O-11'` → **0** in all three, and every `O-8` hit is the `KF-CO-8` substring — each inspected line-by-line, all LayerConfigPanel/ChannelOptions switch-clobber sequencing rows. **Both metrics are now stated separately, because no single command yields a mixed triple** ⟨**round-3 correction, check `D3-9`, stated not silently applied (E-3)**: this parenthetical read *"(KF-W5 ×1, KF-W6 ×2, KF-W9 ×5)"* — **STRUCK as a MIXED enumeration**. W5's 1 and W9's 5 are `grep -o 'O-8' | wc -l` **occurrence** counts; W6's 2 is a `grep -c 'O-8'` **line** count; the triple reproduces under **no** command. The load-bearing claim was, and remains, TRUE.⟩ Re-measured this seat over all **six** non-consumers, both metrics: **occurrences** — KF-W2 **1** · KF-W5 **1** · KF-W6 **4** · KF-W7 **2** · KF-W8 **0** · KF-W9 **5**; **lines** — KF-W2 **1** · KF-W5 **1** · KF-W6 **2** · KF-W7 **2** · KF-W8 **0** · KF-W9 **3**. `grep -c 'O-11'` → **0** in all six, and the bare-`KF.W1` token is **0** in all six. **So the COHESION §3.1 conditional mark discharges on the grep across all SIX non-consumers** (W2 · W5 · W6 · W7 · W8 · W9), not the three round 1 could measure, and the reciprocity set stands unchanged at four declared consumers plus KF-W0's recorded reverse edge. An earlier draft measured four and enumerated seven; **the measured set is the authored set, now ten.** Re-measured: KF-W3 and KF-W4 reciprocate exactly as owed (**§Preconditions OP-5** at both, plus the §Cross-edges/§Sequencing rows named in §1); **KF-W10 reciprocates hardest of all and was MISSED at round 1** — three explicit dependencies (**§1 `Opens after`** · **§Preconditions `OP-3`** UNLANDED, "false close" · **§Sequencing cross-edge `B. → KF.W1`**) with 8 × `O-8` and 3 × `O-11` real letter citations, i.e. precisely *"a kf wave consuming O-8/O-11 obligations"*, the class this wave declares itself to precede — now carried at **cross-edge 11**; **KF-W0 declares the REVERSE dependency edge** at its **§Sequencing OUTBOUND row** and its **§Excluded row** (conflict recorded, cross-edge 2 — for the COHESION §3 check and the owner, not for either spec to rewrite the other). **All cross-spec receipts in this item are stable anchors per R2-7**; the round-1 line coordinates were dead at every one and are struck, the edges themselves unchanged.
6. **Every remaining figure RE-EXECUTED at repair round 2, and split by what it measures** (R2-5: a witness a seat leaves standing without re-running is that seat's defect).

   **(a) Keyframes-side, re-derived at the frontier of record `origin/master` `81a56990` — ALL HOLD, byte-exact**: clone HEAD `81a56990` ✓ · the **9-entry** coordination listing + `vnext/`, no addendum ✓ · the three `parseStylesheet` **call sites** (`compile/adapter.ts:222` · `scroll/grammar.ts:109` · `validate.ts:182`) and their three **imports** (`:7`/`:37`/`:47`) ✓ *(the raw grep's full output is nine lines — see G-KF1-3)* · the **62**-import split (`/css` 29 · `/value` 16 · `/color` 7 · `/math` 5 · `/easing` 3 · `/transform` 2) ✓ · the **9-row** `INBOUND-LEDGER` (IN-ATLAS-1..5 · IN-GLASS-1..2 · IN-VALUE-1..2), no IN-VALUE-3/4 ✓ · `package.json:77` glass-ui `"7.0.0"` exact — SCH-1's falsifier, F-1 FALSIFIED-AT-HEAD ✓ — and `:70` value.js `"4.0.0"` pin ✓ · the three ABSENT packet anchors (`compile/value-ast.ts` · `compile/easing/easing-registry.ts` · `compile/emit/backward.ts`) ✓ · `resolve/browser.ts:162` ✓ · every §5a row incl. **row 14 now measured** (`test/internal/leaves-parity.test.ts` **PRESENT**), `emit/backward/` holding exactly `backward.ts, color.ts, index.ts, walk.ts`, and `color.ts` at **385** lines.

   **(b) Sacred checkout (read-only) — HOLDS, and at round 3 it is re-labelled as a WORKTREE-STATE RECORD, not a sha**: HEAD `8281638c` + `41  1` behind/ahead ✓ + **252** dirty ✓ + both packet bodies present at **7,064 B** (mtime Jul 24 16:41) and **15,633 B** (mtime Jul 27 12:27) ✓ — **those four figures together are the substrate name**, because the tree the packets measured is the uncommitted worktree and no sha addresses it (§5b). **Added at round 3**: the worktree import census **61** (`/css` 29 · `/value` 15 · `/color` 7 · `/math` 5 · `/easing` 3 · `/transform` 2) — byte-identical to O-11 `:52` on all six lines ✓ · the same census at bare `8281638c` = **81** over a disjoint alphabet ✓ · and the **nine** drifted-or-absent §5a anchors resolved in the worktree at the packets' own coordinates (`value-ast.ts:71` · `easing-registry.ts:36` · `backward.ts:47` · `backward-color.ts:171`/`:250`/`:263` · `browser.ts:165` · `package.json:69` · `easing-serialize.ts:71` bracketed by the packet's `:70-71`) ✓. **This bucket is why the wave's causal account changed at round 3** (§1 Goal criterion; C-5; C-6).

   **(c) Value-side in-tree citations — HOLD**: `CARRY-CUT-LEDGER.md:186` (CC-084 *"no emergency 4.0.1 — ruled"*) and `:255` (X-W9) ✓ · `library-band.md:205` (*"DECLINE — `sampleBezier`"*) ✓ · the **58**-record adjudicated corpus with **18** `KF.W1-DEP` anchors across **5** records, bare-`KF.W1` token **0**, literal substring **33** = 18 + 15 `KF.W10` ✓ · both `W1.md` and `KF-W1.md` ABSENT immediately before the first write (a dated authoring record — `KF-W1.md` PRESENT thereafter; G-KF1-11).

   **(d) Value-side `INBOX.md` — THE ONE AXIS THAT MOVED, and the whole of round 2's blocker.** The file is **97 L / 56,702 B**, clean in git, last written by `7a7dc6ef` (2026-08-28 12:33:59) after `48003630` (12:32:16) — **both landing before round 1's 12:41 write, neither read by it.** Four round-1 figures are **STRUCK** and restated: *"`:13-14`"* → the keyframes sweep-path law at **`:15-16`** (`:13-14` is now the glass BI→BJ→BK note) · *"`:45` (I-10)"* → **I-10 at `:47`** (`:45` is I-7) · *"`:90` (I-26 "ROWED 2026-08-03")"* → **I-26 at `:92`** (`:90` is I-21a), text verbatim · *"outbound max **O-19**"* → **max `O-20`** (the SS-6 batched glass communique at `:95`). The commissioned mint moves **O-20 → O-21** at every site, under C-10's MINT LAW. **Row ids are the anchors from here on; the line numbers ride as convenience and are re-resolved at open.**

---

## 10. Repair round 1 (X·KF L-20 round 1) — what changed and on whose authority

Recorded, not silently applied, on the same law §9 states. Inputs: `conformance/PASS-1/RULINGS.md` (cross-wave authority; where a local defect and a ruling collide, **the ruling wins**) and `conformance/PASS-1/KF-W1-CHECK.md` (local register: verdict DEFECTIVE — 3 MAJOR · 3 MINOR · 2 INFO, census CLEAN at 6 routed / 6 booked / 0 escaped, 0 inventions). **No carry row was added, renamed, split or dropped: C-1..C-14 stand, ids untouched (anti-rename). Net row delta 0.** Nothing here stamps a verb, authorizes execution, or opens product source.

| # | authority | change |
|---|---|---|
| 1 | **RULINGS R-15** (+ check D-3, MAJOR) | The NO-WAVE-OWNER cardinality **16 → 17**, the canonical roster, **receipted id for id** at C-14 leg (iii), cross-edge 9 and §8. 16 was stated by no document in the tree; the reconciliation is **KF-W0's** then-stale 15 ∪ {amiga, EDITOR-UNIT} (**KF-W9 S-11's** adds) — *[receipt re-anchored at round 2: **KF-W0 §Sequencing, the packet-roster line `the NO-WAVE-OWNER packets — the canonical 17`** — **numeric convenience STRUCK at round-2 close (U-2/R2-7)** (`:401` had drifted onto a §Carry carries-note; the roster line label is the sole anchor); round 1's `KF-W0.md:276` is dead]*. Cross-edge 9's membership restated canonically — **KF.W11 ×9 · KF.W12 ×6 · KF.W13 ×2 (minted)**, with **dock-menu at KF.W13**, homing authority **KF.W4**. The cohesion FLAG this cross-edge raised is marked **DISCHARGED** at that authority, recorded not deleted. |
| 2 | **RULINGS R-19d** (+ check D-1, MAJOR) | Reciprocity re-measured over **all seven** authored siblings, not four: §1's paragraph, cross-edge 1 and §9 item 5 gain **KF-W10**, and **cross-edge 11** is declared for it with its three hard-blocking anchors (**§1 State's `Opens after` line · §Preconditions row `OP-3` · §Sequencing cross-edge `B. → KF.W1` — **numeric convenience STRUCK at round-2 close (U-2/R2-7)**; the round-1 spellings `:16`/`:50`/`:296` are dead and stay struck, as this file already records two bullets above**) and its 8 × `O-8` / 3 × `O-11` receipt. The measured set is now the authored set (W0 · W2 · W3 · W4 · W7 · W8 · W10). |
| 3 | check **D-2** (MAJOR) | Born-RED tally corrected **11 → 10** at wave-open. **G-KF1-11 relabelled DECLARED-SATISFIED** (the treatment G-KF1-12 already had and this gate was owed): its condition is discharged by this file's existence, so no act in `.a`/`.b`/`.c` can turn it. The §4 Bounds grant for this spec moves **create → modify** (the file exists), and `:194`'s "turned GREEN by this file's landing" — an execution-voice claim in a `planned` spec — is gone. |
| 4 | check **D-4** (MINOR) | C-14 leg (ii): the range spelling `B10-17..27` replaced by the intake's own **enumeration with gaps** ⟨`INTAKE-ADJUDICATION-2026-08-03.md` **§3, heading `### KF.W4 · K-Quartet Hygiene`** — *[receipt re-anchored at round 2: `:146`/`:147-149`; round 1's `:144-145` is the tail of the §3 **KF.W0** block and is dead]*⟩, which no longer routes `B10-21` to KF.W4 and KF.W5 in one sentence. |
| 5 | check **D-5** (MINOR) | The alias block's *"once and for all"* made true: the **third live `W1` namespace** — keyframes' own `BUILD W1` (`DISPOSITIONS.md`), six corpus anchors, all three identities folding to **KF.W0** — is declared. Nothing escaped at either state; the mapping is now closed over all three meanings. |
| 6 | check **D-6** (MINOR) | The alias block's anchor census completed **13 → 18**: each record's ADJUDICATED-summary line (`:135` · `:155` · `:149` · `:130` · `:122`) added. Record set (5) was correct throughout. |
| 7 | check **D-7** (INFO) | C-14 leg (i) re-worded to survive the grep it invites: literal `KF.W1` = **33** (18 `KF.W1-DEP` + 15 `KF.W10` substrings); the load-bearing measurement is the **token** — bare `KF.W1` = **0** across 58 records. Substance unchanged. |
| 8 | check **D-8** (INFO) | The COHESION §3.1 cohesion mark's own condition **measured instead of asserted**: KF-W2 cites no O-8/O-11 (`grep -c 'O-11'` → 0; its one `O-8` is `KF-CO-8` **in its §Excluded row `KF-CO-1 / KF-CO-8`**, **numeric convenience STRUCK at round-2 close (U-2/R2-7)** — `:267` drifted), so the conditional is FALSE and the mark **DISCHARGES**. KF-W7 and KF-W8 measure identically and are named, so W2 is no longer singled out. |

**Not changed, and why.** The check's §8 findings stand as recorded and needed no edit: the ∅-column finding (C-14) is independently re-derived, the eleven reproducible gate witnesses are byte-exact, the §5a drift table reproduces row for row, every quoted lock is verbatim at its anchor, and every dissent (`a59d3a22`; the KF-W0 reciprocity conflict; C-4's recorded asymmetry) remains **carried, not resolved**. **L-19 holds**: no gate is a script, no row was manufactured to fill the empty column, and the one gate that could not fail was relabelled rather than kept for the count.

*(§10 is a **dated round-1 record**. It is not rewritten at round 2; where one of its receipts pointed at a line that has since moved, a bracketed `[receipt re-anchored at round 2: …]` marker is added beside the original spelling, which is preserved. Every substantive correction round 2 makes is recorded in §11 below, never by editing this table.)*

---

## 11. Repair round 2 (X·KF L-20 round 2) — what changed and on whose authority

Recorded, not silently applied, on the same law §9 and §10 state. **Inputs**: `conformance/PASS-2/RULINGS-2.md` (cross-wave authority; where a local defect and a ruling collide, **the ruling wins** — except on a *measurement*, where **R2-5** makes the tree outrank the ruling's prose) and `conformance/PASS-2/KF-W1-CHECK.md` (local register: verdict **DEFECTIVE — 1 BLOCKER · 4 MAJOR · 3 MINOR · 1 INFO**; census **CLEAN** at 6 routed / 6 booked / 0 escaped; **0 inventions**; every keyframes-side witness byte-exact). **No carry row was added, renamed, split or dropped: C-1..C-14 stand, ids untouched (anti-rename). Net row delta 0.** No gate was added or removed: the tally stays **10 born-RED · 1 DECLARED-SATISFIED · 1 stay-GREEN INVARIANT**. Nothing here stamps a verb, authorizes execution, or opens product source; every probe was a read-only `git`/`grep`/`ls`/`sed` command.

| # | authority | change |
|---|---|---|
| 1 | **RULINGS-2 R2-1 items 1–3** (+ check **D2-1**, BLOCKER) | **The commissioned outbound mint moves `O-20` → `O-21`** at all five load-bearing sites (§1 hard-gate condition 7 · §3 item 6 · §4 Bounds · C-10 (ii) and its ANTI-RENAME pointer clause · G-KF1-7's title and GREEN) **and at its trailing citation** (cross-edge 11's ledger-receipt clause). Re-measured this seat: `INBOX.md`'s maximum outbound row is **`O-20`** — the SS-6 batched glass-BK communique at `:95`, **SENT 2026-08-28**, minted by `48003630` (12:32:16), nine minutes before round 1's 12:41 write. Executing round 1's text would have written a **duplicate live id** into the E13 ledger and back-pointed O-8/O-11 at it — I-26 reproduced in the ledger layer, under the spec whose thesis is I-26. **G-KF1-7's witness re-cut**: the max-grep's stated output is now **`O-20`** (round 1 said `O-19` — struck). |
| 2 | **RULINGS-2 R2-1 item 2** | **The MINT LAW is written into C-10**: the commissioned id is **`max+1` measured at open**, never a spec-frozen literal; the executing seat re-runs the max-grep at open and, if the ledger has moved, mints `max+1` and records the delta as a dated addendum **before** executing. The id also stops being load-bearing downstream — cross-edge 11 now hands KF.W10 *the row*, not the literal. |
| 3 | **RULINGS-2 R2-1 item 4** (+ check **D2-2**, MAJOR, and **D2-8**, MINOR) | **The three dead `INBOX.md` anchors re-keyed at every citing site** — the four-verb AUDITED cell, §3 item 5, §4 Bounds, C-1, C-4, C-9, G-KF1-6, G-KF1-7, G-KF1-8, §9 item 6: the keyframes **sweep-path law** `:13-14` → **`:15-16`** · **I-10** `:45` → **`:47`** · **I-26** `:90` → **`:92`**. §4 Bounds' three-edit grant is re-aimed accordingly. **Row ids are now the anchors**; line numbers ride as convenience and are re-resolved at open. **G-KF1-8's witness narrowed** so the command produces its stated reading: `grep -n '^- \`\.\./keyframes\.js' …` → exactly **1 hit** at `:15`. |
| 4 | **RULINGS-2 R2-5** (the witness re-derivation law) **over R2-1 item 4** | **One departure from the ruling's prose, declared.** R2-1 item 4 states the broad grep `grep -n 'keyframes.js/docs/tranches' INBOX.md` returns **4 lines** (`:15`/`:66`/`:72`/`:75`). **Re-executed at the current bytes it returns FIVE** — the ruling's enumeration omits **`:68`**, the **O-4** WL-verdicts outbound path cell (in the ledger since 2026-07-17). R2-5 is explicit that *"a ruling orders a cure; it is never a source of measurements,"* so **the measured five is written** and the delta is recorded here for the pass-3 seat. The cure R2-1 ordered is unaffected: the gate carries the narrowed 1-hit pattern, with the broad form admitted only when stated as "5 hits, the law at the first." |
| 5 | **RULINGS-2 R2-1 item 6** (running **RULINGS R-12**'s general rule here) (+ check **D2-3**, MAJOR) | **The phantom "SS-2 CARRY" provenance is STRUCK** at all five load-bearing sites (`:7` the header · the SPECIFIED evidence cell · §4 Bounds' create-grant row · §5's preamble · §9's framing and its six items). Provenance is restated: **authored from the 58-record adjudicated registry directly**, with the routed denominator the registry's — **6 routed / 6 booked / 0 escaped**, re-derived this seat. `find docs -iname '*CARRY*'` under `X/keyframes/` returns exactly one file, `carry/KF-W6-CARRY.md`, which is **W6's**. §9's six items are re-labelled **dated authoring-time findings** with content preserved whole (E-3); each superseded reading is quoted **as the reading corrected, never as an authority**. **No ledger absent from the tree is cited as a source in this file again**, and no `N of N rows carried` claim is keyed to one. |
| 6 | **RULINGS-2 R2-1 item 5** (+ check **D2-5**, MAJOR) | **C-9 gains its own precedent**, one sentence with its receipt: commit **`7a7dc6ef`** (2026-08-28 12:33:59, seven minutes before round 1's write) cured the **glass limb** of C-9's exact defect class in this very file — a codified sweep path pinned to a dead tranche — and recorded the cost in-row: *"The stale BI pin here cost 19 days on the 08-09 letters → I-28/I-29."* Two limbs of one law-level defect, one already cured at a measured 19-day cost: corroboration of C-9's thesis and **the template for its two-line cure**. **No new obligation, no scope change** — and no UNREAD-mail breach follows, since I-28/I-29 are glass rows routed to the X·V fold, outside this wave. |
| 7 | **RULINGS-2 R2-7** (the citation idiom) (+ checks **D2-4** MAJOR, **D2-6** and **D2-7** MINOR) | **Every cross-spec receipt this seat touched is converted to a STABLE ANCHOR** — §-heading, gate id, OP id, or table-row label — with any line number riding parenthetically and declared non-load-bearing. Converted and re-resolved this seat: **KF-W4** §Preconditions OP-5 + §Sequencing row `KF.W1 · Mail Cure` (`:206`→`:242`) · **KF-W3** §Cross-edges row `KF.W1 · Mail Cure (O-8/O-11) + E13` (`:209`→`:220`) · **KF-W10** §1 `Opens after` (`:16`→`:20`), §Preconditions `OP-3` (`:50`→`:54`), §Sequencing `B. → KF.W1` (`:296`→`:332`) · **KF-W0** §Sequencing OUTBOUND row + §Excluded row (`:269`/`:306`→`:393`/`:431`), §Sequencing packet-roster line (`:276`→`:401`), §Sequencing KF.W4 OUTBOUND row's gap-preserving `B10` transcription (`:271`→`:394`) · **KF-W2** §Excluded rows (`:270`→`:375`, `:267`→`:428`) · **KF-W7** §Excluded row + N-10 paragraph (`:103`/`:338`→`:355`/`:111`) · **`INTAKE-ADJUDICATION-2026-08-03.md` §3** heading `### KF.W4 · K-Quartet Hygiene` (`:144-145`→`:146`/`:147-149`). ⟨**ROUND-2 CLOSE STAMP (U-2 re-anchor sweep, 2026-08-28).** This crosswalk is preserved as the round-2 conversion *record* and is not re-numbered. **Every right-hand coordinate above is now RETIRED, not re-issued**: the sibling specs were themselves repaired in the same round, and re-measured at this close only the `INTAKE-ADJUDICATION` pair (`:146`/`:147-149`) and **KF-W10 §1's `Opens after` line** still resolve — the other nine drifted again within the round, which is the argument for the idiom in one fact. **Every edge is now carried at its §-heading + row label alone.** The four `KF-W7` coordinates (`:355`/`:111`) resolved at this sweep but are retired with the rest, on the same rule.⟩ ⟨**STAMP CORRECTED at repair round 3 — RULINGS-3 LAW B + `(c)(1)`, check `D3-3`.** The clause that stood here — *"with **NO line number anywhere** on this file's cross-spec receipts"* — is **STRUCK**. It was a **completeness claim in spec voice**, and `conformance/PASS-3/KF-W1-CHECK.md` §6 **D3-3** (2026-08-28) falsified it against this file's own bytes: **five** coordinates survived the sweep, **four of them dead** (`:393`→ the row is at `:439` · `:431`→`:477` · `:54`→`OP-3` at `:59` · `:332`→`:382`) and **one alive** (`:20`). Round 3 struck the four at cross-edges 2 and 11 and left `:20` carried **openly** as a dated, declared non-load-bearing convenience. **The state of this axis is certified nowhere in this spec's voice** — it is whatever the freshest conformance census artifact measures, presently that check, re-pointed at pass 4. The lesson R2-7 stated and this stamp broke: *carrying coordinates openly is honest; certifying zero while carrying five is not.*⟩ **Every edge survives at its new home in the same direction and with the same force** — R-19d and the D-4/R-15 substance were cured in kind at round 1; only their receipts had drifted. The residue is the **Re-anchor seat's**, which writes LAST under R2-7's ordering law. |
| 8 | **RULINGS-2 R2-5** (+ check **D2-9**, INFO) | **§5a row 14 is measured**: `git cat-file -e 81a56990:test/internal/leaves-parity.test.ts` → **PRESENT**. It was the file's one figure marked *"inherited … not re-measured here"*, under a header whose own law is *"anchors are re-resolved at authoring, never copied."* **The executing seat now inherits nothing.** |
| 9 | **RULINGS-2 R2-5**, applied to witnesses this seat touched | **G-KF1-3's witness states its command's true output**: the `parseStylesheet` grep returns **nine** lines, not six — the six load-bearing code lines (3 call sites + 3 imports) **plus three prose mentions** (`adapter.ts:202` · `validate.ts:41` · `validate.ts:130`); a narrowing is supplied. **C-3 and G-KF1-5's O-8 anchor is corrected `:100-102` → `:101-103`** (`:100` is blank), re-measured read-only in the sacred checkout. **G-KF1-11's sibling count `seven` → `eleven` specs** (this one + ten siblings, `KF-W5`/`KF-W6`/`KF-W9` landed since round 1) — the third consecutive round in which a frozen sibling count went stale, so the figure is now declared measure-at-open. |
| 10 | **RULINGS-2 R2-5**, the reciprocity extension | **The COHESION §3.1 conditional mark is re-measured over the three new siblings** and discharges wider than round 1 could see: bare-`KF.W1` token is **0** in KF-W5 · KF-W6 · KF-W9 (as in W2 · W7 · W8), `grep -c 'O-11'` is **0** in all three, and every `O-8` hit in them is the `KF-CO-8` substring — each line inspected this seat. **The mark discharges across all SIX non-consumers**; the consumer set is unchanged at four (W0's reverse edge recorded, W3/W4/W10 declaring forward). |

**Witnesses re-derived at this repair (R2-5).** **Forty-four**, each by a read-only command run at write time, in three buckets.

**(i) Twenty-four keyframes-side**, at the frontier of record `origin/master` **`81a56990`** (never at `8281638c`, which was read only to date the drift): the frontier sha · sacred-checkout HEAD `8281638c` · `41 1` behind/ahead · 252 dirty · the 9-entry coordination listing · the 9-row INBOUND-LEDGER · the three ABSENT packet anchors · `browser.ts:162` · `package.json:70` · `package.json:77` · the three `parseStylesheet` call sites + three imports with the nine-line raw output · the 62-import split · `compile.ts:32` · `registry.ts:36` · `easing-serialize.ts:71` · the `emit/backward/` four-file listing · `color.ts` 385 L · `options.ts:31` · `leaves.ts:28` · `load-engine.ts:65` · `css-text.ts:41` · **row 14 `leaves-parity.test.ts` PRESENT** · the two sacred-checkout packet bodies (7,064 B / 15,633 B with mtimes) · O-8's `:101-103`.

**(ii) Thirteen value-side**, at the current tree bytes: `INBOX.md` 97 L / 56,702 B · max outbound `O-20` at `:95` · the sweep-path law at `:15-16` · I-10 at `:47` (I-7 at `:45`) · I-26 at `:92` (I-21a at `:90`) · the broad grep's **five** hits against the narrowed grep's **one** · commits `48003630` and `7a7dc6ef` with their timestamps · the 7-file retained set · the 0-hit back-fill `find` · `CARRY-CUT-LEDGER.md:186`/`:255` · `library-band.md:205` · the 58-record corpus census (18 / 0 / 33 / 15, with the 5 preamble + 6 routing + 5 summary + 2 rider anchor decomposition) · the 11-spec sibling set with the ten siblings' `KF.W1` / `O-8` / `O-11` counts.

**(iii) Seven sibling-spec anchor re-resolutions** (R2-7): KF-W4 · KF-W3 · KF-W10 · KF-W0 · KF-W2 · KF-W7 · `INTAKE-ADJUDICATION-2026-08-03.md`.

**Nothing was inherited from a ruling, a check, a lane doc, or this file's own prior text.** Where round 2's measurement contradicts round 1's, the round-1 reading is quoted and struck by name beside the true one (E-3); the one place it contradicts **RULINGS-2's own prose** is row 4 above, declared rather than quietly conformed.

*(§11 is a **dated round-2 record**, treated as §10 is: it is not rewritten at round 3. Where one of its certifications was falsified since, a bracketed `⟨STAMP CORRECTED at repair round 3: …⟩` marker stands beside the original wording, which is preserved. Every substantive correction round 3 makes is recorded in §12 below, never by editing this table.)*

**Still standing, unchanged and deliberately so.** Census carriage per **`conformance/PASS-3/KF-W1-CHECK.md` §1 (2026-08-28)**, the freshest artifact — **6 routed · 6 booked · 0 escaped · 0 inventions**, re-derived there from the corpus bytes with no denominator inherited from pass 1 or pass 2 (**LAW B**: the claim lives in the artifact, not in this sentence; the same artifact returned DEFECTIVE on four other axes, cured at §12). The ∅-column finding is a **finding, not a gap**, and **no row, gate or bounds item was manufactured to fill it** (C-14's L-19 lock). Every **dissent is carried, not resolved**: the `a59d3a22` rider (KF.W0's re-measurement) · the KF-W0 reverse-edge **conflict** (recorded for the COHESION §3 check and the owner — neither spec rewrites the other) · C-4's recorded asymmetry. Every **lock** is carried verbatim: C-1's *"Amend, do not re-send"* + ANTI-STALENESS · C-3's HARD NEGATIVE · C-7's no-re-ask/no-re-scope + anti-rename on `K1–K4`/`G-L7a–d` · C-8's HARD NEGATIVE + DESIGN LOCK · C-9's SIZING LOCK · C-10's ORDERING lock · C-11's SEQ-LOCK + verbatim-retention · C-12's HARD NON-GATING · C-14's L-19. **L-19 holds affirmatively**: no gate is a script, no phantom command is cited, and the mail tooling a lesser spec would mint is excluded by name. **Status stays `planned`; VERIFIED stays NO; no product source is opened in either repo.**

---

## 12. Repair round 3 (X·KF L-20 round 3) — what changed and on whose authority

Recorded, not silently applied, on the same law §9, §10 and §11 state. **Inputs**: `conformance/PASS-3/RULINGS-3.md` (cross-wave authority — **LAW A** the import-graph law, **LAW B** the closure-claim ban, **R3-2** this wave's BLOCKER, **(c)(1)** the anchor-rot family, and the §END directive map; where a local defect and a ruling collide, **the ruling wins** — except on a *measurement*, where R2-5 keeps the tree above the ruling's prose) and `conformance/PASS-3/KF-W1-CHECK.md` (local register: **DEFECTIVE — 1 BLOCKER · 5 MAJOR · 3 MINOR · 2 INFO**; census **CLEAN** at 6 routed / 6 booked / 0 escaped; **0 inventions**; every keyframes-side witness and every value-side `INBOX.md` anchor byte-exact). **All eleven local defects are cured; none is declined.**

**No carry row was added, renamed, split or dropped: C-1..C-14 stand, ids untouched (anti-rename) — C-6's banked id `SCH-4` is unchanged and only its false descriptive tail was struck. Net row delta 0.** **No gate was added or removed**: the tally stays **10 born-RED · 1 DECLARED-SATISFIED · 1 stay-GREEN INVARIANT**, and §1's header now *states* that tally instead of contradicting it. Nothing here stamps a verb, authorizes execution, or opens product source; every probe was a read-only `git` / `grep` / `sed` / `ls` command, and the sacred checkout was read and never written.

| # | authority | change |
|---|---|---|
| 1 | **RULINGS-3 `R3-2`.1 + `.3`** (+ check **D3-1**, BLOCKER) | **C-6 and G-KF1-4 re-cut so the cure stops commissioning a NEW false certification.** The addendum now withdraws *"matches your tree exactly"* by naming a **WORKTREE-STATE RECORD** — HEAD `8281638c` + 252 dirty paths, dated, byte-identical to O-11's split on all six lines — and explicitly **not** a sha; the round-2 order to inscribe *"`8281638c`"* is quoted and **STRUCK**. **§5b is new**: the three censuses (worktree **61** · bare `8281638c` **81**, disjoint alphabet · frontier **62**) pasted with their commands, so the commissioned sentence rides beside the measurement that makes it true (**LAW B corollary**). The **ANCHOR DUALITY** is declared there verbatim, and its final clause is written into C-6 as a LOCK: ***"No certification cites a sha as 'the tree measured' unless the census reproduces at that sha."*** |
| 2 | **RULINGS-3 `R3-2`.4** (+ check **D3-2**, MAJOR) | **The wave's inverted thesis is struck with dated corrections at all three sites** — §1's Goal criterion (*"they certified a tree they had not measured"*), C-5 (*"Both packets measured the stale tree"*), C-6 (*"import-census off-by-one"*). Re-derived this seat: **nine of nine** drifted-or-absent §5a anchors resolve in the sacred worktree at the packets' own coordinates, and the corroborating instrument agrees — `backward-color.ts:171/:250/:263` are the `sampleRamp` declaration and its two calls (O-11 `:175`'s *"`count=1024`"* tuple) in the worktree, and **docblock prose** at bare `8281638c`. The packets **measured precisely and named nothing**; the defect is unreachability plus sha-less certification. **The operational payload is untouched** — the `origin/master` re-anchoring and the §5a table were correct and remain so. This matters beyond hygiene: the wave's product is a letter, and relaying *"you measured a stale tree and were off by one"* would send a **false statement about the addressee's tree** out of the repo. |
| 3 | **RULINGS-3 `R3-2`.2** (+ check **D3-6**, MAJOR) | **The two tree-identity gates are re-keyed off bare `HEAD`.** G-KF1-2 and G-KF1-4 now name **repo AND substrate in every command** — frontier legs `in keyframes-v-exec` at `origin/master` (the idiom G-KF1-3/G-KF1-10 already used), historical legs against §5b's worktree-state record. Demonstrated failure, measured this seat: in the sacred checkout — the tree a seat is most likely to be sitting in, since §4 puts it in bounds and OP-1's `ls` runs there — bare `HEAD` is the **DISQUALIFIED** `8281638c`, where `git grep -n 'parseCssScalar' HEAD -- src/animation/resolve/browser.ts` returns **nothing (exit 1)** and `git grep -c 'parseStylesheet' HEAD -- src` returns **0 files**. A prose preamble declaring the frontier does not govern a command; the command now carries it. **The sweep this ruling ordered found a THIRD member of the class, worse-shaped than the two ruled** — the **alias block's SCH-1 falsifier**, which also ran bare `HEAD -- package.json`. Run in the sacred checkout it **does not fail**: it returns `package.json:71: "@mkbabb/glass-ui": "6.0.0"` — a different line, a different version, and a hit a seat reads as *confirmation*. Three substrates, three answers, all measured this seat: sacred **worktree** → **absent** (F-1's phantom-dependency claim, RED, as banked) · **`8281638c`** → `6.0.0` at `:71` · **`origin/master`** → `7.0.0` at `:77`, the reading that makes **F-1 FALSIFIED-AT-HEAD**. The wave's whole SCH-1 disposition turned on an unnamed substrate; it is now named in the command. **G-KF1-3 and G-KF1-10 are re-spelled in the same motion** (`HEAD` → `origin/master`; they already named the repo, and `keyframes-v-exec` is at `81a56990` porcelain 0, so both readings re-verified **identical** — nine `parseStylesheet` lines, nine `INBOUND-LEDGER` rows — but the spelling no longer *depends* on that holding at the executing seat's clock). **Four gate/witness sites re-keyed in total.** |
| 4 | **RULINGS-3 `(c)(1)`** (+ checks **D3-3** and **D3-4**, MAJOR) | **The four dead coordinates are dropped from cross-edges 2 and 11** (`:393` · `:431` · `:54` · `:332`) and the edges ride the stable anchors §1 already names. This also cures the **one file, two verdicts** contradiction: §1's KF-W0 and KF-W10 bullets had struck those same four **by name**, while §7 asserted them live. **The single surviving coordinate — KF-W10 §1's `Opens after` at `:20`, ALIVE — is carried openly**, dated, declared non-load-bearing, and re-resolved at open. **§11 row 7's close stamp is corrected per LAW B**: its clause *"NO line number anywhere on this file's cross-spec receipts"* is struck as a spec-voiced completeness claim that its own file falsified, and the axis is now certified by citation to `PASS-3/KF-W1-CHECK.md` §6 D3-3 (2026-08-28), re-pointable at pass 4. |
| 5 | check **D3-5** (MAJOR) | **The census cardinality schism at the booking site the header cites is closed.** §8 row 5 read *"All **five** `KF.W1-DEP` / census F-1 **rows**"* — it counted **records as rows** and under-booked by one, dropping `kf-AnimationControlsGroup.md:47` and the `useControlsKeyboardShortcuts.ts:1` sole-binding-site rider it owns. Re-derived this seat (`grep -n 'KF\.W1-DEP' kf-*.md`, every hit read and classified): **six routing rows across five records — ACG carries two.** §8 row 5 now enumerates all six by anchor; the alias block gains the same enumeration; C-13's *"five"* is kept and labelled as a **record** count. The header PROVENANCE cites this cell as its booking receipt, so the two now agree instead of colliding. |
| 6 | **RULINGS-3 LAW B** | **Every closure sentence this seat touched is converted to citation form.** The header PROVENANCE, the alias block's *"the COMPLETE census"*, §5's preamble, C-14's *"verified three ways"*, §11's close stamp and its *"Still standing"* paragraph now **cite `conformance/PASS-3/KF-W1-CHECK.md` §1 / §6 (2026-08-28) by path and date, and state what that artifact found — including that it returned DEFECTIVE on four axes** — instead of asserting carriage, anchor-cleanliness or zero-escape in this spec's own voice. When a newer pass supersedes it, **the citation is re-pointed, never the claim re-asserted here.** |
| 7 | **RULINGS-3 LAW A** | **§4c is new, and it discharges the law in both directions.** (i) The **module clause is VACUOUS by enumeration**: the six bounds rows are two `modify` and four `create`, all markdown; this wave deletes, repoints or shims **no module** in either repo. (ii) The wave's **one repoint act** — C-9's mail-path law — gets a LAW-A-shaped consumer census anyway, pasted with its commands and outputs (23 hits / 17 files; 12 hits / 7 files on the law's own spelling; the subject grep at 1). **It found a second law-limb OUTSIDE this wave's bounds**: `~/.claude/…/memory/feedback-mail-inbox-law.md:16` codifies the same four-path sweep with the frozen keyframes path, so **repairing `INBOX.md` alone does not close the class** — the memory limb re-injects it at the next session open. Recorded as an **OPEN residual routed to the OWNER**; edited by nobody here. The 22 non-consumer hits (the four O-2/O-4/O-8/O-11 ledger path cells, `lane-docs.md:290`'s transcription of the now-dead `:13-14` coordinate, five reformation/letter records, the apotheosis and harvest artifacts) are **recorded as context, never counted as consumers**. |
| 8 | check **D3-7** (MINOR) | **§1's hard-gate header re-cut `twelve` → ten hard conditions + one DECLARED-SATISFIED + one stay-GREEN INVARIANT**, with the two non-gates named (G-KF1-11, G-KF1-12) instead of counted. §6 and §11 already read 10 · 1 · 1; the header had re-inflated by exactly the two its own next paragraph deflates. |
| 9 | check **D3-8** (MINOR) | **Cross-edge 9's transit roster gains `KF-AV-28`**, the supersession rider on the W7-evaluated surfaces. §8's 17-packet row already carried it; the roster introduced as *"the locks that must travel WITH those homings, restated so they are not lost in transit"* enumerated five and dropped it — an omission in the **operative** transit declaration. The two enumerations now agree at **six**. |
| 10 | check **D3-9** (MINOR) | **The discharge tally stops mixing metrics.** *"(KF-W5 ×1, KF-W6 ×2, KF-W9 ×5)"* is struck: W5/W9 were occurrence counts, W6 a line count, and **no single command yields the triple**. Re-measured over all six non-consumers, both metrics stated separately — occurrences W2 1 · W5 1 · W6 4 · W7 2 · W8 0 · W9 5; lines W2 1 · W5 1 · W6 2 · W7 2 · W8 0 · W9 3; `O-11` **0** in all six; bare-`KF.W1` token **0** in all six. **The load-bearing claim was and is TRUE** — every hit is a `KF-CO-8`/`KF-CO-1` substring. |
| 11 | check **D3-10** (INFO) | **G-KF1-5's third witness leg is made literal.** *"Grep of both coordination trees for any withdrawal → 0"* carried no pattern, no path and no command inside a §6 promising *"literal commands with literal outputs"*. Replaced by two commands over **all three** coordination trees with their outputs read hit by hit: `4\.0\.1` → 4 hits (three ledger row cells + the question itself at O-8 `:101`), **zero in the exec-visible tree**; `withdraw` → 4 hits, each identified (an O-10a glass row, two CONSTELLATION configurator notes, a vnext skeptic table), **none a withdrawal of the vehicle question**. |
| 12 | check **D3-11** (INFO) | **§5a rows 6–8 are measured** — this file's last unmeasured figure, sitting under a header whose own law is *"anchors are re-resolved at authoring, never copied"*. All three are **EXACT at the frontier** (`emit/backward/color.ts:171`/`:250`/`:263` = the `sampleRamp` declaration and its two calls) and byte-identical at the worktree's `backward-color.ts`, both files **385 L**: **path drift only, zero line drift**. The executing seat re-derives them at open and **inherits nothing.** |

**Witnesses re-derived at this repair (R2-5, run as the FULL-SURFACE sweep RULINGS-3 §(e) orders — every witness, every anchor, BOTH baselines checked for silent mis-resolution).** Each by a read-only command at write time.

**(i) Frontier, `keyframes-v-exec` at `origin/master` `81a56990` (porcelain 0)** — sha ✓ · the **62** import split (`/css` 29 · `/value` 16 · `/color` 7 · `/math` 5 · `/easing` 3 · `/transform` 2) ✓ · `browser.ts:162` + its import `:3` ✓ · `package.json:70` (value.js `4.0.0`) and `:77` (glass-ui `"7.0.0"`, SCH-1's falsifier) ✓ · the three ABSENT packet anchors ✓ · **`emit/backward/color.ts:171`/`:250`/`:263` measured, 385 L** ✓.

**(ii) The worktree-state record, sacred checkout, read-only** — HEAD `8281638c` ✓ · `41  1` ✓ · porcelain **252** ✓ · the worktree census **61**, six lines byte-identical to O-11 `:52` ✓ · the bare-`8281638c` census **81** over a disjoint alphabet ✓ · the **nine** §5a anchors resolved at the packets' own coordinates ✓ · the two packet bodies at 7,064 B / 15,633 B with their mtimes ✓ · **the D3-6 failure reproduced** (bare `HEAD` legs return nothing there) ✓.

**(iii) Value-side, current tree bytes** — `INBOX.md` **97 L / 56,702 B** ✓ · the sweep-path law's narrowed grep → **1 hit at `:15`** ✓ · the broad grep → **5** (`:15` `:66` `:68` `:72` `:75`) ✓ · max outbound **`O-20`** ✓ · **I-26 `:92`** / I-21a `:90` / **I-10 `:47`** / I-7 `:45` ✓ · the LAW-A mail-path census (23/17 · 12/7 · 1) ✓ · the D3-10 withdrawal probes (4 + 4, each read) ✓ · the **58**-record corpus (18 anchors / 5 records / **6 routing rows** / bare token 0 / literal 33 = 18 + 15) ✓ · the ten siblings' `KF.W1` / `O-8` / `O-11` counts, both metrics ✓.

**Nothing above is inherited from a ruling, a check, a CARRY, a lane doc, or this file's own prior text** — RULINGS-3's own case facts were re-derived here before being written, as R2-5 requires of every seat, and the one figure this seat re-derived beyond the rulings' text (the `emit/backward/color.ts` line anchors) is recorded as a measurement, not a correction to any ruling.

**Still standing, unchanged and deliberately so.** Every **lock** is carried verbatim, and C-6 gains one (the LAW-B substrate lock). Every **dissent is carried, not resolved**: the `a59d3a22` rider (KF.W0's re-measurement) · the **KF-W0 reverse-edge conflict** (recorded for the COHESION §3 check and the owner — neither spec rewrites the other; round 3 struck only its dead coordinates, never its direction or force) · C-4's recorded asymmetry. **L-19 holds affirmatively**: no gate is a script, no row or bounds item was manufactured, the mail tooling a lesser spec would mint is excluded by name — and the two non-gates stay labelled rather than counted. **Status stays `planned`; SPECIFIED stays YES; IMPLEMENTED and VERIFIED stay NO; no product source is opened in either repo; the sacred checkout is unchanged.**
