# X.KF.W1 - Mail Cure (the O-8/O-11 amendment-addendum, and our own sweep-path defect)

**Sub-tranche**: X·KF, **frontend half (SS-2)** ⟨COHESION §1 SS-2 — "glass suffusion · timeline evaluate · Safari visual · **mail cure** → KF.W1/W6/W7/W9"⟩. Spec home `docs/tranches/X/keyframes/waves/KF-W1.md`; authority `docs/tranches/X/COHESION.md` §0/§2/§3, ratified under **M-25**. Validated at authoring against `docs/precepts/instructions/tranche/WAVE_SPEC.md` (**L-20**).

**Status**: **planned**

**PROVENANCE (restated at repair round 2, under RULINGS-2 R2-1 item 6 / RULINGS R-12).** This spec is authored **from the 58-record adjudicated registry directly** (`docs/tranches/V/megatranche/registry/adjudicated/`, `ls kf-*.md | wc -l` → **58**, re-derived this seat) and from the dated in-tree formation documents named in §4 (`CENSUS-2026-08-03.md`, `lane-docs.md`, `INTAKE-ADJUDICATION-2026-08-03.md`, `INBOX.md`). **The routed denominator is the registry's; the CARRIAGE CLAIM is the conformance artifact's, never this spec's** ⟨**LAW B**, `conformance/PASS-3/RULINGS-3.md` — no wave spec asserts a completeness/zero-escape claim in its own voice⟩: census carriage per **`docs/tranches/X/keyframes/conformance/PASS-5/KF-W1-CHECK.md` §1 (2026-08-28)** — **re-pointed at repair round 5 under this sentence's own rule** ⟨check **D5-2** (MAJOR): round 4 re-pointed this site and the alias block and left **three** live carriage citations standing at PASS-3, so one file named two different "freshest" artifacts for one quantity. **The class is now swept WHOLE, in one act, and is greppable** — the command, the enumeration, the counting rule and every hit's disposition live at **§14's CARRIAGE-CITATION scope receipt**; the PASS-4 and PASS-3 citations are preserved below as the dated predecessors (E-3), never deleted⟩ — which re-derived the routing alphabet **BY RECORD from the 58 files' bytes** (the inherited nine-block partition retired per RULINGS-3 R3-5) and found **6 routed · 6 booked · 0 escaped · 0 inventions** — the six rows standing across **five** records, all six the census-F-1 identity under the collided `KF.W1-DEP` token (§5 C-13; §8 row 5, whose *"all five rows"* cardinality PASS-3 convicted at **D3-5** and which was corrected at §8 at round 3). That artifact also returned **DEFECTIVE — 0 BLOCKER · 5 MAJOR · 3 MINOR · 2 INFO**, with the severity head **MONOTONE-FLAT** (pass 4 = 0·5·3·2 → pass 5 = 0·5·3·2) and **all ten PASS-4 local defects verified CURED at the bytes**; **§14** records this round's cures. ⟨**Dated predecessors, preserved not deleted (E-3)**: `PASS-4/KF-W1-CHECK.md` §1 (2026-08-28) found the same **6 / 6 / 0 / 0** census and returned **DEFECTIVE — 0 BLOCKER · 5 MAJOR · 3 MINOR · 2 INFO** with all eleven PASS-3 defects cured (§13 records those cures); `PASS-3/KF-W1-CHECK.md` §1 (2026-08-28) found the same **6 / 6 / 0 / 0** census and returned **DEFECTIVE — 1 BLOCKER · 5 MAJOR · 3 MINOR · 2 INFO** (§12). **Three artifacts, one census, one figure — the re-point is of the CITATION, never of the claim.**⟩ When a newer pass supersedes this one, the citation is **re-pointed again, never re-asserted here**. Every earlier citation of an "SS-2 CARRY" as this spec's source is **STRUCK**: `find docs -iname '*CARRY*'` returns exactly one carry under `X/keyframes/` — `carry/KF-W6-CARRY.md`, which is **W6's** and per RULINGS **R-12** the ONLY in-tree X·KF carry. **No ledger that is not in the tree is cited as a source in this file again**, and no `N rows carried, 0 dropped` claim is keyed to one.

**Filename reconciliation (declared so the filename never becomes a second namespace schism).** Re-measured **2026-08-28, repair round 2, this seat**: `ls docs/tranches/X/keyframes/waves/` → **eleven** specs, this one and **ten** siblings — `KF-W0.md` · `KF-W2.md` · `KF-W3.md` · `KF-W4.md` · `KF-W5.md` · `KF-W6.md` · `KF-W7.md` · `KF-W8.md` · `KF-W9.md` · `KF-W10.md` — every one self-naming `KF-W<N>.md`. The sibling convention governs: this spec's home is **`KF-W1.md`** (PRESENT, 65,138 B at *that* repair's open ⟨**dated round-2 observation, marked at repair round 5 — check `D5-10` (INFO), stated not silently applied (E-3)**: the figure is a **round-2 reading and is NOT re-issued** — this file measured **200,681 B at round-5 OPEN**, per `conformance/PASS-5/RULINGS-5.md` **LAW E(3)**'s hash-bracketed opening table, which is an immutable frozen source rather than a per-seat stamp (**LAW E(4)**), and its **closing** bytes are printed at `PASS-5/CLOSE-CERT-2.md`'s sha256 table. The same round-2 spelling rides `KF-W10.md`'s `OP-5` cell; that end is **KF-W10's**, not this file's, and is not edited from here. The load-bearing halves — `KF-W1.md` **PRESENT**, `W1.md` **ABSENT** — are unchanged and were re-verified at pass 5⟩), and `W1.md` is **never** created (`test -e W1.md` → ABSENT, re-run this seat). The superseded `waves/W1.md` spelling and its "parent directory ABSENT at 2026-08-25" reading are recorded here **as the readings corrected, never as a source**.

**Alias law (R-B idiom, inherited from `KF-W4.md`).** The sibling spelling `KF.W1` denotes `X.KF.W1`. It is a search alias, never a rename; `X.KF.W1.<x>` is the WAVE_SPEC §5 form and is authoritative.

**Second alias — LOAD-BEARING, and the one omission that would corrupt other waves' tables.** `KF.W1-DEP`, as written in the superseded `formation/keyframes/lane-frontend.md §10` lane taxonomy, **does not denote this wave.** Five adjudicated records carry a `KF.W1`-form token and every one of them means `KF.W1-DEP` — **eighteen anchors across SIX routing rows**, re-measured 2026-08-28 at repair round 3 (`grep -c 'KF\.W1-DEP' kf-*.md` → 4 · 4 · 3 · 4 · 3 = **18**). **Anchor completeness is certified at `conformance/PASS-5/KF-W1-CHECK.md` §1.1–§1.2 (2026-08-28), not here** (LAW B; **re-pointed at repair round 5 with the whole carriage class, check `D5-2`** — the `PASS-4` and `PASS-3` citations it replaces each found the **identical** decomposition and are preserved as the dated predecessors, E-3): that seat re-derived the decomposition from the bytes — **5** taxonomy-preamble + **6** routing rows + **5** ADJUDICATED-summary + **2** riders = **18** — and found it exact, per-file counts included, closing the alphabet four ways (bare token · substring decomposition **33 = 18 + 15** · terminal-disposition alphabet · carry sweep). The anchors: ⟨kf-AnimationControlsGroup.md:17/:28/:47/**:135** · kf-ChannelControls.md:17/:56/:142/**:155** · kf-ControlsPaneWrapper.md:6/:60/**:149** · kf-DemoGlobalChrome.md:17/:43/:44/**:130** · kf-KfPillTabs.md:19/:46/**:122**⟩. The five bolded anchors are each record's ADJUDICATED-summary line, which also names `KF.W1-DEP` as an owning fold; a block whose purpose is anchor completeness states all eighteen, not the thirteen an earlier draft cited.

**ROWS vs RECORDS, stated because they differ and the difference was mis-carried (D3-5, cured at repair round 3).** The **six** routing rows are `kf-AnimationControlsGroup.md:28` (the F-1 severity row) **and `:47`** (the L-3/C-1 row that owns the `useControlsKeyboardShortcuts.ts:1` sole-binding-site rider) · `kf-ChannelControls.md:56` · `kf-ControlsPaneWrapper.md:60` · `kf-DemoGlobalChrome.md:44` · `kf-KfPillTabs.md:46` — **six rows across five records, because ACG carries two.** Re-derived this seat (`grep -n 'KF\.W1-DEP' kf-*.md`, every hit read and classified); independently re-derived at **`PASS-5/KF-W1-CHECK.md` §1.2** (re-pointed at repair round 5 with the carriage class, check **D5-2**; `PASS-4/KF-W1-CHECK.md` §1.2 and `PASS-3/KF-W1-CHECK.md` §1.2 are the dated predecessors, both printing the same six), which prints the same six ids. **C-13's "five" is correct — it counts RECORDS**; §8 row 5's superseded *"All five `KF.W1-DEP` / census F-1 **rows**"* counted rows and under-booked by one, dropping `:47` and its rider. Corrected at §8 this round; recorded here, not silently applied. The mapping, declared once and for all:

> **KF.W1-DEP ≡ census F-1 (the phantom `@mkbabb/glass-ui` dependency) ≡ X.KF.W0 §B-12 — and FALSIFIED-AT-HEAD per SCH-1. ZERO `KF.W1-DEP` rows land in X.KF.W1 · Mail Cure.**

Falsifier, re-measured by this seat 2026-08-28, **repo and substrate named in the command** (repair round 3): **in `keyframes-v-exec`**, `git grep -n '"@mkbabb/glass-ui"' origin/master -- package.json` → **`package.json:77: "@mkbabb/glass-ui": "7.0.0",`** — an EXACT devDep, present.

⟨**ROUND-3 CORRECTION — the THIRD member of the D3-6 silent-mis-resolution class, and the worst-shaped one, found by this seat's own full-surface sweep (R2-5 §(e)); stated not silently applied (E-3).** This falsifier read `… HEAD -- package.json` with **no repo named**, declaring the substrate only in prose. Run in the sacred checkout — which §4 puts in bounds as a read target and where OP-1's `ls` runs — bare `HEAD` is the DISQUALIFIED `8281638c`, and the command **does not fail**: it returns **`package.json:71: "@mkbabb/glass-ui": "6.0.0"`**. A different line, a different version, and a hit that reads as *confirmation*. Unlike G-KF1-2's legs, which return nothing and are merely uninformative, **this one silently answers the wrong question with a plausible answer** — the exact failure mode the SUBJECT-IDENTITY / substrate-naming discipline exists to kill. **Three substrates, three answers**, all measured this seat: sacred **worktree** → `grep -n '"@mkbabb/glass-ui"' package.json` returns **nothing** (glass-ui absent from the manifest — F-1's phantom-dependency claim, RED, exactly as the bank states it) · bare **`8281638c`** → `6.0.0` at `:71` · **`origin/master`** → `7.0.0` at `:77`, which is the reading that makes **F-1 FALSIFIED-AT-HEAD**. The wave's SCH-1 disposition therefore turns entirely on the substrate, and the substrate is now written into the command instead of into the sentence beside it.⟩ F-1's RED is true only of the sacred checkout ⟨KF.W0 §B-12 manifest schism: origin/master pins = installed; local HEAD `8281638c` stale — **every wave re-baselines against `origin/master`**⟩. The lane taxonomy's names are **not** renamed (anti-rename; the five records' routing text is banked and must stay citable) — the collision is disambiguated by declaration, not by editing the bank. F-1 is **never re-booked**: the identity guard the five records already hold ("booked once at the census; never re-booked per component") extends across the namespace boundary. ⟨kf-LayerConfigPanel.md:12 routes F-1/SCH-1 → KF.W0 §B-12 explicitly⟩

**Third alias — the `W1` namespace that is neither of the two above, and the reason "once and for all" now means what it says.** keyframes' own `docs/tranches/V/DISPOSITIONS.md` uses **`BUILD W1`** for *their* build wave. It is live in this corpus at **six anchors** ⟨kf-CopyButton.md:15 · :40 (**EE-01**, `DISPOSITIONS.md:69`) · kf-KeyframesEditor.md:10 · :40 (**FE-3**) · :51 (**EE-03**) · :140⟩ and **denotes neither X.KF.W1 nor `KF.W1-DEP`.** Nothing escapes and nothing is owed here: all three identities route explicitly — *"→ **FOLD to EE-01 + KF.W0**"*, *"→ **FOLD to FE-3 + KF.W0**"*, *"→ **FOLD to EE-03 + KF.W0**"* — and the surviving obligation in each case is **KF.W0's re-regression guard** (the stale fork's in-flight migration must not re-land the dead bytes over the frontier's repair), not this wave's. Declared because a seat grepping `W1` across the corpus meets these six hits, and a mapping that admits two of three live meanings is exactly the corruption this block exists to prevent. **Three namespaces, closed: `X.KF.W1` (this wave) · `KF.W1-DEP` (census F-1 → KF.W0 §B-12) · `BUILD W1` (keyframes' own DISPOSITIONS → EE-01/FE-3/EE-03 → KF.W0).**

**EXECUTION IS NOT AUTHORIZED BY THIS FILE.** Tranche X's execution gate stands: nothing here opens, writes, builds, or runs against product source until the **owner's begin-word**. This wave opens **no product source in either repo** — not one `src/` file, not one `.vue`. Specifying is not doing.

**SACRED-CHECKOUT LAW.** ⟨keyframes' own `docs/tranches/V/EXECUTION-HANDOFF.md:13-18`, **quoted WHOLE at repair round 4 — the cited authority is conditional and self-terminating, and the round-3 citation carried the absolute half while dropping the condition** (check **D4-7**), re-read verbatim this seat by `sed -n '13,18p'` in `/Users/mkbabb/Programming/keyframes.js`: *"The OWNER CHECKOUT `/Users/mkbabb/Programming/keyframes.js` sits on stale local master **`a59d3a22`** (5.3.5 base) and deliberately carries the UNCOMMITTED K6-producer + Glass-7-consumer transaction (**~254 dirty paths**) PLUS the untracked V plan folder. It is SACRED: **no mutating git, no npm operations there, ever** — W2 reads its FILES (the 65-path slice export) and nothing else. **This invariant survives until W2 completes.**"*⟩ X.KF.W1 writes **NOTHING** under `/Users/mkbabb/Programming/keyframes.js/`. Every access to that tree is a read or a copy **out**.
⟨**ROUND-4 CORRECTION, stated not silently applied (E-3) · check `D4-7`.** Three things the round-3 citation dropped, each now carried: (i) the **expiry clause** *"This invariant survives until W2 completes"* — the authority is **conditional**, and a file whose C-5 lock is *"anchors are re-resolved at authoring, never copied"* and whose M-25 idiom demands verbatim-or-declared-paraphrase owes that discipline to its own most-cited external authority; (ii) the handoff's own dated tree record — **`a59d3a22`** / **~254 dirty paths** — against this file's independently measured **`8281638c`** / **252** (`git rev-parse HEAD` and `git status --porcelain | wc -l`, both re-run read-only this seat, 2026-08-28); the two records name the same sacred tree at **different clocks** and neither corrects the other, which is why the worktree-state record of §5b is a **dated four-figure tuple** and never a sha; (iii) the **independence declaration**: **value.js's read-only posture toward that tree does not depend on the keyframes-side condition.** X.KF.W1 writes nothing there whether or not their W2 completes — the prohibition is value's own (§4 **Do NOT touch**, the execution gate, and G-KF1-12's stay-GREEN invariant), and the quoted authority is corroboration, not its source. **Nothing is at risk either way — this wave performs zero writes in that tree** — but the citation now says what its source says.⟩

---

## 1. State

**Name**: W1 - Mail Cure (the O-8/O-11 amendment-addendum, and our own sweep-path defect)
**Opens after**: the owner's begin-word. **Not** after X.KF.W0 — see §7 cross-edge 2: this wave is deliberately **completable without §B-12**, because the exec-clone delivery path exists precisely so the mail cure never queues behind the owner's hand. (KF-W0's authored spec claims the reverse edge; the conflict is RECORDED, not silently resolved — §7 cross-edge 2.)
**Agents**: 3 Opus units, **serial** (`.a` → `.b` → `.c`). No Fable seat: nothing here is adjudicated or designed — it is a delivery act with three re-measurements. Owner cap (max 4 concurrent) respected by construction.
**Hard gate**: **TEN hard conditions in §6 (G-KF1-1..G-KF1-10), plus one DECLARED-SATISFIED and one stay-GREEN INVARIANT** — the addendum exists at an exec-visible path · every anchor in it resolves at `origin/master` · the three missed `parseStylesheet` sites named with their failure postures · the "matches your tree exactly" certification withdrawn **against a truthfully named substrate** (C-6 / G-KF1-4, re-cut at repair round 3, and **re-cut again at round 4 onto the C-10 MINT-LAW pattern — the gate commissions the SENTENCE TEMPLATE and the substrate-naming RULE, never the five dated literals**, check `D4-4`) · the vehicle question closed honestly · the D-GAP-6 conditional resolved to *not adopted* · I-26 CURED + **O-21** minted (the mint is `max+1` **measured at open**, never this literal — C-10's MINT LAW) · our own sweep-path law repaired · both outbound bodies retained in value's tree · the ledger mark ASKED (and declared non-gating). **NOT hard conditions, and never counted as such**: the spec home populated (**G-KF1-11**, DECLARED-SATISFIED) · the sacred checkout unchanged (**G-KF1-12**, stay-GREEN INVARIANT).
⟨**Round-3 correction, stated not silently applied (E-3; check D3-7).** This line read *"**twelve** conditions in §6"* and enumerated all twelve as hard. **STRUCK**: §6 declares G-KF1-11 *"NOT a close gate this wave passes"* and G-KF1-12 *"INVARIANT, declared, **not** born-RED"*, and §11's tally already read **10 born-RED · 1 DECLARED-SATISFIED · 1 stay-GREEN INVARIANT** — a condition explicitly declared not to be a close gate cannot be one of twelve hard-gate conditions. The header now agrees with §6 and §11 instead of re-inflating by two what the next paragraph deflates by two.⟩

**Born-RED tally at WAVE-OPEN — 10 · 1 · 1, not eleven (L-19).** **Ten** are born-RED with a named, literal witness that a seat can re-execute and watch fail: G-KF1-1..G-KF1-10. **G-KF1-11 is DECLARED-SATISFIED, not born-RED** — its condition ("the spec home is populated") was RED at the instant of authoring and is discharged by the existence of this very file; at wave-open it is already GREEN and **no act inside `.a`/`.b`/`.c` can turn it**. **G-KF1-12 is a stay-GREEN INVARIANT.** Both are labelled as such rather than dressed as born-RED: counting a self-satisfying condition as born-RED inflates the tally by one and dresses a fact as a test — the same contrivance G-KF1-12 was relabelled to avoid, and the identical treatment was owed to G-KF1-11.

### Four-verb status (separate fields; this file moves exactly one)

| verb | value | evidence |
|---|---|---|
| AUDITED | **YES** | `CENSUS-2026-08-03.md` §0/§0.5/§4/(c)5/(c)6 · `formation/keyframes/lane-docs.md` §0/§4/§6/§7.1/§7.2 · `INBOX.md` row **I-26** (at `:92` at this writing — the **row id** is the anchor) · `INTAKE-ADJUDICATION-2026-08-03.md` §3 |
| SPECIFIED | **YES — by this file, 2026-08-28** | authored from the **58-record adjudicated registry directly** (header PROVENANCE); the routed denominator is the registry's — **6 routed / 6 booked / 0 escaped**, re-derived this seat; the fourteen carry rows C-1..C-14 each name a ground re-opened at the bytes (§5); §8 states every non-carry with its reason; L-20 validated; witnesses re-measured at authoring and again at repair round 2 |
| IMPLEMENTED | **NO** | — gates green + the addendum landed at the exec-visible path stamps this |
| VERIFIED | **YES — stamped 2026-09-19 at X·KF's close (X.KF.W10 unit `.f`, R-A), and honestly** | **No wave stamps VERIFIED at its own close; KF.W10 stamps it here.** Basis **by commit**: **G-1** GREEN (kf `025e894c`) · **G-2** GREEN (value `15da439f`) · **G-3/G-4** GREEN (kf `27ec9c37`) · **G-5** GREEN (kf `b50a23de`) · **G-6** GREEN (value + kf `dd28da55`) · **G-7 = LAND stamped (`0a329c57`), RED-HONEST on its two CI-dependent clauses** (ESC-e1, an out-of-bounds `package-lock.json` desync, identical at the pre-landing SHA) — **X·KF closes `complete_with_misses`, stated rather than rounded up**. **This wave carried the close's hardest dependency and paid it**: the Mail Cure's instrument `O-21` is exec-visible at kf `origin/master`, which is what **OP-3** demanded and what **G-6** could not read terminal without. **IMPLEMENTED cited, not re-asserted**: `execution/LEDGER.md` — **KF.W1 CLOSED 2026-09-17** (10 born-RED → 10 GREEN at the close seat's own re-run). **ACCEPTED is NOT conferred** (L-18). **One row edit; no other byte touched (E-3).** |

**This wave is the DECLARED MAIL HEAD of X·KF.** ⟨COHESION §2, verbatim: "**I-26 mail cure (KF.W1) precedes any kf wave consuming O-8/O-11 obligations.**"⟩ ⟨census (c)6, verbatim: "**The mail cure precedes everything kf-side.**"⟩ **Reciprocity, measured 2026-08-28 at repair round 4 over the WHOLE authored sibling set — all TEN (`KF-W0` · `KF-W2` · `KF-W3` · `KF-W4` · `KF-W5` · `KF-W6` · `KF-W7` · `KF-W8` · `KF-W9` · `KF-W10`), per RULINGS R-19d.** Enumeration source, pasted (**LAW D(3)** — the figure is re-run at write time, and the count lives AT its enumeration with its counting rule): `ls docs/tranches/X/keyframes/waves/` → **eleven** `KF-W<N>.md` specs, **this one and TEN siblings**; the counting rule is *one file, one sibling*. **Four declare a KF.W1 edge** — KF-W3 · KF-W4 · KF-W10 forward, KF-W0 the reverse edge (conflict recorded, cross-edge 2) — and **six do not** (`:58` below). **The measured set and the enumerated-as-authored set are the same set**, because a mail head that measures a subset of its consumers is measuring the wrong thing.
⟨**ROUND-4 CORRECTION, stated not silently applied (E-3) · check `D4-1`, MAJOR — the D3-4 class (*one file, two verdicts*) reproduced one level up, on the sibling cardinality.** This sentence read *"the WHOLE authored sibling set — all **seven** (`KF-W0` · `KF-W2` · `KF-W3` · `KF-W4` · `KF-W7` · `KF-W8` · `KF-W10`)"* — **STRUCK**, and struck at all three of its sites in one motion (**here** · **cross-edge 1** · **`:58`**'s non-consumer list), because the file already carried the true figure twice: **§9 item 5** reads *"KF-W5 · KF-W6 · KF-W9 as well — **ten siblings on disk** … the measured set is the authored set, now ten"* and **§11 row 10** discharges *"across all **SIX** non-consumers."* **Seven at the head, ten in the records** — and the head sentence's own justification is that measuring a subset of one's consumers is measuring the wrong thing. Round 3 cured D3-4 at cross-edges 2 and 11 and never swept §1's own reciprocity paragraph; **the two-verdicts-one-file split is ended AT THE HEAD this round**, which is where the sibling cardinality is read first. `KF-W5`, `KF-W6` and `KF-W9` have been on disk since repair round 2 — G-KF1-11's own dated record has said *"eleven specs — this one and ten siblings"* since then, so the head was stale against its own §6 as well as against §9 and §11.⟩

**CITATION IDIOM (RULINGS-2 R2-7, binding program-wide from this round).** Every cross-spec receipt below cites a **STABLE ANCHOR** — the destination spec's §-heading, gate id, OP id, or table-row label. Any line number rides **parenthetically, declared non-load-bearing**, resolved at this repair's clock (2026-08-28, round 2). Intra-spec line citations stand (they move with this file). Quotations are verbatim at their source at write time or say they are paraphrase (M-25).

- **KF-W4 — RECIPROCATED.** **§Preconditions OP-5**: *"OPEN — KF.W1's … the amendment-addendum must land before they are honestly citable"*; and **KF-W4 §Sequencing, the cross-edge table row `KF.W1 · Mail Cure`** → **PRECONDITION on anchor citability** (**numeric convenience STRUCK at round-2 close (U-2/R2-7)** — the round-2 spelling `:242` had drifted onto W4's NO-WAVE-OWNER packet-roster paragraph; **the §-heading + row label `KF.W1 · Mail Cure` are the sole anchor and no line number rides this receipt**. Round-1's `:206` was already dead and stays struck).
- **KF-W3 — RECIPROCATED.** **§Preconditions OP-5** UNLANDED · **L-4** · **KF-W3 §Cross-edges, the row `KF.W1 · Mail Cure (O-8/O-11) + E13`** → **DEPENDS ON** (**numeric convenience STRUCK at round-2 close (U-2/R2-7)** — the round-2 spelling `:220` had drifted onto W3's SS-6 BH-relay row; **the §-heading + row label are the sole anchor and no line number rides this receipt**. Round-1's `:209` was already dead and stays struck).
- **KF-W10 — RECIPROCATED, and it hard-blocks on this wave THREE ways** (each re-resolved this seat; the round-1 `:16`/`:50`/`:296` receipts were all dead). ⟨**KF-W10 §1 State, the `Opens after` line** — **ANCHOR ALONE**: the §-heading + the `Opens after` label are the whole receipt ⟨**ROUND-5 STRIKE, check `D5-5` (MAJOR), stated not silently applied (E-3)**: a quoted `Opens after` sentence stood here, verbatim at W10's round-3 bytes and a **TRUNCATION** at its later bytes — W10's parenthesis now continues past the quoted closing paren into the naming of this wave's outbound row `O-21`, so **the quoted string, with its paren where it sat, does not exist**. The quotation is **STRUCK**, the anchor **KEPT and unchanged**; the drift is in W10's favour (it now names by id what this wave commissioned), and no direction, force or subject moves. Per **LAW C(3)** and **RULINGS-5 LAW E(4)** a stage-1 seat carries no stage-2 prose, no line numbers and no per-seat stamps at all; the anchor's resolution is verified at `PASS-5/CLOSE-CERT-2.md`⟩⟩ ⟨**KF-W10 §Preconditions, row `OP-3`** — **ANCHOR ALONE**, the §-heading + the row id `OP-3` ⟨**PARAPHRASE, not a quotation (LAW D(1))**, retained because the row's *force* is what this bullet asserts: the row states the addendum's exec-visibility as a named open precondition, still **UNLANDED**, and reasons that terminalizing a ledger whose packets were never delivered would record a false close. **ROUND-5 STRIKE, check `D5-5`**: the verbatim-labelled sentence that stood here is a **TRUNCATION** at W10's later bytes — the bolded clause now continues into the naming of `O-21` by id — so it is **STRUCK and re-cut as a labelled paraphrase**, per LAW D(1) and **RULINGS-5 LAW E(4)**; `UNLANDED` survives at the far end, and the anchor is verified at `PASS-5/CLOSE-CERT-2.md`⟩⟩ ⟨**KF-W10 §Sequencing, cross-edge `B. → KF.W1 (Mail Cure, SS-2)`** — **ANCHOR ALONE**, the §-heading + the cross-edge label ⟨**ROUND-5 STRIKE, check `D5-5`**: the sentence quoted here as verbatim is a **TRUNCATION** at W10's later bytes — **two unmarked elisions**, both of them the `O-21` naming — and is **STRUCK**. The edge is unchanged: same direction, same force, same subject⟩⟩ **The property, not a numeral (check `D5-4`, MAJOR — the D4-6 cure applied a second time): KF-W10 cites BOTH letters by id, repeatedly, at declared anchors** — which is what makes it precisely *"a kf wave consuming O-8/O-11 obligations"*, the class COHESION §2 says this wave precedes, and the **hardest downstream consumer this wave has**. ⟨**ROUND-5 STRIKE, stated not silently applied (E-3)**: this cell read *"Re-measured this seat: `grep -c 'O-8' KF-W10.md` → **8**, `grep -c 'O-11' KF-W10.md` → **3**"* — **STRUCK**. Both figures were true at the substrate they were taken from and **false at the round's end** (KF-W10 writes in **stage 2**, and LAST of the per-wave seats), and — the aggravation that makes this a defect rather than a dated reading — **the cell carried no substrate stamp and no command substrate at all**, so the numerals read as current. Re-measurement moves them **UP**, so the load-bearing conclusion is **strengthened, never weakened**. Under **RULINGS-5 LAW E(4)** any figure that must ride rides `PASS-5/CLOSE-CERT-2.md`'s tables, never a per-seat stamp; the executing seat re-derives at open.⟩ Cross-edge **11** carries the edge. **SHADOW (RULINGS-5 LAW F(1), mechanism S)**: struck set = {the three KF-W10 quotations ⟨§1 State `Opens after`⟩ ⟨§Preconditions `OP-3`⟩ ⟨§Sequencing `B. → KF.W1 (Mail Cure, SS-2)`⟩ · the two KF-W10 numerals ⟨`O-8` count⟩ ⟨`O-11` count⟩}; **disposition per id**: each quotation's **own banked anchor is re-read and KEPT** (LAW F(3) — per row, never per carrier), OP-3's force retained as a labelled paraphrase, both numerals **RETIRED to the property they supported**; **nothing is dropped, re-keyed, narrowed or minted** — no row, gate, bounds item, edge, direction or force changes, and the three edges stand as declared; **citing/terminus waves notified** = {**KF.W10** (the far ends are W10's own receipts, cured at its seat per RULINGS-5 §END KF-W10 D-6), **RECONCILE** (`PASS-5/CLOSE-CERT-2.md` verifies all three anchors at final bytes)}. The same struck set is carried at **cross-edge 11** and **§9 item 5**, its other two sites, and is enumerated once at **§14**.
- **KF-W0 — declares the REVERSE edge**, at two stable anchors re-resolved this seat (the round-1 `:269`/`:306` receipts were dead): **§Sequencing, the OUTBOUND table row `KF.W1 · Mail Cure`** — *"W1's re-delivery depends on this wave making them visible and on the re-anchored counts replacing the stale ones."* — and **§Excluded, the row `KF.W1's mail re-delivery`** — *"Depends on this wave, is not performed by it."* ⟨**numeric convenience STRUCK at round-2 close (U-2/R2-7)**: the round-2 spellings `:393`/`:431` had both drifted (onto a §Gates falsifier and a §Sequencing lock respectively); **the two §-headings + the two row labels are the sole anchors and no line numbers ride these receipts**. Round-1's `:269`/`:306` were already dead and stay struck⟩. Conflict RECORDED, never silently reconciled (cross-edge 2).
- **KF-W2 · KF-W5 · KF-W6 · KF-W7 · KF-W8 · KF-W9 — the SIX non-consumers, and the conditional mark DISCHARGES on the grep.** ⟨**Re-cut at repair round 4** — checks **D4-1** (the set was stated as three, when six siblings carry no KF.W1 edge) and **D4-5** (the bare-token zero was stated as universal and is false at one member). **SUBSTRATE, RE-CUT AT REPAIR ROUND 5 under RULINGS-5 LAW E(4)**: the round-4 spelling — *"every figure in this bullet is this seat's own command output over the sibling bytes listed in the §13 **mtime table**, at **2026-08-28 16:44 EDT**"* — is **STRUCK as a per-seat clock this seat cannot keep** (the mechanism behind nine of the drift tail's sixteen rows, RULINGS-5 R5-9). The **substrate of record** for every same-round cross-wave figure in this file is **`PASS-5/CLOSE-CERT-2.md`'s substrate table**, hash-proven under LAW E(1)–(2) and written after the last per-wave write; **`PASS-4/CLOSE-CERT.md` is VOID as proof-of-order** — its twelfth write is attributed at **RULINGS-5 R5-2** and its repairs bank, but the lawful close of rounds 4 **and** 5 is CLOSE-CERT-5 (this file's `D5-3` citation, re-pointed here). Cross-wave receipts in this bullet are **anchor-only**: §-heading + row/gate id, no quoted sibling prose, no line numbers, no stamps.⟩ Bare-`KF.W1` token — `grep -oE 'KF\.W1([^0-9A-Za-z-]|$)' <sibling>.md | wc -l` — carries **NO KF.W1 EDGE in any of the six** — the property, which no sibling write can falsify, stated in place of the per-sibling zeros ⟨***the lead clause ~~"0 in FOUR (KF-W2 · KF-W5 · KF-W6 · KF-W8; W2's literal `KF.W1` greps resolve to KF.W11/W13 prefixes)"~~ is STRUCK at repair round 6, PASS-6 D6-2, at all THREE of its live sites (`:60` · `:368` · `:415`). It was falsified in four consecutive rounds and does not get a fifth restatement: at round-5 final bytes the token reads KF-W2 **7** · KF-W5 0 · KF-W6 0 · KF-W8 0 · KF-W7 1 · KF-W9 1, all seven KF-W2 hits are the genuine bare token, and the KF.W11/W13 parenthetical is false on its own terms. The four legs of the property are re-verified: each of the six either declares KF.W1 a NON-EDGE or names it nowhere.***⟩ and **1 each in KF-W7 and KF-W9 — BOTH DECLARED NON-EDGES, neither an edge** ⟨**RECONCILE SEAT, 2026-08-28 — THE TOKEN FIGURE IS SUPERSEDED AT FINAL BYTES; THE EDGE CLAIM STANDS (`PASS-5/CLOSE-CERT-2.md` §4 · finding R5-R1; LAW E(5)(iii), the stage-1↔stage-1 class).** Re-run after the last per-wave write: `grep -oE 'KF\.W1([^0-9A-Za-z-]|$)' <sibling>.md | wc -l` → **KF-W2 7** · KF-W5 **0** · KF-W6 **0** · KF-W8 **0** · KF-W7 **1** · KF-W9 **1**. ***"0 in FOUR" is FALSE at the round's final bytes, and so is the KF.W11/W13 parenthetical beside it***: all seven KF-W2 hits are the genuine bare token `KF.W1'` — the possessive in *"`O-21` — KF.W1's outbound row"* — minted by **KF-W2's OWN round-5 `D-13` cure**, which landed at **18:54:59**, *before* this file's 18:56:54 write and *after* this seat read it. `grep -oE 'KF\.W1[0-9]' KF-W2.md` returns the prefixes separately (**KF.W10 3 · KF.W11 12 · KF.W12 8 · KF.W13 6**), so the regex was never the issue. **D5-1's class fired a THIRD consecutive round, one sibling over, inside its own cure — and this time from a *stage-1 peer*, which is precisely the class LAW E(5)(iii) added to the sweep roster this round. THE SUBSTANCE IS UNTOUCHED AND WAS RE-VERIFIED, NOT INHERITED**: both load-bearing properties HOLD at final bytes — `grep -c 'O-11'` → **0** in all six, and **every one of the fifteen `O-8` hits** across the six resolves to a `KF-CO-8`/`KF-CO-1` substring — so **the SIX carry no KF.W1 edge exactly as this bullet claims**; W2's seven name this wave's outbound row as a *relay-vehicle id*, which is an attribution, not an edge. **Per LAW E(4) the per-sibling token figures are hereby DATED OBSERVATIONS superseded by the cert and are NOT re-issued at fresh numerals: the ANCHOR and the two properties are the receipt.**⟩ ⟨**ROUND-5 CORRECTION, check `D5-1` (MAJOR), stated not silently applied (E-3).** This clause read *"**0** in **FIVE** (KF-W2 · KF-W5 · KF-W6 · **KF-W7** · KF-W8)"* — **STRUCK as false at the bytes.** KF-W7 acquired its hit at its **stage-2** write, at **KF-W7 §LAW C SUBSTRATE DISCLOSURE, the `BACKWARD (stage-1 siblings …)` bullet**, where it lists this wave among the stage-1 siblings whose receipts it quotes by command — **a stage-roster listing, not an O-8/O-11 citation** (`grep -c 'O-11' KF-W7.md` → **0**; both its `O-8` hits are `KF-CO-8` substrings). This is **D4-5's shape one sibling over**: round 4 corrected the zero at KF-W9 and left the universal standing over a set whose fifth member moved later in the same round, on *the one axis that defines this wave's consumer set*. **Consequently the per-sibling zeros are RETIRED as the load-bearing form** — they are readings any sibling write can move, and under **LAW E(4)** any surviving numeral rides `PASS-5/CLOSE-CERT-2.md`, never a stamp taken here. **What carries is the property below, which no sibling write can falsify.**⟩ ⟨**SHADOW (RULINGS-5 LAW F(1))**: struck set = {the universal *"0 in FIVE"* ⟨this bullet⟩ · its twin at **cross-edge 1** · its twin at **§9 item 5**, each re-read at its own site before striking (LAW F(3))}; **disposition per site**: restated as a count with both non-edges named, then **retired to the property**; **minted set = ∅**; **citing/terminus waves notified** = {KF-W7 ⟨§LAW C SUBSTRATE DISCLOSURE — its listing is correct and unchanged; no obligation is created⟩ · KF-W9 ⟨`S-10.1` — unchanged⟩ · RECONCILE ⟨CLOSE-CERT-5⟩}; enumerated once at **§14's round-5 shadow ledger**.⟩ — each of the two naming KF.W1 **in order to declare it, or to list it as, a NON-EDGE** — *a stronger reciprocation than silence*, and carried as such rather than as a defect ⟨**KF-W9 §Carry, the `S-10.1` cross-edge enumeration** — the row's *"…are **not** edges"* clause. **ANCHOR-ONLY per LAW C(3)**: KF-W9 writes in **stage 2**, later than this seat, so this receipt carries the §-heading + row id alone — no quoted prose, no line number — and the Reconcile seat verifies it after the last write.⟩ The COHESION §3.1 cohesion mark was raised *conditionally* — owed from their end **if** they cite any O-8/O-11 obligation — and the condition is **measured, not asserted: FALSE across all six.** `grep -c 'O-11'` → **0** in all six; every `O-8` hit is the `KF-CO-8`/`KF-CO-1` substring ⟨***the per-sibling operand ~~"KF-W2's at 2 occurrences / 2 lines"~~ is STRUCK at repair round 6, PASS-6 D6-1: it was 3 / 3 at round-5 final bytes and the certificate that swept this sentence names W2 at 3 in its own pasted sum three clauses away. Per this file's own D5-4/D5-5 arm the cure is subtraction — the PROPERTY carries and no per-sibling count is restated: every `O-8` hit in the six is a `KF-CO-8`/`KF-CO-1` substring, so zero real O-8/O-11 letter citations exist across the non-consumer set, and that is what the COHESION §3.1 mark turns on.***⟩ ⟨**KF-W2 §Excluded, the row `KF-CO-1 / KF-CO-8`** · **KF-W7 §Excluded, the row `KF-CO-1/KF-CO-8`** and its **N-10 SPEC-INPUT** paragraph — **all three now cited by §-heading + row label alone, **numeric convenience STRUCK at round-2 close (U-2/R2-7)****: W7's `:355`/`:111` still resolved at this sweep but ride no receipt from here on, and W2's `:428` had drifted onto its §Sequencing OP-2 item. Round 1's `:267` / `:103`/`:338` are dead and stay struck; the substring finding is unchanged at the current bytes⟩ ⟨**ROUND-4 measurement, LAW D(3), the numerals re-run at write time**: `grep -c 'O-11'` → **0** in all six; W2's `O-8` hits are **`:414`** (an F3 row citing `KF-CO-48`) and **`:670`** (its §Excluded `KF-CO-1 / KF-CO-8` row) — **both `KF-CO-8` substrings, both read this seat**, so the load-bearing claim (**zero real O-8/O-11 letter citations across the six**) is unchanged and now rests on the true count.⟩ ⟨**Round-5 note (LAW E(4))**: the two W2 coordinates just above are **dated round-4 readings, RETIRED as receipts** — they ride nothing from here on, and the W2 end is carried at its **§Excluded row `KF-CO-1 / KF-CO-8`** anchor alone.⟩ **THE PROPERTY THAT SURVIVES WRITING — the `D5-1` cure's substance, and the only form of this claim any act here depends on**: across the six non-consumers, `grep -c 'O-11' <sibling>.md` → **0**, and **every** `O-8` hit is a `KF-CO-8` / `KF-CO-1` substring — i.e. **zero real O-8/O-11 letter citations across the non-consumer set**. That is what the COHESION §3.1 conditional mark turns on; it is a property of what those files *say*, not a count that a later write can move, and it is re-verified at `PASS-5/CLOSE-CERT-2.md`. **The mark discharges; nothing is owed from any of those six ends**, and W2 is no longer singled out for a property its five siblings share.

### Goal criterion

This wave succeeds if, when work ends, **every obligation value has placed on keyframes is readable by the agent that will execute keyframes' work, at anchors that resolve in the tree that agent has checked out, with every question value has since answered itself withdrawn in writing** — and if **value's own sweep-path law can no longer produce the same defect again.** I-26's own words are the bar: *"a letter that lands untracked in a frozen checkout is NOT delivered — the path, not the content, was the defect."*

**The letters were not wrong, and they did not mismeasure.** They were *unreachable*, and their certification **named no substrate a recipient could re-derive**. This wave fixes the path and the certification; it re-scopes nothing, and it relays **no accusation of mismeasurement** to the addressee repo. The operational payload — re-anchor at `origin/master`, plus the §5a drift table — is **untouched, because it was correct.**

⟨**ROUND-3 CORRECTION, stated not silently applied (E-3) · authority RULINGS-3 `R3-2`.4 + check `D3-2`.** This paragraph read *"they certified a tree they had not measured"* — **STRUCK as false at the bytes.** Re-derived this seat, read-only, in the sacred checkout's **uncommitted worktree**: **every** anchor §5a marks ABSENT or DRIFTED resolves there, at the packets' own coordinates — `compile/value-ast.ts:71` = `const parsed = parseCssValues(value);` · `compile/easing/easing-registry.ts:36` = the exact docstring · `compile/emit/backward.ts:47` = the `sampleColorRamp`/`deltaEOK` symbol-list docblock line O-11 `:158` cites · `emit/backward-color.ts:171/:250/:263` = the `sampleRamp` declaration and its two call sites, i.e. the *"`count=1024`"* evidence tuple O-11 `:175` cites, byte-for-byte · `resolve/browser.ts:165` = `const parsed = parseCssScalar(source);` · `package.json:69` = the `"@mkbabb/value.js": "4.0.0"` pin · `emit/easing-serialize.ts:71` = the `timingFunctionEntries.find(` reverse-map head, which the packet's `:70-71` span brackets from the preceding early-return. Nine of nine drifted-or-absent rows resolve in that worktree; eight at the cited coordinate exactly, row 4 as the bracketing span just described. The packets **measured a live tree precisely and certified it accurately**; what they never did was **name** it — and the tree they left unnamed is an uncommitted worktree that **no sha addresses**. For a wave whose entire product is a letter telling an external repo what went wrong, relaying *"your letters measured a stale tree and were off by one"* would be a **false statement about their tree**, sent by the wave that exists to stop false certifications leaving this repo.⟩

## 2. Open preconditions (checked at wave-open; NOT close gates)

| # | precondition | state at authoring (2026-08-28, this seat) | check at open |
|---|---|---|---|
| **OP-1** | **The source bodies are still readable.** Their only copies on this machine are untracked files inside a frozen sibling checkout. | **PRESENT** — `VALUEJS-INBOUND-2026-07-24-parser-totality-exposure.md` **7,064 B** (mtime Jul 24 16:41) · `VALUEJS-INBOUND-2026-07-27-library-band-r1-widened-k1-k4.md` **15,633 B** (mtime Jul 27 12:27), in `/Users/mkbabb/Programming/keyframes.js/docs/tranches/V/coordination/`, alongside **252** dirty porcelain entries. | Re-`ls` before anything else. **This is the wave's single largest liveness risk** (the X.P.W1 OP-2 class): one `git clean` in that tree destroys the addendum's own source text. Unit `.a` runs first for exactly this reason. |
| **OP-2** | **The exec clone is the frontier.** | **TRUE** — `keyframes-v-exec` HEAD = **`81a56990`** = kf `origin/master`, re-verified 2026-08-28. | Re-verify; record the sha *in* the addendum (G-KF1-1 green condition). |
| **OP-3** | **Write authority for `keyframes-v-exec`.** ⟨census (c)5: "keyframes.js is sibling-read-only under value.js law … K1–K4 and every kf-side wave have no owning formation on the kf side today"⟩ | **UNRESOLVED** — the same OP-1 that `KF-W4.md` names. Note the asymmetry: this wave writes **one documentation file into a coordination directory**, which is what a mail path is *for*; it does not touch their source. | The owner's word if the begin-word does not already carry it. |
| **OP-4** | **§B-12 reconciliation** (the sacred checkout 41 behind). | **NOT DONE** — `git rev-list --left-right --count origin/master...master` → **`41  1`**, HEAD `8281638c`, re-measured 2026-08-28. | **Explicitly NOT required.** See §7 cross-edge 2. If it lands first, this wave is unaffected — it anchors on `origin/master` either way. |

---

## 3. Scope

1. Back-fill verbatim copies of O-8 and O-11 into value's tree under the live `value-inbox-<date>-<slug>.md` convention, each with a one-line provenance header (source path, original mtime, byte count). Originals are **never** corrected in place.
2. Re-resolve every anchor the two packets cite against kf `origin/master`, producing the site-drift correction table.
3. Author ONE amendment-addendum carrying exactly four payload items — (a) the HEAD-resolved anchors, (b) the three missing `parseStylesheet` sites with their failure postures, (c) the withdrawn delivery-vehicle question, (d) the D-GAP-6 `sampleBezier` resolution — plus the withdrawn import-census certification, the minimal ledger-mark ask, and the owner-facing §B-12 request.
4. Deliver it to `keyframes-v-exec/docs/tranches/V/coordination/` in their `<SENDER>-INBOUND-*` grammar; retain a copy in value's tree.
5. Repair the **keyframes sweep-path law** in `INBOX.md`'s four-path preamble — the bullet at **`:15-16`** (re-measured this seat, repair round 2; the round-1 `:13-14` anchor is dead — `:13-14` is now the glass BI→BJ→BK note that commit `7a7dc6ef` added) — so the path names the exec-visible tree and the sacred checkout is marked READ-ONLY / NEVER-DELIVER.
6. Move I-26 (at `:92`) ROWED → CURED with its delivery receipt; mint outbound row **O-21** — the measured ledger maximum **O-20** (at `:95`) **+ 1**, re-measured at open per C-10's MINT LAW, never taken from this literal; leave the kf-side mark tracked OPEN under I-26.

### 3a. Triumvirate dispatch

Triggered by: (i) OP-1 failing at open — the source bodies gone from the sacred checkout (bounds expansion: reconstruction from the census's quotations is a *different wave* and must not be improvised here); (ii) any anchor in the drift table failing to resolve to *any* live path at `origin/master`, which means the drift is deeper than a rename and O-11 §A3's scope claim needs re-derivation, not re-anchoring; (iii) a second consecutive failed delivery to the exec clone (write authority is then the root cause, not the path — halt, do not retry a third time).

---

## 4. Bounds

| File | Access |
|---|---|
| `/Users/mkbabb/Programming/value.js/docs/tranches/X/keyframes/waves/KF-W1.md` | **modify** (this spec; filename per the reconciliation in the header). The superseded **create** grant is **SPENT**: the file exists (2026-08-28, `acff6265`) and is repaired in place thereafter; `W1.md` is never created. Listing a file already on disk as `create` was the same error G-KF1-11 carried — corrected in the same motion |
| `/Users/mkbabb/Programming/keyframes-v-exec/docs/tranches/V/coordination/VALUEJS-INBOUND-<open-date>-o8-o11-amendment-addendum.md` | **create** — the wave's **SOLE sibling-tree write**; their `<SENDER>-INBOUND-*` grammar; the tranche root is NOT a mail path |
| `/Users/mkbabb/Programming/value.js/docs/tranches/V/coordination/INBOX.md` | **modify** — exactly three edits, **re-aimed at repair round 2** (all three round-1 coordinates were dead; re-measured this seat against the 97-line / 56,702 B file last written by `7a7dc6ef`): the **keyframes sweep-path law** at **`:15-16`** · **I-26** at **`:92`** ROWED→CURED · **a new outbound row `O-21`** (= the measured maximum **O-20** at `:95`, **+1**; re-measured at open per C-10's MINT LAW). The row ids (`I-26`, `O-20`) are the anchors; the line numbers ride as convenience and are re-resolved at open |
| `/Users/mkbabb/Programming/value.js/docs/tranches/V/coordination/value-inbox-<open-date>-o8-o11-amendment-addendum.md` | **create** (retained copy) |
| `…/value-inbox-2026-07-24-parser-totality-exposure.md` · `…/value-inbox-2026-07-27-library-band-r1-widened-k1-k4.md` | **create** (verbatim back-fill + provenance header) |

**READ-ONLY, SACRED — never written, never git-mutated**: `/Users/mkbabb/Programming/keyframes.js/**`, specifically the two untracked packet bodies. Reads and copy-out only.

**READ-ONLY (anchor re-resolution target)**, `keyframes-v-exec` at `81a56990`: `src/animation/compile/adapter.ts` · `compile/value/compile.ts` · `compile/easing/registry.ts` · `compile/emit/easing-serialize.ts` · `compile/emit/backward/backward.ts` · `compile/emit/backward/color.ts` · `compile/emit/css-text.ts` · `scroll/grammar.ts` · `validate.ts` · `internal/leaves.ts` · `load-engine.ts` · `resolve/browser.ts` · `engine/options.ts` · `package.json` · `docs/tranches/V/coordination/INBOUND-LEDGER.md`.

**READ-ONLY, NEVER REWRITTEN** (dated documents; amendments land as dated addenda per COHESION §0): `megatranche/formation/keyframes/CENSUS-2026-08-03.md` · `lane-docs.md` · `lane-frontend.md` · `lane-library.md` · `audit/codex-provenance/INTAKE-ADJUDICATION-2026-08-03.md`.

**READ-ONLY (citation sources)**: `registry/CARRY-CUT-LEDGER.md` (CC-084 at `:186`; X-W9 at `:255`) · `registry/adjudicated/library-band.md` (the `sampleBezier` DECLINE at **`:205-206`** — the span, re-cut at repair round 4 per check **D4-8**: the quoted string straddles the two lines and *"`:205`"* alone named a coordinate the quotation does not fit; the corroborating retirement sentence is at `:173`) · `docs/tranches/X/COHESION.md` §2/§3.

**Do NOT touch**: any `src/` in either repo · the census file bodies · KF.W0's §B-12 act · glass-ui and the SS-6 communique · `scripts/dev/dev.sh` (unowned, either repo, **NEVER**).

### 4a. Disjointness

Three serial units; `.a` writes only the two back-fill files, `.b` only the addendum + its retained copy, `.c` only `INBOX.md`. No shared modify path. No parallel wave writes `INBOX.md` — E13 mail edits are single-writer by standing law. No sibling KF wave writes `coordination/**` or this spec file (re-checked against the seven authored siblings, 2026-08-28).

### 4b. Agent units

| unit | goal | mechanism | files | sub-gate |
|---|---|---|---|---|
| **X.KF.W1.a** Retention and Re-anchoring | The packet bodies survive in value's git, and every anchor they cite is resolved against the frontier | Copy **out** of the sacred checkout (read-only), add provenance headers, then `git cat-file -e` / line-content assertion per anchor at `81a56990` | the two `value-inbox-2026-07-{24,27}-*.md` | G-KF1-9, G-KF1-2 (table produced) |
| **X.KF.W1.b** The Addendum | One letter, exec-visible, carrying exactly the four payload items | Author in their inbound grammar; first payload section = the drift table; deliver; record the clone HEAD sha inside the letter | the addendum + its retained copy | G-KF1-1, G-KF1-2..6, G-KF1-10 (ask half) |
| **X.KF.W1.c** Ledger and Law | The ledger records what happened, and the law that produced I-26 stops pointing at the frozen tree | Three surgical edits to `INBOX.md`, performed **after** delivery | `INBOX.md` | G-KF1-7, G-KF1-8 |

### 4c. LAW A — the import-graph law, discharged ⟨`conformance/PASS-3/RULINGS-3.md` **LAW A**⟩

**(i) The module clause is VACUOUS here, by enumeration of the bounds table above — not by assertion.** This wave's six bounds FILES on five rows (counting rule at §14's receipt) are `modify` ×2 (`KF-W1.md`, `INBOX.md`) and `create` ×4 (the addendum, its retained copy, the two back-fills). **All six are markdown documents. This wave deletes, repoints or shims NO module in either repo** — every `src/` is out of bounds by §4's **Do NOT touch** row and by the execution gate. LAW A's census requirement has no subject at this wave; it is discharged by having none.

**(ii) This wave does contain ONE repoint act — of a mail-path law, not a module — and it gets a LAW-A-SHAPED CENSUS anyway** (C-9 / G-KF1-8, which repoints the keyframes landing path off the frozen checkout). Commands **re-executed by this seat at the value.js tree bytes at repair round 4, 2026-08-28 16:44 EDT**, pasted with their outputs ⟨**LAW D(3), the numeric arm**: every figure below was produced by re-running its command **at this write's final bytes**, after every other round-4 edit to this file had landed — see the round-4 correction block that follows for why that ordering is the whole point⟩:

```
# (1) SPECIFIER census — the path as written:
grep -rnF 'keyframes.js/docs/tranches/V/coordination' docs/ --include='*.md'
#   → 26 hits across 18 files
# (1b) the law's own relative spelling (the memory limb's spelling too):
grep -rnF '../keyframes.js/docs/tranches/V/' docs/ --include='*.md'
#   → 15 hits across 8 files
# (2) SUBJECT census — the sweep law itself, by its four-path preamble bullet:
grep -n '^- `\.\./keyframes\.js' docs/tranches/V/coordination/INBOX.md
#   → exactly 1 — INBOX.md:15, the law spanning :15-16
```

⟨**ROUND-4 CORRECTION, stated not silently applied (E-3) · check `D4-2` (MAJOR) — the wave's sole repoint-act discharge neither reproduced nor summed.** Three independent failures, all cured here, none of them touching the finding:
**(a) The totals were invalidated by this file's OWN later edits in the same round.** Round 3 pasted *"**23** hits / 17 files"* and *"**12** hits / 7 files"*; pass 4 measured **25 / 17** and **14 / 7**, and **the entire 2-hit delta was inside `KF-W1.md` itself** — `:255` (G-KF1-5's new three-tree literal grep, the D3-10 cure) and `:269` (G-KF1-8's witness) were **written after §4c's census was taken, and the census was never re-run.** *A wave whose LAW-A census is invalidated by its own later edits in the same round is the exact failure LAW A exists to force into view.* **The mechanism is now named and defused below** (the self-reference disclosure + the stable denominator), and the numerals above are re-run **last**.
**(b) The non-consumer enumeration did not sum to its own stated total** — *"**22** of the 23 hits"* enumerated 4 + 1 + 1 + 1 + 1 + 2 + 6 + 1 + 1 + 3 = **21**, one short at the spec's own figures. **Cured by partition**: the enumeration below is **exhaustive and disjoint**, its classes sum to the pasted total by construction, and the total is stated **at** the enumeration with its counting rule (LAW D(3), the S-10.1 pattern) rather than asserted beside it.
**(c) *"six `V/apotheosis/**` analysis files"* was SEVEN at the bytes** — the seventh (`probe/coordination/keyframes-inbox-2026-07-18-vnext-formation-handoff.md:456`) sits under `apotheosis/probe/` and was dropped by a reading that swept only `armA/` and `armB/`. All seven are enumerated by path and line below.
**THE FINDING SURVIVES INTACT AND IS RE-VERIFIED THIS SEAT**: every hit was inspected, the **executing-consumer set is still exactly TWO**, and **there is no third**. It is the census's *arithmetic* and *reproducibility* — the two properties LAW A names — that failed, and both are repaired here.⟩

**THE SELF-REFERENCE DISCLOSURE, stated once so no future seat re-runs into (a).** This census's pattern matches **the census's own instruments**: the command lines in this block, C-9's cure text, OP-1's bounds row, G-KF1-5's and G-KF1-8's witnesses, **and every conformance check file that audits them**. So the raw total is **structurally unstable inside a repair round** — it rises whenever this spec or a pass check is written, and it did. **Therefore the load-bearing quantity is NOT the total.** It is:

> **THE STABLE DENOMINATOR (the counting rule): the census's load-bearing figure is the count of sites that EXECUTE the path — sites that would reproduce I-26 — which is invariant under any amount of writing ABOUT the path.** It is **TWO**, measured three rounds running. The raw totals are dated readings of a drifting corpus, re-run at open and never inherited; the class partition below is what makes them checkable.

**Resolved consumer set = the sites that EXECUTE the path (the sites that would reproduce I-26) — TWO, and only one of them is in bounds:**

1. `docs/tranches/V/coordination/INBOX.md` **`:15-16`** — the four-path sweep law. **IN BOUNDS; C-9's cure site; G-KF1-8's subject.**
2. `~/.claude/projects/-Users-mkbabb-Programming-value-js/memory/feedback-mail-inbox-law.md:16` — **the same law, codified a SECOND time** in the session memory that makes every future session open with the sweep, naming *"`../keyframes.js/docs/tranches/V/`"* among its four landing paths (measured this seat). **OUT OF BOUNDS: owner configuration, not this wave's and not any wave's to edit unasked.** **Recorded as an OPEN residual, routed to the OWNER**, and stated plainly in the addendum's own self-defect paragraph: **repairing `INBOX.md` alone does not close the class** — the memory limb re-injects the frozen path at the next session open. This is exactly the finding LAW A exists to force, and it is invisible to any count of `INBOX.md` lines or to the wave's own text. **No obligation is minted on keyframes by it** (it is value's defect, in value's own configuration).

**FULL PARTITION OF CENSUS (1) — EXHAUSTIVE, DISJOINT, AND SUMMING TO THE PASTED TOTAL. Class A is the ONE in-census executing consumer; classes B–I are the NON-CONSUMER context, recorded and never counted as consumers.** ⟨**Counting rule, stated AT the enumeration (LAW D(3) / the S-10.1 pattern)**: one line-hit of census (1) = one member; classes are assigned by *what the file is*, not by what it says; every hit of the pasted total appears in **exactly one** class; the class counts sum to **26** **by construction, and the sum is the check** — `1 + 4 + 2 + 7 + 1 + 1 + 3 + 2 + 5 = 26`. **Round-3's cell partitioned only the non-consumers and stated their total beside them, which is how it came to enumerate 21 under a stated 22**; partitioning the WHOLE census makes the arithmetic self-checking. **The second executing consumer — the memory limb — is NOT a member of any class here**, because it lives outside `docs/` and therefore outside this census's scope: *the finding that matters was found by widening past the census, and the partition says so rather than leaving it to inference.*⟩

| # | class | members (path : line) | n |
|---:|---|---|---:|
| **A** | **THE LAW ITSELF — the one in-bounds EXECUTING consumer** | `docs/tranches/V/coordination/INBOX.md:15` (the law spanning **`:15-16`**) | **1** |
| **B** | **Outbound ledger path cells** — dated records of deliveries that really did land there; **never rewritten** (C-11's verbatim-retention logic applied to the ledger) | `INBOX.md:66` (**O-2**) · `:68` (**O-4**) · `:72` (**O-8**) · `:75` (**O-11**) | **4** |
| **C** | **Dated letters** (E13 bodies, never rewritten) | `coordination/keyframes-inbox-2026-07-18-vnext-formation-handoff.md:456` · `coordination/value-inbox-2026-07-20-residual-repairs.md:33` | **2** |
| **D** | **`V/apotheosis/**` analysis files** ⟨**SEVEN, not six** — round-4 correction (c); the seventh lives under `apotheosis/probe/`, outside the `armA/`+`armB/` sweep that produced the six⟩ | `armA/edict-matrix-draft.md:140` · `armA/edict-matrix.md:143` · `armB/final-PA-parse.md:73` · `armB/final-PB-color.md:224` · `armB/program-PA-parse.md:72` · `armB/program-PB-color.md:151` · `probe/coordination/keyframes-inbox-2026-07-18-vnext-formation-handoff.md:456` | **7** |
| **E** | **Formation document** — transcribes value's law **and cites its now-dead `INBOX.md:13-14` coordinate**; **READ-ONLY, NEVER REWRITTEN** per §4, so the drift is *recorded here, not repaired there* | `megatranche/formation/keyframes/lane-docs.md:290` | **1** |
| **F** | **Registry harvest receipt** (dated) | `megatranche/registry/harvest/packet-seat-O11-O15-receipts.md:11` | **1** |
| **G** | **Reformation records** (dated) | `reformation/DISPOSITIONS.md:51` · `reformation/waves/W40.md:136` · `reformation/waves/WL.md:63` | **3** |
| **H** | **Conformance artifacts auditing THIS census** — the self-reference limb: each pass check that measures the census **enters** it | `X/keyframes/conformance/PASS-1/KF-W1-CHECK.md:125` · `PASS-4/KF-W1-CHECK.md:189` | **2** |
| **I** | **This spec** — the instruments, not consumers | **`:74`** OP-1's source-bodies precondition · **`:136`** this census block's own command (1) · **`:198`** C-9's cure text · **`:322`** G-KF1-5's three-tree literal grep (the D3-10 cure) · **`:337`** G-KF1-8's witness | **5** |
| | **TOTAL** | `1 + 4 + 2 + 7 + 1 + 1 + 3 + 2 + 5` | **26** |

**Census (1b), the relative spelling — same rule, same discipline**: **15** hits across **8** files = `INBOX.md:15`/`:66`/`:68`/`:72`/`:75` (**5**) · `lane-docs.md:290` · `DISPOSITIONS.md:51` · `W40.md:136` · `WL.md:63` (**4**) · `PASS-1/KF-W1-CHECK.md:125` · `PASS-4/KF-W1-CHECK.md:190` (**2**) · this spec at **`:139`** (this block's command (1b)) · **`:159`** (the memory limb's own spelling, quoted in the consumer finding) · **`:198`** (C-9) · **`:337`** (G-KF1-8's witness) (**4**) — `5 + 4 + 2 + 4 = 15`. **1b is a strict sub-alphabet of (1) plus this spec's own relative-spelling uses**; it exists because it is the spelling **the out-of-bounds memory limb uses**, which is how that limb was found at all. ⟨**ROUND-5 RE-MEASUREMENT — the self-reference disclosure firing exactly as it declared it would, recorded not silently applied (E-3).** Class **H**'s stated membership rule (*each pass check that measures this census enters it*) took its **THIRD** member when `conformance/PASS-5/KF-W1-CHECK.md` landed. Re-run at this write's bytes, both commands unchanged: census (1) → **27 hits / 19 files**, class H = **3** (`PASS-1/KF-W1-CHECK.md:125` · `PASS-4/KF-W1-CHECK.md:189` · `PASS-5/KF-W1-CHECK.md:165`); census (1b) → **16 / 9**, class H = **3** (the pass-5 member at `:166`). The partition still **sums by construction**: `1 + 4 + 2 + 7 + 1 + 1 + 3 + 3 + 5 = 27` and `5 + 4 + 3 + 4 = 16`. The round-4 figures **26 / 18** and **15 / 8** were true at round-4 close and are preserved above as the dated round-4 readings they were — **not re-asserted, and the raw total is deliberately not re-issued as an operative numeral**, because it moves every time this census is audited, which is precisely the instability the disclosure exists to declare (`PASS-5/KF-W1-CHECK.md` §3.2 verified 26/18/15/8 at ITS read, before its own file existed — the cleanest possible demonstration). **The STABLE DENOMINATOR is unchanged at TWO** — `INBOX.md:15-16` in bounds, the memory limb out of bounds and routed to the OWNER — and it is the only figure any act, gate or row here depends on. The raw total at the round's close rides `PASS-5/CLOSE-CERT-2.md` (**RULINGS-5 LAW E**), never a per-seat stamp; the executing seat re-derives both censuses at open. **No class was added, dropped or re-keyed; class H gained one member by its own rule** — SHADOW booking at §14.⟩

**Nothing above is inherited from a CARRY, a bank paraphrase, a lane doc or a prior pass** — every figure is this seat's own command output at write time, re-measured at open like every other dated figure in this file.

**SCOPE RECEIPT (RULINGS-4 R4-10(1) — the sweep's enumeration source is the SCOPE, never the example list).** **§Bounds ACCESS COLUMN, grepped and enumerated whole this seat: SIX rows — `modify` ×2 · `create` ×4, and ZERO rows carrying `delete`, `modify/delete`, `repoint`, `shim` or `move`.** LAW-A censuses required by the access column: **0 of 0 — none owed, none missing.** The one census present (the mail-path repoint of C-9/G-KF1-8) is owed by **an act declared in §3 and §5, not by the access column**, and is written above; it is a **repoint of a law, not of a module**, which is why (i) records the module clause as vacuous **by enumeration** rather than by assertion. **SUBJECT-IDENTITY scope receipt (R4-10(1), second class)**: symbol-bearing §Bounds rows = **0** (all six rows are markdown documents; this wave names no module symbol anywhere in §4), and **every gate-witness symbol** in §6 — `parseCssValues` · `parseCssScalar` · `parseStylesheet` · `requireParsed` · `serializeCssValue` · `lerpArray` · `sampleRamp` · `sampleColorRamp` · `deltaEOK` · `timingFunctionEntries` · `sampleBezier` — is carried **with its file AND its line at a named substrate** (§5a rows 1–14 and G-KF1-2/-3/-4's commands), never as a bare leaf.

---

## 5. Carry

**Fourteen rows, C-1..C-14**, agglomerated by identity (M-25: dedupe by identity, carry cure-shape locks, route by mechanism), each with its **banked id** and each traced to a ground re-opened at the bytes. **Eight** trace to banked ids in tree (`I-26` · `SCH-4` · `SCH-5` · `lane-docs` §6 rows 1 / 2–10 / 11 / 14 / 16 / 17); **six** are declared **minted** and each names its ground (C-9..C-14). **No ledger absent from the tree is cited as a source** (header PROVENANCE; RULINGS R-12), and no `N of N rows carried` claim is keyed to one. The routed denominator is the registry's, and **its carriage is certified at `conformance/PASS-5/KF-W1-CHECK.md` §1 (2026-08-28) — 6 routed · 6 booked · 0 escaped · 0 inventions — not in this spec's voice** (LAW B) ⟨**re-pointed at repair round 5 with the whole carriage class, check `D5-2` (MAJOR)**: this site cited `PASS-3/KF-W1-CHECK.md` while the header and the alias block already cited PASS-4 — *one file, two "freshest" artifacts for one quantity*, the D3-4 class in citation form. PASS-4 and PASS-3 are the dated predecessors (E-3); all three found the identical **6 / 6 / 0 / 0**⟩. §8 states the non-carries with their reasons; **whether that statement is complete is the conformance seat's finding, not this file's claim** — and at PASS-3 that seat convicted §8 row 5's cardinality (D3-5), cured this round. Ids are **anti-rename, original for life**.

| # | banked id | sev | lands as | binding lock (verbatim where quoted) |
|---|---|---|---|---|
| C-1 | **I-26** ⟨`INBOX.md` **row I-26** — the row id is the anchor, at `:92` at this writing (re-measured repair round 2; the round-1 `:90` coordinate is dead — `:90` is row **I-21a**) ≡ `CENSUS §0.5` ≡ `lane-docs §0/§6 row 15/§7.1/§7.2` — one identity, four witnesses⟩ | **BLOCKER** | the whole wave; gates G-KF1-1, G-KF1-7 | **"Amend, do not re-send."** ⟨lane-docs §7.2⟩ Second leg, verbatim: "**and** ask the owner to reconcile the sacred checkout (§B-12), which is theirs to do, not ours." · **ANTI-STALENESS**: no measurement is inherited into the addendum — every anchor, count and interval is re-measured at authoring against `origin/master` (the census's "silent 16 days" is a dated figure; silence measured **41 days** at this authoring — newest kf-authored letter in our tree 2026-07-18) |
| C-2 | **DOCS §6 row 17** — O-11 §A2/§A3 undercount ⟨`VALUEJS-INBOUND-2026-07-27-*.md:34,:40,:52-63`⟩ | MAJOR | payload item **(b)**; gate G-KF1-3 | O-11 §A2 raised `parseStylesheet` to a **NEW crash class** (measured `parseStylesheet("a{color:constructor}")` → THROW, `:40`) yet §A3's site table named **zero** of keyframes' own call sites — the exposure letter is INCOMPLETE by exactly the sites §A2 made reachable. The addendum names **all three** with their failure postures. It **re-derives** the §A3 end-state count; it does not correct the stale number by arithmetic. It names **only** the three missed sites — LIB §4.1's 13-site Tier-A inventory is **KF.W2's object, not this letter's** |
| C-3 | **DOCS §6 row 1** — the delivery-vehicle question ⟨`…2026-07-24-*.md:101-103` — re-measured at repair round 2 in the sacred checkout (read-only); round 1's `:100-102` was off by one, `:100` being blank⟩ | MAJOR | payload item **(c)**; gate G-KF1-5 | One sentence **WITHDRAWING** the question, citing **CC-084 at `CARRY-CUT-LEDGER.md:186`** ("*no emergency 4.0.1 — ruled*"), stating value ruled without their answer. **"The asked-for decision has been pre-empted by our own ruling; the letter must be closed honestly, not left dangling."** ⟨lane-docs §6 row 1⟩ **HARD NEGATIVE**: never re-asked, never restated as open, and the letter does not pretend keyframes' silence caused the ruling. **No cut date is promised** (X·P's release condition is OWNER-GATED — COHESION §2 PLAW-BIND) |
| C-4 | **DOCS §6 row 11** — I-10 / D-GAP-6 `sampleBezier` conditional ⟨`INBOX.md` **row I-10** — the row id is the anchor, at `:47` at this writing (re-measured repair round 2; the round-1 `:45` coordinate is dead — `:45` is row **I-7**) · `library-band.md` **`:205-206`** — the span, re-verified this seat by `sed -n '205,206p'` ⟨**round-4 correction, check `D4-8`**: the citation read *"`:205`"*, but the DECLINE verb ends `:205` and its *"(permanently, measured zero demand)"* ground opens `:206`; corroborated at `:173`⟩⟩ | MAJOR | payload item **(d)**; gate G-KF1-6 | **ONE line** resolving the conditional to **"not adopted"**, citing the permanence and its ground (measured zero demand). **"CONVERGED — the conditional resolves to 'not adopted'."** ⟨lane-docs §6 row 11⟩ Not a re-litigation: D-GAP-6 is terminal both sides (DOCS §6 row 12). Recorded asymmetry (not a dissent): keyframes marked it terminal on the strength of a conditional value has now foreclosed — the addendum states the foreclosure plainly rather than letting the two ledgers read as agreeing |
| C-5 | **SCH-5** — packet anchor drift + the site-drift correction table ⟨`CENSUS §0` · `lane-docs §0/§6`⟩ | MAJOR | payload item **(a)**, the addendum's **FIRST** section; gate G-KF1-2 | **The packets measured the sender-side working tree accurately; what drifted is the RECIPIENT's tree.** ⟨**Round-3 correction (R3-2.4 / D3-2), stated not silently applied (E-3)**: this cell read *"Both packets measured the stale tree"* — **STRUCK**. Re-derived this seat: every anchor §5a marks ABSENT or DRIFTED resolves in the sacred checkout's uncommitted worktree at the packets' own coordinates (§1 Goal criterion's round-3 block prints all nine). The drift is between **that worktree** and **`origin/master`** — a real, load-bearing drift that makes the letters unusable to a recipient at the frontier, and **not** a mismeasurement by their authors.⟩ V's W5 carves (`eb4379ca`, `6f6adfaa`, `94f2c3c9`, `b3b362d9`) renamed 4+ cited paths. **"The findings are real … but four cited paths were renamed by V's W5 module carves and any keyframes executor working from HEAD will bounce off them."** ⟨lane-docs §0⟩ **The findings survive; the anchors do not.** Anchors are re-resolved at authoring, **never copied** from the table below or from the census (the D-19 re-resolve edict) |
| C-6 | **SCH-4** — the import-census **CERTIFICATION** ⟨O-11 `:52`, `:82`⟩ ⟨**round-3**: the descriptive tail *"off-by-one"* is **STRUCK as false** (R3-2.4 / D3-1 — there is no arithmetic error; see §5b); the **banked id `SCH-4` is anti-rename and unchanged**⟩ | MINOR | rides C-5's section; gate G-KF1-4 | **The error is not the count, and there was never an off-by-one — it is the CERTIFICATION, which names no substrate.** The addendum restates the measured split and explicitly withdraws *"matches your tree exactly"* by naming a **WORKTREE-STATE RECORD, not a sha**, **in the SENTENCE TEMPLATE and under the SUBSTRATE-NAMING RULE commissioned at G-KF1-4** ⟨**re-cut at repair round 4 on the C-10 MINT-LAW pattern, check `D4-4`**: the shape is commissioned, **the five dated measurements are filled at authoring**; the round-3 wording below is **PRESERVED as the dated EXEMPLAR (E-3), no longer as text to inscribe**, because *"commissioned verbatim"* and this row's own closing lock — *"Re-measure all three censuses at authoring; copy neither 61 nor 62 nor 81 from here"* — cannot both be satisfied once OP-1's *"single largest liveness risk"* fires⟩. **Dated exemplar, round 3** ⟨originally commissioned verbatim at RULINGS-3 `R3-2`.1; the wording is retained whole and the numerals in it are **exemplary, never copied**⟩: *"the census matched the keyframes.js UNCOMMITTED WORKTREE at the sacred checkout — HEAD `8281638c` + 252 dirty paths (`git status --porcelain \| wc -l` → 252, measured 2026-08-28) — a state no sha names and no recipient can re-derive; byte-identical on all six census lines (61 = /css 29 · /value 15 · /color 7 · /math 5 · /easing 3 · /transform 2). It does NOT match bare `8281638c` (81 imports, disjoint subpath alphabet) and does NOT match `81a56990` (62)."* **The measurement that makes that sentence true rides beside it at §5b**, pasted with its commands ⟨**LAW B corollary**: no cure commissions a certification its own receipts do not reproduce⟩. **LOCK (LAW B corollary, verbatim, binding on the executing seat): *"No certification cites a sha as 'the tree measured' unless the census reproduces at that sha."*** **Round-2 → round-3 correction, stated not silently applied (E-3)**: this row read *"naming the tree it matched (`8281638c`) and the tree it does not (`81a56990`)"* — **STRUCK**. That order withdrew one false certification by **inscribing another** (SCH-4 reproduced inside SCH-4's own cure, exactly as round 1's frozen ledger literal reproduced I-26 one layer up). **Re-measure all three censuses at authoring; copy neither 61 nor 62 nor 81 from here** |
| C-7 | **DOCS §6 rows 2–10 + 14** — the inherited payload ⟨lane-docs §6⟩ | MAJOR | re-anchored **by table reference only** | §E2 `lerpArray` length assertion (the single code ask; mismatched lengths silently yield `[2.5, 3.5, NaN]` in their FrameCompiler hot loop) · §C **K1** easing reference instability (21/40 unstable, 9 collisions, the `.find()` reverse-map at `emit/easing-serialize.ts:71-73`, the load-bearing false docstring at `compile/easing/registry.ts:36`) · §C **K2** tautology spec + false "barrel" docstrings (`internal/leaves.ts:28` exact) · §C **K3** nine `noUnusedLocals` errors incl. the dead `/css` type import on the light/heavy boundary (`load-engine.ts:65` exact) · §C **K4** provenance gate · §B the `serializeCssValue` fork (`emit/css-text.ts:41` exact) · §D1 the `bezierPresets` fence · §D2 the 8-curve shape change (max\|Δ\| 0.192 on `ease-out-circ`, unacknowledged) · §E1 the post-cut additive-symbol covenant · IN-ATLAS-3's `TimingFunction` fence. **LOCK: no re-ask, no re-scope, no severity change, no new obligation.** The addendum amends two letters; it is not a third — anything not already in O-8/O-11 or in payload (a)–(d) does not belong in it. Ids `K1–K4` / `G-L7a–d` are **anti-rename, original for life**. **Scope tension recorded**: §E2 is the only row asking keyframes to write code; this wave re-delivers the ask and must **not** schedule it |
| C-8 | **DOCS §6 row 16** — §B-12 owner-checkout reconciliation | MAJOR | **NOT OWNED HERE** — the addendum's second leg *asks* | The sacred checkout's 41-behind state produced I-26 and every SCH drift; its cure is one act (`git fetch && git reset --hard origin/master`, untracked V docs unharmed) that **"value.js must NOT perform"** ⟨DOCS §6 row 16, HARD NEGATIVE⟩. The addendum ASKS the owner ⟨lane-docs §7.2⟩. **DESIGN LOCK: this wave is COMPLETABLE WITHOUT §B-12** |
| C-9 | **KF-MAIL-PATH** (minted) — our own law names the wrong tree | MAJOR | gate G-KF1-8 | `INBOX.md`'s four-path sweep preamble, **the keyframes bullet at `:15-16`** (re-measured repair round 2; the round-1 `:13-14` coordinate is dead), declares the keyframes landing path as `../keyframes.js/docs/tranches/V/coordination/` — the frozen checkout. **I-26 is not a slip; it is our codified procedure executing correctly.** Unrepaired, the next letter reproduces it exactly. **PRECEDENT, in this same file and this same class, measured this seat**: commit **`7a7dc6ef`** (2026-08-28 12:33:59) cured the *glass* limb of the identical defect — a codified sweep path pinned to a dead tranche — by naming the live one, and recorded the cost in-row: *"glass moved BI→BJ→BK; sweep the LIVE tranche's coordination dir, not a pinned letter. **The stale BI pin here cost 19 days on the 08-09 letters → I-28/I-29**"* (now at `:13-14`, the very lines this row's superseded anchor named). Two independent limbs of one law-level defect, one already cured at a measured 19-day cost: that is the corroboration of this row's thesis and **the template for its two-line cure** — same shape, same file, keyframes limb instead of glass. No new obligation and no scope change follow from the precedent. Cure: name the exec-visible tree for delivery; mark the sacred checkout READ-ONLY / NEVER-DELIVER rather than deleting it (other senders' inbound does land there, and the sacred rule must stay visible); keep the grammar note verbatim. **SIZING LOCK (KISS, L-19): two lines plus one sentence of rationale — not a routing subsystem, not a script, not a linter** |
| C-10 | **KF-MAIL-LEDGER** (minted) | MINOR | gate G-KF1-7 | (i) I-26 ROWED → **CURED** with the receipt (absolute path + date + exec-clone HEAD sha); (ii) mint **O-21** naming payload items (a)–(d); (iii) the kf-side mark stays an OPEN sub-row under I-26. **MINT LAW (added at repair round 2, RULINGS-2 R2-1)**: *the commissioned id is **O-21** (= the measured ledger maximum **O-20** + 1, measured 2026-08-28 at `INBOX.md:95` / commit `48003630`); the executing seat **re-runs the max-grep at open** and, if the ledger has moved again, mints **max+1** and records the delta as a dated addendum before executing — **a spec-frozen outbound literal is never trusted over the measured ledger.*** The round-1 spelling commissioned **O-20**, an id spent nine minutes before that write on the SS-6 glass communique; executing it would have written a duplicate live id into the E13 ledger — I-26 reproduced in the ledger layer under the spec whose thesis is I-26. **ANTI-RENAME**: I-26 keeps its id for life; O-8/O-11's rows keep "SENT — *no reply on record*" **plus** an **amended-by-O-21** pointer (the pointer names whatever id the mint law yields at open), never a rewrite. **ORDERING**: valid only *after* G-KF1-1 — a ledger that marks CURED ahead of delivery reproduces I-26 in the ledger layer |
| C-11 | **KF-MAIL-COPY** (minted) | MAJOR | gate G-KF1-9 | Value's two most consequential outbound packets are **single-copy, outside value's git, on a tree value is forbidden to touch.** Any cleanup of untracked files there destroys them and the addendum's source text with them. Cure: verbatim back-fill under the live convention, + the addendum retained the same way. **LOCK: a read of the sacred checkout, never a write.** **LOCK: copies are verbatim — the retained originals are NOT corrected; the corrections live in the addendum (that is what "amend, do not re-send" means, and a silently corrected original destroys the evidence that I-26 happened).** **SEQ-LOCK: precedes addendum authoring** (OP-1) |
| C-12 | **KF-MAIL-MARK** (minted) | MINOR | gate G-KF1-10 (ask half only) | Explicit minimal ask for `IN-VALUE-3` (O-8) / `IN-VALUE-4` (O-11) in their grammar — their numbering, their call. **HARD NON-GATING LOCK: the marking may never come and MUST NOT gate this wave's close.** What we owe is delivery + the ask, both ours. Gating on a sibling repo's reply makes the wave un-completable by construction ⟨census (c)5⟩. Absence is re-measured at close and **recorded, not repaired** |
| C-13 | **KF-MAIL-NS** (minted) | MAJOR | the alias block at the head of this file | Declared **before** any X·KF spec is grepped for carry rows. **Reciprocal declaration OWED at KF.W0 — the identity travels today; the five component riders are COMMISSIONED to land there this round and are NOT asserted as present** ⟨**re-cut at repair round 4 · check `D4-3` (MAJOR) · RULINGS-4 §END KF-W0, the `F-1 five-rider landing (W1 D4-3's receiving end)` directive**⟩. The five riders, by id: the `useControlsKeyboardShortcuts.ts:1` sole-binding-site blast radius ⟨kf-AnimationControlsGroup:47⟩ · `ChannelControls.vue:219`'s first-failing import ⟨kf-ChannelControls:56⟩ · `ControlsPaneWrapper.vue:166`'s sole `/drawer` consumer ⟨kf-ControlsPaneWrapper:60⟩ · kf-DemoGlobalChrome's four toast utility classes ⟨kf-DemoGlobalChrome:44⟩ · kf-KfPillTabs' one-class/eight-custom-property inventory ⟨kf-KfPillTabs:46⟩ — **verbatim at the records** (§8 row 5 enumerates all six routing rows by anchor). **Landing surface**: **`KF-W0 §Carry`, the `C-1.F` fold table, row `F-1`** (the `KF.W1-DEP` identity's home there) — cited by **§-heading + row id**, the stable anchor; **no line number rides this receipt.** **LANDING VERIFIED at KF-W0's POST-LANDING BYTES, quote-by-command per LAW C(2)** — substrate named: `KF-W0.md`, **222,633 B, mtime 2026-08-28 16:57:50** (KF-W0 writes FIRST in stage 1; this seat re-probed after its write) ⟨**RECONCILE SEAT, 2026-08-28 — RE-VERIFIED AT KF-W0's TRUE FINAL BYTES, AND THE LANDING HOLDS (`PASS-4/CLOSE-CERT.md` §R-10).** The stamp above names **222,633 B / 16:57:50**, but KF-W0 wrote **once more** in stage 1: its final bytes are **224,173 B, mtime 16:58:59** — sixty-nine seconds and 1,540 bytes later. This seat re-probed there. **All five component riders are present at the final bytes**: `grep -c 'useControlsKeyboardShortcuts.ts:1' KF-W0.md` → **2** · `'ChannelControls.vue:219'` → **2** · `'ControlsPaneWrapper.vue:166'` → **1** · `'toast utility classes'` → **2** · `'eight-custom-property'` → **1**, and `C-1.F` still resolves across its table (`:226` the landing row, `:341`/`:367` the fold table). **FIVE OF FIVE, verified after W0's last write rather than its second-to-last** — the receipt was right and was taken one write early, which is the LAW C hazard in miniature and the reason this stamp is corrected rather than the claim⟩:
```
$ ls -lT KF-W0.md            → 222633  Aug 28 16:57:50 2026
$ for p in useControlsKeyboardShortcuts 'ChannelControls.vue:219' 'ControlsPaneWrapper.vue:166' \
           text-body 'eight custom' '18 shortcuts' /drawer; do printf '%-32s %s\n' "$p" \
           "$(grep -cF "$p" KF-W0.md)"; done
  useControlsKeyboardShortcuts     2      ChannelControls.vue:219   2
  ControlsPaneWrapper.vue:166      1      text-body                 1
  eight custom                     1      18 shortcuts              2
  /drawer                          2
$ grep -n 'useControlsKeyboardShortcuts' KF-W0.md
  226:| **F-1 rider landing** (the receiving end of a sibling's round-4 booking — sending end cited
       **anchor-only**, LAW C(3): `KF-W1.md` §Repair Round 4, directive D4-3) | **C-1.F row F-1**
       gains the **five component riders verbatim from their banks**, each pasted with the `sed -n`
       that produces it: …
```
**FIVE of five present, where zero were.** The two ends name each other: W0's landing row cites **this file's D4-3 directive** as its sending end, and this cell cites **W0's C-1.F row F-1** as its receiving end. ⟨**This is a RECEIPT, not a certification.** Under LAW C the round is not closed by either seat: the **RECONCILE seat** re-verifies both ends at their FINAL bytes after the last per-wave write and records the verification in `PASS-4/CLOSE-CERT.md`. **This spec certifies nothing about KF-W0's final state** (LAW B) — it states what it measured, when, and against which bytes.⟩
⟨**ROUND-4 CORRECTION, stated not silently applied (E-3) · check `D4-3`.** This cell read *"which **receives** F-1 plus its five component riders"* — a **present-tense claim about a sibling's bytes**, and false at those bytes. Measured this seat by command, substrate named per **LAW C(2)** — `KF-W0.md`, **188,714 B, mtime 2026-08-28 15:43:24**, its pre-round-4 bytes:
```
$ cd docs/tranches/X/keyframes/waves
$ for p in useControlsKeyboardShortcuts 'ChannelControls.vue:219' 'ControlsPaneWrapper.vue:166' \
           text-body 'eight custom' '18 shortcuts' drawer; do printf '%-32s %s\n' "$p" \
           "$(grep -cF "$p" KF-W0.md)"; done
  useControlsKeyboardShortcuts     0      ChannelControls.vue:219   0
  ControlsPaneWrapper.vue:166      0      text-body                 0
  eight custom                     0      18 shortcuts              0
  drawer                           0
$ grep -n 'KF\.W1-DEP' KF-W0.md
  295:| **F-1** | **D-5 / L-3 / C-2** ⟨kf-DemoGlobalChrome:43⟩ … the record's own three riders travel with it: (i) sequencing on census **F-1/KF.W1-DEP**; …
```
**Zero of five.** KF-W0's nine `KeyboardShortcuts` hits are all `kf-KeyboardShortcutsModal` — **a different component** — and its single `KF.W1-DEP` mention at `:295` carries `kf-DemoGlobalChrome`**`:43`**'s **S-9 toast-family** sequencing rider, **not** the `:44` four-utility-class rider named here. **The identity DOES travel** (`KF-W0` C-1 = *"F-1 ≡ SCH-1"*, plus its fold-index rows) — **the riders did not.** This is load-bearing on the census axis, not on prose: **the booking mechanism for all six routed rows is "routed away to KF.W0"** (§8 row 5), so riders absent at the routing target mean six rows booked into a wave that cannot execute what the booking says it received — the **D3-8 class** (*a rider omitted from the operative transit declaration is a rider lost in transit*) one seam further out, found and cured **inside** this file at round 3 while this instance, which crosses a wave boundary, went unswept. **The cure is two-ended and neither end asserts the other's bytes**: this cell states the obligation and names the landing surface by anchor; RULINGS-4 §END has **KF-W0** write the five riders verbatim into its §Carry `F-1`/`KF.W1-DEP` row; the **RECONCILE seat** (LAW C(4)) verifies both ends at their FINAL bytes and records the verification in `PASS-4/CLOSE-CERT.md`. **Nothing here rewrites KF-W0**, and no green is claimed on the landing's behalf. **The zeros above are the DATED RECORD OF THE DEFECT, at `KF-W0.md`'s pre-round-4 bytes (mtime 15:43:24) — preserved, never overwritten by the later reading** (E-3); the post-landing verification at **16:57:50** rides in the cell above, with its own command and its own substrate. *Two dated readings of one file at two clocks, each naming its bytes — which is the whole discipline LAW C(2) exists to impose, applied to the defect that made LAW C necessary.*⟩ **Dissent recorded, not resolved**: `kf-DemoGlobalChrome.md:44` banks a rider that survives SCH-1's falsification — commit `a59d3a22` ("fix(dependencies): consume Glass UI 6 from one registry core") exists in history "yet the declaration is still absent — F-1 is not merely open, it survived a commit that claimed to address the dependency." That rider was written against the stale tree; whether it survives at `origin/master` is **KF.W0's** re-measurement, not this wave's |
| C-14 | **KF-MAIL-∅** (minted) | INFO | §7 cross-edge 10; gate G-KF1-11 | **Zero adjudicated registry rows target this wave — and the CLOSURE of that set is the conformance seat's finding, cited, never asserted in this spec's voice** ⟨**LAW B**⟩: `conformance/PASS-5/KF-W1-CHECK.md` **§1 (2026-08-28)** re-enumerated the routing alphabet from the 58 records' bytes, closed it **four** ways (bare token · substring decomposition · terminal-disposition alphabet · carry sweep), and found the **∅ column TRUE with 0 inventions** — while returning **DEFECTIVE** on this file's other axes (§14 records the cures) ⟨**re-pointed at repair round 5 with the whole carriage class, check `D5-2`**: this cell cited `PASS-3/KF-W1-CHECK.md`; PASS-4 and PASS-3 are preserved as the dated predecessors (E-3), each of which closed the alphabet the same four ways and found the same **∅ column TRUE with 0 inventions**⟩ ⟨**Dated observation, recorded at repair round 5 — check `D5-9` (INFO); a correction to a frozen register, which is why it lives HERE and not in an edit to that register.** `PASS-4/KF-W1-CHECK.md` §1.3's escape sweep reported *"`O-11` → **2 records**"*; **it does not reproduce.** Re-run this seat over the 58-record corpus: `grep -l 'O-11' kf-*.md` → **ONE** record (`kf-ChannelOptions.md`), `grep -l 'O-8' kf-*.md` → **two** (`kf-ChannelOptions.md` · `kf-LayerConfigPanel.md`); the registry is **READ-ONLY and unchanged**, so this is a mis-transcription at that seat, **not drift**. **Nothing follows for the census**: every hit under both tokens is a `KF-CO-8`/`KF-CO-11`/`KF-CO-12` substring and **none is a letter citation** — re-verified this seat by printing each hit with its preceding characters — so the escape sweep's verdict (**0 escaped**) is untouched, and `PASS-5/KF-W1-CHECK.md` §1.3 carries the corrected figure⟩. The three legs below are **this seat's own measurements, offered as the material that check re-derived independently**, not as a completeness claim: **(i)** no `kf-*.md` routes a row to census-taxonomy KF.W1. **Stated so the grep it invites cannot falsify it**: a literal `grep -o 'KF\.W1' kf-*.md \| wc -l` returns **33** — **18** `KF.W1-DEP` anchors (C-13) **plus 15 `KF.W10` substrings**, since `KF.W10` contains `KF.W1`. The load-bearing measurement is the *token*, not the substring: `grep -oE 'KF\.W1([^0-9A-Za-z-]\|$)' kf-*.md` → **0 across all 58 records**, and `KF.W1` is absent from the terminal-disposition alphabet (`→ **…**`) entirely. An earlier draft's *"the only `KF.W1` hits are the five folds"* was TRUE in substance and falsifiable as written; this phrasing is neither. **(ii)** the intake CARRY table's kf targets are KF.W0 (X-1, B10-9, B19-10/11, B20-6, B21-7), KF.W4 (**B10-17 · B10-18 · B10-19 · B10-20 · B10-22 · B10-23 · B10-27** — the intake's own **enumeration with gaps**, never the range spelling `B10-17..27`, which would swallow `B10-21` and route it to two waves in one sentence ⟨**`INTAKE-ADJUDICATION-2026-08-03.md` §3, the heading `### KF.W4 · K-Quartet Hygiene (born-RED gate-authoring law, feeds G-L7a–d)` and the enumeration under it** — at `:146` / `:147-149` at this writing, re-resolved repair round 2; the round-1 `:144-145` coordinate is dead (it is the tail of the §3 **KF.W0** block); **`KF-W0.md` §Sequencing, the OUTBOUND row `KF.W4 · K-Quartet Hygiene`** transcribes it gap-preserving as `B10-17/18/19/20/22/23 + B10-27 + B18-19 + B21-17` ⟨**numeric convenience STRUCK at round-2 close (U-2/R2-7)**; `:394` had drifted onto a §Gates falsifier — **the §-heading + the OUTBOUND row label `KF.W4 · K-Quartet Hygiene` are the sole anchor**⟩⟩ · B18-19("derivation helpers" half) · B21-17(secondary)), KF.W5 (X-4, **B10-21**), KF.W9 (B18-26), KF.W10 (B18-12/13/14, B18-23+B19-14+B20-14, B18-27) — **none targets KF.W1**; **(iii)** none of the **17** NO-WAVE-OWNER packets proposes KF.W1 as a home. **The cardinality is now RECEIPTED, per RULINGS R-15**: 17 is the canonical roster — the reconciliation of **KF-W0's** then-stale 15 and **KF-W9 S-11's** 14, namely W0's 15 ∪ {**amiga**, **EDITOR-UNIT**} = **17**. **Re-resolved repair round 2**: the round-1 receipt `KF-W0.md:276` is dead; KF-W0 carries the canonical 17 at **§Sequencing, the packet-roster line `the NO-WAVE-OWNER packets — the canonical 17`** — self-declared *"re-cut this round from this file's stale 15"* (**numeric convenience STRUCK at round-2 close (U-2/R2-7)**; `:401` had drifted onto a §Carry carries-note — the **§-heading + roster line label** are the sole anchor and no line number rides this receipt). *The **16** this row previously carried was stated by no document in the tree and was never true; C-1's ANTI-STALENESS lock forbids exactly such an inherited count, and it was applied to fourteen other figures and skipped for this one.* The roster, id for id: **cube · sequence · spring · spring-plot · spring-physics-facet · spring-artifact-truth · square · amiga · drag-seam · CARD-UNIT · OPTIONS-UNIT · KFED-UNIT · EDITOR-UNIT · APPLY-UNIT · AXISLINE-UNIT · dock-menu · transport/ribbon**. Homing authority is **KF.W4** (R-15), whose §Sequencing lists partition them **KF.W11 ×9 · KF.W12 ×6 · KF.W13 ×2**; the set-difference against KF.W1 is ∅ by enumeration, not by assertion (cross-edge 9). **The empty column is a finding, not a gap.** NEXT-COMMUNIQUE for this wave: **EMPTY** ⟨intake §3: "Zero producer-facing asks survived adjudication in any lane"⟩ — nothing here rides the SS-6 BH letter; the standing edict is satisfied **by declaration**, not by minting a rider. **LOCK (L-19): a wave with no registry rows gets no manufactured ones** — the specced work is the census's four amendment items plus the three minted self-defect repairs, and nothing else |

### 5a. The site-drift correction table (payload item (a))

Re-measured **2026-08-28** at `keyframes-v-exec` HEAD `81a56990` = kf `origin/master`. **Count reconciliation, stated not silently applied**: `lane-docs §6` banks this as **12 rows**; re-measurement resolves to **14 anchor rows** (9 drifted, 5 exact) because `emit/backward-color.ts`'s three line anchors are three rows against one path rename. The executing seat re-derives this table again — it does not copy it.

| # | packet anchor | state at `81a56990` | measured |
|---|---|---|---|
| 1 | `src/animation/compile/value-ast.ts` | **ABSENT** → live `compile/value/compile.ts:32` (`const parsed = parseCssValues(value);`) | 2026-08-28 |
| 2 | `resolve/browser.ts:165` | → **`:162`** (`const parsed = parseCssScalar(source);`) | 2026-08-28 |
| 3 | `compile/easing/easing-registry.ts:36` | **ABSENT** → live `compile/easing/registry.ts:36` (same docstring: *"Stable identities let the serializer distinguish named curves from closures."*) | 2026-08-28 |
| 4 | `emit/easing-serialize.ts:70-71` | → **`:71-73`** (the `timingFunctionEntries.find(([_name, func]) => func === easing.fn)?.[0]` reverse-map) | 2026-08-28 |
| 5 | `emit/backward.ts:47` | **ABSENT** → live `emit/backward/backward.ts` (dir now holds `backward.ts`, `color.ts`, `index.ts`, `walk.ts`) | 2026-08-28 (path only) |
| 6–8 | `emit/backward-color.ts:171` / `:250` / `:263` — O-11 `:175`'s *"`count=1024`"* evidence tuple | path → `emit/backward/color.ts`; **all three LINE anchors now MEASURED, and all three are EXACT at the frontier**: `:171` = `const sampleRamp = (` · `:250` = `const ramp = sampleRamp(fromColor, toColor_, stopCount, space, hueOpt.hueMethod);` · `:263` = `const kfRefRamp = sampleRamp(fromColor, toColor_, 1024, space, hueOpt.hueMethod);` — byte-identical to the sacred worktree's `backward-color.ts` at the same three lines (both files **385 L**). **PATH drift only; ZERO line drift.** ⟨Round-2's *"line anchors NOT re-measured at spec-authoring — the executing seat resolves all three"* is **STRUCK (D3-11)**: it was this file's last unmeasured figure, sitting under a header whose own law is *"anchors are re-resolved at authoring, never copied"* and beside §11 row 8, which measured row 14 for exactly this reason. The executing seat re-derives it at open like every other row, but **inherits nothing**.⟩ | 2026-08-28 (round 3) |
| 9 | `package.json:69` (the `"@mkbabb/value.js": "4.0.0"` pin) | → **`:70`** (re-verified this seat) | 2026-08-28 |
| 10 | `engine/options.ts:31` | **EXACT** (`const parsed = parseCssScalar(raw);`) | 2026-08-28 |
| 11 | `internal/leaves.ts:28` | **EXACT** (`export { clamp, scale, lerp, lerpArray } from "@mkbabb/value.js/math";`) | 2026-08-28 |
| 12 | `load-engine.ts:65` | **EXACT** (`import type { Stylesheet } from "@mkbabb/value.js/css";` — the dead `/css` type import) | 2026-08-28 |
| 13 | `emit/css-text.ts:41` | **EXACT** (`export const serializeCssValue = (value: CssValue): string => {`) | 2026-08-28 |
| 14 | `test/internal/leaves-parity.test.ts` | **EXACT — re-measured at repair round 2** (`git cat-file -e 81a56990:test/internal/leaves-parity.test.ts` → **PRESENT**). Round 1 left this the file's one inherited figure under a header whose own law is *"anchors are re-resolved at authoring, never copied"*; it is now measured, so **the executing seat inherits nothing** — and re-measures it again at open, like every other row | 2026-08-28 (round 2) |

### 5b. The substrate receipt (C-6 · G-KF1-4) — the ANCHOR DUALITY, measured

**Why this block exists.** C-6 commissions a sentence about which tree O-11's census matched. Under **LAW B's corollary** no cure may commission a certification its own receipts do not reproduce, so the measurement rides here, beside the order, with its commands. All three were run **read-only** by this seat on **2026-08-28**; the sacred checkout was read (`git rev-parse`, `git status`, `grep`, `git grep`) and never written, per the SACRED-CHECKOUT LAW.

```
# (A) the sacred checkout's UNCOMMITTED WORKTREE — the tree O-11 actually measured
$ cd /Users/mkbabb/Programming/keyframes.js && git rev-parse HEAD
  8281638c0ac4ac8c54a67a018ca5bf6a9117174f
$ git rev-list --left-right --count origin/master...master   →  41   1
$ git status --porcelain | wc -l                             →  252
$ grep -h -o -E 'from "@mkbabb/value\.js[^"]*"' -r src | sort | uniq -c
   7 /color   29 /css   3 /easing   5 /math   2 /transform   15 /value      → 61
  # BYTE-IDENTICAL to O-11 :52's certified split — all six lines, and the total.

# (B) bare 8281638c — the sha an earlier cure ordered inscribed as "the tree it matched"
$ git grep -h -o -E 'from "@mkbabb/value\.js[^"]*"' 8281638c -- src | sort | uniq -c
  10 (root)  9 /color  5 /easing  5 /math  24 /parsing  2 /transform  26 /units  → 81
  # DISJOINT ALPHABET: /css and /value do not exist at that sha; /parsing and /units do.
  # ZERO of O-11's six lines reproduce. 81 ≠ 61.

# (C) the frontier of record
$ cd /Users/mkbabb/Programming/keyframes-v-exec
$ git grep -h -o -E 'from "@mkbabb/value\.js[^"]*"' origin/master -- src | sort | uniq -c
   7 /color  29 /css  3 /easing  5 /math  2 /transform  16 /value            → 62
```

**Corroborating receipt, same seat, same clock — the ANCHORS agree with the census.** The three anchors §5a rows 6–8 carry resolve **byte-exactly** in the worktree at the packet's own coordinates (`backward-color.ts:171/:250/:263` = the `sampleRamp` declaration and its two calls, the *"`count=1024`"* tuple O-11 `:175` cites) and **do not** resolve at bare `8281638c`, where those same three line numbers are docblock prose (`:171` *"The WORST midpoint ΔE this key drifts…"* · `:250` a `sampleColorRamp` docblock line · `:263` an EN-b docblock line). Two independent instruments — the import census and the line anchors — name the **same** substrate. The certification's fault is therefore **not** arithmetic.

> **THE ANCHOR DUALITY, declared once and binding on every measurement in this wave and on every sentence in the addendum** ⟨commissioned verbatim, RULINGS-3 `R3-2`.3⟩: *"Two substrates, each named at each use: cures and forward anchors bind to `origin/master 81a56990` (the frontier of record); the historical census is certified against the dirty-worktree record above. No certification cites a sha as 'the tree measured' unless the census reproduces at that sha."*

**Consequences, stated so no seat has to re-derive them**: (1) `8281638c` may appear in this wave **only** as a named historical measurement context inside the worktree-state record — never as a witness substrate, never as "the tree measured"; (2) **every leg whose reading TURNS ON TREE IDENTITY names both its repo and its substrate** (§6, re-keyed at round 3) — **four gates plus the alias block's SCH-1 falsifier: G-KF1-2 · G-KF1-3 · G-KF1-4 · G-KF1-10**, enumerated this round so the claim is checkable at its own enumeration ⟨**round-4 correction, check `D4-10` (INFO), stated not silently applied (E-3)**: this clause read *"**every gate leg** names both its repo and its substrate"* — **STRUCK as a false universal.** Measured across §6 this seat: **G-KF1-1** is an `ls` on a path (a filesystem listing has **no ref**) · **G-KF1-5** runs absolute-path `grep`/`sed` over three named coordination directories · **G-KF1-7**/**G-KF1-8** are value-side and bind to *"the current tree bytes"* by declaration, relative path only · **G-KF1-9** is a `find` on the value.js root · **G-KF1-12** is `git status --porcelain` with its tree named in prose. **None of the six is a defect on its own** — the substrate discipline exists to kill *silent mis-resolution between two candidate trees*, which is a property of legs whose reading changes with the tree, and none of those six has a second candidate. The overstatement is the universal itself: in a file that struck a round-2 stamp for **certifying zero coordinates while carrying five**, a universal its own §6 does not meet is the same shape at smaller scale. *"Every leg whose reading turns on tree identity"* is **both true and exactly sufficient**, and it is what the D3-6 cure actually delivered⟩; (3) the recipient cannot re-derive the worktree state, and the addendum **says so in that sentence** rather than handing them a sha that would fail under them — the whole point of withdrawing the certification.

---

## 6. Gates

All witnesses below are **literal commands with literal outputs**, and every one was **RE-EXECUTED at repair round 2 (2026-08-28)** — keyframes-side against the frontier of record `origin/master` **`81a56990`** (local `8281638c` is DISQUALIFIED and consulted only to date drift), value-side against the current tree bytes (`INBOX.md` = 97 L / 56,702 B, last written by `7a7dc6ef`). **Nothing below is inherited from prose, from a ruling, or from this file's own prior text** (RULINGS-2 **R2-5**, the witness re-derivation law). Dated figures are non-inheritable: they are re-measured again at open. **Where round 2's measurement differs from round 1's, the round-1 reading is quoted, struck by name, and the true one stated beside it** — never silently overwritten (E-3).

**G-KF1-1 · DELIVERY — the addendum exists at an exec-visible path** — *born-RED*
Witness ⟨***ROOT VARIABLE DECLARED ONCE, HERE — PASS-7, 2026-08-29, check `P7-8`.*** `$PROG` = **the directory that holds the sibling clones**; at this seat `$PROG=/Users/mkbabb/Programming`. Every cross-repo command in this file is written against `$PROG` from this round forward, so no command carries a machine path in its own body; the binding is a **dated fact of this seat**, re-stated by whoever runs them elsewhere⟩: `ls $PROG/keyframes-v-exec/docs/tranches/V/coordination/` → **9 entries + `vnext/`**: `ATLAS-INBOUND-2026-07-16-*`, `ATLAS-INBOUND-2026-07-17-*`, `GLASS-INBOUND-2026-07-16-*`, `GLASS-INBOUND-2026-07-17-*`, `INBOUND-LEDGER.md`, `SPEEDTEST-INBOUND-2026-07-17-*`, `VALUEJS-INBOUND-2026-07-17-{formation-exchange,formation-exchange-marks,wl-verdicts}.md`. **No addendum. No O-8. No O-11.** Clone HEAD `81a56990`, same session.
GREEN: the file exists at `…/VALUEJS-INBOUND-<open-date>-o8-o11-amendment-addendum.md` and the clone's HEAD sha is recorded **inside** it. Rows: C-1, C-10.

**G-KF1-2 · ANCHOR LIVENESS — every `file:line` in the addendum resolves at kf `origin/master`** — *born-RED*
**REPO AND SUBSTRATE ARE NAMED IN EVERY COMMAND** ⟨re-keyed at repair round 3 — RULINGS-3 `R3-2`.2, check **D3-6**; the idiom G-KF1-3 and G-KF1-10 already use⟩. **Frontier legs run `in keyframes-v-exec` against `origin/master`, never bare `HEAD`.**
Witness, **in `keyframes-v-exec`**: `git cat-file -e origin/master:src/animation/compile/value-ast.ts` → **ABSENT** (O-11 §A3's primary authored-input anchor); same for `compile/easing/easing-registry.ts` and `compile/emit/backward.ts`. `git grep -n 'parseCssScalar' origin/master -- src/animation/resolve/browser.ts` → **`:162`** (`const parsed = parseCssScalar(source);`; the import at `:3`), packet says `:165`. `git grep -n '"@mkbabb/value.js"' origin/master -- package.json` → **`:70`**, packet says `:69`. Five anchors verified EXACT (rows 10–13 above + row 14, measured at round 2) and rows 6–8 measured at round 3 — **so this is a per-anchor check, not a blanket one.**
**Historical legs — where the packets' own coordinates are resolved — run against the WORKTREE-STATE RECORD of §5b** (the sacred checkout's dirty worktree: HEAD `8281638c` + porcelain 252, dated), **never bare `HEAD`, never bare `8281638c`.**
⟨**Round-2 → round-3 correction, stated not silently applied (E-3).** This gate's legs read bare `HEAD:` and `HEAD --` with **no repo named**, while §4's SACRED-CHECKOUT LAW puts `/Users/mkbabb/Programming/keyframes.js/**` in bounds as a read target and OP-1's `ls` runs there — so bare `HEAD` most likely resolves to the **DISQUALIFIED** `8281638c`. Demonstrated by this seat: run in the sacred checkout, `git grep -n 'parseCssScalar' HEAD -- src/animation/resolve/browser.ts` returns **nothing (exit 1)** — the assertion `→ :162` cannot even fail informatively — and `git grep -c 'parseStylesheet' HEAD -- src` returns **0 files**. §6's blanket preamble declares the frontier in prose; **the commands are what a seat runs**, so the substrate is now written into each of them.⟩
GREEN: every anchor passes `git cat-file -e` + a line-content assertion at authoring time **at the substrate its own row names**, and the §5a table is the addendum's **first** payload section. Rows: C-5, C-7.

**G-KF1-3 · `parseStylesheet` COMPLETENESS — the three missed sites named** — *born-RED*
Witness, **in `keyframes-v-exec`, against `origin/master`** ⟨substrate written into the command at repair round 3, on the D3-6 rule: `HEAD` in that clone is `81a56990` with porcelain **0**, so the reading is unchanged — but the spelling no longer *depends* on that being true at the executing seat's clock⟩: `git grep -n 'parseStylesheet' origin/master -- src` → call sites **`compile/adapter.ts:222`** (`const result = parseStylesheet(source);`), **`scroll/grammar.ts:109`** (`? requireParsed(parseStylesheet(input), input)`), **`validate.ts:182`** (`const parsed = parseStylesheet(css);`); imports at `adapter.ts:7`, `scroll/grammar.ts:37`, `validate.ts:47` — all six re-verified at repair round 2 against `81a56990`. **The command's full output is NINE lines, stated so a re-running seat gets what the witness promises**: the six code lines above **plus three prose mentions** (`adapter.ts:202` *"Value's `parseStylesheet` directly if it needs the full set"* · `validate.ts:41` the `@mkbabb/value.js` edges docblock · `validate.ts:130` the numbered `parseStylesheet(css)` walk-through). The load-bearing set is the **six code lines**; narrow with `-- src` plus a `^\s*\(import\|.*= *parseStylesheet\|? requireParsed\)` filter, or read the nine and take the six. O-11 §A2 (`:34`) declares `parseStylesheet` a **NEW crash class** and `:40` records the measured THROW, while §A3's site table (`:52-63`) names **0 of 3**.
GREEN: all three named at re-resolved anchors, each with its LIB §7.5 failure posture (**absorb-to-diagnostics** at the adapter · **`requireParsed` THROW** at scroll/grammar · **swallow-to-`[]`** at validate), so the recipient sees that ONE value diagnostic-shape change touches three of their sites three different ways; the §A3 end-state count re-derived, not corrected by arithmetic. Row: C-2.

**G-KF1-4 · IMPORT CENSUS — the exactness certification withdrawn** — *born-RED*
**REPO AND SUBSTRATE ARE NAMED IN EVERY COMMAND** ⟨re-keyed at repair round 3 — RULINGS-3 `R3-2`.1/.2, checks **D3-1** and **D3-6**⟩. **Three censuses, three named substrates, all pasted with their outputs at §5b** — that block is this gate's receipt and is not restated here.
Witness (frontier leg), **in `keyframes-v-exec`**: `git grep -h -o -E 'from "@mkbabb/value\.js[^"]*"' origin/master -- src | sort | uniq -c` → **62** total: `/css` **29** · `/value` **16** · `/color` **7** · `/math` **5** · `/easing` **3** · `/transform` **2** (re-run 2026-08-28, exact). Witness (historical leg), **in the sacred checkout, against the §5b WORKTREE-STATE RECORD** — read-only, no `git` object needed: `grep -h -o -E 'from "@mkbabb/value\.js[^"]*"' -r src | sort | uniq -c` → **61**, split `/css` 29 · `/value` **15** · `/color` 7 · `/math` 5 · `/easing` 3 · `/transform` 2 — **byte-identical to O-11 `:52`, all six lines**. Contrast leg, same tree, the **DISQUALIFIED** sha: `git grep … 8281638c -- src` → **81**, over a **disjoint subpath alphabet** (`/parsing` 24 · `/units` 26; `/css` and `/value` absent). O-11 `:52` says *"61 import statements total"* and that it *"matches your tree exactly"*; `:82` repeats *"across 61 of your sites"*.
GREEN **(re-cut at repair round 4 on the C-10 MINT-LAW pattern — check `D4-4`)**: the measured **frontier** split is stated, **and** the certification is explicitly withdrawn by naming a **WORKTREE-STATE RECORD, not a sha**, in **C-6's commissioned SENTENCE TEMPLATE**, with the substrate-naming rule below governing and §5b's measurement standing beside it.

> **THE SUBSTRATE-NAMING RULE (what this gate commissions — the rule, never the literals).** The addendum's withdrawal sentence must (i) name the substrate as a **dated worktree-state RECORD** — the tuple ⟪HEAD sha · porcelain count · the six-line import split · the date of measurement⟫ — and **never a bare sha**; (ii) state that **no sha addresses that state and the recipient cannot re-derive it**, which is *why* the certification is withdrawn; (iii) state the **frontier** split beside it, named as `origin/master` at its measured sha; (iv) name the **disqualified** contrast reading as a contrast, never as the tree matched. **Every numeral in (i)–(iv) is FILLED AT AUTHORING by re-running §5b's three commands** — the five dated measurements this spec carries (`8281638c` · **252** dirty · **61** with its six-line split · **81** · **62**) are **the round-3 exemplar, not the text to copy**.

⟨**ROUND-4 CORRECTION, stated not silently applied (E-3) · check `D4-4` (MAJOR) — the gate whose subject is "do not inscribe a measurement you cannot reproduce" was commissioning five measurements to be inscribed verbatim.** This GREEN read *"— **C-6's commissioned sentence verbatim**, with §5b's measurement standing beside it"*, and **C-6's own lock closes**: *"**Re-measure all three censuses at authoring; copy neither 61 nor 62 nor 81 from here**."* Under **OP-1** the sacred worktree is declared *"the wave's single largest liveness risk"* — this file **expects** that tree to be able to change between now and open. If it changes, *"verbatim"* and *"re-measure, copy nothing"* cannot both be satisfied and **the gate has no reachable GREEN**; if it cannot change, the anti-copy lock is decorative and the **C-1 ANTI-STALENESS** lock it derives from is void here. **The file already contained the correct pattern and had not applied it**: **C-10's MINT LAW** solves this exact shape for the outbound id by commissioning the **rule** (`max+1` measured at open) rather than the literal, *"so a spec-frozen outbound literal is never trusted over the measured ledger."* The same treatment is now owed and given to C-6 — **the TEMPLATE and the substrate-naming rule are commissioned; the numbers are filled at authoring.** The round-3 sentence is **preserved at C-6 as the dated exemplar** (E-3 — nothing is deleted), and re-labelled there as an exemplar rather than as text to inscribe. **Nothing about the CURE changes**: the same withdrawal, the same honesty, the same four legs — only the gate stops ordering a copy of figures its own lock forbids copying.⟩

**RED remains reachable and the gate remains falsifiable**: it fails if the addendum inscribes **any sha** as *"the tree it matched"*; fails if it names an import total **without naming the state that produced it**; fails if it calls the defect an **off-by-one**; fails if any numeral in the withdrawal sentence is **carried from this spec instead of re-measured** (the executing seat re-runs §5b's three commands and, where a figure has moved, **records the delta as a dated addendum before authoring the sentence** — C-10's MINT-LAW mechanism, applied here); and fails if the frontier and worktree readings are stated **without their two distinct substrates named**.
⟨**Round-2 → round-3 correction, stated not silently applied (E-3).** This GREEN read *"naming the tree it matched (`8281638c`) and the tree it does not (`81a56990`)"* — **STRUCK**. At `8281638c` the census is **81** over an alphabet sharing **zero of six lines** with O-11's certified split, so that GREEN condition commissioned a **NEW false certification into the cure letter**: the SCH-4 class reproduced inside SCH-4's own cure. The gate now orders the honest sentence, and the measurement that makes it true is pasted at §5b (**LAW B corollary**).⟩ Row: C-6.

**G-KF1-5 · VEHICLE QUESTION CLOSED HONESTLY** — *born-RED*
Witness: O-8 **`:101-103`** (re-measured at repair round 2 by read-only `sed` in the sacred checkout; round 1's `:100-102` was off by one — `:100` is blank — **struck**) still reads "**Decision we need from you:** do you want the fix as a deliberate `4.0.1` you take on your own schedule, or folded into the next coherent tuple … We will not cut a version into your dependency graph without your answer." The internal ruling exists at `CARRY-CUT-LEDGER.md:186` (CC-084 — "*no emergency 4.0.1 — ruled*", line re-verified 2026-08-28).
**Third leg, MADE LITERAL at repair round 3** ⟨check **D3-10**: this leg read *"Grep of both coordination trees for any withdrawal → **0**"* — no pattern, no path, no command, in a §6 whose own preamble promises *"literal commands with literal outputs"*. **STRUCK** and replaced by two commands over **all three** coordination trees, run by this seat 2026-08-28⟩:
(a) `grep -rn '4\.0\.1' $PROG/value.js/docs/tranches/V/coordination/ $PROG/keyframes.js/docs/tranches/V/coordination/ $PROG/keyframes-v-exec/docs/tranches/V/coordination/` ⟨***re-written against the root variable at PASS-7, 2026-08-29 — check `P7-8`; `$PROG` is bound once at the OP-3 coordination-listing witness above. This is the command a path could not be removed from — it spans THREE sibling trees, two of them outside this repo, so it is repo-root-relative in the only sense available: relative to a stated root. Re-run at this seat in the `$PROG` spelling: **4 hits, the same four coordinates**.***⟩ → **4 hits**, each read: value `INBOX.md:71` (the **O-7** glass-BJ row cell) · `:72` (the **O-8 row cell itself**) · `:80` (the **O-10a** glass-BJ row cell) · sacred-checkout `VALUEJS-INBOUND-2026-07-24-parser-totality-exposure.md:101` — **the question being asked**. **ZERO hits under `keyframes-v-exec/docs/tranches/V/coordination/`**, which is the exec-visible tree the addendum lands in.
(b) `grep -rniE 'withdraw' <the same three paths>` → **4 hits**, each read: value `INBOX.md:80` (*"withdrawn."* inside the **O-10a glass** row's summary) · `CONSTELLATION.md:9` and `:69` (the V-A136 / V-A168 configurator instructions) · exec `vnext/skeptic-H1-r2.md:65` (*"withdrawn on atlas evidence"*, a vnext skeptic table). **Not one is a withdrawal of O-8's vehicle question.** The question is asked in exactly one place and withdrawn in none — which is the fact this gate is born RED on, now re-executable and falsifiable by any seat.
GREEN: one sentence withdraws the question citing CC-084 at `:186`; the question appears nowhere else in the letter; **no cut date promised**. Row: C-3.

**G-KF1-6 · D-GAP-6 CONDITIONAL CLOSED** — *born-RED*
Witness: `INBOX.md` **row I-10** — at **`:47`**, re-measured at repair round 2 — records the acceptance "they adopt `sampleBezier` only if a future 4.1 ships it"; `registry/adjudicated/library-band.md` **`:205-206`** reads "DECLINE — `sampleBezier`" / "(permanently, measured zero demand)", re-read verbatim this seat by `sed -n '205,206p'`.
⟨**ROUND-4 CORRECTION, stated not silently applied (E-3) · check `D4-8` (MINOR) — a "re-read verbatim" receipt one line short.** The citation read **`library-band.md:205`** for the whole quoted string. Measured: **`:205`** ends *"…`strictTypes: 62` deletion. **DECLINE — `sampleBezier`**"* and the parenthetical *"**(permanently, measured zero demand)**"* opens **`:206`** — **the quoted string as a whole does not exist at the cited coordinate.** The substance is unimpeachable and independently corroborated at **`library-band.md:173`** (*"D-GAP-6 retired permanently on measured zero demand"*, re-read this seat), but *"re-read verbatim this seat"* is a **receipt promise**, and a receipt one line short is the same shape this file convicts elsewhere. **The span `:205-206` is the cure**; the row id and the DECLINE verb remain the stable anchors and the coordinates are re-resolved at open like every other line citation here.⟩ **Round-1 correction, stated not silently applied**: this gate's anchor read *"`INBOX.md:45` (I-10)"* — **struck**; `:45` is row **I-7** (the 07-17 v-execution-open letter). The row id `I-10` is the anchor; the line rides as convenience and is re-resolved at open. **The two statements have never been joined in any letter** — the conditional is live in their ledger and foreclosed in ours.
GREEN: one line resolves it to "not adopted" with the permanence and its ground; no re-litigation. Row: C-4.

**G-KF1-7 · LEDGER VERBS — I-26 CURED + O-21 minted** — *born-RED*
Witness, **re-executed at repair round 2 against the current bytes**: `INBOX.md` **row I-26** — at **`:92`** — reads "**ROWED 2026-08-03**" with the cure described-but-unperformed (text re-read verbatim this seat); and `grep -oE '^\| O-[0-9]+' docs/tranches/V/coordination/INBOX.md | sort -t- -k2 -n | tail -1` → **`O-20`**. No outbound row exists for a letter that does not exist.
**Round-1 corrections, stated not silently applied** (E-3): (i) the anchor read *"`INBOX.md:90`"* — **struck**; `:90` is row **I-21a** (the D-2 card-condense ratification). (ii) the max-grep's stated output read *"**O-19**"* — **struck**; the measured maximum is **O-20**, the SS-6 batched communique to glass BK at `:95`, **SENT 2026-08-28**, minted by commit `48003630` (12:32:16) — nine minutes before round 1's write, which is why round 1 commissioned an id already spent.
GREEN: I-26 reads **CURED** with its receipt (absolute path + date + exec-clone HEAD sha); **O-21** minted naming (a)–(d); O-8/O-11's rows keep their text plus an **amended-by-O-21** pointer. The commissioned id is **`max+1` measured at open**, not this literal — C-10's MINT LAW governs, and a ledger that has moved again yields a different id plus a dated addendum recording the delta. **Ordered strictly after G-KF1-1.** Row: C-10.

**G-KF1-8 · SWEEP-PATH LAW REPAIRED — our own procedure stops pointing at the frozen tree** — *born-RED*
Witness, **narrowed at repair round 2 so the command produces its stated reading**: `grep -n '^- \`\.\./keyframes\.js' docs/tranches/V/coordination/INBOX.md` → **exactly 1 hit**, **`:15`** — "`../keyframes.js/docs/tranches/V/coordination/` — the keyframes exchange (their `<SENDER>-INBOUND-*` grammar; the tranche root is NOT a mail path)", the law spanning **`:15-16`**, re-read verbatim this seat. That tree measured this seat at `8281638c`, **41 behind / 1 ahead** (`git rev-list --left-right --count origin/master...master` → `41  1`), **252** dirty. The law that produced I-26 is unchanged.
**Round-1 corrections, stated not silently applied** (E-3): (i) the anchor read *"`:13-14`"* — **struck**; `:13-14` now carries the **glass** BI→BJ→BK sweep note that commit `7a7dc6ef` added, and the keyframes law moved to `:15-16`. (ii) the broad pattern `grep -n 'keyframes.js/docs/tranches' …` was presented as returning the law alone; **re-executed this seat it returns FIVE lines** — `:15` (the law) plus `:66` · `:68` · `:72` · `:75`, the **O-2 / O-4 / O-8 / O-11** outbound path cells, all in the ledger since 2026-07-17/17/24/27 and therefore already present at round-1 authoring. *(RULINGS-2 R2-1 item 4 enumerates four — `:15`/`:66`/`:72`/`:75` — omitting `:68`, the **O-4** WL-verdicts cell. Under **R2-5** the tree outranks the ruling's prose: the measured count is **five**, recorded here as a dated correction and relayed to the pass-3 seat.)* A seat may use either form: the narrowed pattern above (1 hit), or the broad one **with "5 hits, the law at the first"** stated.
GREEN: the block names the exec-visible tree for delivery, marks the sacred checkout READ-ONLY/NEVER-DELIVER, keeps the grammar note verbatim. Two lines plus one sentence. No tooling. Row: C-9.

**G-KF1-9 · OUTBOUND RETAINED — value holds copies of its own letters** — *born-RED*
Witness: `find . -name '*parser-totality-exposure*' -o -name '*library-band-r1-widened*'`, **run from the value.js repo root** ⟨***made repo-root-relative at PASS-7, 2026-08-29 — check `P7-8`.*** The authored spelling `find /Users/mkbabb/Programming/value.js …` was the one of this file's four machine-path commands that names **no sibling tree**: its whole subject is *this* repo, so it needs no root variable and no absolute path at all. Both spellings re-run at this seat, **0 hits, identical**⟩ → **0 hits** (re-run 2026-08-28; **re-run again 2026-08-29 in the relative spelling — still 0**). The `value-inbox-*` convention is live in `docs/tranches/V/coordination/`. **Dated authoring-time finding on the retained-directory census** (stated, not silently applied — the anti-staleness lock earning its keep): re-measured at repair round 2, the retained set is **7 files — 07-19 ×1 and 07-20 ×6** (`union-apotheosis-relay` · `bbnf-coordination` · `d23-ruled-mirror-primary` · `owner-rulings-relay` · `parser-proof-evidence` · `pi-minitranche-notice` · `residual-repairs`); there is **no `value-inbox-2026-07-21-*`**. The superseded *"07-19, 07-20 ×5, 07-21 present"* reading is recorded **as the reading corrected, never as a source** (header PROVENANCE). Either way the load-bearing fact holds: **07-24 and 07-27 are absent** — the sole copies are the two untracked sacred-checkout files (7,064 B / 15,633 B, mtimes re-verified today).
GREEN: both bodies retained verbatim with provenance headers, and the addendum retained the same way; read-only access to the sacred checkout; originals never corrected in place. Row: C-11.

**G-KF1-10 · MARK REQUESTED (our half) — and MARK RECEIVED declared NON-GATING (their half)** — *born-RED on our half only; the counterparty half is explicitly NOT a gate*
Witness, **in `keyframes-v-exec`, against `origin/master`** (substrate written into the command at repair round 3, same rule as G-KF1-2/G-KF1-3): `git show origin/master:docs/tranches/V/coordination/INBOUND-LEDGER.md | grep -oE 'IN-[A-Z]+-[0-9]+' | sort -u` → **9 rows** (IN-ATLAS-1..5, IN-GLASS-1..2, IN-VALUE-1..2; re-run 2026-08-28); no IN-VALUE-3/4, nothing dated after 2026-07-17. Value-side: the newest keyframes-authored letter in our tree is `keyframes-inbox-2026-07-18-*` → silence **41 days** at 2026-08-28 (the census's "16 days" and the CARRY's "38 days" are dated, **non-inheritable** figures). O-8 unanswered **35 days**; O-11 **32 days**.
GREEN: the addendum contains the explicit minimal ask for `IN-VALUE-3`/`IN-VALUE-4` in their grammar. **The actual marking is NOT a close condition** — it is theirs, may never come, and gating on it makes the wave un-completable ⟨census (c)5⟩. Its absence is re-measured at close and recorded under I-26. Row: C-12.

**G-KF1-11 · SPEC HOME POPULATED** — ***DECLARED-SATISFIED · NOT born-RED, NOT a close gate this wave passes*** (L-19; the same relabelling G-KF1-12 receives, owed here and now applied)
The condition was RED at the instant of authoring and is **discharged by the existence of this file**. At wave-open it is already GREEN, and **no act inside `.a`, `.b` or `.c` can turn it** — a gate that cannot fail for its intended reason is not a gate, and dressing one as born-RED inflates the tally L-19 exists to keep honest. It is retained as a **declared condition with a dated record**, not deleted, because the filename reconciliation it carries is load-bearing for every sibling's tables.
Dated authoring record, **re-measured at repair round 2**: `ls docs/tranches/X/keyframes/waves/` → **eleven specs — this one and ten siblings** (`KF-W0` · `KF-W2` · `KF-W3` · `KF-W4` · `KF-W5` · `KF-W6` · `KF-W7` · `KF-W8` · `KF-W9` · `KF-W10`), every one self-naming `KF-W<N>.md`. Round 1 measured **seven** and named `KF-W5`/`KF-W6`/`KF-W9` as absent — **struck**: the set is now the whole eleven, and the figure is re-measured again at open rather than inherited (this is the third consecutive round in which a frozen sibling count went stale, which is the argument for measuring it and not carrying it). Immediately before the first write: `test -e …/KF-W1.md` → **ABSENT**; `test -e …/W1.md` → **ABSENT**.
State now (re-measured 2026-08-28, this repair seat): `test -e …/KF-W1.md` → **PRESENT** (`acff6265`); `test -e …/W1.md` → **ABSENT**. **SATISFIED** — `KF-W1.md` exists under the sibling convention, `Status: planned`, the four-verb block reading SPECIFIED=YES / IMPLEMENTED=NO / VERIFIED=NO, validated against `docs/precepts/instructions/tranche/WAVE_SPEC.md` (L-20). Re-measured at open and **recorded, never gated**. Row: C-14.

**G-KF1-12 · SACRED CHECKOUT UNTOUCHED** — *INVARIANT, declared, **not** born-RED* (it is GREEN at open and must stay green; labelling a stay-green guard as born-RED is the contrivance L-19 forbids)
Witness: keyframes' `docs/tranches/V/EXECUTION-HANDOFF.md:13-18` — *"no mutating git, no npm operations there, ever — W2 reads its FILES (the 65-path slice export) and nothing else. **This invariant survives until W2 completes**"* ⟨**the expiry clause carried at repair round 4, check `D4-7`** — the quotation stands whole at the SACRED-CHECKOUT LAW in the header, with the handoff's own `a59d3a22` / ~254 tree record beside it. **This gate does NOT depend on the condition**: the invariant it measures is **value's own** — §4's Do-NOT-touch row and the execution gate — and stays GREEN whether or not the keyframes-side W2 completes. The external quotation is corroboration, not the gate's source; it is cited **with its qualifier** because a spec that convicts siblings for dropping qualifiers owes the same to itself⟩. Checkable quantity, **MEASURE-AT-OPEN**: `git status --porcelain | wc -l` in `/Users/mkbabb/Programming/keyframes.js` → **252** (2026-08-28; the docs lane's 2026-08-03 figure was also 252 — the agreement is coincidence and is not to be inherited as an expected value), plus the two packet mtimes (Jul 24 16:41 / Jul 27 12:27).
GREEN: unchanged across the wave; every access is a read (`ls`, `git show`, copy **out**). Zero writes, zero git mutations, zero npm. Rows: C-1, C-11.

---

## 7. Sequencing

**Internal order (hard).** `.a` before `.b` (the addendum is derived from bodies whose liveness is a wave-open precondition — OP-1; C-11's SEQ-LOCK). `.b` before `.c` (G-KF1-7 is void ahead of G-KF1-1: a ledger that marks CURED before delivery reproduces I-26 one layer up). C-9 and C-10 land in the **same motion** — repairing the ledger without repairing the law that broke it ships the cure and keeps the cause, the class the registry books elsewhere as "two writers for one persisted flag". Standing E13 law: the wave opens with the four-path mail sweep; the amended path takes effect at the very next wave-open program-wide.

**Substrate law (carried from KF.W0 §B-12, binding here as everywhere)**: origin/master pins glass-ui `7.0.0` exact devDep = installed; local `8281638c` is stale — **every measurement in this wave re-baselines against `origin/master`**, never the sacred checkout.

**Cross-edges, declared from this end.**

1. **→ KF.W2 (Parse Façade) · KF.W3 (Parser Consumption, GATED) · KF.W4 (K-Quartet Hygiene) · KF.W10 (Fold Discharge — see cross-edge 11) — and every future kf wave consuming O-8/O-11.** Type: **PRECEDES (hard)**. This wave must close before any of them may cite an O-8/O-11 obligation ⟨COHESION §2 · census (c)6⟩. **Reciprocity, measured 2026-08-28 at repair round 4 over all TEN authored siblings** (§1, per RULINGS R-19d — ⟨**round-4 correction, check `D4-1`, stated not silently applied (E-3)**: this clause read *"over all **seven** authored siblings"*, **STRUCK**; the enumeration and its counting rule live at §1's head paragraph, and this cell **points at that enumeration rather than re-issuing the numeral** (LAW D(3))⟩): **KF-W4** declares it (**§Preconditions OP-5** "OPEN — KF.W1's"; **§Sequencing, cross-edge row `KF.W1 · Mail Cure`** → PRECONDITION on anchor citability, cited by §-heading + row label alone, **numeric convenience STRUCK at round-2 close (U-2/R2-7)**); **KF-W3** declares it (**§Preconditions OP-5** UNLANDED · **L-4** · **§Cross-edges, row `KF.W1 · Mail Cure (O-8/O-11) + E13`** → DEPENDS ON, cited by §-heading + row label alone, **numeric convenience STRUCK at round-2 close (U-2/R2-7)**); **KF-W10** declares it three ways and hard-blocks (cross-edge 11). *(All four receipts re-resolved at repair round 2 under R2-7's stable-anchor idiom; the round-1 line coordinates `:206`/`:209` were dead — the edges themselves were live at every one, so R-19d is cured in kind and only its receipts drifted.)* **KF-W2 · KF-W5 · KF-W6 · KF-W7 · KF-W8 · KF-W9 — the SIX — carry no KF.W1 edge** (no KF.W1 edge in any of the six — the property; the per-sibling token counts are struck at repair round 6 per PASS-6 D6-2, the strike stated whole at `:60`; **KF-W7 and KF-W9 each name KF.W1 exactly to DECLARE it a non-edge**, both of which name KF.W1 **to declare it, or to list it as, a NON-EDGE** ⟨**RECONCILE SEAT, 2026-08-28 — token figure SUPERSEDED at final bytes, edge claim STANDS (`PASS-5/CLOSE-CERT-2.md` §4 · R5-R1)**: re-run after the last per-wave write the token is **KF-W2 7** (minted by W2's own round-5 `D-13` cure, a relay-vehicle *attribution*, not an edge) · 0 at KF-W5/W6/W8 · 1 each at KF-W7/W9. **The SIX-carry-no-edge claim is unaffected and was re-verified at the cert on its two load-bearing properties** (`O-11` → 0 in all six; every `O-8` a `KF-CO-8`/`KF-CO-1` substring). The numeral is a dated observation and is not re-issued here — §1's bullet carries the full correction.⟩ ⟨**round-5 correction, check `D5-1` (MAJOR), stated not silently applied (E-3)**: this read *"token **0** in five; **1 in KF-W9**"* — **STRUCK**, KF-W7 acquired its hit at its stage-2 write⟩ — see **§1's SIX-non-consumers bullet**, where the correction, the retirement of the per-sibling zeros and **the property that survives sibling writes** (`O-11` = 0 across the six; every `O-8` a `KF-CO-8`/`KF-CO-1` substring) are stated **once**, with their substrate, and are not re-issued here). The COHESION §3.1 **cohesion mark** raised against KF-W2 was *conditional* — owed **if** it cites any O-8/O-11 obligation — and the condition is **measured 2026-08-28: FALSE** (`grep -c 'O-11' KF-W2.md` → **0**; its **two** `O-8` hits are `KF-CO-8` substrings, one **in its §Excluded row `KF-CO-1 / KF-CO-8`** (**numeric convenience STRUCK at round-2 close (U-2/R2-7)** — `:267`/`:103`/`:338` all drifted; the §-heading + row label are the anchor); **the other five measure `O-11` = 0 identically**, every `O-8` hit a `KF-CO-8`/`KF-CO-1` substring). **The mark DISCHARGES on the grep across all SIX** — nothing is owed from any of those ends, and W2 is no longer singled out for a property its five siblings share. This wave hands anchors, never a façade design (LIB §4.1's 13-site inventory is KF.W2's).
2. **→ KF.W0 (Substrate Settle · §B-12; + intake CARRY X-1, the Codex v8 contract pinned to `8281638c`).** Type: **ASKS, does NOT depend on — RECIPROCITY CONFLICT recorded.** The addendum asks the **owner** to perform §B-12 ⟨lane-docs §7.2⟩; **HARD NEGATIVE: value.js must NOT perform §B-12** ⟨DOCS §6 row 16⟩; this wave is deliberately completable without it — it anchors on `origin/master` and delivers to the exec clone. **Re-measured at repair round 2: KF-W0 declares the REVERSE edge at two stable anchors** — **§Sequencing, the OUTBOUND table row `KF.W1 · Mail Cure`** (*"W1's re-delivery depends on this wave making them visible and on the re-anchored counts replacing the stale ones."*) and **§Excluded, the row `KF.W1's mail re-delivery`** (*"Depends on this wave, is not performed by it."*). Both quotations verbatim at their source; **the edges ride the two §-headings + row labels alone**. ⟨**Round-3 correction (RULINGS-3 `(c)(1)`; checks **D3-3**/**D3-4**), stated not silently applied (E-3).** This cross-edge carried the coordinates *"at `:393`"* and *"at `:431`"* — **both dead and both STRUCK**: per `PASS-3/KF-W1-CHECK.md` §6 D3-3 the OUTBOUND row is at `:439` (`:393` is a §Gates falsifier) and the §Excluded row is at `:477` (`:431` is a §Sequencing lock). Worse than dead, they **contradicted §1's own KF-W0 bullet**, which had already struck the same two coordinates by name — one file, two verdicts on the same anchors (D3-4). The direction and force of the edge are unchanged at every reading; only the receipts drifted.⟩ That claim contradicts this CARRY's design lock (the exec-clone path exists precisely so the mail cure never queues behind the owner's hand). **The conflict is recorded for the COHESION §3 check and the owner's ruling — neither spec silently rewrites the other.** This spec stands on its CARRY: KF.W1 opens on the begin-word, not on KF.W0.
3. **→ KF.W0 (census F-1 / SCH-1 identity).** Type: **ROUTES-AWAY (namespace disambiguation).** See the alias block and C-13. Zero `KF.W1-DEP` rows land here. **KF.W0 carries the IDENTITY, and the five component riders were OWED there — commissioned this round and MEASURED PRESENT at W0's post-landing bytes** ⟨**re-cut at repair round 4, check `D4-3`. Both dated readings — the pre-landing zeros at `KF-W0.md` mtime **15:43:24** and the five-of-five verification at mtime **16:57:50** — live once at **C-13**, each with its pasted command and its named substrate, and are **not re-issued here** (LAW D(3): dependent cells point at the enumeration). Landing surface: `KF-W0 §Carry`, the `C-1.F` fold table, row `F-1` — **§-heading + row id, no line number.** Final-bytes verification is the **RECONCILE seat's**, not this spec's (LAW B)⟩. The `a59d3a22` dissent rider remains **KF.W0's re-measurement**, carried not resolved.
4. **→ KF.W4 (born-RED gates G-L7a/b/c/d).** Type: **SUPPLIES ANCHORS.** KF.W4's gates consume the addendum's HEAD-resolved table, never the packets' stale anchors ⟨census §0 formation law: "no wave spec may cite a `file:line` until re-anchored at `origin/master`"⟩. `G-L7a–d` and `K1–K4` are anti-rename. Reciprocated (`KF-W4.md` OP-5).
5. **→ X-W9 (value's dated 4.1 cut wave, `CARRY-CUT-LEDGER.md:255` — line re-verified 2026-08-28).** Type: **SHARED CLOSURE LINES.** The CC-084 vehicle-withdrawal and the D-GAP-6 "not adopted" line ride **both** this addendum and X-W9's cut packet, declared from both ends. The addendum does not replace the cut packet and promises no cut date.
6. **→ X·P (parse-that) release condition.** Type: **NON-EDGE, declared.** This wave carries no parser schedule. PLAW-BIND routing (parser → value → packed release → consumers) and the OWNER-GATED bench bar mean the letter states the ruling (CC-084) and nothing about timing. Direct parse-that→consumer promises are forbidden.
7. **→ SS-6 (glass-ui BH communique).** Type: **NON-EDGE, declared.** Zero glass-producer rows here; intake §3 NEXT-COMMUNIQUE is **EMPTY**. The standing BH-relay edict is satisfied by declaration; **no rider is minted.** The glass-producer rows found elsewhere in X·KF (LabeledSelect `open` / LabeledSwitch `modelValue` `default: undefined`; the `useTouchGate` styling contract; the standard-variant Slider thumb; the D-18 collapsed-summary a11y half) ride SS-6 **from their own waves**, never from here and **never as demo-side hacks**.
8. **→ keyframes.js (external; no owning formation on their side — census (c)5).** Type: **OPEN OBLIGATION, non-gating.** The `IN-VALUE-3`/`IN-VALUE-4` marks and any reply are theirs. We deliver and ask; we do not close on their answer. Tracked as an OPEN sub-row under I-26; silence re-measured at close (41 days at 2026-08-28), never inherited. **The addendum says plainly that the addressee repo's successor formation is this megatranche — its executor is us-adjacent** — rather than assuming a keyframes agent will act.
9. **→ KF.W11 (Demo Scene Repair) · KF.W12 (Authoring-Surface Repair) · KF.W13 (Chrome, Dock & Transport Repair) · KF.W6 · KF.W7.** Type: **HOMES NONE — routing declaration.** This wave homes **zero** of the **17** NO-WAVE-OWNER packets — the **canonical roster per RULINGS R-15**, receipted in full at C-14 leg (iii) and partitioned by the homing authority **KF.W4**:
   - **KF.W11 · Demo Scene Repair (9)** — cube · sequence · spring · spring-plot · spring-physics-facet · spring-artifact-truth · square · **amiga** · **drag-seam**.
   - **KF.W12 · Authoring-Surface Repair (6)** — CARD-UNIT · OPTIONS-UNIT · KFED-UNIT · **EDITOR-UNIT** · APPLY-UNIT · AXISLINE-UNIT.
   - **KF.W13 · Chrome, Dock & Transport Repair (2, MINTED)** — **dock-menu** · **transport/ribbon**.

   **dock-menu is KF.W13's, not KF.W12's** — R-15 reconciles **KF-W2's §Excluded sibling-packet row** (*"The cube · sequence · spring · **dock-menu** · CARD-UNIT packets — routing restated on the canonical 17-packet roster (RULINGS R-15)…"* — **numeric convenience STRUCK at round-2 close (U-2/R2-7)**: `:375` had drifted onto a §Carry census-corrections note and round-1's `:270` was already dead; **the §-heading + the row's opening label are the sole anchor**) and this cross-edge, which both route it there. `transport/ribbon` is homed for the first time by that ruling. Each routes to the waves above ⟨COHESION §3.3 — no silent drops⟩; the cardinality **16** an earlier draft carried was never true in any document and is struck (C-14 leg (iii)). **COHESION FLAG, raised because the sequencing locks arrive with this wave**: KF.W11/W12/W13 **do not exist in the census sketch, which stops at KF.W10** — the X·KF spec set must **MINT them explicitly**, and §3.2's "no silent drops, no re-booking" is symmetric: a wave minted beyond the sketch needs the same declaration a killed sketched wave needs. **FLAG STATUS, 2026-08-28: DISCHARGED at the homing authority** — R-15 has KF.W4 mint **KF.W13 explicitly** and re-cut its homing lists over the canonical 17, which is the declaration this flag asked for. The flag is recorded as answered, not deleted. ⟨**ROUND-4 ADDITION · RULINGS-4 `R4-8`, which RULED the disposition of KF.W11/W12/W13 as arm (a) — REGISTERED SUCCESSOR FORMATIONS; the mints STAND.** That ruling names **this cross-edge and its DISCHARGED flag** as one of the two **minting citations of record** (the other is `KF-W4 §Sequencing`, the R-15 homing block), and rules the three **`MINTED-UNAUTHORED`**, their authoring seat the **SS-1/SS-2 authoring block** — ownership assignment stays the root session's act (the R-18 pattern). **This flag is therefore not merely answered; it is REGISTERED.** Second end, **ANCHOR-ONLY per LAW C(3)** — KF-W10 writes **LAST** of the per-wave seats this round: **`KF-W10 §6.D · SUCCESSOR-FORMATION REGISTER`**, one row per wave carrying the cargo partition, the travelling locks, the minting citations and the terminal verb. **No prose is quoted from it and no line number rides this receipt**; the **RECONCILE seat** verifies the edge at W10's final bytes. **Nothing about this wave changes**: it still homes **zero** of the seventeen, and its §Excluded row says so.⟩ The locks that must travel with those homings, restated so they are not lost in transit: **comment-stated invariants are test obligations** (kf-SquareScene law; KF.W4's MISS-6 keeps only its non-waiting green) · **KF-CO-1/KF-CO-8's cures are SEQUENCED by LP-1** (no write→render edge) in their owning wave · **kf-ChromeDock M-4's worded cure ships an unopenable menu without the MbabbMenu MUST-CARRY rider** (KF.W13) · **TD-1/TD-2's cures bundle, unsplit**, in their owning wave · **glass-producer rows go to the BH relay (SS-6), never demo-side hacks** · **the KF-AV-28 supersession rider travels on the W7-evaluated surfaces** ⟨**added at repair round 3 — check D3-8**, stated not silently applied (E-3): §8's 17-packet row already listed this rider among the locks that travel with the homings, and this list — the one introduced as *"the locks that must travel WITH those homings, restated so they are not lost in transit"* — **enumerated five and dropped it**. A rider omitted from the **operative transit declaration** is a rider lost in transit, which is the one thing this list exists to prevent. **KF-AV-28 is live across the sibling set** — per **`PASS-5/KF-W1-CHECK.md` §6 `D5-7`** (2026-08-28), which re-measured it **live in NINE of the ten siblings, absent only from KF-W5** ⟨**re-pointed at repair round 5 with the carriage class, check `D5-2`**: `PASS-4/KF-W1-CHECK.md` §6 `D4-6` is the dated predecessor and found the identical nine-of-ten; the property is cited **by id**, and no per-sibling numeral rides this cell — §13's dated reading lost its numerals at `D5-7`⟩ — so the omission was local to this list, not to the program. The two enumerations now agree at **six** members⟩ ⟨**ROUND-4 CORRECTION, stated not silently applied (E-3) · check `D4-6` (MINOR) — the per-sibling census was stale on nine of nine.** This parenthetical carried *"(W9 ×11 · W6 ×10 · W7 ×7 · W2/W10 ×5 · W4/W3 ×4 · W8 ×2 · W0 ×1, per `PASS-3/KF-W1-CHECK.md` §6 D3-8)"* — **STRUCK, numerals and all.** Its **provenance was lawful** (the figures were attributed to the PASS-3 artifact by path and date, which is what LAW B asks), but **every sibling was rewritten in the same repair round after that artifact was cut**, and **not one of the nine reproduced under either metric** at pass 4 — so **C-1's ANTI-STALENESS** lock (*"no measurement is inherited"*) and **R2-5** (*"a witness a seat leaves standing without re-running is that seat's defect"*) both bite. **The ruling's chosen arm is taken: the parenthetical numerals are DROPPED and the citation is KEPT**, re-pointed at the freshest artifact. **The per-sibling counts are NOT re-inscribed here, deliberately** — under **LAW C** every sibling moves inside a repair round, and four of the ten (KF-W7 · KF-W8 · KF-W9 · KF-W10) write in **stage 2**, later than this seat, so a per-sibling numeral written here is a figure that is false before the round closes. **This seat's own dated re-measurement, over the named bytes in §13's mtime table, is recorded at §13 as a dated reading — never carried into this operative sentence.** **The load-bearing claim STRENGTHENS on re-measurement**: KF-AV-28 is live in **nine of ten**, absent only from KF-W5, so the rider's transit obligation is wider than the struck figures implied, not narrower.⟩
10. **→ COHESION §3.1 / §3.3 cohesion checks.** Type: **SET-DIFFERENCE ∅ (affirmative).** Re-verifiable from C-14's three enumerations without re-deriving the greps. **§3.1's mark against KF-W2 is discharged by measurement** (cross-edge 1); **§3.3's no-silent-drops obligation is met against the canonical 17** (cross-edge 9), not against an inherited 16.
11. **→ KF.W10 (Fold Discharge & Coordination Terminal, SS-2 per RULINGS R-18).** Type: **PRECEDES (hard) — the hardest downstream dependency this wave has.** Declared here because it was **missing** from the earlier reciprocity measurement and from this cross-edge list; cured per **RULINGS R-19d**. KF-W10 was on disk at this spec's authoring (born 11:43:22, 44 s before this file; committed `b6e09ed4`) and declares the edge **three times**, at three stable anchors re-resolved at repair round 2 (the round-1 coordinates `:16`/`:50`/`:296` are all dead — **struck**; every edge survives at its new home, same direction, same force): ⟨**§1 State, the `Opens after` line** — **ANCHOR ALONE**; this wave is named there as an opens-after dependency ⟨**ROUND-5 STRIKE, checks `D5-5` (the quotation) and LAW E(4) (the coordinate), stated not silently applied (E-3)**: the quoted parenthesis was verbatim at W10's round-3 bytes and is a **TRUNCATION** at its later bytes, and **`:20` — the last surviving line coordinate on this file's cross-spec receipts — is RETIRED with it**, since under **RULINGS-5 LAW E(4)** a per-wave seat's cross-wave receipts carry no line numbers at all. The anchor is unchanged and resolves; `PASS-5/CLOSE-CERT-2.md` verifies it at final bytes⟩⟩ ⟨**§Preconditions, the row `OP-3`** — **§-heading + row id `OP-3` are the sole anchor**; the addendum's exec-visibility is a named open precondition, still **UNLANDED**, and the row reasons that terminalizing a ledger whose packets were never delivered would record a false close ⟨**PARAPHRASE, not a quotation (LAW D(1))** — the embedded verbatim fragment carried here is **STRUCK at round 5** per check `D5-5`; the row's force is retained, its words are not borrowed⟩⟩ ⟨**§Sequencing, the cross-edge `B. → KF.W1 (Mail Cure, SS-2)`** — **§-heading + cross-edge label are the sole anchor**; the row declares W10 the dependent party and gates coordination terminalization on this wave's landing ⟨**PARAPHRASE, not a quotation (LAW D(1))**; the sentence quoted here as verbatim carried **two unmarked elisions** at W10's later bytes and is **STRUCK at round 5**, check `D5-5`⟩⟩. ⟨**Round-3 correction (RULINGS-3 `(c)(1)`; checks **D3-3**/**D3-4**), stated not silently applied (E-3).** This cross-edge carried *"at `:54`"* and *"at `:332`"* — **both STRUCK as dead**: per the PASS-3 check, `OP-3` is at `:59` (`:54` is **blank**) and the `B. → KF.W1` cross-edge is at `:382` (`:332` is **blank**). Both had already been struck **by name** in §1's own KF-W10 bullet as *"blank at the current bytes"*, so this file asserted as live what it had elsewhere declared dead — the D3-4 contradiction, cured here. Each edge survives at its stable anchor, same direction, same force (§2.1 of the check).⟩ **KF-W10 cites both letters by id, repeatedly, at declared anchors — the property, not a numeral** ⟨**ROUND-5 STRIKE, check `D5-4` (MAJOR)**: *"Re-measured this seat: 8 × `O-8`, 3 × `O-11`, all real letter citations"* is **STRUCK** — true at the substrate it was taken from, false at the round's end, and stamped nowhere. Re-measurement moves both figures **UP**, so *"the hardest downstream dependency this wave has"* is **more** true, not less; the numerals are retired to the property under **LAW E(4)** and any that must ride ride `PASS-5/CLOSE-CERT-2.md`. **SHADOW**: this is the same struck set booked at **§1's KF-W10 bullet** — three quotations (anchors kept) and two numerals (retired) — enumerated once at **§14**; termini notified = {KF.W10, RECONCILE}⟩. **Direction is unambiguous and uncontested — unlike cross-edge 2, there is no conflict to record**: W10 depends on this wave, this wave depends on nothing of W10's. What this wave owes W10 is exactly what it owes everyone — the addendum at an exec-visible path plus the **I-26/O-21** ledger receipt (G-KF1-1, G-KF1-7 — the outbound id is `max+1` measured at open per C-10's MINT LAW, so W10 consumes the **row**, never a frozen literal), which is W10's OP-3 artefact check verbatim. **No new obligation, no re-scope, no new row** — the edge was always true; only its measurement was missing. W10's own ownership assignment (SS-2) is **R-18's ruling and the root session's act**, not this wave's to make or to consume.

---

## 8. Excluded

| excluded | reason + route |
|---|---|
| **The 13-site Tier-A inventory** (LIB §4.1) and any façade design | **KF.W2's object.** This wave hands over anchors, never a façade design ⟨C-2 lock⟩ |
| **§E2's `lerpArray` length assertion — scheduling it** | The addendum re-delivers the ask; it must not schedule it ⟨DOCS §6 row 2: "the value-side cure (W.L3 `./math` failure protocol) cannot reach them until the pin moves"⟩ |
| **Any re-ask, re-scope, or severity change on DOCS §6 rows 2–10 + 14** | The addendum amends two letters; it is not a third ⟨C-7 lock⟩ |
| **§B-12 (the `git fetch && git reset --hard origin/master` on the sacred checkout)** | **HARD NEGATIVE — value.js must NOT perform it.** Asked, not done ⟨C-8⟩; owned by KF.W0 |
| **All SIX `KF.W1-DEP` / census F-1 rows — across FIVE records — and their component riders**: `kf-AnimationControlsGroup.md:28` **and `:47`** · `kf-ChannelControls.md:56` · `kf-ControlsPaneWrapper.md:60` · `kf-DemoGlobalChrome.md:44` · `kf-KfPillTabs.md:46` | Different namespace, different wave (KF.W0 §B-12), **and falsified at HEAD** by SCH-1 ⟨C-13⟩. ⟨**Round-3 correction (check `D3-5`), stated not silently applied (E-3)**: this cell read *"All **five** `KF.W1-DEP` / census F-1 **rows**"* — **STRUCK**. It counted **records as rows** and **under-booked by one**: ACG carries **two** routing rows, and the dropped `:47` is the row that owns the `useControlsKeyboardShortcuts.ts:1` sole-binding-site rider. The header PROVENANCE cites **this cell** as its booking receipt, so a seat booking from this cardinality would have dropped **R-2 and its rider** while the header claimed six booked. C-13's *"five"* is correct — **it counts RECORDS** — and the two now agree instead of colliding. This is exactly the unreceipted-cardinality class this spec struck at C-14 leg (iii) (*"**16** was stated by no document in the tree"*), applied to fourteen figures and skipped for this one.⟩ |
| **Correcting the retained O-8/O-11 originals in place** | Verbatim retention is the point: "amend, do not re-send" ⟨C-11⟩ |
| **The kf-side `INBOUND-LEDGER` mark as a close condition** | Theirs to perform; gating on it makes the wave un-completable by construction ⟨C-12⟩ |
| **A cut date for 4.1** | X·P's release condition is gate-keyed and OWNER-GATED (COHESION §2 PLAW-BIND) ⟨C-3⟩ |
| **The 17 NO-WAVE-OWNER scene/authoring/chrome packets** — the canonical roster **per RULINGS R-15**, whole: cube · sequence · spring · spring-plot · spring-physics-facet · spring-artifact-truth · square · amiga · drag-seam · CARD-UNIT · OPTIONS-UNIT · KFED-UNIT · EDITOR-UNIT · APPLY-UNIT · AXISLINE-UNIT · dock-menu · transport/ribbon | **Homing authority is KF.W4** (R-15), which partitions them **KF.W11 ×9 / KF.W12 ×6 / KF.W13 ×2 (minted)** per cross-edge 9; **dock-menu and transport/ribbon ride KF.W13**. Their riders travel with them (MbabbMenu MUST-CARRY at KF.W13 · LP-1 sequencing · TD-1/TD-2 bundle · kf-SquareScene test-obligation law · the KF-AV-28 supersession rider on the W7-evaluated surfaces). The earlier cardinality **16** is struck as unreceipted ⟨C-14 leg (iii)⟩ |
| **Any SS-6 rider, and any glass-ui touch** | Zero producer rows survived adjudication in any lane; the edict is discharged by declaration ⟨C-14, cross-edge 7⟩ |
| **Mail tooling — a routing script, a path linter, a delivery cron** | KISS / L-19. The cure is a two-line law edit ⟨C-9 sizing lock⟩. No cron: standing owner order |
| **Any registry row, gate, or bounds item manufactured to fill the empty column** | A wave with no adjudicated rows gets no invented ones. The ∅ is a finding ⟨C-14⟩ |
| **Any product source in either repo; the census bodies; `scripts/dev/dev.sh`** | Out of bounds by §4 and by the execution gate |

---

## 9. Dated authoring-time findings

**Framing restated at repair round 2 (RULINGS-2 R2-1 item 6, running RULINGS R-12's general rule here).** These six are **dated findings of this seat's own measurement**, not "corrections to a CARRY": the SS-2 carry ledger they were framed against **is not in the tree** (`find docs -iname '*CARRY*'` under `X/keyframes/` → one file, `carry/KF-W6-CARRY.md`, which is W6's), and R-12 forbids citing a ledger that does not exist as a source. **Content is preserved whole (E-3)** — only the provenance framing changes: each item now states what this seat **measured**, and the superseded reading is quoted **as the reading corrected, never as an authority**. Recorded rather than silently applied, because silent drops and silent re-counts are the defect class this program exists to kill. **All figures re-measured at repair round 2 (2026-08-28)** — keyframes-side at `origin/master` `81a56990`, value-side at the current tree bytes.

1. **Spec home** — measured: `docs/tranches/X/keyframes/waves/` holds **eleven** specs (this one + `KF-W0` · `KF-W2` · `KF-W3` · `KF-W4` · `KF-W5` · `KF-W6` · `KF-W7` · `KF-W8` · `KF-W9` · `KF-W10`), all self-naming `KF-W<N>.md`; `W1.md` **ABSENT**. Bounds reconciled to **`KF-W1.md`**; `W1.md` never created. Superseded readings, recorded as corrected: *"`waves/W1.md`, parent dir ABSENT (2026-08-25)"* · the two-sibling draft reading · round 1's **seven**. (G-KF1-11's record restated accordingly — and, at repair round 1, the gate **relabelled DECLARED-SATISFIED** and the Bounds grant moved **create → modify**, since the file now exists.) **Three rounds, three stale counts: the figure is measured at open, never carried.**
2. **Retention census** — measured: **7 files, 07-19 ×1 and 07-20 ×6; no 07-21.** Superseded reading, recorded as corrected: *"07-19, 07-20 ×5, 07-21 present"*. The load-bearing fact is unchanged: 07-24 and 07-27 absent.
3. **Drift-table row count** — `lane-docs §6` (in tree) banks **12 rows**. Re-measured: **14 anchor rows** (9 drifted + 5 exact); `emit/backward-color.ts`'s three line anchors are three rows against one path rename.
4. **Dated intervals re-measured** — silence since the newest keyframes-authored letter (`keyframes-inbox-2026-07-18-*`): **41 days** (`CENSUS-2026-08-03.md`, in tree, banks "16 days" — a dated figure). O-8 unanswered **35 days**; O-11 **32 days**. ⟨***RE-MEASURE AT OPEN, not at read (PASS-6 D6-9, 2026-08-29): at the pass-6 seat's clock these read **42 · 36 · 33**. They are LAWFUL IN FORM and are not booked as drift — each is explicitly declared dated and non-inheritable, and C-1's ANTI-STALENESS lock is why they are written that way. They are the one figure class in this file that goes stale WITHOUT ANY SEAT WRITING ANYTHING, which is the pure case the lock exists for: the executing seat re-runs them at open and never reads them as current.***⟩ Superseded reading, recorded as corrected: *"38 days at 08-25"*. All figures are dated and non-inheritable; the executing seat re-measures again.
5. **Reciprocity measured over the WHOLE authored sibling set — TEN, not seven, not four** ⟨RULINGS **R-19d**; the heading re-cut at repair round 4 per check **D4-1**, which convicted *"seven"* at this file's head while this item's own body already read *"ten siblings on disk"* — one file, two verdicts on the sibling cardinality⟩. The superseded cross-edge sketch named KF.W2/W3/W4 and KF.W0 as owing reciprocation once authored; all four are authored — **and so are KF-W7, KF-W8 and KF-W10** **and, at repair round 2, KF-W5 · KF-W6 · KF-W9 as well — ten siblings on disk.** Re-measured **at repair round 4** over all ten, command pasted (LAW D(3), re-run at write time; substrate = the sibling bytes in §13's mtime table, per LAW C(2)): bare-`KF.W1` token counts `grep -oE 'KF\.W1([^0-9A-Za-z-]|$)' <sibling>.md | wc -l` → **0 for KF-W2 · KF-W5 · KF-W6 · KF-W8** · **1 for KF-W7 and 1 for KF-W9**, each a **declared NON-EDGE or a stage-roster listing, not an edge** (§1's SIX-non-consumers bullet). ⟨**ROUND-5 CORRECTION, check `D5-1` (MAJOR), stated not silently applied (E-3)**: *"**0** for … **KF-W7** …"* is **STRUCK** — W7 acquired its hit at its stage-2 write, in its **§LAW C SUBSTRATE DISCLOSURE, the `BACKWARD (stage-1 siblings …)` bullet**. And the four declared consumers' numerals — *"KF-W0 4 · KF-W3 5 · KF-W4 5 · KF-W10 8"* — are **RETIRED**, not re-issued: under **RULINGS-5 LAW E(4)** this seat keeps no clock over siblings that write after it, and **what this item asserts is the EDGES, which are declared at their siblings' own §-anchors and cannot be moved by a count**. Any surviving numeral rides `PASS-5/CLOSE-CERT-2.md`. **SHADOW (LAW F(1))**: struck set = {the KF-W7 zero · the four declared-consumer token counts · the KF-W10 `O-8`/`O-11` pair at this cell}; **disposition**: the zero corrected and then retired with its siblings, the counts retired to the edges and the property; **minted set = ∅** — **no edge, consumer, non-consumer or reciprocation changes**; **termini notified** = {KF-W0 · KF-W3 · KF-W4 · KF-W10 ⟨all four edges stand, unchanged, at their own §-anchors⟩ · KF-W7 · KF-W9 ⟨non-edges, unchanged⟩ · RECONCILE}; enumerated at **§14**.⟩ ⟨**Round-4 corrections, stated not silently applied (E-3), check `D4-5`**: *"KF-W4 **2**"* → **5** and *"**0** for … KF-W9"* → **1**; both drifted when those siblings were rewritten later in round 3 than this file (W4 at 15:49, W9 at 15:39, this file closed 15:42 — see **D4-9**).⟩ **The three new siblings add no KF.W1 edge and consume no O-8/O-11 obligation**: `grep -c 'O-11'` → **0** in all three, and every `O-8` hit is the `KF-CO-8` substring — each inspected line-by-line, all LayerConfigPanel/ChannelOptions switch-clobber sequencing rows. **Both metrics are now stated separately, because no single command yields a mixed triple** ⟨**round-3 correction, check `D3-9`, stated not silently applied (E-3)**: this parenthetical read *"(KF-W5 ×1, KF-W6 ×2, KF-W9 ×5)"* — **STRUCK as a MIXED enumeration**. W5's 1 and W9's 5 are `grep -o 'O-8' | wc -l` **occurrence** counts; W6's 2 is a `grep -c 'O-8'` **line** count; the triple reproduces under **no** command. The load-bearing claim was, and remains, TRUE.⟩ Re-measured **at repair round 4** over all **six** non-consumers, both metrics, every numeral re-run at write time: **`O-8` occurrences** — KF-W2 **2** · KF-W5 **1** · KF-W6 **4** · KF-W7 **2** · KF-W8 **0** · KF-W9 **5**; **`O-8` lines** — KF-W2 **2** · KF-W5 **1** · KF-W6 **2** · KF-W7 **2** · KF-W8 **0** · KF-W9 **3**. `grep -c 'O-11'` → **0** in all six. ⟨**Round-5 note (LAW E(4))**: the twelve per-sibling `O-8` numerals above are **dated round-4 readings, retired as receipts** — they are not re-issued and nothing here rides them. **The two properties carry**: `O-11` = **0** across the six, and **every** `O-8` hit is a `KF-CO-8`/`KF-CO-1` substring — both re-verified at round 5 and again at `PASS-5/CLOSE-CERT-2.md`.⟩ **None of the six carries a KF.W1 edge; KF-W7 and KF-W9 name KF.W1 only to declare it a non-edge** (the *"0 in FOUR"* numeral is struck at repair round 6 per PASS-6 D6-2 — struck at all three of its live sites, the strike stated whole at `:60`; the property is what §9 item 5 carries and it is re-verified at all four legs) ⟨**RECONCILE SEAT, 2026-08-28 — SUPERSEDED AT FINAL BYTES (`PASS-5/CLOSE-CERT-2.md` §4 · R5-R1)**: the token reads **KF-W2 7** · 0 at KF-W5/W6/W8 · 1 each at KF-W7/W9. The promise two clauses above — *"both re-verified at round 5 and again at `PASS-5/CLOSE-CERT-2.md`"* — **is kept, and the cert is `CLOSE-CERT-2.md`**: the two properties (`O-11` → 0 across the six; every `O-8` a `KF-CO-8`/`KF-CO-1` substring) **both HOLD at final bytes**, and it is only the per-sibling token numeral that moved, KF-W2 having minted seven relay-vehicle attributions in its own round-5 `D-13` cure. **The numeral is retired to a dated observation (LAW E(4)); the anchor and the two properties are the receipt.**⟩ ⟨**ROUND-5 CORRECTION over the round-4 one below, check `D5-1`**: the round-4 spelling *"0 in FIVE and 1 in KF-W9"* is **STRUCK**; the round-4 record is preserved beneath it unedited, because the class it names is the class that fired again⟩ ⟨**ROUND-4 CORRECTION, stated not silently applied (E-3) · check `D4-5` (MAJOR).** This clause read *"the bare-`KF.W1` token is **0 in all six**"* — **STRUCK as false at the bytes**, here and at **§11 row 10**, its two sites. `grep -oE 'KF\.W1([^0-9A-Za-z-]|$)' KF-W9.md` → **1**, in W9's own **`S-10.1` cross-edge enumeration** — its round-3 D3-5 cure, written at **15:39**, three minutes before this file closed at 15:42 — where W9 names KF.W1 **in order to declare it NOT an edge**. **Substantively the hit CONFIRMS the non-consumer finding and strengthens it**: an explicit declared non-edge is a **stronger reciprocation than silence**. But the *stated measurement* was false, at both sites, **on the axis that defines this wave's consumer set**, which is the one place this file cannot afford a stale zero. **KF-W2's `O-8` count is likewise restated 1 → 2** (occurrences and lines both): W2 was written at **15:52**, ten minutes after this file, and its second `KF-CO-8` substring arrived with that write. **The load-bearing claim is unchanged and re-verified**: `grep -c 'O-11'` → 0 in all six, and **every** `O-8` hit across the six is a `KF-CO-8`/`KF-CO-1` substring — **zero real O-8/O-11 letter citations across the non-consumer set.** Per **LAW C(3)** the KF-W9 receipt rides its **§-heading + row id alone** — W9 writes in stage 2, later than this seat — and the **RECONCILE seat** verifies it at W9's final bytes.⟩ **So the COHESION §3.1 conditional mark discharges on the grep across all SIX non-consumers** (W2 · W5 · W6 · W7 · W8 · W9), not the three round 1 could measure, and the reciprocity set stands unchanged at four declared consumers plus KF-W0's recorded reverse edge. An earlier draft measured four and enumerated seven; **the measured set is the authored set, now ten.** Re-measured: KF-W3 and KF-W4 reciprocate exactly as owed (**§Preconditions OP-5** at both, plus the §Cross-edges/§Sequencing rows named in §1); **KF-W10 reciprocates hardest of all and was MISSED at round 1** — three explicit dependencies (**§1 `Opens after`** · **§Preconditions `OP-3`** UNLANDED, "false close" · **§Sequencing cross-edge `B. → KF.W1`**) citing **both letters by id, repeatedly, at declared anchors** ⟨round-5 strike, check `D5-4`: *"with 8 × `O-8` and 3 × `O-11`"* — the numerals retired to the property, which re-measurement only strengthens⟩, i.e. precisely *"a kf wave consuming O-8/O-11 obligations"*, the class this wave declares itself to precede — now carried at **cross-edge 11**; **KF-W0 declares the REVERSE dependency edge** at its **§Sequencing OUTBOUND row** and its **§Excluded row** (conflict recorded, cross-edge 2 — for the COHESION §3 check and the owner, not for either spec to rewrite the other). **All cross-spec receipts in this item are stable anchors per R2-7**; the round-1 line coordinates were dead at every one and are struck, the edges themselves unchanged.
6. **Every remaining figure RE-EXECUTED at repair round 2, and split by what it measures** (R2-5: a witness a seat leaves standing without re-running is that seat's defect).

   **(a) Keyframes-side, re-derived at the frontier of record `origin/master` `81a56990` — ALL HOLD, byte-exact**: clone HEAD `81a56990` ✓ · the **9-entry** coordination listing + `vnext/`, no addendum ✓ · the three `parseStylesheet` **call sites** (`compile/adapter.ts:222` · `scroll/grammar.ts:109` · `validate.ts:182`) and their three **imports** (`:7`/`:37`/`:47`) ✓ *(the raw grep's full output is nine lines — see G-KF1-3)* · the **62**-import split (`/css` 29 · `/value` 16 · `/color` 7 · `/math` 5 · `/easing` 3 · `/transform` 2) ✓ · the **9-row** `INBOUND-LEDGER` (IN-ATLAS-1..5 · IN-GLASS-1..2 · IN-VALUE-1..2), no IN-VALUE-3/4 ✓ · `package.json:77` glass-ui `"7.0.0"` exact — SCH-1's falsifier, F-1 FALSIFIED-AT-HEAD ✓ — and `:70` value.js `"4.0.0"` pin ✓ · the three ABSENT packet anchors (`compile/value-ast.ts` · `compile/easing/easing-registry.ts` · `compile/emit/backward.ts`) ✓ · `resolve/browser.ts:162` ✓ · every §5a row incl. **row 14 now measured** (`test/internal/leaves-parity.test.ts` **PRESENT**), `emit/backward/` holding exactly `backward.ts, color.ts, index.ts, walk.ts`, and `color.ts` at **385** lines.

   **(b) Sacred checkout (read-only) — HOLDS, and at round 3 it is re-labelled as a WORKTREE-STATE RECORD, not a sha**: HEAD `8281638c` + `41  1` behind/ahead ✓ + **252** dirty ✓ + both packet bodies present at **7,064 B** (mtime Jul 24 16:41) and **15,633 B** (mtime Jul 27 12:27) ✓ — **those four figures together are the substrate name**, because the tree the packets measured is the uncommitted worktree and no sha addresses it (§5b). **Added at round 3**: the worktree import census **61** (`/css` 29 · `/value` 15 · `/color` 7 · `/math` 5 · `/easing` 3 · `/transform` 2) — byte-identical to O-11 `:52` on all six lines ✓ · the same census at bare `8281638c` = **81** over a disjoint alphabet ✓ · and the **nine** drifted-or-absent §5a anchors resolved in the worktree at the packets' own coordinates (`value-ast.ts:71` · `easing-registry.ts:36` · `backward.ts:47` · `backward-color.ts:171`/`:250`/`:263` · `browser.ts:165` · `package.json:69` · `easing-serialize.ts:71` bracketed by the packet's `:70-71`) ✓. **This bucket is why the wave's causal account changed at round 3** (§1 Goal criterion; C-5; C-6).

   **(c) Value-side in-tree citations — HOLD**: `CARRY-CUT-LEDGER.md:186` (CC-084 *"no emergency 4.0.1 — ruled"*) and `:255` (X-W9) ✓ · `library-band.md` **`:205-206`** (*"DECLINE — `sampleBezier`"* / *"(permanently, measured zero demand)"* — **the span, corrected at repair round 4 per check `D4-8`**; the round-2/3 spelling *"`:205`"* is struck as one line short of its own quotation) ✓ · the **58**-record adjudicated corpus with **18** `KF.W1-DEP` anchors across **5** records, bare-`KF.W1` token **0**, literal substring **33** = 18 + 15 `KF.W10` ✓ · both `W1.md` and `KF-W1.md` ABSENT immediately before the first write (a dated authoring record — `KF-W1.md` PRESENT thereafter; G-KF1-11).

   **(d) Value-side `INBOX.md` — THE ONE AXIS THAT MOVED, and the whole of round 2's blocker.** The file is **97 L / 56,702 B**, clean in git, last written by `7a7dc6ef` (2026-08-28 12:33:59) after `48003630` (12:32:16) — **both landing before round 1's 12:41 write, neither read by it.** Four round-1 figures are **STRUCK** and restated: *"`:13-14`"* → the keyframes sweep-path law at **`:15-16`** (`:13-14` is now the glass BI→BJ→BK note) · *"`:45` (I-10)"* → **I-10 at `:47`** (`:45` is I-7) · *"`:90` (I-26 "ROWED 2026-08-03")"* → **I-26 at `:92`** (`:90` is I-21a), text verbatim · *"outbound max **O-19**"* → **max `O-20`** (the SS-6 batched glass communique at `:95`). The commissioned mint moves **O-20 → O-21** at every site, under C-10's MINT LAW. **Row ids are the anchors from here on; the line numbers ride as convenience and are re-resolved at open.**

---

## 10. Repair round 1 (X·KF L-20 round 1) — what changed and on whose authority

Recorded, not silently applied, on the same law §9 states. Inputs: `conformance/PASS-1/RULINGS.md` (cross-wave authority; where a local defect and a ruling collide, **the ruling wins**) and `conformance/PASS-1/KF-W1-CHECK.md` (local register: verdict DEFECTIVE — 3 MAJOR · 3 MINOR · 2 INFO, census CLEAN at 6 routed / 6 booked / 0 escaped, 0 inventions). **No carry row was added, renamed, split or dropped: C-1..C-14 stand, ids untouched (anti-rename). Net row delta 0.** Nothing here stamps a verb, authorizes execution, or opens product source.

| # | authority | change |
|---|---|---|
| 1 | **RULINGS R-15** (+ check D-3, MAJOR) | The NO-WAVE-OWNER cardinality **16 → 17**, the canonical roster, **receipted id for id** at C-14 leg (iii), cross-edge 9 and §8. 16 was stated by no document in the tree; the reconciliation is **KF-W0's** then-stale 15 ∪ {amiga, EDITOR-UNIT} (**KF-W9 S-11's** adds) — *[receipt re-anchored at round 2: **KF-W0 §Sequencing, the packet-roster line `the NO-WAVE-OWNER packets — the canonical 17`** — **numeric convenience STRUCK at round-2 close (U-2/R2-7)** (`:401` had drifted onto a §Carry carries-note; the roster line label is the sole anchor); round 1's `KF-W0.md:276` is dead]*. Cross-edge 9's membership restated canonically — **KF.W11 ×9 · KF.W12 ×6 · KF.W13 ×2 (minted)**, with **dock-menu at KF.W13**, homing authority **KF.W4**. The cohesion FLAG this cross-edge raised is marked **DISCHARGED** at that authority, recorded not deleted. |
| 2 | **RULINGS R-19d** (+ check D-1, MAJOR) | Reciprocity re-measured over **all seven** authored siblings, not four: §1's paragraph, cross-edge 1 and §9 item 5 gain **KF-W10**, and **cross-edge 11** is declared for it with its three hard-blocking anchors (**§1 State's `Opens after` line · §Preconditions row `OP-3` · §Sequencing cross-edge `B. → KF.W1` — **numeric convenience STRUCK at round-2 close (U-2/R2-7)**; the round-1 spellings `:16`/`:50`/`:296` are dead and stay struck, as this file already records two bullets above**) and its 8 × `O-8` / 3 × `O-11` receipt. The measured set is now the authored set (W0 · W2 · W3 · W4 · W7 · W8 · W10). |
| 3 | check **D-2** (MAJOR) | Born-RED tally corrected **11 → 10** at wave-open. **G-KF1-11 relabelled DECLARED-SATISFIED** (the treatment G-KF1-12 already had and this gate was owed): its condition is discharged by this file's existence, so no act in `.a`/`.b`/`.c` can turn it. The §4 Bounds grant for this spec moves **create → modify** (the file exists), and `:194`'s "turned GREEN by this file's landing" — an execution-voice claim in a `planned` spec — is gone. |
| 4 | check **D-4** (MINOR) | C-14 leg (ii): the range spelling `B10-17..27` replaced by the intake's own **enumeration with gaps** ⟨`INTAKE-ADJUDICATION-2026-08-03.md` **§3, heading `### KF.W4 · K-Quartet Hygiene`** — *[receipt re-anchored at round 2: `:146`/`:147-149`; round 1's `:144-145` is the tail of the §3 **KF.W0** block and is dead]*⟩, which no longer routes `B10-21` to KF.W4 and KF.W5 in one sentence. |
| 5 | check **D-5** (MINOR) | The alias block's *"once and for all"* made true: the **third live `W1` namespace** — keyframes' own `BUILD W1` (`DISPOSITIONS.md`), six corpus anchors, all three identities folding to **KF.W0** — is declared. Nothing escaped at either state; the mapping is now closed over all three meanings. |
| 6 | check **D-6** (MINOR) | The alias block's anchor census completed **13 → 18**: each record's ADJUDICATED-summary line (`:135` · `:155` · `:149` · `:130` · `:122`) added. Record set (5) was correct throughout. |
| 7 | check **D-7** (INFO) | C-14 leg (i) re-worded to survive the grep it invites: literal `KF.W1` = **33** (18 `KF.W1-DEP` + 15 `KF.W10` substrings); the load-bearing measurement is the **token** — bare `KF.W1` = **0** across 58 records. Substance unchanged. |
| 8 | check **D-8** (INFO) | The COHESION §3.1 cohesion mark's own condition **measured instead of asserted**: KF-W2 cites no O-8/O-11 (`grep -c 'O-11'` → 0; its one `O-8` is `KF-CO-8` **in its §Excluded row `KF-CO-1 / KF-CO-8`**, **numeric convenience STRUCK at round-2 close (U-2/R2-7)** — `:267` drifted), so the conditional is FALSE and the mark **DISCHARGES**. KF-W7 and KF-W8 measure identically and are named, so W2 is no longer singled out. |

**Not changed, and why.** The check's §8 findings stand as recorded and needed no edit: the ∅-column finding (C-14) is independently re-derived, the eleven reproducible gate witnesses are byte-exact, the §5a drift table reproduces row for row, every quoted lock is verbatim at its anchor, and every dissent (`a59d3a22`; the KF-W0 reciprocity conflict; C-4's recorded asymmetry) remains **carried, not resolved**. **L-19 holds**: no gate is a script, no row was manufactured to fill the empty column, and the one gate that could not fail was relabelled rather than kept for the count.

*(§10 is a **dated round-1 record**. It is not rewritten at round 2; where one of its receipts pointed at a line that has since moved, a bracketed `[receipt re-anchored at round 2: …]` marker is added beside the original spelling, which is preserved. Every substantive correction round 2 makes is recorded in §11 below, never by editing this table.)*

---

## 11. Repair round 2 (X·KF L-20 round 2) — what changed and on whose authority

Recorded, not silently applied, on the same law §9 and §10 state. **Inputs**: `conformance/PASS-2/RULINGS-2.md` (cross-wave authority; where a local defect and a ruling collide, **the ruling wins** — except on a *measurement*, where **R2-5** makes the tree outrank the ruling's prose) and `conformance/PASS-2/KF-W1-CHECK.md` (local register: verdict **DEFECTIVE — 1 BLOCKER · 4 MAJOR · 3 MINOR · 1 INFO**; census **CLEAN** at 6 routed / 6 booked / 0 escaped; **0 inventions**; every keyframes-side witness byte-exact). **No carry row was added, renamed, split or dropped: C-1..C-14 stand, ids untouched (anti-rename). Net row delta 0.** No gate was added or removed: the tally stays **10 born-RED · 1 DECLARED-SATISFIED · 1 stay-GREEN INVARIANT**. Nothing here stamps a verb, authorizes execution, or opens product source; every probe was a read-only `git`/`grep`/`ls`/`sed` command.

| # | authority | change |
|---|---|---|
| 1 | **RULINGS-2 R2-1 items 1–3** (+ check **D2-1**, BLOCKER) | **The commissioned outbound mint moves `O-20` → `O-21`** at all five load-bearing sites (§1 hard-gate condition 7 · §3 item 6 · §4 Bounds · C-10 (ii) and its ANTI-RENAME pointer clause · G-KF1-7's title and GREEN) **and at its trailing citation** (cross-edge 11's ledger-receipt clause). Re-measured this seat: `INBOX.md`'s maximum outbound row is **`O-20`** — the SS-6 batched glass-BK communique at `:95`, **SENT 2026-08-28**, minted by `48003630` (12:32:16), nine minutes before round 1's 12:41 write. Executing round 1's text would have written a **duplicate live id** into the E13 ledger and back-pointed O-8/O-11 at it — I-26 reproduced in the ledger layer, under the spec whose thesis is I-26. **G-KF1-7's witness re-cut**: the max-grep's stated output is now **`O-20`** (round 1 said `O-19` — struck). |
| 2 | **RULINGS-2 R2-1 item 2** | **The MINT LAW is written into C-10**: the commissioned id is **`max+1` measured at open**, never a spec-frozen literal; the executing seat re-runs the max-grep at open and, if the ledger has moved, mints `max+1` and records the delta as a dated addendum **before** executing. The id also stops being load-bearing downstream — cross-edge 11 now hands KF.W10 *the row*, not the literal. |
| 3 | **RULINGS-2 R2-1 item 4** (+ check **D2-2**, MAJOR, and **D2-8**, MINOR) | **The three dead `INBOX.md` anchors re-keyed at every citing site** — the four-verb AUDITED cell, §3 item 5, §4 Bounds, C-1, C-4, C-9, G-KF1-6, G-KF1-7, G-KF1-8, §9 item 6: the keyframes **sweep-path law** `:13-14` → **`:15-16`** · **I-10** `:45` → **`:47`** · **I-26** `:90` → **`:92`**. §4 Bounds' three-edit grant is re-aimed accordingly. **Row ids are now the anchors**; line numbers ride as convenience and are re-resolved at open. **G-KF1-8's witness narrowed** so the command produces its stated reading: `grep -n '^- \`\.\./keyframes\.js' …` → exactly **1 hit** at `:15`. |
| 4 | **RULINGS-2 R2-5** (the witness re-derivation law) **over R2-1 item 4** | **One departure from the ruling's prose, declared.** R2-1 item 4 states the broad grep `grep -n 'keyframes.js/docs/tranches' INBOX.md` returns **4 lines** (`:15`/`:66`/`:72`/`:75`). **Re-executed at the current bytes it returns FIVE** — the ruling's enumeration omits **`:68`**, the **O-4** WL-verdicts outbound path cell (in the ledger since 2026-07-17). R2-5 is explicit that *"a ruling orders a cure; it is never a source of measurements,"* so **the measured five is written** and the delta is recorded here for the pass-3 seat. The cure R2-1 ordered is unaffected: the gate carries the narrowed 1-hit pattern, with the broad form admitted only when stated as "5 hits, the law at the first." |
| 5 | **RULINGS-2 R2-1 item 6** (running **RULINGS R-12**'s general rule here) (+ check **D2-3**, MAJOR) | **The phantom "SS-2 CARRY" provenance is STRUCK** at all five load-bearing sites (`:7` the header · the SPECIFIED evidence cell · §4 Bounds' create-grant row · §5's preamble · §9's framing and its six items). Provenance is restated: **authored from the 58-record adjudicated registry directly**, with the routed denominator the registry's — **6 routed / 6 booked / 0 escaped**, re-derived this seat. `find docs -iname '*CARRY*'` under `X/keyframes/` returns exactly one file, `carry/KF-W6-CARRY.md`, which is **W6's**. §9's six items are re-labelled **dated authoring-time findings** with content preserved whole (E-3); each superseded reading is quoted **as the reading corrected, never as an authority**. **No ledger absent from the tree is cited as a source in this file again**, and no `N of N rows carried` claim is keyed to one. |
| 6 | **RULINGS-2 R2-1 item 5** (+ check **D2-5**, MAJOR) | **C-9 gains its own precedent**, one sentence with its receipt: commit **`7a7dc6ef`** (2026-08-28 12:33:59, seven minutes before round 1's write) cured the **glass limb** of C-9's exact defect class in this very file — a codified sweep path pinned to a dead tranche — and recorded the cost in-row: *"The stale BI pin here cost 19 days on the 08-09 letters → I-28/I-29."* Two limbs of one law-level defect, one already cured at a measured 19-day cost: corroboration of C-9's thesis and **the template for its two-line cure**. **No new obligation, no scope change** — and no UNREAD-mail breach follows, since I-28/I-29 are glass rows routed to the X·V fold, outside this wave. |
| 7 | **RULINGS-2 R2-7** (the citation idiom) (+ checks **D2-4** MAJOR, **D2-6** and **D2-7** MINOR) | **Every cross-spec receipt this seat touched is converted to a STABLE ANCHOR** — §-heading, gate id, OP id, or table-row label — with any line number riding parenthetically and declared non-load-bearing. Converted and re-resolved this seat: **KF-W4** §Preconditions OP-5 + §Sequencing row `KF.W1 · Mail Cure` (`:206`→`:242`) · **KF-W3** §Cross-edges row `KF.W1 · Mail Cure (O-8/O-11) + E13` (`:209`→`:220`) · **KF-W10** §1 `Opens after` (`:16`→`:20`), §Preconditions `OP-3` (`:50`→`:54`), §Sequencing `B. → KF.W1` (`:296`→`:332`) · **KF-W0** §Sequencing OUTBOUND row + §Excluded row (`:269`/`:306`→`:393`/`:431`), §Sequencing packet-roster line (`:276`→`:401`), §Sequencing KF.W4 OUTBOUND row's gap-preserving `B10` transcription (`:271`→`:394`) · **KF-W2** §Excluded rows (`:270`→`:375`, `:267`→`:428`) · **KF-W7** §Excluded row + N-10 paragraph (`:103`/`:338`→`:355`/`:111`) · **`INTAKE-ADJUDICATION-2026-08-03.md` §3** heading `### KF.W4 · K-Quartet Hygiene` (`:144-145`→`:146`/`:147-149`). ⟨**ROUND-2 CLOSE STAMP (U-2 re-anchor sweep, 2026-08-28).** This crosswalk is preserved as the round-2 conversion *record* and is not re-numbered. **Every right-hand coordinate above is now RETIRED, not re-issued**: the sibling specs were themselves repaired in the same round, and re-measured at this close only the `INTAKE-ADJUDICATION` pair (`:146`/`:147-149`) and **KF-W10 §1's `Opens after` line** still resolve — the other nine drifted again within the round, which is the argument for the idiom in one fact. **Every edge is now carried at its §-heading + row label alone.** The four `KF-W7` coordinates (`:355`/`:111`) resolved at this sweep but are retired with the rest, on the same rule.⟩ ⟨**STAMP CORRECTED at repair round 3 — RULINGS-3 LAW B + `(c)(1)`, check `D3-3`.** The clause that stood here — *"with **NO line number anywhere** on this file's cross-spec receipts"* — is **STRUCK**. It was a **completeness claim in spec voice**, and `conformance/PASS-3/KF-W1-CHECK.md` §6 **D3-3** (2026-08-28) falsified it against this file's own bytes: **five** coordinates survived the sweep, **four of them dead** (`:393`→ the row is at `:439` · `:431`→`:477` · `:54`→`OP-3` at `:59` · `:332`→`:382`) and **one alive** (`:20`). Round 3 struck the four at cross-edges 2 and 11 and left `:20` carried **openly** as a dated, declared non-load-bearing convenience. **The state of this axis is certified nowhere in this spec's voice** — it is whatever the freshest conformance census artifact measures, presently that check, re-pointed at pass 4. ⟨**STAMP CORRECTED at repair round 5 — check `D5-2`, the carriage-citation class swept whole.** *"Re-pointed at pass 4"* was **stated and not performed** at this site: the axis's certification is now **`PASS-5/KF-W1-CHECK.md` §2.1** (every cross-spec anchor re-resolved at the siblings' final bytes and found to hold) **+ §6 `D5-5`** (the three stage-2 **quotations** convicted as truncations and struck at this round). The last surviving line coordinate on this file's cross-spec receipts — KF-W10 §1's `Opens after` `:20` — is **RETIRED at round 5** under **RULINGS-5 LAW E(4)**, so the axis now holds as **zero coordinates by construction**, and it is still certified in the artifact's voice, never in this file's.⟩ The lesson R2-7 stated and this stamp broke: *carrying coordinates openly is honest; certifying zero while carrying five is not.*⟩ **Every edge survives at its new home in the same direction and with the same force** — R-19d and the D-4/R-15 substance were cured in kind at round 1; only their receipts had drifted. The residue is the **Re-anchor seat's**, which writes LAST under R2-7's ordering law. |
| 8 | **RULINGS-2 R2-5** (+ check **D2-9**, INFO) | **§5a row 14 is measured**: `git cat-file -e 81a56990:test/internal/leaves-parity.test.ts` → **PRESENT**. It was the file's one figure marked *"inherited … not re-measured here"*, under a header whose own law is *"anchors are re-resolved at authoring, never copied."* **The executing seat now inherits nothing.** |
| 9 | **RULINGS-2 R2-5**, applied to witnesses this seat touched | **G-KF1-3's witness states its command's true output**: the `parseStylesheet` grep returns **nine** lines, not six — the six load-bearing code lines (3 call sites + 3 imports) **plus three prose mentions** (`adapter.ts:202` · `validate.ts:41` · `validate.ts:130`); a narrowing is supplied. **C-3 and G-KF1-5's O-8 anchor is corrected `:100-102` → `:101-103`** (`:100` is blank), re-measured read-only in the sacred checkout. **G-KF1-11's sibling count `seven` → `eleven` specs** (this one + ten siblings, `KF-W5`/`KF-W6`/`KF-W9` landed since round 1) — the third consecutive round in which a frozen sibling count went stale, so the figure is now declared measure-at-open. |
| 10 | **RULINGS-2 R2-5**, the reciprocity extension | **The COHESION §3.1 conditional mark is re-measured over the three new siblings** and discharges wider than round 1 could see: bare-`KF.W1` token is **0** in KF-W5 · KF-W6 · KF-W9 (as in W2 · W7 · W8), `grep -c 'O-11'` is **0** in all three, and every `O-8` hit in them is the `KF-CO-8` substring — each line inspected this seat. **The mark discharges across all SIX non-consumers**; the consumer set is unchanged at four (W0's reverse edge recorded, W3/W4/W10 declaring forward). ⟨**STAMP CORRECTED at repair round 4 — check `D4-5`, LAW D.** The clause *"bare-`KF.W1` token is **0** in KF-W5 · KF-W6 · KF-W9"* is **falsified at KF-W9**, which now carries **1** — its `S-10.1` cross-edge enumeration naming KF.W1 **in order to declare it a non-edge**, written three minutes before this file closed at round 3 and never swept. **The round-2 wording is preserved** (§11 is a dated record and is not rewritten); the true reading, its command and its LAW-C(3) anchor-only treatment are at **§9 item 5** and **§1 `:58`**, and the round-4 record is at **§13**. **The row's conclusion is unaffected and is re-verified this seat**: `grep -c 'O-11'` → 0 in all six and every `O-8` hit is a `KF-CO-8`/`KF-CO-1` substring, so the mark still discharges across all six — a declared non-edge discharges it more strongly than silence does.⟩ |

**Witnesses re-derived at this repair (R2-5).** **Forty-four**, each by a read-only command run at write time, in three buckets.

**(i) Twenty-four keyframes-side**, at the frontier of record `origin/master` **`81a56990`** (never at `8281638c`, which was read only to date the drift): the frontier sha · sacred-checkout HEAD `8281638c` · `41 1` behind/ahead · 252 dirty · the 9-entry coordination listing · the 9-row INBOUND-LEDGER · the three ABSENT packet anchors · `browser.ts:162` · `package.json:70` · `package.json:77` · the three `parseStylesheet` call sites + three imports with the nine-line raw output · the 62-import split · `compile.ts:32` · `registry.ts:36` · `easing-serialize.ts:71` · the `emit/backward/` four-file listing · `color.ts` 385 L · `options.ts:31` · `leaves.ts:28` · `load-engine.ts:65` · `css-text.ts:41` · **row 14 `leaves-parity.test.ts` PRESENT** · the two sacred-checkout packet bodies (7,064 B / 15,633 B with mtimes) · O-8's `:101-103`.

**(ii) Thirteen value-side**, at the current tree bytes: `INBOX.md` 97 L / 56,702 B · max outbound `O-20` at `:95` · the sweep-path law at `:15-16` · I-10 at `:47` (I-7 at `:45`) · I-26 at `:92` (I-21a at `:90`) · the broad grep's **five** hits against the narrowed grep's **one** · commits `48003630` and `7a7dc6ef` with their timestamps · the 7-file retained set · the 0-hit back-fill `find` · `CARRY-CUT-LEDGER.md:186`/`:255` · `library-band.md:205` · the 58-record corpus census (18 / 0 / 33 / 15, with the 5 preamble + 6 routing + 5 summary + 2 rider anchor decomposition) · the 11-spec sibling set with the ten siblings' `KF.W1` / `O-8` / `O-11` counts.

**(iii) Seven sibling-spec anchor re-resolutions** (R2-7): KF-W4 · KF-W3 · KF-W10 · KF-W0 · KF-W2 · KF-W7 · `INTAKE-ADJUDICATION-2026-08-03.md`.

**Nothing was inherited from a ruling, a check, a lane doc, or this file's own prior text.** Where round 2's measurement contradicts round 1's, the round-1 reading is quoted and struck by name beside the true one (E-3); the one place it contradicts **RULINGS-2's own prose** is row 4 above, declared rather than quietly conformed.

*(§11 is a **dated round-2 record**, treated as §10 is: it is not rewritten at round 3. Where one of its certifications was falsified since, a bracketed `⟨STAMP CORRECTED at repair round 3: …⟩` marker stands beside the original wording, which is preserved. Every substantive correction round 3 makes is recorded in §12 below, never by editing this table.)*

**Still standing, unchanged and deliberately so.** Census carriage per **`conformance/PASS-3/KF-W1-CHECK.md` §1 (2026-08-28)**, the freshest artifact ⟨**STAMP CORRECTED at repair round 5 — check `D5-2` (MAJOR), the third and last of this file's unswept carriage citations.** *"The freshest artifact"* became **false on its face** when round 4 re-pointed the header and the alias block and left this dated paragraph behind: one file, **two** named "freshest" artifacts for **one** quantity — the D3-4 class in citation form, and R4-10(1) failing on the round that wrote it. **The freshest artifact is `conformance/PASS-5/KF-W1-CHECK.md` §1 (2026-08-28)**, which found the identical **6 / 6 / 0 / 0**; the round-2 wording is preserved unedited per §11's own law, and the whole class — command, enumeration, counting rule, per-hit disposition — is receipted at **§14**⟩ — **6 routed · 6 booked · 0 escaped · 0 inventions**, re-derived there from the corpus bytes with no denominator inherited from pass 1 or pass 2 (**LAW B**: the claim lives in the artifact, not in this sentence; the same artifact returned DEFECTIVE on four other axes, cured at §12). The ∅-column finding is a **finding, not a gap**, and **no row, gate or bounds item was manufactured to fill it** (C-14's L-19 lock). Every **dissent is carried, not resolved**: the `a59d3a22` rider (KF.W0's re-measurement) · the KF-W0 reverse-edge **conflict** (recorded for the COHESION §3 check and the owner — neither spec rewrites the other) · C-4's recorded asymmetry. Every **lock** is carried verbatim: C-1's *"Amend, do not re-send"* + ANTI-STALENESS · C-3's HARD NEGATIVE · C-7's no-re-ask/no-re-scope + anti-rename on `K1–K4`/`G-L7a–d` · C-8's HARD NEGATIVE + DESIGN LOCK · C-9's SIZING LOCK · C-10's ORDERING lock · C-11's SEQ-LOCK + verbatim-retention · C-12's HARD NON-GATING · C-14's L-19. **L-19 holds affirmatively**: no gate is a script, no phantom command is cited, and the mail tooling a lesser spec would mint is excluded by name. **Status stays `planned`; VERIFIED stays NO; no product source is opened in either repo.**

---

## 12. Repair round 3 (X·KF L-20 round 3) — what changed and on whose authority

Recorded, not silently applied, on the same law §9, §10 and §11 state. **Inputs**: `conformance/PASS-3/RULINGS-3.md` (cross-wave authority — **LAW A** the import-graph law, **LAW B** the closure-claim ban, **R3-2** this wave's BLOCKER, **(c)(1)** the anchor-rot family, and the §END directive map; where a local defect and a ruling collide, **the ruling wins** — except on a *measurement*, where R2-5 keeps the tree above the ruling's prose) and `conformance/PASS-3/KF-W1-CHECK.md` (local register: **DEFECTIVE — 1 BLOCKER · 5 MAJOR · 3 MINOR · 2 INFO**; census **CLEAN** at 6 routed / 6 booked / 0 escaped; **0 inventions**; every keyframes-side witness and every value-side `INBOX.md` anchor byte-exact). **All eleven local defects are cured; none is declined.**

**No carry row was added, renamed, split or dropped: C-1..C-14 stand, ids untouched (anti-rename) — C-6's banked id `SCH-4` is unchanged and only its false descriptive tail was struck. Net row delta 0.** **No gate was added or removed**: the tally stays **10 born-RED · 1 DECLARED-SATISFIED · 1 stay-GREEN INVARIANT**, and §1's header now *states* that tally instead of contradicting it. Nothing here stamps a verb, authorizes execution, or opens product source; every probe was a read-only `git` / `grep` / `sed` / `ls` command, and the sacred checkout was read and never written.

| # | authority | change |
|---|---|---|
| 1 | **RULINGS-3 `R3-2`.1 + `.3`** (+ check **D3-1**, BLOCKER) | **C-6 and G-KF1-4 re-cut so the cure stops commissioning a NEW false certification.** The addendum now withdraws *"matches your tree exactly"* by naming a **WORKTREE-STATE RECORD** — HEAD `8281638c` + 252 dirty paths, dated, byte-identical to O-11's split on all six lines — and explicitly **not** a sha; the round-2 order to inscribe *"`8281638c`"* is quoted and **STRUCK**. **§5b is new**: the three censuses (worktree **61** · bare `8281638c` **81**, disjoint alphabet · frontier **62**) pasted with their commands, so the commissioned sentence rides beside the measurement that makes it true (**LAW B corollary**). The **ANCHOR DUALITY** is declared there verbatim, and its final clause is written into C-6 as a LOCK: ***"No certification cites a sha as 'the tree measured' unless the census reproduces at that sha."*** |
| 2 | **RULINGS-3 `R3-2`.4** (+ check **D3-2**, MAJOR) | **The wave's inverted thesis is struck with dated corrections at all three sites** — §1's Goal criterion (*"they certified a tree they had not measured"*), C-5 (*"Both packets measured the stale tree"*), C-6 (*"import-census off-by-one"*). Re-derived this seat: **nine of nine** drifted-or-absent §5a anchors resolve in the sacred worktree at the packets' own coordinates, and the corroborating instrument agrees — `backward-color.ts:171/:250/:263` are the `sampleRamp` declaration and its two calls (O-11 `:175`'s *"`count=1024`"* tuple) in the worktree, and **docblock prose** at bare `8281638c`. The packets **measured precisely and named nothing**; the defect is unreachability plus sha-less certification. **The operational payload is untouched** — the `origin/master` re-anchoring and the §5a table were correct and remain so. This matters beyond hygiene: the wave's product is a letter, and relaying *"you measured a stale tree and were off by one"* would send a **false statement about the addressee's tree** out of the repo. |
| 3 | **RULINGS-3 `R3-2`.2** (+ check **D3-6**, MAJOR) | **The two tree-identity gates are re-keyed off bare `HEAD`.** G-KF1-2 and G-KF1-4 now name **repo AND substrate in every command** — frontier legs `in keyframes-v-exec` at `origin/master` (the idiom G-KF1-3/G-KF1-10 already used), historical legs against §5b's worktree-state record. Demonstrated failure, measured this seat: in the sacred checkout — the tree a seat is most likely to be sitting in, since §4 puts it in bounds and OP-1's `ls` runs there — bare `HEAD` is the **DISQUALIFIED** `8281638c`, where `git grep -n 'parseCssScalar' HEAD -- src/animation/resolve/browser.ts` returns **nothing (exit 1)** and `git grep -c 'parseStylesheet' HEAD -- src` returns **0 files**. A prose preamble declaring the frontier does not govern a command; the command now carries it. **The sweep this ruling ordered found a THIRD member of the class, worse-shaped than the two ruled** — the **alias block's SCH-1 falsifier**, which also ran bare `HEAD -- package.json`. Run in the sacred checkout it **does not fail**: it returns `package.json:71: "@mkbabb/glass-ui": "6.0.0"` — a different line, a different version, and a hit a seat reads as *confirmation*. Three substrates, three answers, all measured this seat: sacred **worktree** → **absent** (F-1's phantom-dependency claim, RED, as banked) · **`8281638c`** → `6.0.0` at `:71` · **`origin/master`** → `7.0.0` at `:77`, the reading that makes **F-1 FALSIFIED-AT-HEAD**. The wave's whole SCH-1 disposition turned on an unnamed substrate; it is now named in the command. **G-KF1-3 and G-KF1-10 are re-spelled in the same motion** (`HEAD` → `origin/master`; they already named the repo, and `keyframes-v-exec` is at `81a56990` porcelain 0, so both readings re-verified **identical** — nine `parseStylesheet` lines, nine `INBOUND-LEDGER` rows — but the spelling no longer *depends* on that holding at the executing seat's clock). **Four gate/witness sites re-keyed in total.** |
| 4 | **RULINGS-3 `(c)(1)`** (+ checks **D3-3** and **D3-4**, MAJOR) | **The four dead coordinates are dropped from cross-edges 2 and 11** (`:393` · `:431` · `:54` · `:332`) and the edges ride the stable anchors §1 already names. This also cures the **one file, two verdicts** contradiction: §1's KF-W0 and KF-W10 bullets had struck those same four **by name**, while §7 asserted them live. **The single surviving coordinate — KF-W10 §1's `Opens after` at `:20`, ALIVE — is carried openly**, dated, declared non-load-bearing, and re-resolved at open. **§11 row 7's close stamp is corrected per LAW B**: its clause *"NO line number anywhere on this file's cross-spec receipts"* is struck as a spec-voiced completeness claim that its own file falsified, and the axis is now certified by citation to `PASS-3/KF-W1-CHECK.md` §6 D3-3 (2026-08-28), re-pointable at pass 4. |
| 5 | check **D3-5** (MAJOR) | **The census cardinality schism at the booking site the header cites is closed.** §8 row 5 read *"All **five** `KF.W1-DEP` / census F-1 **rows**"* — it counted **records as rows** and under-booked by one, dropping `kf-AnimationControlsGroup.md:47` and the `useControlsKeyboardShortcuts.ts:1` sole-binding-site rider it owns. Re-derived this seat (`grep -n 'KF\.W1-DEP' kf-*.md`, every hit read and classified): **six routing rows across five records — ACG carries two.** §8 row 5 now enumerates all six by anchor; the alias block gains the same enumeration; C-13's *"five"* is kept and labelled as a **record** count. The header PROVENANCE cites this cell as its booking receipt, so the two now agree instead of colliding. |
| 6 | **RULINGS-3 LAW B** | **Every closure sentence this seat touched is converted to citation form.** The header PROVENANCE, the alias block's *"the COMPLETE census"*, §5's preamble, C-14's *"verified three ways"*, §11's close stamp and its *"Still standing"* paragraph now **cite `conformance/PASS-3/KF-W1-CHECK.md` §1 / §6 (2026-08-28) by path and date, and state what that artifact found — including that it returned DEFECTIVE on four axes** — instead of asserting carriage, anchor-cleanliness or zero-escape in this spec's own voice. When a newer pass supersedes it, **the citation is re-pointed, never the claim re-asserted here.** ⟨**STAMP CORRECTED at repair round 5 — check `D5-2`, and the record of how the scope law failed here.** This cell **names the six sites** (header PROVENANCE · alias block · §5's preamble · C-14 · §11 row 7's close stamp · §11's *"Still standing"* paragraph). Round 4 re-pointed **two** and the round's scope receipt did not enumerate this class, so the law stayed green over its own counterexample — **R4-10(1) failing on the round that wrote it**. **All six are re-pointed at round 5 in ONE act**, the class is made **greppable**, and its command + enumeration + counting rule now live at **§14's CARRIAGE-CITATION scope receipt** so no future round can sweep a subset and call the class closed.⟩ |
| 7 | **RULINGS-3 LAW A** | **§4c is new, and it discharges the law in both directions.** (i) The **module clause is VACUOUS by enumeration**: the six bounds rows are two `modify` and four `create`, all markdown; this wave deletes, repoints or shims **no module** in either repo. (ii) The wave's **one repoint act** — C-9's mail-path law — gets a LAW-A-shaped consumer census anyway, pasted with its commands and outputs (23 hits / 17 files; 12 hits / 7 files on the law's own spelling; the subject grep at 1). **It found a second law-limb OUTSIDE this wave's bounds**: `~/.claude/…/memory/feedback-mail-inbox-law.md:16` codifies the same four-path sweep with the frozen keyframes path, so **repairing `INBOX.md` alone does not close the class** — the memory limb re-injects it at the next session open. Recorded as an **OPEN residual routed to the OWNER**; edited by nobody here. The 22 non-consumer hits (the four O-2/O-4/O-8/O-11 ledger path cells, `lane-docs.md:290`'s transcription of the now-dead `:13-14` coordinate, five reformation/letter records, the apotheosis and harvest artifacts) are **recorded as context, never counted as consumers**. |
| 8 | check **D3-7** (MINOR) | **§1's hard-gate header re-cut `twelve` → ten hard conditions + one DECLARED-SATISFIED + one stay-GREEN INVARIANT**, with the two non-gates named (G-KF1-11, G-KF1-12) instead of counted. §6 and §11 already read 10 · 1 · 1; the header had re-inflated by exactly the two its own next paragraph deflates. |
| 9 | check **D3-8** (MINOR) | **Cross-edge 9's transit roster gains `KF-AV-28`**, the supersession rider on the W7-evaluated surfaces. §8's 17-packet row already carried it; the roster introduced as *"the locks that must travel WITH those homings, restated so they are not lost in transit"* enumerated five and dropped it — an omission in the **operative** transit declaration. The two enumerations now agree at **six**. |
| 10 | check **D3-9** (MINOR) | **The discharge tally stops mixing metrics.** *"(KF-W5 ×1, KF-W6 ×2, KF-W9 ×5)"* is struck: W5/W9 were occurrence counts, W6 a line count, and **no single command yields the triple**. Re-measured over all six non-consumers, both metrics stated separately — occurrences W2 1 · W5 1 · W6 4 · W7 2 · W8 0 · W9 5; lines W2 1 · W5 1 · W6 2 · W7 2 · W8 0 · W9 3; `O-11` **0** in all six; bare-`KF.W1` token **0** in all six. **The load-bearing claim was and is TRUE** — every hit is a `KF-CO-8`/`KF-CO-1` substring. |
| 11 | check **D3-10** (INFO) | **G-KF1-5's third witness leg is made literal.** *"Grep of both coordination trees for any withdrawal → 0"* carried no pattern, no path and no command inside a §6 promising *"literal commands with literal outputs"*. Replaced by two commands over **all three** coordination trees with their outputs read hit by hit: `4\.0\.1` → 4 hits (three ledger row cells + the question itself at O-8 `:101`), **zero in the exec-visible tree**; `withdraw` → 4 hits, each identified (an O-10a glass row, two CONSTELLATION configurator notes, a vnext skeptic table), **none a withdrawal of the vehicle question**. |
| 12 | check **D3-11** (INFO) | **§5a rows 6–8 are measured** — this file's last unmeasured figure, sitting under a header whose own law is *"anchors are re-resolved at authoring, never copied"*. All three are **EXACT at the frontier** (`emit/backward/color.ts:171`/`:250`/`:263` = the `sampleRamp` declaration and its two calls) and byte-identical at the worktree's `backward-color.ts`, both files **385 L**: **path drift only, zero line drift**. The executing seat re-derives them at open and **inherits nothing.** |

**Witnesses re-derived at this repair (R2-5, run as the FULL-SURFACE sweep RULINGS-3 §(e) orders — every witness, every anchor, BOTH baselines checked for silent mis-resolution).** Each by a read-only command at write time.

**(i) Frontier, `keyframes-v-exec` at `origin/master` `81a56990` (porcelain 0)** — sha ✓ · the **62** import split (`/css` 29 · `/value` 16 · `/color` 7 · `/math` 5 · `/easing` 3 · `/transform` 2) ✓ · `browser.ts:162` + its import `:3` ✓ · `package.json:70` (value.js `4.0.0`) and `:77` (glass-ui `"7.0.0"`, SCH-1's falsifier) ✓ · the three ABSENT packet anchors ✓ · **`emit/backward/color.ts:171`/`:250`/`:263` measured, 385 L** ✓.

**(ii) The worktree-state record, sacred checkout, read-only** — HEAD `8281638c` ✓ · `41  1` ✓ · porcelain **252** ✓ · the worktree census **61**, six lines byte-identical to O-11 `:52` ✓ · the bare-`8281638c` census **81** over a disjoint alphabet ✓ · the **nine** §5a anchors resolved at the packets' own coordinates ✓ · the two packet bodies at 7,064 B / 15,633 B with their mtimes ✓ · **the D3-6 failure reproduced** (bare `HEAD` legs return nothing there) ✓.

**(iii) Value-side, current tree bytes** — `INBOX.md` **97 L / 56,702 B** ✓ · the sweep-path law's narrowed grep → **1 hit at `:15`** ✓ · the broad grep → **5** (`:15` `:66` `:68` `:72` `:75`) ✓ · max outbound **`O-20`** ✓ · **I-26 `:92`** / I-21a `:90` / **I-10 `:47`** / I-7 `:45` ✓ · the LAW-A mail-path census (23/17 · 12/7 · 1) ✓ · the D3-10 withdrawal probes (4 + 4, each read) ✓ · the **58**-record corpus (18 anchors / 5 records / **6 routing rows** / bare token 0 / literal 33 = 18 + 15) ✓ · the ten siblings' `KF.W1` / `O-8` / `O-11` counts, both metrics ✓.

**Nothing above is inherited from a ruling, a check, a CARRY, a lane doc, or this file's own prior text** — RULINGS-3's own case facts were re-derived here before being written, as R2-5 requires of every seat, and the one figure this seat re-derived beyond the rulings' text (the `emit/backward/color.ts` line anchors) is recorded as a measurement, not a correction to any ruling.

**Still standing, unchanged and deliberately so.** Every **lock** is carried verbatim, and C-6 gains one (the LAW-B substrate lock). Every **dissent is carried, not resolved**: the `a59d3a22` rider (KF.W0's re-measurement) · the **KF-W0 reverse-edge conflict** (recorded for the COHESION §3 check and the owner — neither spec rewrites the other; round 3 struck only its dead coordinates, never its direction or force) · C-4's recorded asymmetry. **L-19 holds affirmatively**: no gate is a script, no row or bounds item was manufactured, the mail tooling a lesser spec would mint is excluded by name — and the two non-gates stay labelled rather than counted. **Status stays `planned`; SPECIFIED stays YES; IMPLEMENTED and VERIFIED stay NO; no product source is opened in either repo; the sacred checkout is unchanged.**

*(§12 is a **dated round-3 record**, treated as §10 and §11 are: it is not rewritten at round 4. Where one of its certifications has since been falsified, a bracketed `⟨STAMP CORRECTED at repair round 4: …⟩` marker stands beside the original wording, which is preserved. Every substantive correction round 4 makes is recorded in §13 below, never by editing this table.)*

---

## 13. Repair round 4 (X·KF L-20 round 4) — what changed and on whose authority

Recorded, not silently applied, on the same law §9, §10, §11 and §12 state. **Inputs**: `conformance/PASS-4/RULINGS-4.md` (cross-wave authority — **LAW C** the two-stage/reconcile law, **LAW D** the verbatim law, **R4-8** the KF.W11/W12/W13 disposition, **R4-10** the scope law, and the §END directive map; where a local defect and a ruling collide, **the ruling wins** — except on a *measurement*, where **R2-5** keeps the tree above the ruling's prose) and `conformance/PASS-4/KF-W1-CHECK.md` (local register: **DEFECTIVE — 0 BLOCKER · 5 MAJOR · 3 MINOR · 2 INFO**; census **CLEAN** at 6 routed / 6 booked / 0 escaped; **0 inventions**; **all eleven PASS-3 local defects verified CURED at the bytes**; **the severity head MONOTONE for the first time in this loop**). **All ten local defects are cured; none is declined.**

**No carry row was added, renamed, split or dropped: C-1..C-14 stand, ids untouched (anti-rename). Net row delta 0.** **No gate was added or removed**: the tally stays **10 born-RED · 1 DECLARED-SATISFIED · 1 stay-GREEN INVARIANT**. Nothing here stamps a verb, authorizes execution, or opens product source; every probe was a read-only `git` / `grep` / `sed` / `ls` command, and the sacred checkout was read and never written.

**THE ROUND-4 SEAT'S OWN POSTURE (LAW C).** This seat writes in **STAGE 1**. Its sole write is this file. Every same-round cross-wave receipt below (i) **names its substrate** — the sibling file and its mtime at quote time (the table under *Witnesses*) — and (ii) **rides beside the command that produces it**; forward references to **stage-2** siblings (KF-W7 · KF-W8 · KF-W9 · KF-W10) carry the **stable anchor ALONE**, per **LAW C(3)** — no quoted prose, no line numbers. **KF-W0 is the exception the stage order was designed to produce**: it writes FIRST, its commissioned D4-3 landing **landed during this seat's write**, and the receipt is therefore **quote-by-command at its post-landing bytes with its mtime named** (LAW C(2)) rather than anchor-only — *the two-stage law's first working instance in this file.* **This spec certifies nothing about any sibling's final bytes**; the **RECONCILE seat** re-verifies every receipt after the last per-wave write and writes `PASS-4/CLOSE-CERT.md`, **without which the round does not close** (LAW C(4)). ⟨**STAMP CORRECTED at repair round 5 — check `D5-3`.** That certificate is **VOID as proof-of-order** (a per-wave spec wrote after it; **RULINGS-5 R5-2** attributes the write and rules the round-4 **repairs BANK**). **The certification of record for rounds 4 and 5 is `PASS-5/CLOSE-CERT-2.md`**, hash-proven under **LAW E(1)–(2)** — write-last **by construction**, not by assertion — and it, not this seat, is the substrate authority for every cross-wave figure in this file (**LAW E(4)**; §14's posture block states the round-5 form).⟩

| # | authority | change |
|---|---|---|
| 1 | check **D4-1** (MAJOR) | **The reciprocity head-claim is re-measured over the TEN siblings on disk, at all three of its sites in one motion** — §1's head paragraph, **cross-edge 1**, and §1 `:58`'s non-consumer list — with the enumeration and its counting rule stated **at** §1's head and the dependent cells pointing there rather than re-issuing the numeral (LAW D(3)). *"All seven (`KF-W0` · `KF-W2` · `KF-W3` · `KF-W4` · `KF-W7` · `KF-W8` · `KF-W10`)"* is **STRUCK**: `ls …/waves/` → eleven specs, ten siblings, and `KF-W5`/`KF-W6`/`KF-W9` have been on disk since round 2. This was the **D3-4 class (*one file, two verdicts*) reproduced one level up** — seven at the head, **ten** in §9 item 5 and **six non-consumers** in §11 row 10 — sitting in the wave's head sentence, whose own justification is that *"a mail head that measures a subset of its consumers is measuring the wrong thing."* **§9 item 5's heading is re-cut in the same motion** (*"seven not four"* → *"TEN, not seven, not four"*). |
| 2 | check **D4-2** (MAJOR) · **RULINGS-4 LAW D(3)** | **§4c's LAW-A census is re-run, re-partitioned and made self-stable.** (a) The totals are **re-executed at this write's FINAL bytes**, after every other round-4 edit landed — round 3's *"23/17 · 12/7"* were invalidated **by this file's own later edits in the same round** (`:255` and `:269`, both written after the census was taken), which is the failure LAW A exists to force into view. (b) The **SELF-REFERENCE DISCLOSURE** is stated once: the census's pattern matches the census's own instruments **and every conformance check that audits them**, so the raw total is structurally unstable inside a repair round — and **the STABLE DENOMINATOR is declared**: the count of sites that **execute** the path, invariant under any amount of writing *about* it, **TWO**, three rounds running. (c) The non-consumer paragraph is replaced by an **exhaustive, disjoint, nine-class partition that sums to the pasted total by construction** — round 3's *"22 of the 23"* enumerated **21**, one short at its own figures. (d) *"six `V/apotheosis/**` analysis files"* → **SEVEN**, all enumerated by path and line; the seventh sits under `apotheosis/probe/`, outside the `armA/`+`armB/` sweep. **The two-executing-consumer finding is retained whole and re-verified**, including the out-of-bounds memory limb routed to the OWNER — which, being outside `docs/`, **is not a member of the census's own denominator**, now stated rather than left to inference. |
| 3 | check **D4-3** (MAJOR) · **RULINGS-4 §END `KF-W0`** | **C-13's and cross-edge 3's present-tense claim about a sibling's bytes is converted to a declared OBLIGATION.** *"KF.W0 … **which receives** F-1 plus its five component riders"* is **STRUCK**: measured this seat at `KF-W0.md`'s pre-round-4 bytes (mtime **15:43:24**, command pasted at C-13), **zero of the five riders are present** — W0's nine `KeyboardShortcuts` hits are all `kf-KeyboardShortcutsModal`, *a different component*, and its single `KF.W1-DEP` mention carries `kf-DemoGlobalChrome:43`'s **S-9 toast-family** rider, **not** the `:44` four-utility-class rider named here. **The identity travels; the riders did not** — the **D3-8 class one seam further out** (cured *inside* this file at round 3, unswept across the wave boundary). This is load-bearing on the **census axis**: the booking mechanism for all six routed rows is *"routed away to KF.W0"*, so riders absent at the target mean six rows booked into a wave that cannot execute what the booking says it received. **The cure is two-ended and neither end asserts the other's bytes**: this file states the obligation and names the landing surface by **§-heading + row id**; **RULINGS-4 §END has KF-W0 write the five riders verbatim**. **KF-W0 writes FIRST in stage 1 and its landing LANDED during this seat's write** — re-probed at its post-landing bytes (**222,633 B, mtime 16:57:50**), **five of five riders present** where zero were, at its §Carry **`C-1.F` row `F-1`**, whose own text cites **this file's D4-3 directive** as the sending end. **Both ends now name each other, and both dated readings are preserved** (the pre-landing zeros are the record of the defect; the post-landing verification rides beside them — E-3, neither overwrites the other). Riders are carried **with their banked record ids** so the landing is checkable id-for-id. **Final-bytes verification remains the RECONCILE seat's** (LAW B — this spec certifies nothing about W0's final state). |
| 4 | check **D4-4** (MAJOR) · the **C-10 MINT-LAW pattern** | **G-KF1-4 stops commissioning verbatim what C-6's own lock forbids copying.** The GREEN read *"C-6's commissioned sentence **verbatim**"* while C-6 closes *"**Re-measure all three censuses at authoring; copy neither 61 nor 62 nor 81 from here**"* — and under **OP-1** the sacred worktree is *"the wave's single largest liveness risk"*, i.e. the file **expects** that tree to move. If it moves, *"verbatim"* and *"copy nothing"* cannot both hold and **the gate has no reachable GREEN**; if it cannot move, the anti-copy lock is decorative and C-1's ANTI-STALENESS lock is void here. **The gate now commissions the SENTENCE TEMPLATE + a four-part SUBSTRATE-NAMING RULE**, with the five dated measurements **filled at authoring** by re-running §5b's three commands — exactly as **C-10's MINT LAW** commissions `max+1` measured at open rather than a spec-frozen literal. **The round-3 sentence is preserved at C-6 as the dated EXEMPLAR** (E-3; nothing deleted, the banked id `SCH-4` untouched), and **the falsifier gains two legs**: the gate now fails if any numeral is *carried from this spec instead of re-measured*, and fails if the two readings are stated without their two distinct substrates named. **The cure itself is unchanged** — same withdrawal, same honesty, same four payload legs. |
| 5 | check **D4-5** (MAJOR) | **The bare-token zeros are corrected at both sites.** *"The bare-`KF.W1` token is **0** in all six"* (§9 item 5 and §11 row 10) is **STRUCK**: `grep -oE 'KF\.W1([^0-9A-Za-z-]|$)' KF-W9.md` → **1**, in W9's own **`S-10.1`** cross-edge enumeration — written at **15:39**, three minutes before this file closed at 15:42 — where **W9 names KF.W1 in order to declare it NOT an edge**. Substantively the hit **confirms and strengthens** the non-consumer finding (*a declared non-edge is a stronger reciprocation than silence*), and it is carried as such; but the stated measurement was false **on the axis that defines this wave's consumer set**. **`KF-W4`'s token count is restated 2 → 5** and **`KF-W2`'s `O-8` count 1 → 2** (occurrences and lines), both drifted by siblings written after this file at round 3. **The load-bearing claim is unchanged and re-verified**: `grep -c 'O-11'` → **0** in all six, every `O-8` hit a `KF-CO-8`/`KF-CO-1` substring — **zero real O-8/O-11 letter citations across the six**. §11 row 10, a dated record, receives a **`⟨STAMP CORRECTED at repair round 4⟩`** marker and is **not rewritten**. |
| 6 | check **D4-6** (MINOR) | **Cross-edge 9's KF-AV-28 parenthetical is DROPPED and its citation KEPT** (the ruling's own arm), re-pointed at `PASS-4/KF-W1-CHECK.md` §6 D4-6. The struck figures (*"W9 ×11 · W6 ×10 · W7 ×7 · W2/W10 ×5 · W4/W3 ×4 · W8 ×2 · W0 ×1"*) had **lawful provenance** — attributed to the PASS-3 artifact by path and date, which is what LAW B asks — but **every sibling was rewritten in the same round after that artifact was cut and not one of the nine reproduced**, so C-1's ANTI-STALENESS and R2-5 both bite. **The numerals are deliberately NOT re-inscribed**: under LAW C every sibling moves inside a round and four write in stage 2, so a per-sibling numeral written here is false before the round closes. This seat's own dated re-measurement is recorded below as a reading of named bytes, never carried into the operative sentence. **The load-bearing claim strengthens**: KF-AV-28 is live in **nine of ten**, absent only from KF-W5. |
| 7 | check **D4-7** (MINOR) | **The SACRED-CHECKOUT LAW is quoted WHOLE**, with the three things the round-3 citation dropped: the **expiry clause** *"This invariant survives until W2 completes"* (the cited authority is **conditional and self-terminating**, and the citation carried the absolute half); the handoff's own dated tree record **`a59d3a22` / ~254 dirty paths**, set beside this file's independently measured **`8281638c` / 252** — *two records of one sacred tree at different clocks, neither correcting the other*, which is precisely why §5b's substrate is a dated tuple and never a sha; and the **independence declaration** — value.js's read-only posture does **not** depend on the keyframes-side condition (§4's Do-NOT-touch row, the execution gate and G-KF1-12 are its source; the quoted authority is corroboration). **Nothing was at risk** — the wave performs zero writes there — but *a spec that convicts sibling waves for citing a target without its qualifier owes the same discipline to its own most-cited external authority.* |
| 8 | check **D4-8** (MINOR) | **`library-band.md:205` → `:205-206` at every live site** (C-4, G-KF1-6, §4 Bounds' citation-sources row, §9 item 6(c)). The quoted string *"DECLINE — `sampleBezier` (permanently, measured zero demand)"* **straddles two lines**: `:205` ends at the DECLINE verb, `:206` opens the parenthetical — so *"re-read verbatim this seat"*, a **receipt promise**, was one line short of its own quotation. The substance is corroborated independently at **`:173`** (*"D-GAP-6 retired permanently on measured zero demand"*, re-read this seat). |
| 9 | check **D4-9** (INFO) — **superseded by RULINGS-4 LAW C(4)** | **The missing instrument is named and its successor is recorded.** RULINGS-3 §END commissioned a **RE-ANCHOR seat** as LAST writer under `waves/` and made the pass-4 union seat its auditor; **it never ran** — this file closed at **15:42** and **seven siblings were written after it** (W0/W3 15:43 · W4/W5 15:49 · W6/W8 15:50 · W10 15:51 · W2 15:52) — so there was **no certification to audit**, and **D4-1, D4-5 and D4-6 are precisely the same-round sibling drift a LAST-writer sweep exists to catch.** **RULINGS-4 LAW C(4) supersedes it**: the **RECONCILE seat** runs AFTER the last per-wave write, re-verifies **every** cross-wave receipt written this round in both directions, and writes **`PASS-4/CLOSE-CERT.md`** with the eleven-spec mtime table, the receipt-by-receipt commands and a LAW-B certification. **Without it the round does not close**, and the pass-5 union seat audits it against the bytes as its first act. **This spec asserts nothing about that instrument's outcome** (LAW B) — it records the obligation and hands the seat its receipts, which is why every cross-wave figure in this round carries its substrate and its command. ⟨**STAMP CORRECTED at repair round 5 — check `D5-3` (MAJOR), the one-line re-point RULINGS-5 **R5-2**(3) orders.** The pass-5 union seat performed the audit this row commissioned and **falsified the instrument's load-bearing claim**: `PASS-4/CLOSE-CERT.md` certified *proof-of-last-write*, and `KF-W10.md` wrote **after** it. R5-2 **attributes** the twelfth write — it was the RECONCILE seat's own, a receipt-level rewording of an existing dated insertion, benign in content — and **RULES**: round 4's **repairs BANK**, its **close certificate is VOID as proof-of-order**, and **the lawful close of rounds 4 AND 5 is `PASS-5/CLOSE-CERT-2.md`**, hash-proven under **RULINGS-5 LAW E**. **This file's certification citation is re-pointed accordingly** — every "the RECONCILE seat verifies it" promise in this spec now names CLOSE-CERT-5 — and `PASS-4/CLOSE-CERT.md` is **NOT edited** (E-3: prior-pass artifacts are immutable; the attribution of record lives in RULINGS-5, not in a patch to the falsified instrument). **Nothing this row banked is disturbed**: D4-9's finding, its successor instrument and its receipts all stand.⟩ |
| 10 | check **D4-10** (INFO) | **§5b's consequence (2) is narrowed to what its own §6 meets**: *"**every gate leg** names both its repo and its substrate"* → *"**every leg whose reading TURNS ON TREE IDENTITY**"*, with the four members enumerated (**G-KF1-2 · G-KF1-3 · G-KF1-4 · G-KF1-10**, plus the alias block's SCH-1 falsifier) and the six non-members named with the reason each is **not** a defect — an `ls` has no ref, a `find` on the value.js root has no second candidate tree, and the value-side legs bind to *"the current tree bytes"* by declaration. **In a file that struck a round-2 stamp for certifying zero coordinates while carrying five, a universal its own §6 does not meet is the same shape at smaller scale.** |
| 11 | **RULINGS-4 `R4-8`** (ruled arm **(a)**) | **Cross-edge 9's COHESION FLAG is re-recorded from ANSWERED to REGISTERED.** R4-8 read the minting bytes and ruled the three successor formations **REGISTERED SUCCESSOR FORMATIONS — the mints STAND**, naming **this cross-edge and its DISCHARGED flag** as one of the two minting citations of record (the other being `KF-W4 §Sequencing`'s R-15 homing block) and the verb **`MINTED-UNAUTHORED — AUTHORING = the SS-1/SS-2 authoring block`**. The **second end is added ANCHOR-ONLY per LAW C(3)** — `KF-W10 §6.D · SUCCESSOR-FORMATION REGISTER`, no prose, no line — because W10 writes **last** of the per-wave seats. **This wave's own posture is untouched: it homes ZERO of the seventeen**, and §8 says so. |
| 12 | **RULINGS-4 LAW B**, the re-point rule this file wrote for itself | **The census-carriage citations are re-pointed PASS-3 → PASS-4** at the header PROVENANCE and the alias block, stating what the freshest artifact found (**6 / 6 / 0 / 0**, **DEFECTIVE 0·5·3·2**, head **MONOTONE**, all eleven PASS-3 defects **CURED at the bytes**) and preserving the PASS-3 citation as the **dated predecessor** rather than deleting it. The header's own rule — *"when a newer pass supersedes it, this citation is re-pointed, never re-asserted here"* — is executed rather than merely stated. |

**SCOPE RECEIPTS (RULINGS-4 R4-10(1) — tested BY SCOPE, not by instance; one line per class).**
- **LAW A / access column** — *"§Bounds access column: **6** rows enumerated (`modify` ×2 · `create` ×4); rows carrying `delete`/`modify-delete`/`repoint`/`shim`/`move`: **0**; censuses owed **0**, censuses present **0**."* The one census in the file is owed by a **declared act** (the mail-path repoint of C-9/G-KF1-8), not by the access column, and is at §4c. Receipt written into §4c itself.
- **SUBJECT-IDENTITY** — *"symbol-bearing §Bounds rows: **0** (all six rows are markdown documents); gate-witness symbols: **11**, enumerated at §4c, **each carried with file AND line at a named substrate**; bare leaves: **0**."*
- **NUMERIC ARM (LAW D(3))** — *"every pasted command re-run at write time: §4c's three censuses (re-run **last**, after all other round-4 edits), the ten sibling token counts, the six non-consumer `O-8`/`O-11` counts, the KF-W0 rider probe, the ten-sibling KF-AV-28 census, the sacred-checkout tuple, the exec-clone HEAD, and the two external quotations (`EXECUTION-HANDOFF.md:13-18`, `library-band.md:205-206`). Figures inherited from a prior round or a ruling: **0**."*

**Witnesses re-derived at this repair (R2-5 + LAW D(3)).** Each by a read-only command at write time.

**(i) Keyframes-side, read-only, both trees.** Exec clone `keyframes-v-exec`: `git rev-parse HEAD` → **`81a56990736ced5b5edde0b84c527680ac7689b1`**, `git status --porcelain | wc -l` → **0** — the frontier of record, unchanged ✓. Sacred checkout `/Users/mkbabb/Programming/keyframes.js`: `git rev-parse HEAD` → **`8281638c`** ✓ · `git rev-list --left-right --count origin/master...master` → **`41  1`** ✓ · `git status --porcelain | wc -l` → **252** ✓ · both packet bodies present at **7,064 B** (Jul 24 16:41) and **15,633 B** (Jul 27 12:27), listed by `ls -l` in the coordination directory ✓ · `sed -n '13,18p' docs/tranches/V/EXECUTION-HANDOFF.md` → the SACRED-CHECKOUT block **quoted whole**, including *"This invariant survives until W2 completes"* and its `a59d3a22` / ~254 record ✓. **Zero writes, zero git mutations, zero npm in either tree.**

**(ii) Value-side, current tree bytes.** §4c's three censuses, **re-run last** ✓ · `library-band.md:205-206` and the corroborating `:173` ✓ · the memory limb `~/.claude/projects/-Users-mkbabb-Programming-value-js/memory/feedback-mail-inbox-law.md:16`, still naming the frozen keyframes path among its four landing paths — **the out-of-bounds executing consumer, unchanged and still routed to the OWNER** ✓.

**(iii) Sibling specs — THE SUBSTRATE TABLE (LAW C(2)).** Every same-round cross-wave figure in this file was measured against exactly these bytes, at **2026-08-28 16:44 EDT**; **stage-2 siblings are marked, and receipts to them are anchor-only (LAW C(3))**: ⟨**STAMP CORRECTED at repair round 5 — check `D5-6` (MINOR), and the class RETIRED by RULINGS-5 LAW E(4).** **This table is no longer any figure's stamp authority.** Measured at pass 5: **nine of its ten rows stamp the round-3 COMMITTED bytes** (`git show HEAD:…` reproduces them byte-for-byte), and **three stage-1 peers — KF-W3 · KF-W4 · KF-W6 — had already published round-4 bytes before this seat wrote** and were stamped at superseded ones anyway; only the KF-W0 row was re-read at both clocks (and its first reading was one write early, caught and cured at **C-13**). **Nothing in the table is load-bearing** — no gate, row, lock or disposition rests on any figure in it, and the two that could have mattered (KF-W4's token count, KF-W6's token/`O-8`/`O-11` counts) **re-measure IDENTICAL at final bytes**. The mechanism is the one RULINGS-5 **LAW E(4)** retires program-wide: **seats stamp clocks they cannot keep** (nine of the drift tail's sixteen rows). **The stamp authority of record is `PASS-5/CLOSE-CERT-2.md`'s substrate table**, derived after the last per-wave write and hash-bracketed against `RULINGS-5` LAW E(3)'s opening table; **the local clocks below are struck as authority and preserved as the dated round-4 record** (E-3 — §13 is not rewritten). Per-seat substrate stamping is not repeated at round 5: §14 carries **anchors, not clocks**. **SHADOW (LAW F(1))**: struck set = {the ten substrate stamps in this table, each row re-read at its own bytes before striking (LAW F(3))}; **disposition**: struck **as authority**, preserved **as record**, authority re-pointed to CLOSE-CERT-5; **minted set = ∅**; **termini notified** = {RECONCILE ⟨the substrate table of record⟩ · every cell in this file that named this table as its substrate — §1's SIX-non-consumers bullet and §9 item 5, both re-cut this round}; enumerated at **§14**.⟩

| sibling | bytes | mtime at quote time | stage | this file's receipts to it |
|---|--:|---|---|---|
| `KF-W0.md` | 188,714 **→ 222,633** | 15:43:24 **→ 16:57:50** | 1 (**writes first; landed during this write**) | rider probe **at BOTH clocks** (C-13: 0-of-5 pre-landing · **5-of-5 post-landing**, each with its own command) · reverse-edge conflict · packet-roster |
| `KF-W2.md` | 215,194 | 15:52:57 | 1 | `O-8` ×2 / `O-11` 0 · §Excluded rows |
| `KF-W3.md` | 138,765 | 15:43:45 | 1 | OP-5 · L-4 · §Cross-edges row |
| `KF-W4.md` | 169,260 | 15:49:43 | 1 | OP-5 · §Sequencing row · token ×5 |
| `KF-W5.md` | 209,901 | 15:49:32 | 1 | token 0 · `O-11` 0 · **KF-AV-28 absent (the one of ten)** |
| `KF-W6.md` | 181,977 | 15:50:46 | 1 | token 0 · `O-11` 0 |
| `KF-W7.md` | 174,104 | 15:39:04 | **2** | §Excluded row + N-10 — **anchor-only** |
| `KF-W8.md` | 144,190 | 15:50:14 | **2** | token 0 · `O-11` 0 — **anchor-only** |
| `KF-W9.md` | 177,670 | 15:39:31 | **2** | **token 1 — the declared NON-EDGE**, `S-10.1` — **anchor-only** |
| `KF-W10.md` | 155,227 | 15:51:10 | **2 (writes last)** | three hard-blocking anchors · **§6.D register — anchor-only** |

**Dated reading, recorded here and deliberately NOT carried into any operative sentence (D4-6's cure).** KF-AV-28 across the ten siblings at the bytes above — **the numerals are STRUCK at repair round 5** ⟨**check `D5-7` (MINOR); the R4-11/D4-6 arm applied a second time — drop the numerals, keep the citation, one edit.** The struck reading was *"W0 6/3 · W2 10/7 · W3 5/5 · W4 6/5 · **W5 0/0** · W6 13/12 · W7 13/12 · W8 4/2 · W9 15/12 · W10 6/5"*. It was **lawful in form** — declared a dated mid-round reading, load-bearing nowhere — and **seven of its ten pairs no longer reproduce**, which is the whole argument against re-inscribing them: this file's own `D5-6` conviction shows the table those bytes were named in was *itself* stamped at superseded clocks, so a dated reading was dated against a clock that never existed. **The numerals are not re-issued.**⟩ **What is recorded is the property, which reproduces**: KF-AV-28 is **LIVE IN NINE OF TEN siblings and ABSENT ONLY FROM KF-W5** — re-verified this seat at the current bytes and independently at `PASS-5/KF-W1-CHECK.md` §6 `D5-7`, re-verified again at `PASS-5/CLOSE-CERT-2.md`. **No gate, lock or cross-edge in this file depends on it**; cross-edge 9's transit roster names `KF-AV-28` by **id**, never by count. **SHADOW (LAW F(1))**: struck set = {the ten KF-AV-28 numeral pairs}; **disposition**: retired to the nine-of-ten property, which is re-verified at both ends; **minted set = ∅**; **termini notified** = {cross-edge 9 ⟨its transit roster, which cites the id and whose authority is re-pointed to the pass-5 check this round⟩ · RECONCILE}; enumerated at **§14**.

**Nothing above is inherited from a ruling, a check, a CARRY, a lane doc, or this file's own prior text** — RULINGS-4's own case facts were re-derived here before being written, as R2-5 requires of every seat, and the one place this seat's measurement extends beyond the rulings' text (the `KF-W4` token count 2 → **5**) is recorded as a measurement, not as a correction to any ruling.

**Still standing, unchanged and deliberately so.** Every **lock** is carried verbatim; **C-6's banked id `SCH-4` is untouched** and only the *status* of its commissioned sentence changed (commissioned-verbatim → **commissioned TEMPLATE + preserved dated exemplar**), which adds a reachable GREEN and removes nothing. Every **dissent is carried, not resolved**: the `a59d3a22` rider (KF.W0's re-measurement — and note that `a59d3a22` is *also* the sha the sacred handoff names for that tree, now recorded at the SACRED-CHECKOUT LAW) · the **KF-W0 reverse-edge conflict** (recorded for the COHESION §3 check and the owner — neither spec rewrites the other) · C-4's recorded asymmetry. The **∅-column finding** stands as a finding, and **no row, gate or bounds item was manufactured to fill it** (C-14's L-19 lock). **L-19 holds affirmatively**: no gate is a script, no phantom command is cited, the mail tooling a lesser spec would mint is excluded by name, and the two non-gates stay labelled rather than counted. **Status stays `planned`; SPECIFIED stays YES; IMPLEMENTED and VERIFIED stay NO; no product source is opened in either repo; the sacred checkout is unchanged; no sibling spec was written by this seat.**

*(§13 is a **dated round-4 record**, treated as §10, §11 and §12 are: it is not rewritten at round 5. Where one of its certifications has since been falsified, a bracketed `⟨STAMP CORRECTED at repair round 5: …⟩` marker stands beside the original wording, which is preserved. Every substantive correction round 5 makes is recorded in §14 below, never by editing this table.)*

---

## 14. Repair round 5 (X·KF L-20 round 5) — what changed and on whose authority

Recorded, not silently applied, on the same law §9, §10, §11, §12 and §13 state. **Inputs**: `conformance/PASS-5/RULINGS-5.md` (cross-wave authority — **LAW E** the hash-proven cert law, **LAW F** the shadow law (mechanism S), **R5-2** the round-4 attribution, **R5-9** the drift tail (this wave owns rows **1–5**), **R5-11** the scope law's sixth issue, and the §END directive map, which hands this seat **seven** directives; where a local defect and a ruling collide, **the ruling wins** — except on a *measurement*, where **R2-5** keeps the tree above the ruling's prose) and `conformance/PASS-5/KF-W1-CHECK.md` (local register: **DEFECTIVE — 0 BLOCKER · 5 MAJOR · 3 MINOR · 2 INFO**; census **CLEAN** at 6 routed / 6 booked / 0 escaped; **0 inventions**; **all ten PASS-4 local defects verified CURED at the bytes**; every keyframes-side witness byte-exact at `origin/master 81a56990`).

**No carry row was added, renamed, split or dropped: C-1..C-14 stand, ids untouched (anti-rename). Net row delta 0.** **No gate was added or removed**: the tally stays **10 born-RED · 1 DECLARED-SATISFIED · 1 stay-GREEN INVARIANT**, every born-RED gate keeps its reachable GREEN, and every GREEN still lands inside §4's six bounds rows. **No row, bounds item, cross-edge, direction or force moved**: every round-5 act is editorial — a citation re-pointed, a numeral retired, a quotation reduced to the anchor beneath it. Nothing here stamps a verb, authorizes execution, or opens product source; every probe was a read-only `git` / `grep` / `sed` / `ls` command, and the sacred checkout was read and never written.

**THE ROUND-5 SEAT'S OWN POSTURE (RULINGS-5 LAW E).** This seat writes in **STAGE 1**. Its sole write is this file. **Per-seat substrate stamping is RETIRED** per **LAW E(4)**: this round adds **no mtime table, no per-seat clock, and no line number on any cross-wave receipt**. Every same-round cross-wave receipt written here is **ANCHOR-ONLY** — §-heading + row/gate id, no quoted sibling prose — and **the RECONCILE seat alone** verifies them at final bytes and appends the dated quote-by-command receipts. **The certification of record is `PASS-5/CLOSE-CERT-2.md`**: write-last **by construction** rather than by assertion, hash-bracketed against `RULINGS-5` **LAW E(3)**'s opening table (this file opened the round at **200,681 B**), and re-hashed by the pass-6 union seat as its first act. **`PASS-4/CLOSE-CERT.md` is VOID as proof-of-order and is NOT edited** (E-3, prior-pass artifacts immutable); its sixteen repairs bank, and the attribution of its falsifier lives at **RULINGS-5 R5-2**, not in a patch to it.

| # | authority | change |
|---|---|---|
| 1 | check **D5-1** (MAJOR) · **R5-14 §END KF-W1** | **The bare-`KF.W1` zero is corrected at every live site, and the per-sibling zeros are RETIRED as the load-bearing form.** *"**0** in **FIVE** (KF-W2 · KF-W5 · KF-W6 · **KF-W7** · KF-W8)"* is **STRUCK** at its three live sites — **§1's SIX-non-consumers bullet · cross-edge 1 · §9 item 5** — and restated **0 in FOUR, 1 each in KF-W7 and KF-W9, both non-edges**. KF-W7's hit is at **KF-W7 §LAW C SUBSTRATE DISCLOSURE, the `BACKWARD (stage-1 siblings …)` bullet** — a **stage-roster listing**, arriving with W7's stage-2 write, *after* this file's round-4 write: **D4-5's shape one sibling over**, on the one axis that defines this wave's consumer set. The **D4-5 idiom is applied** (count + named non-edge), and then **carried past it**, because a per-sibling numeral written by a stage-1 seat is false before the round closes: **what now carries is the property no sibling write can falsify** — `O-11` = **0** across the six and **every** `O-8` hit a `KF-CO-8`/`KF-CO-1` substring, i.e. **zero real O-8/O-11 letter citations across the non-consumer set**, which is the claim the COHESION §3.1 mark actually turns on. ⟨**Correction of record to the check, stated not silently applied (E-3)**: `PASS-5/KF-W1-CHECK.md` §6 D5-1 names a **fourth** site, §13's substrate table *"`:563`, token 0 against KF-W7"*. **It does not reproduce**: at the bytes the `KF-W7.md` row of that table carries **receipts, not counts** (*"§Excluded row + N-10 — anchor-only"*); the row carrying *"token 0"* is **`KF-W8.md`'s**, and W8's zero is **true**. The live sites were **three**, and all three are cured; the table itself is struck as an authority at row 6 below.⟩ |
| 2 | check **D5-2** (MAJOR) · **R5-9 row 1** · **R5-11** | **The census-carriage citation class is swept WHOLE, in one act, and made greppable.** Round 4 re-pointed **two** of **six** LAW-B sites and left three live citations at PASS-3 — one of them calling PASS-3 *"the freshest artifact"* — so one file named **two** freshest artifacts for **one** quantity (**6 / 6 / 0 / 0**): the **D3-4 class in citation form**, and **R4-10(1) failing on the round that wrote it**. **One artifact of record — `PASS-5/KF-W1-CHECK.md`** — now stands at all six: header PROVENANCE · alias block · the ROWS-vs-RECORDS corroboration · §5's preamble · C-14 · §11 row 7's close stamp · §11's *"Still standing"* paragraph (the two §11 sites by `⟨STAMP CORRECTED at repair round 5⟩` marker, since §11 is a dated round-2 record and is never rewritten). PASS-4 and PASS-3 are **preserved as dated predecessors**, each having found the identical census. **The class enters the scope receipt with its command, its enumeration and its counting rule**, so a future round cannot sweep a subset and call it closed. |
| 3 | check **D5-3** (MAJOR) · **R5-2(3)** | **The certification citation is re-pointed to `PASS-5/CLOSE-CERT-2.md` + R5-2, at both sites that condition anything on the instrument** (§13's LAW-C posture block and §13 row 9), and the ruling's attribution is recorded: the twelfth write was the **RECONCILE seat's own** — a receipt-level rewording of an existing dated insertion, **+21 B, 22 s after its own certification** — so *"this seat wrote last"* was **asserted, not constructed**. **Round 4's repairs BANK; its certificate is VOID as proof-of-order; the lawful close of rounds 4 and 5 is CLOSE-CERT-5.** **Not this file's defect to cure and not cured here** — the owner is the RECONCILE seat; what lands here is the **one-line re-point** the ruling orders, plus the deletion of this file's dependence on a per-seat clock (row 6). |
| 4 | check **D5-4** (MAJOR) · **R5-9 row 2** | **The three KF-W10 receipt figures are RETIRED to the property they supported** (`D4-6`'s arm, applied a second time). *"`grep -c 'O-8' KF-W10.md` → **8**, `grep -c 'O-11' KF-W10.md` → **3**"* is **STRUCK** at **§1's KF-W10 bullet**, at **cross-edge 11**, and in **§9 item 5**; the four declared consumers' token counts go with them. Two aggravations made these defects rather than dated readings: the §1 bullet carried **no substrate stamp and no command substrate at all**, so the figures read as current, and §13's NUMERIC-ARM receipt enumerated eight classes **without this pair** — true over what it listed, silent over what it omitted. **The re-measurement moves both figures UP**, so *"the hardest downstream consumer this wave has"* is **strengthened**; what is written now is the property — **KF-W10 cites both letters by id, repeatedly, at declared anchors** — which no sibling write can falsify. Under **LAW E(4)** any figure that must ride rides **CLOSE-CERT-5**. |
| 5 | check **D5-5** (MAJOR) · **R5-9 row 3** | **The three quoted-prose receipts to KF-W10 are STRUCK to the anchors beneath them** — the cure is **subtraction**. Each was verbatim at W10's round-3 bytes and is a **truncation** at its later bytes (the `Opens after` parenthesis continues past the quoted paren; `OP-3`'s bolded clause continues; the `B. → KF.W1` sentence carries **two unmarked elisions**) — and **all three drifts are W10 naming this wave's outbound row `O-21` by id**, i.e. drift *in this wave's favour*. They should never have carried prose: **§13's own posture block declares stage-2 receipts anchor-only (LAW C(3))**, so the file was in breach of its own declaration, and **LAW D(1)** convicts a verbatim label that no longer byte-diffs. Struck; the §-anchors kept; `OP-3`'s and `B.`'s force retained as **labelled paraphrases** (LAW D(1)). **The last surviving line coordinate on this file's cross-spec receipts (`:20`) is retired with them** under **LAW E(4)** — the anchor-cleanliness axis now holds at **zero coordinates by construction**, not by a promise. |
| 6 | check **D5-6** (MINOR) · **R5-9 row 4** · **LAW E(4)** | **§13's substrate table is STRUCK as the stamp authority; the stamp authority is CLOSE-CERT-5's substrate table.** Nine of its ten rows stamp the **round-3 committed** bytes, and **three stage-1 peers (KF-W3 · KF-W4 · KF-W6) were final and available and were stamped at superseded bytes anyway**. Nothing in it is load-bearing and the two figures that could have mattered re-measure **identical** at final bytes — *nothing is wrong except the stamps*, which is exactly why the class dies rather than being refreshed: **seats stamp clocks they cannot keep** (nine of the drift tail's sixteen rows). The table is **preserved as the dated round-4 record** (E-3) with the correction beside it; **round 5 adds no table of its own**. |
| 7 | check **D5-7** (MINOR) · **R5-9 row 5** | **§13's dated KF-AV-28 reading loses its numerals and keeps its citation** (`R4-11`/`D4-6`, one edit). Seven of the ten pairs no longer reproduce; the cell was lawful in form — declared a dated mid-round reading, load-bearing nowhere — but a dated reading whose own substrate table is stale (row 6) is **dated against a clock that never existed**. **The property is re-verified and kept**: KF-AV-28 is **live in nine of ten siblings, absent only from KF-W5** — measured this seat and independently at the pass-5 check — and cross-edge 9's transit roster names it **by id**, never by count. |
| 8 | check **D5-9** (INFO) | **The pass-4 register's escape-sweep figure is corrected as a dated observation, at C-14** — `O-11` → **one** record, not two, re-derived over the 58-record corpus with every hit printed and read. The registry is **READ-ONLY and unchanged**, so this is a mis-transcription at that seat, **not drift**; **nothing follows for the census** (every hit under both tokens is a `KF-CO-*` substring, so **0 escaped** is untouched). It is recorded **here** because `PASS-n` checks are never edited. |
| 9 | check **D5-10** (INFO) | **The filename-reconciliation block's round-2 byte figure is marked as the dated observation it is** — `65,138 B` was *that* repair's open; this file opened round 5 at **200,681 B** per **RULINGS-5 LAW E(3)**'s hash-bracketed table (an immutable frozen source, **not** a per-seat stamp), and its closing bytes are printed at CLOSE-CERT-5. The load-bearing halves (`KF-W1.md` PRESENT · `W1.md` ABSENT) are unchanged. The twin instance of the same figure at `KF-W10.md`'s `OP-5` cell is **W10's end and is not edited from here** — named, not touched. |
| 10 | **§4c's own SELF-REFERENCE DISCLOSURE** · **LAW E(4)** | **The LAW-A census's class H took its third member, exactly as the class's stated rule predicts, and the raw total is retired rather than re-issued.** With `PASS-5/KF-W1-CHECK.md` on disk, census (1) reads **27 / 19** and census (1b) **16 / 9**, both still summing by construction (`1+4+2+7+1+1+3+3+5 = 27`; `5+4+3+4 = 16`). The round-4 figures are preserved as the dated readings they were. **This is not a defect and is not treated as one** — it is the disclosure working — but it is recorded because a numeral that moves whenever the census is audited must not sit in this file as an operative figure. **The STABLE DENOMINATOR is unchanged at TWO** (`INBOX.md:15-16` in bounds; the memory limb out of bounds, routed to the OWNER, re-verified present this seat), and it is the only figure any act here depends on. |

**THE ROUND-5 SHADOW LEDGER (RULINGS-5 LAW F(1)–(3), mechanism S — every act that strikes, moves, drops, re-keys, narrows, re-points or mints books its shadow in the same edit; the ledger enumerates the round's whole set so the RECONCILE seat can re-sweep every terminus).**

- **STRUCK — quotations (5 acts, 3 ids)**: {KF-W10 §1 State `Opens after` ⟨banked anchor: §-heading + `Opens after` label⟩ · KF-W10 §Preconditions `OP-3` ⟨§-heading + row id⟩ · KF-W10 §Sequencing `B. → KF.W1 (Mail Cure, SS-2)` ⟨§-heading + cross-edge label⟩}, struck at **both** their sites (§1's KF-W10 bullet and cross-edge 11). **Disposition per id**: each row's **own banked anchor was re-read first and is KEPT** (LAW F(3) — per row, never per carrier); `OP-3` and `B.` retain their force as **labelled paraphrases**; **zero edges, directions or forces change**.
- **STRUCK — numerals (6 acts)**: {KF-W10 `O-8` count · KF-W10 `O-11` count · the four declared-consumer token counts · the twelve round-4 per-sibling `O-8` numerals · the ten KF-AV-28 pairs · §13's ten substrate stamps}. **Disposition**: **retired to the properties they supported**, each of which is stated at the site and re-verified at CLOSE-CERT-5; **no figure is re-inscribed at a clock this seat cannot keep**.
- **RETIRED — coordinates (1 act)**: {KF-W10 §1 `Opens after` `:20`} — the last line coordinate on this file's cross-spec receipts, retired under LAW E(4).
- **RE-POINTED — citations (10 acts)**: {the **eight** LAW-B carriage sites → `PASS-5/KF-W1-CHECK.md` — two more than the six §12 row 6 enumerated, because the class is swept by **what a citation does**, not by an earlier round's list} + {the two certification sites → `PASS-5/CLOSE-CERT-2.md` + R5-2}.
- **MINTED**: **NOTHING.** No id, row, gate, bounds item, routing, edge or terminus was minted, re-keyed, narrowed or dropped this round; `C-1..C-14` and `G-KF1-1..12` are untouched, and **the ∅ column is still a finding, still unfilled** (C-14's **L-19** lock).
- **CITING / TERMINUS WAVES NOTIFIED**: **KF-W10** (all three struck quotations are its rows; the far ends are cured at its own seat per RULINGS-5 §END KF-W10 — this file changes nothing at W10 and asks nothing new of it) · **KF-W7** (named as the D5-1 non-edge at its `§LAW C SUBSTRATE DISCLOSURE` anchor; **no obligation is created** — the listing is W7's own and correct) · **KF-W9** (its `S-10.1` declared non-edge, unchanged) · **KF-W0** (unchanged; the D4-3 landing at its `C-1.F` row `F-1` was verified 5-of-5 at pass 5 and is not re-opened) · **RECONCILE** (`PASS-5/CLOSE-CERT-2.md` — every anchor above, the substrate table, and the closing hash).

**SCOPE RECEIPTS (RULINGS-5 R5-11 — RECEIPT-ARITHMETIC-FIRST: each receipt carries its enumeration, its counting rule and its command AT the receipt; a receipt whose own arithmetic does not reproduce is a defect regardless of the scope's truth).**

- **CARRIAGE-CITATION class (new this round, the `D5-2` cure's instrument).** **Command**: `grep -n 'PASS-[0-9]/KF-W1-CHECK' docs/tranches/X/keyframes/waves/KF-W1.md`. **Counting rule**: one *line* carrying one or more citations = one hit; a line citing two artifacts (a re-point beside its preserved predecessor) is **one** hit, and the enumeration below classifies **by what the citation DOES**, not by which pass it names. **Enumeration, exhaustive and disjoint** — (a) **CARRIAGE / LAW-B sites — EIGHT, every one re-pointed to `PASS-5` this round**: header PROVENANCE · alias block · the ROWS-vs-RECORDS corroboration · §5's preamble · C-14 · **cross-edge 9's KF-AV-28 property cite** · §11 row 7's close stamp · §11's *"Still standing"* paragraph ⟨**eight named sites where §12 row 6 enumerated six — the discrepancy is stated, not reconciled away**: that cell counts the two §11 sites as a single *close stamp*, and it never named the ROWS-vs-RECORDS corroboration or cross-edge 9's property cite, both of which cite a check as a **live** authority. **The class is defined by what a citation DOES, not by an earlier round's list** — sweeping the earlier list is exactly how round 4 left three live citations standing⟩; (b) **dated-record references ANYWHERE IN THIS FILE** — 15 members at `:369 :430 :451 :484 :493 :495 :521 :534 :568 :580 :588 :589 :597 :604 :622`, of which 8 lie in §10–§13, **one in §7** (`:369`, cross-edge 2's round-3 correction block) and **six in §14** (the round-5 record itself); the round-5 spelling ~~"inside §10–§13"~~ is struck at repair round 6 per PASS-6 D6-6, under which a seat re-running the receipt from its stated rule got 8 and the partition `8 + 15 + 2 + 1 = 26` stopped summing — which name a pass artifact as **that round's input or that round's finding** — **frozen by E-3, never re-pointed**; (c) **census class-H members**, which cite pass checks as **census hits**, not as authorities; (d) **THIS RECEIPT ITSELF** — the line being read carries the class's own command spelling and the freshest artifact's name, so **the receipt is a member of the class it enumerates**, exactly as §4c's class H is a member of the census it partitions. Declared, not hidden: a receipt that silently excluded itself would be a scope claim measured over a set it had quietly narrowed. **The rule that closes the class**: *every hit in (a) cites the freshest artifact; no hit in (b), (c) or (d) is ever re-pointed.* **Measured at this write's final bytes, the command re-run LAST: 26 lines carrying 45 citations — class (a) 8 · (b) 15 · (c) 2 · (d) 1, and `8 + 15 + 2 + 1 = 26` is the check.** `grep -oE 'PASS-5/KF-W1-CHECK' … | wc -l` → **20**: the freshest artifact is named at every (a) site, at each site's round-5 correction marker, and here. **The arithmetic reproduces before the scope is tested, per R5-11(2).**
- **LAW A / access column (unchanged, re-verified BY SCOPE this seat).** §Bounds access column enumerated whole: **6 FILES on 5 table rows — `modify` ×2 · `create` ×4** ⟨***counting rule, stated AT the receipt per R5-11(1) and added at repair round 6 (PASS-6 D6-7): one bounds FILE = one unit; the §Bounds table `:98-105` carries FIVE data rows, and row 5 names TWO files (`…parser-totality-exposure.md` · `…library-band-r1-widened-k1-k4.md`); row 1's superseded `create` grant is explicitly SPENT and is a struck grant, not a live access word, so a bare `grep -oE '\*\*(modify|create)\*\*'` over the table over-counts it. The round-5 spelling ~~"6 rows"~~ is struck: it was a FILE count wearing the word ROWS.***⟩; rows carrying `delete` / `modify-delete` / `repoint` / `shim` / `move`: **0**; LAW-A censuses **owed 0, present 0**. The one census in the file is owed by a **declared act** (C-9/G-KF1-8's mail-path repoint) and lives at §4c. **This round opened no bounds row and changed no access word.**
- **SUBJECT-IDENTITY (unchanged, re-verified).** Symbol-bearing §Bounds rows: **0** (all six rows are markdown documents). Gate-witness symbols: **11**, enumerated at §4c, each carried with **file AND line at a named substrate**. Bare leaves: **0**.
- **NUMERIC ARM, RE-CUT UNDER LAW E(4).** Round 4's arm promised *"every pasted command re-run at write time"* and was **silent over the two classes that failed** (the KF-W10 pair; the substrate stamps) — silence is how a scope receipt stays true while its scope rots. **The round-5 arm is a rule, not a list**: *this seat inscribes **no** numeral measured over a sibling that writes after it, and **no** clock of its own; every same-round cross-wave figure retired this round is enumerated in the SHADOW ledger above; any figure that must ride rides `PASS-5/CLOSE-CERT-2.md`.* **Numerals inscribed by this round over stage-2 siblings: 0. Per-seat mtime stamps written this round: 0. Line numbers written on cross-wave receipts this round: 0.**
- **STAGE-2 RECEIPT class (LAW C(3) + LAW E(4)), enumerated so the RECONCILE seat's stage-2 sweep has this end's list.** **Counting rule**: one distinct named §-anchor = one receipt, regardless of how many times this file cites it. **KF-W7 — 3** (§Excluded row `KF-CO-1/KF-CO-8` · the `N-10` SPEC-INPUT paragraph · §LAW C SUBSTRATE DISCLOSURE's `BACKWARD` bullet) · **KF-W8 — 0** (property only, no anchor) · **KF-W9 — 1** (§Carry `S-10.1`) · **KF-W10 — 4** (§1 State `Opens after` · §Preconditions `OP-3` · §Sequencing `B. → KF.W1 (Mail Cure, SS-2)` · §6.D SUCCESSOR-FORMATION REGISTER) = **EIGHT anchors, 0 quotations, 0 line numbers, 0 stamps.**

**Witnesses re-derived at this repair (R2-5 + LAW E(4) — anchors, not clocks).** Each by a read-only command at write time; **no measurement is inherited from a ruling, a check, a CARRY, a lane doc, or this file's own prior text**, and RULINGS-5's own case facts were re-derived here before being written.

**(i) Sibling-spec measurements, value-side tree.** The bare-`KF.W1` token over the six non-consumers (`grep -oE 'KF\.W1([^0-9A-Za-z-]|$)'`) → **0 · 0 · 0 · 0** for KF-W2 / KF-W5 / KF-W6 / KF-W8 and **1 · 1** for KF-W7 / KF-W9, **the two hits read and classified individually** (W7's = its stage-1-sibling roster listing; W9's = its `S-10.1` declared non-edge) ⟨**RECONCILE SEAT, 2026-08-28 — THIS LEG IS THE ONE THAT MOVED: at final bytes the vector is `7 · 0 · 0 · 0` and `1 · 1` (`PASS-5/CLOSE-CERT-2.md` §4 · R5-R1).** KF-W2's seven are its own round-5 `D-13` relay-vehicle attributions (`KF.W1's outbound row`), read and classified as **non-edges** like the other two. **The remaining three legs of this receipt were re-run at final bytes by the RECONCILE seat and every one reproduces exactly as printed** — `O-11` → 0 in all six ✓ · all fifteen `O-8` hits `KF-CO-8`/`KF-CO-1` substrings ✓ · KF-AV-28 live in nine, absent only from KF-W5 ✓ · all eight stage-2 anchors resolve ✓. **One leg of four moved, and it moved on a stage-1 peer** — LAW E(5)(iii)'s class, booked at the cert.⟩ ✓ · `grep -c 'O-11'` → **0** in all six, and **every** `O-8` hit across the six read and confirmed a `KF-CO-8`/`KF-CO-1` substring ✓ · KF-AV-28 over the ten siblings → **live in nine, absent only from KF-W5** ✓ · the four stage-2 anchor sets present and resolving (`S-10.1` · `§6.D` · `OP-3` · `Opens after`) ✓. **These are the round's only sibling measurements, and none of them is inscribed as a per-sibling numeral.**

**(ii) Corpus, the 58 adjudicated records (read-only, unchanged).** `ls kf-*.md | wc -l` → **58** ✓ · `grep -l 'O-11' kf-*.md` → **1** record · `grep -l 'O-8' kf-*.md` → **2** ✓ · every hit under both tokens printed with its preceding characters and read: **all `KF-CO-*` substrings, none a letter citation** ✓ — the `D5-9` correction, and the **0 escaped** verdict untouched.

**(iii) Value-side, current tree bytes.** §4c's two censuses re-run **last**, after every other round-5 edit landed — **27 / 19** and **16 / 9**, class H at **3** by its own rule ✓ · the STABLE DENOMINATOR re-verified at **TWO**, the out-of-bounds memory limb still naming the frozen keyframes path among its four landing paths and **still routed to the OWNER, edited by nobody here** ✓ · `PASS-5/KF-W1-CHECK.md` and `PASS-5/RULINGS-5.md` present at `docs/tranches/X/keyframes/conformance/PASS-5/` ✓ · this file's carriage-citation class re-grepped at final bytes and classified hit by hit ✓.

**(iv) Keyframes-side.** **NONE — and that is the round's correct posture.** No round-5 defect is a keyframes-side measurement: the pass-5 check re-derived **every** keyframes-side witness byte-exact at `origin/master 81a56990` (§5a row for row, §5b's three censuses **61 / 81 / 62**, the glass-ui triple, the nine-line `parseStylesheet` output, the 9-row `INBOUND-LEDGER`, the sacred tuple), so re-running them here would inherit nothing and prove nothing. **The frontier of record is unchanged and the sacred checkout was not opened this round.**

**Still standing, unchanged and deliberately so.** Every **lock** is carried verbatim (C-1's *"Amend, do not re-send"* + ANTI-STALENESS · C-3's HARD NEGATIVE · C-6's LAW-B substrate lock · C-7 · C-8 · C-9 · C-10's MINT LAW · C-11 · C-12's HARD NON-GATING · C-14's L-19), and **round 5 adds none** — the round's whole motion is subtraction. Every **dissent is carried, not resolved**: the `a59d3a22` rider (KF.W0's re-measurement) · the **KF-W0 reverse-edge conflict** (recorded for the COHESION §3 check and the owner — neither spec rewrites the other) · C-4's recorded asymmetry. The **∅-column finding** stands as a finding, and **no row, gate or bounds item was manufactured to fill it**. **L-19 holds affirmatively**: no gate is a script, no phantom command is cited, the mail tooling a lesser spec would mint is excluded by name, and the two non-gates stay labelled rather than counted. **`O-21` is still `max+1` measured at open, governed by C-10's MINT LAW rather than by the literal.** **Status stays `planned`; SPECIFIED stays YES; IMPLEMENTED and VERIFIED stay NO; no product source is opened in either repo; the sacred checkout is unchanged; no sibling spec, no registry record and no prior-pass conformance artifact was written by this seat.**
