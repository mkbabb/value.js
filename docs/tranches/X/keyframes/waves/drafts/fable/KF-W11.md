SERVED MODEL: claude-fable-5-1

# KF.W11 — Demo Scene Repair

**Wave**: KF.W11 · Demo Scene Repair — the NINE NO-WAVE-OWNER scene packets of RULINGS R-15's canonical seventeen (`KF-W10.md §6.D` row 1; homed at `KF-W4 §Sequencing`, the R-15 homing block; rostered at `KF-W1 §Cross-edges` edge 9): **cube · sequence · spring · spring-plot · spring-physics-facet · spring-artifact-truth · square · amiga · drag-seam**. Repair, not evaluate: every packet's cure shape is already ruled at its home record and this file HOMES and SEQUENCES it — it re-derives no ruling, mints no id, and books nothing a sibling wave already owns.
**Half**: frontend (keyframes.js demo). **Sub-tranche**: X·KF. **Track**: B.
**Status**: planned. No product byte is written by this file; authoring it opened no product source for writing; execution awaits the sequencing head (§State).
**Ref of record**: keyframes.js `origin/master` **`69095552`** — ⟨cmd⟩ `git -C /Users/mkbabb/Programming/keyframes.js rev-parse --short origin/master` → `69095552`; `git log -1 --format='%h %ad %s' --date=short origin/master` → `69095552 2026-09-18 test(kf/dialog-pair): G12 — characterization coverage for the folded paste/add dialog pair (KF.W8 .g)`; local `master` = the same sha; `git status --porcelain` → two untracked `docs/tranches/V/coordination/VALUEJS-INBOUND-*` letters, **zero modified product paths**. **The banked anchors are older than this ref** (the records were adjudicated at worktree-on-`8281638c` and re-probed at `81a56990`), and KF.W6/KF.W7/KF.W8 have since MOVED several of them — each move this seat measured is named at its packet below as a dated observation; **every line anchor is re-resolved at open before any cure** (KF.W0 §B-12 law; D-19).
**Standing supersession rider (KF-AV-28), carried expressly on the drag-seam limb (register row 1, lock 1)**: no cure on a governed row lands before KF.W7's per-surface SWAP verdict. **The verdict is IN, and it is KEEP**: `execution/B/KF-W7.md` `.a` G1 `4c03ceda` (2026-09-18) — *"six surfaces, six KEEP-BESPOKE, ZERO SWAP"*, `SequenceScrubber (S-3/S-4)` = **KEEP-BESPOKE** (*"the gesture drives `Sequence.scrub` → each child's `--ball-p` under an explicit inv ζ single-writer rule; the primitive would insert a producer spring clock as a second writer"*), and the `.f` verdict table row 4: **CARRY — the shape rows PERSIST**, discharge set **NONE**. **The drag-seam packet is therefore UN-GATED and every one of its rows is live**; nothing under it is `DISCHARGED by KF.W7 SWAP verdict <surface>, <date>` (that alphabet has zero members, W7's own count). The rider stays on the packet as the record of why it sequences where it does.
**Provenance**: authored BLIND by the fable arm of the SS-1/SS-2 authoring block (COHESION §0d; `EXECUTION-RUNBOOK.md §4.5`; M-23 §3 — the Opus arm's draft was not read) from: the register of record `KF-W10.md §6.D` row 1 (cargo, locks, blast-radius receipts, verb); the two minting citations; the home records `docs/tranches/V/megatranche/registry/adjudicated/kf-{CubeTarget,CubeScene,OrbitalDrag,SequenceScene,SequenceTarget,SequenceAxis,SequencePlayhead,SequenceScrubber,SpringScene,SpringTarget,SpringTrace,SpringHeatmap,SpringPhysicsFacet,StartingStyleTarget,SquareScene,SquareInstrument,AmigaScene}.md` — their `NO-WAVE-OWNER — the … packet` terminal lines are the packet rosters, consumed **by mechanism, deduped by identity, never transcribed** (M-25); `execution/B/KF-W7.md` (the SWAP verdicts and the landed precedents); and the live keyframes tree, READ-ONLY. Validated at authoring against `docs/precepts/instructions/tranche/WAVE_SPEC.md` (L-20). **No gate exists here without a named live witness (L-19).**

| verb | state | basis |
|---|---|---|
| AUDITED | YES | the seventeen home records named above, each carrying a `NO-WAVE-OWNER` terminal line that names the SS-1/SS-2 authoring as the seat that must home it — ⟨cmd⟩ `grep -l 'NO-WAVE-OWNER' registry/adjudicated/kf-{CubeTarget,CubeScene,OrbitalDrag,SequenceScene,SequenceTarget,SequenceAxis,SequencePlayhead,SequenceScrubber,SpringScene,SpringTarget,SpringTrace,SpringHeatmap,SpringPhysicsFacet,StartingStyleTarget,SquareScene,SquareInstrument,AmigaScene}.md \| wc -l` → **17** |
| SPECIFIED | YES — this file (a draft: the authoring block's arm; ratification is the block's fold seat, never this seat) | **the packet denominator is the register's — 9 of 17 — and carriage over the packets' row-ids is NOT asserted in this file's voice (LAW B)**: the rows land by their home records' terminal lines, quoted at §Carry by anchor; the carriage census is the fold seat's artefact and is cited by path when it exists |
| IMPLEMENTED | NO | stays NO until the gates green after the sequencing head lands |
| VERIFIED | NO | stamped only at sub-tranche close — and **KF.W10's close stamps the AUTHORED ELEVEN only** (COHESION §0d); this wave's VERIFIED is a successor close's act |

**Execution shape**: 10 seats across 4 phases — phase 1: 3 parallel (cube ∥ spring-family-A ∥ square); phase 2: 3 parallel (sequence ∥ spring-family-B ∥ amiga); phase 3: 1 serial (drag-seam — AFTER sequence, same file); phase 4: 1 serial (close). Peak concurrency **3**, inside the owner's four-workflow cap (2026-07-24). Tiering per M-12 tri-fold is the orchestrator's at execution.

---

## §State

**Name**: KF.W11 — Demo Scene Repair
**Opens after**: **G-KFW4-1 lands GREEN** (KF-W4 §Sequencing, the sequencing-head paragraph: *"No repair packet and no UNIT may open before G-KFW4-1 lands"* — **measured RED at this seat, twice**: ⟨cmd⟩ `npx vue-tsc --noEmit -p tsconfig.json 2>&1 \| grep -c 'error TS'` → **54** · **54**; the LEDGER's KF.W4 row reads `CLOSED 2026-09-17 (honest-RED: G-KFW4-1 …)`, so the head is CLOSED-honest-RED and **this wave cannot open until a repair seat turns that count to 0**) · **KF.W7 CLOSED** with its verdict table LANDED (`4c03ceda`; the drag-seam limb's lock, §Sequencing 4) · **KF.W8 CLOSED** (its colocation moves are what re-anchored this wave's square and card seams; every anchor below is post-W8) · **KF.W0 §B-12** (the re-baseline law; inherited by every anchor here).
**Agents**: 10 (a→j); phases as the execution shape above; **no jury**.
**Hard gate**: **ten conditions, ALL RED**, measured at `69095552` and pasted at §Gates, each double-run.
**Status**: planned

## §Goal criterion

This wave succeeds if, when work ends, every one of the nine scene packets has landed **as its home record shaped it** — one commit family per packet, the family's internal ordering edicts intact — and the demo's six scenes are honest instruments: what a scene's docblock asserts is what its test asserts (the MISS-3 law), what its readout prints is what its engine computes (the instrument-truth cores), and what its gesture affords is what its machine does (the gesture specs). **A wave that lands the hygiene tails and leaves a BLOCKER core (CubeTarget #53, SquareScene L-2, AmigaScene D-1, SpringTarget C-1) un-cured has failed this goal**, whatever its gate count says: it closes `complete_with_misses`.

## §0 Open preconditions (checked at wave-open; NOT close gates)

| # | precondition | state at authoring |
|---|---|---|
| **OP-0** | **G-KFW4-1 GREEN** — the sequencing head (§State). | **RED — 54 vue-tsc errors at `69095552`** (double-run). Nothing in nine `.vue`-heavy packets is defensible until a `.vue` file can fail a build; the head law is the register's own first lock (*"all 17 sequence after G-KFW4-1"*). |
| **OP-1** | **KF.W7's per-surface SWAP verdict for `SequenceScrubber`** — the drag-seam limb's gate under KF-AV-28. | **MET**: KEEP-BESPOKE, `4c03ceda`, 2026-09-18; discharge set NONE; the `.f` table's row 4 says CARRY. The packet is un-gated (header rider). |
| **OP-2** | **The `grabDx` precedent** — SquareScene MISS-1 *"shares grabDx's class"* (KF-W7 §Sequencing → KF.W11 arm (d)). | **MET AS LANDED CODE**: `7225cdd1` (KF.W7 `.d`, G3 — grabDx + L-m-14, one family, `TimelineTrack.vue`). Arm (c) inherits a worked precedent, not a design question (`execution/B/KF-W7.md`, *"What the two gated successors read here"*). |
| **OP-3** | **D-19 geometry re-derivation before any geometry cure** (KF.W7's OP-3, the same law): the SequenceAxis/Playhead/Target geometry commit, SquareInstrument's tether-truth commit and CubeTarget's latch/pointer seam each carry px figures banked at the stale fork. | **OPEN** — each geometry unit re-derives its figures at `69095552` in its receipt before writing a cure. A cure computed off a border box is a cure to the wrong number. |
| **OP-4** | **The spring-plot draw route's export decision** — KF.W7 `.a` ruled *"C-3 DECISION: the export is WARRANTED"* with the implementation edge at KF.W5/KF.W8. | **UNLANDED AT THE ARTIFACT**: ⟨cmd⟩ `grep -c 'sampleNormalizedSpring\\|resolveLinearStops' dist/keyframes.d.ts` → **0**. The packet's *"shared-builder route with a range parameter, preferred"* is therefore NOT available at this clock and unit `.e` takes the **anchor-hoist minimum** route unless the export has landed by its open (re-measured there). No `src/**` byte is this wave's. |
| **OP-5** | **KF-ES-2's facility-honesty spec** (kf-EditorShell; owner: the shell's wave) — the spring packet is *"sequenced with … the KF-ES-2 facility-honesty spec"* (kf-SpringScene:155). | **CONSUMED BY REFERENCE**: KF-SS-10-as-witness and KF-SST-10 ride the spec's cure and never re-derive it; if it has not landed at `.c`'s open, both rows land as **witness-only** (paint-or-declare-light) and say so. |
| **OP-6** | **The transport/ribbon packet (KF.W13)** — KF-SS-8 (*"the transport drives the selected channel"*) has a ribbon-side half. | **CROSS-EDGE, declared at §Sequencing**: KF-SS-8's scene-side cure lands here; its ribbon-side half is KF.W13 `.b`'s C-2 time-space contract. Neither blocks the other's open; the seam is named at both ends. |

---

## §Scope

1. **cube** — the three-record packet lands as ONE family per home record: CubeTarget's Roll stack + the **#53 + #7 paired seam** (the die's only pre-start orientation writer has never painted: `useTransformState.ts:122`/`:208` hand `transformTargetsStyle` a `Matrix3dCall` object) + the latch/pointer seam; CubeScene's matrix-editor family + state/identity + glass-conformance (D-2/C-3: `variant: "outline"` is not a prop of glass-ui 7 Button); OrbitalDrag's gesture-math + **latch family (= KF-AX-1's registry adoption + blur clear + expose reset)** + model-contract + presentation.
2. **sequence** — the five-record packet: the loop-seam commit, the transport-surface decision, the reel packet, the row-surface packet, the instrument-truth core (one canonical domain), the geometry commit (after OP-3), the a11y cluster **including ST-1's affordance edit** (one word in `SequenceTarget.vue` + the `.seq-handle:focus-visible` box-shadow deletion in `SequenceTarget.css`, measured at `:175-178` — the banked `:163-167` moved), **ST-4's one-binding cure** (`:loading="demo.isReeling.value"`, closes three rows), the gesture spec (one `applyScrub(p, {gesture})`), the prose-truth sweep, and the N-10 test obligation.
3. **spring** — SpringScene's interaction-truth + coordination + surface sweep; SpringTarget's contract head C-1, honest-instrument core, **M-4→M-3** (SpringTrace onto `.fn`; parser, both dead guards and the regex die together), M-5, N-1, the gesture spec, the a11y one-edit family, and the four born-RED tests (a)–(d).
4. **spring-plot** — SpringTrace's instrument-truth core as ONE commit family (the N-1 fork decided FIRST, then the draw route per OP-4), mark-design rows, type/register rows, N-5, L-8 (pending SS-13 #5), L-14.
5. **spring-physics-facet** — SpringHeatmap's instrument-truth core as ONE design decision, the input-integrity core, the contract commit (two `defineModel`s), N-SH-3, boundary/hover/pips, prose, hygiene, **L-m-5's born-RED tests**; plus SpringPhysicsFacet's NO-WAVE-OWNER rows SPF-3 (the `clamp(values[i] ?? 0, 0, 1)` painter at `:169` discards 100 % of the overshoot), SPF-4 (four exclusive presets as four `aria-pressed` toggles — the ToggleGroup port or the per-chip `aria-label` partial), SPF-5 (active-cell AA), SPF-7 (both inert `!important`s deleted).
6. **spring-artifact-truth** — StartingStyleTarget's artifact core KF-SST-3·4·5·9 (+11·12·26) as ONE spec, the verb/state pair KF-SST-1·6 (the survivor gets `aria-expanded`/`aria-pressed`; measured **0** today), KF-SST-2, -10 (with OP-5), -31, the prose sweep, the hygiene tail.
7. **square** — the **editor-seam packet (L-2 BLOCKER)**: `updateFromString` must re-supply the animation's own renderer, or `adoptCompiled` must preserve a receiver's non-default transform — the demo half of that decision (`useKeyframeOps.ts`, modify-carve) is this wave's; the library half is NOT (§Excluded); + the `RAFPlayback` degrade-not-wedge rider; the playback-authority packet; the colour packet; the instrument packet; the input/a11y packet (MISS-1 grabDx via OP-2's precedent); geometry D-8; the hygiene tail; **and SquareInstrument's six commits** (owner-decision gate D-2 · tether-truth · instrument-truth · disclosure · field · chrome).
8. **amiga** — the twenty-two-row packet headed by **D-1 (BLOCKER: the paused stage is pinned to HOME — `rendered` is written only under `playing`; nothing reads `pose` outside that branch)**, L-B1, D-2's shared idiom, D-3+C-18+M-3, the L-M/C pairs, MISSED-A/C/E/F/G, C-16/-17/-20/-10/-5, L-m3/L-m6, the INFO rows.
9. **drag-seam** — **KF-SCR-1 + L·D-1 as ONE cure family** (re-entrancy guard + `pointerId` latch + scope-disposal release + the "Nesting-safe" docblock corrected) spec'd WITH **KF-AV-15** across both seams (`useDragScrub.ts` is the shared composable: `acquireSelectSuppression()` unconditional per `pointerdown`, release gated behind `dragging` → acquire ×2 / release ×1 → `activeGestureCount` pinned); **C·C-1 ≡ KF-AV-16** (throttle-vs-decouple at the machine seam, ONE decision for pointer + coast); KF-SCR-5's dead-API/prose cleanup on the same seam edit.
10. **Close** — receipts, the per-packet commit roster audited against §Bounds, the BH relay of every producer row this wave surfaced (glass-producer→SS-6, lock 4), the LEDGER row.

### Triumvirate dispatch (WAVE_SPEC §3a)

- **Bounds whose expansion invalidates the wave**: any `src/**` write (the square L-2 library half, the spring-plot export) — ESCALATE, never expand; any write to `TimelineTrack.vue`/`AnimationVisualizer.vue` beyond the KF-AV-15/-16 twins (KF.W7's surfaces).
- **Non-local gate failures**: G-KFW11-7 (square L-2) failing because the demo half cannot preserve the renderer without the library half → triumvirate, with KF.W5/KF.W8 named; G-KFW11-1 (cube #53) failing because `transformTargetsStyle`'s contract is the engine's → same.
- **Diagnostic loops that halt at three**: the SpringTarget M-5 race (a double-tap at t≈1500 ms) reproduced three times without a deterministic harness → halt, escalate the harness shape.

---

## §Bounds

All paths keyframes.js-relative at `69095552`; line counts ⟨cmd⟩ `wc -l` this seat. **Access** per WAVE_SPEC §4. Units in parentheses.

| File | L | Access | Unit |
|---|---|---|---|
| `demo/scenes/cube/CubeTarget.vue` | 271 | modify | `.a` |
| `demo/scenes/cube/CubeTarget.css` | 210 | modify | `.a` |
| `demo/scenes/cube/CubeScene.vue` | 294 | modify | `.a` |
| `demo/scenes/cube/matrix-editor/useTransformState.ts` | 227 | modify (the #53 seam: `transformTargetsStyle({ transform: matrix3dEnd.value }, …)` at `:122`/`:208`) | `.a` |
| `demo/scenes/cube/orbital-drag/OrbitalDrag.vue` | 354 | modify (the OD families; **the latch family's registry adoption at `:276-277` — `useEventListener(window, "keydown"/"keyup")` — is KF-AX-1's upstream half and is THIS unit's write; KF.W12's AXISLINE-UNIT consumes it by commit**) | `.a` |
| `demo/scenes/cube/orbital-drag/{composables/**,quaternionEuler.ts,types.ts,index.ts}` | — | modify-carve (OD model-contract rows only) | `.a` |
| `demo/scenes/cube/{useCubeDemo.ts,useCubeRelit.ts,cubeTransformStore.ts}` | — | modify-carve (lighting-truth #4/#16/#36/#56; state/identity) | `.a` |
| `demo/scenes/sequence/SequenceScene.vue` | 43 | modify | `.d` |
| `demo/scenes/sequence/SequenceTarget.vue` | 252 | modify | `.d` |
| `demo/scenes/sequence/SequenceTarget.css` | 271 | modify (ST-1's deletion at `:175-178`, the `.seq-handle:focus-visible` rule) | `.d` |
| `demo/scenes/sequence/SequenceAxis.vue` | 49 | modify | `.d` |
| `demo/scenes/sequence/SequencePlayhead.vue` | 92 | modify | `.d` |
| `demo/scenes/sequence/{useSequenceDemo.ts,useSequenceInstrument.ts,useTypedTrigger.ts,sequenceKeys.ts}` | — | modify-carve (loop-seam, reel, store hygiene) | `.d` |
| `demo/scenes/sequence/SequenceScrubber.vue` | 193 | modify — **SHARED by `.d` (gesture spec, typography, prose, hygiene, a11y) and `.i` (drag-seam)**; `.i` opens only after `.d` commits (§Disjointness) | `.d` → `.i` |
| `demo/composables/useDragScrub.ts` | 150 | modify (KF-SCR-1 + L·D-1: the re-entrancy guard at `acquireSelectSuppression()`, the `pointerId` latch, scope-disposal release) | `.i` |
| `demo/utils/gestureSelectSuppression.ts` | 23 | modify-carve (only if the guard belongs at the token, per the seam spec) | `.i` |
| `demo/components/playback/AnimationVisualizer.vue` | 305 | modify-carve — **KF-AV-15 / KF-AV-16 twins ONLY** (the seam-packet spec covers both seams); every other AV row is KF.W7-carried NO-WAVE-OWNER and stays untouched | `.i` |
| `demo/scenes/spring/SpringScene.vue` | 272 | modify | `.c` |
| `demo/scenes/spring/SpringTarget.vue` | 476 | modify | `.c` |
| `demo/scenes/spring/{useSpringDerby.ts,useSpringDemo.ts,useSpringHotPath.ts,useSpringKeyframesEditor.ts,useCompiledEntry.ts,useSpringLinearStops.ts,springPresets.ts,springKeys.ts}` | — | modify-carve (the rows that name them) | `.c` |
| `demo/scenes/spring/SpringTrace.vue` | 148 | modify | `.e` |
| `demo/scenes/spring/SpringHeatmap.vue` | 383 | modify | `.f` |
| `demo/scenes/spring/SpringPhysicsFacet.vue` | 262 | modify | `.f` |
| `demo/scenes/spring/StartingStyleTarget.vue` | 297 | modify | `.g` |
| `demo/scenes/square/SquareScene.vue` | 331 | modify | `.b` |
| `demo/scenes/square/SquareScene.css` | 159 | modify | `.b` |
| `demo/scenes/square/SquareInstrument.vue` | 212 | modify | `.b` |
| `demo/scenes/square/{useSquareDemo.ts,useSquareKeyboard.ts,useSquareTumble.ts,squareKeys.ts}` | 429 / 103 / — / — | modify | `.b` |
| `demo/components/instrument/keyframes/composables/useKeyframeOps.ts` | 219 | **modify-carve — the L-2 demo half ONLY** (`updateFromString` at `:58-69` re-supplies the receiver's renderer); **shared with KF.W12's KFED-UNIT, which opens AFTER this unit's L-2 commit** (§Sequencing 6) | `.b` |
| `demo/scenes/amiga/AmigaScene.vue` | 302 | modify | `.h` |
| `demo/scenes/amiga/{useAmigaDemo.ts,useAmigaThree.ts,useSphereSpin.ts,utils.ts,amigaKeys.ts}` | — | modify-carve | `.h` |
| `test/demo/scenes/cube-roll-and-prestart.test.ts` | — | create | `.a` |
| `test/demo/scenes/sequence-instrument-truth.test.ts` | — | create | `.d` |
| `test/demo/scenes/spring-derby-truth.test.ts` | — | create | `.c` |
| `test/demo/scenes/spring-trace-truth.test.ts` | — | create | `.e` |
| `test/demo/scenes/spring-heatmap-reversibility.test.ts` | — | create | `.f` |
| `test/demo/scenes/starting-style-artifact.test.ts` | — | create | `.g` |
| `test/demo/scenes/square-editor-seam.test.ts` | — | create | `.b` |
| `test/demo/scenes/amiga-paused-pose.test.ts` | — | create | `.h` |
| `test/demo/instrument/drag-scrub-reentrancy.test.ts` | — | create | `.i` |
| `test/demo/scenes/{cube-scene,square-scene,sequence-scene,amiga-sphere-spin}.test.ts` · `test/demo/instrument/sequence-scrubber-mount.test.ts` | — | modify (existing witnesses extended, never weakened; `sequence-scrubber-mount.test.ts` is KF.W7 `.e`'s `8cfee94e` and already carries keyboard parity + per-sample direction latch — L·D-12's obligation is **PARTLY DISCHARGED there** and this wave adds only the gesture-spec cases) | owning unit |
| value.js `docs/tranches/X/execution/B/KF-W11.md` | — | create (the execution record; unit receipts) | every unit, append-only |
| value.js `docs/tranches/X/execution/LEDGER.md` | — | modify (this wave's row cells + appended event lines ONLY) | `.j` |
| value.js `docs/tranches/V/coordination/INBOX.md` + the SS-6 accretion register (COHESION §4a) | — | modify (append: the producer rows this wave surfaces) | `.j` |

**Do NOT touch**: `src/**` (the library — the square L-2 library half and the spring-plot export are KF.W5/KF.W8's; `adoptCompiled`, `usesDefaultRenderer` are read-only oracles) · `demo/components/instrument/timeline/**` and `demo/components/instrument/keyframes/**` except the one `useKeyframeOps.ts` carve (KF.W7 / KF.W12 surfaces) · `demo/components/playback/PlaybackRibbon.vue`, `demo/styles/playback-idiom.css`, `demo/styles/design-idioms.css` (KF.W13's) · `demo/scenes/cube/CubeAxisLines.vue` (KF.W12's AXISLINE-UNIT) · `demo/app/**` (KF.W13) · `node_modules/**` · `package.json` / `vitest.config.ts` (the harness is present: `@vue/test-utils ^2.5.1`, `vue-tsc ^3.3.11`, the `demo` vitest project with `test/demo/**/*.test.ts` — measured; no manifest change is owed) · every glass-ui tree (READ-ONLY always) · `scripts/dev/dev.sh` (unowned, never staged) · every dated spec, the adjudicated registry, conformance artefacts (E-3).

### Disjointness (WAVE_SPEC §4a)

Every `modify`/`modify-carve` path above belongs to exactly one unit **except two, resolved by SERIAL order, never parallel**: (i) `SequenceScrubber.vue` — `.d` then `.i`; `.i` opens on `.d`'s commit sha in the record; (ii) `useKeyframeOps.ts` — this wave's `.b` then KF.W12's KFED-UNIT (a cross-wave seam declared at both ends; §Sequencing 6). `AnimationVisualizer.vue`'s carve is `.i`'s alone; KF.W7 is CLOSED and wrote its last byte there. No two parallel units of this wave share a path; phases 1 and 2 are each three-way disjoint by directory.

### Worktree plan (WAVE_SPEC §4b)

Parallel units run in sibling worktrees of keyframes.js named `keyframes-kfw11-<unit>` (absolute path stated in each dispatch), each committing on its own branch and merging to `master` by the orchestrator in phase order; serial units (`.i`, `.j`) run on `master`. No Cargo; the column is n/a.

---

## §Carry — the nine packets, homed

Each packet's roster is its home record's terminal `NO-WAVE-OWNER — the … packet` line, cited by record + line at `69095552`'s registry bytes (the registry is immutable, E-3). **Ids are the records' own, for life; nothing is renumbered.** A packet's *internal ordering edicts are theirs and travel intact* (KF-W4 §Sequencing). The two identity-dedupes this wave performs are stated where they occur.

### P1 · cube — `kf-CubeTarget.md:102` · `kf-CubeScene.md:178` · `kf-OrbitalDrag.md:158` (spine = kf-CubeScene's routing block)

- **CubeTarget**: the Roll stack **#1·#2·#5·#15·#20·#38·#49** (one four-defect repair, its own born-RED gate) · **#53 + #7 as ONE paired seam repair** (demo serialization + library diagnostic; born-RED: pre-start drag → computed transform asserts) — **the library diagnostic half (#7) is a `src/**` byte and is NOT this wave's; the seam lands with the demo half and a declared edge to KF.W5** · #6 (transform ownership, re-derived post-#53) · lighting-truth **#4/#16/#36/#56** · the latch/pointer seam **#13/#18/#19/#54/#55** · loading-state #12/#22 · #14 (latent under KF-APP-1) · #17 · axis-reveal **#31/#57** (the CubeTarget side only; the CubeAxisLines side is KF.W12's) · hygiene #23–#28/#32/#33/#35/#37 · INFO #29/#40–#48/#52/#59.
- **CubeScene**: the matrix-editor family as core (**L-3 · L-4+M-6 · L-5/C-6/S-9 · C-7/L-13/L-14 · L-9 · C-16+D-10 · MISS-1 · M-5 · M-8**) · state/identity (**L-2/C-5+C-20 · L-7 · L-19 · L-29**) · glass-conformance (**D-2/C-3 + D-3/C-4+D-25inv**, the SpringScene rider rides to `.c`) · **the KF-APP-1/-17 motion with ARB-1's sequencing law — DELETE ARM ONLY until the glass-ui M-A relay lands** (the KF-APP-1/-17 same-motion lock is homed inside an authored wave and is NOT one of this wave's six travelling locks; this packet CONSUMES ARB-1's arm and re-derives nothing) + MISS-4's checklist + the C-14 bucket rider · the C-15 limb settled WITH kf-AnimationControlsGroup **D-1**'s portal-architecture cure (KF.W7's OP-0 — consumed as settled or the limb waits) · the hygiene tail **D-16 · D-17/C-10 · C-8/L-28 · C-11 · C-17 · C-18 · L-16 · L-26 · MISS-2 · MISS-5 · MISS-6 · INFO**.
- **OrbitalDrag**: gesture-math + hand-off **OD-1·7·12·13·21·22·27·33·36** (signed hand-offs, per-component angles, tracked deps, derived constants; born-RED gates named in-row) · **the latch family OD-3·4·5·6·25·31 — ONE cure = KF-AX-1's registry adoption + blur clear + expose reset** (riders ⌘Z + `event.code`) — **measured live at `OrbitalDrag.vue:276-277`**: `useEventListener(window, "keydown"|"keyup", … pointer.updatePressedKeys …)` — the window latch that bypasses the demo's ONE-registry law; this is the byte KF.W12's AXISLINE-UNIT waits on · model-contract **OD-2·8·14·16·18·19·20·23·24** · presentation **OD-10·32·35·43** · hygiene/INFO **OD-17·26·28·30·34·37·39·40·41·42·44**.
- **Locks riding**: glass-producer rows (D-2/C-3's `variant: "outline"` — the consumer cure is ours; any producer ask → SS-6) · comment-stated invariants are test obligations.

### P2 · sequence — `kf-SequenceScene.md:154` · `kf-SequenceTarget.md:165` (+ `:87` ST-4) · `kf-SequenceAxis.md:131` · `kf-SequencePlayhead.md:152` · `kf-SequenceScrubber.md:137`

- **Scene**: the loop-seam commit **L-3 + L-10 + SC-5 + SC-6** (one `SweepSceneOptions` parameter; one adapter, one name) · the transport-surface decision **SC-2 (+ D19(a) · L-12/C-8's 10 unconsumed members)** — expose the verbs or delete them; the drag gesture gets its undo · the reel packet **D7 + SC-3 + L-8 + L-9 + SC-7 + D9/D22-residue** · the row-surface packet **D2 (+SA-1) · D3 · D10 · SC-8 (≡ N-12) · D16 · D15** · store hygiene **D23/L-4/C-3 + SC-1** · the prose-truth sweep **L-6/C-5 · L-11/C-4 (three instances fleet-wide) · SC-4 · the F.W9 docblock** · **D6 + D8** · hygiene **D21 · D24 · D25 · L-13 · D11** · C-9's census extension.
- **Target**: instrument-truth folds (D-1/N-2/N-4 + the seek-shape rider) · the geometry commit **D-6 + D-13 + D-22 + ST-9** (+ ruling-1's cure; **after OP-3**) · the state-signal spec **D-9 + D-15 + ST-2 + ST-4 + ST-10** — **ST-4 is the packet's cheapest cure**: `:loading="demo.isReeling.value"` on the shipped Button prop (aria-busy/data-loading/suppressed activation) closes D-9.2, D-15 and makes `playReel`'s silent guard user-visible · the a11y cluster **D-5 + ST-1 + the N-14 riders** — **ST-1 (KF-W4 §Sequencing, carried verbatim): one word in `SequenceTarget.vue` + the css deletion**, measured at `69095552`: ⟨cmd⟩ `grep -c 'focus-ring' demo/scenes/sequence/SequenceTarget.vue` → **0** (the word is absent) · `grep -c 'box-shadow: var(--focus-ring-shadow)' demo/scenes/sequence/SequenceTarget.css` → **1** (the bespoke rule at `:175-178`, under the `S.G3 S3` comment; the banked `:163-167` moved) — **KF.W9 holds the before/after witnesses and lands no cure (R-4/R-9a)** · the gesture/ownership spec **L-5 + ST-7 + ST-8 + L-2 (seam-level with KF-AV-15 — coordinated with `.i`) + ST-6** · the consumption row **C-2 · C-3/D-26 · C-5 + ST-3 · C-8/C-10/C-12/C-13** · render-scope **L-3/C-9** (SS-13 #6 its kill switch) · the seam-hygiene tail **L-8 · L-9 · L-10/C-4 · C-11 · D-17/D-18 · D-19 · D-21 · D-23/D-24/L-12**.
- **Axis**: the ruler-legibility trio **D-3 · D-2 · D-4** (one raster item) · the geometry-commit rider (SS-13 #4 its gate) · seam **L-3/C-3/D-6b** · **L-2 + N-16** (`--tick-count`, one edit) · type **D-7 + SA-1** · **D-8a + L-10** · **D-9** (breakpoint tokens; T.F's born-RED gate) · **SA-2** (+N-12) · hygiene **D-5/L-6 · D-10/L-11 · D-13 · SA-3 · SA-4 · SA-5 (+N-17)** · **L-12 ≡ N-10**.
- **Playhead**: the instrument-truth core **N-1 · N-2 · D-1 · N-4** — ruling-1's cure (`gap: 0.5rem var(--col-gap)`; SS-13 #10), the K-25 note (N-1's domain cure and N-2's recompute cure land against each other), M-10's frame (**which of four domains is canonical — ONE spec, not four patches**) · interaction **N-12 · N-13 · N-3 (+N-18)** — one gesture spec · geometry **D-10/L-2 + D-2/L-4/C-5** (via N-8) · stacking **D-3/L-3** (the one deletion) · property-hygiene **D-11 + D-14 + L-9 + D-13/L-12** · **N-6 · N-7 · N-15 · N-5** · prose **D-4** (four sites, three names) · **D-5 · L-7 · N-19 · D-6 · D-12** · **N-14's valuetext cure, SEQUENCED AFTER N-1** · **N-16/N-17/N-20** · **L-10/L-11/N-9 · L-14/N-8** · **N-10 — the packet's test obligation, born-RED: the three-rect equality + the axis-terminal-label = duration gate** (G-KFW11-2).
- **Scrubber (the sequence half; `.d`)**: the gesture spec — one `applyScrub(p, {gesture})` landing **C-2/L-D-4 + KF-SCR-6 + the N-13 bloom rider + C·C-12's deadband** at once · geometry residue **D·D-1's N-18 cells · C-7 · KF-SCR-3** · typography **D·D-2 + KF-SCR-4** · the prose-truth sweep (two Fira-Code comments, two brightest-number claims, the `:584` citation, KF-SCR-5's three MotionPath paragraphs, the header's "editable CONTENT" claim) · hygiene **C-5/L-D-3 · L-D-8 (pending SS-13 #4) · L-D-9 · C-11 · D-12 · D-8** · a11y **L-D-10 + the N-14-sequenced valuetext + L-D-6 + the NaN-projector guard** · coverage **L·D-12** — partly discharged by `8cfee94e` (six cases at `test/demo/instrument/sequence-scrubber-mount.test.ts`, measured); the gesture-spec cases are what remain.
- **Also carried here per KF-W4 §Sequencing's KF.W11 line**: ST-5's one-guard fix (witness KF.W9's, cure ours) · MISS-6's scene cure · **the KF-ES-4/-5/-18 and L-M8 rows the unchecked `h(PlaybackRibbon, …)` bag enables** — the sequence scene's consumption side only; the ribbon's own rows are KF.W13's.
- **Locks riding**: KF-AV-28 (the scrubber surface: KEEP, un-gated) · comment-stated invariants are test obligations · glass-producer → SS-6.

### P3 · spring — `kf-SpringScene.md:155` · `kf-SpringTarget.md:148`

- **Scene**: the interaction-truth core **KF-SS-1** (reseat participates in the machine) · **KF-SS-2** (facility-aware Reset) · **KF-SS-8** (the transport drives the selected channel — scene half here, ribbon half KF.W13 `.b`, OP-6) · **KF-SS-10-as-witness** (paint or declare light; OP-5) · the coordination core **KF-SS-3** (one debounced, cancellable recompile seam; the library `.fn`-without-`.css` API signal is RECORDED for KF.W5, never written here) · **KF-SS-16/17's banked-cure riders** · the surface sweep **KF-SS-4 · -5 · -7 · -18 · -19 · -21 · -22..30 · -33 · INFO**.
- **Target**: the contract head **C-1** (who dispatches play-intent; rest-on-entry preserved) · the honest-instrument core **M-2 + D-2 + D-7/C-8 + the prose corrections** (banked coordinates `:126-130`, `SpringScene:192-193`, `:75-79`, `:179-180`, `:402-403`, `useSpringDerby:100-101` — **re-resolved at open**; measured today the clamp sites sit at `SpringTarget.vue:210` (`clamp(live.sampled, 0, 1)`) and `:222` (`clamp(trackValues[i] ?? 0, 0, 1.18)`)) · **M-4→M-3** · **M-5** (clearTimeout + gate on `derbyActive`) · **N-1** · the gesture spec (**D-5 · m-11 · i-18 · N-3** — one spec) · the a11y one-edit family (**D-6 · D-14 · N-4**) · **D-9 + C-7-nit** · **D-3's three-site flag pass** · **N-2** · **N-6** · hygiene (m-6 · m-7 · m-9 · m-10 · m-12 · D-10 · D-11 · D-12-riders · D-13 · i-15 · i-16 · N-5 · N-7 · N-8).
- **Born-RED test obligation (the record's own four)**: (a) paused-entry rail-tap moves the ball (C-1) · (b) the trace's first/last plotted x = 0/100 (M-3) · (c) a double-tap at t≈1500 ms leaves the overlay mounted through race #2 (M-5) · (d) the settle pulse paints at `target`, not at 1 (D-2) → G-KFW11-3.
- **Locks riding**: glass-producer → SS-6 (the SpringScene rider from CubeScene D-2/C-3 lands here) · comment-stated invariants (the DFA-test-vs-comment assertion, KF-SS's own born-RED candidate).

### P4 · spring-plot — `kf-SpringTrace.md:140`

The instrument-truth core **D-1 + N-1 + N-2 + C-2/L-3 as ONE commit family**: the N-1 fork decided FIRST (drop the prop, or label x in ms and discharge D-6 — `defineProps<{ response; dampingFraction }>` at `:41`), then the draw route (**anchor-hoist minimum** under OP-4's measured 0; the shared-builder route only if the export lands first), with the parser posture **D-12/L-6/C-4 + the filter-cell**, L-5's bindings, D-11's label, **L-10's extraction/test obligation** inside it · mark-design **D-3 + D-4 + N-3** (neutral reference vocabulary, one motion) + **D-6 · D-7 · D-13** · type/register **D-2 + N-4 (+M-6) + D-10 + D-9** (one pass) · **N-5** · **L-8** (pending SS-13 #5) · **L-14** (document or clamp the coupling) · D-16 clean-by-construction. **Identity note**: M-3's *"swap SpringTrace onto `.fn`"* is SpringTarget's row and lands in `.c`; `.e` consumes the new prop surface — `.e` opens after `.c`'s M-3 commit (§Sequencing 3).

### P5 · spring-physics-facet — `kf-SpringHeatmap.md:132` + the kf-SpringPhysicsFacet NO-WAVE-OWNER rows (`:40` SPF-3 · `:41` SPF-4 · `:42` SPF-5 · `:47` SPF-7; SPF-33 INFO recorded)

The instrument-truth core **D-B1 · D-B2 · D-M8 · D-M1(+rider)** — ONE design decision (what the field encodes, on which axes, at what scale, with what legend) · the input-integrity core **L-M-1 · N-SH-1 · N-SH-2(site) · D-M5 · N-SH-6(site) · N-SH-4** — one lattice, one gesture/focus spec, one honest published tolerance · the contract commit **L-M-7/C-M-2 + C-M-3** (two `defineModel`s) · **N-SH-3** · **D-M4 + D-M7 + N-SH-5** (with the ramp redesign) · prose **L-M-2 · L-M-5 · L-M-6** · hygiene **L-m-1/L-m-2/L-m-3/C-m-6 + INFO** · **L-m-5's born-RED tests: reversibility property + N-SH-4 tolerance + the D-B2 mix-table snapshot** (G-KFW11-5). **Facet**: **SPF-3** (the `[0,1]` painter clamp at `:169` — remap `[-0.25,1.25]→[0,1]` or a stated-geometry headroom cap; pairs with SPF-15) · **SPF-4** (the `ToggleGroup` port at `@mkbabb/glass-ui/toggle-group`, or the per-chip `aria-label` partial; the producer constraint → SS-6 as a rider on KF-ET-4's letter) · **SPF-5** (active-cell AA: 4.464 FAIL → cure the mix) · **SPF-7** (delete both `!important`s).

### P6 · spring-artifact-truth — `kf-StartingStyleTarget.md:140`

The artifact core **KF-SST-3 · 4 · 5 · 9 (+11 · 12 · 26)** — ONE spec: one source of truth for the endpoints, true polarity/duration, case-preserving register, a readable+reachable viewport, one honest state model with a refusal surface · the verb/state pair **KF-SST-1 · 6** (which copy survives; the survivor gets `aria-expanded`/`aria-pressed` — measured **0** today) · **KF-SST-2** · **KF-SST-10** (with OP-5) · **KF-SST-31** (view gate + house throttle) · prose **KF-SST-20 · 21 · 22 (+13 · 25 · 33)** · hygiene **KF-SST-15 · 16 · 17 · 18 · 19 · 23 · 24 · 27 · 29**.

### P7 · square — `kf-SquareScene.md:162` (+ `:40` L-2) · `kf-SquareInstrument.md:145`

- **Scene**: the **editor-seam packet — L-2 BLOCKER**: a keyframes-panel edit destroys the scene's custom renderer (`updateFromString` recompiles with no transform → `resolveTransform(undefined)` → default renderer; `adoptCompiled` copies the flag). **The demo half — `updateFromString` re-supplies the receiver's renderer — is `.b`'s carve of `useKeyframeOps.ts:58-69`; the library half (`adoptCompiled` preserving a non-default transform) is KF.W5/KF.W8's and is DECLARED, not written** (§Excluded). `usesDefaultRenderer` is the reference oracle (read-only). + the **`RAFPlayback` degrade-not-wedge rider** (D-27/L-7/C-9) · the playback-authority packet **L-1/C-3 · L-8/C-2 (kf-CubeScene cross-ref) · C-4 · L-3 · L-5 + ARB-1 · D-14/L-9 · MISS-9** · the colour packet **C-1 · L-11/C-5 · MISS-4 · MISS-5 · L-22a** · the instrument packet **D-1 + MISS-2 (px-space geometry) · L-4/C-12 · D-9 · D-21 · D-15** · the input/a11y packet **MISS-1 grabDx (OP-2's landed precedent) · D-4 · D-7 · D-5 + L-17 · D-6/L-6 · D-19 · D-11 · D-12** · geometry **D-8** · hygiene **D-10 · D-13 · D-16 · D-17 · D-20 · D-22 · L-10 · L-13/C-11 · L-15 · L-16/C-10 · L-18 · L-19 · C-8 · MISS-3 · MISS-7 · MISS-8 · MISS-10 · D-24/D-25 · L-22b/c**.
- **MISS-3 — the comment-stated-invariant LOCK (travelling lock 3), measured RELOCATED**: the docblock *"paced by the spring's own settle, not a fixed timer"* and the `setTimeout(step, 520)` it contradicts live today in **`demo/scenes/square/useSquareKeyboard.ts`** — ⟨cmd⟩ `grep -n 'paced by the spring\\|setTimeout(step, 520)' demo/scenes/square/useSquareKeyboard.ts` → `:20` · `:66` — **forty-six lines apart, exactly the banked distance, in a different file** (the register's `SquareScene.vue` was the pre-colocation home). The lock's content is unchanged: the docblock's assertion is a test obligation, and `square-scene.test.ts`'s existing *"'c' launches the envelope tour"* case does not assert pacing — the cure lands the settle-paced tour AND the assertion in one commit.
- **Instrument**: the owner-decision gate **D-2** (OD-5/T.M2 sign-off decides which layers live; D-18, D-10, N-SQ-5 ride it — **OWNER-RULED; if unruled at `.b`'s open the layers question lands `complete_with_misses` and says so**) · the tether-truth commit **D-1 + D-6 + D-16 + N-SQ-4** (N-SQ-4 ORDERS the cure: derive-from-travel or px-space, never `viewBox`+`none`) · the instrument-truth commit **C-3/L-D3 + N-SQ-1 + L-D5 + N-SQ-8** · the disclosure commit **D-4 + D-5 + C-6** · the field commit **D-7 + N-SQ-2 + N-SQ-3** · the chrome commit **D-8 + D-11 + N-SQ-5** · hygiene/prose **D-9/C-7 · D-14 · D-15 · D-17 (×4) · L-D8 · L-D10 · N-SQ-6 · N-SQ-7 · N-SQ-9 · C-9 · C-5's rider · D-19 · L-D6 · L-D9 · the S-9/S-10 intake**.

### P8 · amiga — `kf-AmigaScene.md:159`

**D-1 (BLOCKER)** — the paused stage is pinned to HOME: `rendered` is written only under `playing` or during the self-clearing re-seat; the scrub reaches `pose` but nothing reads it outside the `playing` branch, and the gate is doubled (`onFrame` false at rest; `setProgress` never marks dirty) · **L-B1 · D-2 (shared idiom) · D-3+C-18+M-3 · D-6/D-11 · D-8+C-9 · L-M1/C-8 · L-M2/C-14 · L-M3/C-11 · L-M4/C-2 · L-M5/C-4 · MISSED-A · MISSED-C · MISSED-E · MISSED-F · MISSED-G · C-16 · C-17 · C-20 · C-10 · C-5 · L-m3 · L-m6 · INFO**. The existing `amiga-sphere-spin.test.ts` (five cases, the grab/glide contract) is extended, never weakened.

### P9 · drag-seam — `kf-SequenceScrubber.md:136` (+ `:40` KF-SCR-1 · `:42` C·C-1 · `:125` the identity line)

**KF-SCR-1 + L·D-1 as ONE cure family** — `useDragScrub.ts`: `acquireSelectSuppression()` unconditional per `pointerdown` (measured `:113-116` banked → the call is at the composable's pointerdown path today, ⟨cmd⟩ `grep -c 'acquireSelectSuppression()' demo/composables/useDragScrub.ts` → **1**) vs release gated behind the shared `dragging` boolean → **acquire ×2 / release ×1 on a second pointer → `activeGestureCount` (`demo/utils/gestureSelectSuppression.ts`, 23 L) pinned ≥ 1 → `user-select: none !important` strands the whole page** — cure: re-entrancy guard + `pointerId` latch + scope-disposal release + the "Nesting-safe" docblock corrected, **spec'd WITH KF-AV-15** (`kf-SequenceScrubber.md:125`: *"KF-SCR-1's class ≡ KF-AV-15 — one seam-packet spec covers both seams"*) so `AnimationVisualizer.vue`'s twin is the same guard, not a second one · **C·C-1 ≡ banked KF-AV-16** — the scrub drives a `localStorage`-persisting machine at raw pointer-sample frequency (one `JSON.stringify` + `setItem` per admitted `pointermove`); **ONE decision at the machine seam for pointer + coast** (throttle-vs-decouple), landed once for both consumers · **KF-SCR-5**'s dead-API/prose cleanup on the same seam edit. **Inherits from KF.W7 as landed code**: the grab-offset arm (`7225cdd1`) and C·C-4's provider guard (`43556828`, the scrubber names the contract it depends on). **Sequenced AFTER `.d`** (same file) and, by the rider, after the verdict that has already landed.

---

## §Agent Units

Each unit: Goal (forward) · Mechanism · Files (§Bounds rows) · Sub-gate (backward, evidence-bearing). Every unit's receipt is appended to `execution/B/KF-W11.md` with `SERVED MODEL: <id>` on its first line, the commands it ran and their outputs double-run, and the inherited-paths clause (CRASH-RECOVERY law). **No unit writes outside its §Bounds rows; an out-of-bounds need is an ESCALATION returned, never a write.**

### KF.W11.a — The Cube Packet (phase 1)
- **Goal**: the die's pre-start orientation writer paints, the Roll stack is one repair, and the OD latch family adopts the demo's one keyboard registry.
- **Mechanism**: P1 in the home records' internal order — CubeTarget #53 demo half first (the `Matrix3dCall` object serialized before `transformTargetsStyle`), the Roll stack, the latch/pointer seam, lighting truth; CubeScene's matrix-editor family, state/identity, glass-conformance (the `variant` prop deleted, `emphasis` authored), ARB-1's delete arm only; OrbitalDrag's four families with the latch family's window listeners replaced by registry adoption + blur clear + `reset` exposed.
- **Files**: the `.a` rows of §Bounds; `test/demo/scenes/cube-roll-and-prestart.test.ts` (create); `cube-scene.test.ts` (extend).
- **Sub-gate**: G-KFW11-1 GREEN; `git diff --stat` touches only `.a` rows; the receipt names #7's library half as DECLARED to KF.W5 with the edge's anchor.

### KF.W11.b — The Square Packet (phase 1)
- **Goal**: a keyframes-panel edit no longer destroys the square's custom renderer, and the tour is paced by the spring's settle as its docblock says.
- **Mechanism**: L-2's demo half at `useKeyframeOps.ts:58-69` (re-supply the receiver's renderer on `updateFromString`; `usesDefaultRenderer(frame.transform)` is the assertion's oracle); the `RAFPlayback` rider; the playback-authority, colour, instrument, input/a11y (grabDx via `7225cdd1`'s shape), geometry and hygiene packets; MISS-3's settle-paced tour with its assertion in the same commit; SquareInstrument's six commits in the record's order, D-2's owner ruling consumed or its absence stated.
- **Files**: the `.b` rows; `test/demo/scenes/square-editor-seam.test.ts` (create); `square-scene.test.ts` (extend).
- **Sub-gate**: G-KFW11-7 GREEN (both clauses); the `useKeyframeOps.ts` diff is the `updateFromString` hunk alone (its sha recorded for KF.W12's KFED-UNIT to open on).

### KF.W11.c — The Spring Packet (phase 1)
- **Goal**: the derby is an honest instrument — one dispatcher of play-intent, a marker that overshoots where the spring does, a trace on `.fn`, no race that unmounts the overlay.
- **Mechanism**: P3 in order — C-1 first (the contract head sizes the rest), M-2/D-2/D-7 with the prose corrections re-anchored, M-4→M-3, M-5, N-1, the gesture spec, the a11y family, the register/flag passes, hygiene; SpringScene's KF-SS-1/-2/-3/-8(scene half)/-10(witness) and the sweep.
- **Files**: the `.c` rows; `test/demo/scenes/spring-derby-truth.test.ts` (create — cases (a)–(d)).
- **Sub-gate**: G-KFW11-3 GREEN; the M-3 commit sha recorded (`.e` opens on it); KF-SS-8's ribbon-side half named to KF.W13 `.b` in the receipt.

### KF.W11.d — The Sequence Packet (phase 2)
- **Goal**: the sequence scene has one canonical time domain, one gesture spec, and an affordance edit whose before/after KF.W9 already holds.
- **Mechanism**: P2 — the loop-seam commit, SC-2's decision, the reel packet, N-1/N-2/D-1/N-4's one-domain spec, the geometry commit after OP-3's re-derivation, ST-4's one binding, ST-1's one word + one deletion (`SequenceTarget.css:175-178`), the N-14-sequenced valuetext, the scrubber's gesture spec (`applyScrub(p, {gesture})`), the prose sweeps, the store hygiene, the Axis trio and tokens.
- **Files**: the `.d` rows (including `SequenceScrubber.vue`, first writer); `test/demo/scenes/sequence-instrument-truth.test.ts` (create); `sequence-scene.test.ts` + `sequence-scrubber-mount.test.ts` (extend).
- **Sub-gate**: G-KFW11-2 GREEN (all three clauses); the commit sha `.i` opens on is in the receipt.

### KF.W11.e — The Spring-Plot Packet (phase 2; after `.c`'s M-3 commit)
- **Goal**: the trace plots what the spring computes, in a unit the axis names, drawn by one route.
- **Mechanism**: P4 — the N-1 fork decided in the receipt before any byte, the draw route per OP-4 re-measured at open, the parser posture and L-10's extraction+test in the same family; mark-design; register pass; N-5; L-14.
- **Files**: `SpringTrace.vue`; `test/demo/scenes/spring-trace-truth.test.ts` (create).
- **Sub-gate**: G-KFW11-4 GREEN; the receipt prints OP-4's grep at its own clock.

### KF.W11.f — The Spring-Physics-Facet Packet (phase 2)
- **Goal**: the heatmap encodes one stated thing on stated axes with a legend, the facet's presets overshoot where the physics does, and the field round-trips.
- **Mechanism**: P5 — the instrument-truth design decision written in the receipt first; the input-integrity core; the two-`defineModel` contract; the ramp redesign; SPF-3's remap; SPF-4's port-or-partial with the producer rider to SS-6; SPF-5's mix; SPF-7's two deletions; L-m-5's three tests.
- **Files**: `SpringHeatmap.vue` · `SpringPhysicsFacet.vue`; `test/demo/scenes/spring-heatmap-reversibility.test.ts` (create).
- **Sub-gate**: G-KFW11-5 GREEN (both clauses).

### KF.W11.g — The Spring-Artifact-Truth Packet (phase 2)
- **Goal**: the starting-style artifact tells the truth about its endpoints, polarity, duration and case, and has one copy verb with a state.
- **Mechanism**: P6 — the artifact core as one spec, KF-SST-1·6's survivor with `aria-expanded`/`aria-pressed`, KF-SST-2, -10 (OP-5), -31, prose, hygiene.
- **Files**: `StartingStyleTarget.vue`; `test/demo/scenes/starting-style-artifact.test.ts` (create).
- **Sub-gate**: G-KFW11-6 GREEN (both clauses).

### KF.W11.h — The Amiga Packet (phase 2)
- **Goal**: a paused amiga stage shows the scrubbed pose, not HOME.
- **Mechanism**: P8 — D-1's single read of `pose` outside the `playing` branch with the doubled gate undone (`onFrame`/`setProgress` marking dirty), then the packet in the record's order.
- **Files**: the `.h` rows; `test/demo/scenes/amiga-paused-pose.test.ts` (create); `amiga-sphere-spin.test.ts` (extend).
- **Sub-gate**: G-KFW11-8 GREEN.

### KF.W11.i — The Drag-Seam Packet (phase 3; serial, after `.d`)
- **Goal**: two pointers on any drag seam never strand the page's select-suppression token, and the machine is written at a rate it owns.
- **Mechanism**: P9 — the ONE guard family in `useDragScrub.ts` (re-entrancy + `pointerId` latch + disposal release + docblock), its KF-AV-15 twin as the same guard in `AnimationVisualizer.vue`, C·C-1/KF-AV-16's one throttle-vs-decouple decision at the machine seam, KF-SCR-5's cleanup; the born-RED two-pointer test.
- **Files**: the `.i` rows; `test/demo/instrument/drag-scrub-reentrancy.test.ts` (create).
- **Sub-gate**: G-KFW11-9 GREEN (both clauses); the receipt quotes the KEEP verdict sha it sequences after.

### KF.W11.j — Close (phase 4; serial, last)
- **Goal**: the wave's record is whole, its commits are audited to §Bounds, its producer rows have sailed, and its row in the LEDGER moves.
- **Mechanism**: re-run all ten gates at this seat's own commands (double-run); audit every commit sha against §Bounds by `git show --stat`; write the BH relay entries for every producer row surfaced (CubeScene D-2/C-3's `variant`, SPF-4's `role`/`aria-pressed` filter, LP-class Boolean-cast defaults if met) to the SS-6 accretion register; the E13 four-path mail sweep; the LEDGER row cells + event line; the residuals and escalations.
- **Files**: the value.js rows of §Bounds.
- **Sub-gate**: G-KFW11-10 GREEN; the LEDGER row reads what the gates say and nothing more.

### Standing law for every unit

CRASH-RECOVERY first (`git status --porcelain` in every repo you may write, own-set paths only) · the spec GOVERNS: no quick fix, no workaround, no masking fallback (no `try/catch` around a defect, no `test.skip`, no allowlist, no copied producer selector, no `node_modules` patch — each a HIGH defect) · idiomatic root-cause cures · pathspec commits, one per meaning, families unsplit · E-3 · WRITE-THEN-MEASURE, double-run · quote-by-command · sibling trees READ-ONLY, glass-ui READ-ONLY always (producer rows → SS-6) · no stash/reset --hard/force-push · E13 · probe parsimony (§5.2) · `scripts/dev/dev.sh` never staged · LEDGER edited only by minimal in-place cell replacement or appended lines.

---

## §Gates — ten, all born RED at `69095552`, every command literal, every figure double-run

Runtime gates are `vitest` runs (WAVE_SPEC: *no grep-only runtime gates*); byte clauses are paired with them, never alone. `RED today` prints this seat's measurement; `GREEN when` is the acceptance form. All commands run from `/Users/mkbabb/Programming/keyframes.js`.

**G-KFW11-1 — cube: the pre-start orientation paints; the Roll stack and the latch family are one repair each.** ⟨cmd⟩ `npx vitest run --project demo test/demo/scenes/cube-roll-and-prestart.test.ts` → RED today: `No test files found` (the file is absent: ⟨cmd⟩ `ls test/demo/scenes/cube-roll-and-prestart.test.ts` → `No such file or directory`, twice). GREEN when the file exists and passes with cases: a pre-start drag → the die's computed `transform` is a `matrix3d(...)` string (#53), the Roll stack's four defects each asserted (#1·#2·#5·#15·#20·#38·#49), and the OD latch clears on blur. Byte clause: ⟨cmd⟩ `grep -c 'useEventListener(window, "keydown"' demo/scenes/cube/orbital-drag/OrbitalDrag.vue` → **1** today · **1** (must read **0**: the window latch is replaced by registry adoption).

**G-KFW11-2 — sequence: one canonical domain; ST-1 and ST-4 landed.** ⟨cmd⟩ `npx vitest run --project demo test/demo/scenes/sequence-instrument-truth.test.ts` → RED today: `No test files found`. GREEN when it passes the N-10 obligation (the three-rect equality; the axis-terminal-label = duration) and ST-4's binding (`aria-busy` present while `isReeling`). Byte clauses: ⟨cmd⟩ `grep -c 'focus-ring' demo/scenes/sequence/SequenceTarget.vue` → **0** · **0** (must read ≥ **1**); ⟨cmd⟩ `grep -c 'box-shadow: var(--focus-ring-shadow)' demo/scenes/sequence/SequenceTarget.css` → **1** · **1** (must read **0**).

**G-KFW11-3 — spring: the four born-RED derby cases.** ⟨cmd⟩ `npx vitest run --project demo test/demo/scenes/spring-derby-truth.test.ts` → RED today: `No test files found`. GREEN when cases (a) paused-entry rail-tap moves the ball, (b) first/last plotted x = 0/100, (c) the overlay survives race #2 at t≈1500 ms, (d) the settle pulse paints at `target` all pass.

**G-KFW11-4 — spring-plot: instrument truth.** ⟨cmd⟩ `npx vitest run --project demo test/demo/scenes/spring-trace-truth.test.ts` → RED today: `No test files found`. GREEN when the extracted builder (L-10) is unit-tested against the engine's own samples and the axis unit matches the N-1 decision. Clock clause, printed at open and at close: ⟨cmd⟩ `grep -c 'sampleNormalizedSpring\|resolveLinearStops' dist/keyframes.d.ts` → **0** · **0** today (the route taken is stated against this figure).

**G-KFW11-5 — spring-physics-facet: reversibility, tolerance, the mix table; the overshoot painter.** ⟨cmd⟩ `npx vitest run --project demo test/demo/scenes/spring-heatmap-reversibility.test.ts` → RED today: `No test files found`. GREEN when L-m-5's three cases pass. Byte clause: ⟨cmd⟩ `grep -c 'clamp(values\[i\] ?? 0, 0, 1)' demo/scenes/spring/SpringPhysicsFacet.vue` → **1** · **1** (must read **0**).

**G-KFW11-6 — spring-artifact-truth: one honest artifact, one stateful verb.** ⟨cmd⟩ `npx vitest run --project demo test/demo/scenes/starting-style-artifact.test.ts` → RED today: `No test files found`. GREEN when the endpoints/polarity/duration/case assertions pass. Byte clause: ⟨cmd⟩ `grep -c 'aria-expanded\|aria-pressed' demo/scenes/spring/StartingStyleTarget.vue` → **0** · **0** (must read ≥ **1**).

**G-KFW11-7 — square: the renderer survives a panel edit; the tour is settle-paced.** ⟨cmd⟩ `npx vitest run --project demo test/demo/scenes/square-editor-seam.test.ts` → RED today: `No test files found`. GREEN when, after `updateFromString` on the square's animation, `usesDefaultRenderer(frame.transform)` is `false` for the nested leaf and the tour test asserts settle-pacing. Byte clause (the MISS-3 lock): ⟨cmd⟩ `grep -c 'setTimeout(step, 520)' demo/scenes/square/useSquareKeyboard.ts` → **1** · **1** (must read **0**). Witness census: ⟨cmd⟩ `grep -rl 'usesDefaultRenderer\|adoptCompiled' test/demo | wc -l` → **0** · **0** today.

**G-KFW11-8 — amiga: a paused stage shows the scrubbed pose.** ⟨cmd⟩ `npx vitest run --project demo test/demo/scenes/amiga-paused-pose.test.ts` → RED today: `No test files found`. GREEN when a paused scene + `setProgress(t)` renders `pose`, not HOME.

**G-KFW11-9 — drag-seam: two pointers, one token; one machine-write policy.** ⟨cmd⟩ `npx vitest run --project demo test/demo/instrument/drag-scrub-reentrancy.test.ts` → RED today: `No test files found`. GREEN when a second `pointerdown` during a drag leaves `activeGestureCount` at exactly 1 and the last `pointerup` returns it to 0, for both `useDragScrub` and the AnimationVisualizer twin, and the machine sees ≤ 1 dispatch per animation frame under a scripted 240 Hz pointer stream. Byte clauses (RECORDED, not acceptance — the runtime clause decides): ⟨cmd⟩ `grep -c 'acquireSelectSuppression()' demo/composables/useDragScrub.ts` → **1** · **1** (the unconditional acquire at the pointerdown path); ⟨cmd⟩ `grep -c 'pointerId' demo/composables/useDragScrub.ts` → **1** · **1** (a `pointerId` mention exists today and is NOT the latch — this seat's first draft printed **0** here and WRITE-THEN-MEASURE corrected it before publication; a byte count cannot stand in for the two-pointer run).

**G-KFW11-10 — close: the demo project is green and the head still holds.** ⟨cmd⟩ `npm run test:demo 2>&1 \| tail -3` → RED today by construction (nine absent files are nine failing runs once they exist; today the existing 30 demo files' verdict is the baseline and is pasted at open). GREEN when every `test/demo/**` file passes with no `test.skip`/`.only` in the wave's diff (⟨cmd⟩ `git diff <open-sha>..HEAD -- test | grep -c 'test.skip\|it.skip\|\.only('` → must read **0**) and the head is unmoved: ⟨cmd⟩ `npx vue-tsc --noEmit -p tsconfig.json 2>&1 | grep -c 'error TS'` → must read **0** (OP-0's own figure, re-run at close).

**GREEN-BEFORE-CURE (R.2)**: a gate found GREEN at open without this wave's cure is recorded as such, its cause named (a sibling landed it), and the packet row it governs is booked `LANDED-BY <sha>` — never re-cured, never claimed.

---

## §Sequencing — locks and cross-edges

### Intra-wave order (binding)

1. **`.a` ∥ `.b` ∥ `.c` (phase 1) → `.d` ∥ `.f` ∥ `.g` ∥ `.h` (phase 2; `.e` joins phase 2 once `.c`'s M-3 commit is in) → `.i` (phase 3, after `.d`) → `.j`.** Peak 3.
2. **`.e` after `.c`'s M-3 commit** — the trace's prop surface is SpringTarget's row.
3. **`.i` after `.d`** — same file (`SequenceScrubber.vue`); the drag-seam family lands on the gesture spec, not under it.
4. **`.i` after KF.W7's SWAP verdict** — the KF-AV-28 lock; satisfied (`4c03ceda`, 2026-09-18, KEEP) and named regardless.
5. **Every geometry commit after OP-3's re-derivation** at `69095552`, in the unit's receipt.

### The travelling locks (register row 1 — four of the six ride here; all four named)

- **KF-AV-28's standing supersession rider** — on the drag-seam limb; verdict KEEP; packet un-gated; rider retained as record.
- **LP-1's write→render ordering** — KF-CO-1/KF-CO-8's cures are sequenced by it **in their owning wave (KF.W12's OPTIONS-UNIT)**; this wave's spring KF-SS-8 scene-half touches no ChannelOptions byte and inherits nothing from LP-1 except the knowledge that a ribbon-side `:max` is not live until KF.W12 lands the edge (OP-6).
- **Comment-stated invariants are test obligations** — the kf-SquareScene MISS-3 law, measured relocated to `useSquareKeyboard.ts:20`/`:66`; binds every docblock a unit touches (the DFA-test-vs-comment assertion in P3; the "Nesting-safe" docblock in P9).
- **Glass-producer rows → SS-6, never demo-side hacks** — CubeScene D-2/C-3 (consumer half ours), SPF-4's producer filter, any Boolean-cast `default: undefined` ask: relayed at `.j`.

### Cross-edges, declared from this end

| to | type | declaration |
|---|---|---|
| **KF.W4** | **DEPENDS ON (hard) — the sequencing head** | G-KFW4-1 GREEN before any unit opens; measured RED (54) at this seat. This wave names the law rather than satisfying it transitively. |
| **KF.W7** | **DEPENDS ON (met)** | the SequenceScrubber KEEP verdict (`4c03ceda`) un-gates P9; `7225cdd1` (grabDx) is P7 MISS-1's precedent; `43556828` (C·C-4) is P9's landed provider guard. W7 promised no reciprocation; none is needed — this end reads the verdict table and the `.f` "What the two gated successors read here" block. |
| **KF.W8** | **DEPENDS ON (met)** | its colocation moves are the reason P7's MISS-3 lives in `useSquareKeyboard.ts` and the card seam sits where KF.W12 finds it; every anchor here is post-W8. |
| **KF.W12** | **SUPPLIES (two commits) + SHARES ONE PATH SERIALLY** | (1) `.a`'s OD latch-family commit is KF-AX-1's upstream half — AXISLINE-UNIT opens on its sha; (2) `.b`'s `useKeyframeOps.ts` L-2 carve precedes KFED-UNIT's open on that file. Both shas are printed in this wave's record for W12 to consume; W12 writes neither `orbital-drag/**` nor the `updateFromString` hunk. |
| **KF.W13** | **SEAM (named both ends)** | KF-SS-8's scene half here; its ribbon half is W13 `.b`'s C-2 time-space contract. Neither blocks the other's open. |
| **KF.W5 / KF.W8 (library)** | **DECLARES (not owned)** | CubeTarget #7's library diagnostic; SquareScene L-2's `adoptCompiled` half; SpringTrace's `sampleNormalizedSpring`/`resolveLinearStops` export (W7 C-3: warranted); KF-SS-3's `.fn`-without-`.css` API signal. Each row lands its demo half here and names the library half by id. |
| **KF.W9 / SS-13** | **DEPENDS ON (witnesses)** | ST-1's before/after (KF.W9 holds them, lands no cure); SS-13 #4/#5/#6/#10 witnesses named in P2/P4; SPF-3/-5's delivered-pixel cells. |
| **KF.W10** | **REGISTERED AT** | `§6.D` row 1 is this wave's terminal disposition for the X·KF close; this file changes that verb by nothing — the register records, the authoring block authors, a later close stamps. |
| **SS-6 (BH relay)** | **SUPPLIES** | the producer rows at `.j`. |

---

## §Excluded

| excluded | reason |
|---|---|
| **Any `src/**` byte** — CubeTarget #7, SquareScene L-2's `adoptCompiled` half, SpringTrace's export, KF-SS-3's API signal | library waves (KF.W5 / KF.W8); declared at §Sequencing, never written here. |
| **`CubeAxisLines.vue`** and every AXISLINE-UNIT row | KF.W12; this wave supplies KF-AX-1's upstream half only. |
| **The eight NO-WAVE-OWNER packets not in this partition** — CARD · OPTIONS · KFED · EDITOR · APPLY · AXISLINE (KF.W12) · dock-menu · transport/ribbon (KF.W13) | R-15's partition; no re-booking (register: *"no successor packet re-books a row homed here"*). |
| **KF-APP-1/-17's same-motion lock** | homed inside an authored wave; P1 consumes ARB-1's delete arm only. |
| **KF-APP-41 / kf-EditorShell C-22's orphan-token limb** | `ADOPTED-BY-KF.W6` (RULINGS-4 R4-1; register row 2's ANTI-RE-BOOK). |
| **KF-AV-10/-13/-14/-24/-32 and every AnimationVisualizer row other than the KF-AV-15/-16 twins** | KF.W7-carried NO-WAVE-OWNER under KEEP; the twins ride P9 by the record's own identity line. |
| **L·D-12's already-landed cases** | `8cfee94e` (KF.W7 `.e`); P2 adds only the gesture-spec cases. |
| **The retired-gate prose radius** (register row 1's blast-radius receipt) | measured at `69095552`, reproduced exactly — ⟨cmd⟩ `git grep -l 'proof:sequence-rows-draggable' HEAD -- docs \| cut -d/ -f3 \| sort \| uniq -c` → **13 H · 3 J · 1 K · 1 S · 1 U** (19 files); the K row: `docs/tranches/K/audit/ci-cd-k.md:73` `\| proof:sequence-rows-draggable \| H.W12 \| ~1m (drag + re-sort wait) \| ci.yml:1031-1034 \|` — **a deleting seat must meet the K row's historical CI-gating evidence**; this wave deletes no `proof:*` prose (the `proof:` family is KF.W4/KF.W9's) and records the radius as re-measured. |
| **Owner rulings** (SquareInstrument D-2's layer sign-off; OD-V5) | never proxied; `complete_with_misses` with the ruling named if unruled. |

---

## §Commit plan

One commit per meaning; every family the home record declares lands unsplit; pathspec on the commit itself; `Claude-Session:` trailer per the standing law. Expected roster (keyframes.js unless noted):

1. `fix(kf/cube · X.KF.W11.a · #53+#7 demo half)` · `fix(kf/cube · X.KF.W11.a · roll stack)` · `fix(kf/cube · X.KF.W11.a · OD latch family → registry)` · `refactor(kf/cube · X.KF.W11.a · matrix-editor family)` · `fix(kf/cube · X.KF.W11.a · glass conformance)` · `chore(kf/cube · X.KF.W11.a · hygiene tail)` · `test(kf/cube · X.KF.W11.a · G-KFW11-1)`.
2. `fix(kf/square · X.KF.W11.b · L-2 demo half — updateFromString re-supplies the renderer)` (the sha KF.W12 opens on) · `fix(kf/square · X.KF.W11.b · MISS-3 settle-paced tour + assertion)` · the six SquareInstrument commits by name · `test(kf/square · X.KF.W11.b · G-KFW11-7)`.
3. `fix(kf/spring · X.KF.W11.c · C-1 contract head)` · `fix(kf/spring · X.KF.W11.c · honest instrument M-2/D-2/D-7)` · `refactor(kf/spring · X.KF.W11.c · M-4→M-3 trace onto .fn)` (the sha `.e` opens on) · `fix(kf/spring · X.KF.W11.c · M-5 race)` · `test(kf/spring · X.KF.W11.c · G-KFW11-3 (a)–(d))` · the sweep.
4. `fix(kf/sequence · X.KF.W11.d · loop seam)` · `fix(kf/sequence · X.KF.W11.d · one canonical domain)` · `fix(kf/sequence · X.KF.W11.d · geometry after D-19)` · `fix(kf/sequence · X.KF.W11.d · ST-1 + ST-4)` · `feat(kf/sequence · X.KF.W11.d · applyScrub gesture spec)` · `test(kf/sequence · X.KF.W11.d · G-KFW11-2)` (the sha `.i` opens on).
5. `.e` · `.f` · `.g` · `.h`: one family per packet core + `test(…)` each.
6. `fix(kf/drag-seam · X.KF.W11.i · KF-SCR-1+L·D-1+KF-AV-15 one guard family)` · `fix(kf/drag-seam · X.KF.W11.i · C·C-1≡KF-AV-16 one machine-write policy)` · `test(… G-KFW11-9)`.
7. value.js: `docs(x-kf-w11/<unit>): receipt` per unit; `docs(x-execution/ledger): KF.W11 row` at `.j`; the SS-6 accretion + INBOX append at `.j`.

## §Format and lint cadence · §Verification artefacts

`npm run check` (vue-tsc + tsc test config + `proof:structure`) and `npm run test:demo` after each unit's last commit and at close; `npx eslint demo/scenes demo/composables demo/components/playback` at close; `git diff --check` on every commit. Artefacts saved at close in `execution/B/KF-W11.md`: the ten gate transcripts (double-run), the commit roster with `git show --stat` per sha, the OP-4 grep at open and close, the D-19 re-derivation tables (P1 latch seam · P2 geometry · P7 tether), the BH relay entries' ids, and the E13 sweep line.

## §L-18 rider (reference, not ceremony)

Green gates are not acceptance: this spec's implementation must survive two challenging gestalt passes and a fresh-Fable agglomeration. Likely bases against it, named so they are looked for: (i) a packet whose hygiene tail landed while its BLOCKER core slipped to a residual (the §Goal criterion's own failure); (ii) a geometry cure written off the banked figure instead of the re-derived one (OP-3); (iii) the drag-seam guard landed in `useDragScrub.ts` and not in its AnimationVisualizer twin — two guards, or one — contradicting the record's *"one seam-packet spec covers both seams"*; (iv) a `test.skip` or a widened timeout used to turn G-KFW11-10 green (the masking class); (v) a `src/**` write smuggled as "the only way" for L-2 or #53 — that is an ESCALATION, and a seat that writes it has failed the wave.

**Dissents recorded, not resolved by this seat**: (1) whether `AnimationVisualizer.vue`'s KF-AV-15/-16 twins belong to this wave or to a KF.W7 successor round — this file follows the record's identity line and bounds the carve to the twins; the fold seat may re-home them. (2) whether the amiga packet's 22 rows warrant a split into a core (D-1/L-B1) and a tail unit — this file keeps one unit per the register's one packet.
