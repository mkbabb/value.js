SERVED MODEL: claude-fable-5-1

# KF.W12 — Authoring-Surface Repair

**Wave**: KF.W12 · Authoring-Surface Repair — the SIX NO-WAVE-OWNER authoring UNIT packets of RULINGS R-15's canonical seventeen (`KF-W10.md §6.D` row 2; homed at `KF-W4 §Sequencing`, the R-15 homing block; rostered at `KF-W1 §Cross-edges` edge 9): **CARD-UNIT · OPTIONS-UNIT · KFED-UNIT · EDITOR-UNIT · APPLY-UNIT · AXISLINE-UNIT**. Each UNIT was minted at its home record as *"a proposed single wave-surface … for SS-1/SS-2 spec authoring to consume; no census KF.W0–W10 row names this repair, and scattering it would orphan its one root cause"* — this file is that consumption. Repair, not evaluate: the cure shapes are ruled at the records; this file HOMES, ORDERS and BOUNDS them, re-derives nothing, mints nothing, and re-books nothing a sibling wave owns (register row 2's ANTI-RE-BOOK, honoured below).
**Half**: frontend (keyframes.js demo). **Sub-tranche**: X·KF. **Track**: B.
**Status**: planned. No product byte is written by this file; authoring it opened no product source for writing; execution awaits the sequencing head (§State).
**Ref of record**: keyframes.js `origin/master` **`69095552`** — ⟨cmd⟩ `git -C /Users/mkbabb/Programming/keyframes.js rev-parse --short origin/master` → `69095552` (2026-09-18, `test(kf/dialog-pair): G12 … (KF.W8 .g)`); local `master` = the same; `git status --porcelain` → two untracked coordination letters, zero modified product paths. The home records were adjudicated at worktree-on-`8281638c` (41 behind the then-frontier) and several of their BLOCKERs were **already partly cured at the frontier** (kf-KeyframeCardList's FE-3 `startScalar`; kf-KeyframesEditor's EE-03) — **every such row lands here as a RE-REGRESSION GUARD, not a cure** (the records' own instruction), and every line anchor is re-resolved at open (KF.W0 §B-12; D-19). The dated observations this seat measured are printed at their packets.
**Standing supersession rider (KF-AV-28)** — register row 2, lock 1: *"on any W7-evaluated surface a packet touches."* **Measured: none of the six UNITs touches a KF.W7-evaluated surface** (the six surfaces are the timeline cluster, SequenceScrubber, AnimationVisualizer, and the SpringTarget/SpringTrace idiom arms; this wave's bounds contain none of them). The rider therefore governs no row here and is named so its absence is a measurement, not an omission. **The KFED-UNIT second-end note (lock 3)**: KF.W7 cites *"the R4-8 §6.D register row"* as KFED-UNIT's second end *until the successor spec exists* — **this file is that spec**, and its §Sequencing `→ KF.W7` row is the reciprocal end; W7 promised nothing and is owed nothing (*cite it, promise nothing*).
**Provenance**: authored BLIND by the fable arm of the SS-1/SS-2 authoring block (COHESION §0d; `EXECUTION-RUNBOOK.md §4.5`; M-23 §3 — the Opus arm's draft was not read) from: the register of record `KF-W10.md §6.D` row 2; the minting citations; the home records `docs/tranches/V/megatranche/registry/adjudicated/kf-{KeyframeCardList,KeyframeCard,ChannelOptions,LayerConfigPanel,TimingFunctionPanel,ControlsPaneWrapper,KeyframesEditor,CSSCodeEditor,KeyframesStringControls,RibbonBar,CubeAxisLines}.md` — their UNIT routing lines are the rosters, consumed **by mechanism, deduped by identity, never transcribed** (M-25); `execution/B/KF-W7.md` (the verdict table, for the rider's measurement); and the live keyframes tree, READ-ONLY. Validated against `docs/precepts/instructions/tranche/WAVE_SPEC.md` (L-20). **No gate exists here without a named live witness (L-19).**

| verb | state | basis |
|---|---|---|
| AUDITED | YES | the eleven home records above; the six UNIT mints quoted by command at §Carry — ⟨cmd⟩ `grep -l 'CARD-UNIT\|OPTIONS-UNIT\|KFED-UNIT\|EDITOR-UNIT\|APPLY-UNIT\|AXISLINE-UNIT' registry/adjudicated/kf-*.md \| wc -l` → **13** · **13** records name at least one UNIT by its token: **nine** of the eleven homes (kf-KeyframeCardList · kf-ChannelOptions · kf-LayerConfigPanel · kf-TimingFunctionPanel · kf-KeyframesEditor · kf-CSSCodeEditor · kf-KeyframesStringControls · kf-RibbonBar · kf-CubeAxisLines) **+ four** that route single rows into a UNIT (kf-EasingSidebar · kf-EasingScene · kf-PlaybackRibbon · kf-SpringPhysicsFacet — each single row is booked at its UNIT below or excluded with its reason); **kf-KeyframeCard and kf-ControlsPaneWrapper name no UNIT token** and are consulted by seam (the card seam; the `proof:stage-visible` residue), which is why the home count is eleven and the token count thirteen ⟨this seat's first draft printed **15** from an unverified sum; WRITE-THEN-MEASURE corrected it⟩ |
| SPECIFIED | YES — this file (a draft: the authoring block's fable arm; ratification is the block's fold seat) | **the packet denominator is the register's — 6 of 17; carriage over row-ids is NOT asserted in this file's voice (LAW B)** — the rows land by their home records' routing lines cited at §Carry; the carriage census is the fold seat's artefact |
| IMPLEMENTED | NO | stays NO until the gates green after the sequencing head lands |
| VERIFIED | NO | a successor close's act; **KF.W10's close stamps the AUTHORED ELEVEN only** (COHESION §0d) |

**Execution shape**: 7 seats across 4 phases — phase 1: 2 parallel (CARD-UNIT ∥ OPTIONS-UNIT); phase 2: 2 parallel (KFED-UNIT — after CARD, same files ∥ EDITOR-UNIT); phase 3: 2 parallel (APPLY-UNIT — after KFED and EDITOR, shared composables ∥ AXISLINE-UNIT — after KF.W11 `.a`); phase 4: 1 serial (close). Peak concurrency **2**, inside the four-workflow cap.

---

## §State

**Name**: KF.W12 — Authoring-Surface Repair
**Opens after**: **G-KFW4-1 lands GREEN** (the sequencing head — *"nothing in it is defensible until a `.vue` file can fail a build"* is every one of the six records' own precondition; **measured RED at this seat, twice**: ⟨cmd⟩ `npx vue-tsc --noEmit -p tsconfig.json 2>&1 \| grep -c 'error TS'` → **54** · **54**; the LEDGER's KF.W4 row is `CLOSED 2026-09-17 (honest-RED: G-KFW4-1 …)`) · **KF.W6 CLOSED** (the `EditorHeader.vue` delete + the R4-1 atomic commit — register row 2's ANTI-RE-BOOK; W6's rename of the demo focus ring to `kf-focus-ring` re-anchored two of this wave's files) · **KF.W8 CLOSED** (the card cluster moved: `KeyframeCard.vue` sits at `keyframes/components/` by `34b051eb`) · **KF.W11 `.a`'s OD latch-family commit** (AXISLINE-UNIT's upstream half; the AXISLINE unit alone waits on it) · **KF.W11 `.b`'s `useKeyframeOps.ts` L-2 commit** (the shared-path seam; KFED-UNIT alone waits on it).
**Agents**: 7 (a→g); phases as above; **no jury**.
**Hard gate**: **seven conditions, ALL RED**, measured at `69095552` and pasted at §Gates, each double-run.
**Status**: planned

## §Goal criterion

This wave succeeds if, when work ends, the keyframes authoring loop is honest end to end: a card's offset reads, writes and removes (CARD); the options card's dropdowns open, its switch switches, and every widget re-renders when the engine writes (OPTIONS — and never before the render edge exists, LP-1); the retiming control retimes, Apply applies, and the action cluster is reachable (KFED); the code editor is a language-aware, keyboard-escapable editor whose format path fails loudly (EDITOR); the applied stylesheet binds the class it adds under one name with one lifetime (APPLY); and the axis-lock reveal reveals what it claims on the demo's one keyboard registry (AXISLINE). **A wave that restores the write legs before the render edge, or lands a second name derivation beside `cssIdent`, has failed this goal** whatever its gate count says.

## §0 Open preconditions (checked at wave-open; NOT close gates)

| # | precondition | state at authoring |
|---|---|---|
| **OP-0** | **G-KFW4-1 GREEN** — the head; every UNIT record names it as its own precondition. | **RED — 54 · 54**. |
| **OP-1** | **KF.W11 `.a` — the OD latch family (KF-AX-1's registry adoption + blur clear + expose reset) is LANDED** with its sha in KF.W11's record. | **UNLANDED** (KF.W11 is MINTED-UNAUTHORED at this clock; its draft names the commit). Measured today: ⟨cmd⟩ `grep -c 'useEventListener(window, "keydown"' demo/scenes/cube/orbital-drag/OrbitalDrag.vue` → **1** — the window latch is live. AXISLINE-UNIT (`.f`) does not open until this reads **0** by KF.W11's commit; **this wave writes no `orbital-drag/**` byte**. |
| **OP-2** | **KF.W11 `.b` — the `useKeyframeOps.ts` L-2 carve is LANDED.** | **UNLANDED**. KFED-UNIT (`.c`) opens on that sha; the two waves never write the file in parallel (§Disjointness). |
| **OP-3** | **KC-34's edict** (register row 2, the OPTIONS/CARD internal locks): *the highlight path must stop replacing Vue-owned DOM BEFORE the KC-8/KC-9 keep-mounted repair lands.* | **BY CONSTRUCTION** — `.a`'s first commit is KC-34; KC-8/KC-9 are its third. Named here so the order is a gate, not a habit. |
| **OP-4** | **The `cssIdent` publication** — kf-KeyframesStringControls `:55`: *"library half (publish `cssIdent`) → KF.W5; demo half (route all three derivations through it) → APPLY-UNIT, sequenced after the publication."* | **MEASURED PRESENT AT THE ARTIFACT THIS SEAT, twice**: ⟨cmd⟩ `grep -c 'cssIdent' dist/keyframes.d.ts` → **2** · **2** — the publication the record sequenced APPLY-UNIT after appears landed at `69095552`'s built types (a dated observation; **re-measured at `.e`'s open** and the export's exact shape read there, never inherited from this cell). If it reads **0** at open, APPLY-UNIT lands N-8's one-name rule against the demo's existing in-tree derivation and names KF.W5's export as the edge it consumes later — **never a second hand-rolled derivation** (N-8's own constraint). |
| **OP-5** | **The glass-ui producer half of KF-CO-1/KF-CO-8** — `open`/`modelValue` Boolean-cast props need `default: undefined` in LabeledSelect/LabeledSwitch (a 7.0.0 defect; the BH relay note is *already filed* per kf-LayerConfigPanel LP-3). | **PRODUCER-SIDE, NOT BLOCKING**: the consumer cure (`:is-open`→`:open` ×3; `:checked`→`:model-value`) is ours and lands regardless; the producer row rides the SS-6 accretion register (lock 2). Glass-ui is READ-ONLY always. |

---

## §Scope

1. **CARD-UNIT** — *"the keyframe authoring loop made honest"* across `KeyframeCardList.vue` + `KeyframeCard.vue` + `KeyframesEditor.vue`'s card seam: KC-34 first (the highlight path stops replacing Vue-owned DOM), then the offset readout (KC-1 — **PARTLY CURED at the frontier by FE-3's `startScalar`**, measured: ⟨cmd⟩ `grep -c 'startScalar' demo/components/instrument/keyframes/components/KeyframeCardList.vue` → **2**; the surviving obligations are fraction-for-percent, the named-selector fallthrough, and the canonical renderer still bypassed — `selectorText` is the one serializer), the offset writer (**KC-2 ≡ KF-KE-2 — ONE identity, ONE cure, homed HERE**: `KeyframesEditor.vue:81` `frame.start.value = starts![i]` writes into a deep-frozen parse result; read via `selectorPercent`, write by whole-selector replacement, domain `0..100`, fractional step, marks, per-thumb `aria-valuetext`), the delete affordance (KC-10, with KF-CB-11's a11y shell at its bank), remount discipline (KC-8/KC-9 keep-mounted), the a11y triad (KC-27's offset-bearing names, after KC-1), the ref bookkeeping (KC-17/KC-18/KC-32), and the tail (KC-3..KC-36 as routed).
2. **OPTIONS-UNIT** — in the record's FIXED ORDER: (i) one-line capability restorations — `:is-open`→`:open` ×3 (`ChannelOptions.vue:96/:120/:479`, measured) and the switch `model-value` rewire (`LayerConfigPanel.vue:90-91` `:checked`/`@update:checked`, measured) — **landed in the SAME commit as LP-1's write→render edge** (the lock: *"the write legs must not be restored without the render edge"*; the whole prop surface, `blendAvailable` included); (ii) persist/representation/authority — KF-CO-3, KF-CO-4, KF-CO-11, KF-CO-12, KF-CO-13, KF-CO-14, KF-CO-16, KF-CO-17; (iii) the steps cluster — KF-CO-9 (the stored default 100 vs the vendor domain [1,12]) + KF-CO-10 + KF-TFP-5≡KF-CO-40 (child owns the store, parent owns the engine — the steps arm's shape) + KF-TFP-6≡KF-CO-9; (iv) a11y — KF-CO-5 ≡ KF-TFP-7 with the KF-CO-46 cure rider (`inert`, not bare class toggles; the house cure sits at `ChannelControls.vue` `:inert="!keyframesActive"`); (v) hygiene — KF-CO-2 (the phantom-prop family, 21 bindings), KF-CO-6, KF-CO-21..29, KF-CO-31, KF-CO-33..36, KF-CO-38, KF-CO-39, L·N-16, KF-CO-41/42/44/45, LP-6/LP-9/LP-11..18/LP-21..23; plus **KF-CO-15**'s options-side half (the sync bridge polls `effectiveT`/`started`/`reversed` and not `options.duration`) — **its ribbon-side extension is KF.W13 `.b`'s C-2 joint commit, which opens after this unit** (§Sequencing); plus kf-ControlsPaneWrapper's `proof:stage-visible` residue (KF-W4 §Excluded 19: ⟨cmd⟩ `git grep -c 'proof:stage-visible' HEAD -- demo` → `ControlsPaneWrapper.css:1`).
3. **KFED-UNIT** — *"the keyframes authoring surface made honest"*: KF-KE-3 (`requireKeyframeSelector` at the field; `parseCssScalar` measured at `KeyframesEditor.vue:240/:303`), KF-KE-5 (the action cluster painted UNDER the opaque code plate — `class="absolute top-2 right-4 …"` with no z rung beside a `<pre … relative>`; measured), KF-KE-7 (the 700 ms unskippable gate before `removeKeyframeData` + KF-CB-11's shell, together), KF-KE-8 (PRM on the three engine animations), KF-KE-10..14, -17..22, -24's **pipeline half** (the hljs layer / re-tokenize pipeline — the TOKEN decision alone is KF.W6's rider), -25..29, -33..35, -37, -38, -41, -44, -46..52, -54..58, -60..66; KF-KE-62 / KF-KC-35's `proof:accent-census` residue (KF-W4 §Excluded 18: `KeyframeCard.vue:1` hit). **Identity dedupes stated**: KF-KE-2 → `.a` (above); **KF-KE-4 (Apply-CSS has never applied anything: class ≠ selector) and KF-KE-6 (the unlifted shadow state) → `.e` APPLY-UNIT** — one apply identity, one lifetime, homed once.
4. **EDITOR-UNIT** — *"the Monaco seam made honest"*: the KF-CE-1/-3/-4 decision — (a) restore the contribution set, or (b) hand-register a Monarch tokenizer + `setLanguageConfiguration` + `tabFocusMode: true` — taken ONCE in `.d`'s receipt before a byte (the killed register's own fork; measured: ⟨cmd⟩ `grep -c 'tabFocusMode\|accessibilitySupport' demo/components/instrument/keyframes/CSSCodeEditor.vue` → **1**, the dynamic `import("monaco-editor")` at `:37-40`); KF-CE-8 (the external-sync watch rewrites under the caret; **settled WITH the model→editor re-projection contract** kf-KeyframesStringControls `:47` requires); **KF-CE-9 + KF-CE-37 as one boundary** (`formatCSSContent` has no error path; `isFormatting` sticks — `KeyframesStringControls.vue:81-111`, measured 4 sites; `RibbonBar.vue`'s Format button is the PRIMARY unguarded path — ≡ kf-RibbonBar M-3/C-8's format half); KF-CE-2, -5..-7, -10, -11, -13..-15, -18..-21, -24..-29, -31, -33..-36, -38..-41, -43..-45, -47..-49; KF-CE-16 is KF.W4's (booked once there, never re-booked).
5. **APPLY-UNIT** — *"the apply-CSS toggle, the name pipeline, the brush, the teardown"*: **N-8's one-name rule** (`getClassName` = `options.styleId` = `keyframes-style-${uuid}` vs the emitted selector `getTmpAnimationName()` = the same string stripped and `.toLowerCase()`d — `useKeyframesState.ts:16/:42`, `useKeyframeBrushApply.ts:33`, measured; ONE `cssIdent`-derived name, per OP-4), which lands KF-KE-4 ≡ L-BL-1 by identity; KF-KE-6's lift (`isApplied`/`prevPaused`/node ownership to the per-animation altitude, refcounted); **RB-6** (the Apply state outlives its only control — the state and its affordance get one lifetime; `RibbonBar.vue:13/:49`); N-5 (`clear()` unwired); D-2/L-M-8, N-7, D-25 (`respectReducedMotion` on the brush and sweep — with KF-KE-8, one PRM motion), S-6-as-corrected (the `prevPaused` mechanism is right; keep it).
6. **AXISLINE-UNIT** — *"the axis-lock reveal made honest"*: KF-AX-1 (the demo-side half — consume the registry via KF.W11 `.a`'s adoption; measured today the reveal binds `lock.x/y/z` at `CubeAxisLines.vue:11-14` ×3, 4 `axis-line--locked` sites), KF-AX-2 (reveal semantics false three ways — rotate/translate/scale; armed-vs-active; the wheel sixth op — a prose/prop decision + the labels rider), KF-AX-4..-10, -13, -14, -16..-23, -28..-30; every geometric assertion written against the settled `rotate3d(-1,1,0,30deg)` frame.
7. **Close** — receipts, the commit roster audited to §Bounds, the BH relay (OP-5's producer row; SPF-4-class filters if met), E13, the LEDGER row.

### Triumvirate dispatch (WAVE_SPEC §3a)

- **Bounds whose expansion invalidates the wave**: any `src/**` write (`cssIdent`'s publication is KF.W5's); any `orbital-drag/**` write (KF.W11's); any `node_modules/monaco-editor` patch (a HIGH defect by standing law).
- **Non-local gate failures**: G-KFW12-2 failing because the render edge cannot be built without engine-side change (`invalidateEntries` flips dirty flags only) → triumvirate with KF.W5 named; G-KFW12-4 failing because option (a) restores bytes the bundle budget forbids and option (b) cannot cure the keyboard trap → triumvirate on the fork.
- **Diagnostic loops that halt at three**: the KF-CE-8 echo loop reproduced three times without a deterministic mount harness → halt.

---

## §Bounds

All paths keyframes.js-relative at `69095552`; line counts ⟨cmd⟩ `wc -l` this seat.

| File | L | Access | Unit |
|---|---|---|---|
| `demo/components/instrument/keyframes/components/KeyframeCardList.vue` | 110 | modify | `.a` |
| `demo/components/instrument/keyframes/components/KeyframeCard.vue` | 168 | modify — **SHARED `.a` → `.c`** (serial) | `.a` → `.c` |
| `demo/components/instrument/keyframes/KeyframesEditor.vue` | 430 | modify — **SHARED `.a` (the card seam: `:76-84` retiming Slider) → `.c` (the rest)** (serial) | `.a` → `.c` |
| `demo/components/instrument/keyframes/composables/useKeyframesEditor.ts` | 57 | modify-carve | `.c` |
| `demo/components/instrument/keyframes/composables/useKeyframeOps.ts` | 219 | modify — **AFTER KF.W11 `.b`'s L-2 hunk (`:58-69`) lands; that hunk is never re-touched** | `.c` |
| `demo/components/instrument/keyframes/composables/useToolbarKeyboard.ts` | 112 | modify-carve | `.c` |
| `demo/components/instrument/keyframes/components/KeyframesAddDialog.vue` | 289 | modify-carve (KFED rows naming the add dialog; KF.W7's KAD folds stay theirs) | `.c` |
| `demo/components/instrument/keyframes/composables/useApplyCSS.ts` | 69 | modify | `.e` |
| `demo/components/instrument/keyframes/composables/useKeyframeBrushApply.ts` | 46 | modify | `.e` |
| `demo/components/instrument/keyframes/composables/useKeyframesState.ts` | 61 | modify | `.e` |
| `demo/components/instrument/keyframes/composables/useHighlightCSS.ts` | 234 | modify — **SHARED `.a` (KC-34: stop replacing Vue-owned DOM) → `.e` (refcounted teardown)** (serial) | `.a` → `.e` |
| `demo/components/instrument/keyframes/composables/useKeyframesParsing.ts` | 118 | modify-carve | `.e` |
| `demo/components/instrument/keyframes/KeyframesStringControls.vue` | 195 | modify — **SHARED `.d` (the `isFormatting` latch, KF-CE-9) → `.e` (the apply/brush/teardown rows)** (serial) | `.d` → `.e` |
| `demo/components/instrument/keyframes/CSSCodeEditor.vue` | 261 | modify | `.d` |
| `demo/components/instrument/transport/controls-pane/RibbonBar.vue` | 151 | modify — **SHARED `.d` (M-3/C-8 format half, `:28`-class path) → `.e` (RB-6 apply half, `:13/:46/:49`)** (serial) | `.d` → `.e` |
| `demo/components/instrument/transport/channel-controls/ChannelOptions.vue` | 731 | modify | `.b` |
| `demo/components/instrument/transport/channel-controls/LayerConfigPanel.vue` | 124 | modify | `.b` |
| `demo/components/instrument/transport/channel-controls/TimingFunctionPanel.vue` | 173 | modify | `.b` |
| `demo/components/instrument/transport/channel-controls/composables/useTimingFunctionEditor.ts` | 248 | modify | `.b` |
| `demo/components/instrument/transport/channel-controls/ChannelControls.vue` | 384 | modify-carve (KF-CO-5/-46's `inert` posture; the L-2/C-2 `:key` row is KF.W7's OP-1 and is NOT touched) | `.b` |
| `demo/components/instrument/transport/controls-pane/ControlsPaneWrapper.{vue,css}` | 307 / 150 | modify-carve (`proof:stage-visible` residue; LP-1's `controlHosts` tracking at the render edge) | `.b` |
| `demo/scenes/cube/CubeAxisLines.vue` | 120 | modify | `.f` |
| `test/demo/instrument/keyframe-card-offset-loop.test.ts` | — | create | `.a` |
| `test/demo/instrument/channel-options-render-edge.test.ts` | — | create | `.b` |
| `test/demo/instrument/keyframes-editor-honest.test.ts` | — | create | `.c` |
| `test/demo/instrument/css-code-editor-seam.test.ts` | — | create | `.d` |
| `test/demo/instrument/apply-css-identity.test.ts` | — | create | `.e` |
| `test/demo/scenes/cube-axis-reveal.test.ts` | — | create | `.f` |
| `test/demo/instrument/{highlight-css-roundtrip,kf-toolbar-keyboard,ios-text-entry,value4-editor-boundary}.test.ts` | — | modify (existing witnesses extended, never weakened; `value4-editor-boundary.test.ts` is the non-deterministic fixture KF.W7 residual 15 names — **its timeout is NOT widened here**; if it is this wave's byte that makes it deterministic, the assertion is what changes) | owning unit |
| value.js `docs/tranches/X/execution/B/KF-W12.md` | — | create (unit receipts; append-only) | every unit |
| value.js `docs/tranches/X/execution/LEDGER.md` | — | modify (this wave's row cells + appended lines ONLY) | `.g` |
| value.js `docs/tranches/V/coordination/INBOX.md` + the SS-6 accretion register (COHESION §4a) | — | modify (append) | `.g` |

**Do NOT touch**: `src/**` (`cssIdent`, `selectorText`, `requireKeyframeSelector` are consumed, never edited) · `demo/scenes/cube/orbital-drag/**` (KF.W11 `.a`) · the `updateFromString` hunk of `useKeyframeOps.ts` (KF.W11 `.b`) · `demo/components/instrument/timeline/**` and `demo/components/playback/**` (KF.W7 / KF.W13) · `demo/app/**` (KF.W13) · `EditorHeader.vue` (DELETED by KF.W6 — **KF-APP-41 is `ADOPTED-BY-KF.W6`; no row here re-books it**; kf-EditorShell `C-22`'s orphan-token limb likewise; `C-22`'s `D-27` · `RR-2 M7` limbs stay NO-WAVE-OWNER and are NOT adopted here) · `node_modules/**` · `package.json` / `vitest.config.ts` (harness present; no manifest change owed) · every glass-ui tree · `scripts/dev/dev.sh` · every dated spec, the adjudicated registry, conformance artefacts (E-3).

### Disjointness (WAVE_SPEC §4a)

Five paths are shared and each is resolved SERIALLY by the phase order, never in parallel: `KeyframeCard.vue` and `KeyframesEditor.vue` (`.a` → `.c`), `useHighlightCSS.ts` (`.a` → `.e`), `KeyframesStringControls.vue` and `RibbonBar.vue` (`.d` → `.e`). `useKeyframeOps.ts` is a cross-wave serial path (KF.W11 `.b` → this `.c`). Phase 1's pair (`.a` ∥ `.b`) is directory-disjoint (`keyframes/` vs `transport/channel-controls/`); phase 2's pair (`.c` ∥ `.d`) shares nothing (`.d` owns `CSSCodeEditor.vue`, `KeyframesStringControls.vue`'s latch, `RibbonBar.vue`'s format half — `.c` touches none); phase 3's pair (`.e` ∥ `.f`) shares nothing.

### Worktree plan (WAVE_SPEC §4b)

Parallel units run in sibling worktrees `keyframes-kfw12-<unit>`, merged by the orchestrator in phase order; serial dependents open on the merged sha named in the predecessor's receipt. No Cargo.

---

## §Carry — the six UNITs, homed

Rosters are the home records' routing lines at `69095552`'s registry bytes (immutable, E-3); ids are the records' own, for life. Internal ordering edicts travel intact.

### U1 · CARD-UNIT — `kf-KeyframeCardList.md:30` (the mint) · `:135` (the verdict) · KC rows `:33-74`

**Order (KC-34's edict, then the loop)**: **KC-34** (D-17 — the blanking: the highlight path replaces Vue-owned DOM; it stops BEFORE keep-mounted) → **KC-1** (`selectorText(frames[i].start)` — the surviving obligations after FE-3, above; prerequisite for **KC-27**'s offset-bearing aria names, land together) → **KC-2 ≡ KF-KE-2** (the frozen-selector write — measured live ⟨cmd⟩ `grep -c 'frame.start.value = starts' demo/components/instrument/keyframes/KeyframesEditor.vue` → **1**; the ordering constraint is load-bearing: *fix the unit before the freeze, or the 100×-destructive write L-B1 warned of becomes reachable*) → **KC-8/KC-9** (keep-mounted; KC-9 is the adjudicator's own one-timer-for-N-cards data loss) → **KC-10** (removal; with KF-CB-11's shell at its bank) → the a11y triad **KC-27 · KC-28 · KC-29** → ref bookkeeping **KC-17 · KC-18 · KC-32** → **KC-3 · KC-4 · KC-5 · KC-6 · KC-7 · KC-11 · KC-12 · KC-14 · KC-15 · KC-16 · KC-19 · KC-22 · KC-23 · KC-24 · KC-25 · KC-26 · KC-31 · KC-33 · KC-36**; KC-13 (≡ KF-APP-4, the type gate) is KF.W4's. **kf-KeyframeCard.md** routes no row to CARD-UNIT by name (⟨cmd⟩ `grep -c 'CARD-UNIT' kf-KeyframeCard.md` → **0**); its NO-WAVE-OWNER rows enter by the card seam and are booked at `.a`'s receipt by id.

### U2 · OPTIONS-UNIT — `kf-ChannelOptions.md:25` (the mint) · `:45-99` · `:156` · `:198-203` · `kf-LayerConfigPanel.md:35` · LP rows · `kf-TimingFunctionPanel.md:45-47`

**Order (the record's, fixed)**: (i) **KF-CO-1** (`:is-open`→`:open` ×3 — also makes D-1 §4's mutex real) + **KF-CO-8 ≡ LP-3** (`:model-value` + `@update:model-value` on the switch) **+ LP-1 in the SAME commit** (the render edge: `layerConfig` is a plain object off a never-proxied graph; `controlHosts` tracks group identity only; `invalidateEntries` flips dirty flags — the cure covers the whole prop surface, `blendAvailable` and `singleTarget`'s post-mount recompute included; kin recorded, not folded: kf-ChannelControls L-16, KF-CO-15) → (ii) **KF-CO-3 · KF-CO-4 · KF-CO-11 · KF-CO-12 · KF-CO-13 · KF-CO-14 · KF-CO-16 · KF-CO-17** → (iii) **KF-CO-9 ≡ KF-TFP-6 · KF-CO-10 · KF-CO-40 ≡ KF-TFP-5** → (iv) **KF-CO-5 ≡ KF-TFP-7 + KF-CO-46** → (v) **KF-CO-2 · KF-CO-6 · L·B-2 · KF-CO-21 · KF-CO-22 · KF-CO-23 · KF-CO-26 · KF-CO-27 · KF-CO-28 · KF-CO-29 · KF-CO-31 · KF-CO-33 · KF-CO-34 · KF-CO-35 · KF-CO-36 · KF-CO-38 · KF-CO-39 · L·N-16 · KF-CO-41 · KF-CO-42 · KF-CO-44 · KF-CO-45 · LP-6 · LP-9 · LP-11..LP-18 · LP-21..LP-23**. **KF-CO-15**'s options half here; ribbon half KF.W13 `.b`. KF-CO-7 ≡ KF-APP-4 → KF.W4 (booked once). **Producer half → SS-6** (OP-5).

### U3 · KFED-UNIT — `kf-KeyframesEditor.md:36` (the mint) · `:41-114` · `:172` (the verdict)

**KF-KE-3** · **KF-KE-5** (BLOCKER: a z rung on the cluster or drop the `<pre>`'s `relative`; then D-4's shell and the annotation's contrast become live) · **KF-KE-7** (+KF-CB-11 shell; gate + shell together) · **KF-KE-8** (standalone animations take `respectReducedMotion: true`; the delete group `g.respectReducedMotion = true`; the sweep's rest state lands with the flag — KF-KE-21) · **KF-KE-10 · 11 · 12 · 13 · 14 · 17 · 18 · 19 · 20 · 21 · 22 · 24 (pipeline half) · 25 · 26 · 27 · 28 · 29 · 33 · 34 · 35 · 37 · 38 · 41 · 44 · 46 · 47 · 48 · 49 · 50 · 51 · 52 · 54 · 55 · 56 · 57 · 58 · 60 · 61 · 62 · 63 · 64 · 65 · 66**. **Dedupes**: KF-KE-2 → U1; KF-KE-4 and KF-KE-6 → U5 (identity: L-BL-1 / RB-6); KF-KE-14 ≡ KF-APP-4 → KF.W4; EE-03's length-watch (frontier-cured) → re-regression guard only. **Sequenced after KF.W0** (the record: *"two blockers are already repaired at the frontier; a careless rebase re-lands them"* — the guard is the obligation) and after OP-2.

### U4 · EDITOR-UNIT — `kf-CSSCodeEditor.md:30` (the mint) · `:33-87` · `kf-RibbonBar.md:48` (M-3/C-8 format half)

**The KF-CE-1/-3/-4 fork** — decided once, in the receipt, before any byte: (a) restore contributions (bytes return, a11y contribs return) or (b) Monarch tokenizer + `setLanguageConfiguration` + `tabFocusMode: true` (bytes stay saved; the keyboard trap is cured by option; comment-toggle/find/context-menu stay absent and are SAID to be absent) · **KF-CE-8 + the re-projection contract** (kf-KeyframesStringControls `:47`) · **KF-CE-9 + KF-CE-37 ≡ RB M-3/C-8 — one boundary** (an error path for `formatCSSContent`; the latch released on rejection; the Format button awaited; the `withErrorToastAsync` idiom one file over is the house form) · **KF-CE-2 · 5 · 6 · 7 · 10 · 11 · 13 · 14 · 15 · 18 · 19 · 20 · 21 · 24 · 25 · 26 · 27 · 28 · 29 · 31 · 33 · 34 · 35 · 36 · 38 · 39 · 40 · 41 · 43 · 44 · 45 · 47 · 48 · 49**. KF-CE-16 → KF.W4. Single-row routings from kf-EasingSidebar / kf-EasingScene into EDITOR-UNIT are booked at `.d`'s receipt by id or excluded with reason.

### U5 · APPLY-UNIT — `kf-KeyframesStringControls.md:17` (the mint) · `:41` · `:44` · `:55` · `:57-67` · `kf-RibbonBar.md:76` (RB-6) · `:135`

**N-8, first** — ONE `cssIdent`-derived name shared by `getClassName` and the emitted selector (measured: `useKeyframesState.ts:16` `keyframes-style-${animationUUID}` · `:42` `.replace("keyframes-style-", "").toLowerCase()` · `useKeyframeBrushApply.ts:33` `getClassName: () => options.styleId` — *"keyframes-style-"+X ≠ X.toLowerCase() unconditionally*), which is **KF-KE-4 ≡ L-BL-1 landed by identity** (then L-M3/C-B2's residue re-derived against a working feature) · **KF-KE-6's lift** (per-animation, refcounted `isApplied`/`prevPaused`/node ownership; `useHighlightCSS.ts:58-61` removes the node unconditionally, no refcount) · **RB-6** (one lifetime for the state and its affordance) · **N-5** (`clear()` wired or deleted) · **D-2/L-M-8 · N-7 · D-25 (+KF-KE-8's PRM motion, one commit)** · **S-6-as-corrected** kept · the `:57`/`:59`/`:65`/`:67` rows as routed. **OP-4 governs the derivation source.**

### U6 · AXISLINE-UNIT — `kf-CubeAxisLines.md:34` (the mint) · the roster `KF-AX-1 · 2 · 4 · 5 · 6 · 7 · 8 · 9 · 10 · 13 · 14 · 16 · 17 · 18 · 19 · 20 · 21 · 22 · 23 · 28 · 29 · 30` (⟨cmd⟩ `grep '^| KF-AX' kf-CubeAxisLines.md \| grep -c AXISLINE` → **22**)

**KF-AX-1 first** — the demo-side half: the reveal consumes the ONE registry (KF.W11 `.a` adopted it upstream in `orbital-drag/`; `allowInInput`, keyup events, modal are the registry's own cures) · **KF-AX-2** (semantics: the reveal claims a rotate lock and the drag also constrains translate/scale, armed-vs-active is conflated, the wheel is a sixth op — one prose/prop decision; the deuteranopia labels rider rides J-lane C6's ask) · **KF-AX-9** (the opacity-grouping cure, its benefit as corrected) · the rest by id. Every geometric assertion against `rotate3d(-1,1,0,30deg)`. Discrete rows to KF.W0 / KF.W6 / KF.W9 stay theirs.

---

## §Agent Units

Each unit: Goal · Mechanism · Files · Sub-gate. Receipts to `execution/B/KF-W12.md`, `SERVED MODEL: <id>` line 1, commands double-run, inherited paths named (CRASH-RECOVERY). Out-of-bounds = ESCALATION, returned.

### KF.W12.a — CARD-UNIT (phase 1)
- **Goal**: a keyframe card's offset reads the selector the engine holds, writes it back through the selector grammar, and can be removed from the keyboard.
- **Mechanism**: U1 in KC-34's order; the frozen-selector write replaced by whole-selector replacement via `selectorPercent`/`percentSelector`; keep-mounted; the a11y triad after KC-1.
- **Files**: the `.a` rows; `test/demo/instrument/keyframe-card-offset-loop.test.ts` (create).
- **Sub-gate**: G-KFW12-1 GREEN; the receipt records the shas `.c` and `.e` open on.

### KF.W12.b — OPTIONS-UNIT (phase 1)
- **Goal**: the options card's controls are live in both directions and re-render on engine writes.
- **Mechanism**: U2 in the fixed order; commit 1 = KF-CO-1 + KF-CO-8 + LP-1 (one commit, the lock); then (ii)–(v).
- **Files**: the `.b` rows; `test/demo/instrument/channel-options-render-edge.test.ts` (create).
- **Sub-gate**: G-KFW12-2 GREEN; the producer row appended to the SS-6 register at `.g` with this unit's evidence.

### KF.W12.c — KFED-UNIT (phase 2; after `.a` and after KF.W11 `.b`)
- **Goal**: the retiming control retimes, the start field validates through the keyframe grammar, the action cluster is reachable, and delete is a real control.
- **Mechanism**: U3; KF-KE-5's z rung; KF-KE-3's `requireKeyframeSelector`; KF-KE-7's gate + shell; KF-KE-8's PRM; the pipeline half; the tail.
- **Files**: the `.c` rows; `test/demo/instrument/keyframes-editor-honest.test.ts` (create).
- **Sub-gate**: G-KFW12-3 GREEN (all clauses).

### KF.W12.d — EDITOR-UNIT (phase 2)
- **Goal**: the code editor tokenizes CSS, can be left by keyboard, and its format path fails loudly and releases its latch.
- **Mechanism**: U4; the fork in the receipt first; KF-CE-9 + KF-CE-37 as one boundary; KF-CE-8 with the re-projection contract.
- **Files**: the `.d` rows; `test/demo/instrument/css-code-editor-seam.test.ts` (create).
- **Sub-gate**: G-KFW12-4 GREEN.

### KF.W12.e — APPLY-UNIT (phase 3; after `.c` and `.d`)
- **Goal**: Apply applies — the class added is the selector emitted — under one name and one lifetime.
- **Mechanism**: U5; N-8 first with OP-4's source measured; KF-KE-6's lift; RB-6; N-5; the PRM motion.
- **Files**: the `.e` rows; `test/demo/instrument/apply-css-identity.test.ts` (create).
- **Sub-gate**: G-KFW12-5 GREEN (both clauses); the receipt prints OP-4's grep.

### KF.W12.f — AXISLINE-UNIT (phase 3; after KF.W11 `.a`)
- **Goal**: the axis-lock reveal reveals exactly what the drag constrains, on the one registry.
- **Mechanism**: U6; KF-AX-1's consumer half; KF-AX-2's decision; the roster.
- **Files**: `CubeAxisLines.vue`; `test/demo/scenes/cube-axis-reveal.test.ts` (create).
- **Sub-gate**: G-KFW12-6 GREEN; OP-1's grep printed at 0 in the receipt.

### KF.W12.g — Close (phase 4; serial, last)
- **Goal**: record whole, commits audited, producer rows sailed, LEDGER row moved.
- **Mechanism**: all seven gates re-run double; `git show --stat` per sha against §Bounds; the SS-6 accretion entries; E13 four-path sweep; LEDGER cells + event line; residuals/escalations.
- **Files**: the value.js rows.
- **Sub-gate**: G-KFW12-7 GREEN.

### Standing law for every unit

As KF-W11's — CRASH-RECOVERY first · the spec GOVERNS, no masking fallback (no `try/catch` around a defect, no `test.skip`, no allowlist, no copied producer selector, no `node_modules` patch) · pathspec commits, families unsplit · E-3 · WRITE-THEN-MEASURE double-run · quote-by-command · sibling trees and glass-ui READ-ONLY · E13 · probe parsimony · `dev.sh` never staged · LEDGER minimal edits only.

---

## §Gates — seven, all born RED at `69095552`, literal commands, double-run

All commands from `/Users/mkbabb/Programming/keyframes.js`. Runtime gates are `vitest` runs; byte clauses ride beside them, never alone.

**G-KFW12-1 — CARD: the offset loop round-trips.** ⟨cmd⟩ `npx vitest run --project demo test/demo/instrument/keyframe-card-offset-loop.test.ts` → RED today: `No test files found` (⟨cmd⟩ `ls test/demo/instrument/keyframe-card-offset-loop.test.ts` → `No such file or directory`, twice). GREEN when: a `from`/`50%`/`to` card renders `selectorText` of its frame; a retiming write lands as a whole-selector replacement (no `TypeError` on a frozen result); delete is reachable from the keyboard. Byte clause: ⟨cmd⟩ `grep -c 'frame.start.value = starts' demo/components/instrument/keyframes/KeyframesEditor.vue` → **1** · **1** (must read **0**).

**G-KFW12-2 — OPTIONS: live in both directions, on a render edge.** ⟨cmd⟩ `npx vitest run --project demo test/demo/instrument/channel-options-render-edge.test.ts` → RED today: `No test files found`. GREEN when an engine-side `setLayerConfig` re-renders the weight slider and the switch, and the direction dropdown opens on click. Byte clauses: ⟨cmd⟩ `grep -c ':is-open=' demo/components/instrument/transport/channel-controls/ChannelOptions.vue` → **3** · **3** (must read **0**); ⟨cmd⟩ `grep -c '@update:checked' demo/components/instrument/transport/channel-controls/LayerConfigPanel.vue` → **1** · **1** (must read **0**). **Lock clause**: the commit that turns either byte clause green also contains the render-edge hunk (`git show <sha> --stat` names both files) — a byte clause green in a commit without the edge is a FAILED gate, not a green one.

**G-KFW12-3 — KFED: honest surface.** ⟨cmd⟩ `npx vitest run --project demo test/demo/instrument/keyframes-editor-honest.test.ts` → RED today: `No test files found`. GREEN when the start field rejects `500%` at the field and accepts `from`, the action cluster is hit-testable above the code plate, and delete is a named focusable with a skippable gate. Byte clauses: ⟨cmd⟩ `grep -c 'parseCssScalar' demo/components/instrument/keyframes/KeyframesEditor.vue` → **2** · **2** (must read **0**); ⟨cmd⟩ `grep -c 'class="absolute top-2 right-4' demo/components/instrument/keyframes/components/KeyframeCard.vue` → **1** · **1** (must read **0** — the cluster carries a z rung, or the plate loses `relative`).

**G-KFW12-4 — EDITOR: tokenized, escapable, loud on format failure.** ⟨cmd⟩ `npx vitest run --project demo test/demo/instrument/css-code-editor-seam.test.ts` → RED today: `No test files found`. GREEN when a rejected `formatCSSContent` surfaces an error and leaves `isFormatting` false, Tab leaves the editor (or `tabFocusMode` is on and announced), and the CSS tokenizer produces ≥ 2 token classes for `a { color: red }`. Byte clause: ⟨cmd⟩ `grep -c 'isFormatting' demo/components/instrument/keyframes/KeyframesStringControls.vue` → **4** · **4** (recorded; the acceptance is the runtime clause, not a count).

**G-KFW12-5 — APPLY: the class added is the selector emitted.** ⟨cmd⟩ `npx vitest run --project demo test/demo/instrument/apply-css-identity.test.ts` → RED today: `No test files found`. GREEN when, for an animation whose id contains uppercase and a `keyframes-style-` prefix, `getClassName()` strictly equals the selector name inside `getCSSString()` and the target animates under the injected sheet; and unmounting one of two owners leaves the sheet for the other. Byte clause: ⟨cmd⟩ `grep -c 'toLowerCase()' demo/components/instrument/keyframes/composables/useKeyframesState.ts` → **1** · **1** (must read **0**: one `cssIdent`-derived name). Clock clause: ⟨cmd⟩ `grep -c 'cssIdent' dist/keyframes.d.ts` printed at open and close.

**G-KFW12-6 — AXISLINE: the reveal is true and on the one registry.** ⟨cmd⟩ `npx vitest run --project demo test/demo/scenes/cube-axis-reveal.test.ts` → RED today: `No test files found`. GREEN when the reveal lights only the axis the drag constrains, in the `rotate3d(-1,1,0,30deg)` frame, and a registry-dispatched lock (not a window keydown) drives it. Inbound clause: ⟨cmd⟩ `grep -c 'useEventListener(window, "keydown"' demo/scenes/cube/orbital-drag/OrbitalDrag.vue` → **1** · **1** today (must read **0** by KF.W11 `.a` — this wave does not write it).

**G-KFW12-7 — close: green demo project, unmoved head.** ⟨cmd⟩ `npm run test:demo 2>&1 \| tail -3` → RED by construction (six absent files). GREEN when every `test/demo/**` file passes; ⟨cmd⟩ `git diff <open-sha>..HEAD -- test | grep -c 'test.skip\|it.skip\|\.only('` → **0**; ⟨cmd⟩ `npx vue-tsc --noEmit -p tsconfig.json 2>&1 | grep -c 'error TS'` → **0** (OP-0 re-run at close).

**GREEN-BEFORE-CURE (R.2)** as KF-W11's: a gate green at open without this wave's cure is booked `LANDED-BY <sha>`, never claimed.

---

## §Sequencing — locks and cross-edges

### Intra-wave order (binding)

1. **`.a` ∥ `.b` → `.c` ∥ `.d` → `.e` ∥ `.f` → `.g`.** Peak 2.
2. **KC-34 before KC-8/KC-9** inside `.a` (OP-3).
3. **LP-1 in the same commit as KF-CO-1/KF-CO-8** inside `.b` (the write→render lock).
4. **`.c` after `.a`** (shared card seam) **and after KF.W11 `.b`** (the `useKeyframeOps.ts` hunk).
5. **`.e` after `.c` and `.d`** (shared composables and `KeyframesStringControls.vue`/`RibbonBar.vue`); N-8 before every other `.e` row.
6. **`.f` after KF.W11 `.a`** (OP-1).

### The travelling locks (register row 2 — three named there; all six accounted)

- **KF-AV-28's rider** — measured to govern no row here (no W7-evaluated surface in bounds); named.
- **Glass-producer rows → SS-6** — OP-5's `default: undefined` ask (KF-CO-1/KF-CO-8's producer half); SPF-4-class Chip filter if the OPTIONS a11y leg meets it; relayed at `.g`.
- **The KFED-UNIT second-end note** — this file is the second end; `→ KF.W7` below.
- **LP-1's write→render ordering** — enforced at order 3 and at G-KFW12-2's lock clause.
- **Comment-stated invariants are test obligations** — binds every docblock `.a`..`.f` touches (e.g. `MbabbMenu`-class comments are KF.W13's; here, `KeyframeCard.vue`'s KF-KC-4 comment block and `useApplyCSS.ts`'s "Unapply: restore previous pause state" become assertions).
- **MbabbMenu MUST-CARRY · TD-1/TD-2 bundling** — KF.W13's; not this wave's surfaces; named for the count.

### Cross-edges, declared from this end

| to | type | declaration |
|---|---|---|
| **KF.W4** | **DEPENDS ON (hard) — the sequencing head** | every UNIT record names the vue-tsc gate as its precondition; measured RED (54). |
| **KF.W6** | **DEPENDS ON (met) + ANTI-RE-BOOK honoured** | `EditorHeader.vue` deleted + C-22's orphan-token limb adopted there (R4-1); no row here re-books KF-APP-41 or that limb; `kf-focus-ring` is the demo's focus ring name in every file this wave touches. |
| **KF.W7** | **RECIPROCAL END (the KFED-UNIT second-end note)** | W7 cited the §6.D row *until the successor spec exists*; this file exists; W7's OP-1 (`kf-ChannelControls` L-2/C-2 `:key`) is NOT touched here; the KAD folds (`R-7 ≡ KAD-F3`, KAD-6) stay W7's. |
| **KF.W8** | **DEPENDS ON (met)** | the card cluster's location (`34b051eb`); the serializer-unification body `.a`'s `selectorText` consumes. |
| **KF.W11** | **DEPENDS ON (two commits) + SHARES ONE PATH SERIALLY** | `.a`'s OD latch family (OP-1) → this `.f`; `.b`'s L-2 hunk (OP-2) → this `.c`. Declared at both ends. |
| **KF.W13** | **SUPPLIES (ordering)** | KF-CO-15's options half here; W13 `.b`'s C-2 + KF-CO-15 joint commit opens after `.b` closes, because a reactive `:max`/one-duration-read is not live until LP-1's edge exists. |
| **KF.W5 (library)** | **DECLARES (not owned)** | `cssIdent`'s publication (OP-4); any engine-side change the render edge needs. |
| **KF.W9 / SS-13** | **DEPENDS ON (witnesses)** | SS-13 #1 (KF-KE-5's plate), #2 (Apply's press signature), #4 (the KF-CE-8 loss), #5 (unmount ordering); KF-CO-9's staircase; LP-9's magnitude. |
| **KF.W10** | **REGISTERED AT** | `§6.D` row 2 — unchanged by this file. |
| **SS-6** | **SUPPLIES** | the producer rows at `.g`. |

---

## §Excluded

| excluded | reason |
|---|---|
| **KF-APP-41** (`EditorHeader.vue` delete) and **kf-EditorShell C-22's orphan-token limb** | `ADOPTED-BY-KF.W6` (RULINGS-4 R4-1) — register row 2's ANTI-RE-BOOK; `C-22`'s `D-27` · `RR-2 M7` limbs stay NO-WAVE-OWNER and are NOT adopted here (this note discharges nothing of theirs). |
| **KF-APP-4 ≡ KF-CE-16 ≡ KF-CO-7 ≡ KC-13 ≡ KF-KE-14** (the type gate) | KF.W4's, booked once at its row 1. |
| **KF-KE-24's TOKEN decision** | KF.W6's rider; only the pipeline half is here. |
| **kf-ChannelControls L-2/C-2 (`:key`)** | KF.W7's OP-1 (a settlement, not a cure here). |
| **Any `src/**` byte** — `cssIdent`, `selectorText`, `requireKeyframeSelector`, `invalidateEntries` | KF.W5 / KF.W8; consumed by import. |
| **`orbital-drag/**`** | KF.W11 `.a`; AXISLINE consumes by commit. |
| **The eleven packets not in this partition** | R-15's partition; no re-booking. |
| **Owner rulings** | none required by these six; `complete_with_misses` if one surfaces (e.g. the EDITOR fork's bundle-budget arm). |

---

## §Commit plan

One commit per meaning; families unsplit; pathspec on the commit itself; `Claude-Session:` trailer. Expected (keyframes.js):

1. `.a`: `fix(kf/card · X.KF.W12.a · KC-34 highlight stops replacing Vue DOM)` · `fix(kf/card · X.KF.W12.a · KC-1+KC-27 selectorText + offset names)` · `fix(kf/card · X.KF.W12.a · KC-2≡KF-KE-2 whole-selector write)` · `fix(kf/card · X.KF.W12.a · KC-8/KC-9 keep-mounted)` · `fix(kf/card · X.KF.W12.a · KC-10 removal + shell)` · `chore(… tail)` · `test(… G-KFW12-1)`.
2. `.b`: `fix(kf/options · X.KF.W12.b · KF-CO-1+KF-CO-8+LP-1 — capability restorations ON the render edge)` (one commit) · `fix(… persist/representation/authority)` · `fix(… steps cluster)` · `fix(… a11y inert)` · `chore(… hygiene)` · `test(… G-KFW12-2)`.
3. `.c`: `fix(kf/kfed · X.KF.W12.c · KF-KE-5 cluster above the plate)` · `fix(… KF-KE-3 requireKeyframeSelector)` · `fix(… KF-KE-7 gate+shell)` · `fix(… KF-KE-8 PRM)` · `refactor(… pipeline half)` · `chore(… tail)` · `test(… G-KFW12-3)`.
4. `.d`: `fix(kf/editor · X.KF.W12.d · KF-CE-1/3/4 <arm>)` · `fix(… KF-CE-9+KF-CE-37 one format boundary)` · `fix(… KF-CE-8 re-projection)` · `chore(… tail)` · `test(… G-KFW12-4)`.
5. `.e`: `fix(kf/apply · X.KF.W12.e · N-8 one cssIdent-derived name ≡ KF-KE-4/L-BL-1)` · `refactor(… KF-KE-6 lifted refcounted state)` · `fix(… RB-6 one lifetime)` · `fix(… N-5 clear + PRM motion)` · `test(… G-KFW12-5)`.
6. `.f`: `fix(kf/axis · X.KF.W12.f · KF-AX-1 consumer half)` · `fix(… KF-AX-2 semantics)` · `chore(… roster)` · `test(… G-KFW12-6)`.
7. value.js: `docs(x-kf-w12/<unit>): receipt` per unit; `docs(x-execution/ledger): KF.W12 row`; SS-6 accretion + INBOX append at `.g`.

## §Format and lint cadence · §Verification artefacts

`npm run check` + `npm run test:demo` after each unit's last commit and at close; `npx eslint demo/components/instrument demo/scenes/cube/CubeAxisLines.vue` at close; `git diff --check` per commit. Artefacts at close in `execution/B/KF-W12.md`: the seven gate transcripts (double-run), the commit roster with `git show --stat`, the EDITOR fork decision with its measured bundle delta, OP-4's grep at open/close, OP-1's grep at `.f`'s open, the SS-6 entries' ids, the E13 line.

## §L-18 rider (reference, not ceremony)

Green gates are not acceptance: the implementation must survive two challenging gestalt passes and a fresh-Fable agglomeration. Likely bases: (i) the write legs restored one commit before the render edge (G-KFW12-2's lock clause exists for this); (ii) a second name derivation beside `cssIdent` "for now" (N-8 forbids it by name); (iii) the EDITOR fork taken by bytes rather than by receipt — a tokenizer registered without the trap cured, or contributions restored without the budget stated; (iv) `value4-editor-boundary.test.ts`'s timeout widened to make G-KFW12-7 green (the masking class; KF.W7 residual 15's own law); (v) a KF-KE row cured in `.c` that U5 owns by identity (KF-KE-4/-6), landing the same feature twice.

**Dissents recorded, not resolved by this seat**: (1) whether OPTIONS-UNIT's 40-odd rows warrant two units (capability+authority ∥ hygiene) — this file keeps the record's *one* surface and its fixed order; (2) whether AXISLINE-UNIT belongs in KF.W11 beside its cube kin — R-15 partitioned it here and this file follows the partition, declaring the seam instead.
