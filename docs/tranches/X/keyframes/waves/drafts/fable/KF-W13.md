SERVED MODEL: claude-fable-5-1

# KF.W13 — Chrome, Dock & Transport Repair

**Wave**: KF.W13 · Chrome, Dock & Transport Repair — **the wave MINTED EXPLICITLY at `KF-W4 §Sequencing`** (*"a wave beyond the census sketch is minted explicitly, per KF-W1 cross-edge 9's cohesion flag"*; RULINGS-4 R4-8 arm (a): the mint STANDS), carrying the TWO NO-WAVE-OWNER chrome packets of RULINGS R-15's canonical seventeen (`KF-W10.md §6.D` row 3): **dock-menu · transport/ribbon**. Repair, not evaluate: the cure shapes are ruled at the home records; this file HOMES them with their two hardest travelling locks — **kf-ChromeDock M-4's MUST-CARRY MbabbMenu rider** and **the TD-1/TD-2 bundle WHOLE** — re-derives nothing, mints nothing, re-books nothing.
**Half**: frontend (keyframes.js demo). **Sub-tranche**: X·KF. **Track**: B.
**Status**: planned. No product byte is written by this file; authoring it opened no product source for writing; execution awaits the sequencing head (§State).
**Ref of record**: keyframes.js `origin/master` **`69095552`** — ⟨cmd⟩ `git -C /Users/mkbabb/Programming/keyframes.js rev-parse --short origin/master` → `69095552` (2026-09-18); local `master` = the same; `git status --porcelain` → two untracked coordination letters, zero modified product paths. Every line anchor below was re-resolved at this ref by this seat and is re-resolved again at open (KF.W0 §B-12; D-19). The banked figures that MOVED since RULINGS-4's census (the register's blast-radius receipt) are re-measured at §Excluded and recorded as dated observations, never adopted silently.
**Standing supersession rider (KF-AV-28), on the transport/ribbon surfaces (register row 3, lock 3)** — the transport/ribbon packet *"carries this wave's rider AND supplies its counter-evidence"* (KF-W7 §Sequencing → KF.W13): `PR-CAUTION`, quoted verbatim at KF-W7's header — *"this ribbon is the NON-bespoke case (it consumes the real `Slider`) and carries BLOCKERs anyway, so 'swap onto the primitive' is never sufficient as a cure."* **The verdicts are IN — six KEEP-BESPOKE, ZERO SWAP** (`execution/B/KF-W7.md` `.a` G1 `4c03ceda`, 2026-09-18; `.f`: *"KF.W13 — both packets are un-gated by a swap that did not happen"*). **Both packets are therefore UN-GATED**; the rider stays as the record of why they sequence after W7, and PR-CAUTION binds the cure shape: the ribbon's rows are cured ON the primitive it already consumes, never by swapping.
**Provenance**: authored BLIND by the fable arm of the SS-1/SS-2 authoring block (COHESION §0d; `EXECUTION-RUNBOOK.md §4.5`; M-23 §3 — the Opus arm's draft was not read) from: the register of record `KF-W10.md §6.D` row 3; the minting citations (`KF-W4 §Sequencing` R-15 homing block, KF.W13 bullet; `KF-W1 §Cross-edges` edge 9); the home records `docs/tranches/V/megatranche/registry/adjudicated/kf-{ChromeDock,MbabbMenu,PlaybackRibbon,TransportDock}.md` (`kf-TransportDock.md:131` names *"the transport repair packet"*; `kf-PlaybackRibbon.md:157` names *"the transport/ribbon repair packet … under the KF.W7 rider"*; `kf-MbabbMenu.md:169` names *"the dock-menu actuation/typography/brand family"*; `kf-ChromeDock.md:45` is M-4) — consumed **by mechanism, deduped by identity, never transcribed** (M-25); `execution/B/KF-W7.md` (the verdict table; *"What the two gated successors read here"*); `KF-W9.md` §Bounds (the G-KFW9-9 witness row — *"The two-deletion act is KF.W13's"*); and the live keyframes tree, READ-ONLY. Validated against `docs/precepts/instructions/tranche/WAVE_SPEC.md` (L-20). **No gate exists here without a named live witness (L-19).**

| verb | state | basis |
|---|---|---|
| AUDITED | YES | the four home records; the two packet terminal lines and M-4 quoted by anchor at §Carry — ⟨cmd⟩ `grep -c 'NO-WAVE-OWNER' registry/adjudicated/kf-{ChromeDock,MbabbMenu,PlaybackRibbon,TransportDock}.md` → **35 · 32 · 14 · 32** |
| SPECIFIED | YES — this file (a draft: the authoring block's fable arm; ratification is the block's fold seat) | **the packet denominator is the register's — 2 of 17; carriage over row-ids is NOT asserted in this file's voice (LAW B)** — rows land by their home records' lines cited at §Carry; the carriage census is the fold seat's artefact |
| IMPLEMENTED | NO | stays NO until the gates green after the sequencing head lands |
| VERIFIED | NO | a successor close's act; **KF.W10's close stamps the AUTHORED ELEVEN only** (COHESION §0d) |

**Execution shape**: 3 seats, strictly serial — `.a` dock-menu → `.b` transport/ribbon → `.c` close. Peak concurrency **1** (the two packets share `App.vue`'s dock wiring and the transport's host; the TD bundles are one-commit families that tolerate no interleaving). Tiering per M-12 is the orchestrator's.

---

## §State

**Name**: KF.W13 — Chrome, Dock & Transport Repair
**Opens after**: **G-KFW4-1 lands GREEN** (the sequencing head; **measured RED at this seat, twice**: ⟨cmd⟩ `npx vue-tsc --noEmit -p tsconfig.json 2>&1 \| grep -c 'error TS'` → **54** · **54**; the LEDGER's KF.W4 row is `CLOSED 2026-09-17 (honest-RED: G-KFW4-1 …)`) · **KF.W7's per-surface SWAP verdicts** (LANDED `4c03ceda` — the LEDGER's own row-56 condition *"drag-seam/transport after KF.W7's SWAP verdicts"*, which W7's close reads GREEN: *"The SWAP verdicts are STATED (six KEEP-BESPOKE, zero SWAP)"*) · **KF.W12 `.b` (OPTIONS-UNIT) CLOSED** — for `.b` only: the C-2 + KF-CO-15 joint commit needs LP-1's render edge to exist (§Sequencing 3) · **KF.W6 CLOSED** (the `kf-focus-ring` rename re-anchored this wave's `design-idioms.css` counterpart) · **KF.W9's G-KFW9-9 before/after pair** held READ-ONLY there.
**Agents**: 3 (a→c), serial; **no jury**.
**Hard gate**: **six conditions, ALL RED**, measured at `69095552` and pasted at §Gates, each double-run.
**Status**: planned

## §Goal criterion

This wave succeeds if, when work ends, the dock's brand menu holds the dock open by itself and still opens (M-4's round-trip gone, the MUST-CARRY binding kept), the transport actuates once per gesture on the control the gesture began on with Space working on every button (the TD bundles, unsplit), and the playback ribbon is a named, stepped, keyboard-scrubbable slider whose time-space is declared and shared with the options card (C-2 + KF-CO-15, one commit) — all cured ON the glass primitives the demo already consumes, never by swapping them. **A wave that deletes M-4's round-trip as worded and ships an unopenable menu, or lands TD-2 without TD-38/TD-40, has failed this goal** whatever its gate count says.

## §0 Open preconditions (checked at wave-open; NOT close gates)

| # | precondition | state at authoring |
|---|---|---|
| **OP-0** | **G-KFW4-1 GREEN** — the head. | **RED — 54 · 54**. |
| **OP-1** | **KF.W7's SWAP verdicts for the transport/ribbon surfaces and the drag seam.** | **MET** — KEEP ×6, `4c03ceda`, 2026-09-18; discharge set NONE. Both packets un-gated. |
| **OP-2** | **LP-1's write→render edge (KF.W12 `.b`)** — the C-2/KF-CO-15 joint commit's reactive `:max` and one-duration-read are inert until the options card re-renders on engine writes. | **UNLANDED** (KF.W12 is MINTED-UNAUTHORED at this clock). `.b` opens on KF.W12 `.b`'s close sha; `.a` does not wait. |
| **OP-3** | **The glass-ui producer facts the MUST-CARRY rider rests on** — `dropdown-menu` chunk `:13-16`: `defaultOpen`/`open`/`modal` are bare `{ type: Boolean }`, no defaults → Boolean-cast makes the primitive permanently controlled and non-modal by accident (kf-MbabbMenu, the headline). | **RE-MEASURED AT `.a`'s OPEN against the installed dist** (glass-ui is READ-ONLY; the producer half — `default: undefined` — rides SS-6, lock 4). If the producer has shipped defaults by then, the rider's *shape* changes (the local binding may become optional) but **the rider's obligation does not: the menu must open after M-4's cure, proven by G-KFW13-1's runtime clause**, not by a byte. |
| **OP-4** | **TD-37 — one face order — is an SS-2 DESIGN DECISION** (kf-TransportDock:131). | **OWNER/DESIGN-RULED, not this seat's**: `.b` lands TD-37 only with the ruling in hand; otherwise `complete_with_misses` naming it. |
| **OP-5** | **G-KFW9-9's offsets** — KF-W9 §Bounds: *"kf `demo/styles/design-idioms.css:76-79` + `demo/styles/playback-idiom.css:72-75` … READ-ONLY WITNESS SURFACE … The two-deletion act is KF.W13's; G-KFW9-9 measures both offsets and holds the before/after pair."* | **BOTH OFFSETS HAVE MOVED (dated observation)**: ⟨cmd⟩ `grep -n 'focus-visible' demo/styles/playback-idiom.css` → **`:74 .btn-playback:focus-visible {`** (the rule spans `:74-77`: `box-shadow: var(--focus-ring-shadow); outline: none;`); ⟨cmd⟩ `grep -n '^\.kf-focus-ring:focus-visible\|^\.focus-ring:focus-visible' demo/styles/design-idioms.css` → **`:106 .kf-focus-ring:focus-visible {`** — the counterpart was renamed by KF.W6 `.g` (`a6418729`, KF-KE-30) and its comment block (`:73-105`) now states WHY the name is `kf-` prefixed. **`.b` re-reads G-KFW9-9's statement at KF-W9.md at open and lands the two-deletion act against the offsets KF.W9's pair witnesses — K-5: one deletion fixes nothing, the pair is ONE act.** |

---

## §Scope

1. **dock-menu** — **M-4** (`kf-ChromeDock.md:45`): delete the `itemsPopupOpen` prop and its five-site round-trip — measured live at `69095552`: ⟨cmd⟩ `cat demo/app/dock/ChromeDock.vue demo/app/App.vue demo/app/dock/MbabbMenu.vue \| grep -c 'itemsPopupOpen\\|items-popup-open'` → **5** (`ChromeDock.vue:114` the prop · `:237` the `isAnyOpen` term · `App.vue:29` `:items-popup-open="mbabbPopupOpen"` · `:369` the comment · `MbabbMenu.vue:186` the comment) — MbabbMenu self-holds via `useOptionalDockContext()` on the `/dock` subpath it already imports (the C/S-2 kill: Vue resolves `inject` along the runtime parent chain; the portalled `SelectContent` proves it inside this very render); **i-2** dies with it. **WITH THE MUST-CARRY RIDER (MM-28, kf-MbabbMenu `:26`)**: *"`v-model:open` is load-bearing (permanently-controlled primitive) … executed as worded, the menu becomes permanently unopenable"* — measured: `MbabbMenu.vue:4` `<DropdownMenu v-model:open="open">` and `:204` `const open = defineModel<boolean>("open", { default: false })` — **the local open binding SURVIVES M-4's motion** (what goes is the ChromeDock round-trip, not the menu's own open state); the `:186` comment that calls it dock-plumbing is corrected to say what it holds. **Plus kf-MbabbMenu `:169`'s dock-menu family**: MM-2·3·5·7·8·9·10·11·12·13·16·17·18·19·20·21·22·24·25·26·29·30·31·32·39·40·42 + MM-1/MM-6's cure law (≡ the banked KF-APP-1 motion — **CONSUMED**, not re-booked: repair `:100`, dispose `setPPMode` with the KF-APP-17 sweep, resolve the C-14 bucket split, land the MM-5 CheckboxItem in the same edit; the KF-APP-1/-17 same-motion lock is homed in its authored wave and this packet rides its arm). **Plus kf-ChromeDock's NO-WAVE-OWNER chrome roster** by id: M-5/C-6 · RR-2 MISSED #1 · R3-2 · M-2/C-4 · M-3 · D-3 · D-4 · C-8 · D-20 · M-1/C-3/D-14+R3-7 · D-2-RESCOPED · RR-1 MISSED #2/#3/#4 · RR-2 MISSED #2 · D-13 · D-6/m-6 · D-7/C-15/m-13 · D-11/m-4/R3-5 · D-12 · D-25 · D-26 · C-10 · m-2/C-13 · m-12 · m-3/R3-6 · R3-4 · D-21 · D-16+L/i-1 · R3-8 (D-18/C-1 is ADJUDICATED with TD-39's rider on its cure spec; TD-22's re-pin inversion is *one cure spec with the ChromeDock row*).
2. **transport/ribbon — the TransportDock half** (`kf-TransportDock.md:131`): **the keyboard-propagation commit TD-2 + TD-38 + TD-40 — ONE commit** (measured: `TransportDock.vue:205-209` five `.stop`s on the collapsed mirror — TD-38's inversion against the dock's capture-phase listeners; `useControlsKeyboardShortcuts.ts:50` `registerShortcut("Space", …)` with `preventDefault: true` unconditionally — TD-40's Space inoperable on Reset/Collapse-timeline; TD-2's double actuation on the focused expanded Play) — one propagation policy, `.stop` symmetry decided once on both mirrors, Space registration scoped away from button targets, **composition tests that mount the buttons WITH the registry**; **the actuation-cancellation commit TD-21(+r2 rider) + TD-41 — ONE commit** (per-control origin, release-elsewhere cleanup, blur-class disarm, orphan-keyup + stale-id cases — measured: `transport-play-actuation.test.ts` already carries eight cases incl. *"a pointerup whose press began elsewhere does NOT actuate"*; ⟨cmd⟩ `grep -c 'blur\\|orphan\\|stale' test/demo/instrument/transport-play-actuation.test.ts` → **0** — the cancellation arm is the residue); **TD-1 + TD-4 + PRM + test — ONE commit** (measured: `useIconSpin.ts:5,7` `instanceof HTMLElement` ×2 against a functional-lucide `SVGSVGElement` ref — 100 % dead; TD-4's eager `kfEngine()` + zero teardown; TD-17's typed refs ride the re-authoring); **TD-36** (the full-viewport-width pointer trap: `fixed left-0 right-0 z-dock`, ~90 px, cured with the ChromeDock pointer-events pair — two classes; measured ⟨cmd⟩ `grep -c 'pointer-events' demo/components/instrument/transport/TransportDock.vue` → **1**, re-read at open); **TD-37** (OP-4); **TD-39** (one stable name, with TD-5/TD-28's prose pass); **TD-34/TD-35** (the TD-31 cluster: glyph translate-not-shrink; `--dock-layer-gap`).
3. **transport/ribbon — the PlaybackRibbon half** (`kf-PlaybackRibbon.md:157`, under the rider and PR-CAUTION): the instrument-truth core — **D-1** (+N-2's demo half: the sole AT-exposed scrub control has no accessible name; measured ⟨cmd⟩ `grep -c 'aria-label' demo/components/playback/PlaybackRibbon.vue` → **0**) · **D-2**'s demo arm (no visible playhead) · **C-2 — the time-space contract** (`currentT` declares no space; `scrubTo` inverts under Reverse; 2 of 3 mounts feed RAW `t`; **the channel mount is the pin; cured JOINTLY with the KF-CO-15 extension so scale and space land together** — one commit touching `PlaybackRibbon.vue` and `ChannelOptions.vue`'s KF-CO-15 seam, after OP-2) · the KF-CO-15 riders (reactive `:max`, one duration read, one guard — L-m9) · **C-3** (`valueCommit` forwarded and ignored — measured **0** bindings) · **C-4** (`:step` — measured **0**; reka defaults 1 ms per arrow press over a ms `:max`) — the one-prop/one-token cures **D-5 (+M-6) · D-6 (`disabled`, not the `.is-disabled` costume) · D-10 (the accent pair's 3.58–4.02:1 label) · L-M1 (delete the wrapper touch gate — glass-ui 7.0.0's Slider owns its own `useTouchGate`; carry S-3; measured `gatedSliderDown` at `:167`)** — the hygiene sweep **D-7 · D-8/N-1/D-15 · N-3 · D-17/N-4 · D-13 · D-16 · D-19 · D-20/D-25 · D-23-as-preconditions (with KF-ES-2's owner) · L-m5/L-m6/L-m9/C-9/C-11/N-5/N-6 · L-i2/C-15**; **D-14/C-10/L-m1 ≡ KF-CPW** at its bank. Born-RED the packet inherits: the signed-seek witness (residue 9), the keyboard-scrub pause/resume assertion (C-3), the axe/name assertion on the thumb (D-1), the forced-colors focus screenshot (D-3 → SS-13).
4. **G-KFW9-9's two-deletion act with the K-5 lock intact** (OP-5) and **kf-PlaybackRibbon D-12's demo-side stopgap bytes** (the `:deep` slider-height cure at `PlaybackRibbon.vue:216-226`'s F4 block — measurement KF.W9's, cure this packet's): W13 owns `playback-idiom.css`; `design-idioms.css`'s counterpart rides the same single act.
5. **Close** — receipts, the commit roster audited to §Bounds, the BH relay (the `default: undefined` producer ask for `dropdown-menu`'s Boolean props; PlaybackRibbon's producer seams if any), E13, the LEDGER row.

### Triumvirate dispatch (WAVE_SPEC §3a)

- **Bounds whose expansion invalidates the wave**: any glass-ui or `node_modules` byte (the Boolean-cast defaults are the producer's); any `src/**` byte; any write to `demo/components/instrument/timeline/**` or `AnimationVisualizer.vue` (KF.W7's / KF.W11 `.i`'s).
- **Non-local gate failures**: G-KFW13-1's runtime clause failing after M-4's deletion with the local binding kept → the producer's Boolean-cast is the cause; triumvirate names SS-6 and the rider's shape is re-cut, never the menu hacked open; G-KFW13-4 failing because the options card cannot re-render (OP-2 not really landed) → triumvirate with KF.W12 named.
- **Diagnostic loops that halt at three**: the TD-21 stale-id resurrection reproduced three times without a deterministic pointer harness → halt.

---

## §Bounds

All paths keyframes.js-relative at `69095552`; line counts ⟨cmd⟩ `wc -l` this seat.

| File | L | Access | Unit |
|---|---|---|---|
| `demo/app/dock/ChromeDock.vue` | 534 | modify (M-4: `:114` prop, `:237` term; the chrome roster) | `.a` |
| `demo/app/dock/MbabbMenu.vue` | 229 | modify (the MUST-CARRY: `:4` and `:204` SURVIVE; `:186` comment corrected; the `:169` family; self-hold via `useOptionalDockContext()`) | `.a` |
| `demo/app/App.vue` | 412 | modify-carve (`:29` the binding, `:369-372` the comment + `mbabbPopupOpen` ref — M-4's App sites; the KF-APP-1 motion's `:100`-class repair as consumed) | `.a` |
| `demo/components/instrument/transport/TransportDock.vue` | 403 | modify (TD-2/38/40 · TD-36 · TD-37 · TD-39 · TD-34/35) | `.b` |
| `demo/components/instrument/transport/TransportDock/usePlayActuation.ts` | 88 | modify (TD-21 + TD-41) | `.b` |
| `demo/components/instrument/transport/TransportDock/useIconSpin.ts` | 29 | modify (TD-1 + TD-4 + TD-17) | `.b` |
| `demo/components/instrument/transport/TransportDock/useMenubarMeasure.ts` | 28 | modify-carve | `.b` |
| `demo/components/instrument/transport/AnimationControlsGroup/useControlsKeyboardShortcuts.ts` | 72 | modify (TD-40: Space registration scoped) | `.b` |
| `demo/components/playback/PlaybackRibbon.vue` | 244 | modify | `.b` |
| `demo/components/instrument/transport/channel-controls/ChannelOptions.vue` | 731 | **modify-carve — the KF-CO-15 extension ONLY, in the C-2 joint commit, AFTER KF.W12 `.b` closes** (the file is KF.W12's; this carve is the declared exception and is never parallel with it) | `.b` |
| `demo/styles/playback-idiom.css` | 89 | modify (owned; the G-KFW9-9 deletion at the `.btn-playback:focus-visible` rule `:74-77`; D-12's stopgap) | `.b` |
| `demo/styles/design-idioms.css` | 338 | modify-carve (the counterpart deletion in the SAME act — K-5) | `.b` |
| `test/demo/app/mbabb-menu-self-hold.test.ts` | — | create | `.a` |
| `test/demo/instrument/transport-keyboard-propagation.test.ts` | — | create (the composition tests that mount the buttons WITH the registry) | `.b` |
| `test/demo/instrument/transport-icon-spin.test.ts` | — | create (TD-1's test, in TD-1's commit) | `.b` |
| `test/demo/instrument/playback-ribbon-contract.test.ts` | — | create | `.b` |
| `test/demo/instrument/transport-play-actuation.test.ts` | — | modify (TD-21/TD-41's cancellation cases added; existing eight never weakened) | `.b` |
| value.js `docs/tranches/X/execution/B/KF-W13.md` | — | create (unit receipts; append-only) | every unit |
| value.js `docs/tranches/X/execution/LEDGER.md` | — | modify (this wave's row cells + appended lines ONLY) | `.c` |
| value.js `docs/tranches/V/coordination/INBOX.md` + the SS-6 accretion register (COHESION §4a) | — | modify (append) | `.c` |

**Do NOT touch**: every glass-ui tree and `node_modules/**` (the Boolean-cast defaults, the Slider's `useTouchGate`, the dock's capture-phase listeners are the producer's — READ-ONLY always; producer rows ride SS-6) · `src/**` · `demo/components/instrument/timeline/**`, `demo/components/playback/AnimationVisualizer.vue`, `demo/scenes/**` (KF.W7 / KF.W11) · `demo/components/instrument/keyframes/**`, `LayerConfigPanel.vue`, `TimingFunctionPanel.vue` (KF.W12) · `ChannelOptions.vue` beyond the KF-CO-15 carve · `demo/app/App.vue` beyond M-4's sites and the consumed motion · `package.json` / `vitest.config.ts` (harness present) · `scripts/dev/dev.sh` · every dated spec, the adjudicated registry, conformance artefacts (E-3).

### Disjointness (WAVE_SPEC §4a)

The wave is serial; no path is written by two units. `App.vue` is `.a`'s alone. The one cross-wave shared path, `ChannelOptions.vue`, is written by KF.W12 `.b` first and by this `.b` (one carve) strictly after its close — never in parallel.

### Worktree plan (WAVE_SPEC §4b)

Serial on `master`; no sibling worktrees needed. No Cargo.

---

## §Carry — the two packets, homed

### P1 · dock-menu — `kf-ChromeDock.md:45` (M-4) · `:99` (i-2) · `:106` (the C/S-2 kill) · `kf-MbabbMenu.md:15` (the headline) · `:26` (MM-28 → the MUST-CARRY rider) · `:169` (the family)

**M-4 with the rider, as ONE motion**: delete the round-trip's five sites; MbabbMenu calls `keepOpen()`/`release()` itself via `useOptionalDockContext()` (the C/S-2 kill's mechanism — `SelectContent` stamps `data-glass-dock-portal` from inside the same slot; `DockSeparator` rides the same DI); **keep** `v-model:open="open"` + `defineModel("open")` as the menu's own open state (the rider), and say so in the docblock that today says the opposite. **Runtime proof, not byte proof**: the menu opens after the motion (G-KFW13-1). **Then the `:169` family** in the record's cure law (the KF-APP-1 motion consumed; MM-5's CheckboxItem in the same edit), **then the ChromeDock chrome roster** by id (§Scope 1). **Producer half → SS-6**: `default: undefined` on `open`/`modal`/`defaultOpen` (and LabeledSelect/LabeledSwitch's, already filed by kf-LayerConfigPanel — cross-referenced, not re-filed).

### P2 · transport/ribbon — `kf-TransportDock.md:131` · `:45-47` (TD-1/2/4) · `:64` (TD-21) · `:82-89` (TD-34..41) · `kf-PlaybackRibbon.md:157` · `:40-55` (the rows)

**Three one-commit families, unsplit (the TD-1/TD-2 bundle WHOLE — lock 2; re-affirmed from W7's end: *"TD-2+TD-38+TD-40 and TD-21+TD-41 MUST NOT split"*)**: (1) **TD-2 + TD-38 + TD-40**; (2) **TD-21 + TD-41**; (3) **TD-1 + TD-4 + PRM + test**. Then **TD-36 · TD-37 (OP-4) · TD-39 · TD-34/35**. **The ribbon**: (4) **C-2 + KF-CO-15 — ONE commit, after OP-2**; (5) **D-1 + C-3 + C-4** (name, `valueCommit`, `:step` — the three bindings glass-ui already forwards; PR-CAUTION: cured ON the Slider); (6) **D-5 · D-6 · D-10 · L-M1**; (7) **D-2's demo arm**; (8) the hygiene sweep; (9) **the G-KFW9-9 two-deletion act + D-12's stopgap** (OP-5). Born-RED witnesses the packet inherits land in G-KFW13-4's file (signed seek; keyboard-scrub pause/resume; the thumb's axe/name); the forced-colors focus screenshot is SS-13's and is named, not taken here.

**Identity notes**: TD-37's stale-Set rider ≡ TD-21 (one identity, `:94`); TD-39 ≡ r1 superlative 4's subject, a rider on the ChromeDock D-18 cure spec (`:96` — lands with `.a`'s D-18 cure or names it); TD-22 is *one cure spec with the ChromeDock row* (`.a`'s RR-2 M#1 mechanism); D-14/C-10/L-m1 ≡ KF-CPW (its bank).

---

## §Agent Units

### KF.W13.a — The Dock-Menu Packet (serial, first)
- **Goal**: the brand menu holds the dock open by itself, opens, and the chrome roster's rows are cured in one family each.
- **Mechanism**: P1 — M-4's five-site deletion with the MUST-CARRY binding kept and its docblock corrected; self-hold via the dock context; the `:169` family under the KF-APP-1 motion; the ChromeDock roster; the producer ask recorded for `.c`.
- **Files**: the `.a` rows; `test/demo/app/mbabb-menu-self-hold.test.ts` (create).
- **Sub-gate**: G-KFW13-1 GREEN (runtime + both byte clauses); the receipt prints OP-3's dist measurement.

### KF.W13.b — The Transport/Ribbon Packet (serial, after `.a` and after KF.W12 `.b`)
- **Goal**: one actuation per gesture on the control it began on, Space on every button, a named stepped keyboard-scrubbable ribbon in a declared time-space shared with the options card.
- **Mechanism**: P2's nine families in order; TD-37 only with OP-4's ruling; the G-KFW9-9 pair re-read at KF-W9.md before the two-deletion act.
- **Files**: the `.b` rows; the three created tests; `transport-play-actuation.test.ts` extended.
- **Sub-gate**: G-KFW13-2 · -3 · -4 · -5 GREEN; each family's sha listed with `git show --stat` proving it unsplit.

### KF.W13.c — Close (serial, last)
- **Goal**: record whole, commits audited, producer rows sailed, LEDGER row moved.
- **Mechanism**: all six gates re-run double; `git show --stat` per sha against §Bounds; SS-6 accretion entries; E13 sweep; LEDGER cells + event line; residuals/escalations.
- **Files**: the value.js rows.
- **Sub-gate**: G-KFW13-6 GREEN.

### Standing law for every unit

As KF-W11's — CRASH-RECOVERY first · the spec GOVERNS, no masking fallback (no `try/catch` around a defect, no `test.skip`, no allowlist, **no copied producer selector** — the dock's and Slider's internals are consumed through their exported seams — no `node_modules` patch) · pathspec commits, families unsplit · E-3 · WRITE-THEN-MEASURE double-run · quote-by-command · glass-ui READ-ONLY always · E13 · probe parsimony · `dev.sh` never staged · LEDGER minimal edits only.

---

## §Gates — six, all born RED at `69095552`, literal commands, double-run

All commands from `/Users/mkbabb/Programming/keyframes.js`. Runtime gates are `vitest` runs; byte clauses ride beside them.

**G-KFW13-1 — dock-menu: self-hold, and the menu still opens (M-4 + the MUST-CARRY rider).** ⟨cmd⟩ `npx vitest run --project demo test/demo/app/mbabb-menu-self-hold.test.ts` → RED today: `No test files found` (⟨cmd⟩ `ls test/demo/app/mbabb-menu-self-hold.test.ts` → `No such file or directory`, twice). GREEN when: MbabbMenu mounted inside a GlassDock provider opens on trigger activation (the rider's runtime proof), calls the dock's keep-open on open and release on close, and ChromeDock exposes no `itemsPopupOpen` prop. Byte clauses: ⟨cmd⟩ `cat demo/app/dock/ChromeDock.vue demo/app/App.vue demo/app/dock/MbabbMenu.vue | grep -c 'itemsPopupOpen\|items-popup-open'` → **5** · **5** (must read **0**); **the MUST-CARRY clause** ⟨cmd⟩ `grep -c 'v-model:open="open"' demo/app/dock/MbabbMenu.vue` → **1** · **1** (must STILL read **1** — a commit that drives this to 0 while OP-3's Boolean-cast stands has shipped the unopenable menu the rider exists to prevent).

**G-KFW13-2 — transport: one propagation policy, Space on every button (TD-2 + TD-38 + TD-40, one commit).** ⟨cmd⟩ `npx vitest run --project demo test/demo/instrument/transport-keyboard-propagation.test.ts` → RED today: `No test files found`. GREEN when, with the buttons mounted WITH the registry: Space on the focused expanded Play actuates exactly once; Space on Reset and Collapse-timeline activates them and does not fire playback; the collapsed mirror's `.stop`s sit on the events the dock's listener phases can see. Byte clauses (recorded, re-read at open): ⟨cmd⟩ `grep -c '\.stop=' demo/components/instrument/transport/TransportDock.vue` → **5** · **5**; ⟨cmd⟩ `grep -c 'registerShortcut("Space"' demo/components/instrument/transport/AnimationControlsGroup/useControlsKeyboardShortcuts.ts` → **1** · **1** (must be scoped away from button targets — the runtime clause decides). **Bundle clause**: the greening commit's `git show --stat` names `TransportDock.vue` AND `useControlsKeyboardShortcuts.ts` AND the test — three files, one sha.

**G-KFW13-3 — transport: cancellation (TD-21 + TD-41) and the icon spin (TD-1 + TD-4), one commit each.** ⟨cmd⟩ `npx vitest run --project demo test/demo/instrument/transport-play-actuation.test.ts test/demo/instrument/transport-icon-spin.test.ts` → RED today: the second file is absent (`No test files found` for it; the first's eight cases pass — the baseline, pasted at open). GREEN when the actuation file also passes: a blur mid-press disarms `spaceArmed`; a release over neither control clears the origin id (no stale-id resurrection); an orphan keyup does not actuate — and the spin file passes: the lucide `SVGSVGElement` ref spins, PRM is honoured, teardown runs. Byte clause: ⟨cmd⟩ `grep -c 'instanceof HTMLElement' demo/components/instrument/transport/TransportDock/useIconSpin.ts` → **2** · **2** (must read **0**). Residue clause: ⟨cmd⟩ `grep -c 'blur\|orphan\|stale' test/demo/instrument/transport-play-actuation.test.ts` → **0** · **0** (must read ≥ **3**).

**G-KFW13-4 — ribbon: named, stepped, keyboard-scrubbable, in one declared time-space (C-2 + KF-CO-15 · D-1 + C-3 + C-4).** ⟨cmd⟩ `npx vitest run --project demo test/demo/instrument/playback-ribbon-contract.test.ts` → RED today: `No test files found`. GREEN when: the thumb has an accessible name; an arrow press moves by the declared `:step`, not 1 ms; a keyboard scrub during playback pairs with pause/resume (`valueCommit`); under Reverse the ribbon's slider and its ball agree on effective time (the signed-seek witness); and the options card's duration write re-scales `:max` (KF-CO-15, on LP-1's edge). Byte clauses: ⟨cmd⟩ `grep -c 'aria-label' demo/components/playback/PlaybackRibbon.vue` → **0** · **0** (must read ≥ **1**); ⟨cmd⟩ `grep -c 'valueCommit\|value-commit' demo/components/playback/PlaybackRibbon.vue` → **0** · **0** (≥ **1**); ⟨cmd⟩ `grep -c ':step' demo/components/playback/PlaybackRibbon.vue` → **0** · **0** (≥ **1**). **Joint-commit clause**: the C-2 sha's `git show --stat` names `PlaybackRibbon.vue` AND `ChannelOptions.vue`.

**G-KFW13-5 — the two-deletion act with the K-5 lock (G-KFW9-9's subject) and D-12's stopgap.** ⟨cmd⟩ `grep -c 'box-shadow: var(--focus-ring-shadow)' demo/styles/playback-idiom.css` → **1** · **1** today (the `.btn-playback:focus-visible` rule at `:74-77`); GREEN when it reads **0** AND the counterpart KF.W9's pair witnesses is gone in the SAME commit (`git show --stat` names both stylesheets — one sha, two files; a sha touching one is the K-5 violation *one deletion fixes nothing*), AND the focus affordance survives on the primitive: runtime clause — the `playback-ribbon-contract` file asserts a focused Play button matches the producer's `.focus-ring:focus-visible` box-shadow (the `--focus-ring-shadow` token) with no demo-owned rule painting it. Radius clause, recorded: ⟨cmd⟩ `git grep -l 'btn-playback' HEAD -- demo | wc -l` → **8** · **8** (see §Excluded — the register's 7 has grown by `KeyframeTimeline.vue`).

**G-KFW13-6 — close: green demo project, unmoved head.** ⟨cmd⟩ `npm run test:demo 2>&1 \| tail -3` → RED by construction (four absent files). GREEN when every `test/demo/**` file passes; ⟨cmd⟩ `git diff <open-sha>..HEAD -- test | grep -c 'test.skip\|it.skip\|\.only('` → **0**; ⟨cmd⟩ `npx vue-tsc --noEmit -p tsconfig.json 2>&1 | grep -c 'error TS'` → **0** (OP-0 re-run at close).

**GREEN-BEFORE-CURE (R.2)** as KF-W11's.

---

## §Sequencing — locks and cross-edges

### Intra-wave order (binding)

1. **`.a` → `.b` → `.c`.** Serial; peak 1.
2. **Inside `.a`**: M-4 + the rider as ONE motion before the `:169` family; the family's KF-APP-1 arm consumed as its authored wave shaped it.
3. **Inside `.b`**: the three TD one-commit families first (they touch the host every later row paints on) → TD-36/37/39/34/35 → the C-2 + KF-CO-15 joint commit **after KF.W12 `.b` closes** (OP-2) → D-1/C-3/C-4 → the prop/token cures → D-2 → hygiene → the two-deletion act + D-12 last (it repaints the focus affordance every earlier row was measured under).
4. **Both packets after KF.W7's SWAP verdicts** — the KF-AV-28 lock; met (`4c03ceda`) and named.

### The travelling locks (register row 3 — four named there; all six accounted)

- **kf-ChromeDock M-4's MUST-CARRY MbabbMenu rider** — G-KFW13-1's second byte clause + its runtime clause; *M-4's cure as worded ships an unopenable menu without it*.
- **The TD-1/TD-2 bundle WHOLE** — G-KFW13-2/-3's bundle clauses; TD-1's *"cure with TD-4 + PRM + test, one commit"*; TD-2's *"one commit with TD-38/TD-40"*; TD-21+TD-41 likewise (W7 re-affirmed from its end).
- **KF-AV-28's rider on the transport/ribbon surfaces** — met; PR-CAUTION binds the shape (cure ON the Slider).
- **Glass-producer rows → SS-6, never demo-side hacks** — `dropdown-menu`'s Boolean-cast defaults; any Slider naming-seam gap; relayed at `.c`.
- **LP-1's write→render ordering** — consumed via OP-2 (KF.W12's); the C-2 joint commit waits on it.
- **Comment-stated invariants are test obligations** — `MbabbMenu.vue:184-188` and `App.vue:366-370` state the dock-hold mechanism; after M-4 they state the new one, and `mbabb-menu-self-hold.test.ts` asserts it; `usePlayActuation.ts:13-16`'s F2 rationale becomes the TD-41 cases.

### Cross-edges, declared from this end

| to | type | declaration |
|---|---|---|
| **KF.W4** | **DEPENDS ON (hard) — the sequencing head** | G-KFW4-1 GREEN before `.a` opens; measured RED (54). |
| **KF.W7** | **DEPENDS ON (met) — the SWAP verdicts** | six KEEP, zero SWAP (`4c03ceda`); PR-CAUTION carried at the header; W7's *"both packets are un-gated by a swap that did not happen"* read at `.f`. W7 promised no reciprocation; this row is the reciprocal end W7 addressed to the §6.D register row. |
| **KF.W9 / SS-13** | **DEPENDS ON (witnesses, READ-ONLY there)** | G-KFW9-9's before/after pair (OP-5 — offsets moved, re-read at open); D-12's measurement; the forced-colors focus screenshot (D-3); OD-V5's 390 at-rest observation is KF.W9's and the owner's, never proxied here. |
| **KF.W6** | **DEPENDS ON (met)** | `kf-focus-ring` rename (`a6418729`) re-anchored the `design-idioms.css` counterpart; KF-KE-30's ruling stands and is not re-litigated. |
| **KF.W12** | **DEPENDS ON (`.b` only) + SHARES ONE PATH SERIALLY** | LP-1's edge (OP-2); the `ChannelOptions.vue` KF-CO-15 carve lands after W12 `.b` closes. Declared at both ends. |
| **KF.W11** | **SEAM (named both ends)** | KF-SS-8's ribbon half is the C-2 contract; the scene half is W11 `.c`'s. Neither blocks the other. |
| **KF.W10** | **REGISTERED AT** | `§6.D` row 3 — unchanged by this file; the LEDGER row 56 condition *"drag-seam/transport after KF.W7's SWAP verdicts"* is satisfied and stated. |
| **SS-6 (BH relay)** | **SUPPLIES** | the producer rows at `.c`. |
| **The owner / SS-2 design** | **DEPENDS ON (ruling)** | TD-37's one face order (OP-4). |

---

## §Excluded

| excluded | reason |
|---|---|
| **Any glass-ui / `node_modules` byte** — the Boolean-cast defaults, the Slider's touch gate, the dock's listener phases | producer's; SS-6 (lock 4). |
| **The drag-seam packet** (KF-SCR-1 + L·D-1 + KF-AV-15) — named by KF-W7 §Sequencing → KF.W13 as *"the drag-seam packet"* | **homed at KF.W11** by the register (row 1, cargo 9 of 9) and R-15; W7's edge names it beside this wave's two packets because both share the grab-offset precedent, and this file records that adjacency without re-booking the packet. |
| **KF-APP-1/-17's same-motion lock** | homed in its authored wave; P1's MM-1/MM-6 arm consumes it. |
| **kf-ChromeDock D-18/C-1** (ADJUDICATED) | its cure spec carries TD-39's rider; the row's own home is as adjudicated — this wave lands TD-39 and names D-18 by id. |
| **OD-V5** (glass dock mark) and the SS-6 batch rows | owner + producer; never demo-side. |
| **The fifteen packets not in this partition** | R-15's partition; no re-booking. |
| **The blast-radius receipts of register row 3 — RE-MEASURED at `69095552`, recorded as dated observations** | **`btn-playback` demo-scoped files**: RULINGS-4 says **7** (DESIGN.md · PlaybackRibbon.vue · EasingScene.vue · SpringScene.vue · StartingStyleTarget.vue · font-roles.json · playback-idiom.css); ⟨cmd⟩ `git grep -l 'btn-playback' HEAD -- demo` → **8** — the seven plus **`demo/components/instrument/timeline/KeyframeTimeline.vue`** (a KF.W7-era consumer; the count grew by one and the list says which). **`.focus-ring` consumers**: RULINGS-4 says **exactly 4** class applications / 4 files (`KeyframeCard.vue:45` · `SpringHeatmap.vue:30` · `SpringTarget.vue:63` · `SquareScene.vue:46`); ⟨cmd⟩ `grep -rn 'focus-ring' demo \| grep '\.vue:' \| grep -v 'kf-focus-ring' \| grep -c 'class='` → **2** — `SpringTarget.vue:63` and `SquareScene.vue:46` still apply the bare `focus-ring`; `KeyframeCard.vue` and `SpringHeatmap.vue` now apply **`kf-focus-ring`** (KF.W6's rename, `a6418729`). **Neither figure is adopted as a new register truth here**: the deleting seat re-runs both commands at its own clock and meets the list it measures, and the two surviving bare consumers are KF.W11's files (the deletion here touches stylesheets, not scenes). |

---

## §Commit plan

One commit per meaning; **the three TD families and the C-2 joint commit and the two-deletion act are each ONE sha**; pathspec on the commit itself; `Claude-Session:` trailer. Expected (keyframes.js):

1. `.a`: `fix(kf/dock · X.KF.W13.a · M-4 round-trip deleted, MbabbMenu self-holds, the open binding KEPT (MM-28 rider))` · `fix(kf/dock · X.KF.W13.a · MM family under the KF-APP-1 motion + MM-5 CheckboxItem)` · `fix(kf/dock · X.KF.W13.a · ChromeDock roster <family>)` ×n · `test(kf/dock · X.KF.W13.a · G-KFW13-1)`.
2. `.b`: `fix(kf/transport · X.KF.W13.b · TD-2+TD-38+TD-40 one propagation policy + composition tests)` · `fix(kf/transport · X.KF.W13.b · TD-21+TD-41 actuation cancellation)` · `fix(kf/transport · X.KF.W13.b · TD-1+TD-4 icon spin + PRM + test)` · `fix(… TD-36 pointer trap)` · `fix(… TD-37 one face order)` (if ruled) · `fix(… TD-39 + TD-34/35)` · `fix(kf/ribbon · X.KF.W13.b · C-2 time-space contract + KF-CO-15 extension — scale and space land together)` · `fix(kf/ribbon · X.KF.W13.b · D-1+C-3+C-4 name/valueCommit/step on the Slider)` · `fix(… D-5/D-6/D-10/L-M1)` · `fix(… D-2 visible playhead)` · `chore(… hygiene sweep)` · `fix(kf/idiom · X.KF.W13.b · the two-deletion act (G-KFW9-9, K-5) + D-12 stopgap)` · `test(… G-KFW13-4)`.
3. value.js: `docs(x-kf-w13/<unit>): receipt` per unit; `docs(x-execution/ledger): KF.W13 row`; SS-6 accretion + INBOX append at `.c`.

## §Format and lint cadence · §Verification artefacts

`npm run check` + `npm run test:demo` after each unit's last commit and at close; `npx eslint demo/app demo/components/instrument/transport demo/components/playback demo/styles` at close; `git diff --check` per commit. Artefacts at close in `execution/B/KF-W13.md`: the six gate transcripts (double-run), the commit roster with `git show --stat` proving each family unsplit, OP-3's dist measurement, OP-5's offsets at open and the pair KF.W9 holds, the radius re-measurements, the SS-6 entries' ids, the E13 line.

## §L-18 rider (reference, not ceremony)

Green gates are not acceptance: the implementation must survive two challenging gestalt passes and a fresh-Fable agglomeration. Likely bases: (i) M-4 executed as worded — the round-trip AND the local binding deleted — a menu that never opens, invisible to a byte-only check (G-KFW13-1's runtime clause exists for this); (ii) TD-2 landed without TD-40's registry scoping, or TD-21 without TD-41's blur disarm — a split family (the bundle clauses exist for this); (iii) a "swap onto the primitive" cure for the ribbon (PR-CAUTION forbids it by name); (iv) one stylesheet deletion without its counterpart (K-5); (v) a producer selector copied into `TransportDock.vue` to reach the dock's internals (a HIGH defect by standing law).

**Dissents recorded, not resolved by this seat**: (1) whether kf-ChromeDock's full NO-WAVE-OWNER roster (beyond M-4) belongs to this packet or to a KF.W6 successor round — the register names the packet *dock-menu* with M-4 as its head and this file carries the roster by id under it, so nothing is dropped; the fold seat may narrow it. (2) whether the drag-seam packet, which W7's edge addresses to this wave, should move here from KF.W11 — this file follows the register's partition and records the adjacency.
