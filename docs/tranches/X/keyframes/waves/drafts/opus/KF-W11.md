SERVED MODEL: claude-opus-5[1m]

# KF.W11 — Demo Scene Repair

**Wave**: KF.W11 · Demo Scene Repair — the nine demo-scene packets the X·KF census never owned: **cube · sequence · spring · spring-plot · spring-physics-facet · spring-artifact-truth · square · amiga · drag-seam**.
**Half**: **demo** (keyframes.js `demo/scenes/**`, plus the two `demo/components/playback/**` seam files the drag-seam packet's own rows sit in). **Sub-tranche**: X·KF. Spec home `docs/tranches/X/keyframes/waves/KF-W11.md` (this file is the **opus author's blind draft** at `waves/drafts/opus/KF-W11.md`, authored under M-23's twice-authored idiom; the agglomerated file at the ratified path is the fold seat's product, never this one — a draft is not a spec).
**Authority**: `docs/tranches/X/COHESION.md` §0/§0d/§1, ratified under `docs/tranches/V/megatranche/SCOPE.md` **M-25**. **Minting authority**: `KF-W4 §Sequencing`, the **R-15 homing block**, KF.W11 packet-roster bullet. **Terminal disposition of record**: `KF-W10 §6.D · SUCCESSOR-FORMATION REGISTER`, row 1 — cited by **STABLE ANCHOR ALONE (LAW C(3))**; no line number rides that citation, and this file promises nothing back to a stage-2 sibling's bytes.
**Status**: **planned**. No product byte is written by this file. Authoring opened no product source for writing — every keyframes.js anchor below was re-resolved **read-only via `git show origin/master:<path>` / `git grep … origin/master`**, never by opening a working-tree file. Execution awaits the **SS-1/SS-2 authoring block's fold and ratification**, and the wave enters no order until then (`EXECUTION-RUNBOOK.md` §4.5).

**Alias law (R-B idiom).** `KF.W11` denotes `X.KF.W11`; a search alias, never a rename. The wave's prose name — *"Demo Scene Repair"* — is **KF-W1 cross-edge 9's** and **KF-W4 §Sequencing's** spelling, carried verbatim; no second name is minted here.

**Ref of record.** keyframes.js `origin/master` **`69095552`**, measured by this seat 2026-09-18. **THE SIBLING SPECS' REF OF RECORD HAS MOVED AND THE MOVE IS LOAD-BEARING**: the eleven authored X·KF specs (KF-W0..KF-W10) and RULINGS-4's censuses are anchored at `81a56990`; ⟨cmd⟩ `git rev-list --count 81a56990..origin/master` → **203**. Two hundred and three commits of X·KF execution (KF.W0/W2/W4/W5/W6/W7/W8/W9 landings, the glass suffusion, the structure-and-colocation settle) sit between the register's figures and the tree this wave would cure. **Every inherited anchor below is therefore re-resolved at `69095552` and its drift is DECLARED, never silently carried** — KF.W0 §B-12's re-baseline law and the **KF-AT-28 re-anchor law** (*re-resolve every cite at the landing substrate before writing*), which this seat read as binding on an authoring act and not merely on a cure.

**Provenance.** Authored **blind** (M-23 twice-authored: this is the **opus arm**; the Fable arm is unread by this seat by construction of the authoring block). Rows consumed **by mechanism, deduped by identity, never transcribed** (M-25). Sources: the nine packets' banked routing blocks in `docs/tranches/V/megatranche/registry/adjudicated/` — **kf-CubeScene · kf-CubeTarget · kf-MatrixEditor · kf-OrbitalDrag · kf-SequenceScene · kf-SequencePlayhead · kf-SequenceAxis · kf-SequenceTarget · kf-SequenceScrubber · kf-SpringTarget · kf-SpringScene · kf-SpringTrace · kf-SpringHeatmap · kf-SpringPhysicsFacet · kf-StartingStyleTarget · kf-SquareScene · kf-SquareInstrument · kf-AmigaScene** (18 records); `KF-W4 §Sequencing` R-15 (the homing); `KF-W10 §6.D` row 1 (the terminal disposition, its travelling locks and its inbound blast-radius receipt); `execution/B/KF-W7.md` §3 + Act 3 (the SWAP verdicts). **No gate exists here without a named live witness (L-19).** Validated at authoring against `docs/precepts/instructions/tranche/WAVE_SPEC.md` and `STYLE.md` (**L-20**).

| verb | state | basis |
|---|---|---|
| AUDITED | **YES** | the 18 adjudicated records above, each ADJUDICATED-not-VERIFIED, each routing its behavioral remainder `NO-WAVE-OWNER` into one of this wave's nine packets by name |
| SPECIFIED | **NO — this draft is one arm of the authoring, not the spec** | SPECIFIED is stamped by the **fold seat** over the agglomerated file at `waves/KF-W11.md`. A draft that stamps itself SPECIFIED has certified its own work |
| IMPLEMENTED | NO | gates green + bytes landed in the named execution site stamps this |
| VERIFIED | NO | stamped at the successor sub-tranche's close, never here and never by KF.W10 (**§6.D**: these three *"take no verb from this close"*) |

**Goal criterion.** This wave succeeds if, after it, **every scene in the demo tells the truth about its own motion at the one moment a reader most trusts it** — the square's panel edit no longer silently discards the scene's renderer; the playback authority is singular and the transport never lies about it; the signature instruments (tether, ruler, playhead, trace, heatmap, strip) report the value they are painting rather than a stale one; and **every comment-stated invariant in the nine packets' file set is either true at the bytes or is a test obligation that fails today**. A wave that lands forty hygiene edits and leaves `useSquareKeyboard`'s docblock asserting a spring-paced tour 46 lines above `setTimeout(step, 520)` has **failed this goal**: the MISS-3 law is that a docblock asserting an invariant *is* an assertion, and an unmet assertion is a defect whether or not a test names it.

**Execution shape**: **6 seats, 3 phases** — phase 1: 3 parallel (`.a` cube ∥ `.b` sequence ∥ `.c` the spring quartet: spring + spring-plot + spring-physics-facet + spring-artifact-truth); phase 2: 2 parallel (`.d` square ∥ `.e` amiga); phase 3: 1 serial (`.f` drag-seam + close). **Peak concurrency 3**, inside the owner's four-workflow cap (§5.1). The drag-seam is deliberately last and alone: it is the only packet whose rows cross a KF.W7-evaluated surface, and it inherits landed code from that wave (§Sequencing L-1).

---

## §0 Open preconditions (checked at wave-open; NOT close gates)

| # | precondition | state at authoring (measured by this seat, 2026-09-18, at `69095552`) |
|---|---|---|
| **OP-1** | **This spec exists at the ratified path**, folded from the two blind arms and ratified. | **OPEN BY CONSTRUCTION.** `ls docs/tranches/X/keyframes/waves/KF-W11.md` → *No such file*. This draft is an input to that act, never a substitute for it. **A MINTED-UNAUTHORED wave cannot open on a draft** (§6.D: *"their authoring seat is the SS-1/SS-2 authoring block"*). |
| **OP-2** | **`G-KFW4-1` GREEN** — the `vue-tsc --noEmit` gate chassis on the merge path (KF-CB-8 + KF-CE-16 + KF-APP-4). **R-15 sequences all seventeen packets after it.** | **LANDED, HONEST-RED AT CLOSE.** KF.W4 CLOSED 2026-09-17 with G-KFW4-1 among its five honest-RED gates (LEDGER row KF.W4). **The chassis exists; its colour does not.** This wave's gates are demo-side `.vue` assertions and are unenforceable until a `.vue` file can fail a build — so OP-2 is read as *the chassis lands and its RED is relieved*, not as *the chassis is mentioned*. **HARD.** |
| **OP-3** | **KF.W7's per-surface SWAP verdicts STATED** (KF-AV-28 rider). The drag-seam packet sequences after them. | **DISCHARGED — and the answer is the positive one.** `execution/B/KF-W7.md` §3: *"six surfaces, six KEEP-BESPOKE, **ZERO SWAP**"*; *"this unit emits ZERO `DISCHARGED by KF.W7 SWAP verdict <surface>, <date>` receipts"*; discharge set **EMPTY**. KF.W7's own → KF.W11 arm reads: *"**the SequenceScrubber shape rows PERSIST**, they do not discharge … SquareScene MISS-1 shares `grabDx`'s class and the class is now LANDED here (`7225cdd1`), so arm (c) inherits a worked precedent rather than a design question."* **This wave therefore inherits work, not a deletion.** |
| **OP-4** | **Write authority named** — where kf waves execute and under whose hand (census §(c) gap 5; KF-W3 OP-2, still UNRESOLVED there). | **UNRESOLVED, and it is this wave's largest open question**: KF.W11 writes more keyframes.js demo bytes than any authored X·KF wave. This file names the question and does not answer it. **HARD.** |
| **OP-5** | **KF.W6 Glass Suffusion IMPLEMENTED** — the token/`kf-` prefix settlement the scene files now carry. | **LANDED at the frontier.** Its consequence is measured, not assumed: §B.1 row 6 shows `.focus-ring` → `kf-focus-ring` migration has already moved two of RULINGS-4's four census sites. A cure written against the register's figures without re-resolution would edit a class that no longer exists at two of its four sites. |
| **OP-6** | **KF.W8 Structure & Colocation Settle IMPLEMENTED** — the demo tree's current shape. | **LANDED at the frontier** (`69095552` is KF.W8 `.g`'s own commit). **Every path in the banked records is pre-KF.W8** and is re-resolved in §B.1/§B.2; this is why the re-anchor table is the first section of the bounds and not a footnote. |
| **OP-7** | **The owner-decision gate D-2** (kf-SquareInstrument) — the OD-5/T.M2 sign-off deciding which instrument layers live. | **OWNER-UNRULED.** The square packet's tether/chrome commits are **conditionally spent** behind it: no layer is deleted before the sign-off. `.d` states which of its rows the ruling gates **before** spending any cure (the KF.W7 G15 discharge discipline, applied to an owner gate rather than a rider). |

**OP-1, OP-2, OP-4 are hard.** OP-7 gates a named subset, not the wave.

---

## §Scope — the nine packets, one unit each except the spring quartet

| # | packet | seat | one-sentence subject |
|---|---|---|---|
| 1 | **cube** | `.a` | the matrix-editor family, the state/identity pair, the glass-conformance phantom prop, the Roll stack, the lighting-truth frame error, the latch/pointer seam, the axis-reveal rows, and the hygiene tail — **four records, one packet** |
| 2 | **sequence** | `.b` | the instrument-truth core (which of four domains is canonical), the loop-seam commit, the reel packet, the gesture spec, the ruler-legibility trio, the geometry commit, the row-surface and state-signal specs — **five records, one packet** |
| 3 | **spring** | `.c` | the contract head (who dispatches play-intent), the honest-instrument core, the swap of SpringTrace onto `.fn`, the gesture spec, the a11y one-edit family, the interaction- and coordination-truth cores |
| 4 | **spring-plot** | `.c` | SpringTrace's instrument-truth core as ONE commit family — the N-1 fork decided **first**, then the draw route |
| 5 | **spring-physics-facet** | `.c` | SpringHeatmap's field design decision (what the field encodes, on which axes, at what scale, with what legend), the input-integrity core, the two-`defineModel` contract commit |
| 6 | **spring-artifact-truth** | `.c` | StartingStyleTarget's artifact core — one source of truth for the endpoints, true polarity/duration, case-preserving register, one honest state model with a refusal surface |
| 7 | **square** | `.d` | the editor-seam BLOCKER, the playback-authority packet, the colour packet, the instrument packet, the input/a11y packet, geometry, and the hygiene tail — **two records, one packet**, gated in part by OP-7 |
| 8 | **amiga** | `.e` | the amiga scene-repair roster whole (D-1 · L-B1 · D-2 · D-3+C-18+M-3 · D-6/D-11 · D-8+C-9 · the five L-M pairs · five MISSED rows · the C tail · the INFO tail) |
| 9 | **drag-seam** | `.f` | KF-SCR-1 + L·D-1 as **ONE cure family**, spec'd with KF-AV-15 across **both** seams; C·C-1 with KF-AV-16; KF-SCR-5's dead-API/prose cleanup on the same edit |

**What this wave is not.** It is not a re-audit: the 18 records are ADJUDICATED and their ids are **for life** (anti-rename). It is not a homing act: **KF.W4 homed all nine** and this file consumes that homing. It is not the place any glass-producer row is cured (§B.4, and the travelling lock in §Sequencing L-4).

---

## §Bounds

### B.1 Frontier re-anchor (performed by this authoring seat, read-only, at `origin/master` `69095552`, 2026-09-18)

Every anchor this wave inherits is banked against `8281638c` or `81a56990`. **203 commits later, six of them needed correction, two of them load-bearing.** Each row below states the command that produced it; WRITE-THEN-MEASURE, double-run.

| # | inherited anchor ⟨bank · register⟩ | frontier truth (this seat's measurement, `69095552`) | disposition |
|---|---|---|---|
| 1 | **kf-SquareScene MISS-3** — a docblock asserting *"paced by the spring's own settle, not a fixed timer"* sitting **46 lines above** `setTimeout(step, 520)`; banked against `SquareScene.vue` | **HOLDS — and it has MOVED FILES.** ⟨cmd⟩ `git grep -n "paced by the spring" origin/master -- demo/` → `demo/scenes/square/useSquareKeyboard.ts:20`; ⟨cmd⟩ `git grep -n "setTimeout(step" origin/master -- demo/` → `demo/scenes/square/useSquareKeyboard.ts:66`. **66 − 20 = 46 exactly**, in the *extracted* composable. The colocation settle carried the defect and its comment together, unbroken. | **RE-ANCHORED; the arithmetic is the receipt.** The register's "46 lines" is TRUE at a path the register does not name |
| 2 | **kf-SquareScene C-1** — `el.style.backgroundColor` *"useSquareDemo.ts:138, the sole colour write"* | **PARTLY CORRECTED.** ⟨cmd⟩ `git grep -n "style.backgroundColor" origin/master -- demo/` → **four** hits in `demo/scenes/square/useSquareDemo.ts`: `:29` (a docblock naming `:161`), `:163` (the write), `:259`/`:260` (a settle-time clear). The write is at **`:163`**, not `:138`; **it is no longer sole** — `:260` clears it. The occluder holds: ⟨cmd⟩ `git grep -n "background-image" origin/master -- demo/scenes/square/SquareScene.css` → `:52`. **And the file's own `:29` docblock now cites `:161` for a write that lives at `:163`** — the comment drifted two lines inside the defect's own explanation. | **CORRECTED; and row 2 gains a comment-truth limb** (the MISS-3 law applied to the colour packet) |
| 3 | **kf-SquareInstrument D-1** — no `viewBox` on the tether SVG; `stroke-width: 0.8` + `vector-effect: non-scaling-stroke` are no-ops at 1:1 | **HOLDS EXACTLY.** ⟨cmd⟩ `git grep -c "viewBox" origin/master -- demo/scenes/square/SquareInstrument.vue` → *(no output; exit 1)* = **0**. ⟨cmd⟩ `git grep -n "vector-effect\|stroke-width" origin/master -- demo/scenes/square/SquareInstrument.vue` → `:162 stroke-width: 0.8;` · `:165 vector-effect: non-scaling-stroke;`. | anchor-safe, line anchors re-pinned |
| 4 | **kf-CubeScene D-2/C-3** — *"exactly four `variant:` Button sites demo-wide, all four in `h()` render functions (CubeScene ×2, SpringScene ×2)"* | **HOLDS EXACTLY, count and distribution.** ⟨cmd⟩ `git grep -n "variant:" origin/master -- demo/` → **4**: `demo/scenes/cube/CubeScene.vue:193`, `:198`; `demo/scenes/spring/SpringScene.vue:202`, `:225` — all four `variant: "outline"`, all four inside `h()`. **The idiom that escapes the SFC compiler and every eye survived 203 commits and a type-gate wave.** | anchor-safe — and it is G-KFW11-6's denominator |
| 5 | **kf-SquareScene L-1/C-3** — *"the ONLY `animationGroup.pause()` in any scene"* | **HOLDS.** ⟨cmd⟩ `git grep -c "animationGroup.pause()" origin/master -- demo/` → `demo/scenes/square/SquareScene.vue:1` — one file, one hit, zero elsewhere. | anchor-safe; the census claim is re-executable |
| 6 | **`.focus-ring` consumers = exactly 4** (`KeyframeCard.vue:45` · `SpringHeatmap.vue:30` · `SpringTarget.vue:63` · `SquareScene.vue:46`), per **RULINGS-4 R4-4**, quoted at §6.D's KF.W13 row | **DRIFTED — and two of the four are no longer that class.** ⟨cmd⟩ `git grep -n '\bfocus-ring\b' origin/master -- demo/ \| grep -v kf-focus-ring \| grep -v focus-ring-shadow` → **bare `.focus-ring` class applications = 2**: `demo/scenes/spring/SpringTarget.vue:63`, `demo/scenes/square/SquareScene.vue:46`. `KeyframeCard` and `SpringHeatmap` now carry **`kf-focus-ring`** (`KeyframeCard.vue:117`, `SpringHeatmap.vue:37`), the KF-KE-30 prefixed idiom KF.W6 landed. | **DECLARED RE-ANCHOR, stated not patched.** The register's figure is RULINGS-4's and RULINGS-4 is that round's fixed authority — **it is not amended here** (E-3). What this file books is the frontier reading beside it, with its command. **Two of this wave's files own two of the two bare sites**, so the drift lands in this wave's bounds and in KF.W13's (§Cross-edges → KF.W13) |

**The rule this table exists to state.** Rows 1 and 6 are the two that would have sent a cure to a byte that is not there. Row 1 survives as a *stronger* finding at a path the bank does not name; row 6 fails at half its sites. **Neither was findable from the register alone** — which is the whole argument for a re-anchor pass being §B.1 rather than a closing note.

### B.2 Owned files — demo half (all under `/Users/mkbabb/Programming/keyframes.js/` unless marked value.js)

Line counts are `git show origin/master:<path> | wc -l`, measured twice by this seat at `69095552`.

| file | L | access | packet · why |
|---|---|---|---|
| `demo/scenes/cube/CubeScene.vue` | 294 | modify | **cube** — the glass-conformance phantom prop (`:193`/`:198`), the state/identity rows, the C-15 limb, MISS-1's tabpanel |
| `demo/scenes/cube/CubeTarget.vue` · `CubeTarget.css` | 271 · 210 | modify | **cube** — the Roll stack, the lighting-truth frame error (#56), the latch/pointer seam, the token-hygiene rider |
| `demo/scenes/cube/CubeAxisLines.vue` | 120 | **READ-ONLY WITNESS — NOT OWNED** | the **AXISLINE-UNIT** is KF.W12's packet (§6.D row 2). Cube rows #31/#57 name its bytes; **this wave books the finding and cures nothing there** (§Cross-edges → KF.W12) |
| `demo/scenes/cube/matrix-editor/MatrixEditor.vue` · `transformMath.ts` · `useTransformState.ts` | 187 · 186 · 227 | modify | **cube** — the ME family core + the sync-topology spec (ME-30/ME-31 ≡ kf-CubeScene L-4+M-6/L-3, **one spec**) |
| `demo/scenes/cube/orbital-drag/**` (9 files) | 354 · 249 · 201 · 145 · 116 · 64 · 34 · 15 (+`index.ts`) | modify | **cube** — the gesture-math + hand-off family, the latch family, the model-contract family, the presentation arm |
| `demo/scenes/cube/useCubeDemo.ts` · `useCubeRelit.ts` · `cubeTransformStore.ts` · `cubeKeys.ts` | 191 · 87 · 20 · 7 | modify | **cube** — the graph attitude contract gap (#56's cure is a *parameter*, not arithmetic), the relit egg, the ppMode checklist |
| `demo/scenes/sequence/SequenceScene.vue` | 43 | modify | **sequence** — the loop-seam commit (L-3 + L-10 + SC-5 + SC-6, one `SweepSceneOptions` parameter), the transport-surface decision |
| `demo/scenes/sequence/SequencePlayhead.vue` · `SequenceAxis.vue` | 92 · 49 | modify | **sequence** — the instrument-truth core (N-1 · N-2 · D-1 · N-4), the ruler-legibility trio, the geometry commit, `--tick-count` |
| `demo/scenes/sequence/SequenceTarget.vue` · `SequenceTarget.css` | 252 · 271 | modify | **sequence** — the geometry commit, the state-signal spec (ST-4 closes three rows), ST-1's **one-word** affordance edit + the `:163-167` deletion |
| `demo/scenes/sequence/useSequenceDemo.ts` · `useSequenceInstrument.ts` · `useTypedTrigger.ts` | 482 · 45 · 31 | modify | **sequence** — the reel packet, the store hygiene, the prose-truth sweep |
| `demo/scenes/sequence/SequenceScrubber.vue` | 193 | modify | **BOTH sequence AND drag-seam** — the file is split by packet, not by hand: `.b` owns the scene rows (`kf-SequenceScrubber.md:137`), `.f` owns the seam rows (`:136`). **The two seats never hold it at once** (§Sequencing L-5) |
| `demo/scenes/spring/SpringTarget.vue` · `SpringScene.vue` | 476 · 272 | modify | **spring** — the contract head C-1, the honest-instrument core, M-4→M-3, the interaction-truth core |
| `demo/scenes/spring/SpringTrace.vue` | 148 | modify | **spring-plot** — the instrument-truth core D-1 + N-1 + N-2 + C-2/L-3 as ONE commit family |
| `demo/scenes/spring/SpringHeatmap.vue` · `SpringPhysicsFacet.vue` | 383 · 262 | modify | **spring-physics-facet** — the field design decision, the input-integrity core, the two-`defineModel` contract commit, SPF-3's clamp policy |
| `demo/scenes/spring/StartingStyleTarget.vue` | 297 | modify | **spring-artifact-truth** — the artifact core, the verb/state pair, the refusal surface |
| `demo/scenes/spring/useSpringDemo.ts` · `useSpringDerby.ts` · `useSpringHotPath.ts` · `useSpringLinearStops.ts` · `useCompiledEntry.ts` · `springPresets.ts` · `springKeys.ts` | 499 · 115 · 146 · 34 · 84 · 42 · 10 | modify | **spring / spring-plot / spring-artifact-truth** — the coordination core (one debounced cancellable recompile seam), the derby prose corrections, KF-SST-31's view gate |
| `demo/scenes/spring/useSpringKeyframesEditor.ts` | 77 | **READ-ONLY WITNESS** | the KFED seam is **KF.W12's** (KFED-UNIT). This wave measures the mount, never cures it |
| `demo/scenes/square/SquareScene.vue` · `SquareScene.css` | 331 · 159 | modify | **square** — the playback-authority packet, the colour packet, the input/a11y packet, geometry |
| `demo/scenes/square/SquareInstrument.vue` | 212 | modify (**OP-7-gated in part**) | **square** — the tether-truth commit, the instrument-truth commit, the disclosure commit, the field/chrome commits |
| `demo/scenes/square/useSquareDemo.ts` · `useSquareKeyboard.ts` · `useSquareTumble.ts` · `squareKeys.ts` | 429 · 103 · 50 · 5 | modify | **square** — the colour write at `:163`, the MISS-3 comment-invariant at `useSquareKeyboard.ts:20/:66`, the tumble accumulator desync |
| `demo/scenes/amiga/AmigaScene.vue` · `useAmigaDemo.ts` · `useAmigaThree.ts` · `useSphereSpin.ts` · `utils.ts` · `amigaKeys.ts` | 302 · 156 · 272 · 245 · 103 · 5 | modify | **amiga** — the packet whole |
| `demo/components/playback/AnimationVisualizer.vue` | 305 | modify (**seam rows only**) | **drag-seam** — KF-AV-15 / KF-AV-16's halves. `grabOffset` at `:107`/`:132`/`:224`/`:244` is the seam this wave makes symmetric with the scrubber's. **KEEP-BESPOKE is settled (OP-3); this is not a swap** |
| `demo/scenes/spring/useSpringDerby.ts` prose sites + `demo/DESIGN.md` | 115 · — | modify (**prose only**) | the prose-truth sweeps the packets carry; `DESIGN.md` edits are **corrections of false statements**, never new design |
| `docs/tranches/X/keyframes/evidence/W11/**` *(in value.js)* | — | create | born-RED baselines, the per-packet before/after witnesses, the two SS-13 shared-capture receipts this wave is allowed |
| `docs/tranches/V/coordination/INBOX.md` *(in value.js)* | — | modify-append | E13 rows: the glass-producer relays this wave discovers and **routes to SS-6 rather than curing** |

### B.3 Read-only witness sites (named, never opened for writing by this wave)

- `demo/scenes/cube/CubeAxisLines.vue` — **AXISLINE-UNIT, KF.W12's**. Rows #31/#57 are cube-packet *findings* whose cure is another packet's; naming them here without curing them is the anti-re-book law working.
- `demo/components/instrument/timeline/components/TimelineTrack.vue` — `grabDx` at `:480`/`:489`/`:557`/`:703`. **KF.W7 landed this arm** (`7225cdd1`); it is the drag-seam's **worked precedent**, read and never re-written.
- `demo/components/instrument/keyframes/composables/useKeyframeOps.ts:69` (`animation.adoptCompiled(compiled)`) and `src/animation/engine/animation.ts:159/:269` (`usesDefaultRenderer` / `adoptCompiled`) — **the square editor-seam BLOCKER L-2's other end**. This wave owns the *scene* half and the decision; the **engine-seam limb is a library act** and is declared, not spent (§Excluded E-3).
- `src/animation/compile/frame/compiler.ts:118` — the in-library comment that already names the contract (*"the compiler never replaces it. Exposed so `Animation.adoptCompiled`…"*). It is the reference test L-2's cure must satisfy.
- `demo/components/playback/PlaybackRibbon.vue` — **transport/ribbon is KF.W13's**. The spring packet's "sequenced with the transport/ribbon packet" note is a *sequencing* obligation, not a write grant.

### B.4 Explicitly out of bounds

- **All glass-producer rows → SS-6 BH relay**, never a demo-side hack. This is a travelling lock (§Sequencing L-4), not a preference.
- `demo/components/instrument/**` except the two named seam files — **KF.W12's** (CARD/OPTIONS/KFED/EDITOR/APPLY units).
- `demo/app/**`, `demo/components/playback/PlaybackRibbon.vue`, `demo/components/instrument/transport/TransportDock.vue`, `demo/styles/playback-idiom.css` — **KF.W13's**.
- `src/**` — the library. The L-2 engine limb, the C-3 export decision, and every value.js-facing row are declared to their owners and cured by none of this wave's seats.
- value.js `scripts/dev/dev.sh` — **never staged, by standing arrangement**.

### B.5 LAW A censuses — every delete this wave books

**LAW A (RULINGS-3): no delete is authorized on a line-count.** Three deletes are booked; each carries its import-graph or consumer census, run at `69095552`, and no seat may spend one without re-running it:

1. **ST-1's `SequenceTarget.css:163-167` deletion** (the affordance edit's other half) — consumer census = the selector's applications in `SequenceTarget.vue`, re-derived at execution; the one-word edit at the `.vue` and the deletion **land in one commit or neither**.
2. **kf-SequenceScene N-3's `:76-78` deletion** (PlaybackRibbon's) — **NOT THIS WAVE'S**; listed here only so a sweeping seat does not adopt it. → KF.W13.
3. **the dead-declaration family** (kf-SequencePlayhead D-13/L-12; kf-SpringTarget's dead guards and regex dying with M-4→M-3) — each census is the symbol's reachable consumer set, pasted in the unit's receipt; **a declaration with zero consumers is still a byte with a comment that may be load-bearing**, so each row states whether its comment dies with it.

### B.6 Scope receipts (R4-10(1) — a sweep's enumeration source is the SCOPE, never the example list)

- **Packet count**: 9. ⟨cmd⟩ `grep -c '^| [1-9] | \*\*' KF-W11.md` over §Scope → **9**; the same nine names appear in §Carry's headings and in §Gates' subjects, and no tenth name occurs in any of the three.
- **Record count**: 18, enumerated by name at §Provenance. ⟨cmd⟩ against `registry/adjudicated/` confirms each is a file; **none is re-audited here**.
- **Owned-file count**: enumerated by path at §B.2; the union is `demo/scenes/**` ∪ {`demo/components/playback/AnimationVisualizer.vue`} minus the two read-only witnesses named at §B.3, and it is **computable from the table, not from a wildcard**.

---

## §Carry — the nine packets, cargo by banked id (never transcribed, deduped by identity)

**Carriage is not asserted in this file's voice (LAW B).** Every list below is the banked routing block's own enumeration, consumed by mechanism; the completeness claim belongs to the fold seat's conformance artifact, not to a draft.

### C-1 · **cube** (`.a`) — four records, one packet; spine = kf-CubeScene's routing block
- **matrix-editor family core** — kf-CubeScene L-3 · L-4+M-6 · L-5/C-6/S-9 · C-7/L-13/L-14 · L-9 · C-16+D-10 · MISS-1 · M-5 · M-8, with kf-MatrixEditor ME-1·2·4·9·10·11·12·15·16·23·26·27·33·34·35·36·37·38·39·41·**42·43·44·45**.
- **sync-topology spec** — ME-30 · ME-31 **≡ kf-CubeScene L-4+M-6/L-3, ONE spec** with ME-7's retain-and-retarget cure. *(Fold by reference; zero re-bookings — the anti-rename law applied to the registry's own seats.)*
- **ME-42's edit-representation limb** is the packet's sharpest row: the 2-dp display string **is** the write path, so the digit policy must state the *edit* representation or `formatOptions` must separate the two. A display-only cure is the defect.
- **state/identity packet** — L-2/C-5+C-20 · L-7 · L-19 · L-29.
- **glass-conformance packet** — D-2/C-3 + D-3/C-4+D-25inv, **with the SpringScene rider riding the same commit** (the four `variant:` sites are one edit; see G-KFW11-6).
- **KF-APP-1/-17 motion** — with **ARB-1's sequencing law (delete arm only, until the glass-ui M-A relay lands)** + MISS-4's ppMode checklist + the C-14 bucket rider. **The `headerLeft` 'fill' arm is a TRAP** (it lands inside an inert toolbar) — **DELETE is the only self-contained cure until KF-APP-5's producer relay lands.**
- **C-15 limb** — settled WITH ACG D-1's portal-architecture cure.
- **CubeTarget** — Roll stack #1·#2·#5·#15·#20·#38·#49 (one four-defect repair, born-RED gate) · **#53 + #7 as one paired seam repair** (demo serialization + library diagnostic, its own born-RED gate) · transform-ownership #6 · **lighting-truth #4/#16/#36/#56** (the graph attitude is a *contract gap*: `useCubeRelit` receives no graph attitude at all) · latch/pointer #13/#18/#19/#54/#55 · loading-state #12/#22 · #14 · #17 · **axis-reveal #31/#57 — BOOKED, CURED AT KF.W12** · hygiene #23–#28/#32/#33/#35/#37 · INFO #29/#40–#48/#52/#59.
- **OrbitalDrag** — gesture-math + hand-off OD-1·7·12·13·21·22·27·33·36 · latch OD-3·4·5·6·25·31 (cure = KF-AX-1's registry adoption + blur clear + expose reset) · model-contract OD-2·8·14·16·18·19·20·23·24 · presentation OD-10·32·35·43 · hygiene/INFO OD-17·26·28·30·34·37·39·40·41·42·44.

### C-2 · **sequence** (`.b`) — five records, one packet
- **instrument-truth core** — N-1 · N-2 · D-1 · N-4, with the ruling-1 cure (`gap: 0.5rem var(--col-gap)`), the **K-25 sequencing note** (N-1's domain cure and N-2's recompute cure land *against each other*) and M-10's adopted frame: **the repair is a decision about which of four domains is canonical — one spec, not four patches.**
- **loop-seam commit** — L-3 + L-10 + SC-5 + SC-6 (one `SweepSceneOptions` parameter: supply the loop verbs; one adapter, one name).
- **transport-surface decision** — SC-2 (+ D19(a)'s dead reverse arm, L-12/C-8's 10 unconsumed members): expose the verbs or delete them, and give the drag gesture its undo.
- **reel packet** — D7 + SC-3 + L-8 + L-9 + SC-7 + D9/D22-residue.
- **row-surface packet** — D2 (+SA-1) · D3 · D10 · SC-8 (≡ N-12's interaction spec) · D16 · D15.
- **ruler-legibility trio** — D-3 · D-2 · D-4 (container-query label thinning; graduations off the label band; the two-pitch law via the already-minted `--graph-*` tokens).
- **geometry commit** — D-10/L-2 + D-2/L-4/C-5 (via N-8's owning-side home) + D-6 + D-13 + D-22 + ST-9, **landing WITH the head/label collision cure**.
- **state-signal spec** — D-9 + D-15 + ST-2 + **ST-4 (one binding closes three rows)** + ST-10.
- **a11y cluster** — D-5 (heading) + **ST-1 (the one-word focus-ring cure + the `:163-167` deletion)** + the N-14 fold's riders: **valuetext SEQUENCED AFTER N-1**; PageUp/PageDown.
- **gesture/ownership spec** — L-5 + ST-7 + ST-8 + **L-2 (seam-level with KF-AV-15 — the drag-seam's edge, see L-5)** + ST-6.
- **scrubber scene rows** — the gesture spec (`applyScrub(p, {gesture})` landing C-2/L-D-4 + KF-SCR-6 + the N-13 bloom rider + C-12's deadband at once) · the geometry residue · the typography pair · the prose-truth sweep · hygiene · a11y · **L·D-12's born-RED mount test**.
- **test obligation** — **N-10**: the three-rect equality **and** the axis-terminal-label = duration gate, **both born-RED** (see G-KFW11-9).

### C-3 · **spring** (`.c`)
- **contract head C-1** — one decision: who dispatches play-intent, rest-on-entry posture preserved.
- **honest-instrument core** — M-2 (clamp-with-allowance) + D-2 (pulse re-home) + D-7/C-8 (overshoot room or clip allowance) + the **six prose corrections** the record enumerates.
- **M-4→M-3** — swap SpringTrace onto `.fn`; **the parser, both dead guards and the regex die together** (LAW A census at §B.5).
- **M-5** — clearTimeout + gate on `derbyActive`. **N-1** — base-rule transitions + a leave treatment.
- **gesture spec** — D-5 (owner decision) · m-11 (race-time rail policy) · i-18 · N-3 — **one spec, not four patches.**
- **a11y one-edit family** — D-6 heading · D-14 status region · N-4 name+valuetext.
- **interaction-truth core** — KF-SS-1 (reseat participates in the machine) · KF-SS-2 (facility-aware Reset) · KF-SS-8 (the transport drives the selected channel) · KF-SS-10-as-witness.
- **coordination core** — KF-SS-3 (one debounced, cancellable recompile seam) + KF-SS-16/17's banked-cure riders.
- **surface sweep** — KF-SS-4 · 5 · 7 · 18 · 19 · 21 · 22..30 · 33 + the INFO tail; D-9 + C-7-nit · D-3's three-site flag pass · N-2's register cure · N-6's generation guard · the hygiene tail.

### C-4 · **spring-plot** (`.c`)
- **instrument-truth core D-1 + N-1 + N-2 + C-2/L-3 as ONE commit family.** **The spec decides the N-1 fork FIRST** (drop the prop, or label x in ms and discharge D-6), **then** the draw route — anchor hoist minimum; the shared-builder route with a range parameter preferred, its value.js/easing trade stated. Inside it: the parser posture (D-12/L-6/C-4 + the filter cell), L-5's bindings, D-11's label, L-10's extraction/test obligation.
- **mark-design rows** D-3 + D-4 + N-3 (neutral reference vocabulary, EasingTarget precedent; one motion) + D-6's labels + D-7's allocation + D-13's containment.
- **type/register rows** D-2 + N-4 (+ M-6 spacing rider) + D-10 + D-9 — one register pass over the header. **N-5** the column's overflow posture. **L-8** (pending SS-13 #5). **L-14** (document or clamp the coupling). **D-16 recorded clean-by-construction so no sweep manufactures work.**

### C-5 · **spring-physics-facet** (`.c`)
- **instrument-truth core** D-B1 · D-B2 · D-M8 · D-M1(+rider) — **one design decision** (what the field encodes, on which axes, at what scale, with what legend), **not four patches**.
- **input-integrity core** L-M-1 · N-SH-1 · N-SH-2(site) · D-M5 · N-SH-6(site) · N-SH-4 — one lattice, one gesture/focus spec, one honest published tolerance.
- **contract commit** L-M-7/C-M-2 + C-M-3 — two `defineModel`s; the facet binds the heatmap exactly as its sliders do.
- **SPF-3** — the preset-ball painter clamps to `[0,1]` and discards **100 %** of the overshoot that is the only thing distinguishing the four presets, while the sibling painting from the same array relaxes to 1.18 under written rationale. **Three clamp policies over one array in one scene; only the strictest is undocumented.** Pairs with SPF-15. *(L's cure #2 is KILLED (K-10); sound forms = remap `[-0.25,1.25]→[0,1]`, or a stated-geometry headroom cap.)*
- **SPF-13** — rides **KF-CO-47's** decision (tooltip idiom vs slotted copy), which is **OPTIONS-UNIT's = KF.W12's**. **Booked here, decided there** (§Cross-edges → KF.W12).
- N-SH-3's transition decision · D-M4 + D-M7 + N-SH-5 · the prose-truth sweep L-M-2/5/6 · the hygiene deletions + INFO residues · **L-m-5's born-RED test obligation** (see G-KFW11-11).

### C-6 · **spring-artifact-truth** (`.c`)
- **artifact core** KF-SST-3 · 4 · 5 · 9 (+ 11 · 12 · 26) — **ONE spec**: a single source of truth for the endpoints, the true polarity/duration, a case-preserving register, a readable+reachable viewport, one honest state model **with a refusal surface**.
- **verb/state pair** KF-SST-1 · 6 — which copy survives, and the survivor gets `aria-expanded`/`aria-pressed` (buying three vendor affordances).
- **instrument-truth row** KF-SST-2 (the response/duration decision, priced range caveat). **facility-honesty row** KF-SST-10 (with KF-ES-2's cure spec). **hot-path row** KF-SST-31 (view gate + house throttle, with 12's guard).
- **prose-truth sweep** KF-SST-20 · 21 · 22 (+ 13's dead block, 25's chain, 33's leads). **hygiene tail** 15 · 16 · 17 · 18 · 19 · 23 · 24 · 27 · 29.

### C-7 · **square** (`.d`)
- **editor-seam packet** — **L-2 BLOCKER**: preserve-the-renderer at `useKeyframeOps`/`adoptCompiled`, + the `RAFPlayback` degrade-not-wedge rider (D-27/L-7/C-9). **The engine-seam decision is declared, not spent here** (§Excluded E-3); `usesDefaultRenderer` already exists as the reference test.
- **playback-authority packet** — L-1/C-3 · L-8/C-2 (**settled WITH kf-CubeScene L-2/C-5 — ONE playing-state authority; `facility.isPlaying()` exists**) · C-4 · L-3 · L-5 + ARB-1 · D-14/L-9 · MISS-9.
- **colour packet** — C-1 (the sole colour write at `:163` is occluded by `.demo-box`'s opaque `background-image` at `SquareScene.css:52`) · L-11/C-5 · MISS-4 · MISS-5 · L-22a. **One honest cure**: sweep the gradient's own custom property, or reset `background-image` while an inline colour owns the paint. **Plus the `:29` docblock's two-line drift** (§B.1 row 2).
- **instrument packet** — D-1 + MISS-2 (**px-space geometry; N-SQ-4 ORDERS the cure: derive-from-travel or px-space, NEVER `viewBox`+`none`**) · L-4/C-12 · D-9 · D-21 · D-15; the tether-truth commit D-1 + D-6 + D-16 + N-SQ-4; the instrument-truth commit C-3/L-D3 + N-SQ-1 + L-D5 + N-SQ-8 (**one clock question**: what does the strip report, in which mode, under whose name).
- **owner-decision gate D-2** (OP-7) — the OD-5/T.M2 sign-off decides which layers live; **D-18, D-10, N-SQ-5 ride it.**
- **input/a11y packet** — MISS-1 `grabDx` (**inherits KF.W7's landed class**, `7225cdd1`) · D-4 · D-7 · D-5 + L-17 · D-6/L-6 · D-19 · D-11 · D-12; the disclosure commit D-4 + D-5 + C-6.
- **field/chrome commits** — D-7 + N-SQ-2 + N-SQ-3; D-8 + D-11 + N-SQ-5. **geometry** D-8.
- **hygiene/prose tail** — D-10 · D-13 · D-16 · D-17 · D-20 · D-22 · L-10 · L-13/C-11 · L-15 · L-16/C-10 · L-18 · L-19 · C-8 · **MISS-3 (the comment-invariant law, G-KFW11-5)** · MISS-7 · MISS-8 · MISS-10 · D-24/D-25 · L-22b/c; D-9/C-7 · D-14 · D-15 · D-17 (×4 sites) · L-D8 · L-D10 · N-SQ-6/7/9 · C-9 · C-5's rider · D-19 · L-D6 · L-D9 · the S-9/S-10 intake.

### C-8 · **amiga** (`.e`)
D-1 · L-B1 · D-2 (shared idiom) · D-3+C-18+M-3 · D-6/D-11 · D-8+C-9 · L-M1/C-8 · L-M2/C-14 · L-M3/C-11 · L-M4/C-2 · L-M5/C-4 · MISSED-A · MISSED-C · MISSED-E · MISSED-F · MISSED-G · C-16 · C-17 · C-20 · C-10 · C-5 · L-m3 · L-m6 · **all INFO rows**. *(kf-AmigaScene D-2 banks SquareScene as the house exemplar among the subject scenes — the shared-idiom row is a **borrowing**, and the square packet's own a11y cure must land before amiga borrows from it: §Sequencing L-6.)*

### C-9 · **drag-seam** (`.f`)
- **KF-SCR-1 + L·D-1 as ONE cure family** — re-entrancy guard + `pointerId` latch + scope-disposal release + **the "Nesting-safe" docblock corrected** — spec'd with **KF-AV-15 across BOTH seams**.
- **C·C-1** spec'd with **KF-AV-16** — throttle-vs-decouple at the machine seam, **one decision for pointer + coast**.
- **KF-SCR-5's dead-API/prose cleanup rides the same seam edit.**
- **KF-AV-17's resize-immune form is a LOCK from KF.W7** (`execution/B/KF-W7.md`, `.a`'s verdict block): `translateX(calc(var(--p) * (100cqw − 100%)))` binds any transform-based playhead, and **the overshooting `translateX(calc(p*100cqw))` form is LIVE** — re-verified by this seat at the frontier: ⟨cmd⟩ `git grep -n "translateX" origin/master -- demo/scenes/sequence/SequenceScrubber.vue` → `:34 :style="{ transform: \`translateX(calc(${clamp(demo.progress.value, 0, 1) * 100}cqw))\` }"`. **K-12's own coordinate, unmoved across 203 commits.**

---

## §Agent Units

| unit | seat | packets | writes | closes when |
|---|---|---|---|---|
| `.a` | Opus (challenge/mechanical: censuses, greps, four-record fold) | **cube** | `demo/scenes/cube/**` except `CubeAxisLines.vue` | G-KFW11-6 · G-KFW11-7 · G-KFW11-8 GREEN; #31/#57 **booked and routed**, not cured |
| `.b` | Fable-worker ∥ Opus-worker → fresh-Fable arbiter (**the domain decision is a design act**) | **sequence** (incl. SequenceScrubber's *scene* rows) | `demo/scenes/sequence/**` | G-KFW11-9 GREEN; N-1's canonical-domain decision **written before any of the four patches** |
| `.c` | Fable-worker ∥ Opus-worker → fresh-Fable arbiter | **spring · spring-plot · spring-physics-facet · spring-artifact-truth** | `demo/scenes/spring/**` except `useSpringKeyframesEditor.ts` | G-KFW11-10 · G-KFW11-11 · G-KFW11-12 GREEN; the N-1 fork and the field-encoding decision **each written before their commit family opens** |
| `.d` | Fable-worker ∥ Opus-worker → fresh-Fable arbiter | **square** | `demo/scenes/square/**` | G-KFW11-1 · G-KFW11-2 · G-KFW11-3 · G-KFW11-4 · G-KFW11-5 GREEN; OP-7-gated rows **named before any cure is spent** |
| `.e` | Opus solo | **amiga** | `demo/scenes/amiga/**` | G-KFW11-13 GREEN; the shared-idiom borrow **verified against `.d`'s landed a11y cure**, not against the bank |
| `.f` | Opus solo (a seam, with a landed precedent — mechanical by M-12's rule) | **drag-seam** + close | `demo/scenes/sequence/SequenceScrubber.vue` (seam rows) · `demo/components/playback/AnimationVisualizer.vue` | G-KFW11-14 · G-KFW11-15 GREEN; the close record written; **zero `DISCHARGED by KF.W7 SWAP verdict` receipts emitted** (OP-3: the discharge set is empty and this wave adds nothing to it) |

**Receipts every seat**: line 1 = `SERVED MODEL: <id>`; ⟨cmd⟩ provenance on every published figure; **self-count law**; dated corrections appended, never rewritten (E-3).

---

## §Gates — fifteen, every one **BORN-RED**, every one with its literal command

**Gate law.** No gate exists without a named live witness (L-19). Every command below was **run read-only at `69095552` by this authoring seat** and its output is quoted; a gate whose command returns something else at execution has been re-anchored by the tree and is **declared, not adjusted**. Commands are portable BSD.

### G-KFW11-1 — the square editor seam preserves the scene's renderer · **BORN-RED**
**Statement.** After a keyframes-panel edit, the square scene's nested `transform.a.b.c.d` leaf remains reachable from the engine path.
**Falsifier today.** `useKeyframeOps.updateFromString` recompiles with no transform; `resolveTransform(undefined)` installs the default DOM renderer; `adoptCompiled` transplants the compiler whole and copies the flag. From the first round-trip the leaf is unreachable — **silently, and Play-only.**
⟨cmd⟩ `git grep -n "adoptCompiled" origin/master -- demo/ src/` → `demo/.../useKeyframeOps.ts:69` · `demo/scenes/cube/useCubeDemo.ts:70` · `src/animation/engine/animation.ts:269`; ⟨cmd⟩ `git grep -n "usesDefaultRenderer" origin/master -- src/` → `src/animation/engine/animation.ts:159` · `compile/emit/entry.ts:279` · `compile/emit/refusal-probes.ts:32` — **the reference test exists and nothing in the demo path consults it.**
**GREEN requires** a mounted vitest that edits the panel string and asserts the leaf still paints. **RED today: the test does not exist and would fail if it did.**

### G-KFW11-2 — one playback authority · **BORN-RED**
⟨cmd⟩ `git grep -c "animationGroup.pause()" origin/master -- demo/` → `demo/scenes/square/SquareScene.vue:1` — the only direct group pause in any scene, **behind the machine**, against the repo's own named law.
**GREEN requires**: the count goes to **0** and the takeover edge dispatches PAUSE through the machine; `isPlaying` is either exposed and read or deleted (**one authority — `facility.isPlaying()` exists**); the keyboard layer's pause sits on the FSM edge, not on one input modality. **Denominator stated: 1 → 0, measured by the same command.**

### G-KFW11-3 — the colour write paints · **BORN-RED**
⟨cmd⟩ `git grep -n "style.backgroundColor" origin/master -- demo/` → `useSquareDemo.ts:29` (docblock, **cites `:161`**) · `:163` (the write) · `:259`/`:260` (the clear). ⟨cmd⟩ `git grep -n "background-image" origin/master -- demo/scenes/square/SquareScene.css` → `:52`.
**GREEN requires**: a pixel witness showing the tumble sweep and all five tour stops reaching the plate, **and** the `:29` docblock naming the line the write actually occupies. **RED today on both limbs.**

### G-KFW11-4 — the tether is drawn in a frame that exists · **BORN-RED**
⟨cmd⟩ `git grep -c "viewBox" origin/master -- demo/scenes/square/SquareInstrument.vue` → **0** (exit 1). ⟨cmd⟩ `git grep -n "vector-effect\|stroke-width" …` → `:162` · `:165` — **both no-ops at 1:1**.
**GREEN requires** N-SQ-4's ordered cure — **derive-from-travel or px-space, never `viewBox`+`none`** — and the instrument rendering against a subject travelling ±110 px. **The cure that adds a `viewBox` fails this gate by construction.**

### G-KFW11-5 — every comment-stated invariant in the packet file set is true or is a failing test · **BORN-RED** *(the MISS-3 law, and the wave's goal criterion in gate form)*
⟨cmd⟩ `git grep -n "paced by the spring" origin/master -- demo/` → `demo/scenes/square/useSquareKeyboard.ts:20`; ⟨cmd⟩ `git grep -n "setTimeout(step" origin/master -- demo/` → `:66`. **66 − 20 = 46.**
**GREEN requires** either a spring-settle-paced tour **or** the docblock corrected **and** a test that fails if the pacing regresses. **A comment edit alone does not discharge this gate** — the law is that a comment-stated invariant is a *test obligation*.

### G-KFW11-6 — zero phantom glass props in `h()` render functions · **BORN-RED**
⟨cmd⟩ `git grep -n "variant:" origin/master -- demo/` → **4**: `CubeScene.vue:193`, `:198`, `SpringScene.vue:202`, `:225` — all `variant: "outline"`, **not a prop of installed glass-ui 7**, landing in the DOM as a junk attribute.
**GREEN requires**: the command returns **0**, and **all four land in ONE commit** (the SpringScene pair is the cube packet's rider — C-1). **Denominator 4 → 0.** *(KF-APP-4 is why it compiled; the type gate's relief is OP-2's, not this gate's.)*

### G-KFW11-7 — the Roll stack and the lighting frame · **BORN-RED**
**Two assertions, one gate**: (a) the Roll stack's four-defect repair asserts a computed transform after a pre-start drag (#53 + #7's paired seam gate); (b) `useCubeRelit` receives the **graph attitude** — today ⟨cmd⟩ `git grep -n "rotate3d" origin/master -- demo/scenes/cube/` names the mount-time `rotate3d(-1,1,0,30deg)` frame that `litFor` never sees, and `CubeTarget.vue` passes `transform` alone. **A constant ~30° error at rest, compounding #4's sign flip.**
**GREEN requires** both assertions pass. **`0.6 → −0.6` alone fails this gate** — it fixes the model's frame, not the screen's (β's insufficiency caveat, adopted).

### G-KFW11-8 — the matrix editor has a model behind its field · **BORN-RED**
**ME-42**: the 2-dp truncated display string **is** the write path; editing a cell holding >2 decimals (`0.7071067811865476` → `"0.71"`) commits precision-destroyed values.
**GREEN requires** a test that round-trips a >2-dp cell through an edit and asserts full precision survives — i.e. the digit policy states the **edit** representation, not only the display one. **ME-43 rides**: the same gate asserts a `-1000` value is readable (title/tooltip/`aria-describedby`) on the **selected** cell, where ME-2 destroys the ellipsis.

### G-KFW11-9 — the sequence instrument reports one canonical domain · **BORN-RED** *(N-10's obligation, both limbs)*
**Limb 1** — the three-rect equality. **Limb 2** — the axis terminal label equals the duration.
**Sequencing lock inside the gate (K-25)**: N-1's domain cure and N-2's recompute cure **land against each other**; the gate is written once, after the canonical-domain decision, and **is not four patches' union**. **N-14's valuetext cure is SEQUENCED AFTER N-1** and is asserted in the same run.
**RED today**: neither limb has a test; ⟨cmd⟩ `ls demo/scenes/sequence/*.test.ts` → *No such file*.

### G-KFW11-10 — the spring plot's first and last plotted x are 0 and 100 · **BORN-RED**
The banked M-3 gate, verbatim as a test obligation. **Riders asserted in the same run** (kf-SpringTarget's own four): (a) paused-entry rail-tap moves the ball (C-1); (b) a double-tap at t≈1500 ms leaves the overlay mounted through race #2 (M-5); (c) the settle pulse paints at `target`, not at 1 (D-2).
**RED today**: ⟨cmd⟩ `git grep -c "SpringTrace" origin/master -- test/` → **0** — no test names the component.

### G-KFW11-11 — the heatmap is reversible and its tolerance is published honestly · **BORN-RED** *(L-m-5's obligation)*
Three assertions: a **reversibility property test**; the **N-SH-4 tolerance assertion**; the **D-B2 mix-table snapshot**. **All RED today with zero browser** — the record says so and this seat re-confirms no test file names the component.
**SPF-3 rides the same gate**: the preset-ball painter must express the four presets' analytic peaks (1.005 / 1.068 / 1.205 / 1.000) or state its headroom geometry. **A clamp to `[0,1]` fails.**

### G-KFW11-12 — the artifact tells the truth about its own endpoints · **BORN-RED**
KF-SST-3/4/5/9: one source of truth for the endpoints, true polarity/duration, a **case-preserving** register (rendered text ≡ copied text), a readable+reachable viewport, one honest state model **with a refusal surface**.
**GREEN requires** a case-fidelity assertion on the artifact and a test that the refusal path is reachable. **RED today.**

### G-KFW11-13 — the amiga packet's roster is spent and its borrow is verified · **BORN-RED**
**GREEN requires**: every id in C-8 LANDED or KILLED-with-rationale, **and** D-2's shared-idiom borrow verified against `.d`'s **landed** SquareScene a11y cure — not against the bank's description of it. **A borrow from an uncured exemplar is the defect the row exists to prevent.**

### G-KFW11-14 — the drag seam is re-entrant-safe on both surfaces · **BORN-RED**
**KF-SCR-1 + L·D-1 as ONE cure family**: re-entrancy guard + `pointerId` latch + scope-disposal release + the "Nesting-safe" docblock **corrected**.
⟨cmd⟩ `git grep -n "pointerdown\|setPointerCapture\|pointerId" origin/master -- demo/scenes/sequence/SequenceScrubber.vue` → **one hit**: `:28 @pointerdown="onScrubDown"`. **No `pointerId` latch, no capture, no release — the seam is bare.** The counterpart at `AnimationVisualizer.vue` has `grabOffset` at `:107`/`:132`/`:224`/`:244` and the two seams disagree.
**GREEN requires** **L·D-12's born-RED mount test** (keyboard parity + direction latch + projection) passing on **both** surfaces, with KF-AV-15's cure symmetric across them. **The precedent is landed code** (`TimelineTrack.vue:480-703`, KF.W7 `7225cdd1`) — read, never copied blindly (a copied producer selector is a HIGH defect; a *read* precedent is engineering).

### G-KFW11-15 — the playhead transform is resize-immune · **BORN-RED** *(KF-AV-17's lock, inherited from KF.W7)*
⟨cmd⟩ `git grep -n "translateX" origin/master -- demo/scenes/sequence/SequenceScrubber.vue` → `:34` carries the **overshooting** `translateX(calc(<p>*100cqw))` form; `:130` and `:187` are comments describing it.
**GREEN requires** the resize-immune form `translateX(calc(var(--p) * (100cqw − 100%)))` at `:34` and the two comments re-written to describe what is there. **The comment limb is not optional** (G-KFW11-5's law applies inside this gate).

---

## §Sequencing

### Locks (binding on execution order, inside and around this wave)

- **L-1 · ALL NINE PACKETS SEQUENCE AFTER `G-KFW4-1`** (R-15, verbatim from the homing block: *"All 17 sequence after G-KFW4-1"*). Nothing in a `.vue` file is defensible until a `.vue` file can fail a build.
- **L-2 · THE DRAG-SEAM PACKET SEQUENCES AFTER KF.W7's SEQUENCESCRUBBER SURFACE VERDICT** — **KF-AV-28's STANDING SUPERSESSION RIDER**, carried (not cited) from `kf-SequenceScrubber.md:36` / `kf-AnimationVisualizer.md:35`. **The verdict is IN and it is KEEP-BESPOKE with ZERO SWAP** (OP-3), so the packet proceeds on its own cures. **`.f` STATES this before spending a cure** (KF-W7 G15's discharge discipline) and **emits zero discharge receipts** — the discharge set is empty and this wave does not add to it.
- **L-3 · LP-1's write→render ordering** — **KF-CO-1/KF-CO-8's cures are sequenced by it**; write legs are never restored without the render edge. *(The cures themselves are **OPTIONS-UNIT's = KF.W12's**; the lock travels with §6.D row 1 because the spring/sequence packets' recompile seams sit downstream of the same ordering discipline. This wave **obeys** the lock; it does not spend those rows.)*
- **L-4 · GLASS-PRODUCER ROWS → SS-6, NEVER DEMO-SIDE HACKS.** A producer defect found in any of the nine packets is **relayed**, and the demo row is marked blocked-on-relay. A demo-side workaround for a producer defect is a **HIGH defect** under the standing law.
- **L-5 · COMMENT-STATED INVARIANTS ARE TEST OBLIGATIONS** — the kf-SquareScene MISS-3 law, generalized to every packet by G-KFW11-5. This is the lock that makes the prose-truth sweeps gates rather than tidying.
- **L-6 · `SequenceScrubber.vue` IS HELD BY ONE SEAT AT A TIME** — `.b` (scene rows, `kf-SequenceScrubber.md:137`) then `.f` (seam rows, `:136`). Two seats on one file across a shared git index is how a sibling's staged work enters a commit; the phase boundary is the mechanism, and the pathspec is the belt.
- **L-7 · AMIGA'S SHARED-IDIOM BORROW LANDS AFTER SQUARE'S A11Y CURE** (`.e` after `.d`, phase 2's internal edge) — kf-AmigaScene D-2 banks SquareScene as the exemplar, and the exemplar's own operability half is a defect until `.d` lands it.
- **L-8 · ARB-1's sequencing law inside the KF-APP-1/-17 motion** — **delete arm only**, until the glass-ui M-A relay lands; the `headerLeft` **'fill' arm is a TRAP** (it lands inside an inert toolbar).
- **L-9 · ST-1's two halves land together** — the `SequenceTarget.vue` one-word edit and the `SequenceTarget.css:163-167` deletion are **one commit or neither**; KF.W9 holds the before/after witnesses and lands no cure (RULINGS R-4/R-9a).
- **L-10 · THE DECISION PRECEDES THE PATCH, three times**: N-1's canonical domain (sequence), N-1's fork (spring-plot), the field encoding (spring-physics-facet). **Each is written down before its commit family opens**; a packet that lands four patches and infers the decision afterwards has done the thing the banks each explicitly forbid (*"one spec, not four patches"*).

### Cross-edges, declared from this end

| → | subject | form |
|---|---|---|
| **KF.W12** | **CubeAxisLines rows #31/#57** (the Z-axis projective degeneracy and the axis-reveal limb) are **findings of the cube packet whose cure is AXISLINE-UNIT's**. Booked here by id, cured there. Reciprocally, **SPF-13's decision is KF-CO-47's** (OPTIONS-UNIT) and this wave waits on it rather than deciding it twice | **DECLARED, no reciprocation promised** — KF.W12 is MINTED-UNAUTHORED and this seat promises nothing from an unauthored spec |
| **KF.W13** | (a) the **spring packet is "sequenced with the transport/ribbon packet"** (kf-SpringScene's own routing words) — the sequencing is declared, the bytes are KF.W13's; (b) the **`.focus-ring` / `kf-focus-ring` drift of §B.1 row 6** lands in both waves' bounds: **2 bare sites, and one of them is this wave's `SquareScene.vue:46`** | **DECLARED**; the census figure in §6.D stays RULINGS-4's and is **not amended** (E-3) |
| **KF.W7** | **consumed, not owed**: the six KEEP-BESPOKE verdicts, the empty discharge set, the landed `grabDx` class (`7225cdd1`), the landed C-10/L-15 rulings, and **KF-AV-17's resize-immune form as a binding lock** | **INBOUND ONLY** — KF.W7 is CLOSED and this end writes nothing to it |
| **KF.W9 / SS-13** | every packet's visual residue rides to SS-13 by **apotheosis row id**, never as free-form screenshots; the shared-capture families are declared so no probe is double-spent (§5.2) | **DECLARED** |
| **SS-6 (glass BH relay)** | every producer row the nine packets surface | **MAIL, per L-4** |
| **the library (`src/**`)** | **L-2's engine-seam limb** (`updateFromString` re-supplying the receiver's renderer, or `adoptCompiled` preserving a non-default transform) and **C-3's export decision** | **DECLARED, NOT SPENT** (§Excluded E-3) |

---

## §Excluded — everything not carried, with its reason and its named owner

| excluded | owner · reason |
|---|---|
| **`CubeAxisLines.vue`'s cure** (the AXISLINE-UNIT) | **KF.W12** — §6.D row 2's cargo. The cube packet **books** #31/#57; homing them here would re-book another packet's rows |
| **The L-2 engine-seam limb** (`usesDefaultRenderer` / `adoptCompiled` semantics in `src/`) | **the library half** — this wave owns the scene's half and the *decision*; `src/` bytes are not in its bounds and a demo-side shim for an engine-seam defect is a masking fallback |
| **`PlaybackRibbon.vue` · `TransportDock.vue` · `demo/app/**` · `demo/styles/playback-idiom.css`** | **KF.W13** — dock-menu and transport/ribbon. kf-SequenceScene's N-3 `:76-78` deletion is named at §B.5 **only so no sweeping seat adopts it** |
| **Every `demo/components/instrument/**` surface** except the two named drag-seam files | **KF.W12** — CARD/OPTIONS/KFED/EDITOR/APPLY units |
| **`KF-APP-41` and the kf-EditorShell `C-22` orphan-token limb** | **ADOPTED-BY-KF.W6 (RULINGS-4 R4-1)** — spent, never re-booked by any successor packet. *(C-22's other limbs `D-27`/`RR-2 M7` stay NO-WAVE-OWNER and are **not** adopted here either.)* |
| **The KF-AV-28 rider's discharge alphabet** | **KF.W10's** — this wave emits **zero** `DISCHARGED by KF.W7 SWAP verdict <surface>, <date>` receipts, because KF.W7 emitted zero and the set is empty |
| **Re-auditing any of the 18 records** | **the registry's** — dispositions are ADJUDICATED; ids are for life; **folds are by reference at the banked id** |
| **Amending RULINGS-4's `.focus-ring` and `btn-playback` censuses** | **E-3** — dated authorities are immutable. §B.1 row 6 is a **frontier reading beside** the fixed figure, with its command |

---

## §Commit plan, artefacts, and the L-18 rider

| # | scope | body required | family lock |
|---|---|---|---|
| 1 | `feat(kf/cube): the matrix-editor family + sync-topology, ME-30/31 ≡ L-4+M-6/L-3` | yes — ME ids spent, the ME-42 edit-representation decision stated | — |
| 2 | `fix(kf/glass-conformance): the four phantom `variant:` sites, one edit` | yes — denominator 4 → 0 with the command | **CubeScene ×2 + SpringScene ×2 MUST NOT SPLIT** (C-1's rider) |
| 3 | `fix(kf/cube): the Roll stack + the lighting frame contract` | yes — both born-RED assertions | #53 + #7 = **one paired seam repair** |
| 4 | `feat(kf/sequence): the canonical-domain decision + the instrument-truth core` | yes — the decision text precedes the patches | N-1 ∥ N-2 **land against each other** (K-25); N-14 **after** N-1 |
| 5 | `fix(kf/sequence): ST-1's affordance edit + the CSS deletion` | yes — LAW A census pasted | **one commit or neither** (L-9) |
| 6 | `feat(kf/spring): the contract head + the honest-instrument core + M-4→M-3` | yes — the parser, both dead guards and the regex die together | one family |
| 7 | `feat(kf/spring-plot): the N-1 fork decided, then the draw route` | yes — the fork's decision quoted before the route | **ONE commit family** (D-1 + N-1 + N-2 + C-2/L-3) |
| 8 | `feat(kf/spring-physics-facet): the field decision + input integrity + two defineModels` | yes — the encoding decision, the published tolerance | SPF-3 rides |
| 9 | `feat(kf/spring-artifact-truth): one endpoint source, one state model, one refusal surface` | yes — case-fidelity assertion | — |
| 10 | `fix(kf/square): preserve-the-renderer + one playback authority` | yes — the L-2 decision and its declared library limb | L-1/C-3 + L-8/C-2 + C-4 + L-3 + L-5 **one authority** |
| 11 | `fix(kf/square): the colour packet + the tether in px-space` | yes — pixel witness, N-SQ-4's ordered cure | D-1 + D-6 + D-16 + N-SQ-4 = **tether-truth commit** |
| 12 | `fix(kf/square): the input/a11y packet + the comment-invariant sweep` | yes — the 46-line arithmetic re-run | D-4 + D-7 **one motion** |
| 13 | `fix(kf/amiga): the scene-repair roster` | yes — every id LANDED or KILLED-with-rationale | borrow **after** commit 12 |
| 14 | `fix(kf/drag-seam): KF-SCR-1 + L·D-1 + KF-AV-15, both surfaces` | yes — the mount test, both seams | **ONE cure family**; C·C-1 + KF-AV-16 = one decision |
| 15 | `fix(kf/drag-seam): KF-AV-17's resize-immune playhead form + its two comments` | yes — the `:34` before/after | comment limb **not optional** |
| 16 | `docs(X·KF W11 close): close record + E13 discharge + SS-13 routing` | yes — gate table, commits, **zero SWAP-discharge receipts stated positively** | — |

**Pathspec commits only**, on the commit itself; `scripts/dev/dev.sh` never staged; four tracks share this git index. **Artefacts**: `evidence/W11/**` (born-RED baselines, per-packet witnesses, the pixel and mount-test receipts), the close record at `execution/B/KF-W11.md`, the `LEDGER.md` row moved `MINTED-UNAUTHORED → planned → OPEN → IMPLEMENTED` by minimal in-place cell replacement.

**L-18 rider.** Passing all fifteen gates makes X.KF.W11 **IMPLEMENTED**. It never makes it **ACCEPTED**. Per L-18 the implementation must first survive **two challenging gestalt passes**, each a quartet of Opus 5 agents assuming it is wrong, across all three altitudes — total-tranche, wave, feature — reporting defects **and** superlatives with provenance and proof, then adjudicated into an apotheosis by a **singular fresh Fable instance**; any incongruity is addressed and dispatched, never parked (L-18 §5). Reference, not ceremony: for a scene-repair wave the quartet's most probable finds are **a decision inferred from its own patches rather than written before them** (L-10's class), **a comment left true-sounding beside a cured line** (G-KFW11-5's class), **a producer defect cured demo-side** (L-4's class), **a packet's rows spent against the register's figures rather than the frontier's** (§B.1's class), and **a "green" gate whose test was written to the cure rather than to the invariant**. Each is already a named falsifier above; the quartet exists to find the ones that are not.
