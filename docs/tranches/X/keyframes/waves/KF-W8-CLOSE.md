SERVED MODEL: claude-opus-5[1m]

# X.KF.W8 — Structure & Colocation Settle — CLOSE CERTIFICATE

**Seat**: `i` (CLOSE), Track B (X·KF, `/Users/mkbabb/Programming/keyframes.js`). **VERIFY-ONLY — this
seat authored no cure, turned no gate and opened no product file.** **Date**: 2026-09-18.
**Spec of record**: `docs/tranches/X/keyframes/waves/KF-W8.md` (476 L, read whole).
**Wave record**: `docs/tranches/X/execution/B/KF-W8.md` (1082 L at open of this seat, read whole).

---

## 0 · Substrate, and the crash-recovery sweep (STANDING LAW, before any other act)

⟨cmd⟩ `git -C ../keyframes.js status --porcelain` → **2 rows, both `??`**
(`docs/tranches/V/coordination/VALUEJS-INBOUND-2026-07-24-parser-totality-exposure.md` ·
`…-2026-07-27-library-band-r1-widened-k1-k4.md` — our own delivered letters, the same two every unit
of this wave opened on). ⟨cmd⟩ `git -C . status --porcelain` → **15 rows**, and
⟨cmd⟩ `git status --porcelain -- <this seat's four paths>` → **empty**. **Nothing inherited, nothing
stashed; `scripts/dev/dev.sh` never opened; no dirty path outside this seat's set touched.**

**Substrate at close**: kf `HEAD = e2d04331237d94ef8cac015addd4967c71def77c`, **5 commits ahead of
`origin/master 1fa98a5d`** at the moment of measurement (the wave's own five product commits; pushed
by this seat as its last act). Every figure below is read at **`e2d04331`** — the settled bytes — and
**double-run**; the spec's own readings at `origin/master 81a56990` are **seven waves stale** and are
printed beside, never struck (E-3).

⟨cmd⟩ a 16-figure harness over the gate readings, run twice: **`SAME` on all sixteen**
(`g1c1=2/2 · g2exp=0 · g3=2 · g5=2 · g6=1 · g7=7/3 · g8i=2 · g8iii=2 · g9tracked=154 · g13=1/8 ·
g14=13/11 · g15=7/2`).

---

## 1 · VERDICT — **PARTIAL**

**Four of the wave's eight implementing units executed; four never ran.**

| unit | act | state at close | evidence |
|---|---|---|---|
| **a** | re-baseline census | **DONE** | `fa2417d7` + `6971e9ca`; `KF-W8-census.md` 637 L |
| **b** | ratify KF.W8-I; nine rows; two directions | **DONE** | `ac251a38` + `e24a7cfb`; census §6, 364 L |
| **c** | deep-import extirpation | **DONE (honest-RED, 2 escalations)** | `0d456cff` · `1e2e0331` · `b0d378b0` · `cdb31643` + `aa8c8cbd` |
| **d** | one serializer body + §0o repoint | **DONE (honest-RED, 2 escalations)** | `e2d04331` + `fdebfef5` |
| **e** | barrel family ruling (G4) + R-4 structural ruling (G13) | **NOT EXECUTED** | no commit names it; no receipt in the record; `git grep -c 'KF.W8 .e'` over kf log → 0 |
| **f** | the colocation moves (G5 · G6 · G7 · G8) | **NOT EXECUTED** | `CopyButton.vue` still at `demo/components/` (G6 = 1) |
| **g** | test-lane settlement (G9 · G10 · G11 · G12) | **NOT EXECUTED** | the four `create` fixtures are absent at HEAD |
| **h** | seam/extraction (G14 · G15) | **NOT EXECUTED** | `progressFromPointerX` alive at `AnimationVisualizer.vue:121` |

**This seat does not stamp what did not happen.** The record's §Unit receipts carries receipts for
`a`…`d` and for none of `e`…`h`; the kf log carries five product commits, all tagged `.c` or `.d`;
the worktree holds no partial work of a killed `e`/`f`/`g`/`h` seat. **The honest verb is PARTIAL**,
and the four unexecuted units' gates are recorded RED-UNATTEMPTED rather than merely RED.

---

## 2 · The fifteen conditions — BEFORE → AFTER, every one re-run by this seat

Columns: the spec's dated born-RED reading at `81a56990` · the wave's own baseline at `1fa98a5d`
(record §Baseline) · **this seat's reading at `e2d04331`, double-run** · verdict.

| gate | spec @ `81a56990` | baseline @ `1fa98a5d` | **CLOSE @ `e2d04331`** | verdict |
|---|---|---|---|---|
| **G1 c1** — zero `@src/` in `demo/` | 7 stmts / 6 files | 8 / 7 | **2 / 2** — `timeline/KeyframeTimeline.vue:363` (`serializeCssValue`) · `playback/AnimationVisualizer.vue:65` (`bumpLayoutEpoch`) | **RED** — **6 of 8 cured (unit c)**; the two survivors are exactly **ESC-c1** and **ESC-c2** |
| **G1 c2** — no library-PRIVATE module reached | 4 of 4 private | 4 of 4 | **2 reached** (`compile/emit/css-text` · `resolve/browser`); `resolve/browser` → exit 1 at all four published entries; `css-text`'s one hit is **PROSE** at `public.ts:157` — recorded, not counted | **RED** — denominator halved with clause 1, as the gate's own text requires |
| **G2 c1** — consumed symbols published | 0 of 8 | 5 of 7 unpublished | **2 still consumed-and-unpublished**: `serializeCssValue` · `bumpLayoutEpoch`. `reverseCSSTime` · `serializeTimingFunction` published (`public.ts:168`, `load-engine.ts:62`/`:63`) and consumed through the door; `camelCaseToHyphen` · `convertToPixels` · `namedSelectorToFraction` **no longer library-consumed at all** | **RED** |
| **G2 c2** — narrowness (`export *` = 0) | 0 (declared GREEN-must-stay) | 0 | **0** (run 1 = run 2) | **GREEN — HELD.** No export was added to any published entry by this wave |
| **G3** — exactly one serializer body | 3 | 3 | **2** — `src/animation/compile/emit/css-text.ts:73 export const serializeSelector` · `demo/utils/keyframeSelector.ts:28 export const selectorText` | **RED (owed 1)** — the **`src/` half is ONE body** (unit d); the demo copy is **ESC-d1** |
| **G3 falsifier** | — | — | armed and clean: `git grep -n 'selectorText' HEAD -- src` → **1 hit, a comment at `format.ts:3`**; both library callers call the one body | **HOLDS** |
| **G4** — barrel family, four clauses | all four RED, 4 barrels | c1 + c4 GREEN-BEFORE-CURE; c2 + c3 RED | **unchanged**: `transport/index.ts` ABSENT ✅ · `instrument/index.ts` ABSENT ✅ · `keyframes/index.ts` present, importers exit 1 ❌ · `timeline/index.ts` present, importers exit 1 ❌ | **RED — UNATTEMPTED (unit e)**. The conjunction fails on clauses 2 and 3 |
| **G5** — no parent-directory sibling imports | 3 | 2 | **2** — `KeyframeCardList.vue:46` → `../KeyframeCard.vue` · `TimelineTrack.vue:296` → `../TimelineCaret.vue` | **RED — UNATTEMPTED (unit f)**. The third member died with `KfPillTabs` in KF.W6's S-1 family |
| **G6** — zero loose `.vue` at `demo/components/` (**ACTING**) | 1 | 1 | **1** — `demo/components/CopyButton.vue` | **RED — UNATTEMPTED (unit f)**. This wave's own act, unspent |
| **G7** — shell barrel symmetry | (i) 8 · (ii) 4 · (iii) FALSE for `EditorHeader` · (iv) RED | (i) 7 · (ii) 3 · (iii) HOLDS · (iv) DIVERGENT | **(i) 7 SFCs · (ii) 3 exports · (iii) HOLDS — every export has ≥1 importer** (`EditorShell` barrel `App.vue:147` + direct `App.skeleton.vue:84` · `EditorStartScreen` barrel `App.vue:147` · `SharePopover` barrel `MbabbMenu.vue:189` + direct `EditorShell.vue:227`) · **(iv) RED**: `HeroAurora` (dynamic `App.vue:164` + `aurora-opacity-ceiling.test.ts:62`) and `TypingDots` (`typing-dots-engine-seam.test.ts:39`) are **externally consumed and not exported** | **RED via leg (iv) — UNATTEMPTED (unit f)**; legs (i)–(iii) HOLD. KF.W6's R4-1 half of the dependency **landed**; this wave's `HeroAurora` move did not |
| **G8 leg (i)** — `editor-shell/` citations | 2 | 2 | **2** — `demo/app/App.vue:148` · `shell/EditorStartScreen.vue:235` | **RED — UNATTEMPTED (unit f)** |
| **G8 leg (ii)** — false value.js attribution | AV 4 + test 6 | AV 4 (`:90`/`:92`/`:96`/`:97`), test arm CURED | **AV 4, unchanged**; `resize-tracks.test.ts:8` still carries KF.W4's correction | **RED (AV arm) — UNATTEMPTED (unit f)** · **GREEN-BEFORE-CURE (test arm, KF.W4)** |
| **G8 leg (iii)** — barrel chunk prose | 4 files | 2 files | **2 files** — `keyframes/index.ts` · `timeline/index.ts` | **RED — UNATTEMPTED (unit e/f)**; 2 discharged with their barrels by predecessors |
| **G9** — every `*.test.ts` in exactly one project | 132 tracked / 2 orphans | 154 tracked / 152 collected / 2 orphans | **tracked 154** (`test` 152 + `bench` 2) · ⟨cmd⟩ `npx vitest list --filesOnly` → **152** · **orphans = the same 2** `bench/d3-changed-keys.measure.test.ts` · `bench/sync-step.measure.test.ts` | **RED — UNATTEMPTED (unit g)**. R-5's `measure` project unwritten |
| **G10** — the `demo` project can load an SFC | both legs RED | (a) GREEN (W4 `vitest.config.ts:16`) · (b) GREEN (`@vue/test-utils ^2.5.1`, W7's idempotent add) · probe ABSENT | **legs (a)+(b) GREEN-BEFORE-CURE, unchanged; the probe `test/demo/instrument/sfc-load.probe.test.ts` is ABSENT** (exit 1) | **RED on this wave's own act — UNATTEMPTED (unit g)** |
| **G11** — `groupShortcuts()` beside the SFC + covered | inline `:57-67`, 0 cover | inline `:308-318`, 0 cover | ⟨cmd⟩ `git grep -l groupShortcuts HEAD -- demo test` → **exit 1**; inline body alive at `KeyboardShortcutsModal.vue:308`, template consumer `:139` | **RED — UNATTEMPTED (unit g)** |
| **G12** — characterization pair | 0 | 0 | neither `CSSPasteDialog.test.ts` nor `KeyframesAddDialog.test.ts` exists | **RED — UNATTEMPTED (unit g)** |
| **G13** — the `src/` chain is RULED | 1 child · 8 stutters · no ruling | identical | **`git ls-tree --name-only HEAD -- src/` → one entry, `src/animation`**; `basename == dirname` stutters → **8**; **no committed ruling exists in the tree** | **RED — UNATTEMPTED (unit e)**. R-4's decline is written in the *spec*; G13 demands it **in the tree** |
| **G14** — hand-rolled projectors outside `useDragScrub` | 11 lines / 10 files | 13 / 11 | **13 lines / 11 files** (identical set); `useDragScrub.ts` → **exit 1, zero** (falsifier clean); the in-bounds projector **`AnimationVisualizer.vue:121 progressFromPointerX`** alive with call sites **`:230`/`:235`** | **RED — UNATTEMPTED (unit h)**. Unit a's binding enumeration is **SUPPLIED** (census §3), so the gate has a reachable scoped green for the first time — it was simply never spent |
| **G15 leg 1** — coupling | 7 convicted lines | 7 | **7** — `RibbonBar.vue` `:8`·`:13`·`:55`·`:65`·`:100`·`:105`·`:138` | **RED — CORRECTLY UNSPENT.** The gate's own falsifier: *"a G15 recorded green on a KF.W8 commit is itself the defect"*; this wave contributes spec input and **zero bytes**, and zero bytes landed |
| **G15 leg 2** — the pad allowance | 2 permitted | 2 | **exactly 2** — `:5` the Teleport comment · `:7` `id="controls-ribbon-target"`; no `<Teleport>` element, no binding | **HOLDS** |

### The one gate outside §Gates that this close is ordered to re-measure

**`G-W2-2` (KF.W2's Tier-A single-entry, `src/` arm) — COHESION §0o `ESC-KFW2-1`: *"the one-import
repoint … is performed by KF.W8's unit d … and G-W2-2 is re-measured at KF.W8's close."***

Re-run by **this** seat, the gate's own multi-line-tolerant witness (ii) over
`git grep -l 'from "@mkbabb/value.js/css"' HEAD -- src/`:

```
src/animation/compile/emit/css-text.ts      :: serializeCssColor        ← emit-half, Tier-D, out of denominator
src/animation/compile/frame/interp-slot.ts  :: serializeCssColor        ← emit-half, Tier-D, out of denominator
src/animation/compile/parse-facade.ts       :: 15 grammar/collector symbols
src/animation/resolve/function.ts           :: coerceToSyntax           ← the gate's DECLARED stray
```

**Parse-surface symbols at exactly ONE path — `compile/parse-facade.ts`. G-W2-2 is GREEN on the
`src/` arm**, and it is the **only gate this wave turned**. Falsifier armed and clean: the removed
importer's call sites are **routed, not orphaned** — `metadata.ts:28` imports from
`../../compile/parse-facade`, consumed at **`:47`** and **`:107`**. Leg (i) is **25 lines / 22
files**, non-decisive by the gate's own words.

### Tally

**GREEN at close: 2 whole conditions + 7 legs/clauses.** G2 c2 (held) · **G-W2-2 (turned here)**;
legs: G4 c1 · G4 c4 · G7 (i)(ii)(iii) · G8 (ii)'s test arm · G10 (a) · G10 (b) · G15 leg 2 · the G3
and G-W2-2 falsifiers. **RED at close: 13 of the 15.** Of those thirteen, **two moved substantially
without turning** (G1 c1 8→2, G3 3→2), **one is correctly unspent by its own falsifier** (G15), and
**nine are RED-UNATTEMPTED** because units e, f, g, h never ran.

**No gate was recorded green that this seat did not itself re-run, and no GREEN-BEFORE-CURE leg is
claimed as this wave's work** — each is attributable to KF.W4, KF.W5, KF.W6 or KF.W7 by name.

---

## 3 · Commit roster — every commit verified to exist and to touch only its unit's writable set

⟨cmd⟩ `git show --stat <hash>` on each, at both repos.

| # | repo | hash | unit | files | inside writable set? |
|---|---|---|---|---|---|
| 1 | value.js | `fa2417d7` | a | `docs/tranches/X/keyframes/waves/KF-W8-census.md` (+637) | ✅ |
| 2 | value.js | `6971e9ca` | a | `docs/tranches/X/execution/B/KF-W8.md` (+162) | ✅ |
| 3 | value.js | `ac251a38` | b | `…/KF-W8-census.md` (+364) | ✅ |
| 4 | value.js | `e24a7cfb` | b | `…/execution/B/KF-W8.md` (+217) | ✅ |
| 5 | kf | `0d456cff` | c | `useKeyframeOps.ts` · `parseAnimationCSS.ts` (2 f, 3+/4−) | ✅ §Bounds deep-import row |
| 6 | kf | `1e2e0331` | c | `demo/utils/helpers.ts` (1 f, 23+/3−) | ✅ the concentrator row |
| 7 | kf | `b0d378b0` | c | `timelineEngine.ts` · `demo/utils/helpers.ts` (2 f, 44+/3−) | ✅ |
| 8 | kf | `cdb31643` | c | `demo/utils/keyframeSelector.ts` (1 f, 27+/5−) | ✅ copy-3 row |
| 9 | value.js | `aa8c8cbd` | c | `…/execution/B/KF-W8.md` (+210) | ✅ |
| 10 | kf | `e2d04331` | d | `emit/css-text.ts` · `emit/format/format.ts` · `emit/index.ts` · `engine/css/metadata.ts` (4 f, 46+/22−) | ✅ three §Bounds rows + **§0o ESC-KFW2-1's granted addendum-beside** |
| 11 | value.js | `fdebfef5` | d | `…/execution/B/KF-W8.md` (+225) | ✅ |

**Not one commit touched a path outside its unit's grant.** ⟨cmd⟩ `git show --stat` over all eleven:
**zero** hits for `scripts/dev/dev.sh`, `scripts/gates/**`, `.dependency-cruiser.cjs`,
`src/animation/public.ts`, `src/animation/load-engine.ts`, `demo/components/instrument/shell/index.ts`
or any sibling seat's staged rows. **No `git add -A`, no `-u`, no `commit -a`, no reset, no stash, no
force** is recorded at any seat, and the two contaminated-commit classes X-W0 measured did not recur.

**Family locks, checked at the tree rather than at the receipts**: unit d's *serializer unification +
`metadata.ts` repoint = ONE commit* is **honoured whole** (`e2d04331` carries all four files). Unit
c's *KF-AV-8 three-site extirpation = ONE commit* is **honoured for the arm that could land**
(`1e2e0331`), with the AV third site escalated (**ESC-c2**) and named in that commit's own message —
a **declared, legible split of a bundle broken upstream**, not a silent one. `demo/utils/helpers.ts`
appears in two commits (`1e2e0331` · `b0d378b0`) under **two different ids** (KF-AV-8 · KF-CE-12);
that is two meanings, two commits — the rule, not a breach of it.

---

## 4 · Landed-wrong — **NONE**

This seat looked for: writes outside §Bounds; split commit-families; a gate bought with an
out-of-bounds edit; a masking cure (try/catch around a defect, `test.skip`, an allowlist, a copied
producer selector, a `node_modules` patch); a green asserted where the bytes are red. **It found
none.** The two places a seat could have bought a numeral — `KeyframeTimeline.vue:363` (G1 c1's
eighth statement, in no §Bounds row) and a demo-side forward of `serializeSelector` (G1 c2's own
named falsifier) — were **escalated, not taken**, and the tree shows the restraint: ⟨cmd⟩
`git grep -n 'export .* from "@src/' HEAD -- demo` → **exit 1**.

---

## 5 · Residuals and escalations, each with its named owner

**Escalations raised in-wave: 4** (units c and d; a and b raised none).

| id | subject | owner named |
|---|---|---|
| **ESC-c1** | `demo/components/instrument/timeline/KeyframeTimeline.vue:363` — G1 c1's 8th `@src/` statement has **no §Bounds row** (the file post-dates the spec's ref; named five ways in the spec, bounded nowhere) | a §Bounds widening by **dated addendum-beside (E-3)**, or `KF-W10 §6.D` (anchor-only) |
| **ESC-c2** | `AnimationVisualizer.vue:65` `bumpLayoutEpoch` — a **cure gap, not a bounds gap**: publication is KF.W5's and W5 closed without an epoch surface; relocation is impossible at the bytes (the symbol mutates library-private `browserScalarCache` state); deleting the wire is unit h's grant | **KF.W5 addendum-beside** (the epoch surface) taken **with** unit h's debounce/scoping half — one surface decision owns both |
| **ESC-d1** | G3's third body cannot retire: the **package-surface** half of MISS-β2's publication is KF.W5's file (`public.ts` + `load-engine.ts`, both READ-ONLY here). Links 1–2 landed (`css-text.ts` exports, `emit/index.ts` surfaces); links 3–4 cannot | **KF.W5 addendum-beside** publishing `serializeSelector` (one named line each), or `KF-W10 §6.D` |
| **ESC-d2** | A-9's repoint set is **TWELVE**, five members carry no §Bounds row (all KF.W7 creates/edits post-dating `81a56990`) — so even a granted publication cannot execute in-bounds | a §Bounds widening by dated addendum-beside enumerating the five, or `KF-W10 §6.D`. **ORDERED after ESC-d1** |

**Residuals carried forward (17, deduplicated across the four receipts):**

1. **Units e, f, g, h are unexecuted** — nine gates RED-UNATTEMPTED. **Owner: the orchestrator / a
   resumption of this wave.** Their §Bounds grants, their family locks and their unit plans stand
   unspent and unchanged; nothing about them was consumed here.
2. **The §Bounds coordinate column is stale wave-wide** (+20 in `AnimationVisualizer.vue`, larger
   elsewhere; the 22-row re-anchoring table is **census §5**). **No seat may execute from the spec's
   numerals** — every grant resolves **by subject**. Owner: any successor seat.
3. **A-9's two in-bounds coordinate drifts** — `useTimelineBuild.ts:7` where the minted row says
   `:8`; `timelineEngine.ts:15`→ now `:13-16` where the carve says `:16`. Owner: unit d on resumption.
4. **A-7's second consumer** `test/demo/instrument/timeline-mount-keyboard.test.ts:28` — unbounded; a
   move+rename repoints **two** files. Owner: unit f.
5. **A-8 / `scripts/gates/census.mjs:528`** holds `demo/components/instrument/shell/HeroAurora.vue`'s
   **literal path** inside `EMBEDDED_SITES`. `scripts/gates/**` is **DO-NOT-TOUCH (KF.W4)**, so unit
   f's booked `git mv` breaks a gate script this wave may not repair. **ESCALATION-SHAPED, unraised
   because the act never ran.** Owner: KF.W4 by addendum-beside, before unit f moves the file.
6. **G7's `TypingDots` predicate conflict** (record F-2): under leg (iv)'s own definition of
   *external*, a KF.W4 test makes `TypingDots` externally consumed — inverting the spec's verdict —
   and the falsifier forbids curing it by adding an export. Owner: unit f, with G7.
7. **F-3 / `HeroAurora` has zero STATIC importers**: its two consumers are both dynamic, which the
   leg's `from "…/N.vue"` command shape does not match. A seat must state which reading it used
   before moving the file. Owner: unit f.
8. **Row 8 of unit b's re-derivation has no landing grant** — §Bounds gives unit h a `modify-carve`
   and **no `create`** for the extracted composable, where R-10's twin is granted explicitly. Owner:
   unit h's escalation; the direction is ruled, the grant is not.
9. **Rider (iv) of unit b is ARMED and ungranted** — direction ruled, act needs a §Bounds widening
   this wave does not hold. Owner: `KF-W10 §6.D` (anchor-only).
10. **The demo holds two `CssValue` serializers** until ESC-c1 is granted — its own at
    `demo/utils/helpers.ts` and the library-private one reached at `KeyframeTimeline.vue:363`. **One
    specifier line closes it.**
11. **`demo/utils/keyframeSelector.ts` mirrors `internal/scroll-phases.ts`'s four phase spans.** Both
    sides carry the symbol name `PHASE_FRACTIONS`, so the pair is one `git grep` apart; the true cure
    is a published door onto the phase mapping — **KF.W5's class**.
12. **`emit/index.ts`'s new barrel line publishes nothing onward yet** — the sub-zone half of the
    publication act, structurally clean (R6 PASS), naming a symbol no consumer outside `src/` can
    reach until ESC-d1 lands. **Named in its own comment as exactly that.**
13. **`resolve/function.ts`'s `coerceToSyntax`** is still the one stray runtime grammar import —
    KF.W2's own *"delete-or-declare at open"*, outside G-W2-2's parse-surface denominator by that
    gate's own reading, and outside every grant here. **Re-measured at this close and unsurprising.**
14. **`useKeyframeOps.ts`'s pre-existing TS2322 at `(80,13)`** — untouched, unrelated, one of the 54
    the tree already carried.
15. **G13's ruling is written in the spec and not in the tree** — R-4 declines both structural
    instances together, and the gate demands *a committed ruling*. Owner: unit e.
16. **The two `bench/*.measure.test.ts` orphans** are collected by no project; R-5's `measure` project
    is specified and unwritten. Owner: unit g.
17. **G15 is hard-bundled with `kf-ChannelControls` L-2/C-2** and cannot go green here by
    construction. **Correctly unspent.** Owner: the structural settlement that owns L-2/C-2.

---

## 6 · Tree-health floors, re-run by this seat at the settled bytes

| harness | baseline | **close** | verdict |
|---|---|---|---|
| `npx vitest run` | 1518 passed / 3 expected fail / 14 skipped · 147 files passed / 5 skipped (152) | **identical** | **UNMOVED** |
| `npx vue-tsc --noEmit -p tsconfig.json` | 54 errors | **54** | **UNMOVED** |
| `node scripts/gates/structure/index.mjs` | PASS | **PASS — 0 violations across R1–R6** | **UNMOVED** |
| `npx vitest list --filesOnly` | 152 collected | **152** | **UNMOVED** |

The wave's five product commits cost the tree **nothing**: no new error, no new failure, no new
structure violation, and `demo/` behaviour is preserved by construction at every repoint (unit c's
re-homes are bodies, never forwards; unit d's unification is proved equivalent by the emit
round-trip's own suite).

---

## 7 · E13 — the four-path close sweep

Swept read-only at this seat's clock (2026-09-18, after unit d's 20:41 sweep), classification taken
from each row's **Status cell**, never from a bare `grep -i unread`; `INBOX.md` **self-excluded**.

1. `docs/tranches/V/coordination/` → **19** entries; newest non-self
   `valuejs-outbound-2026-09-18-kfw7-bh-relay-ADDENDUM-A9.md` **19:00** = **O-31**, rowed.
2. `../glass-ui/docs/tranches/` → **`BK/`** newest (Sep 18 17:53); `BK/coordination/` → **9**
   entries, newest `glass-outbound-2026-09-18-valuejs-o26-reply.md` **17:18** = **I-35**.
3. `../keyframes.js/docs/tranches/V/coordination/` → newest **Sep 17 19:08**, unmoved.
4. `../sci-report/atlas/docs/tranches/P/coordination/` → newest **Aug 3 15:01**, unmoved.

**I-35 is the only row routed to X·KF (Track B)**, and its Status cell reads **READ + CONSUMED WHOLE
2026-09-18 17:1x EDT (KF.W6 unit `.l`)** — its `§4(2)` answered, its `§4(1)` (registry re-install)
**escalated to seat 0 / KF.W0**, which is where it stays; **nothing in it is KF.W8's**. The three
live `UNREAD` cells (**I-32 · I-33 · I-34**) each route at their own Routing cell to **X-W0.j / the X
formation mail seat / X-EXT-1**, and **not one names a keyframes byte**.

**Result: 0 unrowed · 0 UNREAD in this wave's scope. The wave does not close with UNREAD mail.**
This wave's outbound obligation is **NEGATIVE** by its own §Sequencing; **zero glass-ui bytes** were
written by any seat, and no letter was sent.

---

## 8 · The four-verb line

The spec's §State table reads `AUDITED YES · SPECIFIED YES · IMPLEMENTED NO · VERIFIED NO`, and
**names no seat to stamp VERIFIED**. This seat therefore moves **nothing to YES**:

| verb | state at close | why |
|---|---|---|
| AUDITED | **YES** (unmoved) | the 58-record corpus, per §State |
| SPECIFIED | **YES** (unmoved) | by the spec file |
| IMPLEMENTED | **PARTIAL — units a·b·c·d only** | four of eight units executed; nine gates RED-UNATTEMPTED. A `YES` here would assert acts that did not happen |
| VERIFIED | **NO** | the spec designates no seat to stamp it, and a PARTIAL implementation cannot carry it |

**The spec declares no `§Verification Artefacts` section** — ⟨cmd⟩ `grep -c 'Verification'
KF-W8.md` → **0**. What stands in its place is §Gates' fifteen literal commands and G13's
*"the ruling is in the tree"* clause, all re-run above; this seat invents no artefact the spec did
not ask for.

---

## 9 · SELF-COUNT, read back from the settled bytes

**Counting rules stated at the receipt.** Gates enumerated: **15**, `G1…G15`, no gap — plus the one
§0o-ordered outside gate (`G-W2-2`), counted separately and never folded into the fifteen. Commits
verified: **11** (5 kf product · 6 value.js doc), each `git show --stat`-ed individually. Units with
receipts: **4** of 8. Escalations: **4** (`ESC-c1` · `ESC-c2` · `ESC-d1` · `ESC-d2`) — ⟨cmd⟩
`grep -c '^| \*\*ESC-' KF-W8-CLOSE.md` → **4**. Residuals: **17**, numbered 1…17, no gap, and the
**counting rule is stated**: this is the *deduplicated union* of the four unit receipts' 21 items
(a 5 · b 6 · c 5 · d 5) **plus 5 raised first here** (units e–h unexecuted · F-3's dynamic-importer
reading · G13's ruling absent from the tree · the two `bench` orphans · G15's bundling), **minus 9
that are the same fact twice** (b1≡a3 · b2≡a4 · c3⊂a5 · d4⊂a5 · b6⊂G15 · a1⊂ESC-c1 · d2⊂ESC-d1 ·
**c5 and d5**, the two *"nothing was pushed"* items, which this seat **spends** rather than carries).
21 + 5 − 9 = **17**. Gate figures: **16-figure harness, run twice, `SAME` on all
sixteen**. Tree-health harnesses: **4**, each run at `e2d04331`. Every figure in this certificate was
produced by a command at the settled bytes and re-run; **none is inherited from a unit receipt** —
where a receipt and this seat agree, both ran the command.

**This certificate, read back from its own settled bytes**: ⟨cmd⟩ `wc -l KF-W8-CLOSE.md` → **304** ·
`head -1` → `SERVED MODEL: claude-opus-5[1m]` · ⟨cmd⟩ `grep -c '^| \*\*G' ` → **21** gate rows
(15 gates, four of them split into their legs/clauses, + `G-W2-2` and the two falsifier rows) ·
residual list numbering `1…17` with no gap, verified by ⟨cmd⟩ `grep -oE '^[0-9]+\. '`.

---

## ADDENDUM-BESIDE — 2026-09-18, REPAIR ROUND 1 (E-3: this certificate's bytes above are NOT edited)

**SERVED MODEL**: `claude-opus-5[1m]`. Raised by **CHECK 1 · D-2 (MEDIUM)**; cured here as a dated
addendum, never as a patch of the figures above. The prior artefact stands as the record of what that
seat printed; this addendum states the counting rule and the figures that reproduce under it.

**COUNTING RULE, stated AT the receipt: one unit = one GATE ID, never a leg or a clause** — G1's two
clauses are one gate, G7's four legs are one gate, G15's two legs are one gate.

1. **RED-UNATTEMPTED at close is ELEVEN, not nine.** This certificate printed *"nine are
   RED-UNATTEMPTED"* at `:111`, `:177` and `:274`, while its own gate table marked **eleven** —
   **G4 · G5 · G6 · G7 · G8 · G9 · G10 · G11 · G12 · G13 · G14** — and the LEDGER row printed the
   numeral `9` with those same eleven ids enumerated beside it in one sentence. Under the rule above
   the enumeration yields **11**. **No counting rule yields 9 over that set**, so the figure is
   restated rather than rescued: **11**.
2. **RED at close is FOURTEEN of fifteen, not thirteen.** Every one of the fifteen carried a RED
   member at close. **14/15** under the rule *a gate counts RED at close iff it carried a RED member
   this wave could in principle have spent* — which excludes **G15** alone, whose own text forbids a
   KF.W8 byte (*"a G15 recorded green on a KF.W8 commit is itself the defect"*). Counting G15's
   correctly-unspent leg 1 gives **15**. **13 reproduces under no stated rule.**

**Nothing was laundered by the original print**: every RED was individually recorded, marked
UNATTEMPTED and owner-attributed, and the enumeration was complete and correct. The defect was a
figure reprinted beside an enumeration that does not support it — the class the programme convicts by
name — and the cure is to make the rule ride the figure.

**SUPERSEDED BY EVENTS, recorded here so this certificate is not read as current.** Repair round 1
(2026-09-18) resumed the wave at units `e` → `f` → `g`; **eight of the eleven turned GREEN**
(**G4 · G6 · G8 · G9 · G10 · G11 · G12 · G13**) and the remaining three (**G5 · G7 · G14**) now carry
measured escalations with named owners (ESC-R1-1 … ESC-R1-7). **RED-UNATTEMPTED is now 0.** Unit `h`
spent zero bytes and is wholly escalated, so the row **stays `PARTIAL`**. The live record is
`docs/tranches/X/execution/B/KF-W8.md` **§Repair 1**.
