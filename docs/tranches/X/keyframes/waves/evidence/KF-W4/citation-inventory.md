SERVED MODEL: claude-opus-5[1m]

# KF.W4 `.d` — the citation inventory (G-KFW4-8's enumerated-site artefact)

**Dated** 2026-09-17 · **Substrate** `/Users/mkbabb/Programming/keyframes.js`, branch `master`.
Every figure below is read from the settled bytes and double-run. This file is also the
`--sites` roster `scripts/gates/census.mjs --clause citations` reads: the eight paths are
enumerated as `- \`path\`` list items in §1 and the script re-reads and re-hashes each one
at use (never allowlisted — rule (b)/(c), B10-27).

## 0 · THE DECLARED CARVE, stated in the run's artefact (R2-13)

**`proof:brittleness` at `demo/styles/layout.css:152` is OUT of G-KFW4-8's denominator BY
NAME.** `layout.css` is an enumerated citation target and a §Bounds file, so the family
leaked into the gate's own in-bounds set at round 1; **§Excluded 17 owns it WHOLE at
KF.W6** (3 sites / 1 name: `demo/styles/style.css:38` · `demo/styles/layout.css:152` ·
`scripts/lib/demo-driver.mjs:69`). **This wave wrote NO brittleness byte.** The clause
prints the carve on every run and FAILS if the carve's subject has been cured here —
curing it inside this wave is the scope-creep the carve exists to forbid.

⟨cmd⟩ `node scripts/gates/census.mjs --clause citations` → *"DECLARED CARVE:
`proof:brittleness` at demo/styles/layout.css is OUT of this clause's denominator BY NAME;
routed whole to KF.W6 (X.KF.W4 §Excluded 17). Subject present: 1 citation(s)."*

**No residue family was pulled in.** §Excluded 16–21's six families each keep their named
destination; this unit touched none of their sites.

## 1 · The EIGHT enumerated sites (the roster, machine-read by `--sites`)

- `demo/DESIGN.md`
- `demo/components/instrument/shell/EditorStartScreen.vue`
- `demo/app/App.vue`
- `demo/components/instrument/shell/HeroAurora.vue`
- `demo/components/instrument/shell/TypingDots.vue`
- `demo/styles/layout.css`
- `demo/styles/design-idioms.css`
- `demo/scenes/spring/SpringPhysicsFacet.vue`

`EditorShell.vue` is **STRUCK** and stays struck — 0 `proof:` bytes at either coordinate
(D-2). Adding a ninth site is a §Bounds amendment, not a gate edit.

## 2 · The arithmetic, BEFORE → AFTER, at the bytes

| figure | BEFORE (`origin/master` + `.a`/`.b`) | AFTER (this unit) |
|---|--:|--:|
| citation LINES at the eight | **33** | **4** |
| `proof:` TOKENS at the eight | **38** | **5** |
| distinct NAMES | **19** | **3** |
| dead citations (C2's own count) | **34** | **0** |

⟨cmd⟩ BEFORE `git grep -n 'proof:' origin/master -- <the eight>` → **33** lines; the same
piped through `grep -oh 'proof:[a-z0-9-]*' | sort -u | wc -l` → **19** names — reproducing
§Bounds' denominator byte-for-byte at this seat's clock.
⟨cmd⟩ AFTER `grep -n 'proof:' <the eight>` → **4** lines, printed whole:

```
demo/DESIGN.md:236:| **R2** | … | `proof:publish` reachability clause (OD-U11 folded chunk reachability); U.D. |
demo/DESIGN.md:244:inside `proof:publish`; U.G does not create a standalone chunk gate. All other
demo/DESIGN.md:251:this codex: `proof:owner-golden` → §§3, 7, 8; `proof:publish` → §10 R2. This is
demo/styles/layout.css:152:   dock bands, NOT a :has() test on a generic .z-dock utility class (proof:brittleness
```

## 3 · The R-7 partition, discharged name by name

**17 cure · 1 routed · 1 live** — total, and total at the bytes.

| # | name | sites | disposition |
|---|---|---|---|
| 1 | `appearance-suffusion` | `DESIGN.md:251` · `layout.css:31` | **DELETED** (R-4 RULED: retire the citations, do not build the gate) |
| 2 | `hero-two-focal` | `EditorStartScreen.vue:15` | **DELETED**; the OWNER ruling it named is kept as prose, its false enforcement claim is not |
| 3 | `cursor-light-subtle` | `App.vue:49` · `HeroAurora:18` · `HeroAurora:44` | **DELETED ×3**, and **REPLACED BY A REAL ORACLE**: `test/demo/instrument/aurora-opacity-ceiling.test.ts` now asserts the OD-2 bound and its template binding |
| 4 | `no-hand-rolled-cursor-tracker` | `HeroAurora:5` | **DELETED** |
| 5 | `typing-dots` | `TypingDots :45` `:51` `:59` | **DELETED ×3**, and **REPLACED BY A REAL ORACLE**: `test/demo/instrument/typing-dots-engine-seam.test.ts` (R-5's single seam spec) |
| 6 | `design-refinement` | `EditorStartScreen.vue:56` | **DELETED** |
| 7 | `hero-deck-voice` | `EditorStartScreen.vue:138` (spec'd `:134`; **DRIFT +4 from `.a`'s type-surface commit `5388907b`, INTENT taken at the true bytes**) | **DELETED** |
| 8 | `styling-idioms` | `EditorStartScreen.vue:163` (spec'd `:159`, same drift) · `DESIGN.md:237` · `DESIGN.md:250` | **DELETED ×3** |
| 9 | `font-census` | `DESIGN.md:29` · `DESIGN.md:250` · `EditorStartScreen.vue:184` (spec'd `:180`, same drift) | **REPOINTED at `:29`** to `node scripts/gates/register-census.mjs` — the gate it always claimed (R-7's named repoint); **DELETED** at the other two |
| 10 | `idioms` | `design-idioms.css:3` · `design-idioms.css:43` · `layout.css:10` | **DELETED ×3** (the round-3 D-6 correction's three sites, all found) |
| 11 | `easing-sidebar-normalized` | `SpringPhysicsFacet.vue:118` | **DELETED** |
| 12 | `app-is-shell` | `App.vue:322` | **DELETED** |
| 13 | `icon-paint-live` | `App.vue:117` | **DELETED** |
| 14 | `colocation` | `DESIGN.md:224` · `:239` · `:240` · `:252` | **DELETED ×4** (three inside the §10 table — the declared bounded act, §4) |
| 15 | `crayon-preserved` | `DESIGN.md:251` | **DELETED** |
| 16 | `style-file-ceiling` | `DESIGN.md:224` · `:236` · `:252` | **DELETED ×3** (one inside the §10 table) |
| 17 | `zone-cohesion` | `DESIGN.md:238` · `:253` | **DELETED ×2** (one inside the §10 table) |
| — | `brittleness` | `layout.css:152` | **ROUTED — the DECLARED CARVE**, §0 above. Untouched. |
| — | `publish` | `DESIGN.md:235`→`:236` · `:243`→`:244` · `:253`→`:251` | **LIVE, KEPT**: `package.json:52` `"proof:publish": "node scripts/gates/surface/index.mjs"` |

**One name ENTERED the denominator, by repoint, and it resolves**: `DESIGN.md:251`'s
cross-reference block carried a bare *"owner-golden"* beside the dead
`proof:appearance-suffusion`. The dead limb is deleted and the live one is written at its
real name — `proof:owner-golden` (`package.json:53` → `scripts/gates/visual/index.mjs`).
That is R-7's *"repoint before delete where a real gate exists"* applied a second time,
and it is why the AFTER name count is **3** rather than 2.

## 4 · The DECLARED SEPARATE BOUNDED ACT — `DESIGN.md`'s §10 rules table

§Bounds declares the §10 rules table its own bounded act **with its own diff review**,
never a token deletion: seven rows whose entire **Proof** column is citations. Five rows
carried dead names. The act, reviewed row by row:

| row | Proof cell BEFORE | Proof cell AFTER | why |
|---|---|---|---|
| R1 | dependency-cruiser clauses; U.B9/U.C | **untouched** | names REAL depcruise rules, carries no `proof:` token — outside the denominator |
| R2 | `proof:publish` reachability clause; U.D | **untouched** | the one LIVE citation in the table |
| R3 | `proof:style-file-ceiling`; U.B12 | `NO standing gate; U.B12 owns the rule.` | the cited gate does not exist |
| R4 | Clause on `proof:styling-idioms`; U.B12 | `NO standing gate; U.B12 owns the rule.` | idem |
| R5 | Mirror clause on `proof:zone-cohesion`; U.H/U.B | `NO standing gate; U.H/U.B own the rule.` | idem |
| R6 | Existing `proof:colocation` clause; U.B1 | `NO standing gate; U.B1 owns the rule.` | idem |
| R7 | `proof:colocation` no-single-child clause; U.B1 | `NO standing gate (the no-single-child clause is unenforced); U.B1.` | idem |

**The rule text of every row is byte-unchanged.** Only the column that claimed enforcement
moved, and it moved to the truth: the rulings are standing law with no gate behind them.
**A routed residual is named here rather than smoothed** (§6, R-1).

## 5 · The second declared act — the `Cross-reference witness` block (`:248-253`)

Four lines of pure citation mapping. Cured per-clause, ≤1 clause per name, live names
KEPT: the block now reads *"…require a section anchor to this codex: `proof:owner-golden`
→ §§3, 7, 8; `proof:publish` → §10 R2. This is a close-out witness, not a new standing
gate."* Five dead mappings (`styling-idioms`, `font-census`, `crayon-preserved`,
`appearance-suffusion`, `colocation`+`style-file-ceiling`, `zone-cohesion`) are gone.

## 6 · Routed residuals — named, not smoothed

1. **`DESIGN.md:245-247`, the §10 tail prose** — *"All other enforcement is clause-shaped
   on surviving gates, so the net gate count remains flat"* is FALSE now that R3–R7 name
   no gate. It carries **no `proof:` token**, so it is outside G-KFW4-8's denominator, and
   §Bounds books the surrounding rationale prose as **KF.W6's** (*"the counterfactual
   rationale blocks around them ride KF.W6's file-touch"*). **ROUTED → KF.W6**, stated
   here so the next seat does not have to find it.
2. **`demo/styles/font-roles.json:2`'s `_doc`** still names `proof:font-census` as the
   artefact's reader. `font-roles.json` is **not one of the eight targets** — it is out of
   C2's denominator by R-7's own partition, and the §Bounds row names four anchors
   (`:16-26`, `:34-38`, `:68`, `:82`), none of them `:2`. **ROUTED → KF.W6**, with the
   note that the real reader is now `scripts/gates/register-census.mjs`.
3. **The wider tree is NOT this gate's**: `grep 'proof:' demo/ src/ scripts/` finds a far
   larger surface, routed at §Excluded 16 to **KF.W0's G-0.7 roster**. This unit asserts
   nothing over it.
