SERVED MODEL: claude-opus-5[1m]

# K3 — the `noUnusedLocals` site set, partitioned by BOUNDS

**Dated addendum-beside (E-3).** No byte of `KF-W4.md`, the registry or any conformance
artefact is edited by this file.

**Unit** X.KF.W4.`.c` · **Gate** G-KFW4-4 (G-L7c) · **§Bounds rows** L57 · L68 · L69 · L70 ·
L71 · **Trigger** §Sequencing L276, *"any write outside §Bounds"*.
**Substrate** `/Users/mkbabb/Programming/keyframes.js`, branch `master`,
⟨`git rev-parse --short=8 origin/master`⟩ → `55e9bf0d`. **Date** 2026-09-17.
**Commit** `3c8a5525`. Every figure double-run.

## The flag's own verdict is the instrument

§Bounds L71 fixes the method for this class in its own voice — *"the census is the per-site
**use count**, and **the flag itself is the instrument**"*, and, for the sites the spec could
not enumerate before the flag existed, *"the demo arm's falsifier closes on **the sites that
do red** — never on a count this file guessed."* Both arms were therefore run with the flag
forced on **before** any file was opened, read-only:

⟨`npx tsc --noEmit -p tsconfig.lib.json --noUnusedLocals`⟩ → **9** `noUnusedLocals`
diagnostics (+ F-1's one `TS2307`) — **reproducing §Gates' banked *"9 measured errors"* to
the digit**, at the frontier rather than at the disqualified worktree.
⟨`npx vue-tsc --noEmit -p tsconfig.json --noUnusedLocals`⟩ → **30** (9 src + 21 demo).

## BEFORE → AFTER

| arm | command | BEFORE | AFTER (commit `3c8a5525`) |
|---|---|---|---|
| flag | ⟨`grep -c noUnusedLocals tsconfig.json`⟩ | **0** | **1** — set in the shared base, so it reaches `tsconfig.lib.json` and `tsconfig.test.json` through their `extends` |
| src | ⟨`npx tsc --noEmit -p tsconfig.lib.json`⟩ | 9 unused + 1 `TS2307` | **3 unused + 1 `TS2307`** |
| demo | ⟨`npx vue-tsc --noEmit -p tsconfig.json`⟩ | 30 unused + 13 other | **18 unused + 13 other** |
| leg 2 | ⟨`npx tsc --noEmit -p tsconfig.test.json`⟩ | 0 | **17** (the flag reaching `test/` + `bench/`) |

**12 of the 30 cured, every one in-bounds. 18 survive, and not one of them is writable by
any unit of KF.W4.**

## CURED — 12, each with its LAW A census (`git grep -c <symbol>` in its own file → 1, the
declaration itself; consumer set = ∅ by the flag's own definition)

**src arm (written first, per the internal lock):**

| site | symbol | §Bounds |
|---|---|---|
| `src/animation/load-engine.ts:65` | `Stylesheet` | **L68, the row's named site** — one value.js edge off the LIGHT/HEAVY boundary module |
| `src/animation/load-engine.ts:62` · `:63` | `TimingFunction` · `TimingFunctionNames` | L68's file; **line-enumeration extension, declared below** |
| `src/animation/engine/css/animation.ts:54` + `:5` + `:48-53` + `:82` | `_boundTimeline` | **L69 / R-9's else-branch** — field and prose in one commit (`k3-decision.md`) |
| `src/animation/engine/css/animation.ts:32` | `transformTargetsStyle` | L69's file; line-enumeration extension |
| `src/animation/compile/emit/backward/backward.ts:72` | `CompileRefusalReason` | L70's file; line-enumeration extension. The type stays published at `emit/index.ts`, `compile/index.ts`, `index.ts`, `public.ts` — only this file's unused import goes |

**demo arm (written second):** `AmigaScene.vue:21` · `SquareScene.vue:81` ·
`SequenceScene.vue:8` · `SpringScene.vue:17` dead `computed` imports (all four named at
L71); `SpringScene.vue:55`'s dead `isPlaying` alias **with its clause** (KF-SS-34's second,
named at L71); `ChannelOptions.vue:480`'s unused `convertedFromName` destructure (the file
is L71's; the line is not, and the flag reds it); plus `useSquareTumble.ts:37`'s redundant
`as CssColor` (L71's named site, **not** a `noUnusedLocals` diagnostic — removal re-measured
at 31 errors before and after, and `CssColor` stays used at `:21`).

**DECLARED — the line-enumeration extension, so it is falsifiable rather than silent.** Four
cured sites (`load-engine.ts:62`/`:63`, `animation.ts:32`, `backward.ts:72`) sit in files
§Bounds opens for `.c` at coordinates the rows' line lists do not name. They are taken under
L71's own stated method (*"the flag itself is the instrument"*) because the enumeration could
not exist before the flag landed, and they are named here rather than folded into the rows'
counts. **No file outside `.c`'s writable set was written** — that bound was held absolutely.

## SURVIVING — 18, partitioned, every one outside every KF.W4 unit's writable set

**src (3):**

```
src/animation/group/composite/compositor.ts(79,11)  TS6133  'groupedKeys'
src/animation/group/waapi.ts(9,1)                   TS6133  'KeyframesAnimation'
src/animation/physics/smooth.ts(194,13)             TS6133  '_startLoop'
```

**demo (15):**

```
demo/components/instrument/keyframes/KeyframesStringControls.vue(44,9)   'CSSKeyframesAnimation'
demo/components/instrument/keyframes/KeyframesStringControls.vue(65,5)   'getTmpAnimationName'
demo/components/instrument/timeline/utils/snapshotCapture.ts(34,10)      'captureNonDefaultSnapshot'
demo/components/instrument/transport/channel-controls/ChannelControls.vue(378,7) 'tabsContentEl'
demo/components/instrument/transport/TransportDock.vue(276,7)            'menubarHostEl'
demo/components/instrument/transport/TransportDock.vue(366,9)            'resetIconEl'
demo/scenes/cube/CubeScene.vue(27,10)                                    'computed'
demo/scenes/cube/matrix-editor/MatrixEditor.vue(149,7)                   'resetMatrix'
demo/scenes/cube/matrix-editor/useTransformState.ts(9,5)                 'MATRIX_AXES'
demo/scenes/cube/matrix-editor/useTransformState.ts(10,5)                'transformSliderOptions'
demo/scenes/easing/EasingScene.vue(8,10)                                 'computed'
demo/scenes/easing/EasingScene.vue(45,7)                                 'isPlaying'
demo/scenes/spring/useSpringDemo.ts(1,29)                                'onScopeDispose'
demo/utils/reference-data/animationDescriptions.ts(108,7)                'COLOR_SPACE_DESCRIPTIONS'
demo/utils/reference-data/animationDescriptions.ts(116,7)                'HUE_METHOD_DESCRIPTIONS'
```

Two of the fifteen are worth naming for routing rather than left as a list:
`EasingScene.vue:8`/`:45` are **mechanically identical** to the `SpringScene.vue` pair this
unit cured, in a file `.e`'s row opens **for other lines**; and
`animationDescriptions.ts:108`/`:116` sit in a file **`.e` holds outright**.

**check leg 2 adds 17** under the same flag, of which **10 live in `test/`**
(`test/engine/animation.test.ts` ×3 · `test/group/group.test.ts` ×4 ·
`test/ingest/platform-adopt.test.ts` ×2 · `test/scroll/scroll-scene.test.ts` ×1) and the
rest are the src trio and four demo `.ts` files already listed. `test/**` is in no KF.W4
unit's writable set beyond the three files the wave creates or deletes by name.

## Gate consequence, stated plainly

**G-KFW4-4 is RED — FLAG LANDED, EVERY IN-BOUNDS SITE CURED, GREEN UNREACHABLE INSIDE
§BOUNDS.** The gate's falsifier reads *"Fails if the flag lands while any **in-bounds** site
survives"*, and by that clause alone the arms close: **zero in-bounds sites survive**. But
both arms' **commands** exit non-zero on 18 sites, and `check:lib` — a CI merge step — goes
from 1 error to 4. That is two oracles for one gate, and it is booked as RED rather than
resolved in this seat's favour.

**This is F-8's shape at a third address** (`.b` found it at G-KFW4-3, R4-2 found it at
G-KFW4-11): a witness column stating *"9 measured errors … chief among them
`load-engine.ts:65`* — a figure that **reproduces exactly** — with no partition of those 9
by writable set, and no demo-arm denominator at all, because none could exist before the
flag and an SFC-parsing checker both existed. The spec is not wrong about the count; it
never states how many of the count it owns.

**Nothing was suppressed to reach this reading**: no allowlist, no `exclude`, no
`@ts-expect-error`, no `@ts-ignore`, no `skipLibCheck` widening, no per-file override, no
`test.skip`, no `node_modules` patch. The flag was not weakened, and it was not placed in a
narrower config to dodge the fallout (`tsconfig.lib.json` and `tsconfig.test.json` are not
in `.c`'s writable set, and both `extends` the base by design).

**Honest dispositions available to the triumvirate, named not chosen:** (i) a successor seat
with the 18 files in its writable set cures them as one commit — every one is a plain dead
binding, the smallest possible cure class; (ii) the wave books G-KFW4-4 arms-closed on its
own falsifier with this enumeration attached as the declared residue, which is the same
shape R-3 already grants `TimelineCaret.vue` at one site and this file grants at eighteen;
(iii) the flag is reverted, which the spec's own goal criterion forbids.

## Witnesses measured NOT red — recorded, per L71's instruction, rather than cured on a guess

| witness | banked expectation | measured at this seat |
|---|---|---|
| `ChannelOptions.vue:437` `Teleport` import | L71: *"whether `vue-tsc --noUnusedLocals` reds this binding is a measurement this wave takes … not a premise it asserts"* | **NOT red.** The template's single live element (`:377` open / `:401` close) uses it. Recorded as a K3 **witness**; no byte written |
| `TimelineTrack.vue:120` `const props` | L71's named site | **NOT red** — `props` is used. Witness only |
| `TimelineCaret.vue:35` `const props` | R-3: KF.W7's cure site, witness-only here | **NOT red**, and **no byte of that file was written by this unit** |
| `SpringScene.vue` `SCENE_ID` (`:29`→`:185`) · `isStarted` (`:56`/`:186`) | KF-SS-34's other two of four | **NOT red** — both used at their stated lines. Only `computed` and `isPlaying` of the four red, and both are cured |
