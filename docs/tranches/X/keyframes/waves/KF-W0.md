# KF.W0 — Substrate Settle

**Preamble.** This file **SPECIFIES**; it does not execute. Every act below — above all the `git fetch && git reset --hard origin/master` in `/Users/mkbabb/Programming/keyframes.js` — **awaits the owner's begin-word**, and the reset itself is the **owner's hand**: `lane-docs.md:380` (verified verbatim at fold), *"Blocked from our side by lane law and by their sacred-checkout rule — value.js must NOT perform it."* A wave that performs the reset itself, or opens without the begin-word, re-runs the schism it exists to close. Specifying is not doing.

## Fold Note (fresh-Fable seat, 2026-08-28)

This spec is the M-23 agglomeration of one Opus draft against the CARRY authority (21 rows · 10 gates), verified row-for-row. All 21 CARRY rows carried (C-1..C-21); zero silent drops found; the following **fold repairs** were applied, loud:

- **F-R1 (path re-home)**: the CARRY's boundsFiles named `docs/tranches/X/kf/waves/W0.md`; the lane's real home is `docs/tranches/X/keyframes/` — this file lands at `waves/KF-W0.md` beside the already-authored siblings `KF-W4.md` and `KF-W7.md`. All W0 work-product and artefact paths re-homed accordingly. The draft's claim that this wave *creates* the lane dir is corrected: the dir exists (`carry/` + `waves/`, verified 2026-08-28).
- **F-R2 (witness re-verification)**: every RED baseline below was **re-measured by this fold seat on 2026-08-28**, read-only, in `/Users/mkbabb/Programming/keyframes.js` (and `/Users/mkbabb/Programming/glass-ui` where named). The CARRY seat's 2026-08-25 figures reproduce exactly (41/1 · 252/325/124 · four manifest coordinates · EE-02 stat 6+/76− · TypingDots inversion · 153/58/185 · the gate roster · lane-docs:380 · S-count 8).
- **F-R3 (N-3 corrected)**: the draft claimed `git grep 'templateFrames.length' HEAD -- demo/` returns zero rows. False — it returns **2 unrelated guard sites** (`KeyframesEditor.vue:204`, `useKeyframeOps.ts:179`). The correct witness is that the **cure line** (`() => animation.templateFrames.length,` = `origin/master:useKeyframesParsing.ts:97`) is absent at HEAD; G-0.4 uses the precise form.
- **F-R4 (G-0.7 widened)**: the dead-gate prose surface is larger than banked — a **third dead gate name** `proof:styling-idioms` (`demo/DESIGN.md:250`) and additional `proof:font-census` sites (`DESIGN.md:29`, `EditorStartScreen.vue:180`, `font-roles.json:2`/`:62`) plus `proof:brittleness` at **both** `demo/styles/style.css:38` and `demo/styles/layout.css:152`, all measured at `origin/master` today.
- **F-R5 (dropped riders restored)**: kf-SpringTrace's *"F-1 must be re-measured against origin/master before routing any wave"* and kf-SharePopover's *"the stripped worktree manifest remains an unexplained local artifact → KF.W0 §B-12"* restored to C-1; the **~94 dirty src rows** figure restored to C-2's inventory.
- **F-R6 (shadow-census home pinned)**: S-1..S-8 live in **`lane-frontend.md`** (`^### S-` count = 8; S-1 `:264` … S-8 `:387`), not in CENSUS — C-17/G-0.6 cite the real file.
- **F-R7 (N-2 confirmed by diff)**: disk `emit/backward.ts` (393 L) vs frontier `emit/backward/backward.ts` (393 L) diff returns **only import-path-depth hunks** (`../css-text`→`./css-text`, `./walk`→`./backward-walk`, `../format`→`./format`); HEAD = 410 L. The banked "diverges from BOTH refs" figures are corrected by appended note (E-3), never rewritten.

## Repair Round 1 (L-20 conformance, 2026-08-28)

Authority: `docs/tranches/X/keyframes/conformance/PASS-1/RULINGS.md` (cross-wave, binding) over `PASS-1/KF-W0-CHECK.md` (local register) — where the two collide, the ruling wins. Every amendment below is an append or a replace **inside this planned spec**; nothing here stamps a verb, opens product source, or re-runs a measurement. Registry corrections stay E-3 addenda under original ids (§Bounds, the 58-record row) — this file books the obligation, it does not perform it. Rows adopted from the registry keep their **banked ids** (anti-rename); riders, cure-shape locks and sequencing locks are **carried verbatim, not cited**.

| Directive | Landing site |
|---|---|
| **R-1a** — the twelve fold-rider escapes get their one adopting wave | new §Carry **C-1.F** id-keyed fold table (12 + KF-SKEL-7 riding from R-19b); C-1's blast-surface rider list widens from six records to the full count; C-9's hard order is thereby scoped on a complete blast surface (cures **D-5**) |
| **R-5** — the S-10 census slot triple-claim | §Carry **C-17** re-cut to the FULL claimant set (S-9 ≥ 8 · S-10 ≥ 4) as inputs to one mint; the kf-KeyboardShortcutsModal seat-ruled renumber **DEMOTED to a claim-input** by E-3 addendum; the fourth S-9 subject corrected (cures **D-1**, **D-9**) |
| **R-11** — `build:gh-pages` exists at no coordinate | seven sites corrected to **`npm run gh-pages`**. Receipt, re-verified this round: `git show origin/master:package.json` → `:43 "gh-pages": "vite build --mode gh-pages"`; `git grep -n 'build:gh-pages' origin/master` → no hits. Sites: §Scope 10 · C-13 · G-0.10 GREEN · unit `.e` · the KF.W6 OUTBOUND edge · §Cadence · §Artefacts (cures **D-2**) |
| **R-15** — the NO-WAVE-OWNER packet partition is CLOSED | §Sequencing's packet row re-cut from this file's stale **15** to the **canonical 17** (= the 15 ∪ {amiga, EDITOR-UNIT}; `transport` named canonically as `transport/ribbon`), with KF.W4's homing lists restated (W11 ×9 · W12 ×6 · **W13 minted ×2**). Homing stays SS-1/SS-2's act; this wave names the roster and supplies the ground |
| **R-17** — one proof roster, one owner, one sweep | **G-0.7** widened by its three missing demo sites, receiving the two residue families routed here from KF.W4 under R-8; the `scripts/gates/` inventory stated at the frontier **9 files** (cures **D-3**) |
| **R-19b** — KF.W6's CARRY §CrossEdges answered from this end | new §Carry row **C-22** + a new **INBOUND** row in §Sequencing's cross-edge table (the edge was declared at one end only; an answer that exists in §Carry but not in the edge table still leaves the seam half-open); all three corrections booked **by id** as G-0.9 appended-correction obligations (cures **D-6**) |
| **Local — the id-keyed census closed** (KF-W0-CHECK §2.C/§2.D) | new §Carry **C-1.G**: the 13 F-1-fold ids whose CONTENT C-1 already carried and whose **id** was absent by bytes, indexed with each row's banked routing words and its not-covered-by-the-fold limb named. **Nothing is re-booked** — indexing is not curing; it is what makes the fold's closure checkable. §2.D's third escape (**MISS-α5 ≡ RR-β K-BASE**'s attached kf-KeyframeTimeline header correction) booked at **C-1.R** + G-0.9 as the sixth named appended correction |

Local defects cured beside the rulings: **D-4** (G-0.4's three oracles re-founded on addressable witnesses; the `/\[object Object\]/` smoke assertion is keyframes.js's OWN tranche-V W1 born-RED gate and is declared as a cross-edge, never asserted as an in-tree instrument here) · **D-7** (C-9's provenance corrected to `C·C-13 / L·D-2`; the dropped MAJOR half booked) · **D-8** (C-19's firing ledger enumerated at its true count) · **D-10** (KF-CE-32's re-route declared **loud** as a ruling with the record's own words, E-3 addendum owed at the bank) · **D-11** (C-12's R-7/R-9 pairing corrected) · **D-12** (`e2e/**` struck as a phantom path; `dist/gh-pages/**` given a bounds row).

**Altitude sweep (the cure is not the cure until every altitude carries it).** A defect repaired in §Carry and left standing in §Scope, in a gate's prose, or in a unit's charge is not repaired — it is relocated, which is D-3's own mechanism one level up. So each cure was swept across all four altitudes of this file: **D-1** — §Scope item 6's "four-way S-9 collision + the requested S-10" **struck** and re-pointed at C-17.R's thirteen; **D-3** — §Scope item 8, G-0.7's roster prose and unit `.e`'s charge all re-cut from "three dead names / seven sites" to **four dead names over 10 sites + D-13's seven**, and from a bare gate roster to **3 entry points over the frontier's 9 files**; **D-2** — all seven execution voices now read `npm run gh-pages` while the banked words at C-13 and C-1.R stay verbatim (E-3); **R-15** — the §Sequencing packet roster and the KF.W12 edge row agree at 17 and no longer read "proposed" against a homed list. Receipts re-run read-only at this seat, 2026-08-28: `git grep -n 'build:gh-pages' origin/master` → **no hits**; `git ls-tree -r --name-only origin/master scripts/gates/` → **9 files**; `git grep -n 'proof:styling-idioms' origin/master -- demo/` → **4 hits incl. `DESIGN.md:237`, `EditorStartScreen.vue:159`, `SequenceTarget.css:212`**; `git ls-tree --name-only origin/master` → **no `e2e/`**; `scripts/observe/demo/{smoke,live-session}.mjs` + `scripts/lib/console-budget.mjs` → **all present** (G-0.4's runtime tier names instruments that exist).

**Status is unchanged: `planned`.** No gate here is turned; three gates gain born-RED witnesses they did not have.

## State

**Name**: KF.W0 — Substrate Settle (owner-hand §B-12 reconciliation of the sacred stale checkout)
**Lane**: X·KF · **Half**: **library** (authored from the library half's seat; the substrate settle is *indivisible* and binds both halves, so §Sequencing declares the demo-half edges from this end rather than partitioning bytes that cannot be partitioned).
**Opens after**: sub-tranche X·KF opens **AND** the owner's begin-word for the §B-12 act. No predecessor wave; this is the lane's first-executing file.
**Agents**: 5 units — OP-1 is the owner's act (not a seat) · then 2 parallel (.b ∥ .c) · then 2 parallel (.d ∥ .e). All Opus implementation seats (M-12/M-23); nothing here is design content.
**Hard gate**: ten conditions G-0.1..G-0.10, **all ten RED at authoring**, every baseline re-measured by the fold seat on **2026-08-28** with the command and its literal output pasted below.
**Status**: **planned**

| verb | value | evidence |
|---|---|---|
| AUDITED | **YES** | 58 adjudicated `kf-*.md` records (`ls registry/adjudicated/kf-*.md \| wc -l` → 58, 2026-08-28) + `CENSUS-2026-08-03.md` + `INTAKE-ADJUDICATION-2026-08-03.md` |
| SPECIFIED | **YES — by this file, 2026-08-28** | validated against `docs/precepts/` latest (L-20) |
| IMPLEMENTED | **NO** | stamped at this wave's own close |
| VERIFIED | **NO** | stamped only at KF.W10's sub-tranche release close (R-A) |

**Goal criterion.** This wave succeeds if, when work ends, **every adjudicated X·KF record names the ref it cures against, that ref is one ref, and no later wave can cite a count, a gate, a glass-ui citation, or a Codex denominator that was measured on a tree nobody named.** It changes no product behaviour: the only product bytes that move are moved by the owner's reset, toward the frontier, never away from it.

**COMPLETABLE**: **YES, once open.** Every unit but OP-1 writes `docs/**` only. OP-1's product-byte motion is a `reset --hard` toward an already-published ref — no authoring, no migration, no producer dependency. Nothing waits on glass-ui 8.0.0, on a producer release, or on a sibling wave. A row that returns *stale-by-substrate* is a complete result, not a deferral.

## Scope

1. Reconcile the sacred checkout to `origin/master` `81a56990` by the owner's hand, with the 1-ahead local commit and the full reconciliation surface each explicitly dispositioned (kept / committed / discarded) in a written record — both denominators stated (252 status-vs-HEAD · 325 diff-vs-frontier · 124 untracked).
2. Land one glass-ui declaration across all four coordinates (`HEAD` / `origin/master` / worktree / installed) and **rule** on the uncommitted deletion and on `legacy-peer-deps=true` versus the two genuinely-absent peers.
3. Reconcile the three lagging disk files **disk←master**, never master←disk, so the landed EE-02 css-twin cannot be clobbered by a stale-WIP commit.
4. Re-run EE-01 · FE-3 · EE-03 on the reconciled tree as the rebase oracle.
5. Re-run SCH-1..SCH-7 and X-2 at `origin/master`, each with its command and literal output pasted and dated.
6. Mint fresh non-colliding ids for every proposed shadow-census row **in ONE motion over the full claimant roster at §Carry C-17.R — ≥8 S-9 claimants and ≥4 S-10 claimants, thirteen rows** (R-5/D-1; the earlier "four-way S-9 collision + the requested S-10" framing is **struck at every altitude of this file** — a mint run over four-plus-one re-collides on its first motion, since `kf-KeyboardShortcutsModal:29` already renumbered a second "S-10" on top of `kf-KeyframesEditor:63`'s and `kf-SquareInstrument:78`'s). Each minted id cites its originating record; each dead or refused claimant is **enumerated, not minted**. **Claims are inputs to the mint; a claim is never a slot**, and no wave other than KF.W0 mints or renumbers a census slot.
7. Add the cascade-coupled census column beside the import census, **before** F-1's remediation scopes its regression surface; re-scope census §6.5's PRM roster and the z-index cells in the same motion.
8. Book one authoritative, dated gate roster — **3 npm `proof:*` entry points over the frontier's 9 `scripts/gates/` files** (R-17; the "surface, visual" two-script figure is a stale-worktree reading) — and strike every prose citation of a gate that does not exist: **four dead names over 10 demo sites**, not three names over seven (`proof:brittleness` · `proof:font-census` · `proof:styling-idioms` · **`proof:demo-no-oversize`**, the fourth arriving with kf-DemoGlobalChrome L-6's inbound residue family at **16 hits across 13 files**), **plus kf-AnimationControlsGroup D-13's seven further `proof:*` names** routed here from KF.W4 under R-8. One roster, one owner, one sweep.
9. Stamp a ref-of-record on every adjudicated record and reconcile the contradicting cells by **appended** correction (E-3 — dated evidence is never rewritten).
10. Re-derive every surviving `glass-ui/src/**` citation against the consumed dist, or strike it; run one fresh **`npm run gh-pages`** (R-11 — the script is `package.json:43` `"gh-pages": "vite build --mode gh-pages"`; **no `build:gh-pages` exists at any coordinate**) so the byte-offset receipts stop riding a Jul-16 artifact.
11. Disposition the Codex v8 contract pin — re-anchored, or declared stale-by-substrate and never citable for a live count — and **name** owner row OG-KF1 for KF.W10.

### Triumvirate Dispatch (mandatory, not optional)

- **The reset performed by any seat other than the owner**, or opened without the begin-word — invalidates the wave outright.
- **File-bound expansion**: any write under `/Users/mkbabb/Programming/keyframes.js/{src,demo,test,scripts}/**` beyond the two EE-02 paths, or any write to a producer repository. Product bytes in a substrate wave invalidate it. (`e2e/**` is struck from this clause — **no `e2e/` directory exists at any coordinate**: `git ls-tree --name-only origin/master` lists `.changeset .claude … bench demo docs … scripts src test`, no `e2e`. A phantom path in a bounds clause is the same defect class the wave exists to kill — D-12.) **`dist/gh-pages/**` is build OUTPUT, not authored product bytes** — G-0.10's one `npm run gh-pages` regenerates it and that regeneration is in-bounds by the row below; a hand-edit of any file under it is not.
- **A blanket re-anchor.** Any pass that applies one substrate direction across all 325 files halts — KF-TD-2 proves the direction is per-file (§Carry C-21).
- **A MANIFEST MISMATCH after the settle** (the four coordinates still disagreeing) is an owner return, not a local repair.
- **Third diagnostic iteration** on any single gate.

## Bounds

Reconciled against the CARRY `boundsFiles` (paths re-homed per F-R1). Owner-hand rows are marked; **none of them may be touched before the begin-word**.

| File | Access |
|---|---|
| `/Users/mkbabb/Programming/value.js/docs/tranches/X/keyframes/waves/KF-W0.md` | **create** — this spec (the CARRY named `X/kf/waves/W0.md`; re-homed at fold to the lane's real dir, beside `KF-W4.md`/`KF-W7.md`) |
| `…/docs/tranches/X/keyframes/W0/SUBSTRATE-SETTLE-2026-XX-XX.md` | create (dated, append-never-rewrite: the disposition record for the 1-ahead commit + every row of the reconciliation surface) |
| `…/docs/tranches/X/keyframes/W0/MANIFEST-RULING.md` | create (the four-coordinate ruling + the `legacy-peer-deps` / absent-peers ruling) |
| `…/docs/tranches/X/keyframes/W0/COUNTS-2026-XX-XX.md` | create (SCH-1..7 + X-2, command + literal output per row) |
| `…/docs/tranches/X/keyframes/W0/GATE-ROSTER.md` | create (the authoritative dated gate inventory) |
| `…/docs/tranches/X/keyframes/W0/REF-OF-RECORD.md` | create (the 58-record stamp ledger + the appended-correction index) |
| `…/docs/tranches/X/keyframes/W0/V8-DISPOSITION.md` | create (X-1's ruling; OG-KF1 named, not ruled) |
| `…/docs/tranches/V/megatranche/formation/keyframes/CENSUS-2026-08-03.md` | modify-append (SCH re-run · §6.5 PRM re-scope · the cascade-coupled legend column · X-2's 185) |
| `…/formation/keyframes/lane-frontend.md` | modify-append (F-1 row disposition; the S-1..S-8 shadow census — `^### S-` = 8, S-1 `:264`…S-8 `:387` — incl. the minted fresh ids; the `:194` EditorHeader roster row) |
| `…/formation/keyframes/lane-library.md` | modify-append (SCH-3/4/5/7 re-anchor: the 13-site parse-seam inventory, the four dead Tier-A paths, the depcruise note restated as unverified-at-HEAD) |
| `…/formation/keyframes/lane-docs.md` | modify-carve (§B-12 row 16 disposition cell only; `:145`'s live-unexecuted-maintenance-act framing untouched) |
| `…/megatranche/audit/codex-provenance/INTAKE-ADJUDICATION-2026-08-03.md` | modify-append (X-1's disposition; the KF.W0 carried-rows table's outcome column) |
| `…/megatranche/registry/adjudicated/kf-*.md` (all **58**) | **modify-append only** — ref-of-record stamp + DISSENT-3 reconciliation, **E-3: never a rewrite of dated evidence** |
| `…/docs/tranches/V/coordination/INBOX.md` | modify-append (E13: the KF-APP-59 unmet-peers FYI, the EH-1 letter-vs-artifact rider, the KF-APP-5 producer-half-MOOT update — the three relay packets of §Sequencing) |
| `/Users/mkbabb/Programming/keyframes.js/package.json` | **OWNER'S HAND**, post-begin-word only (the 7.0.0 exact devDep + the kf 6.0.0 / value.js 4.0.0 pairing) |
| `/Users/mkbabb/Programming/keyframes.js/package-lock.json` | **OWNER'S HAND**, post-begin-word only (the three glass-ui entries incl. lock `:612` integrity + `dev:true`) |
| `…/keyframes.js/demo/components/instrument/transport/channel-controls/composables/useTimingFunctionEditor.ts` | **OWNER'S HAND** — EE-02 site 1, reconcile disk←master |
| `…/keyframes.js/demo/components/instrument/transport/channel-controls/TimingFunctionPanel.vue` | **OWNER'S HAND** — EE-02 site 2 (`:144` bezier-drag write), reconcile disk←master |
| `/Users/mkbabb/Programming/keyframes.js/dist/gh-pages/**` | **regenerate-only** (D-12) — G-0.10's single `npm run gh-pages` rewrites this tree wholesale; it is generated output, gitignored at the frontier (`package.json:32` `"!dist/gh-pages"`), and the wave's one byte-producing act. **No file under it is ever hand-edited**, and the artefact banked is the build hash, not the bytes. Naming it here closes the hole the Triumvirate Dispatch's "product bytes in a substrate wave invalidate it" would otherwise leave ambiguous. |

**Do NOT touch**: `keyframes.js/src/**`, `demo/**` (beyond the two EE-02 paths), `test/**`, `scripts/**`; **any producer repository** — `/Users/mkbabb/Programming/glass-ui/**` is read/hash only, and `node_modules/@mkbabb/glass-ui/**` likewise; `value.js/scripts/dev/dev.sh` (unowned, never touched); every `docs/tranches/X/waves/W*.md` (X·V's own files), `docs/tranches/X/parse-that/**` (X·P's), and the sibling `X/keyframes/waves/KF-W4.md` / `KF-W7.md` (their own waves' files). **`e2e/**` is NOT listed** — it does not exist (D-12); a do-not-touch entry for a phantom path manufactures false coverage. If keyframes.js's own tranche V ever creates `e2e/`, it is out of bounds by the `demo/**`-class rule, not by name.

**Disjointness.** OP-1 runs **alone and first** — it is the only unit that moves a product byte, and every other unit reads what it moves. After it: `.b` → `MANIFEST-RULING.md` + `INBOX.md`; `.c` → `SUBSTRATE-SETTLE-*.md` (the two EE-02 paths are the owner's, verified-not-written by `.c`); `.d` → `COUNTS-*.md` + `CENSUS` + both lane files; `.e` → `GATE-ROSTER.md` + `REF-OF-RECORD.md` + `V8-DISPOSITION.md` + the 58 records + `INTAKE-ADJUDICATION` + `lane-docs.md`. No two concurrent units share a `modify` path. `INBOX.md` is appended by `.b` alone, once, at close.

## Carry

Every CARRY row lands as a gate, a bounds item, or a named cure with its locks. `⟨…⟩` = provenance. **22 rows** (C-1..C-21 from the CARRY, all carried, 0 dropped; **C-22 adopted this round under R-19b**), plus **four id-keyed sub-tables** minted by repair round 1: **C-1.F** (13 fold-identity rows ADOPTED — ids and riders both previously absent, R-1a + R-19b) · **C-1.G** (13 fold ID-INDEX rows — content already carried at C-1, ids previously absent by bytes; KF-W0-CHECK §2.C) · **C-1.R** (4 declared re-routes and corrections, loud per E-3) · **C-17.R** (13 census-slot claimants, R-5). Sub-table row counts are stated so a closure sweep has a denominator: **22 + 13 + 13 + 4 + 13 = 65 addressable rows**, every one greppable by its banked id. Every row and sub-row keeps its **banked id** — no id in this file is coined, renamed, or renumbered; the only fresh ids this wave ever mints are the census-slot ids C-17 assigns, and it mints those in one motion.

| # | Banked id | Mechanism (compressed) | Cure + locks carried | Gate |
|---|---|---|---|---|
| C-1 | **F-1 ≡ SCH-1** ≡ kf-AmigaScene C-1 ≡ **KF-CE-32** ≡ kf-App ruling 1 ⟨~20 axes/readers across 41 records⟩ | Four disagreeing glass-ui manifest states over one installed artifact. `npm ci` at HEAD does not fail — it silently **downgrades 7.0.0→6.0.0**; the "phantom" is an uncommitted local deletion. | Land the frontier's 7.0.0 exact devDep + lock and **rule on the deletion**. Locks verbatim: kf-CubeScene *"sync the sacred checkout to `81a56990`+; RULE on the worktree's uncommitted glass-ui devDep deletion (committing it as-is revives census F-1)"*; kf-LayerConfigPanel *"the reconciliation must land the frontier's 7.0.0 devDep, at which moment every prop verdict here is a verdict about the shipping build"*; kf-SpringTrace *"F-1 must be re-measured against origin/master before routing any wave"* (discharged by this file's re-measurement, dated); kf-SharePopover — the stripped worktree manifest *"remains an unexplained local artifact → KF.W0 §B-12"* (the deletion-ruling is its disposition). **KF-HA-6 (pin-and-bump-one-commit) is DISCHARGED BY, not blocking**: `origin/master` already IS that commit (kf 6.0.0 · value.js 4.0.0 — both violated peers satisfied at the frontier; only HEAD, at 5.3.5 / `^3.1.0`, violates). **Cross-repo rider**: keyframes.js's own tranche V pre-booked this as **FAM-01 RAIL / H→V-01 / W2 producer-gated** ⟨AUDIT-REGISTRY.md:15 + V.md:74⟩ — the deleted pin is a deliberate in-flight migration state, which corrects the axes' "nobody noticed for three majors" causal story. **Blast-surface riders carried, not re-booked**: 8 specifiers / 5 modules transitively via `@components/instrument/shell` ⟨kf-MbabbMenu ruling 2⟩; 18 subpaths / 44 sites, the "no demo file uses a subpath" convention true only for `/button` + `/card` ⟨kf-RibbonBar K10, rider on banked CPW C-20⟩; ChromeDock the densest consumer (`:6-11`/`:22-28`/`:29`/`:158`, graph dies at `:11`, mounted unconditionally from App.vue); kf-ChannelOptions' four subpath edges (`:143-146` / TimingFunctionPanel `:26-31`, authored against 3.4.0/4.0.1 while 7.0.0 resolves — *"the unpinned-drift half is live and is what KF-CO-1/2/18 measure"*); kf-KeyframesStringControls' two transitive critical-path edges (`CSSCodeEditor.vue:38`; `useHighlightCSS.ts:2` via `useApplyCSS.ts:4`); kf-EditorStartScreen's `z-controls` exposure cell. **The blast-surface list is NOT six records — it is the full fold-identity table at §Carry C-1.F below** (R-1a): thirteen ids, each with its banked travel-rider verbatim. C-1.F is where the fold's identity mechanism becomes id-keyed; a later seat greps its own row there. **This is what makes C-9's hard order satisfiable**: the cascade column lands before F-1's remediation scopes a blast radius that is now counted whole rather than under-counted at six. **Probes RETIRED here** (no seat re-spends the budget): the clean-`npm ci`-fails probe ⟨L-M-9⟩; the "verify against 6.0.0" residue — *"discharged; no 6.0.0 verification is owed by anyone"* ⟨kf-CubeScene kill 3⟩; every "blocked on F-1" gate in the CopyButton corpus (D-9, C-15, M-6 leg 2) — **RE-SCOPED, not blocked**. **Dissent preserved, charged nowhere**: kf-CubeAxisLines β-miss-1's `--omit=optional` failure-SHAPE note, a HEAD-only artifact killed at the frontier ⟨kf-CubeScene kill 4⟩. | **G-0.2** |
| C-2 | **census §0 substrate schism** ⟨kf-CubeScene reader-B M-1, RESCOPED-BY-INVERSION⟩ | The checkout is a **hybrid**: 41 behind AND 1 ahead; neither ref an ancestor of the other (merge-base `a59d3a22`, both `--is-ancestor` probes NO ⟨kf-TimelineCaret⟩). Every anchor binds to one of three coordinates, and the corpora silently disagree which — with a fourth coordinate alleged (the `backward` divergence, corrected at F-R7/N-2). | The §B-12 act, then re-anchor every lane and registry finding. **Binding on the whole sub-tranche** ⟨kf-TimelineCaret⟩: *"any spec consuming this registry states WHICH ref it cures against."* **Tree-motion law** ⟨kf-KeyframesStringControls DISSENT 3⟩: *"every anchor in this record is a TODAY-disk read; every cure re-resolves at `origin/master` first; and any future seat re-running a corpus grep on this repo must date its run."* Uncommitted-deletion inventory carried by record: `demo/utils/{formatEditorCSS,helpers,keyframeSelector}.ts` −75 L ⟨kf-OrbitalDrag⟩; the first and third **`??` UNTRACKED and absent from HEAD's tree while cited by line** ⟨kf-TimelineCaret⟩; the src refactor-in-flight ⟨kf-CubeTarget⟩; **~94 dirty src rows behind the CopyButton corpus's every src anchor**; StartingStyleTarget's `emphasis` migration observed live ⟨kf-SpringScene⟩. Reader-B's "describes uncommitted code" caveats (L-5/L-9/L-14/C-7/P-1/P-6) **VACATED with the inversion**, and its corollary ("P-1's adoptCompiled watch never fires at HEAD") with them; the factual observation (the corpus DID audit a dirty tree) preserved as true-but-mis-baselined. | **G-0.1** |
| C-3 | **THE DELETION** — rider on **KF-APP-5** + SCH-1 ⟨first-banked kf-EditorShell RR-1 missed #1 ≡ RR-2 missed M1, ONE identity⟩ | glass-ui `4bf53962` (2026-08-04, ancestor of producer HEAD) deletes `src/components/header-ribbon/` WHOLE (exports 73→68); the frontier consumer still imports the deleted subpath; installed 7.0.0 still ships the dist file. All three legs re-verified at fold (G-0.5 witness). | **Verbatim sequencing law**: *"KF.W0's substrate settle must NOT advance the installed glass-ui past `4bf53962` before the shell's chrome host migrates off `/header-ribbon` — else the build dies at `:116`."* Consequences ruled with it: (i) every axis's remediation step 1 ("declare glass-ui ≥ the `4e8c6387` cut and regenerate the lock" — L §6.1, C §8.1, D §7.1) is **UNBUILDABLE**; both readers' kills ratified, incl. RR-2's kill of L §1.0's escape-hatch framing (*"L-1 was never a pinning problem — the design system retired the component under its only consumer"*); (ii) **KF-APP-5's producer half is MOOT** — the producer answered by deletion; the relay downgrades to FYI + the consumer-evidence correction; (iii) producer-side content: `docs/consumer-evidence/header-ribbon.md:3` still says "RETAINED persistent-only", contradicting producer HEAD. **Severity dissent** ⟨DISSENT 4⟩ ruled blocker-weight at the banks with RR-1's MAJOR preserved — *"the shell still renders today against the installed artifact; the break is at the next producer advance."* Migration decision (which chrome host replaces the band) = **NO-WAVE-OWNER**, SS-1/SS-2's; it carries this tripwire wherever it lands. | **G-0.5** |
| C-4 | **L·B-2** ⟨kf-ChannelOptions, RESCOPED-BY-SUBSTRATE⟩ | Disk == `origin/master` byte-for-byte **except three named lags**: the 7.0.0 devDep; the **EE-02 css-twin** on `setAnimationTimingFunction` (disk `{ fn }`, master `{ fn, css }` + `timingFunctionLiteralFor`); the same twin at `TimingFunctionPanel.vue:144`. Both measured consequences (WAAPI demotion, export-serializer identity-miss) are **cured at the frontier**; the disk WIP would regress them. | **Verbatim**: *"KF.W0 obligation: §B-12 must reconcile the three lagging disk files so the landed EE-02 cure is not clobbered by a stale-WIP commit."* **Direction lock: disk←master, never master←disk.** Not this wave's, at MAJOR → OPTIONS-UNIT/engine lane: the direct-field-write bypass of `setTimingFunction`; the engine-side `applyTimingFunction` frame-propagation gap; N-10's double write; the N-17 flatten tripwire. | **G-0.3** |
| C-5 | **EE-01** ⟨DISPOSITIONS.md:69, born V → BUILD W1; folded from KF-CB-1 · D-1 = L·B-1 = C·C-1⟩ | `timingFunction: "bounceInEase"` resolves to nothing under value.js 4.0.0; the ctor throws inside an uncaught async `onMounted` (no try/catch `:65-102`, no `app.config.errorHandler`). Frontier carries the one-word repair (`origin/master:42` = `"easeInBounce"`, re-verified at fold). | **Verbatim**: *"FOLD to EE-01 + KF.W0: the reconciliation's one obligation here is that the stale fork's in-flight migration must not re-land the dead name over the frontier's repair."* **Sequencing inversion ruled at the bank**: K1's "the fix must land WITH the migration commit" is **superseded** — the fix landed upstream; the danger inverts to a **re-regression**. Surviving phantom trail (prose `easing.ts:44`, `waapi/eligibility.ts:169`, the m-6 test) → **KF-CB-18 / KF.W4**, not here. | **G-0.4** |
| C-6 | **FE-3** ⟨DISPOSITIONS.md §D; folded from KF-KE-1 · D-1 = L-B2 **and** KF-KC-1 · D-1 = L-1 = C-1⟩ | `KeyframeCardList.vue:11` calls `.toString()` on a bare frozen `KeyframeSelector` → `[object Object]` on every start field and ghost label; the correct renderer `selectorText()` is already in the closure's import graph (`useKeyframeOps.ts:111`). Frontier `:11` = `startScalar(frames[i].start)` (commit `add20b7e`, re-verified at fold). | **Verbatim**: *"FOLD to FE-3 + KF.W0: the surviving obligation is the re-regression guard; FE-3's `/\[object Object\]/` smoke assertion is the born-RED witness"* (low collision risk — neither file locally dirty — but the guard is the same one EE-01 carries). **PARTIAL-CURE caveat that must travel with it** ⟨kf-KeyframeCard⟩: the frontier's cure renders the FRACTION `"0.5"` where the commit parser (`:206-209`) demands `"50%"`, a NAMED selector still coerces to `"[object Object]"`, and `selectorText` remains uncalled at both seams → **NO-WAVE-OWNER** terminal cure. Cause not cured: **KF-KE-17** (`frames: any[]` erases the library's type at the one seam the editor owns) → **KFED-UNIT, after KF.W4**. | **G-0.4** |
| C-7 | **EE-03** ⟨DISPOSITIONS.md; folded from KF-KE-9 · L-M1⟩ | The `templateFrames` **array** watch never fires and warns per element at setup (re-confirmed on vue 3.5.35). Frontier's cure is the exact dispositioned one — `watch(() => animation.templateFrames.length)` at `useKeyframesParsing.ts:97`. | **Verbatim**: *"FOLD to EE-03 + KF.W0 re-regression guard (EE-03's watch-warn-count→0 gate)."* Consequence living on: **KF-KE-10**'s visibility change — `fromString` **APPENDS** (3→6 frames, selectors `0,0,0.5,0.5,1,1`; silent at the audited bytes, projected as 10 duplicate cards at the frontier) → KFED-UNIT + **one library letter to KF.W5** on `fromString` append semantics. | **G-0.4** |
| C-8 | **SCH-1..SCH-7 + X-2** ⟨CENSUS §schism-table + §(a) row 1; INTAKE X-2⟩ | Two of three census lanes measured a tree 41 commits stale. SCH-1 falsified (C-1). SCH-3: 145/22 636 claimed vs **153** at frontier (fold re-measured), with FOLD-FORWARD §C's "159 modules" a third basis and Codex's "80 public library surfaces" a fourth. SCH-4: 61 value.js `from`-lines (`/value` 15) vs 62 (16). SCH-5: 4+ Tier-A anchors DEAD (`value-ast.ts:71`→`compile/value/compile.ts:32`; `easing-registry.ts`→`compile/easing/registry.ts`; `emit/backward/backward.ts`; `emit/easing-serialize.ts:71-73`). SCH-6: **58** `.vue` holds (fold re-measured), contents drifted. SCH-7: LIB L-1/L-2 depcruise unverified — "0 depcruise violations" does **not** refute L-1 (silent coverage loss reds nothing). X-2: 184 is the stale-worktree figure; **185** at frontier (fold re-measured). | Re-run every count at `origin/master`, command + literal output pasted, dated. **SCH-2** (KfPillTabs 217-line fork) **CONFIRMED at HEAD — re-verify, do not re-book**; stays KF.W6's top suffusion target. **W0 re-runs COUNTS; it does NOT reconcile the counting BASES** — X-4 (80 / 139 / 153 / "159") is **KF.W5's**. SCH-7's depcruise re-verification is KF.W5's input; W0 only restates it as unverified-at-HEAD. | **G-0.6** |
| C-9 | **L-5/C-7 legend rider** ⟨kf-SequenceAxis; ≡ kf-SequenceScrubber **C·C-13 / L·D-2** folded by name — **provenance corrected this round (D-7)**: the banked row is `kf-SequenceScrubber.md:71` *"C·C-13 / L·D-2 (surviving refinement)"*, re-stated at `:126` *"C·C-13 ≡ banked kf-SequenceAxis L-5/C-7 legend rider → KF.W0 §B-12"*. The previously cited "D-11/C-13" named a different family in that record — `D·D-11` at `:57` (aria-valuenow/valuetext) and `L·D-11` at `:65` (centre-anchor, killed at K-5), routed to KF.W7/W9, never here; restated kf-App.skeleton ("the §B-12 reconciliation adopts a cascade-level coupling arm beside the import census"), kf-EditorStartScreen L-EST-1/D-C9, kf-SquareInstrument (NO-WAVE-OWNER census-column note)⟩ | Zero-import files whose entire visual identity resolves through the **undeclared** glass-ui via the cascade: `text-mono-caption` is an `@utility` inside node_modules; `--border`/`--muted-foreground` vendor-only; `--radius-pill`'s sole definer is glass `theme/radius.css`; `z-controls` is glass-**GENERATED** (`--z-index-controls: var(--z-controls)` in `@theme inline`, bridges.css). An import-graph census scores every one of these CLEAN and is wrong. | **Verbatim**: *"a second census column separating import-coupled from cascade-coupled **before** F-1's remediation scopes its regression surface — routes to KF.W0 §B-12 as a legend rider."* **Hard order**: must land BEFORE the F-1 remediation scopes its blast radius, else the reconciliation under-counts what it can break — now scoped against C-1.F's full thirteen, not six (R-1a). **`L·D-2`'s MAJOR half is BOOKED here, not dropped (D-7)**: verbatim ⟨kf-SequenceScrubber:71⟩ — *"a component with ZERO glass-ui imports is fully glass-coupled through the `@import` cascade (`text-caption`/`text-mono-caption`/`text-muted-foreground`/`border-border` + `--radius-pill`, sole definer glass theme/radius.css) — an import-graph census scores it clean and is wrong."* That refinement is the surviving half; **what died** is L·D-2's phantom-dependency arm, killed at the record's K-1 (*"ONE-BASELINE-SHORT — the sixth on record"*, `:80`) along with *"every 'blocking'/sequencing/post-`npm ci` consequence in all three axes"*. The kill's corollary rides to G-0.9: the banked `kf-AnimationVisualizer.md:9` census cell is **corrected to worktree-scope** by appended note. This record's K-1 is also a C-19 firing (see C-19's enumerated ledger). | **G-0.6** |
| C-10 | **KF-AX-32** ⟨kf-CubeAxisLines, NEW · α-miss-5⟩ | Census §6.5's "13 PRM enforcement sites" is a roster of **local guards misread as coverage**. The vendor's universal `*:not([data-allow-motion])` rule reaches every demo element, so the roster's two "delegation … unverified statically" gap rows (TypingDots, KeyframeTimeline — the latter citing the wrong vendor file) actually **HOLD**, and D·D-1's BLOCKER ground was manufactured by the same mis-scoping. MINOR at the row, but *"the exact mechanism of the corpus's only false BLOCKER."* | **Verbatim**: *"KF.W0 (re-scope §6.5 in the same motion as the z-index rows)."* Universal-reset decider banked at kf-CubeScene D-16 (extracted from shipped `dist/gh-pages/assets/index-CL_QYCiO.css`) and kf-EditorHeader kill #1 (`glass-ui/dist/styles/utilities/a11y-overrides.css`, byte 0, unlayered, `!important`). Feeds **KF.W9**, whose tallies re-derive from the corrected roster (kf-CubeScene: 4 engine motions + 1 gated; **kf-OrbitalDrag OD-11 corrects the cube tally to 5 ungated**). Dissent preserved: the citation-inheritance ledger for this scene (α's ×4 fallback cell, third generation past a published correction). | **G-0.6** |
| C-11 | **KF-AX-12** ⟨kf-CubeAxisLines, both readers independently; ≡ kf-CubeTarget #51⟩ | The census is wrong about this file at its OWN HEAD, twice (`raw z-index:-10` ×2), inherited from stale prose no seat re-read. | **Verbatim**: *"KF.W0 (census re-anchor); prose sites ride KF.W6"* — bundled with C-10, *"in the same motion"*. The in-tree prose recut lands with KF-AX-11/12 + **KF-AX-31's z-contract block** in one KF.W6 motion, which additionally resolves the `--z-behind` **POLICY CONTRADICTION** (`layout.css:8-11` documents the shadow as deliberate override policy — *"OUTSIDE @layer … authoritative for the demo"*): **a contradiction to RESOLVE, not a stray to delete.** | **G-0.6** |
| C-12 | **KF-AX-31** ⟨kf-CubeAxisLines, NEW · β-miss-5⟩ + **R-7's dead-`proof:font-census` arm** ⟨kf-KeyboardShortcutsModal **R-7**, `:43` — **provenance corrected this round (D-11)**: `:43` is R-7, the dead-witness row (*"the named witness is dead"*; `DESIGN.md:24-30` states the Mono-as-data law and names `proof:font-census`); `:45` is R-9, the *"nothing automated verifies this file"* row that routes to KF.W4. The cell separates them correctly below; the tag was a transcription slip of exactly the class C-19 books, so it is booked as one⟩ | Comment chains cite dissolved gates. `demo/styles/style.css:38-39` says *"`proof:brittleness` gates against drift"*; no such gate exists (dissolution `70b32501`) — *"a comment chain pointing to a contract pointing to a deleted enforcement, two falsehoods deep"*, and the same block also carries C-11's stale raw-−10 sentence. `DESIGN.md:24-30` names `proof:font-census` as the Mono-as-data law's witness; it does not exist. **Fold-widened surface (F-R4, measured at `origin/master` 2026-08-28)**: a THIRD dead name `proof:styling-idioms` at `DESIGN.md:250`; further `proof:font-census` cites at `DESIGN.md:29`, `EditorStartScreen.vue:180`, `font-roles.json:2`/`:62`; `proof:brittleness` also at `layout.css:152`. **Round-1 widening (R-17, D-3) — three further `proof:styling-idioms` sites, re-measured at `origin/master` this round** (`git grep -n 'proof:styling-idioms' origin/master -- demo/`): `demo/DESIGN.md:237` (the **R4** codex clause, *"Clause on `proof:styling-idioms`; U.B12"*), `demo/components/instrument/shell/EditorStartScreen.vue:159` (*"silent-flatten (proof:styling-idioms membership)"*), `demo/scenes/sequence/SequenceTarget.css:212` (*"owned here so it resolves under proof:styling-idioms"*). **The demo-side surface is 10 sites, not 7** — a sweep run against the seven-site list leaves three citations alive, which is this gate's own stated falsifier reproduced one level up. | Book **one authoritative dated gate roster** and strike every prose citation of a gate that does not exist. **The record is SPLIT**: the dead-gate arm lands here; the **demo-typechecking arm routes to KF.W4** (post-F-1), folding to the single banked no-SFC-typecheck identity — KF-APP-4 ≡ KF-CE-16 ≡ KF-CO-7 ≡ MM-36 ≡ LP-7 ≡ KF-AT-12 ≡ KF-HA-5 ≡ L-4/C-6 ≡ B-1 ≡ RB B-1 — **booked ONCE, never re-booked**. R-9's carve-out (no `vue-tsc` in devDeps or `.bin`; `vitest.config.ts` two projects, NO `@vitejs/plugin-vue`; root-only vite config) is KF.W4's input, **dated here**. **INBOUND under R-17/R-8 — the roster RECEIVES two residue families KF.W4 routes here** (reciprocal edge declared at KF-W4's §Excluded residue table; one roster, one owner, one sweep): (i) **kf-AnimationControlsGroup D-13** — *"seven `proof:*` gate citations resolve to zero runnable gates"*, banked NO-WAVE-OWNER (prose-truth hygiene), the seven names adopted into the sweep by id; (ii) **kf-DemoGlobalChrome L-6** — `proof:demo-no-oversize` at **16 hits across 13 files** (that seat's executed grep; *"RR-L said 10; missed `useAnimationGroupActions.ts` and `useAnimationGroupPlayback.ts` among others"*), carried with its verbatim lock *"A fix confined to this file leaves twelve files standing; **the fold owns the sweep**"* — and with the record's own consequence preserved: the header's SOLE stated reason for `DemoGlobalChrome.vue`'s existence cites the retired gate, which strengthens its C-3 re-home (that re-home is not this wave's). **`proof:demo-no-oversize` is therefore a FOURTH dead name**, and the sweep's denominator is the three prose names + D-13's seven + this one. | **G-0.7** |
| C-13 | **Corpus-integrity row** ⟨kf-CSSPasteDialog, MAJOR/process; K-9 = 2nd instance; kf-EditorHeader EH-1 = 3rd⟩ | challenge-D's evidence root (`challenge-D-design.md:7`, "verified 7.0.0 both sides") matched version **STRINGS, not ARTIFACTS** — *"the glass-ui source tree was weeks of unpublished work ahead of the consumed 7.0.0 dist. Roughly a third of D's file:line provenance does not resolve in the artifact under audit"* (both re-readers convergent; 7.0.0 cut 2026-07-16, `--ink-perimeter` landed 2026-08-04). Second instance self-inflicted: R-B, the reader who convicted D, cited "glass-ui src at `b155ca4c~1:DialogContent.vue:482-489`" and was KILLED on the shipped chunk. | **Verbatim**: *"every D citation into `glass-ui/src/**` must be re-derived against `node_modules` before any wave acts on it; L and C, which read the dist, are the durable records on every overlapping row."* **Companion caveat carried whole**: *"the built witness `dist/gh-pages/assets/index-CL_QYCiO.css` is dated Jul 16 — possibly pre-glass-7 — so every byte-offset receipt in the corpus is provisional until a fresh `build:gh-pages`"* (fold re-verified: mtime Jul 16 09:11) — **the caveat is carried verbatim as banked, and the COMMAND it names is corrected in the execution text: `npm run gh-pages` (R-11). No `build:gh-pages` script exists at HEAD, `origin/master` or the worktree; `package.json:43` is `"gh-pages": "vite build --mode gh-pages"`. The banked words are not rewritten (E-3); the act this wave performs is the real one.** **Precedent** (same class): the value.js PaletteCard R-4 registry repair carried in COHESION §4 — an evidence-ledger correction no product wave owns, homed at the substrate wave because that is where the re-anchor law lives. Run the fresh build **once, here** (KF.W9's SS-13 witnesses need the same act). | **G-0.10** |
| C-14 | **EH-1** ⟨kf-EditorHeader, C §0 substance — MAJOR, *"the row inverts the disposition and was load-bearing for the whole corpus"*⟩ | HeaderRibbon 7.0.0 **still ships** the `anchor` slot (`{pinned}` payload, `HeaderRibbon.vue.d.ts`), the entire collapsible machinery (x/S/C flags, inert/aria-hidden, Esc) and `--header-ribbon-actions-width`; the archived letter claims all three were deleted. Only `mode` and `anchorLabel` are actually gone. **Under the letter the target file is irreplaceable; under the tree it is deletable.** The corpus also cited a NONEXISTENT path (`coordination/…-consumer-updates.md`) — a separate citation kill; the real letter is `value.js/docs/tranches/V/archive/GLASS-INBOUND-2026-07-16-headerribbon-persistent-only.md:15-19`. | **Verbatim**: *"KF.W0 (re-read XR-4 / IN-GLASS-1 / DISPOSITIONS.md:21 against the ARCHIVE file + `dist/header-ribbon.js` before any Glass-7 consume slice is scoped) + rider to the standing glass BH relay: producer docs/letters must be re-derived against shipped bytes (the kf-ChromeDock doc-drift ledger's pattern, third instance)."* **Composes with C-3**: the letter is wrong about 7.0.0 **and** the component is gone at producer HEAD — the reconciliation must read BOTH the installed dist and producer HEAD before ruling. Gates any Glass-7 consume slice: **nothing in KF.W6 may be scoped from the letter.** | **G-0.10** |
| C-15 | **L-i2** ⟨kf-EditorHeader, INFO — substrate-dated, + the phantom-attr rider⟩ | `mode="persistent"` is a stray attr, worktree-only; the frontier is a clean `placement="right"`. But **HEAD carries `position="right"` — also not a 7.0.0 prop**. The phantom-attr class exists at HEAD in a different spelling, and a naive reconciliation would carry it forward. | **Verbatim**: *"the reconciliation belongs to KF.W0's substrate settle, not to a component cure."* Corroborated: L-3 is frontier-CURED (phantom deleted at `origin/master:16`) and **L-17 reframes it as a FOSSIL of a contract cut underneath the consumer, not author error** ⟨kf-EditorShell⟩. **It is a reason to diff the reconciliation output rather than assume it.** | **G-0.1** |
| C-16 | **KF-APP-59** ⟨kf-App β-miss 7, INFO — *"a closed falsifier, not a defect"*; kf-EditorHeader C-18's two-unmet-peers rider; kf-HeroAurora KF-HA-6's `.npmrc` decider⟩ | glass-ui 7.0.0's peer set is unsatisfiable in-tree (`@mkbabb/pencil-boil ^0.9.2`, `embla-carousel-vue ^8.0` — fold re-read the installed peer map) but **UNREACHABLE from App's closure** — the root barrel imports neither. `.npmrc` = the single line `legacy-peer-deps=true`; `ls node_modules/@mkbabb/` → glass-ui, parse-that, value.js (no pencil-boil). | **Verbatim**: *"KF.W0 substrate note + BH relay FYI; no App booking."* **Compose with C-1's `.npmrc` rider**: the reconciliation must decide whether `legacy-peer-deps=true` continues to mask the two genuinely-absent peers, or whether the absence is declared and the flag retired — **either is a ruling; silence is not.** Producer-facing half rides SS-6 / the standing BH relay per owner edict, **NEVER a demo-side hack**. | **G-0.2** |
| C-17 | **S-9 collision (four proposals) + S-10** ⟨kf-SpringHeatmap C-M-1; kf-EditorHeader F1(c); kf-CubeScene L-5/C-6 + kf-LayerConfigPanel ruling 8; kf-AmigaScene C-6; kf-KeyframesEditor asks S-10⟩ | The shadow census runs **S-1..S-8** — home pinned at fold (F-R6): `lane-frontend.md`, `^### S-` = 8, S-1 `:264` … S-8 `:387`. **The roster is NOT four-plus-one — it is ≥8 S-9 claimants and ≥4 S-10 claimants** (R-5, D-1), enumerated id-for-id at **§Carry C-17.R** below. The four originally named: glass `./canvas` (present at installed 7.0.0, 73 exports; falsifier bytes in `dist/resolveCanvasColor-CBnFEM0G.js`, `dist/canvas.js` a re-export shim); `EditorHeader → HeaderRibbon — DEAD, replace-by-deletion` (`lane-frontend.md:194` rosters the file live while the shadow census omits it); the number-field row; and — **fourth subject corrected (D-9)** — kf-AmigaScene **C-6**, which is *"Two shipped glass primitives (`Surface`, `PaperBackdrop`; `Card` per the sibling register) shadowed by 8 lines of bespoke CSS"* and which the record books as *"a new census shadow-census row — booked ONCE here, **extends S-1..S-8**"* (`:61`, `:146`). It extends the **block**, not S-1; S-1 is `KfPillTabs → SegmentedTabs` (`lane-frontend.md:264`), a different subject entirely. **Leaving this to a later wave is exactly what the anti-rename law forbids** — and a roster short by six claimants, one of them already renumbered into a *second* collision, is the precise mechanism of the collision this row exists to kill. | Mint every shadow row here under **FRESH non-colliding ids, in one motion**, each citing its originating record — **over the FULL claimant set at C-17.R, which is the row's roster of record**. **Claims are inputs to the mint; a claim is never a slot** (R-5): no wave other than KF.W0 mints or renumbers a census slot, and the mint is the ONLY census-slot authority. kf-LayerConfigPanel ruling 8 states it directly: the "consumed-but-mis-called" class needs *"a FRESH id (ruling 8 — the S-9 collision)"*; kf-SquareInstrument's S-9/S-10 are likewise **intake rows, not re-derivations of S-1..S-8**. Must land **inside** the census re-anchor, before any wave cites a shadow row by number. The `/number-field` **EVALUATION** is KF.W6's (ME-18 precedent); **W0 only fixes the identity.** **The kf-KeyboardShortcutsModal seat-ruled renumber is DEMOTED to a claim-input** (R-5): an **E-3 addendum under the original id** at `kf-KeyboardShortcutsModal.md` records *"the renumber is superseded by the KF.W0 C-17 single-motion mint; the slot id is whatever the mint assigns"* — the seat's own booked lesson (*"census extension ids must be allocated against the banked registry, not against the local corpus"*) is thereby honoured rather than repeated. **Sibling edge declared**: KF.W6's booking of that row carries *"census id pending the KF.W0 C-17 mint"*. | **G-0.6** |
| C-18 | **X-1** ⟨INTAKE-ADJUDICATION-2026-08-03, carried table row **KF.W0**; CENSUS `:297`/`:322`/`:330`⟩ | The entire Codex "Keyframes v8" contract lineage is pinned to `8281638c`. Every denominator (414 contracts, 152 surfaces, 73 568 Safari cells, 184 demo members) is measured on the tree §0 already ruled non-authoritative. | **Verbatim**: *"the §B-12 reconciliation (KF.W0) must also disposition the v8 contract — re-anchor it **or** declare it stale-by-substrate, never citable for a live count."* Plus X-2: *"KF.W0's re-count adopts 185-at-HEAD"* — **184's exact Codex/census agreement is the PROOF both censused the same stale worktree.** Seal's citable form carried as input (terminal disposition is KF.W10's): *"cite only as `345 exact / 12 partial / 57 unresolved @ 8281638c`, never 357/414, never as HEAD coverage."* The lineage's zero-authority self-declaration is accurate (X-6); its terminal-stop language is not load-bearing (X-7). **CONDITIONAL GATE**: the TCC re-reads **B10-9 · B19-10 (pin half) · B19-11 · B20-6 · B21-7** are *"owed only if the v8 contract is ever cited"* — this disposition decides whether they ever open. **Owner row OG-KF1** (B21-18: *"Does the Codex Keyframes B-lineage continue at all — and must any continuation re-anchor to `origin/master` `81a56990` before its denominators may be cited?"*) is **NAMED here, RULED at KF.W10** beside OD-V3/OD-V5, **never proxied**. Dissent recorded: **B18-26** split adoption — numerator INHERITED (zero real-Safari execution), denominator REJECTED (the 73 568-cell Kronecker self-cross-product has no product consumer); **B18-27** (slot 4-vs-5) UNRESOLVED and carried to KF.W10. | **G-0.8** |
| C-19 | **the dirty-manifest law** ⟨kf-SequencePlayhead, a priori⟩ + **the registry-sweep law** ⟨kf-KeyframesStringControls DISSENT 4⟩ — **32 records (count corrected this round, D-8: the cell's own enumeration named 24 distinct records, not 25, and eight further records bank their own firing)** | A manifest/tree claim filed **without naming its baseline**, then INHERITED as corroboration by a second and third seat. **At least the ninth firing**, twice at re-reader tier and twice at adjudicator tier. Enumerated and dated per firing: kf-AmigaScene (1st — *"a challenge lane's 're-run today' is worthless without naming the tree"*), kf-CubeScene (2nd), kf-ChannelOptions (first RE-READER-tier — both re-readers re-ran the probe against the dirty disk while naming a commit), kf-CopyButton, kf-SquareScene (5th — *"name the tree before you grade it"*), kf-StartingStyleTarget / kf-SpringHeatmap / kf-SpringTarget ("sixth", "sixth-plus"), kf-SpringScene ("sixth AND seventh, in one corpus"), kf-SquareInstrument, kf-SpringTrace ("the FOURTH re-booking event and the SIXTH one-baseline-short measurement"), kf-OrbitalDrag K15 (4th library-side, four corpora), kf-RibbonBar K10, kf-TimelineCaret ("a sixth one-baseline-short event for the fleet"), kf-SequenceAxis (kills 8, 9 — one per reader), kf-SequenceScene (three re-inheritance events, one per axis). **Eight further firings enumerated this round (D-8 — a ledger claiming "enumerated per firing" must actually enumerate; an under-counted denominator is un-closable)**: kf-MatrixEditor **K17** (*"RE-FIRED against both — SUBSTRATE-DEAD"*, `:34`; K28 records *"four banked kills each re-fired; five separate events"*); kf-SequenceTarget **K-1** (*"KILLED at every committed baseline"*, `:105`); kf-SharePopover **L·D-12/§5/§7** (*"SUBSTRATE-DEAD"*, `:75`); kf-SequenceScrubber **K-1** (*"ONE-BASELINE-SHORT — the sixth on record"*, `:80`); kf-LayerConfigPanel **K4/K5** (*"the axes' premise is false at local HEAD — ruled four times before this corpus filed it"*, `:88`); kf-CubeTarget **L·B-3** (α's kill SUSTAINED against a re-filed CONFIRMED BLOCKER, `:29`); kf-KeyframeCard **L-2** (a **reader-confirmation REVERSAL** — *"Reader-2 SUSTAINED; reader-1's cell REVERSED"*, `:24`, the rarer and sharper form); kf-EasingScene **KF-ES-15** (*"Fourth and fifth occurrences of frontier-blindness in the program (corrections register #5)"*, `:54`). **Sibling class**: banked rows re-filed as fresh BLOCKERs — kf-ChromeDock (*"BOTH re-readers filed this as a fresh confirmed BLOCKER; it was banked eleven days of registry ago"*), kf-MbabbMenu K-2, kf-EditorShell DISSENT 1 (*"RR-2 filed a banked identity under 'missed by all three axes'"*), kf-EditorHeader (*"THREE already-banked heavyweights the readers filed as fresh"*). | Book the ledger as a **durable record** and the derived laws **verbatim** where every later seat reads them: *"the failure is never derivation; it is an unread registry and an unnamed baseline"* ⟨kf-MbabbMenu⟩; *"EVERY re-reader row — confirmed, killed, or missed — runs through the bank before booking"* ⟨kf-EditorShell⟩; *"the M-25 agglomeration seat should treat 'checked the adjudicated registry' as a mandatory reader instrument"* ⟨kf-EditorHeader⟩; *"a 'missed' claim without a bank sweep is unadjudicable"* ⟨DISSENT 4⟩; the tree-motion law ⟨DISSENT 3⟩. **Binds every subsequent X·KF wave's reader/adjudicator dispatch** — the method half of the re-anchor law, travelling with C-2. Anti-inheritance events preserved beside the failures as fleet evidence, not tree evidence (ColorSpaceSelector DISSENT-1 precedent): kf-CubeAxisLines' reader-β re-deriving F-1's substrate from git; kf-AnimatedText's two readers converging independently on the `role="img"` implicit-role override, *"the anti-signature of citation-inheritance."* | **G-0.9** |
| C-20 | **kf-EditorShell DISSENT 3** — inter-bank MbabbMenu contradiction | kf-ChromeDock: MbabbMenu *"MOVED at the frontier (−146 lines)"*. kf-CubeScene: MbabbMenu *"byte-identical between the worktree and origin/master"*. kf-EditorShell's own probe shows only the load-bearing shape (the `@select.prevent` menuitem wrappers around SharePopover/DarkModeToggle) present verbatim at `origin/master`. | **Verbatim**: *"the two banked cells cannot both be right and the KF.W0 substrate settle should reconcile them."* Per **E-3** the reconciliation is an **APPENDED** correction at both banks' ids, never a rewrite of dated evidence. **It is the named test-case for the anchor-hygiene constraint** — if two adjudicated records disagree about one file's bytes, the wave has not re-anchored. | **G-0.9** |
| C-21 | **KF-TD-2** ⟨kf-TypingDots, reader-2 missed-2 · ruling 1 — the substrate **INVERSION**⟩ | For `TypingDots.vue` the **worktree is IDENTICAL to `origin/master` and local HEAD is the OUTLIER** (fold re-verified: diff vs origin/master empty; vs HEAD 1 file, 4+/9−). The direction of every other substrate ruling reverses for this file: *"the WAAPI-lane readings are the shipping truth, the rAF-lane readings describe stale HEAD."* | **Verbatim**: *"KF.W0 substrate settle; the registry corrections attach at the banks' ids."* Three corrections travel with it: it **corrects KF-EST-21's scoping**, **corrects EST ruling 7's direction for this file**, and **DISCHARGES KF-AT-28's re-anchor obligation** for it. **It proves the re-anchor pass must run per-FILE, not per-repo** — "stale HEAD, current frontier" is not uniform, and a blanket re-anchor would corrupt three banked rows. | **G-0.9** |
| C-22 | **The KF.W6 CARRY §CrossEdges packet** ⟨`docs/tranches/X/keyframes/carry/KF-W6-CARRY.md:354`, `## §CrossEdges (declared from this end)` → **KF.W0 · Substrate Settle**⟩ — adopted this round under **R-19b** (cures **D-6**) | An edge **declared from KF.W6's end and unanswered from this one**. Three corrections that seat measured, all of them G-0.9 appended-correction business, none of them present in this file by bytes before this round (`grep -c` → `App.vue:176` 0 · `TABS_EXTERNALLY` 0 · `dock-margin` 0 · `KF-SKEL` 0). Two of the three are *byte-level* corrections to banked citations — **precisely the class C-20 names as this wave's test case**: if two authorities disagree about one file's bytes, the wave has not re-anchored. | Book all three **by id** as G-0.9 appended-correction obligations (E-3: appended at the banks, never a rewrite): (i) the `TABS_EXTERNALLY_MANAGED_KEY` provide site is **`App.vue:176`**, not the banked stale-HEAD **`:169`** — a one-line stale-offset correction of exactly the substrate class C-2 governs; (ii) **`--dock-margin` has 9 live unfallbacked sites + 1 prose mention**, so any census cell quoting a smaller figure is corrected by appended note; (iii) **KF-SKEL-7** — *"the §B-12 reconciliation adopts a cascade-level coupling arm beside the import census"* (`kf-App.skeleton.md:42`), whose instrument critique **survives the death of the phantom-dependency premise** on its own witness (a file with zero imports and zero `@import` whose 4-of-5 custom-property reads resolve only through glass-ui's cascade). KF-SKEL-7 is the **named counterpart of C-9's legend rider** and rides C-1.F row F-13; its token-namespace probe half is KF.W6's, not this wave's. **The answer is returned to the declaring end**: this file now carries the edge, so the seam closes at both ends. | **G-0.9** |

### C-1.F — the fold-identity table (R-1a · 12 adopted escapes + 1 riding from R-19b)

C-1's mechanism is a fold *by reference*: the record terminates its row at census F-1/SCH-1 and the cure is stated once, here. That mechanism was carried; the **ids and their travel-riders were not**, so no later seat could grep this spec for its own row and no count of the F-1 blast surface was complete. **Partition law (R-1a)**: each id below lands in exactly ONE wave — this one — and no other wave may book it except as an exclusion-with-named-owner pointing here. Riders are quoted from the bank **verbatim, carried not cited** (M-25: a sequencing rider dropped in transit is a silent drop).

| # | Banked id ⟨record : line⟩ | Rider carried verbatim — travels with the fold |
|---|---|---|
| **F-1** | **D-5 / L-3 / C-2** ⟨kf-DemoGlobalChrome:43⟩ | *"MAJOR · FOLD → census S-9 (identity guard; the swap verdict is census-owned)"* — the glass `./toast` family. Booked here **with the identity guard only**; the **swap verdict is not this wave's**, and the row enters the mint as an S-9 claim-input at C-17.R. The record's own three riders travel with it: (i) sequencing on census **F-1/KF.W1-DEP**; (ii) *"the swap retires M-L1's leak, C-4's guard module, and D-2/D-3/D-7/M-3/D-11/D-13/D-14 in one motion"*; (iii) *"the S-9 row contradicts `lane-frontend.md:398` ('Bespoke, no glass counterpart') — the census tally row must be corrected when S-9 is booked."* |
| **F-2** | **KF-ET-3** ⟨kf-EasingTarget:42⟩ | *"This component's exposure facts (4 specifiers here + 3 in the sidebar; sole consumer of `/fading-scroll` + `/toggle-group`) ride as the fold's rider."* |
| **F-3** | **KF-ES-33** ⟨kf-EasingSidebar:77⟩ | *"→ fold to SCH-1 → KF.W0; rider: this component's 3 subpath edges."* |
| **F-4** | **KF-ES-15** ⟨kf-EasingScene:54⟩ | *"This scene's exposure census (7 entry points across the closure) rides as the fold's rider."* (The same line books this record's C-19 firing — *"Fourth and fifth occurrences of frontier-blindness"*.) |
| **F-5** | **KF-KE-16** ⟨kf-KeyframesEditor:58⟩ | *"the subpath-import blast radius (`/dark`, `/forms`) is real until the fork reconciles."* |
| **F-6** | **KF-TFP-29** ⟨kf-TimingFunctionPanel:75⟩ | *"The panel-local rider: the 4.0.1-era prose (`:25-31`) is unpinned-rationale drift = the **KF-CO-17** row, not a dependency defect."* — a routing rider: it keeps a prose-drift row out of the dependency class. |
| **F-7** | **KF-AT-7**, declaration leg ⟨kf-AnimatedText:42⟩ | *"Declaration leg → KF.W0 (census F-1/SCH-1 §B-12 reconciliation, fold-by-reference, **three-way-drift evidence attached**)."* The **cascade-invisibility leg is KF.W4's** (instrument law) and is not booked here — the row splits by leg, and only the declaration leg is this wave's. |
| **F-8** | **L·B-3 / C·B-2**, the **discharge half** ⟨kf-ChannelOptions:31,:55,:149⟩ | The record's KF.W0 packet (`:149`) is *"the **L·B-3 discharge** (≡ census F-1, SCH-1) + the three-file disk↔master reconciliation … + the re-anchor law over every anchor in this record."* **C-4 carries only the second limb**; this row books the first, so the `:149` packet is whole. The component rider stays at C-1 (four subpath edges, `:143-146`/`TimingFunctionPanel:26-31`, *"the unpinned-drift half is live and is what KF-CO-1/2/18 measure"*). |
| **F-9** | **L/B-1** ⟨kf-ChromeDock:40⟩ | *"FOLD → census SCH-1/F-1 (KF.W0 substrate settle), per kf-App ruling 1."* Frontier-CURED; *"Both re-readers booked it BLOCKER without the substrate triage; carried corrected"* — which is why the id must exist here to be grepped. The densest-consumer rider is already at C-1. |
| **F-10** | **F-1 row C-11 / L-I1** ⟨kf-CSSPasteDialog:72⟩ | *"census row `lane-frontend.md:15` **RED**, already ruled 'F-1 first' (:612). **All six of this file's component imports ride it.** Fold by reference → the census's own F-1 row (KF.W0/W6 precondition). Not re-booked here."* **Sibling correction (R-1a)**: KF.W6 does **not** adopt this row — its W6-precondition arm is the fold's, not a W6 row. |
| **F-11** | **F-1 row L-5 / C-11** ⟨kf-KeyframesAddDialog:87⟩ | *"census row RED, ruled 'F-1 first.' **All six glass imports ride it**; L-5's MAJOR arm collapses to MINOR-on-F-1."* Same sibling correction as F-10: KF.W6 does not adopt it. |
| **F-12** | **L-9 = C-D-7 = C-D-9** ⟨kf-KeyframeCardList:120⟩ | *"FOLD by reference; **identity owned by lane-frontend §10 step 1**, exactly as kf-CSSCodeEditor's KF-CE-32 folded it. Never re-booked."* Rider: *"the barrel-vs-subpath inconsistency inside the 2-file pair is a named instance of lane-frontend §3.1 — extends that census row, no new booking; the subpath swap rides the F-1 declaration."* The KF-CE-32 precedent it invokes is **declared, not assumed** — see C-1.R below. |
| **F-13** | **KF-SKEL-7** ⟨kf-App.skeleton:42⟩ — **rides here from R-19b/C-22** | *"KF.W0 (re-anchor/re-count law: the §B-12 reconciliation adopts a cascade-level coupling arm beside the import census) + KF.W6 (the token-namespace audit is the natural home of the probe). F-1 itself: fold by reference to SCH-1, **never re-booked**."* Booked in `KF-W6-CARRY.md:90` by id; absent here until this round. Its witness is the census instrument's own falsifier and it is C-9's named counterpart. |

**Count discipline.** Thirteen ids, thirteen riders. C-1's blast-surface list is these thirteen plus the six record-level riders already inside C-1 — that union, not the six alone, is what C-9's cascade column must precede.

### C-1.G — the F-1 fold id index (13 ids whose CONTENT C-1 carries and whose ID did not)

These thirteen rows are **already cured by C-1's mechanism** — each record terminates its row at census F-1/SCH-1 and the cure is stated once at C-1. What was missing is the **id**: under an id-keyed census, a row whose content is carried but whose id is absent by bytes is an escape, because **no later seat can grep this spec for its own row** and no closure check can distinguish "folded" from "dropped" (KF-W0-CHECK §2.C, E-19..E-31).

**This table mints nothing and re-routes nothing.** Every id below keeps its banked spelling (anti-rename), the disposition of every one is *"folded at C-1, cured by the §B-12 act"*, and the leading number is an **index, not an id** — it is never citable as a row name. Where a row has a limb that is NOT the fold's, that limb is named and stays where it is booked.

| # | Banked id ⟨record : lines⟩ | Banked routing words | Limb not covered by the fold |
|---|---|---|---|
| 1 | **K17** ⟨kf-MatrixEditor:34,:128,:170⟩ | *"K17 ≡ census F-1/SCH-1 → KF.W0 §B-12"* | K17 is **also** a C-19 firing (*"RE-FIRED against both — SUBSTRATE-DEAD"*), enumerated there; the K1/FAM-03 MR4 `--project demo` CI rider is **KF.W4's** (§Sequencing) |
| 2 | **OD-41** ⟨kf-OrbitalDrag:94,:124⟩ | *"NO-WAVE-OWNER hygiene; I-3's residue ≡ census F-1 → KF.W0 §B-12"* | the NO-WAVE-OWNER hygiene half travels with its packet, not with the settle |
| 3 | **C-14** ⟨kf-PlaybackRibbon:141,:152⟩ | *"C-14 ≡ census F-1 → KF.W0 §B-12"* | the ribbon's own rows ride the **transport/ribbon** packet (R-15, KF.W13) |
| 4 | **L-2** ⟨kf-KeyframeCard:24,:103⟩ | *"glass-ui dependency family → census SCH-1 / kf-App ruling 1 → KF.W0"* | L-2 is **also** a C-19 firing — the reader-confirmation **REVERSAL** — enumerated there |
| 5 | **K-1 / L·D-2** ⟨kf-SequenceScrubber:71,:80⟩ | *"KF.W0 fold"* | **L·D-2's surviving refinement is BOOKED at C-9** (D-7), not here; its phantom-dependency arm died at K-1 |
| 6 | **K-1 / row 1** ⟨kf-SequenceTarget:21,:105,:154⟩ | *"the K-1 fold (census F-1/SCH-1, §B-12)"* | ST-1's affordance edit is the **sequence packet's** (KF.W11), never the settle's |
| 7 | **K-1** ⟨kf-SpringHeatmap:80,:117⟩ | *"→ KF.W0; sixth-plus baseline event"* | C-M-1's S-9 `./canvas` claim is a **mint input at C-17.R**, a separate limb |
| 8 | **K-1** ⟨kf-SpringScene:95,:149⟩ | *"KF.W0 §B-12: the K-1 substrate reconciliation"* | the observed-live `emphasis` migration is carried as C-2 inventory |
| 9 | **B-1 / K-1 / row 11** ⟨kf-SpringTarget:29,:87,:132⟩ | *"B-1 ≡ census F-1 ≡ SCH-1 ≡ kf-CubeScene M-1 residue → KF.W0 §B-12"* | the AT/announcement arm (D-5 aria-hidden half · D-6 · **D-14+N-4, one binding, never split**) is **KF.W9's** (R-1e) |
| 10 | **K-1 / row 7** ⟨kf-StartingStyleTarget:27,:89,:130⟩ | *"K-1 ≡ census SCH-1 ≡ F-1 → KF.W0 §B-12"* | KF-EST-21's scoping + EST ruling 7's direction are **corrected by C-21**, per-file |
| 11 | **L-20/C-7** ⟨kf-SquareScene:108,:119⟩ | *"residue → KF.W0 §B-12"* | the comment-stated-invariants **test obligation** is NO-WAVE-OWNER (§Sequencing); **C S-C**'s parser posture is **KF.W2's F0** (R-1b), never re-probed here |
| 12 | **K4/K5** ⟨kf-LayerConfigPanel:88,:97,:125⟩ | *"K4/K5's manifest schism ≡ census F-1/SCH-1 → KF.W0 §B-12"* | ruling 8's *"consumed-but-mis-called"* class is a **mint input at C-17.R** |
| 13 | **i-1 / F-1 / row 7** ⟨kf-RibbonBar:26,:60,:128⟩ | *"KF.W0 §B-12: the phantom-dep substrate schism (K10 …)"* | K10's 18-subpath/44-site rider is already inside C-1's blast surface |

**Why an index and not thirteen more cures.** C-1.F adopts rows that were **not** carried — ids *and* riders both absent, riders that M-25 forbids dropping. C-1.G is the opposite case: the cure was never missing, only the handle. Booking them as fresh cures would re-book once-booked rows — the exact tenth-re-booking defect §Excluded convicts. Indexing them makes the census **closable**: 13 ids that a closure sweep can now resolve to a named disposition instead of to silence.

### C-1.R — declared re-routes and corrections (loud, per E-3)

A routing this spec states differently from its bank is a **ruling**, and a ruling that is not declared is a silent rewrite. Both live here.

| Subject | The bank's words | This spec's reading | Disposition |
|---|---|---|---|
| **KF-CE-32** (cures **D-10**) | `kf-CSSCodeEditor.md:68` — *"**KF-CE-32 · L-i4 = C-16 — FOLD, not booked here** … identity owned by **lane-frontend §10 step 1** … → **FOLD → census F-1 (KF.W6-adjacent sequencing)**."* | The §Sequencing KF.W12 edge calls KF-CE-32 *"census F-1's owning id, discharged at KF.W0 §B-12"* — i.e. an owning id, and W0's. | **DECLARED RULING, not a silent change.** The two readings are reconcilable and the reconciliation is stated: **identity ownership stays with lane-frontend §10 step 1** (the bank's, untouched); what W0 discharges is the **F-1 substrate leg** that KF-CE-32 folds into, and the *"KF.W6-adjacent sequencing"* clause is preserved as a sequencing rider on the prose recut, which is KF.W6's. An **E-3 addendum under the original id** records the reading at the bank so a later seat reads the ruling and not a contradiction. kf-KeyframeCardList F-12 invokes this precedent by name, so the precedent must be legible. |
| **The `build:gh-pages` command** (cures **D-2**) | Five banked/derived cells say *"a fresh `build:gh-pages`"*. | No such script exists at HEAD, `origin/master` or the worktree. | **The banked words are carried verbatim** (E-3 — dated evidence is never rewritten); **the act is corrected** to `npm run gh-pages` at all seven execution sites (R-11). |
| **`kf-AnimationVisualizer.md:9`** | The census cell as banked. | Corrected to **worktree-scope** by the kf-SequenceScrubber K-1 kill. | Appended correction under G-0.9; carried at C-9. |
| **MISS-α5 ≡ RR-β K-BASE** ⟨kf-TimelineCaret:45⟩ — the attached correction (KF-W0-CHECK §2.D **E-32**) | The row *"also **CORRECTS the banked kf-KeyframeTimeline header's 'working tree = the audited tree' equation**"*. | The row's substrate CONTENT — hybrid tree, merge-base `a59d3a22`, untracked-but-cited-by-line — is carried at C-2 / G-0.1. The **attached correction** was not: `grep -c 'KeyframeTimeline'` in this file before this round → **1**, and that one hit is C-10's PRM-roster cell, not a correction obligation. | **BOOKED as a G-0.9 appended-correction obligation under the banked id** (E-3: an addendum at `kf-KeyframeTimeline`'s header; dated evidence never rewritten). The equation *"working tree = the audited tree"* is **false at every coordinate this wave measures** — 41 behind / 1 ahead, neither ref an ancestor of the other — so a record whose header asserts it cannot state which ref it cures against, which is G-0.9's whole content. Same class as C-22's two byte-level corrections and the named C-20 test case. |

### C-17.R — the census-slot claimant roster (R-5 · the mint's complete input set)

**The mint is the only slot authority; every row below is an INPUT, not a slot.** Fresh non-colliding ids are assigned in **ONE motion**, each citing its originating record. A dead or refused claimant is **enumerated, not minted** — enumeration is what stops the next seat re-raising it.

| Claim | Banked id ⟨record : line⟩ | Subject | Mint disposition |
|---|---|---|---|
| S-9 | **C-M-1** ⟨kf-SpringHeatmap:46,:128⟩ | glass `./canvas` (present at installed 7.0.0, 73 exports) | MINT |
| S-9 | **F1(c)** ⟨kf-EditorHeader:117⟩ | `EditorHeader → HeaderRibbon` — DEAD, replace-by-deletion | MINT (the slot kf-SharePopover was refused against) |
| S-9 | **L-5/C-6 + ruling 8** ⟨kf-CubeScene; kf-LayerConfigPanel⟩ | the number-field row; *"the 'consumed-but-mis-called' class needs a FRESH id (ruling 8 — the S-9 collision)"* | MINT (the `/number-field` **evaluation** is KF.W6's) |
| S-9 | **C-6** ⟨kf-AmigaScene:61,:146⟩ | `Surface`/`PaperBackdrop`/`Card` shadowed by 8 lines of bespoke CSS — *"extends S-1..S-8"* (**the block**, not S-1 — D-9) | MINT |
| S-9 | **KF-AV-28 · D-18 = C-13** ⟨kf-AnimationVisualizer:72⟩ | *"the shadow census skipped the larger of the two rail/ball shadows … ScrubberTimeline (256 L vs the censused 162-L SequenceScrubber)"* | MINT — **and this id is KF.W7's defining supersession rider**, so the minted slot carries the rider by name: KF.W7's S-9 evaluation may supersede a behavioural cure on a governed row, and the row's own verdict (*"evaluate, not swap"*) is KF.W7's intake, never W0's |
| S-9 | **R-5** ⟨kf-CSSPasteDialog:43⟩ | *"bespoke shadow of shipped `Textarea`, census-invisible"*, sized at 1 site; *"census row 80 classified `G` on import-list evidence alone"* | MINT (the suffusion cure is KF.W6's) |
| S-9 | **D-5 / L-3 / C-2** ⟨kf-DemoGlobalChrome:43⟩ | the `./toast` family (= C-1.F row F-1) | MINT with the identity guard; **swap verdict census-owned, not W0's** |
| S-9 | **K-6 / D-§S-9 = C-S-B** ⟨kf-SpringTrace:89⟩ | *"S-9 · SpringTrace — JUSTIFIED BESPOKE"* — **KILLED AS STATED** (`generateCurveSVGPath` *"was one `ls` away from a directory both axes cited"*) | **ENUMERATED AS A DEAD OCCUPANT, NOT MINTED.** The row *"must be re-derived (0..1-range caveat included) before any census landing"* — that re-derivation is not this wave's |
| S-9 | **C·C-2's census amendment** ⟨kf-SharePopover:90⟩ | *"ALREADY CLOSED, TWICE … the S-9 slot is spoken for by an adjudicated booking (kf-EditorHeader.md:117). **Refused per anti-rename**"* | **ENUMERATED AS REFUSED, NOT MINTED** — the refusal is itself the anti-rename law working, and it is recorded so it is not re-litigated |
| S-10 | **C-9** ⟨kf-KeyboardShortcutsModal:29,:68⟩ | the `/command` family shadow | **CLAIM-INPUT ONLY (R-5).** The seat's own *"COLLISION — renumber to S-10"* ruling is **superseded by this mint**; an E-3 addendum under the original id records it. The seat's booked lesson stands verbatim: *"census extension ids must be allocated against the banked registry, not against the local corpus."* |
| S-10 | **KF-KE-21** ⟨kf-KeyframesEditor:63⟩ | the progress bar as a shadow of glass `./progress` | MINT (the `Progress` evaluation is KF.W6's; binding it to something true is KFED-UNIT's) |
| S-10 | **C-12** ⟨kf-SquareInstrument:78⟩ | the status pill vs glass-ui `Chip`, *"correctly evaluate"*; *"NO-WAVE-OWNER (census S-10 intake; the shape completion is a design-idioms.css edit)"* | MINT — an **intake row, not a re-derivation of S-1..S-8** |
| S-10 | the fifth record's original request | as banked | MINT |

**Why the roster, not the number.** Three records independently claimed "S-10" and one of them arrived there by a seat renumbering off a *local* corpus. A mint over four-plus-one would have re-collided on its first motion; a mint over the enumerated thirteen cannot, because every claimant — including the two that receive no slot — is visible to the one motion that assigns.

### Fold sharpenings (verified at this authoring, 2026-08-28 — booked, not re-booked)

| # | Finding | Consequence |
|---|---|---|
| **N-1** | The reconciliation surface has two denominators. `git status --short \| wc -l` → **252** is the *disk↔HEAD* count; `git diff --name-only origin/master \| wc -l` → **325** is the *disk↔frontier* count (124 of the rows untracked: `git ls-files --others --exclude-standard \| wc -l` → **124**). A record quoting "~252 dirty rows" for the §B-12 act's scope is quoting the wrong denominator — the act moves the tree to the frontier, so the frontier diff is the surface. | G-0.1's GREEN condition disposes the full surface with **both figures stated with their commands**; a disposition record covering only the 252 status rows leaves 73 files of the diff-vs-frontier surface unexamined. |
| **N-2** | The `backward` module's banked "diverges from BOTH refs" figures ⟨kf-KeyframesStringControls: 399/143 · 154/114⟩ **do not reproduce**. The module was **MOVED upstream**: HEAD/disk carry `src/animation/compile/emit/{backward,backward-walk,backward-color}.ts`; `origin/master` carries `emit/backward/{backward,walk,color,index}.ts`. Fold-measured: disk `backward.ts` **393** L ≡ frontier `backward/backward.ts` **393** L; HEAD **410**; the diff of the two 393-line files is **import-path-depth hunks only**. **The disk is the frontier's content at HEAD's layout** — content-forward, path-stale. | Corrects a banked figure by **appended** note (E-3). Names a real reconciliation hazard: a partial `git checkout origin/master -- src/` would leave the flat paths as **orphaned duplicates**; only the full `reset --hard` removes them. And it recharacterizes the 87 dirty src rows as largely a **landed-upstream refactor**, not novel local work — strengthening C-1's FAM-01 RAIL rider and further weakening the axes' "nobody noticed" story. |
| **N-3** | EE-03's frontier cure located exactly: `origin/master:demo/components/instrument/keyframes/composables/useKeyframesParsing.ts:97` = `() => animation.templateFrames.length,`. **The cure line is absent at HEAD** — but a naive `git grep 'templateFrames.length' HEAD -- demo/` is NOT a valid witness: it returns **2 unrelated guard sites** (`KeyframesEditor.vue:204`, `useKeyframeOps.ts:179`). The oracle is the cure line at its file:line, or the watch-warn count — not the bare grep. | G-0.4's EE-03 leg gets a file:line witness with the false-positive named, so no later seat "greens" it on the wrong grep. |
| **N-4** | KF-TD-2's inversion re-verified at fold: `git diff --stat origin/master -- demo/components/instrument/shell/TypingDots.vue` → **empty**; `git diff --stat HEAD -- <same>` → **1 file, 4 insertions(+), 9 deletions(−)**. | C-21's per-file law is not an inference from one record; it is a reproducible measurement, and G-0.9's falsifier names it. |

## Gates

All ten **RED at authoring**. Every baseline below was re-measured read-only by the fold seat on **2026-08-28** in `/Users/mkbabb/Programming/keyframes.js` (and `/Users/mkbabb/Programming/glass-ui` where named); commands and outputs are pasted, none inferred. Each gate states how it fails **for its intended reason** (L-19). No proof script is authored for any gate — every one is a `git`/`grep`/`node -p` one-liner or a named live witness.

**G-0.1 — SUBSTRATE SETTLED.** The sacred checkout reconciled by the owner's hand.
*RED baseline* (fold-measured 2026-08-28, reproducing the CARRY seat's 2026-08-25 run exactly): `git rev-list --count HEAD..origin/master` → **41** · `git rev-list --count origin/master..HEAD` → **1** · `git status --short | wc -l` → **252** · `git diff --name-only origin/master | wc -l` → **325** · `git ls-files --others --exclude-standard | wc -l` → **124** · `git merge-base HEAD origin/master` → `a59d3a22da080a8ed224e8d675112bb3bb0135b0`; HEAD `8281638c…`, origin/master `81a56990…`; neither is an ancestor of the other.
*GREEN*: 0 behind; the 1-ahead commit and **every row of the reconciliation surface** explicitly dispositioned (kept / committed / discarded) in a written record that states **both denominators with their commands** (252 status-vs-HEAD · 325 diff-vs-frontier, N-1); the reconciliation output **diffed, not assumed** (C-15 — `position="right"` must not be carried forward under a different spelling).
*Falsifier*: **§B-12 is the OWNER'S act — a wave that performs it itself fails this gate**, green counts notwithstanding ⟨lane-docs.md:380⟩. A record that disposes only the 252 status rows fails: 73 files of the frontier-diff surface are not in that set.
⟨carries C-2, C-15, N-1⟩

**G-0.2 — MANIFEST SINGLE-STATE.** One glass-ui declaration across all four coordinates.
*RED baseline*, four disagreeing states, fold-measured:
```
git show HEAD:package.json           → :71  "@mkbabb/glass-ui": "6.0.0" under optionalDependencies   (name @mkbabb/keyframes.js, version 5.3.5)
git show origin/master:package.json  → :77  "@mkbabb/glass-ui": "7.0.0" under devDependencies        (version 6.0.0; :70 "@mkbabb/value.js": "4.0.0")
grep -c glass-ui package.json        → 0        grep -c glass-ui package-lock.json → 0     (the worktree DELETES the row from both)
git show HEAD:package-lock.json | grep -c glass-ui           → 3
git show origin/master:package-lock.json | grep -c glass-ui  → 3
node -p "require('./node_modules/@mkbabb/glass-ui/package.json').version" → 7.0.0
cat .npmrc → legacy-peer-deps=true          ls node_modules/@mkbabb/ → glass-ui  parse-that  value.js   (no pencil-boil)
installed peers: {"@mkbabb/keyframes.js":"^6.0.0","@mkbabb/pencil-boil":"^0.9.2","@mkbabb/value.js":"^4.0.0","embla-carousel-vue":"^8.0", …}
```
*GREEN*: HEAD == origin/master == worktree == installed at `"@mkbabb/glass-ui": "7.0.0"` **exact devDep** + lock, **AND** a written ruling on `legacy-peer-deps=true` versus the two genuinely-absent peers (`pencil-boil`, `embla-carousel-vue`).
*Falsifier*: committing the worktree deletion as-is **revives census F-1** and this gate reads three states again. A settle that leaves `.npmrc` unruled passes a naive "one version" check and fails here — silence is not a disposition.
⟨carries C-1, C-16; KF-HA-6 discharged, not blocking⟩

**G-0.3 — EE-02 CSS-TWIN NOT REGRESSED.** The lagging disk files reconciled disk←master.
*RED baseline* (fold-measured): `git diff origin/master --stat -- <the four paths>` →
```
 .../channel-controls/TimingFunctionPanel.vue       |  9 +---
 .../composables/useTimingFunctionEditor.ts         | 19 ++------
 package-lock.json                                  | 50 ----------------------
 package.json                                       |  4 +-
 4 files changed, 6 insertions(+), 76 deletions(-)
```
The disk carries `{ fn }` where master carries `{ fn, css }` + `timingFunctionLiteralFor`.
*GREEN*: **empty diff** for the two `.ts`/`.vue` paths — the EE-02 twin present at BOTH write sites (`setAnimationTimingFunction` and `TimingFunctionPanel.vue:144`).
*Falsifier*: reconcile master←disk and the diff also goes empty while the landed cure is destroyed — which is why the gate names the **direction** and not just the emptiness. Both consequences (WAAPI demotion, export-serializer identity-miss) then reappear on a tree that reports clean.
⟨carries C-4⟩

**G-0.4 — RE-REGRESSION GUARD.** EE-01 · FE-3 · EE-03 re-run GREEN on the reconciled tree.
*RED baseline*, three oracles, each green at the frontier and **red at the audited disk today** — which is what makes them oracles (all fold-verified):
```
EE-01  git show HEAD:demo/components/CopyButton.vue         | sed -n 42p → timingFunction: "bounceInEase",
       git show origin/master:demo/components/CopyButton.vue | sed -n 42p → timingFunction: "easeInBounce",
       git status --short -- demo/components/CopyButton.vue → (empty: disk == HEAD == the dead name)
FE-3   demo/components/instrument/keyframes/components/KeyframeCardList.vue:11
         HEAD (== disk)  :frame-start="frames[i].start.toString()"
         origin/master   :frame-start="startScalar(frames[i].start)"     (commit add20b7e)
EE-03  origin/master:demo/components/instrument/keyframes/composables/useKeyframesParsing.ts:97 → () => animation.templateFrames.length,
       the cure line ABSENT at HEAD (N-3 — the bare `templateFrames.length` grep is NOT the witness: 2 unrelated guard sites hit)
```
*GREEN* — **witnesses re-founded this round (D-4). Every oracle now names an instrument that exists at a coordinate, or is declared as another tranche's gate and not asserted here.** The gate closes on the **static tier**, which is wholly addressable; the runtime tier is confirmation, named by literal path.

**Static tier (the gate's closing condition, three file:line assertions on the settled tree):**
```
EE-01  sed -n 42p demo/components/CopyButton.vue                                   → timingFunction: "easeInBounce",
FE-3   sed -n 11p demo/components/instrument/keyframes/components/KeyframeCardList.vue
                                                                                    → :frame-start="startScalar(frames[i].start)"
EE-03  sed -n 97p demo/components/instrument/keyframes/composables/useKeyframesParsing.ts
                                                                                    → () => animation.templateFrames.length,
```
plus the negative form for each (`git grep -n 'bounceInEase' -- demo/` → 0 · `git grep -n '\.start\.toString()' -- demo/` → 0), which is what actually proves the rebase did not re-land the dead name, the `.toString()`, or the array watch. **These are RED today and green only after the settle** — the same three probes the RED baseline above pastes, re-run against the reconciled tree, so the oracle and its baseline are the same instrument read twice.

**Runtime tier (confirmation; named by the paths that exist at `origin/master`, verified this round via `git ls-tree -r --name-only origin/master scripts/`):** `node scripts/observe/demo/smoke.mjs` and `node scripts/observe/demo/live-session.mjs` (both real; `scripts/lib/console-budget.mjs` is the warn-count instrument they load) — EE-01's pageerror stream on `/#/easing` and `/#/spring`, and EE-03's watch-warn-count→0. **No npm script wraps either**; they are invoked by path, and §Cadence's carve-out covers exactly these three named oracles.

**FE-3's `/\[object Object\]/` smoke assertion is NOT an instrument in this tree and is not asserted here.** Receipt: `git ls-tree --name-only origin/master` has **no `e2e/`** (`.changeset .claude … bench demo docs … scripts src test`), and `git grep -n 'object Object' origin/master -- e2e/ test/` → **zero hits**. The assertion exists as a *planned born-RED gate of keyframes.js's own tranche V*, W1 · Render Truth (`origin/master:docs/tranches/V/V.md:73` — *"pageerror==0 + no `[object Object]`, 7 routes × 2 viewports (born RED — live defects)"*). It is therefore **declared as an outbound cross-edge to that tranche** (see §Sequencing) and never authored here: §Bounds forbids this wave writing `test/**`, and a substrate wave that invents a test file to green its own gate is the vacuous-evidence defect L-19 exists to kill.
*Falsifier*: a careless rebase of the in-flight migration re-lands any one of the three and the corresponding static probe reds; a wave that runs these gates **before** the migration lands measures the frontier and proves nothing. **A gate leg closed on an oracle that names no coordinate is the same failure one level up** — the reason this leg was re-founded. **FE-3's PARTIAL-cure residue is explicitly NOT this gate's** (fraction-for-percent, named-selector fallthrough, `selectorText` uncalled) — KFED-UNIT's, after KF.W4.
⟨carries C-5, C-6, C-7, N-3⟩

**G-0.5 — HEADER-RIBBON TRIPWIRE (negative gate — RED means unsafe to advance).**
*RED baseline*, fold-measured across both repositories:
```
git -C /Users/mkbabb/Programming/glass-ui ls-tree --name-only HEAD src/components/header-ribbon/   → (empty)
git -C … ls-tree --name-only '4bf53962^' src/components/header-ribbon/ → HeaderRibbon.vue  README.md  index.ts  styles.css  types.ts
grep -c header-ribbon /Users/mkbabb/Programming/glass-ui/package.json → 0
git show origin/master:demo/components/instrument/shell/EditorShell.vue | grep -n header-ribbon
     → 116:import { HeaderRibbon } from "@mkbabb/glass-ui/header-ribbon";
ls node_modules/@mkbabb/glass-ui/dist/header-ribbon.js → present (installed 7.0.0)
```
*GREEN*: **either** the installed producer stays at-or-below `4bf53962` (i.e. 7.0.0, today's state), **or** `origin/master:EditorShell.vue:116` no longer imports the deleted subpath.
*Falsifier*: the gate fires the moment a settle advances glass-ui past `4bf53962` with `:116` intact — the build dies at `:116`. **This gate BOUNDS G-0.2's cure rather than following it**: a "declare the newest glass-ui and regenerate the lock" remediation passes G-0.2 and destroys the build.
⟨carries C-3⟩

**G-0.6 — COUNTS RE-RUN.** SCH-1..7 + X-2 each dated with its command and literal output.
*RED baseline*, census claims vs the fold seat's re-measurement at `origin/master`:
```
SCH-3  claim 145 src .ts / 22 636 LOC
       git ls-tree -r --name-only origin/master src/  | grep -c '\.ts$'      → 153
SCH-4  claim 61 value.js `from`-lines (/value 15)  vs 62 (16)                 → MEASURE-AT-OPEN
SCH-5  4+ Tier-A anchors DEAD (value-ast.ts:71 → compile/value/compile.ts:32; easing-registry.ts →
       compile/easing/registry.ts; emit/backward/backward.ts; emit/easing-serialize.ts:71-73)      → re-anchor, see N-2
SCH-6  git ls-tree -r --name-only origin/master demo/ | grep -c '\.vue$'      → 58   (holds; contents drifted)
SCH-7  depcruise L-1/L-2 unverified at HEAD                                   → restated, not resolved
X-2    claim 184 demo .ts/.vue (STALE-WORKTREE figure)
       git ls-tree -r --name-only origin/master demo/ | grep -cE '\.(ts|vue)$' → 185
```
*GREEN*: every SCH row and X-2 carries a dated, re-derived figure with its command pasted; the cascade-coupled column exists beside the import census; §6.5 and the z-index cells are re-scoped **in one motion**; **every claimant on the C-17.R roster is dispositioned in ONE motion** — minted under a fresh non-colliding id citing its originating record, or enumerated as a dead/refused occupant with its reason (the S-block lives in `lane-frontend.md`, `^### S-` = 8 today).
*Second falsifier (R-5)*: **a mint run over four-plus-one rather than the enumerated thirteen re-collides on its first motion** — `kf-KeyboardShortcutsModal:29`'s renumber already landed a second "S-10" on top of `kf-KeyframesEditor:63`'s and `kf-SquareInstrument:78`'s, so a short roster is not an incomplete cure but the exact mechanism of the defect. A minted slot that does not cite its originating record fails too: the citation is what makes the next collision detectable.
*Falsifier*: a figure restated without its command is indistinguishable from the stale one it replaces — **X-2's 184/185 is the proof**: the stale figure and Codex's agreed *exactly*, which is how both were caught. **Explicitly NOT green-by-basis-reconciliation**: X-4 (80 / 139 / 153 / "159") is **KF.W5's** row.
⟨carries C-8, C-9, C-10, C-11, C-17⟩

**G-0.7 — GATE-INVENTORY TRUTH.** No prose cites a gate that does not exist.
*RED baseline*, fold-measured at `origin/master`:
```
git show origin/master:package.json → :50 "proof:structure":    node scripts/gates/structure/index.mjs
                                      :51 "proof:publish":      node scripts/gates/surface/index.mjs
                                      :52 "proof:owner-golden": node scripts/gates/visual/index.mjs
                                      :37 "check": tsc --noEmit && tsc --noEmit -p tsconfig.test.json && npm run proof:structure   ← NO vue-tsc
                                      :44 "lint":  depcruise src
git ls-tree --name-only origin/master scripts/gates/ → structure  surface  visual        (three DIRECTORIES)
git ls-tree -r --name-only origin/master scripts/gates/ → 9 FILES (re-measured this round, R-17/R-8 item 5):
     structure/index.mjs · surface/{index,agent-surface,boundary,consume-bundle,published-surface,readme-runs,verify-diff}.mjs · visual/index.mjs
```
**The roster's denominator is 3 npm `proof:*` entry points over 9 gate files** — the "surface, visual" two-script figure is a stale-worktree reading, decided against by `kf-SequenceScrubber.md:119` (*"`proof:structure` present in origin/master's `check`, deleted in the worktree edit"*) and corroborated by kf-TypingDots K8. **The frontier roster is the denominator** (R-17); KF.W4's OP-3 is RESOLVED on it, and this wave owns the roster it resolves to.

Against that, **three banked-and-fold dead gate names across 10 demo prose sites** (a fourth, `proof:demo-no-oversize`, arrives with the inbound residue families below — **four dead names in the sweep's denominator**) — seven sites at fold (F-R4), **three added this round** (R-17, cures **D-3**; `git grep -n 'proof:styling-idioms' origin/master -- demo/`):
`proof:brittleness` — `demo/styles/style.css:38-39` (dissolution `70b32501`) · `demo/styles/layout.css:152`.
`proof:font-census` — `demo/DESIGN.md:29` (§24-30, the Mono-as-data law's named witness) · `DESIGN.md:250` · `EditorStartScreen.vue:180` · `font-roles.json:2`/`:62`.
`proof:styling-idioms` — `DESIGN.md:250` · **`DESIGN.md:237`** (the R4 codex clause) · **`components/instrument/shell/EditorStartScreen.vue:159`** · **`scenes/sequence/SequenceTarget.css:212`**.
**None exist.** **Two residue families route INBOUND from KF.W4 under R-8 and are received here** (C-12): **kf-AnimationControlsGroup D-13** — *"seven `proof:*` gate citations resolve to zero runnable gates"* (NO-WAVE-OWNER, prose-truth hygiene) — and **kf-DemoGlobalChrome L-6** — `proof:demo-no-oversize` at **16 hits across 13 files**, carrying its lock *"A fix confined to this file leaves twelve files standing; the fold owns the sweep."* **The sweep's true surface is therefore 10 sites over three names + 7 names from D-13 + 16 hits over a fourth name — not 7 sites over three names.**
*GREEN*: one authoritative dated gate roster in the tree — stating the 3 entry points and the 9 files by path — and **zero** prose citations of a non-existent gate, swept by name over **all four dead names** (the three banked-plus-fold names + `proof:demo-no-oversize`) and over D-13's seven, **not just the two banked ones**.
*Falsifier*: **this gate must precede KF.W4** — a gate wave cannot be authored against an inventory that includes gates which do not exist. A sweep that strikes only the two banked citations while `DESIGN.md:250` survives passes the banked check and fails this one — **and the same falsifier fires one level up**: a sweep run against the seven-site fold list leaves `DESIGN.md:237`, `EditorStartScreen.vue:159` and `SequenceTarget.css:212` alive, which is this gate's own defect reproduced by its own roster. **A roster short by three sites, on the one gate whose entire content is enumeration completeness, is the failure — not a rounding error.** Likewise a sweep that ignores the two inbound residue families closes with twelve files still citing `proof:demo-no-oversize`. The **demo-typechecking arm is NOT this gate's**; it routes to KF.W4 against the single banked no-SFC-typecheck identity.
⟨carries C-12⟩

**G-0.8 — CODEX v8 PIN DISPOSITIONED.**
*RED baseline*: X-1 — the whole v8 lineage is pinned to `8281638c`; every denominator (414 contracts / 152 surfaces / 73 568 Safari cells / **184** demo members) is measured there, and 184's exact Codex/census agreement is the proof both censused the same stale worktree. **No disposition exists today.**
*GREEN*: a written ruling — the v8 contract **re-anchored at `81a56990`**, **or** declared **stale-by-substrate and never citable for a live count** — plus **185** adopted as the frontier demo count (fold re-measured), and the seal cited only as `345 exact / 12 partial / 57 unresolved @ 8281638c`. **OG-KF1 named here; ruled at KF.W10.**
*Falsifier*: a disposition of "carried" or "pending" is not a ruling — the gate reads the verdict token. A ruling that re-anchors the contract without re-measuring its denominators passes a naive check and leaves 414/152/73 568 citable against a tree they were never measured on.
⟨carries C-18 + the conditional TCC re-reads B10-9 · B19-10 (pin half) · B19-11 · B20-6 · B21-7⟩

**G-0.9 — REGISTRY RE-ANCHOR.** Every adjudicated record states WHICH ref it cures against.
*RED baseline*: `ls registry/adjudicated/kf-*.md | wc -l` → **58** (fold-verified), **0** of which carry a ref-of-record stamp. At least two name mutually contradictory substrates for one file (C-20). `git status --short demo/utils/` → `?? demo/utils/formatEditorCSS.ts` and `?? demo/utils/keyframeSelector.ts` — **UNTRACKED, absent from HEAD's tree, yet cited by line** in landed records. The `backward` module's banked divergence figures do not reproduce (N-2, corrected by appended note). `TypingDots.vue` inverts the substrate direction (N-4). **Four inbound appended-correction obligations are RED for the same reason — declared elsewhere, absent here until this round** (C-22 / R-19b; `grep -c` in this file before repair → `App.vue:176` 0 · `TABS_EXTERNALLY` 0 · `dock-margin` 0 · `KF-SKEL` 0), plus the kf-AnimationVisualizer census-cell correction carried at C-9 **and the kf-KeyframeTimeline header-equation correction booked at C-1.R this round** (`grep -c 'KeyframeTimeline'` before repair → 1, and that hit is C-10's PRM-roster cell, never a correction obligation) — **six inbound corrections, all RED, none of them stampable by a per-repo pass**.
*GREEN*: every record carries a stamped ref-of-record; every contradicting cell reconciled by **APPENDED** correction (**E-3 — dated evidence is never rewritten**); the pass demonstrably ran **per-file** (C-21's three corrections landed, KF-AT-28's obligation discharged for that file); **and the six named appended corrections have landed by id**: (i) `TABS_EXTERNALLY_MANAGED_KEY` → `App.vue:176`, not the banked stale-HEAD `:169`; (ii) `--dock-margin` = 9 live unfallbacked sites + 1 prose mention; (iii) **KF-SKEL-7**'s cascade-level coupling arm recorded beside the import census; (iv) `kf-AnimationVisualizer.md:9`'s census cell corrected to worktree-scope (kf-SequenceScrubber K-1); (v) the C-17.R E-3 addendum demoting the kf-KeyboardShortcutsModal renumber to a claim-input; (vi) **kf-TimelineCaret MISS-α5 ≡ RR-β K-BASE's attached correction** — the banked `kf-KeyframeTimeline` header's *"working tree = the audited tree"* equation, false at every coordinate this wave measures (C-1.R). Binding form ⟨kf-TimelineCaret⟩: *"any spec consuming this registry states WHICH ref it cures against."*
*Falsifier*: **if two adjudicated records still disagree about one file's bytes, the wave has not re-anchored** — C-20 is the named test case. A blanket per-repo re-anchor passes a naive stamp count and **corrupts three banked rows** (C-21) — the falsifier is `git diff --stat origin/master -- …/TypingDots.vue` returning empty while the stamp says "stale HEAD".
⟨carries C-2's law, C-19, C-20, C-21, N-2, N-4⟩

**G-0.10 — SOURCE-OVER-DIST PROVENANCE.** glass-ui citations re-derived against the consumed artifact.
*RED baseline*: ~1/3 of challenge-D's `glass-ui/src/**` file:line provenance does not resolve in the consumed 7.0.0 dist (cut 2026-07-16; `--ink-perimeter` landed 2026-08-04); K-9 records the *convicting* re-reader committing the identical error; EH-1 is the third instance (archived letter vs `dist/header-ribbon.js`). Companion, fold-verified: `dist/gh-pages/assets/index-CL_QYCiO.css` mtime **Jul 16 09:11** — possibly pre-glass-7 — so **every byte-offset receipt in the corpus is provisional**.
*GREEN*: every surviving `glass-ui/src/**` citation re-derived against `node_modules/@mkbabb/glass-ui/dist/**` **or struck**; XR-4 / IN-GLASS-1 / `DISPOSITIONS.md:21` re-read against the **archive file** (`value.js/docs/tranches/V/archive/GLASS-INBOUND-2026-07-16-headerribbon-persistent-only.md:15-19`) + `dist/header-ribbon.js`; **one fresh `npm run gh-pages`** so the byte-offset receipts stop riding a Jul-16 artifact. **(R-11, execution site 3 of 7 — the real script is `origin/master:package.json:43` `"gh-pages": "vite build --mode gh-pages"`; `git grep -n 'build:gh-pages' origin/master` → no hits, re-verified at this repair seat 2026-08-28. The banked caveat's words at C-13 are NOT rewritten; only the act is corrected.)**
*Falsifier*: matching **version strings** on both sides passes the check that produced this defect in the first place — the gate's content is that a version string is not an artifact. A Glass-7 consume slice scoped from the letter rather than the tree is the second falsifier: under the letter the target file is irreplaceable, under the tree it is deletable.
⟨carries C-13, C-14⟩

## Agent Units

Seats are **Opus** (M-23 §2) — this wave is enumeration, measurement, ruling, and appended correction; there is no design content.

**OP-1 — the reset (OWNER'S HAND, not a seat).** `git fetch && git reset --hard origin/master`, untracked V docs unharmed ⟨lane-docs.md:380, row 16⟩. Runs alone and first. *Sub-gate*: G-0.1.

**KF.W0.b — The Manifest Ruling.** Land the four-coordinate single state; rule the deletion and `legacy-peer-deps`; write the three relay packets into `INBOX.md`. *Files*: `MANIFEST-RULING.md`, `INBOX.md`; owner-hand `package.json`/`package-lock.json`. *Sub-gates*: G-0.2, G-0.5.

**KF.W0.c — Reconciliation Record and the Rebase Oracle.** Disposition the full reconciliation surface (both denominators); verify (not author) the two EE-02 sites; re-run the three oracles on the reconciled tree; diff the reconciliation output rather than assume it. *Files*: `SUBSTRATE-SETTLE-*.md`. *Sub-gates*: G-0.3, G-0.4.

**KF.W0.d — Counts, Columns, and Fresh Shadow Ids.** Re-run SCH-1..7 + X-2 with pasted outputs; add the cascade-coupled column; re-scope §6.5 and the z cells in one motion; mint the shadow ids in `lane-frontend.md`. *Files*: `COUNTS-*.md`, `CENSUS-2026-08-03.md`, `lane-frontend.md`, `lane-library.md`. *Sub-gate*: G-0.6.

**KF.W0.e — Ref-of-Record, Gate Roster, Provenance, v8.** Stamp all 58 records and reconcile contradictions by appended correction; book the gate roster (3 entry points / 9 frontier gate files) and strike dead-gate prose over **all four dead names plus D-13's seven** (R-17/R-8 — not three names, and not the seven-site list); re-derive or strike every `glass-ui/src/**` citation and run one **`npm run gh-pages`** (R-11, site 4 of 7); disposition the v8 pin and name OG-KF1. *Files*: `GATE-ROSTER.md`, `REF-OF-RECORD.md`, `V8-DISPOSITION.md`, the 58 records, `INTAKE-ADJUDICATION-2026-08-03.md`, `lane-docs.md`. *Sub-gates*: G-0.7, G-0.8, G-0.9, G-0.10.

## Sequencing

**Within the wave.** OP-1 → (.b ∥ .c) → (.d ∥ .e). Hard orders: C-9's cascade column **before** F-1's remediation scopes its blast radius; C-17's id minting **inside** the census re-anchor; G-0.5 **bounds** G-0.2 (negative gate); G-0.4's oracles run **after** the migration lands, never before.

**Cross-edges declared from this end.**

| To | Direction | Edge |
|---|---|---|
| keyframes.js OWNER (§B-12) | **INBOUND — OP-1** | The reset is the owner's hand; value.js must NOT perform it ⟨lane-docs.md:380⟩. Every row here is downstream of it. A wave that performs the reset itself, or opens without the begin-word, re-runs the schism it exists to close. |
| glass-ui producer (SS-6 / standing BH relay) | **OUTBOUND HOLD + FYI** | **HOLD**: installed glass-ui may NOT advance past `4bf53962` while `origin/master:EditorShell.vue:116` imports `/header-ribbon` — declared from this end so the producer's next bump does not silently break the consumer's build. **FYI packet** (producer rows NEVER become demo-side hacks): C-16's unmet peers (closed falsifier); C-14's rider — *producer docs/letters must be re-derived against shipped bytes* (third instance of the kf-ChromeDock doc-drift ledger pattern), incl. `docs/consumer-evidence/header-ribbon.md:3` still claiming "RETAINED persistent-only" against producer HEAD; C-3's producer half now **MOOT** (answered by deletion — the relay downgrades to FYI + the consumer-evidence correction); kf-CSSPasteDialog's DialogFooter `sm:`-only gap (cured in glass-ui source HEAD but **UNRELEASED** — relay live, discharging when keyframes.js consumes the release). |
| **KF.W1 · Mail Cure** | OUTBOUND | The O-8/O-11 obligation packets are untracked in the sacred stale checkout — invisible to any executor, unanswered 10–16 days, measured against the stale tree (SCH-4/5). W1's re-delivery depends on this wave making them visible and on the re-anchored counts replacing the stale ones. |
| **KF.W4 · K-Quartet Hygiene** | OUTBOUND — **may not open before this re-anchor** | (a) C-12's demo-typechecking arm folds to the single banked no-SFC-typecheck identity (KF-APP-4 ≡ KF-CE-16 ≡ KF-CO-7 ≡ MM-36 ≡ LP-7 ≡ KF-AT-12 ≡ KF-HA-5 ≡ L-4/C-6 ≡ B-1 ≡ RB B-1 — **booked ONCE, never re-booked**); (b) EE-01's phantom trail → KF-CB-18; (c) the `--project demo` CI rider ⟨kf-MatrixEditor K1 / FAM-03 MR4⟩ + OD-29/ME-32's gate-truth rows. **G-0.7's roster is KF.W4's precondition.** Also carried to KF.W4 from INTAKE as born-RED gate-authoring law (G-L7a–d): B10-17/18/19/20/22/23 + B10-27 + B18-19 + B21-17. |
| **KF.W5 · D/L/C Tri-Fold Library Audit** | OUTBOUND | W0 re-runs COUNTS; **W5 reconciles the BASES** — X-4 (80 / 139 / 153 / "159", the SCH-3 drift class with Codex's fourth basis) plus B10-21 (control-coverage ratio). SCH-7's depcruise re-verification is W5's input. One library letter travels: `fromString` APPEND semantics (KF-KE-10). |
| **KF.W6 · its CARRY file** ⟨`docs/tranches/X/keyframes/carry/KF-W6-CARRY.md:354`, `## §CrossEdges (declared from this end)` → **KF.W0 · Substrate Settle**⟩ | **INBOUND — ANSWERED THIS ROUND (R-19b, cures D-6)** | Declared from KF.W6's end and **unanswered from this one** until this round (`grep -c` in this file before repair → `App.vue:176` 0 · `TABS_EXTERNALLY` 0 · `dock-margin` 0 · `KF-SKEL` 0). Received whole at §Carry **C-22** and booked **by id** as G-0.9 appended-correction obligations: (i) the `TABS_EXTERNALLY_MANAGED_KEY` provide site is **`App.vue:176`**, not the banked stale-HEAD `:169`; (ii) **`--dock-margin` = 9 live unfallbacked sites + 1 prose mention**; (iii) **KF-SKEL-7**'s cascade-level coupling arm beside the import census (which additionally rides §Carry C-1.F row F-13; its **token-namespace probe half stays KF.W6's**). Two of the three are byte-level corrections to banked citations — **exactly the class C-20 names as this wave's test case**. The seam now closes at **both** ends. |
| **KF.W6 · Glass Suffusion** | OUTBOUND | The z-contract prose recut (KF-AX-11/12/31 in one motion, resolving the `--z-behind` policy contradiction at `layout.css:8-11`); the phantom-token/utility audit (KF-APP-6's re-cut, kf-CubeScene D-11's 8 sites); the `/number-field` evaluation (ME-18 precedent) **under whatever fresh id W0 mints**; SCH-2's KfPillTabs 217-line fork, confirmed-at-HEAD, still the top suffusion target. **Nothing in KF.W6 may be scoped from the archived Glass-7 letter (C-14).** G-0.10's fresh **`npm run gh-pages`** build is a shared precondition (R-11, site 5 of 7 — **no `build:gh-pages` script exists at HEAD, `origin/master` or the worktree**; a shared precondition stated as an unrunnable command is a precondition no sibling can satisfy). |
| **KF.W9 · Safari Visual Audit** | OUTBOUND | PRM unification consumes the **re-scoped §6.5** — the vendor's universal `*:not([data-allow-motion])` reset reaches every demo element, so every per-scene tally re-derives (kf-CubeScene: 4 engine motions + 1 gated; **OD-11 corrects the cube to 5**). SS-13's denominator is KF.W9's own scoped surface list — the 73 568-cell Kronecker self-cross-product is **REJECTED** (B18-26, numerator inherited). |
| **KF.W10 · Fold Discharge & Close** | OUTBOUND | The v8 seal's terminal disposition (357/414 dead within Codex's own corpus → sole citable form `345 exact / 12 partial / 57 unresolved @ 8281638c`); B18-12/13/14 (three external coordinates no artifact reproduces — cannot close rows); B18-27 (slot 4-vs-5, settle or drop); **OWNER BLOCK: OG-KF1 beside OD-V3/OD-V5 — never proxied.** W0 **NAMES**; W10 **RULES**. |
| **KF.W12 · Authoring-Surface Repair** (homed at KF.W4 per R-15 — 6 packets incl. **EDITOR-UNIT**; leg (a) below) | OUTBOUND — **NOT this wave's** | (a) EDITOR-UNIT's F-1 leg **IS** this wave's — census F-1's owning id is **KF-CE-32**, discharged at KF.W0 §B-12; **nothing else** in EDITOR-UNIT is. (b) Declared law travels intact: *"no census KF.W0–W10 row names this repair, and scattering it across ten waves would orphan its one root cause. It must sequence AFTER the KF.W4 type-gate lands (nothing in it is verifiable until a `.vue` file can fail a build)."* (c) Its cure is entangled with KF-CE-3/4 — **no cheap highlighting-only import**: restore contributions, or hand-register a Monarch tokenizer + `setLanguageConfiguration`. (d) Sibling units citing it as precedent (KFED-, OPTIONS-, APPLY-, CARD-UNIT) each carry *"after KF.W0 + KF.W4."* |
| **the NO-WAVE-OWNER packets — the canonical 17** (R-15; **re-cut this round from this file's stale 15**): cube · sequence · spring · spring-plot · spring-physics-facet · spring-artifact-truth · square · **amiga** · drag-seam · CARD-UNIT · OPTIONS-UNIT · KFED-UNIT · **EDITOR-UNIT** · APPLY-UNIT · AXISLINE-UNIT · dock-menu · **transport/ribbon** | OUTBOUND — **homing is SS-1/SS-2's act; this wave supplies their ground** | **Cardinality settled (R-15)**: this file previously enumerated **15**; the canonical roster is **17** = W0's 15 ∪ {**amiga**, **EDITOR-UNIT**} (KF.W9 S-11's adds). **The number 16 was never true at any spec** — KF.W1's unreceipted "16" is corrected at its own end. `transport` is named canonically as **transport/ribbon** ⟨kf-PlaybackRibbon:36⟩. **Homing is discharged by KF.W4's lists, not by this wave**: KF.W11 · Demo Scene Repair ×9 (cube · sequence · spring · spring-plot · spring-physics-facet · spring-artifact-truth · square · amiga · drag-seam) · KF.W12 · Authoring-Surface Repair ×6 (CARD-UNIT · OPTIONS-UNIT · KFED-UNIT · EDITOR-UNIT · APPLY-UNIT · AXISLINE-UNIT) · **KF.W13 · Chrome, Dock & Transport Repair ×2, MINTED** (dock-menu, moved off the KF.W12 list per `KF-W2.md:270` + KF.W1 cross-edge 9 · transport/ribbon, **homed for the first time**). W0 **names the roster and supplies the ground; it homes none of them** — and a roster stated at a count no enumeration reaches is the same enumeration defect C-17 and G-0.7 exist to kill, one level up. Every packet re-anchors against the reconciled tree before any cure executes (the KF.W0 law, stated in each record's roster preamble) and sequences after KF.W4's vue-tsc gate. **Locks that must travel WITH each packet, not with this wave**: KFED-UNIT *"sequences after KF.W0 (two blockers are already repaired at the frontier; a careless rebase re-lands them) and after the KF.W4 vue-tsc gate"*; **OPTIONS-UNIT's KF-CO-1/KF-CO-8 cures are SEQUENCED BY LP-1** — no write→render edge exists for the panel (`App.vue:218` `shallowRef(markRaw(…))`, re-`markRaw`'d at every shell-binding assignment; **curing the prop names without curing the reactivity ships a still-dead control**); **kf-ChromeDock M-4's worded cure ships an unopenable menu without MM-28's MUST-CARRY correctness rider**; **TD-1/TD-2's cures BUNDLE** (one commit with TD-4 + PRM + test); **kf-SequenceAxis ruling-1's geometry cure lands WITH the head/label collision cure as ONE commit**; **kf-CubeScene ARB-1** gates the KF-APP-1/-17 motion to the delete arm only until the glass-ui M-A relay lands; **kf-SquareScene's comment-stated invariants are TEST OBLIGATIONS** (the kf-SquareScene law — two stated invariants proved false; the misses added two more). |
| **V·π parser program / value.js parser lane** | OUTBOUND — census annotation, **explicitly NOT SS-13** | Three carries stay value.js-side, named so no KF wave books them: the **C-L-2 residual** (do `parseCssValue`/`parseCssScalar` internally dispatch into the colour grammar for non-colour declarations? — untraced by all five CubeScene seats); **R1**'s identity (banked, KF.W3-gated — *"a value.js-side question, not an SS-13 pixel"*; KF-HA-19's R1-framing contradiction relayed as a NO-WAVE-OWNER carry); the `parseTimingFunction` non-string raw-TypeError (R1 class, evidence forwarded from kf-ChannelOptions, identity already banked). **KF.W3 is GATED on PLAW-BIND → V.L1/V.L5 → packed Value release.** |
| **keyframes.js engine lane (`src/`)** | OUTBOUND | Preserved halves of rows whose substrate faces land here: the `applyTimingFunction` frame-propagation gap (L·B-2's residue, per L's own handoff note) and the `parseAnimationCSS.ts` per-stop timing-function drop (R3-forwarded, lane-library §4.6 seam). **Engine work, not substrate work — named so the reconciliation does not absorb them.** |

## Excluded

Not carried by this wave, each with its reason. Silent drops are the defect class this program exists to kill; these are loud.

| Excluded | Reason |
|---|---|
| **X-4 basis reconciliation** (80 / 139 / 153 / "159") | W0 re-runs the **counts**; the **bases** are KF.W5's row per the INTAKE carried table. Restating four bases here would mint a fifth. |
| **SCH-7 depcruise re-verification** | KF.W5's input. W0 restates it only as *unverified-at-HEAD* — "0 depcruise violations" does not refute L-1, and a green run here would be exactly that false refutation. |
| **SCH-2 (KfPillTabs 217-line fork)** | CONFIRMED at HEAD; **re-verify, do not re-book**. Cure is KF.W6's top suffusion target. |
| **The `/number-field` evaluation** | KF.W6's (ME-18 precedent). W0 fixes only the **identity** (C-17). |
| **The z-contract prose recut** | KF.W6, in one motion with KF-AX-11/12/31. W0 owns only the **census** re-anchor. |
| **The header-ribbon migration decision** (which chrome host replaces the band) | NO-WAVE-OWNER, SS-1/SS-2's design decision — *"the cheapest close of the banked blocker AND of D-15/D-17"*. W0 carries only the tripwire. |
| **The demo-typechecking arm of C-12** | KF.W4 (post-F-1), against the single banked no-SFC-typecheck identity. Booking it here would be the tenth re-booking of a once-booked row. |
| **KF-CB-18** — EE-01's surviving phantom trail (`easing.ts:44`, `waapi/eligibility.ts:169`, the m-6 test) | Not cured at the frontier; KF.W4's. |
| **KF-KE-17** (`frames: any[]`) and FE-3's terminal cure (`frameStart: KeyframeSelector` or a `selectorText`-fed label; L-15's three-line acceptance test) | The **cause** is not cured by the substrate settle. KFED-UNIT, after KF.W0 + KF.W4. |
| **FE-3's partial-cure residue** — fraction-for-percent (`"0.5"` vs the commit parser's `"50%"` at `:206-209`), named-selector fallthrough, `selectorText` uncalled at both seams | Explicitly not G-0.4's; KFED-UNIT's. |
| **KF-KE-10** (`fromString` APPEND semantics; 3→6 frames, 10 duplicate cards projected) | KFED-UNIT + one library letter to KF.W5. |
| **L·B-2's engine residue** — direct-field-write bypass of `setTimingFunction`, `applyTimingFunction` frame-propagation gap, N-10 double write, N-17 flatten tripwire | OPTIONS-UNIT / engine lane at MAJOR, per L's own handoff note. |
| **Every NO-WAVE-OWNER scene/behaviour packet's cure** | Homing is SS-1/SS-2's act. W0 supplies their ground (the reconciled tree) and declares their locks; it homes none of them. |
| **B18-12/13/14** (three external coordinates no artifact reproduces), **B18-27** (slot 4-vs-5) | KF.W10's. Cannot close rows here; carried unresolved by design. |
| **B10-9 · B19-10 (pin half) · B19-11 · B20-6 · B21-7** (TCC re-reads) | **Conditional** — owed only if the v8 contract is ever cited. G-0.8's disposition decides whether they open at all. |
| **OG-KF1's ruling** | **Named** here, **ruled** at KF.W10's owner block beside OD-V3/OD-V5 — never proxied. A seat that rules it here fails the wave. |
| **B18-26's numerator** | Split adoption stands as banked: numerator INHERITED, denominator REJECTED. KF.W9 scopes its own list; W0 re-books neither half. |
| **kf-CubeAxisLines β-miss-1** — the `optionalDependencies` failure-SHAPE (`npm ci --omit=optional` breaking the build at `style.css:3`) | HEAD-only artifact, **killed at the frontier** (devDep, not optionalDep). *"Recorded for the KF.W0 reconciliation narrative, charged nowhere"* ⟨kf-CubeScene kill 4⟩ — carried as narrative, booked as nothing. |
| **The clean-`npm ci`-fails probe · the "verify against 6.0.0" residue · every "blocked on F-1" gate in the CopyButton corpus (D-9, C-15, M-6 leg 2)** | RETIRED / DISCHARGED / RE-SCOPED at their banks (C-1). Recorded so no seat re-spends the budget. |
| **Reader-B's substrate caveats** L-5 / L-9 / L-14 / C-7 / P-1 / P-6 and the corollary "P-1's adoptCompiled watch never fires at HEAD" | **VACATED** with the RESCOPED-BY-INVERSION ruling. Preserved as a dated record, not as live findings. |
| **KF.W1's mail re-delivery** | Depends on this wave, is not performed by it. |
| **Any product-source change beyond the owner's reset and the two EE-02 paths** | **Execution gate**: nothing opens product source; the specs await the owner's begin-word. |

## Cadence, Artefacts, Commits

`git diff --check` at each unit boundary; Prettier over touched `.md`. **No `npm run lint` / `check` / `test` runs in this wave** beyond G-0.4's three named oracles (invoked **by path**: `node scripts/observe/demo/smoke.mjs`, `node scripts/observe/demo/live-session.mjs`) and G-0.10's one **`npm run gh-pages`** (R-11, site 6 of 7) — inventing a green run over untouched code is the vacuous evidence L-19 forbids; the replacement evidence is the ten gates' pasted before/after outputs.

Banked under `docs/tranches/X/keyframes/artefacts/W0/`: `substrate-open.txt` / `substrate-close.txt` (the G-0.1 sextuple both sides) · `manifest-four-coordinates.txt` · `ee02-diff-open.txt` / `ee02-diff-close.txt` · `oracle-ee01-fe3-ee03.txt` · `headerribbon-tripwire.txt` · `counts-2026-XX-XX.txt` · `gate-roster.txt` · `refofrecord-stamp-audit.txt` · `glass-citation-rederivation.txt` + the fresh **`npm run gh-pages`** build hash (R-11, site 7 of 7) · `v8-disposition.txt`.

Commits, pathspec only, `scripts/dev/dev.sh` never included: `docs(kf-w0/settle)` · `docs(kf-w0/manifest)` · `docs(kf-w0/counts)` · `docs(kf-w0/roster+provenance)` · `docs(kf-w0/ref-of-record)` · `docs(kf-w0/v8)` · `docs(kf/w0 close)` — the last stamps **IMPLEMENTED**; **VERIFIED is KF.W10's to stamp** (R-A).

## L-18 rider

Landing the ten gates green makes this wave **IMPLEMENTED**, not ACCEPTED — two challenging gestalt passes, each a quartet of Opus 5 skeptics at three altitudes, adjudicated and agglomerated by a fresh Fable instance. Bases with obvious purchase here: a re-anchor applied per-repo when C-21 proves it must be per-file; a disposition record covering the 252 status rows over a 325-file frontier surface; a manifest ruled single-state while `.npmrc` stays unruled; oracles run before the migration lands, measuring the frontier and proving nothing; a gate roster booked while a struck prose citation survives elsewhere (`DESIGN.md:250` is the named residue-check); glass citations "verified" by matching version strings — the exact defect C-13 convicts. **L-20** binds this file: re-validated against `docs/precepts/` at authoring, at every amendment, and at each L-18 pass.
