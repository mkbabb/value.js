# KF.W0 — Substrate Settle

**Preamble.** This file **SPECIFIES**; it does not execute. Every act below — above all the `git fetch && git reset --hard origin/master` in `/Users/mkbabb/Programming/keyframes.js` — **awaits the owner's begin-word**, and the reset itself is the **owner's hand**: `lane-docs.md:380` (verified verbatim at fold), *"Blocked from our side by lane law and by their sacred-checkout rule — value.js must NOT perform it."* A wave that performs the reset itself, or opens without the begin-word, re-runs the schism it exists to close. Specifying is not doing.

## Fold Note (fresh-Fable seat, 2026-08-28)

This spec is the M-23 agglomeration of one Opus draft against the CARRY authority (21 rows · 10 gates), verified row-for-row. All 21 CARRY rows carried (C-1..C-21); zero silent drops found; the following **fold repairs** were applied, loud:

- **F-R1 (path re-home)**: the CARRY's boundsFiles named `docs/tranches/X/kf/waves/W0.md`; the lane's real home is `docs/tranches/X/keyframes/` — this file lands at `waves/KF-W0.md` beside the already-authored siblings `KF-W4.md` and `KF-W7.md`. All W0 work-product and artefact paths re-homed accordingly. The draft's claim that this wave *creates* the lane dir is corrected: the dir exists (`carry/` + `waves/`, verified 2026-08-28).
- **F-R2 (witness re-verification)**: every RED baseline below was **re-measured by this fold seat on 2026-08-28**, read-only, in `/Users/mkbabb/Programming/keyframes.js` (and `/Users/mkbabb/Programming/glass-ui` where named). The CARRY seat's 2026-08-25 figures reproduce exactly (41/1 · 252/325/124 · four manifest coordinates · EE-02 stat 6+/76− · TypingDots inversion · 153/58/185 · the gate roster · lane-docs:380 · S-count 8).
- **F-R3 (N-3 corrected)**: the draft claimed `git grep 'templateFrames.length' HEAD -- demo/` returns zero rows. False — it returns **2 unrelated guard sites** (`KeyframesEditor.vue:204`, `useKeyframeOps.ts:179`). The correct witness is that the **cure line** (`() => animation.templateFrames.length,` = `origin/master:useKeyframesParsing.ts:97`) is absent at HEAD; G-0.4 uses the precise form.
- **F-R4 (G-0.7 widened)**: the dead-gate prose surface is larger than banked — a **third dead gate name** `proof:styling-idioms` (`demo/DESIGN.md:250`) and additional `proof:font-census` sites (`DESIGN.md:29`, `EditorStartScreen.vue:180`, `font-roles.json:2`/`:62`) plus `proof:brittleness` at **both** `demo/styles/style.css:38` and `demo/styles/layout.css:152`, all measured at `origin/master` today.
- **F-R5 (dropped riders restored)**: kf-SpringTrace's *"F-1 must be re-measured against origin/master before routing any wave"* and kf-SharePopover's *"the stripped worktree manifest remains an unexplained local artifact → KF.W0 §B-12"* restored to C-1; the **~94 dirty src rows** figure restored to C-2's inventory.
- **F-R6 (shadow-census home pinned)**: S-1..S-8 live in **`lane-frontend.md`** (`^### S-` count = 8; S-1 `:264` … S-8 `:387`), not in CENSUS — C-17/G-0.6 cite the real file.
- **F-R7 (N-2 confirmed by diff)**: disk `emit/backward.ts` (393 L) vs frontier `emit/backward/backward.ts` (393 L) diff returns **only import-path-depth hunks** (`../css-text`→`./css-text`, `./walk`→`./backward-walk`, `../format`→`./format`); HEAD = 410 L. The banked "diverges from BOTH refs" figures are corrected by appended note (E-3), never rewritten.

## Repair Round 1 (L-20 conformance, 2026-08-28)

Authority: `docs/tranches/X/keyframes/conformance/PASS-1/RULINGS.md` (cross-wave, binding) over `PASS-1/KF-W0-CHECK.md` (local register) — where the two collide, the ruling wins. Every amendment below is an append or a replace **inside this planned spec**; nothing here stamps a verb, opens product source, or re-runs a measurement. Registry corrections stay E-3 addenda under original ids (§Bounds, the 58-record row) — this file books the obligation, it does not perform it. Rows adopted from the registry keep their **banked ids** (anti-rename); riders, cure-shape locks and sequencing locks are **carried verbatim, not cited**.

| Directive | Landing site |
|---|---|
| **R-1a** — the twelve fold-rider escapes get their one adopting wave | new §Carry **C-1.F** id-keyed fold table (12 + KF-SKEL-7 riding from R-19b); C-1's blast-surface rider list widens from six records to the full count; C-9's hard order is thereby scoped on a complete blast surface (cures **D-5**) |
| **R-5** — the S-10 census slot triple-claim | §Carry **C-17** re-cut to the FULL claimant set (S-9 ≥ 8 · S-10 ≥ 4) as inputs to one mint; the kf-KeyboardShortcutsModal seat-ruled renumber **DEMOTED to a claim-input** by E-3 addendum; the fourth S-9 subject corrected (cures **D-1**, **D-9**) |
| **R-11** — `build:gh-pages` exists at no coordinate | seven sites corrected to **`npm run gh-pages`**. Receipt, re-verified this round: `git show origin/master:package.json` → `:43 "gh-pages": "vite build --mode gh-pages"`; `git grep -n 'build:gh-pages' origin/master` → no hits. Sites: §Scope 10 · C-13 · G-0.10 GREEN · unit `.e` · the KF.W6 OUTBOUND edge · §Cadence · §Artefacts (cures **D-2**) |
| **R-15** — the NO-WAVE-OWNER packet partition is CLOSED | §Sequencing's packet row re-cut from this file's stale **15** to the **canonical 17** (= the 15 ∪ {amiga, EDITOR-UNIT}; `transport` named canonically as `transport/ribbon`), with KF.W4's homing lists restated (W11 ×9 · W12 ×6 · **W13 minted ×2**). Homing stays SS-1/SS-2's act; this wave names the roster and supplies the ground |
| **R-17** — one proof roster, one owner, one sweep | **G-0.7** widened by its three missing demo sites, receiving the two residue families routed here from KF.W4 under R-8; the `scripts/gates/` inventory stated at the frontier **9 files** (cures **D-3**) |
| **R-19b** — KF.W6's CARRY §CrossEdges answered from this end | new §Carry row **C-22** + a new **INBOUND** row in §Sequencing's cross-edge table (the edge was declared at one end only; an answer that exists in §Carry but not in the edge table still leaves the seam half-open); all three corrections booked **by id** as G-0.9 appended-correction obligations (cures **D-6**) |
| **Local — the id-keyed census closed** (KF-W0-CHECK §2.C/§2.D) | new §Carry **C-1.G**: the 13 F-1-fold ids whose CONTENT C-1 already carried and whose **id** was absent by bytes, indexed with each row's banked routing words and its not-covered-by-the-fold limb named. **Nothing is re-booked** — indexing is not curing; it is what makes the fold's closure checkable. §2.D's third escape (**MISS-α5 ≡ RR-β K-BASE**'s attached kf-KeyframeTimeline header correction) booked at **C-1.R** + G-0.9 as the sixth named appended correction |

Local defects cured beside the rulings: **D-4** (G-0.4's three oracles re-founded on addressable witnesses; the `/\[object Object\]/` smoke assertion is keyframes.js's OWN tranche-V W1 born-RED gate and is declared as a cross-edge, never asserted as an in-tree instrument here) · **D-7** (C-9's provenance corrected to `C·C-13 / L·D-2`; the dropped MAJOR half booked) · **D-8** (C-19's firing ledger enumerated at its true count) · **D-10** (KF-CE-32's re-route declared **loud** as a ruling with the record's own words, E-3 addendum owed at the bank) · **D-11** (C-12's R-7/R-9 pairing corrected) · **D-12** (`e2e/**` struck as a phantom path; `dist/gh-pages/**` given a bounds row).

**Altitude sweep (the cure is not the cure until every altitude carries it).** A defect repaired in §Carry and left standing in §Scope, in a gate's prose, or in a unit's charge is not repaired — it is relocated, which is D-3's own mechanism one level up. So each cure was swept across all four altitudes of this file: **D-1** — §Scope item 6's "four-way S-9 collision + the requested S-10" **struck** and re-pointed at C-17.R's thirteen; **D-3** — §Scope item 8, G-0.7's roster prose and unit `.e`'s charge all re-cut from "three dead names / seven sites" to **four dead names over 10 sites + D-13's seven**, and from a bare gate roster to **3 entry points over the frontier's 9 files**; **D-2** — all seven execution voices now read `npm run gh-pages` while the banked words at C-13 and C-1.R stay verbatim (E-3); **R-15** — the §Sequencing packet roster and the KF.W12 edge row agree at 17 and no longer read "proposed" against a homed list. Receipts re-run read-only at this seat, 2026-08-28: `git grep -n 'build:gh-pages' origin/master` → **no hits**; `git ls-tree -r --name-only origin/master scripts/gates/` → **9 files**; `git grep -n 'proof:styling-idioms' origin/master -- demo/` → **4 hits incl. `DESIGN.md:237`, `EditorStartScreen.vue:159`, `SequenceTarget.css:212`**; `git ls-tree --name-only origin/master` → **no `e2e/`**; `scripts/observe/demo/{smoke,live-session}.mjs` + `scripts/lib/console-budget.mjs` → **all present** (G-0.4's runtime tier names instruments that exist).

**Status is unchanged: `planned`.** No gate here is turned; three gates gain born-RED witnesses they did not have.

## Repair Round 2 (L-20 conformance, 2026-08-28)

Authority: `docs/tranches/X/keyframes/conformance/PASS-2/RULINGS-2.md` (cross-wave, binding) over `PASS-2/KF-W0-CHECK.md` (local register) — where the two collide the ruling wins; where a ruling **states** a figure, this seat **re-derives it and reports the delta**, because **R2-5 (the witness re-derivation law) binds every seat this round**: every measurement written, carried, or left standing below was RE-EXECUTED at the substrate of record at this seat's clock — keyframes.js **`origin/master` `81a56990`** (`git rev-parse origin/master` → `81a56990736ced5b5edde0b84c527680ac7689b1`, run this seat; local `8281638c` is **DISQUALIFIED** and was read once, only to date drift) and the value.js tree at its current bytes — and **never inherited** from a ruling's prose, a check register, a CARRY, a pass-1 register, or this file's own prior text. Every probe below is a read-only `git`/`grep`; nothing here stamps a verb, opens product source, or moves a byte outside this file.

**Citation idiom (R2-7).** Every cross-spec receipt this round touches cites a **stable anchor** — the destination's §-heading, gate id, or banked id — with any line number riding parenthetically and declared non-load-bearing. The Re-anchor seat sweeps the receipts this round did not touch.

| Directive | Landing site |
|---|---|
| **R2-17** — G-0.7's GREEN is unsatisfiable inside this wave's own §Bounds (check **D-1**), and the roster it is keyed to is short by ~47 names at the true altitude (check **D-2**) | **G-0.7 re-cut to ROSTER + ROUTE, never strike**: GREEN is now two docs-only clauses over the re-derived **53 dead names** (54 distinct `proof:*` names in `demo/` at the frontier, 1 runnable), each disposed *struck-in-owner-wave* or *routed-with-named-owner*, and **W0 executes NO strike** — the gate is reachable by writing `docs/**` only. Unit **KF.W0.e**'s charge is re-worded *strike* → *roster + route*; §Scope item 8 and C-12's cure cell are swept to the same altitude. **D-13's seven are ENUMERATED** (check **D-5**) and `proof:demo-no-oversize`'s 16×13 gains its scope word (check **D-6**) |
| **R2-5(g)** — every roster minted or corrected this round is **re-derived from the BANK**, never adopted from a prior pass's register | **C-1.G re-derived over the 58 records**: the pairs absent by bytes are indexed; the count is **EIGHT, not the ruling's seven** — the eighth (**kf-SquareInstrument L-D9**) is this seat's re-derivation delta, declared loudly below. The §Carry denominator is **re-counted at 73**, not computed from the ruling's expected 72 (cures **D-3**) |
| **R2-13** — the `proof:brittleness` residue family is **KF.W6's** whole | G-0.7's roster carries the name with disposition **cure → KF.W6** and its three measured non-`docs/` frontier sites; the keyframes `docs/**` surface is declared loudly out-of-scope |

Local defects cured beside the rulings: **D-4** (census **§6.5 is not in CENSUS** — the PRM roster lives at `lane-frontend.md` §6.5; re-homed at all **five** altitudes that carry it — C-10 (mechanism + act), G-0.6's GREEN, §Bounds (both the CENSUS row and the lane-frontend row), §Scope 7, unit `.d`'s charge — this being F-R6's class, second instance) · **D-7** (G-0.4's runtime tier restated on what actually wires: `smoke.mjs` loads no budget and installs no `pageerror` handler) · **D-8** (the `dist/gh-pages` bounds row's citation proved a neighbouring fact; the git receipt is `.gitignore`, the `files` array is the npm-publish one).

**Altitude sweep, round 2.** Each cure was swept across every altitude that carries it, because a cure landed in one place and left standing in another is *relocated*, not repaired (this file's own round-1 law, and the precise mechanism check D-4 convicts): **D-1/D-2** — §Scope 8 · C-12's cure cell · G-0.7's roster prose, GREEN and falsifier · unit `.e`'s charge · the L-18 rider's named residue-check, all re-cut from *"four dead names over 10 sites"* to the **53-name route-not-strike roster**; **D-3** — §Carry's preamble denominator · C-1.G's heading, intro and closing note; **D-4** — C-10 · G-0.6 GREEN · §Bounds (two rows) · §Scope 7 · unit `.d`; **D-7** — G-0.4's runtime tier only (§Cadence's by-path carve-out is unchanged and still true). Receipts re-run read-only at this seat, 2026-08-28, at `81a56990`: `git ls-tree -r --name-only origin/master scripts/gates/` → **9 files**; `git show origin/master:package.json` → `:50/:51/:52` the three `proof:*` entry points, `:37` `check`, `:43` `gh-pages`, `:44` `lint`; `git grep -ho 'proof:[a-z0-9-]*' origin/master -- demo/ | grep -v -- '-$' | sort -u` → **54 distinct names** (+ one bare `proof:` token), of which `proof:publish` alone is runnable → **53 dead**; `git grep -c 'proof:' origin/master -- demo/` → **116 hits across 51 files**.

## Repair Round 3 (L-20 conformance, 2026-08-28)

Authority: `docs/tranches/X/keyframes/conformance/PASS-3/RULINGS-3.md` (cross-wave, binding) over `PASS-3/KF-W0-CHECK.md` (local register) — where the two collide the ruling wins. Every measurement written, carried or left standing below was **re-executed at this seat's clock** at keyframes.js `origin/master` `81a56990736ced5b5edde0b84c527680ac7689b1` (`git rev-parse origin/master`, run this seat) and at the value.js tree's current bytes; local `8281638c` stays **DISQUALIFIED** and is cited only as a named historical measurement context. Nothing here stamps a verb, opens product source, or moves a byte outside this file.

**Two program-wide laws bind every edit in this round.**

- **LAW A — THE IMPORT-GRAPH LAW** (RULINGS-3 §LAW A). Any act that **deletes, repoints or shims** a module `M` at path `P` — whether the act is performed, carried, or merely left standing — carries a consumer census derived **from the import graph at `origin/master`**, pasted with its commands and their outputs: (1) a specifier census over every path-tail and alias-root spelling that can reach `P`; (2) a symbol census over `M`'s exported names, **each hit resolved to its own import specifier and compared against `P`** (a consumer of a *sibling* module is not a consumer of `M`); (3) consumer set = (1) ∪ the resolved (2). Prose/doc hits are recorded as non-import context and never counted as consumers. No census here is inherited from a CARRY row, a bank paraphrase, a lane doc or a prior pass. This wave's delete/repoint/shim inventory is **exactly two acts, both carried rather than performed**, and both censuses are executed and pasted at **§LAW-A CENSUSES** below.
- **LAW B — THE CLOSURE-CLAIM BAN** (RULINGS-3 §LAW B). No sentence in this file asserts, **in its own voice**, completeness / closure / zero-escape over an enumerable set — census carriage, roster completeness, "the complete input set", "every one greppable", "all N booked". Each such clause now **cites the freshest conformance census artifact by repo path and date** and states what that artifact found *including its defects*; when a newer pass supersedes it the citation is re-pointed, never the claim re-asserted. **Corollary, applied to every cure below**: no cure commissions a sentence its own receipts do not reproduce — where this seat could not produce the receipt, the **honest sentence** is written instead and the substitution is declared loudly (see D-2 row 13, and the D-5 discard-rule delta).

| Directive | Landing site |
|---|---|
| **R3-10.1** — the C-17-mint consumption edge is declared from KF.W10's end and unanswered from this one (the exact class this file cured for KF.W6 at C-22 one round ago, un-swept) | §Sequencing's **KF.W10 row** gains the answer by bytes: the mint **PRECEDES** W10's CARRY-FORWARD RECORD block, `CARRY-C-3`/`CARRY-C-5` consume the ids C-17 assigns. The seam now closes at both ends for the sibling that declares the *hardest* downstream dependency (cures **D-4**) |
| **R3-11(f) D-1** — C-14/G-0.10 assert a **live** coordinate is dead | The *"NONEXISTENT path"* sentence is **STRUCK, loudly** (E-3), and both sites carry **two repo-qualified letters**. The path exists — in the *other repo*. The sibling that measured it declared the correction back to this wave by name (`KF-W10.md` §Sequencing A, the EH-1 rider), and the wave was converting a *"different repo"* finding into a *"citation kill"* |
| **R3-11(f) D-2** — the mint's roster is short, mis-keyed, anonymous and coined | **C-17.R re-cut**: `KAD-12` **adopted** from the sole carry · the `/number-field` claim **re-keyed** to the carry's head id `LP-8 ≡ KF-CO-35` · the coined **`F1(c)` RETIRED** for kf-EditorHeader's real ids · row 13 **named and struck** as a duplicate, with the S-10 claimant set re-derived at the corpus bytes and the `S-10` id-overload named |
| **R3-11(f) D-5 + R3-11(a)2/3/4** — the round-2 re-derivation receipt reproduces at no reading, and the sweep run properly lands the escapes | The **200/48 figure is STRUCK**; both readings are re-run and pasted with their commands **and the discard predicate that produces them**; C-1.G's membership is re-derived from the **44-record post-discard set** and gains **row 22 (kf-SequenceScene)** carrying the *countable-cell* datum as its named limb; the **kf-AmigaScene per-file offset** enters C-21/G-0.9 as the exemplar row; the **kf-SpringTrace D-15 no-re-book guard** enters C-1.R, disambiguated from kf-EditorHeader's D-15/D-17 |
| **R3-11(f) D-3** — C-19's denominator counts two records the cell itself declares are **not** firings | The headline reads **30 ledger members + 2 anti-firings, enumerated separately**, with the partition and its command pasted; **G-0.9's closure sweep is keyed to 30** |
| **R3-11(f) E-3 + E-5 + D-6** | **§Sequencing, the NO-WAVE-OWNER packets row** — its transit roster (the `KF-AV-28 · THE STANDING SUPERSESSION RIDER` cell; `:651` at this writing, **parenthetical and non-load-bearing**, R2-7 — **the round-3 header's `:447` was stale by a round-3 edit and is corrected at repair round 4, D-4**) gains the **KF-AV-28 standing supersession rider** for **both** governed packets (transport/ribbon **and** drag-seam) with the C-axis **§7 caution** and the **NON-bespoke** limb verbatim · C-17.R row 4's cell regains ***"owned by KF.W6"*** · G-0.4's two negative probes gain their **scope word** |

### §LAW-A CENSUSES (executed at `origin/master 81a56990`, this seat, read-only)

**The inventory first, because LAW A convicts an unlisted act as readily as a mis-censused one.** This wave authors no code and moves no module. It *carries* two module-motion acts whose consumer sets are load-bearing for its own gates, and neither had an import-graph census before this round — both had a **single-file grep** standing in for one:

**Census 1 — the `backward` module family (N-2's orphan hazard; OP-1's `reset --hard` removes the flat HEAD-layout files).** `M` = the emit/backward module; `P` = `src/animation/compile/emit/backward/` at the frontier, against HEAD's flat `src/animation/compile/emit/{backward,backward-walk,backward-color}.ts`.

```
git ls-tree -r --name-only origin/master src/animation/compile/emit/   → backward/{backward,color,index,walk}.ts
                                                                        + 7 FLAT siblings (css-text · densify · easing-serialize ·
                                                                          entry · index · refusal-probes · view-transition)
                                                                        + format/{format,index,options}.ts  ← a MODULE, not flat
   (count corrected at repair round 4, D-5: the struck "10 flat siblings" counted `format/`'s three as flat;
    the frontier carries them in a sub-dir, and the flat/module split is the very distinction this census turns on)
git ls-tree -r --name-only HEAD          src/animation/compile/emit/   → backward.ts · backward-walk.ts · backward-color.ts (flat) + 8

(1) SPECIFIER census
git grep -nF 'emit/backward' origin/master -- .        → 1 IMPORT hit outside P: test/compile/value4-color-emit.test.ts:9
                                                          → 2 in-source DOCBLOCK hits INSIDE P (non-import context, added at
                                                            repair round 4, D-5 — the struck clause read "all other hits are
                                                            docs/ prose", which is false of these two):
                                                              backward/index.ts:2  " * compile/emit/backward/ — the BACKWARD/emit core module (V.W5 LT-06)."
                                                              backward/walk.ts:2   " * compile/emit/backward/walk.ts — the COMPILE input walkers (K.W10, carved off"
                                                          → every remaining hit is docs/ prose — non-import context, never counted
git grep -nE '"\.{1,2}/backward' origin/master -- .    → emit/index.ts :32 :36 :41 :42 :43 :53 :57   (the zone barrel)
                                                          emit/view-transition.ts :46 :47 :50 :54
                                                          emit/refusal-probes.ts :2   ("./backward/walk")
                                                          backward/index.ts :23 :28   (intra-module)
alias roots that can reach P (vite + tsconfig: @src, @mkbabb/keyframes.js self-alias, @state, @components,
  @utils, @composables, @styles, @kf-engine):
git grep -nE '"@src/animation/compile' origin/master -- .  → 5 hits, NONE into P
      (emit/css-text ×3 · compile/selector ×1 · compile/value ×1 — SIBLING modules, not M)
git grep -nE 'backward-(walk|color)' origin/master -- src/ demo/ test/ scripts/
      → 0 import specifiers; the only in-source hits are stale doc-COMMENTS
        (emit/index.ts:7-8 · emit/densify.ts:17,:70 · backward/backward.ts:158) — non-import context

(2) SYMBOL census (the barrel's exports: compileToCSS · compileChild · cssIdent · colorUnitToOklabCSS ·
    isColorUnit · densifyColorBlock · DEFAULT_DELTA_E_EPSILON · DEFAULT_DENSIFY_STOPS + 5 types)
    the hits enumerated below are each resolved to their own import specifier
    (the enumeration is this seat's, re-run at write time; TWO hits were missing from it before
     repair round 4 and are entered now, marked ← D-5):
      src/animation/public.ts:140 · src/animation/validate.ts:49        → "./compile"      (the ZONE barrel, not P)
      test/engine/color-fidelity.test.ts:41 · test/engine/en-fix-oracle.test.ts:25 → "../../src/animation/compile"
      test/compile/compile-roundtrip.test.ts:42-44                      → "../../src/animation/compile"   ← D-5
            (`import { compileToCSS, DEFAULT_DELTA_E_EPSILON } from "../../src/animation/compile"` —
             the LARGEST consumer of the symbol in the tree and the same class as the two test/engine hits above)
      src/animation/load-engine.ts:47                                   → "./public" (at :57)              ← D-5
            (the symbol rides an import block that closes `} from "./public";` — the PUBLISHED barrel, not P)
      demo/…/KeyframesStringControls.vue:44 · scripts/lib/agent-surface.mjs:176 ·
      test/characterization/stable-surfaces.test.ts:13                  → loadAnimationEngine() (the published barrel)
    → every one of the nine reaches the symbols through `src/animation/compile` or the published barrel;
      ZERO additional DIRECT consumers of P. The CONSUMER SET was always right; the RESOLUTION TABLE was short,
      and an unresolved hit is precisely the failure LAW A exists to catch — committed, before this round, inside
      the block that promulgates it. Per LAW B this arm asserts no completeness in its own voice: the omission was
      found by `PASS-4/KF-W0-CHECK.md` §1 E-5 / §2 D-5 (2026-08-28), and that artifact also verified the export
      roster byte-exact (8 values + 5 types), the specifier arm exactly, and the alias arm exactly.

(3) CONSUMER SET of P = 3 frontier source files (emit/index.ts · emit/view-transition.ts · emit/refusal-probes.ts)
    + 1 test (test/compile/value4-color-emit.test.ts:9). Everything else reaches the SYMBOLS through
    `src/animation/compile`, never through P.
```

**What the census changes.** N-2's hazard is now *bounded and stated by the graph*: because **no frontier specifier traverses the flat `backward-walk` / `backward-color` spelling**, a partial `git checkout origin/master -- src/` leaves three flat files with **zero importers** — dead duplicates that no build error surfaces, which is precisely why only the full `reset --hard` discharges the act. G-0.1's *"diffed, not assumed"* clause is keyed to this: the reconciliation record states the flat triad's removal by path.

**Census 2 — the deleted glass-ui `/header-ribbon` subpath (C-3 / G-0.5; the producer's delete, carried here as a tripwire).** `M` = glass-ui's `header-ribbon` entry; `P` = `@mkbabb/glass-ui/header-ribbon` (producer `src/components/header-ribbon/`, deleted whole at `4bf53962`; the installed 7.0.0 dist still ships `dist/header-ribbon.js`).

```
(1) SPECIFIER census
git grep -nF 'glass-ui/header-ribbon' origin/master -- .
      → demo/components/instrument/shell/EditorShell.vue:116   ← the ONE live import
      → docs/tranches/H/audit/a-glass-ui-consumption.md:48 · docs/tranches/V/audit/R1-15-cross-repo.md:54
        (docs prose — non-import context, never counted)
git grep -n 'header-ribbon' origin/master -- src/ demo/ test/ scripts/   → 1 hit, the same :116

(2) SYMBOL census
git grep -n '\bHeaderRibbon\b' origin/master -- demo/ test/ src/ scripts/
      → EditorShell.vue:16 (<HeaderRibbon placement="right">) · :50 (close) · :116 (the import)
      → resolved: all three are the SAME file and the SAME specifier. No sibling-basename consumer exists.

(3) CONSUMER SET = { demo/components/instrument/shell/EditorShell.vue }, exactly one, tests included.
```

**What the census changes.** G-0.5's blast radius was asserted from a single-file `grep -n header-ribbon` on `EditorShell.vue`; it is now **derived from the import graph and reproduces at one consumer**, so the tripwire's *"the build dies at `:116`"* is a graph fact rather than a sample. A second datum falls out and is booked at C-15: the frontier already reads `<HeaderRibbon placement="right">` at `:16` — the phantom `mode=`/`position=` attr is gone at the frontier and survives only at HEAD, which is exactly C-15's *"a naive reconciliation would carry it forward"* stated by measurement.

**Declared negative, per LAW A's own reach — CONVERTED TO LAW-B CITED FORM AT REPAIR ROUND 5 (D-6), with its enumeration inline (RULINGS-5 R5-11).** The round-3/4 sentence read, in this file's own voice, *"**no other row in §Bounds deletes, repoints or shims a module**"* — a universal over an enumerable set, which is **LAW B's exact ban**, sitting inside the block that promulgates LAW A; round 4 derived it from the column but pointed only *forward* (§Scope receipts → here), so the sentence read first was still the bare universal, and the tally it silently leaned on was the one that did not sum (D-2). **That voice is STRUCK (E-3, loudly).** The negative is not asserted here; it is **measured**, and the measurement is `docs/tranches/X/keyframes/conformance/PASS-5/KF-W0-CHECK.md` §2 D-2 + §3 axis (3) (2026-08-28), whose finding is **quoted rather than re-asserted**: *"the negative HOLDS"* — *"zero §Bounds rows carry `delete`/`repoint`/`shim`/`move`, verified by classifying all 19 access cells"*. **The enumeration it classifies rides beside this sentence rather than one section away** (the G-0.7 form this file already names as lawful; counting rule: *one row per `|`-row of the §Bounds table; the verb is the first access token of the Access cell*): `create ×7 · modify-append ×6 · modify-carve ×1 · OWNER'S HAND ×4 · regenerate-only ×1 = 19`, `delete` / `repoint` / `shim` / `move` **×0** — full coordinates at **§Scope receipts, round 4 · LAW A · access column** (re-summed at repair round 5, D-2; that receipt and this sentence now state each other in both directions). **What the negative means where it holds**: `dist/gh-pages/**` is **regenerate-only build output** (D-12), the two EE-02 paths are **modify-in-place** reconciliations (no path motion), and every remaining bounds row is `docs/**` modify-append. If a later seat adds a delete/repoint/shim row, LAW A binds it and the census rides in that row.

### R2-5 run to completion — the full-surface sweep (RULINGS-3 §R3-11(e))

**THE SUBJECT-IDENTITY CHECK (RULINGS-3 R3-4.3), executed over every symbol-bearing §Bounds row in this file.** R2-5 checked that an anchor *exists*; this checks that an anchor's **content mentions its subject** — the silent-mis-resolution class. Both of this wave's symbol-bearing rows are the owner-hand EE-02 pair, and both return their stated subject:

```
git grep -n 'setAnimationTimingFunction\|timingFunctionLiteralFor' origin/master \
     -- demo/components/instrument/transport/channel-controls/composables/useTimingFunctionEditor.ts
  → :96 setAnimationTimingFunction (decl) · :128 timingFunctionLiteralFor (decl) · :170 both (the call site)
    · :178 timingFunctionLiteralFor · :245 setAnimationTimingFunction (the returned surface)      ✔ IDENTITY HOLDS
git show origin/master:demo/components/instrument/transport/channel-controls/TimingFunctionPanel.vue | sed -n '143,148p'
  → :143 const pts = v.points as [number, number, number, number];
    :144 props.storedAnimationOptions.cubicBezierOptions.controlPoints = pts;     ← the bezier-drag write, as the row states
    :145-147 the EE-02 comment (“attach its faithful cubic-bezier(...) CSS twin — else the readout serialize throws”)
    :148 const timingFunction = {   ← where the twin attaches                                      ✔ IDENTITY HOLDS
```

Both rows keep their coordinates; the `:148` twin-attachment site is added to §Bounds' site-2 row so the owner's reconciliation is diffed against the seam and not the line above it. **No W0 §Bounds row names a symbol its own file does not carry** — the failure R3-4 convicts at a sibling does not reproduce here, and the check is recorded rather than asserted so pass 4 can re-run it.

**The C-1.G re-derivation, re-run with its predicate stated (cures D-5).** Round 2's receipt read *"200 routing lines across 48 records"*. That figure reproduces at **neither** reading, and it is struck:

```
$ cd docs/tranches/V/megatranche/registry/adjudicated
$ ls kf-*.md | wc -l                                                     → 58
$ grep -h -E 'KF\.W0|KF-W0' kf-*.md | wc -l                              → 237     (pre-discard lines)
$ grep -l -E 'KF\.W0|KF-W0' kf-*.md | wc -l                              →  50     (pre-discard records)
# the discard predicate, STATED so it reproduces (taxonomy / routing-law boilerplate):
$ BP='ADJUDICATED, not VERIFIED|agglomeration input|wave sketch|census lane taxonomy|census taxonomy|Status verbs per M-25'
$ grep -h -E 'KF\.W0|KF-W0' kf-*.md | grep -cE "$BP"                     →  64     (discarded)
$ grep -h -E 'KF\.W0|KF-W0' kf-*.md | grep -vE "$BP" | wc -l             → 173     (post-discard lines)
$ per-record survivor count > 0                                          →  44     (post-discard RECORDS)
$ the six records the discard drops whole (their ONLY KF.W0 token is preamble boilerplate):
    kf-AnimationVisualizer · kf-CSSCodeEditor · kf-KeyframeCardList · kf-KeyframeTimeline
    · kf-TimelineHoverPreview · kf-TransportDock
```

**Both readings are pasted, per the ruling.** The **record set is the membership instrument, not the line count**: the 44 reproduces exactly against `PASS-3/KF-W0-CHECK.md §0` (2026-08-28), while that artifact's post-discard **line** figure (167) and this seat's (173) differ by the *spelling of the discard predicate*, not by the substrate — which is why this seat writes its predicate down and keys membership to records. Round 2's 200/48 belongs to a third basis and is retired; that is the SCH-3 defect this wave exists to kill, and it had been committed inside the table that exists to prove the roster was measured.

**Two limits of the instrument, declared rather than papered over** (the KF-W2 D-1 lesson — an exclusion basis derived by **reading**, not token-matching): (i) the sweep's predicate is a *token* match, so a record can route here without the token — `kf-KeyframeCardList`'s real routing line (`:120`, booked at **C-1.F row F-12**) carries no `KF.W0` string at all, and the discard drops that record whole; (ii) six records' only token is boilerplate, so a record's *absence* from the 44 is never evidence that it routes nowhere. The 44 is a floor on membership, and it is used as one.

**What the properly-run sweep lands.** Resolving the 44 against this file's bytes reaches **kf-SequenceScene**, whose four KF.W0 lines carry **two** routed obligations (`:145` and `:153`) — the F-1 family id and the countable-cell inheritance datum, neither present here before this round. Both are booked at **C-1.G row 22**. The remaining thin resolutions were re-checked by bytes and each resolves to a booked row: kf-HeroAurora → KF-HA-6 at C-1/C-16; kf-EasingTarget → C-1.F F-2; kf-EasingSidebar → C-1.F F-3; kf-KeyframesAddDialog → C-17.R (KAD-12, adopted this round).

### LAW B conversions applied in this file

Four sentences asserted closure in this file's own voice; each now cites the artifact and states what it found:

1. **§Carry's preamble** — *"every one greppable by its banked id"* → the carriage claim cites `PASS-3/KF-W0-CHECK.md §0/§1` (2026-08-28) **and names its finding of ten escapes**, six of which this round books.
2. **C-1.G's closing note** — *"N ids that a closure sweep can now resolve"* → restated as a citation to the same artifact plus the re-derivation predicate above.
3. **C-17.R's heading** — *"the mint's complete input set"* → the completeness claim moves to the artifact; the table states its **re-derived** claimant set and the two limits of its instrument.
4. **G-0.7's roster clauses** keep their pasted command block (a measurement riding beside its sentence is the lawful form) and gain the census citation for the *carriage* half.

**Altitude sweep, round 3.** Each cure was swept across every altitude that carries it — a cure landed in one place and left standing in another is *relocated*, not repaired (this file's own round-1 law): **D-1** → C-14's mechanism cell · G-0.10's GREEN · the §Sequencing glass-ui FYI row's letter reference; **D-2** → C-17 (mechanism + cure cells) · C-17.R (four rows + heading + closing note) · §Scope item 6 · G-0.6's GREEN and second falsifier · C-1.G row 17's limb cell (which named `F1(c)` as a separate booking); **D-3** → C-19's headline and G-0.9's GREEN sweep key; **D-5** → C-1.G's heading, intro receipt, table and closing note · §Carry's denominator; **E-3** → **§Sequencing, the NO-WAVE-OWNER packets row** only (the roster is the file's single enumeration of travelling locks; `:651` at this writing, parenthetical and non-load-bearing — **the round-3 sweep wrote `:447`, a bare code fence, and is corrected at repair round 4, D-4**); **E-5** → C-17.R row 4; **D-6** → G-0.4's static tier. **The §Carry denominator is re-counted at the bytes, never computed forward**: 22 (C-1..C-22) + 13 (C-1.F) + **22** (C-1.G, +1) + **5** (C-1.R, +1) + **14** (C-17.R: 13 live claimants **+KAD-12**, plus the row-13 duplicate retained as an **enumerated strike**) = **76 addressable rows**, every one resolvable to a named disposition by a closure sweep. The count is **counted**, not carried: `awk`-scoped row counts over the four sub-tables at the current bytes → C-1.F **13** · C-1.G **22** · C-1.R **5** · C-17.R **14**.

**Status is unchanged: `planned`.** No gate is turned; two gates (G-0.5, G-0.9) gain witnesses derived from the import graph rather than from a sample.

## Repair Round 4 (L-20 conformance, 2026-08-28)

Authority: `docs/tranches/X/keyframes/conformance/PASS-4/RULINGS-4.md` (cross-wave, binding) over `PASS-4/KF-W0-CHECK.md` (local register) — where the two collide the ruling wins. Every measurement written, carried or left standing below was **re-executed at this seat's clock** at keyframes.js `origin/master` (`git rev-parse origin/master` → `81a56990736ced5b5edde0b84c527680ac7689b1`, run this seat, read-only) and at the value.js tree's current bytes; local `8281638c` stays **DISQUALIFIED** and is cited only in the form R3-2 allows. Nothing here stamps a verb, opens product source, or moves a byte outside this file.

**LAW C — THE TWO-STAGE / RECONCILE LAW (RULINGS-4 §LAW C). This file is STAGE 1's FIRST WRITE.** It is the roster source three siblings consume, so it writes before them and they quote it at its **post-round-4** bytes. Two clauses bind every cross-wave sentence in this round: **(2) quote-by-command** — a quoted sibling or corpus sentence names its substrate and rides beside the pasted command that produces it; **(3) forward references are anchor-only** — every X·KF sibling writes AFTER this file, so each is cited here by **stable anchor alone** (§-heading + row/gate id), never by quoted prose and never by a line number. The **RECONCILE seat** re-verifies every cross-wave receipt written this round after the last per-wave write and certifies at ~~`PASS-4/CLOSE-CERT.md`~~ → **`PASS-5/CLOSE-CERT-2.md`** — **citation re-pointed at repair round 5 (D-1), per `PASS-5/RULINGS-5.md` R5-2 and LAW E.** The round-4 certificate's write-order exhibits were falsified by a twelfth spec write **attributed by R5-2 to the round-4 RECONCILE seat itself** (+21 B / +22 s after the cert, a receipt-level rewording of its own insertion); R5-2 rules **round 4's repairs BANK** and its **close certificate VOID as proof-of-order**, and makes **CLOSE-CERT-5 the lawful close of rounds 4 AND 5**, hash-proven under LAW E (write-last by construction · a `shasum -a 256` table of all eleven specs · re-hashed by the pass-6 union seat as its first act). **`PASS-4/CLOSE-CERT.md` is NOT edited and is not re-read as an authority — E-3: prior-pass conformance artifacts are immutable, and the attribution of its falsifier lives in R5-2, never in an edit to it.** **Without that certification the round did not close**, and no sentence in this file is written on its behalf.

**LAW D — THE VERBATIM LAW (RULINGS-4 §LAW D).** Every quotation labelled *verbatim* below is produced by a pasted `grep -n` / `sed -n` at write time; anything else is labelled a paraphrase with its elisions marked. **Numeric arm (LAW D(3))**: every figure printed beside a command is produced by **re-running that command at write time**, a count asserted over an enumeration is stated **at** the enumeration with its counting rule, and dependent cells point at the enumeration rather than re-issuing the numeral.

| Directive | Landing site |
|---|---|
| **D-1** — `D-19`, the corpus's most-cited routing id, reaches no table (the pass's sharpest defect; two of the three records this file never names reach the wave only through it) | **C-1.R row 6** — a carry-not-cure guard: *content carried whole at C-2 · C-21 · G-0.9; NO RE-BOOK; record-qualified because the token is overloaded*, with the census pasted (bare-token **42 records / 94 lines**; re-anchor-context **34 records / 39 lines** by the stated predicate) and **G-0.9's stamp keyed to it** |
| **D-2** — G-0.1's falsifier states an arithmetic denominator (`325 − 252 = 73`) over two sets that do not nest | The falsifier, **G-0.1's GREEN**, **N-1's consequence cell** and the **L-18 rider** all carry the **measured 225** with `comm -13` pasted, the **152 / 124 / 100** counter-figures, and the rule that GREEN requires **both enumerations, never one plus a difference** |
| **D-3** — C-17.R row 13's re-derivation receipt is unreproducible and closes in this file's own voice | The **37 hits / 15 records** raw reading and the **19 lines / 9 records** post-discard reading pasted with the substring-discard rule (`KF-ES-10` · `MISS-10` · `KF-SS-10` are not `S-10`); **`KF-ET-27` ADOPTED as the FOURTH S-10 claimant** (new C-17.R row) with **`KF-ES-20`** as its second provenance leg and *"the design-idioms census arm is KF.W5's"* as its disposition; *"there is no fourth or fifth claimant"* **struck** for the LAW-B form |
| **D-4** — the round-3 landing cells point at `:447`, which is a bare code fence | All **four** `:447` cells re-anchored on **§Sequencing, the NO-WAVE-OWNER packets row**, with the line number parenthetical and declared non-load-bearing (the R2-7 idiom, applied to this file's own change log) |
| **D-5** — LAW A's Census 1 symbol arm is short under its own completeness sentence | The **two unresolved hits resolved to their specifiers** (`test/compile/compile-roundtrip.test.ts:42-44` → `"../../src/animation/compile"`; `src/animation/load-engine.ts:47` → `"./public"` at `:57`); *"10 flat siblings"* → **7 flat + `format/` ×3**; the docs-prose clause qualified for the two in-source comment hits inside `P` |
| **D-6** — G-0.7 receives *"two"* residue families while a third of the same class exists | **`KF-EST-1` named as the THIRD R-8 residue family** at C-12 and G-0.7, its **eight sites** listed with their frontier measurements and **`TypingDots.vue:45`/`:51`/`:59`** entered in the roster by name |
| **F-1 rider landing** (the receiving end of a sibling's round-4 booking — sending end cited **anchor-only**, LAW C(3): `KF-W1.md` §Repair Round 4, directive D4-3) | **C-1.F row F-1** gains the **five component riders verbatim from their banks**, each pasted with the `sed -n` that produces it: `useControlsKeyboardShortcuts.ts:1` (sole binding site of all 18 shortcuts) · `ChannelControls.vue:219` (first-failing import on a clean `npm ci`) · `ControlsPaneWrapper.vue:166` (the demo's sole `/drawer` consumer) · the four toast utility classes · the one-class / eight-custom-property inventory |

### Scope receipts, round 4 (RULINGS-4 R4-10(1) — the sweep's enumeration source is the SCOPE, never the example list)

Three one-line receipts, one per class, because *"a repair that cures the named instance and not the stated scope leaves the law green over its own counterexample."*

- **LAW A · access column — RE-SUMMED AND RE-PASTED AT REPAIR ROUND 5 (D-2; RULINGS-5 R5-11, RECEIPT-ARITHMETIC-FIRST: a scope receipt carries its enumeration AND its counting rule AT the receipt, and a receipt whose own arithmetic does not reproduce is a defect regardless of the scope's truth).** The round-4 partition read *"`create` ×7 · `modify-append` ×7 · `modify-carve` ×1 · `OWNER'S HAND` ×4 · `regenerate-only` ×1"* over a stated denominator of **19**: **7 + 7 + 1 + 4 + 1 = 20**. **That partition is STRUCK, loudly (E-3 — the strike is declared, not silently absorbed): `modify-append` is ×6, not ×7.** The corrected receipt, with its counting rule and its row coordinates beside it so it reproduces:

  **Counting rule** — *one row per `|`-row of the §Bounds table (the header row and its separator excluded); the verb is the **first access token** of the Access cell.* **Enumeration, at the enumeration** — §Bounds, the table between the `## Bounds` heading and the `**Do NOT touch**` paragraph. **Rows are keyed by ORDINAL, which does not drift**; line numbers ride in parentheses and are **declared non-load-bearing** (the D-4 idiom this file already applies to its own change log — the §-heading plus the first-access-token rule is the anchor, and the round-5 writes above this section moved every one of these numbers, which is precisely why the ordinal is the key):

  ```
  19 rows total (ordinal · line at this writing)
    create          ×7   1(:331) 2(:332) 3(:333) 4(:334) 5(:335) 6(:336) 7(:337)
    modify-append   ×6   8(:338) 9(:339) 10(:340) 12(:342) 13(:343) 14(:344)
    modify-carve    ×1   11(:341)
    OWNER'S HAND    ×4   15(:345) 16(:346) 17(:347) 18(:348)
    regenerate-only ×1   19(:349)
    delete / repoint / shim / move  ×0
  7 + 6 + 1 + 4 + 1 = 19        ← sums to its own denominator
  ordinals 1..19 each appear exactly once, in order   ← partition is a partition
  ```

  **The negative half re-verifies unchanged**: **zero rows carry `delete` / `repoint` / `shim` / `move`**, so the wave's whole LAW-A surface is the **two carried module-motion acts** already censused at §LAW-A CENSUSES (Census 1 `emit/backward`, Census 2 glass `/header-ribbon`). **19 rows enumerated, 2 censuses present, 0 owed.** The declared negative at the end of §LAW-A CENSUSES is derived from **this** column — and, from repair round 5, is stated there in LAW-B cited form with this enumeration inline (D-6), so the sentence a later seat reads first is no longer the bare universal.
- **SUBJECT-IDENTITY · every symbol-bearing row and gate-witness symbol**: re-run this seat at `81a56990` — `TABS_EXTERNALLY_MANAGED_KEY` → `demo/app/App.vue:176` `provide(TABS_EXTERNALLY_MANAGED_KEY, true)` ✔ (**and the bare leaf is path-qualified by this sweep: the file is `demo/app/App.vue`, not a root `demo/App.vue` — `git show origin/master:demo/App.vue` → `fatal: path … does not exist`**; C-22, G-0.9 and the §Sequencing KF.W6 row now carry the path) · `useKeyframesParsing.ts:97` = `() => animation.templateFrames.length,` ✔ · `KeyframeCardList.vue:11` = `:frame-start="startScalar(frames[i].start)"` ✔ (**path-qualified this sweep: `demo/components/instrument/keyframes/components/KeyframeCardList.vue`**) · `CopyButton.vue:42` = `timingFunction: "easeInBounce",` ✔ · `EditorShell.vue:116` = `import { HeaderRibbon } from "@mkbabb/glass-ui/header-ribbon";` ✔ · the EE-02 pair re-verified at round 3 and unchanged ✔. The remaining gate-witness symbols were re-executed axis-by-axis at `PASS-4/KF-W0-CHECK.md` §3 axis (2) (2026-08-28), whose finding is quoted rather than re-asserted: *"Not one anchor resolved to the wrong subject"*, with the only failures being **self**-anchors (D-4) and the two unresolved LAW-A symbol hits (D-5) — both cured this round.
- **NUMERIC ARM · every pasted command re-run at write time**: the round-4 figures below are this seat's own re-runs, not carried — `git rev-list --count HEAD..origin/master` → **41** · `origin/master..HEAD` → **1** · `git status --short | wc -l` → **252** · `git diff --name-only origin/master | wc -l` → **325** · `git ls-files --others --exclude-standard | wc -l` → **124** · `comm -13` → **225** · `comm -23` → **152** · `comm -12` → **100** · `grep -l 'D-19' kf-*.md | wc -l` → **42** · `grep -n 'S-10' kf-*.md | wc -l` → **37** · post-discard → **19**. Where a round-3 figure is re-stated below it is re-run, not inherited.

**Altitude sweep, round 4.** Each cure was swept across every altitude that carries it — a cure landed in one place and left standing in another is *relocated*, not repaired (this file's round-1 law, and R4-10's re-statement of it): **D-1** → C-1.R (row 6 + heading + preamble) · §Carry's denominator · G-0.9's GREEN and RED baseline; **D-2** → G-0.1's falsifier **and** GREEN · N-1's consequence cell · the L-18 rider — all three now carry the measured **225** with `comm -13` beside it, and the struck arithmetic survives only where it is **named as struck** (this row and the three cure sites); this spec schedules no disposition against the subtracted figure, and the receipt is written in that form rather than as a self-falsifying zero (the R4-11 D4-6 idiom, borrowed); **D-3** → C-17.R (heading · preamble · the new row · row 13's receipt · the closing note) · C-17's mechanism cell **and its provenance ⟨…⟩ list** · §Scope item 6 · §Carry's denominator · **G-0.6's second falsifier** (the fifth altitude carrying the roster's shape — and its predicate is widened there from *"the wave's own carry"* to *"the corpus OR the carry"*, since a falsifier scoped to one source stays green over the other) · the §Sequencing **KF.W5** row (the census arm's owner declared from this end) and **KF.W10** row (the roster stated at its final shape, so the consuming sibling quotes fourteen live rows and not round 3's thirteen); **D-4** → all four `:447` cells (round-3 directive table · round-3 altitude sweep · §Carry preamble · C-1.G's closing note); **D-5** → §LAW-A CENSUSES Census 1, arms (1) and (2) and the flat-sibling parenthetical; **D-6** → C-12's INBOUND clause · G-0.7's residue paragraph · G-0.7's roster block; **F-1 riders** → C-1.F row F-1 only (the row that names `KF.W1-DEP`); **R4-10's SUBJECT-IDENTITY arm** → the two bare leaves the sweep surfaced are path-qualified at **all four cells that carry them** — C-22's correction (i) · G-0.9's GREEN (i) · the §Sequencing KF.W6 INBOUND row (i) · C-6's frontier cell — because a path-qualification landed at one cell and left standing at three is the relocation this sweep exists to prevent.

**Status is unchanged: `planned`.** No gate is turned; no verb is stamped; G-0.9 gains one keyed obligation (`D-19`) and G-0.1 gains a measured falsifier where it carried an arithmetic one.

## Repair Round 5 (L-20 conformance, 2026-08-28)

Authority: `docs/tranches/X/keyframes/conformance/PASS-5/RULINGS-5.md` (cross-wave, binding) over `PASS-5/KF-W0-CHECK.md` (local register) — where the two collide the ruling wins, and it collides once: the check routes `L-M-9` to **C-1.G row 23**, the ruling routes it to **C-1.R**; the ruling's home is the one taken, and the reason it is the better home is written into the row (the id was *present and mis-resolving*, not absent — C-1.R's class, not C-1.G's). Every measurement written or re-stated below was **re-executed at this seat** at keyframes.js `origin/master` (`git rev-parse origin/master` → `81a56990736ced5b5edde0b84c527680ac7689b1`, read-only) and at the value.js tree's current bytes; local `8281638c` stays **DISQUALIFIED**. Nothing here stamps a verb, opens product source, or moves a byte outside this file. **This file writes FIRST of the eleven (LAW C stage 1).**

**LAW E — THE HASH-PROVEN CERT LAW (RULINGS-5 §LAW E), as it binds a stage-1 per-wave seat.** (1) The round's closing certificate is **`PASS-5/CLOSE-CERT-2.md`**, written by the RECONCILE seat as its **absolutely last act** and proved by a `shasum -a 256` table over all eleven specs, re-hashed by the pass-6 union seat as its first act — **an mtime table is a claim about the past; a hash table is a challenge to the future**, and this round's opening hashes are pinned in RULINGS-5 §LAW E(3). (4) **ANCHOR-ONLY EVERYWHERE, amending LAW C(2) for per-wave seats**: every same-round cross-wave receipt this file writes is **§-heading + row/gate id — no quoted sibling prose, no line numbers, and NO per-seat substrate mtime stamps.** Per-seat stamping is retired (it was the mechanism behind nine of the drift tail's sixteen rows: seats stamp clocks they cannot keep); **a seat that needs a sibling's words asks the cert for them.** This round's three new cross-wave receipts — the two KF.W6 rows and the KF.W10 terminus row at §Excluded — are written in exactly that form, and this file stamps **no** sibling's bytes.

**LAW F — THE SHADOW LAW (mechanism S, RULINGS-5 §LAW F).** Every act that moves, strikes, drops, re-keys, narrows, re-points or mints a row/routing carries its **SHADOW line in the same edit** — the set, each id against **its own banked anchor** (never by co-id twin, host file or fold-carrier), the disposition per id, and the citing/terminus waves notified. This wave performs **one** such act this round — the §Excluded EH-family routing declaration (D-5) — and its SHADOW line lands beneath that table. **Nothing else in this round strikes, moves or drops a row**: D-2 and D-6 strike *sentences of this file's own* (a partition that did not sum, a universal in the wrong voice) and both strikes are declared loudly at their sites per E-3; D-3 and D-4 **add** handles and spend no cure.

| Directive | Landing site |
|---|---|
| **D-1** — the round closes on a certification the tree refutes (`KF-W10.md` wrote **after** `PASS-4/CLOSE-CERT.md`, +21 B / +22 s) | **§Repair Round 4, the LAW C paragraph** — the sole `CLOSE-CERT` citation in this file, re-pointed to **`PASS-5/CLOSE-CERT-2.md` + RULINGS-5 R5-2**, which **attributes** the twelfth write to the round-4 RECONCILE seat itself, banks round 4's **repairs**, voids its **certificate as proof-of-order**, and makes CLOSE-CERT-5 the lawful close of rounds 4 **and** 5. `PASS-4/CLOSE-CERT.md` is **not edited** (E-3) |
| **D-2** — the R4-10(1) LAW-A scope receipt's own verb tally does not sum to its own row count (`7+7+1+4+1 = 20` over a denominator of **19**) | **§Scope receipts, round 4 · LAW A · access column** — the partition **struck loudly**, re-summed `create ×7 · modify-append ×6 · modify-carve ×1 · OWNER'S HAND ×4 · regenerate-only ×1 = 19`, with the **counting rule** (*one row per `\|`-row; the verb is the first access token of the Access cell*) and the **row coordinates pasted** so it reproduces (R5-11) |
| **D-3** — `L-M-9`, the one F-1 fold with no row in either fold table, whose single hit resolved to the RETIRED-probe limb | **C-1.R row 7** (the fold indexed by id, both limbs separated, NO RE-BOOK) **+ C-1's *"Probes RETIRED here"* cell record-qualified** to the probe sub-clause — the two limbs are separable by grep from this round |
| **D-4** — `AGG-P1`, the probe that DECIDED SCH-1, absent from the wave that re-runs SCH-1 | **C-1's identity chain** — the sixth spelling named at the SCH-1 cell it decided, as the **census-probe id it is** (not an obligation), with its `CENSUS-2026-08-03.md` `:9`/`:32`/`:62` home and the head record's two citations quoted by command |
| **D-5** — the EH family is routed to three homes, W0 first, and W0 books one of six | **§Excluded — three new rows + the family partition + the SHADOW line** (R5-1(7)): `EH-4/5/8` **KF.W6 and LIVE** (the round-4 MOOT-ON-DELETE premise **struck at R5-1**), `EH-9/10` KF.W6 discharged-by-twin, `EH-2/3/12/13/14/15` **NO-WAVE-OWNER** under G-2's terminal sweep, `EH-1` W0's own and booked — **and the four ids outside R5-1's set (`EH-6` · `EH-7` · `EH-11` · `EH-16`) carried at their banked dispositions**, so the partition covers the **sixteen** ids a command derives at the bank rather than the eleven the ruling names. All sibling citations **anchor-only** |
| **D-6** — the §LAW-A declared negative asserts closure in the file's own voice, and its derivation points only one way | **§LAW-A CENSUSES, the declared negative** — the universal **struck**, restated in **LAW-B cited form** against `PASS-5/KF-W0-CHECK.md` §2 D-2 / §3 axis (3) with its finding quoted, and the access-column enumeration + counting rule carried **inline beside the sentence** (the G-0.7 form), so the receipt and the negative now state each other in both directions |

### Scope receipts, round 5 (RULINGS-5 R5-11 — RECEIPT-ARITHMETIC-FIRST: the enumeration, the counting rule and the command AT the receipt; a receipt whose own arithmetic does not reproduce is a defect regardless of the scope's truth)

- **ARITHMETIC · every count this round asserts, re-summed at its own enumeration.** `§Bounds` access column: `7 + 6 + 1 + 4 + 1 = 19` ✔ (rule + coordinates at the receipt, D-2). `§Carry` denominator: `22 + 13 + 22 + 7 + 15 = 79` ✔ (rule: one row per `|`-row per sub-table, headers excluded, the enumerated-and-struck C-17.R duplicate counted; **+1 against round 4's 78**, and the +1 is named — C-1.R row 7). The **EH partition** (§Excluded, the family paragraph): `1 (EH-1 → W0) + 3 (EH-4/5/8 → KF.W6 live) + 2 (EH-9/10 → KF.W6 by twin) + 6 (EH-2/3/12/13/14/15 → NO-WAVE-OWNER) + 4 (EH-6/7/11/16 → their banked homes, outside R5-1's set) = **16** ✔` against a denominator derived **by command** at the bank, not asserted — `grep -oE '\*\*EH-[0-9]+' kf-EditorHeader.md | sort -u` → **EH-1 … EH-16, sixteen ids**. **This seat's own first draft of this receipt summed a twelve-id partition and called the family ten; it was caught by re-running the denominator at the bank before the round closed, and the corrected form is the one above** — recorded rather than absorbed, because R5-11 exists precisely because the fifth consecutive partial round failed inside its own receipts. **Stated as the enumeration and never as a numeral elsewhere**: the ids are listed one by one at §Excluded, the partition is read off that list, and **no other cell in this file re-issues an EH count**.
- **CROSS-WAVE FORM · every same-round sibling receipt written this round is ANCHOR-ONLY (LAW E(4)), enumerated here so the RECONCILE seat can verify the set whole.** **Three**, all at §Excluded: (i) **`KF-W6.md` §Excluded — the re-cut MOOT-ON-DELETE row and its restored `EH-4`/`EH-5`/`EH-8` rows**; (ii) **`KF-W6.md` §Excluded — the same row's discharged-by-twin clauses for `EH-9`/`EH-10`**; (iii) **`KF-W10.md` §E — the `NWO-TERMINAL-SWEEP` row · gate `G-2`**. **Counting rule**: one receipt per ⟨sibling file : anchor⟩ pair introduced *this round*; pre-existing cross-wave anchors are not re-counted. **No quoted sibling prose, no sibling line numbers, and no sibling mtime/byte stamps appear anywhere in this file's round-5 writes** — the stamps live at CLOSE-CERT-5.
- **SUBSTRATE · what this round measured, and where.** keyframes.js `origin/master` `81a56990` (read-only) for the frontier facts carried unchanged; the **corpus at `docs/tranches/V/megatranche/registry/adjudicated/`** for the two quote-by-command receipts this round adds — `grep -n 'L-M-9' kf-KeyframesStringControls.md` (C-1.R row 7) and `grep -n 'AGG-P1' kf-AmigaScene.md` (C-1) — and `docs/tranches/V/megatranche/formation/keyframes/CENSUS-2026-08-03.md` `:9`/`:32`/`:62` for `AGG-P1`'s own home. **No gate's RED baseline was re-run this round and none is re-stated**: round 5 touched no gate, so every gate's pasted baseline stands at its round-4 measurement, unchanged and undisturbed.

**Altitude sweep, round 5.** A cure landed in one place and left standing in another is *relocated*, not repaired (this file's round-1 law): **D-1** → the LAW C paragraph's certification clause (the file's **only** `CLOSE-CERT` site — `grep -c 'CLOSE-CERT'` → 1 before this round, so the sweep is the site); **D-2** → the §Scope-receipts partition **and** the §LAW-A declared negative that leans on it (D-6's site — the two now cite each other, which is why the pair had to move together); **D-3** → **C-1.R row 7** · **C-1.R's heading** (6 → 7 rows) · **C-1's retired-probe cell** (record-qualified) · **§Carry's denominator** (78 → 79, re-counted not carried); **D-4** → **C-1's identity chain** (the SCH-1 cell it decided — the chain is this file's single enumeration of the identity's spellings, so one site is the whole altitude); **D-5** → **§Excluded's three new rows** · **the family-partition paragraph** · **the SHADOW line**; **D-6** → **§LAW-A CENSUSES' declared negative** · the back-pointer added at the §Scope receipt. **The two ids this round adds are swept against the tables that must not double-book them**: `L-M-9` is booked **once**, at C-1.R, with C-1's cell disposing only the retired probe (C-1.G is **not** given a row — the fold's home is one, and two rows for one fold would be the re-booking C-1.G's charter forbids); `AGG-P1` is booked **once**, at C-1, and is **not** given a C-1.R row, because a probe id is not a routing this spec states differently from its bank.

**Status is unchanged: `planned`.** No gate is turned; no verb is stamped; no gate baseline is re-run or re-stated; no product source is opened. The wave's ten gates stand exactly as round 4 left them.

## State

**Name**: KF.W0 — Substrate Settle (owner-hand §B-12 reconciliation of the sacred stale checkout)
**Lane**: X·KF · **Half**: **library** (authored from the library half's seat; the substrate settle is *indivisible* and binds both halves, so §Sequencing declares the demo-half edges from this end rather than partitioning bytes that cannot be partitioned).
**Opens after**: sub-tranche X·KF opens **AND** the owner's begin-word for the §B-12 act. No predecessor wave; this is the lane's first-executing file.
**Agents**: 5 units — OP-1 is the owner's act (not a seat) · then 2 parallel (.b ∥ .c) · then 2 parallel (.d ∥ .e). All Opus implementation seats (M-12/M-23); nothing here is design content.
**Hard gate**: ten conditions G-0.1..G-0.10, **all ten RED at authoring**, every baseline re-measured by the fold seat on **2026-08-28** with the command and its literal output pasted below.
**Status**: **planned**

| verb | value | evidence |
|---|---|---|
| AUDITED | **YES** | 58 adjudicated `kf-*.md` records (`ls registry/adjudicated/kf-*.md \| wc -l` → 58, 2026-08-28) + `CENSUS-2026-08-03.md` + `INTAKE-ADJUDICATION-2026-08-03.md` |
| SPECIFIED | **YES — by this file, 2026-08-28** | validated against `docs/precepts/` latest (L-20) |
| IMPLEMENTED | **NO** | stamped at this wave's own close |
| VERIFIED | **NO** | stamped only at KF.W10's sub-tranche release close (R-A) |

**Goal criterion.** This wave succeeds if, when work ends, **every adjudicated X·KF record names the ref it cures against, that ref is one ref, and no later wave can cite a count, a gate, a glass-ui citation, or a Codex denominator that was measured on a tree nobody named.** It changes no product behaviour: the only product bytes that move are moved by the owner's reset, toward the frontier, never away from it.

**COMPLETABLE**: **YES, once open.** Every unit but OP-1 writes `docs/**` only. OP-1's product-byte motion is a `reset --hard` toward an already-published ref — no authoring, no migration, no producer dependency. Nothing waits on glass-ui 8.0.0, on a producer release, or on a sibling wave. A row that returns *stale-by-substrate* is a complete result, not a deferral.

## Scope

1. Reconcile the sacred checkout to `origin/master` `81a56990` by the owner's hand, with the 1-ahead local commit and the full reconciliation surface each explicitly dispositioned (kept / committed / discarded) in a written record — both denominators stated (252 status-vs-HEAD · 325 diff-vs-frontier · 124 untracked).
2. Land one glass-ui declaration across all four coordinates (`HEAD` / `origin/master` / worktree / installed) and **rule** on the uncommitted deletion and on `legacy-peer-deps=true` versus the two genuinely-absent peers.
3. Reconcile the three lagging disk files **disk←master**, never master←disk, so the landed EE-02 css-twin cannot be clobbered by a stale-WIP commit.
4. Re-run EE-01 · FE-3 · EE-03 on the reconciled tree as the rebase oracle.
5. Re-run SCH-1..SCH-7 and X-2 at `origin/master`, each with its command and literal output pasted and dated.
6. Mint fresh non-colliding ids for every proposed shadow-census row **in ONE motion over the claimant roster at §Carry C-17.R — TEN S-9 claimants and FOUR S-10 claimants — fourteen LIVE rows, plus one row retained as an enumerated strike, as re-derived at repair round 3 and EXTENDED at repair round 4** (R-5/D-1; the fourth S-10 claimant is **`KF-ET-27`** — the adopted `design-idioms.css` census arm, with `KF-ES-20` as its second provenance leg and the arm itself KF.W5's act — booked at repair round 4 under D-3, where the roster's counting rule and both S-10 readings are pasted; the round-2 shape *"≥8 / ≥4"* is superseded by measurement — `KAD-12` adopted from the sole carry, the anonymous fourth S-10 named as a duplicate of `KF-KE-21` and struck; the earlier "four-way S-9 collision + the requested S-10" framing is **struck at every altitude of this file** — a mint run over four-plus-one re-collides on its first motion, since `kf-KeyboardShortcutsModal:29` already renumbered a second "S-10" on top of `kf-KeyframesEditor:63`'s and `kf-SquareInstrument:78`'s). Each minted id cites its originating record; each dead or refused claimant is **enumerated, not minted**. **Claims are inputs to the mint; a claim is never a slot**, and no wave other than KF.W0 mints or renumbers a census slot.
7. Add the cascade-coupled census column beside the import census, **before** F-1's remediation scopes its regression surface; re-scope the PRM roster and the z-index cells in the same motion — **the PRM roster is `lane-frontend.md` §6.5 (13 sites / 12 files) plus its counts-table tally row, NOT a CENSUS section** (D-4, repair round 2; CENSUS holds one pointer cell, corrected by appended note).
8. Book one authoritative, dated gate roster — **3 npm `proof:*` entry points over the frontier's 9 `scripts/gates/` files** (R-17; the "surface, visual" two-script figure is a stale-worktree reading) — and **ROSTER + ROUTE** (never strike) every prose citation of a gate that does not exist, over the re-derived universal: **53 dead names** at `origin/master` (54 distinct `proof:*` names in `demo/`, of which only `proof:publish` is runnable), **116 prose hits across 51 files** — not four names over 10 sites (R2-17, cures **D-1**/**D-2**). The four the bank and the fold name (`proof:brittleness` → **cure KF.W6** per R2-13 · `proof:font-census` · `proof:styling-idioms` · `proof:demo-no-oversize` at **16 hits / 13 files, `demo/`-scoped**, with the repo-wide 121/70 declared loudly out-of-scope) and the **three R-8 residue families enumerated at C-12/G-0.7** — kf-AnimationControlsGroup **D-13**'s seven · kf-DemoGlobalChrome **L-6**'s 16×13 · **kf-EditorStartScreen `KF-EST-1`'s eight sites over six names, entered at repair round 4 (D-6)** — are worked examples inside that 53, not the denominator. **Each name is struck-in-its-owner-wave or routed-with-a-named-owner; this wave writes `docs/**` and strikes nothing.** One roster, one owner, one sweep.
9. Stamp a ref-of-record on every adjudicated record and reconcile the contradicting cells by **appended** correction (E-3 — dated evidence is never rewritten).
10. Re-derive every surviving `glass-ui/src/**` citation against the consumed dist, or strike it; run one fresh **`npm run gh-pages`** (R-11 — the script is `package.json:43` `"gh-pages": "vite build --mode gh-pages"`; **no `build:gh-pages` exists at any coordinate**) so the byte-offset receipts stop riding a Jul-16 artifact.
11. Disposition the Codex v8 contract pin — re-anchored, or declared stale-by-substrate and never citable for a live count — and **name** owner row OG-KF1 for KF.W10.

### Triumvirate Dispatch (mandatory, not optional)

- **The reset performed by any seat other than the owner**, or opened without the begin-word — invalidates the wave outright.
- **File-bound expansion**: any write under `/Users/mkbabb/Programming/keyframes.js/{src,demo,test,scripts}/**` beyond the two EE-02 paths, or any write to a producer repository. Product bytes in a substrate wave invalidate it. (`e2e/**` is struck from this clause — **no `e2e/` directory exists at any coordinate**: `git ls-tree --name-only origin/master` lists `.changeset .claude … bench demo docs … scripts src test`, no `e2e`. A phantom path in a bounds clause is the same defect class the wave exists to kill — D-12.) **`dist/gh-pages/**` is build OUTPUT, not authored product bytes** — G-0.10's one `npm run gh-pages` regenerates it and that regeneration is in-bounds by the row below; a hand-edit of any file under it is not.
- **A blanket re-anchor.** Any pass that applies one substrate direction across all 325 files halts — KF-TD-2 proves the direction is per-file (§Carry C-21).
- **A MANIFEST MISMATCH after the settle** (the four coordinates still disagreeing) is an owner return, not a local repair.
- **Third diagnostic iteration** on any single gate.

## Bounds

Reconciled against the CARRY `boundsFiles` (paths re-homed per F-R1). Owner-hand rows are marked; **none of them may be touched before the begin-word**.

| File | Access |
|---|---|
| `/Users/mkbabb/Programming/value.js/docs/tranches/X/keyframes/waves/KF-W0.md` | **create** — this spec (the CARRY named `X/kf/waves/W0.md`; re-homed at fold to the lane's real dir, beside `KF-W4.md`/`KF-W7.md`) |
| `…/docs/tranches/X/keyframes/W0/SUBSTRATE-SETTLE-2026-XX-XX.md` | create (dated, append-never-rewrite: the disposition record for the 1-ahead commit + every row of the reconciliation surface) |
| `…/docs/tranches/X/keyframes/W0/MANIFEST-RULING.md` | create (the four-coordinate ruling + the `legacy-peer-deps` / absent-peers ruling) |
| `…/docs/tranches/X/keyframes/W0/COUNTS-2026-XX-XX.md` | create (SCH-1..7 + X-2, command + literal output per row) |
| `…/docs/tranches/X/keyframes/W0/GATE-ROSTER.md` | create (the authoritative dated gate inventory) |
| `…/docs/tranches/X/keyframes/W0/REF-OF-RECORD.md` | create (the 58-record stamp ledger + the appended-correction index) |
| `…/docs/tranches/X/keyframes/W0/V8-DISPOSITION.md` | create (X-1's ruling; OG-KF1 named, not ruled) |
| `…/docs/tranches/V/megatranche/formation/keyframes/CENSUS-2026-08-03.md` | modify-append (SCH re-run · the cascade-coupled legend column · X-2's 185 · **the single `§6.5` pointer cell** — *"delegation sites unverified (FE §6.5)"* — corrected by appended note). **The §6.5 PRM re-scope itself is NOT here** (D-4, repair round 2): `grep -n '6\.5' CENSUS-2026-08-03.md` → one hit, and it points away. A bounds row naming a surface that is not there manufactures false coverage — the same class as the struck `e2e/**` phantom (D-12) |
| `…/formation/keyframes/lane-frontend.md` | modify-append (F-1 row disposition; the S-1..S-8 shadow census — `^### S-` = 8, S-1 `:264`…S-8 `:387` — incl. the minted fresh ids; the `:194` EditorHeader roster row; **§6.5's PRM roster re-scope — 13 enforcement sites across 12 files, the §-heading is the anchor — and its counts-table tally row**, re-homed here at repair round 2 with the z-index cells in one motion, C-10/G-0.6) |
| `…/formation/keyframes/lane-library.md` | modify-append (SCH-3/4/5/7 re-anchor: the 13-site parse-seam inventory, the four dead Tier-A paths, the depcruise note restated as unverified-at-HEAD) |
| `…/formation/keyframes/lane-docs.md` | modify-carve (§B-12 row 16 disposition cell only; `:145`'s live-unexecuted-maintenance-act framing untouched) |
| `…/megatranche/audit/codex-provenance/INTAKE-ADJUDICATION-2026-08-03.md` | modify-append (X-1's disposition; the KF.W0 carried-rows table's outcome column) |
| `…/megatranche/registry/adjudicated/kf-*.md` (all **58**) | **modify-append only** — ref-of-record stamp + DISSENT-3 reconciliation, **E-3: never a rewrite of dated evidence** |
| `…/docs/tranches/V/coordination/INBOX.md` | modify-append (E13: the KF-APP-59 unmet-peers FYI, the EH-1 letter-vs-artifact rider, the KF-APP-5 producer-half-MOOT update — the three relay packets of §Sequencing) |
| `/Users/mkbabb/Programming/keyframes.js/package.json` | **OWNER'S HAND**, post-begin-word only (the 7.0.0 exact devDep + the kf 6.0.0 / value.js 4.0.0 pairing) |
| `/Users/mkbabb/Programming/keyframes.js/package-lock.json` | **OWNER'S HAND**, post-begin-word only (the three glass-ui entries incl. lock `:612` integrity + `dev:true`) |
| `…/keyframes.js/demo/components/instrument/transport/channel-controls/composables/useTimingFunctionEditor.ts` | **OWNER'S HAND** — EE-02 site 1, reconcile disk←master |
| `…/keyframes.js/demo/components/instrument/transport/channel-controls/TimingFunctionPanel.vue` | **OWNER'S HAND** — EE-02 site 2 (`:144` bezier-drag write; **the css-twin attaches at `:148`, the `const timingFunction = {` object — SUBJECT-IDENTITY re-verified at `origin/master` at repair round 3, RULINGS-3 R3-4.3**: `:143` builds `pts`, `:144` writes `…cubicBezierOptions.controlPoints = pts`, `:145-147` carry the EE-02 comment, `:148` opens the twin. The row's stated subject is present at the row's stated line, and the seam the reconciliation must be diffed against is named beside it), reconcile disk←master |
| `/Users/mkbabb/Programming/keyframes.js/dist/gh-pages/**` | **regenerate-only** (D-12) — G-0.10's single `npm run gh-pages` rewrites this tree wholesale; it is generated output, **git-ignored at the frontier — receipt corrected at repair round 2 (D-8)**: `git show origin/master:.gitignore` → `:10` `dist/`. The previously cited `package.json` `files` array (`:30-33` — `"dist"` · `"!dist/gh-pages"` · `"!dist/_*"`) is the **npm PUBLISH exclusion**, a neighbouring fact and not a git one; a bounds row whose citation proves the fact next door is the provenance defect **G-0.10** exists to kill, inside this wave's own access contract. Both receipts are now stated, each against its own claim. It is the wave's one byte-producing act. **No file under it is ever hand-edited**, and the artefact banked is the build hash, not the bytes. Naming it here closes the hole the Triumvirate Dispatch's "product bytes in a substrate wave invalidate it" would otherwise leave ambiguous. |

**Do NOT touch**: `keyframes.js/src/**`, `demo/**` (beyond the two EE-02 paths), `test/**`, `scripts/**`; **any producer repository** — `/Users/mkbabb/Programming/glass-ui/**` is read/hash only, and `node_modules/@mkbabb/glass-ui/**` likewise; `value.js/scripts/dev/dev.sh` (unowned, never touched); every `docs/tranches/X/waves/W*.md` (X·V's own files), `docs/tranches/X/parse-that/**` (X·P's), and the sibling `X/keyframes/waves/KF-W4.md` / `KF-W7.md` (their own waves' files). **`e2e/**` is NOT listed** — it does not exist (D-12); a do-not-touch entry for a phantom path manufactures false coverage. If keyframes.js's own tranche V ever creates `e2e/`, it is out of bounds by the `demo/**`-class rule, not by name.

**Disjointness.** OP-1 runs **alone and first** — it is the only unit that moves a product byte, and every other unit reads what it moves. After it: `.b` → `MANIFEST-RULING.md` + `INBOX.md`; `.c` → `SUBSTRATE-SETTLE-*.md` (the two EE-02 paths are the owner's, verified-not-written by `.c`); `.d` → `COUNTS-*.md` + `CENSUS` + both lane files; `.e` → `GATE-ROSTER.md` + `REF-OF-RECORD.md` + `V8-DISPOSITION.md` + the 58 records + `INTAKE-ADJUDICATION` + `lane-docs.md`. No two concurrent units share a `modify` path. `INBOX.md` is appended by `.b` alone, once, at close.

## Carry

Every CARRY row lands as a gate, a bounds item, or a named cure with its locks. `⟨…⟩` = provenance. **22 rows** (C-1..C-21 from the CARRY, all carried, 0 dropped; **C-22 adopted this round under R-19b**), plus **four id-keyed sub-tables** minted by repair round 1: **C-1.F** (13 fold-identity rows ADOPTED — ids and riders both previously absent, R-1a + R-19b) · **C-1.G** (**22** fold ID-INDEX rows — content already carried at C-1, ids previously absent by bytes; 13 at round 1 from KF-W0-CHECK §2.C, **8 added at repair round 2 by re-derivation from the bank** (R2-5(g)), **1 added at repair round 3 by the properly-run 44-record sweep** — kf-SequenceScene's F-1 family, D-5/E-1) · **C-1.R** (**7** declared re-routes, corrections and no-re-book guards, loud per E-3; the fifth added at repair round 3, E-6, **the sixth — the `D-19` re-anchor-law guard — at repair round 4, D-1**, **the seventh — the `L-M-9` fold-index-and-limb-disambiguation row — at repair round 5, D-3**) · **C-17.R** (**15 rows = 14 live census-slot claimants + 1 enumerated-and-struck duplicate**, as re-derived at repair round 3 and **extended at repair round 4 by `KF-ET-27`, the fourth S-10 claimant (D-3)** — `+KAD-12` adopted from the sole carry; the anonymous row-13 duplicate **struck but KEPT IN THE TABLE as an enumerated strike**, because enumeration is what stops the next seat re-raising it, and a struck row still resolves to a named disposition under a closure sweep; R-5/D-2). Sub-table row counts are stated so a closure sweep has a denominator — and the denominator is **re-counted at the bytes, never carried forward by arithmetic** (R2-5(g); round 1's stated **65** was arithmetic over a roster that was itself short, which is how the count outlived the escape it hid): **22 (C-1..C-22) + 13 (C-1.F) + 22 (C-1.G) + 7 (C-1.R) + 15 (C-17.R) = 79 addressable rows** — **re-counted at the bytes at each repair round by table-row scan, never carried forward** (counting rule: one row per `|`-row of each sub-table, header rows excluded, the enumerated-and-struck C-17.R duplicate counted because a struck row still resolves to a named disposition under a closure sweep). Round 4's **78** was correct for round 4's tables and round 3's **76** for round 3's — **the single round-5 addition is C-1.R row 7, the `L-M-9` fold-index row (D-3)**; the two round-4 additions were C-1.R row 6, the `D-19` guard (D-1), and C-17.R's `KF-ET-27` (D-3); round 2's 73 was correct for round 2's. **This preamble asserts no closure in its own voice (LAW B, repair round 3; citation re-pointed at repair round 4)**: the id-keyed carriage of these tables is measured at **`docs/tranches/X/keyframes/conformance/PASS-4/KF-W0-CHECK.md` §0/§1/§3 (2026-08-28)** — which reproduced the round-3 denominator exactly by table-row scan (**76** = 22/13/22/5/14) and the round-3 discard sweep byte-exactly (237/50 · 64 · 173/44 · the six dropped-whole records), **and still found routed 121 · booked 114 · escaped 7** — the seven being `D-19` (E-1), the fourth S-10 claimant `KF-ET-27` and its second leg `KF-ES-20` (E-2/E-3), `KF-EST-1` (E-4), the two unresolved LAW-A symbol hits (E-5), the stale `:447` self-anchor (E-6) and G-0.1's non-nested denominator (E-7). **All seven are cured at this round's D-1..D-6** and the two new rows are counted in the 78 above. Its predecessor `PASS-3/KF-W0-CHECK.md` §0/§1 (2026-08-28) reproduced the round-2 denominator exactly (22/13/21/4/13) **and found ten escaped ⟨record : id⟩ pairs**, six booked at round 3 and four routed to **§Sequencing, the NO-WAVE-OWNER packets row** (D-4, repair round 4: that landing was recorded as `:447`, which is a bare code fence — the roster is the `KF-AV-28 · THE STANDING SUPERSESSION RIDER` cell of that row, at `:651` at this writing, **parenthetical and non-load-bearing** per R2-7). **The lesson the artifact carries and this file records**: a denominator that reproduces proves the arithmetic, never the membership. RULINGS-2 R2-5(g) expected **72** on a seven-pair list; this seat's re-derivation over the 58 records found an **eighth** absent pair (kf-SquareInstrument **L-D9**, C-1.G row 21), and the ruling's own instruction governs — *the count is re-derived, not computed by arithmetic here*. The delta is declared, not absorbed. Every row and sub-row keeps its **banked id** — no id in this file is coined, renamed, or renumbered; the only fresh ids this wave ever mints are the census-slot ids C-17 assigns, and it mints those in one motion.

| # | Banked id | Mechanism (compressed) | Cure + locks carried | Gate |
|---|---|---|---|---|
| C-1 | **F-1 ≡ SCH-1** ≡ kf-AmigaScene C-1 ≡ **KF-CE-32** ≡ kf-App ruling 1 **≡ `AGG-P1`** ⟨~20 axes/readers across 41 records⟩ — **`AGG-P1` named at repair round 5 (D-4): the census agglomerator probe that PRODUCED the SCH-1 override this wave's §Bounds CENSUS row re-runs. It is the census-probe id, NOT a separate obligation, and every consequence it carries is already discharged at C-1 / G-0.2 / G-0.6 — a provenance handle, not a lost cure.** Cited by the very record this chain names as its head — **quoted byte-exact, no emphasis added and none dropped** (LAW D(1) · RULINGS-5 R5-13; `grep -n 'AGG-P1' kf-AmigaScene.md`, run this seat at the corpus's current bytes): ⟨`kf-AmigaScene.md:6`⟩ *"`origin/master` package.json:77 carries `@mkbabb/glass-ui: 7.0.0` as an EXACT devDep (census SCH-1, AGG-P1), so the phantom-dependency headline is FALSIFIED at HEAD."* · ⟨`:98`⟩ *"exact devDep (this seat's `git show`; census SCH-1/AGG-P1). True only of the stale sacred checkout both readers "re-verified today." Cure = KF.W0 §B-12, never a dependency wave."* (the `:6` fragment's inner quoting of the manifest value is re-spelled without its nested double quotes for markdown legibility — **declared**, and the value is unchanged). Its own home is `docs/tranches/V/megatranche/formation/keyframes/CENSUS-2026-08-03.md` — `:9` the probe class (*"**Agglomerator probes** (read-only, marked AGG-P\*)"* — the bold is the source's), `:32` SCH-1's evidence cell, `:62` *"DOCS+AGG-P1 override FE (SCH-1)"* (no emphasis in the source, none added here) — i.e. the dated override the `.d` unit's SCH re-run supersedes, so the seat performing that re-run now has a **handle on the probe whose finding it replaces**. **Byte receipt, self-scoped**: `grep -c 'AGG-P1' KF-W0.md` → **0** before this round. **Dated observation, declared as such and NOT a standing claim** (LAW E(4): this file is stage 1 and writes FIRST; a figure over stage-2 siblings' bytes cannot be kept by this seat): at this seat's read on 2026-08-28, before this round's writes, the id occurred **0 times program-wide** across the eleven X·KF specs | Four disagreeing glass-ui manifest states over one installed artifact. `npm ci` at HEAD does not fail — it silently **downgrades 7.0.0→6.0.0**; the "phantom" is an uncommitted local deletion. | Land the frontier's 7.0.0 exact devDep + lock and **rule on the deletion**. Locks verbatim: kf-CubeScene *"sync the sacred checkout to `81a56990`+; RULE on the worktree's uncommitted glass-ui devDep deletion (committing it as-is revives census F-1)"*; kf-LayerConfigPanel *"the reconciliation must land the frontier's 7.0.0 devDep, at which moment every prop verdict here is a verdict about the shipping build"*; kf-SpringTrace *"F-1 must be re-measured against origin/master before routing any wave"* (discharged by this file's re-measurement, dated); kf-SharePopover — the stripped worktree manifest *"remains an unexplained local artifact → KF.W0 §B-12"* (the deletion-ruling is its disposition). **KF-HA-6 (pin-and-bump-one-commit) is DISCHARGED BY, not blocking**: `origin/master` already IS that commit (kf 6.0.0 · value.js 4.0.0 — both violated peers satisfied at the frontier; only HEAD, at 5.3.5 / `^3.1.0`, violates). **Cross-repo rider**: keyframes.js's own tranche V pre-booked this as **FAM-01 RAIL / H→V-01 / W2 producer-gated** ⟨AUDIT-REGISTRY.md:15 + V.md:74⟩ — the deleted pin is a deliberate in-flight migration state, which corrects the axes' "nobody noticed for three majors" causal story. **Blast-surface riders carried, not re-booked**: 8 specifiers / 5 modules transitively via `@components/instrument/shell` ⟨kf-MbabbMenu ruling 2⟩; 18 subpaths / 44 sites, the "no demo file uses a subpath" convention true only for `/button` + `/card` ⟨kf-RibbonBar K10, rider on banked CPW C-20⟩; ChromeDock the densest consumer (`:6-11`/`:22-28`/`:29`/`:158`, graph dies at `:11`, mounted unconditionally from App.vue); kf-ChannelOptions' four subpath edges (`:143-146` / TimingFunctionPanel `:26-31`, authored against 3.4.0/4.0.1 while 7.0.0 resolves — *"the unpinned-drift half is live and is what KF-CO-1/2/18 measure"*); kf-KeyframesStringControls' two transitive critical-path edges (`CSSCodeEditor.vue:38`; `useHighlightCSS.ts:2` via `useApplyCSS.ts:4`); kf-EditorStartScreen's `z-controls` exposure cell. **The blast-surface list is NOT six records — it is the full fold-identity table at §Carry C-1.F below** (R-1a): thirteen ids, each with its banked travel-rider verbatim. C-1.F is where the fold's identity mechanism becomes id-keyed; a later seat greps its own row there. **This is what makes C-9's hard order satisfiable**: the cascade column lands before F-1's remediation scopes a blast radius that is now counted whole rather than under-counted at six. **Probes RETIRED here** (no seat re-spends the budget): the clean-`npm ci`-fails probe ⟨**kf-KeyframesStringControls `L-M-9`, the RETIRED sub-clause of that row — NOT its MAJOR fold limb**, which is booked by id at **C-1.R row 7** (repair round 5, D-3); the two limbs are separable by grep from this round, and this cell disposes only the spent probe⟩; the "verify against 6.0.0" residue — *"discharged; no 6.0.0 verification is owed by anyone"* ⟨kf-CubeScene kill 3⟩; every "blocked on F-1" gate in the CopyButton corpus (D-9, C-15, M-6 leg 2) — **RE-SCOPED, not blocked**. **Dissent preserved, charged nowhere**: kf-CubeAxisLines β-miss-1's `--omit=optional` failure-SHAPE note, a HEAD-only artifact killed at the frontier ⟨kf-CubeScene kill 4⟩. | **G-0.2** |
| C-2 | **census §0 substrate schism** ⟨kf-CubeScene reader-B M-1, RESCOPED-BY-INVERSION⟩ | The checkout is a **hybrid**: 41 behind AND 1 ahead; neither ref an ancestor of the other (merge-base `a59d3a22`, both `--is-ancestor` probes NO ⟨kf-TimelineCaret⟩). Every anchor binds to one of three coordinates, and the corpora silently disagree which — with a fourth coordinate alleged (the `backward` divergence, corrected at F-R7/N-2). | The §B-12 act, then re-anchor every lane and registry finding. **Binding on the whole sub-tranche** ⟨kf-TimelineCaret⟩: *"any spec consuming this registry states WHICH ref it cures against."* **Tree-motion law** ⟨kf-KeyframesStringControls DISSENT 3⟩: *"every anchor in this record is a TODAY-disk read; every cure re-resolves at `origin/master` first; and any future seat re-running a corpus grep on this repo must date its run."* Uncommitted-deletion inventory carried by record: `demo/utils/{formatEditorCSS,helpers,keyframeSelector}.ts` −75 L ⟨kf-OrbitalDrag⟩; the first and third **`??` UNTRACKED and absent from HEAD's tree while cited by line** ⟨kf-TimelineCaret⟩; the src refactor-in-flight ⟨kf-CubeTarget⟩; **~94 dirty src rows behind the CopyButton corpus's every src anchor**; StartingStyleTarget's `emphasis` migration observed live ⟨kf-SpringScene⟩. Reader-B's "describes uncommitted code" caveats (L-5/L-9/L-14/C-7/P-1/P-6) **VACATED with the inversion**, and its corollary ("P-1's adoptCompiled watch never fires at HEAD") with them; the factual observation (the corpus DID audit a dirty tree) preserved as true-but-mis-baselined. | **G-0.1** |
| C-3 | **THE DELETION** — rider on **KF-APP-5** + SCH-1 ⟨first-banked kf-EditorShell RR-1 missed #1 ≡ RR-2 missed M1, ONE identity⟩ | glass-ui `4bf53962` (2026-08-04, ancestor of producer HEAD) deletes `src/components/header-ribbon/` WHOLE (exports 73→68); the frontier consumer still imports the deleted subpath; installed 7.0.0 still ships the dist file. All three legs re-verified at fold (G-0.5 witness). | **Verbatim sequencing law**: *"KF.W0's substrate settle must NOT advance the installed glass-ui past `4bf53962` before the shell's chrome host migrates off `/header-ribbon` — else the build dies at `:116`."* Consequences ruled with it: (i) every axis's remediation step 1 ("declare glass-ui ≥ the `4e8c6387` cut and regenerate the lock" — L §6.1, C §8.1, D §7.1) is **UNBUILDABLE**; both readers' kills ratified, incl. RR-2's kill of L §1.0's escape-hatch framing (*"L-1 was never a pinning problem — the design system retired the component under its only consumer"*); (ii) **KF-APP-5's producer half is MOOT** — the producer answered by deletion; the relay downgrades to FYI + the consumer-evidence correction; (iii) producer-side content: `docs/consumer-evidence/header-ribbon.md:3` still says "RETAINED persistent-only", contradicting producer HEAD. **Severity dissent** ⟨DISSENT 4⟩ ruled blocker-weight at the banks with RR-1's MAJOR preserved — *"the shell still renders today against the installed artifact; the break is at the next producer advance."* Migration decision (which chrome host replaces the band) = **NO-WAVE-OWNER**, SS-1/SS-2's; it carries this tripwire wherever it lands. | **G-0.5** |
| C-4 | **L·B-2** ⟨kf-ChannelOptions, RESCOPED-BY-SUBSTRATE⟩ | Disk == `origin/master` byte-for-byte **except three named lags**: the 7.0.0 devDep; the **EE-02 css-twin** on `setAnimationTimingFunction` (disk `{ fn }`, master `{ fn, css }` + `timingFunctionLiteralFor`); the same twin at `TimingFunctionPanel.vue:144`. Both measured consequences (WAAPI demotion, export-serializer identity-miss) are **cured at the frontier**; the disk WIP would regress them. | **Verbatim**: *"KF.W0 obligation: §B-12 must reconcile the three lagging disk files so the landed EE-02 cure is not clobbered by a stale-WIP commit."* **Direction lock: disk←master, never master←disk.** Not this wave's, at MAJOR → OPTIONS-UNIT/engine lane: the direct-field-write bypass of `setTimingFunction`; the engine-side `applyTimingFunction` frame-propagation gap; N-10's double write; the N-17 flatten tripwire. | **G-0.3** |
| C-5 | **EE-01** ⟨DISPOSITIONS.md:69, born V → BUILD W1; folded from KF-CB-1 · D-1 = L·B-1 = C·C-1⟩ | `timingFunction: "bounceInEase"` resolves to nothing under value.js 4.0.0; the ctor throws inside an uncaught async `onMounted` (no try/catch `:65-102`, no `app.config.errorHandler`). Frontier carries the one-word repair (`origin/master:42` = `"easeInBounce"`, re-verified at fold). | **Verbatim**: *"FOLD to EE-01 + KF.W0: the reconciliation's one obligation here is that the stale fork's in-flight migration must not re-land the dead name over the frontier's repair."* **Sequencing inversion ruled at the bank**: K1's "the fix must land WITH the migration commit" is **superseded** — the fix landed upstream; the danger inverts to a **re-regression**. Surviving phantom trail (prose `easing.ts:44`, `waapi/eligibility.ts:169`, the m-6 test) → **KF-CB-18 / KF.W4**, not here. | **G-0.4** |
| C-6 | **FE-3** ⟨DISPOSITIONS.md §D; folded from KF-KE-1 · D-1 = L-B2 **and** KF-KC-1 · D-1 = L-1 = C-1⟩ | `KeyframeCardList.vue:11` calls `.toString()` on a bare frozen `KeyframeSelector` → `[object Object]` on every start field and ghost label; the correct renderer `selectorText()` is already in the closure's import graph (`useKeyframeOps.ts:111`). Frontier `:11` = `startScalar(frames[i].start)` (commit `add20b7e`, re-verified at fold; **path-qualified at repair round 4 by the R4-10 SUBJECT-IDENTITY sweep — the file is `demo/components/instrument/keyframes/components/KeyframeCardList.vue`**, and `git show origin/master:<that path> \| sed -n '11p'` → `:frame-start="startScalar(frames[i].start)"`, re-run this seat). | **Verbatim**: *"FOLD to FE-3 + KF.W0: the surviving obligation is the re-regression guard; FE-3's `/\[object Object\]/` smoke assertion is the born-RED witness"* (low collision risk — neither file locally dirty — but the guard is the same one EE-01 carries). **PARTIAL-CURE caveat that must travel with it** ⟨kf-KeyframeCard⟩: the frontier's cure renders the FRACTION `"0.5"` where the commit parser (`:206-209`) demands `"50%"`, a NAMED selector still coerces to `"[object Object]"`, and `selectorText` remains uncalled at both seams → **NO-WAVE-OWNER** terminal cure. Cause not cured: **KF-KE-17** (`frames: any[]` erases the library's type at the one seam the editor owns) → **KFED-UNIT, after KF.W4**. | **G-0.4** |
| C-7 | **EE-03** ⟨DISPOSITIONS.md; folded from KF-KE-9 · L-M1⟩ | The `templateFrames` **array** watch never fires and warns per element at setup (re-confirmed on vue 3.5.35). Frontier's cure is the exact dispositioned one — `watch(() => animation.templateFrames.length)` at `useKeyframesParsing.ts:97`. | **Verbatim**: *"FOLD to EE-03 + KF.W0 re-regression guard (EE-03's watch-warn-count→0 gate)."* Consequence living on: **KF-KE-10**'s visibility change — `fromString` **APPENDS** (3→6 frames, selectors `0,0,0.5,0.5,1,1`; silent at the audited bytes, projected as 10 duplicate cards at the frontier) → KFED-UNIT + **one library letter to KF.W5** on `fromString` append semantics. | **G-0.4** |
| C-8 | **SCH-1..SCH-7 + X-2** ⟨CENSUS §schism-table + §(a) row 1; INTAKE X-2⟩ | Two of three census lanes measured a tree 41 commits stale. SCH-1 falsified (C-1). SCH-3: 145/22 636 claimed vs **153** at frontier (fold re-measured), with FOLD-FORWARD §C's "159 modules" a third basis and Codex's "80 public library surfaces" a fourth. SCH-4: 61 value.js `from`-lines (`/value` 15) vs 62 (16). SCH-5: 4+ Tier-A anchors DEAD (`value-ast.ts:71`→`compile/value/compile.ts:32`; `easing-registry.ts`→`compile/easing/registry.ts`; `emit/backward/backward.ts`; `emit/easing-serialize.ts:71-73`). SCH-6: **58** `.vue` holds (fold re-measured), contents drifted. SCH-7: LIB L-1/L-2 depcruise unverified — "0 depcruise violations" does **not** refute L-1 (silent coverage loss reds nothing). X-2: 184 is the stale-worktree figure; **185** at frontier (fold re-measured). | Re-run every count at `origin/master`, command + literal output pasted, dated. **SCH-2** (KfPillTabs 217-line fork) **CONFIRMED at HEAD — re-verify, do not re-book**; stays KF.W6's top suffusion target. **W0 re-runs COUNTS; it does NOT reconcile the counting BASES** — X-4 (80 / 139 / 153 / "159") is **KF.W5's**. SCH-7's depcruise re-verification is KF.W5's input; W0 only restates it as unverified-at-HEAD. | **G-0.6** |
| C-9 | **L-5/C-7 legend rider** ⟨kf-SequenceAxis; ≡ kf-SequenceScrubber **C·C-13 / L·D-2** folded by name — **provenance corrected this round (D-7)**: the banked row is `kf-SequenceScrubber.md:71` *"C·C-13 / L·D-2 (surviving refinement)"*, re-stated at `:126` *"C·C-13 ≡ banked kf-SequenceAxis L-5/C-7 legend rider → KF.W0 §B-12"*. The previously cited "D-11/C-13" named a different family in that record — `D·D-11` at `:57` (aria-valuenow/valuetext) and `L·D-11` at `:65` (centre-anchor, killed at K-5), routed to KF.W7/W9, never here; restated kf-App.skeleton ("the §B-12 reconciliation adopts a cascade-level coupling arm beside the import census"), kf-EditorStartScreen L-EST-1/D-C9, kf-SquareInstrument (NO-WAVE-OWNER census-column note)⟩ | Zero-import files whose entire visual identity resolves through the **undeclared** glass-ui via the cascade: `text-mono-caption` is an `@utility` inside node_modules; `--border`/`--muted-foreground` vendor-only; `--radius-pill`'s sole definer is glass `theme/radius.css`; `z-controls` is glass-**GENERATED** (`--z-index-controls: var(--z-controls)` in `@theme inline`, bridges.css). An import-graph census scores every one of these CLEAN and is wrong. | **Verbatim**: *"a second census column separating import-coupled from cascade-coupled **before** F-1's remediation scopes its regression surface — routes to KF.W0 §B-12 as a legend rider."* **Hard order**: must land BEFORE the F-1 remediation scopes its blast radius, else the reconciliation under-counts what it can break — now scoped against C-1.F's full thirteen, not six (R-1a). **`L·D-2`'s MAJOR half is BOOKED here, not dropped (D-7)**: verbatim ⟨kf-SequenceScrubber:71⟩ — *"a component with ZERO glass-ui imports is fully glass-coupled through the `@import` cascade (`text-caption`/`text-mono-caption`/`text-muted-foreground`/`border-border` + `--radius-pill`, sole definer glass theme/radius.css) — an import-graph census scores it clean and is wrong."* That refinement is the surviving half; **what died** is L·D-2's phantom-dependency arm, killed at the record's K-1 (*"ONE-BASELINE-SHORT — the sixth on record"*, `:80`) along with *"every 'blocking'/sequencing/post-`npm ci` consequence in all three axes"*. The kill's corollary rides to G-0.9: the banked `kf-AnimationVisualizer.md:9` census cell is **corrected to worktree-scope** by appended note. This record's K-1 is also a C-19 firing (see C-19's enumerated ledger). | **G-0.6** |
| C-10 | **KF-AX-32** ⟨kf-CubeAxisLines, NEW · α-miss-5⟩ | The banked *"census §6.5"* roster — **"13 PRM enforcement sites across 12 files"** — is a roster of **local guards misread as coverage**. **HOME PINNED AT REPAIR ROUND 2 (D-4; F-R6's class, second instance)**: that roster is **NOT in `CENSUS-2026-08-03.md`**. It lives at `lane-frontend.md` **§6.5 `prefers-reduced-motion` — 13 enforcement sites across 12 files** (the §-heading is the anchor; at `:462` at this writing, non-load-bearing), with its tally row *"| `prefers-reduced-motion` enforcement sites | 13 (10 CSS + 3 JS) |"* in the same file's counts table (at `:598`). Receipt, re-run this seat at the current value.js bytes: `grep -n '6\.5' CENSUS-2026-08-03.md` → **one hit**, and it points **away** — *"delegation sites unverified (**FE** §6.5)"* (at `:141`). A bounds/gate row naming a surface that is not there is the defect class **D-12** convicts, inside the wave that exists to kill it. The vendor's universal `*:not([data-allow-motion])` rule reaches every demo element, so the roster's two "delegation … unverified statically" gap rows (TypingDots, KeyframeTimeline — the latter citing the wrong vendor file) actually **HOLD**, and D·D-1's BLOCKER ground was manufactured by the same mis-scoping. MINOR at the row, but *"the exact mechanism of the corpus's only false BLOCKER."* | **Verbatim** (banked words untouched, E-3): *"KF.W0 (re-scope §6.5 in the same motion as the z-index rows)."* **The act is re-aimed at the roster's real home** (D-4): the re-scope lands in `lane-frontend.md` **§6.5** and its counts-table tally row; `CENSUS-2026-08-03.md`'s single pointer cell (*"delegation sites unverified (FE §6.5)"*) is corrected **by appended note in the same motion**, so the two files stop disagreeing about where the roster is. Universal-reset decider banked at kf-CubeScene D-16 (extracted from shipped `dist/gh-pages/assets/index-CL_QYCiO.css`) and kf-EditorHeader kill #1 (`glass-ui/dist/styles/utilities/a11y-overrides.css`, byte 0, unlayered, `!important`). Feeds **KF.W9**, whose tallies re-derive from the corrected roster (kf-CubeScene: 4 engine motions + 1 gated; **kf-OrbitalDrag OD-11 corrects the cube tally to 5 ungated**). Dissent preserved: the citation-inheritance ledger for this scene (α's ×4 fallback cell, third generation past a published correction). | **G-0.6** |
| C-11 | **KF-AX-12** ⟨kf-CubeAxisLines, both readers independently; ≡ kf-CubeTarget #51⟩ | The census is wrong about this file at its OWN HEAD, twice (`raw z-index:-10` ×2), inherited from stale prose no seat re-read. | **Verbatim**: *"KF.W0 (census re-anchor); prose sites ride KF.W6"* — bundled with C-10, *"in the same motion"*. The in-tree prose recut lands with KF-AX-11/12 + **KF-AX-31's z-contract block** in one KF.W6 motion, which additionally resolves the `--z-behind` **POLICY CONTRADICTION** (`layout.css:8-11` documents the shadow as deliberate override policy — *"OUTSIDE @layer … authoritative for the demo"*): **a contradiction to RESOLVE, not a stray to delete.** | **G-0.6** |
| C-12 | **KF-AX-31** ⟨kf-CubeAxisLines, NEW · β-miss-5⟩ + **R-7's dead-`proof:font-census` arm** ⟨kf-KeyboardShortcutsModal **R-7**, `:43` — **provenance corrected this round (D-11)**: `:43` is R-7, the dead-witness row (*"the named witness is dead"*; `DESIGN.md:24-30` states the Mono-as-data law and names `proof:font-census`); `:45` is R-9, the *"nothing automated verifies this file"* row that routes to KF.W4. The cell separates them correctly below; the tag was a transcription slip of exactly the class C-19 books, so it is booked as one⟩ | Comment chains cite dissolved gates. `demo/styles/style.css:38-39` says *"`proof:brittleness` gates against drift"*; no such gate exists (dissolution `70b32501`) — *"a comment chain pointing to a contract pointing to a deleted enforcement, two falsehoods deep"*, and the same block also carries C-11's stale raw-−10 sentence. `DESIGN.md:24-30` names `proof:font-census` as the Mono-as-data law's witness; it does not exist. **Fold-widened surface (F-R4, measured at `origin/master` 2026-08-28)**: a THIRD dead name `proof:styling-idioms` at `DESIGN.md:250`; further `proof:font-census` cites at `DESIGN.md:29`, `EditorStartScreen.vue:180`, `font-roles.json:2`/`:62`; `proof:brittleness` also at `layout.css:152`. **Round-1 widening (R-17, D-3) — three further `proof:styling-idioms` sites, re-measured at `origin/master` this round** (`git grep -n 'proof:styling-idioms' origin/master -- demo/`): `demo/DESIGN.md:237` (the **R4** codex clause, *"Clause on `proof:styling-idioms`; U.B12"*), `demo/components/instrument/shell/EditorStartScreen.vue:159` (*"silent-flatten (proof:styling-idioms membership)"*), `demo/scenes/sequence/SequenceTarget.css:212` (*"owned here so it resolves under proof:styling-idioms"*). **The demo-side surface is 10 sites, not 7** — a sweep run against the seven-site list leaves three citations alive, which is this gate's own stated falsifier reproduced one level up. | Book **one authoritative dated gate roster** and **route** every prose citation of a gate that does not exist to the wave whose §Bounds carries its file — **W0 rosters and routes; it strikes nothing** (R2-17; the strike sites are all under `demo/**`, which this wave's §Bounds forbids and the Dispatch calls wave-invalidating). **The record is SPLIT**: the dead-gate arm lands here; the **demo-typechecking arm routes to KF.W4** (post-F-1), folding to the single banked no-SFC-typecheck identity — KF-APP-4 ≡ KF-CE-16 ≡ KF-CO-7 ≡ MM-36 ≡ LP-7 ≡ KF-AT-12 ≡ KF-HA-5 ≡ L-4/C-6 ≡ B-1 ≡ RB B-1 — **booked ONCE, never re-booked**. R-9's carve-out (no `vue-tsc` in devDeps or `.bin`; `vitest.config.ts` two projects, NO `@vitejs/plugin-vue`; root-only vite config) is KF.W4's input, **dated here**. **INBOUND under R-17/R-8 — the roster RECEIVES three residue families KF.W4 routes here** (reciprocal edge declared at KF-W4's **§Excluded, the residue table** — stable anchor only, LAW C(3), that sibling writing after this file; one roster, one owner, one sweep): (iii) **kf-EditorStartScreen KF-EST-1 — the THIRD family, entered at repair round 4 (D-6)**: *"Eight `proof:*` citations across two files name gates deleted by `70b32501`/`92746148` as binding authority"* ⟨`kf-EditorStartScreen.md:58`⟩, its eight sites and six distinct names measured at the frontier and listed at G-0.7, with `TypingDots.vue:45`/`:51`/`:59` entered in the roster by name — the wave had been citing **two** of the eight under other names while the family, its id and its second file went unnamed; (i) **kf-AnimationControlsGroup D-13** — *"seven `proof:*` gate citations resolve to zero runnable gates"*, banked NO-WAVE-OWNER (prose-truth hygiene), the seven names adopted into the sweep by id; (ii) **kf-DemoGlobalChrome L-6** — `proof:demo-no-oversize` at **16 hits across 13 files** (that seat's executed grep; *"RR-L said 10; missed `useAnimationGroupActions.ts` and `useAnimationGroupPlayback.ts` among others"*), carried with its verbatim lock *"A fix confined to this file leaves twelve files standing; **the fold owns the sweep**"* — and with the record's own consequence preserved: the header's SOLE stated reason for `DemoGlobalChrome.vue`'s existence cites the retired gate, which strengthens its C-3 re-home (that re-home is not this wave's). **`proof:demo-no-oversize` is therefore a FOURTH dead name** — and **the sweep's denominator is none of these four (R2-17, repair round 2)**: re-derived at `origin/master` this round it is the **53 dead names** of the `demo/` surface (54 distinct, 1 runnable; 116 hits / 51 files), of which these four and D-13's seven (enumerated at G-0.7) are worked examples. `proof:brittleness`'s three non-`docs/` sites (`style.css:38` · `layout.css:152` · `scripts/lib/demo-driver.mjs:69`) carry the disposition **cure → KF.W6, family whole** (R2-13; KF.W4 carves them out by name at its own §Excluded residue table), and `proof:demo-no-oversize`'s 16×13 is **`demo/`-scoped** — repo-wide 121 hits / 70 files, 57 of them under `docs/`, declared out-of-scope by bounds. | **G-0.7** |
| C-13 | **Corpus-integrity row** ⟨kf-CSSPasteDialog, MAJOR/process; K-9 = 2nd instance; kf-EditorHeader EH-1 = 3rd⟩ | challenge-D's evidence root (`challenge-D-design.md:7`, "verified 7.0.0 both sides") matched version **STRINGS, not ARTIFACTS** — *"the glass-ui source tree was weeks of unpublished work ahead of the consumed 7.0.0 dist. Roughly a third of D's file:line provenance does not resolve in the artifact under audit"* (both re-readers convergent; 7.0.0 cut 2026-07-16, `--ink-perimeter` landed 2026-08-04). Second instance self-inflicted: R-B, the reader who convicted D, cited "glass-ui src at `b155ca4c~1:DialogContent.vue:482-489`" and was KILLED on the shipped chunk. | **Verbatim**: *"every D citation into `glass-ui/src/**` must be re-derived against `node_modules` before any wave acts on it; L and C, which read the dist, are the durable records on every overlapping row."* **Companion caveat carried whole**: *"the built witness `dist/gh-pages/assets/index-CL_QYCiO.css` is dated Jul 16 — possibly pre-glass-7 — so every byte-offset receipt in the corpus is provisional until a fresh `build:gh-pages`"* (fold re-verified: mtime Jul 16 09:11) — **the caveat is carried verbatim as banked, and the COMMAND it names is corrected in the execution text: `npm run gh-pages` (R-11). No `build:gh-pages` script exists at HEAD, `origin/master` or the worktree; `package.json:43` is `"gh-pages": "vite build --mode gh-pages"`. The banked words are not rewritten (E-3); the act this wave performs is the real one.** **Precedent** (same class): the value.js PaletteCard R-4 registry repair carried in COHESION §4 — an evidence-ledger correction no product wave owns, homed at the substrate wave because that is where the re-anchor law lives. Run the fresh build **once, here** (KF.W9's SS-13 witnesses need the same act). | **G-0.10** |
| C-14 | **EH-1** ⟨kf-EditorHeader, C §0 substance — MAJOR, *"the row inverts the disposition and was load-bearing for the whole corpus"*⟩ | HeaderRibbon 7.0.0 **still ships** the `anchor` slot (`{pinned}` payload, `HeaderRibbon.vue.d.ts`), the entire collapsible machinery (x/S/C flags, inert/aria-hidden, Esc) and `--header-ribbon-actions-width`; the archived letter claims all three were deleted. Only `mode` and `anchorLabel` are actually gone. **Under the letter the target file is irreplaceable; under the tree it is deletable.** **THE "NONEXISTENT PATH" SENTENCE IS STRUCK, LOUDLY (repair round 3, D-1 — E-3: the strike is declared, not silently absorbed).** This file previously read *"The corpus also cited a NONEXISTENT path (`coordination/…-consumer-updates.md`) — a separate citation kill"*. **That is false, and it inverts the sibling's finding.** Re-measured this seat: `git -C keyframes.js ls-tree -r --name-only origin/master \| grep -i headerribbon` → **`docs/tranches/V/coordination/GLASS-INBOUND-2026-07-16-headerribbon-consumer-updates.md`** — the path is **live at the frontier**; it is simply in the **other repo**. **There are TWO repo-qualified letters, both dated 2026-07-16, both from the glass-ui BI/P/Q session, and the qualification is LOAD-BEARING at the point of delivery** — this wave's substrate is keyframes.js, where only one of the two exists: **(a) value.js `docs/tranches/V/archive/GLASS-INBOUND-2026-07-16-headerribbon-persistent-only.md:15-19`** (*"Deleted with it: the anchor button/slot, `anchorLabel`, the reveal/pin/Escape/focus-presence machinery, `HeaderRibbonMode`, `HeaderRibbonAnchorSlotProps`, and the `--header-ribbon-actions-width` knob"* — the claim EH-1 falsifies against the shipped 7.0.0 dist); **(b) keyframes.js `origin/master:docs/tranches/V/coordination/GLASS-INBOUND-2026-07-16-headerribbon-consumer-updates.md`** (26 L — the *consumer-update* mark: two `EditorShell.vue` deletions, `:16` `mode="persistent"` and `:197` the dead `defineExpose`). **They are different documents in different repos** — `KF-W10.md` §Sequencing A declares exactly this back to this wave by name, and a consumer handed the bare filename searches the wrong tree and concludes the coordinate is dead, **which is what this file did**. Third coordinate recorded so the next seat does not repeat the inference: letter (b) cites the cross-repo record at **`value.js docs/tranches/V/GLASS-INBOUND-…-persistent-only.md`** — the **pre-archive** home; the file moved to `V/archive/` on 2026-07-17 (its own line 1 says so), which is the real provenance of the "nonexistent" reading. **A dead-by-inference verdict on a live cross-repo coordinate is C-13's own conviction committed by the row that books C-13.** | **Verbatim**: *"KF.W0 (re-read XR-4 / IN-GLASS-1 / DISPOSITIONS.md:21 against the ARCHIVE file + `dist/header-ribbon.js` before any Glass-7 consume slice is scoped) + rider to the standing glass BH relay: producer docs/letters must be re-derived against shipped bytes (the kf-ChromeDock doc-drift ledger's pattern, third instance)."* **Composes with C-3**: the letter is wrong about 7.0.0 **and** the component is gone at producer HEAD — the reconciliation must read BOTH the installed dist and producer HEAD before ruling. Gates any Glass-7 consume slice: **nothing in KF.W6 may be scoped from either letter.** **The re-read is over BOTH repo-qualified letters (a) and (b), each named with its repo at every point of delivery (D-1, repair round 3) — a re-read of one of the two, stamped as the gate's receipt, is the failure G-0.10 exists to catch, one level up.** | **G-0.10** |
| C-15 | **L-i2** ⟨kf-EditorHeader, INFO — substrate-dated, + the phantom-attr rider⟩ | `mode="persistent"` is a stray attr, worktree-only; the frontier is a clean `placement="right"`. **Measured by the LAW-A census 2 this round (repair round 3): `origin/master:demo/components/instrument/shell/EditorShell.vue:16` = `<HeaderRibbon placement="right">`** — so update 1 of the keyframes-side letter (C-14's letter (b): *"drop `mode="persistent"` … it falls through as a stray `mode=` attribute"*) is **already landed at the frontier**, and the stray survives only at HEAD. But **HEAD carries `position="right"` — also not a 7.0.0 prop**. The phantom-attr class exists at HEAD in a different spelling, and a naive reconciliation would carry it forward. | **Verbatim**: *"the reconciliation belongs to KF.W0's substrate settle, not to a component cure."* Corroborated: L-3 is frontier-CURED (phantom deleted at `origin/master:16`) and **L-17 reframes it as a FOSSIL of a contract cut underneath the consumer, not author error** ⟨kf-EditorShell⟩. **It is a reason to diff the reconciliation output rather than assume it.** | **G-0.1** |
| C-16 | **KF-APP-59** ⟨kf-App β-miss 7, INFO — *"a closed falsifier, not a defect"*; kf-EditorHeader C-18's two-unmet-peers rider; kf-HeroAurora KF-HA-6's `.npmrc` decider⟩ | glass-ui 7.0.0's peer set is unsatisfiable in-tree (`@mkbabb/pencil-boil ^0.9.2`, `embla-carousel-vue ^8.0` — fold re-read the installed peer map) but **UNREACHABLE from App's closure** — the root barrel imports neither. `.npmrc` = the single line `legacy-peer-deps=true`; `ls node_modules/@mkbabb/` → glass-ui, parse-that, value.js (no pencil-boil). | **Verbatim**: *"KF.W0 substrate note + BH relay FYI; no App booking."* **Compose with C-1's `.npmrc` rider**: the reconciliation must decide whether `legacy-peer-deps=true` continues to mask the two genuinely-absent peers, or whether the absence is declared and the flag retired — **either is a ruling; silence is not.** Producer-facing half rides SS-6 / the standing BH relay per owner edict, **NEVER a demo-side hack**. | **G-0.2** |
| C-17 | **S-9 collision (four proposals) + S-10** ⟨kf-SpringHeatmap C-M-1; kf-EditorHeader **F1 ≡ D-1/L-B1/C-1 → kf-App KF-APP-41** (`:33`; the S-9 ask id-less at `:117`) — **the coined `F1(c)` retired at repair round 3, D-2/E-9**; **kf-LayerConfigPanel LP-8 ≡ KF-CO-35 + ME-18** (the carry's head id, re-keyed at repair round 3, D-2/E-8; provenance legs kf-CubeScene L-5/C-6 + ruling 8 retained beneath it); kf-AmigaScene C-6 (**owned by KF.W6**); kf-KeyframesEditor asks S-10 (`:63` = KF-KE-21; **the duplicate row-13 entry of that same ask is struck**); **kf-KeyframesAddDialog KAD-12, adopted at repair round 3 from the sole carry, D-2/E-7**; **kf-EasingTarget KF-ET-27 (`:69`) with kf-EasingSidebar KF-ES-20 (`:61`) as its second provenance leg — the FOURTH S-10 claimant, adopted at repair round 4 from the corpus bytes, D-3/E-2/E-3**⟩ | The shadow census runs **S-1..S-8** — home pinned at fold (F-R6): `lane-frontend.md`, `^### S-` = 8, S-1 `:264` … S-8 `:387`. **The roster is NOT four-plus-one — as re-derived at repair round 3 and EXTENDED at repair round 4 it is TEN S-9 claimants and FOUR S-10 claimants — fourteen LIVE rows, plus one row retained as an enumerated strike** (R-5, D-1; the round-2 shape *"≥8 S-9 / ≥4 S-10"* is superseded by measurement: `+KAD-12` from the sole carry; the anonymous fourth S-10 named as a duplicate of `KF-KE-21` and struck; and **`+KF-ET-27` adopted at repair round 4 (D-3)** — the corpus-side `design-idioms.css` census arm, with `KF-ES-20` as its second provenance leg — after the round-3 receipt was found unreproducible. **The S-10 enumeration, its counting rule and its substring-discard rule live at C-17.R row 13; this cell points at that enumeration and re-issues no numeral**), enumerated id-for-id at **§Carry C-17.R** below. The four originally named: glass `./canvas` (present at installed 7.0.0, 73 exports; falsifier bytes in `dist/resolveCanvasColor-CBnFEM0G.js`, `dist/canvas.js` a re-export shim); `EditorHeader → HeaderRibbon — DEAD, replace-by-deletion` (`lane-frontend.md:194` rosters the file live while the shadow census omits it); the number-field row; and — **fourth subject corrected (D-9)** — kf-AmigaScene **C-6**, which is *"Two shipped glass primitives (`Surface`, `PaperBackdrop`; `Card` per the sibling register) shadowed by 8 lines of bespoke CSS"* and which the record books as *"a new census shadow-census row — booked ONCE here, **extends S-1..S-8**"* (`:61`, `:146`). It extends the **block**, not S-1; S-1 is `KfPillTabs → SegmentedTabs` (`lane-frontend.md:264`), a different subject entirely. **Leaving this to a later wave is exactly what the anti-rename law forbids** — and a roster short by six claimants, one of them already renumbered into a *second* collision, is the precise mechanism of the collision this row exists to kill. | Mint every shadow row here under **FRESH non-colliding ids, in one motion**, each citing its originating record — **over the FULL claimant set at C-17.R, which is the row's roster of record**. **Claims are inputs to the mint; a claim is never a slot** (R-5): no wave other than KF.W0 mints or renumbers a census slot, and the mint is the ONLY census-slot authority. kf-LayerConfigPanel ruling 8 states it directly: the "consumed-but-mis-called" class needs *"a FRESH id (ruling 8 — the S-9 collision)"*; kf-SquareInstrument's S-9/S-10 are likewise **intake rows, not re-derivations of S-1..S-8**. Must land **inside** the census re-anchor, before any wave cites a shadow row by number. The `/number-field` **EVALUATION** is KF.W6's (ME-18 precedent); **W0 only fixes the identity.** **The kf-KeyboardShortcutsModal seat-ruled renumber is DEMOTED to a claim-input** (R-5): an **E-3 addendum under the original id** at `kf-KeyboardShortcutsModal.md` records *"the renumber is superseded by the KF.W0 C-17 single-motion mint; the slot id is whatever the mint assigns"* — the seat's own booked lesson (*"census extension ids must be allocated against the banked registry, not against the local corpus"*) is thereby honoured rather than repeated. **Sibling edge declared**: KF.W6's booking of that row carries *"census id pending the KF.W0 C-17 mint"*. | **G-0.6** |
| C-18 | **X-1** ⟨INTAKE-ADJUDICATION-2026-08-03, carried table row **KF.W0**; CENSUS `:297`/`:322`/`:330`⟩ | The entire Codex "Keyframes v8" contract lineage is pinned to `8281638c`. Every denominator (414 contracts, 152 surfaces, 73 568 Safari cells, 184 demo members) is measured on the tree §0 already ruled non-authoritative. | **Verbatim**: *"the §B-12 reconciliation (KF.W0) must also disposition the v8 contract — re-anchor it **or** declare it stale-by-substrate, never citable for a live count."* Plus X-2: *"KF.W0's re-count adopts 185-at-HEAD"* — **184's exact Codex/census agreement is the PROOF both censused the same stale worktree.** Seal's citable form carried as input (terminal disposition is KF.W10's): *"cite only as `345 exact / 12 partial / 57 unresolved @ 8281638c`, never 357/414, never as HEAD coverage."* The lineage's zero-authority self-declaration is accurate (X-6); its terminal-stop language is not load-bearing (X-7). **CONDITIONAL GATE**: the TCC re-reads **B10-9 · B19-10 (pin half) · B19-11 · B20-6 · B21-7** are *"owed only if the v8 contract is ever cited"* — this disposition decides whether they ever open. **Owner row OG-KF1** (B21-18: *"Does the Codex Keyframes B-lineage continue at all — and must any continuation re-anchor to `origin/master` `81a56990` before its denominators may be cited?"*) is **NAMED here, RULED at KF.W10** beside OD-V3/OD-V5, **never proxied**. Dissent recorded: **B18-26** split adoption — numerator INHERITED (zero real-Safari execution), denominator REJECTED (the 73 568-cell Kronecker self-cross-product has no product consumer); **B18-27** (slot 4-vs-5) UNRESOLVED and carried to KF.W10. | **G-0.8** |
| C-19 | **the dirty-manifest law** ⟨kf-SequencePlayhead, a priori⟩ + **the registry-sweep law** ⟨kf-KeyframesStringControls DISSENT 4⟩ — **30 LEDGER MEMBERS + 2 ANTI-FIRINGS, ENUMERATED SEPARATELY (count corrected at repair round 3, D-3; round 1 stated 25, round 2's D-8 restated 32, and 32 was wrong for a NEW reason — it silently counted the cell's own counter-examples as firings).** Receipt, re-derived at the bytes this seat: the cell names **32 distinct records** (`grep -o -E 'kf-[A-Za-z.]+' | sort -u | wc -l` → 32) of which **two are the closing clause's declared ANTI-inheritance events** — `kf-CubeAxisLines` and `kf-AnimatedText`, records where the law **did not** fire — leaving **30**. The 30 partition as: **16** in the first enumeration · **8** added at round 2 · **4** in the sibling class (banked rows re-filed as fresh BLOCKERs) · **2** the law-source records that bank their own entries (`kf-SequencePlayhead`, the *a priori* law; `kf-KeyframesStringControls`, DISSENT 4). **An over-counted denominator is as un-closable as an under-counted one, and harder to see because the roster looks full** — this cell's own round-2 words (*"a ledger claiming 'enumerated per firing' must actually enumerate"*) convict it in the other direction | A manifest/tree claim filed **without naming its baseline**, then INHERITED as corroboration by a second and third seat. **At least the ninth firing**, twice at re-reader tier and twice at adjudicator tier. Enumerated and dated per firing: kf-AmigaScene (1st — *"a challenge lane's 're-run today' is worthless without naming the tree"*), kf-CubeScene (2nd), kf-ChannelOptions (first RE-READER-tier — both re-readers re-ran the probe against the dirty disk while naming a commit), kf-CopyButton, kf-SquareScene (5th — *"name the tree before you grade it"*), kf-StartingStyleTarget / kf-SpringHeatmap / kf-SpringTarget ("sixth", "sixth-plus"), kf-SpringScene ("sixth AND seventh, in one corpus"), kf-SquareInstrument, kf-SpringTrace ("the FOURTH re-booking event and the SIXTH one-baseline-short measurement"), kf-OrbitalDrag K15 (4th library-side, four corpora), kf-RibbonBar K10, kf-TimelineCaret ("a sixth one-baseline-short event for the fleet"), kf-SequenceAxis (kills 8, 9 — one per reader), kf-SequenceScene (three re-inheritance events, one per axis). **Eight further firings enumerated this round (D-8 — a ledger claiming "enumerated per firing" must actually enumerate; an under-counted denominator is un-closable)**: kf-MatrixEditor **K17** (*"RE-FIRED against both — SUBSTRATE-DEAD"*, `:34`; K28 records *"four banked kills each re-fired; five separate events"*); kf-SequenceTarget **K-1** (*"KILLED at every committed baseline"*, `:105`); kf-SharePopover **L·D-12/§5/§7** (*"SUBSTRATE-DEAD"*, `:75`); kf-SequenceScrubber **K-1** (*"ONE-BASELINE-SHORT — the sixth on record"*, `:80`); kf-LayerConfigPanel **K4/K5** (*"the axes' premise is false at local HEAD — ruled four times before this corpus filed it"*, `:88`); kf-CubeTarget **L·B-3** (α's kill SUSTAINED against a re-filed CONFIRMED BLOCKER, `:29`); kf-KeyframeCard **L-2** (a **reader-confirmation REVERSAL** — *"Reader-2 SUSTAINED; reader-1's cell REVERSED"*, `:24`, the rarer and sharper form); kf-EasingScene **KF-ES-15** (*"Fourth and fifth occurrences of frontier-blindness in the program (corrections register #5)"*, `:54`). **Sibling class**: banked rows re-filed as fresh BLOCKERs — kf-ChromeDock (*"BOTH re-readers filed this as a fresh confirmed BLOCKER; it was banked eleven days of registry ago"*), kf-MbabbMenu K-2, kf-EditorShell DISSENT 1 (*"RR-2 filed a banked identity under 'missed by all three axes'"*), kf-EditorHeader (*"THREE already-banked heavyweights the readers filed as fresh"*). | Book the ledger as a **durable record** and the derived laws **verbatim** where every later seat reads them: *"the failure is never derivation; it is an unread registry and an unnamed baseline"* ⟨kf-MbabbMenu⟩; *"EVERY re-reader row — confirmed, killed, or missed — runs through the bank before booking"* ⟨kf-EditorShell⟩; *"the M-25 agglomeration seat should treat 'checked the adjudicated registry' as a mandatory reader instrument"* ⟨kf-EditorHeader⟩; *"a 'missed' claim without a bank sweep is unadjudicable"* ⟨DISSENT 4⟩; the tree-motion law ⟨DISSENT 3⟩. **Binds every subsequent X·KF wave's reader/adjudicator dispatch** — the method half of the re-anchor law, travelling with C-2. Anti-inheritance events preserved beside the failures as fleet evidence, not tree evidence (ColorSpaceSelector DISSENT-1 precedent): kf-CubeAxisLines' reader-β re-deriving F-1's substrate from git; kf-AnimatedText's two readers converging independently on the `role="img"` implicit-role override, *"the anti-signature of citation-inheritance."* | **G-0.9** |
| C-20 | **kf-EditorShell DISSENT 3** — inter-bank MbabbMenu contradiction | kf-ChromeDock: MbabbMenu *"MOVED at the frontier (−146 lines)"*. kf-CubeScene: MbabbMenu *"byte-identical between the worktree and origin/master"*. kf-EditorShell's own probe shows only the load-bearing shape (the `@select.prevent` menuitem wrappers around SharePopover/DarkModeToggle) present verbatim at `origin/master`. | **Verbatim**: *"the two banked cells cannot both be right and the KF.W0 substrate settle should reconcile them."* Per **E-3** the reconciliation is an **APPENDED** correction at both banks' ids, never a rewrite of dated evidence. **It is the named test-case for the anchor-hygiene constraint** — if two adjudicated records disagree about one file's bytes, the wave has not re-anchored. | **G-0.9** |
| C-21 | **KF-TD-2** ⟨kf-TypingDots, reader-2 missed-2 · ruling 1 — the substrate **INVERSION**⟩ | For `TypingDots.vue` the **worktree is IDENTICAL to `origin/master` and local HEAD is the OUTLIER** (fold re-verified: diff vs origin/master empty; vs HEAD 1 file, 4+/9−). The direction of every other substrate ruling reverses for this file: *"the WAAPI-lane readings are the shipping truth, the rAF-lane readings describe stale HEAD."* | **Verbatim**: *"KF.W0 substrate settle; the registry corrections attach at the banks' ids."* Three corrections travel with it: it **corrects KF-EST-21's scoping**, **corrects EST ruling 7's direction for this file**, and **DISCHARGES KF-AT-28's re-anchor obligation** for it. **It proves the re-anchor pass must run per-FILE, not per-repo** — "stale HEAD, current frontier" is not uniform, and a blanket re-anchor would corrupt three banked rows. **The corpus hands this clause exactly one MEASURED per-file offset and it is carried here rather than left in the bank (E-4, repair round 3)**: ⟨kf-AmigaScene `:155`⟩ *"**KF.W0**: the C-1 discharge + the corpus-wide re-anchor clause (this component: **comment-only drift, useAmigaDemo −2 below `:73`**)."* It is the exemplar of the per-file direction in the *other* polarity from KF-TD-2 — a small, comment-only, **signed** offset rather than a whole-file inversion — which is why the pass must record an offset per file and not a verdict per repo. | **G-0.9** |
| C-22 | **The KF.W6 CARRY §CrossEdges packet** ⟨`docs/tranches/X/keyframes/carry/KF-W6-CARRY.md:354`, `## §CrossEdges (declared from this end)` → **KF.W0 · Substrate Settle**⟩ — adopted this round under **R-19b** (cures **D-6**) | An edge **declared from KF.W6's end and unanswered from this one**. Three corrections that seat measured, all of them G-0.9 appended-correction business, none of them present in this file by bytes before this round (`grep -c` → `App.vue:176` 0 · `TABS_EXTERNALLY` 0 · `dock-margin` 0 · `KF-SKEL` 0). Two of the three are *byte-level* corrections to banked citations — **precisely the class C-20 names as this wave's test case**: if two authorities disagree about one file's bytes, the wave has not re-anchored. | Book all three **by id** as G-0.9 appended-correction obligations (E-3: appended at the banks, never a rewrite): (i) the `TABS_EXTERNALLY_MANAGED_KEY` provide site is **`demo/app/App.vue:176`** (**path-qualified at repair round 4 by the R4-10 SUBJECT-IDENTITY sweep** — the file is `demo/app/App.vue`; `git show origin/master:demo/App.vue` → *fatal: path … does not exist*, so the bare leaf `App.vue:176` sent a seat to a path that is not there — and the site itself re-verified this seat: `git grep -n 'TABS_EXTERNALLY_MANAGED_KEY' origin/master -- demo/app/App.vue` → `:140` the import, **`:176` `provide(TABS_EXTERNALLY_MANAGED_KEY, true);`**), not the banked stale-HEAD **`:169`** — a one-line stale-offset correction of exactly the substrate class C-2 governs; (ii) **`--dock-margin` has 9 live unfallbacked sites + 1 prose mention**, so any census cell quoting a smaller figure is corrected by appended note; (iii) **KF-SKEL-7** — *"the §B-12 reconciliation adopts a cascade-level coupling arm beside the import census"* (`kf-App.skeleton.md:42`), whose instrument critique **survives the death of the phantom-dependency premise** on its own witness (a file with zero imports and zero `@import` whose 4-of-5 custom-property reads resolve only through glass-ui's cascade). KF-SKEL-7 is the **named counterpart of C-9's legend rider** and rides C-1.F row F-13; its token-namespace probe half is KF.W6's, not this wave's. **The answer is returned to the declaring end**: this file now carries the edge, so the seam closes at both ends. | **G-0.9** |

### C-1.F — the fold-identity table (R-1a · 12 adopted escapes + 1 riding from R-19b)

C-1's mechanism is a fold *by reference*: the record terminates its row at census F-1/SCH-1 and the cure is stated once, here. That mechanism was carried; the **ids and their travel-riders were not**, so no later seat could grep this spec for its own row and no count of the F-1 blast surface was complete. **Partition law (R-1a)**: each id below lands in exactly ONE wave — this one — and no other wave may book it except as an exclusion-with-named-owner pointing here. Riders are quoted from the bank **verbatim, carried not cited** (M-25: a sequencing rider dropped in transit is a silent drop).

| # | Banked id ⟨record : line⟩ | Rider carried verbatim — travels with the fold |
|---|---|---|
| **F-1** | **D-5 / L-3 / C-2** ⟨kf-DemoGlobalChrome:43⟩ | *"MAJOR · FOLD → census S-9 (identity guard; the swap verdict is census-owned)"* — the glass `./toast` family. Booked here **with the identity guard only**; the **swap verdict is not this wave's**, and the row enters the mint as an S-9 claim-input at C-17.R. The record's own three riders travel with it: (i) sequencing on census **F-1/KF.W1-DEP**; (ii) *"the swap retires M-L1's leak, C-4's guard module, and D-2/D-3/D-7/M-3/D-11/D-13/D-14 in one motion"*; (iii) *"the S-9 row contradicts `lane-frontend.md:398` ('Bespoke, no glass counterpart') — the census tally row must be corrected when S-9 is booked."* **— THE FIVE COMPONENT RIDERS OF THE `KF.W1-DEP` FOLD, LANDED AT REPAIR ROUND 4** (the receiving end of a sibling's round-4 booking; sending end cited **anchor-only** per LAW C(3) — `KF-W1.md` §Repair Round 4, directive **D4-3** — because that file writes after this one. Each rider is quoted **verbatim from its own bank** with the command that produces it, `sed -n '<n>p' <record>.md`, run this seat; M-25: a rider dropped in transit is a silent drop, and these five are the component-level exposure facts the census could not see): **(a)** ⟨kf-AnimationControlsGroup:47⟩ *"Component rider: three specifiers (`.vue:124`, `.vue:126`, `useControlsKeyboardShortcuts.ts:1`), the third being **the sole binding site of all 18 shortcuts**."* · **(b)** ⟨kf-ChannelControls:56⟩ *"Component rider: `ChannelControls.vue:219` (root-barrel `TooltipProvider, Button`) is **the first import that fails on a clean `npm ci`**; every glass-ui token/utility/d.ts claim in this record is resolved against the INSTALLED 7.0.0 and inherits F-1's reproducibility caveat."* · **(c)** ⟨kf-ControlsPaneWrapper:60⟩ *"Component rider the census could not see: `.vue:166` is the demo's **SOLE `/drawer` consumer**, statically on every scene's shell path — resolution failure deletes the entire mobile control surface at the root of the transport tree."* · **(d)** ⟨kf-DemoGlobalChrome:44, the L-8 row of this same record⟩ *"Component rider: **all four toast utility classes** (`text-body`/`text-small`/`bg-foreground`/`text-background`) resolve SOLELY through the phantom (glass `@utility` defs + `@theme inline` bridges; the demo declares none)."* · **(e)** ⟨kf-KfPillTabs:46⟩ *"This component's rider (confirmed): its consumption surface is **one class + eight custom properties, seven glass-ui-owned**, with fallback posture inconsistent within one file (:84/:98/:121 yes; :100/:103-105/:112-118 no)."* **Nothing here is re-booked**: all five ride the ONE identity guard this row already carries (each bank says so in its own words — *"booked once at the census, never per component"*), and their landing is what makes the F-1 blast surface countable at the component altitude rather than at the record altitude. |
| **F-2** | **KF-ET-3** ⟨kf-EasingTarget:42⟩ | *"This component's exposure facts (4 specifiers here + 3 in the sidebar; sole consumer of `/fading-scroll` + `/toggle-group`) ride as the fold's rider."* |
| **F-3** | **KF-ES-33** ⟨kf-EasingSidebar:77⟩ | *"→ fold to SCH-1 → KF.W0; rider: this component's 3 subpath edges."* |
| **F-4** | **KF-ES-15** ⟨kf-EasingScene:54⟩ | *"This scene's exposure census (7 entry points across the closure) rides as the fold's rider."* (The same line books this record's C-19 firing — *"Fourth and fifth occurrences of frontier-blindness"*.) |
| **F-5** | **KF-KE-16** ⟨kf-KeyframesEditor:58⟩ | *"the subpath-import blast radius (`/dark`, `/forms`) is real until the fork reconciles."* |
| **F-6** | **KF-TFP-29** ⟨kf-TimingFunctionPanel:75⟩ | *"The panel-local rider: the 4.0.1-era prose (`:25-31`) is unpinned-rationale drift = the **KF-CO-17** row, not a dependency defect."* — a routing rider: it keeps a prose-drift row out of the dependency class. |
| **F-7** | **KF-AT-7**, declaration leg ⟨kf-AnimatedText:42⟩ | *"Declaration leg → KF.W0 (census F-1/SCH-1 §B-12 reconciliation, fold-by-reference, **three-way-drift evidence attached**)."* The **cascade-invisibility leg is KF.W4's** (instrument law) and is not booked here — the row splits by leg, and only the declaration leg is this wave's. |
| **F-8** | **L·B-3 / C·B-2**, the **discharge half** ⟨kf-ChannelOptions:31,:55,:149⟩ | The record's KF.W0 packet (`:149`) is *"the **L·B-3 discharge** (≡ census F-1, SCH-1) + the three-file disk↔master reconciliation … + the re-anchor law over every anchor in this record."* **C-4 carries only the second limb**; this row books the first, so the `:149` packet is whole. The component rider stays at C-1 (four subpath edges, `:143-146`/`TimingFunctionPanel:26-31`, *"the unpinned-drift half is live and is what KF-CO-1/2/18 measure"*). |
| **F-9** | **L/B-1** ⟨kf-ChromeDock:40⟩ | *"FOLD → census SCH-1/F-1 (KF.W0 substrate settle), per kf-App ruling 1."* Frontier-CURED; *"Both re-readers booked it BLOCKER without the substrate triage; carried corrected"* — which is why the id must exist here to be grepped. The densest-consumer rider is already at C-1. |
| **F-10** | **F-1 row C-11 / L-I1** ⟨kf-CSSPasteDialog:72⟩ | *"census row `lane-frontend.md:15` **RED**, already ruled 'F-1 first' (:612). **All six of this file's component imports ride it.** Fold by reference → the census's own F-1 row (KF.W0/W6 precondition). Not re-booked here."* **Sibling correction (R-1a)**: KF.W6 does **not** adopt this row — its W6-precondition arm is the fold's, not a W6 row. |
| **F-11** | **F-1 row L-5 / C-11** ⟨kf-KeyframesAddDialog:87⟩ | *"census row RED, ruled 'F-1 first.' **All six glass imports ride it**; L-5's MAJOR arm collapses to MINOR-on-F-1."* Same sibling correction as F-10: KF.W6 does not adopt it. |
| **F-12** | **L-9 = C-D-7 = C-D-9** ⟨kf-KeyframeCardList:120⟩ | *"FOLD by reference; **identity owned by lane-frontend §10 step 1**, exactly as kf-CSSCodeEditor's KF-CE-32 folded it. Never re-booked."* Rider: *"the barrel-vs-subpath inconsistency inside the 2-file pair is a named instance of lane-frontend §3.1 — extends that census row, no new booking; the subpath swap rides the F-1 declaration."* The KF-CE-32 precedent it invokes is **declared, not assumed** — see C-1.R below. |
| **F-13** | **KF-SKEL-7** ⟨kf-App.skeleton:42⟩ — **rides here from R-19b/C-22** | *"KF.W0 (re-anchor/re-count law: the §B-12 reconciliation adopts a cascade-level coupling arm beside the import census) + KF.W6 (the token-namespace audit is the natural home of the probe). F-1 itself: fold by reference to SCH-1, **never re-booked**."* Booked in `KF-W6-CARRY.md:90` by id; absent here until this round. Its witness is the census instrument's own falsifier and it is C-9's named counterpart. |

**Count discipline.** Thirteen ids, thirteen riders. C-1's blast-surface list is these thirteen plus the six record-level riders already inside C-1 — that union, not the six alone, is what C-9's cascade column must precede.

### C-1.G — the F-1 fold id index (22 ids whose CONTENT C-1 carries and whose ID did not — **row 22 added at repair round 3**)

These twenty-two rows are **already cured by C-1's mechanism** — each record terminates its row at census F-1/SCH-1 and the cure is stated once at C-1. What was missing is the **id**: under an id-keyed census, a row whose content is carried but whose id is absent by bytes is an escape, because **no later seat can grep this spec for its own row** and no closure check can distinguish "folded" from "dropped" (KF-W0-CHECK §2.C, E-19..E-31).

**Rows 14–21 were added at repair round 2 by RE-DERIVATION FROM THE BANK, not by adoption (R2-5(g)).** Round 1 populated this table from the pass-1 register — an inherited roster in a file whose C-19 law is *"the failure is never derivation; it is an unread registry"* — and it was short by eight. **The round-2 receipt read *"200 routing lines across 48 records"* and is STRUCK at repair round 3 (D-5): it reproduces at NEITHER reading, and a receipt with a third basis is the SCH-3 defect this wave was built to kill, committed inside the table that exists to prove the roster was measured.** The re-derivation method, **re-run this seat over the 58 records at their current bytes with its discard predicate written down so it reproduces** (both readings pasted, per the ruling): `grep -h -E 'KF\.W0|KF-W0' kf-*.md | wc -l` → **237** lines and `grep -l …| wc -l` → **50** records **pre-discard**; discarding taxonomy/routing-law boilerplate by the stated predicate `BP='ADJUDICATED, not VERIFIED|agglomeration input|wave sketch|census lane taxonomy|census taxonomy|Status verbs per M-25'` (**64** lines discarded) → **173 lines across 44 RECORDS** post-discard. **The record set — 44 — is the membership instrument, not the line count**: it reproduces exactly against `PASS-3/KF-W0-CHECK.md §0` (2026-08-28), whose post-discard line figure (167) differs from this seat's (173) by the *spelling of the discard predicate* and not by the substrate. **Two declared limits of the instrument** (the exclusion basis is derived by READING, never by token-matching): the six records the discard drops whole (`kf-AnimationVisualizer` · `kf-CSSCodeEditor` · `kf-KeyframeCardList` · `kf-KeyframeTimeline` · `kf-TimelineHoverPreview` · `kf-TransportDock`) carry their only `KF.W0` token inside a preamble; and a record can route here **without the token at all** — `kf-KeyframeCardList`'s real routing line is `:120`, booked at **C-1.F row F-12**, and contains no `KF.W0` string. The 44 is therefore a **floor** on membership and is used as one. Each survivor was resolved to its banked ⟨record : id⟩ pair; each pair grepped against this file **by bytes**, a hit counted **only when the id resolves to that record** (a bare `K-1` belonging to kf-SpringTarget does not book kf-MbabbMenu's `K-1` — this table's own rule). Byte receipts, re-run at this seat: `grep -c 'L-13'` → **0** · `'N-11'` → **0** · `'K3'` → **0** (and `'K-3'` → **0**) · `'F2'` → **0** · `'L-2 / C-2'` → **0** · `'MbabbMenu K-1'` → **0** · `'L-D9'` → **0**; kf-SpringTrace's `L-2` present only as kf-KeyframeCard's (row 4) — no SpringTrace pair. **Row 21 (kf-SquareInstrument L-D9) is this seat's delta against RULINGS-2 R2-5(g)'s seven-pair list**, declared loudly rather than absorbed: its banked terminal is *"**KF.W0 fold (the F-1 identity) + NO-WAVE-OWNER residue (census column note).**"* ⟨kf-SquareInstrument, the L-D9 row⟩ — the same class as the other seven, and it escaped the ruling's list for the same reason the other seven escaped round 1's: nobody re-derived. Rows 14–15 are the sharpest of the set — **kf-SequencePlayhead supplies C-19's founding dirty-manifest law** and both of its own KF.W0 folds were unindexed, so the record this spec cites for its central law could not grep this spec for its own rows.

**This table mints nothing and re-routes nothing.** Every id below keeps its banked spelling (anti-rename), the disposition of every one is *"folded at C-1, cured by the §B-12 act"*, and the leading number is an **index, not an id** — it is never citable as a row name. Where a row has a limb that is NOT the fold's, that limb is named and stays where it is booked.

| # | Banked id ⟨record : lines⟩ | Banked routing words | Limb not covered by the fold |
|---|---|---|---|
| 1 | **K17** ⟨kf-MatrixEditor:34,:128,:170⟩ | *"K17 ≡ census F-1/SCH-1 → KF.W0 §B-12"* | K17 is **also** a C-19 firing (*"RE-FIRED against both — SUBSTRATE-DEAD"*), enumerated there; the K1/FAM-03 MR4 `--project demo` CI rider is **KF.W4's** (§Sequencing) |
| 2 | **OD-41** ⟨kf-OrbitalDrag:94,:124⟩ | *"NO-WAVE-OWNER hygiene; I-3's residue ≡ census F-1 → KF.W0 §B-12"* | the NO-WAVE-OWNER hygiene half travels with its packet, not with the settle |
| 3 | **C-14** ⟨kf-PlaybackRibbon:141,:152⟩ | *"C-14 ≡ census F-1 → KF.W0 §B-12"* | the ribbon's own rows ride the **transport/ribbon** packet (R-15, KF.W13) |
| 4 | **L-2** ⟨kf-KeyframeCard:24,:103⟩ | *"glass-ui dependency family → census SCH-1 / kf-App ruling 1 → KF.W0"* | L-2 is **also** a C-19 firing — the reader-confirmation **REVERSAL** — enumerated there |
| 5 | **K-1 / L·D-2** ⟨kf-SequenceScrubber:71,:80⟩ | *"KF.W0 fold"* | **L·D-2's surviving refinement is BOOKED at C-9** (D-7), not here; its phantom-dependency arm died at K-1 |
| 6 | **K-1 / row 1** ⟨kf-SequenceTarget:21,:105,:154⟩ | *"the K-1 fold (census F-1/SCH-1, §B-12)"* | ST-1's affordance edit is the **sequence packet's** (KF.W11), never the settle's |
| 7 | **K-1** ⟨kf-SpringHeatmap:80,:117⟩ | *"→ KF.W0; sixth-plus baseline event"* | C-M-1's S-9 `./canvas` claim is a **mint input at C-17.R**, a separate limb |
| 8 | **K-1** ⟨kf-SpringScene:95,:149⟩ | *"KF.W0 §B-12: the K-1 substrate reconciliation"* | the observed-live `emphasis` migration is carried as C-2 inventory |
| 9 | **B-1 / K-1 / row 11** ⟨kf-SpringTarget:29,:87,:132⟩ | *"B-1 ≡ census F-1 ≡ SCH-1 ≡ kf-CubeScene M-1 residue → KF.W0 §B-12"* | the AT/announcement arm (D-5 aria-hidden half · D-6 · **D-14+N-4, one binding, never split**) is **KF.W9's** (R-1e) |
| 10 | **K-1 / row 7** ⟨kf-StartingStyleTarget:27,:89,:130⟩ | *"K-1 ≡ census SCH-1 ≡ F-1 → KF.W0 §B-12"* | KF-EST-21's scoping + EST ruling 7's direction are **corrected by C-21**, per-file |
| 11 | **L-20/C-7** ⟨kf-SquareScene:108,:119⟩ | *"residue → KF.W0 §B-12"* | the comment-stated-invariants **test obligation** is NO-WAVE-OWNER (§Sequencing); **C S-C**'s parser posture is **KF.W2's F0** (R-1b), never re-probed here |
| 12 | **K4/K5** ⟨kf-LayerConfigPanel:88,:97,:125⟩ | *"K4/K5's manifest schism ≡ census F-1/SCH-1 → KF.W0 §B-12"* | ruling 8's *"consumed-but-mis-called"* class is a **mint input at C-17.R** |
| 13 | **i-1 / F-1 / row 7** ⟨kf-RibbonBar:26,:60,:128⟩ | *"KF.W0 §B-12: the phantom-dep substrate schism (K10 …)"* | K10's 18-subpath/44-site rider is already inside C-1's blast surface |
| 14 | **L-13 / C-9** ⟨kf-SequencePlayhead, the L-13/C-9 row (at `:71`)⟩ — *added round 2* | *"the F-1 limb stays a fold (≡ census F-1/SCH-1 → **KF.W0 §B-12**) … **KF.W0 fold + NO-WAVE-OWNER residue.**"* | the `--radius-pill` **import-invisible producer-token edge** and K-14's ~1px caps are the NO-WAVE-OWNER residue — the sequence scene-repair packet's, never the settle's; the cascade-coupling limb is C-9's column |
| 15 | **N-11** ⟨kf-SequencePlayhead, the N-11 row (at `:83`)⟩ — *added round 2* | *"the manifest triangulation (fold ≡ census F-1/SCH-1, **KF.W0 §B-12**)"* | the pattern-ledger entries it carries (r2's two inheritance events + K-21 + K-30's arithmetic-slip variant) are **C-19's ledger**, enumerated there |
| 16 | **K3** ⟨kf-CopyButton, the K3 killed-claim row (at `:91`)⟩ — *added round 2* | *"K3 (reader-2) · census F-1 'absent from package.json' — SUSTAINED IN DIRECTION … **ONE identity with census SCH-1/kf-App ruling 1 → KF.W0.**"* | the mechanism correction (HEAD's lock = 3 consistent entries, not 0; `npm ci` at HEAD **downgrades** rather than fails) is carried in C-1's mechanism cell; the ~94 dirty src rows behind this corpus's src anchors are C-2's inventory |
| 17 | **F2** ⟨kf-EditorHeader, the F2 fold row (at `:34`)⟩ — *added round 2* | *"**F2** — L-B2 / C-13 / C-18 / C-14 (phantom dep family) → census F-1 ≡ SCH-1 (**KF.W0**), per kf-App ruling 1."* | the record's other KF.W0 rows are separately booked and stay so: **EH-1** at C-14, **L-i2** at C-15, **the S-9 DELETE claim at C-17.R — booked under `F1 ≡ D-1/L-B1/C-1 → kf-App KF-APP-41` (`:33`), the coined `F1(c)` RETIRED at repair round 3 (D-2/E-9); the claim's own sentence at `:117` is id-less at the bank and is stated as such, never re-spelled from this end**, the two-unmet-peers rider at C-16, kill #1's universal-reset decider at C-10 |
| 18 | **L-2 / C-2** ⟨kf-EditorShell, the L-2/C-2 fold row (at `:39`)⟩ — *added round 2* | *"**L-2 / C-2 — FOLD → census SCH-1/F-1 (KF.W0)**, per kf-App ruling 1: frontier-CURED."* | this record's four other rows are already booked elsewhere and are NOT this row: **THE DELETION** (C-3), **DISSENT 3** (C-20), **DISSENT 1** (C-19), **L-17** (C-15) |
| 19 | **K-1** ⟨**kf-MbabbMenu**, the K-1 killed-claim row (at `:128`, restated at `:170`)⟩ — *added round 2* | *"**K-1's residue ≡ census F-1/SCH-1 → KF.W0 §B-12**"*; *"KF.W0 §B-12: K-1's residue."* | **id-collision note (this table's rule)**: seven other records bank a `K-1`; this row is **kf-MbabbMenu's**, and only a record-qualified grep books it. Its ruling-2 blast rider (8 specifiers / 5 modules via `@components/instrument/shell`) is at C-1; **K-2** is a C-19 firing |
| 20 | **L-2** ⟨**kf-SpringTrace**, the L-2 fold row (at `:129`, restated at `:139`)⟩ — *added round 2* | *"**L-2 ≡ census F-1 ≡ SCH-1 ≡ kf-AmigaScene C-1 → KF.W0 §B-12**"*; *"KF.W0: the L-2/F-1 substrate fold (§B-12; re-measure obligation)."* | **id-collision note**: distinct from kf-KeyframeCard's `L-2` at row 4 — the two are different records' rows and the pre-round-2 file booked only the latter. The **re-measure obligation is DISCHARGED** by C-1's dated re-measurement; **K-6** is C-17.R's enumerated dead occupant |
| 21 | **L-D9 (cascade half)** ⟨kf-SquareInstrument, the L-D9 row (at `:84`; the F-1 limb restated at `:136`, the wave roster at `:144`)⟩ — *added round 2; **the delta against RULINGS-2's seven***, R2-5(g) | *"**KF.W0 fold (the F-1 identity) + NO-WAVE-OWNER residue (census column note).**"* — the `b`/no-glass-ui label true of the import graph and misleading at the render (every typography rung + four of six custom properties are producer-owned strings no compiler validates) | the **census-column note** is C-9's cascade-coupled column (this record is already named in C-9's provenance — content carried, id absent until now); **C-12** is C-17.R's S-10 mint input; the flat-namespace `--rainbow-*` datum and D-20's producer-truth correction are KF.W6's |
| 22 | **The F-1 family — `L-1 · C-11 · D §6`** ⟨**kf-SequenceScene**, the F-1 family row at `:91`, the killed-claim row at `:99`, routed at `:145` and `:153`⟩ — ***added at repair round 3 by the properly-run 44-record sweep (D-5/E-1); this is what a sweep reporting 44 records lands and a sweep reporting 48 did not*** | *"**The F-1 family (L-1 · C-11 · D §6)** ≡ **census SCH-1** / kf-App ruling 1"* (`:91`); routed *"The F-1 limb ≡ census SCH-1 ≡ kf-App ruling 1 → **KF.W0 §B-12**"* (`:145`) and *"**KF.W0**: the F-1 fold (§B-12) **+ the countable-cell inheritance datum**"* (`:153`). The record's own kill is the BASELINE-LAW TRIPLE KILL at `:99` — *"the 0/0 read the dirty worktree; `origin/master:package.json:77` = `"@mkbabb/glass-ui": "7.0.0"` exact devDep … `git diff origin/master -- demo/scenes/sequence/` EMPTY so every other citation binds clean"* | **The record routes TWO things here and only one was carried (E-2).** The second is the **countable-cell inheritance datum** — *"the pattern ledger gains three re-inheritance events (one per axis) **plus the '24-member' countable-cell signature (killed-claims #14) — the class kf-App.md predicted by name**"* (`:145`). The three re-inheritance events are already enumerated at **C-19**; the **`24-member` countable-cell signature is booked HERE, by id, as this row's named limb** — it is the *shape* evidence (a claim whose cell count is countable and was counted wrong by every axis), not another firing, so it rides the fold rather than the ledger. `D §6`'s *"re-verify after F-1 lands"* rider is **VOID at the bank** (`:99`) and is not re-raised |

**Why an index and not twenty-two more cures.** C-1.F adopts rows that were **not** carried — ids *and* riders both absent, riders that M-25 forbids dropping. C-1.G is the opposite case: the cure was never missing, only the handle. Booking them as fresh cures would re-book once-booked rows — the exact tenth-re-booking defect §Excluded convicts. Indexing them gives a closure sweep **22** ids to resolve to a named disposition instead of to silence. **This note asserts no closure in its own voice (LAW B, repair round 3)**: the carriage of this table is measured at `docs/tranches/X/keyframes/conformance/PASS-3/KF-W0-CHECK.md` §0/§1 (2026-08-28), which found **routed 128 · booked 118 · escaped 10** against the round-2 file — of which this round books **E-1/E-2** (row 22), **E-4** (C-21/G-0.9), **E-5** (C-17.R row 4), **E-6** (C-1.R), **E-7/E-8/E-9** (C-17.R) and **E-3/E-10** (**§Sequencing, the NO-WAVE-OWNER packets row** — `:651` at this writing, parenthetical and non-load-bearing; the round-3 cell's `:447` is corrected at repair round 4, D-4); a later pass re-points this citation rather than re-asserting the claim. **The membership is the re-derivation's, not a register's** (R2-5(g)) — and the next seat re-derives it again **with its own discard predicate written down**, rather than copying this table, because a roster adopted twice is a roster nobody has measured and a roster whose predicate is unstated is a roster nobody can re-measure.

### C-1.R — declared re-routes, corrections and no-re-book guards (7 rows, loud per E-3 — **row 6 added at repair round 4, D-1; row 7 — `L-M-9` — added at repair round 5, D-3**)

A routing this spec states differently from its bank is a **ruling**, and a ruling that is not declared is a silent rewrite. Both live here — **and, from repair round 3, so does a routed pair whose banked disposition is "do nothing", because a routing word of *"no re-book"* is still a routing word and dropping it is still a silent drop** (row 5, E-6). **Row 6 (repair round 4, D-1) is the same class at the largest amplitude in the corpus**: an obligation whose CONTENT this file carries better than any file in the program and whose **id** — the one 42 records route their re-anchor obligation on — reached no table until now.

| Subject | The bank's words | This spec's reading | Disposition |
|---|---|---|---|
| **KF-CE-32** (cures **D-10**) | `kf-CSSCodeEditor.md:68` — *"**KF-CE-32 · L-i4 = C-16 — FOLD, not booked here** … identity owned by **lane-frontend §10 step 1** … → **FOLD → census F-1 (KF.W6-adjacent sequencing)**."* | The §Sequencing KF.W12 edge calls KF-CE-32 *"census F-1's owning id, discharged at KF.W0 §B-12"* — i.e. an owning id, and W0's. | **DECLARED RULING, not a silent change.** The two readings are reconcilable and the reconciliation is stated: **identity ownership stays with lane-frontend §10 step 1** (the bank's, untouched); what W0 discharges is the **F-1 substrate leg** that KF-CE-32 folds into, and the *"KF.W6-adjacent sequencing"* clause is preserved as a sequencing rider on the prose recut, which is KF.W6's. An **E-3 addendum under the original id** records the reading at the bank so a later seat reads the ruling and not a contradiction. kf-KeyframeCardList F-12 invokes this precedent by name, so the precedent must be legible. |
| **The `build:gh-pages` command** (cures **D-2**) | Five banked/derived cells say *"a fresh `build:gh-pages`"*. | No such script exists at HEAD, `origin/master` or the worktree. | **The banked words are carried verbatim** (E-3 — dated evidence is never rewritten); **the act is corrected** to `npm run gh-pages` at all seven execution sites (R-11). |
| **`kf-AnimationVisualizer.md:9`** | The census cell as banked. | Corrected to **worktree-scope** by the kf-SequenceScrubber K-1 kill. | Appended correction under G-0.9; carried at C-9. |
| **MISS-α5 ≡ RR-β K-BASE** ⟨kf-TimelineCaret:45⟩ — the attached correction (KF-W0-CHECK §2.D **E-32**) | The row *"also **CORRECTS the banked kf-KeyframeTimeline header's 'working tree = the audited tree' equation**"*. | The row's substrate CONTENT — hybrid tree, merge-base `a59d3a22`, untracked-but-cited-by-line — is carried at C-2 / G-0.1. The **attached correction** was not: `grep -c 'KeyframeTimeline'` in this file before this round → **1**, and that one hit is C-10's PRM-roster cell, not a correction obligation. | **BOOKED as a G-0.9 appended-correction obligation under the banked id** (E-3: an addendum at `kf-KeyframeTimeline`'s header; dated evidence never rewritten). The equation *"working tree = the audited tree"* is **false at every coordinate this wave measures** — 41 behind / 1 ahead, neither ref an ancestor of the other — so a record whose header asserts it cannot state which ref it cures against, which is G-0.9's whole content. Same class as C-22's two byte-level corrections and the named C-20 test case. |
| **kf-SpringTrace `D-15`** — the dual-owner-token **NO-RE-BOOK guard** ⟨`kf-SpringTrace.md:72`⟩ — *added at repair round 3 (E-6)* | *"**D-15** — dual-owner token consumption (`--color-progress` demo-owned, `--border` vendor-owned) with no call-site qualification; corroborates lane-frontend §6.3; the banked measurement (kf-AnimationVisualizer KF-AV-42: collision set EMPTY today) keeps it a dated hazard note. **KF.W0 fold-adjacent; no re-book.**"* | The pair was **absent by bytes** before this round: `grep -n 'D-15' KF-W0.md` returned **one** hit, in §Excluded's header-ribbon-migration row (*"the cheapest close of the banked blocker AND of D-15/D-17"*) — **kf-EditorHeader's family, a different record's ids entirely.** Under this file's own record-qualification rule that hit does not book kf-SpringTrace's `D-15` | **BOOKED AS A ONE-LINE GUARD, NOT A CURE — `kf-SpringTrace D-15: KF.W0 fold-adjacent, NO RE-BOOK (banked `:72`)`.** *"Fold-adjacent; no re-book"* **is a routing word**, and a routing word that reaches no table is a silent drop — the defect class this program exists to kill, per this file's own §Excluded law. The guard exists so a later seat greps `D-15`, finds **two record-qualified rows**, and re-books neither: kf-SpringTrace's is fold-adjacent-and-closed (its collision set is measured EMPTY at `kf-AnimationVisualizer` KF-AV-42), kf-EditorHeader's `D-15/D-17` ride the header-ribbon-migration decision at §Excluded (NO-WAVE-OWNER, SS-1/SS-2's). **No cure is spent here and none is owed** |
| **`D-19` — THE RE-ANCHOR / RE-RESOLVE-BEFORE-CURE LAW's banked id** ⟨the corpus's most-cited routing id⟩ — *added at repair round 4 (D-1/E-1)* | Quoted by command, three exemplars of one sentence-shape (LAW D — `grep -n 'D-19' <record>.md`, run this seat at the corpus's current bytes): ⟨kf-RibbonBar.md:35⟩ *"Anchors bind to worktree ≡ `origin/master` `81a56990`; re-anchor before any cure (D-19 law)."* · ⟨kf-EditorHeader.md:29⟩ *"Line anchors are worktree ≡ `origin/master` for EditorHeader.vue (verified this session); re-resolve all EditorShell/MbabbMenu anchors at `origin/master` before any cure (D-19 law + kf-App ruling 1)."* · ⟨kf-TimelineTrack.md:155⟩ *"every sibling anchor in this record is worktree-resolved and must be re-resolved before any cure (D-19)."* | **The CONTENT is carried whole and the ID was absent by bytes** — `grep -c 'D-19' KF-W0.md` before this round → **0**, in the wave that owns the re-anchor. **The census, re-run this seat with both readings and its predicate stated so it reproduces** (LAW D numeric arm; `cd docs/tranches/V/megatranche/registry/adjudicated`): `grep -l 'D-19' kf-*.md \| wc -l` → **42 records** · `grep -h 'D-19' kf-*.md \| wc -l` → **94 lines** (bare token); filtering to the re-anchor sense by the stated predicate — *a `D-19`-bearing line that also matches `re-anchor\|re-resolve\|Re-anchor\|Re-resolve`* — → **34 records / 39 lines**. `PASS-4/KF-W0-CHECK.md` §1 E-1 (2026-08-28) reports **35 records** for the same class; the delta is the **spelling of the context predicate**, not the substrate — the identical class this file already declares for the 167-vs-173 sweep reading — and it is declared, not absorbed. **Two of the three records this file never names** (`kf-ChannelControls` `:150`, `kf-TimelineTrack` `:155`; `grep -c 'kf-ChannelControls' KF-W0.md` → 0, `grep -c 'kf-TimelineTrack' KF-W0.md` → 0) **reach this wave through this id alone**; the third (`kf-SpringPhysicsFacet`) carries no `D-19` (`grep -c` → 0) | **BOOKED AS A CARRY-NOT-CURE GUARD, NOT A FRESH CURE — `D-19: the re-anchor/re-resolve-before-cure law. CONTENT carried whole at C-2 (the tree-motion law + the binding form) · C-21 (the per-file proof and its one measured offset) · G-0.9 (the stamp whose whole GREEN is that every record names the ref it cures against). NO RE-BOOK.`** Nothing is re-booked and no cure is spent: indexing is not curing, it is what makes the carriage checkable — C-1.G's charter, applied to the id that charter never swept for. **Record-qualification is load-bearing, because `D-19` is an OVERLOADED token** — the same lesson C-17.R wrote for `S-10` one round ago, applied here: ⟨kf-KeyframesAddDialog.md:69⟩ *"**KAD-16 — dead-token cluster** (D-12/D-18/D-14/D-19 + R-A `relative`)"* is a component-local row id; ⟨kf-RibbonBar.md:117⟩ *"**D-19** — which clamp arm binds with `--glass-tint-strength-aa: 0%`"* is a local probe id; ⟨kf-EditorHeader.md:36⟩ *"**F4 — D-19 (`icon-lg` 24px vs producer `--icon-lg` 20px)**"* is a local fold row. **Eight records carry `D-19` in no re-anchor sense at all** (`kf-ChannelOptions` · `kf-CubeScene` · `kf-KeyframesStringControls` · `kf-OrbitalDrag` · `kf-SquareScene` · `kf-SquareInstrument` · `kf-StartingStyleTarget` · `kf-TimelineCaret`, by the predicate above), so a bare `grep 'D-19'` books rows this guard does not own. **G-0.9's stamp is keyed to this row** (see the gate's GREEN, clause (vii)): the `.e` seat sweeping the 58 records for the re-anchor obligation **by its banked id** now has a row to key the stamp against, which is what E-1 found it did not |
| **`L-M-9` — the MAJOR F-1 FOLD of `kf-KeyframesStringControls`, whose id resolved to the WRONG LIMB of this spec** ⟨`kf-KeyframesStringControls.md:86`⟩ — *added at repair round 5 (D-3/E-1; RULINGS-5 §R5-14, which routes it HERE and not to C-1.G)* | Quoted by command (LAW D — `grep -n 'L-M-9' kf-KeyframesStringControls.md`, run this seat at the corpus's current bytes): ⟨`:86`⟩ *"**L-M-9 — MAJOR · FOLD → census F-1 ≡ SCH-1 → KF.W0. NOT re-booked.** Riders: the two transitive critical-path edges verified by this seat (`CSSCodeEditor.vue:38` — this component's entire render; `useHighlightCSS.ts:2` via `useApplyCSS.ts:4` — the Apply-CSS driver); and the substrate correction: `origin/master:package.json:77` already carries `"@mkbabb/glass-ui": "7.0.0"` (my `git show`) — the phantom is a checkout-lag artifact on the audited disk; KF.W0's reconciliation discharges it, and the "clean `npm ci` fails" probe is RETIRED from the residue."* — **one banked row with TWO limbs**: a MAJOR fold that terminates at this wave, and a probe that is retired. | **The CONTENT is carried whole and the ID resolved to the WRONG LIMB by bytes.** Both riders are carried **verbatim** in C-1's blast-surface list (*"kf-KeyframesStringControls' two transitive critical-path edges (`CSSCodeEditor.vue:38`; `useHighlightCSS.ts:2` via `useApplyCSS.ts:4`)"*) and the substrate correction is C-1's own mechanism cell — so **nothing was lost but the handle**. What the handle did instead was worse than silence: `grep -o 'L-M-9' KF-W0.md \| wc -l` → **1** before this round, and that single hit was **C-1's RETIRED-probes clause** — so a seat greping the banked id of a **MAJOR** fold landed on a *retired* disposition and a *spent* budget, and would have read the fold as discharged-by-retirement. **Why this table and not C-1.G**: C-1.G's class is *id absent by bytes*; this id was **present and mis-resolving**, which is a reading this spec states differently from its bank — C-1.R's own class (*"a routing this spec states differently from its bank is a **ruling**, and a ruling that is not declared is a silent rewrite"*). **Membership receipt, re-run this seat**: thirty-four records fold F-1 to KF.W0 under a banked id; thirty-three carry a **C-1.F** row (13) or a **C-1.G** row (22 — `kf-SequencePlayhead` twice); `kf-KeyframesStringControls` was **the single omission**, and it is not a marginal record — it supplies C-2's tree-motion law (DISSENT 3) and the F-R7/N-2 `backward` divergence datum. The structure of the miss is C-1.G's own mechanism (*a receipt that proves the thing next door*): the record's five KF.W0 tokens sit at `:6` · `:9` · `:86` · `:161` · `:167`, and rounds 2–4 harvested `:6` (F-R7/N-2), `:161` (C-1's retired probe) and `:167` (C-2's law) — **three of five, every one of them a non-fold limb**. | **BOOKED AS A FOLD-INDEX ROW AND A LIMB DISAMBIGUATION, NOT A FRESH CURE — `L-M-9: MAJOR · FOLD → census F-1 ≡ SCH-1 → KF.W0 §B-12. CONTENT carried whole at C-1 (both transitive critical-path edges, verbatim; the substrate correction in C-1's mechanism cell). NO RE-BOOK — the fold is cured by the §B-12 act, once, at C-1.`** Indexing is not curing; it is what makes the fold's closure checkable — C-1.G's charter, applied to the one F-1 record that charter's sweep walked past. **No cure is spent here and none is owed.** **The second limb is disambiguated at its own site in the same edit**: C-1's *"Probes RETIRED here"* cell now reads ⟨kf-KeyframesStringControls `L-M-9`, **the RETIRED sub-clause of that row — NOT its MAJOR fold limb**, which is booked here⟩, so from this round a seat greping `L-M-9` finds **exactly TWO dispositions, each stating which limb it disposes** — the **MAJOR fold at this row (C-1.R row 7)** and the **spent probe at C-1's *"Probes RETIRED here"* cell** — and re-books neither. **Stated as an enumeration and not as a numeral** (LAW D(3)): every other occurrence of the id in this file is a **pointer** to one of those two — the §Repair Round 5 authority paragraph, its D-3 directive row, its substrate receipt and its altitude sweep, the §Carry preamble's sub-table description, and this table's own heading — and **none of them is a third disposition**. That is the whole test a later seat runs: *not how many hits, but how many dispositions* |

### C-17.R — the census-slot claimant roster (R-5 · the mint's input set, re-derived at repair round 3, **EXTENDED at repair round 4 by the corpus-side S-10 re-derivation, D-3**)

**The mint is the only slot authority; every row below is an INPUT, not a slot.** Fresh non-colliding ids are assigned in **ONE motion**, each citing its originating record. A dead or refused claimant is **enumerated, not minted** — enumeration is what stops the next seat re-raising it.

**Carriage claim, per LAW B (repair round 3, citation re-pointed at repair round 4): this table does NOT assert its own completeness.** Its carriage is measured in the freshest conformance census artifact — **`docs/tranches/X/keyframes/conformance/PASS-4/KF-W0-CHECK.md` §1 (2026-08-28)** — which found the round-3 roster **short by a FOURTH S-10 claimant, `KF-ET-27`** (E-2), short by its second provenance leg **`KF-ES-20`** (E-3), and carrying a **row-13 re-derivation receipt that reproduces at no reading and closes in this file's own voice** (D-3); all three are cured below. The predecessor artifact `PASS-3/KF-W0-CHECK.md` §1 (2026-08-28) found the round-2 roster **missing `KAD-12`** (E-7), **mis-keyed on the `/number-field` claim** (E-8), **coined at `F1(c)`** (E-9) and **anonymous at row 13**; those four were cured at round 3. A later pass re-points this citation rather than re-asserting the claim. **The re-derivation this seat ran, stated so it reproduces**: the S-9/S-10 claimants are enumerated in exactly one place in the tree as a set — this wave's **sole carry**, `KF-W6-CARRY.md` `:174`–`:177` (the S-9 quartet: CPD R-5 · **LP-8 ≡ KF-CO-35 + ME-18** · **KAD-12** · DGC toast) and `:178`/`:179` (S-10 · KF-KE-21, S-10 · KSM C-9) — the same block `KF-W10.md` §2b cites as the ground `CARRY-C-3` terminalizes; plus the corpus-side claims, re-derived by `grep -n 'S-10' kf-*.md` over the 58 records. **All four carry claimants are now booked.** **Two instrument limits, declared** (repair round 4 adds the second): (i) `S-10` is an **overloaded ID** — three records use it as their own roster index (`kf-App.skeleton.md:91` · `kf-ChromeDock.md:125` · `kf-ControlsPaneWrapper.md:122`), none of them a census-slot claim, and that overload is what manufactured a phantom claimant at row 13 (see below); (ii) **`S-10` is also an overloaded SUBSTRING** — `grep -n 'S-10'` matches `KF-ES-10` (×11), `MISS-10` (×4) and `KF-SS-10` (×3), which is how the round-3 seat read **past** a live claimant while reporting a smaller set than its own command returns. The discard rule and both readings (**37/15 raw · 19/9 post-discard**) are pasted at row 13, where the enumeration lives; every cell that depends on them points at that enumeration and re-issues no numeral.

| Claim | Banked id ⟨record : line⟩ | Subject | Mint disposition |
|---|---|---|---|
| S-9 | **C-M-1** ⟨kf-SpringHeatmap:46,:128⟩ | glass `./canvas` (present at installed 7.0.0, 73 exports) | MINT |
| S-9 | **F1 ≡ D-1 / L-B1 / C-1 → kf-App KF-APP-41** ⟨kf-EditorHeader:33; the S-9 ask itself at the record's `## Verdict` DELETE line `:117`, **which carries no id**⟩ — **`F1(c)` RETIRED (D-2/E-9, repair round 3)** | `EditorHeader → HeaderRibbon` — DEAD, replace-by-deletion | MINT (the slot kf-SharePopover was refused against). **The coined id is struck at every altitude.** Receipt, re-run this seat: `grep -rn 'F1(c)' registry/adjudicated/` → **0 hits, repo-wide**, in a table whose own preamble reads *"no id in this file is coined, renamed, or renumbered"* and whose C-1.G preamble reads *"Every id below keeps its banked spelling (anti-rename)"* — **three of this file's own laws convicted the cell.** The record's real fold ids are `F1` `:33` · `F2` `:34` · `F3` `:35` · `F4` `:36`; the S-9 claim lives at `:117` (*"the census gains S-9"*) and is **id-less at the bank**, which is stated here rather than repaired by coinage — E-3 forbids minting a bank id from this end |
| S-9 | **LP-8 ≡ KF-CO-35 + ME-18** ⟨`KF-W6-CARRY.md:175`; provenance legs kf-LayerConfigPanel D-5/C-7 · kf-ChannelOptions RR-B net-new · kf-MatrixEditor C-8 · kf-CubeScene L-5/C-6⟩ — **RE-KEYED to the carry's head id (D-2/E-8, repair round 3)** | the number-field row; *"the 'consumed-but-mis-called' class needs a FRESH id (ruling 8 — the S-9 collision)"* | MINT (the `/number-field` **evaluation** is KF.W6's). **Anti-rename is a two-way law**: round 2 booked this claim under **two of the carry's four provenance legs** and under **neither of its head ids**, so a later seat grepping the carry's id found nothing and a seat grepping this file's id found a different record. Both head ids verified at the bank this seat: `kf-LayerConfigPanel.md:53` (`LP-8 \| D-5 / C-7 (coercion limb = KF-CO-35)`) and `:95` (*"**LP-8 ≡ KF-CO-35 + census S-9** (→ KF.W6 evaluation, ME-18 precedent)"*); `kf-ChannelOptions.md:90` carries KF-CO-35. The four provenance legs are retained beneath the head id, never in place of it |
| S-9 | **C-6** ⟨kf-AmigaScene:61,:146⟩ | `Surface`/`PaperBackdrop`/`Card` shadowed by 8 lines of bespoke CSS — *"extends S-1..S-8"* (**the block**, not S-1 — D-9) | MINT — **owned by KF.W6** (E-5, repair round 3: the banked sentence at `:146` reads *"booked ONCE here, extends S-1..S-8, **owned by KF.W6**"*, and `:61` states the owner's terms — *"KF.W6 (evaluate-not-swap; the `:6–12` no-DOM-layer ruling may reduce the cure to a shared `.stage-plate` recipe)"*. The owner limb was dropped from this cell while **every sibling cell carries one**; it is restored, so the mint assigns identity and KF.W6 evaluates) |
| S-9 | **KF-AV-28 · D-18 = C-13** ⟨kf-AnimationVisualizer:72⟩ | *"the shadow census skipped the larger of the two rail/ball shadows … ScrubberTimeline (256 L vs the censused 162-L SequenceScrubber)"* | MINT — **and this id is KF.W7's defining supersession rider**, so the minted slot carries the rider by name: KF.W7's S-9 evaluation may supersede a behavioural cure on a governed row, and the row's own verdict (*"evaluate, not swap"*) is KF.W7's intake, never W0's |
| S-9 | **R-5** ⟨kf-CSSPasteDialog:43⟩ | *"bespoke shadow of shipped `Textarea`, census-invisible"*, sized at 1 site; *"census row 80 classified `G` on import-list evidence alone"* | MINT (the suffusion cure is KF.W6's) |
| S-9 | **D-5 / L-3 / C-2** ⟨kf-DemoGlobalChrome:43⟩ | the `./toast` family (= C-1.F row F-1) | MINT with the identity guard; **swap verdict census-owned, not W0's** |
| S-9 | **K-6 / D-§S-9 = C-S-B** ⟨kf-SpringTrace:89⟩ | *"S-9 · SpringTrace — JUSTIFIED BESPOKE"* — **KILLED AS STATED** (`generateCurveSVGPath` *"was one `ls` away from a directory both axes cited"*) | **ENUMERATED AS A DEAD OCCUPANT, NOT MINTED.** The row *"must be re-derived (0..1-range caveat included) before any census landing"* — that re-derivation is not this wave's |
| S-9 | **KAD-12** ⟨kf-KeyframesAddDialog:56, C-3/D-23b; the claimant row in this wave's **sole carry**, `KF-W6-CARRY.md:176`⟩ — **ADOPTED at repair round 3 (D-2/E-7)** | *"S-9 · KAD-12 — REGISTRY ADOPTION … the reformat hotkey hand-rolls **a second global keyboard mechanism** beside the house `/keyboard` registry: the demo's sole `useMagicKeys` site, while the registry has **19** `registerShortcut` calls surfaced by `KeyboardShortcutsModal`; this binding enters none of them, is **layout-dependent** (macOS `Ï` / US-literal disjunct), and leaks a window listener for the component's life"* | MINT — **the cure is KF.W6's, not this wave's**: banked exact, *"`registerShortcut("Shift+Alt+KeyF", reformat, {allowInInput:true, label, group})` — glass ships `allowInInput` + an `e.code` fallback — **WHICH DISSOLVES THE ENTIRE `Ï` CLUSTER**"*, and the record routes the `Ï` cluster **→ KF.W6** at `:90`. W0 fixes the identity only. **A claimant in the wave's own sole carry that never reached the roster is the exact mechanism of G-0.6's second falsifier** (*"a mint run over a short roster re-collides on its first motion"*), which is why the adoption is booked by id and not by content |
| S-9 | **C·C-2's census amendment** ⟨kf-SharePopover:90⟩ | *"ALREADY CLOSED, TWICE … the S-9 slot is spoken for by an adjudicated booking (kf-EditorHeader.md:117). **Refused per anti-rename**"* | **ENUMERATED AS REFUSED, NOT MINTED** — the refusal is itself the anti-rename law working, and it is recorded so it is not re-litigated |
| S-10 | **C-9** ⟨kf-KeyboardShortcutsModal:29,:68⟩ | the `/command` family shadow | **CLAIM-INPUT ONLY (R-5).** The seat's own *"COLLISION — renumber to S-10"* ruling is **superseded by this mint**; an E-3 addendum under the original id records it. The seat's booked lesson stands verbatim: *"census extension ids must be allocated against the banked registry, not against the local corpus."* |
| S-10 | **KF-KE-21** ⟨kf-KeyframesEditor:63⟩ | the progress bar as a shadow of glass `./progress` | MINT (the `Progress` evaluation is KF.W6's; binding it to something true is KFED-UNIT's) |
| S-10 | **C-12** ⟨kf-SquareInstrument:78⟩ | the status pill vs glass-ui `Chip`, *"correctly evaluate"*; *"NO-WAVE-OWNER (census S-10 intake; the shape completion is a design-idioms.css edit)"* | MINT — an **intake row, not a re-derivation of S-1..S-8** |
| S-10 | **KF-ET-27** ⟨kf-EasingTarget:69⟩ — **ADOPTED as the FOURTH S-10 claimant at repair round 4 (D-3/E-2)**; second provenance leg **`KF-ES-20`** ⟨kf-EasingSidebar:61⟩ (E-3) | the **`design-idioms.css` census arm** — glass overlap + contract truth. Quoted by command (LAW D; `sed -n '69p' kf-EasingTarget.md`): *"**MINOR.** → **NO-WAVE-OWNER** (one home for 14px + fix the sheet); C's proposed **S-10 census arm** over design-idioms.css (glass overlap + contract truth) is ADOPTED as a KF.W5 input."* Second leg (`sed -n '61p' kf-EasingSidebar.md`): *"**MINOR.** → **KF.W5** (census-truth arm, with the KF-ET-27/S-10 design-idioms precedent)."* | **MINT — and the disposition travels with the slot: *the design-idioms census arm is KF.W5's*.** Two independent records name one S-10 census arm and route it to a sibling wave; the mint that declares itself re-derived *"at the corpus bytes"* saw neither until this round. **Why the mint must see it even though the arm executes elsewhere**: §Scope 6's monopoly — *"no wave other than KF.W0 mints or renumbers a census slot"* — makes an **adopted** S-10 arm either a claimant the mint enumerates or a monopoly breach that must be ruled; it is enumerated. **W0 assigns the identity only**; the arm itself is KF.W5's act and is named as such at the §Sequencing KF.W5 row. G-0.6's second falsifier is what this row answers: *"a mint whose roster is short by a claimant the wave's own carry enumerates is the defect, not an approximation of the cure"* — round 3 closed the carry-side short roster (KAD-12) and left a **corpus-side** one open |
| S-10 | ~~the fifth record's original request~~ — **NAMED AND STRUCK AS A DUPLICATE (D-2 row 13, repair round 3)** | **The row is kf-KeyframesEditor's own S-10 ask** — C-17's provenance cell lists five records and its fifth is *"kf-KeyframesEditor asks S-10"* ⟨`kf-KeyframesEditor.md:63`, C-M6 + reader-1 miss M-4 merged⟩ — **which is already booked one row above as `KF-KE-21`**. The row was the same claim entered twice, the second time without its id | **STRUCK, NOT MINTED — enumerated here so the strike is checkable.** The row as written **could not be minted from**: C-17's own falsifier is *"a minted slot that does not cite its originating record fails too — the citation is what makes the next collision detectable"*, and a row that does not know its record cannot cite it. **Re-derivation RE-CUT AT REPAIR ROUND 4 (D-3) — the round-3 receipt is STRUCK: it did not reproduce, and its closing sentence asserted closure in this file's own voice inside the row that cites LAW B as its authority.** The round-3 text read *"`grep -n 'S-10' kf-*.md` … returns **exactly three census-slot claims** … **There is no fourth or fifth S-10 claimant in the corpus.**"* Both halves are false at the bytes. **The reading of record, re-run this seat with its counting rule stated AT the enumeration (LAW D(3)), `cd docs/tranches/V/megatranche/registry/adjudicated`**: `grep -n 'S-10' kf-*.md \| wc -l` → **37 hits** · `grep -l 'S-10' kf-*.md \| wc -l` → **15 records** (raw). **Most of the excess is a SUBSTRING ARTIFACT, and the discard rule is written down so membership reproduces: `KF-ES-10` (×11) · `MISS-10` (×4) · `KF-SS-10` (×3) are NOT `S-10`.** After the discard: **19 lines across 9 records**, enumerated whole, one line per `grep -n` hit — **four census-slot claimants by record**: `kf-KeyboardShortcutsModal.md:29`/`:68` (+`:127` restatement — C-9, renumbered off a local corpus) · `kf-KeyframesEditor.md:63` (+`:128`/`:172` — KF-KE-21) · `kf-SquareInstrument.md:78` (+`:129`/`:140`/`:145` — C-12) · **`kf-EasingTarget.md:69` (KF-ET-27, ADOPTED as the fourth claimant at the row below)**, with **`kf-EasingSidebar.md:61` (KF-ES-20)** as KF-ET-27's second provenance leg; **three per-record roster indices that are not census claims at all**: `kf-App.skeleton.md:91` (+`:21`/`:73`, the same record restating its own row) · `kf-ChromeDock.md:125` · `kf-ControlsPaneWrapper.md:122`; and **two cross-references to another record's roster index**: `kf-RibbonBar.md:7` (an instrument list) · `:72` (*"CPW S-10 (the `defer` correctness)"*). 3+3+4+1+1+3+1+1+2 = **19**. **LAW B form, replacing the struck completeness sentence**: this row asserts no closure in its own voice — the claimant set's carriage is measured at `docs/tranches/X/keyframes/conformance/PASS-4/KF-W0-CHECK.md` §1 (2026-08-28), which found the round-3 roster **short by `KF-ET-27` (E-2) and by its second leg `KF-ES-20` (E-3)** and the receipt itself unreproducible (D-3); both are booked this round, and a later pass re-points this citation rather than re-asserting the claim. The id-overload that manufactured the phantom is named at the heading, and the **substring** overload is named here, so the next seat re-raises neither |

**Why the roster, not the number.** Three records independently claimed "S-10" and one of them arrived there by a seat renumbering off a *local* corpus. A mint over four-plus-one would have re-collided on its first motion; a mint over the roster **as re-derived at repair round 3** cannot, because every claimant — including the two that receive no slot and the one struck as a duplicate — is visible to the one motion that assigns. **Roster shape after round 4 (D-3): ten S-9 claimants** (eight minted · one dead-and-enumerated · one refused-and-enumerated) **and FOUR S-10 claimants** (`+KF-ET-27`, adopted this round with `KF-ES-20` as its second provenance leg) — **fourteen LIVE rows, and a fifteenth retained as an enumerated strike**. The shape is **counted at the current bytes by table-row scan, never carried forward**: round 3 stood at ten + three = thirteen live (`+KAD-12` adopted, `−` the row-13 duplicate struck); round 4 adds one S-10 claimant and no S-9. Its carriage is measured at **`PASS-4/KF-W0-CHECK.md §1`** (2026-08-28) — which is the artifact that found this roster short by that claimant — and this table states what that artifact found, including its defects, rather than asserting closure in its own voice.

### Fold sharpenings (verified at this authoring, 2026-08-28 — booked, not re-booked)

| # | Finding | Consequence |
|---|---|---|
| **N-1** | The reconciliation surface has two denominators. `git status --short \| wc -l` → **252** is the *disk↔HEAD* count; `git diff --name-only origin/master \| wc -l` → **325** is the *disk↔frontier* count (124 of the rows untracked: `git ls-files --others --exclude-standard \| wc -l` → **124**). A record quoting "~252 dirty rows" for the §B-12 act's scope is quoting the wrong denominator — the act moves the tree to the frontier, so the frontier diff is the surface. | G-0.1's GREEN condition disposes the full surface with **both figures stated with their commands** — **and with BOTH ENUMERATIONS, because the two surfaces do not nest (D-2, repair round 4)**: a disposition record covering only the 252 status rows leaves **225** frontier-diff files unexamined (`comm -13`, pasted at G-0.1's falsifier), while **152** status rows — 124 of them untracked — lie outside the frontier diff entirely and the overlap is **100**. The struck `325 − 252 = 73` was arithmetic over non-nested sets and understated the exposure threefold. |
| **N-2** | The `backward` module's banked "diverges from BOTH refs" figures ⟨kf-KeyframesStringControls: 399/143 · 154/114⟩ **do not reproduce**. The module was **MOVED upstream**: HEAD/disk carry `src/animation/compile/emit/{backward,backward-walk,backward-color}.ts`; `origin/master` carries `emit/backward/{backward,walk,color,index}.ts`. Fold-measured: disk `backward.ts` **393** L ≡ frontier `backward/backward.ts` **393** L; HEAD **410**; the diff of the two 393-line files is **import-path-depth hunks only**. **The disk is the frontier's content at HEAD's layout** — content-forward, path-stale. | Corrects a banked figure by **appended** note (E-3). Names a real reconciliation hazard: a partial `git checkout origin/master -- src/` would leave the flat paths as **orphaned duplicates**; only the full `reset --hard` removes them. And it recharacterizes the 87 dirty src rows as largely a **landed-upstream refactor**, not novel local work — strengthening C-1's FAM-01 RAIL rider and further weakening the axes' "nobody noticed" story. |
| **N-3** | EE-03's frontier cure located exactly: `origin/master:demo/components/instrument/keyframes/composables/useKeyframesParsing.ts:97` = `() => animation.templateFrames.length,`. **The cure line is absent at HEAD** — but a naive `git grep 'templateFrames.length' HEAD -- demo/` is NOT a valid witness: it returns **2 unrelated guard sites** (`KeyframesEditor.vue:204`, `useKeyframeOps.ts:179`). The oracle is the cure line at its file:line, or the watch-warn count — not the bare grep. | G-0.4's EE-03 leg gets a file:line witness with the false-positive named, so no later seat "greens" it on the wrong grep. |
| **N-4** | KF-TD-2's inversion re-verified at fold: `git diff --stat origin/master -- demo/components/instrument/shell/TypingDots.vue` → **empty**; `git diff --stat HEAD -- <same>` → **1 file, 4 insertions(+), 9 deletions(−)**. | C-21's per-file law is not an inference from one record; it is a reproducible measurement, and G-0.9's falsifier names it. |

## Gates

All ten **RED at authoring**. Every baseline below was re-measured read-only by the fold seat on **2026-08-28** in `/Users/mkbabb/Programming/keyframes.js` (and `/Users/mkbabb/Programming/glass-ui` where named); commands and outputs are pasted, none inferred. Each gate states how it fails **for its intended reason** (L-19). No proof script is authored for any gate — every one is a `git`/`grep`/`node -p` one-liner or a named live witness.

**G-0.1 — SUBSTRATE SETTLED.** The sacred checkout reconciled by the owner's hand.
*RED baseline* (fold-measured 2026-08-28, reproducing the CARRY seat's 2026-08-25 run exactly): `git rev-list --count HEAD..origin/master` → **41** · `git rev-list --count origin/master..HEAD` → **1** · `git status --short | wc -l` → **252** · `git diff --name-only origin/master | wc -l` → **325** · `git ls-files --others --exclude-standard | wc -l` → **124** · `git merge-base HEAD origin/master` → `a59d3a22da080a8ed224e8d675112bb3bb0135b0`; HEAD `8281638c…`, origin/master `81a56990…`; neither is an ancestor of the other.
*GREEN*: 0 behind; the 1-ahead commit and **every row of the reconciliation surface** explicitly dispositioned (kept / committed / discarded) in a written record that states **both denominators with their commands** (252 status-vs-HEAD · 325 diff-vs-frontier, N-1) **and disposes BOTH ENUMERATIONS, never one plus a difference (D-2, repair round 4)** — the two surfaces are not nested, so the record covers the **152** status rows outside the frontier diff (124 of them untracked) **and** the **225** frontier-diff files outside the status set, with `comm -13` / `comm -23` pasted beside the figures at the falsifier below; the reconciliation output **diffed, not assumed** (C-15 — `position="right"` must not be carried forward under a different spelling), **and the flat `emit/{backward,backward-walk,backward-color}.ts` triad's REMOVAL stated by path in the record** (N-2 + the LAW-A census 1 at repair round 3: **no frontier import specifier traverses the flat spelling**, so a partial `git checkout origin/master -- src/` would leave three files with **zero importers** — orphaned duplicates that no build error surfaces and no `tsc` run reds; only the full `reset --hard` discharges the act, and the record is where that is checkable).
*Falsifier*: **§B-12 is the OWNER'S act — a wave that performs it itself fails this gate**, green counts notwithstanding ⟨lane-docs.md:380⟩. **A record that disposes only the 252 status rows leaves 225 frontier-diff files undispositioned** — measured, not subtracted (D-2, repair round 4; the struck figure was `325 − 252 = 73`, arithmetic over two sets that **do not nest**):
```
$ comm -13 <(git status --short | sed 's/^...//' | sort -u) \
           <(git diff --name-only origin/master | sort -u) | wc -l   → 225   ← frontier-diff files NOT in the status set
$ comm -23 <(git status --short | sed 's/^...//' | sort -u) \
           <(git diff --name-only origin/master | sort -u) | wc -l   → 152   ← status rows NOT in the frontier diff (124 of them untracked)
$ comm -12 … | wc -l                                                  → 100   ← the overlap (252 = 152 + 100 · 325 = 225 + 100)
```
**The two surfaces are NOT nested**, so their sizes cannot be subtracted: `git status --short` counts **124 untracked** paths `git diff --name-only origin/master` never walks, plus 28 further tracked rows that differ from HEAD but not from the frontier. **The GREEN therefore requires BOTH enumerations, never one plus a difference** — a record disposing "the 252 plus 73 more" stamps this gate with **125** frontier-diff files unreconciled. This is the wave's own recorded disease in its most load-bearing sentence: §Carry's preamble already binds *"the denominator is re-counted at the bytes, never carried forward by arithmetic"*, and round 1 was convicted for exactly this over a §Carry roster.
⟨carries C-2, C-15, N-1⟩

**G-0.2 — MANIFEST SINGLE-STATE.** One glass-ui declaration across all four coordinates.
*RED baseline*, four disagreeing states, fold-measured:
```
git show HEAD:package.json           → :71  "@mkbabb/glass-ui": "6.0.0" under optionalDependencies   (name @mkbabb/keyframes.js, version 5.3.5)
git show origin/master:package.json  → :77  "@mkbabb/glass-ui": "7.0.0" under devDependencies        (version 6.0.0; :70 "@mkbabb/value.js": "4.0.0")
grep -c glass-ui package.json        → 0        grep -c glass-ui package-lock.json → 0     (the worktree DELETES the row from both)
git show HEAD:package-lock.json | grep -c glass-ui           → 3
git show origin/master:package-lock.json | grep -c glass-ui  → 3
node -p "require('./node_modules/@mkbabb/glass-ui/package.json').version" → 7.0.0
cat .npmrc → legacy-peer-deps=true          ls node_modules/@mkbabb/ → glass-ui  parse-that  value.js   (no pencil-boil)
installed peers: {"@mkbabb/keyframes.js":"^6.0.0","@mkbabb/pencil-boil":"^0.9.2","@mkbabb/value.js":"^4.0.0","embla-carousel-vue":"^8.0", …}
```
*GREEN*: HEAD == origin/master == worktree == installed at `"@mkbabb/glass-ui": "7.0.0"` **exact devDep** + lock, **AND** a written ruling on `legacy-peer-deps=true` versus the two genuinely-absent peers (`pencil-boil`, `embla-carousel-vue`).
*Falsifier*: committing the worktree deletion as-is **revives census F-1** and this gate reads three states again. A settle that leaves `.npmrc` unruled passes a naive "one version" check and fails here — silence is not a disposition.
⟨carries C-1, C-16; KF-HA-6 discharged, not blocking⟩

**G-0.3 — EE-02 CSS-TWIN NOT REGRESSED.** The lagging disk files reconciled disk←master.
*RED baseline* (fold-measured): `git diff origin/master --stat -- <the four paths>` →
```
 .../channel-controls/TimingFunctionPanel.vue       |  9 +---
 .../composables/useTimingFunctionEditor.ts         | 19 ++------
 package-lock.json                                  | 50 ----------------------
 package.json                                       |  4 +-
 4 files changed, 6 insertions(+), 76 deletions(-)
```
The disk carries `{ fn }` where master carries `{ fn, css }` + `timingFunctionLiteralFor`.
*GREEN*: **empty diff** for the two `.ts`/`.vue` paths — the EE-02 twin present at BOTH write sites (`setAnimationTimingFunction` and `TimingFunctionPanel.vue:144`).
*Falsifier*: reconcile master←disk and the diff also goes empty while the landed cure is destroyed — which is why the gate names the **direction** and not just the emptiness. Both consequences (WAAPI demotion, export-serializer identity-miss) then reappear on a tree that reports clean.
⟨carries C-4⟩

**G-0.4 — RE-REGRESSION GUARD.** EE-01 · FE-3 · EE-03 re-run GREEN on the reconciled tree.
*RED baseline*, three oracles, each green at the frontier and **red at the audited disk today** — which is what makes them oracles (all fold-verified):
```
EE-01  git show HEAD:demo/components/CopyButton.vue         | sed -n 42p → timingFunction: "bounceInEase",
       git show origin/master:demo/components/CopyButton.vue | sed -n 42p → timingFunction: "easeInBounce",
       git status --short -- demo/components/CopyButton.vue → (empty: disk == HEAD == the dead name)
FE-3   demo/components/instrument/keyframes/components/KeyframeCardList.vue:11
         HEAD (== disk)  :frame-start="frames[i].start.toString()"
         origin/master   :frame-start="startScalar(frames[i].start)"     (commit add20b7e)
EE-03  origin/master:demo/components/instrument/keyframes/composables/useKeyframesParsing.ts:97 → () => animation.templateFrames.length,
       the cure line ABSENT at HEAD (N-3 — the bare `templateFrames.length` grep is NOT the witness: 2 unrelated guard sites hit)
```
*GREEN* — **witnesses re-founded this round (D-4). Every oracle now names an instrument that exists at a coordinate, or is declared as another tranche's gate and not asserted here.** The gate closes on the **static tier**, which is wholly addressable; the runtime tier is confirmation, named by literal path.

**Static tier (the gate's closing condition, three file:line assertions on the settled tree):**
```
EE-01  sed -n 42p demo/components/CopyButton.vue                                   → timingFunction: "easeInBounce",
FE-3   sed -n 11p demo/components/instrument/keyframes/components/KeyframeCardList.vue
                                                                                    → :frame-start="startScalar(frames[i].start)"
EE-03  sed -n 97p demo/components/instrument/keyframes/composables/useKeyframesParsing.ts
                                                                                    → () => animation.templateFrames.length,
```
plus the negative form for each, **each now naming its coordinate (D-6, repair round 3 — the scope word was the one thing missing from this leg)**: **`git grep -n 'bounceInEase' <the SETTLED WORKTREE at `.c`'s clock, dated> -- demo/` → 0** · **`git grep -n '\.start\.toString()' <the same settled worktree, same date> -- demo/` → 0**. **The coordinate is the settled worktree and NOT a ref**, deliberately and statedly: these two probes exist to prove the reconciled *tree* carries neither dead form, so running them at `origin/master` would measure the frontier and prove nothing about the rebase — the same *"a wave that runs these gates before the migration lands measures the frontier and proves nothing"* failure this gate's own falsifier names, in the other direction. `.c` **pastes the date and the `git rev-parse HEAD` of the settled tree beside each output**; that pair is the coordinate. This is what actually proves the rebase did not re-land the dead name, the `.toString()`, or the array watch. **These are RED today and green only after the settle** — the same three probes the RED baseline above pastes, re-run against the reconciled tree, so the oracle and its baseline are the same instrument read twice.

**Runtime tier (confirmation; re-verified at `origin/master` this round via `git ls-tree -r --name-only origin/master scripts/observe/` — cures D-7, whose finding is that a path existing is not an instrument wiring, C-13's own conviction turned on this file):**

- **`node scripts/observe/demo/live-session.mjs` (1612 L) is the warn-count instrument** — it is the file that loads the budget. Receipt: `git grep -n 'console-budget' origin/master -- scripts/observe/` → **`live-session.mjs:77`** (`import { chargeBudget, isNamedBenign } from "../../lib/console-budget.mjs"`) and **`live-session-mobile.mjs:89`**, **and no other**. It also carries the `pageerror` stream (6 `pageerror` bytes in that file). EE-01's pageerror confirmation on `/#/easing` and `/#/spring` **and** EE-03's watch-warn-count→0 therefore both run here; `live-session-mobile.mjs` is the mobile sibling on the same single budget authority.
- **`node scripts/observe/demo/smoke.mjs` (200 L) is a different and weaker oracle, and is named as one**: it does **not** load `console-budget.mjs` and installs **no** `pageerror` handler (`git grep -c 'pageerror' origin/master -- scripts/observe/demo/smoke.mjs` → 0). Its leg is *"zero console errors during mount"* — `page.on("console", …)` collecting `msg.type() === "error"` (`:131-132`), asserted at `:181-184` — plus the entry-chunk static-import assertions. It confirms mount-time console-error zero and nothing about warn counts.
- **The correction is stated rather than absorbed**: this file's round-1 sentence said the budget is *"the warn-count instrument **they** load"*, which is false of `smoke.mjs`; EE-03's confirmation has **one** instrument, not two. **No npm script wraps either**; they are invoked by path, and §Cadence's carve-out covers exactly these named oracles.

**FE-3's `/\[object Object\]/` smoke assertion is NOT an instrument in this tree and is not asserted here.** Receipt: `git ls-tree --name-only origin/master` has **no `e2e/`** (`.changeset .claude … bench demo docs … scripts src test`), and `git grep -n 'object Object' origin/master -- e2e/ test/` → **zero hits**. The assertion exists as a *planned born-RED gate of keyframes.js's own tranche V*, W1 · Render Truth (`origin/master:docs/tranches/V/V.md:73` — *"pageerror==0 + no `[object Object]`, 7 routes × 2 viewports (born RED — live defects)"*). It is therefore **declared as an outbound cross-edge to that tranche** (see §Sequencing) and never authored here: §Bounds forbids this wave writing `test/**`, and a substrate wave that invents a test file to green its own gate is the vacuous-evidence defect L-19 exists to kill.
*Falsifier*: a careless rebase of the in-flight migration re-lands any one of the three and the corresponding static probe reds; a wave that runs these gates **before** the migration lands measures the frontier and proves nothing. **A gate leg closed on an oracle that names no coordinate is the same failure one level up** — the reason this leg was re-founded. **FE-3's PARTIAL-cure residue is explicitly NOT this gate's** (fraction-for-percent, named-selector fallthrough, `selectorText` uncalled) — KFED-UNIT's, after KF.W4.
⟨carries C-5, C-6, C-7, N-3⟩

**G-0.5 — HEADER-RIBBON TRIPWIRE (negative gate — RED means unsafe to advance).**
*RED baseline*, fold-measured across both repositories:
```
git -C /Users/mkbabb/Programming/glass-ui ls-tree --name-only HEAD src/components/header-ribbon/   → (empty)
git -C … ls-tree --name-only '4bf53962^' src/components/header-ribbon/ → HeaderRibbon.vue  README.md  index.ts  styles.css  types.ts
grep -c header-ribbon /Users/mkbabb/Programming/glass-ui/package.json → 0
git show origin/master:demo/components/instrument/shell/EditorShell.vue | grep -n header-ribbon
     → 116:import { HeaderRibbon } from "@mkbabb/glass-ui/header-ribbon";
ls node_modules/@mkbabb/glass-ui/dist/header-ribbon.js → present (installed 7.0.0)
```
**CONSUMER SET, derived from the IMPORT GRAPH at `origin/master` (LAW A census 2, repair round 3 — the previous reading was a single-file grep standing in for a census):** specifier census `git grep -nF 'glass-ui/header-ribbon' origin/master -- .` → **one live import, `demo/components/instrument/shell/EditorShell.vue:116`** (two further hits are docs prose at `docs/tranches/H/audit/a-glass-ui-consumption.md:48` and `docs/tranches/V/audit/R1-15-cross-repo.md:54` — non-import context); symbol census `git grep -n '\bHeaderRibbon\b' origin/master -- demo/ test/ src/ scripts/` → `:16`, `:50`, `:116`, **all three the same file resolving through the same specifier**, no sibling-basename consumer. **The blast radius is exactly one consumer, tests included.**
*GREEN*: **either** the installed producer stays at-or-below `4bf53962` (i.e. 7.0.0, today's state), **or** `origin/master:EditorShell.vue:116` no longer imports the deleted subpath.
*Falsifier*: the gate fires the moment a settle advances glass-ui past `4bf53962` with `:116` intact — the build dies at `:116`. **A GREEN reached by re-deriving the consumer set from anything other than the import graph is RED by LAW A**: the count is one *because the graph says one*, not because one file was grepped. **This gate BOUNDS G-0.2's cure rather than following it**: a "declare the newest glass-ui and regenerate the lock" remediation passes G-0.2 and destroys the build.
⟨carries C-3⟩

**G-0.6 — COUNTS RE-RUN.** SCH-1..7 + X-2 each dated with its command and literal output.
*RED baseline*, census claims vs the fold seat's re-measurement at `origin/master`:
```
SCH-3  claim 145 src .ts / 22 636 LOC
       git ls-tree -r --name-only origin/master src/  | grep -c '\.ts$'      → 153
SCH-4  claim 61 value.js `from`-lines (/value 15)  vs 62 (16)                 → MEASURE-AT-OPEN
SCH-5  4+ Tier-A anchors DEAD (value-ast.ts:71 → compile/value/compile.ts:32; easing-registry.ts →
       compile/easing/registry.ts; emit/backward/backward.ts; emit/easing-serialize.ts:71-73)      → re-anchor, see N-2
SCH-6  git ls-tree -r --name-only origin/master demo/ | grep -c '\.vue$'      → 58   (holds; contents drifted)
SCH-7  depcruise L-1/L-2 unverified at HEAD                                   → restated, not resolved
X-2    claim 184 demo .ts/.vue (STALE-WORKTREE figure)
       git ls-tree -r --name-only origin/master demo/ | grep -cE '\.(ts|vue)$' → 185
```
*GREEN*: every SCH row and X-2 carries a dated, re-derived figure with its command pasted; the cascade-coupled column exists beside the import census; **`lane-frontend.md` §6.5's PRM roster (13 sites / 12 files) and its counts-table tally row** and the z-index cells are re-scoped **in one motion** — **at the roster's real home, which is NOT `CENSUS-2026-08-03.md`** (D-4; CENSUS carries one pointer cell, *"delegation sites unverified (FE §6.5)"*, corrected by appended note in the same motion); **every claimant on the C-17.R roster is dispositioned in ONE motion** — minted under a fresh non-colliding id citing its originating record, or enumerated as a dead/refused occupant with its reason (the S-block lives in `lane-frontend.md`, `^### S-` = 8 today).
*Second falsifier (R-5)*: **a mint run over four-plus-one rather than the enumerated FOURTEEN live rows of C-17.R re-collides on its first motion** — and a mint run over any earlier roster collides too: the round-2 thirteen was **missing `KAD-12`** (the sole carry's own claimant), **mis-keyed** on the `/number-field` claim, **coined** at `F1(c)` and **anonymous** at row 13 (all four cured at repair round 3, D-2), and **the round-3 thirteen was short by `KF-ET-27`** — an *adopted* S-10 census arm named by **two** corpus records and routed to KF.W5 — **cured at repair round 4 (D-3)**. Carriage per **`PASS-4/KF-W0-CHECK.md §1`** (2026-08-28), the artifact that found that shortfall; the count and its counting rule live at C-17.R and this cell re-issues no numeral beyond the roster's shape. **A mint whose roster is short by a claimant the corpus OR the wave's own carry enumerates is the defect, not an approximation of the cure** — the carry side was closed at round 3 and the corpus side at round 4, and the falsifier now names both sources, because a falsifier scoped to one of them stays green over the other — `kf-KeyboardShortcutsModal:29`'s renumber already landed a second "S-10" on top of `kf-KeyframesEditor:63`'s and `kf-SquareInstrument:78`'s, so a short roster is not an incomplete cure but the exact mechanism of the defect. A minted slot that does not cite its originating record fails too: the citation is what makes the next collision detectable.
*Falsifier*: a figure restated without its command is indistinguishable from the stale one it replaces — **X-2's 184/185 is the proof**: the stale figure and Codex's agreed *exactly*, which is how both were caught. **Explicitly NOT green-by-basis-reconciliation**: X-4 (80 / 139 / 153 / "159") is **KF.W5's** row.
⟨carries C-8, C-9, C-10, C-11, C-17⟩

**G-0.7 — GATE-INVENTORY TRUTH.** No prose cites a gate that does not exist.
*RED baseline*, fold-measured at `origin/master`:
```
git show origin/master:package.json → :50 "proof:structure":    node scripts/gates/structure/index.mjs
                                      :51 "proof:publish":      node scripts/gates/surface/index.mjs
                                      :52 "proof:owner-golden": node scripts/gates/visual/index.mjs
                                      :37 "check": tsc --noEmit && tsc --noEmit -p tsconfig.test.json && npm run proof:structure   ← NO vue-tsc
                                      :44 "lint":  depcruise src
git ls-tree --name-only origin/master scripts/gates/ → structure  surface  visual        (three DIRECTORIES)
git ls-tree -r --name-only origin/master scripts/gates/ → 9 FILES (re-measured this round, R-17/R-8 item 5):
     structure/index.mjs · surface/{index,agent-surface,boundary,consume-bundle,published-surface,readme-runs,verify-diff}.mjs · visual/index.mjs
```
**Every line of that baseline was RE-EXECUTED at `81a56990` by the repair-round-2 seat** (R2-5: a baseline a seat leaves standing without re-verifying is that seat's defect at the next pass) — the three entry points, the `:37` `check` with **no `vue-tsc`**, the `:44` bare `depcruise src`, and the **9** files under `scripts/gates/` all reproduce byte-exactly.

**The roster's denominator is 3 npm `proof:*` entry points over 9 gate files** — the "surface, visual" two-script figure is a stale-worktree reading, decided against by `kf-SequenceScrubber.md:119` (*"`proof:structure` present in origin/master's `check`, deleted in the worktree edit"*) and corroborated by kf-TypingDots K8. **The frontier roster is the denominator** (R-17); KF.W4's OP-3 is RESOLVED on it, and this wave owns the roster it resolves to.

**THE DEAD-NAME SURFACE, RE-DERIVED AT THE TRUE ALTITUDE (R2-17, cures D-2 — the third consecutive round in which a hand-list stood in for the universal this gate's own title asserts).** The four names below are what the bank and the fold happened to name; they are **four members of a measured set**, and the set is the denominator from this round forward:

```
git grep -ho 'proof:[a-z0-9-]*' origin/master -- demo/ | grep -v -- '-$' | sort -u   → 54 distinct names (+ one bare `proof:` token)
   of which runnable as an npm script (package.json :50/:51/:52)                     → 1   (proof:publish)
                                                                                     → 53 DEAD names
git grep -c 'proof:' origin/master -- demo/                                          → 116 prose hits across 51 files
```

A sweep executed to the old GREEN would have left **~47 dead names and ~90 citations standing** — this gate's own falsifier, reproduced by its own roster, one altitude up. Names recur across subtrees (`DESIGN.md` 8 distinct · `demo/styles` 7 · `demo/components` 21 · `demo/scenes` 14 · `demo/app` 8; the per-directory sums exceed the union of 54, which is exactly why the roster is **name-keyed with a per-site disposition** and not directory-keyed).

The four banked-and-fold names, with their sites re-measured at `origin/master` this round, are retained as the fold's own receipts (they are the sweep's worked examples, no longer its denominator):
`proof:brittleness` — `demo/styles/style.css:38` (dissolution `70b32501`) · `demo/styles/layout.css:152` · `scripts/lib/demo-driver.mjs:69` (a comment mirror). **Disposition: cure → KF.W6, family whole (R2-13)** — the banked routing wins; KF.W4 carves these citations out of its own gate BY NAME at its §Excluded residue table, and W0's roster records the name, the three sites and the owner. A further **68 files under keyframes `docs/**` carry the name and are OUT OF SCOPE by bounds** (see the scope declaration below), declared here so the family is not silently re-scoped by whoever runs the sweep.
`proof:font-census` — `demo/DESIGN.md:29` (§24-30, the Mono-as-data law's named witness) · `DESIGN.md:250` · `EditorStartScreen.vue:180` · `font-roles.json:2`/`:62`.
`proof:styling-idioms` — `DESIGN.md:250` · **`DESIGN.md:237`** (the R4 codex clause) · **`components/instrument/shell/EditorStartScreen.vue:159`** · **`scenes/sequence/SequenceTarget.css:212`**.
`proof:demo-no-oversize` — **`demo/`-SCOPED at 16 hits across 13 files** (re-measured: `git grep -c 'proof:demo-no-oversize' origin/master -- demo/` → 13 files / 16 hits, reproducing kf-DemoGlobalChrome **L-6**'s banked figure exactly). **The scope word is load-bearing (cures D-6)**: repo-wide the same name reads **121 hits across 70 files, 57 of those files under `docs/`** — and keyframes `docs/**` is outside **every** X·KF wave's §Bounds, so the record's lock (*"A fix confined to this file leaves twelve files standing; the fold owns the sweep"*) is discharged **at demo scope** and the `docs/**` surface is declared **loudly out-of-scope**, never silently inherited. Without the scope word the record's own lock reproduces one directory up, unanswered.
`KF-EST-1`'s eight sites — **the THIRD R-8 residue family, entered at repair round 4 (D-6)**. Banked ⟨`kf-EditorStartScreen.md:58`, quoted by command — `sed -n '58p'`⟩: *"Eight `proof:*` citations across two files name gates deleted by `70b32501`/`92746148` as **binding authority** — :15 styled as an OWNER lock, :56, :134, :159, :180 + TypingDots :45, :51, :59 — the sweep missed both files (their last touch predates it)"*, routed at the bank to **KF.W4**'s provenance-gate law, whose `proof:*` residue R-8 routes here. **All eight re-measured at `origin/master` this round** (`git grep -n 'proof:' origin/master -- 'demo/components/instrument/shell/EditorStartScreen.vue' 'demo/components/instrument/shell/TypingDots.vue'`), each with the name it cites: `EditorStartScreen.vue:15` `proof:hero-two-focal` (**styled as an OWNER lock**) · `:56` `proof:design-refinement` · `:134` `proof:hero-deck-voice` · `:159` `proof:styling-idioms` · `:180` `proof:font-census` · **`TypingDots.vue:45`/`:51`/`:59` `proof:typing-dots` ×3**. **Six distinct dead names over eight sites**, every one a member of the 53 and disposed there; the roster's worked examples already cited **two** of the eight (`:180` under `proof:font-census`, `:159` under `proof:styling-idioms`) **without naming the family, its id, or its second file** — disposing the sites while losing the family, which is the accounting falsifier (b) turns on. `TypingDots.vue`'s three sites enter the roster **by name** here. **Fold note, carried not re-booked**: the same file is C-21's substrate-INVERSION exemplar (worktree ≡ `origin/master`, HEAD the outlier), so its three sites are stamped against the frontier and any HEAD-based reading of them is stale by construction.
**None of the four exists as a runnable gate.** **THREE residue families route INBOUND from KF.W4 under R-8 and are received here** (C-12; the third added at repair round 4, D-6): **kf-DemoGlobalChrome L-6** (above), **kf-EditorStartScreen KF-EST-1** (above) and **kf-AnimationControlsGroup D-13** — *"seven `proof:*` gate citations resolve to zero runnable gates"* — whose **seven are ENUMERATED here for the first time (R2-17.2, cures D-5: a GREEN keyed to an unenumerated set is undecidable, on the one gate whose entire content is enumeration)**. Re-derived at `origin/master` this round over the record's subject surface (`git grep -ho 'proof:[a-z0-9-]*' origin/master -- 'demo/components/instrument/transport/AnimationControlsGroup*'`): **8 distinct names over 16 citations** — `proof:demo-no-oversize` ×6 · `proof:stage-within-docks` ×2 · `proof:no-shadow-playback-authority` ×2 · `proof:mobile-single-page` ×2 · `proof:stage-not-clipped` ×1 · `proof:panel-naked-rail` ×1 · `proof:dock-zorder` ×1 · `proof:live-session-mobile` ×1. The eighth is the record's own carve-out — *"one cited name matches a DEMO_ROSTER observation, prefix-less"*: `live-session-mobile` is row 6 of `scripts/demo-roster.mjs`'s `DEMO_ROSTER` (`{ name: "live-session-mobile", script: "scripts/observe/demo/live-session-mobile.mjs" }`, both present at the frontier). **8 − 1 = the banked seven**, reproduced by measurement rather than inherited; the count needs no bank correction, and each of the seven is a member of the 53 and receives its disposition there. (The row's *"two `proof:` scripts exist"* remains the stale-worktree reading this file corrects to three at the RED baseline above.)
*GREEN* — **two clauses, both satisfiable by writing `docs/**` only (R2-17.1, cures D-1)**:
**(i) THE ROSTER**: one authoritative dated roster artifact in the tree (`GATE-ROSTER.md`, already unit KF.W0.e's) stating the **3 npm entry points and the 9 gate files by path**, and enumerating the **53 dead names** as measured at `81a56990` by the command block above, **re-run at authoring**. **LAW B (repair round 3)**: the command block above **is** the measurement and rides beside its sentence, which is the lawful form; the separate claim that this gate's roster is *carried completely by this spec* belongs to the conformance artifact, not to this gate's voice — `PASS-3/KF-W0-CHECK.md` §3 axis (4) (2026-08-28) re-executed every figure in this gate's baseline and found them reproducing byte-exactly, with the roster's **only** surviving local defect being D-6's missing scope word at G-0.4 (R2-5 — a roster copied from this spec's prose is the defect it exists to kill), each name carrying its site list and a per-name disposition.
**(ii) EVERY NAME DISPOSED, AND W0 STRIKES NOTHING**: each of the 53 is either **struck-in-owner-wave** (the wave whose §Bounds carries the file performs the strike, on its own clock) or **routed-with-a-named-owner** — sites in KF.W6-bounded style files route to **KF.W6** (incl. `proof:brittleness`, R2-13); the R-8 residue families ride their packets per R-8.2's routing table; the `proof:styling-idioms` / `proof:font-census`-class prose sites route to the wave or packet whose §Bounds carries the file; and any name with **no** owner gets a **loud NO-WAVE-OWNER roster line — routed, never dropped**. **This wave executes NO strike**: every dead-name site is under `keyframes.js/demo/**` or `docs/**`, surfaces §Bounds forbids and the Triumvirate Dispatch calls wave-invalidating, so a GREEN demanding the strike here was a GREEN no lawful execution could reach. Roster + route is the whole of W0's act; the strikes are other waves' bytes.
*Falsifier*: **this gate must precede KF.W4** — a gate wave cannot be authored against an inventory that includes gates which do not exist. **The gate now fails three ways.** (a) A roster keyed to any hand-list short of the measured 53 — two names, four names, ten sites — is this gate's own defect reproduced by its own roster, and short rosters are its recorded history (7 sites → 10 sites → 53 names); the falsifier is the command block, re-run: a roster whose name count is less than the sweep's is RED regardless of how many citations it disposes. (b) **A disposition that is neither a strike-in-owner-wave nor a route-with-a-named-owner** — "noted", "carried", "hygiene" — fails: silence is not a disposition, and an unenumerated dependent set (D-13's seven before this round) is undecidable, not green. (c) **W0 striking a demo byte fails the wave outright**, Dispatch-fatal, green count notwithstanding — the gate is green with `docs/**` writes alone or it is not green. The **demo-typechecking arm is NOT this gate's**; it routes to KF.W4 against the single banked no-SFC-typecheck identity.
⟨carries C-12⟩

**G-0.8 — CODEX v8 PIN DISPOSITIONED.**
*RED baseline*: X-1 — the whole v8 lineage is pinned to `8281638c`; every denominator (414 contracts / 152 surfaces / 73 568 Safari cells / **184** demo members) is measured there, and 184's exact Codex/census agreement is the proof both censused the same stale worktree. **No disposition exists today.**
*GREEN*: a written ruling — the v8 contract **re-anchored at `81a56990`**, **or** declared **stale-by-substrate and never citable for a live count** — plus **185** adopted as the frontier demo count (fold re-measured), and the seal cited only as `345 exact / 12 partial / 57 unresolved @ 8281638c`. **OG-KF1 named here; ruled at KF.W10.**
*Falsifier*: a disposition of "carried" or "pending" is not a ruling — the gate reads the verdict token. A ruling that re-anchors the contract without re-measuring its denominators passes a naive check and leaves 414/152/73 568 citable against a tree they were never measured on.
⟨carries C-18 + the conditional TCC re-reads B10-9 · B19-10 (pin half) · B19-11 · B20-6 · B21-7⟩

**G-0.9 — REGISTRY RE-ANCHOR.** Every adjudicated record states WHICH ref it cures against.
*RED baseline*: `ls registry/adjudicated/kf-*.md | wc -l` → **58** (fold-verified), **0** of which carry a ref-of-record stamp. At least two name mutually contradictory substrates for one file (C-20). `git status --short demo/utils/` → `?? demo/utils/formatEditorCSS.ts` and `?? demo/utils/keyframeSelector.ts` — **UNTRACKED, absent from HEAD's tree, yet cited by line** in landed records. The `backward` module's banked divergence figures do not reproduce (N-2, corrected by appended note). `TypingDots.vue` inverts the substrate direction (N-4). **Four inbound appended-correction obligations are RED for the same reason — declared elsewhere, absent here until this round** (C-22 / R-19b; `grep -c` in this file before repair → `App.vue:176` 0 · `TABS_EXTERNALLY` 0 · `dock-margin` 0 · `KF-SKEL` 0), plus the kf-AnimationVisualizer census-cell correction carried at C-9 **and the kf-KeyframeTimeline header-equation correction booked at C-1.R this round** (`grep -c 'KeyframeTimeline'` before repair → 1, and that hit is C-10's PRM-roster cell, never a correction obligation) — **six inbound corrections, all RED, none of them stampable by a per-repo pass**. **Seventh RED condition, measured at repair round 4 (D-1)**: the corpus routes its re-anchor obligation on the banked id **`D-19`** — `grep -l 'D-19' kf-*.md | wc -l` → **42 records** (bare token; **34** in the re-anchor sense by C-1.R row 6's stated predicate) — and `grep -c 'D-19' KF-W0.md` **before this round → 0**, so the stamp this gate demands had **no key** in the wave that owns the re-anchor; the key is now C-1.R row 6.
*GREEN*: every record carries a stamped ref-of-record; every contradicting cell reconciled by **APPENDED** correction (**E-3 — dated evidence is never rewritten**); **C-19's ledger resolved at its corrected denominator — each of the 30 ledger members to a named disposition, with the 2 anti-firings enumerated separately and NOT swept as firings** (D-3, repair round 3: a sweep keyed to 32 resolves two records to the *opposite* disposition and cannot close); the pass demonstrably ran **per-file** (C-21's three corrections landed, KF-AT-28's obligation discharged for that file), **with the corpus's one MEASURED per-file offset carried as the exemplar row — kf-AmigaScene `:155`: *"the C-1 discharge + the corpus-wide re-anchor clause (this component: **comment-only drift, useAmigaDemo −2 below `:73`**)"*** (E-4, repair round 3 — a GREEN demanding a demonstrably per-file pass, on a wave whose C-21 proves the direction is per-file, was carrying **zero** of the per-file offsets the corpus hands it; one is now named and any further offsets the `.e` pass measures join it in `REF-OF-RECORD.md`); **and the six named appended corrections have landed by id**: (i) `TABS_EXTERNALLY_MANAGED_KEY` → **`demo/app/App.vue:176`** (path-qualified at repair round 4, R4-10 SUBJECT-IDENTITY sweep — a bare `App.vue:176` resolves to no path at the frontier), not the banked stale-HEAD `:169`; (ii) `--dock-margin` = 9 live unfallbacked sites + 1 prose mention; (iii) **KF-SKEL-7**'s cascade-level coupling arm recorded beside the import census; (iv) `kf-AnimationVisualizer.md:9`'s census cell corrected to worktree-scope (kf-SequenceScrubber K-1); (v) the C-17.R E-3 addendum demoting the kf-KeyboardShortcutsModal renumber to a claim-input; (vi) **kf-TimelineCaret MISS-α5 ≡ RR-β K-BASE's attached correction** — the banked `kf-KeyframeTimeline` header's *"working tree = the audited tree"* equation, false at every coordinate this wave measures (C-1.R). **(vii) THE STAMP IS KEYED TO `D-19` (D-1, repair round 4)**: the `.e` pass resolves each record's re-anchor obligation **by its banked id**, against **C-1.R row 6** — for the **34 records / 39 lines** whose `D-19` reads in the re-anchor sense (predicate stated at the row; bare-token census **42 records / 94 lines**) the stamp discharges that record's own routing word, and for the **eight** records whose `D-19` is a component-local id the row's record-qualification rule keeps the sweep from booking a dead-token cluster as a re-anchor obligation. **Two records this file names nowhere else — `kf-ChannelControls`, `kf-TimelineTrack` — reach this GREEN only through that key.** Binding form ⟨kf-TimelineCaret⟩: *"any spec consuming this registry states WHICH ref it cures against."*
*Falsifier*: **if two adjudicated records still disagree about one file's bytes, the wave has not re-anchored** — C-20 is the named test case. A blanket per-repo re-anchor passes a naive stamp count and **corrupts three banked rows** (C-21) — the falsifier is `git diff --stat origin/master -- …/TypingDots.vue` returning empty while the stamp says "stale HEAD".
⟨carries C-2's law, C-19, C-20, C-21, N-2, N-4⟩

**G-0.10 — SOURCE-OVER-DIST PROVENANCE.** glass-ui citations re-derived against the consumed artifact.
*RED baseline*: ~1/3 of challenge-D's `glass-ui/src/**` file:line provenance does not resolve in the consumed 7.0.0 dist (cut 2026-07-16; `--ink-perimeter` landed 2026-08-04); K-9 records the *convicting* re-reader committing the identical error; EH-1 is the third instance (archived letter vs `dist/header-ribbon.js`). Companion, fold-verified: `dist/gh-pages/assets/index-CL_QYCiO.css` mtime **Jul 16 09:11** — possibly pre-glass-7 — so **every byte-offset receipt in the corpus is provisional**.
*GREEN*: every surviving `glass-ui/src/**` citation re-derived against `node_modules/@mkbabb/glass-ui/dist/**` **or struck**; XR-4 / IN-GLASS-1 / `DISPOSITIONS.md:21` re-read against **BOTH repo-qualified letters and the shipped artifact** — **(a)** value.js `docs/tranches/V/archive/GLASS-INBOUND-2026-07-16-headerribbon-persistent-only.md:15-19`, **(b)** keyframes.js `origin/master:docs/tranches/V/coordination/GLASS-INBOUND-2026-07-16-headerribbon-consumer-updates.md`, **+ `dist/header-ribbon.js`**. **The repo qualification is part of the GREEN, not decoration (D-1, repair round 3): this wave's substrate is keyframes.js, where only (b) exists, so a seat handed the bare filename searches the wrong tree — the "NONEXISTENT path" claim this gate previously closed on is STRUCK at C-14 and the two letters are named there with what each says.** A receipt naming one letter is RED; **one fresh `npm run gh-pages`** so the byte-offset receipts stop riding a Jul-16 artifact. **(R-11, execution site 3 of 7 — the real script is `origin/master:package.json:43` `"gh-pages": "vite build --mode gh-pages"`; `git grep -n 'build:gh-pages' origin/master` → no hits, re-verified at this repair seat 2026-08-28. The banked caveat's words at C-13 are NOT rewritten; only the act is corrected.)**
*Falsifier*: matching **version strings** on both sides passes the check that produced this defect in the first place — the gate's content is that a version string is not an artifact. A Glass-7 consume slice scoped from the letter rather than the tree is the second falsifier: under the letter the target file is irreplaceable, under the tree it is deletable.
⟨carries C-13, C-14⟩

## Agent Units

Seats are **Opus** (M-23 §2) — this wave is enumeration, measurement, ruling, and appended correction; there is no design content.

**OP-1 — the reset (OWNER'S HAND, not a seat).** `git fetch && git reset --hard origin/master`, untracked V docs unharmed ⟨lane-docs.md:380, row 16⟩. Runs alone and first. *Sub-gate*: G-0.1.

**KF.W0.b — The Manifest Ruling.** Land the four-coordinate single state; rule the deletion and `legacy-peer-deps`; write the three relay packets into `INBOX.md`. *Files*: `MANIFEST-RULING.md`, `INBOX.md`; owner-hand `package.json`/`package-lock.json`. *Sub-gates*: G-0.2, G-0.5.

**KF.W0.c — Reconciliation Record and the Rebase Oracle.** Disposition the full reconciliation surface (both denominators); verify (not author) the two EE-02 sites; re-run the three oracles on the reconciled tree; diff the reconciliation output rather than assume it. *Files*: `SUBSTRATE-SETTLE-*.md`. *Sub-gates*: G-0.3, G-0.4.

**KF.W0.d — Counts, Columns, and Fresh Shadow Ids.** Re-run SCH-1..7 + X-2 with pasted outputs; add the cascade-coupled column; re-scope **`lane-frontend.md` §6.5's PRM roster (13 sites / 12 files) + its tally row** and the z cells in one motion — **not a CENSUS section; CENSUS gets the pointer-cell correction only** (D-4); mint the shadow ids in `lane-frontend.md`. *Files*: `COUNTS-*.md`, `CENSUS-2026-08-03.md`, `lane-frontend.md`, `lane-library.md`. *Sub-gate*: G-0.6.

**KF.W0.e — Ref-of-Record, Gate Roster, Provenance, v8.** Stamp all 58 records and reconcile contradictions by appended correction; book the gate roster (3 entry points / 9 frontier gate files) and **ROSTER + ROUTE the dead-gate prose — never strike it** (R2-17): enumerate the **53 dead names** re-derived at `81a56990` at authoring, each with its sites and a per-name disposition (struck-in-owner-wave, or routed-with-a-named-owner — `proof:brittleness` → KF.W6 per R2-13; a name with no owner gets a loud NO-WAVE-OWNER line), with the three R-8 residue families among them **by name and by family** — D-13's seven · L-6's 16×13 · **KF-EST-1's eight sites, `TypingDots.vue:45`/`:51`/`:59` included** (D-6, repair round 4). **This unit writes `docs/**` only and performs no strike under `demo/**`** — a strike here invalidates the wave. Re-derive or strike every `glass-ui/src/**` citation and run one **`npm run gh-pages`** (R-11, site 4 of 7); disposition the v8 pin and name OG-KF1. *Files*: `GATE-ROSTER.md`, `REF-OF-RECORD.md`, `V8-DISPOSITION.md`, the 58 records, `INTAKE-ADJUDICATION-2026-08-03.md`, `lane-docs.md`. *Sub-gates*: G-0.7, G-0.8, G-0.9, G-0.10.

## Sequencing

**Within the wave.** OP-1 → (.b ∥ .c) → (.d ∥ .e). Hard orders: C-9's cascade column **before** F-1's remediation scopes its blast radius; C-17's id minting **inside** the census re-anchor; G-0.5 **bounds** G-0.2 (negative gate); G-0.4's oracles run **after** the migration lands, never before.

**Cross-edges declared from this end.**

| To | Direction | Edge |
|---|---|---|
| keyframes.js OWNER (§B-12) | **INBOUND — OP-1** | The reset is the owner's hand; value.js must NOT perform it ⟨lane-docs.md:380⟩. Every row here is downstream of it. A wave that performs the reset itself, or opens without the begin-word, re-runs the schism it exists to close. |
| glass-ui producer (SS-6 / standing BH relay) | **OUTBOUND HOLD + FYI** | **HOLD**: installed glass-ui may NOT advance past `4bf53962` while `origin/master:EditorShell.vue:116` imports `/header-ribbon` — declared from this end so the producer's next bump does not silently break the consumer's build. **FYI packet** (producer rows NEVER become demo-side hacks): C-16's unmet peers (closed falsifier); C-14's rider — *producer docs/letters must be re-derived against shipped bytes* (third instance of the kf-ChromeDock doc-drift ledger pattern), incl. `docs/consumer-evidence/header-ribbon.md:3` still claiming "RETAINED persistent-only" against producer HEAD — **and the rider is now delivered with BOTH repo-qualified letters named, never the bare filename (D-1, repair round 3): (a) value.js `docs/tranches/V/archive/GLASS-INBOUND-2026-07-16-headerribbon-persistent-only.md`, (b) keyframes.js `origin/master:docs/tranches/V/coordination/GLASS-INBOUND-2026-07-16-headerribbon-consumer-updates.md`. The producer sent one letter into each consumer tree on the same day; a relay that names one of the two hands the recipient the wrong tree**; C-3's producer half now **MOOT** (answered by deletion — the relay downgrades to FYI + the consumer-evidence correction); kf-CSSPasteDialog's DialogFooter `sm:`-only gap (cured in glass-ui source HEAD but **UNRELEASED** — relay live, discharging when keyframes.js consumes the release). |
| **KF.W1 · Mail Cure** | OUTBOUND | The O-8/O-11 obligation packets are untracked in the sacred stale checkout — invisible to any executor, unanswered 10–16 days, measured against the stale tree (SCH-4/5). W1's re-delivery depends on this wave making them visible and on the re-anchored counts replacing the stale ones. |
| **KF.W4 · K-Quartet Hygiene** | OUTBOUND — **may not open before this re-anchor** | (a) C-12's demo-typechecking arm folds to the single banked no-SFC-typecheck identity (KF-APP-4 ≡ KF-CE-16 ≡ KF-CO-7 ≡ MM-36 ≡ LP-7 ≡ KF-AT-12 ≡ KF-HA-5 ≡ L-4/C-6 ≡ B-1 ≡ RB B-1 — **booked ONCE, never re-booked**); (b) EE-01's phantom trail → KF-CB-18; (c) the `--project demo` CI rider ⟨kf-MatrixEditor K1 / FAM-03 MR4⟩ + OD-29/ME-32's gate-truth rows. **G-0.7's roster is KF.W4's precondition.** Also carried to KF.W4 from INTAKE as born-RED gate-authoring law (G-L7a–d): B10-17/18/19/20/22/23 + B10-27 + B18-19 + B21-17. |
| **KF.W5 · D/L/C Tri-Fold Library Audit** | OUTBOUND | W0 re-runs COUNTS; **W5 reconciles the BASES** — X-4 (80 / 139 / 153 / "159", the SCH-3 drift class with Codex's fourth basis) plus B10-21 (control-coverage ratio). SCH-7's depcruise re-verification is W5's input. One library letter travels: `fromString` APPEND semantics (KF-KE-10). **Declared at repair round 4 (D-3): the `design-idioms.css` S-10 CENSUS ARM is KF.W5's ACT** — banked at `KF-ET-27` ⟨kf-EasingTarget:69, *"C's proposed S-10 census arm over design-idioms.css (glass overlap + contract truth) is ADOPTED as a KF.W5 input"*⟩ and cross-named by `KF-ES-20` ⟨kf-EasingSidebar:61, *"→ KF.W5 (census-truth arm, with the KF-ET-27/S-10 design-idioms precedent)"*⟩. **W0 mints the slot identity at C-17.R and performs no part of the arm**; the split is the C-17 rule applied — *claims are inputs to the mint, and the mint is never the evaluation*. |
| **KF.W6 · its CARRY file** ⟨`docs/tranches/X/keyframes/carry/KF-W6-CARRY.md:354`, `## §CrossEdges (declared from this end)` → **KF.W0 · Substrate Settle**⟩ | **INBOUND — ANSWERED THIS ROUND (R-19b, cures D-6)** | Declared from KF.W6's end and **unanswered from this one** until this round (`grep -c` in this file before repair → `App.vue:176` 0 · `TABS_EXTERNALLY` 0 · `dock-margin` 0 · `KF-SKEL` 0). Received whole at §Carry **C-22** and booked **by id** as G-0.9 appended-correction obligations: (i) the `TABS_EXTERNALLY_MANAGED_KEY` provide site is **`demo/app/App.vue:176`** (path-qualified at repair round 4, R4-10), not the banked stale-HEAD `:169`; (ii) **`--dock-margin` = 9 live unfallbacked sites + 1 prose mention**; (iii) **KF-SKEL-7**'s cascade-level coupling arm beside the import census (which additionally rides §Carry C-1.F row F-13; its **token-namespace probe half stays KF.W6's**). Two of the three are byte-level corrections to banked citations — **exactly the class C-20 names as this wave's test case**. The seam now closes at **both** ends. |
| **KF.W6 · Glass Suffusion** | OUTBOUND | The z-contract prose recut (KF-AX-11/12/31 in one motion, resolving the `--z-behind` policy contradiction at `layout.css:8-11`); the phantom-token/utility audit (KF-APP-6's re-cut, kf-CubeScene D-11's 8 sites); the `/number-field` evaluation (ME-18 precedent) **under whatever fresh id W0 mints**; SCH-2's KfPillTabs 217-line fork, confirmed-at-HEAD, still the top suffusion target. **Nothing in KF.W6 may be scoped from the archived Glass-7 letter (C-14).** G-0.10's fresh **`npm run gh-pages`** build is a shared precondition (R-11, site 5 of 7 — **no `build:gh-pages` script exists at HEAD, `origin/master` or the worktree**; a shared precondition stated as an unrunnable command is a precondition no sibling can satisfy). |
| **KF.W9 · Safari Visual Audit** | OUTBOUND | PRM unification consumes the **re-scoped §6.5** — the vendor's universal `*:not([data-allow-motion])` reset reaches every demo element, so every per-scene tally re-derives (kf-CubeScene: 4 engine motions + 1 gated; **OD-11 corrects the cube to 5**). SS-13's denominator is KF.W9's own scoped surface list — the 73 568-cell Kronecker self-cross-product is **REJECTED** (B18-26, numerator inherited). |
| **KF.W10 · Fold Discharge & Close** | OUTBOUND **+ INBOUND-DEPENDENCY ANSWERED THIS ROUND (R3-10.1, cures D-4)** | **THE C-17-MINT CONSUMPTION EDGE, answered from this end: `KF.W0`'s **C-17 MINT PRECEDES** `KF.W10`'s **CARRY-FORWARD RECORD block** (`KF-W10.md` **§6.A intra-wave order, item 3**, and **§6.B**'s carried lock *"KF.W0's C-17 MINT PRECEDES THE CARRY-FORWARD RECORD BLOCK"* — line parentheticals `:363`/`:374`, non-load-bearing); **`CARRY-C-3` and `CARRY-C-5` consume the ids C-17 assigns**, and a record written before the mint either guesses a slot or invents one. **W10 consumes; it never mints** — C-17 is the only census-slot authority (R-5). Ground: `KF-W6-CARRY.md:361` item 3 (*"the census S-9/S-10 extensions terminalized"*) + item 5, and the claimant block at `:174`–`:179` that `KF-W10.md` §2b cites by coordinate.** This edge is booked **in the edge table, not only in §Carry** — **this file's own C-22 precedent** (*"an edge declared at one end only; an answer that exists in §Carry but not in the edge table still leaves the seam half-open"*) was adopted for KF.W6 one round ago **and not swept to the sibling declaring the harder dependency**; the sweep lands here. The mint's roster is C-17.R **as re-derived at repair round 3 (D-2) and EXTENDED at repair round 4 (D-3)**, so what W10 consumes is no longer short, mis-keyed, coined or anonymous — **and it must be consumed at THIS file's post-round-4 bytes, never at a prior round's** (LAW C: this file writes first in stage 1; the roster it hands forward is **TEN S-9 claimants** — eight minted, one dead-and-enumerated (`K-6`), one refused-and-enumerated (kf-SharePopover `C·C-2`) — **and FOUR S-10 claimants**, the fourth being **`KF-ET-27`** with **`KF-ES-20`** as its second provenance leg, **plus row 13 retained as an enumerated strike**: **fourteen LIVE rows in a fifteen-row table**. A sibling consuming the round-3 thirteen consumes a roster this file has already superseded, and the enumerated-strike row exists precisely so the superseded claim cannot be re-raised as new. — Then: the v8 seal's terminal disposition (357/414 dead within Codex's own corpus → sole citable form `345 exact / 12 partial / 57 unresolved @ 8281638c`); B18-12/13/14 (three external coordinates no artifact reproduces — cannot close rows); B18-27 (slot 4-vs-5, settle or drop); **OWNER BLOCK: OG-KF1 beside OD-V3/OD-V5 — never proxied.** W0 **NAMES**; W10 **RULES**. |
| **KF.W12 · Authoring-Surface Repair** (homed at KF.W4 per R-15 — 6 packets incl. **EDITOR-UNIT**; leg (a) below) | OUTBOUND — **NOT this wave's** | (a) EDITOR-UNIT's F-1 leg **IS** this wave's — census F-1's owning id is **KF-CE-32**, discharged at KF.W0 §B-12; **nothing else** in EDITOR-UNIT is. (b) Declared law travels intact: *"no census KF.W0–W10 row names this repair, and scattering it across ten waves would orphan its one root cause. It must sequence AFTER the KF.W4 type-gate lands (nothing in it is verifiable until a `.vue` file can fail a build)."* (c) Its cure is entangled with KF-CE-3/4 — **no cheap highlighting-only import**: restore contributions, or hand-register a Monarch tokenizer + `setLanguageConfiguration`. (d) Sibling units citing it as precedent (KFED-, OPTIONS-, APPLY-, CARD-UNIT) each carry *"after KF.W0 + KF.W4."* |
| **the NO-WAVE-OWNER packets — the canonical 17** (R-15; **re-cut this round from this file's stale 15**): cube · sequence · spring · spring-plot · spring-physics-facet · spring-artifact-truth · square · **amiga** · drag-seam · CARD-UNIT · OPTIONS-UNIT · KFED-UNIT · **EDITOR-UNIT** · APPLY-UNIT · AXISLINE-UNIT · dock-menu · **transport/ribbon** | OUTBOUND — **homing is SS-1/SS-2's act; this wave supplies their ground** | **Cardinality settled (R-15)**: this file previously enumerated **15**; the canonical roster is **17** = W0's 15 ∪ {**amiga**, **EDITOR-UNIT**} (KF.W9 S-11's adds). **The number 16 was never true at any spec** — KF.W1's unreceipted "16" is corrected at its own end. `transport` is named canonically as **transport/ribbon** ⟨kf-PlaybackRibbon:36⟩. **Homing is discharged by KF.W4's lists, not by this wave**: KF.W11 · Demo Scene Repair ×9 (cube · sequence · spring · spring-plot · spring-physics-facet · spring-artifact-truth · square · amiga · drag-seam) · KF.W12 · Authoring-Surface Repair ×6 (CARD-UNIT · OPTIONS-UNIT · KFED-UNIT · EDITOR-UNIT · APPLY-UNIT · AXISLINE-UNIT) · **KF.W13 · Chrome, Dock & Transport Repair ×2, MINTED** (dock-menu, moved off the KF.W12 list per **KF-W2 §Excluded, the sibling-packet row `The cube · sequence · spring · dock-menu · CARD-UNIT packets`** ⟨round-1/round-2 coordinate `KF-W2.md:270` **struck at round-2 close, U-2/R2-7** — dead at the current bytes; the §-heading + row label are the anchor⟩ + KF.W1 cross-edge 9 · transport/ribbon, **homed for the first time**). W0 **names the roster and supplies the ground; it homes none of them** — and a roster stated at a count no enumeration reaches is the same enumeration defect C-17 and G-0.7 exist to kill, one level up. Every packet re-anchors against the reconciled tree before any cure executes (the KF.W0 law, stated in each record's roster preamble) and sequences after KF.W4's vue-tsc gate. **Locks that must travel WITH each packet, not with this wave**: KFED-UNIT *"sequences after KF.W0 (two blockers are already repaired at the frontier; a careless rebase re-lands them) and after the KF.W4 vue-tsc gate"*; **OPTIONS-UNIT's KF-CO-1/KF-CO-8 cures are SEQUENCED BY LP-1** — no write→render edge exists for the panel (`App.vue:218` `shallowRef(markRaw(…))`, re-`markRaw`'d at every shell-binding assignment; **curing the prop names without curing the reactivity ships a still-dead control**); **kf-ChromeDock M-4's worded cure ships an unopenable menu without MM-28's MUST-CARRY correctness rider**; **TD-1/TD-2's cures BUNDLE** (one commit with TD-4 + PRM + test); **kf-SequenceAxis ruling-1's geometry cure lands WITH the head/label collision cure as ONE commit**; **kf-CubeScene ARB-1** gates the KF-APP-1/-17 motion to the delete arm only until the glass-ui M-A relay lands; **kf-SquareScene's comment-stated invariants are TEST OBLIGATIONS** (the kf-SquareScene law — two stated invariants proved false; the misses added two more); **KF-AV-28 · THE STANDING SUPERSESSION RIDER — carried VERBATIM here for BOTH packets it governs (E-3, repair round 3; M-25: a sequencing rider dropped in transit is a silent drop, and this row is this file's own enumeration of "locks that must travel WITH each packet")**: banked at `kf-AnimationVisualizer.md:35`, **`kf-PlaybackRibbon.md:36`** (the **transport/ribbon** packet) and **`kf-SequenceScrubber.md:36`** (the **drag-seam** packet) — ⟨kf-PlaybackRibbon:36⟩ *"Standing rider: **KF.W7's timeline-evaluate verdict may supersede any NO-WAVE-OWNER cure below** (the KF-AV-28 rider); note the C axis's own **§7 caution** rides with it — this ribbon is the **NON-bespoke** case (it consumes the real `Slider`) and carries BLOCKERs anyway, so **'swap onto the primitive' is never sufficient as a cure**"*; ⟨kf-SequenceScrubber:36⟩ *"KF-AV-28's standing rider applies: the KF.W7 ScrubberTimeline evaluation gates the component-shape rows."* **Both limbs travel; the §7 caution and the NON-bespoke clause are the half that makes the rider a CURE-SHAPE lock and not merely a sequencing one** — a packet that carries only the supersession sentence can still be cured by a primitive swap the caution forbids. `KF-W4.md` §Sequencing (the KF-AV-28 standing-supersession-rider paragraph) carries the supersession core for drag-seam and names all three bank sites; the §7 caution and NON-bespoke limb are carried **here**, so the two ends now agree and neither drops a half. |
| **V·π parser program / value.js parser lane** | OUTBOUND — census annotation, **explicitly NOT SS-13** | Three carries stay value.js-side, named so no KF wave books them: the **C-L-2 residual** (do `parseCssValue`/`parseCssScalar` internally dispatch into the colour grammar for non-colour declarations? — untraced by all five CubeScene seats); **R1**'s identity (banked, KF.W3-gated — *"a value.js-side question, not an SS-13 pixel"*; KF-HA-19's R1-framing contradiction relayed as a NO-WAVE-OWNER carry); the `parseTimingFunction` non-string raw-TypeError (R1 class, evidence forwarded from kf-ChannelOptions, identity already banked). **KF.W3 is GATED on PLAW-BIND → V.L1/V.L5 → packed Value release.** |
| **keyframes.js engine lane (`src/`)** | OUTBOUND | Preserved halves of rows whose substrate faces land here: the `applyTimingFunction` frame-propagation gap (L·B-2's residue, per L's own handoff note) and the `parseAnimationCSS.ts` per-stop timing-function drop (R3-forwarded, lane-library §4.6 seam). **Engine work, not substrate work — named so the reconciliation does not absorb them.** |

## Excluded

Not carried by this wave, each with its reason. Silent drops are the defect class this program exists to kill; these are loud.

| Excluded | Reason |
|---|---|
| **X-4 basis reconciliation** (80 / 139 / 153 / "159") | W0 re-runs the **counts**; the **bases** are KF.W5's row per the INTAKE carried table. Restating four bases here would mint a fifth. |
| **SCH-7 depcruise re-verification** | KF.W5's input. W0 restates it only as *unverified-at-HEAD* — "0 depcruise violations" does not refute L-1, and a green run here would be exactly that false refutation. |
| **SCH-2 (KfPillTabs 217-line fork)** | CONFIRMED at HEAD; **re-verify, do not re-book**. Cure is KF.W6's top suffusion target. |
| **The `/number-field` evaluation** | KF.W6's (ME-18 precedent). W0 fixes only the **identity** (C-17). |
| **The z-contract prose recut** | KF.W6, in one motion with KF-AX-11/12/31. W0 owns only the **census** re-anchor. |
| **The header-ribbon migration decision** (which chrome host replaces the band) | NO-WAVE-OWNER, SS-1/SS-2's design decision — *"the cheapest close of the banked blocker AND of D-15/D-17"*. W0 carries only the tripwire. |
| **kf-EditorHeader `EH-4` · `EH-5` · `EH-8`** — three of the corpus's six **LIVE** escapes ⟨`kf-EditorHeader.md:43` · `:44` · `:47`⟩ — *added at repair round 5 (D-5; RULINGS-5 R5-1(7), the reciprocal of the BLOCKER cured at KF.W6)* | **KF.W6's, and LIVE — not moot.** Their banked anchors lie **outside** the deleted fork: `EditorShell.vue:45` (EH-4, the `title="Toggle dark mode"` collision), `EditorShell.vue:46` (EH-5, the inert `aspect-square w-8` / 32×36 box), and the live shell ribbon cluster (EH-8, one sizing decision WITH EH-5). **RULINGS-5 R5-1 STRIKES the round-4 premise that every EH-\* limb is MOOT-ON-DELETE** — that premise was built from co-id twins rather than from the rows' own banked anchors, and it inverted the record's central finding: *the fork is dead precisely because its defects are live at the shell*. The three are **RESTORED LIVE at KF.W6**, one row per anchor, spendable under that wave's existing `EditorShell.vue` / `MbabbMenu.vue` grants; rendered-box residue → **KF.W9** (SS-13). W0 books **none of the three** and asserts nothing about their cures — it declares the routing so the family is enumerable from the roster wave. Anchor-only per LAW C(3) / RULINGS-5 LAW E(4): **`KF-W6.md` §Excluded, the re-cut MOOT-ON-DELETE row, and its restored EH-4 / EH-5 / EH-8 rows**. |
| **kf-EditorHeader `EH-2` · `EH-3` · `EH-12` · `EH-13` · `EH-14` · `EH-15`** — the remaining live-escape pair plus the four-member state-seam family ⟨`kf-EditorHeader.md:41` · `:42` · `:51` · `:52` · `:53` · `:54`⟩ — *added at repair round 5 (D-5; R5-1(7))* | **NO-WAVE-OWNER**, per their own banked rows, and terminalized **under G-2's terminal sweep** — anchor-only: **`KF-W10.md` §E, the `NWO-TERMINAL-SWEEP` row · gate G-2**. Homing a NO-WAVE-OWNER packet is **SS-1/SS-2's act**, never this wave's (the row two above states the same law for the migration decision); W0 supplies their ground and homes none of them. Booking any of the six here would be the re-booking §Excluded convicts. |
| **kf-EditorHeader `EH-9` · `EH-10`** ⟨`kf-EditorHeader.md:48` · `:49`⟩ — *added at repair round 5 (D-5), so the **sixteen**-member family enumerates WHOLE from the roster wave (the id set derived by command at the bank; the partition and its counting rule sit beneath this table)* | **KF.W6's, DISCHARGED-BY-TWIN and verified at their carriers there** — EH-9 via the SharePopover twins, EH-10 via the token-alignment family. **The disposition is KF.W6's to state and to verify; W0 only enumerates it** — and R5-1(3)(c) binds that wave: *if either carrier is absent at its bytes, the row is restored LIVE, no id resting on an unverified twin*. Anchor-only: **`KF-W6.md` §Excluded, the re-cut MOOT-ON-DELETE row (the `SP-8`/`SP-10` and token-alignment clauses)**. W0 books neither. |
| **The demo-typechecking arm of C-12** | KF.W4 (post-F-1), against the single banked no-SFC-typecheck identity. Booking it here would be the tenth re-booking of a once-booked row. |
| **KF-CB-18** — EE-01's surviving phantom trail (`easing.ts:44`, `waapi/eligibility.ts:169`, the m-6 test) | Not cured at the frontier; KF.W4's. |
| **KF-KE-17** (`frames: any[]`) and FE-3's terminal cure (`frameStart: KeyframeSelector` or a `selectorText`-fed label; L-15's three-line acceptance test) | The **cause** is not cured by the substrate settle. KFED-UNIT, after KF.W0 + KF.W4. |
| **FE-3's partial-cure residue** — fraction-for-percent (`"0.5"` vs the commit parser's `"50%"` at `:206-209`), named-selector fallthrough, `selectorText` uncalled at both seams | Explicitly not G-0.4's; KFED-UNIT's. |
| **KF-KE-10** (`fromString` APPEND semantics; 3→6 frames, 10 duplicate cards projected) | KFED-UNIT + one library letter to KF.W5. |
| **L·B-2's engine residue** — direct-field-write bypass of `setTimingFunction`, `applyTimingFunction` frame-propagation gap, N-10 double write, N-17 flatten tripwire | OPTIONS-UNIT / engine lane at MAJOR, per L's own handoff note. |
| **Every NO-WAVE-OWNER scene/behaviour packet's cure** | Homing is SS-1/SS-2's act. W0 supplies their ground (the reconciled tree) and declares their locks; it homes none of them. |
| **B18-12/13/14** (three external coordinates no artifact reproduces), **B18-27** (slot 4-vs-5) | KF.W10's. Cannot close rows here; carried unresolved by design. |
| **B10-9 · B19-10 (pin half) · B19-11 · B20-6 · B21-7** (TCC re-reads) | **Conditional** — owed only if the v8 contract is ever cited. G-0.8's disposition decides whether they open at all. |
| **OG-KF1's ruling** | **Named** here, **ruled** at KF.W10's owner block beside OD-V3/OD-V5 — never proxied. A seat that rules it here fails the wave. |
| **B18-26's numerator** | Split adoption stands as banked: numerator INHERITED, denominator REJECTED. KF.W9 scopes its own list; W0 re-books neither half. |
| **kf-CubeAxisLines β-miss-1** — the `optionalDependencies` failure-SHAPE (`npm ci --omit=optional` breaking the build at `style.css:3`) | HEAD-only artifact, **killed at the frontier** (devDep, not optionalDep). *"Recorded for the KF.W0 reconciliation narrative, charged nowhere"* ⟨kf-CubeScene kill 4⟩ — carried as narrative, booked as nothing. |
| **The clean-`npm ci`-fails probe · the "verify against 6.0.0" residue · every "blocked on F-1" gate in the CopyButton corpus (D-9, C-15, M-6 leg 2)** | RETIRED / DISCHARGED / RE-SCOPED at their banks (C-1). Recorded so no seat re-spends the budget. |
| **Reader-B's substrate caveats** L-5 / L-9 / L-14 / C-7 / P-1 / P-6 and the corollary "P-1's adoptCompiled watch never fires at HEAD" | **VACATED** with the RESCOPED-BY-INVERSION ruling. Preserved as a dated record, not as live findings. |
| **KF.W1's mail re-delivery** | Depends on this wave, is not performed by it. |
| **Any product-source change beyond the owner's reset and the two EE-02 paths** | **Execution gate**: nothing opens product source; the specs await the owner's begin-word. |

**THE kf-EditorHeader FAMILY, ENUMERATED WHOLE (repair round 5, D-5 · RULINGS-5 R5-1(7)).** The record routes its escapes **KF.W0 / KF.W6 / NO-WAVE-OWNER** with W0 named **first**, over a family W0 books **one** member of — and before this round **eleven of the other fifteen had zero hits** in this file (`grep -oE 'EH-N\b' KF-W0.md | wc -l` → **0** at each of `EH-2` · `EH-3` · `EH-4` · `EH-5` · `EH-8` · `EH-9` · `EH-10` · `EH-12` · `EH-13` · `EH-14` · `EH-15`, run this seat before the writes; `EH-1` → **7**; `EH-6` · `EH-7` · `EH-11` · `EH-16` → **0** likewise and are the four outside R5-1's set), so a seat reading the first-named home could not decide whether any of them was W0's.

**The partition, in one place. Counting rule**: *one line per banked `EH-*` id in `kf-EditorHeader.md`, the id set derived by command — `grep -oE '\*\*EH-[0-9]+' kf-EditorHeader.md | sort -u` → **EH-1 … EH-16, sixteen ids**, run this seat; each id disposed exactly once.*

1. **`EH-1` → KF.W0, BOOKED** — the two repo-qualified letters at **C-14**, the gate at **G-0.10**, the §Sequencing glass-ui FYI row. **1 id.**
2. **`EH-4` · `EH-5` · `EH-8` → KF.W6, LIVE** — the **first** of this round's three new §Excluded rows above; the round-4 MOOT-ON-DELETE premise struck at RULINGS-5 R5-1, their banked anchors lying outside the deleted fork. **3 ids.**
3. **`EH-9` · `EH-10` → KF.W6, DISCHARGED-BY-TWIN** at their carriers, verified there — the **third** of the three rows above. **2 ids.**
4. **`EH-2` · `EH-3` · `EH-12` · `EH-13` · `EH-14` · `EH-15` → NO-WAVE-OWNER** under G-2's terminal sweep — the **second** of the three rows above. **6 ids.**
5. **`EH-6` · `EH-7` · `EH-11` · `EH-16` — OUTSIDE R5-1's struck set and outside the record's `:117` routing sentence, carried here so the enumeration is honest rather than convenient**, each at its **banked** disposition and none of them W0's — **quoted byte-exact from the `Disposition:` clause of each row, inner emphasis reproduced as the source carries it, ellipses marked** (LAW D(1) · R5-13; `grep -nE '^\- \*\*EH-(6\|7\|11\|16) ' kf-EditorHeader.md`, run this seat): `EH-6` ⟨`:45`⟩ *"**rides KF-APP-41's delete** (NO-WAVE-OWNER); the delete wave must decide umbrella-vs-line, not just the line."* · `EH-7` ⟨`:46`⟩ *"**glass-ui BH relay** (rides KF-APP-5's existing relay: Esc semantics + empty-anchor focus target)."* · `EH-11` ⟨`:50`⟩ *"**NO-WAVE-OWNER** (SS-1/SS-2 decide the icon-button naming idiom once, harmonizing KF-CB-7/-26 with this row; …)"* · `EH-16` ⟨`:55`⟩ *"**glass-ui BH relay**."* **4 ids.** W0 books none of the four; the two relay members ride the standing glass BH relay this wave already carries a §Bounds `INBOX.md` row for, under **KF-APP-5's** packet and not under a fresh mint.

**`1 + 3 + 2 + 6 + 4 = 16`** — sixteen ids, sixteen dispositions, **every member of the family resolving to a named home from the roster wave**, which is what §Excluded exists for: *"Silent drops are the defect class this program exists to kill; these are loud."* **No cell in this file re-issues an EH count**: the numeral above is read off this enumeration and is stated only here.

**SHADOW** (RULINGS-5 LAW F, mechanism S — every act that moves, strikes, drops, re-keys, narrows, re-points or mints a routing books its shadow in the same edit): **re-pointed set = {`EH-4` ⟨`kf-EditorHeader.md:43` → `EditorShell.vue:45`⟩ · `EH-5` ⟨`:44` → `EditorShell.vue:46`⟩ · `EH-8` ⟨`:47` → the live shell ribbon cluster⟩ · `EH-9` ⟨`:48`⟩ · `EH-10` ⟨`:49`⟩ · `EH-2` ⟨`:41`⟩ · `EH-3` ⟨`:42`⟩ · `EH-12` ⟨`:51`⟩ · `EH-13` ⟨`:52`⟩ · `EH-14` ⟨`:53`⟩ · `EH-15` ⟨`:54`⟩}, **each entered against its OWN banked anchor and never by co-id twin, host file or fold-carrier** (LAW F(3)); plus **`EH-6` ⟨`:45`⟩ · `EH-7` ⟨`:46`⟩ · `EH-11` ⟨`:50`⟩ · `EH-16` ⟨`:55`⟩ carried at their BANKED dispositions and NOT re-pointed** (item 5 of the partition — enumerated in the shadow because an act that declares a family's routing must show the members it left untouched, or the enumeration is a selection); **disposition per id** as partitioned above — **nothing is struck, dropped or narrowed by this wave: this is a DECLARATION of a routing, not a disposal of one**, and no cure is spent, moved or denied here; **citing / terminus waves notified = {KF.W6 (the restored-live and discharged-by-twin rows — the acting wave, RULINGS-5 R5-1), KF.W9 (the SS-13 rendered-box residue and the guard row R5-1(5) vindicates), KF.W10 (§E · `NWO-TERMINAL-SWEEP` · G-2, the terminus for the six NO-WAVE-OWNER ids)}**, each cited **anchor-only** per LAW C(3) / LAW E(4).

## Cadence, Artefacts, Commits

`git diff --check` at each unit boundary; Prettier over touched `.md`. **No `npm run lint` / `check` / `test` runs in this wave** beyond G-0.4's three named oracles (invoked **by path**: `node scripts/observe/demo/smoke.mjs`, `node scripts/observe/demo/live-session.mjs`) and G-0.10's one **`npm run gh-pages`** (R-11, site 6 of 7) — inventing a green run over untouched code is the vacuous evidence L-19 forbids; the replacement evidence is the ten gates' pasted before/after outputs.

Banked under `docs/tranches/X/keyframes/artefacts/W0/`: `substrate-open.txt` / `substrate-close.txt` (the G-0.1 sextuple both sides) · `manifest-four-coordinates.txt` · `ee02-diff-open.txt` / `ee02-diff-close.txt` · `oracle-ee01-fe3-ee03.txt` · `headerribbon-tripwire.txt` · `counts-2026-XX-XX.txt` · `gate-roster.txt` · `refofrecord-stamp-audit.txt` · `glass-citation-rederivation.txt` + the fresh **`npm run gh-pages`** build hash (R-11, site 7 of 7) · `v8-disposition.txt`.

Commits, pathspec only, `scripts/dev/dev.sh` never included: `docs(kf-w0/settle)` · `docs(kf-w0/manifest)` · `docs(kf-w0/counts)` · `docs(kf-w0/roster+provenance)` · `docs(kf-w0/ref-of-record)` · `docs(kf-w0/v8)` · `docs(kf/w0 close)` — the last stamps **IMPLEMENTED**; **VERIFIED is KF.W10's to stamp** (R-A).

## L-18 rider

Landing the ten gates green makes this wave **IMPLEMENTED**, not ACCEPTED — two challenging gestalt passes, each a quartet of Opus 5 skeptics at three altitudes, adjudicated and agglomerated by a fresh Fable instance. Bases with obvious purchase here: a re-anchor applied per-repo when C-21 proves it must be per-file; **a disposition record covering the 252 status rows over a 325-file frontier surface — the two surfaces do not nest, so the residue is the MEASURED 225 (`comm -13`, pasted at G-0.1's falsifier), never the subtracted 73, and 152 status rows (124 untracked) sit outside the frontier diff in the other direction (D-2, repair round 4)**; a manifest ruled single-state while `.npmrc` stays unruled; oracles run before the migration lands, measuring the frontier and proving nothing; a gate roster booked at a hand-list altitude while the measured surface is larger (the named residue-check is now the universal itself: `git grep -ho 'proof:[a-z0-9-]*' origin/master -- demo/ | sort -u` → **54 names, 1 runnable**, so any roster naming fewer than **53 dead** is RED); a wave that "strikes" a `demo/**` byte to green its own roster gate and invalidates itself; glass citations "verified" by matching version strings — the exact defect C-13 convicts. **L-20** binds this file: re-validated against `docs/precepts/` at authoring, at every amendment, and at each L-18 pass.
