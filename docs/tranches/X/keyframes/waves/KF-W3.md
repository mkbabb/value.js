# KF.W3 — Parser Consumption (GATED, never scheduled)

**Wave**: KF.W3 · Parser Consumption — the repin `4.0.0 → V`, the `serializeCssValue` fork retirement, the DUAL-1 unification, the 8-curve delta acknowledgement, and the R1 call-map at the consumed coordinate.
**Half**: **library** (keyframes.js `src/animation/compile/**` + `test/**`). **Sub-tranche**: X·KF. Spec home `docs/tranches/X/keyframes/waves/KF-W3.md` (earlier sketches spell the path `waves/W3.md`; `KF-W3.md` is the ratified filename — a search alias, never a rename; R-B idiom. The former attribution of that spelling to *"the CARRY's boundsFiles"* is **struck**: no KF-W3 CARRY exists — RULINGS R-12). Authority `docs/tranches/X/COHESION.md` §0/§1/§2, ratified under `docs/tranches/V/megatranche/SCOPE.md` **M-25**.
**Status**: **planned**. No product byte is written by this file. Authoring opened no product source for writing — every keyframes.js anchor below was re-resolved **read-only via `git show origin/master:<path>`**, never by opening a working-tree file. Execution awaits the owner's begin-word **and** `RC-P(V)`.

**THE OPENING CONDITION, VERBATIM AND BY PREDICATE NAME** (X.P.W4 §6 G-6's owed sentence; this file is its only lawful site):

> **This wave opens if and only if `RC-P(V)` evaluates TRUE per `docs/tranches/X/parse-that/RELEASE-CONDITION.md` §6a, run against the registry coordinate `V`; the repin 4.0.0→`V`, the `serializeCssValue` fork retirement, and the DUAL-1 unification are all downstream of that evaluation.**

`RC-P` is named as a **predicate**, never as a wave number — wave numbers renumber, predicates do not. It is evaluated by running the six commands against the registry coordinate, **never by reading a status word in any document, including this one.**

**Alias law (R-B idiom).** `KF.W3` denotes `X.KF.W3`; a search alias, never a rename. **Second alias, load-bearing and adversarial**: `KF.W3-SHIM`, as written in the superseded `formation/keyframes/lane-frontend.md §10` seven-wave sketch, **does not denote this wave** — see §Excluded E-1. The number 3 is not inherited across taxonomies.

**Ref of record.** keyframes.js `origin/master` **`81a56990`**. Local HEAD `8281638c` is **1 ahead / 41 behind** (re-measured by this fold seat, 2026-08-28) and is DISQUALIFIED as witness substrate (banked kf-App ruling 1; KF.W0 §B-12).

**Provenance**: adjudicated rows consumed by mechanism, deduped by identity, never transcribed (M-25); validated against `docs/precepts/instructions/tranche/WAVE_SPEC.md` (L-20) — the lean form maps §1→Header, §2/§2a→four-verb+goal, §3→Scope, §4→§Bounds, §6→§Gates, §10→§Sequencing, §11→§Excluded; no gate exists here without a named live witness (L-19).

**Provenance restated at repair round 1 (RULINGS R-12).** The prior claim — *"the fresh-Fable fold (M-23) of one draft against the 7-row CARRY: 7/7 rows carried, 0 dropped"* — is **STRUCK**: no KF-W3 CARRY ledger exists at any coordinate (`docs/tranches/X/keyframes/carry/` holds **`KF-W6-CARRY.md` only**). A completeness claim keyed to a ledger that does not exist is unverifiable, and the accompanying "0 dropped" was false.
**Figure basis re-cut at repair round 3** (RULINGS-3 §END · PASS-3/KF-W3-CHECK **D-5**). The round-1 replacement — *"201 rows / 54 records; **"424"** was the literal `KF.W6` token count, never a row count"* — is **STRUCK**: measured by this seat over the carry file, 2026-08-28, `grep -o 'KF\.W6' KF-W6-CARRY.md | wc -l` → **11** (not 424) and `grep -c '424' KF-W6-CARRY.md` → **0**; *no corpus yields 424*, so a struck unverifiable number had been replaced by a differently unverifiable one — the same shape R-12 convicted. What replaces it is stated **only with the command that reproduces it**: `wc -c` → **199 055 B** · `grep -c '^- '` → **201** bullet rows · `grep -o 'kf-[A-Za-z0-9]\+' | sort -u | wc -l` → **55** distinct record identifiers, of which **50** are basenames of the 58 adjudicated records (`comm -12` against `ls registry/adjudicated/kf-*.md`). The identifier figure is **pattern-dependent** — PASS-3/KF-W3-CHECK D-5 reports **63 / 39** under its own pattern — so it is never cited bare, only beside its command; the two readings are recorded together rather than reconciled by assertion. **None of them is load-bearing**: the whole point is that `KF-W6-CARRY.md` is a **KF.W6** ledger and there is no KF-W3 CARRY. This spec is **authored from the 58-record registry directly; the routed denominator is the registry's (PASS-1 census)**: **19 canonical routed ids, 24 with the `KF.W3-SHIM` family**. Carriage of record: **9** carried as fold-identities — **enumerated per row, at repair round 5, because a range was not an enumeration** (RULINGS-5 §END KF-W3 **D-4**; PASS-5/KF-W3-CHECK **D-4**): **§Carry C-1's own kf-AmigaScene S+2 row (1) + its four riders (4) · C-2's KF-CB-33 ⊃ KF-SST-36 / KF-KC-53 fold (3) · C-3's KF-ET-32 KF.W3 leg (1)** — **1 + 4 + 3 + 1 = 9**, id for id, identical to §Carry's own arithmetic sentence. The former parenthetical *"§Carry C-1..C-5 riders"* is **STRUCK**: it named **C-4 (K-6)** and **C-5 (D-27 / L-7 / C-9)** as sources of the nine, which the **round-1 correction had already killed at §Carry** (*"C-4 (K-6) and C-5 (D-27/L-7/C-9) are carried rows that are NOT members of the routed 19"*) — the correction was applied to the instance a ruling named and not to its SCOPE, so it survived four rounds at this second site; this is R4-10(1)'s class, cured here by sweeping the scope. **The count never moved** (9 at both sites, `9 + 5 + 10 = 24` unchanged) — only the provenance range was wrong, and it was the one place a reader rebuilding the nine from §Provenance alone would have built them from the wrong five rows. **SHADOW** (LAW F): struck set = {the provenance range *"C-1..C-5"* ⟨§Provenance, this cell — the range form occurs in no other wave spec⟩}; disposition = replaced by the per-row enumeration above; **C-4 and C-5 keep their §Carry rows, verbs, severities and riders untouched** — they are *carried-but-not-counted* witnesses (C-4 a kill-scope bounds correction, C-5 the SquareScene witness), not dropped rows, and no id leaves this file; citing/terminus waves notified = **none** (`grep -l 'C-1\.\.C-5' waves/KF-W*.md` names this file only). *Receipt*: `grep -c 'C-1\.\.C-5' KF-W3.md` → **2** — this line and §B.6's round-5 SHADOW ledger, the only two places the struck range survives, in both cases **named as the struck historical form** and in neither as a live provenance claim. **5** disposed at §Excluded **E-1** (the SHIM family), **10** booked at §Excluded **E-15** as exclusions-with-named-owner (owner **KF.W2**) inside G-KF3-7's call-map denominator — the arithmetic is **9 + 5 + 10 = 24**. **Carriage is not asserted in this file's voice (LAW B, RULINGS-3)**: the closure claim lives in the freshest conformance census artifact — **re-pointed at repair round 5 to `docs/tranches/X/keyframes/conformance/PASS-5/KF-W3-CHECK.md` §1/§2 (2026-08-28) — THE ONE ARTIFACT OF RECORD for this claim at every site in this file** (RULINGS-5 §END KF-W3 **D-3** / R5-9 row 6; the sweep's enumeration source is the **SCOPE**, never the example list — R4-10(1) — so the class is made greppable, `grep -on 'PASS-[0-9]/KF-W3-CHECK\.md' KF-W3.md`, and its discriminator is stated at §B.6's round-5 block), which derived the denominator **by record** from the 58 records' bytes, inheriting no prior pass's partition, and reported **routedTotal 24 · BOOKED 24 · ESCAPED 0 (carriage 100 %)** — the identical three figures its predecessor `PASS-4/KF-W3-CHECK.md` §1/§2 reported from an equally independent by-record derivation, so this re-point moves the **citation** and not the number. That seat **independently re-derived all six PASS-4 defects as CURED** and reported five of its own — **PASS-5 D-1** (this file's five-position in-spec receipt, falsified at its own bytes; cured at E-15's **(R-i)** reading step), **PASS-5 D-2** (the round-4 CLOSE-CERT falsified by a post-certification sibling write; cured at §B.6's round-5 block), **PASS-5 D-3** (this re-point), **PASS-5 D-4** (the fold-identity provenance range, struck above) and **PASS-5 D-5** (the emphasis idiom, disclosed at E-14.5) — above the round-4 defects it re-derived as cured: **PASS-4 D-1** (the payload admission rule applied to twenty ids and withheld from ≥ six banked negatives of identical shape; cured at E-15's round-4 sub-line and at G-KF3-7's arithmetic), **PASS-4 D-2** ((R-i)'s self-description false; cured at E-15's membership law), **PASS-4 D-3** (the KF.W10 seam undeclared from this end; cured at §Cross-edges), **PASS-4 D-4** (G-KF3-5's six-vs-eight; cured at the gate), **PASS-4 D-5** (`emit/index.ts`'s no-act `modify`; cured at §B.2 → §B.3) and **PASS-4 D-6** (E-14.5's stale sibling anchors; cured under LAW C(3)). The predecessor artifact `PASS-3/KF-W3-CHECK.md` is **superseded, not deleted**, and every citation of it in this file carries an explicit `PASS-3/` prefix — passes 2, 3, 4 **and 5** all number their own defects in a `D-*` series, so an unqualified cite resolves to four artifacts and is not a receipt (LAW D). When a later pass supersedes this one the citation is re-pointed **by scope, in one sweep over the greppable class above, not at the instances a ruling happens to name** (repair round 5, PASS-5/KF-W3-CHECK **D-3**); the claim is never re-asserted here. *(E-15 books **twenty-six** ids, not ten: the ten escapes **plus sixteen surplus** — the two R-1b cross-declarations in its table (kf-KeyboardShortcutsModal's R1-negative and kf-SpringTrace's posture datum), **the eight banked R1 negatives added at repair round 2 on its surplus sub-line** (RULINGS-2 **R2-11**; PASS-2/KF-W3-CHECK **D-02**) and **the six added at repair round 4** (RULINGS-4 §END KF-W3 **D-1**; PASS-4/KF-W3-CHECK **D-1**). None of the sixteen surplus ids is a member of the 19/24 routed census — each measured **0** on every routing markup, by command at each sub-line — and none is ever counted into it; the two arithmetics are stated separately here so neither can be read off the other.)* The fold seat independently re-measured every gate-load-bearing anchor at `origin/master` and repaired five draft imprecisions (recorded in §B.1).

| verb | state | basis |
|---|---|---|
| AUDITED | YES | `registry/adjudicated/`: kf-AmigaScene · kf-CSSCodeEditor · kf-KeyframeTimeline · kf-KeyframeCardList · kf-KeyframesStringControls · kf-CopyButton · kf-StartingStyleTarget · kf-KeyframeCard · kf-EasingTarget · kf-SquareInstrument · kf-SquareScene · kf-ChannelControls · kf-AnimationControlsGroup · kf-KfPillTabs · kf-DemoGlobalChrome · kf-ControlsPaneWrapper · `audit/codex-provenance/INTAKE-ADJUDICATION-2026-08-03.md §3` · `CENSUS-2026-08-03 §(a)/§(c)` |
| SPECIFIED | YES — this file | **basis restated at repair round 1 (RULINGS R-12)**: the former *"the SS CARRY table consumed whole"* is **STRUCK** — no KF-W3 CARRY exists at any coordinate, so it could never have been consumed. Basis = the **registry's PASS-1 routed census** (19 canonical / 24 with the SHIM family), read id for id; every routed id lands as a gate, a bounds item, a named cure with its locks, or an §Excluded line with its reason **and its named owner** |
| IMPLEMENTED | NO | gates green + bytes landed in the named execution site stamps this |
| VERIFIED | NO | stamped only at X·KF close (KF.W10); no wave stamps VERIFIED at its own close |

**Goal criterion.** This wave succeeds if, after the repin, **a reader can name every kf site that can reach a value.js parse or emit throw, by call and not by import, across both organs** — the colour/scalar channel *and* the easing/css channel — and **exactly one `serializeCssValue` and exactly one `reverseAnimationShorthand` are reachable from the emit tier _and every sentence naming their provider is true_** *(the second conjunct added at repair round 3: §B.5 census 2 shows `reverseAnimationShorthand` is **already** numerically one at the frontier while six sites narrate it as value.js's, which it is not — a counting-only goal would score itself satisfied with the defect standing)*, with the 8-curve shape delta acknowledged in the mail ledger before the boot-evaluated fence consumes it. A wave that bumps the pin and runs the suite has **failed this goal**: the suite is green today at 4.0.0 with 324 throws live in the dependency.

**Execution shape**: 3 seats, 2 phases — phase 1: 2 parallel (`.a` the R1 call-map + attribution harness ∥ `.b` the DUAL-1/fork unification design); phase 2: 1 serial (`.c` repin + fixture attribution + the two mail-ledger discharges + BH relay). Peak concurrency 2, inside the owner's four-workflow cap.

---

## §0 Open preconditions (checked at wave-open; NOT close gates)

| # | precondition | state at authoring (measured by this seat, 2026-08-28) |
|---|---|---|
| **OP-1** | **`RC-P(V)` TRUE.** The opening condition above. | **FALSE — all six conjuncts FALSE.** `grep '"version"' package.json` (value.js) → `4.0.0`; `ls docs/tranches/X/parse-that/RELEASE-CONDITION.md` → *No such file*; `ls docs/tranches/X/parse-that/RELEASE-PACKET.md` → *No such file*; `grep -c 'RC-P' docs/tranches/V/coordination/INBOX.md` → **0**; OC-1 UNRULED since 2026-07-20; the full-surface harness has never been run. |
| **OP-2** | **Write authority named** (census §(c) gap 5, verbatim: *"keyframes.js is sibling-read-only under value.js law and the sacred-checkout rule … The specs must name WHERE kf waves execute (the exec clone / a new kf tranche letter) and under whose hand."*) | **UNRESOLVED.** This wave names the question; it does not answer it. |
| **OP-3** | **The authoring/ownership seat named.** COHESION §1: SS-1 covers KF.W0/W2/W4/W5/W8; SS-2 covers KF.W1/W6/W7/W9. | **AUTHORING HALF DISCHARGED — re-anchored on `COHESION.md` §0b (RULINGS R-7b).** §1's table is stale against its own dated addendum: **§0b ADDENDUM 2026-08-28** rules *"**KF.W3 ownership gap … CURED by assignment.** §1 assigned parser-consumption to neither SS-1 nor SS-2. Disposition: KF.W3 is a library wave (parse façade → consumption); it belongs to **SS-1**, and its spec was authored under that seat at `KF-W3.md` (banked `b6e09ed4`). The wave's GATED posture is unchanged — PLAW-BIND keys it to the X·P release condition; never scheduled by this assignment."* The addendum names this file; §1 is read through it. **ONE owed ruling remains — the write hand (OP-2), which this row does not touch.** The KF.W10 sibling gap is **not this wave's to declare**: RULINGS **R-18** recommends **SS-2** (coordination/close wave; hardest dependency KF.W1, SS-2's Mail Cure) and routes the cure to a **COHESION §0c addendum — the root session's act**. This row cites §0c and books nothing. |
| **OP-4** | **KF.W0 Substrate Settle** — the frontier re-anchor and the manifest schism. | OPEN. §B.1 below is this wave's own re-anchor pass and is offered to W0 as input, not as a substitute. |
| **OP-5** | **KF.W1 Mail Cure** — O-11 §B (`serializeCssValue` retirement, booked *"OPEN — gated on the 4.1 bump"*) and O-11 §D2 (the 8-curve delta, *"DECLARED; no keyframes acknowledgement on record"*) must be exec-visible. Both packets were mis-delivered into the stale sacred checkout. | UNLANDED. Census §(c)6: *"The mail cure precedes everything kf-side."* G-KF3-4 and G-KF3-6 are the kf-side discharge of exactly those two packets and **cannot be authored honestly until the cure lands them exec-visible**. |
| **OP-6** | **KF.W4 K-Quartet Hygiene IMPLEMENTED** — the easing-name trap disarmed (KF-CB-18/24/29 + K1) *and* the gate chassis landed (KF-CB-8 + KF-CE-16 + KF-APP-4: `vue-tsc --noEmit` on the merge path). | ABSENT. Two separate blocks: repinning before the easing letter lands **re-arms** the trap this wave exists to name (§Seq L-3); and until the chassis lands, every demo-side gate here is unenforceable (G-KF3-9). |
| **OP-7** | **KF.W2 Parse Façade** — the delegation target that makes consumption one-touch (one kf-side `parse()` over the 13 Tier-A sites; the 6 Tier-C regexes deleted incl. the `cssom.ts:214-216` identifier→RegExp injection). | OPEN. Without it a diagnostic-shape change at the repin touches five failure postures independently. **This wave owns reachability; KF.W2 owns posture.** |

OP-1, OP-2 and OP-6 are hard. **OP-3's authoring half is DISCHARGED at COHESION §0b (SS-1); its write-hand half is OP-2's and stays hard** — the two rulings census §(c)5 named were never one ruling, and only one has landed.

---

## §Bounds

### B.1 Frontier re-anchor (performed by this fold seat, read-only, at `origin/master` `81a56990`, 2026-08-28)

KF.W0 §B-12 binds **every anchor a spec inherits** to re-baseline against `origin/master` before any cure. *(Phrasing corrected at repair round 1, RULINGS **R-12**: the earlier text read *"every anchor in the CARRY"* and the table below was headed *"CARRY anchor"* — **struck**. There is no KF-W3 CARRY ledger to inherit from; the anchors below are inherited from the **census**, the **58-record bank**, and the **draft**, and are named as such row by row. The law is unchanged; only the false provenance of its subjects is.)* This seat performed the re-baseline by command and **five inherited anchors needed correction at the frontier** — two of them path errors that would send a cure to a file that does not exist. The corrections are load-bearing.

| inherited anchor ⟨census · bank · draft⟩ | frontier truth (this seat's measurement) | disposition |
|---|---|---|
| `src/animation/compile/emit/css-text.ts` `:17` / `:41` / `:54` | **HOLDS exactly.** `:17 export const reverseAnimationShorthand = (` · `:41 export const serializeCssValue = (value: CssValue): string => {` · `:54 if (!serialized.ok) throw new TypeError("Value returned an unserializable CSS color.");` **Precision repair**: the fork spans `:41-56`; `:43 :48` are recursive self-calls, and `:66 :83 :89` are **in-file callers** (declaration serialize · `initial-value` emit · parameter default) — five use sites re-route at the retirement, not "self-calls at five lines". | anchor-safe, spans corrected |
| `src/animation/compile/emit/format-options.ts` `:20 / :81 / :121` | **PATH WRONG.** True path `src/animation/compile/emit/format/options.ts`; **all three line anchors hold at the corrected path** (`:20` import of `reverseAnimationShorthand`, `:81` *"the published 0.12.0 inverse"*, `:121 return reverseAnimationShorthand(cssOptions);`). **PROVIDER RESOLVED at repair round 3 (R3-4 SUBJECT-IDENTITY check, §B.5 census 2)**: `:20`'s import block **closes at `:23` with `} from "../css-text";`** — the specifier is kf's own fork, **not** `@mkbabb/value.js`. The line existed; its provider was inherited, never measured. Bonus anchors: `:10` docstring *"SHORTHAND via value.js's `reverseAnimationShorthand`"*; `:104` `parseTimingFunction` call. | **CORRECTED** |
| `src/animation/compile/emit/backward.ts:20` | **PATH WRONG.** True path `src/animation/compile/emit/backward/backward.ts`; `:20` holds (*"value.js's OWN `reverseAnimationShorthand`"*). Second prose site: `:46` (*"statically imports `@mkbabb/value.js`"*); echoes at `:252`/`:285`. | **CORRECTED** |
| the boot-evaluated easing fence — census `registry.ts:43`, kf-CopyButton `easing-registry.ts:123-136`, the **draft's** "today's disk" guess `src/animation/compile/easing/easing-registry.ts` | **ALL THREE DEAD.** No `easing-registry.ts` exists at the frontier (easing tree = `index.ts`, `option.ts`, `registry.ts`). TRUE fence = `src/animation/compile/easing/registry.ts`: `DIRECT_NAMES` `:18-28` (carries `"smoothStep3"` `:26`, `"easeInBounce"` `:27`); `registryNames` `:30-34` with `:31 ...Object.keys(bezierPresets),` (the *addition-silently-widens* leg); the **module-top-level** `timingFunctionEntries` initializer `:37-48` (`registryNames.map` at `:40`) whose `:43-46` is `throw new Error(\`value.js rejected its own easing ${JSON.stringify(name)}: …\`)` (the *removal-is-a-boot-throw* leg). Boot-evaluation **CONFIRMED**: a top-level `const` initializer, not a lazy call. `parseTimingFunction` consumed at `:131`. | **RESOLVED — the single most consequential correction in this table.** The 8-curve delta lands *here*, at module load. |
| `package.json:69` `"@mkbabb/value.js": "4.0.0"` | line is **`:70`** at `origin/master`; value exact. `"@mkbabb/glass-ui": "7.0.0"` exact at `:77` (the KF.W0 §B-12 manifest-schism lock: origin/master pins what is installed; F-1's phantom-dependency headline holds only at stale HEAD). | **CORRECTED** |
| `orchestration-api.test.ts:142-148` (pathless in the bank) | full path **`test/orchestration/orchestration-api.test.ts`** — **all line anchors hold at origin/master**: `:142` the `it("BITE: …")`, `:143-144` the comment *"`easeOutCubic` / `bounceInEase` are real registry curves but map to NO CSS keyword"*, `:146` `expect(cssTwinFor("bounceInEase")).toBeUndefined();`, `:147` the in-file `not-a-real-easing` proof. Frontier-durable: the registry carries `"easeInBounce"` (`registry.ts:27`), so `:143`'s comment is false at the frontier. | **PATH PINNED**; witness durable |
| `CopyButton.vue:42` (the frontier's one-word EE-01 repair) | full path **`demo/components/CopyButton.vue`** (NOT under `instrument/`); `:42 timingFunction: "easeInBounce",` confirmed. | **PATH PINNED** |
| `scroll/grammar.ts:136-149` (`roundTripScrollCSS`) | true path **`src/animation/scroll/grammar.ts`** (not `compile/scroll/`); the oracle doc `:138-142` (*"`serialize(parse(s))` canonical-form-equal to `s`"*), `export function roundTripScrollCSS` at `:143`; publicly exported — **paths pinned at repair round 3 (PASS-3/KF-W3-CHECK D-4)**: **`src/animation/public.ts:109`** (`    roundTripScrollCSS,`) and **`src/animation/load-engine.ts:40`** (`    roundTripScrollCSS,`) / **`:95`** (`    roundTripScrollCSS: typeof roundTripScrollCSS;`). The bare basenames were **pathless and resolved only off tree-root**: `git ls-tree origin/master -- src/public.ts src/load-engine.ts` returns **nothing — neither file exists** — so *"`public.ts:109`"* named a path that is not in the tree, inside the very table whose law is that a path error sends a cure to a file that does not exist. All three line anchors hold at the corrected paths (re-read by this seat, 2026-08-28). | **CORRECTED (paths pinned at round 3)** |
| `test/fixtures/keyframes/**` = 14 `.css` + `manifest.json` | **CONFIRMED** at the frontier, all 14 names as listed. Manifest `_README` verbatim confirms the epsilon row is *"a value.js HANDOFF (the oklab parse↔serialize float drift), NOT a kf serializer channel-drop"*. | anchor-safe |
| `test/compile/compile-roundtrip.test.ts:426` | HOLDS as the fixture-arm comment block (`:424-428`), which self-describes as *"Authored GATE-FIRST: confirmed RED on the UNCURED tree BEFORE any cure."* | anchor-safe |
| "the 15 parser-facing test modules" | round 1 recorded *"all 15 confirmed present at origin/master by `git cat-file -e`"*; **plus `test/compile/roundtrip-fidelity.test.ts`** (named separately in the draft roster) = **16**, and a 17th sibling, `test/compile/frame-compiler.test.ts`, unrostered (**both re-confirmed present at `81a56990` by `git ls-tree` at repair round 2**). **But the fifteen were never written down anywhere in this spec**, so that round-1 confirmation is **not re-executable by any later seat** and is not carried as a measurement — it is recorded as an unreproducible inherited claim (R2-5). | denominator ask → **KF.W5 X-4** (counting-base reconciliation). No denominator in §Gates cites a module count — **and at repair round 2 the clause is STRUCK from G-KF3-3's subject set** (PASS-2/KF-W3-CHECK **D-01**): that gate's arm (c) is enumerated by path and re-derived by one command at execution, so the unsettled 15/16/17 count binds nothing here. |

### B.2 Owned files — library half (all under `/Users/mkbabb/Programming/keyframes.js/` unless marked value.js)

| file | access | why |
|---|---|---|
| `docs/tranches/X/keyframes/waves/KF-W3.md` *(in value.js: this spec)* | **modify** (was `create`; the file exists — banked `b6e09ed4`, and this row was stale at its own authoring instant) | the sole lawful site for the RC-P reciprocity sentence — X·P is forbidden by its own §4 bounds from writing here, and *"a reciprocity a seat authored on both ends is not a reciprocity"* |
| `docs/tranches/X/keyframes/evidence/W3/**` *(in value.js)* | create | the R1 CALL-MAP (G-KF3-7), the pasted RC-P evaluator output, the 14-fixture receipts with per-row value/kf attribution, the 8-curve delta table |
| `package.json` (`:70` only) | modify | the repin `4.0.0 → V`. Exact-pin; kf **is** a notified exact-pin consumer of the X-W9 cut |
| `src/animation/compile/emit/css-text.ts` | modify | `:41-56` the fork **deleted** (G-KF3-4); `:17` kf's `reverseAnimationShorthand` **deleted** (G-KF3-5); `:54`'s throw dies with the fork; the five in-file use sites (`:43 :48` recursive · `:66 :83 :89` callers) re-route. **The delete's consumer set is NOT the in-file five** — that is a line-count, and **LAW A** (RULINGS-3) forbids authorizing a delete on one: the census derived from the **import graph** at `origin/master` is pasted at **§B.5 census 1** and returns **fourteen importing modules** (8 `src/` · 1 `demo/` via the `@src` alias root · 5 `test/`) plus the five in-file uses. Every one of the fourteen is inside this wave's re-route obligation or must be named as out of it |
| `src/animation/compile/emit/format/options.ts` | modify | `:20/:81/:121` — the surviving DUAL-1 half; re-point and re-word `:10`/`:81` |
| `src/animation/compile/emit/backward/backward.ts` | modify | `:20`, `:46` (+ echoes `:252`/`:285`) — the prose half of DUAL-1, and **at repair round 3 the measured whole of it**: §B.5 census 2 shows these sentences name a provider (`@mkbabb/value.js`'s `reverseAnimationShorthand`) that **the packed 4.0.0 surface does not carry** — the sole implementation is kf's own. **KF-CE-41 law: the comment is part of the defect surface**, so neither discharge of G-KF3-5 is done while *"value.js's OWN"* stands. Two further prose sites join the carve for the same reason: `src/animation/index.ts:232` and `test/compile/compile-roundtrip.test.ts:23` (comment text only, assertions untouched) |
| ~~`src/animation/compile/emit/index.ts`~~ | ~~modify~~ → **STRUCK at repair round 4**, relocated to **§B.3 (read-only witness, library half)** | **The `modify` grant had no act.** RULINGS-4 §END KF-W3 **D-5** (PASS-4/KF-W3-CHECK **D-5**): the row's stated reason was *"the re-export surface after the fork deletion"*, and the barrel's surface is **not touched by either fork deletion** — re-measured at write time, repair round 4: `git show origin/master:src/animation/compile/emit/index.ts \| grep -nE 'serializeCssValue\|reverseAnimationShorthand\|css-text'` → **no output (exit 1)**. The barrel re-exports `compileToCSS` · `DEFAULT_DELTA_E_EPSILON` · `DEFAULT_DENSIFY_STOPS` · `compileChild` · `CSSKeyframesToString`/`CSSKeyframesToStrings`/`formatCSSKeyframeString`/`declaredKeyframeBodyFor` · `serializeEasing` · `cssIdent` · `colorUnitToOklabCSS` · `isColorUnit` (+ types) — **neither forked symbol among them**. Its one remaining subject (the `cssIdent` quantifier) is an ask **DECLARED to KF.W5, not cured here**, and its `:26` docstring *"HEAVY (value.js-bearing) — reached only via `loadAnimationEngine()`"* stays **TRUE under both G-KF3-5 discharges** (the barrel's value.js bearing runs through `./backward`, not through the fork), so no concrete edit is forced by anything this wave books. **A `modify` row with no act widens the write surface of a gated wave** — struck, and the file is carried as a named read-only witness instead |
| `src/animation/compile/easing/registry.ts` | modify (guarded) | `:31` + `:37-48` — the 8-curve delta's landing site. **NO cure without OP-6**; this wave's touch is limited to whatever the delta forces, and any easing-*name* change is KF.W4's |
| `test/fixtures/keyframes/manifest.json` | modify-append | the divergence row for any fixture whose expected text changes at the repin — **dated, never a silent re-baseline** (G-KF3-4 falsifier) |
| `test/compile/roundtrip-fidelity.test.ts` · `test/compile/compile-roundtrip.test.ts` · the 15 parser-facing modules | modify | attribution only: each RED classified value-side or kf-side. **Assertions are not relaxed to obtain green** |
| `docs/tranches/V/coordination/INBOX.md` *(in value.js)* | modify-append | the two E13 discharge rows: O-11 §B (fork retired) and O-11 §D2 (8-curve delta acknowledged) — G-KF3-4, G-KF3-6 |

**EXECUTE-NO-WRITE (R-E, run unmodified):** `docs/tranches/V/megatranche/audit/probes/r1-published-totality.mjs` — confirmed on disk by this seat (3476 B), header self-states *"runs against the PACKED ARTIFACT, not the source tree (L-12) … RED today: 324 throws / 1548 calls, all one failure mode, all from src/css/grammar.ts:181."*

### B.3 Read-only witness sites (named, never opened for writing by this wave)

The census's lane-library §4.6 five-site roster **is wrong in two of five at the frontier.** Measured by this seat: `git grep -n "parseCssColor\|parseCssScalar\|parseCssValues" origin/master -- demo/` → **exactly six hits: three imports and three calls.**

| site | measured | routing |
|---|---|---|
| `demo/scenes/square/useSquareTumble.ts:2` (import) `:22` (call, `parseCssColor`) `:23` (the `!ok` **throw** arm) | HOLDS; the `:17` `if (value)` empty-skip guard confirmed; `:21-25` re-read at `origin/master` by the repair seat — `:23 if (!parsed.ok) throw new TypeError(\`Invalid square palette color: ${css}\`);` | **PAIR-READ, per RULINGS R-7a.** K-6 kills exactly one claim — **R1-through-a-token** (`getPropertyValue` → `""` → the `:17` guard skips): §Carry **C-4** governs that path and it stays unassertable. The **`!ok` throw arm at `:23`** is a *kf-authored* crash surface independent of R1, its reachability ESTABLISHED by §Carry **C-5** (`colorAt` ×3 inside the rAF frame) and by kf-EditorShell **C-19** (§Excluded E-15: RR-2 reproduced R1 live against installed 4.0.0): §Carry **C-5** governs it and it **MAY be asserted RED** → G-KF3-7 |
| `demo/scenes/square/useSquareDemo.ts:4` (import) `:82` (call, `parseCssScalar`) | HOLDS | unclassified → G-KF3-7 |
| `demo/components/instrument/keyframes/KeyframesEditor.vue:123` (**import**) `:186` (**call**) | HOLDS — and the census's `:123` anchor is the **import line**. KF-ET-32's link≠call law applies to the census's own citation. | → G-KF3-7 |
| `demo/utils/keyframeSelector.ts:15` — census files this under the colour/scalar channel | **MIS-CLASSIFIED.** It is `parseKeyframeSelector` (import block `:1-5`, the name at `:2`), and `requireKeyframeSelector` `:14-21` converts a typed `!ok` into `throw new TypeError` at `:18`. Two live callers: `useKeyframeOps.ts:174`, `timelineEngine.ts:83`; `test/demo/instrument/value4-editor-boundary.test.ts:28` asserts the throw (confirmed at origin/master). | a **kf-authored** throw-on-typed-failure, not a value.js throw → **posture is KF.W2's**; reachability recorded here as a G-KF3-7 row |
| `demo/utils/reference-data/animationDescriptions.ts:76` — census files this under the colour/scalar channel | **WRONG ORGAN.** It is `parseTimingFunction` (call `:76`, import `:128`). | **This is KF-CB-33's prediction firing a second time, measured, before the wave opens**: the census's own colour/scalar roster already contains an easing/css-organ site. → G-KF3-7 channel (ii) |
| `demo/components/instrument/keyframes/CSSCodeEditor.vue` — *"the fuzz entry of record"* (KF-CE-12) | the `:116` anchor at the frontier lands inside the `debouncedEmit = debounce(` block (`:114`, the KF-CE-16 TS2554 exhibit vicinity), **not** the ingress. | anchor **UNRESOLVED** → re-resolve before the map is built; the fuzz-entry note itself rides KF.W2/KF.W3 jointly and is **never re-booked** |
| `demo/components/instrument/keyframes/KeyframesStringControls.vue:123-125` — sole mount writer, no `try` | **HOLDS exactly**: `onMounted(async () => { await updateCSSAnimationKeyframesStringFromAnimation(); })` | chain ends at `css-text.ts:54` → dies with G-KF3-4 |
| the KeyframeTimeline inline editor (`demo/components/instrument/timeline/KeyframeTimeline.vue`; rider C-7) | *"a live untrusted-CSS ingress to the R1 crash shape … CONFIRMED BY THIS SEAT'S EXECUTION"* (banked) | reachability here; **the silent `console.error` posture books to KF.W7**, not here |
| **LIBRARY HALF — `src/animation/compile/emit/index.ts`** *(relocated from §B.2 at repair round 4, RULINGS-4 §END KF-W3 **D-5**)* | **READ-ONLY.** Re-measured at write time, repair round 4: `git show origin/master:src/animation/compile/emit/index.ts \| grep -nE 'serializeCssValue\|reverseAnimationShorthand\|css-text'` → **no output (exit 1)** — the barrel re-exports **neither** forked symbol, so neither G-KF3-4's nor G-KF3-5's delete forces an edit here. `:53 export { cssIdent } from "./backward";` is **one of TWO** re-export sites (sibling `emit/backward/index.ts:29 export { cssIdent } from "./walk";`, definition `backward/walk.ts:145`; consumers `view-transition.ts:47/:305/:330/:333` + in-file `walk.ts:89/:110/:131`) — full census at **§B.5 census 3**, quantifier corrected at repair round 3 (PASS-3/KF-W3-CHECK D-3). `:26` reads *"HEAVY (value.js-bearing) — reached only via `loadAnimationEngine()`"* and is **true under both G-KF3-5 discharges** | **witness only — no write grant.** The `cssIdent` re-route ask is **DECLARED to KF.W5** (cross-edge below); its *conclusion* survives the quantifier correction — `git grep -c '\bcssIdent\b' origin/master -- src/animation/public.ts src/animation/load-engine.ts src/animation/index.ts` → **0 hits in all three**, so neither `kfEngine()` nor `loadAnimationEngine()` reaches it. **This wave re-routes neither site** |

**Easing/css organ, measured (channel (ii) seed set):** `parseTimingFunction` call sites = `easing/registry.ts:131`, `emit/format/options.ts:104`, `animationDescriptions.ts:76`. `bezierPresets` consumers = `registry.ts:4/:31`, `TimingFunctionPanel.vue:52/:97/:98/:127`, `EasingSidebar.vue:77/:112/:155`. That is the 8-curve delta's measured blast radius.

### B.4 Explicitly out of bounds

- value.js `src/css/**`, and specifically `src/css/grammar.ts:181` (measured: `const components = splitTopLevel(slash[0]!.replace(/,/g, " "), "space");` — the non-null assertion defeating `noUncheckedIndexedAccess`). **X·V owns its bytes; PLAW-BIND; CC-084 → X-W9; ruled "no emergency 4.0.1."**
- The F-5 shim/barrel deletion family → **KF.W6** (§Excluded E-1).
- The easing-name hygiene commit (KF-CB-18/24/29) + the gate chassis (KF-CB-8, KF-CE-16, KF-APP-4) → **KF.W4**.
- All glass-producer rows → **SS-6 BH relay**, never a demo-side hack.

### B.5 LAW A import-graph censuses — every delete / repoint / shim act this wave books or carries

**Added at repair round 3 under RULINGS-3 LAW A** (*"a delete/repoint/shim act's consumer census is derived from the import graph at the frontier, never from line counts, never from the wave tree's own text"*), which binds every seat that **writes, carries, or leaves standing** such an act. All five censuses were executed by this seat at keyframes.js `origin/master` **`81a56990`** on 2026-08-28 (`git rev-parse origin/master` → `81a56990736ced5b5edde0b84c527680ac7689b1`, exact) and are pasted with their outputs. **Specifier alphabet measured, not assumed**: the alias roots that can reach `src/` are `@src/*` (tsconfig `paths` + vite `alias`) and the self-alias `@mkbabb/keyframes.js` → `src/animation/index.ts` (the LIGHT barrel, which re-exports none of `compile/emit/css-text` — 0 hits); demo-side roots `@components` / `@state` / `@styles` cannot reach `src/`. Prose/doc hits are recorded as **non-import context** and never counted as consumers.

**Census 1 · `serializeCssValue` @ `src/animation/compile/emit/css-text.ts:41` — the G-KF3-4 DELETE + re-route.**
```
git grep -nF 'emit/css-text'     origin/master -- .   # long-tail + alias spellings
git grep -nE 'from "\.{1,2}/css-text"' origin/master -- .   # every relative-depth spelling
git grep -n  '\bserializeCssValue\b' origin/master -- src/ demo/ test/ scripts/   # symbol census
```
→ **14 importing modules**, each resolved to its own specifier and compared against `P`:
**src/ (8)** `compile/emit/densify.ts:13` (`./css-text`) · `compile/emit/entry.ts:37` (`./css-text`) · `compile/emit/format/format.ts:1` (`../css-text`) · `compile/frame/interp-slot.ts:19` (`../emit/css-text`) · `engine/css/metadata.ts:33-35` (`../../compile/emit/css-text`) · `resolve/browser.ts:4` · `resolve/conditional.ts:2` · `resolve/function.ts:6` (all `../compile/emit/css-text`).
**demo/ (1)** `demo/components/instrument/timeline/utils/timelineEngine.ts:17` — **`import { serializeCssValue } from "@src/animation/compile/emit/css-text";`**, called at `:88`. A demo module reaching **library-private `src/`** through the alias root: it is invisible to any `src/`-scoped grep, and it is the reason LAW A names alias roots explicitly. *(Same file, different symbol, as §B.3 row 4's `timelineEngine.ts:83` `parseKeyframeSelector` caller.)*
**test/ (5)** `test/compile/structural-emit.test.ts:10` · `test/resolve/emerging-css-resolve-fn.test.ts:31` · `test/resolve/emerging-css-resolve-now.test.ts:23` · `test/resolve/emerging-css-resolve-p2.test.ts:26` · `test/resolve/value4-immutable-resolve.test.ts:9` — **all five are members of G-KF3-3 arm (c)'s derived roster**, so the delete and the regression net meet on the same files.
Plus the **five in-file uses** (`:43 :48` recursive · `:66 :83 :89` callers) already recorded at §B.1. **The in-file five were never the consumer set**; the round-1/round-2 text that re-routed "five use sites" was a line-count reading of a delete act.

**Census 1b · the ROUTING TARGET, measured at the consumed coordinate (the half a delete census is worthless without).**
```
node --input-type=module -e "for (const s of ['color','value','css','easing','math','transform','quantize']) {
  const m = await import('<kf>/node_modules/@mkbabb/value.js/dist/subpaths/'+s+'.js');
  console.log(s, Object.keys(m).length, Object.keys(m).filter(n=>/serializeCssValue|reverseAnimationShorthand/.test(n))); }"
```
→ installed **4.0.0** (`node_modules/@mkbabb/value.js/package.json` → `4.0.0`), the pinned coordinate, **packed artifact per L-12**: `color 23 · value 1 · css 19 · easing 16 · math 9 · transform 9 · quantize 2` = **79 exports across all seven subpaths, and `serializeCssValue` is on NONE of them**; the `./css` `serialize*` surface is exactly `serializeCssColor` · `serializeTimelineOptions`. Corroborated source-side: value.js defines `serializeCssValue` at `src/css/stylesheet.ts:81` but **`src/css/index.ts` does not re-export it** (`grep -n 'serializeCssValue' src/css/index.ts` → 0). **Consequence, booked at G-KF3-4**: *"route the callers to value's published `./css` export"* names an export that **does not exist at 4.0.0** — the gate's GREEN is unreachable unless coordinate `V` publishes it, so the publication is declared as an inbound ask rather than assumed.

**Census 2 · `reverseAnimationShorthand` @ `src/animation/compile/emit/css-text.ts:17` — the G-KF3-5 DELETE.**
```
git grep -n '\breverseAnimationShorthand\b' origin/master -- src/ demo/ test/ scripts/
git show origin/master:src/animation/compile/emit/format/options.ts | sed -n '19,23p'
```
→ **exactly ONE live import specifier in the whole tree**: `src/animation/compile/emit/format/options.ts` `:20` — and the block it sits in **closes at `:23` with `} from "../css-text";`**, i.e. it imports **kf's OWN fork**, not value.js's. Call site `:121`. Everything else is **prose**: `backward/backward.ts:20 :46 :252 :285` · `format/options.ts:10 :81` · `src/animation/index.ts:232` · `test/compile/compile-roundtrip.test.ts:23`.

**THE PROSE SURFACE, ENUMERATED AND PARTITIONED — the count lives HERE, with its counting rule, and no dependent cell re-issues the numeral** *(added at repair round 4: RULINGS-4 §END KF-W3 **D-4** / LAW D(3); PASS-4/KF-W3-CHECK **D-4** convicted the bounds surface (8) and the gate denominator (6) of disagreeing by two inside one file).* **Counting rule**: a site is **PROVIDER-NAMING** iff its sentence names a *provider* for `reverseAnimationShorthand` — the predicate G-KF3-5's green is written over — and **PROVIDER-SILENT** iff it names the symbol without attributing it. Each line below was re-read at write time, repair round 4, by `git show origin/master:<path> | sed -n '<n>p'`:
```
backward/backward.ts:20   * The per-child `animation` shorthand is value.js's OWN `reverseAnimationShorthand`
backward/backward.ts:46   * This module statically imports `@mkbabb/value.js` (`reverseAnimationShorthand`
format/options.ts:10      * SHORTHAND via value.js's `reverseAnimationShorthand` ({@link animationShorthand}),
format/options.ts:81      * `reverseAnimationShorthand` (the published 0.12.0 inverse of the
format/options.ts:80      * K.W10 CC-1 — the per-child `animation` SHORTHAND emit via value.js's      ← :81's sentence opens here
src/animation/index.ts:232  // CSS. HEAVY (compile.ts statically imports value.js's reverseAnimationShorthand
backward/backward.ts:252     // projection + the animation shorthand (reverseAnimationShorthand).
backward/backward.ts:285     // express). `reverseAnimationShorthand` already emits `options.delay`; the
test/compile/compile-roundtrip.test.ts:23   * (`reverseAnimationShorthand` unconsumed). The behaviour body rides the real
```
**EIGHT prose sites; FIVE provider-naming** (`backward.ts:20` · `backward.ts:46` · `options.ts:10` · `options.ts:81`, whose sentence opens at `:80` with *"via value.js's"* · `index.ts:232`) **and THREE provider-silent** (`backward.ts:252` · `backward.ts:285` · `compile-roundtrip.test.ts:23` — each names the symbol and attributes it to no one; `:23`'s clause is a born-RED provenance note, *"before this wave no `compileToCSS` existed"*). **All eight stay in §B.2's carve** under **KF-CE-41** (the comment is part of the defect surface, and a retirement leaves no sentence describing a twin that no longer exists) — **but only the five bear on G-KF3-5's predicate**, which is what the struck *"six"* mis-stated in both directions at once: it was two short of the carve and one long of the predicate. The gate below therefore **states no numeral and points here** (LAW D(3): a count is stated at its enumeration; dependent cells reference the enumeration). **Zero modules import the symbol from `@mkbabb/value.js`** (`git grep -n '@mkbabb/value.js' origin/master -- src/animation/compile/emit/` returns type/`serializeCssColor`/`convertColor` imports only), and **Census 1b's enumeration shows value.js publishes no `reverseAnimationShorthand` on any subpath at 4.0.0** — nor does its source define one (`grep -rn 'reverseAnimationShorthand' src/ --include='*.ts'` in value.js → **0**; corroborated by the megatranche harvest record `registry/harvest/wf_076062ab-dc8.json:412`, which measured the sibling `sampleColorRamp`/`deltaEOK` prose the same way and found the same shape). **This is a SUBJECT-IDENTITY mis-resolution of the R3-4 class** — the symbol name matched, the **provider** did not — and it is corrected at G-KF3-5 below.

**Census 3 · `cssIdent` — the re-export surface (PASS-3/KF-W3-CHECK D-3).**
```
git grep -n '\bcssIdent\b' origin/master -- src/ demo/ test/ scripts/
```
→ **two** re-export sites — `compile/emit/index.ts:53` (`from "./backward"`) **and** `compile/emit/backward/index.ts:29` (`from "./walk"`) — definition `backward/walk.ts:145`; consumers `emit/view-transition.ts:47` (import) `:305 :330 :333` (calls) + in-file `walk.ts:89 :110 :131`; comments `backward.ts:157`, `walk.ts:9` (non-import context). **Zero** hits under `demo/`, `test/`, `scripts/`.

**Census 4 · the F-5 shim `demo/components/instrument/transport/composables/useAnimationGroupPlayback.ts` — CARRIED OUT to KF.W6 at §Excluded E-1, censused here because LAW A binds the carrier too.**
```
git grep -nF 'transport/composables/useAnimationGroupPlayback' origin/master -- .
git grep -n  '\buseAnimationGroupPlayback\b' origin/master -- demo/ test/ src/ scripts/
```
→ the file is a one-line re-export (`:1 export { useAnimationGroupPlayback } from "../AnimationControlsGroup/useAnimationGroupPlayback";`). **Specifier census: exactly ONE live import — `test/demo/state/no-shadow-playback-authority.test.ts:21`** (three further hits are keyframes' own docs prose at `U/audit/lane-20…:116`, `V/audit/R1-05…:87`, `V/audit/R2-06…:34/:126` — non-import context). **Symbol census resolved**: `AnimationControlsGroup.vue:137` and `test/demo/instrument/useAnimationGroupPlayback.test.ts:5` both import the **REAL** module (`…/transport/AnimationControlsGroup/useAnimationGroupPlayback`) — **neither is a shim consumer**. This reproduces **RULINGS-3 R3-1** independently and at the same substrate. **Two carrier-side facts recorded, no act re-homed** (the delete + repoint is KF.W6's, R3-1): (i) E-1 rider (a)'s banked words — *"the test's `:21` import string traverses `composables/useAnimationGroupPlayback`"* — are **true at the frontier** and are quoted unchanged; (ii) the in-tree authority that names the repoint target, `keyframes.js docs/tranches/V/audit/R2-06-demo-target-tree.md:126`, spells it `…/transport/animation-controls-group/useAnimationGroupPlayback` — **that directory casing does not exist**: `git ls-tree -r --name-only origin/master -- demo/components/instrument/transport/` returns `AnimationControlsGroup/…` (PascalCase). A seat repointing from that doc's spelling on a case-sensitive checkout gets an unresolved import. Flagged to KF.W6; not fixed here.

**Census 5 · the sibling shim `demo/components/instrument/transport/composables/useKfPillTabs.ts` — same carry, same law.**
```
git grep -nF 'transport/composables/useKfPillTabs' origin/master -- .        # long tail: 0 imports
git grep -n  'composables/useKfPillTabs'          origin/master -- demo/ test/   # relative-depth spelling
git grep -n  '\buseKfPillTabs\b'                  origin/master -- demo/ test/ src/ scripts/
```
→ **the long-tail pattern alone returns ZERO imports** (two docs-prose hits only) — **the sole live consumer is reached only by the relative-depth spelling**: `demo/components/instrument/transport/channel-controls/ChannelControls.vue:230` — `import type { KfPillTabOption } from "../composables/useKfPillTabs";`. This is LAW A's *"repeated for every relative-depth spelling"* clause earning its keep: the tail-only census would have declared this shim dead. It **confirms the bank** (kf-ChannelControls L-15/C-10, *"sole consumer `ChannelControls.vue:230`"*) by the import graph rather than by inheritance. Repoint target verified present: `demo/components/instrument/transport/KfPillTabs/useKfPillTabs.ts:37`, reached from `channel-controls/` as `../KfPillTabs/useKfPillTabs` — exactly E-1 rider (b)'s banked instruction (*"NOT at the `.vue`"*), and again **not** the lowercase `kf-pill-tabs/` spelling `R2-06-demo-target-tree.md:127` proposes, which does not exist. Symbol census: `KfPillTabs.vue:43/:44` and `test/demo/instrument/KfPillTabs.test.ts:28` import the **real** module (`./KfPillTabs/useKfPillTabs`, `@components/instrument/transport/KfPillTabs/useKfPillTabs`) — **not shim consumers**.

### B.6 Scope receipts — the round-4 block (RULINGS-4 **R4-10(1)** — a sweep's enumeration source is the SCOPE, never the example list; one line per class) **and the round-5 block below** (RULINGS-5 **R5-11 · R5-13 · LAW E · LAW F**)

- **LAW A · enumeration source = the §Bounds ACCESS COLUMN.** Enumerated at write time, repair round 4: §B.2 carries **eleven** rows — ten live plus the one **struck** at D-5. This wave's access column spells every library act `modify`, so the delete/repoint/shim/move acts are enumerated from the rows' *subjects*: `css-text.ts` books **two deletes** (`:41-56` the `serializeCssValue` fork · `:17` `reverseAnimationShorthand`), and §Excluded **E-1** carries **two shim deletes + two repoints** out to KF.W6. **Censuses present: 5 of 5** — census 1 (delete 1: fourteen importing modules) · census 1b (that delete's *routing target*, at the consumed coordinate) · census 2 (delete 2: one live importer + the eight-site prose partition) · census 3 (`cssIdent`, the surface this wave re-routes **not at all**) · censuses 4 + 5 (the two carried shims — LAW A binds the carrier). **No §Bounds row carries a delete, repoint, shim or move act without its census on this page**, and the one row that carried a write grant with **no** act at all is struck (D-5).
- **SUBJECT-IDENTITY · enumeration source = every symbol-bearing bounds row and every gate-witness symbol, whole file.** Re-resolved this round rather than only where a ruling pointed: `emit/index.ts`'s **entire export surface** was re-read by command (neither forked symbol on it — the D-5 relocation rests on that, not on the row's prose); G-KF3-5's witness sentences were re-read line by line and partitioned **by provider**, which is the identity the R3-4 class turns on and the axis the struck *"six"* blurred; G-KF3-4's `serializeCssValue`, G-KF3-6's `bezierPresets` → `timingFunctionEntries` fence and G-KF3-8's two phantom easing names keep their round-3 file+symbol anchors — re-checked, unmoved, and so not re-cut.
- **NUMERIC ARM · LAW D(3).** Every figure printed beside a command in this repair was produced by **re-running that command at write time** (2026-08-28): corpus size **58** · payload sweep **41** records · markup union **26** case-insensitive / **23** case-sensitive, with the case rule declared · the five discarded-by-reading records and their in-spec counts *(the figure printed here at round 4 — ~~**0 · 0 · 2 · 0 · 0**~~ — was **FALSIFIED at this file's own bytes in all five positions** and is **STRUCK**: PASS-5/KF-W3-CHECK **D-1**, RULINGS-5 §END KF-W3 **D-1**. The corrected figure, its counting rule, its command and the strike's SHADOW live **once**, at E-15's **(R-i)** reading step; this receipt cites that enumeration and **re-issues no numeral**, which is the discipline the rest of this bullet already states and this one item had broken)* · the six negatives' **0** on all five markups and their banked bytes by `sed -n` · the prose partition **5 provider-naming / 3 provider-silent** · `emit/index.ts`'s export surface. **Counts asserted over an enumeration are stated AT that enumeration with the counting rule** (E-15's arithmetic paragraph · §B.5 census 2's partition); every dependent cell points at the enumeration and re-issues no numeral.
- **LAW C · stage discipline.** This seat wrote in **STAGE 1**. Every cross-wave receipt to a stage-2 sibling is **anchor-only** (LAW C(3)) — §Cross-edges' new KF.W10 row · E-14.5's `KF-W10 §6.C-I` anchor · E-8's `KF-W10 §6.D` anchor — and, measured at write time, **this file cites no sibling spec's line numbers and quotes no sibling spec's prose anywhere**: `grep -o 'KF-W[0-9]*\.md[:0-9]*'` returns only self-references to `KF-W3.md`. The RECONCILE seat verifies all three after the last per-wave write (LAW C(4)).

**THE ROUND-5 BLOCK** *(repair round 5, 2026-08-28 — `PASS-5/KF-W3-CHECK.md` **D-1..D-5** and RULINGS-5 §END KF-W3; this seat writes in **STAGE 1**, as it did at round 4)*

- **R5-11 · RECEIPT-ARITHMETIC-FIRST — the class round 4's instrument was structurally unable to see.** Every receipt printing a figure now carries its **enumeration**, its **counting rule** and its **command** AT the receipt; where a figure appeared twice, it is now **stated once at the enumeration and cited at the mirror**. The round-5 sweep's enumeration source is **this file's own bytes** — CLOSE-CERT-4's declared sweep scope was same-round **cross-wave** receipts, so a receipt a spec takes over *itself* fell outside it, which is exactly where this file failed (PASS-5 **D-1**: the (R-i) five-position figure, wrong in all five positions, under a *"measured at write time"* label). Cured at E-15's **(R-i)** reading step with the re-run figure, the rule, the command and the strike's **SHADOW**; the **§B.6 NUMERIC-ARM mirror above is re-cut in the same edit** to cite that enumeration and re-issue no numeral. **Standing rule for this file from here**: a figure over this file's own bytes is re-run at the seat's last edit or it is not printed.
- **R5-9 row 6 / PASS-5 D-3 · ARTIFACT OF RECORD — one claim, one artifact, swept by SCOPE.** *Enumeration source*: the greppable class, `grep -on 'PASS-[0-9]/KF-W3-CHECK\.md' KF-W3.md` — **never** the sites a ruling names (R4-10(1); the round-4 re-point moved three sites and left three, which is how one claim came to have two artifacts of record a hundred lines apart). *Discriminator, stated once and applied to the whole class*: **(i) ARTIFACT-OF-RECORD cites** — those sourcing a LIVE claim this file declines to assert in its own voice under LAW B (carriage **24 / 24 / 0** · the SHIM cardinality **5 rows / 4 records** · E-15's denominator completeness · the markup case split) — **all name `PASS-5/KF-W3-CHECK.md`, the one artifact**; **(ii) PREDECESSOR mentions** (*superseded, not deleted*) and **(iii) DEFECT-PROVENANCE cites** naming the pass that filed a numbered defect or computed a dated figure (`PASS-2 D-03` · `PASS-3 D-1/D-2/D-5` · `PASS-4 D-1..D-6` · `PASS-5 D-1..D-5`) — **never re-pointed**, because re-pointing them would falsify the history they record. *Census at this seat's last edit* (counting rule: one hit per pattern match, `grep -on`, multiple hits per line counted separately): **16 hits on 8 lines — 9 `PASS-5` · 5 `PASS-3` · 2 `PASS-4`** (the round-5 block's own two `PASS-5` cites are inside the census, as the rule requires). *Acts this round*: **six cites re-pointed** — §Provenance's LAW-B closure claim · §Carry's denominator source · §Carry's carriage claim · §Carry **C-6**'s SHIM cardinality · **G-KF3-7**'s denominator completeness · **E-15**'s arithmetic carriage sentence — **plus one new** artifact-of-record cite at (R-i)'s agreement note, which sources the round-5 case-split re-derivation. **SHADOW** (LAW F): re-pointed set = those six, each named above with its own site; disposition per site = **citation moved, claim never re-asserted, figure unchanged** (all three passes derived **24 / 24 / 0** independently and by record, so the re-point moves no number); struck alongside them = the superlative *"the freshest artifact … without inheriting any prior pass's denominator"*, which PASS-4 and PASS-5 both falsify at their own heads and which is **not** restated; citing/terminus waves notified = **none required** — no sibling spec cites this file's artifact of record, and the single `KF-W3-CHECK` string outside this file sits inside **KF.W10's INBOUND row for `E-14.5`**, where it rides a quotation of this file's E-14.5 **header** — a defect-provenance cite of class (iii), left untouched this round precisely so that sibling's quotation stays true.
- **LAW E / PASS-5 D-2 · THE CLOSE CERTIFICATE OF RECORD.** `PASS-4/CLOSE-CERT.md` §1 certified itself the round's last write; it is **falsified at the bytes** — one spec moved **22 s after** the certification, `+21 B` over the size it certified, and RULINGS-5 **R5-2** attributes that twelfth write to **the round-4 RECONCILE seat itself** (a receipt-level rewording of its own dated insertion — lawful in scope, benign in content, fatal to a certificate of ORDER). **RULED there and recorded here**: round 4's repairs **BANK**; its certificate is **VOID as proof-of-order**; the lawful close of rounds **4 AND 5** is **`PASS-5/CLOSE-CERT-2.md` (CLOSE-CERT-5)**, hash-proven under **LAW E(2)** and re-hashed by the pass-6 union seat as its first act. **This file's exposure is nil, and the reason is the idiom, not luck**: all three of its forward receipts are **anchor-only under LAW C(3)** — no bytes, no line numbers, nothing a sibling's write can falsify — and the pass-5 seat re-resolved all three at `KF.W10`'s *current* bytes, both ends of the seam agreeing. Accordingly, **no closure claim in this file is conditioned on CLOSE-CERT-4**; the verification of record for this file's cross-wave receipts is **CLOSE-CERT-5 + RULINGS-5 R5-2**, and per **LAW E(4)** every same-round cross-wave receipt here stays **anchor-only, with no per-seat substrate stamp** — a seat that needs a sibling's words asks the cert for them.
- **R5-13 / PASS-5 D-5 · LAW D(1) EMPHASIS — disclosed once, for the whole file.** R5-13 rules that markdown emphasis added inside a quotation labelled *verbatim* is an **ALTERATION** unless disclosed at the quote, and that undisclosed emphasis reclassifies the quote as a **paraphrase**. **Disclosure, hereby, over the class**: every quotation in this file labelled *verbatim* is verbatim **as to words, their order and their meaning**, and may carry **added (or, in one case, dropped) markdown emphasis and code spans** for readability; where a quotation elides, **the ellipsis is present and the elided clause is named at the quote**. *Measured basis, not asserted*: at `kf-HeroAurora.md:66` the banked payload cell carries **zero** bold markers (`sed -n '66p' … | cut -d'|' -f4 | grep -o '\*\*' | wc -l` → **0**) against this file's two added bold spans and two added code spans, itemised in full at **E-14.5**; at `kf-SpringHeatmap.md:101` the bank's own bold span is **preserved** and a second is **added** over the closing clause; at `kf-HeroAurora.md:125` the bank's bold is **dropped**. **No word is altered at any site this round's instrument tested** — the pass-5 seat independently verified the six sub-line-II banked quotations at their cited lines, KF-AV-28's rider bytes and X-08's mid-line quotation (`PASS-5/KF-W3-CHECK` §4) — and **completeness of that verification is not certified in this file's voice** (LAW B): the disclosure governs the class, the instrument measures it, and R5-13 commissions no retro-sweep beyond it.
- **LAW F · THE ROUND'S SHADOW LEDGER (F(2)) — the struck/minted set, enumerated in one place.** Struck this round: **(1)** the (R-i) five-position figure `0 · 0 · 2 · 0 · 0` ⟨E-15 (R-i)⟩ and **(2)** its §B.6 NUMERIC-ARM mirror ⟨above⟩ — both replaced by the re-run figure at (1)'s enumeration; **(3)** the fold-identity provenance range *"C-1..C-5"* ⟨§Provenance⟩ — replaced by the per-row enumeration, with **C-4 and C-5 keeping their §Carry rows, verbs, severities and riders untouched**; **(4)** the *"freshest artifact … without inheriting"* superlative ⟨§Carry⟩ — not restated. Minted this round: **nothing** — no row, no id, no routing, no gate, no bound. **Nothing in this file MOVES a row**: the routed **24** and its `9 + 5 + 10`, E-15's `26 + KF-HA-19 = 27` denominator, the five `KF.W3-SHIM` rows, every §Bounds row and access word, every gate's RED baseline and every cross-edge verb are **unchanged in substance from their round-4 state** — this round's whole write-set is receipts, citations and disclosures. Termini and citing siblings re-swept for every struck id: **`grep -l 'C-1\.\.C-5' waves/KF-W*.md`** → this file only; the artifact-of-record class is cited by no sibling; the one external `KF-W3-CHECK` string is the class-(iii) cite inside **KF.W10's INBOUND row for `E-14.5`**, unaffected by every act above. **Notified: none required.**

---

## §Carry — the agglomerated rows

Deduped by identity. Severity is the **bank's**, never re-graded at a fold. Rows folded *by reference* are named and **never re-booked as new rows**. **Denominator (RULINGS R-12): the registry's own routed census — 19 canonical routed ids, 24 with the SHIM family — never a CARRY ledger.** *(Source of record **re-pointed at repair round 5** — LAW B, and by SCOPE this time, RULINGS-5 §END KF-W3 **D-3** / R5-9 row 6: the figure was first taken from the PASS-1 census, then homed at `PASS-3/KF-W3-CHECK.md` at round 3 while three other sites in this file moved on to PASS-4, leaving **one claim with two artifacts of record a hundred lines apart**. The **one** artifact for this claim, at this site and at every other, is `docs/tranches/X/keyframes/conformance/PASS-5/KF-W3-CHECK.md` §1 (2026-08-28), which re-enumerated **19 canonical + 5 SHIM = 24 id-for-id** from the 58 records' bytes. The struck superlative — *"the freshest artifact that derives it … **without inheriting any prior pass's denominator**"* — is **not restated**: PASS-4 and PASS-5 each declare the same independence at their own heads, so the phrase distinguished nothing and only pinned the citation to the pass that first earned it. Later passes re-point this citation **in one sweep over the greppable class** (`grep -on 'PASS-[0-9]/KF-W3-CHECK\.md' KF-W3.md`, discriminator at §B.6's round-5 block); the spec does not re-assert the number.)* This table carries **9** of them as fold-identities — **id for id, so the count can be checked and not merely asserted**: **C-1**'s own kf-AmigaScene **S+2** ownership row **(1)** + its riders **(a) KF-CE-12 · (b) KeyframeTimeline C-7 · (c) KeyframesStringControls C-2 · (d) KeyframeCardList Axis-C S-C (4)**; **C-2**'s **KF-CB-33 ⊃ KF-SST-36 / KF-KC-53** fold **(3)**; **C-3**'s **KF-ET-32** KF.W3 leg **(1)** — **1 + 4 + 3 + 1 = 9**. **C-4 (K-6) and C-5 (D-27/L-7/C-9) are carried rows that are NOT members of the routed 19** (corrected at repair round 1: the earlier parenthetical listed them inside the nine and the arithmetic did not close) — neither is routed to this wave by a dotted `KF.W2/W3` or parser-lane form; C-4 is a **kill-scope bounds correction** and C-5 a **witness**, both carried because the gates need them, and both outside the census arithmetic. **5** are disposed at §Excluded **E-1**; **10** are booked at §Excluded **E-15** as exclusions-with-named-owner (**KF.W2**) and enter G-KF3-7's map denominator. **9 + 5 + 10 = 24.** **The membership law that makes that 24 auditable is stated once, at §Excluded E-15's preamble (two admission clauses, each with its command), and re-cut at repair round 3 (PASS-3/KF-W3-CHECK D-1) because the round-2 form contradicted itself; this table consumes it and does not restate it.** Carriage itself is **not claimed in this file's voice** (LAW B): it is measured at `docs/tranches/X/keyframes/conformance/PASS-5/KF-W3-CHECK.md` §1/§2 (2026-08-28) — **24 routed, 24 booked id-for-id, 0 escaped** — the same figures the PASS-3 and PASS-4 seats each derived independently and by record. *(Re-pointed at repair round 5 to the one artifact of record, RULINGS-5 §END KF-W3 **D-3**; the predecessor findings this table's cures answer keep their pass prefixes and are cited by them — **PASS-3 D-1** against the membership law, cured at E-15's preamble; **PASS-4 D-1** against the admission rule's corpus scope, cured at E-15's sub-line II)*. *(E-15 books more ids than those ten — the ten **plus sixteen surplus**: the two R-1b cross-declarations, the eight banked R1 negatives added at repair round 2 and the six added at repair round 4, none ever counted into the 24; the total, its counting rule and the map's resolution live at E-15's arithmetic paragraph, which this cell references and does not re-issue.)*

| # | row ⟨provenance⟩ | sev | witness | lands as |
|---|---|---|---|---|
| **C-1** | **R1** ⟨megatranche identity ≡ CC-084 ≡ MT-F024 ≡ DR-12 ≡ census lane-library §4.6 crash-surface row; guarded at kf-AmigaScene §Identity guards, kf-CSSCodeEditor KF-CE-12/C-7, kf-KeyframeTimeline C-7, kf-KeyframeCardList Axis C S-C §122, kf-KeyframesStringControls C-2 §161⟩ | **BLOCKER** (value-side, published-and-immutable at 4.0.0) | `parseCssColor` throws a raw `TypeError` on every empty-body functional colour (`oklch()`, `rgb()`, `lab()`, `color()`, `foo()`) instead of returning `{ok:false}` — every consumer's `!ok` degenerate is **bypassed**. Probe: **324 throws / 1548 calls, 4 of 9 entry points RED**, root `src/css/grammar.ts:181`. Independently reproduced by execution at the kf-SquareInstrument seat against installed 4.0.0. | **G-KF3-2** (totality at the consumed coordinate) + **G-KF3-7** (the consumer half). **THE OWNERSHIP IS THE CARRY**, verbatim from kf-AmigaScene: *"**S+2/superlative 6 ≡ census R1 crash-surface row (lane-library §4.6)** — amiga is the negative exemplar (zero exposure); the R1 row itself stays owned by KF.W3."* The **value-side root fix is NOT this wave's** (CC-084 → X-W9). This wave owns the **CONSUMER half**: prove that at coordinate `V`, no kf ingress can reach the throw. Riders **carried verbatim, never re-booked**: (a) KF-CE-12 — `CSSCodeEditor` is *"the demo's largest untrusted-CSS ingress … the **fuzz entry of record**"*, folding by reference to the megatranche R1 identity; (b) KeyframeTimeline C-7 — crash identity FOLDS, the census correction to `GATE-VERDICT.md §F-2` is recorded, the fuzz-entry note rides KF.W2/KF.W3, **the silent `console.error` posture books to KF.W7 — NOT here**; (c) KeyframesStringControls C-2 — *"**Reachability of the `!ok` arm is the open value.js R1 question** … absence-of-posture → NO-WAVE-OWNER; **reachability → KF.W3**"*; (d) KeyframeCardList — the seam is `KeyframesEditor.vue` `parseCssScalar`, class unreachable from that component, folds to R1 per the KF-CE-12 precedent |
| **C-2** | **KF-CB-33** ⟨kf-CopyButton bank; KF-SST-36 explicit FOLD BY REFERENCE, *"Not re-booked"*; KF-KC-53 *"Same posture as banked KF-CB-33"*; kf-KeyframeCardList Axis C S-C; kf-AmigaScene S+2⟩ | **MAJOR — a SCOPING EDICT, the sharpest single carry in this table** | Verbatim: *"the R1 colour-parser class is unreachable (transform/opacity only), yet value.js broke this component through the easing/css organ (`parseTimingFunction` + `bezierPresets`) — exactly the class a §4.6-scoped parser wave would miss. **EE-01 is the proof the prediction already fired once.**"* EE-01 live: `timingFunction: "bounceInEase"` resolved to nothing under 4.0.0, ctor threw inside an uncaught async `onMounted`, `group` never assigned, **zero sighted feedback at all four mount sites**; receipted by value.js's own browser audit as **DP2-02 pageerror**. | **G-KF3-7 channel (ii)** — the consumption inventory is a **union of two channels, never §4.6 alone** — plus **G-KF3-8** and **G-KF3-6**. **Fired a second time, measured by this seat** (§B.3): `animationDescriptions.ts:76` sits in the census's colour/scalar roster and is a `parseTimingFunction` site. Fence anchor **RE-RESOLVED** (§B.1) to `registry.ts:31` + `:37-48` (throw `:43-46`) |
| **C-3** | **KF-ET-32** (KF.W3 leg only — the N1 map lock) ⟨kf-EasingTarget⟩ | INFO/MINOR (bundle) — **METHOD-BINDING** | Verbatim: *"the 41.6 KB `/css` module (exporting `parseCssColor`) enters the static graph for two frozen tables (**link, not call** — N1 keeps this component **clean** on the R1 map, a KF.W3 scoping input)."* | **G-KF3-7's predicate**: the map is built from the **CALL graph**, not the import graph. A `depcruise`/import inventory yields false positives here and, per C-2, false negatives on the easing organ. Corroborated at the frontier: many `@mkbabb/value.js/css` import lines under `src/` are links, not calls. The record's other legs route away: `ToggleValue`/`AcceptableValue` drift → **SS-6 BH letter**; the missing synchronous name→fn easing surface off `loadAnimationEngine()` → **KF.W5 letter** |
| **C-4** | **K-6** ⟨kf-SquareInstrument §Killed-claims / §Ruled disagreements row 4⟩ | **KILL — a bounds correction, or this wave manufactures a false RED** | Verbatim: *"**DEAD BY EXECUTION, reproduced by this seat** … `parseCssColor("oklch(0.684 0.250 327.9)")` → `{ok:true, …}` … R1's true input is empty-args `oklch()` (throws in my run) which **no token can produce** — `getPropertyValue` yields `""` and `useSquareTumble.ts:17`'s `if (value)` guard skips empties. **Safe regardless of cascade order.** Reader-B overruled."* Both anchors (`:17` guard, `:22` call) re-confirmed at origin/master by this fold seat. | **BINDS G-KF3-7 and G-KF3-2 — ON THE TOKEN PATH ONLY (scope fixed by RULINGS R-7a).** The born-RED gate on *"the R1 demo crash surface"* **MAY NOT** be written as a **token-path** reachability claim at `useSquareTumble.ts:22` — *that* RED is unavailable (empty-args `oklch()` is unreachable through `getPropertyValue` → `""` → the `:17` guard) and asserting it violates **L-19**. **The kill stops there.** It does **not** reach the `:23` `!ok → throw` arm, whose reachability C-5 establishes on authored CSS. **Read as a pair: C-4 governs the token→R1 path; C-5 governs the authored-CSS/throw path** — one file, two mechanisms, two dispositions, no contradiction. Residue routes away: the two-producer `--rainbow-*` `:root` collision (glass-ui `scale-paper.css` oklch vs demo `design-idioms.css:18-21` hsl, held only by import order) ≡ the census flat-namespace row → **KF.W6**; the unguarded computed-style reads stay a `useSquareTumble` lane note. **Dissent PRESERVED**: reader-B independently CONFIRMED C-13 as MINOR and was **OVERRULED by execution** — a kill-of-a-confirm, recorded so no seat re-spends the budget |
| **C-5** | **D-27 / L-7 / C-9** ⟨kf-SquareScene §MAJOR, §Identity guards line 125⟩ | **MAJOR (confirmed)** — this wave supplies the **WITNESS**, another owner holds the **CURE** | Five `throw` sites execute inside the rAF frame (`num()` ×2, `colorAt` ×3) with no engine guard. **A throw does not drop a frame — it BRICKS the loop for the mount's lifetime**: `RAFPlayback._run` skips `reschedule`, `_cleanup` never runs, `running` stays true, and `useSweepScene`'s `!playback.running` guard makes `startLoop` a permanent no-op — drag, keyboard and tumble die **silently**. **Reachability carried WHOLE — both conjuncts, verbatim** (the second was trimmed at first authoring and is RESTORED per RULINGS R-7a; a cure-shape lock is carried, never quoted to fit an exclusion): *"Reachable via the same editor path L-2 opens (`calc()` is ordinary authored CSS; `parseCssScalar("calc(1px + 2px)")` → ERR → `num()` throws), **and via any `--rainbow-*` re-author in a syntax value.js 4.0.0 rejects** (the megatranche R1 identity — `parseCssColor("oklch()")` throws; noted as a fold to the V·π parser program, never re-booked here)."* | **WITNESS ONLY → G-KF3-7's severity clause.** The R1-class blast radius in the kf demo is not *"a bad swatch"*; it is a **permanently wedged animation loop** — that is what makes the born-RED gate honest. Adjudicated disposition carried unchanged: *"**NO-WAVE-OWNER (scene-repair: degrade-not-throw in the frame path) + a library rider: `RAFPlayback._run` should fail a frame loudly and recoverably, not wedge — engine-seam decision.**"* The R1 note ≡ megatranche R1: **annotated, never re-booked**. Companion L-19: *"the cure this scene exists for has no regression test … `num()`, `transformFunc`, `seatFromPose`, `frame()`, and all of `useSquareTumble` (including the throw sites) are untested — the exact S.G2 `"42px"` regression would ship silently."* **Read as a pair with C-4** (R-7a): the census's single named surface is killed *as a token claim*, and the genuinely reachable ones are the authored-CSS paths — `calc()` through `num()`, the `--rainbow-*` re-author through `colorAt` → `useSquareTumble.ts:22-23`'s `!ok` throw — at sites the census does not list. **This row is the wave's licence to assert that RED** (with kf-EditorShell C-19 at §Excluded E-15: RR-2 reproduced it live against installed 4.0.0) |
| **C-6** | **F-5** (census identity) — the **KF.W3-SHIM TAXONOMY COLLISION** ⟨the five rows, across **four** records — kf-KfPillTabs books two: kf-ChannelControls L-15/C-10 · kf-AnimationControlsGroup L-5 §17/§39 · kf-KfPillTabs N-KPT-2 **and** L:D-8/C:C-6, §122 · kf-DemoGlobalChrome M-L3 §17 — **plus one non-row citation**: `kf-ControlsPaneWrapper:6`, a header taxonomy sentence, counted as evidence of the spelling's reach, **never as a sixth row**⟩ | MINOR (hygiene) — **a ROUTING HAZARD of high consequence** | **FIVE adjudicated ROWS across FOUR records** name a wave literally spelled **"KF.W3-SHIM"** — cardinality corrected at repair round 1 for the row count and **again at repair round 2 for the record count** (PASS-2/KF-W3-CHECK **D-03**: round 1 corrected *"six records"* to *"five records"* and thereby **moved** the error instead of killing it). `kf-KfPillTabs.md` books **two** of the five (`:58` N-KPT-2 · `:70` L:D-8/C:C-6), so the family is **5 rows / 4 records** — kf-ChannelControls · kf-AnimationControlsGroup · kf-KfPillTabs ×2 · kf-DemoGlobalChrome. A **fifth record**, `kf-ControlsPaneWrapper`, carries the spelling **only** in its `:6` **header taxonomy sentence** (*"Wave targets follow the census lane taxonomy as instantiated by the parent record (`lane-frontend.md` §10): KF.W1-DEP · KF.W2-TABS · **KF.W3-SHIM** · …"*), **not as an adjudicated row**; that record books no F-5/shim row at all. **Re-derived at the bank by this seat, 2026-08-28** — `grep -n 'KF\.W3-SHIM' kf-*.md` over the 58 records returns hits in five records; subtract the five header taxonomy sentences (`kf-AnimationControlsGroup:17` · `kf-ChannelControls:17` · `kf-ControlsPaneWrapper:6` · `kf-DemoGlobalChrome:17` · `kf-KfPillTabs:19`), the one in-row **cross-reference to the law** inside a different row (`kf-AnimationControlsGroup:39`) and the one **terminal-manifest routing restatement** (`kf-KfPillTabs:122`, which re-routes its own two rows and mints none), and the five adjudicated rows above remain. **Per LAW B (RULINGS-3) that cardinality is not certified in this spec's voice**: it is measured at `docs/tranches/X/keyframes/conformance/PASS-5/KF-W3-CHECK.md` §1 (2026-08-28) — **re-pointed at repair round 5 to the one artifact of record for this file's LAW-B claims** (RULINGS-5 §END KF-W3 **D-3** / R5-9 row 6; the predecessor `PASS-3/KF-W3-CHECK.md` §1 derived the identical cardinality and is superseded, not deleted) — which re-derived the family from the bytes: *"**12 hits in 5 records** … **12 − 7 = 5 rows across 4 records**"* (emphasis as in the source; the one elision marked, and it drops only the subtraction's itemised seven), and recorded the round-2 D-03 repair as holding **under independent re-derivation**; when a later pass supersedes that artifact the citation is re-pointed **by scope over the greppable class** (§B.6's round-5 block), not the claim re-asserted. The routing hazard is unchanged (a header spelling collides exactly as a row spelling does) — and the **row** count 5 is what §Carry's `9 + 5 + 10 = 24` consumes, so the arithmetic is untouched by this correction. A class booked at the wrong cardinality cannot be checked for closure, and that law binds this spec's own bookkeeping first. The taxonomy is instantiated from `lane-frontend.md §10`'s SEVEN-wave sketch, whose numbering collides with the canonical `CENSUS-2026-08-03.md:177-206` KF.W0–KF.W10 sketch under which **this** wave is Parser Consumption. *A spec author agglomerating by `grep 'KF.W3'` swallows the entire shim/barrel deletion family into the parser wave.* | **REJECTED FROM BOUNDS AND ROUTED — the disposal is an act, not a mention** (M-25). → **§Excluded E-1**, riders intact, to **KF.W6 · Glass Suffusion** |
| **C-7** | **B10-17 · B10-18 · B10-19 · B10-20 · B10-22 · B10-23 · B10-27** (+ **X-5**, + **C-7 citation cap**) ⟨`INTAKE-ADJUDICATION-2026-08-03.md §3` KF.W4 group + §2⟩ | INFO — **inherited LAW, binding on every gate below** | The intake yields **no KF.W3-targeted row** (*"CARRY row" struck at repair round 1, R-12*); what it yields is transferable born-RED gate-design law distilled from three CONFIRMED Codex falsifiers (B18-16 finalizer rewrite · B18-20 envelope-only receipt validation · B21-9..11 hand-listed 5-key envelope vs the 8-key authenticated artifact). | **§Gates preamble.** The law verbatim: *"(a) one immutable map keyed by physical coordinate; (b) reauthenticate every payload node and checksum row at use; (c) read every declared predecessor authority — never allowlist; (d) derive every envelope assertion from the pinned artifact; **and a gate may not re-derive its own oracle.**"* Two negative locks ride along: **X-5** — *"ZERO overlap with the census top-10 live defects — the B-corpus is a harness audit, not product coverage, and **must never be cited as such**"* (so **no B-lineage coverage number appears in any gate here**); **C-7** — the keyframes v8 seal is *"Citable ONLY as `345/12/57 @ 8281638c`, never 357/414, never as HEAD coverage."* Primary routing of the rows is **KF.W4**; this wave **inherits the law without re-booking them**. X-1 (the B-lineage pinned to stale `8281638c`) → KF.W0, not re-booked |

---

## §Gates

**SEVEN close gates — G-KF3-1 … G-KF3-7 — all born-RED with a named live witness, plus TWO DECLARED FLOORS (G-KF3-8, G-KF3-9) that are NOT close conditions.** The count is corrected at repair round 1 (RULINGS R-7a): the earlier preamble read *"nine conditions"*, but G-KF3-9 cannot be turned green by this wave **in its own words** (*"This wave may NOT author that gate"*) and G-KF3-8 **declares itself fail-open** — two honest declarations wearing gate numbers inflate a gate count by two. **The numbers G-KF3-8/-9 are RETAINED, never renumbered** (every cross-edge, lock and §Excluded line in this file and its siblings cites them by id; a renumber to express a re-class is exactly the churn the alias law forbids) — they are re-labelled **FLOOR** in place. Every close gate is a command against a **pinned artifact or a physical coordinate**; none re-derives its own oracle (C-7 clause d). No gate cites a B-lineage coverage number (X-5) or the v8 seal outside its `345/12/57 @ 8281638c` form (C-7).

**G-KF3-1 — THE RC-P RECIPROCITY IS WHOLE AND THE PREDICATE IS EVALUATED BY COMMAND.**
**Re-founded at repair round 1 (RULINGS R-7a companion): the earlier form was green the moment it was written** — its two conjuncts (*the sentence exists* ∧ *the predicate is FALSE*) were both TRUE at authoring, and its stated green condition (*"the predicate is FALSE"*) could never be discharged by a wave that opens on the predicate being TRUE. A gate satisfied by the act of authoring is not a gate.
**The close condition (three conjuncts, none satisfiable by writing this file):** (1) this file carries the opening-condition sentence verbatim and **by predicate name** — satisfied at authoring, and stated here as the gate's *precondition*, not its discharge; (2) the **fourier half of the same reciprocity exists** — X.P.W4 G-6's two-hit falsifier is two-ended, and `grep -rn 'RC-P' docs/tranches/X/fourier/` returns **0** today (re-measured at repair round 2, 2026-08-28, over the **eleven** existing `F-W*.md` specs — `ls docs/tranches/X/fourier/waves/` → `F-W0 … F-W10`. *Off-by-one corrected per PASS-2/KF-W3-CHECK **D-04**: round 1 wrote "ten". The 0-hit result is unchanged and the gate's RED is unaffected — but a gate that states the size of the set it swept states it right, on the same law that convicts a class booked at the wrong cardinality at §Carry C-6*); (3) at execution the wave **pastes the RC-P evaluator's own output** — the six commands run against registry coordinate `V` — into `docs/tranches/X/keyframes/evidence/W3/` (directory does not exist today: `ls docs/tranches/X/keyframes/evidence/` → *No such file or directory*), so the predicate is read from a command's transcript and **never from a status word in any document, including this one**.
**DISCHARGE**: conjunct (2) is discharged by the fourier seat's own file (never by a write from here — the same both-ends law that forbids X·P from writing this file); conjunct (3) is discharged by the paste. RED stands until both land.
*RED baseline, re-measured by this seat 2026-08-28*: `ls docs/tranches/X/parse-that/RELEASE-CONDITION.md` → *No such file*; `ls docs/tranches/X/parse-that/RELEASE-PACKET.md` → *No such file*; `grep '"version"' package.json` → `4.0.0`; `grep -c 'RC-P' docs/tranches/V/coordination/INBOX.md` → **0**. **The baseline has SHARPENED since X.P.W4 authored it**: X.P.W4 recorded *"neither sub-tranche's wave directory exists"*; today `ls -d docs/tranches/X/keyframes/waves docs/tranches/X/fourier/waves` succeeds for **both**, and before this file was written `grep -rn 'RC-P' docs/tranches/X/keyframes/waves/ docs/tranches/X/fourier/` returned **zero hits**. This file discharges the **keyframes half** of the reciprocity; the fourier half is still absent, so X.P.W4's own two-hit falsifier remains RED from the other end — and the predicate itself is FALSE on all six conjuncts.
*Falsifier*: X·P **may not** satisfy this by writing here (its §4 bounds forbid it; *"a reciprocity a seat authored on both ends is not a reciprocity"*). Fails for its intended reason if this file cites X.P.W4 by number instead of naming RC-P — **and a green claimed on this file's own existence fails**: conjuncts (2) and (3) are discharged elsewhere, by another seat's file and by a pasted command transcript respectively. Neither can be produced by editing this spec, which is precisely what makes the gate a gate (C-7 clause d: a gate may not re-derive its own oracle).

**G-KF3-2 — R1 TOTALITY AT THE CONSUMED COORDINATE.**
`node docs/tranches/V/megatranche/audit/probes/r1-published-totality.mjs` (unmodified — R-E) against `V`'s registry tarball exits **0**: 0 throws / 1548 calls across all nine public parsers. This is **RC-P conjunct 2**, restated as this wave's entry condition.
*RED baseline*: at 4.0.0 the probe reports **324 throws / 1548 calls, 4 of 9 entry points RED, one failure mode**, rooted at `src/css/grammar.ts:181` — measured: `const components = splitTopLevel(slash[0]!.replace(/,/g, " "), "space");`. Probe present on disk (3476 B); its own header states it runs against the packed artifact per L-12.
*Falsifier*: a green obtained by **editing the probe** or by **pointing it at `src/`** fails — *a gate may not re-derive its own oracle* (C-7 d). Per C-4/K-6, this gate may **not** be restated as a token-path claim at `useSquareTumble.ts:22`.

**G-KF3-3 — 14-FIXTURE ROUND-TRIP CORPUS GREEN AT `V`, WITH ATTRIBUTION.**
The regression net passes at the repinned coordinate over a subject set that is **ENUMERATED BY PATH, never by count** — **(a)** `test/fixtures/keyframes/` (14 `.css` + `manifest.json`) via `test/compile/roundtrip-fidelity.test.ts`; **(b)** `test/fixtures/compile/{scroll-driven,multi-color-scroll}.css` via `test/compile/compile-roundtrip.test.ts` (`:424-428` fixture-arm block, *"Authored GATE-FIRST"*); **(c)** the **value.js parse/emit-facing test modules**, by path: `test/compile/{compile-roundtrip,interp-slot,structural-emit,valuejs-contract}.test.ts` · `test/engine/{color-fidelity,computed-resolution,interpolate-anything}.test.ts` · `test/group/structural-composition.test.ts` · `test/resolve/{emerging-css-resolve-fn,emerging-css-resolve-now,emerging-css-resolve-p2,value4-immutable-resolve}.test.ts`. **Each RED is attributed value-side or kf-side before the wave closes.**
**The inherited *"15 parser-facing modules"* clause is STRUCK** (repair round 2, PASS-2/KF-W3-CHECK **D-01**): it named a subject set this spec never enumerated anywhere — `grep` finds no roster — while the gate's own falsifier forbids citing a module count, so at execution a seat could close over any 15 of the 17 candidate files and no reader could convict it. **Arm (c)'s roster is DERIVED, not inherited**, by one command re-executed at the substrate of record (R2-5) — `git grep -l -E '@mkbabb/value\.js/css|parseCss(Color|Scalar|Values)|parseTimingFunction|serializeCssValue|reverseAnimationShorthand|cssIdent' <ref> -- test/` — run by this seat at `origin/master` `81a56990` on 2026-08-28 to produce the paths above, and **re-run by the executing seat against `V`, its raw output pasted into `docs/tranches/X/keyframes/evidence/W3/`**: if the tree has moved, the roster moves with it and the delta is recorded rather than the spec's frozen list being trusted. kf's **own** `parseKeyframeSelector` consumers (`test/compile/selector-value4.test.ts`, `test/engine/nan-frame.test.ts`) are **excluded by name** — the symbol is kf-authored, not a value.js parse symbol (§B.3 row 4), and conflating the two is exactly the F1 conflation RULINGS-2 **R2-6.1** excludes by name from KF.W2's G-W2-2. The 15-vs-16-vs-17 counting-base finding stays where §B.1 routed it: **KF.W5's X-4**.
*RED baseline*: **RED by non-existence of the coordinate** — the corpus has never run against any `V ≠ 4.0.0` because no such published version exists. Corpus verified at the frontier by this seat: 14 `.css` (box-shadow, color-achromatic, color-chromatic, drop-shadow, filter-multi, linear-easing, matrix3d, multi-prop, opacity-3stop, per-kf-easing, property-block, steps-easing, transform-multiarg, var-calc) + `manifest.json`.
*Falsifier*: the manifest `_README` declares its single `epsilon` row *"a value.js HANDOFF (the oklab parse↔serialize float drift), NOT a kf serializer channel-drop."* A repin that reddens that row is a **VALUE-side** regression; a gate that books it as a kf failure — **or silently widens the epsilon to stay green** — fails. `roundTripScrollCSS` (`src/animation/scroll/grammar.ts:143`, doc `:138-142`) already codifies the `serialize(parse(s)) ≡ s` oracle and is the shape the rest of the seam adopts. **No denominator here cites a module count** — arm (c) is an enumeration by path, re-derived by command at execution — and none may, until KF.W5's X-4 reconciles 80 / 139 / 153 / "159" (and this seat's own 15-vs-16-vs-17 finding, §B.1). **Two further fail arms, added at repair round 2 with the D-01 strike**: a green obtained over a roster the executing seat did **not** re-derive at `V` (the spec's list adopted as authority) fails; and a green obtained over a **subset** of the enumerated paths, or over any file the re-derived roster does not return, fails.

**G-KF3-4 — `serializeCssValue` FORK RETIRED ONTO THE PUBLISHED EXPORT.**
kf's diverged `serializeCssValue` is **deleted** and its callers route to value's published `./css` export; the known text divergence is reconciled in the fixtures **in the same commit**, never absorbed silently. An E13 row discharging **O-11 §B** is appended to `INBOX.md` in that commit.
**CONSUMER SET AND ROUTING TARGET, both measured (RULINGS-3 LAW A; added at repair round 3).** (i) **The delete's consumers are the import graph's, not the file's**: **§B.5 census 1** returns **fourteen importing modules** — 8 `src/` · **1 `demo/`** (`timelineEngine.ts:17`, reaching library-private `src/` through the `@src` alias root) · 5 `test/`, all five of the latter inside G-KF3-3 arm (c)'s derived roster — plus the five in-file uses. The gate is green only when **every one of the fourteen** resolves to the routing target; a re-route of the in-file five is a line-count discharge and fails. (ii) **THE ROUTING TARGET IS AN INBOUND DEPENDENCY, NOT AN ASSUMPTION**: **§B.5 census 1b** enumerated the pinned coordinate's packed surface — **79 exports across all seven subpaths of `@mkbabb/value.js@4.0.0`, and `serializeCssValue` is on none of them** (the `./css` `serialize*` surface is `serializeCssColor` · `serializeTimelineOptions`; source-side, value.js's `src/css/index.ts` does not re-export the definition at `src/css/stylesheet.ts:81`). **So "route the callers to value's published `./css` export" names an export that does not exist at 4.0.0**, and this gate's GREEN is reachable **only** if coordinate `V` publishes it. That publication is hereby **declared as an ask on the X-W9 / X·V adoption cut** (the cross-edge below carries it) and is stated as a **conjunct of this gate, not a background assumption**: GREEN = the fork is deleted **AND** `serializeCssValue` is enumerable on `V`'s packed `./css` surface by the census-1b command **AND** the fourteen consumers import it from there. Authored this way the gate can fail for its intended reason; authored the old way it could not pass at all — and *"an oracle that can never pass"* is the convicted class, the mirror of G-KF3-1's *"green the moment it was written."*
*RED baseline*, re-verified at the frontier: `src/animation/compile/emit/css-text.ts:41-56` the fork (recursive self-calls `:43 :48`; in-file callers `:66 :83 :89`), and `:54 if (!serialized.ok) throw new TypeError("Value returned an unserializable CSS color.");`. O-11 §B is booked **"OPEN — gated on the 4.1 bump"**, divergence recorded as `"a : b"` → `"a: b"` vs `"a : b"` (**2/3 fixtures RED**).
*Falsifier*: fails if the fork is deleted but the two RED fixtures are **re-baselined to the new text without a dated divergence row** — the retirement would then hide a behaviour change inside a refactor. Fails also if **`css-text.ts:54`'s throw survives** the retirement: it is the terminus of kf-KeyframesStringControls C-2's chain from `KeyframesStringControls.vue:123-125` (anchor confirmed exact at the frontier — the sole mount writer, no `try`). **Three arms added at repair round 3 with the LAW A census**: fails if the fork is deleted while **`serializeCssValue` is absent from `V`'s packed `./css` surface** — a delete onto a non-existent target is not a retirement; fails if green is obtained by **re-implementing the fork under another name inside kf** (the G-KF3-5 rename law, applied to this gate); and fails if the **`demo/` consumer `timelineEngine.ts:17/:88` is left importing `@src/animation/compile/emit/css-text`** — the alias-root consumer no `src/`-scoped grep would have shown, and the one this wave would have missed.

**G-KF3-5 — DUAL-1 UNIFIED: ONE `reverseAnimationShorthand` REACHABLE FROM THE EMIT TIER.**
**RED BASELINE RE-CUT AT REPAIR ROUND 3 — the inherited one silently mis-resolved the PROVIDER (RULINGS-3 R3-4's SUBJECT-IDENTITY class; census at §B.5 census 2).** The struck text read: *"value.js's published one is imported at `format/options.ts:20` and called at `:121`."* Measured at `origin/master`: `options.ts:20` sits in an import block that **closes at `:23` with `} from "../css-text";`** — it imports **kf's OWN fork**, not value.js's; and value.js **publishes no `reverseAnimationShorthand` at all** (0 of the 79 exports across the pinned 4.0.0's seven subpaths, census 1b; `grep -rn 'reverseAnimationShorthand' src/ --include='*.ts'` in value.js → **0**). The symbol name matched at the anchor; the provider did not — the exact silent mis-resolution R2-5 checked existence for and R3-4 rules identity for.
*RED baseline, as measured*: **one implementation** — `src/animation/compile/emit/css-text.ts:17`, the sole live import specifier being `format/options.ts:20` ← `../css-text`, called `:121` — and **a prose surface asserting a second provider that does not exist, enumerated and partitioned at §B.5 census 2** *(re-cut at repair round 4, RULINGS-4 §END KF-W3 **D-4**: the struck figure — *"four prose sites plus two comments"* — was two short of the carve and one long of the predicate; the enumeration carries the count and its counting rule, this cell carries none)*: `backward/backward.ts:20` (*"value.js's OWN `reverseAnimationShorthand`"*), `:46` (*"This module statically imports `@mkbabb/value.js` (`reverseAnimationShorthand` via `format.ts` …)"* — `format.ts:1` in fact imports `serializeCssValue` from `../css-text`, and value.js appears there only for types), `format/options.ts:10` and `:81` (*"the published 0.12.0 inverse"*), plus `src/animation/index.ts:232` and `test/compile/compile-roundtrip.test.ts:23`. The census's DUAL-1 sentence — *"a parser wave that changes value.js's shorthand output changes only ONE of them"* — is **true of the risk and wrong about the mechanism at the frontier**: the danger is not two live implementations but **a live fork narrated as if it were the published one**, so a shorthand change at `V` moves *nothing* here while every comment says it does. Under **L-6** and **KF-CE-41** the prose **is** the defect surface, and it is this gate's principal subject. *(Corroboration, same shape, same repo: the megatranche harvest record `registry/harvest/wf_076062ab-dc8.json:412` measured `backward.ts`'s sibling claim that value.js supplies `sampleColorRamp`/`deltaEOK` and found **no such export on any subpath**.)*
**GREEN, restated so it is reachable and falsifiable** — two lawful discharges, and the executing seat names which it took **before** it edits: **(a) UNIFY-BY-PUBLICATION** — `V` publishes `reverseAnimationShorthand` (enumerable by the census-1b command, an ask carried on the same X-W9 / X·V cross-edge as G-KF3-4's), kf's `css-text.ts:17` is **deleted**, `options.ts:20-23`'s specifier resolves to `@mkbabb/value.js`, and **every PROVIDER-NAMING site in §B.5 census 2's partition reads true**; or **(b) UNIFY-BY-TRUTH** — `V` does not publish it, kf's implementation stands as the **sole and acknowledged** one, and **those same sites are corrected to say so**. **The gate states no numeral** (repair round 4, D-4 / LAW D(3)): its subject set is *the census-2 partition, re-read at execution* — if the tree has moved, the partition moves with it and the delta is recorded, exactly as G-KF3-3 arm (c) treats its roster. GREEN in either case = `git grep -n '\breverseAnimationShorthand\b' <ref> -- src/ test/` returns exactly one implementation and **zero sentences naming a provider the packed surface does not carry** — the predicate, which is what binds; the enumeration is how it is checked.
*Falsifier*: a green obtained by **renaming** kf's copy rather than deleting or acknowledging it fails — under discharge (a) the defect is a live fork, not a name collision, and a rename leaves the fork; under discharge (b) a rename hides the very identity the prose must state. *(The round-2 form of this clause — "the defect is two implementations" — is corrected with the baseline above: at the frontier there is **one** implementation and a second **narrated** provider that does not exist.)* **Added at round 3**: a green obtained under discharge (a) while `V`'s packed surface does not export the symbol fails (the delete would land on nothing); a green obtained under discharge (b) while **any PROVIDER-NAMING site of §B.5 census 2's partition** still names value.js as the provider fails **— and a green obtained by re-reading the partition at execution and finding it larger, then closing over the frozen five, fails identically** (the roster is re-derived, never inherited: PASS-4/KF-W3-CHECK **D-4**'s cure is a predicate, not a list); and a green claimed on the **struck** baseline — *"value's published one is imported at `options.ts:20`"* — fails on its face, the anchor being a `../css-text` import. Per `feedback_no_backwards_compat`, **no alias shim may survive**. Per **KF-CE-41** (the comment is part of the defect surface), the gate also fails if `backward.ts:20`/`:46`, `format/options.ts:10`/`:81` or `src/animation/index.ts:232` — the five PROVIDER-NAMING sites of §B.5 census 2 — still describe a twin that no longer exists. **The three PROVIDER-SILENT sites (`backward.ts:252`/`:285`, `compile-roundtrip.test.ts:23`) are in §B.2's carve under KF-CE-41 but are NOT this predicate's subjects**: they attribute the symbol to no one, so no correction of theirs can make a false provider sentence true, and a gate that counted them would be counting the wrong thing (repair round 4, D-4).

**G-KF3-6 — THE 8-CURVE DELTA IS ACKNOWLEDGED IN WRITING, kf-SIDE.**
A **dated kf-side mail-ledger row** exists for **O-11 §D2**: 8 curves change shape at the bump (analytic in/out arms restored, RD-5), `max|Δ| = 0.192` on `ease-out-circ`, then `ease-in-expo` `6.930e-2`, `ease-in-circ` `4.489e-2`; the `approximated: boolean` discriminant was proposed and **DECLINED**.
*RED baseline*: the census ledger cell reads verbatim **"DECLARED; no keyframes acknowledgement on record"** (`lane-docs.md:373`).
*Why it is not paperwork* — the fence, **re-resolved by this seat** (§B.1): the curves land in `src/animation/compile/easing/registry.ts` at `:31 ...Object.keys(bezierPresets),` and are consumed at **module top level** by the `timingFunctionEntries` initializer `:37-48`, whose `:43-46` is `throw new Error(\`value.js rejected its own easing …\`)`. **A key removal is a kf boot `throw`; an addition silently widens kf's public registry.** Demo blast radius measured: `TimingFunctionPanel.vue:52/:97/:98/:127`, `EasingSidebar.vue:77/:112/:155`.
*Falsifier*: an acknowledgement recording the curve **names without the measured deltas**, or **omitting the DECLINED discriminant**, fails. Delivered as prose rather than an INBOX ledger row (E13), it fails.

**G-KF3-7 — THE R1 CALL-MAP EXISTS, IS CALL-BASED, AND SPANS BOTH ORGANS.**
A per-site map classifies every kf consumption of value.js's parse/emit surface as **REACHABLE** or **UNREACHABLE with the mechanism named**, built from the **CALL** graph, covering **(i)** the colour/scalar parse channel and **(ii)** the easing/css organ. Severity per site is stated in blast-radius terms, not in "a bad value" terms (C-5).

**THE DENOMINATOR IS NAMED, NOT IMPLIED (RULINGS R-7b).** The map's denominator = the eight measured sites of **§B.3** ∪ the channel-(ii) seed set (§B.3 tail) ∪ **the registry ids booked at §Excluded E-15 and E-14.5** — E-15's twelve-row table (ten routed exclusions-with-named-owner + X-16/X-17 surplus), **surplus sub-line I** (eight, repair round 2, RULINGS-2 **R2-11**), **surplus sub-line II** (six, repair round 4, RULINGS-4 §END KF-W3 **D-1**) and **`KF-HA-19`** at E-14.5 — each id entered in the map with its banked mechanism. **This cell states no total**: the enumeration, its count and its counting rule live at E-15's arithmetic paragraph and are referenced, never re-issued (LAW D(3), repair round 4). **The admission rule is PAYLOAD-BASED and markup-free, re-cut at repair round 3 (PASS-3/KF-W3-CHECK D-1): a row enters this denominator iff its banked payload is a value.js-R1 reachability, containment or posture finding** — read off the row's bytes, never off a token count. That rule admits every one of them **uniformly** — which is what the round-2 text did not do (it admitted three markup-silent rows and withheld the same admission from eight of identical shape) **and what the round-3 text still did not do at corpus scale**: the rule was applied to twenty ids and withheld from six banked negatives of the same shape, because the sweep that finds them ran **markup-first** (PASS-4/KF-W3-CHECK **D-1**). **The sweep is now the corpus**: the payload sweep runs over all **58** records independent of markup, at E-15's sub-line II, with its commands pasted there. **Admission is not routing**: the routed/surplus split is a separate, owner-derived question answered once at E-15's preamble, and this denominator is indifferent to it. Completeness of the denominator is **not asserted here** (LAW B) — it is measured at `docs/tranches/X/keyframes/conformance/PASS-5/KF-W3-CHECK.md` §1/§2 (2026-08-28), **the one artifact of record, re-pointed by SCOPE at repair round 5** (RULINGS-5 §END KF-W3 **D-3** / R5-9 row 6), which re-derived this denominator's arithmetic from zero and found it closing at **27** (E-15's 12 + 8 + 6 = 26, plus `KF-HA-19` at E-14.5) against RULINGS-4's ruled floor of **≥ 26**, with the map's resolution re-adding to the same 27. The findings this sentence cures keep their own pass prefixes and are cited by them, never bare: **PASS-4 D-1** (the admission rule applied to twenty ids and withheld from six banked negatives of identical shape) and its predecessor **PASS-3 D-1** (the round-3 form). **MOST OF THE DENOMINATOR IS UNREACHABLE-with-the-mechanism-named rows — they ARE the map** (its root nodes and its reference shapes): the **six** RULINGS R-7b names among the ten first-authoring escapes — KF-APP-56 (the boot-graph negative), CubeAxisLines C·§4, TimelineTrack SUP-3 (**the only positive containment posture among the ROUTED ids** — an ingress wrapped `await`-inside-`try`; the corpus-wide form of this superlative is corrected at E-15's surplus sub-line, which puts two more containment postures on the page), DemoGlobalChrome C-10, ChannelControls C-L-1 (an independent second-seat re-run of this wave's own six-hit grep), CubeTarget challenge-C S-1 (a three-leg structural negative, twice hostile re-read) — **plus** the R-1b cross-declared kf-KeyboardShortcutsModal R1-negative (**X-16**, *"Do not re-file"*) — **plus the eight surplus negatives of sub-line I** (CSSPasteDialog C-4 · LayerConfigPanel LP-24 · KeyframesAddDialog C's K-5 · OrbitalDrag #3 · SpringPhysicsFacet ★S-7 · TimelineHoverPreview S+3/S-d · AnimatedText C-10 · SpringTrace S-A) **plus the six of sub-line II** (MbabbMenu **MM-37** — the bundle-graph root node · SequenceScene **C S★-2** · SpringHeatmap **superlative 7** · MatrixEditor **superlative 7 (r1)** · EditorHeader **C S-A** · AnimationVisualizer **SUP-7**), every one a banked negative and none of them routed to any wave. *(The tallies are corrected at rounds 1, 2 and 4 and are **stated once, at E-15's arithmetic paragraph**, with the REACHABLE/REACHABLE-but-correct/posture/declined-framing remainder enumerated there by id; this cell points at that enumeration and re-issues no numeral — LAW D(3). **Every surplus id is outside the routed 24**, which stays 9 + 5 + 10.)* **Ownership of their cures is KF.W2's** (each is an exclusion-with-named-owner at E-15); **their presence in this denominator is this wave's** — a map that omits its own negatives cannot know what completeness is, and cannot be audited for it.
*RED baseline, three ways*: **(a)** no such map exists. **(b)** the census's single named entry is **killed**: lane-library §4.6 names `demo/scenes/square/useSquareTumble.ts:22 parseCssColor(css)` as *"the known R1 crash surface"*, and K-6 killed it by execution — *"Safe regardless of cascade order. Reader-B overruled."* **(c)** the roster is **measurably wrong at the frontier**, this seat, 2026-08-28: `git grep -n "parseCssColor\|parseCssScalar\|parseCssValues" origin/master -- demo/` returns **six hits = three imports + three calls**; of the census's five §4.6 sites, `demo/utils/keyframeSelector.ts:15` is **`parseKeyframeSelector`** (whose `requireKeyframeSelector` `:14-21` converts a typed `!ok` into a **kf-authored** `throw new TypeError` at `:18`, two live callers at `useKeyframeOps.ts:174` and `timelineEngine.ts:83`), and `demo/utils/reference-data/animationDescriptions.ts:76` is **`parseTimingFunction`** — a **channel (ii)** site mis-filed in the colour/scalar roster. Meanwhile the genuinely reachable colour/scalar path is at sites the census does not list: D-27/L-7/C-9's `parseCssScalar("calc(1px + 2px)")` → ERR → `num()` throws **inside the rAF frame** → `RAFPlayback._run` wedged for the mount's lifetime.
Meanwhile the `useSquareTumble.ts:22-23` `!ok → throw` arm is **REACHABLE** on the authored-CSS path (C-5, both conjuncts; kf-EditorShell C-19's live RR-2 reproduction) while the *token* path there is UNREACHABLE with its mechanism named (C-4/K-6) — the map states both, per the C-4/C-5 pair reading, and is wrong if it states only one.
*Falsifier*: **an import-graph inventory fails by construction** — KF-ET-32 proves `/css` enters a static graph as a **LINK, not a CALL** (*"N1 keeps this component clean on the R1 map"*), and `KeyframesEditor.vue:123` is itself an import line the census cited as a call site. A map **scoped to §4.6 alone fails KF-CB-33**. Required ingress seeds: the `CSSCodeEditor` untrusted-CSS ingress (**the banked `:116` anchor lands inside the `:114` debounce block at the frontier, not the ingress — re-resolve before building the map**), the KeyframeTimeline inline editor, and `KeyframesStringControls.vue:123-125` → `css-text.ts:54`.

**G-KF3-8 — DECLARED FLOOR (not a close gate) — THE REPIN DOES NOT RE-ARM THE EASING-NAME TRAP.**
**Re-classed at repair round 1 (RULINGS R-7a): this condition states of itself that it "fails-open if KF.W4 has not landed"** — a condition that does not block is a floor, not a close gate. It keeps its number for citation stability (L-6 and cross-edge KF.W4 leg 1 bind it by id) and is excluded from the seven-gate close set.
Post-repin, `pageerror` count is **0** on `/#/easing` and `/#/spring` (the EE-01 gate), and **no test can pass by asserting a phantom easing name**.
*RED baseline, frontier-durable, re-verified by this seat at the full path* **`test/orchestration/orchestration-api.test.ts`**: `:142-148` asserts **TWO** phantoms undefined under ONE false comment — **`:145 expect(cssTwinFor("easeOutCubic")).toBeUndefined();` and `:146 expect(cssTwinFor("bounceInEase")).toBeUndefined();`** — under the `:143-144` comment calling **both** *"real registry curves"*, while `:147`'s `not-a-real-easing` proves **in-file** that the assertion cannot distinguish real from phantom. **Exhibit widened at repair round 1**: the earlier text carried `bounceInEase` alone; the comment-stated-invariant obligation (L-6) attaches to **each name the comment asserts**, so a cure that repairs one name and leaves the other under the same comment does not discharge it (`easeOutCubic` re-read at `origin/master` by the repair seat) — and the frontier registry carries `"easeInBounce"` (`registry.ts:27`), not `bounceInEase`. The frontier repaired **exactly one word** (`demo/components/CopyButton.vue:42` = `"easeInBounce"`) and left the test plus the prose phantoms at `easing.ts:44` and `waapi/eligibility.ts:169` standing (all three re-confirmed at origin/master). EE-01 already fired once, receipted as **DP2-02 pageerror**.
*Falsifier*: the **comment-stated-invariant law** (kf-SquareScene) makes *"a real registry curve"* a **test obligation**, not prose. The cure is **KF.W4's** (KF-CB-18/24/29, one commit); **this gate fails-open if KF.W4 has not landed — which is exactly the sequencing it exists to expose.** Fails for its intended reason if a repin lands with the phantom trail intact.

**G-KF3-9 — DECLARED FLOOR (not a close gate) — INHERITED-RED: NO MERGE-PATH GATE OBSERVES THE DEMO.**
**Re-classed at repair round 1 (RULINGS R-7a): this condition states of itself that "this wave may NOT author that gate"** — a condition no seat of this wave can turn green is a floor by construction, and counting it as a close gate inflates the gate count without adding an obligation. Number retained for citation stability (OP-6, E-4 and cross-edge KF.W4 leg 2 bind it by id). Its title already said FLOOR; the arithmetic now agrees with the title.
This wave's demo-side gates are **unenforceable** until a merge-path gate typechecks and exercises `demo/`. Declared as an inherited floor with its baseline measured — **not owned as this wave's cure**.
*RED baseline, frontier-durable, re-verified by this seat* (KF-CB-8): the only job observing these components is `ci.yml:53-55` — `demo-correctness` with `if: github.event_name == 'schedule' || github.event_name == 'workflow_dispatch'` — nightly/manual only; the merge path runs `check:lib` over `tsconfig.lib.json`, so `demo/` is never typechecked. Compounding: KF-CE-16 measured a **TS2554** surviving in `CSSCodeEditor.vue` (`debounce(fn, 200, false)` into a two-parameter function — `debouncedEmit = debounce(` pinned at `:114` at the frontier) because *"zero `.vue` files are typechecked by any script in the repo"*; kf-SquareScene L-14/C-13.1 records the same class.
*Falsifier*: the cure is **KF.W4's** (`vue-tsc --noEmit` into `check` + CI; the KF-CE-16 gate identity banked at kf-App KF-APP-4). **This wave may NOT author that gate.** It fails **honestly by naming the missing enforcement** rather than by pretending its own demo-side gates bind.

---

## §Sequencing

### Locks (binding on execution order inside and around this wave)

- **L-1 · The gate-key, not a schedule.** This wave is **gate-keyed and NEVER scheduled**. It opens on `RC-P(V)` evaluated by command. *TOTALITY is RC-P conjunct 2 and is FALSE today.* A row that unblocks on a document's status word is exactly the defect RC-P exists to kill.
- **L-2 · KF.W0 §B-12 re-anchor law.** Every anchor re-baselines against `origin/master` `81a56990` before any cure. §B.1 is this wave's discharge, and it **found three dead paths and two imprecise spans** — proof the law is not ceremony. Two W0 locks inherited: `origin/master` `package.json:77` pins `"@mkbabb/glass-ui": "7.0.0"` **exact devDep** (= installed), falsifying the F-1 phantom-dependency headline at HEAD; and the **EE-01 repair ALREADY LANDED at the frontier** (`demo/components/CopyButton.vue:42`), so the danger inverts to **re-regression** — *"the stale fork's in-flight migration must not re-land the dead name over the frontier's repair."* G-KF3-8 is the born-RED witness if it does.
- **L-3 · KF.W4 BEFORE THE REPIN.** Repinning before KF.W4's easing hygiene lands **re-arms the trap C-2 exists to name**. Ordering, not advice.
- **L-4 · The mail cure precedes everything kf-side** (census §(c)6). G-KF3-4 and G-KF3-6 are the kf-side discharges of O-11 §B and §D2; both packets were mis-delivered into the stale sacred checkout, invisible to any executor, unanswered 10–16 days. They cannot be authored honestly until KF.W1 lands them exec-visible.
- **L-5 · Reachability here; posture elsewhere.** This wave takes **only reachability**. Absence-of-posture cures are other waves': KeyframesStringControls C-2's missing `try`, KeyframeTimeline's silent `console.error` (**KF.W7**), the five failure postures over the 13 Tier-A sites (**KF.W2**).
- **L-6 · The comment-stated-invariant law** (kf-SquareScene): a comment asserting a runtime property is a **test obligation**. Binds G-KF3-8 (*"a real registry curve"*) and G-KF3-5 (KF-CE-41: *"value.js's OWN"* is part of the defect surface).
- **L-7 · One commit per meaning.** The fork retirement + its fixture divergence row is **one commit** (G-KF3-4). The repin + its attribution receipts is **one commit**. A retirement folded into a refactor hides a behaviour change.
- **L-8 · Taxonomy ratification.** X·KF ratifies **ONE** taxonomy — the census `KF.W0–W10` sketch, which is what COHESION §1's SS-1/SS-2 columns use — and records the `lane-frontend.md §10` spellings as a **search alias, never a rename** (the R-B idiom X·P already uses). §Excluded E-1 is this wave's half of that act.
- **L-9 · KF-AV-28 · STANDING SUPERSESSION RIDER (KF.W7's defining lock) — carried, not cited.** **QUOTED, then restated — the round-2 text claimed verbatim carriage and delivered a restatement (PASS-3/KF-W3-CHECK D-6); both now stand, each labelled.** The rider's canonical bytes, `kf-AnimationVisualizer.md:35`, **verbatim**:
  > *"A standing sequencing rider on every NO-WAVE-OWNER row below: **KF.W7's S-9 evaluation (KF-AV-28) may supersede any behavioral cure here** — if the evaluate verdict is "swap", the bespoke rows discharge with the component."*
  **This spec's restatement, so labelled** (the scoping is drawn from the rider's own preceding clause — *"behavioral repairs with no census owner are NO-WAVE-OWNER"* — and adds nothing to it): KF.W7's S-9 evaluation may supersede any behavioral cure on a NO-WAVE-OWNER row banked under an evaluated surface; a per-surface SWAP verdict discharges the bespoke rows banked under that surface with the component. **The two sibling records are NOT byte-mirrors and are no longer cited as if they were** (measured 2026-08-28): `kf-PlaybackRibbon.md:36` states the rider in its own words **and adds the C axis's §7 caution** — that ribbon is the **NON-bespoke** case (it consumes the real `Slider`) and carries BLOCKERs anyway, *"so 'swap onto the primitive' is never sufficient as a cure"*; `kf-SequenceScrubber.md:36` applies it narrowly — *"the KF.W7 ScrubberTimeline evaluation gates the component-shape rows."* A seat relying on a SWAP verdict reads all three. Rows in this spec governed by the rider are spent CONDITIONALLY: no cure on a governed row lands before KF.W7's verdict for its surface, or the executing seat accepts the throwaway explicitly; at execution the seat STATES which governed rows a SWAP verdict has discharged BEFORE spending any cure (KF-W7 G15's discharge discipline).
  **Governed rows in this spec** (added at repair round 1 per RULINGS R-10 — the rider was previously absent by id, `grep -cF 'KF-AV-28'` → 0): the rows this wave **routes to KF.W7** at §Excluded **E-5** and the KF.W7 cross-edge below — (i) **kf-KeyframeTimeline C-7's silent `console.error` posture**, and (ii) its **L-6/C-4 hand-rolled declaration parser/emitter**. This wave spends no cure on either (it takes reachability only, L-5), so the rider costs it nothing *here* — but a KF.W7 *evaluate* verdict may discharge both with the component, and the receiving seat must not spend a posture cure on a surface KF.W7 has swapped. **Carried by id AND quoted verbatim above, with this spec's restatement labelled as a restatement** (the round-2 formula *"carried by id, never paraphrased"* over-claimed against its own delivery — D-6; the claim now matches the bytes on the page).

### Cross-edges, declared from this end

*(COHESION §2: cross-repo edges are declared FROM BOTH ENDS in the spec files — the X·V W4-D2 lesson, now law.)*

| edge | direction | declaration |
|---|---|---|
| **X·P — X.P.W4 / RC-P** | **DEPENDS ON** | The opening-condition sentence at the head of this file, verbatim, by predicate name. All six conjuncts FALSE, re-measured 2026-08-28. Reciprocity is a **G-6 hard gate on X.P.W4** and this file is the only lawful site for the sentence; the fourier half is still owed from that side's other consumer. |
| **X·V L1/L5 + the X·V adoption wave** (owner-gated, OP-4/G-9) | **DEPENDS ON** | **PLAW-BIND is the only route**: parser → value (X·V L1/L5) → packed release → consumers. This wave receives parser effects **only through an admitted, packed value.js release**. There is **no direct parse-that → keyframes edge, and none may be created**. The X·V adoption wave consumes `SEAM-CONTRACT.md` at `src/css/**`; X·P writes no bytes there and neither does this wave. |
| **X-W9 / CC-084** (the value-side R1 root fix) | **DEPENDS ON** | RC-P conjunct 2 cannot be TRUE until `parseCssColor` returns `{ok:false}` for the empty-args class — root `src/css/grammar.ts:181`, RED-first 56-vector battery, `no-non-null-assertion` under `src/css/`, plus the behavioural half of `verify-packed-surface.mjs`. One dated 4.0.1/4.1 cut with CC-088 + the AM-13 dependency strip; **"no emergency 4.0.1 — ruled"**; exact-pin consumers notified. **keyframes.js IS an exact-pin consumer** — `origin/master` `package.json:70` = `"4.0.0"`. **TWO PUBLICATION ASKS ADDED AT REPAIR ROUND 3, declared from this end and measured, not assumed** (§B.5 census 1b — 79 exports enumerated across the pinned coordinate's seven subpaths): **(1)** `serializeCssValue` — defined at value.js `src/css/stylesheet.ts:81`, **not re-exported by `src/css/index.ts`, absent from the packed `./css` surface** — must be published at `V` or G-KF3-4's retirement has no target; **(2)** `reverseAnimationShorthand` — **does not exist in value.js at any coordinate measured** (0 hits in `src/`), so G-KF3-5's discharge (a) is unavailable unless `V` adds it, and discharge (b) is the honest path. Both are **asks, not this wave's bytes** (X·V owns its source, E-2); both belong on the same dated cut as CC-084, and the exact-pin notification that carries the cut carries them. Until `V` answers, G-KF3-4's target conjunct and G-KF3-5's discharge (a) stay RED **for their intended reason**, which is why they are stated as conjuncts. |
| **KF.W0 · Substrate Settle (§B-12)** | **DEPENDS ON** | L-2. §B.1's re-anchor table is offered to W0 as measured input; W0's pasted table remains authoritative. X-1 (the Codex B-lineage pinned to stale `8281638c`) also lands there, not here. |
| **KF.W2 · Parse Façade** | **DEPENDS ON** | The pre-work that makes consumption one-touch. The R1 fuzz-entry note **rides KF.W2/KF.W3 jointly**; this wave owns reachability, KF.W2 owns posture. **The R1 negative/posture census of record is KF.W2's F0** (RULINGS R-1b/R-7b) — neither parser wave may again assume the other holds the negatives: the ids at §Excluded **E-15** — twelve in its table, eight on surplus sub-line I (RULINGS-2 **R2-11**), six on surplus sub-line II (RULINGS-4 §END KF-W3 **D-1**) — are booked THERE and counted HERE (total and counting rule at E-15's arithmetic paragraph), and this wave mints no negative register of its own. Its cure also gives `requireKeyframeSelector` (§B.3) a lawful non-throwing shape. |
| **KF.W4 · K-Quartet Hygiene** | **DEPENDS ON** | Three blocking legs: (1) the easing-name trap disarmed before the repin (L-3): KF-CB-18 (`test/orchestration/orchestration-api.test.ts:142-148`), KF-CB-24 (`TimingFunctionNames` advertises `"steps"`, which throws), KF-CB-29 (the `| string` arm at **`src/animation/constants/types.ts:195`** — path pinned at repair round 3, PASS-3/KF-W3-CHECK **D-4**: the tree carries **four** `types.ts` (`src/animation/constants/`, `src/animation/group/`, `src/animation/physics/spring/`, `demo/scenes/cube/orbital-drag/`) and the bare basename named none of them; re-read at `origin/master` → `:193-196` are the union's four arms, `Easing` · `TimingFunctionNames` · **`string`** · `undefined`, the third being the arm this row names — *"the mechanism by which the value.js rename shipped"*) — one commit, plus the K1 stable-easing-identities row; (2) the gate topology — KF-CB-8 + KF-CE-16 + KF-APP-4 — without which **every demo-side gate here is unenforceable** (G-KF3-9); (3) the born-RED gate-design law (B10-17..27), authored there, **inherited here without re-booking**. |
| **KF.W5 · D/L/C Tri-Fold Library Audit** | **DECLARES** (asks a repin does not close) | Two public-surface holes this wave must **not** patch demo-side: (a) **quantifier corrected at repair round 3 (PASS-3/KF-W3-CHECK D-3; census §B.5 census 3)** — `cssIdent` is re-exported at **TWO** sites, not one: `compile/emit/index.ts:53` (`export { cssIdent } from "./backward";`) **and** `compile/emit/backward/index.ts:29` (`export { cssIdent } from "./walk";`), definition `backward/walk.ts:145`. The round-2 *"only"* was marked *confirmed* on the first site's **existence**, which is not what an exclusivity claim asserts — and a seat re-routing *"the one re-export site"* would leave `backward/index.ts:29` standing. **The ask survives the correction intact**: neither site is `public.ts`, `load-engine.ts` or the LIGHT barrel — `git grep -c '\bcssIdent\b' origin/master -- src/animation/public.ts src/animation/load-engine.ts src/animation/index.ts` → **no output, 0 hits in all three** (measured by this seat, 2026-08-28) — so neither `kfEngine()` nor `loadAnimationEngine()` can reach it — *which is why the demo hand-rolls `.replace().toLowerCase()` and ends with three names for one animation* (kf-KeyframesStringControls N-8); (b) no synchronous name→fn easing surface off `loadAnimationEngine()` (KF-ET-32). Plus KF.W5's **X-4**: reconcile the 80 / 139 / 153 / "159" counting bases — and this seat's own **15 / 16 / 17** parser-facing-module finding (§B.1) — before any denominator is cited. |
| **KF.W6 · Glass Suffusion** | **ROUTES OUT** | The whole **"KF.W3-SHIM"** family (§Excluded E-1), plus the two-producer `--rainbow-*` `:root` collision surfaced by C-4's residue and kf-SquareScene D-23. |
| **KF.W7 · Timeline Evaluate** | **ROUTES OUT** | KeyframeTimeline C-7's silent-`console.error` posture; its L-6/C-4 hand-rolled declaration parser/emitter (cross-referencing KF.W2). Neither comes here. **Both are governed by `KF-AV-28`, the standing supersession rider carried verbatim at §Sequencing L-9** — a KF.W7 SWAP verdict on the timeline surface discharges them with the component; the rider is named **by id and quoted verbatim** at §Sequencing L-9, with this spec's scoping restatement labelled as a restatement beside it (repair round 3, PASS-3/KF-W3-CHECK **D-6** — the round-2 wording *"because paraphrase is not carry"* asserted of its own delivery something its delivery did not do). |
| **NO-WAVE-OWNER — square scene-repair + the engine-seam rider** | **SUPPLIES A WITNESS ONLY** | D-27/L-7/C-9's cure is not this wave's: *"NO-WAVE-OWNER (scene-repair: degrade-not-throw in the frame path) + a library rider: `RAFPlayback._run` should fail a frame loudly and recoverably, not wedge — engine-seam decision."* This wave takes the blast-radius witness only. Companion L-19: the throw sites are untested. |
| **SS-6 · glass-ui BH relay** (owner edict, standing formation invariant) | **ROUTES OUT** | Any producer-side row met while mapping consumption leaves via the **batched BH letter** and **NEVER becomes a demo-side hack**. Live in these records: KF-ET-32's `ToggleValue`/`AcceptableValue` payload-type re-export ask (glass-ui re-exports **no** alias — 0 hits, so no supported spelling exists); kf-CSSCodeEditor's Skeleton export gap — **UNREACHABLE, not unreached** (no `./skeleton` among 73 export subpaths, 0 `Skeleton` matches in `dist/index.d.ts`; census S-6's verb corrected). |
| **KF.W1 · Mail Cure (O-8/O-11) + E13** | **DEPENDS ON** | L-4. RC-P conjunct 6 (ROUTED) requires a dated INBOX row naming the RELEASE-PACKET path and the SS-6 batch carrying it cross-repo — measured **0** today. |
| **KF.W10 · Fold Discharge & Close** | **DEPENDS ON** (the verb) **· SUPPLIES** (the E-14.5 pass-through) | **ADDED AT REPAIR ROUND 4 — RULINGS-4 §END KF-W3 D-3 (PASS-4/KF-W3-CHECK **D-3**): the seam was substantively fulfilled at E-14.5 and never declared from this end, in the one table whose stated purpose is to hold it — in a spec that opens this table by quoting the both-ends law.** Both legs are **ANCHOR-ONLY per LAW C(3)**: KF.W10 writes **LAST of the per-wave seats** this round, so this row carries stable anchors alone — §-heading plus row identity, **no quoted sibling prose and no line numbers** — and the **RECONCILE seat verifies it after the last per-wave write** (LAW C(4)). **(1) DEPENDS ON — the verb.** This file's four-verb table stamps **VERIFIED** nowhere: it is stamped only at X·KF close, by KF.W10, because no wave stamps VERIFIED at its own close. This wave's terminal verb is therefore that wave's act, and this is the dependency. **(2) SUPPLIES — the pass-through.** `KF-W10 §6.C-I`'s not-owned pass-through of the **KF-HA-19 / KF-APP-56 / kf-EditorShell C-19** triple is discharged from this end: **X-06** and **X-07** at §Excluded **E-15**, and **KF-HA-19 at §Excluded E-14.5** — booked by the bank's own bytes, with its NO-WAVE-OWNER verb, its severity and its home untouched, its bank anchor carried, **beside the two carried siblings on the dissent-preservation surface** (§Excluded E-14, whose whole function is preserved dissent). That is what `KF-W10 §6.C-0 · INBOUND-DEPENDENCY TABLE`'s **KF.W3 row (row 2)** asks be declared at the source wave so its OP-4 and terminal table are fulfillable by construction. **The COHESION §0b/§0c row below is NOT this edge** — it is an *ownership* row and says so, routing the KF.W10 ownership gap rather than re-declaring it; this row is the *dependency*. **Anti-drift note for the RECONCILE seat**: adding this row raises `grep -c 'KF-HA-19' KF-W3.md` from the **4** that RULINGS-4 **R4-7 D-2** pre-printed for W10's edge-2 cell — a figure measured *before* this repair — to **10** at this file's stage-1 bytes (re-run at write time, 2026-08-28). Under LAW D(3) that command is re-run by W10 at **its** write time against this file's FINAL stage-1 bytes; the pre-printed 4 is a pre-repair reading and must not be pasted as a post-repair one. **The booking itself is unmoved**: E-14.5 is still the row, which is what the edge-2 cell asserts. |
| **COHESION §0b (this wave) / §0c (KF.W10)** | **RECEIVES A DATED ASSIGNMENT; DECLARES ONE RESIDUAL** | **Re-anchored at repair round 1 (RULINGS R-7b + R-18); the prior "assigned to NEITHER" reading is STRUCK as stale against its own cited authority.** `COHESION.md` **§0b ADDENDUM 2026-08-28** cures the gap by assignment: **KF.W3 is a library wave and belongs to SS-1**, its spec authored under that seat at this path (banked `b6e09ed4`), **GATED posture unchanged — PLAW-BIND keys it to the X·P release condition; never scheduled by this assignment**. §1's SS-1/SS-2 table predates the addendum and is read through it. **ONE owed ruling remains, not two: the write hand** (census §(c)5 / OP-2) — *"the authoring seat"* is discharged. The sibling KF.W10 gap is routed, not re-declared here: RULINGS **R-18** recommends **SS-2** and names the cure a **COHESION §0c addendum — the root session's act, flagged upward**; this wave neither assigns it nor blocks on it. |

---

## §Excluded

Nothing is dropped silently. Each line names what does not land here and why.

- **E-1 · The entire "KF.W3-SHIM" family — FIVE rows across FOUR records** *(kf-KfPillTabs books two; record count corrected at repair round 2 per PASS-2/KF-W3-CHECK **D-03**, C-6)* **→ KF.W6 · Glass Suffusion.** ⟨kf-ChannelControls L-15/C-10 · kf-AnimationControlsGroup L-5 · kf-KfPillTabs N-KPT-2 + L:D-8/C:C-6 · kf-DemoGlobalChrome M-L3 (NO-WAVE-OWNER — see (e)) · **and, as a non-row citation, `kf-ControlsPaneWrapper:6`'s header taxonomy sentence, which books no shim row**⟩ **Reason**: `lane-frontend.md §10`'s seven-wave taxonomy (KF.W1-DEP · KF.W2-TABS · **KF.W3-SHIM** · KF.W4-PROSE · KF.W5-PARTIALS · KF.W6-TIMELINE · KF.W7-TOKENS) is a **different numbering** from the canonical census KF.W0–W10 sketch. Under the canonical taxonomy the F-5 shim deletes are named in **KF.W6** (*"S-1 replace + **F-5 shim deletes** + S-5/S-6/S-7 partials + the `--kf-*` token-namespace audit"*). **The number 3 is not inherited across taxonomies.** Riders travel **intact**, and their corrections travel with them: (a) **kf-ACG L-5** — *"census F-5's 'zero consumers' evidence for the `useAnimationGroupPlayback` shim is **wrong** — and the one consumer is the suite that proves the L-1 contract broken … the test's `:21` import string traverses `composables/useAnimationGroupPlayback`; the lane's scan root excluded `test/`. **Correction of evidence, not law**: the no-backwards-compat charge stands … the deletion wave must repoint the test import — and per L-1, that test must move to the component seam in the same stroke, not merely repoint"*; (b) **kf-KfPillTabs L:D-8/C:C-6** — *"because the advertised path was never implemented, the F-5 shim is the only working type path — its deletion must repoint `ChannelControls.vue:230` at `../KfPillTabs/useKfPillTabs`, **NOT** at the `.vue`"*; (c) **kf-ChannelControls L-15/C-10** — the 4-line `transport/composables/useKfPillTabs.ts` shim, sole consumer `ChannelControls.vue:230`, the `KfPillTabs.vue`-beside-`KfPillTabs/` duplicate-name hazard, *"under §0 the whole triple deletes together"*; (d) **kf-KfPillTabs N-KPT-2** — the entire instrument barrel chain dead (`demo/components/instrument/index.ts` zero importers; `transport/index.ts` referenced only by that dead file), *"one deletion family with the F-5 shim"*; (e) **kf-DemoGlobalChrome M-L3** — the `transport/`/`instrument/` barrels incl. three `defineAsyncComponent` wrappers, zero consumers repo-wide. **Terminal-disposition correction (repair round 1)**: the bank ends this row *"**NO-WAVE-OWNER** (hygiene; **cross-ref** KF.W3-SHIM's no-backwards-compat law)"* — `KF.W3-SHIM` there is a **cross-reference to a law, not the row's home**. M-L3's terminal disposition is therefore **NO-WAVE-OWNER**, carried unchanged (routing is the bank's, as severity is); it is named in this line because it is the same *deletion family* as (a)–(d) and must not be swept into the parser wave by the spelling — **not** because this wave re-homes it. Whichever seat executes the F-5 family at KF.W6 inherits it as an adjacency with its NO-WAVE-OWNER verb intact; if that seat declines it, it stays NO-WAVE-OWNER and does not return here. All five cite `feedback_no_backwards_compat`. **LAW A DISCHARGE FOR THE CARRIED DELETES (added at repair round 3).** RULINGS-3 **LAW A** binds every seat that *"writes, **carries**, or leaves standing"* a delete/repoint/shim act, so the two shim modules named in riders (a)–(c) are censused **from the import graph** at §B.5 — **census 4** (`transport/composables/useAnimationGroupPlayback.ts`: one live consumer, `test/demo/state/no-shadow-playback-authority.test.ts:21`; the sibling `test/demo/instrument/useAnimationGroupPlayback.test.ts:5` imports the REAL module and is **not** a shim consumer — reproducing **RULINGS-3 R3-1** independently) and **census 5** (`transport/composables/useKfPillTabs.ts`: sole consumer `ChannelControls.vue:230`, reachable **only** by the relative-depth spelling `../composables/useKfPillTabs`, which a long-tail-only census would have missed). **Neither act is re-homed by this line** — the deletes and repoints are **KF.W6's** under R3-1 — and both censuses carry one flag for the executing seat: keyframes' own `docs/tranches/V/audit/R2-06-demo-target-tree.md:126/:127` names its repoint targets with directory casings (`animation-controls-group/`, `kf-pill-tabs/`) that **do not exist** at `origin/master`, where `git ls-tree` returns `AnimationControlsGroup/` and `KfPillTabs/`. Flagged forward, not fixed here.
- **E-2 · The value-side R1 root fix (CC-084, `src/css/grammar.ts:181`) → X-W9.** **Reason**: X·V owns its bytes (COHESION §2 routing law); PLAW-BIND; ruled *"no emergency 4.0.1."* This wave owns the consumer half only.
- **E-3 · The easing-name hygiene commit (KF-CB-18 / KF-CB-24 / KF-CB-29 + K1) → KF.W4.** **Reason**: it is a *prerequisite*, not a payload (L-3). A repin that also renames is two changes wearing one commit message.
- **E-4 · The gate chassis (KF-CB-8, KF-CE-16, KF-APP-4, `vue-tsc --noEmit`) → KF.W4.** **Reason**: authored there as the sub-tranche's declared sequencing head. This wave declares the floor (G-KF3-9) and may not author the cure.
- **E-5 · Absence-of-posture cures.** KeyframesStringControls C-2's missing `try` → **NO-WAVE-OWNER**; KeyframeTimeline C-7's silent `console.error` and L-6/C-4's hand-rolled declaration parser → **KF.W7**; the five failure postures / 13 Tier-A sites / 6 Tier-C regexes incl. `cssom.ts:214-216` → **KF.W2**. **Reason**: L-5 — this wave takes reachability, not posture. **The two KF.W7-routed rows are governed by `KF-AV-28`** (§Sequencing L-9, where the rider's canonical bytes are **quoted verbatim** and this spec's scoping restatement is labelled as such — D-6): a KF.W7 SWAP verdict on the timeline surface discharges them with the component, and the receiving seat states that discharge before spending a cure.
- **E-6 · The square scene-repair packet + the `RAFPlayback._run` engine-seam rider.** **Reason**: adjudicated **NO-WAVE-OWNER** + a library rider; carried unchanged. This wave supplies the witness that makes the severity honest and nothing else.
- **E-7 · All six NO-WAVE-OWNER kf scene-repair packets (cube, sequence, spring, square, dock-menu) and CARD-UNIT are NOT homed here.** **Reason and act**: the taxonomy list handed to this seat proposes zero packets for this wave and **this seat concurs, on the merits**: this is a **library-half** wave whose product is a call-map, a fork deletion, a name unification and two mail discharges. None of the six is a parse/emit consumption defect. This is a declination **with the routing named** (they remain NO-WAVE-OWNER pending the KF.W4 sequencing head, where the homing question must be answered by the specs that own their surfaces), not a silent pass.
- **E-8 · KF-CO-1 / KF-CO-8 (LP-1 no-write→render lock), kf-ChromeDock M-4's MbabbMenu MUST-CARRY rider, and the TD-1/TD-2 cure bundle.** **Reason RE-FOUNDED at repair round 1 (RULINGS R-7a companion).** The former reason — *"no ChannelOptions, ChromeDock or TransportDock row lands in this wave's CARRY"* — is **STRUCK as factually false**: `kf-ChannelOptions.md:126/:155` books **C·S-4**, *"`parseTimingFunction(null|42|{})` throws a raw TypeError out of a Result-returning parser — **the banked R1 class**"*, forwarded to the **value.js parser lane** — a channel-(ii) R1-class row, exactly the miss C-2/KF-CB-33 predicts, and an exclusion resting on an untrue premise is not a lawful disposal. **The true routing**: C·S-4 is **not excluded** — it is booked at §Excluded **E-15** as an exclusion-with-named-owner (**KF.W2**, the R1 negative/posture census of record per R-1b) and counted in G-KF3-7's denominator. What this line excludes is the **rest** of those records: LP-1's no-write→render lock, M-4's MbabbMenu MUST-CARRY rider and the TD-1/TD-2 bundle are **surface-packet rows, not parse/emit consumption rows** — their homes are the OPTIONS-UNIT packet at **KF.W12** and the dock-menu / transport-ribbon packets at **KF.W13** (RULINGS R-15's canonical 17-packet roster, homed by KF.W4). Their sequencing locks are recorded here so they are not lost in transit, and they **must be carried by whichever spec homes those rows** — an unowned lock is how a cure ships an unopenable menu. **Round-4 status of those two homes, recorded and not promised (RULINGS-4 **R4-8**, ruled at the minting bytes).** **KF.W11 · KF.W12 · KF.W13 are MINTED — deliberately, by KF-W4's R-15 homing block, with KF-W1's cross-edge-9 cohesion flag recorded DISCHARGED — and UNAUTHORED**: no spec exists at either coordinate this round. Their terminal disposition here is the **successor-formation register at `KF-W10 §6.D`**, verb **MINTED-UNAUTHORED — authoring = the SS-1/SS-2 authoring block**, ownership assignment remaining the root session's act. The citation is **ANCHOR-ONLY under LAW C(3)** — KF.W10 writes last of the per-wave seats, so this line names the register's identity and neither its bytes nor its line numbers, and the RECONCILE seat verifies it after the last write. **So this line cites a register row and promises nothing**: until a successor spec exists, the register is the artifact that carries these locks, and no packet of this family is re-booked here or read as homed by naming a wave number.
- **E-9 · Producer-side rows (glass-ui).** KF-ET-32's `ToggleValue`/`AcceptableValue` ask; the Skeleton export gap (**UNREACHABLE**, verb corrected). → **SS-6 BH relay**, batched, never a demo-side hack.
- **E-10 · The B10 intake rows themselves (B10-17..20, -22, -23, -27) and X-1.** **Reason**: §3 of the intake groups them under KF.W0/W4/W5/W9/W10; **no KF.W3-targeted row exists there** (*"CARRY row" struck at repair round 1, R-12*). Only the transferable **law** is inherited (C-7 / §Gates preamble). X-1 (the Codex B-lineage pinned to stale `8281638c`) → **KF.W0**. Neither is re-booked.
- **E-11 · Any B-lineage coverage number, and any citation of the keyframes v8 seal outside `345/12/57 @ 8281638c`.** **Reason**: **X-5** — *"the B-corpus is a harness audit, not product coverage, and must never be cited as such"*; **C-7** — *"never 357/414, never as HEAD coverage."*
- **E-12 · SCOPE OF THE K-6 KILL** *(struck and re-founded on the bytes at repair round 1, RULINGS **R-7a**; the former text — "A reachability claim at `useSquareTumble.ts:22` … Asserting that RED violates L-19" — read the kill wider than the bank wrote it and thereby **banned the gate that can fail for its intended reason**, inverting L-19)*. **K-6 kills only the claim that a TOKEN can produce the empty-args R1 degenerate** — `kf-SquareInstrument.md:24/:100`, verbatim: *"`parseCssColor("oklch(0.684 0.250 327.9)")` → `{ok:true,…}` … R1's true input is empty-args `oklch()` … which **no token can produce**"* (`getPropertyValue` yields `""`; the `:17` `if (value)` guard skips empties). **Reachability of the `useSquareTumble.ts:22` call site via authored CSS, and of the `!ok` throw arm at `:23`, is ESTABLISHED** (§Carry **C-5** = D-27/L-7/C-9's five in-frame throw sites, `colorAt` ×3; **kf-EditorShell C-19** at E-15, where *"RR-2 reproduced R1 live against the installed 4.0.0"*) **and MAY be asserted RED**. **Read as a pair: C-4 governs the token→R1 path; C-5 governs the authored-CSS/throw path.** What remains excluded is the token-path claim alone — preserved as a kill-of-a-confirm (reader-B overruled by execution) so no seat re-spends that budget.
- **E-13 · The `--rainbow-*` two-producer `:root` collision** (glass-ui `scale-paper.css` oklch vs demo `design-idioms.css:18-21` hsl, held only by import order) ≡ the census flat-namespace row → **KF.W6**. **Reason AMENDED to the bank's reading (repair round 1, RULINGS R-7a): it is BOTH** — a token-namespace defect **and** parse-adjacent. The earlier *"not a parse defect"* was the trim that let C-5's second reachability conjunct be dropped (see C-5): `kf-SquareScene.md:53` books this collision as a reachability leg — *"and via any `--rainbow-*` re-author in a syntax value.js 4.0.0 rejects (the megatranche R1 identity)"*. **What leaves is the token-namespace CURE (→ KF.W6, which owns the flat-namespace row); the parse-adjacent REACHABILITY stays here**, carried in C-5 and entered in G-KF3-7's map. Surfaced by C-4's residue and kf-SquareScene D-23.
- **E-14 · Preserved dissents, not re-litigated here**: kf-KeyframesStringControls C-2 was rated BLOCKER by axis C and **demoted to MAJOR on RR-L's motion** (demotion adopted); kf-KeyframeTimeline's D-1/L-1/C-3 sibling shows the same corpus demoting an RR-A BLOCKER — **preserved as calibration**. kf-CopyButton KF-CB-3 carries an axis-D + reader-1 BLOCKER as dissent against the adjudicated MAJOR — **untouched**. kf-ACG L-5 is a recorded correction **of the census's own evidence**, not of its law — preserved as an evidence-quality dissent against `lane-frontend §10`'s scan root.
  **E-14.5 · `KF-HA-19` — THE ADVERSE MEMBER OF W10's R1-FRAMING TRIPLE, CARRIED BY BYTES (added at repair round 3: RULINGS-3 **R3-10.2**; PASS-3/KF-W3-CHECK **D-2**. Sibling anchors RE-CUT at repair round 4 under **LAW C(3)**: RULINGS-4 §END KF-W3 **D-6**; PASS-4/KF-W3-CHECK **D-6**).**
**The sibling citation is ANCHOR-ONLY — no line numbers, no quoted sibling prose.** `KF-W10` writes **LAST of the per-wave seats** in this round's stage order (LAW C(1)), so any receipt taken at its pre-round-4 bytes is stale before it is read. That is precisely what the struck citation did: *"§6.C-I … `:398`/`:419` at this writing"* pinned two numbers that KF-W10's own round-3 repair moved beneath the section, leaving the section id resolving and the numbers pointing at unrelated §B content (measured at PASS-4/KF-W3-CHECK **D-6**; the measured replacements are **deliberately not written in here**, because LAW C(3) forbids a forward reference to carry line numbers at all — a number correct today is falsified by W10's stage-2 write). **The anchor of record is `KF-W10 §6.C-I`, the pass-through row that hands this class forward** — §-heading plus row identity, nothing else — and the **RECONCILE seat verifies it after the last per-wave write** (LAW C(4)). The *"at this writing"* hedge is kept on every parenthetical below, and the R2-7 idiom still governs: **the banked id is the anchor; record line numbers ride parenthetically, non-load-bearing.**
**What that row hands forward, stated as THIS spec's labelled characterization rather than as a quotation** (LAW D(1)/LAW C(2) — the source moves inside this round, so none of its prose is quoted here and nothing below should be read as its words): a **triple** — **KF-HA-19 / KF-APP-56 / kf-EditorShell C-19** — declared there as a **not-owned pass-through**, on the ground that the parser waves own the class in-repo and that declaring it is how the carry avoids being lost. This spec received two of the three (**X-06** = KF-APP-56, **X-07** = kf-EditorShell C-19, both at E-15) and returned **`grep -c 'KF-HA-19\|HeroAurora'` → 0** on the third — closing a declared carry **by omission**, and doing it to the one member **adverse to this wave's headline**, in a spec whose E-14 exists to preserve exactly that. Booked here, in the bank's words (`kf-HeroAurora.md:66` — **verbatim as to words, order and meaning; EMPHASIS ADDED**, disclosed at the quote per RULINGS-5 **R5-13** / LAW D(1); PASS-5/KF-W3-CHECK **D-5**):
  > *"The **"R1 = live `parseCssColor('oklch()')` shipping crash" framing does NOT reproduce** against this repo's pinned value.js 4.0.0 css subpath (ok on all oklch forms; diagnostics-not-throws on `var()`/`color-mix`) — **three independent executions**"* — disposition **NO-WAVE-OWNER** — *"a value.js-side registry carry (megatranche parser-band / STATE reconciliation), relayed up, never a KF wave row."*
  Its superlative rides with it (`:101`): *"Axis C proved a negative properly and **against interest** — its §4.1 killed its own repo's standing R1 framing with executions … and both readers re-ran it rather than inheriting it"*; the relay is booked at `kf-HeroAurora.md:125` (*"Value.js megatranche registry — KF-HA-19's R1-framing contradiction, relayed as a NO-WAVE-OWNER carry"*).
  **EMPHASIS DISCLOSURE for this booking's three quotations, itemised so a reader can byte-diff without opening the record** *(RULINGS-5 **R5-13**, the idiom ruled once and program-wide; PASS-5/KF-W3-CHECK **D-5**)*: the words, their order and their meaning are the bank's, **unaltered at all three**; the **markdown is not**. At **`:66`** the banked payload cell carries **no bold whatsoever** and spells `var()/color-mix` **without code spans** — `sed -n '66p' kf-HeroAurora.md | cut -d'|' -f4 | grep -o '\*\*' | wc -l` → **0**, its only two backticks being the ones around `parseCssColor('oklch()')`, which this rendering preserves — so this spec **adds two bold spans and two code spans**. At **`:101`** the bank bolds the **whole** clause *"Axis C proved a negative properly and against interest"* where this rendering bolds only its tail (*"against interest"*) — a redistribution, not an addition — and the **single elision is marked with its ellipsis**: it drops *"flagged the contradiction explicitly,"* — a clause that **strengthens** the quoted finding, so nothing adverse is elided. At **`:125`** the bank bolds *"Value.js megatranche registry"* and this rendering **drops** that bold. **Emphasis added at `:66`/`:101`, emphasis dropped at `:125`; no word altered, no clause reversed, one marked elision.** Nothing here is load-bearing on a figure or a verb: KF-HA-19's **NO-WAVE-OWNER** disposition, its severity and its home stay the bank's, and the file-scope form of this disclosure is declared once at **§B.6's round-5 block**.
  **Scope of the booking, stated precisely.** KF-HA-19 returns **0** on every routing markup and its banked verb is **NO-WAVE-OWNER**, so it is correctly **not** a member of the routed 24 and `routedTotal` is untouched; it is carried here as a **preserved adverse dissent** and enters G-KF3-7's denominator under the payload admission rule (a value.js-R1 reachability finding) on the **surplus** side. **No severity is re-graded, no cure is spent, and its home is not moved**: the reconciliation belongs to the **megatranche parser-band / STATE** surface, per the bank and per W10's edge.
  **RECONCILIATION, executed by this seat rather than argued (LAW B corollary — the measurement rides beside the sentence).** Against the pinned artifact `node_modules/@mkbabb/value.js@4.0.0`, `./css` packed subpath, 2026-08-28:
  ```
  parseCssColor("oklch()")                     THROW TypeError: Cannot read properties of undefined (reading 'replace')
  parseCssColor("oklch(0.7 0.1 200)")          ok=true
  parseCssColor("oklch(70% 0.1 200 / 50%)")    ok=true
  parseCssColor("rgb()") / "lab()" / "color()" / "foo()"   THROW  (same single mode)
  parseCssColor("var(--x)")                    ok=false
  parseCssColor("color-mix(in srgb, red, blue)") ok=false
  ```
  **Both records are true and they do not conflict**: KF-HA-19's three executions probed **populated** colour forms and the `var()`/`color-mix()` diagnostics path — green, exactly as banked — while R1's input class is the **empty-body functional colour**, which throws, exactly as §Carry **C-1** already states it (*"on every empty-body functional colour (`oklch()`, `rgb()`, `lab()`, `color()`, `foo()`)"*). What KF-HA-19 falsifies is the **loose framing** — *"a live shipping crash on `oklch()`"* read as ordinary authored oklch — and this spec never carried that framing; what survives is the scoped class, which the R-E probe re-measured this pass at **324 throws / 1548 calls, 4 of 9 entry points RED, one failure mode**. **This is a carriage of the contradiction, not a re-adjudication of it**: KF-HA-19's verb, severity and home are the bank's and are untouched, and the reconciliation above is offered to the parser-band / STATE surface as the receipt it is owed. It is recorded here so that the licence C-1/E-12 take from X-07's *"RR-2 reproduced R1 live against the installed 4.0.0"* is read beside the corpus's thrice-executed narrowing of it, instead of against corpus silence.

- **E-15 · THE TWELVE-ROW REGISTRY R1-CLASS TABLE — TEN ROUTED IDS AS EXCLUSIONS-WITH-NAMED-OWNER (owner: **KF.W2**) PLUS TWO SURPLUS CROSS-DECLARATIONS, ALL COUNTED IN THIS WAVE'S DENOMINATOR — AND THE TWO SURPLUS-NEGATIVES SUB-LINES: EIGHT ROWS ADDED AT REPAIR ROUND 2, SIX MORE AT REPAIR ROUND 4.** *(The line id **E-15** is unchanged and is what sibling specs cite; the title states the ten/two split its body always carried and gains the sub-line — nothing is renumbered.)* *(Booked at repair round 1 per RULINGS **R-7b** and **R-1b**. Ten of these escaped this spec's first authoring entirely — no row, no fold-identity, no exclusion line — dropping census carriage to 9 of 19; the escape is cured here **without moving a single cure into this wave**.)* **The law this line obeys, restated at repair round 2 to the width its rows actually support** *(R2-5: a law a seat re-asserts is a law that seat re-measures)*: **THE MEMBERSHIP LAW IS RE-CUT AT REPAIR ROUND 3 (PASS-3/KF-W3-CHECK D-1); THE ROUND-2 FORM IS STRUCK AS SELF-CONTRADICTORY.** *What is struck*: the discriminator *"all ten surplus records return **0** on `KF.W3`, `KF.W2/W3` and parser-lane prose alike — **which is exactly what makes them surplus** and what keeps `routedTotal` at **24**."* It is falsified by its own table. Re-measured at the bank by this seat, 2026-08-28: **`kf-CubeTarget.md` (X-13) returns 0 on every one of those markups too** — `KF.W3` **0** · `KF.W2/W3` **0** · `parser lane` **0** · `parser wave` **0** · `parser ingress` **0** · `parser consumption` **0** · `megatranche R1` **0** — and **X-12 carries no wave token in its row either**: `grep -n 'KF\.W3' kf-ChannelControls.md` → `:17` (header taxonomy) and `:79` (the SHIM row already counted among the five), never `:126`. A criterion that admits X-12/X-13 on markup admits the eight surplus rows; one that excludes the eight excludes X-12/X-13. Nor does token density rescue it — bare `R1` counts run **kf-LayerConfigPanel 6 · kf-KeyframesAddDialog 5 · kf-CSSPasteDialog 4 · kf-SpringTrace 3 · kf-SpringPhysicsFacet 3** against **kf-CubeTarget 1**, i.e. the surplus side is *denser*. Only an unstated hand-partition yielded 24, and *"a class booked at the wrong cardinality cannot be checked for closure, and that law binds this spec's own bookkeeping first"* (C-6) is this spec's own sentence.
**THE LAW THAT REPLACES IT — admission is payload-based (G-KF3-7's denominator, all twenty, uniformly); the ROUTED/SURPLUS split is OWNER-DERIVED, never markup-derived, and closes in two clauses, each with its command:**
**(R-i) MARKUP-ROUTED — RESTATED AT REPAIR ROUND 4 (RULINGS-4 §END KF-W3 **D-2**; PASS-4/KF-W3-CHECK **D-2**) AS A TWO-PART TEST, because the round-3 form was a *record*-level command doing *row*-level work and said so nowhere.** A row is markup-routed iff **(1) it is PAYLOAD-ADMISSIBLE — its banked payload is a value.js-R1 reachability, containment or posture finding, *read off the row's own bytes* (G-KF3-7's admission rule, applied at the row) — AND (2) its record is MARKUP-POSITIVE**: `grep -c` over `KF\.W3` · `KF\.W2/W3` · `KF\.W3-SHIM` · `parser lane|parser wave|parser ingress|parser consumption` · `megatranche R1`, run per record against the 58 records, returns ≥ 1.
**Why both clauses are needed, stated rather than assumed.** Clause (2) alone **cannot enumerate**: a record-level ≥ 1 test is satisfied identically by every row in a hit record, so it can *confirm* a claimed member and never *derive* one — `kf-KeyframeTimeline` returns **5** hits and carries many rows while only **C-7** routes here; `kf-ChannelControls` returns 2 `KF.W3` hits (`:17` header taxonomy · `:79` the SHIM row), neither belonging to X-12. Clause (1) is what resolves the row inside the record, and it is a **reading** — declared here as one.
**The reading step, measured and named** *(the counting rule is stated with the figure, LAW D(3); both figures re-run at write time, repair round 4, over the 58 records)*: the markup union is **26 records case-INSENSITIVELY** and **23 case-SENSITIVELY** — the law never declared its case, and the three-record delta is exactly the header-taxonomy sentence *"W3 **Parser Consumption** (GATED)"*, whose capitals a case-sensitive `grep` misses. **The wider (26) reading is the one this law takes**, so nothing hides in a lowercase assumption. Of the 26, **21 are booked or named in this spec and FIVE are discarded by reading** — named here rather than left to a silent subtraction: **`kf-EasingSidebar:32` · `kf-EasingScene:33` · `kf-TimingFunctionPanel:34`**, each a **header routing-law taxonomy sentence** enumerating the whole `KF.W0–W10` census taxonomy (the same collision C-6 books at the *row* level, one register up); and **`kf-EditorStartScreen:113` · `kf-TypingDots:89`**, each a **superlative-register sentence** citing the parser lane's R1 as *context for a read-only budget decision* — *"Given the parser lane's R1 is a live shipping crash, checking the boundary string on the demo's most-viewed component was the correct spend of a read-only budget"* (`kf-EditorStartScreen.md:113`, quoted by `grep -niE 'parser lane' kf-EditorStartScreen.md kf-TypingDots.md` at write time). **None of the five is an adjudicated row routing to any wave**, so all five fail clause (1) and none is a census escape. **IN-SPEC OCCURRENCE RECEIPT — RE-RUN AND CORRECTED IN ALL FIVE POSITIONS AT REPAIR ROUND 5** (RULINGS-5 §END KF-W3 **D-1**, under **R5-11**'s receipt-arithmetic-first mechanism: the enumeration, the counting rule and the command all live AT the receipt; PASS-5/KF-W3-CHECK **D-1**). *Command, re-run at this file's own bytes*: `for n in EasingSidebar EasingScene TimingFunctionPanel EditorStartScreen TypingDots; do grep -c "$n" KF-W3.md; done` → **3 · 1 · 3 · 1 · 1**. *Counting rule*: `grep -c` counts **matching LINES of this spec**, self-references included — and **every one of the five counts includes THIS sentence**, which names all five, so after the round-4 repair that put the five names into this file **no zero was arithmetically available**. *Enumeration of the matching lines, by content rather than by line number*: **(a) this sentence** (all five names, 1 line each); **(b) §B.3's tail**, the *"Easing/css organ, measured (channel (ii) seed set)"* line; **(c) G-KF3-6's** *"Why it is not paperwork"* fence line — and (b) and (c) each name the `bezierPresets` blast radius as **one PAIR in one breath**, `TimingFunctionPanel.vue:52/:97/:98/:127` **and** `EasingSidebar.vue:77/:112/:155`, so `TimingFunctionPanel` and `EasingSidebar` carry the **same two** blast-radius lines (2 + this sentence = **3** each) while `EasingScene` · `EditorStartScreen` · `TypingDots` occur **only here** (**1** each). The former figure — *"`grep -c` in this spec returns **0 · 0 · 2 · 0 · 0** … and **both** `TimingFunctionPanel` hits are the blast-radius anchors — measured at write time"* — is **STRUCK**: those were PASS-4 **D-2**'s *pre-repair* readings merely permuted into the new name order and carried under a *"measured at write time"* label that had not been re-run, i.e. **LAW D(3)'s numeric arm failing inside the one clause PASS-4 D-2 was raised to make honest**; it was also wrong at the class, crediting one member of a pair this spec itself writes as a pair and reporting the other absent. **SHADOW** (LAW F): struck set = {the five-position figure `0 · 0 · 2 · 0 · 0` ⟨here, E-15's **(R-i)** reading step⟩ · its mirror in the **§B.6 NUMERIC-ARM** receipt ⟨re-cut in this same edit, pointing here and re-issuing no numeral⟩}; disposition per item = replaced by the re-run figure above, stated **once**, with rule and command; **no row, routing, id or census figure moves** — none of the five is a row booking, every one of their hits is this spec's own citation, `routedTotal` stays **24** and `9 + 5 + 10 = 24` is untouched; citing/terminus waves notified = **none**, this being a receipt over this file's OWN bytes that no sibling spec cites (`grep -o 'KF-W[0-9]*\.md[:0-9]*' KF-W3.md` returns self-references only).
Members: the **nine** §Carry fold-identities, the **five** `KF.W3-SHIM` rows (§Carry C-6 / E-1), and **eight of E-15's ten** — **X-06 · X-07 · X-08 · X-09 · X-10 · X-11 · X-14 · X-15**. **9 + 5 + 8 = 22.**
**(R-ii) INSTRUMENT-ROUTED (the owner-derived clause)** — the row's banked payload is a finding **about this wave's own map instrument** — its measured roster, its method, or its denominator — which routes it to the wave that owns the instrument **whatever token the row carries**. **Closed by enumeration at exactly two members**, each with the byte that makes it one: **X-12** ⟨`kf-ChannelControls.md:126`⟩ — *"**grep re-run by this seat**: `parseCssColor` = 1 file, `scenes/square/useSquareTumble.ts`, outside this closure"* — an independent second-seat re-run of **§B.3's own six-hit roster grep**, a finding about this map's *roster*; **X-13** ⟨`kf-CubeTarget.md:76`⟩ — *"adopted as **the model for proving absence with structure rather than with a grep that found nothing**"* — a ruling on the *method* by which this map's UNREACHABLE side must be proved. **Neither is a finding about its own component's exposure**; every one of the ten surplus rows is, and each is quoted below so the distinction is checkable line by line rather than asserted. **22 + 2 = 24 — `routedTotal` is auditable by two stated commands, one enumeration, and one DECLARED reading, with no UNDECLARED partition** *(the round-3 clause "with no hand-partition anywhere" is **STRUCK** at repair round 4, RULINGS-4 §END KF-W3 **D-2**: the reading was always there — clause (R-i)(1) is one, the five discards above are its output, and C-6 subtracts seven sentences from the SHIM count by the same faculty and says so. A law that performs a reading and denies performing one is false about itself, and the gap it denied is the exact aperture the six negatives of E-15's round-4 sub-line fell through — the sweep that finds them **is** the reading step the round-3 text declared unnecessary).*
**Agreement with the independent instrument**: (R-i)'s **22** is exactly the *"strict-markup reading `routedTotal` = 22"* that `docs/tranches/X/keyframes/conformance/PASS-3/KF-W3-CHECK.md` **D-1** (2026-08-28) computed from zero; the two instruments now agree at every step and differ only by the clause that check found missing — which is (R-ii), stated above and closed by enumeration rather than by adjective. *(This citation is a **dated defect-provenance cite**, not an artifact-of-record cite: it names the pass that computed this particular figure and the defect it filed there, so the round-5 scope sweep deliberately does **not** re-point it — the discriminator between the two classes is stated at §B.6's round-5 block, RULINGS-5 §END KF-W3 **D-3**. The current artifact of record, `PASS-5/KF-W3-CHECK.md` §1, re-derives this law's case split from zero — **26** records case-insensitively / **23** case-sensitively, with the three-record delta named — and confirms that the wider reading is the one taken.)*
**SURPLUS** = admitted to G-KF3-7's denominator by the payload rule and a member of **neither** clause: 0 on all the markups **and** no instrument finding. **X-16 · X-17** in the table, plus the eight on surplus sub-line I and the six on surplus sub-line II — *"surplus" being a census class only, never a severity or an ownership class* (discipline (v)). **X-16/X-17 remain surplus cross-declarations adopted by KF.W2's F0 register under R-1b (#15/#16), not routed rows.** The census enumeration itself is **not re-adjudicated here** (that is the census's act, never a consuming wave's); the admission rule that carries the surplus into the map's UNREACHABLE side is stated at the sub-line; **KF.W2 declared itself census-of-record for the R1 negative/posture class and adopts them at its §Carry F0** (R-1b); this wave therefore **books each as an exclusion-with-named-owner and enters it in G-KF3-7's call-map denominator**. Ids are the bank's and are never renamed; riders are quoted, never paraphrased; **no severity is re-graded and no cure is spent here**. **Label discipline**: the `X-06 … X-17` column is a *local row label for this table only* — the registry id is the one in the second column and is the only id any sibling spec may cite. It is **zero-padded and starts at 06** precisely so it cannot be mistaken for the unpadded Codex-intake ids **X-1** (§Excluded E-10), **X-4** (the KF.W5 counting-base ask) and **X-5** (§Carry C-7's negative lock), nor for the **X-W9** cross-edge. No renumber is available to remove the prefix collision without churning every citation, and a renumber to express a naming preference is exactly what the alias law forbids.

| # | id ⟨record : line⟩ | banked routing bytes (verbatim) | owner | role in the G-KF3-7 map |
|---|---|---|---|---|
| **X-06** | **KF-APP-56 · C-i2 / N-1** ⟨`kf-App.md:111`⟩ | *"value.js R1 parser-crash class NOT reachable from App's boot graph (`css_syntax` ×0 in the entry; colour module is the exposure = KF-APP-15) \| **fold by reference → megatranche R1 / census risk-3** (KF.W2/W3 own the class; App books nothing)"* | KF.W2 (its B1) | **UNREACHABLE, mechanism named — the map's ROOT NODE** (the entry-graph negative) |
| **X-07** | **kf-EditorShell C-19** ⟨`kf-EditorShell.md:77`⟩ | *"MINOR · FOLD (parser class) → kf-App KF-APP-56 ≡ megatranche R1 / census risk-3 (KF.W2/W3 own the class)."* Component rider: *"EditorShell.vue:239-248 establishes `color-mix(in srgb, …)` as the demo's token idiom — the exact form value.js 4.0.0 rejects — while a sibling scene pipes computed token text through `parseCssColor` and throws on `!ok` (**RR-2 reproduced R1 live against the installed 4.0.0**). The square-scene seam is spec input for whichever component adjudication owns `useSquareTumble`."* | KF.W2 (its B9) | **REACHABLE** — the live-reproduction witness under **E-12**'s re-founding and **C-5**'s throw-path leg; names `useSquareTumble` by name |
| **X-08** | **kf-CubeAxisLines C·§4's R1 negative** ⟨`kf-CubeAxisLines.md:108`⟩ | *"**C·§4's R1 negative → megatranche R1** (KF.W2/W3)"* — folded (`:97`), sustained | KF.W2 (its B8) | **UNREACHABLE, mechanism named** |
| **X-09** | **KF-KE-54 · C-m2** ⟨`kf-KeyframesEditor.md:99`⟩ | *"`kf-engine.ts`'s "the one place value.js enters the graph" is false as written (three static edges in this closure alone); the cited gate scopes `src/` only … → **KFED-UNIT** prose cure; the ingress note folds by reference to the megatranche R1 identity (per the KF-CE-12 precedent) → **KF.W2/W3** scoping input."* | KF.W2 (its B10) — **prose cure is KFED-UNIT's** (KF.W12, R-15) | **REACHABLE (three static edges)** — the *site* is at §B.3 row 3; **the ROW is booked here** because its payload is a **false written invariant about value.js's edge count into the graph**, which is precisely what the call-map overturns |
| **X-10** | **kf-TimelineTrack axis-C SUP-3** ⟨`kf-TimelineTrack.md:18, :130`⟩ | *"**KF.W2/W3 (parser ingress, by fold)**"*; *"the only path from a TimelineTrack emit into the value.js parser is wrapped with the `await` inside the `try` (`useTimelineBuild.ts:40-50` — my read) … recorded as a negative result **so parser waves skip this file**."* | KF.W2 (its B16) | **UNREACHABLE, mechanism named — the only positive containment posture among the ROUTED ids**: the reference shape the routed REACHABLE/UNREACHABLE classification is calibrated against. *(Superlative NARROWED at repair round 2: E-15's surplus sub-line books two further banked containment postures — kf-CSSPasteDialog **C-4** and kf-SpringPhysicsFacet **★S-7** — so the corpus-wide form is false and the routed form is what the bank supports.)* |
| **X-11** | **kf-ChannelOptions C·S-4** ⟨`kf-ChannelOptions.md:126, :155`⟩ | *"`parseTimingFunction(null\|42\|{})` throws a raw TypeError out of a Result-returning parser — **the banked R1 class**; every string returns `ok:false` cleanly. Evidence FOLDED by reference to the **value.js parser lane** … one identity, no re-booking here."*; *"**value.js parser lane**: the `parseTimingFunction` non-string raw-TypeError (R1 class) — evidence forwarded, identity already banked."* | KF.W2 (its B17) | **REACHABLE, channel (ii)** — **KF-CB-33's prediction firing a THIRD time** (EE-01 first, `animationDescriptions.ts:76` second, this third). It is why **E-8's old reason was struck**: this row exists |
| **X-12** | **kf-ChannelControls C's L-1** ⟨`kf-ChannelControls.md:126`⟩ | *"grep re-run by this seat: `parseCssColor` = 1 file, `scenes/square/useSquareTumble.ts`, outside this closure · **the value.js R1 blast radius through this subtree is ZERO**, and the one grammar edge reached (`parseTimingFunction`) is consumed on the result contract with diagnostics surfaced — the best posture available."* | KF.W2 (its B23) | **UNREACHABLE, mechanism named** — an **independent second-seat re-run** of §B.3's six-hit grep (this wave's measurement is corroborated, not novel) + the channel-(ii) *best-posture* reference shape |
| **X-13** | **kf-CubeTarget challenge-C S-1** (superlative #11) ⟨`kf-CubeTarget.md:76`⟩ | *"A negative established on three structural legs (colour rides `var(--face-N)` and never becomes a parsed string; the only parse leaves are `"0deg"` and computed-number templates; the sole value.js edge is the certified grammar-free `/math` leaf) with its one near-miss named (`"ease-out-back"` → `parseTimingFunction` rejects → registry resolves)."* Sustained through two hostile re-reads. | KF.W2 (its B22) | **UNREACHABLE, mechanism named — the highest-evidence negative in the corpus**; the model for proving absence with structure rather than with a grep that found nothing |
| **X-14** | **kf-DemoGlobalChrome C-10** ⟨`kf-DemoGlobalChrome.md:77`⟩ | *"INFO · zero value.js/engine edges; R1 reachability CLEAN … the parent's `@mkbabb/value.js/math` edge is at `AnimationControlsGroup.vue:125` … and **`/math` is not the parse surface**. Identity guard: respects value.js's banked R1 row; **recorded so parser waves skip this file**."* | KF.W2 — adopted at its **§Carry F0 verified-negative register** (R-1b #13), *do-not-re-probe lock quoted there* | **UNREACHABLE, mechanism named** |
| **X-15** | **kf-SquareScene C · S-C** ⟨`kf-SquareScene.md:132`⟩ | *"**`parseCssScalar` at the two-writer boundary** (C S-C) — the demo's best use of value.js and the counter-example to the library's own internal regex re-parsing; **worth citing upward when the parser wave lands**. (Its unit-blindness is L-10; the shape is still right.)"* | KF.W2 — **F0 positive exemplar** (R-1b #14), cited upward as the bank instructs | **REACHABLE-but-correct**: the map's *good-shape* exemplar in channel (i); the wave that the bank told to cite it upward is this one, and it now does |
| **X-16** | **kf-KeyboardShortcutsModal C-§1 / R-B R1-negative** ⟨`kf-KeyboardShortcutsModal.md:78`⟩ | *"CLEARED — the R1 crash class is not on this component's graph; same containment posture as the banked kf-CSSPasteDialog row. **Do not re-file.**"* (R-B independently re-traced 62/11/1) | KF.W2 — F0 verified negative (R-1b #15) | **UNREACHABLE** — cross-declared here so neither parser wave assumes the other holds it; the *do-not-re-file* lock travels with it |
| **X-17** | **kf-SpringTrace D-12 = L-6 = C-4** ⟨`kf-SpringTrace.md:56`⟩ | *"silent-swallow error posture: an unparseable stop becomes `{v: 0}` — indistinguishable from a legitimate baseline datum — while the header still counts it as plotted … violates the engine's own fail-explicit law (`easing.ts:13-16`). **NO-WAVE-OWNER (one fail-explicit cure for both mechanisms).**"* | cure = **NO-WAVE-OWNER** (bank, unchanged); **posture census entry = KF.W2's F0 posture registry** (R-1b #16, which moves G-W2-1's denominator from 8 to ≥11) | **POSTURE, not reachability** — cross-declared, never adopted: **L-5** gives posture to other waves. Counted in the denominator as a posture datum so the map's classification is exhaustive |

**E-15 · SURPLUS-NEGATIVES SUB-LINE — THE EIGHT BANKED R1-CLASS NEGATIVES THAT ROUTE NOWHERE, booked as *surplus, owner **KF.W2** (its §Carry **F0** posture/negative register)*.**
*(Added at repair round 2 per RULINGS-2 **R2-11**'s sibling directive — the one lawful substantive touch of this CLEAN spec — and it is simultaneously the cure for PASS-2/KF-W3-CHECK **D-02**. **Citation idiom (R2-7)**: the **banked id is the anchor**; record line numbers ride parenthetically, non-load-bearing, resolved at this writing.)*
**The defect cured.** E-15's table admits into G-KF3-7's denominator three negatives the bank gives **no wave a cure for** — **X-13** (a superlative-register entry, no disposition cell at all), **X-14** (*"recorded so parser waves skip this file … **No wave.**"*), **X-16** (*"Do not re-file"*). *(Round-3 note: admission and routing are now separate questions — the payload rule admits; the two-clause law above routes. X-14 is markup-routed, X-13 instrument-routed, X-16 surplus; all three are in the denominator, which is what this sub-line was always about.)* By that admission rule, eight structurally identical banked negatives belong in the same denominator and were absent — one of them, **kf-CSSPasteDialog C-4**, named inside X-16's own admitted quote (*"same containment posture as the banked kf-CSSPasteDialog row"*). This line's own law is *"a map that omits its own negatives cannot know what completeness is, and cannot be audited for it"* (G-KF3-7); an admission rule applied to three rows and withheld from eight of the same shape **is** that failure, wearing a table.
**Not a census escape — and the reason is re-cut at repair round 3 to the two-clause law above, because the markup half alone never was the reason (D-1).** Measured at the bank by this seat, 2026-08-28: for all eight records `grep -c` returns **0** on every routing markup this spec's census uses — `KF.W3` · `KF.W2/W3` · parser-lane prose (`parser lane` · `parser wave` · `parser ingress` · `parser consumption`) — **which by itself proves nothing, since X-12 and X-13 measure 0 as well.** What makes these eight surplus is the **second** clause: none carries an **instrument** finding. Each is a finding about **its own component's exposure** — *"NOT reachable from this component"* (LP-24) · *"is CONTAINED on this path"* (C-4) · *"this component's one value.js edge"* (C's K-5) · *"unreachable by module topology"* (#3) · *"does not reach this subtree"* (★S-7) · *"structural immunity … by construction"* (S+3/S-d) · *"exposure delta … is exactly zero"* (C-10) · *"the closure is value.js-free by construction"* (S-A) — and none re-runs this map's roster or rules its method, which is what routes X-12 and X-13. On both clauses together the eight are surplus, **`routedTotal` stays 24** by the auditable arithmetic 22 + 2, and §Carry's `9 + 5 + 10 = 24` is untouched. Ids are the bank's and are never renamed; bytes are quoted, never paraphrased; **no severity is re-graded and no cure is spent**; every *do-not-re-file* / *do-not-re-open* lock travels intact.

| banked id ⟨record · line at this writing⟩ | banked bytes (verbatim) | role in the G-KF3-7 map |
|---|---|---|
| **C-4 containment** ⟨`kf-CSSPasteDialog.md` · `:76`⟩ | *"the value.js R1 throw class is CONTAINED on this path (every throwing call awaited inside `importCSS`'s try; the R1 crash surface at lane-library.md:243 is NOT on this path). Cleared — do not re-file."* | **UNREACHABLE by containment, mechanism named** — the row X-16's own admitted quote points at, and the **second** positive containment posture in the corpus (see correction (i) below) |
| **LP-24** ⟨`kf-LayerConfigPanel.md` · `:79`, fold restated `:98`⟩ | *"The value.js exposure is dev-graph only; **R1 (`parseCssColor` crash class) is NOT reachable from this component** — an explicit verified NEGATIVE, folded by reference onto the banked R1 identity **so no later lane re-opens it**"* | **UNREACHABLE, mechanism named** — dev-graph-only exposure; booking it here is what honours the *"so no later lane re-opens it"* lock instead of leaving the lock unenforced |
| **C's K-5** ⟨`kf-KeyframesAddDialog.md` · `:91`⟩ | *"**`R1` value.js throw class** (C's K-5): CONTAINED — this component's one value.js edge (submit → `resolveKeyframes`) is the diagnostics-returning path, not the R1 crash surface. Cleared — do not re-file."* | **UNREACHABLE, mechanism named** — the *diagnostics-returning-path* shape in channel (i) |
| **#3 (C S★1 / SUP-B's surviving half)** ⟨`kf-OrbitalDrag.md` · `:132`⟩ | *"a 1,110-byte zero-import leaf making the R1 crash class unreachable by module topology while a sibling scene took the exposed edge"* | **UNREACHABLE by module topology** — and its *"a sibling scene took the exposed edge"* clause corroborates §Carry **C-5**'s square-scene reachability **from the negative side** |
| **★ S-7** ⟨`kf-SpringPhysicsFacet.md` · `:107`⟩ | *"the value.js subpath is the minimal correct one, and **the R1 crash class does not reach this subtree** … `@mkbabb/value.js/math` = nine pure functions, zero imports; every user-driven parse below the facet runs through `withErrorToastAsync` with Retry; the single exception is machine-fed"* | **UNREACHABLE, mechanism named** — and the **third** containment posture (correction (i)); the `/math`-is-not-the-parse-surface shape X-14 also uses |
| **S+3 / S-d** ⟨`kf-TimelineHoverPreview.md` · `:112`⟩ | *"zero `@mkbabb` imports in the SFC; the entire closure is one erased `import type`, one hop, honouring the published surface in a cluster banked for reaching into library-private `src/` … and **structural immunity to the R1 crash class by construction**"* | **UNREACHABLE structurally** — the zero-runtime-edge shape; its published-surface clause is a live cross-datum for KF.W8's G1 (declared, not re-homed) |
| **C-10** ⟨`kf-AnimatedText.md` · `:105`⟩ | *"the R1 crash-class **exposure delta** of adopting `splitText` **is exactly zero**: one value.js edge, allow-listed with a specifier-end lookahead, 1110-byte artifact with zero import specifiers"* | **UNREACHABLE — a delta-zero negative**: the corpus's only R1 negative stated as a *change in exposure* rather than a state, which is the exact shape the repin's attribution arm (G-KF3-3) reasons in |
| **S-A (C)** ⟨`kf-SpringTrace.md` · `:108`⟩ | *"the light/heavy boundary is honoured and **the R1 crash class is provably unreachable** — the closure is value.js-free by construction (sample.ts:16), proven by enumeration. **Cure note (this seat's)**: the N-2 route imports runtime `@mkbabb/value.js/easing` and spends the "zero value.js imports" cell — R1 (`/css`) stays unreachable either way; **the adopting spec must state this trade**."* | **UNREACHABLE, proven by enumeration** — carrying an **obligation on a receiving spec**, discharged here by naming where it lands: the trade-statement binds whichever spec adopts the **N-2** cure route (bank: **NO-WAVE-OWNER**, packet with D-1/C-2), **not this wave**, which spends no cure on any row of this line (L-5) |

**E-15 · SURPLUS-NEGATIVES SUB-LINE II (REPAIR ROUND 4) — THE SIX FURTHER BANKED R1-CLASS NEGATIVES THE MARKUP-FIRST SWEEP COULD NOT SEE, booked as *surplus, owner **KF.W2** (its §Carry **F0** posture/negative register)*.**
*(Added per RULINGS-4 §END KF-W3 **D-1**; it cures PASS-4/KF-W3-CHECK **D-1**. Same citation idiom (R2-7): the **banked id is the anchor**; record line numbers ride parenthetically, non-load-bearing, re-resolved at write time by `sed -n '<n>p'`.)*
**The defect cured, and its mechanism.** Round 2's sub-line enlarged the *admitted set* without enlarging the *sweep that finds it*: every prior pass swept **markup first** and read payloads only inside the records the markup surfaced, so a banked R1 negative in a record carrying no routing token was invisible by construction. Run the payload sweep over **all 58 records independent of markup** and six more negatives of exactly the admitted shape appear — each a superlative- or roster-register entry, which is precisely the register this line already admits from (★S-7, #3, S+3/S-d, C-10, S-A all are). **A map that omits its own negatives cannot know what completeness is** (G-KF3-7) — and an admission rule applied to twenty rows and withheld from six of the same shape **is** that failure, one register wider than the table round 2 convicted.
**Receipts, every command re-run at write time (2026-08-28, repair round 4; LAW D(3)).** `ls kf-*.md | wc -l` → **58** · payload sweep `grep -l '\bR1\b' kf-*.md | wc -l` → **41 records**, from which the six below survive by reading (subtract the 22 the routed/surplus tables already touch, and the records whose `R1` mentions are header taxonomy or method prose). **Not a census escape**: `grep -c` per record over every routing markup this spec's census uses returns **0 on all five markups for all six records** —
```
kf-MbabbMenu  kf-SequenceScene  kf-SpringHeatmap  kf-MatrixEditor  kf-EditorHeader  kf-AnimationVisualizer
KF.W3=0  KF.W2/W3=0  KF.W3-SHIM=0  parser lane|wave|ingress|consumption=0  megatranche R1=0     (each, all six)
```
— and none carries an **instrument** finding (none re-runs this map's roster or rules its method, which is what routes X-12 and X-13), so on both clauses of the law above the six are **surplus**: **`routedTotal` stays 24**, §Carry's `9 + 5 + 10 = 24` is untouched, and the arithmetic below is restated in the same motion rather than silently. Before this sub-line `grep -c` in this spec returned **MM-37 → 0 · SequenceScene → 0 · SpringHeatmap → 0 · MatrixEditor → 0 · EditorHeader → 0 · SUP-7 → 0** (the two record-*name* hits that did return were unrelated: `AnimationVisualizer` = the KF-AV-28 rider anchor at §Sequencing L-9; `MbabbMenu` = kf-ChromeDock M-4's MUST-CARRY rider at E-8) — six unbooked negatives, now booked. Ids are the bank's and are never renamed; bytes are quoted, never paraphrased; **no severity is re-graded, no cure is spent, no row is re-homed** (L-5).
**Completeness of this sweep is NOT certified in this spec's voice (LAW B).** The denominator is stated as a **floor** — RULINGS-4 ruled **≥ 26** and this file measures **27** (arithmetic below, counting rule with it) — and the instrument that tests it is the next conformance pass, which under RULINGS-4 **R4-10(1)** tests **by scope, not by instance**: the sweep's enumeration source is the corpus, never the example list a ruling happened to name. If a later pass's payload sweep surfaces a further banked negative of this shape, it enters **here**, with its owner unchanged, and the arithmetic is restated in the same motion — never silently (discipline (v)).

| banked id ⟨record · line at this writing⟩ | banked bytes (verbatim) | role in the G-KF3-7 map |
|---|---|---|
| **MM-37** ⟨`kf-MbabbMenu.md` · `:93`, fold restated `:131`⟩ | *"the value.js R1 `parseCssColor` class is in the bundle graph via `/dock` but on no reachable path: dock.js imports only the numeric guards (`n`,`t`), never the `i` wrapper; both readers re-traced byte-for-byte, concordant \| INFO — exposure-not-reachability, ≡ the census R1 line; dead-weight note rides the bundle row"*; `:131` *"**MM-37 ≡ the census R1-exposure line**"* | **UNREACHABLE, mechanism named — the map's SECOND ROOT NODE** (X-06 is the entry-graph negative; this is the **bundle-graph** one, measured in the *shipped chunk* rather than the module graph) **and the corpus's only row that separates *exposure* from *reachability* by name** — the distinction the whole UNREACHABLE side of this map turns on, and the one X-14's *"`/math` is not the parse surface"* shape assumes without naming |
| **C S★-2** (superlative 4) ⟨`kf-SequenceScene.md` · `:119`⟩ | *"**The value.js edge is the narrowest possible leaf and R1 is structurally unreachable** — four imports, all `clamp` from `/math`; the 4.0.0 exports map has no root entry; no colour string ever enters the engine. Package-enforced, not discipline-enforced. *(C S★-2 ≡ banked kf-SequencePlayhead superlative 7.)*"* | **UNREACHABLE, mechanism named** — the `/math`-is-not-the-parse-surface shape (X-14, ★S-7), stated in its strongest form: **package-enforced by the exports map**, not by discipline. Its *"no root entry"* clause is the same fact §B.5 census 1b measures from the other side (79 exports, seven subpaths, no root) |
| **superlative 7** ⟨`kf-SpringHeatmap.md` · `:101`⟩ | *"**The narrowest possible value.js surface** — one pure function (`clamp`) by exact subpath on the repo's sole declared runtime dependency; `/css`/`/color` untouched, transitive reach verified absent — **the R1 parser-crash class is structurally unreachable from this component**."* | **UNREACHABLE, mechanism named** — ★S-7's shape verbatim, with the **transitive** leg measured (*"transitive reach verified absent"*), which is the leg an import-graph inventory reports and a call-map must prove |
| **superlative 7 (r1)** ⟨`kf-MatrixEditor.md` · `:139`⟩ | *"**The value.js edge is type-only and R1-clean** *(r1)*: erased `import type` under `verbatimModuleSyntax`; no parser entered."* | **UNREACHABLE structurally** — the S+3/S-d shape (*"one erased `import type`"*): a **zero-runtime-edge** negative, the class an import-graph census counts as a consumer and the CALL-graph predicate of G-KF3-7 correctly does not |
| **C S-A** ⟨`kf-EditorHeader.md` · `:95`⟩ | *"**C S-A — proving a negative**: the full import-closure walk that **DECLINED an R1 parser-crash claim** and enumerated the chunks through which glass-ui does reach value.js to justify the refusal."* | **UNREACHABLE — a DECLINED claim, method-bearing**: X-13's *prove-absence-with-structure* shape applied to a **producer-mediated** edge (glass-ui → value.js), the one path this wave's own denominator never enumerates. It is also the corpus's second declination of an R1 claim against interest, beside KF-HA-19 at E-14.5 — recorded together so the map's negative side is not read as unanimity manufactured by silence |
| **SUP-7** ⟨`kf-AnimationVisualizer.md` · `:124`⟩ | *"the value.js edge is textbook (`:46` `clamp` from `/math`): **parser-free subpath (no R1 crash class in this call graph)**, correct arity at both sites, necessary (the kf barrel exports no clamp — grep 0), and the one `@mkbabb` dep that is declared AND locked."* | **UNREACHABLE, mechanism named** — ★S-7 / X-14's shape, and **the only one of the twenty-six stated in CALL-GRAPH terms** (*"no R1 crash class in this call graph"*), which is G-KF3-7's own predicate quoted back at it from the bank. *(Its record is already cited in this spec at §Sequencing L-9 for the **KF-AV-28** rider — a different row of the same record, not a double-booking; the kf-SpringTrace precedent at correction (ii) governs.)* |

**Arithmetic, stated separately so neither can be read off the other.** **Restated at repair round 4 (RULINGS-4 §END KF-W3 **D-1**), with its counting rule stated AT the enumeration (LAW D(3)).** E-15 now books **twenty-six** ids: the **ten** routed exclusions-with-named-owner (**X-06 … X-15**), counted inside the routed 24 — plus **sixteen surplus** (**X-16**, **X-17** in the table above; the **eight** of sub-line I, repair round 2; the **six** of sub-line II, repair round 4), counted in G-KF3-7's denominator and **never** in the routed census.
**G-KF3-7's denominator is therefore TWENTY-SEVEN, against the ruled floor of ≥ 26.** *Counting rule*: **every id entered in the map's denominator by the payload admission rule, wherever it is booked in this file** — E-15's twenty-six **plus `KF-HA-19`**, booked at **E-14.5** and entered on the surplus side by that line's own sentence. The floor of 26 that RULINGS-4 ruled counts E-15's twenty plus this round's six; the twenty-seventh is E-14.5's, which was already in the denominator before this round and is named here so the two readings are reconciled on the page rather than by assertion. **The routed census is untouched at 24** and stays **derived** (repair round 3, re-cut at round 4): **(R-i) payload-admissible ∧ markup-positive: 9 + 5 + 8 = 22 · (R-ii) instrument-routed X-12 + X-13 = 2 · total 24** — the same **9 + 5 + 10 = 24** §Carry states, reached by two stated commands and one declared reading instead of by hand.
**Carriage over that 24 is not claimed in this file's voice (LAW B)** — it is measured at `docs/tranches/X/keyframes/conformance/PASS-5/KF-W3-CHECK.md` §1/§2 (2026-08-28), **the one artifact of record and the one this round repairs against** (re-pointed by scope at repair round 5, RULINGS-5 §END KF-W3 **D-3**): **24 routed · 24 booked id-for-id · 0 escaped**, with its axis 1 PASSING for the first time in five passes. *(Its predecessors `PASS-4/KF-W3-CHECK.md` and `PASS-3/KF-W3-CHECK.md` §1/§2 each reported the same three figures from a wholly independent by-record derivation — **three instruments, one arithmetic, agreeing at every step**, which is why every pass-3/4/5 verdict is **not census-fatal** and their defects are admission-rule, declaration and receipt defects, never carriage escapes. The round-4 findings this sub-line cures are cited by their own prefixes: **PASS-4 D-1** (this cure) and **PASS-4 D-2** (the membership law above); the round-5 finding against this file's own receipts is **PASS-5 D-1**, cured at (R-i).)*
Across the twenty-seven the map resolves as **twenty-one UNREACHABLE-with-the-mechanism-named** (G-KF3-7's seven · sub-line I's eight · sub-line II's six) · **three REACHABLE** (X-07 · X-09 · X-11) · **one REACHABLE-but-correct** (X-15) · **one posture** (X-17) · **one DECLINED-FRAMING record** (KF-HA-19, E-14.5 — a negative *about the framing of the class itself*, which is why it is counted apart from the twenty-one rather than folded into them) — **21 + 3 + 1 + 1 + 1 = 27**.
**Two corrections this sub-line forces on this spec's own text, recorded rather than absorbed.** **(i)** **X-10's superlative is NARROWED**: *"the corpus's ONLY positive containment posture"* holds of the **routed** ids only — the surplus side carries two more (kf-CSSPasteDialog **C-4**'s `try`-wrapped `importCSS`; kf-SpringPhysicsFacet **★S-7**'s `withErrorToastAsync`-with-Retry). X-10 (an ingress wrapped `await`-inside-`try`) remains the reference shape the **routed** REACHABLE/UNREACHABLE classification is calibrated against; the corpus-wide claim does not survive the rows this sub-line puts on the page, and is corrected at both of its sites. **(ii)** **kf-SpringTrace is booked twice, on two distinct banked rows, and is not double-booked**: **X-17** books the `:56` silent-swallow **posture** row (D-12 = L-6 = C-4); the surplus row above books the separate `:108` **S-A** structural negative. Same record, different ids, different roles, one owner (KF.W2 F0).

**Discipline on this line.** (i) Every row is an **exclusion**, not a carry: this wave spends **no cure** on any of them and re-books none as a §Carry row (anti-double-booking; the ids stay where the bank put them). (ii) Every row is **counted** in G-KF3-7's denominator: exclusion of a *cure* is not exclusion from a *census*, and the earlier spec's silence on these ten was a census failure, not a scoping choice. (iii) **Neither parser wave may again assume the other holds the negatives** — the sentence *"the R1 negative/posture census of record is KF.W2's F0"* is stated once at the KF.W2 cross-edge above and once here. (iv) X-07's rider and X-11's row are **load-bearing against this spec's own earlier text** (E-12 and E-8 respectively); both were re-founded at repair round 1, and the rows that falsified them are now on the page rather than in the corpus only. (v) **The eight surplus rows obey (i)–(iii) identically, and add one rule**: *surplus* is a **census** class, never a severity or an ownership class — a surplus row is not counted into the routed 24, not re-graded, and not re-homed here; if a later census finds one of them routed to this wave in a markup this seat did not measure, it moves **into** the routed table with its owner unchanged and the arithmetic above is restated in the same motion, never silently.
