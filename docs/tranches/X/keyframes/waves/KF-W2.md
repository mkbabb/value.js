# X.KF.W2 - Parse Façade

**Wave**: KF.W2 · Parse Façade — one kf-side `parse()` façade collapsing the failure postures over the Tier-A grammar seam; the six Tier-C ad-hoc regexes deleted including the `cssom.ts` identifier injection; the entry-point contract and the R1 ingress census published once. **This wave is the pre-work that makes KF.W3's parser consumption one-touch.** Its product is a *surface plus two registries* (postures, ingresses), not a behaviour change.
**Half**: library (keyframes.js `src/`), with a declared demo-side census arm. **Sub-tranche**: X·KF (SS-1 library half), authority `docs/tranches/X/COHESION.md` §0/§1, ratified under `docs/tranches/V/megatranche/SCOPE.md` **M-25**. Validated at authoring against `docs/precepts/instructions/tranche/WAVE_SPEC.md` (**L-20**).
**Status**: **planned**. No product byte is written by this file; authoring it opened no product source for writing. Execution awaits the owner's begin-word.
**Alias law (R-B idiom)**: `KF.W2` denotes `X.KF.W2`; `X.KF.W2.<x>` is the WAVE_SPEC §5 unit form. Search alias, never a rename. Spec home: `docs/tranches/X/keyframes/waves/KF-W2.md` — the in-tree sibling convention (KF-W4.md, KF-W7.md). The drafting seat's `docs/tranches/X/kf/waves/KF.W2.md` target came from an artifact that **does not exist in-tree** and is struck (§Provenance; RULINGS **R-12**). No file is re-glyphed.
**Namespace disambiguation (repair round 1; KF-W2-CHECK D-8)**: two taxonomies collide on the glyph `KF.W2` across the registry — **(i) X·KF / CENSUS-2026-08-03 §(a)**, `W2 Parse Façade` = **this spec**; **(ii) lane-frontend §10**, `KF.W2-TABS` (`kf-ChannelControls:17`, `kf-KfPillTabs:19`, `kf-AnimationControlsGroup:17`, `kf-ControlsPaneWrapper:6`, `kf-DemoGlobalChrome:17`, plus kf-EditorShell **L-15/C-9**). The **~25** rows routed to the string `KF.W2-TABS` are **not this wave's and are not escapes from it**: **`KF-W6` §Taxonomy** binds — re-quoted verbatim at the current bytes (repair round 2; the round-1 rendering moved the parenthetical inside the quotation and is corrected under M-25): *"**Taxonomy (binding, adjudicated KF-W4 §11 ¶1)**: lane-frontend §10's KF.W2-TABS / W3-SHIM / W5-PARTIALS / W7-TOKENS all COLLAPSE INTO THIS WAVE"*. **The §-heading is the anchor** — the round-1 receipt cited `KF-W6.md:10`, which now holds that spec's own repair-round-2 header (the sibling was re-written this round; a bare line number in a live repair round is a receipt with a half-life) — **re-verified at round-2 close (U-2): `KF-W6.md:10` still resolves to that round-2 header, and the strike stands regardless, on the rule**. Recorded here so a `grep -nE 'KF\.?W2'` over the 58 records (70 hit lines) is separable from this file rather than from a sibling's, and so no later seat re-homes the TABS mass by accident.
**Ref of record (KF.W0 §B-12 / MISS-α5)**: keyframes.js **`origin/master` `81a56990736ced5b5edde0b84c527680ac7689b1`** or later. Local HEAD `8281638c` is DISQUALIFIED as witness substrate (banked kf-App ruling 1); measured this sitting: `HEAD 8281638c` / `origin/master 81a56990` / merge-base `a59d3a22`, neither head an ancestor of the other. Every anchor below was **re-resolved against `origin/master` by this seat** and the drift is tabled in §Bounds — it is not cosmetic (**seven** banked paths no longer exist; the seventh was found at repair round 2). D-19 re-resolution before any cure still binds.
**Provenance (restated at repair round 1 — RULINGS R-12; denominator re-derived at repair round 2 — RULINGS-2 R2-11/R2-5(g))**: authored **from the 58-record adjudicated registry directly** (`docs/tranches/V/megatranche/registry/adjudicated/`); the routed denominator is the registry's — **62 routed cells**, re-derived by a **58-record** sweep at repair round 2 under PASS-1's own routing rule (terminal disposition in any markup: table cells, prose routings, dotted `KF.W2/W3` forms, `cross-ref KF.W2's registry` posture edges, and R1 boundary cells recorded *"so parser waves skip this file"*). The round-1 figure of **47** was a **37-record** measurement adopted as if it were a corpus census; it is **struck as a denominator and preserved as a dated sub-count** (E-3 — the round-1 text below is corrected in place, the PASS-1 check file is never edited). *A register measured over a sample may not be published as a closure claim* is the lesson the round-2 register exists to make structural: G-W2-1 and G-W2-5 now assert over the **58**, and their basis is named in the gate. **There is no KF-W2 CARRY artifact in-tree** (`docs/tranches/X/keyframes/carry/` holds exactly one file, `KF-W6-CARRY.md` — 201 rows / 54 records); the drafting seat's "11 rows, 11 carried, 0 dropped" fold-verification cited a ledger that does not exist and is **STRUCK**, here and at §Carry and §Excluded. What the drafting seat called "the CARRY" is agglomeration input, never provenance and never a denominator (M-25: the records are the authority). Its `boundsFiles` set survives as *measurement* because this seat re-resolved every path against `origin/master` itself (§Bounds). Repairs applied at authoring: G-W2-2's check re-scoped from specifier-count to symbol-set; the `coerceToSyntax` stray runtime import recorded; the `parseKeyframeSelector` diagnostic-code precision. Every gate carries a named live witness (**L-19**); the R1 witness and the entry-point contract witness were **executed read-only by the fold seat** on 2026-08-28, not inherited.
**Repair round 1 (2026-08-28)**: this file was repaired in place against `conformance/PASS-1/RULINGS.md` (directives **R-1b · R-10 · R-12 · R-16**, executed verbatim) and its own defect register `conformance/PASS-1/KF-W2-CHECK.md` (D-1..D-10). Status stays `planned`; no stamp is moved; every carried row keeps its banked id (anti-rename).
**Repair round 2 (2026-08-28)**: repaired in place against `conformance/PASS-2/RULINGS-2.md` (directives **R2-5(b) · R2-6.1 · R2-11**, executed verbatim; the **R2-5 witness re-derivation law** and the **R2-7 citation idiom** bind everything below) and its own PASS-2 register `conformance/PASS-2/KF-W2-CHECK.md` (D-1..D-10). **Every measurement in this file was RE-EXECUTED at the substrate of record by the round-2 seat** — keyframes.js at `origin/master 81a56990`, value.js at the current tree bytes, the F0 matrix against the installed `@mkbabb/value.js@4.0.0` dist — and none is inherited from a ruling's prose, a check file, or this spec's own prior text. Cross-spec receipts now cite **stable anchors** (§-heading, gate id, OP id, or banked id); any line number riding beside one is declared **non-load-bearing**. Status stays `planned`; no stamp is moved; anti-rename holds.

| verb | value | evidence |
|---|---|---|
| AUDITED | **YES — the WHOLE 58** | `registry/adjudicated/`, **58 records** — the full corpus, re-enumerated at repair round 2 (`ls kf-*.md \| wc -l` → 58 at the current bytes; the 37-record basis is **superseded, not amended**). **37 audited through repair round 1**: kf-CSSCodeEditor · kf-KeyframeTimeline · kf-App · kf-EditorShell · kf-KeyframesEditor · kf-KeyframeCardList · kf-CSSPasteDialog · kf-KeyframesAddDialog · kf-TimelineCaret · kf-HeroAurora · kf-SquareInstrument · kf-ChannelOptions · kf-CopyButton · kf-SpringTrace · kf-CubeAxisLines · kf-TimelineTrack · kf-KeyframeCard · kf-LayerConfigPanel · kf-MbabbMenu · kf-CubeTarget · kf-ChannelControls · kf-KeyboardShortcutsModal · kf-DemoGlobalChrome · kf-SquareScene · kf-KeyframesStringControls · kf-EasingTarget · kf-TimingFunctionPanel · kf-CubeScene · kf-AnimationControlsGroup · kf-AmigaScene · kf-AnimatedText · kf-AnimationVisualizer · kf-SpringPhysicsFacet · kf-OrbitalDrag · kf-TimelineHoverPreview · kf-EditorStartScreen · kf-TypingDots. **+ the 21 swept at repair round 2** (the remainder, derived by set difference at the bytes, not asserted — this is the mechanism behind the PASS-2 escapes): kf-App.skeleton · kf-ChromeDock · kf-ControlsPaneWrapper · kf-EasingScene · kf-EasingSidebar · kf-EditorHeader · kf-KfPillTabs · kf-MatrixEditor · kf-PlaybackRibbon · kf-RibbonBar · kf-SequenceAxis · kf-SequencePlayhead · kf-SequenceScene · kf-SequenceScrubber · kf-SequenceTarget · kf-SharePopover · kf-SpringHeatmap · kf-SpringScene · kf-SpringTarget · kf-StartingStyleTarget · kf-TransportDock. 37 + 21 = 58, closed by `comm` against the directory listing. + `formation/keyframes/lane-library.md` §4.1/§4.3/§4.4/§4.6/§4.7/§7.5 |
| SPECIFIED | **YES — this file** | the registry's **62 routed cells** accounted by id — **re-derived from the 58-record sweep at repair round 2, never incremented from a prior pass's register** (R2-5(g)): **28 carried at authoring + 19 adopted at repair round 1** (R-1b's four, the KSC posture pair, the nine *further* R1 negatives, the two easing-organ cells, the CubeScene residual, the SquareScene crash-surface annotation) **+ 15 adopted at repair round 2** (**X1–X9** the nine R1-boundary negatives in records this wave had never audited-or-audited-but-missed · **X10–X14** the five further failure-posture cells · **X15** the positive-posture exemplar) = **62 accounted, 0 escaped**; arithmetic reconciled at §Carry, no cell counted twice; §Excluded states every non-carry with its reason and its named owner |
| IMPLEMENTED | **NO** | gates green + bytes landed at the named execution site stamps this |
| VERIFIED | **NO** | stamped only at X·KF's close; no wave stamps VERIFIED at its own close |

**EXECUTION IS NOT AUTHORIZED BY THIS FILE.** Tranche X's execution gate stands: nothing here opens, writes, builds, or runs against product source until the **owner's begin-word**. Status fields stay `planned`. Write authority for keyframes.js is UNRESOLVED (OP-1). This file was authored read-only against both trees.

---

## Goal criterion

This wave succeeds if, when work ends, **there is exactly one module in `src/animation/**` that speaks to value.js's grammar, exactly one declared thing that happens when a CSS string is bad, and one published answer to "which grammar belongs at this seam"** — so that KF.W3's repin is an edit to one file, and so that no later seat has to re-discover, for the fourth time, which value.js entry throws on what. A wave that adds a wrapper and leaves the twenty-one call sites and the **sixteen enumerated postures** standing has failed this goal. (The integer is the registry's floor, re-derived over the whole 58-record corpus at repair round 2 — §Carry F3 / G-W2-1; the charter's "five", the drafting seat's "eight" and round 1's "eleven" were each refuted by the bank, the last of them because it was measured over 37 records and published over 58. **The floor is a floor, and its basis is now part of the gate.**)

**Execution shape**: 4 Opus seats, 3 phases — phase 1: 1 serial (`.a`, the census + reconciliation + posture registry: the two registries size everything after them); phase 2: 2 parallel, file-disjoint (`.b` façade + Tier-A repoint; `.c` Tier-C deletion + the cssom cure); phase 3: 1 serial (`.d`, the round-trip net, the fuzz corpus extension, the §F-2 addendum, close). Peak concurrency 2 — inside the owner's four-workflow cap.

---

## §0 Open preconditions (checked at wave-open; NOT close gates)

| # | precondition | state at authoring (2026-08-28) |
|---|---|---|
| **OP-1** | **Write authority for keyframes.js is named.** Census §(c) gap 5: kf is sibling-read-only under value.js law and the sacred-checkout rule; no kf-side formation owns these waves. | **UNRESOLVED.** This wave names the question; it does not answer it. Required: the owner's dated word naming the execution site and the hand. Inherited verbatim from KF.W4 OP-1. |
| **OP-2** | **KF.W0 §B-12 substrate settle.** origin/master pins glass-ui `"7.0.0"` exact devDep (verified at `package.json:77`; = installed) while HEAD declares `"6.0.0"` under `optionalDependencies` (verified at HEAD `package.json:71`), so `npm ci` at HEAD silently downgrades. | **OPEN.** Measured: HEAD `8281638c`, origin/master `81a56990`, neither an ancestor. The wave **states its ref in its opening commit** and re-resolves every anchor (D-19). A gate authored against `8281638c` is void. |
| **OP-3** | **R1 entry-point reconciliation** — which value.js entry actually throws (the registry's blocking evidence dependency — three seats, one entry point, two outcomes: kf-HeroAurora KF-HA-19 vs kf-EditorShell C-19/RR-2 vs kf-SquareInstrument K-6). | **DISCHARGED AT AUTHORING by this fold seat.** Executed read-only against `node_modules/@mkbabb/value.js/dist/subpaths/css.js` (**4.0.0**, the installed pin, version re-verified) on 2026-08-28. Matrix in §Carry F0/R1 and gate G-W2-5. The L-19 lock is lifted **as corrected**, not as inherited. |
| **OP-4** | **KF.W4's vue-tsc gate** (KF-CE-16 ≡ KF-APP-4, "the sequencing head of the whole record") — no `.vue` file is typechecked anywhere; `check` = `tsc --noEmit && tsc --noEmit -p tsconfig.test.json` (+ `proof:structure`), no vue-tsc in any script; CI merge runs `check:lib` = `tsc --noEmit -p tsconfig.lib.json` over `src/` only. | **ABSENT.** Binds the **demo-side arms only**. Every `src/` arm of this wave is covered by `check:lib` and proceeds independently. Also needed: KF-CE-12's **depcruise-over-demo** arm at KF.W4, because KF-KE-54 proves the current graph gate scopes `src/` only. |
| **OP-5** | **KF.W3 owns the repin.** `4.0.0 → 4.1.x`, the `serializeCssValue` fork retirement, the DUAL-1 unification, the 8-curve delta. | **BY CONSTRUCTION.** **KF.W2 MUST NOT REPIN.** Every gate here is measured against the installed 4.0.0 and must stay green or be re-rowed at the repin — the façade is the thing that makes that re-rowing one file. |
| **OP-6** | **KF.W8 ordering for the serializer publication** (MISS-β2, joint with banked C-12). | **ORDERED: KF.W8 precedes (RULINGS R-16).** The banked routing decides it — `kf-TimelineCaret.md:46`, *"→ KF.W8 (publish-or-relocate, joint with banked C-12)"* — so **KF.W8's row 10 / G3 is the publication ACT and comes first**; G-W2-8 gates the **façade-side contract only** and **arms if-and-only-if W8 has not preceded**. That is this spec's own stated alternative, now the declared order rather than a mutual conditional (the W2↔W8 circle is broken at the W8 end). G-W2-8 stays **conditional in form** — a gate whose act belongs to another wave is contrivance if declared unconditionally (L-19) — but its condition is now a **named order**, not an open question. |

---

## §Bounds

All paths under `/Users/mkbabb/Programming/keyframes.js/` unless prefixed. **The drafting seat's `boundsFiles` set was reconciled against `origin/master`; SEVEN named paths do not exist at the ref of record** (each `git cat-file -e` checked, both spellings). Provenance note (R-12): that set is **agglomeration input, not a citeable ledger** — what makes the table below authoritative is that every left-hand path was re-resolved here, and every right-hand path re-verified present. The drift is load-bearing: a unit dispatched on the stale paths writes nothing and reports success.

**Repair round 2 — the seventh member, and why the table missed it (RULINGS-2 R2-5(b) · KF-W2-CHECK D-2).** The round-1 table asserted *"every left-hand path was re-resolved here"* while `src/animation/resolve/resolve-if.ts` — a path this wave **owns** — was carried unresolved, with the symptom recorded as a line-anchor miss (*"neither found at the banked lines by this seat's grep"*) rather than as what it is: **a HEAD-only artefact with no bytes at the ref of record.** The failure mode this drift table exists to close reproduced inside the drift table. Re-derived by the round-2 seat at `81a56990`:

```
$ git cat-file -e 81a56990:src/animation/resolve/resolve-if.ts   → ABSENT ("exists on disk, but not in 'origin/master'")
$ git ls-tree --name-only 81a56990 src/animation/resolve/        → browser · conditional · core · element-resolve · env · function · index · spring-css  (8 files, no resolve-if)
$ git grep -n legacyClauses 81a56990 -- src
    src/animation/resolve/conditional.ts:27:const legacyClauses = (list: CssList): readonly IfClause[] => {
    src/animation/resolve/conditional.ts:52:        return legacyClauses(fn.args[0]);
$ git show 81a56990:src/animation/resolve/conditional.ts | wc -l → 150
```

The real module is **`src/animation/resolve/conditional.ts`**, and the whitespace canonicalisation the row was written for is its **`:102`** `const normalize = (value: string) => value.trim().replace(/\s+/g, " ")` (read verbatim this round). Re-pathed at §Bounds (owned files), at G-W2-3's assertion, and at §Excluded — the three sites that named the phantom.

### Path drift — drafting-seat `boundsFiles` (worktree/`8281638c`) → `origin/master` `81a56990` (re-resolved 2026-08-28; row 7 added at repair round 2)

| stale path | `origin/master` |
|---|---|
| `src/animation/compile/value-ast.ts` | **`src/animation/compile/value/compile.ts`** (A3 → `:32`) |
| `src/animation/compile/emit/format-options.ts` | **`src/animation/compile/emit/format/options.ts`** (A6 → `:104`) |
| `src/animation/compile/emit/format.ts` | **`src/animation/compile/emit/format/format.ts`** (Tier-C `formatCSSKeyframeString` `:135-144`; `selectorText` `:20`, consumers `:132/:201/:280/:313` per bank) |
| `src/animation/compile/easing/easing-registry.ts` | **`src/animation/compile/easing/registry.ts`** (A5 → `:131`) — the same correction KF.W4 OP-4 already carries |
| `src/animation/engine/css/css-animation.ts` | **`src/animation/engine/css/animation.ts`** (`:176`) |
| `src/animation/compile/interp-slot.ts` | **`src/animation/compile/frame/interp-slot.ts`** (`:325` `serializeCssColor`) |
| **`src/animation/resolve/resolve-if.ts`** *(added repair round 2)* | **`src/animation/resolve/conditional.ts`** (150 L) — `legacyClauses` **`:27`** (declaration) / **`:52`** (the `clausesOf` call), whitespace canonicalisation **`:102`**. **The stale spelling exists ONLY on disk / at the DISQUALIFIED HEAD `8281638c`** — `git cat-file -e 81a56990:…/resolve-if.ts` fails, so a unit dispatched on it writes nothing and reports success, exactly as this table's opening sentence warns |
| `demo/utils/keyframeSelector.ts` | **exists at origin/master** (absent from HEAD's tree) — the ref statement is what makes copy 3 real |

### Owned files

| File | Access | Note |
|---|---|---|
| `docs/tranches/X/keyframes/waves/KF-W2.md` (value.js repo) | create | this file — the only file this wave's authoring writes |
| `src/animation/compile/parse-facade.ts` | **create** | the façade. DECLARED, not created by this file — verified absent at origin/master. |
| `src/animation/compile/adapter.ts` | modify | A1 `:222` `parseStylesheet`; the ABSORB posture `parseSource` `:219-226`; collectors `:205/:241/:341/:374/:377`; `resolveKeyframes` **defined** `:266` (see census correction) |
| `src/animation/compile/value/compile.ts` | modify | A3 `:32` `parseCssValues`; THROW-TypeError posture `:33-38` |
| `src/animation/compile/selector.ts` | modify | A4 `:24` `parseValueSelector` (= value.js `parseKeyframeSelector`, aliased at `:3`); kf's throwing wrapper `:23-35`, `AnimationOptionError` + `EMPTY_PARSE` `:33`; `namedSelectorToFraction` **exported** `:42` |
| `src/animation/compile/easing/registry.ts` | modify | A5 `:131` `parseTimingFunction` |
| `src/animation/compile/emit/format/options.ts` | modify | A6 `:104` `parseTimingFunction`; `:121` calls value.js's `reverseAnimationShorthand` (DUAL-1 half — the unification is **KF.W3's**) |
| `src/animation/engine/options.ts` | modify | A7 `:31` `parseCssScalar` |
| `src/animation/resolve/browser.ts` | modify | A8 `:162` `parseCssScalar` (banked `:165`, drifted). Deep-import extirpation is **KF.W8's** |
| `src/animation/scroll/grammar.ts` | modify | A9 `:109` / A10 `:77` / A11 `:85`; `requireParsed` THROW posture `:57-63`; `roundTripScrollCSS` `:143` (the in-tree replay-equality oracle to generalise); collectors `:111/:112` |
| `src/animation/validate.ts` | modify | A12 `:182` `parseStylesheet`; SWALLOW posture `keyframesNames` `:180-192`; `collectKeyframes` `:186` |
| `src/animation/engine/css/animation.ts` | modify | `:176` `resolveKeyframes` inside `fromString` — **kf's own adapter (imported from `../../compile/adapter` at `:15`), not a value.js entry** (census correction) |
| `src/animation/compile/frame/compiler.ts` | modify-carve | `:146` calls **kf's own** `parseKeyframeSelector` — the façade's first natural consumer; a Tier-A-adjacent site the banked roster does not name |
| `src/animation/resolve/function.ts` | modify-carve | **NET-NEW census cell (this seat's re-derivation)**: runtime import of value.js `coerceToSyntax` — neither a grammar entry, nor a collector, nor a serializer; ruled **delete-or-declare at wave open** (same treatment as the `format/format.ts:341` candidate) |
| `src/animation/ingest/cssom.ts` (466 L) | modify | Tier B: `:28-33` the open VJ-9 self-declared handoff comment (verified verbatim); `:117` `isKeyframesRule`; `:156` the `CSSGroupingRule` duck-type; **`:214-216` THE IDENTIFIER-INJECTION REGEX** (verified verbatim at origin/master) |
| `src/animation/presets/catalog.ts` | modify | Tier C `:16` (`@keyframes` wrapper strip, verified) |
| `src/animation/compile/emit/view-transition.ts` | modify | Tier C `:146` (serialize→regex-reparse of kf's OWN emitted body via `declaredDecls`); `CQ_UNIT_RE` **`:136`** (banked `:159`, drifted); consumers `:202/:207`; `:157` camel→kebab |
| `src/animation/engine/composition.ts` | modify | Tier C **`:175`** (banked `:176`, drifted) — positional numeric extraction from an inline style string, no unit awareness |
| `src/animation/compile/emit/format/format.ts` | modify | Tier C `formatCSSKeyframeString` `:135-144`; **candidate 7th at `:341`** (`.replace(/\(\s*\{/g,"{").replace(/\}\s*\)/g,"}")`, verified) — a de-paren by regex over the emitted block, not in the banked six; the roster re-derives at open |
| `src/animation/svg/draw-svg.ts` | modify | Tier C `:89` (percentage validation by regex instead of `parseCssScalar`, verified) |
| `src/animation/compile/emit/css-text.ts` | modify | Tier D mirror (in-degree 12, the most-imported module per bank): kf's own `reverseAnimationShorthand` `:17`, `serializeTimingFunction` `:30`, `serializeCssValue` `:41` (throws on value.js `!ok`), **`serializeSelector` `:58`** (private; consumer `:75`); runtime-imports value.js `serializeCssColor` (emit half) |
| `src/animation/compile/emit/index.ts` | modify-carve | the export list — MISS-β2's publication decision surface. **ACT is KF.W8's** |
| `src/animation/resolve/conditional.ts` (150 L) | modify-carve | **RE-PATHED at repair round 2 (R2-5(b) / D-2) — the banked `resolve/resolve-if.ts` has NO BYTES at the ref of record; anti-rename is unaffected, this is a coordinate correction, not an id change.** Whitespace canonicalisation **`:102`** (`value.trim().replace(/\s+/g," ")`) + `legacyClauses` (LEG-1) **`:27`** decl / **`:52`** call — all three read verbatim at `81a56990` by the round-2 seat, not re-resolved at open |
| `test/compile/valuejs-contract.test.ts` | **modify** (exists, verified) | where the entry-point contract + posture registry pin |
| `test/compile/grammar-fuzz.test.ts` | **modify** (exists, verified) | see G-W2-5 — its own docblock states it generates *"random VALID @keyframes fragments from MODEL grammars (not raw-string fuzz)"* (verified verbatim); the malformed corpus is an extension, not a new file |
| `test/compile/roundtrip-fidelity.test.ts` | modify-carve | the consumer of the 14-fixture net |
| `test/fixtures/keyframes/` | modify | **verified 14 `.css` + `manifest.json`** at origin/master |
| `test/ingest/` | create (one file) | the cssom identifier-injection fixture (G-W2-4); dir exists with 3 tests (verified) |
| `docs/tranches/X/keyframes/registries/POSTURES.md` · `INGRESS-CENSUS.md` (value.js repo) | create | the two published registries — the wave's durable product |
| `docs/tranches/V/apotheosis/parser-proof/GATE-VERDICT-F2-ADDENDUM-<date>.md` (value.js repo) | create | the §F-2 correction as an **E-3 addendum beside** the pinned authority — never a rewrite (epoch rule); the falsified phrase verified live at `GATE-VERDICT.md:42` |

**Census cell of record, ADDED at repair round 2 (RULINGS-2 R2-11.3 · KF-W2-CHECK D-4) — `demo/components/instrument/keyframes/utils/parseAnimationCSS.ts` (58 L), READ ONLY, no cure here.** The demo's **principal CSS→AST adapter** was absent from every list in this file — not §Bounds, not the census cells, not §Excluded — while the spec cited `timelineEngine.ts:11`, which is *the line that imports it*. A census of record that omits the module its own cited import points at is not a census. Re-read verbatim at `81a56990` by the round-2 seat:

- **`:1-5`** runtime import of **`collectAnimationOptions` + `collectStyleRules`** from `@mkbabb/value.js/css` — calls at **`:36`** (`collectStyleRules(ast)`) and **`:41`** (`collectAnimationOptions(selectedDeclarations).at(0) ?? {}`). Two of the four demo-side **collector** entries in the whole tree.
- **`:7`** `import { serializeTimingFunction } from "@src/animation/compile/emit/css-text"` — a **deep `@src/` import**, the banked C-12/KF-CE-12 breach class. **Declared edge, not a new owner**: it is already inside **KF.W8 §Gates · G1**'s denominator (`git grep -c 'from "@src/' 81a56990 -- demo` → **6 files / 7 specifier lines**, this one among them, re-run this round). This wave books **no** extirpation limb; W8 owns the act.
- **`:32-34`** `throw new TypeError(\`Invalid animation CSS: ${parseIssue.message}\`)` on a `PARSE_ERROR` diagnostic — **a value.js-bearing failure posture on the demo side**, and the one that **KAD-9 / KAD-10** and **KF-KE-3**'s *"fails ~1 s later"* both sit downstream of (`:26` is `async`, `:27` `await loadAnimationEngine()` — the latency the banked row measures).
- **`:22-24`** docblock: *"The engine adapter is the single grammar authority, including its bare-stop-list handling; **this module performs no regex pre-detection or second parse**"* (verbatim) — the **single-grammar-authority invariant already stated in the tree**, and the demo-side statement of exactly what this wave publishes on the `src/` side. It is also precisely the comment class **this wave's own declared commit-family rule** (§Carry F2) governs — recorded, but **not gated here**, because the file is READ-ONLY at this wave and the rule binds only inside §Bounds.

Its posture arm is the **sixteenth** row of the registry's subject matter but is **NOT counted in G-W2-1's floor**: the floor enumerates *banked registry cells*, and this is a tree-side census cell with no banked id. Recorded here so no seat mints one.

**Census cells — READ ONLY, no cure here** (named so the census is complete and no later seat re-probes them; every anchor verified at origin/master by this seat): `demo/components/instrument/keyframes/CSSCodeEditor.vue:116` (the debounced emit of editor text into the model — the demo's largest untrusted-CSS ingress; adopted as the **fuzz entry of record**) · `demo/components/instrument/timeline/KeyframeTimeline.vue:239-244` (emitter) / `:251-261` (hand-rolled parser) / `:263` `kf.vars = newVars` / `:264` un-awaited `rebuild()` — **all four exact at origin/master** · `demo/components/instrument/timeline/composables/useTimelineBuild.ts:40-50` try, `:47-50` the silent `console.error` · `demo/components/instrument/timeline/utils/timelineEngine.ts:11/:13/:15` (imports `parseAnimationCSS` + `requireKeyframeSelector` + `selectorText` — the in-tree delegation-doctrine reference; **all three re-read at `81a56990` this round**, `:12-16` being the `@utils/keyframeSelector` block. **`:11` is the import of the census cell added above** — the pointer that made D-4 visible. Its `serializeCssValue` limb is a **deep `@src/` import at `:17`**, re-pinned this round in place of the banked `:90` call-site anchor; the extirpation is KF.W8's) · `demo/components/instrument/keyframes/KeyframesEditor.vue:123` (import) / **`:186`** (the call) · `demo/components/instrument/keyframes/composables/useKeyframeOps.ts:174` (call) and `demo/utils/keyframeSelector.ts:14-15` (**the reference implementation itself** — precision correction, §Carry F1) · `demo/scenes/square/useSquareTumble.ts:22` `parseCssColor` · `demo/scenes/square/useSquareDemo.ts:82` `parseCssScalar` · `demo/utils/reference-data/animationDescriptions.ts:76` `parseTimingFunction`.

**Do NOT touch**: `package.json` / `package-lock.json` version pins (OP-5 — KF.W3 owns the repin) · `node_modules/@mkbabb/value.js/**` and `node_modules/@mkbabb/glass-ui/**` (read/execute only) · every demo `.vue` cure site (KFED-UNIT / EDITOR-UNIT / KF.W6 / KF.W7 / KF.W8) · `demo/components/instrument/keyframes/CSSCodeEditor.vue` beyond reading `:116` · value.js `src/**` · `docs/tranches/V/apotheosis/parser-proof/GATE-VERDICT.md` (pinned, immutable — the correction is an addendum file) · `scripts/dev/dev.sh` (unowned, either repo, **NEVER**).

**Disjointness**: `.b` owns `parse-facade.ts` + the Tier-A modules; `.c` owns the 6 Tier-C modules + `cssom.ts` + `test/ingest/`; **`emit/format/format.ts` is claimed by both** (Tier-C `:135-144` and the `selectorText` mirror) and is therefore **SERIAL — `.c` writes it, `.b` reads it**. `emit/index.ts` is touched by neither in this wave (OP-6).

---

## §Carry

Every routed registry cell lands as a gate, a bounds item, a census cell, a named cure with its locks, or an §Excluded line **with a named owner**. Deduped by identity: **the R1 crash books ONCE at megatranche R1** and is re-booked by no component and no gate here. Severity is the bank's. Ids are the bank's — no row carried here is renamed (anti-rename).

**Census accounting (repair round 1; R-12 — the phantom "11 rows, 11 carried, 0 dropped" is struck)**: **28** cells were carried at authoring; **19** escaped and were adopted at round 1 — RULINGS **R-1b**'s four (kf-DemoGlobalChrome **C-10** · kf-SquareScene **C S-C** · kf-KeyboardShortcutsModal **C-§1/R-B** · kf-SpringTrace **D-12 = L-6 = C-4**), the kf-KeyframesStringControls posture pair (**C-2** · **D-6/L-BL-3/C-1**), the **nine** further R1 verified negatives, the two easing/CSS-organ cells (**KF-ET-2** · **KF-TFP-20**), kf-CubeScene's **C-L-2 residual**, and kf-SquareScene **D-27/L-7/C-9**'s crash-surface annotation. **4 + 2 + 9 + 2 + 1 + 1 = 19; 28 + 19 = 47.** *Arithmetic note (so the two counts cannot be read as a discrepancy)*: F0's round-1 boundary table holds **eleven** rows, of which **two** — kf-DemoGlobalChrome **C-10** and kf-KeyboardShortcutsModal **C-§1/R-B** — are R-1b's and are counted in the four; the nine are the remainder. No cell is counted twice. Adoption is by id at the bank's own disposition: a verified negative is adopted **as a negative**, never converted into work.

**Census accounting RE-DERIVED — repair round 2 (RULINGS-2 R2-11 · R2-5(g); KF-W2-CHECK D-3/D-4/D-5/D-8/D-10). The 47 was a 37-record measurement published as a 58-record closure; the number is re-derived, not incremented.** A fresh sweep of the **whole 58** under PASS-1's own routing rule finds **fifteen further routed cells** — nine R1-boundary negatives, five failure-posture cells, one positive-posture cell. They are adopted here **by banked id, at the bank's own disposition** (anti-rename; a negative stays a negative, a NO-WAVE-OWNER row keeps its owner):

**28 (authoring) + 19 (round 1) + 15 (round 2) = 62 routed · 62 booked · 0 escaped**, each of the 62 landing as a gate, a bounds item, a census cell, a named cure with its locks, or an §Excluded line with a named owner.

*Where each of the fifteen lands*: **X1–X9** → the F0 boundary set (below, the round-2 table) · **X10–X14** → the posture registry at F3, rows **12–16** · **X15** → F3's positive-reference block, **outside the count** (the same discipline ★ S-7 already rides). *Why they escaped*: the AUDITED basis was **37 of 58**, and no §Excluded line ever stated why 21 records carried no routed row — so *"No registry-routed row is unaccounted"* rested on an unstated sample. **That is now impossible by construction**: the verb table enumerates all 58 by name and closes 37 + 21 = 58 by set difference at the bytes, and G-W2-1/G-W2-5 assert over the 58 with the basis named in the gate.

### F0 · Census, reconciliation, and the boundary set

- **R1** ⟨kf-CSSCodeEditor KF-CE-12 (R1 arm) ≡ kf-KeyframeTimeline C-7 ≡ kf-App KF-APP-56 ≡ kf-EditorShell C-19 ≡ kf-KeyframeCardList ≡ kf-KeyframesEditor KF-KE-54 ≡ kf-CubeAxisLines C·§4 ≡ kf-TimelineTrack SUP-3 ≡ kf-CSSPasteDialog C-4 ≡ kf-KeyframesAddDialog K-5 ≡ kf-ChannelOptions C·S-4⟩ — **BLOCKER-class at its bank; FOLD BY REFERENCE, no new row.** What this wave owns is the **ingress census of record** and, now, the **entry-point reconciliation**.

  **EXECUTED BY THIS FOLD SEAT** (read-only, `node_modules/@mkbabb/value.js/dist/subpaths/css.js`, **4.0.0** verified, 2026-08-28):

  | input class | `parseCssColor` | `parseCssValues` / `parseCssValue` | `parseCssScalar` | `parseTimingFunction` / `parseKeyframeSelector`¹ | `parseStylesheet` |
  |---|---|---|---|---|---|
  | `oklch()` `rgb()` `rgb( )` `hsl()` `color()` `lab()` | **THROW** | **THROW** | **THROW** | `ok:false` | ok:false top-level; **THROW nested** (`@keyframes a{from{color:oklch()}}`) |
  | `calc()` | **THROW** | `ok:false` | **THROW** | `ok:false` | ok:false |
  | `color-mix(in srgb, …)` | `ok:false [css_syntax]` | **`ok:true`** | `ok:false` | `ok:false` | ok:false |
  | `steps()` | — | `ok:false` | **THROW** | `ok:false` | ok:false |
  | `var(--x)` | `ok:false [color_context_required]` | `ok:true` | `ok:false [css_syntax]` | `ok:false` | ok:false |
  | `oklch(0.7 0.1 200)` | **`ok:true`** | `ok:true` | `ok:true` | `ok:false` | ok:false |
  | `null` `undefined` | **THROW** | **THROW** | **THROW** | **THROW** | **THROW** (`.length`) |
  | `42` `{}` `[]` | **THROW** (`e.trim is not a function`) | **THROW** | **THROW** | **THROW** | **`ok:true` with `[]`** |

  ¹ Diagnostic codes differ between the two columns' entries: `parseTimingFunction`'s string rejections carry `css_syntax`; `parseKeyframeSelector`'s carry `keyframe_selector_invalid` (fold-seat precision — a census that copies `css_syntax` into the selector column mis-states the contract). Every throw is `TypeError` — `Cannot read properties of undefined (reading 'replace')` on the string arm, `…(reading 'trim')` / `e.trim is not a function` on the non-string arm.

  **The reconciliation, stated:** the R1 shape is **empty-argument COLOR functional notation**, and it escapes **five** entries (`parseCssColor`, `parseCssValues`, `parseCssValue`, `parseCssScalar`, and `parseStylesheet` on the nested form), not one. The registry's contradiction was a **corpus difference, not an entry-point difference**. kf-HeroAurora **KF-HA-19** is **CONFIRMED IN MEASUREMENT and REFUTED IN CONCLUSION** — every reading it reports reproduces exactly here (`oklch(0.7 0.1 200)` → ok; `var()`/`color-mix()` → diagnostics, not throws), but its probe set is the **well-formed** one; on that basis its conclusion, *"The 'R1 = live `parseCssColor('oklch()')` shipping crash' framing does NOT reproduce against this repo's pinned value.js 4.0.0 css subpath"* (banked verbatim, `kf-HeroAurora.md:66`), is **false as to the empty-argument form**.

  **kf-SquareInstrument K-6 — the drafting seat's statement of the bank was WRONG and is corrected here (KF-W2-CHECK D-2; E-3: the correction sits beside the dated record, which is not rewritten).** K-6 **did** probe the empty-argument form, first, and reported it: `kf-SquareInstrument.md:24` — *"Bonus: `parseCssColor("oklch()")` — **the true R1 degenerate form** — throws an uncaught TypeError in my run, confirming R1 is real AND unreachable from any token (`getPropertyValue` yields `""` for undefined tokens; useSquareTumble.ts:17 `if (value)` skips empties; no CSS token can resolve to empty-args `oklch()`)"*; and `:100` — *"R1's true input is empty-args `oklch()` (throws in my run) **which no token can produce** (`getPropertyValue` → `""` → the `if (value)` guard). Safe regardless of cascade order."* So: **K-6's measurement is CONFIRMED and its conclusion is NOT retired.** What K-6 killed is exactly one claim — **R1-through-a-token** at `useSquareTumble` — and this wave carries **K-6's reachability guard as a first-class census datum**, not as a footnote. Two consequences bind: (i) any "sharpest argument" for the façade may **not** be built on a novelty this wave does not have — the empty-args probe is K-6's, and the credit is K-6's; (ii) per RULINGS **R-7a**, K-6's kill does **not** reach the `!ok → throw` crash surface, whose reachability via **authored CSS** is established elsewhere (kf-SquareScene **D-27/L-7/C-9**; kf-EditorShell **C-19**'s RR-2) — token path and authored-CSS path are read as a **pair**, never merged. kf-EditorShell C-19's **RR-2** and kf-KeyframeTimeline **C-7** are **CONFIRMED**. The memory-of-record framing *"R1 = live `parseCssColor("oklch()")` shipping crash"* is **VINDICATED as to `parseCssColor` and CORRECTED as to scope**. kf-ChannelOptions **C·S-4**'s third shape (`parseTimingFunction(null|42|{})`) is confirmed and generalises: **every** entry throws on non-string (`parseStylesheet` excepted — see below, worse).

  **NET-NEW, and the sharpest single argument for the façade**: the library's most defensive posture is **unreachable on this class**. `adapter.ts:219-226`'s `parseSource` is written `result.ok ? {ast, issues:[]} : {ast:[], issues:result.diagnostics}` — but `parseStylesheet` **throws before returning** on a stylesheet containing `color: oklch()` (re-executed: the nested form throws). The absorb arm never runs on precisely the input it exists for. **Booked as a mechanism finding under R1's identity, not as a new crash id.**

  **NET-NEW, forwarded not booked**: `parseStylesheet(42|{}|[])` returns **`ok:true` with an empty stylesheet** (re-executed) — a non-string silently accepted as "no rules". Worse than a throw for a validator. Routed to the value.js parser lane as evidence; **no id minted here**.

  **VERIFIED NEGATIVES (census cells; each folded, none re-booked, none to be re-probed)**: KF-APP-56 (`css_syntax` ×0 in the boot entry; the colour-module exposure is KF-APP-15, KF.W6's) · C-19 (folds to KF-APP-56) · kf-KeyframeCardList (class unreachable; the seam is `KeyframesEditor.vue`) · kf-CubeAxisLines C·§4 · kf-TimelineTrack (the only emit→parser path is awaited inside the try at `useTimelineBuild.ts:40-50` — *"recorded as a negative result so parser waves skip this file"*) · kf-CSSPasteDialog C-4 (*"Cleared — do not re-file"*) · kf-KeyframesAddDialog C K-5 (*"CONTAINED … Cleared — do not re-file"*) · kf-KeyframeCard KF-KC-53 · kf-LayerConfigPanel LP-24 · kf-MbabbMenu MM-37 · kf-CubeTarget #11 · kf-ChannelControls C's L-1.

  **BOUNDARY SET WIDENED — repair round 1 (RULINGS R-1b #13/#15 + KF-W2-CHECK D-3). Eleven further banked R1 negatives, adopted by id, each carried with its lock (**two of the eleven — kf-DemoGlobalChrome C-10 and kf-KeyboardShortcutsModal C-§1/R-B — are RULINGS R-1b's #13/#15 and are counted there, not twice; the other nine are this table's own adoption**). G-W2-5's assertion (*"the boundary set is recorded as negatives so no later seat re-probes it"*) is only true of the set that is written down; before this repair it was short by these eleven.** They are adopted **as negatives** — none becomes work, none is re-probed, and a later seat that re-files one is in breach of the bank's own words:

  | id ⟨record:line⟩ | the lock, verbatim |
  |---|---|
  | **C-§1 / R-B R1-negative** ⟨kf-KeyboardShortcutsModal:78⟩ | *"(C-§1, R-B independently re-traced 62/11/1): CLEARED — the R1 crash class is not on this component's graph; same containment posture as the banked kf-CSSPasteDialog row. **Do not re-file.**"* |
  | **C-10** ⟨kf-DemoGlobalChrome:77⟩ | *"zero value.js/engine edges; R1 reachability CLEAN … Identity guard: respects value.js's banked R1 row; **recorded so parser waves skip this file**. … No wave."* **Mechanism named** (the ruling's requirement): the file's only `@mkbabb` neighbourhood edge is the parent's `@mkbabb/value.js/math` at `AnimationControlsGroup.vue:125` — the live line, settling the readers' `:131`/`:125` drift — and **`/math` is not the parse surface**. The negative is topological, not probabilistic. The space-separated-`hsl()` seam marker carries with it. |
  | **SUP-1/2/3** ⟨kf-AnimationControlsGroup:106⟩ | *"the 1.1 KB `value.js/math` leaf everywhere `clamp` is needed (**the direct cause of the R1-unreachable verified negative** — value.js's banked R1 row respected, not re-booked)"*; carried with M-11's scope correction (the runtime `RAFPlayback` edge one hop out) |
  | **S+2 / superlative 6** ⟨kf-AmigaScene:150⟩ | *"≡ census R1 crash-surface row (lane-library §4.6) — amiga is the **negative** exemplar (zero exposure); **the R1 row itself stays owned by KF.W3**"* (routing carried: the row is W3's, the negative is the census's) |
  | **C-10** ⟨kf-AnimatedText:105⟩ | *"the R1 crash-class exposure delta of adopting `splitText` is **exactly zero**"* — one value.js edge, allow-listed with a specifier-end lookahead, 1,110-byte artifact, zero import specifiers |
  | **SUP-7** ⟨kf-AnimationVisualizer:124⟩ | *"parser-free subpath (**no R1 crash class in this call graph**)"* (`:46` `clamp` from `/math`) |
  | **★ S-7** ⟨kf-SpringPhysicsFacet:107⟩ | *"the R1 crash class does not reach this subtree … **every user-driven parse below the facet runs through `withErrorToastAsync` with Retry**; the single exception is machine-fed (SPF-19/20)"* — **doubles as the registry's positive posture exemplar** (see F3) |
  | **superlative 3 / C S★1** ⟨kf-OrbitalDrag:132⟩ | *"a 1,110-byte zero-import leaf making the R1 crash class **unreachable by module topology** while a sibling scene took the exposed edge"* |
  | **S+3 / S-d** ⟨kf-TimelineHoverPreview:112⟩ | *"**structural immunity to the R1 crash class by construction**"* — zero `@mkbabb` imports in the SFC, in a cluster banked for reaching into library-private `src/` |
  | **S-C2** ⟨kf-EditorStartScreen:113⟩ | *"the one string that crosses into value.js was **EXECUTED** against the installed 4.0.0, not reasoned about"* — method precedent for this wave's own F0 matrix |
  | **C's S★-3** ⟨kf-TypingDots:89⟩ | *"the one string crossing the package boundary **checked against the installed `@mkbabb/value.js@4.0.0` dist**"* — same precedent |

  **BOUNDARY SET COMPLETED OVER THE 58 — repair round 2 (RULINGS-2 R2-11.2 · KF-W2-CHECK D-5). The round-1 widening adopted eleven negatives from a 37-record read and then asserted completeness over the corpus; G-W2-5's promise — *"recorded as negatives so no later seat re-probes it"* — was true only of the set written down, and the set was short by NINE.** Eight sit in records this wave had never audited; **one (kf-SpringTrace S-A) sits in an AUDITED record**, which is the sharper fact: the miss was not only sampling. Each is indistinguishable in kind from the nine adopted at round 1. Adopted **as negatives** — none becomes work, none is re-probed, and the register is now the closed thing G-W2-5 asserts:

  | # | id ⟨record:line⟩ | the lock, verbatim (re-read at the current bytes this round) |
  |---|---|---|
  | **X1** | **S-9 (RR-2)** ⟨kf-ControlsPaneWrapper:121⟩ | *"this node's keyframes.js consumption is 100% `import type` (`.vue:161-163`; no value.js import at all) — **the parser blast radius has no entry point in this component's import closure**."* |
  | **X2** | **C S-A** ⟨kf-EditorHeader:95⟩ | *"proving a negative: **the full import-closure walk that DECLINED an R1 parser-crash claim** and enumerated the chunks through which glass-ui does reach value.js to justify the refusal."* — the corpus's method exemplar for a *declined* R1 claim |
  | **X3** | **superlative 7** ⟨kf-MatrixEditor:139⟩ | *"**The value.js edge is type-only and R1-clean** *(r1)*: erased `import type` under `verbatimModuleSyntax`; **no parser entered**."* |
  | **X4** | **C-negative (upheld)** ⟨kf-PlaybackRibbon:88⟩ | *"the value.js **R1 parser-crash class is NOT reachable** from this component's scrub path (no value.js import; the child's one import is `/math` `clamp`; the seat path is numeric end-to-end) — concord with KF-AV SUP-11's precise phrasing; bundle co-residency only. **Negative recorded so it is never re-litigated.**"* |
  | **X5** | **superlative 7** ⟨kf-SequencePlayhead:121⟩ | *"The one visible dependency edge is the correct one — `@mkbabb/value.js/math`, declared, 1110 import-free bytes, **parser-crash class unreachable**; the edge is forced, not lazy."* |
  | **X6** | **superlative 4** ⟨kf-SequenceScene:119⟩ | *"The value.js edge is the narrowest possible leaf and **R1 is structurally unreachable** — four imports, all `clamp` from `/math`; the 4.0.0 exports map has no root entry; no colour string ever enters the engine. **Package-enforced, not discipline-enforced.**"* |
  | **X7** | **superlative 7** ⟨kf-SpringHeatmap:101⟩ | *"The narrowest possible value.js surface — one pure function (`clamp`) by exact subpath …; `/css`/`/color` untouched, transitive reach verified absent — **the R1 parser-crash class is structurally unreachable from this component**."* |
  | **X8** | **KF-SST-36 · C-13** ⟨kf-StartingStyleTarget:80⟩ | *"**FOLD BY REFERENCE → banked KF-CB-33** (negative finding upheld AND predictive): **the R1 colour-parser class is unreachable here** (opacity/transform only; `eligible: true` re-executed by this seat) … Not re-booked; recorded as a **KF.W3 scoping input**, per the banked row."* — **routing carried whole**: the negative is this census's, the scoping input stays W3's. A dependent of the very organ lock this wave carries (KF-CB-33) |
  | **X9** | **S-A (C)** ⟨kf-SpringTrace:108⟩ | *"the light/heavy boundary is honoured and **the R1 crash class is provably unreachable** — the closure is value.js-free by construction (sample.ts:16), **proven by enumeration**."* **Cure note carried verbatim (M-25 — it qualifies the negative and may not be dropped)**: *"the N-2 route imports runtime `@mkbabb/value.js/easing` and spends the 'zero value.js imports' cell — R1 (`/css`) stays unreachable either way; **the adopting spec must state this trade**."* Stated: **this spec adopts X9 as an R1(`/css`) negative only**, and takes no position on the N-2 route, whose cure is kf-SpringTrace's packet's |

  **The eight-vs-one split is recorded, not smoothed** (E-3, dated, the PASS-1 register unedited): X1–X8 sit in kf-ControlsPaneWrapper · kf-EditorHeader · kf-MatrixEditor · kf-PlaybackRibbon · kf-SequencePlayhead · kf-SequenceScene · kf-SpringHeatmap · kf-StartingStyleTarget — eight of the 21 records the AUDITED basis did not span until this round. **X9 is in an audited record**, so *"we only sampled"* does not fully explain the miss and the register is not allowed to claim it does.

  **POSITIVE EXEMPLAR, cited upward as the bank instructs (R-1b #14)** — **C S-C / superlative 3** ⟨kf-SquareScene:132⟩: *"`parseCssScalar` at the two-writer boundary — the demo's best use of value.js and the **counter-example to the library's own internal regex re-parsing**; worth citing upward when the parser wave lands. (Its unit-blindness is **L-10**; the shape is still right.)"* **The parser wave has landed: this is it.** The cell is carried whole, both halves — the shape is the one the façade publishes (delegate to the grammar at the boundary, do not re-parse), and **L-10's unit-blindness rides with it** so the façade's scalar seam does not inherit the flaw it is being praised for avoiding. This cell is the strongest in-corpus argument for G-W2-3 (Tier-C deletion) and is cited there rather than at a gate of its own.

  **CRASH-SURFACE ANNOTATION, not a new row (KF-W2-CHECK D-6; anti-rename)** — **D-27/L-7/C-9** ⟨kf-SquareScene:53, identity at `:125`⟩ **MAJOR at bank**: *"Five `throw` sites execute inside the rAF frame (`num()` ×2, `colorAt` ×3) with no engine guard, and a throw does not drop a frame — it **BRICKS the loop for the mount's lifetime** … Reachable via the same editor path L-2 opens (`calc()` is ordinary authored CSS; `parseCssScalar("calc(1px + 2px)")` → ERR → `num()` throws), **and via any `--rainbow-*` re-author in a syntax value.js 4.0.0 rejects**."* Its own identity lock, verbatim (`:125`): *"The R1 parser crash-class note inside D-27/L-7/C-9 ≡ the megatranche R1 identity (V·π parser program) — **annotated, never re-booked**."* Honoured exactly: **annotated here, booked nowhere**. Two riders travel with it — kf-SquareScene **C-1** (*"the colour half of the scene's value.js consumption is **dead paint**"*: the sole colour write at `useSquareDemo.ts:138` is occluded by `.demo-box`'s opaque `background-image`), so the colour arm's *user-visible* stake is zero while the *crash* stake is total; and **MISS-4** (`colorAt` re-parses two static colour strings per tumble frame — *"the per-frame parse is what moves the C-9 throw from a survivable init site into the loop-bricking frame path"*). **Cure is the square packet's (KF.W11 — R-15), not this wave's**; what this wave owes it is the measured entry-point matrix.

  **OPEN PARSER-LANE QUESTION, received and partially answered (KF-W2-CHECK D-3/E10)** — **C-L-2 residual** ⟨kf-CubeScene:170⟩: *"whether value.js `parseCssValue`/`parseCssScalar` internally dispatch into the colour grammar for non-colour declarations (the C-L-2 residual, **untraced by all five seats**) — routed as a census annotation to the V·π parser program, where that surface is already under proof."* **This wave's F0 execution bears on it and the datum is recorded rather than left dangling**: `parseCssScalar("oklch()")` and `parseCssScalar("rgb()")` **THROW** — a *scalar* entry dying on a *colour* functional notation is behavioural evidence that the dispatch is shared. **Stated at its true strength**: this is black-box evidence from the installed 4.0.0 dist, **not** a read of value.js source; the residual therefore stays **routed to the V·π parser program** for a source-level answer, now with an executed datum attached instead of five seats' silence. **No id is minted here.**

  **Organ lock (kf-CopyButton KF-CB-33)**: the census spans the **easing/CSS organ**, not only the colour organ — *"value.js broke this component through the easing/css organ (`parseTimingFunction` + `bezierPresets`) — exactly the class a §4.6-scoped parser wave would miss."* Honoured: `parseTimingFunction` and `parseKeyframeSelector` are in the matrix above and in the census by name.

  **THE EASING/CSS ORGAN, censused by id — repair round 1 (KF-W2-CHECK D-10). Carrying KF-CB-33's organ lock while omitting the corpus's only *executed* `parseTimingFunction` conformance measurement was the lock honoured in letter and broken in fact.** Both cells are adopted as census cells with their banked owners intact; **neither cure is this wave's**:

  - **KF-ET-2** ⟨kf-EasingTarget:41⟩ **MAJOR, NO-WAVE-OWNER** — *"My probes against value.js's own grammar: `parseTimingFunction("ease-in-bounce")` and `("smooth-step-3")` **FAIL** — the two name-branch specimens that print bare names print strings that are not CSS `<easing-function>`s, under the ':38 Copy easing literal' label (silently `ease` on paste)."* And the cure-lock, carried whole because M-25 forbids dropping it: the corpus-unanimous cure (*"delete `literal`; bind `demo.cssValue`"*) is **REFUTED as a discharge of C-2 and adjudicated harmful** — `cssValue` returns the bare NAME for everything except `cubic-bezier`/`steps`, so adopting it puts **20 of 28** specimens on invalid CSS. **This is the census's only executed measurement of a value.js *easing* entry against real kf inputs**, and it is the empirical form of KF-CB-33's warning: a §4.6-scoped (colour-only) parser wave misses it. It enters the INGRESS-CENSUS as an easing-organ ingress with its executed outcome; the component cure stays NO-WAVE-OWNER, one spec with KF-ET-1, under the record's own restated law — *"the cure is adjudicated separately from the defect, and a proposedCure inherits no evidential weight from its defect's evidence."*
  - **KF-TFP-20** ⟨kf-TimingFunctionPanel:63⟩ **MINOR, NO-WAVE-OWNER** — *"`kind` runs value.js's full timing-function grammar once per pointermove, one of FOUR classifier funnels invalidated by the same per-move literal write … **a parser recovering a two-valued tag the handler already holds verbatim as `v.mode`**."* The census's **only live per-move grammar consumer**, and the sharpest available statement of the anti-pattern the façade exists to make visible (parse-once/interpolate-many, the same idiom kf-SquareScene MISS-4 finds inverted in the tumble frame). Registry-swept by the reader: *"KF-CO-10 and the R1 class are different mechanisms"* — carried so no seat folds this into R1. Census cell only; the cure (*"the seat composable carries mode as data, never by re-parsing its own output"*) is NO-WAVE-OWNER's.

  - **KF-ET-28 · D-3 = C-6** ⟨kf-EasingTarget:70⟩ **MINOR, NO-WAVE-OWNER — adopted at repair round 2 (RULINGS-2 R2-11.1 · KF-W2-CHECK D-3), booked ONCE at F3 row 16 and pointed at from here so the easing organ's grep trail is whole.** The **third** cell this same record routes at this wave (with KF-ET-2 above), and the one that closes the organ: *"value.js's Result API reaches the render path through a **throwing adapter with no boundary**."* It is simultaneously an easing-organ **ingress** (this section's subject) and a **failure posture over a value.js-bearing path** (F3's subject) — booked in the register, censused here, **counted once**. **Do-not-re-probe lock attached**: latent by measurement, MINOR at bank, cure NO-WAVE-OWNER with the *degrade-don't-detonate / skip-not-throw* shape; a later seat that re-files it as new work is in breach of the bank's own grade.

  → **G-W2-5**, the INGRESS-CENSUS registry, and the §F-2 addendum. Cure of any ingress: **not this wave's**.

- **C-7** ⟨kf-KeyframeTimeline⟩ **MAJOR** — the timeline's inline variable editor is a **live** untrusted-CSS ingress, and its containment is the file's only silent failure: *"`:252-260` admits `oklch()` as a value verbatim → `kf.vars` → un-awaited `rebuild()` (`:264`) → `buildAnimationFromTimeline` → engine parse → THROW → swallowed at `useTimelineBuild.ts:47-50` (`console.error` + `animation.value = null` — the file's ONLY non-toasting failure; compare `:121/:133/:137/:148/:155/:157`)."* **Chain re-walked and confirmed byte-exact at origin/master by this seat** (parser `:251-261`, replacement `:263`, un-awaited rebuild `:264`, try `:40` / `console.error` `:48`). Split lock, verbatim: *"The crash identity FOLDS to the banked megatranche R1 row (per the KF-CE-12 precedent) — never re-booked; the census correction to GATE-VERDICT §F-2 is recorded; the fuzz-entry note rides KF.W2/KF.W3; the silent `console.error` posture books HERE → KF.W7 (one failure posture; cross-ref KF.W2's posture registry)."*
  **CENSUS CORRECTION this wave delivers**: GATE-VERDICT §F-2's *"no known consumer feeds the crash shape"* (live at `GATE-VERDICT.md:42`, verified) is **FALSIFIED** — *"a genuine scope-correction … the census covered kf's `src/` seams, not this live typing surface."* → **G-W2-5** + the E-3 addendum; the posture cell → **G-W2-1**; the cure → **KF.W7** (whose **§0 · OP-5** and **§Gates · G14** name this wave from the other end — both re-resolved by id at the current bytes this round; the round-1 receipt's `:31`/`:284` are **struck as coordinates**, `:284` having pointed at **G13** even at the round-1 bytes — KF-W2-CHECK D-7).

- **KF-CE-12 (R1 ingress arm ONLY)** ⟨kf-CSSCodeEditor⟩ **MAJOR at bank** — arm split is a **hard lock**, verbatim: *"publish-or-relocate `convertPixelsToCh` + the deep-import extirpation → KF.W8; the depcruise-over-demo gate arm → KF.W4; the R1 ingress note → KF.W2/KF.W3."* And: *"the `:116` fuzz-entry note folds to the R1 identity in the parser lanes; no new row."* This wave takes **only** the ingress note and adopts `:116` (verified: the debounced `modelValue` write) as the **fuzz entry of record**. Anti-scope-creep: the deep import, `sideEffects:false` falsification, and `useKeyframesState.ts:1`'s identical breach are **KF.W8's** and must not be pulled here. → **G-W2-5** corpus arm.

- **KF-APP-56** ⟨kf-App⟩ **INFO (verified negative)** — the R1 class is **not** reachable from App's boot graph; the colour-module exposure is KF-APP-15. Verbatim: *"fold by reference → megatranche R1 / census risk-3 (KF.W2/W3 own the class; App books nothing)."* Lands as the census's **boundary cell**: the boot graph is OUT. → INGRESS-CENSUS registry.

- **C-19** ⟨kf-EditorShell⟩ **MINOR** — parser-class fold whose **component rider is the sharper fact**: `EditorShell.vue:239-248` establishes `color-mix(in srgb, …)` as the demo's token idiom while a sibling scene pipes computed token text through `parseCssColor` and throws on `!ok`. Two census inputs: **(i)** re-executed by this seat — `parseCssColor("color-mix(in srgb, red, blue)")` → `ok:false [css_syntax]` while `parseCssValues` on the same string → **`ok:true`**. The demo's own token idiom is **rejected by the colour entry and accepted by the value entry**. Any façade diagnostic surface must therefore treat `color-mix()` as **a first-class not-an-error at the colour seam**, or the demo's token system reads as broken. **(ii)** `demo/scenes/square/useSquareTumble.ts:22` `parseCssColor(css)` (verified) is lane-library §4.6's named R1 crash surface and enters the census **by name**. Fold direction is C-19 → KF-APP-56 → R1; never re-booked at R1 directly. → **G-W2-5** + **G-W2-6**'s contract. The `useSquareTumble` **cure** is the square packet's (§Sequencing).

### F1 · The façade and the entry-point contract

- **Tier-A single entry** ⟨lane-library §4.1⟩ — **CENSUS CORRECTED BY RE-DERIVATION, re-verified by this fold seat's own grep of `origin/master`.** The direct value.js grammar-entry call sites are **11 over 9 modules**: `adapter.ts:222` `parseStylesheet` · `easing/registry.ts:131` `parseTimingFunction` · `emit/format/options.ts:104` `parseTimingFunction` · `selector.ts:24` `parseValueSelector` · `value/compile.ts:32` `parseCssValues` · `engine/options.ts:31` `parseCssScalar` · `resolve/browser.ts:162` `parseCssScalar` · `scroll/grammar.ts:77/:85/:109` · `validate.ts:182`. **Two of the banked thirteen name kf's OWN symbol**: `adapter.ts:266` is the **definition** of `resolveKeyframes` (`export const resolveKeyframes =`, read) and `engine/css/animation.ts:176` is a **call to it** (imported from `../../compile/adapter` at `:15`, read) — neither is a value.js grammar entry. Substituting in: `frame/compiler.ts:146` calls kf's own `parseKeyframeSelector` wrapper — a Tier-A-adjacent consumer the banked roster does not name. Plus **10 AST-collector call sites** over 4 modules (`adapter.ts:205/:241/:341/:374/:377` · `engine/css/metadata.ts:42/:102` · `scroll/grammar.ts:111/:112` · `validate.ts:186`) — *"the same value.js surface and the same blast radius."* **True parse-surface denominator: 21.**
  **Runtime-importer split — RE-MEASURED at repair round 2 (RULINGS-2 R2-5 · R2-6.1; the figure below is the operative denominator for G-W2-2's re-cut check).** The round-1 statement (*"15 modules … with a non-`import type` statement"*) was a **lexical** count and is preserved as such, but it is not the runtime-edge count, and the gate now turns on the latter. Measured at `81a56990`:

  | quantity | value | how |
  |---|--:|---|
  | `from "@mkbabb/value.js/css"` specifier lines under `src/` | **29** | `git grep -n 'from "@mkbabb/value.js/css"' 81a56990 -- src/` |
  | distinct files | **27** | same, `-l` (`emit/css-text.ts` and `scroll/grammar.ts` each carry two statements) |
  | statements not lexically beginning `import type` | **15** | the round-1 figure — **retained, and declared non-operative** |
  | files with **≥1 runtime (value-position) specifier** | **13** | the per-file import-block read below |
  | of those, on the **grammar/collector parse surface** | **10** | `adapter` · `easing/registry` · `emit/format/options` · `selector` · `value/compile` · `engine/css/metadata` · `engine/options` · `resolve/browser` · `scroll/grammar` · `validate` |
  | runtime specifiers, total | **25** | 22 grammar/collector + 2 emit-half `serializeCssColor` + 1 stray `coerceToSyntax` |

  The **15 − 13 = 2** gap is the two modules whose named specifiers are all **inline** `type` — `resolve/element-resolve.ts:24` (`import { type CustomFunctionDescriptor }`) and `engine/css/animation.ts:11-14` (`import { type CSSPropertyDescriptor, type CSSTimelineOptions }`) — lexically not `import type`, semantically erased, **no runtime edge**. The round-1 text already said this in prose; the round-2 correction is that the gate may not be denominated on the lexical number.
  Out of the 13: **2 emit-half importers** (`emit/css-text.ts:8` + `frame/interp-slot.ts:8`, both `serializeCssColor` — Tier-D, out of this gate's denominator, lockstep per §4.4) and **1 stray** (`resolve/function.ts:1-4` `coerceToSyntax` — the net-new census cell, delete-or-declare at open). **A specifier-count grep can never reach 1 while the emit half legitimately stands** — which is why the gate is symbol-scoped and, from this round, **specifier-anchored** (G-W2-2).

  **Demo-side runtime grammar/collector modules — the "8 demo-side seams" figure RECONCILED (RULINGS-2 R2-11.3; the round-1 assertion was unreconciled).** Measured at `81a56990` by the same per-file read: **7 files** under `demo/` name the subpath (one, `timeline/timelineTypes.ts:1`, is `import type` only), **6 carry a runtime symbol**, and those six carry **8 runtime specifiers** — *that* is the 8. Module-for-module:

  | module | runtime specifier(s) | call anchor(s) |
  |---|---|---|
  | `components/instrument/keyframes/KeyframesEditor.vue` | `parseCssScalar` ⟨`:123`⟩ | `:186` |
  | `components/instrument/keyframes/utils/parseAnimationCSS.ts` | `collectStyleRules` · `collectAnimationOptions` ⟨`:1-5`⟩ | `:36` · `:41` |
  | `scenes/square/useSquareDemo.ts` | `parseCssScalar` ⟨`:4`⟩ | `:82` |
  | `scenes/square/useSquareTumble.ts` | `parseCssColor` · `serializeCssColor` ⟨`:2`⟩ | `:22` (the emit half is Tier-D) |
  | `utils/keyframeSelector.ts` | `parseKeyframeSelector` ⟨`:1-4`⟩ | `:15`, inside `requireKeyframeSelector` `:14-20` |
  | `utils/reference-data/animationDescriptions.ts` | `parseTimingFunction` ⟨`:128`⟩ | `:76` |

  **8 specifiers over 6 modules — not 8 modules.** Every one is a census cell of this wave and **none is a cure site here**; the sixth (`parseAnimationCSS.ts`) is the cell D-4 convicted this spec for omitting (§Bounds). → **G-W2-2** and its demo clause **G-W2-2b**.

- **KF-KE-3** ⟨kf-KeyframesEditor; seam located by kf-KeyframeCardList⟩ **BLOCKER** — the start field validates through the **wrong entry point**: `parseCssScalar` (any CSS scalar) instead of the keyframe grammar. Verbatim: *"admits 500%/-20%, renders them, then fails ~1 s later as 'Could not update keyframes' from a different op naming neither field nor stop; rejects from/to that the sibling add path accepts via `requireKeyframeSelector` … Frontier-durable (`parseCssScalar` at frontier `:186`). → KFED-UNIT (`requireKeyframeSelector` at the field; the S-5/C-S5 error posture stays — it is the reference implementation asking the wrong function)."*
  **RE-EXECUTED CONFIRMATION + one precision correction** (this seat, 2026-08-28): `parseCssScalar("500%")` → `ok:true {number,500,%}`; `("-20%")` → `ok:true`; `parseKeyframeSelector("500%")` / `("-20%")` → `ok:false [keyframe_selector_invalid]`. The banked admissions are exact. **Correction**: `parseCssScalar("from")` → **`ok:true`** (keyword payload; `"to"` likewise) — the entry does **not** reject `from`/`to`; the *field* fails downstream. A cure written against "`parseCssScalar` rejects `from`" is written against a false mechanism. Also measured: the selector grammar returns a **fraction** (`"from"|"to"|"50%"` → `{kind:"percent", value: 0|1|0.5}`) where the scalar returns `500`/`-20`/`50` — the unit mismatch that MISS-β2's `*100` exists to bridge, and the source of its corruption set.
  **Second precision correction**: the reference implementation is **`demo/utils/keyframeSelector.ts:14-15`** (`requireKeyframeSelector`, verified at origin/master) — `useKeyframeOps.ts:174` is its **call site**. It matters: the correct-grammar wrapper lives in the *same file* as serializer copy 3, so MISS-β2's publication decision and KF-KE-3's cure share a file. → **G-W2-6** (contract, born-RED). **KF.W2 does not open the call site.**

- **KF-KE-54** ⟨kf-KeyframesEditor⟩ **MINOR** — verbatim: *"`kf-engine.ts`'s 'the one place value.js enters the graph' is false as written (three static edges in this closure alone); the cited gate scopes `src/` only; correctly filed documentation-accuracy, value.js IS declared. → KFED-UNIT prose cure; the ingress note folds by reference to the megatranche R1 identity → KF.W2/W3 scoping input."* **This wave takes only the scoping consequence, and it is binding twice**: the ingress census MAY NOT inherit the false single-entry premise (re-measured at repair round 2 — **21** `src/`-side parse sites over **10** parse-surface modules · **13** `src/` runtime importers of the subpath · **6** demo-side runtime grammar/collector modules carrying **8** runtime specifiers), and the gate guarding the façade **must not be `src/`-scoped**. **The second limb was carried in prose and violated in the gate** (KF-W2-CHECK **D-6**): G-W2-2's assertion and check were both `src/`-scoped while the six demo-side modules were covered by no gate at all. Cured at repair round 2 by **G-W2-2b**, the paired demo clause, whose falsifier arms on the same terms as G-W2-2's. Pairs with KF-CE-12's depcruise-over-demo arm at KF.W4. Prose cure is KFED-UNIT's; E-3 — the false comment is corrected in the cure commit, never rewritten in the dated record.

### F2 · Regex extirpation and the injection cure

- **Tier-C six** ⟨lane-library §4.3⟩ — all six survive at origin/master with drift; anchors in §Bounds, **each verified by this seat**. `view-transition.ts:146` is a **serialize→regex-reparse round trip inside the library** — kf re-parsing its own emitted declaration body with a regex (`declaredDecls` matching `/([\w-]+)\s*:\s*([^;]+);/g` over `declaredKeyframeBodyFor` output, read). **Candidate 7th at `format/format.ts:341`** recorded, not asserted (the roster re-derives at open — L-19: an unverified count is not a gate). Wider-class evidence, **not gated here**: kf-SpringTrace **C-2 ≡ L-3** is the same serialize→regex-reparse shape *one layer out*, *"invisible to §4.6's value.js-scoped roster"* — cited as proof the Tier-C class is wider than the six named `src/` sites; its own row is NO-WAVE-OWNER.
  **L-6/C-4's demo half belongs to this class** (see F4): the SFC's hand-rolled declaration scanner is the demo-side instance of the same ad-hoc-parse shape; its cure is KF.W7's, its façade target is this wave's. → **G-W2-3**.

- **cssom identifier injection** ⟨lane-library §4.2 / Tier B⟩ — verified verbatim at origin/master, `src/animation/ingest/cssom.ts:214-216`: ``new RegExp(`\\banimation(?:-name)?\\s*:[^;}]*\\b${name}\\b`)`` interpolates a **CSSOM-supplied `@keyframes` identifier UNESCAPED**. A name carrying regex metacharacters (legal via CSS escapes) throws or mis-matches. Companion at `:28-33` (verified verbatim): the **VJ-9 self-declared handoff** — *"The ingest ships on the SHIPPED value.js `cssText → resolveKeyframes` contract … the ingest's robustness WIDENS on VJ-9's publish."* **FABRICATED AUTHORITY STRUCK (KF-W2-CHECK D-1).** The drafting seat invoked *"the kf-SquareScene law (comment-stated invariants are test obligations)"* here, at G-W2-4, and in a §Sequencing commit-family lock. **No such law is in the bank** — kf-SquareScene's only named "law" is the repo's playback law (`:25`/`:46`, `animationGroup.pause()`); its nearest cells are **MISS-3** (`:88`), **MISS-9** (`:103`) and **L-16/C-10** (`:82`). A gate assertion resting on an authority nobody banked is exactly the contrivance L-19 forbids, so the attribution is deleted and the obligation is **re-founded on this spec's own rule plus real banked evidence**:

  > **This wave's rule (declared here, binding on this wave only, not attributed to any bank row)**: within this wave's §Bounds, **a comment that states an invariant about a mechanism this wave changes is corrected or made executable in the same commit as the change** — it may not be left standing beside a changed mechanism. This is a commit-family discipline over three named files, not a corpus law and not a prose-sweep claim on anyone else's rows.

  Banked evidence that the failure mode is real, cited as **evidence, not as authority, and booked nowhere here** (both NO-WAVE-OWNER; the general prose class is KF.W4/KF.W6's per RULINGS R-6, and this wave takes no limb of it): kf-SquareScene **MISS-3** ⟨`:88`⟩ — *"`useSquareKeyboard`'s docblock asserts the precise opposite of its own code … **the cleanest instance of the corpus's own §7 false-invariant thesis**"*; and kf-SpringTrace **D-1's `:48-49` comment truth**, whose disposition is **KF.W4-PROSE** (R-1c homes it there — named so this wave is visibly not claiming it). Applied to the one comment inside this wave's bounds: `cssom.ts:28-33`'s VJ-9 handoff is a **test obligation** — the G-W2-4 fixture covers the stated robustness bound, or the comment is corrected in the cure commit. → **G-W2-4**.

### F3 · The failure-posture registry

- **Five library postures** ⟨lane-library §7.5⟩, **all five re-verified at origin/master by this seat, anchors re-pinned**: `adapter.ts:219-226` `parseSource` **ABSORB** (→ empty AST + `ParseIssue[]` surfaced as Diagnostic rows) · `scroll/grammar.ts:57-63` `requireParsed` **THROW TypeError** · `validate.ts:180-192` `keyframesNames` **SWALLOW** (`try{}catch{return []}`) · `compile/selector.ts:23-35` **THROW `AnimationOptionError`, code `EMPTY_PARSE`** (`:33`) · `compile/value/compile.ts:33-38` **THROW TypeError**. Recorded with them: posture 1 is **unreachable on the R1 class** (F0), and posture 5's `parseAuthoredValue` type-guards `typeof value === "string"` before parsing (`value/compile.ts:31`, read) — so kf's value arm is the *only* seam that normalises non-string input today, by accident of shape rather than by contract.

- **R-4** ⟨kf-CSSPasteDialog⟩ **MAJOR** — submit has no acknowledgement channel and a failed parse destroys the paste. Verbatim, with the seat's own precision correction preserved: *"the close sits inside `if (text.trim())` — conditional on non-empty text, unconditional on parse result … **A sixth failure posture for the lane-library §7.5 registry (cite verified).** → KF.W7 for the component/consumer contract (busy/error/awaitable-close or `v-model:text` hoisting — the twin's shape), cross-referenced to KF.W2's posture registry so the façade counts it."* **The registry's scope must therefore span `src/` AND `demo/`, or it is under-counted by construction.**

- **KAD-9** ⟨kf-KeyframesAddDialog⟩ **MAJOR** — the only in-dialog completion signal **fires on failure**, and there is no in-dialog error surface; because success unmounts the bar mid-flight, the sweep is only ever *visible* on failure. Verbatim: *"→ KF.W7 + cross-ref KF.W2's failure-posture registry (as the twin's R-4 did)"*; *"the failure-posture / busy-state contract (KF.W7, joined to KF.W2's registry)."* **The seventh.** The **eighth** is C-7's silent `console.error`.

- **THE REGISTRY IS SHORT BY THREE — adopted at repair round 1 (RULINGS R-1b #16 + KF-W2-CHECK D-4/E3-E5).** The spec convicted the charter of a false count (*"'5 postures' is provably 8"*) and then made the identical error one number higher: **8 was itself refuted by the registry at authoring time**. Three further banked posture cells exist, each a *failure posture over a value.js-bearing path*, which is precisely this registry's subject:

  | # | id ⟨record:line⟩ | the posture, verbatim | bank disposition |
  |---|---|---|---|
  | **9** | **D-12 = L-6 = C-4** ⟨kf-SpringTrace:56⟩ | *"**silent-swallow error posture**: an unparseable stop becomes `{v: 0}` — indistinguishable from a legitimate baseline datum — while the header still counts it as plotted; and one line earlier, `filter(Boolean)` silently RENUMBERS the axis … Latent, not live (the emitter is total over the regex today); **violates the engine's own fail-explicit law** (easing.ts:13-16)"* | **NO-WAVE-OWNER** — *"one fail-explicit cure for both mechanisms"* (cure-shape lock carried); the packet names it *"the parser posture (D-12/L-6/C-4 + the filter-cell)"* ⟨:140⟩ |
  | **10** | **C-2** ⟨kf-KeyframesStringControls:63⟩ **MAJOR** | *"the pane's only content-production path is unguarded over a throwing, value.js-bearing serializer; a mount-time rejection boots Monaco empty, indistinguishable from 'no keyframes', with no remount available … the chain ends at `css-text.ts:54` `throw new TypeError("Value returned an unserializable CSS color.")` … every sibling op routes through `withErrorToastAsync` with Retry — **only the mount path is bare**"* | *"absence-of-posture → **NO-WAVE-OWNER**; **reachability → KF.W3**"* — the split is carried whole: the **posture** counts here, the **reachability** question is W3's and is not answered here |
  | **11** | **D-6 / L-BL-3 / C-1** ⟨kf-KeyframesStringControls:60⟩ **MAJOR** | *"`formatEditor` **has no error posture**: a prettier rejection latches `isFormatting` true for the life of the never-unmounted instance, permanently muting the success channel, with **both callsites bare**"* — one callsite is *"a discarded promise in a sync keydown → true unhandled rejection"* | **NO-WAVE-OWNER** (EDITOR-UNIT adjacency; its success-path twin is banked **KF-CE-7**). Cure-shape lock carried: *"one `finally` + the idiom"* |

  **THE REGISTRY IS SHORT A THIRD TIME — five more adopted at repair round 2 (RULINGS-2 R2-11.1/R2-11.2 · KF-W2-CHECK D-3). This is the third consecutive round in which this register published a completeness claim over a number the bank refuted, and the third time the number was measured over a sample.** All five sit in **AUDITED** records — so sampling explains none of it — and **four are named from the consumer's end** as registering *here*, at **KF-W7 §Gates · G14**, whose Carries line reads (re-read at the current bytes this round; the gate id is the anchor, line non-load-bearing): *"one declared failure posture across cluster + dialogs, **registered in KF.W2's posture registry (cross-referenced both ends)**. Carries: C-7 …, **L-15 (TimelineCaret)**, **L-11 (KeyframeTimeline)**, **D-15 (KeyframeTimeline)**, R-4 (+ KAD-9, **KAD-10**)."* Four of those six names had no row here — **a one-ended edge, declared closed at both ends.** Adopted by banked id with their owners intact; **no cure moves**:

  *The `#` column is this registry's own row number (the id anti-rename law governs the second column, never this one); the parenthetical `=X<n>` is the PASS-2 check's escape label, carried so the grep trail from `KF-W2-CHECK` §2b to this table is one hop.*

  | # | id ⟨record:line⟩ | the posture, verbatim | bank disposition |
  |---|---|---|---|
  | **12** *(=X10)* | **L-15** ⟨kf-TimelineCaret:68⟩ **INFO** | *"**silent swallow on unparseable input diverges from the cluster's toasting posture** (`toast.error` siblings — readers' cites, spot-checked); the axis itself flags it **ruling-dependent** (a garbage keystroke is arguably a cancel). **Recorded as filed, unactioned pending the W7 posture ruling.** → **KF.W7**."* | **KF.W7** — the *ruling-dependent* qualifier is carried: this registry records the posture and does **not** pre-empt the ruling |
  | **13** *(=X11)* | **L-11 (KeyframeTimeline)** ⟨kf-KeyframeTimeline:72⟩ **MINOR** | *"`rebuild` is `async` typed/called as `() => void` (useTimelineOps :19 — my read); `snapshot` **toasts success before the rebuild can fail** (:31-34); **failures reach only `console.error`**. → **KF.W7**."* | **KF.W7**. Carried with the bank's own disambiguation: this is **KT's** L-11, **NOT the caret's L-11** — two banked ids, one glyph; conflating them is an anti-rename breach |
  | **14** *(=X12)* | **D-15 (KeyframeTimeline)** ⟨kf-KeyframeTimeline:59⟩ **MAJOR** | *"empty / single-frame / **rebuild-failure states are unexpressed** … **the one failure reachable BY TYPING is the one that doesn't toast**."* | **KF.W7**. Fold directions carried whole: the whitespace-submit arm FOLDS → banked kf-CSSPasteDialog **R-17**; the closes-before-parse arm FOLDS → banked **R-4** (already row 6 here) — **this row books only the rebuild-failure limb**, so R-4 is not double-counted |
  | **15** *(=X13)* | **KAD-10** ⟨kf-KeyframesAddDialog:54⟩ **MAJOR** | *"**async submit, no busy state**; re-entry re-appends stops (D-7) … `addKeyframesStringToAnimation` awaits `parseAnimationCSS` and loops `addFrame` (`useKeyframeOps.ts:172-177`) **with no in-flight guard**, so repeated clicks duplicate the stops. → **KF.W7**."* | **KF.W7**. **This is the busy-state half of the very contract this spec already quotes from KAD-9** — *"the failure-posture / **busy-state** contract (KF.W7, joined to KF.W2's registry)"* — carried in name and dropped in limb until now (M-25). Its await is on `parseAnimationCSS`, the census cell added at §Bounds this round: **the two round-2 findings are one mechanism** |
  | **16** *(=X14)* | **KF-ET-28 · D-3 = C-6** ⟨kf-EasingTarget:70⟩ **MINOR at bank** | *"**value.js's Result API reaches the render path through a throwing adapter with no boundary**, latent by measurement (25/25 names resolve; the catalogue test REDs a typo before a browser can) — the L re-grade to MINOR is CORRECT and its ground verified … The `?? ""` **decoy at :288 guarantees the throw it appears to prevent**, after the painter is already retired. **MINOR.** → **NO-WAVE-OWNER** (**degrade-don't-detonate posture**; skip-not-throw on missing attribute)."* | **NO-WAVE-OWNER**, with the **do-not-re-probe lock** (RULINGS-2 R2-11.1). The bank's own **MINOR** grade is stated here, not inflated; the *latent-by-measurement* qualifier and the **degrade-don't-detonate** cure shape are carried verbatim as the cure-shape lock. **The cure is not this wave's** |

  **The registry's denominator is therefore ≥ 16, and G-W2-1's count cell is restated at 16** (round 1 restated it at 11 under R-1b's *"the gate's count cell is re-stated at the true number"*; round 2 re-derives it from the 58). The gate's shape is unchanged and was already right: a bare integer that the registry can refute is a gate that fails for the wrong reason, so the assertion is **enumeration-first** — **every banked failure-posture cell in the 58-record corpus has a row**, the count is a **floor**, and a seventeenth found at wave-open is **ADDED and named**, never absorbed silently. **What changed at round 2 is the basis, not the shape**: the enumeration now spans the corpus instead of a sample, which is the only correction that can stop this row from recurring a fourth time.

  **The POSITIVE posture rows, recorded as reference and explicitly NOT counted in the 16** — a registry of sixteen absences with no exemplar of the present tense teaches nothing, and counting an exemplar would inflate the number the gate depends on:

  - **★ S-7** ⟨kf-SpringPhysicsFacet:107⟩: *"**every user-driven parse below the facet runs through `withErrorToastAsync` with Retry**; the single exception is machine-fed"* (`useKeyframeOps.ts:25-40`, named by kf-KeyframesStringControls D-6 as *"the house idiom sits unused in the same closure"*) — the house idiom, already in the tree.
  - **SUP-3 (L axis)** ⟨kf-ChannelControls:121⟩, **adopted at repair round 2 (RULINGS-2 R2-11.2 · KF-W2-CHECK D-10)**: *"(L axis; re-read by this seat at `ChannelOptions.vue:461-477`) · **`trySetOption` catches BY ERROR NAME and re-throws everything else** while the store still records the raw string — **the best consumption of a fail-explicit engine API in the repo, against lane-library §7.5's five inconsistent postures**."* **This is the corpus's ONLY cell measured directly against §7.5 — this registry's own governing authority — and the only banked exemplar of *correct* consumption of a fail-explicit engine API.** Missing it while gating on §7.5 was the register carrying its authority and dropping its one positive datum. Carried at the bank's disposition (a superlative, not a defect); **no cure, no owner change** — the re-read anchor `ChannelOptions.vue:461-477` is the bank's own and is not re-pinned here (the file is outside this wave's §Bounds; **KF-CO-33's `proof:bezier-*` limb at that file is routed elsewhere by RULINGS-2 R2-4, and this wave takes no limb of it**).

  → **G-W2-1**: the charter's "5 postures" is provably wrong, the drafting seat's "8" is provably wrong, round 1's "11" is provably wrong over the 58, and **the registry — not a number inherited from any of them — is the gate**.

### F4 · The emit mirror (Tier D)

- **L-6/C-4** ⟨kf-KeyframeTimeline⟩ **MAJOR** — a hand-rolled CSS declaration parser **and** emitter inside the SFC of a CSS engine's demo: `:251-261` (`split("\n")` + `indexOf(":")` + comment-prefix skip) then `:263` `kf.vars = newVars` — **a whole replacement, so anything the scanner misses is DELETED, not mis-parsed**; the emitter at `:239-244` is the mirror. All anchors byte-verified at origin/master by this seat. Verbatim: *"The sibling path in the same cluster honours the delegation doctrine (`timelineEngine.ts:13` `parseAnimationCSS`, `:90` `serializeCssValue` — my read), which makes this a **bypass**, not an omission; `@mkbabb/value.js/css` exports the collectors (RR-B verified). → KF.W7 (delegate), cross-ref KF.W2 (the parse façade this bypass would have used)."* Confirmed at origin/master: `timelineEngine.ts:11/:13/:15` imports `parseAnimationCSS` + `requireKeyframeSelector` + `selectorText`. **KF.W2's obligation: the façade publishes BOTH halves the SFC hand-rolled — declaration parse AND declaration emit — or KF.W7's delegation has nothing to delegate to** (KF.W7 OP-5 declares the same edge from its end, verified). Owner KISS: the façade publishes what `value.js/css` already exports; it mints no grammar. → **G-W2-7** + the façade's emit surface.

- **MISS-β2** ⟨kf-TimelineCaret⟩ **MAJOR** — the library privatizes its keyframe-selector serializer **twice**, publishes neither, and the demo authored a **third** copy. Limbs, **all three re-verified at origin/master by this seat**: copy 1 `emit/format/format.ts:20` `const selectorText` (module-private; consumed per bank at `:132/:201/:280/:313`) · copy 2 `emit/css-text.ts:58` `const serializeSelector` (private, equivalent `value * 100` body; consumed `:75`) · copy 3 `demo/utils/keyframeSelector.ts:7` (exported; consumed by `useKeyframeOps.ts:9` **and** `timelineEngine.ts:15`). **Neither library copy is exported** — `emit/index.ts` export list read whole by this seat: `compileToCSS`, the two densify constants, `compileChild`, the `formatCSSKeyframeString` family, `serializeEasing`, `cssIdent`, the colour-endpoint helpers, the compile/refusal types — **no keyframe-selector serializer**. Corruption set exact (bank): **8 integers in [0,100] fail `(p/100)*100 === p` — 7, 14, 28, 29, 55, 56, 57, 58**; `29 → 28.999999999999996`. Consistent with the measured selector AST being a **fraction** (`"50%"` → `0.5`, re-executed).
  **E-3 precision correction, recorded here, dated record unrewritten**: the banked limb says copy 3 reaches *"library-private `namedSelectorToFraction`"*. At origin/master `namedSelectorToFraction` is **exported** (`compile/selector.ts:42`, read) and **re-exported** (`compile/index.ts:24`, read); `frame/compiler.ts:35-39` even records *"the re-export CEREMONY is DEAD"*. The breach at `demo/utils/keyframeSelector.ts:5` is therefore the **deep-source `@src/…` import path** (verified: `import { namedSelectorToFraction } from "@src/animation/compile/selector"` — the banked C-12 family), not a private-symbol reach. **The unpublished symbol is the serializer, and only the serializer** — which sharpens the cure rather than weakening it. The banked **KC-1** citation correction stands as recorded: its prescribed *"`selectorText`, `format.ts:131` … one line, one import"* names a **non-exported** symbol; the compilable import today is the **demo** copy. **KF-KE-1's framing survives** (it cites the demo-graph symbol).
  **§4.4 lock, binding**: Tier-D is *"the round-trip mirror; the parser wave must move these in lockstep."* → **G-W2-8 (conditional, and now ORDERED — OP-6 / RULINGS R-16: KF.W8 precedes)**; the publish-or-relocate ACT is **KF.W8's**, joint with banked C-12.

### F5 · Standing riders carried (not cited)

- **KF-AV-28 · STANDING SUPERSESSION RIDER (KF.W7's defining lock) — carried, not cited.** Banked at `kf-AnimationVisualizer.md:35`, `kf-PlaybackRibbon.md:36`, `kf-SequenceScrubber.md:36`: KF.W7's S-9 evaluation (KF-AV-28) may supersede any behavioral cure on a NO-WAVE-OWNER row banked under an evaluated surface; a per-surface SWAP verdict discharges the bespoke rows banked under that surface with the component. Rows in this spec governed by the rider are spent CONDITIONALLY: no cure on a governed row lands before KF.W7's verdict for its surface, or the executing seat accepts the throwaway explicitly; at execution the seat STATES which governed rows a SWAP verdict has discharged BEFORE spending any cure (KF-W7 G15's discharge discipline).

  **Rows in this spec governed by the rider (the enumeration the clause requires; RULINGS R-10 · KF-W2-CHECK D-7)** — exactly one, and it is load-bearing:

  - **L-6/C-4** ⟨kf-KeyframeTimeline:47⟩, whose surface is the **timeline cluster** — the surface **`KF-W7 §Gates · G1`** puts in its verdict table (`KeyframeTimeline.vue`, 312 L; `TimelineHoverPreview.vue`, 38 L; the table's other four are TimelineTrack 246 L, SequenceScrubber 162 L, TimelineCaret 70 L, AnimationVisualizer 256 L — re-read at the current bytes this round). **Consequence, stated plainly: G-W2-7's delegation-target obligation is CONDITIONAL.** The gate's clause *"the façade publishes both halves L-6/C-4 hand-rolled … so KF.W7's delegation has a target"* was written as unconditional; under KF-AV-28 a **SWAP** verdict on the timeline cluster discharges L-6/C-4 **with the component**, and the emit-half obligation is **moot** — the façade would then owe a delegation target to a component that no longer exists. The edge was declared from KF.W7's end (its header **Standing supersession rider (KF-AV-28)**, *"the wave's defining lock"*) and unacknowledged from this one; it is acknowledged here.
    *Anchor note (R2-7, repair round 2)*: the round-1 receipt read `KF-W7.md:234` for the verdict table and `KF-W7.md:7` for the rider. **Both are struck as coordinates**: `:234` was a *Runnability* paragraph even at the round-1 bytes (KF-W2-CHECK D-7), and at the repair-round-2 bytes it was a horizontal rule (**re-verified at round-2 close, U-2: `KF-W7.md:234` still resolves to that rule; `KF-W7.md:7` still resolves to the rider it named — both remain struck as coordinates on the rule, not on their resolution**) — the sibling has been written since. **The gate id and the named rider are the anchors; no line number in this receipt is load-bearing.**
  - **Not governed, and named so the boundary is checkable**: the façade itself, the Tier-A repoint, the Tier-C deletions, the cssom cure, both registries, and the entry-point contract are `src/`-side library acts under no evaluated surface — no KF.W7 verdict touches them. **The emit half of the façade is built either way**; what the rider conditions is only the *obligation owed to L-6/C-4's delegation*, not the surface's existence. A seat may therefore build the façade before KF.W7 reports, and must **state the L-6/C-4 discharge status before spending any cure that exists only to serve it**.

---

## §Gates

**Nine** (G-W2-2b minted at repair round 2 as G-W2-2's paired demo clause), all **born-RED with a named live witness** (L-19). Witnesses re-verify at wave-open against the stated ref (D-19). No gate here names a bar this wave may move. **Every witness command below was RE-EXECUTED at `origin/master 81a56990` (or, for the F0 matrix, against the installed `@mkbabb/value.js@4.0.0` dist) by the round-2 repair seat, and each is stated with the output it actually produced** — RULINGS-2 **R2-5**: a gate whose named command does not produce its stated reading is not a gate, and a witness a seat leaves standing without re-verifying is that seat's defect.

**G-W2-1 — The failure-posture registry is complete and single-sourced.** RED — the registry does not exist, and **both** prior counts (the charter's 5, the drafting seat's 8) are refuted by the bank.
*Witness*: the five library postures at origin/master, re-pinned by this seat (`adapter.ts:219-226` `parseSource` absorb · `scroll/grammar.ts:57-63` throw TypeError · `validate.ts:180-192` swallow → `[]` · `compile/selector.ts:23-35` throw `AnimationOptionError`/`EMPTY_PARSE` · `compile/value/compile.ts:33-38` throw TypeError) · the three consumer-side postures (R-4 · KAD-9 · C-7's `useTimelineBuild.ts:47-50` `console.error`, the file's only non-toasting failure against five toasting ones at `:121/:133/:137/:148/:155/:157`) · **the three adopted at repair round 1** (§Carry F3, by id: kf-SpringTrace **D-12=L-6=C-4** silent-swallow · kf-KeyframesStringControls **C-2** bare mount path over a throwing serializer · kf-KeyframesStringControls **D-6/L-BL-3/C-1** `formatEditor` no-posture latch) · **and the five adopted at repair round 2** (§Carry F3 rows 12–16, by id: kf-TimelineCaret **L-15** · kf-KeyframeTimeline **L-11** · kf-KeyframeTimeline **D-15** · kf-KeyframesAddDialog **KAD-10** · kf-EasingTarget **KF-ET-28**). Each is a banked cell with a file:line, not an inference; all sixteen were re-read at the corpus's current bytes this round.
*Assertion*: **every banked failure-posture cell in the WHOLE 58-record corpus has exactly one row in one place** — **16 today**, scope spanning `src/` **and** `demo/`; **zero Tier-A sites handle a parse failure outside the façade**. Fails today at **0-of-16 enumerated** (no registry exists) and 21 out-of-façade parse-surface call sites. **The basis is named in the gate on purpose**: this gate's two prior denominators (37 records, then 37 records again) were samples published as closures, and a gate whose basis is unstated cannot be falsified by finding a record it never read. Two footnotes are part of the assertion, not decoration: posture 1 is **unreachable on the R1 class** (F0's mechanism finding — a registry that lists ABSORB without that footnote is a registry that lies), and the **two positive rows** (kf-SpringPhysicsFacet **★ S-7**'s `withErrorToastAsync` + Retry house idiom; kf-ChannelControls **SUP-3**'s `trySetOption`, the corpus's only §7.5-measured cell) are recorded as **reference rows, outside the count**.
*Count discipline (restated at repair round 2 — the failure mode this gate has now reproduced three times)*: the drafting seat convicted the charter's 5 and gated on its own 8, which the registry refuted the same day; round 1 restated 11 over 37 records, which the 58-record sweep refuted. **The recurrence is not arithmetic — it is basis.** So: **16 is a FLOOR under an enumeration over the 58, not a target**; a seventeenth banked posture found at wave-open is **added and named in the same commit**; the gate reds on an *unenumerated* posture, never on the arithmetic; and **the gate reds if its stated basis is smaller than the corpus** — the clause that makes a fourth recurrence a gate failure rather than a later seat's discovery. Numbers inherited from a charter, a prior seat, or a check file are not evidence here — the 58 records are.
*Falsifier*: delete a posture row and the completeness check reds; add a handler outside the façade and the site check reds; produce a banked posture cell with no row and the gate reds by construction; **enumerate over fewer than 58 records and the gate reds on its basis**. Not contrivance — every posture is a file:line in the tree, and the basis is a directory listing (`ls docs/tranches/V/megatranche/registry/adjudicated/kf-*.md | wc -l` → **58**, re-run this round).

**G-W2-2 — Tier-A single-entry (`src/` arm).** RED — **21** parse-surface call sites over **10** runtime importer modules at the ref of record.

*Why this gate was re-cut at repair round 2 (RULINGS-2 R2-6.1 · KF-W2-CHECK D-1) — three independent faults, each fatal, each reproduced by the round-2 seat before the re-cut was written*:

```
$ git grep -nE "import[^;]*\b(parseStylesheet|…|collect(…))\b" 81a56990 -- src/     → 0 hits          (1)
$ git grep -nP "import[^;]*\b(parseStylesheet|…|collect(…))\b" 81a56990 -- src/     → 5 hits, 5 files (2,3)
    src/animation/compile/frame/compiler.ts:40   ← kf's OWN parseKeyframeSelector, from "../selector"
    src/animation/compile/value/compile.ts:1 · engine/options.ts:17 · resolve/browser.ts:3 · validate.ts:47
```

1. **Inert.** POSIX ERE defines no `\b`; git grep's `-E` engine matched nothing. **A gate asserting 21 sites was witnessed by a command naming none, at the ref the spec pins** — the exact failure this wave's own doctrine (a phantom coordinate convicts) exists to catch.
2. **Line-oriented against multi-line imports.** Even repaired to `-P` it names **4** of the 10 parse-surface modules and is blind to `adapter.ts` (block `:1-14`), `easing/registry.ts` (`:11-15`), `emit/format/options.ts` (`:24-28`), `selector.ts` (`:2-6`), `engine/css/metadata.ts` (`:22-30`), `scroll/grammar.ts` (`:32-44`). A seat could route the four single-line importers through the façade and the check would read *"0 modules"* with six runtime grammar importers still standing.
3. **Self-conflating.** `frame/compiler.ts:40` — `import { FRAME_ID_SCALE, parseKeyframeSelector } from "../selector";` — is **kf's own** wrapper, the precise conflation §Carry F1 corrects for two of the banked thirteen. The gate imported the bug it was written to fix.

*Witness (re-cut, TWO commands, each re-executed at `81a56990` this round with its output stated)*:

**(i) Module enumeration — specifier-anchored, so kf's own symbols cannot enter.**
```
$ git grep -n 'from "@mkbabb/value.js/css"' 81a56990 -- src/ | wc -l   → 29 specifier lines
$ git grep -l 'from "@mkbabb/value.js/css"' 81a56990 -- src/ | wc -l   → 27 files
```
**(ii) Per-file runtime-symbol read over exactly those files — multi-line tolerant by construction (it reads whole import blocks, not lines), and `type`-filtered.**
```
$ for f in $(git grep -l 'from "@mkbabb/value.js/css"' 81a56990 -- src/ | sed 's|^81a56990:||'); do
    git show 81a56990:$f | perl -0777 -ne 'BEGIN{$F=shift @ARGV}
      while(/import\s+(type\s+)?\{([^}]*)\}\s*from\s*"\@mkbabb\/value\.js\/css"/gs){
        next if $1; for my $m (split /,/,$2){ $m=~s/^\s+|\s+$//g; next unless length $m;
        next if $m=~/^type\b/; print "$F :: $m\n" } }' "$f"; done
  → 25 runtime specifiers over 13 modules
```
**Stated reading, reproduced**: **13** runtime importer modules — **10 on the parse surface** (`adapter` ×6 · `easing/registry` ×1 · `emit/format/options` ×1 · `selector` ×1 · `value/compile` ×1 · `engine/css/metadata` ×2 · `engine/options` ×1 · `resolve/browser` ×1 · `scroll/grammar` ×6 · `validate` ×2 = **22 grammar/collector specifiers**), **2 emit-half** (`emit/css-text.ts:8` · `frame/interp-slot.ts:8`, `serializeCssColor` — Tier-D, out of denominator), **1 stray** (`resolve/function.ts` `coerceToSyntax`). Against those 13 sit the **21 call sites** enumerated in §Carry F1 with their lines.

**Each of the three faults dies structurally, not by patching the regex**: (1) the commands carry no `\b` and no `-E`, so there is nothing to be inert about; (2) command (ii) reads **whole import blocks** across newlines, so no multi-line importer can hide; (3) **both commands are anchored on the specifier `"@mkbabb/value.js/css"`**, so `frame/compiler.ts:40`'s `from "../selector"` cannot match — **kf's own `parseKeyframeSelector` is excluded by construction, not by a name on a deny-list.** The one place the two names legitimately meet is `compile/selector.ts`, which imports the **package's** `parseKeyframeSelector` **aliased to `parseValueSelector`** (`:2-6`, read) — a real value.js entry, correctly counted; the aliasing is what made the conflation possible and is now stated in the gate.

*Census corrections carried*: two of the banked thirteen (`adapter.ts:266`, `engine/css/animation.ts:176`) name kf's **own** `resolveKeyframes`; `frame/compiler.ts:146` is the unnamed Tier-A-adjacent consumer; `resolve/function.ts`'s `coerceToSyntax` is the stray runtime import, delete-or-declare at open.
*Assertion*: **exactly one module in `src/animation/**` imports value.js's grammar entries or collectors at runtime** — command (ii) returns the parse-surface symbols at **exactly one path, `parse-facade.ts`**, and command (i)'s file count may still exceed 1 (the type surface and the emit half legitimately stand). **21 sites → 1 module.**
*Falsifier*: a second runtime import edge of any grammar/collector symbol anywhere under `src/animation/**` reds it — and, because command (ii) enumerates by file, **a repair that removes an importer without routing its call sites reds too** (the sites in §Carry F1 are the second half of the denominator). Out of scope, explicitly: the emit-half `serializeCssColor` imports (Tier-D, §4.4 lockstep, moved at W3/W8) and the type-only surface — the façade collapses *behaviour*, not types (27 `src/` files name the subpath, most type-only; 2 of the 15 lexically-runtime statements are inline-`type` with no runtime edge; collapsing any of that is a different, unmandated act).

**G-W2-2b — the façade gate is NOT `src/`-scoped: the demo arm.** RED — **6** demo-side runtime grammar/collector modules at the ref of record, covered by no gate in this wave until this round.
*Why it exists (RULINGS-2 R2-6.1's scope clause · KF-W2-CHECK D-6)*: §Carry F1 carries **KF-KE-54**'s binding consequence verbatim — *"the gate guarding the façade **must not be `src/`-scoped**"* — and G-W2-2's assertion and check were both `src/`-scoped. The spec stated the binding and then violated it in the only place it mattered. This clause is the cure; it is a **paired clause of G-W2-2**, not a new obligation, and it changes no wave's bounds.
*Witness*: the same two commands, re-aimed at `demo/` and re-executed this round —
```
$ git grep -l 'from "@mkbabb/value.js/css"' 81a56990 -- demo/ | wc -l   → 7 files (1 type-only: timeline/timelineTypes.ts:1)
  … per-file runtime read (command ii)                                  → 8 runtime specifiers over 6 modules
```
`KeyframesEditor.vue:123` (`parseCssScalar`, call `:186`) · `keyframes/utils/parseAnimationCSS.ts:1-5` (`collectStyleRules` `:36` + `collectAnimationOptions` `:41`) · `scenes/square/useSquareDemo.ts:4` (call `:82`) · `scenes/square/useSquareTumble.ts:2` (`parseCssColor` call `:22`; `serializeCssColor` = emit half) · `utils/keyframeSelector.ts:1-4` (`parseKeyframeSelector`, call `:15`) · `utils/reference-data/animationDescriptions.ts:128` (call `:76`). Enumerated in full at §Carry F1.
*Assertion*: **the demo-side set is CENSUSED AND FROZEN at six modules / eight runtime specifiers, and no demo module gains a new runtime grammar/collector edge while this wave is open.** **This wave opens no demo call site** (§Excluded; the execution gate) — so the clause asserts *enumeration and non-growth*, never a cure. The cures are named and owned elsewhere: `KeyframesEditor.vue` → KFED-UNIT · `useSquareTumble`/`useSquareDemo` → the square packet at KF.W11 · `keyframeSelector.ts`'s serializer limb → KF.W8 (MISS-β2) · `parseAnimationCSS.ts`'s deep-import limb → **KF.W8 §Gates · G1** · `animationDescriptions.ts` → NO-WAVE-OWNER (KF-TFP-20's neighbourhood).
*Falsifier*: a seventh demo-side runtime grammar/collector module, or a ninth runtime specifier among the six, reds it — and **a `src/`-only re-scoping of either clause reds the pair**, which is the KF-KE-54 breach written as a check rather than as prose.

**G-W2-3 — Tier-C regex deletion.** RED — all six present at origin/master, each verified by this seat.
*Witness*: `presets/catalog.ts:16` · `emit/view-transition.ts:146` (serialize→regex-reparse) + `CQ_UNIT_RE` `:136` (consumers `:202/:207`) · `engine/composition.ts:175` · `emit/format/format.ts:135-144` · `svg/draw-svg.ts:89`.
*Assertion*: **0 of 6 survive; each deletion lands in the same commit as the façade call that replaces it.** The `format/format.ts:341` de-paren, **`resolve/conditional.ts`**'s whitespace-canonicalisation (`:102`) / `legacyClauses` (LEG-1, `:27`/`:52`) tail — **re-pathed at repair round 2 (R2-5(b)); the banked `resolve/resolve-if.ts` spelling has no bytes at the ref of record and a unit dispatched on it would report success having written nothing** — and `resolve/function.ts`'s `coerceToSyntax` are ruled **delete-or-declare**. Their coordinates are **resolved here, not deferred to open** (R2-5: a witness a seat leaves for later is a witness it has not verified); a seventh Tier-C site found at re-derivation is carried, not silently absorbed.
*Falsifier*: a deletion that lands without its replacement call is a regression, not a cure; the round-trip net (G-W2-7) is what distinguishes them.

**G-W2-4 — cssom identifier-injection cure.** RED — the unescaped interpolation ships today.
*Witness*: `src/animation/ingest/cssom.ts:214-216`, read verbatim at origin/master by this seat. Companion: the `:28-33` VJ-9 self-declared handoff comment, read verbatim.
*Assertion*: a fixture `@keyframes` name carrying regex metacharacters (via CSS escapes) **links correctly through ingest**; zero constructed-`RegExp` identifier interpolation remains in `cssom.ts`. Today: throws or mis-matches. Under **this wave's declared commit-family rule** (§Carry F2 — the fabricated "kf-SquareScene law" is struck per KF-W2-CHECK D-1; the rule binds this wave's bounds only and claims no corpus authority) the `:28-33` comment becomes a **test obligation** — either the fixture covers the stated robustness bound or the comment is corrected in the same commit.
*Falsifier*: the fixture is the falsifier — it fails today by construction and cannot be made to pass by narrowing a grep.

**G-W2-5 — R1 ingress census + entry-point reconciliation (GATE-VERDICT §F-2 correction).** RED — §F-2 currently reads *"no known consumer feeds the crash shape"* (verified live at `GATE-VERDICT.md:42`), falsified by an executed witness.
*Witness*: the F0 matrix, executed by this fold seat against `node_modules/@mkbabb/value.js/dist/subpaths/css.js` (4.0.0) on 2026-08-28 — five entries throw on empty-argument colour functions; every entry throws on non-string; `parseStylesheet` accepts `42|{}|[]` as an empty sheet. Plus C-7's demo chain, re-walked byte-exact at origin/master (`:239-244` / `:251-261` / `:263` / `:264` / `useTimelineBuild.ts:40-50`).
*Assertion*: the census names, **per ingress, the exact value.js entry reached and the executed outcome at the pinned artifact**, covering the colour organ AND the easing/CSS organ; **the boundary set is recorded as negatives, ENUMERATED OVER THE WHOLE 58-RECORD CORPUS, so no later seat re-probes it**; §F-2 is corrected **by addendum** (E-3, beside the pinned authority — never a rewrite).
*Basis clause added at repair round 2 (RULINGS-2 R2-11 · KF-W2-CHECK D-5)*: this sentence — *"recorded as negatives so no later seat re-probes it"* — was **false as written at both prior rounds**, because the set was measured over 37 records and asserted over 58. Nine banked R1 negatives sat outside it (§Carry F0's round-2 table, **X1–X9**), eight in unaudited records and **one (kf-SpringTrace S-A) in an audited one**. The set is now closed over the 58 and **the gate reds if its enumeration basis is smaller than the corpus** — the same basis clause G-W2-1 carries, for the same reason: a register that cannot be falsified by a record it never read is not a register.
*Assertion, third clause (unchanged, restated here after the basis clause so the gate reads as one)*: **the fuzz corpus is extended to the malformed class** — `test/compile/grammar-fuzz.test.ts` today generates *"random VALID @keyframes fragments from MODEL grammars (not raw-string fuzz)"* by its own docblock (verified verbatim) — it is **structurally incapable** of producing `oklch()`, which is why eight audits missed the class. `CSSCodeEditor.vue:116` is the adopted entry of record for that corpus.
*L-19 status*: the lock the **drafting seat's agglomeration input** imposed (the artifact it called "the CARRY", struck as provenance at §Provenance — R-12; it is input, never authority) is **DISCHARGED AT AUTHORING and rewritten**: *"R1 = `parseCssColor("oklch()")` shipping crash"* may now be cited **with the scope correction attached** — the class is empty-argument colour notation across five entries plus the non-string arm, not a `parseCssColor` fact and not a `parseCssValues`-only fact (that input's reconciliation hypothesis — "the R1 shape lives at `parseCssValues`, NOT at `parseCssColor`" — is also corrected by execution: it lives at **both**, and at three more).
*Why this is the wave's highest-value gate*: it can fail in the direction that **retires** a claim. It already did — twice: KF-HA-19's and K-6's **conclusions** are re-scoped by execution while their measurements stand (K-6's reachability guard is CONFIRMED and carried, not retired — §Carry F0, KF-W2-CHECK D-2), and the drafting input's dissent-resolution hypothesis is sharpened.

**G-W2-6 — Entry-point conformance: keyframe-selector seams use the selector grammar.** RED — two grammars in one component, frontier-durable.
*Witness*: `KeyframesEditor.vue:123` (import) / `:186` (call) `parseCssScalar` (both verified at origin/master) against the sibling `requireKeyframeSelector` (`demo/utils/keyframeSelector.ts:14-15`, called at `useKeyframeOps.ts:174` — both verified). Executed by this seat 2026-08-28: `parseCssScalar("500%"|"-20%")` → `ok:true`; `parseKeyframeSelector("500%"|"-20%")` → `ok:false [keyframe_selector_invalid]`; `parseKeyframeSelector("from"|"to"|"50%")` → `ok:true` at `0`/`1`/`0.5`; and the precision correction — `parseCssScalar("from"|"to")` → **`ok:true`** (keyword payload), so the banked "rejects from/to" is a *field* behaviour, not an entry behaviour. Corroborating helper-skip pattern: KF-KE-46 (`onUpdateStart` hand-rolls `percentSelector`, already exported and already imported by the closure — *"the third skipped-helper site, which is what made the blockers a pattern"*).
*Assertion*: the façade publishes **one** answer per seam — which grammar belongs where — and a conformance test pins it; `parseCssScalar` appears at **zero** selector seams. **CONTRACT ONLY at this wave**: the call-site edit is KFED-UNIT's, after KF.W0 and after KF.W4's vue-tsc gate. This wave does not open the call site.
*Falsifier*: point the conformance test at the current call site and it reds; the correct-grammar reference implementation already exists in-tree, so the contract is a publication, not an invention.

**G-W2-7 — Round-trip regression net covers the façade.** RED — no fixture exercises a façade that does not exist.
*Witness*: `test/fixtures/keyframes/` — **verified by this seat: 14 `.css` inputs + `manifest.json`** at origin/master (`box-shadow`, `color-achromatic`, `color-chromatic`, `drop-shadow`, `filter-multi`, `linear-easing`, `matrix3d`, `multi-prop`, `opacity-3stop`, `per-kf-easing`, `property-block`, `steps-easing`, `transform-multiarg`, `var-calc`) — three round-trip modes (byte · one epsilon row for chromatic colour, explicitly *"a value.js HANDOFF … NOT a kf serializer channel-drop"* · text rows for `var()`/`calc()`), consumed by `test/compile/roundtrip-fidelity.test.ts`. In-tree oracle precedent: `scroll/grammar.ts:143` `roundTripScrollCSS` already codifies `serialize(parse(s)) ≡ s` — *"the pattern the rest of the seam lacks."*
*Assertion*: **all 14 fixtures pass THROUGH the façade at the same fidelity mode they pass today**; the epsilon row stays a declared value.js handoff and is **not silently widened**. **Second clause, CONDITIONAL (KF-AV-28 · §Carry F5 · RULINGS R-10)**: the façade publishes both halves L-6/C-4 hand-rolled (declaration parse **and** declaration emit) so KF.W7's delegation has a target — **unless KF.W7's S-9 verdict for the timeline cluster is SWAP**, which discharges L-6/C-4 with the component and moots the delegation-target obligation. The executing seat **states the L-6/C-4 discharge status before spending the emit-half cure**; the round-trip clause above is unconditional and stands either way.
*Falsifier*: widening the epsilon row to make a fixture pass is the convicted failure mode (S.W3's re-baseline); the manifest's mode column is fixed at wave-open and may not be edited by the implementing seat.

**G-W2-8 — Serializer publication (the emit mirror) — CANDIDATE, edge-conditioned.** RED — 3 copies, 0 exported.
*Witness*: `emit/format/format.ts:20` (private; bank's consumers `:132/:201/:280/:313`) · `emit/css-text.ts:58` (private; consumer `:75`) · `demo/utils/keyframeSelector.ts:7` (exported; two consumers) — all three re-read by this seat; `emit/index.ts` export list read whole: no keyframe-selector serializer appears. Corruption set exact (bank): 7, 14, 28, 29, 55, 56, 57, 58.
*Assertion*: **one published symbol; 3 → 1.** **CONDITIONED ON A NAMED ORDER (OP-6 / RULINGS R-16): KF.W8 PRECEDES.** The banked routing decides it — `kf-TimelineCaret.md:46`, *"→ KF.W8 (publish-or-relocate, joint with banked C-12)"* — so **KF.W8's row 10 / G3 is the publication act**, and this gate covers the **façade-side contract only**: the emit mirror exists and is the façade's single caller. The count-gate (3 → 1) **arms if-and-only-if W8 has not preceded**, which under the declared order it will have.
*Why conditional, and why that is no longer a circle*: a gate whose act belongs to another wave is contrivance if declared unconditionally (L-19) — but "UNORDERED, each waiting on the other" was the W2↔W8 mutual conditional the ruling struck. The order is declared at both ends and **re-verified by id at the current bytes this round**: **`KF-W8 §Rows · MISS-β2 (unit d · G3)`** carries *"the publication act, and it **PRECEDES KF.W2**"*, and **`KF-W8 §Cross-edges · → KF.W2/W3`** carries *"**KF.W8's MISS-β2 row (unit d / G3) is the publication act and PRECEDES**; KF.W2's emit-façade work gates the **façade-side contract only** and **arms if-and-only-if KF.W8 has not preceded** … Reciprocated at KF.W2's OP-6."* So the arming is a **checked edge against a named predecessor**, not an assumption and not a standoff. *Anchor note (R2-7)*: the round-1 receipt cited `KF-W8.md:254`, which was that spec's `## Sequencing` heading at the repair-round-2 bytes, not the reciprocal (KF-W2-CHECK D-7); **the row id and the cross-edge heading are the anchors** and no line number here is load-bearing. ⟨**round-2 close re-verification (U-2)**: the characterisation of the struck coordinate above was measured at the repair-round-2 bytes; the sibling has moved again since and it no longer resolves to what is described. **Recorded, not re-derived — a struck coordinate is never re-adjudicated**, and the strike stands.⟩

---

## §Sequencing

**Hard ordering (blocking, in order):**
1. **KF.W0 §B-12 substrate settle** (OP-2). The wave's opening commit **states its ref** — `origin/master 81a56990` or later — and no anchor is trusted before it. The §Bounds drift table is this wave's contribution to that settle and should be consumed by KF.W0's re-anchor pass rather than re-derived.
2. **Phase 1 (`.a`) publishes both registries before any cure commit.** The posture registry (G-W2-1) and the ingress census (G-W2-5) size everything after them, and **KF.W7 may not decide a failure posture before KF.W2 publishes the registry** (declared at both ends — KF-W7.md G14 resolves against this registry, verified).
3. **The façade exists before any Tier-A repoint and before any Tier-C deletion** — a deletion without its replacement call is a regression (G-W2-3's falsifier).
4. **`emit/format/format.ts` is written by `.c` only** (shared file; §Bounds disjointness).
5. **G-W2-6 publishes the contract; KFED-UNIT edits the call site** — after KF.W0 and after KF.W4's vue-tsc gate, because *"nothing in the template layer is verifiable until a `.vue` file can fail a build"* (KF-CE-16, the sequencing head).
6. **No repin.** OP-5 — the façade must be green against installed 4.0.0 at close, or KF.W3 inherits an unmeasurable delta.

**Commit families (rows that must not split):** the façade + its first Tier-A repoint (one commit — a façade with zero callers is not a seam); each Tier-C deletion **with** its replacement call; the cssom cure **with** its fixture **and** the `:28-33` comment correction (**this wave's declared commit-family rule**, §Carry F2: a comment stating an invariant about a mechanism this wave changes is corrected or made executable in the cure commit, never left standing beside a changed mechanism — the previously cited "kf-SquareScene law" was fabricated and is struck, KF-W2-CHECK D-1); the ingress census + the §F-2 addendum; the entry-point contract + its conformance test.

**Packet homing (NO-WAVE-OWNER, homed as authoring requires):** the **square** packet's **parse arm** is HOMED HERE **as a census cell and a contract consumer only** — `useSquareTumble.ts:22` `parseCssColor` (lane-library §4.6's named R1 crash surface) and `useSquareDemo.ts:82` `parseCssScalar`.
**Precision correction at repair round 2 (KF-W2-CHECK D-9; re-executed against the installed 4.0.0 this round, output stated).** The round-1 sentence read *"it throws on `oklch()`/`calc()`"* — an **unqualified reachability claim standing beside a pair-reading lock whose whole content is that reachability claims must be qualified.** Only the **empty-argument** form throws:

```
parseCssColor("oklch()")           → THROW TypeError: Cannot read properties of undefined (reading 'replace')
parseCssColor("calc()")            → THROW TypeError (same)
parseCssColor("calc(1px + 2px)")   → ok:false [css_syntax]        ← NOT a throw
parseCssScalar("calc(1px + 2px)")  → ok:false [css_syntax]        ← the banked D-27 "→ ERR" reading, confirmed as a Result, not a throw
```

**Restated**: at this seam `parseCssColor` **throws on the empty-argument forms** `oklch()` / `calc()` / `rgb()`, and **returns `ok:false [css_syntax]` on well-formed-but-unsupported input** — `calc(1px + 2px)`, `color-mix(in srgb, …)` — and `ok:false [color_context_required]` on `var(--x)`. The distinction is load-bearing in both directions: it is what makes **K-6's token guard** true (no CSS token can resolve to empty-args), and it is what leaves **kf-SquareScene D-27/L-7/C-9**'s authored-CSS path intact (the `!ok → throw` at `useSquareTumble.ts:23` converts an `ok:false` into a throw *in kf's own code*, which is the crash surface — a fact the collapsed sentence erased). Both readings survive **only** when the form is named. **The cure is the square packet's, whose owning wave is KF.W11** (canonical 17-packet roster, RULINGS R-15) — this wave **declares the seam and does not cure it**.

**The square record's own parse rows travel with the seam (repair round 1; KF-W2-CHECK D-2/D-6). Homing a packet's parse arm while dropping the record's parse findings is how a wave inherits a reachability claim the bank already qualified.** Three banked facts ride the `useSquareTumble.ts:22` cell and are stated wherever it is:

1. **K-6's reachability guard** ⟨kf-SquareInstrument:24/:100⟩ — *"R1's true input is empty-args `oklch()` (throws in my run) **which no token can produce** (`getPropertyValue` → `""` → the `if (value)` guard). Safe regardless of cascade order."* **A bare "it throws on `oklch()`" is not a statement of this seam's reachability** and must not be written as one.
2. **The pair reading (RULINGS R-7a, binding)** — K-6 kills exactly one claim, **R1-through-a-token**. It does **not** kill the authored-CSS/`!ok`-throw path, which is established by kf-SquareScene **D-27/L-7/C-9** (*"`calc()` is ordinary authored CSS; `parseCssScalar("calc(1px + 2px)")` → ERR → `num()` throws"*, five throw sites bricking the rAF loop) and by kf-EditorShell **C-19**'s RR-2. **Token path and authored-CSS path are read as a pair**: C-4 governs the token→R1 path, C-5 governs the authored-CSS/throw path, and neither is asserted through the other.
3. **The colour arm's stake is bounded by kf-SquareScene C-1** — *"the colour half of the scene's value.js consumption is **dead paint**"* (the sole colour write is occluded by an opaque `background-image`). So the tumble sweep's *visible* payoff is currently zero while its *crash* exposure is total, and kf-SquareScene **MISS-4**'s per-frame re-parse is what moves the throw *into* the frame path. The census records all three; the cure — degrade-not-throw in the frame path, plus the library rider on `RAFPlayback._run` — is **KF.W11's**, with the engine-seam half routed as the bank routes it.

`demo/utils/reference-data/animationDescriptions.ts:76` `parseTimingFunction` is homed here as the only demo-side timing **seam** (census cell); the easing organ's two banked *findings* (**KF-ET-2** · **KF-TFP-20**) are censused at §Carry F0 with their NO-WAVE-OWNER dispositions intact.

**Sibling packets (routing restated on the canonical 17-packet roster, R-15; the drafting seat's routing was stale)**: **cube · sequence · spring · square · amiga** → **KF.W11**; **CARD-UNIT** → **KF.W12**; **dock-menu** and **transport/ribbon** → **KF.W13**. **Correction (KF-W2-CHECK D-5): the spring packet does NOT carry "no parse row"** — `kf-SpringTrace.md:140` books it *"with the parser posture (D-12/L-6/C-4 + the filter-cell)"*. Its **posture half is adopted here** (§Carry F3, row 9) and its **cure travels with the packet to KF.W11**, one fail-explicit cure for both mechanisms, per the bank's own cure-shape lock. The remaining packets carry no parse row of their own; each is stated in §Excluded with its owner so the grep trail shows it considered.

**Cross-edges (declared from this end):**
- **KF.W0 · Substrate Settle** — HARD PRECONDITION. MISS-α5 ≡ RR-β K-BASE: the audited tree is a hybrid; `demo/utils/keyframeSelector.ts` is untracked and absent from HEAD, so MISS-β2's copy 3 exists only once the ref is stated. **Owed to KF.W0**: the six-path drift table above.
- **KF.W4 · K-Quartet Hygiene** — SEQUENCING PRECONDITION **for the demo-side arms only**. The `src/` Tier-A/B/C arms are covered by `check:lib` and proceed independently. **This wave depends on KF-CE-12's depcruise-over-demo arm landing at W4**, because KF-KE-54 proves the current graph gate scopes `src/` only and the "one place value.js enters the graph" premise is false. **Owed to KF.W4**: the corrected 21-site / 15-importer denominator, so the graph gate is authored against a real number.
- **KF.W3 · Parser Consumption (GATED)** — SUCCESSOR; this wave is its pre-work. W3 owns the repin `4.0.0 → 4.1.x`, the `serializeCssValue` fork retirement, the **DUAL-1** unification (kf's `reverseAnimationShorthand` at `emit/css-text.ts:17` vs value.js's called at `emit/format/options.ts:121` — two functions, one name, both live, both verified) and the 8-curve delta. **KF.W2 MUST NOT REPIN.** W3 also inherits KF-CB-33's scoping input: the R1 colour class was unreachable in CopyButton yet value.js broke it through the **easing/CSS organ** — *"exactly the class a §4.6-scoped parser wave would miss."* **Owed to KF.W3**: the F0 matrix as the repin's before-table — every cell must be re-measured at 4.1.x, and any cell that changes from THROW to `ok:false` is the repin's headline, not a footnote.
  **The mutual-assumption hole is closed at this end (RULINGS R-1b, one sentence, stated because neither parser wave may assume the other holds it): the R1 negative/posture census of record is KF.W2's F0.** Consequences both ways: the four boundary rows the two waves each assumed the other held (kf-DemoGlobalChrome **C-10** · kf-SquareScene **C S-C** · kf-KeyboardShortcutsModal **C-§1/R-B** · kf-SpringTrace **D-12=L-6=C-4**) are adopted **here**, by id, with their do-not-re-probe locks; and the eight dotted `KF.W2/W3` rows (**KF-APP-56 · EditorShell C-19 · CubeAxisLines C·§4 · KF-KE-54 · TimelineTrack SUP-3 · ChannelOptions C·S-4 · ChannelControls C's L-1 · CubeTarget #11**) are parked on **this** side and carried in F0, while KF.W3 counts them in its own call-map denominator as exclusions-with-named-owner (**owner = KF.W2**, per R-7b). Reachability questions the bank routes to W3 — kf-KeyframesStringControls **C-2**'s `!ok` arm, kf-AmigaScene's R1 row — stay W3's and are **not** answered here.
- **KF.W7 · Timeline Evaluate** — CONSUMER. **`KF-W7 §0 · OP-5`** already names this wave, re-quoted verbatim at the current bytes: *"**KF.W2's parse façade must exist as L-6/C-4's delegation target.** … The cure is DELEGATE, and delegation needs a target: `@mkbabb/value.js/css` exports the collectors; the sibling path already honours the doctrine (`timelineEngine.ts:13` `parseAnimationCSS`, `:90` `serializeCssValue`)."* **`KF-W7 §Gates · G14`** ("one declared failure posture across cluster + dialogs") resolves against **this wave's registry** and may not be decided before it publishes. **All EIGHT of G14's named consumers now have a row here** — C-7's silent-`console.error` posture, R-4's sixth, KAD-9's seventh, and the five adopted at repair round 2 (**L-15 · L-11 · D-15 · KAD-10**, plus **KF-ET-28** from the easing organ): the edge is closed at both ends, where round 1 left it one-ended. Every one is **cured** there, not here.
  **KF-AV-28 acknowledged from this end (RULINGS R-10; §Carry F5).** KF.W7's defining lock — the S-9 evaluation may supersede any behavioral cure on a NO-WAVE-OWNER row banked under an evaluated surface — governs **L-6/C-4** here, whose surface (`KeyframeTimeline.vue`) sits in the verdict table at **`KF-W7 §Gates · G1`**. So G-W2-7's delegation-target clause is conditional on that verdict, and this wave's registry publication is **not**: W7 may not decide a posture before the registry exists, and the registry does not wait on the SWAP verdict. Both directions are now declared at both ends.
- **KF.W8 · Structure & Colocation Settle** — PAIRED ACT, lockstep (§4.4), **and now ORDERED: KF.W8 PRECEDES (OP-6 / RULINGS R-16)**. MISS-β2's publish-or-relocate (joint with banked C-12) is W8's act, banked there (`kf-TimelineCaret.md:46`) and booked there (W8 row 10 / G3); **G-W2-8 gates the façade-side contract only and arms if-and-only-if W8 has not preceded**. The previous "UNORDERED / mutual conditional" phrasing is struck — two waves each conditioned on the other is not an order, and the serializer would have been published by neither. KF-CE-12's body (deep-import extirpation, `convertPixelsToCh` publish-or-relocate) is W8's and must **not** be pulled here.
- **KFED-UNIT (NO-WAVE-OWNER, SS-1/SS-2)** — CONTRACT CONSUMER. KF-KE-3's call-site cure, KF-KE-54's prose cure and KF-KE-46's `percentSelector` helper-skip ride one packet, sequenced after KF.W0 and KF.W4. **Owed to KFED-UNIT**: the two precision corrections in F1 — `parseCssScalar` **accepts** `from`/`to`, and the reference implementation is `demo/utils/keyframeSelector.ts:14-15`.
- **value.js parser lane (X·PT / parse-that) + megatranche parser-band / STATE reconciliation** — was a BLOCKING evidence dependency; **now a DELIVERY.** The reconciliation this wave was told to wait for has been executed and is delivered upward: KF-HA-19's and K-6's conclusions are retired, C-7's and RR-2's are confirmed, the class is re-scoped (five entries + the non-string arm, not one entry), and two forwarded observations ride along (`parseStylesheet` silent-empty on non-string; the ABSORB posture unreachable on the R1 class). **No id is minted here.**
- **megatranche R1 / census risk-3** — FOLD-ONLY (anti-rename). The crash books **once**, there. This wave books no crash row; eleven kf records fold to R1 and every one is cited as a witness. A later seat re-booking the crash under a component id violates the identity guard.
- **`docs/tranches/V/apotheosis/parser-proof/GATE-VERDICT.md` §F-2** — DELIVERABLE OWED BY THIS WAVE, as an **E-3 addendum file beside** the pinned authority. The epoch rule holds: pinned authorities are immutable.
- **glass-ui BH relay (SS-6)** — **DECLARED NEGATIVE.** No row routed to this wave by the registry is glass-producer-owned; this wave raises **zero** BH-relay asks. Recorded so no later seat invents a relay here, and so no producer row elsewhere is converted into a demo-side hack. (The Skeleton export, separator `--ink-seam`, toast id/dismiss/action and HeaderRibbon relays belong to KF.W0/W6 and their own banks.)
- **Codex-provenance intake (`INTAKE-ADJUDICATION-2026-08-03.md`, keyframes B10–B21)** — **DECLARED NEGATIVE, checked, empty.** Its §3 CARRY routes B-lineage rows to KF.W0/W4/W5/W9/W10; **zero route to KF.W2**. Its own agreement register records **X-5**: *"ZERO overlap with the census top-10 live defects — the B-corpus is a harness audit, not product coverage, and must never be cited as such."* Recorded so no seat re-searches it.

---

## §Excluded

**No registry-routed row is unaccounted, over the WHOLE 58** — the **62** routed cells land above (28 at authoring + 19 at repair round 1 + **15 at repair round 2**), each by its banked id. The drafting seat's *"No CARRY row is dropped (11 of 11 land above)"* is **STRUCK** here as it is at §Provenance and §Carry: it counted against a ledger that does not exist in-tree (RULINGS **R-12**; KF-W2-CHECK D-9), and a true sentence about the registry replaces it.

**THE BASIS LINE the round-1 §Excluded never wrote (KF-W2-CHECK D-8 — the mechanism behind every round-2 escape).** Round 1 audited **37** records and asserted closure over **58**, and no line here stated why the other 21 carried no routed row — so *"No registry-routed row is unaccounted"* rested on an unstated sample and could not be falsified by a record it had never read. Written now, at the bytes:

- **8 of the 21 DID carry a routed row** and were escapes, not exclusions: kf-ControlsPaneWrapper · kf-EditorHeader · kf-MatrixEditor · kf-PlaybackRibbon · kf-SequencePlayhead · kf-SequenceScene · kf-SpringHeatmap · kf-StartingStyleTarget — all eight adopted above as **X1–X8**.
- **13 carry no routed cell**, and are excluded **by measurement, with the instrument named**: kf-App.skeleton · kf-ChromeDock · kf-EasingScene · kf-EasingSidebar · kf-KfPillTabs · kf-RibbonBar · kf-SequenceAxis · kf-SequenceScrubber · kf-SequenceTarget · kf-SharePopover · kf-SpringScene · kf-SpringTarget · kf-TransportDock. `grep -cE 'KF\.?W2\b'` → **0** on twelve of the thirteen. The thirteenth, **kf-KfPillTabs**, returns **24** hits and **every one is `KF.W2-TABS`** (`grep -cE 'KF\.?W2-TABS'` → 24; the non-TABS residue is empty) — the lane-frontend §10 collision this spec declares at its head, **collapsed into KF.W6 by `KF-W6` §Taxonomy**, not an escape from here. **Owner for the TABS mass: KF.W6.** Owner for the other twelve: none needed — there is no routed cell to own.
- **The remaining escapes were in AUDITED records** (X9 kf-SpringTrace · X10–X15), so sampling explains only part of the failure and this line does not pretend otherwise (§Carry F0/F3 record the split).

These are the explicit non-carries, each with its reason and its named owner:

- **The R1 crash identity itself** — FOLDS to megatranche R1 (KF-CE-12 precedent). This wave books the **census**, the **reconciliation**, and one **mechanism finding** under that identity; it mints no crash row and no new id.
- **KF-CE-12's body** — the `@src/animation/resolve/browser` deep import, the module-scope `window` resize listener, `convertPixelsToCh`'s publish-or-relocate, the `sideEffects:false` falsification, `useKeyframesState.ts:1`'s identical breach — **KF.W8's**; the depcruise gate arm is **KF.W4's**. Only the `:116` ingress note is this wave's.
- **Every ingress cure** — `KeyframeTimeline.vue`'s hand-rolled parser/emitter (KF.W7), `KeyframesEditor.vue:186`'s wrong entry (KFED-UNIT), `useSquareTumble.ts:22` (the square packet's owning adjudication), the CSSPasteDialog and KeyframesAddDialog consumer contracts (KF.W7). **KF.W2 publishes surfaces and registries; it opens no call site.** The execution gate forbids opening product source at all until the begin-word.
- **The repin, the `serializeCssValue` fork, DUAL-1, the 8-curve delta** — **KF.W3's** (OP-5). Naming DUAL-1's `emit/format/options.ts:121` half in §Bounds is a bounds fact, not a claim on the unification.
- **MISS-β2's publish-or-relocate ACT and banked C-12** — **KF.W8's**; G-W2-8 is conditioned accordingly. Only the façade-side emit mirror and the two E-3 corrections are this wave's.
- **KF-APP-15** (HeroAurora's static import putting glass-ui's aurora module + value.js's colour graph in the boot chunk) — **KF.W6's**. KF-APP-56 records only that the boot graph is **outside** the R1 surface.
- **kf-SpringTrace C-2 ≡ L-3** — the serialize→regex-reparse shape one layer out. Cited as **evidence** that the Tier-C class is wider than the six named `src/` sites; **NO-WAVE-OWNER**, not gated here. Widening G-W2-3 to cover it would make an unowned row this wave's blocker.
- **KAD-1** (the `innerHTML` DOM-XSS sink) — **KF.W5's, front-loaded**; blocks nothing here. Carried with it: kf-KeyframesAddDialog's **DISSENT-1** (this record's BLOCKER deliberately diverges from the twin's demoted pair) — recorded only so a later seat reading the shared posture registry does not re-open the divergence.
- **KF-CO-1 / KF-CO-8** — no KF-CO row is routed to this wave by the registry; their cures are **SEQUENCED by LP-1 (no write→render edge)** in the owning wave's spec. Noted so the grep trail shows the lock considered, not dropped.
- **kf-ChromeDock M-4 + the MbabbMenu MUST-CARRY rider** — **KF.W13's**; no row here. M-4's worded cure ships an unopenable menu without the MbabbMenu rider — the lock rides where the cure lands, not here.
- **TD-1/TD-2's bundled cures** — likewise KF.W13's; the bundle lock is a standing sequencing edict and neither row lands here.
- **The cube · sequence · spring · dock-menu · CARD-UNIT packets** — **routing restated on the canonical 17-packet roster (RULINGS R-15); the drafting seat's routing was stale and its "no parse row" blanket was false.** Owners: **cube · sequence · spring → KF.W11** (Demo Scene Repair) · **CARD-UNIT → KF.W12** (Authoring-Surface Repair) · **dock-menu → KF.W13** (Chrome, Dock & Transport Repair, minted). **The spring packet is the exception the drafting seat's blanket erased (KF-W2-CHECK D-5)**: `kf-SpringTrace.md:140` books it *"with the parser posture (D-12/L-6/C-4 + the filter-cell)"* — so its **posture half is CARRIED here** (§Carry F3, row 9, by id, with the bank's cure-shape lock *"one fail-explicit cure for both mechanisms"* quoted) and only its **cure** travels with the packet to KF.W11. Cube, sequence, CARD-UNIT and dock-menu carry no parse row of their own; each is named here so the grep trail shows it considered, not dropped. The **square** packet's parse arm **is** homed here as a census cell and contract consumer, its cure KF.W11's (§Sequencing).
- **The type-only `@mkbabb/value.js/css` import surface** — 27 `src/` files name the subpath at origin/master (this seat's count); only 15 carry a runtime statement and only 10 touch the parse surface. G-W2-2 gates **runtime grammar/collector symbols** only. Collapsing the type surface is a different act, unmandated by any registry-routed row, and would collide with KF.W4's `noUnusedLocals` arm.
- **The `format/format.ts:341` de-paren, `resolve/conditional.ts`'s LEG-1 tail (`:27`/`:52`) and whitespace canonicalisation (`:102`), and `resolve/function.ts`'s `coerceToSyntax`** — recorded as **re-derivation candidates**, not asserted members of the six. A count not verified is not a gate (L-19); all three are ruled delete-or-declare at open. **Re-pathed at repair round 2 (R2-5(b)): the banked `resolve-if.ts` spelling is a HEAD-only artefact and named nothing at the ref of record** — the coordinates above are resolved, not deferred.
- **`demo/…/utils/parseAnimationCSS.ts`'s three non-census limbs** — its **deep `@src/` import** (`:7`) belongs to **KF.W8 §Gates · G1**'s denominator (already inside it — a declared edge, not a new owner); its **THROW posture** (`:32-34`) is a *tree-side* cell with no banked id and is therefore **outside G-W2-1's floor** (booking it would inflate the number the gate depends on, the same discipline that keeps `parseStylesheet`'s silent-empty finding out); its **docblock invariant** (`:22-24`) is outside this wave's §Bounds and so outside this wave's commit-family rule. Only the **census cell** is this wave's (§Bounds).
- **kf-ChannelControls SUP-3's cure, and its neighbouring `KF-CO-33` limb** — SUP-3 is adopted here as a **positive reference row only** (§Carry F3), with no cure and no owner change; `ChannelOptions.vue` is outside this wave's §Bounds, and **KF-CO-33's `proof:bezier-*` limb at that file is routed by RULINGS-2 R2-4 to the OPTIONS-UNIT packet at KF.W12** — named so the grep trail shows it considered and refused, not dropped.
- **The `parseStylesheet` silent-empty finding** — measured here, **forwarded** to the value.js parser lane. It is a value.js-side posture, not a kf posture; booking it in this registry would inflate the count the gate depends on.

---

*Authored as KF.W2's spec **from the 58-record adjudicated registry directly** — the drafting seat's "authored against the CARRY ledger whole" is **STRUCK** (RULINGS **R-12**: the only in-tree carry is `KF-W6-CARRY.md`, 201 rows / 54 records, and it is not this wave's); the fold seat (M-23) verified the drafting seat's product row-for-row and applied three fold repairs (G-W2-2 symbol-scoping, the `coerceToSyntax` cell, the `keyframe_selector_invalid` code). **Repair round 1 (2026-08-28)** then executed RULINGS **R-1b · R-10 · R-12 · R-16** verbatim and cured **D-1..D-10**: 19 escaped registry cells adopted by banked id (anti-rename), the fabricated "kf-SquareScene law" struck and the obligation re-founded, K-6's bank statement corrected with its reachability guard carried, G-W2-1 restated at an enumerated floor of 11, the KF-AV-28 rider carried verbatim with its one governed row named, the phantom ledger struck at every site, and OP-6/G-W2-8 re-declared as an order (KF.W8 precedes). The R1 witness and the entry-point contract witness were **executed read-only by the fold seat** on 2026-08-28 against the installed value.js 4.0.0, discharging OP-3 and re-scoping the claim it was meant to protect.

**Repair round 2 (2026-08-28)** executed RULINGS-2 **R2-5(b) · R2-6.1 · R2-11** verbatim under the standing **R2-5 witness re-derivation law** and the **R2-7 citation idiom**, and cured **D-1..D-10**. What changed, and the one sentence that explains all of it: **this spec twice published a closure claim over a corpus it had measured as a sample, and gated on a command it had not executed.** Both are now structural, not corrected in prose.

(i) **The AUDITED basis is the whole 58** — the verb table names every record and closes 37 + 21 = 58 by set difference at the bytes; the routed denominator is **re-derived at 62**, not incremented from 47, with the fifteen round-2 cells adopted by banked id at the bank's own disposition: **X1–X9** the R1-boundary negatives (§Carry F0 — eight in never-audited records and **one in an audited one**, recorded rather than smoothed), **X10–X14** the posture cells (§Carry F3 rows 12–16, closing the one-ended **`KF-W7 §Gates · G14`** edge and restoring **KAD-10**, the busy-state limb of a contract this spec already quoted from KAD-9), **X15** kf-ChannelControls **SUP-3** as a positive reference row outside the count. G-W2-1's floor moves 11 → **16**, and **both** completeness gates gain a **basis clause that reds when the enumeration spans less than the corpus** — the only cure that stops a fourth recurrence.
(ii) **G-W2-2 is re-cut as a two-command witness** whose three prior faults die by construction rather than by patching a regex: no `\b`/`-E` left to be inert, whole-import-block reads that multi-line importers cannot hide from, and **specifier-anchoring that excludes kf's own `parseKeyframeSelector` structurally**. **G-W2-2b** is minted as the paired demo clause, because **KF-KE-54**'s binding *"the gate guarding the façade must not be `src/`-scoped"* was carried in prose and violated in the gate.
(iii) **`resolve/resolve-if.ts` is re-pathed to `resolve/conditional.ts`** at all three homes — the seventh drift-class member, missed *inside the drift table whose whole subject is that class*.
(iv) **`parseAnimationCSS.ts` enters the census of record** — the demo's principal CSS→AST adapter, absent while the spec cited the very line that imports it; its deep-import limb is a **declared edge** to **`KF.W8 §Gates · G1`**, never a new owner, and the unreconciled *"8 demo-side seams"* resolves to **8 runtime specifiers over 6 modules**.
(v) Cross-spec receipts now cite **stable anchors**; six were re-resolved and their round-1 line numbers struck — three had already drifted at the round-1 bytes, and **`KF-W6.md:10` now points at a sibling's own round-2 header**, which is the argument for the idiom in one fact.
(vi) The `useSquareTumble` reachability sentence is qualified to the **empty-argument** form, re-executed — the distinction that keeps **K-6's token guard** and **kf-SquareScene D-27/L-7/C-9**'s authored-CSS path both true.

**Every figure, path, command output and quotation in this file was re-derived by the round-2 seat at the substrate of record** — keyframes.js `origin/master 81a56990736ced5b5edde0b84c527680ac7689b1`, value.js at the current tree bytes, the F0 matrix against the installed `@mkbabb/value.js@4.0.0` — and none was inherited from a ruling's prose, a check file, a CARRY, or this spec's own prior text. **No ruling addressed to this wave was declined.** Every carried row keeps its banked id; every lock, rider, cure-shape lock and dissent is carried unchanged; nothing was renumbered or re-homed away from its bank.

Status: **planned**. Every gate is born-RED with a named live witness. No gate has run. No product source was opened for writing — this spec is the only file written; every keyframes.js read was `git show`/`git grep`/`git ls-tree`/`git cat-file` at `origin/master`, and the only execution was a read-only node probe of the installed value.js dist. Execution awaits the owner's begin-word.*
