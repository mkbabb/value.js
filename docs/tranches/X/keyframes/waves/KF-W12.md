SERVED MODEL: claude-fable-5-1

# KF.W12 — Authoring-Surface Repair

**Wave**: KF.W12 · Authoring-Surface Repair — the SIX NO-WAVE-OWNER authoring UNIT packets of RULINGS R-15's canonical seventeen (`KF-W10 §6.D · SUCCESSOR-FORMATION REGISTER` row 2; homed at `KF-W4 §Sequencing`, the R-15 homing block; rostered at `KF-W1 §Cross-edges` edge 9): **CARD-UNIT · OPTIONS-UNIT · KFED-UNIT · EDITOR-UNIT · APPLY-UNIT · AXISLINE-UNIT**. Each UNIT was minted at its home record as *"a proposed single wave-surface … for SS-1/SS-2 spec authoring to consume; no census KF.W0–W10 row names this repair, and scattering it would orphan its one root cause"* — this file is that consumption. **The six UNIT names are the banks' own minted wave-surface names — packet names, never wave names; this file mints no seventh.** Repair, not evaluate: the cure shapes are ruled at the records; this file HOMES, ORDERS and BOUNDS them, re-derives nothing, mints nothing, and re-books nothing a sibling wave owns (register row 2's ANTI-RE-BOOK, honoured below).
**Half**: frontend (keyframes.js demo — `demo/components/instrument/keyframes/**`, `…/transport/channel-controls/**`, `…/transport/controls-pane/**`, one `demo/utils/helpers.ts` carve, one `demo/scenes/easing/EasingSidebar.vue` carve, and the one cube-scene file AXISLINE-UNIT owns). **Sub-tranche**: X·KF. **Track**: B.
**Authority**: `docs/tranches/X/COHESION.md` §0d; `EXECUTION-RUNBOOK.md §4.5`; ratified under `docs/tranches/V/megatranche/SCOPE.md` M-25. **Minting authority**: `KF-W4 §Sequencing`, the R-15 homing block, KF.W12 bullet. **Terminal disposition of record for the X·KF close**: `KF-W10 §6.D` row 2, cited by STABLE ANCHOR ALONE (LAW C(3)).
**Alias law (R-B idiom)**: `KF.W12` denotes `X.KF.W12`; a search alias, never a rename.
**Status**: planned. No product byte is written by this file; authoring it opened no product source for writing; execution awaits the sequencing head (§State).
**Ref of record**: keyframes.js `origin/master` **`69095552`** — ⟨cmd⟩ `git -C /Users/mkbabb/Programming/keyframes.js rev-parse --short origin/master` → `69095552` (2026-09-18); `git rev-parse --short HEAD` → the same; `git status --porcelain` → two untracked coordination letters, **zero modified product paths**. **The drift is load-bearing and larger here than anywhere**: the six records are banked at `8281638c`; ⟨cmd⟩ `git rev-list --count 81a56990..origin/master` → **203**; KF.W8's colocation settle moved almost every file this wave owns (`CSSCodeEditor.vue`, `KeyframesEditor.vue`, `KeyframeCard.vue`, `ChannelOptions.vue`, `RibbonBar.vue`, `ControlsPaneWrapper.vue` sit at paths no bank names), and several BLOCKERs were **already partly cured at the frontier** (kf-KeyframeCardList's FE-3 `startScalar`; kf-KeyframesEditor's EE-03) — **every such row lands here as a RE-REGRESSION GUARD, not a cure** (the records' own instruction), and every line anchor is re-resolved at open (KF.W0 §B-12; D-19; the KF-AT-28 re-anchor law).
**Standing supersession rider (KF-AV-28)** — register row 2, lock 1: *"on any W7-evaluated surface a packet touches."* **Measured: none of the six UNITs writes a KF.W7-evaluated surface** (the six are `KeyframeTimeline / TimelineTrack rail` · `TimelineCaret` · `TimelineHoverPreview` · `SequenceScrubber` · `AnimationVisualizer` · `SpringTarget / SpringTrace idiom arms`; this wave's bounds contain none of them for writing — `KeyframeTimeline.vue` is a read-only witness). The rider therefore governs no row here and is named so its absence is a measurement, not an omission; **the verdict is settled anyway — six KEEP-BESPOKE, ZERO SWAP, discharge set EMPTY** (`execution/B/KF-W7.md`, value.js `4c03ceda`), and **no seat of this wave may write `DISCHARGED by KF.W7 SWAP verdict <surface>, <date>` on any row**. **The KFED-UNIT second-end note (lock 3)**: KF.W7 cites *"the R4-8 §6.D register row"* as KFED-UNIT's second end *until the successor spec exists* — **this file is that spec**, and its §Sequencing `→ KF.W7` row is the reciprocal end; W7 promised nothing and is owed nothing (*cite it, promise nothing*).
**Provenance**: **AGGLOMERATED** by the fresh fold seat of the SS-1/SS-2 authoring block (M-23 §3) from the two BLIND drafts at `waves/drafts/fable/KF-W12.md` and `waves/drafts/opus/KF-W12.md`, the register of record `KF-W10 §6.D` row 2, the minting citations, the home records in `docs/tranches/V/megatranche/registry/adjudicated/` — the **eleven homes** kf-KeyframeCardList · kf-KeyframeCard · kf-ChannelOptions · kf-LayerConfigPanel · kf-TimingFunctionPanel · kf-ControlsPaneWrapper · kf-KeyframesEditor · kf-KeyframesAddDialog · kf-CSSCodeEditor · kf-KeyframesStringControls · kf-RibbonBar · kf-CubeAxisLines, **plus the records that route single rows into a UNIT by token** — kf-EasingSidebar (KF-ES-12) · kf-EasingScene · kf-PlaybackRibbon (KF-CO-15's ribbon side, read here) · kf-SpringPhysicsFacet (SPF-1/-2/-13 → OPTIONS-UNIT; SPF-11/-19/-20/-23 → KFED-UNIT) · kf-ChannelControls (a witness) — ⟨cmd⟩ `grep -c 'CARD-UNIT\|OPTIONS-UNIT\|KFED-UNIT\|EDITOR-UNIT\|APPLY-UNIT\|AXISLINE-UNIT' registry/adjudicated/kf-*.md | grep -v ':0$'` → **13 records name at least one UNIT token** (CSSCodeEditor 44 · ChannelOptions 44 · CubeAxisLines 24 · EasingSidebar 1 · EasingScene 1 · KeyframeCardList 35 · KeyframesEditor 52 · KeyframesStringControls 16 · RibbonBar 4 · LayerConfigPanel 19 · SpringPhysicsFacet 8 · PlaybackRibbon 1 · TimingFunctionPanel 3); kf-KeyframeCard and kf-ControlsPaneWrapper name no token and are consulted by seam; rows consumed **by mechanism, deduped by identity, never transcribed** (M-25); `execution/B/KF-W7.md` (the verdict table); and the live keyframes tree, READ-ONLY. Every disagreement between the arms was resolved at the register's bytes and the live tree — §Provenance at the foot. Validated against `docs/precepts/instructions/tranche/WAVE_SPEC.md` (L-20). **No gate exists here without a named live witness (L-19).**

| verb | state | basis |
|---|---|---|
| AUDITED | YES | the records above, each ADJUDICATED-not-VERIFIED, each routing its behavioral remainder into one of this wave's six UNITs by token or by seam |
| SPECIFIED | YES — this file, the spec of record, written by the fold seat over both blind arms | **the packet denominator is the register's — 6 of 17; carriage over row-ids is NOT asserted in this file's voice (LAW B)** — the rows land by their home records' routing lines cited at §Carry; the carriage census is the successor conformance pass's artefact |
| IMPLEMENTED | NO | stays NO until the gates green after the sequencing head lands |
| VERIFIED | NO | a successor close's act; **KF.W10's close stamps the AUTHORED ELEVEN only** (COHESION §0d) |

**Goal criterion.** This wave succeeds if, when work ends, **a user can author a keyframe and see what they authored**: a card's offset reads, writes and removes (CARD); the options card's dropdowns open, its switch switches, and every widget re-renders when the engine writes — and never before the render edge exists, LP-1 (OPTIONS); the retiming control retimes, Apply applies something, and the action cluster is reachable (KFED); the code editor is a language-aware, keyboard-escapable editor whose format path fails loudly (EDITOR); the applied stylesheet binds the class it adds under one `cssIdent`-derived name with one lifetime (APPLY); and the axis-lock reveal reveals what it claims on the demo's one keyboard registry (AXISLINE). **A wave that restores the write legs before the render edge, lands a second name derivation beside `cssIdent`, or lands sixty hygiene edits and leaves `getClassName()` producing `keyframes-style-X` while the injected sheet's selector is `.x`, has failed this goal** whatever its gate count says — KF-KE-4's chain is a feature that has never worked, audited four times without one reader noticing, and it is the single row that makes this wave's name honest.

**Execution shape**: **7 seats across 4 phases** — phase 1: 2 parallel (`.a` CARD-UNIT ∥ `.b` OPTIONS-UNIT); phase 2: 2 parallel (`.c` KFED-UNIT — after `.a`, same files, and after KF.W11 `.b` ∥ `.d` EDITOR-UNIT); phase 3: 2 parallel (`.e` APPLY-UNIT — after `.c` and `.d`, shared composables ∥ `.f` AXISLINE-UNIT — after KF.W11 `.a`); phase 4: 1 serial (`.g` close). Peak concurrency **2**, inside the four-workflow cap. APPLY-UNIT is last **by the banks' own sequencing**: N-8's one-derivation rule and EDITOR-UNIT's projection contract both land before it. Tiering per M-12: the three design-shaped units (`.b`'s five-step order and the `useEasingPickerSeat` specification, `.c`'s apply identity, `.d`'s Monaco arm with its byte budget) are Fable-worker ∥ Opus-worker → fresh-Fable arbiter; the rest Opus solo.

---

## §State

**Name**: KF.W12 — Authoring-Surface Repair
**Opens after**: **G-KFW4-1 lands GREEN** (the sequencing head — *"nothing in it is defensible until a `.vue` file can fail a build"* is five of the six records' own precondition in their own words; **measured RED at this seat, twice**: ⟨cmd⟩ `npx vue-tsc --noEmit -p tsconfig.json 2>&1 | grep -c 'error TS'` → **54** · **54**; the LEDGER's KF.W4 row is `CLOSED 2026-09-17 (honest-RED: G-KFW4-1 …)`; **this is the hardest precondition this wave has, because every defect it cures lives in the one surface nothing type-checks**) · **KF.W6 CLOSED** (the `EditorHeader.vue` delete + the R4-1 atomic adoption — register row 2's ANTI-RE-BOOK; verified ⟨cmd⟩ `git ls-tree origin/master -- demo/components/instrument/shell/EditorHeader.vue` → *(no output)* — the file is gone; W6's `kf-focus-ring` rename re-anchored `KeyframeCard.vue`) · **KF.W7 CLOSED** (KF-CE-2's parent half is W7's landed work) · **KF.W8 CLOSED** (the card cluster moved: `KeyframeCard.vue` sits at `keyframes/components/` by `34b051eb`) · **KF.W0 CLOSED** (two KFED blockers already repaired at the frontier — the guard obligation) · **KF.W11 `.a`'s OD latch-family commit** (AXISLINE-UNIT's upstream half; `.f` alone waits on it) · **KF.W11 `.b`'s `useKeyframeOps.ts` L-2 commit** (the shared-path seam; `.c` alone waits on it).
**Agents**: 7 (a→g); phases as above; **no jury**.
**Hard gate**: **seven conditions, ALL RED**, measured at `69095552` and pasted at §Gates, each double-run.
**Status**: planned

## §0 Open preconditions (checked at wave-open; NOT close gates)

| # | precondition | state at authoring (this seat, 2026-09-18, at `69095552`) |
|---|---|---|
| **OP-0** | **G-KFW4-1 GREEN** — the head; every UNIT record names it as its own precondition. | **RED — 54 · 54.** HARD. |
| **OP-1** | **KF.W11 `.a` — the OD latch family (KF-AX-1's registry adoption + blur clear + expose reset) is LANDED** with its sha in KF.W11's record. | **UNLANDED** (KF.W11 is planned at this clock). ⟨cmd⟩ `grep -c 'useEventListener(window, "keydown"' demo/scenes/cube/orbital-drag/OrbitalDrag.vue` → **1** — the window latch is live. `.f` does not open until this reads **0** by KF.W11's commit; **this wave writes no `orbital-drag/**` byte** (the identity dedupe: `kf-CubeAxisLines.md:38` *"AXISLINE-UNIT (first; cure upstream in orbital-drag/)"* and kf-OrbitalDrag's latch family are ONE cure with ONE home — KF.W11's file; `.f` lands the consumer half). |
| **OP-2** | **KF.W11 `.b` — the `useKeyframeOps.ts` L-2 carve is LANDED.** | **UNLANDED**. `.c` opens on that sha; the two waves never write the file in parallel (§Disjointness); the `updateFromString` hunk is never re-touched here. |
| **OP-3** | **KC-34's edict** (register row 2, carried verbatim from KF-W4 §Sequencing): *the highlight path must stop replacing Vue-owned DOM BEFORE the KC-8/KC-9 keep-mounted repair lands.* | **BY CONSTRUCTION** — `.a`'s first commit is KC-34; KC-8/KC-9 land after it. Named so the order is a gate, not a habit. |
| **OP-4** | **The `cssIdent` publication** — `kf-KeyframesStringControls.md:55`: *"library half (publish `cssIdent`) → KF.W5; demo half (route all three derivations through it) → NO-WAVE-OWNER (APPLY-UNIT), sequenced after the publication."* | **PRESENT AT THE ARTIFACT, ABSENT AS A CONSUMER**: ⟨cmd⟩ `grep -c 'cssIdent' dist/keyframes.d.ts` → **2** · **2** (the publication appears landed at the built types); ⟨cmd⟩ `grep -rn 'cssIdent' demo` → **one hit, a comment** (`demo/utils/helpers.ts:10`) — **the demo has no `cssIdent` consumer; all three derivations are hand-rolled today**. Re-measured at `.e`'s open and the export's exact shape read there. If it reads **0** at open, `.e` lands the *identity* cure's derivation against the demo's existing in-tree derivation and names KF.W5's export as the edge — **never a second hand-rolled derivation** (N-8's own constraint). |
| **OP-5** | **The glass-ui producer half of KF-CO-1/KF-CO-8** — `open`/`modelValue` Boolean-cast props need `default: undefined` in LabeledSelect/LabeledSwitch (a 7.0.0 defect; the BH relay note is *already filed* per kf-LayerConfigPanel LP-3). | **PRODUCER-SIDE, NOT BLOCKING**: the consumer cure (`:is-open`→`:open`, the switch `model-value` rewire) is ours and lands regardless; **the producer's prop name is quoted from the installed `.d.ts` in the receipt** (a guessed prop name is a copied producer selector — a HIGH defect); the producer row rides the SS-6 accretion register (lock 2). Glass-ui is READ-ONLY always. |
| **OP-6** | **KF.W0's two frontier repairs (KFED)** — *"two blockers are already repaired at the frontier; a careless rebase of the in-flight migration re-lands them."* | **PARTLY DISCHARGED, and the risk is the reverse of the usual one.** `.c`'s FIRST act is verifying **which** frontier repairs exist (measured today: ⟨cmd⟩ `grep -c 'startScalar' …/KeyframeCardList.vue` → **2**), receipted, before spending a cure — a cure written from the bank alone re-introduces a fixed defect. |

**OP-0 is hard.** OP-1 and OP-2 gate one unit each. OP-6 is a receipt obligation.

---

## §Scope

1. **CARD-UNIT** — *"the keyframe authoring loop made honest"* across `KeyframeCardList.vue` + `KeyframeCard.vue` + `KeyframesEditor.vue`'s card seam: **KC-34 first** (the highlight path stops replacing Vue-owned DOM), then the offset readout (**KC-1** — PARTLY CURED at the frontier by FE-3's `startScalar`; the surviving obligations are fraction-for-percent, the named-selector fallthrough, and the canonical renderer still bypassed — `selectorText` is the one serializer), the offset writer (**KC-2 ≡ KF-KE-2 — ONE identity, ONE cure, homed HERE at the card seam**: `KeyframesEditor.vue:81` `frame.start.value = starts![i]` writes into a deep-frozen parse result, `:76` reads the fraction, `:88` `:step="1"` — read via `selectorPercent`, write by whole-selector replacement, domain `0..100`, fractional step, `marks`, per-thumb `aria-valuetext`, **with KF-KE-34; the freeze and the unit fixed in ONE motion** — *"fixing the freeze without the unit creates the 100×-destructive write L-B1 warned of"*; `percentSelector`/`selectorPercent` exist at `demo/utils/keyframeSelector.ts:44`/`:49`), the delete affordance (**KC-10**, with KF-CB-11's a11y shell at its bank — the removal affordance today is a mouse-only `<svg>`), remount discipline (**KC-8/KC-9** keep-mounted; KC-9 is the one-timer-for-N-cards data loss), the a11y triad (**KC-27**'s offset-bearing names, after KC-1; KC-28 · KC-29), the ref bookkeeping (**KC-17/KC-18/KC-32**), the tail, and **KF-KC-48 — the UNIT's acceptance test, born-RED, and it gates KF-KC-1** (⟨cmd⟩ `git grep -c "KeyframeCard" origin/master -- test/` → **0**).
2. **OPTIONS-UNIT** — in the record's FIXED ORDER (a travelling lock, not a preference — *a persistence cure landed before a capability restoration persists a broken state*): (i) **the one-line capability restorations** — `:is-open`→`:open` (measured **×4, not ×3**: ⟨cmd⟩ `grep -rn ':is-open' demo` → `ChannelOptions.vue:96` · `:120` · `:479` · **`LayerConfigPanel.vue:11`** — the fourth is LP-11's limb the bank already homes here) and the switch `model-value` rewire (`LayerConfigPanel.vue:90-91` `:checked`/`@update:checked`, measured) — **landed in the SAME commit as LP-1's write→render edge** (the lock: *"the write legs must not be restored without the render edge"*; `layerConfig` is a plain object off a never-proxied graph; `controlHosts` tracks group identity only; `invalidateEntries` flips dirty flags — the cure covers the whole prop surface, `blendAvailable` and `singleTarget`'s post-mount recompute included); (ii) **persist/representation/authority** — KF-CO-3 · -4 · -11 · -12 · -13 · -14 · -16 · -17; (iii) **the steps cluster** — KF-CO-9 ≡ KF-TFP-6 (the stored default 100 vs the vendor domain [1,12]) · KF-CO-10 · KF-CO-40 ≡ KF-TFP-5 (child owns the store, parent owns the engine); (iv) **a11y** — KF-CO-5 ≡ KF-TFP-7 + KF-CO-46 (`inert`, not bare class toggles; the house cure sits at `ChannelControls.vue` `:inert="!keyframesActive"`) + focus management; (v) **hygiene** — KF-CO-2 (the phantom-prop family), KF-CO-6, KF-CO-21..29, -31, -33..36, -38, -39, L·N-16, KF-CO-41/42/44/45, LP-6/LP-9/LP-11..18/LP-21..23, the LP-20 tail; **plus** kf-TimingFunctionPanel **KF-TFP-1 (BLOCKER)** — the `:key` derived from the state its own handler writes (`pickerKey` at `:102` from `seedPreset` at `:93`, bound at `:33`/`:35`; the first drag off a preset-matched quad remounts `EasingPicker` mid-gesture) — **cure = the `useEasingPickerSeat` composable (KF-ES-12) under the KF-ES-4/KF-CO-17 split-seam cure-lock; the one-prop cure is DEAD (register #3, killed three times)** — KF-ES-12 *"cross-referenced to KF-CO-17's OPTIONS-UNIT so the two specs converge on one artifact"*: **one composable SPECIFIED, not lifted** (seed, key, live-state echo predicate, container CSS, one parameter for where truth lives), consumed by both seats (`TimingFunctionPanel.vue` and `EasingSidebar.vue`'s two call-sites — the easing SCENE is not this wave's); **plus** kf-ControlsPaneWrapper **D-B1/N-2/C-2** (the drawer-inset adoption — `:style` on `<DrawerContent>`, NEVER the scoped-selector declaration; the producer side is DISCHARGED at 7.0.0) and its `proof:stage-visible` residue (KF-W4 §Excluded 19; ⟨cmd⟩ `grep -c 'proof:stage-visible' …/ControlsPaneWrapper.css` → **1**); **plus KF-CO-15's options-side half** (the sync bridge polls `effectiveT`/`started`/`reversed`, not `options.duration`) — **its ribbon-side extension is KF.W13's C-2 joint commit, which opens after this unit closes**; **plus the KF-CO-47 decision** (tooltip idiom vs slotted copy) for SPF-1/SPF-2 here and **SPF-13, spent by KF.W11 on the facet's bytes** — the decision is written in `.b`'s receipt.
3. **KFED-UNIT** — *"the keyframes authoring surface made honest"*: **OP-6's frontier verification first, receipted**; **KF-KE-4 — the headline: Apply-CSS has never applied anything** (⟨cmd⟩ `grep -rn 'keyframes-style-' demo` → **exactly two hits**: `useKeyframesState.ts:16` `keyframes-style-${animationUUID}` — the class — and `:42` `.replace("keyframes-style-", "").toLowerCase()` — the selector's name; *`"keyframes-style-" + X ≠ X.toLowerCase()` unconditionally*; **disposition verbatim: `→ KFED-UNIT (one apply identity: class = selector, or pass getTmpAnimationName() as the class; then re-derive L-M3/C-B2's residue against a working feature)`**); **KF-KE-6** (→ KFED-UNIT: lift `isApplied`/`prevPaused`/node ownership to the identity's altitude — per-animation, refcounted; `useHighlightCSS.ts:58-61` removes the node unconditionally, stranding two live survivors of the three-closures-over-one-animation seam); **KF-KE-12 ≡ kf-KeyframesStringControls N-5** (`clear()` never wired, `useKeyframeBrushApply.ts:29` — rides the apply-identity spec, wire it into `onUnmounted`); KF-KE-3 (`requireKeyframeSelector` at the field — `parseCssScalar` at `KeyframesEditor.vue:240`/`:303`; the S-5/C-S5 error posture stays); **KF-KE-5 (BLOCKER)** — the action cluster painted UNDER the opaque code plate (`KeyframeCard.vue:30` `absolute top-2 right-4` with no z rung; the `<pre>` at `:113` with its class list at `:117` still ending `relative`; **and the file's own `:38-47` comment states the mechanism verbatim — a defect whose own file explains why it is a defect is a comment-stated invariant**; a z rung on the cluster or drop the `<pre>`'s `relative`, then D-4's shell and the annotation's contrast become live); KF-KE-7 (the 700 ms unskippable gate before `removeKeyframeData` + KF-CB-11's shell, together); KF-KE-8 (PRM on the three engine animations); KF-KE-10..14, -17..22, -24's **pipeline half** (the hljs layer / re-tokenize pipeline — the TOKEN decision alone is KF.W6's rider), -25..29, -33..35, -37, -38, -41, -44, -46..52, -54..58, -60..66; KF-KE-62 / KF-KC-35's `proof:accent-census` residue (KF-W4 §Excluded 18); the add dialog (`KeyframesAddDialog.vue`; KF.W7's KAD folds stay theirs); SPF-11/-19/-20/-23 by token. **Identity dedupes stated**: KF-KE-2 → `.a` (the card seam); KF-KE-14 ≡ KF-APP-4 → KF.W4; EE-03's length-watch (frontier-cured) → re-regression guard only.
4. **EDITOR-UNIT** — *"the Monaco seam made honest"*: **KF-CE-3 FIRST in the repair order** (the bank's own ruling on `5fe2e4cb` — the WCAG 2.1.2 keyboard trap outranks the highlighting; `tabFocusMode: true` at create is a contrib-free fix; measured ⟨cmd⟩ `grep -c 'tabFocusMode\|accessibilitySupport' …/CSSCodeEditor.vue` → **1** — `accessibilitySupport: "off"` at `:176`, `tabFocusMode` nowhere); **the KF-CE-1/-4 decision, ONE, taken in `.d`'s receipt before a byte** — (a) restore the contribution set (bytes return, a11y contribs return) or (b) hand-register a Monarch tokenizer + `setLanguageConfiguration` + `tabFocusMode: true` (bytes stay saved; comment-toggle/find/context-menu stay absent BY DECISION and are SAID to be absent) — **there is no cheap highlighting-only import (killed register #3); either way `vendor-monaco` is re-measured and U.D5's 4.18→2.53 MB is not re-cited as like-for-like**; KF-CE-5 (`accessibilitySupport: "off"`, un-propped); **KF-CE-2's child half** (cancel-on-external-write + cancel-on-unmount, one `replaceContent()` seam — `demo/utils/helpers.ts:31-39`'s `debounce` returns a bare function over a closure-private timer with no cancel handle; consumers `CSSCodeEditor.vue:60`/`:132`/`:185`; **the parent half is KF.W7's landed work and is not re-booked**); **KF-CE-8 + the model→editor re-projection contract** (kf-KeyframesStringControls `:47` — a joint obligation with APPLY-UNIT); **KF-CE-9 + KF-CE-37 ≡ kf-RibbonBar M-3/C-8, one boundary** (`formatCSSContent` has no error path; `isFormatting` sticks — `KeyframesStringControls.vue` measured **4** sites; `RibbonBar.vue:28`-class Format button is the PRIMARY unguarded path; the `withErrorToastAsync` idiom one file over is the house form); KF-CE-2, -5..-7, -10, -11, -13..-15, -18..-21, -24..-29, -31, -33..-36, -38..-41, -43..-45, -47..-49; **KF-CE-41's law binds every row** — *the comment is part of the defect surface*; KF-CE-16 is KF.W4's.
5. **APPLY-UNIT** — *"the apply-CSS toggle, the name pipeline, the brush, the teardown"*: **N-8, first** — ONE `cssIdent`-derived name shared by `getClassName` and the emitted selector (`useKeyframeBrushApply.ts:33` `getClassName: () => options.styleId`; the escalation *"the Export artifact is equally invalid"* is REFUTED at the bank — `walkList` applies `cssIdent`), routed after OP-4's measurement and **after `.c`'s identity cure** (the identity is decided at KFED and routed through one derivation at APPLY — `useKeyframesState.ts` is `.c`'s in phase 2 and `.e`'s in phase 3, serial); **RB-6** (the Apply state outlives its only control — `v-if === 'keyframes'`; applied residue persists across tabs with no visible undo — the state and its affordance get one lifetime; `RibbonBar.vue:13/:49`); D-2/L-M-8, N-7, **D-25** (`respectReducedMotion` on the brush and sweep — with KF-KE-8, one PRM motion), S-6-as-corrected (the `prevPaused` mechanism is right; keep it), the `:57`/`:59`/`:65`/`:67` rows as routed. **The filed "both animate the same target simultaneously" scenario is DEAD at the bank (L-BL-1) — only the residue is cured; curing a killed scenario manufactures work.**
6. **AXISLINE-UNIT** — *"the axis-lock reveal made honest"*; 32 rows stand (0 BLOCKER · 4 MAJOR · 17 MINOR · 11 INFO): **KF-AX-1** (the demo-side consumer half — the reveal consumes the ONE registry; KF.W11 `.a` adopted it upstream in `orbital-drag/`; measured today the reveal binds `lock.x/y/z` at `CubeAxisLines.vue:11-14`), **KF-AX-2** (reveal semantics false three ways — rotate/translate/scale; armed-vs-active; the wheel sixth op — a prose/prop decision + the labels rider), **#57 / the Z stroke** (`rotateY(90deg)` at `:117` puts the stroke in a plane containing the view direction under `perspective: 1200px`; `rotateZ(90deg)` at `:113`; `width: 1000vw` at `:63`), KF-AX-4..-10, -13, -14, -16..-23, -28..-30; **every geometric assertion written against the settled `rotate3d(-1,1,0,30deg)` frame and frame-stamped per KF-AX-4** — the frame lives in KF.W11's `useCubeDemo.ts` and is re-derived read-only, never edited. **KF-AX-9's three raw `180ms` token rider is measured LANDED at the frontier** (⟨cmd⟩ `grep -n '180ms' demo/scenes/cube/CubeAxisLines.vue` → `:41` · `:81`, both comments; no declaration) — booked `LANDED-BY KF.W6` under GREEN-BEFORE-CURE, re-measured at `.f`'s open.
7. **Close** — receipts, the commit roster audited to §Bounds, the BH relay (OP-5's producer row; the seven producer rows this wave surfaces — the viewport-gated grid · the `steps(1, jump-none)` render throw · the preset seam · the slider readout seam · the keyboard registry's missing `defaultPrevented`/scope · unlayered scoped styles · the lossy `cn` table), E13, the LEDGER row.

### Triumvirate dispatch (WAVE_SPEC §3a)

- **Bounds whose expansion invalidates the wave**: any `src/**` write (`cssIdent`'s publication is KF.W5's); any `orbital-drag/**` write (KF.W11's); any `node_modules/monaco-editor` patch (a HIGH defect by standing law); any write to `EasingSidebar.vue` beyond the two seat call-sites (the easing scene is no wave's packet); any write to `PlaybackRibbon.vue` (KF.W13's).
- **Non-local gate failures**: G-KFW12-2 failing because the render edge cannot be built without engine-side change (`invalidateEntries` flips dirty flags only) → triumvirate with KF.W5 named; G-KFW12-4 failing because arm (a) restores bytes the bundle budget forbids and arm (b) cannot cure the keyboard trap → triumvirate on the fork; G-KFW12-5 failing because `cssIdent`'s exported shape at `.e`'s open does not match the demo's three call sites → triumvirate with KF.W5 named.
- **Diagnostic loops that halt at three**: the KF-CE-8 echo loop reproduced three times without a deterministic mount harness → halt.

---

## §Bounds

### B.1 Frontier re-anchor (this seat, read-only, `69095552`, 2026-09-18)

| # | inherited anchor | frontier truth | disposition |
|---|---|---|---|
| 1 | **KF-KE-4** — *"`keyframes-style-${superKey}-${animationId}`"* vs the lowercased strip | ⟨cmd⟩ `grep -rn 'keyframes-style-' demo` → **two hits**, both `useKeyframesState.ts` (`:16`, `:42`); the id form is one term. | RE-ANCHORED; the defect is unchanged and reproduces in a two-line grep — G-KFW12-3's witness |
| 2 | **KF-KE-2** — the retiming Slider at `:43`/`:44` | ⟨cmd⟩ `grep -c 'frame.start.value = starts' …/KeyframesEditor.vue` → **1** (the write at `:81`; the read `:76`; `:step="1"` at `:88`). | CORRECTED; cure shape unchanged |
| 3 | **KF-KE-3** — `parseCssScalar` at `:186` | ⟨cmd⟩ `grep -c 'parseCssScalar' …/KeyframesEditor.vue` → **2** (`:240` import · `:303` the call); `requireKeyframeSelector` live at `useKeyframeOps.ts:8`/`:173`. | CORRECTED |
| 4 | **KF-CE-2** — `helpers.ts:15-24` | `demo/utils/helpers.ts:31-39` — `debounce`, no `cancel`, closure-private timer; consumers ⟨cmd⟩ `grep -rl 'debounce' demo | xargs grep -l helpers` → `CSSCodeEditor.vue` · `useKeyframeOps.ts` · `useKeyframesParsing.ts` (**3**). | CORRECTED (path + lines); the LAW A census for the additive cancel handle is these three |
| 5 | **KF-KE-5** — `:11-13` vs `:41-45` | `KeyframeCard.vue:30` the cluster, `:113` the `<pre>`, `:117` its class list ending `relative`, **`:38-47` an in-file comment stating the mechanism**; ⟨cmd⟩ `grep -c 'class="absolute top-2 right-4' …/KeyframeCard.vue` → **1**. | RE-ANCHORED and PROMOTED: the comment is a comment-stated invariant (the MISS-3 law) |
| 6 | **`.focus-ring` at `KeyframeCard.vue:45`** (RULINGS-4 R4-4) | `kf-focus-ring` at `:117` (KF.W6, `a6418729`). | DECLARED DRIFT (E-3 — RULINGS-4 not amended); routed to KF.W13's policy as the census's other half |
| 7 | **`:is-open` ×3** (kf-ChannelOptions; KF-W4 §Sequencing's carried words) | **×4**: `ChannelOptions.vue:96` · `:120` · `:479` · `LayerConfigPanel.vue:11`. | CORRECTED: ×3 + ×1; the fourth is LP-11's limb, homed here already |
| 8 | **KF-TFP-1's cycle** | `TimingFunctionPanel.vue:33` `:key="pickerKey"` · `:35` `:preset="seedPreset"` · `:93` `seedPreset` · `:102` `pickerKey`. | anchors re-pinned; the one-prop cure stays DEAD |
| 9 | **KF-AX-9's three raw `180ms` at `:60-63`** | comments only at `:41`/`:81`. | LANDED-BY KF.W6 (GREEN-BEFORE-CURE); re-measured at `.f`'s open |
| 10 | **`cssIdent`'s publication** | `dist/keyframes.d.ts` **2** hits; demo consumer **0** (a comment at `helpers.ts:10`). | OP-4: present at the artifact, unconsumed |

### B.2 File bounds

All paths keyframes.js-relative at `69095552`; line counts ⟨cmd⟩ `wc -l` this seat.

| File | L | Access | Unit |
|---|---|---|---|
| `demo/components/instrument/keyframes/components/KeyframeCardList.vue` | 110 | modify | `.a` |
| `demo/components/instrument/keyframes/components/KeyframeCard.vue` | 168 | modify — **SHARED `.a` (the a11y triad, the ref contract) → `.c` (KF-KE-5's z rung + the `:38-47` comment)** (serial) | `.a` → `.c` |
| `demo/components/instrument/keyframes/KeyframesEditor.vue` | 430 | modify — **SHARED `.a` (the card seam: `:76-88` retiming Slider = KC-2 ≡ KF-KE-2) → `.c` (the rest)** (serial) | `.a` → `.c` |
| `demo/components/instrument/keyframes/composables/useHighlightCSS.ts` | 234 | modify — **SHARED `.a` (KC-34: stop replacing Vue-owned DOM) → `.c` (KF-KE-6's refcounted node ownership, `:30-33` adopt / `:58-61` remove)** (serial) | `.a` → `.c` |
| `demo/components/instrument/keyframes/composables/useKeyframesEditor.ts` | 57 | modify-carve | `.c` |
| `demo/components/instrument/keyframes/composables/useKeyframeOps.ts` | 219 | modify — **AFTER KF.W11 `.b`'s L-2 hunk (`:58-69`) lands; that hunk is never re-touched** (`:173` `requireKeyframeSelector` is the entry KF-KE-3 consumes) | `.c` |
| `demo/components/instrument/keyframes/composables/useToolbarKeyboard.ts` | 112 | modify-carve | `.c` |
| `demo/components/instrument/keyframes/components/KeyframesAddDialog.vue` | 289 | modify-carve (KFED rows naming the add dialog; KF.W7's KAD folds stay theirs) | `.c` |
| `demo/components/instrument/keyframes/composables/useKeyframesState.ts` | 61 | modify — **SHARED `.c` (the apply identity: `:16`/`:42`, class = selector) → `.e` (N-8: the derivation routed through `cssIdent`)** (serial) | `.c` → `.e` |
| `demo/components/instrument/keyframes/composables/useApplyCSS.ts` | 69 | modify — **SHARED `.c` (KF-KE-6's lift) → `.e` (RB-6's lifetime, D-25's PRM)** (serial) | `.c` → `.e` |
| `demo/components/instrument/keyframes/composables/useKeyframeBrushApply.ts` | 46 | modify — **SHARED `.c` (KF-KE-12 ≡ N-5: `clear()` wired) → `.e` (`:33` `getClassName` on the one name)** (serial) | `.c` → `.e` |
| `demo/components/instrument/keyframes/composables/useKeyframesParsing.ts` | 118 | modify-carve (the emitted-selector half of the identity, `:26`/`:39`) | `.e` |
| `demo/components/instrument/keyframes/KeyframesStringControls.vue` | 195 | modify — **SHARED `.d` (the `isFormatting` latch, KF-CE-9) → `.e` (the apply/brush/teardown rows)** (serial) | `.d` → `.e` |
| `demo/components/instrument/keyframes/CSSCodeEditor.vue` | 261 | modify | `.d` |
| `demo/utils/helpers.ts` | 88 | modify-carve (**`debounce` only, `:31-39` — an additive `.cancel` handle; LAW A census = its three consumers (§B.1 row 4), pasted before it lands; the change lands ALONE**) | `.d` |
| `demo/components/instrument/keyframes/monaco-themes/{Dracula,GitHub}.json` | — | modify (**conditional on KF-CE-1's arm (a)**; ~98 of 100 rules inert under the regression) | `.d` |
| `demo/components/instrument/transport/controls-pane/RibbonBar.vue` | 151 | modify — **SHARED `.d` (M-3/C-8 format half, the `:28`-class path) → `.e` (RB-6 apply half, `:13/:46/:49`)** (serial) | `.d` → `.e` |
| `demo/components/instrument/transport/channel-controls/ChannelOptions.vue` | 731 | modify | `.b` |
| `demo/components/instrument/transport/channel-controls/LayerConfigPanel.vue` | 124 | modify (`:11`'s fourth `:is-open`; `:90-91` the switch) | `.b` |
| `demo/components/instrument/transport/channel-controls/TimingFunctionPanel.vue` | 173 | modify (KF-TFP-1 → the seat composable) | `.b` |
| `demo/components/instrument/transport/channel-controls/composables/useTimingFunctionEditor.ts` | 248 | modify | `.b` |
| `demo/components/instrument/transport/channel-controls/composables/useEasingPickerSeat.ts` | — | **create** (KF-ES-12 ≡ KF-TFP-1's one artifact; SPECIFIED in `.b`'s receipt before the byte) | `.b` |
| `demo/scenes/easing/EasingSidebar.vue` | 238 | modify-carve (**the two `EasingPicker` seat call-sites ONLY** — the easing scene is no packet's; every other byte of the file is out of bounds) | `.b` |
| `demo/components/instrument/transport/channel-controls/ChannelControls.vue` | 384 | modify-carve (KF-CO-5/-46's `inert` posture; the L-2/C-2 `:key` row is KF.W7's OP-1 settlement and is NOT touched) | `.b` |
| `demo/components/instrument/transport/controls-pane/ControlsPaneWrapper.{vue,css}` | 307 / 150 | modify-carve (D-B1/N-2/C-2's `:style` inset on `<DrawerContent>`; the `proof:stage-visible` residue; LP-1's `controlHosts` tracking at the render edge) | `.b` |
| `demo/scenes/cube/CubeAxisLines.vue` | 120 | modify | `.f` |
| `test/demo/instrument/keyframe-card-offset-loop.test.ts` | — | create (KF-KC-48) | `.a` |
| `test/demo/instrument/channel-options-render-edge.test.ts` | — | create | `.b` |
| `test/demo/instrument/keyframes-editor-honest.test.ts` | — | create (the apply-identity execution among its cases) | `.c` |
| `test/demo/instrument/css-code-editor-seam.test.ts` | — | create | `.d` |
| `test/demo/instrument/apply-css-identity.test.ts` | — | create | `.e` |
| `test/demo/scenes/cube-axis-reveal.test.ts` | — | create | `.f` |
| `test/demo/instrument/{highlight-css-roundtrip,kf-toolbar-keyboard,ios-text-entry,value4-editor-boundary}.test.ts` | — | modify (existing witnesses extended, never weakened; `value4-editor-boundary.test.ts` is the non-deterministic fixture KF.W7 residual 15 names — **its timeout is NOT widened here**; if this wave's byte makes it deterministic, the assertion is what changes) | owning unit |
| value.js `docs/tranches/X/execution/B/KF-W12.md` | — | create (unit receipts; append-only) | every unit |
| value.js `docs/tranches/X/keyframes/evidence/W12/**` | — | create (born-RED baselines, the apply-identity execution receipt with both strings, the Monaco contribution census + `vendor-monaco` re-measurement, the Z triptych) | owning unit |
| value.js `docs/tranches/X/execution/LEDGER.md` | — | modify (this wave's row cells + appended lines ONLY) | `.g` |
| value.js `docs/tranches/V/coordination/INBOX.md` + the SS-6 accretion register (COHESION §4a) | — | modify (append) | `.g` |

**Do NOT touch**: `src/**` (`cssIdent` at `src/animation/compile/emit/backward/walk.ts`, `selectorText`, `requireKeyframeSelector`, `invalidateEntries` are consumed, never edited) · `demo/scenes/cube/orbital-drag/**` (KF.W11 `.a`) · the `updateFromString` hunk of `useKeyframeOps.ts` (KF.W11 `.b`) · `demo/components/instrument/timeline/**` (KF.W7; `KeyframeTimeline.vue` holds KF-CE-2's parent half and is a read-only witness) and `demo/components/playback/**` (KF.W11's drag-seam / KF.W13's ribbon) · `demo/components/instrument/transport/TransportDock*` and `demo/app/**` (KF.W13) · `demo/components/instrument/shell/**` (`EditorHeader.vue` is DELETED by KF.W6 — **KF-APP-41 is `ADOPTED-BY-KF.W6`; no row here re-books it**; kf-EditorShell `C-22`'s orphan-token limb likewise; `C-22`'s `D-27` · `RR-2 M7` limbs stay NO-WAVE-OWNER and are NOT adopted here; kf-EditorShell and kf-EditorStartScreen route no row to any UNIT by token) · `demo/scenes/**` except `CubeAxisLines.vue` and the two `EasingSidebar.vue` seat call-sites (KF.W11) · `node_modules/**` · `package.json` / `vitest.config.ts` (harness present; no manifest change owed) · every glass-ui tree (its `.d.ts` is read to quote prop names, never edited) · `scripts/dev/dev.sh` · every dated spec, the adjudicated registry, conformance artefacts (E-3).

### B.3 LAW A censuses — every delete / repoint / shim act this wave books

**LAW A (RULINGS-3): no delete or repoint is authorized on a line-count.** (1) **`debounce`'s cancel handle** (`helpers.ts:31-39`) — additive; census = the three consumers of §B.1 row 4; the receipt states no consumer's behaviour changes when it does not call `.cancel()`; lands alone. (2) **The Monaco contribution decision** (KF-CE-1/-4) — two honest arms only; either way `vendor-monaco` is re-measured. (3) **The `:is-open` → `:open` rewire** (4 sites) — census = the prop's declaration at the installed producer; the receipt quotes the `.d.ts`. (4) **The apply identity's cure** — census = every reader of the class token (the bank's own grep showed nothing else keys on it), re-run before either arm is chosen. (5) **The `useEventListener(window, …)` latch** — KF.W11 `.a`'s census, consumed by `.f` as landed.

### Disjointness (WAVE_SPEC §4a)

Eight paths are shared and each is resolved SERIALLY by the phase order, never in parallel: `KeyframeCard.vue`, `KeyframesEditor.vue`, `useHighlightCSS.ts` (`.a` → `.c`); `useKeyframesState.ts`, `useApplyCSS.ts`, `useKeyframeBrushApply.ts` (`.c` → `.e`); `KeyframesStringControls.vue`, `RibbonBar.vue` (`.d` → `.e`). `useKeyframeOps.ts` is a cross-wave serial path (KF.W11 `.b` → this `.c`); `ChannelOptions.vue` is a cross-wave serial path the other way (this `.b` → KF.W13's KF-CO-15 carve). Phase 1's pair (`.a` ∥ `.b`) is directory-disjoint (`keyframes/` vs `transport/channel-controls/` + `controls-pane/` + the `EasingSidebar.vue` carve); phase 2's pair (`.c` ∥ `.d`) shares nothing (`.d` owns `CSSCodeEditor.vue`, `helpers.ts`, the themes, `KeyframesStringControls.vue`'s latch, `RibbonBar.vue`'s format half — `.c` touches none); phase 3's pair (`.e` ∥ `.f`) shares nothing.

### Worktree plan (WAVE_SPEC §4b)

Parallel units run in sibling worktrees `keyframes-kfw12-<unit>`, merged by the orchestrator in phase order; serial dependents open on the merged sha named in the predecessor's receipt. No Cargo.

---

## §Carry — the six UNITs, homed

Rosters are the home records' routing lines at `69095552`'s registry bytes (immutable, E-3); ids are the records' own, for life. Internal ordering edicts travel intact. **Carriage is not asserted in this file's voice (LAW B).**

### U1 · CARD-UNIT (`.a`) — `kf-KeyframeCardList.md:30` (the mint) · `:135` (the verdict) · KC rows `:33-74` · kf-KeyframeCard by seam

**Order (KC-34's edict, then the loop)**: **KC-34** (D-17 — the blanking: the highlight path replaces Vue-owned DOM; it stops BEFORE keep-mounted) → **KC-1** (`selectorText(frames[i].start)` — the surviving obligations after FE-3; prerequisite for **KC-27**'s offset-bearing aria names, land together) → **KC-2 ≡ KF-KE-2** (the frozen-selector write; **the identity dedupe, stated**: kf-KeyframeCardList's scope names *"`KeyframesEditor.vue`'s card seam"* and kf-KeyframesEditor's KF-KE-2 names the same Slider with KF-KE-34 riding — ONE cure, homed HERE at the seam, `.c` inherits it landed; **KF-KC-19**: one field, two units — the Slider reads a `0..1` fraction onto a `−10..110` rail and writes `0..110` back — route both seams through the total pair) → **KC-8/KC-9** (keep-mounted; the one-timer-for-N-cards data loss) → **KC-10** (removal; with KF-CB-11's shell) → the a11y triad **KC-27 · KC-28 · KC-29** → ref bookkeeping **KC-17 · KC-18 · KC-32** → **KC-3 · KC-4 · KC-5 · KC-6 · KC-7 · KC-11 · KC-12 · KC-14 · KC-15 · KC-16 · KC-19 · KC-22 · KC-23 · KC-24 · KC-25 · KC-26 · KC-31 · KC-33 · KC-36** → **KF-KC-48** (the acceptance test; gates KF-KC-1). KC-13 (≡ KF-APP-4, the type gate) is KF.W4's. kf-KeyframeCard's NO-WAVE-OWNER rows enter by the card seam and are booked at `.a`'s receipt by id (⟨cmd⟩ `grep -c 'CARD-UNIT' kf-KeyframeCard.md` → **0** — it names no token).

### U2 · OPTIONS-UNIT (`.b`) — `kf-ChannelOptions.md:25` (the mint) · `:45-99` · `:156` · `:198-203` · `kf-LayerConfigPanel.md:35` · LP rows · `kf-TimingFunctionPanel.md:45-47` · `kf-EasingSidebar.md:50` (KF-ES-12) · kf-ControlsPaneWrapper (D-B1/N-2/C-2; `proof:stage-visible`) · `kf-SpringPhysicsFacet.md:38-39` (SPF-1/-2), `:53` (SPF-13's decision)

**Order (the record's, fixed — a travelling lock)**: (i) **KF-CO-1** (`:is-open`→`:open` ×4 — also makes D-1 §4's mutex real) + **KF-CO-8 ≡ LP-3** (`:model-value` + `@update:model-value` on the switch) **+ LP-1 in the SAME commit** (the render edge, whole prop surface; kin recorded, not folded: kf-ChannelControls L-16, KF-CO-15) → (ii) **KF-CO-3 · -4 · -11 · -12 · -13 · -14 · -16 · -17** → (iii) **KF-CO-9 ≡ KF-TFP-6 · KF-CO-10 · KF-CO-40 ≡ KF-TFP-5** → (iv) **KF-CO-5 ≡ KF-TFP-7 + KF-CO-46** + focus management → (v) **KF-CO-2 · -6 · L·B-2 · -21..-29 · -31 · -33..-36 · -38 · -39 · L·N-16 · -41 · -42 · -44 · -45 · LP-6 · LP-9 · LP-11..LP-18 · LP-21..LP-23 · the LP-20 tail** (incl. the RR-A inert-declaration census folded into KF-CO-33). **Beside the order**: **KF-TFP-1 (BLOCKER) ≡ KF-ES-12 → `useEasingPickerSeat`** under the KF-ES-4/KF-CO-17 split-seam cure-lock — the composable is SPECIFIED, not lifted (neither donor is clean); KF-ES-14's motion-preview story is *designed*, not patched; **kf-ControlsPaneWrapper D-B1/N-2/C-2** — the BG-11 occlusion's discharge lever ships in installed glass-ui 7.0.0, unconsumed, while the file's own `:26` comment asserts it does not exist (cure form per MISS-1: `:style` on `<DrawerContent>`; D-M12's detent budget recomputes in the same edit; producer side DISCHARGED); **KF-CO-15**'s options half (ribbon half KF.W13); **the KF-CO-47 decision** (SPF-1/SPF-2 spent here; SPF-13 spent by KF.W11). KF-CO-7 ≡ KF-APP-4 → KF.W4 (booked once). **Producer half → SS-6** (OP-5).

### U3 · KFED-UNIT (`.c`) — `kf-KeyframesEditor.md:36` (the mint) · `:41-114` · `:172` (the verdict) · kf-KeyframesAddDialog · `kf-SpringPhysicsFacet.md:51,:59,:60,:66`

**OP-6's verification first** · **KF-KE-4** (the apply identity — class = selector, or pass `getTmpAnimationName()` as the class; **proven by execution, both strings pasted**; then re-derive L-M3/C-B2's residue against a working feature) · **KF-KE-6** (the lift, refcounted; the three-closures seam — `KeyframesEditor` ×2 slot copies + `KeyframesStringControls` ×1 force-mounted; the mount-policy row is banked at kf-ControlsPaneWrapper M-4 + MISS-3 — folded, NOT re-booked) · **KF-KE-12 ≡ N-5** · **KF-KE-3** · **KF-KE-5** (BLOCKER; + the `:38-47` comment re-written) · **KF-KE-7** (+KF-CB-11 shell; gate + shell together) · **KF-KE-8** (standalone animations take `respectReducedMotion: true`; the delete group `g.respectReducedMotion = true`; the sweep's rest state lands with the flag — KF-KE-21) · **KF-KE-10 · 11 · 13 · 17 · 18 · 19 · 20 · 21 · 22 · 24 (pipeline half) · 25 · 26 · 27 · 28 · 29 · 33 · 34 · 35 · 37 · 38 · 41 · 44 · 46 · 47 · 48 · 49 · 50 · 51 · 52 · 54 · 55 · 56 · 57 · 58 · 60 · 61 · 62 · 63 · 64 · 65 · 66** · SPF-11/-19/-20/-23. **Dedupes**: KF-KE-2 → U1; KF-KE-14 ≡ KF-APP-4 → KF.W4; EE-03's length-watch (frontier-cured) → re-regression guard only. **Sequenced after KF.W0** and after OP-2.

### U4 · EDITOR-UNIT (`.d`) — `kf-CSSCodeEditor.md:30` (the mint) · `:33-87` · `kf-RibbonBar.md:48` (M-3/C-8 format half)

**KF-CE-3 FIRST** · **the KF-CE-1/-3/-4 fork** — decided once, in the receipt, before any byte (§B.3(2)) · **KF-CE-5** · **KF-CE-2's child half** (the `replaceContent()` seam; the additive `debounce.cancel`, §B.3(1)) · **KF-CE-8 + the re-projection contract** (kf-KeyframesStringControls `:47`) · **KF-CE-9 + KF-CE-37 ≡ RB M-3/C-8 — one boundary** · **KF-CE-6 · 7 · 10 · 11 · 13 · 14 · 15 · 18 · 19 · 20 · 21 · 24 · 25 · 26 · 27 · 28 · 29 · 31 · 33 · 34 · 35 · 36 · 38 · 39 · 40 · 41 · 43 · 44 · 45 · 47 · 48 · 49**. KF-CE-16 → KF.W4. Single-row routings from kf-EasingSidebar / kf-EasingScene into EDITOR-UNIT are booked at `.d`'s receipt by id or excluded with reason. **KF-CE-41's law** — the comment is part of the defect surface — binds every row.

### U5 · APPLY-UNIT (`.e`) — `kf-KeyframesStringControls.md:17` (the mint) · `:41` · `:44` · `:55` (N-8) · `:57-67` · `kf-RibbonBar.md:27,:76` (RB-6) · `:135`

**N-8, first** — ONE `cssIdent`-derived name shared by `getClassName` and the emitted selector (OP-4 governs the source; **the identity cure is `.c`'s and is inherited landed — `.e` routes it through one derivation**; L-BL-1's *"the injected sheet binds nothing"* is the reason the identity comes first) · **RB-6** (one lifetime for the state and its affordance; compounds N-5, cured at `.c`) · **D-2/L-M-8 · N-7 · D-25 (+KF-KE-8's PRM motion, one commit)** · **S-6-as-corrected** kept · the `:57`/`:59`/`:65`/`:67` rows as routed. **The killed scenario is named as killed, never cured.**

### U6 · AXISLINE-UNIT (`.f`) — `kf-CubeAxisLines.md:34` (the mint) · `:38` (KF-AX-1) · the roster `KF-AX-1 · 2 · 4 · 5 · 6 · 7 · 8 · 9 · 10 · 13 · 14 · 16 · 17 · 18 · 19 · 20 · 21 · 22 · 23 · 28 · 29 · 30` (⟨cmd⟩ `grep '^| KF-AX' kf-CubeAxisLines.md | grep -c AXISLINE` → **22**)

**KF-AX-1 first** — the consumer half on the ONE registry (the upstream half is KF.W11 `.a`'s sha, OP-1; `allowInInput`, keyup events, modal are the registry's own cures) · **KF-AX-2** (semantics; the deuteranopia labels rider rides J-lane C6's ask) · **#57 / the Z stroke** (the orientation redundancy holds for X-vs-Y and fails for exactly Z — it punctures challenge-D §5.3's kill of the 1.4.1 colour-only claim) · **KF-AX-9** (the opacity-grouping cure; its `180ms` token rider LANDED-BY KF.W6) · the rest by id, **every geometric assertion frame-stamped against `rotate3d(-1,1,0,30deg)` (KF-AX-4)**. Discrete rows to KF.W0 / KF.W6 (the dark arm, the ladder, the `--z-behind` policy, the z-contract recut, the namespace audit) / KF.W9-SS-13 (nine witnesses, **the Z triptych first — it decides three rows**) stay theirs.

---

## §Agent Units

Each unit: Goal · Mechanism · Files · Sub-gate. Receipts to `execution/B/KF-W12.md`, `SERVED MODEL: <id>` line 1, commands double-run, SELF-COUNT, inherited paths named (CRASH-RECOVERY). Out-of-bounds = ESCALATION, returned.

### KF.W12.a — CARD-UNIT (phase 1)
- **Goal**: a keyframe card's offset reads the selector the engine holds, writes it back through the selector grammar, and can be removed from the keyboard.
- **Mechanism**: U1 in KC-34's order; the frozen-selector write replaced by whole-selector replacement via `selectorPercent`/`percentSelector` with the unit fixed in the same motion; keep-mounted; the a11y triad after KC-1; KF-KC-48's RED run pasted before its GREEN.
- **Files**: the `.a` rows; `test/demo/instrument/keyframe-card-offset-loop.test.ts` (create).
- **Sub-gate**: G-KFW12-1 GREEN; the receipt records the shas `.c` opens on.

### KF.W12.b — OPTIONS-UNIT (phase 1)
- **Goal**: the options card's controls are live in both directions, re-render on engine writes, and the timing panel survives its own first drag.
- **Mechanism**: U2 in the fixed order — commit 1 = KF-CO-1 (×4) + KF-CO-8 + LP-1 (one commit, the lock; the producer's prop name quoted from the installed `.d.ts`); then (ii)–(v), each step its own commit in sequence; `useEasingPickerSeat` SPECIFIED in the receipt then created, consumed by both seats; the D-B1 inset via `:style`; the KF-CO-47 decision written down.
- **Files**: the `.b` rows; `test/demo/instrument/channel-options-render-edge.test.ts` (create).
- **Sub-gate**: G-KFW12-2 GREEN (all limbs); the producer row appended to the SS-6 register at `.g` with this unit's evidence; the close sha KF.W13's KF-CO-15 carve opens on is in the receipt.

### KF.W12.c — KFED-UNIT (phase 2; after `.a` and after KF.W11 `.b`)
- **Goal**: Apply-CSS applies something, the retiming control retimes, the start field validates through the keyframe grammar, the action cluster is reachable, and delete is a real control.
- **Mechanism**: U3 — OP-6's verification receipted first; KF-KE-4's identity proven by execution; KF-KE-6's lift; KF-KE-12's `clear()`; KF-KE-3; KF-KE-5's z rung + comment; KF-KE-7's gate + shell; KF-KE-8's PRM; the pipeline half; the tail.
- **Files**: the `.c` rows; `test/demo/instrument/keyframes-editor-honest.test.ts` (create).
- **Sub-gate**: G-KFW12-3 GREEN (all clauses); the shas `.e` opens on recorded.

### KF.W12.d — EDITOR-UNIT (phase 2)
- **Goal**: the code editor can be left by keyboard, tokenizes CSS, its emit seam cancels, and its format path fails loudly and releases its latch.
- **Mechanism**: U4 — KF-CE-3 first; the fork in the receipt with `vendor-monaco` re-measured; the `debounce.cancel` handle with its census, landed alone; KF-CE-8 with the re-projection contract; KF-CE-9 + KF-CE-37 as one boundary.
- **Files**: the `.d` rows; `test/demo/instrument/css-code-editor-seam.test.ts` (create).
- **Sub-gate**: G-KFW12-4 GREEN (all limbs).

### KF.W12.e — APPLY-UNIT (phase 3; after `.c` and `.d`)
- **Goal**: the class added is the selector emitted, under one `cssIdent`-derived name and one lifetime.
- **Mechanism**: U5 — OP-4 re-measured at open and the export's shape read; N-8's route landed on `.c`'s identity; RB-6; D-2/L-M-8; N-7; D-25's PRM motion; the killed scenario named.
- **Files**: the `.e` rows; `test/demo/instrument/apply-css-identity.test.ts` (create).
- **Sub-gate**: G-KFW12-5 GREEN (all limbs); the receipt prints OP-4's grep and says which arm (derivation routed, or identity-only with KF.W5's edge named).

### KF.W12.f — AXISLINE-UNIT (phase 3; after KF.W11 `.a`)
- **Goal**: the axis-lock reveal reveals exactly what the drag constrains, on the one registry, with every assertion frame-stamped.
- **Mechanism**: U6 — KF-AX-1's consumer half on KF.W11 `.a`'s sha; KF-AX-2's decision; #57's Z stroke; the roster; the frame re-derived read-only.
- **Files**: `CubeAxisLines.vue`; `test/demo/scenes/cube-axis-reveal.test.ts` (create).
- **Sub-gate**: G-KFW12-6 GREEN; OP-1's grep printed at 0 in the receipt.

### KF.W12.g — Close (phase 4; serial, last)
- **Goal**: record whole, commits audited, producer rows sailed, LEDGER row moved.
- **Mechanism**: all seven gates re-run double; `git show --stat` per sha against §Bounds; the SS-6 accretion entries (the seven producer rows + OP-5's); E13 four-path sweep; LEDGER cells + event line; residuals/escalations; zero SWAP-discharge receipts and zero re-bookings stated positively.
- **Files**: the value.js rows.
- **Sub-gate**: G-KFW12-7 GREEN.

### Standing law for every unit

As KF-W11's — CRASH-RECOVERY first · the spec GOVERNS, no masking fallback (no `try/catch` around a defect, no `test.skip`, no allowlist, **no copied producer selector — a guessed prop name is one**, no `node_modules` patch) · pathspec commits, families unsplit · E-3 · WRITE-THEN-MEASURE double-run · SELF-COUNT · quote-by-command · sibling trees and glass-ui READ-ONLY · E13 · probe parsimony · `dev.sh` never staged · LEDGER minimal edits only.

---

## §Gates — seven, all born RED at `69095552`, literal commands, double-run

**SELF-COUNT (LAW D(3))**: ⟨cmd⟩ `grep -c '^\*\*G-KFW12-' KF-W12.md` → **7**. All commands from `/Users/mkbabb/Programming/keyframes.js`. Runtime gates are `vitest` runs; byte clauses ride beside them, never alone.

**G-KFW12-1 — CARD: the offset loop round-trips.** ⟨cmd⟩ `npx vitest run --project demo test/demo/instrument/keyframe-card-offset-loop.test.ts` → RED today: `No test files found` (⟨cmd⟩ `ls test/demo/instrument/keyframe-card-offset-loop.test.ts` → `No such file or directory`, twice). GREEN when (KF-KC-48, the UNIT's acceptance test): a `from`/`50%`/`to` card renders `selectorText` of its frame (no `[object Object]`); a retiming write lands as a whole-selector replacement in the `0..100` domain with fractional step (no `TypeError` on a frozen result); delete is reachable from the keyboard. Byte clause: ⟨cmd⟩ `grep -c 'frame.start.value = starts' demo/components/instrument/keyframes/KeyframesEditor.vue` → **1** · **1** (must read **0**). **Order clause (KC-34)**: the highlight-path commit precedes the keep-mounted commit in `git log`; a green obtained in the other order is not this gate's green.

**G-KFW12-2 — OPTIONS: live in both directions, on a render edge, in the fixed order.** ⟨cmd⟩ `npx vitest run --project demo test/demo/instrument/channel-options-render-edge.test.ts` → RED today: `No test files found`. GREEN when an engine-side `setLayerConfig` re-renders the weight slider and the switch, the direction dropdown opens on click, and a first drag off a preset-matched quad does NOT remount `EasingPicker` (KF-TFP-1's cycle broken by the seat composable). Byte clauses: ⟨cmd⟩ `grep -rc ':is-open' demo/components/instrument/transport/channel-controls | grep -v ':0'` → `ChannelOptions.vue:3` · `LayerConfigPanel.vue:1` (must read nothing — 4 → 0, and the producer's declared prop name appears instead, **quoted from the installed `.d.ts` in the receipt**); ⟨cmd⟩ `grep -c '@update:checked' …/LayerConfigPanel.vue` → **1** · **1** (must read **0**). **Lock clause**: the commit that turns either byte clause green also contains the render-edge hunk (`git show <sha> --stat` names the files) — a byte clause green in a commit without the edge is a FAILED gate. **Order clause**: the five steps are visible as five commits in sequence; a persistence cure landed first fails this gate even if every row is green.

**G-KFW12-3 — KFED: Apply-CSS applies something; honest surface.** ⟨cmd⟩ `npx vitest run --project demo test/demo/instrument/keyframes-editor-honest.test.ts` → RED today: `No test files found`. GREEN when an **executed** assertion — not a reading — shows the class added to the target and the selector in the injected stylesheet are **the same string** (KF-KE-4, both strings pasted in the receipt); the start field rejects `500%`/`-20%` at the field and accepts `from`/`to` (KF-KE-3); the action cluster is hit-testable above the code plate (KF-KE-5); delete is a named focusable with a skippable gate (KF-KE-7); unmounting one of two owners leaves the sheet for the other (KF-KE-6). Byte clauses: ⟨cmd⟩ `grep -c 'parseCssScalar' …/KeyframesEditor.vue` → **2** · **2** (must read **0**); ⟨cmd⟩ `grep -c 'class="absolute top-2 right-4' …/components/KeyframeCard.vue` → **1** · **1** (must read **0** — the cluster carries a z rung, or the plate loses `relative`); the `:38-47` comment re-written to describe what is there (KF-CE-41's law); ⟨cmd⟩ `grep -c 'toLowerCase()' …/composables/useKeyframesState.ts` → **1** · **1** (must read **0** after `.c` — the identity cure).

**G-KFW12-4 — EDITOR: escapable first, tokenized, cancelling, loud on format failure.** ⟨cmd⟩ `npx vitest run --project demo test/demo/instrument/css-code-editor-seam.test.ts` → RED today: `No test files found`. GREEN when Tab leaves the editor (or `tabFocusMode` is on and announced) — **the first commit of the unit**; a rejected `formatCSSContent` surfaces an error and leaves `isFormatting` false; a selection change cancels the pending debounced emit (no stale emit on the wrong keyframe); the CSS tokenizer produces ≥ 2 token classes for `a { color: red }`; and the KF-CE-1/-4 arm is written down with `vendor-monaco` re-measured. Byte clauses: ⟨cmd⟩ `grep -c 'tabFocusMode' …/CSSCodeEditor.vue` → **0** · **0** (must read ≥ **1**); ⟨cmd⟩ `grep -c 'isFormatting' …/KeyframesStringControls.vue` → **4** · **4** (recorded; the acceptance is the runtime clause); every docblock the seam carries is true at the bytes.

**G-KFW12-5 — APPLY: one name, one lifetime.** ⟨cmd⟩ `npx vitest run --project demo test/demo/instrument/apply-css-identity.test.ts` → RED today: `No test files found`. GREEN when, for an animation whose id contains uppercase, `getClassName()` strictly equals the selector name inside `getCSSString()` **via one `cssIdent`-derived route** (or, under OP-4's 0-at-open arm, via the identity with KF.W5's edge named), the target animates under the injected sheet, the applied state and its affordance share one lifetime (RB-6), and `clear()` is wired. Byte clause: ⟨cmd⟩ `grep -rc 'cssIdent' demo | grep -v ':0'` → `helpers.ts:1` · `helpers.ts:1` (a comment; must read ≥ **1** non-comment consumer under the routed arm). Clock clause: ⟨cmd⟩ `grep -c 'cssIdent' dist/keyframes.d.ts` → **2** · **2**, printed at open and close. **Anti-work clause**: the killed "both animate the same target" scenario appears nowhere as this unit's work.

**G-KFW12-6 — AXISLINE: the reveal is true, frame-stamped, and on the one registry.** ⟨cmd⟩ `npx vitest run --project demo test/demo/scenes/cube-axis-reveal.test.ts` → RED today: `No test files found`. GREEN when the reveal lights only the axis the drag constrains, the `.z` stroke is legible under `rotate3d(-1,1,0,30deg)` (`rotateY(90deg)` out-of-plane is the defect, not the style), every geometric assertion carries its frame stamp (KF-AX-4), and a registry-dispatched lock (not a window keydown) drives it. Inbound clause: ⟨cmd⟩ `grep -c 'useEventListener(window, "keydown"' demo/scenes/cube/orbital-drag/OrbitalDrag.vue` → **1** · **1** today (must read **0** by KF.W11 `.a` — this wave does not write it). Recorded: ⟨cmd⟩ `grep -n 'rotateY\|rotateZ\|1000vw' …/CubeAxisLines.vue` → `:63` · `:113` · `:117`.

**G-KFW12-7 — close: green demo project, unmoved head, zero re-bookings, zero demo-side producer cures.** ⟨cmd⟩ `npm run test:demo 2>&1 | tail -3` → RED by construction (six absent files). GREEN when every `test/demo/**` file passes; ⟨cmd⟩ `git diff <open-sha>..HEAD -- test | grep -c 'test.skip\|it.skip\|\.only('` → **0**; ⟨cmd⟩ `npx vue-tsc --noEmit -p tsconfig.json 2>&1 | grep -c 'error TS'` → **0** (OP-0 re-run at close); ⟨cmd⟩ `git log <open-sha>..HEAD --format=%B | grep -c 'KF-APP-41\|C-22'` → **0** (the ANTI-RE-BOOK receipt in gate form); ⟨cmd⟩ `grep -c 'DISCHARGED by KF.W7 SWAP verdict' execution/B/KF-W12.md` → **0**; each of the seven producer rows appears as an SS-6 relay row with its date and **zero** has a demo-side workaround in the diff (a scoped-style override, a re-implemented primitive, a copied producer selector, or a `node_modules` patch — each a HIGH defect); every id in §Carry reads LANDED / KILLED-with-rationale / carried.

**GREEN-BEFORE-CURE (R.2)** as KF-W11's: a gate green at open without this wave's cure is booked `LANDED-BY <sha>`, never claimed (KF-AX-9's token rider is the first such booking).

---

## §Sequencing — locks and cross-edges

### Intra-wave order (binding)

1. **`.a` ∥ `.b` → `.c` ∥ `.d` → `.e` ∥ `.f` → `.g`.** Peak 2.
2. **KC-34 before KC-8/KC-9** inside `.a` (OP-3).
3. **LP-1 in the same commit as KF-CO-1/KF-CO-8** inside `.b` (the write→render lock); **the five-step order is the commit order**.
4. **`.c` after `.a`** (shared card seam and `useHighlightCSS.ts`) **and after KF.W11 `.b`** (the `useKeyframeOps.ts` hunk) **and after KF.W0** (OP-6's guard).
5. **`.e` after `.c` and `.d`** (shared composables, `KeyframesStringControls.vue`, `RibbonBar.vue`); N-8 before every other `.e` row; the identity cure is `.c`'s, the derivation route `.e`'s.
6. **`.f` after KF.W11 `.a`** (OP-1).
7. **KF-CE-3 first inside `.d`** (the bank's ruling on `5fe2e4cb`).
8. **The decision precedes the patch**: `.b`'s `useEasingPickerSeat` specification and KF-CO-47 decision, `.c`'s apply identity, `.d`'s Monaco arm — each written in the receipt before its commit family opens.
9. **`.b` closes before KF.W13's KF-CO-15 carve opens** on `ChannelOptions.vue` (a reactive `:max`/one-duration-read is not live until LP-1's edge exists).

### The travelling locks (register row 2 — three named there; all six accounted)

- **KF-AV-28's rider** — measured to govern no written row here (no W7-evaluated surface in the writable set); named; the verdict is settled (KEEP ×6) and the discharge alphabet has zero members.
- **Glass-producer rows → SS-6** — OP-5's `default: undefined` ask (KF-CO-1/KF-CO-8's producer half); the seven rows at §Scope 7; SPF-4-class Chip filters if met; relayed at `.g`; a demo-side workaround is a HIGH defect (G-KFW12-7).
- **The KFED-UNIT second-end note** — this file is the second end; `→ KF.W7` below.
- **LP-1's write→render ordering** — enforced at order 3 and at G-KFW12-2's lock clause.
- **Comment-stated invariants are test obligations** — binds every docblock `.a`..`.f` touches (`KeyframeCard.vue:38-47`; `ControlsPaneWrapper.vue:26`'s false claim about the inset lever; `useApplyCSS.ts`'s "Unapply: restore previous pause state"; every editor docblock under KF-CE-41).
- **MbabbMenu MUST-CARRY · TD-1/TD-2 bundling** — KF.W13's; not this wave's surfaces; named for the count.

### Cross-edges, declared from this end

| to | type | declaration |
|---|---|---|
| **KF.W4** | **DEPENDS ON (hard) — the sequencing head** | every UNIT record names the vue-tsc gate as its precondition; measured RED (54 · 54). |
| **KF.W6** | **DEPENDS ON (met) + ANTI-RE-BOOK honoured** | `EditorHeader.vue` deleted + C-22's orphan-token limb adopted there (R4-1); no row here re-books KF-APP-41 or that limb; KF-KE-24's TOKEN decision is W6's rider (the pipeline half is ours); KF-AX-9's `180ms` rider is W6's landed byte; `kf-focus-ring` is the demo's focus-ring name in every file this wave touches. |
| **KF.W7** | **RECIPROCAL END (the KFED-UNIT second-end note); INBOUND otherwise** | W7 cited the §6.D row *until the successor spec exists*; this file exists; KF-CE-2's parent half is W7's landed work; W7's OP-1 (`kf-ChannelControls` L-2/C-2 `:key`) is NOT touched here; the KAD folds (`R-7 ≡ KAD-F3`, KAD-6) stay W7's. |
| **KF.W8** | **DEPENDS ON (met)** | the card cluster's location (`34b051eb`); the serializer-unification body `.a`'s `selectorText` consumes. |
| **KF.W11** | **DEPENDS ON (two commits) + SHARES ONE PATH SERIALLY + SUPPLIES ONE DECISION** | `.a`'s OD latch family (OP-1) → this `.f`; `.b`'s L-2 hunk (OP-2) → this `.c`; **CubeAxisLines #31/#57 are booked there and cured here**; **SPF-13's decision (KF-CO-47) is taken at `.b` and spent there**; the mount-time `rotate3d(-1,1,0,30deg)` frame is read from KF.W11's files and edited by neither. Declared at both ends. |
| **KF.W13** | **SUPPLIES (ordering) + DECLARES (census half)** | KF-CO-15's options half here; W13's C-2 + KF-CO-15 joint commit opens after `.b` closes; `KeyframeCard.vue`'s `kf-focus-ring` at `:117` is the `.focus-ring` census's other half — RULINGS-4 is not amended (E-3). |
| **KF.W5 (library)** | **ASK, DECLARED (not owned)** | `cssIdent`'s publication (OP-4 — present at the artifact, unconsumed); any engine-side change the render edge needs. |
| **KF.W9 / SS-13** | **DEPENDS ON (witnesses)** | SS-13 #1 (KF-KE-5's plate), #2 (Apply's press signature), #4 (the KF-CE-8 loss), #5 (unmount ordering); KF-CO-9's staircase; LP-9's magnitude; the Z triptych first; the twelve CARD, thirteen TFP and nine sidebar witnesses by apotheosis row id. |
| **KF.W10** | **REGISTERED AT** | `§6.D` row 2 — unchanged by this file. |
| **SS-6** | **SUPPLIES** | the producer rows at `.g`. |

---

## §Excluded

| excluded | reason |
|---|---|
| **KF-APP-41** (`EditorHeader.vue` delete) and **kf-EditorShell C-22's orphan-token limb** | `ADOPTED-BY-KF.W6` (RULINGS-4 R4-1) — register row 2's ANTI-RE-BOOK; verified the file is not in the tree; `C-22`'s `D-27` · `RR-2 M7` limbs stay NO-WAVE-OWNER and are NOT adopted here (this note discharges nothing of theirs). |
| **KF-APP-4 ≡ KF-CE-16 ≡ KF-CO-7 ≡ KC-13 ≡ KF-KE-14** (the type gate) | KF.W4's, booked once at its row 1. |
| **KF-KE-24's TOKEN decision** | KF.W6's rider; only the pipeline half is here. |
| **KF-CE-2's PARENT half** (emit-time target resolution; a `:key`d remount or arm-time id capture) | KF.W7 — landed; C-9 is the same defect's milder trigger, folded, not double-booked. |
| **kf-ChannelControls L-2/C-2 (`:key`)** | KF.W7's OP-1 (a settlement, not a cure here). |
| **Any `src/**` byte** — `cssIdent`, `selectorText`, `requireKeyframeSelector`, `invalidateEntries` | KF.W5 / KF.W8; consumed by import. |
| **`orbital-drag/**`** | KF.W11 `.a`; AXISLINE consumes by commit (the KF-AX-1 dedupe). |
| **`EditorShell.vue` · `EditorStartScreen.vue` · `DemoGlobalChrome.vue` · `SharePopover.vue` · `KeyboardShortcutsModal.vue`** | their records route no row to any UNIT by token (⟨cmd⟩ the UNIT-token census above lists neither kf-EditorShell nor kf-EditorStartScreen); the Opus arm's narrow rows are not carried. |
| **`demo/scenes/**` except `CubeAxisLines.vue` and the two `EasingSidebar.vue` seat call-sites** | KF.W11; no easing packet exists in the canonical seventeen. |
| **`demo/app/**` · `demo/components/playback/**` · `TransportDock*` · `playback-idiom.css`** | KF.W13 (and KF.W11's drag-seam composable under `transport/composables/`). |
| **The one-prop cure for KF-TFP-1** | DEAD — killed three times (register #3); a seat that re-proposes it has not read the register. |
| **The "cheap highlighting-only import" for KF-CE-1** | DEAD — killed register #3; two honest arms only (§B.3(2)). |
| **The "both animate the same target simultaneously" scenario** | KILLED at the bank (L-BL-1); only the residue is cured. |
| **The eleven packets not in this partition** | R-15's partition; no re-booking. |
| **The KF-AV-28 discharge alphabet** | zero members; nothing here may write it. |
| **Amending RULINGS-4's `.focus-ring` census** | E-3 — §B.1 row 6 is a frontier reading beside a fixed figure. |
| **Owner rulings** | none required by these six; `complete_with_misses` if one surfaces (e.g. the EDITOR fork's bundle-budget arm). |
| **Re-auditing any of the records** | the registry's — ADJUDICATED; ids for life; folds by reference. |

---

## §Commit plan

One commit per meaning; families unsplit; pathspec on the commit itself; `Claude-Session:` trailer. Expected (keyframes.js):

1. `.a`: `fix(kf/card · X.KF.W12.a · KC-34 highlight stops replacing Vue DOM)` (**BEFORE** the keep-mounted commit) · `fix(kf/card · X.KF.W12.a · KC-1+KC-27 selectorText + offset names)` · `fix(kf/card · X.KF.W12.a · KC-2≡KF-KE-2 whole-selector write, freeze and unit in one motion)` (**MUST NOT SPLIT**) · `fix(kf/card · X.KF.W12.a · KC-8/KC-9 keep-mounted)` · `fix(kf/card · X.KF.W12.a · KC-10 removal + shell)` · `chore(… tail)` · `test(kf/card · X.KF.W12.a · KF-KC-48 — G-KFW12-1, RED run pasted)`.
2. `.b`: `fix(kf/options · X.KF.W12.b · STEP 1 — KF-CO-1 ×4 + KF-CO-8 + LP-1, capability restorations ON the render edge)` (**one commit; must not be reordered**) · `fix(… STEP 2 persist/representation/authority)` · `fix(… STEP 3 steps cluster)` · `fix(… STEP 4 a11y inert + focus)` · `chore(… STEP 5 hygiene + LP-20)` · `feat(kf/options · X.KF.W12.b · useEasingPickerSeat under the split-seam cure-lock — KF-TFP-1 ≡ KF-ES-12, one composable, two seats)` · `fix(… D-B1 inset via :style)` · `test(… G-KFW12-2)`.
3. `.c`: `docs(x-kf-w12/c): OP-6 frontier verification` (value.js) · `fix(kf/kfed · X.KF.W12.c · KF-KE-4 the apply identity — class = selector, executed)` · `fix(kf/kfed · X.KF.W12.c · KF-KE-6 lifted refcounted state + KF-KE-12 clear wired)` · `fix(… KF-KE-5 cluster above the plate + the :38-47 comment)` · `fix(… KF-KE-3 requireKeyframeSelector)` · `fix(… KF-KE-7 gate+shell)` · `fix(… KF-KE-8 PRM)` · `refactor(… pipeline half)` · `chore(… tail)` · `test(… G-KFW12-3)`.
4. `.d`: `fix(kf/editor · X.KF.W12.d · KF-CE-3 keyboard trap — tabFocusMode at create)` (**FIRST**) · `feat(kf/editor · X.KF.W12.d · KF-CE-1/4 <arm>, chosen and written down, vendor-monaco re-measured)` · `fix(kf/editor · X.KF.W12.d · debounce cancel handle — census pasted, lands alone)` · `fix(… KF-CE-2 child half + KF-CE-8 re-projection)` · `fix(… KF-CE-9+KF-CE-37 one format boundary)` · `chore(… tail)` · `test(… G-KFW12-4)`.
5. `.e`: `fix(kf/apply · X.KF.W12.e · N-8 one cssIdent-derived name <routed | identity-only, KF.W5 edge named>)` · `fix(… RB-6 one lifetime)` · `fix(… D-25 PRM motion)` · `test(… G-KFW12-5)`.
6. `.f`: `fix(kf/axis · X.KF.W12.f · KF-AX-1 consumer half on the registry)` · `fix(… KF-AX-2 semantics)` · `fix(… #57 the Z stroke, frame-stamped)` · `chore(… roster)` · `test(… G-KFW12-6)`.
7. value.js: `docs(x-kf-w12/<unit>): receipt` per unit; `docs(x-execution/ledger): KF.W12 row`; SS-6 accretion + INBOX append at `.g`; `evidence/W12/**`.

## §Format and lint cadence · §Verification artefacts

`npm run check` + `npm run test:demo` after each unit's last commit and at close; `npx eslint demo/components/instrument demo/scenes/cube/CubeAxisLines.vue demo/utils/helpers.ts` at close; `git diff --check` per commit. Artefacts at close in `execution/B/KF-W12.md` and `evidence/W12/**`: the seven gate transcripts (double-run), the commit roster with `git show --stat`, the apply-identity execution receipt with both strings, the EDITOR fork decision with its measured bundle delta, the `useEasingPickerSeat` specification, the `.d.ts` prop-name quotation, OP-4's grep at open/close, OP-1's grep at `.f`'s open, the SS-6 entries' ids, the E13 line.

## §L-18 rider (reference, not ceremony)

Passing all seven gates makes X.KF.W12 **IMPLEMENTED**; it never makes it **ACCEPTED**. The implementation must first survive two challenging gestalt passes and a fresh-Fable agglomeration. Likely bases: (i) the write legs restored one commit before the render edge (G-KFW12-2's lock clause exists for this); (ii) a second name derivation beside `cssIdent` "for now" (N-8 forbids it by name); (iii) the EDITOR fork taken by bytes rather than by receipt — a tokenizer registered without the trap cured, or contributions restored without the budget stated; (iv) `value4-editor-boundary.test.ts`'s timeout widened to make G-KFW12-7 green (the masking class; KF.W7 residual 15's own law); (v) an apply-identity "fix" proven by reading rather than by execution — the exact failure that let four audits miss KF-KE-4; (vi) a re-booked `KF-APP-41` or `C-22` limb; (vii) a cure written against the banks' pre-KF.W8 paths; (viii) the OPTIONS order silently re-sequenced so a persistence cure landed first; (ix) a producer defect cured demo-side.

**Dissents recorded, not resolved by this seat**: (1) whether OPTIONS-UNIT's 40-odd rows warrant two units (capability+authority ∥ hygiene) — this file keeps the record's *one* surface and its fixed order; (2) whether AXISLINE-UNIT belongs in KF.W11 beside its cube kin — R-15 partitioned it here and this file follows the partition, declaring the seam; (3) whether KC-2 ≡ KF-KE-2 is better homed at KFED (the Opus arm's shape, with the freeze-and-unit commit there) — this file homes it at the card seam per kf-KeyframeCardList's own scope words and pays with the `KeyframesEditor.vue` serial share.

---

## §Provenance — the agglomeration record (M-23 §3 / M-25)

**Fold seat**: a fresh Fable instance that authored neither draft; both drafts read whole; every disagreement resolved at the register's bytes and the live tree at `69095552`.

**Structure taken from the Fable arm**: the header and verb table, §State, the OP table's shape, the seven units in Fable's phase order (CARD ∥ OPTIONS → KFED ∥ EDITOR → APPLY ∥ AXISLINE → close), the vitest-based gate form with byte clauses, the serial-share resolution of shared files, the six-lock accounting, the cross-edge and §Excluded tables, §Commit plan, §L-18. **Taken from the Opus arm**: the §B.1 frontier re-anchor table (rows 1–8 are its findings, re-measured), the LAW A census block, the `:is-open` ×4 correction, KF-TFP-1 ≡ KF-ES-12's `useEasingPickerSeat` with the `EasingSidebar.vue` seat carve and the new composable path, kf-ControlsPaneWrapper's D-B1/N-2/C-2 inset row, the `debounce` cancel handle (`helpers.ts`) with KF-CE-2's child half, the conditional theme JSONs, KF-CE-3-first, the `.d.ts` prop-name quotation clause, KF-KC-48 as the acceptance test, the `:38-47` comment limb, the seven named producer rows, the anti-re-book gate clause, the anti-work clause, the `evidence/W12/**` path, OP-6's frontier-verification-first.

**Disagreements, and the byte that settled each**: (a) **KF-KE-4 / KF-KE-6 / KF-KE-12's home** — the Fable arm moved them to APPLY-UNIT by identity with L-BL-1/RB-6; `kf-KeyframesEditor.md:43`/`:45`/`:54` dispose all three `→ KFED-UNIT` verbatim, so they stay at KFED and APPLY takes N-8's derivation route after (the Opus arm's split; `useKeyframesState.ts` serial `.c` → `.e`). (b) **KC-2 ≡ KF-KE-2's home** — CARD (Fable) vs KFED (Opus): kf-KeyframeCardList's scope names the card seam by words; homed at CARD with the dissent recorded. (c) **`:is-open` ×3 vs ×4** — the tree reads 4; the fourth is `LayerConfigPanel.vue:11`, already OPTIONS-UNIT's. (d) **`cssIdent`** — present at the artifact (Fable, `dist` 2) AND absent as a demo consumer (Opus): both true, both stated at OP-4. (e) **KF-AX-9's `180ms` gate clause** (Opus) — the tree has only comments at `:41`/`:81`; booked LANDED-BY KF.W6 under GREEN-BEFORE-CURE, not gated. (f) **EditorShell/EditorStartScreen/DemoGlobalChrome bounds** (Opus) — no UNIT token in their records; excluded. (g) **unit order** — Fable's phase order kept; the Opus arm's "APPLY last by the banks' sequencing" is satisfied by it. (h) **write authority (Opus OP-4)** — not carried; settled by the landed X·KF roster.

**Killed from the Opus arm**: the G-KFW12-8/-9 separate gates (folded into G-KFW12-7's clauses — one close gate); the three narrow shell/chrome bounds rows; the KFED-holds-then-CARD-seam order (inverted to CARD → KFED by the seam's home). **Killed from the Fable arm**: the APPLY-UNIT homing of KF-KE-4/-6 (against the record's disposition); the AUDITED cell's "13 records" arithmetic as a *home* count (re-cut: 13 name a token; the eleven homes plus the token-routing records are enumerated at Provenance). **Blind convergences banked**: both arms produced the same six UNIT names, the same KC-34 / LP-1 / fixed-order locks, the same `keyframes-style-` two-line witness, the same `KeyframeCard.vue:117` drift, the same KF-CE-2 parent-half exclusion, the same peak concurrency, and the same KF-APP-41 / C-22 anti-re-book posture.
