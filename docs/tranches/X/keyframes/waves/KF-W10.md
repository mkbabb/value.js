# X.KF.W10 — Fold Discharge and Close

**Sub-tranche**: X·KF. Spec home `docs/tranches/X/keyframes/waves/KF-W10.md`; authority `docs/tranches/X/COHESION.md` §0/§1, ratified under `docs/tranches/V/megatranche/SCOPE.md` **M-25**. Validated at authoring against `docs/precepts/instructions/tranche/WAVE_SPEC.md` (**L-20**).

**OWNERSHIP GAP — DECLARED, NOT PAPERED.** COHESION §1 assigns KF.W0/W2/W4/W5/W8 to **SS-1** and KF.W1/W6/W7/W9 to **SS-2**. **KF.W10 is assigned to neither** (and KF.W3 likewise). This file is authored from the **SS-1 (library-half) seat** under M-25's route-by-mechanism clause because the wave's mechanism is ledger discharge, not design. That is an authoring act of necessity, **not an inherited routing**: §6.C-E declares the gap upward as a blocking defect, and §1 of COHESION requires an amendment naming an owner for KF.W3 and KF.W10 before X-whole may stamp SPECIFIED. SS-1/SS-2 may re-home this wave; it may not be dropped.

**Alias law (R-B idiom).** The sibling spelling `KF.W10` denotes `X.KF.W10` — a search alias, never a rename. `X.KF.W10.<x>` is the WAVE_SPEC §5 form and is authoritative.

**EXECUTION IS NOT AUTHORIZED BY THIS FILE.** Tranche X's execution gate stands: **nothing here opens, writes, builds, or runs against product source** in either repo until the owner's begin-word. Specifying a discharge is not performing one. Additionally — as in KF.W4 — **write authority for keyframes.js is UNRESOLVED** (§2a OP-1), and nine of this wave's thirteen bounds files live in that tree. This file is authored **read-only**.

---

## 1. State

**Name**: W10 — Fold Discharge and Close
**Opens after**: **X.KF.W0** (Substrate Settle — §B-12, the manifest schism, the Codex-v8 pin disposition), **X.KF.W1** (Mail Cure — the O-8/O-11 amendment-addendum on an exec-visible path), **X.KF.W9/SS-13** (the OD-V3 capture packet and the OD-V5 390 at-rest observation), and **X.KF.W2..W9 IMPLEMENTED** (the set-difference sweep consumes their final bounds). The dependency is on the **artefacts**, not the labels — §2a checks each.
**Agents**: 6 Opus units, **serial** (`.a` → `.f`; each maps to exactly one gate), plus **`.g`, a fresh Fable adjudicator** (M-23 §1 — a sub-tranche close is adjudication; this mirrors X.P.W4`.d` exactly, so the two sub-tranche close disciplines cannot drift). Serial by construction, so the owner's **max-4 concurrent workflows** cap (2026-07-24) is respected without a scheduling clause.
**Hard gate**: **seven** conditions in §5 — the §B 15-row discharge · the NO-WAVE-OWNER set-difference · the three-ruling owner block · the never-cite register · the four doc-truth addenda · coordination terminal · `v/w9-staging` land-or-kill. **All seven born-RED with a witness measured read-only 2026-08-28 and named below.**
**Status**: **planned**

### Four-verb status (separate fields; this file moves exactly one)

| verb | value | evidence |
|---|---|---|
| AUDITED | **YES** | `registry/adjudicated/kf-*.md` — **58** records, SS-10b CLOSED at `35fc8ebf` · `CENSUS-2026-08-03` §1/§4 · `audit/codex-provenance/INTAKE-ADJUDICATION-2026-08-03.md` §3 · keyframes.js `origin/master:docs/tranches/V/FOLD-FORWARD.md` §A/§B |
| SPECIFIED | **YES — by this file, 2026-08-28** | the CARRY table consumed whole; §7 states every non-carry with its reason |
| IMPLEMENTED | **NO** | — gates green + bytes landed in the named execution site stamps this |
| VERIFIED | **NO** | — **stamped here, and only here** (R-A) |

**R-A — the stamping site, ratified for X·KF.** Gates green + bytes landed stamps a wave **IMPLEMENTED**; that is all any sibling X·KF wave's own close may claim. **VERIFIED is stamped only at this wave's close**, against G-1, G-2 and G-7. X.KF.W10 advances its own four-verb row and every sibling X·KF wave's from IMPLEMENTED to VERIFIED **in one act at close** — a row edit in each file's four-verb table, nothing more. No X·KF wave stamps VERIFIED at its own close. ACCEPTED is later still (§10 rider).

---

## 2. Goal criterion

This wave succeeds if a reader who has never met this program can open **one document** and read, for every inherited obligation, exactly one terminal word with its evidence coordinate: each of FOLD-FORWARD §B's fifteen rows, each NO-WAVE-OWNER row across the 58 kf records, each of the three owner-gated rulings, each unciteable coordinate, and each of the two coordination ledgers.

It fails if any obligation closes **by omission** — which is the failure the ledger was minted to prevent, in the owner's own words at the Glass-7 adopt: *"ensure that ALL heretofore features that were NOT audited and developed … those exhortations and marks, are folded forward."*

**This wave consumes; it does not home and it does not cure.** Homing NO-WAVE-OWNER rows is SS-1/SS-2's authoring act. Rebuilding a dissolved oracle is KF.W4's. Editing a `.vue` file is KF.W6's. A close wave that repairs a mechanism is not a close wave, and its assertion of finality becomes circular.

---

## 2a. Open preconditions (checked at wave-open; NOT close gates)

| # | precondition | state at authoring (measured read-only 2026-08-28) | check at open |
|---|---|---|---|
| **OP-1** | **Write authority for keyframes.js named.** Nine of thirteen bounds paths are in that tree; four are dated-evidence addenda and two are ledgers. | **UNRESOLVED** — census §(c) gap 5, carried from KF.W4 OP-1 verbatim: keyframes.js is sibling-read-only under value.js law and the sacred-checkout rule. | The owner's dated word naming the execution site and the hand. **No inference from this file.** A wave that opens without it writes where it has no authority. |
| **OP-2** | **KF.W0 §B-12 has acted.** Every disposition in this wave re-baselines against `origin/master`. | `HEAD` = `8281638c`; `origin/master` = `81a56990`; `git rev-list --left-right --count HEAD...origin/master` = **1 ahead / 41 behind**; `git status --porcelain \| wc -l` = **252** dirty paths. The sacred checkout is stale, ahead, and dirty. | KF.W0's pasted re-anchor table. **W10 ASSERTS §B-12 happened; it never performs it** — lane-docs row 16: *"Blocked from our side by lane law and by their sacred-checkout rule — value.js must NOT perform it."* |
| **OP-3** | **KF.W1's O-8/O-11 amendment-addendum is exec-visible.** | **UNLANDED.** The census records both live obligation packets sitting untracked in the stale checkout, invisible to any executor, unanswered 10–16 days. | The addendum landed on an exec-visible path. Terminalizing a ledger whose packets were never delivered records a **false close** (G-6 depends on this, not the reverse). |
| **OP-4** | **KF.W9/SS-13's capture packet exists** — both transport homes (in-panel card, floating pill) on all four duplicating scenes at **390 AND 1280**, real Glass 7, both options mocked or annotated; plus the 390 at-rest observation recorded either way. | **UNRUN.** SS-13 is QUEUED (COHESION §1). | Packet present. **PACKET-FIRST is a lock, not a preference**: the OD-V3 ask may not be assembled without it. |
| **OP-5** | **KF.W2..W9 bounds are FINAL.** G-2 is a set-difference over sibling wave bounds blocks. | Re-measured this seat: **six** sibling spec files exist (`KF-W0.md`, `KF-W2.md`, `KF-W3.md`, `KF-W4.md`, `KF-W7.md`, `KF-W8.md`); `KF-W1`/`KF-W5`/`KF-W6`/`KF-W9` are absent — and `KF-W2.md` names **KF.W11** (cube) and **KF.W13** (dock-menu) as packet homes, so the sibling roster provably extends past COHESION §1's W0..W9 set. | Every sibling spec present with a `## File bounds` block — **including any wave a sibling spec names as a packet home (KF.W11, KF.W13)**. An unfinal denominator makes G-2 vacuous, and a vacuous gate is **deleted, not counted** (L-2). |
| **OP-6** | **COHESION §1 names this wave's owner.** | **ABSENT** — KF.W10 and KF.W3 are assigned to neither SS-1 nor SS-2. | A §1 register amendment. This wave may be **authored** unowned (M-25 route-by-mechanism, this seat's act) but **may not assert G-2's set-difference** over waves it does not know are final and does not own. §6.C-E is the upward declaration. |

OP-1, OP-2 and OP-6 are hard.

---

## 2b. Carry — the agglomerated rows

Every CARRY row lands here as a gate, a bounds item, a named cure with its locks, or an §7 EXCLUDED line with its reason. **Deduped by identity**: KF-HA-4 and KF-AT-26 are booked **once** each at their banked ids and never re-keyed at a fold. **Severity is the bank's** and is never re-graded downward at a fold.

| row ⟨provenance⟩ | sev | witness (born-RED, measured or re-measured) | lands as |
|---|---|---|---|
| **FF-B-DISCHARGE** ⟨kf `origin/master:docs/tranches/V/FOLD-FORWARD.md` §B rows 1–15 · CENSUS:154-156 · lane-docs.md:141 · charter⟩ | **BLOCKER-to-close** | §B present at `81a56990` (header line 30, §C at line 48; 15 numbered rows). **0 of 15 carry a terminal verb in any X·KF artifact today.** kf's tranche V is CLOSED-BY-FOLD and §B is the entire kf-side inheritance | **G-1** · unit `.a` · the 15-row table of §3.1, pre-adjudicated dispositions carried, never re-derived |
| **NWO-TERMINAL-SWEEP** ⟨COHESION §3.3 + §4 · the per-record NO-WAVE-OWNER registers across the 58 kf records · CENSUS §1⟩ | **BLOCKER-to-close** | measured: `ls registry/adjudicated/kf-*.md` = **58**; **57** of 58 contain `NO-WAVE-OWNER`; **1,216** total occurrences. Census in its own words: *"kf-side asks have no owning wave."* Re-measured this seat: `KF-W2.md` homes the square packet's **parse arm only** and names KF.W7/**KF.W11**/**KF.W13** as the other packet homes — KF.W11/KF.W13 have **no spec file**, and the amiga packet + the orientation/a11y cluster remain unclaimed by any existing spec | **G-2** · unit `.b` · **W10 ASSERTS ∅ in both directions; it does not home** |
| **OD-V3** ⟨FOLD-FORWARD §B-5 · kf `origin/master:OWNER-DECISIONS.md:7` · lane-docs.md:154-157 · census §4⟩ | **OWNER-GATED (blocker-to-close)** | verbatim at origin/master today: *"**THE TRANSPORT-HOME RULING IS HELD FOR CAPTURE REVIEW.**"* Unruled since **2026-07-17** | **G-3** · unit `.c` · the owner block; **PACKET-FIRST** obligation and the `complete_with_misses` escape both inherited |
| **OD-V5** ⟨FOLD-FORWARD §B-5/§B-4 · kf `OWNER-DECISIONS.md:9` · lane-docs.md:154-157⟩ | **OWNER-GATED (blocker-to-close)** | verbatim at origin/master today: *"**THE AT-REST REOPEN QUESTION IS DEFERRED PENDING GLASS'S DOCK MARK.**"* Unruled since **2026-07-17**; the glass mark is outstanding | **G-3** · `.c` · gated on SS-6's batched BJ letter returning G-1..G-4 |
| **OG-KF1** ⟨INTAKE-ADJUDICATION-2026-08-03.md:232 (from B21-18) · CENSUS:336-338⟩ | **OWNER-GATED (blocker-to-close)** | *Does the Codex Keyframes B-lineage continue at all, and must any continuation re-anchor to `origin/master 81a56990` first?* The whole v8 contract lineage is pinned to `8281638c` — measured **41 behind, 252 dirty**. Census: *"value.js cannot answer it."* | **G-3** · `.c` · **GATES** the three Codex-provenance rows below; cross-read with OG-V1/OG-V2 in **ONE** owner sitting (§6.C-F) |
| **B18-23 + B19-14 + B20-14** ⟨INTAKE-ADJUDICATION:115 conflict C-7 + :163-166 · CENSUS:314,:326⟩ | MINOR (evidence integrity) | the Codex v8 seal **357/414 = 86.23%** is dead inside Codex's own corpus: its prior falsifier caps at **333/414**, its later record demotes to **345 exact / 12 partial / 57 unresolved @ `8281638c`**. Three ids assert one dead figure | **G-4** · `.c` · ONE disposition, three ids: *"**RESOLVED — superseded within Codex's own corpus.** Citable ONLY as `345/12/57 @ 8281638c`, never 357/414, never as HEAD coverage."* |
| **B18-12 · B18-13 · B18-14** ⟨INTAKE-ADJUDICATION:82 + :163-166 (CARRY → KF.W10) · CENSUS:326⟩ | INFO (terminal-unresolvable) | three of the corpus's 42 UNPROVEN claims are genuinely unlocatable external coordinates — no file on this machine reproduces them. The adjudication's own law: *"an external coordinate that nothing reproduces cannot close a row."* | **G-4** · `.c` · ONE terminal disposition **UNRESOLVABLE-BY-CONSTRUCTION**; re-booking **forbidden** |
| **B18-27** ⟨INTAKE-ADJUDICATION:116 conflict C-8 + :166 · CENSUS:326⟩ | INFO (evidence integrity) | Codex-internal slot bookkeeping disagrees with itself — B18-27 says **0/5**, the 08-03 delta says **0/4**; the fresh census is silent, so nothing external decides it | **G-4** · `.c` · *"**CONFLICT UNRESOLVED → CARRIED** (KF.W10): Codex-internal bookkeeping; enters no keyframes spec until 4-vs-5 settles or the ledger is dropped."* **Settle-or-drop is ONE act at close; a third number is not an option** |
| **NEVER-CITE-REGISTER** ⟨kf-ChromeDock DISSENT-4 + kf-EditorHeader killed-claim #8 — both filed with **NO wave**; homed here by mechanism (M-25)⟩ | MINOR (evidence integrity) | (a) ChromeDock's `challenge-C-consumption.md` header tally counts **eight defects and two superlatives that were never written** (its own Round-3 note concedes it); with S-2 and R3-1 killed on the merits *"the axis's arithmetic must be restated from scratch by any downstream consumer — never quoted."* (b) EditorHeader's C-axis §0 cite — **RE-ANCHORED BY THIS SEAT, see §3.3 lock** | **G-4** · `.c` · the register's fifth and sixth entries |
| **KF-AT-21** ⟨kf-AnimatedText, merged C-contra-G-lane / L:C-2⟩ | MINOR | verified at `81a56990`: `r-animation-sota.md` G26-3 asserts *"**The F26-4 demo grapheme-bug is DISCHARGED** … the old raw-UTF-16 per-char split is gone"* behind a zero-hit grep; **both halves false at the tree** (the per-char split returned under the owner's ruling; `split-text/` exists, 4 files). The ledger row *"demo-fix **DISCHARGED** (F.W16 word-split)"* inherits the falsity. **A formally-discharged defect is still shipping** | **G-5** addendum 1 · `.d` |
| **KF-EST-23** ⟨kf-EditorStartScreen L:C-1 / C:D-C10, both re-readers concordant⟩ | INFO | verified at `81a56990`: `lane-22-perf-demo-runtime.md:106-115` — *"the hero `<h1>` … **zero engine dependency**"* — false transitively (`<TypingDots />` sits inside the LCP `<h1>` and awaits the engine); the lane's `@utils/kfEngine.ts` path never existed. `U.D.md:193-195` repeats *"has ZERO engine dependency"* | **G-5** addendum 2 · `.d` · **CORRECTION-BOUNDARY LOCK** (§3.4) |
| **KF-HA-4** (doc-truth arm only) ⟨kf-HeroAurora L:D-5 / C:D-6 + both readers' convergent cure-correction; rider witness kf-EditorShell D-19⟩ | **MAJOR at the bank**; the W10 arm is the manifest limb | verified at `81a56990`: `home.json:9` names `proof:cursor-light-subtle` as the browser assertion behind the **owner-ruled 0.1 aurora ceiling**; that oracle was dissolved at `70b32501` (roster is exactly `proof:publish` + `proof:owner-golden`; 0 hits across `scripts/`, `test/`, `src/`). **RIDER RE-ANCHORED**: the `proof:hero-two-focal` clause-(c) cites are **not at `:11`** — at `81a56990` they sit at **`:2`, `:13`, `:16`** (three sites, `:11` is the bare `"forbidden": [` opener). Neither `proof:hero-two-focal` nor `proof:appearance-suffusion` exists | **G-5** addendum 3 · `.d` · **SPLIT-LOCK: W10 owns the manifest lines ONLY** (§7) |
| **KF-AT-26(e)** ⟨kf-AnimatedText INFO cluster limb (e)⟩ | INFO | verified at `81a56990`: `docs/frontend-design/demo/home.md:67` cites `@keyframes liftDown` (8 doc hits incl. `:67`, `:187-191`, `:367`; **0** in `demo/` + `src/` — the shipped keyframe is `charLift`) and builds a KEEP-and-PROMOTE recommendation on the phantom | **G-5** addendum 4 · `.d` · **ANTI-REBOOK**: the cluster banks once; W10 owns **(e) only** |
| **W9-STAGING** ⟨FOLD-FORWARD §A row W9 · branch `v/w9-staging` · charter⟩ | MAJOR | measured this seat: `git ls-remote --heads origin \| grep w9` → **`b920b190  refs/heads/v/w9-staging`** — live, pushed, **unmerged**. MR1–4, the relabel, the TC-5 trio, the BV-2 golden and the prune set with tombstones are authored + staged and un-landed since the fold. **A staged branch is neither landed nor killed**; it rots against a moving master and every downstream spec is ambiguous about whether the prune set exists | **G-7** · `.e` · **LAND-OR-KILL** |
| **COORD-TERMINAL** ⟨CENSUS §4 · FOLD-FORWARD §A W12 · lane-docs rows 12/14/16 · value `docs/tranches/V/coordination/INBOX.md` (E13)⟩ | MAJOR | FOLD-FORWARD §A W12 explicitly folds *"ledger terminalization at V-close (= successor opening)"* forward **unexecuted**. Two converged rows still need one closing line each — **D-GAP-6 / `sampleBezier`** resolves to *not adopted* (4.1 declines it permanently) and **O-8's vehicle question** was pre-empted by **CC-084**. G-1..G-4 glass marks outstanding. Value-side INBOX measured: 52,543 B, and the four `UNREAD` hits are all **law-preamble text** (`:4`, `:5`, `:17`, `:27`), i.e. **zero UNREAD status rows** — the RED is the missing terminal verbs, not an unread backlog | **G-6** · `.f` · both ledgers terminalized **honestly** |

---

## 3. Scope

Numbered by owning unit; ordering inside a unit is binding. **No unit opens product source in either repo.**

### 3.1 `.a` — the FOLD-FORWARD §B discharge table (G-1)

ONE table, 15 rows, **exactly one terminal verb each**, from the closed vocabulary `LANDED <commit>` / `KILLED <tombstone path>` / `RULED <owner quote>` / `FOLDED-TO <banked id>` / `STANDING-CARRIED <charter text>`. Appended to kf `docs/tranches/V/FOLD-FORWARD.md` as a **dated addendum**; §A/§B source text is byte-unchanged. The pre-adjudicated dispositions are **carried, not re-derived**:

| §B | row | disposition carried | evidence the verb requires |
|---|---|---|---|
| 1 | aurora reverse-drag black slab (V-A95) | glass-owned (born-RED BJ wave **GF-AURORA**); rides **SS-6** with kf-HeroAurora's relay packet (KF-HA-1/-2/-9/-12/-13); surface verify **KF.W9** | SS-6 letter row + KF.W9 surface receipt |
| 2 | Chip/Badge orphaned dist CSS | **FOLDED-TO** COHESION §4a **SC-1** — glass-ui 7.0.0 ships `glass-chip.css` and imports it nowhere; all Chip adoption BLOCKED-ON the producer cure | the SC-1 register row |
| 3 | Glass §4 dock contract re-verify (subsumes CH2-02 ×4: BG-5, GU-1, GU-2, subject-legible) | **KF.W6 + KF.W9** | both waves' bounds blocks |
| 4 | G-1..G-4 batch marks | **SS-6** ledger; OD-V5 keys off the same boundary | SS-6's dated sent-row |
| 5 | OD-V3 / OD-V5 | **RULED** — this wave's owner block; *"Never proxied."* | G-3's verbatim quotes, or two `complete_with_misses` closures |
| 6 | IN-ATLAS-5 TimingFunction fence (3 kf chase sites) | **STANDING-CARRIED** — verified held through every V restructure (lane-docs row 14). **No action.** | the lane-docs row, quoted |
| 7 | nested `<TooltipProvider>` at `AnimationControlsGroup.vue:2` | **FOLDED-TO KF-APP-21** — verified banked this seat at `kf-App.md:71`, routed **KF.W6** (*one provider policy; config unification, NOT deletion — the frontier keeps App's root provider deliberately per DP-02/CC-03*). §B-7's open question is **ANSWERED by the bank** | the banked id + its KF.W6 row |
| 8 | the W6×transaction collision LESSON | **FOLDED-TO KF.W4** gate-authoring law, verbatim: *"a src-scoped dead-sweep must also grep the LANDING consumer tree when a consume transaction is in flight"* — joining B10-17/18/19/20/22/23 + B10-27 + B18-19 + B21-17 | KF.W4's rule list containing the sentence |
| 9 | MR2 runner-parity watch (system Chrome vs pinned revision, Linux runner) | verified **AT** the W9-staging landing, not before | the landing's CI witness |
| 10 | CH-02 watch row | **STANDING-CARRIED verbatim** — *"becomes DISEASE if the successor re-words rather than decides."* A **decide-not-reword lock**: a re-worded CH-02 is a defect by its own text | the verbatim carry |
| 11 | VM-4 | close-audit doc-drift check against the 6.0.0 tarball byte authority **184,430 B**. Measured this seat: `package.json#version` = **6.0.0**; CHANGELOG head is **`## 6.0.0` at line 6**, no Unreleased section — no version cut since | the byte check + the two reads |
| 12 | owner-checkout reconciliation | **FOLDED-TO KF.W0** (census SCH-1/F-1). **W10 ASSERTS it happened; never performs it** | KF.W0's re-anchor table |
| 13 | standing invariants | **STANDING-CARRIED verbatim** into the successor charter: frozen `.`+`./engine` 44-key mirror · `TimingFunction` at `constants/types.ts:45` · `scenes/` exemplar fence · `docs/precepts/` read-only · exact-pin consume-edges, bilaterally ratified · no-legacy/no-masking · zero-deferral close · **batches-of-3 agent law** · Fable-deepest/Opus-fanout routing | the charter text, **plus the §9.1 collision flag** |
| 14 | OD-V2's owner-ordered TOTAL demo audit | **DISCHARGED BY CONSTRUCTION with a receipt** — the megatranche adjudication program **is** that audit: keyframes **58/58** units banked at `35fc8ebf`, SS-10b CLOSED (COHESION §0a) | the receipt, cited |
| 15 | external watches | value **SCI-1** (their 4.x into-variant) rides the 4.1 coordination event **CC-084** via **PLAW-BIND**; *"no kf action"* preserved | the watch's terminal verb only (§6.C-G) |

### 3.2 `.b` — the NO-WAVE-OWNER set-difference (G-2)

ONE table proving **∅ in BOTH directions** between (a) the union of NO-WAVE-OWNER rows across the 58 kf records **plus COHESION §4**, and (b) the union of X·KF wave bounds. Every row reads **LANDED in a wave** / **KILLED with rationale** / **explicitly carried to the next formation boundary's ledger**. Re-booking forbidden: folds are **by reference at the banked id**; original ids for life.

**Sequencing locks that MUST survive the sweep intact, carried verbatim from the records:**

- **KF-CO-1 / KF-CO-8** cures are **SEQUENCED by LP-1** (no write→render edge).
- **kf-ChromeDock M-4's** worded cure **ships an unopenable menu** without the **MbabbMenu MUST-CARRY rider**.
- **TD-1 / TD-2's** cures **bundle**.
- **KF-APP-1's** `ppMode` repair must dispose the `setPPMode` twin **in the same motion** as the **KF-APP-17** `headerLeft` sweep; the `headerLeft` **'fill' arm is a TRAP** — it lands inside an inert toolbar, so **until the KF-APP-5 producer relay lands, DELETE is the only self-contained cure**.
- **Comment-stated invariants are test obligations** — the kf-SquareScene MISS-3 law: a docblock asserting *"paced by the spring's own settle, not a fixed timer"* sitting **46 lines above** `setTimeout(step, 520)`.
- **Glass-producer rows go to SS-6, never demo-side hacks.**

### 3.3 `.c` — the owner block and the never-cite register (G-3, G-4)

1. **Owner block**: three rulings — OD-V3, OD-V5, OG-KF1 — each quoted **verbatim** with its date and the owner's own words, or closed `complete_with_misses` citing the **exact** pending precondition (OD-V3's packet; OD-V5's glass mark). **No inference, no default, no proxy.**
2. **Never-cite register**, seven entries, each naming the coordinate and attaching its supersession or true source: the three unreproducible external coordinates (B18-12/13/14), the 357/414 block, B18-27's slot count, ChromeDock's `challenge-C-consumption.md` header tally, and EditorHeader's C-axis §0 citation.

**RE-ANCHOR CORRECTION — load-bearing, this seat's measurement, and it RESTATES the sixth entry rather than transcribing it.** kf-EditorHeader killed-claim #8 records the cited `coordination/GLASS-INBOUND-2026-07-16-headerribbon-consumer-updates.md` as *"does not exist (my `ls`/`find`)"*. Measured at keyframes.js `origin/master` today: **that path EXISTS** — `git ls-tree -r origin/master` returns it, and it is a distinct glass-authored letter (*"HeaderRibbon consumer updates"*, 2026-07-16) which itself cites the value.js record. The seat's `ls` was **value.js-scoped**; the coordinate is dead in value.js (archived at `docs/tranches/V/archive/GLASS-INBOUND-2026-07-16-headerribbon-persistent-only.md`, verified present) and **live in keyframes.js**. The register therefore books: *"**REPO-SCOPED, NOT ABSENT** — dead at value.js, live at keyframes.js `origin/master`; the two letters are different documents. The paraphrase-presented-as-quotation finding is re-scoped to the kf-local letter and re-verified there before it may be asserted."* The **substance** (EH-1) is untouched and survives on its own witness — installed 7.0.0 still ships the `anchor` slot, the collapsible machinery and `--header-ribbon-actions-width`. This is exactly the **KF-AT-28 re-anchor law** firing: re-resolve every cite at the landing substrate before writing.

### 3.4 `.d` — the four doc-truth addenda (G-5)

Four addenda **beside — never inside** — the dated originals. **E-3 addenda-not-patch**: the originals stay byte-unchanged, and `git diff` on all four is empty at close. Anchors re-resolved at the landing substrate before writing.

1. **KF-AT-21** → beside `r-animation-sota.md:107` and `:253`: the addendum states the tree fact and **re-opens ledger row `:253`**. The G-lane verdict text is **not patched**. The tree fact is fixed by **KF-AT-3/KF-AT-4 in KF.W6/KF.W4** — write the addendum against the state at close, or state the cure's fate **by reference**.
2. **KF-EST-23** → beside `lane-22-perf-demo-runtime.md:108-114` and `U.D.md:194`. **CORRECTION-BOUNDARY LOCK — carried, or the addendum over-corrects**: F3's transposition verdict **SURVIVES on corrected premises** (glyphs paint engine-free at frame 0; the dots' late start is designed), and the `main.ts` elision charge is **RETIRED** (reader-LC's kill, byte-verified). The addendum corrects **exactly two claims and reinstates neither**.
3. **KF-HA-4 doc arm** → beside `home.json:9` (`proof:cursor-light-subtle`) and the rider's **three** re-anchored sites `:2`, `:13`, `:16` (`proof:hero-two-focal` clause (c)). **The addendum states the oracle's fate BY REFERENCE to KF.W4 and never re-derives it** — otherwise it asserts an unguarded bound KF.W4 has already re-armed. Carried correction on the replacement's shape: `HERO_AURORA_OPACITY_CEILING` is a `<script setup>` **compile-local const with nothing to import**; the assertable surface is the rendered `--aurora-opacity-ceiling` on `.aurora-root`.
4. **KF-AT-26(e)** → beside `docs/frontend-design/demo/home.md:67`, `:187-191`, `:367`.

Addenda 1, 2 and 4 are **ONE motion** — same class, four documents (KF-EST-23's own framing: *"same class, second and third documents"*).

### 3.5 `.e` — `v/w9-staging` land-or-kill (G-7)

**LAND**, with the fold's own landing work carried verbatim: *"the LANDING: rebase over the slice-landed `deploy-pages.yml` (MR3 conflict — re-derive mechanically); MR4 is now GREEN-able (demo tests pass on the consumed tree — its CI red-once witness at landing); the four CI-run witnesses; TC-5 verification."* §B-9's MR2 runner-parity watch is verified **at** this landing.

**Or KILL**, with a tombstone naming **each** dying unit: MR1, MR2, MR3, MR4, the relabel, the TC-5 trio, the BV-2 golden, the prune set. **Silence is not a disposition.**

### 3.6 `.f` — coordination terminalization and the close ledger (G-6)

Terminalize **both** ledgers — value `INBOX.md` (E13) and kf `coordination/INBOUND-LEDGER.md` + `DISPOSITIONS.md` (both verified present at `origin/master`). Every inbound/outbound row carries a terminal verb and a dated row; zero UNREAD at close. The two converged rows get their one closing line, **HONESTLY**: D-GAP-6 / `sampleBezier` recorded as **"not adopted — 4.1 declines it permanently"**; O-8's vehicle question recorded as **pre-empted by CC-084**. Neither is dressed as an adoption. Already-terminal rows are **recorded, not re-opened**: I-6 / IN-VALUE-1..2 / W12 are TERMINAL both sides (lane-docs row 12); IN-ATLAS-3's TimingFunction fence is STANDING, verified held (row 14). Then write `docs/tranches/X/keyframes/close/FINAL-KF.md` and advance every X·KF wave's four-verb row IMPLEMENTED → VERIFIED in one act (R-A).

### 3.7 `.g` — fresh-Fable adjudication

One fresh Fable instance adjudicates the close against §5, assuming it is wrong. Its most probable finds, named so they are not the ones it makes: a §B row whose verb is a status word rather than a coordinate; a set-difference table built from a stale sibling bounds block; an owner row closed `complete_with_misses` without naming its exact precondition; an addendum that patched.

---

## 4. §Bounds — file bounds

| File | Access |
|---|---|
| `docs/tranches/X/keyframes/waves/KF-W10.md` | create (this file) |
| `docs/tranches/X/keyframes/close/FINAL-KF.md` | create — the close ledger: the §B 15-row discharge, the NO-WAVE-OWNER set-difference, the never-cite register, the owner block |
| `docs/tranches/X/COHESION.md` | modify-carve — **§1 register amendment naming this wave's owner · §3 criteria receipts · §4 NO-WAVE-OWNER register drain · §5 status board**. Dated sections never rewritten |
| `docs/tranches/V/coordination/INBOX.md` | modify — mail rows only (E13, value side) |
| kf `docs/tranches/V/FOLD-FORWARD.md` | **append-only** — the §B discharge table as a dated addendum; §A/§B text never rewritten |
| kf `docs/tranches/V/OWNER-DECISIONS.md` | **append-only, conditional** — OD-V3/OD-V5 rulings appended **when and only when the owner rules**. Never proxied |
| kf `docs/tranches/V/coordination/INBOUND-LEDGER.md` | modify — ledger terminalization (FOLD-FORWARD §A W12) |
| kf `docs/tranches/V/DISPOSITIONS.md` | modify — ledger terminalization |
| kf `docs/tranches/G/audit/r-animation-sota.md` | **append-only addendum** beside `:107` and `:253` (KF-AT-21) |
| kf `docs/tranches/U/audit/lane-22-perf-demo-runtime.md` | **append-only addendum** beside `:108-114` (KF-EST-23) |
| kf `docs/tranches/U/waves/U.D.md` | **append-only addendum** beside `:194` (KF-EST-23) |
| kf `docs/tranches/T/stage-manifests/home.json` | **append-only addendum** beside `:9` and the re-anchored `:2`/`:13`/`:16` (KF-HA-4 doc arm + the `proof:hero-two-focal` rider) — **a JSON file takes its addendum in a sidecar, never as an inline comment** |
| kf `docs/frontend-design/demo/home.md` | **append-only addendum** beside `:67` / `:187-191` / `:367` (KF-AT-26e) |
| kf branch `v/w9-staging` @ `b920b190` | **the only branch this wave touches** — land-or-kill |
| every X·KF sibling wave file | modify-carve — **one row** in the four-verb table (R-A), nothing more; no sibling prose is reflowed |

**EXPLICITLY NOT IN BOUNDS**: any product source in either repo (execution gate) · the keyframes owner-checkout reconciliation §B-12 (KF.W0 + the owner's hand; **lane law forbids value.js performing it**) · `scripts/dev/dev.sh` (unowned, NEVER-touch) · any glass-ui tree (SS-6 relays letters; a letter is not a commit in someone else's repo).

**Disjointness.** No two units share a write path. `.a` alone writes `FOLD-FORWARD.md`; `.b` alone writes COHESION §4; `.c` alone writes `OWNER-DECISIONS.md`; `.d` alone writes the four doc-truth paths; `.e` alone touches the branch; `.f` alone writes both coordination ledgers, `FINAL-KF.md`, and the sibling four-verb rows. Pathspec commits only.

---

## 5. §Gates — the hard gate

Seven conditions. **All seven born-RED with the failing probe measured read-only 2026-08-28** against keyframes.js `origin/master 81a56990` and value.js `tranche-u`.

### G-1 — FOLD-FORWARD §B discharge, 15/15 · **BORN-RED**

**Statement**: every one of §B's 15 rows carries **exactly one** terminal verb (`LANDED` / `KILLED` / `RULED` / `FOLDED-TO <banked id>` / `STANDING-CARRIED`) with a commit, a tombstone path, an owner quote, or a banked id.

**Witness**: `git show origin/master:docs/tranches/V/FOLD-FORWARD.md` §B rows 1–15, read against the wave's discharge table.

**RED today**: §B exists (header line 30, 15 rows); **0 of 15** carry a terminal verb in any X·KF artifact. The ledger exists and the discharge table does not.

**Acceptance**: 15/15 verbs, each with its evidence coordinate; §A/§B source text **byte-unchanged** (addendum-appended only).

**Falsifier**: a row whose "verb" is a status word (*"handled"*, *"noted"*, *"in flight"*) fails, naming the row number. A row carrying two verbs fails. A `FOLDED-TO` whose banked id does not resolve in the registry fails on the id, not the prose.

### G-2 — NO-WAVE-OWNER set-difference, ∅ both directions · **BORN-RED**

**Statement**: set-difference ∅ in both directions between the union of NO-WAVE-OWNER rows across the 58 kf registry records **+ COHESION §4**, and the union of X·KF wave bounds.

**Witness**: `grep -c 'NO-WAVE-OWNER' docs/tranches/V/megatranche/registry/adjudicated/kf-*.md` + the per-record routing summaries (kf-AmigaScene · kf-SquareScene's seven packets · kf-ChannelOptions OPTIONS-UNIT · kf-CubeTarget/kf-CubeScene cube packet · kf-App's ~30 rows · the kf-ChromeDock/kf-EditorShell/kf-EditorHeader a11y surface) **vs** `docs/tranches/X/keyframes/waves/*.md` bounds blocks.

**RED today**: measured — **58** kf records, **57** carrying `NO-WAVE-OWNER`, **1,216** occurrences. The census records the gap in its own words: *"kf-side asks have no owning wave."* Re-measured this seat: **six** sibling specs exist (W0/W2/W3/W4/W7/W8); `KF-W1`/`KF-W5`/`KF-W6`/`KF-W9` are absent, and `KF-W2.md` routes packets to **KF.W11** and **KF.W13**, which have no spec files at all — so the right-hand union is not yet computable. Partial homing has begun and is BOOKED, not double-counted: KF-W2 homes the **square** packet's parse arm (the cure stays with the packet's owning adjudication) and declares sequence/spring/CARD-UNIT → KF.W7, cube → KF.W11, dock-menu → KF.W13; the **amiga** packet and the orientation/a11y cluster remain unclaimed by any existing spec.

**Acceptance**: one table; every row LANDED / KILLED-with-rationale / carried to the next formation boundary's ledger; **zero re-bookings** (folds by reference at the banked id); every §3.2 sequencing lock reproduced intact.

**Falsifier**: a row homed by re-keying its id fails on the id. A table computed against a sibling bounds block that changed after the sweep fails on OP-5, not on arithmetic. A green ∅ produced because the right-hand union was empty is **vacuous and is deleted, not counted** (L-2).

### G-3 — Owner block: three rulings, never proxied · **BORN-RED by construction**

**Statement**: OD-V3, OD-V5 and OG-KF1 are each **RULED**, quoted verbatim in the close ledger with the ruling's date and the owner's own words.

**Witness**: `git show origin/master:docs/tranches/V/OWNER-DECISIONS.md` rows OD-V3 (`:7`) and OD-V5 (`:9`) — read this seat, both still HELD / DEFERRED — and `INTAKE-ADJUDICATION-2026-08-03.md:232` (OG-KF1 under *"Owner rulings owed"*).

**RED today**: **0 of 3 ruled.** OD-V3/OD-V5 unruled since **2026-07-17** (42 days at authoring); OG-KF1 since **2026-08-03**.

**Acceptance**: three verbatim rulings, **or** three `complete_with_misses` closures each citing its **exact** pending precondition (OD-V3's capture packet; OD-V5's glass dock mark; OG-KF1's owner sitting). No inference, no default, no proxy.

**Falsifier**: a ruling paraphrased rather than quoted fails. A `complete_with_misses` whose precondition is prose rather than a named artefact fails. A transport home implemented under any reading of a held ruling fails **G-3 and the wave**.

### G-4 — Never-cite register, seven entries · **BORN-RED**

**Statement**: no X·KF spec asserts a rejected Codex denominator or an unciteable corpus coordinate except in its superseded form with the supersession attached.

**Witness**: `grep -rnE '357/414|86\.23|0/5 slots' docs/tranches/X/ docs/tranches/V/megatranche/` — re-measured this seat: the hits are `CENSUS:314` + `INTAKE-ADJUDICATION:115` (quoted with their kill), `KF-W0.md:111`/`:274` and `KF-W3.md:124`/`:228` (each carries the `345/12/57 @ 8281638c` supersession beside the figure — compliant), this file, and the dated 2026-07-31 resurrection-handoff records (`CONSTELLATION-RESURRECTION-HANDOFF-2026-07-31.md` + its manifest JSON — sealed dated evidence, quoted-in-context, E-3-protected). **Zero bare citations in a live wave spec today**; the RED condition is a bare citation, and the register that would adjudicate one does not exist.

**RED today**: the register **does not exist**, so nothing distinguishes a compliant quotation from a live citation, and seven coordinates are unregistered — B18-12/13/14 (three unreproducible externals), the 357/414 block, B18-27's slot count, ChromeDock's phantom header tally, EditorHeader's C-axis §0 cite.

**Acceptance**: one register, **seven** entries, each with name + supersession/true-source; grep returns zero bare citations across X·KF; the sixth entry carries §3.3's **REPO-SCOPED, NOT ABSENT** restatement rather than the transcribed kill.

**Falsifier**: a spec that cites `357/414` without `345/12/57 @ 8281638c` beside it fails, naming the file and line. A third slot number for B18-27 fails on sight — settle-or-drop, never a new figure. Re-booking any of B18-12/13/14 as evidence fails: **UNRESOLVABLE-BY-CONSTRUCTION is terminal.**

### G-5 — Doc-truth addenda, four documents · **BORN-RED**

**Statement**: four addenda exist **beside — never inside** — the dated originals, and the originals are byte-unchanged.

**Witness**: `r-animation-sota.md:107` + `:253` · `lane-22-perf-demo-runtime.md:108-114` + `U.D.md:194` · `home.json:9` plus the re-anchored `:2`/`:13`/`:16` · `home.md:67`,`:187-191`,`:367` — **all verified present at keyframes.js `origin/master` by this seat**, and the `home.json` rider anchor **corrected from `:11`** in the process.

**RED today**: all four documents currently assert the falsified claim; **zero** addenda exist.

**Acceptance**: 4 addenda; `git diff` on the four originals is **empty** (E-3); anchors re-resolved at the landing substrate before writing; the KF-EST-23 addendum corrects exactly two claims and reinstates neither (correction-boundary lock); the KF-HA-4 addendum states KF.W4's oracle fate **by reference**.

**Falsifier**: one changed byte in an original fails the gate — the addendum's whole value is that the dated record stayed dated. An addendum that re-derives KF.W4's rebuilt oracle fails on the SPLIT-LOCK. An addendum written against a line number that no longer resolves fails the re-anchor law (this gate's own `:11` correction is the exemplar).

### G-6 — Coordination terminal, both ledgers · **BORN-RED**

**Statement**: both coordination ledgers are terminal — zero UNREAD, every owed letter with a dated sent-row, and the two converged rows each carrying one honest closing line.

**Witness**: value `docs/tranches/V/coordination/INBOX.md` (E13; measured 52,543 B — the four `UNREAD` hits at `:4`/`:5`/`:17`/`:27` are **law-preamble text, not status rows**) + kf `docs/tranches/V/coordination/INBOUND-LEDGER.md` and `docs/tranches/V/DISPOSITIONS.md`, **both confirmed present at `origin/master`**.

**RED today**: FOLD-FORWARD §A W12 folds *"ledger terminalization"* forward **unexecuted**; D-GAP-6 and O-8's vehicle question have no closing line; G-1..G-4 glass marks outstanding.

**Acceptance**: terminal verb + dated row on every entry; **D-GAP-6 = "not adopted (4.1 declines permanently)"**; **O-8 = pre-empted by CC-084** — neither dressed as an adoption.

**Falsifier**: a closing line that reads as an adoption where the record says decline fails on the word, not the row. Terminalization performed before OP-3's packet lands records a false close and fails **G-6 and OP-3 together**. A letter cited by `FINAL-KF.md` that `git cat-file -e` cannot resolve fails (L-6).

### G-7 — `v/w9-staging` land-or-kill · **BORN-RED**

**Statement**: branch `v/w9-staging` is **merged** with its four CI-run witnesses and TC-5 verification, **or killed** with a tombstone naming every dying unit.

**Witness**: `git ls-remote --heads origin | grep w9` → **`b920b190  refs/heads/v/w9-staging`** — live, unmerged, verified this seat.

**RED today**: the branch exists, is pushed, and is unlanded; MR3's `deploy-pages.yml` conflict is un-rebased. `HEAD` is **41 behind** `origin/master` with **252** dirty paths.

**Acceptance**: merge commit + four CI-run witnesses + TC-5 verification + MR4's CI red-once witness at landing + §B-9's MR2 runner-parity observation; **or** a tombstone covering MR1–4, the relabel, the TC-5 trio, the BV-2 golden and the prune set. **Gated on KF.W0 §B-12** — a branch cannot be rebased onto a checkout 41 commits behind origin/master.

**Falsifier**: a "landing" recorded from an `exit 0` with no run ID fails (L-2 — T.W9's ceremony-deploy defect, measured again). A partial kill — a tombstone missing one unit — fails naming the unit. Leaving the branch as-is is **not a third option**; the ambiguity it creates downstream is the defect.

---

## 6. §Sequencing — locks and cross-edges

### 6.A Intra-wave order (binding)

`.a` **→** `.b` **→** `.c` **→** `.d` **→** `.e` **→** `.f` **→** `.g`. Two orderings are load-bearing:

1. **`.b` runs LAST among the survey acts** in the sense that it consumes every other X·KF wave's **final** bounds — it may not open until OP-5 holds. Its position before `.c`/`.d` in the unit chain is a write-disjointness convenience, not permission to compute against a moving denominator.
2. **`.c` before `.d`**: the never-cite register fixes the citation rules the addenda must obey.

### 6.B Carried sequencing locks (must land intact where their rows land)

- **KF.W0 §B-12 manifest schism**: `origin/master` pins glass-ui 7.0.0 exact devDep = installed; local HEAD `8281638c` is stale. **Every wave re-baselines against `origin/master`.** Every disposition in §3.1 inherits this, and §B-12's own verb cannot be stamped before KF.W0 acts.
- **Glass rows (§B-1/-2/-3/-4) and OD-V5 route to SS-6 and NEVER become demo-side hacks** (standing owner edict).
- **§B-5 cannot take a terminal verb until the owner block is RULED** (or `complete_with_misses`).
- **§B-13's charter carry must explicitly reconcile the inherited "batches-of-3 agent law" against the value.js owner cap of max 4 concurrent workflows (2026-07-24).** Flag it; **do not drop it silently** (§9.1).
- **PACKET-FIRST (OD-V3)**: KF.W9/SS-13 produces the 390+1280 captures against real Glass 7 **before** the ask is assembled. Never proxied.
- **The §3.2 packet locks** (LP-1 write→render edge · MbabbMenu MUST-CARRY rider · TD-1/TD-2 bundling · KF-APP-1/-17 same-motion + the `headerLeft` 'fill' TRAP · comment-stated invariants are test obligations) survive the sweep verbatim.
- **W9-STAGING downstream-assumption lock (carried)**: until G-7's verb is stamped, **no X·KF wave may assume the staged prune set or the tombstones have landed.**
- **The discharge table and every addendum are appended as dated addenda. §A/§B and the four originals are never rewritten** (E-3).

### 6.C Cross-edges, declared from this end

**A. → KF.W0 (Substrate Settle, SS-1) — W10 depends.** Three dependencies. (1) §B-12 is KF.W0's act; W10 asserts and never performs (lane-docs row 16: *"value.js must NOT perform it"*). (2) X-1's disposition of the Codex v8 contract pin — re-anchor at `origin/master 81a56990` or declare **stale-by-substrate, "never citable for a live count"** — **binds the `@ 8281638c` scope clause** on the 357/414 row; W10 may not stamp that scope until KF.W0 settles it. (3) The manifest-schism re-baseline law governs every anchor in W10's four addenda. **RIDER W10 declares BACK to KF.W0**: kf-EditorHeader **EH-1** routes there — the archived Glass-7 letter's premise is contradicted by installed 7.0.0 (the `anchor` slot, the whole collapsible machinery, `--header-ribbon-actions-width` all still ship) and **XR-4 / IN-GLASS-1 / `DISPOSITIONS.md:21` must be re-read** against `archive/GLASS-INBOUND-2026-07-16-headerribbon-persistent-only.md` **and** the kf-local `coordination/GLASS-INBOUND-2026-07-16-headerribbon-consumer-updates.md` (§3.3's correction: both letters exist, in different repos) **and** `dist/header-ribbon.js`. That re-read is a doc-truth act and E-3 binds it.

**B. → KF.W1 (Mail Cure, SS-2) — W10 depends.** No coordination terminalization (G-6) before the O-8/O-11 amendment-addendum lands on an exec-visible path. Census finding: both live obligation packets sat untracked in the stale sacred checkout, invisible to any executor, unanswered 10–16 days.

**C. ↔ KF.W4 (K-Quartet Hygiene / gate law, SS-1) — bidirectional.** **W10 → W4**: §B-8's lesson lands there **verbatim** — *"a src-scoped dead-sweep must also grep the LANDING consumer tree when a consume transaction is in flight"* — joining B10-17/18/19/20/22/23 + B10-27 + B18-19 + B21-17. **W4 → W10**: KF-HA-4's oracle rebuild is KF.W4's (*"rebuild the bound as a mount-and-read-the-style vitest, or export the const — the un-writable import form must not enter a spec"*), and kf-EditorShell D-19's registry-truth decision on `proof:hero-two-focal` / `proof:appearance-suffusion` is KF.W4's; **W10's `home.json` addendum states both fates BY REFERENCE and never re-derives them.**

**D. → KF.W9 (Safari Visual Audit) / SS-13 — W10 depends.** OD-V3's ruling cannot be **asked** without the packet, and the packet is SS-13's capture set. OD-V5's 390 at-rest observation is recorded there *"either way"* — which is precisely what lets that row close `complete_with_misses` without proxying the owner.

**E. → COHESION §1 / §3.5 — W10 declares upward. BLOCKING DEFECT.** §1 assigns KF.W0/W2/W4/W5/W8 to SS-1 and KF.W1/W6/W7/W9 to SS-2; **KF.W10 is assigned to neither, and KF.W3 likewise.** §3.2 requires every census wave-sketch id to map to a full spec or a terminal kill with rationale; §3.5 requires each sub-tranche to close its own L-20 loop to a SPECIFIED stamp. **An unowned close wave cannot assert the §3.3 set-difference over waves it does not know are final** (G-2 / OP-6). §1's register needs an amendment naming an owner for KF.W3 and KF.W10 **before X·KF may stamp SPECIFIED**.

**F. → X·V owner block (OG-V1 / OG-V2) — W10 declares to X·V.** **OG-KF1 must be ruled in the SAME owner sitting as OG-V1** (*do the self-authored Codex denominators — 0/44, 0/1,892, 11/11, 15/15, 0/15, 0/12, **0/5**, 14/34, 0/5 slots — enter the substrate, or retire per red-matrices M1/M5/M7?*) and **OG-V2** (*do Codex-authored prohibitions bind the Claude-owned formation after M-15?*). **B18-27's slot count sits inside OG-V1's enumeration**; a ruling here that contradicts a ruling there manufactures a new conflict at close.

**G. → X·P → X·V → KF.W3 (PLAW-BIND release chain) — W10 RECORDS, never schedules.** §B-15's external watch on value **SCI-1** and the 4.1 coordination event **CC-084** (parser totality cure + SCI-1 + easing restoration; *"no emergency 4.0.1 — ruled"*) resolve through the gate-keyed PLAW-BIND routing: parse-that release condition → value (X·V L1/L5) → packed release → **KF.W3** repin 4.0.0→4.1.x. **W10 stamps the watch's terminal verb only; it does not schedule the cut. Direct parse-that→fourier remains FORBIDDEN.**

**H. → SS-6 (Glass communique / BH relay) — W10 depends.** Four §B rows and one owner row resolve **producer-side only**: §B-1 (V-A95 aurora reverse-drag black slab; born-RED BJ wave GF-AURORA), §B-2 ≡ COHESION §4a **SC-1** (glass-ui 7.0.0 ships `glass-chip.css` and imports it nowhere), §B-4 (G-1..G-4 batch marks), and **OD-V5** (gated on glass's dock mark). kf-HeroAurora's relay packet (**KF-HA-1** crayon/light incompatibility · **KF-HA-2** placeholder-not-hidden-when-armed · **KF-HA-9** `wake()` PRM seam · **KF-HA-12** a11y-sweep omission · **KF-HA-13** diagnostic→throw bridge) rides the **same ONE batched letter**. **STANDING EDICT carried: glass-producer rows go to the BH relay and NEVER become demo-side hacks.**

**I. → value.js megatranche parser-band / STATE — pass-through, DECLARED NOT-OWNED.** kf-HeroAurora **KF-HA-19** routes the R1-framing contradiction (three independent executions show `@mkbabb/value.js@4.0.0`'s `./css` subpath handles `oklch()` cleanly and returns **diagnostics-not-throws** on `var()`/`color-mix()`, contradicting the memory-of-record *"R1 = live `parseCssColor('oklch()')` shipping crash"*) as *"a value.js-side registry carry … relayed up, never a KF wave row."* **W10 declares the edge so the carry is not lost at close, and books nothing**: the reconciliation belongs to the value.js megatranche parser-band/STATE surface, with **kf-App KF-APP-56** and **kf-EditorShell C-19** folding to the same class (KF.W2/KF.W3 own the class in-repo).

---

## 7. §Excluded — everything not carried, with its reason

| excluded | reason |
|---|---|
| **Performing the §B-12 owner-checkout reconciliation** | KF.W0 + the owner's hand. Lane law and the sacred-checkout rule forbid value.js performing it (lane-docs row 16). **W10 asserts; it never acts.** |
| **KF-HA-4's oracle REBUILD limb** (mount-and-read vitest, or `export const`) | **KF.W4** (gate law). SPLIT-LOCK: W10 owns the manifest line only. Re-deriving it here would assert an unguarded bound KF.W4 has already re-armed. ONE identity **KF-HA-4**, never re-booked. |
| **KF-HA-4's in-file prose cites** | **KF.W6** file-touch. |
| **kf-EditorShell D-19's registry-truth decision** on `proof:hero-two-focal` / `proof:appearance-suffusion` | **KF.W4.** W10's addendum states the fate **by reference**. |
| **KF-AT-26 limbs (a), (b), (d)** | **KF.W6** — the cluster banks ONCE; W10 owns limb **(e)** only and never re-keys the id. |
| **KF-AT-26 limb (c)** | **KF.W8.** |
| **KF-APP-21's cure** (the nested `<TooltipProvider>` provider policy) | **KF.W6** — verified banked at `kf-App.md:71`: config unification, **not deletion**; the frontier keeps App's root provider deliberately (DP-02/CC-03). §B-7 **folds to it by reference**. |
| **§B-8's gate-authoring lesson, as an implemented rule** | **KF.W4.** W10 declares the edge (§6.C-C) and stamps §B-8's verb; it does not author gate rules. |
| **HOMING any NO-WAVE-OWNER row** (the amiga/cube/square/sequence/spring/dock-menu packets, OPTIONS-UNIT, EDITOR-UNIT, CARD-UNIT, the orientation/a11y cluster) | Homing is **SS-1/SS-2's authoring act**. W10 **asserts the set-difference**; a close wave that homes rows is certifying its own work. |
| **Every glass-producer cure** (§B-1, §B-2/SC-1, §B-3's producer half, §B-4, OD-V5's dock mark, the five KF-HA relay rows) | **SS-6's ONE batched BJ letter.** Standing owner edict: never demo-side hacks, never a piecemeal channel. |
| **Implementing the winning transport home** (OD-V3) | The ruling is HELD. Implementation follows the ruling, in the ruling's own wave. **Never proxied, never defaulted.** |
| **Scheduling the 4.1 cut** (SCI-1 / CC-084 / KF.W3 repin) | Gate-keyed PLAW-BIND, never scheduled. W10 stamps §B-15's verb and stops. |
| **KF-HA-19 / KF-APP-56 / kf-EditorShell C-19 — the R1-framing reconciliation** | value.js megatranche parser-band/STATE surface; KF.W2/KF.W3 own the class in-repo. **Declared (§6.C-I) so the carry is not lost; booked nowhere here.** |
| **Any product source byte, either repo** | Execution gate. A close wave that edits mechanism is not a close wave. |
| **`scripts/dev/dev.sh`** | Unowned, NEVER-touch; never staged. |
| **Re-deriving ChromeDock's `challenge-C-consumption.md` header arithmetic** | The register **names** it unquotable; restating the arithmetic is the downstream consumer's act, not the register's. |

---

## 8. Dispositions carried without re-derivation

| inherited row | verb here | act |
|---|---|---|
| **§B-14** OD-V2's owner-ordered TOTAL demo audit | **DISCHARGED BY CONSTRUCTION** | the receipt is the megatranche program itself: keyframes **58/58** banked at `35fc8ebf`, SS-10b CLOSED (COHESION §0a). Not re-run, not re-argued. |
| **§B-6** IN-ATLAS-5 TimingFunction fence | **STANDING-CARRIED** | verified held through every V restructure (lane-docs row 14). **No action** is the disposition. |
| **§B-10** CH-02 watch row | **STANDING-CARRIED verbatim** | *"becomes DISEASE if the successor re-words rather than decides."* A re-worded CH-02 is a defect **by its own text**. |
| **I-6 / IN-VALUE-1..2 / W12** | recorded, **not re-opened** | TERMINAL both sides (lane-docs row 12). |
| **B18-12/13/14** | **UNRESOLVABLE-BY-CONSTRUCTION** | close no row, enter no X·KF spec as evidence, never re-opened. Recorded once in the register, then closed. **Re-booking forbidden.** |

---

## 9. Dissents recorded, not resolved by this seat

1. **§B-13's "batches-of-3 agent law" vs the value.js owner cap of max 4 concurrent workflows (2026-07-24)** — an unreconciled collision between **two owner-issued laws**. The charter carry must state both and flag the collision; it may not silently adopt either. (This wave's own 6-serial-unit shape sidesteps it by construction and therefore does **not** constitute a precedent.)
2. **The ownership gap (OP-6 / §6.C-E)** — COHESION §1 assigns KF.W10 to neither SS-1 nor SS-2. G-2's assertion has **no authoring owner today**; this file is authored from the SS-1 seat by mechanism, and the amendment is owed.
3. **The never-cite register's homing** — kf-ChromeDock files its coordinate as **DISSENT-4** and kf-EditorHeader as **killed-claim #8**, both with **NO routing**. Homing them here is **this seat's act under M-25 route-by-mechanism** (an unciteable-coordinate disposition is exactly the 357/414 class), not an inherited routing. SS-1/SS-2 may re-home it; **it may not be dropped.**
4. **KF-HA-4's severity** is MAJOR at the bank; only its **manifest limb** lands here. Splitting a MAJOR across two waves is recorded as a risk: if KF.W4's rebuild slips, the addendum documents an unguarded owner-ruled bound with no gate behind it — honest, and worse than a cure.

---

## 10. Commit plan, artefacts, and the L-18 rider

| # | scope | body required |
|---|---|---|
| 1 | `docs(X·KF W10): FOLD-FORWARD §B 15-row discharge addendum` | yes — per-verb counts, evidence coordinates |
| 2 | `docs(X·KF W10): NO-WAVE-OWNER set-difference, ∅ both directions` | yes — both denominators, zero re-bookings asserted |
| 3 | `docs(X·KF W10): owner block + never-cite register (7 entries)` | yes — the three rulings or their exact preconditions |
| 4 | `docs(X·KF W10): four doc-truth addenda (E-3, originals byte-unchanged)` | yes — `git diff` empty on all four originals |
| 5 | `chore(X·KF W10): v/w9-staging LANDED @ <sha>` **or** `docs(X·KF W10): v/w9-staging tombstone (8 units)` | yes — run IDs + TC-5, or the 8 named units |
| 6 | `docs(X·KF W10 close): FINAL-KF + ledgers terminal + IMPLEMENTED→VERIFIED stamps` | yes — the per-wave verb stamps this close alone may set (R-A) |

Pathspec commits only; `scripts/dev/dev.sh` never staged. Artefacts: `close/FINAL-KF.md` (commits and pasted commands only; cites no document this close authored — L-6), the §B addendum, the set-difference table, the register, the four addenda, the landing run IDs or the tombstone, both terminal ledgers.

**L-18 rider.** Passing all seven gates makes X.KF.W10 **IMPLEMENTED** and — on G-1/G-2/G-7's receipts — lets this close stamp X·KF **VERIFIED** (R-A). It never makes it **ACCEPTED**. Per L-18 the implementation must first survive **two challenging gestalt passes**, each a quartet of Opus 5 agents assuming it is wrong, across all three altitudes — total-tranche, wave, feature — reporting defects **and** superlatives with provenance and proof, then adjudicated into an apotheosis by a **singular fresh Fable instance**; any incongruity is addressed and dispatched, never parked (L-18 §5). Reference, not ceremony: for a close wave the quartet's most probable finds are a §B verb that is a status word, a set-difference computed against a stale bounds block, an owner row closed by default, an addendum that patched, and a "landing" evidenced by an exit code. Each is already a named falsifier above; the quartet exists to find the ones that are not.
