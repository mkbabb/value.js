# KF.W7 — Timeline Evaluate

**Wave**: KF.W7 · Timeline Evaluate — the 666-line timeline cluster (KeyframeTimeline **312 L at origin/master** — the banked "313 L" is a ±1 counting-convention artifact, corrected so the disposition table inherits the right number · TimelineTrack 246 L · TimelineCaret 70 L · TimelineHoverPreview 38 L) + SequenceScrubber 162 L (S-3/S-4) + AnimationVisualizer 256 L, evaluated against the glass-ui `/timeline` + `ScrubberTimeline` family. **Evaluate, not mechanical swap** — this wave's product is a per-surface verdict table plus the cure program the verdict leaves standing.
**Half**: frontend (keyframes.js demo). **Sub-tranche**: X·KF.
**Status**: planned. No product byte is written by this file; authoring it opened no product source for writing; execution awaits the owner's begin-word.
**Ref of record (G12 / MISS-α5)**: keyframes.js `origin/master` `81a56990` or later. Local HEAD `8281638c` is DISQUALIFIED as witness substrate (banked kf-App ruling 1); every line anchor below re-resolves against origin/master before cure (D-19). KF.W0 §B-12: origin/master pins glass-ui 7.0.0 exact devDep = installed; **every wave re-baselines against origin/master**.
**Standing supersession rider (KF-AV-28), the wave's defining lock**: this wave's S-9 evaluation may supersede any behavioral cure — a per-surface SWAP verdict discharges the bespoke rows banked under that surface with the component. **Counter-evidence lock (`PR-CAUTION` — a SPEC-LOCAL LABEL, not a banked id; bound to `kf-PlaybackRibbon.md` §*Adjudicated defect roster* → its **Routing law** paragraph, the clause that rides the KF-AV-28 standing rider; disclosed at §Carry's preamble), weighed before any SWAP verdict.** **QUOTED VERBATIM at the registry bytes, this repair round (RULINGS-3 §END D8)** — ⟨the round-2 text was a PARAPHRASE carrying a "(verbatim)" label: subject substituted (*"this ribbon"* → *"kf-PlaybackRibbon"*), the conjunction *"so"* dropped for an em-dash, emphasis added. **Struck**; this is the same class R2-7 convicted at §Sequencing item 2, and the label was doing exactly the work that ruling forbade⟩: *"note the C axis's own §7 caution rides with it — this ribbon is the NON-bespoke case (it consumes the real `Slider`) and carries BLOCKERs anyway, so "swap onto the primitive" is never sufficient as a cure."* ⟨Subject resolution, stated OUTSIDE the quotation so no substitution enters it: *"this ribbon"* = `PlaybackRibbon.vue`, the record's own subject.⟩
**Provenance**: twice-authored (Fable arm + Opus arm), agglomerated by the fresh-Fable fold seat per M-23; adjudicated rows consumed by mechanism, deduped by identity, never transcribed (M-25); validated against `docs/precepts/` latest (L-20); no gate exists here without a named live witness (L-19).

| verb | state | basis |
|---|---|---|
| AUDITED | YES | `registry/adjudicated/`: kf-KeyframeTimeline · kf-TimelineTrack · kf-TimelineCaret · kf-TimelineHoverPreview · kf-SequenceScrubber · kf-AnimationVisualizer · kf-SpringTarget · kf-SpringTrace · kf-CSSPasteDialog · kf-KeyframesAddDialog · kf-CSSCodeEditor · kf-RibbonBar · kf-KeyframeCard |
| SPECIFIED | YES — this file | **authored from the 58-record registry directly; the routed denominator is the registry's** — **Census carriage is NOT asserted in this spec's voice (LAW B, `RULINGS-3.md`).** The claim lives in the freshest conformance census artifact and is cited by path: **`docs/tranches/X/keyframes/conformance/PASS-5/KF-W7-CHECK.md` §1 (2026-08-28)** — a FRESH id-keyed re-enumeration from the 58 records' bytes, inheriting nothing from PASS-4 — found **135 routed · 135 booked · 0 escaped by bytes**; the same artifact recorded **12 defects (6 MAJOR · 6 MINOR), verdict DEFECTIVE** — the defects this repair round cures. ⟨**Cite re-pointed at repair round 5 (RULINGS-5 §END KF-W7 D3 / R5-11); the figure is READ from the artifact, never re-issued here** (LAW D(3): a count is stated AT its enumeration, and dependent cells point at it). **THE ARITHMETIC FIRST, THEN THE SCOPE (R5-11, RECEIPT-ARITHMETIC-FIRST): the enumeration, the counting rule and the command all ride at this receipt.** Command and output, run by this seat at the artifact's bytes — `grep -n 'routed\*\* \|booked\*\*\|escaped by bytes\*\*' PASS-5/KF-W7-CHECK.md` → `:83 \| **routed** \| **135** (127 roster + 8 non-roster) \| 136 \|` · `:84 \| **booked** (124 carried + 3 excluded-with-named-owner + 8 non-roster) \| **135** \| 136 \|` · `:85 \| **escaped by bytes** \| **0** \| 0 \|` ⟨the quoted cells are the artifact's own table rows, three columns wide — this seat's figure, then the superseded PASS-4 figure; their pipes are escaped `\|` for this cell's grid and are literal in the source⟩. **THE −1 IS NAMED, ROW BY ROW, SO IT IS NEVER RE-LITIGATED AS AN ESCAPE.** The PASS-4 figure `136` did not re-derive from the rule printed beneath it — the sixth consecutive generation of that class, and this time the arithmetic sat inside the artifact the spec cited as authority. **Two of its nine non-roster entries do not survive the rule**: (i) **`kf-SpringTarget.md:69` was counted TWICE** — §1a books that record at 2 (`C-4` · `m-8`) and `m-8`'s routing line *is* `:69`, which §1b then books again as *"the kf-EasingTarget P-2 cure caution riding m-8"*; **a cure-caution clause riding INSIDE an already-counted row is not a second identity** (rules (3)/(5), this cell's own granularity and dedupe rules); (ii) **`kf-PlaybackRibbon.md:36` is a routing-law preamble**, the class rule (1) excludes **by name**. **Nine non-roster entries − 2 = 7** (the routing-summary-tail seam that rule (1) states outright, below; **the two COUNTED tails rule (1) names — `kf-RibbonBar.md:131` and `kf-SequenceScrubber.md:134` — are ALREADY INSIDE THE NINE, so no entry is newly admissible and the round-5 `+1` had no referent**); **127 + 7 = 134** ⟨***CORRECTED AT REPAIR ROUND 6, PASS-6 D1 — the SEVENTH consecutive generation of this figure, and the first in which the arithmetic contained a phantom operand. The cited artifact was itself contradictory and was re-issued untested: `PASS-5/KF-W7-CHECK.md` §1b lists nine rows, marks exactly TWO ✘, then prints "subtotal counted 8", and its §1d footnote concedes "under a strict reading of rule (3) that also drops `kf-PlaybackRibbon.md:36`, the figure is 134" — i.e. its 135 required counting a row its own table struck. CARRIAGE IS UNAFFECTED: 0 escaped under every reading, and both contested identities are carried regardless.***⟩. **THE DELTA IS A DENOMINATOR AND NEVER A DROP — both identities are CARRIED by this spec under either reading** (`m-8` at P0's KF-AV-10 row; `PR-CAUTION` at the header lock and at §Carry's label disclosure), and **escaped is 0 under all three admissible readings** (135 · 134 under the strict rule-(1) reading that also drops the preamble · 136 under the superseded arithmetic). **What the six generations turned on is now stated plainly**: rounds 1–4 kept re-cutting the MEMBERSHIP test (the owner/mention reconciliation), and the delta never turned on membership at all — it turns on the **ROSTER/NON-ROSTER BOUNDARY**, which no round before this one touched. That boundary is rule (1), and it is repaired there. **The PASS-4 figure `136 · 136 · 0` and the PASS-3 figure `138 · 138 · 0` are the dated log, superseded**: the delta is FOUR rows and it is a rule question, not an escape — carriage is complete under both readings, and the four rows are carried by this spec either way (named at rule (2) below).⟩ When a later pass supersedes that artifact the citation is re-pointed; the claim is never re-asserted here. ⟨**The round-2 figure "150 routed · 150 accounted (144 + 6) · 0 escaped" is STRUCK** (RULINGS-3 §END, KF-W7 D5): it is not re-derivable from any rule this spec stated. The spec gave a dual-home rule and **no granularity rule and no record-set boundary**, so 150 sits between the two reproducible readings and matches neither — roster-explicit-only reads **131**; expanding every banked merged multi-id header overshoots **150**. A figure that may well be right but is not checkable is precisely the failure R-12 and R2-9 were written to end.⟩ ⟨**The rule under which the cited artifact's 136 reproduces, stated so a later seat re-runs it rather than inheriting it. RECONCILED AT REPAIR ROUND 4 (RULINGS-4 §END KF-W7 D1): the OWNER test GOVERNS and the mention test is its corollary** — the two were written as co-equal and they conflict on four rows, which is why the figure admitted two readings for five consecutive generations⟩ ⟨**THE FIVE-PART RULE, as reconciled**: (1) **RECORD-SET BOUNDARY** — the 58 `kf-*.md` adjudicated records; roster rows plus hand-enumerated non-roster routings (FOLDS lines, routing-summary tails, §BLOCKER blocks). Routing-law preambles and prose mentions are context, never routings. **⟨THE ROUTING-SUMMARY-TAIL DISCRIMINATOR, WRITTEN AT REPAIR ROUND 5 (PASS-5 check D8 / R5-11) — the half of the boundary the rule ADMITTED a class into and then never told anyone how to test.⟩ A routing-summary tail is a NON-ROSTER ROUTING iff it carries an obligation not otherwise booked; a tail that merely RESTATES an identity already counted at the roster or already fold-deduped is context, not a second routing.** Six records carry a tail naming this wave, and the rule's verdict on each is now stated rather than practised: **COUNTED — `kf-RibbonBar.md:131`** (carries the expanded-timeline settlement obligation; booked at §Sequencing item 8 + P5's RB-7) and **`kf-SequenceScrubber.md:134`** (carries D-5/D-6/D-11's verdict-gated arms; booked at the → KF.W11 arm (d) + P0's C·C-4). **NOT COUNTED — `kf-SpringTrace.md:139` · `kf-SpringTarget.md:146` · `kf-SequencePlayhead.md:151`**, each restating an identity already counted at §1a or fold-carried at §1c, adding no obligation. **RECORDED AS AN EXPLICIT ZERO — `kf-SequenceScene.md:153`** (*"KF.W7: none"*), beside `kf-SequenceAxis.md:130`'s *"KF.W6/W7: no axis-owned rows"*. **The practice was always defensible; the omission was that the rule nowhere said so** — and an unstated discriminator on an admitted class is how a denominator drifts for six generations while carriage stays clean. **This is the residual half of the round-4 reconciliation**: that round fixed the MEMBERSHIP test and left the BOUNDARY test unwritten. (2) **MEMBERSHIP — THE OWNER TEST, GOVERNING**: a row is ROUTED to this wave iff **KF.W7 is the OWNER named in its own terminal disposition**, bare or in a `KF.Wn/W7` dual-home. **Corollary (the former rule (2), demoted to a NECESSARY-NOT-SUFFICIENT filter)**: the disposition text must name `KF.W7` — but a `KF.W7` appearing only as a *cross-ref*, an *evaluation-input pointer*, or a *rider-governance clause* on a row whose owner is `NO-WAVE-OWNER` or another wave is **NOT** a routing, which is rule (5) applied at the membership seam rather than after it. `KF.W7-TOKENS` is held OUT by mechanism (the naming-schism namespace → §Excluded) and `KF.W6-TIMELINE` dispositions are held IN (§Sequencing's taxonomy reconciliation). **The FOUR rows the two readings disagree on, named so the delta is auditable and never re-litigated as an escape — each quoted by command at the registry bytes, this seat**: ⟨`grep -n '^- \*\*KF-AV-13 \|^- \*\*KF-AV-14 \|^- \*\*KF-AV-24 ' kf-AnimationVisualizer.md` → **KF-AV-13** *"→ **NO-WAVE-OWNER** (SS-1/SS-2; decide press-to-seek with the KF.W7 evaluation)."* · **KF-AV-14** *"→ **NO-WAVE-OWNER** (SS-1/SS-2; cross-ref KF.W7)."* · **KF-AV-24** *"→ **KF.W8** (seam consolidation decision), cross-ref KF.W7."*; `grep -n 'KAD-F3' kf-KeyframesAddDialog.md` → **KAD-F3** *"→ **FOLD to `kf-CSSPasteDialog` R-7**"*⟩ — the first three are **rider-governed non-routings** (owner = NO-WAVE-OWNER / KF.W8; excluded by the owner test, counted by the mention test) and **KAD-F3 is fold-deduped** to the counted `R-7` under rule (5)/M-25. **All four are CARRIED by this spec regardless** (KF-AV-13/-14/-24 at P0's riders row; KAD-F3 at P8's `R-7 ≡ KAD-F3`), so no reading produces an escape — the delta is a denominator, never a drop. (3) **GRANULARITY** — a banked MERGED multi-id header counts **ONCE**, at its banked identity (`m-9/m-11/m-12` = 1; `C-10/C-11/D-m6/m-1/D-m5` = 1); a merged group is never truncated to shrink a denominator nor expanded to inflate one. (4) **DUAL-HOMES count once** — carried here if the mechanism is this wave's (kf-TimelineHoverPreview **MISSED-3**, carried whole at §Carry P7's head), excluded-with-named-owner if it is not (**KAD-6** carried; **KAD-15 · KAD-16 · KAD-20** excluded to KF.W6, §Excluded). (5) **NO-WAVE-OWNER rows the KF-AV-28 rider merely GOVERNS are not routings** and are not counted — **and its second arm, stated at round 4 because the two readings turned on it: IDENTITY DEDUPE (M-25)** — a FOLDS/identity-guard line that discharges to an already-counted banked id is **fold-carried, not a new routing**. Rule (5) is the governing test of which rule (2) is the filter; where they disagree, (5) wins. **⟨THE ENUMERATION THIS RULE DISCHARGES AGAINST, RE-POINTED AT REPAIR ROUND 5 (PASS-5 check D7).** The rule does not enumerate the fold-carried lines here; it points at the one enumeration, *"so the dedupe is auditable, never silent"* — and the artifact it pointed at listed **16** while the sweep finds **17**. The live enumeration is now **`PASS-5/KF-W7-CHECK.md` §1c**, whose heading is its own receipt: `grep -n '^### §1c' PASS-5/KF-W7-CHECK.md` → `:75 ### §1c · Fold-carried lines resolved by identity — 17, not 16` — **and the enumeration beneath that heading lists EIGHTEEN coordinates** (`KT:106 :114` · `TT:86` · `TC:74 :75 :77 :78` · `THP:77 :80 :81 :82 :84 :86 :88` · `SP:92 :143` · `SS:128` · `ST:135`). **COUNTING RULE, stated here because R5-11(1) requires it AT the receipt and the cited artifact states none: `KT:114` is annotated "counted once at §1b" and is therefore NOT counted a second time here, which is the reading that yields 17; under a bare coordinate count the figure is 18.** ⟨***stated at repair round 6, PASS-6 D10: D7's cure corrected the enumeration's MEMBERSHIP and re-inherited its ARITHMETIC — PASS-4's "16 lines" enumerated 17, in the same direction. E-3: the cited artifact is not edited; the rule lives here.***⟩. **The missing member, named**: `grep -nE 'KF\.W7' kf-SequencePlayhead.md` → **THREE** hits — `:92` · `:143` · `:151` — and `:92` is the one the old enumeration omitted: *"**Folded-by-identity (carried, not rows):** (1) the **C-§4 export-surface correction** ≡ banked kf-KeyframeTimeline C-15 (→ KF.W7)"*. **Nothing escapes** — its target **C-15 is this wave's evaluation input, carried at P0 with maximum spec weight** — but an enumeration that misses a member cannot discharge the rule that cites it, and a rule whose auditable list is short by one is silent about exactly the thing it promised to be loud about.⟩⟩ ⟨Dated log, not a live figure: the round-1 "148 routed · 145 carried + 3 excluded" was defective twice — it dropped MISSED-3 from the denominator while counting its identically-shaped twins, and its composition contradicted §Excluded two lines below it. Superseded twice over.⟩ **Provenance correction (RULINGS R-12)**: this spec previously bound itself to a "KF.W7 CARRY ledger" that exists at no coordinate — the only in-tree **X·KF** carry is `docs/tranches/X/keyframes/carry/KF-W6-CARRY.md` (201 rows / 54 records, KF.W6's; 424 was the literal `KF.W6` token count, never a row count) ⟨scope word restored: X·KF. The tranche-X tree also holds `fourier/carry/F-W1-CARRY.md` and `fourier/carry/F-W4-CARRY.md` — other sub-tranches', not this one's⟩. **FIVE bindings struck**, not four: the ruling's four (this cell · §Bounds' `boundsFiles` · §Excluded's opener · the fold-repair footer) **plus a fifth this repair found at the closing footer**. Nothing here is provenanced on an unproduceable source, and the falsifiability claim the old text made ("grep-verifiable against the ledger") now names a source that can actually be grepped. |
| IMPLEMENTED | NO | stays NO until gates green after the begin-word |
| VERIFIED | NO | stamped only at sub-tranche close |

**Goal criterion.** This wave succeeds if, when work ends, the lane can answer — with the installed export surface in hand, not with taste — *"is the timeline instrument ours to keep, and if we keep it, what is the one seam that makes scrubbing real?"* Concretely: a per-surface ADOPT/PARTIAL/KEEP-BESPOKE ruling against the IMPORTABLE glass surface; the scrub seam ruled either LIVE (then wired, guarded, single-engine, in one commit) or DISPLAY-ONLY (then said so, with the rungs that hang off that ruling re-graded); and every NO-WAVE-OWNER row beneath the rider given a terminal disposition. **A wave that wires `scrub` into the drag and calls it done has failed this goal** — that is N-10's convicted regression, not a cure.

**Execution shape**: 6 seats across 4 sequential phases — phase 1: 1 serial (the S-9 evaluate seat; its verdict sizes the rest); phase 2: 2 parallel (seam design ∥ preview/ghost design); phase 3: 2 parallel (implementation, disjoint by file); phase 4: 1 serial (close + BH relay). Peak concurrency 2, inside the owner's four-workflow cap.

---

## §0 Open preconditions (checked at wave-open; NOT close gates)

| # | precondition | state at authoring |
|---|---|---|
| **OP-0** | **kf-AnimationControlsGroup D-1 (ADJUDICATED BLOCKER, restored) — the document-singleton portal sink — must be SETTLED before this wave's timeline-cluster work.** One sink, N Teleport sources: expanding the timeline stacks every painting channel's KeyframeTimeline into one clipped box. The cure is **NO-WAVE-OWNER**; the **SETTLEMENT** — a design ruling: keep or replace the portal architecture, carrying its banked scope enumeration (**cube 3 · amiga 3 · spring 2**, all animations stamped with the scene `superKey` so ONE bucket flips ALL instances) — **is this wave's intake, exactly as OP-1's `:key` row is.** Banked terminal disposition, verbatim: *"**NO-WAVE-OWNER**, must be settled **before** KF.W6-TIMELINE (any timeline-cluster swap inherits the portal architecture)"* ⟨`kf-AnimationControlsGroup.md`, the §BLOCKER **D-1** row — head at `:40`, disposition at `:42` at this writing; the banked id is the anchor⟩. Cure space recorded, not chosen (per-channel targets · a selection-gated source · this component becoming the single owner of the expanded instrument, which already computes `activeTimelineRef`). | UNSETTLED — **booked by RULINGS-2 R2-9**; it is one rung above OP-1's peers at the bank (a restored BLOCKER), so it OPENS the hard ordering (§Sequencing 1). Load-bearing here, not clerical: **C-6's cure is routed through this very component** (§Carry P1, restated in G2's assertion) and is CONDITIONED on this settlement; D-1's own scope surface (the expanded, stacked timeline) is the same surface D-10 (TimelineTrack, expanded mode) + RB-7 settle. |
| **OP-1** | **kf-ChannelControls L-2/C-2 — the `:key` remount BLOCKER — settles BEFORE this wave opens.** The bank's own words (verbatim at `kf-ChannelControls.md`, the L-2/C-2 terminal disposition): *"any timeline-cluster swap that inherits the `:key` inherits the data loss."* | UNSETTLED. Hard ordering, not advice. Second-order collision resolved by this spec at KF-CE-2 (parent half), §Carry P8. |
| **OP-2** | **KF.W4's vue-tsc gate (KF-CE-16, "the sequencing head of the whole record") + `noUnusedLocals` (K3).** | ABSENT ⟨measured: `package.json` at origin/master `"check": "tsc --noEmit && tsc --noEmit -p tsconfig.test.json && npm run proof:structure"` — no `vue-tsc` (citation correction contributed: the banked quotation of `"check"` omits the `&& npm run proof:structure` leg); `demo/env.d.ts:3-7` maps `*.vue` → `DefineComponent<{}, {}, any>`⟩. Every typed cure here is authorable but UNVERIFIABLE until that gate lands. |
| **OP-3** | **D-19 geometry re-derivation before any geometry cure** — K-13 corrected every table cell ~1px (padding-box); banked D-17 carries the SAME uncorrected figures. | OPEN. Both tables re-derive before a cure is written (§Carry P4). A cure computed off the border box is a cure to the wrong number. |
| **OP-4** | **A mount harness must exist for G11 — it costs ONE devDependency and one INBOUND dependency, not two adds.** | PARTLY ABSENT. **Re-measured by THIS seat at `origin/master 81a56990` (RULINGS-3 R3-8.3; the round-2 cost model was false at the frontier and is struck):** `git show origin/master:package.json \| sed -n '81p'` → `"@vitejs/plugin-vue": "^6.0.7",` · `ls -d node_modules/@vitejs/plugin-vue` → PRESENT · `git show origin/master:package.json \| grep -c 'test-utils'` → **0** · `git show origin/master:vitest.config.ts \| grep -n plugins` → **(none)** · `ls -d node_modules/@vue/*` → **nine**: `compiler-core` · `compiler-dom` · `compiler-sfc` · `compiler-ssr` · `reactivity` · `runtime-core` · `runtime-dom` · `server-renderer` · `shared` ⟨the round-2 reading *"compiler/reactivity/runtime only"* under-enumerated — the conclusion held, the enumeration did not⟩; `jsdom ^29.1.1` IS present. **THE `+@vitejs/plugin-vue` ADD IS STRUCK AS A NO-OP** — the plugin is already a devDependency at `package.json:81` and is installed. **KF.W4 struck exactly this add at ITS round 2 under the same witness-re-derivation law — `KF-W4.md` §Bounds, the `package.json` row** ⟨**RE-ANCHORED AT REPAIR ROUND 4 UNDER LAW C(1)/(2)**: the round-3 citation was the raw coordinate `KF-W4.md:46`, and W4's own round-4 write MOVED it — re-derived by this seat, and **RE-RUN WHOLE AT REPAIR ROUND 5** at KF.W4's FINAL round-5 bytes ⟨per-seat mtime stamps are retired at LAW E(4) — the substrate table lives at CLOSE-CERT-5; W4 is a stage-1 sibling whose round-5 write precedes this one⟩. **The command and ALL FOUR of its lines, nothing trimmed** — `grep -n 'vitejs/plugin-vue' KF-W4.md` → **`:55`** (the `package.json` §Bounds row — the devDeps carve, where the add is struck as present) · **`:59`** (the `vitest.config.ts` §Bounds row — the `projects`/`plugins` registration surface) · **`:166`** (W4's carry item 3, `ME-32 ≡ R-10`) · **`:212`** (W4's `G-KFW4-2` gate row); `grep -c` → **4**. ⟨**P7 COORDINATE SWEEP, 2026-08-29** — re-run at final bytes: whole output **`:55 :59 :167 :213`** — `:55`/`:59` unmoved, the carry item and the gate row now **`:167`**/**`:213`**; `grep -c` still **4**, and all four rows are byte-identical⟩ **Both extra hits CORROBORATE** — each is a further W4-side booking of the same registration act — and neither touches this cell's conclusion. ⟨**STRUCK at repair round 5, PASS-5 check D4 (a LAW D(3) numeric-arm violation)**: rounds 3–4 printed this command at **2 of its 4 lines** — *"→ `:54` (the `package.json` row) and `:58` (the `vitest.config.ts` row)"* — a short transcript under a pasted-command label, at the very site the round-4 LAW-C block advertised as its flagship re-derivation. The conclusion was true and is unchanged; the transcript was not the command's output, and a command whose output is silently trimmed guarantees nothing. **Note the second lesson, which is why the numbers are not the binding thing**: W4's round-5 write moved all four again (`:54`→`:55`, `:58`→`:59`, `:165`→`:166`, `:211`→`:212`). The binding anchor is the §Bounds ROW, never the number — `sed -n '46p' KF-W4.md` already returned a different row at round 4, and under R2-7's own law a receipt that returns a different row is not a receipt⟩⟩ — **and the third claimant on this manifest line is named as an ANCHOR ALONE** ⟨`KF-W8.md` **§Bounds → its `package.json` row**. **Forward reference**: KF.W8 writes LATER in this round's stage order, so LAW C(3) as amended by LAW E(4) binds — **no verb, no glob, no count, no prose, no line number**; the round-3 coordinate `KF-W8.md:244` was STRUCK unverifiable-at-this-clock, and the **RECONCILE seat verifies this citation after W8's write**. ⟨**Re-cut again at repair round 5, PASS-5 check D12, by scope**: rounds 3–4 added the characterization *"KF.W8 independently states the correctly-scoped form"* — true or not, it is a claim about a sibling's prose wearing an anchor's clothes — one of the eight members of the class this round retires. **This cell's own conclusion never needed it**: the `+@vitejs/plugin-vue` add is struck as a no-op on THIS seat's own frontier measurement (`package.json:81`, `^6.0.7`, installed), which is a fact about the tree, not about a neighbour's spec⟩⟩; this spec was the outlier of three, and the error had propagated into the cost model of the gate that gates ten other gates. **The true residue is two things, and only two**: (a) **REGISTRATION** of the plugin in `vitest.config.ts` — **KF.W4's booked act, not this wave's** (`KF-W4.md` §Bounds `vitest.config.ts` row · §Commits **commit 2**) ⟨**RE-QUOTED AT REPAIR ROUND 4 UNDER LAW C(2) + LAW D(1), at W4's FINAL stage-1 bytes** (substrate `waves/KF-W4.md`, mtime **2026-08-28T16:59:44**), command pasted: `grep -n 'plugin-vue REGISTRATION' KF-W4.md` → **`:269`** (W4's `→ KF.W8` cross-edge row, booking the same registration act from the other side — **corroborating**) **and `:322`** (the §Commits line quoted below) — **2 lines, printed whole** ⟨**P7 COORDINATE SWEEP, 2026-08-29** — re-run at final bytes: whole output **`:270 :323`**, still 2 lines, both byte-identical⟩ ⟨***transcribed whole at repair round 6, PASS-6 D9: the round-5 paste summarised a two-line output as "the §Commits line". It is the fifth pasted command in a live cell, and `:438`'s TRANSCRIPT ARM enumerated only four (`vitejs/plugin-vue` · `KF-AV-17` · `sed 150p` · `§1c` heading) — the class re-cut BY SCOPE at the W8 receipts and BY INSTANCE here.***⟩ — *"2 `ci(kf/merge-path)` — demo vitest project + **plugin-vue REGISTRATION** (the devDep is already present at `package.json:81`; only `vitest.config.ts` changes **— and this commit is the witness KF.W8's G10 cites to discharge its RED condition; the registration is performed here, once, and W8 performs no `vitest.config.ts` edit**, R3-8.2) …"* ⟨***the round-4 ellipsis is corrected at repair round 6, PASS-6 D11: the source carries an EM-DASH at that position and NOTHING is elided, so the "…" both reported an elision that did not occur and substituted a character of the source. Harmless in conclusion; recorded because a quotation whose ellipsis is decorative teaches a reader that this file's ellipses are decorative.***⟩. **The elision is marked and the addition disclosed**: rounds 1–3 closed the parenthesis at *"only `vitest.config.ts` changes"* and printed it as a complete quotation — a TRUNCATION under a verbatim label, the U4-E class, harmless in conclusion (the registration is still W4's, still once) but false as a quotation. W4's round-4 write EXTENDED that parenthetical, which is precisely why a same-round sibling quote is re-run rather than inherited⟩, consumed here as an inbound dependency (§Sequencing → KF.W4); (b) **`@vue/test-utils`** — genuinely absent from manifest and disk, and this wave's ONE manifest add. The house drive-the-composable-through-a-host idiom still cannot mount the render-only THP SFC, so the harness is still not free — it is costed at one devDep plus a dependency-cite. Hence the `package.json` carve narrowed to `+@vue/test-utils` and the **struck** `vitest.config.ts` bounds row (§Bounds). |
| **OP-5** | **KF.W2's parse façade must exist as L-6/C-4's delegation target.** | OPEN. The cure is DELEGATE, and delegation needs a target: `@mkbabb/value.js/css` exports the collectors; the sibling path already honours the doctrine (`timelineEngine.ts:13` `parseAnimationCSS`, `:90` `serializeCssValue`) — which is what makes the SFC's hand-rolled parser a BYPASS, not an omission. |
| **OP-6** | **The S-9 evaluate verdict sizes this wave's own roster.** | BY CONSTRUCTION. Under KF-AV-28 the phase-1 verdict may supersede rows in §Carry. Phases 2–4 dispatch AFTER phase 1 commits, never concurrently. |

---

## §Bounds

Owned files (bounds reconciled against the **registry records' own file coordinates** — there is no KF.W7 CARRY `boundsFiles` list to reconcile against, and the phantom claim that there was is struck per RULINGS R-12; all under `/Users/mkbabb/Programming/keyframes.js/`; line counts = `git show origin/master:<f> | wc -l`, each re-measured by this seat):

| File | L | Access | Note |
|---|---|---|---|
| `demo/components/instrument/timeline/KeyframeTimeline.vue` | 312 | modify | cluster head; ` M` vs HEAD |
| `demo/components/instrument/timeline/components/TimelineTrack.vue` | 246 | modify | byte-identical at all three refs (anchor-safe) |
| `demo/components/instrument/timeline/TimelineCaret.vue` | 70 | modify | byte-identical at all three refs |
| `demo/components/instrument/timeline/components/TimelineHoverPreview.vue` | 38 | modify | |
| `demo/components/instrument/timeline/composables/useTimeline.ts` | 129 | modify | origin-side anchor tier (G12) |
| `demo/components/instrument/timeline/composables/useTimelineBuild.ts` | 200 | modify | origin-side |
| `demo/components/instrument/timeline/composables/useTimelineOps.ts` | 88 | modify | origin-side |
| `demo/components/instrument/timeline/composables/useZoomPan.ts` | 110 | modify | byte-identical at all three refs |
| `demo/components/instrument/timeline/timelineTypes.ts` | 41 | modify | `selector: KeyframeSelector` exists at origin/master `:5-6` ONLY — absent at HEAD |
| `demo/components/instrument/timeline/index.ts` | 8 | modify | barrel; the dead-lazy-barrel row is KF.W8's |
| `demo/components/instrument/timeline/utils/timelineEngine.ts` | 102 | modify | origin-side; SHARED — serial-only (see disjointness); count correction: 102 at origin/master — the worktree 104 is the dirty `+4/−2` edit that WIDENS the C-12 breach |
| `demo/components/instrument/timeline/utils/snapshotCapture.ts` | 66 | modify | |
| `demo/components/instrument/timeline/utils/flattenVars.ts` | 33 | **delete-candidate** | deletion rides `loadPreset`'s adopt-or-delete (L-13). **LAW A IMPORT-GRAPH CENSUS (RULINGS-3 LAW A), executed by this seat at `origin/master 81a56990`, commands + outputs, inherited from no CARRY, no bank paraphrase, no prior pass:** ⟨**(1) SPECIFIER census**, every relative-depth spelling and every alias root that can reach `P` as its own pattern — the alias roots are read from `vite.config.ts` `resolve.alias` + `tsconfig.json` `paths` (`@src` · `@mkbabb/keyframes.js` · `@styles` · `@state` · `@components` · `@utils` · `@kf-engine` · `@composables` · `@app` · `@assets`), of which `@components/*` is the only one that can reach this path: `git grep -nF '../utils/flattenVars' origin/master -- demo/ test/ src/ scripts/` → **1** — `demo/components/instrument/timeline/composables/useTimelineBuild.ts:14 import { flattenVars } from "../utils/flattenVars";`; `git grep -nF 'utils/flattenVars' …` → the same 1; `git grep -nF '@components/instrument/timeline/utils/flattenVars' …` → **0**; `git grep -nF 'instrument/timeline/utils/flattenVars' …` → **0**; `git grep -nF './flattenVars' …` → **0**. **(2) SYMBOL census** over the module's single exported name (`export function flattenVars(` at `flattenVars.ts:10` — the file exports nothing else): `git grep -n '\bflattenVars\b' origin/master -- demo/ test/ src/ scripts/` → **4 hits**, each RESOLVED to its own import specifier and compared against `P`: `useTimelineBuild.ts:14` (the import) · `useTimelineBuild.ts:170` (its call, inside `loadPreset`, declared `:163`, returned `:197`) · `flattenVars.ts:10` (the declaration) · `flattenVars.ts:26` (the module's own recursion — not a consumer). **(3) CONSUMER SET = exactly ONE module: `useTimelineBuild.ts`**, and its single call site sits inside `loadPreset` — so L-13's *"sole consumer"* claim VERIFIES at the frontier, by the import graph rather than by line count. The ~40 further `flattenVars` hits under `docs/tranches/**` (D · G · U lanes) are **prose/docs, recorded as non-import context, never counted as consumers**; the U-loop `SPEC-B2.md:229` row claiming **2** consumers (`useTimelineBuild.ts`, `timelineEngine.ts`) is **stale — `timelineEngine.ts` imports it at NO ref reachable here** (0 hits in the census above), and it is cited as historical context only.⟩ **Binding on execution**: `loadPreset` is itself re-exported at `useTimeline.ts:44`/`:121`, so the adopt-or-delete decision must dispose that surface in the SAME act — a delete of `flattenVars.ts` alone leaves `loadPreset` uncompilable. |
| `demo/components/instrument/timeline/CSSPasteDialog.vue` | 80 | modify | fold TARGET (R-7 direction pinned: this shell survives) |
| `demo/components/instrument/keyframes/components/KeyframesAddDialog.vue` | 161 | modify | fold SOURCE; its `innerHTML` BLOCKER is KF.W5's, front-loaded, NOT gated here |
| `demo/scenes/sequence/SequenceScrubber.vue` | 162 | modify — shape rows verdict-gated | S-3/S-4; component-shape rows discharge or persist with the G1 verdict |
| `demo/components/playback/AnimationVisualizer.vue` | 256 | modify — verdict-gated | S-9 census extension subject; the LARGER of the two rail/ball shadows |
| `demo/utils/keyframeSelector.ts` | 31 | modify | exists at origin/master ONLY — untracked at stale HEAD (G12) |
| `package.json` (keyframes.js) | — | modify-carve — **ONE line: `+@vue/test-utils` in `devDependencies`** ⟨narrowed at this repair round, RULINGS-3 R3-8.3 + §END DH-2⟩ | OP-4's residue (b). The `+@vitejs/plugin-vue` half is **STRUCK as a no-op** (present at `:81`, `^6.0.7`, installed). **FOUR-PARTY COLLISION DECLARED (DH-2 — the R2-12 both-ends law extended from directories to this file); this row names the other three claims and its own carve**: **KF.W3** claims `package.json` **`:70` only** — the value.js exact re-pin `4.0.0 → V` (`KF-W3.md` §Bounds, the `package.json (:70 only)` row); **KF.W4** owns the **scripts** surface (`check` · `check:lib` · `lint` · `test:*`) **and** its own devDeps (`+vue-tsc`, `+eslint`/`eslint-plugin-vue`, `−monaco-themes` at `:92`) and **lands first** (`KF-W4.md` §Bounds `package.json` row); **KF.W8** also claims this file ⟨`KF-W8.md` §Bounds → its `package.json` row — **anchor-only, no verb restated**; re-cut at repair round 5 with the D1/D12 class, by scope⟩, and the narrowing of that claim to **`+@vue/test-utils`** — the same single add as this row — is **RULINGS-3 R3-8.1's**, cited at the ruling (an immutable artifact) rather than read off a sibling that writes after this seat; the **export-surface DECISION is KF.W5's**, not W8's. **This wave writes NO script, NO exports/sideEffects key, and no other dependency line.** Ordering, declared: W4's manifest edit precedes both W7's and W8's; the `@vue/test-utils` add is **idempotent and single-instance** — the first of {W7, W8} to land writes it, the second CONSUMES it and asserts its presence rather than writing a second entry; the two never run concurrently (W8 opens after W4/W5 and depends on this wave's G15 ruling, §Sequencing → KF.W8) |
| ~~`vitest.config.ts`~~ | — | **STRUCK — NO WRITE BY THIS WAVE** ⟨RULINGS-3 R3-8.3 + §END DH-2, DH-2's first branch taken⟩ | The round-2 row carved a `plugins` key here on a cost model that was false at the frontier (OP-4). **The plugin-vue REGISTRATION is KF.W4's booked act** — `KF-W4.md` §Bounds `vitest.config.ts` row (the absent `plugins` array; the `projects` array at `:38-56`) · item 3 **ME-32 ≡ R-10** · **§Commits commit 2** (*"only `vitest.config.ts` changes"*). This wave **consumes** it as an inbound dependency and **performs no `vitest.config.ts` edit**, which is what makes the KF.W4 cross-edge's scope sentence *"depends-on, for VERIFICATION not authoring"* TRUE — it was false for exactly as long as this row stood. **Three-party collision declared** (DH-2): claimants are **KF.W4** (authoring: `projects` + the `plugins` registration), **KF.W8** ⟨`KF-W8.md` §Bounds → its `vitest.config.ts` row — **anchor-only, no verb and no ordering word restated**; re-cut at repair round 5 with the D1/D12 class, by scope⟩, and this wave, **now non-writing**. G11's runnability therefore depends on W4's commit 2 having landed — a dependency-cite, not an act (see G11's witness block) |
| `test/demo/instrument/timeline-mount-projection.test.ts` | — | **create** | G11 fixture 1 — pointer-guard + grab-offset + rebuild-rate mounts (G2/G3/G4) |
| `test/demo/instrument/timeline-mount-keyboard.test.ts` | — | **create** | G11 fixture 2 — keyboard parity, non-destructive selection, caret read-gesture (G7/G8/G13) |
| `test/demo/instrument/timeline-hover-preview.test.ts` | — | **create** | G11 fixture 3 — THP mount: accessible description, ghost truth, cache invalidation (G9/G10); the C-9 (THP) coverage row's own surface |
| `test/demo/instrument/sequence-scrubber-mount.test.ts` | — | **create** | G11 fixture 4 — SequenceScrubber mount: direction latch + projection (kf-SequenceScrubber L·D-12, the banked N-10 coverage class's second instance) |
| `demo/styles/design-idioms.css` (`:161-187` only) | — | modify-carve | **KF-AV-10's idiom adoption — the retint seam and nothing else**; carve extent stated so it cannot widen. Added at repair round 6, PASS-6 D8. |
| `demo/scenes/spring/SpringTarget.vue` (`:113` + the `.spring-ball`/`.preset-ball` anchor sites) · `demo/scenes/spring/SpringPhysicsFacet.vue` (the `.sampler-ball`/`.derby-lane-ball` anchor sites) | — | modify-carve | **KF-AV-10's live retint seam + m-8's T.G4 anchor triplet re-authored four times**; **VERDICT-GATED — no byte is spent before G1's KEEP-BESPOKE-or-ADOPT ruling**, and if G1 rules KEEP-BESPOKE these carves are SPENT-UNUSED and close with the wave. Added at repair round 6, PASS-6 D8. |
| `demo/scenes/sequence/SequenceTarget.vue` (`:80` only) | — | modify-carve | **KF-AV-10's retint seam, the sequence-side site.** Note: KF.W6 holds a disjoint one-line carve on this file (`:31`'s `.btn-interactive` class attribute) — **the two carves do not overlap and neither widens.** Added at repair round 6, PASS-6 D8. |

**`test/demo/instrument/` is a SHARED directory this wave does NOT own — paths are enumerated, never globbed by shared prefix** (the X.P.W3 §4a lesson; the same law KF-W5's §Disjointness applies to itself). RULINGS-2 **R2-12**: the former `test/demo/instrument/**` create-glob is **STRUCK**; the four fixtures above are this wave's entire footprint in that directory, by full filename. **The NINE files already tracked there are READ-ONLY WITNESSES, by name** ⟨re-derived this seat: `git ls-tree -r --name-only origin/master -- test/demo/instrument` → `KfPillTabs.test.ts` · `ios-text-entry.test.ts` · `kf-toolbar-keyboard.test.ts` · `resize-tracks.test.ts` · `timeline-undo.test.ts` · `transport-play-actuation.test.ts` · `useAnimationGroupPlayback.test.ts` · `useThrottledReadout.test.ts` · `value4-editor-boundary.test.ts` — nine⟩. Two of them are this spec's own cited witnesses and **this wave's seats must not edit them to make a gate pass**: **`timeline-undo.test.ts`** (G11's witness — the test that exercises the same state WITHOUT the surface) and **`resize-tracks.test.ts`**. A `**` create-grant over a directory containing a file the wave uses as a witness is authority over its own evidence; that is why the glob dies.

**FOUR-PARTY SHARED-DIRECTORY DECLARATION (RULINGS-3 §END DH-3 — the R2-12 both-ends law extended from the KF.W5 pair to ALL claimants; each wave names every other claimant's files BY NAME).** Round 2 applied the rule to the first *neighbour* only — the same shape R2-9 convicted in the other direction — so this declaration is now exhaustive. Measured at the sibling specs' bytes, this seat:

| claimant | its footprint in `test/demo/instrument/`, by filename |
|---|---|
| **KF.W7** (this wave) | **4 creates** — `timeline-mount-projection.test.ts` · `timeline-mount-keyboard.test.ts` · `timeline-hover-preview.test.ts` · `sequence-scrubber-mount.test.ts`; **0 modifies**; the nine tracked files read-only to this wave's seats |
| **KF.W5** | ⟨`KF-W5.md` §Disjointness — its create row⟩ — **anchor-only, same rule (LAW E(4); converted at repair round 6, PASS-6 D14).** |
| **KF.W4** | ⟨`KF-W4.md` §Bounds — the `test/demo/instrument/` create rows (`typing-dots-engine-seam.test.ts` · `aurora-opacity-ceiling.test.ts`) and the `resize-tracks.test.ts` cure row⟩ — **named by filename, which IS an anchor; no verb, no glob, no count restated at this end (LAW E(4)).** ⟨***converted at repair round 6, PASS-6 D14: both claims were TRUE at their siblings' final bytes and are converted anyway, because the class LAW E(4) retires is same-round sibling content claims by a per-wave seat, not false ones. The round-5 conversion enumerated the named sibling (`grep -n 'KF-W8\.md\|KF\.W8' KF-W7.md`) instead of the class — the same keyed-on-the-sibling-not-the-scope mechanism that let D2 survive three rounds.***⟩ |
| **KF.W8** | **ANCHOR-ONLY — no verb, no glob, no count is restated at this end** ⟨`KF-W8.md` **§Bounds → its `test/demo/instrument/` tracked-files row** and **its `resize-tracks.test.ts` row**⟩ · **4 creates, BY FILENAME** — `sfc-load.probe.test.ts` · `groupShortcuts.test.ts` · `CSSPasteDialog.test.ts` · `KeyframesAddDialog.test.ts` ⟨`KF-W8.md` §Bounds, **the `test/demo/instrument/` create rows** — named by FILENAME (`sfc-load.probe.test.ts` · `groupShortcuts.test.ts` · `CSSPasteDialog.test.ts` · `KeyframesAddDialog.test.ts`), which IS an anchor; **no count is restated, at repair round 6, PASS-6 D6**; and the four are exactly what the no-collision assertion below consumes — nothing more is read from that file⟩. ⟨**RE-CUT AT REPAIR ROUND 5 (RULINGS-5 R5-9 row 13 + LAW E(4); PASS-5 check D1/D12).** Rounds 2–4 booked here, in this cell, *"`test/demo/instrument/**` (all nine tracked files) `modify-carve` · `resize-tracks.test.ts` `modify-carve` (KF-AV-18's prose arm, 6 value.js cites)"* — **a forward QUOTATION of a stage-2 sibling's access verbs wearing an anchor's clothes**. It was convicted false by `PASS-5/KF-W7-CHECK.md` **D1** (W8's round-4 write struck the modify-carve grant over the nine), and **RULINGS-5 R5-3 re-cuts that same row again this round** with two declared exceptions inside the narrowing — so the cell would have been false at close for the third consecutive generation had it been merely *updated*. **It is not updated; the class is retired.** A receipt that must be re-derived every round because its subject writes after this seat is not a receipt, it is a forecast — and the RECONCILE seat, not this one, verifies the anchor at final bytes (LAW E(4)).⟩ |

**FOUR waves write this directory. The round-2 sentence *"Neither wave owns the directory"* was a two-party statement over a four-party surface and is restated: NO wave owns `test/demo/instrument/`; each owns exactly its named files, and no two of the eleven created filenames above collide** (verified against all four §Bounds blocks at this seat's clock). **The tracked count is NINE at `81a56990`** (`git ls-tree`, above) — ⟨`KF-W4.md` §Disjointness — **the cardinality question is DISCHARGED at that end**: RULINGS-3 §END DH-3 routed the correction to the KF.W4 seat and that seat executed it **at round 3**, striking the round-2 double-count and stating NINE tracked files with its command. **Nothing is owed here and nothing is reconciled silently.**⟩ ⟨***struck at repair round 6, PASS-6 D2: the round-2 finding was carried as LIVE and PRESENT-TENSE for three rounds after the sibling cured it, and it survived round 5 for a mechanical reason the round itself names — the D1/D12 conversion enumerated its class with `grep -n 'KF-W8\.md\|KF\.W8' KF-W7.md`, KEYED ON THE NAMED SIBLING, NOT ON THE STATED SCOPE. This is this file's own quoted lesson — "a repair that cures the named instance and not the stated scope leaves the law green over its own counterexample" — firing one sibling over, in the round that quoted it twice.***⟩. **The read-only-witness reconciliation, stated as THIS end's own act and nothing more**: the nine tracked files are claimed by neighbours under their own bounds — ⟨`KF-W8.md` **§Bounds → its `test/demo/instrument/` tracked-files row**⟩ and ⟨`KF-W4.md` **§Bounds → its `resize-tracks.test.ts` cure-site row**⟩ — and **this end asserts NOTHING about the verbs, globs or counts on those rows** (anchor-only, LAW C(3) as amended by LAW E(4); the RECONCILE seat verifies both anchors at final bytes). ⟨**Re-cut at repair round 5, PASS-5 check D1.** The round-2..4 text read *"`KF-W8.md` §Bounds asserts `modify-carve` over exactly the nine files this wave reads as witnesses"* — an access claim about a sibling that writes after this seat, false at that sibling's own round-4 bytes and re-cut again by RULINGS-5 R5-3. **The invariant this paragraph carries outward never rested on that premise, and it is therefore restated UNCONDITIONAL rather than repaired**: it binds whatever the neighbours' verbs turn out to be, which is the only form a witness-freshness rule can safely take.⟩ This wave's declaration binds **its own seats** (they edit none of the nine) and carries one invariant outward instead of a prohibition: **no KF.W7 gate may be read against `timeline-undo.test.ts` or any tracked file at bytes a neighbour has since changed** — if KF.W4 or KF.W8 lands first, G11's *"exercises the same state WITHOUT the surface"* contrast is **re-executed at the edited bytes before the gate is read**, and the re-execution is recorded with its ref. A witness whose bytes moved under a gate is not a witness.

**Disjointness (phases 2–3 split by FILE, never by concern)**: seat `.d` owns TimelineTrack.vue + TimelineCaret.vue + useZoomPan.ts + useTimelineOps.ts; seat `.e` owns KeyframeTimeline.vue + TimelineHoverPreview.vue + useTimelineBuild.ts + snapshotCapture.ts + both dialogs. **`timelineEngine.ts` and `timelineTypes.ts` are shared and therefore SERIAL** — G5's collision decision writes both and lands before either seat dispatches. Two seats editing the merge map is how a merge cure becomes two merge cures. **The four G11 fixtures are seat `.d`'s and `.e`'s by subject** (`timeline-mount-projection` + `timeline-mount-keyboard` with `.d`; `timeline-hover-preview` + `sequence-scrubber-mount` with `.e`) — created, never co-edited. **Cross-wave filename disjointness, asserted over ALL FOUR claimants (R2-12 as extended by RULINGS-3 §END DH-3, every end named)**: the claimant table under §Bounds is this assertion's roster — KF.W5's one create (`highlight-css-roundtrip.test.ts`), KF.W4's two creates plus its `resize-tracks.test.ts` cure site, **KF.W8's four creates by filename** ⟨and its claims on the tracked files ANCHOR-ONLY — `KF-W8.md` §Bounds; re-cut at repair round 5 with the D1/D12 class, by scope: the round-3/4 text said *"plus its nine-file and `resize-tracks.test.ts` carves"*, a count and a verb about a sibling that writes after this seat⟩, and this wave's four fixtures. **NO wave owns the directory; each owns exactly its named files**, and none of this wave's four filenames collides with any of the other seven creates or with any of the nine tracked files. KF.W5's reciprocal naming of these four is present at `KF-W5.md` §Disjointness and re-verified this seat; the KF.W4 and KF.W8 ends are declared from here at §Sequencing.

Read-only witnesses (**never modified by THIS WAVE'S SEATS** — the universal is scoped to THIS wave's seats, because two of the entries below are claimed by neighbours under their own bounds — ⟨`KF-W4.md` §Bounds → its `resize-tracks.test.ts` cure-site row⟩ and ⟨`KF-W8.md` §Bounds → its `test/demo/instrument/` tracked-files row⟩, **anchor-only, no verb restated at this end**; re-cut at repair round 5 with the D1/D12 class, by scope — the round-2..4 text read *"since KF.W4 and KF.W8 carve two of the entries below"*. §Bounds' four-party table governs): `node_modules/@mkbabb/glass-ui/dist/components/timeline/**`, `dist/components/slider/types.d.ts`, `dist/components/tooltip/**`, reka `TooltipContentImpl.js`, **and the nine tracked `test/demo/instrument/` files enumerated above**. Producer changes go to the BH relay (SS-6) — a demo-side patch of a producer seam is a defect. Not touched by any seat: `node_modules/@mkbabb/glass-ui/**`, keyframes.js `src/**` (library half — KF.W5), `demo/components/instrument/channels/ChannelControls.vue` (OP-1's owner), `demo/components/instrument/keyframes/KeyframeCard*.vue` (CARD-UNIT), `demo/components/playback/PlaybackRibbon.vue` + TransportDock (KF.W13), value.js `src/**`, `scripts/dev/dev.sh` (unowned, either repo, NEVER touch).

---

## §Verdict Protocol (the evaluate — G1's subject)

One disposition table, per surface, each ruled **ADOPT · PARTIAL · KEEP-BESPOKE** with named reason, against the **importable** surface only:

| surface | evaluation input (not to be re-derived) |
|---|---|
| KeyframeTimeline / TimelineTrack rail | **C-15 split verdict is the input**: playhead 0..1 = mechanical-swap candidate; N-marker zoom/pan rail = NO counterpart (`geometry.d.ts` has no percent↔position map, no zoom/pan; scrubber region has zero `renderSlot`). Cite C-15; re-deriving it is the killed-claim #15 class. |
| TimelineCaret | NumberField question is KF.W6's (C-4/D·M-9); this wave rules only the contract exception (C-10) and posture (L-15) |
| TimelineHoverPreview | tooltip producer seams → BH relay; ghost redesign is bespoke regardless of verdict |
| SequenceScrubber (S-3/S-4) | KF-AV-28: EVALUATE, NOT SWAP — the `aria-hidden` charter + fling semantics may exceed the primitive; `role="slider"` would conflict with the deliberate disposition; `SliderVariant = "standard" \| "spectrum"`, no "timeline" member in 7.0.0 |
| AnimationVisualizer | KF-AV-28 intake row (D-18=C-13): re-derives ScrubberTimeline's shape (256 L vs the censused 162 L) on a subpath the demo imports 0 times |
| SpringTarget/SpringTrace idiom arms | KF-AV-10 + C-4 (SpringTarget) + C-3 (SpringTrace), sequenced WITH the verdict (§Carry P0) |

**Rider mechanics**: per-surface SWAP ⇒ the bespoke behavioral rows banked under that surface DISCHARGE with the component (each discharge named in the verdict table — silent discharge is a silent drop). KEEP-BESPOKE or PARTIAL ⇒ the rows below execute. **DISCHARGE RECEIPT FORM, adopted here (RULINGS-3 R3-10.3 — the W10-seam edge cured at its SOURCE end, which is this wave)**: naming a discharge in this wave's own prose is not enough, because the consumer is another wave's terminal table. Every SWAP-discharged row is therefore written in **KF.W10's G-2 terminal alphabet, verbatim** — **`DISCHARGED by KF.W7 SWAP verdict <surface>, <date>`** — where `<surface>` is one of the six §Verdict rows (`KeyframeTimeline / TimelineTrack rail` · `TimelineCaret` · `TimelineHoverPreview` · `SequenceScrubber` · `AnimationVisualizer` · the `SpringTarget/SpringTrace idiom arms`) and `<date>` is the verdict-table commit's date. A row this wave discharges without that exact string is **not** discharged for W10's purposes and W10 must carry it — so the omission is a silent drop at the far end, which is what R3-10.3 convicts. The verdict table is the surface that emits these receipts; G-2's consumption of them is declared at §Sequencing's → KF.W10 edge. **D-1/C-1 dissent trigger**: if the verdict rules scene-scrubbing the instrument's core promise, the whole scrub seam inherits BLOCKER weight as one cluster. **M-3/C-4 inverse trigger**: if the verdict permanently rules the scrub seam display-only, M-3/C-4 drops to MINOR.

---

## §Carry

Agglomerated by mechanism into commit families (M-25: dedupe by identity, carry cure-shape locks and sequencing edicts, route by mechanism). Every row lands with its banked id verbatim, rung, witness anchor, cure-shape lock, and family. **Anchor cleanliness is not asserted in this spec's voice (LAW B, `RULINGS-3.md`)**: the round-2 sentence *"no row in this spec is renamed, and no id below is this spec's coinage"* is a completeness claim over an enumerable set and is **struck as a self-certification**. The claim lives in the conformance artifact and **the citation is re-pointed at repair round 5 (RULINGS-5 §END KF-W7 D3, applied by SCOPE per R4-10(1)/R5-11 — every LAW-B cite in this file, swept in ONE act, not only the census one, so this file never carries two artifacts of record for one claim)** to **`docs/tranches/X/keyframes/conformance/PASS-5/KF-W7-CHECK.md` §0 axis 3 + §3 (2026-08-28)**. ⟨**The re-point is not cosmetic and the direction is again against this spec's interest.** The superseded PASS-4 cite graded **axis 3 RED** on three findings — a banked **INFO** row carried at MINOR (D2), **two of four preserved dissents** dropped from one DISSENT item (D4), one row carried under a header verb its registry home contradicts (D10) — and **all three verify CLOSED at the PASS-5 seat's independent re-sampling**: the rung restored (`### INFO` at `kf-KeyframeCard.md:95`, `KF-KC-52` at `:100`), the roster completed, the verb split. Axis 3 is now **AMBER** on two findings, both cured at THIS round: the round-4 dissent cure's own *"quoted byte-exact by command"* receipt was **a truncation with no ellipsis that dropped a FIFTH preserved dissent** (D5, cured at P4's D-12/C-12/i-2 row), and the fold-dedupe enumeration the counting rule points at was **short by one line** (D7, cured at rule (5)). **The cure written to end the truncation class committed one inside the same sentence** — which is precisely why a LAW-B cite is re-pointed each round and never re-asserted here. The two SPEC-LOCAL LABELS disclosed immediately below remain this spec's only non-banked handles, and the second of them is re-bound to its record's literal heading at this round (D8).⟩ Line anchors are origin/master anchors and re-resolve before cure (D-19, G12). Identity discipline: the corpus reuses ids across source files — every colliding id below is disambiguated by its parenthetical, exactly as banked.

**Two SPEC-LOCAL LABELS, disclosed here so the promise above is exactly true** (this repair): `PR-CAUTION` and `L-15-PROTECTED` appear in **zero** registry files — they are this spec's handles for two banked *locks*, not rows and not renamed ids. **RE-CUT TO THE STABLE-ANCHOR IDIOM at this repair round (RULINGS-3 §END (c)(4)): each label is now bound to a NAMED SECTION + BANKED ID inside its record, with the raw line number demoted to a parenthetical convenience** — the two were the file's last raw-line bindings after R2-7 retired that idiom for the `KF-W6.md` receipts *because they drifted*, and carrying two anchor idioms for one job is how the next drift goes unnoticed:

- **`PR-CAUTION` ≡ `kf-PlaybackRibbon.md` § *Adjudicated defect roster — id · severity · claim · disposition* → its **Routing law** paragraph → the clause riding the **KF-AV-28** standing rider** (*"note the C axis's own §7 caution rides with it …"*) — quoted verbatim in the header. ⟨At this seat's clock that paragraph is `:36`; the identifying predicate is the section + the rider clause, never the number.⟩
- **`L-15-PROTECTED` ≡ `kf-KeyframeTimeline.md` § *Killed-claims register — 10 rows, refutations re-executed* → entry **`K-8 · L-15`*** ⟨**HEADING CORRECTED at repair round 4 (RULINGS-4 §END KF-W7 D8)**: the round-3 binding named the section *"the adjudicator's kill register"* — a **description, not a heading**, which returns nothing: `grep -c 'kill register' kf-KeyframeTimeline.md` → **0**, while `grep -n 'Killed-claims register' kf-KeyframeTimeline.md` → **`:116 ## Killed-claims register — 10 rows, refutations re-executed`**. Its sibling label one line above (`PR-CAUTION`) used its section's literal title and resolved, so the (c)(4) stable-anchor idiom was half-applied — and a downstream sweep keyed to a description is exactly the drift the idiom retired line numbers to prevent. The banked entry itself always resolved: `grep -n 'K-8 · L-15' kf-KeyframeTimeline.md` → **`:125`**⟩ (*"REFUTED (ruled table): the stale 200 ms timer … makes the guard load-bearing; the axis's implied cleanup would ship a crash. **A kill that protects live code.**"*) — carried at P8's KF-CE-2 hard lock 3. ⟨At this seat's clock that entry is `:125`; the identifying predicate is the register entry `K-8 · L-15`.⟩

Neither label may be used as a routing id, and **any downstream sweep keyed to the registry sweeps the two ANCHORS above — record + section + banked id — not the two labels and not the two line numbers.**

### P0 · Evaluate intake (phase 1; gates the whole roster)

- **C-15** MINOR rung, MAXIMUM spec weight — THE WAVE'S EVALUATION INPUT ⟨KT "the corpus's best row" · TT re-verified · THP C-7 fold, export status corrected · kf-SequencePlayhead C-§4 correction carried · SpringTrace C-3 cites as precedent⟩ — the glass `/timeline` shadow is real for the 0..1 playhead ONLY: `dist/components/timeline/index.d.ts` is TWO lines (`GlassTimeline` + 3 types); `ContinuousRail`/`ContinuousMarkers`/`ScrubberTimeline`/`SegmentedTimeline` `.d.ts` files ship but are NOT re-exported ⟨**census enumeration completed at G1 by this repair**: the un-re-exported set is SEVEN, not four — `ContinuousTimeline.vue.d.ts` and `types.d.ts` also ship. This corrects a count, nothing else: **C-15's SPLIT VERDICT is carried exactly as banked and is NOT re-derived — killed-claim #15's lock is intact**⟩; the seam is reachable only through GlassTimeline's forwarded `popoverContent`; `geometry.d.ts` is weighted-segment math with NO percent↔position map and NO zoom/pan. **Census correction verbatim**: lane-frontend S-3 is corrected to a SPLIT VERDICT — playhead = mechanical swap candidate; N-marker zoom/pan rail = no counterpart. **Killed-claim #15 lock**: three axes independently re-derived this settled adjudication as a novelty — CITE C-15, do not re-open. Producer-side export gaps → BH relay (SS-6), never demo-side hacks. Feeds G1.
- **KF-AV-28** SPEC-INPUT — THE WAVE'S INTAKE ⟨AnimationVisualizer D-18≡C-13 · SequenceScrubber routing · PlaybackRibbon standing rider⟩ — census extension S-9: the shadow census skipped the LARGER of the two rail/ball shadows; AnimationVisualizer (256 L) re-derives glass `ScrubberTimeline`'s shape on a `/timeline` subpath the demo never imports (ships in dist; demo imports 0). **Verbatim**: both axes and both readers converge on EVALUATE, NOT SWAP. The standing supersession rider (header) and the PlaybackRibbon counter-evidence lock are this row's. Binds AnimationVisualizer's whole NO-WAVE-OWNER roster, SequenceScrubber's shape rows, PlaybackRibbon's transport packet, and the KF.W13 drag-seam packet ("resolve KF.W7 first"). Locks G1.
- **KF-AV-10** MAJOR, verdict-gated ⟨AnimationVisualizer · SpringTarget m-8 rides⟩ — the flagship rail/ball hand-rolls the demo's promoted `.progress-rail`/`.progress-ball` idiom — its SEVENTH, divergent authoring — against DESIGN.md §5: `design-idioms.css:161-187` (2px rail, `--rail-tint` 8%, `--ball-size` 36px, `--ball-glow` 35%) vs `h-1`/`bg-accent-kf/20`/`h-12 w-12`/`shadow-md`; 7 consumers; no `<style>` block to reconcile; the retint seam is LIVE (SequenceTarget `:80`, SpringTarget `:113`). **Verbatim routing**: bespoke-survives ⇒ adopt the idiom; swap ⇒ moot. **Cure caution lock — RE-ATTRIBUTED AND RE-QUOTED AT REPAIR ROUND 4 (RULINGS-4 LAW D + §END KF-W7 D3)**. ⟨**Struck**: the round-1..3 text printed *"the idiom leaves `transform` unclaimed BY DESIGN; a careless consolidation drops 28 balls out of their rails"* under the label **"(kf-EasingTarget P-2, verbatim)"**. That sentence appears **nowhere in `kf-EasingTarget.md`** — it is `kf-SpringTarget.md:69`'s parenthetical GLOSS of P-2, lifted with the closing paren replaced by a period, subject elided to *"the idiom"* and *"one tidy-up"* substituted by *"a careless consolidation"*. Same class as the PR-CAUTION paraphrase RULINGS-3 §END D8 convicted at the header — D8's cure swept one site and left its sibling four lines away, which is why LAW D now binds the label itself.⟩ **The bank, quoted byte-exact by command** — `sed -n '109p' kf-EasingTarget.md` (substrate: the value.js registry at this seat's clock): *"2. **P-2 · the `.progress-ball` idiom leaves `transform` unclaimed BY DESIGN** — `margin-top` centring on the ball, `translateY(-50%)` on the rail that nothing paints; the asymmetry is the proof of intent, and one tidy-up would drop 28 balls out of their rails."* **The vehicle by which the caution reaches THIS row, quoted byte-exact AS THE GLOSS IT IS (not as P-2)** — `sed -n '69p' kf-SpringTarget.md` → **THE RETURNED LINE, PRINTED WHOLE AT REPAIR ROUND 5** (PASS-5 check **D11**; **the ruling asked for the fragment to be MARKED, and printing whole is the cheaper discharge — it retires the class instead of labelling it**): *"- **m-8** *(rides banked KF-AV-10 — the seventh-authoring family row, which already cites SpringTarget :113; NOT a new identity)* — the T.G4 anchor triplet re-authored four times (`.spring-ball`/`.sampler-ball`/`.derby-lane-ball`/`.preset-ball` — my reads) against the idiom file's own "promoted ONCE (was authored four ways)" boast; `.derby-lane-ball` also re-declares the idiom's `margin-top`. **Cure cautioned by banked kf-EasingTarget P-2** (the idiom leaves `transform` unclaimed BY DESIGN; a careless consolidation drops 28 balls out of their rails). **KF.W7 (with KF-AV-10).**"* ⟨**Rounds 3–4 printed only the `Cure cautioned…` clause** — byte-exact within its bounds and honestly labelled *"m-8's clause"*, but LAW D(1) makes the pasted command the guarantor of the quote, and a command whose output is silently trimmed guarantees nothing. **A figure is corrected in the same breath, since this seat measured rather than inherited it**: the PASS-5 check describes the returned line as *"a full 400-word m-8 row"*; measured here, `sed -n '69p' … | wc -w` → **75 words**, `| wc -c` → **590 bytes** — small enough to print whole, which is why it now is. The check's finding is sound; its incidental figure is not, and this spec does not carry an unmeasured number even when a conformance artifact supplies it.⟩ Both quotations are labelled by source; neither is presented as the other. Bundles SpringTarget m-8 (the T.G4 anchor triplet re-authored four times: `.spring-ball`/`.sampler-ball`/`.derby-lane-ball`/`.preset-ball`; `.derby-lane-ball` also re-declares the idiom's `margin-top`).
- **SpringTarget C-4 ≡ C-4 (SpringTarget)** MINOR, verdict-gated, **route dissent recorded** ⟨SpringTarget; both readers concordant; S-9 nomination stands⟩ — the header's load-bearing rationale claims adoption of `MetricBadge`, which exists in NO resolvable glass-ui (grep: 0 files; the metric barrel exports exactly four names); the markup is hand-rolled; credibility borrowed from the real `MetricSize "xl"`; `font-roles.json:82` encodes "the MetricBadge telemetry label idiom" as house law. Cure: repo-wide prose sweep + the S-9 evaluate — adopt `/metric` or DELETE THE SENTENCE; the `.status-badge` AA mix is load-bearing and Metric has no badge affordance. B-1's death (7.0.0 pinned exact) makes the claim STRONGER, not weaker. **Route dissent (M-25 route-by-mechanism)**: the mechanism is a glass-ADOPTION evaluate, KF.W6-adjacent — carried here because the record sequences it with KF-AV-10; the record's KF.W7 routing is preserved verbatim, nothing silently re-homed. **C-4's repo-wide prose arm lands NO-WAVE-OWNER; only its `SpringTarget.vue` sentence is in bounds under the carve above, and the S-9 `/metric` evaluate is KF.W7's verdict, not its spend.** (Out-of-bounds landing declared at repair round 6, PASS-6 D8, in the form this file already uses correctly four times — R-7 → G15 · KF-KC-11 → CARD-UNIT · C-3 (SpringTrace) → KF.W5/KF.W8 · KF-AV-13 → NO-WAVE-OWNER.)
- **C·C-4 (SequenceScrubber)** MINOR, narrowed, verdict-gated ⟨SequenceScrubber⟩ — three colocated sub-units split by one rule landed on TWO contracts, divergence nowhere declared; `inject(SEQUENCE_DEMO_KEY)!` erases the missing-provider signal (runtime `TypeError` in a render function — the class the tree carries a scar comment about). **K-8 kill lock, verbatim**: the prescribed `{progress}`-prop-plus-emits cure is DEAD — *"the scrubber consumes THREE writer verbs the parent does not own; the flip relocates coupling into pass-through ceremony the KISS posture forbids."* SUP-5 SUSTAINED head-on. What survives: a one-line provider guard + declaring the asymmetry. The contract question belongs to G1's verdict; D-5/D-6/D-11's "the primitive already solves it" arms discharge or persist with it. *Dissent: reader-A's INFO preserved.*
- **KF-AV-13 / KF-AV-14 / KF-AV-24 (evaluate-input riders — DECISIONS owned here) + KF-AV-17 (a CURE-SHAPE LOCK carried, NOT a decision — it folds into KF-AV-1's cure)** MINOR / spec-input ⟨AnimationVisualizer⟩ — ⟨**HEADER VERB SPLIT at repair round 4 (RULINGS-4 §END KF-W7 D10)**: rounds 1–3 ran all four ids under one verb, *"decisions owned here"*, which makes a fold-target read as a W7-owned decision. **KF-AV-17's registry disposition, quoted by command — the transcript RE-RUN AND PRINTED WHOLE at repair round 5 (PASS-5 check D9: rounds 4's print showed 1 of the command's 2 lines)**: `grep -n 'KF-AV-17' kf-AnimationVisualizer.md` → **TWO hits**. **`:61`** (the roster row, the disposition) — *"…the resize-immune form is `translateX(calc(var(--p) * (100cqw − 100%)))`, the dashed marker's own proven expression. **→ folds into KF-AV-1's cure**."* · **`:105`** (the killed-claims entry that SUPPLIES this row's cure-shape lock) — *"12. **K-12 (reader-2) · D-3's cited cure portability** — SequenceScrubber's `translateX(calc(p*100cqw))` maps progress over the FULL container width (verified at `:34`); verbatim adoption overshoots the rail at p=1 and breaks SUP-1's three-way coherence. The resize-immune form is the dashed mark…"* ⟨the second line is quoted to its own truncation point and marked — LAW D(1); its full text is at the record⟩. **`:105` corroborates, it does not contradict** — it is the kill entry K-12 this row already cites by id, which is exactly what made the omission cheap and the class expensive. **Its fold home, stated as the ruling requires**: **KF-AV-1** — `grep -n '^- \*\*KF-AV-1 ' kf-AnimationVisualizer.md` → *"→ **NO-WAVE-OWNER** (SS-1/SS-2)"* — is NOT routed to this wave, so **no KF-AV-17 decision is taken here and none may be recorded**. What this wave carries is the LOCK's content, binding on any transform-based playhead the evaluate adopts (C-15's `left`-vs-`transform` arm); the row's body already treated it that way and the header now agrees. Nothing was lost at rounds 1–3 — the disposition was mis-labelled, not dropped, and the correction spends no cure mass⟩ — **KF-AV-13**: no press-to-seek: `:25` is the sole handler; rail + both markers `pointer-events-none`; substituting `:200` into `:108` cancels `clientX` and `ballW/2` — the on-grab projection is an exact identity, so `progressFromPointerX`'s generality only works for a press the handler placement forbids; the sibling binds the whole rail (SequenceScrubber `:28`), as does `Slider`. **The DECISION is this wave's (G1 table); the cure lands NO-WAVE-OWNER (SS-1/SS-2).** MINOR because the operative `Slider` sits directly above and owns seek. **KF-AV-14**: the "visual twin" is geometrically NOT a twin — the ball's centre travels [24px, W−24px] while the Slider's visible indicator travels [0, W], coinciding only at 50%, ~12px apart at the quartiles, 24px at each end in OPPOSITE directions; a container resize leaves the ball at a stale absolute px; perceived magnitude → SS-13, judged in the same frame as KF-AV-11's hue split. **KF-AV-17 CURE-SHAPE LOCK (K-12, sustained at the bytes)**: SequenceScrubber's `translateX(calc(p*100cqw))` is NOT portable — it maps progress over the FULL container width and at p=1 the ball overshoots the rail; the resize-immune form is `translateX(calc(var(--p) * (100cqw − 100%)))`, the dashed marker's own proven expression — **binding on any transform-based playhead the evaluate adopts** (C-15's `left`-vs-`transform` arm). **KF-AV-24**: `progressFromPointerX` is a FOURTH hand-rolled rect-ratio projector against useDragScrub's "the ONE pointer-drag scrub seam" charter — and TimelineTrack's `getPercentFromPointer` is the FIFTH; the evaluate decides whether the count goes up or down; the consolidation itself routes KF.W8. *Dissent: reader-1's MAJOR on KF-AV-13 preserved.*
- **SpringTrace C-3 ≡ C-3 (SpringTrace)** MINOR, decision-here / implementation-edge-declared, **route dissent recorded** (M-25 route-by-mechanism: a value.js LIBRARY export-surface decision, natural home KF.W8 / the export settle; the record's KF.W7 routing preserved verbatim, nothing silently re-homed) ⟨SpringTrace⟩ — the library exports the emitter but neither `resolveLinearStops` nor `sampleNormalizedSpring` (dist grep → 0); every plotting consumer re-derives the CSS rule by hand. *"The sharpest reusable row in the corpus: exporting either makes D-1 structurally unwritable downstream, and the same export cures reader-B's monotonic-clamp divergence."* Precedent-bound to C-15. **A library-side ask reaching into a frontend evaluate wave** — this half takes the DECISION into G1's precedent frame and declares the implementation edge to KF.W5/KF.W8 (§Sequencing); the row is carried, its code is not this wave's.

### P1 · The scrub seam — ONE design problem, ONE commit family (N-10 law)

The law first: **N-10** SPEC-INPUT ⟨KT RR-B, net-new⟩ — C-1's prescribed cure and C-6's defect are mutually exclusive; wiring `scrub` into the drag converts the transient hover stomp into a continuous one. *Verbatim: "the remedy is upstream of both. Booked so the W7 spec author reads the scrub seam as ONE design problem."* **Any row curing one of {C-1, C-6, L-3, L-5, M-3/C-4} in isolation is defective by this law.** Typography twin: MISSED-3 (P7). **LP-1 rider carried where it lands**: the seam cure must not introduce a write→render edge — a scrub that writes inline styles during render re-manufactures the KF-CO-1/KF-CO-8 sequencing defect in a new file.

- **C-1** MAJOR, cluster head ⟨KT head · TT C-2 fold · caret/THP cited; RR-A BLOCKER preserved in DISSENT⟩ — `scrub` returned at useTimeline.ts:117, never destructured (the 15-member destructure at :194-210 takes `scrubT`/`scrubAndCapture`) ⟨B-arm re-measured, exact: `git show origin/master:…/KeyframeTimeline.vue | grep -n scrub` → hits `78`, `83`, `197`, `202`, `224` — the bare `scrub` symbol never among them⟩; `@update:scrub-t` bare ref write :83; the complete scrubber affordance (TT :24 cursor, :27-34 six pointer/touch handlers + `@wheel`, :52-55 playhead, 10× zoom) drives nothing; the engine reaches the playhead only as a hover-restore side effect (`finally { scrub(prevT) }`, useTimelineBuild :112-115). Cure designed jointly with C-6; standalone wiring FORBIDDEN. *Dissent (RR-A BLOCKER): "if the W7 spec rules scene-scrubbing IS the instrument's core promise, the cluster head inherits blocker weight as one seam" — the demotion is a program-law ruling, not a mechanism refutation.*
- **C-6** MAJOR ⟨KT⟩ — a SECOND engine instance writes over the scene's DOM: ChannelControls :194 passes `animation.targets` by reference; timelineEngine.ts:51 spreads the same `HTMLElement`s into an independent `CSSKeyframesAnimation`; `scrub` → `interpFrames(t, true)` writes inline styles onto the scene's elements; hover restore returns them to the TIMELINE's `t`, not the scene's. Cure: own detached/cloned preview target, OR drive the scene's single engine through AnimationControlsGroup. **CONDITIONED (RULINGS-2 R2-9): the "drive the scene's single engine through AnimationControlsGroup" arm is conditioned on the OP-0 settlement — no cure routed through AnimationControlsGroup lands before the portal ruling.** A cure designed through an architecture an unbooked BLOCKER says must settle first is a cure to an architecture that may not survive; the detached/cloned-target arm is unconditioned and is the arm available if the settlement replaces the portal. Cross-ref banked kf-ChannelControls L-16 (same boundary, milder trigger). Visible displacement → SS-13 residue #6.
- **L-3** MAJOR ⟨KT⟩ — no reactive edge re-derives the animation; watch census across the 1500+-line cluster = exactly ONE (CSSPasteDialog.vue:65, a dialog flag); all 9 `rebuild` calls imperative; `animation: KeyframesAnimation<any>` is a raw class prop mutated in place (TimingFunctionPanel:145). **K-4 kill lock**: `watch([animOptions, targets], rebuild, {deep:true})` binds a non-reactive class instance and can never fire — *"the cure would compile, typecheck, and change nothing."* The failure is OWNERSHIP, not a missing watcher — stronger than the axis stated. Joint with kf-ChannelControls L-16. Blast radius → SS-13 residue #11.
- **L-5** MAJOR ⟨KT · THP L-D5 fold; citation corrections carried: emit TT:83 not :84; fn useTimelineBuild:89-117⟩ — concurrent `scrubAndCapture` calls cross scrub/restore pairs: guard keyed per `kf.id` (:221), `prevT` read per call (:96), no lock/queue/generation counter, `@mouseenter` per diamond bare and undebounced. Self-indicting cure shape: `nextFrame()` (:63-87) was hardened for EXACTLY this concurrent caller and stopped one layer short of the shared `prevT` — extend the same discipline to the restore pair. Stranded-playhead percept → SS-13 residue #5 (carried at the bank, not double-booked at THP).
- **M-3/C-4** MAJOR (rung ruled on cure-coupling) ⟨TT⟩ — pointer + touch pipelines on one element, no multi-touch guard; a pinch also scrubs (`:168-172`/`:174-186` test no `isPrimary`/`pointerType`/pointer-count; a touch contact reports `buttons === 1` so :182 passes); corrupted `scrubT` IS `snapshot()`'s default capture percent (useTimelineOps.ts:28). **Verbatim: the guard is a PRECONDITION of wiring the seam, not a follow-up — same commit as C-1's cure.** Rung stays MAJOR precisely so *"booking this MINOR would let the W7 spec wire the seam without the guard."* Axis-L's harm sentence KILLED (K-7); never restated. *Dissent: RR-B would book lower; reopening INVERTED — if this spec permanently rules the seam display-only, the rung drops to MINOR.*
- **RR-B missed-3 (TimelineTrack)** MINOR ⟨TT, net-new⟩ — no scrub-drag state: `:182`'s `event.buttons > 0` proxy, mis-commented as "pointer is captured"; no `isScrubbing` flag (the marker path has one, :137/:194) — any button-held pointer ENTERING the 48/128px full-width band scrubs, text-selection drags included. Cure: explicit scrub-drag flag (the design system's own rail gates on `dragging`) + correct the lying comment in the same commit (KF-CE-41).
- **m-9/m-11/m-12 (TimelineTrack, merged)** MINOR ⟨TT, all three re-read at the bytes⟩ — (a) in-band sentinel `0` on a null ref :160-161 (a failure scrubs to START); (b) dead `!draggingKeyframeId.value` re-test :182 (the :178 return guarantees it); (c) no `event.button`/`isPrimary` guard — right/middle-press scrubs and takes capture BEFORE the context menu opens (:168-172). Cure: ONE guard policy for the whole handler set — out-of-band failure signal, delete the dead re-test, one primary-button/primary-pointer test shared with M-3/C-4's guard; lands as the pointer-guard commit, precondition of C-1's wiring.
- **M2 ≡ RR-B-7** MAJOR ⟨KT bank · caret B-1/L-3/C-2 fold + MISS-β3 · TT M-6/m-13/C-9 fold⟩ — a pointerdown on the percent caret scrubs the playhead AND captures the pointer on a transient node: the caret display div carries `@click.stop` ONLY (:12) while its sibling input carries `@click.stop` + `@pointerdown.stop` (:25-26) and the marker carries `@pointerdown.stop` (:81) — the caret's pointerdown bubbles to `onTrackPointerDown` (:168-171), which scrubs and `setPointerCapture`s on `event.target`: a node `startEdit` immediately unmounts. **MISS-β3 cure-shape lock — RE-QUOTED BYTE-EXACT AT REPAIR ROUND 4 (RULINGS-4 LAW D + §END KF-W7 D6)**, by command: `sed -n '74p' kf-TimelineCaret.md` (substrate: the value.js registry at this seat's clock) → *"the guard the author DID write is INERT: `@click` listeners on the track = **0** (my grep; KeyframeTimeline's five `@click`s are button-local), so `:12`/`:25` `@click.stop` stop nothing — the fix REPLACES the inert guards with `@pointerdown.stop`, it does not stack a third modifier"*. ⟨**Struck**: the round-1..3 text carried this under a *"verbatim"* label having (a) replaced the colon with an em-dash, (b) stripped the anchor backticks, and (c) **dropped the scoping parenthetical `(my grep; KeyframeTimeline's five `@click`s are button-local)` with no ellipsis** — the cell that says *why* the count is 0 and where the exception lives. An executing seat reading the elided form and finding five `@click`s in `KeyframeTimeline.vue` would read the lock as refuted; the parenthetical is the reason it is not. Same class as D3 and as the round-2 PR-CAUTION.⟩ Capture on `currentElement`, never `event.target`. **K-8 kill lock**: m-13's latch escalation is UNREACHABLE (the latch target is keyed `kf.id` and Vue MOVES it, never remounts) — *"the escalation is unreachable and must not enter a spec."* Jump magnitude → SS-13.
- **D-1 (TimelineTrack)** MAJOR (axis BLOCKER killed at K-9; preserved at C-1's dissent terms) ⟨TT⟩ — scrubbing has no keyboard path anywhere in the tree (SC 2.1.1): bare `<div>`, six pointer/touch listeners + `@wheel` (:21-35), no role/tabindex/keydown; `defineExpose` publishes 10 members with `scrubT`/`scrub` ABSENT so no ancestor can drive it; the SpringTarget mirror the comment claims (:199) landed on the markers, not the rail. The one modality-specific residual harm, isolated and adopted: useTimelineOps.ts:28 + RibbonBar's argless `snapshot()` ⇒ **a keyboard user's every snapshot lands at 0%**. Design jointly with the seam; escalates back if the verdict rules scene-scrubbing the core promise. The caret's click-only-div limb folds to banked D-3. Feeds G8.

### P2 · Drag fidelity + rebuild economics

- **RR-B missed-1 (TimelineTrack)** MAJOR ⟨TT, adjudicator net-new, re-walked at the bytes⟩ — a marker drag TELEPORTS the keyframe to the pointer: `onMarkerPointerDown` :192-196 records no grab offset; `:174-178` emits the ABSOLUTE pointer percent; a legit grab starts up to ~12px off-centre ≈ 3% of the 400-512px rail at z=1 — 3× the component's own finest keyboard increment, shrinking as 1/z. Not cosmetic: useTimelineOps.ts:56-62 commits percent AND re-derives `selector`; `useRefHistory(clone:true)` banks the jump as an undo step. **Cure shape FIXED by banked class precedent GradientStopEditor C11/G5** — *"a grab is not a teleport"*: `grabDx`, capture grab offset at pointerdown, project pointer−offset (the value.js row is the severity precedent, not the identity — registry-swept: zero kf rows). Same class: KF.W13 drag-seam packet's grab-offset arm; SquareScene MISS-1 (KF.W11 arm c). Jump extent → SS-13 residue #4. Locks **G3**.
- **RR-B missed-2 (TimelineTrack)** MAJOR ⟨TT, chain re-walked end to end⟩ — every pointermove runs a full engine construction + CSS parse: `:177` per-move (~60-120/s) → KeyframeTimeline.vue:84 → useTimelineOps.ts:56-62 `rebuild()` unconditional (no early-out on zero clamped delta — a drag held at a rail end rebuilds at pointer rate for ZERO delta) → `buildAnimationFromTimeline` (its own docblock: "ASYNC because the engine constructor is HEAVY") → `new CSSKeyframesAnimation(...).fromKeyframes(...)`. No throttle, no rAF coalescing, no dirty check; held arrow auto-repeat same. Cure: throttle/rAF-coalesce AND dirty check (zero-delta must not rebuild). Scope bound verified and accepted: CPU/garbage churn, not a leak; needs ≥2 keyframes (the normal case). Emit granularity books at TT. Adjacent-NOT-identical: banked L-11 (KeyframeTimeline), N-6 — declared at the bank, re-declared here. Frame cost → SS-13 residue #5 ("drop-frame proof feeds the W7 throttle design"). Locks **G4**.
- **L-m-14 (TimelineTrack)** MINOR ⟨TT⟩ — `draggingKeyframeId` never reconciled against `sortedKeyframes`; mid-drag delete/undo dangles the id and the sink no-ops silently (useTimelineOps.ts:57 `find` → no-op, no signal). Cure: reconcile against the live collection; surface the miss. Same latch family as L-8/C-9 (KeyframeTimeline) — one identity policy.
- **ARB-1 (TimelineTrack)** INFO ⟨TT, the adjudicator's own missed row, bounds-checked⟩ — the blind drag zone: at zoom > 1 a captured drag continues past the window edge (`getPercentFromPointer` unbounded in pointer space :160-166; the clamp is domain 0-100, not window) while `overflow-x-clip` hides the marker; no auto-pan exists (pan-writer census); arrows/Home/End produce the same off-window disappearance. Cure: auto-pan past the edge, or clamp to the visible window. **Killed-claim lock**: the stronger claim ("drag is clamped to the window") was FORMED AND KILLED by the same seat — pointer overshoot extends the domain; do not re-derive. Joint with M-7 + RR-B missed-4 (joint) — the cure needs a pan writer to exist. Percept → SS-13 residue #10.

### P3 · Wheel / zoom / pan — ONE wheel policy: "prevent only on consumed events"

- **M5 ≡ RR-B-3** MAJOR ⟨KT · TT D-11/M-1/C-3 fold · caret L-11 DISTINCT⟩ — `@wheel.prevent` (TT :31) cancels page/pane scroll BEFORE the handler; a plain wheel matches NEITHER branch (ctrl/meta zoom; shift+zoomed pan); `touch-none` (:24) removes the touch escape — a 48-128px full-width scroll trap inside stacked `overflow-y-auto`. *"The affordance D-11 says is undiscoverable is ALSO a scroll trap."* Cure verbatim: **prevent only on consumed events**. Locks **G13**.
- **M-7 + RR-B missed-4 (TimelineTrack)** (joint) MINOR jointly, **escalation armed** ⟨TT; RR-A's MAJOR on M-7 in dissent⟩ — shift-wheel pan reads only `deltaY` ⟨anchor corrected at origin/master: the only delta reads are `:50` (the zoom factor) and `:59` (`event.deltaY * 0.1`) — the banked `:57-61` span is one line short; `deltaX` appears nowhere in the file⟩; pan is DISPLAY-ONLY (mini range bar: no handler, no role, no tabindex :4-18; complete writer set = zoom recentre + shift-wheel + `clampPan`; no drag-to-pan, no click-to-jump, no keyboard route). `event.deltaX || event.deltaY` is the one-token half; **the composition is the row** — *"the only pan readout is inert while the only pan gesture reads the wrong axis, and no axis assembled it."* Cure the two together. **TRIGGER verbatim**: jointly MAJOR (pan unreachable) if the SS-13 Chromium probe logs `deltaY: 0` on shift-wheel (residue #1). Joint with ARB-1.
- **D-11** MAJOR ⟨KT · TT D-m11/C-13 fold⟩ — zoom bar `v-if="zoomLevel > 1"` mounts mid-gesture (~32px row above the track, no reserved height/transition; `zoomLevel` continuous through 1.0 both directions) AND zoom has no discoverable affordance and no keyboard route (wheel + pinch only, verified across the full return surface). Cure: reserve the height (or transition the mount) + a discoverable, keyboard-reachable zoom affordance. Mount-shift magnitude → SS-13 residue #7. The `(panOffset/100)*100` clause folds to banked hygiene D-26.
- **L-11 (TimelineCaret wheel)** MINOR ⟨caret — NOT KeyframeTimeline's L-11⟩ — the open editor's wheel protection is accidental/non-local; ctrl/⌘+wheel still zooms the rail under the editor; no is-editing guard in `onWheel`; the only shield is the ancestor `@wheel.prevent` declared for zoom. Cure: explicit is-editing guard at the handler, inside the one wheel policy. Felt half → SS-13 residue #9.
- **L-m-10 (useZoomPan signature)** MINOR ⟨TT⟩ — `useZoomPan(trackEl: Ref<HTMLElement | null>)` admits null then asserts `trackEl.value!` (:5 vs :46); the sole caller supplies a genuinely-nullable `useTemplateRef`, so the signature cannot be narrowed and the `!` is a real unguarded dereference invisible to the absent type gate. Cure: guard, not assert — the signature is honest; the dereference is not. Verification blocked on OP-2.

### P4 · Geometry — re-derive before ANY cure (D-19 lock, OP-3)

**D-19 re-derive lock** (from caret K-13, binding on every row here, verbatim): every occlusion/clearance table cell shifts ~1px — `top:50%` resolves against the 46px PADDING box (the track declares `border border-border`, :24): collapsed mid 23 not 24, clearance ≈1.7px not 2.69, expanded vertex 84.21 under `scale-125`; direction unchanged. Banked D-17 carries the SAME uncorrected figures — BOTH tables re-derive before any cure.

- **M1** MAJOR ⟨KT RR-A · caret D·M-1/C-8 fold, reachability re-verified at preset 0%/100%⟩ — `overflow-x: clip` halves the celebrated 24px hit pad and cuts the caret in half at 0% and 100% — the two positions a keyframe timeline almost always occupies. The asymmetry is the proof of oversight: tick labels got three-way edge handling in the same file (:44-47); markers (`-translate-x-1/2`) and carets (`translateX(-50%)`) did not. **Bounded by S-1/S-2/S-3**: the mixed `overflow-x-clip overflow-y-visible` pair is CORRECT and deliberate — the cure is edge-aware placement matching :44-47, NOT deleting the clip. Pixel counts → SS-13 residue #4. Cure with D-m2.
- **D-17** MAJOR ⟨KT · caret D·M-2 fold + K-13 corrections⟩ — the expanded diamond occludes AND hit-steals its own caret (half-diagonals 11.31/14.14/16.97/21.21px vs `--caret-offset: 14px`; the marker carries `z-controls`, the caret nothing — the marker wins paint AND hit-test regardless of DOM order). The z-cell is a hit-steal, not only occlusion. Under the D-19 lock above. Rendered overlap → SS-13 residue #3.
- **RR-B missed-5 (TimelineTrack)** INFO ⟨TT, net-new⟩ — pointer→percent uses the BORDER box (`getBoundingClientRect` on a bordered element) while percent→pixel resolves against the PADDING box: a fixed ~1px origin / 2px span skew growing with any border change. Cure: exactly banked D-20's — one box for both maps. Rides D-19.
- **C-9 (TimelineCaret)** INFO, mechanism corrected ⟨caret — NOT THP's C-9⟩ — three-node overflow coupling, zero comments at any node: the 10px label overhangs ~1px (NOT a Card-clip truncation — that mechanism KILLED, #13); the element NEEDING `overflow-y-visible` is the `h-5` EDIT INPUT (spans ≈37-57, hanging ~11px into `.timeline-track`'s `margin-bottom: 1rem`); the ancestor chain is provisioned for the TICK labels. Cure: comment the contract at all three nodes. Namespace arm folds to banked D-28. Same padding-box family as D-17/K-13 and RR-B missed-5 — one geometry re-derivation.
- **D-14/i-1 + D-m2 (TimelineTrack geometry constants)** MINOR ⟨TT⟩ — **D-14/i-1**: `margin-top: 1.25rem` (scoped, :218-221) and `-top-5` (:45) are coupled magic numbers 174 lines apart with nothing stating the coupling (against DESIGN.md R4); home both constants the way the file already homed `--caret-offset` — the provenance that makes this an oversight. **D-m2**: tick-label edge-clamp threshold (2%/98%) under-sized for the actual 400-512px rail (half a "100%" label ≈ 5%; suffices only ≥ ~1025px). Adjudicated as banked S-8's bounding clause: the three-way handling is deliberate and the file's only edge-aware layer; **re-tune the constant, do NOT delete the mechanism**. S-8 is the provenance that makes M1 a defect rather than an unknown — cure M1 and D-m2 with the same edge-awareness.
- **D-4 (tick pitch, TimelineTrack)** MINOR (RR-A MAJOR in dissent; reopening = photographed collision) ⟨TT⟩ — tick-ladder pitch swings 9.4× and drops 5× on a 0.01 zoom increment (74.8% at z=2.99 → 30% at z=3; 39.9% at z=7.99 → 8% at z=8). Cure: the `1`-step rung is simply wrong — `2`/`5` gives 16%/40% pitch. MINOR because labels carry the information; the label-collision consequence needs a measured advance width → SS-13 residue #2.
- **D-12/C-12/i-2 (TimelineTrack, merged)** MINOR (**RR-A MAJOR in dissent, RESTORED at repair round 4**; reopening = the marker-vs-marker hit-collision photograph — TT residue row 6, folded with D-17/M1's) ⟨TT + caret contribution⟩ — ⟨**RULINGS-4 §END KF-W7 D4: the DISSENT ROSTER IS CARRIED WHOLE, all FOUR members.** `kf-TimelineTrack.md` §DISSENT item 1, quoted byte-exact by command — **THE RETURNED LINE, RE-RUN AND PRINTED WHOLE AT REPAIR ROUND 5**: `sed -n '150p' kf-TimelineTrack.md` → *"1. **RR-A's MAJOR rungs on D-4, M-7, D-12, and D-13's net arm** — preserved with their reopening conditions: residue rows 1, 2, and 6 are each a single live probe away from restoring the higher rung; the demotions here are burden-of-proof rulings, not mechanism refutations. Axis D's BLOCKER on D-1 is preserved at the banked C-1 dissent's terms (if the W7 spec rules scene-scrubbing the instrument's core promise, the whole seam inherits blocker weight as one cluster)."* Rounds 1–3 carried **D-4** (P4) and **M-7** (P3) with their dissent clauses and carried **nothing** for **D-12** or **D-13's net arm** — and two carried members make the pair read as complete, which is the harm. Reopening map, stated so the probes are not re-derived: residue **1** = M-7's `deltaY: 0` shift-wheel log · residue **2** = D-4's label-collision advance width · residue **6** = D-12's marker-vs-marker hit collision at ≤2-3% separation. **Disclosed, not smoothed — CORRECTED AT REPAIR ROUND 5 (PASS-5 check D5).** Round 4's cure printed this line at its first sentence only and disclosed *"the item names FOUR members and THREE probes"*: **a truncation with no ellipsis under a byte-exact label, committed by the cure written to end the dissent-truncation class, inside the same sentence** — and the elided sentence carries a **FIFTH preserved dissent**. The true roster: the item preserves **FIVE** dissents (RR-A's four MAJOR rungs on **D-4 · M-7 · D-12 · D-13's net arm**, plus **Axis D's BLOCKER on D-1**) and names **THREE** probes. **Where each of the five lives in this spec, so the roster is discharged by carriage and not by assertion**: D-4 at P4 · M-7 at P3 · D-12 at this row · **D-13's net arm** at its own row below — **D-13's net arm has no residue-probe reopening** (residue row 8 covers only its pointer-half reachability; its keyboard half stands regardless), so its dissent is preserved WITHOUT a restoring probe · and **Axis D's BLOCKER on D-1** at **P1's C-1 dissent** and at the `D-1 (TimelineTrack)` row's *"preserved at C-1's dissent terms"* — its terms are also this spec's §Verdict Protocol **D-1/C-1 dissent trigger**, which is the operative form of the same clause. Nothing was dropped in substance at round 4; what was dropped was the fifth member's presence **in the roster the cure certified as whole**, and two carried members are exactly what makes an incomplete roster read as complete⟩ — SC 2.5.8's spacing exception unmet; near-coincident pads make the earlier marker unhittable below ~6% separation at z=1 (arrow step 1%, :204; `moveKeyframe` clamps only); the later DOM sibling wins paint and hit at equal `z-controls`; the caret and diamond 24px spacing circles INTERSECT (caret y≈31-55 vs diamond y≈12-36). Zoom is a real escape hatch → MINOR; the 24-vs-44 arm folds to banked S-1 BOUNDED. **Citation correction adopted**: `--rail-width = clamp(25rem, 33svi, 32rem)` = 400-512px; axis-L's "800px track" and axis-C's "~600px rail" are FICTIONS. Hit test → SS-13, folded with D-17/M1's photographs — do not re-measure separately.
- **m-6 (TimelineTrack)** INFO, structure input ⟨TT; adjudicated IN; neither reader adopted⟩ — markers and carets: the same datum rendered in two sibling `v-for`s with independent geometry calls (:62, :98). *"The Tooltip wrapper forces SOME split; the duplicated geometry does not follow from it."* Structure input for the rail redesign; cure with MISS-β4.
- **MISS-β4** INFO ⟨caret RR-β⟩ — culling asymmetry inverted vs cost: `visibleTicks` windows a ~5-span loop while BOTH N-node keyframe loops (full Tooltip subtree + caret per keyframe) are never windowed, patched per pan frame then discarded by the clip. Registry sweep: no banked culling row. One geometry pass for both loops, with m-6.

### P5 · Marker state, selection, motion, the expanded settlement

- **M3** MAJOR ⟨KT RR-A, net-new⟩ — a keyboard user cannot select without mutating: `onMarkerKeydown` :203-214 computes `next` only for arrows/Home/End, discards Enter/Space (a bare div synthesizes no click), emits `select` ONLY paired with `moveKeyframe`; each inspection keystroke lands in the debounced undo history. Pointer users get non-destructive selection two ways. Cure: non-destructive keyboard selection (Enter/Space select without retiming) — the correctly-targeted residue of D-3's killed 2.1.1 arm (K-3). **Reopening exported**: THP D-3's rung reverts for the keyboard modality if this lands. Bounds superlative S-2. Locks **G8**.
- **D-8 (state contract, TimelineTrack)** MAJOR ⟨TT⟩ — selection exposes no AT-perceivable state (`aria-selected`/`aria-current`/`data-state` all absent); one attribute buys glass-ui's free `prefers-contrast: more` + forced-colors treatment (both keyed on exactly those attributes — verified verbatim in the installed accessibility.css). **Caution lock (caret killed-claim #6)**: `aria-selected` is INVALID off its seven admitted roles — *"the cure ships invalid ARIA to harvest a stylesheet; real role or the primitive"*; the marker here DOES carry `role="slider"` — pick what the role admits (`aria-current`/`data-state`). Forced-colors COVERAGE arm folds to banked D-21 (→ KF.W9); the AT-state mechanism books here. `scale-125`-survives-HCM keeps this MAJOR-not-BLOCKER. Feeds G8.
- **RR-A missed-1 (TimelineTrack)** MINOR ⟨TT, registry-swept⟩ — no accessible container (`role="group"`/`aria-label` absent :21-35) so N `role="slider"` markers are orphan sliders in a generic div; tick labels un-hidden — ~5-15 bare percent strings interleave in the AT tree. Two-attribute cure. Banked a11y rows (D-3/D-20/D-21/M3/M8/D-10) — none touches container semantics. One a11y commit with the tooltip seam (P7). Browse-mode cost → SS-13 residue #9. Feeds G9.
- **RR-A missed-2 (TimelineTrack)** MINOR ⟨TT⟩ — selecting DELETES the hover affordance (both hover signals live only on the unselected branch :70-73) — the affordance gradient is inverted; the kept bespoke `scale-125` does not animate (D-12 (KeyframeTimeline)). Cure: selected state ADDS, not replaces, the hover register — same element/commit as D-12 (KeyframeTimeline).
- **D-12 (dead marker motion)** MAJOR ⟨KT id D-12 (KeyframeTimeline) · TT M-2/D-m1 headline fold + compiler-probe sharpening — NOT THP's D-12⟩ — the diamond's selection/hover scale is un-transitioned: the scoped rule transitions `transform` (TT :224-227, verbatim UNLAYERED in the shipped chunk) while Tailwind 4 writes individual `rotate`/`scale`/`translate`; `transition-all` on the marker (:69) is DEAD CODE that reads as live. **K-5 correction lock**: D-m1's consequence is REFUTED AND INVERTED — Tailwind 4.3.0 emits `scale`, nothing ever sets `transform`; the selection scale SNAPS exactly like the fill and only `border-color` transitions; D-m1's version would send a fixer to widen the shorthand and leave the scale dead. **K-4 (TT)**: the installed PRM bracket is `*:not([data-allow-motion])` `!important` — universal; the axis's honest PRM non-claim stands. Layer-roster cell carries the witness-staleness caveat (§Sequencing, SS-13 witness refresh).
- **RR-A missed-3 (TimelineTrack)** MINOR ⟨TT⟩ — one gesture, two motion languages on expand: the 80px `h-12↔h-32` height delta EASES (`transition-all duration-fast` on the track; the scoped rule declares only margins) while markers/carets/ticks SNAP; height is a layout-animating property the repo's own rail contract rejects. Cure: one motion language; the height must not be the eased property. Teleport interplay (`:disabled` same-tick — whether the 200ms ease ever paints) → SS-13 residue #7.
- **D-10 (expanded mode, TimelineTrack)** MAJOR ⟨TT — NOT KeyframeTimeline's D-10⟩ — `expanded` grows the track 48→128px (2.67×) and adds NO content (no `v-if="expanded"` branch, no slot, no lanes; ~62% of the box is a ~1.03:1 plate; graduations stay pinned at `-top-5`, stranded ~84px from the marks they measure). Cure verbatim: **"expanded mode earns lanes/curves or dies."** ONE SETTLEMENT with RB-7 + kf-ChannelControls C-8/D-6 + kf-KeyframeTimeline D-7's expanded-mode plate strip (→ KF.W6) — see §Sequencing; OP-1's `:key` row lands FIRST.
- **RB-7** MINOR ⟨kf-RibbonBar, net-new, reader-LC MISS #2⟩ — the four Timeline commands (Snapshot/Import/Export/Add CSS — NOWHERE else per the defineExpose census :296-307; the in-pane bar owns only undo/redo/expand/clear) vanish exactly when the timeline is most visible: the ribbon dispatches on `=== 'timeline'` (:69) while the widget mounts on the wider `isTimelineVisible = selectedControl === "timeline" || isTimelineExpanded` (ChannelControls :377-379); persisted `isTimelineExpanded`, reachable in three clicks. Settles inside the D-10 expanded settlement. Same family as D-16 (rung reopens if the ribbon ever detaches from the pane).

### P6 · The caret — rounding, identity, focus, posture

- **L-2/C-3/D·M-4 (TimelineCaret)** MAJOR ⟨caret; RR-α dissent preserved: the caret remains the chief interactive integer feeder — if a wave ever establishes an integers-only invariant at the UI, MISS-β2 inherits C-1's full user-visible harm⟩ — the caret is a rounding well and its commit path dirties history when changing nothing: `Math.round` at :13/:18/:62; `commitEdit` reachable by `@blur` ALONE (:22), never compares incoming (:58-65); the model is CONTINUOUS at every producer; `moveKeyframe` allocates a fresh selector object per call (:60) + deep-keyed `useRefHistory` ⇒ **a pure READ gesture (open, blur) mutates the document's history unconditionally**. Cure verbatim: **compare-before-commit AND display at model precision** (family precedent: banked kf-MatrixEditor ME-42). **Kill lock**: C-1's float story ("29% → 28.999…996%") is KILLED — never re-derive; the surviving locus is MISS-β2 (KF.W8). ONE ROW with MISS-β1. Locks **G7**.
- **MISS-β1 (TimelineCaret)** MAJOR ⟨caret RR-β, booked by ruling⟩ — the rounding is the cluster's strongest collision manufacturer; a collision silently DROPS a keyframe from the built animation AND the exported CSS while the track keeps painting two (42.2 and 42.4 each read "42%"; open-and-blur each — a READ gesture — makes them identical; `selectorText` the same map key; **anchors re-derived at `81a56990` by the round-3 repair seat (RULINGS-3 §END D2)**: keyed **`timelineEngine.ts:38`**, merge admitted at the **`:40`** comment, merge loop `:37-47`, feeding build **`:49-51`** and the export path **`exportTimelineToCSS:60-70`** (which inherits the merge via `buildAnimationFromTimeline` at `:66`) while `state.keyframes` holds two — ⟨the round-2 spelling *"merge admitted `:42`, keyed `:40`"* had the two coordinates **TRANSPOSED** relative to the frontier AND to the disqualified historical tree; struck⟩). UI and artifact disagree with NO signal. Cure: **minimum-separation or coalesced rendering, DECIDED, not inherited** — one decision with L-2/C-3/D·M-4 and N-6 (the N-10 precedent class: in neither parent; both cited, neither re-booked). Locks **G5**.
- **N-6** MINOR ⟨KT RR-B · caret L-14 cell · THP C-5 ladder anchor⟩ — same-percent keyframes silently MERGE in the built animation and exported CSS while the track renders two diamonds (**anchors re-derived at `81a56990`, RULINGS-3 §END D2**: the merge loop `timelineEngine.ts:37-47`, keyed `:38`, the admitting comment at **`:40`** — ⟨the round-2 pair *"`:40-48` … the `:42` comment"* resolved at no ref and is struck; `:42` is the property loop⟩); arrow keys step whole percents ⇒ collisions COMMON; no dedupe/coalesce/surfacing. Decided with MISS-β1, one cure. **Ladder constraint ruled**: a display-only collision cannot out-rank an output-corrupting one without inverting the ladder. Locks **G5**.
- **D-3 (percent readout a11y)** MAJOR ⟨KT · TT D-1 caret-limb + D-12 spacing folds · caret B-2/L-12/C-11 fold + D·M-8 cell⟩ — the percent readout is a click-only `<div>` (TimelineCaret :6-13: no role/tabindex/keydown/aria; `cursor-pointer` the entire affordance) and its successor `<input>` is 40×20px at 10px type (:14-27) — SC 4.1.2 + SC 2.5.8, and the D·M-8 cell (the caret/diamond 24px spacing circles INTERSECT, caret y≈31-55 vs diamond y≈12-36) defeats the 2.5.8 spacing exception. The a11y arm of the caret redesign: cure with m-5/L-6/C-5 (TimelineCaret)'s one-shape decision + MISS-α2's focus restore, under G8. **K-3 kill lock (re-killed at the caret, #5)**: the 2.1.1 citation and the BLOCKER rung are REFUTED — retiming IS keyboard-operable on the sibling marker; the correctly-targeted 2.1.1 residue is M3. *Dissent: axis-D/-C BLOCKER rungs killed twice; never re-derived.*
- **m-5/L-6/C-5 (TimelineCaret)** MINOR ⟨caret⟩ — `keyframeId` is dead API beside identity-free emits (declared :36, bound TT:100, read nowhere; `select` :44 carries no payload so the parent closes over `kf.id`). Cure verbatim: the intended linkage (`id`/`aria-controls` to the sibling `role="slider"` marker) is ALSO MISS-α2's cure — **decide one shape, wire or delete**; one decision covers m-5, MISS-α2, part of RR-A missed-1 (TimelineTrack).
- **MISS-α2 (TimelineCaret)** MINOR ⟨caret RR-α⟩ — Enter AND Escape destroy keyboard focus: the `v-else` unmounts the focused input, nothing calls `.focus()` on a successor; the user lands on `<body>` at the top of the tab order. All three axes traced the blur triad; none noticed the focus goes nowhere. Natural restore target = the sibling slider diamond — exactly what dead `keyframeId` would address. Live half → SS-13 residue #11; the nothing-refocuses half stands statically.
- **m-1/L-10 (TimelineCaret)** MINOR, **promotion armed** ⟨caret⟩ — focus never requested; the whole exit contract (blur/Enter/Escape) hangs on `select()`'s unasserted side effect (:53-55 `nextTick(() => inputEl.value?.select())`; no `.focus()` in the file). Cure: call `.focus()` explicitly. Blink-no-op assertion KILLED (#11 — `select()` and the `setSelectionRange` family are separate spec hooks); the design objection is source-decidable and stands. **TRIGGER**: SS-13 residue #1 ("click a caret, read `document.activeElement`" — the component's single sharpest probe) decides BOTH this row's promotion (stuck-open editor = MAJOR) AND MISS-α2's reachability.
- **m-4 + MISS-α1 (TimelineCaret)** MINOR ⟨caret⟩ — no discoverable entry point in any state: the selected branch carries no `hover:` variant (:10); no title/tooltip/underline/icon, against the marker's instructional `<Tooltip>` (:75); AND the residual signal axis-D credited does not exist — `cursor-pointer` sits on the ENTIRE track (:24, re-measured), so the caret's own (:9) differentiates nothing. Cure: an affordance the caret does not share with the whole rail. No fold to kf-OrbitalDrag's cursor census (it never covered :24-vs-caret).
- **L-9/m-8 (TimelineCaret)** MINOR ⟨caret⟩ — `min`/`max` decorative; implied `step=1` marks every fractional model value `:invalid` while the app commits it anyway; no form/checkValidity in the cluster. Cure: `step="any"` or the NumberField swap (the swap itself is C-4/D·M-9 → KF.W6 — if it lands there, this row discharges with it; edge declared both ends). **Protection lock (S-3)**: the `:62` clamp is where untrusted DOM text becomes a model number — it survives any triple-clamp dedupe; do not delete it.
- **L-7 (TimelineCaret)** MINOR ⟨caret⟩ — `const props =` dangling binding, zero `props.` reads. Delete. Root cause is the banked KF-CE-16 gate hole; verification blocked on OP-2 (vue-tsc + `noUnusedLocals`, K3).
- **MISS-α4 (TimelineCaret)** MINOR ⟨caret RR-α⟩ — the file's single most load-bearing line (`:59` `if (!isEditing.value) return;`, the re-entrancy guard) is uncommented in a cluster that comments heavily — *"the exact line a dedupe wave deletes"*; challenge-C had to write the do-not-delete note in an AUDIT document instead of in the code. **KF-CE-41 law verbatim: the comment is part of the defect surface — the cure commit carries the comment.** Any dedupe row touching :58-65 carries this comment FIRST. This is the general law under which every "comment rides the cure" clause in this spec operates — the kf-SquareScene class: **comment-stated invariants are test obligations**.
- **C-10 / C-9 / L-15 / MISS-β4 (TimelineCaret INFO group)** — split by mechanism: C-9 (TimelineCaret) and MISS-β4 cure in P4's geometry pass; L-15 (TimelineCaret) below; C-10 here — INFO, **cure-shape PROHIBITION** ⟨caret⟩ — `update:percent` is `v-model`-shaped but `v-model:percent` would BYPASS the ops layer (selector re-derivation + `rebuild()`). Verbatim: **"`defineModel` here would be actively WRONG; rename the emit off the protocol."** This wave RULES the exception explicitly against the demo's `defineModel`/named-tuple law cited by D-m5: the law stands everywhere else in the cluster; this emit is the ruled exception, by mechanism (ops-layer bypass), not by taste.
- **L-15 (TimelineCaret)** INFO, ruling-dependent, UNACTIONED ⟨caret⟩ — silent swallow on unparseable input vs the cluster's toasting posture; the axis itself flags it ruling-dependent (a garbage keystroke is arguably a cancel). Verbatim: *"recorded as filed, UNACTIONED pending the W7 posture ruling."* **The W7 spec OWES a failure-posture ruling for the cluster** — delivered at G14: one ruling, its consumers enumerated there.

### P7 · Hover preview — cache, ghost, tooltip, typography collision

**MISSED-3** SPEC-INPUT (MINOR rung, binds two waves) ⟨THP RR-A, arithmetic re-derived⟩ — the corpus's two headline cures COLLIDE: `text-mono-small` makes rows 19.6→28px (`--type-small` 14→20px × leading 1.4), cutting the 96px scroller from ~9 visible rows to 4.9→3.4 — **D-4's cure materially WORSENS D-3**, and the two rows are adjacent in the same axis document. **Law (N-10's typography twin, verbatim): a repair resizes or drops `max-h-24` in the SAME COMMIT; `normal-case` always pairs `tracking-normal` (MM-29 — `--type-tracking-caps: 0.1em` rides onto the literal otherwise).** Declared from this end to KF.W6; no wave ships the regression.

- **D-4/L-4/C-5** MAJOR ⟨KT · TT M-4 fold · THP D-8/L-D1/C-3 fold + provenance correction⟩ — `previewCache` never invalidated or evicted; the tooltip pairs a LIVE percent with a PRE-EDIT capture; ids stable across every mutation ⇒ every edit makes every cached thumbnail a lie; failed captures retry forever with no feedback; removed/cleared/imported-over frames orphan base64 PNGs (monotonic ids, no `delete` anywhere); the sibling history is bounded (`capacity: 50`) — the contrast that convicts. **Merged mechanism correction (RR-A + RR-B convergent)**: `scrubAndCapture` swallows its own failures (:92-93, :110-111) so the `// KEEP:` catch (:228-230) documents unreachable conditions — the ONE throw it can catch is `canvas.toDataURL`'s cross-origin SecurityError (:226), precisely the failure the comment does not name; the comment rides the cure (KF-CE-41). **Provenance correction**: `updateKeyframeProperty` is DEAD (banked L-13); live routes = `moveKeyframe` + `onKeyframeCSSChange`. **K-7 kill lock**: the fallback is the DEFAULT outcome of failure, not dead code — retry-forever is the sharper true statement. WebGL blank-resolve arm → SS-13 residue #1 ("the single sharpest probe for this component"). Locks **G10**.
- **D-10 (KeyframeTimeline)** MAJOR ⟨KT · THP D-9/C-2 fold · TT m-16/C-8 fold — NOT TimelineTrack's D-10⟩ — capture armed on `@mouseenter` ONLY; the tooltip opens on FOCUS (reka `TooltipTrigger` `focus: handleFocus` → `onOpen`) — keyboard users get `previewSrc` permanently undefined: the 4-property ghost forever. C-2's sharpening rides as spec input: the leaf declares no emits and cannot request its own content. **Cure JOINTLY with MISSED-1 — one seam.** Compounds GHOST-PLATE (the branch the keyboard path ALWAYS receives). Feeds G9.
- **MISSED-1 (THP)** MAJOR ⟨THP RR-A, decider re-executed · RR-A missed-4 (TimelineTrack) description arm⟩ — the accessible description reka builds EXCLUDES the alt all three axes filed as their headline superlative, and announces an unpunctuated run-on frozen at first mount: `TooltipContentImpl.js:87` `computed(() => props.ariaLabel || currentElement.value?.textContent)`; (1) `textContent` excludes `<img alt>` so THP:8 contributes NOTHING; (2) block boundaries contribute no separator; (3) `.textContent` is an UNTRACKED DOM read captured once, never re-derived on the ghost→image or Capturing→settled swap; on the focus path the `<img>` never renders and the ghost has no text/role/name — an AT user's ENTIRE preview is this one frozen run-on. Per-marker arm: tabbing ten markers reads ten uppercased stylesheets. Cure verbatim: **pass a composed `ariaLabel` at the mount** (`TooltipContent` accepts it at :16, unpassed at TT:86); joint with D-10 (KeyframeTimeline) — ONE seam. Bounds superlative S-1 HARD. Per-AT utterance → SS-13 residue #9. Locks **G9**.
- **M7** MAJOR ⟨KT RR-A, falsifier RUN AND HELD · THP D-1/C-1 fold + census corrections · TT missed-4 arm⟩ — `text-admin-label` force-uppercases the declaration dump (`transform: translateX(10px)` renders `TRANSFORM: TRANSLATEX(10PX)`): case-lossy over case-sensitive CSS value grammar (`var(--myVar)`, `url()`, custom idents), on the instrument's ONLY textual ground truth — *"in a demo whose subject is CSS, the tooltip lies about the text of the CSS."* Falsifier held: ZERO CSS rules target `[data-register]` across demo/styles and the whole glass dist (it exists only as the T.D4 font-census selector contract in font-roles.json). **Cure guards**: `normal-case` pairs `tracking-normal` (MM-29); the `text-mono-small` route resizes the box (MISSED-3). Census corrections carried: 15 occurrences / 9 files (not 16); THP:20 sole `data-register="code"` co-occurrence; 8 such .vue sites; 5 `normal-case` sites (not 3). **DISSENT-4 recorded as law for this spec's execution: require the verbatim element, not the class list.** Joint a11y commit with MISSED-1 — same string. Feeds G9.
- **D-7 (THP)** MAJOR, ghost-family head ⟨THP = TimelineHoverPreview, re-scoped by adjudication⟩ — the ghost misrepresents where it matters most: (1) `opacity: "0"` survives the capture filter as a truthy string ⇒ the world's most common authored keyframe renders an INVISIBLE 64px box that still passes the guard and occupies layout; (2) `scale(0.3) ${matrix}` applies translate INSIDE the scaled frame — a 300px authored translate displaces the swatch 90px, SILENTLY CLIPPED by the tooltip's own `overflow-hidden` (a wrong-but-invisible preview, not a spill); (3) the `{}` arm survives only at authored-vars scope. Cure input adopted: value.js ships `decompose.ts` for exactly the scale-only treatment. **The ghost redesign is ONE design problem: D-7 + MISSED-4 + GHOST-PLATE + L-D8/C-4(a) — spec'd as such.** Kill locks: K-3 (L-D10's escape arm REFUTED — TooltipContent itself clips) and K-4 (`{}` UNREACHABLE on the capture path — `opacity`/`background-color` always survive; reachable only via import/typed editor). Clip extent → SS-13 residue #4. Feeds G10.
- **MISSED-4 (THP)** MINOR ⟨THP RR-A⟩ — `scale(0.3)` shrinks the ghost's own FRAME, not just its payload (border 0.3px at 30% alpha, radius 1.2px, swatch 19.2px) while the layout slot stays 64px — a FOURTH envelope on the banked M9 three; the border goes sub-pixel exactly when a near-transparent background makes it the only legibility aid. Cure verbatim: **decompose; apply scale to a WRAPPER, not the plate.** Both axes read the line; neither noticed the frame scales with the content.
- **GHOST-PLATE (THP)** MINOR ⟨THP RR-B, registry-swept, not banked⟩ — inline `backgroundColor` beats `bg-muted/30` unconditionally; a transparent target renders the whole branch as a 1px 30%-alpha hairline on a glass plate — *"the branch the keyboard path ALWAYS receives (D-10 fold), strictly worse than the opacity-0 arm because it needs no authored keyframe."* Owned by the ghost redesign; joint with D-10 (KeyframeTimeline).
- **L-D8/C-4(a) (THP)** MINOR ⟨THP⟩ — `ghostStyle` is a required prop that is a pure function of prop #1, derived in a geometry component with no other use for it, required while its two siblings are optional. Cure verbatim: derive locally; delete the prop. **The withdrawn perf arm STAYS WITHDRAWN** (the slot is `withCtx`-lazy, re-verified by both readers) — do not re-manufacture the slot-laziness hypothesis.
- **m-7/m-8 (TimelineTrack, merged)** MINOR ⟨TT⟩ — `getGhostStyle` hardcodes 4 properties against the 17-property capture set (an all-`filter` keyframe renders NO ghost); composes `scale(0.3) ${transform}` UNVALIDATED (`transform: none` drops the whole declaration; reachable — the inline editor commits untyped text). Cross-ref L-6/C-4 and C-7 — **the validation belongs at the parse boundary, not at the ghost**; the validation half sequences AFTER L-6/C-4's delegation.
- **L-D4/C-4(b) (THP)** MINOR, narrowed ⟨THP⟩ — the `// KEEP:` comment's documented fallback is FALSE on the authored-vars path: a no-ghostable-property keyframe renders NOTHING with no explanation, beside a file that renders an explicit empty state 8 lines away. Cure: a terminal `v-else` state; the comment rides the cure (KF-CE-41). Survives only at authored-vars scope (K-4); corroborates banked K-7 and supplies the mechanism that seat lacked.
- **M9** MINOR — **rung locked** ⟨KT RR-A · THP D-6 fold rung-kept + D-13 reflow arm fold⟩ — the tooltip resizes under the pointer when the capture lands (unsized 64×64 ghost swapped for `w-36 h-auto` of unknown aspect, mid-hover, no reserved box) — *"on a motion-quality instrument, the one async surface jumps."* Cure: reserve the box. **Rung lock verbatim**: *"an identity fold cannot carry a different severity than its bank without re-adjudicating the bank"* — RR-B's MAJOR re-rung REFUSED and recorded. Decode-reflow + WCAG 1.4.13 hoverability → SS-13.
- **D-2/L-D9 (THP)** MINOR (BLOCKER killed on its own falsifier; RR-A MAJOR preserved: reverts if Monaco detaches from the timeline card) ⟨THP⟩ — in-tooltip truncation, no `title`/wrap/copy, ~29 chars/row (206px ÷ ~7px/char at the 10px rung + caps tracking) vs 35-char `background-color` rows and 80+-char matrices. K-2: every value is one selection away, full-fidelity, in Monaco at 250px (:123-127/:239-244) — *"the reason this is MINOR, not the reason it is nothing."* Cure: `:title` or wrap; joint with D-4's re-rung (KF.W6) under MISSED-3's guard.
- **D-3/L-D6/C-6 (THP)** MINOR (RR-A MAJOR preserved with the M3 reopening — **THP's own dissent**) ⟨THP · D-13 net arm (TimelineTrack) folded⟩ — ⟨**TWO DISSENTS RIDE THIS ROW, kept distinct at repair round 4 (RULINGS-4 §END KF-W7 D4)**: (i) THP's, above, reopening on M3; (ii) **TimelineTrack D-13's net arm carries its OWN preserved dissent — RR-A's MAJOR, the FOURTH member of `kf-TimelineTrack.md` §DISSENT item 1** (quoted at P4's D-12 row). Its banked terms, by command — `grep -n 'D-13 net arm' kf-TimelineTrack.md` → *"the double-reflow arm folds to banked **M9**; the axis's own falsifier honesty stands (glass forwards `disableHoverableContent`, default off, so the pointer-unreachable-scroll claim was withdrawn)"* — so the net arm's dissent has **no residue-probe reopening**: the withdrawn pointer horn is what leaves it un-restorable by measurement, and the keyboard horn stands regardless. Rounds 1–3 folded the arm here and let THP's dissent stand in for TT's; the two are recorded separately now, because a fold carries the rung AND the dissent or it is a truncation⟩ — a `max-h-24 overflow-y-auto` scroller inside a portalled `role="tooltip"`, keyboard-unreachable, APG-hostile either horn; `truncate` on every row against a 17-property capture set — the tooltip's stated purpose defeated for most `transform`/`box-shadow` values; wheel does NOT dismiss, the pointer path is fully functional (the axis's falsifier honesty stands: glass forwards `disableHoverableContent`, default off). Cure: resize or drop `max-h-24` — **ONLY in the same commit as the type-rung change (MISSED-3's guard)**. Reopening: reverts for the keyboard modality if M3 lands. Chrome ≥127 focusable-scroller wording → SS-13 residue #10.
- **C-5 (THP)** MINOR (RR-A MAJOR preserved: reverts the day scroll-phase authoring becomes first-class — "aren't YET wired" is a dated promise) ⟨THP — NOT SpringTrace's C-3 nor the caret's C-5⟩ — the `KeyframeSelector` discriminant dropped at :3/:8: named scroll phases caption a percent the author never wrote (`entry 100%` and `cover 0%` both "25%"; `contain 0%`/`cover 25%` both "38%"); `selectorText` sits one call away, already consumed by timelineEngine. Cure: render the authored form, resolved percent secondary. Reachability SETTLED (adapter.ts:179-196 emits named selectors as literal text; L-D13's falsifier is dead). **Substrate lock**: VACUOUS at stale HEAD, REAL at origin/master — the row itself names the ref (G12). Feeds G6.
- **D-12 (THP) + D-15 (THP) (TimelineHoverPreview layout pair)** MINOR ⟨THP — NOT KeyframeTimeline's D-12/D-15⟩ — alignment axis changes mid-stack with no rule (`items-center` vs the full-width left code block; 144px media narrower than its 206px annotation, ~31px dead gutters); no height cap/aspect-ratio on the capture while the cheap sibling IS capped; reka publishes `--reka-tooltip-content-available-height` and glass's TooltipContent consumes none of it (**producer half → BH relay**). Cure: one layout rule; consume the published variable. Geometry FIRM (P2-DEAD decided the padding question). Gutter/tall-capture → SS-13.
- **C-9 (THP) — coverage** MINOR ⟨THP⟩ — zero test coverage: no test names the SFC or either cache prop; `timeline-undo.test.ts` exercises the same state without this surface. Verbatim: *"locks the cures above"* — the wave's born-RED mount test. Sibling of kf-SequenceScrubber L·D-12 (no spec mounts SequenceScrubber; the keyboard divergence is exactly what a mount test reds on run one) — banked N-10 class, second instance. **One coverage decision for the wave** — locks G11, which locks every other gate's runnability.
- **L-D15 / MISSED-6 / L-D14 / P2-DEAD (THP hygiene group)** INFO ⟨THP⟩ — duplicated `Math.round` at :3/:8 (three superlatives celebrate it as drift-proof single-sourcing; it is one expression written twice); no `@error` on the `<img>` (decode failure paints the broken-image glyph); `overflow-y-auto` alone coerces :20 into a scroll container on BOTH axes (CSS Overflow 3 — the mechanism behind D-3's focusable-scroller horn; cure with S-3's mixed-pair discipline the cluster already banks); `class="p-2"` at TT:86 is dead — delete the token. **P2-DEAD's producer seam (glass `cn` padding groups) is BANKED and already on the BH relay (TEP-12/MCP-31/SwatchHoverMenu family) — NOT re-filed; only the demo residue books here.**

### P8 · Import/export, dialogs, failure posture — one posture, one round-trip commit, one fold

- **L-6/C-4** MAJOR ⟨KT⟩ — a hand-rolled CSS declaration parser+emitter inside the SFC of a CSS engine's demo (:251-261 `split("\n")` + `indexOf(":")` + comment-prefix skip; `:263` `kf.vars = newVars` — WHOLE replacement, so anything the scanner misses is DELETED, not mis-parsed; emitter :239-244 the mirror). Cure: **DELEGATE** — the sibling path honours the doctrine (timelineEngine.ts:13/`:90`), making this a BYPASS, not an omission; `@mkbabb/value.js/css` exports the collectors. The delegation target must exist or be named (OP-5) — edge to KF.W2 declared both ends. Feeds G6.
- **C-7** MAJOR (posture arm HERE; crash identity FOLDS to megatranche R1, NEVER re-booked) ⟨KT, settled by the seat's OWN execution⟩ — the inline editor is a live untrusted-CSS ingress to the R1 crash shape: against installed `@mkbabb/value.js@4.0.0`, `parseCssValues("oklch()")`/`rgb()`/`hsl()`/`color()` ALL THROW (`TypeError: … reading 'replace'`); chain: :252-260 admits `oklch()` verbatim → `kf.vars` → un-awaited `rebuild()` (:264) → engine parse → THROW → swallowed at useTimelineBuild.ts:47-50 (`console.error` + `animation.value = null` — the file's ONLY non-toasting failure vs :121/:133/:137/:148/:155/:157). What books here: the SILENT posture. This wave also carries the **scope correction to GATE-VERDICT §F-2** ("no known consumer feeds the crash shape" — the census covered kf's `src/` seams, not this live typing surface) and the fuzz-entry note → KF.W2/W3. Locks **G14**.
- **D-15 (empty/failure states)** MAJOR ⟨KT id D-15 (KeyframeTimeline) · TT D-15 fold: "no empty state; six undiscoverable gestures"⟩ — empty / single-frame / rebuild-failure states UNEXPRESSED (no empty branch in the whole template; the `<2` contract surfaces only as an export-time toast; *"the one failure reachable BY TYPING is the one that doesn't toast"*). Glass-conformance tail: `/forms` publishes `invalid` + `useUserInvalidAria` on a subpath the file already imports. Whitespace-submit arm folds → banked R-17; closes-before-parse arm folds → banked R-4. In the G14 posture ruling.
- **L-11 (KeyframeTimeline)** MINOR ⟨KT — NOT the caret's L-11⟩ — `rebuild` async-typed, called as `() => void` (useTimelineOps:19); `snapshot` toasts SUCCESS before the rebuild can fail (:31-34); failures reach only `console.error`. Cure: await or type honestly; the toast follows the outcome. Adjacent-NOT-identical to RR-B missed-2 (declared at the bank). In G14.
- **N-8** MINOR ⟨KT RR-B⟩ — `captureSnapshot` drops computed `none`/`auto` (:16-21) over a set including transform/filter/box-shadow: rest snapshots asymmetric; "animate to none" UNAUTHORABLE. Cure: preserve (or record intent). **Interaction lock**: THP's K-4 kill depends on `opacity`/`background-color` ALWAYS surviving this filter — any change here re-opens the `{}` frequency question (P7). Cure with N-9 and L-14/C-16. Feeds G6.
- **N-9** MINOR ⟨KT RR-B · THP L-D12/C-10 fold⟩ — round-trip fidelity gap + dead surfaces: `importCSSToTimeline` DROPS per-stop `animation-timing-function`; `TimelineKeyframe.easing?` never written/read; `isPlaying` dead state (declared :31, returned :109, never written); `clampPan` never destructured. **Round-trip fidelity is a GATE obligation (G6), not a hygiene note.** `CSSPasteDialog.textEl` arm folds → banked R-19. One round-trip commit with N-8 + C-5 (TimelineHoverPreview).
- **N-2** MAJOR ⟨KT RR-B · TT D-m4 fold · THP C-10 sharpening⟩ — the keyframe Label input is write-only dead state (`v-model="selectedKeyframe.label"` :106 + `label?: string` timelineTypes:11 are the ONLY two occurrences; stored, deep-cloned into all 50 undo snapshots, read by NOTHING; the identity panel shows a NUMBER instead of the typed name — *"D-9 spent a MAJOR on this input's typography without noticing the control is inert"*). Cure: **wire it (hover preview + export + accessible name — D-m4's one-cure-for-both) or delete it — RULED HERE, before KF.W6 spends D-9's typography MAJOR on an inert control.** Edge declared both ends. In G15's disposition discipline.
- **R-3** MAJOR ⟨kf-CSSPasteDialog⟩ — "Add" promises merge, performs replace (`description="…merge into the timeline"` + `button-label="Add"` + FilePlus2 → `state.value.keyframes = imported` ⟨**`demo/components/instrument/timeline/composables/useTimelineBuild.ts:152`** — path-qualified at repair round 6, PASS-6 D7⟩, whole-array; `importCSSToTimeline` never reads existing state). Cure verbatim: **implement merge or tell the truth; the copy rides the same commit.** Mitigation (undo, capacity 50) adjudicates the rung, not the row; the code fix lands in the consumer/composable. One import-semantics commit with N-9 + C-5 (TimelineHoverPreview), under G6.
- **R-4 (+ KAD-9, KAD-10)** MAJOR ⟨CSSPasteDialog · KeyframesAddDialog⟩ — submit has no acknowledgement channel; a failed parse destroys the paste (open-watch wipes text on reopen; **precision correction**: the close sits inside `if (text.trim())` — conditional on non-empty text, UNCONDITIONAL on parse result); KAD-9: the only in-dialog completion signal is ONLY EVER VISIBLE ON FAILURE (success unmounts the bar mid-flight); no `aria-live`/`aria-invalid`/error node; KAD-10: async submit, no busy state, re-entry re-appends stops (glass `Button` ships `loading`/`aria-busy`/derived-`disabled`, none bound). Cure verbatim: busy/error/awaitable-close or `v-model:text` hoisting (the twin's shape), cross-referenced to KF.W2's posture registry from both ends — *"a sixth failure posture for the lane-library §7.5 registry."* R-19's `initialText` is exactly the channel R-4 needed, minted and never wired. In G14.
- **R-7 (the twin fold) ≡ KAD-F3** MAJOR ⟨both dialogs⟩ — near-twin duplication (161-line re-authoring of the 80-line shell; byte-identical `@interact-outside` guard; same `onInput` body; same flush `<pre><code>{{ text }}</code></pre>` shape; a `footer-extra` slot shaped exactly for the twin's `progressBarEl`; TWO OPPOSITE state-ownership contracts for one widget). **Fold-direction constraints pinned at the bank, verbatim**: adopt THIS file's (CSSPasteDialog's) a11y composition (siblings under DialogContent) AND the twin's text-hoisting contract; genuine deltas are exactly FOUR (DialogTrigger, highlighting, Tab-insert, reformat) — none requires re-deciding footer/type-rungs/heading-nesting; the `defineModel` half has no defence (six-site house idiom). The fold experiment OWNS L-4's open falsifier (whether KAD folds across the `footer-extra` seam without losing `<pre>` identity for `useCodeHighlight`). Consumer end of the fold **TARGET** = the two `<CSSPasteDialog>` mounts at `KeyframeTimeline.vue:135`/`:145` (L-16/L-17). **CONSUMER END OF THE FOLD *SOURCE* — LAW A IMPORT-GRAPH CENSUS (RULINGS-3 LAW A), executed by this seat at `origin/master 81a56990`, commands + outputs, sourced from no CARRY row, no bank paraphrase and no prior pass; a fold that deletes or repoints `KeyframesAddDialog.vue` is a delete/repoint act and its census may not be inherited**: ⟨**(1) SPECIFIER census** over every spelling that can reach `demo/components/instrument/keyframes/components/KeyframesAddDialog.vue`, including every alias root read from `vite.config.ts`/`tsconfig.json` (`@components` is the only one that can reach it): `git grep -nF 'components/KeyframesAddDialog' origin/master -- demo/ test/ src/ scripts/` → **exactly ONE live import** — `demo/components/instrument/keyframes/KeyframesEditor.vue:118` `import KeyframesAddDialog from "./components/KeyframesAddDialog.vue";`; `git grep -nF '@components/instrument/keyframes/components/KeyframesAddDialog' …` → **0**; `git grep -nF 'keyframes/components/KeyframesAddDialog' …` → **0**. **(2) SYMBOL census**: `git grep -nF 'KeyframesAddDialog' origin/master -- .` → the import above, its single mount `KeyframesEditor.vue:75`, one prose mention in a docblock (`keyframes/composables/useToolbarKeyboard.ts:24` — non-import context), and ~20 further hits under `docs/tranches/**` (D · E · G · J · L · T · U lanes) recorded as **non-import context, never counted as consumers**. **(3) CONSUMER SET = exactly ONE module: `demo/components/instrument/keyframes/KeyframesEditor.vue`** (import `:118`, mount `:75`).⟩ **The consequence, declared rather than assumed**: `KeyframesEditor.vue` is **OUTSIDE §Bounds** — it is neither a timeline-cluster file nor a dialog shell — so **the fold's repoint site is not this wave's to write**. G15 is a **ruling** gate, and this is why: the wave rules the fold direction and answers L-4's falsifier; **any landing that deletes or re-points `KeyframesAddDialog.vue` must carry `KeyframesEditor.vue:75`/`:118` in the SAME atomic act**, and that act is declared to the KFED-UNIT owner **at the only end that exists at this clock** (P8's KF-KC-11 precedent, re-based the same way). ⟨**"FROM BOTH ENDS" STRUCK at repair round 4 (RULINGS-4 §END KF-W7 D9)**: **KFED-UNIT is NO-WAVE-OWNER** (`grep -n 'KFED-UNIT' kf-KeyframesEditor.md` → the packet's terminal disposition) and has **no spec file** — `ls docs/tranches/X/keyframes/waves/` returns eleven `KF-W0..KF-W10` documents and no KFED/EDITOR document — so there is no second end to declare to, and a reciprocity nothing can verify is a promise, not a receipt. **The second end, named as the ruling directs and cited by STABLE ANCHOR ALONE**: `KF-W10.md` **§6.D · SUCCESSOR-FORMATION REGISTER → the KF.W12 row** (Authoring-Surface Repair; its inbound cargo includes CARD-UNIT · KFED-UNIT · EDITOR-UNIT), minted by RULINGS-4 R4-8 with the verb **MINTED-UNAUTHORED — AUTHORING = the SS-1/SS-2 authoring block**. **LAW C(3) governs the form**: KF.W10 writes LAST of the per-wave seats this round, so this citation carries the §-heading and row identity only — **no quoted prose, no line numbers** — and the RECONCILE seat verifies it against W10's post-round-4 bytes. **What is promised here: nothing.** The constraint is stated and addressed to a register row; the atomic-act requirement binds whatever seat lands the fold, and the finding itself (the fold's SOURCE census, the out-of-bounds consequence, G15 as a *ruling* gate) is unchanged.⟩ A fold ruled here and landed without its one consumer is an unbuildable tree. ⟨Round 2 named only the TARGET's two mounts and never censused the SOURCE — the precise shape LAW A was written against after KF.W6's F-5.⟩ **KAD's `innerHTML` BLOCKER is KF.W5's, front-loaded — this wave does not gate on it and must not wait on the fold for it.** Locks **G15**.
- **KAD-6** MAJOR ⟨KAD⟩ — `DialogDescription` nested inside `DialogTitle` (`<h2><h3>…</h3><p>…</p></h2>`; `DialogHeader` unused though in the same barrel, used correctly at KeyboardShortcutsModal.vue:4-8) — content-model violation + accessible-name pollution via `aria-labelledby`. Discharges with R-7's fold, or stands alone at KF.W6 if the fold is declined — declared both ends. In G15.
- **KAD-13** MAJOR ⟨KAD⟩ — the `format` prop's JSDoc declares a pure formatter; the wiring secretly WRITES the parent model; `reformat()` never emits `update:text`; TRIPLE-WRITE — the `:149` write is DEAD. Cure verbatim: `emit("update:text", formatted)` + a genuinely pure formatter ("which the triple-write makes cheap") — **this IS R-7's text-hoisting constraint**; the prose correction rides the commit (KF-CE-41). In G15.
- **R-17 / R-19 / R-21 / R-22 / R-23 / KAD-21 (dialog MINOR group)** MINOR — whitespace-only submit is a silent dead click (R-17, folds KT D-15's whitespace arm); three dead configuration surfaces (`initialText`/`preClass`/`footer-extra`) + `defineExpose({ textEl })` publishes an internal DOM handle, structurally null while closed (R-19); `preClass` REPLACES instead of `cn()`-merging — the idiom is exported from the barrel the file already imports (R-21, "dies in the fold or the swap"); unguarded `EventTarget → Element` casts charged asymmetrically — either both are MINOR or neither (R-22); no keyboard submit path in all 80 lines — no Mod+Enter, a keyboard user Tabs out of a multi-line editor to commit, in a repo that treats shortcuts as first-class (R-23); the description restates the title, capitalization disagrees with itself (KAD-21). **Discharge discipline (a G15 condition)**: most die in R-7's fold or KF.W6's S-9 Textarea swap ("kills R-1/R-6/R-8/R-9/R-12/R-14/R-20 in one move") — **the execution seat STATES which rows the KF.W6 swap already discharges BEFORE spending a cure**.
- **KF-CE-2 (parent half)** BLOCKER ⟨kf-CSSCodeEditor, banked⟩ — a stale in-flight 200ms debounced emit lands on the WRONG keyframe after selection change; `kf.vars` FULL-REPLACED with the previous keyframe's text — silent data loss (helpers.ts:15-24 no cancel handle, closure-private timer; the guarded `setValue` fires `onDidChangeModelContent` synchronously so the armed timer is neither cancelled nor re-armed; KeyframeTimeline binds NO `:key` :123-127; `:246-249` resolves the target from the LIVE ref at emit time; `:263` assigns, not merges). Parent-half cure: **emit-time target resolution via arm-time id capture**. **THREE HARD LOCKS**: (1) the two banked BLOCKERs that destroy this component's data (this stale emit; ChannelControls' `:key` remount) settle BY OR BEFORE this wave (OP-1); (2) **cure-collision resolved by this spec (N-10 class): the bank's "keyed remount" option is REJECTED — it is the exact mechanism kf-ChannelControls L-2/C-2 convicts as a data-loss BLOCKER; arm-time id capture is the ruled shape** (a keyed remount would need written justification against L-2/C-2 — none is offered); (3) **`L-15-PROTECTED` (the do-not-delete lock — a SPEC-LOCAL LABEL, not a banked id; §Carry preamble)** ≡ the protection lock banked at `kf-KeyframeTimeline.md` § *Killed-claims register — 10 rows, refutations re-executed*, entry **`K-8 · L-15`** ⟨stable anchor per RULINGS-3 §END (c)(4), **the section name corrected to the record's LITERAL heading at repair round 4 (RULINGS-4 §END KF-W7 D8) — the description *"the adjudicator's kill register"* returns 0 by grep and is struck at both binding sites**; `:125` at this seat's clock is a convenience, not the binding⟩, a kill that PROTECTS live code: the `:248-249` null-check guard is LOAD-BEARING against this exact timer; any dedupe that removes it ships a crash; the guard gains its comment (KF-CE-41) and binds L-16/L-17's descriptor de-duplication. Child half = EDITOR-UNIT (cancel-on-external-write + cancel-on-unmount, one `replaceContent()` seam; §Sequencing); neither half alone closes the loss. Feeds G2/OP-1.
- **KF-KC-11 (undo-trail unification) + KF-KC-52** MAJOR arm (KC-11) / **INFO** (KC-52) ⟨kf-KeyframeCard⟩ ⟨**RUNG CORRECTED at repair round 4 (RULINGS-4 §END KF-W7 D2)**: rounds 1–3 carried KC-52 at **MINOR** — a silent PROMOTION off its bank, on the one row from that record whose rung this spec restates, and forbidden by the lock this same spec carries at P7's M9 (*"an identity fold cannot carry a different severity than its bank without re-adjudicating the bank"*). Re-measured by this seat at the registry bytes: `grep -n '^### ' kf-KeyframeCard.md` → `:38 ### BLOCKER` · `:47 ### MAJOR` · `:71 ### MINOR` · **`:95 ### INFO`**; `grep -n 'KF-KC-52' kf-KeyframeCard.md` → **`:100`** — inside the INFO block. The bank's rung is **INFO**, restored here; no re-adjudication is offered and none is implied. The row's cure mass is unchanged: the docblock correction rides INSIDE the undo-authority ruling either way (KF-CE-41), so the rung governs precedence, not scope⟩ — the card's destructive X has no dialog and no undo, and the demo's only undo (`useRefHistory` at useTimeline.ts:77-88, Mod+Z wired) is bound to timeline state, not `templateFrames` — the card's delete sits OUTSIDE the app's only undo trail; KF-KC-52: useTimeline's docblock cites a "100ms debounce" keystone that is actually 1000ms (the 100ms is parsing.ts:62's reprojection throttle). **This wave rules the undo authority ONCE**: the same `useRefHistory` instance that L-2/C-3/D·M-4 dirties on a read, S-4 celebrates, RR-B missed-1 (TimelineTrack) banks teleports into, and L-2/C-2 destroys wholesale — **one authority, four consumers, one ruling**. The docblock correction rides INSIDE the ruling (KF-CE-41). **Homing (this spec's act)**: the `canRemove`/confirm-or-undo DECISION is homed here as a consequence of the trail ruling; its cure site (`KeyframeCard.vue`) is outside §Bounds and is declared to the CARD-UNIT owner **at the only end that exists at this clock** ⟨**"from both ends" struck here too — RULINGS-4 §END KF-W7 D9 applied BY SCOPE, not by named instance (R4-10(1)): the ruling names KFED-UNIT, but CARD-UNIT is the identical shape one row away, and a repair that cures the named instance and not the stated scope leaves the law green over its own counterexample.** CARD-UNIT is likewise a NO-WAVE-OWNER packet with no spec file; its second end is `KF-W10.md` **§6.D · SUCCESSOR-FORMATION REGISTER → the KF.W12 row** (CARD-UNIT is named in that row's inbound cargo), cited by stable anchor alone under LAW C(3) because W10 writes last this round⟩.

### P9 · Contract & dead-surface hygiene

- **L-8/C-9 / L-11 / L-16-L-17 (KeyframeTimeline hygiene group)** — split: L-11 (KeyframeTimeline) rides P8's posture family; L-16/L-17 below; L-8/C-9 here — MINOR ⟨KT — NOT the caret's or THP's C-9⟩ — two divergent removals nine lines apart: the ✕ (:117, `!` assertion) dangles `selectedKeyframeId`; `removeSelectedKeyframe` (:289-294) clears it; undo-resurrection re-opens the editor on the restored id. One-token cure: `@click="removeSelectedKeyframe()"`, deleting the `!`. **K-1 kill lock**: both manufactured harms ("press Delete twice"/"no memory of selecting") REFUTED at :289-294/:48-54 — never carried. Same latch family as L-m-14 — one identity policy.
- **L-12/C-8 / L-13 / L-14/C-16 / N-9 (dead-surface group)** — split: N-9 rides P8's round-trip commit; L-13 / L-14/C-16 in the pair row below; L-12/C-8 here — MINOR ⟨KT⟩ — 3 of 10 exposed members dead (`canUndo`/`canRedo`/`selectedKeyframeId` — repo grep: 0 consumers); the 7-verb command contract crosses three seams as `any` (RibbonBar:140, ControlsPaneWrapper:196, useControlsKeyboardShortcuts:22), erased at hop ONE by the `*.vue` shim (env.d.ts:3-7). Cure: expose the verbs or delete them — the same decision kf-SequencePlayhead's transport packet books. Typing UNVERIFIABLE until OP-2 — cross-edge declared.
- **L-13 / L-14/C-16 (dead-surface pair)** MINOR ⟨KT⟩ — L-13: 22 returned members, 7 never taken; `loadPreset` dead (0 callers) AND sole consumer of all 33 lines of `flattenVars.ts` (deletion rides). L-14/C-16: `captureNonDefaultSnapshot` — the MORE careful strategy — unexported and uncalled (snapshotCapture:34), invisible to `npm run check`. Adopt or delete, both. `noUnusedLocals` rides KF.W4/K3 — the gate that would have caught these is not this wave's.
- **L-7 (TimelineCaret) + C-10/C-11/D-m6/m-1/D-m5 (TimelineTrack, merged)** — the cluster contract-hygiene group, MINOR ⟨caret + TT⟩ — dead prop `keyframeId` (sole occurrence its own declaration); redundant derived `position` beside `percent` (the zoom transform lives in the parent, neither provided nor passed); the public prop type drags `KeyframeSelector` from value.js for fields the subtree never reads; `const props` bound and never read in two of three cluster SFCs; hand-written `update:*` emits + call-signature overloads vs the demo's stated `defineModel`/named-tuple law (:129-134). Cure here; ENFORCEMENT at KF.W4 by fold (unenforceable by construction per the banked gate hole). **Collision with C-10 (TimelineCaret) ruled in P6: the `defineModel` law stands for this group; `update:percent` is the one ruled exception.**
- **C-17 / L-18-C-11 / L-20-L-21 / C-13 / D-25-D-26-D-28 / N-12 (KeyframeTimeline INFO group)** INFO ⟨KT; the D-25-D-26-D-28 hygiene cells sit with the coherence row above⟩ — C-17: `targets` plural, the preview reads `targets.value[0]` only, unlabeled as a sample. C-13: the **value.js D-23 blast-radius record** (`TimelineKeyframe.selector` typed by transitive value.js; `clamp` from `value.js/math` at five sites — legal; recorded, edge declared to the value.js lane, nothing cured). L-18/C-11: dual Tooltip specifiers — consistency clause only (K-10 killed the bundle-graph tail). L-20: no unmount guard on an in-flight capture (zero lifecycle hooks; `finally` scrubs a detached target, writes into a disposed reactive) — convicted as a regression against the in-tree standard (CSSCodeEditor's banked unmount-race discipline): adopt that discipline. L-21: the keyframe sort computed twice. **N-12 PROHIBITION verbatim: no future wave preserves `markRaw` as load-bearing while changing the ref kind** (corrects superlative S-7: the protection is real but SINGLE-mechanism).
- **L-16/L-17** MINOR ⟨KT⟩ — four copy-pasted Tooltip/Button blocks (~62 of 154 template lines) + two CSSPasteDialog mounts differing in four strings (:11-72, :135-152). **Cure-shape lock verbatim (`feedback_kiss_no_contrivance`): an in-file `v-for` over a descriptor, NO new component.** Sequenced AFTER R-7's fold settles (the mounts are its consumer end). In G15.
- **D-13 / D-16 / D-22 (KeyframeTimeline coherence group)** + D-25-D-26-D-28 hygiene cells, MINOR/INFO ⟨KT; RR-A/axis-D MAJORs on D-16/D-13 preserved with the co-visibility reopening⟩ — D-13: literal `Undo (Mod+Z)`/`Mod+Shift+Z` tooltip strings vs the modal rendering the SAME registry rows through glass `formatComboParts`, already imported at `shell/KeyboardShortcutsModal.vue:50` (path correction carried) — one-line cure. D-16: create-vs-destroy verb split survives on emphasis/shape/labelling alone (K-5 killed the height column: both rows floor at 36px); rung reopens if the ribbon detaches from the pane; settles in the D-10/RB-7 family. D-22: three verbs for one operation (Clear/Remove/Delete) + `Capturing...` with no live region (≡ banked D-22; THP D-13's live-region arm folds here; checks whether KF.W5 already dissolved `Capturing...` — KF-SKEL-16). Hygiene: broken wrapper indentation (:2-4/:131-153); identity arithmetic; `--caret-offset` unprefixed `:root` singleton with one consumer.
- **RR-B missed-5 + m-6 + m-3 + L-m-4 + L-m-5 + i-3 (TimelineTrack INFO group)** — the composable four land here; RR-B missed-5 (TimelineTrack) and m-6 cure in P4's geometry pass — INFO ⟨TT⟩ — two clamp idioms in one 110-line composable that imports `clamp` on line 3 (:19/:51 vs :86-89); `percentToPosition(tick)` ×3 per tick per render at pointermove rate; the affine remap hand-rolled one file from the module exporting it (`scale(pct, pan, pan+100/z, 0, 100) = (pct−pan)·z`, algebra identical) — **KISS cuts AGAINST the mechanical swap** ("the two-line hand-roll is arguably more legible than a five-argument `scale()` call"); indentation fingerprint at useZoomPan.ts:51. **K-6 kill lock**: L-m-5's ~102-tick magnitude is ~7× wrong (real set ≤ ~15 at any zoom ≥ 3; 5 at z=1); churn shape survives at INFO only. Under-consumption record; no formatter reaches the demo (banked family). Fleet-register note carried: RR-B's confirmation repeated the ~102 figure while citing the lines that refute it — a citation-inheritance event.

### P10 · Substrate (constraint on this and every KF wave)

- **MISS-α5 ≡ RR-β K-BASE** MAJOR (PROCESS) ⟨caret merged net-new pair · TT K-2 · THP DISSENT-4 · SequenceScrubber K-1⟩ — the tree every document calls "8281638c" is a HYBRID of two divergent lines: local master `8281638c` vs `origin/master` `81a56990`, merge-base `a59d3a22`, NEITHER an ancestor of the other. At HEAD: `keyframeSelector.ts` does not exist, `timelineTypes.ts` has no `selector` field, `moveKeyframe` has no `percentSelector` — C-1's chain, L-2's selector limb, D·M-4's selector cell UNEVALUABLE at the anchor all three axes declare. Manifests differ per ref: HEAD `optionalDependencies: glass-ui 6.0.0` (`npm ci` installs optionals ⇒ SILENT DOWNGRADE) · origin/master `devDependencies: 7.0.0` EXACT = installed · worktree declares NONE; value.js HEAD `^3.1.0` / origin+worktree `4.0.0` = installed. **Verbatim: any spec consuming this registry STATES WHICH REF IT CURES AGAINST** — done, header. Also CORRECTS the banked kf-KeyframeTimeline header's "working tree = the audited tree" equation (seven of that cluster's files are ` M` vs HEAD). Anchor-safety map: TimelineTrack.vue, useZoomPan.ts, TimelineCaret.vue, layout.css, design-idioms.css byte-identical at all three refs; the composable/engine tier is origin-side ONLY. **F-1 re-rule request carried**: census F-1 and every "phantom dependency" corroboration re-derives PER-BASELINE, never re-books — at origin/master manifest and disk AGREE; at HEAD the true residue is 6.0.0-declared vs 7.0.0-installed DRIFT; only the worktree is phantom; the banked kf-AnimationVisualizer cell "glass-ui in package.json/package-lock.json = 0/0 (F-1)" is corrected to worktree-scope. Sixth one-baseline-short event on the program's ledger. Locks **G12**.

---

## §Gates

Fifteen gates, born-RED at authoring, none stamped. **Gate-witness soundness is not asserted in this spec's voice (LAW B, `RULINGS-3.md`)** — the round-2 blanket *"each with its named live witness"* was falsified at pass 3 by two of its own members. The claim lives in the conformance artifact, **re-pointed at repair round 5 (RULINGS-5 §END KF-W7 D3, by scope)**: **`docs/tranches/X/keyframes/conformance/PASS-5/KF-W7-CHECK.md` §0 axis 4 + §2 (2026-08-28)** re-executed its witnesses read-only at `81a56990` and found **11 of 11 sampled §Bounds line counts EXACT · 9 of 9 sampled gate witnesses reproducing, several to the line** (G5's five anchors, G6's four newly path-qualified leaves, G11's 0/13 split, G13's two delta reads, G14's six toast lines, OP-4 exact), **all 15 gates born-RED, none stamped, every one with reachable green**, and **LAW A exemplary** — both delete/repoint censuses reproducing **command-for-command** from an independent execution, *"the best-founded material in the file"*. ⟨**The scope word is stated because the two artifacts differ in it, and the weaker word is the honest one**: PASS-4 re-executed **31 of 31** frontier anchors exhaustively; PASS-5 re-executed a **sample** and found it exact. This cite claims what the freshest artifact measured — a sample — and does not inherit the older artifact's exhaustive count as though this round had re-earned it.⟩ Its single axis-4 finding is **a universal closure claim asserted in this spec's own voice and falsified by this spec's own receipt** (**D6**), cured at the R4-10(1) scope-receipt block below. ⟨Dated log: the superseded PASS-3 cite recorded **twelve EXACT** and **three defective** — G5's anchors resolving at no ref, G10's `<cache module>` placeholder, G11's plugin-vue cost model false at the frontier — all three cured at repair round 3 with their re-derivations pasted at the gate, and **all three verified CLOSED by the PASS-4 seat's independent re-execution**.⟩ A pass-5 seat re-measures rather than inheriting this sentence. Witnesses re-verify at wave-open (D-19).

**Runnability — ONE reading** (this repair replaces three incompatible statements of the same precondition; the preamble previously over-claimed "no gate is runnable" against the narrower and correct scopes at G11 and §Sequencing's **G12-ref-pin + G11-mounts** hard-ordering item — item 3 since RULINGS-2 R2-9 inserted OP-0's settlement at the head; the item's SUBJECT is the anchor, not its number): **G12 pins the ref and is stamped first — it conditions every other gate's anchors, and G12 itself is discharged by the opening commit, not by a mount.** **G11's mounts are the runnability precondition of the ten behavioural gates — G2–G10 and G13** (each asserts runtime behaviour of a mounted surface). **G1, G14 and G15 are documentary/ruling gates: they need no mount and are runnable as soon as G12 is stamped** — G1 is the verdict table (its subject is the installed export surface, read with `ls`/`wc`), G14 is the one-posture ruling, G15 is the fold-or-decline ruling. That hard-ordering item's scope is COMMITS — no *cure* commit precedes G12 + G11 — which is not the same claim and no longer reads as one. **And it is now itself preceded**: OP-0's portal settlement and OP-1's `:key` settlement open the ordering ahead of the ref-pin, because a settlement is a ruling, not a commit.

**G1 — The evaluate verdict exists, per-surface, against the real export surface.** RED.
Witness: `wc -l node_modules/@mkbabb/glass-ui/dist/components/timeline/index.d.ts` → 2 lines (`GlassTimeline` + 3 types: `TimelineSegment` / `TimelineSegmentGradient` / `TimelineSegmentState`); `ls …/dist/components/timeline/` → **nine files, enumeration completed by this repair** (the earlier five-item list was not exhaustive): `GlassTimeline.vue.d.ts` (the ONE re-exported component) + `index.d.ts`, against **seven shipped-but-NOT-re-exported**: `ContinuousRail.vue.d.ts` · `ContinuousMarkers.vue.d.ts` · **`ContinuousTimeline.vue.d.ts`** · `ScrubberTimeline.vue.d.ts` · `SegmentedTimeline.vue.d.ts` · `geometry.d.ts` · **`types.d.ts`** (which supplies the three re-exported type names — the type surface is reachable, the component surface is not); `grep -n "SliderVariant" …/dist/components/slider/types.d.ts` → `"standard" | "spectrum"`, no "timeline"; demo imports of `/timeline` = 0 (census §3.1).
Assertion: the §Verdict table exists — KeyframeTimeline (312 L) / TimelineTrack (246 L) / TimelineCaret (70 L) / TimelineHoverPreview (38 L) / SequenceScrubber (162 L) / AnimationVisualizer (256 L) — each ruled ADOPT · PARTIAL · KEEP-BESPOKE with named reason against the IMPORTABLE surface only; every SWAP-discharged row named. RED today: no table exists and 5 of 6 counterparts are not importable. Carries: C-15 (input, never re-derived), KF-AV-28, KF-AV-10's routing, C-4 (SpringTarget), C·C-4 (SequenceScrubber), KF-AV-13's decision, the PlaybackRibbon counter-evidence lock (PR-CAUTION); export gaps → BH relay.

**G2 — Scrub seam wired, guarded, single-engine — one commit.** RED.
Witness: `git show origin/master:…/KeyframeTimeline.vue | grep -n scrub` → hits 78/83/197/202/224, the bare `scrub` symbol among NONE of them (`:197 scrubT`, `:202 scrubAndCapture`; `scrub` absent from the :194-210 destructure); `:83` bare ref write; `grep -n "isPrimary\|pointerType" …/components/TimelineTrack.vue` → 0 in :168-186; `timelineEngine.ts:51` spreads scene `HTMLElement`s into a second engine.
Assertion: non-primary pointerdown ⇒ NO scrub; pinch ⇒ NO scrub; the driven engine is provably NOT the scene's instance (or the scene's single engine via AnimationControlsGroup — **that arm CONDITIONED on OP-0's portal settlement, RULINGS-2 R2-9: no cure routed through AnimationControlsGroup lands before the portal ruling, so this gate cannot be discharged on the AnimationControlsGroup arm while OP-0 is UNSETTLED**). Carries: N-10, C-1, C-6, L-3, L-5, M-3/C-4, RR-B missed-3 (TimelineTrack), m-9/m-11/m-12 (TimelineTrack, merged), M2 ≡ RR-B-7, D-1 (TimelineTrack), KF-CE-2 (parent half)'s ordering locks, **OP-0's condition** ⟨identity discipline: **D-1 (TimelineTrack)** — the no-keyboard-path row carried at P1 — is NOT **kf-AnimationControlsGroup D-1**, the portal BLOCKER at OP-0; the corpus reuses the id across source files and both are disambiguated by their parenthetical everywhere below⟩.

**G3 — A grab is not a teleport.** RED.
Witness: `TimelineTrack.vue:192-196` records no offset; `:174-178` emits the absolute pointer percent; grabbable region = the 24px pad ⇒ ~12px ≈ 3% error at z=1 on the 400-512px rail.
Assertion: grab at +11px off-centre, z=1, 400px rail: first-pointermove emitted percent == pre-grab percent; `useRefHistory` banks ZERO entries for the grab alone. Carries: RR-B missed-1 (TimelineTrack) (`grabDx` per GradientStopEditor C11/G5), L-m-14, L-8/C-9 (KeyframeTimeline)'s shared identity policy.

**G4 — Rebuild rate bounded and dirty-checked.** RED.
Witness: `useTimelineOps.ts:56-62` `rebuild()` unconditional, no early-out even on zero clamped delta; `buildAnimationFromTimeline` docblock: "ASYNC because the engine constructor is HEAVY."
Assertion: a synthesized 60-move drag ⇒ ≤1 `buildAnimationFromTimeline` per animation frame; a rail-end hold (zero clamped delta) ⇒ ZERO. Carries: RR-B missed-2 (TimelineTrack); frame-cost profile → SS-13.

**G5 — UI and artifact cannot disagree about keyframe count.** RED.
Witness — **RE-DERIVED AT THE FRONTIER BY THIS REPAIR SEAT (RULINGS-3 §END D2; the round-2 anchors resolved at NO ref and are struck)**: `git show origin/master:…/utils/timelineEngine.ts | nl -ba` → the merge loop spans **`:37-47`**; the keying is **`:38`** `const key = selectorText(kf.selector);`; the merge-admitting comment is **`:40`** `// Merge vars into keyframe (multiple keyframes at same percent get merged)`; **`:42`** is the property loop `for (const [prop, value] of Object.entries(kf.vars))`, not the comment; the build call is **`:49-51`** `new CSSKeyframesAnimation(options, ...targets).fromKeyframes(keyframesMap …)` with `anim.name` at `:52`; the export path is **`exportTimelineToCSS` `:60-70`**, which INHERITS the merge by calling `buildAnimationFromTimeline` at `:66` rather than re-performing it. Both render loops still paint two — `TimelineTrack.vue:62-94` (the Tooltip/marker `v-for`) and `:97-106` (the `TimelineCaret` `v-for`), **both re-executed and EXACT at `81a56990`**. The caret quantizes every commit to 101 slots via `@blur` alone. ⟨**Struck, with the historical form stated so the transposition cannot re-enter**: the round-2 wording *"`:40-48` keys … merge admitted in the `:42` comment … build `:51-53` + export `:62-72`"* was inherited from `kf-KeyframeTimeline.md`'s C-1/N-6 cell and never re-run. It resolves at no ref this program recognises — at the DISQUALIFIED `8281638c`, cited **only** as a named historical measurement context (RULINGS-3 R3-2), the same comment sits at **`:37`** (file 100 L there, 102 at `81a56990`). No sha is named as "the tree measured" except the one at which the census reproduces.⟩
Assertion: keyframes that merge in the built animation and exported CSS CANNOT render as two markers — minimum separation OR coalesced-and-said-so. Carries: N-6, MISS-β1, L-2/C-3/D·M-4's rounding. Serial-only (shared-file rule, §Bounds).

**G6 — Import → export round-trip preserves what the user authored.** RED. Ref: origin/master (C-5 (TimelineHoverPreview) is vacuous at stale HEAD).
Witness — **the four bare leaves PATH-QUALIFIED at repair round 4 (RULINGS-4 R4-10(1), SUBJECT-IDENTITY arm: the enumeration source is every gate-witness symbol, whole file — not the rows a ruling named), each re-derived by this seat at `origin/master 81a56990`**: `importCSSToTimeline` — declared **`demo/components/instrument/timeline/utils/timelineEngine.ts:76`**, imported `useTimelineBuild.ts:12`, awaited `:146` — drops per-stop `animation-timing-function`; `TimelineKeyframe.easing?` — declared **`demo/components/instrument/timeline/timelineTypes.ts:10`** — never written/read; named scroll phases caption fabricated percents (`entry 100%` / `cover 0%` both "25%"); `state.value.keyframes = imported` ⟨**`demo/components/instrument/timeline/composables/useTimelineBuild.ts:152`**, on the `CSSPasteDialog` consumer path (R-3)⟩ ⟨***path-qualified at repair round 6, PASS-6 D7: the round-4/5 leaf named ~~`CSSPasteDialog`~~ as the holder of `:152`, and that file is 80 lines at `81a56990` — it cannot hold the coordinate. P8's R-3 row at `:226` carries the same bare `:152` with no file and takes the identical qualification in this edit. The R4-10(1) receipt at `:414` certified "all four are now file+line qualified at 81a56990" over three qualified leaves and this one — LAW G's shape at a gate witness.***⟩ replaces against copy that says "merge"; `captureSnapshot` — declared **`demo/components/instrument/timeline/utils/snapshotCapture.ts:8`**, called `useTimelineOps.ts:29` — drops `none`/`auto` at its **`:16-21`** filter. ⟨A symbol without its file is not an anchor: two of these four names resolve in more than one module at the frontier, and the round-3 spelling left an executing seat to guess which.⟩
Assertion: a fixture corpus round-trips — per-stop easing survives, named phases render as named, "Add" does what the copy says, "animate to none" is authorable or its exclusion recorded. Carries: N-9, N-8, C-5 (TimelineHoverPreview), R-3, L-6/C-4's delegation.

**G7 — A read gesture writes nothing.** RED.
Witness: `TimelineCaret.vue:22` `@blur`-alone commit; `:58-65` no compare; `useTimelineOps.ts:60` fresh selector object per call; deep-keyed `useRefHistory`.
Assertion: open the caret editor, blur without typing: ZERO undo entries, ZERO export-byte change, display returns at MODEL precision. Carries: L-2/C-3/D·M-4 compare-before-commit, MISS-α4's comment rider, L-9/m-8 (TimelineCaret)'s clamp protection. Kill lock: C-1's float story never re-derived (locus = MISS-β2, KF.W8).

**G8 — Keyboard parity, non-destructive.** RED.
Witness: `TimelineTrack.vue:203-214` (`select` only paired with `moveKeyframe`; Enter/Space discarded); the bare-div track; no keyboard zoom/pan route; `useTimelineOps.ts:28` + argless `snapshot()` in RibbonBar ⇒ keyboard snapshots at 0%.
Assertion: every pointer verb (scrub, zoom, pan, select, retime) has a keyboard route; NO inspection keystroke mutates the document; keyboard `snapshot()` captures at the user's position; Enter/Escape exit the caret editor without stranding focus on `<body>`. Carries: M3, D-3 (percent readout a11y)'s caret-redesign a11y arm, D-1 (TimelineTrack)'s adopted harm, D-8 (state contract, TimelineTrack), D-11's keyboard arm, M-7 + RR-B missed-4 (joint)'s route, MISS-α2, m-1/L-10, m-4 + MISS-α1 (TimelineCaret), m-5/L-6/C-5 (TimelineCaret)'s one-shape decision. Exports the THP D-3 reopening.

**G9 — The tooltip announces what it shows.** RED.
Witness: reka `TooltipContentImpl.js:87` (`props.ariaLabel || currentElement.value?.textContent`, untracked, captured once); `ariaLabel` unpassed at `TimelineTrack.vue:86`; `textContent` excludes `<img alt>`; `text-admin-label` uppercases the payload; `grep -rn "data-register"` across demo styles + glass dist CSS → zero targeting rules.
Assertion: the accessible description carries the derived alt, punctuated at row boundaries, re-derives on the ghost→image swap, is not force-uppercased; the rail has an accessible container and AT-hidden graduations. Carries: MISSED-1 + D-10 (KeyframeTimeline) (one seam), M7 with its MM-29/MISSED-3 cure guards and DISSENT-4 verbatim-element law, RR-A missed-1 (TimelineTrack).

**G10 — No thumbnail outlives its keyframe's edit; the ghost tells the truth.** RED.
Witness — **the placeholder is RESOLVED at this repair round (RULINGS-3 §END D9; L-19 forbids a gate resting on an unnamed witness, and `<cache module>` named no coordinate while this gate locks the whole ghost/cache family)**: the cache module is **`demo/components/instrument/timeline/KeyframeTimeline.vue`**, where `previewCache` is declared at **`:217`** (`const previewCache = reactive<Record<string, string>>({});`), passed down at `:81` (`:preview-cache="previewCache"`), read at `:221`, written at `:226` — keyed on mutation-stable ids; `git show origin/master:demo/components/instrument/timeline/KeyframeTimeline.vue | grep -n 'delete'` → **0 hits** (re-executed by this seat at `81a56990`: the only `previewCache`-adjacent lines are `:81`/`:217`/`:221`/`:226`, and no `delete` keyword appears anywhere in the file) — so nothing evicts; failed captures retry forever; the sibling history bounded at `capacity: 50`; `opacity: "0"` survives the capture filter; `scale(0.3) ${matrix}` on the bordered element itself.
Assertion: post-edit, the cached thumbnail is gone or regenerated; no orphan base64 PNGs after remove/clear/import-over; a repeatedly-failing capture stops and says so; the ghost renders decomposed (scale on a wrapper), has a terminal `v-else`, and reserves its box. Carries: D-4/L-4/C-5 (comment rides the cure, KF-CE-41), D-7 (TimelineHoverPreview), MISSED-4, GHOST-PLATE, L-D8/C-4(a), L-D4/C-4(b), m-7/m-8 (TimelineTrack, merged), M9's rung lock, D-2/L-D9, D-3/L-D6/C-6 under MISSED-3's guard, D-12 (THP) + D-15 (THP) (TimelineHoverPreview layout pair), L-D15 / MISSED-6 / L-D14 / P2-DEAD (THP hygiene group). WebGL blank-resolve → SS-13 residue #1.

**G11 — The cluster is mounted by a test at all.** RED — and the runnability precondition of the ten behavioural gates **G2–G10 and G13** (§Gates preamble's one reading; the documentary/ruling gates G1, G14, G15 do not wait on it — they wait only on G12's ref pin).
Witness — **TWO commands, re-cut by RULINGS-2 R2-6.5 so each returns what its reading says** ⟨the round-1 wording ran one command over `test/ demo/` and read it as "no mount": the `demo/` leg can only ever return render sites, so the reading was carried by the `test/` leg alone. Both legs re-executed by this seat at `origin/master 81a56990`⟩:
- **(i) the coverage leg — THE gate's claim**: `git grep -n "TimelineHoverPreview\|previewCache\|SequenceScrubber" origin/master -- test/` → **0 hits**. Zero tests name the surface or either cache prop. `timeline-undo.test.ts` (a tracked read-only witness, §Bounds) exercises the same state *without* the surface — the contrast that convicts.
- **(ii) the render-site leg — enumeration, NOT the claim**: the same pattern over `demo/` returns **13 hits, all render sites, expected and lawful**: `KeyframeTimeline.vue:81/:217/:221/:226` · `TimelineTrack.vue:87/:89/:117/:125` · `SequenceTarget.vue:125/:127/:146/:205` · `useSequenceInstrument.ts:5`. Printed here so the number is falsifiable and never again read as a mount.
- ⟨**OP-4 re-measured at `81a56990` by the round-3 repair seat — the round-2 cost model is STRUCK (RULINGS-3 R3-8.3)**⟩ `@vue/test-utils` is NOT installed and NOT in origin/master devDependencies (`git show origin/master:package.json | grep -c 'test-utils'` → **0**; `jsdom ^29.1.1` present) — **that half stands and is this wave's ONE manifest add**. **`@vitejs/plugin-vue` IS a devDependency at `package.json:81` (`^6.0.7`) and IS installed** (`ls -d node_modules/@vitejs/plugin-vue` → PRESENT), so the round-2 claim *"no Vue-SFC plugin exists"* is false at the frontier and *"the gate costs a dependency AND a Vue-SFC plugin"* is struck. What remains true: `git show origin/master:vitest.config.ts | grep -n plugins` → **(none)** — the plugin is **unregistered**, and the **registration is KF.W4's booked act** (§Commits commit 2, cited at §Sequencing → KF.W4). **G11's true cost: ONE devDep (`+@vue/test-utils`, §Bounds) + ONE inbound dependency-cite (W4's commit 2). This gate performs no `vitest.config.ts` edit** — and it is RED on its own leg (i) regardless, which is the gate's claim.

Assertion: the **four named fixtures** (§Bounds, by full filename — `timeline-mount-projection.test.ts` · `timeline-mount-keyboard.test.ts` · `timeline-hover-preview.test.ts` · `sequence-scrubber-mount.test.ts`) exist and mount the timeline cluster AND SequenceScrubber, asserting keyboard parity, direction latch, and projection; **leg (i) returns non-zero for those files and only those files** — no tracked file in `test/demo/instrument/` is edited to make this gate pass. Carries: C-9 (THP) — coverage, kf-SequenceScrubber L·D-12 (banked N-10 class, second instance) — one coverage decision.

**G12 — The spec names its ref; every anchor re-resolves against it.** RED until stamped at open.
Witness ⟨***ROOT VARIABLE DECLARED ONCE, HERE — PASS-7, 2026-08-29, check `P7-8`.*** `$PROG` = **the directory that holds the sibling clones**; at this seat `$PROG=/Users/mkbabb/Programming`. This is this file's only cross-repo command, and it now carries no machine path in its own body; the binding is a **dated fact of this seat**, re-stated by whoever runs it elsewhere. Re-run in the `$PROG` spelling at this seat: **both shas identical**⟩: `git -C $PROG/keyframes.js rev-parse master origin/master` → `8281638c` / `81a56990`; `git merge-base master origin/master` → `a59d3a22`; `git cat-file -e 8281638c:demo/utils/keyframeSelector.ts` → fails; `git grep -n "selector" 8281638c -- demo/components/instrument/timeline/timelineTypes.ts` → absent; manifest triple-state per MISS-α5 ≡ RR-β K-BASE.
Assertion: the opening commit states origin/master `81a56990` (or later), marks composable/engine anchors origin-side, re-resolves every line anchor before cure (D-19). Carries: MISS-α5 ≡ RR-β K-BASE, the witness law (kf-App ruling 1), KF.W0 §B-12, the F-1 per-baseline re-rule.

**G13 — The wheel does not eat the page.** RED.
Witness: `@wheel.prevent` at `TimelineTrack.vue:31`; neither branch matches a plain wheel; `touch-none` :24; `useZoomPan.ts:59` reads `deltaY` only (`:50` the sole other delta read; `deltaX` nowhere); the pan readout carries no handler/role/tabindex (:4-18).
Assertion: plain wheel over the track scrolls the ancestor; shift-wheel pans on whichever axis the engine delivers; the pan readout is operable; the open caret editor is wheel-shielded by an explicit is-editing guard. Carries: M5 ≡ RR-B-3, M-7 + RR-B missed-4 (joint) (escalation trigger armed: `deltaY: 0` ⇒ jointly MAJOR), L-11 (TimelineCaret), ARB-1's pan-writer dependency.

**G14 — The cluster's failure posture is ONE posture.** RED (six divergent postures counted).
Witness: `useTimelineBuild.ts:47-50` (the sole non-toasting failure — the one reachable by typing) vs toasts at :121/:133/:137/:148/:155/:157; caret L-15 silent swallow; `snapshot` success-toast-before-outcome (L-11 (KeyframeTimeline)); KAD-9 failure-only completion signal; R-4 close-regardless-of-parse; D-15 (KeyframeTimeline)'s unexpressed empty/failure states.
Assertion: one declared failure posture across cluster + dialogs, registered in KF.W2's posture registry (cross-referenced both ends). Carries: C-7 (posture arm; crash identity = megatranche R1, never re-booked; §F-2 scope correction rides out), L-15 (TimelineCaret), L-11 (KeyframeTimeline), D-15 (KeyframeTimeline), R-4 (+ KAD-9, KAD-10).

**G15 — One dialog shell, or the divergence ruled in writing.** RED.
Witness: `wc -l` → CSSPasteDialog.vue 80 / KeyframesAddDialog.vue 161; byte-identical `@interact-outside` guard and `onInput` body in both (grep); KAD nests `DialogDescription` inside `DialogTitle` (grep -A2 DialogTitle) while `DialogHeader` sits unused in the same barrel; **`initialText` is dead AT THE MOUNT, not in the file** ⟨witness re-founded by this repair seat — the earlier wording ("declared, never bound") was refuted by its own command: `git show origin/master:…/CSSPasteDialog.vue | grep -n initialText` → **four** hits, `:49` declared · `:53` defaulted · `:59` `ref(props.initialText)` · `:67` re-read on open. The bank's actual claim (R-19) is a MOUNT claim, and it holds: `git show origin/master:…/KeyframeTimeline.vue | sed -n '135,152p'` → both `<CSSPasteDialog>` mounts pass only `v-model:open` / `title` / `description` / `button-label` / `:button-icon` / `@submit` — **`:initial-text` appears at neither**, so the in-file channel is fed by nothing and R-4's needed acknowledgement channel is minted-but-unwired⟩; **N-2's two occurrences take two commands** ⟨also re-founded: `grep -rn "\.label" …/timeline/` returns **one** hit (`KeyframeTimeline.vue:106 v-model="selectedKeyframe.label"`) — the pattern `\.label` cannot match a type declaration; the second occurrence is `grep -n "label" …/timelineTypes.ts` → `:11 label?: string`. Two occurrences, one per command; an execution seat running a single `\.label` sweep reads 1-of-2 and would grade N-2 half-cured⟩.
Assertion: the twin folds onto the 80-line shell under the pinned constraints (CSSPasteDialog's a11y composition + the twin's text-hoisting contract; four genuine deltas preserved; L-4's falsifier answered) OR the fold is declined with the falsifier's answer in writing; every dialog-residue row's disposition (discharged-by-fold / discharged-by-KF.W6-swap / cured) is STATED before any cure is spent — **and that statement enumerates the three W6-owned dual-homed ids by id (KAD-15 · KAD-16 · KAD-20, §Excluded), because KAD-15's footer-placement and KAD-16's `grid`-over-flex cells are the structural root of the fold direction R-7 pins: the seat cannot declare the footer bare without saying what happened to them**; N-2 is ruled wire-or-delete. Carries: R-7 ≡ KAD-F3, KAD-6, KAD-13, R-17 / R-19 / R-21 / R-22 / R-23 / KAD-21 (dialog MINOR group), N-2, L-16/L-17's sequencing.

---

## §Sequencing

**Hard ordering (blocking, in order):**
1. **kf-AnimationControlsGroup D-1 — the document-singleton portal sink (ADJUDICATED BLOCKER, restored) — is SETTLED before this wave's timeline-cluster work** (OP-0; RULINGS-2 R2-9). It opens the list because it sits one rung above OP-1's peers at the bank. The bank's words, verbatim: *"**NO-WAVE-OWNER**, must be settled **before** KF.W6-TIMELINE (any timeline-cluster swap inherits the portal architecture)"* ⟨`kf-AnimationControlsGroup.md`, the §BLOCKER **D-1** row's terminal disposition⟩ — and this spec's own taxonomy reconciliation (below) rules KF.W6-TIMELINE references to BE this wave. **The settlement is a design ruling (keep or replace the portal), not a cure**: the cure is NO-WAVE-OWNER. **Consequence carried forward: C-6's "drive the scene's single engine through AnimationControlsGroup" arm and G2's restatement of it are CONDITIONED on this settlement** — no cure routed through AnimationControlsGroup lands before the portal ruling. The expanded-timeline settlement (item 8) inherits the same condition: D-1's scope surface (cube 3 · amiga 3 · spring 2, one `superKey` bucket) IS the expanded, stacked timeline.
2. **kf-ChannelControls L-2/C-2 (`:key` remount BLOCKER) lands BEFORE this wave** (OP-1). **Quotation discipline (RULINGS-2 R2-7, M-25): the round-1 sentence *"the `:key` data-loss row MUST be settled BEFORE KF.W7"* was a PARAPHRASE presented as the bank's own words, and it silently hardened "before/within" into "BEFORE" — it is struck.** The two true quotes, each verbatim at its source at this writing: `kf-ChannelControls.md`, the **L-2/C-2** terminal disposition — *"it MUST be settled before **KF.W6-TIMELINE** — any timeline-cluster swap that inherits the `:key` inherits the data loss"*; and `kf-KeyframeTimeline.md`, the **Cross-refs, not folds** line — *"must be settled **before/within** KF.W7"*. **Paraphrase, said so**: the two together bind the row to land before this wave's cluster work, which is the ordering this item asserts; the direction of the round-1 error was conservative, so nothing downstream weakens. Second-order collision RESOLVED by this spec: KF-CE-2 (parent half)'s "keyed remount" option is REJECTED; **arm-time id capture is the ruled cure** (P8).
3. **G12 ref-pin + G11 mounts (with OP-4's ONE devDep add and its ONE inbound dependency-cite — KF.W4's commit-2 plugin-vue REGISTRATION, which this wave consumes and does not author; RULINGS-3 R3-8.3 / §END DH-2)** open the wave; **no CURE COMMIT precedes them** — the scope is commits, not readability: G1's verdict table, G14's posture ruling and G15's fold ruling are documentary acts that run on the pinned ref alone (§Gates preamble's one reading), and G1's verdict is itself item 4 below.
4. **G1's verdict table precedes every verdict-gated row** (KF-AV-28 mechanics, §Verdict) — and precedes KF.W11 arm (d)'s shape rows and KF.W13's two packets ("resolve KF.W7 first").
5. **The pointer-guard commit (M-3/C-4 + RR-B missed-3 (TimelineTrack) + m-9/m-11/m-12 (TimelineTrack, merged)) precedes or ships with the seam commit (G2 family)** — precondition, not follow-up.
6. **D-19 geometry re-derivation precedes every P4 cure** (OP-3; both stale tables: D-17's and K-13's corrections; one padding-box correction family with RR-B missed-5 (TimelineTrack) and C-9 (TimelineCaret)).
7. **N-2 wire-or-delete is ruled here BEFORE KF.W6 opens D-9**; the R-7 fold settles before L-16/L-17's descriptor.
8. **The expanded-timeline settlement (D-10 (TimelineTrack, expanded mode) + RB-7 + D-16 + kf-ChannelControls C-8/D-6 + KT D-7's plate strip) follows items 1 and 2** — both BLOCKERs govern the same expanded surface, and the bank routes C-8/D-6 into *"the same expanded-timeline spec family as D-1 (the structure and the portal must be settled together)."*
9. **KF.W2 PUBLISHES ITS POSTURE REGISTRY BEFORE THIS WAVE DECIDES A FAILURE POSTURE (G14).** ⟨**NEW AT REPAIR ROUND 4 — RULINGS-4 §END KF-W7 D5.** This is a **sixth inbound edge** and rounds 1–3 declared it at ONE end: `KF-W2.md` asserted a bilateral, blocking ordering; this file's hard-ordering block enumerated eight items, none of them this one, and its `KF.W2/W3` cross-edge listed the posture rows as **OUT** only. G14's *"registered in KF.W2's posture registry"* is a COMPLETION condition, not an ordering — so a hard inbound ordering W2 believes is reciprocal existed here only as a registration clause. Exact class of the W10-seam defect R3-10 cured at its source, and its sixth member.⟩ **The sibling's assertion, quoted-by-command under LAW C(2) at its FINAL stage-1 bytes** (substrate `docs/tranches/X/keyframes/waves/KF-W2.md`, mtime **2026-08-28T17:02:55**, re-read by this seat AFTER W2's round-4 write): `grep -n 'KF.W7 may not decide' KF-W2.md` → **`:782`** ⟨**P7 COORDINATE SWEEP, 2026-08-29** — re-run at final bytes: **`:786`**, one hit, sentence byte-exact⟩ *"**KF.W7 may not decide a failure posture before KF.W2 publishes the registry** (declared at both ends — KF-W7.md G14 resolves against this registry, verified)"*; `grep -n 'Both directions are now declared at both ends' KF-W2.md` → **`:825`** ⟨**P7 COORDINATE SWEEP, 2026-08-29** — re-run at final bytes: **`:829`**, one hit, sentence byte-exact⟩ ⟨***re-run at repair round 6, PASS-6 D4. BOTH SENTENCES ARE BYTE-EXACT AT THEIR TRUE LINES and the hard ordering they carry is unaffected — this is the one hard ordering this file added BECAUSE a sibling believed it was reciprocal. Per this cell's own rule THE ANCHOR OF RECORD IS THE SENTENCE, NOT THE NUMBER: the coordinates are printed once, dated, and are not load-bearing.***⟩, closing W2's KF-AV-28 acknowledgement — *"W7 may not decide a posture before the registry exists, and the registry does not wait on the SWAP verdict. Both directions are now declared at both ends."* ⟨The PASS-4 check read these at `:571`/`:608`; they are at `:710`/`:753` after W2's round-4 write — the drift is why LAW C(2) requires the substrate, the mtime and the command, and why the anchor of record is the **sentence**, not the number.⟩ **The ordering this end now asserts, matching W2's**: G14's posture ruling is a DOCUMENTARY act (§Gates preamble) and therefore not blocked by G11's mounts — but it **is** blocked by W2's registry publication, which supplies the registry the ruling registers into. **Direction, both halves**: W2's registry PRECEDES this wave's G14 ruling; this wave's SWAP verdict does NOT precede W2's registry (W2's own sentence — the registry does not wait on the verdict), so the two obligations do not deadlock. **Falsifier**: this item fails if G14 is stamped against a registry that does not exist, and it fails if any seat reads it as making W2's registry wait on G1's verdict table.

**Commit families (rows that must not split):** the scrub seam (G2's family); grabDx + L-m-14; the wheel policy (G13's four); the collision decision (G5's three, serial on the shared files); the ghost redesign (D-7 + MISSED-4 + GHOST-PLATE + L-D8/C-4(a) + L-D4/C-4(b), with m-7/m-8's validation half sequenced after L-6/C-4's delegation); the a11y commit (MISSED-1 + D-10 (KeyframeTimeline) + M7 + RR-A missed-1 (TimelineTrack)); D-12 (KeyframeTimeline) + RR-A missed-2 (TimelineTrack); the expanded-timeline settlement (D-10 (TimelineTrack, expanded mode) + RB-7 + D-16 + kf-ChannelControls C-8/D-6 + KT D-7's plate strip — one settlement); the round-trip commit (N-8 + N-9 + C-5 (TimelineHoverPreview) + R-3 under G6); the posture ruling (G14's consumers, one ruling); the undo-authority ruling (KF-KC-11 (undo-trail arm) + KF-KC-52 + L-2/C-3/D·M-4 + RR-B missed-1 + L-2/C-2 — one authority, four consumers, one ruling); MISSED-3's same-commit box law wherever a type rung moves.

**Packet homing (the NO-WAVE-OWNER scene-repair packets, homed as authoring requires):** the **sequence** packet's contract question (C·C-4 (SequenceScrubber)) and the **spring** packet's idiom/prose/export arms (KF-AV-10, C-4 (SpringTarget), C-3 (SpringTrace)) are HOMED HERE — decided in G1's table, cures verdict-gated; **CARD-UNIT**'s undo question is HOMED HERE via the undo-authority ruling (cure site declared to the CARD-UNIT owner); KF-AV-13's press-to-seek DECISION is homed here, its cure lands NO-WAVE-OWNER (SS-1/SS-2); **cube** and **square** packets are KF.W11's (SquareScene MISS-1 shares grabDx's class — arm c), **dock-menu** is KF.W13's — ⟨**ADDRESSEE CORRECTED AT REPAIR ROUND 5 (PASS-5 check D10; RULINGS-4 R4-8)**: the round-4 text said these were *"homed by their owning specs"*. **There are no owning specs.** KF.W11 and KF.W13 are **MINTED-UNAUTHORED** waves, and their terminal disposition is `KF-W10.md` **§6.D · SUCCESSOR-FORMATION REGISTER → the `KF.W11 · Demo Scene Repair` row and the `KF.W13 · Chrome, Dock & Transport Repair` row**, exactly as KF.W12's row is EDITOR-UNIT's addressee at the cross-edge below — stable anchor alone per LAW C(3), W10 writing last. **The round-4 D9 cure applied itself *"by SCOPE, not by named instance"* and then stopped two rows short of its own scope**: KFED-UNIT, CARD-UNIT and EDITOR-UNIT were converted; these two addressees, of identical shape and in the same section, were not. *"A repair that cures the named instance and not the stated scope leaves the law green over its own counterexample"* is this spec's own quoted lesson, and it applied one paragraph over⟩ — edges declared from this end, reciprocation promised from no unauthored spec. **kf-SquareScene law carried: comment-stated invariants are test obligations** (MISS-α4 / KF-CE-41 are this spec's instances).

**LAW C SUBSTRATE DISCLOSURE (new at repair round 4 — RULINGS-4 LAW C, the two-stage / reconcile law; this seat writes in STAGE 2).** Every cross-wave receipt in this file is now one of exactly two forms, and which one it is depends only on write order:

- **BACKWARD (stage-1 siblings — KF.W0 · KF.W1 · KF.W2 · KF.W3 · KF.W4 · KF.W5 · KF.W6): quoted-by-command at their FINAL stage-1 bytes**, each with its substrate and mtime named beside the pasted command, all re-read by this seat AFTER those waves' round-4 writes. **Verified by ANCHOR, after the last per-wave write (LAW E(4): no quoted sibling prose, no line numbers, no per-seat mtime stamps)**: **KF-W2** ⟨§Sequencing — the two ordering sentences, *"KF.W7 may not decide a failure posture before KF.W2 publishes the registry"* and *"Both directions are now declared at both ends"*, both byte-exact at that file's final bytes⟩ · **KF-W4** ⟨§Bounds — the two rows this file's own `:31` already prints correctly — and §Commits commit 2⟩ · **KF-W5** ⟨§Disjointness⟩ · **KF-W6** + **KF-W6-CARRY** ⟨§Carry⟩ ⟨***re-cut at repair round 6, PASS-6 D3: the round-4 BACKWARD block was untouched round-5 text under the words "Verified this round", carrying FOUR per-seat sibling mtimes (~~17:02:55~~ · ~~16:59:44~~ · ~~16:55:55~~ · ~~16:59:25 / 10:49:45~~) and FOUR false coordinates (~~KF-W2 :710/:753~~ → `:782`/`:825`; ~~KF-W4 :54/:58~~ → `:55`/`:59`). The header at `:423` opens the round-5 ledger with "LAW E(4) governs every cross-wave receipt below" — LAW E(4) is unrestricted, the FORWARD half was re-cut with a four-part test and a scope receipt, and the BACKWARD half received nothing. Every ordering this block records is SOUND and re-verified by anchor.***⟩ ⟨*the round-4 mtimes and coordinates struck above, preserved as a dated record*: (mtimes 16:59:25 / 10:49:45 — both §Excluded ends re-verified present **by banked row header**; the underlying line numbers moved again this round, which is precisely why the header is the anchor and the numbers are retired)⟩.
- **FORWARD (stage-2 siblings that write AFTER this seat — KF.W8 · KF.W9 · KF.W10): stable anchor ALONE — §-heading + row/gate id, no quoted prose, no line numbers**, per LAW C(3), verified by the RECONCILE seat after the last per-wave write. ⟨**CLASSIFICATION CORRECTED AT REPAIR ROUND 5 — PASS-5 check D12, the mechanism of D1** (RULINGS-5 R5-9 row 13). Rounds 4's enumeration listed *"§Bounds' four-party footprint row"* among the FORWARD **anchors**. **It was not an anchor.** It asserted KF.W8's access verbs, its glob spelling and its file count — a **content claim about a sibling's unwritten bytes**, filed under the exemption reserved for claims that make none. Classified honestly it was a forward QUOTATION, **forbidden outright by LAW C(3)**, and it would have been caught at the taxonomy rather than at the sixth-pass census. **The exemption is what hid the drift, so the taxonomy is what is repaired**: an entry qualifies as FORWARD-ANCHOR only if it survives the four-part test — **no verb, no glob, no count, no prose** (and never a line number). **EIGHT sites failed it and are re-cut this round, by scope and not by named instance — the enumeration IS the receipt, and its command is `grep -n 'KF-W8\.md\|KF\.W8' KF-W7.md`, every hit read and classified (the arithmetic rides at the scope receipt below)**: (1) §Bounds' claimant-table **KF.W8** row · (2) §Bounds' `package.json` row · (3) the STRUCK `vitest.config.ts` row · (4) the four-party paragraph's read-only-witness sentence · (5) §Disjointness' cross-wave roster sentence · (6) §Bounds' read-only-witness scoping parenthetical · (7) the → KF.W8 cross-edge's SHARED-SURFACES sentence · (8) **OP-4's *"KF.W8 independently states the correctly-scoped form"* characterization**, which round 4 had already re-cut once and left carrying prose. **The PASS-5 check convicted THREE cites; the command found EIGHT** — the five it did not name are cured here anyway, because a class repaired at its named instances is the failure this file has now recorded three times (R4-10(1) · D10 · this). **What is DELETED at all six is only ever a claim about a neighbour; not one of this wave's own acts, bounds, orderings or dependencies changes** — which is the test of whether the class was load-bearing, and it was not.⟩ The members, enumerated so none hides: **KF.W8** — §Bounds' claimant-table row, its `package.json` row, the struck `vitest.config.ts` row, the four-party read-only-witness sentence, the → KF.W8 cross-edge, OP-4's third-claimant anchor, and the two characterization-pair create FILES, which `KF-W8.md` books on ONE row, **the members are enumerated below and each is stated against the four-part test; the round-5 spelling ~~"every one of them now passing"~~ is struck under LAW G, since the cell that certified the test was itself violating it**; **KF.W9** — the SS-13 residue edge (its cargo is read from the REGISTRY's own residue lists, not from W9's spec, so nothing there is a sibling quotation); **KF.W10** — §3.2's `KF-AV-28 · STANDING SUPERSESSION RIDER` packet-lock bullet, §G-2's Acceptance clause, and **§6.D · SUCCESSOR-FORMATION REGISTER → the KF.W12 row** (D9's addressee, minted by RULINGS-4 R4-8). **One item is flagged rather than silently kept**: §Verdict Protocol adopts W10's terminal alphabet string `DISCHARGED by KF.W7 SWAP verdict <surface>, <date>` as this wave's OUTPUT FORM. It is carried as an adopted form, not as a quotation-receipt, and the **RECONCILE seat is asked to byte-check it against W10's post-round-4 §G-2 Acceptance clause** — if W10's alphabet moves, this form moves with it, because a receipt emitted in a superseded alphabet is a silent drop at the far end (R3-10.3's whole point). ⟨**RECONCILE SEAT, 2026-08-28 — BYTE-CHECKED AS ASKED; THE ALPHABET DID NOT MOVE (`PASS-4/CLOSE-CERT.md` §R-12).** Re-run at KF-W10's post-round-4 bytes (**mtime 17:21:25**, the last per-wave write of the round): its **§G-2 Acceptance clause** carries the adopted string **byte-identical** — *"every row LANDED / KILLED-with-rationale / **`DISCHARGED by KF.W7 SWAP verdict <surface>, <date>`** / **`ADOPTED-BY-KF.W6 (RULINGS-4 R4-1)`** / carried to the next formation boundary's ledger"*. **The form this wave emits is the form W10 consumes**; no receipt of this wave's is emitted in a superseded alphabet. **The other three forward anchors resolve too**: §3.2's `KF-AV-28 · STANDING SUPERSESSION RIDER` packet-lock bullet is live, and **§6.D · SUCCESSOR-FORMATION REGISTER** exists with **the KF.W12 row present** — D9's addressee, which did not exist when this wave named it and was minted into being by R4-8 at the seat that writes last. **The one item this file flagged rather than assumed is the one item that needed no repair** — which is the argument for flagging it⟩. ⟨**REPAIR ROUND 5, THIS SEAT — NOT EDITED, AND WHY.** The insertion immediately above is the RECONCILE seat's own product, and its substrate stamp (*"mtime 17:21:25, the last per-wave write of the round"*) is convicted by `PASS-5/KF-W7-CHECK.md` **D2**: `KF-W10.md` moved twice more after that reading, the last time 22 s AFTER the certification that vouched for it — the falsification `RULINGS-5` **R5-2** attributes to the RECONCILE seat itself. **The cure is owned elsewhere by name**: R5-9 row **14** assigns it to **RECONCILE** (*"CLOSE-CERT-5 re-stamps its predecessor's insertion — its own product class; LAW E(1) note"*), and RULINGS-5's governing law gives every cross-wave defect exactly ONE owning cure a single wave can apply. This seat therefore books the defect and does not touch the text: a per-wave seat re-stamping another seat's clock is the very mechanism LAW E(4) retires. **The finding's substance is separately safe and was re-verified by the pass-5 seat, not inherited** — all four forward W10 anchors resolve at W10's current bytes; what failed is the certification's instrument, not this wave's dependency.⟩ ⟨**RECONCILE SEAT, 2026-08-28 — THE RE-STAMP OWED BY R5-9 DRIFT ROW 14, LANDED; THE ALPHABET STILL HAS NOT MOVED (`PASS-5/CLOSE-CERT-2.md` §4 · R5-R2, and §6's substrate table).** This seat owns the cure and the wave was right to book it rather than take it. **The struck stamp**: the round-4 insertion above reads *"Re-run at KF-W10's post-round-4 bytes (**mtime 17:21:25**, the last per-wave write of the round)"* — and `KF-W10.md` moved twice more after that reading, the last of them **22 s AFTER the certification that vouched for it** (RULINGS-5 **R5-2** attributes that twelfth write to the round-4 RECONCILE seat itself). **That clock is STRUCK as a live claim and retained as the dated round-4 reading it was; per LAW E(4) this seat inscribes no replacement mtime here — the stamp authority is the cert's substrate table.** **The BYTE-CHECK IS RE-RUN AT THE ROUND'S FINAL BYTES, which is the half that was ever load-bearing**: at `KF-W10.md`'s round-5 final bytes ⟨**247,385 B** — ***THE STAMP IS STRUCK BY THE SUCCESSOR RECONCILE SEAT, 2026-08-29 (`PASS-5/CLOSE-CERT-2.md` §4 · finding R5-R2, and §6's substrate table).*** Those were **not** final bytes. `KF-W10.md` was written **once more, at 19:30:41 — +1,058 B and 116 s after this insertion's own 19:28:45 clock** — by the *predecessor reconcile pass itself*, which then ended without laying a certificate. **That is the R5-2 mechanism firing a THIRD time — inside the very round that minted LAW E to kill it, and again at the hand of the seat that certifies write-order** — which is precisely why LAW E(1) makes the cert the last act **by construction** and LAW E(2) proves it with a **hash** rather than a clock. **No replacement numeral is inscribed here** (LAW E(4)): W10's final bytes and its sha256 live in the cert's closing hash table, derived after this seat's last spec edit and re-hashed by the pass-6 union as its first act⟩ `grep -n 'DISCHARGED by KF.W7 SWAP verdict' KF-W10.md` → **`:276` `:407` `:530`** ⟨**RE-RUN AT THE ROUND'S TRUE FINAL BYTES BY THE SUCCESSOR SEAT, 2026-08-29: `:276` `:407` `:530` — UNMOVED across the twelfth write** ⟨***THE "UNMOVED" IS FALSE AND IS STRUCK — PASS-7 COORDINATE SWEEP, 2026-08-29, check `P7-6`***: re-run at `KF-W10.md`'s final bytes the command returns **`:282` `:413` `:536`** — **all three moved, each `+6`**, and the successor seat's own dated stamp asserted the opposite. **The load-bearing half still holds and is re-verified here**: three hits, and **§G-2's Acceptance clause — now `:413`, not `:407` — still carries this wave's adopted OUTPUT FORM byte-identical.** That a stamp written to convict stale per-seat clocks was itself stale within the same day is the sharpest single instance of the class this sweep exists to close⟩, and **§G-2's Acceptance clause at `:407`** ⟨*`:413` at final bytes — P7 coordinate sweep, 2026-08-29; see the strike above*⟩ **still carries this wave's adopted OUTPUT FORM byte-identical**. **The load-bearing half held; only the stamp failed** — which is the whole content of LAW E(4)'s indictment of per-seat clocks, and the reason this wave was right to flag the item rather than assume it⟩, and **§G-2's Acceptance clause at `:407`** ⟨*re-pointed to **`:413`** at the P7 coordinate sweep, 2026-08-29 — check `P7-6`; the clause itself is byte-identical, which is what this sentence rides on*⟩ **carries this wave's adopted OUTPUT FORM byte-identical**: *"every row LANDED / KILLED-with-rationale / **`DISCHARGED by KF.W7 SWAP verdict <surface>, <date>`** / **`ADOPTED-BY-KF.W6 (RULINGS-4 R4-1)`** / carried to the next formation boundary's ledger"*. **The form this wave emits is still the form W10 consumes — across two further W10 writes and a whole repair round, the alphabet did not move.** The other three forward W10 anchors re-resolve at the same bytes: §3.2's `KF-AV-28 · STANDING SUPERSESSION RIDER` bullet ✓ · **§6.D · SUCCESSOR-FORMATION REGISTER** ✓ **with the `KF.W12` row present** ✓. **FOUR of four forward anchors resolve; zero receipts of this wave's are emitted in a superseded alphabet.** The item this file flagged rather than assumed is, for the second round running, the item that needed no repair — which remains the argument for flagging it.⟩

**Cross-edges (declared from this end):**
- **KF.W0 §B-12** (depends-on): origin/master pins glass-ui 7.0.0 exact devDep = installed; every wave re-baselines against origin/master. F-1 re-rule request carried (per-baseline re-derivation; the AnimationVisualizer census cell corrected to worktree-scope).
- **KF.W4** (depends-on, for VERIFICATION **and** for one INBOUND ACT — **never for authoring**; the scope sentence is now TRUE, RULINGS-3 R3-8.3 + §END DH-2): KF-CE-16 vue-tsc gate + `noUnusedLocals` (K3) precede verification of every typed cure here (L-12/C-8, L-7 (TimelineCaret), L-m-10, the TimelineTrack contract-hygiene group, L-13 / L-14/C-16 (dead-surface pair)). ⟨Round 2's *"for VERIFICATION not authoring"* was FALSE the moment this wave carved `vitest.config.ts`; the carve is struck at §Bounds and the sentence now holds.⟩ **INBOUND DEPENDENCY-CITE (the plugin-vue registration collision, declared at this end; R3-8's reciprocal is owed by KF.W4)**: the Vue-SFC plugin registration G11's mounts require is **KF.W4's booked act** — `KF-W4.md` §Bounds `vitest.config.ts` row · item 3 **ME-32 ≡ R-10** · **§Commits commit 2** (*"plugin-vue REGISTRATION … only `vitest.config.ts` changes"*) — and W4 lands first. **This wave writes no `vitest.config.ts` byte**; G11's runnability is a dependency on that commit, witnessed by `vitest.config.ts` declaring the plugins line with provenance W4's §Commits. **MANIFEST ORDERING (DH-2)**: W4 owns `package.json`'s scripts surface + its own devDeps and edits the file FIRST; this wave's carve is the single `+@vue/test-utils` devDep line and nothing else; KF.W3 (`:70` value.js re-pin) and KF.W8 (`+@vue/test-utils`, narrowed by R3-8.1) are the other two claimants and are named in the §Bounds row. Owed to KF.W4: the census-gate design brief (C-S+1 + REGISTER-LAUNDER).
- **KF.W6** (feeds and is bound by): (1) MISSED-3's collision guard binds BOTH waves — no `text-mono-small` swap without the box resize in the SAME commit; no `normal-case` without `tracking-normal` (MM-29); (2) N-2 ruled here first; (3) caret C-7's `.focus-ring` one-class cure inverts the primitive-swap order — D·M-5 discharges without waiting on the NumberField evaluation; (4) L-9/m-8 (TimelineCaret) discharges with KF.W6's NumberField swap if it lands there (declared both ends); (5) KAD-6 stands alone at KF.W6 if the fold is declined.
- **KF.W2/W3** (bidirectional): OUT — C-7's §F-2 scope correction + fuzz-entry note + the posture-registry rows (G14); IN — **(a)** L-6/C-4's delegation target (`@mkbabb/value.js/css` collectors / the parse façade, OP-5) must exist or be named; **(b) THE POSTURE-REGISTRY ORDERING — added at repair round 4 (RULINGS-4 §END KF-W7 D5), the leg whose absence made this edge one-ended**: KF.W2's posture registry (its phase-1 `.a` publication, G-W2-1) **PRECEDES** this wave's G14 ruling — hard, blocking, and now booked as **§Sequencing hard-ordering item 9**, where the sibling's own sentence is quoted by command at its final stage-1 bytes. The posture rows travel OUT as this wave's contribution to that registry and the ORDERING travels IN; rounds 1–3 carried only the OUT half, which is why `KF-W2.md`'s *"declared at both ends"* was true at one end. G14's *registration* clause is retained as the completion condition it always was — it is not the ordering, and the two are no longer conflated.
- **KF.W5** (feeds/defers, **and shares one directory**): KAD `innerHTML` BLOCKER front-loaded there, never gated here; C-3 (SpringTrace)'s library export (`resolveLinearStops`/`sampleNormalizedSpring`, C-15 precedent) — implementation edge declared to KF.W5/KF.W8; THP C-8's Skeleton half ≡ KF-SKEL-16 — the status-line cure checks whether KF.W5 already dissolved `Capturing...`. **SHARED DIRECTORY DECLARED (RULINGS-2 R2-12, the collision round 1 left silent): `test/demo/instrument/` is written by BOTH waves and OWNED by NEITHER.** KF.W5's `.a` seat creates exactly `test/demo/instrument/highlight-css-roundtrip.test.ts`; this wave creates exactly the four G11 fixtures named in §Bounds; the nine files already tracked there are read-only witnesses to both. **Filename disjointness holds and is asserted from this end; the reciprocal naming of these four filenames is owed by KF.W5's §Disjointness** (same ruling, both ends). Neither wave's `**` glob survives — paths are enumerated, never globbed by shared prefix.
- **KF.W8** (feeds — **and this wave GATES one of its creates**): L-19/C-14 dead barrel, C-12 private-`src/` reach, N-11 colocation, caret m-7 rename, MISS-β2 (the serializer triple-copy; the surviving locus of C-1's killed float story), KF-AV-24 seam consolidation. **G15 DEPENDENCY DECLARED FROM THIS END (RULINGS-3 §END DH-3 / PASS-3 check D3's compounding cell)**: `KF-W8.md` **§Bounds → the `test/demo/instrument/CSSPasteDialog.test.ts` and `KeyframesAddDialog.test.ts` create rows** author a characterization pair whose shape it makes conditional on this wave's ruling — a hard dependency on **G15**'s fold-or-decline ruling, undeclared at this end until round 3. ⟨**FORWARD REFERENCE, re-cut at repair round 4 under LAW C(3)**: KF.W8 writes LATER than this seat in the round's stage order, so its sentence may not be quoted here — the round-3 text carried W8's prose (*"against whichever shape KF.W7 rules"*) as a same-round receipt, which is the U4-C mechanism. **The citation is now the stable anchor alone** — the §-heading plus the two create rows by filename, no quoted prose and no line numbers — and the **RECONCILE seat verifies it against W8's post-round-4 bytes** after the last per-wave write. The DIRECTION is unaffected and is this wave's own declaration, not a quotation: G15's ruling precedes KF.W8's characterization pair.⟩ **G15's ruling therefore precedes KF.W8's G12 pair**, and the ruling's output — fold landed (one 80-line shell) or fold declined (two shells, L-4's falsifier answered in writing) — is the shape those two files characterize; if the fold lands, `KeyframesAddDialog.vue` may not survive as a mount target at all (see the LAW A census at P8's R-7). **SHARED SURFACES, all three declared — ANCHOR-ONLY at the W8 end (re-cut at repair round 5, PASS-5 check D1/D12 applied BY SCOPE, not by named instance: this was the third of the three convicted cites and the two clauses beside it are the same class)**: `test/demo/instrument/` — **this wave's footprint is its four named creates and nothing else**; W8's footprint is named at §Bounds' four-party table as an anchor ⟨`KF-W8.md` §Bounds⟩, with no verb, glob or count restated here. `package.json` — this wave books **one line, `+@vue/test-utils`**; **W8 also claims this file** ⟨`KF-W8.md` §Bounds → its `package.json` row⟩, and the narrowing of that claim to the same single add is **RULINGS-3 R3-8.1's**, read at the ruling — an immutable artifact — rather than off a sibling that writes after this seat; the two must not race (first-lander writes, second consumes and asserts presence; §Bounds). `vitest.config.ts` — **this wave performs no edit** (the row is STRUCK at §Bounds); the registration is KF.W4's booked act, and W8's own claim on the file is an anchor ⟨`KF-W8.md` §Bounds⟩. ⟨The struck text asserted *"W8 carves all nine tracked files + `resize-tracks.test.ts` and creates four"* and *"W8's carve is narrowed … W8 carves it after KF.W4"* — three forward access claims in one sentence. **None of this wave's own acts changes**; what changes is that none of them is now premised on a sibling's unwritten bytes.⟩
- **KF.W10** (this wave FEEDS its close; **the edge was one-ended and is declared here — RULINGS-3 R3-10.3, the W10-seam class cured at its SOURCE**): round 2 carried **zero** `KF.W10` references while W10 carried three obligations naming this wave, so W10's terminal table was assembled against silence. What W10 asserts: `KF-W10.md` **§3.2 packet-locks list → the `KF-AV-28 · STANDING SUPERSESSION RIDER (KF.W7's defining lock)` bullet** — the seventh lock — carries the rider *"carried, not cited"*, banked at `kf-AnimationVisualizer.md`, `kf-PlaybackRibbon.md` and `kf-SequenceScrubber.md`, and binds it **as a SWEEP INPUT to G-2** over the three evaluated surfaces plus the drag-seam and transport/ribbon packets; **`KF-W10.md` §G-2's Acceptance clause** admits **`DISCHARGED by KF.W7 SWAP verdict <surface>, <date>`** as a lawful terminal word beside LANDED / KILLED-with-rationale / carried, and forbids LANDED or KILLED on any governed row whose surface verdict is outstanding. **THE OUTPUT THIS WAVE DECLARES, so W10 consumes a produced artifact rather than silence**: per governed surface, this wave emits the SWAP-verdict receipt in exactly that alphabet form (§Verdict Protocol's rider mechanics, adopted verbatim this round), dated to the verdict-table commit; a governed row this wave does NOT discharge is stated as still-governed so W10 carries it naming the pending surface. **Direction**: KF.W7's verdict table PRECEDES W10's G-2 sweep; W10 depends. Nothing here schedules W10 or stamps any of its gates.
- **KF.W11 arm (d)** (this wave GATES) ⟨**ADDRESSEE NAMED AT REPAIR ROUND 5 (PASS-5 check D10, by scope)**: KF.W11 is **MINTED-UNAUTHORED** (RULINGS-4 R4-8) and has no spec file at this clock, so this edge is addressed to `KF-W10.md` **§6.D · SUCCESSOR-FORMATION REGISTER → the `KF.W11 · Demo Scene Repair` row** (the sequence and square packets are named in that row's inbound cargo) — **stable anchor alone per LAW C(3), W10 writing last**. This wave promises no reciprocation from an unauthored spec; the gating is this end's own declaration, binding on whichever seat authors that formation⟩: SequenceScrubber shape rows (C·C-4 (SequenceScrubber)'s contract question; D-5/D-6/D-11's "the primitive already solves it") discharge or persist with G1's verdict; the arm's spine and banked packets proceed independently — declared so the formation authors without blocking. SquareScene MISS-1 (arm c) shares grabDx's class.
- **KF.W13** (this wave GATES) ⟨**ADDRESSEE NAMED AT REPAIR ROUND 5 (PASS-5 check D10, by scope)**: KF.W13 is **MINTED-UNAUTHORED** — minted explicitly at KF-W4 §Sequencing per RULINGS-4 R4-8 — and has no spec file at this clock, so this edge is addressed to `KF-W10.md` **§6.D · SUCCESSOR-FORMATION REGISTER → the `KF.W13 · Chrome, Dock & Transport Repair` row** (the dock-menu and transport/ribbon packets are named in that row's inbound cargo) — **stable anchor alone per LAW C(3), W10 writing last**. No reciprocation is promised from an unauthored spec; the locks below are re-affirmed from this end and bind whichever seat authors that formation⟩: the drag-seam packet (KF-SCR-1 + L·D-1 + KF-AV-15; shares the grab-offset arm and the GradientStopEditor precedent) and the transport/ribbon packet (which carries this wave's rider AND supplies its counter-evidence). Locks re-affirmed from this end: **TD-1/TD-2's cures BUNDLE**, and the TD-2+TD-38+TD-40 and TD-21+TD-41 commits MUST NOT split; **kf-ChromeDock M-4's worded cure ships an unopenable menu without the MbabbMenu MUST-CARRY rider** — the rider rides wherever M-4's cure lands.
- **EDITOR-UNIT** (must land with this wave's half): KF-CE-2's child half (cancel-on-external-write + cancel-on-unmount, one `replaceContent()` seam); neither half alone closes the data loss; EDITOR-UNIT itself sequenced after KF.W4's gate. ⟨**Addressee named at repair round 4 (RULINGS-4 §END KF-W7 D9, applied by scope — the third member of the unauthored-packet class alongside KFED-UNIT and CARD-UNIT)**: EDITOR-UNIT is a NO-WAVE-OWNER packet with no spec file at this clock, so this edge is addressed to `KF-W10.md` **§6.D · SUCCESSOR-FORMATION REGISTER → the KF.W12 row** (EDITOR-UNIT is named in that row's inbound cargo) — stable anchor alone per LAW C(3), W10 writing last. **This wave promises no reciprocation from an unauthored spec**; the "must land together" constraint is a statement of the data-loss mechanism, binding on whichever seat authors the child half, and it is not a scheduling claim over KF.W12.⟩
- **glass-ui BH relay (SS-6)** (feeds, NEVER patches locally — owner edict: every producer-level change relays to the active BH inbox): C-15's `/timeline` export gaps; TooltipContent's non-consumption of `--reka-tooltip-content-available-height` (the D-15 half of the THP layout pair); KF-SCR-2 (the void italic register); KF-AV-32 (mutually-disarming `useTouchGate` pair); the `cn` padding-group seam is ALREADY relayed (TEP-12/MCP-31 family) — not re-filed.
- **KF.W9 / SS-13** (feeds): the residue lists (KT 13 rows, TT 12, caret 12, THP 11, SequenceScrubber 12), forced-colors D-21 and RTL D-24 whole. **Armed triggers this wave honours, never pre-decides**: (a) shift-wheel `deltaY: 0` ⇒ M-7 + RR-B missed-4 (joint) jointly MAJOR; (b) `document.activeElement` ≠ input after caret click ⇒ m-1/L-10 MAJOR + MISS-α2 reachability. **Witness-refresh obligation: re-run `npm run gh-pages` before trusting any byte-offset receipt** (the Jul-16 CSS witness may predate glass-7); installed-dist token/cascade cells exempt. ⟨**Phantom script struck — RULINGS-2 R2-5(f), extending R-11's enumeration to this, its eleventh site.** The round-1 text named `build:gh-pages`, which exists at no coordinate: re-derived this seat at `origin/master 81a56990` — `git show origin/master:package.json` carries `"gh-pages": "vite build --mode gh-pages"` at `:43`, and `git grep -n "build:gh-pages" origin/master` → **0 hits**. A seat executing the round-1 obligation got `npm ERR! Missing script: "build:gh-pages"` — load-bearing here, because this is the precondition attached to EVERY byte-offset receipt in the wave, and three of this wave's four core records (kf-KeyframeTimeline · kf-TimelineCaret · kf-TimelineTrack, plus kf-CSSPasteDialog) are the records whose byte-offset trust hangs on the refresh.⟩
- **value.js lane (D-23)**: C-13 recorded as blast-radius input inside the record group (nothing cured); TT's negative result recorded so parser waves SKIP the file (the sole emit→parser path is awaited inside the try, useTimelineBuild.ts:40-50 — also why RR-B missed-2 is churn, not crash).
- **Taxonomy reconciliation** (a naming schism resolved, not an edge): three pre-census records use lane-frontend §10 naming where "KF.W7" = the TOKEN-NAMESPACE audit — their grep hits are NOT this wave's (§Excluded). Conversely, **"KF.W6-TIMELINE" references ARE this wave and are carried above as hard-ordering locks — the rule applied EXHAUSTIVELY, over all THREE rows in TWO records that carry the disposition, not one** ⟨*"BOTH rows"* **STRUCK at repair round 4 (RULINGS-4 §END KF-W7 D7)**: the round-2 headline was written when the count was two; the round-3 receipt inside this same bullet moved it to **three dispositions in two records** (D-1 · L-2/C-2 · C-8/D-6) and re-stated *"three rows, two records"* — and the headline did not follow. A seat reading the bold clause and stopping there under-counts the very set OP-0 exists to protect, which is the whole harm of an internal contradiction surviving its own receipt. **Both commands re-run by this seat at write time (LAW D(3)), outputs unchanged**: the exhaustive sweep → **8 hits / 4 records** (`kf-AnimationControlsGroup` 2 · `kf-ChannelControls` 4 · `kf-ControlsPaneWrapper` 1 · `kf-DemoGlobalChrome` 1 = 4 preambles + 1 tally + 3 dispositions); the disposition filter → `kf-AnimationControlsGroup.md:42` · `kf-ChannelControls.md:40` · `kf-ChannelControls.md:52`⟩ ⟨RULINGS-2 R2-9. **RECEIPT REPLACED AT THIS REPAIR ROUND (RULINGS-3 §END D7): the round-2 command did not return what its reading claimed, and under R2-7's own law a receipt that returns a different row is not a receipt.** The struck command — `grep -rn "before \*\*KF.W6-TIMELINE\*\*\|before KF.W6-TIMELINE" registry/adjudicated/*.md` — returns **three hits in ONE record** (`kf-ChannelControls.md:40`, `:52`, `:155`), because (a) `kf-AnimationControlsGroup`'s literal is `must be settled **before** KF.W6-TIMELINE` — the bold markers sit BETWEEN `before` and `KF`, so neither alternation can reach it — and (b) the third hit is that record's closing-verdict tally line, not a disposition row. A seat re-running it would land on one record and conclude ACG D-1 escaped again — the very event OP-0 exists to prevent. **Two commands replace it, both executed by this seat at the value.js registry bytes, outputs pasted:** **(i) the exhaustive sweep** `grep -rn 'KF.W6-TIMELINE' registry/adjudicated/*.md` → **8 hits over 4 records**, decomposing as **4 routing-law preambles** (`kf-ChannelControls:17` · `kf-ControlsPaneWrapper:6` · `kf-AnimationControlsGroup:17` · `kf-DemoGlobalChrome:17` — lane-taxonomy declarations, not routings), **1 closing tally** (`kf-ChannelControls:155`), and **3 DISPOSITIONS**. **(ii) the disposition filter, which returns those three and nothing else** — `grep -rniE 'disposition:.*KF\.W6-TIMELINE' registry/adjudicated/*.md` → `kf-AnimationControlsGroup.md:42` (**D-1**, *"Terminal disposition: **NO-WAVE-OWNER**, must be settled **before** KF.W6-TIMELINE"*) · `kf-ChannelControls.md:40` (**L-2/C-2**, *"Terminal disposition: **NO-WAVE-OWNER** (SS-1/SS-2 spec input), and it MUST be settled before **KF.W6-TIMELINE**"*) · `kf-ChannelControls.md:52` (**C-8/D-6**, *"Disposition: **NO-WAVE-OWNER**, same expanded-timeline spec family as D-1 (the structure and the portal must be settled together, before KF.W6-TIMELINE)"*) — **three rows, two records**, per-file counts `kf-AnimationControlsGroup:1` / `kf-ChannelControls:2`. The three are the same three the round-2 prose named, and all three are booked here: **L-2/C-2** at OP-1, **C-8/D-6** in the expanded settlement, **D-1** at OP-0. The conclusion was independently true; only its receipt was false, and it is the receipt that a later seat runs. Round 1 applied the rule to the first record only and named kf-AnimationControlsGroup in the same sentence for its taxonomy line alone — so the corpus's other restored BLOCKER escaped by bytes. It does not escape now.⟩ The same record's S-3/S-4 (*"wants its own spec"*) and its **D-12** are unaffected: **D-12 stays EXCLUDED** as a naming-schism hit (§Excluded) — one record, two dispositions, decided per row by mechanism, never per file.

---

## §Excluded

**Census carriage and non-carriage are NOT asserted in this spec's voice (LAW B, `RULINGS-3.md`) — the claim lives in the conformance artifact and is cited by path.** ⟨The round-2 opener *"No registry-routed row is unaccounted"* and the figure *"150 routed · 150 accounted · 0 escaped — 144 CARRIED + 6 EXCLUDED-WITH-NAMED-OWNER"* are **STRUCK in spec voice**: this is the fourth consecutive generation in which an enumerated set was cured perfectly and a spec-voiced closure sentence over it was falsified at the next pass. The round-1 composition *"145 carried + 3 excluded"* was already false against the six bullets two lines below it and its denominator already missed **kf-AnimationControlsGroup D-1** (now carried at OP-0); the round-2 restatement fixed the composition and left the claim in the wrong voice. Both are recorded as the dated log, not re-asserted.⟩ **The live claim, re-pointed at repair round 5 (RULINGS-5 §END KF-W7 D3 — ONE artifact of record, swept at every site in one act so the freshest-citation class cannot split this file)**: per **`docs/tranches/X/keyframes/conformance/PASS-5/KF-W7-CHECK.md` §1 (2026-08-28)** — an independent id-keyed re-enumeration from the 58 records' bytes, inheriting nothing from PASS-4 — **135 routed · 135 booked · 0 escaped by bytes**, every routed row resolving to a carried row, an §Excluded bullet with a named owner, or a named precondition; the same artifact's §3 recorded **12 defects (6 MAJOR · 6 MINOR)** against this spec, all addressed at repair round 5. ⟨**Dated log, superseded, kept as the log**: PASS-4 read **136 · 136 · 0**, PASS-3 **138 · 138 · 0**. The **−2** against PASS-4 is the roster/non-roster BOUNDARY question — **one double-counted line** (`kf-SpringTarget.md:69`) and **one routing-law preamble** (`kf-PlaybackRibbon.md:36`) — **and it agrees with `:13`'s `9 − 2 = 7` exactly** ⟨***reconciled at repair round 6, PASS-6 D1: the round-5 file's two census cells disagreed — `:13` computed −2 +1 = −1 while this cell enumerated TWO removals under the label −1. Two cells, one quantity, two answers.***⟩ — enumerated row by row at the verb table's SPECIFIED cell; the earlier −2 was the membership question cured at round 4. **Neither is an escape: carriage is clean under every reading, and every pass since PASS-3 has certified 0 escaped by bytes.**⟩ The counting rule under which **135** reproduces — including the routing-summary-tail discriminator written at round 5 — is stated at the verb table's SPECIFIED cell, at its enumeration, with its command. When a later pass supersedes that artifact, **this citation is re-pointed — the claim is never re-asserted here**. The bullets below are the explicit non-carries, each with its reason, and they are the artifact's §1b/§1c inputs, not a substitute for it:

- **KAD-15 · KAD-16 · KAD-20 (kf-KeyframesAddDialog)** — **exclusions-with-named-owner: KF.W6** (RULINGS R-12). All three terminate `**→ KF.W6/W7**` in `kf-KeyframesAddDialog.md` (its MINOR/fold tail) and all three are ALREADY BOOKED at the W6 end. **Anchors re-derived by THIS seat at the current bytes and re-cut to the stable-anchor idiom (RULINGS-2 R2-7; the round-1 anchors `KF-W6.md:250`/`:320` had drifted +15 lines under a sibling repair and returned different rows — a receipt that returns a different row is not a receipt)**: **KAD-15** rides KF.W6's **`S-10 · KF-KE-21 (+KAD-15)`** row — the progress-bar row whose cure is census S-10, glass `Progress` (**numeric convenience STRUCK at round-2 close (U-2/R2-7)** — `KF-W6.md:265` had drifted again onto W6's S-5 `KF-AT-*` roster and is retired, not re-issued; **the banked row header `S-10 · KF-KE-21` is the sole anchor at BOTH ends** — the CARRY half `KF-W6-CARRY.md` **§S-10, row `S-10 · KF-KE-21`** re-verified present at this close); **KAD-16 and KAD-20** ride KF.W6's **`KAD-3 + KAD-7 + KAD-19 + KAD-20 + KAD-25 + KAD-16 + KAD-F1 + KAD-F4 + KAD-F5`** group row — the S-9 Textarea/well-swap group (**numeric convenience STRUCK at round-2 close (U-2/R2-7)** — `KF-W6.md:335` was blank at this close and is retired, not re-issued; **the banked group-row header `KAD-3 + KAD-7 + KAD-19 + KAD-20 + KAD-25 + KAD-16 + KAD-F1 + KAD-F4 + KAD-F5` is the sole anchor at BOTH ends**, and the CARRY half re-verified present at this close; same convention). Both W6 ends verified present at the bytes by this seat; both CARRY halves verified present. Reason each is W6's and not this wave's, by mechanism: **KAD-15** is the progress-bar animator fold whose cure "rides `design-idioms.css`'s owner" (the record's own words) — a token/idiom-owner act, and `design-idioms.css` is outside §Bounds; **KAD-16** is the dead-token cluster (literal `class`, inert `sticky bottom-0`, `type="submit"` with no `<form>`) which the record routes to "die in the fold/swap" — the S-9 Textarea/well swap is KF.W6's; **KAD-20** is the no-empty-state row, a member of that same swap-discharged group. **Binding declared to KF.W6 from this end**: KAD-16's `grid`-defeats-`DialogFooter`-flex cell and KAD-15's footer-placement cell are the *structural root* of the fold direction this spec pins at R-7 (adopt CSSPasteDialog's a11y composition + the twin's text-hoisting contract) — so if KF.W6's swap discharges them, R-7's fold inherits a bare footer; if the swap is declined, the two structural cells reach the fold un-cured and R-7's execution seat must say so. G15's discharge discipline names all three by id (below) so the statement is executable; **no cure for any of the three is spent by this wave.**
- **kf-DemoGlobalChrome M-4, kf-AnimationControlsGroup D-12, kf-ControlsPaneWrapper D-m3** — naming-schism grep hits; they belong to the census taxonomy's KF.W6 `--kf-*` token-namespace audit, not this wave (taxonomy reconciliation, §Sequencing). ⟨**Identity guard, since the same record now sits on both sides of the line**: `kf-AnimationControlsGroup` **D-12** is excluded HERE (a `→ KF.W7-TOKENS` routing under the schism naming), while `kf-AnimationControlsGroup` **D-1** — the portal BLOCKER — is CARRIED at **OP-0**. One record, two rows, two dispositions, each decided by mechanism. Round 1's single `grep -n "AnimationControlsGroup D-1"` returned only this bullet's D-12 and read the record as fully disposed; the two ids are one character apart and that is precisely why each is named with its rung and its disposition here.⟩
- **The R1 crash identity** — FOLDS to the banked megatranche R1 row (KF-CE-12 precedent), never re-booked; only C-7's posture arm and the §F-2 scope correction book here.
- **KAD `innerHTML` DOM-XSS BLOCKER** — KF.W5's, front-loaded ("one line, `el.textContent = s`"); this wave does not gate on it and must not wait on the R-7 fold for it.
- **Typography/token cure family** (KT D-5/D-6/D-7/D-8/D-9/D-14/D-18/D-20/M8/M10; TT D-3/D-5/m-17/RR-A missed-5's style arm; caret C-7/D·M-3/D·M-5/D·M-7/C-4·D·M-9/MISS-α3/MISS-α6/m-2; THP D-4/L-D7/MISSED-2/D-5/D-11/D-17-type; KF-SCR-2's demo side) — KF.W6's, bound by the declared bindings (MISSED-3 chief among them).
- **Structure/colocation rows** (L-19/C-14, C-12, N-11, caret m-7, MISS-β2, KF-AV-24) — KF.W8's, edges declared.
- **Forced-colors coverage (D-21) and RTL (D-24)** — KF.W9 whole; only D-8 (TimelineTrack)'s AT-state rider books here.
- **C-3 (SpringTrace)'s implementation** — library-side; this wave takes the DECISION into G1's precedent frame and declares the implementation edge to KF.W5/KF.W8 (the row is carried; its code is not this wave's).
- **KF-AV-13's cure and the `canRemove`/confirm-or-undo cure (KF-KC-11)** — NO-WAVE-OWNER cures; the DECISIONS are taken here (G1 table; the undo-authority ruling), the code lands with the owning packets.
- **Killed claims, never to re-enter any spec** (carried as locks, not rows): C-1's float story (K-7); m-13's latch escalation (K-8); L-9's fallback-dead-code (K-7); L-D10's escape arm (K-3); the `{}` frequency claim (K-4); L-m-5's ~102-tick magnitude (K-6); D-m1's transform-eases consequence (K-5); K-1's two manufactured Delete harms; the "800px/~600px rail" fictions; the caret Blink-no-op assertion (#11); the caret Card-clip truncation (#13); ARB-1's earlier "drag is clamped" claim; killed-claim #15's re-derivation of C-15; C·C-4's `{progress}`-prop-plus-emits cure (K-8); the `aria-selected`-on-a-div harvest (killed-claim #6); L-D8's slot-laziness perf arm (withdrawn, stays withdrawn).
- **KF-CO-1/KF-CO-8** — no KF-CO row is routed to this wave by the registry; their cures are SEQUENCED by LP-1 (no write→render edge) in the owning wave's spec; noted here so the grep trail shows the lock considered, not dropped.
- **SS-13 residue probes themselves** — routed, not executed here (probe parsimony edict); this wave consumes their verdicts via the two armed triggers only.

---

*Fold-repair pass (same seat class, 2026-08-28): exact **registry** ids restored to every row header (grep-verifiable against `docs/tranches/V/megatranche/registry/adjudicated/`, the only source this spec is provenanced on); PR-CAUTION and L-15-PROTECTED named — **and, at repair round 1, re-declared as SPEC-LOCAL LABELS bound to their banked coordinates (§Carry preamble)**; D-3 (percent readout a11y), D-14/i-1, and the KF-AV-14/KF-AV-17/KF-AV-24 rider substance landed whole (incl. the translateX(calc(var(--p) * (100cqw − 100%))) cure-shape lock); OP-2's `check`-script citation corrected against origin/master; OP-4 widened to the missing vitest Vue-SFC plugin with its bounds row; timelineEngine count corrected 104→102; the useZoomPan `:59` anchor corrected; route dissents recorded on SpringTrace C-3 / SpringTarget C-4 (M-25 route-by-mechanism); the LP-1 no-write→render-edge rider attached to N-10.*

*Authored as KF.W7's agglomerated spec by the fresh-Fable fold seat **from the 58-record registry directly** (two drafts consumed choosily: A's structure and carry program; B's preconditions, measurements, intake rows, disjointness, and fold gate). Status: planned. Fifteen gates, born-RED, none stamped — witness soundness per **`conformance/PASS-5/KF-W7-CHECK.md` §0 axis 4 + §2 (2026-08-28)** — **every sampled frontier anchor re-executed and reproducing exactly (11 of 11 §Bounds counts · 9 of 9 gate witnesses), all 15 gates born-RED with reachable green** — not asserted here (LAW B) ⟨cite re-pointed at repair round 5, RULINGS-5 §END KF-W7 D3, applied by scope: this was the fourth and last live PASS-4 citation in the file, swept in the same act as the other three so no two cells of this spec name different artifacts of record for one claim — the freshest-citation split is R5-9's own convicted class. The PASS-3 and PASS-4 citations that remain sit inside the dated round-1..4 logs, which E-3 preserves as history and never rewrites⟩. No gate has run; no product source was opened. Execution awaits the owner's begin-word.*

*Repair round 1 (X·KF L-20, 2026-08-28) — sole write = this file; status stays `planned`, no gate stamped, no product source opened, every carried row keeps its banked id. Applied: **RULINGS R-12** (all five phantom "KF.W7 CARRY ledger" bindings struck at the verb table, §Bounds, §Excluded's opener and both footers — provenance restated over the registry; **KAD-15 · KAD-16 · KAD-20 booked in §Excluded as exclusions-with-named-owner KF.W6**, both W6 ends re-verified by **banked row header** at both ends — the `KF-W6.md` coordinates `:250`/`:320` and the CARRY coordinates `:178`/`:248` are **retired at round-2 close (U-2/R2-7); only the row headers carry these receipts now** ⟨**those two `KF-W6.md` numbers DRIFTED within round 1** — a sibling repair added ~15 lines and they now return different rows; re-cut to stable anchors at §Excluded, round 2, per R2-7. Recorded, not rewritten: this is the dated log of what round 1 did⟩, with the R-7 fold-direction binding declared and G15's discharge discipline naming all three by id) · **CHECK D1** (cured by R-12, the ruling's shape) · **D2** (cured by R-12) · **D3 + D4** (G15's third and fourth witnesses RE-FOUNDED on commands that return what they claim: `initialText` is dead at the MOUNT, four in-file hits notwithstanding; N-2's two occurrences need two commands — L-19 restored, the gate still born-RED for its true reason) · **D5** (one reading of the runnability precondition: G12 pins for all · G11 gates the ten behavioural gates G2–G10 + G13 · G1/G14/G15 are documentary and wait only on G12; §Sequencing 2's scope re-stated as COMMITS) · **D6** (the two coinages disclosed as spec-local labels bound to `kf-PlaybackRibbon.md:36` and `kf-KeyframeTimeline.md:125`) · **D7** (G1's shipped-but-un-re-exported enumeration completed to seven at the bytes; C-15's split verdict carried unchanged, killed-claim #15 lock intact). Census after that repair: **148 routed · 148 accounted (145 carried + 3 excluded-with-named-owner) · 0 escaped** — ⟨**CORRECTED 2026-08-28 (repair round 2)**: this figure is STRUCK. Its composition was false against §Excluded's own six bullets (the true round-1 split was 142 + 6), and its denominator missed both the dual-homed **MISSED-3** and the escaped BLOCKER **kf-AnimationControlsGroup D-1**. The census of record is the round-2 line: **150 routed · 144 carried + 6 excluded-with-named-owner · 0 escaped.**⟩ ⟨**SUPERSEDED AGAIN, repair round 3 (2026-08-28)**: the round-2 line is itself struck as not re-derivable (RULINGS-3 §END D5), and **no census figure is stated in this spec's voice at all** (LAW B). The live claim is the citation at the verb table's SPECIFIED cell and at §Excluded's opener — `conformance/PASS-3/KF-W7-CHECK.md` §1. This whole bracket is the dated log of two superseded figures; **a later seat cites the artifact, never any number in this paragraph.**⟩ No ruling addressed to that wave was declined; R-3 and R-10 both direct "KF-W7: no change" and none was made.*

*Repair round 2 (X·KF L-20, 2026-08-28) — sole write = this file; status stays `planned`, no gate stamped, no product source opened (every probe below is a read-only `git` command), every carried row keeps its banked id, every lock, rider and dissent carried forward unchanged. **All four RULINGS-2 directives addressed to KF-W7 applied, none declined**: **R2-9** (kf-AnimationControlsGroup **D-1** booked as **OP-0**, the opening hard precondition — census row Q flips E→B; §Sequencing's hard ordering now opens with it; **C-6's AnimationControlsGroup arm and G2's restatement CONDITIONED on the portal settlement**; the taxonomy rule extended exhaustively over both records that carry the "before KF.W6-TIMELINE" disposition; census restated **150 · 144+6 · 0**) · **R2-12** (§Bounds' `test/demo/instrument/**` create-glob STRUCK → **four literal fixture filenames**; the **nine tracked files declared READ-ONLY WITNESSES by name**, `timeline-undo.test.ts` and `resize-tracks.test.ts` called out; §Disjointness names KF.W5's `highlight-css-roundtrip.test.ts` and asserts filename disjointness; the KF.W5 cross-edge declares the shared directory and the reciprocal naming owed) · **R2-6.5** (**G11 re-cut to two commands**: the `test/` coverage leg → 0, which is the gate's claim; the `demo/` leg restated separately as the 13-hit render-site enumeration, printed) · **R2-5(f)** (`build:gh-pages` → **`npm run gh-pages`**, R-11's eleventh site). **Local check defects D1–D9 all cured** (D1/D3 by R2-9 · D2 by R2-5(f) · D5 by R2-6.5 · D6 by R2-12 · D4 by the stable-anchor re-cut of the two W6 receipts, R2-7 · D7 by striking the hardened paraphrase and giving both true quotes, R2-7 · D8 by restoring the **X·KF** scope word with the two fourier carries named · D9 by stating the dual-homed counting rule and adopting the 150 denominator). **WITNESS RE-DERIVATION LAW (R2-5) honoured**: every witness, anchor and gate this seat touched was re-executed at the substrate of record — keyframes.js `origin/master` **`81a56990`** (package.json `:43` `"gh-pages"` + 0 hits for `build:gh-pages`; the G11 two-leg split 0/13 with all 13 sites enumerated; `git ls-tree` → the nine tracked fixtures) and the value.js tree at this seat's clock (kf-AnimationControlsGroup D-1's verbatim disposition; the three-row `before KF.W6-TIMELINE` sweep; kf-ChannelControls' and kf-KeyframeTimeline's two true quotes; KF-W6's two live KAD rows and both CARRY halves; the three tranche-X carry files). ~~**Nothing was inherited from the rulings' prose, the check's prose, or this spec's own prior text.**~~ ⟨**DATED CORRECTION, repair round 3 (2026-08-28): that sentence is FALSIFIED and struck.** The PASS-3 fresh check (`conformance/PASS-3/KF-W7-CHECK.md` §3 D2) proved three anchors in G5 / §Carry N-6 / §Carry MISS-β1 — `timelineEngine :40-48` · `the :42 comment` · `build :51-53` / `export :62-72` — were carried from `kf-KeyframeTimeline.md`'s C-1/N-6 cell and never re-run; they resolve at **no ref**, and MISS-β1's spelling had the two coordinates transposed. A footer that certifies a universal its own receipts do not reproduce is the LAW-B-corollary defect, committed while legislating against it. The honest statement of round 2 is: *every witness round 2 **re-cut** was re-executed at `81a56990`; the witnesses it did not touch were not.* All three anchors are re-derived, with commands and outputs pasted, at repair round 3.⟩ E-3: the registry is untouched; no `proof:*` invention; every gate remains born-RED.*

*Repair round 3 (X·KF L-20, 2026-08-28) — sole write = this file; status stays `planned`, no gate stamped, no product source opened (every keyframes.js probe below is a read-only `git show` / `git grep` / `git ls-tree`, plus two `ls -d node_modules/…` existence reads), every carried row keeps its banked id, every lock, rider and dissent carried forward unchanged. **All four RULINGS-3 directives addressed to KF-W7 applied, none declined**, under **LAW A** (import-graph censuses) and **LAW B** (the closure-claim ban), both binding every edit:*

- ***R3-10.3 — the W10 seam closed at its SOURCE end.*** *§Sequencing gains a **→ KF.W10** cross-edge (round 2 carried `KF.W10` ×0 against W10's three obligations naming this wave), citing W10 by stable anchor — §3.2's `KF-AV-28 · STANDING SUPERSESSION RIDER` bullet (the seventh packet lock, bound as a SWEEP INPUT to G-2) and §G-2's Acceptance clause — and declaring the produced output. §Verdict Protocol's rider mechanics adopt W10's terminal alphabet **verbatim**: `DISCHARGED by KF.W7 SWAP verdict <surface>, <date>`, with `<surface>` drawn from the six §Verdict rows, so W10's terminal table consumes a declared artifact instead of silence.*
- ***R3-8.3 + §END DH-2 — the cost model corrected and the two manifest collisions declared at four ends.*** *The `+@vitejs/plugin-vue` add is **STRUCK as a no-op** (re-measured this seat: `package.json:81` `^6.0.7`, installed; `vitest.config.ts` declares no `plugins` key) — the residue is **registration**, which is **KF.W4's commit 2**, consumed here as an inbound dependency-cite. OP-4, §Bounds and G11's witness block all restated; the `ls node_modules/@vue/*` enumeration corrected to its **nine** real entries. **`vitest.config.ts` bounds row STRUCK — this wave writes no byte of it** (DH-2's first branch), which makes the KF.W4 edge's `"for VERIFICATION not authoring"` scope sentence true for the first time. `package.json` narrowed to the single `+@vue/test-utils` line, its row naming KF.W3's `:70` re-pin, KF.W4's scripts+devDeps, and KF.W8's R3-8.1-narrowed carve, with the idempotent-single-add ordering stated.*
- ***§END DH-3 — the shared directory declared at FOUR ends.*** *§Bounds gains the claimant table (W7's 4 creates · W5's 1 · W4's 2 + `resize-tracks.test.ts` · W8's nine-file carve + `resize-tracks.test.ts` + 4 creates); `"Neither wave owns the directory"` is restated as the four-party sentence; the READ-ONLY-WITNESS declaration is scoped to this wave's seats and carries the outward invariant instead of a prohibition (**a witness whose bytes moved under a gate is re-executed before the gate is read**); `KF-W4.md` §Disjointness's nine-vs-ten contradiction is named, its correction routed to the W4 seat; **KF.W8's G15 dependency** (`KF-W8.md` §Bounds' characterization pair, authored *"against whichever shape KF.W7 rules"*) is declared from this end.*
- ***§END D5 + LAW B — the census re-voiced.*** *The `150 · 144+6 · 0` figure is **struck as not re-derivable**; the claim now cites `conformance/PASS-3/KF-W7-CHECK.md` §1 (2026-08-28) — **138 routed · 138 booked · 0 escaped**, with its own 10 defects stated — and the five-part rule (record-set boundary · membership · granularity · dual-homes · rider-governed rows) is written out so the figure reproduces. §Excluded's opener, §Carry's anchor-cleanliness sentence, §Gates' fifteen-witness blanket and the closing footer are converted to the cited form; the round-2 footer's `"Nothing was inherited"` universal is **struck as falsified** by its own D2.*
- ***§END D2 · D7 · D8 · D9 · (c)(4).*** ***D2***: G5's witness and §Carry N-6 / MISS-β1 re-derived at `81a56990` — keyed **`:38`**, merge comment **`:40`**, property loop `:42`, merge loop `:37-47`, build **`:49-51`**, export **`exportTimelineToCSS:60-70`** (inherits the merge at `:66`); the render loops `TimelineTrack.vue:62-94`/`:97-106` re-executed EXACT; the historical `:37` form stated (at the DISQUALIFIED `8281638c`, cited only as a named measurement context) so the transposition cannot re-enter. ***D7***: the false taxonomy grep replaced by two commands with pasted outputs — the exhaustive sweep (**8 hits / 4 records** = 4 preambles + 1 tally + **3 dispositions**) and the disposition filter returning **exactly those three rows in two records**. ***D8***: `PR-CAUTION` quoted verbatim at the registry bytes, the paraphrase-under-a-verbatim-label struck, subject resolution moved outside the quotation. ***D9***: G10's `<cache module>` resolved to `KeyframeTimeline.vue` (`previewCache` `:217`, read `:221`, written `:226`, passed `:81`; `grep -n delete` → **0**). ***(c)(4)***: both spec-local labels re-cut to record + section + banked id (`kf-PlaybackRibbon.md` §Adjudicated defect roster → Routing law → the KF-AV-28 rider clause; `kf-KeyframeTimeline.md` § kill register → entry `K-8 · L-15`), at all three binding sites, line numbers demoted to parentheticals.*
- ***LAW A — two import-graph censuses, executed at `origin/master 81a56990`, commands and outputs pasted at the acts they authorize, sourced from no CARRY row and no prior pass.*** *(1) **`flattenVars.ts`** (§Bounds delete-candidate): specifier census over every relative spelling and every `vite.config.ts`/`tsconfig.json` alias root → **1** import (`useTimelineBuild.ts:14`); symbol census over its sole export → 4 hits resolving to **one consumer module**, the call inside `loadPreset` (`:163-183`); ~40 `docs/tranches/**` hits recorded as **non-import context**; the U-loop `SPEC-B2.md:229` two-consumer row shown **stale** and cited as history only; the binding consequence added (`loadPreset` is re-exported at `useTimeline.ts:44`/`:121` and must be disposed in the same act). (2) **`KeyframesAddDialog.vue`** (P8's R-7 fold SOURCE — a delete/repoint act round 2 never censused, censusing only the fold TARGET's two mounts): **exactly ONE live import**, `KeyframesEditor.vue:118` (mount `:75`), one docblock prose mention, all other hits docs prose — and **`KeyframesEditor.vue` is OUTSIDE §Bounds**, so the fold's one repoint site is declared to the KFED-UNIT owner from both ends and required to ride the same atomic act. This is the KF.W6 F-5 failure class, checked before it could be committed here.*

*Repair round 4 (X·KF L-20, 2026-08-28) — **a STAGE-2 seat under RULINGS-4 LAW C**: this file was written AFTER KF.W0–KF.W6's round-4 writes and BEFORE KF.W8/KF.W9/KF.W10's, and every cross-wave receipt below was re-read at that clock rather than inherited. Sole write = this file; status stays `planned`; no gate stamped; no product source opened (every keyframes.js probe is a read-only `git grep`/`git show`); every carried row keeps its banked id; every lock, rider and dissent carried forward — **and two dissents RESTORED**. **All seven RULINGS-4 directives addressed to KF-W7 applied, none declined:***

- ***§END D1 — the census rule reconciled.*** *The **OWNER test now GOVERNS** (rule 5) and the former mention test is restated as its **necessary-not-sufficient corollary** — the two were co-equal and conflicted on four rows, which is why the figure admitted two readings for five generations. The four rows are **named and quoted by command** (KF-AV-13/-14/-24 = rider-governed non-routings; KAD-F3 = fold-deduped to R-7), with the standing fact that **all four are carried either way, so no reading produces an escape**. The LAW-B cite is re-pointed from PASS-3 to **`PASS-4/KF-W7-CHECK.md` §1** at **136 · 136 · 0**, READ from the artifact by pasted command with its mtime, never re-issued (LAW D(3)); the `138` line becomes dated log.*
- ***§END D2 — the silent promotion reversed.*** ***KF-KC-52 restored to INFO***, its bank's rung, measured at the registry bytes (`### INFO` at `:95`; the row at `:100`). Rounds 1–3 carried it at MINOR against this spec's own P7 M9 lock; no re-adjudication is offered, and the cure mass is unchanged because the docblock correction rides inside the undo-authority ruling regardless.*
- ***§END D3 + D6 — LAW D applied to the label, not just the text.*** *The **P-2 cure-caution is re-attributed**: the sentence printed as *"(kf-EasingTarget P-2, verbatim)"* exists nowhere in that record — it is `kf-SpringTarget.md:69`'s **gloss**. Both are now quoted byte-exact by command and **labelled by source**: P-2 whole from `kf-EasingTarget.md:109`, the gloss as the gloss. **MISS-β3 is re-quoted with its scoping parenthetical restored** (`(my grep; KeyframeTimeline's five `@click`s are button-local)`) — dropped without an ellipsis at rounds 1–3, and it is the cell that keeps the lock from reading as refuted.*
- ***§END D4 — the dissent roster carried WHOLE.*** *`kf-TimelineTrack.md` §DISSENT item 1 names **four** members; rounds 1–3 carried two. **D-12/C-12/i-2 gains its preserved RR-A MAJOR** with residue row 6 as its reopening, and **TT D-13's net arm gains its own**, kept distinct from the THP dissent it was folded beneath. The item is quoted verbatim, the three-probes-for-four-members asymmetry **disclosed rather than smoothed**.*
- ***§END D5 — the sixth inbound edge, second end built.*** *KF.W2's registry-before-posture ordering enters as **hard-ordering item 9** and as the **IN leg (b)** of the KF.W2/W3 cross-edge, with W2's two sentences **quoted-by-command at its FINAL stage-1 bytes** (`:710`/`:753`, mtime 17:02:55 — the PASS-4 check read them at `:571`/`:608`, and the drift is the law's own justification). Registration (G14's completion condition) and ordering are no longer conflated, and the no-deadlock direction is stated from W2's own words.*
- ***§END D7 + D8 + D10 — three contradictions closed.*** ***D7***: *"BOTH rows"* → **"all THREE rows in TWO records"**, the headline finally following the receipt beneath it (both taxonomy commands re-run, outputs unchanged). ***D8***: `L-15-PROTECTED` re-bound to the record's **literal heading** *"Killed-claims register — 10 rows, refutations re-executed"* — the round-3 description returned `grep -c` → **0** — at **both live binding sites**; the round-3 dated log keeps its historical wording (E-3). ***D10***: the P0 riders header **verb-split** so KF-AV-17 reads as the **CURE-SHAPE LOCK** it is, with its fold home stated (→ KF-AV-1's cure, itself NO-WAVE-OWNER) and no decision recorded on its behalf.*
- ***§END D9 — a reciprocity that cannot exist is retired, and its addressee named.*** *"From both ends" is struck for **KFED-UNIT** and — **by SCOPE, not by named instance (R4-10(1))** — for **CARD-UNIT** and **EDITOR-UNIT**, the two identical shapes one row and one edge away. All three are NO-WAVE-OWNER packets with no spec file at this clock; their second end is **`KF-W10.md` §6.D · SUCCESSOR-FORMATION REGISTER → the KF.W12 row** (R4-8), cited **by stable anchor alone** under LAW C(3) because W10 writes last. **Nothing is promised**: the constraints stand, addressed to a register row.*

***LAW C compliance (the U4-C class, this seat's own exposure).*** *A **substrate disclosure block** was added at §Sequencing partitioning every cross-wave receipt into BACKWARD (quoted-by-command at final stage-1 bytes, substrate + mtime named) and FORWARD (stable anchor alone). Three live drifts were found and repaired by re-derivation rather than inheritance: **`KF-W4.md:46` had MOVED** under W4's own round-4 write (`sed -n '46p'` now returns a different row) and is re-anchored to its §Bounds row; **the W4 commit-2 quotation was a TRUNCATION** — W4 extended that parenthetical this round — now re-quoted with the elision marked and the addition disclosed; **`KF-W8.md:244` and W8's quoted sentence are struck as forward references** and reduced to anchors. The `KF-W6.md` §Excluded ends were re-verified at W6's post-round-4 bytes **by banked row header and both hold — while their line numbers moved again**, which is the stable-anchor idiom earning its keep for the third round running. The §Verdict alphabet adopted from W10 is **flagged to the RECONCILE seat** for byte-check rather than assumed.*

***R4-10(1) SCOPE RECEIPTS — one line per class, enumeration source = the SCOPE, never the example list:***
- ***ACCESS COLUMN (LAW A): 24 rows enumerated, 2 of 2 censuses present.*** *Command: `sed -n '/^## §Bounds/,/^## §Verdict/p' KF-W7.md | awk -F'|' 'NF>4 && $2 !~ /File|---/ {print $2" => "$4}'` → 24 rows; `| grep -iE 'delete|repoint|shim|move'` → **exactly ONE** member, `utils/flattenVars.ts => **delete-candidate**`, census present at the row. **The column is not the whole scope**: P8's R-7 fold is a delete/repoint ACT on a row whose access reads `modify` (`KeyframesAddDialog.vue`) — its census is present too. **Both were RE-RUN by this seat at `81a56990` at write time and reproduce command-for-command** (flattenVars: 1 specifier / 4 symbol hits / 1 consumer module; KeyframesAddDialog: 1 live import `KeyframesEditor.vue:118`, mount `:75`, 1 docblock prose mention).*
- ***SUBJECT-IDENTITY: 24 of 24 §Bounds rows path-qualified; gate-witness symbols swept WHOLE-FILE, 4 bare leaves found and cured.*** *The same access-column command prints a path for every row. The gate sweep — not limited to the rows any ruling named — found **G6 carrying four symbols without their modules** (`importCSSToTimeline`, `TimelineKeyframe.easing?`, `captureSnapshot`, the `imported` assignment); all four are now file+line qualified at `81a56990`. Two of the four resolve in more than one module at the frontier, so the omission was load-bearing, not cosmetic.*
- ***NUMERIC ARM (LAW D(3)): every figure printed beside a pasted command in the cells this round touched was produced by RE-RUNNING that command at write time.*** *Re-run and unchanged: the two LAW-A censuses; the taxonomy pair (**8 hits / 4 records**; disposition filter → **3 rows / 2 records**); `kf-KeyframeCard`'s rung headings (`:95` INFO) and `:100`; both `KF-W6` ends + both CARRY halves (by row header); `KF-W5`'s reciprocal naming of all four fixtures; `KF-W4`'s `:54`/`:58` and its §Commits line; `KF-W2`'s `:710`/`:753`; the PASS-4 check's `:80`/`:81`/`:82`. ⟨**STRUCK AT REPAIR ROUND 5 — PASS-5 check D6, the third generation of the LAW-B-corollary class in this file.** The round-4 text asserted, in this spec's own voice, ***"No figure in the cells this round touched was carried over unre-run"*** — a completeness claim over an enumerable set, asserted here rather than cited, the exact shape LAW B exists to forbid and the shape this file has now had struck three times (round 2's *"Nothing was inherited…"*, round 3's scoped restatement, and this). **It was falsified by its own paragraph**: the `vitejs/plugin-vue` grep listed two lines above as re-run had been printed at **2 of its 4 lines** (PASS-5 D4). **The cited form, which is the only form this file may carry**: whether every figure re-derives is a completeness claim, and it lives in the conformance artifact — `PASS-5/KF-W7-CHECK.md` §0 axis 2 + §2, which re-executed this file's receipts independently and convicted three (D1 · D4 · D9), all cured at this round with their commands re-run and printed WHOLE. What this seat may state, and does, is **particular and checkable**: the specific commands re-run at repair round 5 are the four enumerated above plus `grep -n 'vitejs/plugin-vue' KF-W4.md` (4 lines, all four printed), `grep -n 'KF-AV-17' kf-AnimationVisualizer.md` (2 lines, both printed), `sed -n '150p' kf-TimelineTrack.md` (whole line printed) and `grep -n '^### §1c' PASS-5/KF-W7-CHECK.md` — **each printed at its full output at the cell it authorizes, which is a property a reader verifies rather than a promise this file makes.**⟩ The second clause of the struck sentence survives and is restated as the particular claim it always was: the sibling anchors this file exposes to KF.W8's R4-5(b) receipt were verified unmoved before close — `grep -n 'test-utils' KF-W7.md` → the three exposed cells among its hits, re-run this seat.*

*Local PASS-4 check defects: **D1** cured by the owner-test reconciliation + the re-pointed cite · **D2** by the INFO restoration · **D3** by the P-2 re-attribution · **D4** by the four-member dissent roster · **D5** by hard-ordering item 9 + the IN leg · **D6** by the MISS-β3 re-quote · **D7** by the three-row headline · **D8** by the literal heading · **D9** by the §6.D addressee · **D10** by the verb split. **What the check found HOLDING was not regressed and was not re-touched**: census carriage (0 escaped), the 16 fold-carried identity lines, the 31 frontier witnesses, both LAW-A censuses, LAW B's converted voice, and all 15 gates born-RED with reachable green. **E-3 holds**: the registry is untouched — the four adjudicated rows named at D1, KF-KC-52's rung, the P-2 and MISS-β3 quotations and the TT dissent roster are all READ from the bank, never edited, and the PASS-1..PASS-4 check files are not edited. **LAW B holds**: no closure claim is asserted in this spec's voice — the census, anchor-cleanliness and gate-witness claims all cite `PASS-4/KF-W7-CHECK.md` by path, and this ledger claims only what its own pasted commands returned. No ruling addressed to this wave was declined.*

*Local PASS-3 check defects: **D1** cured by R3-8.3 · **D2** cured at G5/N-6/MISS-β1 with pasted re-derivations · **D3** cured by the DH-3 four-party table + the KF.W8 G15 declaration · **D4** cured by the → KF.W10 edge + the adopted receipt form · **D5** cured per LAW B with the five-part rule · **D6** cured by the DH-2 declarations at both files · **D7** cured by the replaced receipt · **D8** cured by the verbatim quotation · **D9** cured by naming the cache module · **D10** cured by the (c)(4) stable-anchor re-cut. **Nothing was inherited from RULINGS-3's prose, the check's prose, or this spec's own prior text: every frontier fact restated above was re-executed by this seat at `origin/master 81a56990` before it was written, and every registry fact at the value.js bytes at this seat's clock** — this sentence is scoped to the anchors this round touched, which is the correction round 2's unscoped version earned. E-3 holds: the registry is untouched, corrections land in place as dated addenda under original ids, the PASS-1/2/3 check files are not edited, no gate is stamped, and no ruling addressed to this wave was declined.*

---

*Repair round 5 (X·KF L-20, 2026-08-28) — **a STAGE-2 seat under RULINGS-4 LAW C as amended by RULINGS-5 LAW E**: this file was written AFTER KF.W0–KF.W6's round-5 writes (verified at this seat's clock: all seven carry round-5 mtimes) and BEFORE KF.W8/KF.W9/KF.W10's. **LAW E(4) governs every cross-wave receipt below — anchor-only, and per-seat substrate stamps are retired**; the RECONCILE seat alone verifies receipts at final bytes and holds the substrate table. Directives executed: RULINGS-5 §END, seat KF-W7 (7 directives, 11 local defects).*

- ***D1 + D12 — the struck-grant citation retired as a CLASS, not repaired as an instance (drift row 13).*** *The convicted receipt asserted KF.W8's access verbs, glob spelling and file count at three sites and was **false at W8's round-4 bytes**; RULINGS-5 **R5-3** re-cuts that same sibling row AGAIN this round, so an updated numeral would have been false at close for a third consecutive generation. **EIGHT sites reduced to anchor-only** — the three the check convicted plus five its defect text never named, found by running the class's own enumeration command before writing the claim (enumerated at the LAW-C block, arithmetic at the scope receipt). **D12 is the mechanism and is repaired at the taxonomy**: the FORWARD class now carries a four-part test — **no verb, no glob, no count, no prose** — under which the footprint row could never have been filed as an anchor. **The outward invariant this wave carries — no KF.W7 gate read at bytes a neighbour has since changed — is restated UNCONDITIONAL**, since it never depended on the struck premise and is stronger without it.*
- ***D3 + D8 + D7 — the census figure re-derived FROM THE RULE, at the artifact, with the boundary finally written (R5-11).*** *The cite re-points to `PASS-5/KF-W7-CHECK.md` §1 — **135 · 135 · 0** — with **the arithmetic riding AT the receipt**: 9 non-roster entries **− 2** (`kf-SpringTarget.md:69` double-counted inside an already-counted row; `kf-PlaybackRibbon.md:36` a routing-law preamble, the class rule (1) excludes by name) **+ 1** boundary seam **= 8**; **127 + 8 = 135**. **Six generations of this class turned on the ROSTER/NON-ROSTER BOUNDARY, not on membership** — which is why four rounds of membership re-cuts never moved it. **D8** writes the missing discriminator INTO rule (1) (a tail counts iff it carries an unbooked obligation) and settles all six tails by name. **D7** re-points the fold enumeration to `PASS-5 §1c` (**17**, not 16) and names the omitted member, `kf-SequencePlayhead.md:92`. **Escaped is 0 under all three admissible denominators (135 · 134 · 136) — the delta is a denominator, never a drop.***
- ***D4 + D9 + D11 — the three short transcripts, each printed at its command's FULL output.*** ***D4***: `grep -n 'vitejs/plugin-vue' KF-W4.md` re-run at W4's final round-5 bytes → **all four lines** (`:55` `:59` `:166` `:212`) ⟨**P7 COORDINATE SWEEP, 2026-08-29** — re-run at final bytes: **`:55 :59 :167 :213`**, still four lines, all four rows byte-identical⟩, where round 4 printed two; both extra hits corroborate, and **all four moved again this round**, which is the argument for the row-anchor. ***D9***: `grep -n 'KF-AV-17' kf-AnimationVisualizer.md` → **both** lines (`:61` the disposition, `:105` the K-12 kill entry that supplies this row's own cure-shape lock). ***D11***: **both `sed -n 'Np'` sites are now printed WHOLE rather than marked as fragments** — the ruling asked for fragment marks, and whole-printing is the cheaper discharge because it retires the class instead of labelling it. `sed -n '150p' kf-TimelineTrack.md` (D5's site) and `sed -n '69p' kf-SpringTarget.md` both fit: the latter measures **75 words / 590 bytes** at this seat, where the check described *"a full 400-word m-8 row"* — **a figure corrected because this seat measured it rather than inheriting it**, which is D6's lesson applied to a conformance artifact's own incidental number.*
- ***D5 — the fifth preserved dissent restored, inside the cure written against truncation.*** *`sed -n '150p' kf-TimelineTrack.md` is printed **whole**; the item preserves **FIVE** dissents (RR-A's four MAJOR rungs **plus Axis D's BLOCKER on D-1**) and names **THREE** probes, where round 4's cure printed the first sentence only and certified *"FOUR members"*. **Each of the five is now shown at the row that carries it** — D-4 at P4 · M-7 at P3 · D-12 here · D-13's net arm at its own row · **D-1's BLOCKER at P1's C-1 dissent and at §Verdict Protocol's D-1/C-1 trigger**. Nothing was lost in substance at round 4; what was lost was the fifth member's presence in the roster the cure certified as whole.*
- ***D6 — the third LAW-B universal in this file's history, struck.*** ***"No figure in the cells this round touched was carried over unre-run"*** *is a completeness claim asserted in spec voice and **falsified by its own paragraph** (the D4 grep it listed as re-run). It is replaced by the cited form (`PASS-5 §0 axis 2 + §2`) plus a **particular, checkable** statement of which commands this seat re-ran and printed whole — a property a reader verifies rather than a promise this file makes.*
- ***D10 — the by-scope cure completed two rows past where round 4 stopped.*** *`KF.W11` and `KF.W13` were still addressed as though owning specs exist. **They are MINTED-UNAUTHORED** (R4-8): both are re-pointed to `KF-W10.md` **§6.D · SUCCESSOR-FORMATION REGISTER**, rows 1 and 3, matching the KF.W12 conversion round 4 already made at EDITOR-UNIT — anchor alone, W10 writing last. This spec's own quoted lesson (*"a repair that cures the named instance and not the stated scope leaves the law green over its own counterexample"*) applied one paragraph over from where round 4 read it.*
- ***THE FRESHEST-ARTIFACT SWEEP (R5-9 row 1's class, pre-empted here).*** *All **five** live `PASS-4/KF-W7-CHECK.md` citations — census (verb table), LAW-B anchor-cleanliness (§Carry), gate-witness soundness (§Gates), the §Excluded census cite, and the closing footer — are re-pointed to `PASS-5/KF-W7-CHECK.md` **in one act**, so no two cells of this file name different artifacts of record for one claim. The PASS-3/PASS-4 citations that remain sit inside the dated round-1..4 logs, which E-3 preserves and never rewrites. **The scope word is carried honestly**: PASS-5 re-executed a **sample** (11 of 11 §Bounds counts · 9 of 9 gate witnesses) where PASS-4 was exhaustive (31 of 31), and this file claims the sample rather than inheriting the older count.*

***SHADOW LINE (RULINGS-5 LAW F(1)/(2), mandatory at every act with a striking verb).*** ***SHADOW: struck/minted ROUTING set = EMPTY.*** *No row moved home, no id was struck, re-keyed, narrowed, folded or minted at this seat this round; **all 127 roster identities and all 7 non-roster identities remain carried at the rows that carried them before** (**the denominator follows `:13`'s corrected derivation, `9 − 2 = 7`; the round-5 `8` carried D1's phantom operand — corrected at repair round 6, PASS-6 D12. The SUBSTANCE is unchanged and verified: no row moved home, and both contested identities are carried — `m-8` at P0's KF-AV-10 row, `PR-CAUTION` at the header lock and §Carry's disclosure.**), and the two identities the denominator lost (`m-8` at `kf-SpringTarget.md:69`, `PR-CAUTION` at `kf-PlaybackRibbon.md:36`) are **carried under every reading** — at P0's KF-AV-10 row and at the header lock + §Carry's label disclosure respectively, which is why a **−1 in a denominator is not a disposition and books no shadow of the routing kind**. **What WAS struck is citation-layer only, and is enumerated with its disposition**: **eight** forward W8 characterizations → replaced by anchors at the same eight cells (disposition: RECONCILE verifies at final bytes) · one LAW-B universal at the numeric arm → replaced by a cited form + a particular enumeration · **five** PASS-4 citations → re-pointed to PASS-5 · two minted-unauthored addressees → re-pointed to `KF-W10.md` §6.D rows 1 and 3. **Citing/terminus waves notified**: **KF.W8** (eight anchors now await its §Bounds at final bytes — no verb, glob, count or prose of its is restated anywhere in this file) · **KF.W10** (§6.D rows 1 and 3 gain two new citing ends; §G-2's alphabet unchanged and still consumed verbatim at §Verdict Protocol) · **KF.W4** (its `package.json`/`vitest.config.ts` §Bounds rows are cited by row, their four line coordinates re-run and printed whole) · **RECONCILE** (one booked, uncured defect — below).*

***R5-11 SCOPE RECEIPTS — arithmetic first, then scope; enumeration and counting rule AT the receipt:***
- ***FORWARD-RECEIPT SWEEP: 8 of 8 converted.*** *Command, re-run at this file's FINAL bytes: `grep -n 'KF-W8\.md\|KF\.W8' KF-W7.md` → **29 hits**, lines `13 31 52 61 62 77 79 81 83 122 123 181 284 344 348 351 352 373 375 394 395 400 410 415 419 423 425 433 436`. **Counting rule**: one hit = one line; a hit is a CONVERTIBLE cross-wave receipt **iff it characterizes KF.W8's own spec content** (verb · glob · count · prose). A statement of where a REGISTRY row is routed is sourced from the 58 records, not from a sibling's bytes, and is **not** in the class. **8 convertible — `:31 :61 :62 :77 :79 :81 :83 :352` — all eight converted to anchors**; **11 registry-sourced or this-wave's-own** (`:13 :52 :122 :123 :181 :284 :344 :348 :351 :373 :375` — census rule, barrel routing, riders, kill locks, the LAW-C taxonomy itself, dependency and edge declarations); **10 inside the dated round-1..5 logs**, which E-3 preserves and never rewrites (`:394 :395 :400 :410 :415 :419 :423 :425 :433 :436`). **8 + 11 + 10 = 29.** ⟨**The command found two members the ruling's own defect text did not name** — `:81`'s *"KF.W8's four creates plus its nine-file and `resize-tracks.test.ts` carves"* and `:83`'s *"since KF.W4 and KF.W8 carve two of the entries below"*. The PASS-5 check convicted three cites; the SCOPE holds eight. **Both were cured rather than reported**, which is the whole content of R5-11: the enumeration is run before the claim is written, and a receipt that reproduces at eight is worth more than a receipt that asserts six.⟩*
- ***FRESHEST-ARTIFACT SWEEP: 5 of 5 re-pointed.*** *Command: `grep -n 'PASS-4/KF-W7-CHECK' KF-W7.md` → **7 hits** before this round (`:13 :106 :254 :366 :385 :402 :417`); **4 hits after** (`:402 :417` + this ledger's two records of the sweep). **Counting rule**: a hit is LIVE iff a present-tense claim rests on it; a hit inside a dated repair-round log is HISTORY and is never rewritten (E-3). **5 live** — the verb table's census cell (`:13`) · §Carry's LAW-B anchor-cleanliness cite (`:106`) · §Gates' witness-soundness cite (`:254`) · §Excluded's census cite (`:366`) · the closing footer (`:385`) — **all five re-pointed to PASS-5 in one act**; **2 history** (`:402` the round-4 ledger's D1 entry, `:417` its local-defect line). **5 + 2 = 7**, and **0 live PASS-4 citations remain**.*
- ***TRANSCRIPT ARM (LAW D(3), the D4/D9/D11 class): 4 commands re-run at this seat, each printed at its FULL output.*** *`grep -n 'vitejs/plugin-vue' KF-W4.md` → 4 of 4 lines printed · `grep -n 'KF-AV-17' kf-AnimationVisualizer.md` → 2 of 2 printed · `sed -n '150p' kf-TimelineTrack.md` → whole line printed · `grep -n '^### §1c' PASS-5/KF-W7-CHECK.md` → 1 of 1 printed. **Both `sed -n 'Np'` excerpts are printed WHOLE — `sed -n '69p' kf-SpringTarget.md` at `:119` (75 words / 590 bytes, measured by this seat) and `sed -n '150p' kf-TimelineTrack.md` at `:164` (77 words) — which is the cheaper discharge the ruling's fragment-marking option left open, and it is the discharge this file took.** ⟨***corrected at repair round 6, PASS-6 D5: the round-5 spelling described a discharge the file did NOT take (there is no fragment-marked `sed` excerpt in the live file) and re-issued the ~~"400-word"~~ figure that `:427`, eleven lines above, had corrected to 75 BY MEASUREMENT in the same round. The two cells were mutually exclusive and the bytes agree with `:427`. The R5-11 TRANSCRIPT ARM — the round's own arithmetic-first instrument — was the cell that failed.***⟩ **No figure in this ledger is asserted as a completeness claim; each is a count of a command's own output** (D6's lesson, applied to the ledger that records D6).*

*Local PASS-5 check defects: **D1** cured by the six-site anchor-only conversion · **D3** by the re-pointed census with its arithmetic at the receipt · **D4** by the four-line transcript · **D5** by the whole-line dissent quotation and the five-member roster · **D6** by the struck universal · **D7** by the re-pointed 17-line enumeration · **D8** by the tail discriminator written into rule (1) · **D9** by the two-line transcript · **D10** by the two §6.D re-points · **D11** by the fragment marks · **D12** by the corrected FORWARD taxonomy. **TEN of the twelve cured here** (**corrected at repair round 6, PASS-6 D13: D2 is booked-and-declined to RECONCILE, correctly — the certificate landed it at §4 · R5-R2. The two over-claimed are D7, whose cure re-points to an enumeration whose own arithmetic does not reproduce — 18 coordinates under a "17" heading, which is verbatim D7's OWN named class, "an enumeration that misses a member cannot discharge the rule that cites it" — and D11, whose discharge is described two ways in this same ledger at `:427` and `:438`.**) **D2 is BOOKED AND DECLINED AT THIS SEAT, with its owner named**: it convicts a substrate stamp inside the **RECONCILE seat's own inserted block** in this file, and **RULINGS-5 R5-9 row 14 assigns the cure to RECONCILE** (*"CLOSE-CERT-5 re-stamps its predecessor's insertion"*) under the governing law that every cross-wave defect gets exactly ONE owning cure a single wave can apply. A per-wave seat re-stamping another seat's clock is the precise mechanism LAW E(4) retires; the decline is recorded at the block itself, not only here. **The defect's substance is separately safe** — all four forward W10 anchors resolve at W10's current bytes, re-verified by the pass-5 seat.*

*Status unchanged: **planned**. No product byte written; no product source opened; no gate stamped; no registry byte touched; no prior-pass conformance artifact edited; `scripts/dev/dev.sh` untouched. E-3 holds — repairs land in place, the dated round-1..4 logs above are preserved as history, and every figure in this ledger is a count of a command this seat ran.*
