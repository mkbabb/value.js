# KF.W7 — Timeline Evaluate

**Wave**: KF.W7 · Timeline Evaluate — the 666-line timeline cluster (KeyframeTimeline **312 L at origin/master** — the banked "313 L" is a ±1 counting-convention artifact, corrected so the disposition table inherits the right number · TimelineTrack 246 L · TimelineCaret 70 L · TimelineHoverPreview 38 L) + SequenceScrubber 162 L (S-3/S-4) + AnimationVisualizer 256 L, evaluated against the glass-ui `/timeline` + `ScrubberTimeline` family. **Evaluate, not mechanical swap** — this wave's product is a per-surface verdict table plus the cure program the verdict leaves standing.
**Half**: frontend (keyframes.js demo). **Sub-tranche**: X·KF.
**Status**: planned. No product byte is written by this file; authoring it opened no product source for writing; execution awaits the owner's begin-word.
**Ref of record (G12 / MISS-α5)**: keyframes.js `origin/master` `81a56990` or later. Local HEAD `8281638c` is DISQUALIFIED as witness substrate (banked kf-App ruling 1); every line anchor below re-resolves against origin/master before cure (D-19). KF.W0 §B-12: origin/master pins glass-ui 7.0.0 exact devDep = installed; **every wave re-baselines against origin/master**.
**Standing supersession rider (KF-AV-28), the wave's defining lock**: this wave's S-9 evaluation may supersede any behavioral cure — a per-surface SWAP verdict discharges the bespoke rows banked under that surface with the component. **Counter-evidence lock (`PR-CAUTION` — a SPEC-LOCAL LABEL for the standing §7 caution banked at `kf-PlaybackRibbon.md:36`, not a banked id; disclosed at §Carry's preamble), weighed before any SWAP verdict (verbatim)**: kf-PlaybackRibbon is the NON-bespoke case (it consumes the real `Slider`) and carries BLOCKERs anyway — *"swap onto the primitive" is NEVER SUFFICIENT as a cure.*
**Provenance**: twice-authored (Fable arm + Opus arm), agglomerated by the fresh-Fable fold seat per M-23; adjudicated rows consumed by mechanism, deduped by identity, never transcribed (M-25); validated against `docs/precepts/` latest (L-20); no gate exists here without a named live witness (L-19).

| verb | state | basis |
|---|---|---|
| AUDITED | YES | `registry/adjudicated/`: kf-KeyframeTimeline · kf-TimelineTrack · kf-TimelineCaret · kf-TimelineHoverPreview · kf-SequenceScrubber · kf-AnimationVisualizer · kf-SpringTarget · kf-SpringTrace · kf-CSSPasteDialog · kf-KeyframesAddDialog · kf-CSSCodeEditor · kf-RibbonBar · kf-KeyframeCard |
| SPECIFIED | YES — this file | **authored from the 58-record registry directly; the routed denominator is the registry's (PASS-1 census)** — 148 routed, 148 accounted (145 carried + 3 excluded-with-owner). **Provenance correction (RULINGS R-12)**: this spec previously bound itself to a "KF.W7 CARRY ledger" that exists at no coordinate — the only in-tree carry is `docs/tranches/X/keyframes/carry/KF-W6-CARRY.md` (201 rows / 54 records, KF.W6's; 424 was the literal `KF.W6` token count, never a row count). **FIVE bindings struck**, not four: the ruling's four (this cell · §Bounds' `boundsFiles` · §Excluded's opener · the fold-repair footer) **plus a fifth this repair found at the closing footer**. Nothing here is provenanced on an unproduceable source, and the falsifiability claim the old text made ("grep-verifiable against the ledger") now names a source that can actually be grepped. |
| IMPLEMENTED | NO | stays NO until gates green after the begin-word |
| VERIFIED | NO | stamped only at sub-tranche close |

**Goal criterion.** This wave succeeds if, when work ends, the lane can answer — with the installed export surface in hand, not with taste — *"is the timeline instrument ours to keep, and if we keep it, what is the one seam that makes scrubbing real?"* Concretely: a per-surface ADOPT/PARTIAL/KEEP-BESPOKE ruling against the IMPORTABLE glass surface; the scrub seam ruled either LIVE (then wired, guarded, single-engine, in one commit) or DISPLAY-ONLY (then said so, with the rungs that hang off that ruling re-graded); and every NO-WAVE-OWNER row beneath the rider given a terminal disposition. **A wave that wires `scrub` into the drag and calls it done has failed this goal** — that is N-10's convicted regression, not a cure.

**Execution shape**: 6 seats across 4 sequential phases — phase 1: 1 serial (the S-9 evaluate seat; its verdict sizes the rest); phase 2: 2 parallel (seam design ∥ preview/ghost design); phase 3: 2 parallel (implementation, disjoint by file); phase 4: 1 serial (close + BH relay). Peak concurrency 2, inside the owner's four-workflow cap.

---

## §0 Open preconditions (checked at wave-open; NOT close gates)

| # | precondition | state at authoring |
|---|---|---|
| **OP-1** | **kf-ChannelControls L-2/C-2 — the `:key` remount BLOCKER — settles BEFORE this wave opens.** The bank's own words: *"any timeline-cluster swap that inherits the `:key` inherits the data loss."* | UNSETTLED. Hard ordering, not advice. Second-order collision resolved by this spec at KF-CE-2 (parent half), §Carry P8. |
| **OP-2** | **KF.W4's vue-tsc gate (KF-CE-16, "the sequencing head of the whole record") + `noUnusedLocals` (K3).** | ABSENT ⟨measured: `package.json` at origin/master `"check": "tsc --noEmit && tsc --noEmit -p tsconfig.test.json && npm run proof:structure"` — no `vue-tsc` (citation correction contributed: the banked quotation of `"check"` omits the `&& npm run proof:structure` leg); `demo/env.d.ts:3-7` maps `*.vue` → `DefineComponent<{}, {}, any>`⟩. Every typed cure here is authorable but UNVERIFIABLE until that gate lands. |
| **OP-3** | **D-19 geometry re-derivation before any geometry cure** — K-13 corrected every table cell ~1px (padding-box); banked D-17 carries the SAME uncorrected figures. | OPEN. Both tables re-derive before a cure is written (§Carry P4). A cure computed off the border box is a cure to the wrong number. |
| **OP-4** | **A mount harness must exist for G11 — and it costs a dependency.** | ABSENT ⟨measured: `ls node_modules/@vue/*` → compiler/reactivity/runtime only; `@vue/test-utils` is NOT installed and NOT in origin/master devDependencies; `jsdom ^29.1.1` IS present⟩. G11 is not "write a test" — it is "add `@vitejs/plugin-vue` + `@vue/test-utils`, wire a `vitest.config.ts` `plugins` line, then write a test": origin/master's `vitest.config.ts` declares `resolve.alias` + test config and NO `plugins` — no Vue-SFC plugin exists, and the house drive-the-composable-through-a-host idiom cannot mount the render-only THP SFC. Declared so the gate is not costed as free; hence the `package.json` devDependencies-only and `vitest.config.ts` plugins-only bounds rows. |
| **OP-5** | **KF.W2's parse façade must exist as L-6/C-4's delegation target.** | OPEN. The cure is DELEGATE, and delegation needs a target: `@mkbabb/value.js/css` exports the collectors; the sibling path already honours the doctrine (`timelineEngine.ts:13` `parseAnimationCSS`, `:90` `serializeCssValue`) — which is what makes the SFC's hand-rolled parser a BYPASS, not an omission. |
| **OP-6** | **The S-9 evaluate verdict sizes this wave's own roster.** | BY CONSTRUCTION. Under KF-AV-28 the phase-1 verdict may supersede rows in §Carry. Phases 2–4 dispatch AFTER phase 1 commits, never concurrently. |

---

## §Bounds

Owned files (bounds reconciled against the **registry records' own file coordinates** — there is no KF.W7 CARRY `boundsFiles` list to reconcile against, and the phantom claim that there was is struck per RULINGS R-12; all under `/Users/mkbabb/Programming/keyframes.js/`; line counts = `git show origin/master:<f> | wc -l`, each re-measured by this seat):

| File | L | Access | Note |
|---|---|---|---|
| `demo/components/instrument/timeline/KeyframeTimeline.vue` | 312 | modify | cluster head; ` M` vs HEAD |
| `demo/components/instrument/timeline/components/TimelineTrack.vue` | 246 | modify | byte-identical at all three refs (anchor-safe) |
| `demo/components/instrument/timeline/TimelineCaret.vue` | 70 | modify | byte-identical at all three refs |
| `demo/components/instrument/timeline/components/TimelineHoverPreview.vue` | 38 | modify | |
| `demo/components/instrument/timeline/composables/useTimeline.ts` | 129 | modify | origin-side anchor tier (G12) |
| `demo/components/instrument/timeline/composables/useTimelineBuild.ts` | 200 | modify | origin-side |
| `demo/components/instrument/timeline/composables/useTimelineOps.ts` | 88 | modify | origin-side |
| `demo/components/instrument/timeline/composables/useZoomPan.ts` | 110 | modify | byte-identical at all three refs |
| `demo/components/instrument/timeline/timelineTypes.ts` | 41 | modify | `selector: KeyframeSelector` exists at origin/master `:5-6` ONLY — absent at HEAD |
| `demo/components/instrument/timeline/index.ts` | 8 | modify | barrel; the dead-lazy-barrel row is KF.W8's |
| `demo/components/instrument/timeline/utils/timelineEngine.ts` | 102 | modify | origin-side; SHARED — serial-only (see disjointness); count correction: 102 at origin/master — the worktree 104 is the dirty `+4/−2` edit that WIDENS the C-12 breach |
| `demo/components/instrument/timeline/utils/snapshotCapture.ts` | 66 | modify | |
| `demo/components/instrument/timeline/utils/flattenVars.ts` | 33 | **delete-candidate** | deletion rides `loadPreset`'s adopt-or-delete (L-13) |
| `demo/components/instrument/timeline/CSSPasteDialog.vue` | 80 | modify | fold TARGET (R-7 direction pinned: this shell survives) |
| `demo/components/instrument/keyframes/components/KeyframesAddDialog.vue` | 161 | modify | fold SOURCE; its `innerHTML` BLOCKER is KF.W5's, front-loaded, NOT gated here |
| `demo/scenes/sequence/SequenceScrubber.vue` | 162 | modify — shape rows verdict-gated | S-3/S-4; component-shape rows discharge or persist with the G1 verdict |
| `demo/components/playback/AnimationVisualizer.vue` | 256 | modify — verdict-gated | S-9 census extension subject; the LARGER of the two rail/ball shadows |
| `demo/utils/keyframeSelector.ts` | 31 | modify | exists at origin/master ONLY — untracked at stale HEAD (G12) |
| `package.json` (keyframes.js) | — | modify-carve — **devDependencies only** | bounds reconciliation added by this spec: OP-4 requires `@vue/test-utils`; a gate that needs a dependency needs a bounds row, or the gate is uncosted |
| `vitest.config.ts` | — | modify-carve — **`plugins` only** | bounds reconciliation (OP-4): no Vue-SFC plugin at origin/master; G11's THP mount is impossible without `@vitejs/plugin-vue` |
| `test/demo/instrument/**` | — | create | G11's mount tests |

**Disjointness (phases 2–3 split by FILE, never by concern)**: seat `.d` owns TimelineTrack.vue + TimelineCaret.vue + useZoomPan.ts + useTimelineOps.ts; seat `.e` owns KeyframeTimeline.vue + TimelineHoverPreview.vue + useTimelineBuild.ts + snapshotCapture.ts + both dialogs. **`timelineEngine.ts` and `timelineTypes.ts` are shared and therefore SERIAL** — G5's collision decision writes both and lands before either seat dispatches. Two seats editing the merge map is how a merge cure becomes two merge cures.

Read-only witnesses (never modified): `node_modules/@mkbabb/glass-ui/dist/components/timeline/**`, `dist/components/slider/types.d.ts`, `dist/components/tooltip/**`, reka `TooltipContentImpl.js`. Producer changes go to the BH relay (SS-6) — a demo-side patch of a producer seam is a defect. Not touched by any seat: `node_modules/@mkbabb/glass-ui/**`, keyframes.js `src/**` (library half — KF.W5), `demo/components/instrument/channels/ChannelControls.vue` (OP-1's owner), `demo/components/instrument/keyframes/KeyframeCard*.vue` (CARD-UNIT), `demo/components/playback/PlaybackRibbon.vue` + TransportDock (KF.W13), value.js `src/**`, `scripts/dev/dev.sh` (unowned, either repo, NEVER touch).

---

## §Verdict Protocol (the evaluate — G1's subject)

One disposition table, per surface, each ruled **ADOPT · PARTIAL · KEEP-BESPOKE** with named reason, against the **importable** surface only:

| surface | evaluation input (not to be re-derived) |
|---|---|
| KeyframeTimeline / TimelineTrack rail | **C-15 split verdict is the input**: playhead 0..1 = mechanical-swap candidate; N-marker zoom/pan rail = NO counterpart (`geometry.d.ts` has no percent↔position map, no zoom/pan; scrubber region has zero `renderSlot`). Cite C-15; re-deriving it is the killed-claim #15 class. |
| TimelineCaret | NumberField question is KF.W6's (C-4/D·M-9); this wave rules only the contract exception (C-10) and posture (L-15) |
| TimelineHoverPreview | tooltip producer seams → BH relay; ghost redesign is bespoke regardless of verdict |
| SequenceScrubber (S-3/S-4) | KF-AV-28: EVALUATE, NOT SWAP — the `aria-hidden` charter + fling semantics may exceed the primitive; `role="slider"` would conflict with the deliberate disposition; `SliderVariant = "standard" \| "spectrum"`, no "timeline" member in 7.0.0 |
| AnimationVisualizer | KF-AV-28 intake row (D-18=C-13): re-derives ScrubberTimeline's shape (256 L vs the censused 162 L) on a subpath the demo imports 0 times |
| SpringTarget/SpringTrace idiom arms | KF-AV-10 + C-4 (SpringTarget) + C-3 (SpringTrace), sequenced WITH the verdict (§Carry P0) |

**Rider mechanics**: per-surface SWAP ⇒ the bespoke behavioral rows banked under that surface DISCHARGE with the component (each discharge named in the verdict table — silent discharge is a silent drop). KEEP-BESPOKE or PARTIAL ⇒ the rows below execute. **D-1/C-1 dissent trigger**: if the verdict rules scene-scrubbing the instrument's core promise, the whole scrub seam inherits BLOCKER weight as one cluster. **M-3/C-4 inverse trigger**: if the verdict permanently rules the scrub seam display-only, M-3/C-4 drops to MINOR.

---

## §Carry

Agglomerated by mechanism into commit families (M-25: dedupe by identity, carry cure-shape locks and sequencing edicts, route by mechanism). Every row lands with its banked id verbatim, rung, witness anchor, cure-shape lock, and family — **no row in this spec is renamed, and no id below is this spec's coinage.** Line anchors are origin/master anchors and re-resolve before cure (D-19, G12). Identity discipline: the corpus reuses ids across source files — every colliding id below is disambiguated by its parenthetical, exactly as banked.

**Two SPEC-LOCAL LABELS, disclosed here so the promise above is exactly true** (this repair): `PR-CAUTION` and `L-15-PROTECTED` appear in **zero** registry files — they are this spec's handles for two banked *locks*, not rows and not renamed ids, and each is bound to its banked coordinate so a registry-keyed grep lands on the bank rather than missing the lock: **`PR-CAUTION` ≡ the standing §7 counter-evidence caution banked at `kf-PlaybackRibbon.md:36`** (carried verbatim in the header) · **`L-15-PROTECTED` ≡ the K-8 protection lock banked at `kf-KeyframeTimeline.md:125`** (the kill that PROTECTS live code; carried at P8's KF-CE-2 hard lock 3). Neither label may be used as a routing id, and any downstream sweep keyed to the registry must sweep the two coordinates, not the two labels.

### P0 · Evaluate intake (phase 1; gates the whole roster)

- **C-15** MINOR rung, MAXIMUM spec weight — THE WAVE'S EVALUATION INPUT ⟨KT "the corpus's best row" · TT re-verified · THP C-7 fold, export status corrected · kf-SequencePlayhead C-§4 correction carried · SpringTrace C-3 cites as precedent⟩ — the glass `/timeline` shadow is real for the 0..1 playhead ONLY: `dist/components/timeline/index.d.ts` is TWO lines (`GlassTimeline` + 3 types); `ContinuousRail`/`ContinuousMarkers`/`ScrubberTimeline`/`SegmentedTimeline` `.d.ts` files ship but are NOT re-exported ⟨**census enumeration completed at G1 by this repair**: the un-re-exported set is SEVEN, not four — `ContinuousTimeline.vue.d.ts` and `types.d.ts` also ship. This corrects a count, nothing else: **C-15's SPLIT VERDICT is carried exactly as banked and is NOT re-derived — killed-claim #15's lock is intact**⟩; the seam is reachable only through GlassTimeline's forwarded `popoverContent`; `geometry.d.ts` is weighted-segment math with NO percent↔position map and NO zoom/pan. **Census correction verbatim**: lane-frontend S-3 is corrected to a SPLIT VERDICT — playhead = mechanical swap candidate; N-marker zoom/pan rail = no counterpart. **Killed-claim #15 lock**: three axes independently re-derived this settled adjudication as a novelty — CITE C-15, do not re-open. Producer-side export gaps → BH relay (SS-6), never demo-side hacks. Feeds G1.
- **KF-AV-28** SPEC-INPUT — THE WAVE'S INTAKE ⟨AnimationVisualizer D-18≡C-13 · SequenceScrubber routing · PlaybackRibbon standing rider⟩ — census extension S-9: the shadow census skipped the LARGER of the two rail/ball shadows; AnimationVisualizer (256 L) re-derives glass `ScrubberTimeline`'s shape on a `/timeline` subpath the demo never imports (ships in dist; demo imports 0). **Verbatim**: both axes and both readers converge on EVALUATE, NOT SWAP. The standing supersession rider (header) and the PlaybackRibbon counter-evidence lock are this row's. Binds AnimationVisualizer's whole NO-WAVE-OWNER roster, SequenceScrubber's shape rows, PlaybackRibbon's transport packet, and the KF.W13 drag-seam packet ("resolve KF.W7 first"). Locks G1.
- **KF-AV-10** MAJOR, verdict-gated ⟨AnimationVisualizer · SpringTarget m-8 rides⟩ — the flagship rail/ball hand-rolls the demo's promoted `.progress-rail`/`.progress-ball` idiom — its SEVENTH, divergent authoring — against DESIGN.md §5: `design-idioms.css:161-187` (2px rail, `--rail-tint` 8%, `--ball-size` 36px, `--ball-glow` 35%) vs `h-1`/`bg-accent-kf/20`/`h-12 w-12`/`shadow-md`; 7 consumers; no `<style>` block to reconcile; the retint seam is LIVE (SequenceTarget `:80`, SpringTarget `:113`). **Verbatim routing**: bespoke-survives ⇒ adopt the idiom; swap ⇒ moot. **Cure caution lock (kf-EasingTarget P-2, verbatim)**: *"the idiom leaves `transform` unclaimed BY DESIGN; a careless consolidation drops 28 balls out of their rails."* Bundles SpringTarget m-8 (the T.G4 anchor triplet re-authored four times: `.spring-ball`/`.sampler-ball`/`.derby-lane-ball`/`.preset-ball`; `.derby-lane-ball` also re-declares the idiom's `margin-top`).
- **SpringTarget C-4 ≡ C-4 (SpringTarget)** MINOR, verdict-gated, **route dissent recorded** ⟨SpringTarget; both readers concordant; S-9 nomination stands⟩ — the header's load-bearing rationale claims adoption of `MetricBadge`, which exists in NO resolvable glass-ui (grep: 0 files; the metric barrel exports exactly four names); the markup is hand-rolled; credibility borrowed from the real `MetricSize "xl"`; `font-roles.json:82` encodes "the MetricBadge telemetry label idiom" as house law. Cure: repo-wide prose sweep + the S-9 evaluate — adopt `/metric` or DELETE THE SENTENCE; the `.status-badge` AA mix is load-bearing and Metric has no badge affordance. B-1's death (7.0.0 pinned exact) makes the claim STRONGER, not weaker. **Route dissent (M-25 route-by-mechanism)**: the mechanism is a glass-ADOPTION evaluate, KF.W6-adjacent — carried here because the record sequences it with KF-AV-10; the record's KF.W7 routing is preserved verbatim, nothing silently re-homed.
- **C·C-4 (SequenceScrubber)** MINOR, narrowed, verdict-gated ⟨SequenceScrubber⟩ — three colocated sub-units split by one rule landed on TWO contracts, divergence nowhere declared; `inject(SEQUENCE_DEMO_KEY)!` erases the missing-provider signal (runtime `TypeError` in a render function — the class the tree carries a scar comment about). **K-8 kill lock, verbatim**: the prescribed `{progress}`-prop-plus-emits cure is DEAD — *"the scrubber consumes THREE writer verbs the parent does not own; the flip relocates coupling into pass-through ceremony the KISS posture forbids."* SUP-5 SUSTAINED head-on. What survives: a one-line provider guard + declaring the asymmetry. The contract question belongs to G1's verdict; D-5/D-6/D-11's "the primitive already solves it" arms discharge or persist with it. *Dissent: reader-A's INFO preserved.*
- **KF-AV-13 / KF-AV-14 / KF-AV-17 / KF-AV-24 (evaluate-input riders)** MINOR / spec-input, decisions owned here ⟨AnimationVisualizer⟩ — **KF-AV-13**: no press-to-seek: `:25` is the sole handler; rail + both markers `pointer-events-none`; substituting `:200` into `:108` cancels `clientX` and `ballW/2` — the on-grab projection is an exact identity, so `progressFromPointerX`'s generality only works for a press the handler placement forbids; the sibling binds the whole rail (SequenceScrubber `:28`), as does `Slider`. **The DECISION is this wave's (G1 table); the cure lands NO-WAVE-OWNER (SS-1/SS-2).** MINOR because the operative `Slider` sits directly above and owns seek. **KF-AV-14**: the "visual twin" is geometrically NOT a twin — the ball's centre travels [24px, W−24px] while the Slider's visible indicator travels [0, W], coinciding only at 50%, ~12px apart at the quartiles, 24px at each end in OPPOSITE directions; a container resize leaves the ball at a stale absolute px; perceived magnitude → SS-13, judged in the same frame as KF-AV-11's hue split. **KF-AV-17 CURE-SHAPE LOCK (K-12, sustained at the bytes)**: SequenceScrubber's `translateX(calc(p*100cqw))` is NOT portable — it maps progress over the FULL container width and at p=1 the ball overshoots the rail; the resize-immune form is `translateX(calc(var(--p) * (100cqw − 100%)))`, the dashed marker's own proven expression — **binding on any transform-based playhead the evaluate adopts** (C-15's `left`-vs-`transform` arm). **KF-AV-24**: `progressFromPointerX` is a FOURTH hand-rolled rect-ratio projector against useDragScrub's "the ONE pointer-drag scrub seam" charter — and TimelineTrack's `getPercentFromPointer` is the FIFTH; the evaluate decides whether the count goes up or down; the consolidation itself routes KF.W8. *Dissent: reader-1's MAJOR on KF-AV-13 preserved.*
- **SpringTrace C-3 ≡ C-3 (SpringTrace)** MINOR, decision-here / implementation-edge-declared, **route dissent recorded** (M-25 route-by-mechanism: a value.js LIBRARY export-surface decision, natural home KF.W8 / the export settle; the record's KF.W7 routing preserved verbatim, nothing silently re-homed) ⟨SpringTrace⟩ — the library exports the emitter but neither `resolveLinearStops` nor `sampleNormalizedSpring` (dist grep → 0); every plotting consumer re-derives the CSS rule by hand. *"The sharpest reusable row in the corpus: exporting either makes D-1 structurally unwritable downstream, and the same export cures reader-B's monotonic-clamp divergence."* Precedent-bound to C-15. **A library-side ask reaching into a frontend evaluate wave** — this half takes the DECISION into G1's precedent frame and declares the implementation edge to KF.W5/KF.W8 (§Sequencing); the row is carried, its code is not this wave's.

### P1 · The scrub seam — ONE design problem, ONE commit family (N-10 law)

The law first: **N-10** SPEC-INPUT ⟨KT RR-B, net-new⟩ — C-1's prescribed cure and C-6's defect are mutually exclusive; wiring `scrub` into the drag converts the transient hover stomp into a continuous one. *Verbatim: "the remedy is upstream of both. Booked so the W7 spec author reads the scrub seam as ONE design problem."* **Any row curing one of {C-1, C-6, L-3, L-5, M-3/C-4} in isolation is defective by this law.** Typography twin: MISSED-3 (P7). **LP-1 rider carried where it lands**: the seam cure must not introduce a write→render edge — a scrub that writes inline styles during render re-manufactures the KF-CO-1/KF-CO-8 sequencing defect in a new file.

- **C-1** MAJOR, cluster head ⟨KT head · TT C-2 fold · caret/THP cited; RR-A BLOCKER preserved in DISSENT⟩ — `scrub` returned at useTimeline.ts:117, never destructured (the 15-member destructure at :194-210 takes `scrubT`/`scrubAndCapture`) ⟨B-arm re-measured, exact: `git show origin/master:…/KeyframeTimeline.vue | grep -n scrub` → hits `78`, `83`, `197`, `202`, `224` — the bare `scrub` symbol never among them⟩; `@update:scrub-t` bare ref write :83; the complete scrubber affordance (TT :24 cursor, :27-34 six pointer/touch handlers + `@wheel`, :52-55 playhead, 10× zoom) drives nothing; the engine reaches the playhead only as a hover-restore side effect (`finally { scrub(prevT) }`, useTimelineBuild :112-115). Cure designed jointly with C-6; standalone wiring FORBIDDEN. *Dissent (RR-A BLOCKER): "if the W7 spec rules scene-scrubbing IS the instrument's core promise, the cluster head inherits blocker weight as one seam" — the demotion is a program-law ruling, not a mechanism refutation.*
- **C-6** MAJOR ⟨KT⟩ — a SECOND engine instance writes over the scene's DOM: ChannelControls :194 passes `animation.targets` by reference; timelineEngine.ts:51 spreads the same `HTMLElement`s into an independent `CSSKeyframesAnimation`; `scrub` → `interpFrames(t, true)` writes inline styles onto the scene's elements; hover restore returns them to the TIMELINE's `t`, not the scene's. Cure: own detached/cloned preview target, OR drive the scene's single engine through AnimationControlsGroup. Cross-ref banked kf-ChannelControls L-16 (same boundary, milder trigger). Visible displacement → SS-13 residue #6.
- **L-3** MAJOR ⟨KT⟩ — no reactive edge re-derives the animation; watch census across the 1500+-line cluster = exactly ONE (CSSPasteDialog.vue:65, a dialog flag); all 9 `rebuild` calls imperative; `animation: KeyframesAnimation<any>` is a raw class prop mutated in place (TimingFunctionPanel:145). **K-4 kill lock**: `watch([animOptions, targets], rebuild, {deep:true})` binds a non-reactive class instance and can never fire — *"the cure would compile, typecheck, and change nothing."* The failure is OWNERSHIP, not a missing watcher — stronger than the axis stated. Joint with kf-ChannelControls L-16. Blast radius → SS-13 residue #11.
- **L-5** MAJOR ⟨KT · THP L-D5 fold; citation corrections carried: emit TT:83 not :84; fn useTimelineBuild:89-117⟩ — concurrent `scrubAndCapture` calls cross scrub/restore pairs: guard keyed per `kf.id` (:221), `prevT` read per call (:96), no lock/queue/generation counter, `@mouseenter` per diamond bare and undebounced. Self-indicting cure shape: `nextFrame()` (:63-87) was hardened for EXACTLY this concurrent caller and stopped one layer short of the shared `prevT` — extend the same discipline to the restore pair. Stranded-playhead percept → SS-13 residue #5 (carried at the bank, not double-booked at THP).
- **M-3/C-4** MAJOR (rung ruled on cure-coupling) ⟨TT⟩ — pointer + touch pipelines on one element, no multi-touch guard; a pinch also scrubs (`:168-172`/`:174-186` test no `isPrimary`/`pointerType`/pointer-count; a touch contact reports `buttons === 1` so :182 passes); corrupted `scrubT` IS `snapshot()`'s default capture percent (useTimelineOps.ts:28). **Verbatim: the guard is a PRECONDITION of wiring the seam, not a follow-up — same commit as C-1's cure.** Rung stays MAJOR precisely so *"booking this MINOR would let the W7 spec wire the seam without the guard."* Axis-L's harm sentence KILLED (K-7); never restated. *Dissent: RR-B would book lower; reopening INVERTED — if this spec permanently rules the seam display-only, the rung drops to MINOR.*
- **RR-B missed-3 (TimelineTrack)** MINOR ⟨TT, net-new⟩ — no scrub-drag state: `:182`'s `event.buttons > 0` proxy, mis-commented as "pointer is captured"; no `isScrubbing` flag (the marker path has one, :137/:194) — any button-held pointer ENTERING the 48/128px full-width band scrubs, text-selection drags included. Cure: explicit scrub-drag flag (the design system's own rail gates on `dragging`) + correct the lying comment in the same commit (KF-CE-41).
- **m-9/m-11/m-12 (TimelineTrack, merged)** MINOR ⟨TT, all three re-read at the bytes⟩ — (a) in-band sentinel `0` on a null ref :160-161 (a failure scrubs to START); (b) dead `!draggingKeyframeId.value` re-test :182 (the :178 return guarantees it); (c) no `event.button`/`isPrimary` guard — right/middle-press scrubs and takes capture BEFORE the context menu opens (:168-172). Cure: ONE guard policy for the whole handler set — out-of-band failure signal, delete the dead re-test, one primary-button/primary-pointer test shared with M-3/C-4's guard; lands as the pointer-guard commit, precondition of C-1's wiring.
- **M2 ≡ RR-B-7** MAJOR ⟨KT bank · caret B-1/L-3/C-2 fold + MISS-β3 · TT M-6/m-13/C-9 fold⟩ — a pointerdown on the percent caret scrubs the playhead AND captures the pointer on a transient node: the caret display div carries `@click.stop` ONLY (:12) while its sibling input carries `@click.stop` + `@pointerdown.stop` (:25-26) and the marker carries `@pointerdown.stop` (:81) — the caret's pointerdown bubbles to `onTrackPointerDown` (:168-171), which scrubs and `setPointerCapture`s on `event.target`: a node `startEdit` immediately unmounts. **MISS-β3 cure-shape lock verbatim**: *"the guard the author DID write is INERT — `@click` listeners on the track = 0, so :12/:25 `@click.stop` stop nothing — the fix REPLACES the inert guards with `@pointerdown.stop`, it does not stack a third modifier."* Capture on `currentElement`, never `event.target`. **K-8 kill lock**: m-13's latch escalation is UNREACHABLE (the latch target is keyed `kf.id` and Vue MOVES it, never remounts) — *"the escalation is unreachable and must not enter a spec."* Jump magnitude → SS-13.
- **D-1 (TimelineTrack)** MAJOR (axis BLOCKER killed at K-9; preserved at C-1's dissent terms) ⟨TT⟩ — scrubbing has no keyboard path anywhere in the tree (SC 2.1.1): bare `<div>`, six pointer/touch listeners + `@wheel` (:21-35), no role/tabindex/keydown; `defineExpose` publishes 10 members with `scrubT`/`scrub` ABSENT so no ancestor can drive it; the SpringTarget mirror the comment claims (:199) landed on the markers, not the rail. The one modality-specific residual harm, isolated and adopted: useTimelineOps.ts:28 + RibbonBar's argless `snapshot()` ⇒ **a keyboard user's every snapshot lands at 0%**. Design jointly with the seam; escalates back if the verdict rules scene-scrubbing the core promise. The caret's click-only-div limb folds to banked D-3. Feeds G8.

### P2 · Drag fidelity + rebuild economics

- **RR-B missed-1 (TimelineTrack)** MAJOR ⟨TT, adjudicator net-new, re-walked at the bytes⟩ — a marker drag TELEPORTS the keyframe to the pointer: `onMarkerPointerDown` :192-196 records no grab offset; `:174-178` emits the ABSOLUTE pointer percent; a legit grab starts up to ~12px off-centre ≈ 3% of the 400-512px rail at z=1 — 3× the component's own finest keyboard increment, shrinking as 1/z. Not cosmetic: useTimelineOps.ts:56-62 commits percent AND re-derives `selector`; `useRefHistory(clone:true)` banks the jump as an undo step. **Cure shape FIXED by banked class precedent GradientStopEditor C11/G5** — *"a grab is not a teleport"*: `grabDx`, capture grab offset at pointerdown, project pointer−offset (the value.js row is the severity precedent, not the identity — registry-swept: zero kf rows). Same class: KF.W13 drag-seam packet's grab-offset arm; SquareScene MISS-1 (KF.W11 arm c). Jump extent → SS-13 residue #4. Locks **G3**.
- **RR-B missed-2 (TimelineTrack)** MAJOR ⟨TT, chain re-walked end to end⟩ — every pointermove runs a full engine construction + CSS parse: `:177` per-move (~60-120/s) → KeyframeTimeline.vue:84 → useTimelineOps.ts:56-62 `rebuild()` unconditional (no early-out on zero clamped delta — a drag held at a rail end rebuilds at pointer rate for ZERO delta) → `buildAnimationFromTimeline` (its own docblock: "ASYNC because the engine constructor is HEAVY") → `new CSSKeyframesAnimation(...).fromKeyframes(...)`. No throttle, no rAF coalescing, no dirty check; held arrow auto-repeat same. Cure: throttle/rAF-coalesce AND dirty check (zero-delta must not rebuild). Scope bound verified and accepted: CPU/garbage churn, not a leak; needs ≥2 keyframes (the normal case). Emit granularity books at TT. Adjacent-NOT-identical: banked L-11 (KeyframeTimeline), N-6 — declared at the bank, re-declared here. Frame cost → SS-13 residue #5 ("drop-frame proof feeds the W7 throttle design"). Locks **G4**.
- **L-m-14 (TimelineTrack)** MINOR ⟨TT⟩ — `draggingKeyframeId` never reconciled against `sortedKeyframes`; mid-drag delete/undo dangles the id and the sink no-ops silently (useTimelineOps.ts:57 `find` → no-op, no signal). Cure: reconcile against the live collection; surface the miss. Same latch family as L-8/C-9 (KeyframeTimeline) — one identity policy.
- **ARB-1 (TimelineTrack)** INFO ⟨TT, the adjudicator's own missed row, bounds-checked⟩ — the blind drag zone: at zoom > 1 a captured drag continues past the window edge (`getPercentFromPointer` unbounded in pointer space :160-166; the clamp is domain 0-100, not window) while `overflow-x-clip` hides the marker; no auto-pan exists (pan-writer census); arrows/Home/End produce the same off-window disappearance. Cure: auto-pan past the edge, or clamp to the visible window. **Killed-claim lock**: the stronger claim ("drag is clamped to the window") was FORMED AND KILLED by the same seat — pointer overshoot extends the domain; do not re-derive. Joint with M-7 + RR-B missed-4 (joint) — the cure needs a pan writer to exist. Percept → SS-13 residue #10.

### P3 · Wheel / zoom / pan — ONE wheel policy: "prevent only on consumed events"

- **M5 ≡ RR-B-3** MAJOR ⟨KT · TT D-11/M-1/C-3 fold · caret L-11 DISTINCT⟩ — `@wheel.prevent` (TT :31) cancels page/pane scroll BEFORE the handler; a plain wheel matches NEITHER branch (ctrl/meta zoom; shift+zoomed pan); `touch-none` (:24) removes the touch escape — a 48-128px full-width scroll trap inside stacked `overflow-y-auto`. *"The affordance D-11 says is undiscoverable is ALSO a scroll trap."* Cure verbatim: **prevent only on consumed events**. Locks **G13**.
- **M-7 + RR-B missed-4 (TimelineTrack)** (joint) MINOR jointly, **escalation armed** ⟨TT; RR-A's MAJOR on M-7 in dissent⟩ — shift-wheel pan reads only `deltaY` ⟨anchor corrected at origin/master: the only delta reads are `:50` (the zoom factor) and `:59` (`event.deltaY * 0.1`) — the banked `:57-61` span is one line short; `deltaX` appears nowhere in the file⟩; pan is DISPLAY-ONLY (mini range bar: no handler, no role, no tabindex :4-18; complete writer set = zoom recentre + shift-wheel + `clampPan`; no drag-to-pan, no click-to-jump, no keyboard route). `event.deltaX || event.deltaY` is the one-token half; **the composition is the row** — *"the only pan readout is inert while the only pan gesture reads the wrong axis, and no axis assembled it."* Cure the two together. **TRIGGER verbatim**: jointly MAJOR (pan unreachable) if the SS-13 Chromium probe logs `deltaY: 0` on shift-wheel (residue #1). Joint with ARB-1.
- **D-11** MAJOR ⟨KT · TT D-m11/C-13 fold⟩ — zoom bar `v-if="zoomLevel > 1"` mounts mid-gesture (~32px row above the track, no reserved height/transition; `zoomLevel` continuous through 1.0 both directions) AND zoom has no discoverable affordance and no keyboard route (wheel + pinch only, verified across the full return surface). Cure: reserve the height (or transition the mount) + a discoverable, keyboard-reachable zoom affordance. Mount-shift magnitude → SS-13 residue #7. The `(panOffset/100)*100` clause folds to banked hygiene D-26.
- **L-11 (TimelineCaret wheel)** MINOR ⟨caret — NOT KeyframeTimeline's L-11⟩ — the open editor's wheel protection is accidental/non-local; ctrl/⌘+wheel still zooms the rail under the editor; no is-editing guard in `onWheel`; the only shield is the ancestor `@wheel.prevent` declared for zoom. Cure: explicit is-editing guard at the handler, inside the one wheel policy. Felt half → SS-13 residue #9.
- **L-m-10 (useZoomPan signature)** MINOR ⟨TT⟩ — `useZoomPan(trackEl: Ref<HTMLElement | null>)` admits null then asserts `trackEl.value!` (:5 vs :46); the sole caller supplies a genuinely-nullable `useTemplateRef`, so the signature cannot be narrowed and the `!` is a real unguarded dereference invisible to the absent type gate. Cure: guard, not assert — the signature is honest; the dereference is not. Verification blocked on OP-2.

### P4 · Geometry — re-derive before ANY cure (D-19 lock, OP-3)

**D-19 re-derive lock** (from caret K-13, binding on every row here, verbatim): every occlusion/clearance table cell shifts ~1px — `top:50%` resolves against the 46px PADDING box (the track declares `border border-border`, :24): collapsed mid 23 not 24, clearance ≈1.7px not 2.69, expanded vertex 84.21 under `scale-125`; direction unchanged. Banked D-17 carries the SAME uncorrected figures — BOTH tables re-derive before any cure.

- **M1** MAJOR ⟨KT RR-A · caret D·M-1/C-8 fold, reachability re-verified at preset 0%/100%⟩ — `overflow-x: clip` halves the celebrated 24px hit pad and cuts the caret in half at 0% and 100% — the two positions a keyframe timeline almost always occupies. The asymmetry is the proof of oversight: tick labels got three-way edge handling in the same file (:44-47); markers (`-translate-x-1/2`) and carets (`translateX(-50%)`) did not. **Bounded by S-1/S-2/S-3**: the mixed `overflow-x-clip overflow-y-visible` pair is CORRECT and deliberate — the cure is edge-aware placement matching :44-47, NOT deleting the clip. Pixel counts → SS-13 residue #4. Cure with D-m2.
- **D-17** MAJOR ⟨KT · caret D·M-2 fold + K-13 corrections⟩ — the expanded diamond occludes AND hit-steals its own caret (half-diagonals 11.31/14.14/16.97/21.21px vs `--caret-offset: 14px`; the marker carries `z-controls`, the caret nothing — the marker wins paint AND hit-test regardless of DOM order). The z-cell is a hit-steal, not only occlusion. Under the D-19 lock above. Rendered overlap → SS-13 residue #3.
- **RR-B missed-5 (TimelineTrack)** INFO ⟨TT, net-new⟩ — pointer→percent uses the BORDER box (`getBoundingClientRect` on a bordered element) while percent→pixel resolves against the PADDING box: a fixed ~1px origin / 2px span skew growing with any border change. Cure: exactly banked D-20's — one box for both maps. Rides D-19.
- **C-9 (TimelineCaret)** INFO, mechanism corrected ⟨caret — NOT THP's C-9⟩ — three-node overflow coupling, zero comments at any node: the 10px label overhangs ~1px (NOT a Card-clip truncation — that mechanism KILLED, #13); the element NEEDING `overflow-y-visible` is the `h-5` EDIT INPUT (spans ≈37-57, hanging ~11px into `.timeline-track`'s `margin-bottom: 1rem`); the ancestor chain is provisioned for the TICK labels. Cure: comment the contract at all three nodes. Namespace arm folds to banked D-28. Same padding-box family as D-17/K-13 and RR-B missed-5 — one geometry re-derivation.
- **D-14/i-1 + D-m2 (TimelineTrack geometry constants)** MINOR ⟨TT⟩ — **D-14/i-1**: `margin-top: 1.25rem` (scoped, :218-221) and `-top-5` (:45) are coupled magic numbers 174 lines apart with nothing stating the coupling (against DESIGN.md R4); home both constants the way the file already homed `--caret-offset` — the provenance that makes this an oversight. **D-m2**: tick-label edge-clamp threshold (2%/98%) under-sized for the actual 400-512px rail (half a "100%" label ≈ 5%; suffices only ≥ ~1025px). Adjudicated as banked S-8's bounding clause: the three-way handling is deliberate and the file's only edge-aware layer; **re-tune the constant, do NOT delete the mechanism**. S-8 is the provenance that makes M1 a defect rather than an unknown — cure M1 and D-m2 with the same edge-awareness.
- **D-4 (tick pitch, TimelineTrack)** MINOR (RR-A MAJOR in dissent; reopening = photographed collision) ⟨TT⟩ — tick-ladder pitch swings 9.4× and drops 5× on a 0.01 zoom increment (74.8% at z=2.99 → 30% at z=3; 39.9% at z=7.99 → 8% at z=8). Cure: the `1`-step rung is simply wrong — `2`/`5` gives 16%/40% pitch. MINOR because labels carry the information; the label-collision consequence needs a measured advance width → SS-13 residue #2.
- **D-12/C-12/i-2 (TimelineTrack, merged)** MINOR ⟨TT + caret contribution⟩ — SC 2.5.8's spacing exception unmet; near-coincident pads make the earlier marker unhittable below ~6% separation at z=1 (arrow step 1%, :204; `moveKeyframe` clamps only); the later DOM sibling wins paint and hit at equal `z-controls`; the caret and diamond 24px spacing circles INTERSECT (caret y≈31-55 vs diamond y≈12-36). Zoom is a real escape hatch → MINOR; the 24-vs-44 arm folds to banked S-1 BOUNDED. **Citation correction adopted**: `--rail-width = clamp(25rem, 33svi, 32rem)` = 400-512px; axis-L's "800px track" and axis-C's "~600px rail" are FICTIONS. Hit test → SS-13, folded with D-17/M1's photographs — do not re-measure separately.
- **m-6 (TimelineTrack)** INFO, structure input ⟨TT; adjudicated IN; neither reader adopted⟩ — markers and carets: the same datum rendered in two sibling `v-for`s with independent geometry calls (:62, :98). *"The Tooltip wrapper forces SOME split; the duplicated geometry does not follow from it."* Structure input for the rail redesign; cure with MISS-β4.
- **MISS-β4** INFO ⟨caret RR-β⟩ — culling asymmetry inverted vs cost: `visibleTicks` windows a ~5-span loop while BOTH N-node keyframe loops (full Tooltip subtree + caret per keyframe) are never windowed, patched per pan frame then discarded by the clip. Registry sweep: no banked culling row. One geometry pass for both loops, with m-6.

### P5 · Marker state, selection, motion, the expanded settlement

- **M3** MAJOR ⟨KT RR-A, net-new⟩ — a keyboard user cannot select without mutating: `onMarkerKeydown` :203-214 computes `next` only for arrows/Home/End, discards Enter/Space (a bare div synthesizes no click), emits `select` ONLY paired with `moveKeyframe`; each inspection keystroke lands in the debounced undo history. Pointer users get non-destructive selection two ways. Cure: non-destructive keyboard selection (Enter/Space select without retiming) — the correctly-targeted residue of D-3's killed 2.1.1 arm (K-3). **Reopening exported**: THP D-3's rung reverts for the keyboard modality if this lands. Bounds superlative S-2. Locks **G8**.
- **D-8 (state contract, TimelineTrack)** MAJOR ⟨TT⟩ — selection exposes no AT-perceivable state (`aria-selected`/`aria-current`/`data-state` all absent); one attribute buys glass-ui's free `prefers-contrast: more` + forced-colors treatment (both keyed on exactly those attributes — verified verbatim in the installed accessibility.css). **Caution lock (caret killed-claim #6)**: `aria-selected` is INVALID off its seven admitted roles — *"the cure ships invalid ARIA to harvest a stylesheet; real role or the primitive"*; the marker here DOES carry `role="slider"` — pick what the role admits (`aria-current`/`data-state`). Forced-colors COVERAGE arm folds to banked D-21 (→ KF.W9); the AT-state mechanism books here. `scale-125`-survives-HCM keeps this MAJOR-not-BLOCKER. Feeds G8.
- **RR-A missed-1 (TimelineTrack)** MINOR ⟨TT, registry-swept⟩ — no accessible container (`role="group"`/`aria-label` absent :21-35) so N `role="slider"` markers are orphan sliders in a generic div; tick labels un-hidden — ~5-15 bare percent strings interleave in the AT tree. Two-attribute cure. Banked a11y rows (D-3/D-20/D-21/M3/M8/D-10) — none touches container semantics. One a11y commit with the tooltip seam (P7). Browse-mode cost → SS-13 residue #9. Feeds G9.
- **RR-A missed-2 (TimelineTrack)** MINOR ⟨TT⟩ — selecting DELETES the hover affordance (both hover signals live only on the unselected branch :70-73) — the affordance gradient is inverted; the kept bespoke `scale-125` does not animate (D-12 (KeyframeTimeline)). Cure: selected state ADDS, not replaces, the hover register — same element/commit as D-12 (KeyframeTimeline).
- **D-12 (dead marker motion)** MAJOR ⟨KT id D-12 (KeyframeTimeline) · TT M-2/D-m1 headline fold + compiler-probe sharpening — NOT THP's D-12⟩ — the diamond's selection/hover scale is un-transitioned: the scoped rule transitions `transform` (TT :224-227, verbatim UNLAYERED in the shipped chunk) while Tailwind 4 writes individual `rotate`/`scale`/`translate`; `transition-all` on the marker (:69) is DEAD CODE that reads as live. **K-5 correction lock**: D-m1's consequence is REFUTED AND INVERTED — Tailwind 4.3.0 emits `scale`, nothing ever sets `transform`; the selection scale SNAPS exactly like the fill and only `border-color` transitions; D-m1's version would send a fixer to widen the shorthand and leave the scale dead. **K-4 (TT)**: the installed PRM bracket is `*:not([data-allow-motion])` `!important` — universal; the axis's honest PRM non-claim stands. Layer-roster cell carries the witness-staleness caveat (§Sequencing, SS-13 witness refresh).
- **RR-A missed-3 (TimelineTrack)** MINOR ⟨TT⟩ — one gesture, two motion languages on expand: the 80px `h-12↔h-32` height delta EASES (`transition-all duration-fast` on the track; the scoped rule declares only margins) while markers/carets/ticks SNAP; height is a layout-animating property the repo's own rail contract rejects. Cure: one motion language; the height must not be the eased property. Teleport interplay (`:disabled` same-tick — whether the 200ms ease ever paints) → SS-13 residue #7.
- **D-10 (expanded mode, TimelineTrack)** MAJOR ⟨TT — NOT KeyframeTimeline's D-10⟩ — `expanded` grows the track 48→128px (2.67×) and adds NO content (no `v-if="expanded"` branch, no slot, no lanes; ~62% of the box is a ~1.03:1 plate; graduations stay pinned at `-top-5`, stranded ~84px from the marks they measure). Cure verbatim: **"expanded mode earns lanes/curves or dies."** ONE SETTLEMENT with RB-7 + kf-ChannelControls C-8/D-6 + kf-KeyframeTimeline D-7's expanded-mode plate strip (→ KF.W6) — see §Sequencing; OP-1's `:key` row lands FIRST.
- **RB-7** MINOR ⟨kf-RibbonBar, net-new, reader-LC MISS #2⟩ — the four Timeline commands (Snapshot/Import/Export/Add CSS — NOWHERE else per the defineExpose census :296-307; the in-pane bar owns only undo/redo/expand/clear) vanish exactly when the timeline is most visible: the ribbon dispatches on `=== 'timeline'` (:69) while the widget mounts on the wider `isTimelineVisible = selectedControl === "timeline" || isTimelineExpanded` (ChannelControls :377-379); persisted `isTimelineExpanded`, reachable in three clicks. Settles inside the D-10 expanded settlement. Same family as D-16 (rung reopens if the ribbon ever detaches from the pane).

### P6 · The caret — rounding, identity, focus, posture

- **L-2/C-3/D·M-4 (TimelineCaret)** MAJOR ⟨caret; RR-α dissent preserved: the caret remains the chief interactive integer feeder — if a wave ever establishes an integers-only invariant at the UI, MISS-β2 inherits C-1's full user-visible harm⟩ — the caret is a rounding well and its commit path dirties history when changing nothing: `Math.round` at :13/:18/:62; `commitEdit` reachable by `@blur` ALONE (:22), never compares incoming (:58-65); the model is CONTINUOUS at every producer; `moveKeyframe` allocates a fresh selector object per call (:60) + deep-keyed `useRefHistory` ⇒ **a pure READ gesture (open, blur) mutates the document's history unconditionally**. Cure verbatim: **compare-before-commit AND display at model precision** (family precedent: banked kf-MatrixEditor ME-42). **Kill lock**: C-1's float story ("29% → 28.999…996%") is KILLED — never re-derive; the surviving locus is MISS-β2 (KF.W8). ONE ROW with MISS-β1. Locks **G7**.
- **MISS-β1 (TimelineCaret)** MAJOR ⟨caret RR-β, booked by ruling⟩ — the rounding is the cluster's strongest collision manufacturer; a collision silently DROPS a keyframe from the built animation AND the exported CSS while the track keeps painting two (42.2 and 42.4 each read "42%"; open-and-blur each — a READ gesture — makes them identical; `selectorText` the same map key; merge admitted timelineEngine :42, keyed :40, feeding build :51-53 and export :62-72 while `state.keyframes` holds two). UI and artifact disagree with NO signal. Cure: **minimum-separation or coalesced rendering, DECIDED, not inherited** — one decision with L-2/C-3/D·M-4 and N-6 (the N-10 precedent class: in neither parent; both cited, neither re-booked). Locks **G5**.
- **N-6** MINOR ⟨KT RR-B · caret L-14 cell · THP C-5 ladder anchor⟩ — same-percent keyframes silently MERGE in the built animation and exported CSS while the track renders two diamonds (timelineEngine :40-48, the :42 comment admitting it); arrow keys step whole percents ⇒ collisions COMMON; no dedupe/coalesce/surfacing. Decided with MISS-β1, one cure. **Ladder constraint ruled**: a display-only collision cannot out-rank an output-corrupting one without inverting the ladder. Locks **G5**.
- **D-3 (percent readout a11y)** MAJOR ⟨KT · TT D-1 caret-limb + D-12 spacing folds · caret B-2/L-12/C-11 fold + D·M-8 cell⟩ — the percent readout is a click-only `<div>` (TimelineCaret :6-13: no role/tabindex/keydown/aria; `cursor-pointer` the entire affordance) and its successor `<input>` is 40×20px at 10px type (:14-27) — SC 4.1.2 + SC 2.5.8, and the D·M-8 cell (the caret/diamond 24px spacing circles INTERSECT, caret y≈31-55 vs diamond y≈12-36) defeats the 2.5.8 spacing exception. The a11y arm of the caret redesign: cure with m-5/L-6/C-5 (TimelineCaret)'s one-shape decision + MISS-α2's focus restore, under G8. **K-3 kill lock (re-killed at the caret, #5)**: the 2.1.1 citation and the BLOCKER rung are REFUTED — retiming IS keyboard-operable on the sibling marker; the correctly-targeted 2.1.1 residue is M3. *Dissent: axis-D/-C BLOCKER rungs killed twice; never re-derived.*
- **m-5/L-6/C-5 (TimelineCaret)** MINOR ⟨caret⟩ — `keyframeId` is dead API beside identity-free emits (declared :36, bound TT:100, read nowhere; `select` :44 carries no payload so the parent closes over `kf.id`). Cure verbatim: the intended linkage (`id`/`aria-controls` to the sibling `role="slider"` marker) is ALSO MISS-α2's cure — **decide one shape, wire or delete**; one decision covers m-5, MISS-α2, part of RR-A missed-1 (TimelineTrack).
- **MISS-α2 (TimelineCaret)** MINOR ⟨caret RR-α⟩ — Enter AND Escape destroy keyboard focus: the `v-else` unmounts the focused input, nothing calls `.focus()` on a successor; the user lands on `<body>` at the top of the tab order. All three axes traced the blur triad; none noticed the focus goes nowhere. Natural restore target = the sibling slider diamond — exactly what dead `keyframeId` would address. Live half → SS-13 residue #11; the nothing-refocuses half stands statically.
- **m-1/L-10 (TimelineCaret)** MINOR, **promotion armed** ⟨caret⟩ — focus never requested; the whole exit contract (blur/Enter/Escape) hangs on `select()`'s unasserted side effect (:53-55 `nextTick(() => inputEl.value?.select())`; no `.focus()` in the file). Cure: call `.focus()` explicitly. Blink-no-op assertion KILLED (#11 — `select()` and the `setSelectionRange` family are separate spec hooks); the design objection is source-decidable and stands. **TRIGGER**: SS-13 residue #1 ("click a caret, read `document.activeElement`" — the component's single sharpest probe) decides BOTH this row's promotion (stuck-open editor = MAJOR) AND MISS-α2's reachability.
- **m-4 + MISS-α1 (TimelineCaret)** MINOR ⟨caret⟩ — no discoverable entry point in any state: the selected branch carries no `hover:` variant (:10); no title/tooltip/underline/icon, against the marker's instructional `<Tooltip>` (:75); AND the residual signal axis-D credited does not exist — `cursor-pointer` sits on the ENTIRE track (:24, re-measured), so the caret's own (:9) differentiates nothing. Cure: an affordance the caret does not share with the whole rail. No fold to kf-OrbitalDrag's cursor census (it never covered :24-vs-caret).
- **L-9/m-8 (TimelineCaret)** MINOR ⟨caret⟩ — `min`/`max` decorative; implied `step=1` marks every fractional model value `:invalid` while the app commits it anyway; no form/checkValidity in the cluster. Cure: `step="any"` or the NumberField swap (the swap itself is C-4/D·M-9 → KF.W6 — if it lands there, this row discharges with it; edge declared both ends). **Protection lock (S-3)**: the `:62` clamp is where untrusted DOM text becomes a model number — it survives any triple-clamp dedupe; do not delete it.
- **L-7 (TimelineCaret)** MINOR ⟨caret⟩ — `const props =` dangling binding, zero `props.` reads. Delete. Root cause is the banked KF-CE-16 gate hole; verification blocked on OP-2 (vue-tsc + `noUnusedLocals`, K3).
- **MISS-α4 (TimelineCaret)** MINOR ⟨caret RR-α⟩ — the file's single most load-bearing line (`:59` `if (!isEditing.value) return;`, the re-entrancy guard) is uncommented in a cluster that comments heavily — *"the exact line a dedupe wave deletes"*; challenge-C had to write the do-not-delete note in an AUDIT document instead of in the code. **KF-CE-41 law verbatim: the comment is part of the defect surface — the cure commit carries the comment.** Any dedupe row touching :58-65 carries this comment FIRST. This is the general law under which every "comment rides the cure" clause in this spec operates — the kf-SquareScene class: **comment-stated invariants are test obligations**.
- **C-10 / C-9 / L-15 / MISS-β4 (TimelineCaret INFO group)** — split by mechanism: C-9 (TimelineCaret) and MISS-β4 cure in P4's geometry pass; L-15 (TimelineCaret) below; C-10 here — INFO, **cure-shape PROHIBITION** ⟨caret⟩ — `update:percent` is `v-model`-shaped but `v-model:percent` would BYPASS the ops layer (selector re-derivation + `rebuild()`). Verbatim: **"`defineModel` here would be actively WRONG; rename the emit off the protocol."** This wave RULES the exception explicitly against the demo's `defineModel`/named-tuple law cited by D-m5: the law stands everywhere else in the cluster; this emit is the ruled exception, by mechanism (ops-layer bypass), not by taste.
- **L-15 (TimelineCaret)** INFO, ruling-dependent, UNACTIONED ⟨caret⟩ — silent swallow on unparseable input vs the cluster's toasting posture; the axis itself flags it ruling-dependent (a garbage keystroke is arguably a cancel). Verbatim: *"recorded as filed, UNACTIONED pending the W7 posture ruling."* **The W7 spec OWES a failure-posture ruling for the cluster** — delivered at G14: one ruling, its consumers enumerated there.

### P7 · Hover preview — cache, ghost, tooltip, typography collision

**MISSED-3** SPEC-INPUT (MINOR rung, binds two waves) ⟨THP RR-A, arithmetic re-derived⟩ — the corpus's two headline cures COLLIDE: `text-mono-small` makes rows 19.6→28px (`--type-small` 14→20px × leading 1.4), cutting the 96px scroller from ~9 visible rows to 4.9→3.4 — **D-4's cure materially WORSENS D-3**, and the two rows are adjacent in the same axis document. **Law (N-10's typography twin, verbatim): a repair resizes or drops `max-h-24` in the SAME COMMIT; `normal-case` always pairs `tracking-normal` (MM-29 — `--type-tracking-caps: 0.1em` rides onto the literal otherwise).** Declared from this end to KF.W6; no wave ships the regression.

- **D-4/L-4/C-5** MAJOR ⟨KT · TT M-4 fold · THP D-8/L-D1/C-3 fold + provenance correction⟩ — `previewCache` never invalidated or evicted; the tooltip pairs a LIVE percent with a PRE-EDIT capture; ids stable across every mutation ⇒ every edit makes every cached thumbnail a lie; failed captures retry forever with no feedback; removed/cleared/imported-over frames orphan base64 PNGs (monotonic ids, no `delete` anywhere); the sibling history is bounded (`capacity: 50`) — the contrast that convicts. **Merged mechanism correction (RR-A + RR-B convergent)**: `scrubAndCapture` swallows its own failures (:92-93, :110-111) so the `// KEEP:` catch (:228-230) documents unreachable conditions — the ONE throw it can catch is `canvas.toDataURL`'s cross-origin SecurityError (:226), precisely the failure the comment does not name; the comment rides the cure (KF-CE-41). **Provenance correction**: `updateKeyframeProperty` is DEAD (banked L-13); live routes = `moveKeyframe` + `onKeyframeCSSChange`. **K-7 kill lock**: the fallback is the DEFAULT outcome of failure, not dead code — retry-forever is the sharper true statement. WebGL blank-resolve arm → SS-13 residue #1 ("the single sharpest probe for this component"). Locks **G10**.
- **D-10 (KeyframeTimeline)** MAJOR ⟨KT · THP D-9/C-2 fold · TT m-16/C-8 fold — NOT TimelineTrack's D-10⟩ — capture armed on `@mouseenter` ONLY; the tooltip opens on FOCUS (reka `TooltipTrigger` `focus: handleFocus` → `onOpen`) — keyboard users get `previewSrc` permanently undefined: the 4-property ghost forever. C-2's sharpening rides as spec input: the leaf declares no emits and cannot request its own content. **Cure JOINTLY with MISSED-1 — one seam.** Compounds GHOST-PLATE (the branch the keyboard path ALWAYS receives). Feeds G9.
- **MISSED-1 (THP)** MAJOR ⟨THP RR-A, decider re-executed · RR-A missed-4 (TimelineTrack) description arm⟩ — the accessible description reka builds EXCLUDES the alt all three axes filed as their headline superlative, and announces an unpunctuated run-on frozen at first mount: `TooltipContentImpl.js:87` `computed(() => props.ariaLabel || currentElement.value?.textContent)`; (1) `textContent` excludes `<img alt>` so THP:8 contributes NOTHING; (2) block boundaries contribute no separator; (3) `.textContent` is an UNTRACKED DOM read captured once, never re-derived on the ghost→image or Capturing→settled swap; on the focus path the `<img>` never renders and the ghost has no text/role/name — an AT user's ENTIRE preview is this one frozen run-on. Per-marker arm: tabbing ten markers reads ten uppercased stylesheets. Cure verbatim: **pass a composed `ariaLabel` at the mount** (`TooltipContent` accepts it at :16, unpassed at TT:86); joint with D-10 (KeyframeTimeline) — ONE seam. Bounds superlative S-1 HARD. Per-AT utterance → SS-13 residue #9. Locks **G9**.
- **M7** MAJOR ⟨KT RR-A, falsifier RUN AND HELD · THP D-1/C-1 fold + census corrections · TT missed-4 arm⟩ — `text-admin-label` force-uppercases the declaration dump (`transform: translateX(10px)` renders `TRANSFORM: TRANSLATEX(10PX)`): case-lossy over case-sensitive CSS value grammar (`var(--myVar)`, `url()`, custom idents), on the instrument's ONLY textual ground truth — *"in a demo whose subject is CSS, the tooltip lies about the text of the CSS."* Falsifier held: ZERO CSS rules target `[data-register]` across demo/styles and the whole glass dist (it exists only as the T.D4 font-census selector contract in font-roles.json). **Cure guards**: `normal-case` pairs `tracking-normal` (MM-29); the `text-mono-small` route resizes the box (MISSED-3). Census corrections carried: 15 occurrences / 9 files (not 16); THP:20 sole `data-register="code"` co-occurrence; 8 such .vue sites; 5 `normal-case` sites (not 3). **DISSENT-4 recorded as law for this spec's execution: require the verbatim element, not the class list.** Joint a11y commit with MISSED-1 — same string. Feeds G9.
- **D-7 (THP)** MAJOR, ghost-family head ⟨THP = TimelineHoverPreview, re-scoped by adjudication⟩ — the ghost misrepresents where it matters most: (1) `opacity: "0"` survives the capture filter as a truthy string ⇒ the world's most common authored keyframe renders an INVISIBLE 64px box that still passes the guard and occupies layout; (2) `scale(0.3) ${matrix}` applies translate INSIDE the scaled frame — a 300px authored translate displaces the swatch 90px, SILENTLY CLIPPED by the tooltip's own `overflow-hidden` (a wrong-but-invisible preview, not a spill); (3) the `{}` arm survives only at authored-vars scope. Cure input adopted: value.js ships `decompose.ts` for exactly the scale-only treatment. **The ghost redesign is ONE design problem: D-7 + MISSED-4 + GHOST-PLATE + L-D8/C-4(a) — spec'd as such.** Kill locks: K-3 (L-D10's escape arm REFUTED — TooltipContent itself clips) and K-4 (`{}` UNREACHABLE on the capture path — `opacity`/`background-color` always survive; reachable only via import/typed editor). Clip extent → SS-13 residue #4. Feeds G10.
- **MISSED-4 (THP)** MINOR ⟨THP RR-A⟩ — `scale(0.3)` shrinks the ghost's own FRAME, not just its payload (border 0.3px at 30% alpha, radius 1.2px, swatch 19.2px) while the layout slot stays 64px — a FOURTH envelope on the banked M9 three; the border goes sub-pixel exactly when a near-transparent background makes it the only legibility aid. Cure verbatim: **decompose; apply scale to a WRAPPER, not the plate.** Both axes read the line; neither noticed the frame scales with the content.
- **GHOST-PLATE (THP)** MINOR ⟨THP RR-B, registry-swept, not banked⟩ — inline `backgroundColor` beats `bg-muted/30` unconditionally; a transparent target renders the whole branch as a 1px 30%-alpha hairline on a glass plate — *"the branch the keyboard path ALWAYS receives (D-10 fold), strictly worse than the opacity-0 arm because it needs no authored keyframe."* Owned by the ghost redesign; joint with D-10 (KeyframeTimeline).
- **L-D8/C-4(a) (THP)** MINOR ⟨THP⟩ — `ghostStyle` is a required prop that is a pure function of prop #1, derived in a geometry component with no other use for it, required while its two siblings are optional. Cure verbatim: derive locally; delete the prop. **The withdrawn perf arm STAYS WITHDRAWN** (the slot is `withCtx`-lazy, re-verified by both readers) — do not re-manufacture the slot-laziness hypothesis.
- **m-7/m-8 (TimelineTrack, merged)** MINOR ⟨TT⟩ — `getGhostStyle` hardcodes 4 properties against the 17-property capture set (an all-`filter` keyframe renders NO ghost); composes `scale(0.3) ${transform}` UNVALIDATED (`transform: none` drops the whole declaration; reachable — the inline editor commits untyped text). Cross-ref L-6/C-4 and C-7 — **the validation belongs at the parse boundary, not at the ghost**; the validation half sequences AFTER L-6/C-4's delegation.
- **L-D4/C-4(b) (THP)** MINOR, narrowed ⟨THP⟩ — the `// KEEP:` comment's documented fallback is FALSE on the authored-vars path: a no-ghostable-property keyframe renders NOTHING with no explanation, beside a file that renders an explicit empty state 8 lines away. Cure: a terminal `v-else` state; the comment rides the cure (KF-CE-41). Survives only at authored-vars scope (K-4); corroborates banked K-7 and supplies the mechanism that seat lacked.
- **M9** MINOR — **rung locked** ⟨KT RR-A · THP D-6 fold rung-kept + D-13 reflow arm fold⟩ — the tooltip resizes under the pointer when the capture lands (unsized 64×64 ghost swapped for `w-36 h-auto` of unknown aspect, mid-hover, no reserved box) — *"on a motion-quality instrument, the one async surface jumps."* Cure: reserve the box. **Rung lock verbatim**: *"an identity fold cannot carry a different severity than its bank without re-adjudicating the bank"* — RR-B's MAJOR re-rung REFUSED and recorded. Decode-reflow + WCAG 1.4.13 hoverability → SS-13.
- **D-2/L-D9 (THP)** MINOR (BLOCKER killed on its own falsifier; RR-A MAJOR preserved: reverts if Monaco detaches from the timeline card) ⟨THP⟩ — in-tooltip truncation, no `title`/wrap/copy, ~29 chars/row (206px ÷ ~7px/char at the 10px rung + caps tracking) vs 35-char `background-color` rows and 80+-char matrices. K-2: every value is one selection away, full-fidelity, in Monaco at 250px (:123-127/:239-244) — *"the reason this is MINOR, not the reason it is nothing."* Cure: `:title` or wrap; joint with D-4's re-rung (KF.W6) under MISSED-3's guard.
- **D-3/L-D6/C-6 (THP)** MINOR (RR-A MAJOR preserved with the M3 reopening) ⟨THP · D-13 net arm (TimelineTrack) folded⟩ — a `max-h-24 overflow-y-auto` scroller inside a portalled `role="tooltip"`, keyboard-unreachable, APG-hostile either horn; `truncate` on every row against a 17-property capture set — the tooltip's stated purpose defeated for most `transform`/`box-shadow` values; wheel does NOT dismiss, the pointer path is fully functional (the axis's falsifier honesty stands: glass forwards `disableHoverableContent`, default off). Cure: resize or drop `max-h-24` — **ONLY in the same commit as the type-rung change (MISSED-3's guard)**. Reopening: reverts for the keyboard modality if M3 lands. Chrome ≥127 focusable-scroller wording → SS-13 residue #10.
- **C-5 (THP)** MINOR (RR-A MAJOR preserved: reverts the day scroll-phase authoring becomes first-class — "aren't YET wired" is a dated promise) ⟨THP — NOT SpringTrace's C-3 nor the caret's C-5⟩ — the `KeyframeSelector` discriminant dropped at :3/:8: named scroll phases caption a percent the author never wrote (`entry 100%` and `cover 0%` both "25%"; `contain 0%`/`cover 25%` both "38%"); `selectorText` sits one call away, already consumed by timelineEngine. Cure: render the authored form, resolved percent secondary. Reachability SETTLED (adapter.ts:179-196 emits named selectors as literal text; L-D13's falsifier is dead). **Substrate lock**: VACUOUS at stale HEAD, REAL at origin/master — the row itself names the ref (G12). Feeds G6.
- **D-12 (THP) + D-15 (THP) (TimelineHoverPreview layout pair)** MINOR ⟨THP — NOT KeyframeTimeline's D-12/D-15⟩ — alignment axis changes mid-stack with no rule (`items-center` vs the full-width left code block; 144px media narrower than its 206px annotation, ~31px dead gutters); no height cap/aspect-ratio on the capture while the cheap sibling IS capped; reka publishes `--reka-tooltip-content-available-height` and glass's TooltipContent consumes none of it (**producer half → BH relay**). Cure: one layout rule; consume the published variable. Geometry FIRM (P2-DEAD decided the padding question). Gutter/tall-capture → SS-13.
- **C-9 (THP) — coverage** MINOR ⟨THP⟩ — zero test coverage: no test names the SFC or either cache prop; `timeline-undo.test.ts` exercises the same state without this surface. Verbatim: *"locks the cures above"* — the wave's born-RED mount test. Sibling of kf-SequenceScrubber L·D-12 (no spec mounts SequenceScrubber; the keyboard divergence is exactly what a mount test reds on run one) — banked N-10 class, second instance. **One coverage decision for the wave** — locks G11, which locks every other gate's runnability.
- **L-D15 / MISSED-6 / L-D14 / P2-DEAD (THP hygiene group)** INFO ⟨THP⟩ — duplicated `Math.round` at :3/:8 (three superlatives celebrate it as drift-proof single-sourcing; it is one expression written twice); no `@error` on the `<img>` (decode failure paints the broken-image glyph); `overflow-y-auto` alone coerces :20 into a scroll container on BOTH axes (CSS Overflow 3 — the mechanism behind D-3's focusable-scroller horn; cure with S-3's mixed-pair discipline the cluster already banks); `class="p-2"` at TT:86 is dead — delete the token. **P2-DEAD's producer seam (glass `cn` padding groups) is BANKED and already on the BH relay (TEP-12/MCP-31/SwatchHoverMenu family) — NOT re-filed; only the demo residue books here.**

### P8 · Import/export, dialogs, failure posture — one posture, one round-trip commit, one fold

- **L-6/C-4** MAJOR ⟨KT⟩ — a hand-rolled CSS declaration parser+emitter inside the SFC of a CSS engine's demo (:251-261 `split("\n")` + `indexOf(":")` + comment-prefix skip; `:263` `kf.vars = newVars` — WHOLE replacement, so anything the scanner misses is DELETED, not mis-parsed; emitter :239-244 the mirror). Cure: **DELEGATE** — the sibling path honours the doctrine (timelineEngine.ts:13/`:90`), making this a BYPASS, not an omission; `@mkbabb/value.js/css` exports the collectors. The delegation target must exist or be named (OP-5) — edge to KF.W2 declared both ends. Feeds G6.
- **C-7** MAJOR (posture arm HERE; crash identity FOLDS to megatranche R1, NEVER re-booked) ⟨KT, settled by the seat's OWN execution⟩ — the inline editor is a live untrusted-CSS ingress to the R1 crash shape: against installed `@mkbabb/value.js@4.0.0`, `parseCssValues("oklch()")`/`rgb()`/`hsl()`/`color()` ALL THROW (`TypeError: … reading 'replace'`); chain: :252-260 admits `oklch()` verbatim → `kf.vars` → un-awaited `rebuild()` (:264) → engine parse → THROW → swallowed at useTimelineBuild.ts:47-50 (`console.error` + `animation.value = null` — the file's ONLY non-toasting failure vs :121/:133/:137/:148/:155/:157). What books here: the SILENT posture. This wave also carries the **scope correction to GATE-VERDICT §F-2** ("no known consumer feeds the crash shape" — the census covered kf's `src/` seams, not this live typing surface) and the fuzz-entry note → KF.W2/W3. Locks **G14**.
- **D-15 (empty/failure states)** MAJOR ⟨KT id D-15 (KeyframeTimeline) · TT D-15 fold: "no empty state; six undiscoverable gestures"⟩ — empty / single-frame / rebuild-failure states UNEXPRESSED (no empty branch in the whole template; the `<2` contract surfaces only as an export-time toast; *"the one failure reachable BY TYPING is the one that doesn't toast"*). Glass-conformance tail: `/forms` publishes `invalid` + `useUserInvalidAria` on a subpath the file already imports. Whitespace-submit arm folds → banked R-17; closes-before-parse arm folds → banked R-4. In the G14 posture ruling.
- **L-11 (KeyframeTimeline)** MINOR ⟨KT — NOT the caret's L-11⟩ — `rebuild` async-typed, called as `() => void` (useTimelineOps:19); `snapshot` toasts SUCCESS before the rebuild can fail (:31-34); failures reach only `console.error`. Cure: await or type honestly; the toast follows the outcome. Adjacent-NOT-identical to RR-B missed-2 (declared at the bank). In G14.
- **N-8** MINOR ⟨KT RR-B⟩ — `captureSnapshot` drops computed `none`/`auto` (:16-21) over a set including transform/filter/box-shadow: rest snapshots asymmetric; "animate to none" UNAUTHORABLE. Cure: preserve (or record intent). **Interaction lock**: THP's K-4 kill depends on `opacity`/`background-color` ALWAYS surviving this filter — any change here re-opens the `{}` frequency question (P7). Cure with N-9 and L-14/C-16. Feeds G6.
- **N-9** MINOR ⟨KT RR-B · THP L-D12/C-10 fold⟩ — round-trip fidelity gap + dead surfaces: `importCSSToTimeline` DROPS per-stop `animation-timing-function`; `TimelineKeyframe.easing?` never written/read; `isPlaying` dead state (declared :31, returned :109, never written); `clampPan` never destructured. **Round-trip fidelity is a GATE obligation (G6), not a hygiene note.** `CSSPasteDialog.textEl` arm folds → banked R-19. One round-trip commit with N-8 + C-5 (TimelineHoverPreview).
- **N-2** MAJOR ⟨KT RR-B · TT D-m4 fold · THP C-10 sharpening⟩ — the keyframe Label input is write-only dead state (`v-model="selectedKeyframe.label"` :106 + `label?: string` timelineTypes:11 are the ONLY two occurrences; stored, deep-cloned into all 50 undo snapshots, read by NOTHING; the identity panel shows a NUMBER instead of the typed name — *"D-9 spent a MAJOR on this input's typography without noticing the control is inert"*). Cure: **wire it (hover preview + export + accessible name — D-m4's one-cure-for-both) or delete it — RULED HERE, before KF.W6 spends D-9's typography MAJOR on an inert control.** Edge declared both ends. In G15's disposition discipline.
- **R-3** MAJOR ⟨kf-CSSPasteDialog⟩ — "Add" promises merge, performs replace (`description="…merge into the timeline"` + `button-label="Add"` + FilePlus2 → `state.value.keyframes = imported` :152, whole-array; `importCSSToTimeline` never reads existing state). Cure verbatim: **implement merge or tell the truth; the copy rides the same commit.** Mitigation (undo, capacity 50) adjudicates the rung, not the row; the code fix lands in the consumer/composable. One import-semantics commit with N-9 + C-5 (TimelineHoverPreview), under G6.
- **R-4 (+ KAD-9, KAD-10)** MAJOR ⟨CSSPasteDialog · KeyframesAddDialog⟩ — submit has no acknowledgement channel; a failed parse destroys the paste (open-watch wipes text on reopen; **precision correction**: the close sits inside `if (text.trim())` — conditional on non-empty text, UNCONDITIONAL on parse result); KAD-9: the only in-dialog completion signal is ONLY EVER VISIBLE ON FAILURE (success unmounts the bar mid-flight); no `aria-live`/`aria-invalid`/error node; KAD-10: async submit, no busy state, re-entry re-appends stops (glass `Button` ships `loading`/`aria-busy`/derived-`disabled`, none bound). Cure verbatim: busy/error/awaitable-close or `v-model:text` hoisting (the twin's shape), cross-referenced to KF.W2's posture registry from both ends — *"a sixth failure posture for the lane-library §7.5 registry."* R-19's `initialText` is exactly the channel R-4 needed, minted and never wired. In G14.
- **R-7 (the twin fold) ≡ KAD-F3** MAJOR ⟨both dialogs⟩ — near-twin duplication (161-line re-authoring of the 80-line shell; byte-identical `@interact-outside` guard; same `onInput` body; same flush `<pre><code>{{ text }}</code></pre>` shape; a `footer-extra` slot shaped exactly for the twin's `progressBarEl`; TWO OPPOSITE state-ownership contracts for one widget). **Fold-direction constraints pinned at the bank, verbatim**: adopt THIS file's (CSSPasteDialog's) a11y composition (siblings under DialogContent) AND the twin's text-hoisting contract; genuine deltas are exactly FOUR (DialogTrigger, highlighting, Tab-insert, reformat) — none requires re-deciding footer/type-rungs/heading-nesting; the `defineModel` half has no defence (six-site house idiom). The fold experiment OWNS L-4's open falsifier (whether KAD folds across the `footer-extra` seam without losing `<pre>` identity for `useCodeHighlight`). Consumer end = the two mounts at KeyframeTimeline :135/:145 (L-16/L-17). **KAD's `innerHTML` BLOCKER is KF.W5's, front-loaded — this wave does not gate on it and must not wait on the fold for it.** Locks **G15**.
- **KAD-6** MAJOR ⟨KAD⟩ — `DialogDescription` nested inside `DialogTitle` (`<h2><h3>…</h3><p>…</p></h2>`; `DialogHeader` unused though in the same barrel, used correctly at KeyboardShortcutsModal.vue:4-8) — content-model violation + accessible-name pollution via `aria-labelledby`. Discharges with R-7's fold, or stands alone at KF.W6 if the fold is declined — declared both ends. In G15.
- **KAD-13** MAJOR ⟨KAD⟩ — the `format` prop's JSDoc declares a pure formatter; the wiring secretly WRITES the parent model; `reformat()` never emits `update:text`; TRIPLE-WRITE — the `:149` write is DEAD. Cure verbatim: `emit("update:text", formatted)` + a genuinely pure formatter ("which the triple-write makes cheap") — **this IS R-7's text-hoisting constraint**; the prose correction rides the commit (KF-CE-41). In G15.
- **R-17 / R-19 / R-21 / R-22 / R-23 / KAD-21 (dialog MINOR group)** MINOR — whitespace-only submit is a silent dead click (R-17, folds KT D-15's whitespace arm); three dead configuration surfaces (`initialText`/`preClass`/`footer-extra`) + `defineExpose({ textEl })` publishes an internal DOM handle, structurally null while closed (R-19); `preClass` REPLACES instead of `cn()`-merging — the idiom is exported from the barrel the file already imports (R-21, "dies in the fold or the swap"); unguarded `EventTarget → Element` casts charged asymmetrically — either both are MINOR or neither (R-22); no keyboard submit path in all 80 lines — no Mod+Enter, a keyboard user Tabs out of a multi-line editor to commit, in a repo that treats shortcuts as first-class (R-23); the description restates the title, capitalization disagrees with itself (KAD-21). **Discharge discipline (a G15 condition)**: most die in R-7's fold or KF.W6's S-9 Textarea swap ("kills R-1/R-6/R-8/R-9/R-12/R-14/R-20 in one move") — **the execution seat STATES which rows the KF.W6 swap already discharges BEFORE spending a cure**.
- **KF-CE-2 (parent half)** BLOCKER ⟨kf-CSSCodeEditor, banked⟩ — a stale in-flight 200ms debounced emit lands on the WRONG keyframe after selection change; `kf.vars` FULL-REPLACED with the previous keyframe's text — silent data loss (helpers.ts:15-24 no cancel handle, closure-private timer; the guarded `setValue` fires `onDidChangeModelContent` synchronously so the armed timer is neither cancelled nor re-armed; KeyframeTimeline binds NO `:key` :123-127; `:246-249` resolves the target from the LIVE ref at emit time; `:263` assigns, not merges). Parent-half cure: **emit-time target resolution via arm-time id capture**. **THREE HARD LOCKS**: (1) the two banked BLOCKERs that destroy this component's data (this stale emit; ChannelControls' `:key` remount) settle BY OR BEFORE this wave (OP-1); (2) **cure-collision resolved by this spec (N-10 class): the bank's "keyed remount" option is REJECTED — it is the exact mechanism kf-ChannelControls L-2/C-2 convicts as a data-loss BLOCKER; arm-time id capture is the ruled shape** (a keyed remount would need written justification against L-2/C-2 — none is offered); (3) **`L-15-PROTECTED` (the do-not-delete lock — a SPEC-LOCAL LABEL, not a banked id; §Carry preamble)** ≡ the K-8 protection lock banked at `kf-KeyframeTimeline.md:125`, a kill that PROTECTS live code: the `:248-249` null-check guard is LOAD-BEARING against this exact timer; any dedupe that removes it ships a crash; the guard gains its comment (KF-CE-41) and binds L-16/L-17's descriptor de-duplication. Child half = EDITOR-UNIT (cancel-on-external-write + cancel-on-unmount, one `replaceContent()` seam; §Sequencing); neither half alone closes the loss. Feeds G2/OP-1.
- **KF-KC-11 (undo-trail unification) + KF-KC-52** MAJOR arm (KC-11) / MINOR (KC-52) ⟨kf-KeyframeCard⟩ — the card's destructive X has no dialog and no undo, and the demo's only undo (`useRefHistory` at useTimeline.ts:77-88, Mod+Z wired) is bound to timeline state, not `templateFrames` — the card's delete sits OUTSIDE the app's only undo trail; KF-KC-52: useTimeline's docblock cites a "100ms debounce" keystone that is actually 1000ms (the 100ms is parsing.ts:62's reprojection throttle). **This wave rules the undo authority ONCE**: the same `useRefHistory` instance that L-2/C-3/D·M-4 dirties on a read, S-4 celebrates, RR-B missed-1 (TimelineTrack) banks teleports into, and L-2/C-2 destroys wholesale — **one authority, four consumers, one ruling**. The docblock correction rides INSIDE the ruling (KF-CE-41). **Homing (this spec's act)**: the `canRemove`/confirm-or-undo DECISION is homed here as a consequence of the trail ruling; its cure site (`KeyframeCard.vue`) is outside §Bounds and is declared to the CARD-UNIT owner from both ends.

### P9 · Contract & dead-surface hygiene

- **L-8/C-9 / L-11 / L-16-L-17 (KeyframeTimeline hygiene group)** — split: L-11 (KeyframeTimeline) rides P8's posture family; L-16/L-17 below; L-8/C-9 here — MINOR ⟨KT — NOT the caret's or THP's C-9⟩ — two divergent removals nine lines apart: the ✕ (:117, `!` assertion) dangles `selectedKeyframeId`; `removeSelectedKeyframe` (:289-294) clears it; undo-resurrection re-opens the editor on the restored id. One-token cure: `@click="removeSelectedKeyframe()"`, deleting the `!`. **K-1 kill lock**: both manufactured harms ("press Delete twice"/"no memory of selecting") REFUTED at :289-294/:48-54 — never carried. Same latch family as L-m-14 — one identity policy.
- **L-12/C-8 / L-13 / L-14/C-16 / N-9 (dead-surface group)** — split: N-9 rides P8's round-trip commit; L-13 / L-14/C-16 in the pair row below; L-12/C-8 here — MINOR ⟨KT⟩ — 3 of 10 exposed members dead (`canUndo`/`canRedo`/`selectedKeyframeId` — repo grep: 0 consumers); the 7-verb command contract crosses three seams as `any` (RibbonBar:140, ControlsPaneWrapper:196, useControlsKeyboardShortcuts:22), erased at hop ONE by the `*.vue` shim (env.d.ts:3-7). Cure: expose the verbs or delete them — the same decision kf-SequencePlayhead's transport packet books. Typing UNVERIFIABLE until OP-2 — cross-edge declared.
- **L-13 / L-14/C-16 (dead-surface pair)** MINOR ⟨KT⟩ — L-13: 22 returned members, 7 never taken; `loadPreset` dead (0 callers) AND sole consumer of all 33 lines of `flattenVars.ts` (deletion rides). L-14/C-16: `captureNonDefaultSnapshot` — the MORE careful strategy — unexported and uncalled (snapshotCapture:34), invisible to `npm run check`. Adopt or delete, both. `noUnusedLocals` rides KF.W4/K3 — the gate that would have caught these is not this wave's.
- **L-7 (TimelineCaret) + C-10/C-11/D-m6/m-1/D-m5 (TimelineTrack, merged)** — the cluster contract-hygiene group, MINOR ⟨caret + TT⟩ — dead prop `keyframeId` (sole occurrence its own declaration); redundant derived `position` beside `percent` (the zoom transform lives in the parent, neither provided nor passed); the public prop type drags `KeyframeSelector` from value.js for fields the subtree never reads; `const props` bound and never read in two of three cluster SFCs; hand-written `update:*` emits + call-signature overloads vs the demo's stated `defineModel`/named-tuple law (:129-134). Cure here; ENFORCEMENT at KF.W4 by fold (unenforceable by construction per the banked gate hole). **Collision with C-10 (TimelineCaret) ruled in P6: the `defineModel` law stands for this group; `update:percent` is the one ruled exception.**
- **C-17 / L-18-C-11 / L-20-L-21 / C-13 / D-25-D-26-D-28 / N-12 (KeyframeTimeline INFO group)** INFO ⟨KT; the D-25-D-26-D-28 hygiene cells sit with the coherence row above⟩ — C-17: `targets` plural, the preview reads `targets.value[0]` only, unlabeled as a sample. C-13: the **value.js D-23 blast-radius record** (`TimelineKeyframe.selector` typed by transitive value.js; `clamp` from `value.js/math` at five sites — legal; recorded, edge declared to the value.js lane, nothing cured). L-18/C-11: dual Tooltip specifiers — consistency clause only (K-10 killed the bundle-graph tail). L-20: no unmount guard on an in-flight capture (zero lifecycle hooks; `finally` scrubs a detached target, writes into a disposed reactive) — convicted as a regression against the in-tree standard (CSSCodeEditor's banked unmount-race discipline): adopt that discipline. L-21: the keyframe sort computed twice. **N-12 PROHIBITION verbatim: no future wave preserves `markRaw` as load-bearing while changing the ref kind** (corrects superlative S-7: the protection is real but SINGLE-mechanism).
- **L-16/L-17** MINOR ⟨KT⟩ — four copy-pasted Tooltip/Button blocks (~62 of 154 template lines) + two CSSPasteDialog mounts differing in four strings (:11-72, :135-152). **Cure-shape lock verbatim (`feedback_kiss_no_contrivance`): an in-file `v-for` over a descriptor, NO new component.** Sequenced AFTER R-7's fold settles (the mounts are its consumer end). In G15.
- **D-13 / D-16 / D-22 (KeyframeTimeline coherence group)** + D-25-D-26-D-28 hygiene cells, MINOR/INFO ⟨KT; RR-A/axis-D MAJORs on D-16/D-13 preserved with the co-visibility reopening⟩ — D-13: literal `Undo (Mod+Z)`/`Mod+Shift+Z` tooltip strings vs the modal rendering the SAME registry rows through glass `formatComboParts`, already imported at `shell/KeyboardShortcutsModal.vue:50` (path correction carried) — one-line cure. D-16: create-vs-destroy verb split survives on emphasis/shape/labelling alone (K-5 killed the height column: both rows floor at 36px); rung reopens if the ribbon detaches from the pane; settles in the D-10/RB-7 family. D-22: three verbs for one operation (Clear/Remove/Delete) + `Capturing...` with no live region (≡ banked D-22; THP D-13's live-region arm folds here; checks whether KF.W5 already dissolved `Capturing...` — KF-SKEL-16). Hygiene: broken wrapper indentation (:2-4/:131-153); identity arithmetic; `--caret-offset` unprefixed `:root` singleton with one consumer.
- **RR-B missed-5 + m-6 + m-3 + L-m-4 + L-m-5 + i-3 (TimelineTrack INFO group)** — the composable four land here; RR-B missed-5 (TimelineTrack) and m-6 cure in P4's geometry pass — INFO ⟨TT⟩ — two clamp idioms in one 110-line composable that imports `clamp` on line 3 (:19/:51 vs :86-89); `percentToPosition(tick)` ×3 per tick per render at pointermove rate; the affine remap hand-rolled one file from the module exporting it (`scale(pct, pan, pan+100/z, 0, 100) = (pct−pan)·z`, algebra identical) — **KISS cuts AGAINST the mechanical swap** ("the two-line hand-roll is arguably more legible than a five-argument `scale()` call"); indentation fingerprint at useZoomPan.ts:51. **K-6 kill lock**: L-m-5's ~102-tick magnitude is ~7× wrong (real set ≤ ~15 at any zoom ≥ 3; 5 at z=1); churn shape survives at INFO only. Under-consumption record; no formatter reaches the demo (banked family). Fleet-register note carried: RR-B's confirmation repeated the ~102 figure while citing the lines that refute it — a citation-inheritance event.

### P10 · Substrate (constraint on this and every KF wave)

- **MISS-α5 ≡ RR-β K-BASE** MAJOR (PROCESS) ⟨caret merged net-new pair · TT K-2 · THP DISSENT-4 · SequenceScrubber K-1⟩ — the tree every document calls "8281638c" is a HYBRID of two divergent lines: local master `8281638c` vs `origin/master` `81a56990`, merge-base `a59d3a22`, NEITHER an ancestor of the other. At HEAD: `keyframeSelector.ts` does not exist, `timelineTypes.ts` has no `selector` field, `moveKeyframe` has no `percentSelector` — C-1's chain, L-2's selector limb, D·M-4's selector cell UNEVALUABLE at the anchor all three axes declare. Manifests differ per ref: HEAD `optionalDependencies: glass-ui 6.0.0` (`npm ci` installs optionals ⇒ SILENT DOWNGRADE) · origin/master `devDependencies: 7.0.0` EXACT = installed · worktree declares NONE; value.js HEAD `^3.1.0` / origin+worktree `4.0.0` = installed. **Verbatim: any spec consuming this registry STATES WHICH REF IT CURES AGAINST** — done, header. Also CORRECTS the banked kf-KeyframeTimeline header's "working tree = the audited tree" equation (seven of that cluster's files are ` M` vs HEAD). Anchor-safety map: TimelineTrack.vue, useZoomPan.ts, TimelineCaret.vue, layout.css, design-idioms.css byte-identical at all three refs; the composable/engine tier is origin-side ONLY. **F-1 re-rule request carried**: census F-1 and every "phantom dependency" corroboration re-derives PER-BASELINE, never re-books — at origin/master manifest and disk AGREE; at HEAD the true residue is 6.0.0-declared vs 7.0.0-installed DRIFT; only the worktree is phantom; the banked kf-AnimationVisualizer cell "glass-ui in package.json/package-lock.json = 0/0 (F-1)" is corrected to worktree-scope. Sixth one-baseline-short event on the program's ledger. Locks **G12**.

---

## §Gates

All fifteen born-RED at authoring, each with its named live witness (L-19). Witnesses re-verify at wave-open (D-19).

**Runnability — ONE reading** (this repair replaces three incompatible statements of the same precondition; the preamble previously over-claimed "no gate is runnable" against the narrower and correct scopes at G11 and §Sequencing 2): **G12 pins the ref and is stamped first — it conditions every other gate's anchors, and G12 itself is discharged by the opening commit, not by a mount.** **G11's mounts are the runnability precondition of the ten behavioural gates — G2–G10 and G13** (each asserts runtime behaviour of a mounted surface). **G1, G14 and G15 are documentary/ruling gates: they need no mount and are runnable as soon as G12 is stamped** — G1 is the verdict table (its subject is the installed export surface, read with `ls`/`wc`), G14 is the one-posture ruling, G15 is the fold-or-decline ruling. §Sequencing 2's scope is COMMITS — no *cure* commit precedes G12 + G11 — which is not the same claim and no longer reads as one.

**G1 — The evaluate verdict exists, per-surface, against the real export surface.** RED.
Witness: `wc -l node_modules/@mkbabb/glass-ui/dist/components/timeline/index.d.ts` → 2 lines (`GlassTimeline` + 3 types: `TimelineSegment` / `TimelineSegmentGradient` / `TimelineSegmentState`); `ls …/dist/components/timeline/` → **nine files, enumeration completed by this repair** (the earlier five-item list was not exhaustive): `GlassTimeline.vue.d.ts` (the ONE re-exported component) + `index.d.ts`, against **seven shipped-but-NOT-re-exported**: `ContinuousRail.vue.d.ts` · `ContinuousMarkers.vue.d.ts` · **`ContinuousTimeline.vue.d.ts`** · `ScrubberTimeline.vue.d.ts` · `SegmentedTimeline.vue.d.ts` · `geometry.d.ts` · **`types.d.ts`** (which supplies the three re-exported type names — the type surface is reachable, the component surface is not); `grep -n "SliderVariant" …/dist/components/slider/types.d.ts` → `"standard" | "spectrum"`, no "timeline"; demo imports of `/timeline` = 0 (census §3.1).
Assertion: the §Verdict table exists — KeyframeTimeline (312 L) / TimelineTrack (246 L) / TimelineCaret (70 L) / TimelineHoverPreview (38 L) / SequenceScrubber (162 L) / AnimationVisualizer (256 L) — each ruled ADOPT · PARTIAL · KEEP-BESPOKE with named reason against the IMPORTABLE surface only; every SWAP-discharged row named. RED today: no table exists and 5 of 6 counterparts are not importable. Carries: C-15 (input, never re-derived), KF-AV-28, KF-AV-10's routing, C-4 (SpringTarget), C·C-4 (SequenceScrubber), KF-AV-13's decision, the PlaybackRibbon counter-evidence lock (PR-CAUTION); export gaps → BH relay.

**G2 — Scrub seam wired, guarded, single-engine — one commit.** RED.
Witness: `git show origin/master:…/KeyframeTimeline.vue | grep -n scrub` → hits 78/83/197/202/224, the bare `scrub` symbol among NONE of them (`:197 scrubT`, `:202 scrubAndCapture`; `scrub` absent from the :194-210 destructure); `:83` bare ref write; `grep -n "isPrimary\|pointerType" …/components/TimelineTrack.vue` → 0 in :168-186; `timelineEngine.ts:51` spreads scene `HTMLElement`s into a second engine.
Assertion: non-primary pointerdown ⇒ NO scrub; pinch ⇒ NO scrub; the driven engine is provably NOT the scene's instance (or the scene's single engine via AnimationControlsGroup). Carries: N-10, C-1, C-6, L-3, L-5, M-3/C-4, RR-B missed-3 (TimelineTrack), m-9/m-11/m-12 (TimelineTrack, merged), M2 ≡ RR-B-7, D-1 (TimelineTrack), KF-CE-2 (parent half)'s ordering locks.

**G3 — A grab is not a teleport.** RED.
Witness: `TimelineTrack.vue:192-196` records no offset; `:174-178` emits the absolute pointer percent; grabbable region = the 24px pad ⇒ ~12px ≈ 3% error at z=1 on the 400-512px rail.
Assertion: grab at +11px off-centre, z=1, 400px rail: first-pointermove emitted percent == pre-grab percent; `useRefHistory` banks ZERO entries for the grab alone. Carries: RR-B missed-1 (TimelineTrack) (`grabDx` per GradientStopEditor C11/G5), L-m-14, L-8/C-9 (KeyframeTimeline)'s shared identity policy.

**G4 — Rebuild rate bounded and dirty-checked.** RED.
Witness: `useTimelineOps.ts:56-62` `rebuild()` unconditional, no early-out even on zero clamped delta; `buildAnimationFromTimeline` docblock: "ASYNC because the engine constructor is HEAVY."
Assertion: a synthesized 60-move drag ⇒ ≤1 `buildAnimationFromTimeline` per animation frame; a rail-end hold (zero clamped delta) ⇒ ZERO. Carries: RR-B missed-2 (TimelineTrack); frame-cost profile → SS-13.

**G5 — UI and artifact cannot disagree about keyframe count.** RED.
Witness: `timelineEngine.ts:40-48` keys on `selectorText(kf.selector)`, merge admitted in the :42 comment, feeding build :51-53 + export :62-72, while both render loops (:62-94, :97-106) still paint two; the caret quantizes every commit to 101 slots via `@blur` alone.
Assertion: keyframes that merge in the built animation and exported CSS CANNOT render as two markers — minimum separation OR coalesced-and-said-so. Carries: N-6, MISS-β1, L-2/C-3/D·M-4's rounding. Serial-only (shared-file rule, §Bounds).

**G6 — Import → export round-trip preserves what the user authored.** RED. Ref: origin/master (C-5 (TimelineHoverPreview) is vacuous at stale HEAD).
Witness: `importCSSToTimeline` drops per-stop `animation-timing-function`; `TimelineKeyframe.easing?` never written/read; named scroll phases caption fabricated percents (`entry 100%` / `cover 0%` both "25%"); `state.value.keyframes = imported` replaces against copy that says "merge"; `captureSnapshot` :16-21 drops `none`/`auto`.
Assertion: a fixture corpus round-trips — per-stop easing survives, named phases render as named, "Add" does what the copy says, "animate to none" is authorable or its exclusion recorded. Carries: N-9, N-8, C-5 (TimelineHoverPreview), R-3, L-6/C-4's delegation.

**G7 — A read gesture writes nothing.** RED.
Witness: `TimelineCaret.vue:22` `@blur`-alone commit; `:58-65` no compare; `useTimelineOps.ts:60` fresh selector object per call; deep-keyed `useRefHistory`.
Assertion: open the caret editor, blur without typing: ZERO undo entries, ZERO export-byte change, display returns at MODEL precision. Carries: L-2/C-3/D·M-4 compare-before-commit, MISS-α4's comment rider, L-9/m-8 (TimelineCaret)'s clamp protection. Kill lock: C-1's float story never re-derived (locus = MISS-β2, KF.W8).

**G8 — Keyboard parity, non-destructive.** RED.
Witness: `TimelineTrack.vue:203-214` (`select` only paired with `moveKeyframe`; Enter/Space discarded); the bare-div track; no keyboard zoom/pan route; `useTimelineOps.ts:28` + argless `snapshot()` in RibbonBar ⇒ keyboard snapshots at 0%.
Assertion: every pointer verb (scrub, zoom, pan, select, retime) has a keyboard route; NO inspection keystroke mutates the document; keyboard `snapshot()` captures at the user's position; Enter/Escape exit the caret editor without stranding focus on `<body>`. Carries: M3, D-3 (percent readout a11y)'s caret-redesign a11y arm, D-1 (TimelineTrack)'s adopted harm, D-8 (state contract, TimelineTrack), D-11's keyboard arm, M-7 + RR-B missed-4 (joint)'s route, MISS-α2, m-1/L-10, m-4 + MISS-α1 (TimelineCaret), m-5/L-6/C-5 (TimelineCaret)'s one-shape decision. Exports the THP D-3 reopening.

**G9 — The tooltip announces what it shows.** RED.
Witness: reka `TooltipContentImpl.js:87` (`props.ariaLabel || currentElement.value?.textContent`, untracked, captured once); `ariaLabel` unpassed at `TimelineTrack.vue:86`; `textContent` excludes `<img alt>`; `text-admin-label` uppercases the payload; `grep -rn "data-register"` across demo styles + glass dist CSS → zero targeting rules.
Assertion: the accessible description carries the derived alt, punctuated at row boundaries, re-derives on the ghost→image swap, is not force-uppercased; the rail has an accessible container and AT-hidden graduations. Carries: MISSED-1 + D-10 (KeyframeTimeline) (one seam), M7 with its MM-29/MISSED-3 cure guards and DISSENT-4 verbatim-element law, RR-A missed-1 (TimelineTrack).

**G10 — No thumbnail outlives its keyframe's edit; the ghost tells the truth.** RED.
Witness: `previewCache` keyed on mutation-stable ids; `grep -n "delete" <cache module>` → none; failed captures retry forever; the sibling history bounded at `capacity: 50`; `opacity: "0"` survives the capture filter; `scale(0.3) ${matrix}` on the bordered element itself.
Assertion: post-edit, the cached thumbnail is gone or regenerated; no orphan base64 PNGs after remove/clear/import-over; a repeatedly-failing capture stops and says so; the ghost renders decomposed (scale on a wrapper), has a terminal `v-else`, and reserves its box. Carries: D-4/L-4/C-5 (comment rides the cure, KF-CE-41), D-7 (TimelineHoverPreview), MISSED-4, GHOST-PLATE, L-D8/C-4(a), L-D4/C-4(b), m-7/m-8 (TimelineTrack, merged), M9's rung lock, D-2/L-D9, D-3/L-D6/C-6 under MISSED-3's guard, D-12 (THP) + D-15 (THP) (TimelineHoverPreview layout pair), L-D15 / MISSED-6 / L-D14 / P2-DEAD (THP hygiene group). WebGL blank-resolve → SS-13 residue #1.

**G11 — The cluster is mounted by a test at all.** RED — and the runnability precondition of the ten behavioural gates **G2–G10 and G13** (§Gates preamble's one reading; the documentary/ruling gates G1, G14, G15 do not wait on it — they wait only on G12's ref pin).
Witness: `grep -rn "TimelineHoverPreview\|previewCache\|SequenceScrubber" test/ demo/` → no mount; `timeline-undo.test.ts` exercises the same state without the surface; ⟨OP-4 measured⟩ `@vue/test-utils` NOT installed, not in origin/master devDependencies (`jsdom ^29.1.1` present) — the gate costs a dependency, bounded in §Bounds.
Assertion: mount tests exist for the timeline cluster AND SequenceScrubber asserting keyboard parity, direction latch, and projection. Carries: C-9 (THP) — coverage, kf-SequenceScrubber L·D-12 (banked N-10 class, second instance) — one coverage decision.

**G12 — The spec names its ref; every anchor re-resolves against it.** RED until stamped at open.
Witness: `git -C /Users/mkbabb/Programming/keyframes.js rev-parse master origin/master` → `8281638c` / `81a56990`; `git merge-base master origin/master` → `a59d3a22`; `git cat-file -e 8281638c:demo/utils/keyframeSelector.ts` → fails; `git grep -n "selector" 8281638c -- demo/components/instrument/timeline/timelineTypes.ts` → absent; manifest triple-state per MISS-α5 ≡ RR-β K-BASE.
Assertion: the opening commit states origin/master `81a56990` (or later), marks composable/engine anchors origin-side, re-resolves every line anchor before cure (D-19). Carries: MISS-α5 ≡ RR-β K-BASE, the witness law (kf-App ruling 1), KF.W0 §B-12, the F-1 per-baseline re-rule.

**G13 — The wheel does not eat the page.** RED.
Witness: `@wheel.prevent` at `TimelineTrack.vue:31`; neither branch matches a plain wheel; `touch-none` :24; `useZoomPan.ts:59` reads `deltaY` only (`:50` the sole other delta read; `deltaX` nowhere); the pan readout carries no handler/role/tabindex (:4-18).
Assertion: plain wheel over the track scrolls the ancestor; shift-wheel pans on whichever axis the engine delivers; the pan readout is operable; the open caret editor is wheel-shielded by an explicit is-editing guard. Carries: M5 ≡ RR-B-3, M-7 + RR-B missed-4 (joint) (escalation trigger armed: `deltaY: 0` ⇒ jointly MAJOR), L-11 (TimelineCaret), ARB-1's pan-writer dependency.

**G14 — The cluster's failure posture is ONE posture.** RED (six divergent postures counted).
Witness: `useTimelineBuild.ts:47-50` (the sole non-toasting failure — the one reachable by typing) vs toasts at :121/:133/:137/:148/:155/:157; caret L-15 silent swallow; `snapshot` success-toast-before-outcome (L-11 (KeyframeTimeline)); KAD-9 failure-only completion signal; R-4 close-regardless-of-parse; D-15 (KeyframeTimeline)'s unexpressed empty/failure states.
Assertion: one declared failure posture across cluster + dialogs, registered in KF.W2's posture registry (cross-referenced both ends). Carries: C-7 (posture arm; crash identity = megatranche R1, never re-booked; §F-2 scope correction rides out), L-15 (TimelineCaret), L-11 (KeyframeTimeline), D-15 (KeyframeTimeline), R-4 (+ KAD-9, KAD-10).

**G15 — One dialog shell, or the divergence ruled in writing.** RED.
Witness: `wc -l` → CSSPasteDialog.vue 80 / KeyframesAddDialog.vue 161; byte-identical `@interact-outside` guard and `onInput` body in both (grep); KAD nests `DialogDescription` inside `DialogTitle` (grep -A2 DialogTitle) while `DialogHeader` sits unused in the same barrel; **`initialText` is dead AT THE MOUNT, not in the file** ⟨witness re-founded by this repair seat — the earlier wording ("declared, never bound") was refuted by its own command: `git show origin/master:…/CSSPasteDialog.vue | grep -n initialText` → **four** hits, `:49` declared · `:53` defaulted · `:59` `ref(props.initialText)` · `:67` re-read on open. The bank's actual claim (R-19) is a MOUNT claim, and it holds: `git show origin/master:…/KeyframeTimeline.vue | sed -n '135,152p'` → both `<CSSPasteDialog>` mounts pass only `v-model:open` / `title` / `description` / `button-label` / `:button-icon` / `@submit` — **`:initial-text` appears at neither**, so the in-file channel is fed by nothing and R-4's needed acknowledgement channel is minted-but-unwired⟩; **N-2's two occurrences take two commands** ⟨also re-founded: `grep -rn "\.label" …/timeline/` returns **one** hit (`KeyframeTimeline.vue:106 v-model="selectedKeyframe.label"`) — the pattern `\.label` cannot match a type declaration; the second occurrence is `grep -n "label" …/timelineTypes.ts` → `:11 label?: string`. Two occurrences, one per command; an execution seat running a single `\.label` sweep reads 1-of-2 and would grade N-2 half-cured⟩.
Assertion: the twin folds onto the 80-line shell under the pinned constraints (CSSPasteDialog's a11y composition + the twin's text-hoisting contract; four genuine deltas preserved; L-4's falsifier answered) OR the fold is declined with the falsifier's answer in writing; every dialog-residue row's disposition (discharged-by-fold / discharged-by-KF.W6-swap / cured) is STATED before any cure is spent — **and that statement enumerates the three W6-owned dual-homed ids by id (KAD-15 · KAD-16 · KAD-20, §Excluded), because KAD-15's footer-placement and KAD-16's `grid`-over-flex cells are the structural root of the fold direction R-7 pins: the seat cannot declare the footer bare without saying what happened to them**; N-2 is ruled wire-or-delete. Carries: R-7 ≡ KAD-F3, KAD-6, KAD-13, R-17 / R-19 / R-21 / R-22 / R-23 / KAD-21 (dialog MINOR group), N-2, L-16/L-17's sequencing.

---

## §Sequencing

**Hard ordering (blocking, in order):**
1. **kf-ChannelControls L-2/C-2 (`:key` remount BLOCKER) lands BEFORE this wave** (OP-1) — the bank's own words: *"the `:key` data-loss row MUST be settled BEFORE KF.W7"*; any timeline-cluster swap that inherits the `:key` inherits the data loss. Second-order collision RESOLVED by this spec: KF-CE-2 (parent half)'s "keyed remount" option is REJECTED; **arm-time id capture is the ruled cure** (P8).
2. **G12 ref-pin + G11 mounts (with OP-4's dependency)** open the wave; **no CURE COMMIT precedes them** — the scope is commits, not readability: G1's verdict table, G14's posture ruling and G15's fold ruling are documentary acts that run on the pinned ref alone (§Gates preamble's one reading), and G1's verdict is itself item 3 below.
3. **G1's verdict table precedes every verdict-gated row** (KF-AV-28 mechanics, §Verdict) — and precedes KF.W11 arm (d)'s shape rows and KF.W13's two packets ("resolve KF.W7 first").
4. **The pointer-guard commit (M-3/C-4 + RR-B missed-3 (TimelineTrack) + m-9/m-11/m-12 (TimelineTrack, merged)) precedes or ships with the seam commit (G2 family)** — precondition, not follow-up.
5. **D-19 geometry re-derivation precedes every P4 cure** (OP-3; both stale tables: D-17's and K-13's corrections; one padding-box correction family with RR-B missed-5 (TimelineTrack) and C-9 (TimelineCaret)).
6. **N-2 wire-or-delete is ruled here BEFORE KF.W6 opens D-9**; the R-7 fold settles before L-16/L-17's descriptor.

**Commit families (rows that must not split):** the scrub seam (G2's family); grabDx + L-m-14; the wheel policy (G13's four); the collision decision (G5's three, serial on the shared files); the ghost redesign (D-7 + MISSED-4 + GHOST-PLATE + L-D8/C-4(a) + L-D4/C-4(b), with m-7/m-8's validation half sequenced after L-6/C-4's delegation); the a11y commit (MISSED-1 + D-10 (KeyframeTimeline) + M7 + RR-A missed-1 (TimelineTrack)); D-12 (KeyframeTimeline) + RR-A missed-2 (TimelineTrack); the expanded-timeline settlement (D-10 (TimelineTrack, expanded mode) + RB-7 + D-16 + kf-ChannelControls C-8/D-6 + KT D-7's plate strip — one settlement); the round-trip commit (N-8 + N-9 + C-5 (TimelineHoverPreview) + R-3 under G6); the posture ruling (G14's consumers, one ruling); the undo-authority ruling (KF-KC-11 (undo-trail arm) + KF-KC-52 + L-2/C-3/D·M-4 + RR-B missed-1 + L-2/C-2 — one authority, four consumers, one ruling); MISSED-3's same-commit box law wherever a type rung moves.

**Packet homing (the NO-WAVE-OWNER scene-repair packets, homed as authoring requires):** the **sequence** packet's contract question (C·C-4 (SequenceScrubber)) and the **spring** packet's idiom/prose/export arms (KF-AV-10, C-4 (SpringTarget), C-3 (SpringTrace)) are HOMED HERE — decided in G1's table, cures verdict-gated; **CARD-UNIT**'s undo question is HOMED HERE via the undo-authority ruling (cure site declared to the CARD-UNIT owner); KF-AV-13's press-to-seek DECISION is homed here, its cure lands NO-WAVE-OWNER (SS-1/SS-2); **cube** and **square** packets are KF.W11's (SquareScene MISS-1 shares grabDx's class — arm c), **dock-menu** is KF.W13's — homed by their owning specs, edges declared from this end. **kf-SquareScene law carried: comment-stated invariants are test obligations** (MISS-α4 / KF-CE-41 are this spec's instances).

**Cross-edges (declared from this end):**
- **KF.W0 §B-12** (depends-on): origin/master pins glass-ui 7.0.0 exact devDep = installed; every wave re-baselines against origin/master. F-1 re-rule request carried (per-baseline re-derivation; the AnimationVisualizer census cell corrected to worktree-scope).
- **KF.W4** (depends-on, for VERIFICATION not authoring): KF-CE-16 vue-tsc gate + `noUnusedLocals` (K3) precede verification of every typed cure here (L-12/C-8, L-7 (TimelineCaret), L-m-10, the TimelineTrack contract-hygiene group, L-13 / L-14/C-16 (dead-surface pair)). Owed to KF.W4: the census-gate design brief (C-S+1 + REGISTER-LAUNDER).
- **KF.W6** (feeds and is bound by): (1) MISSED-3's collision guard binds BOTH waves — no `text-mono-small` swap without the box resize in the SAME commit; no `normal-case` without `tracking-normal` (MM-29); (2) N-2 ruled here first; (3) caret C-7's `.focus-ring` one-class cure inverts the primitive-swap order — D·M-5 discharges without waiting on the NumberField evaluation; (4) L-9/m-8 (TimelineCaret) discharges with KF.W6's NumberField swap if it lands there (declared both ends); (5) KAD-6 stands alone at KF.W6 if the fold is declined.
- **KF.W2/W3** (bidirectional): OUT — C-7's §F-2 scope correction + fuzz-entry note + the posture-registry rows (G14); IN — L-6/C-4's delegation target (`@mkbabb/value.js/css` collectors / the parse façade, OP-5) must exist or be named.
- **KF.W5** (feeds/defers): KAD `innerHTML` BLOCKER front-loaded there, never gated here; C-3 (SpringTrace)'s library export (`resolveLinearStops`/`sampleNormalizedSpring`, C-15 precedent) — implementation edge declared to KF.W5/KF.W8; THP C-8's Skeleton half ≡ KF-SKEL-16 — the status-line cure checks whether KF.W5 already dissolved `Capturing...`.
- **KF.W8** (feeds): L-19/C-14 dead barrel, C-12 private-`src/` reach, N-11 colocation, caret m-7 rename, MISS-β2 (the serializer triple-copy; the surviving locus of C-1's killed float story), KF-AV-24 seam consolidation.
- **KF.W11 arm (d)** (this wave GATES): SequenceScrubber shape rows (C·C-4 (SequenceScrubber)'s contract question; D-5/D-6/D-11's "the primitive already solves it") discharge or persist with G1's verdict; the arm's spine and banked packets proceed independently — declared so KF.W11 authors without blocking. SquareScene MISS-1 (arm c) shares grabDx's class.
- **KF.W13** (this wave GATES): the drag-seam packet (KF-SCR-1 + L·D-1 + KF-AV-15; shares the grab-offset arm and the GradientStopEditor precedent) and the transport/ribbon packet (which carries this wave's rider AND supplies its counter-evidence). Locks re-affirmed from this end: **TD-1/TD-2's cures BUNDLE**, and the TD-2+TD-38+TD-40 and TD-21+TD-41 commits MUST NOT split; **kf-ChromeDock M-4's worded cure ships an unopenable menu without the MbabbMenu MUST-CARRY rider** — the rider rides wherever M-4's cure lands.
- **EDITOR-UNIT** (must land with this wave's half): KF-CE-2's child half (cancel-on-external-write + cancel-on-unmount, one `replaceContent()` seam); neither half alone closes the data loss; EDITOR-UNIT itself sequenced after KF.W4's gate.
- **glass-ui BH relay (SS-6)** (feeds, NEVER patches locally — owner edict: every producer-level change relays to the active BH inbox): C-15's `/timeline` export gaps; TooltipContent's non-consumption of `--reka-tooltip-content-available-height` (the D-15 half of the THP layout pair); KF-SCR-2 (the void italic register); KF-AV-32 (mutually-disarming `useTouchGate` pair); the `cn` padding-group seam is ALREADY relayed (TEP-12/MCP-31 family) — not re-filed.
- **KF.W9 / SS-13** (feeds): the residue lists (KT 13 rows, TT 12, caret 12, THP 11, SequenceScrubber 12), forced-colors D-21 and RTL D-24 whole. **Armed triggers this wave honours, never pre-decides**: (a) shift-wheel `deltaY: 0` ⇒ M-7 + RR-B missed-4 (joint) jointly MAJOR; (b) `document.activeElement` ≠ input after caret click ⇒ m-1/L-10 MAJOR + MISS-α2 reachability. Witness-refresh obligation: re-run `build:gh-pages` before trusting any byte-offset receipt (the Jul-16 CSS witness may predate glass-7); installed-dist token/cascade cells exempt.
- **value.js lane (D-23)**: C-13 recorded as blast-radius input inside the record group (nothing cured); TT's negative result recorded so parser waves SKIP the file (the sole emit→parser path is awaited inside the try, useTimelineBuild.ts:40-50 — also why RR-B missed-2 is churn, not crash).
- **Taxonomy reconciliation** (a naming schism resolved, not an edge): three pre-census records use lane-frontend §10 naming where "KF.W7" = the TOKEN-NAMESPACE audit — their grep hits are NOT this wave's (§Excluded). Conversely, "KF.W6-TIMELINE" references (kf-AnimationControlsGroup S-3/S-4 "wants its own spec"; kf-ChannelControls "settled before KF.W6-TIMELINE") ARE this wave and are carried above as hard-ordering locks.

---

## §Excluded

**No registry-routed row is unaccounted.** (RULINGS R-12: the former opener — *"No CARRY row is dropped"* — was keyed to a ledger that does not exist, and it is exactly the assertion that hid the three dual-homed KAD rows below. The true denominator is the registry's: 148 routed · 145 carried · 3 excluded-with-named-owner.) The following are the explicit non-carries, each with its reason:

- **KAD-15 · KAD-16 · KAD-20 (kf-KeyframesAddDialog)** — **exclusions-with-named-owner: KF.W6** (RULINGS R-12). All three terminate `**→ KF.W6/W7**` at `kf-KeyframesAddDialog.md:68/:69/:73` and all three are ALREADY BOOKED at the W6 end, re-verified at the bytes: KAD-15 at `KF-W6.md:250` (S-10 · KF-KE-21 +KAD-15) and `KF-W6-CARRY.md:178`; KAD-16 and KAD-20 at `KF-W6.md:320` and `KF-W6-CARRY.md:248` (the KAD-3+KAD-7+KAD-19+KAD-20+KAD-25+KAD-16+F1/F4/F5 group). Reason each is W6's and not this wave's, by mechanism: **KAD-15** is the progress-bar animator fold whose cure "rides `design-idioms.css`'s owner" (the record's own words) — a token/idiom-owner act, and `design-idioms.css` is outside §Bounds; **KAD-16** is the dead-token cluster (literal `class`, inert `sticky bottom-0`, `type="submit"` with no `<form>`) which the record routes to "die in the fold/swap" — the S-9 Textarea/well swap is KF.W6's; **KAD-20** is the no-empty-state row, a member of that same swap-discharged group. **Binding declared to KF.W6 from this end**: KAD-16's `grid`-defeats-`DialogFooter`-flex cell and KAD-15's footer-placement cell are the *structural root* of the fold direction this spec pins at R-7 (adopt CSSPasteDialog's a11y composition + the twin's text-hoisting contract) — so if KF.W6's swap discharges them, R-7's fold inherits a bare footer; if the swap is declined, the two structural cells reach the fold un-cured and R-7's execution seat must say so. G15's discharge discipline names all three by id (below) so the statement is executable; **no cure for any of the three is spent by this wave.**
- **kf-DemoGlobalChrome M-4, kf-AnimationControlsGroup D-12, kf-ControlsPaneWrapper D-m3** — naming-schism grep hits; they belong to the census taxonomy's KF.W6 `--kf-*` token-namespace audit, not this wave (taxonomy reconciliation, §Sequencing).
- **The R1 crash identity** — FOLDS to the banked megatranche R1 row (KF-CE-12 precedent), never re-booked; only C-7's posture arm and the §F-2 scope correction book here.
- **KAD `innerHTML` DOM-XSS BLOCKER** — KF.W5's, front-loaded ("one line, `el.textContent = s`"); this wave does not gate on it and must not wait on the R-7 fold for it.
- **Typography/token cure family** (KT D-5/D-6/D-7/D-8/D-9/D-14/D-18/D-20/M8/M10; TT D-3/D-5/m-17/RR-A missed-5's style arm; caret C-7/D·M-3/D·M-5/D·M-7/C-4·D·M-9/MISS-α3/MISS-α6/m-2; THP D-4/L-D7/MISSED-2/D-5/D-11/D-17-type; KF-SCR-2's demo side) — KF.W6's, bound by the declared bindings (MISSED-3 chief among them).
- **Structure/colocation rows** (L-19/C-14, C-12, N-11, caret m-7, MISS-β2, KF-AV-24) — KF.W8's, edges declared.
- **Forced-colors coverage (D-21) and RTL (D-24)** — KF.W9 whole; only D-8 (TimelineTrack)'s AT-state rider books here.
- **C-3 (SpringTrace)'s implementation** — library-side; this wave takes the DECISION into G1's precedent frame and declares the implementation edge to KF.W5/KF.W8 (the row is carried; its code is not this wave's).
- **KF-AV-13's cure and the `canRemove`/confirm-or-undo cure (KF-KC-11)** — NO-WAVE-OWNER cures; the DECISIONS are taken here (G1 table; the undo-authority ruling), the code lands with the owning packets.
- **Killed claims, never to re-enter any spec** (carried as locks, not rows): C-1's float story (K-7); m-13's latch escalation (K-8); L-9's fallback-dead-code (K-7); L-D10's escape arm (K-3); the `{}` frequency claim (K-4); L-m-5's ~102-tick magnitude (K-6); D-m1's transform-eases consequence (K-5); K-1's two manufactured Delete harms; the "800px/~600px rail" fictions; the caret Blink-no-op assertion (#11); the caret Card-clip truncation (#13); ARB-1's earlier "drag is clamped" claim; killed-claim #15's re-derivation of C-15; C·C-4's `{progress}`-prop-plus-emits cure (K-8); the `aria-selected`-on-a-div harvest (killed-claim #6); L-D8's slot-laziness perf arm (withdrawn, stays withdrawn).
- **KF-CO-1/KF-CO-8** — no KF-CO row is routed to this wave by the registry; their cures are SEQUENCED by LP-1 (no write→render edge) in the owning wave's spec; noted here so the grep trail shows the lock considered, not dropped.
- **SS-13 residue probes themselves** — routed, not executed here (probe parsimony edict); this wave consumes their verdicts via the two armed triggers only.

---

*Fold-repair pass (same seat class, 2026-08-28): exact **registry** ids restored to every row header (grep-verifiable against `docs/tranches/V/megatranche/registry/adjudicated/`, the only source this spec is provenanced on); PR-CAUTION and L-15-PROTECTED named — **and, at repair round 1, re-declared as SPEC-LOCAL LABELS bound to their banked coordinates (§Carry preamble)**; D-3 (percent readout a11y), D-14/i-1, and the KF-AV-14/KF-AV-17/KF-AV-24 rider substance landed whole (incl. the translateX(calc(var(--p) * (100cqw − 100%))) cure-shape lock); OP-2's `check`-script citation corrected against origin/master; OP-4 widened to the missing vitest Vue-SFC plugin with its bounds row; timelineEngine count corrected 104→102; the useZoomPan `:59` anchor corrected; route dissents recorded on SpringTrace C-3 / SpringTarget C-4 (M-25 route-by-mechanism); the LP-1 no-write→render-edge rider attached to N-10.*

*Authored as KF.W7's agglomerated spec by the fresh-Fable fold seat **from the 58-record registry directly** (two drafts consumed choosily: A's structure and carry program; B's preconditions, measurements, intake rows, disjointness, and fold gate). Status: planned. Every gate is born-RED with a named witness; no gate has run; no product source was opened. Execution awaits the owner's begin-word.*

*Repair round 1 (X·KF L-20, 2026-08-28) — sole write = this file; status stays `planned`, no gate stamped, no product source opened, every carried row keeps its banked id. Applied: **RULINGS R-12** (all five phantom "KF.W7 CARRY ledger" bindings struck at the verb table, §Bounds, §Excluded's opener and both footers — provenance restated over the registry; **KAD-15 · KAD-16 · KAD-20 booked in §Excluded as exclusions-with-named-owner KF.W6**, both W6 ends re-verified at `KF-W6.md:250/:320` and `KF-W6-CARRY.md:178/:248`, with the R-7 fold-direction binding declared and G15's discharge discipline naming all three by id) · **CHECK D1** (cured by R-12, the ruling's shape) · **D2** (cured by R-12) · **D3 + D4** (G15's third and fourth witnesses RE-FOUNDED on commands that return what they claim: `initialText` is dead at the MOUNT, four in-file hits notwithstanding; N-2's two occurrences need two commands — L-19 restored, the gate still born-RED for its true reason) · **D5** (one reading of the runnability precondition: G12 pins for all · G11 gates the ten behavioural gates G2–G10 + G13 · G1/G14/G15 are documentary and wait only on G12; §Sequencing 2's scope re-stated as COMMITS) · **D6** (the two coinages disclosed as spec-local labels bound to `kf-PlaybackRibbon.md:36` and `kf-KeyframeTimeline.md:125`) · **D7** (G1's shipped-but-un-re-exported enumeration completed to seven at the bytes; C-15's split verdict carried unchanged, killed-claim #15 lock intact). Census after this repair: **148 routed · 148 accounted (145 carried + 3 excluded-with-named-owner) · 0 escaped.** No ruling addressed to this wave was declined; R-3 and R-10 both direct "KF-W7: no change" and none was made.*
