SERVED MODEL: claude-fable-5-1

# KF.W13 — Chrome, Dock & Transport Repair

**Wave**: KF.W13 · Chrome, Dock & Transport Repair — **the wave MINTED EXPLICITLY at `KF-W4 §Sequencing`** (*"KF.W13 · Chrome, Dock & Transport Repair (2) — MINTED here, declared (a wave beyond the census sketch is minted explicitly, per KF-W1 cross-edge 9's cohesion flag)"*; RULINGS-4 R4-8 arm (a): the mint STANDS — *"deliberate, load-bearing, and flagged-and-answered"*; the COHESION FLAG KF-W1 edge 9 raised is recorded by that edge as DISCHARGED, not deleted), carrying the TWO NO-WAVE-OWNER chrome packets of RULINGS R-15's canonical seventeen (`KF-W10 §6.D · SUCCESSOR-FORMATION REGISTER` row 3): **dock-menu · transport/ribbon**. Repair, not evaluate: the cure shapes are ruled at the home records; this file HOMES them with their two hardest travelling locks — **kf-ChromeDock M-4's MUST-CARRY MbabbMenu rider** and **the TD-1/TD-2 bundle WHOLE** — re-derives nothing, mints nothing, re-books nothing. **And it is not a place where a lock may be honoured in spirit**: three of its locks are *must-not-split* commit-family locks, and a split is a defect whatever the bytes look like afterwards.
**Half**: frontend (keyframes.js demo — `demo/app/dock/**` + `demo/app/App.vue` + `demo/components/instrument/transport/TransportDock*` + `…/AnimationControlsGroup/useControlsKeyboardShortcuts.ts` + `demo/components/playback/PlaybackRibbon.vue` + `demo/styles/playback-idiom.css` + the `design-idioms.css` counterpart of the two-deletion act + one `ChannelOptions.vue` carve). **Sub-tranche**: X·KF. **Track**: B.
**Authority**: `docs/tranches/X/COHESION.md` §0d; `EXECUTION-RUNBOOK.md §4.5`; ratified under `docs/tranches/V/megatranche/SCOPE.md` M-25. **Minting authority**: `KF-W4 §Sequencing`, the R-15 homing block, the KF.W13 bullet. **Terminal disposition of record for the X·KF close**: `KF-W10 §6.D` row 3, cited by STABLE ANCHOR ALONE (LAW C(3)) — the two ends name each other and neither carries the other's line numbers.
**Alias law (R-B idiom)**: `KF.W13` denotes `X.KF.W13`; a search alias, never a rename.
**Status**: planned. No product byte is written by this file; authoring it opened no product source for writing; execution awaits the sequencing head (§State).
**Ref of record**: keyframes.js `origin/master` **`69095552`** — ⟨cmd⟩ `git -C /Users/mkbabb/Programming/keyframes.js rev-parse --short origin/master` → `69095552` (2026-09-18); local `master` = the same; `git status --porcelain` → two untracked coordination letters, zero modified product paths; ⟨cmd⟩ `git rev-list --count 81a56990..origin/master` → **203**. **The drift here is the most consequential of the three successors**: §6.D row 3's inbound blast-radius receipt is a **pair of censuses** — the `btn-playback` file list and the `.focus-ring` consumer list — **and both have moved at the frontier**, and the G-KFW9-9 witness pair the two-deletion act targets has moved with KF.W6's rename. §B.1 states each drift with its command **beside** RULINGS-4's fixed figure; **RULINGS-4 is not amended** (E-3). Every line anchor is re-resolved again at open (KF.W0 §B-12; D-19).
**Standing supersession rider (KF-AV-28), on the transport/ribbon surfaces (register row 3, lock 3)** — the transport/ribbon packet *"carries this wave's rider AND supplies its counter-evidence"* (KF-W7 §Sequencing → KF.W13): `PR-CAUTION`, quoted verbatim at KF-W7's header — *"this ribbon is the NON-bespoke case (it consumes the real `Slider`) and carries BLOCKERs anyway, so 'swap onto the primitive' is never sufficient as a cure."* **The verdicts are IN — six KEEP-BESPOKE, ZERO SWAP, discharge set EMPTY** (`execution/B/KF-W7.md` `.a` G1, value.js **`4c03ceda`**, 2026-09-18 — ⟨cmd⟩ `git -C /Users/mkbabb/Programming/value.js cat-file -t 4c03ceda` → `commit`; the `.f` edge: *"KF.W13 — both packets are un-gated by a swap that did not happen … the transport/ribbon packet keeps the rider it supplies the counter-evidence for. The locks this end re-affirms are unchanged: TD-1/TD-2's cures BUNDLE, TD-2+TD-38+TD-40 and TD-21+TD-41 must not split, and kf-ChromeDock M-4's cure ships an unopenable menu without the MbabbMenu MUST-CARRY rider."*). **Both packets are therefore UN-GATED**; the rider stays as the record of why they sequence after W7; PR-CAUTION binds the cure shape (the ribbon's rows are cured ON the primitive it already consumes, never by swapping); **this wave inherits work, not a deletion, and no seat may write `DISCHARGED by KF.W7 SWAP verdict <surface>, <date>` on any row** — *"the inverse error (a downstream wave assuming a swap happened) costs exactly as much as a silent discharge."*
**Provenance**: **AGGLOMERATED** by the fresh fold seat of the SS-1/SS-2 authoring block (M-23 §3) from the two BLIND drafts at `waves/drafts/fable/KF-W13.md` and `waves/drafts/opus/KF-W13.md`, the register of record `KF-W10 §6.D` row 3, the minting citations, the home records in `docs/tranches/V/megatranche/registry/adjudicated/` — **kf-ChromeDock · kf-MbabbMenu · kf-TransportDock · kf-PlaybackRibbon** (the four homes: `kf-TransportDock.md:131` *"NO-WAVE-OWNER — the transport repair packet"*; `kf-PlaybackRibbon.md:36` *"HOMED for the first time … under the KF.W7 rider"*; `kf-MbabbMenu.md:169` *"the dock-menu actuation/typography/brand family"*; `kf-ChromeDock.md:45` M-4) **plus kf-App (the KF-APP-1/-17 motion the MM-1/MM-6 cure law consumes; ARB-1)**, consumed **by mechanism, deduped by identity, never transcribed** (M-25); `execution/B/KF-W7.md` (the verdict table; *"What the two gated successors read here"*); `KF-W9 §Bounds` and its G-KFW9-9 row (the witness pair, READ-ONLY there); and the live keyframes tree, READ-ONLY. Every disagreement between the arms was resolved at the register's bytes and the live tree — §Provenance at the foot. Validated against `docs/precepts/instructions/tranche/WAVE_SPEC.md` (L-20). **No gate exists here without a named live witness (L-19).**

| verb | state | basis |
|---|---|---|
| AUDITED | YES | the four home records + kf-App; the two packet terminal lines and M-4 quoted by anchor at §Carry — ⟨cmd⟩ `grep -c 'NO-WAVE-OWNER' registry/adjudicated/kf-{ChromeDock,MbabbMenu,PlaybackRibbon,TransportDock}.md` → **35 · 32 · 14 · 32** |
| SPECIFIED | YES — this file, the spec of record, written by the fold seat over both blind arms | **the packet denominator is the register's — 2 of 17; carriage over row-ids is NOT asserted in this file's voice (LAW B)** — rows land by their home records' lines cited at §Carry; the carriage census is the successor conformance pass's artefact |
| IMPLEMENTED | NO | stays NO until the gates green after the sequencing head lands |
| VERIFIED | NO | a successor close's act; **KF.W10's close stamps the AUTHORED ELEVEN only** (COHESION §0d) |

**Goal criterion.** This wave succeeds if, when work ends, **the chrome a user presses does what the chrome says it does**: the @mbabb menu opens and stays open by its own hold rather than by a five-site round-trip built on a false Vue mechanism claim (M-4's round-trip gone, the MUST-CARRY binding kept); one propagation policy governs both keyboard mirrors and is stated once, the transport actuates once per gesture on the control the gesture began on and cancels cleanly on release-elsewhere and on blur, with Space working on every button (the TD bundles, unsplit); and the playback ribbon is a named, stepped, keyboard-scrubbable slider whose time-space is declared and shared with the options card (C-2 + KF-CO-15, one commit) — all cured ON the glass primitives the demo already consumes, never by swapping them. **A wave that deletes M-4's round-trip as worded and ships an unopenable menu has failed this goal and has shipped a regression** — the rider is the only thing standing between the cure and the regression; a wave that lands TD-2 without TD-38/TD-40, or one stylesheet deletion without its counterpart, has failed it too.

**Execution shape**: **4 seats across 3 phases** — phase 1: 2 parallel (`.a` dock-menu — whose FIRST commit is the product-byte-free mechanism proof, G-KFW13-0, and whose deletion may not precede it ∥ `.b` transport/ribbon — whose C-2 joint commit waits on KF.W12 `.b`'s close); phase 2: 1 serial (`.c` the two-deletion act with the K-5 lock, after `.b`); phase 3: 1 serial (`.d` close). Peak concurrency **2**, inside the owner's four-workflow cap. The proof is first and alone by design: M-4's cure is a **deletion whose safety depends on a claim about Vue's `inject` resolution**, and the register's own history at this row is a superlative awarded for a false statement about Vue and then withdrawn. Tiering per M-12: `.a`'s proof-then-deletion and `.b`'s remainder derivation are Fable-worker ∥ Opus-worker → fresh-Fable arbiter; `.c` and `.d` Opus solo.

---

## §State

**Name**: KF.W13 — Chrome, Dock & Transport Repair
**Opens after**: **G-KFW4-1 lands GREEN** (the sequencing head; R-15: *"All 17 sequence after G-KFW4-1"*; **measured RED at this seat, twice**: ⟨cmd⟩ `npx vue-tsc --noEmit -p tsconfig.json 2>&1 | grep -c 'error TS'` → **54** · **54**; the LEDGER's KF.W4 row is `CLOSED 2026-09-17 (honest-RED: G-KFW4-1 …)`) · **KF.W7's per-surface SWAP verdicts** (LANDED `4c03ceda` — the LEDGER's own row condition *"drag-seam/transport after KF.W7's SWAP verdicts"*, which W7's close reads GREEN: *"The SWAP verdicts are STATED (six KEEP-BESPOKE, zero SWAP)"*) · **KF.W12 `.b` (OPTIONS-UNIT) CLOSED** — for `.b`'s C-2 joint commit only: the KF-CO-15 extension needs LP-1's render edge to exist (§Sequencing 3) · **KF.W6 CLOSED** (the `kf-focus-ring` rename `a6418729` re-anchored the two-deletion act's `design-idioms.css` counterpart and added a forced-colors block beside it — OP-5) · **KF.W8 CLOSED** (`usePlayActuation.ts`, `useIconSpin.ts`, `useMenubarMeasure.ts` are extracted composables that did not exist when kf-TransportDock was banked — OP-6) · **KF.W9's G-KFW9-9 before-witness pair** held READ-ONLY there.
**Agents**: 4 (a→d); phases as above; **no jury**.
**Hard gate**: **eight conditions, ALL RED**, measured at `69095552` and pasted at §Gates, each double-run.
**Status**: planned

## §0 Open preconditions (checked at wave-open; NOT close gates)

| # | precondition | state at authoring (this seat, 2026-09-18, at `69095552`) |
|---|---|---|
| **OP-0** | **G-KFW4-1 GREEN** — the head. | **RED — 54 · 54.** HARD. |
| **OP-1** | **KF.W7's SWAP verdicts for the transport/ribbon surfaces and the drag seam.** | **MET** — KEEP ×6, `4c03ceda`, 2026-09-18; discharge set EMPTY. Both packets un-gated; `.b` states it before spending a cure. |
| **OP-2** | **LP-1's write→render edge (KF.W12 `.b`)** — the C-2/KF-CO-15 joint commit's reactive `:max` and one-duration-read are inert until the options card re-renders on engine writes. | **UNLANDED** (KF.W12 is planned at this clock). `.b`'s C-2 joint commit opens on KF.W12 `.b`'s close sha; every other `.b` row does not wait. Measured: `PlaybackRibbon.vue:19` `:max="effectiveDuration"` — **the reactive `:max` rider appears partly landed; `.b` derives the remainder and says so.** |
| **OP-3** | **The glass-ui producer facts the MUST-CARRY rider rests on** — `dropdown-menu`'s `defaultOpen`/`open`/`modal` are bare `{ type: Boolean }`, no defaults → Boolean-cast makes the primitive permanently controlled (kf-MbabbMenu MM-28 → the rider). | **RE-MEASURED AT `.a`'s OPEN against the installed dist** (glass-ui is READ-ONLY; the producer half — `default: undefined` — rides SS-6, lock 4). If the producer has shipped defaults by then, the rider's *shape* changes (the local binding may become optional) but **the rider's obligation does not: the menu must open after M-4's cure, proven by G-KFW13-1's runtime clause**, never by a byte. Today the demo's own bytes hold the premise: `MbabbMenu.vue:4` `<DropdownMenu v-model:open="open">` · `:204` `const open = defineModel<boolean>("open", { default: false })`. |
| **OP-4** | **The self-hold mechanism M-4's deletion rests on** — *"a `keepOpen()` MbabbMenu can call directly via `useOptionalDockContext()` on the subpath it already imports"* (`kf-ChromeDock.md:45`); the C/S-2 + C-17 kills: Vue resolves `inject` along the runtime parent chain, proven by glass-ui's own portal stamp inside ChromeDock's render. | **THE EXPORT IS REACHABLE; THE DEMO DOES NOT CALL IT** — ⟨cmd⟩ `grep -rn 'useOptionalDockContext' demo` → **one hit, and it is the false comment itself** (`ChromeDock.vue:110`, inside the `:109-113` claim that slot content's call *"resolves ABOVE this provider and cannot hold the dock open itself"*); ⟨cmd⟩ `grep -n 'useOptionalDockContext' node_modules/@mkbabb/glass-ui/dist/components/dock/index.d.ts` → `:12` (exported); ⟨cmd⟩ `grep -n 'glass-ui/dock' demo/app/dock/MbabbMenu.vue` → `:192` `import { DockTrigger } from "@mkbabb/glass-ui/dock"` (the subpath is already imported). **The kill's premise is the registry's, ratified; its consequence is unexecuted at the tree.** `.a` executes it in a mounted proof (G-KFW13-0) **before** deleting a site. HARD for the deletion. |
| **OP-5** | **G-KFW9-9's witness pair and its re-derivation obligation** — KF-W9 §Bounds: *"kf `demo/styles/design-idioms.css:76-79` + `demo/styles/playback-idiom.css:72-75` … READ-ONLY WITNESS SURFACE … The two-deletion act is KF.W13's; G-KFW9-9 measures both offsets and holds the before/after pair"*; KF-W9 D-3+DU-M-1: *"the redundancy ground for `.btn-playback:focus-visible` is UNTESTED here and must be re-derived at KF.W13 **before** the deletion lands."* | **BOTH OFFSETS HAVE MOVED, AND ONE HALF HAS CHANGED SHAPE (dated observation)**: ⟨cmd⟩ `grep -n 'focus-visible' demo/styles/playback-idiom.css` → **`:74 .btn-playback:focus-visible {`** (`:74-77`: `box-shadow: var(--focus-ring-shadow); outline: none;`) — the playback half is unchanged at a moved offset; ⟨cmd⟩ `grep -n 'focus-visible' demo/styles/design-idioms.css` → the counterpart is **`:106 .kf-focus-ring:focus-visible {`** (`:106-109`, `box-shadow` + `outline: none`) **followed at `:111-116` by a `@media (forced-colors: active)` block restoring `outline: 2px solid Highlight`**, under a WHY block (`:73-105`) stating the name is `kf-` prefixed because glass-ui 7.0.0 ships a realized `.focus-ring:focus-visible` in `@layer components` (KF-KE-30, KF.W6 `a6418729`). **K-5's ground — both copies defeat the producer's forced-colors outline — now holds for the playback half only at the bytes.** The act stays ONE act on the pair G-KFW9-9 witnesses, but its *shape* is the re-derivation's output, not the bank's: `.c` re-reads KF-W9's G-KFW9-9 statement and KF.W6's KF-KE-30 ruling at open, re-derives the redundancy ground for both selectors, and lands whatever the re-derivation finds as ONE commit naming both stylesheets (a deletion, or the forced-colors parity the counterpart already carries) — **never a silent single-file edit where the bank named two, and never a deletion of a rule KF.W6 ruled load-bearing without saying so.** The AFTER witness is KF.W9's to re-shoot (S-9). |
| **OP-6** | **KF.W8's extracted composables** — `usePlayActuation.ts` (88 L), `useIconSpin.ts` (29 L), `useMenubarMeasure.ts` (28 L) did not exist when kf-TransportDock was banked, and **part of the TD keyboard family appears already landed inside them**. | **MEASURED**: ⟨cmd⟩ `grep -n 'isSpace\|spaceArmed' …/TransportDock/usePlayActuation.ts` → `:39` (`isSpace`) · `:47` (`let spaceArmed`) · `:74` · `:78` · `:82-83` — **the native Space/keyup semantics TD-38's docblock prescribes are IMPLEMENTED**; the asymmetry TD-2 names is live in one command (⟨cmd⟩ `grep -c '\.stop' …/TransportDock.vue` → **5**; `…/dock/ChromeDock.vue` → **0**). **`.b`'s FIRST act is deriving what remains of TD-2/TD-38/TD-40 against landed code, receipted** — a cure written from the bank alone re-lands a repaired defect; **the bundle lock still binds whatever remains** (a member already landed upstream does not dissolve the bundle for the rest). HARD for `.b`. |
| **OP-7** | **TD-37 — one face order — is an SS-2 DESIGN DECISION** (`kf-TransportDock.md:131`). | **OWNER/DESIGN-RULED, not this seat's**: `.b` lands TD-37 only with the ruling in hand; otherwise `complete_with_misses` naming it. |
| **OP-8** | **R3-1's kill is RATIFIED and binds the cure's shape** — `InstanceType` = slot bag was KILLED at the real package: `x.expanded: boolean`, `x.expand()/keepOpen()/release()` all clean. | **STANDING**: the five ChromeDock call sites are typed today; C-2's cure is NOT gated; **the `ComponentExposed<>`/`Pick<>` remedies MUST NOT LAND** — a seat that "fixes types" here is curing a defect the registry killed with three concordant real-package probes. |

**OP-0 is hard for the wave; OP-4 is hard for the deletion; OP-6 is hard for `.b`.** OP-7 gates one row.

---

## §Scope

1. **dock-menu — the proof, then M-4 with the rider, as ONE motion** (`kf-ChromeDock.md:45`; `kf-MbabbMenu.md:26` MM-28): **first, the product-byte-free mechanism proof** — a mounted vitest in which slot content inside a `GlassDock` provider calls `useOptionalDockContext()` and resolves the provider's `keepOpen()`/`release()` (G-KFW13-0; the proof is a test file and an evidence receipt, no `demo/**` byte); **then** delete the `itemsPopupOpen` round-trip — measured live: ⟨cmd⟩ `grep -rn 'itemsPopupOpen\|items-popup-open\|mbabbPopupOpen' demo` → **7 lines: 5 code sites** (`App.vue:29` `:items-popup-open="mbabbPopupOpen"` · `App.vue:37` `v-model:open="mbabbPopupOpen"` · `App.vue:372` `const mbabbPopupOpen = ref(false)` · `ChromeDock.vue:114` the prop · `ChromeDock.vue:237` the `isAnyOpen` term) **+ 2 prose sites** (`App.vue:369` · `MbabbMenu.vue:186`) — the round-trip is ref → prop → `isAnyOpen` → `defineModel` → back; **MbabbMenu self-holds via `useOptionalDockContext()`** on the `/dock` subpath it already imports (`:192`); **i-2's unpaired `release()` — safe today only by the undocumented `Math.max(0, …)` clamp — dies with it and is named as dying**; **the `:109-113` false-mechanism comment is deleted or corrected** (the comment is part of the defect surface); **WITH THE MUST-CARRY RIDER: `MbabbMenu.vue:4` `v-model:open="open"` and `:204` `defineModel("open")` SURVIVE the motion** (what goes is the ChromeDock round-trip, not the menu's own open state — *"executed as worded, the menu becomes permanently unopenable"*), and the `:186` comment that calls the binding dock-plumbing is corrected to say what it holds; **`ComponentExposed<>`/`Pick<>` do not appear** (OP-8). **Runtime proof, not byte proof**: the menu opens after the motion (G-KFW13-1). **Then kf-MbabbMenu `:169`'s dock-menu family**: MM-2·3·5·7·8·9·10·11·12·13·16·17·18·19·20·21·22·24(consistency)·25·26·29·30·31·32·39·40·42 — **twenty-seven rows** — **+ MM-1/MM-6's cure law ≡ the banked KF-APP-1 motion, a four-part single edit** (repair `:100`, dispose `setPPMode` with the KF-APP-17 sweep, resolve the C-14 bucket split, land the MM-5 CheckboxItem in the same edit) **under ARB-1's sequencing law — DELETE ARM ONLY until the glass-ui M-A relay lands; the `headerLeft` 'fill' arm is a TRAP** (it lands inside an inert toolbar; the KF-APP-1/-17 same-motion lock is homed in its authored wave and this packet CONSUMES its arm; shared with KF.W11's cube packet, re-booked by neither); the typography/brand arm (MM-29's two `normal-case` sites at `MbabbMenu.vue:5-14`; the `text-admin-label` chip register; the `text-micro` rung). **Then TD-36** — the ChromeDock pointer-events pair, two classes, **measured in ChromeDock's bytes** (⟨cmd⟩ `grep -n 'pointer-events' demo/app/dock/ChromeDock.vue` → `:299` `pointer-events-none` on the `fixed … z-dock` wrapper · `:302` `pointer-events-auto`; the `TransportDock.vue:299` hit is a comment) — banked at kf-TransportDock, **bytes in `ChromeDock.vue`, spent ONCE here (one identity, one home)**. **Then kf-ChromeDock's own NO-WAVE-OWNER chrome roster by id**, carried under this packet because *Chrome* is this wave's name and `ChromeDock.vue` is this unit's file: M-5/C-6 · RR-2 MISSED #1 · R3-2 · M-2/C-4 · M-3 · D-3 · D-4 · C-8 · D-20 · M-1/C-3/D-14+R3-7 · D-2-RESCOPED · RR-1 MISSED #2/#3/#4 · RR-2 MISSED #2 · D-13 · D-6/m-6 · D-7/C-15/m-13 · D-11/m-4/R3-5 · D-12 · D-25 · D-26 · C-10 · m-2/C-13 · m-12 · m-3/R3-6 · R3-4 · D-21 · D-16+L/i-1 · R3-8 (D-18/C-1 is ADJUDICATED with TD-39's rider on its cure spec; TD-22's re-pin inversion is *one cure spec with the ChromeDock row*) — each LANDED or KILLED-with-rationale or carried, never dropped (M-25; dissent 1 at §L-18).
2. **transport/ribbon — the TransportDock half** (`kf-TransportDock.md:131`): **OP-6's remainder derivation first, receipted**; then **the keyboard-propagation commit TD-2 + TD-38 + TD-40 — ONE commit** (measured: `TransportDock.vue:205-209` five `.stop`s on the collapsed mirror — `@pointerdown.stop` · `@pointerup.stop` · `@pointercancel.stop` · `@keydown.stop` · `@keyup.stop` — TD-38's inversion against the dock's capture-phase listeners; ChromeDock's mirror **0**; `useControlsKeyboardShortcuts.ts:50` `registerShortcut("Space", …)` with `preventDefault: true` unconditionally — TD-40's Space inoperable on Reset/Collapse-timeline; TD-2's double actuation on the focused expanded Play) — **one propagation policy written down once, `.stop` symmetry decided once on both mirrors (symmetry is the invariant, not `.stop`), Space registration scoped away from button targets, composition tests that mount the buttons WITH the registry** (a test that mounts them without it proves nothing); **the actuation-cancellation commit TD-21(+r2 rider) + TD-41 — ONE commit** (per-control origin, release-elsewhere cleanup, blur-class disarm, orphan-keyup + stale-id cases — measured: `transport-play-actuation.test.ts` exists with ⟨cmd⟩ `grep -c 'it(' …` → **10** `it(` lines and ⟨cmd⟩ `grep -c 'blur\|orphan\|stale' …` → **0** — the cancellation arm is the residue); **TD-1 + TD-4 + PRM + test — ONE commit** (measured: ⟨cmd⟩ `grep -c 'instanceof HTMLElement' …/useIconSpin.ts` → **2** against a functional-lucide `SVGSVGElement` ref — 100 % dead; TD-4's eager `kfEngine()` + zero teardown; TD-17's typed refs ride the re-authoring); **TD-37** (OP-7); **TD-39** (one stable name, with TD-5/TD-28's prose pass; `font-roles.json:12`'s `.btn-playback` selector row if the rename lands); **TD-34/TD-35** (the TD-31 cluster: glyph translate-not-shrink; `--dock-layer-gap`). **TD-36 is spent in `.a` (its bytes are ChromeDock's) and is named here by both routing blocks, not booked twice.**
3. **transport/ribbon — the PlaybackRibbon half** (`kf-PlaybackRibbon.md:157`, under the rider and PR-CAUTION): the instrument-truth core — **D-1** (+N-2's demo half: the sole AT-exposed scrub control has no accessible name; ⟨cmd⟩ `grep -c 'aria-label' demo/components/playback/PlaybackRibbon.vue` → **0**) · **D-2**'s demo arm (no visible playhead) · **C-2 — the time-space contract** (`currentT` declares no space; `scrubTo` inverts under Reverse; 2 of 3 mounts feed RAW `t`; **the channel mount is the pin; cured JOINTLY with the KF-CO-15 extension so scale and space land together** — one commit touching `PlaybackRibbon.vue` and `ChannelOptions.vue`'s KF-CO-15 seam, after OP-2) · the KF-CO-15 riders (reactive `:max` — partly live at `:19`, remainder derived; one duration read; one guard — L-m9) · **C-3** (`valueCommit` forwarded and ignored — ⟨cmd⟩ `grep -c 'valueCommit\|value-commit' …/PlaybackRibbon.vue` → **0**) · **C-4** (`:step` — ⟨cmd⟩ `grep -c ':step' …/PlaybackRibbon.vue` → **0**; reka defaults 1 ms per arrow press over a ms `:max`) — the one-prop/one-token cures **D-5 (+M-6) · D-6 (`disabled`, not the `.is-disabled` costume at `:9`/`:74`) · D-10 (the accent pair's 3.58–4.02:1 label) · L-M1 (delete the wrapper touch gate — glass-ui 7.0.0's Slider owns its own `useTouchGate`; carry S-3; measured `gatedSliderDown` at `:167`, bound `@pointerdown.capture` at `:11`)** — one pressed authority **D-8/N-1/D-15** — the hygiene sweep **D-7 · N-3 (delete the doubled press affordance — banked `playback-idiom.css:76-78`, the `.btn-playback:active` rule, measured today at `:78-80`; kf-PlaybackRibbon's own row, its bytes this packet's) · D-17/N-4 · D-13 · D-16 · D-19 · D-20/D-25 (the five-false-comments prose sweep) · D-23-as-preconditions (with KF-ES-2's owner) · L-m5/L-m6/L-m9/C-9/C-11/N-5/N-6 · L-i2/C-15**; **D-14/C-10/L-m1 ≡ KF-CPW** at its bank. **`PlaybackRibbon.vue:93`/`:137` consumes `useDragCapture` — KF.W11's drag-seam packet writes that composable with its exported surface unchanged; `.b` re-measures the consumer at its open.** Born-RED the packet inherits: the signed-seek witness (residue 9), the keyboard-scrub pause/resume assertion (C-3), the axe/name assertion on the thumb (D-1), the forced-colors focus screenshot (D-3 → SS-13, one shared capture).
4. **The two-deletion act with the K-5 lock intact** (OP-5) and **kf-PlaybackRibbon D-12's demo-side stopgap bytes** (the `:deep` slider-height cure at `PlaybackRibbon.vue:216-226`'s F4 block — measurement KF.W9's, cure this packet's): W13 owns `playback-idiom.css`; `design-idioms.css`'s counterpart rides the same single act — **the re-derivation receipt first, then ONE commit naming both stylesheets**; the K-5 ground re-tested at the frontier; N-3's `:active` deletion carries its own census in its own commit.
5. **Close** — receipts, the commit roster audited to §Bounds with every family proven unsplit, the BH relay (the `default: undefined` producer ask for `dropdown-menu`'s Boolean props — the rider is the demo-side *accommodation* of that producer row, not a fix of it; PlaybackRibbon's producer seams if any), E13, the LEDGER row.

### Triumvirate dispatch (WAVE_SPEC §3a)

- **Bounds whose expansion invalidates the wave**: any glass-ui or `node_modules` byte (the Boolean-cast defaults, the Slider's `useTouchGate`, the dock's capture-phase listeners are the producer's); any `src/**` byte; any write to `demo/components/instrument/timeline/**` (KF.W7's — including `KeyframeTimeline.vue`, the frontier's eighth `btn-playback` file), `AnimationVisualizer.vue` or `useDragCapture.ts` (KF.W11 `.i`'s), `demo/scenes/**` (KF.W11's — including the two bare `.focus-ring` sites), `demo/components/instrument/keyframes/**` (KF.W12's).
- **Non-local gate failures**: G-KFW13-0 failing (the slot child cannot resolve the provider in a mount) → the M-4 deletion is NOT spent; triumvirate names the registry's C/S-2 kill for re-examination and SS-6; G-KFW13-1's runtime clause failing after M-4's deletion with the local binding kept → the producer's Boolean-cast is the cause; triumvirate names SS-6 and the rider's shape is re-cut, never the menu hacked open; G-KFW13-5 failing because the options card cannot re-render (OP-2 not really landed) → triumvirate with KF.W12 named.
- **Diagnostic loops that halt at three**: the TD-21 stale-id resurrection reproduced three times without a deterministic pointer harness → halt.

---

## §Bounds

### B.1 Frontier re-anchor (this seat, read-only, `69095552`, 2026-09-18)

| # | inherited anchor ⟨bank · register⟩ | frontier truth | disposition |
|---|---|---|---|
| 1 | **kf-ChromeDock M-4's false mechanism claim at `:73-78`** | the claim sits at **`:109-113`**, in prose, as the design's justification (*"resolves ABOVE this provider and cannot hold the dock open itself"*). | RE-ANCHORED; the comment is part of the defect |
| 2 | **M-4's five-site round-trip** (`isAnyOpen` `:172`, `MbabbMenu.vue:96`) | **7 grep lines = 5 code + 2 prose** (§Scope 1); `defineModel` at **`:204`**. | CORRECTED, all re-pinned; G-KFW13-1's denominator |
| 3 | **`useOptionalDockContext` reachable on the imported subpath** | exported at glass `dock/index.d.ts:12`; imported subpath at `MbabbMenu.vue:192`; **called by no demo file** (the sole hit is the false comment). | LOAD-BEARING: the predicate is unexecuted → G-KFW13-0 |
| 4 | **TD-2 + TD-38 + TD-40** | `.stop` ×5 at `TransportDock.vue:205-209`, ChromeDock 0; **Space/keyup semantics already in `usePlayActuation.ts`** (OP-6). | DECLARED RE-ANCHOR; the remainder is derived at `.b`'s open; the bundle binds it |
| 5 | **`btn-playback` demo-scoped files = 7** (RULINGS-4 R4-4; *"8" is struck*) | ⟨cmd⟩ `git grep -l 'btn-playback' HEAD -- demo` → **8**: the seven **plus `demo/components/instrument/timeline/KeyframeTimeline.vue`** (a KF.W7-era consumer that adopted the class after the census ran — a different file from the struck eighth candidate). | DECLARED DRIFT (E-3 — RULINGS-4 stands; both statements are true at their own coordinates). The sweep's denominator at execution is re-derived by this command; the eighth file is **counted and routed, never edited** |
| 6 | **`.focus-ring` consumers = exactly 4** | ⟨cmd⟩ `grep -rn 'focus-ring' demo | grep '\.vue:' | grep -v 'kf-focus-ring' | grep -v 'focus-ring-shadow'` → **2 class applications**: `SquareScene.vue:46` · `SpringTarget.vue:63`; `KeyframeCard.vue` → `kf-focus-ring` at `:117`, `SpringHeatmap.vue` → `:37` (KF.W6). | DECLARED DRIFT (E-3). **Both bare sites are KF.W11's files** — this wave states the policy, KF.W11 spends the edits |
| 7 | **G-KFW9-9's witness pair** `design-idioms.css:76-79` + `playback-idiom.css:72-75` | `playback-idiom.css:74-77` `.btn-playback:focus-visible` (unchanged shape) · `design-idioms.css:106-109` `.kf-focus-ring:focus-visible` **+ `:111-116` forced-colors parity** (OP-5). The Opus arm's *"the copy is gone"* measured `btn-playback` in `design-idioms.css` (**0**) — but the counterpart was never a `btn-playback` rule; it is the renamed `.focus-ring` rule shape. | RE-ANCHORED, shape change DECLARED; the act is ONE act on the pair, its form the re-derivation's |
| 8 | **kf-PlaybackRibbon N-3's `playback-idiom.css:76-78`** (`.btn-playback:active`) | `:78-80` today. | RE-ANCHORED |
| 9 | **TD-36's bytes** | `ChromeDock.vue:299`/`:302`; the `TransportDock.vue:299` hit is a comment. | homed at `.a` (one identity, one home) |

### B.2 File bounds

All paths keyframes.js-relative at `69095552`; line counts ⟨cmd⟩ `wc -l` this seat.

| File | L | Access | Unit |
|---|---|---|---|
| `demo/app/dock/ChromeDock.vue` | 534 | modify (M-4: `:114` prop, `:237` term, the `:109-113` comment; TD-36's pair `:299`/`:302`; the chrome roster) | `.a` |
| `demo/app/dock/MbabbMenu.vue` | 229 | modify (the MUST-CARRY: `:4` and `:204` SURVIVE; `:186` comment corrected; self-hold via `useOptionalDockContext()`; the `:169` family; MM-29's `:5-14`) | `.a` |
| `demo/app/dock/index.ts` | 2 | modify-carve (the barrel, only if the self-hold changes the surface) | `.a` |
| `demo/app/App.vue` | 412 | modify-carve (`:29` · `:37` · `:369` · `:372` — M-4's App sites; the KF-APP-1 motion's `:100`-class repair as consumed, delete arm only) | `.a` |
| `demo/components/instrument/transport/TransportDock.vue` | 403 | modify (TD-2/38/40 · TD-37 · TD-39 · TD-34/35) | `.b` |
| `demo/components/instrument/transport/TransportDock/usePlayActuation.ts` | 88 | modify (TD-21 + TD-41; the landed Space/keyup semantics at `:39`/`:47`/`:74`/`:78`/`:82-83` are the remainder's baseline) | `.b` |
| `demo/components/instrument/transport/TransportDock/useIconSpin.ts` | 29 | modify (TD-1 + TD-4 + TD-17) | `.b` |
| `demo/components/instrument/transport/TransportDock/useMenubarMeasure.ts` | 28 | modify-carve (TD-34/35 only) | `.b` |
| `demo/components/instrument/transport/AnimationControlsGroup/useControlsKeyboardShortcuts.ts` | 72 | modify (TD-40: `:50`'s Space registration scoped away from button targets) | `.b` |
| `demo/components/playback/PlaybackRibbon.vue` | 244 | modify | `.b` |
| `demo/components/instrument/transport/channel-controls/ChannelOptions.vue` | 731 | **modify-carve — the KF-CO-15 extension ONLY, in the C-2 joint commit, AFTER KF.W12 `.b` closes** (the file is KF.W12's; this carve is the declared exception and is never parallel with it) | `.b` |
| `demo/styles/font-roles.json` | — | modify-carve (`:12`'s `.btn-playback` selector row, only if TD-39's rename lands) | `.b` |
| `demo/styles/playback-idiom.css` | 89 | modify — **SHARED `.b` (N-3's `:active` deletion at `:78-80`; D-12's stopgap) → `.c` (the two-deletion act's playback half at `:74-77`)** (serial) | `.b` → `.c` |
| `demo/styles/design-idioms.css` | 338 | modify-carve (the counterpart `:106-116` in the SAME act — K-5; nothing else in the file) | `.c` |
| `test/demo/app/dock-context-slot-resolution.test.ts` | — | create (**G-KFW13-0's proof — no product byte in its commit**) | `.a` |
| `test/demo/app/mbabb-menu-self-hold.test.ts` | — | create | `.a` |
| `test/demo/instrument/transport-keyboard-propagation.test.ts` | — | create (the composition tests that mount the buttons WITH the registry) | `.b` |
| `test/demo/instrument/transport-icon-spin.test.ts` | — | create (TD-1's test, in TD-1's commit) | `.b` |
| `test/demo/instrument/playback-ribbon-contract.test.ts` | — | create | `.b` → `.c` (the focus-affordance case added by `.c`) |
| `test/demo/instrument/transport-play-actuation.test.ts` | — | modify (TD-21/TD-41's cancellation cases added; the existing ten `it(` never weakened) | `.b` |
| value.js `docs/tranches/X/execution/B/KF-W13.md` | — | create (unit receipts; append-only) | every unit |
| value.js `docs/tranches/X/keyframes/evidence/W13/**` | — | create (the mounted self-hold proof's output, born-RED baselines, the TD remainder derivation, the propagation-policy matrix, the two-deletion re-derivation receipt, the four SS-13-keyed witnesses) | owning unit |
| value.js `docs/tranches/X/execution/LEDGER.md` | — | modify (this wave's row cells + appended lines ONLY) | `.d` |
| value.js `docs/tranches/V/coordination/INBOX.md` + the SS-6 accretion register (COHESION §4a) | — | modify (append) | `.d` |

**Do NOT touch**: every glass-ui tree and `node_modules/**` (the Boolean-cast defaults, the Slider's `useTouchGate`, the dock's capture-phase listeners are the producer's — READ-ONLY always; its `dock/index.d.ts` is read to confirm the export, never edited) · `src/**` · `demo/components/instrument/timeline/**` (KF.W7, incl. `KeyframeTimeline.vue` — counted, routed, never edited), `demo/components/playback/AnimationVisualizer.vue`, `demo/components/instrument/transport/composables/useDragCapture.ts`, `demo/scenes/**` (KF.W11 — incl. `SpringTarget.vue:63`/`SquareScene.vue:46`) · `demo/components/instrument/keyframes/**`, `LayerConfigPanel.vue`, `TimingFunctionPanel.vue`, `controls-pane/**` (KF.W12) · `ChannelOptions.vue` beyond the KF-CO-15 carve · `App.vue` beyond M-4's sites and the consumed motion · `demo/components/instrument/transport/AnimationControlsGroup.vue` + its other composables (ACG D-1's portal cure is KF.W11's cube-packet rider; TD rows that *name* ACG are read-only here) · `demo/components/instrument/transport/components/DemoGlobalChrome.vue`, `shell/SharePopover.vue`, `shell/KeyboardShortcutsModal.vue` (their records route no row to either packet) · `package.json` / `vitest.config.ts` (harness present) · `scripts/dev/dev.sh` · every dated spec, the adjudicated registry, conformance artefacts (E-3).

### B.3 LAW A censuses — every delete this wave books

**LAW A (RULINGS-3): no delete is authorized on a line-count.** (1) **M-4's five-site round-trip deletion** — census = the seven lines of §Scope 1 **plus** every consumer of `isAnyOpen` inside `ChromeDock.vue` (the mutex the scene/controls Selects ride — BLK-8 / D9); **the delete's safety predicate is not a count**: it is G-KFW13-0's executed proof; `i-2`'s `release()` named as dying. (2) **N-3's `:active` deletion** (`playback-idiom.css:78-80`) — census = the rule's subjects and the producer's own spring press that remains; its own commit. (3) **The two-deletion act** — census = both selectors' consumers AND the re-derived redundancy ground for each, pasted together in one receipt; **a commit containing exactly one of the pair fails G-KFW13-6 even if the tree is green**. (4) **The `.stop` ×5 on the collapsed mirror** — not deleted on a count: the policy is written first, then whatever the policy removes carries the dock-listener-phase census.

### Disjointness (WAVE_SPEC §4a)

Phase 1's pair (`.a` ∥ `.b`) is directory-disjoint: `.a` = `demo/app/**` + `test/demo/app/**`; `.b` = `transport/TransportDock*` + `AnimationControlsGroup/useControlsKeyboardShortcuts.ts` + `playback/PlaybackRibbon.vue` + the `ChannelOptions.vue` carve + the two style rows + `test/demo/instrument/**`. **TD-36's bytes are `.a`'s alone** (ChromeDock), which is what makes the pair disjoint. `playback-idiom.css` and `playback-ribbon-contract.test.ts` are `.b` → `.c` serial. The one cross-wave shared path, `ChannelOptions.vue`, is written by KF.W12 `.b` first and by this `.b` (one carve) strictly after its close.

### Worktree plan (WAVE_SPEC §4b)

Phase 1 runs in sibling worktrees `keyframes-kfw13-a` and `keyframes-kfw13-b`, merged by the orchestrator; `.c` and `.d` run serially on `master`. No Cargo.

---

## §Carry — the two packets, homed

**Carriage is not asserted in this file's voice (LAW B).** Ids are the records' own, for life.

### P1 · dock-menu — `kf-ChromeDock.md:45` (M-4) · `:99` (i-2) · `:106` (the C/S-2 kill) · `kf-MbabbMenu.md:15` (the headline) · `:26` (MM-28 → the MUST-CARRY rider) · `:169` (the family) · kf-App (the KF-APP-1/-17 motion; ARB-1) · kf-TransportDock TD-36 (bytes here)

**The proof (G-KFW13-0), then M-4 with the rider as ONE motion**: delete the round-trip's five code sites and correct the two prose sites; MbabbMenu calls `keepOpen()`/`release()` itself via `useOptionalDockContext()` (the C/S-2 kill's mechanism — `SelectContent` stamps `data-glass-dock-portal` from inside the same slot; `DockSeparator` rides the same DI); **keep** `v-model:open="open"` + `defineModel("open")` as the menu's own open state (the rider, carried whole and not cited: *glass-ui's bare `{ type: Boolean }` declarations make the primitive permanently controlled, so M-4's cure AS WORDED ships an unopenable menu without it*), and say so in the docblock that today says the opposite; `i-2` named as dying; R3-1's kill obeyed. **Runtime proof, not byte proof** (G-KFW13-1). **Then the `:169` family** in the record's cure law (the KF-APP-1 motion consumed, delete arm only; MM-5's CheckboxItem in the same edit), **then TD-36** (one identity, one home), **then the ChromeDock chrome roster** by id (§Scope 1). **Producer half → SS-6**: `default: undefined` on `open`/`modal`/`defaultOpen` (and LabeledSelect/LabeledSwitch's, already filed by kf-LayerConfigPanel — cross-referenced, not re-filed).

### P2 · transport/ribbon — `kf-TransportDock.md:131` · `:45-47` (TD-1/2/4) · `:64` (TD-21) · `:82-89` (TD-34..41) · `kf-PlaybackRibbon.md:157` · `:36` (the routing law + rider) · `:40-55` (the rows) · `:64` (N-3)

**OP-6's remainder derivation first.** **Three one-commit families, unsplit (the TD-1/TD-2 bundle WHOLE — lock 2; re-affirmed from W7's end)**: (1) **TD-2 + TD-38 + TD-40** (whatever remains after OP-6, still one commit); (2) **TD-21 + TD-41**; (3) **TD-1 + TD-4 + PRM + test**. Then **TD-37 (OP-7) · TD-39 · TD-34/35**. **The ribbon**: (4) **C-2 + KF-CO-15 — ONE commit, after OP-2**; (5) **D-1 + C-3 + C-4** (name, `valueCommit`, `:step` — the three bindings glass-ui already forwards; PR-CAUTION: cured ON the Slider); (6) **D-5 · D-6 · D-10 · L-M1**; (7) **D-8/N-1/D-15** (one pressed authority); (8) **D-2's demo arm**; (9) the hygiene sweep incl. **N-3's own-commit deletion** and **D-20/D-25's five false comments**; (10) **D-12's stopgap** in `playback-idiom.css`. Born-RED witnesses the packet inherits land in G-KFW13-5's file (signed seek; keyboard-scrub pause/resume; the thumb's axe/name); the forced-colors focus screenshot is SS-13's — one shared capture, keyed by row id, named not taken here. **Then `.c`'s two-deletion act** (§Scope 4; G-KFW13-6).

**Identity notes**: TD-37's stale-Set rider ≡ TD-21 (one identity, `:94`); TD-39 ≡ r1 superlative 4's subject, a rider on the ChromeDock D-18 cure spec (`:96` — lands with `.a`'s D-18 cure or names it); TD-22 is *one cure spec with the ChromeDock row* (`.a`'s RR-2 M#1 mechanism); TD-36 spent at `.a`; D-14/C-10/L-m1 ≡ KF-CPW (its bank); the filed "both animate the same target" shape is not this wave's.

---

## §Agent Units

Each unit: Goal · Mechanism · Files · Sub-gate. Receipts to `execution/B/KF-W13.md`, `SERVED MODEL: <id>` line 1, commands double-run, SELF-COUNT, inherited paths named (CRASH-RECOVERY). Out-of-bounds = ESCALATION, returned.

### KF.W13.a — The Dock-Menu Packet (phase 1)
- **Goal**: the brand menu holds the dock open by itself and still opens, the chrome roster's rows are cured in one family each, and no deletion is spent on a reading.
- **Mechanism**: **commit 1 = the mechanism proof, no product byte** (`dock-context-slot-resolution.test.ts` mounts slot content inside a `GlassDock` provider and asserts `useOptionalDockContext()` resolves it; its output banked at `evidence/W13/`); **commit 2 = M-4's deletion + the self-hold + the corrected comments + the kept binding, ONE sha** (the rider is not a follow-up); then MM-1/MM-6's four-part edit under ARB-1; the 27-row family; TD-36; the chrome roster; the producer ask recorded for `.d`; OP-3's dist measurement printed.
- **Files**: the `.a` rows; the two `test/demo/app/*.test.ts` files (create).
- **Sub-gate**: G-KFW13-0 → G-KFW13-1 → G-KFW13-2 GREEN, in that order in `git log`.

### KF.W13.b — The Transport/Ribbon Packet (phase 1; the C-2 joint commit after KF.W12 `.b`)
- **Goal**: one actuation per gesture on the control it began on, Space on every button, one propagation policy stated once and symmetric on both mirrors, a named stepped keyboard-scrubbable ribbon in a declared time-space shared with the options card.
- **Mechanism**: the KF-AV-28 KEEP verdict stated first, zero discharge receipts; OP-6's remainder derivation receipted before any TD byte; P2's families in order, each family's sha listed with `git show --stat` proving it unsplit; TD-37 only with OP-7's ruling; the C-2 joint commit on KF.W12 `.b`'s close sha; `useDragCapture`'s consumer re-measured at open.
- **Files**: the `.b` rows; the three created tests; `transport-play-actuation.test.ts` extended.
- **Sub-gate**: G-KFW13-3 · -4 · -5 GREEN; the receipt prints the `.stop` census on both mirrors before and after.

### KF.W13.c — The Two-Deletion Act (phase 2; serial, after `.b`)
- **Goal**: G-KFW9-9's pair is handled as ONE act whose ground was re-tested at the frontier, and the focus affordance survives on the primitive.
- **Mechanism**: re-read KF-W9's G-KFW9-9 statement and KF.W6's KF-KE-30 block at open; re-derive the redundancy ground for `.btn-playback:focus-visible` and for `.kf-focus-ring:focus-visible` (OP-5); paste both consumer censuses together; land the act as ONE commit naming both stylesheets; add the focus-affordance case to `playback-ribbon-contract.test.ts`; hand the AFTER witness to KF.W9 by row id.
- **Files**: `playback-idiom.css` (`:74-77`), `design-idioms.css` (`:106-116`), `playback-ribbon-contract.test.ts` (extend).
- **Sub-gate**: G-KFW13-6 GREEN; the sweep denominators of G-KFW13-7 printed beside RULINGS-4's.

### KF.W13.d — Close (phase 3; serial, last)
- **Goal**: record whole, commits audited with every family proven unsplit, producer rows sailed, LEDGER row moved.
- **Mechanism**: all eight gates re-run double; `git show --stat` per sha against §Bounds; SS-6 accretion entries; E13 sweep; LEDGER cells + event line; residuals/escalations; the discharge set stated positively as EMPTY.
- **Files**: the value.js rows.
- **Sub-gate**: G-KFW13-7 GREEN.

### Standing law for every unit

As KF-W11's — CRASH-RECOVERY first · the spec GOVERNS, no masking fallback (no `try/catch` around a defect, no `test.skip`, no allowlist, **no copied producer selector** — the dock's and Slider's internals are consumed through their exported seams — no `node_modules` patch) · pathspec commits, families unsplit · E-3 · WRITE-THEN-MEASURE double-run · SELF-COUNT · quote-by-command · glass-ui READ-ONLY always · E13 · probe parsimony · `dev.sh` never staged · LEDGER minimal edits only.

---

## §Gates — eight, all born RED at `69095552`, literal commands, double-run

**SELF-COUNT (LAW D(3))**: ⟨cmd⟩ `grep -c '^\*\*G-KFW13-' KF-W13.md` → **8** — `G-KFW13-0` (the phase-1 safety predicate; numbered before the thing it gates) plus `G-KFW13-1` … `G-KFW13-7`. All commands from `/Users/mkbabb/Programming/keyframes.js`. Runtime gates are `vitest` runs; byte clauses ride beside them, never alone.

**G-KFW13-0 — the self-hold is PROVEN before anything is deleted.** ⟨cmd⟩ `npx vitest run --project demo test/demo/app/dock-context-slot-resolution.test.ts` → RED today: `No test files found` (⟨cmd⟩ `ls test/demo/app/` → `e-w1-encapsulation.test.ts` only, twice). GREEN when, in a mounted render, a component authored as slot content of a `GlassDock` provider calls `useOptionalDockContext()` and receives that provider's context (non-null; `keepOpen()`/`release()` callable and observable on `expanded`). Byte clause: ⟨cmd⟩ `grep -rc 'useOptionalDockContext' demo | grep -v ':0'` → `ChromeDock.vue:1` · `ChromeDock.vue:1` (the false comment — the proof's commit leaves this unchanged; **no `demo/**` byte is in G-KFW13-0's sha**). **A cure spent on a reading fails this gate.**

**G-KFW13-1 — dock-menu: the round-trip is gone and the menu still opens (M-4 + the MUST-CARRY rider, ONE commit).** ⟨cmd⟩ `npx vitest run --project demo test/demo/app/mbabb-menu-self-hold.test.ts` → RED today: `No test files found`. GREEN when: MbabbMenu mounted inside a GlassDock provider opens on trigger activation and stays open (the rider's runtime proof), calls the dock's keep-open on open and release on close, and ChromeDock exposes no `itemsPopupOpen` prop. Byte clauses, all in ONE sha: ⟨cmd⟩ `grep -rn 'itemsPopupOpen\|items-popup-open\|mbabbPopupOpen' demo | wc -l` → **7** · **7** (must read **0**); **the MUST-CARRY clause** ⟨cmd⟩ `grep -c 'v-model:open="open"' demo/app/dock/MbabbMenu.vue` → **1** · **1** (must STILL read **1** — a commit that drives this to 0 while OP-3's Boolean-cast stands has shipped the unopenable menu the rider exists to prevent); ⟨cmd⟩ `sed -n '109,113p' demo/app/dock/ChromeDock.vue | grep -c 'cannot hold the dock open'` → **1** · **1** (must read **0** — the false-mechanism prose deleted or corrected); ⟨cmd⟩ `git show <sha> | grep -c 'ComponentExposed\|Pick<'` → must read **0** (OP-8); `i-2`'s `release()` named as dying in the body.

**G-KFW13-2 — dock-menu: the roster is spent under ARB-1's law.** GREEN when every id in P1's 27-row family and the ChromeDock roster reads LANDED / KILLED-with-rationale / carried in the receipt; MM-1/MM-6's four-part cure is ONE sha; ⟨cmd⟩ `git diff <open-sha>..HEAD -- demo/app | grep -c "headerLeft"` → **0** (no 'fill' arm — a 'fill' arm in this wave's diff is a HIGH defect and a lock violation); and `cube-scene.test.ts`-class existing witnesses stay green. RED today by construction (no roster commit exists).

**G-KFW13-3 — transport: one propagation policy, symmetric on both mirrors, Space on every button (TD-2 + TD-38 + TD-40, one commit).** ⟨cmd⟩ `npx vitest run --project demo test/demo/instrument/transport-keyboard-propagation.test.ts` → RED today: `No test files found`. GREEN when, with the buttons mounted WITH the registry: Space on the focused expanded Play actuates exactly once; Space on Reset and Collapse-timeline activates them and does not fire playback; both mirrors conform to the policy the receipt states once. Byte clauses (recorded; re-read at open): ⟨cmd⟩ `grep -c '\.stop' demo/components/instrument/transport/TransportDock.vue` → **5** · **5**; ⟨cmd⟩ `grep -c '\.stop' demo/app/dock/ChromeDock.vue` → **0** · **0** (the asymmetry; symmetry is the invariant, not `.stop`); ⟨cmd⟩ `grep -c 'registerShortcut("Space"' …/useControlsKeyboardShortcuts.ts` → **1** · **1** (must be scoped away from button targets — the runtime clause decides). **Remainder clause**: the receipt states what `usePlayActuation.ts:39-83` already implements before the commit. **Bundle clause**: the greening commit's `git show --stat` names `TransportDock.vue` AND `useControlsKeyboardShortcuts.ts` AND the test — one sha.

**G-KFW13-4 — transport: cancellation (TD-21 + TD-41) and the icon spin (TD-1 + TD-4), one commit each.** ⟨cmd⟩ `npx vitest run --project demo test/demo/instrument/transport-play-actuation.test.ts test/demo/instrument/transport-icon-spin.test.ts` → RED today: the second file is absent (`No test files found` for it; the first's ten `it(` pass — the baseline, pasted at open). GREEN when the actuation file also passes: a blur mid-press disarms `spaceArmed`; a release over neither control clears the origin id (no stale-id resurrection); an orphan keyup does not actuate — and the spin file passes: the lucide `SVGSVGElement` ref spins, PRM is honoured, teardown runs. Byte clauses: ⟨cmd⟩ `grep -c 'instanceof HTMLElement' …/TransportDock/useIconSpin.ts` → **2** · **2** (must read **0**); ⟨cmd⟩ `grep -c 'blur\|orphan\|stale' test/demo/instrument/transport-play-actuation.test.ts` → **0** · **0** (must read ≥ **3**). **Bundle clause**: two shas, each `git show --stat` naming its composable AND its test.

**G-KFW13-5 — ribbon: named, stepped, keyboard-scrubbable, in one declared time-space (C-2 + KF-CO-15 · D-1 + C-3 + C-4).** ⟨cmd⟩ `npx vitest run --project demo test/demo/instrument/playback-ribbon-contract.test.ts` → RED today: `No test files found`. GREEN when: the thumb has an accessible name (axe/name); an arrow press moves by the declared `:step`, not 1 ms; a keyboard scrub during playback pairs with pause/resume (`valueCommit`); under Reverse the ribbon's slider and its ball agree on effective time (the signed-seek witness); the options card's duration write re-scales `:max` (KF-CO-15, on LP-1's edge); and D-20/D-25's five false comments are corrected in the same motion. Byte clauses: ⟨cmd⟩ `grep -c 'aria-label' demo/components/playback/PlaybackRibbon.vue` → **0** · **0** (≥ **1**); ⟨cmd⟩ `grep -c 'valueCommit\|value-commit' …/PlaybackRibbon.vue` → **0** · **0** (≥ **1**); ⟨cmd⟩ `grep -c ':step' …/PlaybackRibbon.vue` → **0** · **0** (≥ **1**); ⟨cmd⟩ `grep -c 'gatedSliderDown' …/PlaybackRibbon.vue` → **2** · **2** (must read **0** — L-M1). **Joint-commit clause**: the C-2 sha's `git show --stat` names `PlaybackRibbon.vue` AND `ChannelOptions.vue`; **PR-CAUTION clause**: no `Slider` import is replaced or wrapped by a bespoke primitive in the diff.

**G-KFW13-6 — the two-deletion act is ONE act with its ground re-derived (G-KFW9-9's subject, K-5's lock).** ⟨cmd⟩ `grep -c 'box-shadow: var(--focus-ring-shadow)' demo/styles/playback-idiom.css` → **1** · **1** today (`:74-77`); ⟨cmd⟩ `sed -n '106,116p' demo/styles/design-idioms.css | grep -c 'outline'` → **2** · **2** (`outline: none` + the forced-colors `outline: 2px solid Highlight`). GREEN when: the re-derivation receipt precedes the sha (OP-5); ONE commit's `git show --stat` names BOTH stylesheets (a sha touching one is the K-5 violation — *one deletion fixes nothing*); the playback half's `outline: none` no longer defeats the producer's forced-colors outline (the first command reads **0**, or a forced-colors parity block beside it is the re-derived form and the receipt says why); the runtime clause — `playback-ribbon-contract.test.ts` asserts a focused Play button carries a focus indicator under `forced-colors: active` with no demo-owned unlayered rule defeating it; N-3's `:active` deletion is its own sha with its own census. The AFTER witness is handed to KF.W9 by row id.

**G-KFW13-7 — close: the sweeps use the frontier's denominators, neither amends a dated authority; green demo project, unmoved head.** ⟨cmd⟩ `git grep -l 'btn-playback' HEAD -- demo | wc -l` → **8** · **8**; ⟨cmd⟩ `grep -rn 'focus-ring' demo | grep '\.vue:' | grep -v 'kf-focus-ring' | grep -v 'focus-ring-shadow' | grep -c 'class='` → **2** · **2**. GREEN when the receipt states **8** and **2** with these commands re-run at close, **quotes RULINGS-4's 7 and 4 beside them and amends neither** (E-3), routes the eighth file (`KeyframeTimeline.vue`, KF.W7's) and the two bare sites (KF.W11's) without editing them; ⟨cmd⟩ `npm run test:demo 2>&1 | tail -3` → every `test/demo/**` file passes (RED today by construction — five absent files); ⟨cmd⟩ `git diff <open-sha>..HEAD -- test | grep -c 'test.skip\|it.skip\|\.only('` → **0**; ⟨cmd⟩ `npx vue-tsc --noEmit -p tsconfig.json 2>&1 | grep -c 'error TS'` → **0** (OP-0 re-run at close); ⟨cmd⟩ `grep -c 'DISCHARGED by KF.W7 SWAP verdict' execution/B/KF-W13.md` → **0**; every producer row relayed and none cured demo-side.

**GREEN-BEFORE-CURE (R.2)** as KF-W11's — the partly-landed `:max` rider (OP-2) and the Space/keyup semantics (OP-6) are the first such bookings: `LANDED-BY <sha>`, never claimed.

---

## §Sequencing — locks and cross-edges

### Intra-wave order (binding)

1. **`.a` ∥ `.b` → `.c` → `.d`.** Peak 2.
2. **Inside `.a`**: G-KFW13-0's proof commit (no product byte) → M-4 + the rider as ONE sha → MM-1/MM-6's four-part edit → the 27-row family → TD-36 → the chrome roster. **No deletion before the proof** (L-1).
3. **Inside `.b`**: the KEEP verdict stated → OP-6's remainder derivation → the three TD one-commit families (they touch the host every later row paints on) → TD-37/39/34/35 → the C-2 + KF-CO-15 joint commit **after KF.W12 `.b` closes** (OP-2) → D-1/C-3/C-4 → the prop/token cures → one pressed authority → D-2 → hygiene (N-3 its own sha) → D-12's stopgap.
4. **`.c` after `.b`** (shared `playback-idiom.css`; the act repaints the focus affordance every earlier row was measured under), with the re-derivation receipt before the sha.
5. **Both packets after KF.W7's SWAP verdicts** — the KF-AV-28 lock; met (`4c03ceda`) and named.

### The travelling locks (register row 3 — four named there; all six accounted)

- **kf-ChromeDock M-4's MUST-CARRY MbabbMenu rider** — G-KFW13-1's second byte clause + its runtime clause; the self-hold and the deletion are ONE commit; *M-4's cure as worded ships an unopenable menu without it*. Its producer cause (bare `{ type: Boolean }`) rides SS-6 — the rider is the demo-side accommodation, not the fix, and the relay says so.
- **The TD-1/TD-2 bundle WHOLE** — G-KFW13-3/-4's bundle clauses; TD-1's *"cure with TD-4 + PRM + test, one commit"*; TD-2's *"one commit with TD-38/TD-40"*; TD-21+TD-41 likewise (W7 re-affirmed from its end). **A member already landed upstream does not dissolve the bundle for the rest.**
- **KF-AV-28's rider on the transport/ribbon surfaces** — met; PR-CAUTION binds the shape (cure ON the Slider); zero discharge receipts.
- **Glass-producer rows → SS-6, never demo-side hacks** — `dropdown-menu`'s Boolean-cast defaults; any Slider naming-seam gap; relayed at `.d`.
- **LP-1's write→render ordering** — consumed via OP-2 (KF.W12's); the C-2 joint commit waits on it.
- **Comment-stated invariants are test obligations** — `ChromeDock.vue:109-113`, `MbabbMenu.vue:184-188` and `App.vue:366-370` state the dock-hold mechanism; after M-4 they state the new one, and `mbabb-menu-self-hold.test.ts` asserts it; `usePlayActuation.ts`'s rationale becomes the TD-41 cases; D-20/D-25's five false comments are asserted in G-KFW13-5.

**Further locks this wave carries from its records**: **K-5's two-deletion lock** (G-KFW13-6); **ARB-1's delete-arm-only law** (shared with KF.W11's cube packet; both obey, neither re-books); **R3-1's ratified kill** (OP-8); **the sweeps re-derive, the authorities stand** (G-KFW13-7); **TD-36's one identity, one home** (`.a`).

### Cross-edges, declared from this end

| to | type | declaration |
|---|---|---|
| **KF.W4** | **DEPENDS ON (hard) — the sequencing head** | G-KFW4-1 GREEN before `.a`/`.b` open; measured RED (54 · 54). |
| **KF.W7** | **DEPENDS ON (met) — the SWAP verdicts; INBOUND ONLY** | six KEEP, zero SWAP (`4c03ceda`); PR-CAUTION carried at the header; W7's *"both packets are un-gated by a swap that did not happen"* and its three re-affirmed locks read at `.f`; `KeyframeTimeline.vue` is a KF.W7 surface and the eighth `btn-playback` file — counted, routed, never edited. This row is the reciprocal end W7 addressed to the §6.D register row. |
| **KF.W9 / SS-13** | **DEPENDS ON (witnesses, READ-ONLY there) + HANDS BACK** | G-KFW9-9's before-witness pair and its re-derivation obligation (OP-5 — offsets moved, one half changed shape; re-read at open); D-12's measurement; the forced-colors focus screenshot (D-3); the AFTER witness handed back by row id; OD-V5's 390 at-rest observation is KF.W9's and the owner's, never proxied here. |
| **KF.W6** | **DEPENDS ON (met)** | the `kf-focus-ring` rename (`a6418729`, KF-KE-30) re-anchored the counterpart and added its forced-colors parity; the ruling stands and is not re-litigated — the two-deletion act's re-derivation reads it. |
| **KF.W12** | **DEPENDS ON (`.b`'s C-2 joint commit only) + SHARES ONE PATH SERIALLY + DECLARES (census half)** | LP-1's edge (OP-2); the `ChannelOptions.vue` KF-CO-15 carve lands after W12 `.b` closes; `KeyframeCard.vue`'s `kf-focus-ring` at `:117` is the census's other half. Declared at both ends. |
| **KF.W11** | **SEAM (named both ends) + STATES THE POLICY** | KF-SS-8's ribbon half is the C-2 contract; the scene half is W11 `.c`'s; the spring packet is *"sequenced with the transport/ribbon packet"* (declared from both ends); **the two remaining bare `.focus-ring` sites are W11's files — this wave states the policy, W11 spends the edits**; **`useDragCapture.ts` is W11 `.i`'s write, `PlaybackRibbon.vue` its consumer here — re-measured at `.b`'s open**; ACG D-1's portal cure is W11's rider; ARB-1 shared. Neither blocks the other's open. |
| **KF.W10** | **REGISTERED AT** | `§6.D` row 3 — unchanged by this file; the LEDGER row's condition *"drag-seam/transport after KF.W7's SWAP verdicts"* is satisfied and stated. |
| **SS-6 (BH relay)** | **SUPPLIES** | the producer rows at `.d` — glass-ui's bare `{ type: Boolean }` declarations first. |
| **The owner / SS-2 design** | **DEPENDS ON (ruling)** | TD-37's one face order (OP-7). |

---

## §Excluded

| excluded | reason |
|---|---|
| **Any glass-ui / `node_modules` byte** — the Boolean-cast defaults, the Slider's touch gate, the dock's listener phases | producer's; SS-6 (lock 4). The MUST-CARRY rider accommodates the producer defect; it does not cure it, and says so. |
| **The `ComponentExposed<>` / `Pick<>` remedies for R3-1** | KILLED (ratified) — three concordant real-package probes vs one hand-mirror; they must not land (OP-8). |
| **The `headerLeft` 'fill' arm** | TRAPPED — ARB-1; DELETE only, until the KF-APP-5 producer relay lands. |
| **The drag-seam packet** (KF-SCR-1 + L·D-1 + KF-AV-15/-16) and `AnimationVisualizer.vue` / `useDragCapture.ts` | **homed at KF.W11** by the register (row 1, cargo 9 of 9) and R-15; W7's edge names it beside this wave's two packets because both share the grab-offset precedent — recorded as adjacency, never re-booked. |
| **`AnimationControlsGroup**` and ACG D-1's portal cure** | KF.W11 (the cube packet's C-15 limb rider). |
| **`KeyframeTimeline.vue`** — the frontier's eighth `btn-playback` file | KF.W7, CLOSED; counted in the sweep's denominator, routed, never edited (G-KFW13-7). |
| **`SpringTarget.vue:63` · `SquareScene.vue:46`** — the two bare `.focus-ring` sites | KF.W11's files; this wave sets the policy. |
| **`KeyframeCard.vue`'s `kf-focus-ring`** | KF.W12 — the census's other half. |
| **`DemoGlobalChrome.vue` · `SharePopover.vue` · `KeyboardShortcutsModal.vue` · `dock/index.ts` beyond the barrel carve** | their records route no row to either packet (⟨cmd⟩ `grep -l 'dock-menu\|transport repair packet\|transport/ribbon' registry/adjudicated/kf-*.md` → kf-AnimationControlsGroup · kf-AnimationVisualizer · kf-ControlsPaneWrapper · kf-MbabbMenu · kf-PlaybackRibbon · kf-SpringScene · kf-TransportDock — none of the three); the Opus arm's narrow rows are not carried. |
| **KF-APP-1/-17's same-motion lock** | homed in its authored wave; P1's MM-1/MM-6 arm consumes it. |
| **kf-ChromeDock D-18/C-1** (ADJUDICATED) | its cure spec carries TD-39's rider; the row's own home is as adjudicated — this wave lands TD-39 and names D-18 by id. |
| **OD-V5** (glass dock mark) and the SS-6 batch rows | owner + producer; never demo-side. |
| **The fifteen packets not in this partition** | R-15's partition; no re-booking. |
| **Amending RULINGS-4's `btn-playback` (7) and `.focus-ring` (4) figures** | E-3 — §B.1 rows 5–6 are frontier readings beside them, each with its command; the deleting seat re-runs both commands at its own clock and meets the list it measures. |
| **Emitting any `DISCHARGED by KF.W7 SWAP verdict <surface>, <date>` receipt** | KF.W7's set is EMPTY; this wave adds nothing to a set that has no members. |
| **Re-auditing any of the records** | the registry's — ADJUDICATED; ids for life; folds by reference. |

---

## §Commit plan

One commit per meaning; **the proof commit, the M-4+rider commit, the three TD families, the C-2 joint commit, N-3's deletion and the two-deletion act are each ONE sha**; pathspec on the commit itself; `Claude-Session:` trailer. Expected (keyframes.js):

1. `.a`: `test(kf/dock · X.KF.W13.a · G-KFW13-0 — useOptionalDockContext resolves the ChromeDock provider from slot content)` (**NO product byte**) · `refactor(kf/dock · X.KF.W13.a · M-4 round-trip deleted, MbabbMenu self-holds, the open binding KEPT (MM-28 rider), the :109-113 claim corrected, i-2 dies)` (**MUST NOT SPLIT**) · `fix(kf/dock · X.KF.W13.a · MM-1/MM-6 — repair :100, dispose setPPMode with KF-APP-17, C-14 split, MM-5 CheckboxItem; delete arm only)` (**four parts, ONE edit**) · `fix(kf/dock · X.KF.W13.a · the actuation/typography/brand roster (27 rows))` · `fix(kf/dock · X.KF.W13.a · TD-36 — the ChromeDock pointer-events pair)` · `fix(kf/dock · X.KF.W13.a · ChromeDock roster <family>)` ×n · `test(kf/dock · X.KF.W13.a · G-KFW13-1)`.
2. `.b`: `docs(x-kf-w13/b): the TD keyboard-family remainder, derived against landed code` (value.js; **precedes the next**) · `fix(kf/transport · X.KF.W13.b · TD-2+TD-38+TD-40 one propagation policy, both mirrors + composition tests)` (**MUST NOT SPLIT**) · `fix(kf/transport · X.KF.W13.b · TD-21+TD-41 actuation cancellation — orphan-keyup + stale-id, RED run pasted)` (**MUST NOT SPLIT**) · `fix(kf/transport · X.KF.W13.b · TD-1+TD-4 icon spin + PRM + test)` (**one commit**) · `fix(… TD-37 one face order)` (if ruled) · `fix(… TD-39 + TD-34/35)` · `fix(kf/ribbon · X.KF.W13.b · C-2 time-space contract + KF-CO-15 extension — scale and space land together)` (**JOINT, MUST NOT SPLIT**) · `fix(kf/ribbon · X.KF.W13.b · D-1+C-3+C-4 name/valueCommit/step on the Slider)` · `fix(… D-5/D-6/D-10/L-M1 + one pressed authority)` · `fix(… D-2 visible playhead)` · `chore(… hygiene sweep + the five-false-comments prose pass)` · `fix(kf/ribbon · X.KF.W13.b · N-3 — the doubled press affordance deleted, census pasted)` · `fix(… D-12 stopgap)` · `test(… G-KFW13-5)`.
3. `.c`: `docs(x-kf-w13/c): G-KFW9-9's ground re-derived at the frontier — both selectors` (value.js; **precedes the act**) · `fix(kf/idiom · X.KF.W13.c · the two-deletion act (G-KFW9-9, K-5) — both stylesheets, one sha)` (**BOTH FILES, ONE COMMIT**) · `test(… the focus-affordance case)`.
4. value.js: `docs(x-kf-w13/<unit>): receipt` per unit; `docs(x-execution/ledger): KF.W13 row`; SS-6 accretion + INBOX append at `.d`; `evidence/W13/**`.

## §Format and lint cadence · §Verification artefacts

`npm run check` + `npm run test:demo` after each unit's last commit and at close; `npx eslint demo/app demo/components/instrument/transport demo/components/playback demo/styles` at close; `git diff --check` per commit. Artefacts at close in `execution/B/KF-W13.md` and `evidence/W13/**`: the eight gate transcripts (double-run), the mounted self-hold proof's output, the commit roster with `git show --stat` proving each family unsplit, OP-3's dist measurement, OP-5's re-derivation receipt and the offsets at open, OP-6's remainder derivation, the propagation-policy matrix, the sweep denominators beside RULINGS-4's, the four SS-13-keyed witnesses, the SS-6 entries' ids, the E13 line.

## §L-18 rider (reference, not ceremony)

Passing all eight gates makes X.KF.W13 **IMPLEMENTED**; it never makes it **ACCEPTED**. The implementation must first survive two challenging gestalt passes and a fresh-Fable agglomeration. Likely bases: (i) M-4 executed as worded — the round-trip AND the local binding deleted — a menu that never opens, invisible to a byte-only check (G-KFW13-1's runtime clause and the MUST-CARRY byte clause exist for this); (ii) a deletion landed on a reading rather than an execution (G-KFW13-0's class — and the register's own withdrawn superlative is the precedent); (iii) the MUST-CARRY rider landed as a follow-up commit (one commit's width of shipped regression); (iv) TD-2 landed without TD-40's registry scoping, or TD-21 without TD-41's blur disarm, or a bundle split because one member was already landed upstream (OP-6 makes it likely); (v) a "swap onto the primitive" cure for the ribbon (PR-CAUTION forbids it by name); (vi) one stylesheet deletion without its counterpart, or the counterpart deleted without re-deriving KF.W6's ruling (K-5 / OP-5); (vii) a sweep run on RULINGS-4's file list rather than the frontier's (7 vs 8, 4 vs 2); (viii) a "types fixed" commit re-curing R3-1's killed defect; (ix) a producer selector copied into `TransportDock.vue` to reach the dock's internals (a HIGH defect by standing law).

**Dissents recorded, not resolved by this seat**: (1) whether kf-ChromeDock's full NO-WAVE-OWNER roster (beyond M-4 and TD-36) belongs to this packet or to a KF.W6 successor round — the register names the packet *dock-menu* with M-4 as its head and this file carries the roster by id under it so nothing is dropped (M-25); the Opus arm bounded the packet to M-4 + the MbabbMenu family + TD-36, and a later seat may narrow it by ruling, never by silence. (2) whether the drag-seam packet, which W7's edge addresses beside this wave, should move here from KF.W11 — this file follows the register's partition and records the adjacency. (3) whether the mechanism proof deserves its own seat (the Opus arm's `.a` design act) rather than `.a`'s first commit — this file makes it the first commit under a no-deletion-before-it lock, which is the same law with one fewer seat.

---

## §Provenance — the agglomeration record (M-23 §3 / M-25)

**Fold seat**: a fresh Fable instance that authored neither draft; both drafts read whole; every disagreement resolved at the register's bytes and the live tree at `69095552`.

**Structure taken from the Fable arm**: the header and verb table, §State, the OP table's shape (OP-0..OP-7 re-cut), the vitest-based gate form with byte clauses beside runtime runs, the MUST-CARRY byte clause (`v-model:open="open"` must STILL read 1), the G-KFW9-9 offsets measured (`:74-77`, `:106`), the six-lock accounting with the four register locks named, the cross-edge and §Excluded tables, §Commit plan by unit, §L-18, kf-ChromeDock's roster carried under dock-menu (with the dissent). **Taken from the Opus arm**: the mechanism proof before any deletion (G-KFW13-0 — its `useOptionalDockContext` finding reproduces: the sole demo hit is the false comment), the `:109-113` comment limb, the full seven-line M-4 grep (`mbabbPopupOpen` included), `i-2` named as dying, OP-8's R3-1 kill and the `ComponentExposed`/`Pick<` clause, OP-6's remainder derivation (the Space/keyup semantics landed in `usePlayActuation.ts`), TD-36 homed by its bytes in ChromeDock (one identity, one home), the `.stop` 5/0 asymmetry command, the sweep-denominator gate with RULINGS-4's figures quoted beside (G-KFW13-7), the parallel `.a` ∥ `.b` shape with the two-deletion act as its own serial unit, the B.1 re-anchor table as the bounds' first section, the LAW A census block, `font-roles.json`'s conditional row, the `evidence/W13/**` path, the transport-dock composables' line counts.

**Disagreements, and the byte that settled each**: (a) **the two-deletion act's counterpart** — the Opus arm read *"the copy is gone"* from `grep -c 'btn-playback' design-idioms.css` → 0; KF-W9 §Carry D-3+DU-M-1 defines the pair as `.focus-ring:focus-visible` (design-idioms) + `.btn-playback:focus-visible` (playback-idiom) — the counterpart was never a `btn-playback` rule, and the tree reads it renamed at `design-idioms.css:106` with a forced-colors block at `:111-116` (this seat's measurement, in neither draft); the Fable arm's "both offsets moved" holds and OP-5 now carries the shape change and KF-W9's own re-derivation obligation. (b) **TD-36's home** — Fable put it in the transport unit (a comment hit at `TransportDock.vue:299`); the bytes are `ChromeDock.vue:299`/`:302`; homed at `.a`. (c) **M-4's site count** — 5 (Fable's two-token grep) vs 7 (Opus's three-token grep): both commands reproduce; the spec carries the wider command with its 5-code/2-prose partition. (d) **execution shape** — serial ×3 (Fable) vs 4 seats with a separate design act (Opus): the disjointness measured at (b) permits `.a` ∥ `.b`, the proof becomes `.a`'s first commit under a lock, the act gets its own serial unit. (e) **`transport-play-actuation.test.ts`'s case count** — "eight" (Fable) vs the command: ⟨cmd⟩ `grep -c 'it(' …` → **10**; the spec prints the command's figure. (f) **N-3's identity** — Opus labelled it kf-SequenceScene's in `PlaybackRibbon.vue`; `kf-PlaybackRibbon.md:64` owns it, bytes `playback-idiom.css:76-78` banked → `:78-80` today. (g) **DemoGlobalChrome / SharePopover / KeyboardShortcutsModal bounds** (Opus) — no record routes a row to either packet; excluded with the census command. (h) **write authority (Opus OP-4)** — not carried; settled by the landed X·KF roster.

**Killed from the Opus arm**: the separate `.a` design-act seat (folded into `.a`'s proof commit + lock); the "copy is gone / single-file act" reading; the three narrow chrome-file rows; the `kf-App`/`kf-DemoGlobalChrome`/`kf-SharePopover`/`kf-KeyboardShortcutsModal` record set as homes (kf-App is kept as the KF-APP-1/-17 motion's source only). **Killed from the Fable arm**: TD-36 in `.b`; the "eight cases" figure; the serial-only shape; the AUDITED cell's implication that four records are the whole record set (kf-App added for the motion). **Blind convergences banked**: both arms produced the same two packet names, the same KEEP reading, the same MUST-CARRY reading of `MbabbMenu.vue:4`/`:204`, the same three TD bundle locks, the same 8 / 2 sweep denominators beside RULINGS-4's 7 / 4, the same `:max` partly-landed observation, the same PR-CAUTION posture, and the same C-2 + KF-CO-15 joint-commit dependency on KF.W12 `.b`.

---

**ADDENDUM 2026-09-20 (dated, beside — E-3; COHESION §0ai). The supplement wave `KF.W13S`.** **`KF.W13.a2`** (Opus) — writable: every `.a` row of §B.2 (`ChromeDock.vue` · `MbabbMenu.vue` · `dock/index.ts` carve · `App.vue` carve · the two `test/demo/app/*.test.ts` creates) **plus `vitest.config.ts` — one row: `test.server.deps.inline: ["@mkbabb/glass-ui"]`** (KF13-E2's cure; the Do-NOT-touch entry is lifted for that key alone). Sub-gate: G-KFW13-0 → -1 → -2 GREEN in `git log` order. **`KF.W13.e`** (Opus) — writable: `demo/styles/playback-idiom.css` (the six E-b1 halves: D-10 `:30-33` · D-13 `:23` · D-16 `:19` · N-1 `:33/:48/:69/:88` · D-8/D-15 `:87-90` · D-20 `:12-13`) · `TransportDock.vue` (TD-37 under OP-7's ruling: play leading on both faces; TD-21's rider) · `test/demo/instrument/channel-options-render-edge.test.ts` (C-11's one `vi.mock`) · `test/demo/instrument/css-code-editor-seam.test.ts` (the preset double gains `setTargets`) · `demo/env.d.ts` (the five-line ambient declaration) · `demo/components/instrument/keyframes/CSSCodeEditor.vue` (KF-CE-1/4 arm (b), byte-exact from `evidence/W12/KF-W12-d-born-red.md`) · `src/animation/engine/compositor.ts` · `src/animation/waapi.ts` · `src/animation/smooth.ts` (§Excluded lifted for the three TS6133 declarations only) · `test/demo/scenes/spring-trace-truth.test.ts` (the (4b) re-bind) · `demo/scenes/easing/useEasingDemo.ts` · `EasingSidebar.vue` · `EasingTarget.vue` (KF11-E3's typed seam, no cast) · `demo/components/editor/EditorShell.vue` `:175` only (`:key="superKey ?? ''"`) · `keyframes/utils/parseAnimationCSS.ts` (E-c1, type-level). Paths are named by their §B.2 / KF-W11 / KF-W12 spellings; a seat that finds a path drifted records INTENT at the true bytes. **`KF.W13.f`** (Opus, serial last) — the value.js rows (`execution/B/KF-W13S.md` · `LEDGER.md` cells · `INBOX.md` + SS-6 append · `evidence/W13/**`): `vue-tsc` **0** · `npm run test:demo` green · `npm run check` exit 0 · keyframes.js pushed; value.js pushed only when no sibling path is staged. **M-1 ratified beside**: TD-17's `useMenubarMeasure.ts` landing stands. Gates of record for the supplement: G-KFW13-0 · -1 · -2 · -7 (all four honest-REDs), plus KF.W12's G-KFW12-4 · -7 and KF.W11's G-KFW11-4 · -10 re-read at `.f`'s close.

---

**ADDENDUM 2026-09-22 (dated, beside — E-3; COHESION §0am). KF.W13S resumes on three grants.** **`KF.W13.a4`** (Opus) — writable: `demo/app/dock/MbabbMenu.vue` · `demo/scenes/cube/CubeScene.vue` `:96-97` `:129-135` `:267` + their dead imports ONLY · MM-5's `CheckboxItem` site · the record; acts: the MM-1/MM-6 four-part cure as ONE sha (ARB-1), no `headerLeft` fill arm, `sceneExposedApi.ts:43` untouched; gates: G-KFW13-2 roster spent, `MbabbMenu.vue:333` TS2339 ×2 → 0, `cube-scene.test.ts` green; push. **`KF.W13.e3`** (Opus, after `.a4`) — writable: `demo/utils/reference-data/easingGroups.ts` · `demo/utils/reference-data/animationDescriptions.ts` · `demo/components/**/EasingSidebar.vue` `:150` · `EasingTarget.vue` `:251` (consumer ends only if the catalogue typing demands) · `package.json` line 37 ONLY (leg 2 → `vue-tsc --noEmit -p tsconfig.test.json`) · `test/demo/instrument/playback-ribbon-contract.test.ts:230` · the record; gates: `npx vue-tsc --noEmit -p tsconfig.json` → **0** ×2; leg 2 row count banked BEFORE/AFTER (47 → 33 → n); push. **`KF.W13.t`** (Opus, after `.e3`) — writable: `test/demo/**` + the demo module a row's root sits in ONLY where the root is a type declaration; acts: the residual leg-2 rows (the 29 across 12 files, enumerated BEFORE from `npx vue-tsc --noEmit -p tsconfig.test.json 2>&1 | grep 'error TS'`), typed root cures only — never a cast, `@ts-expect-error`, `skip` or a loosened assertion; any row rooted in product behaviour outside the carve RETURNED by `file:line`; gates: leg 2 → **0** ×2, `npm run check` exit 0 ×2, `npm run test:demo` green ×2; push. **`KF.W13.f2`** (Opus, last) — as §0ai's `.f`: the close literal (`vue-tsc 0` · `test:demo` green · `check` exit 0), the R-f2-5 producer asks relayed to glass-ui BK (mail only), the SS-6 accretion, keyframes.js pushed, the LEDGER row; value.js pushed only if the shared index carries no sibling's staged path.

---

**ADDENDUM 2026-09-22 (dated, beside — E-3; COHESION §0ao). Wave `KF.W13T` — OA-6, the dock does not contain its elements.** Opens after KF.W13S CLOSED. **`KF.W13.k`** (Opus) — writable: `demo/app/dock/ChromeDock.vue` · `demo/app/dock/MbabbMenu.vue` · `demo/app/App.vue` (the dock's consumer end only) · `demo/styles/**` · `test/demo/app/**` (witness) · the record. Acts: measure in a live browser at 3 viewports every ChromeDock child's bounding box against the `GlassDock` capsule box (Playwright, bounded, screenshots under `keyframes/evidence/W13T/`); every overflow named; cured at the root — the layout (the dock's own axis, `DockLayerGroup` sizing, a child sized in px instead of the dock's control floor), never `overflow: hidden` as a mask; a producer defect (the capsule itself) → BK relay. Gate **G-KFW13T-1**: 0 children outside the capsule at 3 viewports ×2, witness committed; **G-KFW13T-2**: vue-tsc 0 · `test:demo` green · push. **`KF.W13.k2`** (Opus, verify-only close) — the LEDGER row, E13, push.

**ADDENDUM 2026-09-22 (second, beside — E-3; COHESION §0ao.1). `KF.W13T` gains unit `.e`; order [`.k`] → [`.e`] → [`.k2`].** **`KF.W13.e`** (Opus) — writable: `demo/scenes/easing/**` · `demo/components/instrument/transport/channel-controls/**` (the easing picker seat) · `demo/components/instrument/timeline/**` · `demo/components/playback/**` · `demo/composables/useDragScrub.ts` · `demo/utils/reference-data/easingGroups.ts` (glyph data only if the catalogue is the glyph's source) · `test/demo/**` (witnesses) · the record. Acts, each measured in a live browser at the dev server BEFORE (the owner's screenshots reproduce) and AFTER: **OA-7** every easing row in the picker renders its curve glyph (an SVG path from the easing function itself — the same function the animation runs — never a hard-coded sprite), sized to the row; **OA-8** the timeline slider (locate: the control the owner dragged — `PlaybackRibbon.vue` / `KeyframeTimeline.vue` / `SequenceScrubber.vue`) drags by pointer AND keyboard, on glass-ui's slider primitive with the producer's track/thumb styling — the raw purple thumb and bare dark track are gone; the pointer path (`useDragScrub.ts`) is bound at the root, not patched with a second listener; **OA-9** the animations RUN: name the clock/driver that is not starting (the transport's play actuation, a `requestAnimationFrame` never scheduled, a `paused` state never cleared) and cure it there; a witness that plays for 500 ms and asserts progress > 0; **OA-10** a small inline toggle beside the easing-ball preview (producer toggle/switch primitive, `aria-pressed`, accessible name) that hides the preview; persisted with the scene's view state. Gates: **G-KFW13T-3** picker rows with a rendered `<svg><path` glyph = rows, ×2; **G-KFW13T-4** slider `aria-valuenow` changes under a pointer drag and ArrowRight, screenshots before/after; **G-KFW13T-5** playback witness green ×2; **G-KFW13T-6** toggle hides/shows the preview (`hidden`/absent from the a11y tree), persisted across reload; vue-tsc 0 · `test:demo` green · push. Producer gaps (a slider or toggle capability glass-ui lacks) → BK relay, never a local control.

**ADDENDUM 2026-09-22 (third, beside — E-3; COHESION §0ap). KF.W13S resumes on `.t2`; KF.W13T `.k`/`.k2` widened.** **`KF.W13.t2`** (Opus) — writable: the nine library test files §0ap names · the record; acts: the 10 unused bindings deleted; the 6 invalid-easing rows re-expressed per §0ap's idiom ((i) `unknown` ingress, else (ii) `@ts-expect-error` with reason), each recorded as (i)/(ii); gates: `npx vue-tsc --noEmit -p tsconfig.test.json` → **0** ×2 · `npm run check` exit **0** ×2 · `npm test` (library) green ×2 · `test:demo` green ×2; push. **Lint erratum**: this file's §Verification eslint command is read WITHOUT the `demo/styles` glob. **`KF.W13T.k`** additionally owns R-a4-1 (`App.vue:35` + the `superKey` prop declaration in `MbabbMenu.vue`) and R-a4-2 (`demo/scenes/cube/CubeScene.vue`, the hover-card spans §0ap enumerates, nothing else). **`KF.W13T.k2`** additionally owns the 7 pre-existing eslint errors in the three files the lint line reports (granted; cure-or-return by `file:line`).

**ADDENDUM 2026-09-22 (fourth, beside — E-3; COHESION §0ar). KF.W13T resumes: [`.e2`] → [`.k3`] → [`.k4`].** **`KF.W13.e2`** (Opus) — writable: `demo/state/controlOptionsStore.ts` (the one optional field `easingPreview?: "shown" | "hidden"` beside `ppMode?`) · `demo/scenes/easing/EasingScene.vue` · `demo/components/playback/**` (R-e-2, the paused-scrub twin's repaint trigger) · `test/demo/**` · the record; gates: **G-KFW13T-6** whole (hide/show ×2 AND persisted across reload ×2, Playwright witness) · R-e-2 witness green ×2 · vue-tsc 0 · `npm run check` exit 0 · push. **`KF.W13.k3`** (Opus) — writable: `demo/components/instrument/shell/EditorShell.vue` · `demo/app/dock/ChromeDock.vue` · `demo/components/instrument/transport/AnimationControlsGroup.vue` · `…/channel-controls/ChannelOptions.vue` · `…/channel-controls/TimingFunctionPanel.vue` · `…/controls-pane/ControlsPaneWrapper.vue` · `test/demo/**` · the record; acts: (1) R-k-1 — the ribbon's controls (Share · Keyboard shortcuts · theme toggle) ride the ChromeDock as a group/layer on the producer's dock primitives, the ribbon retired at every viewport, names and the `?` shortcut intact; (2) ESC-k2-1 — the six `vue/no-mutating-props` rows cured at the state's owner via `defineModel`/`emit`; gates: **G-KFW13T-7** 0 dock/ribbon collisions at 390×844 · 768 · 1440 ×2 (screenshots) · the §Verification eslint line (without `demo/styles`) → **0** ×2 · `test:demo` green ×2 · push. **`KF.W13.k4`** (Opus, verify-only close) — every G-KFW13T gate re-run ×2, bounds, E13, the LEDGER row, push; value.js pushed if fast-forward.

## ADDENDUM 2026-09-23 — KF.W13U: the owner's third keyframes docket (COHESION §0be). E-3: the bytes above are untouched.

**Opens after**: KF.W13T CLOSED. **Model**: Opus 5.5 every seat (owner 2026-09-23). **Record**: `docs/tranches/X/execution/B/KF-W13U.md`. **Frames**: `docs/tranches/X/keyframes/evidence/W13U/owner-2026-09-23-{easing-label,timeline-grey}.png` (READ both).

**Authority — the owner's words, verbatim (2026-09-23)**: *"Dock animations and transitions for keyframes.js are also awful, blurry, janky, and jittery. The cube doesnt' animate, none of the animationsa re wired up [frame easing-label] the easing picker doesn't diplsay the current curve in the preview/header label or in the dropdown [frame timeline-grey]--the timeline is always greyed out; the animations are all entirely broken"*

**The instrument rule (why this wave exists).** KF.W13T closed CONFORMANT claiming curves 29/29, a draggable slider and running animations; the owner's live surface shows the opposite. Every gate in this wave is therefore read on the SERVED dev page (`http://localhost:5173/`, the owner's surface) in a HEADED real-GPU browser, plus the gh-pages build — a harness-only GREEN does not count. Each unit first reproduces the owner's observation on that surface and pastes it; if it cannot reproduce, it says so with frames and asks via ESCALATED rather than claiming GREEN.

Units, strictly serial:
- **`.w` (Opus, effort high) — wiring**: why "none of the animations are wired up": trace from the demo's play/transport to the library's animation instances; the cube (the demo's hero/preview) must animate on load and on play. Name the break at the bytes (a store not connected, a disabled flag, a paused timeline at boot, a PRM default, a dist/source split serving a stale library) and cure at its owner. Gate: on the served page, the cube's transform changes across 1 s at rest AND after play ×2 (frame diff + `getAnimations()`/rAF sample); every animation in the demo's list plays.
- **`.t` (Opus) — the timeline**: the timeline is "always greyed out": find the disabled/inert state's cause (no active animation selected, a `disabled` derived from a broken wiring, an opacity token) and cure; the slider drags and scrubs the cube. Gate: served page, slider enabled, drag moves the playhead and the cube ×2.
- **`.e` (Opus) — the easing label + dropdown**: the trigger shows "ease-in-outslow start & end" (label and description concatenated, no curve); the trigger/header shows the CURRENT curve's glyph and a separated name, and each dropdown row shows its curve. Gate: served page, trigger contains an SVG path matching the selected easing; dropdown rows each render a path; name and description visually separated ×2.
- **`.d` (Opus, effort high) — the dock motion**: "awful, blurry, janky, and jittery": measure blur (a `filter: blur`/backdrop re-raster or a scale on text during the morph), jitter (layout shift, sub-pixel transforms, double owners) on the served page; cure consumer causes at the root; producer causes ride O-55/O-56 (glass BL is folding them; honest-RED id `DOCK-MORPH-ROOT`) — no consumer copy. Gate: a WebM of each dock transition before/after; 0 elements with two animation owners per property; text not blurred at rest after a morph (pixel-sharp crop).
Close: `npm run check` exit 0, vitest GREEN, the kf e2e suite GREEN, AND the four served-page gates re-read by the check seat itself.

**ADDENDUM 2026-09-23 (KF.W13U, second; COHESION §0bg) — OA-31 sharpens `.e`.** Owner, verbatim: *"The easing should not say "slow start and end" inline like that in the displayed label--and the curve preview should be in the dropdown and the label, too."* So the easing trigger (the displayed label) shows the curve glyph + the easing NAME only — the description ("slow start & end") never renders inline in the trigger; it may appear in the dropdown rows as secondary text or as a tooltip. Every dropdown row AND the trigger render the curve preview. Gate `.e` (served page, headed ×2): the trigger's text content equals the easing name exactly (no description substring); the trigger and each row contain a curve path matching its easing.

**ADDENDUM 2026-09-23 (KF.W13U, third; COHESION §0bh) — OA-32 joins `.d`.** Owner, verbatim: *"the small dock icons for the chosen animations should animate in a small way, reflective of the ACTUAL animation thereof (and the stacking, too) whilst being bounded and small."* The scene icons (`assets/icons/{cube,amiga,square,easing,spring,…}.svg`, bound on each descriptor in `demo/app/scene/scenes.ts`, rendered by `ChromeDock.vue` as `<component :is="scene.icon">`) become miniature live renditions: each icon's motion is DERIVED from its scene's real animation definition (the same keyframes/easing/spring/sequence data, driven by the keyframes.js library — never a hand-drawn look-alike loop), with the scene's layer stacking preserved in miniature (Amiga: ball over shadow over grid). Bounded: the motion stays inside the icon box (no overflow, no layout shift), small amplitude, at the icon's size; the active/chosen scene's icon animates (others at rest or on hover — record the choice); PRM → static. Gate (served page, headed ×2): the chosen scene's icon frame-diffs > 0 across 1 s, its layers are in the scene's order, its bounding box is unchanged ±0.5 px, 0 pixels outside the icon box; PRM context → frame-diff 0.

**ADDENDUM 2026-09-23 (KF.W13U, fourth; COHESION §0bi) — OA-33 joins `.d`.** Owner, verbatim (frame `keyframes/evidence/W13U/owner-2026-09-23-dock-mbabb.png`): *"this should likely be one icon in the dock, just the mbabb logo, and then have a dropdown like before--it takes up too much space"*. The dock's trailing utility zone (Share · Keyboard shortcuts · theme toggle, `ChromeDock.vue` ≈:565-598) collapses into ONE dock control: the mbabb logo, which opens a dropdown (the existing `MbabbMenu.vue` family — "like before"; read its history) holding Share, Keyboard shortcuts (with the `?` hint) and the theme toggle, on glass's dock trigger + menu primitives. Every action keeps working (share popover opens, shortcuts modal opens, theme flips) and keyboard reachable. Gate (served page, headed ×2): the trailing zone renders exactly one control; the dock's width drops by the two removed controls' footprint; each of the three actions works from the dropdown; Escape closes and returns focus to the logo.

**ADDENDUM 2026-09-23 (KF.W13U, fifth; COHESION §0bj) — OA-34 joins `.e` (same pane).** Owner, verbatim (frame `keyframes/evidence/W13U/owner-2026-09-23-shadow-clip.png`): *"and the shadows on the left clip and are not displayed properly"*. The controls card's drop shadow is cut by a hard vertical edge on its left and its bottom-left corner is squared — an ancestor's clip (`overflow: hidden|auto|clip`, `contain: paint`, a scroll container or a mask) cuts the shadow. Find the clipping ancestor at the bytes and cure at the owner: the shadow lives inside the scroller's padding / a `overflow-clip-margin` that covers the shadow extent, or the clip moves to the layer that needs it — never delete the shadow, never a per-card hack. Sweep every card/panel in the demo for the same clip (both themes). Gate (served page, headed ×2, light and dark): for each card, a clipped screenshot 24 px beyond its box shows the shadow continuous on all four sides with rounded corners (no column of pixels where the shadow alpha drops to 0 at the ancestor's edge).

## ADDENDUM 2026-09-23 — KF.W13V: one idiom for every animation view + the two audit registers (COHESION §0bl). E-3: the bytes above are untouched.

**Opens after**: KF.W13U CLOSED and both registers landed — `docs/tranches/X/keyframes/audit/KF-ANIMATION-AUDIT.md` (OA-30, `KFA-n`) and `docs/tranches/X/audit/UI-AUDIT-keyframes.md` (OA-36, `UIA-KF-n`). **Model**: Opus 5.5 every seat. **Record**: `docs/tranches/X/execution/B/KF-W13V.md`. **Frame**: `keyframes/evidence/W13U/owner-2026-09-23-easing-page.png` (the Spring scene).

**Authority — the owner's words, verbatim (2026-09-23)**: *"this entire UI is god awful, not glass-ui idiomatic, cluttered, and too rounded in some pills. A full UI audit of every page should be done, too. The smooth and bouncy pills, for example are too rounded and should be more card like--mark this and route all glass-ui changes, too to the glass-ui session and agent thereof, to be fixed at the root. And for example, why does this page have an inline keyframes editior and NOT properly leverage our idiom of the cube, amiga, etc of having the keyframes pane separate--ensure that we have cohesion between all animation views, too, and NO one-off instances such as this."*

Units, strictly serial:
- **`.s` (Opus, effort high) — one scene idiom (OA-37).** Census every scene (`demo/scenes/{cube,amiga,square,easing,spring,sequence}` + Home): which shell regions it uses (stage, controls pane, the SEPARATE keyframes pane, transport), and every one-off (the Spring scene's inline `@keyframes (editable)` block via `useSpringKeyframesEditor.ts`; the Sequence scene's own editor paths; any scene-private controls chrome). The canonical idiom is Cube/Amiga's: the stage in the centre, scene controls in the controls pane, the keyframes in the shared keyframes pane (`components/instrument/keyframes/*`), one transport. Every scene is brought onto that idiom through the shared shell descriptors — scene-specific facets (the spring heatmap, the linear() trace) become that scene's controls-pane content or stage content, never a second editor. Gate (served page, headed ×2): for each scene, exactly one keyframes editor exists and it is the shared pane; 0 scene-local keyframes editors (grep + DOM); every scene's regions match the census table's canonical row.
- **`.p` (Opus) — the tile shapes (OA-35).** The Smooth/Snappy/Bouncy/Gentle preset tiles and every multi-line selectable tile in the demo compose the primitive glass rules for it (O-58, sent live to `glass-ui-9d`); until glass answers, the tiles use glass's existing card/surface radius token (never `rounded-full` on a multi-line tile) — a consumer re-point to a producer token, not a new style. Gate: every multi-line tile's radius ≤ the card token; 0 `rounded-full`/`9999px` on elements taller than one line.
- **`.k` (Opus, effort high) — the animation register.** Every `KFA-n` row not already cured by KF.W13U: cured at its cause, or honest-RED by id with owner (glass rows → the batched glass letter). Each cure re-captured frame by frame with the audit's own script.
- **`.u` (Opus, effort high) — the UI register.** Every `UIA-KF-n` row: cured (declutter, hierarchy, spacing, glass primitives) or routed to glass by id.
Close: the served-page gates re-read by the check seat; `npm run check` 0; vitest + e2e GREEN.

**ADDENDUM 2026-09-23 (KF.W13V, second; COHESION §0bl) — `.p` pinned by glass's canon.** glass-ui (session `glass-ui-9d`, filed O-58 at glass `d29be90e`): the radius role table (`glass-ui/DESIGN.md:385-391`) keeps the stadium for SINGLE-LINE controls only (`--radius-control`, `--radius-tab`, `--radius-badge`); multi-line holders sit on the 16 px card rung **`--radius-field`** (`DESIGN.md:386`). So `.p` puts every multi-line selectable tile on `--radius-field` now (a canon re-point, not a local style); WHICH primitive owns a selectable multi-line tile (a card item on RadioGroup/ToggleGroup, or a selectable Card/Surface) is BL's ruling — `.p` adopts it if installable at its sitting, else records `TILE-PRIMITIVE` honest-RED with O-58.

**ADDENDUM 2026-09-23 (KF.W13V, third; COHESION §0bn).** `.k` consumes `keyframes/audit/KF-ANIMATION-AUDIT.md` (228 rows): consumer rows cured at cause (KFA-1/-2 cube writers, KFA-3 sequence scale×translate, KFA-12 the view-transition receiver, KFA-17 seek→resume, KFA-18/-181 Amiga curves + loop overshoot, …); glass rows (O-60) adopted when installable, else honest-RED by id; KFA-14 lands through value.js X-W12 `.l` (keyframes bumps value.js to consume it). `.k` also captures and judges the critic's gaps: matrix-editor cell/reset tweens, toasts, tooltips, the tab-panel `@keyframes enter` slide, a 6-scene × {Play, Pause, Reset, Reverse, scrub} transport matrix with a frame per cell, dark-mode legs of the cube re-light and Amiga grid/shadow, the mobile Drawer.

**ADDENDUM 2026-09-23 (KF.W13V, fourth; COHESION §0bq) — OA-39 joins `.s`.** Owner, verbatim (frame `keyframes/evidence/W13U/owner-2026-09-23-mobile-landing.png`, ≈390 px wide, dark): *"Mobile ladning page is not centred properly"*. Measured on the frame: the cube's centre sits ≈65 % down the viewport, not in the stage's centre, and the cube overlaps the "Select an animation…" headline and its sub-line, which are left-aligned beside it; the home control and transport are centred. The landing (Home + the idle cube stage) at phone widths composes as one centred column: the stage (cube) centred in the space between the top dock and the transport, the headline block centred and NOT overlapping the cube (stacked above or below it by the layout, never absolutely positioned over it), both in the shared scene shell `.s` establishes. Gate (served page, headed ×2, 390×844 and 360×740, light and dark): the cube's bounding-box centre within ±4 px of the stage region's centre on both axes; headline box ∩ cube box = ∅; headline horizontally centred ±2 px; the same at 1440×900 unchanged from the census's canonical row.

**ADDENDUM 2026-09-23 (KF.W13U, sixth; COHESION §0br) — the supplement units.** `.w` `.t` `.e` landed and passed on the served page; `.d` partial. Order [`.t2`] → [`.d2`] → [`.d3`] → [`.d4`] → [`.x`], all Opus:
- **`.t2`** (R-t-1): retire the unread `isAnimStarted` prop — the declaration in the ribbon AND its three mounts (`ChannelOptions.vue:586`, `EasingScene.vue:114`, `SpringScene.vue:196`) in one commit. Gate: grep 0; vue-tsc 0.
- **`.d2`** (OA-32, ESC-d-2): grant `demo/app/scene/scenes.ts` (the descriptor's icon binding stays the ONE binding) + a mini-rendition seam in each `demo/scenes/*/` + `demo/app/dock/ChromeDock.vue`. Each scene exports its miniature from its own animation data through the keyframes.js library; no dock-local registry. Gate: §0bh's OA-32 gate, headed ×2.
- **`.d3`** (ESC-d-1): grant `demo/app/App.vue` script `:266-276` + `demo/state/controlSurfaces.ts`: the scene machine is not fed `surfacesFor(undefined)` while a non-home scene is pending — dock and pane change ONCE when the swap resolves. Gate: across 10 scene switches, the dock width and the controls-pane surface set change once per switch (sampled every frame), 0 empty-surface frames, headed ×2.
- **`.d4`** (ESC-d-3): grant `demo/components/instrument/shell/SharePopover.vue` (expose its open model) + `demo/app/dock/MbabbMenu.vue`: Enter on the Share row opens Share. The theme row's keyboard actuation is producer-owned (`DARK-MENU-ITEM`, O-61 R-3) — adopt it if installed glass has it, else honest-RED by id; never a second `useGlobalDark` actuation (EH-4).
- **`.x`** (ESC-R2-2, the kf e2e close clause): grant `scripts/observe/demo/{subject-animates,live-session}.mjs` — re-seat the two oracles on the cured bytes (the cube's nested targets, the live timeline) keeping their properties, and cure `usability · S4 · S5 · M1` at their causes. **Instrument condition**: the host has read load averages of 575–707 during this wave (four workflows); e2e gates are read at `--workers=1` and the load average is recorded beside each run; a failure that passes ×2 at `--workers=1` and whose trace shows a timeout before any assertion is the load family, not a RED.
Honest-RED ids carried: `DOCK-MORPH-ROOT` (O-56), `DOCK-SCROLL-MORPH` (O-55), `GLASS-SURFACE-PAINT-CONTAIN` + `KF-TIMELINE-FILL` + `DARK-MENU-ITEM` (O-61). The intermittent `var(--rotationX)` pageerror → the owner-seat instrumented M4 read, non-blocking.

## ADDENDUM 2026-09-23 — KF.W13R: keyframes.js repins glass-ui 7.0.0 → 10.0.1 (COHESION §0bs). KF.W13V now opens after KF.W13R.

**Opens after**: KF.W13U CLOSED. **Blocks**: KF.W13V (its "Opens after" becomes KF.W13R CLOSED + the two registers). **Model**: Opus 5.5. **Record**: `docs/tranches/X/execution/B/KF-W13R.md`.
**Why now**: the owner keeps meeting dock/morph defects glass has since moved on (glass HEAD `morph.css:69-76`: the authored morph blur is `blur(0px)`; keyframes' installed 7.0.0 holds `filter: blur(...)` through the settle — KFA-53). BL re-measures every relayed row at HEAD; a consumer three majors behind cannot see those cures.
**Owner rows carried**: OA-40, OA-41 (below), with OA-26 and OA-32/33.
Units, serial:
- **`.m` (Opus, effort high) — the migration.** Read glass's CHANGELOG and migration notes for 8.0.0, 9.0.0, 10.0.0 and 10.0.1 WHOLE; pin `"@mkbabb/glass-ui": "10.0.1"`; migrate every breaking change at its consumer root (renamed props/exports, the dismiss axis, surface classes, dock API) — no compatibility shims, no deep imports; `npm run check` 0, vitest GREEN, the kf e2e suite at `--workers=1` with load recorded.
- **`.v` (Opus) — re-read on the served page.** Re-run the KF.W13U served-page gates, the KF-ANIMATION-AUDIT capture scripts for every dock-morph row (KFA-7/8/13/50–53/109–112/189/221/222) and KFA-23/-163/-168; each row reads CURED-BY-REPIN (with frames) or still-live (stays for BL / KF.W13V). Record a table.
- **`.d` (Opus, effort high) — OA-41 dock motion at 10.0.1.** With the repinned dock: the small↔large morph is smooth and sharp — 0 frames with a `filter`/backdrop blur on text after settle, no wrap-then-snap, no size overshoot to a negative radius, duration within glass's motion tokens; whatever still fails is producer (DOCK-MORPH-ROOT, BL) with the 10.0.1 measurement relayed.

## OA-40 · OA-41 (owner, 2026-09-23, verbatim; frame `keyframes/evidence/W13U/owner-2026-09-23-mobile-controls.png`)
*"the mobile controls panels are offset and unreadbale--the dock transitions and animations from small to large are blurry, slow, and jittery--not smooth and ios 27 loke"*
- **OA-40 → KF.W13V `.s`**: on phones the controls drawer's inner card is inset unevenly (≈60 px left, ≈0 right), the collapsed transport pill floats OVER the controls (it covers the "fill mode" row), and the top dock wraps `@mbabb` to a second line. Gate (served, headed ×2, 390×844 + 360×740, both themes): the drawer card's left and right insets equal ±1 px; the transport never intersects the open drawer's content box (it docks above the drawer or yields); every control row fully visible and readable; the top dock is one row (OA-33's single mbabb control).
- **OA-41 → KF.W13R `.d`** (above), and the same row for value.js's dock (X-W7R).

**ADDENDUM 2026-09-23 (KF.W13V, fifth; COHESION §0by) — OA-46 sharpens `.s`.** Owner, verbatim: *"the UI for the sequcne, easing, etc is still not right--we should have dock items for keyframes, timeline, etc--NOT inline keyframes like that, etc."* So the scene idiom is: the STAGE is the scene's only in-page content; the **Keyframes** editor, the **Timeline**, the **Controls** (and the scene's own facet, e.g. the spring heatmap or the easing catalogue, where it is an editor rather than the subject) are **dock items** that open their shared pane — one per kind, for every scene (Cube, Amiga, Square, Easing, Spring, Sequence). No scene renders an inline keyframes block, an inline timeline, or an inline editor of any kind. Census first (per scene: which editors are inline today), then move each onto its dock item + shared pane. Gate (served, headed ×2, 1440 and 390): per scene, 0 inline editors in the stage region (DOM census); the dock shows the same item set on every scene (a scene may disable an item it has no data for, never invent its own); opening each item shows the shared pane with that scene's data.

**ADDENDUM 2026-09-23 (KF.W13V sixth + KF.W13R second; COHESION §0bz) — OA-47 · OA-48 · OA-49.** Owner, verbatim (frame `keyframes/evidence/W13U/owner-2026-09-23-controls-space.png`): *"I think we should have better usage of space here, and probably a dividing line above easing--the easing label and dropdown should likely be on one line; all of the dock transitions still are awful and blurry. The easing, sequence, and spring UIs are all awful, too rounded, and not keyframes.js idiomatic with poor design hierarchy and layout, usage of space."*
- **OA-47 → KF.W13V unit `.c` (Opus, after `.s`) — the controls-pane control-row idiom.** One idiom for every control in the Controls pane, across scenes: label and control on ONE line (the easing label, its edit pencil and the easing select share a row; likewise duration/delay/iterations/direction/fill-mode), a divider above the easing group separating timing from curve, tighter vertical rhythm on the glass spacing scale, no half-empty rows. Gate (served, headed ×2, 1440 and 390): every control row is one line at 1440 (label box and control box share a baseline row ±2 px); a separator element precedes the easing group; the pane's total height for the Cube controls drops (before/after measured, pasted); at 390 rows may stack only where the control's min width forces it.
- **OA-48 → KF.W13R `.d`** (and X-W7R `.d`): the owner re-reports the dock transitions as "awful and blurry" after KF.W13U; KF.W13R reads the morph at glass 10.0.1 on the served page and relays the 10.0.1 measurement to BL (dock design family) — no consumer copy.
- **OA-49 → KF.W13V unit `.y` (Opus, effort high, design-author, after `.c`) — Easing, Sequence and Spring redesigned to the keyframes.js idiom** (Cube/Amiga's: the stage is the subject, editors are dock items per OA-46): read `demo/DESIGN.md` whole; per scene a design note (hierarchy, the stage's composition, what is subject vs editor), then rebuild: radii on glass's canon (multi-line holders `--radius-field`, stadium only for single-line controls — nothing rounded-full that is not a single-line control), the type scale for titles/labels/readouts, space used by the subject not by chrome. Gate: per scene before/after frames at 1440 and 390 committed; 0 `rounded-full`/`9999px` on multi-line elements; each scene's DOM census matches the canonical scene row from `.s`.
- KF.W13V order: [`.s`] → [`.c`] → [`.y`] → [`.p`] → [`.k`] → [`.u`].

## ADDENDUM 2026-09-23 — UI-audit fold into KF.W13V `.u` (COHESION §0ca)
- `.u` also covers the audit critic's keyframes gaps. The catch-all route was never visited, so visit it and audit it. Every UIA-KF row with no frame gets one on the served page before its cure.
