# X.KF.W8 — Structure & Colocation Settle

> **Provenance.** Agglomerated by the fresh-Fable fold seat (M-23) from one received arm (Opus, library-half-marked) verified **row-for-row against the CARRY authority (34 rows, 15 gates)**. Every load-bearing measurement in this file was **re-taken read-only by the fold seat this session** against **`origin/master 81a56990`** in `/Users/mkbabb/Programming/keyframes.js` (`git grep` / `git ls-tree` / `git show`; the 252-row dirty worktree and the divergent local `master 8281638c` were never consulted — KF.W0 §B-12 / MISS-α5; merge-base `a59d3a22`, neither ref an ancestor of the other). Fold repairs to the received arm: **D-19-9** (up-import denominator 3, not 2), **D-19-10** (AnimatedText importer basis), **D-19-11** (G14 host list), the units/seats inconsistency, and anchor corrections in G4/G7/G8. L-20 validated against `docs/precepts/instructions/README.md:127-130` (tests outside `src/`) and `:132-135` (doc drift is uncommitted work). This is a *specification*: **status fields stay `planned`; nothing opens product source; execution awaits the owner's begin-word.**

## State

**Name**: KF.W8 — Structure & Colocation Settle
**Opens after**: **KF.W0** (substrate settle — the wave states its ref; every inherited count re-derived), **KF.W4** (vue-tsc = "the sequencing head of the whole record"; W4 touches `vitest.config.ts`/`package.json` *first*), **KF.W5** (the library **surface decision** — which symbols join the published map). Named by artifact, not only by number.
**Units**: 8 (a→h), serial; no parallelism, no jury; tiering per M-12 tri-fold is the orchestrator's at execution.
**Hard gate**: **fifteen conditions, ALL RED**, measured at `81a56990` and pasted below.
**Status**: planned

| verb | state | evidence |
|---|---|---|
| AUDITED | YES | 21 adjudicated `registry/adjudicated/kf-*.md` records + `formation/keyframes/CENSUS-2026-08-03.md` |
| SPECIFIED | YES — by this file (fold-seat agglomeration of the received arm against the CARRY) | — |
| IMPLEMENTED | NO — status `planned`; awaits begin-word | — |
| VERIFIED | NO | — |

**COMPLETABLE — conditionally.** Units a, b, d, e, f, g complete inside this wave. Units c and h **cannot** go green alone: c consumes KF.W5's surface ruling; h's G15 is bundled with `kf-ChannelControls` L-2/C-2. Both are specified with their blockers named, not smuggled.

## Units

| unit | act | gates |
|---|---|---|
| a | re-baseline census at the stated ref — publish `KF-W8-census.md` (the D-19 table below is its seed); every downstream number binds to it | feeds all |
| b | ratify the colocation idiom (**KF.W8-I**) in one paragraph; re-derive all nine banked colocation rows against it | directions of G5/G7 |
| c | deep-import extirpation + demo-side repoint — **after KF.W5's surface ruling** | G1, G2 |
| d | keyframe-selector serializer unification to one body | G3 |
| e | the dead-barrel family ruling (one ruling, four sites) + the `src/` single-child chain ruling | G4, G13 |
| f | the colocation moves as **pure-move** commits, each with its prose corrections in the same commit | G5, G6 (verify), G7, G8 |
| g | test-lane settlement — SFC runtime loading, orphan adoption, `groupShortcuts()` extraction, characterization coverage — **after KF.W4** | G9–G12 |
| h | seam/extraction work: KF-AV-26 extraction, KF-AV-24 projection seam, KF-AV-9 demo wire, m-4 re-derivation, RibbonBar-side spec input | G14, G15 |

## Goal criterion

This wave succeeds if, at close, **no demo module reaches around the two-entry exports map**, **no module lives above its only consumer**, **no barrel or test file exists that nothing loads**, and **the colocation idiom is written down once and every banked colocation row is re-derived against it**. A wave that moves files without ratifying the direction has done migration instead of settlement and has failed.

## Scope

1. Re-baseline: state the ref, re-derive every inherited denominator, publish the corrected anchors (§Carry D-19).
2. Ratify the colocation idiom in one paragraph; re-derive all nine banked colocation rows against it.
3. Extirpate the `@src/` deep reaches after KF.W5 rules the surface.
4. Unify the keyframe-selector serializer to one body.
5. Rule the dead `defineAsyncComponent` barrel family — adopt-and-gate or delete-with-its-comment — one ruling, four sites.
6. Land the colocation moves as **pure-move** commits (no behavior), each with its prose corrections in the same commit.
7. Make the demo lane able to load an SFC; adopt the two orphaned bench measure-tests; extract and cover `groupShortcuts()`.
8. Rule the `src/` single-child chain.
9. Specify the two-ribbon hoist as the RibbonBar-side input to the one structural settlement it belongs to.

## Bounds

CARRY `boundsFiles` reconciled against `origin/master`. **Bold** = the CARRY path was wrong or unresolved and is corrected here (D-19).

| path (at `81a56990`) | access | why |
|---|---|---|
| `src/animation/index.ts` · `src/animation/public.ts` · `src/animation/load-engine.ts` | read-only (W5 writes) | **CARRY said `src/index.ts`, `src/public.ts`, `load-engine.ts` at root — none exist; all three live under `src/animation/`.** The surface decision is W5's; this wave reads it. |
| `src/animation/compile/emit/format/format.ts` | modify-carve | **CARRY said `emit/format.ts`.** Serializer copy 1 (`selectorText:20`). |
| `src/animation/compile/emit/css-text.ts` | modify-carve | copy 2 (`serializeSelector:58`) |
| `src/animation/compile/emit/index.ts` | modify-carve | the export list G3 amends; note `cssIdent` IS exported here (`:53`) — the N-8 gap is at the *package* surface, not this internal barrel |
| **`src/animation/compile/selector.ts`** | read-only | **CARRY named no path**; `namedSelectorToFraction:42`, re-exported by `compile/index.ts:24`, unpublished at the map |
| `src/animation/internal/helpers.ts` · `src/animation/resolve/browser.ts` | read-only | reach targets; `browser.ts:23` installs the module-eval resize listener; `:1-3` bear `@mkbabb/value.js/{value,css}` |
| `src/animation/**` | census-read | the single-child + stutter re-count (unit a) |
| `package.json` (exports · sideEffects · scripts · devDeps) | modify-carve **after KF.W4** | 2-entry map; `@vitejs/plugin-vue ^6.0.7` present, `@vue/test-utils` **absent**; vitest `^4.1.8`; vue `^3.5.35` |
| `vitest.config.ts` | modify-carve **after KF.W4** | G9/G10 |
| `bench/d3-changed-keys.measure.test.ts` · `bench/sync-step.measure.test.ts` | adopt | **CARRY said `test/**/*.measure.test.ts` — they are in `bench/`.** |
| `test/demo/instrument/**` (**9** files) | modify-carve | **CARRY said 8.** |
| `test/demo/instrument/resize-tracks.test.ts` | modify-carve | KF-AV-18 prose arm (6 value.js cites: `:4/:5/:13/:35/:105/:127`) |
| `demo/utils/helpers.ts` | modify-carve | **the concentrator** — `:9` is the surviving deep reach; its own `:1-8` docblock names this wave |
| `demo/utils/keyframeSelector.ts` | modify-carve | copy 3 (exported `selectorText:7`, reach at `:5`). Exists at origin/master; **absent at local HEAD** (MISS-α5) |
| `demo/components/instrument/keyframes/composables/useKeyframeOps.ts` (`:1`) · `keyframes/utils/parseAnimationCSS.ts` (`:7`) · `timeline/utils/timelineEngine.ts` (`:1`, **`:17`**) · `playback/AnimationVisualizer.vue` (**`:45`**) | modify-carve | the 7 deep-import statements. **CARRY's `timelineEngine:4/:19`, `useKeyframesState:1`, `useKeyframesParsing:1`, `CSSCodeEditor:40/:42` are stale — see D-19-1/D-19-3.** |
| `demo/components/instrument/index.ts` · `keyframes/index.ts` · `timeline/index.ts` | delete-or-gate | the barrel family |
| `demo/components/instrument/shell/index.ts` | modify-carve | 4-of-8 barrel |
| `demo/components/instrument/shell/HeroAurora.vue` | move | sole importer `demo/app/App.vue` (`:51` mount, `:147` import) |
| **`demo/components/instrument/shell/AnimatedText.vue`** | **stays** | **its one importer is shell-local — U.B5's trio re-derives to a duo (D-19-6/D-19-10)** |
| `demo/components/instrument/keyframes/KeyframeCard.vue` → `keyframes/components/` | move | KC-30 |
| `demo/components/instrument/timeline/TimelineCaret.vue` → `timeline/components/` (+ rename) | move | N-11 + m-7 |
| `demo/components/instrument/keyframes/components/KeyframeCardList.vue` (`:31`) · `timeline/components/TimelineTrack.vue` (`:116`) | modify-carve | two of the three up-imports (the third is D-19-9's, not unit f's) |
| `demo/components/instrument/shell/KeyboardShortcutsModal.vue` (`:57`) + a new sibling `groupShortcuts.ts` | modify-carve · create | R-10 |
| `demo/components/instrument/timeline/CSSPasteDialog.vue` · `keyframes/KeyframesAddDialog.vue` | read-only here | characterization targets; shape is KF.W7's |
| **`demo/components/instrument/transport/controls-pane/RibbonBar.vue`** (151 L) | spec-input only | **CARRY's path was unresolved.** Bundled — no byte lands without L-2/C-2 |
| `demo/components/instrument/transport/channel-controls/{ChannelControls,ChannelOptions}.vue` | read-only | the hoist's destinations; `ChannelControls:186` already teleports with `defer`; `:229` is D-19-9's third up-import |
| **`demo/app/App.vue`** (`:144`) | modify-carve | **CARRY said `demo/App.vue`.** The `editor-shell/` phantom |
| `demo/components/CopyButton.vue` | **verify-only** | ruled to KF.W6 (§Sequencing R-1) |
| `demo/composables/useDragScrub.ts` · `demo/components/instrument/transport/AnimationControlsGroup/useControlsKeyboardShortcuts.ts` | read-only | seam charter · the documented extraction precedent |
| `demo/scenes/spring/SpringPhysicsFacet.vue` | read-only | SPF-22's decider (`:135` bypassing import) |
| `docs/tranches/.../waves/KF-W8-census.md` · `KF-W8-CLOSE.md` | create | unit a / close |

**Do NOT touch** — each is another owner's row: `.dependency-cruiser.cjs` and every gate script under `scripts/gates/**` (KF.W4) · `src/animation/presets/**` and the pass-through shims (§Excluded, owner-confirm) · any `src/` god-module split (KF.W5) · `demo/components/instrument/transport/TransportDock/**` (TD-1/TD-2, NO-WAVE-OWNER, BH-relay rider) · `scripts/dev/dev.sh` (unowned).

## Carry

### D-19 — what re-baselining changed (unit a's seed; every downstream row depends on it)

Rows 1–8 were the received arm's; the fold seat re-measured and confirmed each. Rows 9–11 are the fold seat's repairs.

| # | banked as | measured at `81a56990` | consequence |
|---|---|---|---|
| 1 | `convertPixelsToCh` + `debounce` deep-imported (`CSSCodeEditor:40/:42`, `useKeyframesState:1`) | **Both are demo-owned**: `demo/utils/helpers.ts:16` (`debounce`) and `:28` (`convertPixelsToCh`); the call sites import `@utils/helpers`. | **Mechanism SURVIVES, indirected**: `helpers.ts:9` imports `convertToPixels` from `@src/animation/resolve/browser`, whose `:1-3` bear `@mkbabb/value.js/{value,css}` and whose `:23` installs `window.addEventListener("resize", …)` at module eval. Importing two lines of arithmetic still drags the value.js-bearing, side-effectful module onto the editor chunk. **Anchors corrected; grade unchanged.** |
| 2 | — | `demo/utils/helpers.ts:1-8` docblock: *"the remaining deep-import retirement belongs to the folded-forward demo settlement wave."* | **A live in-tree witness naming this wave.** Cited in G1. |
| 3 | KF-KE-15: 6 in closure / 8 in directory / 1 from `internal/` | **2** in the `keyframes/` directory (`useKeyframeOps:1`, `parseAnimationCSS:7`); `internal/helpers` is reached from `timeline/`, not `keyframes/`. | Census **UNREPRODUCED at origin/master**; the sharpened figure was taken on a different ref. G1 counts **7 statements / 6 files / 4 library modules**, enumerated. |
| 4 | KF-AV-8: HEAD ships a build-breaking `from "@mkbabb/value.js"` import of `bumpLayoutEpoch` | At origin/master `AnimationVisualizer.vue:45` = `@src/animation/resolve/browser`; `:46` = `clamp` from `@mkbabb/value.js/math` (a real export). | The "uncommitted repair" **is committed** at origin/master. The deep import is the live state; the build-break is a local-HEAD fact only. |
| 5 | census: 16 dir/name stutters + 1 single-child | **`src/` has exactly one child (`src/animation`) — CONFIRMED.** Stutters by `basename == dirname`: **8** (`emit/backward/backward.ts`, `emit/format/format.ts`, `group/group.ts`, `orchestration/{sequence,split-text,timeline,view-transition}/<same>.ts`, `physics/spring/solver/solver.ts`). | **16 is UNREPRODUCED (no command was recorded — L-9).** G13 cites 8 with its command, never the bare number. |
| 6 | KF-AT-26(c)/U.B5: the home-hero **trio** moves to `app/` | `HeroAurora` — sole importer `demo/app/App.vue` (`:51`/`:147`). `AnimatedText` — see D-19-10. | **U.B5's trio is a duo.** Under the ratified idiom AnimatedText **stays** in `shell/`; moving it would manufacture the exact up-import G5 forbids. The unexecuted carry is re-derived, not replayed. |
| 7 | m-4: "the directory named for the component doesn't contain it" | `transport/controls-pane/` contains `ControlsPaneWrapper.{vue,css}` **and** `RibbonBar.vue`. | **Does not reproduce as worded.** Booked MEASURED-DIVERGENT: unit h re-derives it against the ratified idiom before any directory settle; not silently dropped, not executed on a stale mechanism. |
| 8 | census: 131 test files / 8 demo-lane files | **130** `test/**/*.test.ts`; **27** under `test/demo/`, **9** under `test/demo/instrument/`. | Denominators restated; case counts (1 051) not re-derived and therefore **not cited** by any gate. |
| 9 | up-imports RED at 2 (received arm) | **RED at 3**: `keyframes/components/KeyframeCardList.vue:31` · `timeline/components/TimelineTrack.vue:116` · **`transport/channel-controls/ChannelControls.vue:229 → ../KfPillTabs.vue`** — a third site the received arm missed. `KfPillTabs` is also flagged a "T.H gated-on-publish excision" (`demo/styles/font-roles.json:32`). | ⟨fold seat⟩ G5's denominator corrected. Two sites are unit f's; the third sits in the transport cluster — its settle rides the M-4/L-2/C-2 structural settlement or the flagged excision, and **G5 may not go green by ignoring it**. |
| 10 | received arm: AnimatedText has "two shell-local importers" | **One importer**: `shell/EditorStartScreen.vue:62`. `TypingDots.vue:4` mentions it only in a comment — prose, not an import. | ⟨fold seat⟩ Basis corrected; the **conclusion stands** — the single importer is shell-local, so AnimatedText stays in `shell/` under KF.W8-I. |
| 11 | received arm G14 hosts: `useDragCapture`, `useDragScrub` among candidates | `getBoundingClientRect()` = **11 lines / 10 files**: `TimelineTrack.vue`, `timeline/composables/useZoomPan.ts`, `TransportDock/useMenubarMeasure.ts` (**DO-NOT-TOUCH territory**), `AnimationVisualizer.vue` ×2, `scenes/amiga/useSphereSpin.ts`, `SequenceScrubber.vue`, `SequenceTarget.vue`, `SpringHeatmap.vue`, `SpringTarget.vue`, `SquareScene.vue`. **`useDragScrub` itself has zero** — it receives geometry. | ⟨fold seat⟩ Host list corrected; the charter framing sharpens (the seam owns projection, callers own rect reads). Which lines are *projectors* is unit a's enumeration; G14 binds to it. |

⟨unit a · commands: `git grep -n 'from "@src/' origin/master -- demo` · `git grep -n 'from "\.\./[A-Z][A-Za-z]*\.vue"' origin/master -- demo` · `git ls-tree --name-only origin/master -- src/` · `git ls-tree -r --name-only origin/master -- src \| awk -F/ '{f=$NF;sub(/\.ts$/,"",f);if(f==$(NF-1))print}'` · `git ls-tree -r --name-only origin/master -- test \| grep -c '\.test\.ts$'` · `git grep -c 'getBoundingClientRect()' origin/master -- demo`⟩

### The ratified idiom (unit b) — census §3 FE §7 · L-17 · resolves the KC-30 conflict

> **KF.W8-I.** A module with exactly **one** consumer lives **beside or below** that consumer — never above it. A `components/` (or `composables/`, `utils/`) subdirectory is the canonical home for a cluster's single-owner children (**L-17**, blessed); the cluster root holds only what more than one directory consumes, or the cluster's entry. A barrel exports exactly its own directory and must have ≥1 importer. No new `shared/` directory and no wrapper component that does not already exist (`feedback_kiss_no_contrivance`); extractions are focused modules, never additions to a god module (`feedback_no_god_modules`).

**There is no L-17-vs-KC-30 conflict once the direction is stated.** L-17 blessed the *container*; KC-30 and N-11 convict the two files still sitting *outside* it. Re-derivation of all nine colocation rows: KC-30 → `KeyframeCard.vue` moves **down** into `keyframes/components/` · N-11/m-15/L-8 → `TimelineCaret.vue` moves **down** into `timeline/components/` · KF-AT-26(c)/KF-HA-14 → `HeroAurora.vue` moves **beside** `demo/app/App.vue`; **AnimatedText stays** (D-19-6/D-19-10) · KF-CB-37 → `CopyButton.vue` leaves the root (executed in W6, R-1) · m-4 → re-derive (D-19-7) · KF-AV-26 → the extracted composable lands **beside** `AnimationVisualizer.vue`, mirroring the already-colocated `use*.ts` population (re-counted at unit a) · R-10 → `groupShortcuts.ts` lands **beside** `KeyboardShortcutsModal.vue`, the seam `useControlsKeyboardShortcuts.ts` already documents.

⟨L-17 `kf-KeyframesAddDialog` · census `:142-144` · authority `R1-05-demo-colocation.md:118` — **a keyframes.js V-tranche doc, not in the value.js tree; re-resolve before citing** (cite, never re-mint)⟩

### Rows — all 34 CARRY rows, none dropped

| id | lands as | witness / lock |
|---|---|---|
| **KF-CE-12** | unit c · G1 + G2 | Cure is a **publication/relocation decision, not an import edit** — "no compliant path exists; the epoch surface is unpublished" (KF-AV-8 sharpening, adopted). K-10 read-down carried: the `sideEffects:false` "contradiction" framing is **DROPPED** — the declaration is self-consistent; the dated record stays unrewritten (E-3). R1 ingress note (CSSCodeEditor `:116` = the demo's largest untrusted-CSS ingress, the fuzz entry of record) folds **by reference** to the megatranche R1 identity → KF.W2/W3, never re-booked. |
| **KF-CE-42** | folded into KF-CE-12; identity retained for life, carries no independent cure | anti-rename: appears in this map, never re-booked as new |
| **KF-AV-8** | unit c · G1/G2 (three-site extirpation = one commit) | bundles with KF-AV-9 (one epoch-surface decision owns eviction scoping/debounce); `browser.ts:23` listener verified; D-19-4 corrects the local-HEAD framing |
| **KF-AV-35** | rides KF-AV-8 | the K-10 read-down is carried so no spec re-argues a killed framing |
| **KF-AV-9** | library half → KF.W5 with KF-CE-12; demo half (wire relocation + debounce) → unit h | bounding note verbatim: *"the eviction API is global-by-construction (no scoped bump exists) and the wire mirrors the library's own undebounced window-resize policy, so half of this row is a library-surface design question."* `AnimationVisualizer.vue:45` is the only wire in demo/ |
| **KF-AV-18 (KF.W8 arm only)** | prose sweep in the **same commit** as the KF-AV-8 extirpation · G8 | AV comment block `:70-:78` (≥4 value.js attributions) + `resize-tracks.test.ts:4/:5/:13/:35/:105/:127` (×6). **The source-regex gate pin is KF.W4's and is NOT authored here.** Adjudicated MINOR (reader-1's MAJOR recorded) |
| **KF-KE-15** | folded to KF-CE-12; census **re-derived** (D-19-3) | pure-repoint commit; must not race KFED-UNIT |
| **C-8 (kf-KeyframesStringControls)** | unit c; **split lock**: KF.W5 rules *which* symbols publish, KF.W8 repoints | `cssIdent` (N-8) is **W5's**, not this wave's — note it is already exported at the internal `emit/index.ts:53`; the gap is the *package* surface |
| **C-12 (kf-KeyframeTimeline)** | unit c · G1 | load-bearing clause carried: **the demo round-trip is not a consumer proof** — any wave citing it as evidence of library correctness is citing a private-path proof. Depcruise arm → KF.W4 |
| **MISS-β2 (kf-TimelineCaret)** | unit d · G3 | three copies verified (D-19 paths). **Gates banked KC-1**, whose cure names a non-exported symbol and does not compile as written; the citation correction is recorded **here**, KC-1's dated record unrewritten (E-3). Cross-ref KF.W2's emit façade as possible vehicle. Ref stated per MISS-α5 |
| **C-13 (kf-PlaybackRibbon)** | **fold only** — ≡ KF-AV-8/-9/-18 | recorded so the ribbon's confirmation is not mistaken for a fourth site. Zero re-booking |
| **KF-CB-37** | **G6 verify-only**; the move executes in KF.W6 (R-1) | S-2 binds: a real `<button type="button">` root earns roving-tabindex membership (`useToolbarKeyboard.ts:41-49` + its docblock naming CopyButton) — any move/rename corrects that docblock in the same edit |
| **KF-AT-26(c)** | unit f · G7 | **IDENTITY GUARD**: legs (a)(b)(d) → KF.W6, leg (e) → KF.W10. Annexing any other leg here is forbidden. U.B5 cited, never re-minted |
| **KF-HA-14** | rides KF-AT-26(c); contributes the `editor-shell/` phantom (`App.vue:144`) to G8 | corrected in the same move edit |
| **KAD-24** | unit e · G4 — **the surviving identity for the barrel family** | cure-shape lock (kf-SquareScene law): the stated chunk invariant is a **test obligation** — adopt-and-gate with a real chunk-graph assertion, or delete barrel **and** comment in one commit. A silent delete leaving the rationale prose elsewhere is doc drift (README:132-135) |
| **SPF-22** | folds ≡ KAD-24; contributes the decider (`SpringPhysicsFacet.vue:135` direct import bypasses both barrels) | rides KAD-24 |
| **KF-KC-45** | **FOLDED into KAD-24** (R-2), identity retained for life | the overlap the CARRY flagged, resolved in R-2 |
| **L-19/C-14 (kf-KeyframeTimeline)** | third site of the family; rides the one ruling | inside the W7-named cluster — file-sharing edge declared |
| **N-11 (kf-KeyframeTimeline)** | unit f · G5 · **pure-move**, m-7's rename in the same edit | bank's **INFO** governs (axis-L's MINOR corrected) |
| **m-15 (kf-TimelineTrack)** · **L-8 (kf-TimelineCaret)** | fold only, ride N-11 | grade corrections recorded so no spec re-inflates them |
| **m-7 (kf-TimelineCaret)** | rename **with** the move | the ScrubberTimeline mapping arm is **CLOSED at banked C-15** and may not be re-derived. Every downstream cite (census S-3/S-4, test names, comments) corrected in the same commit |
| **KC-30 (kf-KeyframeCardList)** | unit f · G5 · **pure-move, no behavior** | so it cannot invert CARD-UNIT's KC-34 highlight-before-blanking ordering |
| **L-17 colocation fold (kf-KeyframesAddDialog)** | **unit b** — the ratification anchor | see KF.W8-I |
| **R-10 (kf-KeyboardShortcutsModal)** | unit g · G10/G11 | one `vitest.config.ts` edit closes R-10 + C-10 + the KAD zero-coverage fold. **Boundary**: W4 owns SFC *type*-checking and lands first; W8 owns SFC *runtime* loading. **R-9 stays NO-WAVE-OWNER and is not absorbed** |
| **C-10 (kf-CSSPasteDialog)** | unit g · G12 | plugin capability lands unconditionally; the characterization test is written against **whichever shape KF.W7 rules**. Pins the falsy-`""` marker (KAD-14) + the undocumented `format`-writes-model requirement (KAD-13) |
| **KF-AV-26** | unit h — extract the DOM-free block to a colocated `use*.ts` | **STANDING RIDER**: a KF.W7 "swap" verdict (S-9 / KF-AV-28) discharges this row with the component. Sequence after that verdict or accept the throwaway |
| **KF-AV-24** | unit h · G14 (projection math only) | **RIDER**: SequenceScrubber's `role=slider`+keyboard and AnimationVisualizer's `aria-hidden`+no-keyboard are *both deliberate, both verified*. The seam consolidates **projection**, never a11y posture. Same swap rider |
| **M-4 (kf-RibbonBar) — NOT kf-ChromeDock's M-4** | unit h · G15 — **spec input only**, hard-bundled | **NAME-COLLISION GUARD**: this is kf-RibbonBar's M-4, **never** kf-ChromeDock's (whose worded cure carries the MbabbMenu MUST-CARRY rider). The two ids must never merge. Also shrinks C-11's undeclared document-scoped deps (`#controls-ribbon-target`, `url(#rainbow-gradient)`) |
| **m-4 (kf-RibbonBar)** | rides M-4 — **one** directory settle; **re-derive first** (D-19-7) | U.B2 cited, never re-minted; the rename corrects every prose citation of the old home in the same commit |
| **KF-KC-42** | conditional on R-7 (R-3) | R-7's fold constraints carried: adopt **this** file's a11y composition (siblings under `DialogContent`) + the twin's text-hoisting contract. **R-25 third-copy warning**: the `innerText`-mirror is TRIPLICATED (`KeyframeCard.vue:43`, verified) — a two-dialog fold leaves the third |
| **census §(a)9 + §2-flags · `src/animation` single-child flatten** | unit e · G13 — **a ruling, not a move** (R-4) | stale count killed (D-19-5); shims/presets excluded pending owner-confirm |
| **census §1 tests-row (LIB §5) · two orphaned `*.measure.test.ts`** | unit g · G9 — **adopt in place** (R-5) | precept-bound: `test/`-external is *conformant*; isomorphism means mirroring src shape **inside** `test/`, never colocating into `src/` |
| **census §3 FE §7 · colocation ratification** | unit b — the ratification (KF.W8-I) | counts re-derived; owner precepts bind the text |

## Gates

Fifteen, all born RED, measured by the fold seat at `origin/master 81a56990`. Every command is literal; no gate cites an inherited number.

**G1 — zero `@src/` specifiers in `demo/`, and no demo module transitively imports a value.js-bearing library module.**
`git grep -c 'from "@src/' origin/master -- demo` → **0**; and `demo/utils/helpers.ts` no longer reaches `src/animation/resolve/browser`.
*RED today* — **7 statements / 6 files / 4 library modules**, re-measured: `keyframes/composables/useKeyframeOps.ts:1` (`reverseCSSTime`) · `keyframes/utils/parseAnimationCSS.ts:7` (`serializeTimingFunction`) · `timeline/utils/timelineEngine.ts:1` (`camelCaseToHyphen`) and `:17` (`serializeCssValue`) · `playback/AnimationVisualizer.vue:45` (`bumpLayoutEpoch`) · `utils/helpers.ts:9` (`convertToPixels`) · `utils/keyframeSelector.ts:5` (`namedSelectorToFraction`). Comment-only mentions are **not** counted. In-tree witness: `helpers.ts:1-8` names this wave.
*Falsifier*: a demo re-export shim that forwards the same module passes clause 1 and fails clause 2.
*Blocked by*: KF.W5's ruling — clause 1 is unreachable while zero compliant paths exist.

**G2 — every demo-consumed library symbol resolves through the published surface.**
`git grep -nE "bumpLayoutEpoch|convertPixelsToCh|reverseCSSTime|serializeTimingFunction|serializeCssValue|camelCaseToHyphen|namedSelectorToFraction|cssIdent" origin/master -- src/animation/index.ts src/animation/public.ts src/animation/load-engine.ts` returns a hit for every symbol still consumed.
*RED today*: **zero hits for all eight — re-run by the fold seat.** The exports map has exactly **two** entries (`.` → `dist/keyframes.js`, `./engine` → `dist/engine/index.js`); `src/animation/index.ts` has **zero `export *` lines**, so nothing leaks transitively.
*Falsifier*: a symbol published by widening a barrel to `export *` passes the grep and fails the narrowness clause.
*The gate MEASURES; KF.W5 DECIDES.*

**G3 — exactly one keyframe-selector serializer body in the repo.**
`git grep -n "const selectorText\|const serializeSelector" origin/master -- src demo` → 1.
*RED today* — three, two byte-equivalent: `src/animation/compile/emit/format/format.ts:20` (private; consumed `:132/:201/:280/:313`) · `src/animation/compile/emit/css-text.ts:58` (private) · `demo/utils/keyframeSelector.ts:7` (**exported**, reaching private `namedSelectorToFraction` at `:5`; defined `compile/selector.ts:42`, re-exported `compile/index.ts:24`, unpublished at the map). **Ref stated: copy 3 exists at `81a56990` and does not exist at local `8281638c`** (MISS-α5).
*Falsifier*: publishing one and leaving two callers on their private copy passes a naive count and fails the "one body" clause — the gate counts **definitions**, and `emit/index.ts`'s export list is read (no serializer export today; verified).

**G4 — every `defineAsyncComponent` barrel export has ≥1 importer, or the barrel and its rationale comment are both gone.**
`git grep -l 'from "@components/instrument"' origin/master -- demo test` non-empty **or** the barrels absent.
*RED today*: `instrument/index.ts` — **0 importers**; the sub-barrels' only inbound refs are the umbrella's own `export *` lines (`:24-:27`). `instrument/keyframes/index.ts` — three async exports, **0 importers**. `instrument/timeline/index.ts` — one async export + one type export, **0 importers** (`git grep 'instrument/timeline"'` → 0); the sole consumer hand-wraps its own `defineAsyncComponent` (`ChannelControls.vue:253`). The real mount bypasses all of it (`SpringPhysicsFacet.vue:135` direct import). All three carry a stated chunk invariant ("importing this umbrella never eager-loads the Monaco / highlight.js chunk") that nothing enforces.
*Falsifier*: adopting the barrels without a chunk-graph assertion leaves the invariant unenforced — the gate then requires the assertion, and a build whose entry graph contains monaco must fail it.
*Fourth site*: `KF-CE-40` (EDITOR-UNIT) — **one ruling covers all four**, co-declared.

**G5 — no component imports a sibling from a parent directory.**
`git grep -cn 'from "\.\./[A-Z][A-Za-z]*\.vue"' origin/master -- demo` → 0.
*RED today* at **exactly 3** (D-19-9, fold-seat corrected): `keyframes/components/KeyframeCardList.vue:31` → `../KeyframeCard.vue` · `timeline/components/TimelineTrack.vue:116` → `../TimelineCaret.vue` · `transport/channel-controls/ChannelControls.vue:229` → `../KfPillTabs.vue`. The first two are unit f's moves; the third sits in the transport cluster and settles with the M-4/L-2/C-2 structural settlement or KfPillTabs' flagged excision (`font-roles.json:32`) — **declared, not ignored; G5 binds to the enumeration**.
*Falsifier*: moving the parent into `components/` while leaving a re-export at the old path passes the grep and fails KF.W8-I.

**G6 — zero loose `.vue` files at `demo/components/` root.** *(verify-only; the act is KF.W6's — R-1)*
`git ls-tree --name-only origin/master -- demo/components/ | grep -c '\.vue$'` → 0.
*RED today*: **1** — `CopyButton.vue`, beside `instrument/` and `playback/`.

**G7 — `shell/index.ts` exports exactly its directory, and every export has ≥1 importer.**
*RED today*: **4 of 8 SFCs** exported (`EditorShell`, `EditorHeader`, `EditorStartScreen`, `SharePopover`); absent: `AnimatedText`, `HeroAurora`, `KeyboardShortcutsModal`, `TypingDots`. `EditorHeader` is exported with **zero component importers** (its only other mention is a CSS comment in `demo/styles/layout.css:15`). `HeroAurora`'s sole importer is `demo/app/App.vue` (`:51`/`:147`).
*Discharged by* the move (G5's direction), **not** by editing the barrel in place. **`AnimatedText` is discharged by staying** (D-19-6/D-19-10).

**G8 — zero directory or module citations in comments that resolve to nothing.**
*RED today*: `demo/app/App.vue:144` cites `editor-shell/` — no such directory (`shell/` is the real home; `.editor-shell` is a CSS class, a different referent) · `AnimationVisualizer.vue:70-:78` attributes the layout-epoch cache to value.js (≥4 mentions) while `:45` imports `bumpLayoutEpoch` from keyframes' own `src/animation/resolve/browser` (defined there; verified) · `test/demo/instrument/resize-tracks.test.ts` repeats it at `:4/:5/:13/:35/:105/:127` · three barrel chunk-rationale comments describe protection the tree does not get.
*Authority*: README:132-135 — every correction lands in the **same commit** as its move.

**G9 — every `*.test.ts` in the repo is picked up by exactly one declared vitest project.**
`npx vitest list --filesOnly` (vitest `^4.1.8`) enumerates the same set as `git ls-files '*.test.ts'`.
*RED today*: **2 orphans** — `bench/d3-changed-keys.measure.test.ts`, `bench/sync-step.measure.test.ts`. The `library` project includes `test/**/*.test.ts` (excluding `test/demo/**`), the `demo` project `test/demo/**/*.test.ts`, and `benchmark.include` is `bench/*.bench.ts` — all three verified in `vitest.config.ts`. Nothing matches `bench/*.measure.test.ts`.
*Falsifier*: adding an include glob that silently also swallows `*.bench.ts` — the gate compares the enumerated sets, not counts.
*Precept*: verify glob behaviour **before** choosing the shape (README:127-130).

**G10 — the `demo` vitest project can load a `.vue` SFC.**
A one-file probe importing any `demo/**/*.vue` runs green under `npx vitest run --project demo`.
*RED today*: `vitest.config.ts`'s `test.projects` block declares two jsdom projects with **no `@vitejs/plugin-vue`** (verified — no plugin key exists) — an SFC import throws at resolve, while `test/demo/instrument/` holds **9** real test files. `@vitejs/plugin-vue ^6.0.7` **is** a devDependency (so the cure is wiring, not installation); **`@vue/test-utils` is absent** and is the cure's one new devDep.
*Boundary*: KF.W4's vue-tsc edit to the same files lands **first**.

**G11 — `groupShortcuts()` exists beside the SFC and is covered in the demo lane.**
`git grep -l groupShortcuts origin/master -- demo test` returns both a module and a test.
*RED today*: `groupedShortcuts` is locked inside `KeyboardShortcutsModal.vue:57`; `git grep -l "KeyboardShortcuts\|groupedShortcuts" origin/master -- test` → **0** (verified). The colocated-composable seam is documented by `useControlsKeyboardShortcuts.ts` itself.
*Gated on G10.*

**G12 — characterization coverage exists for the paste/add dialog pair.**
*RED today*: `git grep -l "CSSPasteDialog\|KeyframesAddDialog" origin/master -- test` → **0** (verified).
*Gated on G10 **and** KF.W7's R-7 verdict — do not pin tests to a shape W7 may delete.*

**G13 — the `src/` single-child chain is RULED, and the ruling is in the tree.** *(restated from CARRY's "zero chains" — R-4; the CARRY's own cure shape allows "flatten **or** write the ruling that it stays")*
A committed ruling names the chain, its disposition, and its trigger; `git ls-tree --name-only origin/master -- src/` output is pasted beside it.
*RED today*: `src/` → **one entry, `src/animation`** (verified); no ruling exists. Stutters by `basename == dirname`: **8**, enumerated in D-19-5 with the command.
*L-19 hazard, honored*: the census's `16` is **not** cited by this gate — no command was ever recorded for it.
*Falsifier*: a ruling with no trigger, or a flatten executed while adjudicated records still cite `src/animation/**:line` anchors.

**G14 — hand-rolled rect-ratio progress projectors = 0 outside `useDragScrub`.**
*RED today*: `git grep -c 'getBoundingClientRect()' origin/master -- demo` → **11 lines / 10 files** (D-19-11's corrected host list; `useDragScrub` itself: zero — it receives geometry). **Which lines are *projectors* (rect → 0..1 progress) rather than bare rect reads is unit a's enumeration** — the gate binds to that enumeration, never to `11`. `useMenubarMeasure.ts` is TransportDock territory (DO-NOT-TOUCH) and can only be enumerated, not edited, here.
*RIDER (binding)*: the gate counts **projection math only**. A consolidation that erases the `aria-hidden` charter or forces `role=slider` onto the decorative twin is a **regression, not a cure**.

**G15 — RibbonBar carries zero cross-tree remote-control lines.**
*RED today*: `RibbonBar.vue` = **151 lines** (verified); the Teleport landing pad opens at `:5-:9` (`#controls-ribbon-target`) and knows nothing; `defineProps` at `:137`; `vue ^3.5.35` (so `defer` is available, and `ChannelControls:186` already teleports `KeyframeTimeline` with it); `ChannelOptions` fills the pad with typed props.
***Cannot go green independently***: hard-bundled with `kf-ChannelControls` **L-2/C-2** (the `:key` remount data-destroying blocker — the `:key="storedControls.selectedControl"` remount is live at `ChannelControls:189`) + the expanded-timeline family as **ONE** structural settlement. This wave contributes the RibbonBar-side spec input and no byte.

## Sequencing

**Rulings this file is required to make (the CARRY forbade silent picks).**

- **R-1 · KF-CB-37 vs KF-CB-7 (W6 or W8).** **RULED: the file moves inside KF.W6's reshell commit; KF.W8 verifies (G6).** One edit changes root element, path, and the `useToolbarKeyboard.ts:41-49` docblock together — honoring KF-CB-37's "same wave as the reshell decision" without a second touch that would break the roving-tabindex cohort between commits. KF.W6 carries the reciprocal declaration. If W6 declines the reshell, the bare move falls back to KF.W8 unit f.
- **R-2 · KF-KC-45 vs KAD-24 (same file, two seats).** **RULED: KAD-24 survives as the family identity; KF-KC-45 folds into it as the `keyframes/index.ts` site-witness, identity retained for life.** KAD-24 is the row that enumerates the family and carries the cure-shape lock; KF-KC-45 names one site. *Mechanical check at wave-open*: if the registry's banking order shows KF-KC-45 earlier, the fold direction inverts (earlier-banked wins for life) — the ids and the fold line swap, nothing else.
- **R-3 · KF-KC-42 vs R-7 (W7 or W8).** **RULED: the fold/swap verdict is KF.W7's; KF.W8 executes only its structural consequence.** If W7 rules *fold*, W8 lands the consolidation under R-7's constraints and G12's tests target the ruled shape. If W7 rules *no fold*, KF-KC-42's ARIA-strip repair lands here standalone — it never depended on the fold. Either way the **third `innerText`-mirror copy** (`KeyframeCard.vue:43`, R-25) is in scope; a two-dialog-only fold is a defect.
- **R-4 · the `src/` flatten.** **RULED: DECLINED for the duration of X·KF; the ruling is the deliverable (G13).** A rename of the library's entire source root while a 12-wave program's line anchors point into `src/animation/**` maximizes D-19 churn for zero behavior — KF.W0 §B-12 exists because that tax is already the program's largest. **Receiving surface: KF.W10**, as one pure-rename commit, triggered when the last adjudicated record's `src/animation/**` anchors are discharged. The user-visible symptom — the `@src/animation/...` deep specifier — dies at G1 regardless.
- **R-5 · the two orphans.** **RULED: adopt in place** via a third declared project (`measure`, include `bench/**/*.measure.test.ts`), not by moving them away from the bench fixtures they measure. Both shapes are precept-conformant (`bench/` is outside `src/`); in-place is the smaller act. Glob behaviour verified first, per README:127-130.

**Cross-edges, declared.**

- **→ KF.W0 (precondition).** §B-12 binds. This wave states its ref — **`origin/master 81a56990`** — and re-derives every count (SCH-3..7 drifted). Local `8281638c` and origin/master are divergent (merge-base `a59d3a22`, neither an ancestor); `demo/utils/keyframeSelector.ts` exists only origin-side, so MISS-β2's copy 3 and C-12's chain are origin-side facts.
- **→ KF.W4 (sequenced-before + arm-ownership).** W4 lands **first** and owns: the depcruise-over-demo arm of KF-CE-12/C-12 (note `"lint": "depcruise src"` — the demo is outside its scope today, which is exactly why these reaches were invisible), KF-AV-18's source-regex pin (gate-authoring-law: a gate may not pin a mechanism the component cannot exercise), KF-CE-16's vue-tsc (the sequencing head), and manifest hygiene. **KF.W8 authors no type gate.** Shared files `vitest.config.ts` + `package.json` — W4's type edit precedes W8's runtime edit; they never race.
- **→ KF.W5 (decision-upstream).** W5 owns which of the eight G2 symbols publish, `emit/index.ts`'s export list, `cssIdent` (N-8, APPLY-UNIT's blocker), and KF-AV-9's library half. **W8 may not decide the surface**; units c and d execute after the ruling.
- **→ KF.W6 (routing + guard).** R-1 above. KF-AT-26 legs (a)(b)(d) are W6's and are **not annexed**.
- **→ KF.W7 (verdict-upstream + routing + file-sharing).** The standing swap rider governs KF-AV-26 and KF-AV-24. R-3 above. N-11/m-7/L-19·C-14/C-12 sit inside the W7-named `instrument/timeline/` cluster — **W8's edits there are pure-move / pure-repoint with no behavior**, so they cannot invert W7's scrub-seam settlement. m-7's rename may not re-derive banked **C-15** (playhead = swap candidate; zoom/pan rail = no counterpart).
- **→ KF.W2/W3 (fold-by-reference).** KF-CE-12's R1 ingress note folds to the megatranche R1 identity; never re-booked here. MISS-β2 cross-refs W2's emit façade as a possible publication vehicle — if W2 mints it, the publication half discharges there.
- **→ CARD-UNIT (NO-WAVE-OWNER).** MISS-β2's publication **gates** banked KC-1 (whose cure names a non-exported symbol and does not compile as written; the compilable import today is the demo copy — the very reach the bank convicts). KC-30's move is pure-move so it cannot invert KC-34's highlight-before-blanking ordering; CARD-UNIT is sequenced after the KF.W4 vue-tsc gate.
- **→ EDITOR-UNIT (NO-WAVE-OWNER).** KF-CE-40 is the fourth barrel site; **one ruling across all four**, co-declared across the packet boundary. KF-CE-48's `monacoBoot.ts` extraction re-derives against KF.W8-I.
- **→ KFED-UNIT / APPLY-UNIT (NO-WAVE-OWNER).** KF-KE-15's repoint is a pure-import commit that does not race KFED-UNIT's repairs on the same files (`KeyframesEditor.vue`, `useKeyframesState.ts`, `useKeyframeOps.ts`). APPLY-UNIT's `cssIdent` dependency is W5's, though it rides the same surface decision.
- **→ KF.W9 / SS-13 (declared negative + one residue).** No row here carries a live witness — every gate is source-certain (greps, import graphs, barrel export lists, config files). **Residue**: KF-AV-8's bundle-weight half is UNPROVEN (kf-AnimationVisualizer residue #4) and is measured at W9/SS-13; **it may never be asserted as a benefit here.**
- **→ KF.W10 (identity guard).** KF-AT-26 leg (e) (`liftDown` doc phantom) is W10's. Every fold-by-reference identity carried here — census F-1/SCH-1, §6.3, §6.5, banked C-15, KC-1, N-6, U.B5, U.B2, L.W8 ED-3, `R1-05-demo-colocation.md:118` — is **cited, never re-minted**; W10 forbids re-booking. R-4's flatten arrives at W10 as a named act.
- **→ glass-ui BH relay (SS-6): NEGATIVE.** This wave routes **zero** producer rows, and none may become a demo-side hack. (Producer rows in the same source records — the DialogFooter gap, the registry suspend/modality LIFO/FIFO law, the keycap accessible-name companion, the aurora a11y sweep — belong to their own waves' relays, per the standing BH/BI relay edict.)
- **→ codex intake B10–B21: NEGATIVE.** No B-row lands in KF.W8 (the §3 CARRY table groups its 45 rows to KF.W0/W4/W5/W9/W10 only). Nearest adjacency C-6/X-4 (library counting bases) is **W5's**; this wave states its own structural denominators and defers the library denominator.
- **→ scene-repair packets (cube, sequence, spring, square, dock-menu): NEGATIVE, one contribution.** No packet is homed here; SPF-22 flows *out* of the spring packet into KAD-24. TransportDock is clear — TD-1/TD-2 stay NO-WAVE-OWNER, bundled, with their BH rider. **Name-collision guard: this wave's M-4 is kf-RibbonBar's, never kf-ChromeDock's (whose worded cure ships an unopenable menu without the MbabbMenu MUST-CARRY rider).**
- **→ docs/precepts (L-20 authority).** (1) `instructions/README.md:127-130` + Invariant 26 — tests outside `src/`; keyframes.js's 100%-external `test/` tree is **conformant**; "test-tree isomorphism" = mirror src shape *inside* `test/`. (2) `README.md:132-135` — doc drift is uncommitted work; every stale-comment rider lands in the same commit as its move. (3) Owner feedback `feedback_kiss_no_contrivance` + `feedback_no_god_modules` bind KF-AV-26's extraction and the ratified idiom's text.

## Excluded

No CARRY row is excluded — all 34 land above. The following adjacent material is *deliberately not carried*:

| not carried | reason |
|---|---|
| The 5 pure pass-through fragment shims + the `presets/` 4-hop re-export ladder | Same disease as R-4, but **census-flagged, not banked**. Annexing them is the defect this program exists to kill. **OWNER-CONFIRM required** before any wave takes them. |
| The 9 god modules + any `src/` LoC cure | KF.W5's tri-fold. |
| `cssIdent` / N-8 publication | KF.W5 (three APPLY-UNIT blockers depend on it). |
| The depcruise-over-demo gate arm; KF-AV-18's source-regex pin; KF-CE-16 vue-tsc; manifest hygiene | KF.W4. Authoring any of them here is a boundary breach. |
| **R-9** (the whole typecheck void) | NO-WAVE-OWNER; **not absorbed** by R-10's config edit despite touching adjacent files. |
| KF-AT-26 legs (a) `--wave-cycle`, (b) dead `var()` fallback, (d) `.sr-only` | KF.W6. Leg **(e)** `liftDown` doc phantom → KF.W10. |
| KF-CE-12's R1 ingress arm | Folds by reference to the megatranche R1 identity → KF.W2/W3. |
| KC-1's cure text | Stands as banked (CARD-UNIT); only its **citation** is corrected here, the dated record unrewritten (E-3). |
| The `sideEffects: false` "contradiction" | **Killed by K-10's read-down.** Carried so no spec re-argues it. |
| The ScrubberTimeline geometry mapping | **Closed at banked C-15's split verdict.** m-7's rename may not re-derive it. |
| Any bundle-weight or chunk-size claim | UNPROVEN (KF-AV-8 residue #4) — KF.W9/SS-13 measures it. |
| The library surface decision itself | KF.W5's. This wave measures (G2) and repoints; it does not choose. |
| RibbonBar bytes | G15 is spec-input only; the settlement is L-2/C-2's bundle. |
| The KfPillTabs excision (D-19-9's third up-import host) | Flagged "T.H gated-on-publish" in-tree; its up-import is **enumerated** in G5 and settles with the transport/M-4 bundle or the excision — not annexed as a new row. |
| Producer/glass rows in the same source records (DialogFooter gap, registry suspend/modality LIFO/FIFO, keycap accessible name, aurora a11y sweep) | Their own waves' BH relays. Never demo-side hacks. |
| Case counts (`1 051`) and the census `16` | No command was ever recorded (L-9). Uncited by any gate here. |

---

**L-18 rider (reference, not ceremony).** Green gates are not acceptance: this spec's implementation must survive two challenging gestalt passes and a fresh-Fable agglomeration. Likely bases against **this** wave: a deep import "extirpated" into a demo shim that forwards the same side-effectful module (G1 clause 2 exists for that); a barrel deleted while its chunk-rationale prose survives elsewhere (G4/G8); a colocation move that silently inverted CARD-UNIT's ordering by carrying one behavior line; a "ratified idiom" that blessed both directions; an orphan test adopted by a glob that also swallowed the bench files (G9's set comparison exists for that); a G5 pass that quietly dropped the third up-import site (D-19-9 exists for that). **L-19** governs every gate above — each names a literal command and a falsifier, and the fifteen are the minimum, not a suite. **L-20** re-validates this file against the current precepts at authoring, at amendment, and at each pass.
