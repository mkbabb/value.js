# X.KF.W9 — Safari Visual Audit (frontend half)

> **Provenance.** Agglomerated 2026-08-28 by the fresh-Fable fold seat (M-23) from two blind drafts, **authored from the 58-record registry directly; the routed denominator is the registry's (PASS-1 census)** — **79 §Carry bullets; 13 gates born-RED; the cross-edge set is enumerated at §Sequencing S-10 and its numeral is stated only there** (recounted at the bytes at repair round 2 — 76 at round 1, +3 from R2-10's hard escapes; the count is re-derived at every round, never incremented) (fold record in §Fold ledger). *(**CLOSURE CLAIM RE-HOMED 2026-08-28, repair round 3 — RULINGS-3 LAW B.** The authored *"79/79 rows carried, 0 dropped"* asserted, in this spec's own voice, a completeness claim over an enumerable set; LAW B forbids that shape. **Census carriage per `docs/tranches/X/keyframes/conformance/PASS-5/KF-W9-CHECK.md §1` (2026-08-28)** — *citation re-pointed at repair round 5, exactly as this sentence instructs and as LAW B requires; the readings it supersedes are retained as their rounds' dated measurements (PASS-3: 139/139/0 · PASS-4: 168/168/0)* — which re-enumerated the corpus by record at a granularity **larger again** and found **181 routed (record, id) pairs, bookedCount 181, escapedCount 0 — "AXIS 1 VERDICT: CLEAN"**, with four extractor candidates and nine route-(d) resolutions individually adjudicated at the bytes — and which also books **eight defects against this wave on other axes (2 HIGH · 5 MEDIUM · 1 LOW)**, cured at §Repair ledger — round 5. That artifact's own instruction travels with the citation: ***"No pass-6 seat should inherit 181 either."*** The artifact holds the claim; this spec cites it and does not re-assert it. When a later pass supersedes that check, the citation is re-pointed, never the claim re-written here.)* *Provenance correction (RULINGS R-12's general law, applied at a site the UNION §P-3 census did not enumerate): there is no in-tree `KF.W9 CARRY` ledger — the only in-tree carry is `carry/KF-W6-CARRY.md` (201 rows / 54 records). Every "carried from the KF.W9 CARRY" claim is struck; the basis is the registry.* Consumed by mechanism, deduped by identity, never transcribed (**M-25**); validated against `docs/precepts/` latest (**L-20**); no gate exists here without a named live witness (**L-19**). All anchors re-resolve at keyframes.js **`origin/master 81a56990`** per KF.W0 §B-12 (origin/master pins glass-ui **7.0.0 exact devDep = installed**; local HEAD `8281638c`, 1 ahead / 41 behind / 252 dirty, is **DISQUALIFIED as witness substrate** — with the TypingDots INVERSION carried, §Carry H). This is a *specification*: **status planned; execution awaits the owner's begin-word** — authoring it opened no keyframes.js product source.
>
> **Repair round 1 · 2026-08-28 (X·KF L-20, PASS-1).** The five directives addressed to this wave at `conformance/PASS-1/RULINGS.md` §END — **R-1e** (the AT/announcement arm carried), **R-9a** (the wave MEASURES; every cure arm leaves), **R-10** (the KF-AV-28 standing rider carried by id), **R-11** (`npm run gh-pages`, 3 sites), **R-15** (S-11 re-pointed at the canonical 17-packet roster) — are applied VERBATIM, and every defect `PASS-1/KF-W9-CHECK.md` books against this wave is cured or declined on the record (§Repair ledger). Where a check defect and a ruling collide the ruling wins. Rows carried from the registry keep their banked ids (anti-rename); cure-shape locks, sequencing riders and dissents are CARRIED, never cited (M-25).
>
> **Repair round 2 · 2026-08-28 (X·KF L-20, PASS-2).** The two directives addressed to this wave at `conformance/PASS-2/RULINGS-2.md` §END — **R2-10** (the three hard escapes booked: kf-AmigaScene **D-2** + **MISSED-A** into §F, kf-HeroAurora **KF-HA-10** into §G; counts **re-enumerated, not incremented**) and **R2-15** (the round-1 headline closure claim STRUCK and restated under the dated-correction idiom; the whole counting family recounted at the bytes) — are applied VERBATIM, and every defect `PASS-2/KF-W9-CHECK.md` books is cured on the record (§Repair ledger — round 2). **R2-5 (the witness re-derivation law) binds this seat**: every figure, witness and anchor touched below was RE-EXECUTED this session — keyframes.js at **`origin/master 81a56990`** (local `8281638c` consulted only to date drift), value.js at the current tree bytes — never inherited from a ruling's prose, a check's register, or this spec's own prior text. **R2-7 (the citation idiom)** applies to every cross-spec receipt this seat touched: the destination's §-heading, gate id or banked id is the anchor; a line number rides parenthetically as a declared non-load-bearing convenience. The Re-anchor seat sweeps the rest and writes LAST. *(**SCOPE CORRECTED 2026-08-28, repair round 3 — PASS-3 check D3-4, LAW B.** The sentence above was written as a **universal** and was **false for one enumerated family**: the §Bounds `kf library reference` row was absent from round 2's receipt block, and PASS-3 proved the claim could not hold for it — the file it names, `src/animation/engine/play-lifecycle.ts`, **does not exist at `81a56990`**, so no re-execution at that ref produced those coordinates. The universal is **struck and replaced by scope-with-enumeration**: each round's receipt block enumerates the families that round re-derived, and a family absent from the block was NOT re-derived that round. Round 2's block is at §Repair ledger — round 2; round 3's, which carries the library-reference family, is at §Repair ledger — round 3.)*
>
> **Repair round 3 · 2026-08-28 (X·KF L-20, PASS-3).** The four directives addressed to this wave at `conformance/PASS-3/RULINGS-3.md` §END — **R3-7** (the play-lifecycle witness family re-derived at the frontier layout; all five citing sites re-anchored; the KF-TD-1 SUBSTRATE-INVERSION row restated as a frontier fact), **R3-9** (G-KFW9-6's witness restated truthfully — `reducedMotionScale` has three live LIBRARY consumers; the in-tree spring-lane precedent NAMED), **R3-10.4** (the W10-seam edge: the KF.W10 cross-edge at S-10, the OD-V3 capture packet + OD-V5 390 at-rest observation entered at S-8 with the **1280** arm, the four duplicating scenes and both transport homes enumerated, and the §Bounds surface-receipt row), and the local tail **D3-4 · D3-5 · D3-6 · U-2(c)(3) · D3-8** — are applied VERBATIM. Two program laws bind every edit this round: **LAW A** (a delete/repoint/shim act's consumer census is derived from the import graph at the frontier, pasted with its outputs — never from line counts, never from a CARRY's or a spec's own text) and **LAW B** (no spec asserts a completeness/closure/zero-escape claim in its own voice; the claim lives in the freshest conformance census artifact, cited by path and date, **including its defects**). This wave holds **zero write grants in the kf tree** and authors no delete/repoint/shim act of its own; the three such acts it **carries as declarations for other waves** are censused at §Repair ledger — round 3 §LAW-A, because LAW A binds a seat that *carries or leaves standing* such an act, not only one that writes it.
>
> **Repair round 4 · 2026-08-28 (X·KF L-20, PASS-4).** The seven directives addressed to this wave at `conformance/PASS-4/RULINGS-4.md` §END — **R4-3** (G-KFW9-6's CLOSES re-cut on the G-KFW9-9 template; the unification constraint HANDED to KF.W5/KF.W6 as a declared sequencing obligation, booked at both ends), **R4-4** (the three LAW-A censuses re-run, the four wrong cells corrected, the blast-radius hand-off re-issued to the R4-8 successor-formation register), **R4-6** (*"fourteen folds"* corrected at the enumeration and at all three dependent cells, the gate GREEN included), **D4-4** (`shadowTick` decl **`:53`**, both sites and the receipt), **D4-5** (the §Bounds immutability cell split), **D4-6** (the self-falsifying zero) and **D4-7** (`src/animation/physics/numeric.ts:82-89` in full) — are applied VERBATIM, and every defect `PASS-4/KF-W9-CHECK.md` books is cured on the record (§Repair ledger — round 4). **This wave writes in STAGE 2** (LAW C(1)), so: every stage-1 sibling receipt below is re-verified **at that sibling's FINAL stage-1 bytes** and rides the pasted `grep -n` that produces it, with the file's mtime named (**LAW C(2)**, quote-by-command); and every receipt whose source wave writes LATER — **KF.W10, which writes last of the per-wave seats** — is reduced to its **stable anchor alone** (§-heading + row/gate id; no quoted prose, no line numbers), for the RECONCILE seat to verify after the last write (**LAW C(3)**). **LAW D** binds every quotation and every figure: a *verbatim* label means byte-diffed-at-write-time by a pasted command, and every numeral printed beside a command was produced by **re-running that command this seat** — the numeric arm's head case is this wave's D4-1 (§Repair ledger — round 4 §LAW-A). **R4-10's two scope arms are executed by SCOPE, not by instance**: LAW A's enumeration source is the §Bounds **access column** (grepped, with a scope receipt), and SUBJECT-IDENTITY's is **every symbol-bearing §Bounds row and every gate-witness symbol**, whole file (both receipts at §Repair ledger — round 4 §SCOPE). Nothing here stamps a verb, opens product source, or grants a byte: status stays `planned`, the kf tree stays read-only, and the round does not close until the RECONCILE seat writes `PASS-4/CLOSE-CERT.md` — which this file does not and may not certify for itself (LAW B). ⟨**CITATION RE-POINTED at repair round 5 — RULINGS-5 R5-2(3); PASS-5 check D5-1.** `PASS-4/CLOSE-CERT.md` is **VOID as proof-of-order**: its §1 mtime table — the LAW C(4)(i) exhibit that proves the certifying seat wrote last — tabled `KF-W10.md` at 17:31:54 / 209,902 B, while that file stood at **17:38:21 / 209,923 B** — **+21 bytes and 6 min 27 s later than the state certified, and later than the certificate itself (17:37:59)** (`stat -f '%N %Sm %z'`, two commands, run by the PASS-5 check; the same state is pinned independently as this round's opening baseline by RULINGS-5 **LAW E(3)**, sha `086ea228…`, so the figure rests on no clock this seat keeps — and `KF-W10.md` has moved AGAIN since, its round-5 seat writing while this file is repaired, which is the class rather than an exception). RULINGS-5 R5-2 attributes the twelfth write **to the RECONCILE seat itself** from the workflow transcripts (one `Edit KF-W10.md` at 21:38:21.804 Z, a rewording of its own receipt cell, +21 B exactly) and rules: **round 4's repairs BANK; its close certificate does not.** The lawful close of rounds 4 AND 5 is **`PASS-5/CLOSE-CERT-2.md`**, hash-proven under **LAW E** (write-last by construction; sha256 of all 11 specs; pass-6 re-hashes as its first act). Every closure condition in this file reads to CLOSE-CERT-5 + R5-2 from this round forward; **E-3 holds — the round-4 artifact is NOT edited, and this line is the re-point, not a rewrite of it.**⟩
>
> **Repair round 5 · 2026-08-28 (X·KF L-20, PASS-5).** The eight directives addressed to this wave at `conformance/PASS-5/RULINGS-5.md` §END — **R5-8(a)** (the W10 delegation replaced by this pass's verification, the six anchors carried BY COMMAND), **R5-8(b)** (the anchor set enumerated ONCE at S-10 — **six**, with its counting rule; §Bounds points there), **R5-8(c) + D5-1** (closure re-pointed to **`PASS-5/CLOSE-CERT-2.md` + R5-2**, the round-4 seal being VOID as proof-of-order), **D5-4** (the `KF.W3` figures restated at one enumeration, R5-11), **D5-5** (census 1's four docblocks restated as what they are — three carve-origin provenance lines and one live specifier, no stale prose), **D5-6** (the standing census verdict re-pointed to `PASS-5/KF-W9-CHECK.md §1`, **181/181/0**, at §F **and across the whole citation class**), **D5-7** (the gate-witness warrant re-scoped to what its artifacts actually re-executed), **D5-8** (both banked aliases spelled) — plus **R5-1(5)**'s guard-vindication line — are applied VERBATIM, and every defect `PASS-5/KF-W9-CHECK.md` books is cured on the record (§Repair ledger — round 5). **Two new program laws bind this seat. LAW E**: the certification is write-last BY CONSTRUCTION and hash-proven, per-seat substrate stamping is RETIRED, every same-round cross-wave receipt a per-wave seat writes is **anchor-only**, and the sweep roster gains the **stage-2 → stage-2** class this wave's seam fell through. **LAW F (mechanism S)**: every act that strikes, moves, drops, re-keys, narrows, re-points or mints books its **SHADOW line in the same edit**, with **per-row anchors, never per-carrier** — the discipline whose absence made R4-1 strike three rows whose defects are live at the shell (**R5-1**, the round's BLOCKER; this wave's guard said *"do not shoot the fork"* and is vindicated at §G). Nothing here stamps a verb, opens product source, or grants a byte: status stays `planned`, the kf tree stays read-only, **zero rows moved and zero routings minted this round** (§Repair ledger — round 5 §SHADOW), and this file certifies nothing about its own siblings (LAW B).


## State

**Name**: KF.W9 — Safari Visual Audit · the PRM and Forced-Colors Horizon
**Half**: FRONTEND — the keyframes.js demo surface. The PRM unification's ENGINE half is KF.W5's; producer bytes are SS-6's (BH relay), never touched here.
**Opens after**: KF.W0 §B-12 names the execution substrate (OP-1, blocking). KF.W4 is **not** a precondition (OP-6) — this wave measures, it does not type-cure. **After RULINGS R-9a every cure arm has LEFT the wave** (the two-deletion act + PlaybackRibbon D-12's stopgap bytes → KF.W13's transport/ribbon packet; ST-1's affordance edit → KF.W11's sequence packet), so OP-6 is no longer merely declared: it is **true by construction**, and the KF.W4 sequencing head lawfully does not bind a pure measurement wave.
**Agents**: 5 seats across 4 sequential phases — `.a` serial chassis (substrate pin, surface list, the two registers; nothing dispatches until it commits) → `.b`/`.c`/`.d` parallel, **cell-disjoint** (desktop-Safari · mobile-Safari/iOS · contrast/forced-colors/AT) → `.e` serial (write-backs, census amendment, BH-relay evidence packet) → close. **Peak concurrency 3** — inside the owner's four-workflow cap. Probe parsimony (owner edict 2026-07-12) is a LAW block on every seat.
**Status**: **planned** — status fields stay `planned`; NOTHING in this file opens product source.

| verb | state | evidence |
|---|---|---|
| AUDITED | YES | 58/58 `registry/adjudicated/kf-*.md` banked · `CENSUS-2026-08-03.md` · `lane-frontend.md §6.5` · `INTAKE-ADJUDICATION-2026-08-03.md §3` |
| SPECIFIED | YES — by this file (agglomerated) | — |
| IMPLEMENTED | NO | stamped only when gates go green after the begin-word |
| VERIFIED | NO | stamped only at X·KF close; no wave stamps VERIFIED at its own close |

## Goal criterion

This wave succeeds if, when work ends, **the keyframes.js demo has been seen by a real Safari** — mobile and desktop, at a named substrate, in labelled cells — and **every one of the ≈590 banked UNPROVEN-NEEDS-LIVE probes has reached a terminal state**: EXECUTED (capture path + per-shot sha256 + substrate ref + cell), RETIRED (naming the ruling), or UNREACHABLE-IN-CELL (naming the UA capability that forecloses it). Concretely: one **PRM enumeration** replaces census §6.5's tally; the authored `outline: none` **REMOVAL** is brought to its born-RED witness (focus a ribbon button under forced-colors, assert NO indicator) with the **two-deletion cure itself routed to KF.W13** (RULINGS R-9a/R-15 — the transport/ribbon packet owns `playback-idiom.css`; `design-idioms.css`'s copy rides the same single act, K-5 intact) and the after-witness re-shot here once it lands (S-9 discipline); **the demo-side `forced-colors` count is expected to stay 0 and THAT IS the recorded result** — raising it would require authoring demo-side `forced-colors` rules, which the standing edict (*glass-producer rows ride the SS-6 BH relay and NEVER become demo-side hacks*) and KF-ET-4's cure lock (*the fix is one `@import`; NEVER a demo-side hack*) both forbid, and which no §Carry row owns; and the **thirteen** provisional severities (the §H escalation register's own enumeration, recounted at the bytes at repair round 2 — R2-15; the round-1 "fourteen" is struck) either close with a measurement written back under their original ids or are reported PROVISIONAL at close. **A wave that produces screenshots without cell labels, per-shot hashes, and a named substrate has failed** — that is I-20's convicted failure, twice fired, not a measurement.

## Scope (three named deliverables + one backlog)

1. The demo visually audited on **real Safari, mobile + desktop** — the horizon's named condition.
2. **PRM mechanism unification, intensity form** (`respectReducedMotion: ~0.3` via `reducedMotionScale`, never a binary snap).
3. The **demo-wide forced-colors gap** (≡ KF-CE-13), headed by the **born-RED MEASUREMENT** of the authored focus-ring REMOVAL — **the two-deletion cure bytes are KF.W13's** (R-9a/R-15); this wave holds the before/after witnesses and re-shoots after the act lands.
4. **ALL SS-13 UNPROVEN-NEEDS-LIVE residue from all 58 adjudicated records** — **≈590 probes** (re-derived at the bytes at repair round 2: **565 enumerated + 25 prose-carried**; the round-1 ≈588 rested on the superseded 563 cell — R2-15), the single largest concrete backlog in the registry, 0 executed today.

**Standing edicts carried whole**: comment-stated invariants are test obligations (the kf-SquareScene law) · glass-producer rows ride the SS-6 BH relay and NEVER become demo-side hacks · the **17 canonical NO-WAVE-OWNER packets** (RULINGS R-15's roster, enumerated at §Sequencing S-11) are NO-WAVE-OWNER — this wave DECLINES their behaviour, HOLDS their witnesses, and states their homing (KF.W11 ×9 · KF.W12 ×6 · KF.W13 ×2, homed by KF.W4) in §Sequencing S-11 · **KF-AV-28's standing supersession rider** (§H) governs every carried NO-WAVE-OWNER row banked under a KF.W7-evaluated surface.

## Open preconditions (checked at wave-open; NOT close gates)

| # | precondition | state at authoring | check at open |
|---|---|---|---|
| **OP-1** | **KF.W0 §B-12 names the execution substrate**, carrying the TypingDots inversion (the dirty worktree IS the frontier for the stagger surface; the inversion flips the WAAPI-vs-rAF lane and the PRM prediction). | UNSETTLED — hard blocking edge; a capture against `8281638c` is void by ruling. | W0's pasted re-anchor table, inversion stated. |
| **OP-2** | **Write authority for keyframes.js is named** (census §(c) gap 5, inherited from KF.W4 OP-1). This wave *reads and renders* a built demo; it writes evidence into **value.js**. | UNRESOLVED for any cure; not blocking for read-only capture at the named ref. Stated so the boundary is not assumed. **RESTORED TO TRUE at repair round 1 (R-9a item 3)**: §Bounds' five EXECUTION-TIME-ONLY kf write grants are STRUCK, so this wave now grants itself no authority it records as un-granted (KF-W9-CHECK D-5 cured). | The owner's dated word — no inference from this file. |
| **OP-3** | **A real Safari cell is reachable at all**: `safaridriver --enable` on the capture host; Develop ▸ Allow Remote Automation on. | UNVERIFIED. The only real-Safari corpus on disk (2026-07-27, Safari 26.4 / macOS 26.4.1, `localhost:9000`, API-less) is **value.js routes**, pre-X — not keyframes. | A trivial session open, recorded. Without it the wave cannot open (G-KFW9-1). |
| **OP-4** | **UA-capability feature tests, asserted not assumed**: in each cell evaluate `matchMedia('(forced-colors: active)').media`, `('(prefers-reduced-transparency: reduce)')`, `('(prefers-reduced-motion: reduce)')`. If a query does not *parse* in the safari-app cell, every affected row's safari column is UNREACHABLE-IN-CELL **by UA capability, not by omission** — measured, never inherited from this sentence. | UNMEASURED. | The three `.media` strings, recorded per cell, in the sidecar. |
| **OP-5** | **A build exists at the named ref**: **`npm run gh-pages`** re-run before *any* byte-offset receipt is trusted (**R-11**: `build:gh-pages` exists at no coordinate — the real script is `package.json:43` → `"gh-pages": "vite build --mode gh-pages"`; the `KF_ANALYZE=1 vite build --mode gh-pages` form in §Excluded was already correct) (the Jul-16 witness may predate glass-7) — kf-CSSPasteDialog #8 · kf-KeyframeTimeline #13 · kf-TimelineCaret #12 · kf-TimelineTrack #11. Installed-dist re-derived cells are EXEMPT and say so. | STALE. | A fresh build at the pinned sha, its hash recorded. |
| **OP-6** | **KF.W4's gate chassis is NOT a precondition** — declared so the wave is not falsely blocked. | **STANDS, AND NOW TRUE (R-9a item 5)**: at authoring the declaration was in tension with G-KFW9-10, which landed a packet-owned affordance edit before the declared sequencing head (**KF-W4 §Sequencing**, the head-law paragraph — *"This wave is the declared sequencing head of X·KF. No repair packet and no UNIT may open before G-KFW4-1 lands"*; **numeric convenience STRUCK at round-2 close (U-2/R2-7)** — the round-2 spelling `:223` had drifted onto KF-W4's `Unit .e` heading; the §-heading and the quoted paragraph are the sole anchor and no line number rides this receipt. **Re-anchored at repair round 2 (R2-7; the round-1 `KF-W4.md:193` coordinate died when KF-W4 was repaired in the same round and, at those bytes, pointed at G-KFW4-12's manifest-hygiene row).** ⟨**round-2 close re-verification (U-2)**: the characterisation of the struck coordinate above was measured at the repair-round-2 bytes; the sibling has moved again since and it no longer resolves to what is described. **Recorded, not re-derived — a struck coordinate is never re-adjudicated**, and the strike stands.⟩) With every cure arm gone — the two-deletion act to KF.W13, ST-1's edit to KF.W11, the five write grants struck — W9 is a **pure measurement wave** and the KF.W4 head lawfully does not bind it. | — |
| **OP-7** | **The AT-cell scope question is OWNER-UNRULED** (≡ §Sequencing S-12). `.d`'s AT arm runs only under the proposed resolution; if the owner routes AT to its own lane, `.d` sheds that arm and affected rows report UNREACHABLE-IN-CELL with the routing named. | UNRULED — recorded as a dissent, not a decision. | The owner's ruling. |

## Bounds

All evidence lands in **value.js**; this wave writes no evidence into the kf tree.

| Path (absolute unless noted) | Access | Note |
|---|---|---|
| `/Users/mkbabb/Programming/value.js/docs/tranches/X/keyframes/waves/KF-W9.md` | create | this spec; status `planned` until the begin-word |
| `…/docs/tranches/V/megatranche/audit/visual/safari-real/` | create/write (`.b` `.c` `.d`) | the real-Safari evidence cell. **Born-RED, measured**: 4 tracked (`MATRIX-SAFARI.md`, `ROUTE-MATRIX.json`, `STATE-MATRIX.json`, `picker-safari26.4-light.png`) vs **31 on disk** — 27 witnesses uncommitted and therefore nonexistent (L-7). Every capture **force-added**, per-shot **sha256** sidecar |
| `…/audit/visual/REPORT.md` · `REPORT.json` · `STATES.json` | modify (`.a` skeleton, `.e` fold ONLY) | the scoped surface list + per-cell results |
| `…/audit/visual/shots/` | modify (`.b` `.c` `.d`) | also born-RED: 11 on disk, **0 tracked** — same L-7 class |
| `…/audit/visual/capture.mjs` · `states.mjs` | modify-carve (`.a` alone, serial) | the ≈590-probe surface list + I-20 cell labels; lands before phase 2 dispatches |
| `…/megatranche/workflows/safari-real-matrix.js` | **EXECUTE, NO WRITE** | safaridriver harness; **shared apparatus with X-W11 G8, SEPARATE cells**. A harness change needed to carry a cell label is a **bounds expansion → triumvirate**, never a quiet edit |
| `…/docs/tranches/X/keyframes/evidence/W9/**` | create (all seats) | per-seat capture indices, the two registers, the PRM enumeration, the census-amendment draft |
| `…/megatranche/registry/adjudicated/kf-*.md` (58 records) | **read-only for the EXISTING bytes (E-1); APPEND-ONLY for dated addenda under original ids (E-3), by `.e` alone** | **CELL SPLIT at repair round 4 (RULINGS-4 §END KF-W9 D4-5; PASS-4 check D4-5).** The authored cell read *"read-only, IMMUTABLE (E-1/E-3)"* while **G-KFW9-13 can close only by writing into these files** (*"each trigger's measured value + resulting severity written back as an ADDENDUM under the ORIGINAL id"*) and **S-13** arms its triumvirate on *"any adjudicated-record edit **that is not an addendum under its original id**"* — a carve-out that is meaningless unless addenda under original ids ARE writes this wave may perform. The Access column and the gate contradicted each other on one path: a seat that wrote the addendum breached §Bounds, a seat that did not could never close the gate. **The split is what S-13 and G-KFW9-13 already assume, made explicit**: the existing bytes are immutable — no edit, no re-grade, no renumber, no deletion, ever — and the ONLY lawful write is an **append** of a dated addendum under an original banked id, by the **`.e` seat alone** (serial; the disjointness rule at §Disjointness governs). An addendum that edits an existing line, or that mints a new id, or that lands from any seat but `.e`, is a bounds expansion → triumvirate. G-KFW9-5's shape is unchanged and remains consistent (there the amendment lands as a **DRAFT under the `evidence/W9/**` create row** (*"the census-amendment draft"*), so the census file's `read-only` is never breached and no split is owed ⟨***ground corrected at repair round 6, PASS-6 D6-5: the round-4 ground ~~"§Bounds says `read-only` of `lane-frontend.md §6.5` specifically"~~ is FALSE at the cell it describes — `:70`'s `read-only` attaches to BOTH paths, the census file included, and the word that singles out the lane file is "never rewritten". The conclusion was right and was resting on the wrong one of its two grounds — the D5-5 shape, and the D4-5 contradiction's last unswept sibling.***⟩) |
| `…/megatranche/formation/keyframes/CENSUS-2026-08-03.md` · `lane-frontend.md §6.5` | **read-only (both paths) — the amendment lands as a DRAFT elsewhere** | **`CENSUS-2026-08-03.md`: read-only HERE; the census row it amends (G-KFW9-5) is drafted under the `evidence/W9/**` create row and lands from seat `.e`, so this wave writes no byte of the census file. `lane-frontend.md §6.5`: read-only AND never rewritten — dated evidence, no amendment of any kind.** ⟨***the two clauses are attached to their own paths at repair round 6 (PASS-6 D6-5); the round-4 cell put one access word over two paths and then distinguished them the other way round.***⟩ |
| `…/audit/codex-provenance/INTAKE-ADJUDICATION-2026-08-03.md` · `intakes/lane-keyframes-b10-b21.md` | read-only | B18-26's numerator/denominator ruling |
| kf `demo/styles/design-idioms.css:76-79` + `demo/styles/playback-idiom.css:72-75` | ~~EXECUTION-TIME ONLY~~ → **READ-ONLY WITNESS SURFACE** | **GRANT STRUCK (R-9a item 3).** The two-deletion act is **KF.W13's** (transport/ribbon packet owns `playback-idiom.css`; the `design-idioms.css` copy rides the SAME single act — K-5: one deletion fixes nothing). G-KFW9-9 measures both offsets and holds the before/after pair |
| kf `demo/scenes/sequence/SequenceTarget.vue:97` + `SequenceTarget.css:160-167` | ~~EXECUTION-TIME ONLY~~ → **READ-ONLY WITNESS SURFACE** | **GRANT STRUCK (R-4/R-9a item 1).** ST-1's edit travels with the **sequence packet to KF.W11**, sequenced after G-KFW4-1; this wave keeps ST-1 as measurement + held witness. S-13's triumvirate trigger (*any behaviour edit in a packet/UNIT-owned component*) is thereby never armed by this wave's own gates |
| kf `demo/app/index.html:6` | ~~EXECUTION-TIME ONLY~~ → **READ-ONLY WITNESS SURFACE** | **GRANT STRUCK (R-9a item 3).** D-25's notch witness is this wave's (`kf-EditorShell.md:70` routes the *witness* only); the `viewport-fit=cover` edit is a CURE with no named write authority under OP-2 and is not spent here |
| kf PRM surface: `demo/scenes/amiga/AmigaScene.vue` (:58, :107-112) · `cube/useCubeDemo.ts:159-169` + `CubeScene.vue` · **`demo/scenes/cube/orbital-drag/OrbitalDrag.vue`** (rAF glide — *path written in FULL at repair round 3, PASS-3 check D3-8: the row's shorthand base is `demo/scenes/`, under which the authored `cube/OrbitalDrag.vue` resolves at NEITHER ref; `git ls-tree -r --name-only origin/master \| grep OrbitalDrag.vue` → `demo/scenes/cube/orbital-drag/OrbitalDrag.vue`, this seat*) · `square/*.ts` + `SquareScene.css:136` + `SquareInstrument.vue:207` · `spring/SpringTarget.vue:462` + `SpringHeatmap.vue:333` + `StartingStyleTarget.vue:211` · `sequence/useSequenceDemo.ts:128-132` + `SequenceTarget.css:238` · `easing/EasingTarget.css:48` + `EasingTarget.vue:234` · `components/instrument/shell/AnimatedText.vue:121` · `app/App.skeleton.vue:95` · `…/controls-pane/ControlsPaneWrapper.css:144` · `KeyframesEditor.vue:254-258` + `KeyframesAddDialog.vue:128` + `useKeyframeBrushApply.ts:18-27` | ~~EXECUTION-TIME ONLY~~ → **READ-ONLY REFERENCE** | **Consequential to R-9a**: the wave ENUMERATES flag state and layer at these coordinates (G-KFW9-5/-6); every PRM cure is KF.W5's (engine), KF.W6's (tokenization), or a packet's — a standing write grant here would re-open the OP-2 breach the ruling closed. **Path resolution per `PASS-3/KF-W9-CHECK.md §4` (2026-08-28)**, which re-executed this row's members at `origin/master 81a56990` and found them resolving — *and which convicted its one non-resolving member, `cube/OrbitalDrag.vue`, at **D3-8**; that member is repaired above.* *(LAW B, repair round 3: the row's former self-issued universal — "Paths re-resolve at `origin/master 81a56990`" — was falsified by its own member, so the warrant now cites the artifact that measured it, with the artifact's finding stated including the defect. KF.W0 §B-12 remains the substrate authority; it is not the measurement.)* ⟨**FRESHNESS SWEPT at repair round 5 — D5-6's CLASS arm (RULINGS-5 R5-9 row 16 / R5-11).** The freshest measurement of this row is `PASS-5/KF-W9-CHECK.md §3` (2026-08-28), which re-executed the members at `origin/master 81a56990` and records **§Bounds PRM surface: 15 of 15 paths resolve**, D3-8's repaired member included. The PASS-3 citation is retained as the **dated origin of the cure**, not as the standing measurement — the partition rule is stated at §Repair ledger — round 5 §SCOPE, where the whole citation class is enumerated by command.⟩ |
| kf library reference (base `src/animation/`, **every member re-derived at `origin/master 81a56990` at repair round 3 — R3-7/D3-4**): `index.ts:36-52` (`reducedMotionScale` on the LIGHT barrel, the export at `:50`) · **`engine/play-lifecycle/frame.ts` — `playFrame` (decl `:121`), its `withReducedMotion` flip (`:131`) and the `snapToReducedMotion(anim)` call (`:137`)** · **`engine/play-lifecycle/strategies.ts` — `snapToReducedMotion` (docblock `:66-75`, decl `:76`) and the final-frame `beginPlay(anim._playback, () => withReducedMotion(…` (`:109`)** · `group/group.ts:57` · **`src/animation/waapi/delegation.ts`** (`shadowTick` decl **`:53`**; `animation.playback.loop(shadowTick)` `:64`) · `constants/defaults.ts:87` | **READ-ONLY REFERENCE** | **PINPOINT CORRECTED at repair round 4 (RULINGS-4 §END KF-W9 D4-4; PASS-4 check D4-4; R4-10's SUBJECT-IDENTITY arm).** The authored `shadowTick` declaration `:54` is **one line late and was never right** — re-run this seat: `git grep -n 'shadowTick' origin/master -- src/animation/waapi/delegation.ts` → **`:53`** `const shadowTick = (now: number): boolean \| Promise<boolean> => {` and **`:64`** `animation.playback.loop(shadowTick);`; `git show origin/master:src/animation/waapi/delegation.ts \| sed -n '54p'` → `if (animation.done) return false;`, the body's first statement, not the declaration. **Identical at the disqualified `8281638c`, so this is not stale-ref drift**: round 3 kept `:54`/`:64` *"as measured"* while measuring only one of the pair, and the §RECEIPT block certified the family re-executed while printing the unmeasured half — the fault is the receipt's, not the substrate's. `:53` is written at **both** citing sites (here and §A's KF-TD-1 row) and in the round-3 §RECEIPT row, per LAW D's numeric arm (a figure beside a pasted command is the command's output). The **substance is untouched and independently re-verified this seat**: `git grep -c 'withReducedMotion' origin/master -- src/animation/waapi/delegation.ts` → **exit 1, no hits**, so KF-TD-1's *"`shadowTick` never consults it"* stands. KF.W5 owns any cure. **RE-ANCHORED 2026-08-28, repair round 3 (RULINGS-3 R3-7; PASS-3 check D3-1 — the convicted class).** The authored `play-lifecycle.ts:214/:337-341/:376` resolved at the **DISQUALIFIED** `8281638c` ONLY: at the frontier `git cat-file -e origin/master:src/animation/engine/play-lifecycle.ts` **fails** and the module is a **DIRECTORY** — `engine/play-lifecycle/{events,frame,index,strategies,transport}.ts` — carved at **`53b907c5`** (*"refactor(lib/engine): carve engine/play-lifecycle — the 24 free functions module"*, `git log --oneline -1`, this seat). The `./play-lifecycle` **specifier is unchanged** and resolves to the dir barrel: `engine/animation.ts:37` `import * as playback from "./play-lifecycle";` + `engine/option-setters.ts:27` `{ shouldReverse }` — the ONLY two import consumers in `src/ demo/ test/ scripts/` (LAW A census, §Repair ledger round 3 §LAW-A). Anchors are **file + symbol**, R2-7 idiom; line numbers ride parenthetically. **`delegation.ts` written in full** (PASS-3 check D3-8: the row's base is `src/animation/`, under which the authored `delegation.ts` resolves at neither ref — the file is `src/animation/waapi/delegation.ts`; the authored `:50` struck as unresolvable to a named symbol — ⟨**round 3's accompanying claim that "its `:54`/`:64` offsets were sound and are kept" is SUPERSEDED at repair round 4, D4-4**: `:64` was sound, `:54` was not, and the pair was carried as a pair without the declaration being measured. The **path** cure D3-8 landed is untouched and correct; only its offset half is corrected, above⟩) |
| `…/megatranche/audit/visual/safari-real/SS-13-CAPTURE-RECEIPT.md` | **create (`.e`, serial)** | **THE SURFACE RECEIPT — added at repair round 3 (RULINGS-3 R3-10.4; PASS-3 check D3-3); the inbound receipt reduced to ANCHOR-ONLY at repair round 4 (LAW C(3)).** `KF-W10.md` names KF.W9/SS-13 a **hard opening precondition** at the anchor set **enumerated ONCE at §Sequencing S-10** — *this cell POINTS THERE and re-issues neither the list nor its numeral* (**re-cut at repair round 5 — RULINGS-5 R5-8(b); PASS-5 check D5-3**: this cell said *"these five anchors"* while naming five WITHOUT the PACKET-FIRST law, and S-10 said *"five"* while naming six WITH it and §B-1/§B-3 merged — one numeral, two enumerations, union **six**, and a third party asked to verify *"these five"* against no single list. The file's own remedy, now applied for the fifth time: **state the count at its enumeration and have dependent cells point there**). *No W10 prose is quoted here and no W10 line number is carried: **that spec writes LAST of the per-wave seats this round**, so a quotation taken at these bytes would be a receipt against a substrate that moves after this file closes — the R4-7/U4-C failure mode, at the seam it fired on, and from round 5 forward **LAW E(4)'s anchor-only rule** program-wide. **THE DELEGATION THAT STOOD HERE IS STRUCK AND REPLACED BY A VERIFICATION THAT HAPPENED** (RULINGS-5 R5-8(a); PASS-5 check D5-2): round 4 delegated these anchors to the RECONCILE seat, and `PASS-4/CLOSE-CERT.md` **never swept the class** — its §3 `KF-W9` row counts five BACKWARD receipts (KF-W5 ×2 · KF-W6 ×2 · `KF-W6-CARRY.md`), its §7 forward roll-call carries **no `KF-W9 → KF-W10` row** under a scope sentence reading *"all **stage-1 → stage-2** forward references"*, and its §4 R-8 runs the OTHER direction (W10 → W9). `PASS-5/KF-W9-CHECK.md §2` verified all **six** against `KF-W10.md`'s bytes instead and found every one resolving; that verification is carried **by command at S-10** as this round's baseline, and **CLOSE-CERT-5 re-verifies from it** — the **stage-2 → stage-2** class being a named sweep class from this round forward (**LAW E(5)(ii)**). The round-3 quotations are retained only in that round's ledger as dated historical readings, and this wave's closure conditions on **`PASS-5/CLOSE-CERT-2.md` + RULINGS-5 R5-2** (the round-4 certificate is VOID as proof-of-order — its own author moved `KF-W10.md` +21 B / +22 s after certifying write-last; the round-4 repairs BANK, the seal does not).* The asymmetry those anchors named was FALSE from this end because this wave's §Bounds had no such row. It does now. The receipt indexes, by capture path + per-shot sha256 + cell + substrate ref: (a) the **OD-V3 capture packet** — both transport homes on all four duplicating scenes at **390 AND 1280** (S-8 family (iii)); (b) the **OD-V5 390 at-rest observation**, recorded *"either way"*; (c) the **glass §4 dock-contract re-verify** cell that **KF-W10 §B-3** re-homes here (anchor-only, LAW C(3)) — subsuming **CH2-02 ×4 — BG-5 · GU-1 · GU-2 · subject-legible**, re-run at the frontier this seat under LAW D: `git grep -n 'CH2-02' origin/master -- docs/tranches/V/FOLD-FORWARD.md` → `:34` *"**Glass §4 dock contract** (PEEK is NOT native; binary FSM + opacity crossfade; manual-mode suppression): our dock consumption re-verifies against THIS shipped contract — subsumes the folded CH2-02 ×4 (BG-5, GU-1, GU-2, subject-legible)."* (a dated authority at a pinned ref, not a moving sibling — quotable in full) — whose sibling half is KF.W6's TransportDock bounds rows; (d) the **V-A95 aurora surface verify** that **KF-W10 §B-1** routes here (anchor-only). **This wave produces the receipt and rules nothing**: OD-V3/OD-V5 are OWNER-GATED at KF.W10's own owner block — *"Never proxied"* |

**Do NOT touch** — **any keyframes.js byte**: after R-9a this wave holds **ZERO write grants in the kf tree** (all five EXECUTION-TIME-ONLY grants struck; every kf coordinate above is read-only witness surface), and a kf write is a bounds expansion → triumvirate (S-13) · any glass-ui producer byte (SS-6 BH relay only, standing owner edict) · any demo BEHAVIOUR owned by a NO-WAVE-OWNER packet or UNIT · `docs/tranches/T/**` (dated evidence) · the megatranche coordination tree (append-never-rewrite) · `scripts/dev/dev.sh` in either repo (unowned, NEVER).

**Disjointness**: `.b` writes only `safari-real/desktop-*`; `.c` only `safari-real/mobile-*`; `.d` only `safari-real/hcm-*`/`at-*`. `REPORT.*` is written only by `.a` and `.e` — two seats editing the surface list is how one surface list becomes three.

## Surface-list protocol (the wave's own product — G-KFW9-3/-12's subject)

1. **The denominator, adjudicated (B18-26)**: `152 × 11 × 4 × 11 = 73,568` is a Kronecker self-cross-product with no product consumer (the handoff's split `46,816 + 26,752` = 73,568 exactly). Ruled verbatim: **inherit the NUMERATOR** (zero real-Safari execution), **REJECT the DENOMINATOR; KF.W9 scopes its own surface list** — published before any fraction is quoted.
2. **The surface list IS the banked residue**: **≈590 = 565 enumerated** UNPROVEN-NEEDS-LIVE line-items **+ 25 prose-carried** (kf-CubeAxisLines' r1 nine-item queue with three sharpenings, `:104`; kf-TransportDock's "1–8 carried from r1", `:122`; kf-CubeTarget's r1 items 1–4/6–8 + amended 5, `:83` — all three re-read at the bank this round). **CORRECTED 2026-08-28 (repair round 2, R2-15/R2-5):** the round-1 cell **563** is struck; the enumerated figure re-derived at the bytes this session is **565**, over **57** of the 58 records (kf-CubeAxisLines carries zero enumerated items — its whole residue is the prose-carried r1 queue), by the reproducing command *per record, count `- ` / `N. ` top-level items inside every `##` **or `###`** heading matching `UNPROVEN` / `SS-13`, excluding identity-guard sections*. *(**COMMAND AMENDED 2026-08-28, repair round 3 — RULINGS-3 §END KF-W9 D3-6.** The published command specified `##` sections ONLY and therefore **could not reproduce its own figure**: executed literally at the bytes this seat (two variants, strict-top-level and indent-tolerant) it returns **563** — the struck round-1 number — while the `##`-or-`###` form returns **565 over 57 of the 58 records**, both measured this session by one script, printed side by side: `h2-only: 563 · h2+h3: 565 · records with items: 57 · files: 58`. **The missing two are named so the delta cannot regrow**: `kf-ChannelOptions.md`'s **`### G. SS-13 additions (append to the standing list)`** (`:223`) — an **h3** nested inside `## ADDENDUM R2` (`:174`) — carrying items **11** (dropdown WIDTH overflow, `:225`) and **12** (focus-ring 2px bleed sufficiency, `:226`). **563 + 2 = 565.** This is live, not cosmetic: **S-13** arms a triumvirate trigger on *"a material divergence from 565+25"*, so an executing seat running the UNAMENDED command would measure 563 and face a 2-count divergence **manufactured by the command, not by the corpus** — S-13 now names the amended form. The FIGURE of record (565, hence ≈590) is unchanged and is not re-opened; what was defective was its warrant.)* The PASS-2 check's independent recount returned the same **565** — *and the PASS-3 check's third recount returned it again; the agreement is on the figure, never on the published command, which all three seats had to amend or bypass to reach it.* The headline denominator therefore moves **588 → 590**; both dependent cells (§Scope 4, G-KFW9-4) are restated in the same motion, and the denominator still re-derives at open. VISUAL-AUDIT-INPUT markers are enumeration SEEDS, never denominators: V6-18 (88 SFCs) · V6-19 (717 static mounts) · V6-21 (2 Teleports) · V6-22 (15 routes) · V6-23 (11 panes) · V6-28 (137 state axes, catalog-only) · CM-15 · V6-33/V6-34/XF-6.
3. **Bounds law (KF-AX-26)**: the list must NOT inherit the demo gate's own named exclusions — the excluded surface is exactly the one no instrument has ever seen; `e2e/` does not exist.
4. **Cells (I-20)**: `safari-app/desktop` · `safari-app/ios-device` · `safari-app/ios-simulator` · `webkit-engine` (playwright) · `chromium` · `chromium/emulated-forced-colors` · `windows/real-HCM` · `at/voiceover-safari` · `at/nvda` · `at/jaws`. A `webkit-engine` verdict copied into a SAFARI column is a gate failure, not a shortcut. Unrun cells read UNMEASURED / UNREACHABLE-IN-CELL / UNVERIFIABLE-HERE; green without per-shot sha256 FAILS.
5. **Every probe carries a DISCRIMINATOR and a FALSIFIER** — a probe satisfied by both hypotheses is a spec defect, not a measurement (exemplar: kf-ChromeDock D-19's "a Curve-labelled node proves NOTHING").
6. **Shared-capture families** (§Sequencing S-8) are honoured so no probe is double-spent.
7. **Contrast numerals re-derive AT CAPTURE** (KF-SKEL-22) — no corpus numeral is the figure of record; frames stamped (KF-AX-4); substrate ref + sha + cell on every capture; one pass, one pinned commit, re-anchored lines (KF-AT-28).

## Carry — the registry-routed rows, exact ids (79 bullets; census carriage cited, not asserted — LAW B)

Format: **id** ⟨source⟩ severity — mechanism · cure+locks. Severity is the bank's, never re-graded at a fold; zero re-bookings — folds cite their bank. **STANDING ID RULE (entered at repair round 5 — RULINGS-5 §END KF-W9 D5-8): every id in this file is RECORD-QUALIFIED by its ⟨source⟩; a bare token is never a key.** The corpus reuses short tokens across records — **two distinct `D-15`s** (kf-RibbonBar's forced-colors fold · kf-SequencePlayhead's RTL posture, **never merged**) and **two distinct `D-6`s** (kf-SpringTarget's heading/landmark row at §F · kf-ChromeDock's truncation row, spelled at §G) — and **the TWO banked aliases PASS-5 named are written in full — `KF-ET-14 / D-M-6` ⟨kf-EasingTarget⟩ at §E and `D-6 / m-6` ⟨kf-ChromeDock⟩ at §G, 4 occurrences each; the remaining banked co-ids are carried BY PRIMARY ID OR BY FOLD and are enumerated here so the rule is a scope and not a memory** ⟨***the round-5 universal ~~"banked aliases are written in full, never discharged by identity"~~ is STRUCK at repair round 6 under LAW G (PASS-6 D6-7): six banked co-ids in this same file falsify it, four of them colliding live on a bare token — `KF-CE-47 · D-17` ⟨kf-CSSCodeEditor:85⟩, spec spells `KF-CE-47` only, bare `D-17` ×2 both resolving to a PASS-1 check-defect id · `KF-ET-12 · C-15` ⟨kf-EasingTarget:51⟩, spec spells the suffix form `KF-ET-26/12`, bare `C-15` ×3 all kf-AmigaScene's · `KF-KE-8 · D-5` ⟨kf-KeyframesEditor:50⟩, bare `D-5` ×15 headed by kf-SpringTarget's aria-hidden row at §F · `KF-SST-13 · D-4` ⟨kf-StartingStyleTarget:54⟩, bare `D-4` ×9 · `KF-SST-14 · D-7(b)` ⟨:55⟩, bare `D-7` ×12 all kf-AmigaScene's at §A · `KF-SST-17 · D-10` ⟨:58⟩, spec spells the suffix form `KF-SST-14/-17` and `KF-SST-17` is not a resolvable token in this file. NOTHING ESCAPES — each pair discharges by its primary id or by fold, the substance travels, and no row moves; what was false was a rule stated in this file's own voice about this file's own bytes. THE SWEEP IS NOW A COMMAND, entered at §SCOPE: `grep -on 'KF-SST-1[47]\|KF-ET-12\|KF-CE-47\|KF-KE-8\|KF-SST-13' KF-W9.md`.***⟩. The round-2 D-9 cure's remedy, stated as the class rather than at its two instances.

**Census recount, repair round 2 (recounted at the bytes, never incremented — R2-10 item 4 / R2-5(g)).** Round 1 recounted the authoring claim of "69/69" against its own bytes and found **68** (A 18 · B 5 · C 1 · D 8 · E 7 · F 4 · G 18 · H 7), restating it at **76** after R-1e's seven AT/announcement rows and R-10's KF-AV-28 rider. Round 2 adds R2-10's three hard escapes and **re-runs the count rather than adding three to it**: **A 18 · B 5 · C 1 · D 8 · E 7 · F 13 · G 19 · H 8 = 79** (scripted section-scoped bullet count over this file, 2026-08-28). The round-2 delta is **kf-AmigaScene D-2** and **kf-AmigaScene MISSED-A** (§F) and **kf-HeroAurora KF-HA-10** (§G). KF-CE-47 remains a named line-item inside §H's SS-13 aggregate (R-1e), not a bullet. *A spec whose central claim is exhaustive carry may not miscount its own carry — this line is recounted at the bytes at every repair round, and the count is re-derived, never arithmetic on the previous count.*
**What "79/79" denominates, stated so it cannot be misread against the census.** PASS-1's id-keyed census found **167** roster-level adjudicated ids whose terminal disposition routes to KF.W9; PASS-2's fresh record-scoped enumeration found **147** distinct **(record, id)** pairs. PASS-3's fresh per-record enumeration found **139**; PASS-4's, at a deliberately larger granularity, found **168**. The four are bookkeeping variants at different fold/limb granularity, not rival truths — **the enumeration of record is always the freshest conformance artifact's, never this spec's: `docs/tranches/X/keyframes/conformance/PASS-5/KF-W9-CHECK.md §1` (2026-08-28), routedTotal **181**, bookedCount 181, escapedCount 0, and the discharge below is keyed to it** (LAW B — *"no pass-4 check inherits any prior pass's denominator"*, RULINGS-3 R3-5, honoured in both directions: **citation re-pointed at repair round 5 — RULINGS-5 R5-9 row 16; PASS-5 check D5-6's CLASS arm**, and that artifact's own instruction — *"No pass-6 seat should inherit 181 either"* — travels with it; PASS-4's **168** joins 167 · 147 · 139 as a superseded dated reading, five granularity variants of one corpus). Routed pairs discharge by **four** lawful mechanisms, not one: (a) a §Carry bullet by id — **79**, the figure above; (b) a named fold-identity inside a §Carry bullet (KF-CE-13's **sixteen** folds — *the count is stated at its enumeration in §B and is not re-issued here; **"fourteen" is struck at repair round 4**, RULINGS-4 R4-6* — D-15's RTL folds, D-16's sequence-family folds, D-21's timeline folds — zero re-bookings); (c) an §Excluded row with a named receiving owner; (d) a residue line-item inside §H's **SS-13 aggregate** (≈590), granted only where the record's OWN `UNPROVEN-NEEDS-LIVE` list carries the item, **with record attribution enforced** (a bare id matching some other record's id is a substring collision, not a booking). **79 is the bullet count, never a claim that 147 = 79 or that 167 = 79.**

> **CORRECTED 2026-08-28 (repair round 2)**: the round-1 sentence *"no routed id now escapes all four mechanisms"* was an inference from PASS-1's ten-id list, never a re-enumeration, and is struck. A fresh 58-record enumeration (PASS-2) found three escapes — kf-AmigaScene **D-2**, kf-AmigaScene **MISSED-A**, kf-HeroAurora **KF-HA-10** — now booked (RULINGS-2 R2-10). PASS-1's booking of KF-HA-10 (*"all have residue homes"*) is refuted on the record: the residue is P-1..P-8 and names no KF-HA-10. Restated true: every routed (record, id) pair in the PASS-2 enumeration (147) is discharged by one of the four mechanisms, **re-derived at this correction's date**. *(**RE-HOMED 2026-08-28, repair round 3 — LAW B.** The sentence above is still a spec-voiced closure claim over an enumerable set, and LAW B bans the shape whoever is right: **four consecutive generations cured the enumerated set perfectly and did not close the class** (32 hard escapes at pass 1, 9 at pass 2, 6 at pass 3, each generation's set fresh). The claim is therefore **transferred to the artifact and cited, not asserted here** — *citation re-pointed at repair round 5, as the last sentence of this block instructed*: `PASS-5/KF-W9-CHECK.md §1` (2026-08-28) — **181 routed pairs · bookedCount 181 · escapedCount 0 · "AXIS 1 VERDICT: CLEAN"**, the **fifth** independent enumeration of this corpus, run at a granularity larger again, with **four** extractor candidates (kf-CSSCodeEditor `corpus-missed` · kf-SequenceTarget `HCM-family` — not ids; kf-ChromeDock `m-6` · kf-EasingTarget `D-M-6` — banked aliases, discharged and now SPELLED at their carrying rows per D5-8) and **nine** route-(d) resolutions individually adjudicated at the bytes and recorded so pass 6 does not re-book them. Its predecessor `PASS-4/KF-W9-CHECK.md §1` reached **168/168/0** (described there as the fifth enumeration; it was the fourth — 167 · 147 · 139 · 168 · 181) and is retained as that round's dated measurement. Its predecessor `PASS-3/KF-W9-CHECK.md §1` (2026-08-28) reached **139 routed · 139 booked · 0 escaped** by a two-shape extraction (roster rows in **both** bullet and table shapes — the table shape being what bullet-only extraction had silently missed across 38 pairs at 8 records — plus the per-record `**KF.W9**:` routing-summary difference), with eleven machine-flagged candidates adjudicated and the **kf-TimelineTrack D-8 false positive** struck (it routes → KF.W7; only its forced-colors arm folds to D-21, carried at §B — **re-verified by PASS-4 and recorded there as a standing decline**). The pass-4 artifact also books **seven defects against this wave on axes 2/4/5** — the census being clean is not the wave being clean. When a pass-5 census supersedes it, this citation is re-pointed; the claim is never re-written in this spec's voice.)*

*Re-derivation receipt for the correction block (R2-5).* This seat re-read the three escaping rows at the bank and re-ran its own routing sweep over all 58 records this session: `kf-AmigaScene.md:42` (D-2's disposition) and `:54` (MISSED-A's) and `:158` (that record's KF.W9 witness list, naming both) — and `kf-AmigaScene`'s own twelve-item residue (`:130-141`) carries **neither** id; `kf-HeroAurora.md:52` (KF-HA-10's roster row, *"frame cost → KF.W9"*) against that record's residue **P-1..P-8** (`:103-114`), which names it nowhere — `grep -n "KF-HA-10" kf-HeroAurora.md` returns exactly one hit, the roster row itself. An independent per-record sweep of every `**KF.W9**:` routing-summary line, differenced against this spec and against each record's own residue, surfaced no fourth escape **at that round's reading** — *and per LAW B (repair round 3) that is reported as a round-2 measurement, never as a closure of the class; the class's standing verdict lives in the freshest census artifact — **`PASS-5/KF-W9-CHECK.md §1` (2026-08-28), citation re-pointed at repair round 5 (RULINGS-5 R5-9 row 16)**, which re-enumerated by record at a granularity larger again (181/181/0) and found none, while booking eight non-census defects; its predecessors `PASS-4/KF-W9-CHECK.md §1` (168/168/0) and `PASS-3/KF-W9-CHECK.md §1` (139/139/0, two-shape extractor) also found none, and all three name PASS-2's R2-10 bookings as the class's last members*; every other flagged token resolved to a sibling wave's segment on the same line (kf-StartingStyleTarget's KF-SST-14/17/13/32, kf-SquareInstrument's D-13/D-21/D-12, kf-SpringTrace's D-5 datum and D-14 fold, kf-SequenceAxis's D-12 relay, kf-SequencePlayhead's D-15 — all carried). **The PASS-1 mis-booking is recorded here as an E-3 addendum; the PASS-1 check file is NOT edited.**

### A · The PRM family — the unification (deliverable 2)

- **D-7 / C-15 / L-m8** ⟨kf-AmigaScene⟩ MAJOR — **THE ANCHOR ROW**. PRM posture inverted: the ~400ms settle is the ONLY gated motion; all three CSSKeyframesAnimation + the group omit `respectReducedMotion` (engine default false, `defaults.ts:87`); the demo's stored default `true` is never applied to a live animation. Cure ADJUDICATED VERBATIM: *"the remedy is the **intensity** form (`respectReducedMotion: ~0.3`), not a binary snap"* — `reducedMotionScale` **exported on the LIGHT barrel (`index.ts:50`); zero DEMO consumers; THREE LIBRARY consumers** (`physics/spring/progress.ts:153` / `:232` / `:385`, each `this.amplitudeScale = reducedMotionScale(…)`; import at `:2`). *(**WITNESS RESTATED 2026-08-28, repair round 3 — RULINGS-3 R3-9 / PASS-3 check D3-2.** The authored *"used by nothing"* is **STRUCK as an unbanked amplification a one-line grep refutes at BOTH baselines**: the bank says only *"`reducedMotionScale` numeric-intensity resolver exported on the LIGHT barrel — index.ts:36–52 read by this seat"* (`kf-AmigaScene.md:47`); the escalation from *exported* to *used by nothing* was this spec's own. Re-derived this seat at `origin/master 81a56990`: `git grep -n '\breducedMotionScale\b' origin/master -- src/ demo/ test/ scripts/` → **6 hits** — the export `index.ts:50`, the definition `internal/reduced-motion.ts:125`, and the spring import + three assignment sites above; `git grep -c reducedMotionScale origin/master -- demo/` → **no hits (exit 1)**, so the demo half of the claim is TRUE and is what survives. **THE IN-TREE PRECEDENT, NAMED (R3-9.2):** the wave's central adjudicated cure IS the intensity form, and **the shipped precedent is the SPRING LANE** — `SpringProgress` holds `private amplitudeScale = 1` (`:100`), re-resolves it from `reducedMotionScale(…)` at its three construction/option sites, and **applies it as a multiplicative displacement-from-rest scalar at `:323` and `:352`** (`const s = this.amplitudeScale;`) — exactly the mechanism the scene rows order, already shipping. The unification is therefore an **extension of a live library mechanism to the demo half, not an invention**. A dated note on the likely inheritance source: kf `docs/published-surface.md:35` files `reducedMotionScale` as *"manifest-only: K.W11 PHYS-E"* — read verbatim this seat; that doc is **not edited by this wave** and the note is spec-side only.)* Rides the census correction: JS tally amends to **4 sites / 3 mechanisms** (AmigaScene.vue:58 the uncounted fourth). Identity guard: ≡ FE §6.5's PRM tally, booked ONCE here, never re-booked at its folds.
- **D-8 + M-7** ⟨kf-CubeScene; folds kf-MatrixEditor K10/K26 booking-kills⟩ MAJOR — cube gates 1 of 4 engine motions (650ms graph settle only); ungated: 1.1s two-axis Roll, 300ms cell tween, 500ms easeInBounce reset — all WAAPI/rAF, unreachable by CSS resets. Cure: intensity form per the anchor; `NumericAnimation` accepts the flag (**`src/animation/physics/numeric.ts:82-89`**). *(**PATH WRITTEN IN FULL at repair round 4 — RULINGS-4 §END KF-W9 D4-7; PASS-4 check D4-7, the D3-8 class swept in §Bounds at round 3 and not in §Carry.** The authored `numeric.ts:82-89` was a **bare filename under no declared base**: §Bounds' shorthand bases do not reach into §Carry, and no base is declared here. Resolved this seat: `git ls-tree -r --name-only origin/master | grep 'numeric.ts'` → **one hit**, `src/animation/physics/numeric.ts`; `git show origin/master:src/animation/physics/numeric.ts | sed -n '82p;89p'` → `:82` `constructor(keyframes: T[], options?: NumericAnimationOptions) {` · `:89` `this._respectReducedMotion = options?.respectReducedMotion ?? false;` — the constructor accepting the flag, exactly as the row claims; **the offsets were sound and are kept, only the address was short.** **STANDING RULE, stated so the class cannot regrow**: **every source anchor in §Carry carries its FULL repo-relative path** — §Bounds' declared bases are §Bounds-local and a §Carry reader has no base to resolve against.)* Anti-rename: no MatrixEditor-side PRM id exists.
- **OD-11** ⟨kf-OrbitalDrag⟩ MAJOR — **EXTENSION, NOT RE-BOOK** of the cube row: the post-release inertial glide (`useRafFn`) honours no PRM in the scene that names "the house reduced-motion contract". Knob exists (`inertiaFactor`→instant stop); the producer's own `useDragVelocity` defaults `respectReducedMotion` true — the demo glide is the outlier. Corrects the cube tally to **5** (4 engine + 1 rAF).
- **#10 / D·M-1 / C·D-1** ⟨kf-CubeTarget⟩ as banked at r1, carried whole unrestated — folds under the cube tally; the cube packet (NO-WAVE-OWNER) owns the behaviour, KF.W9 the posture + witnesses for #8/#9/#11/#32/#58.
- **D-3 / L-12 / C-6 (+ the PRM-inert rider)** ⟨kf-SquareScene⟩ MAJOR — PRM asserted in prose, unimplemented in fact: all three springs decline the flag; 100% of motion is unguarded JS incl. a gesture 360° roll; the CSS blocks the comments call compliance govern the layer with no motion. Cure: intensity form. RIDER (folds by reference): those CSS blocks are themselves **INERT** — the producer's unlayered `*:not([data-allow-motion]) … !important` reset defeats every local `transition: none` (≡ **kf-ControlsPaneWrapper's unnumbered `MISSED (RR-1, adopted)` INFO row** at `:94` — *"the component's own PRM block (`.css:144-148`) is INERT … Presence ≠ effect"*, the id resolved at repair round 1: `RR-1` is that record's re-reader name, and the row it carries is unnumbered) — ZERO effective PRM in either layer, the strictly stronger reading. **SIGN CORRECTION (KF-W9-CHECK D-17):** kf-CubeScene **D-16 is NOT a witness of PRM-inertness and is no longer cited as one** — at that bank D-16's PRM half is a **KILLED claim** (killed-claims #8: *"the shipped stylesheet carries glass-ui's universal `!important` PRM reset … the spinner is CSS-animated and fully neutralised"*), i.e. the **same mechanism at the opposite valence** — there the universal reset *manufactures* compliance by accident. It is carried here as a **mechanism corroborant with its valence stated**, never as a witness; D-8 survives at that record on engine-motion grounds alone. The PRM-inert mechanism is a PRODUCER fact → SS-6 relay, never a demo hack. Square = **0 of 4** gated — the sharpest §6.5 correction.
- **D-13** ⟨kf-SquareInstrument⟩ MAJOR at scene level — PRM honoured cosmetically only (the 160ms fade snap, correct for what the file owns — S-8); the 5-leg `setTimeout(step,520)` tour, +360° tumble spring, persist-chase, engine Play tour all ungated; zero matchMedia in `demo/scenes/square/*.ts`. KF.W9 owns the posture; the composable edit is the **square packet's** (NO-WAVE-OWNER; booked as scene identity, anti-rename). Pairs with D-3/L-12/C-6 as ONE square-family PRM decision.
- **D-4** ⟨kf-SequenceTarget⟩ MAJOR, concordant both readers — `playReel()` (deliberately under-damped 0.34, user-retriggerable ×2) has NO PRM path in its call chain; glass's blanket cannot reach engine-written inline styles; the same file declares "MANDATORY PRM degrade" for the boot and `useSequenceInstrument.ts:29-32` proves the idiom known. Cure: one flag at `useSequenceDemo:128-132` or a matchMedia gate in `playReel`. Distinct from banked D-8/C-§5 (no-charge) — never merged.
- **D-3 (SpringTarget)** ⟨kf-SpringTarget⟩ MINOR (demoted, ruling 4) — PRM reaches only two decorations; the 60 Hz solver has no gate; the engine ships the opt-in and none of the three construction sites passes it. Cure: the flag at three sites (NO-WAVE-OWNER) + KF.W9 owns the datum. **CURE-KILL CARRIED (K-6)**: the universal a11y rule constrains duration/iteration-count NOT `animation-name` — the local `animation: none` is NOT dead; deleting it would leave 0.01ms animations still firing events. Escalation capped by `autoPlays: false`.
- **KF-SS-20** ⟨kf-SpringScene⟩ MINOR (regraded) — three cosmetic CSS sites gated, 60 Hz physics ungated while three siblings gate in JS. NO-WAVE-OWNER flag; data ride here. Regrade rationale carried (`autoPlays:false` = user-initiated + pausable, no AA criterion engaged; the defect is convention-divergence). **Dissent preserved: MAJOR (axis + reader-1).** Rides the SAME pass as D-3 (SpringTarget) — one spring-family decision.
- **KF-KE-8** ⟨kf-KeyframesEditor⟩ MAJOR-band — three engine animations, one infinite (`useKeyframeBrushApply.ts:18-27`), none opting in; glass CSS cannot reach WAAPI/inline writers. CORRECTED CURE MAP (M-6 reversal): brush + 1s sweep = STANDALONE flags in options bags — honoured on the standalone play path at **`engine/play-lifecycle/frame.ts`'s `playFrame` (the `withReducedMotion` flip, `:131`)** and at **`engine/play-lifecycle/strategies.ts`'s final-frame `beginPlay(… withReducedMotion(…` (`:109`)** *(**RE-ANCHORED at repair round 3 — R3-7**; the authored `play-lifecycle.ts:214/:376` resolves only at the DISQUALIFIED `8281638c`; frontier map and carve receipt at §Bounds' library-reference row)*; delete GROUP needs `g.respectReducedMotion = true` (`group/group.ts:57`). **LOAD-BEARING NUANCE (from kf-CopyButton)**: PRM snaps to FINAL frame = 100% width for the sweep, idle-indistinguishable given KF-KE-21 — **land the bar's rest state WITH the flag, never the flag alone.**
- **KAD-11** ⟨kf-KeyframesAddDialog⟩ MAJOR — the 1000ms width sweep, `{ duration: 1000 }` only (:128); WAAPI/rAF inline write, not CSS-delegable. Cure → KF.W6; **PAIR LOCK**: twin at KeyframesEditor.vue:254-258 — the fix is a pair, one motion, the twin carrying KF-KE-8's rest-state constraint.
- **KF-EST-11** ⟨kf-EditorStartScreen⟩ MINOR (ruling 8b) — dots rest at 0.2 by the engine's GATED snap-to-final (documented); the sharper defect is **RELEASE ASYMMETRY**: on reduce→no-preference the CSS wave re-arms, the engine dots never do (PRM evaluated once at `play()`). Witness **P-9 decides the unification's SHAPE**. **Dissent preserved: D's MAJOR** (kf-TypingDots re-filed; the later EST row governs).
- **KF-TD-1** ⟨kf-TypingDots⟩ MAJOR — on the SHIPPING (WAAPI-delegated) lane a live flip to reduce is NEVER observed (`snapToReducedMotion`'s sole caller is `playFrame`, the rAF lane; `shadowTick` never consults `withReducedMotion`); the library docblock asserts the opposite verbatim — three dots pulsing on the LCP `<h1>` forever. **Frontier anchors (re-derived at `origin/master 81a56990`, repair round 3 — R3-7):** the docblock is **`engine/play-lifecycle/strategies.ts:66-75`**, its declaration `snapToReducedMotion` at **`:76`** — the asserting sentence read verbatim this seat: *"The WAAPI lane snaps via the same path: the up-front gate already routes reduced-motion away from WAAPI, and a live flip on a WAAPI animation cancels the compositor handles before settling."* The mechanism **reproduces at the frontier**: `git grep -n '\bsnapToReducedMotion\b'` over `src/ demo/ test/ scripts/` returns exactly ONE call site, **`engine/play-lifecycle/frame.ts:137`, inside `playFrame` (decl `:121`, the rAF lane)** — and `withReducedMotion` appears **nowhere in `src/animation/waapi/delegation.ts`** (`git grep -c 'withReducedMotion' origin/master -- src/animation/waapi/delegation.ts` → **exit 1**, re-run this seat), so `shadowTick` (**`:53`**, looped at `:64`) still never consults it — *declaration pinpoint corrected at repair round 4 (D4-4); `git grep -n 'shadowTick' origin/master -- src/animation/waapi/delegation.ts` → `:53` the `const shadowTick = …` declaration · `:64` the loop, this seat. The authored `:54` is the body's first statement (`if (animation.done) return false;`) and was wrong at BOTH refs; the mechanism claim it decorates is unchanged and re-verified.* KF.W5 owns the live-flip design decision + docblock cure; **KF.W9 owns the witness** (U-1), rider-linked to KF-EST-11 as ONE two-direction story. **SUBSTRATE INVERSION binding on capture** (ruling 1): at stale HEAD (rAF lane) live PRM worked; frontier-vs-HEAD flips the lane and the prediction — every capture NAMES its substrate. *(**RESTATED AS A FRONTIER FACT 2026-08-28, repair round 3 — RULINGS-3 R3-7.2**; PASS-3 check D3-1, which convicted this row as the one row whose whole point is that the lane flips between the two refs while its own anchor resolved at only one of them.* **The layout split, stated so the two waves' anchors agree:** the module is now a **DIRECTORY** — `src/animation/engine/play-lifecycle/{events,frame,index,strategies,transport}.ts` — carved off the flat `engine/play-lifecycle.ts` at **`53b907c5`**; the `./play-lifecycle` specifier resolves to the dir barrel and is unchanged, and the barrel **deliberately EXCLUDES `snapToReducedMotion`** (its own docblock: a *"module-internal cross-file export … kept `export`, barrel-EXCLUDED — consumed by `frame.ts`'s `playFrame`"*), so a docblock cure **must land in `strategies.ts`, never in the barrel**. **CROSS-CITED, both directions**: KF.W5's docblock cure (**B-6**) is anchored at `strategies.ts:66-75` — the two specs name the same file and the same lines, and the `:337-341` spelling is retired as an **`8281638c`-only historical coordinate**, citable only as a named historical measurement context. ⟨**RE-VERIFIED AT THE SIBLING'S FINAL STAGE-1 BYTES, repair round 4 — LAW C(2), quote-by-command.** KF.W5 writes in **stage 1** and this wave in **stage 2**, so the receipt is taken after that file's last write, not inherited from round 3: substrate `docs/tranches/X/keyframes/waves/KF-W5.md`, **mtime 2026-08-28T16:55:55**; `grep -n '66-75' KF-W5.md` → **two** live cells, its §Bounds `play-lifecycle` row (*"**`strategies.ts:66-75`** the **false** live-PRM docblock (declaration `snapToReducedMotion` at `:76`)"*) and its §Carry **B-6 · KF-TD-1** row (*"re-read at `81a56990` this seat: `engine/play-lifecycle/strategies.ts:66-75`, the docblock over `snapToReducedMotion` (declared `:76`)"*). **Both directions hold at the final bytes**, and W5's B-6 additionally spells `shadowTick` as **`delegation.ts:53-64`** — a sibling that had the declaration line right while this file carried `:54`, which is the independent corroboration of D4-4 and the reason the two specs now agree to the line.⟩ ⟨**RECONCILE SEAT, 2026-08-28 — THE TWO CELLS ARE CONFIRMED; THE COMMAND'S ARITHMETIC IS CORRECTED (LAW D(3); `PASS-4/CLOSE-CERT.md` §R-9).** The substrate above is right — `KF-W5.md` at **16:55:55**, its true final stage-1 bytes — and **both named cells verify byte-exact there**: the §Bounds `play-lifecycle` row at **`:216`** (*"**`strategies.ts:66-75`** the **false** live-PRM docblock (declaration `snapToReducedMotion` at `:76`)"*) and the §Carry **B-6 · KF-TD-1** row at **`:329`** (*"re-read at `81a56990` this seat: `engine/play-lifecycle/strategies.ts:66-75`, the docblock over `snapToReducedMotion` (declared `:76`)"*). **What is corrected is the figure printed beside the command**: `grep -n '66-75' KF-W5.md` returns **SIX lines — `:216` `:218` `:329` `:372` `:391` `:485`** — not two. *"Two live cells"* is a true statement about the two rows this receipt names and a false transcript of the command that was pasted to prove it; the other four hits are W5's own §Bounds continuation, its gate-verdict roll-up, its round-3 re-anchor note and its historical-figure retirement — none of them adverse, all of them corroborating. **The receipt's substance stands; its numeric arm now matches its command.**⟩)*
- **KF-SST-13** ⟨kf-StartingStyleTarget⟩ MINOR (ruled) — the "MANDATORY PRM degrade" is inert (vendor unlayered `!important` overrides both longhands); rendered PRM acceptable by VENDOR ACCIDENT. **Third independent witness of the producer PRM-inert mechanism — the three enumerated (KF-W9-CHECK D-16 cured by enumeration, not by re-grading): (i) kf-SquareScene's D-3/L-12/C-6 rider · (ii) kf-ControlsPaneWrapper's unnumbered `MISSED (RR-1, adopted)` INFO row · (iii) this row.** kf-CubeScene D-16 is deliberately NOT counted (its PRM half is killed and its valence is opposite — see §A's sign correction). **The unification decides the `data-allow-motion` policy ONCE, demo-wide.** NO-WAVE-OWNER prose/posture cure; rendered witness here. Kills L-S2/C-S★5 (K-2). **Dissent preserved: A's MAJOR.**
- **D-8 / C-§5** ⟨kf-SequencePlayhead⟩ INFO, NO-CHARGE ruled — glass's blanket cannot reach a custom-property-driven transform: the **fourth distinct producer-blanket failure mode**, a mechanism input to the unification. B's MINOR re-contest DECLINED (K-31); A concordant INFO.
- **KF-SKEL-11** ⟨kf-App.skeleton⟩ MINOR — 1.6s shimmer hardcoded ≈3.1× the house clock (`--duration-shimmer: 5s`), unreachable by `.motion-calm`/`--motion-weight`. KF.W6 tokenizes; **KF.W9 owns the motion-register reachability arm** — that register is the demo's declared PRM intensity channel and this animation sits outside it. Era annotation MANDATORY when quoted.
- **KF-HA-9** ⟨kf-HeroAurora⟩ MINOR (ruling 5) — under PRM every forwarded mousemove runs one synchronous identical full-viewport draw in the input task: **"PRM gates the reschedule, not the suspend set"** — a mechanism finding the unification MUST carry (a flag that stops scheduling but not synchronous work is not compliance). Cure → KF.W6 (early-out + `useWindowSize()`); the `wake()` seam is PRODUCER → SS-6 with KF-HA-1/-2. KF.W9 owns rate/cost (SS-13 P-5).
- **D-24 (PlaybackRibbon)** ⟨kf-PlaybackRibbon⟩ MINOR-band — zero authored PRM/RTL/loading/error; PRM rescued ONLY by the producer allowlist (`transform` absent — exactly what neutralizes the icon flip + press transforms). The demo's clearest **accidental compliance**: the allowlist's contents, not intent, decide the outcome. Rides the producer-allowlist mechanism with KF-SST-13 and the square rider; carries KF-AV-37's RTL datum.

### B · Forced-colors (deliverable 3)

- **KF-CE-13** ⟨kf-CSSCodeEditor — the demo-wide anchor⟩ MAJOR-band — monaco half (explicit `theme:` + `setTheme` defeat `autoDetectHighContrast`; unconditional `forced-color-adjust: none`) → **EDITOR-UNIT**. The categorical half — **zero `forced-colors` sites demo-wide vs 13+ PRM sites** (grep re-run by seven seats) — is this wave's second named scope. **FOUR-FILE CORRECTION binding** (kf-SequenceAxis ruling 7): producer forced-colors rules live in FOUR dist files (components.css, accessibility.css, glass/a11y-fallback.css, utilities/a11y-overrides.css); every "sole forced-colors rule is X" cell is KILLED; a11y-fallback DOES reach scene chrome. **ONE IDENTITY, ZERO RE-BOOKINGS** — folds by reference: KF-AV-20/D-9 · KF-CB-32/D-19 · KF-ET-16/D-M-8 · D-21 (kf-KeyframeTimeline = kf-TimelineTrack D-9 ≡ kf-TimelineHoverPreview D-10) · KF-KE-43/D-31 · SPF-28 · KF-SS-40/D-19 · D-14 (kf-SpringTrace) · D-21 (kf-SquareInstrument) · KF-SST-14/-17 · KF-APP-24 ≡ kf-TypingDots D:D-8 · D-15 (kf-AmigaScene) · kf-SquareScene's demo-wide row · kf-CubeAxisLines' keying correction · kf-SpringHeatmap D-M6/D-M7 (**canvas bitmaps EXEMPT from forced-colors adjustment — a mechanism limit the cure must state, not paper over**) · kf-TimingFunctionPanel D-14 residual (vendor-owned strokes). — **SIXTEEN fold families by this row's ` · ` separation** (**the count of record, stated AT its enumeration with its counting rule; corrected from the authored "fourteen" at repair round 4 — RULINGS-4 R4-6; PASS-4 check D4-3**). *Counting rule: the unit is one ` · `-separated item of the list above, script-counted at the bytes. **Granularities disclosed, because a numeral is only meaningful at its rule** (the S-10.1 pattern this file already built): by **banked id** the count is larger — `D-M6`/`D-M7` are two ids, `KF-SST-14`/`-17` are two, and the `D-21` item folds three records (kf-KeyframeTimeline ≡ kf-TimelineTrack D-9 ≡ kf-TimelineHoverPreview D-10); by **record** it is different again. No re-booking follows from any of them — the folds cite their banks and the identity stays ONE.* **Three dependent cells formerly asserted "fourteen" and enumerated at none — §Carry head route (b), G-KFW9-8's CLOSES (a gate's GREEN) and S-2's measurement order — and all three now POINT HERE rather than re-issue a numeral.** *Why this was live and not cosmetic: G-KFW9-8's green was defined as "its fourteen folds discharged by reference", so an executing seat discharging fourteen left two fold families unshot and closed the gate anyway. This is the identical fault the file has cured three times for other registers (the negative register at round 1, the escalation register at round 2, the cross-edge set at round 3), each time by this same remedy.*
- **D-16 (SequencePlayhead)** ⟨kf-SequencePlayhead — the sequence-family bank⟩ INFO — zero forced-colors tree-wide across the sequence scene, mitigated by earned `aria-hidden` + value carriers (N-14 thinness qualification). Family folds cite it, never re-book: D-11 (kf-SequenceAxis, four-file-corrected) · D·D-8's limb (kf-SequenceScrubber) · D-7 (kf-SequenceTarget — the family's SHARPEST: state carriers vanish, washes survive). **Dissent preserved: kf-SequenceTarget A's MINOR.**
- **RB-1** ⟨kf-RibbonBar⟩ MAJOR net-new — **A REMOVAL, NOT AN OMISSION**: the demo's unlayered `.focus-ring:focus-visible { box-shadow: …; outline: none }` DEFEATS glass's forced-colors focus outline (`outline: 2px solid Highlight`, a11y-overrides.css, `@layer` count 0) — under WHC no ribbon button and no glass Button demo-wide has ANY keyboard-focus indicator (WCAG 2.4.7/2.4.11 failing invisibly). Cascade proof carried: same specificity (0,2,0), demo imports later (style.css :3 vs :14), box-shadow forced to none by spec. **ONE IDENTITY, TWO BANKED NAMES** with D-3+DU-M-1 below; neither renamed; KF-SST-14 folds to "the banked RB-1 three-rule ordering"; **the kf-RibbonBar fold is carried BY ITS BANKED ID — `D-15 ⟨kf-RibbonBar⟩` MINOR (demo-wide row)** — *(**ANTI-RENAME TRAIL RESTORED 2026-08-28, repair round 2, KF-W9-CHECK D-9**: that record routes `D-15 → CPW **D-m9** + kf-CSSCodeEditor **KF-CE-13** → **KF.W9**` at `:57`, and its KF.W9 routing summary at `:133` reads *"RB-1 + the D-15 fold (the demo-wide forced-colors row ≡ KF-CE-13/D-m9)"* — both re-read at the bank this session. Round 1 discharged the identity through the two fold targets but never wrote the banked id, so a reader resolving a bare "D-15" in this spec landed on **kf-SequencePlayhead's RTL posture row at §C** — a different record's id at the same token. **The two D-15s are distinct banked ids at distinct records and are never merged**: `D-15 ⟨kf-RibbonBar⟩` = the forced-colors fold here; `D-15 ⟨kf-SequencePlayhead⟩` = the RTL posture row at §C.)* — and **KF-SS-40 folds to kf-ControlsPaneWrapper `D-m9`** — *"zero forced-colors / prefers-contrast / prefers-reduced-transparency handling demo-wide … the producer models the axis, so the consumer opts out of an axis its design system honors"* (`:77`). **DANGLING-CITE REPAIR (KF-W9-CHECK D-8):** the authored target *"kf-ControlsPaneWrapper D-3+M-1"* resolves to NOTHING at that record — its ids are D-B1 · D-M3 · D-M5 · D-M7 · D-M10 · D-M11 · D-m4 · **D-m9** · D-m12 · D-i2 · MISSED (×4, unnumbered) · S-1 · N-2 · C-2. The cite was inherited from kf-SpringScene's KF-SS-40 without re-resolving — the citation-inheritance failure the corpus convicts elsewhere (kf-CubeScene killed-claims #8, twice). Re-pointed at the banked id; no row renamed. Subsumes-and-raises the banked D-m9/KF-CE-13 omission class.
- **D-3 + DU-M-1** ⟨kf-PlaybackRibbon, demo-wide home; the demo-wide sibling row is kf-ControlsPaneWrapper **D-m9**, per the repair above⟩ MAJOR, blocker-in-mode — the rule authored TWICE. **SELECTOR ELISION REPAIRED (KF-W9-CHECK D-19)**: the two copies are the same *rule shape*, not the same selector — `design-idioms.css:76` is **`.focus-ring:focus-visible`** (the surviving authority; `:78` is its `outline: none` declaration and the `:76-79` rule range stands) and `playback-idiom.css:72-75` is **`.btn-playback:focus-visible`** — different selectors at the same (0,2,0) specificity, both unlayered, both carrying `outline: none`. *(**PINPOINT CORRECTED 2026-08-28, repair round 2 — R2-7 / KF-W9-CHECK D-10**: round 1 pinned the selector at `:78`, its declaration line. Re-derived at the frontier this session — `git grep -n "focus-visible" origin/master -- demo/styles/design-idioms.css demo/styles/playback-idiom.css` → `design-idioms.css:76` `.focus-ring:focus-visible` · `playback-idiom.css:72` `.btn-playback:focus-visible`; the bank agrees, `kf-RibbonBar.md:65` writes the rule as `design-idioms.css:76-79`.)* K-5 SURVIVES the correction (both defeat the producer's forced-colors outline over their own subjects, so deleting only the playback copy fixes nothing) — but the bank's stated *ground*, *"both demo copies are wholly redundant against producer base.css's identical rule"*, is asserted for a selector the producer does not ship. **RE-DERIVATION OBLIGATION, carried to the acting wave:** the redundancy ground for `.btn-playback:focus-visible` is UNTESTED here and must be re-derived at KF.W13 **before** the deletion lands; this wave measures it (G-KFW9-9) and hands the finding over. **CURE LOCK (ruling 2), carried whole**: the complete cure is **two deletions**, K-5 SUSTAINED, **ONE act, never split**. **CURE OWNER MOVED (R-9a item 2 / R-15):** the act belongs to **KF.W13 · Chrome, Dock & Transport Repair** (the transport/ribbon packet owns `playback-idiom.css`; the `design-idioms.css` copy rides the same single act) — *"may land with any packet commit"* is superseded by that homing. **HEAD OF THE FORCED-COLORS MEASUREMENT ORDER** (the cure order is KF.W13's). fr-App MG-ζ distinguished, not folded. **Dissent preserved: DU's BLOCKER.**
- **ST-1** ⟨kf-SequenceTarget⟩ MAJOR net-new (ruling 8) — five row sliders with NO focus indicator in forced-colors because the scene FORKED the demo's single keyboard-focus affordance to satisfy `proof:sequence-rows-draggable` — **a gate that exists in a CSS comment and old prose ONLY** (no such script at either baseline). Cure: one word (`focus-ring` at .vue:97) + delete css:163-167 (justification void); D §4's "outline replaced" cell killed at K-8. **CURE BYTES MOVED (R-4 / R-9a item 1): the edit travels WITH the sequence packet to KF.W11**, sequenced after **G-KFW4-1** — this wave's S-11 already DECLINES that packet's behaviour and S-13 fences it, so landing it here was the breach; **KF.W9 keeps ST-1 as MEASUREMENT + held witness** (the HCM datum and the AT limb). The ordering survives as a **sequencing DECLARATION**, not a gate: ST-1's re-point must not precede the two-deletion act, or the sliders move to *"no indicator via the defeated class"* — a green-looking no-op. SECOND LIMB RELAYED: SequenceScrubber.vue has zero `focus` hits in 162 lines — the master `role="slider"` has no focus indication in ANY mode.

### C · RTL posture — one project-level row

- **D-15 (RTL posture)** ⟨kf-SequencePlayhead, the banked precedent⟩ INFO — all-physical geometry, zero `dir=` surface demo-wide: ONE project-level posture row, not N component defects. Folds by reference: D-12 (kf-SquareInstrument) · KF-SS-36/D-25 · KF-SST-32/D-18 (cites this precedent by name) · D-16 (kf-TimelineHoverPreview) · D-24 (kf-KeyframeTimeline — mirrored toolbar over unmirrored axis, the predicted accident) · D-24 + KF-AV-37 (kf-PlaybackRibbon — `pl-px`/`scale-x-[-1]` need mirroring; producer Slider handles `:dir(rtl)` itself) · D-m9's RTL limb (kf-SpringHeatmap — legend flips, field doesn't). **Promotion condition written WITH the row** (per kf-TimelineHoverPreview), never discovered later. **Dissent preserved: kf-StartingStyleTarget's axis MINOR.**

### D · iOS / mobile / touch (the device session — family (v))

- **R-9** ⟨kf-CSSPasteDialog⟩ MAJOR — 14px text entry below the repo's OWN ruled + unit-tested 16px no-zoom floor (`clampIOSNoZoomFontSize`, consumed by CSSCodeEditor, tested 14→16 at `ios-text-entry.test.ts:88-97`); the R-8 bypass is why the coarse-pointer 1.5× never rescues it. Cure → KF.W6; **KF.W9 owns the live witness** (zoom is hardware-only). **Head of the iOS-floor family** — only this wave's Safari-mobile cell can witness it.
- **SP-4** ⟨kf-SharePopover⟩ MAJOR net-new (reader-1 MISS-1) — share-URL field ~12.2px at 390px; the displaced DS default was zoom-safe (~21px coarse). Consequence: focus auto-zoom UN-RESTORED on blur, right-anchored popover ends off-screen. Cure → KF.W6 (one edit with SP-7); witness here. **PRECEDENT, NOT IDENTITY** with R-9 — two banked ids, never merged.
- **R-20 / KAD-F4** ⟨kf-CSSPasteDialog (bank) + kf-KeyframesAddDialog (fold)⟩ MINOR at the bank's grade — no `spellcheck/autocorrect/autocapitalize/dir` on any of three contenteditable source-code surfaces (grep 0 repo-wide); iOS MUTATES typed CSS and `onInput` emits the mutation into the model — data corruption, not cosmetics. Cure → KF.W6 (attrs); iOS witness here. **Dissent preserved VERBATIM + ARMED**: R-A's MAJOR — "KF.W9's iOS witness may revive it."
- **KF-KC-16 / KF-KC-25** ⟨kf-KeyframeCard⟩ as banked — **DESCRIPTOR CORRECTED (KF-W9-CHECK D-15)**: KF-KC-16 is *"iOS Safari auto-zooms on focusing the **`<pre>`** — `--type-small` floors at 14px < 16px"* (`:58`) — the **CSS text region**, not the offset field (that surface is kf-KeyframeCardList's); the authored "offset-field" descriptor was drift on a carried id and would have mis-aimed the device probe. KF-KC-25 = autocorrect/autocapitalize/spellcheck rewriting the same element. Mechanisms closed statically, device confirmation outstanding. Folds into the R-20/KAD-F4 attribute family for the CURE, distinct ids preserved; same iOS session (with TimelineCaret probe 10, riding banked D-9's fold).
- **MISSED-F** ⟨kf-AmigaScene, house-wide census⟩ MINOR house-wide — `touch-action: none` suppresses pinch-zoom at **6 scene sites** (amiga largest; page-level escape intact). Banked cure: shared `touch-action: pinch-zoom` refinement (NO-WAVE-OWNER); KF.W9 witnesses. **CURE-KILL CARRIED (kf-SpringTarget ruling 6)**: C-3's prescribed `touch-action: none` would manufacture a SEVENTH instance — "the edit is owned by the banked row, never by this component." Witnesses riding: cube (B's UNPROVEN folds by reference), OD-35's stage-wide trap, i-13/C-3 (MINOR).
- **D-25 (EditorShell)** ⟨kf-EditorShell⟩ MINOR — no `viewport-fit=cover` anywhere → every `env(safe-area-inset-*)` term resolves 0px on a shell that calls `initIOSPlatformClass()`; four stranded terms each commented for the notch. Notch witness here; explicitly NOT banked elsewhere. **Compounds KF-APP-6** — one 390×844 capture decides both; do not shoot separately.
- **KF-SCR-1** ⟨kf-SequenceScrubber⟩ as banked (highest-value probe on that record) — two-finger pinch on `.seq-scrub` at 375×667: does `body.is-dragging` stick; does rubber-band fire `pointercancel` mid-drag (D-5 residue). Bundles with i-13/C-3 and K-13 — **one device session, three rows**.
- **K-13** ⟨kf-SequenceScrubber probe 4⟩ open question — Safari mousedown-focus on `tabindex="0"` divs, keypress-after-scrub on BOTH `.seq-scrub` and `.spring-rail`. **A cure-HOME decider, not severity** ("a shared gap changes the cure's HOME"): a shared gap makes the cure a demo-wide idiom, not two patches — **run before either packet writes its focus cure**.

### E · Contrast / non-text / canvas (the measurement seat — family (ii))

- **KF-AV-19 / D-8** ⟨kf-AnimationVisualizer⟩ MAJOR-band — rail + both end markers below 1.4.11's 3:1 in both themes; dashed end marker load-bearing; direction robust. **NO NUMERAL IN THE CORPUS IS QUOTABLE** (three mutually inconsistent recomputes, corrections register #5) — **KF.W9 is the DECIDING seat**: SS-13 re-derives on the live composited `Card cartoon tier="quiet"` plate. Same plate serves kf-KeyframeTimeline D-6/D-7 + kf-TimelineTrack D-5/D-6/D-m3 (ride banked D-7 residue #2, do NOT re-measure separately) + kf-TimelineCaret D·M-7.
- **`D-4` / `D-8` ⟨kf-SpringTarget⟩** — the contrast reads the record routes to this wave FIRST (`kf-SpringTarget.md:147`), discharged by **route (d)** at that record's residue item **#4** (*"Contrast reads over the live glass plate — the four derby tags (composited chain says 1.92-3.9 light); `.spring-target-marker` (50%) and `.sampler-ball` (65%), the two rows near the 3:1 floor"*). **No re-booking and no re-grade — the bank's disposition is NO-WAVE-OWNER + KF.W9 for both.** The ids are written here at repair round 6 (PASS-6 D6-6) because PASS-5's operationalization of route (d) was **id-token presence in the residue**, under which these were hard escapes, while pass 6 discharged them **by item**: the two passes' *"0 escapes"* were not the same claim, and the corpus's self-described worst contrast figure (**1.92:1 light**, this record's own superlative) reached this wave only as an unnumbered line inside ≈590 bytes. **Standing clause, stated once at §Carry head: route (d) discharges BY ITEM; a residue item need not name its ids — and where it does not, the consuming spec writes them, so the next census reproduces.**
- **KF-ET-8** ⟨kf-EasingTarget⟩ MAJOR (rescoped from BLOCKER) — the graphic the component exists to show below 3:1 while `aria-hidden`; strongest under PRM (compounds KF-ET-7). Reader-1's 1.1.1 kill SUSTAINED, narrowed to 1.4.11. **ARMED CONTEST**: selected-sparkline dark arm 3.31:1 vs 2.71:1 across the floor — SS-13 decides; severity follows the measurement.
- **KF-ET-14 / D-M-6** ⟨kf-EasingTarget⟩ MAJOR contingent — *(the banked reader-D alias **`D-M-6`** SPELLED at repair round 5 — RULINGS-5 §END KF-W9 D5-8; PASS-5 check D5-8: the bank's row is `kf-EasingTarget.md:53` — *"**KF-ET-14 · D-M-6** — the entire selected state is one colour channel at ≤4.63:1 light plus a 12px weight bump"* — and rounds 1–4 carried it under `KF-ET-14` alone, discharging the alias **by identity**. Folds cite their bank, and an id carried by identity is an id a reader cannot grep. No re-booking, no re-grade, no new row, no severity change.)* — selected state = one colour channel ≤4.63:1 + 12px weight bump (KF-ET-4 means the wash never paints): 1.4.1's exact target. The 4.63 is against the OPTIMISTIC `--card` baseline; SS-13 decides the AA arm on the true `.glass-capsule` plate. **Do not close on a producer fix alone** — the 1.4.1 single-channel point survives a restored wash.
- **KF-ET-4** ⟨kf-EasingTarget⟩ MAJOR at the consumer surface (ruled) — glass 7.0.0 ships `glass-chip.css` ORPHANED from its own style graph: the selected wash never paints; 28 cells lose `--radius-card` for 9999px; lost with it the coarse-pointer floor + `prefers-contrast: more` arm. **GLASS-OWNED → SS-6 BH relay; the fix is one `@import`; NEVER a demo-side hack.** Consumer obligation booked: any wave touching the file corrects :169-172's "primary 15%" (vendor truth `max(18%, floor+10%)`). KF.W9 witnesses the rendered pill-corner/no-wash — **one glance decides**. Substrate ruling: 7.0.0 measured IS the frontier's producer. KF-ET-16's producer half rides the same letter. **Dissent preserved: reader-2's BLOCKER.**
- **SPF-5** ⟨kf-SpringPhysicsFacet⟩ MAJOR — muted mono readout on the ACTIVE preset: rest 5.197 / hover 4.688 / active **4.464 FAIL**; direction arithmetic (5.20 → 4.46 on selection); the plus-lighter rider DEAD (K-3). NO-WAVE-OWNER cure; delivered pixels → SS-13 #3 (gamut mapping + color-mix rounding own the last digit). **Same capture as KF-ET-14** — identical question, identical producer plate.
- **#58** ⟨kf-CubeTarget⟩ MINOR — LOCKED Y axis ≈2.7:1 (or α's 2.53) light-arm below 3:1; at lock the line IS the state signal. Cure → KF.W6 (axis tokens, with the #8/#9 theme packet); KF.W9 witnesses; backdrop pixel = SS-13 #11. Capture in the same arm as D·B-1.
- **D-B2 / L-M-4 / C-M-4 (y-limb) + D-M4** ⟨kf-SpringHeatmap⟩ MAJOR each — 9 of 20 rows pixel-identical to the field's own background (nine zeros re-executed); worst adjacent step 1.026:1; all four presets in dead bands. D-M4: the interactive field has no perceivable boundary (border-vs-fill 1.22:1, fill-vs-card 1.039:1). Cure NO-WAVE-OWNER (one paint decision with the D-B2/D-M8 ramp redesign); KF.W9 witness rider — arithmetic closed to three decimals, only the percept open. **ARMED ESCALATION on the same capture**: the D-M3 three-branch fillStyle probe (SS-13 #1) — raw `light-dark(…)` + non-round-tripping fillStyle ⇒ field solid #000 in BOTH themes ⇒ **D-M3 → BLOCKER**. Cheapest equivalent: one light-mode screenshot of the Physics facet.

### F · AT / semantics (the AT cells — scope OWNER-UNRULED, OP-7/S-12)

> **S-12's "≥8 records" is made TRUE BY CARRIAGE, not by assertion (R-1e; R2-10) — and the claim is now stated at its TRUE COMPOSITION, counted, not asserted (KF-W9-CHECK D-12/D-2 cured at repair round 2, R2-15).** At authoring §F carried 4 rows from 4 records against a stated scope of ≥8. Round 1 added seven rows and stated "ten records" — a figure that reached ten only by counting a §H line-item (KF-CE-47) and a site-reference (kf-ChromeDock) alongside the bullets, which is not a §F bullet count and must not be read as one.
>
> **Counted at the bytes this round: §F carries 13 bullets over 8 records by bullet attribution** — kf-SpringHeatmap ×2 (D-B3 · D-m8) · kf-PlaybackRibbon ×2 (D-1/L-i1 · D-12) · kf-SpringTarget ×3 (D-5 · D-6 · D-14+N-4) · **kf-AmigaScene ×2 (D-2 · MISSED-A — new at repair round 2, R2-10)** · kf-App ×1 (KF-APP-33) · kf-CopyButton ×1 (KF-CB-9) · kf-SequenceAxis ×1 (D-12 + relay) · kf-SequenceScene ×1 (D17/D18-unit). **S-12's bar is cleared by the bullet count alone (8 ≥ 8)** — the strongest form of the claim, and the one that does not depend on how neighbours are counted. Three further AT/announcement rows ride the arm while booked elsewhere, named so nothing is hidden inside the number: **kf-SequenceTarget** (ST-1's AT limb, booked at §B; also the received-relay counterparty) · **kf-CSSCodeEditor** (KF-CE-47, a named line-item in §H's SS-13 aggregate) · **kf-ChromeDock** (the `:241`/`:291` sites under KF-APP-33). **Arm span: 11 records; §F bullet attribution: 8.**
>
> **THE ARM IS NOW SWEPT AS A CLASS, NOT AS A LIST (KF-W9-CHECK D-2).** Round 1 cured the ten ids PASS-1 enumerated and left the class open; the two AmigaScene rows below were exactly the class's remaining members — AT/discoverability rows routed to KF.W9 at both their own dispositions and their record's KF.W9 routing summary, with no §Carry line and no residue ordinal. This round re-ran the routing sweep over all 58 records rather than the previous seat's list (receipt at §Carry head) and found no further member **at that round's reading** — *a dated measurement, not a closure of the class (LAW B, repair round 3): **the class's standing verdict lives in the FRESHEST census artifact — `PASS-5/KF-W9-CHECK.md §1` (2026-08-28): 181 routed (record, id) pairs · 181 discharged · **0 escapes** · "AXIS 1 VERDICT: CLEAN", a fifth independent enumeration at the largest granularity yet attempted, which re-adjudicated four extractor candidates and nine route-(d) resolutions at the bytes and records that **the class booked at PASS-2 (R2-10's three) remains the last one found** — while booking eight defects against this wave on other axes.** ⟨**CITATION RE-POINTED at repair round 5 — RULINGS-5 R5-9 row 16; PASS-5 check D5-6.** Round 4 re-pointed this identical standing-verdict claim at four sibling cells (the header, §Carry head, the round-2 correction block, the R2-5 receipt) and **did not sweep §F**, so this file named two artifacts and two figures — PASS-3's 139 here, PASS-4's 168 there — as the home of ONE standing verdict, with the stale one governing the AT arm's class sweep. That is R4-10's *scope-not-instance* lesson in miniature. The superseded readings are retained as their rounds' dated measurements — **PASS-3 §1: 139/139/0** (two-shape extraction; named PASS-2's R2-10 bookings *"the class's last members"*); **PASS-4 §1: 168/168/0** — and the artifact's own instruction travels with this citation: ***"No pass-6 seat should inherit 181 either."*** **The class is now GREPPABLE, not remembered**: `grep -on 'PASS-[0-9]/KF-W9-CHECK' KF-W9.md` enumerates every citing site, and the round-5 scope receipt (§Repair ledger — round 5 §SCOPE) partitions them into *standing-verdict cells* (which MUST name the freshest pass) and *dated round-N records* (which MUST NOT be re-pointed).⟩ **SHADOW (LAW F(1)).** SHADOW: re-pointed set = {this cell's standing-verdict citation ⟨§F preamble⟩ — `PASS-3/KF-W9-CHECK.md §1` → `PASS-5/KF-W9-CHECK.md §1`}; struck/minted set = **none** — no row, id, routing or figure of this wave moves: the AT-arm class sweep, its members and the two AmigaScene bookings are untouched, and only the artifact that holds the verdict changes; disposition: the superseded readings are retained in place as dated measurements, never deleted; citing/terminus waves notified = **none owed** — the citation is local to this file, and the four sibling cells re-pointed at round 4 are swept to the same artifact this round by the §SCOPE class receipt.* Every row below is a **witness/datum booking**: the cures are NO-WAVE-OWNER or packet-owned, and OP-7/S-12's scope question remains the owner's.

- **D-B3** ⟨kf-SpringHeatmap⟩ MAJOR (D's BLOCKER declined) — `role="application"` keyboard widget exposing no value, announcing no change, **both children aria-hidden = EMPTY application subtree**; sole such role in the demo, unbanked. KF.W9 + NO-WAVE-OWNER (the packet decides the semantics). Capped MAJOR (the two LabeledSliders fifteen lines up are fully operable). **CURE SEQUENCING VERBATIM**: "any `aria-valuetext` must wait on the packet's lattice decision (L-M-1) — there is no honest cell to announce until the two input modes share one."
- **D-1 / L-i1** ⟨kf-PlaybackRibbon⟩ MAJOR-band — the sole AT-exposed scrub control has no accessible name (all three glass naming seams unused; announces "slider, 4820"; N-2: no meaningful value either); deliberately the ONLY AT surface, which CONCENTRATES the failure. Cure: one `aria-label` + tooltip re-homed as `aria-describedby` (D-5) → transport packet; the datum rides here. **SCOPE DISSENT ON RECORD** (M-2/N-2 → AT lane vs ≥8 records routing utterances here) — owner ruling requested, S-12.
- **KF-APP-33** ⟨kf-App; sites also kf-ChromeDock :241/:291⟩ MINOR-band — `aria-label="Scene"` overrides `<SelectValue />` per accname §4.3.1-2C, ×3 sites. Cure NO-WAVE-OWNER (orientation cluster); utterance → KF.W9 SS-13. kf-ChromeDock's D-9 announcement half FOLDS here, STRUCK from that corpus's UNPROVEN list.
- **KF-CB-9** ⟨kf-CopyButton⟩ as banked — **the registry's single highest-value SS-13 probe**: does the NESTED `role="status"` announce at all in VoiceOver/NVDA/JAWS despite Children-Presentational; if silent, ZERO working feedback on any channel, through every mount. **REACH-THROUGH**: kf-StartingStyleTarget #9 — silence re-weights KF-SST-7/11/29/39 together. One AT pass, five rows. (ST-1's AT limb — the five sliders' announced state — rides the same pass, sequenced with §B's ordering lock.)

- **D-5 (aria-hidden half)** ⟨kf-SpringTarget⟩ MINOR (demoted, ruling 4) — the derby is reachable by exactly ONE pointer gesture (`useDoubleTap` binds pointer events only; no Enter/Space/registry shortcut — census verified) and the whole overlay is `aria-hidden` (:107) with the discovery affordance deliberately retired (:173): keyboard-only cannot trigger, AT can neither trigger nor perceive. **OWNER DECISION CARRIED VERBATIM**: *"if eggs are ruled pointer-only by design, the row reduces to the `aria-hidden` half"* — and it is exactly that half this wave receives (`kf-SpringTarget.md:147` — *"the a11y data (D-5's aria-hidden half, D-6, D-14, N-4)"*). Cure NO-WAVE-OWNER, inside the packet's **gesture spec** (one spec with m-11 · i-18 · N-3 — never four patches); KF.W9 owns the AT witness. **Dissent preserved: reader-1's MAJOR** (ruling 4 preserved all six D MAJORs as dissent).
- **D-6** ⟨kf-SpringTarget⟩ MINOR (ruling 4) — *"no heading, no landmark, no accessible name anywhere in the scene (grep zero across seven files); the sibling at the identical rung is semantic (EasingTarget.vue:22-28 `<header>` + `<h2>`, same utilities)"* — **intra-repo divergence, not house style**, which is why it books as a defect and not a taste. Cure NO-WAVE-OWNER, inside the packet's **a11y one-edit family** (`:148`: *"D-6 heading · D-14 status region · N-4 name+valuetext"* — one edit, three rows); KF.W9 owns the utterance witness (the AT pass, S-8 family (vi)). **Dissent preserved: reader-1's MAJOR.**
- **D-14 + N-4 — ONE BINDING** ⟨kf-SpringTarget⟩ MINOR each (ruling 4) — **CURE LOCK RESTORED VERBATIM (M-25; the lock died with the row at authoring): D-14 = "NO-WAVE-OWNER + KF.W9 (one binding, with N-4)"; N-4 = "NO-WAVE-OWNER + KF.W9 (with D-14)". D-14 and N-4 are ONE cure and are NEVER split** — a repair that lands a live region without fixing the name/valuetext announces a state change about an unlabelled scalar. D-14: the one discrete, high-salience state change (settled↔tracking, :44-47) has no `role="status"`/`aria-live`; the surrounding 6 Hz restraint is correct and preserved; `aria-valuenow` tracks the COMMANDED value and cannot convey settle. N-4 (NET-NEW, reader-1 #4): the slider's accessible name is an instruction naming a modality (*"Drag to re-seat the spring target"*) while `aria-valuenow` announces the commanded target and the hero numeral 20px away shows live displacement — **two scalars, one unlabelled 0-100 announcement, no `aria-valuetext`**. The valuetext half is concordant with the banked **kf-SequenceAxis D-12 relay class**; the instruction-as-name and commanded-vs-live halves are net-new; **unlike banked N-14, no domain decision blocks this cure** (announce the commanded target honestly: noun-phrase name + valuetext) — so it is NOT sequenced behind S-14's N-1 lock. Killed-candidate C-#5 tested presence, not correctness: the `role="slider"` posture is the house exemplar (kf-OrbitalDrag ruling 5 cites SpringTarget.vue:65-70 first) and **presence ≠ correctness** is the whole of the row.
- **D-12 + the kf-SequenceTarget relay** ⟨kf-SequenceAxis⟩ INFO datum + **RECEIVED RELAY** — the `aria-hidden` half is an **adjudicated NON-DEFECT** (superlative 7: no focusable descendants AND the domain independently exposed by the five sliders' min/now/max triple, Target :99-104) and is carried so it is never re-filed — kf-SequenceScene's D18 aria-hidden limb dies on it (that record's killed-claims #9). What this wave receives is the datum + relay, verbatim: *"`text-mono-caption` on the row label uppercases the child span, so `@800ms` renders `@800MS` while the accessible name stays lowercase; and the sliders lack `aria-valuetext` (unit never announced — concordant with banked N-14's class)"*. **THE RELAY IS RECEIVED HERE, not merely noted (R-1e #28)**: its counterparty is the kf-SequenceTarget rows this wave already carries (ST-1's AT limb · D-4 · D-7) — one AT/HCM pass shoots the `@800MS` render and the missing valuetext together, and the finding is written back **under kf-SequenceTarget's own ids as an addendum** (G-KFW9-13, E-3 addenda-not-patch). The valuetext limb is sequenced by **S-14**'s N-1 lock; the uppercase-render limb is not.
- **D-m8** ⟨kf-SpringHeatmap⟩ MINOR (grouped D-m8/D-m9/D-m10, reader-1 confirmed) — **the 33-word `aria-label` announced whole on every focus, with no `aria-describedby` split**. Booked here beside the **carried D-m9 RTL limb at §C**: `:130` routes *"D-B3 · D-M6's forced-colors/contrast limbs · **D-m8**/m9 · witness list"* — the bank splits the pair across two of this wave's families, and both halves are now carried (at authoring only the RTL limb was). Cure: **KF.W9 owns the first two (D-m8/D-m9)**; the error-posture limb (D-m10 — a failed paint indistinguishable from a successful one by D-B2's own arithmetic) is NO-WAVE-OWNER with D-M3's cure and is NOT booked here. **Sequenced with D-B3's lock, carried verbatim**: *"any `aria-valuetext` must wait on the packet's lattice decision (L-M-1) — there is no honest cell to announce until the two input modes share one."*
- **D-12's demo-side stopgap** ⟨kf-PlaybackRibbon⟩ MINOR at that record, **GLASS-OWNED** — the slider's focus affordance is box-shadow-only and its selectors are absent from the producer's forced-colors list; per the ColorSpaceSelector L-10 precedent **the verdict class follows the cure's owner**, so the row is a BH-relay item (the ribbon's four-item letter, item (c) — *"one selector addition"*), and the demo-side `@media (forced-colors: active)` outline on `.timeline-green :deep(.slider-track)` is the bank's **named stopgap**. **BOOKED AS MEASUREMENT (R-1e #30, R-9a's measure-only posture): the witness is held here; the stopgap's cure bytes ride the transport/ribbon packet at KF.W13** and the producer half rides the SS-6 letter. **No demo-side forced-colors rule is authored by this wave** — the standing edict forbids it, and the WHC pass (S-8 family (iv)) is where this row is spent. **Dissent preserved: DU's MAJOR.**
- **The D17/D18-unit fold** ⟨kf-SequenceScene⟩ — `:153` routes *"D20's announcement gap · **the D17/D18-unit fold (SEQUENCED after banked N-1)** · the SS-13 list"*. The fold, carried by reference with zero re-bookings: **D17 + D18(unit limb) ≡ banked N-14 + kf-SequenceAxis D-8a/L-9/C-5** — the ms master-clock domain has no accessible OR visual-numeric exposure anywhere (Metric %, slider `valuenow` %, timecode a unitless 0-1 scalar — a third normalized exposure; the axis that NAMES ms is itself `aria-hidden` while the row handles announce ms correctly, the counter-example done right). **D18's aria-hidden limb is KILLED BY THE BANK** (≡ kf-SequenceAxis D-12, non-defect) and is not carried. **THE SEQUENCING LOCK IS CARRIED, NOT CITED (M-25) — entered at §Sequencing S-14: "SEQUENCED after banked N-1" — the valuetext-in-ms cure stays BLOCKED on N-1's time-domain decision, "no honest millisecond exists until the scene picks a clock."** D20's own announcement gap discharges through that record's residue item #12 (route (d), §H's SS-13 aggregate) and is not re-booked here.

- **D-2** ⟨kf-AmigaScene⟩ MAJOR — **BOOKED AT REPAIR ROUND 2 (R2-10 item 1; PASS-2 hard escape 3).** The bank, quoted: *"The rendered DOM is one bare `<canvas>` (:13–16, read) — **no role, name, tabindex, keydown, or fallback content**; both interactions pointer-only. The in-repo counter-example (SquareScene role=group + sliders + focus-ring) and the sharpest form (the Suspense skeleton is more accessible than the scene it loads) stand"* (`kf-AmigaScene.md:42`). **TWO SUPPORTING LEGS KILLED AT THE BANK, carried so neither is re-argued**: cube has NO keyboard rotation path (`updatePressedKeys` sets modifier booleans consumed only by pointer-delta handlers) and a real `<main>` landmark DOES exist (`EditorShell.vue:74`, deliberately not `display:contents`) — so the defect narrows to *no accessible name/role/operable path on the subject*. **CURE-SHAPE LOCK, verbatim**: *"the cure is a **shared subject-a11y idiom for cube AND amiga**"* — **never an amiga-only patch**, and never this wave's to spend: **Disposition NO-WAVE-OWNER (scene-repair wave, shared-idiom scope); KF.W9's Safari audit witnesses** (`:42`), routed a second time by that record's KF.W9 summary (`:158`, the witness list). **Route (d) is unavailable and stated so**: that record's twelve-item residue (`:130-141`) carries L-B1 · D-1 · D-6 · D-4/D-5 · MISSED-B · MISSED-C · C-6/S-9 · C-9 · C-17 · L-i5 · D-15 · C-15 — **no D-2**; this bullet is the id's only lawful home. Witness: the AT pass (S-8 family (vi)) reads the subject's announced role/name on the amiga route — **the falsifier is a named subject** (any announced accessible name kills the row); the trap is the surrounding `<main>`, which announces a landmark and no subject. Pairs with MISSED-A below as ONE mobile-Safari finding.
- **MISSED-A** ⟨kf-AmigaScene⟩ MAJOR — **BOOKED AT REPAIR ROUND 2 (R2-10 item 2; PASS-2 hard escape 4).** The bank, quoted: *"On touch there is NO affordance for the primary interaction: `cursor: grab` is the entire discoverability story (:249–255, read) and cursors do not exist on touch; the gesture legend was deleted at T.A10 (the template's own removal changelog says so); the sibling in the same register renders the literal 'drag me' as its stated design principle. Verified adjacent: no keyboard route either (D-2) → **zero discoverable interaction on a phone-filling canvas**"* (`kf-AmigaScene.md:54`). **Disposition NO-WAVE-OWNER (scene-repair wave; SS-13 confirms on-device)** — routed a second time at `:158`; that record's residue carries MISSED-**B** and MISSED-**C** and **not MISSED-A**, so route (d) is unavailable here too. **THE MOST ON-THESIS ROW IN THE FILE**: a findability defect whose only instrument is a real phone — *"all three axes audited the gesture's a11y, physics, and render gate; none audited findability."* Witness: the **iOS device session** (S-8 family (v)) plus the 390×844 mobile pass (family (iii)) — open the amiga route on a real device and record, before any hint, whether a first-time user has **any** signal that the canvas is draggable; **DISCRIMINATOR**: a capture that merely shows the scene proves nothing — the probe is the *affordance*, so the record is the visible affordance inventory (cursor is void on touch; legend deleted; no text, no icon, no hint) and the **FALSIFIER is any rendered discoverability signal at all**. **This wave spends no cure**: the touch-affordance repair is the amiga scene-repair packet's (S-11's roster; homed at KF.W11 by KF.W4), and R-9a's measure-only posture is untouched by both rows.

### G · Chrome, shell, and app spine

⟨kf-ChromeDock⟩ **`D-2-RESCOPED` / `D-2-residue`** — route (d), residue item #7 ⟨***the work order's "#8" corrected at execution, 2026-08-29: `kf-ChromeDock.md:136` numbers `D-2-residue` as item **7** of the UNPROVEN-NEEDS-LIVE register; item 8 is C-10's coarse-pointer read with D-6's label overflow***⟩; the record spells the id two ways and this spec spelled neither, which made it the single candidate a machine reader could not resolve without opening the bank (PASS-6 D6-9, entered at repair round 6 on the D5-8 pattern). Discharge unchanged: NO-WAVE-OWNER (icon ink policy with D-3); over-aurora visibility → KF.W9.

- **M-5 / C-6** ⟨kf-ChromeDock⟩ MAJOR (RESCOPED) — the re-expand watchdog's rationale names a FALSE mechanism but the watchdog is LIVE on the touch axis; `useTouchGate`'s document `touchstart` uses raw containment, no owner-stamp exemption: touch-expand → open scene Select (portalled) → tap item → **dock collapses UNDER THE OPEN MENU**. **CURE BLOCK CARRIED: deleting the watchdog would regress touch.** NO-WAVE-OWNER: fix prose to the touch-gate truth, KEEP watchdog, drop :207 `expand()`, consolidate C-14's twins. Producer half → SS-6 (touch gate consults hold counter + `data-glass-dock-owner`). KF.W9 SS-13 **#1** on that record: the touch witness decides relay urgency AND the prose wording; same session as the M-4 rider check. ⟨**BANKED ALIAS SPELLED at repair round 5 — RULINGS-5 §END KF-W9 D5-8; PASS-5 check D5-8**: the same record's **`D-6 / m-6`** (MINOR — `kf-ChromeDock.md:76`, the `[&>span]:line-clamp-none` no-op cargo-cult plus the absent truncation contract; disposition read at the bank this seat: *"NO-WAVE-OWNER; live overflow → KF.W9"*) is carried by this wave through **route (d)** — that record's residue item **#8** (`:137`): *"**D-6's label overflow** (longest live string 'Matrix Controls')"* — while its alias `m-6` was never written here. A reader resolving a bare `D-6` in this spec therefore landed on **kf-SpringTarget's D-6** at §F: a different record's id at the same token, the collision the round-2 D-9 repair existed to kill for the two `D-15`s. Spelled now and **record-qualified — `⟨kf-ChromeDock⟩ D-6 / m-6`**. This adds no §Carry bullet and no booking: the id's discharge is and remains route (d), exactly as PASS-5 §1 adjudicated it.⟩
- **M-2 / C-4 (the D-19 discriminator)** ⟨kf-ChromeDock⟩ as banked — D-19 KILLED by enumeration; the surviving sub-claim books INSIDE M-2/C-4. KF.W9 receives **the discriminating test**: combobox-vs-bare-div on /easing. **FALSE-CONFIRMATION TRAP, NAMED**: "a Curve-labelled node proves NOTHING" — the exemplar for G-KFW9-12.
- **D-4 (ChromeDock glyphs)** ⟨kf-ChromeDock⟩ MAJOR — three 32×32 pixel-art glyphs nearest-neighbour scaled at NON-INTEGER factors (icon-md fractional at BOTH common DPRs); +25% jump (16→20px) across the collapse crossfade on "the scene's identity moment". NO-WAVE-OWNER; geometry static-certain; perceived shear → KF.W9 at DPR 1 AND 2, collapsed and across the crossfade — **a DPR-2 capture is MANDATORY** (invisible at DPR 1 alone).
- **D-22 + RR-1 MISSED #1** ⟨kf-ChromeDock⟩ MAJOR — the `gap-2` wrapper as GlassDock's single slot child substitutes 8px for 6px AND DEFEATS the producer's entire staggered morph choreography (`nth-child(2 of *)` arms no-op with one child); latently defeats `overflow="wrap"`. Cure → KF.W6 (delete the wrapper; retune `--dock-layer-gap` if 8px wanted). Magnitude → KF.W9 — the stagger-loss capture is a visual call, not a measurement.
- **D-23 / C-5** ⟨kf-ChromeDock⟩ MAJOR — dock floats over the animated aurora canvas and never tells the legibility observer (sampler resolves `null` → static stack-walk). Cure → KF.W6 with **CURE LAW (RR-1's missed caveat, adopted)**: the INSTALLED resolver returns `null` for a string — the remedy MUST pass a getter or element; **a selector-string diff would ship green and change nothing**. Contrast delta + observer cost → KF.W9. The d.ts drift cell joins D-13's ledger → SS-6 relay.
- **KF-APP-5** ⟨kf-App; geometry per kf-EditorShell; riders kf-EditorHeader F3⟩ BLOCKER at bank — the header ribbon's three chrome controls `inert` + `aria-hidden` + zero-width at rest, no focusable affordance; source-decided from the installed producer; the phantom `mode="persistent"` already deleted at origin/master — **the lockout is NOT**. Cures: KF.W6 (supply `#anchor` / adopt the real API) + SS-6 relay (empty-anchor state unreachable-by-construction). KF.W9: rest-state render + tap affordance. **GEOMETRY CORRECTED**: rest sliver ≈12×52 px (not 12×40); only "does it read as an affordance" stays live. **COMPLETING WCAG 2.1.1 LEMMA**: with the dock's MbabbMenu copies keyboard-inoperable + kf-ChromeDock D-18, **no keyboard route to Share or theme-toggle exists anywhere in the app** — dead at three layers, frontier-durable. **TOUCH RIDER (F3a)**: the shortcuts modal is WHOLLY unreachable on touch.
- **KF-APP-6** ⟨kf-App; folds kf-ChromeDock D-1, kf-EditorStartScreen ND-1⟩ BLOCKER at bank — `--dock-margin` is a PHANTOM at all three substrates + producer; nine unfallbacked `var()` sites make the dock-anchor/work-area chain IACVT; fallback-carrying sites MASK it. Cure → KF.W6 (the phantom-token audit, D-15's recommendation ratified). KF.W9: rendered dock offset at 390×844. **PROVENANCE**: deleting commit `c7f7c96d` — died on a FALSE BELIEF about producer ownership. **SETTLER PROBE FIRST (P-2)**: `getComputedStyle(…).getPropertyValue('--work-area-top-offset')` at <1024px — empty string = IACVT confirmed; D-1/D-7/ND-6, the ChromeDock anchor, TD-36's extent, D-25's safe-area terms ALL hang off it. **Identity-guard event: three reader generations re-filed this bank as fresh; it appears ONCE, here.**
- **KF-APP-16 / KF-HA-7** ⟨kf-App (bank) + kf-HeroAurora (fold)⟩ MAJOR — `initStrategy: 'eager'` arms the GPU inside the mount commit on the LCP route, against the file's own docblock; re-arms on EVERY return home. Cure → KF.W6 (delete the prop — library-endorsed). Magnitude → KF.W9 (SS-13 P-4): **>50 ms attributed = escalation**; per-navigation re-arm measured; WebGPU AND WebGL2-only engines; Q14-class (carries T's LCP 5141/TBT 5988 history as the reason to measure). Same trace observes P-8's heap/leak witness. Axis C's BLOCKER KILLED (K11), concordant MAJOR.
- **KF-HA-2** ⟨kf-HeroAurora⟩ MAJOR — delivered wash structurally **~0.19**, ≈double the authored 0.1 (stacked grid-area siblings both at the ceiling; placeholder never hidden; canvas opaque); page ramps 0.10→0.19 at the LCP moment; every prior computation was on the false 0.1 premise. Mechanism PRODUCER → SS-6 (placeholder-hide-on-arm / documented compose-mode); which-number-was-blessed → KF.W6; perceptual magnitude + ramp → KF.W9 (SS-13 P-2, **with the calibration instruction**: compare `opacity-ceiling="0.05"` ⇒ composite ≈0.0975 ≈ the blessed 0.1). KF-HA-12 is judged AT the true ~0.19.
- **KF-HA-10** ⟨kf-HeroAurora⟩ MINOR — **BOOKED AT REPAIR ROUND 2 (R2-10 item 3; PASS-2 hard escape 5).** The bank, quoted: *"`motion: \"drifting\"` resolves to a nonzero drift quadruple (probed: 0.015/0.015/0.008/0.05), so `needsAnimation()` never parks outside PRM — **a whole-session full-viewport GPU loop under the least-visible layer on the page**; the amendment cut presence, not cost"* (`kf-HeroAurora.md:52`). **THE SPLIT IS THE ROW'S SHAPE, carried whole**: *"P-HERO provenance makes this a ruling question, not a fix order (the L axis's own calibration, adopted)"* — the **owner ruling (is the drift register blessed?) is KF.W6's**, banked at **KF-W6 §Carry** and in `KF-W6-CARRY.md` under the same id. ⟨**RE-VERIFIED AT THE SIBLING'S FINAL STAGE-1 BYTES, repair round 4 — LAW C(2)/LAW D, quote-by-command; the round-2 elision closed.** Substrate `docs/tranches/X/keyframes/waves/KF-W6.md`, **mtime 2026-08-28T16:59:25**; `grep -n 'KF-HA-10' KF-W6.md` → the §Carry row, quoted **whole** this seat: *"**KF-HA-10** (MIN) — drift never parks outside PRM: **AN OWNER RULING QUESTION, not a fix order — is the drift register blessed?** Frame cost → KF.W9."* — the round-2 receipt printed this with an unmarked ellipsis that swallowed *"— is the drift register blessed?"*, which is the ruling question itself and the whole reason the row splits; it is restored. Sibling half: substrate `carry/KF-W6-CARRY.md`, `grep -n 'KF-HA-10' KF-W6-CARRY.md` → **`:220`**, carrying the same disposition verbatim. That spec's → KF.W9 cross-edge additionally declares this a **split row at both ends** — measurement here, owner ruling there, *"neither end may record the id as closed on its own"*. **R2-10's conditional sibling correction is therefore still NOT triggered and no W6-side motion is owed** — re-measured at the final bytes, not inherited from round 2's reading.⟩ **KF.W9 owns the FRAME COST and nothing else.** Route (d) is unavailable and stated so: that record's residue is the named set **P-1..P-8** (`:103-114`) and names no KF-HA-10 — `grep -n "KF-HA-10" kf-HeroAurora.md` → exactly one hit, its own roster row. **PASS-1 MIS-BOOKING RECORDED (E-3, no edit to the PASS-1 file)**: `PASS-1/KF-W9-CHECK.md §4` asserted *"KF-HA-3 (SS-13 P-3), KF-HA-8 (P-1), KF-HA-10 (frame cost) all have residue homes. **BOOKED.**"* — true of the first two, **false of the third**; asserting a residue home for an id its record never lists is the citation-inheritance failure the corpus convicts elsewhere, and it is why this row survived a whole repair round. Witness: **the same trace as KF-APP-16's P-4 and P-8** (S-8's KF-APP-16 trace family — one profile, three rows; no probe double-spent): with the hero mounted and PRM **active**, record continuous-loop cost over a fixed window — draws/s, GPU time, and whether `needsAnimation()` ever parks. **DISCRIMINATOR**: an idle-looking page proves nothing (the layer is the least visible on the page by construction) — the reading is the *counter*, never the pixel; **FALSIFIER**: any parked frame under PRM kills the row. Sequenced with KF-HA-2's calibration instruction (the cost is judged at the true ~0.19 presence, not the authored 0.1). **Cure spent here: none** — the ruling is KF.W6's, the row is a measurement.
- **KF-HA-12** ⟨kf-HeroAurora⟩ MAJOR — neither `forced-colors: active` nor `prefers-reduced-transparency: reduce` reaches the aurora, though BOTH are house idioms in the producer's own sweeps — a PRODUCER conformance gap. Primary cure → SS-6; two-line consumer stopgaps → KF.W6; visual outcome at ~0.19 → KF.W9 (SS-13 P-6, settles MINOR vs `display:none`-outright). **The wave's only reduced-transparency row** — the census counted neither query (OP-4's `.media` test applies).
- **EH-5 + EH-8** ⟨kf-EditorHeader⟩ MAJOR / MINOR — `aspect-square w-8` INERT → 32×36 box; the ribbon cluster incoherent three ways (boxes 24×24/32×40/32×36, glyphs 24/16/20). Cure → KF.W6 (ONE sizing decision); rendered boxes + the three-height ladder → KF.W9 under real font/zoom. **MOOT-ON-DELETE guard carried (#5)**: the fork's as-if-live defects die with KF-APP-41's delete; the SIX live escapes (EH-1..EH-5, EH-9's SharePopover batch) survive it — **do not shoot the fork**. ⟨**THE GUARD WAS VINDICATED — dated line entered at repair round 5, RULINGS-5 R5-1(5).** At repair round 4 a cross-wave ruling (R4-1) read KF-APP-41's delete as mooting *"every EH-\* limb hosted in EditorHeader.vue"* and KF.W6 struck **EH-4 · EH-5 · EH-8** on that premise — built from co-id twins, never from the rows' banked anchors, and inverting the record's own central finding. **PASS-5 booked the strike as hard escape 1 and RULINGS-5 R5-1 rules it a BLOCKER**: the premise is STRUCK and **EH-4 / EH-5 / EH-8 are RESTORED as LIVE rows at their banked anchors**, one row per anchor (LAW F(3)), spendable under KF.W6's existing grants. Receipt, anchor-only per LAW E(4): **`KF-W6.md` §Carry — the restored EH-4 / EH-5 / EH-8 rows** and their SHADOW line; **`KF-W0.md` §Excluded** carries the family's routing reciprocal (R5-1(7)). **The frontier fact this wave asserted is re-derived here, read-only at `origin/master 81a56990` this seat** — `git show 81a56990:demo/components/instrument/shell/EditorShell.vue | sed -n '32,47p'`: **`:45`** `title="Toggle dark mode"` · **`:46`** `class="aspect-square w-8 scale-on-hover"` (the DarkModeToggle at `:44-47`), the shortcuts `Button`'s own `aspect-square w-8 scale-on-hover` at **`:36`** (cluster mate, `:32-40`); and `git show 81a56990:demo/app/dock/MbabbMenu.vue | sed -n '19,22p'` → the **third** DarkModeToggle instance at **`:20-21`**, `title="Toggle dark mode"` + `class="aspect-square w-5"`. **The defects are live at the SHELL, which is why the fork's death moots nothing here.** **Nothing in this wave's row moves**: EH-5's rendered box and EH-8's three-height ladder remain KF.W9 measurements (SS-13, under real font/zoom), severities unchanged, cure still KF.W6's ONE sizing decision, no cure spent here — the restoration gives those measurements a live cure-owner again instead of a struck one. **Measurement note (so the capture is not taken at the dead fork)**: the ladder is shot at the **live shell ribbon cluster** (`EditorShell.vue:32-47`), and the MbabbMenu instance carries a DIFFERENT sizing utility (`w-5`), so it is a second, separately-labelled cell — never averaged into the shell's. **EH-4 is not a KF.W9 row** (its cure is dropping the `title`; no render measurement is owed) and is named here only because the guard names it.⟩
- **D-8 / C-17 (EditorShell z-contract)** ⟨kf-EditorShell⟩ MINOR — the "strictly ascending, demo-OWNED" z-contract omits `--z-header: 35` + seven producer rungs; grep enforcement blind to consumed-component drift. KF.W9 owns the occlusion question: expanded ChromeDock at 375×667 vs the ribbon rect (z 40 over z 35, shared band); :29's "no occlusion (inv δ)" TRUE of the bottom dock, UNESTABLISHED for the top. kf-SharePopover probe 5 (`z-index` on `[data-slot="dropdown-menu-content"]` — 130 confirms K-2; 140 kills K-2 AND MM-15/MM-4 together) rides the same capture.
- **RR-1 missed #4 (clip-crop) + D-11/L-9 + D-28** ⟨kf-EditorShell⟩ MINOR/MINOR/INFO — (a) `overflow: clip` crops the 1.08 hover lift (~1.6px) — filed four times, never asked if it has room; one hover frame; dies with the migration. (b) `:key="superKey"` full-subtree remount vs App.vue's reactive same-expression — transport HARD-CUTS while the subject cross-fades; perceived severity only; cross-refs banked kf-AnimationControlsGroup C-9 (lift DemoGlobalChrome) + L-S2 (borrowed correctness). (c) `<Transition appear>` starts the LCP `<h1>` at opacity 0 ≈1 frame — **trace before acting**. All three: KF.W9 captures.
- **KF-EST-2 / KF-EST-4 / KF-EST-8** ⟨kf-EditorStartScreen⟩ MAJOR each — (2) italic swap-window: deck + hint paint upright for the whole window; the "zero new payload" claim false in both halves. (4) `.hero-band` unbounded in a no-scroll shell: **the failure mode is a CLIP, not a scroll — tell the audit explicitly** (419.9px into 397.1px at 1280×720, two concordant derivations). (8) `line-height: 0.92` → −0.04..+0.01em ink clearance; `title` is public; the default escapes by letter-luck. Cures all → KF.W6. KF.W9 owns: the cold-cache swap **FILMSTRIP**; clip magnitude at 1280×720 / 1024×640 / 320×568-two-line-hint; the collision render with a descender-bearing two-line title. **Residue ORDERING BINDING**: P-1 (home Play control) FIRST — before any KF-EST-7 copy cure — then P-2 (the KF-APP-6 settler); "the first two decide arithmetic several others consume."
- **KF-APP-1** ⟨kf-App; folds kf-MbabbMenu's lead BLOCKER⟩ BLOCKER — `togglePpMode` TypeErrors on every click (message pinned by execution) AND the only correct writer lives in a never-rendered `headerLeft` fn (KF-APP-17): a shipped, persisted feature simultaneously crashing and dead. Cure NO-WAVE-OWNER (demo repair) with **CURE LAW**: the `setPPMode` twin must be disposed in the SAME motion as the KF-APP-17 sweep (repairing :100 alone lands two writers for one flag). KF.W9: click-witness, console string pinned. Riders carried: `440e5c30^` extraction provenance; seeded-bucket write per failing click. kf-CubeScene SS-13 #8 is **MOOT until this lands** (post-cure only: the ppMode faces).
- **MM-4 (+ MM-15 / MM-34)** ⟨kf-MbabbMenu⟩ MAJOR-band, GLASS-OWNED — the unlayered dropdown sheet beats the demo's layered rules, nine consumer casualties incl. the destructive row's colour. → one of four items on the single SS-6 communiqué. **CONSUMER LOCK**: the destructive row repairs to the demo's OWN red (MM-30), not the vendor's. Computed-style witness (SS-13 #1): `cursor: pointer` on the Clear row or the destructive token resolving kills MM-4/MM-15/MM-34 together — a falsifier that kills three rows. **MUST-CARRY RIDER on banked kf-ChromeDock M-4**: bare `{ type: Boolean }` makes the menu non-modal by accident and permanently controlled — **M-4's cure as worded would ship an UNOPENABLE MENU**; two riders travel with M-4, never separated; KF.W9's touch/menu session is the evidence.
- **TD-1 / TD-2 (bundle) + TD-38 / TD-40 / TD-41** ⟨kf-TransportDock⟩ MAJOR-band — Space-vs-Enter divergence, the collapsed-mirror inert swap, and a stale-id path predicting a SPURIOUS actuation from drag-in-and-release. KF.W9 owns **13 probes**; TD-41(b) is **"the born-RED witness for the cancellation law"**; TD-38 decides which branch of the inverted `.stop` set ships ("either way the policy is wrong"). **STANDING LOCK: TD-1/TD-2's cures BUNDLE — one motion, never split.** TD-36's occluded extent measured BEFORE and AFTER the KF-APP-6 cure — two captures, one row.
- **LP-1 (+ KF-CO-1 / KF-CO-8)** ⟨kf-LayerConfigPanel + kf-ChannelOptions⟩ BLOCKER-band at the ChannelOptions bank — the frozen render edge (model updates, control doesn't); KF-CO-1 the controlled-closed dropdown chain (all three axes graded "~nil", one re-reader alone caught); KF-CO-8 the switch rendering OFF while true. KF.W9 owns the witnesses; cures OPTIONS-UNIT / NO-WAVE-OWNER; KF-CO-1 carries a GLASS-OWNED half on the relay. **SEQUENCING LOCK (standing): the KF-CO-1/KF-CO-8 cures are SEQUENCED BY LP-1 — no write→render edge.** Witness lock: **run them together** — one capture, three rows, the cheapest confirmation in the registry.

### H · Wave-law rows (bind the harness, not a component)

- **B18-26** ⟨INTAKE-ADJUDICATION §3 / lane-keyframes-b10-b21:151⟩ wave LAW — "0/73,568 real Safari" is TRUE as arithmetic (`152 × 11 × 4 × 11 = 73,568` exactly), UNPROVEN as coverage (Kronecker self-cross-product, no product consumer). ADJUDICATED VERBATIM: **inherit the NUMERATOR, REJECT the DENOMINATOR; KF.W9 scopes its own surface list.** Binding on gate authoring (G-KFW9-3); seeds-never-denominators per §Surface-list protocol 2.
- **I-20 (cell separation)** ⟨**`X/waves/W6.md` §8 Verification Artefacts** — the **MATRIX** π-obligation, a *bolded inline label* inside that section's π-obligations paragraph (`:367`), cited as an inline label and not as a heading · **`X/waves/W11.md` §G8** — *"G8 — Real-Safari evidence cell, held separate · BORN-RED"* (`:290`)⟩ wave LAW — webkit-engine ≠ safari-app; the law has fired TWICE. Unrun cells read UNMEASURED / UNREACHABLE-IN-CELL / UNVERIFIABLE-HERE; a green cell without per-shot sha256 FAILS. Applies to every per-engine probe: kf-CSSPasteDialog #2, kf-KeyframesAddDialog #1 (both verbatim "chromium / webkit / safari-app are separate cells"), KF-ET-26/12's freeze half, KF-SST-9 (Safari; Chromium <127), kf-TimelineHoverPreview D-3's Chrome ≥127 horn, kf-KeyboardShortcutsModal #1 (Space-vs-button per engine).
- **KF-AX-26** ⟨kf-CubeAxisLines⟩ INFO, BOUNDS LAW — the axis-lock reveal is a NAMED EXCLUSION from the demo's only visual gate, and `e2e/` does not exist. **The scoped surface list must NOT inherit the demo gate's own exclusions** — the excluded surface is exactly the one no instrument has ever seen. Test row rides KF-AX-16.
- **SS-13 residue — the whole backlog** ⟨all 58 adjudicated records⟩ the wave's principal deliverable — **565 enumerated + 25 prose-carried ≈ 590 probe rows; 0 executed** *(re-derived at the bytes at repair round 2 — R2-15/R2-5; the round-1 cell read 563/≈588, see §Surface-list protocol 2 for the command and the two-seat agreement)*. Each closes EXECUTED (capture path + per-shot sha256 + substrate ref + cell) / RETIRED (naming the ruling) / UNREACHABLE-IN-CELL. **This ≈590 IS the scoped surface list** — the honest replacement for the rejected 73,568. Shared-capture families honoured (S-8) so no probe is double-spent. **NAMED LINE-ITEM ADDED (R-1e #32): kf-CSSCodeEditor KF-CE-47** — *"latent token-contrast debt in both themes (comments 3.03/2.73:1, numerics 3.30:1), due the moment KF-CE-1 lands; plus the Δhue ≈ 207° two-plates taste question … ruled UNPROVEN-NEEDS-LIVE here"* (`:85`). It is booked here as **witnesses to SS-13/KF.W9** because that record's own nine-item residue does not carry it (KF-CE-33 does, and rides residue #3 — this row escaped both routes at authoring). **The EDITOR-UNIT theme-repair cure half is NOT booked here** — it stays with the unauthored **KF.W12** packet (noted, not booked); this wave supplies the live numerals only, re-derived AT CAPTURE per KF-SKEL-22. The record's line-number sibling KF-CE-34 is EDITOR-UNIT's and is not this wave's.
- **The escalation-trigger register** ⟨twelve records · **thirteen armed triggers**⟩ wave-level obligation — the ONLY registry rows whose grade is not final. **ARITHMETIC CORRECTED 2026-08-28 (repair round 2, R2-15; KF-W9-CHECK D-5)**: the enumeration below is **thirteen** ` · `-separated armed triggers over **twelve** records, counted at the bytes; two dependent cells asserted **fourteen** (§Goal and G-KFW9-13's witness) and are restated at thirteen in the same motion. *Composition stated so the discrepancy cannot regrow*: the fourteenth was the kf-TimelineTrack item, which binds **two banked ids at one record** (M-7/missed-4's `deltaY: 0` joint MAJOR **and** D-4's label-collision) as **one armed row** — one trigger, one measurement, two ids. **This is the D-10 class the round-1 repair cured for the sibling negative register and did not apply here; both registers now state their figures at their own enumerations.** Armed: KC-25 (z-modal punch-through → MAJOR) · KF-ES-3 (`jump-none` faults render → BLOCKER; decides L-i2's ordering) · D-M3 branch (a) (→ BLOCKER) · kf-TimelineTrack M-7/missed-4 (`deltaY: 0` ⇒ joint MAJOR) + D-4 (label-collision) · KAD-7/D-2 (`pre[contenteditable].tabIndex` per engine, toward BLOCKER) · KF-SST-15/-16/-17 · KF-TFP-7 (via KF-CO-5) · KF-TD-8 (one `performance.measure`: INFO-forever or promote) · KF-AV-41 (live counterexample promotes) · KAD-F4 (R-A's MAJOR revivable) · KF-ET-8's contested dark arm · N-5 (kf-SpringTrace — cheapest probe, decides MINOR-vs-MAJOR) · L-M-10/N-3 + KF-TFP-19 (INP hooks). **No trigger closed by inference; each names its measurement; an unrun trigger's row is reported PROVISIONAL at close, never silently inherited** (G-KFW9-13).
- **The negative register** ⟨**six records · eleven probes**, verbatim⟩ wave LAW — **ARITHMETIC CORRECTED (KF-W9-CHECK D-10): the head claimed "eight records" over an enumeration of six, while G-KFW9-12's witness said "six retired/mooted/struck probes" over an enumeration of eleven — three numbers, none agreeing, on a register whose entire point is that a retired probe re-entering the list is a gate failure.** The true count, by enumeration below: **6 records · 11 probes** (KSM 1 · KeyframesStringControls 4 · KeyframeCard 2 · MbabbMenu 2 · TimelineTrack 1 · SequenceAxis 1) **+ 4 named traps** at distinct records (ChromeDock · SpringHeatmap · EasingTarget · SquareScene). Both dependent cells (this head and G-KFW9-12's witness) now read the same figures. RETIRED/MOOTED/STRUCK, never re-probed: kf-KeyboardShortcutsModal's Tab-reach probe · kf-KeyframesStringControls' four (D-1 rendered-landing; the Monaco escape-hatch ≡ banked KF-CE-3; the `npm ci` probe mooted by `origin/master:77`; C-2 ≡ value.js R1) · kf-KeyframeCard's former L-10/C-5 dialog/drawer probes (STRUCK, containment) · kf-MbabbMenu's `useHideOthers` probe (MOOTED, non-modal branch never calls it) + D-7's leading question · kf-TimelineTrack #12 (checked-and-negative) · kf-SequenceAxis #5. **TRAPS, discriminator required**: kf-ChromeDock M-2 (combobox-vs-bare-div) · kf-SpringHeatmap D-M2 ("Light→dark is NOT the tell" — K-6; the signature is cured-by-resize-not-by-retoggle) · kf-EasingTarget KF-ET-24 ("on a filter change while paused at scrubbed phase ≠ 0 — NOT on entry", kill #9) · kf-SquareScene C-1 ("if the pixel tracks the swept hue the row dies — it will not"). Published with the surface list (G-KFW9-12); a retired probe re-entering the list is a gate failure.
- **The witness-substrate law** ⟨kf-App.skeleton r.1 · kf-TypingDots r.1 · KF.W0 §B-12 · KF-AX-4 · KF-SKEL-22 · KF-AT-28⟩ wave LAW — the corpus was measured against stale `8281638c`; DISQUALIFIED as witness substrate; kf-TypingDots INVERTS (the dirty worktree IS the frontier). Every witness runs against **origin/master or later**; every capture names substrate ref + sha + cell; contrast numerals re-derived AT CAPTURE (KF-SKEL-22); frames stamped (KF-AX-4); one pass, one pinned commit, re-anchored lines (KF-AT-28). **WITNESS REFRESH** (four independent records, = OP-5): re-run **`npm run gh-pages`** before trusting ANY byte-offset receipt (**R-11** — `build:gh-pages` exists at no coordinate; `package.json:43` is `"gh-pages": "vite build --mode gh-pages"`); installed-dist re-derived records are EXEMPT and say so. The four records whose byte-offset trust hangs on the refresh — kf-CSSPasteDialog #8 · kf-KeyframeTimeline #13 · kf-TimelineCaret #12 · kf-TimelineTrack #11 — are unchanged: **the command they wait on now exists.**

- **KF-AV-28 · STANDING SUPERSESSION RIDER (KF.W7's defining lock) — carried, not cited.** Banked at `kf-AnimationVisualizer.md:35`, `kf-PlaybackRibbon.md:36`, `kf-SequenceScrubber.md:36`: KF.W7's S-9 evaluation (KF-AV-28) may supersede any behavioral cure on a NO-WAVE-OWNER row banked under an evaluated surface; a per-surface SWAP verdict discharges the bespoke rows banked under that surface with the component. Rows in this spec governed by the rider are spent CONDITIONALLY: no cure on a governed row lands before KF.W7's verdict for its surface, or the executing seat accepts the throwaway explicitly; at execution the seat STATES which governed rows a SWAP verdict has discharged BEFORE spending any cure (KF-W7 G15's discharge discipline).
  **Governed rows carried in this spec, by id (R-10; KF-W9-CHECK D-7/E-10 cured at round 1 — the rider was paraphrased at S-10 as *"evaluate, not mechanical swap"* and never named, and M-25 holds that a rider is CARRIED, not cited). ENUMERATION WIDENED 2026-08-28 (repair round 2, KF-W9-CHECK D-8) — re-derived against the three banked coordinates rather than inherited from R-10's named set:** the rider is banked at three records and each states its own reach, re-read this session — `kf-AnimationVisualizer.md:35` (*"A standing sequencing rider on **every NO-WAVE-OWNER row below**"*) · `kf-PlaybackRibbon.md:36` (*"Standing rider: KF.W7's timeline-evaluate verdict may supersede **any NO-WAVE-OWNER cure below** (the KF-AV-28 rider)"*) · `kf-SequenceScrubber.md:36` (*"the KF.W7 ScrubberTimeline evaluation **gates the component-shape rows**"*). R-10's named set was the **floor**, not the census: the ribbon's rider governs every NO-WAVE-OWNER row at that record, and this spec carries four ribbon rows. **The governed set, by id — R-10's four plus the ribbon's three packet-owned rows**: **KF-AV-19 / D-8** (§E — kf-AnimationVisualizer, the `ScrubberTimeline` re-derivation surface KF-AV-28 names by intake) · **KF-SCR-1** (§D — kf-SequenceScrubber) · **K-13** (§D — kf-SequenceScrubber probe 4) · **D·D-8's limb** (§B's D-16 fold — kf-SequenceScrubber's forced-colors limb) · **D-3 + DU-M-1** (§B — kf-PlaybackRibbon; cure = KF.W13's transport/ribbon packet, so **RB-1 ⟨kf-RibbonBar⟩ is governed BY IDENTITY**, though its own bank is not a rider coordinate — stated, not assumed) · **D-1 / L-i1** (§F — kf-PlaybackRibbon; cure → the transport packet) · **D-12's demo-side stopgap** (§F — kf-PlaybackRibbon; the stopgap bytes ride the transport/ribbon packet at KF.W13). **Carried but NOT counted governed**: **D-24** (§A — kf-PlaybackRibbon) is a PRM/RTL **datum** row on which this spec names no cure; it enters the governed set the moment a cure is spent on it, and is named here so its absence is a decision rather than an oversight. **Seven governed rows, one identity-governed twin.** This wave spends **no cure** on any of them — it holds their witnesses — so the rider binds here as a **witness-ordering** obligation: a capture is still lawful before the verdict, but a verdict-superseded row's witness is reported as such at close rather than silently inherited (G-KFW9-13). The ribbon's own §7 caution rides with the rider: **this ribbon is the NON-bespoke case (it consumes the real `Slider`) and carries BLOCKERs anyway — "swap onto the primitive" is never sufficient as a cure.**

## Gates

**Thirteen gates, all born-RED at authoring, each with its named live witness (L-19 — no descriptive invocation).** *Count restated at repair round 1: fourteen were authored; **G-KFW9-10 is STRUCK as a gate** and re-cut as a sequencing DECLARATION (R-9a item 1). **Gate ids are NOT renumbered** — anti-rename holds for this spec's own coordinates exactly as it holds for banked row ids, so the surviving thirteen keep `-1..-9` and `-11..-14` and every inbound citation still resolves.* No gate is runnable before G-KFW9-1 opens a session, G-KFW9-14 pins the substrate, and G-KFW9-3 publishes the surface list. Witnesses re-verify at wave-open. **This wave measures; it authors no product cure and claims no CI colour.**

- **G-KFW9-1 · A real-Safari cell exists at all** — RED. Witness (measured): `git ls-files …/audit/visual/safari-real/` → **4** tracked (`MATRIX-SAFARI.md`, `ROUTE-MATRIX.json`, `STATE-MATRIX.json`, `picker-safari26.4-light.png`) vs **31** on disk — 27 witnesses uncommitted and therefore nonexistent (L-7); corroborating, `shots/` = 11 on disk / 0 tracked. The only real-Safari corpus (2026-07-27, Safari 26.4 / macOS 26.4.1, `localhost:9000`, API-LESS) is **value.js routes, pre-X** — zero real-Safari execution against any keyframes.js tree. CLOSES: safaridriver capture via `workflows/safari-real-matrix.js` against the named substrate; every shot force-added; per-shot sha256 sidecar.
- **G-KFW9-2 · Cell separation (I-20)** — RED. Witness: I-20 has fired TWICE; six records author per-engine cells; no separating matrix exists. CLOSES: every result carries its cell label from the §Protocol-4 roster; unrun cells read UNMEASURED/UNREACHABLE-IN-CELL/UNVERIFIABLE-HERE; a webkit verdict in a SAFARI column is a gate FAILURE; green without sha256 fails; each cell carries OP-4's `.media` capability record.
- **G-KFW9-3 · The wave's own scoped surface list** — RED. Witness: B18-26's 73,568 = `152 × 11 × 4 × 11` exactly (split `46,816 + 26,752` exact) — a Kronecker self-cross-product; adjudicated inherit-numerator/reject-denominator; no scoped list exists. CLOSES: a published list derived from the ≈590 banked probes (G-KFW9-4) + the seeds (V6-18/-19/-21/-22/-23/-28 catalog-only, CM-15) as seeds only, honouring KF-AX-26 (never inherit the demo gate's own exclusions; `e2e/` does not exist). A fraction quoted before the list is published fails this gate.
- **G-KFW9-4 · SS-13 residue discharge, 0 of ≈590** — RED. Witness: **565 enumerated + 25 prose-carried**, re-counted at the bytes at repair round 2 (R2-15/R2-5 — the round-1 cell read 563; the reproducing command and the two-seat agreement are at §Surface-list protocol 2); 0 executed. CLOSES: every probe EXECUTED / RETIRED / UNREACHABLE-IN-CELL with the shared-capture families honoured; the denominator re-derives at open.
- **G-KFW9-5 · The PRM census is made true** — RED. Witness: `lane-frontend.md:462` claims verbatim "13 enforcement sites across 12 files" (10 CSS + 3 JS); seventeen banked rows correct it (JS = 4 sites / 3 mechanisms; **zero engine motions counted**: amiga 0-of-4, cube 1-of-5 with the rAF glide, square 0-of-4, spring 60 Hz, `playReel`, KeyframesEditor ×3 one infinite, the twin 1000ms sweep). CLOSES: ONE enumeration of every demo motion with flag state + layer (CSS/WAAPI/rAF/engine), replacing §6.5; each of the seventeen rows resolves against it; the census row amends, the lane file stays dated evidence.
- **G-KFW9-6 · The intensity form + the final-frame hazard** — RED. Witness *(**re-cut 2026-08-28, repair round 3 — RULINGS-3 R3-7 + R3-9**; PASS-3 check D3-1/D3-2. Two faults cured in one motion: a witness clause a one-line grep refutes at BOTH baselines, and a witness family anchored at the ref this wave exists to disqualify.)*: engine default false (`defaults.ts:87`); **`reducedMotionScale` exported on the LIGHT barrel (`index.ts:50`) with ZERO demo consumers and THREE LIVE LIBRARY consumers** — `physics/spring/progress.ts:153`/`:232`/`:385`, each assigning `this.amplitudeScale` (the authored *"used by nothing"* is **STRUCK as unbanked**; §A carries the census and the bank's own narrower wording); PRM snaps to FINAL frame — **`engine/play-lifecycle/frame.ts`'s `playFrame` flip (`:131`) → `snapToReducedMotion(anim)` (`:137`)**, and the up-front/final-frame arm at **`engine/play-lifecycle/strategies.ts:109`** *(the authored `play-lifecycle.ts:214/:376` resolves only at `8281638c`; frontier map at §Bounds)* = 100% width for KF-KE-8's sweep, idle-indistinguishable per KF-KE-21. **RED restated to the gap it actually measures (R3-9.2)**: *the DEMO half does not yet consume the resolver the LIBRARY half already ships* — `SpringProgress.amplitudeScale` is the in-tree precedent (multiplicative displacement-from-rest scalar, applied `:323`/`:352`), so the unification extends a shipped mechanism rather than inventing one, and the gate must not be read as claiming the resolver is dead code. **Falsifier**: fails if the gate is driven green by adding a demo consumer that binary-snaps rather than scaling, or by any re-statement that the resolver is unconsumed.

  **CLOSES (re-cut 2026-08-28, repair round 4 — RULINGS-4 R4-3, on the G-KFW9-9 template; PASS-4 check D4-2).** This wave closes on its own measurements only: (i) the **flag-state-and-layer enumeration** — the group arm (`g.respectReducedMotion`, `group/group.ts:57`) and both standalone-arm options bags, published as a register row; (ii) the **KAD-11 pair's rest-state capture** — the idle-indistinguishability witness at 100 % width; (iii) the `reducedMotionScale` consumer census (R3-9's, re-cited). The unification constraint — *"no unification lands that snaps a progress animation to a rest state its own banked defect makes idle-indistinguishable; group arm via `g.respectReducedMotion`, standalone arms via options bags; the KAD-11 pair lock holds"* — is HANDED to **KF.W5** (engine half) and **KF.W6** (tokenization half) as a **DECLARED SEQUENCING OBLIGATION** (the S-2/S-15 idiom), read **UNMEASURED here** until their acts land. **Falsifier**: fails if a green is recorded on the constraint's behalf rather than on the three measurements; fails if the register omits an arm.

  *Why the re-cut, recorded so it is not undone.* The authored CLOSES named **edits to keyframes.js `src/animation/`** — the PRM unification itself, whose engine half is KF.W5's and whose demo half is KF.W6's or a packet's, as **this gate's own §Bounds row says**: *"every PRM cure is KF.W5's (engine), KF.W6's (tokenization), or a packet's — a standing write grant here would re-open the OP-2 breach the ruling closed."* After R-9a this wave holds **zero write grants in the kf tree** and spends **no cure**, so **no act available to this wave could turn the gate green**: the wave's central adjudicated deliverable was gated on an oracle it cannot reach. This is round-1 check **D-3** surviving at its third gate — that check cured two of three (G-KFW9-9 re-cut to the born-RED witness pair with the AFTER witness deferred; G-KFW9-10 struck outright) and round 3 rewrote this gate's **witness** without touching the close condition standing underneath it. **Of the two lawful cures the ruling weighed, the in-grant arm is the one taken**: the acts cannot move to W9 without re-opening the OP-2 breach, so the CLOSES moves to the acts instead. The constraint is not weakened, dropped or diluted — it is **relocated to the waves that can discharge it and booked at both ends**, and it is carried here verbatim so the obligation travels with its words rather than by reference.

  **The reciprocals, verified at the siblings' FINAL stage-1 bytes this seat (LAW C(2), quote-by-command — both write in stage 1, this wave in stage 2):** `grep -n 'R4-3' waves/KF-W5.md` (substrate `KF-W5.md`, **mtime 2026-08-28T16:55:55**) → its §Carry **B-6 · KF-TD-1** row carries *"**← KF.W9 `§Gates` `G-KFW9-6` hands the unification constraint here as a DECLARED SEQUENCING OBLIGATION** … **This wave carries the ENGINE half** … **This wave's act discharges the obligation and KF.W9 records the discharge**"*, with its own inbound anchor stated as **anchor-only** because this seat writes later; `grep -n 'G-KFW9-6' waves/KF-W6.md` (substrate `KF-W6.md`, **mtime 2026-08-28T16:59:25**) → its **KAD-11 — THE PRM ROW** carries *"**← `KF.W9` `G-KFW9-6` hands the unification constraint here as a DECLARED SEQUENCING OBLIGATION (RULINGS-4 R4-3); this wave's act discharges it and W9 records the discharge**"* with the constraint quoted whole and the **tokenization** half named, and its → KF.W9 cross-edge declares it a **third split row at both ends** beside `KF-HA-10` and `CubeTarget #8`/`#9`. **Both ends are booked; neither may record `G-KFW9-6` closed on its own.** **What this wave undertakes**: to read the constraint UNMEASURED until those acts land, and then to **RECORD THE DISCHARGE** — the recording is a measurement of their act, never a green of its own. This retires the round-1 **D-3** class at its third and last gate.
- **G-KFW9-7 · The two-direction PRM pair, one pass, substrate named** — RED. Witness: neither direction witnessed (RELEASE: KF-EST-11's frozen-at-0.2 forever; ENGAGEMENT: KF-TD-1's never-observed live flip under a docblock asserting the opposite — **`engine/play-lifecycle/strategies.ts`, the `snapToReducedMotion` docblock `:66-75` above its declaration `:76`**, whose closing sentence is the assertion: *"a live flip on a WAAPI animation cancels the compositor handles before settling"*; the sole call site is **`frame.ts:137` inside `playFrame`**, the rAF lane, and `withReducedMotion` is absent from `src/animation/waapi/delegation.ts` entirely) *(**re-anchored 2026-08-28, repair round 3 — RULINGS-3 R3-7**; the authored `play-lifecycle.ts:337-341` resolves only at the DISQUALIFIED `8281638c`. The module is a directory (carve `53b907c5`) and the docblock's frontier home is `strategies.ts`, which is also where KF.W5's **B-6** cure is anchored — the two waves' anchors now agree, cross-cited at §A's KF-TD-1 row.)*. CLOSES: P-9 + U-1 in ONE pass, substrate NAMED (HEAD = rAF lane, dots stop; worktree/origin-master = WAAPI lane, dots keep pulsing). The pair decides the unification's shape.
- **G-KFW9-8 · The producer's forced-colors rule is measured back into effect — and the demo-side count stays zero** — RED. *(Title and goal re-worded at repair round 1, R-9a item 4 / KF-W9-CHECK D-9: the authored title "zero becomes nonzero" stated an outcome **no §Carry row owns and the standing edict forbids**. The two deletions restore a **producer** rule (`a11y-overrides.css`); they raise the **demo-side** `forced-colors` count by exactly zero. Raising it would require authoring demo-side `forced-colors` rules — forbidden by the standing edict and by KF-ET-4's cure lock. **The recorded result of this gate is that the demo-side count is still 0 and the focus indicator returns anyway.**)* Witness: `grep -rn forced-colors demo/` → 0, seven independent seats, vs 13+ PRM sites; producer rules in FOUR dist files (ruling 7 kills every "sole rule" cell; a11y-fallback DOES reach scene chrome); canvas bitmaps EXEMPT (a limit the cure states). CLOSES: KF-CE-13's demo-wide row lands **as a measurement** with its folds — **the §B enumeration, sixteen ` · ` families** — discharged by reference, zero re-bookings *(**re-pointed at repair round 4, RULINGS-4 R4-6**: the authored "fourteen" was asserted here, at §Carry's route (b) and at S-2 over an enumeration of sixteen, and this cell is a **gate's GREEN** — the gate now points at the enumeration and never re-issues a numeral, per the S-10.1 pattern)*; one WHC capture per fold family; the safari-app column carries OP-4's capability reading, never a silent blank; and the demo-side `forced-colors` grep is **re-run and recorded at 0** with the producer-restoration read taken from the rendered focus indicator, not from a demo-side rule count.
- **G-KFW9-9 · The authored REMOVAL, MEASURED — both copies, before-witness held** — RED. *(Re-cut at repair round 1, R-9a item 2 / KF-W9-CHECK D-3: the authored gate closed on **two deletions** — a product cure the preamble forswears and OP-2 records as unauthorised. **The two-deletion act is KF.W13's** (transport/ribbon packet owns `playback-idiom.css`; the `design-idioms.css` copy rides the SAME single act — **K-5 intact: one deletion fixes nothing, the pair is ONE act, never split**). This gate now closes on the **born-RED witness pair only**.)* Witness: `design-idioms.css:76-79` (`.focus-ring:focus-visible`) AND `playback-idiom.css:72-75` (`.btn-playback:focus-visible`) both unlayered `outline: none` — **different selectors, same (0,2,0) specificity** (the D-19 elision repaired at §B); demo imports later (style.css :3 vs :14); box-shadow forced to none by spec; under WHC no glass Button demo-wide has ANY focus indicator (WCAG 2.4.7/2.4.11). CLOSES: the **BEFORE witness** — focus a ribbon button under forced-colors in the safari-app and real-HCM cells, assert **NO indicator**, per-shot sha256 + cell + substrate ref — plus the recorded **re-derivation obligation** handed to KF.W13 (`.btn-playback:focus-visible`'s "wholly redundant against producer `base.css`" ground is UNTESTED here and must be re-derived **before** the deletion lands). The **AFTER witness** (delete, assert the Highlight outline returns) is the packet's outcome, **re-shot here once KF.W13's act lands** (S-9 discipline); until then it reads UNMEASURED, never inherited.
- **G-KFW9-10 · STRUCK AS A GATE (R-9a item 1) — re-cut as the sequencing DECLARATION at §Sequencing S-15.** *The id is retained and NOT reused (anti-rename holds for this spec's own coordinates), so every inbound citation still resolves to a stated disposition.* Grounds, verbatim from the check (D-4): the authored gate landed `SequenceTarget.vue:97` + `SequenceTarget.css:163-167` — a **behaviour edit in a packet-owned component**, which this wave's own **S-11 DECLINES** and **S-13 fences as a mandatory triumvirate trigger**, before the declared sequencing head (**KF-W4 §Sequencing**, the head-law paragraph — **numeric convenience STRUCK at round-2 close (U-2/R2-7)**: the round-2 spelling `:223` had drifted onto KF-W4's `Unit .e` heading; **the §-heading and the quoted head law are the sole anchor and no line number rides this receipt**; **re-anchored at repair round 2 per R2-7**, the round-1 `:193` coordinate having died in KF-W4's own repair). The edit travels with the **sequence packet to KF.W11**, sequenced after **G-KFW4-1** (R-4). What survives here is **measurement**: the bare `seq-handle` at `.vue:97`, the fork justified by `proof:sequence-rows-draggable` — *a gate that exists in a CSS comment and old prose ONLY* — five keyboard-operable sliders with zero focus indication under HCM, and the relayed second limb (SequenceScrubber: zero `focus` hits in 162 lines). Those witnesses are held under **G-KFW9-9**'s WHC pass and S-8 family (iv). **Thirteen gates remain: `-1..-9` and `-11..-14`.**
- **G-KFW9-11 · The repo's own iOS no-zoom floor honoured where needed** — RED. Witness: `clampIOSNoZoomFontSize` exists, consumed, unit-tested 14→16 — and unused at the two surfaces that need it (R-9's 14px, SP-4's ~12.2px) on a shell that calls `initIOSPlatformClass()`. CLOSES: one real-iOS-Safari session recording focus-zoom fire AND non-restoration on blur at both surfaces, plus KAD-F4's autocapitalize/autocorrect mutation in `contenteditable pre` (may revive R-A's MAJOR). Same session discharges family (v).
- **G-KFW9-12 · Every probe carries a discriminator and a falsifier** — RED. Witness: **four named traps + eleven retired/mooted/struck probes across six records** exist with no register *(figure corrected at repair round 1, KF-W9-CHECK D-10: this cell read "six retired/mooted/struck" against §H's enumeration of eleven and a head claiming eight records — both dependent cells now read the SAME figures, **6 records · 11 probes · 4 traps**, by enumeration at §H)* (exemplar: D-19's "cheap live kill" that would have manufactured a false confirmation). CLOSES: the register published with the surface list; a probe satisfied by both hypotheses is a spec defect; a retired probe re-entering the list is a gate failure (budget loss).
- **G-KFW9-13 · Escalation triggers close with measurements, addenda under original ids** — RED. Witness: **thirteen** banked severities PROVISIONAL over twelve records (§H's enumeration, recounted at the bytes at repair round 2 — R2-15; the round-1 "fourteen" is struck, its source being the kf-TimelineTrack item's two ids under one armed row), three can reach BLOCKER, none measured. CLOSES: each trigger's measured value + resulting severity written back as an ADDENDUM under the ORIGINAL id (E-3 addenda-not-patch / E-1 immutable); unrun triggers reported PROVISIONAL at close.
- **G-KFW9-14 · Substrate naming and witness refresh** — **RED at authoring** (its witness is measured below), and **re-stamped at open** against the then-current frontier. *(Form corrected at repair round 1, KF-W9-CHECK D-13: "RED until stamped at open" contradicted the preamble's "all born-RED at authoring"; the witness itself was always real — `git rev-parse` on both shas executed exact.)* Witness: corpus measured against disqualified `8281638c` (local HEAD) vs `origin/master 81a56990`; the TypingDots inversion flips lane + prediction; four records require **`npm run gh-pages`** re-run first (OP-5) — **R-11**: `build:gh-pages` exists at no coordinate; the real script is `package.json:43` → `"gh-pages": "vite build --mode gh-pages"`, so the command this gate names now EXISTS and the gate is executable as written. CLOSES: every capture names substrate ref + sha + cell; numerals re-derived at capture; frames stamped; one pass, one pinned commit, re-anchored lines; installed-dist-exempt records say so.

## Sequencing

**S-1 · Open condition (BLOCKING)** — KF.W9 cannot open until **KF.W0 §B-12** names the execution substrate (OP-1), OP-3 (safaridriver reachable) + OP-5 (fresh build at the pinned ref) + OP-4 (per-cell capability record) hold, and `.a` commits the chassis (surface list, cell matrix, negative register, trigger register). No phase-2 seat dispatches before `.a` commits.
**S-2 · Forced-colors MEASUREMENT order (this wave) and CURE order (declared, executed elsewhere)** — *re-cut at repair round 1 (R-9a/R-4): the cure order is no longer this wave's to run.* **Measurement order here**: G-KFW9-9's before-witness (both unlayered copies, WHC cell) is the HEAD; ST-1's HCM datum and AT limb are shot in the SAME WHC pass (S-8 family (iv)); the KF-CE-13 sweep's folds — **the §B enumeration, sixteen ` · ` families** *(re-pointed at repair round 4, RULINGS-4 R4-6; the authored "fourteen" is struck at this third and last dependent cell)* — discharge by reference against the **pre-deletion** cascade, and each fold's after-state reads UNMEASURED until the acts land. **Cure order, declared not executed**: the two-deletion act (D-3+DU-M-1 / RB-1, K-5 — ONE act, never split) is **KF.W13's**; **ST-1's re-point is KF.W11's, strictly after it** (S-15) — adopting `.focus-ring` first moves the sliders to *"no indicator via the defeated class"*, a green-looking no-op. This wave re-shoots the after-witnesses once each act lands (S-9 discipline).
**S-3 · PRM unification order** — the AmigaScene anchor's intensity form is precedent for every PRM row; KF-KE-8+KAD-11 land as a PAIR carrying the final-frame rest-state constraint (G-KFW9-6); square family (D-3/L-12/C-6 + D-13) one decision; spring family (D-3 SpringTarget + KF-SS-20) one decision; the `data-allow-motion` producer-inert policy decided ONCE demo-wide (KF-SST-13, third witness); engine half → KF.W5, demo half here; G-KFW9-7's two-direction pass decides the shape before the unification lands.
**S-4 · LP-1 lock (standing)** — KF-CO-1/KF-CO-8 cures SEQUENCED BY LP-1, no write→render edge; witnesses run TOGETHER — one capture, three rows.
**S-5 · Transport lock (standing)** — TD-1/TD-2 cures BUNDLE, one motion, never split; TD-36 measured BEFORE and AFTER the KF-APP-6 cure.
**S-6 · Dock-menu riders (MUST-CARRY)** — the two kf-MbabbMenu riders travel with banked kf-ChromeDock M-4 (whose cure as worded ships an unopenable menu); KF.W9's touch/menu session (M-5/C-6 witness + M-4 rider check + MM-4's computed-style falsifier, same session) supplies the deciding evidence.
**S-7 · Probe ordering** — KF-EST P-1 (home Play control) FIRST, then P-2 (the KF-APP-6 IACVT settler — D-1/D-7/ND-6, the ChromeDock anchor, TD-36, D-25 all hang off it); K-13 runs BEFORE either packet writes its focus cure (cure-home decider); KF-APP-1's crash-witness precedes kf-CubeScene SS-13 #8 (MOOT until the cure lands); D-25 + KF-APP-6 in one 390×844 capture.
**S-8 · Shared-capture families (no probe double-spent)** — (i) the three-rect probe, ONE mount (SS-13 #1; K-29 stands against ruling it statically) for kf-SequenceAxis/Playhead/Scene/Target; (ii) the composited `Card cartoon tier="quiet"` plate ONCE for KF-AV-19/D-8 + kf-KeyframeTimeline D-6/D-7 + kf-TimelineTrack D-5/D-6/D-m3 + kf-TimelineCaret D·M-7; (iii) the 390×844 + 375×667 mobile pass for KF-APP-6/-8, EditorShell D-25/D-6, TD-36, KF-EST-3/4 — **plus the 1280 ARM and the OD-V3/OD-V5 shared-capture family, entered 2026-08-28 at repair round 3 (RULINGS-3 R3-10.4; PASS-3 check D3-3, which convicted this wave for closing a sibling's HARD inbound dependency by silence).** *The family, stated so `KF-W10.md` **OP-4** is checkable against a declared output rather than an omission:* **(iii-a) THE OD-V3 CAPTURE PACKET** — **both transport homes** (the **in-panel transport card**: progress bar + `Play`/`Reverse` + scrubber; and the **bottom-center floating transport pill**: play triangle in a rainbow ring + scene dropdown + reset) on **all four duplicating scenes — `cube` · `amiga` · `square` · `easing`** — at **390 AND 1280**, against **real Glass 7**, both options mocked or annotated. *Enumeration source, read verbatim this seat at the frontier* (`kf docs/tranches/V/audit/R2-01-visual-design.md`, DP2-06): *"On cube, amiga, square, and easing the left column shows an in-panel transport card … at the same time as the bottom-center floating transport pill … Two `Play` controls drive one animation"*, the storyboard scenes (spring, sequence) carrying **only** the pill — which is why the packet is four scenes and not six. The **1280 arm is not optional**: OD-V3's own ruling text (kf `docs/tranches/V/OWNER-DECISIONS.md:7`) names *"390 AND 1280"*, and this family previously carried 390/375 only. **(iii-b) THE OD-V5 390 AT-REST OBSERVATION**, a named member of this family: the at-rest state at 390 is **recorded either way** — that is precisely what lets W10's row close `complete_with_misses` without proxying an owner (OD-V5 is *"DEFERRED PENDING GLASS'S DOCK MARK"*, `OWNER-DECISIONS.md:9`, and the mark is SS-6's boundary, not this wave's). **THIS WAVE PRODUCES THE PACKET AND RULES NOTHING** — OD-V3/OD-V5 are OWNER-GATED blockers at KF.W10's own owner block, *"Never proxied"*; the receipt is the §Bounds `SS-13-CAPTURE-RECEIPT.md` row, and no gate here closes on a ruling. **DISCRIMINATOR**: a capture of one home, or of either home at one viewport only, does NOT satisfy the packet — the ask is a comparison, so a single-home shot proves nothing about the choice; **FALSIFIER**: any scene shot at 390 alone, or against a pre-Glass-7 build, voids that scene's cell; (iv) the WHC pass for RB-1, ST-1, KF-CE-13's folds; (v) ONE iOS device session for R-9, SP-4, KAD-F4, KF-KC-16/25, KF-SCR-1, i-13/C-3, K-13, TimelineCaret probe 10; (vi) ONE AT pass for KF-CB-9, KF-APP-33, KF-KC-2/3/26, MM-2, D-B3, D-1/L-i1, ST-1; plus LP-1+KF-CO-1+KF-CO-8 (one capture, three rows) · the SpringHeatmap Physics-facet light-mode shot (D-B2 + D-M4 + D-M3's branch) · KF-ET-14 + SPF-5 (one plate) · #58 + D·B-1 (one arm) · the ChromeDock touch/menu session (S-6) · KF-APP-16's trace (P-4 + P-8) · the stacking capture (D-8/C-17 + SharePopover probe 5).
**S-9 · Before/after re-shoots (KF.W6 two-way hazard, declared from this end)** — KF.W6 must not land a cure whose only evidence is an unrun KF.W9 probe (KF-HA-7's P-7 explicitly BLOCKS KF.W6's cure set — needs one glass-ui 6.0.0 install, forbidden under lane law, §Excluded); KF.W9 must not close a witness on a surface KF.W6 changed without re-shooting: kf-CubeAxisLines' Z triptych BEFORE AND AFTER the filter gate; kf-SequenceAxis probe 4 re-shot AFTER the ruling-1 cure — the before/after pair is the born-RED material.
**S-10 · Cross-edges (declared from this end)** — **KF.W5**: owns KF-TD-1's live-flip decision + docblock cure, KF-TD-8's densify question, KF-EST-5's P-8 test; KF.W9 supplies U-1 + the `performance.measure`. **KF.W4**: inbound accepted (§3a's `text-transform` honest-UNPROVEN routes here per L-19); G-KFW4-9's six gate rules + the Codex-distilled LAW (B10-27/B18-19/B21-17) bind this harness — immutable map by physical coordinate, reauthenticate at use, read every predecessor authority, derive envelope assertions from the pinned artifact, a gate may not re-derive its own oracle, no grep-only runtime gates. **KF.W7**: owns the timeline cures ("evaluate, not mechanical swap"); KF.W9 owns every capture (48 residue rows, two armed triggers) and must not pre-empt a cure decision with a witness verdict. **The rider on this edge is named by id, not paraphrased (R-10; M-25 — a rider is CARRIED, not cited): `KF-AV-28`, KF.W7's defining standing supersession lock, carried whole at §H with its governed rows in this spec enumerated — R-10's four (KF-AV-19/D-8 · KF-SCR-1 · K-13 · D·D-8's limb) plus the three kf-PlaybackRibbon rows the ribbon's own banked rider reaches (D-3+DU-M-1, with RB-1 governed by identity · D-1/L-i1 · D-12's stopgap), enumeration widened at repair round 2 from the three banked coordinates (KF-W9-CHECK D-8).** **SS-6 relay**: KF-ET-4 (+KF-ET-16 half) · MM-4 · KF-HA-2 · KF-HA-12 · KF-APP-5 producer half · M-5/C-6 producer half · KF-SST-30 · the PRM-inert universal reset — KF.W9's captures ARE the relay's evidence; the letter waits on them. **X-W11 G8**: shared apparatus, SEPARATE cells at distinct coordinates; neither consumes the other's captures ("all five corroborate G8's premise; NONE clears it"). **SS-13**: this wave owns the kf surface ONLY; fourier/value.js residues never fold into its denominator. **KF.W10** — **PRECEDES; W10 DEPENDS** *(added 2026-08-28 at repair round 3 — RULINGS-3 R3-10.4; PASS-3 check D3-3. **Receipt reduced to ANCHOR-ONLY at repair round 4 — LAW C(3).** `KF-W10.md` declares this wave a hard opening precondition, and **THE ANCHOR SET IS ENUMERATED HERE — the numeral stated ONLY here** *(re-cut at repair round 5 — RULINGS-5 R5-8(b); PASS-5 check D5-3; the round-4 spelling **"five"**, asserted at this cell over six named anchors and at §Bounds over a different five, is **STRUCK**)*. **Counting rule, stated so the figure reproduces**: one anchor = **one named site in `KF-W10.md` that declares a dependency on this wave**, counted at the site and never at the sentence — so **§B-1 and §B-3 are TWO** (two rows, two subjects: the V-A95 aurora surface verify; the Glass §4 dock-contract re-verify) and **PACKET-FIRST is ONE** (a law, not a row). By that rule, **six**: **(A1) §State — the *Opens after* row** · **(A2) OP-4** · **(A3) the PACKET-FIRST law** · **(A4) §D — the → KF.W9 cross-edge row** · **(A5) §B-1** · **(A6) §B-3** (the two coordination rows routed to this wave's surface receipt and to both waves' bounds blocks). *The §Bounds `SS-13-CAPTURE-RECEIPT.md` cell points HERE and re-issues no numeral.* **`A1..A6` are ANCHOR ordinals inside ONE cross-edge (KF.W10) — they are not cross-edges and do not enter S-10.1's set, whose numeral is stated only there.**

**VERIFIED, NOT DELEGATED — the six by command (RULINGS-5 R5-8(a); PASS-5 check D5-2).** Round 4 reduced these to anchors under LAW C(3) and delegated their verification twice; the certification never happened (CLOSE-CERT-4 §3 counts five BACKWARD receipts, §7's roll-call has no `KF-W9 → KF-W10` row, §4 R-8 runs the other way). The verification below is `PASS-5/KF-W9-CHECK.md §2`'s, **carried as this round's baseline** and **re-run by this seat at write time**; **the LOAD-BEARING receipt is the ANCHOR** — the offsets are **DATED READINGS**, never stamps, because KF.W10 writes last and **CLOSE-CERT-5 re-verifies under LAW E(5)(ii)**. *The two columns below are the argument: **all six anchors resolve in both, and all six offsets moved between them, inside this round.***

```
# THE SIX, BY ANCHOR — with the offsets shown TWICE to prove which half is load-bearing.
# col A = PASS-5/KF-W9-CHECK.md §2 (2026-08-28), read at the round-5 OPENING baseline for
#         KF-W10.md pinned by RULINGS-5 LAW E(3): 17:38:21 · 209,923 B · sha 086ea228…
# col B DELETED at repair round 6 (PASS-6 D6-1, HIGH · D6-2 folded to union U6-A · D6-3): a per-wave seat may not
# print a sibling's line numbers at all (LAW E(4)), and four of the six numerals this column carried described NO
# state of KF-W10.md at any clock in round 5 (A3 −10 · A4 −10 · A5 −8 · A6 +3 against the file's only two
# reconstructible states). Three of the six pasted commands were multi-line and were printed as single numerals
# (`grep -n 'OP-4'` → 5 lines, `'PACKET-FIRST'` → 4, `'CH2-02'` → 4). col A is KEPT as the check's dated baseline,
# cited to `PASS-5/KF-W9-CHECK.md §2` as an artifact reading and not re-measured here. THE SIX ANCHORS STAND ALONE
# — all six resolve and all six quoted passages are byte-exact at their true lines — and the verifier of record for
# their final-bytes offsets is `PASS-6/CLOSE-CERT-6.md` §ERRATA E-2, which re-derives them. If a numeral must ever
# ride, it is printed as its command's WHOLE output.
#                                                                         col A
(A1) §State "Opens after"   grep -n 'X.KF.W9' KF-W10.md                  → :20
(A2) OP-4                   grep -n 'OP-4' KF-W10.md                     → :66
(A3) PACKET-FIRST law       grep -n 'PACKET-FIRST' KF-W10.md             → :485
(A4) §D cross-edge row      grep -n 'D. → KF.W9 (Safari Visual Audit)'    → :519
(A5) §B-1 (V-A95 aurora)    grep -n 'surface verify' KF-W10.md           → :182
(A6) §B-3 (Glass §4 dock)   grep -n 'CH2-02' KF-W10.md                   → :184
# RESULT: 6 of 6 anchors RESOLVE in both columns. 6 of 6 OFFSETS MOVED between them — inside a
# single round, before this file closed. That is the drift class in one table, and the reason
# LAW E(4) makes the anchor the receipt and the offset a dated reading. CLOSE-CERT-5 re-verifies.
```

**SHADOW (LAW F(1)).** SHADOW: struck set = {the round-4 DELEGATION SENTENCE at §Bounds `SS-13-CAPTURE-RECEIPT.md` ⟨its own cell⟩ · the same sentence at this cell ⟨§Sequencing S-10⟩ · the numeral *"five"* at both ⟨superseded by the six-anchor enumeration above⟩}; minted set = **none** — no row, id, gate or routing is created, moved, re-keyed or dropped by this act, and the six anchors are unchanged in identity; disposition per item: the delegation is **replaced in place** by the verification above (the obligation is discharged, not transferred), the numeral is **re-issued once, here**; citing/terminus waves notified = **KF.W10** (the six anchors' host — anchor-only, no motion asked of it) and the **RECONCILE seat** (CLOSE-CERT-5 re-verifies this class under LAW E(5)(ii)). *No W10 prose is quoted as a receipt of record and no W10 offset is load-bearing: **KF.W10 writes LAST of the per-wave seats this round**, so any quotation taken now would be a receipt against bytes that move after this file closes — precisely the same-round sibling drift LAW C exists to kill, at the seam where R4-7 found it, and LAW E(4) now retires per-seat stamping program-wide. The round-3 quotations are retained in that round's ledger as dated historical readings; this wave's closure conditions on **`PASS-5/CLOSE-CERT-2.md` + RULINGS-5 R5-2**, never on the void round-4 seal.* The substance is unchanged and is what matters: **this spec declared none of it** at round 3, in the very section headed "declared from this end", and under M-25 an inbound hard dependency is a HEAD the receiving spec must carry.)* **What this wave undertakes to produce**: the OD-V3 capture packet and the OD-V5 390 at-rest observation as the S-8 family (iii-a)/(iii-b) shared-capture family, indexed in the §Bounds `SS-13-CAPTURE-RECEIPT.md` row, which also carries the §B-3 dock-contract re-verify (**CH2-02 ×4**) whose sibling half is KF.W6's TransportDock bounds rows. **What this wave does NOT do**: rule OD-V3 or OD-V5, implement a transport home, or proxy an owner — those are W10's owner block, and a transport home implemented under any reading of a held ruling fails W10's G-3 **and** this wave's S-13 (a behaviour edit in a packet-owned component).
**S-10.1 · THE CROSS-EDGE SET, ENUMERATED — and the numeral stated ONLY here** *(repair round 3; PASS-3 check D3-5: **"13 cross-edges declared"** was asserted at two sites and enumerated at none — the one figure of the provenance quartet with no reproducing derivation, while its three siblings all re-derive. Per LAW B and the file's own "three numbers, one enumeration" idiom, the count now lives at its enumeration and the two dependent cells point here.)* **Counting rule, stated so the figure reproduces**: a cross-edge is a **named counterparty wave or program with a declared direction**. By that rule, **thirteen**: **(1) KF.W0** inbound-blocking (OP-1/S-1, §B-12 names the substrate) · **(2) KF.W4** inbound-accepted (§3a `text-transform`) + head-law declared non-binding (OP-6) · **(3) KF.W5** shared subject (engine half; W9 supplies U-1 + the `performance.measure`) · **(4) KF.W6** cure-home for rows measured here + the S-9 two-way before/after hazard · **(5) KF.W7** governs, via the KF-AV-28 rider · **(6) KF.W10** precedes — W10 depends (this row) · **(7) KF.W11** receives ST-1's affordance edit + the nine scene packets · **(8) KF.W12** receives EDITOR-UNIT incl. KF-CE-47's theme-repair half + the six authoring packets · **(9) KF.W13** receives the two-deletion act, D-12's stopgap bytes, dock-menu + transport/ribbon · **(10) SS-6 (BH relay)** — this wave's captures ARE the letter's evidence · **(11) X-W11 §G8** — shared apparatus, SEPARATE cells · **(12) SS-13** — scope bound (kf surface only) · **(13) V·π parser program** — routed out (§Excluded, kf-CubeScene #10). **Granularity disclosed, because the numeral is only meaningful at its rule**: counting §Excluded's three owner-less routings (BUILD-LEVEL ANALYSIS · CI reading · AT-verification lane) as edges yields **sixteen**; counting only the rows physically inside S-10 yields **seven**. KF.W1, KF.W2, KF.W3 and KF.W8 are **not** edges — W3 deliberately so (gated-unscheduled; **this spec schedules nothing against it; the only occurrence of the token is this disclosure** — *scope stated so the claim stays true as the file grows, on the dissent roster's own rule: occurrences in ledger and provenance prose, **including the repair-ledger rows that record this very correction**, are not spec-body references, exactly as that roster holds that "occurrences in ledger and provenance prose, including this sentence, are not roster members"*). *(**SELF-FALSIFYING ZERO RE-WORDED at repair round 4 — RULINGS-4 §END KF-W9 D4-6; PASS-4 check D4-6.** The authored clause read *"this spec holds **0** references to it"* — and `grep -c 'KF\.W3\b' KF-W9.md` returned **1**, the sole occurrence being that clause: round 3 authored S-10.1 and **made the count 1 in the act of asserting it is 0**. PASS-3 had verified zero correctly, at *pre-round-3* bytes. The **posture is sound and is deliberately not disturbed** — W3 is gated-unscheduled, nothing here schedules against it, and naming it as a deliberate non-edge is a stronger disclosure than silence. What was defective was a count that its own sentence falsifies, and the file already knew the remedy: the dissent roster's scope rule says *"occurrences in ledger and provenance prose, **including this sentence**, are not roster members."* The re-worded form **stays true as this file grows** — it makes a **scheduling** claim, which is checkable and which no amount of prose can change, instead of a token count, which the claim itself changes. **The scope rule is carried WITH it and is not decorative**: recording this cure necessarily printed the token again in that round's ledger — *(**FIGURE STRUCK at repair round 5 — RULINGS-5 §END KF-W9 D5-4 / R5-11; PASS-5 check D5-4: round 4 printed **4** occurrences beside its own pasted command, which returns **3**. The count is not re-issued here: it is stated ONCE, with its counting rule and its command, at **§Repair ledger — round 5 §SCOPE**, and every dependent cell points there.)* — so a bare *"the only occurrence"* would have re-falsified itself **in the act of being cured** — the identical recursion, one level up, and the reason the ruling named the dissent-roster pattern rather than a bare re-count. **Outside ledger and provenance prose the count is 1**, which is what the sentence claims.)* *That the enumeration lands on the same numeral the un-enumerated cells asserted is a coincidence at this granularity, not a confirmation of them* — the prior figure had no derivation to agree with.
**S-11 · NO-WAVE-OWNER packets — the CANONICAL 17-PACKET ROSTER; behaviour DECLINED, witnesses HELD, homing declared (R-15)** — *re-pointed at repair round 1: the authored enumeration listed thirteen and was one of three mutually inconsistent counts (W0's then-15, this wave's 14 at S-11, W1's unreceipted "16"). **The number 16 was never true.** **RE-ANCHORED AND RE-STATED 2026-08-28 (repair round 2, R2-7; KF-W9-CHECK D-4):** the round-1 coordinate `KF-W0.md:276` is dead — the roster now lives at **KF-W0 §Sequencing**, the NO-WAVE-OWNER-packets row (**numeric convenience STRUCK at round-2 close, U-2/R2-7** — `:401` had drifted onto a §Carry carries-note; the §-heading + the roster row label are the sole anchor and no line number rides this receipt) — **and the state it cited is superseded**: W0 has itself been re-cut to the canonical **17** ("this file previously enumerated 15; the canonical roster is 17 = W0's 15 ∪ {amiga, EDITOR-UNIT} (KF.W9 S-11's adds)" — quoted verbatim from that row, read at the bytes this session), so the "15" survives only as W0's own dated prior figure and the two specs no longer disagree. The reconciliation is **W0's 15 ∪ {amiga, EDITOR-UNIT}** (this wave's own adds) = **17**; this spec's enumeration gains **spring-physics-facet · spring-artifact-truth · drag-seam**. Homing is **KF.W4's act** (the homing authority); this wave states the roster and holds the witnesses.*
**The roster, whole**: cube · sequence · spring · spring-plot · spring-physics-facet · spring-artifact-truth · square · amiga · drag-seam · CARD-UNIT · OPTIONS-UNIT · KFED-UNIT · EDITOR-UNIT · APPLY-UNIT · AXISLINE-UNIT · dock-menu · transport/ribbon.
**Homing, as KF.W4 cuts it** — **KF.W11 · Demo Scene Repair (9)**: cube (kf-CubeScene spine + kf-CubeTarget + kf-MatrixEditor family) · sequence (**ST-1's affordance edit rides here**, sequenced after G-KFW4-1 — R-4/S-15) · spring · spring-plot (kf-SpringTrace) · spring-physics-facet · spring-artifact-truth · square (comment-invariants = test obligations, the kf-SquareScene law) · amiga (arm (a) first) · drag-seam (`kf-SequenceScrubber:36`; **carries the KF-AV-28 rider** — the scrubber surface is KF.W7-evaluated and the packet sequences after that verdict); sequenced after the KF.W4 gate, on the banked ground *"KF.W6 is glass-consumption and KF.W9 is visual-audit; neither can hold scene behavior"*. **KF.W12 · Authoring-Surface Repair (6)**: CARD-UNIT (ordered by KC-34's highlight-before-blanking edict, after the KF.W4 vue-tsc gate) · OPTIONS-UNIT · KFED-UNIT · EDITOR-UNIT (KF-CE-47's theme-repair cure half lives here — §H) · APPLY-UNIT · AXISLINE-UNIT. **KF.W13 · Chrome, Dock & Transport Repair (2, MINTED)**: dock-menu (kf-MbabbMenu + the M-4 MUST-CARRY riders) · transport/ribbon (**TD-1/TD-2 bundle · G-KFW9-9's two-deletion act · kf-PlaybackRibbon D-12's stopgap bytes**; carries the KF-AV-28 rider).
**None homed here; witnesses stay held here** — every packet keeps its witnesses in this wave's surface list, and this wave spends no cure on any of them.
**S-12 · AT-cell scope (UNRESOLVED — owner ruling requested, = OP-7)** — kf-PlaybackRibbon routes M-2/N-2 to an AT-verification lane; ≥8 records route AT utterances INTO KF.W9. PROPOSED: AT cells are SEPARATE cells INSIDE this wave under I-20's law (VoiceOver is a Safari-platform capability, inside the horizon's named condition), NVDA/JAWS their own cells. Recorded as a dissent, not decided.
**S-13 · Triumvirate triggers (mandatory; the orchestrator may not redispatch the failing seat alone)** — *Bounds expansions that invalidate the wave*: any write to `safari-real-matrix.js` (EXECUTE-NO-WRITE; a cell label inexpressible in `capture.mjs`/`states.mjs` is a harness redesign shared with X-W11 G8) · any glass-ui producer byte · any behaviour edit in a packet/UNIT-owned component · any write under `docs/tranches/T/**` · any adjudicated-record edit that is not an addendum under its original id. *Gate failures not local-recoverable*: G-KFW9-1 RED because safaridriver cannot be enabled (the named condition is unreachable → the horizon is re-planned, not faked with webkit) · G-KFW9-8's safari column UNREACHABLE by UA capability AND no Windows HCM cell (book UNREACHABLE-IN-CELL with the bound stated — **a chromium emulation labelled as WHC is the I-20 failure**) · G-KFW9-4 blocked by a material divergence from **565+25** (the denominator is wrong, not the probes — the cell re-derived at repair round 2; a ±2 drift of the round-1 kind is a precision correction, not a material divergence, and does not arm this trigger) — **and the divergence is measured with the AMENDED command only** (`##` **or `###`** headings matching UNPROVEN/SS-13; §Surface-list protocol 2, amended at repair round 3): *the unamended `##`-only form returns **563** at the current bytes and would arm this trigger on a 2-count divergence the command itself manufactures. A seat that measures 563 has run the retired command, not found a corpus change.* *Diagnostic loops*: a third pass at a probe whose observation does not discriminate HALTS — it is a spec defect; re-author, do not re-run.
**S-14 · The N-1 time-domain lock (CARRIED, not cited — M-25; R-1e #31)** — banked at `kf-SequenceScene.md:153`, routed to this wave with its ordering constraint intact: **the D17/D18-unit fold is SEQUENCED after banked N-1.** *A sequencing lock routed to a wave must live in that wave's §Sequencing* — at authoring it was received as a §Carry mention with no §Sequencing entry, which is exactly how a rider dies. The lock, whole: the **valuetext-in-ms cure stays BLOCKED on N-1's time-domain decision — "no honest millisecond exists until the scene picks a clock"** (Metric %, slider `valuenow` %, timecode a unitless 0-1 scalar = a third normalized exposure; the axis that NAMES ms is itself `aria-hidden`). **Governed here**: kf-SequenceScene's D17 + D18(unit limb) ≡ banked N-14 + kf-SequenceAxis D-8a/L-9/C-5, and the **valuetext limb** of kf-SequenceAxis D-12's relay (§F). **NOT governed** — stated so the lock is not over-applied: D-12's **uppercase-render limb** (`@800ms` → `@800MS`) is free of it, and kf-SpringTarget **N-4** is explicitly free of it (no domain decision blocks announcing a commanded target honestly). D18's `aria-hidden` limb is KILLED BY THE BANK (≡ kf-SequenceAxis D-12, adjudicated non-defect) and is not carried. Witness capture is lawful now; only the CURE waits — and no cure here is this wave's to spend.
**S-15 · ST-1's affordance edit — a DECLARATION, not a gate (R-4 / R-9a item 1; ex-G-KFW9-10)** — **the cure bytes (`SequenceTarget.vue:97` one word + `SequenceTarget.css:163-167` deletion) belong to the sequence packet at KF.W11, sequenced after G-KFW4-1**; this wave holds the before/after witnesses and spends nothing. The ordering the struck gate encoded survives as a binding declaration handed to KF.W11: **ST-1's re-point must not precede KF.W13's two-deletion act**, or the five sliders move to *"no indicator via the defeated class"* — a green-looking no-op that ships green and changes nothing. Stated from this end so the receiving wave inherits the order with its reason, not the reason's absence.

## Excluded (§EXCLUDED — routed elsewhere with owners; M-25: nothing lost, head, tail, or interval; none closed by this wave, none droppable at close)

- → **BUILD-LEVEL ANALYSIS**: LP-19's dev-graph magnitude (C-19a precedent) · ME-25's bundle-graph cost · KF-AV-8's bundle-weight half (fresh `KF_ANALYZE=1 vite build --mode gh-pages`) · C-15's barrel-vs-granular shipped-bytes delta · kf-App #15 (HEAD/frontier rebuild closes the KF-APP-14/-15 stale-artifact caveat) · kf-TimelineTrack C-7.
- → **KF.W5**: KF-EST-5's P-8 group-rewrite viability TEST ("not a browser probe") · KF-TD-8's densify-cost design question.
- → **KF.W7**: kf-CSSPasteDialog #9 (the KeyframesAddDialog-onto-this-shell build experiment, R-7 fold).
- → **V·π parser program**: kf-CubeScene #10 (`parseCssValue`/`parseCssScalar` colour-grammar dispatch — "Cross-repo, NOT SS-13"; census annotation).
- → **SS-6 relay**: KF-SKEL-4's M1 blast radius ("belongs to the BH/BI relay, not this component's file").
- → **CI reading, not a probe**: kf-CopyButton #8 (`gh run list --workflow=ci.yml` at the FRONTIER) · kf-KeyboardShortcutsModal #7.
- → **BLOCKED, resolve before KF.W6 executes**: KF-HA-7's P-7 version probe vs glass-ui 6.0.0 — needs one install, forbidden under lane law; it changes the CURE (silent cross-major regression vs authoring error).
- → **AT-verification lane**: kf-PlaybackRibbon M-2/N-2's actual announcement (per that record's routing; contested under S-12's owner-ruling request — flagged so it is not lost).
- **Not this wave's surface**: fourier and value.js SS-13 residues (same law that rejects the Kronecker denominator) · all NO-WAVE-OWNER packet BEHAVIOURS (S-11) · every negative-register probe (re-spending is a budget loss; G-KFW9-12 enforces).

## Fold ledger (M-23/M-25 — the agglomeration record)

**Structure taken from Draft B**: the State verb table, Goal criterion, Open-preconditions table (OP-1..OP-7 — OP-4's per-cell `.media` capability tests and OP-2's write-authority boundary are B-only and load-bearing), the ten-cell roster, the Surface-list protocol, seat disjointness, and the triumvirate law (S-13). **Carry mass and closing sections taken from Draft A**: the eight-family §Carry (**at the fold, 68 rows on disk against a claimed 69 — the fold's own miscount, corrected by recount at repair round 1 and now 76**), §Excluded, the packet-homing declaration (S-11), and the dissent registry. **Killed from Draft B**: its §Carry P0–P6 organization (duplicated A's families at greater length — union-bloat), its truncated §Sequencing tail (cut mid-sentence at "three failed attem…"; the cross-edges it lost are carried via A's S-10..S-12), and its restatement of full gate witnesses inside carry rows (booked once, at the gate). **Killed from Draft A**: its single-row open condition (S-1 alone under-specified wave-open — B's OP table replaces it) and its bounds table's omission of the `evidence/W9/**` write path and seat disjointness. **Grafted row-level**: B's Kronecker factorization into the B18-26 row; B's four tracked filenames + `shots/` 11/0 datum into G-KFW9-1; B's census verbatim quote into G-KFW9-5; B's "no gate runnable before -1/-14/-3" ordering into the gates preamble. **Blind convergences banked**: both arms independently produced the same 14 gates (**13 after R-9a struck G-KFW9-10**), the same residue count (**as authored, ≈588 = 563+25; re-derived at repair round 2 to ≈590 = 565+25 — the convergence was on the method, and the method's own output moved when it was re-run**), the same two-deletion/ST-1 ordering (**now carried as declarations S-2/S-15, the acts homed at KF.W13/KF.W11**), the same intensity-form anchor, and the same AT-scope dissent — the strongest confirmation twice-authoring can give. *Convergence caveat entered at repair round 1: both arms also converged on the two boundary faults the check convicted — a cure-bearing gate set inside a measurement wave, and an AT arm scoped at ≥8 records but carried at 4. **Twice-authoring confirms agreement, not correctness**; the fold ledger records this so the convergence is never quoted as a warrant.*

## Repair ledger — round 1 (X·KF L-20, PASS-1)

Sole authorities: `conformance/PASS-1/RULINGS.md` (cross-wave, wins on collision) and `conformance/PASS-1/KF-W9-CHECK.md` (local). Sole writable file: this spec. **Rulings applied VERBATIM: 5 of 5** — R-1e · R-9a · R-10 · R-11 · R-15. **Check defects: 19 booked · 17 cured · 1 declined · 1 no-defect = 19.** *(**ARITHMETIC CORRECTED 2026-08-28, repair round 2 — R2-15; KF-W9-CHECK D-6**: the round-1 line read "18 cured · 1 declined", which counts D-18 — recorded **NO DEFECT**, not cured — as a cure. Recounted at the bytes in the table below: `**CURED**` ×17 · DECLINED ×1 (D-14) · NO DEFECT ×1 (D-18). The round whose signature act was curing an off-by-one census claim reproduced the fault in its own ledger one section later; the fix is the same discipline — count, do not assert.)*

| defect | severity | disposition | where |
|---|---|---|---|
| D-1 phantom `build:gh-pages` | HIGH | **CURED** — `npm run gh-pages` at all three sites (R-11) | OP-5 · §H witness-substrate law · G-KFW9-14 |
| D-2 census off by one (69 vs 68) | HIGH | **CURED** — recounted at the bytes, stated by count: 18·5·1·8·7·11·18·8 = **76** *(the round-1 figure; recounted again at round 2 to **79** after R2-10's three bookings — §Carry head)* | §Carry head |
| D-3 preamble forswears cures two gates close on | HIGH | **CURED** — G-KFW9-9 closes on the witness pair only; G-KFW9-10 struck (R-9a) | §Gates |
| D-4 G-KFW9-10 edits a component S-11 declines / S-13 fences | HIGH | **CURED** — struck as a gate, re-cut as declaration S-15; edit → KF.W11 (R-4) | §Gates · S-15 |
| D-5 OP-2 forbids the writes §Bounds grants | HIGH | **CURED** — all five EXECUTION-TIME-ONLY kf grants struck; zero kf write grants remain | §Bounds · OP-2 |
| D-6 AT scope stated, not carried | MEDIUM | **CURED** — §F carries seven added rows (R-1e). *The round-1 wording "the arm stands at ten records" is **superseded at round 2** (check D-12): §F's true composition is **13 bullets over 8 attributed records**, arm span 11 — S-12's bar clears on the bullet count alone* | §F |
| D-7 / E-10 KF-AV-28 never named | MEDIUM | **CURED** — rider carried verbatim + four governed rows by id; S-10's KF.W7 edge names it (R-10) | §H · S-10 |
| D-8 dangling `kf-ControlsPaneWrapper D-3+M-1` | MEDIUM | **CURED** — re-pointed at the banked **D-m9**; no row renamed | §B RB-1 |
| D-9 G-KFW9-8 states a forbidden outcome | MEDIUM | **CURED** — title + goal re-worded; the recorded result is that the demo-side count stays 0 | §Goal · G-KFW9-8 |
| D-10 negative-register arithmetic (three numbers) | MEDIUM | **CURED** — **6 records · 11 probes · 4 traps** in both dependent cells | §H · G-KFW9-12 |
| D-11 PlaybackRibbon D-12 stopgap uncarried | MEDIUM | **CURED** — booked as MEASUREMENT; cure bytes → KF.W13 (R-1e #30) | §F |
| D-12 SequenceScene D17/D18 lock uncarried | MEDIUM | **CURED** — lock entered at **S-14**, verbatim (R-1e #31) | §F · S-14 |
| D-13 G-KFW9-14 not born-RED by its own words | LOW | **CURED** — form restated: RED at authoring, re-stamped at open | G-KFW9-14 |
| D-14 KF-EST-4 has no counterparty in KF.W6's CARRY | LOW | **DECLINED — cure is not this wave's** (see below) | S-9 |
| D-15 KF-KC-16 mis-described (`offset-field`) | LOW | **CURED** — restated as the **`<pre>`** CSS text region | §D |
| D-16 "third witness" is the fourth | LOW | **CURED** — the three enumerated; kf-CubeScene D-16 deliberately excluded | §A KF-SST-13 |
| D-17 `≡ kf-CubeScene D-16` cites a KILL as witness | LOW | **CURED** — sign correction; carried as mechanism corroborant with valence stated | §A |
| D-18 KF-ET-12 correctly omitted | LOW | **NO DEFECT — recorded** so pass 2 does not book it as an escape: its disposition is `MAJOR → NO-WAVE-OWNER (clamp)`, not a KF.W9 routing, and this spec must not adopt it | — |
| D-19 "authored TWICE" selector elision | LOW | **CURED** — two selectors named (`.focus-ring` / `.btn-playback`); K-5 survives; the untested redundancy ground is carried to KF.W13 as a re-derivation obligation | §B · G-KFW9-9 |

**The one decline, stated so it survives a hostile re-read.** *D-14 — S-9's before/after hazard names KF.W6 as the cure home for **KF-EST-4**, and `KF-W6-CARRY.md` has 0 hits for that id.* The finding is TRUE and is not disputed; what is declined is a **W9-side edit**. The cross-wave seat ruled it at **R-19e**: the missing counterparty is cured **at the receiving end** — KF-W6 adds the CARRY row as an E-3 addendum — and the ruling states in terms that *"W9's D-14 needs no W9-side edit."* Rulings win on collision (this seat's standing law). Two further grounds hold independently of that: (i) `KF-W6-CARRY.md` is **not this seat's writable file** — the only writable file is this spec, so the sole cure available here would be to re-home KF-EST-4 away from KF.W6, which would **contradict the bank's own routing** and manufacture a second defect to hide the first; (ii) the hazard as this wave declares it is sound in shape and is declared **from this end** — the declaration is not falsified by the counterparty's absence, it is what makes the absence visible. Re-pointing or silently dropping the declaration would delete the only in-tree evidence of the gap. **Nothing is lost: the row is named here, the cure is named at R-19e, and the seam closes at KF-W6.**

**Escapes E-1..E-10, id for id**: E-1 D-5 · E-2 D-6 · E-3 D-14 · E-4 N-4 (E-3+E-4 carried as **ONE binding**, the *"one binding, with N-4"* lock restored verbatim — M-25) · E-5 SequenceAxis D-12 + the kf-SequenceTarget relay (RECEIVED, not noted) · E-6 SpringHeatmap D-m8 · E-7 PlaybackRibbon D-12 · E-8 SequenceScene D17/D18-unit + its lock at S-14 · E-9 KF-CE-47 (named line-item in §H's SS-13 aggregate) · E-10 KF-AV-28 (standing rider, §H). **10 of 10 booked. Net row delta +8** (§F 4→11, §H 7→8); KF-CE-47 books as a line-item, not a bullet, per R-1e.

**Unchanged by design (as stated at round 1; two of these cells MOVED at round 2 and the claim is corrected here rather than left standing — see the round-2 ledger below)**: ~~the ≈588 denominator (563 + 25)~~ → **re-derived at round 2 to ≈590 (565 + 25)**, the round-1 "independently confirmed to the unit" having been confirmed against a figure both seats then recomputed · every gate witness the check re-executed true (**still true; all thirteen re-verified at round 2's frontier**) · ~~the nine dissents~~ → **the dissent roster was never nine at the bytes; re-enumerated at round 2 to twelve** (see the provenance line) · every cure-shape lock · the anti-rename guards, including this spec's own gate ids.

## Repair ledger — round 2 (X·KF L-20, PASS-2)

Sole authorities: `conformance/PASS-2/RULINGS-2.md` (cross-wave, wins on collision) and `conformance/PASS-2/KF-W9-CHECK.md` (local). Sole writable file: this spec. **Rulings applied VERBATIM: 2 of 2** — **R2-10** · **R2-15** — under the three laws binding every seat this round: **R2-5** (measure at the tree, never at the rulings), **R2-7** (stable anchors; the Re-anchor seat writes last), and round 1's standing rule that a ruling outranks any local finding it meets.

**Witnesses re-derived this session (R2-5).** keyframes.js at **`origin/master 81a56990`**: `git rev-parse` exact; `git grep -n "focus-visible" -- demo/styles/design-idioms.css demo/styles/playback-idiom.css` → `design-idioms.css:76` `.focus-ring:focus-visible` (`:78` its `outline: none`) · `playback-idiom.css:72` `.btn-playback:focus-visible` — the D-10 pinpoint. value.js at current bytes: the three escaping rows at their banks (`kf-AmigaScene.md:42`/`:54`/`:158` + its 12-item residue `:130-141`; `kf-HeroAurora.md:52` + its P-1..P-8 residue `:103-114`); the KF-AV-28 rider at all three banked coordinates (`kf-AnimationVisualizer.md:35` · `kf-PlaybackRibbon.md:36` · `kf-SequenceScrubber.md:36`) and that record's KF.W9 routing line (`kf-PlaybackRibbon.md:155`); `kf-RibbonBar.md:57`/`:65`/`:133` (D-15's routing, RB-1's `design-idioms.css:76-79` bank, the KF.W9 summary); the residue recount over all 58 records (**565**, 57 records carrying enumerated items) and the three prose-carried queues (`kf-CubeAxisLines.md:104` nine · `kf-TransportDock.md:122` eight · `kf-CubeTarget.md:83` eight = **25**); this spec's own §Carry bullets (**79**, scripted per section), `**CURED**` tokens (**17**), `Dissent preserved` instances (**11**), and §H's escalation enumeration (**13** over twelve records); the sibling booking of KF-HA-10's owner-ruling half at **KF-W6 §Carry** and `KF-W6-CARRY.md`; the re-anchor targets **KF-W4 §Sequencing** (head-law) and **KF-W0 §Sequencing** (canonical-17 roster, itself now at 17). **Nothing below is inherited from a ruling's prose, a check's register, or this spec's prior text.**

| directive / defect | severity | disposition | where |
|---|---|---|---|
| **R2-10** items 1–2 — kf-AmigaScene **D-2** + **MISSED-A** into §F | ruling | **APPLIED VERBATIM** — both quoted from the bank, witness-held, cure NO-WAVE-OWNER (scene-repair packet); route (d) shown unavailable at the record's own residue | §F (two new bullets) |
| **R2-10** item 3 — kf-HeroAurora **KF-HA-10** into §G/§H | ruling | **APPLIED VERBATIM** — booked at §G beside the HeroAurora family; measurement W9's, owner ruling KF.W6's, cross-edge named at the row. **Conditional sibling correction NOT triggered**: the W6 half is already booked (KF-W6 §Carry + `KF-W6-CARRY.md`), verified this session | §G |
| **R2-10** item 4 — counts **re-enumerated, not incremented** | ruling | **APPLIED** — §Carry recounted by script to **79** (A 18 · B 5 · C 1 · D 8 · E 7 · F 13 · G 19 · H 8); §F restated at 13 bullets / 8 records / 11-record arm span; S-12's "≥8" cleared by bullet count alone | §Carry head · §F preamble |
| **R2-15** — the false headline closure claim | HIGH (check D-1) | **APPLIED VERBATIM** — struck and restated in the ruling's dated-correction block, with this seat's own re-derivation receipt beside it; the PASS-1 mis-booking of KF-HA-10 recorded as an E-3 addendum, **the PASS-1 file untouched** | §Carry head |
| check **D-2** — the AT sweep run as a list, not a class | HIGH | **CURED** — the class re-swept over all 58 records (per-record KF.W9 routing lines differenced against this spec and each record's residue); the two AmigaScene rows were its remaining members; **no fourth found**, and the sweep's method is stated so pass 3 can re-run it | §F preamble · §Carry head |
| check **D-3** — `KF-W4.md:193` dead, cited twice, load-bearing | MEDIUM | **CURED** — both sites re-anchored to **KF-W4 §Sequencing**'s head-law paragraph (R2-7 idiom; line number parenthetical and declared non-load-bearing) | OP-6 · G-KFW9-10 |
| check **D-4** — `KF-W0.md:276` dead **and** its state superseded | MEDIUM | **CURED** — re-anchored to **KF-W0 §Sequencing**'s packet-roster row and the superseded "15" restated as W0's own dated prior figure (W0 now reads the canonical 17) | S-11 |
| check **D-5** — escalation register: three numbers, one register over | MEDIUM | **CURED** — **thirteen** at the register head and at both dependents (§Goal, G-KFW9-13), with the fourteenth's composition named (kf-TimelineTrack's two ids under one armed row) | §H · §Goal · G-KFW9-13 |
| check **D-6** — the repair ledger miscounts its own dispositions | MEDIUM | **CURED** — **17 cured · 1 declined · 1 no-defect = 19**, recounted at the bytes | round-1 ledger head |
| check **D-7** — the dissent roster stale after the repair that widened it | MEDIUM | **CURED** — re-enumerated by count: **11 row-level `Dissent preserved` + 1 wave-level scope dissent = 12**; the three R-1e-era additions (SpringTarget D-5, SpringTarget D-6, PlaybackRibbon D-12) are named | provenance line · round-1 "unchanged" block |
| check **D-8** — KF-AV-28's governed-row enumeration under-inclusive | MEDIUM | **CURED** — re-derived from the three banked rider coordinates: R-10's four **+ three ribbon rows** (D-3+DU-M-1, with RB-1 governed by identity · D-1/L-i1 · D-12's stopgap); D-24 named as a datum row that is not governed until a cure is spent | §H rider · S-10 |
| check **D-9** — kf-RibbonBar `D-15` booked by identity, never by id | LOW | **CURED** — the banked id carried at §B with the anti-rename trail and the explicit distinction from kf-SequencePlayhead's D-15 (RTL, §C) | §B RB-1 |
| check **D-10** — `.focus-ring:focus-visible` pinned one selector-line late | LOW | **CURED** — `:76` (selector) with `:78` named as its declaration; `:76-79` range stands; re-derived at the frontier | §B D-3+DU-M-1 |
| check **D-11** — the 563 cell 2 short of an independent recount | LOW | **CURED** — re-derived at **565** by this seat's own script (agreeing with the check's independent recount); the dependent denominator moved **≈588 → ≈590** at all seven sites, and S-13's material-divergence trigger is stated not to arm on a precision correction | §Protocol 2 · §Scope 4 · §Bounds · §H · G-KFW9-3/-4 · S-13 · fold ledger |
| check **D-12** — §F's "ten records" counts a §H line-item and a site-reference | LOW | **CURED** — restated at its true composition (13 bullets · 8 attributed records · 11-record arm span), with the three elsewhere-booked riders named | §F preamble |

**No defect declined this round.** Round 1's single decline (D-14, KF-EST-4's missing KF.W6 counterparty) is untouched and stands on R-19e; nothing in PASS-2 disturbs it.

**Posture unchanged by this round, stated so the additions cannot be misread**: the wave still **measures** — R-9a holds, no cure arm returns with the three new rows (D-2 and MISSED-A are witness bookings whose cures are the amiga scene-repair packet's; KF-HA-10's ruling half is KF.W6's), **zero write grants in the kf tree**, status `planned`, gate ids un-renumbered, thirteen live gates, every cure-shape lock and every dissent carried.

## Repair ledger — round 3 (X·KF L-20, PASS-3)

Sole authorities: `conformance/PASS-3/RULINGS-3.md` (cross-wave, wins on collision) and `conformance/PASS-3/KF-W9-CHECK.md` (local). Sole writable file: this spec. **Rulings applied VERBATIM: 3 of 3** — **R3-7** · **R3-9** · **R3-10.4** — under the two program laws this round adds (**LAW A** import-graph census · **LAW B** closure-claim ban) and the standing laws of rounds 1–2 (**R2-5** measure at the tree · **R2-7** stable anchors · rulings outrank local findings on collision). **Check defects: 8 booked · 8 cured · 0 declined.** Nothing here opens product source; keyframes.js was read **read-only** at `origin/master 81a56990` throughout; status stays `planned`.

### §LAW-A · Import-graph censuses (RULINGS-3 LAW A), executed at `origin/master 81a56990` this seat, commands + outputs

**Scope statement first, because LAW A's trigger is broader than authorship.** LAW A binds *"every seat that **writes, carries, or leaves standing** an act that deletes, repoints, or shims a module `M` at path `P`."* This wave **authors none** — after R-9a it holds **zero write grants in the kf tree** and its own §Bounds acts are `create`/`modify-carve` inside value.js. It **carries three such acts as declarations for other waves**, and those are censused here so the receiving seats inherit a census rather than a filename. Where the subject is a CSS rule/class rather than an ES module, the census is run in LAW A's own shape — specifier-analogue (every spelling of the subject) ∪ symbol-analogue (every consumer of the subject's name), each hit resolved to its own site, prose/docs hits recorded as non-import context and never counted as consumers.

**Census 1 — the `./play-lifecycle` module (R3-7's subject; a completed CARVE, not this wave's act).** Command + output:
```
git ls-tree -r --name-only origin/master -- src/animation/engine
  → …/engine/play-lifecycle/{events,frame,index,strategies,transport}.ts   (a DIRECTORY; no flat play-lifecycle.ts)
git cat-file -e origin/master:src/animation/engine/play-lifecycle.ts       → FAILS
git grep -nF 'play-lifecycle' origin/master -- src/ demo/ test/ scripts/
  → 2 IMPORT consumers, both static, both intra-engine:
      src/animation/engine/animation.ts:37       import * as playback from "./play-lifecycle";
      src/animation/engine/option-setters.ts:27  import { shouldReverse } from "./play-lifecycle";
  → NON-IMPORT CONTEXT, not consumers — SCOPE WIDENED at repair round 4 (R4-4), because the
    authored "inside the module's own five files" is refuted by this same command's output:
    (a) docblock/comment prose INSIDE the module's own five files (events/frame/index/
        strategies/transport .ts), AND
    (b) FOUR DOCBLOCK LINES over THREE SIBLING MODULES *OUTSIDE* THE MODULE that name the
        flat path or the live specifier — three as the CARVE'S ORIGIN, one as the LIVE
        SPECIFIER; NONE is stale prose. ⟨RE-CHARACTERISED at repair round 5 — RULINGS-5 §END
        KF-W9 D5-5; PASS-5 check D5-5. Round 4 wrote "each naming the pre-carve
        `engine/play-lifecycle.ts` AS IF IT STILL EXISTED … exactly the stale prose a carve
        leaves behind", which THE FOUR LINES PRINTED DIRECTLY BELOW REFUTE. Re-read whole at
        `origin/master 81a56990` this seat (`git show …:<file> | sed -n '1,8p'`).⟩ The lines: 
          src/animation/engine/playback-state.ts:3  · "`engine/play-lifecycle.ts` at S.B2 on the
                                                       STATE↔BEHAVIOR cohesion seam (the"
          src/animation/engine/playback-state.ts:4  · "play machine's free functions live in
                                                       `./play-lifecycle`; the state they mutate lives"
          src/animation/group/lifecycle.ts:5       · "carve, mirroring the engine's
                                                       `engine/play-lifecycle.ts`). The class keeps thin"
          src/animation/orchestration/sequence/lifecycle.ts:6 · "engine's `engine/play-lifecycle.ts`).
                                                       The class keeps thin `this`-delegates so"
    (c) scripts/gates/structure/index.mjs:259 (a comment).
git grep -n '\bsnapToReducedMotion\b' origin/master -- src/ demo/ test/ scripts/
  → declaration engine/play-lifecycle/strategies.ts:76 · SOLE call site engine/play-lifecycle/frame.ts:137
    (inside playFrame, decl :121) · barrel index.ts names it only to EXCLUDE it.
git log --oneline -1 53b907c5
  → 53b907c5 refactor(lib/engine): carve engine/play-lifecycle — the 24 free functions module
```
**Finding**: the specifier is unchanged and resolves to the dir barrel, so **no consumer repoint is owed by the carve** — the coordinates moved, the import graph did not. **This is why the defect was a stale ANCHOR and not a broken build**, and why a line-count check would never have caught it. `snapToReducedMotion` is barrel-EXCLUDED, so **KF.W5's docblock cure must land in `strategies.ts`**, not in `index.ts` — the one consequence this census puts beyond argument.

**Finding on the non-import scope, CORRECTED at repair round 5 (RULINGS-5 §END KF-W9 D5-5; PASS-5 check D5-5) — the sentence a receiving seat would act on.** Round 4 advertised the four lines at (b) as *"stale prose a carve leaves behind and the kind a census exists to surface"*; **that characterisation is STRUCK, because this block's own pasted transcript is its refutation** — the shape D4-1 was convicted for (a scope statement its own output contradicts), surviving inside D4-1's own cure, and it entered by executing RULINGS-4 R4-4 verbatim without re-reading the four lines the ruling named. Re-read whole at the frontier this seat:

```
git show origin/master:src/animation/engine/playback-state.ts        | sed -n '1,6p'
  :2-3  "the `PlaybackState` run-state STORE, carved off / `engine/play-lifecycle.ts` at S.B2 …"
  :4    "play machine's free functions live in `./play-lifecycle`; the state they mutate lives / HERE"
git show origin/master:src/animation/group/lifecycle.ts              | sed -n '1,8p'
  :4-5  "lifted off the `AnimationGroup` class as FREE FUNCTIONS (S.B5 / a19 F1 — the group ceiling
         carve, mirroring the engine's `engine/play-lifecycle.ts`)"
git show origin/master:src/animation/orchestration/sequence/lifecycle.ts | sed -n '1,8p'
  :5-6  "(S.B5 — the sequence ceiling carve, mirroring `group/lifecycle.ts` + the
         engine's `engine/play-lifecycle.ts`)"
```

**THREE of the four lines** — `playback-state.ts:3` · `group/lifecycle.ts:5` · `sequence/lifecycle.ts:6` — name the flat path as the **OBJECT OF A CARVE** (*"carved off X"*, *"mirroring X"*): **correct historical provenance statements as written**, not assertions that `X` exists; a repair that "cured" them would delete true provenance. **The fourth** — `playback-state.ts:4` — names **`./play-lifecycle`**, the **LIVE SPECIFIER** that resolves to the directory barrel, which is the very fact this census's Finding turns on; it is not a pre-carve path at all. **No stale reference to the pre-carve module exists outside the module: the carve left NO prose debt, and this census advertises no cleanup.** *(Counting rule, stated so it reproduces: **four lines over three sibling modules** — `playback-state.ts` contributes two. (a) and (c) are unaffected and unchanged. The census's numeric arm — 2 import consumers, the widened non-import scope, both falsifier exits — was re-run at PASS-5 §3 and reproduces cell-for-cell; **only the characterisation was false**, and the id/consequence set is untouched.)*

**Census 2 — the two-deletion act (`.focus-ring:focus-visible` + `.btn-playback:focus-visible`), carried as a DECLARATION; cure bytes are KF.W13's.** Command + output:
```
git grep -n 'focus-ring' origin/master -- demo/ test/ src/ scripts/     → 11 hits / 7 files
  DEFINITION      demo/styles/design-idioms.css:74 (the doc comment) · :76 the rule · :77 its box-shadow
  CONSUMERS (4 class applications / 4 files — CORRECTED from the authored "5" at repair round 4,
             R4-4: the header asserted five over a list of four, `git grep -c` re-run this seat):
      demo/components/instrument/keyframes/KeyframeCard.vue:45
      demo/scenes/spring/SpringHeatmap.vue:30   (+ :26, the comment naming it "the demo-wide" idiom)
      demo/scenes/spring/SpringTarget.vue:63
      demo/scenes/square/SquareScene.vue:46     (+ :43, the comment citing P.W6 S1(b))
  TOKEN-ONLY    demo/scenes/sequence/SequenceTarget.css:165 · demo/styles/playback-idiom.css:73
                — these consume the CUSTOM PROPERTY var(--focus-ring-shadow), NOT the class.
git grep -n  'btn-playback' origin/master -- demo/ test/ src/ scripts/ | wc -l   → 24 hits
git grep -ln 'btn-playback' origin/master -- demo/ test/ src/ scripts/ | wc -l   → 7 FILES
  (CORRECTED from the authored "8 files" at repair round 4, R4-4 — both commands re-run this seat;
   the authored count disagreed with its own list because the list omitted the SEVENTH file)
  THE SEVEN, enumerated so count and list can never again disagree:
      demo/DESIGN.md · demo/components/playback/PlaybackRibbon.vue · demo/scenes/easing/EasingScene.vue
      · demo/scenes/spring/SpringScene.vue · demo/scenes/spring/StartingStyleTarget.vue
      · demo/styles/font-roles.json · demo/styles/playback-idiom.css
  DEFINITION   demo/styles/playback-idiom.css (:16 base · :72 the :focus-visible rule under deletion)
  CONSUMERS    demo/components/playback/PlaybackRibbon.vue:30,:55 · demo/scenes/spring/SpringScene.vue:144
               · demo/scenes/spring/StartingStyleTarget.vue:42 · demo/styles/font-roles.json:12 (a SELECTOR
               STRING in a build/registry artifact — a consumer of the NAME, and the one hit a template-only
               sweep misses) · demo/DESIGN.md:117-118 non-import context.
  NON-IMPORT CONTEXT, entered at repair round 4 (R4-4) — the omitted seventh file:
      demo/scenes/easing/EasingScene.vue:61 · "// Play/Reverse on the shared `.btn-playback`
        `grid-cols-2` skin (equal w/h by" — a COMMENT site, so not a consumer, but it IS the
        seventh file and its absence is exactly why the count and the list disagreed. Recorded
        so a KF.W13 seat inheriting this radius does not have to re-derive it.
      (also non-import, already listed above: SpringScene.vue:71 and the four PlaybackRibbon
       comment lines :40/:47/:52/:86 — comments inside files already counted as consumers.)
```
**Finding, handed to the R4-8 successor-formation register's KF.W13 row** *(hand-off RE-ISSUED at repair round 4 — RULINGS-4 R4-4: the corrected censuses are addressed to **KF-W10 §6.D · SUCCESSOR-FORMATION REGISTER, the KF.W13 row** — anchor-only, LAW C(3), that spec writing last — and **never to an unauthored spec**; KF.W11/KF.W12/KF.W13 are **MINTED-UNAUTHORED** per R4-8, so a census handed to a filename would have no reader)*: the deletion's blast radius is **4 class-consumers for `.focus-ring` across 4 files** *(corrected at repair round 4 from the authored "five"; the header and this Finding both asserted five over an enumeration of four — the same "number asserted beside an enumeration that does not produce it" fault this file convicts elsewhere three times over)* **and 4 class applications plus one selector-string registry entry for `.btn-playback`, over a 7-file radius** *(corrected from "8 files")* — the two rules are **not** local to their scenes, which is exactly why **K-5 holds** (one deletion fixes nothing) and why the bank's *"wholly redundant against producer `base.css`"* ground must be re-derived **before** the act, per G-KFW9-9's standing obligation. **The `--focus-ring-shadow` token consumers are NOT class consumers** — a sweep that conflates them over-counts the radius by two and would mis-scope the act.

**Census 3 — ST-1's repoint (`seq-handle` → `focus-ring` at `SequenceTarget.vue:97`) + the `:163-167` rule deletion, carried as a DECLARATION; cure bytes are KF.W11's.** Command + output:
```
git grep -n 'seq-handle' origin/master -- demo/ test/ src/ scripts/      → 11 hits / 2 files
  demo/scenes/sequence/SequenceTarget.vue:97   class="seq-handle"     ← the ONE template consumer
  demo/scenes/sequence/SequenceTarget.css      :23 --z-seq-handle · :120 base rule · :135 z-index use
      · :139 ::after · :155-156 hover/focus-visible ::after · :163 the FORKED :focus-visible rule
      · :168 :active · :200 the traveller's z-index use
git grep -n 'proof:sequence-rows-draggable' origin/master -- .
  → demo/scenes/sequence/SequenceTarget.css:162 (a COMMENT)
  + DOC PROSE ACROSS FIVE TRANCHES — "H prose ONLY" is STRUCK at repair round 4 (R4-4);
    the true field, re-measured this seat by tranche (files / line-hits):
        H ×13 files / 22 line-hits · J ×3 / 6 · K ×1 / 1 · S ×1 / 1 · U ×1 / 1
        = 19 files, 31 doc line-hits.
  THE K ROW, QUOTED BY COMMAND (LAW D) — the historical CI-gating evidence a deleting seat must meet:
    git grep -n 'proof:sequence-rows-draggable' origin/master -- docs/tranches/K/
      → docs/tranches/K/audit/ci-cd-k.md:73:
        | `proof:sequence-rows-draggable` | H.W12 | ~1m (drag + re-sort wait) | ci.yml:1031-1034 |
    i.e. the retired gate is filed against **`ci.yml:1031-1034`** — it once gated CI. The J row
    docs/tranches/J/waves/J.W7c-impl.md:177 likewise reads "gated GREEN by
    `proof:sequence-rows-draggable`". Neither revives the gate; both are what a KF.W11 seat
    will meet before deleting a rule with a CI history.
  THE FALSIFIER IS RE-CONFIRMED AND UNCHANGED — the fork's justification IS void at the frontier:
    git grep -c 'proof:sequence-rows-draggable' origin/master -- package.json          → exit 1
    git grep -n 'proof:sequence-rows-draggable' origin/master -- .github/ scripts/     → exit 1
    NO script, NO workflow, NO package.json entry. The SUBSTANCE of the row is unharmed;
    what was false was the SCOPE of its prose radius.
```
**Finding, handed to the R4-8 successor-formation register's KF.W11 row** *(hand-off RE-ISSUED at repair round 4 — RULINGS-4 R4-4: addressed to **KF-W10 §6.D · SUCCESSOR-FORMATION REGISTER, the KF.W11 row**, anchor-only per LAW C(3), never to an unauthored spec; the retired-gate PROSE radius above travels with it, because that is the cell this census got wrong)*: the class has **exactly one template consumer**, so the repoint is a one-site act — but `seq-handle` also carries **eight further style obligations in its own stylesheet** (`--z-seq-handle`, the base rule, both `::after` states, `:active`, the traveller's z-index), so **the class is NOT deleted by the repoint**: only the `:163-167` `:focus-visible` fork is. A cure that removes `class="seq-handle"` in order to adopt `.focus-ring` would silently drop the handle's geometry, z-order and hover affordance — the failure mode this census exists to foreclose. **The ordering lock stands** (S-15): the repoint must not precede KF.W13's two-deletion act, or the sliders move to *"no indicator via the defeated class"*.

### §RECEIPT · Witnesses re-derived this session (R2-5), enumerated BY FAMILY — the D3-4 cure

*Scope statement per LAW B: this block is an **enumeration, not a universal**. A family not listed was not re-derived this round; a family listed was re-executed at the substrate named beside it. `PASS-3/KF-W9-CHECK.md §6 D3-4` (2026-08-28) convicted round 2's universal precisely because the library-reference family was absent from its block — the family is the FIRST entry here.*

| family | substrate | what was re-executed |
|---|---|---|
| **kf library reference (§Bounds row) — the D3-4 member** | `origin/master 81a56990` | `index.ts:50` (`reducedMotionScale` export) · `engine/play-lifecycle/frame.ts` `playFrame` `:121`/flip `:131`/call `:137` · `engine/play-lifecycle/strategies.ts` docblock `:66-75`/decl `:76`/final-frame `:109` · `group/group.ts:57` · `src/animation/waapi/delegation.ts` `shadowTick` **`:53`** *(printed `:54` here at round 3 and **corrected at repair round 4, D4-4** — a receipt whose whole purpose is to replace a false universal with an enumerated re-derivation must not carry a coordinate a one-line `git grep` refutes; the pair was half-measured, `:64` right and `:53` read as `:54`)*/loop `:64` · `constants/defaults.ts:87` · the carve `53b907c5` |
| **the `reducedMotionScale` consumer set (R3-9)** | `origin/master 81a56990` | 6 hits over `src/ demo/ test/ scripts/`; **demo → no hits (exit 1)**; `physics/spring/progress.ts` import `:2`, assignments `:153`/`:232`/`:385`, field `:100`, applications `:323`/`:352`; kf `docs/published-surface.md:35` read verbatim |
| **the three carried delete/repoint acts (LAW A)** | `origin/master 81a56990` | censuses 1–3 above, pasted with outputs |
| **the OD-V3/OD-V5 packet definition (R3-10.4)** | `origin/master 81a56990` | kf `docs/tranches/V/OWNER-DECISIONS.md:7` (OD-V3, *"held for capture review"*, 390 AND 1280, both homes, four scenes) · `:9` (OD-V5, *"deferred pending glass's dock mark"*) · `docs/tranches/V/audit/R2-01-visual-design.md` §DP2-06 (the four scenes named: cube · amiga · square · easing; the two homes described) · `docs/tranches/V/FOLD-FORWARD.md:34` (CH2-02 ×4 = BG-5 · GU-1 · GU-2 · subject-legible) |
| **the sibling W10 anchors (R3-10.4)** | value.js current bytes | `KF-W10.md` §State *Opens after* · **OP-4** · the PACKET-FIRST law · §D *"→ KF.W9 … W10 depends"* · the two coordination rows (*"KF.W9 surface receipt"*, *"both waves' bounds blocks"*) |
| **the residue recount (D3-6)** | value.js current bytes | one script, both variants, over all 58 records: `h2-only 563 · h2+h3 565 · records-with-items 57 · files 58`; the two h3 members located at `kf-ChannelOptions.md` `### G. SS-13 additions` (`:223`), items 11 (`:225`) / 12 (`:226`) under `## ADDENDUM R2` (`:174`) |
| **the U-2(c)(3) anchor targets** | value.js current bytes | `X/waves/W6.md` `## 8. Verification Artefacts` (`:354`) with **MATRIX** as a bolded inline label at `:367` · `X/waves/W11.md` `### G8 — Real-Safari evidence cell, held separate` (`:290`) |
| **the D3-8 paths** | `origin/master 81a56990` | `git ls-tree -r --name-only origin/master \| grep -i 'OrbitalDrag.vue\|delegation.ts'` → `demo/scenes/cube/orbital-drag/OrbitalDrag.vue` · `src/animation/waapi/delegation.ts` |
| **NOT re-derived this round** (stated, not hidden) | — | the §Carry rows' banked quotations (round 2's receipt stands) · the thirteen gate witnesses other than -6/-7 (PASS-3 §4 re-executed them and found them TRUE at the frontier; this seat inherits that artifact **by citation**, per LAW B, rather than re-asserting it) |

### Defect register — round 3

| directive / defect | severity | disposition | where |
|---|---|---|---|
| **R3-7** — the play-lifecycle witness family re-derived at the frontier layout; **all five citing sites** re-anchored; SUBSTRATE-INVERSION restated as a frontier fact | ruling | **APPLIED VERBATIM** — file+symbol anchors under the R2-7 idiom at §Bounds' library row, §A KF-KE-8, §A KF-TD-1, G-KFW9-6, G-KFW9-7; the carve `53b907c5` and the barrel-exclusion stated so KF.W5's **B-6** docblock cure lands in `strategies.ts`; both specs now name the same file and lines, cross-cited | §Bounds · §A ×2 · G-KFW9-6/-7 |
| **R3-9** — G-KFW9-6's witness restated truthfully; the in-tree precedent NAMED | ruling | **APPLIED VERBATIM** — *"used by nothing"* STRUCK as unbanked at **both** sites; **zero demo / three library consumers** with the grep pasted; the **spring lane** (`SpringProgress.amplitudeScale`, applied `:323`/`:352`) named as the shipped intensity-form precedent; the gate's RED restated as *the demo half does not yet consume the resolver the library half already ships*; `published-surface.md:35`'s *"manifest-only"* recorded as the likely inheritance source (spec-side note; that doc is not edited here) | §A anchor row · G-KFW9-6 |
| **R3-10.4** — the W10-seam edge cured at its SOURCE end | ruling | **APPLIED VERBATIM** — S-10 gains the **KF.W10 cross-edge (PRECEDES — W10 depends)**; S-8 family (iii) gains the **1280 arm**, the **four duplicating scenes** (cube · amiga · square · easing, enumerated from DP2-06's own words) and **both transport homes** (in-panel card · floating pill), with the **OD-V5 390 at-rest observation** as a named member; §Bounds gains the **surface-receipt row** so W10's *"both waves' bounds blocks"* is true from this end. **This wave rules nothing** — OD-V3/OD-V5 stay OWNER-GATED at W10's owner block | S-10 · S-8(iii) · §Bounds |
| check **D3-1** — the `play-lifecycle.ts` family resolves ONLY at the DISQUALIFIED `8281638c` | HIGH | **CURED by R3-7** — the conviction is accepted whole: this was the wave's own central discipline turned inward, load-bearing on two born-RED gates and on the one row whose point is the lane flip | as R3-7 |
| check **D3-2** — a born-RED gate witness a one-line grep refutes at BOTH baselines | HIGH | **CURED by R3-9** — and recorded as the sharper fault: the claim was never true at either ref, so no substrate change can excuse it | as R3-9 |
| check **D3-3** — a sibling's HARD inbound dependency closed by OMISSION | HIGH | **CURED by R3-10.4** — the asymmetry broke W10's OP-4 *by construction*; the cure is a declared output, not a promise | as R3-10.4 |
| check **D3-4** — R2-5's universal false for the library-reference family | MEDIUM | **CURED** — the universal is scoped-with-enumeration at the preamble and the per-round receipt block above leads with that family; *"state the scope, or enumerate the members"* — this file's own remedy, applied to itself | preamble · §RECEIPT |
| check **D3-5** — *"13 cross-edges"* asserted twice, enumerated nowhere | MEDIUM | **CURED** — the set enumerated at **S-10** with its **counting rule stated** and two alternative granularities disclosed (16 / 7); the numeral now lives ONLY at its enumeration and both dependent cells point here; the coincidence with the prior figure is disclosed as coincidence | S-10 · preamble · provenance |
| check **D3-6** — the published reproducing command yields **563**, not 565 | MEDIUM | **CURED** — command amended to `##` **or `###`**; the kf-ChannelOptions h3 section named with both item coordinates; **S-13's material-divergence trigger re-pointed at the amended form** so it cannot arm on a command-manufactured 2-count delta. The FIGURE (565 / ≈590) is not re-opened — the check declines that explicitly and so does this seat | §Protocol 2 · S-13 |
| check **D3-7** — `⟨X-W6 §MATRIX⟩` resolves to no §-heading, gate id or banked id | LOW | **CURED** — re-anchored to **`X/waves/W6.md` §8 Verification Artefacts** with **MATRIX** cited as what it is, a bolded inline label (`:367`), parenthetical; the sibling half re-stated at **`X/waves/W11.md` §G8** | §H I-20 |
| check **D3-8** — two §Bounds paths unresolvable under the row's own shorthand base | LOW | **CURED** — both written in FULL (`demo/scenes/cube/orbital-drag/OrbitalDrag.vue` · `src/animation/waapi/delegation.ts`), each with its `ls-tree` receipt; the authored `delegation.ts:50` struck as resolving to no named symbol while `:54`/`:64` were kept *"as measured"* — ⟨**superseded at repair round 4, D4-4**: only `:64` was measured; `:53` was read as `:54` and the pair kept as a pair. Corrected at all three sites (§Bounds, §A KF-TD-1, the §RECEIPT row); the *path* cure D3-8 landed is untouched and correct⟩ | §Bounds ×2 |

**No defect declined this round.** Round 1's single decline (**D-14**, KF-EST-4's missing KF.W6 counterparty) is untouched and stands on **R-19e**; nothing in PASS-3 disturbs it, and PASS-3 §7 records it as standing.

**Counting family, restated at its own enumeration (LAW B — every figure at its rule).** §Bounds table rows: **16 data rows** at repair round 3 (rule: table rows excluding the header and the `|---|` separator), **15 before this round's receipt-row addition**. *Disclosed so a pass-4 seat does not read a phantom drop:* `PASS-3/KF-W9-CHECK.md §1`'s counting-family cell read the pre-round-3 table at **16**, a count that includes the header row. Gates: **thirteen live** (`-1..-9`, `-11..-14`; `-10` struck-in-place, id retained, never reused). §Carry bullets: **79**, unmoved this round — **no row was added, removed, renamed or re-graded**; every round-3 edit is an anchor, a witness restatement, an enumeration or a declaration.

**Posture unchanged by this round**: the wave still **measures** — zero write grants in the kf tree, zero cures spent, status `planned`, gate ids un-renumbered, every cure-shape lock, every rider and all twelve dissents carried. The three acts censused under LAW A are **other waves' bytes**; carrying their censuses adds evidence, never authority.

## Repair ledger — round 4 (X·KF L-20, PASS-4)

Sole authorities: `conformance/PASS-4/RULINGS-4.md` (cross-wave, wins on collision) and `conformance/PASS-4/KF-W9-CHECK.md` (local). Sole writable file: this spec. **Rulings applied VERBATIM: 3 of 3** — **R4-3** · **R4-4** · **R4-6** — plus the four local directives **D4-4 · D4-5 · D4-6 · D4-7**. **Check defects: 7 booked · 7 cured · 0 declined.** Two new program laws bind every edit: **LAW C** (the two-stage / reconcile law — write order, quote-by-command, anchor-only forward references, the mandatory close certification) and **LAW D** (the verbatim law and its numeric arm), on top of the standing laws of rounds 1–3 (**LAW A** import-graph census · **LAW B** closure-claim ban · **R2-5** measure at the tree · **R2-7** stable anchors · rulings outrank local findings on collision). **This wave writes in STAGE 2.** Nothing here opens product source; keyframes.js was read **read-only** at `origin/master 81a56990` (`git rev-parse origin/master` → `81a56990736ced5b5edde0b84c527680ac7689b1`, re-derived this seat) throughout; status stays `planned`; no gate ran; no verb moved.

### §STAGE · LAW C compliance — write order, substrates, and what was reduced to an anchor

**Stage position.** LAW C(1) orders STAGE 1 = KF-W0 first, then KF-W1..KF-W6; STAGE 2 = KF-W7, KF-W8, **KF-W9**, then KF-W10 last. This seat therefore consumes stage-1 siblings **at their final stage-1 bytes** and hands KF.W10 nothing but anchors. Substrate table, `stat`-ed at this seat's clock immediately before the receipts below were written:

| sibling | stage | mtime at quote time | how this file consumes it |
|---|---|---|---|
| `waves/KF-W5.md` | 1 | 2026-08-28T16:55:55 | **quoted by command** — §A KF-TD-1's B-6 cross-cite; G-KFW9-6's R4-3 reciprocal |
| `waves/KF-W6.md` | 1 | 2026-08-28T16:59:25 | **quoted by command** — §G KF-HA-10's owner-ruling half; G-KFW9-6's R4-3 reciprocal |
| `carry/KF-W6-CARRY.md` | 1 | (read this seat) | **quoted by command** — `grep -n 'KF-HA-10'` → `:220` |
| `waves/KF-W0.md` · `waves/KF-W4.md` | 1 | 16:58:59 · 16:59:44 | consumed by **§-heading anchor** (S-11's packet roster · OP-6/G-KFW9-10's head law) — the R2-7 idiom already in force, unchanged |
| `waves/KF-W10.md` | 2, **writes LAST** | — | **ANCHOR-ONLY (LAW C(3))** — five inbound anchors at S-10 and §Bounds, two hand-off anchors at §LAW-A; **no prose quoted, no line numbers** |

**What LAW C changed.** Round 3's W10 receipts were quotations — five sentences and the §6.C-0 seam reading — taken from a spec that had not finished moving. They are **struck as receipts and retained only as that round's dated historical readings**; what stands in their place is `KF-W10` **§State (Opens after) · OP-4 · the PACKET-FIRST law · §D's → KF.W9 cross-edge · §B-1 and §B-3**, by name. The R4-4 blast-radius hand-offs are addressed the same way — to **KF-W10 §6.D · SUCCESSOR-FORMATION REGISTER**, the KF.W13 and KF.W11 rows — because R4-8 rules those three waves **MINTED-UNAUTHORED**, so a census addressed to an unauthored spec would have no reader at all. **The RECONCILE seat verifies every one of these anchors after the last per-wave write and certifies at `PASS-4/CLOSE-CERT.md`; without that artifact the round did not close, and this file certifies nothing about its own siblings (LAW B).** ⟨**RE-POINTED at repair round 5 — RULINGS-5 R5-2(3)/R5-8; PASS-5 check D5-1/D5-2.** The certification named here was **never executed for this wave's forward class** (CLOSE-CERT-4 §3 counts five backward receipts; §7's roll-call has no `KF-W9 → KF-W10` row; §4 R-8 runs the other way), **and the artifact is void as proof-of-order** (its §1 table is refuted by a two-command `stat` on `KF-W10.md`: +21 B / +6 min 27 s after the certificate). `PASS-5/KF-W9-CHECK.md §2` verified all six anchors instead — carried by command at §Sequencing S-10 — and the certifying artifact of record is **`PASS-5/CLOSE-CERT-2.md`** under LAW E, which sweeps **stage-2 → stage-2** as a named class (LAW E(5)(ii)).⟩

### §SCOPE · R4-10's two arms, executed BY SCOPE and receipted

*R4-10(1)'s mechanism change, quoted because it is the reason this block exists: "a repair that cures the named instance and not the stated scope leaves the law green over its own counterexample." Each class below is enumerated from its **source** — the access column, the symbol-bearing rows — never from the ruling's example list.*

**LAW A scope receipt — the enumeration source is the §Bounds ACCESS COLUMN.** Column grepped at this seat's bytes: **16 data rows enumerated; 0 carry `delete`, `modify/delete`, `repoint`, `shim` or `move`.** The access values present are `create` (×3), `create/write`, `modify` (×2), `modify-carve`, `EXECUTE, NO WRITE`, `read-only`/`read-only reference`/`read-only witness surface` (×7) and the round-4 **append-only** split at the registry row (D4-5) — **an append is not a delete, a repoint or a move, and arms no census.** This wave therefore **authors no LAW-A act**, exactly as its round-3 scope statement said, and the statement now rests on a grepped column rather than on a reading. **The three acts it CARRIES as declarations for other waves have 3 of 3 censuses present** (censuses 1–3 above), all four convicted cells corrected this round under R4-4, all commands re-run at write time under LAW D(3).

**SUBJECT-IDENTITY scope receipt — the enumeration source is every symbol-bearing §Bounds row and every gate-witness symbol, whole file.** Swept and re-executed this seat at `origin/master 81a56990`, not the two rows a ruling named: **library-reference row** — `reducedMotionScale` `index.ts:50` ✓ · `playFrame` decl `frame.ts:121` ✓ · `withReducedMotion` flip `:131` ✓ · `snapToReducedMotion(anim)` call `:137` ✓ · `snapToReducedMotion` decl `strategies.ts:76` ✓ (docblock `:66-75`) · final-frame `beginPlay(… withReducedMotion(` `:109` ✓ · `respectReducedMotion` `group/group.ts:57` ✓ (`respectReducedMotion = false;`) · `constants/defaults.ts:87` ✓ (`respectReducedMotion: false,`) · **`shadowTick` decl `:53` ✗→ CORRECTED (D4-4)**, loop `:64` ✓; **gate-witness symbols** — `.focus-ring:focus-visible` `design-idioms.css:76` ✓ · `.btn-playback:focus-visible` `playback-idiom.css:72` ✓ (G-KFW9-9) · `clampIOSNoZoomFontSize` decl `demo/components/instrument/utils/iosTextEntry.ts:10`, consumed `CSSCodeEditor.vue:39`/`:137`, unit-tested `test/demo/instrument/ios-text-entry.test.ts:88`/`:89`/`:97` ✓ · `initIOSPlatformClass` decl `iosTextEntry.ts:14`, called `EditorShell.vue:133` ✓ (G-KFW9-11) · `"gh-pages": "vite build --mode gh-pages"` `package.json:43` ✓ (OP-5/G-KFW9-14/§H) · `seq-handle` `SequenceTarget.vue:97` ✓ (S-15) · `class="seq-handle"`/`--z-seq-handle` obligations ✓ (census 3); **§Carry source anchors** — `numeric.ts` **✗→ path-qualified (D4-7)**, and the standing rule *"every §Carry source anchor carries its full repo-relative path"* written at the cure so the class cannot regrow. **One symbol-identity defect found and cured; one bare-path defect found and cured; all other members resolve.**

**Numeric arm (LAW D(3)) scope receipt.** Every figure printed beside a command in this file's LAW-A block was re-run at write time: `24` / **`7`** (was 8) · `11` / `7` files / **`4`** class applications (was 5) · `2` import consumers + the widened non-import scope · `11` / `2` files · the five-tranche prose field **19 files / 22·6·1·1·1 line-hits** (was "H prose ONLY") · both falsifier exits (`package.json` → 1, `.github/ scripts/` → 1). The counting family was likewise re-derived at the **post-repair** bytes by one script: **§Carry bullets 79** (A 18 · B 5 · C 1 · D 8 · E 7 · F 13 · G 19 · H 8 — unmoved; no row added, removed, renamed or re-graded this round) · **§Bounds data rows 16** (18 pipe rows − header − separator; unmoved — D4-5 split a cell, it did not add a row) · **gate rows 14, live 13** (`-1..-9`, `-11..-14`; `-10` struck-in-place, id retained) · **row-level dissents 11 + 1 wave-level = 12** · **`KF.W3` occurrences — FIGURE STRUCK AND RE-HOMED at repair round 5** (RULINGS-5 §END KF-W9 D5-4 / R5-11; PASS-5 check D5-4): this cell printed *"4 file-wide … 1 in the spec body … plus 3 in this round-4 ledger"* **beside the very command that returns 3**, and a third cell then printed *"×1"* — three cells, three figures, one token, inside the block whose own header claims every figure beside a command was re-run at write time. **True at the round-4 post-repair bytes: 3 file-wide · 1 spec body · 2 ledger** (re-measured this seat at those bytes, which had not moved). The standing count is stated ONCE, with its rule and its command, at **§Repair ledger — round 5 §SCOPE**; this cell re-issues no numeral. *This is the D4-6 cure measured honestly rather than flatteringly: writing the correction down printed the token three more times, so the cured sentence claims a **scheduling** fact and a **scoped** count, not a bare one — the bare form would have re-falsified itself in the act of being cured.*

### §RECEIPT · Witnesses re-derived this round, enumerated BY FAMILY (the D3-4 idiom, held)

*Scope statement per LAW B: an **enumeration, not a universal**. A family not listed was not re-derived this round.*

| family | substrate | what was re-executed |
|---|---|---|
| **the three LAW-A censuses (R4-4)** | `origin/master 81a56990` | all four convicted cells + their commands: `btn-playback` 24/**7** with the seven files listed and `EasingScene.vue:61` entered · `focus-ring` 11/7 with **4** class applications · census 1's non-import scope widened to the four sibling-module docblocks · census 3's five-tranche field with `ci-cd-k.md:73` quoted by command and both falsifier exits re-confirmed |
| **the `shadowTick` pair (D4-4)** | `origin/master 81a56990` | `git grep -n 'shadowTick' … delegation.ts` → `:53`/`:64`; `sed -n '54p'` → the body's first statement; `git grep -c 'withReducedMotion' … delegation.ts` → exit 1 |
| **`numeric.ts` (D4-7)** | `origin/master 81a56990` | `git ls-tree -r --name-only \| grep numeric.ts` → one hit, `src/animation/physics/numeric.ts`; `sed -n '82p;89p'` → the constructor + the flag assignment |
| **the SUBJECT-IDENTITY symbol sweep (R4-10)** | `origin/master 81a56990` | every symbol enumerated at §SCOPE above, each resolved to its own site |
| **the stage-1 sibling receipts (LAW C(2))** | value.js, mtimes tabled at §STAGE | `KF-W5.md` `66-75` ×2 cells + its R4-3 reciprocal at B-6 · `KF-W6.md` KF-HA-10 §Carry row quoted WHOLE (round 2's unmarked ellipsis closed) + its R4-3 reciprocal at KAD-11 + its → KF.W9 split-row declaration · `KF-W6-CARRY.md:220` |
| **the kf dated authorities (LAW D)** | `origin/master 81a56990` | `OWNER-DECISIONS.md:7` (OD-V3, *"390 AND 1280"*, both homes, four scenes) · `:9` (OD-V5 deferred pending glass's dock mark) · `FOLD-FORWARD.md:34` (CH2-02 ×4 = BG-5 · GU-1 · GU-2 · subject-legible) — all three reproduce byte-exact |
| **the §B fold enumeration (R4-6)** | value.js current bytes | the KF-CE-13 row split on its own ` · ` separator by script → **16** |
| **the counting family** | value.js **post-repair** bytes | 79 bullets by section · 16 §Bounds data rows · 14 gate rows / 13 live · 11+1 dissents · **`KF.W3` — the *×1* is STRUCK at repair round 5** (D5-4): a THIRD figure, in a list whose every other member is a file-wide count, disagreeing with both cells beside it; the file-wide count is stated once at §Repair ledger — round 5 §SCOPE and this row points there |
| **NOT re-derived this round** (stated, not hidden) | — | the §Carry rows' banked registry quotations (rounds 2–3's receipts stand; PASS-4 §2/§3 re-verified them at the bytes and found them subject-exact, and this seat inherits that artifact **by citation**, per LAW B) · **the gate witnesses — WARRANT RE-SCOPED at repair round 5 (RULINGS-5 §END KF-W9 D5-7; PASS-5 check D5-7): the round-4 wording *"the thirteen gate witnesses other than -6 (PASS-4 §4 re-executed them…)"* claimed twelve witnesses from an artifact that names SEVEN gate ids and re-executes FIVE, and is STRUCK.** Enumerated at the artifacts, by command (`awk '/^## §4/,/^## §5/' <artifact> \| grep -o 'G-KFW9-[0-9]\+' \| sort \| uniq -c`, run this seat): **`PASS-4 §4` names 7** (`-1 -6 -8 -9 -10 -13 -14`) and **re-executes born-RED witnesses for 5** — `G-KFW9-1` · `-8` · `-9` · `-10`'s surviving measurement · `-14` (`-6`/`-13` appear as reachability findings, not witness runs); **`PASS-5 §4` names 8** (`-1 -6 -8 -9 -10 -11 -13 -14`) and **re-executes 6** — the same five **plus `-11`**. **The six with NO re-executed witness in either artifact — `-2` · `-3` · `-4` · `-5` · `-7` · `-12` — are DOCUMENTARY** (I-20's two firings · the B18-26 Kronecker arithmetic · the 565+25 residue denominator · the census sentence · G-KFW9-7's two-direction pair · the trap/retired register): they rest on this file's own enumerations, not on a frontier probe, and **each is re-verified at WAVE-OPEN per the §Gates preamble**, never inherited by citation · the residue recount 565/25 (PASS-4 §1 reproduced it independently and **declines to re-open it**; PASS-5 §1 reproduced it a third time — `h2-only 563 · h2+h3 565 · 57 records · 58 files` — and so does this seat) |

### Defect register — round 4

| directive / defect | severity | disposition | where |
|---|---|---|---|
| **R4-3** — G-KFW9-6 re-cut on the G-KFW9-9 template; the unification constraint handed on as a declared sequencing obligation | ruling | **APPLIED VERBATIM** — the ruled CLOSES written in full (three own-wave measurements + the falsifier); the constraint carried **verbatim** and HANDED to KF.W5 (engine) / KF.W6 (tokenization); both reciprocals verified at the siblings' final stage-1 bytes by command; the wave records the discharge and never a green on the constraint's behalf | G-KFW9-6 |
| **R4-4** — the three LAW-A censuses re-run; four cells corrected; the blast-radius hand-off re-issued | ruling | **APPLIED VERBATIM** — 8→**7** files · 5→**4** class-consumers (header AND Finding) · census 1's scope widened to the four sibling docblocks · census 3's *"H prose ONLY"* struck for the five-tranche field with the K row quoted by command; hand-offs re-addressed to **KF-W10 §6.D**'s KF.W13 / KF.W11 rows, anchor-only | §LAW-A censuses 1–3 |
| **R4-6** — *"fourteen folds"* corrected at the enumeration and at all three dependent cells | ruling | **APPLIED VERBATIM** — **SIXTEEN** stated AT §B's enumeration with its counting rule and its id-/record-level granularities disclosed; §Carry route (b), **G-KFW9-8's CLOSES (a gate's GREEN)** and **S-2** all re-pointed at the enumeration, none re-issuing a numeral | §B · §Carry head · G-KFW9-8 · S-2 |
| check **D4-1** — the LAW-A block's counts and scopes refuted by its own pasted commands, in all three censuses | HIGH | **CURED by R4-4** — and recorded as the round's sharpest lesson: *round 3's repairs did not audit their own arithmetic.* A blast radius is the one thing a delete-act consumer must not have to re-derive | as R4-4 |
| check **D4-2** — G-KFW9-6 an unpassable oracle: it closed on cure acts in a tree where this wave holds zero write grants | HIGH | **CURED by R4-3** — round-1 check D-3 at its third and last gate; round 3 rewrote the *witness* correctly and left the close condition standing underneath it | as R4-3 |
| check **D4-3** — *"fourteen"* asserted at three cells over an enumeration of sixteen, one of them a gate's green | HIGH | **CURED by R4-6** — the fourth register in this file to be cured by the same remedy it wrote for itself: **state the count at its enumeration** | as R4-6 |
| check **D4-4** — the `shadowTick` declaration anchor one line late at BOTH refs, inside the receipt that certifies it re-executed | MEDIUM | **CURED** — `:53` at both citing sites **and** in the §RECEIPT row; the round-3 *"kept as measured"* claim superseded at its own ledger row; the substance (`withReducedMotion` absent from `delegation.ts`) independently re-confirmed | §Bounds · §A KF-TD-1 · §RECEIPT · round-3 register |
| check **D4-5** — §Bounds marks the 58 records `read-only, IMMUTABLE` while G-KFW9-13 can close only by writing into them | MEDIUM | **CURED** — the cell split into **read-only for the existing bytes (E-1) / APPEND-ONLY for dated addenda under original ids (E-3), by `.e` alone**, which is what S-13's carve-out and G-KFW9-13's CLOSES already assume; the alternative (route addenda to `evidence/W9/**`) is declined on the record below | §Bounds registry row |
| check **D4-6** — S-10.1's *"this spec holds 0 references to it"* falsified by the sentence that states it | LOW | **CURED** — re-worded to the ruled sentence *"this spec schedules nothing against it; the only occurrence of the token is this disclosure"*, **carrying the dissent roster's scope rule with it** (ledger and provenance prose are not spec-body references). Measured at the post-repair bytes: ~~**4 file-wide, 1 in the spec body, 3 in this ledger**~~ — **the numeric arm of this cure was itself false and is STRUCK at repair round 5 (D5-4/R5-11): the true round-4 figures are 3 · 1 · 2**, and the standing count now lives at one enumeration (§Repair ledger — round 5 §SCOPE). The **posture** the row records is untouched and correct — the scope rule is load-bearing, not decorative, because recording the cure printed the token again; a bare re-count would have re-falsified itself in the act of being cured | S-10.1 |
| check **D4-7** — `numeric.ts:82-89` a bare filename under no declared base | LOW | **CURED** — written `src/animation/physics/numeric.ts:82-89` with its `ls-tree` receipt and both offsets re-verified, plus the **standing rule** that §Carry source anchors carry full paths (§Bounds' bases are §Bounds-local) — the D3-8 class swept in §Carry, where round 3 did not reach | §A D-8 + M-7 |

**No defect declined this round.** Round 1's single decline (**D-14**, KF-EST-4's missing KF.W6 counterparty) is untouched and stands on **R-19e**; nothing in PASS-4 disturbs it, and PASS-4 §7 item 9 records it as standing. **Two alternatives offered by the check and NOT taken, stated so the choice is auditable rather than silent**: (i) D4-4's *"or drop to the R2-7 idiom (file + symbol, no offset)"* — the line is written instead, because the pinpoint is cheap, now measured, and independently corroborated by KF.W5's own `delegation.ts:53-64` spelling; (ii) D4-5's *"or route the addenda into `evidence/W9/**` and re-cut G-KFW9-13 to close there"* — declined because it would move this wave's write-back **off the records the bank routes it to**, leaving thirteen provisional severities discharged in a file no record points at, and because **S-13's own trigger sentence presumes the append is lawful**; the split honours both the gate and E-1/E-3 without inventing a second home. PASS-4 §7's twelve standing declines are carried forward untouched — in particular items 2 (the 565/≈590 denominator and its amended command), 5 (the W10 seam, cured as a declared output), 6 (S-10.1's coincidence disclosure, *"must not be trimmed"*), 10 (the two `D-15`s never merged) and 12 (no LAW-B violation on S-10.1's thirteen).

**Posture unchanged by this round, stated so the edits cannot be misread as motion**: the wave still **measures** — **zero write grants in the kf tree** (the D4-5 split grants nothing there; it disambiguates an append inside value.js's own registry, which §Bounds already contemplated), **zero cures spent**, status `planned`, gate ids un-renumbered, thirteen live gates, §Carry unmoved at 79 rows, every cure-shape lock, every rider and all twelve dissents carried. **G-KFW9-6's re-cut removes no obligation** — it relocates one to the two waves that can discharge it and books it at both ends, which is the difference between a constraint that binds and an oracle that cannot be reached. **This ledger asserts no closure in its own voice (LAW B)**: the census claim is the pass-4 artifact's, cited above with its defects; the cross-wave receipts written this round are the **RECONCILE seat's** to certify at `PASS-4/CLOSE-CERT.md`, and **without that certification this round did not close**. ⟨**RE-POINTED at repair round 5 — RULINGS-5 R5-2(3).** That certification is **void as proof-of-order** and **incomplete as a sweep**: round 4's repairs BANK (15 of 16 verified; the pass-5 checks re-verified the rest), but the seal does not. This wave's closure — round 4's receipts included — conditions on **`PASS-5/CLOSE-CERT-2.md` + RULINGS-5 R5-2**, and this file still certifies nothing about its own siblings (LAW B).⟩

## Repair ledger — round 5 (X·KF L-20, PASS-5)

Sole authorities: `conformance/PASS-5/RULINGS-5.md` (cross-wave; wins on collision) and `conformance/PASS-5/KF-W9-CHECK.md` (local). Sole writable file: this spec. **Directives applied VERBATIM: 8 of 8** — **R5-8(a)** (the delegation becomes a verification) · **R5-8(b)** (one enumeration of six, D5-3) · **R5-8(c) + D5-1** (closure re-pointed to CLOSE-CERT-5 + R5-2) · **D5-4** (the token figures at every cell, R5-11) · **D5-5** (the docblock characterisation) · **D5-6** (§F re-pointed, drift row 16) · **D5-7** (the witness warrant re-scoped) · **D5-8** (both banked aliases spelled) — **plus the R5-1(5) guard-vindication line** at §G. **Check defects: 8 booked · 8 cured · 0 declined.** Two new program laws bind every edit: **LAW E** (the cert is write-last BY CONSTRUCTION and hash-proven; **anchor-only for per-wave seats** — no quoted sibling prose, no sibling line numbers, no per-seat substrate stamps; the sweep roster widened to **stage-2 → stage-2** and to substrate receipts) and **LAW F / mechanism S** (every act that strikes, moves, drops, re-keys, narrows, re-points or mints books its **SHADOW** in the same edit; **per-row anchors, never per-carrier**). LAW A, LAW B, LAW C and LAW D of rounds 3–4 remain in force. Frontier re-verified this seat — `git rev-parse origin/master` → **`81a56990736ced5b5edde0b84c527680ac7689b1`**; the local worktree stays DISQUALIFIED (R3-2). Status stays `planned`; no product source was opened; the wave still holds **zero write grants in the kf tree**.

### §SHADOW · LAW F(2) — this round's struck / minted / re-pointed / narrowed set, enumerated

***TWO of this round's eight acts carry an at-the-act SHADOW line — §F `:164` (the citation re-point) and §Sequencing S-10 `:284` (the delegation→verification cure); the remaining SIX are citation-level and are shadowed by ENUMERATION in this table alone, which is stated here rather than assumed** ⟨***the round-5 preamble ~~"Per LAW F(1) each act carries its own SHADOW line AT the act"~~ is STRUCK at repair round 6 under LAW G (PASS-6 D6-4): it was a universal its own file falsifies by grep, in the round that minted the law. The six citation-level acts are: (4) the closure re-point at three cells · (5) the two alias spellings · (6) the §RECEIPT warrant narrowing · (7) the census-1 characterisation strike · (8) the three struck figures + the S-10.1 parenthetical · and four of act (3)'s five re-pointed cells (header `:3`, §Carry head `:98`/`:100`/`:102`, §Bounds PRM row `:75`). Every one uses a verb on LAW F(1)'s own trigger list (STRUCK · re-pointed · NARROWED), and the LAW F(1) exemption for citation-level acts is argued here rather than assumed. The enumeration below is complete and correct, and LAW F(2)'s terminus sweep for this round is discharged at `PASS-6/CLOSE-CERT-6.md` §ERRATA E-6.***⟩; this table is the round's enumeration, which the RECONCILE seat re-sweeps against every named terminus before close. **Per-row anchors, never per-carrier (LAW F(3))**: every item below was read at its own cell before it was touched.*

| # | act (verb) | set | disposition | termini / citing waves notified |
|---|---|---|---|---|
| 1 | **struck + replaced** (R5-8(a)) | the round-4 **delegation sentence** at ⟨§Bounds · the `SS-13-CAPTURE-RECEIPT.md` row⟩ and at ⟨§Sequencing S-10 · the KF.W10 cross-edge⟩ | replaced **in place** by the six-anchor verification-by-command; the obligation is **discharged, not transferred** | **KF.W10** (host of the six anchors — anchor-only, no motion asked) · **RECONCILE** (CLOSE-CERT-5 re-verifies the class under LAW E(5)(ii)) |
| 2 | **struck numeral** (R5-8(b)) | *"five anchors"* at ⟨§Bounds⟩ and at ⟨S-10⟩ | one enumeration of **six** (A1..A6) with its counting rule at S-10; §Bounds points there and re-issues no numeral | none owed — both cells local |
| 3 | **re-pointed** (R5-9 row 16 / D5-6, instance **and** class) | the standing-verdict citation at ⟨header · §Carry head · the round-2 correction block · the R2-5 receipt · **§F preamble**⟩ → `PASS-5/KF-W9-CHECK.md §1`; the PRM-surface **warrant** at ⟨§Bounds · the kf PRM row⟩ gains its freshness line | superseded readings (167 · 147 · **139** · **168**) retained **in place** as their rounds' dated measurements; **no row, id, routing, severity or figure of this wave moves** | none owed — all citations local; the class is now greppable (§SCOPE (2)) |
| 4 | **re-pointed** (R5-2(3) / D5-1) | the closure-conditioning sentence at ⟨round-4 preamble⟩ · ⟨§STAGE⟩ · ⟨round-4 posture⟩ → **`PASS-5/CLOSE-CERT-2.md` + RULINGS-5 R5-2** | **E-3 honoured**: `PASS-4/CLOSE-CERT.md` is NOT edited; round 4's repairs BANK, its seal is void as proof-of-order | **RECONCILE** (the cert of record for rounds 4 **and** 5) |
| 5 | **spelled, not minted** (D5-8) | ⟨kf-EasingTarget⟩ **`KF-ET-14 / D-M-6`** at §E · ⟨kf-ChromeDock⟩ **`D-6 / m-6`** at §G | **MINTED SET = EMPTY.** Both are banked ids written in full at their **existing** carrying rows; no §Carry bullet added; discharge unchanged (§E's row; route (d) via that record's residue item #8) | none — neither id changes home, owner or severity |
| 6 | **narrowed** (D5-7) | the §RECEIPT *"NOT re-derived this round"* row's inherited warrant | narrowed from *"the thirteen gate witnesses other than -6"* to the **five** PASS-4 §4 re-executed + the **six** PASS-5 §4 re-executed, with `-2/-3/-4/-5/-7/-12` declared **documentary** and re-verified at wave-open | none — **no gate id, CLOSES, falsifier or witness is touched**; only the warrant's scope |
| 7 | **struck characterisation** (D5-5) | census 1's *"four stale sibling docblocks"* sentence | struck and restated at the transcript; **no id, consequence, consumer or count moves** — the Finding and the numeric arm are untouched | none — the census is carried for **KF.W5/KF.W13**, whose inherited census is unchanged in substance |
| 8 | **struck figures** (D5-4 / R5-11) | `4 file-wide` · `3 in this ledger` · `×1` at three cells (+ the S-10.1 parenthetical) | one enumeration with rule and command at §SCOPE (1); the round-4 posture the figures decorated is **correct and kept** | none — local arithmetic |

**Rows moved this round: ZERO. Routings minted: ZERO. Ids re-keyed: ZERO.** §Carry stands at **79** (A 18 · B 5 · C 1 · D 8 · E 7 · F 13 · G 19 · H 8), gate ids un-renumbered (14 rows / 13 live), no severity re-graded, every cure-shape lock and all twelve dissents carried, no cure spent. **The one restoration this round touches is not this wave's act**: R5-1 restores **EH-4 / EH-5 / EH-8** LIVE at **KF.W6** — that SHADOW is W6's, and this wave's end is the dated vindication line at its §G guard row, receipted anchor-only.

### §SCOPE · R5-11 — RECEIPT-ARITHMETIC-FIRST: every receipt states its enumeration, its counting rule, and its command

**(1) The `KF.W3` token census — ONE enumeration, and the only cell that states the figure (D5-4).** *Counting rule*: an occurrence is a match of the pattern below; **a pasted spelling of the pattern does not match itself** (the escape breaks the literal run), which is why printing the command never inflates its own output; **spec body** = the S-10.1 non-edge disclosure; **ledger/provenance** = repair-ledger and provenance prose, which the D4-6 cure's carried scope rule excludes by name. Re-run at this round's **post-repair** bytes, this seat:

```
grep -c  'KF\.W3\b' KF-W9.md → 6
grep -on 'KF\.W3\b' KF-W9.md → 6 hits; the MATCHED TOKEN IS ELIDED from the six rows below
                                 BY CONSTRUCTION — printing each hit would add one occurrence
                                 and the transcript would falsify its own count. That is the
                                 D4-6 recursion exactly, one level down, and the reason this
                                 figure is stated WITH its scope rule and never bare:
  :13   ⟨the round-5 preamble — PROVENANCE PROSE, naming the directive being executed⟩
  :285  ⟨§Sequencing S-10.1 — THE SPEC BODY; the deliberate-non-edge disclosure, 1 of 1⟩
  :560  ⟨§Repair ledger — round 4 §SCOPE — the struck-figure cell⟩
  :575  ⟨§Repair ledger — round 4 §RECEIPT — the counting-family row⟩
  :620  ⟨§Repair ledger — round 5 §SCOPE (1) — this census's own header⟩
  :637  ⟨§Repair ledger — round 5 §SCOPE (1) — the posture sentence below⟩
```

→ **6 file-wide · 1 spec body · 5 in ledger and provenance prose** (2 in round 4's ledger, 2 in this one, 1 in the round-5 preamble — *the count grew because CURING it required naming it, which is the D4-6 lesson restated, not a new defect: the **body** figure is the one the disclosure claims, and it is **1**, unmoved through five rounds*). At the **round-4** post-repair bytes the same command returned **3 · 1 · 2**, and those are the figures the round-4 cells should have printed; they printed **4 / "3 in this ledger" / "×1"** instead — *a false numeric arm inside the cure of a numeric defect, printed beside its own pasted command, in the block whose header claims every such figure was re-run at write time*. All three are struck and now **point here**. **The posture is untouched and remains true**: KF.W3 is gated-unscheduled, this spec schedules nothing against it, and outside ledger and provenance prose the count is **1**.

- **`O-20` / `O-21`** — **2 occurrences on 1 line, both at `:692` inside the round-5 ledger's carried decline (*"O-20/O-21 is correctly not this wave's surface"*); 0 in the spec body.** The same scope rule as `KF.W3`: ledger and provenance prose are outside the body census. **The posture is untouched and correct** — no mint is imported, no KF.W1 coordinate rides here. Entered at repair round 6 (PASS-6 D6-8) because this is the D4-6 recursion at its FOURTH register (after the `KF.W3` token, the dissent roster and the §F citation class): a pass-7 seat re-running the command PASS-5 ran gets a different answer, and until this line existed there was no in-file rule to reconcile it.

**(2) The citation-freshness class — made GREPPABLE, not remembered (D5-6's class arm).** *Counting rule*: every match of `PASS-[0-9]/KF-W9-CHECK`, partitioned by what the citing cell CLAIMS — a **STANDING** cell asserts something true *now* (the census verdict, a path-resolution warrant, a witness warrant) and **MUST name the freshest artifact**; a **DATED** cell records what a round measured or which artifact governed that round and **MUST NOT be re-pointed**. Command + output, this seat:

```
grep -on 'PASS-[0-9]/KF-W9-CHECK' KF-W9.md | wc -l   → 35 occurrences
STANDING — 12 occurrences at 11 cells, EVERY ONE naming PASS-5:
  :3   header census carriage       :75  §Bounds PRM-surface warrant  :77  §Bounds capture receipt
  :98  §Carry head, enumeration of record                             :100 the re-homed closure claim
  :102 the R2-5 receipt's standing verdict                            :164 §F's class verdict (×2)
  :255 S-10's verification baseline   :259 its transcript's source label
  :550 §STAGE's re-point             :694 the round-5 ledger's LAW-B census sentence
DATED — 23 occurrences, NOT re-pointed and NOT re-pointable:
  superseded readings retained in place (6): PASS-3 at :75 :100 :102 :164 · PASS-4 at :100 :102
  round-of-record records (12): :5 :7 :11 :13 (×2) :193 :313 :345 :373 :498 :530 :536
  this round's own records (5): :599 :609 :672 (×2) :687
by artifact: PASS-1 ×3 · PASS-2 ×2 · PASS-3 ×7 · PASS-4 ×5 · PASS-5 ×18 (12 standing + 6 dated)
```

**Every STANDING cell names PASS-5 after this round; no DATED cell was touched.** This is the defect's *class* arm: round 4 re-pointed four cells and left §F, so the file named two artifacts (139 and 168) as the home of one standing verdict — R4-10's *scope-not-instance* lesson in miniature, now closed by a partition a grep reproduces.

**(3) The counting family, re-derived at the post-repair bytes by one script (never arithmetic on the previous count).** **§Carry bullets 79** — A 18 · B 5 · C 1 · D 8 · E 7 · F 13 · G 19 · H 8, *unmoved: no row added, removed, renamed or re-graded this round* (the two alias spellings edited **existing** bullets) · **§Bounds data rows 16** (18 pipe rows − header − separator; unmoved — no row added) · **gate rows 14, live 13** (`-1..-9`, `-11..-14`; `-10` struck-in-place, id retained) · **row-level dissents 11 + 1 wave-level = 12** (**15** file-wide occurrences of the phrase `Dissent preserved` at these bytes — the roster's stated scope rule, *inside `- **` §Carry bullets; occurrences in ledger and provenance prose, **including this cell**, are not roster members*, reproduces exactly: the roster is 11, the raw phrase count is not) · **the KF.W10 anchor set 6** (enumerated at S-10 with its rule) · **KF-CE-13 fold families sixteen** (stated only at §B's enumeration; not re-issued) · **cross-edges thirteen** (stated only at S-10.1; the A1..A6 anchor ordinals are **not** cross-edges and do not enter that set).

**(4) LAW A scope receipt — unchanged and re-derived.** The enumeration source stays the §Bounds **access column**: **16 data rows, 0 carrying `delete` / `repoint` / `shim` / `move`**. This wave **authors no LAW-A act**. Of the three it CARRIES as declarations, **census 1's characterisation was false and is cured (D5-5) while its arithmetic was true and is untouched** — PASS-5 §3 reproduced all four R4-4-corrected cells (7 files · 4 class-consumers · the widened census-1 scope · the five-tranche prose field) and both falsifier exits, cell-for-cell.

### §RECEIPT · Witnesses re-derived this round, enumerated BY FAMILY (the D3-4 idiom, held)

*Scope statement per LAW B: an **enumeration, not a universal**. A family not listed was not re-derived this round.*

| family | substrate | what was re-executed |
|---|---|---|
| **the frontier pin** | keyframes.js | `git rev-parse origin/master` → `81a56990736ced5b5edde0b84c527680ac7689b1`, exact |
| **the EH-family anchors (R5-1(5))** | `origin/master 81a56990` | `git show …:demo/components/instrument/shell/EditorShell.vue \| sed -n '32,47p'` → the shortcuts `Button` `aspect-square w-8 scale-on-hover` at `:36` (cluster `:32-40`) and the DarkModeToggle at `:44-47` with `title="Toggle dark mode"` `:45` / `class="aspect-square w-8 scale-on-hover"` `:46`; `git show …:demo/app/dock/MbabbMenu.vue \| sed -n '19,22p'` → the third instance, `:20-21`, `w-5` variant |
| **census 1's four docblock lines (D5-5)** | `origin/master 81a56990` | three `git show … \| sed -n '1,8p'` transcripts printed whole at §LAW-A: `playback-state.ts:2-3`/`:4` · `group/lifecycle.ts:4-5` · `orchestration/sequence/lifecycle.ts:5-6` |
| **the six W10 anchors (R5-8(a))** | `KF-W10.md`, both columns | the check's baseline offsets **and** this seat's write-time re-run: **6 of 6 anchors resolve; 6 of 6 offsets moved** — the drift class demonstrated rather than asserted |
| **the gate-witness warrant (D5-7)** | `PASS-4/KF-W9-CHECK.md` · `PASS-5/KF-W9-CHECK.md` | `awk '/^## §4/,/^## §5/' <artifact> \| grep -o 'G-KFW9-[0-9]\+' \| sort \| uniq -c` → PASS-4 §4 **names 7, re-executes 5**; PASS-5 §4 **names 8, re-executes 6** |
| **the token census + the counting family (R5-11)** | value.js **post-repair** bytes | the two commands at §SCOPE (1) and the section-scoped bullet/dissent/row script at §SCOPE (3) |
| **NOT re-derived this round** (stated, not hidden) | — | the §Carry rows' banked registry quotations (rounds 2–3's receipts stand; **PASS-5 §3 re-verified M-25 depth — every lock, rider and dissent — and found them carried**, inherited here **by citation** per LAW B) · the residue recount **565 + 25** (PASS-5 §1 reproduced it a third time — `h2-only 563 · h2+h3 565 · 57 records · 58 files` — and declines to re-open it; so does this seat) · the born-RED gate witnesses **not** named at D5-7 — of the fourteen, **six were re-executed by PASS-5 §4** (`-1 -8 -9 -10 -11 -14`), **`-6`/`-13` were adjudicated for reachability**, and the remaining **six are documentary and re-verify at wave-open** |

### Defect register — round 5

| directive / defect | severity | disposition | where |
|---|---|---|---|
| **R5-8(a)** — the delegation becomes a verification; the stage-2→stage-2 class enters the sweep roster | ruling | **APPLIED VERBATIM** — both delegation cells re-cut; the six anchors verified **by command in two columns** (the check's baseline + this seat's write-time re-run), anchors load-bearing, offsets dated; CLOSE-CERT-5 re-verifies under LAW E(5)(ii) | §Bounds capture-receipt row · S-10 |
| **R5-8(b)** — the anchors enumerated ONCE | ruling | **APPLIED VERBATIM** — **six** at S-10 (A1..A6) with the counting rule that makes §B-1/§B-3 two and PACKET-FIRST one; §Bounds points there and re-issues no numeral | S-10 · §Bounds |
| **R5-8(c) + check D5-1** — closure re-pointed to CLOSE-CERT-5 + the R5-2 attribution | ruling / **HIGH** | **APPLIED VERBATIM** — three closure-conditioning cells re-pointed; the falsification recorded with its two-command receipt and the LAW E(3) opening-baseline pin; **`PASS-4/CLOSE-CERT.md` is not edited (E-3)**; the cure the check names — *cite this pass's verification, not a delegation that did not happen* — is executed at S-10 | round-4 preamble · §STAGE · round-4 posture · §Bounds · S-10 |
| check **D5-2** — the RECONCILE seat never verified this wave's forward receipts | **HIGH** | **CURED** — the warrant that did not exist is replaced by the verification that did: CLOSE-CERT-4's three structural misses named (§3 five backward receipts · §7 no `KF-W9 → KF-W10` row under a *"stage-1 → stage-2"* scope sentence · §4 R-8 the reverse direction), the six anchors carried by command, and the class made a **named sweep class** for every future round | S-10 · §Bounds |
| check **D5-3** — *"five anchors"* over two enumerations whose union is six | MEDIUM | **CURED by R5-8(b)** — the fifth register in this file cured by the remedy it wrote for itself: **state the count at its enumeration, and have dependent cells point there** (after the negative register, the escalation register, the cross-edge set and the KF-CE-13 folds) | S-10 |
| check **D5-4** — the D4-6 cure's own numeric arm wrong at three cells (`4` / `3 in this ledger` / `×1`) | MEDIUM | **CURED** — one enumeration with its counting rule and command at §SCOPE (1); all three cells struck and re-pointed there, the S-10.1 parenthetical with them; **the posture the figures decorated is correct and kept** | §SCOPE (1) · S-10.1 · round-4 §SCOPE · round-4 §RECEIPT · round-4 register |
| check **D5-5** — census 1's widened scope calls four correct docblocks stale prose | MEDIUM | **CURED** — the characterisation struck at the block header and restated at the transcript: **three lines name the pre-carve path as the CARVE'S ORIGIN (correct as written), one names the LIVE `./play-lifecycle` specifier; no stale reference exists outside the module and the carve left no prose debt.** Four lines over three sibling modules, re-read whole at the frontier this seat. D4-1's shape, caught inside D4-1's own cure | §LAW-A census 1 |
| check **D5-6** — §F homes the class's standing verdict at PASS-3 while every sibling cell moved to PASS-4 | MEDIUM | **CURED at the instance AND at the class** (drift row 16, R5-9) — §F re-pointed to `PASS-5/KF-W9-CHECK.md §1` (**181/181/0**), the four sibling cells swept with it, the PRM-surface warrant's freshness line added, and the class made **greppable** with a STANDING/DATED partition rule at §SCOPE (2) so freshness is swept by command rather than by memory | §F preamble · header · §Carry head ×3 · §Bounds PRM row · §SCOPE (2) |
| check **D5-7** — the §RECEIPT *"NOT re-derived"* row over-claims the artifact it inherits | MEDIUM | **CURED** — the warrant re-scoped by enumeration at both artifacts (PASS-4 §4 names 7 / re-executes 5; PASS-5 §4 names 8 / re-executes 6) and the six with no re-executed witness — `-2 -3 -4 -5 -7 -12` — declared **documentary**, re-verified at wave-open per the §Gates preamble. The citation-inheritance failure this file convicts twice in its own body, now refused a third time | round-4 §RECEIPT · §RECEIPT (this round) |
| check **D5-8** — two banked aliases carried by identity and never spelled | LOW | **CURED** — `KF-ET-14 / D-M-6` written at §E's row and `⟨kf-ChromeDock⟩ D-6 / m-6` at §G's ChromeDock cluster with its route-(d) discharge and the `D-6` collision named; the **standing record-qualification rule** entered at the §Carry format line so the round-2 D-9 remedy is stated as a class (`D-15` ×2 · `D-6` ×2) rather than at its instances. No bullet added, no booking changed | §Carry format line · §E · §G |
| **R5-1(5)** — the W9 guard vindicated, recorded at both ends | ruling | **APPLIED VERBATIM** — the §G guard row gains its dated line: the round-4 MOOT-ON-DELETE premise is struck as a BLOCKER, **EH-4/EH-5/EH-8 are RESTORED LIVE at KF.W6** at their banked anchors, and the frontier bytes the guard always asserted are re-derived here read-only (`EditorShell.vue:45`/`:46`/`:36`, `MbabbMenu.vue:20-21`). W9's own rows do not move; the measurement now has a live cure-owner instead of a struck one | §G · EH-5 + EH-8 |

**No defect declined this round.** Round 1's single decline (**D-14**, KF-EST-4's missing KF.W6 counterparty) is untouched and stands on **R-19e** — PASS-5 §7 item 9 records it standing for the fifth pass. **PASS-5 §7's thirteen standing declines are carried forward whole**, in particular: **no census escape** (181/181/0; a pass-6 seat booking one must first defeat the four adjudications and the nine route-(d) resolutions) · **G-KFW9-6 is CURED** (R4-3's re-cut is in-grant, both reciprocals byte-exact — do not re-open D4-2 or the round-1 D-3 class) · **D4-5 is CURED** (the append-only split; do not re-file the immutability contradiction) · **the 565 / ≈590 denominator and its amended command are BOTH correct** · **the two `D-15`s are distinct and never merged** · **kf-TimelineTrack D-8 is not a W9 escape** · **S-10.1's coincidence disclosure must not be trimmed** · **O-20/O-21 is correctly not this wave's surface**. **One alternative offered by the check and NOT taken, stated so the choice is auditable**: D5-1's *"or have this wave's cells cite `CLOSE-CERT §R-8`/`§7` by finding"* — declined, because R5-2 rules the round-4 certificate **void as proof-of-order**, so citing any of its findings would re-home this wave's closure on the instrument the ruling voided; the cells cite **CLOSE-CERT-5 + R5-2** and carry the pass-5 verification directly instead.

**Posture unchanged by this round, stated so the edits cannot be misread as motion**: the wave still **measures** — **zero write grants in the kf tree**, **zero cures spent**, status `planned`, gate ids un-renumbered, **thirteen live gates**, **§Carry unmoved at 79 rows**, every cure-shape lock, every rider and all twelve dissents carried, no severity re-graded, no id re-keyed, no routing minted. **Every edit this round is a receipt, a citation, an enumeration or a spelling** — the substance of the wave was already sound at PASS-5 (*"None of the eight touches a row, a gate, a lock, a dissent or the posture"*), and what is repaired is the machinery that proves it. **This ledger asserts no closure in its own voice (LAW B)**: the census claim is `PASS-5/KF-W9-CHECK.md §1`'s, cited above with its defects; the six forward anchors are verified here and **re-verified by the RECONCILE seat at `PASS-5/CLOSE-CERT-2.md`**, whose hash table (LAW E(2)) the pass-6 union seat re-runs as its first act — **and this seat's last write to this file is its last act on it.**

---
⟨Provenance: agglomerated 2026-08-28 **from the 58-record registry directly** — `registry/adjudicated/kf-*.md`, all 58 by reference — plus INTAKE-ADJUDICATION-2026-08-03 §3 and KF.W0 §B-12. *The authored line cited a "KF.W9 CARRY table (69 rows + 14 gates …)"; **no such ledger exists in tree** (the only in-tree carry is `carry/KF-W6-CARRY.md`, 201 rows / 54 records), and the citation is STRUCK at repair round 1 under RULINGS R-12's general law — same strike as the header's. Current figures, **each stated at its own enumeration (LAW B, repair round 3)**: **79 §Carry bullets** (recounted at the bytes at round 2, unmoved at rounds 3 **and 4**) · **13 live gates** (`-1..-9`, `-11..-14`) · **16 §Bounds data rows** (15 + round 3's surface-receipt row; unmoved at round 4 — D4-5 split a cell, it added no row; counting rule and the divergence from the PASS-3 cell disclosed at §Repair ledger — round 3) · **KF-CE-13's fold families: the set is enumerated at §B and its numeral — sixteen by that row's ` · ` separation — is stated ONLY there** (repair round 4, RULINGS-4 R4-6; the authored *"fourteen"* was asserted at three dependent cells, one of them a gate's GREEN, and enumerated at none) · **cross-edges: the set is enumerated at §Sequencing S-10 and its numeral is stated ONLY there** — the authored *"13 cross-edges"* was asserted here and at the preamble with no enumeration anywhere (PASS-3 check **D3-5**), and a figure with no derivation is not re-issued.* Exemplar idiom: `docs/tranches/X/waves/W0..W11.md` + `docs/tranches/X/parse-that/waves/` + sibling `KF-W8.md`. **Dissents preserved — ROSTER RE-ENUMERATED 2026-08-28 (repair round 2, R2-15; KF-W9-CHECK D-7: the round-1 roster said nine while the body carried eleven, the three R-1e additions never having been added to the roster that claims to enumerate them). Counted at the bytes, scope stated so the count stays reproducible as this file grows: **eleven** `Dissent preserved` instances **inside §Carry bullets** (lines opening `- **`; occurrences in ledger and provenance prose, including this sentence, are not roster members) **+ one wave-level scope dissent = TWELVE.** Row-level: KF-SS-20 (MAJOR, axis+reader-1) · KF-EST-11 (D MAJOR) · KF-SST-13 (A MAJOR) · D-16 seq-family (kf-SequenceTarget A MINOR) · D-3+DU-M-1 (DU BLOCKER) · D-15 RTL (axis MINOR) · R-20/KAD-F4 (R-A MAJOR, armed) · KF-ET-4 (reader-2 BLOCKER) · **D-5 kf-SpringTarget (reader-1 MAJOR)** · **D-6 kf-SpringTarget (reader-1 MAJOR)** · **D-12 kf-PlaybackRibbon (DU MAJOR)** — the last three entered with R-1e's §F rows at round 1. Wave-level: **S-12/OP-7 (AT-cell scope)**, recorded at OP-7, S-12, §Excluded's AT-verification lane and at §F's D-1/L-i1 row (that record's M-2/N-2 routing contest is the same dissent, not a twelfth row). The three rows added at round 2 (kf-AmigaScene D-2 · MISSED-A · kf-HeroAurora KF-HA-10) carry **no** dissent at their banks — checked, not assumed. Status: planned; execution awaits the owner's begin-word.⟩
