# AUDIT-2 register: fourier-analysis

**Date:** 2026-09-24. **Authority:** COHESION §0cq (Lens 1 component cogency, Lens 2 mobile views) and §0cu (Lens 3 hierarchy and space). **Spec:** `docs/tranches/X/audit/AUDIT-2.md`. **Model:** Opus 5.5, every seat.

**Owner, verbatim (2026-09-24):** *"ensure that we're not duplicating any component in any view, too: KISS, DRY. Audit our component structure for cogency in every project."* … *"Audit every mobile view for every mobile app view for all projects, too."* … *"All issues should be fixed at the glass-ui root, too."* … *"Ensure proper design hierarchy and usage of space in all UIs hereof"*

**Trees audited:** fourier `798c98f` with 13 uncommitted web files (the in-flight X.F.W14U.vdock edits to AnimationControls, BasisCanvas, CanvasControlsDock, EasingPicker, ExportModal, FullscreenViewer and SpeedSelect); glass-ui `2b72324a` (L3 seat) and `25c205a9` (L2 later passes, register confirm). Served at http://localhost:3100 (API :8000), headed Chromium on the real GPU.

**Seat evidence:** `docs/tranches/X/audit/audit-2/fourier-L1/`, `fourier-L2/`, `fourier-L3/`, and this register's confirm probe in `fourier-register/` (`confirm.mjs`, `confirm.json`, frames git-excluded).

**Routing:** a CONSUMER row goes to **F.W14U** (or its successor). A GLASS row goes to **the AUDIT-2 glass letter** (BL). A row with both halves goes to both; the glass half is named in the owner cell. A row marked "cite" is already registered; the cure wave works it under the earlier id.

## Totals

After confirmation (one HIGH downgraded to LOW; see below).

| Lens | BROKEN | HIGH | MEDIUM | LOW | Total |
|---|---|---|---|---|---|
| L1 component cogency | 0 | 1 | 14 | 13 | 28 |
| L2 mobile views | 2 | 7 | 7 | 3 | 19 |
| L3 hierarchy and space | 0 | 2 | 9 | 4 | 15 |
| **All** | **2** | **10** | **30** | **20** | **62** |

Routing split: 56 rows carry a consumer half (F.W14U) and 25 carry a glass half (glass letter); 6 of those are glass only (A2-FO-L2-6, -7, -12, -13, -14, A2-FO-L3-14). 15 glass asks are new to the registers (listed under "Glass letter" below).

Cross-lens dedupe: no two rows name the same defect, so all 62 ids are kept. Where two rows touch one file, each cites the other: L1-1 with L2-16 and L3-15 (the paper ToC), L1-20 with L2-17 (CollapsibleSection), L1-27 with L3-1 and L3-2 (the admin ledgers), L2-8 with L3-7 (ExportModal).

## Confirmation of BROKEN and HIGH rows

The register seat re-read every cited file:line and re-measured each row on the served page at 2026-09-24T17:34Z (`fourier-register/confirm.json`, 0 page errors; frames in `fourier-register/frames/`). Insets were emulated through CDP `Emulation.setSafeAreaInsetsOverride`: portrait bottom 34; landscape left 47, right 47, bottom 21.

| Row | Seat severity | Result | Register measure / re-read |
|---|---|---|---|
| A2-FO-L2-8 | BROKEN | **Confirmed** | Export dialog at 844x390: y -54 to 444, height 498 (the seat measured 516 before later vdock edits), scrollHeight 496 = clientHeight 496, overflow-y visible. `ExportModal.vue:81` bare `<DialogContent>`; glass `DialogContent.vue:183` caps height only under `scroll`. Frame `L2-8_l844_export.jpg`. |
| A2-FO-L2-13 | BROKEN | **Confirmed** | Collapsed animation dock scrollWidth/clientWidth 144/110 at 360, 390 and 430; 147/110 at 844x390. Frames `L2-2_v360_wcanvas.jpg`, `L2-4_l844_wcanvas.jpg`. |
| A2-FO-L1-1 | HIGH | **Confirmed, corrected** | The ToC is rendered **three** times, not four: the drawer tree (`PaperToc.vue:261-330`), the floating dropdown tree (`:424-470`) and PaperView's inline Chapters nav (`PaperView.vue:493-512`). The "tab rail" at `PaperToc.vue:342-362` is the drawer's toggle button, not a ToC. `PaperToc.vue` is 998 lines. HIGH stands: three hand-rolled trees of one section list, and the owner named the ToC as a duplicate to check. |
| A2-FO-L1-3 | HIGH | **Downgraded to LOW** | The "PaperSearch mounted twice" premise is stale. At HEAD the two ToC hosts are exclusive by breakpoint (`PaperView.vue:450-452` `v-if="!isDesktop && !mobileTocVisible"`, `:476` `v-if="isDesktop"`; the UIA-F-22 cure), and the modal is single-owner (`:464`, PSM-3). What remains is two search surfaces on desktop (sidebar inline plus the palette), which UIA-F-159 (MEDIUM) and UIA-F-64 already carry. The row only names an owner, so it is LOW and cite-only. |
| A2-FO-L2-1 | HIGH | **Confirmed** | 390x844, inset 34, /paper: document scrollHeight 878 against a viewport of 844, scrollY reaches 34, body padding-bottom 34px, app dock top at y -26 after the scroll. `style.css:106-108` against the `h-dvh overflow-hidden` shell at `App.vue:124`. Frame `L2-1_v390_paper.jpg`. |
| A2-FO-L2-2 | HIGH | **Confirmed** | Animation dock bottom 762 / 826 / 914 at 360 / 390 / 430, which is 16px inside the 34px inset. Stage bottom 774 / 838 / 926, 6px from the edge. `VisualizationView.vue:600-606` and `:731-733` (`bottom: .75rem`, no env()); `FullscreenViewer.vue:252`, `:257` (`bottom: 0`, no env()). The only `env(safe-area-*)` in glass src is `deck/styles/stage.css:33-34`. Frames `L2-2_v3*_wcanvas.jpg`. |
| A2-FO-L2-3 | HIGH | **Confirmed** | 844x390 with 47px side insets: the first interactive element (the Controls/Canvas tab) at x=12 and the stage from x=5. `index.html:5` sets `viewport-fit=cover`; no fourier or glass rule reads safe-area-inset-left or -right. Frame `L2-4_l844_wcanvas.jpg`. |
| A2-FO-L2-4 | HIGH | **Confirmed** | 844x390 /w Canvas tab: app dock 60px plus margin (bottom 72), tab row 44px (bottom 120), stage 834x255 (y 129-384), canvas dock y 137-197 and animation dock y 312-372 over the stage. The side-by-side gate is width-only (`VisualizationView.vue:74`, `EquationView.vue:60`). Frame `L2-4_l844_wcanvas.jpg`. |
| A2-FO-L2-12 | HIGH | **Confirmed** | Login popover [0..336] at 360 (flush left); About popover [54..390] at 390 (flush right). glass `PopoverContent.vue` and `DropdownMenuContent.vue` set no collisionPadding; only `SelectContent.vue:56` sets 16. No viewport-pad token exists in glass yet, so the cure mints one. Frames `L2-12_v360_login.jpg`, `L2-12_v390_about.jpg`. |
| A2-FO-L2-15 | HIGH | **Confirmed, corrected** | Expanded editor dock layer scrollWidth/clientWidth 432/183 at 360 and 432/253 at 430. It holds **8** buttons, not nine, and about 58% (360) or 41% (430) of the layer sits behind the unmarked scroll. Frames `L2-15_v360_editor.jpg`, `L2-15_v430_editor.jpg`. |
| A2-FO-L2-16 | HIGH | **Confirmed** | 844x390 /paper: floating ToC bar y 72-153 (81px) under the app dock (bottom 72); dropdown y 153-387, 3px from the bottom edge and inside the 21px inset; both full-bleed [0..844]. Frame `L2-16_l844_papertoc.jpg`. |
| A2-FO-L3-1 | HIGH | **Confirmed** | 1440 admin Users: search 1408x36 at y152, sort Select 1408x40 at y196, Prune 112x36 at y244, toolbar 128px tall. `AdminUserList.vue:382-383` (flex-wrap; search `basis-full sm:basis-0 sm:flex-1`), `:423` (`class="shrink-0"` only); glass `SelectTrigger.vue:59` bakes `w-full`. Frame `L3-1_d1440_users.jpg`. |
| A2-FO-L3-2 | HIGH | **Confirmed** | 390 admin Flagged: row and card end at x 373; the action group spans x 97-425 (328px, nowrap), so Delete runs x 318-425, 52px past the card. `admin-row.css:131-136` (`flex-wrap: nowrap`), `:139-151` (narrow arm `"lead body" "lead actions"`). Frame `L3-2_m390_flagged.jpg`. |

No BROKEN or HIGH row was dropped. One was downgraded (L1-3) and two had their counts corrected (L1-1, L2-15).

## Lens 1: component cogency (28 rows)

| ID | Sev | View | Finding | Evidence | Cause | Cure | Owner | Route | Covered by |
|---|---|---|---|---|---|---|---|---|---|
| A2-FO-L1-1 | HIGH | /paper (desktop + mobile) | One sections tree is rendered as a table of contents three times (register correction; the seat said four): the desktop drawer tree, the floating mobile ToC, and PaperView's inline "Chapters" nav. All three are hand-rolled. PaperToc.vue is a 998-line god module (212 script, 268 template, 518 style) that hosts both ToC surfaces and both search variants. | `PaperToc.vue:216-340` (drawer tree), `:342-362` (drawer tab toggle, not a ToC), `:364-470` (floating mobile ToC); `PaperView.vue:493-512` (the "Chapters" list, also the IntersectionObserver sentinel). Runtime `fourier-L1/metrics.json` d:/paper navs = ["Table of contents","Chapters"]; frames `fourier-L1/frames/d-_paper.png`, `m-_paper.png` | Each breakpoint grew its own row markup and styles; there is no shared recursive row component. The active-chain logic (glass useTreeIndex isInActiveChain) paints one rendering only. | Build ONE recursive PaperTocTree.vue (rows, numbering, active chain via glass useTreeIndex/isInActiveChain, one row style) and mount it in two hosts: the desktop drawer and a glass Sheet on mobile. Retire the floating ToC's row markup and PaperView's inline Chapters list, and move the IO sentinel to a plain element. Split PaperToc.vue into PaperTocTree (rows), PaperTocDrawer (desktop host) and PaperTocSheet (mobile host). | CONSUMER `components/paper/PaperToc.vue`, `components/paper/PaperView.vue` | F.W14U | Partial: UIA-F-67 (mobile ToC hand-rolled, diverges from desktop), UIA-F-162 (inline chapter list), UIA-F-24/F-66/F-156/F-163 (per-surface symptoms). New here: the single-owner cure and the god-module split. See also L2-16, L3-15. |
| A2-FO-L1-2 | MEDIUM | /paper; value.js About markdown (cross-app) | glass ships the ToC behaviour (useTreeIndex, isInActiveChain, useSidebarFollow, useClickDelegate, useScrollTo) but no ToC rendering, so every consumer hand-rolls the tree: fourier three times (L1-1), and value.js hand-styles its own `.toc` list. | glass `src/composables/sidebar/index.ts:1-13` (behaviour only; no ToC in src/components); fourier `PaperToc.vue:216-470`; value.js `demo/scenes/about/markdown/Markdown.vue:383-410` | glass delivered the ToC behaviour layer without a view. | glass ships a TocTree component (recursive, start-aligned list-row arm, current/active-chain paint, data-scroll-target wiring for useClickDelegate) over its sidebar composables. fourier's PaperTocTree and value.js's `.toc` adopt it. | GLASS new `src/components/toc/` over `src/composables/sidebar/*`; consumers fourier `PaperToc.vue`, value.js `Markdown.vue` | Glass letter (new ask) + F.W14U adopt | Partial: UIA-F-147 (no start-aligned list-row arm). The missing ToC component is new. |
| A2-FO-L1-3 | LOW (was HIGH) | /paper search (desktop, mobile, palette) | Seat: one query is served by three search surfaces from one state, PaperSearch mounted twice plus PaperSearchModal. **Register:** at HEAD the two PaperSearch variants are breakpoint-exclusive (UIA-F-22 cure), so desktop has two surfaces (sidebar inline plus palette) and mobile one. | `PaperToc.vue:224` (sidebar variant), `:367` (floating variant); `PaperView.vue:450-452`, `:476` (exclusive v-if hosts), `:464` (PaperSearchModal) | Each host mounts its own search surface. | Mount ONE PaperSearch in whichever ToC host is live (follows from L1-1's two-host split). Rebuild the palette on glass Command/CommandDialog, sharing PaperSearchResultRow. | CONSUMER `components/paper/PaperToc.vue`, `components/paper/search/PaperSearchModal.vue` | cite (F.W14U) | UIA-F-22 (cured at HEAD), UIA-F-159 (three surfaces, MEDIUM), UIA-F-64 (palette hand-built). This row only names the single owner. |
| A2-FO-L1-4 | MEDIUM | /paper search | fourier's paper search engine is a fork of glass's internal fuzzy matcher: same export names (fuzzyMatch, searchIndex, clearSearchCache), same scoring, the same `prevIdx = -2` seed and the same multiTokenFuzzy. The scoring fixes now live only in the fork. | fourier `components/paper/search/paperSearchIndex.ts:111-180`, `:317`, `:321`; glass `src/composables/search/match.ts:44-263`, `index.ts:1-19` (INTERNAL; no `./search` export key at 10.0.1) | glass classified the engine INTERNAL when it deleted SearchBar, so a consumer that needs ranked rows copies it. | Pick one owner. Either glass publishes the engine (a `./search` subpath) and fourier keeps only its domain index builder (buildSearchIndex over latex-paper types), or glass records paper search as consumer-owned and fourier renames its fork so it stops shadowing the producer's names. Upstream the fork's scoring corrections (pattern-scaled bonus `:136-156`) either way. | GLASS `src/composables/search/*` (publication ruling); CONSUMER `components/paper/search/paperSearchIndex.ts` | Glass letter (new ruling) + F.W14U | Adjacent: UIA-F-161 (irrelevant fuzzy results) is the symptom. The fork is new. |
| A2-FO-L1-5 | MEDIUM | /visualize equation overlay vs /equation | Two components render the simplified series. EquationPanel re-implements notation, budget, the simplify fetch, energy and the KaTeX body inline; EquationResult (used by EquationView) holds the copy button, the keyboard-reachable scroll region and the safe-centre overflow fix. The simplify flow (notation, budget, fetch, energy, abort) is written twice. | `components/visualization/EquationPanel.vue:21-55`, `:104-109`; `components/equation/EquationView.vue:36-47`, `:230-300`; `components/equation/EquationResult.vue:54-96` | The overlay was built beside the route rather than composed from it. | Extract useSimplifiedSeries(components, {notation, budget}) into `components/equation/composables/`; both views consume it. EquationPanel's body becomes `<EquationResult :latex>` and inherits copy, keyboard reach and the overflow fix. | CONSUMER `EquationPanel.vue`, `EquationView.vue`, `EquationResult.vue` | F.W14U | Partial: UIA-F-176 (panel equation clipped, no scroll), UIA-F-177 (panel chrome off-idiom). The duplicate is new. |
| A2-FO-L1-6 | LOW | /visualize sidebar vs /equation | Two twin coefficient panels: each is a ConfiguratorLayer around the shared CoefficientsSpectrum, differing only in a #graph slot and a "rendered N of M" note. | `components/visualization/CoefficientsPanel.vue:13-25`; `components/equation/EqCoefficientsPanel.vue:72-84` (its own comment calls them twins) | The convergence onto ConfiguratorLayer (X.F.W3 .d) kept both files after their bodies became identical. | One `shared/CoefficientsPanel.vue` with props components, sub, renderedTerms? and an optional #graph slot. Delete EqCoefficientsPanel.vue. | CONSUMER `CoefficientsPanel.vue`, `EqCoefficientsPanel.vue` | F.W14U | New. UIA-F-169 (amplitudes drawn twice in one panel) is a different defect. |
| A2-FO-L1-7 | LOW | /visualize canvas docks (view vs edit) | The image-overlay and contour-trace toggle popover is written twice, once per dock, with a different order and trigger. | `components/visualization/CanvasControlsDock.vue:70-92`; `EditorControlsDock.vue:191-211` | Each dock owns its own copy of the preview toggles. | Extract one ViewOverlayPopover.vue (overlay and trace, fixed order, one indicator) mounted in both docks or in a shared persistent slot. | CONSUMER `CanvasControlsDock.vue`, `EditorControlsDock.vue` | cite (F.W14U) | UIA-F-79 (two docks, two Eye popovers). This row adds the extraction cure. |
| A2-FO-L1-8 | MEDIUM | /visualize playback menu vs /morph | Two easing choosers over two catalogs. /visualize has a DropdownMenuRadioGroup (ANIMATION_EASINGS + getEasingSVGPath); /morph has a Select (EASING_PRESETS + easingCurvePath). lib/easings.ts keeps both catalogs and two curve-path helpers. The local name EasingPicker shadows glass's EasingPicker, which is an authoring editor. | `components/visualization/EasingPicker.vue:1-56`; `components/morph/MorphPhaseConfig.vue:58-110`, `:168`; `lib/easings.ts:186-193`, `:202-240`, `:305`, `:310`; glass `src/components/easing/EasingPicker.vue` | The morph and animation subsystems each grew their own easing vocabulary. | One easing catalog (a subset flag per surface), one curve-path helper, one EasingPresetSelect used in both places, named so it does not shadow the producer. The ESC-4 local-math carve (`lib/easings.ts:8-23`) stays a separate value.js question. | CONSUMER `EasingPicker.vue`, `MorphPhaseConfig.vue`, `lib/easings.ts` | F.W14U | Partial: UIA-F-81 (animation settings one-off across /morph and /visualize). The catalog and component duplication is new. |
| A2-FO-L1-9 | MEDIUM | /gallery (public batch, admin users, admin flagged) | Three hand-built destructive-confirm dialogs, each with its own pending-intent and in-flight-guard state machine and footer. | `GalleryView.vue:160-230`, `:493`; `gallery/AdminFlaggedPanel.vue:164-200`, `:566`; `gallery/AdminUserList.vue:138-180`, `:788-870`. Cross-app: value.js `demo/palettes/browser/admin/AdminUsersPanel.vue:235-251`, `demo/palettes/PalettesPane.vue:126`; keyframes `demo/app/dock/MbabbMenu.vue:289` | glass deliberately mints no Confirm symbol (`src/components/dialog/DialogContent.vue:21-28`), so each surface re-derives the intent machine. | One useDestructiveConfirm() (typed pending intent, in-flight lock, close-after-settle) and one `shared/ConfirmDialog.vue` over glass Dialog; the three sites migrate. Offer glass a logic-only useConfirm composable (within its no-symbol ruling), since six sites across three apps repeat it. | CONSUMER `GalleryView.vue`, `AdminFlaggedPanel.vue`, `AdminUserList.vue`; GLASS candidate useConfirm beside `src/components/dialog` | F.W14U + glass letter (candidate) | New. UIA-F-107 (Delete overflows the plate) is a visual symptom in one of the three. |
| A2-FO-L1-10 | MEDIUM | /gallery card, card modal, admin flagged | The tier readout (Crown/Bookmark by tier) and the tier setter are hand-built three times for one three-state enum. AdminFlaggedPanel also runs its own setTier mutation beside the gallery store's. | `gallery/GalleryCard.vue:154-155`, `:160-180`; `gallery/GalleryCardModal.vue:127-136`, `:255-282`; `gallery/AdminFlaggedPanel.vue:428-443`, `:259-285` | There is no tier component, and the store's action refetches, so the flagged panel forked it. | TierMark.vue (readout) and TierControl.vue (glass ToggleGroup, type single, three values), used by card, modal and flagged row. The store gets setTier(slug, tier, {refetch}); delete the panel's fork. | CONSUMER `GalleryCard.vue`, `GalleryCardModal.vue`, `AdminFlaggedPanel.vue`, `stores/gallery.ts` | F.W14U | Partial: UIA-F-188, UIA-F-191 (visual). The triplication and forked mutation are new. |
| A2-FO-L1-11 | MEDIUM | /visualize (empty and loaded) | Upload plumbing exists twice. VisualizationView owns a file input, a picker and a global useImageUpload drop instance; ImageUpload owns a second file input and a second useImageUpload nested inside the first. | `VisualizationView.vue:46`, `:207-214`, `:322`; `ImageUpload.vue:1-40`; runtime `fourier-L1/metrics.json` d:/visualize fileInputs = 1 (empty state) | The X.F.W13.c "one upload affordance" cure merged the visible affordance but not the plumbing. | One upload owner: VisualizationView holds the single useImageUpload and file input and provides openPicker/isDragging to ImageUpload, which becomes presentation only. This removes the nested drop. | CONSUMER `VisualizationView.vue`, `ImageUpload.vue`, `composables/useImageUpload.ts` | F.W14U | Partial: UIA-F-19 (a drop uploads twice), UIA-F-166. The duplicate plumbing is new. |
| A2-FO-L1-12 | MEDIUM | /visualize equation overlay, /equation, /morph | The card-title rung is spelled by hand four times with margin drift (--space-residue vs --space-body). glass ships CardTitle; fourier imports it zero times. | `EquationPanel.vue:72`; `components/ui/CollapsibleSection.vue:96`; `morph/HarmonicLevelGrid.vue:3`, `:166-172`; `morph/MorphPhaseConfig.vue:8`, `:187-193`; grep CardTitle in web/src = 0; glass `src/components/card/CardTitle.vue` | Section headers were copied from the ConfiguratorLayer label look instead of taken from glass Card parts. | Use glass Card + CardHeader + CardTitle (or ConfiguratorLayer where the section discloses) at all four sites; delete `.card-title`, `.config-card-title` and the inline span. | CONSUMER `EquationPanel.vue`, `ui/CollapsibleSection.vue`, `morph/HarmonicLevelGrid.vue`, `morph/MorphPhaseConfig.vue` | F.W14U | New. Adjacent: UIA-F-134, UIA-F-206. |
| A2-FO-L1-13 | MEDIUM | /equation, /morph, /visualize, /gallery admin | A local surface utility, `.cartoon-card`, still paints 13 surfaces in 9 files where glass Card/Surface is the owner. | `style.css:210-260`; `EquationView.vue:408,426,436,452,531`; `FunctionInput.vue:106,216`; `HarmonicLevelGrid.vue:2`; `MorphPhaseConfig.vue:3`; `MorphShapePreview.vue:18`; `BasisCanvas.vue:551`; `gallery/AdminAuditLog.vue:272`; `gallery/BatchActionBar.vue:82`. Runtime cartoonCards /equation 3, /morph 5, /visualize 1; frames `fourier-L1/frames/d-_equation.png`, `d-_morph.png` | The D.W4.a "dead-class resurrection" shim was never retired app-wide; later waves retired it one panel at a time. | Census and retire: each site moves to glass Card (tier per context) or Surface, and the @utility leaves style.css in the same change. | CONSUMER `style.css` and the 9 files listed | F.W14U | Per instance: UIA-F-61, UIA-F-68, UIA-F-168, UIA-F-108. This row is the app-wide census. |
| A2-FO-L1-14 | MEDIUM | every view with tooltips | glass ships only the four decomposed tooltip primitives. fourier keeps a 294-line compact `<Tooltip text side>` shim with 36 call sites; keyframes and value.js spell out Tooltip/TooltipTrigger/TooltipContent at every site. | fourier `components/ui/tooltip/index.ts:1-115`, `ui/tooltip/Tooltip.vue:1-179`; 36 sites (EditorControlsDock 9, ContourSettings 6, CanvasControlsDock 6, PaperToc 5, ...); glass `src/components/tooltip/`; keyframes `demo/components/instrument/transport/TransportDock.vue:68,134,151,161`, `demo/components/CopyButton/CopyButton.vue:53`; value.js `demo/picker/controls/ComponentSliders/ConsoleRail.vue:24-78` | The compact form of the producer's tooltip family is missing, so each consumer wraps it or repeats three elements per site. | glass ships a single-component tooltip (props text/side/align/measure, a max-width token, the as-child focusable dev guard fourier already wrote) beside the decomposed parts. fourier deletes `components/ui/tooltip`; keyframes and value.js adopt it where a site carries text only. | GLASS `src/components/tooltip/` (new compact export); CONSUMER fourier `components/ui/tooltip/*` | Glass letter (new ask) + F.W14U | Partial: UIA-F-146, UIA-F-219. The compact-tooltip gap is new. |
| A2-FO-L1-15 | LOW | /visualize sidebar, /equation, /morph | SliderControl is a local labelled slider (label, inline NumberField, thumb plus fill) because glass LabeledSlider has no value field and no Slider variant has both a visible thumb and a fill. | `components/ui/SliderControl.vue:31-56`; 7 importers (ContourSettings, EditorControlsDock, BasisSelector, EquationPanel, FunctionInput, HarmonicLevelGrid, MorphPhaseConfig); glass `src/components/labeled-field/LabeledSlider.vue` | A producer gap, already relayed. | When glass lands BL-FW14H-1 (thumb and fill) and BL-FW14H-2 (labelled slider with inline value), fourier retires SliderControl onto LabeledSlider. No new ask. | GLASS `src/components/slider`, `LabeledSlider.vue` (BL-FW14H-1/-2); CONSUMER `components/ui/SliderControl.vue` | cite (BL-FW14H-1/-2, then F.W14U) | BL-FW14H-1, BL-FW14H-2, UIA-F-75, UIA-F-116. |
| A2-FO-L1-16 | MEDIUM | /visualize playback dock, /equation convergence | GlassTimeline (356 lines) re-derives a scrub session (open and close across pointerdown, up, cancel, lostpointercapture and blur) over glass Slider, because Slider emits no scrub-start or scrub-end. Its name also sits in the producer's namespace. keyframes re-derives the same drag session. | `components/visualization/GlassTimeline.vue:1-68`; consumers `AnimationControls.vue:11`, `equation/convergence/ConvergenceTimeline.vue:24`; keyframes `demo/composables/useDragScrub.ts:1-30` | glass Slider emits only update:modelValue and valueCommit (hasChanged-gated), so a consumer cannot tell a scrub is in flight. | glass Slider publishes the scrub session (scrubstart/scrubend events or a `scrubbing` model) that closes unconditionally. fourier renames the component ScrubTimeline, moves it to `components/shared/` and cuts it to the axis and readout. | GLASS `src/components/slider/Slider.vue`; CONSUMER `GlassTimeline.vue` | Glass letter (new ask) + F.W14U | Partial: UIA-F-239 (naming only). The session gap is new. |
| A2-FO-L1-17 | LOW | shell dock account group (every route); value.js palettes | The slug-identity control (log in by slug, generate, copy, log out) is written once per app, and in fourier it lives under visualization/gallery although the shell mounts it. | fourier `components/visualization/gallery/UserSlugBar.vue` (279 lines; mounted at `components/layout/AppDock.vue:33,152`); value.js `demo/palettes/browser/slug/PaletteSlugBar.vue` (241 lines, same verbs `:74-115`) | Both apps share the slug-session auth model with no shared presentation. | Move fourier's control to `components/auth/` now. Offer glass a presentation-only DockAccount/SlugIdentity (events out, no API) so both apps drop their copies; the dock-morph defects in UIA-F-53/F-54 would then be cured once. | CONSUMER `UserSlugBar.vue` (move); GLASS candidate `src/components/dock/` | F.W14U + glass letter (candidate) | Partial: UIA-F-53, UIA-F-54. The cross-app duplicate is new. |
| A2-FO-L1-18 | MEDIUM | /paper | useScrollNavigation.ts (512 lines) re-implements glass useScrollTo's job (scroll to an id with offset, settle and retry, PRM behaviour, scope disposal) plus a teleport arm for the windowed article. PaperView already uses glass useClickDelegate, useSidebarFollow and useTreeIndex from the same family. | `components/paper/useScrollNavigation.ts:50-511`; glass `src/composables/sidebar/useScrollTo.ts:1-94`; `PaperView.vue:7-8`, `:97`, `:363` | glass useScrollTo has no teleport arm for windowed content, so fourier wrote its own navigator. | Compose glass useScrollTo for the settle loop and offset, and upstream the estimate-then-teleport arm to glass as an option. fourier keeps only its navStack/back behaviour. | GLASS `src/composables/sidebar/useScrollTo.ts`; CONSUMER `components/paper/useScrollNavigation.ts` | Glass letter (new ask) + F.W14U | New. |
| A2-FO-L1-19 | LOW | /paper | Two scroll-restoration mechanisms act on one scroller: App.vue restores `<main>`.scrollTop per history entry, and PaperView restores by section id from sessionStorage. | `App.vue:95-117`; `components/paper/PaperView.vue:73`, `:205-217` | A shell-level restore was added without retiring the route-level one. | Keep one. The shell restore suits generic routes; the paper's windowed article needs the section-id restore, so the shell skips routes that declare their own (route meta ownsScroll). | CONSUMER `App.vue`, `PaperView.vue` | F.W14U | Partial: UIA-F-20. The double mechanism is new. |
| A2-FO-L1-20 | LOW | /equation Function card | CollapsibleSection is a second disclosure idiom beside glass ConfiguratorLayer. Its own docblock rules it should be retired, and it has one consumer. | `components/ui/CollapsibleSection.vue:14-31`; sole importer `components/equation/FunctionInput.vue:8` | Retirement was deferred to the owner of the equation column. | Move FunctionInput to ConfiguratorLayer and delete CollapsibleSection.vue; ContourSettings' hand-rolled Advanced disclosure goes the same way. This also removes the L2-17 trigger site. | CONSUMER `FunctionInput.vue`, `ui/CollapsibleSection.vue`, `ContourSettings.vue` | cite (F.W14U) | UIA-F-114, UIA-F-171. This row names the deletion. |
| A2-FO-L1-21 | LOW | every view that toasts | useToast is a compatibility shim over glass toast: a positional (message, type) API, generic titles from TITLE_MAP, the slug appended in parentheses. 7 importers. | `composables/useToast.ts:6-66`; importers `stores/gallery.ts:8`, `GalleryView.vue:8`, `VisualizationView.vue:12`, `useWorkspaceLoader.ts:5`, `AdminFlaggedPanel.vue:16`, `UserSlugBar.vue:17`, `AdminUserList.vue:25` | An old toast API was kept alive in a shim instead of migrating the consumers. | Call glass toast({title, description, tone, action}) directly at each site with a real title, and delete the shim (the owner's no-compat-shim law). | CONSUMER `composables/useToast.ts` and its 7 importers | F.W14U | Partial: UIA-F-214, UIA-F-256 (what the shim produces). The shim is new. |
| A2-FO-L1-22 | LOW | n/a (lib) | No dead components: every .vue file has an importer, including router lazy imports. Dead library code: evaluateBasis has no caller, which leaves evaluateChebyshev and evaluateLegendre reachable only through it; loadDraftByVisualizationSlug has no caller. lib/bases.ts and lib/basis.ts are name twins with different jobs. | `lib/bases.ts:10`; `lib/evaluators.ts:29`, `:61`; `lib/draftStorage.ts:75`; import graph by grep: 0 orphan files | Client-side multi-basis evaluation was superseded and its exports were never removed. | Delete evaluateBasis, evaluateChebyshev, evaluateLegendre and loadDraftByVisualizationSlug. Fold fourierPositionsAt into evaluators.ts and delete lib/bases.ts. | CONSUMER `lib/bases.ts`, `lib/evaluators.ts`, `lib/draftStorage.ts` | F.W14U | New. |
| A2-FO-L1-23 | LOW | /equation plot, /visualize canvas | The "nice number" grid-step algorithm is written twice. | `components/equation/lib/grid.ts:13-21`; `components/visualization/lib/canvas-drawing/grid.ts:8-14` | The two canvas libraries grew separately. | One niceStep in a shared canvas lib (L1-24's shared/canvas), imported by both. | CONSUMER both grid.ts files | F.W14U | New. |
| A2-FO-L1-24 | MEDIUM | n/a (directory structure) | The directory structure no longer matches responsibility. (a) The /gallery route, all admin panels and the shell's account control live under components/visualization/, and a store imports a helper from a component directory. (b) equation/ and visualization/ import each other; FrequencyGraph lives in equation/ though only visualization/ uses it. (c) Equation code has two lib homes. (d) The dev-only shape extractor sits in morph/ and ships as a route. (e) components/ui/ holds only local stand-ins for glass surfaces. | `router/index.ts:88`; `AppDock.vue:33`; `stores/gallery.ts:10`; `equation/ConvergencePlot.vue:10`, `equation/FrequencyGraph.vue:4`; `equation/convergence/ConvergenceTimeline.vue:24`; `visualization/EquationPanel.vue:9`; `visualization/CoefficientsPanel.vue:5`; `lib/equation/*` vs `components/equation/lib/*`; `/demo/shape-extractor` route with `morph/FourierShapeExtractor.vue`; `components/ui/{SliderControl,CollapsibleSection,tooltip}` | Features were added where the first consumer lived and never re-homed as reuse grew. | Re-home: `components/gallery/`, `components/admin/` (Admin*, BatchActionBar, admin-row.css), `components/auth/` (UserSlugBar); lib/api-problem gains problemMessage (retire adminError.ts). `components/shared/` gains canvas/ (useCanvasSetup, niceStep), ScrubTimeline and NotationPills. FrequencyGraph moves to visualization/. Merge the two equation lib homes. The shape extractor moves to a dev-only route (import.meta.env.DEV) under `components/dev/`. `components/ui/` goes as its members retire (L1-14, -15, -20). | CONSUMER `web/src/components/*`, `stores/gallery.ts`, `router/index.ts` | F.W14U | New. UIA-F-119, UIA-F-255 touch only the shape-extractor route. |
| A2-FO-L1-25 | LOW | shell nav, every route | The section registry exists three times: the AppDock tab list and activeTab mapping, the router's VALID_TABS, and the router afterEach writes. | `AppDock.vue:43-60`; `router/index.ts:32-37`, `:224-226` | There is no single route-meta source for navigation. | Declare meta.nav {label, icon, group} on the routes; AppDock builds tabs, active section and the remembered-tab set from router.getRoutes(). | CONSUMER `AppDock.vue`, `router/index.ts` | cite (F.W14U) | UIA-F-212, UIA-F-119. This row names the single source. |
| A2-FO-L1-26 | LOW | /visualize fullscreen | Fullscreen mounts a second copy of the stage (a second BasisCanvas, ContourEditorCanvas and AnimationControls) instead of re-hosting the one stage. | `components/visualization/FullscreenViewer.vue:54-56`, `:89` | The takeover was built as a separate viewer. | Re-host the single stage (same instances, docks in their persistent slots) inside the fullscreen surface so edits and overlays cannot diverge. | CONSUMER `FullscreenViewer.vue`, `VisualizationView.vue` | cite (F.W14U) | UIA-F-14, UIA-F-93. |
| A2-FO-L1-27 | MEDIUM | /gallery admin users and audit; value.js admin | The admin ledgers use two idioms: the audit log is on glass DataTable, users and flagged rows are hand-rolled on admin-row.css. AdminUserList is an 879-line god module (list, search, sort, batch, pager, confirm; 505-line template). The pager is written twice, value.js has a third. | `gallery/AdminAuditLog.vue:6`, `:399`, `:465-485`; `gallery/AdminUserList.vue:31`, `:409-430`, `:521-640`, `:655`, `:765-785`, `:788-870`; `gallery/AdminFlaggedPanel.vue:22`, `:395`; value.js `demo/palettes/browser/admin/PaginationBar.vue` | Migration onto DataTable stopped after the audit log, and glass has no Pagination primitive. | Move users (and flagged, with its media column) to glass DataTable and delete admin-row.css (this subsumes L3-2). Split AdminUserList into toolbar, table and confirm (L1-9; the toolbar split carries L3-1). One pager: glass Pagination when it ships, otherwise one `shared/Pager.vue`. | CONSUMER `AdminUserList.vue`, `AdminFlaggedPanel.vue`, `admin-row.css`; GLASS Pagination (existing ask) | F.W14U + glass letter (existing UIA-F-145) | Partial: UIA-F-108, UIA-F-145, UIA-F-194. The god-module split and the mixed idiom are new. |
| A2-FO-L1-28 | LOW | /visualize loading, /equation, /gallery grid, admin flagged | Six hand-rolled spinners (animate-spin rings) stand in for a glass busy primitive. | `VisualizationView.vue:236`; `EquationPanel.vue:106`; `equation/EquationView.vue:401`, `:429`; `gallery/GalleryInfiniteGrid.vue:50`; `gallery/AdminFlaggedPanel.vue:558` | glass DotRing is internal, so there is no public busy primitive. | When glass exports its busy primitive, all six sites adopt it (or glass Progress indeterminate where a bar fits). | GLASS DotRing publication; CONSUMER the 6 sites | cite (UIA-F-72, then F.W14U) | UIA-F-71, UIA-F-72. The census here is 6 sites. |

## Lens 2: mobile views (19 rows)

Matrix: 360x780, 390x844 and 430x932 portrait plus 844x390 landscape, light and dark, touch, DPR 2, with emulated safe-area insets. Metrics and frames under `audit-2/fourier-L2/`.

| ID | Sev | View | Finding | Evidence | Cause | Cure | Owner | Route | Covered by |
|---|---|---|---|---|---|---|---|---|---|
| A2-FO-L2-1 | HIGH | every route (app shell), all widths, both themes | With a bottom safe-area inset the whole document scrolls by that inset: 34px in portrait, 21px in landscape. A drag that chains out of `<main>` moves the app dock up under the status bar (top at y -26 at 390, -9 in landscape). At rest the bottom of the h-dvh shell sits in the home-indicator zone. | `web/src/style.css:106-108`; `App.vue:124` (h-dvh shell) vs `App.vue:67` comment; `fourier-L2/probe-safearea.json` (v390 docSH 878 vs 844, maxScrollY 34; l844 411 vs 390, 21; no inset: 844, 0); frames `fourier-L2/frames/probe-sa-v390_paper.jpg`, `probe-sa-l844_gallery.jpg`. Register re-measure: `fourier-register/confirm.json` L2-1 | body gets `padding-bottom: env(safe-area-inset-bottom)` but the app is an h-dvh overflow-hidden shell, so the document is vh + inset tall, contradicting App.vue's own premise of a zero document scroll range. | Delete the body padding. Put the inset inside the fixed-height shell (padding-block-end on the h-dvh shell, border-box) or only on bottom-anchored overlays, so the document scroll range stays 0. | CONSUMER `style.css:106-108`, `App.vue:124` | F.W14U | New. |
| A2-FO-L2-2 | HIGH | /w, /v Canvas tab + fullscreen, all widths, both themes | The bottom-anchored docks sit in the home-indicator zone at every phone size. The animation dock ends 16px inside a 34px inset (bottom 762 / 826 / 914 at 360 / 390 / 430; 372 of 390 in landscape, inset from 369). The fullscreen dock ends 22px inside. The stage's bottom edge is 6px from the viewport edge. | `VisualizationView.vue:600-606`, `:731-733`; `FullscreenViewer.vue:250-257` (in flight); `fourier-L2/metrics-v360.json`, `-v390`, `-v430`, `-l844` keys *-w-canvas, *-w-fullscreen; frames `fourier-L2/frames/v360-light-w-canvas.jpg`, `v360-dark-w-fullscreen.jpg`, `v390-light-w-anim-expanded.jpg`. Register re-measure confirms the portrait numbers | Nothing on `.controls-overlay`, the stage or `.fs-controls` reads env(safe-area-inset-bottom). glass GlassDock has no inset awareness; the only env(safe-area-*) in glass src is `deck/styles/stage.css:33-34`. | GLASS: bottom-placed docks and the Configurator stage overlay slot offset by max(own gap, env(safe-area-inset-bottom)) through one root token such as --safe-block-end. CONSUMER: `.controls-overlay` and `.fs-controls` use `bottom: calc(.75rem + env(safe-area-inset-bottom))`. | GLASS `src/components/dock` (placement) + CONSUMER `VisualizationView.vue:600-606`, `FullscreenViewer.vue:250-257` | Glass letter (new ask) + F.W14U | New. |
| A2-FO-L2-3 | HIGH | every route at 844x390 (inset L/R 47, B 21), both themes | The landscape notch side is ignored. Interactive content sits under the sensor housing: the Controls/Canvas tab at x=12 (/w, /equation), the gallery tabs and search at x=16, the canvas stage from x=5 with the "Epicycles" label near x=24, the paper page chip at x=16. The floating ToC bar, its dropdown and the search listbox are full-bleed [0..844]. | `web/index.html:5`; `fourier-L2/probe-safearea.json` l844 firstInteractiveLeft 12 / 16 / 66; metrics l844-light-paper-toc dropdown x0 w844; frames `fourier-L2/frames/probe-sa-l844_equation.jpg`, `l844-light-w-canvas.jpg`, `l844-light-paper-toc.jpg`. Register re-measure: firstLeft 12 | index.html sets viewport-fit=cover, but no fourier or glass rule reads safe-area-inset-left or -right. | Add `padding-inline: env(safe-area-inset-left) env(safe-area-inset-right)` on the `App.vue:124` shell and inset the full-bleed floating ToC and search plates the same way, or drop viewport-fit=cover. | CONSUMER `index.html:5`, `App.vue:124`, `components/paper/PaperToc.vue` (floating) | F.W14U | New (the earlier register never probed landscape). |
| A2-FO-L2-4 | HIGH | /w, /v, /equation at 844x390, both themes | Landscape keeps the portrait tab idiom. Chrome takes 128px of 390 (33%): app dock 72, tab row 56. The stage is 834x255; the canvas dock (y 136-196) and collapsed animation dock (y 312-372) cover about 120 of its rows, so the traced figure runs under the dock. The controls are a tab away. On /equation the equation card fills the band and the plot starts below the fold. | `VisualizationView.vue:74`, `:267-271`; `EquationView.vue:60`, `:347-351`; metrics l844-light-w-canvas; frames `fourier-L2/frames/l844-light-w-canvas.jpg`, `l844-light-w-topdock.jpg`, `l844-light-equation-canvas.jpg`. Register re-measure matches | The side-by-side Configurator layout is gated by width only: useMediaQuery('(min-width: 1024px)'). | Add a short-landscape arm, `(orientation: landscape) and (max-height: 500px)`, using the side-by-side grid (controls rail beside the stage) with no tab row and a compact app dock. On /equation place the plot beside the equation. | CONSUMER `VisualizationView.vue`, `EquationView.vue`, `AppDock.vue` | F.W14U | New. |
| A2-FO-L2-5 | MEDIUM | /w, /v, /equation mobile tab row, all widths | The mobile Controls/Canvas strip is 187px wide and pinned at x=12, aligned to neither the column's centre nor its gutter: its centre is -75 / -90 / -110 / -317 px from the viewport centre at 360 / 390 / 430 / landscape. The same wrapper markup is duplicated in the two views. | `VisualizationView.vue:267`; `EquationView.vue:347`; [role=tablist] cx in metrics *-w-canvas / *-w-default; frames `fourier-L2/frames/l844-light-equation.jpg`, `v360-light-w-canvas.jpg` | A `flex px-3 py-1 bg-background` wrapper places a content-width strip, copied in each view. | One shared mobile tab bar aligned to the column: full-width on the column's gutter, or centred on its axis. Drop the opaque band. | CONSUMER `VisualizationView.vue:267`, `EquationView.vue:347` | F.W14U | UIA-F-238 partial (opaque strip only). Related: L3-6 (gutters). |
| A2-FO-L2-6 | MEDIUM | every SegmentedTabs strip (/w and /equation tabs, /gallery tabs incl. admin, /equation display-mode toggle), all widths | glass SegmentedTabs strip buttons have no coarse-pointer floor. The underline arm is 42.5px tall; the pill arm is 32px tall (the Equation "Σ / a + b" toggle measures 50.5x32 portrait, 54.5x34 landscape), at every width and in both themes. | glass `src/components/tabs/SegmentedTabs.vue:306` (button.segmented-tab) vs `:219` (the Select trigger has data-control-target); `styles/segmented.css:368-369`; `src/styles/utilities/responsive.css:4-8`; frames `fourier-L2/frames/v360-light-equation-canvas.jpg`, `v390-light-gallery-admin-users.jpg` | The `.segmented-tab` button carries no data-control-target, though the component's mobile Select trigger does, so the shared coarse floor never applies. | Add data-control-target to the strip button, or a coarse-pointer `min-block-size: var(--touch-target)` in segmented.css. Fixes every consumer at the root. | GLASS `src/components/tabs/SegmentedTabs.vue:306`, `styles/segmented.css` | Glass letter (new ask) | New. |
| A2-FO-L2-7 | MEDIUM | /w, /v controls, equation panel Terms, /equation Harmonics and Display terms, /morph durations, all widths | Every slider measures a 12x24 thumb on a 24px track on touch, below AUDIT-2's 44px bar. | glass `src/styles/tokens/sizing.css:282-288`; `src/components/slider/styles.css:110-118`; role=slider 12x24 in metrics *-w-default, *-equation, *-morph; frames `fourier-L2/frames/v360-light-w-default.jpg`, `v390-light-morph.jpg` | glass sets the slider coarse floor to 24px on purpose (--slider-touch-target 1.5rem, WCAG 2.5.8), treating the track as the target. | Needs a ruling: raise the coarse floor to --touch-target with a block-axis ::after hit-slop (no paint change), or record the 24px exception against the 44px rule. | GLASS `src/styles/tokens/sizing.css:288`, `src/components/slider/styles.css:110-118` | Glass letter (ruling) | New. |
| A2-FO-L2-8 | BROKEN | /w Export modal at 844x390, both themes | In landscape the Export dialog is taller than the 390px viewport and cannot scroll, so its title and its Cancel/Download footer are unreachable. Seat: 516px tall from y -63. Register re-measure: 498px tall, y -54 to 444, scrollHeight = clientHeight 496, overflow-y visible. Portrait fits (650/780 at 360). | `ExportModal.vue:81` (in flight; bare `<DialogContent>` at HEAD `:49`); glass `src/components/dialog/DialogContent.vue:176-185`; metrics l844-*-w-export; frames `fourier-L2/frames/l844-light-w-export.jpg`, `l844-dark-w-export.jpg`, `fourier-register/frames/L2-8_l844_export.jpg` | ExportModal renders `<DialogContent>` without `scroll`, and glass caps block size only under that opt-in (`DialogContent.vue:183`). | GLASS: cap every DialogContent at `max-block-size: calc(100dvh - 2*gutter)` with overflow-y auto by default; an opt-in cap is easy to forget. CONSUMER: pass `scroll` now. | GLASS `src/components/dialog/DialogContent.vue:176-185` + CONSUMER `ExportModal.vue:81` | Glass letter (new ask) + F.W14U | New. Same file as L3-7. |
| A2-FO-L2-9 | MEDIUM | /gallery card modal, 360/390/430, both themes | The gallery card modal is full-bleed on phones ([0..360], [0..390], [0..430]), its 24px room corners on the viewport edges. Landscape is fine (448px, centred). | `gallery/GalleryCardModal.vue:99`; glass `src/components/dialog/styles.css:14-25`; frame `fourier-L2/frames/v390-light-gallery-card.jpg` | The consumer's `w-full max-w-[28rem]` on DialogContent overrides glass's gutter clamp `min(100% - 2*--space-section, 32rem)`. | Drop `w-full` (keep the 28rem max if wanted) so glass's gutter holds. | CONSUMER `GalleryCardModal.vue:99` | F.W14U | New (UIA-F-44 is the buried close, a different defect). |
| A2-FO-L2-10 | MEDIUM | /gallery admin Users rows + gallery card batch checkbox, all widths | On touch, the checkbox on every admin user row and every gallery card measures 16x16. | `gallery/AdminUserList.vue:536-541`; `GalleryCard.vue:95-99`; glass `src/components/checkbox/Checkbox.vue:29-38`; metrics *-gallery-admin-users, *-gallery-admin; frame `fourier-L2/frames/v430-dark-gallery-admin-users.jpg` | The consumer puts `h-4 w-4` on the glass Checkbox, which collapses its 44px control-bit seat. | Drop `h-4 w-4` and let control-bit size the seat. | CONSUMER `AdminUserList.vue:539`, `GalleryCard.vue:98` | F.W14U | UIA-F-191 partial (per-card box only; the user rows are new). |
| A2-FO-L2-11 | MEDIUM | /gallery admin Audit Log, 360/390/430 | The audit DataTable is 582px wide inside a 326-396px column. The Target and Hash columns sit behind a sideways pan and the action badges are cut mid-word. | `gallery/AdminAuditLog.vue:399-405`; glass `src/components/data-table/types.ts:84-90`, `composables/useDataTableResponsive.ts`; scrollersX 582/326, 583/356, 584/396 in metrics *-gallery-admin-audit; frame `fourier-L2/frames/v360-light-gallery-admin-audit.jpg` | glass DataTable ships a `responsive` card-per-row projection under 640px and the consumer does not enable it. | Pass `responsive` to the DataTable. | CONSUMER `AdminAuditLog.vue:399` | F.W14U | UIA-F-42, UIA-F-108 (predecessors; the 0px column became a pan). |
| A2-FO-L2-12 | HIGH | shell About + Login popovers, canvas View options, animation ⋮ menu, all widths | Floating plates still run into the viewport edges. About [24..360] and [54..390], flush right. Login [0..336] at 360, 390 and 430, flush left. View options [0..336]. The new ⋮ menu rises to y=46 over the app dock at 360 and to y=0 in landscape. | Plate boxes in metrics *-shell-about, *-shell-login, *-w-viewopts, *-w-more; frames `fourier-L2/frames/v360-light-shell-login.jpg`, `v390-light-shell-about.jpg`, `v360-light-w-viewopts.jpg`, `v360-light-w-more.jpg`, `l844-light-w-more.jpg`. Register re-measure: Login [0..336] at 360, About [54..390] at 390 | glass PopoverContent and DropdownMenuContent keep reka's collisionPadding of 0; only `SelectContent.vue:56` sets 16. | Default collisionPadding in glass PopoverContent and DropdownMenuContent to a viewport-pad token (to be minted; none exists in glass yet). | GLASS `src/components/popover/PopoverContent.vue`, `src/components/menu/DropdownMenuContent.vue` | cite (glass letter under UIA-F-56/F-219) | UIA-F-56, UIA-F-219: still RED at HEAD. The Login popover (the F-28 cure) and the ⋮ menu (in-flight X.F.W14U.vdock) are new instances of the same cause. |
| A2-FO-L2-13 | BROKEN | /w, /v Canvas tab + fullscreen, collapsed animation dock, all widths, both themes | The collapsed animation dock still spills its speed readout (scrollWidth 144 vs clientWidth 110 at 360, 390, 430; 147 vs 110 in landscape). The "×" paints outside the pill in both themes and in fullscreen. | animation-dock sw/cw in metrics *-w-canvas, *-w-topdock, *-w-fullscreen; frames `fourier-L2/frames/v360-light-w-canvas.jpg`, `l844-light-w-canvas.jpg`. Register re-measure matches | The glass summary layer is a fixed 40px circle (DOCK-COLLAPSED-FORM); the consumer half landed at b62821d. | Per UIA-F-6 / O-65: let the summary layer wrap a wider face. | GLASS `dock/styles/morph.css` (`.dock-layer--summary`) | cite (glass letter under O-65) | UIA-F-6, O-65 (still RED at 360, 430 and landscape). |
| A2-FO-L2-14 | MEDIUM | /gallery admin tab strip, 360/390/430 | The admin tab strip is 456px inside a 328/358/398px scroller. "Audit Log" is cut at every portrait width including 430; scrolled to Audit Log, the first tab is cut instead ("afts"). No scroll cue. | `GalleryView.vue:321-323`; scrollersX in metrics *-gallery-admin*; frames `fourier-L2/frames/v360-light-gallery-admin-audit.jpg`, `v430-dark-gallery-admin-users.jpg` | The page-level pan (F-41) was cured into an overflow-x-auto wrapper; the glass half (an overflow or compact mode for SegmentedTabs) is still open. | Per UIA-F-41: a glass SegmentedTabs overflow track (scroll-snap plus edge fade) or a compact mode. | GLASS `src/components/tabs/SegmentedTabs.vue` | cite (glass letter under UIA-F-41) | UIA-F-41, UIA-F-215. |
| A2-FO-L2-15 | HIGH | /w contour editor, expanded editor dock, 360/430 | The expanded editor dock still clips its tools: the layer is 432px wide inside 183 (360) or 253 (430), so more than half its 8 buttons (register correction; the seat said six of nine) sit behind an unmarked sideways scroll. Landscape fits. | scrollersX dock-layer--full 432/183, 432/253 in metrics *-w-editor-expanded; frames `fourier-L2/frames/v360-light-w-editor-expanded.jpg`, `v430-light-w-editor-expanded.jpg`. Register re-measure matches | Same as UIA-F-88: tool count against the dock cap. | Per UIA-F-88. | CONSUMER `EditorControlsDock.vue` (+ glass cue, UIA-F-215) | cite (F.W14U under UIA-F-88) | UIA-F-88 (probed at 390; still RED at 360 and 430). |
| A2-FO-L2-16 | HIGH | /paper floating ToC + mobile search at 844x390 | Landscape paper spends 153px of 390 (39%) on chrome: app dock 72, floating ToC bar 81. The ToC dropdown is a 234px plate that shows three rows and ends 3px from the bottom edge, inside the 21px inset. The search listbox is 229px tall, 8px from the edge. The page chip paints over the dropdown. | metrics l844-light-paper-toc (bar y72 h81; dropdown y153 h234), l844-light-paper-search (listbox y153 h229); `PaperView.vue:528-540`; frames `fourier-L2/frames/l844-light-paper-toc.jpg`, `l844-light-paper-search.jpg`. Register re-measure matches | Portrait-sized floating chrome with no rule for short heights. | On short landscape, shrink the floating bar to the title rung or fold it into the dock row; size the dropdown to 100dvh minus chrome minus inset; put the page chip under the plate's z. | CONSUMER `components/paper/PaperToc.vue` (floating), `PaperView.vue:528-540` | F.W14U | UIA-F-162, UIA-F-236 cover portrait at 390; landscape is new. See L1-1 (the floating tree is one of the three). |
| A2-FO-L2-17 | LOW | gallery card modal Like, /equation disclosure headers, About popover links, all widths | Consumer size overrides leave three controls under 44px: Like 82.5x34; the /equation "Function f(x)" and "Controls" disclosure triggers 43.1px; the About popover's @mbabb and "View project on GitHub" links 21-24px. | `GalleryCardModal.vue:178-186`, `:353-355`; `components/ui/CollapsibleSection.vue:87`; `AppDock.vue:95-110`; metrics *-gallery-card, *-equation, *-shell-about | Local literals: `.like-btn` min-height 1.5rem, py-1.5 on the glass CollapsibleTrigger, bare inline anchors. | Drop the literals and let glass Button and Trigger apply their coarse floors; give the About links a 44px row block-size. (L1-20 deletes CollapsibleSection, which removes the second site.) | CONSUMER `GalleryCardModal.vue:353`, `ui/CollapsibleSection.vue:87`, `layout/AppDock.vue:95-110` | F.W14U | New. |
| A2-FO-L2-18 | LOW | /w and /v controls, /equation, admin Users / Flagged / Audit, all widths | Text renders at 11px on phones: the Configurator layer subtitles (text-micro), the admin dt labels ("Entries", "Owner") and the audit hash cell, under a 12px phone floor. | type.minT in metrics *-w-default, *-equation, *-gallery-admin-users, *-gallery-admin-flagged, *-gallery-admin-audit (minFs 11) | glass text-micro is 11px with no coarse-pointer step, and the consumer uses it for body-adjacent labels. | GLASS: step text-micro to 12px or more under pointer: coarse. CONSUMER: use the caption rung for dt labels. | GLASS typography (text-micro) + CONSUMER `admin-row.css` | Glass letter (new ask) + F.W14U | UIA-F-206 partial (equation captions only). |
| A2-FO-L2-19 | LOW | /w expanded animation dock, all widths | The expanded animation dock layer overflows its own width by a constant 6px (266/260, 296/290, 336/330, 750/744), so the dock pans sideways under a thumb. The ⋮ trigger paints at 32x24; glass's ::after hit-slop covers the tap area. | `AnimationControls.vue:221-222` (dirty); scrollersX dock-layer--full in metrics *-w-anim-expanded; frame `fourier-L2/frames/v390-light-w-anim-expanded.jpg` | The width calc `min(max-width, 100dvw - 1rem)` does not account for the layer's content and gaps. Observed on the in-flight X.F.W14U.vdock tree. | Re-measure after vdock lands, then size the layer to its content or let it clip with the glass cue. | CONSUMER `AnimationControls.vue:221-222` | F.W14U (after vdock) | New. |

## Lens 3: hierarchy and space (15 rows)

Viewports 1440x900 and 390x844, light and dark. Metrics and frames under `audit-2/fourier-L3/`.

| ID | Sev | View | Finding | Evidence | Cause | Cure | Owner | Route | Covered by |
|---|---|---|---|---|---|---|---|---|---|
| A2-FO-L3-1 | HIGH | /gallery?admin, Users tab, 1440, both themes | At 1440 the admin Users toolbar breaks into three full-width rows: search 1408x36; the sort Select stretched to 1408x40 to show "Newest"; "Prune empty" (112px) alone on a third row. 128px of toolbar where one ~40px row holds all three. | frames `fourier-L3/frames/d-light-gallery-6-admin-users.png`, `d-dark-gallery-6-admin-users.png`; `fourier-L3/probe-admin.json` d-users; `AdminUserList.vue:382-383`, `:423`; glass `src/components/select/SelectTrigger.vue:59` (`w-full`; also dist `select-fy3vtArm.js:88` at 10.0.1). Register re-measure matches exactly | glass SelectTrigger defaults to width 100%. In the consumer's flex-wrap toolbar a 100%-basis item takes a whole line, pushing search and Prune onto lines of their own. The X.F.W14.u cure for UIA-F-37 (the 390 crush) left this desktop regression. | CONSUMER: SelectTrigger class `w-auto shrink-0 sm:w-40`; search stays flex-1 with Select and Prune trailing on the same row (Prune may move into an overflow per F-194). GLASS: an inline SelectTrigger arm (an `inline` prop, or w-auto outside a LabeledField) so toolbar Selects stop stretching. | CONSUMER `AdminUserList.vue:382-456` + GLASS `SelectTrigger.vue:59` | F.W14U + glass letter (new ask) | New. Adjacent: UIA-F-37 (its desktop regression), UIA-F-194. See L1-27. |
| A2-FO-L3-2 | HIGH | /gallery?admin, Flagged tab, 390, both themes | At 390 the Keep · Dismiss · Delete group on each flagged row runs 52px past the card, so Delete is clipped to "De". The group spans x 97-425 while row and card end at 373; it is nowrap, placed in the body column beside the 64px thumbnail. | frames `fourier-L3/frames/m-light-gallery-6-admin-flagged.png`, `m-dark-gallery-6-admin-flagged.png`, `m-light-probe-flagged.png`; `fourier-L3/probe-admin.json` m-flagged; `admin-row.css:131-151`; the comment at `AdminFlaggedPanel.vue:385-387` claims the group never overruns the row. Register re-measure matches exactly | The narrow container query (<36rem) puts the actions in grid area "lead actions", sharing the 1fr column beside the thumbnail (about 250px). With nowrap, three coarse 44px labelled buttons need 328px. | CONSUMER admin-row.css narrow arm: `grid-template-areas: "lead body" "actions actions"` so the group spans the row, plus flex-wrap: wrap as a floor; or icon-only buttons below 36rem with the verb in aria-label and a tooltip. (L1-27's DataTable move retires admin-row.css.) | CONSUMER `admin-row.css:139-151` (`AdminFlaggedPanel.vue:484-510`) | F.W14U (re-open UIA-F-110) | The UIA-F-110 cure regressed at 390. Re-open F-110 rather than file a duplicate. |
| A2-FO-L3-3 | MEDIUM | /gallery public tab, 1440 and 390, both themes | Chrome takes three stacked rows before content: the tab strip (44px); the search row (40px at 1440, where the 512px input leaves 896px of the row empty; 60px at 390); a lone "8 loaded" count (28 or 24px). The first card lands at y 225 (desktop) and y 242 (phone), so dock plus toolbar take 29% of an 844px screen. | frames `fourier-L3/frames/d-light-gallery-1-public.png`, `d-dark-…`, `m-light-…`, `m-dark-gallery-1-public.png`; `fourier-L3/probe2.json` d-gallery, m-gallery; `GalleryView.vue:313-338`; `gallery/GalleryInfiniteGrid.vue:31` | Tabs, search and count stack as separate rows (flex-col gap-1.5), and the search is capped at --search-measure inside a full-width row. | One toolbar row at >= sm: SegmentedTabs left, search + filter right (justify-between, search at max --search-measure). Put the count on the tab ("Gallery 8") or inline as muted meta and delete the count row. At 390 keep tabs then search, with no count row. | CONSUMER `GalleryView.vue:313-338`, `GalleryInfiniteGrid.vue:31` | F.W14U | UIA-F-247 lists "7 loaded" as chatter only. The three-row toolbar is new. |
| A2-FO-L3-4 | MEDIUM | /gallery public tab, 390, both themes | The phone gallery shows one card per row. Each card is 358x390 and the thumbnail about 205px, so a phone screen shows about 1.5 pieces. | frames `fourier-L3/frames/m-light-gallery-1-public.png`, `m-dark-gallery-1-public.png`; `probe2.json` m-gallery firstCard w358 h390, grid h3092 for 8 cards; `GalleryInfiniteGrid.vue:33` | `minmax(14rem, 1fr)` cannot fit two columns in 358px, so auto-fill collapses to one. | A phone arm of `repeat(2, minmax(0,1fr))` below about 30rem with a compact card (title plus one meta line, basis chips as glyphs), or `minmax(min(10rem,100%),1fr)`. | CONSUMER `GalleryInfiniteGrid.vue:33` (+ GalleryCard.vue compact arm) | F.W14U | New. UIA-F-98 does not set the column count. |
| A2-FO-L3-5 | MEDIUM | /w and /v workspace aside, 1440 and 390 Controls tab, both themes | The Visualize aside draws two rims around every section. Desktop has three nested edges: the Configurator plate, a glass Card wrapped around the aside, then the ConfiguratorLayers, which already paint a bordered, tinted, joined plate (22px spent on two rims). At 390 the Configurator plate (inset 4px) plus the layer rim (inset 13px) push content to x=34 each side, 17% of the width as frame. | crops `fourier-L3/crops/d-light-aside-rims.png`, `m-light-aside-rims.png`; frames `d-light-workspace-1-loaded.png`, `m-light-workspace-2-controls-tab.png` and dark twins; `probe2.json` d-workspace, m-workspace; `VisualizationView.vue:385`, `:453-482`; glass `src/components/configurator/styles.css:129-173` | The consumer wraps the aside in a surface (Card) when the glass layers already are the surface; on phone the glass-opaque plate and the layer plate stack for the same reason. | CONSUMER: drop the desktop Card wrapper so the layer stack is the only rim; on phone let the layer stack meet the plate edge. GLASS rider: a ConfiguratorLayer "flush" arm (no border or tint) for hosts that already paint a surface, plus a README line: layers are the surface; never wrap the aside in Card. | CONSUMER `VisualizationView.vue:385`, `:453-482` + GLASS `src/components/configurator/styles.css:129-173` (README) | F.W14U + glass letter (rider) | New. Adjacent: UIA-F-68, UIA-F-134. |
| A2-FO-L3-6 | MEDIUM | cross-route: /w, /visualize, /equation vs /gallery, /paper, /demo/shape-extractor, not-found; 1440 and 390 | The page gutter changes with the route, so the content edge jumps on every nav switch. Visualize and /equation: 8px at 1440, 4px at 390. Gallery and admin: 16/16. Paper: centred, 8px phone inset, max-w-5xl desktop. Shape extractor 16/32, not-found 16. Gallery to Visualize moves the edge 16 to 8 (desktop) and 16 to 4 (phone) while the dock keeps 16. | `fourier-L3/metrics.json` gut entries; `probe2.json` m-workspace, m-equation, m-gallery; frames `d-light-gallery-1-public.png` vs `d-light-workspace-1-loaded.png`, `m-light-gallery-1-public.png` vs `m-light-workspace-2-controls-tab.png`; `VisualizationView.vue:453-456`, `:480-482`; `equation/EquationView.vue:558-568`; `GalleryView.vue:313`; `paper/PaperView.vue` `.paper-layout`; `morph/FourierShapeExtractor.vue:252-257` | Each route root chooses its own inset literal; no shared page-gutter token exists. | CONSUMER: one --page-gutter (16px phone, 16-24px desktop) read by every route root and by the tool routes' Configurator margin. GLASS: if glass ships a layout-gutter token (or it comes with the O-68 layout slot), consume it. | CONSUMER `VisualizationView.vue:453-482`, `EquationView.vue:558-568`, `GalleryView.vue:313`, `FourierShapeExtractor.vue:252-257` (+ GLASS gutter token if shipped) | F.W14U (+ O-68) | Partly: UIA-F-237 (4px margin, /visualize empty at 390), UIA-F-211. The cross-route inconsistency is new. Related: L2-5. |
| A2-FO-L3-7 | MEDIUM | /w Export Frame dialog, 1440 and 390 (d-light, d-dark, m-dark captured) | The export options now use glass LabeledSwitch with layout="horizontal", a text-field form grid. At 1440 each switch floats mid-dialog in a start-aligned 1fr column with about 190px empty to its right, and the Background description wraps to three lines. At 390 or below (36rem) it stacks: six switches take twelve rows, each label above its switch, though a 44px switch fits beside every label. | frames `fourier-L3/frames/d-light-workspace-4-export-dialog.png`, `d-dark-…`, `m-dark-workspace-4-export-dialog.png`; `ExportModal.vue:94-100` (dirty: in-flight UIA-F-181 cure); glass `src/components/labeled-field/LabeledField.vue:121-134` | glass has one "horizontal" LabeledField geometry, built for text inputs; a switch gets a wide control column and collapses to stacked on phone. | GLASS: a LabeledSwitch settings-row arm (label + description in 1fr, switch auto, justify-self end, align center) that never collapses, as LabeledSwitch's default. CONSUMER: ExportModal adopts it (drop layout="horizontal"). | GLASS `LabeledField.vue:121-134` (`LabeledSwitch.vue`) + CONSUMER `ExportModal.vue:94-100` | Glass letter (new ask) + F.W14U | New. UIA-F-181 covered the pre-cure rows. Same file as L2-8. |
| A2-FO-L3-8 | MEDIUM | /w aside Decomposition + Contour layers, 1440 and 390, both themes | Still RED at HEAD. A single 40px Reset icon takes a whole 339x40 row atop the Decomposition layer (322x60 at 390), and the same in Contour. At 390 the section label truncates to "Decomposit..." while the decorative sub still shows. | `probe2.json` d-workspace, m-workspace reset rows; frames `d-light-workspace-1-loaded.png`, `m-light-workspace-2-controls-tab.png`; `BasisSelector.vue:137-150`; glass HEAD 2b72324a `src/components/configurator/ConfiguratorLayer.vue:96-190` (default slot only) | ConfiguratorLayer has no header #actions slot. | As registered: a GLASS #actions header slot plus label shrink priority; the CONSUMER moves the resets into it. | GLASS `ConfiguratorLayer.vue` + CONSUMER `BasisSelector.vue:137-150`, `ContourSettings.vue:227-229` | cite (UIA-F-134, UIA-F-238) | UIA-F-134 (GLASS), UIA-F-238. |
| A2-FO-L3-9 | LOW | /w aside basis toggles (1440 + 390); /equation Notation (390) | Still RED. Three one-of-N choices wrap into a centred 2+1 orphan: Epicycles and Chebyshev, then Legendre alone; the equation's Notation row (Trig and Exp, then Polar) at 390. | `probe2.json` d-workspace toggles y 547, 547, 589; m-workspace 586, 586, 636; frames `d-light-workspace-1-loaded.png`, `m-light-equation-2-bottom.png`; `BasisSelector.vue:152` | Hand-rolled Button rows with flex-wrap and justify-center. | As registered: a glass ToggleGroup or SegmentedTabs one-of-N that fits one row, or stays left-aligned when it wraps. | CONSUMER `BasisSelector.vue:152`, equation notation chooser | cite (UIA-F-238, F-85, F-204) | UIA-F-238, UIA-F-85, UIA-F-204. |
| A2-FO-L3-10 | LOW | /w aside Image layer, 1440 and 390 | Still RED. The open-by-default Image layer is 341px of an 819px aside at 1440 (42%) and 325px at 390, while Contour and Coefficients sit collapsed at the fold. On a cold /w load at 390 the Controls tab opens first, so the phone's first screen is the source photo. | `probe2.json` layers Image h341 of aside h819 (d), h325 (m); frames `d-light-workspace-1-loaded.png`, `m-light-workspace-1-loaded.png` | The layer's default-open state and the size of the preview. | As registered: a compact image row (64px thumbnail inline with Replace), and on phone the Canvas tab first after a load. | CONSUMER `ImageUpload.vue:64-110`, `VisualizationView.vue` (mobile default tab) | cite (UIA-F-169, F-74) | UIA-F-169, UIA-F-74. |
| A2-FO-L3-11 | MEDIUM | /visualize empty, 1440 and 390, both themes | Still RED. One 240x44 secondary "Drop or click to upload" pill and a caption sit in an empty 1424x810 stage: no heading, no lede, no primary. | frames `d-light-visualize-empty.png`, `m-light-visualize-empty.png`, `d-dark-visualize-empty.png`; `fourier-L3/metrics.json` d-light-visualize-empty (only the 18.6 and 14.4 rungs) | The empty state is not composed. | As registered. | CONSUMER `VisualizationView.vue:~314-321` | cite (UIA-F-69, F-165) | UIA-F-69, UIA-F-165. |
| A2-FO-L3-12 | MEDIUM | /gallery cards + card modal, 1440 and 390 | Still RED. The card's headline is the truncated mono image slug, weighted like the age. The fixture titles never appear on the card or in the modal. In the modal "3 views" and "0 likes" are set in two sizes (16.0 vs 14.0). | frames `d-light-gallery-1-public.png`, `d-light-gallery-3-card-modal.png`, `m-light-gallery-3-card-modal.png`; metrics d-light-gallery-3-card-modal | The card and modal bind image_slug and ignore title. | As registered: title ?? slug as the headline, slug and age as muted meta. | CONSUMER `GalleryCard.vue`, `GalleryCardModal.vue` | cite (UIA-F-99, F-247) | UIA-F-99, UIA-F-247. |
| A2-FO-L3-13 | MEDIUM | /gallery Drafts tab, 1440 and 390 | Still RED. A "My Drafts" collapsible repeats the tab name. At 1440 the single draft row spans 1408px, Publish about 1200px from the title it acts on, over a 658px dead band. | frames `d-light-gallery-4-drafts.png`, `d-dark-gallery-4-drafts.png`, `m-light-gallery-4-drafts.png`; metrics d-light-gallery-4-drafts bands [{y242, h658, tail}] | A list-row idiom where the Gallery uses cards. | As registered: Gallery cards with a Draft badge, no collapsible. | CONSUMER `gallery/GalleryDraftsSection.vue` | cite (UIA-F-189) | UIA-F-189. |
| A2-FO-L3-14 | LOW | not-found at 390; /equation Function card in dark | New sites of registered rows. At 390 the not-found card's button labels (21.0px) outrank its h1 "Page not found" (20.4px). In dark, the primary Compute on /equation recedes to a dark-brown slab near the card fill, while in light it is the heaviest element in the column. | metrics m-light-not-found; frame `m-light-not-found.png`; crop `fourier-L3/crops/eq-function-light-dark.png` (`FunctionInput.vue:183-186` emphasis="primary") | The glass coarse-pointer --ui-scale multiplies control text, and the Button emphasis ladder is too flat in dark. | As registered (GLASS). | GLASS button / ui-scale tokens | cite (glass letter under UIA-F-91, F-131) | UIA-F-91, UIA-F-131. New sites only: NotFoundCard.vue, the /equation Compute button. |
| A2-FO-L3-15 | LOW | /morph 1440 and 390; /paper 390; /demo/shape-extractor; /equation captions; /v dock | Still RED, citation only. /morph: the 180px stage is stranded and Export and Reset trail under the last card. Phone /paper: the inline chapter list owns the first screen and the "pg 4/110" chip overprints the last row. Shape extractor: no type rungs, about 55% of the desktop empty. /equation: 11-12px mono captions beside 25.9px layer headings. /v: the dock claims "Paper". | frames `d-light-morph-1-top.png`, `d-light-morph-2-bottom.png`, `m-light-morph-2-bottom.png`, `m-light-paper-1-top.png`, `d-light-shape-extractor.png`, `d-light-equation-1-top.png`, `d-light-v-saved.png` | Registered causes. | As registered. | CONSUMER (per the cited rows) | cite | UIA-F-115, UIA-F-254 (morph); UIA-F-162, UIA-F-234 (paper phone; see L1-1); UIA-F-211 (shape extractor); UIA-F-206 (equation type scale); UIA-F-119 (dock on /v/). |

## Routing

### F.W14U (consumer halves, 56 rows)

- **BROKEN / HIGH first:** L2-8 (pass `scroll` now), L1-1, L2-1, L2-2 (consumer half), L2-3, L2-4, L2-16, L3-1 (consumer half), L3-2 (re-open UIA-F-110), L2-15 (under UIA-F-88).
- **New MEDIUM:** L1-5, L1-8, L1-9, L1-10, L1-11, L1-12, L1-13, L1-24, L1-27, L2-5, L2-9, L2-10, L2-11, L3-3, L3-4, L3-5, L3-6.
- **Consumer halves of glass asks (adopt when glass lands):** L1-2, L1-4, L1-14, L1-16, L1-18, L2-18, L3-7.
- **New LOW:** L1-6, L1-17, L1-19, L1-21, L1-22, L1-23, L2-17, L2-19 (after vdock).
- **Cite-only (work under the earlier id):** L1-3, L1-7, L1-15, L1-20, L1-25, L1-26, L1-28, L3-8, L3-9, L3-10, L3-11, L3-12, L3-13, L3-15.

### Glass letter (glass halves, 25 rows)

- **New asks (15):** L1-2 TocTree component; L1-4 search engine publication ruling; L1-9 useConfirm composable (candidate); L1-14 compact Tooltip; L1-16 Slider scrub session; L1-17 DockAccount/SlugIdentity (candidate); L1-18 useScrollTo teleport arm; L2-2 bottom-dock safe-area token (--safe-block-end); L2-6 SegmentedTabs strip touch floor; L2-7 slider coarse-floor ruling (24px vs 44px); L2-8 DialogContent default block cap; L2-18 text-micro coarse step; L3-1 SelectTrigger inline arm; L3-5 ConfiguratorLayer flush arm + README; L3-7 LabeledSwitch settings-row arm.
- **Still RED under existing ids (10):** L1-15 (BL-FW14H-1/-2), L1-27 (Pagination, UIA-F-145), L1-28 (DotRing, UIA-F-72), L2-12 (collisionPadding, UIA-F-56/F-219), L2-13 (DOCK-COLLAPSED-FORM, O-65), L2-14 (SegmentedTabs overflow, UIA-F-41), L2-15 (scroll cue, UIA-F-215), L3-6 (gutter token with O-68), L3-8 (ConfiguratorLayer #actions, UIA-F-134), L3-14 (ui-scale and emphasis ladder, UIA-F-91/F-131).

## Views skipped (verbatim from the seats)

### Lens 1 seat

- /gallery admin tabs (users, flagged, audit) at runtime: they need an admin session; the seat is read-only and set up no admin auth, so these were audited from source only
- /gallery card modal, drafts tab, batch confirm dialogs at runtime: need fixtures, selection or a login; source only
- /visualize loaded workspace (canvas docks, contour editor, fullscreen, export modal, equation overlay) at runtime: needs an uploaded image; the seat aborts all non-GET API calls, so no upload was made; source only
- /v/:visualizationSlug at runtime: needs a saved slug; source only (it renders the same VisualizationView)
- /paper search palette and mobile floating-ToC open states at runtime: not driven by this Lens 1 probe; source only
- Lens 2 (mobile viewports 360/430/844x390, both themes) and Lens 3 (hierarchy and space): outside this seat's lens; the 390 frames under frames/ are counting probes, not Lens 2 evidence

### Lens 2 seat

- / root: a redirect to the saved tab (router/index.ts:44-46), not a view; its targets are covered
- /s/:slug: a redirect to /w/:slug (router/index.ts:120-123); /w/ is covered
- visualize save/publish flow and its success/error toasts: needs a session and mutating POST /api/visualizations, so it was not driven (the prior register's visualize-publish-flow covers 390)
- gallery Drafts with a real draft row: needs a session-owned draft (a write); only the empty state was captured
- logged-in account popover contents: the stubbed session only seeded storage; the account popover was not opened (dock form only)
- shell-loggedin light at 360 and 430: page.goto timed out under load; the dark twins at the same widths were captured
- The desktop paper search palette (PaperSearchModal) is lg-only and has no phone surface
- CAVEAT, not a skip: from about 17:06Z the shared :3100 served a mixed optimized-dep state (every route blank, pageerror "Cannot read properties of null (reading 'ce')" at renderSlot, two Vue runtime chunks after a lockfile-triggered re-optimize). Captures after 17:15Z (paper ToC/search, ⋮ menu, Export, View options, admin tabs, dark /morph, and the extra states) ran on a private read-only mirror at :3101 (same fourier tree and vite config, cacheDir in the scratchpad; stopped afterwards; nothing written under the product tree). The fourier tree was also moving: 798c98f with the X.F.W14U.vdock edits uncommitted (AnimationControls, BasisCanvas, CanvasControlsDock, EasingPicker, ExportModal, FullscreenViewer, SpeedSelect). Rows 12 (⋮ menu), 19 and the ExportModal line cite are on that in-flight tree; see tree-state.txt

### Lens 3 seat

- /gallery admin Audit Log, all 4 combos: frames captured but blank. The glass DataTable render throws 'Cannot read properties of null (reading ce)' in renderSlot (probe-audit.mjs stack). The cause is a stale Vite optimized-deps mix: the page loaded chunk-KQQYPJ7S.js?v=13e4a967, which no longer exists after deps re-optimized at 13:03 while a cure seat edited fourier web/src. This is a dev-server state fault, not a proven product defect. Re-frame after a Vite restart (not this seat's to do).
- /morph m-dark: the dynamic import of FourierMorphDemo.vue failed during the same dep re-optimization; lens covered by m-light and d-dark.
- /w Export dialog m-light: navigation timed out (dev server busy).
- Overlay and state sub-views not framed by this seat (the prior register holds them, and the visualize dock files AnimationControls, CanvasControlsDock, EasingPicker, SpeedSelect, FullscreenViewer and ExportModal were being edited live by a cure seat during capture; m-dark also raised 'DropdownMenu parts must be used within DropdownMenu' from that in-flight dock): fullscreen viewer, contour editor mode, equation Σ panel on /w, view-options popover, More-options menu, mobile Canvas tab with docks expanded, paper search palette, floating ToC, inline search, inline login dock, About popover, nav dropdown, toasts.
- /s/:slug redirect: no page of its own, redirect only.
- Landscape 844x390, 360 and 430 widths: Lens 2 viewports, outside this Lens-3 seat's 1440/390 brief.

### Register seat

- The register's confirm probe (2026-09-24T17:34Z) ran on the shared :3100 with 0 page errors, so the mixed-deps fault the L2 and L3 seats hit had cleared. It did not re-frame the L3 seat's blank Audit Log frames; the L2 seat's Audit Log frames (from the :3101 mirror) carry that view for L2-11 and L2-18.
- The fullscreen dock half of L2-2 was confirmed from source (`FullscreenViewer.vue:252`, `:257`), not re-measured.
