SERVED MODEL ID (fresh agglomeration seat, M-23, line-1 declaration): claude-fable-5[1m]

# X-WHOLE EXECUTION RUNBOOK — the runbook of record

**Provenance**: agglomerated 2026-08-30 from two independent drafts (`union/RUNBOOK-DRAFT-A.md`,
Fable · `union/RUNBOOK-DRAFT-B.md`, Opus) by a fresh seat that read both whole and resolved every
ordering/gate disagreement **at the spec bytes** — the specs govern, not either draft. The
resolution record is §7. **Authority**: `COHESION.md` §0f (all three loops closed; the union pass
is the sole remaining artifact) + §2 (the cross-sub-tranche graph) + `union/SEAMS.md` (24 seams ·
15 CONSISTENT · 9 CONVICTED) + `union/G-F-ADJUDICATION.md` (G-F MET WITH RESIDUALS).

**POSTURE — read before anything else.**

- **Status is `planned` for every wave, every gate, every cut, in all four sub-tranches.** This
  file moves no four-verb row and issues **zero VERIFIED stamps in current voice**.
- **This pass is planning. Nothing here opens product source.** Every command below is a
  read-only measurement over shipped bytes (L-19); none writes, none is a proof script.
- **E-3 governs**: this is an addendum-beside. No sibling spec, no COHESION section, no
  conformance artifact, no mail ledger was edited to produce it.
- **The owner's begin-word is the only execution key** — never inferred from a status word in any
  document, including this one.
- Sibling trees (`../keyframes.js`, `../fourier-analysis`, `../parse-that`, `../glass-ui`) are
  **READ-ONLY** until their own wave opens under its own authority. See §5.5.

**The operand set** (close states per `COHESION.md` §0a/§0c/§0e/§0f):

| sub-tranche | waves | close state |
|---|---|---|
| X·V (value.js) | `waves/W0..W11.md` (12) + `REFINEMENT-FOLD-2026-08-28.md` + `refinement/` | fold CONFORMANT pass 4 (`c88e1fed`) |
| X·KF (keyframes.js) | `keyframes/waves/KF-W0..KF-W10.md` (11; KF.W3 GATED) | CONFORMANT pass 8 (`3c4807d0`) |
| X·F (fourier-analysis) | `fourier/waves/F-W0..F-W10.md` (11) | CONFORMANT pass 15; canonical FROZEN `f44362757458` |
| X·P (parse-that) | `parse-that/waves/W0..W4.md` (5) | fold CONFORMANT pass 10 (`08232cd3`) |

**39 authored waves** sequence below. **KF.W11 · KF.W12 · KF.W13 are MINTED-UNAUTHORED**
(COHESION §0d; register of record `keyframes/waves/KF-W10.md` §6.D) — they take no verb from any
close and enter no order until the SS-1/SS-2 authoring block authors them (§1.2, §4.5).

---

## §1 THE ORDER

Derived from the specs' own sequencing declarations: X·V `REFINEMENT-FOLD-2026-08-28.md`
§READINESS R.1 (the fold-amended order); X·KF each wave's `Opens after`/`Hard ordering` line +
`KF-W4.md` §Sequencing (the declared head); X·F **eight of the eleven** `F-W*.md` files'
`Opens after` declarations + `F-W0.md` §6a locks (the other three are derived — provenance note
below); X·P each `W*.md` §State `Opens after`. Every edge carries its dependency reason.

**X·F provenance, restated truthfully ⟨union repair round 1⟩.** The earlier spelling — *"X·F each
`F-W*.md` §State `Opens after`"* — overstated a source that does not exist for three specs.
**Eight declare their own**: `F-W0.md:53` · `F-W1.md:7` · `F-W2.md:48` · `F-W3.md:100` ·
`F-W5.md:35` · `F-W7.md:83` · `F-W8.md:63` · **`F-W9.md:15`** — the last carried *inline on the
§State **Four-verb** line* rather than on a line of its own, which is exactly how this seat's
predecessor missed it and dropped the edge §1.3 now restores. **Three declare none**, and their
edges are derived from other specs' declarations — cited row by row so the derivation is
auditable rather than asserted:

| spec | carries no `Opens after` — its edges derive from |
|---|---|
| `F-W4.md` | `COHESION.md:74` (*"**F.W1 tri-package uplift precedes F.W3/F.W4**: no fourier frontend spec may assume the …"*) for the F.W1 edge; and its own `:33` PIN LAW, which **cites F-W1's** clause rather than declaring one — *"F.W1 is pinned `4→7(→8, G1-gated)` and opens after … **ESC-1** ruled (**G1**)"* — so F.W4's F.W0/ESC-1 dependency is inherited, not stated. |
| `F-W6.md` | its own §4 **In-wave order** `:537` — *"`a` preconditions (FW6-G19 receipt + **F.W5 clause set** + FW6-G18 rulings)"* — supplies the F.W5 edge; `:542` (*"**F.W0 substrate pre-gates FIRST**"*) supplies the F.W0 edge. Both are sequencing declarations; neither is an `Opens after` line. ⟨**DATED ADDENDUM-BESIDE — X-union CHECK-2 tail cure (c), 2026-09-17: THE `:537` NUMERAL IS STRUCK AS A COORDINATE; THE STABLE ANCHOR REPLACES IT. The derivation to the left is unchanged.** `:537` addressed a **blank line** as authored — ⟨cmd⟩ `sed -n '537p' fourier/waves/F-W6.md | wc -c` → **1**; ⟨cmd⟩ `git status --porcelain fourier/waves/F-W6.md` → **empty**, so this was a wrong anchor at authoring, not drift (CHECK-1 read `:536`, the repair wrote `:537`, the bytes are `:538`). Under **COHESION §0g.2**'s standing rule — *"no COHESION citation into a live sibling carries a line number — §-heading plus row/rule id only"* — the numeral is **not re-issued**; a fourth coordinate would repeat the defect X·F **E-11** and SEAMS **S-19** convict by name. **Replacement anchor**: `fourier/waves/F-W6.md` **`## §4 Sequencing — locks and cross-edges (declared from this end)`**, the **In-wave order** block — the serial line opening *"`a` preconditions (FW6-G19 receipt + F.W5 clause set + FW6-G18 rulings)"*. It resolves uniquely, which is the only receipt a stable anchor owes: ⟨cmd⟩ `grep -c '^## §4 Sequencing' fourier/waves/F-W6.md` → **1**; ⟨cmd⟩ `grep -c '^\*\*In-wave order\*\*' fourier/waves/F-W6.md` → **1**; ⟨cmd⟩ ``grep -c '`a` preconditions (FW6-G19 receipt' fourier/waves/F-W6.md`` → **1**. The companion `:542` anchor in this same cell resolved when measured and its quoted string is verbatim; it is **likewise struck as a coordinate** and re-anchored to the same §4 block's **Ordering and cure-integrity locks** item 1, *"F.W0 substrate pre-gates FIRST"* (⟨cmd⟩ `grep -c 'F.W0 substrate pre-gates FIRST' fourier/waves/F-W6.md` → **1** — unique). **Both derived edges — F.W5 and F.W0 — are unchanged**; no wave, gate, order or verdict moves.⟩ |
| `F-W10.md` | `F-W9.md:15`'s chain (F.W10 sits below F.W9); **RE-CUT-0** declared at both ends (`grep -c 'RE-CUT-0'` → F-W9 **4** · F-W10 **3**; `G-F10-6` ×**15** in F-W10); and its own `:7` §Scope (*"The F-side **terminal** wave"*), which is what fixes it as the track's last node. |

### §1.0 The four tracks are genuinely parallel

Each sub-tranche edits a **different repository**. The cross-sub-tranche graph is thin (§1.5).
Under the four-workflow cap (§5.1) the executor runs at most four sub-tranche heads concurrently,
and the four **track** heads may open together at the begin-word — nothing in any track gates
another track's head:

| track | head | repo | gated on | owner items due AT this opening |
|---|---|---|---|---|
| A · X·V | `X-W0` | `value.js` | begin-word only | G-9 disposition (the L1/L5 adoption leg, SEAMS S-4 — A/B/C); the S-11 glass-9.0.0 census widening; the SEAMS §8 cure batch if the owner directs it; the G-F R-1/R-2/R-3 bookings (§4.4) |
| B · X·KF | `KF.W0` ∥ `KF.W1` | `keyframes.js` (+ `keyframes-v-exec`) | begin-word; KF.W0's §B-12 act is **the owner's hand** | **OP-1 — kf write authority (owner-held; nothing kf-side dispatches until ruled)**; OP-6 (`smooth-step-3`) before KF.W4 unit `.e`'s G-14 arm |
| C · X·F | `F.W0` | `fourier-analysis` | begin-word only | OG-F1 (freeze-with-adoption vs re-root); G-15's rulings; G-10's KEEP/ISOLATE/DELETE breath; ESC-1/G1 (the F.W1 pin target) before F.W1 opens |
| D · X·P | `X.P.W0` | `parse-that` + the fresh root | begin-word only | OC-1 (bench bar) — RC-P conjunct 5's subject, rulable any time before X.P.W4 evaluates; the separate release word (X.P.W4 OP-1) |

**HEADS RE-DERIVED FROM THE SETTLED EDGES ⟨union repair round 1⟩.** The claim above is not taken
from the diagrams' left margins — a left-margin node is a drawing decision, and it is precisely
what went wrong in §1.3, where F.W9 sat at the margin with no in-edge while its own spec declared
four hard predecessors. Each track's head is therefore **re-derived as its in-degree-zero set**
over §1.1–§1.4's edge tables as repaired:

| track | root set (in-degree 0, computed) | count | note |
|---|---|---|---|
| A · X·V | `{X-W0}` | **1** | R.1's diagram makes every other wave a child of X-W0; the three surviving cross-wave write-order edges (W2→W5, W6⟂W7, W8→W5's C3 back-gate) add in-edges, never roots. |
| B · X·KF | `{KF.W0, KF.W1}` | **2** | KF-W1 §State `:35` makes W1 a **second** root (*"**Not** after X.KF.W0"*). **KF.W3 is not a third root**: it is gate-keyed and takes no ordering edge in either direction (§1.5 E-1) — it sits outside the ordered graph. The MINTED-UNAUTHORED three likewise take no verb and enter no order (§4.5). |
| C · X·F | `{F.W0}` | **1** | **Exactly one — and only after this round's repair.** As drawn before, F.W9 had no in-edge and the computed root set was `{F.W0, F.W9}`, which would have made this section's own head table wrong at Track C and let an executor open F.W9 beside F.W0. F-W9 `:15`'s chain is restored at §1.3 in both the diagram and the edge list. |
| D · X·P | `{X.P.W0}` | **1** | Strictly serial; every other wave names its predecessor's close. |

**Restated precisely: four track heads, five root waves.** The imprecision is Track B's, and the
table above already spells it — the X·KF head is the declared pair `KF.W0 ∥ KF.W1`, not one wave.
Under the four-workflow cap the executor opens four **tracks**; Track B's two roots share that
track's single slot (KF.W1 is 3 Opus serial, §5.1), so the cap is not strained by the fifth root.

**The "may open together" claim SURVIVES the re-derivation.** No root in any track carries an
in-edge from another track: §1.5's four cross-sub-tranche edges land on **KF.W3** (gate-keyed,
never scheduled), on **F.W5 ∥ the X·V API row** (declared mutually non-gating), and on **X-W9**
(not a head) — and the Glass-8 atomic cut is declared value.js-internal with each sibling's
non-edge stated so the absence is measurable. Five roots, zero cross-track in-edges.

### §1.1 Track A — X·V (`waves/W0..W11.md` as amended by §READINESS R.1)

```
X-W0  (sitting · registers · census · the §3.e escalation slate)   ── FIRST; gates everything
  ├─ X-W1  (harness: R2 dead-locator census + R3 oracle dialect BEFORE the HARD flip)
  ├─ X-W2  (boot cost) ──blocks──► X-W5 (shell composition; bindPane escalation resolved
  │                                 AT THE SITTING before open; PSC deletion + C1
  │                                 successor same-wave)
  ├─ X-W3  (api; route-spec-first law a.4; AdminGate seam contested with W7)
  ├─ X-W9  (library; epoch rule; widened R1 battery inside commit 1)
  ├─ X-W4  (dock/shell actions; A-1-before-A-3; .g cells HELD by W0.j's 0/4 trigger;
  │         MP-3 same-change guard)
  ├─ X-W6  (scenes/workbenches; opens against its fold)   ⟂ W7 on four shared paths
  ├─ X-W7  (palettes/admin; N-1 mount substrate = W1's plugin-vue install)
  ├─ X-W10 (canon; riders cite-don't-fork; receives AB-22 with its three riders)
  ├─ X-W8  (subtraction)                ── LAST of the demo waves
  └─ X-W11 (release + verified close)   ── LAST; consumes everything
```

| edge | reason (source: R.1/R.3) |
|---|---|
| → X-W0 first | The G-F register, the census and the §3.e escalation slate are minted here; W4.g's rows *"MUST have the G-F register to land in — which is why W0 runs first."* |
| X-W0 → X-W1 | The oracle slate (o11/o14/o20/…) is handed from W0's registers; the R2 dead-locator census + R3 oracle-dialect pass run **before** the HARD flip. |
| X-W2 → X-W5 | Boot cost is measured before shell composition rewrites it; **W2's `AFTER.json` is non-terminal until W5's A1/A2 land** — W2 does not stamp its artifact before W5. |
| X-W1 → X-W7 | W7's N-1 gate specifies mounted assertions on an absence (`@vue/test-utils` installed-unused; no `plugin-vue` in `vitest.config.ts`). **The mount substrate is W1's install**; six of W7's twenty gates are unrunnable before it. |
| X-W0.j → X-W4.g | The **one atomic trigger-gated cut**, X·V-internal (S-9/S-24). W0.j's four-condition Glass-8 census is the trigger; W4.g is the receiving surface (62 dispositions). FAIL branch (today: pin 7.0.0, census 1/4) → the rows join MX-CLUSTER. See §3.2. |
| X-W6 ⟂ X-W7 | Four shared paths; **W7's CrossEdges state the write order.** Genuinely concurrent otherwise. |
| X-W8 last of demo | *"it deletes what W4/W5/W6/W7 rewire."* One back-gate: **W5's C3 may not close while Dock G-L (an X-W8 probe) is RED** — the single place the subtraction wave back-gates a composition wave. |
| X-W11 last | Release + verified close; consumes everything, incl. 51 OUT-OF-WAVE census rows handed to owning folds, zero silent drops. |

**Parallel inside track A**: W1 · W2 · W3 · W9 · W4 · W6 · W10 are independent once W0 closes,
subject to the four-workflow cap and the W6⟂W7 write-order.

### §1.2 Track B — X·KF (`keyframes/waves/KF-W0..KF-W10.md`)

```
KF.W0  Substrate Settle (§B-12)   ∥   KF.W1  Mail Cure (mints O-21)   ── both open at begin-word
   │  (the §B-12 act is THE OWNER'S HAND —    │  (deliberately completable WITHOUT §B-12;
   │   value.js must NOT perform the reset)   │   the exec-clone path exists so the mail cure
   ▼                                          │   never queues behind the owner's hand)
KF.W4  K-Quartet + Gate Chassis  ◄────────────┘  ── THE DECLARED SEQUENCING HEAD
   │      ("No repair packet and no UNIT may open before G-KFW4-1 lands" — KF-W4
   │       §Sequencing, stated independently by EIGHT banked records)
   ├─ KF.W2  Parse Façade        ── opening commit states its ref; publishes both
   │                                registries before any cure commit
   ├─ KF.W5  Library Tri-Fold    ── S-0: G-XSS lands as commit 1, before any other byte;
   │                                KAD-1 = "the program's only unconditional sequencing
   │                                head" — FRONT-LOADED, the fix blocks nothing
   ├─ KF.W6  Glass Suffusion     ── ∥-atomic with KF.W4 on scripts/observe/demo/usability.mjs
   ├─ KF.W7  Timeline Evaluate   ── G14 resolves against KF.W2's posture registry
   └─ KF.W8  Structure/Colocation── after W0 · W4 · W5, + KF.W6 SCOPED for G7's
             measurement ONLY (the R4-1 atomic commit precedes G7's reading; W6 is
             NOT a general predecessor — KF-W8 §State)
KF.W9  Safari Visual Audit       ── after KF.W0 §B-12 ONLY (OP-6: KF.W4 is NOT a
       precondition — "this wave measures, it does not type-cure"); PRECEDES KF.W10
       at six enumerated anchors A1..A6
KF.W10 Fold Discharge + Close    ── LAST. Opens after KF.W0 · KF.W1 (O-21 the vehicle,
       named by id) · KF.W9/SS-13 · AND KF.W2·W4·W5·W6·W7·W8·W9 IMPLEMENTED
       (KF-W10.md §State :22); stamps the AUTHORED ELEVEN only — the minted three
       take no verb from this close
KF.W3  Parser Consumption        ── GATE-KEYED, NEVER SCHEDULED (§1.5 E-1)
KF.W11 · KF.W12 · KF.W13         ── MINTED-UNAUTHORED; authored by the SS-1/SS-2 block
       before any packet executes; cargo = the 17-packet partition 9+6+2 with the six
       travelling locks; all 17 sequence after G-KFW4-1; the drag-seam and
       transport/ribbon packets sequence AFTER KF.W7's per-surface SWAP verdict
       (the KF-AV-28 standing rider)
```

| edge | reason (source) |
|---|---|
| KF.W0 ∥ KF.W1 | KF-W1 §State `:35`: *"**Opens after**: the owner's begin-word. **Not** after X.KF.W0 … deliberately **completable without §B-12**."* KF-W0's spec claims the reverse edge; **the conflict is RECORDED at KF-W1 §7 cross-edge 2, not silently resolved** — the operator reads it as permission (W1 may start early), never as licence to skip W0's re-anchor for W1's own anchors. W1 anchors on `origin/master` either way (OP-4). |
| KF.W0 → KF.W4 | KF-W0 §Sequencing: KF.W4 *"may not open before this re-anchor"* — G-0.7's roster is KF.W4's precondition. Every anchor re-resolves at `origin/master 81a56990`; local `8281638c` DISQUALIFIED. |
| KF.W4 = head | KF-W4 §Sequencing: *"No repair packet and no UNIT may open before **G-KFW4-1** lands"* — eight banked records state it independently (*"nothing in the template layer is verifiable until a `.vue` file can fail a build"*). |
| KF.W0 → KF.W2 | KF-W2 hard order 1: the wave's opening commit **states its ref** (`origin/master 81a56990` or later); no anchor is trusted before it. |
| KF.W2 → KF.W7 | KF-W2 hard order 2: *"KF.W7 may not decide a failure posture before KF.W2 publishes the registry"* — declared at both ends; KF-W7's G14 resolves against that registry. |
| KF.W4 → KF.W6 | KF-W6 §Sequencing: the vue-tsc gate precedes verification of every typed cure. Plus the **atomic bundle**: `scripts/observe/demo/usability.mjs` is *"written ONLY inside the KF.W4∥KF.W6 atomic bundle (one commit, both seats); never unilaterally, never concurrently"* (KF-W6 `:121`/`:568`). In-wave, KF-AT-4 ∥ KF-AT-3 land together (oracle + grapheme — fixing either alone re-creates the by-construction pass). |
| KF.W6 → KF.W8, SCOPED | KF-W8 §State `:38`: **"KF.W6 — SCOPED: *for G7's measurement only*"** — W6's R4-1 atomic commit (delete `EditorHeader.vue` + strike the barrel line + strike the `layout.css` orphan token, ONE commit) precedes W8's G7 reading; *"W6 is not a general predecessor of this wave and no other row here waits on it."* G7 performs no barrel edit — written by KF.W6 alone, measured by W8. The MOVE-vs-RESHELL guard: `CopyButton.vue`'s MOVE is W8's; W6's reshell modifies in place; the two acts never share a commit. |
| KF.W4 commit 2 → KF.W8 | Three W8 gates (G10 leg (a), G11, G12) wait on W4's commit 2 (the plugin-vue registration in `vitest.config.ts`) — KF-W4 §Sequencing `:270`, declared at both ends; W8's residual manifest act (`+@vue/test-utils`) is its own. |
| KF.W0 → KF.W9 | KF-W9 §State `:20`: KF.W0 §B-12 names the execution substrate (OP-1 blocking; OP-3 safaridriver · OP-5 fresh build at the pinned ref · OP-4 per-cell capability record). **OP-6: KF.W4 is not a precondition** — after RULINGS R-9a every cure arm has LEFT the wave, so the head lawfully does not bind a pure measurement wave. KF.W9 runs concurrent with the whole W4 fan-out. |
| KF.W9 → KF.W10 | KF-W9 S-10: **PRECEDES; W10 DEPENDS** — six enumerated anchors (A1 §State · A2 OP-4 · A3 PACKET-FIRST · A4 §D cross-edge · A5 §B-1 · A6 §B-3). |
| KF.W0/W1 → KF.W10 | KF-W0's C-17 MINT precedes W10's CARRY-FORWARD block (*"W0 names; W10 rules. W10 consumes; it never mints"*); KF.W1's outbound row **O-21** is the vehicle W10 names by id (§END KF-W10 D-8). |

**Parallel inside track B**: after G-KFW4-1 lands, W2 · W5 · W6 · W7 are concurrent subject to
the W2→W7-posture edge and the W4∥W6 atomic bundle; KF.W9 is concurrent with the whole fan-out.

### §1.3 Track C — X·F (`fourier/waves/F-W0..F-W10.md`)

```
F.W0  Re-ground the substrate  ── FIRST, unconditionally; the pre-gate wave
   │   internal order: G-4 → G-5 → the web/dist quarantine
   │                   G-1 → G-11 → G-10's lift (lift-then-delete, ONE commit)
   │                   G-3 → the P-1/P-6 SENDs
   │   G-6 is authoring+measurement ONLY (the declare+lock landing is F.W1's);
   │   no git stash anywhere
   ├──────────────────────────► F.W5  Shared-provenance contract (ADMISSION KEYSTONE;
   │                              NOT gated on F.W1 — runs beside it; owner rulings
   │                              FLAGGED INLINE, never presumed)
   │                                ├─ F.W6  provenance defect burn-down (consumes the
   │                                │        F.W5 clause set + FW6-G19 receipt + G18)
   │                                ├─ F.W7  trie greenfield ⊙ OWNER-GATED on G-F7-1
   │                                │        (also HARD on F.W0; F.W5 §2 E16 · E1 ·
   │                                │        E3⊕G4 · E10 are hard predecessors;
   │                                │        "If F.W0 fails, the wave HALTS")
   │                                └─ F.W8  CRUD union prototype (also after
   │                                         F.W1.close AND F.W2)
   ▼
F.W1  ONE ATOMIC LAND-OR-LOSE TRANSACTION (TWELVE limbs)  ⊙ ESC-1/G1 OWNER-GATED
   │   opens after F.W0 closes (G2 · G3 · MISS-LC2 · F8-REACH-01+02) AND ESC-1 ruled;
   │   WU-A RE-PIN is the first obligation — "nothing below sizes before it"
   ├─ F.W2  value.js consumption (F.W1 landed for the SPECIFIER ARM ONLY; the colours
   │        arm is NOT so gated — fr-App C-2: the colors.ts cure is landable at 0.13.0
   │        today, alone)
   ├─ F.W3  Shadow retirement + glass suffusion   ── COHESION §2 law: after F.W1
   │        ⊕ PARTIAL — "except the §C.H rows expressly available at the installed
   │          pin, which need F.W0 only" (F-W3 §State :100; K-7/K-12 refuted their
   │          version framing). The track's SECOND partial-parallel edge, honoured
   │          exactly like F.W2's specifier/colours split
   ├─ F.W4  Saturated frontend audit              ── COHESION §2 law: after F.W1
   │
   └─ F.W3 ∧ F.W4 ──►  F.W9  Deploy spine · seats+harness · evidence
                         ── NOT A ROOT. F-W9 §State :15 declares the WHOLE chain:
                            "Opens after: F.W0 (substrate pre-gates, hard) → F.W1
                            (atomic) → F.W3/W4 (the cures this wave gates)" —
                            FOUR hard predecessors. F.W9 is the DEEPEST node in
                            Track C and may NEVER open beside F.W0 at the
                            begin-word. ⟨Drawn as a root in the 08-30
                            agglomeration; chain restored at union repair
                            round 1 — §1.0's Track-C root set is `{F.W0}`.⟩
                              │
                              ▼
                       F.W10  Coordination discharge + close  ── TERMINAL
                         ── F-W10 declares no `Opens after`; its position derives
                            from F-W9 :15's chain, RE-CUT-0 at both ends, and its
                            own :7 §Scope ("The F-side terminal wave").
                            RE-CUT-0 (G-F9-21 ≡ G-F10-6) precedes both stamps; any
                            change to the shared §2.2 bytes is a PAIRED EDIT in ONE
                            commit; "F.W10 does not author F.W9"; F.W10 reconciles
                            the boundary AGAINST the dispatched O-20 packet — "it
                            does not re-send it"
```

| edge | reason (source) |
|---|---|
| F.W0 first, unconditionally | F-W0 §6a lock 1 (COHESION §0a verbatim): *"F.W0 substrate pre-gates FIRST. **G-4 · G-1 · G-13 · G-12 precede F.W1 SIZING.**"* The bank's *"F.W1 runs a build anyway"* clause is STRUCK: *"F.W1 would open on a red build, and no uplift delta computed against a sheet that does not parse is trustworthy."* Restated at F-W2 §6a, F-W4 §5.1.1, F-W6 §4.1, F-W9 §4a.2, F-W10 §4a.2. |
| F.W0 internal: G-4 → G-5 → quarantine | *"No build can run while the stylesheet does not parse; `web/dist` is not touched until G-4 and G-5 are green — delete-before-rebuild destroys the only emission evidence the corpus has."* |
| F.W0 internal: G-1 → G-11 → G-10 | *"Anchors cannot be re-resolved against a tree no commit holds; the 8-site lift is applied at re-resolved anchors, never at the record's raw line numbers."* FR-COB-17's 8-site aria-pressed lift homes at W3/W4 but is SEQUENCED WITH F.W0's `git rm` — same commit. |
| F.W0 internal: G-3 → the SENDs | The P-1/P-6 packets name *"whoever holds the mail seat"*; the seat is created here, so no letter leaves before G-3. |
| **FR-NP-32 before every dist-dependent witness** | 11 of 11 F-W* specs carry it. The corrupt glass 4.0.0 `dist/styles/index.css` reddens every emitted-CSS readback; the SS-13 residues are *"BLOCKED, not merely unrun … gated on G-4/G-5 and must not be queued as independent visual work"* (F-W0 §6b). Citation form: **FR-NP-32 (≡ fr-PaperSidebar M1) — cite both, never substitute** (SEAMS S-23). |
| F.W0 → F.W1 | F-W1 §State `:7`: *"Opens after: F.W0 closes (G2 · G3 · MISS-LC2 · F8-REACH-01+02) **and** the owner's begin-word **and** ESC-1 ruled (G1)."* F.W1 reads F.W0's published results, never contains its acts. |
| F.W1 atomic | F-W1 §4 step 4: **ONE commit, TWELVE limbs**. **The charter, quoted at the spec's own bytes** ⟨`F-W1.md:5`, the `**Name**` masthead — cured at union repair round 1⟩: *"glass-ui **`4→7(→8, G1-gated)`** ∧ keyframes 4.3→6 ∧ value.js 0.13→4.0, ONE land-or-lose transaction, + the `lucide-vue-next`→`@lucide/vue` rename + the pencil-boil floor correction"* (plus the FR-EQC-7 vaul-vue gate and the remaining limbs) — *"cited whole and never restated"* (ten of eleven specs cite it in one spelling, SEAMS S-22). *"A partial landing is the land-or-lose failure the L-18 base forbids."* **⊘ The 9.0.0 branch is NOT in that quotation and must not be written into it.** The prior spelling `4→7(→8/9, G1/ESC-1 owner-gated)` rendered this runbook's own S-11 extension inside a citation of spec bytes that do not carry it: `grep -c '9\.0\.0' fourier/waves/F-W1.md` → **0**, and `grep -rl '9\.0\.0' waves/ keyframes/waves/ fourier/waves/ parse-that/waves/ COHESION.md` → **no output — no X spec anywhere contains the string**. **The runbook's own owed-addendum note, stated beside the quotation and never inside it**: S-11's 9.0.0 ACK widens X·F's election set, and **§3.2 already books it** — *"X·F elects its own target at **ESC-1/G1**, whose PIN-LAW gains the tag-pinned-vs-registry-pinned clause (S-11 cure) so an owner electing 9.0.0 knows it elects a tag"* — as an **addendum-beside owed at F-W1's PIN-LAW** (§3.1 seam 6, owning end `F-W1 PIN-LAW`), not as a fact already in the spec. Writing `/9` into the quote pre-empts the very E-3 addendum §3.2 says is required. ⟨**DATED ADDENDUM-BESIDE — X-union CHECK-2 tail cure (a), 2026-09-17: THE TWO RECEIPTS ARE RESTATED AT THEIR TENSE. The ⊘ and every verdict in this row STAND UNALTERED.** The two greps above were run at this file's own write moment (mtime 2026-08-30 04:34), **before** the same repair round's later writes (`fourier/waves/F-W1.md` 04:37 · `COHESION.md` 04:39) — same-round write ordering, not fabrication. They are **true-as-of-04:34 and false now**, and a universal absence claim is the X·KF loop's own named ban, so they are re-measured rather than left standing. **Re-run at the settled bytes, 2026-09-17, from `docs/tranches/X/`**: ⟨cmd⟩ `grep -c '9\.0\.0' fourier/waves/F-W1.md` → **2** (was 0); ⟨cmd⟩ `grep -rl '9\.0\.0' waves/ keyframes/waves/ fourier/waves/ parse-that/waves/ COHESION.md` → **four files** — `waves/W0.md` · `fourier/waves/F-W1.md` · **`fourier/waves/F-W0.md`** · `COHESION.md` (was: no output). *Self-disclosure, the same discipline applied to this seat:* the **fourth** file is created by **this round's own cure (b)**, the FR-NP-32 producer-side terminus written into `F-W0.md` minutes before this receipt was measured — it is named here rather than left to falsify the receipt one round later, which is precisely the failure mode being cured. **The cure these receipts were offered to support is independently true and is unmoved**: ⟨cmd⟩ `sed -n '5p' fourier/waves/F-W1.md` → *"**Name**: F.W1 — glass-ui 4→7(→8, G1-gated) ∧ keyframes 4.3→6 ∧ value.js 0.13→4.0, ONE land-or-lose transaction, + the `lucide-vue-next`→`@lucide/vue` rename + the pencil-boil floor correction"* — **character-identical to the quotation above, with no `9` in it**, so the ⊘ is earned by the charter's bytes and never rested on the absence claim. **Second limb, restated at the same tense**: read *"not as a fact already in the spec"* as **the 2026-08-30 state**. S-11's PIN-LAW cure has since **LANDED at its owning end** — `fourier/waves/F-W1.md` §3, the **M-16 / FR-EQC-16 / K-17** PIN-LAW row, now carries the tag-pinned-vs-registry-pinned clause in F.W1's own voice (*"as written, PIN-LAW is **unsatisfiable against 9.0.0**"*, widening ESC-1's option set to *"7.0.0 (census) · 8.0.0 … · **9.0.0 (tag-pinned, registry-absent, `./search` removed whole)**"*), which is both 9.0.0 hits the re-run returns. So at 2026-09-17 bytes the §3.1 seam-6 addendum is **DISCHARGED at the F-W1 end**, while the ⊘ on the F.W1-**charter quotation** stands untouched — the clause lives beside the charter, never inside it, which is exactly the shape §3.2 required. No edge, gate, order, arithmetic figure or verdict moves.⟩ |
| F.W5 G11 → F.W1 sizing | fr-AdminFlaggedPanel ruling 5 (F-W5 `:176`): *"the F.W5-W8 producer-or-retire decision must precede F.W1's sizing where grades are load-bearing"* — **a back-edge into the atomic wave; the operator schedules the G11 ruling before F.W1 sizes.** |
| F.W1 → F.W2, partially | F-W2 §State `:48`: *"F.W1 landed **for the specifier arm only** (the colours arm is *not* so gated, fr-App C-2)"* — a genuine partial-parallel edge, honoured. C·D-11's backward pin makes F.W2's specifier arm un-landable alone before W1. |
| F.W1 → F.W3 / F.W4 | `COHESION.md:74`: *"**F.W1 tri-package uplift precedes F.W3/F.W4** — no fourier frontend spec may assume the uplifted tree before W1 lands it."* **F.W3's edge is PARTIAL, as F.W2's is** ⟨restored at union repair round 1⟩: F-W3 §State `:100` reads *"Opens after: F.W0 (substrate: the GAB-13 dirty-set settle, the **`FR-NP-32` (≡ `fr-PaperSidebar M1`)** corrupt-dist settle …) and F.W1 … — **except the §C.H rows expressly available at the installed pin, which need F.W0 only** (K-7/K-12 refuted their version framing)."* The operator schedules the §C.H rows behind F.W0 alone; everything else in F.W3 waits on F.W1. F.W4 carries no such exception and is gated whole. |
| F.W0 → F.W5 (NOT F.W1) | F-W5 §State `:35`: *"Opens after: **F.W0** (substrate pre-gates — HARD, §4 X-1) · the intake adjudication · the adjudicated fourier registry whole (66/66)."* **The ADMISSION KEYSTONE is not downstream of the uplift** — it runs beside F.W1. SS-4: twice-authored; neither side's edits gate the other's waves; TA-4 (value-side atomdiff restoration) is a named prerequisite or the contract re-scopes explicitly. |
| F.W5 → F.W6 / F.W7 / F.W8 | F-W6 order `a` consumes the F.W5 clause set; F-W7 §State `:83` names F.W0 (hard, G-F7-7) · F.W5 §2 clauses E16 · E1 · E3⊕G4 · E10 · the owner's G-F7-1 ruling; F-W8 §State `:63` names F.W0 · **F.W1.close** · **F.W2** · F.W5's three artifacts. |
| **F.W0 → F.W1 → F.W3/W4 → F.W9** ⟨**RESTORED, union repair round 1**⟩ | **The dropped edge, and the reason it mattered.** `F-W9.md:15` (§State, on the **Four-verb** line): *"**Opens after**: F.W0 (substrate pre-gates, hard) → F.W1 (atomic) → F.W3/W4 (the cures this wave gates)."* **Four hard predecessors, drawn as an arrow chain, not a set** — F.W9 gates the cures F.W3/W4 land, so it reads them after they land. Every prior draft and the 08-30 agglomeration drew F.W9 at Track C's left margin with **no in-edge and no row in this table**, which — beside §1.0's *"the four heads may open together"* — licensed an executor to open F.W9 at the begin-word beside F.W0, against a tree with no re-grounded substrate, no uplift and no cures. It is the class §1's own preamble commits to sourcing. **All four in-edges now hold in the diagram and here.** Cross-checks at the bytes: **`F-W9.md:218`** — *"**F.W9 owns the checkpoint, never the cure** — every item below is minted by **F.W1's** transaction (cited whole at §4a-3) or cured at F.W3/W4"* (the same sentence stands in the twin at `F-W10.md:107`, the §2.2 spliced region — cite the F-W9 home when the claim is about F.W9's own predecessors). And F.W9's `Agents` declaration, carried on that same `:15` §State line, is **3 serial, never parallel** — units 2 and 3 both write `web/package.json` and `.github/workflows/ci.yml` — so the wave admits no internal fan-out that could compensate for an early open. |
| F.W9 ↔ F.W10 | RE-CUT-0 (G-F9-21 ≡ G-F10-6) precedes both stamps; shared-§2.2 changes are a paired edit in one commit. F.W9's G-F9-1 owns the unit-test FLOOR; F.W0's G-9 owned only the SEAT (*"the seat is a precondition of the floor, never a down payment on it"*). F.W10 is the terminal disposition of every deferred row. **F.W10 declares no `Opens after`** ⟨repair round 1⟩ — its position is derived, not declared: F-W9 `:15`'s chain places F.W9 above it, RE-CUT-0 is stated at both ends (`grep -c 'RE-CUT-0'` → F-W9 **4** · F-W10 **3**), and F-W10 `:7` §Scope fixes it as *"The F-side **terminal** wave"*. Since F.W10 sits below F.W9, **F.W9's four restored predecessors are transitively F.W10's** — F.W10 was never a root either, and nothing in this track opens at the begin-word but F.W0. |

### §1.4 Track D — X·P (`parse-that/waves/W0..W4.md`) — strictly serial

```
X.P.W0 ─► X.P.W1 ─► X.P.W2 ─► X.P.W3 ─► X.P.W4
```

| edge | reason (each §State `Opens after` line) |
|---|---|
| → W0 | *"tranche X opens (X ratified, SCOPE M-22 ¶1) AND the owner's begin-word for the X·P lane."* The fresh root opens at X.P.W0's FIRST WRITE; G-1/G-2/G-3 authenticate the pause state before any parser work is lawful. |
| W0 → W1 | *"X.P.W0 **closed** (the fresh root exists at the ruled commit and the frozen roots are proven byte-unchanged)."* |
| W1 → W2 | *"X.P.W0 closed GREEN … AND X.P.W1 IMPLEMENTED (the three instruments landed: `harness/totality/**` · `harness/equivalence/**` · `harness/bench/**`, plus the bar ledger)."* |
| W2 → W3 | W3 names W0 + W1 + W2; *"the dependency is on the conditions, not the labels"* — §2b enumerates each as a checked open-precondition. |
| W3 → W4 | *"X.P.W3 IMPLEMENTED (the 52-export universe TOTAL, the R1 throw class dead, the equivalence floor held, the divergence ledger written, the three-leg table published)"* + W2's Wasm artifact + W0's fresh root. |

**X.P.W4 is the single site in the X·P lane that stamps VERIFIED** (its `.d` fresh-Fable
adjudicator seat). Nothing downstream of X·P is scheduled by that stamp — see §1.5 E-1.

### §1.5 The cross-sub-tranche edges — the only four (COHESION §2), plus one declared non-edge

**E-1 · PLAW-BIND: X.P.W4's `RC-P(V)` → KF.W3. GATE-KEYED, NEVER SCHEDULED.** `RC-P(V)` is TRUE
iff all six conjuncts (PUBLISHED · TOTALITY · EQUIVALENCE · ADMITTED · BAR-DISCHARGED · ROUTED)
evaluate TRUE **for the same version `V` of `@mkbabb/value.js`**, *"evaluated by running the six
commands against the registry coordinate `V` — never by reading a status word in any document,
including this one"* (X.P.W4 §6a). All six were FALSE at the dated measurement. The routing is
parser → value (X·V L1/L5 surfaces) → packed release → consumers; **direct parse-that→fourier is
FORBIDDEN** (standing routing law, declared at COHESION §2 and as a NON-EDGE at F-W0). KF.W3's
repin literal is `4.0.0→V` per KF-W2's D-8 ruling (S-5's cure sweeps the surviving `4.1.x`
literals first). **The operator never schedules KF.W3; it opens or it does not.**

**E-2 · The chain's middle link is UNHOMED — an owner decision, not a wave.** No X·V wave adopts
a parser: `waves/W9.md` §5.g declines it and G31's own falsifier is *"adopt a parser inside this
wave."* SEAMS S-4 convicts the spine and routes **G-9's three dispositions — A (a successor X·V
wave) · B (amend G31 to admit adoption inside tranche X) · C (close `BLOCKED-ON <condition> +
<re-trigger>`) — to the owner at the X-W0 opening sitting.** No executor schedules KF.W3 or the
F.W2 parser grants until the owner disposes G-9 and RC-P evaluates TRUE. Related: KF.W3's two
publication asks against `V` (`serializeCssValue` unexported from packed `./css`;
`reverseAnimationShorthand` absent at every measured coordinate) belong on the same dated cut as
X-W9's CC-084.

**E-3 · SS-4 contract co-signature ↔ F.W5 ∥ the X·V API row.** *"Neither side's edits gate the
other's waves"* — F.W5 and X-W3/X-W9 run concurrently. F.W5's owner rulings (E16 trie-vs-KISS,
D3 producer-or-retire, OG-F1, OG-F2) are FLAGGED INLINE, never presumed — sitting agenda.

**E-4 · F.W2's packet P-7 declares a value-side reciprocal X·V has not booked** (SEAMS S-6,
CONVICTED: `grep -rc 'P-7'` → 0 at every X·V file). At X-W9's opening, book (a)–(d) as
**received** at `refinement/X-W9-FOLD.md` (never `W9.md` — untouchable under E-3) — accept or
decline-with-reason, before X-W9's battery closes. Clause (d)'s Buffer-input throw
(`e.startsWith is not a function`) joins the R1/totality family with its own born-RED witness
inside X-W9's commit 1, where the widened R1 battery already lives.

**Non-edge, declared so the absence is measurable**: the Glass-8 atomic-cut law (X-W0.j → X-W4.g)
is **value.js-internal** (S-9 cure re-scopes the spine's universal; S-24 CONSISTENT). Each sibling
pins its own producer dependency at its own gate — X·F at ESC-1/G1; X·KF at KF.W0 §B-12's 7.0.0
exact devDep. No sibling wave takes an edge into X-W0.j.

---

## §2 THE FIRST GATES — born-RED, to run at the begin-word

**27 gates.** Every command quoted from its spec at the cited coordinate; each was confirmed
present there by the two draft seats and re-confirmed by this seat, read-only, 2026-08-30. No
proof script is authored for any gate (L-19).

**Standing law (X·V §READINESS R.2, adopted program-wide)**: *"Running them at wave-open re-dates
the baselines; a PASS on any of them before its cure lands is itself a finding and goes to the
sitting."* Two X·V gates are already in that state (gate 2's leg-4 flip; gate 10's green-at-pin)
— both routed to the sitting via §EXTERNAL.

### §2.1 X·V — ten gates ⟨`REFINEMENT-FOLD-2026-08-28.md` §READINESS R.2, r1-corrected forms⟩

| # | gate | command (literal) | expected |
|---|---|---|---|
| 1 | **X-W0 G-F**, the register | `ls docs/tranches/V/megatranche/registry/adjudicated/*.md \| grep -vE '/(kf\|fr\|pt)-' \| xargs grep -c 'NO-WAVE-OWNER' \| grep -v ':0' \| wc -l` | **73** records / **604** occurrences; `grep -c NO-WAVE-OWNER docs/tranches/X/waves/*.md` → 0 in all twelve. RED until the set-difference is ∅ both ways. |
| 2 | **X-W0.j** Glass-8 repin census (the X-W4.g trigger) | `node -p "JSON.parse(require('fs').readFileSync('node_modules/@mkbabb/glass-ui/package.json','utf8')).version"` | **7.0.0**. Census **1/4** (legs 1–3 unchanged; leg 4 flipped by I-28). FAIL branch is the live branch; the .g cells stay held. Run under S-11's widened option set: **8.0.0 AND 9.0.0** (9.0.0 tag-pinned, registry-absent — §3.2). |
| 3 | **X-W9 G2/NG-2**, the R1 throw class | `node -e 'import("./dist/subpaths/css.js").then(m=>m.parseCssColor("oklch()"))'` | **TypeError** (likewise `calc()`, `foo()` — the class is `<any-ident>()`; the three-parser asymmetry stands). RED; the battery must widen past the nine colour functions. |
| 4 | **W-HYGIENE H-c** falsifier | `node -e 'const fs=require("fs");const t=JSON.parse(fs.readFileSync("tsconfig.demo.json","utf8").replace(/^\s*\/\/.*$/gm,""));const p=require("./package.json").exports;console.log(Object.keys(t.compilerOptions.paths).length, Object.keys(p).length)'` | **`10 7`** — the key drift. RED; **cure = DELETE the inert block, never repair.** |
| 5 | **X-W4 A4** witness | `sed -n '81,87p' demo/shell/dock/layers/SlugEditLayer.vue` | the placeholder-only accessible name. RED at both matrices. |
| 6 | **X-W5 D4** witness | `grep -c '<Transition ' demo/workbenches/mix/MixSourceSelector.vue` | **0**, against the bare mode swap at `:114`. RED. |
| 7 | **X-W7 N-1**, the mount substrate | `grep -rn 'mount(' test/ demo/ --include='*.test.ts' \| wc -l` · `grep -c 'plugin-vue' vitest.config.ts` | **0** and **0**; `@vue/test-utils` installed and unused. RED. |
| 8 | **X-W1 R2**, the dead-locator census headline | `grep -rn 'plate-caption' e2e/ demo/` | a live un-skipped spec asserts a class deleted at `a68ecdc1`. RED; **`test.skip()` is deferral under a new name and does not discharge it.** |
| 9 | **X-W8 G-9** null-DELTA witness | `sed -n '8p;22p' demo/palettes/browser/admin/PaginationBar.vue` | both lines `variant="outline"`. RED; cure collides with X-W7's quiet-migration reading (sitting item). |
| 10 | The glass exports-map parity watch | `node -p "const e=JSON.parse(require('fs').readFileSync('node_modules/@mkbabb/glass-ui/package.json','utf8')).exports;['./blob','./chip','./forms'].map(k=>k+':'+(k in e)).join(' ')"` | **`./blob:true ./chip:true ./forms:true`** — **GREEN-at-pin, not RED.** Stays on the slate because it flips RED at the 8.0.0 repin (`./forms` dies — X-EXT-2), which is exactly what it must catch. |

### §2.2 X·KF — five gates

| # | gate | coordinate | command (literal) | expected |
|---|---|---|---|---|
| 11 | **G-0.1 SUBSTRATE SETTLED** (+ its measured falsifier) | `KF-W0.md` §Gates | `git rev-list --count HEAD..origin/master` · `git status --short \| wc -l` · `git diff --name-only origin/master \| wc -l`; falsifier: `comm -13 <(git status --short \| sed 's/^...//' \| sort -u) <(git diff --name-only origin/master \| sort -u) \| wc -l` | **41** · **252** · **325**; HEAD `8281638c`, `origin/master` `81a56990`. Falsifier: **the two surfaces do not nest** — `comm -13` → **225** (`comm -23` → **152**, `comm -12` → **100**, untracked **124**); *"GREEN requires BOTH enumerations, never one plus a difference."* RED. |
| 12 | **G-0.2 MANIFEST SINGLE-STATE** | `KF-W0.md` §Gates | `git show origin/master:package.json` (`:77`) · `grep -c glass-ui package.json` · `node -p "require('./node_modules/@mkbabb/glass-ui/package.json').version"` | `"@mkbabb/glass-ui": "7.0.0"` exact devDep at origin/master · **0** (the worktree DELETES the row) · **7.0.0** — disagreeing states. RED. |
| 13 | **G-KF1-1 DELIVERY** | `KF-W1.md` §Gates | `ls $PROG/keyframes-v-exec/docs/tranches/V/coordination/` | **9 entries + `vnext/`** — *"No addendum. No O-8. No O-11."* RED. |
| 14 | **G-KF1-2 ANCHOR LIVENESS** | `KF-W1.md` §Gates | `git cat-file -e origin/master:src/animation/compile/value-ast.ts` (in `keyframes-v-exec`, against `origin/master`, never bare `HEAD` — bare HEAD silently answers the wrong question) | **ABSENT** — O-11 §A3's primary authored-input anchor. RED. Per-anchor, not blanket. |
| 15 | **G-KFW4-1 the vue-tsc gate** (THE SEQUENCING HEAD) | `KF-W4.md` §Gates `:207` | `ls node_modules/.bin/vue-tsc` · `npx tsc --noEmit --listFiles` | **ABSENT** · **126 demo files, 0 `.vue`**. RED. GREEN = `check` redefined to `vue-tsc --noEmit -p tsconfig.json && tsc --noEmit -p tsconfig.test.json && npm run proof:structure` — **the `proof:structure` tail SURVIVES verbatim** (NO-SILENT-DELETION, scope = all five opened scripts) — AND the `demo/env.d.ts:3-7` `DefineComponent<{},{},any>` shim retired: *"a green run with the shim standing is the no-op this gate exists to prevent."* |

### §2.3 X·F — seven gates ⟨all `F-W0.md` §4⟩

| # | gate | command (literal) | expected |
|---|---|---|---|
| 16 | **G-1** — the 28-path M.W1a tree SETTLED, per path, minuted | `git rev-parse --abbrev-ref HEAD` · `git rev-parse --short HEAD` · `git status --porcelain \| wc -l` | `m/w1-bump-migration` · `cd26c65` · **28** (27 ` M` + 1 `??`). Every path ruled LAND / ABANDON / LAND-WITH-CORRECTION in `SUBSTRATE-LEDGER.md`; rows 24 (FR-EMT-25) and 25 (C:S-2) MUST LAND; *"a wholesale reset is a gate FAILURE, not a gate pass."* RED. |
| 17 | **G-2** — the O-14 letter COMMITTED | `git ls-files docs/tranches/N/valuejs-inbound-2026-07-27-facility19-migration-table.md` | returns the path. RED today: it is the sole `??` row. |
| 18 | **G-4** — the adopted producer stylesheet PARSES | `grep -o '/\*' web/node_modules/@mkbabb/glass-ui/dist/styles/index.css \| wc -l` vs `grep -o '\*/' …` | **17** vs **8** (src is 15/6); repro with the app's toolchain: `postcss([@tailwindcss/postcss])` → `CssSyntaxError: Unterminated string: 's own'`. **GREEN only upstream — "A consumer-side patch is a GATE FAILURE."** ⟨**FR-NP-32 (≡ fr-PaperSidebar M1)** — cite both, never substitute.⟩ The producer ACCEPTED the ask (the 08-29 ACK, §3.2); the pre-gate stands at the adopted 4.0.0 bytes regardless. RED. |
| 19 | **G-5** — `npm ci && npm run build` completes in `web/` | `git ls-files web/dist` · `ls -ld web/dist` | **0** · present, dated Jun 12. GREEN = build exits 0 **and** the post-build readbacks fire. `web/dist` is not touched until G-4 and G-5 are green. RED. |
| 20 | **G-13** — the producer pin re-derived at the ADOPTED commit | producer `package.json` read + installed-consumer pin read | producer reads **8.0.0**+ ; installed consumer pin **4.0.0**. **The pin cell is the COMMIT HASH, never the version string** (P-4); the dated addendum reading is *"never a live fact — re-measure at wave open."* RED. |
| 21 | **G-12** — ONE corrected-denominator table published | the §4 G-12 table act | superseded figures FORBIDDEN downstream (e.g. the 8-spec e2e count; any 7-spec figure is dead). RED until published. |
| 22 | **G-3** — the mail seat | `ls ../fourier-analysis/docs/tranches/F/coordination/INBOX.md` | *No such file* today. GREEN = ledger + `CLAUDE.md` exist, the three extant 2026-05-29 letters TRIAGED AT CREATION, P-1/P-6 logged SENT. **G-3 gates the SENDs.** RED. |

### §2.4 X·P — five gates ⟨`parse-that/waves/W0.md` §6 · `W4.md`⟩

| # | gate | command (literal) | expected |
|---|---|---|---|
| 23 | **W0 G-1 — handoff identities re-derived** | `shasum -a 256 docs/tranches/V/megatranche/coordination/PARSER-CSS-PAUSE-HANDOFF-2026-08-02.md` + the sibling rows | `ced23440…` (the handoff) · `244c448a…` (parser-law matrix, 30,242 B) · the two further rows — each with the wave's OWN pasted output; *"a row disposed by quoting the handoff rather than by running shasum fails."* |
| 24 | **W0 G-2 — EPERM is not absence** | the `~/Documents/Codex` probes | report **unreachable**; any "the v12 target is absent" sentence unqualified by date + access state fails the gate. Three-state disposition: MATCH / MISMATCH / EPERM, never conflated. |
| 25 | **W0 G-3 — eighteen roots, byte-unchanged** | `git -C <root> rev-parse --short HEAD` · `find <root> -type f -not -path '*/.git/*' -not -path '*node_modules*' \| wc -l`; then `diff census-before.txt census-after.txt` | the eighteen-root table (`parse-that ef10d5b` 20261 files DIRTY · `parse-that-css-totality f575708` 757 · …); GREEN = the diff is empty. |
| 26 | **W0 G-8 — PLAW-BIND declared from both ends** | `grep -n 'X·P release condition → KF.W3' docs/tranches/X/COHESION.md` · `grep -n 'parse-that→fourier is FORBIDDEN' docs/tranches/X/COHESION.md` | each ≥1. **Located by literal-fragment grep, never by line coordinate** — COHESION is a live document; a coordinate into it rots by design. |
| 27 | **W4 G-2 — zero value.js source bytes** | `git -C /Users/mkbabb/Programming/value.js status --porcelain -- src api demo test e2e` | empty at every X·P commit — *"the executable form of 'X·V owns its bytes'."* A standing invariant from W0 through W4. |

---

## §3 THE SEAM PROTOCOL — the adjudicated seams as execution-time checkpoints

`union/SEAMS.md` §1: **24 seams · 15 CONSISTENT · 9 CONVICTED**, every conviction an
addendum-beside at the file that owns the end, none executable before the begin-word.

### §3.1 The nine convictions, as checkpoints

| # | seam | checkpoint — when the operator touches it | owning end |
|---|---|---|---|
| 1 | **S-3** the fourier half of the G-6 reciprocity absent (`grep -rl 'RC-P' fourier/waves/` → ∅) | At F.W0's opening, before G-3's SENDs: adopt G-6's second sentence verbatim **by predicate name** (`RC-P`), beside the existing NON-EDGE row. X·P may not write it. | `fourier/waves/F-W0.md` |
| 2 | **S-4** the L1/L5 adoption leg UNHOMED | X-W0 opening sitting, agenda item 1 (= §1.5 E-2). Owner elects G-9 A / B / C. | `COHESION.md` new §0g |
| 3 | **S-5** the repin coordinate spelled as a literal | Before KF.W5 and KF.W10 open: convert `4.0.0→4.1.x` to `4.0.0→V` at `RC-P(V)` per KF-W2's D-8 ruling — a predicate, never a number the spec may forecast. | `KF-W5.md` ×2 · `KF-W10.md` ×1 |
| 4 | **S-6** F.W2's packet P-7 has no receiving end | At X-W9's opening (= §1.5 E-4): book (a)–(d) as received. | `refinement/X-W9-FOLD.md` |
| 5 | **S-9** Glass-8 atomicity stated at one end only | At X-W0, F.W1 and KF.W0 openings: re-scope the law to value.js; each sibling declares the non-edge in one line so the absence is measurable. | `COHESION.md` §2 + `F-W1.md` + `KF-W0.md` |
| 6 | **S-11** the 2026-08-29 O-20 ACK unrowed | **AT THE BEGIN-WORD, before any wave opens** — §3.3. | `INBOX.md` (+ X-W0.j · §EXTERNAL · F-W1 PIN-LAW · F-W0) |
| 7 | **S-15** F.W3's g20 believes the letter has not left | Before F.W3 opens: re-baseline g20 against the dispatched O-20 by sha (`f6e04c86ddb8`); cargo becomes the **residual**; the verb becomes *accretes to SS-6*, not *files*. No ask withdrawn, no packet re-sent. | `fourier/waves/F-W3.md` |
| 8 | **S-16** the O-20 denominator | Before F.W10 closes: restate **22 id-rows ⊕ 4 §D entries = the packet's own 26**; withdraw the false "correct COHESION" instruction — the figure it would correct is true. | `fourier/waves/F-W10.md` |
| 9 | **S-19** §0d's line anchor into a live sibling (`KF-W4:249` blank; the block moved) | Rider on the same §0g letter as S-4 — one dated addendum at the spine. Re-anchor to §-heading + rule id, no numeral. | `COHESION.md` §0d |

### §3.2 The glass-8.0.0 repin decision point — one decision, three ends, never merged

The trigger and receiver are **X·V-internal and consistently spelled** (S-24): X-EXT-6 → X-W0.j
(the repin act); X-EXT-4 → X-W4.g (the receiving surface). The decision sits at the X-W0 sitting,
**and it has grown a third option**:

- **Measured today**: X·V pin **7.0.0**; the 8.0.0 receipt EXISTS (I-28, `v8.0.0` @ `17a11bc5`);
  the X-W0.j census reads **1/4**, FAIL branch live — the .g cells stay held.
- **§EXTERNAL's six minted rows keep their ids**: X-EXT-1 `SearchBar` DELETE → X-W7 · X-EXT-2
  `./forms`→`./input` → X-W8 · X-EXT-3 `grain` removed → X-W6 · X-EXT-4 `--slider-track-bg`
  no-alias → X-W4.g (mail says ×4, the tree measures 5 — both figures stated, neither silently
  elected) · X-EXT-5 o7 re-pin → X-W1 · X-EXT-6 peer `vue-component-type-helpers ^3.0.3` → X-W0.j.
- **The third option (S-11)**: the 2026-08-29 O-20 ACK in the BK inbox announces **9.0.0 CUT** —
  tag `v9.0.0` @ `d4f7b24f`, `./search` removed whole, `./canvas` gone, the 8.1.0 dark-sync fold
  riding the same section — and every content claim is **tag-pinned, not registry-pinned** (the
  npm PUT is walled).

▶ **At the sitting**: widen X-W0.j's four-condition verdict to enumerate **8.0.0 AND 9.0.0** as
candidate targets; give §EXTERNAL's rows a 9.0.0-successor-fact column where the letter supplies
one (`./search` whole-removal beside X-EXT-1). **The sibling pins are not moved by this**: X·KF
stays at KF.W0 §B-12's 7.0.0 exact devDep (KC-5 is *"the only row arguing a producer major"* and
is weighed against the pin before KF.W6 commits to either arm); X·F elects its own target at
**ESC-1/G1**, whose PIN-LAW gains the tag-pinned-vs-registry-pinned clause (S-11 cure) so an owner
electing 9.0.0 knows it elects a tag, and every producer `file:line` in all 66 records must
re-resolve at the adopted hash. **F.W0's FR-NP-32 BLOCKER stands regardless** — measured at the
adopted 4.0.0 bytes; the ACK supplies the producer-side terminus (A-1 ACCEPTED), not a cure of
the installed corruption.

⟨**DATED ADDENDUM-BESIDE 2026-09-17 — THE DECISION IS RULED.** COHESION **§0i.2** elects **8.0.0**
(registry-pinned, `v8.0.0` @ `17a11bc5`) at X-W0.j; **§0i.3** elects the same tag and hash at F.W1's
ESC-1/G1; **§0i.1** disposes the L1/L5 leg as **C** (BLOCKED-ON the parser proof gate → GREEN via V·π
Phase A; re-trigger = RC-P TRUE; payload = disposition A). 9.0.0 stays the recorded successor fact,
re-entering the enumeration only when the registry returns it. The paragraph above is not rewritten
(E-3); the election it describes as pending is now made, under the owner's 2026-09-17 delegation.⟩

### §3.3 The mail obligations — I-30 first, then O-21

**Before any wave opens** (E13; the union boundary is a boundary):

1. **Row the 2026-08-29 O-20 ACK as I-30** — with the A-1 ACCEPTED disposition and the 9.0.0
   tag/registry split stated in the row. A ledger act, not a wave act. It is not merely unread,
   it is **unrowed** — the identical mechanism I-28's own row minutes as having cost 19 days.
2. **Run the four-path sweep** (§5.3) at the same sitting.

**O-21 — minted exactly once, by KF.W1** (S-13 CONSISTENT; S-14: no X·V/X·F/X·P wave mints an
outbound id). KF-W2 · KF-W4 · KF-W9 · KF-W10 cite the id as a **vehicle**, never as their own
act. The **MINT LAW** governs and survives the ledger moving: *"the executing seat re-runs the
max-grep at open and, if the ledger has moved again, mints max+1 and records the delta as a dated
addendum — a spec-frozen outbound literal is never trusted over the measured ledger."* O-21
carries the O-8/O-11 amendment-addendum to an exec-visible keyframes path (the HEAD-resolved
anchors, the 3 missing `parseStylesheet` sites with failure postures, the closed delivery-vehicle
question, the D-GAP-6 resolution — I-26's cure). Note for the KF.W1 seat: I-30 is an **inbound**
id and does not move the outbound maximum — the MINT LAW's re-measure at open is what makes that
safe without editing KF-W1.

**SS-6 is the only lawful cross-repo write path in tranche X, and it is root-authored** (X.P.W4
G-8: *"a wave that opens its own channel has substituted an ad-hoc letter for the batched-root
law"*). X·P's outbound act is a packet authored in-repo and handed to SS-6's courier. F.W10
reconciles against the dispatched O-20 packet — *"it does not re-send it."*

### §3.4 The coupled events and same-commit locks, restated for the executor

| event | the lock, in one spelling at every citing site |
|---|---|
| **F.W1 land-or-lose** | the TWELVE-limb roster chartered at F-W1 §4 step 4, *"cited whole and never restated"* (10 of 11 specs, one spelling — S-22). A partial landing is the L-18 failure. |
| **FR-NP-32 pre-gate** | 11 of 11 specs; **FR-NP-32 (≡ fr-PaperSidebar M1)**, cite-both-never-substitute (S-23); carried at O-20 row A-1. |
| **repair-arms-a-defect riders** (one spelling, S-21) | `fr-PaperSearchModal` same-commit riders · **PAW-44/LAW-3** (*"restoration lands WITH-or-AFTER PAW-1 + PAW-30, never before"*) · **MPC-31 one-cut law** · **FR-MSP-6 two-channel lock**. |
| **KF-AV-28 standing supersession rider** | KF.W7's per-surface SWAP verdict may supersede any NO-WAVE-OWNER behavioral cure; governed rows are spent CONDITIONALLY — no cure lands before W7's verdict for its surface, or the seat accepts the throwaway explicitly, STATING which governed rows a SWAP verdict discharged BEFORE spending any cure (G15's discipline). **Both limbs travel**: the supersession sentence AND the §7 caution + NON-bespoke clause. |
| **X-W0.j / X-W4.g** | one trigger, one receiver, one spelling, X·V-internal — §3.2. |
| **RE-CUT-0** | G-F9-21 ≡ G-F10-6; neither F.W9 nor F.W10 stamps before it closes; shared-§2.2 changes are a paired edit in ONE commit. |
| **KF.W4 ∥ KF.W6** | `scripts/observe/demo/usability.mjs`: *"written ONLY inside the atomic bundle (one commit, both seats); never unilaterally, never concurrently."* |
| **Same-commit / one-act locks carried in-spec** | F.W0 G-10 lift-then-delete ONE commit · KF K2 delete-and-correct ONE commit · KF-CB-18+24+29 ONE commit · KC-34 before the KC-8/KC-9 keep-mounted repair · G-KFW9-9's two-deletion pair is ONE act · X·V PSC deletion + C1 successor same-wave · A-1-before-A-3 · MP-3 same-change guard · KF.W8's `git mv` + four import repoints + docblock ONE commit · KF.W2's façade before any Tier-A repoint, each Tier-C deletion WITH its replacement call. |
| **X·KF tree during successor authoring** | TRACK-or-defer; never ARCHIVE. |

### §3.5 The fifteen CONSISTENT seams — verify-at-open, do not re-litigate

S-1 · S-2 · S-7 · S-8 · S-10 · S-12 · S-13 · S-14 · S-17 · S-18 · S-20 · S-21 · S-22 · S-23 ·
S-24. Each is stated at both ends in the ends' own bytes; the executor re-runs the quoted command
at wave-open to re-date the reading — **a divergence is a finding for the sitting, never a
licence to re-adjudicate the seam.** Note S-12 especially: KF.W7 C-15 (per-surface
ADOPT/PARTIAL/KEEP-BESPOKE against the importable surface) and F.W3 SR-1 (the `ScrubberTimeline`
adoption REJECTED; the gate asserts a re-derivation, not an import) are **the same verdict at two
altitudes** — two consumers, one producer surface, zero contradictory asks; do not let an
executing seat manufacture one.

---

## §4 THE RESIDUE — what execution must carry that no wave gate discharges

### §4.1 SS-8 — the UNPROVEN-NEEDS-LIVE visual-audit slot (value.js)

**~751 residue items** (739 counted from §UNPROVEN sections, corrected +12 for
wb-gradient-visualizer's prose-carried R-1..R-12). **Live-witness obligations; they route to
SS-8, never to X waves — a wave gate that claims one discharges nothing** (fold §4). Heaviest
feeds: picker-spectrumcanvas 17 · PreviewStrip 16 · shell-dock-profilesection 16 ·
wb-gradient-visualizer 15 · BrowsePane 14 · wb-gradient-codeeditor 14. **THE ZERO-RESIDUE GAP**:
fourteen records carry no §UNPROVEN section at all (App · AdminUsersPanel · ColorInput ·
ColorPicker · ColorSpaceSelector · ConfigSliderPane · Dock · GenerateControls ·
GradientStopEditor · Markdown · chassis-fitness · layout-gestalt · library-band · parser-band —
the pre-X cohort); *"SS-8 may not be called complete until this cohort is swept."* Epistemic
statuses travel verbatim — UNPROVEN stays UNPROVEN, never upgraded by transcription. SS-8 is
task #4, OPEN, real-Safari automation unblocked since 2026-07-27; bounded and parsimonious
(§5.2); **not a wave** — it runs beside the tracks.

### §4.2 SS-13 — the per-repo visual audits

- **X·KF**: KF.W9 owns the kf surface ONLY (*"fourier/value.js residues never fold into its
  denominator"* — S-10). Backlog **≈590 probes** (565 enumerated + 25 prose-carried, 57 of 58
  records, 0 executed) — the amended `##`-or-`###` reproducing form, never the `##`-only form
  (563, a manufactured 2-count divergence). KF.W9 produces the **OD-V3 capture packet** (both
  transport homes × four duplicating scenes × 390 AND 1280 — *the ask is a comparison*; one home
  or one viewport does not satisfy it) and the **OD-V5 390 at-rest observation** — **and rules
  nothing**: OD-V3/OD-V5 are OWNER-GATED at KF.W10's owner block, never proxied. *"This wave may
  not claim a live receipt it did not run."*
- **X·F**: the SS-13 residues are carried as a named class, not absorbed — each rides its
  record's own wave (F.W3/W4 cures); F.W9's claim over them is the instrument only. **Blocked on
  G-4/G-5** — never queued as independent visual work (F-W0 §6b).
- **X·P**: n/a — no demo.
- **Shared-apparatus rule**: KF.W9 and X-W11 G8 use shared apparatus at SEPARATE cells at
  distinct coordinates; neither consumes the other's captures (*"all five corroborate G8's
  premise; NONE clears it"*).

### §4.3 The FORMATION-BOUNDARY packets — 4 terminal, booked id-for-id

Fold §1: **19 packets terminal = 15 HOMED + 4 FORMATION-BOUNDARY** (W-HYGIENE counted HOMED, one
limb at the boundary). Every FORMATION-BOUNDARY booking names the G-F register as its landing and
**the X-W0 sitting as its elector** — sitting agenda, not wave cargo:

| packet | what carries forward |
|---|---|
| **PRE-X MT-REGISTER** (the 2026-07-27 cohort) | the per-record MT-*→X mapping; the same cohort as §4.1's zero-residue gap |
| **BOUNDARY-SCOPE** (AboutPane AB-4/AB-5) | X-W6's fold books them; disposition is the boundary's |
| **AUTH-SESSION SUBSTRATE** | X-W3-FOLD F-4 banks AF-50 NO-WAVE-OWNER |
| **MIGRATE-DIALOG IDENTITY-FLOW** | behind the OWNER-DECISION gate |
| **W-HYGIENE**, one limb | the 8 records that route it in; *silence is not adoption* |

### §4.4 The G-F residuals — 3, booked not waived ⟨`union/G-F-ADJUDICATION.md` §5⟩

**G-F is MET WITH RESIDUALS**; X-whole may stamp SPECIFIED against §3.3. Pre-execution duties on
the X-W0 sitting, not blockers:

1. **R-1 · the ApiOfflineChip transport cluster — MAJOR-bearing.** Seven rows (AP-12 · AP-17 ·
   AP-24 · AP-29 · AP-30 · AP-31 · AP-33) absent from the entire X·V fold layer; **AP-17
   CONFIRMED MAJOR** (the cooldown gate admits an unbounded burst against docs promising "ONE
   probe" twice); `demo/platform/transport/**` has no X-wave claimant — the claimant question
   sits beside W0.22's `ErrorBoundary.vue` contention. ▶ Book as **G-F slate entry 15** at
   `X-W0-FOLD.md`'s §8 idiom, all seven ⟨record · id⟩ id-for-id; correct COHESION §4's `×5`→`×7`
   **in the addendum, never in §4's bytes**.
2. **R-2 · two missing `≡`-pointers** — ⟨AdminFlaggedPanel · AF-12⟩ (= the AdminListItem D-5
   identity, which is carried) and ⟨ColorNutritionLabel · R6⟩. No identity orphaned; two lines.
3. **R-3 · the register's file home is unelected** — CLERICAL; §1 rules it moot (the fold and
   canonical layers ARE the register of record, identity-grain). The sitting elects candidate A
   or ratifies §1; the adjudicator recommends the latter.

### §4.5 The minted-unauthored successors

KF.W11/12/13's authoring is SS-1/SS-2-block work that must complete **before their 17 packets
execute**; KF.W10's close never stamps them (§0d/S-18: *"closure by omission is the one failure
§2 declares fatal"* — their disposition is a register row). Silence at X·V/X·F/X·P is correct by
construction (S-20).

---

## §5 THE SEAT LAW

### §5.1 Model tiering — M-12 TRI-FOLD (current law) + the four-workflow cap

- **Fable orchestrates**; toughest work = Fable-worker ∥ Opus-worker → fresh-Fable arbiter
  apotheosis (the idiom this whole spec layer was authored under, and the idiom execution
  inherits); Opus solo for mechanical/challenge seats (censuses, greps, gate runs, single-file
  cures). **Receipts every seat**: line-1 served-model declaration; ⟨…⟩ provenance; dated
  corrections appended, never rewritten. Tier aliases, never pinned ids; probe the served tier
  (`CLAUDE_CODE_SUBAGENT_MODEL` REMOVED 2026-07-19).
- **MAX 4 CONCURRENT WORKFLOWS** (owner cap 2026-07-24) — overrides ultracode/commissioned scale:
  *"a killed jury seat reads as a clean component (NO_JURY), so wide fan-out under a rate wall
  manufactures false negatives."* The four track heads fit the cap at one each; intra-wave
  concurrency yields first.
- Per-wave seat counts are declared in each spec's §State `Agents` line and are **binding**
  (e.g. KF.W1 = 3 Opus serial; X.P.W2 = 9 across 6 phases; F.W9 = 3 **serial, never parallel** —
  units 2 and 3 both write `web/package.json`; KF-W4 declared peak concurrency 2).

### §5.2 Probe parsimony (owner edict 2026-07-12)

Parsimonious Playwright/DevTools-MCP; fastidious design+code analysis — every wave's LAW block
carries it. Shared-capture families are declared so no probe is double-spent (KF.W9 S-8's six
families: the three-rect probe ONE mount · the composited cartoon plate ONCE · one 390×844 +
375×667 mobile pass · one WHC pass · ONE iOS device session · ONE AT pass). Discriminators and
falsifiers stated per packet. **webkit-engine and safari-app are separate evidence cells; never
infer one from the other.** Live probes bounded; SS-8/SS-13 evidence packets keyed to apotheosis
row ids, not free-form screenshots; a nightly run colour is residue, never a gate input.

### §5.3 The mail law (E13)

**Every wave OPENS with the four-path sweep**; new mail is ROWED in
`docs/tranches/V/coordination/INBOX.md` (status UNREAD) before any other work; **no wave closes
with UNREAD mail addressed to its scope**; mail is MARKED, never merely remembered —
relay-received is never terminal (D37). The four paths:

1. `docs/tranches/V/` + `docs/tranches/V/coordination/`
2. `../glass-ui/docs/tranches/BK/coordination/` — **the ACTIVE tranche's dir, never a pinned
   letter** (the stale-BI pin cost 19 days; it has now cost a second letter — the unrowed 08-29
   ACK, §3.3)
3. `../keyframes.js/docs/tranches/V/coordination/` — their `<SENDER>-INBOUND-*` grammar; the
   tranche root is NOT a mail path
4. `../sci-report/atlas/docs/tranches/P/coordination/`

**NO CRONS** (owner order 2026-07-17). And the delivery-path lesson (I-26): *a letter that lands
untracked in a frozen checkout is NOT delivered — the path, not the content, was the defect* —
which is what KF.W1 exists to cure, and why no kf wave consumes O-8/O-11 obligations before it
lands (COHESION §2). First sweep at begin-word rows I-30.

### §5.4 Commits

- **Pathspec commits only.** `scripts/dev/dev.sh` (value.js) is unowned, dirty by standing
  arrangement, **never staged, NEVER touch** (F-W0 §6a lock 9 binds it cross-repo).
- **Commit families that must not split** are declared per wave and binding — the §3.4 lock
  table governs; F.W1's single atomic `feat(fourier)!:` commit is the largest.
- **One commit per meaning** (KF.W3 L-7): *"a retirement folded into a refactor hides a
  behaviour change."*
- **E-3 everywhere**: censuses and corrections are addenda, never patches; dated specs and the
  adjudicated registry are immutable beside the specs that cite them. Pinned authorities are
  IMMUTABLE (epoch rule); frozen operands (`CENSUS-CANONICAL.md` @ `f44362757458`) are cited,
  never re-derived.

### §5.5 Read-only laws for sibling trees

| tree | law until its wave opens |
|---|---|
| `../keyframes.js` (the sacred checkout) | **READ-ONLY.** The `git fetch && git reset --hard origin/master` is **the OWNER'S HAND** — *"value.js must NOT perform it."* A wave that performs the reset itself, or opens without the begin-word, re-runs the schism it exists to close. All KF.W0-era probes are read-only `git` commands; **kf write authority is OP-1, owner-held, unresolved.** Every kf measurement names its substrate in the command (`origin/master`, never bare `HEAD` — the stale worktree silently answers the wrong question with a plausible answer). |
| `../fourier-analysis` | **READ-ONLY** through fifteen conformance passes and until F.W0 opens. Any producer-tree WRITE is a halt condition — producer defects leave via NWO-1, never as frontend hacks. **No `git stash` anywhere in F.W0** — a stash would erase rows 24/25, the tree's only copies of two correct repairs. |
| `../parse-that` | **READ-ONLY, permanently for this program.** X·P writes only in the fresh root (`parse-that-css-totality-p2`), the shared repository provably untouched (`.git/objects` shares zero inodes); X·P writes **zero value.js source bytes ever** (W4 G-2, gate 27). |
| `../glass-ui` | **READ-ONLY, always.** Producer rows ride SS-6 and **NEVER become frontend hacks** — one rule, four ends, one spelling (S-17); the live inbox is the ACTIVE glass tranche dir (BK today). *"Do not patch locally"* is the producer's own instruction. |
| value.js `src/`, `demo/`, `api/` | **READ-ONLY until the begin-word.** Nothing in any sub-tranche opens product source before it. |

### §5.6 The X·F program-laws — binding on every successor round anywhere in X (§0f)

Census freeze (the frozen canonical is the sole census operand; a gate citing any other is
defective on sight) · the star-topology pin certificate (eleven mutually-pinning files have no
fixed point; one certificate pinning eleven does) · strictly-sequential convergence rounds ·
spelling-agnostic numeric sweeps · the self-count law · authority-list-first numeric
reconciliation · write-then-measure · measure-don't-inherit closure.

### §5.7 Escalation

Triumvirate triggers as each spec declares them: any write outside §Bounds · any third
diagnose→edit→re-measure iteration on one gate · any pressure to allowlist — never an
implementer's decision. Owner rulings are FLAGGED INLINE and never presumed (F.W5/SS-4 · OP-1 ·
OP-6 · OG-F1 · ESC-1 · G-9 · G-F7-1 · OC-1 · OD-V3/OD-V5).

---

## §6 WHAT CONFORMANT MEANT — one paragraph per sub-tranche

**X·V** ⟨`REFINEMENT-FOLD-2026-08-28.md` §ROUND-4, banked `c88e1fed`; criteria at §READINESS
R.4⟩ — The fold layer reached CONFORMANT at pass 4: the twelve dated wave specs byte-identical
(E-3 falsifiable and held — `git diff --stat HEAD -- docs/tranches/X/waves/` prints nothing),
§0's **1,890-row** arithmetic reproduced from the folds' own blocks (row bands verified gapless
by full enumeration), 19 packets terminal (15 HOMED id-for-id + 4 FORMATION-BOUNDARY), 22 bounds
convictions carried with conviction+cure+owner, the census loop closed by monotone strata
(262 → 84 → 123/124 → residue ∅), and §READINESS stated planning-only with ADJUDICATED never
upgraded to VERIFIED. A statement about the **spec layer's** completeness and honesty — not about
the tree, which no gate has yet measured at execution.

**X·KF** ⟨`keyframes/conformance/VERDICT.md`, pass 8, banked `3c4807d0`⟩ — CONFORMANT = hashes
clean against `PASS-7/SEAL.md`, **census escapes ZERO held two passes running**, every pass-7
register item closed at the bytes, all residuals MINOR-with-mitigation, each carrying its named
one-command cure. Eight passes: authoring → a 122-defect pass 1 → seven rulings/repair rounds
under the accreted law set (REGISTRY-FIRST · quote-by-command · the import-graph and
closure-claim-ban laws · stable anchors · LAW E hash-proven close · LAW F shadows · the
false-universal ban), carriage converging 91% → zero-escape, double-homings 6 → 0. Status of
every wave stayed `planned` throughout.

**X·F** ⟨`fourier/conformance/VERDICT.md`, pass 15⟩ — CONFORMANT = hashes clean against
`PASS-14/SEAL.md` with the canonical **FROZEN at `f44362757458` seven consecutive rounds**,
**roster escapes ZERO / fabrication ZERO eight consecutive passes**, every pass-14 register item
closed at the bytes, and a three-item tail (1 MINOR · 1 LOW · 1 INFO) entirely below the bar with
stated mitigations. The longest loop of the three and the one that minted the most law (§5.6);
the fourier tree was READ-ONLY throughout and every repair round banked its own pathspec commit.

**X·P** ⟨`parse-that/REFINEMENT-FOLD-2026-08-06.md`, CONFORMANT pass 10, banked `08232cd3`⟩ —
The fifteen module apotheoses folded onto the five wave specs as one dated addendum: **148
FOLDED · 16 EXCLUDED** (164 bookings; partition 42 · 16 · 72 · 3 = 133 contiguous row headers,
zero unresolved pointers), unchanged from pass 8 — pass 10's two repairs were claims the file
made about itself. The fresh root uncreated by design, the bench bar OWNER-GATED (OC-1), RC-P
left as a predicate only registry-coordinate measurement can flip. Its idiom (line-1 served-model
declaration · ⟨cmd⟩ provenance · dated corrections appended, never rewritten) became the template
the other three layers were held to; its lesson is now program law: *a pass that moves the
arithmetic must re-grep its own arithmetic tokens before it banks.*

---

## §7 AGGLOMERATION RECORD — the disagreements, resolved at the spec bytes

Both drafts were read whole. Draft B's structure (edge-by-edge reason tables, the coordinate-
cited gate roster, the seam checkpoint calendar) is the chassis; Draft A's superior rows are
grafted (the Stage-A owner-items column; the KF.W8 SCOPED spelling; the KF.W10 full opens-after;
the 17-packet successor detail; the X·F G-3/G-12 and X·P G-2/W4-G-2 gates; the same-commit lock
roster; the X·F program-laws as seat law). Eight ordering/gate disagreements were resolved at the
spec bytes — the specs govern, not either draft:

| # | disagreement | resolution at the bytes |
|---|---|---|
| 1 | A: KF.W0 **→** KF.W1 (serial). B: KF.W0 **∥** KF.W1. | **B.** `KF-W1.md` §State `:35`: *"Opens after: the owner's begin-word. **Not** after X.KF.W0 … deliberately completable without §B-12"*; the KF-W0-side reverse claim is a RECORDED conflict (KF-W1 §7 cross-edge 2), read as permission, never as licence to skip W0's re-anchor. |
| 2 | A's serial listing places F.W5 downstream of F.W1/F.W2. B: F.W5 gated on F.W0 only, beside F.W1. | **B.** `F-W5.md` §State `:35`: *"Opens after: F.W0 (HARD, §4 X-1) · the intake adjudication · the registry whole"* — the ADMISSION KEYSTONE is not downstream of the uplift. |
| 3 | A: F.W2 "after W1" whole. B: specifier arm only. | **B.** `F-W2.md` §State `:48`: *"F.W1 landed for the specifier arm only (the colours arm is not so gated, fr-App C-2)."* |
| 4 | A states F.W7 depends on F.W0 + owner gate only. B adds F.W5's clauses. | **B.** `F-W7.md` §State `:83`: F.W0 (hard, G-F7-7) · F.W5 §2 E16 · E1 · E3⊕G4 · E10 hard predecessors · the owner's G-F7-1 ruling. |
| 5 | A gives F.W8 no dependencies (serial implication). B: F.W0 · F.W1.close · F.W2 · F.W5. | **B.** `F-W8.md` §State `:63` states exactly that set. |
| 6 | B: KF.W8 "after W6's R4-1 atomic commit" (general edge). A: KF.W6 SCOPED for G7's measurement only. | **A's spelling, B's content.** `KF-W8.md` §State `:38`: *"KF.W6 — SCOPED: for G7's measurement only … W6 is not a general predecessor of this wave and no other row here waits on it"* — the R4-1 commit precedes G7's reading only. |
| 7 | B: KF.W10 hard-blocks on W0 · W1 · W9/SS-13 (omits the rest). A carries the full set. | **A.** `KF-W10.md` §State `:22` adds *"AND X.KF.W2 · W4 · W5 · W6 · W7 · W8 · W9 IMPLEMENTED (the set-difference sweep consumes their final bounds)."* |
| 8 | A: "W4's two atomic bundles (KF-AT-4, KF-AT-10) land as the first commits of KF.W6." B: the bundle is `usability.mjs`, one commit both seats. | **B, corrected.** `KF-W6.md` `:121`/`:568` carry exactly one atomic-bundle spelling — `scripts/observe/demo/usability.mjs`, *"written ONLY inside the atomic bundle (one commit, both seats)"* — plus **KF-AT-4 ∥ KF-AT-3** (*"oracle + grapheme land together"*). A's "KF-AT-10 first commits" spelling does not appear at the cited bytes and is dropped. |

Numeric cross-checks resolved the same way: KF-W0's G-0.1 falsifier figures are the measured
**225 / 152 / 100** with `comm -13` pasted (B's row, confirmed at `KF-W0.md` numeric arm);
FR-NP-32 rides **11 of 11** F-W* specs (confirmed by per-file grep); the seam roster is
**24 · 15 · 9** with the nine convictions exactly S-3 · S-4 · S-5 · S-6 · S-9 · S-11 · S-15 ·
S-16 · S-19 (confirmed at `SEAMS.md` §1).

---

**Close.** Thirty-nine authored waves, four parallel tracks, twenty-seven born-RED first gates,
one serialized chain (PLAW-BIND), one mail channel (SS-6), one trigger-gated cut (X-W0.j →
X-W4.g), one gate-keyed wave (KF.W3), three minted-unauthored successors, nine seam cures, four
boundary packets, three G-F residuals — all of it **planned**, none of it moving until the owner
says begin. Two acts are owed before the first wave opens, and neither is a wave: **row I-30**
(§3.3), and **the X-W0 opening sitting** (G-9 · the 8.0.0/9.0.0 census target · the G-F bookings
· F.W5's flagged rulings).
