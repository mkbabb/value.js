# X.W1 - Re-gate: CI, visual oracle, boot-smoke, deploy truth, landing

## State

**Name**: W1 - Re-gate: CI, visual oracle, boot-smoke, deploy truth, landing
**Opens after**: X-W0 (Formation) closes
**Agents**: 6 (3 parallel in sub-wave 1, then 3 serial)
**Hard gate**: 21 conditions, all born-RED at authoring; every RED baseline measured read-only at `41450f02` (2026-08-03) except the two marked MEASURE-AT-OPEN
**Status**: planned

**Four-verb status** (each field independent — this file advances exactly one):

| verb | state | basis |
|---|---|---|
| AUDITED | YES | `registry/DISEASE-REGISTRY.md` DR-07..DR-11, DR-22; `registry/CARRY-CUT-LEDGER.md` §1.C |
| **SPECIFIED** | **YES — by this file** | CC-029..CC-034 transposed to WAVE_SPEC shape |
| IMPLEMENTED | NO | no product or workflow byte written |
| VERIFIED | NO | no gate has run |

**COMPLETABLE**: **YES.** The wave writes gates, workflow files, e2e specs, and one YAML ref
change. It depends on no producer release, no owner design ruling, and no other X wave's product
surface. `c4af0ef9` (CC-002 — V·MT1 landing alone) is the template: a single mechanism, its own
evidence, landed by itself. The one owner-held item (X.W1.f, the real-GPU session) is scheduled at
wave-open and **retires with a tombstone if the session does not happen** — it cannot block the
close. Nothing here waits on Glass 8, on the parser cut, or on the frontend scene chain.

## Goal criterion

This wave succeeds if, when work ends, **every claim any later X wave wants to make about the
running product is decidable by a command a machine runs on every push** — the browser suite, the
visual oracle, boot truth in both modes, and the deploy-of-record all execute automatically, and
each one has been demonstrated capable of turning red.

Today none of them execute. Seven downstream rows (CC-015, CC-029, CC-030, CC-032, CC-036,
CC-065, CC-073) are undecidable until they do.

## Scope

1. Cure the two dangling `demo/@/lib/palette/types` type-imports in `e2e/` and bring `e2e/` under a
   typecheck program (CC-031).
2. Publish the real pass/fail count of the 185-test suite as the born-RED baseline (CC-031).
3. Restore `e2e-smoke` and `e2e-safari` as HARD CI jobs — no `continue-on-error`, no branch-push
   substitution (CC-031).
4. Re-wire LHCI against the existing `lighthouserc.json` (CC-031).
5. Author an oracle-slate successor asserting no Playwright project is CI-orphaned (CC-031).
6. Rule the three `test.fail()` legs — each becomes a real assertion or is deleted (CC-031).
7. Demonstrate the one-time falsifier: a deliberately broken spec on a scratch branch turns the job
   red (CC-031).
8. Author `e2e/visual/` — goldens × 3 viewports × light/dark, stated tolerance, goldens COMMITTED,
   regeneration script (CC-030).
9. Run the visual suite on every push under software GL, labelled renderer-honest (CC-029 half a).
10. Schedule and run ONE bounded owner real-GPU session (≤30 min, headed Chrome), or tombstone the
    row (CC-029 half b).
11. Restore `scripts/ci/boot-smoke.mjs` for BOTH modes — cold dev boot and gh-pages production
    preview at a bare `127.0.0.1` origin (CC-032).
12. Re-class NV-7 RED and diagnose its root by name (CC-032).
13. Point `deploy-pages.yml` at the registry version of `@mkbabb/glass-ui`, retiring the
    `ref: tranche/BG` checkout (CC-034).
14. Merge `tranche-u` → `master`, green master, verify ONE non-skipped `deploy-pages` run, re-probe
    `color.babb.dev` for v4 (CC-033).
15. Add a standing check that the last successful deploy is younger than the last close (CC-033).

## Dispositions carried (CC-029..CC-034)

| CC | original | verb | mechanism this wave executes |
|---|---|---|---|
| CC-029 | DR-07 | BUILD | Two halves. (a) software-GL = CC-030's suite on every push, renderer labelled. (b) real-GPU = ONE owner session, dated at wave-open; **if it does not happen it is RETIRED at close with a tombstone — no seventh carry**. The automated-CI framing for half (b) is dropped; it has failed six times. |
| CC-030 | DR-08 | BUILD | Author `e2e/visual/`; N goldens × 3 viewports × light/dark; stated tolerance; goldens COMMITTED (untracked evidence is not evidence — FM-12); regeneration script. |
| CC-031 | DR-09 | BUILD | Repair the fixture imports; publish the real born-RED pass/fail; restore `e2e-smoke` + `e2e-safari` HARD; re-wire LHCI; oracle-slate successor; rule the three `test.fail()` legs; **mandatory one-time falsifier**. |
| CC-032 | DR-10 | BUILD | Restore boot-smoke for BOTH modes. Assertions: `#app` non-empty · `role=main` present · `pageerrors []` · ≥1 desktop utility in emitted CSS. NV-7 re-classed RED with its root diagnosed. The product-cut half is already VERIFIED at `c4af0ef9` (CC-002) — **the GATE is what is owed**. |
| CC-033 | DR-11 | BUILD | Merge → green master → ONE non-skipped deploy → re-probe prod → standing deploy-age check. The two UNVERIFIED `gh run` measurements are re-verified first (**done at authoring — see §Born-RED**). |
| CC-034 | DR-22 | BUILD | Point `deploy-pages.yml` at the registry version; re-run CC-032's production probe. Highest-probability NV-7 root. The remote `tranche/BG` head is re-checked (**done at authoring — 5.0.0 confirmed**). |

**Blocked rows left in the glass bank: none.** All six §1.C rows are BUILD → X-W1 with no
producer trigger. The §1.M bank (CC-105..CC-116) does not touch this wave's surface.

## Born-RED baselines (measured read-only at `41450f02`, 2026-08-03)

Every command below was executed from the repo root. Outputs are pasted, not paraphrased.

| # | probe | RED output |
|---|---|---|
| B1 | `ls e2e/visual` | `ls: e2e/visual: No such file or directory` |
| B2 | `grep -rnE 'toHaveScreenshot\|toMatchSnapshot' e2e/ \| wc -l` | `0` — across `find e2e -name '*.spec.ts' \| wc -l` → `71` |
| B3 | `grep -rniE 'lighthouse\|lhci\|playwright\|test:e2e\|boot-smoke' .github/workflows/ \| wc -l` | `0` |
| B4 | `grep -nE '^    [a-z0-9_-]+:$' .github/workflows/ci.yml` | only `producer:` and `api:` — 2 jobs total |
| B5 | `ls scripts/ci/` | `verify-packed-surface.mjs` — **`boot-smoke.mjs` absent** |
| B6 | `find . -name 'oracle-slate*' -not -path './node_modules/*'` | *(empty)* |
| B7 | `grep -n 'name:' playwright.config.ts` | 6 projects — `smoke`, `smoke-admin`, `smoke-mobile`, `smoke-reactivity`, `smoke-perf`, `smoke-safari`; **6 of 6 CI-orphaned** (B3) |
| B8 | `grep -rn 'test.fail(' e2e/` | 3 legs: `smoke/oracles/o16-computed-cascade.spec.ts:34` · `smoke/oracles/o26-aurora-perceptibility.spec.ts:57` · `smoke/perf/o5-boot-pacing.spec.ts:48` |
| B9 | `ls -d demo/@/lib/palette` | `No such file or directory` — while `e2e/smoke/fixtures/browse-palettes.ts:19` and `e2e/smoke/admin/fixtures/admin-populated.ts:28` import types from it (+3 doc references) |
| B10 | `grep -rn 'e2e' tsconfig*.json eslint.config.*` | *(empty)* — `tsconfig.lib.json` includes `src/**`, `tsconfig.demo.json` includes `demo/` + `src/vite-env.d.ts`; **no program typechecks `e2e/`**, which is why B9 survived |
| B11 | `grep -n 'ref:' .github/workflows/deploy-pages.yml` | `deploy-pages.yml:80  ref: tranche/BG` under the comment *"un-pin at the 5.0.0 master landing"* (line 76) |
| B12 | `gh api 'repos/mkbabb/glass-ui/contents/package.json?ref=tranche/BG'` | **`version: 5.0.0`** — remote head re-verified, the audit seat's UNVERIFIED mark discharged |
| B13 | demo subpaths ∩ BG exports map | 18 subpaths used; **exactly `./blob` and `./chip` ABSENT** from the 5.0.0 exports map (`./blob` ×5 sites incl. `useAtmosphere.ts`; `./chip` ×1, `EasingSpecimenStrip.vue`). `package.json:83` declares `^7.0.0`; installed = `7.0.0`; registry latest = `7.0.0` |
| B14 | `gh run list --branch master --workflow ci --limit 5` | **5 of 5 `failure`**; most recent `29497700873` 2026-07-16T12:19:32Z |
| B15 | `gh run list --workflow deploy-pages.yml --limit 40` | **32 of 40 `skipped`**; last non-skipped = `failure` `29232912462` 2026-07-13T07:41:57Z; **last `success` = `28836880612` 2026-07-07T02:19:20Z (27 days stale)** |
| B16 | `git log -1 master` / `git rev-list --count master..HEAD` | master = `6abef800` (2026-07-13); **HEAD is 234 commits ahead** |
| B17 | `curl -s https://color.babb.dev/ \| grep -oE 'assets/[^"]+\.js'` | serves `assets/index-D9U9KwTn.js` — the artifact of the 2026-07-07 deploy (B15), i.e. **pre-v4** (`feat(package-v4)` landed 2026-07-16) |
| B18 | `grep -n maxNumericValue lighthouserc.json` | LCP 2500 · TBT 300 · CLS 0.1 · INP 200 — present and **read by no automation** (B3) |
| B19 | dev stack liveness | `curl -o /dev/null -w %{http_code} localhost:9000` → `200`; `localhost:3000` → `200` |

### Measured correction to CC-031's stated mechanism

The ledger and DR-09 both describe *"two source-broken fixtures"* whose repair precedes running the
suite. **Measured: the suite already collects clean.**

```
$ npx playwright test --list
Total: 185 tests in 71 files
```

Both dangling references (B9) are `import type` — erased at transpile, so Playwright's collector
never resolves them. The defect is real but is a **typecheck / provenance** defect, not a
collection failure, and its root is B10 (`e2e/` is in no typecheck program), not the fixture files.
X.W1.a therefore cures the root, and G-3's born-RED baseline is the **execution** result, which no
one has ever produced — not a collection error.

### MEASURE-AT-OPEN

Two baselines are deliberately not fabricated here:

- **G-3's pass/fail split of the 185.** Requires a full browser run against a built bundle. The
  first act of X.W1.a is to run it and commit the number. `--list` proves collection only.
- **G-13's gh-pages production-preview mount state.** Requires `npm run gh-pages` + a preview
  server. The standing record is CARRY-LEDGER §F's *"gh-pages prod-preview empty mount"* carry;
  the fresh measurement is taken at open and pasted into the wave log before any cure.

## Triumvirate Dispatch

Triumvirate (research + plan augment + redress) is **mandatory**, not optional, on any of:

- **File-bound expansion that invalidates the wave.** Any unit needing a write under `src/`,
  `demo/`, or `api/`. This wave is gates-and-workflows; a product-source edit means the diagnosis
  was wrong. The single sanctioned exception is the `e2e/` fixture cure in X.W1.a.
- **Non-local hard-gate failures.** (i) master CI still red after the merge for a cause outside
  `.github/workflows/` and the pack step; (ii) boot-smoke RED in production preview *after* the
  CC-034 pin lands — that falsifies the leading NV-7 root and the root is then unknown; (iii) the
  falsifier demonstration (G-7) failing to turn a job red.
- **Third-iteration diagnostic loops.** Any of: visual-golden flake triage, deploy-run
  green-chasing, or NV-7 root hunting reaching a third iteration halts and escalates.

## File Bounds

| File | Access |
|---|---|
| `.github/workflows/ci.yml` | modify |
| `.github/workflows/deploy-pages.yml` | modify |
| `e2e/visual/**` | create |
| `e2e/smoke/fixtures/browse-palettes.ts` | modify-carve |
| `e2e/smoke/admin/fixtures/admin-populated.ts` | modify-carve |
| `e2e/smoke/fixtures/user-auth.ts` | modify-carve |
| `e2e/smoke/admin/fixtures/admin-auth.ts` | modify-carve |
| `e2e/smoke/oracles/o16-computed-cascade.spec.ts` | modify-carve |
| `e2e/smoke/oracles/o26-aurora-perceptibility.spec.ts` | modify-carve |
| `e2e/smoke/perf/o5-boot-pacing.spec.ts` | modify-carve |
| `e2e/fixtures/palette-envelopes.ts` | create |
| `scripts/ci/boot-smoke.mjs` | create |
| `scripts/ci/oracle-slate.mjs` | create |
| `scripts/visual/regenerate-goldens.mjs` | create |
| `playwright.config.ts` | modify |
| `tsconfig.e2e.json` | create |
| `package.json` (scripts block only) | modify-carve |
| `docs/tranches/X/waves/W1-LOG.md` | create |
| `docs/tranches/X/evidence/w1/**` | create |

**Do NOT touch**: `src/`, `demo/`, `api/`, `test/`, `scripts/dev/dev.sh` (CC-021 — unowned, ruled
at the X-W0 sitting), `lighthouserc.json` (re-wired, never re-baselined — its thresholds are the
gate), `.github/workflows/release.yml`.

## Disjointness

No two units share a `modify` or `modify-carve` path.

| path | sole owner |
|---|---|
| `.github/workflows/ci.yml` · `playwright.config.ts` · `package.json` · `tsconfig.e2e.json` · `e2e/smoke/**` fixtures + `test.fail()` specs · `scripts/ci/oracle-slate.mjs` | X.W1.a |
| `e2e/visual/**` · `scripts/visual/regenerate-goldens.mjs` | X.W1.b |
| `scripts/ci/boot-smoke.mjs` | X.W1.c |
| `.github/workflows/deploy-pages.yml` | X.W1.d |
| `docs/tranches/X/evidence/w1/**` (per-unit subdirectories) | all — subdirectory-partitioned |

`ci.yml` is the collision risk: X.W1.b and X.W1.c both need CI jobs. **They do not write it.**
They deliver runnable artifacts with documented invocations; X.W1.a wires every job, which is why
X.W1.a runs in sub-wave 2 after both. X.W1.e writes no repo file (git operations + probes only).

## Worktree Plan

Sub-wave 1 runs three writers in parallel; sub-waves 2 and 3 are serial on clean main.

| Agent unit | Sibling worktree absolute path | CARGO_TARGET_DIR |
|---|---|---|
| X.W1.b | `/Users/mkbabb/Programming/value-js-x-w1-b` | n/a (no Rust) |
| X.W1.c | `/Users/mkbabb/Programming/value-js-x-w1-c` | n/a (no Rust) |
| X.W1.d | `/Users/mkbabb/Programming/value-js-x-w1-d` | n/a (no Rust) |

Orchestrator runs `git worktree list` then `git worktree add` before sub-wave-1 dispatch, and
integrates before dispatching X.W1.a. X.W1.a, X.W1.e, X.W1.f run in the primary tree.

**Model law (M-23)**: every unit here is an implementation seat → **Opus**. No design content lands
in this wave; X-W10 owns the twice-authored Fable∥Opus design track. Orchestration is Fable per
M-12 tri-fold.

## Agent Units

### X.W1.a CI verification surface (CC-031)

- **Goal**: the Playwright suite, the Lighthouse budgets, boot-smoke, and the visual oracle all
  execute on every push as hard jobs, and the suite's honest pass/fail is on the record.
- **Mechanism**: create `tsconfig.e2e.json` covering `e2e/` and add it to the `typecheck` script —
  this is the root cure for B9/B10. Extract the two dangling `Palette` shapes into
  `e2e/fixtures/palette-envelopes.ts` owned by e2e (the demo module is gone; re-pointing at a
  moved demo path would re-couple e2e to a tree X-W5/X-W8 will move again), and correct the three
  stale doc references. Run the full suite against a built bundle, commit the number. Add CI jobs
  `e2e-smoke`, `e2e-safari`, `visual`, `boot-smoke`, `lhci` — no `continue-on-error`, no
  branch-push substitution (D48 and D55(iv) waived exactly these; the waiver is what this wave
  exists to undo). Write `scripts/ci/oracle-slate.mjs`: read `playwright.config.ts`'s project list,
  read the workflow's project args, fail on any project named in one and not the other. Rule each
  `test.fail()` leg (B8) — real assertion or deletion, one line of rationale each.
- **Files**: `.github/workflows/ci.yml`, `playwright.config.ts`, `package.json` (scripts),
  `tsconfig.e2e.json`, `e2e/fixtures/palette-envelopes.ts`, the four fixture files, the three
  `test.fail()` specs, `scripts/ci/oracle-slate.mjs`.
- **Sub-gate**: G-1..G-7 GREEN; the falsifier commit hash and its red run URL are in the wave log.

### X.W1.b Visual regression oracle (CC-030, CC-029 half a)

- **Goal**: a pixel change in the shipped UI cannot reach master unobserved.
- **Mechanism**: author `e2e/visual/` as a Playwright project. Goldens are the routes with a named
  product consumer — Picker, Gradient, Browse, About — at 3 viewports (390, 1024, 3440) × light and
  dark. Tolerance stated numerically in the spec header with its rationale. Goldens COMMITTED
  (FM-12). `scripts/visual/regenerate-goldens.mjs` regenerates with an explicit `--accept` flag and
  refuses to run on a dirty tree. Every golden filename and the suite header carry the renderer
  string read from the running browser — SwiftShader goldens are labelled SwiftShader.
  **L-19 denominator discipline**: routes × viewports × schemes is a product-consumer matrix, not
  an audit cross-product; N is set by the routes that exist, and no golden is minted for a
  combination no user reaches.
- **Files**: `e2e/visual/**`, `scripts/visual/regenerate-goldens.mjs`.
- **Sub-gate**: G-8..G-10 GREEN; a one-pixel hand-edit to one golden turns the suite red, receipt
  in the wave log.

### X.W1.c Boot truth, both modes (CC-032)

- **Goal**: the white-screen failure class is structurally dead in dev *and* in the artifact that
  actually ships.
- **Mechanism**: `scripts/ci/boot-smoke.mjs` takes a mode (`dev` | `prod-preview`) and an origin.
  Prod-preview builds `npm run gh-pages` and serves `dist/gh-pages` at a bare `127.0.0.1:PORT`
  origin — bare, because a sub-path origin masks base-path defects. Four assertions per mode:
  `#app` has ≥1 element child · a `role=main` landmark exists · `page.on('pageerror')` collected
  `[]` · ≥1 desktop utility class is present in the emitted CSS. Diagnose NV-7's root and name it
  in the wave log; CC-034's pin is the leading candidate and X.W1.d tests it directly.
- **Files**: `scripts/ci/boot-smoke.mjs`.
- **Sub-gate**: G-12..G-14 GREEN; deleting the mount call in a scratch build turns prod-preview red.

### X.W1.d Deploy dependency truth (CC-034)

- **Goal**: the deployed bundle is built against the glass-ui the repo declares.
- **Mechanism**: replace the `actions/checkout` of `mkbabb/glass-ui@tranche/BG` (B11) and its
  build steps with the registry install the `^7.0.0` declaration already implies (B13). Retire the
  stale "un-pin at the 5.0.0 master landing" comment — the waypoint it names was skipped; glass
  went 4.2.0 → 7.0.0. Then run X.W1.c's prod-preview gate against a build produced by the amended
  workflow's exact steps.
- **Files**: `.github/workflows/deploy-pages.yml`.
- **Sub-gate**: G-15, G-16 GREEN; `./blob` and `./chip` resolve in the deploy build.

### X.W1.e Landing and deploy-of-record (CC-033)

- **Goal**: production serves the code this repository has, and it is provable it will keep doing so.
- **Mechanism**: merge `tranche-u` → `master` (234 commits, B16). Green master — the historical
  failure is the `pack producer bytes` step (`npm pack --json` polluted by the prepare script's
  vite banner), already fixed on `tranche-u`; if master reds for any other cause, triumvirate.
  Confirm ONE `deploy-pages` run with `conclusion: success` and `event: push` on master — B15's
  `if` gate has produced 32 consecutive `skipped` because it has never seen a green master push.
  Re-probe `color.babb.dev` and assert v4 code. Add a standing CI check comparing the last
  successful `deploy-pages` timestamp against the newest close date — red when the deploy is older.
- **Files**: none (git operations, `gh` probes, and the wave log). The standing check is a job
  X.W1.a wires from this unit's script contract.
- **Sub-gate**: G-17..G-21 GREEN with run IDs pasted.

### X.W1.f Real-GPU oracle session (CC-029 half b)

- **Goal**: the six-close carry of the real-GPU oracle terminates in this wave — run or tombstoned,
  never carried.
- **Mechanism**: at wave-open the orchestrator books a dated ≤30-minute owner session, headed
  Chrome on the owner's machine, walking the four golden routes and capturing N frames plus a
  signed checklist. Frames commit to `docs/tranches/X/evidence/w1/real-gpu/`. **If the session has
  not occurred by close, the row is RETIRED with a tombstone quoting DR-07's chain (N X14 → R R8-22
  → S → T O-3 → U-F54/B8 → V-prime CH-7) and the reason: named six times, run zero times, naming is
  not discharge.** No seventh carry, and no re-framing as a future automated job — that framing has
  failed six times.
- **Files**: `docs/tranches/X/evidence/w1/real-gpu/**`, wave log.
- **Sub-gate**: G-11 GREEN by either arm — session receipts, or the tombstone with a date.

## Hard Gate

All 21 conditions are **born-RED**: every one names a defect live at `41450f02`. Each carries its
falsifier per **L-19** — the specific way it can go red for the reason it exists.

| # | CC | Condition | Born-RED baseline | Falsifier (how it fails for its reason) |
|---|---|---|---|---|
| G-1 | 031 | `npm run typecheck` covers `e2e/` and passes; zero references to `demo/@/lib/palette/types` remain | B9, B10 — target absent, 2 live type-imports + 3 doc refs, no program typechecks `e2e/` | Delete `e2e/fixtures/palette-envelopes.ts` → typecheck reds. A path-only re-point that leaves `e2e/` unprogrammed does **not** pass G-1 |
| G-2 | 031 | `e2e-smoke` and `e2e-safari` run as required jobs; no `continue-on-error`, no branch-push substitution | B3 = 0, B4 = 2 jobs | Add `continue-on-error: true` → G-7's falsifier stops reddening the run; the gate is inspected for the key's absence, not asserted by grep alone |
| G-3 | 031 | The suite's real pass/fail over 185 tests is executed and committed as the baseline | MEASURE-AT-OPEN — never run; `--list` collects 185/71 but execution count is unknown to the corpus | A baseline copied from any prior document instead of a run: the committed number must carry its run ID and duration |
| G-4 | 031 | LHCI executes against `lighthouserc.json` unmodified; its four budgets are the pass condition | B18 — thresholds present, read by nothing | Raise `largest-contentful-paint` above the measured value → the gate stops meaning anything; re-baselining `lighthouserc.json` is an automatic G-4 failure |
| G-5 | 031 | `scripts/ci/oracle-slate.mjs` fails when a Playwright project has no CI job | B6 absent; B7 = 6 projects, 6 orphaned | Add a 7th project to `playwright.config.ts` without a job → slate reds. If it does not, it is grep-theatre |
| G-6 | 031 | The three `test.fail()` legs are each a real assertion or deleted, with recorded rationale | B8 — 3 legs live | A leg converted to `test.skip()` fails G-6: skipping is the same deferral under a new name |
| G-7 | 031 | **Falsifier demonstration**: a deliberately broken spec on a scratch branch turns the e2e job red; the run URL is committed | Never demonstrated — D48 waived it via `continue-on-error`, D55(iv) via branch-push substitution | This gate *is* the falsifier. It fails by the scratch run going green — which would prove the jobs are decorative |
| G-8 | 030 | `e2e/visual/` exists with goldens COMMITTED for the named routes × 3 viewports × light/dark | B1 absent; B2 = 0 assertions / 71 spec files | A golden set generated but git-ignored fails G-8 (FM-12: untracked evidence is not evidence) |
| G-9 | 030 | Tolerance is stated numerically with rationale; `scripts/visual/regenerate-goldens.mjs` requires `--accept` and refuses a dirty tree | no suite exists | Set tolerance high enough to absorb a visible change → a hand-injected 20px block passes. The tolerance is validated by that injection, not by assertion |
| G-10 | 029a | The visual suite runs on every push; each golden and the suite header record the renderer string read from the live browser | six closes, zero runs | Goldens minted under a renderer that is not recorded, or recorded from config rather than the browser, fails G-10 — renderer-honesty is the whole point of the software-GL half |
| G-11 | 029b | The real-GPU session ran (frames + signed checklist committed) **or** the row is RETIRED with a dated tombstone | zero sessions in six closes | Fails by a third state — "carried to X-W11", "scheduled for next window". Only run-or-tombstone closes it |
| G-12 | 032 | `boot-smoke.mjs --mode=dev` passes all four assertions against a cold dev boot | B5 — script deleted at `6d6d3521`; successor witness covers dev only | Comment out the `app.mount()` call in a scratch build → dev mode reds |
| G-13 | 032 | `boot-smoke.mjs --mode=prod-preview` passes all four assertions against `dist/gh-pages` served at bare `127.0.0.1` | MEASURE-AT-OPEN; standing record = CARRY-LEDGER §F "gh-pages prod-preview empty mount" | Serving the preview under a sub-path instead of a bare origin makes a base-path defect invisible — the origin form is checked, and the same mount deletion must red this mode too |
| G-14 | 032 | NV-7 is re-classed RED and its root named in the wave log with the evidence that identifies it | booked as a carry, root undiagnosed | A root asserted without a differential test fails G-14; X.W1.d's pin is a *candidate* until G-16 shows the probe flips |
| G-15 | 034 | `deploy-pages.yml` resolves `@mkbabb/glass-ui` from the registry; no `ref: tranche/BG` | B11 line 80; B12 = 5.0.0; B13 = `./blob` + `./chip` absent from that exports map while the demo imports both | Revert to the branch checkout → the deploy build fails to resolve `./blob` at `useAtmosphere.ts` |
| G-16 | 034 | CC-032's prod-preview probe runs against a build produced by the amended workflow's exact steps and is GREEN | never run post-pin | Passing this while G-13 is red on the same commit means the two builds differ — both must run on one commit |
| G-17 | 033 | The `ci` workflow is green on `master` | B14 — 5 of 5 latest master runs `failure` | A green obtained by removing or softening the `pack producer bytes` step fails G-17 — the fix is on `tranche-u`, and the step's presence is checked |
| G-18 | 033 | `tranche-u` is merged to `master` | B16 — master `6abef800` (2026-07-13), HEAD +234 | A merge that drops commits: `git rev-list --count master..HEAD` must read `0` after |
| G-19 | 033 | ONE `deploy-pages` run with `conclusion: success`, `event: push`, `head_branch: master`, dated in this wave | B15 — 32/40 `skipped`; last success `28836880612`, 2026-07-07 (27 days) | A `workflow_dispatch` success does not satisfy G-19 — the `if` gate's push arm is what has never fired, and dispatch would mask that |
| G-20 | 033 | `color.babb.dev` serves v4 code | B17 — entry `assets/index-D9U9KwTn.js`, artifact of the 2026-07-07 deploy, pre-v4 | Fails if the entry asset hash is unchanged after G-19's run: a green deploy that does not change the served bytes did not deploy |
| G-21 | 033 | A standing check reds when the last successful `deploy-pages` predates the newest close | no gate measures deploy age; that is why B15 went unobserved for 27 days | Back-date the comparison input by a day → the check reds. A check that only reports and never fails does not satisfy G-21 |

**Gate totals: 21 conditions · 21 born-RED · 0 blocked rows.**

## Format And Lint Cadence

- After each integration batch and before close: `npm run lint` (`eslint . --max-warnings=0`) and
  `npm run typecheck` — the latter newly covering `e2e/` via `tsconfig.e2e.json` (G-1).
- `npx prettier --check` on every touched `.ts` / `.yml` / `.json`.
- `git diff --check` before every commit.
- YAML: `actionlint` on both workflow files after each edit; if unavailable in the environment, the
  replacement evidence is a pushed scratch-branch run of each amended workflow, URL in the wave log
  — a workflow that parses only in the linter's opinion is not evidence.
- Goldens are excluded from prettier by path; their integrity evidence is G-9's injection test.

## Verification Artefacts

Saved under `docs/tranches/X/evidence/w1/`:

- `baseline/playwright-full-run.txt` — G-3's first execution, with run ID and duration.
- `baseline/probes-2026-08-03.md` — B1..B19 re-run at open with fresh timestamps.
- `falsifier/` — the scratch-branch commit hash, the broken-spec diff, and the red run URL (G-7);
  the one-pixel golden edit and its red run (G-9); the mount-deletion diff and its two red runs
  (G-12, G-13).
- `visual/` — golden manifest with the renderer string and per-file hashes.
- `real-gpu/` — session frames + signed checklist, **or** `TOMBSTONE.md` with its date (G-11).
- `deploy/` — `gh run view` output for the G-19 run; `curl` receipts for `color.babb.dev` before
  and after (G-20).
- `nv-7-root.md` — the named root and its differential evidence (G-14).
- `docs/tranches/X/waves/W1-LOG.md` — per-gate transitions with commit hashes.

## Commit Plan

| # | scope | contents | body required |
|---|---|---|---|
| 1 | `test(e2e/typecheck)` | `tsconfig.e2e.json`, `e2e/fixtures/palette-envelopes.ts`, fixture + doc-ref cures | yes — names the B9/B10 root |
| 2 | `test(e2e/visual)` | `e2e/visual/**`, goldens, regeneration script | yes — generated output; states tolerance, renderer, and golden count |
| 3 | `ci(boot-smoke)` | `scripts/ci/boot-smoke.mjs` | yes — restores a deleted structural guard; cites `d9c3b9f2` and `6d6d3521` |
| 4 | `ci(deploy-pages/glass-pin)` | registry resolution replacing `ref: tranche/BG` | yes — gate change; cites B12/B13 |
| 5 | `ci(verification-surface)` | `ci.yml` jobs, `oracle-slate.mjs`, `playwright.config.ts`, `package.json` scripts, `test.fail()` rulings | yes — broad + gate change; lists each job and each ruled leg |
| 6 | `docs(X·W1 · evidence)` | falsifier receipts, baselines, `nv-7-root.md`, real-GPU frames or tombstone | yes — evidence commit |
| 7 | *(merge)* `tranche-u` → `master` | the landing | yes — 234 commits; names the pack-step fix |
| 8 | `docs(X·W1 · close)` | `W1-LOG.md` final, gate table with verdicts, four-verb status advanced to VERIFIED | yes — status change |

Commits 1–6 are pathspec-scoped. `scripts/dev/dev.sh` is never staged (CC-021).

## Dependencies

- **Depends on**: **X-W0** — for CC-012's track-or-archive (evidence trees this wave writes into
  must be tracked), CC-025's completeness cure (no `264/264` may appear in any gate here; the split
  is 218 CHALLENGED + 46 REPORT-AUTHORED), and CC-021's ruling on `scripts/dev/dev.sh` (until
  ruled, NEVER touch). No product dependency on any other X wave.
- **Blocks**: **X-W2** (CC-036's p75-LCP-over-N≥20 gate needs LHCI and a pinned runner class from
  G-4; CC-035's eager-bytes delta needs G-13's prod-preview build) · **X-W5** (CC-054's
  frame-budget oracle and CC-048's measured viewport cap need the browser gate) · **X-W6**
  (CC-065's deltaE2000 atmosphere oracle and CC-067's cold-load capture need G-8's harness and
  committed frames) · **X-W7** (CC-073's export-path defects need the e2e surface) · **X-W11**
  (the release battery is this wave's gates run once more). Decidability for CC-015, CC-029,
  CC-030, CC-032, CC-036, CC-065, CC-073 originates here.

## Archaeology

This wave is the seventh attempt at CC-029's class and the fourth at CC-031/CC-032's.

| prior attempt | failure mode | guardrail here |
|---|---|---|
| T built 5 CI jobs (`6e14e90c`, 593 lines) | un-wired at `164343c1` (2026-07-17 03:19, −702 lines), deleted at `6d6d3521` as "prune proof-theater" | G-5's oracle slate reds when a project loses its job; deletion cannot be silent |
| V-prime bracket B3 booked three re-gates | delivered two with **both** falsifiers waived — D48 `continue-on-error`, D55(iv) branch-push substitution — and never built the third | G-7 makes the falsifier demonstration a gate, and G-2 forbids exactly the two waiver mechanisms by name |
| `boot-smoke.mjs` (inv-N-1, `d9c3b9f2`) | deleted at `6d6d3521` as "CI-orphaned; W44 routed-mount witness supersedes"; 11 hours later W44 closed booking a prod empty mount as a carry | G-13 covers the mode the successor witness never did; G-5 makes "CI-orphaned" a red gate rather than a deletion warrant |
| DR-07 across N → R → S → T → U → V-prime | U's G-CLOSE-4 made *naming* the obligation the discharge, so each close passed by re-naming the row | G-11 admits only two terminal states, run or tombstoned; "carried" is not one |
| DR-22 across S → T.W7 → U-F2 → V-prime W44 | attached to a producer-tag trigger that never fired in the form written (FM-08 + FM-19); the named waypoint (5.0.0 master landing) was skipped, glass went 4.2.0 → 7.0.0 | G-15 is unconditional — no trigger, no waypoint; the remote head was re-verified at authoring (B12) |
| DR-11 across M → N → R → S → T.W0 (`bdfb4a5`) | recurred silently: every close reported CI green **on its own branch**, and no gate measured deploy age | G-21 makes deploy age a red gate; G-19 requires the push arm, not a dispatch |
| D.W4's 0% pixel-drift gate | NOT EXECUTED — "pixel-isomorphic by construction", to preserve a 120-min cap; the substituting analysis conceded two non-isomorphic changes | G-9's injection test: a tolerance is validated by a deliberate visible change, never by argument |

Also carried: `vnext`'s 193 gate commands named a runner (`.vnext/proof-runner.mjs`) that does not
exist. Every gate above names a command that exists in this repo after this wave, or that this wave
creates by name in §File Bounds.

## L-18 rider — closing condition

Gates GREEN does **not** make this wave ACCEPTED. Per **L-18**, the implementation must survive
**two quartet challenge passes** — four Opus skeptics per pass, hunting for the failure modes this
wave's own archaeology names (waived falsifiers, decorative jobs, branch-scoped greens, tolerance
absorption, renderer dishonesty, third-state carries) — followed by a **fresh Fable apotheosis**
that has not seen the implementation. Only after that does the four-verb status advance to
VERIFIED and CC-029..CC-034 report terminal to the CARRY-CUT-LEDGER.

Reference, not ceremony: the challenge passes are dispatched by the orchestrator at gate-green, and
their findings land as wave-log entries with dispositions, not as a separate document.
