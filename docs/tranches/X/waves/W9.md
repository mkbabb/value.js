# X.W9 - Parser and library apotheosis (the 4.1 cut)

## State

**Name**: W9 - Parser and library apotheosis (the 4.1 cut)
**Opens after**: X-W0 (formation rulings + track-or-archive). Order-independent of X-W2..X-W8.
**Agents**: 9 units — 4 parallel lanes, 2 serial chains (§4a)
**Hard gate**: 33 numbered conditions (§6); **31 born RED, measured read-only this seat**
(`src-surface-totality.mjs` exit 1 / 28 assertions · `library-band-gates.mjs` exit 1 / 12 ·
`consumer-surface-compile.mjs` exit 1 / 4); 2 are declared GREEN-at-authorship fences with named
RED inputs.
**AUDITED**: yes — carry-cut ledger §1.K + `registry/adjudicated/library-band.md` (M-12 tri-fold).
**SPECIFIED**: yes — this file, 2026-08-03.
**IMPLEMENTED**: no.
**VERIFIED**: no.
**Status**: planned

Substrate for every baseline below: `tranche-u` @ `41450f02`, node v26.0.0, darwin arm64, built
`dist/subpaths/`. Every number in §6 was executed by this seat, read-only, on that substrate,
except the three explicitly marked MEASURE-AT-OPEN.

## Goal criterion

This wave succeeds if the published library is **total on its own declared boundary** — no public
entry throws on a string, no public signature returns a value its own `.d.ts` forbids, and every
type a subpath returns is nameable from that subpath — and that surface ships as **one dated 4.1.0
cut** whose exact-pin consumers were notified by packet before the tag.

## Scope

1. Cure the four prototype-reachable lookup sites and the empty-body colour-function crash; every
   `./css` entry returns a typed failure instead of a `TypeError`.
2. Enable `@typescript-eslint/no-non-null-assertion` under `src/css/`, then under `src/transform/`
   and `src/foundation/` after their retirements.
3. Make `./transform` total: M-less paths, truncated command runs, positional arc flags, singular
   3D matrices.
4. Give `./math` one stated, enforced precondition policy.
5. Land the public-surface law: derived subpath barrels, zero bare `declare`, zero `_2` mangles,
   the colour boundary, `serializeCssValue` published, the `stylesheet.ts` split, the R-T1 ratchet.
6. Restore external-vector colour oracles; delete the falsely-named circular oracle; publish
   measured coverage-by-export over a recorded denominator.
7. Give `verify-packed-surface.mjs` a behavioural half and delete its hardcoded `strictTypes: 62`.
8. Ship SCI-1 (`sampleColorRamp` / `mixColorsInto` / `toRgba8Into`), `toHex`, `easingNames()`,
   memoised `easing()`, the restored analytic in/out arms, as **value 4.1.0** — one date, one bump.
9. Retire the unused transform matrix family; preserve `PathGeometry` for keyframes MorphSVG.
10. Reconcile canon with the tree: delete every tracked reference to the non-existent
    `src/parsing/`; record the parse-that decree against the actual dependency set.
11. Cure the ICtCp/Jzazbz channel-descriptor mis-resolution against the library's exact channel ids.
12. Author and send the five coordination packets; notify every exact-pin consumer before the tag.

## Triumvirate Dispatch

Mandatory triumvirate (research + plan augment + redress); the orchestrator may not redispatch the
failing unit alone.

- **File-bound expansion that invalidates the wave**: any cure that requires editing
  `src/color/model.ts`'s conversion mathematics, or that adds a file under `src/` not named in §4.
  The colour model is not in scope; a cure that reaches it means the crash class was mis-rooted.
- **Non-local hard-gate failures**: G12 (consumer-compile) still RED after PSL-1 derivation lands —
  that means the exports map, not the barrels, is the defect and the topology ruling is wrong;
  G16 (ratchet) rising instead of falling after the split; G24 (analytic-arm drift) failing to
  reach `<1e-3` against 0.13.0 — that reopens RD-5, which is an adjudication, not an edit.
- **Third-iteration halt**: any diagnostic loop on the `stylesheet.ts` split seam (the measured
  cycle-free seam is 265/381/153 with two cross-seam edges; a third re-cut attempt halts), and any
  third attempt to make the packed tarball resolve a root specifier.

## File Bounds

| File | Access |
|---|---|
| `src/css/grammar.ts` | modify |
| `src/css/stylesheet.ts` | modify-carve (split) |
| `src/css/named-colors.ts` | modify |
| `src/css/index.ts` | modify |
| `src/css/types.ts` | modify |
| `src/css/timeline.ts` | modify |
| `src/easing.ts` | modify |
| `src/foundation/math.ts` | modify |
| `src/foundation/result.ts` | modify |
| `src/transform/path.ts` | modify |
| `src/transform/decompose.ts` | modify-carve (matrix family deletion) |
| `src/color/index.ts` | modify |
| `src/color/operations.ts` | modify |
| `src/subpaths/*.ts` | modify (PSL-1 derivation) |
| `src/css/serialize.ts`, `src/css/rules.ts` | create (split products; exact names bind at open) |
| `test/parser-totality.test.ts` | create (RED-first battery) |
| `test/transform/path-geometry.test.ts`, `test/transform/decompose-targeted.test.ts` | modify |
| `test/math.test.ts`, `test/easing-export-stability.test.ts` | modify |
| `test/v4-color-behavior.test.ts` | modify |
| `test/color-anchors.test.ts` | create (external vectors) |
| `scripts/ci/verify-packed-surface.mjs` | modify |
| `eslint.config.js` | modify (two scoped rule objects) |
| `package.json`, `CHANGELOG.md` | modify |
| `demo/color-session/colorSpaceInfo.ts` | modify |
| `demo/picker/controls/ComponentSliders/ConsoleRail.vue` | modify |
| `docs/tranches/V/ARCHITECTURE.md` | modify-carve (`:657` region only) |
| `docs/tranches/V/coordination/*-inbox-2026-*.md` | create (5 packets) |
| `docs/tranches/V/coordination/INBOX.md` | modify (packet rows) |
| `docs/tranches/X/waves/W9.md` | modify (status fields at close) |

**Do NOT touch**: `scripts/dev/dev.sh` (unowned, CC-021 rules it at X-W0) · `api/**` (X-W3) ·
`e2e/**` and `.github/workflows/**` (X-W1) · `demo/**` except the two ICtCp files above ·
`docs/tranches/V/ARCHITECTURE.md:943-945` (the cap sentence is X-W8's, CC-082) ·
`src/color/model.ts` · any peer repository tree (packets only, RD-11).

## Disjointness

Two serial chains, four free lanes. No two units hold `modify` on the same path at the same time.

- **Chain A (serial, load-bearing)**: `X-W9.a` → `X-W9.d` → `X-W9.f`. All three write
  `src/css/stylesheet.ts`; `.a` and `.f` both write `src/easing.ts`; `.d` and `.f` both write
  `src/subpaths/*`. The band's ruling is binding: curing MTS-01 **unmasks** the
  `stylesheet.ts:163-171` twin, so the sites land together, and the split must not move that twin
  mid-cure; the cut must not publish a surface under repair.
- **Chain B (serial)**: `X-W9.b` → `X-W9.f` (`src/transform/*` retirements precede the cut).
- **Free lanes** (parallel with either chain): `X-W9.c` (`src/foundation/math.ts`), `X-W9.e`
  (`test/**` + `scripts/ci/`), `X-W9.g` (docs), `X-W9.h` (two demo files), `X-W9.i` (coordination).
- **Cross-wave**: `docs/tranches/V/ARCHITECTURE.md` is written by X-W8 (cap sentence, `:943-945`)
  and by this wave (`:657` region). The two waves may not run concurrently on that file. This wave
  carves `:657` and touches nothing else in it; if X-W8 is open, X-W9.g sequences after its close.

## Worktree Plan

No Rust; the per-agent isolation variable is the build output directory, since `npm run typecheck`
runs `prepare`/`build` first and two agents sharing one `dist/` measure each other.

| Agent unit | Sibling worktree absolute path | Build dir (`VALUE_DIST`) |
|---|---|---|
| X.W9.a | `/Users/mkbabb/Programming/value.js-x-w9-a` | `<worktree>/dist` |
| X.W9.b | `/Users/mkbabb/Programming/value.js-x-w9-b` | `<worktree>/dist` |
| X.W9.c | `/Users/mkbabb/Programming/value.js-x-w9-c` | `<worktree>/dist` |
| X.W9.e | `/Users/mkbabb/Programming/value.js-x-w9-e` | `<worktree>/dist` |
| X.W9.h | `/Users/mkbabb/Programming/value.js-x-w9-h` | `<worktree>/dist` |

`X.W9.d`, `X.W9.f` (chain heads after integration), `X.W9.g` and `X.W9.i` run on integrated main —
`.d`/`.f` because they consume their predecessor's landed bytes, `.g`/`.i` because they are
docs-only. The orchestrator runs `git worktree list` and `git worktree add` before dispatch.

## Agent Units

Model law M-23: every unit below is an **Opus 5** implementation seat. No design content is
authored here (X-W10 owns design and is twice-authored Fable ∥ Opus → fresh Fable); this wave's
adjudications are already ruled in `registry/adjudicated/library-band.md` §2–§3 and are applied,
not re-opened.

### X.W9.a Prototype-reachable lookups and empty-body colour totality

- **Goal**: no string reaches a public `./css` or `./easing` entry and produces a `TypeError`.
- **Mechanism**: `Object.create(null)`-filled tables (or `Map`) at `src/css/named-colors.ts:1`,
  `src/css/grammar.ts:265-266,:457-462`, `src/css/stylesheet.ts:163-171`, `src/easing.ts:166-170`,
  with `typeof x === "string"` / `map.has(k)` narrowings; `src/css/grammar.ts:181` returns the typed
  failure `Result` instead of `slash[0]!`; the 56-vector empty-body/whitespace-body battery lands
  RED **first**, in its own commit, and is watched failing before any cure. Enable
  `@typescript-eslint/no-non-null-assertion` scoped to `src/css/**` in `eslint.config.js`.
- **Files**: `src/css/named-colors.ts`, `src/css/grammar.ts`, `src/css/stylesheet.ts`,
  `src/easing.ts`, `src/foundation/result.ts`, `eslint.config.js`, `test/parser-totality.test.ts`.
- **Sub-gate**: G1, G2, G3, G4, G5 all green; the RED-first commit is in history with its failing
  output pasted in the body.

### X.W9.b Transform totality and the matrix-family retirement

- **Goal**: `./transform` is total over malformed path text and honest about its own return types.
- **Mechanism**: `tokenizePath` rejects non-multiple-of-arity runs instead of truncating (deletes 12
  asserted reads); arcs tokenize **positionally** (SVG 1.1 §8.3.9 single-char flags — what
  SVGO/Figma/Illustrator emit); `decomposeMatrix3D` joins its own null ladder
  (`if (scaleX===0||scaleY===0||scaleZ===0) return null`, as the 2D sibling already does); delete
  `decomposeMatrix2D`/`decomposeMatrix3D`/`recomposeMatrix2D`/`recomposeMatrix3D`/
  `interpolateDecomposed`/`slerp` (CC-094 — measured zero consumers in `demo/` and
  `../keyframes.js/src`), preserve `PathGeometry`/`getTotalLength`/`getPointAtLength` (measured 4+1
  keyframes seams); then enable `no-non-null-assertion` scoped to `src/transform/**` and
  `src/foundation/**`.
- **Files**: `src/transform/path.ts`, `src/transform/decompose.ts`, `src/subpaths/transform.ts`,
  `eslint.config.js`, `test/transform/*.test.ts`.
- **Sub-gate**: G6, G7, G8, G9, G10, G27 green; no shim, no forwarding export for the deleted six.

### X.W9.c `./math` failure protocol

- **Goal**: `./math` states one precondition policy and enforces the sentence already written at
  `src/foundation/math.ts:58`.
- **Mechanism**: enforce the documented length/arity contract at the boundary (`deCasteljau`,
  `interpBezier`, `lerpArray`); move `scale`'s equal-bounds guard above its own division; retire
  the 7 dangerous assertions. `lerpArray` is keyframes' FrameCompiler hot loop — the length leg is
  asked of keyframes in the W9.i packet, never assumed here.
- **Files**: `src/foundation/math.ts`, `src/subpaths/math.ts`, `test/math.test.ts`.
- **Sub-gate**: G11 green; the policy is stated once in the module docstring and enforced by code,
  not by a comment.

### X.W9.d Public-surface law and module topology

- **Goal**: every type in a published signature is nameable from the subpath that publishes it, and
  the largest god module is gone.
- **Mechanism**: **PSL-1** — `src/subpaths/*.ts` become star-forwards over the area barrel; the area
  barrel is the one place public/internal is decided (two hand-kept lists become one derivation).
  **PSL-2** — zero bare `declare` in emitted `.d.ts`; `./css` re-exports `CssValue`/`CssScalar`/
  `CssCall`/`CssList`; `ColorFactory` published from `./color`. **Colour boundary** — `color/model`
  reachable from `src/color/` only; exactly 3 statements move (`src/css/types.ts:1`,
  `src/css/grammar.ts:18,:19`); `anchors.ts`/`operations.ts` keep their relative `./model` imports
  (routing them through the barrel is a cycle). **Split** — `stylesheet.ts` 899 → the measured
  cycle-free seam 265/381/153, two cross-seam edges, after X-W9.a. `serializeCssValue` joins the
  `Result` idiom **before** it is exported; hoist the six double `declarations.get()` at
  `stylesheet.ts:709-717`; `isSupportedSyntaxDescriptor` goes `@internal`; `./value` is not deleted
  and `isLayoutTrackingUnit` does not move (shape (ii) rejected, closed).
- **Files**: `src/subpaths/*.ts`, `src/css/index.ts`, `src/css/types.ts`, `src/css/grammar.ts`,
  `src/css/stylesheet.ts` (+ split products), `src/color/index.ts`, `src/color/operations.ts`.
- **Sub-gate**: G12, G13, G14, G15, G16, G17 green; `ParseResult` (text→AST) and `Result`
  (value→value) stay **declared per boundary, never unified** (PSL-3).

### X.W9.e Oracle truth, coverage truth, packed-surface truth

- **Goal**: no test asserts the implementation's own constant under an independent-oracle name, and
  the packed artifact is checked for behaviour, not for key presence.
- **Mechanism**: restore external reference vectors for the v4 conversion API (the audit record at
  `docs/tranches/U/audit/oracle/color-anchors/README.md` still describes them) plus the 34-pair
  Sharma CIEDE2000 table; rename or fix `test/v4-color-behavior.test.ts:66`; publish measured
  coverage-by-export over a **recorded denominator command** (this seat measures 142 declared export
  names across `dist/subpaths/*.d.ts` and 79 runtime exports across the 7 subpaths — the wave pins
  the command, not the remembered "141"); give `scripts/ci/verify-packed-surface.mjs` one smoke
  invocation per runtime export in the tarball and delete the hardcoded `strictTypes: 62` at `:137`.
- **Files**: `test/v4-color-behavior.test.ts`, `test/color-anchors.test.ts`,
  `scripts/ci/verify-packed-surface.mjs`.
- **Sub-gate**: G18, G19, G20 green. The coverage number is **published**, not gated on a threshold
  — a coverage floor with no consumer is L-19 contrivance.

### X.W9.f The 4.1.0 cut

- **Goal**: one dated event puts the cured surface and the eight-close SCI-1 debt in consumers'
  hands together.
- **Mechanism**: SHIP — SCI-1 (`sampleColorRamp`/`mixColorsInto`/`toRgba8Into`, with the measured
  atlas evidence tuple `keyframes backward-color.ts:171/:250/:263 count=1024` in the **same**
  commit); `toHex` on `./color`; `easingNames()`; memoised `easing()` (stable references); the
  restored analytic in/out arms (RD-5); the barrel corrections; the AM-13 dependency strip.
  DECLINE — `sampleBezier` (permanently, measured zero demand); `resolveCssColor` (RD-6, with its
  runnable second-consumer re-trigger preserved). FENCE — `bezierPresets`' 30-key set and the
  40-name catalog. Version **4.1.0**, dated, CHANGELOG'd; **no emergency 4.0.1** (ruled); 4.0.0 is
  immutable and stays so.
- **Files**: `package.json`, `CHANGELOG.md`, `src/color/*`, `src/easing.ts`, `src/subpaths/*.ts`.
- **Sub-gate**: G21, G22, G23, G24, G25, G26, G28, G29 green; the cut is ONE commit range with one
  version bump; the bench table is published under G28's restated denominator or it is not
  published at all.

### X.W9.g Canon-truth diff and the parse-that reconciliation

- **Goal**: no tracked canon sentence describes a tree that does not exist, and the parse-that
  decree is reconciled against the actual dependency set in writing.
- **Mechanism**: delete/repoint the `src/parsing/**` citations in
  `docs/tranches/V/ARCHITECTURE.md:657` (measured: `src/parsing/` absent, zero `.bbnf` files in the
  tree, the CSS parser is `src/css/grammar.ts`); record the parse-that position against ground truth
  — `PAUSED_RESEARCH` / `TERMINAL_SOURCE_RED` / `NO_ACTIVE_WRITER`, fresh writer root
  `parse-that-css-totality-p2` (M-22 §3), the `≥10x` floor retired as admission law and the
  replacement portfolio (strict-3x / strict-2x / measured-break-even) governing. This wave does not
  adopt a parser; it records why value.js ships none of parse-that today.
- **Files**: `docs/tranches/V/ARCHITECTURE.md` (`:657` carve only).
- **Sub-gate**: G30, G31 green.

### X.W9.h ICtCp and Jzazbz channel-descriptor resolution

- **Goal**: every channel of every space resolves to exactly its own description.
- **Mechanism**: `componentDescription()` at
  `demo/picker/controls/ComponentSliders/ConsoleRail.vue:172-180` prefix-matches
  (`c.startsWith(upper) || c.startsWith(component)`), which resolves `cp` → `"Ct (tritan)"` and
  degrades `jz` → `"jz"`. Re-key the descriptor tables in `demo/color-session/colorSpaceInfo.ts` by
  the library's **exact** channel ids — already published in the `ChannelsBySpace` type
  (`ictcp: [i, ct, cp]`, `jzazbz: [jz, az, bz]`) — and delete the prefix `find`. No new library
  export: the id vocabulary already exists and a second one would be a duplicate authority.
- **Files**: `demo/color-session/colorSpaceInfo.ts`,
  `demo/picker/controls/ComponentSliders/ConsoleRail.vue`.
- **Sub-gate**: G32 green over all 17 spaces × their channel ids, executed under node (no browser).

### X.W9.i Coordination packets

- **Goal**: every exact-pin consumer learns of the 4.1 surface from a packet with runnable gates
  before the tag, not from a resolution error after it.
- **Mechanism**: RD-11 — packet mechanism, O's content. Five packets: **keyframes.js** (widened R1
  input class + the prototype class with per-site verdicts at `resolve/browser.ts:165`,
  `engine/options.ts:31`, `compile/value-ast.ts:71`; `serializeCssValue` published so the diverged
  fork at `compile/emit/css-text.ts:41` retires; K1–K4 with runnable gates; the catalog fence and
  the declared analytic-arm restoration, 8 names, max|Δ| 0.192; the pin-bump window;
  the `lerpArray` length-assertion ask), **atlas** (16 root-specifier statements, 4 absent symbols,
  the exports-map delta), **glass-ui** (standing BH/BI relay — 7.0.0 imports `parseCssColor` and
  feeds it user-supplied strings; joins the owed R1 relay), **fourier-analysis** (facility 19 under
  RD-8's one-way correspondence law, populated from the I-1..I-10 table, `counts` annotated
  as-specified with the as-built census — http = **30** at HEAD, not 41), **parse-that** (the
  evidence packet on the standing 1.1.0 ask: PT-01, PT-03, PT-04, PT-07). Logged per E13 in
  `docs/tranches/V/coordination/INBOX.md`. **No peer-repo file is edited by this wave.**
- **Files**: `docs/tranches/V/coordination/*-inbox-2026-*.md` (5 create),
  `docs/tranches/V/coordination/INBOX.md`.
- **Sub-gate**: G33 green — five packets authored and sent, five rows in the ledger, and the
  keyframes exact pin (`../keyframes.js/package.json:69` reads `"4.0.0"` today) acknowledged in the
  packet before the tag.

## Hard Gate

Every gate names its command, its state today, and how it can fail for its intended reason (L-19).
Baselines marked **[measured]** were executed by this seat, read-only, at `41450f02`.

| # | Gate | Command | Born state | Falsifier |
|---|---|---|---|---|
| G1 | No public `./css` entry throws on any string | `node docs/tranches/V/megatranche/audit/probes/src-surface-totality.mjs` | **RED [measured]** — exit 1, **28 failing assertions** | Restore any bare index read or `name in TABLE` lookup on a parse-derived key; a battery input throws again |
| G2 | Empty-body colour functions return typed failure | 56-vector battery in `test/parser-totality.test.ts` | **RED [measured]** — `oklch() rgb() hsl() lab() color() rgba() lch() oklab() hwb()` all `TypeError: Cannot read properties of undefined (reading 'replace')`, 9/9 | Revert `grammar.ts:181` to `slash[0]!`; the battery reddens on exactly those inputs |
| G3 | Prototype-key totality across all 7 subpaths | `node …/probes/library-band-gates.mjs` (LIB-02 leg) | **RED [measured]** — **33** published functions throw on a JS-boundary value (value 1 · css 18 · easing 1 · math 5 · transform 8) | Add a public function that indexes an object literal by an unvalidated argument |
| G4 | `easing()` total over `Object.prototype` keys | same probe, LIB-01 leg | **RED [measured]** — 5/5 throw `function is not iterable`; control `ease` ok, unknown → `easing_name_unknown` | Restore `if (!(name in PRESETS))` at `src/easing.ts:168`; `easing("constructor")` throws again |
| G5 | `no-non-null-assertion` clean under `src/css/` | `npx eslint 'src/css/**/*.ts' --rule '{"@typescript-eslint/no-non-null-assertion":"error"}'` | **RED [measured]** — **94** errors (grammar 72 · stylesheet 18 · timeline 4) | Add one `!` under `src/css/`; the rule is scoped, so the lint job reddens |
| G6 | `PathGeometry` total on M-less paths | `test/transform/path-geometry.test.ts` hostile leg | **RED [measured]** — 10/10 throw `Cannot read properties of undefined (reading 'len')` (`L l H V C Q A T S` + leading-space) | Remove the first-command guard; any drawing command before a `moveto` throws |
| G7 | Arc flag spelling is equivalent | `abs(expanded − compact) < 1e-6` + one committed SVGO-optimised fixture | **RED [measured]** — expanded `31.403311569547533` vs compact `0`, Δ = **31.4033** | Re-introduce whitespace-delimited flag parsing; the compact fixture returns 0 again |
| G8 | Truncated command runs honour the declared type | `getTotalLength("M 0 0 L 10")` | **RED [measured]** — returns `null` while `transform.d.ts:55` declares `: number` | Let `tokenizePath` truncate instead of reject; the return type lies again |
| G9 | Singular 3D matrices return `null` | `decomposeMatrix3D` on a singular matrix | **RED [measured]** — returns `{translate:[0,0,0], scale:[0,null,null], skew:[null,null,null], quaternion:[null,null,null,null], …}` instead of `null` | Delete the zero-scale guard; the null-filled object returns |
| G10 | `no-non-null-assertion` clean under `src/transform/` + `src/foundation/` | same eslint invocation, scoped | **RED [measured]** — **155** (decompose 113 · path 35 · foundation/math 7); src-wide total **297** | Add one `!` in either tree |
| G11 | `./math` enforces its stated preconditions | `test/math.test.ts` | **RED [measured]** — `deCasteljau(0.5,[])` → `undefined` typed `number`; `interpBezier(0.5,[])` → `[undefined,undefined]`; `lerpArray` length-mismatch → `TypeError: Cannot set properties of undefined` (and `[2.5,3.5,NaN]` on the out-param arm) | Remove the boundary check; a mis-sized buffer silently produces NaN frames again |
| G12 | A consumer compiles against the **packed tarball** | `node …/probes/consumer-surface-compile.mjs` | **RED [measured]** — exit 1, 4 assertions: TS2459 `CssValue` (`./css`), TS2459 `ColorFactory` (`./color`), TS2307 + `ERR_PACKAGE_PATH_NOT_EXPORTED` on the root | Drop a type from an area barrel; the probe's LEG1 reddens on that exact name |
| G13 | Zero bare `declare` in emitted `.d.ts` | `grep -c '^declare ' dist/subpaths/*.d.ts` (sum) | **RED [measured]** — **33** (css 18 · value 6 · quantize 6 · easing 2 · color 1) | Return a type from a subpath without publishing it there |
| G14 | Zero `_2` mangles in `css.d.ts` | `grep -o '_2' dist/subpaths/css.d.ts \| wc -l` | **RED [measured]** — **58 occurrences / 25 lines** (`Alpha_2 Channel_2 ChannelsBySpace_2 Color_2 SpaceId_2`) | Reach `color/model` from outside `src/color/`; the duplicate declarations re-emit |
| G15 | `serializeCssValue` published + differential vs the keyframes fork | `'serializeCssValue' in CSS` + 3 fixtures against `compile/emit/css-text.ts:41` | **RED [measured]** — `false`; the fork is live (2 of 3 fixtures diverge, e.g. `"a : b"` → `"a: b"` vs `"a : b"`) | Unpublish it, or let the two serializers diverge on a fixture |
| G16 | R-T1 ratchet: max `src/**/*.ts` LoC may not increase | `wc -l src/**/*.ts \| sort -rn \| head -1` | **RED [measured]** — **899** (`src/css/stylesheet.ts`); 5 files over 350 (899 · 609 · 564 · 483 · 377); the split targets ≤ **609** | Grow any src file past the recorded max. **No 350 cap is asserted** — canon caps `demo/` ≤400 and `api/src` ≤350 only (RD-10) |
| G17 | Colour boundary: `color/model` reachable from `src/color/` only | `library-band-gates.mjs` LIB-04 leg | **RED [measured]** — 2 modules reach past the barrel (`src/css/grammar.ts`, `src/css/types.ts`; 3 statements) | Import `color/model` from a third module |
| G18 | Colour oracles are external | `test/v4-color-behavior.test.ts` + `test/color-anchors.test.ts` | **RED [measured]** — `:66` asserts `toBeCloseTo((byte/255)/12.92, 12)` — the implementation's own sRGB constant — under the name *"matches the independent IEC sRGB dark-band oracle"* | Re-derive an expected value from the implementation; the anchor file's committed vectors no longer bind |
| G19 | Coverage-by-export published over a recorded denominator | recorded command in the wave close report | **RED [measured]** — no coverage configuration exists (`vite.config.ts`, no vitest coverage block); denominator measured **142** declared export names / **79** runtime exports across 7 subpaths | Publish a number without its command, or change the denominator without re-recording it |
| G20 | Packed-surface check is behavioural | `node scripts/ci/verify-packed-surface.mjs <tarball>` | **RED [measured]** — presence-only; emits hardcoded `strictTypes: 62` at `:137`, produced by no check | Add a runtime export that imports but throws on invocation; a presence-only check stays green, the behavioural half reddens |
| G21 | SCI-1 shipped with its evidence tuple | `grep -rn 'sampleColorRamp\|mixColorsInto\|toRgba8Into' src/` + packed import | **RED [measured]** — **0** occurrences in `src/`; eighth carry | Ship the symbols without the atlas tuple in the same commit, or ship the tuple with no symbols |
| G22 | `toHex` and `easingNames()` published | packed import from `./color` / `./easing` | **RED [measured]** — `'toHex' in color` → `false`; `'easingNames' in easing` → `false` | Remove either from the barrel |
| G23 | `easing()` reference stability | `easing(x).value === easing(x).value` for the four CSS keywords | **RED [measured]** — `ease`, `ease-in`, `ease-out`, `ease-in-out` all return **fresh closures per call** (4/4 false) | Drop the memo; the identity comparison reddens on all four |
| G24 | Restored analytic in/out arms match 0.13.0 | drift leg of `fourier-value-import-drift.mjs` (`< 1e-3`, 8 names) | **RED — MEASURE-AT-OPEN** (banked: 8/22 curves drift, max\|Δ\| **0.192**) | Keep the approximated curves under the analytic names; the 8 named curves miss the tolerance |
| G25 | Catalog fence | `bezierPresets` 30-key set + `timingFunctionEntries` snapshot | **GREEN at authorship, deliberately.** Named RED input: delete any `bezierPresets` key | A removal is a `loadAnimationEngine()` boot crash in keyframes; an addition silently mutates their public registry — both surface as a reviewed diff |
| G26 | ND-01 prune fence | `grep -rn 'colorScale\|sampleToSVGPath' src/` | **GREEN at authorship [measured] — 0.** Named RED input: add either symbol to the 4.1 surface | Fifteen months of proposal, zero consumers (CC-093 RETIRE); the fence exists so the proposal cannot return as a ship |
| G27 | Matrix family retired, path geometry preserved | packed `./transform` export list + keyframes seam resolution | **RED [measured]** — 6 unused symbols still exported (`decomposeMatrix2D/3D`, `recomposeMatrix2D/3D`, `interpolateDecomposed`, `slerp`); consumers measured **0** in `demo/` and `../keyframes.js/src`; `PathGeometry` has **4** keyframes files, `getTotalLength` **1** | Export any of the six again (RED), or break a MorphSVG seam (RED the other way) — no shim, no forwarding export |
| G28 | Bench table under the restated denominator | G7-successor interleaved three-leg bar, published as a table | **RED [measured for its R1 leg]** — R1 throws **13/13** on the empty-body + prototype corpus. Legs: accepted ≥0.9× published · reject ≥0.6× · R1 **zero throws**. Every 3×/2× budget restates against **1,636,680 µs** (M-22 §4): 3× = 545,560 µs, 2× = 818,340 µs against the fixed native floor 311,883 µs → headroom 233,677 / 506,457 µs. The `1,870,633 µs` figure and its 311,661 / 623,434 headrooms are **UNCITABLE** until `P4-EVIDENCE-REPLAY.json` is readable under a TCC grant | Publish any ratio outside the three legs, or cite a budget against the unproven denominator. **10× stays impossible and strengthens** (1.906× over budget) |
| G29 | One dated cut | `package.json` version + CHANGELOG + tag | **RED [measured]** — version `4.0.0`; keyframes pins `"4.0.0"` exactly (`../keyframes.js/package.json:69`) — one bump event | Publish a second version inside the wave, or ship an emergency `4.0.1` (ruled out: no reproduced active crash in evidence) |
| G30 | Canon cites no non-existent tree | `grep -rn 'src/parsing' docs/tranches/V/ARCHITECTURE.md` | **RED [measured]** — `:657` cites `src/parsing/stylesheet/serialize.ts`, `src/parsing/animation-shorthand.ts`, `src/parsing/units.ts`; measured: `src/parsing/` **absent**, `find -name '*.bbnf'` → **nothing** | Re-introduce a path citation the tree does not contain |
| G31 | parse-that position recorded against ground truth | `node -e` on `package.json` dependencies + the recorded paragraph | **RED [measured]** — dependencies are exactly `{"@mkbabb/glass-ui":"^7.0.0","@mkbabb/keyframes.js":"^6.0.0"}`; `@mkbabb/parse-that` absent while the readopt decree stands | Record a dependency state the manifest does not show, or adopt a parser inside this wave |
| G32 | Channel descriptors resolve exactly | node run over all 17 spaces × their `ChannelsBySpace` ids | **RED [measured]** — `cp` → `"Ct (tritan)"` (wrong row) and `jz` → `"jz"` (silent degradation to the bare key) | Restore prefix matching, or add a space to the table without descriptors for every id |
| G33 | Packets sent, exact-pin consumers notified | five files + five `INBOX.md` rows + `npm ls @mkbabb/value.js` in keyframes and fourier post-window | **RED [measured]** — zero packets exist; the keyframes pin reads `"4.0.0"` exactly | Tag 4.1.0 before the packets are logged; the post-window `npm ls` leg reads a stale exact pin |

**Denominator note (L-19).** Every count above names a product consumer or a published artifact.
No gate is a `scripts/**/proof-*.mjs` (DR-19, structurally honored — this band has zero). No gate
multiplies the audit's own categories together.

## Dispositions

Terminal state of every ledger row this wave carries. One verb each, original identifier preserved.

| CC row | Origin | Verb here | Landing |
|---|---|---|---|
| CC-084 | DR-12 · MT-F001 · MT-F015 · MT-F024 (R1) | BUILD | X-W9.a (items 1–3), X-W9.e (item 4), X-W9.f (item 5) |
| CC-085 | MT-F019 | BUILD | X-W9.b |
| CC-086 | MT-F007 | BUILD | X-W9.g |
| CC-087 | MT-F008 | BUILD | X-W9.g |
| CC-088 | DR-21 (SCI-1) | BUILD | X-W9.f — same dated cut as CC-084; a ninth carry may not happen |
| CC-089 | DR-26 | BUILD | X-W9.e |
| CC-081 | K-10, ICtCp limb | BUILD | X-W9.h (other limbs: Checkbox → X-W7; Button/Skeleton/dead-surfaces/overlay → X-W8) |
| CC-008 | V·MT7 | FOLD | this wave's shape |
| CC-090 | DR-18 (899-line residual) | FOLD | X-W9.d per RD-9 — home moved, identity preserved, terminal: the split lands or the row retires at close. The measured-cap **ruling** rides CC-082 at X-W8 |
| CC-091 | library-band RD-1..RD-5, RD-7..RD-13 | FOLD | W.L1→X-W9.a · W.L2→X-W9.b · W.L3→X-W9.c · W.L4→X-W9.d · W.L6→X-W9.f · W.L7→X-W9.i. **W.L5's value-side half** ships in X-W9.f (`easingNames()`, restored arms, `toHex`) and is relayed in X-W9.i; its fourier-tree edits and π evidence are **not X-W9 gates** — fourier's waves are authored in fourier's own sub-session (COMMISSION §2), and this wave is library-only, no π. W.L8/W.L9 are API and belong to CC-038 at X-W3 |
| CC-096 | OC-2 (staged gate reading) | FOLD | dispositioned here: equivalence (G1–G4), coverage (G19), and bench (G28) read as **separately staged** gates. No composite RED is reported for this wave |
| CC-097 | M7 §2.1–2.2 bench law + M2 denominator | FOLD | G28's restatement rule, verbatim |
| CC-092 | RD-6 (`resolveCssColor`) | RETIRE | X-W9.f DECLINE list — CLOSED-DECLINED with its runnable second-consumer re-trigger preserved |
| CC-093 | ND-01 (`colorScale`/`sampleToSVGPath`) | RETIRE | X-W9.f prune list + G26 fence |
| CC-094 | decompose ruling | RETIRE | X-W9.b — unused matrix family only; `PathGeometry` preserved for keyframes MorphSVG; no shim |
| CC-095 | OC-1 (bench-bar recalibration) | RETIRE | superseded by G28's three-leg bar; the old single published-parser ratio is not published again |

## COMPLETABLE

**YES.** This wave lands alone, and `c4af0ef9` (V·MT1 landed alone) is the template.

1. **Library-only.** Every gate runs under node against `dist/subpaths/` or the packed tarball. No
   browser, no π, no visual oracle, no dev stack. It does not wait on X-W1's CI restoration to be
   *decidable* — only to be *automated*.
2. **No producer dependency.** Nothing here is glass-coupled; no row of §1.M appears above; no
   Glass-8 trigger gates any unit.
3. **No peer-repo write.** RD-11 keeps cross-repo work by packet; the wave closes when the packets
   are sent, not when peers adopt.
4. **The frontend chain is untouched.** X-W5..X-W8 destinations do not move under it; the only
   shared file with another wave is `ARCHITECTURE.md`, carved and sequenced in §4a.
5. **Each unit is itself completable.** X-W9.a alone kills three shipped crash classes; X-W9.d alone
   makes the surface self-describing and deletes the largest god module; X-W9.f is the only unit
   with a stated hard dependency (on X-W9.a/.b/.d landing first).

## Format And Lint Cadence

- After each integration batch and before close: `npm run lint` (`eslint . --max-warnings=0`),
  `npm run typecheck` (`vue-tsc -p tsconfig.lib.json --noEmit && vue-tsc -p tsconfig.demo.json
  --noEmit`), `npm test` (`vitest run`).
- After each of X-W9.a and X-W9.b: the newly scoped `no-non-null-assertion` invocation of G5/G10.
- Before the tag: `npm pack` → `node scripts/ci/verify-packed-surface.mjs <tarball>` →
  `node …/probes/consumer-surface-compile.mjs`. The packed tarball is the oracle; worktree `dist/`
  is never the subject of a surface gate (PT-08).
- Docs-only units (X-W9.g, X-W9.i): `git diff --check` plus the repository's markdown review; no
  formatter is configured for `docs/**` and none is introduced by this wave.

## Verification Artefacts

Saved at close under `docs/tranches/X/waves/evidence/W9/`:

- `born-red-2026-XX-XX.json` — the §6 table re-executed at wave-open, exit codes and counts.
- `src-surface-totality.{before,after}.txt`, `library-band-gates.{before,after}.txt`,
  `consumer-surface-compile.{before,after}.txt` — probe output either side of the cure.
- `parser-totality-red-first.txt` — the RED-first battery's failing run, with its commit hash.
- `eslint-nna.{css,transform}.{before,after}.json` — 94 → 0 and 155 → 0.
- `surface-census.after.json` — bare `declare` 33 → 0, `_2` 58 → 0, max src LoC 899 → n.
- `coverage-by-export.md` — the published number with its recorded command and denominator.
- `bench-table-4.1.md` — the three-leg bar, budgets restated against 1,636,680 µs.
- `packed-surface.after.json` — the behavioural run over the 4.1.0 tarball.
- `packets/` — the five sent packets' paths + `INBOX.md` row ids.
- Commit hashes for: the RED-first commit, each unit's integration commit, the cut commit, the tag.

## Commit Plan

| Order | Scope | Body required |
|---|---|---|
| 1 | `test(x-w9): RED-first empty-body + prototype battery` | yes — pastes the failing output |
| 2 | `fix(css/lookup): prototype-reachable tables + grammar.ts:181 typed failure` | yes |
| 3 | `fix(transform): path totality, positional arc flags, singular-3D null` | yes |
| 4 | `refactor(transform): retire the unused matrix family` | yes — deletion proof, consumer census |
| 5 | `fix(math): stated precondition protocol` | no |
| 6 | `refactor(css/surface): PSL-1 derivation, colour boundary, stylesheet split` | yes — generated `.d.ts` deltas |
| 7 | `test(oracle): external colour vectors; rename the circular oracle` | yes |
| 8 | `ci(packed-surface): behavioural half; delete strictTypes:62` | yes |
| 9 | `feat(4.1.0): SCI-1 + toHex + easingNames + restored analytic arms` | yes — the atlas evidence tuple rides here |
| 10 | `docs(canon): retire src/parsing citations; record the parse-that position` | no |
| 11 | `fix(demo/console-rail): exact channel-id descriptor lookup` | no |
| 12 | `docs(X·mail): the five 4.1 coordination packets` | yes |
| 13 | `docs(X.W9): close report + status fields` | yes |

Pathspec commits only. Implementation scopes name the mechanism or owned surface, never only the
tranche.

## Dependencies

- **Depends on**: **X-W0** — (a) the owner sitting's DR-21 ruling: an opt-out RETIREs CC-088 and
  changes the cut's contents (§X.W9.f); (b) DR-19's structural proof-farm ban, which this wave's
  gate set must satisfy at authoring; (c) CC-025's relabel, so no gate here may cite `264/264`;
  (d) DR-23's track-or-archive act — already discharged for the three probes this wave cites, which
  are tracked at `41450f02`.
- **Soft-couples to**: **X-W1** — CI restoration makes G1–G33 automated rather than locally run.
  The wave is decidable without it and does not block on it.
- **Sequenced against**: **X-W8** on `docs/tranches/V/ARCHITECTURE.md` only (§4a).
- **Blocks**: **X-W11** (release and verified close — the 4.1.0 tuple is its input); the keyframes,
  atlas, fourier and parse-that admissions that read the 4.1 surface; CC-070's consumer formatting
  facility at X-W7 only insofar as it consumes `./math` (informational, not a gate).

## Archaeology

This class has been declared closed three times against surfaces that did not hold it.

- **D.W6** found `test/bbnf-equivalence.test.ts` a "documentation lie" and **renamed** it to
  `parser-snapshot.test.ts`, crediting the rename with preserving a count gate, and routed the real
  check to "a successor effort". E through T never wired it.
- **U-F29** declared the parse-honesty class LANDED — against a surface the v4 cut then deleted.
- **V·π** (2026-07-20) ran the proof gate and returned COMPOSITE RED: coverage 0-of-52, R1 a live
  shipping crash. vnext's 45-wave band has no born-RED row for it because its wave book was authored
  five hours earlier against a read-only corpus.

**New guardrails.** (1) The battery lands RED **first**, in its own commit, with its failing output
in the body — a rename cannot satisfy it. (2) The oracle is the **packed tarball**, not the worktree
(PT-08), so a surface that exists only in `dist/` cannot pass. (3) Gates are staged (CC-096): a RED
coverage leg no longer hides behind a green equivalence leg, and no composite verdict is reported.
(4) The lint rule is scoped and enabled in-tree, so the crash *shape* — not just the instance — is
prevented; the crash was authored by a `!` that silenced `noUncheckedIndexedAccess`, and until this
wave `eslint` had no rule that could see it.

## Closing rider (L-18)

Landing these gates green does not make this wave ACCEPTED. Its implementation must survive **two
challenging gestalt passes**, each a **quartet of Opus 5 agents** assuming the work is wrong on some
reasonable basis, across three altitudes — total-tranche (was this wave optimal in the greater plan;
was this spec adhered to; what friction arose), wave, and feature. Library bases apply directly:
poorly written library code, duplicated implementation, weak colocation, poor modularization, files
too small or too large, long-running directories. Superlatives are reported with the same provenance
duty as defects. The quartets' results are adjudicated and agglomerated into an apotheosis by a
**single fresh Fable instance**; every incongruity is addressed, ratified and dispatched, never
parked. Reference, not ceremony: `FORMATION-LAWS.md` L-18, with L-20 re-validating this file against
`docs/precepts/` at each pass.
