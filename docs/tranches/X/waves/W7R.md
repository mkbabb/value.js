# X.W7R — value.js repins glass-ui ^7.0.0 → 10.0.1

## State
**Opens after**: X-W7 CLOSED. **Blocks**: X-W12. **Minted by**: COHESION §0bs (2026-09-23). **Model**: Opus 5.5. **Record**: `docs/tranches/X/execution/A/X-W7R.md`.

## Why
The owner's dock/morph/blob/aurora findings (OA-6, OA-18..25, OA-41) are measured against glass 7.0.0, three majors behind glass's published 10.0.1; glass's BL re-measures every relayed row (O-53..O-61) at HEAD and answers rows already cured with their version. value.js cannot see those cures, and X-W12 would otherwise re-do work on a superseded producer.

## Units (serial)
- **`.m` (Opus, effort high)** — read glass's CHANGELOG / migration notes for 8.0.0, 9.0.0, 10.0.0, 10.0.1 whole; pin `"@mkbabb/glass-ui": "10.0.1"` (devDependency, exact); migrate every breaking change at its consumer root (the dismiss axis — which also discharges X-W7's G14 `dismiss="deliberate"` grep — dock API, surface classes, renamed exports); no shims, no deep imports; `vue-tsc` demo 0, vitest GREEN, smoke suite at `--workers=1` with load recorded, D1 headed real-GPU ×2.
- **`.v` (Opus)** — re-read on the served page (`:9000`, headed) every open value.js honest-RED id and relay row that glass owns: `DOCK-SCROLL-MORPH`, `DOCK-MORPH-ROOT`, `I3-SEED-SIZE` (does 10.0.1 ship a shader-free derive?), `G3-FALLTHROUGH-TYPES` (strict probe count at 10.0.1), `O12-3-HOVER-GPU`, the blob (O-56 G-3), `.cartoon-cast` (O-57 R-2); each reads CURED-BY-REPIN (evidence) or still-live (stays with BL). Table in the record.
- **`.d` (Opus)** — OA-41 for value.js's dock at 10.0.1: the small↔large morph smooth and sharp (0 blurred-text frames after settle, no wrap-then-snap, token durations); what still fails is BL's with the 10.0.1 measurement.

## ADDENDUM 2026-09-23 (COHESION §0cf): GLASS-VEIL-GREY limb
- The repin measures the light-theme `.dock-plate` composite and every glass surface in view on the served page, before and after. Today, at 7.0.0, it reads `color(srgb 0.916 0.870 0.829 / 0.328)`, a light frost.
- If it turns grey at 10.0.1, record honest-RED **GLASS-VEIL-GREY** (O-62). Never override locally.
