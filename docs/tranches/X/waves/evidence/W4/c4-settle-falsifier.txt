SERVED MODEL: claude-opus-5[1m]

X-W4 · Repair 1 · gate C4 — THE SETTLE INSTRUMENT'S FALSIFIER PAIR
==================================================================

Dated 2026-09-19 (sitting date of record 2026-09-17, the owner's begin-word).
Filed BESIDE the wave's §8 artefacts; nothing already banked is rewritten (E-3).

WHAT IS BEING FALSIFIED
-----------------------
Check 1 defect 1 (HIGH) named C4's `clippers == []` arm RED at the settled
bytes, reproduced 3 of 3 isolated runs, with the mechanism stated: the gate
read a client rect while the pane's enter transform was still in flight.
The cure is `transformRest()` in `e2e/smoke/a11y-gradient-stop-grammar.spec.ts`
— a wait, not a relaxation. A wait that never waits is decorative, so it is
made to BITE here, once, on a deliberately induced instance of the exact
state, and the pair is recorded the way A5 and D3 record theirs.

THE PROBE (temporary; run once at the settled bytes, then reverted — `grep -c
PROBE e2e/smoke/a11y-gradient-stop-grammar.spec.ts` → 0)
-----------------------------------------------------------------------------
Land `/` cold, dwell 3s, then `page.goto("/#/gradient")` — a SAME-DOCUMENT
hash change, so vue-router swaps the panes behind the `vj-enter` transition
(`demo/styles/animations.css:231-236`, `translateX(±110%) rotate(∓2deg)` on
`--spring-snappy`). Read the first `[data-stop-id]` immediately; then call
`transformRest()`; then read it again.

⟨cmd⟩ npx playwright test --project=smoke e2e/smoke/a11y-gradient-stop-grammar.spec.ts -g "PROBE"

  [PROBE-MIDFLIGHT] {"w":24.82,"h":24.82,"x":-426.3,
                     "live":["div:transform@pending"],
                     "paneTransform":"matrix(0.999391, -0.0348995, 0.0348995, 0.999391, -563.2, 0)"}
  [W4-C4-SETTLE] probe inFlightAtEntry=["div:transform@pending:0ms"]
  [PROBE-ATREST]   {"w":24,"h":24,"x":144,"hitW":24,"faceW":20}
  1 passed

READING, TO THE BYTE
--------------------
· `matrix(0.999391, -0.0348995, …)` IS `rotate(-2deg)` — cos2° = 0.999391,
  sin2° = 0.0348995 — with the pane still 563.2px off-station.
· A 2° rotation inflates an axis-aligned client rect by cos2° + sin2° =
  1.03429: 24 × 1.03429 = 24.82 and 20 × 1.03429 = 20.69 → the close's and
  Check 1's measured `w=24.8 faceW=20.7`, to the digit. The inflation is a
  ROTATION, not the "~3.3 % scale overshoot" the close reasoned to; the
  arithmetic is the same and the diagnosis of WHERE it comes from (the pane
  enter, mid-flight) is confirmed exactly.
· The transition is `pending` at `currentTime 0ms` — the stall
  `e2e/smoke/fixtures/dock.ts:paneSettled`'s docblock records ("can STALL at
  currentTime = 0 for ~1s … Playwright's stable-bounding-box check is
  satisfied DURING the stall"). This is why the file's own `settle()` — four
  identical 250ms geometry signatures — could return INSIDE it: geometry
  repeating is exactly what a stalled animation produces.
· After `transformRest()` the same handle reads 24 × 24, hit 24, face 20, at
  x=144. Nothing about the handle changed; only WHEN it was read.

THE GATE ITSELF, AT THE SETTLED BYTES
-------------------------------------
⟨cmd⟩ npx playwright test --project=smoke e2e/smoke/a11y-gradient-stop-grammar.spec.ts -g "C4"
  4 isolated runs → 2 passed each, EXIT 0 each. Every run:
  [W4-C4-FINE]  w=24 h=24 hitW=24 hitH=24 faceW=20 faceH=20 rootFontSize=16 (×2 handles)
  [W4-C4-RING]  shadow="rgba(0,0,0,0.85) 0px 0px 0px 1px, rgba(255,255,255,0.92) 0px 0px 0px 3px"
                spread=3 clippers=[] focusVisible=true
  [W4-C4-COARSE] w=24 h=24 hitW=44 hitH=44 (×2 handles)

⟨cmd⟩ npx playwright test --project=smoke <the four W4 gate specs>
  2 runs → 21 passed, EXIT 0, both.

NOT ONE ASSERTION MOVED
-----------------------
⟨cmd⟩ git diff <this repair> -- e2e/smoke/a11y-gradient-stop-grammar.spec.ts
  → additions only: the `transformRest` helper and its two call sites.
  Zero `expect(...)` lines edited or deleted, zero thresholds touched, zero
  product bytes in this commit. The six C4 assertions are the ones unit c
  authored.
