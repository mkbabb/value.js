SERVED MODEL: `claude-opus-5[1m]`

# X-W9.i — the five 4.1 coordination packets: paths, ledger row ids, and G33's legs at the bytes

Measured 2026-09-18 (22:0x EDT) on `tranche-u`, src frontier **`c8848bed`**, tree HEAD **`2049ffb5`**,
node v26.0.0, darwin arm64. §Verification Artefacts of `W9.md` names this file's subject verbatim:
*"`packets/` — the five sent packets' paths + `INBOX.md` row ids."* Every peer tree was **read** and
never written (RD-11); `glass-ui` is READ-ONLY always.

---

## 1. The five packets

| row | recipient | path (all under `docs/tranches/V/coordination/`) | L | B | sha256 (first 16) |
|---|---|---|---|---|---|
| **O-34** | keyframes.js (EXACT pin) | `keyframes-inbox-2026-09-18-value-4.1-cut-notice.md` | 283 | 20,971 | `7fcdc6726e1817b0` |
| **O-35** | atlas (+ the sci-report exact pin, filed in §5) | `atlas-inbox-2026-09-18-value-4.1-export-delta-refresh.md` | 153 | 11,050 | `a2123afdf60b7003` |
| **O-36** | glass-ui (standing BH/BK relay + the owed R1 relay) | `glassui-inbox-2026-09-18-value-4.1-r1-relay.md` | 132 | 9,574 | `d2d3acfba3a747a4` |
| **O-37** | fourier-analysis (facility 19's first delta) | `fourier-inbox-2026-09-18-value-4.1-facility19-delta.md` | 152 | 11,033 | `c6ee49367b45498c` |
| **O-38** | parse-that (evidence addendum 2) | `parse-that-inbox-2026-09-18-value-4.1-evidence-addendum-2.md` | 133 | 9,619 | `1a6d7d2ce8144555` |

⟨cmd⟩ `for f in <the five>; do wc -l; wc -c; shasum -a 256; done` — read from the settled bytes,
double-run identical. Line 1 of each file is `SERVED MODEL: claude-opus-5[1m]`.

## 2. G33's legs, each measured

**Gate text (`W9.md:375`)**: *"five files + five `INBOX.md` rows + `npm ls @mkbabb/value.js` in
keyframes and fourier post-window"*; born **RED [measured]** — *"zero packets exist; the keyframes pin
reads `"4.0.0"` exactly; the post-window `npm ls` leg is MEASURE-AT-OPEN (it can only be read after
the window)"*.

| leg | BEFORE (wave open) | AFTER (this unit) | verdict |
|---|---|---|---|
| **five packets authored** | ⟨cmd⟩ `ls docs/tranches/V/coordination/*-inbox-2026-09-1*.md` → **no matches / 0 packets** | **5**, the table above; ⟨cmd⟩ `ls docs/tranches/V/coordination/*-inbox-2026-09-18-*.md \| wc -l` → **5** | **GREEN** |
| **five `INBOX.md` rows** | max outbound id **O-33** (O-32/O-33 taken by X.F.W4 `.z` at 22:0x, re-read immediately before the edit) | **O-34 · O-35 · O-36 · O-37 · O-38** appended after the O-33 row; ⟨cmd⟩ `grep -c '^| O-3[4-8] |' docs/tranches/V/coordination/INBOX.md` → **5** | **GREEN** |
| **the keyframes exact pin acknowledged in the packet, before the tag** | ⟨cmd⟩ `sed -n '71p' ../keyframes.js/package.json` → `        "@mkbabb/value.js": "4.0.0"` — exact, at **`:71`** (the spec's `:69` is a drifted anchor; the string is identical) | O-34 §E1 quotes the line and its coordinate, states the drift, and makes the cut-and-repin **one event**; `package.json` at this seat reads **`4.0.0`** — **the tag has not been cut** | **GREEN** |
| **post-window `npm ls` in keyframes and fourier** | MEASURE-AT-OPEN by the gate's own words | **UNREADABLE BEFORE THE WINDOW** — banked pre-window below | **CARRIED, by the gate's own construction** |

### Pre-window `npm ls` bank (so the post-window read has a BEFORE to be compared against)

```
⟨cmd⟩ cd /Users/mkbabb/Programming/keyframes.js && npm ls @mkbabb/value.js
@mkbabb/keyframes.js@6.0.0 /Users/mkbabb/Programming/keyframes.js
├─┬ @mkbabb/glass-ui@7.0.0
│ └── @mkbabb/value.js@4.0.0 deduped
└── @mkbabb/value.js@4.0.0

⟨cmd⟩ cd /Users/mkbabb/Programming/fourier-analysis/web && npm ls @mkbabb/value.js
fourier-analysis-web@0.1.0 /Users/mkbabb/Programming/fourier-analysis/web
├─┬ @mkbabb/glass-ui@8.0.0
│ └── @mkbabb/value.js@4.0.0 deduped
├─┬ @mkbabb/keyframes.js@6.0.0
│ └── @mkbabb/value.js@4.0.0 deduped
```

**Both read `4.0.0` at every node**, which is the correct pre-window reading: the tag does not exist
yet. The gate's falsifier — *"the post-window `npm ls` leg reads a stale exact pin"* — becomes
readable only after X-W9.f tags and the consumers install; **it is not readable at this seat and is
not asserted GREEN here.**

## 3. The surface figures the packets publish, and where each was read

Every number that left this tree in a packet was read at the settled bytes by this seat and
double-run. The table exists so a reader can falsify any one of them without re-deriving the corpus.

| figure | command | reading |
|---|---|---|
| runtime exports, 7 subpaths | `Object.keys` over `dist/subpaths/*.js` | **75** (color 24 · css 20 · easing 16 · math 9 · transform 3 · quantize 2 · value 1); X-W9.d's receipt records 73 → 75 |
| R1 empty-body class | `parseCssColor` over the 9 heads | **0 throws**, 9/9 `ok:false` `css_syntax`; e.g. `oklch()` → `{"ok":false,"diagnostics":[{"code":"css_syntax","start":0,"end":7,"expected":["color components"],"actual":"oklch()"}]}` |
| prototype class | `parseCssColor` / `easing` over 5 `Object.prototype` keys | **0 throws**; `easing(k)` → `{ok:false,{code:"easing_name_unknown"}}` 5/5; control `easing("ease").value(0.5)` = **0.8024** |
| the `steps()` TYPE LIE | `parseTimingFunction("steps(2, constructor)")` | `{"ok":false,"diagnostics":[{"code":"css_syntax","start":0,"end":21,…}]}` (was `ok:true` with a `Function` position) |
| `serializeCssValue` published | `'serializeCssValue' in CSS`; round-trip `1px` | **true**; `{"ok":true,"value":"1px"}` |
| `./math` policy | `lerpArray` mis-sized; `deCasteljau(0.5,[])`; `scale(1,2,2,0,1)` | `RangeError: lerpArray: start, stop and out must share one length; received 2, 2, 3` · `RangeError: deCasteljau: points must hold at least one control point; received an empty array` · `RangeError: scale: fromMax and fromMin cannot be equal` |
| `./transform` after retirement | `Object.keys` | `PathGeometry getPointAtLength getTotalLength` — **3**, the six matrix symbols gone |
| catalog fence | `Object.keys(bezierPresets).length` | **30** |
| the 8 restored arms ⊂ `bezierPresets` | set intersection | **8 of 8** — `ease-out-circ · ease-in-expo · ease-in-circ · ease-in-quad · ease-in-cubic · ease-out-sine · ease-in-sine · ease-out-quad` |
| `easing()` memo (X-W9.f's, not yet landed) | `easing('ease').value === easing('ease').value` | **false** — stated as pending in O-34 §D3, never as shipped |
| keyframes census | `grep -rn 'from "@mkbabb/value\.js/' src/ \| wc -l` at kf `69095552` | **60** (css 25 · value 18 · color 7 · math 5 · easing 3 · transform 2); bare-root **0** |
| atlas census | `grep -rn 'from "@mkbabb/value\.js"' src/ \| wc -l` at atlas `1e2b911` | **16**; installed value.js **3.1.0** |
| sci-report dashboards | `grep -n 'value\.js' dashboards/package.json` | `:20 "@mkbabb/value.js": "4.0.0"` — a second **exact** pin; 6 statements, 2 subpaths |
| glass census | per-subpath `grep` at glass `e3587ec8` | color 5 · css 3 · easing 1 · others 0 · bare-root 0; peer range **`^4.0.0`**; `grep -rn "= easing(" src/` → **0** |
| fourier census | facility 19's own instrument, re-run | 11 symbols / 4 subpaths / 7 statements, all EXIST at 4.1; `easingNames`/`toHex` **ABSENT** (X-W9.f) |
| fourier `http` bank | `grep -rnE '@router\.(get\|post\|patch\|put\|delete)' api/routers/*.py \| wc -l` at F `0a16b83` | **30** — `contours 4 · equations 2 · images 7 · sessions 4 · visualizations 13`; **bank NOT fired** |
| parse-that PT-01/PT-03 | `grep -n` over the published 1.0.0 dist | `diagnostics-DDazRHgl.js:14` · `packrat-entry-CS1td-8B.js:678` (`false`, the initializer) and `:722` (`true`), **no other assignment** |
| parse-that PT-07 | probe over `dist/core.js` | **5/5** raw `TypeError` (`Cannot read properties of undefined (reading 'charCodeAt')`); `.parse()` on failure → `undefined` |
| parse-that PT-04 | `Parser.lazy.length` | **1** re-measured; **the 7,761/7,762 ceiling is CARRIED from O-15 and explicitly NOT re-run** (this seat's re-construction did not match the original instrument) |

## 4. Anchor drifts recorded (METHOD: INTENT at the true bytes)

| spec / O-11 anchor | true bytes 2026-09-18 | consequence |
|---|---|---|
| keyframes pin `package.json:69` | **`:71`**, same exact string | O-34 §E1 cites `:71` and states the drift |
| `resolve/browser.ts:165` | **`:164`** | per-site verdict re-resolved |
| `engine/options.ts:31` | unmoved | verdict unchanged; the FALLBACK posture named |
| `compile/value-ast.ts:71` | **file deleted** by their split `eb4379ca`; `parseCssValues` now at `compile/value/compile.ts:33` and `engine/composition.ts:203` | both successors given verdicts |
| the fork `compile/emit/css-text.ts:41` | **`:42`** | O-34 §B cites both |
| the atlas evidence tuple `backward-color.ts:171/:250/:263` | the file is now `compile/emit/backward/color.ts`; the 1024-sample ramp is at **`:263`** (`sampleRamp(fromColor, toColor_, 1024, space, hueOpt.hueMethod)`), with `backward.ts:33` describing it | recorded here for **X-W9.f**, whose `G21` must ship the tuple with SCI-1 — the banked coordinate no longer resolves |
| O-12's "atlas lives under `~/Programming/sci-report`" | the **code** tree is `~/Programming/atlas`; `sci-report/atlas` is `docs` only (⟨cmd⟩ `ls sci-report/atlas` → `docs`) | O-35 re-states the correction and measures in the code tree |

## 5. Locks honoured

- **RD-11 absolutely — and stated at the bytes rather than as a claim of universal cleanliness.**
  ⟨cmd⟩ `git -C <each peer> status --porcelain` after this unit: `glass-ui` **0** · `keyframes-v-exec`
  **0** · `sci-report` **0** · `atlas` **0** · `fourier-analysis` **0** · `keyframes.js` **2**
  (the two **untracked** letters `VALUEJS-INBOUND-2026-07-24-…` and `-2026-07-27-…`, present and
  identical at this wave's open — I-26's six survivors, not this unit's) · `parse-that` **31** and
  `parse-that-css-totality-p2` **11** (Track D / owner state under `rust/**` and
  `typescript/src/css/**`, nothing under any mail path). **No peer-repo byte was written by this
  unit in any tree**; every peer command it ran was a read (`git log`, `grep`, `sed`, `ls`, `npm ls`,
  and `import()` of an installed dist).
- **Writable set**: five creates under `docs/tranches/V/coordination/`, `INBOX.md` (rows + one sweep
  line, appended — the file was **re-read immediately before the edit**, and the O-32/O-33 rows a
  sibling seat had taken in the interval are why these five are **O-34..O-38** and not O-32..O-36),
  this evidence file, and the unit's own receipt in `execution/A/X-W9.md`.
- `scripts/dev/dev.sh` never touched, never staged. The ten dirty `demo/**` rows and
  `CARRY-LEDGER.md` (sibling seats) never touched.
- **One commit for the packet family** (§Commit Plan row 12), pathspec on the commit itself, and it
  lands **before** X-W9.f's tag, which is G33's whole ordering requirement.
