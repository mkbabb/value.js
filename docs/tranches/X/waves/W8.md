# X.W8 - Shadcn and graph subtraction

## State

**Name**: W8 - Shadcn and graph subtraction
**Opens after**: X-W5, X-W6 and X-W7 stabilize destination ownership (42 of the 48 consumer files
live in their surfaces by group — `demo/palettes` 23 → X-W7 · `demo/shell` + `demo/scenes` 10 →
X-W5 · `demo/workbenches` 9 → X-W6; the remaining six are X.W8.a's census to enumerate), and X-W6
holds `modify` on five paths this wave carves (§Disjointness · Cross-wave); X-W0's typed graph
authority exists as the SCC oracle
**Agents**: 5 serial (Opus seats, M-23 §2 — no design content in this wave)
**Hard gate**: every measured residue reaches zero by subtraction — 19 barrels, 92 barrel imports,
4 SCCs, 5 dead-surface sites, 1 dead composable shim, 1 dead state latch, 1 dead ink rung — with a
recorded C-16 census binding the bounds first and a null-DELTA proof wherever receiver semantics
do not change
**Status**: planned

**Four-verb status (law: these are separate fields, never conflated)**
| verb | state | evidence |
|---|---|---|
| AUDITED | YES | CC-078..CC-083 in `registry/CARRY-CUT-LEDGER.md §1.J`; `registry/adjudicated/Dock.md`; `audit/codex-provenance/axes-quality.md §3.1`; `CONSUMER-ADMIN-DAG-AUDIT-2026-07-28.md §6` |
| SPECIFIED | YES — by this file | — |
| IMPLEMENTED | NO | — |
| VERIFIED | NO | — |

All baselines below measured read-only at HEAD `41450f02` (2026-08-03). No product byte written by
the authoring seat.

## Goal criterion

This wave succeeds if the demo's shadcn-era forwarding layer, its configuration and dependency
residue, and its four measured module cycles no longer exist — and nothing was built to replace
them. Subtraction only: every act deletes a byte or re-points an import at an owner that already
ships in the installed `@mkbabb/glass-ui@7.0.0`.

## Scope

1. Record the C-16 residue census with verbatim commands and outputs, pinned to a commit; the file
   bounds of units b–e bind to that census, not to this file's numbers.
2. Delete the 19 `demo/ui/*` barrels; re-point their 92 import statements across 48 files at the
   narrowest published glass-ui export.
3. Delete `components.json`; delete the six dead `demo/@/**` file globs from `eslint.config.js`;
   ignore `docs/tranches/**`; add the boundary ban that keeps the barrels dead.
4. Delete `cn`, `clsx`, `tailwind-merge`; re-home `debounce` off `demo/shared/utils.ts` and delete
   that file. Retain `tw-animate-css` (receipt in G-4).
5. Drop the dead `variant` attribute from the two glass Button consumers and the dead
   `surface`/`variant` attributes from the four Skeleton sites, in the same touch as their import
   rewrite.
6. Close the four measured SCCs — Dock (runtime), Markdown (type), gradient trio (type),
   Admin/provider seven-node (type) — each by giving the cluster one owner, no forwarding directory.
7. Delete the `useLayerTransition` shim and adopt the installed `DockCrossfade`.
8. Delete the five dead public-surface sites, the two never-read template refs, and the six
   `PointerDebugOverlay` defects.
9. Delete the stored admin-mode latch; derive it.
10. Delete the statically-dead `chrome` ink rung so silent fallback is unrepresentable.
11. Rule the demo/src LoC cap once: wired as a real check, or the ARCHITECTURE.md sentence deleted.

## Triumvirate Dispatch

Mandatory triumvirate (research + plan augment + redress); the orchestrator may not redispatch the
failing unit alone.

- **Bounds expansion**: X.W8.a's census returns a file set materially larger than 48 (>55 files or
  any file outside `demo/**`), or any barrel turns out not to be a pure re-export.
- **Non-local hard-gate failure**: G-3's rewrite makes `npm run typecheck` RED for a reason other
  than a missing named export (i.e. a real type divergence between barrel and owner); G-7 cannot
  reach zero without introducing a new module; G-9's null-DELTA pair shows a real pixel delta.
- **Third diagnostic iteration**: any SCC in G-7 that survives two closure attempts; any
  null-DELTA pair that fails twice on the same family.

## File Bounds

| File | Access |
|---|---|
| `docs/tranches/X/waves/W8-census.md` | create |
| `demo/ui/**` (19 `index.ts`) | delete |
| the 48 barrel-consumer files (`demo/palettes` 23 · `demo/workbenches` 9 · `demo/shell` 5 · `demo/scenes` 5 · `demo/picker` 4 · `demo/color-session` 1 · `demo/color-picker` 1) — exact list = X.W8.a's census | modify-carve |
| `components.json` | delete |
| `eslint.config.js` | modify-carve |
| `package.json` · `package-lock.json` | modify-carve |
| `demo/shared/utils.ts` | delete |
| `demo/shared/debounce.ts` | create |
| the 7 `debounce` importers | modify-carve |
| `demo/shell/dock/index.ts` · `demo/shell/dock/Dock.vue` | modify-carve |
| `demo/scenes/about/markdown/index.ts` · `Markdown.vue` (+ a types module) | modify-carve |
| `demo/workbenches/gradient/composables/{gradientParse,useGradientCSS,useGradientModel}.ts` | modify-carve |
| `demo/palettes/browser/admin/index.ts` · the 4 admin panels · `demo/palettes/useAdminUsers.ts` · `demo/palettes/usePalettePorts.ts` | modify-carve |
| `demo/shell/dock/layers/ActionBarLayer.vue` | modify-carve |
| `demo/shell/dock/ActionToolbar.vue` | modify-carve |
| `demo/picker/display/ColorComponentDisplay/ColorComponentDisplay.vue` | modify-carve |
| `demo/workbenches/gradient/GradientPane.vue` | modify-carve |
| `demo/picker/visual/PointerDebugOverlay.vue` | modify-carve |
| `demo/shell/dock/composables/useDockAdminMode.ts` | modify-carve |
| `demo/color-session/useContrastSafeColor.ts` (+ its ink rung module) | modify-carve |
| `docs/tranches/V/ARCHITECTURE.md` | modify-carve |
| `docs/tranches/X/waves/W8-witness/**` | create |

**Do NOT touch** — each is another owner's row; touching one is a wave defect, not a bonus:

- `demo/styles/foundation.css` — carries the I-17 spectrum shim (BANKED against six named
  conditions; its own comment calls "the W8 /slider consume" the checkpoint — that is V-prime's W8,
  not this one) and the `tw-animate-css` import G-4 proves load-bearing.
- The 11 `@mkbabb/glass-ui/watercolor-dot` import sites — CC-044, BLOCKED-ON CC-003's four-part
  Glass-8 trigger; one atomic cut at **X-W4.g**, not a barrel rewrite.
- The `--slider-track-bg` style sites (`GenerateControls.vue:305`, `ExtractControls.vue:32,75`,
  `ConfigSliderPane.vue:202`, `ComponentSliders.vue:197`) — CC-105, BLOCKED-ON **X-W0's 8.0.0 repin
  census event**, receiving surface **X-W4.g**. The slider *import line* in those files is in
  bounds; the CSS var is not.
- `.github/workflows/**` — X-W1 owns every CI byte. This wave adds none (see G-14).
- `src/**` LoC cures — CC-090's split is X-W9's; only the *ruling* rides here.
- `tsconfig.demo.json` path/export parity — L-22 ≡ W-HYGIENE, another owner.
- `scripts/dev/dev.sh` — CC-021, unowned; NEVER touch.
- `MT-DOCK-LAYERS-1`'s remaining arms (Dock.md G-C, G-G..G-R: registry, login truth, layout,
  register, focus, backdrop, hygiene). This wave takes exactly the three subtractive local rows
  CC-083 names — L-18, L-19, L-20 — and their gates.

## Disjointness

No two units run in parallel; every unit is serial and commits before the next opens. Sequencing is
required, not stylistic: X.W8.b and X.W8.c both write `demo/palettes/browser/admin/*` and
`demo/shell/dock/*`; X.W8.b and X.W8.d both write `ActionBarLayer.vue`. Under WAVE_SPEC §4a a shared
`modify-carve` path is legal only across sub-waves, so the order b → c → d → e is binding.

**Cross-wave.** X-W6 holds `modify` on five paths this wave carves:
`demo/workbenches/gradient/composables/{gradientParse,useGradientCSS,useGradientModel}.ts`
(X-W6.a + X-W6.c → X.W8.c here) · `demo/workbenches/gradient/GradientPane.vue` (X-W6.a/.b →
X.W8.d's G-10 inject deletion) · `demo/color-session/useContrastSafeColor.ts` (X-W6.h → X.W8.d's
G-13 ink rung). This wave opens only after X-W6 closes; the two never run concurrently on any of the
five. X-W6.c lands a leaf `model/types.ts` that breaks the gradient cycle's type edges, so the
gradient SCC may already read zero when X.W8.c opens — G-7 then records it closed and cuts nothing,
because the gate asserts residue zero, not an act performed. `docs/tranches/V/ARCHITECTURE.md` is
also written by X-W9 (`:657` region); this wave carves only the cap sentence at `:943-945`, and
X-W9.g sequences after this wave's close (W9 §4a — reciprocated here). `eslint.config.js` is also
written by X-W4 (.d) and X-W9.f; this wave's two carves are the `demo/@/**` globs + docs ignore (.b)
and the `max-lines` ratchet (.e); the three waves never run concurrently on it (W9 §4a).

## Worktree Plan

Single worktree, no siblings, no `CARGO_TARGET_DIR` (not a Rust tree). Each unit lands a commit on
clean main before the next dispatches; `git status` must be clean at every unit boundary except the
one unowned dirty row (`scripts/dev/dev.sh`), which stays untouched and unstaged.

## Agent Units

### X.W8.a Residue census

- **Goal**: the wave's bounds rest on a command anyone can re-run, not on an inherited figure.
- **Mechanism**: run the four census commands below verbatim at the wave-open commit; write
  `W8-census.md` with command, output, commit SHA, and the enumerated 48-file list. Mark the two
  prior figures — V·MT6's `90/48` and the lane's `107/49` — UNREPRODUCED, naming that neither
  recorded a command (L-9).
- **Files**: `docs/tranches/X/waves/W8-census.md` (create).
- **Sub-gate**: G-1.

### X.W8.b Barrel and configuration subtraction

- **Goal**: the forwarding layer, its configuration, and its dependencies stop existing.
- **Mechanism**: rewrite each of the 92 imports at the narrowest published owner — 13 families have
  a per-component subpath (`badge button card collapsible dialog dropdown-menu label popover select
  separator slider switch tooltip`), `Input` is `@mkbabb/glass-ui/forms`, and 5 (`alert avatar
  checkbox radio-group skeleton`) are root-barrel-only at 7.0.0 and are recorded as such. Delete
  `demo/ui/`, `components.json`, `demo/shared/utils.ts`; re-home `debounce`; drop `clsx` +
  `tailwind-merge`; carve `eslint.config.js`. In the same touch on `PaginationBar.vue`,
  `UserSortMenu.vue`, and `AdminListSkeleton.vue`, delete the dead `variant`/`surface` attributes.
- **Files**: per File Bounds rows 2–9 + the Button/Skeleton consumers.
- **Sub-gate**: G-2, G-3, G-4, G-5, G-6, G-9.

### X.W8.c SCC closure

- **Goal**: the demo module graph is acyclic without a forwarding directory.
- **Mechanism**: Dock — `Dock.vue:4` imports `GlassDock, DockLayerGroup, DockLayer` from
  `@mkbabb/glass-ui/dock` directly (line 5 already does exactly this for two other symbols); the
  barrel exports only `Dock`. Markdown — `DocModule`/`DocItem` move below the component into a types
  module both sides import. Gradient — `linearInterval` and the shared result types move to a leaf
  both `gradientParse` and `useGradientModel` sit above. Admin/provider — the seven-node cluster's
  types move below the panels; the panel barrel stops being an edge in the cycle.
- **Files**: per File Bounds rows 10–13.
- **Sub-gate**: G-7.

### X.W8.d Dead-surface and dead-state subtraction

- **Goal**: no declared API, stored state, or code path in this wave's surface exists without a
  consumer.
- **Mechanism**: delete the local `useLayerTransition` + `SUB_LAYER_CROSSFADE_MS` + `subLayerProps`
  and mount `<DockCrossfade :active reserve="inline">`; delete the `update` emit, the
  `canProposeName` prop *and its parent binding at `ActionBarLayer.vue:106`*, `defineExpose({
  clearHover })` with `clearHover` and both never-read template refs, and `GradientPane.vue:8`'s
  inject; repair the six `PointerDebugOverlay` defects; replace the stored `isAdminMode` ref with a
  derivation; delete the `chrome` static ink rung and root the live probe on the element that
  actually paints, generalising the ancestor walk `useContrastSafeColor.ts:170-206` already
  contains.
- **Files**: per File Bounds rows 14–20.
- **Sub-gate**: G-8, G-10, G-11, G-12, G-13.

### X.W8.e The joint cap ruling

- **Goal**: canon stops asserting an unmeasured number.
- **Mechanism**: one ruling covering DR-17 (demo ≤400) and DR-18's measured-cap half (CC-090; the
  `src` *split* stays X-W9's). Either wire a real `max-lines` check in `eslint.config.js` — a
  monotone ratchet pinned at the measured HEAD worst per band, so it can only descend — and restate
  the ARCHITECTURE.md sentence to match, or delete the sentence. Not both, not neither. The
  restatement must also drop the exclusion of `demo/@/components/ui/`, a tree deleted at W43.
- **Files**: `docs/tranches/V/ARCHITECTURE.md`, `eslint.config.js` (after X.W8.b).
- **Sub-gate**: G-14.

## Hard Gate

Fourteen conditions born RED, one preservation condition. Every RED cell below was measured
read-only at `41450f02`; none is estimated.

**G-1 — the C-16 census is recorded.**
`W8-census.md` exists, carries the four commands verbatim with their outputs and the wave-open SHA,
enumerates the 48 files, and marks `90/48` and `107/49` UNREPRODUCED.
*RED today*: no such artifact; `PROV C-16` books the figure UNPROVEN in both directions.
*Falsifier*: re-run the recorded commands at the recorded SHA; any different number falsifies the
census, and the bounds with it.

The four commands, and their measured output at `41450f02`:

```sh
find demo/ui -name index.ts | wc -l                                    # 19
grep -rEn "from ['\"][^'\"]*ui/(alert|avatar|badge|button|card|checkbox|collapsible|dialog|\
dropdown-menu|input|label|popover|radio-group|select|separator|skeleton|slider|switch|tooltip)['\"]" \
  demo src --include='*.vue' --include='*.ts' | grep -v '^demo/ui/' | wc -l          # 92
… | cut -d: -f1 | sort -u | wc -l                                                    # 48
grep -rn "\bcn(" demo src --include='*.vue' --include='*.ts' | grep -v shared/utils.ts | wc -l  # 0
```

**G-2 — the barrels are gone and nothing replaced them.**
`find demo/ui -name index.ts | wc -l` = 0; `demo/ui/` absent; no new `index.ts` anywhere under
`demo/**` re-exports a `@mkbabb/glass-ui` symbol (`git diff --stat` shows deletions only in the
barrel families).
*RED today*: 19 barrels, each a pure re-export of `@mkbabb/glass-ui` (18 root, 1 `/forms`).
*Falsifier*: a forwarding directory under a new name passes the count and fails the second clause.

**G-3 — every consumer imports its real owner.**
The G-1 grep returns 0; `npm run typecheck` green; each rewritten specifier is the narrowest
published export in `@mkbabb/glass-ui@7.0.0`'s 74-key map.
*RED today*: 92 statements across 48 files.
*Falsifier*: routing a symbol that has a subpath through the root barrel passes the count and fails
the narrowness clause; a wrong symbol name fails typecheck.

**G-4 — configuration and dependency residue is gone; `tw-animate-css` is kept with its receipt.**
`components.json` absent; `grep -c 'demo/@' eslint.config.js` = 0; `clsx` and `tailwind-merge`
absent from `package.json` and the lockfile; `tw-animate-css` still declared, and the census carries
the receipt that it is load-bearing.
*RED today*: `components.json` present and stale in three fields (`demo/@/components` — 0 files;
`tailwind.config.ts` — absent; `@styles/style.scss` — no `.scss` survives the migration).
`eslint.config.js:232-238` and `:274-277` carry six `demo/@/**` globs matching 0 files, and their
`no-restricted-imports` patterns name `@components/...`, an alias with 0 live importers.
*Receipt for the retention*: `node_modules/@mkbabb/glass-ui/dist/styles/utilities/btn.css`'s
`sheet-animate` utility `@apply`s `animate-in`/`animate-out`/`fade-in` — tw-animate-css classes.
Demo authors 0 such classes; the producer consumes them. Deleting the dependency would break the
producer's sheet motion. Retention is measured, not assumed.
*Falsifier*: delete `tw-animate-css` and the producer's sheet-animate utility fails to resolve.

**G-5 — `cn` dies; `debounce` keeps an honest home.**
`grep -rn '\bcn(' demo src` = 0; `demo/shared/utils.ts` absent; all 7 `debounce` importers resolve
to `demo/shared/debounce.ts`; `npm run test` green.
*RED today*: `demo/shared/utils.ts` exports `cn` with **0** consumers repo-wide while holding the
demo's only `debounce`, which has 7 importers.
*Falsifier*: a surviving `cn` call site fails clause 1; a broken debounce import fails typecheck.

**G-6 — the lint targets the real physical tree and is green.**
`npm run lint` exits 0; `eslint.config.js` ignores `docs/tranches/**` (the same rule the config
already applies to `docs/precepts/**` and `docs/tranches/C/**`); a `no-restricted-imports` ban
covers every deleted barrel path.
*RED today*: `npm run lint` → **18 errors, 32 warnings**, all 30 offending files under
`docs/tranches/**` (audit probes, `formation/codex-worktree-7e28/**` tooling, `workflows/*.js`);
`npx eslint demo src api e2e test --max-warnings=0` exits **0**. The lint is red on documents and
blind on the product boundary it was written to guard.
*Falsifier* (mandatory demonstration, committed as output): re-add one `import { Button } from
"../ui/button"` line; `npm run lint` must exit non-zero naming the ban. Revert.

**G-7 — the four SCCs are zero.**
Run under X-W0's typed graph authority over `demo/**`: 0 runtime SCCs, 0 type-inclusive SCCs; each
former cluster has one owner; no forwarding directory was created.
*RED today* — four clusters, reproduced this session by a read-only Tarjan over demo relative
imports (251 nodes / 642 edges; scratchpad instrument, labelled; memberships match
`CONSUMER-ADMIN-DAG-AUDIT-2026-07-28.md §6` exactly):

| SCC | class | members |
|---|---|---|
| Dock | runtime | `shell/dock/Dock.vue` ↔ `shell/dock/index.ts` (`Dock.vue:4` imports from `"./"`; `index.ts:5` exports `Dock.vue`) |
| Markdown | type | `scenes/about/markdown/Markdown.vue` ↔ `.../index.ts` (`Markdown.vue:40` imports type `DocModule` from `"./"`) |
| gradient | type | `gradientParse.ts` ↔ `useGradientCSS.ts` ↔ `useGradientModel.ts` (`gradientParse:22,27`; `useGradientModel:15,16`) |
| Admin/provider | type | 7 nodes: `AdminTagsPanel` · `AdminFlaggedPanel` · `AdminAuditPanel` · `AdminUsersPanel` · `browser/admin/index.ts` · `useAdminUsers.ts` · `usePalettePorts.ts` |

*Falsifier*: reintroduce the `Dock.vue → "./"` edge; the oracle must go RED. If it does not, the
oracle is the defect, not the graph.

**G-8 — the shim is retired against the shipped successor.**
`grep -cE 'useLayerTransition|SUB_LAYER_CROSSFADE_MS' demo/shell/dock/layers/ActionBarLayer.vue` = 0
AND `grep -c '<DockCrossfade' …` ≥ 1; plus Dock.md's G-B rendered-motion arm (chromium :9000, `/#/`,
drive `[aria-label="Toggle action bar"]` → `[aria-label="Open color input"]`; a non-zero opacity
transition-duration or a `[data-crossfading]` ancestor on both faces at +60 ms, and no frame with
both faces at opacity extremes).
*RED today*: 5 shim hits, 0 `<DockCrossfade>`. The successor ships in the version already installed
— `dist/components/dock/index.d.ts:5` exports `DockCrossfade`; `useLayerTransition` is ABSENT from
the installed dist (I-18, DECIDED: retire, clean break). `layers.css` transitions only
`visibility 0s`, so `is-leaving`'s `opacity: 0` lands in one frame — the 260 ms timer times a window
in which nothing animates.
*Falsifier*: the motion arm fails if `DockCrossfade` is mounted but not driven.

**G-9 — the dead Button/Skeleton axes are gone, and the change is provably paint-neutral.**
0 `variant=` on a glass `Button` in the two consumers; 0 `surface=`/`variant=` on `Skeleton` in
`AdminListSkeleton.vue`; a committed null-DELTA pair (1440×900 and 390×844, both schemes, stated
tolerance) shows zero pixel change.
*RED today*: `PaginationBar.vue:8,22` `variant="outline"`, `UserSortMenu.vue:8` `variant="ghost"` —
`dist/components/button/Button.vue.d.ts` declares `emphasis|tone|size|iconOnly|loading|type|disabled|class`
and **no** `variant`. `AdminListSkeleton.vue:13,15,16,18` pass `surface="glass" variant="breath"` —
`dist/components/skeleton/Skeleton.vue.d.ts` declares `{ class?: … }` only.
*Why null-DELTA is the right proof here*: `grep` for `[variant=` / `[surface=` attribute selectors
across `node_modules/@mkbabb/glass-ui/dist/**/*.css` and all demo CSS returns **nothing**, so the
attributes are inert fallthrough and the only legal delta is the attribute's absence from the DOM.
*Falsifier*: a non-zero pixel delta means an attribute selector was missed — adjudicate it, never
re-baseline the golden.

**G-10 — dead public surface is zero.**
All five sites absent, and no dangling counterpart: `ColorComponentDisplay.vue:92-93` emit `update`
(sole `emit(` in the file is `:33` `emit('input', …)`); `ActionToolbar.vue:72` prop `canProposeName`
**together with** its parent binding `ActionBarLayer.vue:106 :can-propose-name`;
`ActionToolbar.vue:87,91` `clearHover` + `defineExpose` (`grep -rn clearHover demo src` = 4 hits,
2 of them an unrelated `clearHoveredPath`, **0** callers); `ActionBarLayer.vue:28,29`
`colorInputRef`/`actionToolbarRef`, bound in template at `:103,:116` and never read in script;
`GradientPane.vue:8` `inject(CSS_COLOR_KEY)!` unread in a 29-line file.
*RED today*: each cited line present at HEAD.
*Falsifier*: deleting the prop without the parent binding leaves a Vue fallthrough attribute —
typecheck stays green, so the gate asserts the binding's absence explicitly.

**G-11 — `PointerDebugOverlay` defects are cured.**
`aria-controls="debug-body"` (`:13`) resolves to a real `id`, or is removed; `document.execCommand`
(`:123`) result is checked before `copied.value = true` (`:125`); the `appendChild`/`removeChild`
pair is `try/finally`; both `setTimeout` handles (`:114`, `:126`) are cleared on unmount (file has
no `onUnmounted`/`onScopeDispose` today); the three action buttons at `:49,:55,:58` carry
`type="button"` (the header disclosure at `:9` already does).
*RED today*: all six present; `grep 'id="'` over the whole file returns nothing.
*Falsifier*: force `execCommand` to fail and assert the UI does not report success.

**G-12 — the admin latch is derived, not stored.**
`useDockAdminMode.ts` holds no `isAdminMode` ref; a test drives anonymous `/#/` → `/#/atmosphere` →
`back()` and asserts no gold on `/#/`.
*RED today*: `:32` seeds `ref(currentView.startsWith("admin-"))`; `:55-59` is the only maintainer —
`if (adminViews.includes(view)) isAdminMode.value = true`, **no else**; the only clearing writers
are the toggle (`:41-48`) and the logout watch (`:62-64`). `atmosphere`/`blob` are in `adminViews`
(`:27`) and the router has zero `beforeEach`/`beforeEnter`, so any anonymous visitor to
`/#/atmosphere` latches gold for the session.
*Falsifier*: reintroduce the stored ref; the back-navigation assertion must fail.
*Labelled UNVERIFIABLE-HERE*: the authed `viewEntries` collapse (`:34-39`) needs
`isAdminAuthenticated` true; dev :9000 is API-less, so that arm is vitest-stubbed and labelled.

**G-13 — the silent chrome ink fallback is unrepresentable.**
`resolveLiveTint("chrome")` returns a defined tint for the painted element; the static `chrome` rung
no longer exists in the ink type, so a fallback call site is a compile error.
*RED today*: `useContrastSafeColor.ts:151-153` reads
`getComputedStyle(document.querySelector('.glass-dock')).backgroundColor`; no rule in glass-ui 7.0.0
or demo CSS paints `.glass-dock`'s own background — every dock-surface rule paints `.dock-plate` —
so the computed value is `rgba(0,0,0,0)` by spec, `:215-217` returns `undefined` on alpha 0, and the
static arm serves on **every** call (`ink.ts:118-122` fallthrough). Sole consumer
`ProfileSection.vue:28`. Statically certain; both Dock.md worker cells agree (plate α 0.5392 light /
0.5776 dark), carried labelled.
*Falsifier*: re-point the probe at an unpainted element and the gate must go RED — an
`undefined`-tolerating call site must not compile.
*Bank, not smuggled*: CC-107 — the probe's eventual **retirement** against glass's shipped
`--glass-tint-source`/`--glass-tint-strength` pair is BLOCKED-ON TR#47. Building the walk now is not
blocked; retiring it is.

**G-14 — the LoC cap is ruled once.**
Exactly one of: (i) `docs/tranches/V/ARCHITECTURE.md:944`'s cap sentence deleted; (ii) the sentence
restated as a wired ratchet AND `eslint.config.js` carries the matching `max-lines` rule AND
`npm run lint` exits 0 AND a falsifier run (add a file one line over the ratchet → lint RED) is
committed as output. The ruling is recorded with its rationale either way.
*RED today*: canon asserts "every `demo/` file … stays ≤ 400 LoC" and excludes
`demo/@/components/ui/`, a tree deleted at W43. Measured breaches: `demo/color-picker/App.vue` 417 ·
`demo/picker/ColorPicker.vue` 414 · `demo/scenes/about/markdown/Markdown.vue` 408 ·
`demo/color-picker/composables/boot/useAtmosphere.ts` 406. `src` band, for the joint ruling's
record: `stylesheet.ts` 899 · `decompose.ts` 609 · `path.ts` 564 · `grammar.ts` 483 ·
`anchors.ts` 377. `api/src`: 0 over 350.
*Falsifier*: if wired, the added-file demonstration; if deleted, `grep -c '400 LoC'
docs/tranches/V/ARCHITECTURE.md` = 0.
*Scope guard*: a ratchet pinned at HEAD-worst is GREEN at HEAD by construction — that is deliberate.
The four demo breaches are cured by X-W5/X-W6 and the `src` residual by X-W9; wiring a cap this wave
cannot meet would import another wave's defect into this one's gate.

**G-15 (preservation, not born-RED) — the suite stays green and semantics do not move.**
`npm run typecheck`, `npx eslint demo src api e2e test --max-warnings=0`, `npm run test`,
`npm run test:e2e` all green; and for every family whose receiver semantics are unchanged, a
committed before/after pair at 1440×900 and 390×844, both schemes, shows zero delta at the stated
tolerance. Preferred instrument: X-W1's `e2e/visual/` goldens (CC-030). If X-W1 has not landed, this
wave commits its own pairs under `docs/tranches/X/waves/W8-witness/` — the wave does not wait.
*Today*: product-tree lint exits 0 (measured). A real delta is adjudicated in the wave, never
renamed and never re-baselined.

## Format And Lint Cadence

`npx eslint demo src api e2e test --max-warnings=0` and `npm run typecheck` after **each** of the
five units, not only at close; `npm run lint` (whole tree) becomes meaningful only after X.W8.b
lands G-6 and is then required at every subsequent unit boundary. `npm run test` after b, c, d;
`npm run test:e2e` after d and at close. `git diff --check` before every commit. No formatter is
skipped.

## Verification Artefacts

- `docs/tranches/X/waves/W8-census.md` — the C-16 census (commands, outputs, SHA, 48-file list).
- `docs/tranches/X/waves/W8-witness/` — the G-6 lint falsifier transcript; the G-7 graph-oracle
  output before and after; the G-8 crossfade phase samples at +60 ms; the G-9 null-DELTA pairs; the
  G-12 back-navigation trace; the G-13 computed-style readings before and after; the G-14 ratchet
  falsifier (or the deletion diff).
- `docs/tranches/X/waves/W8-CLOSE.md` — per-row disposition table, the four-verb state, and every
  gate's final cell.

## Commit Plan

Pathspec commits only; one per unit, each independently green-able.

1. `docs(x-w8): the C-16 residue census — recorded commands, 19/92/48/0 at <SHA>` (body required:
   supersedes `90/48` and `107/49`, both UNREPRODUCED).
2. `refactor(demo/ui): delete the 19 shadcn barrels; consumers import their real glass-ui owner`
   (body required: deletion + dependency + config surface, and the tw-animate-css retention receipt).
3. `refactor(demo/graph): close the four measured SCCs by ownership, not forwarding` (body required).
4. `refactor(demo/dock,picker): retire the layer-transition shim and the dead surface/state/ink`
   (body required: names each deleted site and its parent counterpart).
5. `docs(x-w8): rule the demo/src LoC cap — <wired|deleted>` (body required).
6. `docs(x-w8): close — dispositions, gate cells, witness` at wave close.

## Dependencies

- **Depends on**: **X-W0** (typed graph authority = G-7's oracle; the relabelled completeness
  denominators). **X-W5**, **X-W6** and **X-W7** (destination ownership: 23 of the 48 consumer files
  are `demo/palettes` — X-W7's surface — 10 are `demo/shell`/`demo/scenes` — X-W5's — and 9 are
  `demo/workbenches` — X-W6's instrument surface; rewriting an import into a file about to be
  rewritten manufactures a second migration). X-W6 declares **Blocks: X-W8** — "subtraction needs
  .a/.c's destinations stable before the dead doors and barrels go" — and holds `modify` on five of
  this wave's paths; that claim is reciprocated here and in §Disjointness. **X-W1** *(soft)* —
  CC-030's golden suite is G-15's preferred instrument; absent it the wave commits its own pairs.
- **Blocks**: **X-W10** (opens only once X-W5..X-W9 stabilize the surviving structure).
  **X-W11** (the release table reads this wave's dispositions).
- **Explicitly not coupled**: no API surface, no producer release, no Safari cell, no bundle claim.

## COMPLETABLE — YES

Every act in this wave removes a byte or re-points an import at an owner that already ships in the
installed `@mkbabb/glass-ui@7.0.0` — all 19 barrels are pure re-exports of it, and `DockCrossfade`
ships in the very version W44 adopted. Nothing waits on Glass 8, on the API, on Safari, on a
producer letter, or on a CI file another wave owns. Eleven of the fifteen gate arms run this minute
as `grep`/`wc`/`tsc`/`node` against the tree; the four probe arms have RED cells pasted above with
statically-certain mechanisms. Five units, five independently green-able commits: were this the only
wave ever executed, the forwarding layer would stop existing, the graph would be acyclic, and no
declared surface would lack a consumer. `c4af0ef9` (CC-002 — V·MT1 landed alone) is the template.

## Dispositions

| row | verb | lands as |
|---|---|---|
| CC-078 | BUILD | X.W8.a (census, the C-16 mandate) + X.W8.b — G-1..G-6 |
| CC-079 | BUILD | X.W8.c — G-7 |
| CC-080 | BUILD | X.W8.d — G-8 (I-18: DECIDED, clean break, no alias) |
| CC-081 (Button · Skeleton · dead-surface · overlay limbs only) | BUILD | X.W8.b (G-9) + X.W8.d (G-10, G-11). Checkbox limb → X-W7; ICtCp limb → X-W9. All nine enter `DEFECT-LEDGER` with byte receipts at wave-open |
| CC-007 | FOLD | this wave's identity (V·MT6 ⊕ V·C5, re-authored under X naming; the ledger supersedes both labels) |
| CC-082 | FOLD | X.W8.e — G-14, jointly with CC-090's measured-cap half |
| CC-083 | FOLD | Dock.md local **L-20** → G-8 (co-lands with CC-080) · local **L-19** → G-12 · local **L-18** → G-13. These are adjudication-local IDs, distinct from FORMATION-LAWS L-18..L-20 |

**Left in the glass bank, untouched, with their named triggers** (not smuggled into this wave):
CC-044 (WatercolorDot impostors — CC-003's four-part Glass-8 trigger, one atomic cut) · CC-105
(`--slider-track-bg` sites — the 8.0.0 repin census) · CC-107's retirement half (TR#47) · CC-113
(watercolor paint — TR#84) · CC-114 (dock pill elevation — TR#22) · CC-115 (TR#87) · the I-17
spectrum shim (six conditions, authority re-anchored to glass's live refinement roster).

**Receiving surface (R-B)**: **X-W0** owns the census EVENT — it evaluates CC-003's four
machine-checked conditions (installed major ≥ 8 · `./watercolor-dot` absent from packed exports ·
the consumer-indicator slot present in declarations *and* packed exports · a Glass receipt naming
its keyboard/orientation/motion contract) and records PASS/FAIL with the measured bytes. **X-W4.g**
is the trigger-gated receiving unit: closed while the census FAILs; on PASS it opens and lands
CC-044 + CC-003 together with the banked limbs above as ONE atomic cut — no second migration, no
interim wrapper, no pre-8 shim. X-W11 keeps the terminal BLOCKED-ON accounting. This wave neither
opens nor pre-shims any of them; if the census PASSes while this wave is open, the limbs still land
at X-W4.g, never as a barrel rewrite here.

**Struck and uncitable by any close of this wave** (Dock.md §5): L-20's occlusion-harm clause — the
leaving face is `pointer-events: none`, `position: absolute`, and `inert`, so it occludes nothing;
the injury is motion truth plus a dead fork. L-19's "two disagreeing predicates" as operative
mechanism — the missing falsifying arm alone is operative.

## Archaeology

This wave is the third authoring of the same subtraction. `V·MT6` (MECHANISM-CUT-FORMATION-MAP
§V·MT6, status *CANDIDATE; not SPECIFIED*) and `V·C5` (CONVERGENCE-RESUME §V·C5, status
*NOT-STARTED*) both bounded it at "90 imports across 48 files" — a figure `PROV C-16`/`K-29` rules
**UNPROVEN in both directions**, because no command was ever recorded (L-9) and a later lane
measured 107/49 by an equally unrecorded method. Neither cut executed; both labels are now
superseded by `CARRY-CUT-LEDGER §0.1`.

**New guardrail**: X.W8.a runs before bounds bind, and the census — not this file, not the ledger,
not either prior cut — is the authority for what gets touched. A wave that inherits an unrecorded
number inherits its errors.

## L-18 rider (reference, not ceremony)

This wave's implementation is not ACCEPTED when its gates go green. Under FORMATION-LAWS **L-18**
it must first survive **two** challenging gestalt passes — each a quartet of Opus 5 skeptics working
total-tranche, wave, and feature altitudes, each assuming the implementation is wrong on some
reasonable basis, reporting defects *and* superlatives with provenance — whose returns a **fresh
Fable instance** challenges back and agglomerates into an apotheosis. Any dissonance surfaced is
addressed, ratified, and dispatched; never parked. Likely bases against this wave specifically:
a barrel deleted but its cycle re-formed one directory up; a "null-DELTA" that was never actually
diffed; a dead-surface deletion that left its parent binding; a ratchet that ratified the breach it
was meant to close. **L-20** applies at authoring, at amendment, and at each pass: this file is
re-validated against the current `docs/precepts/instructions/tranche/WAVE_SPEC.md`, or it is not
SPECIFIED whatever its prose claims. **L-19** governs the gates above: no `proof:*` script, no
gate-farm, no cross-product denominator — every gate names a command with a falsifier, and the four
probe arms are the minimum, not a suite.
