# X.W7 - Palette Specimen and Domain Split

## 2. State

**Name**: W7 - Palette Specimen and Domain Split
**Opens after**: X-W3 (API policy kernel) and X-W4 (typed `SceneActionSet`); X-W1 supplies the visual-golden harness for G11
**Agents**: 7 units — 1 parallel pair (`X.W7.a` ∥ `X.W7.b`), then 5 serial
**Hard gate**: 20 numbered gates, 19 born-RED with measured baselines pinned in §6; the palette family renders true counts at N ∈ {0,1,2,5,50,200,201}, every mutation has exactly one owner and a visible result, and the certified export serializers become the shipping path
**Status**: planned

### Four-verb status (status law)

| verb | state | basis |
|---|---|---|
| **AUDITED** | YES | `registry/CARRY-CUT-LEDGER.md` §1.I; the three OM censuses (OM-14/15/16); `CONSUMER-ADMIN-DAG-AUDIT-2026-07-28.md` §2.3; `DEFECT-LEDGER` Checkbox rows |
| **SPECIFIED** | YES — **this file, and nothing more** | authored 2026-08-03 against `WAVE_SPEC.md`, validated per L-20 |
| **IMPLEMENTED** | NO | no product byte written by this file |
| **VERIFIED** | NO | no gate has been run to green |

**COMPLETABLE: YES.** Every row is consumer-side. The API surface this wave reads is already cured
by X-W3, so no backend change rides here. The N-fixture battery is self-contained (fixtures are
authored in-repo, no server, no network). The producer arms that the glass seat declined
(A-19 chip-overflow, A-20 priority-collapse) are `DECLINE-AT-8.0.0` **by default and owner-revivable
next-window** — the ledger rules the composition interim consumer-side, so nothing in this wave
waits on glass 8.0.0. The wave therefore lands alone, on installed glass 7.0.0, and its gates stay
green whether or not the producer ever revives those two rows. `c4af0ef9` is the template.

## 2a. Goal criterion

This wave succeeds if a palette renders as a **quiet, props-only specimen** whose truth does not
depend on how many colors, tags, or digits it happens to carry, and if every mutation of a palette
entity has exactly one owner, one visible result, and — where it destroys — one deliberate
confirmation.

## 3. Scope

1. Bind both `<Checkbox>` consumers to the real glass-ui 7 contract (`modelValue` /
   `update:modelValue`) and close the drift class structurally by turning on `strictTemplates`.
2. Delete `demo/palettes/export.ts`; route `usePaletteExport` at the certified
   `demo/palettes/export/` serializers; collapse the palette family to one `slugify`.
3. Rule the 31 `catch → console.warn` sites in `demo/palettes/` once, as a table, and surface
   export/CRUD failure to the user.
4. Create a props-only `PaletteSpecimen`; move rename, menu, feedback, versions, tags, export,
   publication, vote, and destructive actions out of it.
5. Replace `PaletteCardMeta.vue:37`'s bare `.slice(0, 3)` with a priority-collapsing chip row plus
   a `+N` overflow affordance that reveals the remainder.
6. Author the N-fixture battery (colors 0,1,2,5,50,200,201; tags 0,1,2,3,10,11) and make every
   palette surface honest across it.
7. Fix the PaletteCard cast shadow to follow the rounded silhouette and add a designed hover
   register — both at the card root, zero per-instance overrides.
8. Give each palette mutation exactly one call site and a visible result; kill the soft-delete
   count disagreement and the stale expanded-child list; strip user actions out of the Admin menu.
9. Compose `dismiss="deliberate"` + `<Button tone="destructive">` at every destructive Admin seat;
   rename the "delete all palettes" control to say what it does.
10. Create `demo/color-session/format-color.ts` — four registers (`compact`, `caption`,
    `interchange`, `exact`) over a per-channel decimal table — and route the 29 user-facing
    raw-12dp sites through it. `src/` is not touched.
11. Add a compact-number formatter for counts; keep the exact number in `title`.
12. Execute the OM-15 abrogation: 26 KILL, 31 REDUCE, 3 MERGE; delete `EmptyState.vue`'s `eyebrow`
    prop *and* element.
13. Cut the ShadowPalette plate's mass on **both** `ShadowPalette.vue` and
    `PaletteCardSkeleton.vue`, and delete the `ExtractWorkbench.vue:163-166` caption.

## 3a. Triumvirate Dispatch

Triumvirate is **mandatory**, not optional, on any of these:

- **File-bound expansion that would invalidate the wave**: any unit needing to write under `src/`,
  `api/`, or `@mkbabb/glass-ui`. G18 exists precisely to make this visible. The OM-14 ruling puts
  the display formatter demo-side; a unit that concludes `src/css/grammar.ts:285` must change has
  found a different wave, and halts.
- **Hard-gate failures that are not local-edit-recoverable**: G3 (turning on `strictTemplates`
  surfaces a drift population large enough that the wave cannot absorb it — the count is
  MEASURE-AT-OPEN and unknown today); G9 (the meta row cannot be made non-overflowing without a
  primitive that only glass can mint — which would collide with the A-19/A-20 decline and must be
  re-adjudicated, not worked around with a local mint).
- **Diagnostic loops whose third iteration must halt**: the N-fixture battery. If a single fixture
  value has failed three consecutive cure attempts, stop and triumvirate — three failures on one N
  means the law is wrong, not the code.

## 4. File Bounds

| File | Access |
|---|---|
| `demo/palettes/browser/search/TagEditPopover.vue` | modify |
| `demo/palettes/browser/search/SearchFilterBar.vue` | modify |
| `demo/ui/checkbox/index.ts` | modify-carve |
| `tsconfig.base.json` | modify |
| `demo/palettes/export.ts` | **delete** |
| `demo/palettes/usePaletteExport.ts` | modify |
| `demo/palettes/export/*.ts` | modify |
| `demo/palettes/utils.ts` | modify |
| `demo/test/export/byte-exact.test.ts` | modify |
| `demo/palettes/browser/card/PaletteSpecimen.vue` | create |
| `demo/palettes/browser/card/PaletteCard/PaletteCard.vue` | modify-carve |
| `demo/palettes/browser/card/PaletteCard/PaletteCardMeta.vue` | modify-carve |
| `demo/palettes/browser/card/PaletteCard/PaletteCardMenu.vue` | modify-carve |
| `demo/palettes/browser/card/PaletteCard/PaletteCardSwatches.vue` | modify |
| `demo/palettes/browser/card/PaletteColorStrip.vue` | modify |
| `demo/palettes/browser/card/PaletteCardGrid.vue` | modify |
| `demo/palettes/browser/card/PaletteCardSkeleton.vue` | modify |
| `demo/palettes/browser/card/ShadowPalette.vue` | modify |
| `demo/palettes/browser/card/index.ts` | modify |
| `demo/palettes/BrowsePane.vue` | modify |
| `demo/palettes/PalettesPane.vue` | modify |
| `demo/palettes/admin/AdminPane.vue` | modify |
| `demo/palettes/browser/admin/AdminNamesPanel.vue` | modify |
| `demo/palettes/browser/admin/AdminTagsPanel.vue` | modify |
| `demo/palettes/browser/admin/AdminFlaggedPanel.vue` | modify |
| `demo/palettes/browser/admin/AdminUsersPanel.vue` | modify |
| `demo/palettes/browser/admin/AdminAuditPanel.vue` | modify |
| `demo/palettes/usePaletteActions.ts` | modify |
| `demo/palettes/usePalettePorts.ts` | modify |
| `demo/palettes/useVersionHistory.ts` | modify |
| `demo/palettes/useTagEdit.ts` | modify |
| `demo/palettes/useAdminUsers.ts` | modify |
| `demo/palettes/usePaletteStore.ts` | modify |
| `demo/color-session/format-color.ts` | create |
| `demo/color-session/ColorSpaceSelector.vue` | modify |
| `demo/shared/ui/EmptyState.vue` | modify-carve |
| `demo/workbenches/extract/ExtractWorkbench.vue` | modify-carve |
| `demo/workbenches/mix/MixSourceSelector.vue` | modify |
| `demo/workbenches/mix/MixConfigBar.vue` | modify |
| `demo/scenes/atmosphere/AuroraPane.vue` | modify |
| `demo/shell/dock/ActionToolbar.vue` | modify |
| `demo/test/palettes/n-fixtures/` | create |
| `demo/test/palettes/checkbox-contract.test.ts` | create |
| `demo/test/color-session/format-color.test.ts` | create |
| `e2e/smoke/oracles/o9-shadow-palette.spec.ts` | modify-carve |
| `docs/tranches/X/waves/W7-*.md` (dispositions, censuses, receipts) | create |

**Do NOT touch**: `src/**` · `api/**` · `node_modules/@mkbabb/glass-ui/**` · `scripts/dev/dev.sh`
(unowned; the NEVER-touch posture stands until the X-W0 sitting rules it) · `demo/ui/**` other than
`checkbox/index.ts` (the barrel family is X-W8's subtraction) · `e2e/visual/**` (X-W1 owns it;
this wave consumes it, never authors a second screenshot mechanism).

## 4a. Disjointness

`X.W7.a` and `X.W7.b` are the only concurrent units; their `modify` sets are disjoint
(`browser/search/` + `tsconfig.base.json` vs `export*` + `utils.ts` + `demo/test/export/`).
Every other unit runs **serial**, orchestrator-committed between units, because
`PaletteCardMeta.vue` (c, f), the five Admin panels (d, e, g), and `BrowsePane`/`PalettesPane`
(d, g) are each written by more than one unit. No parallel unit shares a write path.

## 4b. Worktree Plan

| Agent unit | Sibling worktree absolute path | CARGO_TARGET_DIR |
|---|---|---|
| X.W7.a | `/Users/mkbabb/Programming/value-x-w7-a` | n/a (no Rust in this repo) |
| X.W7.b | `/Users/mkbabb/Programming/value-x-w7-b` | n/a |

Only the parallel pair takes worktrees. The orchestrator runs `git worktree list` /
`git worktree add` before dispatch and merges both before `X.W7.c` opens. Units c–g run on clean
main; the orchestrator commits between each.

## 5. Agent Units

Model law **M-23**: every unit below is an **Opus implementation seat**. No design content is
authored here — the canonical design and motion law is X-W10, twice-authored Fable ∥ Opus into a
fresh-Fable apotheosis. A unit that finds itself inventing a design register (rather than applying
a censused disposition) has left its lane and triumvirates per §3a.

### X.W7.a Checkbox contract and template strictness

- **Goal**: the two `<Checkbox>` consumers stop lying — a tick changes state — and the drift class
  that hid it becomes a compile error.
- **Mechanism**: bind `:model-value` / `@update:model-value` at both sites (`SearchFilterBar`
  discards the `CheckedState` payload — `toggleTag` derives the next array; `TagEditPopover`
  consumes it as `v === true`). Then set `vueCompilerOptions.strictTemplates: true` in
  `tsconfig.base.json` and type the `demo/ui/checkbox` re-export so `CheckboxProps` is visible to
  `vue-tsc`. No shim, no dual path, no rename-only patch.
- **Files**: `demo/palettes/browser/search/TagEditPopover.vue`,
  `demo/palettes/browser/search/SearchFilterBar.vue`, `demo/ui/checkbox/index.ts`,
  `tsconfig.base.json`.
- **Sub-gate**: G1, G2, G3.

### X.W7.b Export truth and one slug

- **Goal**: the bytes users download are the bytes W51 certified, and a non-ASCII palette name
  survives export.
- **Mechanism**: point `usePaletteExport` at `demo/palettes/export/` (`json`/`css`/`tailwind`/`svg`/
  `png` + `canonical`/`rfc8785`/`digest`), delete `demo/palettes/export.ts` whole, keep one
  `slugify` in `demo/palettes/utils.ts` (the Unicode-normalising one) and delete the ASCII-only
  duplicate with it. The N-invariant `export/png.ts` (`WIDTH = 1200`) replaces the
  `N × 60 × 2`-wide raster. Author the failure-disposition table (§8) and surface export failure.
- **Files**: `demo/palettes/export.ts` (delete), `demo/palettes/usePaletteExport.ts`,
  `demo/palettes/export/*.ts`, `demo/palettes/utils.ts`, `demo/test/export/byte-exact.test.ts`.
- **Sub-gate**: G4, G5, G6, G7.

### X.W7.c Palette specimen and the arbitrary-N law

- **Goal**: a palette's rendered truth is independent of N.
- **Mechanism**: extract a props-only `PaletteSpecimen` (strip + name + counts, zero CRUD imports,
  zero nested interactive controls). Meta row: `min-w-0 overflow-hidden` on the left group +
  declared collapse priority + a `+N` overflow chip that reveals the remainder, each chip
  `truncate max-w-[9ch]` — **composition, not masking** (the A-20 ruled interim: container queries
  + `min-inline-size: 0` + declared priority). Strip: delete the `Math.max(…, 0.5)` clip-inducing
  floor for `flex-basis` proportional to weight; above the band-legibility threshold switch to a
  summarizing strip (the `PreviewStrip` mask-fade or `MixResultDisplay` gradient idiom, both
  already in-tree). Shadow follows the rounded silhouette at the card root; hover register is a
  designed elevation/material shift at the same root. Author `demo/test/palettes/n-fixtures/`.
- **Files**: `demo/palettes/browser/card/**` (create `PaletteSpecimen.vue`),
  `demo/test/palettes/n-fixtures/`.
- **Sub-gate**: G8, G9, G10, G11, G12.

### X.W7.d Entity inspector and CRUD ownership

- **Goal**: one mutation, one owner, one visible result.
- **Mechanism**: rename, menu, feedback, versions, tags, export, publication, vote, feature, and
  delete move to the selected-entity inspector; `PaletteCard.vue` deletes once every consumer has
  moved. Fix the four confirmed DAG defects: the silent mutation error (HF-5's residue), the
  soft-delete/`deletedAt` count disagreement, the stale expanded-child list on roster refresh, and
  the Admin menu inheriting user actions (Save/Remix/Export/Report leave the Admin scene).
  Enforce the server bounds client-side (`colors ≤ 50`, `tags ≤ 10`) with a pre-flight signal, not
  a relayed zod string.
- **Files**: `demo/palettes/BrowsePane.vue`, `PalettesPane.vue`, `admin/AdminPane.vue`,
  `browser/admin/*.vue`, `usePaletteActions.ts`, `usePalettePorts.ts`, `useVersionHistory.ts`,
  `useTagEdit.ts`, `useAdminUsers.ts`, `usePaletteStore.ts`.
- **Sub-gate**: G13.

### X.W7.e Deliberate dismissal at destructive seats

- **Goal**: nothing destroys on a single unconfirmed click.
- **Mechanism**: compose `dismiss="deliberate"` + `<Button tone="destructive">` at the four
  destructive Admin seats (color-name delete, tag delete, flagged-palette delete, delete-all-
  palettes). No `Confirm*`/`Gate*` symbol is minted producer-side and none is expected — A-3 was
  refused on standing law, and the safety row closes consumer-side by composition. Rename the
  delete-all control's visible and accessible names to state the destruction.
- **Files**: `demo/palettes/browser/admin/AdminNamesPanel.vue`, `AdminTagsPanel.vue`,
  `AdminFlaggedPanel.vue`, `AdminUsersPanel.vue`.
- **Sub-gate**: G14, G15.

### X.W7.f Display formatting facility

- **Goal**: one facility owns display precision; `src/` keeps its round-trip fidelity untouched.
- **Mechanism**: create `demo/color-session/format-color.ts` beside `picker-color.ts` (the existing
  single demo-side chokepoint), importing the same `PICKER_CHANNELS` metadata. Four registers —
  `compact` / `caption` / `interchange` / `exact` — over a **per-channel** decimal table (the
  per-space rule cannot express `oklch`). `exact` is the W51 export path and stays untouched.
  Route the 29 user-facing raw-12dp sites (17 visible text, 11 ARIA, 1 model write-back) through
  it and retire the 3 dead precision APIs. Add the compact-number formatter for counts, exact
  number preserved in `title`. The `lch C` `compact` cell is an **owner-ruling row** — it carries
  a marker, not a guess. The `[VERIFY]` CSS Color 4 / CSSOM citations in OM-14 §4 are re-checked
  against published spec text before they are consumed as authority.
- **Files**: `demo/color-session/format-color.ts` (create), `demo/color-session/ColorSpaceSelector.vue`,
  the censused readout sites, `demo/palettes/browser/card/PaletteCard/PaletteCardMeta.vue`.
- **Sub-gate**: G16, G17, G18.

### X.W7.g Text abrogation and the ShadowPalette plate

- **Goal**: the contrived voice cannot regrow, and the marked plate stops occupying a full result's
  worth of space to say nothing.
- **Mechanism**: execute the OM-15 dispositions (26 KILL / 31 REDUCE / 3 MERGE / 8 KEEP) with a
  per-row receipt. **The structural precondition is the wave, not a rider**: remove `eyebrow` from
  `EmptyState.vue`'s defaults *and* its props block and delete the element; associate the 14
  uncoupled labels (`id` + `aria-labelledby`, or a real `<label for>`) so the duplicate strings
  evaporate; consolidate the seven caption dialects on `.section-label`. Plate: cure lands on
  **both** `ShadowPalette.vue` and `PaletteCardSkeleton.vue` (they share the geometry) and the
  `ExtractWorkbench.vue:163-166` caption dies under every option; `o9-shadow-palette.spec.ts`'s
  premise is re-ruled in the same commit, never left inverted.
- **Files**: `demo/shared/ui/EmptyState.vue`, `demo/palettes/browser/card/ShadowPalette.vue`,
  `PaletteCardSkeleton.vue`, `demo/workbenches/extract/ExtractWorkbench.vue`, the censused copy
  sites, `e2e/smoke/oracles/o9-shadow-palette.spec.ts`.
- **Sub-gate**: G19, G20.

## 6. Hard Gate

Every baseline below is **measured read-only at 2026-08-03 against `tranche-u` HEAD** unless marked
`MEASURE-AT-OPEN`. No number here is inferred.

### G1 — Checkbox binding, zero occurrences

- **Command**: `grep -rn "update:checked\|:checked=" demo/ --include='*.vue' | grep -v node_modules`
- **Born-RED (measured)**: 4 lines / 2 files —
  `TagEditPopover.vue:28`, `:29`; `SearchFilterBar.vue:52`, `:53`. Family rate 2/2 = 100%.
- **GREEN**: zero lines.
- **Falsifier**: re-introduce `:checked=` at either site and the grep is non-zero. The gate cannot
  pass by renaming the file or moving the binding — it is a repo-wide occurrence count.

### G2 — Checkbox behaviour, real emit

- **Command**: `npx vitest run demo/test/palettes/checkbox-contract.test.ts` (mounts both real SFCs
  against real glass-ui, clicks a tag box, reads the emit ledger).
- **Born-RED (measured, `DEFECT-LEDGER` receipts)**: `TagEditPopover` — `emitted "[]"`,
  `saveTagsCalls 0`, box renders `aria-checked="false"` while `currentTags=["warm"]`.
  `SearchFilterBar` — `update:selectedTags = undefined` after click; `data-state` flips
  `unchecked → checked` while `netAfterTagClick` is the unchanged boot triple (zero requests).
- **GREEN**: one click ⇒ one `update:selectedTags` / one `saveTags` call; prop-driven initial state
  reflected.
- **Falsifier**: revert either binding and the emit ledger is empty ⇒ RED. A visual-only assertion
  would have passed the RED state (the control *looks* ticked) — the emit ledger is what
  distinguishes them.

### G3 — Template strictness closes the drift class

- **Command**: `vueCompilerOptions.strictTemplates: true` present in `tsconfig.base.json`, then
  `npx vue-tsc -p tsconfig.demo.json --noEmit` exits 0.
- **Born-RED (measured)**: `grep -n "vueCompilerOptions\|strictTemplates" tsconfig*.json` → **no
  matches**. Today `vue-tsc -p tsconfig.demo.json --noEmit` exits **0 with the defect live** — the
  repo's strictest gate is structurally blind to glass-ui component-API drift. Diagnostic count on
  first enabling: **MEASURE-AT-OPEN** (unknown; triumvirate trigger per §3a if it exceeds the
  wave's absorption).
- **GREEN**: flag on, exit 0.
- **Falsifier (mandatory one-time demonstration)**: mis-bind one prop on any glass-ui component and
  `vue-tsc` must exit non-zero. Run it, paste the output, revert. Without this the gate is a
  config assertion, not a gate.

### G4 — The certified serializers become the shipping path

- **Command**: `grep -rn "palettes/export\"" demo | grep -v node_modules` and an importer census of
  `demo/palettes/export/serializers.ts`.
- **Born-RED (measured)**: `demo/palettes/usePaletteExport.ts:9` imports `./export` (the legacy
  seat). `demo/palettes/export/serializers.ts` has **exactly one importer** —
  `demo/test/export/byte-exact.test.ts:23`, its own test. W51 certified GREEN-PURE bytes that no
  user has ever received.
- **GREEN**: `demo/palettes/export.ts` absent; the composable imports only from
  `demo/palettes/export/`; the serializers' importer set contains at least one product consumer.
- **Falsifier**: point the composable back at a legacy module and the importer census fails. The
  gate names a **product consumer**, per L-19 — a serializer whose only importer is its own test is
  the exact shape L-19 forbids counting as coverage.

### G5 — Non-ASCII names survive export

- **Command**: `npx vitest run demo/test/export/byte-exact.test.ts` with a `日本 Blue` case.
- **Born-RED (measured, node probe 2026-08-03)**: the shipping
  `slugify("日本 Blue")` → `"blue"`. The name is silently destroyed; the file is
  `blue.json`.
- **GREEN**: the exported filename retains the palette's identity (NFD-normalised transliteration or
  percent-safe fallback — the unit rules which, once) and the payload's `name` field is byte-exact.
- **Falsifier**: restore `/[^a-z0-9]+/g` and the expected filename string fails.

### G6 — One slug implementation

- **Command**: `grep -rn "function slugify\|const slugify" demo | grep -v node_modules | wc -l`
- **Born-RED (measured)**: **2** — `demo/palettes/utils.ts:3` (Unicode-normalising) and
  `demo/palettes/export.ts:9` (ASCII-only, the lossy one).
- **GREEN**: exactly 1.
- **Falsifier**: a third implementation, or a re-added second, makes the count ≠ 1.

### G7 — Failure is surfaced, not swallowed

- **Artifact**: `docs/tranches/X/waves/W7-failure-dispositions.md` — one row per site:
  `SURFACE` / `LOG-ONLY-BY-RULING` / `DELETE`, ruled once, not per-site ad hoc.
- **Born-RED (measured)**: `demo/palettes/` carries **38** `console.warn` calls, of which **31**
  sit inside a `catch`. `usePaletteExport.ts:21-23` is the canonical shape: the whole export switch
  is wrapped in one `try` whose `catch` is `console.warn("Export failed:", e)` — every export
  failure is invisible to the user.
- **GREEN**: the table exists and is complete over the 31; a runtime test forces an export throw and
  asserts a visible error surface.
- **Falsifier**: stub the error channel and the runtime test goes red. The table alone is not the
  gate — a doc cannot fail for the right reason.

### G8 — Tag truth

- **Command**: N-fixture assertion at `N_tags = 4` and `N_tags = 10`.
- **Born-RED (measured)**: `demo/palettes/browser/card/PaletteCard/PaletteCardMeta.vue:37` —
  `v-for="tag in (palette.tags ?? []).slice(0, 3)"`. Tags 4–10 render **nowhere**: no `+N`, no
  tooltip, no scroll. Server cap is 10 (`api/src/modules/palette/schema.ts` —
  `tagsArraySchema = z.array(z.string().min(1).max(30)).max(10)`), so up to 7 tags of a legal
  payload are silently lost. Sibling instance: `AdminFlaggedPanel.vue:54` `slice(0,5)`.
- **GREEN**: all tags reachable — rendered, or behind a `+N` affordance that reveals them.
- **Falsifier**: the fixture asserts the reachable tag set equals the fixture's tag set; a
  re-introduced bare slice drops a named tag and fails.

### G9 — Meta row does not overflow inside the legal bound

- **Command**: mounted assertion at 390×844 with 3 tags of 30 chars each (a **legal** server
  payload): `card.scrollWidth <= card.clientWidth` and the name's rendered width > 0.
- **Born-RED (measured)**: every meta chip is `shrink-0` (`PaletteCardMeta.vue:39`) inside a row
  with no `flex-wrap` and no `overflow-hidden`, and the card deliberately carries no clip
  (`PaletteCard.vue:18-19`); `PaletteCard.vue:43-44` is the row. ≈450px of unshrinkable chips on a
  390px card; the name collapses to zero width first. Exact overflow px: **MEASURE-AT-OPEN** at
  390×844 (record the number in the gate log).
- **GREEN**: no horizontal overflow; name width > 0; card height invariant across the fixture set.
- **Falsifier**: restore `shrink-0` on the chips and `scrollWidth` exceeds `clientWidth`. Note this
  is the **P0** of the OM-16 census and it fires *inside* the data-layer bound, not beyond it.

### G10 — The N-fixture battery

- **Command**: `npx vitest run demo/test/palettes/n-fixtures/`
- **Fixture set (the ledger's own; each value names a real boundary, not a cross-product)**:
  colors `N = 0, 1, 2, 5, 50, 200, 201` — empty / singular / plural / typical / **server cap** /
  the measured sliver-and-clip threshold / one past it. Tags `N = 0, 1, 2, 3, 10, 11` — the
  `slice(0,3)` edge, the server cap, and one past it.
- **Born-RED (measured, OM-16 census)**: tags drop at `N ≥ 4` (`PaletteCardMeta.vue:37`); colors
  clip entirely at `N > 200` (`PaletteColorStrip.vue:8,16,70`); bands become illegible slivers at
  `N ≥ 100` and the weight floor `Math.max(100 / n, 0.5)` silently flattens to equal width at
  `n ≥ 13` (`:48,63-68`); the shipping PNG raster is `N × 60 × 2` px wide (`export.ts:61-65,93-96`)
  and produces a blank/null blob at `N ≈ 136` while the N-invariant `export/png.ts` (`WIDTH = 1200`)
  sits unused; `N_colors = 51` on publish surfaces a raw zod string
  (`usePaletteActions.ts:54-57`) because `usePaletteStore.ts:66-95` has no client cap;
  `N_tags = 11` round-trips to a 400 with no visible failure path (`TagEditPopover.vue:64-66,77`).
- **GREEN**: at every fixture value, displayed counts equal the fixture's N, nothing is silently
  dropped, the card height law holds, and the export raster is N-invariant.
- **Falsifier**: each fixture asserts against its own N, so a regression names the exact boundary it
  broke. The set is **self-contained** — fixtures are in-repo objects, no server, no network — which
  is why this wave is COMPLETABLE alone.

### G11 — Shadow silhouette and hover register

- **Command**: X-W1's `e2e/visual/` goldens — PaletteCard rest + hover, 3 viewports, light/dark,
  with X-W1's stated tolerance. **This wave authors no second screenshot mechanism.**
- **Born-RED**: owner witnesses `OM-11-palette-card-shadow-artifacts.png` and
  `OM-12-palette-card-shadow-artifact-closeup.png` — a hard-edged dark slab offset below-left with
  faceted corners against the card's rounded geometry, the band poking out from behind the
  top-right corner; and **no hover treatment at all** on a draggable, menued, selectable container.
  Mechanism sits in the `cartoon-surface` atom / `.cartoon-cast` child
  (`PaletteCard.vue:8-11`). Owner's words: *"this is to be fixed."*
- **GREEN**: goldens show the cast following the rounded silhouette at all four corners, and a
  distinct hover frame.
- **Falsifier**: the golden diff exceeds tolerance. Plus a root-only check — a per-instance shadow
  or hover override reintroduced anywhere in `demo/palettes/` fails the standing root-styling law
  (M-14 clause 1: no adhoc fixes).
- **Dependency note**: if X-W1's harness has not landed, G11 is the one gate that cannot run.
  It **blocks G11 only**; the other 19 gates are independent.

### G12 — Specimen purity

- **Command**: import-graph assertion — `PaletteSpecimen.vue`'s transitive import set excludes
  `usePalettePorts` / `usePaletteActions` / any CRUD transport; plus a mounted count of interactive
  descendants inside the specimen subtree.
- **Born-RED (measured)**: the component does not exist. Today `PaletteCard.vue` hosts the vote
  `<button>` (`PaletteCardMeta.vue:43-49`), the overflow menu (`PaletteCardMenu.vue`), and an
  editable name (`PaletteCard.vue:56`) — presentation and mutation in one seat.
- **GREEN**: zero CRUD imports; zero interactive descendants inside the specimen.
- **Falsifier**: add a port import and the graph assertion fails; add a `<button>` and the mounted
  count is non-zero.

### G13 — One owner per mutation, one visible result

- **Artifact**: `docs/tranches/X/waves/W7-mutation-ownership.md` — one row per mutation
  (rename, tag, version, publish, unpublish, fork, vote, feature, delete, delete-all), each naming
  its single call site and its visible result.
- **Born-RED (DAG audit §2.3, confirmed value defects)**: Admin feature was broken and silent —
  HF-5 closed the wire, the absent mutation error remains (row 1); soft-deleted palettes inflate
  user counts, count and collection query disagree on `deletedAt` (row 2); expanded user palettes
  go stale until collapse/re-expand (row 3); Admin cards inherit Save/Remix/Export/Report beside
  Feature/Delete (row 6).
- **GREEN**: table complete, one call site each, and a browser assertion per row that the result is
  visible.
- **Falsifier**: two call sites for one mutation ⇒ table row fails; a mutation whose result is not
  observable in the same frame ⇒ browser assertion fails.

### G14 — Deliberate dismissal at destructive seats

- **Command**: `grep -rn "dismiss=\"deliberate\"" demo/ | grep -v node_modules` plus an e2e that
  clicks each destructive control and asserts **no mutation request fires** until the confirm is
  accepted.
- **Born-RED (measured)**:
  `grep -rn "window.confirm\|<ConfirmDialog\|dismiss=\"deliberate\"" demo/` → **0 matches,
  repo-wide**. `AdminNamesPanel.vue:111` emits `delete` on click;
  `AdminTagsPanel.vue:101` calls `tagsApi.deleteTag(tag.name)` on click. Flagged-palette deletion
  fires immediately (DAG §2.3 row 5). No confirmation, no undo, anywhere.
- **GREEN**: four seats confirmed; zero destructive requests before acceptance.
- **Falsifier**: remove a confirm wrapper and the network assertion catches the early request. A
  grep alone would pass on a decorative dialog — the network assertion is what makes it a gate.

### G15 — Destructive naming

- **Command**: accessible-name assertion on the delete-all control.
- **Born-RED**: the button that means "delete all palettes" is labelled `Palettes`; it discloses its
  meaning only after activation (DAG §2.3 row 4).
- **GREEN**: visible and accessible names both state the destruction.
- **Falsifier**: the assertion compares the computed accessible name against the required phrase.

### G16 — The formatting facility owns display precision

- **Command**: `npx vitest run demo/test/color-session/format-color.test.ts` (one case per register
  per channel class) plus an import census: every censused display site imports the facility.
- **Born-RED (measured)**: **31** runtime `toFixed(` calls under `src/` + `demo/` in **6** distinct
  precisions (0, 1, 2, 3, 4, 12); **0** `toPrecision`; **0** `Intl.NumberFormat` repo-wide. Root
  emitter `src/css/grammar.ts:283-285` — `format = (value) => Number(value.toFixed(12)).toString()`
  — reaches **29 user-facing sites** (17 visible text, 11 ARIA spoken digit-by-digit, 1 model
  write-back) with no formatter in between. The mark's own card
  (`ColorSpaceSelector.vue:91`) renders `rgb(154.029889788152 …` seventeen times at once, once per
  dropdown row.
- **GREEN**: exact-string assertions per register; the 29 sites read through one module; the 3 dead
  precision APIs deleted.
- **Falsifier**: change a per-channel decimal and the exact-string test fails; bypass the facility
  at any censused site and the import census fails.

### G17 — Compact counts

- **Command**: fixture with `voteCount = 12345`.
- **Born-RED (measured)**: **0** compaction helpers repo-wide. 4–5-digit counts add ≈80px of
  unshrinkable width to the G9 row (`PaletteCardMeta.vue:22,32`).
- **GREEN**: renders `12.3k`; `title` retains `12345`.
- **Falsifier**: assert both the compacted string and the exact `title` — a formatter that loses the
  exact value fails on the second assertion.

### G18 — `src/` containment (the epoch rule)

- **Command**: `git diff --stat src/` across this wave's commit range = 0 changed lines.
- **State today**: **GREEN** — this is a containment gate, RED only if violated. It is the one gate
  here that is not born-RED, and it is deliberate: OM-14 §4.1 rules the display formatter demo-side
  because `grammar.ts:285` is a library contract asserted by `test/v4-css-public.test.ts:86` and
  `test/v4-c1.test.ts:177,189` and sits under the live parser proof gate.
- **Falsifier**: it fails the moment any unit edits `src/`. That is its whole purpose — an
  additive `{ precision }` option on `serializeCssColor` is a **different wave** (X-W9), and this
  gate is how the two stay unconflated.

### G19 — Text abrogation, with the structural precondition

- **Command**: per-row receipt over the OM-15 census + `grep -rn "eyebrow" demo/ | grep -v
  node_modules` → 0.
- **Born-RED (measured, OM-15 census 2026-07-28)**: **68 hits** — class 1 (precious/contrived
  voice) 27, class 2 (duplicative) 27, class 3 (explicit mechanics) 14; 9 carry two classes.
  Dispositions: 26 KILL, 31 REDUCE, 3 MERGE, 8 KEEP. The breeding ground is structural, not
  editorial: `demo/shared/ui/EmptyState.vue:90` makes `eyebrow: "· empty plate ·"` a **prop
  default**, so all 11 consumers are born with a contrived caption; in all 11 the eyebrow is a
  verbatim restatement of the `message` directly beneath it. Densest files:
  `BrowsePane.vue` 6, `AuroraPane.vue` 6, `ActionToolbar.vue` 5, `GradientVisualizer.vue` 4,
  the 5 admin panels 12 combined. The idiom has **7 dialects**, 4 of which re-declare
  `uppercase`/`tracking` that `text-mono-caption` already applies.
- **GREEN**: 26 KILL strings absent; 31 REDUCE strings rewritten to one plain clause (no metaphor,
  no "click/tap", no positional words, no house jargon in an accessible name); the `eyebrow` prop
  **and** element deleted; the 14 uncoupled labels associated; dialects consolidated on
  `.section-label`.
- **Falsifier**: the census is the denominator and it has a product consumer — the rendered UI —
  so this is not an L-19 contrivance. A wave that edits only the 68 strings and leaves the prop
  default **regrows them**; the `eyebrow`-grep is the falsifier that catches exactly that.

### G20 — The ShadowPalette plate

- **Command**: measured height assertion at `k = 16` (the slider max,
  `ExtractControls.vue:29 :max="16"`), on **both** `ShadowPalette.vue` and
  `PaletteCardSkeleton.vue`.
- **Born-RED (measured, OM-15 §3.2)**: at `k = 16` and `sm`+: strip 40 + meta (20 + 20) + swatches
  (3 rows × 56 + 2 gaps × 8 = 184) + `pb-3` 12 ≈ **276px of pure skeleton**; at the default `k = 5`
  ≈ **148px**. Sixteen 56px full discs (`rounded-badge` → `--radius-pill` → 9999px) — the highest-
  mass element on the pane at rest, representing nothing. The ghost is sized as a 1:1 replica of
  the developed card, and its 16 circles restate the 16 strip cells: the plate duplicates its own
  information at ≈5× the height. Geometry is duplicated in `PaletteCardSkeleton.vue:44-77` — same
  four blocks, same `h-10` strip, same `h-5 w-32`/`h-5 w-6` meta, same swatch classes, same stagger
  arithmetic — so a one-file cure leaves the mass at the loading state.
- **GREEN**: measured height at `k = 16` below the stated budget on both components; the
  `ExtractWorkbench.vue:163-166` caption deleted; `o9-shadow-palette.spec.ts` re-ruled in the same
  commit (it currently asserts the seat *survives* — that premise inverts under the cure and may
  not be left dangling).
- **Falsifier**: the height assertion is numeric, at a named `k`; and the oracle re-ruling is a diff,
  not a promise.

## 7. Format And Lint Cadence

- After each agent unit and before the wave closes: `npm run lint` (`eslint . --max-warnings=0`),
  `npm run typecheck` (`vue-tsc -p tsconfig.lib.json --noEmit && vue-tsc -p tsconfig.demo.json
  --noEmit`), `npm test` (`vitest run`), `git diff --check`.
- `npm run test:e2e` after `X.W7.c`, `X.W7.e`, and at close (the browser-bearing gates).
- After `X.W7.a` lands, `typecheck` is running under `strictTemplates` — the cadence is the drift
  gate, not a formality. No unit closes on a red typecheck.
- Nothing is intentionally skipped.

## 8. Verification Artefacts

| Artefact | Path |
|---|---|
| Gate log (one entry per G1–G20: command, RED output, GREEN output, timestamp) | `docs/tranches/X/waves/W7-gate-log.md` |
| `strictTemplates` falsifier demonstration (mis-bind → non-zero → revert) | `docs/tranches/X/waves/W7-gate-log.md` §G3 |
| Failure-disposition table over the 31 `catch → console.warn` sites | `docs/tranches/X/waves/W7-failure-dispositions.md` |
| Mutation-ownership table | `docs/tranches/X/waves/W7-mutation-ownership.md` |
| OM-15 per-row abrogation receipt (68 rows) | `docs/tranches/X/waves/W7-om15-receipt.md` |
| N-fixture battery | `demo/test/palettes/n-fixtures/` |
| PaletteCard rest + hover goldens (3 viewports × light/dark) | `e2e/visual/` (X-W1-owned) |
| Meta-row overflow measurement at 390×844, pre- and post-cure | `docs/tranches/X/waves/W7-gate-log.md` §G9 |
| Plate height at `k = 16`, both components, pre- and post-cure | `docs/tranches/X/waves/W7-gate-log.md` §G20 |
| Integration commit hashes | recorded in the gate log header |

## 9. Commit Plan

| # | Scope | Body required |
|---|---|---|
| 1 | `fix(palettes/checkbox)` — real `modelValue` contract at both sites | yes (data-loss cure + measured receipts) |
| 2 | `build(demo/types)` — `strictTemplates` on; typed checkbox re-export | yes (gate change + falsifier output) |
| 3 | `refactor(palettes/export)` — certified serializers become the shipping path; legacy seat deleted; one slug | yes (deletion) |
| 4 | `fix(palettes/export)` — failure surfaced; 31-site disposition table | yes (broad) |
| 5 | `feat(palettes/specimen)` — props-only `PaletteSpecimen`; priority-collapse meta row; `+N` chip | yes |
| 6 | `test(palettes/n-fixtures)` — the arbitrary-N battery | yes (generated/broad) |
| 7 | `fix(palettes/card)` — cast shadow follows silhouette; hover register at root | yes (visual gate) |
| 8 | `refactor(palettes/inspector)` — mutation ownership; DAG defects 1/2/3/6 | yes (broad) |
| 9 | `fix(palettes/admin)` — deliberate dismissal ×4; destructive naming | yes (safety) |
| 10 | `feat(color-session/format)` — the display formatting facility + compact counts | yes (broad) |
| 11 | `refactor(demo/copy)` — OM-15 abrogation; `eyebrow` prop and element deleted | yes (broad + deletion) |
| 12 | `fix(extract/plate)` — ShadowPalette + PaletteCardSkeleton mass; caption deleted; o9 oracle re-ruled | yes (deletion + oracle) |
| 13 | `docs(X.W7)` — gate log, dispositions, receipts, status | yes (status) |

Pathspec commits only. Units a/b commit in their worktrees; the orchestrator integrates.

## 10. Dependencies

- **Depends on**: **X-W3** (the API policy kernel and write contract — this wave reads the cured
  surface and adds no backend change) · **X-W4** (the typed `SceneActionSet`; the inspector's action
  seat is expressed in it, not in `ref<any>` dispatch) · **X-W1** for **G11 only** (the
  `e2e/visual/` golden harness; the other 19 gates are independent of it).
- **Blocks**: **X-W8** — shadcn abrogation and graph subtraction are pure subtraction *after* this
  wave stabilises the palette destinations; the `demo/ui` barrel census is measured against the
  post-W7 tree, not the pre-W7 one. `demo/ui/checkbox/index.ts` is carved here and deleted there.
- **Feeds**: **X-W10** — the hover register, the shadow silhouette treatment, and the plate's new
  spacing enter the design canon as *applied* precedent, not as open questions.
- **Not blocked by glass 8.0.0.** See §Dispositions.

## Dispositions (CC rows transposed)

| CC row | source ID | verb | lands as |
|---|---|---|---|
| CC-069 | MT-F036 | BUILD | X.W7.c — shadow silhouette + hover register at the card root (G11) |
| CC-070 | MT-F038 | BUILD | X.W7.f — `demo/color-session/format-color.ts`, four registers, per-channel table (G16, G17, G18) |
| CC-071 | MT-F039 | BUILD | X.W7.g — 68-hit abrogation + the plate (G19, G20) |
| CC-072 | MT-F040 | BUILD | X.W7.c — N-fixtures + A-20's ruled composition interim (G8, G9, G10) |
| CC-073 | DR-28 | BUILD | X.W7.b (export half: G4–G7) + X.W7.d (UI half: G13); the two V-prime riders — `__Host-value-session` transition and the Prepare→Download seat — fold into X.W7.b/d |
| CC-074 | A-19 · A-20 | BUILD | X.W7.c, **consumer-side only** — container queries + `min-inline-size: 0` + declared priority; composition, not masking |
| CC-075 | A-3 | BUILD | X.W7.e — `dismiss="deliberate"` + `<Button tone="destructive">` (G14, G15) |
| CC-081 | K-10, Checkbox limb | BUILD | X.W7.a (G1, G2, G3); the Button/Skeleton/dead-surface/PointerDebugOverlay limbs are X-W8, the ICtCp limb X-W9 — not this wave |
| CC-006 | V·MT5 · V·C4 | FOLD | this file's §2a/§3/§6 — the specimen/domain split and the N-tuple completion law are transposed into G8–G13 |
| CC-076 | Admin DAG rows | FOLD | X.W7.d — `CONSUMER-ADMIN-DAG-AUDIT-2026-07-28.md` §2.3 rows 1/2/3/4/5/6 become G13/G14/G15; rows 7/8 (ceremonial impersonation, server-only rails) are **X-W8 subtraction**, row 9 (Admin never owns the canvas) is **X-W5**, row 10 (shadcn residue) is **X-W8** |
| CC-077 | palette component apotheoses | FOLD | X.W7.c/d — consumes `registry/adjudicated/*` palette rows and `registry/harvest/area-palettes.json` |

**Left in the glass bank — not smuggled in:**

- **CC-115** (A-21 compact formatting · A-22 badge `min-inline-size` floor) — **BLOCKED-ON TR#87
  shipping at glass 8.0.0**; consumer adopts at the single 8.0.0 repin census. This wave builds
  **only** CC-070's ruled consumer interim (G16/G17). The interim is explicitly the ledger's own
  disposition, not an early adoption.
- **CC-074's producer half** (A-19 chip-overflow seat TR#43 ⊕⁷, A-20 priority-collapse seat TR#76
  LIB-SEAM ⊕⁷) — `NEW-SEAT (record, default DECLINE-AT-8.0.0)`, owner-revivable next-window.
  Glass-owned; **never a value gate**.
- **CC-044** (six WatercolorDot impostors) and **CC-113** (A-18 watercolor indicator paint) —
  BLOCKED-ON CC-003's four-part trigger and TR#84 respectively. Neither enters this wave, even
  where a palette surface hosts a `watercolor-dot`.

The four GLASS-FORWARD items the OM-16 census identified (+N overflow chip / `ChipGroup
max-visible`, priority-collapse meta row, compact-number atom, count-badge min-width) were relayed
the day found as **O-18a**. Their producer disposition is recorded above; the relay is discharged
and is not re-sent.

## 11. Archaeology

This wave revisits **V·MT5** (`MECHANISM-CUT-FORMATION-MAP-2026-07-29.md:287`) and **V·C4**
(`CONVERGENCE-RESUME-HANDOFF-2026-07-29.md:150`) — both `CANDIDATE`/`NOT-STARTED`, never specified,
never executed. Before them, DR-28's UI half was booked to V·W50, which never ran; and RF-26 lists
CH-8 among the eight riders that may not be re-chartered, while `CARRY-LEDGER §B` chartered it
anyway — the act L1 names as forbidden.

**Failure mode**: the row was carried by naming it, five times, with no gate that could go red.
The certified export serializers are the emblem — W51 proved them byte-exact GREEN-PURE and left
them with one importer, their own test, for the whole of V.

**New guardrail**: every gate in §6 carries a **measured RED baseline taken read-only before any
cure**, and a named falsifier. G4 in particular is written as a *product-consumer* census
(L-19) precisely so that "certified" can never again mean "certified against itself".

## L-18 rider — the challenge obligation

This file makes the wave **SPECIFIED**. It does not make it done, and it does not make it right.

The implementation this wave produces must survive **two quartet challenge passes** — four Opus
skeptics per pass, each hunting the gates' own falsifiers — followed by a **fresh Fable
apotheosis**, before any unit is marked ACCEPTED. The passes are adversarial against the gate
wording as much as against the code: a gate that cannot be made to go red for its intended reason
is a defect in this file, not a pass.

Reference, not ceremony: `FORMATION-LAWS` L-18 (distinct from the Dock adjudication's local
`L-18`/`L-19`/`L-20`, which are unrelated IDs — see CC-083).
