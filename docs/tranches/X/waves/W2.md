# X.W2 - Boot boundary and eager payload

## State

**Name**: W2 - Boot boundary and eager payload
**Opens after**: X-W1 (re-gate) — see §10; the byte legs are bench-executable without it
**Agents**: 4 serial
**Hard gate**: eager JS gzip ≤ 280 KiB with a before/after receipt · p75 TBT ≤ 300 ms and p75 LCP ≤ 2500 ms over N≥20 on ONE pinned runner class · the glass blob barrel absent from the eager module set · the wall-clock park replaced by the shipped `settled` seam
**Status**: **PARTIAL — closed `complete_with_misses` 2026-09-17** (units **c** and **a** landed; **b** ESCALATED under §3a *"Quiescence does not park"* and landed no product byte by design; **d** NEVER RAN). Gates at close: **4 GREEN (G1 · G2 280,811 B gz · G4 · G8) · 4 RED (G3 616/529 ms mobile-4× · G5 escalated · G6 unrun · G7 unrun)**. The §State Hard Gate is **NOT MET**. **IMPLEMENTED is deliberately NOT stamped** — its own condition here is *"gates green, bytes landed"*. Close record: `docs/tranches/X/execution/A/X-W2.md` §Close. — **AMENDED at REPAIR 1, 2026-09-17**: CHECK 1 returned NOT-CONFORMANT on D-1 (G6) and D-2 (G7), whose cure was the never-dispatched unit **d**; **unit d has now RUN**. Gates after repair: **6 GREEN (G1 · G2 280,811 B gz · G4 · G6 by its second arm · G7 · G8) · 2 RED (G3 1,282/655 ms mobile-4× · G5 ESCALATED)**. §8 is **7 of 7**. The §State Hard Gate remains **NOT MET** (G3 and G5), so **IMPLEMENTED is still NOT stamped** and the wave stays **PARTIAL**. Repair record: same file, §Repair 1.

**Four-verb status (C-01 law).** AUDITED: yes — `registry/DISEASE-REGISTRY.md` DR-03/DR-06, `registry/CARRY-CUT-LEDGER.md` §1.D. SPECIFIED: yes — this file. IMPLEMENTED: no. VERIFIED: no. This file advances the wave to SPECIFIED and to nothing further. Thereafter this wave's own close stamps **IMPLEMENTED** — gates green, bytes landed; **VERIFIED** is stamped only at X-W11's release close, against its browser / real-Safari and consumer-tuple receipts. No close in this file may stamp VERIFIED.

**Model law (M-23).** Every seat here is implementation → **Opus**. This wave carries no design content; the design canon is X-W10, twice-authored Fable∥Opus → fresh Fable. The file encodes that split; it does not perform it.

---

## 2a. Goal criterion

This wave succeeds if the boot path of the built demo no longer pays for the WebGL2 metaball engine it does not use at boot, and if the boot cost is thereafter a **measured number with a named environment** rather than an escalation. The four-close Q14 chronic ends because the gate becomes executable by one agent at one bench — not because the number improved.

---

## 3. Scope

1. Repoint `demo/color-picker/composables/boot/useAtmosphere.ts:36` from `@mkbabb/glass-ui/blob` to `@mkbabb/glass-ui/blob-config`.
2. Repoint `demo/scenes/blob/BlobPane.vue:12-13` (value + type imports) to `@mkbabb/glass-ui/blob-config`.
3. Replace `HeroBlob.vue`'s wall-clock park (`BLOB_IDLE_MS` + `SLEEPY_POSE_MS` timer chain) with the Blob instance's shipped `settled` ref; delete the dead-book comment naming "the 5.0.0 adopt".
4. Re-derive `e2e/smoke/fixtures/blob-timing.ts` against the quiescence trigger and update its four consumer specs.
5. Author `scripts/perf/eager-bytes.mjs` — the eager-module-set gzip measurement, read from `dist/gh-pages/index.html`'s entry + `modulepreload` graph.
6. Author `e2e/smoke/perf/eager-payload.spec.ts` in the `smoke-perf` project — the byte budget and the N≥20 p75 LCP/TBT collection against the built bundle.
7. Re-measure `e2e/smoke/perf/o5-boot-pacing.spec.ts`'s spike leg after the cut; remove `test.fail()` or re-record the red with today's ratio and today's cause.
8. Rule `BLOB_HERO` (shipped at 7.0.0, zero consumers): consume on the hero path, or tombstone with a one-line rationale.
9. Write the dated §D producer note recording that glass 7.0.0 already ships `settled`, `settledFrame` and `./blob-config` — **explicitly non-gating** (CC-035: "Producer half = one dated §D letter, never a gate").
10. Supersede `audit/telemetry/PERF.json` (2026-07-24, all-zero LCP/FCP/TBT against the MT-F012 empty-mount build) with a dated successor receipt.

---

## 3a. Triumvirate Dispatch

Mandatory triumvirate (research + plan augment + redress); the orchestrator may not redispatch the failing unit alone.

- **File-bound expansion.** Any cure requiring a write outside §4 — in particular `vite.config.ts` manual-chunk surgery, `demo/shell/usePaneRouter.ts`, or any `src/` byte — invalidates the wave's premise (that the eager barrel is an import-site fact, not a bundler-config fact).
- **Budget still red after the cut.** If eager JS gzip remains > 280 KiB once the barrel leaves the eager set, the barrel was not the dominant term. **Re-baselining the budget is forbidden** — it is S.W3's exact failure mode (347.9 KiB measured against ≤280 KiB, then the bar moved). Triumvirate researches the true eager graph instead.
- **TBT still red after the cut.** p75 TBT > 300 ms post-cure means the blocking work is not the barrel parse; halt and research.
- **Quiescence does not park.** If `settled` never goes true within the fixture window on the software-GL renderer, triumvirate. Do **not** restore the wall clock; a wall-clock park is the disease.
- **Third diagnostic iteration.** Any measure→edit→measure loop halts at its third pass.

---

## 4. File Bounds

| File | Access |
|---|---|
| `demo/color-picker/composables/boot/useAtmosphere.ts` | modify-carve (the line-36 import only) |
| `demo/scenes/blob/BlobPane.vue` | modify-carve (the line-12/13 imports only) |
| `demo/picker/visual/HeroBlob.vue` | modify |
| `e2e/smoke/fixtures/blob-timing.ts` | modify |
| `e2e/smoke/webgl-blob-idle.spec.ts` | modify-carve |
| `e2e/smoke/oracles/o12-blob-seat.spec.ts` | modify-carve |
| `e2e/smoke/mobile/blob-presence-mobile.spec.ts` | modify-carve |
| `e2e/smoke/perf/idle-frame-budget.spec.ts` | modify-carve |
| `e2e/smoke/perf/eager-payload.spec.ts` | create |
| `scripts/perf/eager-bytes.mjs` | create |
| `e2e/smoke/perf/o5-boot-pacing.spec.ts` | modify-carve |
| `e2e/smoke/perf/serve-built.mjs` | execute, no write (G3's and G4's built-bundle origin, re-run unmodified) |
| `docs/tranches/X/waves/W2.md` | modify (the §2 Status field + the four-verb line only) |
| `docs/tranches/X/evidence/W2/**` | create |
| `docs/tranches/V/megatranche/audit/telemetry/PERF-X-W2.json` | create |

**Do NOT touch**: `src/**` · `api/**` · `.github/workflows/**` (X-W1 owns) · `lighthouserc.json` (X-W1 owns) · `vite.config.ts` · `demo/shell/usePaneRouter.ts` · `scripts/dev/dev.sh` (CC-021, unowned until the X-W0 sitting) · `node_modules/@mkbabb/glass-ui/**` · `docs/tranches/V/megatranche/audit/telemetry/PERF.json` (append-never-rewrite, F-7 — it is superseded by a new file, not edited).

### 4a. Disjointness

Four units, **serial**. No two units hold `modify` or `modify-carve` on the same path: unit **a** owns the two import sites; unit **b** owns `HeroBlob.vue` + the fixture + its four consumer specs; unit **c** owns the two created files; unit **d** owns `o5-boot-pacing.spec.ts` and the evidence tree. `e2e/smoke/perf/` is shared as a *directory* only — never as a file.

### 4b. Worktree Plan

Not required: the units are serial, one writer at a time, on the wave branch. No sibling worktrees, no per-agent `CARGO_TARGET_DIR` (no Rust surface in this repo). The orchestrator commits between units so each unit opens on a clean tree.

---

## 5. Agent Units

### X.W2.c The eager-payload instrument (runs FIRST — the baseline must predate the cure)

- **Goal**: a single command that answers "what does this product cost at boot?" against the built artifact, and a spec that fails when the answer regresses.
- **Mechanism**: `scripts/perf/eager-bytes.mjs` parses `dist/gh-pages/index.html`, collects the `<script type="module">` entry plus every `rel="modulepreload"` href plus every non-deferred `rel="stylesheet"`, gzips each from disk (`zlib.gzipSync`, default level), and emits raw/gz totals as JSON. `e2e/smoke/perf/eager-payload.spec.ts` asserts the JS-eager gz budget and drives N≥20 cold-cache loads of `#/` at the two configs, reporting p75 LCP / p75 TBT.
- **Files**: `scripts/perf/eager-bytes.mjs` (create) · `e2e/smoke/perf/eager-payload.spec.ts` (create) · `docs/tranches/X/evidence/W2/BEFORE.json` (create).
- **Sub-gate**: `BEFORE.json` exists, is dated, names its runner class, and reproduces the §6 born-RED numbers within tolerance on a clean `npm run gh-pages`.

### X.W2.a The blob-config boot cut

- **Goal**: the glass blob barrel leaves the eager module set because no boot-path module imports it.
- **Mechanism**: two import repoints (`./blob` → `./blob-config`). `HeroBlob.vue:34` keeps `Blob` from `./blob` — it is already in a lazily-imported chunk (`HeroBlob-BDTrmHWe.js`, 2,026 B, dynamically imported, not preloaded), so the barrel follows it out of the eager set once the static boot-path edge is cut.
- **Files**: `demo/color-picker/composables/boot/useAtmosphere.ts` · `demo/scenes/blob/BlobPane.vue`.
- **Sub-gate**: `rg -c "smin|metaball" <eager chunks>` → 0, and the eager JS gz total falls, both recorded in `AFTER.json`.

### X.W2.b The quiescence park

- **Goal**: the hero parks when the engine is at rest, not when a stopwatch expires.
- **Mechanism**: bind `HeroBlob.vue`'s template ref to the Blob instance's `settled: Readonly<Ref<boolean>>` (`Blob.vue.d.ts:63`); watch it to drive `blobPaused` / `:paused`; delete `BLOB_IDLE_MS`, `SLEEPY_POSE_MS`, `idleTimer`, `poseTimer` and the "booked at the 5.0.0 adopt" comment. `blob-timing.ts` stops mirroring HeroBlob's constants and exports a quiescence-derived `PARK_SETTLE_MS` with a stated ceiling; its four consumers adopt the new export.
- **Files**: `demo/picker/visual/HeroBlob.vue` · `e2e/smoke/fixtures/blob-timing.ts` · `e2e/smoke/webgl-blob-idle.spec.ts` · `e2e/smoke/oracles/o12-blob-seat.spec.ts` · `e2e/smoke/mobile/blob-presence-mobile.spec.ts` · `e2e/smoke/perf/idle-frame-budget.spec.ts`.
- **Sub-gate**: `rg "BLOB_IDLE_MS|SLEEPY_POSE_MS" demo/ e2e/` → 0 hits, and the four park specs pass green against the quiescence trigger on the software-GL renderer.

### X.W2.d The re-measure, the o5 verdict, and the receipts

- **Goal**: the wave's claims are a before/after pair with a named environment, and the o5 spike leg tells the truth about today.
- **Mechanism**: re-run unit c's instrument post-cure into `AFTER.json`; re-run `o5-boot-pacing` and either delete `test.fail()` (if the spike leg is green) or rewrite its header with today's median/max ratio and today's diagnosed cause — the "cured at W7" prophecy is void either way. Write the dated §D producer note and the `PERF-X-W2.json` successor receipt.
- **Files**: `e2e/smoke/perf/o5-boot-pacing.spec.ts` · `docs/tranches/X/evidence/W2/**` · `docs/tranches/V/megatranche/audit/telemetry/PERF-X-W2.json`.
- **Sub-gate**: `AFTER.json` and `BEFORE.json` share a runner-class string; the §6 delta gate reads from them, not from prose.

---

## 6. Hard Gate

Every gate names its command, its asserted product property, and **how it can fail for its intended reason** (L-19). Born-RED measurements below were taken read-only on **2026-08-03** at HEAD `41450f02`, against `dist/gh-pages` built `2026-07-29 13:49` — tree-accurate, because `git diff --stat c4af0ef9..HEAD -- src demo api vite.config.ts package.json package-lock.json` is **empty** and the working tree is clean on every product path.

### G1 — the glass blob barrel is absent from the eager module set · BORN-RED

```
$ rg -c "smin|metaball|satellite" dist/gh-pages/assets/index-xOF2oyst.js
smin: 41   metaball: 17   satellite: 19        ← the WebGL2 metaball engine, in the ENTRY chunk
$ rg -c "smin|metaball" dist/gh-pages/assets/HeroBlob-BDTrmHWe.js
0                                              ← the component's own chunk is shader-free, 2,026 B, NOT preloaded
$ grep -rn "glass-ui/blob\"" demo/ | wc -l
3        (useAtmosphere.ts:36 · BlobPane.vue:12,13 · HeroBlob.vue:34,35)
$ grep -rn "blob-config" demo/ src/ | wc -l
0
```

**Asserted property**: no module reachable from the eager entry graph imports `@mkbabb/glass-ui/blob`.
**Falsifier**: re-introduce a static `from "@mkbabb/glass-ui/blob"` edge in any boot-path module and the shader-token count in the eager set returns non-zero. Passes only because the import graph changed — not because a grep was narrowed.

### G2 — eager JS gzip ≤ 280 KiB, with a before/after receipt · BORN-RED

```
$ node scripts/perf/eager-bytes.mjs        # born-RED probe, run inline 2026-08-03
eager JS modules: 6
eager JS   raw= 979024  gz= 313585 = 306.2 KiB      ← RED against ≤ 280 KiB (286,720 B)
render-block CSS raw= 482204  gz= 82803 = 80.9 KiB  ← measured, NOT gated (see §ENV)
TOTAL eager gz= 396388 = 387.1 KiB
```

**Asserted property**: `eagerJsGz ≤ 286720` on a clean `npm run gh-pages`.
**Prediction, falsifiable**: `./blob` is 103,031 B raw / **35,461 B gz**; `./blob-config` is 245 B raw / 190 B gz over a 1,581 B / 782 B gz `presets` module. Upper-bound post-cut eager JS gz = 313,585 − 35,461 = **278,124 B = 271.6 KiB**. If the realized figure lands above 280 KiB the shared-subchunk assumption was wrong → §3a triumvirate, **not** a budget move.
**Falsifier**: add any static boot-path import of a heavy subpath and the number rises past the bar. The bar is fixed at wave-open and may not be edited by the implementing seat.

### G3 — p75 TBT ≤ 300 ms, N ≥ 20, one pinned runner class · BORN-RED

Pilot, **N=5** per config, built bundle at `http://localhost:8091` (`e2e/smoke/perf/serve-built.mjs`), cold cache via CDP `Network.setCacheDisabled`, Playwright 1.60.0 headless Chromium, 2026-08-03:

| config | p75 LCP | p75 TBT | p75 FCP | CLS | transfer | resources |
|---|---|---|---|---|---|---|
| `desktop-unthrottled` 1440×900 | 148 ms | **359 ms** | 148 ms | 0.0193 | 2,021 KB | 19 |
| `mobile-4x-cpu` 390×844, CPU ×4 | 588 ms | **734 ms** | 588 ms | 0 | 1,708 KB | 11 |

Both legs exceed the `lighthouserc.json` `total-blocking-time` error bar of **300 ms**, on the friendliest possible origin (no network latency, no transfer compression). RED.
**Asserted property**: p75 TBT ≤ 300 ms over N ≥ 20 cold loads of `#/`, per config, with the runner class recorded in the receipt.
**Admissible pins** (one, recorded by string): (i) `ubuntu-24.04` via X-W1's restored job; (ii) the wave-open bench recorded as `sw_vers` + `sysctl -n machdep.cpu.brand_string` + node + Playwright versions. A receipt without a pin string fails the gate.
**Falsifier**: raise N to 20 and the pilot's ~2× margin over the bar cannot be sampling noise; lower the CPU throttle and it goes green for the wrong reason — hence the throttle rate is part of the pinned class.

### G4 — p75 LCP ≤ 2500 ms, N ≥ 20, same pinned class · **MEASURE-AT-OPEN**

**Not born-RED, and this is the wave's most important finding.** The carried Q14 numbers (LCP 5141 ms CI / ~4919 ms local, TBT 5988 ms) are **not reproduced at HEAD**: the pilot reads 148 ms desktop / 588 ms mobile-4×. Those carried numbers predate the v4 cut, the glass-7 adoption and the alias death, exactly as CC-036 anticipated. They are hereby recorded as **superseded, not met** — the product was never re-measured, and re-measuring is the wave's act.

The baseline is `MEASURE-AT-OPEN` rather than RED because the pilot's origin is structurally blind to transfer cost (§ENV): 2,021 KB crossed the wire **uncompressed** with zero latency. The canonical N ≥ 20 run on the pinned class decides the born status, and the wave records it honestly either way.
**Falsifier**: serve the same bundle behind a throttled profile with `Content-Encoding: gzip` and LCP moves; if it does not, the LCP leg was never the Q14 defect and the row says so.

### G5 — the wall-clock park is replaced by the shipped `settled` seam · BORN-RED

```
$ sed -n '213,214p' demo/picker/visual/HeroBlob.vue
const BLOB_IDLE_MS = 2000;
const SLEEPY_POSE_MS = 3300;                   ← a 5.3 s stopwatch, not a quiescence read
$ grep -n "5.0.0 adopt" demo/picker/visual/HeroBlob.vue
209:  quiescence seam (GAP-L5, booked at the 5.0.0 adopt)   ← 5.0.0 was SKIPPED; W44 adopted 7.0.0
$ grep -n "settled" node_modules/@mkbabb/glass-ui/dist/components/blob/Blob.vue.d.ts
63:  settled: Readonly<import("vue").Ref<boolean, boolean>>;
64:  settledFrame: Readonly<...BlobSettledFrame | null...>;
```

`e2e/smoke/fixtures/blob-timing.ts` states the trigger in its own header — *"the park TRIGGER stays wall-clock until P6's `settled` seam ships, BOOKED at T.W4-5"*. **The seam shipped at 7.0.0.** The trigger fired at the W44 adoption and no close observed it; that is DR-03's disease verbatim.
**Asserted property**: the park is driven by the engine's quiescence read; `rg "BLOB_IDLE_MS|SLEEPY_POSE_MS" demo/ e2e/` → 0.
**Falsifier**: stub `settled` to never settle and the four park specs must red. A park that passes with `settled` pinned false is a wall clock wearing a new name.

### G6 — the o5 spike leg tells today's truth · BORN-RED

```
$ grep -n "test.fail()" e2e/smoke/perf/o5-boot-pacing.spec.ts
48:    test.fail();
```

Its header binds the red to *"the ONE eager-payload mount task (the 347.9 KiB gz eager index — RP-2) … the payload cure is W7's (L20 `/blob/config` + GAP-L5 — the Q14 chain). Re-measure at the W7 adopt; flip then."* That W7 is V-prime's, long dead; **this wave is that payload cure**.
**Asserted property**: after the cut, `test.fail()` is deleted (leg green) or the header carries today's measured median / max-ratio and today's diagnosed cause. A stale prophecy naming a dead wave is not an acceptable third state.
**Falsifier**: the spec's own assertion (`maxDelta ≤ 3 × median`) decides it; the seat may not choose the verdict, only report it. X-W1 (CC-031) rules the *disposition* of the three `test.fail()` legs as a class (`o26-aurora-perceptibility:57`, `o16-computed-cascade:34`, `o5-boot-pacing:48`); X-W2 executes the payload cure and supplies the measurement for this one.

### G7 — the zero-reading telemetry artefact is superseded · BORN-RED

```
$ head docs/tranches/V/megatranche/audit/telemetry/PERF.json     # dated 2026-07-24
"lcp": 0, "fcp": 0, "tbt": 0, "resourceCount": 3   ← ×10 rows, every config, every route
```

Those zeros are the honest reading of the MT-F012 empty-mount build; they are also the only telemetry file in the tree, and a reader will take them for a boot measurement.
**Asserted property**: a dated successor (`PERF-X-W2.json`) exists, the original file is **untouched** (F-7 append-never-rewrite), and no document cites the zero rows as a boot measurement.
**Falsifier**: `git diff --stat -- docs/tranches/V/megatranche/audit/telemetry/PERF.json` non-empty fails the gate.

### G8 — `BLOB_HERO` is consumed or tombstoned · BORN-RED

```
$ grep -rn "BLOB_HERO" demo/ src/ | wc -l
0                    ← shipped at 7.0.0 from ./blob-config, zero consumers, three closes
```

**Asserted property**: exactly one of — a hero-path consumption with a measured visual delta, or a one-line tombstone with a rationale in the wave close.
**Falsifier**: a third close that neither consumes nor tombstones. Silence fails.

---

## ENV — where these gates run, and what that environment cannot see (L-12)

- **Origin.** All byte and Core-Web-Vitals numbers are taken against the **built** `dist/gh-pages` served by `e2e/smoke/perf/serve-built.mjs` on `:8091` (the `smoke-perf` project). **No dev-server number may appear anywhere in this wave (MT-F011).** The live dev stack at `:9000` is forbidden as a perf origin.
- **The 38,454 ms cold-boot figure is not a Q14 number.** MT-F011 is context for this wave, never a row: that reading is Vite cold dependency optimisation on a dev server. Conflating it with the built-bundle escalation is a wave-failing error.
- **`serve-built.mjs` is blind to transfer cost.** It is a zero-dependency static server with no `Content-Encoding: gzip` and no latency; the pilot moved 2,021 KB uncompressed in ~0 ms of network time. Therefore **every byte gate is measured from disk** (`gzip` over the emitted files), never from `PerformanceResourceTiming.transferSize`, and G4's baseline is deferred to the pinned class.
- **Renderer.** Headless Chromium is software-GL (SwiftShader). Any frame-timing leg here inherits that; the real-GPU oracle is CC-029 / X-W1, not this wave.
- **Viewport asymmetry, forwarded not cured.** The pilot's mobile leg rendered `#app` innerText of **69 characters** against desktop's **1,767**, with 11 resources against 19. The mobile LCP element is therefore probably not the desktop LCP element — an MT-F028 / CC-051 fact belonging to **X-W5**, recorded here so that `o24-lcp-identity`'s identity leg is re-read after X-W5 lands. X-W2 does not touch it.

---

## CARRIES

| original-ID | source | verb | landing |
|---|---|---|---|
| **CC-035** (DR-03 · GAP-L5) | `DISEASE-REGISTRY.md:131` | **BUILD** — value half | Scope 1–4, 8, 9; gates G1, G5, G8 |
| **CC-036** (DR-06 · Q14 · RP-2 · U-F3 · CH-4) | `DISEASE-REGISTRY.md:161` | **BUILD** | Scope 5–7, 10; gates G2, G3, G4, G6, G7 |
| **MT-F011** | `ROOT-FINDINGS.md:219` | **FOLD as law, not as a row** | the ENV block; no gate may cite a dev-server number |

CC-035's producer half is **discharged on arrival**: glass 7.0.0 already ships `settled`, `settledFrame` and `./blob-config`. The §D letter (Scope 9) records that fact and closes the ask; per the ledger it is **never a gate**.

## BANKS

| banked item | re-trigger, as a runnable condition |
|---|---|
| The owner device matrix (V-prime W55 CH-4: 1,440 loads across Mac17,7 + Pixel 7) | **Optional attestation only, never the gate** (CC-036). Re-trigger = a dated owner session. Its absence never blocks this wave — that unexecutable predicate is precisely how the row survived four closes. |
| The canonical N≥20 run on `ubuntu-24.04` | `gh run list --workflow=ci.yml --limit 1 --json conclusion` returns `success` after X-W1 lands. Until then the wave-open bench pin (G3 pin ii) is admissible. |
| LHCI enforcement of the G3/G4 bars | `grep -rn "lighthouserc" .github/` returns ≥ 1. Today it returns **0** — the config's bars are read by nothing. Wiring it is CC-031 / X-W1; X-W2 only *cites* the bars. |

---

## COMPLETABLE (C-06)

**YES.** This wave lands alone because every surface it needs is already on disk at the installed `@mkbabb/glass-ui@7.0.0`: `./blob-config` (245 B) exports exactly the three symbols the two boot-path sites import, and `Blob` already exposes `settled` / `settledFrame` / `pause` / `resume` / `update:paused`. There is **no Glass-8 trigger** (CC-003's blocked bank is untouched), no API change, no route change, no producer ask on the critical path, and no `src/` byte. Three product files, one fixture, five specs, one script. Both byte gates are executable at the wave's own bench against `dist/gh-pages`; the timing gates need one pinned runner class, and G3's pin (ii) makes that satisfiable without X-W1. `c4af0ef9` (CC-002 — one boot-boundary cut, landed alone, ancestor of HEAD, zero product-path diffs since) is the template and the existence proof.

---

## 7. Format And Lint Cadence

`npm run lint` (`eslint . --max-warnings=0`) and `npm run typecheck` (`vue-tsc` over `tsconfig.lib.json` **and** `tsconfig.demo.json`) after **each** unit, and again before close. `npm test` (vitest) after unit b, because the fixture's constants are imported by spec code. `git diff --check` before every commit. There is no repository formatter script; eslint at `--max-warnings=0` is the named replacement evidence.

## 8. Verification Artefacts

- `docs/tranches/X/evidence/W2/BEFORE.json` — pre-cure eager set + p75 LCP/TBT, N≥20, with the pin string.
- `docs/tranches/X/evidence/W2/AFTER.json` — post-cure, same instrument, same pin string.
- `docs/tranches/X/evidence/W2/DELTA.md` — the before/after pair, one table, plus the realized-vs-predicted 35,461 B gz line.
- `docs/tranches/X/evidence/W2/o5-remeasure.txt` — the `[O-5]` console line (renderer, frames, median, max, ratio, dropped) that decides G6.
- `docs/tranches/V/megatranche/audit/telemetry/PERF-X-W2.json` — the dated successor to the 2026-07-24 zero-reading receipt.
- The dated §D producer note (glass 7.0.0 seam already shipped), filed to the active glass-ui inbox per the standing BH/BI relay.
- Commit hashes for units a–d.

## 9. Commit Plan

| # | scope | body required |
|---|---|---|
| 1 | `perf(x-w2/instrument): eager-module-set gzip probe + N≥20 CWV collection; born-RED baseline` | yes — the born-RED table |
| 2 | `perf(x-w2/boot): drop the glass blob barrel from the eager graph via ./blob-config` | yes — the byte delta |
| 3 | `fix(x-w2/hero): park from the engine's settled seam, retiring the 5.3 s stopwatch` | yes — the fixture contract change |
| 4 | `test(x-w2/o5): re-measure the boot-pacing spike leg after the payload cure` | yes — the verdict and its cause |
| 5 | `docs(x-w2): close — before/after receipts, §D note, PERF successor; verb line to IMPLEMENTED` | yes |

Commits are pathspec-scoped. `scripts/dev/dev.sh` is never staged.

## 10. Dependencies

- **Depends on**: **X-W1** for the canonical pinned-runner run (CC-031's restored jobs, CC-032's boot-smoke, and the LHCI re-wire that gives G3/G4 an enforcing consumer) and for the class-wide ruling on the three `test.fail()` legs. **Not blocking** — G3's pin (ii) and the bench origin make the wave executable today; X-W1 upgrades the receipt's environment, it does not enable the work. **X-W0** for the tracked-evidence rule the `docs/tranches/X/evidence/W2/**` tree obeys (CC-012: untracked evidence is not evidence).
- **Blocks**: **X-W5** — the o24 LCP-identity re-read and the mobile-viewport amputation fact are forwarded from this wave's ENV block. **X-W11** — CC-035 and CC-036 cannot reach VERIFIED in the release table without this wave's receipts.
- **Independent of**: X-W3 (access/API), X-W4 (semantic controls), and the entire §1.M glass-coupled bank. No row here waits on Glass 8.

## 11. Archaeology

This row has been opened and discharged four times without a measurement.

| close | what happened | why it failed |
|---|---|---|
| S.W3 | measured 347.9 KiB gz eager against a ≤280 KiB gate, then **re-baselined the gate** | the bar was editable by the seat that missed it |
| T | ruled the LCP/TBT budgets a HARD gate forbidding re-baseline, then discharged it through the gate's own unbounded `escalate` arm at LCP 5141 / TBT 5988 | an unbounded escape arm is not a gate |
| U | discharged identically as "ESCALATE DELIVERED-as-structural-fact" (~4919 local / 5141 CI) | naming the obligation counted as satisfying it |
| V-prime W55 | re-booked as a 1,440-load matrix across a MacBook Pro Mac17,7 and a physical Pixel 7 | unexecutable by any agent seat; the apotheosis union folded it and it appears **zero** times as gate text across 193 vnext wave rows |

**New guardrails.** (1) The bar is fixed at wave-open and a re-baseline is a §3a triumvirate trigger, not an option available to the implementing seat. (2) There is no `escalate` arm — G2/G3 pass or the wave closes `complete_with_misses` with the measured number. (3) The device matrix is demoted to optional attestation (BANKS) so no gate is unexecutable by one agent at one bench. (4) The carried 5141/4919 numbers are recorded as **superseded by re-measurement**, never as met; G4 says so in the open.

---

## L-18 rider — acceptance is not this file

This wave is **SPECIFIED**. Its implementation is not ACCEPTED until it survives **two quartet challenge passes** — four Opus skeptics per pass, working from the gates above and the born-RED receipts, followed by a **fresh Fable apotheosis** that has not seen the implementing seats' reasoning. The hostiles' first targets are named here so the passes are not ceremonial: the G2 prediction (35,461 B gz) versus the realized delta; whether the eager-graph assertion in G1 was satisfied by an import change or by a narrowed grep; whether the G5 park survives `settled` pinned false; and whether any number anywhere in the receipts came from a dev-server origin (MT-F011). A gate that cannot be made to fail for its intended reason (L-19) is not a gate, and the challenge passes exist to find that out.
