# X.W5 - One route, one scene

## 1. State

**Name**: W5 - One route, one scene
**Opens after**: X-W4 (the typed `SceneActionSet` contract, CC-043) · **X-W2** (reciprocal to W2's `Blocks: X-W5`, `W2.md:284`) — and, for gate D2's baseline only, X-W0's track-or-archive act (CC-012)
**Agents**: 5 serial (single worktree; §4a forces serial — every pair of units shares at least one `modify` path)
**Hard gate**: exactly one visible H1 and one protagonist per public route; the physical left/right pane axis absent from `viewSchema.ts`; the shell caps neither block axis; every pane and sub-pane swap carries a defined, PRM-guarded motion inside a measured frame budget
**Status**: planned

### Four-verb status (separate fields — never conflated)

| verb | state | authority |
|---|---|---|
| AUDITED | **YES** | `registry/adjudicated/App.md` (MT-APP-1, 39 rows) · `registry/adjudicated/layout-gestalt.md` §9 (V·L1/V·L2) · `registry/ROOT-FINDINGS.md` MT-F003/F028/F034 · `registry/DISEASE-REGISTRY.md` DR-27 · `IN-FLIGHT-AUDIT-2026-07-29.md` §6 (P-1) |
| SPECIFIED | **YES — by this file, and nothing more** | `CARRY-CUT-LEDGER.md` §1.G + §2 row X-W5 |
| IMPLEMENTED | NO | — |
| VERIFIED | NO | — |

Ledger rows: **BUILD** CC-048, CC-049, CC-050, CC-051, CC-052, CC-054, CC-055 · **FOLD** CC-004, CC-053.

## 2. Goal criterion

This wave succeeds if every public route is one semantic scene the shell can state out loud —
one owned mount path, one visible H1, one protagonist, no physical left/right identity, no
viewport-aspect cap on the block axis — and if moving between scenes is an animated, budgeted,
reduced-motion-honest event rather than an instant cut.

## 3. Scope

Numbered by the unit that owns it; ordering is binding (MT-APP-1's file collision, rider A-6).

1. **Shell truth (MT-APP-1 residual)** — finish the composition root `main.ts` (router `isReady()`
   await, the four CSS imports in cascade order, dark-mode + API-client provide at app level);
   `bindPane` on all three `PaneSlot` call sites with `onMount` required and generic; one visible
   route H1 inside `<main>`; one shell-owned polite `role="status"` region; boot URL rewrite
   becomes a no-op; `ground.ts` split by lifetime; `useOverture` b3 state-checked arm + b4 terminal
   `unavailable` arm.
2. **The block law (V·L1)** — delete `height:100dvh` + `overflow:hidden` from `.app-layout`; delete
   `--content-max-h` and both `@media` aspect arms that set it; `--app-gutter` replaces
   `--app-padding-x`; `min-block-size:100svh`.
3. **One mount, one column (V·L2)** — one mount path; ordered `regions[]` replaces
   `left`/`right`/`defaultPaneIndex` in `viewSchema.ts`; delete `PaneSegmentedControl.vue` and the
   Dock Picker|Blob toggle; delete every JS breakpoint layout fork; re-seat the T-45 blur carrier on
   the region wrapper in the same commit.
4. **Scene motion (MT-F034 + DR-27)** — re-key the pane transition family off the dying
   `--left`/`--right` class names onto region roles; wrap every segmented-control content swap in a
   named `<Transition>`; wire a frame-budget oracle around the scene swap.
5. **The Browse/Library posture strike (P-1)** — replace the fabricated `64%…66.6666667%` /
   `33.3333333%…36%` tolerance in the normative canon with one exact posture, cited to the named
   producer contract (P-2 citation discipline; no restatement as independent truth).

Not in scope, named so nothing is lost:

- **CC-051's inline-axis half** (ratio, ultrawide width cap, the 62 Tailwind responsive prefixes,
  the 4 layout tokens) rides **CC-056 / V·L3 at X-W6** per the band's own CARRIES lines. This wave
  owns MT-F028's block-axis half and records the adaptation-mechanism census as its baseline.
- **The dock-layer half of CC-055** rides **CC-080 / MT-DOCK-LAYERS-1 at X-W8** (the
  `useLayerTransition` shim's clean-break retirement).
- Anything glass-owned. CC-108 makes the InstrumentChassis a fact, not a prohibition: no chassis
  arms exist at 8.0.0 and nothing chassis-shaped is rebuilt here. The compositions in this wave are
  bespoke by construction.
- Struck and uncitable in any close of this wave: App L-9, D-15, C-4's "no h2", D-4's skip-link
  clause, D-6's nondeterminism clause, D-9's stamp-deletion cure.

## 3a. Triumvirate Dispatch

Mandatory triumvirate (research + plan augment + redress); the orchestrator may not redispatch the
failing unit alone.

- **File-bounds expansion that invalidates the wave**: any write outside §4 — in particular
  `src/`, `api/`, `demo/workbenches/**` interiors beyond the two named motion sites, or any
  `node_modules/@mkbabb/glass-ui/**` path. A cure that needs a glass byte is a letter, not an edit.
- **Hard-gate failures that are not local-edit-recoverable**: A1 (`router.isReady()` await
  reintroduces a boot stall on any of the fourteen routes); C1 (text-parity ratio still < 0.9 after
  the fork dies — means the amputation was never the breakpoint); C5 (WebGL context identity lost
  despite one mount path); D1 (frame budget unmet after the transition re-key — the jank root is
  not the swap).
- **Diagnostic loops whose third iteration halts**: the b3/b4 overture arm (three failed
  settle-arm shapes); the T-45 carrier re-seat (three failed carrier positions); the frame-budget
  oracle's own flake (three runs disagreeing beyond the stated tolerance).

## 4. File Bounds

| File | Access |
|---|---|
| `demo/color-picker/main.ts` | modify |
| `demo/color-picker/index.html` | modify-carve |
| `demo/color-picker/App.vue` | modify-carve |
| `demo/color-picker/composables/boot/useOverture.ts` | modify |
| `demo/color-picker/composables/boot/ground.ts` | modify-carve |
| `demo/color-picker/composables/boot/hydrate.ts` | modify |
| `plugins/vite-ground-tokens.ts` | create (root `plugins/` — the home already has two siblings) |
| `vite.config.ts` | modify-carve |
| `demo/color-session/useColorUrl.ts` | modify-carve |
| `demo/shell/PaneSlot.vue` | modify |
| `demo/shell/usePaneRouter.ts` | modify |
| `demo/shell/useViewManager.ts` | modify |
| `demo/shell/viewSchema.ts` | modify |
| `demo/shell/PaneSegmentedControl.vue` | delete |
| `demo/shell/dock/Dock.vue` | modify-carve |
| `demo/styles/shell.css` | modify-carve |
| `demo/styles/foundation.css` | modify-carve |
| `demo/styles/animations.css` | modify-carve |
| `demo/workbenches/mix/MixSourceSelector.vue` | modify-carve |
| `docs/tranches/V/megatranche/audit/probes/layout-utilization.mjs` | modify (B3's probe, extended per A-5 by unit b) |
| `docs/tranches/V/megatranche/audit/probes/app-wave/app-shell-truth-probe.mjs` | execute, no write (A2's and A3's probe, re-run unmodified — bounded because the gates invoke it) |
| `docs/tranches/V/megatranche/workflows/gates/scene-swap-budget.mjs` | create (unit d; D1 invokes it) |
| `docs/tranches/V/megatranche/workflows/gates/*.mjs` | create (unit a's A5/A6/A7 route probes) |
| `docs/tranches/V/VISUAL-CONSTITUTION.md` | modify-carve |
| `docs/tranches/V/EVIDENCE.md` | modify-carve |
| `docs/tranches/V/OPTICAL-BENCH-COMPOSITIONS.md` | modify-carve |
| `docs/tranches/V/research/proportion-register.md` | modify-carve |
| `e2e/smoke/page-load.spec.ts` | modify-carve (unit a — the H1 / status-region / announced-404 arm) |
| `e2e/smoke/walk.spec.ts` | modify-carve (unit a — the fourteen-route H1 walk) |
| `e2e/smoke/dual-pane-1440.spec.ts` | modify (unit c — the left/right pane identity it asserts dies with the fork) |
| `e2e/smoke/mobile/walk.spec.ts` | modify-carve (unit c — the amputation assertions become presence assertions) |
| `e2e/smoke/mobile/page-load-mobile.spec.ts` | modify-carve (unit c — `isMobile` fork references) |
| `e2e/smoke/perf/view-switch-frame-budget.spec.ts` | modify-carve (unit d — the region-keyed swap replaces the pane-swap spring) |

Do NOT touch: `src/`, `api/`, `test/`, `scripts/dev/dev.sh`, `node_modules/`,
`demo/ui/**` (shadcn residue — X-W8's subtraction), `demo/styles/overture.css` (L-9 struck),
`demo/shell/dock/layers/ActionBarLayer.vue` (CC-080 / X-W8), the `registry/adjudicated/**` and
`registry/DEFECT-LEDGER.md` evidence records (they quote the defect; rewriting them destroys the
witness), `docs/tranches/V/archive/**`.

## 4a. Disjointness

**No parallelism in this wave.** Units share `modify` paths pairwise — a∩c on `App.vue`,
`usePaneRouter.ts`, `useViewManager.ts`; a∩d on `PaneSlot.vue`; b∩c on `shell.css`. Per
WAVE_SPEC §4a they are therefore **sequenced across sub-waves**, not folded and not parallelised.
Each unit commits before the next opens. No other X wave writes any path above while W5 is open;
X-W6 inherits `viewSchema.ts` and `shell.css` only after W5 closes.

**Cross-wave.** X-W2 declares `Blocks: X-W5` (`W2.md:284`); W5 reciprocates in §1 Opens-after and
§10 Depends-on, so W2's boot-path bytes settle before this wave writes any `e2e/**` path. The six
spec paths above are file-disjoint from W2's seven (`e2e/smoke/fixtures/blob-timing.ts`,
`webgl-blob-idle.spec.ts`, `oracles/o12-blob-seat.spec.ts`, `mobile/blob-presence-mobile.spec.ts`,
`perf/idle-frame-budget.spec.ts`, `perf/eager-payload.spec.ts`, `perf/o5-boot-pacing.spec.ts`) —
`e2e/smoke/mobile/` and `e2e/smoke/perf/` are shared as *directories* only, never as files. The
narrowing is the point: the earlier `e2e/**` glob made W2 ∥ W5 a hidden write conflict.

## 4b. Worktree Plan

One writer at a time on clean `main`; no sibling worktrees, no `CARGO_TARGET_DIR` (this is not a
Cargo tree). The orchestrator commits each unit before dispatching the next; a dirty tree at
handoff halts the wave.

## 5. Agent Units

Model law **M-23**: every unit below is an **Opus implementation seat**. No design content is
authored here — the canon strike in X.W5.e applies an already-ruled disposition (P-1, ADOPT) and
authors no new design law; new design law is X-W10's twice-authored Fable∥Opus → fresh-Fable lane.

### X.W5.a Shell truth — the composition root, route-true boot, slot registration

- **Goal**: the shell declares what it owns — one module entry, one route-true mount, one
  registration path, one voice.
- **Mechanism**: `main.ts` gains `await router.isReady()` before `mount`, the four CSS imports
  lifted from `App.vue` in cascade order (utils → foundation → focus-ring → overture), and the
  `useGlobalDark()` + `provideApiClient()` installs. `PaneSlot.onMount` becomes required and
  generic over the mounted instance type, invoked `(instance, liveKey)`; `usePaneRouter` gains
  `bindPane(slot)` and owns the instance refs it dispatches onto. `App.vue` gains one **visible**
  `<h1 tabindex="-1">` inside `<main>` bound to `VIEW_MAP[currentView].label`, one polite
  `role="status"` region written by the route-settlement commit, and role-named pane-wrapper
  classes with role-keyed stagger. `useColorUrl`'s boot URL rewrite becomes a no-op;
  `ground.ts`'s node half moves to `demo/plugins/vite-ground-tokens.ts`.
- **Files**: `main.ts`, `index.html`, `App.vue`, `PaneSlot.vue`, `usePaneRouter.ts`,
  `useViewManager.ts`, `useOverture.ts`, `useColorUrl.ts`, `hydrate.ts`, `ground.ts`,
  `plugins/vite-ground-tokens.ts`, `vite.config.ts` (`:15`), `workflows/gates/*.mjs`,
  `e2e/smoke/page-load.spec.ts` + `e2e/smoke/walk.spec.ts` (the route arm).
- **Sub-gate**: gates A1–A7 all GREEN with the committed BORN-RED JSON beside the matching GREEN
  JSON.

### X.W5.b The block law — the shell stops capping the block axis

- **Goal**: mobile gains a scrolling document and ultrawide gains its block extent back, without
  touching the inline axis.
- **Mechanism**: `.app-layout` → `min-block-size: 100svh`, no `overflow`; `--content-max-h` and
  both `@media` arms that set it deleted (`foundation.css:471`'s width+aspect arm keeps its
  `--dock-*` rows; `foundation.css:479`'s bare `21/9` arm dies whole); `--app-gutter` born,
  `--app-padding-x` dies. `layout-utilization.mjs` extended per A-5 (assert mode; `docScrollable`,
  `.pane-container` box + `gridCols`; routes += `#/atmosphere`, `#/palettes`, `#/admin/users`;
  arms += 320×568, 720×450@2).
- **Files**: `shell.css`, `foundation.css`, `layout-utilization.mjs`.
- **Sub-gate**: B1–B4 GREEN; B5 and B6 carry a written decision with rationale in `shell.css`.

### X.W5.c One mount, one column — the breakpoint fork dies

- **Goal**: every region of every route present in one scrolling column at 390, with no subtree
  that a breakpoint can remount out of existence.
- **Mechanism**: `App.vue` collapses to one mount path; `viewSchema.ts` replaces
  `left`/`right`/`defaultPaneIndex` with an ordered `regions[]` of semantic roles
  (stage / inspector / action); `usePaneRouter` + `useViewManager` re-typed against it;
  `PaneSegmentedControl.vue` deleted with the Dock Picker|Blob toggle; every
  `useBreakpoint`/`isDesktop`/`isMobile`/`mobilePaneIndex` layout fork deleted; the T-45 blur
  carrier re-seated on the region wrapper that replaces `.pane-wrapper` **in the same commit** as
  the wrapper cut.
- **Files**: `App.vue`, `viewSchema.ts`, `usePaneRouter.ts`, `useViewManager.ts`,
  `PaneSegmentedControl.vue` (delete), `Dock.vue`, `shell.css`,
  `e2e/smoke/dual-pane-1440.spec.ts`, `e2e/smoke/mobile/walk.spec.ts`,
  `e2e/smoke/mobile/page-load-mobile.spec.ts`.
- **Sub-gate**: C1–C8 GREEN, with C6 proving the carrier survived the wrapper's death.

### X.W5.d Scene motion — defined transitions on a measured budget

- **Goal**: pane and sub-pane swaps are animated, budgeted events; no swap is an instant cut and
  no swap blows the frame budget.
- **Mechanism**: the `vj-enter` pane family re-keys from `.pane-wrapper--left`/`--right`
  (18 physical-name sites: `animations.css` 12, `shell.css` 3, `App.vue` 3) onto region-role
  selectors; the segmented-control content swap in `MixSourceSelector.vue` gains a named
  `<Transition>` with a direction token; a frame-budget oracle lands under
  `workflows/gates/` and runs the four measured hops. Motion declarations are added, never
  deleted (standing preserve-animations edict).
- **Files**: `animations.css`, `shell.css`, `App.vue` (class names only — sequenced after
  X.W5.c commits), `MixSourceSelector.vue`, `PaneSlot.vue`, `workflows/gates/scene-swap-budget.mjs`,
  `e2e/smoke/perf/view-switch-frame-budget.spec.ts`.
- **Sub-gate**: D1–D4 GREEN; D5 holds under a forced-PRM run.

### X.W5.e The Browse/Library posture strike — P-1 applied, never re-queued

- **Goal**: the canon states a posture a mechanism can emit.
- **Mechanism**: in the four normative documents, replace the `64%…66.6666667%` /
  `33.3333333%…36%` tolerance with one exact posture — the preview-dominant pair
  `66.6666667% / 33.3333333%` when an inspector is selected, 100% protagonist when none is —
  each occurrence citing the named producer contract (`CONSTELLATION.md` P122) as its source, never
  restating the constant as independently derived. The audit/adjudication records that quote the
  defect are read-only.
- **Files**: `VISUAL-CONSTITUTION.md`, `EVIDENCE.md`, `OPTICAL-BENCH-COMPOSITIONS.md`,
  `research/proportion-register.md`.
- **Sub-gate**: E1 and E2 GREEN.

## 6. Hard Gate

28 gates. **25 are born-RED today**; 3 are decision-record / non-regression gates whose falsifier is
stated with them. Every RED baseline below is either measured read-only on **2026-08-03** at HEAD
(marked *measured*) or carried from the named triple-witnessed record with a **MEASURE-AT-OPEN**
re-confirmation duty (marked so because `c4af0ef9` changed the production entry after the record
was taken — the number is not re-asserted without re-running the probe).

### Unit a — shell truth (7 gates, 7 RED)

| # | gate | command / probe | RED today | falsifier (how it fails for its intended reason) |
|---|---|---|---|---|
| **A1** | composition root complete | `node -e "const s=require('fs').readFileSync('demo/color-picker/main.ts','utf8');process.exit(/await router\.isReady\(\)/.test(s)&&(s.match(/^import \"/gm)||[]).length===4?0:1)"` **and** `grep -cE 'useGlobalDark\|provideApiClient\|^import "\.\./styles' demo/color-picker/App.vue` == 0 | **RED, measured**: `main.ts` is 168 B — `createApp`/`use`/`mount` only, no `isReady()` await, no CSS imports; `App.vue:189,190,199,200,206,210,214,219` still own all four CSS imports + both installs | Fails if the entry mounts before the route is true, or if a CSS import is dropped/reordered (cascade break is visible in the π capture pair) |
| **A2** | deeplink-overture | the committed `audit/probes/app-wave` probe: `deeplinkGenerate.marks ⊇ {b3,b4}` AND blob present across `/#/generate,/#/browse,/#/gradient,/#/extract,/#/atmosphere` | **RED of record** `[b0,b1,b2]`, blob absent, terminal (triple-measured 2026-07-27) — **MEASURE-AT-OPEN** | Fails if any deep-linked route still terminates its boot at b2; a single route regressing turns it red |
| **A3** | dock-action-parity | the probe's mobile/desktop Regenerate pair asserting parity over the nine handlers (palette-library arms excluded — API-less is environment, not defect) | **RED of record** `{mobileChanged:false, desktopChanged:true}` — **MEASURE-AT-OPEN**. Structural cause re-measured today: 38 breakpoint references across 13 files still fork the slot plumbing | Fails if the mobile control is inert **or** if the desktop control breaks — parity is two-sided by design |
| **A4** | typed-registration bite | delete ONE `bindPane` argument, then `npx vue-tsc -p tsconfig.demo.json --noEmit` must exit non-zero | **RED, measured**: `grep -rn 'bindPane' demo/` → **0 hits**; the checker is green over an inert action bar, so the gate cannot bite | Fails if the checker stays green with a registration deleted — i.e. the gate is decorative. This is the gate's own falsifier, run once |
| **A5** | route H1 | iterate all fourteen `ViewId`s: `h1===1 && main===1 && h1.text===VIEW_MAP[view].label`, H1 visible | **RED, measured**: `grep -rn '<h1' demo/ --include='*.vue'` → **0 hits**; `h1: 0` on all 60 audit captures (MT-F003) | Fails at 0, at ≥2, on an invisible H1, or on a label that disagrees with `VIEW_MAP` — four distinct ways |
| **A6** | boot-URL silence | fresh context, bare `/#/` and `/#/generate`: `href` contains no `?space=` | **RED of record**: settles at `?space=lab&color=lab(92%25+88.8+20+/+82.7%25)` (triple-measured) — **MEASURE-AT-OPEN** | Fails if any boot writes state no user action produced; a live user edit must still write the URL, so a gate that also kills live sync is over-cured |
| **A7** | announced redirect | `/#/does-not-exist` → the shell status region carries the redirect reason, title/H1 distinct from `/#/`; `grep -rn 'role="status"' demo/shell/ demo/color-picker/` ≥ 1 | **RED, measured**: **0** `role="status"` hits; the 404 render is byte-identical to `/#/` | Fails on a silent substitution, or on a status region that exists but announces nothing (the render-equality arm catches the second) |

### Unit b — the block law (6 gates, 4 RED + 2 decision-record)

| # | gate | command / probe | RED today | falsifier |
|---|---|---|---|---|
| **B1** | mobile document scrolls | at 390×844, on every member route whose content exceeds the viewport: `documentElement.scrollHeight > clientHeight` (App D-8's gate verbatim) | **RED, measured (static root)**: `shell.css:23 height:100dvh` + `:26 overflow:hidden` → `docScrollable=0` on ALL routes | Fails if any content-exceeding route still cannot scroll; a route whose content genuinely fits is excluded by the predicate, not by exception |
| **B2** | no block cap | `getComputedStyle(scene band).maxHeight === 'none'` at 3440×1440 | **RED of record**: `608px` (`--content-max-h`, the 21/9 arm) — root re-measured today at `foundation.css:428,475,481` + `shell.css:75` | Fails if any cap survives under any aspect ratio — re-run at 16/9 and 21/9 both |
| **B3** | block extent | scene-band occupied block extent ≥ 90% at 3440×1440 across the 16 non-measure-bound routes | **RED of record**: **45.4%** (`layout-utilization.mjs`) — **MEASURE-AT-OPEN** | Fails if the surplus is absorbed by a stretched rack instead of the scene — the per-route breakdown, not the mean, is the reading |
| **B4** | the token is unrepresentable | `grep -c '100dvh' demo/styles/shell.css` == 0 AND `grep -rl 'svh' demo/` ≥ 1 AND `grep -rc 'content-max-h' demo/` == 0 | **RED, measured**: `100dvh` **1**, `svh` **0** files, `content-max-h` **8** sites across 3 files | Fails if the token is deleted from its definition but left live at a consumer (`shell.css:75`, `PaneSlot.vue:23`) — the consumer arm is why the grep is repo-wide |
| **B5** | logical-axis support recorded | `node -e "…CSS.supports('overflow-block','auto')…"` output pasted into the wave record; if false, physical spellings ship and the logical form is BANKED with a re-trigger | **not RED — decision-record** | Fails if the wave closes with no recorded output, or ships the logical spelling without the support reading. Absence is the failure |
| **B6** | dock posture recorded | U-7 decided by probe at 390 with a scrolled document; sticky-vs-in-flow written into `shell.css` with rationale | **not RED — decision-record** | Fails if the posture changes with no rationale comment at the site — a silent choice is the defect this gate exists for |

### Unit c — one mount, one column (8 gates, 8 RED)

| # | gate | command / probe | RED today | falsifier |
|---|---|---|---|---|
| **C1** | content parity | per-route rendered text at 390 ≥ 0.9 × the same route at 1440 (ratio form — API-less safe, U-6) | **RED of record**: `#/` **69 vs 893 = 7.7%**; `#/blob` 68 vs 748 — **MEASURE-AT-OPEN** | Fails if any route still amputates a region at 390. A ratio ≥ 0.9 achieved by *shrinking desktop* is caught by B3's extent arm |
| **C2** | mobile liveness | `/#/generate` Regenerate at 390 → `specimenChanged: true` | **RED of record**: `false` — **MEASURE-AT-OPEN**. (The cure is unit a's; this gate proves the *class* dissolved with the fork) | Fails if the control is present but inert — presence is not the assertion, state change is |
| **C3** | the fork is gone | `grep -rEo 'useBreakpoint\|isDesktop\|isMobile\|mobilePaneIndex' demo/ --include='*.vue' --include='*.ts'` == 0 at layout sites | **RED, measured**: **38 occurrences across 13 files** | Fails if a fork is renamed rather than deleted; the gate is source-scoped (L-9) and `node_modules` excluded, so a producer's own breakpoint use cannot false-red it |
| **C4** | the pane selector is gone | `test ! -e demo/shell/PaneSegmentedControl.vue` | **RED, measured**: file exists (1,750 B); imported at `Dock.vue:15,198` | Fails if the file is deleted but its consumer left dangling — `npm run typecheck` is the paired arm |
| **C5** | state survives the old breakpoint | cross the 719px container by live resize at `#/blob`: the WebGL canvas element identity is preserved (no remount) | **RED of record**: the `App.vue:77/94` v-if fork remounts, destroying 3 KeepAlive caches + 2 WebGL contexts — **MEASURE-AT-OPEN** | Fails on element-identity change, which is what a remount *is*; a gate on "canvas still renders" would pass a fresh context and is explicitly not the assertion |
| **C6** | the T-45 carrier survived | pane-card computed `backdrop-filter === 'none'` AND a carrier `::before` with `clip-path` exists on every card route | **RED input**: the wrapper cut without the re-seat returns per-card filters and the t33 rim | Fails if cards regain their own filter (the rim is visible in the π pair) — the gate is why the re-seat must ride the same commit |
| **C7** | viewport queries subtract | source-scoped viewport-dimension `@media` count: 7 → 5 after unit b → **3** after unit c | **RED, measured**: **7** today — 6 width-based (`ConsoleRail:323`, `PaneSegmentedControl:46`, `DockStatusLamp:70`, `animations.css:17`, `shell.css:129`, `foundation.css:471`) + 1 aspect-only (`foundation.css:479`) | Fails if a query is moved rather than retired. The 27 capability queries (`prefers-*`, `forced-colors`, `print`, `pointer`, `hover`) MUST survive — a gate that counts all 34 is wrong and would reward deleting a11y queries |
| **C8** | the physical axis is unrepresentable | `node -e "const s=require('fs').readFileSync('demo/shell/viewSchema.ts','utf8');process.exit((s.match(/right:\s*\"/g)\|\|[]).length===0?0:1)"` (App.md's D-2 bank, promoted to a gate) | **RED, measured**: **13** `right:"` rows | Fails while any route still encodes a physical side. Renaming `right` to `secondary` without collapsing to ordered roles is caught by C3's fork gate and by typecheck on `regions[]` |

### Unit d — scene motion (5 gates, 4 RED + 1 non-regression)

| # | gate | command / probe | RED today | falsifier |
|---|---|---|---|---|
| **D1** | scene-swap frame budget | `node docs/tranches/V/megatranche/workflows/gates/scene-swap-budget.mjs` — for each of the four measured hops: `over32 / frames ≤ 0.15` AND `median ≤ 20ms` | **RED, measured baseline** (`docs/tranches/T/audit/pi/u-gestalt/probe2-log.txt`): `→/gradient {frames:14, max:154, median:59, over32:10}` = **71%** · `→/mix {19,111,48,12}` = 63% · `→/extract {43,89,10,11}` = 26% · `→/generate {35,124,9,8}` = 23% | Fails if any single hop breaches. A budget met by *deleting* the animation is caught by D3/D4, which require the motion to exist — the two gates are adversarial by construction |
| **D2** | the baseline is in git | `git ls-files --error-unmatch docs/tranches/T/audit/pi/u-gestalt/probe2-log.txt` | **RED, measured**: untracked (`?? docs/tranches/T/audit/pi/u-gestalt/`) | Fails while the numbers D1 cites live only on this disk. L-7: an uncommitted witness does not exist. Discharged by X-W0's CC-012 act; re-checked here because D1 depends on it |
| **D3** | pane motion re-keyed, not lost | `grep -rc 'pane-wrapper--left\|pane-wrapper--right' demo/` == 0 AND every region role in `regions[]` has a matching enter/leave rule in `animations.css` | **RED, measured**: **18 physical-name sites** — `animations.css` 12, `shell.css` 3, `App.vue` 3 | Fails at either end: physical names surviving, or a region role with no motion rule. The second arm is what stops "delete the selectors" from passing |
| **D4** | sub-pane swaps animate | every segmented-control content swap is inside a named `<Transition>`; census over the 3 `SegmentedTabs` consumer files (7 occurrences) | **RED, measured**: `MixSourceSelector.vue` has **0** `<Transition ` elements while its `mode` swap at `:114` is a bare `v-if` — the exact surface of the OM-8 owner mandate | Fails on any bare `v-if` content swap behind a segmented control. A `TransitionGroup` on the *list inside* a branch (`:120`) does not satisfy it — the branch swap itself is the assertion |
| **D5** | PRM honesty | under forced `prefers-reduced-motion: reduce`, every transition added by this wave computes a ≈0 duration; every added declaration sits inside `@media (prefers-reduced-motion: no-preference)` or under the global guard at `animations.css:184-193` | **not RED — non-regression**; the global guard exists today and must keep covering the new rules | Fails if a new scroll-driven or `animation-timeline` declaration slips the guard (MT-F023's class — the guard cannot neutralise those). That specific escape is X-W10's CC-099; this gate stops W5 from minting a new instance of it |

### Unit e — the P-1 posture strike (2 gates, 2 RED)

| # | gate | command / probe | RED today | falsifier |
|---|---|---|---|---|
| **E1** | the range is gone from normative canon | `grep -rcE '64%?…66\.6666667\|33\.3333333…36' docs/tranches/V/VISUAL-CONSTITUTION.md docs/tranches/V/EVIDENCE.md docs/tranches/V/OPTICAL-BENCH-COMPOSITIONS.md docs/tranches/V/research/proportion-register.md` == 0 | **RED, measured**: **7 occurrences across 4 files** — `VISUAL-CONSTITUTION.md:44,45` · `EVIDENCE.md:43,47` · `OPTICAL-BENCH-COMPOSITIONS.md:39,40` · `proportion-register.md:35` | Fails while any normative sentence asserts a tolerance no mechanism emits. Scoped to the four normative docs: the adjudication and defect records that *quote* the range are evidence and must NOT change — a repo-wide gate would reward destroying the witness |
| **E2** | the replacement cites, never restates | each replaced sentence names the producer contract (`CONSTELLATION.md` P122) as the source of `66.6666667% / 33.3333333%`; zero new occurrences of a bare constant presented as independently derived | **RED, measured**: the current text presents the numbers with no source attribution (P-2's class) | Fails if the range is swapped for a bare exact number with no citation — that trades one fabrication for another. Pairs with CC-102 at X-W10, which owns the general citation law |

## 7. Format And Lint Cadence

Run after each unit's integration batch and before the wave closes:

- `npm run typecheck` (`vue-tsc`, strict) — the paired arm for C4 and C8;
- `npm run lint`;
- `npm run test` (vitest) — the library suite must not move; a delta here means the wave escaped its bounds;
- `git diff --check` for the docs-only unit e.

No formatter is intentionally skipped. `npm run gh-pages` is **not** this wave's gate — the build
and deploy arms are X-W1's (CC-032, CC-034); running it here produces no evidence W5 may cite.

## 8. Verification Artefacts

Saved at close, all committed (`git add -f` past `.gitignore:34` for PNGs; L-7 — an uncommitted
witness does not exist):

- `docs/tranches/X/waves/W5/born-red/` — the BORN-RED JSON for A2, A3, A6, B3, C1, C2, C5 taken at
  wave-open, plus the static measurements in this file re-run and dated;
- `docs/tranches/X/waves/W5/green/` — the matching GREEN JSON per gate;
- `audit/visual/layout/` — {390×844, 1440×900, 3440×1440} × {`#/`, `#/blob`, `#/browse`} for unit b;
  {390×844, 720×450@2, 1440×900} × {`#/`, `#/generate`, `#/blob`} for unit c; selector
  `.app-layout, main`;
- `audit/probes/app-wave/pi/` — the desktop-1440 / mobile-390 capture pair on `/#/generate` plus the
  H1 accessibility-tree dump pair;
- `workflows/gates/scene-swap-budget.mjs` output for the four hops, before and after;
- the D-E portal-integrity delta: body-direct-children positioning census + `[data-paper-field]`
  ancestry of a portalled dialog, identical before/after (mandatory on any `index.html` body change);
- the B5 `CSS.supports` reading and the B6 dock-posture rationale, quoted in the close report.

## 9. Commit Plan

Pathspec commits only; the unowned dirty row `scripts/dev/dev.sh` is never staged.

| # | scope | contents | body required |
|---|---|---|---|
| 1 | `feat(demo/shell): composition root + route-true boot` | unit a, with the born-RED JSON entering git in this same commit | yes |
| 2 | `feat(demo/styles): the block law` | unit b, `--content-max-h` deletion + probe extension | yes (deletion) |
| 3 | `feat(demo/shell): one mount, ordered regions` | unit c, including `PaneSegmentedControl.vue` deletion and the T-45 re-seat in the same commit | yes (deletion + broad) |
| 4 | `feat(demo/motion): region-keyed scene transitions + swap budget` | unit d, gate script included | yes (gate) |
| 5 | `docs(canon): P-1 — exact Browse/Library posture` | unit e | yes |
| 6 | `docs(X.W5): close report + verification artefacts` | π captures, GREEN JSON, status flip to IMPLEMENTED (VERIFIED is stamped only at X-W11's release close) | yes (status) |

## 10. Dependencies

- **Depends on**: **X-W4** (CC-043's typed, owner-tokened `SceneActionSet` — units a and c re-type
  the dispatch seam and cannot land against `ref<any>`, measured live at `App.vue:317-319`);
  **X-W0** (CC-012's track-or-archive, for gate D2's baseline only — unit d is the sole unit
  affected); **X-W2** (reciprocal to W2's `Blocks: X-W5`, `W2.md:284` — the o24 LCP-identity re-read
  and the mobile-viewport amputation fact are forwarded from W2's ENV block as inputs to C1's parity
  ratio and C3's fork census, and W2's boot-path bytes settle before this wave writes `e2e/**`).
  No dependency on glass-ui, the API, X-W1's gates, or any §1.M bank row.
- **Blocks**: **X-W6** (CC-056 / V·L3 needs `regions[]` and the dead block cap; CC-057 / V·L4 needs
  one mount path), **X-W7** (palette scenes land into W5's region contract), **X-W8** (subtraction
  needs stable destinations), **X-W10** (canon re-authoring inherits the E1/E2 strike).

### COMPLETABLE — **YES**

If X.W5 were the only wave ever executed after X-W4: every public route gains a visible H1 and an
announced 404; mobile gains a scrolling document; ultrawide gains its block extent back; every
region of every route becomes present at 390 instead of amputated by a breakpoint; the physical
left/right identity becomes unrepresentable in the route schema; and pane and sub-pane swaps become
defined animated events on a measured budget. It needs no producer byte, no API change, no build or
deploy repair, and no §1.M glass trigger. **Scenes land route-by-route — Gradient is the pilot** —
and each route is itself a completable slice: a route converted to `regions[]` with its H1 and its
transition is strictly better than the fork it replaced, whether or not the next route follows.
`c4af0ef9` (CC-002 landing V·MT1 alone) is the existence proof and the template.

## 11. Archaeology

- **Prior attempt**: V-prime booked this work at `W46`/`W49-W52` and it never executed; DR-27's
  measurement has existed since T's close window (`T-48 → T-58 → U-F7 → CH-6 → W47`) and entered no
  disposition ledger in T, U, or V. **New guardrail**: gate D2 refuses to let D1 cite a number that
  is not in git, and the CARRY-CUT ledger row (CC-054) is the first ledger entry the measurement has
  ever had.
- **Prior attempt**: worker-F's `LG-1` proposed this whole transposition as one cut. Refused by the
  same-day App adjudication (RD-3): an eleven-member transposition is an arc against a repository
  with a measured 38% landing rate. **New guardrail**: five sequenced units, each with its own
  COMPLETABLE reading, ordered by value-delivered-alone.
- **Alias note**: MT-APP-1 supersedes worker-F's `MT-APP-1` and worker-O's `MT-W-APP-1`; V·L1/V·L2
  supersede nothing but are re-homed under X naming by `CARRY-CUT-LEDGER.md` §0.1 — no `V·MT`/`V·C`
  label may gate work except through its ledger row.
- **Partially landed**: `c4af0ef9` created `demo/color-picker/main.ts` and moved the production
  entry, discharging CC-002 and App G1/G2. It did **not** land the `isReady()` await, the CSS
  import lift, or the provide installs — hence gate A1, and hence A2/A6 carry a MEASURE-AT-OPEN
  duty rather than a re-asserted number.

## 12. L-18 rider — the challenge obligation (reference, not ceremony)

This wave's **implementation** is not ACCEPTED on its gates alone. After the gates are green, the
landed work must survive **two quartet challenge passes** — 4 Opus skeptics per pass, then a fresh
Fable apotheosis over their findings — before the wave may be marked ACCEPTED. The gates prove the
wave did what it said; the quartet asks whether what it said was worth doing and whether the
evidence bears weight. A gate that the challenge shows to be unfalsifiable is struck and re-authored,
and the wave returns to IMPLEMENTED. This rider is referenced here so no close can claim it was
unforeseen; the pass itself is run under `FORMATION-LAWS` L-18, not by this file.

---

## ADDENDUM 2026-09-22 (dated, beside — E-3; COHESION §0aq) — THE §3a TRIUMVIRATE, SAT
Units, serial: **`X.W5.t`** (Fable; research + plan augment — D1 under the ruled instrument conditions with the out-in re-probe committed first under fold `:41`'s locks; the swap-travel and Mix-scene roots at `file:line`; A2's blob arm re-authored falsifiably; C1 re-metric recorded) → **`X.W5.d2`** (Opus; redress — the plan's cures; D1 re-measured) → **`X.W5.c2`** (Opus; grants: `AdminNamesPanel.vue` one-root `<Transition>` (D4) · `AdminPane.vue` (N15) · the `AdminTagsPanel` family's declaration sites (N14) · `census-parity.spec.ts:88-90` · `o12-blob-seat.spec.ts:68` · `o16-computed-cascade.spec.ts:158`). **C1** reads the `textContent` ratio ≥ 0.9 with the scroll-reachability rider. **C7** target 3 with `DockStatusLamp.vue:70` honest-RED by route → X-W8 `.h`. **C3**: capability-only rows classified out; the three layout forks → X-W8 `.i`.
