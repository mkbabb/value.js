# X.W11 - Release and Verified Close

## State

**Name**: W11 - Release and Verified Close
**Opens after**: X-W0 … X-W10 are IMPLEMENTED (four-verb law; not "closed", not "reported")
**Agents**: 5 serial, all Opus 5 (M-23 §2 — this wave carries no design content; X-W10's design
was twice-authored Fable ∥ Opus → fresh-Fable, and X-W11 consumes that apotheosis, never re-opens it)
**Hard gate**: twelve conditions in §6 — terminal ledger state (117/117) · release battery ·
packed-surface behavioural proof · registry coordinate · production boot both modes · deploy of
record · browser cell · real-Safari cell (separate) · consumer tuples · API journeys · zero legacy
seam · close correspondence
**Status**: planned

### Four-verb status (separate fields; this file moves exactly one)

| verb | value | evidence |
|---|---|---|
| AUDITED | **YES** | the megatranche corpus; `registry/CARRY-CUT-LEDGER.md` §1 CC-010 |
| SPECIFIED | **YES — by this file, 2026-08-03** | validated against `docs/precepts/instructions/tranche/WAVE_SPEC.md` per L-20 |
| IMPLEMENTED | **NO** | — |
| VERIFIED | **NO** | — stamped at this wave's release close (R-A) |

**R-A — the stamping site, ratified.** Gates green + bytes landed stamps a wave **IMPLEMENTED**;
that is what every sibling wave's own close may claim. **VERIFIED is stamped only here**, at
X-W11's release close, against the G7 browser cell, the G8 real-Safari cell (separate cells, never
merged), and the G9 consumer tuples. X-W11 advances its own verb line and every sibling wave's row
from IMPLEMENTED to VERIFIED in one act at close; no wave stamps VERIFIED at its own close.
ACCEPTED is later still — post-quartet, per the L-18 rider below.

## Goal criterion

This wave succeeds if the coherent cut is proven across package, demo, and bounded consumers at
one dated version — and if every one of the 117 carry-cut rows can be read off a single table as
VERIFIED (with a commit), RETIRED (with a tombstone path), or BLOCKED-ON (with an exact release
condition and a mechanically checkable re-trigger). It **consumes, never absorbs**: no row is
finished here that another wave owed.

## Scope

1. Author the terminal 117-row state table and append it to the carry-cut ledger as §4.
2. Run the release battery over the post-X-W10 tree and record it with pasted output.
3. Pack, install into a clean consumer, and prove the packed surface behaviourally (X-W9's cured
   `verify-packed-surface.mjs`), including the forbidden-specifier refusals.
4. Push the release tag; capture `release-candidate.json` + `registry-coordinate.json`.
5. Fire the deploy of record on master; re-probe production for the released commit.
6. Run and commit the browser evidence cell (Playwright, chromium + webkit-engine) over the 14
   declared routes + wildcard at desktop / phone / short-landscape.
7. Run and commit the real-Safari evidence cell (`safaridriver`) as a **separate cell**, never
   merged with webkit-engine.
8. Fill the consumer tuple table from install receipts, and send the outbound packets it implies.
9. Re-run X-W3's API journeys against the released build; assert no unresolved CRUD state.
10. Re-assert the zero-occurrence checks for every seam X-W8/X-W9 subtracted.
11. Close the coordination mail: zero UNREAD, every owed letter with a dated sent-row.
12. Write `docs/tranches/X/close/FINAL.md` citing commits and pasted commands only.

## Triumvirate Dispatch

Mandatory triumvirate (research + plan augment + redress); the orchestrator may not redispatch the
failing unit alone.

**File-bound expansion that invalidates the wave.** Any proposal to edit `src/**`, `demo/**`,
`api/src/**`, `e2e/**`, `.github/workflows/**`, or `scripts/ci/verify-packed-surface.mjs` from
inside X-W11. A release wave that edits mechanism is not a release wave; the cure lands in the
owning wave and X-W11 re-runs its battery from the top.

**Hard-gate failures that are not local-edit recoverable.**
- G4 after publish: a registry coordinate mismatch on an immutable version.
- G6: a deploy of record that shipped a commit that is not an ancestor of the tag.
- G8: a real-Safari verdict that inverts its webkit-engine twin (I-20 has already fired twice in
  this formation, both directions) — the disagreement is a finding about the harnesses first.
- Precondition failure at open: `package.json#version` still `4.0.0`, i.e. X-W9's dated cut did
  not set the release literal. X-W11 does not set it unilaterally.

**Diagnostic loops that must halt at the third iteration.** Any gate re-run three times without a
monotone change in its output; the release-battery flake loop; any consumer tuple that requires a
third install attempt.

## File Bounds

| File | Access |
|---|---|
| `docs/tranches/X/close/FINAL.md` | create |
| `docs/tranches/X/close/LEDGER-FINAL.md` | create |
| `docs/tranches/X/close/consumers/TUPLES.md` | create |
| `docs/tranches/X/close/packets/*.md` | create |
| `docs/tranches/X/close/evidence/battery/*.txt` | create |
| `docs/tranches/X/close/evidence/packed/*.json` | create |
| `docs/tranches/X/close/evidence/deploy/*.json` | create |
| `docs/tranches/X/close/evidence/browser/**` | create (PNGs force-added past `.gitignore:34 *.png`) |
| `docs/tranches/X/close/evidence/safari-real/**` | create (PNGs force-added) |
| `docs/tranches/V/megatranche/registry/CARRY-CUT-LEDGER.md` | modify-carve — **append §4 only**; §0–§3 are immutable |
| `docs/tranches/V/coordination/INBOX.md` | modify — mail rows only |
| `scripts/ci/verify-packed-surface.mjs` | execute, no write (G3's packed-surface probe; X-W9 owns the bytes) |
| `docs/tranches/V/megatranche/workflows/safari-real-matrix.js` | execute, no write (G8's real-Safari matrix) |
| `scripts/ci/boot-smoke.mjs` | execute, no write (G5's both-mode boot witness; X-W1 authors it) |

**Do NOT touch**: `src/**` · `demo/**` · `api/src/**` · `test/**` · `e2e/**` (X-W1 authors) ·
`.github/workflows/**` (X-W1) · `eslint.config.js` and `docs/tranches/V/megatranche/workflows/**`
(X-W8 G-6 owns the docs-tree lint cure; W11 only asserts its green) ·
`scripts/ci/verify-packed-surface.mjs` (X-W9 owns the behavioural
half and the `strictTypes` deletion) · `package.json#version` (X-W9's dated cut sets it) ·
`scripts/dev/dev.sh` (CC-021 — owner rules it at the X-W0 sitting; NEVER-touch stands until then) ·
`CARRY-CUT-LEDGER.md` §0–§3.

### Disjointness

No two units share a `modify` or `modify-carve` path. `CARRY-CUT-LEDGER.md` §4 is written by
X.W11.a alone; `INBOX.md` by X.W11.e alone. Every other path is `create` under a unit-owned
subdirectory. No sibling X-wave writes under `docs/tranches/X/close/**`.

### Worktree Plan

Single writer at a time (5 serial units) — no sibling worktrees, no `CARGO_TARGET_DIR` split. The
orchestrator commits between units so each opens on clean main.

## Agent Units

### X.W11.a Terminal ledger state table

- **Goal**: one table from which any reader can read every carry-cut row's terminal state without
  opening another document.
- **Mechanism**: walk `CARRY-CUT-LEDGER.md` §1 row by row (117). Each row emits exactly one of:
  `VERIFIED <commit>` · `RETIRED <tombstone path>` · `BLOCKED-ON <exact release condition>` +
  `<re-trigger command>` (L-4). Blocked rows keep their §1.M conditions verbatim — they are
  transposed, not softened, and never converted to a carry. Every row's terminal word is authored
  by its owning wave before this walk: CC-011's retire act and tombstone, and CC-109 / CC-116 /
  CC-117's words, are all authored at **X-W0** (R-C, R-D) — X-W11 transposes them, never states one
  first. Append as §4; write the same table to `close/LEDGER-FINAL.md`.
- **Files**: `docs/tranches/X/close/LEDGER-FINAL.md`, `CARRY-CUT-LEDGER.md` §4.
- **Sub-gate**: G1.

### X.W11.b Release battery and packed-surface proof

- **Goal**: the release tree passes every producer check, and the packed bytes — not the source
  tree — are what the checks inspected.
- **Mechanism**: run the seven battery commands with output pasted; `npm pack --ignore-scripts`;
  run X-W9's cured `verify-packed-surface.mjs` against the tarball; record the emitted JSON.
- **Files**: `close/evidence/battery/*.txt`, `close/evidence/packed/*.json`.
- **Sub-gate**: G2, G3.

### X.W11.c Tag, publish, deploy of record, production boot

- **Goal**: the released version exists at immutable registry coordinates and is the code
  production actually serves.
- **Mechanism**: push `v<cut>`; let `release.yml` run its own four equality checks; download both
  workflow artifacts. Merge to master; fire `deploy-pages.yml`; assert one non-skipped run; re-probe
  `https://color.babb.dev/` and the local `--mode gh-pages` preview at bare origin with X-W1's
  restored boot-smoke.
- **Files**: `close/evidence/deploy/*.json`, `close/evidence/packed/registry-coordinate.json`.
- **Sub-gate**: G4, G5, G6.

### X.W11.d Browser cell and real-Safari cell

- **Goal**: two independent evidence cells over two binaries, each able to falsify the other.
- **Mechanism**: Playwright across the five smoke projects + X-W1's `e2e/visual/` goldens, 14
  routes + wildcard × desktop / phone / short-landscape, every claim labelled per L-16. Then
  `docs/tranches/V/megatranche/workflows/safari-real-matrix.js` via `safaridriver` against the
  released origin. Force-add every capture (L-7). Unrun cells are `UNMEASURED`, never green.
- **Files**: `close/evidence/browser/**`, `close/evidence/safari-real/**`.
- **Sub-gate**: G7, G8.

### X.W11.e Consumer tuples, API journeys, seam subtraction, close correspondence

- **Goal**: every bounded consumer is proven against the released artifact by install receipt, and
  nothing owed to another repo is left unsent.
- **Mechanism**: for each consumer run `npm ls @mkbabb/value.js` post-install and record the
  resolved version (never a manifest range); re-run X-W3's X.A1..X.A5 journeys against the released
  build; re-assert the zero-occurrence seam checks; send the packets; clear the INBOX; write
  `FINAL.md`.
- **Files**: `close/consumers/TUPLES.md`, `close/packets/*.md`, `close/FINAL.md`, `INBOX.md`.
- **Sub-gate**: G9, G10, G11, G12.

## Hard Gate

Twelve conditions. **10 are born-RED with the failing probe measured read-only at 2026-08-03
against `41450f02` (tranche-u).** 2 are MEASURE-AT-OPEN because their cure lands in a prior wave
and no honest baseline exists until it does.

### G1 — Terminal ledger state, 117/117 · **BORN-RED**

**Probe**: `grep -c '^| CC-' docs/tranches/V/megatranche/registry/CARRY-CUT-LEDGER.md` = 117; then
the §4 extractor asserts each of the 117 carries exactly one terminal word plus its required
argument (commit / tombstone path / exact release condition + re-trigger).

**RED today**: 117 rows; **2** carry VERIFIED (CC-002 `c4af0ef9`, CC-028 `338c513b`); the 11
BLOCKED-ON rows carry *producer trigger* conditions (glass TR#47/#78/#84/#87, the 8.0.0 repin
census), **not release conditions**; **0** rows carry an X-close stamp. 115/117 unresolved.

**Falsifier**: change any row's word to a bare "done", or drop the condition clause from any
BLOCKED-ON row — the extractor fails naming that row's CC-ID. A row that appears twice, or a
118th row without a §1 origin, also fails.

### G2 — Release battery · **BORN-RED**

**Probe**: `npm run lint` · `npx vue-tsc -p tsconfig.lib.json --noEmit` · `npx vue-tsc -p
tsconfig.demo.json --noEmit` · `npm run build` · `npm test` · `(cd api && npx tsc --noEmit && npm
test)` · `npm run gh-pages`.

**RED today**: `npm run lint` → `✖ 50 problems (18 errors, 32 warnings)`; with `--max-warnings=0`
the script exits 1. All 30 offending files are under `docs/tranches/**` (the audit workflow
scripts; e.g. `megatranche/workflows/trifold-parser.js:61:1 Parsing error: 'return' outside of
function`) — **zero** in product source; all 30 are **tracked** bytes, so their cure is **X-W8
G-6**'s docs-tree ignore, which lands before this wave opens (§7). Recorded green cell to defend,
not to re-earn:
`npx vitest run` → `Test Files 26 passed (26) · Tests 348 passed (348) · 4.49s`.

**Falsifier**: the lint gate reddens on one reintroduced warning and names the file. The test
denominator may rise, never fall, without a named deletion row in §4 — a battery that reports
green while running fewer than 348 tests fails on the denominator, not the exit code (L-2).

### G3 — Packed surface, behavioural · **BORN-RED**

**Probe**: `npm pack --ignore-scripts` → `node scripts/ci/verify-packed-surface.mjs <tarball>`;
all 7 subpaths import from the installed tarball; **every runtime export is smoke-invoked** on its
documented input; the three forbidden specifiers still reject with `ERR_PACKAGE_PATH_NOT_EXPORTED`;
the emitted JSON contains no constant that no assertion computed.

**RED today**: `scripts/ci/verify-packed-surface.mjs:137` prints
`JSON.stringify({ runtime, strictTypes: 62 })` — `62` is a **literal that nothing measures** (1
grep occurrence, no producer). The script asserts export *name sets* only (80 names: color 23 ·
value 1 · css 20 · easing 16 · math 9 · transform 9 · quantize 2) and **invokes zero of them**, so
CC-084's R1 crash (`parseCssColor("oklch()")` throwing a raw TypeError from `src/css/grammar.ts:181`)
passes this gate today.

**Falsifier**: pack a build with one export deleted, or with one export throwing on its documented
input — the gate names the export and the subpath. A gate whose output still contains an
unproduced literal fails the fabricated-constant check.

### G4 — Tag, publish, registry coordinate · **BORN-RED**

**Probe**: push the release tag; `release.yml` green; `registry-coordinate.json`'s
`version`/`integrity`/`shasum`/`gitHead` each equal `release-candidate.json`'s.

**RED today**: `git tag --list | tail -1` → `v4.0.0`; `npm view @mkbabb/value.js dist-tags` →
`{"latest":"4.0.0"}`; `package.json#version` → `4.0.0`. No 4.1 artefact exists in the tree, the
registry, or the tag list.

**Falsifier**: already structural — `release.yml` throws by name on each of the four mismatches
(`tag X does not match package Y`, `registry integrity mismatch`, `registry shasum mismatch`,
`registry gitHead mismatch`). A hand-written `release-candidate.json` fails the registry
comparison, which reads npm, not the file.

### G5 — Production boot truth, both modes · **BORN-RED**

**Probe**: X-W1's restored boot-smoke re-run against the release tree in **both** modes — cold dev,
and `--mode gh-pages` preview served at bare origin: `#app` non-empty, `role="main"` present,
`pageerrors []`, ≥1 desktop utility class in the emitted CSS.

**RED today**: no such witness exists. `grep -rl "gh-pages\|vite preview" e2e/` returns exactly one
file — `e2e/smoke/perf/serve-built.mjs`, a perf harness with no boot assertion. The successor
witness covers dev only (CC-032); the structural guard was deleted by the tranche that orphaned it.

**Falsifier**: serve a bundle with the mount stripped — the probe reports an empty `#app` and
reddens. `exit 0` from `npm run gh-pages` is explicitly not accepted as evidence (L-2; this exact
substitution was W44's close evidence).

### G6 — Deploy of record · **BORN-RED**

**Probe**: `gh run list --workflow deploy-pages.yml --limit 5 --json status,conclusion,createdAt,headSha`
shows one **non-skipped, successful** run at the release commit; the standing deploy-age check
confirms the deployed commit is an ancestor of the tag; production re-probed for the release build.

**RED today** (measured 2026-08-03): all five most-recent `deploy-pages` runs are
`"conclusion":"skipped"`, newest `2026-07-18T02:16:57Z`, every one at `headSha 44ddaff7…`.
`git rev-parse master` → `6abef8005f…` (`6abef800`, T-era) while HEAD is `41450f02` on `tranche-u`
— so production cannot be serving `c4af0ef9` (landed 2026-07-29), let alone the release tree.
`gh run list --workflow ci.yml --limit 6` shows **zero** master runs; latest CI activity
2026-07-18.

**Falsifier**: point the deploy at a stale ref — the ancestry check fails and names both SHAs. A
`skipped` conclusion is a failure, not a pass.

### G7 — Browser evidence cell (BROWSER-LIVE) · **BORN-RED**

**Probe**: `npx playwright test` across the five smoke projects plus X-W1's `e2e/visual/` goldens,
covering the 14 declared routes + wildcard (`/`, `/palettes`, `/browse`, `/extract`, `/mix`,
`/generate`, `/gradient`, `/atmosphere`, `/blob`, `/admin/{users,names,audit,flagged,tags}`,
`/:pathMatch(.*)*`) at desktop / phone / short-landscape; every claim labelled per L-16.

**RED today**: 71 spec files under `e2e/` and
`grep -rn "toHaveScreenshot\|toMatchSnapshot" e2e/ | wc -l` → **0** — no visual assertion exists in
the entire suite. `e2e/visual/` is absent. `.github/workflows/ci.yml` contains **zero**
`playwright`/`e2e` references, so the whole suite is executed by no automation.

**Falsifier**: X-W1's mandatory one-time falsifier is re-demonstrated here — a deliberately broken
spec must turn the job red. A report that is green because 0 specs executed fails on the executed
count, which is asserted separately from the exit code.

### G8 — Real-Safari evidence cell, held separate · **BORN-RED**

**Probe**: `docs/tranches/V/megatranche/workflows/safari-real-matrix.js` driven by `safaridriver`
against the released origin, with the API up; every capture force-added; the cell recorded in its
own columns, never merged with `webkit-engine`.

**RED today**: the only real-Safari corpus is `audit/visual/safari-real/` — captured
2026-07-27 12:26–13:14 −0400 against a **pre-X tree** (Safari 26.4 / macOS 26.4.1, origin
`localhost:9000`, **API-LESS, no mongo**). `git ls-files` returns **4** tracked files against **31**
on disk: 27 witnesses are uncommitted and therefore do not exist (L-7). No safari cell exists for
any release tree.

**Falsifier**: I-20 cell separation — copying a `webkit-engine` verdict into a `SAFARI` column is a
gate failure, not a shortcut. An unrun safari cell must read `UNMEASURED` /
`UNREACHABLE-IN-CELL` / `UNVERIFIABLE-HERE`; a cell that reads green without a per-shot sha256 in
the sidecar fails.

### G9 — Consumer tuples, from install receipts · **BORN-RED**

**Probe**: for each bounded consumer, install the released version and record
`npm ls @mkbabb/value.js` — the *resolved* version, never the manifest range — plus the consumer's
own check verdict and the packet receipt.

**RED today** (measured 2026-08-03):

| consumer | declares `@mkbabb/value.js` | verdict today |
|---|---|---|
| `keyframes.js` 6.0.0 | `"4.0.0"` — **EXACT pin** | RED for any 4.1 cut: the pin excludes it. RD-11 by-packet notification owed (CC-084's "exact-pin consumers notified"; no emergency patch — ruled) |
| `glass-ui` 7.0.0 | `"^4.0.0"` | admits the cut; **no green check recorded against it** |
| `fourier-analysis/web` | `"^0.13.0"` | RED — **three majors stale**; also `@mkbabb/glass-ui ^4.0.0` (installed 7.0.0) and `@mkbabb/keyframes.js ^4.3.0` (installed 6.0.0) |
| `value.js` demo (self) | vite self-alias → `dist/subpaths/*` | must resolve through the packed exports map at release, not the source tree |

**Falsifier**: a tuple whose "resolved" column was filled by reading a `package.json` range instead
of an install receipt fails — the recorded command must be `npm ls`, run in the consumer. A green
tuple for a consumer whose packet in G12 is unsent is a contradiction and fails both gates.

### G10 — API journeys and CRUD state · **MEASURE-AT-OPEN**

X-W3 owns the cure (X.A1..X.A5, classes 1 + 8 P0, the admin guard); X-W11 re-runs the journeys
against the released build and asserts no unresolved CRUD state and no API-labelled state
collision.

**Baseline recorded today, not a gate**: `curl -s localhost:3000/health` →
`{"status":"ok","service":"palette-api","version":"2.0.0","commit":"dev","checks":{"mongo":"ok","primary":true}}`.
The release cell requires `commit` ≠ `dev`. The RED baseline is X-W3's to publish at its open; it
is not restated here.

**Falsifier**: a journey labelled BROWSER-LIVE that used a direct API request for any mutation is
rejected (L-16 — the 07-28 consumer audit produced exactly that defect). A journey that needed a
disposable fixture is labelled BROWSER-FIXTURE and does not satisfy this gate.

### G11 — Zero legacy path, zero compatibility seam · **BORN-RED**

**Probe**: the zero-occurrence checks for every seam X-W8/X-W9 subtracted — the 19 `demo/ui`
barrels, `cn`/`clsx`/`tailwind-merge`, `components.json`, stale ESLint paths, the
`useLayerTransition` shim, the pruned library families — each with the command recorded.

**RED today**: `package.json#devDependencies` still declares `"clsx": "^2.1.1"` and
`"tailwind-merge": "^3.6.0"`; the 19 barrels stand (CC-078's verified count, alongside `0` cn()
consumers, `88` SFCs, stale `components.json`).

**Falsifier**: each check names its forbidden specifier and the file that reintroduced it. A check
that passes because its glob matched nothing is a **failure** — the glob must be shown to match the
pre-subtraction tree (recorded in X-W8's close), otherwise it is a vacuous gate and is deleted
(L-2), not counted.

### G12 — Close correspondence · **MEASURE-AT-OPEN**

`docs/tranches/V/coordination/INBOX.md` carries **zero UNREAD** at close, and each owed letter has
a dated sent-row: the exact-pin packet to keyframes.js (RD-11, by-packet, no direct-edit grant);
the glass §D letter(s) for every producer-owned residual; the parse-that routing receipt (parser →
PLAW-BIND → packed release → consumers); the fourier co-signed CRUD-provenance contract.

**Falsifier**: an unsent packet whose G9 tuple reads green fails both gates. A letter cited by
`FINAL.md` that `git cat-file -e` cannot resolve fails L-6.

## Dispositions

| ledger row | verb here | act |
|---|---|---|
| **CC-010** (V·MT9 · V·C6) | **FOLD — this wave** | its completion law is §6 in full; the final state table (G1) is the row's own terminal act |
| CC-002, CC-028 | VERIFIED — carried in | transposed into §4 with their commits (`c4af0ef9`, `338c513b`); not re-proven |
| the 17 RETIRE rows — **incl. CC-011** (V·L5, homed at X-W0 by R-C) | consumed | §4 records each tombstone path authored by its owning wave; X-W11 authors none of them |
| **CC-109, CC-116, CC-117** | consumed — authored at X-W0 | CC-109/CC-116's value action *is* the Glass-8 repin census, run as X-W0's census event (R-B, R-D); CC-117 gets its terminal word there once (evidence-only, FOLD → INBOX I-24). §4 transposes all three verbatim |
| **the §1.M bank** — CC-003, CC-044, CC-105, CC-106, CC-107, CC-110, CC-111, CC-112, CC-113, CC-114, CC-115 | **stay BLOCKED-ON** | 11 rows enter the §4 release table with their exact conditions verbatim (glass 8.0.0 repin census · TR#47 · TR#78 · TR#84 · TR#87) plus a mechanically checkable re-trigger (L-4). **None is smuggled into this wave.** A trigger that fires before close hands the row to its *receiving* surface — **X-W4.g**, the trigger-gated unit that lands the banked limbs as one atomic cut (R-B) — never to X-W11, which keeps only the terminal BLOCKED-ON accounting |

## COMPLETABLE

**TERMINAL — completable only as the close, and completable in the presence of the bank.**

X-W11 adds no product mechanism: its bytes are a tag, four workflow-produced receipts, two evidence
cells, a consumer table, four packets, and two documents. There is no forward hook, because every
gate asserts a property of an already-built artifact. It lands alone in the C-06 sense — once
X-W0..X-W10 are IMPLEMENTED, nothing in X-W11 waits on anything else, and no later wave depends on
it.

The load-bearing half: **the 11 banked rows do not block this close.** They land in the release
table as BLOCKED-ON with exact release conditions, which is precisely what makes a close honest
while a producer dependency is open. The alternative — holding the close until glass 8.0.0 ships —
is the twelfth carry, and re-booking is forbidden (M-10, L-5).

## Format And Lint Cadence

`npm run lint` at each unit boundary and before close (it must reach **0 problems**, from today's
50 — those 50 are tracked bytes under `docs/tranches/V/megatranche/workflows/*.js`, outside this
wave's File Bounds and untouched by X-W0's track-or-archive, which disposes *untracked* bytes only.
They are cured by **X-W8 G-6** (`eslint.config.js` ignores `docs/tranches/**`), which lands before
X-W11 opens: **W11 asserts the green, it does not produce it.** A red lint at open is an X-W8
regression, returned there — X-W11 does not edit `eslint.config.js` or the offending scripts).
`npx prettier --check` over the created documents. `git diff --check` before every commit. No
formatter is skipped.

## Verification Artefacts

- `docs/tranches/X/close/FINAL.md` — commits and pasted commands only; cites no document this
  close authored (L-6)
- `docs/tranches/X/close/LEDGER-FINAL.md` + `CARRY-CUT-LEDGER.md` §4 — the 117-row table
- `docs/tranches/X/close/evidence/battery/*.txt` — the seven battery commands with output
- `docs/tranches/X/close/evidence/packed/{release-candidate.json,registry-coordinate.json,verify-packed.json}`
- `docs/tranches/X/close/evidence/deploy/{deploy-runs.json,prod-probe.json,boot-smoke.json}`
- `docs/tranches/X/close/evidence/browser/**` — Playwright report + goldens, PNGs force-added
- `docs/tranches/X/close/evidence/safari-real/**` — `MATRIX-SAFARI.md`, `ROUTE-MATRIX.json`,
  `STATE-MATRIX.json`, per-shot sha256, PNGs force-added
- `docs/tranches/X/close/consumers/TUPLES.md` — install receipts, not manifest ranges
- `docs/tranches/X/close/packets/*.md` — keyframes / glass / parse-that / fourier
- the release tag, and the two `release.yml` workflow artifacts by run ID

## Commit Plan

| # | scope | body required |
|---|---|---|
| 1 | `docs(X·W11 ledger): terminal 117-row state table` | yes — status change, per-verb counts |
| 2 | `test(X·W11 battery): release battery receipts at <sha>` | yes — pasted denominators |
| 3 | `build(X·W11 packed): packed-surface behavioural proof` | yes — export-invocation counts |
| 4 | `release(X·W11): v<cut> tag + registry coordinate` | yes — integrity/shasum/gitHead |
| 5 | `ci(X·W11 deploy): deploy of record + production boot witness` | yes — run ID, ancestry |
| 6 | `test(X·W11 evidence): browser cell + real-Safari cell (separate)` | yes — cell separation note |
| 7 | `docs(X·W11 consumers): tuples from install receipts + outbound packets` | yes |
| 8 | `docs(X·W11 close): FINAL + INBOX zeroed + IMPLEMENTED→VERIFIED stamps` | yes — the per-wave verb stamps this close alone may set (R-A) |

Pathspec commits only. `scripts/dev/dev.sh` is never staged.

## Dependencies

- **Depends on**: X-W0 (ledger + graph authority + owner rulings), X-W1 (CI, visual oracle,
  boot-smoke, deploy truth — G5/G6/G7 are re-runs of machinery X-W1 builds), X-W2, X-W3 (G10),
  X-W4, X-W5, X-W6, X-W7, X-W8 (G11's subtraction denominators), X-W9 (the dated cut, the
  `package.json#version` literal, the cured `verify-packed-surface.mjs` — G3), X-W10 — all
  **IMPLEMENTED**, not "closed".
- **Blocks**: nothing. Terminal.

## Archaeology

**Prior attempt.** `V·MT9` was authored CANDIDATE, *never SPECIFIED*
(`MECHANISM-CUT-FORMATION-MAP-2026-07-29.md:435`); its twin `V·C6` sat NOT-STARTED
(`CONVERGENCE-RESUME-HANDOFF-2026-07-29.md:198`). Neither ran. Both are re-homed here through
CC-010, under this ledger's naming authority (§0.1) — the `V·MT`/`V·C` labels may not gate work.

**Failure modes this wave is built against.**
- T.W9's "ceremony deploy record" and W44's "build + gh-pages exit 0" both certified health from an
  exit code while `gh-pages` emitted a bundle with no application in it. → **Guardrail**: L-2 is
  written into G2, G3, G5, G7, and G11 — every gate names the artifact property and the input that
  violates it, and a vacuous check is deleted rather than counted.
- N's and J's FINALs certified against trees and documents that never existed. → **Guardrail**:
  L-6 in the Verification Artefacts section; `git cat-file -e` before any citation.
- 574 of 625 visual witnesses untracked behind `.gitignore:34`. → **Guardrail**: L-7 force-add in
  X.W11.d, and G8's RED baseline is literally this defect measured again (4 tracked of 31).
- Evidence modes impersonating one another in the 07-28 consumer audit. → **Guardrail**: L-16
  labels in G7/G8/G10, and I-20 cell separation as a gate condition, not a convention.

## L-18 rider — this wave's implementation is not ACCEPTED at green

Passing all twelve gates makes X-W11 **IMPLEMENTED** — and, on the G7/G8/G9 receipts, lets this
close stamp **VERIFIED** (R-A) — but never ACCEPTED. Per **L-18**, the
implementation must first survive **two challenging gestalt passes**, each a **quartet of Opus 5
agents** assuming it is wrong, across all three altitudes — total-tranche (was closing here optimal
in the greater plan; was this spec adhered to; what friction arose), wave, and feature — reporting
**defects and superlatives**, each with provenance and proof. The two passes are then adjudicated
and agglomerated into an apotheosis by a **singular fresh Fable instance**. Any incongruity it
surfaces is addressed, ratified, and dispatched — never parked (L-18 §5).

Reference, not ceremony: for a release wave the quartet's most probable finds are a gate that went
green on an exit code, a consumer tuple filled from a manifest, a safari cell corroborated from its
webkit twin, and a BLOCKED-ON row whose "exact condition" is prose. Each of those is already a
named falsifier above; the quartet exists to find the ones that are not.
