# X.W12 — the owner's value.js dockets (2026-09-22 OA-4..6 + 2026-09-23 OA-18..25): ground, drag, motion, the About pane, the picker's hierarchy, the dock

## State
**Opens after**: X-W7 CLOSED. **Blocks**: X-W8 (X-W8's `.f .g .h` are ABSORBED here — COHESION §0bd; X-W8 keeps `.a–.e` and `.i`).
**Minted by**: COHESION §0bd (2026-09-23). **Model**: Opus 5.5 every seat (owner 2026-09-23); design seats at effort high.
**Units**: 5 serial — [`.a`] → [`.b`] → [`.c`] → [`.d`] → [`.e`]. **Record**: `docs/tranches/X/execution/A/X-W12.md`.
**Frames (banked)**: `docs/tranches/X/waves/owner-2026-09-23/frame-{1..4}.png` (READ each image).

## Authority — the owner's words, verbatim
2026-09-22: *"the value.js picker is slow on drag of changing the color, and frequently the background areao goes black. And the right side about the spaces is far too long and not clipped. And the dock for every app is not quite right--doesn't properly contain elements (like in keyframes.js), animate (the "login" background and transitions in value.js isn't right)"*
2026-09-23: *"the background is black, the aurora is buggy, the coloc selection and dragging is slow; the startup animation and card animations are janky, jittery, and buggy [frame 1]--the about the color spaces is too long and does not clip properly to be the same size as the left pane [frame 2]--login's background has a strange different color and is squared, same with @mbabb; the dock transitions in tools and others is janky, not glass-ui idiomatic and ios27 like--the dock does not smoothly morph like it should--all glass-ui changes should be done at the root and communicated with the running and developing glass-ui instance. [frame 3]--the usage of space here is bad--ensure that we suffuse proper and better usage of design hierarchy. Maybe the dropdown title should be larger? Maybe the color number components larger with less padding? Why so much blank space; the blob is awful, not animated, and not glass-ui idiomatic? What's going on with that in glass-ui. All of the pane/card transitions are janky, double animated, and broken"*

## Goal criterion
Measured on the owner's surface (`/`, dark, 1440×900 and the 2846×1650 retina frame) with the dev server AND the gh-pages build: the atmosphere never paints black; a picker drag holds frame budget; every pane/card enter runs ONCE on one owner and the boot overture is smooth; the About pane is exactly the Picker pane's height with its own scroll; the Picker card's hierarchy uses its space; the dock's pills and morph sit on glass's primitives. Producer halves ride O-55/O-56 — never a consumer copy.

## §1 Bounds
`demo/**` · `e2e/smoke/**` (new `w12-*` specs and oracles) · `docs/tranches/X/waves/W12-evidence/**` · the record · `LEDGER.md` · `INBOX.md` (mail rows). **Do NOT touch**: `glass-ui/**` (READ-ONLY; asks go to O-55/O-56 and the live glass session), `src/**` (the library) unless `.a`'s trace names a library hot path (then one commit, `npm test` GREEN), `package.json` pins.

## §2 Units
### X.W12.a (Opus) — ground + drag (was X-W8 `.f`; OA-4, OA-18, OA-19)
1. Reproduce the black ground: cold load, idle 30 s, tab-hide/show, resize, drag — on dev and the gh-pages build; record when the atmosphere paints black (a pixel read of the page ground outside the panes). Bisect consumer (ground seed, aurora mount/unmount, context loss, the atmosphere composable) vs producer; cure the consumer cause at the root; a producer cause becomes an O-56 G-4 follow-up with the measurement.
2. "aurora is buggy": name each artefact seen on the frames (banding, flicker, jumps, freezes) with a capture; cure consumer-owned ones.
3. Drag: a 2 s pointer drag on the picker surface and on each slider under a Playwright trace → long tasks, frames > 16.7 ms, per-input work named; cure at the root (coalescing, per-input recompute, reactive fan-out). Gates: 0 black-ground samples over the reproduction script ×2; drag p95 frame ≤ 16.7 ms on the headed real-GPU cell (§0ax D1 instrument) ×2, SwiftShader reading banked beside.

### X.W12.b (Opus, effort high) — motion: boot overture + every pane/card transition (OA-20, OA-25)
1. Census every animation that runs on boot and on each pane navigation (`document.getAnimations()` sampled + transition classes), per element: owner, count, duration. "Double animated" = one element animated by two owners (e.g. a pane `vj-enter` AND a card enter, or a CSS transition AND a WAAPI animation on the same property). Table pasted.
2. One owner per element per property; the boot overture reads as one sequenced entrance on glass's motion tokens; jitter (layout shift during enter, transform + top/left mixes, a re-mount mid-enter) cured at the cause. Respect X-W5's cold-nav cure (`f55e59b7`) — never regress its witness.
3. Gates: the census re-run shows 0 elements with >1 owner per property ×2; CLS during boot ≤ 0.02; the X-W5 cold-nav witness and W6 g2 GREEN ×2; a before/after WebM of boot and of three pane switches committed.

### X.W12.c (Opus) — the About pane (was X-W8 `.g`; OA-5, OA-21)
The About pane's outer box is exactly the Picker pane's height (the shared row contract; W6 g2's track) at 1440×900, 1280×800, 2846×1650-equivalent; its body scrolls inside on the glass scroll primitive with the card's radius clipping the scroller; no page overflow. Gate: `w12-about-clip.spec.ts` — heights equal ±1 px at 3 viewports, the scroller clipped by the radius (clipped screenshot), ×2.

### X.W12.d (Opus, effort high, design-author) — the Picker card's hierarchy + the blob (OA-24, OA-23-consumer)
1. Frame 4: the space-name dropdown title, the channel read-out and the blob share a card with a dead band between the title row and the numerals. Design pass on the type scale: the title is the card's display step, the numerals take the space with tighter padding, no empty interval (this also discharges g1's largest-interval finding if it lands on the scale — record either way). Design notes with before/after frames in the record.
2. The blob: measure whether the hero blob animates (frame diff over 5 s idle and on hover) and whether it renders through glass's `blob` primitive with its defaults or a consumer config that stills it (mood FSM, PRM, config). A consumer cause is cured here; a producer cause is O-56 G-3 (no consumer copy).
3. Gates: the g1 card-rhythm probe re-run; the blob frame-diff > 0 at idle ×2 unless PRM; owner-frame re-crops committed.

### X.W12.e (Opus, effort high) — the dock (was X-W8 `.h`; OA-6, OA-22)
The login and `@user` pills: their plate colour and radius come from glass's dock control (no local background, no square corners); the layer transitions (tools and others) ride glass's `DockLayer`/`DockCrossfade` spine with no local keyframes. Where the installed glass (7.0.0) cannot give the iOS-27 morph, record `DOCK-MORPH-ROOT` honest-RED with O-55/O-56 — no consumer copy. Gates: `w12-dock.spec.ts` — the pills' computed background equals the dock control token and radius ≥ height/2 ×2; 0 local dock keyframes (grep); a WebM of each layer switch committed.

## §3 Close
All unit gates; `vue-tsc` demo 0; vitest and the smoke suite unchanged-GREEN; every owner row OA-4..6, OA-18..25 listed with its sha or honest-RED id.

**ADDENDUM 2026-09-23 (COHESION §0bn) — unit `.l` (Opus), first in order.** KFA-14: `src/css/grammar.ts:221-227` rejects legacy `rgba(r, g, b, a)` — a CSS Color 4 conformance bug (legacy comma syntax for `rgb()/rgba()/hsl()/hsla()`, §5.1/§6.1). Writable: `src/css/grammar.ts` · `test/**`. Cure in the grammar (no preprocessing shim), WPT-derived cases for every legacy form incl. `none` rejection in legacy syntax and percent/number mixing rules; `npm test` GREEN; a keyframes Snapshot built from `rgba(...)` parses. Order: [`.l`] → [`.a`] → … as before.

**ADDENDUM 2026-09-23 (COHESION §0bs).** X-W12 now opens after **X-W7R** (value.js repins glass-ui ^7.0.0 → 10.0.1), so `.e`'s dock work and every glass-owned row are read on 10.0.1.

**ADDENDUM 2026-09-23 (COHESION §0by).** Unit `.l` is RETIRED from X-W12: the legacy comma forms (KFA-14) land in value.js's BBNF grammar (X.P.W6 `.b`), and `src/css/grammar.ts` is deleted by X.P.W6 `.x` — no seat edits it meanwhile. X-W12 starts at `.a`.

## ADDENDUM 2026-09-23 — UI-audit fold (COHESION §0ca)
- `docs/tranches/X/audit/UI-AUDIT-value.md` landed. Every UIA-V row is in X-W12's scope. A row whose cure is a glass surface goes by relay only, and its O-59 row is cited.
- **The audit critic's coverage gaps are X-W12 work.** Each is captured on the served page before its row is cured:
  - the Browse delete-confirm dialog, which was never captured
  - dock-color-input, which has frames only at 1440 light (add 390 and dark)
  - admin-flagged in its populated states, and palettes-view, which each have one leg only
  - every UIA-V row with no frame, and the 4 rows marked UNRESOLVED (resolve each to a verdict)

## ADDENDUM 2026-09-23 (b) — glass BL round-2 interim facts: consumer halves (COHESION §0cb)
- **G-2, dead `variant` bindings.** The installed glass 7.0.0 Button declares `emphasis`, `tone` and `size`, and has no `variant`. value.js binds `variant=` on 13 Button tags across 7 files; glass counted 49. Each silently does nothing. Move each one at the root to `emphasis`/`tone`, then sweep every glass component for other removed props bound in the demo. Measured 2026-09-23.
- **G-3, a parked blob.** HeroBlob parks on wall-clock timers: `BLOB_IDLE_MS`, `SLEEPY_POSE_MS` and the `idleTimer`/`poseTimer` pair. The owner's "not animated" is that consumer half. The cure consults the producer's quiescence/`settled` seam if the pinned glass ships one. If it does not, the unit records honest-RED G3-PARK-CLOCK with its O-row, and adds no local engine. The matte look is producer-side and goes by relay only.
- **G-4, the black ground.** Glass measured the ground maroon, never black, at both 7.0.0 and HEAD. So the black ground is value.js's to bisect. Bisect the served page's ground and fix it at the source.

## ADDENDUM 2026-09-23 (c) — OA-54: the large colour-space dropdowns become text (COHESION §0ci)
- **Owner, verbatim:** *"too gray, and the large dropdowns should not be so rounded. Perhaps for these, the large color space dropdowns, it should just be text, too."* Frame: `audit/owner-2026-09-23-colorspace-dropdown.png`. It shows the "Lab" trigger as a grey stadium plate around display-serif text.
- **Unit `.t`:**
  - Every large, display-type dropdown trigger becomes a text trigger: the colour-space selector in the picker, and every sibling found in a census. It is the label in its display type plus a chevron, with no plate, no fill and no stadium. Hover and press affordance comes from ink and the chevron. Keyboard focus shows a visible ring.
  - Use glass's own quiet or text trigger emphasis. If the pinned glass (7.0.0) has none, relay the gap and record the honest-RED id. Never copy a trigger locally.
  - Every remaining large dropdown or holder that keeps a surface takes `--radius-field`, never a stadium (canon DESIGN.md:385-391; O-58).
  - Gate: on the served page, in headed Chromium at 1440 and 390, in both themes, the trigger paints no background and no border-radius plate. Focus-visible paints a ring. The menu opens and selects by pointer and keyboard. Before and after frames.
- The greyness itself is GLASS-VEIL-GREY (O-62) wherever a glass plate remains. Record it, never override it.

## ADDENDUM 2026-09-23 (d) — OA-55: DOCK-TRIGGER-CLIP (COHESION §0cj)
- **Owner, verbatim:** *"buttons in the dock are clipped on hover and select like this."* Frame: `audit/owner-2026-09-23-dock-trigger-clip.png`.
- The cause is glass's dock row, which clips the block axis (O-63). X-W12 records honest-RED DOCK-TRIGGER-CLIP with a served-page frame of hover, selected and focus-visible. It never adds local dock padding or overflow overrides.
- Exception: if a value.js consumer wrapper adds its own clip around the dock, that consumer clip is cured here. X-W12 checks the ancestor chain.

## ADDENDUM 2026-09-24 (e) — OA-57: the collapsed dock (COHESION §0cq)
- **Owner, verbatim:** *"the dock when collapsed is not correct--same with keyframes.js and the docks in the other apps"*. Frame: `audit/owner-2026-09-24-collapsed-dock.png`.
- X-W12 reads value.js's collapsed dock on the served page, at 1440 and 390 and in both themes. It cures any consumer cause. The producer half is O-65, recorded as honest-RED **DOCK-COLLAPSED-FORM**.

## ADDENDUM 2026-09-24 — OA-66/OA-67 (COHESION §0cs)
- Record honest-RED **GLASS-SELECT-GREY** (O-66) for Select triggers and selected items painting grey. Check the call sites for any consumer class or dead prop that causes it, and cure those locally. Never override the glass surface. Dock motion stays with glass D2 (O-66 §2).

## ADDENDUM 2026-09-24 — OA-68 (COHESION §0ct)
- Read every side and canvas dock on the served page, in both themes. Record honest-RED **SIDE-DOCK-EDGE** (O-67) for plate edges that do not close and for badge overlap. Cure only a consumer clip or crowding in the ancestor chain, such as a missing gutter to the panel above. Never override the dock.

## ADDENDUM 2026-09-24 (f) (COHESION §0dm) — E-R1-1 and ESC-W12d-1 ruled; both are re-homed to X-W12U units with owners
- **E-R1-1 (the drag p95, RED ×2 on the 120 Hz panel).** The named consumer causes are cured (`0ec9bb1e`, `e1e26fe8`). What remains is a whole-app recolour: one forced style-and-layout per frame, billed inside glass's dock-overflow `measure()` (`dock.js:628`). The root custom-property writes are about 25% of it. **Ruled (both halves):**
  - **(a) Glass:** O-80 DOCK-OVERFLOW-LAYOUT. `useDockOverflowFit` must not force a synchronous layout each frame (a ResizeObserver or containment signal, never a layout read on a style change).
  - **(b) Consumer:** the recolour writes each colour custom property only on the subtree that reads it, coalesced into one write per frame.
  - This is unit **X-W12U `.p`**, measured **after X-W7L** (on glass 10.1.0's dock code).
  - **The D1 cell is the 120 Hz panel** (`W12_WINDOW_POSITION=2600,200`), because on the 60 Hz main panel a blank page reads 17.6 ms. The budget is unchanged: p95 ≤ 16.7 ms.
  X-W12's close reads the drag gate as **RE-HOMED → X-W12U `.p` (O-80)**, with the measurement.
- **ESC-W12d-1 / E-R1-2 (four W3-3 idle-park specs against the live-blob gate).** The owner wants the blob alive ("the blob is awful, not animated"). **Ruled: the live-blob gate supersedes.** The four specs (`webgl-blob-idle:53`, `mobile/blob-presence-mobile:56`, `perf/idle-frame-budget:48`, `o12-blob-seat:134`) are **restated** on the settled seam as an idle frame-cost budget with the hero live: a bounded per-frame cost and PRM still parking it. The dead mirror `fixtures/blob-timing.ts` is deleted. `HERO_FISSION_AMP` is not tuned to pass a spec. This is unit **X-W12U `.b`**.
- **X-W12 then closes** CONFORMANT-HONEST-RED, with the honest-RED set the check listed (O-55/56/62/63/64/65/66/67/68) plus these two re-homed rows.
