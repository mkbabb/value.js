# X.W0 - Formation: Rulings, Tombstones, Track-or-Archive, Graph Authority

## State

**Name**: W0 - Formation: Rulings, Tombstones, Track-or-Archive, Graph Authority
**Opens after**: tranche X opens (X is ratified — `registry/CARRY-CUT-LEDGER.md` §0.2)
**Agents**: 10 units, mixed serial/parallel (≤4 concurrent — owner cap 2026-07-24)
**Hard gate**: zero untracked bytes under `docs/`; 264/264 replaced by 218 CHALLENGED + 46 REPORT-AUTHORED wherever it gates; nine tombstones with pasted probes; seven owner rows returning LANDED-AS-RULED or RETIRED; typed graph v3 round-tripping with X-W8's four SCCs named; the V·L5 routing law rewritten against defined waves; the Glass-8 repin census returning a dated four-condition verdict with measured bytes
**Status**: complete_with_misses (2026-09-17 — see §CLOSE)
**Verb status (four-verb law)**: AUDITED ✓ (CARRY-CUT-LEDGER 2026-08-03) · **SPECIFIED ✓ (this file)** · **IMPLEMENTED ✓ 2026-09-17** (17/18 hard gates + 8/8 fold gates GREEN; **HG-7 RED — ESCALATED E-1**; **HG-10 SPLIT — E-2**) · VERIFIED ✗ (R-A — stamped only at X-W11's release close)

**COMPLETABLE (C-06)**: **YES.** Every act writes `docs/**` or `docs/tranches/V/megatranche/workflows/**` bytes. Zero product source, zero producer dependency, zero network requirement except the one VPN-gated probe in CC-023, whose disposition (accept the 301 in canon) is executable without it. The owner sitting is one scheduled calendar entry with rendered artifacts attached. No row in this wave waits on X-W1..X-W11, on glass 8.0.0, or on any external repo — X-W0.j *evaluates* the Glass-8 trigger against installed bytes and returns a dated verdict either way; a FAIL is a complete result, not a deferral. `c4af0ef9` (CC-002, V·MT1 landed alone) is the template.

---

## Goal criterion

This wave succeeds if, when work ends, **no claim this tranche will rely on rests on a byte the owner cannot see, a denominator that conflates authorship with challenge, a row that has ridden three closes without a verdict, an architecture assertion without a typed graph behind it, or a producer trigger no unit is charged with evaluating.** X-W0 makes the record durable and the denominators honest; it changes no product behaviour.

---

## Scope

1. Track or archive every untracked authority and evidence tree under `docs/`, one disposition line per tree, in one commit (CC-012 / DR-23).
2. Re-author the prompt recap from the 181-event corpus as tracked canon with per-row phrase grounding; reconcile the 0/2-vs-2/2 clean-pass contradiction; restore the twelve dropped owner asks by name (CC-013 / DR-30).
3. Print the challenged denominator: every gate citing `264/264` restates as **218 CHALLENGED + 46 REPORT-AUTHORED**; the validator's self-test baseline goes green (CC-025 + CC-001's gate half).
4. Close the 7e28 worktree corpus: re-verify the 90 adopted copies against their recorded original digests, confirm the `PRODUCER-ARTIFACT-BOUNDARY` name collision stays resolved (CC-026 / C-11).
5. Land the three re-run axes as tracked, witnessed evidence and quarantine every unchecked motion/PRM assertion in the 46 report-authored axes (CC-027).
6. Write nine tombstones — one per RETIRE row CC-015..CC-023 — each quoting the original mandate and pasting its probe output.
7. Hold one owner sitting: seven owner-held rows, each returning LANDED-AS-RULED or RETIRED, dated (CC-014 / DR-29).
8. Build typed graph v3 as the authority X-W8 subtracts against, and land the v3 source census as the frontend denominator (CC-001 graph half + CC-024).
9. Execute the C-13 RETIRE act: rewrite every live routing law keyed to the nonexistent `V·L5` against `V·L1..V·L4` plus an explicitly defined new cut, normalize the glyph and register the period-form search alias, and tombstone the row (CC-011).
10. Hold the **Glass-8 repin census event**: evaluate CC-003's four machine-checked conditions against installed bytes, record PASS/FAIL with the measured values, and fire — or keep shut — X-W4.g, the trigger-gated receiving surface (CC-003's trigger + CC-109 + CC-116; CC-117's terminal word).

---

## Ledger rows this wave discharges

| CC row | origin | verb here | unit |
|---|---|---|---|
| CC-012 | DR-23 | BUILD | X-W0.a |
| CC-013 | DR-30 | BUILD | X-W0.b |
| CC-025 | completeness toolchain | BUILD | X-W0.c |
| CC-026 | 7e28 worktree corpus (C-11) | BUILD | X-W0.d |
| CC-027 | three axes + PRM quarantine | BUILD | X-W0.e |
| CC-001 | V·MT0 | FOLD | X-W0.c (denominator) + X-W0.h (graph) |
| CC-014 | DR-29 owner packet | FOLD | X-W0.g |
| CC-024 | M11 v3 source census (K-24) | FOLD | X-W0.h |
| CC-015..CC-018, CC-022 | DR-02/14/15/16/25 | RETIRE (executed) | X-W0.f |
| CC-019, CC-020, CC-021, CC-023 | DR-19/20/24/31 | RETIRE (owner-ruled) | X-W0.g |
| CC-011 | V·L5 (C-13) | RETIRE (executed) | X-W0.i |
| CC-109, CC-116 | glass §1.M | FOLD (the census IS their value action) | X-W0.j |
| CC-117 | provenance row 27 / INBOX I-24 | FOLD (evidence-only, one line) | X-W0.j |

**Blocked rows**: X-W0 opens no §1.M bank row and performs no glass cut. It owns the **census event** that evaluates the bank's trigger: CC-109 and CC-116's entire value action is that census (ledger §1.M), so the verdict discharges them PASS or FAIL. The cut rows — CC-003, CC-044, CC-105, CC-106, CC-113, CC-114, CC-115 — stay banked and land at **X-W4.g**, the receiving surface this wave's census fires (R-B).

---

## Triumvirate Dispatch

Mandatory triumvirate (research + plan augment + redress) — the orchestrator may not redispatch the failing unit alone — on any of:

- **File-bound expansion.** X-W0.a's census growing beyond `docs/**` (any untracked path under `src/`, `demo/`, `api/`, `test/`, `e2e/`, `scripts/`, `.github/` entering the track-or-archive set). Product bytes in a formation wave invalidate it.
- **Hard-gate failures not local-edit-recoverable.** HG-7 baseline still `exit 1` after the `frontend-omissions` band receives a real run record; HG-14 graph receipts failing to round-trip on a second run (non-determinism is a tool defect, not an edit).
- **Third diagnostic iteration.** Three attempts at reconciling HG-4's clean-pass contradiction, or three attempts at the HG-8 clean-checkout replay, halt and escalate.
- **Owner-sitting non-occurrence.** If the X-W0.g sitting does not happen on its scheduled date, the wave does not silently re-name the rows (the FM-21 failure); it triumvirates on scheduling.
- **Repin-census PASS, or any cut attempted under it.** A PASS at X-W0.j is a re-plan event: X-W0 records the verdict and fires X-W4.g; performing any part of the atomic cut here — a `watercolor-dot` deletion, a shim, a copied selector — invalidates the wave. Equally: curing a `V·L5` citation by editing a frozen 7e28 copy (it fails HG-9 instead).

---

## File Bounds

| File | Access |
|---|---|
| `docs/tranches/X/waves/W0.md` | modify |
| `docs/tranches/X/W0/TRACK-OR-ARCHIVE.md` | create |
| `docs/tranches/X/W0/PROMPT-RECAP.md` | create |
| `docs/tranches/X/W0/TOMBSTONES.md` | create |
| `docs/tranches/X/W0/OWNER-SITTING-2026-08-XX.md` | create |
| `docs/tranches/X/W0/FRONTEND-DENOMINATOR.md` | create |
| `docs/tranches/X/W0/GRAPH-V3/**` | create |
| `docs/tranches/X/W0/ROUTING-LAW-V-L5.md` | create (CC-011's tombstone path, emitted by X-W11 G1) |
| `docs/tranches/X/W0/GLASS8-REPIN-CENSUS.md` | create |
| `docs/tranches/V/megatranche/workflows/validate-completeness.mjs` | modify-carve |
| `docs/tranches/V/megatranche/workflows/hydrate-reports.mjs` | execute, no write (HG-8's clean-worktree replay + X-W0.c's ledger regeneration) |
| `docs/tranches/V/megatranche/workflows/graph-v3.mjs` | create |
| `docs/tranches/V/megatranche/workflows/validate-constellation-dag.mjs` | modify-carve (the 8 `V.L5` node identifiers only — HG-17) |
| `docs/tranches/V/megatranche/PARSER-RESURRECTION-HANDOFF-2026-07-31.md` | modify-carve (the `:64` routing law only) |
| `docs/tranches/V/megatranche/CONSTELLATION-COMMISSION-2026-08-03.md` | modify-carve (the `:76` routing law only) |
| `docs/tranches/V/megatranche/coordination/PARSER-CSS-PAUSE-HANDOFF-2026-08-02.md` | modify-carve (the `:35` routing law only) |
| `docs/tranches/V/megatranche/coordination/CONSTELLATION-MEGATRANCHE-DAG-2026-07-29.{md,json}` | modify-carve (the `V.L5` DAG edges only) |
| `docs/tranches/V/megatranche/registry/COMPLETENESS-LEDGER.md` | modify (generated) |
| `docs/tranches/V/megatranche/registry/HYDRATION-LEDGER.md` | modify (generated) |
| `docs/tranches/V/megatranche/registry/CARRY-CUT-LEDGER.md` | modify-carve (disposition column + the §0 period-form alias line) |
| `docs/tranches/V/megatranche/formation/codex-worktree-7e28/CENSUS.md` | modify-carve (re-verification block) |
| `docs/tranches/V/megatranche/audit/components/{ActionFeedback,picker-pointerdebugoverlay,picker-componentsliders-consolerail}/**` | modify (X-W0.e's r2 challenge files + `evidence-r2/` probes, git-tracked; no canonical `challenge-*.md` byte is rewritten) |
| `docs/tranches/V/megatranche/audit/codex-provenance/motion-quarantine.md` | modify-append (X-W0.e's HG-11 stamps — the dated addendum only; the landed record's existing rulings are corrected by addition, never rewritten) |
| the 46 REPORT-AUTHORED axis files — the canonical `docs/tranches/V/megatranche/audit/components/<slug>/challenge-<AXIS>-<band>.md` set, r2 files excluded | read, no write (HG-11's subject set — canonical Codex-authored receipts, C-02; every stamp lands in `motion-quarantine.md`) |
| every untracked tree under `docs/` named by the X-W0.a census | create (git-track) or delete (archive) |

**Do NOT touch**: `src/`, `demo/`, `api/`, `test/`, `e2e/`, `.github/`, `package.json`, `vite.config.ts`, `node_modules/`, `~/.codex/**` (read/hash only — M-21 §2, and the same posture for X-W0.j's four census probes over `package.json` + `node_modules/@mkbabb/glass-ui/**`), and `scripts/**` — including `scripts/dev/dev.sh`, whose NEVER-touch posture stands until the X-W0.g sitting rules commit-or-restore (CC-021). Also immutable: the 90 adopted copies under `formation/codex-worktree-7e28/**` (only `CENSUS.md` takes X-W0.d's re-verification block) — a `V·L5` glyph edit inside a copy breaks HG-9's digest re-hash, which is why X-W0.i's live set excludes them by rule. Equally immutable: the 46 canonical REPORT-AUTHORED `challenge-*.md` axis files (C-02 — receipts literal) — X-W0.e adds r2 artefacts *beside* three of them and writes its HG-11 stamps into `motion-quarantine.md`, never into a canonical axis file.

---

## Disjointness

X-W0.a runs **alone and first**: it is the only unit that changes a path's tracked/untracked state, and every other unit reads paths it moves. After X-W0.a commits, the remaining units are disjoint by write path:

- b → `X/W0/PROMPT-RECAP.md`
- c → `workflows/validate-completeness.mjs` + both registry ledgers
- d → `formation/codex-worktree-7e28/CENSUS.md`
- e → `audit/components/<three slugs>/**` (r2 artefacts only) + `audit/codex-provenance/motion-quarantine.md` (the HG-11 stamps, append-only); the 46 canonical axis files are read-only, and no other unit writes the quarantine record
- f → `X/W0/TOMBSTONES.md`
- g → `X/W0/OWNER-SITTING-*.md`
- h → `X/W0/GRAPH-V3/**` + `X/W0/FRONTEND-DENOMINATOR.md` + `workflows/graph-v3.mjs`
- i → `X/W0/ROUTING-LAW-V-L5.md` + the six law-bearing sites (incl. `workflows/validate-constellation-dag.mjs`) + `CARRY-CUT-LEDGER.md` (§0 alias line, CC-011's row)
- j → `X/W0/GLASS8-REPIN-CENSUS.md` only; everything it reads (`package.json`, installed glass bytes) is read-only

c and e both touch `HYDRATION-LEDGER.md` **through the generator, never by hand**: e git-tracks the three slugs' r2 artefacts, c re-runs `hydrate-reports.mjs`. Sequence c after e. The only other shared file is `CARRY-CUT-LEDGER.md`, held by i and g — disjoint by section (i: the §0 alias line + CC-011's row; g: the seven owner rows' disposition cells) and sequenced i (batch 3) before g (batch 4). No two concurrent units share a `modify` path.

---

## Worktree Plan

Docs-only; no build artefacts; no `CARGO_TARGET_DIR`. Batches commit before the next batch dispatches so all agents share clean main.

| Batch | Units | Sibling worktree |
|---|---|---|
| 1 | X-W0.a | none (main) |
| 2 | X-W0.b · X-W0.d · X-W0.e · X-W0.h | none (disjoint paths, main) |
| 3 | X-W0.c · X-W0.f · X-W0.i · X-W0.j | none (main; c depends on e) |
| 4 | X-W0.g | none (main) |

X-W0.j sits in batch 3 so the census verdict is in hand before the owner sitting; a PASS is reported there rather than discovered after it.

One exception: **HG-8** requires an ephemeral `git worktree` of HEAD for the clean-checkout replay. It is created, read, and removed inside X-W0.c; nothing is committed from it.

---

## Agent Units

Seats are **Opus** (M-23: implementation seats Opus). No unit here is design content; the twice-authored Fable ∥ Opus → fresh-Fable apotheosis process belongs to X-W10 (CC-098) and is not performed in this wave.

### X-W0.a Track-or-Archive Disposition

- **Goal**: every byte a tracked document cites is a byte the owner can see.
- **Mechanism**: census `git ls-files --others --exclude-standard docs/` grouped to tree depth 4; rule each tree TRACK, ARCHIVE (move under `docs/tranches/<L>/archive/` and track), or DELETE with a one-line reason; execute in ONE commit.
- **Files**: `docs/tranches/X/W0/TRACK-OR-ARCHIVE.md`; the censused trees.
- **Sub-gate**: HG-1, HG-2.

### X-W0.b The 181-Event Prompt-Recap Canon

- **Mechanism**: re-author from `docs/tranches/V/apotheosis/pi/formation/session-audit/raw-prompts/` (INDEX.json: 83 + 28 + 70 = 181 prompts, 3 archives); one row per event, each carrying a `<archive>:<line>` phrase anchor; reconcile the clean-pass contradiction with a dated ruling marking the loser SUPERSEDED in place; restore the twelve dropped owner asks by name.
- **Goal**: the standing "recap ALL of our prompts — silent drops are forbidden" edict is dischargeable by reading one tracked file.
- **Files**: `docs/tranches/X/W0/PROMPT-RECAP.md`.
- **Sub-gate**: HG-3, HG-4, HG-5.

### X-W0.c Completeness Toolchain Admission and the Challenged Denominator

- **Mechanism**: give the `frontend-omissions` band a real run record so the validator baseline reaches green; make the validator print `CHALLENGED / REPORT-AUTHORED` as separate integers; replace every unqualified `264/264` in live gate text with the split; replay `hydrate-reports.mjs` → `validate-completeness.mjs` in an ephemeral clean worktree and diff.
- **Goal**: a saturation number that cannot be read as challenge coverage, produced by a script that proves it can fail.
- **Files**: `workflows/validate-completeness.mjs`; both registry ledgers; the gate-text sites in `megatranche/*.md`.
- **Sub-gate**: HG-6, HG-7, HG-8.

### X-W0.d Worktree-7e28 Corpus Closure

- **Mechanism**: re-hash all 90 adopted copies (`tail -c <original-bytes> <copy> | sha256sum` = recorded original digest); confirm 126/126 accounting (90 ADOPT-COPY + 36 LEAVE-IN-PLACE); confirm the `PRODUCER-ARTIFACT-BOUNDARY-2026-07-29` collision resolves to two distinct tracked names; append a dated re-verification block.
- **Goal**: the 07-31 "Exact Value authority" pins resolve from the repo of record, permanently.
- **Files**: `formation/codex-worktree-7e28/CENSUS.md`.
- **Sub-gate**: HG-9.

### X-W0.e Axis Re-runs and the Motion Quarantine

- **Mechanism**: track the existing r2 challenge files and `evidence-r2/` probes for `ActionFeedback` L, `picker-pointerdebugoverlay` D and `picker-componentsliders-consolerail` D; re-status their ledger rows away from REPORT-AUTHORED; carry every motion/reduced-motion assertion's stamp — `QUARANTINED-PENDING-TWO-GUARD-CHECK` or a ruling citing both guards by path and line — **in `audit/codex-provenance/motion-quarantine.md`**, the landed per-assertion two-guard record (`9812f951`: 46 files swept, 34 assertions ruled across 16 files, §7 file-level disposition over all 46), extended by dated addendum for any assertion or axis file its sweep did not reach. The 46 report-authored axis files are **read, never written** — see HG-11's rationale.
- **Goal**: no motion claim in the corpus survives without having looked at the two guards that govern it.
- **Files**: `audit/components/{ActionFeedback,picker-pointerdebugoverlay,picker-componentsliders-consolerail}/**` (the r2 challenge files and `evidence-r2/` probes, git-tracked); `audit/codex-provenance/motion-quarantine.md` (modify-append — the HG-11 stamps). **Read-only**: the 46 report-authored axis files.
- **Sub-gate**: HG-10, HG-11.

### X-W0.f The Tombstone Set

- **Mechanism**: one tombstone per RETIRE row — CC-015 (DR-02 blob extirpation, inverted with no tombstone), CC-016 (DR-14 `siblingFsAllowTransient`), CC-017 (DR-15 precepts submodule drift), CC-018 (DR-16 HG6 taste certification), CC-022 (DR-25 PRM-expand at kf 6) — each quoting the original mandate verbatim, pasting the probe and its output, and naming the terminal disposition. CC-019/020/021/023 tombstones are written by X-W0.g after their rulings.
- **Goal**: every retired row dies with its truth recorded, so it cannot re-enter under a new name.
- **Files**: `docs/tranches/X/W0/TOMBSTONES.md`.
- **Sub-gate**: HG-12.

### X-W0.g The Owner Sitting Packet

- **Mechanism**: one scheduled session, dated at wave-open, with a rendered artifact per row. Rows: DR-16 (taste certification), DR-19 (the vnext `proof:` sites, re-measured, before any adoption), DR-20 (the PARK set), DR-24 (`scripts/dev/dev.sh` commit-or-restore), DR-31 (the NCSU 301), DR-14's retirement trigger, and the U-F12 Pole A/B pick. Each returns LANDED-AS-RULED or RETIRED. The DR-19 ruling lands with a grep-checkable structural ban in canon (no `scripts/**/proof-*.mjs`).
- **Goal**: the seven owner-held rows leave this wave decided, not re-named.
- **Files**: `docs/tranches/X/W0/OWNER-SITTING-2026-08-XX.md`; the CC disposition column.
- **Sub-gate**: HG-13.

### X-W0.h Typed Graph Authority and the Frontend Denominator

- **Mechanism**: emit graph v3 with typed multi-domain edges (resolver alias, CSS, render/DI/route/state/API, worker/asset, package/build/test boundary) and SCCs classified runtime / load / ownership separately, per L-17; land the v3 source census (88 SFC · 310 members · 14 routes + wildcard · 13 `:is` · 2 Teleports · 2 harnesses · per-file SHAs) re-verified at wave HEAD as the frontend denominator every later wave cites and none re-derives.
- **L-19 consumer (named, not implied)**: **X-W8** binds CC-078's file bounds and CC-079's four SCC owners (Admin/provider, Markdown, Gradient, Dock) from this output; X-W8 cannot open without it. The graph is not a proof farm; it is X-W8's input.
- **Goal**: architecture is adjudicated from a graph, not from file counts.
- **Files**: `workflows/graph-v3.mjs`; `docs/tranches/X/W0/GRAPH-V3/**`; `docs/tranches/X/W0/FRONTEND-DENOMINATOR.md`.
- **Sub-gate**: HG-14, HG-15.

### X-W0.i The V·L5 Routing-Law Retirement (CC-011)

- **Goal**: no release routing law is keyed to a wave that does not exist, and no identifier escapes a grep by changing glyph.
- **Mechanism**: enumerate at open — `git ls-files docs/ | xargs grep -ln 'V·L5\|V\.L5'`. The **write set is exactly the six law-bearing sites** named in HG-17 (four routing-law statements, the DAG's JSON twin, the executable encoding). Everything else that carries the token is read-only by rule — frozen 7e28 copies (HG-9), the two `codex-provenance` records that quote the defect as its own proof, the dated 07-29 cross-repo records and harvest/reconciliation JSON that *restate* a law they do not mint (epoch rule), and this tranche's own records. Rewrite each law-bearing site against `V·L1..V·L4` plus one **explicitly defined new cut** — X-W9 (parser and library apotheosis) → X-W11 (release/rebind). Normalize surviving identifiers to the middot form and register the period form as a **search alias** in `CARRY-CUT-LEDGER.md` §0 (L-5: an alias is a search key, never a rename — CC-011 and V·L1..V·L4 all keep their original IDs). Write the tombstone: the minted law quoted verbatim, the no-definition probe pasted, the terminal disposition named.
- **Files**: `docs/tranches/X/W0/ROUTING-LAW-V-L5.md` (the path X-W11 G1 emits for CC-011); the six law-bearing sites named in HG-17; `CARRY-CUT-LEDGER.md` §0 + CC-011's row.
- **Sub-gate**: HG-17.

### X-W0.j The Glass-8 Repin Census Event (CC-003's trigger · CC-109 · CC-116 · CC-117)

- **Goal**: the trigger gating eleven banked rows is evaluated by a named unit on a dated run, so "when the trigger fires" stops being a pointer with no owner.
- **Mechanism**: run CC-003's four machine-checked conditions read-only against installed bytes (HG-18), recording each condition's measured value beside its PASS/FAIL and one verdict word for the census. On **FAIL** the bank stays shut, X-W4.g stays closed, and this verdict is what X-W11's release table transposes. On **PASS** the census emits the fire notice that opens **X-W4.g** — the trigger-gated receiving surface that lands CC-044 + CC-003 + the W6/W7/W8 banked limbs as ONE atomic cut (no second migration, no interim wrapper, no copied selector). X-W0 records the verdict and performs no part of the cut. **CC-109 and CC-116** are discharged by the verdict itself — the ledger §1.M gives their value action as exactly this census, and the 31/31 receipt verdicts stand as received. **CC-117's terminal word, stated once**: evidence-only, **FOLD → coordination INBOX row I-24**, no value act and no X wave row; X-W11 G1's 117-row walk reads it from this line.
- **Files**: `docs/tranches/X/W0/GLASS8-REPIN-CENSUS.md`. Read-only: `package.json`, `node_modules/@mkbabb/glass-ui/**`.
- **Sub-gate**: HG-18.

---

## Hard Gate

All baselines measured read-only on **2026-08-03** at branch `tranche-u`. `MEASURE-AT-OPEN` means the probe is defined but its baseline is taken at wave-open, not fabricated here. Each gate states **how it fails for its intended reason** (L-19).

**HG-1 — zero untracked bytes under `docs/`** (CC-012)
`git ls-files --others --exclude-standard docs/ | wc -l` → `0`.
**BORN-RED (measured)**: `1858` — `docs/tranches/V/apotheosis` 1798 · `docs/tranches/V/megatranche` 33 · `docs/tranches/W/audit` 16 · `docs/tranches/T/audit` 11.
*Falsifier*: leave any tree undecided, or add one untracked doc file, and the count is nonzero.

**HG-2 — one disposition per censused tree** (CC-012)
`docs/tranches/X/W0/TRACK-OR-ARCHIVE.md` carries exactly one TRACK|ARCHIVE|DELETE row per tree in the open-time census (`git ls-files --others --exclude-standard docs/ | cut -d/ -f1-4 | sort -u`), each with a reason.
**BORN-RED (measured)**: `ls docs/tranches/X` → ABSENT at HEAD; the file does not exist.
*Falsifier*: drop a tree from the ledger and the row-set no longer equals the census.

**HG-3 — the 181-event corpus is tracked and anchored** (CC-013)
`git ls-files docs/tranches/V/apotheosis/pi/formation/session-audit/raw-prompts/ | wc -l` → `4`; `X/W0/PROMPT-RECAP.md` has 181 event rows, each anchor resolving to a real `<archive>:<line>`.
**BORN-RED (measured)**: `git ls-files … | wc -l` → **0** (the corpus is untracked); `grep -cE '\bE(1[01]|[1-9])\b' docs/tranches/V/vnext/PROMPT-RECAP.md` → **0** (zero E1–E11 occurrences in the incumbent recap).
*Falsifier*: strip an anchor from any row and anchor resolution fails for that row.

**HG-4 — the clean-pass contradiction is reconciled** (CC-013)
One dated row names which reading is canon and marks the loser SUPERSEDED in place.
**BORN-RED (measured)**: both live — `docs/tranches/V/vnext/PROMPT-RECAP.md:88` reads "Whole-formation credit is **0/2**" while `docs/tranches/V/vnext/FORMATION-CLEAN-PASSES.json` reads `"status":"clean"` with two passes and all six seats `"verdict":"CLEAN"`.
*Falsifier*: leaving both readings unannotated leaves the contradiction greppable.

**HG-5 — the twelve dropped owner asks are restored by name** (CC-013)
Twelve named rows — internal-Browser edict · Aristotelian proportion · precepts binding · the lightningcss/sonic-rs bench targets · ARM-only · the 3×5×3 law · and the six the open-time re-derivation names — each present in tracked canon with a phrase anchor.
**BORN-RED**: DR-30's finding stands — twelve asks survive only in read-only or untracked trees. Per-phrase baseline **MEASURE-AT-OPEN** (one `grep -c` per phrase over `git ls-files`-tracked docs); do not carry DR-30's count forward unmeasured.
*Falsifier*: a listed ask with no anchor, or an anchor into an untracked path.

**HG-6 — the challenged denominator gates, not the saturation count** (CC-001 / C-04)
`validate-completeness.mjs` prints CHALLENGED and REPORT-AUTHORED as separate integers summing to 264; zero unqualified `264/264` remains in live gate text.
**BORN-RED (measured)**: the split is derivable from `HYDRATION-LEDGER.md` today — 213 EXISTS-ORIGINAL + 5 UNWITNESSED-DIRECT = **218 CHALLENGED**, **46 REPORT-AUTHORED**, 264 canonical rows — but `grep -c 218` → **0** in both `COMPLETENESS-LEDGER.md` and `validate-completeness.mjs`, and the live gate text still reads unqualified: `COMPLETENESS-LEDGER.md:20,22,171`, `CONVERGENCE-RESUME-HANDOFF-2026-07-29.md:68`, `IN-FLIGHT-RESUME-HANDOFF-2026-07-29.md:49`.
*Falsifier*: re-badge one REPORT-AUTHORED row as challenged and the two integers stop reconciling against the ledger's own status column.

**HG-7 — the validator's self-test is meaningful** (CC-025)
`node docs/tranches/V/megatranche/workflows/validate-completeness.mjs --self-test` reaches a GREEN baseline and rejects all six synthetic violations S1..S6.
**BORN-RED (measured, verbatim)**:
```
baseline (unmutated tree): exit 1 · 1 uncovered · 264/264 hash-banked
BASELINE IS NOT GREEN — self-test is meaningless until the real tree passes. Fix the tree first.
```
The one uncovered band is `frontend-omissions` / `UNASSIGNED-FRONTEND-OMISSIONS` — `COMPLETENESS-LEDGER.md:135` "record: no record · coverage: UNCOVERED", while the same file's line 171 headline reads "**GREEN — zero incomplete components.**" A ledger whose headline reads GREEN while its own generator exits 1.
*Falsifier*: this gate is already the falsifier — the harness proves it can fail before it is allowed to certify.

**HG-8 — the GREEN is regenerable from a clean checkout** (CC-025 / K-35)
In an ephemeral `git worktree` of HEAD: `node hydrate-reports.mjs && node validate-completeness.mjs` reproduces both committed ledgers (`git diff --exit-code` inside the worktree).
**Baseline**: K-35 measured 264× `NO-LEDGER`, exit 1, from a clean checkout at `87f56f11`. At HEAD the defect looks cured — the committed `hydrate-reports.mjs` already writes 64-hex hashes and the `REPORT-AUTHORED`/`UNWITNESSED-DIRECT` statuses (`git show HEAD:…/hydrate-reports.mjs`, lines 53/178/196/213), and `git status --porcelain -- registry workflows` is empty. **MEASURE-AT-OPEN**: run the replay and record the result; do not assert cured without it.
*Falsifier*: narrow the ledger hash width back to 16 and every axis becomes `NO-LEDGER`.

**HG-9 — the 7e28 corpus is closed and byte-exact** (CC-026 / C-11)
126/126 files accounted (90 ADOPT-COPY + 36 LEAVE-IN-PLACE); all 90 copies re-hash to their recorded original digests; the `PRODUCER-ARTIFACT-BOUNDARY-2026-07-29` collision holds two distinct tracked names; `~/.codex/**` unmodified.
**GREEN-AT-AUTHORING (measured)**: `find docs/tranches/V/megatranche/formation/codex-worktree-7e28 -type f | wc -l` → **91** (90 copies + `CENSUS.md`), all tracked; the collision is resolved as `coordination/PRODUCER-ARTIFACT-BOUNDARY-2026-07-29.md` vs `formation/codex-worktree-7e28/coordination/PRODUCER-ARTIFACT-BOUNDARY-2026-07-29.WORKTREE-VARIANT.md`; source worktree still holds 3,967 files, unmodified. This gate stands as the wave's **re-verification and regression check**, not a cure.
*Falsifier*: any copy whose `tail -c <original-bytes>` digest drifts from `CENSUS.md` §2.

**HG-10 — the three axes are witnessed, not report-authored** (CC-027)
The r2 challenge files and `evidence-r2/` probes for `ActionFeedback` L, `picker-pointerdebugoverlay` D, `picker-componentsliders-consolerail` D are tracked, and their ledger rows carry a witnessed status.
**BORN-RED (measured)**: the r2 artifacts exist on disk but are **untracked** — all 33 untracked files under `docs/tranches/V/megatranche/` are exactly these three slugs' r2 challenge files and evidence probes; the ledger still carries 46 REPORT-AUTHORED rows.
*Falsifier*: a named slug whose row stays REPORT-AUTHORED, or whose evidence stays untracked, fails the same check twice.

**HG-11 — every motion assertion is quarantined or two-guard-cleared, in the quarantine record** (CC-027)
Each motion/reduced-motion assertion across the 46 report-authored axes carries `QUARANTINED-PENDING-TWO-GUARD-CHECK` or a CLEARED verdict citing both guards by path and line — **in `docs/tranches/V/megatranche/audit/codex-provenance/motion-quarantine.md`**, keyed to the assertion's `<axis file>:<line>`, with all 46 axis files present in the record's file-level disposition (including the vacuously-cleared ones), so coverage is checkable by set-difference against `HYDRATION-LEDGER.md`'s 46 REPORT-AUTHORED rows. The axis files are read, never written: **the 46 axes are canonical Codex-authored files — receipts literal, never rewritten (C-02) — so corrections live in separate dated artifacts, which is exactly what the quarantine record is.**
**BORN-RED (measured at authoring)**: zero quarantine markers existed in the corpus; K-6 recorded 12 of 13 raisers never checking gating, with `ActionFeedback` L:12 and `picker-pointerdebugoverlay` D#4 materially false. Both guards verified present at HEAD: `demo/styles/animations.css:184` `@media (prefers-reduced-motion: reduce)` (second at :202) and `node_modules/@mkbabb/glass-ui/dist/styles/utilities/a11y-overrides.css`.
**CURED-IN-PART (measured 2026-08-03, re-measure at open)**: the record landed and is tracked at `9812f951` — 46 files swept, 34 assertions ruled across 16 files (14 TRUE · 15 FALSE · 5 UNPROVEN), §7.1–§7.5 disposing of all 46, both guards enumerated as served bytes with sha256s and four re-runnable probes. The residual RED is the coverage set-difference and any assertion the sweep did not reach; X-W0.e closes it by dated addendum, never by editing an axis file.
*Falsifier*: an assertion with neither marker nor a two-guard citation in the record — or one of the 46 axis files absent from its disposition. A stamp written into an axis file instead fails the C-02 write-ban above (§4) and is not a pass.

**HG-12 — nine tombstones, nine pasted probes** (CC-015..CC-023)
`docs/tranches/X/W0/TOMBSTONES.md` carries one entry per RETIRE row: original mandate quoted verbatim, probe pasted with its output, terminal disposition named.
**BORN-RED (measured 2026-08-03)**:

| row | probe | current output |
|---|---|---|
| CC-015 / DR-02 | `grep -rn "glass-ui/blob" demo/` | 5 hits — `picker/visual/HeroBlob.vue:34`, `scenes/blob/BlobPane.vue:12`, `color-picker/composables/boot/useAtmosphere.ts:36` (+2 type imports): the extirpation mandate is inverted, still no tombstone |
| CC-016 / DR-14 | `grep -n siblingFsAllowTransient vite.config.ts` | `139`, `287` — carve-out live; `package.json:83` pins `^7.0.0`, installed **7.0.0**, six majors past the D-era trigger |
| CC-017 / DR-15 | `git submodule status docs/precepts` ⧸ same in `../keyframes.js` | `63240e67…` vs `8ccf9f4d…` — divergent, on SHAs neither of the historically cited pair |
| CC-018 / DR-16 | `sed -n 30p docs/tranches/T/audit/w8-certification/VERDICT-2026-07-12.md` | `> _(empty — the owner's verdict lands here)_` — still the placeholder across three closes |
| CC-022 / DR-25 | `node -p "require('./node_modules/@mkbabb/keyframes.js/package.json').version"` | **6.0.0**, pin `^6.0.0` — the named retirement gate ("their next tag past v5.2.0") FIRED and no close observed it; the springPlay PRM re-probe is owed |
| CC-019 / DR-19 | `grep -rn "proof:" docs/tranches/V/vnext/ \| wc -l` ⧸ `ls scripts/**/proof-*.mjs` | **69** in vnext (the ledger's carried figure was 81 — record the re-measure, FM-22) ⧸ **0** in `scripts/` — the library band is structurally clean; the vnext ruling is owed |
| CC-020 / DR-20 | `grep -rn "Color.try" src/ demo/ test/ \| wc -l` | **0** — the trigger metric is now zero in both directions; ruling owed |
| CC-021 / DR-24 | `git status --porcelain -- scripts` | ` M scripts/dev/dev.sh` — still dirty, still unowned |
| CC-023 / DR-31 | NCSU alias HTTP status | **MEASURE-AT-OPEN** (VPN-gated; do not fabricate). The ruling — accept the permanent 301 in canon or remove it on a dated host session — does not depend on the probe |

*Falsifier*: a tombstone whose pasted probe does not reproduce when re-run at wave close.

**HG-13 — the owner sitting returns a verdict per row** (CC-014 / DR-29)
Seven rows — DR-16, DR-19 (vnext `proof:` sites), DR-20, DR-24, DR-31, DR-14's trigger, U-F12 Pole A/B — each returning LANDED-AS-RULED or RETIRED, dated, artifact attached.
**BORN-RED**: 0/7 ruled; DR-29 records these attested zero times while their gating events fired twice (glass v5.0.0 2026-07-15, v7.0.0 2026-07-17).
*Falsifier*: a row returning "banked", "pending", or "carried" is not a verdict — the gate reads the verdict token, not the prose.

**HG-14 — typed graph v3 round-trips and names X-W8's SCCs** (CC-001 / L-17)
`node workflows/graph-v3.mjs` emits typed multi-domain edges and runtime/load/ownership SCC classes; a second run reproduces identical node/edge/SCC digests; the four frontend SCCs (Admin/provider, Markdown, Gradient, Dock) appear by name with an owner each.
**BORN-RED (measured)**: no graph-v3 artifact exists — the only graph document under `megatranche/` is the prose `audit/cross-repo/CONSTELLATION-GRAPH-SOL-AUDIT-2026-07-29.md`; `MECHANISM-CUT-FORMATION-MAP-2026-07-29.md:126` records "graph lacks typed multi-domain edges and honest SCC classes".
*Falsifier*: remove one resolver-alias edge and either a named SCC disappears or the round-trip digest changes — the alias-aware rerun that moved keyframes demo edges 245 → 399 is the precedent this must be able to catch.

**HG-15 — the v3 census is the tracked frontend denominator** (CC-024 / K-24)
`docs/tranches/X/W0/FRONTEND-DENOMINATOR.md` carries the census re-verified at wave HEAD — 88 SFC · 310 members · 14 routes + wildcard · 13 `:is` · 2 Teleports · 2 harnesses · per-file SHAs — and every later frontend wave cites it rather than re-deriving.
**BORN-RED**: the census is adopted (provenance row 21, CONFIRMED exact) but lives only in `coordination/CONSTELLATION-FRONTEND-PREEXECUTION-SOURCE-CONTRACT-2026-08-03.md`; no denominator file exists under `docs/tranches/X/`. Spot-check re-verified at authoring: `find demo -name '*.vue' | wc -l` → **88**, exact.
*Falsifier*: a per-file SHA that no longer matches live bytes marks the census stale rather than letting a stale denominator gate a later wave.

**HG-16 — COMPLETABLE is present in every X wave file** (C-06)
`grep -L 'COMPLETABLE' docs/tranches/X/waves/W*.md` → empty, over the set present at X-W0 close.
**BORN-RED (measured)**: `ls docs/tranches/X` → ABSENT at HEAD; no wave file carried the field.
*Falsifier*: author or amend any X wave without the field and the grep names it.

**HG-17 — the V·L5 routing law is retired against defined waves** (CC-011 / C-13)
No law-bearing site survives: each reads `V·L1..V·L4 → X-W9 → X-W11`, and `node docs/tranches/V/megatranche/workflows/validate-constellation-dag.mjs` runs with **zero** `V.L5` node identifiers. Residual token hits are legal only in the read-only classes — frozen 7e28 copies, the two `codex-provenance` proofs, the dated 07-29 restatement/evidence records, X-tranche records, the tombstone, and the ledger's §0 alias line.
**BORN-RED (measured)**: **35** tracked files carry the token — 19 frozen 7e28 copies, 2 `codex-provenance` proofs, 2 X-tranche records, **12 live V-tranche sites**. Of the twelve, **six are law-bearing** — the four statements (`PARSER-RESURRECTION-HANDOFF-2026-07-31.md:64` · `CONSTELLATION-COMMISSION-2026-08-03.md:76` · `coordination/PARSER-CSS-PAUSE-HANDOFF-2026-08-02.md:35` · `coordination/CONSTELLATION-MEGATRANCHE-DAG-2026-07-29.md:78,80,87,174,176,178`), that DAG's JSON twin, and the executable encoding (`workflows/validate-constellation-dag.mjs`, **8** `V.L5` node identifiers) — and six are restatement/evidence records that take the alias, not an edit. The wave is undefined: `grep -c 'V·L5\|V\.L5' registry/adjudicated/layout-gestalt.md` → **0**, against `STATE.md:88` "four born-RED waves **V·L1..V·L4**".
*Falsifier*: re-glyph one `V·Ln` to the period form without registering the alias and the alias probe names the file — that re-glyphing is exactly how the identifier escaped a grep-based ledger the first time. Curing a citation inside a 7e28 copy instead fails HG-9's digest re-hash.

**HG-18 — the Glass-8 repin census returns a dated four-condition verdict** (CC-003's trigger / CC-109 / CC-116)
Four read-only probes, each pasted with its measured value; the census PASSES only if all four pass:
1. `node -p "require('./node_modules/@mkbabb/glass-ui/package.json').version"` → major **≥ 8**;
2. `node -p "Object.keys(require('./node_modules/@mkbabb/glass-ui/package.json').exports).filter(k=>/watercolor/.test(k)).join(',')||'ABSENT'"` → `ABSENT`;
3. the TR#84 consumer-painted indicator slot present in `dist/**/*.d.ts` declarations **and** in the packed exports;
4. the Glass 8.0.0 receipt names its keyboard / orientation / motion contract.
**FAIL-AT-AUTHORING (measured, 0/4)**: (1) installed **7.0.0**, pin `^7.0.0`; (2) `./watercolor-dot` **present** in packed exports; (3) `grep -rn indicator node_modules/@mkbabb/glass-ui/dist --include='*.d.ts' | grep -i slot` → **0**; (4) no 8.0.0 receipt exists. X-W4.g stays closed and the eleven §1.M rows stay banked. **MEASURE-AT-OPEN and again at close** — the verdict is dated, never inherited.
*Falsifier*: pass the census on a partial condition — major 8 while `./watercolor-dot` still ships — and probe 2 prints the subpath, naming the failure. A census recorded without its four measured values is not a verdict; a PASS that does not fire X-W4.g leaves the cut ownerless, which is the disease this gate exists to kill.

---

## Format And Lint Cadence

Docs-only wave. At each batch boundary and before close:

- `git diff --check` over wave-owned paths (whitespace/conflict markers);
- `node docs/tranches/V/megatranche/workflows/validate-completeness.mjs --self-test` (X-W0.c's own gate, run at every batch after batch 2);
- `node docs/tranches/V/megatranche/workflows/graph-v3.mjs --verify` round-trip (X-W0.h);
- `node docs/tranches/V/megatranche/workflows/validate-constellation-dag.mjs` after batch 3 — X-W0.i's node rewrite must leave the DAG runnable (HG-17);
- L-20 precept check: this file and every sibling X wave file re-validated against `docs/precepts/instructions/tranche/WAVE_SPEC.md` at authoring, at amendment, and at each L-18 pass.

`npm run lint` / `vue-tsc` are **intentionally skipped** — this wave writes no `.ts`, `.vue`, or `.css` byte. Replacement evidence: the three script gates above plus `git diff --check`.

---

## Verification Artefacts

Banked under `docs/tranches/X/artefacts/W0/`:

- `census-open.txt` / `census-close.txt` — the HG-1 untracked listings before and after;
- `selftest-open.txt` / `selftest-close.txt` — verbatim `--self-test` output both sides;
- `clean-checkout-replay.txt` — the HG-8 ephemeral-worktree diff result;
- `7e28-reverify.txt` — 90 copy digests re-hashed;
- `graph-v3/` — nodes, edges, SCC classes, and the two round-trip digests;
- `tombstone-probes.txt` — every HG-12 probe re-run at close;
- `vl5-sites-open.txt` / `vl5-sites-close.txt` — the HG-17 live-set enumeration before and after, with the frozen/provenance exclusions listed;
- `glass8-census-2026-08-XX.txt` — the four HG-18 probes verbatim with their outputs and the verdict word;
- `owner-sitting-2026-08-XX.md` — the signed verdict sheet, seven rows;
- the close commit hashes, named in this file at close.

---

## Commit Plan

| # | Scope | Contents |
|---|---|---|
| 1 | `docs(x-w0/track-or-archive)` | the single track-or-archive commit + `TRACK-OR-ARCHIVE.md` (body required: why, what, per-tree disposition counts) |
| 2 | `docs(x-w0/prompt-recap)` | the 181-event tracked recap + the clean-pass supersession ruling (body required) |
| 3 | `docs(x-w0/7e28-census)` | the re-verification block |
| 4 | `docs(x-w0/axes-r2)` | the three re-run axes + the motion quarantine stamps |
| 5 | `chore(megatranche/completeness)` | validator denominator split + regenerated ledgers (generated-output commit, body required) |
| 6 | `docs(x-w0/tombstones)` | the five executable tombstones |
| 7 | `feat(megatranche/graph-v3)` | `graph-v3.mjs` + `GRAPH-V3/**` + `FRONTEND-DENOMINATOR.md` (body required: mechanism, X-W8 consumer, round-trip evidence) |
| 8 | `docs(x-w0/routing-law)` | the V·L5 retirement: rewritten live laws, the DAG node identifiers, the §0 period-form alias, `ROUTING-LAW-V-L5.md` (body required: the live set, and why the frozen corpus is untouched) |
| 9 | `docs(x-w0/glass8-census)` | `GLASS8-REPIN-CENSUS.md` — four measured conditions, dated verdict, the X-W4.g disposition (body required) |
| 10 | `docs(x-w0/owner-sitting)` | the seven rulings, the four ruled tombstones, the CC disposition-column updates (body required) |
| 11 | `docs(x/w0 close)` | status commit: this file's verb line advanced to **IMPLEMENTED** (R-A — VERIFIED is stamped only at X-W11's release close), artefact paths, hashes |

Pathspec commits only. `scripts/dev/dev.sh` is never included in any of the eleven (commit 10 may record its ruling; the file itself moves only under an owner instruction).

---

## Dependencies

- **Depends on**: tranche X open. Nothing else — no prior X wave, no producer release, no external repo (X-W0.j reads installed bytes and returns a verdict at either polarity; it does not wait on glass 8.0.0).
- **Blocks**:
  - **X-W4** — X-W4.g, the trigger-gated receiving surface, opens only on HG-18's census PASS; a FAIL keeps it closed and the §1.M bank intact. X-W4's glass-independent partition is unaffected either way;
  - **X-W11** — G1's 117-row walk reads CC-011's tombstone path (X-W0.i) and CC-117's terminal word (X-W0.j) from this wave; it authors neither;
  - **X-W8** — cannot bind CC-078 file bounds or CC-079 SCC owners without HG-14's graph and HG-15's denominator;
  - **X-W1** — CC-032/CC-054 need the u-gestalt frame evidence tracked by HG-1 before it can be a born-RED baseline;
  - **X-W9** — CC-088's ninth carry cannot be authorized until HG-13 returns the DR-21 opt-out ruling;
  - **X-W10** — CC-018's optional fresh bracket set exists only if HG-13 rules taste certification still wanted;
  - **every X wave** — HG-16's COMPLETABLE check and HG-6's denominator law bind all of them.

---

## Archaeology

This wave revisits V·MT0 (`MECHANISM-CUT-FORMATION-MAP-2026-07-29.md:97`), which was authored, gated on `264/264`, and never opened — FORMATION-BLOCKED. Failure mode: its hard gate measured file existence and hash banking and was read as challenge saturation (C-04 / K-3), so the number could go green while 46 axes had zero commands run. **New guardrail**: HG-6 forbids the unqualified figure in gate text and HG-7 requires the generator to demonstrate it can fail before it is allowed to certify. Second revisit: DR-29's owner packet, named at two closes and attested zero times. Failure mode was scheduling, not judgment. **New guardrail**: HG-13 reads a verdict token, and the non-occurrence of the sitting is a triumvirate trigger rather than a re-naming. Third revisit: the 8.0.0 repin census, cited as the trigger for eleven banked rows across five wave files and owned by none — "the receiving wave" was a pointer with no referent, the same shape as the `V·L5` law keyed to a wave that was never defined. **New guardrail**: X-W0.j is the census event with a dated verdict and X-W4.g is the surface it fires; a FAIL is a result, not a deferral.

---

## L-18 rider — this wave is not ACCEPTED at close

Landing X-W0's gates makes it IMPLEMENTED, not ACCEPTED. Per **L-18** (owner, 2026-08-03), the implementation must survive **two challenging gestalt passes** — each a **quartet of Opus 5 skeptics** working three altitudes (total-tranche: was X-W0 the optimal opening in the greater plan; wave; feature), each assuming the work is wrong on some reasonable basis, reporting **defects and superlatives** with provenance and proof — **adjudicated and agglomerated into an apotheosis by a singular fresh Fable instance**. Any dissonance surfaced is addressed, ratified, and dispatched; nothing is parked. Bases with obvious purchase here: a track-or-archive that tracks noise instead of authority; a recap re-authored with anchors that do not carry meaning; a graph tool that is a proof farm wearing an architecture label (L-19); tombstones that quote mandates without killing them. Reference, not ceremony — the passes are scheduled with the X-W1 open.

---

## §ADDENDUM 2026-08-30 (X-union repair round 1) — X-W0.j's VERDICT IS WIDENED TO THE REAL OPTION SET: **8.0.0 AND 9.0.0**

**Written per X-union SEAMS.md S-11 cure, 2026-08-30.** Authority: `docs/tranches/X/union/SEAMS.md`
§3 conviction **S-11** + the ledger row it produced, `docs/tranches/V/coordination/INBOX.md` **I-30**
(the 2026-08-29 glass O-20 ACK, rowed 2026-08-30).

**E-3 posture — read first.** This is an **addendum-beside**. **§Agent Units X-W0.j and §Hard Gate
HG-18 are NOT rewritten**; their bytes, their four conditions, their falsifier and their
FAIL-AT-AUTHORING measurement all stand exactly as authored 2026-08-03. **No condition is added,
removed, re-weighted or re-staged; the gate is not re-baselined.** What is widened is the
**candidate target set the census evaluates over** — 8.0.0 was the only major in existence when
HG-18 was written, and a second now exists. Status stays `planned`; **no product source is opened,
no verdict is issued here, and the owner's begin-word remains the only execution key.**

### The fact this wave had not been told

Glass-ui cut **9.0.0** on 2026-08-29 and told us in the O-20 ACK (I-30). Its bytes, quoted:

> *"**9.0.0 is CUT**: tag `v9.0.0` at `d4f7b24f`, pushed. `MIGRATION.md` §9.0.0 is the re-read
> datum — `./search` removed whole …, `./canvas` gone, the 8.1.0 dark-sync fold riding the same
> section."*

and, in the same letter, the publish-state disclosure that changes what "installed" can mean:

> *"the npm registry PUT is currently walled by the account-wide token restriction npm began
> enforcing … **every content claim in this letter is tag-pinned, not registry-pinned**."*

X-W0.j was authored against a world whose only candidate major was 8.0.0. **Left unwidened, the
census would return a dated verdict over a stale option set** — it would measure `major ≥ 8` against
an installed tree and never record *which* major the repin was electing, nor that one of the two
candidates is currently unreachable by `npm install`.

### The widening, stated exactly

1. **The census enumerates BOTH candidate targets.** X-W0.j records its four measured conditions
   **per candidate** — **8.0.0** (`v8.0.0` @ `17a11bc5`, registry-published, the source of
   §EXTERNAL X-EXT-1..6) **and 9.0.0** (`v9.0.0` @ `d4f7b24f`, **tag-pinned, registry-walled** at
   the ACK's date) — and its one verdict word names **which target it is a verdict about**. A
   census that returns PASS without naming its elected target is not a verdict, for the same reason
   HG-18 already says *"A census recorded without its four measured values is not a verdict"*.
2. **HG-18 condition 1 is unchanged in text and gains a reading note.** `major ≥ 8` is satisfied by
   either candidate; **satisfying it does not elect one**. The elected target is a recorded
   datum of the census, not an inference from the predicate.
3. **HG-18 condition 4 — "the Glass 8.0.0 receipt names its keyboard / orientation / motion
   contract" — is READ AS TARGET-RELATIVE**: the receipt required is the receipt **of the elected
   target**. If the census elects 9.0.0, the datum is `MIGRATION.md` §9.0.0 (the ACK names it *"the
   re-read datum"*); if it elects 8.0.0, the 8.0.0 receipt is required as authored. **Neither
   substitutes for the other**, and an 8.0.0 receipt does not discharge a 9.0.0 election.
4. **A NEW MEASURED DATUM, RECORDED BESIDE THE FOUR — not a fifth condition.** For each candidate
   the census records whether the target is **registry-resolvable** or **tag-only**:
   `npm view @mkbabb/glass-ui@<target> version` (resolves / 404) beside
   `git -C ../glass-ui tag --list v<target>`. **This does not gate the census** — HG-18 stays a
   four-condition gate and its falsifier is untouched. It exists so that an owner electing 9.0.0 is
   **electing a tag and knows it**, and so that a `npm install` step planned against a walled PUT
   is caught at the census rather than at the repin.
5. **Election is the owner's, and NOT-YET is a complete result.** As X-W0.j already states, *"a FAIL
   is a complete result, not a deferral"*. This addendum adds the third polarity explicitly:
   **electing neither target — recording both candidates measured and the repin deferred — is also
   a complete, dated result**, and leaves X-W4.g closed and the eleven §1.M rows banked exactly as
   the FAIL branch does.

### What does NOT move

- **X-W4.g's trigger relationship is unchanged**: one atomic trigger-gated cut, one receiving
  surface, no interim wrapper, no second migration, no copied selector (S-24 adjudicated
  CONSISTENT). The atomic-cut law governs **whichever** target the census elects — per COHESION
  **§0g.3**, which re-scopes that law to value.js and leaves its in-repo atomicity unweakened.
- **The FAIL-AT-AUTHORING measurement (0/4, 2026-08-03) stands as authored** and is not re-taken
  here. **MEASURE-AT-OPEN and again at close** is unchanged and now applies per candidate.
- **§EXTERNAL's six rows keep their ids.** X-EXT-1..6 are I-28-sourced (8.0.0) and remain so; the
  9.0.0 successor facts are recorded **beside** them by a dated note at
  `REFINEMENT-FOLD-2026-08-28.md` §EXTERNAL — **X-EXT-1 gains the `./search` whole-removal fact**
  as the successor to its `SearchBar` deletion. **No id is minted, re-homed or retired**, and
  X-EXT-6's ownership by this wave (the peer `vue-component-type-helpers ^3.0.3` install step) is
  unaffected by which target is elected.
- **No sibling sub-tranche is bound by this addendum.** X·KF's pin is `KF-W0` §B-12's; X·F's is
  `F-W1` G1/ESC-1's; neither creates an edge into X-W0.j (COHESION §0g.3).

---

## §CLOSE 2026-09-17 — X-W0 IMPLEMENTED (status commit, §Commit Plan row 11)

**E-3 posture.** This is a dated **addendum-beside**, written by the wave's VERIFY-ONLY close seat.
**Not one byte of §State..§ADDENDUM above is rewritten** except the two status lines §Commit Plan row
11 expressly instructs this commit to advance. No gate is re-baselined, no born-RED measurement
re-taken, no condition added or removed.

**Record of record**: `docs/tranches/X/execution/A/X-W0.md` §Close — the full gate table
BEFORE→AFTER at the close seat's own clock, the commit roster, the fifteen escalations, the three
landed-wrong, the E13 sweep and the fifteen residuals with named owners.

### Gates at close — re-run by the close seat, never inherited

**17 of 18 hard gates GREEN · 8 of 8 fold gates GREEN · HG-7 RED (escalated) · HG-10 SPLIT.**

GREEN: HG-1 (live absolute **0**, double-run) · HG-2 (3 rows ≡ 3 trees) · HG-3 (corpus **4** tracked;
**181** rows; **181/181** anchors resolve) · HG-4 (dated ruling, P canon / J SUPERSEDED) · HG-5
(**13/13** anchored) · HG-6 (unqualified `264/264` → **0** at all five live sites) · HG-8 (ephemeral
worktree of HEAD: `diff --exit-code` **0**, double-run) · HG-9 (**90/90** re-hashed MATCH, 0 DRIFT,
126/126) · HG-11 (coverage set-difference **0**; marker **8**) · HG-12 (**9/9** probes reproduce) ·
HG-13 (**7/7** verdict tokens) · HG-14 (`ROUND-TRIP GREEN`, five digests MATCH twice; four SCCs owned
by X.W8.c) · HG-15 (**88/88** per-file SHAs match live bytes) · HG-16 (12 wave files, **0** missing) ·
HG-17 (**0** tokens at all six law-bearing sites; **0** `V.L5` node ids; 58 residual carriers all in a
legal read-only class) · HG-18 (four conditions re-measured **1/4 → FAIL** at the elected target
**8.0.0**; X-W4.g stays CLOSED, the §1.M bank stays shut) · G-A · G-B · G-C · G-D · G-E · G-F
(RATIFIED by GF-R3) · G-G · G-H.

**RED — HG-7**, escalated as **E-1**: baseline `exit 1 · 87 uncovered`. The named cure is **not
executable at the bytes** (the `frontend-omissions` band contributes 0 of the 87 and has no run record
to give); the 87 are non-band run records belonging to four other tranche-X tracks; both candidate
cures lie outside X-W0.c's writable set. **No substitute was written.**

**SPLIT — HG-10**, returned as **E-2**: the GREEN definition is met (r2 files tracked; *their* ledger
rows read `EXISTS-ORIGINAL` with harvest), the falsifier's literal clause is not (the three canonical
rows still read `REPORT-AUTHORED`, truthfully so).

### Artefact paths landed — eight, all tracked, each `SERVED MODEL:` on line 1

| path | bytes |
|---|---:|
| `docs/tranches/X/W0/TRACK-OR-ARCHIVE.md` | 26,917 |
| `docs/tranches/X/W0/PROMPT-RECAP.md` | 60,284 |
| `docs/tranches/X/W0/TOMBSTONES.md` | 67,918 |
| `docs/tranches/X/W0/OWNER-SITTING-2026-09-17.md` | 41,739 |
| `docs/tranches/X/W0/GLASS8-REPIN-CENSUS.md` | 53,103 |
| `docs/tranches/X/W0/ROUTING-LAW-V-L5.md` | 26,471 |
| `docs/tranches/X/W0/FRONTEND-DENOMINATOR.md` | 26,309 |
| `docs/tranches/X/W0/GRAPH-V3/{nodes,edges,sccs,census,digests}.json` | 983,886 |

Plus, outside `X/W0/`: `workflows/graph-v3.mjs` (created) · `workflows/validate-completeness.mjs` +
`hydrate-reports.mjs` (carved) · `registry/{COMPLETENESS,HYDRATION}-LEDGER.md` (regenerated) ·
`registry/CARRY-CUT-LEDGER.md` (§0 clause 7 + eight disposition cells) ·
`formation/codex-worktree-7e28/CENSUS.md` §7 · `audit/codex-provenance/motion-quarantine.md` §9 ·
`X/refinement/X-W0-FOLD.md` §9 · the six law-bearing V·L5 sites · the two 07-29 handoff gate lines.

**§Verification Artefacts' bank `docs/tranches/X/artefacts/W0/` was NOT created** — it appears in no
row of §File Bounds and five seats returned that gap independently (**E-3**). Every named artefact's
content is banked in tracked canon instead; the mapping is the close record's §4.

### Close hashes

| # | commit | unit |
|---|---|---|
| 1 | `befbc05a` | X-W0.a track-or-archive |
| 2 | `5937b6de` | X-W0.b prompt-recap |
| 3 | `9f4b22a7` | X-W0.d 7e28 census |
| 4 | `af03de5c` | X-W0.e axes-r2 / motion quarantine |
| 5 | `58be3626` ⊕ `549353fd` | X-W0.c completeness (row-5 body banked in the record — shared-index carry) |
| 6 | `b5673ae0` | X-W0.f tombstones |
| 7 | `c9aa1fdf` | X-W0.h graph-v3 + denominator |
| 8 | `2012dbfa` ⊕ `379d011c` | X-W0.i routing law |
| 9 | `ad240462` | X-W0.j glass8 census |
| 10 | `f7197d71` | X-W0.g owner sitting |
| 11 | *this status commit* | `docs(x/w0 close)` |

**Union bounds over all wave commits (1,850 paths)**: non-`docs/` **0** · `registry/adjudicated/` **0**
· 7e28 copies **0** · canonical `challenge-*.md` **0** · `scripts/dev/dev.sh` **0**.

**E13**: four paths swept at close; **0 unrowed, 0 UNREAD in scope** (I-31 marked FOLDED with its
three verified receipts). **VERIFIED remains ✗ and stays X-W11's** (R-A).

---

## §ADDENDUM 2026-09-17 (§0k.1 repair round) — §Verification Artefacts POINTED AT ITS TRACKED HOMES · HG-10 GREEN · HG-7 CURED

**E-3 posture — read first.** This is a dated **addendum-beside**, written by **X-W0.k**, the repair
seat COHESION **§0k.3** names (*"X-W0's repair seat lands §0k.1's HG-7 cure and the §Verification
Artefacts pointer; its fresh check then adjudicates the close"*). **Not one byte of §State …
§ADDENDUM 2026-08-30 … §CLOSE above is rewritten.** No gate is re-baselined, no born-RED measurement
re-taken, no condition added, removed or re-weighted. Three readings are recorded beside what stands.

**Authority**: `docs/tranches/X/COHESION.md` **§0k.1** (*"THE X-W0 CLOSE DOCKET, RULED"*), the three
rows quoted verbatim below. **Seat**: Opus (`claude-opus-5[1m]`). **Adjudication of the close is
X-W0.m's, not this addendum's** — VERIFIED stays ✗ and stays X-W11's (R-A).

### 1 · §Verification Artefacts (`:350–363`) — **NO NEW DIRECTORY**; the bank name resolves to tracked homes

§0k.1, quoted: *"**E-3 (`docs/tranches/X/artefacts/W0/`)** — no new directory. §Verification Artefacts
is amended by dated addendum at `waves/W0.md` to point at the tracked homes the close record's §4
maps."*

The bank path `docs/tranches/X/artefacts/W0/` **appears in no row of §File Bounds `:73–99`** — five
seats returned that independently (X-W0.c R-4 · X-W0.h R-4 · X-W0.i R-4 · X-W0.j R-j1 · X-W0.g
residual 4) and **no seat of this wave wrote there**. Re-measured at this addendum's clock:
⟨`ls docs/tranches/X/artefacts`⟩ → `No such file or directory`. **It is not created here either.**

**§Verification Artefacts' ten named artefacts are hereby READ AS POINTERS** into the tracked homes
the close record **`docs/tranches/X/execution/A/X-W0.md` §4 `:1345–1370`** maps, row for row. Every
probe named there was run at the close; the content is in tracked canon, not in an untracked bank:

| §Verification Artefacts name (`:354–363`) | tracked home of record |
|---|---|
| `census-open.txt` / `census-close.txt` | `X/W0/TRACK-OR-ARCHIVE.md` §2 (open, 1825 files / 3 trees) · close record §2, HG-1 (close, **0**, double-run) |
| `selftest-open.txt` / `selftest-close.txt` | close record **X-W0.c C1** (verbatim, open) · close record §2 (close) · **§RESUME** + **X-W0.k** (this round, verbatim both sides) |
| `clean-checkout-replay.txt` | close record §2, HG-8 · **X-W0.k** (this round's re-run, two worktree replays + two falsifiers) |
| `7e28-reverify.txt` | `V/megatranche/formation/codex-worktree-7e28/CENSUS.md` §7.3 (90 rows) + §7.4 |
| `graph-v3/` — nodes, edges, SCC classes, two round-trip digests | `X/W0/GRAPH-V3/{nodes,edges,sccs,census,digests}.json` (tracked, 983,886 B) |
| `tombstone-probes.txt` | close record §2, HG-12 (all nine re-run) + `X/W0/TOMBSTONES.md` §§1–5 |
| `vl5-sites-open.txt` / `vl5-sites-close.txt` | close record **X-W0.i I1** (open, 61) · close record §2, HG-17 (close, 58, classified by legal class) |
| `glass8-census-2026-08-XX.txt` | `X/W0/GLASS8-REPIN-CENSUS.md` §2 (four measured conditions) + §10 (close re-run) |
| `owner-sitting-2026-08-XX.md` | `X/W0/OWNER-SITTING-2026-09-17.md` §2, seven rows |
| the close commit hashes | close record §1 · this file's **§CLOSE** table · and, for the repair round, **X-W0.k**'s receipt |

**What this does not do**: it grants no path, widens no bound, and creates no artefact. A wave that
satisfies a §Verification Artefacts line by writing outside §File Bounds has committed the larger
defect — which is exactly why the close seat declined to, and why this addendum points instead.

### 2 · HG-10 (`:271–274`) — **GREEN**, read against the supersession column

§0k.1, quoted: *"**HG-10 (E-2) — the supersession column IS the cure.** A canonical axis row's
provenance word records the truth of the canonical file and is never re-badged when a later witnessed
round supersedes it; the falsifier's *'fails the same check twice'* is read against the supersession
column. **HG-10 GREEN.**"*

Measured at this addendum's clock, from the settled bytes of the generated `HYDRATION-LEDGER.md`:

| the three X-W0.e slugs | canonical row | round cell | the witnessed r2 row | harvest |
|---|---|---|---|---|
| `ActionFeedback` L | `challenge-L-library.md` · REPORT-AUTHORED | `r1 · re-run at r2 (witnessed)` | `challenge-L-library-r2.md` · EXISTS-ORIGINAL | `wf_1a4c8a8c-557.json` |
| `picker-pointerdebugoverlay` D | `challenge-D-design.md` · REPORT-AUTHORED | `r1 · re-run at r2 (witnessed)` | `challenge-D-design-r2.md` · EXISTS-ORIGINAL | `wf_1a4c8a8c-557.json` |
| `picker-componentsliders-consolerail` D | `challenge-D-design.md` · REPORT-AUTHORED | `r1 · re-run at r2 (witnessed)` | `challenge-D-design-r2.md` · EXISTS-ORIGINAL | `wf_1a4c8a8c-557.json` |

**6** canonical rows in all carry `r1 · re-run at r2 (witnessed)`; **34** r2 artefacts across the three
slugs are git-tracked. **`REPORT-AUTHORED` stays on the canonical rows, and that is the cure, not a
gap**: it is a true statement about the canonical file — the 2026-08-03 closure pass authored it and
no seat wrote it — and re-badging it would falsify the one thing the provenance column exists to
record. **No ledger byte was written for HG-10 by this round.**

### 3 · HG-7 (`:251–259`) — **RED → GREEN**, cured at the law, not at the symptom

§0k.1, quoted: *"**HG-7 (E-1) — SCOPE now, HARVEST at X-W11, both.** The completeness validator's
non-band law gains a **corpus predicate**: it ranges over run records at or before the megatranche
dispatch boundary (timestamped ≤ 2026-08-03) plus any record a canonical row cites; live tranche-X
session journals are outside its corpus by construction. The 87 are X-track run records and are
**harvested once, at X-W11's close** … so the scope predicate suppresses nothing — the finding is
dated and owed. … No run id borrowed, no allowlist, no cutoff written into a fixture."*

**BEFORE** (re-measured at this round's clock, double-run identical, never inherited):
`exit 1 · **89** uncovered · 264/264 hash-banked = 218 CHALLENGED + 46 REPORT-AUTHORED` ·
*"BASELINE IS NOT GREEN — self-test is meaningless until the real tree passes."* The figure had moved
**87 → 89 while X-W0 sat closed and no megatranche byte moved** — the drift is the diagnosis.

**AFTER**: `exit 0 · 0 uncovered` · **SELF-TEST PASS — 6/6 injected violations correctly rejected**
(S1..S6), double-run identical. In-corpus non-band records **27 · 0 violations**; out-of-corpus
**95**, of which **89 OWED** (77 completed-but-NOT-HARVESTED + 12 harvest-short), each **named by run
id** in the ledger with the verdict it would have taken, broken down by shortfall kind and attributed
by dispatching workflow, owed at **X-W11's close via `workflows/harvest-journals.mjs`**. The scope
**gates less and hides nothing**. Landed at **`de85e26c`** (validator + regenerated ledgers, one
commit, §Commit Plan row 5's generated-output idiom).

**The predicate carries its own falsifier, executed not paraphrased**: made fail-open — a record with
no readable timestamp *excluded* instead of included — **S3 and S4 flip to `FAIL — falsely accepted`
and the harness returns `SELF-TEST FAIL — 4/6`**. The fail-closed reading is therefore load-bearing,
and a future weakening of the scope cannot pass this gate silently.

**What did NOT happen, named**: no run id borrowed or invented · no `BANDS` re-point · no allowlist ·
no cutoff written into an S1..S6 fixture · no fixture weakened · no `covered:true` short-circuit · no
harvest file written · no `DEFECT-LEDGER.md` byte · no canonical `challenge-*.md` byte · no
`registry/adjudicated/**` byte · no 7e28 byte · zero product source.

**HG-6 and HG-8 re-read as stay-GREEN in the same breath** (a repair that moves a neighbouring gate is
not a repair): HG-6 — same-line unqualified `264/264` over the five live gate sites → **0**,
double-run **0**; three integers unmoved, **213 · 5 · 46 = 264 = 218 + 46**. HG-8 — ephemeral
`git worktree` of HEAD `de85e26c`: `node hydrate-reports.mjs && node validate-completeness.mjs` →
⟨`git diff --exit-code`⟩ **0**, **double-run 0**, `status --porcelain` **0 lines**; the `:264`
falsifier fired on demand (hash width 64 → 16 ⇒ **0/264 hash-banked**, 90 `NO-LEDGER` lines), the tree
restored to diff-0, and the worktree removed. Nothing was committed from it.

**What this addendum does not do.** It stamps no verb, rules no returned escalation, opens no wave,
and adjudicates no close. The wave's **Status** and **Verb status** lines stand exactly as §CLOSE left
them until X-W0.m's fresh check reads them at its own clock.
