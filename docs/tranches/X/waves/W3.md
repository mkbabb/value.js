# X.W3 — Access, route closure, and API policy (X.A1..X.A5)

**Ledger rows:** BUILD `CC-037` · BUILD `CC-038` · folds `CC-039`'s canon record.
**Authority:** `docs/tranches/V/megatranche/registry/CARRY-CUT-LEDGER.md` §0, §1.E, §2 (X-W3 row).
**Spec of record:** `docs/tranches/V/megatranche/audit/codex-provenance/api-gap-remeasure.md`
including its ADJUDICATION ADDENDUM (`:497-514`); route-closure evidence at
`audit/codex-provenance/red-matrices.md §11.2`. Where this file and the ledger diverge, the
ledger wins; above both, the owner's verbatim wins.

---

## §2 State

**Name**: W3 — Access, route closure, and API policy (X.A1..X.A5)
**Opens after**: X-W1 (re-gate: CI + falsifier demonstration must exist before born-RED gates can
be trusted to flip). Order-independent of the frontend scene chain (X-W4..X-W8).
**Agents**: 6 — five serial (`X.W3.1..X.W3.5`, api, forced cure order) + one parallel
(`X.W3.6`, frontend route closure, file-disjoint).
**Hard gate**: 22 conditions, **19 born-RED with measured baselines at HEAD `41450f02`**; the P0
triad (G-1 anonymous private read · G-6 cross-object revert transplant · G-18 unguarded
`meta:{admin}`) lands in one wave or the wave does not close.
**Status**: `planned`

### Four-verb status (M-22 status law)

| verb | state | evidence |
|---|---|---|
| AUDITED | **YES** | `api-gap-remeasure.md` (9 VERIFIED-GAP · 4 CHANGED · 0 CLOSED) + `red-matrices.md §11.2` (4/4 frontend findings literally true) |
| SPECIFIED | **YES — by this file, and nothing more** | — |
| IMPLEMENTED | **NO** | — |
| VERIFIED | **NO** | — |

### COMPLETABLE (C-06 — mandatory)

**YES.** This wave lands alone because every cure it names is reachable from bytes already on
disk: `api/src` + `demo/color-picker/router` + `demo/shell` + api vitest + e2e smoke. It has
**no producer dependency** (no glass-ui surface, no Glass-8 repin census), **no frontend-scene
dependency** (X-W4's typed action contract is not called here), and **no library dependency**
(the 4.1 cut is X-W9). Its only upstream is X-W1's honest CI, which gates *trust in* the gates,
not the cures. The `c4af0ef9` template holds: one coherent cut, its own gates, shipped alone.

Two limbs are deliberately **not** taken here and are handed off by name rather than smuggled:
the `Stub`-render limb of CC-037 → X-W5 (§Dispositions D-4), and the diff class → a recorded
decision (G-16). Neither is a glass-bank row; **zero rows are left blocked**.

---

## §2a Goal criterion

This wave succeeds if, when work ends, **no palette surface answers a caller it has not
authorized, and no route grants a privilege it has not checked** — one policy predicate owns
every read decision, every revision read and write is joined to the palette that addresses it,
every state-changing write is fenced and idempotent, and the admin route class is fail-closed at
navigation rather than fail-open with a decorative `meta` flag.

---

## §3 Scope

1. Add `assertReadable(doc, viewer)` to `api/src/modules/palette/service/visibility.ts` as the
   single object-read predicate; call it from palette detail, provenance, revision list, revision
   detail, and both fork source reads.
2. Make the trashed arm owner-only: non-owner trashed → `404`; owner trashed → `410`.
3. Join revision reads to the addressing palette (`{ _id: hash, paletteSlug: slug }`) and delete
   the global `getVersionByHash` service export.
4. Join revert's source-revision read to the target palette; refuse cross-object content
   transplant with `404`.
5. Split `payloadHash` (bytes: domain prefix + `0x00` + `uint64be(len)` + `PaletteColor.name`)
   from `releaseHash` (event identity), with a recorded at-rest migration.
6. Carry the ETag predicate into `updateOne` and assert `matchedCount === 1` on revert, PATCH,
   and publish; map `0` → `412`.
7. Require strong `If-Match` on revert (`428` absent / `412` mismatch) and return `201` with the
   appended revision.
8. Make `Idempotency-Key` REQUIRED on revert and fork create; record CC-039's single-replica LRU
   relaxation in canon with its reopening condition.
9. Rename fork creation to `POST /:slug/forks`, birth the child `private`, and authorize the
   source at both the pre-flight and in-transaction reads.
10. Filter the fork list per child through `assertReadable` and derive `total` from the filtered
    join; compute `forkCount` at format time and demote the stored field to an approximate sort
    key.
11. Narrow `PALETTE_VISIBILITIES` to `["public","private"]`, add the `moderation: clear |
    withdrawn` clock, and migrate at-rest `unlisted` rows to `private`.
12. Thread the policy predicate through the admin palette service as an explicit `ADMIN-POLICY`
    branch that emits its taken-branch into the existing audit row.
13. Author the admin operation target in `docs/tranches/X/contracts/ADMIN-POLICY.md`
    (`vnext/api-contract.source.json` stays FROZEN-AS-INHERITED — it is not written).
14. Add a fail-closed `router.beforeEach` guard on `meta.admin`.
15. Replace the wildcard `redirect: "/"` with a source-realized not-found route (the wave's one
    non-`Stub` route record).
16. Make the `usePaneRouter` terminal fallback fail-closed (explicit not-found pane, never
    `ColorPicker`).

---

## §3a Triumvirate Dispatch

A triumvirate (research + plan augment + redress) is **mandatory**, not optional, on any of:

- **File-bound expansion that invalidates the wave** — any cure requiring a write to
  `src/**` (library band, X-W9), `demo/@/components/ui/**` (shadcn, X-W8), any second
  `demo/` scene file beyond the three named paths, or
  `docs/tranches/V/vnext/api-contract.source.json` (frozen).
- **Non-local-edit-recoverable hard-gate failures** — the `payloadHash`/`releaseHash` split
  (G-7) forcing a rewrite of at-rest `palette_versions._id` values; the `unlisted` migration
  (G-15) finding at-rest rows the two-state enum cannot represent; the `Idempotency-Key`
  requirement (G-10) breaking an existing consumer of `POST /:slug/fork`.
- **Third-iteration diagnostic halt** — three consecutive failing runs of the same policy spec
  against the same cure; three consecutive `mongodb-memory-server` replica-set boot failures in
  `api/test/setup.ts`; three consecutive e2e guard-spec flakes.

Per `ORCHESTRATION.md §Triumvirate Auto-Triggers` the orchestrator may not redispatch the
failing unit alone.

---

## §4 File Bounds

| File | Access |
|---|---|
| `api/src/modules/palette/service/visibility.ts` | modify |
| `api/src/modules/palette/service/crud.ts` | modify |
| `api/src/modules/palette/service/crud-list.ts` | modify |
| `api/src/modules/palette/service/versions.ts` | modify |
| `api/src/modules/palette/service/forks.ts` | modify |
| `api/src/modules/palette/routes/crud.ts` | modify |
| `api/src/modules/palette/routes/versions.ts` | modify |
| `api/src/modules/palette/routes/forks.ts` | modify |
| `api/src/modules/palette/repository/palette.ts` | modify |
| `api/src/modules/palette/repository/paletteVersion.ts` | modify |
| `api/src/modules/palette/hash.ts` | modify |
| `api/src/modules/palette/model.ts` | modify |
| `api/src/modules/palette/format.ts` | modify |
| `api/src/modules/palette/etag.ts` | modify |
| `api/src/platform/http/idempotency.ts` | modify |
| `api/src/platform/migrations/check.ts` | modify |
| `api/src/platform/migrations/x-w3-visibility-payloadhash.ts` | create |
| `api/src/modules/admin/policy.ts` | create |
| `api/src/modules/admin/service/palettes.ts` | modify |
| `api/src/modules/palette/__tests__/palette-policy.test.ts` | create |
| `api/src/modules/palette/__tests__/palette-write-contract.test.ts` | create |
| `api/src/modules/palette/__tests__/palette-versions.test.ts` | modify |
| `api/src/modules/palette/__tests__/palettes-forks.test.ts` | modify |
| `api/src/modules/palette/__tests__/palette-forks.test.ts` | modify |
| `demo/color-picker/router/index.ts` | modify |
| `demo/color-picker/router/guards.ts` | create |
| `demo/shell/usePaneRouter.ts` | modify |
| `demo/shell/viewSchema.ts` | modify |
| `demo/scenes/notfound/NotFoundPane.vue` | create |
| `e2e/smoke/admin/route-guard.spec.ts` | create |
| `docs/tranches/X/contracts/ADMIN-POLICY.md` | create |
| `docs/tranches/X/contracts/WRITE-CONTRACT.md` | create |
| `docs/tranches/V/PALETTE-CONTRACT.md` | modify |
| `docs/tranches/X/waves/W3.md` | modify (status + artefact paths at close) |

**Do NOT touch**: `docs/tranches/V/vnext/api-contract.source.json` (FROZEN-AS-INHERITED, M-15) ·
`scripts/dev/dev.sh` (CC-021 — unowned until the X-W0 owner sitting) · `src/**` (library band,
X-W9) · `demo/@/components/ui/**` (shadcn, X-W8) · any `demo/` path not named above ·
`~/.codex/**`.

## §4a Disjointness

`X.W3.1..X.W3.5` run **serial** — the cure order is forced (each cure calls the prior unit's
artifact), so their overlapping `api/src/modules/palette/**` `modify` paths never coexist.
`X.W3.6` writes only `demo/**` + `e2e/**` + `docs/tranches/X/contracts/ADMIN-POLICY.md`; it
shares **no** path with any api unit and is the only parallel writer. No other X wave writes
`api/src/**`.

## §4b Worktree Plan

Two concurrent writers ⇒ sibling worktrees; the orchestrator runs `git worktree list` /
`git worktree add` before dispatch.

| Agent unit | Sibling worktree absolute path | Build isolation |
|---|---|---|
| `X.W3.1`–`X.W3.5` (api chain) | `/Users/mkbabb/Programming/value.js-x-w3-api` | `api/node_modules` in-tree; vitest `TEST_MONGODB_URI` per-process (ephemeral replica set) |
| `X.W3.6` (frontend closure) | `/Users/mkbabb/Programming/value.js-x-w3-fe` | demo Vite cache in-tree; Playwright `--output` scoped to the worktree |

`CARGO_TARGET_DIR` is not applicable (no Rust); the equivalent isolation is the per-worktree
`node_modules` + per-process ephemeral Mongo above.

---

## §5 Agent Units

Every implementation seat is **Opus 5** (M-23 §2). This wave contains no design content, so the
M-23 §3 twice-authored Fable∥Opus → fresh-Fable lane does **not** apply here (it applies to
X-W10). An undeclared seat is a defect.

### X.W3.1 Policy kernel (X.A1)

- **Goal**: one predicate owns every palette object-read decision, and no read path answers a
  caller it has not authorized.
- **Mechanism**: add `assertReadable(doc, viewer)` to `service/visibility.ts` beside the existing
  `isActivePublic` (`:31-38`); it returns for owner-any-state and for active-public-moderation-clear,
  throws `NotFoundError` otherwise. Call it in `getPaletteBySlug` before formatting
  (`service/crud.ts:44-69`), before the provenance walk and per-hop with the viewer threaded
  (`service/forks.ts:171-222`), and on the revision-list route (`routes/versions.ts:25-41`). Move
  the `GoneError` arm behind the ownership check.
- **Files**: `service/visibility.ts`, `service/crud.ts`, `service/forks.ts`, `routes/versions.ts`,
  `service/versions.ts`, `__tests__/palette-policy.test.ts` (create).
- **Sub-gate**: G-1, G-2, G-3, G-4.

### X.W3.2 Membership join (X.A2)

- **Goal**: revision identity is `(paletteSlug, hash)` everywhere, so no content crosses an
  object boundary.
- **Mechanism**: `repository/paletteVersion.ts` gains `findByPaletteAndHash(paletteSlug, hash)`;
  `routes/versions.ts:43-47` reads the route `slug` and calls it; the global
  `getVersionByHash` export (`service/versions.ts:99-106`) is **deleted**, not deprecated
  (no-legacy law). `revertToVersion` (`service/versions.ts:131-135`) uses the same joined read.
  `hash.ts:8-17` gains the domain prefix + `0x00` + `uint64be(len)` framing and `PaletteColor.name`
  as `payloadHash`; `palette_versions._id` becomes the release hash (paletteId + revisionNo +
  parent + author + policy facts), with `payloadHash` a stored field and a recorded migration.
- **Files**: `routes/versions.ts`, `service/versions.ts`, `repository/paletteVersion.ts`,
  `hash.ts`, `model.ts`, `platform/migrations/x-w3-visibility-payloadhash.ts` (create),
  `__tests__/palette-versions.test.ts`.
- **Sub-gate**: G-5, G-6, G-7.

### X.W3.3 Write contract (X.A3)

- **Goal**: every state-changing palette write is fenced against a concurrent writer and safe to
  retry exactly once.
- **Mechanism**: `repository/palette.ts:107-115` `update()` returns `UpdateResult` instead of
  `.then(() => undefined)` and takes the ETag-bearing predicate in its filter; callers assert
  `matchedCount === 1` and map `0` → `412`. `assertIfMatch` (`etag.ts:37-51`) is called on
  `routes/versions.ts` revert as it already is at `routes/crud.ts:122` / `routes/publish.ts:39`.
  `platform/http/idempotency.ts` gains a `required` mode mounted on revert + fork create
  (absent key → `400`). Revert returns `201` with the appended revision. CC-039's single-replica
  LRU relaxation is written into `docs/tranches/X/contracts/WRITE-CONTRACT.md` with its reopening
  condition stated as a **deployment fact** (a second replica), never a wave.
- **Files**: `repository/palette.ts`, `routes/versions.ts`, `routes/crud.ts`, `routes/publish.ts`,
  `etag.ts`, `platform/http/idempotency.ts`, `__tests__/palette-write-contract.test.ts` (create),
  `docs/tranches/X/contracts/WRITE-CONTRACT.md` (create), `docs/tranches/V/PALETTE-CONTRACT.md`.
- **Sub-gate**: G-8, G-9, G-10, G-11.

### X.W3.4 Fork closure (X.A4)

- **Goal**: forking neither publishes a private source nor enumerates private children.
- **Mechanism**: `routes/forks.ts:19` becomes `POST /:slug/forks`; `service/forks.ts:76` births
  the child `visibility: "private"`; `assertReadable` is called at the pre-flight source read
  (`:50-51`) and again inside the transaction (`:99-105`). `repository/palette.ts:69-83`
  `findForksOf`/`countForksOf` accept a viewer and the fetched page is filtered per child, with
  `total` derived from the filtered join. `forkCount` is computed at format time
  (`format.ts:38,74`); the stored field survives only as the `most-forked` sort key
  (`crud-list.ts:80`) with an explicit "approximate" contract.
- **Files**: `routes/forks.ts`, `service/forks.ts`, `repository/palette.ts`, `format.ts`,
  `service/crud-list.ts`, `__tests__/palette-forks.test.ts`, `__tests__/palettes-forks.test.ts`.
- **Sub-gate**: G-12, G-13, G-14. **Classes 2 and 3 land together** — curing the child to
  `private` without the list filter opens the leak (`api-gap-remeasure.md:161`).

### X.W3.5 Enum, moderation clock, and diff decision (X.A5)

- **Goal**: the visibility vocabulary matches D9, and the absent diff class has a recorded
  terminal disposition rather than an open gap.
- **Mechanism**: `model.ts:19` narrows to `["public","private"]`; a `moderation: "clear" |
  "withdrawn"` field is added and separately clocked (D9, `DECISIONS.md:36`); the migration maps
  at-rest `unlisted` → `private`; `platform/migrations/check.ts:58` and the prose at
  `service/visibility.ts:7,29` / `service/forks.ts:165,210` / `format.ts:27` are corrected.
  For the diff class: **record the retirement** with T.W1's excision rationale
  (`service/forks.ts:34-36`) unless a named product consumer is produced at wave-open, in which
  case `GET /:slug/versions/:from/diff/:to` is built over `computeAtomHash` /
  `computeAtomSetHash` (`hash.ts:26-32,44-49`) with **both** memberships policy-checked.
- **Files**: `model.ts`, `format.ts`, `service/visibility.ts`, `service/forks.ts`,
  `platform/migrations/check.ts`, `platform/migrations/x-w3-visibility-payloadhash.ts`.
- **Sub-gate**: G-15, G-16.

### X.W3.6 Route closure and admin policy branch (CC-037 + class 9)

- **Goal**: an unauthenticated visitor cannot reach an admin surface by typing a URL, and no
  unknown route or pane silently resolves to a privileged or arbitrary default.
- **Mechanism**: `demo/color-picker/router/guards.ts` (create) exports a `beforeEach` that
  fail-closes on `to.meta.admin` against the existing `useAdminAuth` state
  (`demo/palettes/usePalettePorts.ts:57`) and is registered in `router/index.ts`. The wildcard
  `{ path: "/:pathMatch(.*)*", redirect: "/" }` (`:36`) becomes a named `not-found` record
  rendering a real `NotFoundPane.vue` — **the wave's one non-`Stub` route record**, and the
  pattern X-W5 generalizes. `usePaneRouter.ts:94` `return ColorPicker;` becomes the explicit
  not-found pane. Api-side, `admin/policy.ts` (create) wraps the class-1 predicate as an explicit
  `ADMIN-POLICY` branch; `admin/service/palettes.ts:34-35,59,64` call it instead of reaching
  `services.repositories.palettes` unmediated, and the taken branch is written into the existing
  typed audit row (`:48-51`, `:79`). The admin operation target is authored in
  `docs/tranches/X/contracts/ADMIN-POLICY.md`.
- **Files**: `demo/color-picker/router/index.ts`, `demo/color-picker/router/guards.ts`,
  `demo/shell/usePaneRouter.ts`, `demo/shell/viewSchema.ts`,
  `demo/scenes/notfound/NotFoundPane.vue`, `e2e/smoke/admin/route-guard.spec.ts`,
  `api/src/modules/admin/policy.ts`, `api/src/modules/admin/service/palettes.ts`,
  `docs/tranches/X/contracts/ADMIN-POLICY.md`.
- **Sub-gate**: G-17, G-18, G-19, G-20, G-21.

---

## §6 Hard Gate

Every gate names the command or runtime probe that closes it, its **measured baseline at HEAD
`41450f02` (2026-08-03, read-only)**, and — per **L-19** — the falsifier: how the gate fails for
its intended reason. `MEASURE-AT-OPEN` marks a baseline that must be taken at wave-open rather
than fabricated here.

Verdict legend: **RED** = born-RED, armed now, flips GREEN only at the cure.

| # | gate | baseline at HEAD | verdict |
|---|---|---|---|
| **G-1** | **P0.** `getPaletteBySlug` on a `private` palette with `currentUserSlug: undefined` throws `NotFoundError`; the owner still reads it. | `grep -rn "assertReadable" api/src` → **0 hits**. `service/crud.ts:44-69` has no visibility predicate; `currentUserSlug` is consumed only for `result.voted` (`:60-64`). `grep -rn "isActivePublic" api/src` → 1 definition (`service/visibility.ts:31`) + **1** caller (`service/forks.ts:201`). | **RED** |
| **G-2** | Non-owner GET of a soft-deleted palette → `404`; owner → `410`. | `service/crud.ts` emits `GoneError` on `doc.deletedAt` **before** any ownership check — a live existence oracle for a non-owner. | **RED** |
| **G-3** | `GET /palettes/{slug}/versions` on a private palette is refused anonymously, and each item carries a release check before formatting. | `routes/versions.ts:25-41` performs no auth, ownership, or visibility check; `repository/paletteVersion.ts:22-27` is `find({ paletteSlug })`. The route emits every version's `{name, colors, authorSlug, parentHash, …}`. | **RED** |
| **G-4** | `GET /palettes/{slug}/provenance` authorizes the target **before** walking, and an owner's own private hops resolve to `palette` steps rather than `unavailable`. | `service/forks.ts:171-222`: hop redaction is present and correct (landed V·W45 item 4 — the GREEN half), but the target is never authorized and no viewer reaches the per-hop predicate. | **RED** |
| **G-5** | `GET /palettes/A/versions/<hash-of-B>` → `404`, and `getVersionByHash` has zero non-test occurrences (structural half of this runtime gate). | `routes/versions.ts:44-45` reads `hash` and never `slug`; `service/versions.ts:99-106` is the global export; `grep -rn "getVersionByHash" api/src` → **3 non-test hits** (`service/versions.ts:99`, `routes/versions.ts:17,45`). | **RED** |
| **G-6** | **P0.** Owner of palette A reverting to a hash belonging to palette B → `404`; A's `name` and `colors` are byte-unchanged afterwards. | `service/versions.ts:134` `findByHash(hash)` is unjoined; `requireOwnership` (`require-ownership.ts:29-42`) gates the **target** only; the write at `:162-175` `$set`s B's `name`+`colors` into A. Hashes are handed out freely by the version list (`routes/versions.ts:36`) and by `currentHash` on every detail envelope. | **RED** |
| **G-7** | Two palettes differing only in per-color `name` produce different `payloadHash`; `payloadHash` is framed `domain ‖ 0x00 ‖ uint64be(len) ‖ bytes`; release identity is a separate field; the migration runs and is recorded. | `hash.ts:8-17` canonicalizes only `{name, colors[{css, position}]}`, lowercases `name`, and has no domain/length framing; the same value is `palette_versions._id` (`service/versions.ts:61`) — payload identity **is** membership identity. | **RED** |
| **G-8** | Revert / PATCH / publish assert `matchedCount === 1`; a concurrent write between read and write yields `412`, not a silent overwrite. | `grep -rn "matchedCount" api/src` → **1 hit, `modules/color/repository/proposedName.ts:79`** — zero in the palette domain. `repository/palette.ts:110-115` is `updateOne({ slug }, update).then(() => undefined)`, result discarded. Self-disclosed at `routes/crud.ts:113-123` and `service/ownership.ts:33-37`. | **RED** |
| **G-9** | Revert without `If-Match` → `428`; with a stale ETag → `412`. | `grep -rn "assertIfMatch" api/src` → **2 call sites**, `routes/crud.ts:122` and `routes/publish.ts:39`; **none** on `/revert`. | **RED** |
| **G-10** | Revert and fork create without `Idempotency-Key` → `400`; a replayed key returns the stored response; the WRITE-CONTRACT canon records the single-replica LRU relaxation and its reopening condition. | `platform/http/idempotency.ts:94-99` — absent key ⇒ `await next()` unconditionally (opt-in). Store is the in-process `LRU` (`:25-35`, `:73`), disclosed only in source — CC-039's exact defect. | **RED** |
| **G-11** | Revert returns `201` carrying the appended revision. | `routes/versions.ts:66` `return c.json(formatPalette(palette));` → **200**. | **RED** |
| **G-12** | `POST /palettes/{slug}/forks` is mounted; the child is born `private`; forking a private palette owned by another user → `404` at both the pre-flight and in-transaction source reads. | Live `/openapi.json` at HEAD: `/palettes/{slug}/fork` → `['post']`, `/palettes/{slug}/forks` → `['get']` **only**. `service/forks.ts:76` `visibility: "public"`; the in-txn recheck (`:99-105`) is existence-only. | **RED** |
| **G-13** | `GET /palettes/{slug}/forks` returns no private child to an anonymous caller, and `total` equals the filtered length. | `repository/palette.ts:69-83`: `findForksOf` → `find({ forkOf: slug, deletedAt: null })`, `countForksOf` → same filter. No visibility, no viewer. `routes/forks.ts:54-70` applies no policy and formats every row in full. | **RED** |
| **G-14** | `forkCount` on a detail envelope equals the viewer-filtered child count; the stored field is documented approximate and reachable only as a sort key. | Stored `model.ts:76`; written `service/forks.ts:84,129` + `service/crud.ts:261-266`; exposed as authority `format.ts:38,74`; sorted `crud-list.ts:80`. The drift is already self-documented at `repository/palette.ts:204-211`. | **RED** |
| **G-15** | `PALETTE_VISIBILITIES` is `["public","private"]`; a `moderation` field exists and is separately clocked; the migration maps at-rest `unlisted` → `private` and the check-migration branch is retired. | `model.ts:19` `["public", "unlisted", "private"]` (3-state, documented canonical at `:61`, `format.ts:27`); live branch `platform/migrations/check.ts:58`. `grep -rn "moderation" api/src` → **zero model/field hits** (prose only, in admin + color modules). D9 (`DECISIONS.md:36`) rules the two-state enum + separate clock. | **RED** |
| **G-16** | The diff class has a terminal disposition in this file at close: RETIRED-BY-RECORD (default), or built with **a named product consumer** and both memberships policy-checked. | Live `/openapi.json`: `diff` path rows = **0**; `routes/index.ts:13-19` mounts no diff sub-router. T.W1 excised it as write-only apparatus (`service/forks.ts:34-36`). | **DECISION** (L-19 — a route without a named consumer is not built) |
| **G-17** | An admin palette mutation on a private palette emits an audit row naming the `ADMIN-POLICY` branch taken; the admin service reaches no repository unmediated. | `admin/service/palettes.ts:34-35,59,64` call `services.repositories.palettes` directly, composing no palette-domain predicate. Audit + actor identity already landed (U-F40, `admin/auth.ts:35,53-55`) — the branch does not exist. Re-measure at HEAD (recorded command, L-9): live `/openapi.json` = **21 admin path rows / 23 admin operations / 51 total operations across 45 path rows**; `vnext/api-contract.source.json` carries **0** admin paths. (The re-measure doc's "17 operations" is superseded by this measurement.) | **RED** |
| **G-18** | **P0.** Anonymous direct navigation to `/admin/users` does not mount `AdminPane`; the guard resolves to a non-admin route; e2e proves it headless. | `grep -rn "beforeEach\|beforeEnter" demo --include="*.ts" --include="*.vue" \| wc -l` → **0**, while `grep -c "meta: { admin: true }" demo/color-picker/router/index.ts` → **5** (`:31-35`). The meta flag is declared and never enforced. | **RED** |
| **G-19** | An unknown pane name resolves to the not-found pane, never `ColorPicker`. | `demo/shell/usePaneRouter.ts:94` `return ColorPicker;` is the terminal fallback. | **RED** |
| **G-20** | An unknown URL renders a source-realized not-found route (a real component, not `Stub`, not a redirect). | `demo/color-picker/router/index.ts:36` `{ path: "/:pathMatch(.*)*", redirect: "/" }`; `grep -c "component: Stub" …/router/index.ts` → **14**, and `:19` `const Stub = { render: () => null };`. | **RED** |
| **G-21** | The `Stub`-render handoff row to X-W5 exists by name in `docs/tranches/X/waves/W5.md` (or the X-W5 authoring brief) and cites the 14-record baseline. | The 14 `Stub` records are measured above; X-W5 (CC-049, one routed scene) owns their replacement. | **HANDOFF** |
| **G-22** | `cd api && npm test` green · `npm run typecheck` green for demo · `e2e/smoke` green including the new guard spec · no `test.fail()` leg added · the one-time falsifier demonstration run (break one policy assertion → the job goes red, restore it). | MEASURE-AT-OPEN (regression guard; not a live defect). Depends on X-W1's HARD e2e jobs. | **MEASURE-AT-OPEN** |

**Totals: 22 gates · 19 born-RED · 1 decision · 1 handoff · 1 measure-at-open.**

**Falsifier discipline (L-19).** Each born-RED gate above fails for exactly one reason: remove
the named cure and the named probe reproduces the named baseline. No gate is a grep-only runtime
gate — G-5's grep is the structural half of a runtime cross-object read; G-21 is a document
handoff and is labelled as such, not counted as evidence of product behavior. No `proof-*.mjs`
script is authored (CC-019's structural ban), no cross-product denominator is constructed, and
no gate counts a category the audit invented for itself.

---

## §7 Format And Lint Cadence

- After each agent unit and before the wave closes: `cd api && npm run lint && npm test`;
  repo-root `npm run typecheck` (demo + lib bands); `npx prettier --check` over the touched
  paths.
- Docs-only edits in this wave additionally run `git diff --check`.
- Nothing is intentionally skipped. If the api lint config lacks a rule the cure needs
  (`no-non-null-assertion` under `api/src/`), the rule is **not** added here — that is X-W9's
  library-band act; the replacement evidence is the typed `Result`/throw path exercised by the
  policy specs.

## §8 Verification Artefacts

Saved at close under `docs/tranches/X/waves/artefacts/W3/`:

1. `born-red-baseline.txt` — every §6 baseline command re-run at wave-open with its output.
2. `openapi-before.json` / `openapi-after.json` — the live `/openapi.json` on both sides of the
   cut (path/operation counts diffed).
3. `api-test-before.txt` / `api-test-after.txt` — `cd api && npm test` transcripts.
4. `falsifier-demonstration.txt` — the G-22 break/restore transcript.
5. `migration-run.txt` — the `unlisted` → `private` + `payloadHash` migration output with
   affected-document counts.
6. `e2e-route-guard.txt` + the Playwright trace for `e2e/smoke/admin/route-guard.spec.ts`.
7. Commit hashes for every checkpoint in §9.

## §9 Commit Plan

| # | scope | body required |
|---|---|---|
| 1 | `feat(api/palette-policy): assertReadable kernel + owner-only trashed arm (X.A1)` | yes — names the four read paths wired |
| 2 | `fix(api/palette-versions): join revision identity to the addressing palette; delete the global getVersionByHash (X.A2)` | yes — P0 |
| 3 | `feat(api/palette-hash): split payloadHash from release identity + migration (X.A2)` | yes — migration |
| 4 | `feat(api/write-contract): CAS + strong If-Match + required Idempotency-Key; revert → 201 (X.A3)` | yes |
| 5 | `fix(api/palette-forks): plural route, private child, both-boundary source auth, viewer-filtered list, computed forkCount (X.A4)` | yes |
| 6 | `feat(api/palette-visibility): unlisted dies, moderation clock lands (X.A5)` | yes — migration |
| 7 | `fix(demo/router): fail-closed admin guard + source-realized not-found route + fail-closed pane fallback (CC-037)` | yes — P0 |
| 8 | `feat(api/admin-policy): explicit ADMIN-POLICY branch + audited branch record (class 9)` | yes |
| 9 | `docs(X·W3): contracts + wave status + artefacts` | yes |

Commits are path-scoped (pathspec only). Orchestrator integration commits merge the two
worktrees; the P0 triad (commits 2, 7, and the G-1 half of 1) must be present in the same
integration before the wave reports.

## §10 Dependencies

- **Depends on**: **X-W1** (honest CI + the mandatory falsifier demonstration — without it a
  born-RED gate flipping GREEN proves nothing). Soft: X-W0's owner sitting only insofar as
  CC-021 keeps `scripts/dev/dev.sh` untouchable, which this wave already honors.
- **Blocks**: **X-W7** (palette specimen/domain split — its panes consume the cured API surface;
  §2 records "API surface already cured by X-W3"). Does **not** block X-W4/X-W5/X-W6.
- **Independent of**: the glass-8 repin census (§1.M) — this wave contains no glass-coupled row.

## §11 Archaeology

- **The diff class (G-16)** was built once and deliberately excised: the J.W2 remix/atom-diff arm
  died at **T.W1 (TA-4)** as a "write-only apparatus" with no consumer
  (`api/src/modules/palette/service/forks.ts:34-36`). Guardrail: L-19 — it is rebuilt in X only
  against a **named product consumer**, otherwise it retires by record.
- **CC-039 (DR-33)** promised a durable Idempotency-Key replay store and shipped a per-process
  LRU, with the relaxation disclosed only in source comments. Guardrail: the relaxation is
  written into `WRITE-CONTRACT.md` canon at G-10, and its reopening condition is a **deployment
  fact** (a second replica), never a future wave — the row does not carry again.
- **X-4 / X-5** were the two rows the re-measure left blocked on an owner ruling; both are RULED
  in the adjudication addendum (`api-gap-remeasure.md:497-514`). They are not re-litigated here
  (§Dispositions D-1, D-2).

---

## §Dispositions

| # | row / limb | disposition |
|---|---|---|
| D-1 | **X-4** — `/versions` vs `/revisions/{revisionNo}` | **`/versions` STANDS.** The rename is RETIRED with rationale (no capability, no consumer, and the alias a migration would smuggle is forbidden by the no-legacy law). The class-6 rename contest resolves identically. Semantics are cured under the current paths (G-5). |
| D-2 | **X-5** — the `0/8` export denominator | **DROPPED.** The export map is exactly D3-conformant (`DECISIONS.md:32`, seven subpaths, sole). Not API policy; no D3 amendment warranted. Out of this wave's scope entirely. |
| D-3 | **CC-039** (DR-33) | **RETIRE by recording the truth** — canon record at G-10; X.A3 makes the key REQUIRED. |
| D-4 | **CC-037's `Stub`-render limb** | **HANDOFF → X-W5** (CC-049, one routed scene). X-W3 lands exactly one real route component (the not-found record, G-20) as the pattern; the remaining 14 `Stub` records are X-W5's. Recorded, not smuggled — G-21. |
| D-5 | **Class 4 provenance redaction half** | **ALREADY CLOSED** at V·W45 item 4; not re-built. Only authorize-target-first + the owner arm are owed (G-4). |
| D-6 | **Class 9 route-local repository access** | **ALREADY CLOSED** (routes call services). Only the explicit audited policy branch is owed (G-17). |
| D-7 | **`vnext/api-contract.source.json`** | **FROZEN-AS-INHERITED (M-15).** The admin operation target is authored in `docs/tranches/X/contracts/ADMIN-POLICY.md` instead; the frozen file is not written. |
| D-8 | **Glass bank rows** | **None.** No row of this wave is glass-coupled; nothing is left in the §1.M bank by this authoring. |

---

## §L-18 rider — two quartet challenge passes before ACCEPTED

This wave's implementation is **SPECIFIED by this file and nothing more**. Per **L-18** it is not
ACCEPTED until it has survived **two** challenging gestalt passes, each a **quartet of Opus 5
agents** assuming the implementation is wrong on some reasonable basis, across three altitudes —
total-tranche (was X-W3 optimal in the greater plan; was this spec adhered to; what friction
arose), wave, and feature — reporting **defects and superlatives** with provenance and proof,
then adjudicated and agglomerated into an apotheosis by a **fresh Fable** instance. Likely bases
for this wave: a policy predicate re-implemented rather than called; a cure that fixes the test
instead of the byte; membership joins added at the route while the repository stays global; a
guard that fail-opens on an async auth race; over-large `service/versions.ts` after the split.
Any incongruity surfaced is fully addressed, ratified, and dispatched — never parked.
**L-20** binds this file: it is re-validated against the current
`docs/precepts/instructions/tranche/WAVE_SPEC.md` at authoring, at each amendment, and at each
L-18 pass.

---

## §E-3 ADDENDUM-BESIDE 2026-09-18 — GF-R1: THE ApiOfflineChip TRANSPORT CLUSTER IS X-W3's, AS UNIT `X.W3.7`

**Nature.** An **E-3 addendum-beside**, not a patch. **No byte above this line is edited** — §2's
`Agents` line, §4's table, §5's unit list and §6's gate table keep their authored bytes; where this
addendum and those bytes differ, the difference is stated here and read from here. Landed as
`X.W3.7`'s **first docs act**, per COHESION **§0k.3 Consequence** (*"each landing the dated addendum
its ruling names as its first docs act"*).

**Authority, by id, quoted at its bytes.** COHESION **§0j.B · GF-R1** (`COHESION.md:625-631`),
ruled under the begin-word's delegation at the 2026-09-17 sitting:

> **Claimant: X-W3**, by a dated E-3 addendum widening its bounds to `demo/platform/transport/**`
> (4 files) as one named unit carrying the seven rows with AP-17 (the unbounded probe burst against
> a *"ONE probe"* promise) as its born-RED gate.

Booked in full at `docs/tranches/X/refinement/X-W0-FOLD.md` **§9.3 — the G-F register slate, entry
15 (W0.38)** (`:1725-1760`), all seven ⟨record · id⟩ id-for-id, over
`docs/tranches/V/megatranche/registry/adjudicated/ApiOfflineChip.md`; the residual it discharges is
`EXECUTION-RUNBOOK.md` **§4.4 R-1** (`:531-548`).

### A. §4 File Bounds — widened, by this addendum, to the four transport files

| File | Access | measured |
|---|---|---|
| `demo/platform/transport/availability.ts` | modify | 195 lines |
| `demo/platform/transport/client.ts` | modify | 158 lines |
| `demo/platform/transport/useApiClient.ts` | modify | 62 lines |
| `demo/platform/transport/api-problem.ts` | modify | 60 lines |

⟨cmd⟩ `ls -1 demo/platform/transport/` → `api-problem.ts` · `availability.ts` · `client.ts` ·
`useApiClient.ts` — **4**, the same four the slate entry measured (`X-W0-FOLD.md:1760-1761`). The
widening is **exactly** `demo/platform/transport/**` and nothing beside it: §4's `Do NOT touch` line
(*"any `demo/` path not named above"*) continues to bind every other demo path, and `scripts/dev/dev.sh`
stays untouched (CC-021).

### B. §5 Agent Units — `X.W3.7` added by the ruling that names it

**§2's `Agents: 6` line is READ AS 7 here; its bytes are NOT edited.** The seventh seat is added by
the owner-delegated ruling above, which directs *"one named unit"*; precedent for adding a unit by
dated addendum is COHESION **§0p** (X.P.W3 `.f`/`.g`).

#### X.W3.7 GF-R1 — the ApiOfflineChip transport cluster (seven rows, one MAJOR)

- **Goal**: the transport latch keeps the promise its own docstring makes twice — **exactly one
  probe per cooldown window** — and the module's init seam, its dead member, and its one-sided
  invariant are cured at the byte rather than carried as a register row.
- **Mechanism**: `availability.ts`'s allow branch re-arms `unavailableSince` as it admits the probe
  (AP-17); `markApiReachable` defends `misconfigured` exactly as `markApiUnreachable` already does
  (AP-31); `initApiEnvironment` gains a real idempotence guard **before** its import-time call moves
  from `client.ts` to the explicit `provideApiClient()` seat (AP-33 **then** AP-24, in that order);
  `ApiClient.baseUrl` is deleted (AP-29); the two trip conditions for *"the backend is not
  answering"* are reconciled to ONE transport-owned predicate (AP-12, transport limb only).
- **Files**: the four above · `docs/tranches/X/waves/W3.md` (this addendum) ·
  `docs/tranches/X/waves/artefacts/W3/**`.
- **Sub-gate**: **AP-17** (born-RED, CONFIRMED MAJOR) · AP-12 · AP-24 · AP-29 · AP-30 · AP-31 · AP-33.
- **Disjointness**: the four transport files appear in **no** other unit's writable set in this wave
  and in no other X wave's §4 — §4a's statement that `X.W3.6` is *"the only parallel writer"* over
  `demo/**` is read with this unit beside it: the two share **zero** paths (`X.W3.6` writes
  `demo/color-picker/router/**`, `demo/shell/**`, `demo/scenes/**`).

### C. The seven rows, and what closes each

| id | grade | closes by |
|---|---|---|
| **AP-17** | **MAJOR — CONFIRMED** | **born-RED**: a runtime probe measures how many callers the allow branch admits in one window — N before the cure, exactly 1 after, and the next window still opens (the falsifier). |
| **AP-12** | MAJOR — **SPLIT** | transport limb only: one predicate owns the trip condition. The 7 hand-written *"is unreachable."* sentences are **X-W7's surface-vocabulary rider** and are not written here. |
| **AP-24** | MINOR | the import-time call moves to `provideApiClient()`; importing the transport module mutates no global state. **Lands after AP-33.** |
| **AP-33** | MINOR | a real idempotence guard, landed **first**, so the move cannot make repeat invocation reachable. |
| **AP-29** | MINOR | the dead `baseUrl` member is deleted. |
| **AP-30** | MINOR | **RECORDED, not rebuilt** — moot-on-AP-1 in effect; the tautology is re-measured at the bytes and written into the receipt. No matrix is invented. |
| **AP-31** | MINOR | `markApiReachable` gains the `misconfigured` guard its sibling already carries. |

### D. The `×5`→`×7` correction — **here, and only here**

COHESION **§4:104**'s ApiOfflineChip cell reads, verbatim:

> | ApiOfflineChip | transport cluster ×5 (`demo/platform/transport/**` has no X-wave claimant; incl. the confirmed unbounded cooldown burst AP-17 MAJOR) | `registry/adjudicated/ApiOfflineChip.md` |

**Read `×5` as `×7`** — the adjudication measured **seven** rows, and the index under-counts by two.
**Read *"has no X-wave claimant"* as superseded**: as of 2026-09-17 the claimant is **X-W3**, and as
of this addendum the unit is `X.W3.7`. **COHESION §4's bytes are NOT edited** (`§0j.B:625-626`:
*"correct `×5`→`×7` in the addendum, never in §4's bytes"*), exactly as `X-W0-FOLD.md` §9.3 already
registered the same correction rather than patching it.

### E. `X-W3-FOLD.md` §BoundsDelta *"Not proposed, deliberately"* — superseded for this surface

That block (`X-W3-FOLD.md:704-708`) records `demo/platform/transport/client.ts` as **deliberately not
proposed**, on the measured ground that `AAP-10`/`ATP-13`/`TEP-19` all bank it **NO-WAVE-OWNER** —
*"`demo/platform/transport/` in no X file list"*. That premise was true when the fold was authored and
is **false as of §0j.B**: the file list now exists, and it is this addendum's §A. The fold's bytes are
**not** edited (E-3); the supersession is recorded here, at the wave that took the claim. The block's
other two entries (`demo/palettes/api/*.ts` — F-23's declined deadline; `demo/palettes/useTagEdit.ts`
— X-W7's file) are **untouched by this widening** and stay as the fold left them.

### F. What this addendum does NOT do

It does not widen §4 by one byte beyond `demo/platform/transport/**`; it does not reopen D-1, D-2 or
D-7; it does not touch `docs/tranches/V/vnext/api-contract.source.json` (FROZEN-AS-INHERITED, M-15);
it adds **no** gate to §6's 22 (AP-* are G-F register rows adjudicated in the registry, reported in
the unit's receipt at `execution/A/X-W3.md`, never renumbered into §6's table); and it changes **no**
wave's §Dispositions. `ApiOfflineChip.vue` itself — the component, its register, its vocabulary — is
**X-W7's** and is not written here.

---

## §E-3 ADDENDUM-BESIDE 2026-09-18 — CLOSE STATUS AND ARTEFACT PATHS (§4's close-seat grant)

**Nature.** An **E-3 addendum-beside**, landed by the **VERIFY-ONLY close seat** under §4's grant
on this file (*"modify (status + artefact paths at close)"*). **No byte above this line is edited**
— §2's `Status: planned` line and its four-verb table keep their authored bytes and are read from
here. The close record of authority is `docs/tranches/X/execution/A/X-W3.md` **§Close**.

### A. Status at close — **PARTIAL**, not IMPLEMENTED

Five of seven units sat: `X.W3.1` · `X.W3.6` · `X.W3.7` (the GF-R1 unit added by the addendum
above) · `X.W3.2` · `X.W3.3`. **`X.W3.4` (Fork closure, X.A4) and `X.W3.5` (Enum, moderation
clock, and diff decision, X.A5) NEVER SAT** — no receipt, no commit, no byte; §9 commits **5** and
**6** are **UNLANDED**.

**§6 Hard Gate at close — 12 GREEN · 9 RED · 1 DECISION UNDISPOSED**, each re-measured by the
close seat's own commands at the settled bytes:

- **GREEN (12)**: G-1 (P0) · G-2 · G-3 · G-5 · G-7 · G-9 · G-10 · G-11 · G-17 · G-18 (P0) ·
  G-19 · G-20.
- **RED (9)**: **G-4** (`ESC-W3.1-G4-BOUNDS`) · **G-6** (P0 — the cure is at the bytes, but its
  only probe now meets `428` before the join it measures; `ESC-W3.3-PRECONDITION-TESTS`) ·
  **G-8** (`ESC-W3.3-CAS-CALLERS`) · **G-12 · G-13 · G-14** (X.W3.4 never sat) · **G-15**
  (X.W3.5 never sat) · **G-21** (`ESC-W3-G21`, the handoff row exists at neither candidate home) ·
  **G-22** (`cd api && npm test` is **not** green — 3 failed / 241 passed, all three out-of-bounds
  rows encoding the old contract).
- **DECISION UNDISPOSED (1)**: **G-16**. No named product consumer was produced at wave-open, so
  the default **RETIRED-BY-RECORD** is the live reading — but the terminal disposition this gate
  requires *in this file at close* is **X.W3.5's act**, and the close seat cures nothing.

**§2's four-verb table is CORRECT AS AUTHORED and is not edited**: AUDITED **YES** · SPECIFIED
**YES** · IMPLEMENTED **NO** · VERIFIED **NO**. **§L-18's two quartet passes stand UNSERVED** —
they are downstream of IMPLEMENTED.

**§3a's named triumvirate trigger HAS FIRED and is UNDISCHARGED**: the `Idempotency-Key`
requirement (G-10) breaks the shipped `demo/palettes/api/versions.ts` consumers of
`POST /:slug/fork` and `/revert`, which answer **400** until a client hunk lands
(`ESC-W3.3-DEMO-WRITE-CONTRACT`). Per §3a the failing unit may **not** be redispatched alone.

### B. §8 artefact paths — all under `docs/tranches/X/waves/artefacts/W3/`

| §8 # | artefact | file(s) at close |
|---|---|---|
| 1 | born-RED baselines | `born-red-baseline.txt` · `W3-2-born-red-baseline.txt` · `W3-3-born-red-baseline.txt` · `W3-6-born-red-baseline.txt` · `W3-7-born-red-before.txt` |
| 2 | openapi before/after | **NOT PRODUCED** — only the admin slice `W3-6-openapi-admin-after.json` (45 path rows / 51 ops / 21 admin rows / 23 admin ops). The *before* half is unrecoverable at close; the route surface is measurably unmoved (X.W3.4's plural rename never ran) |
| 3 | api test before/after | `api-test-before.txt` · **`api-test-after.txt`** (close seat, settled bytes, double-run) · `api-test-after-X-W3-1.txt` · `W3-2-api-test-after.txt` · `W3-3-api-test-after.txt` |
| 4 | falsifier demonstration | **not present under its §8 name**; satisfied in substance by 17 L-19 arms across `born-red-baseline.txt` (F-1..F-3) · `W3-2-falsifiers.txt` · `W3-3-falsifiers.txt` · `W3-6-g19-structural-falsifier.txt` · `W3-6-g17-admin-policy-probe.txt` · `W3-7-born-red-before.txt` · the G-18 standing falsifier inside `e2e/smoke/admin/route-guard.spec.ts` |
| 5 | migration run | `W3-2-migration-run.txt` (the `unlisted` → `private` arm is absent — X.W3.5 never sat) |
| 6 | e2e route guard + trace | `W3-6-e2e-route-guard-before.txt` · `W3-6-e2e-route-guard-after.txt` · **`e2e-route-guard.txt`** (close seat's own run, 3 passed). **No Playwright trace** — the smoke projects retain none on pass |
| 7 | §9 commit hashes | the close record's §Close ¶1 roster (24 commits, 53 paths, **zero out of bounds**) |

Also banked: **`close-gate-readings.txt`** (the close seat's own 22-gate transcript) ·
`W3-2-ESC-REVERT-ADDRESS.md` · `W3-3-ESC-CAS-CALLERS.md` · `W3-3-ESC-DEMO-AND-TESTS.md` ·
`W3-7-cluster-after.txt` · `W3-7-boot-smoke-dev.txt` · `W3-7-transport-cluster-record.md`.
