# X.W12U — AUDIT-2 value.js: component cogency, mobile views, hierarchy and space (the consumer halves)

## State
**Opens after**: X-W12 CLOSED. **Blocks**: X-W8.
**Minted by**: COHESION §0cy (2026-09-24). **Model**: Opus 5.5 every seat (owner 2026-09-23); design seats at effort high.
**Units**: 5 serial — [`.x`] → [`.s`] → [`.k`] → [`.m`] → [`.h`]. **X-W12U IS the supplement X-W12 named "X-W12S"** (COHESION §0cy): one successor, not two. **Record**: `docs/tranches/X/execution/A/X-W12U.md`.
**Register (the scope, read whole)**: `docs/tranches/X/audit/AUDIT-2-value.md` (44 rows, `39d76f92`). Frames: `docs/tranches/X/audit/audit-2/value-L{1,2,3}/` (git-excluded, local).
**Glass letter**: `docs/tranches/X/relay/X-ALL-BK-AUDIT-2.md` (O-74; minted as O-69) and its erratum `X-ALL-BK-AUDIT-2-erratum-2026-09-24.md`.

## Authority — the owner's words, verbatim (2026-09-24)
*"ensure that we're not duplicating any component in any view, too: KISS, DRY. Audit our component structure for cogency in every project. All issues should be fixed at the glass-ui root, too … Audit every mobile view for every mobile app view for all projects, too."*
*"and the refresh button should be inline in the section when expanded too. Ensure proper design hierarchy and usage of space in all UIs hereof"*

## Goal criterion
Every CONSUMER and GLASS+CONSUMER row of AUDIT-2-value.md is CURED (falsifier re-run GREEN ×2 on the served page) or carries an honest-RED id whose glass half is a named O-74 row. Every row the register left unmeasured (§2 `.x`) is measured and rowed. value.js stays on glass 7.0.0 in this wave (§0ci R-5): a cure that needs a glass 10.x/11 primitive is recorded as ADOPT-AT-X-W7L with the row id. No consumer copy of producer behaviour.

## Instrument (law, from the AUDIT-2 critic)
- Measure on **:9000** (`scripts/dev/dev.sh up`; never edit dev.sh). If :9000 hangs, fix or diagnose the hang first and record it. A same-tree mirror is only a fallback, and must be named in the record. The audit's value readings came from mirrors (:9102/:9121/:9983), so the glass statuses marked "open-at-HEAD" are source readings, not measurements.
- Headed Chromium on the real GPU (§0be). Viewports: 360×780 · 390×844 · 430×932 · 844×390 · **768×1024 and 1024×768 (tablet; the dock stays expanded below 1024, `Dock.vue:87,:164`)** · 1440×900. Both themes.
- Safe areas are emulated with CDP `Emulation.setSafeAreaInsetsOverride` (the technique is in `audit-2/fourier-L2/probe-safearea.mjs`: portrait bottom 34; landscape left/right 47, bottom 21). "Chromium cannot emulate insets" is retired.
- Every falsifier is a committed script under `docs/tranches/X/waves/W12U-evidence/` (value-L3 saved none).

## §1 Bounds
`demo/**` · `e2e/smoke/**` (new `w12u-*` specs) · `docs/tranches/X/waves/W12U-evidence/**` · the record · `LEDGER.md` (own hunk only) · `INBOX.md` (mail rows). **Do NOT touch**: `glass-ui/**` (READ-ONLY; new glass asks go as a dated O-74 addendum plus the live glass session), `src/**`, `package.json` pins, `scripts/dev/dev.sh`.

## §2 Units
### X.W12U.x (Opus) — measure what AUDIT-2 never saw, then row it
1. **Admin, populated.** Stub `/admin/*` through a Playwright route (the value-L3 seat already stubbed Browse this way), and seed users, flagged palettes, names, tags, audit rows and more than one page. Read tables, rows, the pager, the action groups and the confirm dialogs at 360/390/430/844×390 and 1440, both themes. The fourier twins (A2-FO-L3-1 toolbar; A2-FO-L3-2 action group overflowing its card at 390) are the expected fault class.
2. **`/admin/tags` at 390 rendered Not Found** on the register's run (register, side observation). Decide whether this is the guard for an unauthenticated visitor or an admin route resolving to the catch-all (`router/index.ts:43-55`, `router/guards.ts`). Row it and cure it.
3. **Overlays and sub-states at 360/430/844×390, read for hierarchy too:** the version-history drawer, the palette card menu, the tag-edit popover, the flag-report, migrate and delete-all dialogs, the dock colour-input and slug-edit layers, the logged-in profile menu, and the pane error and loading plates. Seed saved palettes (value-L3 did). Every dialog is a landscape candidate, because glass DialogContent caps its height only under `scroll` (A2-FO-L2-8).
4. **The value half of A2-VA-L2-6** (erratum): the 32×32 scene seats (Regenerate, Save palette, Copy colors) are consumer `<button class="action-button-wrapper">` (`demo/shell/dock/ActionButton.vue:13-20`), which no glass rule reaches. The `<DockControl compact>` "Toggle action bar" (`ActionBarToggle.vue:86-87`) is the glass ask A2-KE-L2-6.
5. **Micro type.** value-L2 recorded an 11 px floor (`label.fira-code.text-micro` on /extract; `span.truncate.text-micro` on /atmosphere) on 18 states per viewport, with no row. Row it against the O-74 §11 micro-floor ask.
6. **Tablet** 768×1024 and 1024×768 on every route.
Each finding becomes an `A2-VA-X-n` row in the record, with severity, a falsifier and a cure, and is cured in `.k`/`.m`/`.h` by its class. Glass halves go into the dated O-74 addendum.

### X.W12U.s (Opus) — the X-W12 UIA-V residue (the "X-W12S" supplement)
1. Scope: RESIDUE-U1 (152 halves), RESIDUE-U2 (89 halves) and RESIDUE-U3 (492 numbered rows, plus 3 held), exactly as listed in `docs/tranches/X/execution/A/X-W12.md` (§ "The named supplement", about line 836 onward) and `docs/tranches/X/waves/W12-evidence/u/disposition-full.tsv`.
2. First, dedupe against AUDIT-2-value and the `.x` rows. A row both registers name is cured once and cited under both ids.
3. The open seat may split `.s` into `.s1`, `.s2`, `.s3` by page (the `.u1`/`.u2`/`.u3` page split), serial. Every row ends CURED, ADOPT-AT-X-W7L (with its glass row), or honest-RED with a relayed id. The disposition table is re-built and self-counted ×2 with zero silent drops.

### X.W12U.k (Opus, effort high) — component cogency (Lens 1: A2-VA-L1-1..24, consumer halves)
1. The rows as the register writes them.
2. **The duplicate census the value-L1 seat never ran.** Build a per-view runtime mount census like keyframes-L1's `probe-mounts.mjs`: every route and every overlay, listing the components mounted more than once for one job. Read, rather than grep: `demo/scenes/about/markdown/Markdown.vue:380-410` (a hand-rolled `.toc`; the fourier TocTree ask A2-FO-L1-2), `demo/picker/controls/ComponentSliders/*`, `SpectrumCanvas`, `ColorComponentDisplay`, and `demo/palettes/export/*`.
3. **Cross-app adoption rows.** When glass ships each primitive, the value copy is retired: the CopyButton (A2-KE-L1-29 names 5 value sites, including `MixResultDisplay.vue:32-33,133-136`, `GradientVisualizer.vue:111` and `GradientEasingEditor.vue:102`), Aurora `followPointer` (A2-KE-L1-26 → `useAtmosphere.ts:377-394`), TocTree (A2-FO-L1-2), Pagination (A2-FO-L1-27 → `demo/palettes/browser/admin/PaginationBar.vue`), the compact Tooltip (A2-FO-L1-14), the `.ios` no-zoom installer (A2-KE-L1-28), ConfirmDialog, and the attribution menu. Before landing, each row reads ADOPT-AT-X-W7L; the consumer does not copy the producer.
4. **Unrowed pairs.** `useSafeStorage.ts` is byte-identical to fourier's (it moves into glass or a shared package only if glass rules so; otherwise keep one owner per app and cross-cite it). AdminFlaggedPanel exists in value and fourier for one job. The two "sampling law" modules over mixColors (`color-session/color-chips/sample.ts` and the workbench one) become one owner.
5. **The easing specimen, cross-app.** `EasingSpecimenStrip.vue` and `easingCatalogue.ts` are a copy of keyframes' EasingTarget gallery, and fourier has its own. Converge on the glass preset strip and the EasingCurve marker API asked in the O-74 addendum. Until glass lands them, record ADOPT-AT-X-W7L.

### X.W12U.m (Opus) — mobile views (Lens 2: A2-VA-L2-1..12, consumer halves)
1. The rows as the register writes them. **A2-VA-L2-12** (safe area) is re-measured with the CDP override before any `viewport-fit=cover` lands. That cure exposed three HIGH overlaps on fourier.
2. `docSH == innerHeight` on every route at every phone width (keyframes-class check; see A2-KE-L2-2).
3. Gates: every row's falsifier ×2 at the Instrument viewports, both themes; the O-74 "reply requested" items are cited, not waited on.

### X.W12U.h (Opus, effort high) — hierarchy and space (Lens 3: A2-VA-L3-1..8, consumer halves)
1. The rows as the register writes them. Apply the owner's 2026-09-24 rule everywhere: a section's refresh/reset action sits **inline in its header when the section is expanded** (O-68 is the glass primitive; value's consumer layout adopts it at X-W7L). Until then, build no consumer copy: record ADOPT-AT-X-W7L.
2. **A2-VA-L3-6 is HELD for glass's §11 ruling** (O-74 erratum): the three type and sizing asks contradict each other, so the consumer does not pick one.
3. Gates: before/after frames at 1440 and 390, both themes, for every row; one primary per region; heading > control label at every width (the §11 ruling decides the mechanism).

## §3 Close
The record lists every AUDIT-2-value row and every `A2-VA-X-n` row as CURED, ADOPT-AT-X-W7L (with the O-74 row) or honest-RED (with the id relayed to glass). `npm run check` 0; `npm test` GREEN; smoke GREEN `--workers=1`; the served falsifiers GREEN ×2. The row flips CLOSED on a CONFORMANT check.

## ADDENDUM 2026-09-24 (COHESION §0de) — value.js is on glass 10.1.0 before this wave opens
The owner ruled *"All should be on 10.1."* X-W7L (the repin to 10.1.0 exact) now runs between X-W12 and X-W12U. This supersedes "value.js stays on glass 7.0.0 in this wave (§0ci R-5)" in the Goal criterion. Every row marked ADOPT-AT-X-W7L is **adopted in this wave** on 10.1.0, using X-W7L `.v`'s per-row reading (CURED-BY-REPIN or still-live). `.h`'s header actions use 10.1.0's `ConfiguratorLayer #actions` directly (unless X-W7L `.a` has already landed them).
