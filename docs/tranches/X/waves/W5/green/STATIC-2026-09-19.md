SERVED MODEL: claude-opus-5[1m]

# X.W5.a — the GREEN slate, measured at the settled bytes 2026-09-19

Every reading **double-run** (identical both passes) and taken from the bytes as
committed, not from intent. Read beside `../born-red/STATIC-2026-09-19.md`.

## Static arms

| gate | command | BEFORE | AFTER | reading |
|---|---|---|---|---|
| **A1** | `node -e "…/await router\.isReady\(\)/…&&(…match(/^import \"/gm)…).length===4…"` | exit **1** | exit **0** | GREEN |
| **A1** (2nd arm) | `grep -cE 'useGlobalDark\|provideApiClient\|^import "\.\./styles' demo/color-picker/App.vue` | **7** | **0** | GREEN |
| **A4** | `grep -rn 'bindPane' demo/ \| wc -l` | **0** | **12** | GREEN (bite below) |
| **A4** (props seam) | `grep -c 'props: Record<string, unknown>' demo/shell/usePaneRouter.ts` | **1** | **0** | GREEN |
| **A5** | fifteen-route walk, `h1 === 1 && main === 1 && visible && text === VIEW_MAP[view].label` | 0 on all | **15 / 15** | GREEN on four arms; the fifth (outline order) is RED on 4 routes — see residuals |
| **A7** | `grep -rn 'role="status"' demo/shell/ demo/color-picker/ \| wc -l` | **2** | **3** | GREEN (≥ 1), and the two live arms are GREEN below |
| **N1** | `grep -rn 'onDeactivated' demo/ \| wc -l` | **0** | **10** (4 teardown sites + 1 re-arm + imports) | GREEN |
| **N3** | one key deleted / one key typo'd → `vue-tsc` | exit **0** (no bite) | exit **2** / exit **2** | GREEN |
| **N4** | `valueOrThrow` on the render-path readers (`convertPickerColor`, `mapPickerOklabToSrgb`, `channelNumber`, `serializePickerColor`, `pickerColorToHex`) | present in all five | **0** | GREEN (the 18 remaining occurrences are `buildColor`'s switch + the helper's own declaration — the CONSTRUCTION path) |
| **N5** | `grep -c '<ErrorBoundary' demo/color-picker/App.vue` | **1** (around the whole grid) | **3** (one per pane seat, outside `<KeepAlive>`) | GREEN on three arms; the route-reset arm is RED — see residuals |
| **N7** | `grep -c ':max="[0-9]' demo/color-picker/App.vue` | **3** literals | **0** | GREEN (`PANE_CACHE_MAX`, derived from `VIEW_MAP`) |
| **N8** | `grep -c 'interface PaneSlot' demo/shell/usePaneRouter.ts` | **1** | **0** | GREEN (`ResolvedPane` — the fold's own alternative, chosen because the gate's grep cannot distinguish a prefix) |
| **RS-1 / DR-14** | `grep -c 'siblingFsAllowTransient' vite.config.ts` | **2** | **0** | GREEN |

### The two falsifiers, run once each (they are the gates' own proofs)

- **A4** — one `bindPane` argument deleted:
  `demo/color-picker/App.vue(154,44): error TS2554: Expected 1 arguments, but got 0.` → `vue-tsc` exit **2**. Restored; clean exit **0**.
- **N3 (a)** — one listener key deleted from the prop bag:
  `usePaneRouter.ts(465,9): error TS2741: Property 'onCancelEdit' is missing in type '{ savedColorStrings: string[]; onCommitEdit: () => void; }' but required in type 'Required<Pick<{ … readonly onCancelEdit?: () => any; } & VNodeProps & …>>'` → exit **2**.
- **N3 (b)** — one listener key misspelled:
  `usePaneRouter.ts(467,13): error TS2561: Object literal may only specify known properties, but 'onCommitEidt' does not exist in type … Did you mean to write 'onCommitEdit'?` → exit **2**.

  Both N3 errors quote `onCommitEdit?: () => any` **from `PalettesPane`'s own
  declared emits** — the seam is derived from the pane's contract, not
  hand-restated, which is exactly what the falsifier demands.

## Live arms (JSON beside this file)

| gate | BEFORE | AFTER |
|---|---|---|
| **A2** | `deeplinkGenerate.marks = [b0,b1,b2]`, terminal | **`[b0,b1,b3,b2,b4]`** — `⊇ {b3,b4}` on the deep link, on `afterHashNavHome`, and on all fifteen walked routes |
| **A6** | `?space=lab&color=lab(92%25…)` on BOTH route classes | **no `?space=` on any of the fifteen** (`bootSilent: true`); the URL-echo arm applies on all three residue spaces (`jzazbz` → Jzazbz · `display-p3` → Display P3 · `rec2020` → Rec. 2020) |
| **A7** | `statusAnnounces:false · h1Distinct:false · mainNameTracksScene:false` | **all five arms true** |
| **A3** | `{mobileChanged:false, desktopChanged:true}` | **UNCHANGED, honest-RED by ruling** (COHESION §0k.3 S-1 — the cure is X-W8's). Its two source arms did move: `unconditionalPaneFlip` **true → false**, `everySlotReports` **false → true (3/3)**, and `dockCommandRegistry` stays **false** — no registry was resurrected here |

## Residuals (RED, each with its measured cause and its owner)

1. **A5's outline arm — RED on 4 of 15 routes** (`picker`, `palettes`, `mix`,
   `blob`): outline `[1, 3, 2, …]`. Cause measured exactly: a single
   `<h3 class="card-title readout">` in `demo/picker/` — the colour VALUE
   readout, styled as a heading, which is not a section heading at all. The pane
   title's own level was the cure this unit could make (`PaneHeader` now emits
   `<h2>`), and it landed: the remaining eleven routes are outline-clean.
   `demo/picker/**` is outside this unit's writable set. One-line cure for its
   owner: the readout is not a heading.
2. **N5's route-reset arm — RED.** Three of four arms are GREEN (per-pane
   containment, the OTHER pane survives a throw, the fallback now paints inside
   `.pane-container`'s positioned box instead of under the atmosphere canvas).
   The fourth — "a route change clears `caught`" — cannot be cured at the
   containment ALTITUDE, which is all COHESION §0k.3 S-7 grants X-W5: keying the
   boundary on the route would remount `PaneSlot` and destroy every `<KeepAlive>`
   cache and WebGL context on every navigation, and the only other shape
   (caching the boundary inside `<KeepAlive>`) is KILLED by ⟨ErrorBoundary R-7⟩.
   The cure is a route watch in the component's own bytes — **X-W7's**, by S-7.
3. **A2's blob arm — RED, and structurally unsatisfiable as written.**
   `.hero-blob-anchor` exists only in `ColorPicker.vue`, and `VIEW_MAP` places
   no `color-picker` pane on any of the five routes the arm names
   (`/#/generate`, `/#/browse`, `/#/gradient`, `/#/extract`, `/#/atmosphere`).
   Measured this pass: `blob: true` on exactly the four picker-bearing routes
   (`picker`, `palettes`, `mix`, `blob`) and `false` on the eleven others. The
   gate's other arm (`marks ⊇ {b3,b4}`) is GREEN everywhere. Escalated as a
   gate-authoring finding, not re-authored here.
4. **`afterHashNavHome` re-acquires `?space=…&color=…`.** Recorded as an
   observation, not a claim: it follows a user NAVIGATION (the probe sets
   `location.hash`), not a boot, and A6's scope is the boot ("fresh context,
   bare `/#/` and `/#/generate`"), which is green. The live sync is deliberately
   left alive — a cure that killed it would be over-cured by A6's own falsifier.
