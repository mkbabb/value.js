SERVED MODEL: claude-opus-5[1m]

# X.W5.b — the static slate, re-run and dated (2026-09-19)

The live born-RED for B1/B2/B3 is the sibling JSON
`B1-B2-B3-layout-utilization-2026-09-19.json`, taken with
`layout-utilization.mjs` (extended per rider A-5) against the pre-cure bytes.
This file is the STATIC half: every grep census unit b turns, read from the
settled bytes and **double-run** (both passes identical), BEFORE and AFTER.

Engine of record for the live numbers: **WebKit 26.4** (the probe's own default,
kept deliberately — the MT-F028 / M-13 baseline these numbers are compared
against was taken on WebKit, so switching engine would have moved the baseline
silently). `PROBE_BASE=http://localhost:9000`.

## The gate censuses

| # | command | BEFORE | AFTER | gate wants |
|---|---|---|---|---|
| B4.a | `grep -c '100dvh' demo/styles/shell.css` | **1** | **0** | `== 0` |
| B4.b | `grep -rl 'svh' demo/` (files) | **0** | **3** | `>= 1` |
| B4.c | `grep -rn 'content-max-h' demo/` (hits) | **13** in 4 files | **3** in 1 file | `== 0` |
| N9 | `grep -rn 'css-emission-probe' demo/` | **2** (`shell.css`, `foundation.css`) | **0** | strike branch |
| N13 | `grep -c '^\s*--select-font:' demo/styles/foundation.css` | **1** | **0** | `== 0` |
| N13 | `grep -rn -- '--select-font' demo/DESIGN.md` documenting it **live** | **2** (`:11`, `:37`) | **0** | `== 0` |
| — | `grep -rn -- '--app-padding-x:' demo/` (live declarations) | **1** | **0** | token dies |
| — | `grep -rn -- '--app-gutter:' demo/` (live declarations) | **0** | **1** (+1 doc line) | token born |
| C7 | source-scoped viewport-dimension `@media` rules | **7** | **6** | see the divergence below |
| C7 | capability `@media` (`prefers-*`/`forced-colors`/`print`/`pointer`/`hover`) | **28** | **28** | MUST survive — untouched |

### B4.c — the residue is one file, and it is not this unit's

The three surviving hits are all **comments** in `demo/shell/PaneSlot.vue`
(`:46`, `:54`, `:55`) — a paragraph and the E-3 correction block appended beside
it by X.W5.a. `PaneSlot.vue` is **outside X.W5.b's writable set**, so this unit
did not touch it; a write there would have been an out-of-set write, which is an
escalation, not a cure. Zero of the three is a live consumer: the consumer
`shell.css:75` named by B4's own falsifier is **deleted here**, and the
definition plus both `@media` re-pins are deleted in `foundation.css`. The
identifier now appears nowhere under `demo/` except in that one comment block,
and nowhere at all in any rule.

Note that the correction block was written to explain that the token capped
`.pane-container` and not the slot. The token no longer exists, so the
paragraph it corrects and the correction itself are both now describing a
mechanism that is gone — which is exactly why the cure is a **deletion of both**
by whoever next holds that file under the D-1 coupled-architecture lock, not a
re-wording by a unit that does not own it.

### C7 — a stated arithmetic divergence, not a re-adjudication

`W5.md:274` stages C7 as **7 → 5 after unit b → 3 after unit c**. Measured at the
bytes, unit b's specified cure subtracts exactly **one** viewport-dimension
query — `foundation.css`'s bare ultrawide aspect-only arm, which set nothing but
the cap and therefore "dies whole" (`W5.md:182`). The sibling width+aspect arm
is ordered to **keep** its `--dock-*` rows (same line), so it survives as a
query. 7 → **6**. The three unit c retires (the deleted control's own file, the
shell dual-grid arm, the status-lamp arm) still land C7 on **3**. The stage
figure is off by one; the endpoint is not. Recorded as a fact for unit c, which
owns the gate.

## The cure, site by site

`demo/styles/shell.css`
- `.app-layout`: `height: <full-viewport dynamic unit>` and `overflow: hidden`
  **deleted**; `min-block-size: 100svh` added; `padding` re-pointed to
  `--app-gutter`.
- `.pane-container`: `max-height: var(<the cap token>)` **deleted**; the inline
  `max-width` clamp re-pointed to `--app-gutter` and otherwise **untouched**
  (the owner-ruled ≈1050px invariant, EB-8's supersession warning).
- the B5 reading and the B6 posture decision written in at their sites.
- the MOB-1 canary claim struck (N9).

`demo/styles/foundation.css`
- the cap token's `:root` declaration **deleted**; the bare ultrawide
  aspect-only `@media` arm **deleted whole**; the width+aspect arm keeps its two
  `--dock-*` rows and loses only the cap row.
- `--app-padding-x` → `--app-gutter` (rename at the definition).
- the `--select-font` pin **deleted** (N13); `--dropdown-menu-font` **kept** — it
  has a live consumer in the installed producer.
- the CI-emission-probe claim struck (N9).

`demo/DESIGN.md` — token-truth only, on the two BD-23 lines (`--select-font`,
N13) and the layout-token block the deletions make false.

`docs/tranches/V/megatranche/audit/probes/layout-utilization.mjs` — extended per
A-5 (assert mode, `docScrollable`, `.pane-container` box + `gridCols`, three
added routes, two added viewport arms), plus an `occupiedExtentPct` arm that
exists because B3's own falsifier demands it: a rack stretched to the band
around short content would pass a rack-only reading and must not.

## The producer measurement N13 rests on

⟨cmd⟩ `grep -rl 'select-font' node_modules/@mkbabb/glass-ui/` → **0 files**.
⟨cmd⟩ `grep -rn 'dropdown-menu-font' node_modules/@mkbabb/glass-ui/dist/components/_shared/menu.css`
→ live: `.dropdown-menu-content, .dropdown-sub-content { font-family: var(--dropdown-menu-font, inherit); … }`.
Installed version: **@mkbabb/glass-ui 7.0.0**.
