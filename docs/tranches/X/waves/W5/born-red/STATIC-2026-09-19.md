SERVED MODEL: claude-opus-5[1m]

# X.W5.a — the BORN-RED static slate, re-run and dated 2026-09-19

`W5.md` §8 requires the wave's static measurements "re-run and dated" beside the
live BORN-RED JSON. Every reading below was taken **read-only at HEAD
`bd1f014f`**, before this unit wrote a byte of product source, and **double-run**
(identical both passes). The spec's own `RED today` figures are dated
**2026-08-03**; a divergence is stated as a fact for this unit, never as a
licence to re-adjudicate the gate.

Live BORN-RED JSON beside this file:

| file | probe | gates it carries |
|---|---|---|
| `A2-A3-A6-app-shell-truth-2026-09-19.json` | `audit/probes/app-wave/app-shell-truth-probe.mjs`, run **unmodified** (W5.md §4: *execute, no write*) | A2 · A3 · A6 (+ A5's `h1` count) |
| `A5-A6-A7-route-scene-truth-2026-09-19.json` | `workflows/gates/route-scene-truth.mjs` (this unit's) | A5 · A6 · A7 |
| `A3-dock-action-parity-2026-09-19.json` | `workflows/gates/dock-action-parity.mjs` (this unit's) | A3's three arms |

## The static readings

| gate | command | BORN-RED reading | vs the spec's 2026-08-03 |
|---|---|---|---|
| **A1** | `node -e "…/await router\.isReady\(\)/…&&(s.match(/^import \"/gm)\|\|[]).length===4…"` | **exit 1** — `main.ts` 168 B, no `isReady` await, **0** side-effect imports | MATCHES |
| **A1** (2nd arm) | `grep -cE 'useGlobalDark\|provideApiClient\|^import "\.\./styles' demo/color-picker/App.vue` | **7** (gate needs 0) | MATCHES |
| **A4** | `grep -rn 'bindPane' demo/ \| wc -l` | **0** — the checker cannot bite | MATCHES |
| **A4** (props seam) | `grep -c 'props: Record<string, unknown>' demo/shell/usePaneRouter.ts` | **1** | MATCHES (fold §2a A4) |
| **A5** | `grep -rn '<h1' demo/ --include='*.vue' \| wc -l` | **0** | MATCHES |
| **A7** | `grep -rn 'role="status"' demo/shell/ demo/color-picker/ \| wc -l` | **2** — `GenericActionBar.vue:19` a comment · `:146` a live `sr-only` region | **DIVERGENT** (spec: 0). The grep arm is green BEFORE the cure; A7 is a three-arm gate and stays RED on the other two, both measured live below |
| **N1** | `grep -rn 'onDeactivated' demo/ \| wc -l` | **0** (against **1** `onActivated` site, `HeroBlob.vue`) | MATCHES |
| **N3** | `grep -c 'onCommit-edit\|onCancel-edit' demo/shell/usePaneRouter.ts` | **2** live literal keys | MATCHES |
| **N4** | `grep -rn 'valueOrThrow' demo/ \| wc -l` | **22** (`picker-color.ts`), incl. the render-path readers | MATCHES |
| **N5** | `grep -rn 'onErrorCaptured' demo/ src/ \| wc -l` | **3** hits, **exactly one carrier** (`ErrorBoundary.vue:59`; `:39` import, `:66` comment) wrapping the whole grid | MATCHES |
| **N7** | `grep -on ':max="[0-9]*"' demo/color-picker/App.vue` | **3** literals — `:99` **9** · `:125` **6** · `:159` **4** | **DIVERGENT** (fold names two; a THIRD literal `:max="4"` exists) |
| **N8** | `grep -c 'interface PaneSlot' demo/shell/usePaneRouter.ts` | **1** | MATCHES |
| **RS-1 / DR-14** | `grep -c 'siblingFsAllowTransient' vite.config.ts` | **2** — `:139` declaration, `:287` use | MATCHES §0j.A DR-14 exactly |

## The live readings at open (from the JSON beside this file)

- **A2** — `deeplinkGenerate.marks = [b0, b1, b2]`, **terminal**: the overture
  never reaches B3 or B4 on a deep-linked route, for the whole session.
  `blob: false`. `afterHashNavHome` reproduces the same three marks.
- **A3** — `{ mobileChanged: false, desktopChanged: true }`, reproducing the RED
  of record byte-for-byte; the fold's **720×900 arm** (≡ 1440 at 200 % zoom)
  reproduces the MOBILE reading (`dockChanged: false`), confirming ⟨Dock G-I⟩
  from this end. Beside it, an X-W4 improvement worth recording: the mobile
  Regenerate now renders `aria-disabled` rather than present-and-inert — the
  `unavailable` state is surfaced, and the parity failure is no longer silent.
- **A6** — `href` settles at
  `…/#/generate?space=lab&color=lab(92%25+88.8+20+/+82.7%25)` on the deep link
  AND at `…/#/?space=lab&color=…` on the control home: state no user action
  produced, on both route classes.
- **A5/A7 (live)** — `h1: 0` on all fifteen walked routes; `mainCount: 1`;
  outline begins at level **3** on every route; the shell carries **0** polite
  status regions; `/#/does-not-exist` renders with the same `<h1>` (none) and
  announces nothing. `renderDistinct` is the one arm already true at open (the
  not-found pane's text differs), which is why A7's verdict rests on all five.

## The D-E portal-integrity delta (W5.md §8, mandatory on an `index.html` body change)

Taken with `workflows/gates/portal-integrity.mjs` immediately BEFORE and AFTER
the body edit, same session, same viewport. Full JSON at
`../green/PORTAL-INTEGRITY-DELTA-2026-09-19.json`.
