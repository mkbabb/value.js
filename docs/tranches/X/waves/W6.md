# X.W6 - Instruments and Temporal Scenes

**Name**: W6 - Instruments and Temporal Scenes
**Opens after**: X-W5 (one route, one scene)
**Agents**: 10 units in 4 serial lanes (owner cap: max 4 concurrent workflows)
**Hard gate**: **50 acceptance gates** — 46 born-RED with pasted failing input · 1 non-regression (H1) · 1 MEASURE-AT-OPEN (j4) · 2 procedural closure conditions (H3/H4); the Gradient triad, the space catalog, the atmosphere harness and the four scene contracts all close in this repo against installed glass 7.0.0
**Status**: planned

**Authority**: `docs/tranches/V/megatranche/registry/CARRY-CUT-LEDGER.md` §1.H (CC-056..CC-067) + §2 X-W6 row. Folded inputs: CC-005 (V·MT4), CC-068 (V·MT-W-CSS1).
**Four-verb status**: AUDITED yes (per-row, cited below) · **SPECIFIED by this file** · IMPLEMENTED no · VERIFIED no. This file makes the wave SPECIFIED and nothing more.
**Measurement stamp**: every "measured today" figure below was taken read-only on **2026-08-03** at HEAD `c654824e` (branch `tranche-u`), node against the repo tree and installed `@mkbabb/glass-ui@7.0.0`. Figures marked *(adjudicated)* are inherited from the dated adjudication records and are **re-measured at wave open** (D-19 line-anchor law) before any cure.

---

## 2a. Goal criterion

This wave succeeds if each live instrument — Gradient, the space catalog, Easing, HeroBlob, Blob/Atmosphere, Mix — owns its own stage, inspector and action contract as a routed scene, tells one geometric and one colour-science truth at every size, and carries its motion under the shared field/motion law — **with no chassis-shaped abstraction rebuilt on our side** (CC-108: the producer's InstrumentChassis is dead, so "no copied chassis" is a fact to preserve by subtraction, not a prohibition to police).

---

## 3. Scope

1. **Gradient model + gesture + geometry core** — normalise-on-write with stop-owned easing (both, together); one axis over one box; grabDx; primary-button-only mint; cancel-only-disarm; delete the drag double-path, the second selection channel, the parallel `intervals` array + its watcher + three throws, the masking default, the dead injection, and the contextmenu removal species. (CC-058 · MT-GRADSTOP-1 r3)
2. **Gradient seat recomposition** — seat rail frees the meniscus; selected-stop inspector with numeric entry; full keyboard grammar (create + Home/End/arrows + ordinal announcement); one removal owner with an explicit disabled state and its reason; crowding cured by disambiguation, never by a separation law. (CC-058 · MT-GRADSTOP-2, less the WatercolorDot-faces limb — see §Blocked)
3. **Gradient sampling law and literal dialect** — one `sampleAt`; the ` in <space>` clause emitted; leaf `model/types.ts` homing the stop-owned `GradientStop` brand; one colour-literal grammar across authored and minted stops; the seven dead re-export doors deleted. (CC-058 · MT-GRADSTOP-3)
4. **Easing instrument coherence** — one radius scale derived from the panel's own register, the preset chips deciding once between the icon-button circle and the shared rounded-rect; the `cubic-bezier(...)` readout becomes the published glass field primitive or a dated glass-forward ask with its census pasted. (CC-060, CC-063)
5. **Gradient selector aurora** — the ramp gains a subtle aurora-class wave through the shared field/motion law consumed from `@mkbabb/glass-ui/aurora`; no new animation species, no private rAF loop, PRM honoured structurally. (CC-064)
6. **The space catalog becomes one total type; the specimen line one legible, honest sentence** — `SPACE_CATALOG satisfies Record<DisplayColorSpace, SpaceEntry>`, `formatSpecimen` total over all 18 members, gamut MARKED never projected. (CC-068, re-authored as **X:CSS-1**; the ledger supersedes the `V·MT-W-*` labels)
7. **Picker card rhythm and About alignment** — the headline card's largest vertical interval derives from the type scale, not from leftover flex slack; About joins the shared grid row Mix already sits on. (CC-059, CC-062)
8. **HeroBlob chroma fidelity** — measure the pipeline (1×1-canvas sRGB resolver vs `deriveBlobPalette` vs shader perturbation) before cutting; the blob carries the current colour's chroma. (CC-061)
9. **The atmosphere harness** — one capture harness serving three rows: the DR-01 supersession tombstone + the re-minted **X:ATMO-1** response oracle (deltaE2000 from committed frames), the dark-lBand door, and the cold-load arm-replay. (CC-065, CC-066, CC-067)
10. **Scene adoption, route by route** — Gradient pilot, then Mix canary (one interaction owner, no nested controls, canvas lifetime isolated), then persistent Blob/Atmosphere preview with a loss/recovery contract and the short-landscape last-control proof. (CC-056 · V·L3, CC-057 · V·L4)

---

## 3a. Triumvirate Dispatch

Triumvirate (research + plan augment + redress) is **mandatory**, not optional, on any of:

- **File-bound expansion that invalidates the wave**: any unit needing a write outside §4 — in particular any edit under `src/`, `api/`, `demo/color-picker/App.vue`, or the shell/dock trees (those are X-W3/X-W5/X-W8 property).
- **Non-local hard-gate failures**: (a) the Gradient ordering cure failing G1 *and* G2 after the sort+stop-owned-easing landing — that is a model-shape error, not an edit error; (b) X:CSS-1's G6(ii) anti-projection lock going RED, which means a cure shipped a regression under a green gate; (c) the atmosphere oracle unable to produce reproducible committed frames across two runs.
- **Third-iteration diagnostic halts**: three failed attempts at the one-axis cure (G3/G4/G12 as a set); three failed attempts at the Mix canvas-lifetime isolation; three failed cold-load captures that disagree with each other.
- **Producer-boundary surprise**: any unit concluding its cure requires a glass-ui change not already dispositioned here — it halts and files the ask, it does not grow local mechanics (masking-fallback ban).

---

## 4. File Bounds

| File | Access |
|---|---|
| `demo/workbenches/gradient/composables/useGradientModel.ts` | modify |
| `demo/workbenches/gradient/composables/useGradientCSS.ts` | modify |
| `demo/workbenches/gradient/composables/gradientParse.ts` | modify |
| `demo/workbenches/gradient/composables/useGradientInterpolation.ts` | modify |
| `demo/workbenches/gradient/model/types.ts` | create |
| `demo/workbenches/gradient/model/sample.ts` | create |
| `demo/workbenches/gradient/GradientVisualizer/GradientStopEditor.vue` | modify |
| `demo/workbenches/gradient/GradientVisualizer/GradientVisualizer.vue` | modify |
| `demo/workbenches/gradient/GradientVisualizer/easing/GradientEasingEditor.vue` | modify |
| `demo/workbenches/gradient/GradientVisualizer/easing/EasingAuthoringStage.vue` | modify |
| `demo/workbenches/gradient/GradientVisualizer/easing/easingCatalogue.ts` | modify |
| `demo/workbenches/gradient/GradientPane.vue` | modify |
| `demo/color-session/space-catalog.ts` | create |
| `demo/color-session/specimen-format.ts` | create |
| `demo/color-session/ColorSpaceSelector.vue` | modify |
| `demo/color-session/color-model.ts` | modify-carve |
| `demo/color-session/useContrastSafeColor.ts` | modify |
| `demo/picker/ColorPicker.vue` | modify |
| `demo/picker/display/ColorNutritionLabel.vue` | modify-carve |
| `demo/picker/visual/HeroBlob.vue` | modify |
| `demo/scenes/about/AboutPane.vue` | modify |
| `demo/scenes/atmosphere/AuroraPane.vue` | modify |
| `demo/scenes/blob/BlobPane.vue` | modify |
| `demo/color-picker/composables/boot/useAtmosphere.ts` | modify |
| `demo/color-picker/composables/boot/atmosphere-calibration.ts` | modify |
| `demo/workbenches/mix/MixPane.vue` | modify |
| `demo/workbenches/mix/MixConfigBar.vue` | modify |
| `demo/workbenches/mix/MixSourceSelector.vue` | modify |
| `demo/workbenches/mix/MixAnimationCanvas/MixAnimationCanvas.vue` | modify |
| `demo/workbenches/mix/MixAnimationCanvas/composables/useMixingAnimation.ts` | modify |
| `demo/test/glass/aurora-bracket.test.ts` | modify |
| `test/gradient-order-invariant.test.ts` | create |
| `test/interpolation-subset.test.ts` | create |
| `e2e/smoke/views/gradient.spec.ts` | modify (ADD-never-replace) |
| `e2e/smoke/oracles/o21-gradient-rail.spec.ts` | modify (ADD-never-replace) |
| `e2e/smoke/oracles/o21-space-catalog-truth.spec.ts` | create |
| `e2e/smoke/oracles/o22-specimen-legibility.spec.ts` | create |
| `e2e/smoke/oracles/o23-specimen-gamut-honesty.spec.ts` | create |
| `e2e/smoke/oracles/o24-specimen-dot-identity.spec.ts` | create |
| `e2e/smoke/oracles/o25-atmosphere-response.spec.ts` | create |
| `e2e/smoke/oracles/o26-atmosphere-coldload.spec.ts` | create |
| `e2e/smoke/oracles/o27-scene-contracts.spec.ts` | create |
| `docs/tranches/X/gates/gate-literal-dialect.mjs` (c3) | create |
| `docs/tranches/X/gates/gate-catalog-totality.mjs` (f1) | create |
| `docs/tranches/X/gates/gate-specimen-grammar.mjs` (f5) | create |
| `docs/tranches/X/gates/gate-blob-pipeline.mjs` (h2) | create |
| `docs/tranches/X/gates/gate-lband-door.mjs` (i2) | create |
| `docs/tranches/X/gates/gate-no-chassis.mjs` (H1) | create |
| `docs/tranches/V/megatranche/audit/probes/x-w6/gate-easing-radius.mjs` (d1) | create |
| `docs/tranches/V/megatranche/audit/probes/x-w6/gate-easing-readout.mjs` (d2) | create |
| `docs/tranches/V/megatranche/audit/probes/x-w6/gate-prm-idiom.mjs` (e2) | create |
| `docs/tranches/V/megatranche/audit/probes/x-w6/gate-card-rhythm.mjs` (g1) | create |
| `docs/tranches/X/waves/W6-*.md` (tombstones, censuses, letters) | create |
| `docs/tranches/X/waves/W6-evidence/**` (frames, gate transcripts — §8) | create |
| `docs/tranches/V/megatranche/audit/probes/wb-gradient-stopeditor/gate-axis.mjs` | execute, no write (a3's one-axis gate + a4's G1b inverse-map arm, re-run unmodified) |
| `docs/tranches/V/megatranche/audit/probes/wb-gradient-stopeditor/gate-gesture.mjs` | execute, no write (a5's arm A, a6's arm B and a7's arm C, re-run unmodified) |
| `docs/tranches/V/megatranche/audit/probes/wb-gradient-stopeditor/gate-seat.mjs` | execute, no write (b1's keyboard arm, b2's add-path arm and b4's removal arm) |
| `docs/tranches/V/megatranche/audit/probes/wb-gradient-stopeditor/gate-structure.mjs` | execute, no write (c1's ten-condition structural gate) |
| `docs/tranches/V/megatranche/audit/components/wb-gradient-stopeditor/evidence/parse-probe.ts` | execute, no write (a1's serialize-then-parse probe, run under `vite-node`) |
| `docs/tranches/V/megatranche/audit/components/wb-gradient-stopeditor/evidence/WBGSE-O-r3-crossdrag-forcedcolors.mjs` | execute, no write (a2's first block — the live crossing round-trip) |
| `docs/tranches/V/megatranche/audit/components/wb-gradient-stopeditor/evidence/WBGSE-O-r3-gestures.mjs` | execute, no write (a7's block C14) |
| `docs/tranches/V/megatranche/audit/components/wb-gradient-stopeditor/evidence/p3.mjs` | execute, no write (a8's T9) |
| `docs/tranches/V/megatranche/audit/components/wb-gradient-stopeditor/evidence/p4.mjs` | execute, no write (a9's T11, the playwright-webkit iPhone-14 cell) |
| `docs/tranches/V/megatranche/audit/components/wb-gradient-stopeditor/evidence/p2.mjs` | execute, no write (a10's T2) |
| `docs/tranches/V/megatranche/audit/components/wb-gradient-stopeditor/evidence/WBGSE-D-probe4.mjs` | execute, no write (a11's contrast-floor probe) |
| `docs/tranches/V/megatranche/audit/components/wb-gradient-stopeditor/evidence/WBGSE-D-probe2.mjs` | execute, no write (a12's block 5) |

**Bounds law**: every script a gate invokes is bounded above. The ten new instruments — invoked by c3, d1, d2, e2, f1, f5, g1, h2, i2 and H1; `docs/tranches/X/gates/` and `…/audit/probes/x-w6/` are both **absent today** *(measured 2026-08-03)* — are authored by this wave, so writing them is **inside** bounds and does not trip §3a's expansion trigger. The twelve instruments this wave invokes read-only — the four `…/probes/wb-gradient-stopeditor/gate-{axis,gesture,seat,structure}.mjs` gates and the eight `…/components/wb-gradient-stopeditor/evidence/*` probes, all verified present today — each carry their own row above, marked `execute, no write` with the invoking gate id named; no invocation is exempted by prose (R-E, W10's idiom). A unit that concludes it must *edit* one of those existing instruments halts under §3a instead.

**Do NOT touch**: `src/**` (X-W9 property — the parser R1 crash is X-W9's, consumed here only through Bank 1) · `api/**` (X-W3) · `scripts/dev/dev.sh` (unowned dirty row, NEVER touch, standing law) · `demo/color-picker/App.vue` and `demo/color-picker/router/**` (X-W5 shell / X-W3 guards) · `demo/ui/**` and `components.json` (X-W8) · `demo/palettes/**` (X-W7) · `.github/workflows/**` (X-W1) · `node_modules/@mkbabb/glass-ui/**` (producer, read-only census only) · any `docs/tranches/V/megatranche/registry/adjudicated/*` (immutable adjudication records; E-3 addenda only).

---

## 4a. Disjointness

No two units in the same lane run concurrently, and no two lanes share a `modify`/`modify-carve` path. Verified against §4:

- `GradientStopEditor.vue`, `GradientVisualizer.vue`, `GradientEasingEditor.vue` are touched by units **.a/.b/.c/.d/.e** — all four sit in **Lane 1, strictly serial** in the adjudicated order (MT-GRADSTOP-1 → 2 → 3 → easing → aurora). Never parallel.
- `demo/picker/ColorPicker.vue` and `demo/scenes/about/AboutPane.vue` are touched by **.f** and **.g** — **Lane 2, serial** (.f → .g).
- `AuroraPane.vue` is touched by **.i** and **.j** — **Lane 3 then Lane 4, serial**.
- `demo/picker/visual/HeroBlob.vue` + `useContrastSafeColor.ts` (**.h**) share no path with Lane 1, Lane 2 or `.i`.
- Lane 4 (**.j**) opens only after Lanes 1–3 land; it writes route-private scene files and the Mix tree, which no other unit touches.
- The ten new gate/probe scripts are each `create`d by exactly one unit — c3 (.c), d1/d2 (.d) and e2 (.e) in Lane 1, g1 (.g) and f1/f5 (.f) in Lane 2, h2 (.h) and i2 (.i) in Lane 3, H1 at wave level. No two lanes write one script path.

---

## 4b. Worktree Plan

Four lanes write concurrently → four sibling worktrees. No `CARGO_TARGET_DIR` (no Rust); the per-lane isolation variable is `VITE_CACHE_DIR` so parallel vite/vitest runs do not share a dep-optimise cache.

| Agent unit | Sibling worktree absolute path | Isolation |
|---|---|---|
| X.W6.a–e (Lane 1, serial) | `/Users/mkbabb/Programming/value-x-w6-gradient` | `VITE_CACHE_DIR=/Users/mkbabb/Programming/value-x-w6-gradient/.vite-l1` |
| X.W6.f–g (Lane 2, serial) | `/Users/mkbabb/Programming/value-x-w6-catalog` | `VITE_CACHE_DIR=/Users/mkbabb/Programming/value-x-w6-catalog/.vite-l2` |
| X.W6.h–i (Lane 3, serial) | `/Users/mkbabb/Programming/value-x-w6-atmosphere` | `VITE_CACHE_DIR=/Users/mkbabb/Programming/value-x-w6-atmosphere/.vite-l3` |
| X.W6.j (Lane 4, after 1–3) | `/Users/mkbabb/Programming/value-x-w6-scenes` | `VITE_CACHE_DIR=/Users/mkbabb/Programming/value-x-w6-scenes/.vite-l4` |

The orchestrator runs `git worktree list` and `git worktree add` before dispatch. Each lane owns one dev server on a distinct port (Lane 1 :9000, Lane 2 :9001, Lane 3 :9002, Lane 4 :9003) — **and the STALE-SERVER LAW binds**: no literal/precision/serializer claim is witnessed against a long-running dev server; the authoritative cell is headless `vite-node` against the tree or a freshly restarted server (GRADSTOP-A §7, proven 2026-07-27).

---

## 5. Agent Units

Model law **M-23**: every unit below is an **Opus implementation seat**. No unit authors design canon — the radius canon echo (CC-060), the shadow/spacing laws behind CC-059/CC-062, and the Movement-of-Momentum driver/observer carve behind CC-064 are **X-W10 content**, twice-authored Fable ∥ Opus → fresh-Fable apotheosis. This wave *consumes* whatever X-W10 has ratified and *records* what it needs; it does not mint canon.

### X.W6.a Gradient ordering, gesture and geometry core (CC-058 · MT-GRADSTOP-1 r3)

- **Goal**: the gradient model can no longer serialize CSS its own parser rejects, and no gesture can teleport, mint, or destroy a stop the user did not aim at.
- **Mechanism**: `GradientStop` gains `easing` (the interval hangs on the stop that OPENS it); the `intervals` parallel array, its length watcher and the index-keyed `updateInterval` are DELETED; ONE normalising mutator `setStopPosition(id, position)` (map → stable sort; **equality stays legal** — CSS hard stops require coincident positions and `gradientParse` admits them with `<`, never `<=`); ONE axis (ramp painted on the handle track, both languages reading one custom property, one map via `clamp`+`scale` from `@mkbabb/value.js/math`); `grabDx` against a rect cached at pointerdown; first emit gated on the existing 4px dead-zone; `e.preventDefault()` deleted; `if (e.button !== 0) return` atop both pointerdowns; a dedicated `pointercancel` handler that ONLY disarms; interim dual-contrast resting/selected ring generalized from the file's own recipe; `setStopsFromColors` validated through the `parseCssColor` oracle.
- **Binding cure law (GRADSTOP-A §14)**: normalise-on-write and stop-owned easing land **together**. Clamping the sole mutator is BANNED (it silently deletes drag-across-a-neighbour — the D-8 refusal species). Splice-at-insert is BANNED (it does the migration twice; no-backwards-compat law). **GRADSTOP-A §15**: no minimum-ordinal-spacing rule for stops — crowding is cured by disambiguation, never by forbidding coincidence.
- **Files**: `useGradientModel.ts`, `useGradientCSS.ts`, `gradientParse.ts`, `GradientStopEditor.vue`, `GradientVisualizer.vue`, `GradientEasingEditor.vue`, `GradientPane.vue`, `test/gradient-order-invariant.test.ts` (new), `e2e/smoke/views/gradient.spec.ts` + `o21-gradient-rail.spec.ts` (ADD-never-replace).
- **Sub-gate**: all 13 gates below GREEN, each by the same command that is RED today.

| # | Gate | Command | RED today — the failing input |
|---|---|---|---|
| a1 | The model cannot serialize what its own parser rejects; an authored easing never migrates | `npx vite-node docs/tranches/V/megatranche/audit/components/wb-gradient-stopeditor/evidence/parse-probe.ts` → end state `npx vitest run test/gradient-order-invariant.test.ts` | the drag's own output REJECTS `stop positions must be non-decreasing`; a `steps()` authored on 50→100 re-attaches to 25→50 after a 25% insert *(adjudicated 2026-07-28; instrument verified on disk today)* |
| a2 | Live crossing round-trip: leftmost handle past its neighbour, readout re-fed, verdict null | `node …/evidence/WBGSE-O-r3-crossdrag-forcedcolors.mjs` (first block) → e2e `-g "neighbour-crossing drag round-trips"` | labels [84,50,100]; emitted CSS descends; computed background descends *(adjudicated)* |
| a3 | ONE AXIS: \|handleCentreX(p) − rampX(p)\| ≤ 1px, p ∈ {0,50,100} | `node …/probes/wb-gradient-stopeditor/gate-axis.mjs` | **±11.0px** at the terminals (centres 235/675 vs ramp 224/686); 0 only at p=50 *(adjudicated; §17 count-scoping binds — ±11.0 DOM, never ±10.5 PNG)* |
| a4 | The maps are inverse: forward(inverse(p)) = p ± 0.05 at p ∈ {0,100}, read from `style.left` **never** aria-label | `gate-axis.mjs` (G1b arm) | forward(inverse(0)) = 0.226 — border-box forward vs padding-box inverse *(adjudicated; GRADSTOP-A §6 readout-vacuity)* |
| a5 | A grab is not a teleport: press anywhere in the advertised hit region, move 1px → stop moves ≤ dead-zone | `node …/probes/wb-gradient-stopeditor/gate-gesture.mjs` (arm A) | 8px off-centre press + 1px travel → **10.11px** handle travel, label 0%→2% *(adjudicated, two independent workers)* |
| a6 | Pointer select FOCUSES: after a real mouse click `activeElement` IS the handle and ArrowRight moves it | `gate-gesture.mjs` (arm B) | `activeElement` = BODY; 2×ArrowRight no effect *(adjudicated)* |
| a7 | Only the primary button mints; cancel only disarms | `gate-gesture.mjs` (arm C) + `WBGSE-O-r3-gestures.mjs` (block C14) | middle-click 2→3 (no caveat); right-click 3→4 (chromium cell); post-cure a `pointercancel` in the dead-zone must mint NOTHING *(GRADSTOP-A §12)* |
| a8 | One gesture, one owner: one pointermove on a captured handle = ONE position write | `node …/evidence/p3.mjs` (T9) | 2 emits + 2 eager `stops.map` rebuilds per move. **No serialization-cost delta may be claimed** (GRADSTOP-A §8 — measured 1× per move already) |
| a9 | No tap that grabs may delete (coarse cell): destructive and grab hit regions disjoint | `node …/evidence/p4.mjs` (T11), playwright-webkit iPhone-14 | tap handle-centre+20px → 3→2, silent; the 8px band 234.7..242.7 inside the rail, unpainted *(adjudicated)* |
| a10 | The chip reserves its own ground: its border box intersects no sibling rule | `node …/evidence/p2.mjs` (T2) | chip paints y 244.7→268.7 across the hr at 260.7; `elementFromPoint` returns the chip *(adjudicated)* |
| a11 | Contrast floor (interim): resting/selected handle boundary:fill ≥ 3:1 at every stop of the white-ramp input | `node …/evidence/WBGSE-D-probe4.mjs` | `#ffffff→#fafafa` → ring:fill **1.00:1**, handles invisible (committed shot `D-03-white-ramp-handles.png`); ten-hue ramp 10/10 below 3:1 at 1.87–2.43 *(§17: "6/10" may only be cited against the challenger's own ramp)* |
| a12 | Type-scale containment: at rootFS 20px the 0% handle stays inside the rail | `node …/evidence/WBGSE-D-probe2.mjs` (block 5) | handle 25px, JS inset literal 10 → **+1.50px overhang** — the component's own comment claims this is fixed *(adjudicated; GRADSTOP-A §11)* |
| a13 | Test truth: zero assertions target retired DOM; the amended specs pass; the model has an executable owner | `npx playwright test e2e/smoke/views/gradient.spec.ts e2e/smoke/oracles/o21-gradient-rail.spec.ts --project=smoke` + `npx vitest run test/gradient-order-invariant.test.ts` | 5 failed / 8 passed; 4 dead-DOM assertions (o21:135/:163, gradient:57/:112); the model's only `test/` reference is a TYPE import — deleting `.sort()` keeps every assertion in the repo green *(GRADSTOP-A §13 vacuous-suite)* |

**Falsifiers (L-19)**: a1 fails if a position write produces a string its own parser rejects, or if an authored easing re-pairs on insert. a3/a4/a12 fail if the two languages ever compute different pixels for the same ordinal — including at any future type-scale change (a12 is a13's tripwire and D-13's). a5/a6/a7 fail if a gesture's effect is not a function of its displacement, its focus, or its button. a11 fails on any input ramp, not just white. a13 fails if a mutation to `HANDLE_HALF` or `.sort()` leaves the suite green.

### X.W6.b Gradient seat, inspector and keyboard grammar (CC-058 · MT-GRADSTOP-2)

- **Goal**: a keyboard-only or touch-only user can create, position, inspect and remove a stop with the same authority a mouse user has, and no capability is expressed as silence.
- **Mechanism**: seat rail frees the meniscus (the masked left-edge band gains an add path); a selected-stop inspector owns numeric position entry (PR-07) and is the **one removal owner** with an explicit disabled state carrying its reason (`canRemove` already exposed by .a); full §5.2 keyboard grammar including a create path (Home=0%, End=100%, Up/Down where supported, unconditional ordinal announcement); crowding disambiguated by seat/fan-out/overflow; role/`aria-value*`/selection state authored; the flat heading outline corrected so the protagonist is named at instrument rank.
- **Files**: `GradientStopEditor.vue`, `GradientVisualizer.vue`, `GradientPane.vue`, `e2e/smoke/oracles/o21-gradient-rail.spec.ts`.
- **Sub-gate**: 4 gates GREEN.

| # | Gate | Command | RED today |
|---|---|---|---|
| b1 | Keyboard grammar total: create, Home, End, arrows, PageUp/PageDown all act; ordinal announced | `node …/probes/wb-gradient-stopeditor/gate-seat.mjs` (keyboard arm) | Home/End/Up/Down/PageUp/PageDown/Space **absent**; **no keyboard path to CREATE a stop exists at all**; role/`aria-value*` null *(adjudicated)* |
| b2 | The rail seats every addable position: a press at the meniscus mints | `gate-seat.mjs` (add-path arm) | `bar.x+3` mints nothing; each stop sterilises 44px (13.6% of the 324px mobile rail) with zero feedback; left edge dead from frame one *(adjudicated)* |
| b3 | Numeric position entry exists and round-trips: type 37.5 → `style.left` agrees within the axis tolerance | new e2e `-g "stop inspector numeric entry"` | position exists only in the whole-percent aria-label and the CSS string; **no numeric entry in the tree** *(adjudicated, PR-07)* |
| b4 | One removal owner; the floor is an explicit disabled state with its reason, never absence or an early return | `gate-seat.mjs` (removal arm) | three destructive species at source, zero confirm/undo on the route, the floor expressed as `v-if` absence + silent early return *(adjudicated D2-10 / R2-A1)* |

**Falsifiers**: b1 fails if any listed key is a no-op or the ordinal is announced conditionally. b2 fails at any rail width where an addable ordinal has no press that reaches it. b3 fails if entry and paint disagree. b4 fails if a second code path can remove a stop, or if the floor is reachable without a stated reason.

### X.W6.c Gradient sampling law, literal dialect and leaf types (CC-058 · MT-GRADSTOP-3)

- **Goal**: one sampling implementation, one colour-literal grammar, one leaf type module — the dogfood proof stops shipping two of each.
- **Mechanism**: leaf `model/types.ts` lands first (it homes the stop-owned `GradientStop` brand and breaks both type-only cycle edges); `model/sample.ts` replaces the exact-vs-32-discretised pair with one `sampleAt`; the emitted gradient carries its ` in <space>` clause; `formatColorLiteral` gives authored and minted stops one dialect; the seven dead VALUE re-export doors are deleted; the two-site paint-stack dedup rider lands (`.gradient-rail`, `.gradient-render-tile` — **two sites, not four/six**, §17).
- **Files**: `model/types.ts` (new), `model/sample.ts` (new), `useGradientModel.ts`, `useGradientCSS.ts`, `useGradientInterpolation.ts`, `GradientVisualizer.vue`.
- **Sub-gate**: 4 gates GREEN. **The ordering-policy question does NOT reopen here — it is settled in .a (GRADSTOP-A §14).**

| # | Gate | Command | RED today |
|---|---|---|---|
| c1 | One structure: leaf types, one sampler, the space clause | `node …/probes/wb-gradient-stopeditor/gate-structure.mjs` | **exit 1, 10 failures**: no `model/types.ts`, no `model/sample.ts`, no ` in <space>` clause, the 7-name dead re-export block *(adjudicated; instrument verified on disk today)* |
| c2 | Two samplers become one: ghost and rail agree to the sample | `npx vitest run test/gradient-order-invariant.test.ts -t "one sampling law"` | visualizer samples exactly (`:64-88`), `sampleCoalescedStops` at 32 steps — measurably non-equivalent *(adjudicated L-4)* |
| c3 | ONE literal dialect: authored and minted stops print in one grammar | `npx vite-node docs/tranches/X/gates/gate-literal-dialect.mjs` (new; mints a stop through the model's own mint path and prints its literal beside an authored stop's, headless against the tree — **never the long-running server**, GRADSTOP-A §7) | minted `oklch(74.32% 0.15204 153.16deg)` sits beside authored `oklch(0.65 0.18 265)` in one copyable readout *(adjudicated; the "15-significant-digit" characterization is STALE AT THE TREE and may not be cited)* |
| c4 | Dead doors gone; the interpolation set derives from the catalog | `npx vitest run test/interpolation-subset.test.ts` | RED — no catalog exists until .f lands; the 9 `INTERPOLATION_SPACES` labels verbatim-duplicate `PICKER_SPACE_NAMES` and membership is hand-maintained *(ColorSpaceSelector L-15 replacement rider)* |

**Falsifiers**: c1 fails on any of its ten structural conditions independently. c2 fails if ghost and rail diverge at any t. c3 fails if one readout shows two grammars. c4 fails if membership drifts from the catalog — and is **sequenced strictly after .f** (the rider's own law).

### X.W6.d Easing instrument coherence (CC-060 · MT-F030; CC-063 · MT-F037)

- **Goal**: the Easing configurator reads as one instrument — one radius register, one input register — and any part of that which belongs to the producer leaves as a dated ask rather than a local restyle.
- **Mechanism**: one radius scale derived from the panel's own glass register; the preset chips choose **once** between the icon-button circle and the shared rounded-rect (not both adjacent); the `cubic-bezier(...)` readout row composes the published glass field primitive. **Census-first**: the installed `@mkbabb/glass-ui@7.0.0` exports map ships `./labeled-field` and `./number-field`; `./input` is **not** a published subpath and `LabeledInput` declares **no trailing/leading slot** *(measured today)*. If the open-time census confirms that, the readout's cure is a dated glass-forward BJ ask with the census pasted — **never a local restyle** (M-14 clause 1, glass-first law). The glass I-9 Chip residual stays glass-owned and is not touched.
- **Files**: `GradientEasingEditor.vue`, `EasingAuthoringStage.vue`, `easingCatalogue.ts`.
- **Sub-gate**: 2 gates GREEN.

| # | Gate | Command | RED today |
|---|---|---|---|
| d1 | One radius register: every radius in the easing panel derives from the panel's scale or is a declared icon-button circle | `node …/probes/x-w6/gate-easing-radius.mjs` (new; computes `border-radius` over the panel's element set) | **four unrelated registers in one instrument** — circular preset chips (50%), a small-radius strip, a large-radius pill, an outer card at a fourth — owner witness `audit/visual/owner-marked/OM-4-easing-radius-incoherence.png` sha256 `5b7906f3…` |
| d2 | The readout is the glass field primitive, **or** a dated BJ ask exists carrying the census that proves no primitive fits | `node …/probes/x-w6/gate-easing-readout.mjs` (new; asserts either the glass component in the render tree **or** `docs/tranches/X/waves/W6-glass-ask-easing-readout.md` with a pasted exports+slot census) | flat beige rounded rect with copy/configure icon buttons, none of the glass input register — owner witness `OM-13-easing-readout-not-glass-input.png`; census today: `./input` absent from the exports map, `LabeledInput` has zero trailing slots |

**Falsifiers**: d1 fails if any two adjacent surfaces carry radii with no derivation between them. d2 fails **in both directions** — if the readout is locally restyled instead of composed, and equally if a fitting published primitive exists at open and the wave filed a letter instead. (This is what stops d2 being satisfiable by writing prose.)

### X.W6.e Gradient selector aurora (CC-064 · MT-F041)

- **Goal**: the gradient selector's ramp becomes gently alive without minting a new animation species or a private clock.
- **Mechanism**: consume the shared field/motion law from `@mkbabb/glass-ui/aurora` (published subpath, verified today) — the aurora motion vocabulary is glass-owned and already PRM-correct. Motion tokenized per MT-F034; PRM honoured **structurally** by declaring the animation inside `@media (prefers-reduced-motion: no-preference)`, not by relying on the global reduce-guard; no ungated rAF. If the wave mechanism proves to belong inside the glass gradient/aurora primitive, it leaves as a marked BJ ask (same two-way falsifier as d2).
- **Files**: `GradientVisualizer.vue`, `EasingAuthoringStage.vue` (its two rAF sites), gradient scoped styles.
- **Sub-gate**: 2 gates GREEN.

| # | Gate | Command | RED today |
|---|---|---|---|
| e1 | The selector's ramp animates under the shared law | new e2e `-g "gradient selector aurora"` (settled-frame delta over N frames at the same colour) | zero aurora motion on the gradient selector — no animation declared on the ramp today |
| e2 | No private clock: the gradient tree adds no ungated rAF, and every motion declaration sits inside a `no-preference` block | `grep -rn "requestAnimationFrame" demo/workbenches/gradient/` + `node …/probes/x-w6/gate-prm-idiom.mjs` (new) | **2 rAF sites today** (`EasingAuthoringStage.vue:58`, `:65` — `syncVbRatio`), neither PRM-gated *(measured today)*; the wave must land at ≤2 with both gated, or at 0 |

**MOTION-SOURCED · PENDING-QUARANTINE**: `docs/tranches/V/megatranche/audit/codex-provenance/motion-quarantine.md` **does not exist** (verified 2026-08-03; only an in-flight, untracked `evidence-motion-quarantine/probe.mjs` sits beside where it belongs). Per CC-027, every motion/PRM assertion in e1/e2 — and in .j's transition gates — is **quarantined** until re-derived against the two guards: `demo/styles/animations.css:184` (global reduce guard, verified present today) and `node_modules/@mkbabb/glass-ui/dist/styles/utilities/a11y-overrides.css` (verified present today). See wave gate H2.

**Addendum 2026-08-03 (later the same day)**: the record landed and is tracked at `9812f951` (see W0 HG-11); H2's citation legs stay born-RED and the D-19 re-measure-at-open duty governs.

### X.W6.f The space catalog and the specimen line (CC-068 · V·MT-W-CSS1 → **X:CSS-1**)

- **Goal**: the product stops stating false colour science on a first-class route, and every specimen line becomes a true, bounded, legible statement in a declared grammar.
- **Mechanism**: NEW `space-catalog.ts` — one `SPACE_CATALOG satisfies Record<DisplayColorSpace, SpaceEntry>`, exported as a key-preserving `readonly SpaceEntry[]` so `Object.entries` never erases the union; the 5 missing info rows are **authored**, the 7 missing docs **decided explicitly** (`doc: null` renders an authored empty state, never a silent `v-if`). NEW `specimen-format.ts` — one total `formatSpecimen(color, space)` returning `{ text, form: 'css' | 'channels', outOfGamut }`, one digit policy sized to the 234px caption box, gamut **MEASURED and MARKED, never projected**. `ColorSpaceSelector.vue` takes `defineModel<DisplayColorSpace>`, `tag="div"` dies, `:seed="space"` lands, the `../ui/select` edge repoints to `@mkbabb/glass-ui/select`, and the space-switch routes through `updateToColorSpace` (one command home). The masking fallbacks become unreachable and delete.
- **Structure over gates (L-8)**: the `satisfies` record makes a space-with-no-info a compile error; `defineModel<DisplayColorSpace>` makes all four `any`/cast holes unspellable; the discriminated `form` makes two-grammars-in-one-branch unrepresentable. Gates persist only for rendering truth.
- **Files**: `space-catalog.ts` (new), `specimen-format.ts` (new), `ColorSpaceSelector.vue`, `color-model.ts`, `ColorNutritionLabel.vue`, `AboutPane.vue`, `ColorPicker.vue`, four new oracle specs.
- **Sub-gate**: 10 gates GREEN.

| # | Gate | Command | RED today |
|---|---|---|---|
| f1 | Catalog totality | `node docs/tranches/X/gates/gate-catalog-totality.mjs` | offered=18, info=13, docs=11; failing input the string `display-p3` *(adjudicated)* |
| f2 | No masking fallback survives | `test "$(grep -rn 'colorSpaceInfo\.rgb\|(colorSpaceInfo as any)' demo \| wc -l \| tr -d ' ')" = 0` | **2 hits today** *(measured)* |
| f3 | About states true facts, never a bare heading | `npx playwright test e2e/smoke/oracles/o21-space-catalog-truth.spec.ts` | selecting "Display P3" renders the CIE-1931 rgb card ("Created: 1931") and a guide `h2` with childCount 1 *(adjudicated live witness)* |
| f4 | Every specimen caption fits its box | `npx playwright test e2e/smoke/oracles/o22-specimen-legibility.spec.ts` | 16 of 18 overflow, worst 758px in a 234px box; failing input `rgb(385.302835934518 143.376536829596 199.64311881105 / 82.7%)` *(byte-pinned to dist; the rgb triple re-derived today)* |
| f5 | One grammar per row; CSS rows round-trip the shipped parser | `node docs/tranches/X/gates/gate-specimen-grammar.mjs` | `data-specimen-form` **grep → 0** *(measured today)*; the four `·`-rows return `ok:false` from `parseCssColor` while sitting in the same slot as real CSS |
| f6 | Out-of-gamut is MARKED, never projected — **(ii) anti-projection lock** | `npx playwright test e2e/smoke/oracles/o23-specimen-gamut-honesty.spec.ts` | five rows out-of-gamut unmarked, `data-out-of-gamut` **grep → 0** *(measured today)*; the HSL row browser-resolves to `rgba(255,255,255,0.827)` — white — beside a dot painting the true colour. **(ii)** printed CSS coordinates must still satisfy `convertColor(parseCssColor(caption).value,'lab') ≈ model colour` to 1e-6 — GREEN today (≤1e-10) and it goes RED the moment anyone folds `mapColorToGamut` into the formatter |
| f7 | The boundary is the domain type | `test "$(grep -rn 'colorSpace: any' demo \| wc -l \| tr -d ' ')" = 0 && test "$(grep -c 'as DisplayColorSpace' demo/color-session/ColorSpaceSelector.vue)" = 0 && npx vue-tsc --noEmit -p tsconfig.demo.json` with a `@ts-expect-error` witness over `modelValue="not-a-space"` | RED twice: **3 `colorSpace: any` hits today** *(measured)* + the witness's expectation reports UNUSED (the wide prop compiles the wrong binding). **Lint is inadmissible here** — eslint exits 0 today |
| f8 | Eighteen dots, eighteen silhouettes, zero dead props | `npx playwright test e2e/smoke/oracles/o24-specimen-dot-identity.spec.ts && test "$(grep -c 'tag=' demo/color-session/ColorSpaceSelector.vue)" = 0` | `distinctVisualSignatures` = 1 (identical `feTurbulence` seed 240 across all 18); **`tag=` grep → 1 today** *(measured)*. The oracle keys on (borderRadius \| background \| feTurbulence@seed), **explicitly NOT the per-mount filter url** (the recorded false-GREEN trap). Must not be satisfied by repainting dots with projected colours (fails f6 ii) |
| f9 | One command, one home | `test "$(grep -c 'updateToColorSpace' demo/color-session/ColorSpaceSelector.vue)" -ge 1 && test "$(grep -A3 -n 'watch(' demo/picker/ColorPicker.vue \| grep -c 'selectedColorSpace')" = 0` | the component has zero references; the sole caller is a peer component's watcher *(adjudicated)* |
| f10 | color-session draws no edge into the banned layer | `test "$(grep -rn 'from \"\.\./ui/' demo/color-session/ \| wc -l \| tr -d ' ')" = 0` | **exactly 1 hit today** *(measured)*. Deliberately scoped to `color-session` — the repo-wide 90-statement extirpation is **X-W8's**, and booking it behind a component gate is the un-completable-wave failure |

**Falsifiers**: f1 fails the moment a 19th space is offered without an info row. f6(ii) is the wave's own regression tripwire — it is authored to go RED if the cure "fixes" gamut by projection. f7 fails structurally (a wrong binding compiles), which is why lint is excluded. f8 fails if all dots share a signature under any keying that excludes the per-mount url.

### X.W6.g Picker card rhythm and About alignment (CC-059 · MT-F029; CC-062 · MT-F035)

- **Goal**: the Picker headline card's vertical rhythm carries no interval that means nothing, and About sits on the same grid row Mix already proves works.
- **Mechanism**: the card's largest interval derives from the type scale's interval, not from leftover flex/grid slack; About aligns by the **shared row contract** (one grid row, panes aligned to the same track start), not a per-pane nudge — the OM-10 Mix control proves the contract exists and works.
- **Files**: `ColorPicker.vue` (card stack + scoped styles), `AboutPane.vue`.
- **Sub-gate**: 2 gates GREEN.

| # | Gate | Command | RED today |
|---|---|---|---|
| g1 | Every vertical interval in the Picker headline card resolves to a type-scale step | `node …/probes/x-w6/gate-card-rhythm.mjs` (new; DOM gap census against the scale) | the card's **largest single interval is empty** — a band ≈ the readout row's own height between header and readout; owner witness `OM-3-picker-row-gap.png` sha256 `a44389c2b543434ec78cd6ff828351cb6a02d1e1b075518588564e0283736277`, owner's words *"the spacing here between the top row and the bottom row is too much"* |
| g2 | Cross-pane top alignment: Picker↔About top edges within 1px, with Picker↔Mix unchanged as the control | new e2e `-g "companion panes share one track start"` | About's top edge sits visibly higher than the Picker card's (`OM-9-picker-about-height-misaligned.png` sha256 `a57a57eb…9e6b7ca6`); Mix's is aligned (`OM-10-…-CONTROL.png` sha256 `47e115e9…3573c3`) — the defect/control pair proves it is a per-pane deviation, not a grid property |

**Falsifiers**: g1 fails on any interval with no scale derivation — including one that merely *looks* right. g2 fails if the cure moves the Mix control (a per-pane nudge passes the About assertion and breaks the control; the gate asserts both).

### X.W6.h HeroBlob chroma fidelity (CC-061 · MT-F032)

- **Goal**: the blob carries the current colour's vibrancy instead of a desaturated ghost of it.
- **Mechanism**: **measure the pipeline before cutting** — the row's own law. Three candidate strippers: the 1×1-canvas 2D resolver (`useContrastSafeColor.ts:101-102` — `fillStyle` + `fillRect(0,0,1,1)`, an sRGB clamp), `deriveBlobPalette`/`cssToOklch` from `@mkbabb/glass-ui/color`, and the shader's own HSV perturbation/lightening. `HeroBlob.vue:15` binds `:color="cssColorOpaque"` — alpha-stripped. The cure cuts at the measured stripper, not at the first plausible one.
- **Files**: `HeroBlob.vue`, `useContrastSafeColor.ts`.
- **Sub-gate**: 2 gates GREEN.

| # | Gate | Command | RED today |
|---|---|---|---|
| h1 | Chroma fidelity: the painted blob's dominant chroma is within a stated ΔC of the current colour's, at a high-chroma current colour | new e2e `-g "hero blob carries current chroma"` (settled frame, dominant-atom sample) | owner witness `OM-6-blob-vibrancy.png` sha256 `55fcb390…21a620`, owner's words *"The blob current color is not nearly vibrant enough"* |
| h2 | The stripper is named by measurement before the cut: a committed census records the chroma at each pipeline stage | `node docs/tranches/X/gates/gate-blob-pipeline.mjs` (new) + the committed census file | **measured today, read-only**: source `lab(92% 88.8 20)` → `oklch` **C = 0.27245** (hue 9.83°); after the 1×1-canvas sRGB clamp (`rgb(255 143.38 199.64)`) → **C = 0.14868** (hue 349.50°) — a **45.4% chroma loss and a 20.3° hue rotation before the shader's own perturbation runs**. The census does not exist today |

**Falsifiers**: h1 fails at any current colour whose chroma exceeds sRGB, which is exactly the owner's case. h2 fails if the wave cuts at a stage it did not measure — the census names all three stages or the gate is RED (this is what stops the "labelled, not measured" hypothesis becoming the cure by default).

### X.W6.i The atmosphere harness — one harness, three rows (CC-065, CC-066, CC-067)

- **Goal**: the oldest row in the repository closes with a tombstone and a machine oracle, the dark-L band stops being cited as a future event by shipping code, and a cold load paints what the user picked.
- **Mechanism**: **FIRST ACT is the supersession tombstone** — the A-vintage aurora derive-from-color mandate (mandate 13 / Ae-11 / A-02 / Dc-1..3 / CH-2 / D-1) is **RETIRED AS LANDED at N.W5 `e32111c7`**, quoting the original predicate verbatim, citing S's independent confirmation, and correcting RF-26's misdated origin. Only then does the surviving live claim — response visibility — re-mint as **X:ATMO-1**, carry 0, with a machine oracle: for each of N seeds the rendered atmosphere's **named atoms move ≥ a stated ΔE2000, measured from committed frames, never from an agent's eye**. CC-066 re-probes the installed dist and either lands the dark lBand and deletes the three stale comments, or files one dated §D letter — **the row closes either way**. CC-067 rides the same harness: one cold-load capture asserting the first painted atmosphere equals the seeded pick.
- **Files**: `useAtmosphere.ts`, `atmosphere-calibration.ts`, `demo/test/glass/aurora-bracket.test.ts`, `AuroraPane.vue`, `o25-atmosphere-response.spec.ts` (new), `o26-atmosphere-coldload.spec.ts` (new), tombstone doc.
- **Sub-gate**: 3 gates GREEN + the tombstone committed.

| # | Gate | Command | RED today |
|---|---|---|---|
| i1 | **X:ATMO-1** — named atoms move ≥ stated ΔE2000 across N seeds, from committed frames | `npx playwright test e2e/smoke/oracles/o25-atmosphere-response.spec.ts` | RED by absence: no committed frames, no ΔE2000 assertion, no oracle. The mandate itself is satisfied (`grep -rn deriveAurora demo/` → **8 sites today**, incl. `useAtmosphere.ts:142`/`:243`) — which is precisely why the tombstone precedes the oracle |
| i2 | The dark lBand door: either the band is reachable and the three stale comments are gone, or a dated §D letter exists with the probe pasted | `node docs/tranches/X/gates/gate-lband-door.mjs` (new) | **measured today** against installed 7.0.0: `lightnessScheme` **2 occurrences**, `lBand` **4 occurrences** in `dist/aurora.js` — the door plausibly shipped. Three live sites still cite a wave that closed TRIGGER-NOT-FIRED on 2026-07-12: `useAtmosphere.ts:236`, `atmosphere-calibration.ts:24`, `aurora-bracket.test.ts:29` |
| i3 | Cold-load arm-replay: the first painted atmosphere equals the seeded pick | `npx playwright test e2e/smoke/oracles/o26-atmosphere-coldload.spec.ts` | RED — every cold load paints the pre-hydration default until first picker interaction; **`grep -rn armRuntime demo/` → 0 hits today**, so the T-era `armRuntime({immediate:true})` cure is not in the tree |

**Falsifiers**: i1 fails if the atoms do not move, **and equally if it can be passed by an agent's description rather than a frame diff** — the oracle reads committed frames only. i2 fails in both directions (band unreachable and no letter; or a letter filed while the probe shows the atoms exist). i3 fails on any seeded pick whose first paint is the default.

### X.W6.j Scene adoption: Gradient pilot → Mix canary → Blob/Atmosphere persistence (CC-056 · V·L3; CC-057 · V·L4)

- **Goal**: scene composition is proven on the hardest live tools — one at a time, each a completable slice — without recreating the deleted chassis.
- **Mechanism**: **Gradient pilot** (dominant authoring stage, selected-stop inspector, stable action — its substance already landed in .a/.b/.c, so this unit adopts the X-W5 scene contract for the route); **Mix canary** proves the abstraction with no extra axes — one interaction owner, no nested controls, canvas lifetime isolated (`MixAnimationCanvas` mounts/unmounts with the scene, no orphaned rAF or 2D context); **Blob/Atmosphere** carry a persistent live preview with an explicit loss/recovery contract (3 `webglcontextlost`/`restored` handlers exist today — the contract is asserted, not assumed); **short landscape** at 720×450 keeps both the last control and the preview reachable.
- **Order**: Gradient, then Mix, then Blob/Atmosphere — the adjudicated pilot→canary→adoption order. Each route is its own completable slice.
- **Files**: `MixPane.vue`, `MixConfigBar.vue`, `MixSourceSelector.vue`, `MixAnimationCanvas.vue`, `useMixingAnimation.ts`, `BlobPane.vue`, `AuroraPane.vue`, route-private scene styles, `o27-scene-contracts.spec.ts` (new).
- **Sub-gate**: 4 gates.

| # | Gate | Command | RED today |
|---|---|---|---|
| j1 | Gradient owns its stage/inspector/action contract as a routed scene | `npx playwright test e2e/smoke/oracles/o27-scene-contracts.spec.ts -g "gradient scene"` | RED — **all 14 routes render `component: Stub` today** (`demo/color-picker/router/index.ts:22-35`, measured); no route owns a scene contract |
| j2 | Mix canary: one interaction owner, no nested interactive controls, canvas lifetime bound to the scene | `o27… -g "mix canary"` | RED — the Mix tree ships 4 components + 3 composables with two `getContext("2d")` acquisition sites in `useMixingAnimation.ts` (`:91`, `:144`, `:162`) and no scene-bound teardown assertion *(measured today)* |
| j3 | Blob/Atmosphere persistent preview + loss/recovery contract | `o27… -g "preview survives"` (kill the context, assert recovery) | RED — 3 `webglcontextlost`/`restored` handlers exist repo-wide *(measured)* but **no test asserts recovery**; no persistent preview contract exists |
| j4 | Short landscape 720×450: the last control and the preview are both reachable | `o27… -g "short landscape"` (720×450 cell) | **MEASURE-AT-OPEN** — not measured by this seat; the baseline is taken at wave open before any cure. If it measures GREEN the gate is retired with the measurement pasted, not carried |

**Falsifiers**: j1/j2 fail if a second owner can drive the same interaction, or if a canvas outlives its scene (assert by heap/context count after route-away). j3 fails if the preview does not repaint after a forced context loss. j4 fails if any control or the preview is unreachable at 720×450 — and is explicitly allowed to retire GREEN with its measurement, which is why it is not counted born-RED.

**MOTION-SOURCED · PENDING-QUARANTINE**: j1–j3's pane/sub-pane transition assertions inherit the quarantine (see wave gate H2). Transitions themselves are **X-W5's** (CC-054/CC-055) — this wave adopts them per route and asserts no motion property that has not cleared the two-guard check.

---

## 6. Hard Gate

**Roster: 50 gates.** 46 sub-gates (a1–a13, b1–b4, c1–c4, d1–d2, e1–e2, f1–f10, g1–g2, h1–h2, i1–i3, j1–j4) of which **45 are born-RED** and one (j4) is MEASURE-AT-OPEN; plus the four wave-level conditions below, of which **H2 is born-RED**. **46 born-RED total.**

The wave closes when **all 45 born-RED sub-gates are GREEN by the same commands that are RED today**, j4 is measured and either cured or retired with its measurement pasted, plus:

- **H1 — No copied chassis (non-regression, CC-108).** `node docs/tranches/X/gates/gate-no-chassis.mjs` over this wave's diff: no new module, composable, or style block reproduces the dead `InstrumentChassis` shape (a shared stage/inspector/action housing parameterised across instruments). **Not born-RED** — nothing chassis-shaped exists to delete; A-17 killed the producer arms and made our posture a fact rather than a prohibition. **Falsifier**: it fails if any unit factors two instruments' stages into one parameterised housing — which is exactly the shortcut ten units under deadline would reach for. It is a subtraction gate, and a green-at-open gate is only admissible because its falsifier is a *new* artefact, not an existing one.
- **H2 — Motion quarantine discharged (born-RED).** `test -f docs/tranches/V/megatranche/audit/codex-provenance/motion-quarantine.md` and every assertion in .e/.j marked MOTION-SOURCED cites it. **RED today: the file does not exist** *(verified 2026-08-03; a sibling lane's in-flight `audit/codex-provenance/evidence-motion-quarantine/probe.mjs` is present and untracked — evidence, not the quarantine record)*. Until the record exists, every motion/PRM assertion in this wave carries **PENDING-QUARANTINE** and is re-derived at open against the two guards — `demo/styles/animations.css:184` and `node_modules/@mkbabb/glass-ui/dist/styles/utilities/a11y-overrides.css` (both verified present today). **Falsifier**: a motion gate that passes while its assertion was never checked against the two guards. **Addendum 2026-08-03 (later the same day)**: the record landed and is tracked at `9812f951` (see W0 HG-11); H2's citation legs stay born-RED and the D-19 re-measure-at-open duty governs.
- **H3 — Banks swept at open and at close.** (1) Parser R1: `node --input-type=module -e "const {parseCssColor}=await import('./dist/subpaths/css.js'); try{parseCssColor('oklch()');process.exit(0)}catch{process.exit(1)}"` — **exit 1 today** (re-run 2026-08-03: `THREW: Cannot read properties of undefined (reading 'replace')`). It is X-W9's cure; this wave adds **no consumer try/catch** (masking-fallback ban) and re-witnesses the code-editor arm when it flips. (2) Glass rail-variant tripwire. (3) Glass 8.0.0 tripwire — a **sweep, not a door**: when it fires, this wave records the reading and hands the §Blocked limb to **X-W4.g** on X-W0's census PASS. Nothing in §Blocked opens inside this wave under any reading of the tripwire.
- **H4 — Every ledger row leaves with exactly one disposition** (§Dispositions below), ids for life, zero silent drops.

A wave whose gates all pass but whose §2a goal is unmet closes `complete_with_misses`, not `complete`.

---

## 7. Format And Lint Cadence

`npx prettier --check` on touched paths, `npx eslint` on `demo/**`, `npx vue-tsc --noEmit -p tsconfig.demo.json`, and `npx vitest run` — after **each unit lands** (ten integration points) and again before wave close. `git diff --check` on every commit.

**Lint is explicitly inadmissible as evidence for gate f7**: eslint exits 0 today with all three `colorSpace: any` holes present (independently confirmed by two adjudication seats). That gate's evidence is `vue-tsc` plus a `@ts-expect-error` type witness. Lint runs for hygiene, not as the type gate's replacement.

---

## 8. Verification Artefacts

Committed at close (a witness not in the wave's own commit does not exist — force-added past `.gitignore *.png`):

- `docs/tranches/X/waves/W6-evidence/gradient/{before,after}-{cross-drag-rail-1440, axis-overlay-1440, grab-teleport-1440, touch-delete-iphone14, chip-hr-1440, white-ramp-contrast-1440, rootfs20-overhang-1440}.png`
- `docs/tranches/X/waves/W6-evidence/catalog/{before,after}-{catalog-open, about-display-p3, specimen-dots}.png` — the existing `probe-catalog-open.png` / `probe-display-p3-about.png` are adopted as BEFORE frames and force-added in this wave's commit.
- `docs/tranches/X/waves/W6-evidence/owner-marks/` — OM-3, OM-4, OM-6, OM-9 + the OM-10 control, OM-13 re-captured at the **same crop**, each beside its original.
- `docs/tranches/X/waves/W6-evidence/atmosphere/` — the N-seed committed frames backing X:ATMO-1, plus the cold-load first-paint capture.
- `docs/tranches/X/waves/W6-atmosphere-tombstone.md` — the DR-01 supersession row (predicate quoted verbatim, `e32111c7` cited, RF-26 misdating corrected).
- `docs/tranches/X/waves/W6-glass-ask-easing-readout.md` and/or `W6-lband-letter.md` — dated, with their censuses pasted, **only if** their gates' census arm selects that branch.
- `docs/tranches/X/waves/W6-blob-pipeline-census.md` — chroma at each of the three HeroBlob stages.
- Gate transcripts: every RED command's output at open and its GREEN output at close, same command, in `W6-evidence/gates/`.

**π obligations**: MATRIX — chromium 1440×900 (primary) + 1600×1000 for the catalog cell + playwright-webkit iPhone-14 / 390×844 (the coarse cell, a9/b2) + 720×450 (short landscape, j4). **safari-app via safaridriver is a SEPARATE evidence cell (I-20) — nothing here transfers to it; the audit `safari-*` dirs are webkit captures cited as webkit only.** ROUTE — hash URLs, fully client-seeded; the dev cell is **API-LESS**, so any arm needing palette data (the `setStopsFromColors` end-to-end witness) stays **UNVERIFIABLE-HERE** and is honestly labelled, never confirmed from a blank surface. **STALE-SERVER LAW** — literal/precision/serializer claims run headless `vite-node` against the tree or a freshly restarted server. **TELEMETRY LAW** — route-health witnesses are DOM facts (stop count, bar presence), never `pageerror`/console counts, which are structurally blind to the debounce-scheduled `onErrorCaptured` class.

**EXPLICITLY NOT CLAIMED**: any drag-frame serialization-cost win (GRADSTOP-A §8) · any safari-app property · any bundle or perf property (X-W2 owns those; a dev server is structurally blind to both) · any repo-wide `demo/ui` extirpation count (X-W8's) · any parser cure (X-W9's).

---

## 9. Commit Plan

| # | Scope | Contents |
|---|---|---|
| 1 | `fix(demo/gradient-model)` | .a — ordering + gesture + geometry core; body required (broad, deletion-bearing) |
| 2 | `feat(demo/gradient-seat)` | .b — seat rail, inspector, keyboard grammar |
| 3 | `refactor(demo/gradient-sampling)` | .c — one sampler, one dialect, leaf types; body required (deletion) |
| 4 | `style(demo/easing-register)` | .d — radius scale + readout composition or the dated ask |
| 5 | `feat(demo/gradient-aurora)` | .e — shared field/motion law consume |
| 6 | `feat(demo/space-catalog)` | .f — X:CSS-1; body required (broad + deletion) |
| 7 | `fix(demo/picker-rhythm)` | .g — card intervals + shared row contract |
| 8 | `fix(demo/hero-blob-chroma)` | .h — census commit **precedes** the cure commit |
| 9 | `feat(demo/atmosphere-oracle)` | .i — **tombstone commits FIRST**, then the harness; body required (status + tombstone) |
| 10 | `feat(demo/scene-adoption)` | .j — Gradient/Mix/Blob/Atmosphere scene contracts |
| 11 | `docs(x-w6 close)` | gate transcripts, witnesses, dispositions, status flip to **IMPLEMENTED** (never VERIFIED — that is X-W11's stamp) |

Pathspec commits only. `scripts/dev/dev.sh` is never staged.

---

## 10. Dependencies

- **Depends on**:
  - **X-W4** — the typed, owner-tokened `SceneActionSet` (CC-043). Every unit's "stable action" arm binds to it; without it .j's scene contracts have no action type.
  - **X-W5** — one route, one scene (CC-048..CC-055). .j *adopts* that contract route by route; it does not author it. Pane/sub-pane transitions (CC-054/CC-055) are X-W5's — this wave asserts no transition property of its own.
  - **X-W1** — the visual-oracle substrate (CC-030 `e2e/visual/`, **ABSENT today**, verified) and CI wiring (CC-031: Playwright is in **no** workflow today). X:ATMO-1's committed-frame oracle and the OM re-captures ride it. Without X-W1 the frames are produced but not defended.
  - **installed `@mkbabb/glass-ui@7.0.0`** — `./aurora`, `./select`, `./labeled-field`, `./blob`, `./blob-config` all verified published today. No producer change is a gate.
- **Blocks**:
  - **X-W7** — the palette specimen split reuses `formatSpecimen`'s digit policy (CC-070's consumer facility).
  - **X-W8** — subtraction needs .a/.c's destinations stable before the dead doors and barrels go.
  - **X-W10** — the radius canon echo (CC-060), and the design canon that this wave's cures instantiate.
  - **X-W11** — release cannot state instrument truth until these rows are VERIFIED. This wave's own close stamps **IMPLEMENTED** (gates green, bytes landed); **VERIFIED is stamped only at X-W11's release close**, on its browser and real-Safari (separate cells, I-20) plus consumer-tuple receipts.

---

## COMPLETABLE (C-06)

**YES.** If X-W6 were the only wave that ever executed after its predecessors:

- the gradient model could no longer serialize CSS its own parser rejects, an authored easing could no longer migrate, hard stops **and** drag-reordering would both survive by construction, a grab could not teleport, a mouse user would keep the keyboard grammar, no stray button would mint and no cancelled gesture would commit, and a touch user could not destroy a stop by grabbing it;
- the product would stop stating false colour science on a first-class route, every specimen line would be a true, bounded, legible statement in a declared grammar, and the catalog gap would be a **compile error**;
- the oldest row in the repository would close with a tombstone and a machine oracle instead of an eighteenth carry;
- the blob would carry the colour it exists to display, measured;
- and four routes would own their stage/inspector/action contract.

Zero capability lost. No new surface. **No dependency on any successor wave, on the parser cure (X-W9 — consumed only as a runnable bank), on glass-ui shipping anything (7.0.0 is installed and every consumed subpath is verified published), on barrel deletion (X-W8), or on the toolchain.** `c4af0ef9` (CC-002 — V·MT1 landed alone) is the existence proof and the template. Each of the ten units is itself a completable slice: the ledger's per-instrument shape means a stall in one lane costs that instrument, not the wave.

---

## Blocked rows — left in the glass bank with their named triggers

Never smuggled into this wave. The trigger is evaluated by **X-W0's census event** (the four machine-checked CC-003 conditions, PASS/FAIL recorded with measured bytes) and the row opens in **X-W4.g**, the trigger-gated receiving surface — never here, at no point in this wave.

| row | why it cannot open here | exact trigger |
|---|---|---|
| **CC-058 · the D-6 / D2-16 limb** (MT-GRADSTOP-2's WatercolorDot faces) | `@mkbabb/glass-ui/watercolor-dot` **is** published at 7.0.0 and the adjudication ruled the arm a buildable CONSUME — but CC-044 rules that every `watercolor-dot` import dies in **one atomic cut** at the Glass-8 trigger, replaced by a consumer-painted `WatercolorSwatch` in the TR#84 slot, **with no pre-8 wrapper (a second migration is forbidden)**. Adopting it in the gradient rail today mints a seventh impostor that CC-044 must then delete. The rest of MT-GRADSTOP-2 (seat rail, inspector, keyboard grammar, disambiguation) builds here unaffected. | **CC-003's four-condition trigger**: installed `@mkbabb/glass-ui` major **≥ 8** AND `./watercolor-dot` **absent** from packed exports AND the consumer-painted indicator slot (**TR#84**) present in declarations + packed exports AND the Glass receipt names its keyboard/orientation/motion contract. Machine-checked at **X-W0's census event** before any writer opens product source. **Receiving surface: X-W4.g** — on census PASS it opens and lands CC-044 + CC-003 + this limb (and W7's/W8's banked limbs) as ONE atomic cut; no second migration, no interim wrapper. Receiving row: **CC-113**. |

Adjacent bank rows this wave **reads but does not open**: CC-108 (A-17 chassis death — consumed as the *fact* behind H1, RETIRED-BY-PRODUCER, no arms to wait for) · CC-113 (A-18 watercolor indicator paint, BLOCKED-ON TR#84) · CC-068's BANK-1 (glass SelectTrigger title/display variant — the L-10 relay, ruled GLASS-OWNED and deliberately **not** among f1–f10) · the glass I-9 Chip residual (glass-owned; .d touches our radius choices only).

---

## Dispositions — every assigned ledger row leaves with exactly one

| CC row | original id(s) | verb | lands as |
|---|---|---|---|
| CC-056 | V·L3 | BUILD | **X.W6.j** (pilot → canary → adoption order) |
| CC-057 | V·L4 | BUILD | **X.W6.j** (persistent preview + short-landscape/last-control proof) |
| CC-058 | MT-GRADSTOP-1 · 2 · 3 | BUILD | **X.W6.a / .b / .c**, adjudicated order; D-6/D2-16 limb → bank |
| CC-059 | MT-F029 | BUILD | **X.W6.g** (g1) |
| CC-060 | MT-F030 | BUILD | **X.W6.d** (d1); canon echo deferred to X-W10 |
| CC-061 | MT-F032 | BUILD | **X.W6.h** (h1/h2, census before cut) |
| CC-062 | MT-F035 | BUILD | **X.W6.g** (g2, with the OM-10 control) |
| CC-063 | MT-F037 | BUILD | **X.W6.d** (d2, two-way falsifier) |
| CC-064 | MT-F041 | BUILD | **X.W6.e** (e1/e2), MOTION-SOURCED · PENDING-QUARANTINE |
| CC-065 | DR-01 | BUILD | **X.W6.i** — tombstone FIRST, then X:ATMO-1 (i1); RF-26 misdating corrected |
| CC-066 | DR-04 | BUILD | **X.W6.i** (i2) — lands or one dated letter; closes either way |
| CC-067 | DR-05 | BUILD | **X.W6.i** (i3), same harness as CC-065 |
| CC-005 | V·MT4 | FOLD | consumed WHOLE by this file; the no-copied-chassis law survives as **H1**, re-shaped from prohibition to fact by CC-108 |
| CC-068 | V·MT-W-CSS1 | FOLD | re-authored as **X:CSS-1** = **X.W6.f**, all 10 gates carried; the L-1 blocker stands |

---

## 11. Archaeology

- **V·MT4** (`MECHANISM-CUT-FORMATION-MAP-2026-07-29.md:249`) reached **CANDIDATE, never SPECIFIED**, and its hard gate assumed a producer chassis to *not* copy. **New guardrail**: A-17 killed the chassis producer-side (CC-108, owner word twice, proof REJECTED), so H1 is a subtraction gate over this wave's own diff rather than a comparison against a living producer surface.
- **MT-GRADSTOP-1 r1** (2026-07-27) authored a **clamp** ordering cure. r3 (2026-07-28) superseded it *on its own premise*: once easing hangs on its opening stop, the "index-keyed intervals make re-sorting unsafe" objection dissolves, and clamp silently deletes drag-across-a-neighbour. **New guardrail**: GRADSTOP-A §14 — normalise-on-write and stop-owned easing land together; neither clamp nor splice-at-insert may be authored. Worker-F's narrower two-stage seam is the recorded fallback if .a stalls mid-unit.
- **DR-01** carried **17 closes under 13 names** while its mandate had been satisfied since N.W5 `e32111c7`. Every prior close failed by re-verifying an upstream block, running a wave with zero commits, reframing the row as already-fine, or mutating the predicate to something an executing agent could judge with its own eye. **New guardrail**: the tombstone is the unit's **first act**, and the surviving claim gets a new id, carry 0, and an oracle that reads committed frames — an agent's description cannot pass i1.
- **L-8(b) "two value.js's in one build"** was a measured **false positive** (three independent `traceResolution` runs). GRADSTOP-A §9: that phrase may not be cited by any wave or close, and the tsconfig paths block is cured by **deletion** — in **X-W8**, not here.

---

## L-18 rider — the challenge obligation (reference, not ceremony)

Nothing in this wave is ACCEPTED on its gates alone. The implementation must survive **two quartet challenge passes — 4 Opus skeptics per pass, then a fresh Fable apotheosis** — before any unit's status advances past IMPLEMENTED. The skeptics' standing targets, drawn from this wave's own recorded failure modes:

1. **Vacuous greens** — GRADSTOP-A §13: name which assertion catches the `HANDLE_HALF = 0` mutation and which catches deleting `.sort()`. A suite that stays green under both is not evidence.
2. **Cure-shipped regressions** — the cure is adjudicated separately from the defect (ColorSpaceSelector addendum item 2): f6(ii) is the template, and every unit must state the property its cure could break and gate it in the breaking direction.
3. **Scope laundering** — a "sole offender" claim is a claim about a scope: any *sole/only/unique* assertion must paste the same command re-run at repository scope. Three of one prior seat's CONFIRMED labels sat at the wrong scope.
4. **Citation inheritance** — a repeated evidence cell that was never re-derived is detectable and has been caught once already; every figure in the close is re-measured or cited with its measurement date.
5. **Probe-before-meaning** — the recorded false-refutation class (a dot signature that included the per-mount filter url gave 18 where the truth was 1). A probe's output is not read until what it measures is read.
6. **Un-measured claims** — no close may claim a serialization-cost win, a bundle property, a safari-app property, or a motion property still under PENDING-QUARANTINE.

Verdicts land as an E-3 addendum beside this file, never as a rewrite of it.

---

**ADDENDUM 2026-09-19 (dated, beside — E-3; COHESION §0z). The re-open after Check 3.** §4 gains one row: `test/gradient-parse.test.ts` — `modify` (unit `.a`'s migration only: 2 stops → 1, 3 stops → 2). §4's oracle rows re-point at the bytes: `o26-atmosphere-coldload.spec.ts` → **`e2e/smoke/oracles/o28-atmosphere-coldload.spec.ts`**, `o27-scene-contracts.spec.ts` → **`e2e/smoke/oracles/o29-scene-contracts.spec.ts`** (i3 · j1 · j2 · j3 commands read the new paths; o25 stands). `.d` gains `docs/tranches/V/coordination/INBOX.md` — mail rows only (the `O-` relay row for `W6-glass-ask-easing-readout.md`) — and re-captures the d2 negative-control transcript so both directions print RED. **a3–a7**: the `aria-valuenow` / `style.left` substitutes are the commands of record; `gate-axis.mjs` / `gate-gesture.mjs` stay `execute, no write` and their re-point rides X-W11's OUT-OF-WAVE roster (R-5). **h1 restated**: the blob's resolver and WebGL2 drawing buffer declare `display-p3` where `matchMedia('(color-gamut: p3)')` matches (sRGB otherwise); h1 = painted dominant chroma within the stated ΔC of the current colour's chroma **gamut-mapped to the buffer's colour space** (css-color-4 §13); beyond the display's gamut is honest-RED-by-physics by id. Units `.b .c .e .g` dispatch whole; `.d` at d1, `.h` at h1, `.i` at i1/i3; `.j` after X-W5 CLOSED. R-3 rides X-W11's OUT-OF-WAVE roster. H1 runs over the wave's full commit roster.

---

## ADDENDUM 2026-09-22 (dated, beside — E-3; COHESION §0an + §0aq)
Grants and routes for the RESUME: `.c` writable set = its unit list ⊕ §0an's six paths; `.c` also cures **b1** in `GradientStopEditor.vue` (commit #4). `.i` writable set ⊕ `demo/color-picker/index.html` (pre-module boot seed only; dialog-ancestry rider `W5.md:321`). **g1** honest-RED by id → X-W10 (M-23). **h1**: `H1-P3` honest-RED-by-physics (OM-6 beyond-sRGB), oracle on the consumer route, display-p3 ask by mail. §0z E2's two canaries → X-W8 `.i`. `.j` after X-W5 CLOSED. Order: [`.c`] → [`.d`] → [`.e`] → [`.h`] → [`.i`].
