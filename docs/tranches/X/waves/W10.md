# X.W10 - Canonical Design and Motion

## 2. State

**Name**: W10 - Canonical Design and Motion
**Opens after**: X-W5, X-W6, X-W7, X-W8, X-W9 stable (surviving structure fixed) AND the CC-104
precondition ruled at the X-W0 owner sitting
**Agents**: 6 — phase 1: 2 parallel (design authors, Fable ∥ Opus 5, M-23 §3) · phase 2: 1 serial
(fresh Fable adjudicator, M-23 §1) · phase 3: 3 parallel (Opus 5 implementation seats, M-23 §2)
**Hard gate**: one normative design law cited by registry and code, the three named terms defined
with provenance, and the four live motion/shadow/spacing defects cured against pinned witnesses —
nine gates, all RED at HEAD `c654824e`+working-tree, measured 2026-08-03 and pasted below
**Status**: planned

**COMPLETABLE (C-06)**: **YES, once open.** The wave writes one canonical document, three bounded
source cures, and a satellite fold. It publishes no package surface, ships no release, requires no
producer version bump, and no glass-8 repin. Its bounds intersect only closed predecessors'
surfaces (X-W5 ×4, X-W7 ×1), inherited after their closes; no concurrently-open wave shares a write
path — the five paths and their holders are named in §4a Cross-wave. This wave owns
`demo/DESIGN.md`, `PaneHeader.vue`, `PaletteCard.vue`, `foundation.css`'s one survivor recipe, and
the design satellites. Each of the three cures is independently revertible.
`c4af0ef9` (CC-002 — one mechanism landed alone, proven) is the template.

**Conditional-open is real, not ceremonial.** The wave describes surviving structure; authoring it
before X-W5..X-W9 stabilize produces a canon anchored to a tree the next wave deletes — which is
exactly the 12-of-13 stale-anchor defect this wave exists to end.

### 2a. Goal criterion

This wave succeeds if, when work ends, a component author has **one** document that answers "what
does this shadow claim, where does this interval come from, what may move and on whose clock" —
and if the four surfaces the owner marked (PRM-defeating headers, faceted casts, an unshrunk
condensed card, uncited proportion constants) are cured in the tree rather than described in prose.

### 2b. Open preconditions (checked at wave-open; NOT close gates)

| # | precondition | state at authoring |
|---|---|---|
| OP-1 | **CC-104 / DR-32** — DesignSync callable at open, or the owner names the substitute in writing and D-20's frames obligation is amended in the SAME ruling | Tool schema **resolves in this harness** (`DesignSync`, loaded 2026-08-03). Callability against a real design-system project is **UNPROVEN** — `list_projects` is the open probe. Ruling still owed either way. |
| OP-2 | **MOMENTUM driver/observer carve** — does liquid weight bind observer-class motion (sub-pane swaps, list reorders)? | OPEN owner mark. `glass-ui/DESIGN.md:115` scopes it to drivers; the owner's own words (2026-06-23) say "movement in generality". The wave may author the sub-pane law's structure, but ships the curve as a **parameter awaiting the ruling** — never a silent pick. |
| OP-3 | **GOLDEN GLASS ratification** — the owner coined the term; only he ratifies it | The wave DRAFTS from lineage and carries it as a MARK. It may not enact. |
| OP-4 | **OM-11/12 hover-state floor** — confirm the BREATH-OF-LIFE reading (every hoverable product surface responds) as canon law | OPEN owner mark; generalizes an ordered fix into a canon clause. |

A wave that opens with OP-1 unruled repeats DR-32 exactly: a design gate depending on a tool the
same corpus books unavailable.

### 2c. Ledger transposition (CARRY-CUT-LEDGER §1.L / §2)

| CC row | verb | lands as |
|---|---|---|
| CC-098 (Golden Glass · Breath of Life · Movement of Momentum) | BUILD | units .a/.b/.c; gates G-1, G-2, G-3 |
| CC-099 (MT-F023 PRM vs scroll-driven animation) | BUILD | unit .d; gate G-4 |
| CC-100 (MT-F027 cartoon cast facets on rounded cards) | BUILD | unit .e; gate G-6 |
| CC-101 (MT-F043 · D-2 · OM-18 condensed-card spacing) | BUILD | unit .d; gate G-5 |
| CC-102 (P-2 producer-contract citation) | BUILD | unit .f; gate G-7 |
| CC-009 (V·MT8) | FOLD | this file is its X-naming successor; V·MT8's label may not gate |
| CC-104 (DR-32 DesignSync) | FOLD | OP-1 above |
| CC-103 (DR-13 8→11 primitive-ask bundle) | RETIRE-THE-BUNDLE | §5.f dispositions; gate G-8 |
| CC-018 (DR-16 taste certification) | OPTIONAL | §5.f; opens ONLY on owner word at the X-W0 sitting. Not gated. |
| CC-112 (G-9 φ token publication) | **stays in the glass bank** | BLOCKED-ON glass TR#78's canon publication. This wave cites the named contract (CC-102) and does **not** open it. |

## 3. Scope

1. Author `demo/DESIGN.md` whole: the three named laws first, then material/elevation,
   proportion/derivation, chroma fidelity, motion, product surfaces, anti-patterns.
2. Define GOLDEN GLASS from lineage (warm-cream translucent glass at the floating tier; the φ
   proportion register cited to the producer contract; the four negative "gold" constraints) and
   carry it to the owner as a mark.
3. Fold BREATH OF LIFE's owner sentence verbatim as law §1, with the engagement ladder
   (rest→hover→press→engaged→modal) and the hover-floor consequence.
4. Cite MOVEMENT OF MOMENTUM to `glass-ui/DESIGN.md:113`, state the rename so the owner's phrase
   is greppable, and carry the driver/observer carve as an owner mark with the curve parameterized.
5. Move `PaneHeader.vue`'s three scroll-timeline declarations inside
   `@media (prefers-reduced-motion: no-preference)` and delete the two blunt reduce-kill blocks at
   `demo/styles/animations.css:184` and `:202`.
6. Give the scroll-condensed header its own spacing register — the condense keyframes gain a
   spacing channel; glyph scale alone stops standing in for a shrunk box.
7. Rule the cast-shadow treatment in canon (elevation semantics + one outer silhouette) and apply
   it at `PaletteCard.vue`, the sole `cartoon-cast` consumer.
8. Replace every bare `38.1966011`/`61.8033989` in live canon with a citation to the named producer
   contract.
9. Enumerate the DR-13 bundle's survivors by name, then give each a terminal verb: landed-as-local
   with owner-accepted rationale, or deleted.
10. Fold or tombstone every satellite design authority; wire the canon so registry and code cite it.

No new design register, no fourth transition family (`animations.css:60` — "a fourth name is a
defect"), no producer ask before adjudication pins producer ownership.

### 3a. Triumvirate Dispatch

Mandatory, not optional, on any of:

- **File-bound expansion.** Any cure requiring a write outside §4's table — in particular any write
  under `node_modules/@mkbabb/glass-ui/**` or a fourth demo source file — invalidates the wave.
  The `.cartoon-cast` recipe is producer-owned; if the geometry cure cannot be composed
  consumer-side, that is a scope reveal, not an edit.
- **Hard-gate failure that is not local-edit-recoverable.** G-4 green in one engine and red in the
  other (per-engine divergence is expected in magnitude, never in sign); G-6's pixel-diff still
  failing after the nested-spread cure — the cure is labelled UNVERIFIED at authoring and its
  failure is a design question, not a patch.
- **Third diagnostic iteration.** Any diagnose→edit→re-measure loop reaching iteration three on the
  same gate halts. The orchestrator may not redispatch the failing unit alone.

## 4. File Bounds

| File | Access |
|---|---|
| `docs/tranches/X/design/draft-fable.md` | create |
| `docs/tranches/X/design/draft-opus.md` | create |
| `docs/tranches/X/design/APOTHEOSIS.md` | create |
| `demo/DESIGN.md` | modify (full re-author) |
| `demo/shared/ui/PaneHeader.vue` | modify |
| `demo/styles/animations.css` | modify-carve (delete `:184` and `:202` reduce-kill blocks) |
| `demo/picker/header.css` | modify |
| `demo/palettes/browser/card/PaletteCard/PaletteCard.vue` | modify |
| `demo/styles/foundation.css` | modify-carve (`.underline-tabs:554` survivor only) |
| `demo/shell/PaneSegmentedControl.vue` | modify (Ad-18 marker) — X-W5 holds `delete` on this file; if that delete landed, the marker is discharged by deletion, recorded as the survivor's terminal event, and this row is spent |
| `demo/shell/dock/DockViewSelect.vue` | modify (Ad-18 marker) |
| `docs/tranches/V/VISUAL-CONSTITUTION.md` | modify-carve (fold + tombstone header) |
| `docs/tranches/V/OPTICAL-BENCH-COMPOSITIONS.md` | modify-carve (P-2 citations, 7 lines) |
| `docs/frontend-design/color-picker.md` | modify-carve (fold + tombstone header) |
| `docs/tranches/R/audit/R.W4-visual-runtime/transition-inventory.md` | modify (tombstone header only — a sealed R-tranche audit record is never rewritten) |
| `docs/tranches/V/megatranche/audit/probes/prm-scroll-timeline.mjs` | execute, no write (G-4's probe, re-run unmodified — bounded because the gate invokes it) |

Do NOT touch: `src/**`, `api/**`, `test/**`, `e2e/**`, `scripts/dev/dev.sh`,
`node_modules/@mkbabb/glass-ui/**`, `docs/tranches/V/PALETTE-CONTRACT.md` (X-W7's contract),
`docs/tranches/V/PROPORTION-AUDIT.md` (evidence record — read, re-measure, never fold in place),
`docs/tranches/V/EVIDENCE.md`, `docs/tranches/V/archive/**`, `docs/tranches/V/coordination/**`,
`demo/ui/**` (X-W8).

### 4a. Disjointness

No two units share a `modify` or `modify-carve` path. `.a` and `.b` create distinct files. `.c`
owns `demo/DESIGN.md` alone. `.d` owns the three motion/spacing files; `.e` owns the one card;
`.f` owns the satellites, `foundation.css`, and the two Ad-18 sites. `PaneHeader.vue` carries both
CC-099 and CC-101 and is therefore ONE unit (`.d`), not two — the brief's own ruling that the two
must land coherently.

**Cross-wave.** Five §4 paths are held by closed predecessors, never by a concurrently-open wave —
`demo/palettes/browser/card/PaletteCard/PaletteCard.vue` by **X-W7** (`modify-carve`, `W7.md:98`),
`demo/styles/animations.css` by **X-W5** (`modify-carve`, `W5.md:104`), `demo/styles/foundation.css`
by **X-W5** (`W5.md:103`), `demo/shell/PaneSegmentedControl.vue` by **X-W5** (`delete`, `W5.md:100`),
and `docs/tranches/V/VISUAL-CONSTITUTION.md` + `docs/tranches/V/OPTICAL-BENCH-COMPOSITIONS.md` by
**X-W5.e**'s E1/E2 strike (`W5.md:110,112`) — every one inherited after that wave's close, which §2
Opens-after already requires.

### 4b. Worktree Plan

Phase 1 (`.a`, `.b`) and phase 2 (`.c`) commit before the next phase dispatches, so phase 3 starts
from clean main. Phase 3 parallelizes across sibling worktrees:

| Agent unit | Sibling worktree absolute path | Build dir |
|---|---|---|
| X.W10.d | `/Users/mkbabb/Programming/value-js-x-w10-d` | per-worktree `node_modules`; no shared build dir (no Rust target, `CARGO_TARGET_DIR` N/A) |
| X.W10.e | `/Users/mkbabb/Programming/value-js-x-w10-e` | as above |
| X.W10.f | `/Users/mkbabb/Programming/value-js-x-w10-f` | as above |

The orchestrator runs `git worktree list` and `git worktree add` before phase-3 dispatch.

## 5. Agent Units

**Model seats (M-23; an undeclared seat is a defect).** Phase 1 is the twice-authored design lane
(M-23 §3): `.a` Fable ∥ `.b` Opus 5, blind-independent, both through the frontend-design plugin.
Phase 2 (`.c`) is a **fresh** Fable adjudicator (M-23 §1). Phase 3 (`.d`, `.e`, `.f`) authors no
design — it applies the canon already ruled at `.c` — so every phase-3 unit is an **Opus 5
implementation seat (M-23 §2)**.

### X.W10.a Design canon draft (Fable seat)

- Goal: an independent, complete candidate canon that a component author could work from tomorrow.
- Mechanism: Fable seat per M-23 §1, through the `frontend-design` plugin. Blind — no sight of
  `.b`, no favored narrative. Read-only against the tree; writes one file.
- Files: `docs/tranches/X/design/draft-fable.md` (create).
- Sub-gate: the draft covers all seven sections of the brief's order, carries a model receipt, and
  every load-bearing number is re-measured in-seat with the command pasted. No memory recall.

### X.W10.b Design canon draft (Opus seat)

- Goal: as `.a`, independently derived.
- Mechanism: Opus 5 seat per M-23 §3, through the `frontend-design` plugin. Blind.
- Files: `docs/tranches/X/design/draft-opus.md` (create).
- Sub-gate: as `.a`. Blindness is falsifiable: a shared section skeleton or a shared mtime minute
  reads as non-blind (the M6 common-law failure mode, provenance row 15) and voids the pass.

### X.W10.c Design canon apotheosis

- Goal: one canonical `demo/DESIGN.md` that supersedes both drafts and every satellite.
- Mechanism: a **fresh** Fable arbiter (M-23 §3), obligated under L-14 to attempt refutation of
  both drafts against the tree — not to average them. Section order per the ratified brief:
  three laws → material/elevation → proportion/derivation → chroma fidelity → motion → product
  surfaces → anti-patterns. Motion folds `vnext/DESIGN-PROGRAM.md:170`'s six-tier table wholesale.
  Citations anchor to section names, never to line numbers of deletable files.
- Files: `docs/tranches/X/design/APOTHEOSIS.md` (create), `demo/DESIGN.md` (modify).
- Sub-gate: the apotheosis records a named refutation of at least one load-bearing claim from each
  draft; `demo/DESIGN.md` closes G-1, G-2, G-3; OP-2/OP-3/OP-4 are raised as marks, never as picks.

### X.W10.d Pane header motion and condense register

- Goal: reduced-motion users get no scroll-linked transform, and a condensed header is actually
  condensed.
- Mechanism: Opus 5 implementation seat (M-23 §2). (CC-099) move the three declarations at
  `PaneHeader.vue:178-193` inside `@media (prefers-reduced-motion: no-preference)` — the house
  idiom already in tree at six blocks across five files — then delete the blunt reduce-kill at
  `animations.css:184` and `:202`, whose headline sentence is false for this construct. Structure,
  not a stacked override (L-8).
  (CC-101) give `pane-title-shrink`/`pane-desc-shrink` a spacing channel so the condensed header's
  block padding and title→content gap collapse proportionally; `picker/header.css`'s condense rung
  follows the same register.
- Files: `demo/shared/ui/PaneHeader.vue`, `demo/styles/animations.css`, `demo/picker/header.css`.
- Sub-gate: G-4 and G-5 both green; the `vj-*` family count stays three.

### X.W10.e Shadow geometry and elevation semantics

- Goal: a cast shadow that follows the silhouette it is cast from.
- Mechanism: Opus 5 implementation seat (M-23 §2). Apply the canon's ruled treatment at
  `PaletteCard.vue`, the sole consumer of `cartoon-cast`/`cartoon-surface`. The nested-spread cure
  stays labelled **UNVERIFIED** until the pixel-diff proves it. No `overflow: hidden` (the deliberate S.W5-10/S-15-A refusal at `:16`
  stands). No local patch of producer bytes; a producer ask only if adjudication pins producer
  ownership.
- Files: `demo/palettes/browser/card/PaletteCard/PaletteCard.vue`.
- Sub-gate: G-6 green; the WELL material (bg-well, ratified Q4/T.W3-1 rung-2) is unchanged — a cure
  that blurs the cast changes the material and fails.

### X.W10.f Citation wiring, satellite fold, and bundle disposition

- Goal: nothing outside `demo/DESIGN.md` legislates design, and the DR-13 container is dead.
- Mechanism: Opus 5 implementation seat (M-23 §2). (CC-102) replace the bare constants in live
  canon with a citation to the named producer contract. (CC-103) enumerate the bundle's survivors
  by name — the enumeration is the first act, because the count itself drifted 8→7→7→8→9→11 with
  no add/remove event ever recorded — then give each a terminal verb. Measured survivors at HEAD:
  1. **SelectTrigger height** — 12 of 13 `<SelectTrigger` sites hand-set `h-9`. Disposition:
     landed-as-local with rationale, or deleted; if X-W4's `size="xs"` adopt (CC-045) has already
     landed, the ask is discharged and recorded as such.
  2. **`Ad-18` clampLabel workaround** — 2 live source markers
     (`PaneSegmentedControl.vue:45`, `DockViewSelect.vue:57`) citing a tranche-A id in shipping
     source. Disposition: local with rationale, or the marker comment dies with the workaround —
     re-measured at open, since X-W5 deletes `PaneSegmentedControl.vue` and may discharge one of
     the two markers by deletion before this wave opens (that deletion IS the recorded event).
  3. **`variant="mono"`** — 0 occurrences repo-wide; the consumer anchor vanished. Disposition:
     DELETE the ask, recorded.
  4. **`.underline-tabs`** — still shipping at `demo/styles/foundation.css:554`. Disposition:
     local cross-feature recipe with owner-accepted rationale, or deleted.
  An ask unactionable from this repository is a letter, not a row.
  (Satellites) tombstone `VISUAL-CONSTITUTION.md`, `docs/frontend-design/color-picker.md`, and
  `transition-inventory.md`, each naming what moved where. (Optional, owner-word only) CC-018's
  fresh SMALL taste bracket set, derived at a dated owner sitting — **not gated by this wave**.
- Files: `docs/tranches/V/VISUAL-CONSTITUTION.md`, `docs/tranches/V/OPTICAL-BENCH-COMPOSITIONS.md`,
  `docs/frontend-design/color-picker.md`,
  `docs/tranches/R/audit/R.W4-visual-runtime/transition-inventory.md`, `demo/styles/foundation.css`,
  `demo/shell/PaneSegmentedControl.vue`, `demo/shell/dock/DockViewSelect.vue`.
- Sub-gate: G-7, G-8, G-9 green.

## 6. Hard Gate

Nine conditions. **All nine are RED at authoring** — every baseline below was measured read-only on
2026-08-03 against the working tree at `c654824e`; commands and outputs are pasted, none inferred.
Each states how it can fail for its intended reason (L-19).

**G-1 — THREE-LAWS-STATED.** Each of GOLDEN GLASS, BREATH OF LIFE, MOVEMENT OF MOMENTUM is defined
exactly once in `demo/DESIGN.md`, normatively (a MUST/MAY clause, not a mention), each with its
provenance cite.
*RED baseline*: `grep -ic` per term over the four value.js design authorities
(`demo/DESIGN.md`, `VISUAL-CONSTITUTION.md`, `PROPORTION-AUDIT.md`, `PALETTE-CONTRACT.md`):

```
demo/DESIGN.md              golden-glass=0 breath=0 momentum=0
VISUAL-CONSTITUTION.md      golden-glass=0 breath=0 momentum=0
PROPORTION-AUDIT.md         golden-glass=0 breath=0 momentum=0
PALETTE-CONTRACT.md         golden-glass=0 breath=0 momentum=0
```

**0/4 on all three terms.** *Falsifier*: a term named without a normative clause, or without its
provenance cite, fails; deleting any definition turns it red. GOLDEN GLASS additionally fails if it
is stated as enacted law rather than a mark awaiting owner ratification (OP-3).

**G-2 — ONE-DESIGN-DOC.** `demo/DESIGN.md` is cited by at least one megatranche registry file and
at least one demo source comment, and no other document claims design authority.
*RED baseline*: cross-citation among the four authorities is **0 in all 12 cells** — the 7
`DESIGN.md` hits inside `demo/DESIGN.md` all resolve to the *glass-ui* document, not to any
value.js authority; `grep -n "VISUAL-CONSTITUTION\|PROPORTION-AUDIT\|PALETTE-CONTRACT"` over all
four returns nothing. *Falsifier*: delete the citing line in the registry or the code comment and
the gate goes red. Consumer named (L-19): the component author and the registry — a law nothing
cites is folklore with a filename.

**G-3 — CANON-ANCHORS-LIVE.** Every file/section anchor in `demo/DESIGN.md` resolves at HEAD.
*RED baseline*: 5 anchors point into `demo/@/`; `[ -d demo/@ ]` → **NO**;
`find demo -name style.css | wc -l` → **0** (the doc cites that stylesheet seven times). The census
recorded 12 of 13 anchor rows defective. *Falsifier*: delete any cited file and the gate must go
red; an anchor expressed as a line number of a deletable file fails by construction, since the
gate accepts section names only.

**G-4 — PRM-NO-PREFERENCE (CC-099).** With `reducedMotion: reduce`, the computed transform of
`.pane-header-title` after a 200px scroll equals its pre-scroll value, in both engines.
*RED baseline*, recorded probe (`node docs/tranches/V/megatranche/audit/probes/prm-scroll-timeline.mjs`):

```
webkit   rm=reduce         titleTransform matrix(1,0,0,1,0,0) -> matrix(0.310808,...)
webkit   rm=no-preference  titleTransform matrix(1,0,0,1,0,0) -> matrix(0.310808,...)
chromium rm=reduce         titleTransform matrix(1,0,0,1,0,0) -> matrix(0.618029,...)
chromium rm=no-preference  titleTransform matrix(1,0,0,1,0,0) -> matrix(0.618029,...)
```

The reduce row and the no-preference row are identical to the last digit. Static co-measurement at
HEAD 2026-08-03: `animation-timeline` = **4 declarations, all in `demo/shared/ui/PaneHeader.vue`**;
blunt reduce-kill blocks at `demo/styles/animations.css:184` and `:202`; conforming
`no-preference` blocks = **6 across 5 files** (`animations.css:43`, `SpectrumCanvas.vue`,
`DockStatusLamp.vue`, `overture.css` ×2, `ApiOfflineChip.vue`) — the migration baseline, not a
greenfield. *Falsifier*: re-declare any of the three animations outside a `no-preference` block and
the gate goes red in both engines. Post-cure values pin **per engine** (0.311 WebKit / 0.618
Chromium under no-preference); a single cross-engine number would be a false gate.
*Not a grep gate*: this is a computed-style assertion in a real engine. Any optional grep is a
convenience, never the evidence.

**G-5 — CONDENSE-SPACING-REGISTER (CC-101 / OM-18).** At `scrollTop ≥ 120`, the pane header's
block-size AND its title→content gap are each strictly smaller than at `scrollTop 0` by the ratio
canon states, measured at 1440×900 and 390×844; a before/after DELTA pair is filed against
`audit/visual/owner-marked/OM-18-card-shrunken-state-margins.png`.
*RED baseline*: the three condense keyframes at `PaneHeader.vue:200-222` declare **`opacity` and
`transform` only** — zero spacing channel. `grep -n "padding\|gap\|margin"` over the file returns 3
hits, none inside a condense keyframe. `transform: scale()` shrinks glyphs and leaves the box's
layout height and paddings untouched, which is exactly the owner's mark: *"the shrunke/scrolled
state of the cards is not quite right, has too much margin and padding."* Both measured deltas are
**0 today**. *Falsifier*: a cure that scales glyphs harder without moving the box leaves the delta
at 0 → red. Consumer-side per M-22 §2 / I-21a; no producer ask before TR#79.

**G-6 — SHADOW-GEOMETRY + ELEVATION-SEMANTIC (CC-100).** Canon states what a shadow claims
(elevation: in-chrome vs free-floating registers) and that a multi-layer cast shares one outer
silhouette; the rendered `PaletteCard` corners carry no facet at the pinned viewport, proven by
pixel-diff against `OM-2-card-shadow-sharp-corners.png` (sha256 `2e223c1a…3de8`) plus OM-11/OM-12.
*RED baseline*: `grep -ic elevation` over the four design authorities → **0/0/0/0**. The mechanism
at `node_modules/@mkbabb/glass-ui/dist/styles/glass/glass-atom.css` — `.cartoon-cast` paints
`var(--shadow-cartoon-md)`, three layers at `-3px 3px 0` / `-5px 5px 0` / `-7px 7px 0`, **every
layer blur radius 0**, so three offset silhouettes union into visible facets at a rounded corner.
Sole consumer: `PaletteCard.vue` (5 references; deliberate NO `overflow-hidden` at `:16`).
*Falsifier*: the nested-spread cure is labelled UNVERIFIED — a cure that blurs the cast passes a
naive corner check while changing the ratified WELL material, and fails the material clause. The
gate is a witness diff, not a token grep.

**G-7 — PRODUCER-CONTRACT-CITATION (CC-102 / P-2).** Every occurrence of `38.1966011` or
`61.8033989` in **live canon** cites the named producer contract; none restates it as independent
truth.
*RED baseline*, 2026-08-03: 9 restatement lines across the two live canon docs —
`OPTICAL-BENCH-COMPOSITIONS.md` **7**, `VISUAL-CONSTITUTION.md` **2** — with **0** producer-contract
citations. Demo source: **0 occurrences** (the constants live only in prose). Denominator named
explicitly (L-19): archive, coordination, evidence, and sealed audit records are **excluded** — 5
further occurrences live there and are history, not law. *Falsifier*: adding a bare constant to
live canon turns it red. The gate flips from "cite the contract" to "consume the token" only when
CC-112's TR#78 publication lands — until then, no consumer reads dist bytes to write its canon.

**G-8 — DR-13 BUNDLE TERMINAL (CC-103).** The bundle container does not exist; every survivor has
one terminal verb (landed-as-local with owner-accepted rationale, or deleted) with an add/remove
event recorded.
*RED baseline*, measured at HEAD: `<SelectTrigger` sites = **13**, of which **12** hand-set `h-9`;
`Ad-18` markers in shipping source = **2** (`demo/shell/PaneSegmentedControl.vue:45`,
`demo/shell/dock/DockViewSelect.vue:57`); `variant="mono"` = **0** occurrences;
`.underline-tabs` still shipping at `demo/styles/foundation.css:554`. *Falsifier*: a survivor that
ends the wave in neither state — or a count that changes without a recorded event, which is the
original defect — fails. PEER-AUTHORSHIP-REQUIRED is not an acceptable terminal state.

**G-9 — SATELLITE FOLD.** `VISUAL-CONSTITUTION.md`, `docs/frontend-design/color-picker.md`, and
`transition-inventory.md` each carry a tombstone naming what moved where; no document outside
`demo/DESIGN.md` carries unsourced normative design language, and `PROPORTION-AUDIT.md` /
`PALETTE-CONTRACT.md` are named in the canon's §Authority as **evidence and data contract, not
design law**.
*RED baseline*: 4 live authorities (388 / 228 / 83 / 329 lines) plus 2 satellites, **0 tombstones**;
`transition-inventory.md` last written 2026-07-04, before the entire demo restructure, and is cited
as "the standing inventory" by `animations.css:63-65`. *Falsifier*: a satellite surviving with
MUST/never/always language and no tombstone → red. Any PROPORTION-AUDIT number folded into canon
must be **re-measured first** (the 3.4× undercount, census §4.3 / CONTRIVANCE-REGISTER WA-15).

## 7. Format And Lint Cadence

`npm run lint` and `npm run typecheck` after each phase-3 integration batch and before close;
`npm run test` (vitest) before close; `git diff --check` on every commit. Prettier over the touched
`.md`/`.css`/`.vue` files at each commit. Documents have no repository-wide linter — the
replacement evidence is G-3's anchor resolution plus `git diff --check`. **No proof-farm script is
authored for any gate** (L-19): G-4/G-5/G-6 run inside the existing e2e/visual harness that X-W1
establishes (CC-030); the rest are one-line checks recorded in the close report.

## 8. Verification Artefacts

- `docs/tranches/X/design/draft-fable.md`, `draft-opus.md`, `APOTHEOSIS.md` (with model receipts).
- `docs/tranches/X/waves/W10-CLOSE.md` — the nine gates with RED-before / GREEN-after outputs pasted.
- Before/after DELTA pairs at 1440×900 and 390×844 for G-5 (vs OM-18) and G-6 (vs OM-2/11/12),
  saved under `docs/tranches/X/evidence/W10/`, each with its sha256.
- The re-run `prm-scroll-timeline.mjs` output, both engines, both preference states (G-4).
- The DR-13 survivor table with one terminal verb per row (G-8).
- Commit hashes for each phase.

## 9. Commit Plan

- `docs(x-w10): blind design drafts — Fable and Opus seats` (phase 1, one commit, model receipts in
  the bodies).
- `docs(x-w10/canon): re-author demo/DESIGN.md from the twice-authored apotheosis` (phase 2; body
  states which claims of each draft were refuted).
- `fix(x-w10/pane-header): scope scroll-driven motion to no-preference; give condense its own
  spacing register` (unit .d; body carries the two born-RED outputs and the per-engine post-cure
  values).
- `fix(x-w10/palette-card): cast shadow shares one outer silhouette` (unit .e; body carries the
  witness shas).
- `docs(x-w10/wiring): cite the producer proportion contract; fold the design satellites; retire
  the DR-13 bundle` (unit .f; body carries the survivor table).
- `docs(x-w10): close report + evidence` (orchestrator, at close; this close stamps **IMPLEMENTED**
  — VERIFIED is X-W11's release close to stamp, never this one).

Pathspec commits only. `scripts/dev/dev.sh` is unowned and never touched.

## 10. Dependencies

- **Depends on**: X-W5 (route/scene structure settled), X-W6 (instruments settled), X-W7 (palette
  panes settled), X-W8 (barrel/graph subtraction — the canon must not cite a deleted import path),
  X-W9 (library surface settled); X-W0 (the CC-104/OP-1 ruling, the CC-018 tombstone, the tracked
  evidence tree from CC-012); X-W1 (CC-030's visual-oracle harness — G-5 and G-6 run inside it).
- **Blocks**: X-W11 (release and verified close — CC-009 must read VERIFIED in the release table).
- **Not opened here**: CC-112 (glass bank; BLOCKED-ON glass TR#78's canon publication). If still
  blocked at X-W11, it enters the release table as BLOCKED-ON with that exact condition.

## 11. Archaeology

Three prior attempts at this work, each with its guardrail:

1. **DR-16 (HG6 taste certification)** — the verdict file was byte-identical empty across three
   closes and 19 of 20 brackets were silently dropped. *Guardrail*: CC-018 is OPTIONAL here and
   ungated; a bracket set may only be derived at a dated owner sitting, and a certification with no
   recorded verdict is not a close condition.
2. **DR-13 (the primitive-ask bundle)** — carried through 14+ closes while its own count drifted
   8→7→7→8→9→11 with no add/remove event; two explicit ship-or-KILL rulings produced zero decisions
   because the waves owning them never ran. *Guardrail*: G-8 kills the container first and requires
   a terminal verb per named survivor; PEER-AUTHORSHIP-REQUIRED is not a state.
3. **The design-canon census's own PRM baseline** — it claimed "1 of 19" conforming blocks; the
   adjudicating seat measured **6 blocks across 5 files** and corrected it. A gate born with a
   false baseline fails its own duty on day one. *Guardrail*: every baseline in §6 was re-measured
   read-only on 2026-08-03 with the command and output pasted, and §5.f re-measures any
   PROPORTION-AUDIT number before folding it.

## 12. L-18 rider — acceptance is not close

Landing the nine gates green makes this wave IMPLEMENTED, not ACCEPTED. Per **L-18**, no
implemented wave is ACCEPTED until it has survived **two** challenging gestalt passes, each a
**quartet of Opus 5 skeptics** working the three altitudes (total-tranche: was this wave optimal in
the greater plan, was the spec adhered to, what friction arose · wave · feature), each assuming the
implementation is wrong on some reasonable basis. Frontend bases apply directly here: poor
spacing/margin usage, defected or weak glass-ui design language or component usage, trite or cliché
prose or designed items — the last of which is this wave's specific exposure, since it ships prose
as law. Superlatives are reported alongside defects, each with provenance and proof. Both quartets
are then adjudicated and agglomerated by a **fresh Fable instance** into an apotheosis; any
incongruity surfaced is fully addressed, ratified, and dispatched — never parked.

Reference, not ceremony: this section changes who signs the wave off, not what §6 measures.

**Four-verb status**: this file makes X-W10 **SPECIFIED**. It is not AUDITED beyond the baselines
pasted above, not IMPLEMENTED, and not VERIFIED. Per the ratified convention, gates-green at this
wave's own close stamps **IMPLEMENTED**; **VERIFIED is stamped only at X-W11's release close**
(browser and real-Safari receipts in separate cells, plus the consumer tuples) — which is why §10
requires CC-009 to read VERIFIED in *that* table and not in this one.
