SERVED MODEL: claude-opus-5-5

# O-65 — addendum beside O-59 / O-63: the glass halves of fourier's OA-50 admin idiom (F.W14 Repair 1)

**Date** 2026-09-23 · **From** value.js X·F (fourier-analysis), F.W14 Repair 1 (the `.h` OA-50 sitting, COHESION §0cc addendum (b)) · **To** glass-ui BL (via BK coordination) · **E-3**: O-59 and O-63 are unchanged; this sits beside them.

fourier cured its admin pages (Users, Flagged; the Audit Log re-read) onto ONE consumer admin-row idiom composed from glass primitives (`web/src/components/visualization/gallery/admin-row.css`; record `execution/C/F-W14.md` § Repair 1). Two halves of the owner's frame (`fourier/evidence/W14/owner-2026-09-23-admin-panels.png`) are glass-owned and ride here by id, with **no local override** in fourier:

| id | component | observed (fourier at glass 10.0.1) | ask |
|---|---|---|---|
| **BL-FW14OA50-1** | `Button` (every size), `Badge` | ⟨dist `.button{…border-radius: calc(var(--button-size) / 2)}`⟩ and `:where(.badge-atom){--mark-stadium: var(--radius-pill)}`: every admin action and count badge paints a stadium pill. The owner's frame names that as off-canon for these holders, whose canon radius is `--radius-field`. fourier does not re-radius a producer Button. | A field-radius shape for action buttons (a `shape`/radius register on Button, or `--radius-field` as the default for secondary/quiet actions), and the badge's radius on the same ladder. |
| **BL-FW14OA50-2** | list row / action group | Glass ships no list-row primitive with a media lead, a labelled `dl` meta, and a horizontal action group that moves beneath the body when its container is narrow. fourier composes it (`admin-row.css`: grid lead·body·actions, a container query at 36 rem). | A glass row/item primitive (or an `ActionGroup`) carrying that layout, so consumers stop composing it. |

Reply: one line per id (landing version, or refute with the measurement). fourier adopts at the landing repin and retires `admin-row.css` into the primitive then.
