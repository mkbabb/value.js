SERVED MODEL: claude-opus-5-5

# O-64 — keyframes.js KF.W13R.d · the dock small↔large morph measured at glass 10.0.1 on keyframes' served page (DOCK-MORPH-ROOT, OA-41/OA-48)

**From**: value.js tranche X, seat KF.W13R.d (COHESION §0bs · §0bz, 2026-09-23) · **To**: glass-ui BK dir, for BL (the dock design family; beside O-55/O-56/O-60/O-63) · **Mirror**: `glass-ui/docs/tranches/BK/coordination/valuejs-outbound-2026-09-23-kf-w13r-dock-morph.md`, byte-identical. **Installed**: `@mkbabb/glass-ui` **10.0.1** (keyframes.js `d94017ff`). **Instrument**: headed Chromium, real GPU (`ANGLE Metal Renderer: Apple M5 Max`), 1440×900, a per-rAF sampler on `[data-dock-tether=top|bottom] .glass-dock`; 8 runs = dev (`localhost:5173`) ×2 + the gh-pages build ×2, each before and after keyframes' one consumer cure (which touches no dock byte, so the 8 runs are 8 readings of the same dock); load average 34–138 recorded per run. Evidence: `value.js/docs/tranches/X/keyframes/evidence/W13R/d/` (`sampler.js`, `morph-probe.mjs`, `report-*.json`, WebMs per transition). The owner, verbatim (OA-41): *"the dock transitions and animations from small to large are blurry, slow, and jittery--not smooth and ios 27 loke"*; (OA-48): *"all of the dock transitions still are awful and blurry"*.

**What 10.0.1 already cured (credit, measured on kf):** 0 frames with a `filter`/`backdrop-filter` blur on any text-bearing element of either dock, in morph or after settle, in all 8 runs (the KFA-53 blur is gone); the row never wraps (1 row per visible frame, dock height 56 px on every frame); the corner never goes negative (dock `9999px`, plate ≥ `50px` on every frame); the top dock's morph never overshoots its target.

**What is still live (producer; keyframes writes no morph byte and copies none):**

- **R-1 · first-morph stale endpoint (G-1 / KFA-8, both docks).** The top dock mounts `collapse="closed"`; its FIRST expand moves 56 → 454.49 px in **one frame (398.49 px)**, 8 of 8 runs, while `data-morphing` runs 544–551 ms. The transport dock mounts expanded; its FIRST collapse moves 278.59 → 56 px in **one frame (223 px)**, 8 of 8. Every later morph travels over 225–257 ms (largest single-frame step 19–69 px). The endpoint the first morph reads was never measured.
- **R-2 · content-width change while expanded is not morphed.** A scene switch (square ↔ spring) changes the expanded dock 470.26 ↔ 453.47 px in **one frame (16.79 px)** with **0** `data-morphing` frames, 23 of 24 switches. The 24th (gh, before-run 1) started from a 465.5 px plateau and settled 453.47 after dipping **1.06 px past the target** with one reversal — R-close3-1 (1.1 px at 7.0.0) reproduces at 10.0.1, gh only, 1 in 24.
- **R-3 · sub-pixel overshoot on the transport expand.** Every non-first transport expand ends 279 → 278.59 px: **0.41 px past the target** and one reversal, 7 of 8 runs (the 8th rested at 276.27 px and landed dead).
- **R-4 · the morph clock outlasts its own rung.** The morph's width travel (first to last frame off target) takes **225–257 ms** against `--spring-dock-duration` = `calc(0.21s * 1)` (the `dock` preset the morph springs on: response 0.30, ζ 0.88), +15–47 ms; the `data-morphing` window stays up **533–553 ms** (≈ `--duration-panel` 0.55 s), and for 4–55 of those frames per morph the text sits under a non-identity `scale`/`transform` on its chain (0 frames at settle). A soft, long tail is the "slow … not iOS" the owner names.
- **R-5 · two owners of one property on the first expand.** `.dock-select-trigger::before` `opacity` carries two running animations for 27–31 frames on the first expand (8 of 8); the specular `::before` is glass's (keyframes styles no trigger pseudo-element).
- **R-6 · the KF.W13U `.d2` gh open-clause read (OA-32 icon gate).** On the gh build the expanded Scene glyph's 6 px ring changes over 1 s (diffOutside, run 1/run 2: cube 191/191 · amiga 0/530 · square 306/531 · easing 309/152 · spring 0/0 · sequence 0/311) while the glyph's box is still (bboxDelta 0). The trigger carries **0** running animations at every site and is un-hovered on five of the six (sequence, hovered in both runs, reads 0 then 311), so the earlier "hovered capsule" attribution does not hold. Under that ring the stack is the trigger → `.dock-layer--full` → `.dock-layers` → `.dock-plate` (backdrop-filter, two running scroll-timeline animations `gl-dock-cap-inline-start|end`), the same on dev, where the clause reads 0. Attributed to the plate re-sampling what lies behind it, not proven.

**Asks (for BL's dock family):** measure the expanded/collapsed endpoints before the first morph (R-1); morph a content-width change of an expanded dock on the same spring (R-2); land the width dead on the target with no sub-pixel ring (R-2/R-3); settle the morph window and the layer `scale` on the dock rung's own clock (R-4); one owner for the specular `::before` opacity (R-5); confirm or refute the plate-resample reading of R-6. keyframes re-reads every row at the landing repin (§0cb R-5); until then they stand as honest-RED **DOCK-MORPH-ROOT** in KF.W13R.
