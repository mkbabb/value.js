SERVED MODEL: claude-opus-5[1m]

X-W2 · unit X.W2.d (dispatched at REPAIR 1, round 1) · G6 — THE o5 SPIKE LEG, RE-MEASURED
=========================================================================================
Seat clock : 2026-09-17, 19:36-20:0x EDT.  Branch tranche-u.  Track A.
Authority  : docs/tranches/X/waves/W2.md §3 scope 7 · §5 `### X.W2.d` · §6 G6 (L186-195) ·
             §8 (L265) · §9 row 4.  Curing CHECK 1's D-1 (HIGH).
Origin     : BUILT dist/gh-pages served by e2e/smoke/perf/serve-built.mjs on :8091.
             NO dev-server number appears anywhere in this file (MT-F011).
Pin        : macOS 26.4.1 (25E253) · Apple M5 Max · node v26.0.0 · @playwright/test 1.60.0 ·
             Chromium headless (SwiftShader) · serve-built.mjs :8091
             — byte-identical to BEFORE.json's and AFTER.json's runnerClass.pin.
Build      : this seat's own clean `npm run gh-pages` (exit 0), eager JS gz = 280,811 B,
             i.e. the POST-cure tree — unit a's boot cut landed at 13f4ddc2.
Renderer   : ANGLE (Google, Vulkan 1.3.0 (SwiftShader Device (LLVM 10.0.0) (0x0000C0DE)),
             SwiftShader driver)

WHAT §6 G6 ASKS FOR
-------------------
  "after the cut, `test.fail()` is deleted (leg green) or the header carries today's
   measured median / max-ratio and today's diagnosed cause. A stale prophecy naming a
   dead wave is not an acceptable third state."
  "The spec's own assertion (maxDelta <= 3 x median) decides it; the seat may not choose
   the verdict, only report it."

THE VERDICT THE ASSERTION RETURNED: RED. The second arm is therefore the one executed —
`test.fail()` STAYS and the header now carries today's numbers and today's cause.
SPIKE_RATIO (3), DROP_RATIO (2), DROP_FRACTION_MAX (0.1) and the `frames.length > 10`
precondition are BYTE-UNCHANGED. Nothing was narrowed, softened, skipped or re-based.


1. THE SPEC'S OWN COMMAND, TEN SITTINGS
---------------------------------------
<cmd> npx playwright test --project=smoke-perf e2e/smoke/perf/o5-boot-pacing.spec.ts

  #   host load (1-min)   outcome
  --  -----------------   ---------------------------------------------------------------
   1        36.29         precondition failed: "no boot frames captured" (frames <= 10)
   2        35.76         precondition failed: "no boot frames captured"
   3        32.37         SPIKE ASSERTION REACHED AND FAILED  <-- the [O-5] line below
   4        16.66         precondition failed: "no boot frames captured"
   5        17.81         precondition failed: "no boot frames captured"
   6        15.93         precondition failed: "no boot frames captured"
   7        13.59         precondition failed: "no boot frames captured"
   8        13.92         precondition failed: "no boot frames captured"
   9        13.68         precondition failed: "no boot frames captured"
  10        12.88         precondition failed: "no boot frames captured"

  Every sitting reported `status=failed expected=failed ok=true` — the test.fail() marker
  is doing exactly its job: the red is honest and CI is not reddened.
  An eleventh sitting (list reporter, load 35.26) reported the same expected failure; its
  stdout is suppressed by the list reporter on an expected failure, which is why the nine
  JSON-reporter sittings above are the ones counted.

  THE [O-5] CONSOLE LINE THAT DECIDES G6 (sitting 3, verbatim):

[O-5] renderer=ANGLE (Google, Vulkan 1.3.0 (SwiftShader Device (LLVM 10.0.0) (0x0000C0DE)), SwiftShader driver) frames=12 median=140.7ms max=2625.0ms (18.7× median) dropped=3 (25.0%)

  and the assertion that failed, verbatim:

Error: boot jitter spike 2625.0ms = 18.7× median (> 3×) — the 44→315ms hole class, cured at W2-3
expect(received).toBeLessThanOrEqual(expected)
Expected: <= 422.09999999999945

  TODAY'S NUMBERS, stated as §6 G6 words them:
    median frame time  = 140.7 ms
    max inter-frame    = 2,625.0 ms
    MAX-RATIO          = 18.7x median   (bar: 3x)          -> SPIKE LEG RED
    dropped-frame ratio= 25.0%          (bar: <= 10%)      -> PILE-UP LEG RED TOO

  The pile-up leg was GREEN at the W2-3 reading (5.3%). It is RED here. See §3 — that
  flip is a BENCH fact, not a product regression, and this file says so rather than
  letting a reader take it for one.


2. THE DIAGNOSIS — WHY TODAY'S CAUSE IS NOT THE PAYLOAD
-------------------------------------------------------
The spec's own console line cannot locate a spike in the window or attribute it, so this
seat ran a scratchpad probe (NOT a repo byte; no product or spec file was modified for it)
that mirrors e2e/smoke/perf/frame-budget.ts installFrameCollector exactly and additionally
records each frame's ABSOLUTE rAF timestamp and the PerformanceObserver('longtask') census
with startTime + duration. Four probes, same origin, same launch args, same viewport:

  probe  host load  frames  median    max        ratio  dropped   max-gap span
  -----  ---------  ------  --------  ---------  -----  --------  ---------------
    P1     46.63      10    141.7ms   2450.0ms   17.3x  3 (30.0%) [ 262, 2712] ms
    P2     43.86      11    140.8ms   2625.1ms   18.6x  3 (27.3%) [ 173, 2798] ms
    P3     25.49      11    141.8ms   2524.9ms   17.8x  3 (27.3%) [ 180, 2705] ms
    P4     22.79       7    150.0ms   2733.0ms   18.2x  2 (28.6%) [ 273, 3006] ms

  (a) THE WINDOW'S ONLY MAIN-THREAD LONG TASK IS 139-238 ms.
      longtask census, whole 4,000 ms window: P1 = one entry [19, 216] · P2 = one entry
      [13, 139] · P3 = one entry [13, 141] · P4 = one entry [11, 238].  That single task
      is the Vue mount, and it ends at 150-250 ms.  The prophecy's "ONE eager-payload
      mount task (the 347.9 KiB gz eager index — RP-2)" now measures 139-238 ms against
      an eager set of 274.2 KiB gz that this wave made 32,790 B lighter.

  (b) ZERO LONG TASKS LIE INSIDE THE MAX FRAME GAP — in 4 of 4 probes.
      sum of longtask duration intersecting the 2,450-2,733 ms gap: 0 ms, every probe.
      The gap carries NO page main-thread work at all.  It opens immediately after
      loadEventEnd (155-273 ms) and closes at 2.7-3.0 s.

  (c) IT IS NOT HOST-LOAD NOISE EITHER.  Across host load 22.79 -> 46.63 the gap reads
      2450 / 2625 / 2525 / 2733 ms — a +/-6% spread with no monotone relation to load.
      (The MEDIAN and the DROPPED FRACTION are load-sensitive; the gap is not.)

  (d) THE HERO ENGINE IS NOT A CANDIDATE.  Within the 4,000 ms window the full JS
      resource census contains NO HeroBlob chunk and `[data-testid="goo-blob-canvas"]`
      count is 0.  A separate probe timed its arrival:
          t≈4000ms  canvas=0  blob-ish chunks=[]
          t≈8000ms  canvas=1  blob-ish chunks=["HeroBlob-CK6WV_Kd.js"]
      The hero's lazily-fetched 94.6 KB chunk arrives AFTER the boot window closes, so
      unit a's cut neither caused nor could cause this gap.

  (e) SEPARATELY: THE 4,000 ms WINDOW YIELDS ONLY 7-12 rAF FRAMES on headless
      SwiftShader — which is why the spec's own `frames.length > 10` precondition fails
      in 9 of 10 sittings and the legs are usually not evaluated at all at this bench.
      Recorded as a finding, NOT cured: lowering that guard would be the narrowed
      assertion this wave's law forbids.

  DIAGNOSED CAUSE, stated exactly as far as the measurement reaches:
  the surviving spike is a ~2.5 s presentation-side rAF / BeginFrame stall that contains
  no main-thread work of any kind, on a headless software-GL (SwiftShader) compositor with
  no animating surface in the window but the atmosphere field canvas.  It is NOT the eager
  payload, NOT the framework mount, and NOT the hero engine.  What it IS — compositor
  BeginFrame production on SwiftShader vs. a real GPU — is not answerable at this bench,
  and W2.md §ENV already says so: "Headless Chromium is software-GL (SwiftShader). Any
  frame-timing leg here inherits that; the real-GPU oracle is CC-029 / X-W1, not this wave."


3. WHAT THIS DOES AND DOES NOT DISCHARGE
----------------------------------------
DISCHARGED (G6's asserted property, second arm): the dead V-prime "W7" prophecy is gone
from e2e/smoke/perf/o5-boot-pacing.spec.ts; the header carries today's median (140.7 ms),
today's max-ratio (18.7x) and today's diagnosed cause; the payload cure it was waiting on
IS this wave's unit a, landed at 13f4ddc2, and the header now says so.  §6 G6's forbidden
"third state" — a stale prophecy naming a dead wave — no longer exists in the tree.

NOT DISCHARGED, and routed by the spec itself, not by this seat: the DISPOSITION of the
test.fail() marker.  §6 G6: "X-W1 (CC-031) rules the disposition of the three `test.fail()`
legs as a class (o26-aurora-perceptibility:57, o16-computed-cascade:34, o5-boot-pacing:48);
X-W2 executes the payload cure and supplies the measurement for this one."  The payload
cure is executed and the measurement is supplied.  Owner of the disposition: X-W1.

NOT A PRODUCT REGRESSION: no product byte moved between the close and this seat
(`git log 13f4ddc2..HEAD -- src demo api vite.config.ts package.json` is empty), and the
two legs' movement against the W2-3 reading is accounted for in §2 above.
