SERVED MODEL: claude-opus-5-5
# X.W7L.m — I-56 traps 1-6, each with its grep or measurement (2026-09-25, HEAD 059ec6af, glass 10.1.0 installed)

| # | trap | command / probe | reading | verdict |
|---|---|---|---|---|
| 1 | 10.0.0 `@layer components`: unlayered consumer CSS now beats library rules; `!important` flips | (a) `m-trap-probe.mjs` (PRM overlay carve-out, served page, light+dark ×2); (b) glass dist `!important` census `cat node_modules/@mkbabb/glass-ui/dist/*.css \| grep -o '[a-z-]*:[^;{}]*!important'`; (c) consumer `grep -rn --include='*.css' --include='*.vue' '!important' demo` | (a) carve-out unlayered = `0s` (`m-trap1-unlayered-carveout.txt`, glass's layered `!important` guard won) → moved into `@layer base` (`demo/styles/animations.css:241`, c8a4959d) → `0.15s` ×2 (`m-trap-probe-r{1,2}.json` `prm.transitionDuration`); (b) glass dist carries 3 `!important` declarations (`filter`/`transition` on `.goo-blob-wrapper`, `transform` on `.hm-mark`, all PRM-scoped); (c) 30 consumer hits = reduced-transparency modality escape + print + PRM global + EasingAuthoringStage sizing — none on a property glass declares `!important`, so none reversed. No-op-override census: `m-dead-classes-7v10.txt` (43 classes glass 7 shipped and 10.1 does not: all Tailwind utilities the demo's own `@import "tailwindcss"` regenerates from `demo/**` sources, plus demo-declared `watercolor-*`/`dropdown-*`; `rounded-input` migrated to `rounded-media`), `m-dead-tokens-7v10.txt` (0 newly dead), `m-inert-pins-7v10.txt` (2 inert pins, `--glass-tint-source/-strength`) | CURED (carve-out) / CLEAN |
| 2 | stale reka bindings + BI.W renames | `grep -rn --include='*.vue' -e <pattern> demo \| wc -l` for `:pressed=` · `v-model:search-term` · `surface="clear"` · `MultiSelect` · `ContextMenu` → 0 each; ` tag=` → 3 (all Vue built-in `<TransitionGroup tag="div">`, not reka); `<Sheet` → 2 (`SheetContent side="right"`, the 10.x API); plus vue-tsc with `checkUnknownProps`+`checkUnknownEvents` (`m-trap2-unknown-props.txt`): 269 TS2353, 0 stale component props ×2 | SelectTrigger's retired `size`/`variant` arms (8.0.0, no MIGRATION row) migrated in c8a4959d | CLEAN |
| 3 | `:global(.dark) .x` in scoped styles dropped | `grep -rn --include='*.vue' ':global(\.dark)' demo \| wc -l` | 0 | CLEAN |
| 4 | `light-dark()` wrapping an inset shadow computes to none | `grep -rn 'light-dark(' demo src \| grep -ci inset` | 0 | CLEAN |
| 5 | two value.js copies via glass → keyframes → value.js 4.0.0 | `npm ls @mkbabb/value.js`; `m-trap-probe.mjs` `copies`; `grep -c 'mkbabb/value.js' node_modules/.vite/deps/_metadata.json` | npm ls: `@mkbabb/glass-ui@10.1.0 └── @mkbabb/value.js@4.0.0` and `keyframes.js@6.0.0 └── value.js@4.0.0 deduped` (one hoisted install copy; the root is value.js itself). On the page: `nodeModulesValueJs 0` · `repoDistModules 6` ×2 light+dark; deps prebundle metadata names value.js 0 times. The vite self-alias set (`vite.config.ts:23-62`) routes every `@mkbabb/value.js*` specifier to this repo, so ONE runtime copy | CLEAN (runtime single copy) |
| 6 | read MIGRATION bottom-up from the pinned version | `grep -n '^## ' node_modules/@mkbabb/glass-ui/MIGRATION.md` | sections crossed from 7.0.0: 8.0.0 (:731) → 8.1.0 (:580, superseded) → 9.0.0 (:376) → 10.0.0 (:8); 10.1.0 has no MIGRATION section (additive: 5 files, all Configurator, I-55). Each break migrated in c8a4959d: `--spring-snappy` retired → `--spring-panel`; WatercolorDot deleted → owned at `demo/shared/ui/watercolor-dot/`; ConfigSliderPane dock `:collapse="false"` (9.0.0 fold); `--radius-input` → `--radius-media`; `--dropdown-menu-font` reader removed → pin deleted; 10.0.0 layering → carve-out to `@layer base` | CLEAN |
