SERVED MODEL: claude-opus-5[1m]

# X.W1.d — DEPLOY DEPENDENCY TRUTH (CC-034 / DR-22) · G-15 · G-16

**2026-09-18** · worktree `/Users/mkbabb/Programming/value-js-x-w1-d` (detached) · gates read at
**`2e1fd65f`** · authorities: `waves/W1.md` §X.W1.d (`:255-264`), §Hard Gate G-15/G-16 (`:315-316`),
B11/B12/B13 (`:93-95`) · `COHESION.md` §0j.F(1)(3) · `X-W1-FOLD.md` §6.1 R56 (`:1202-1239`), §6.2's
G-15 sharpening (`:1249-1254`) · `execution/A/X-W1.md` (the wave record, incl. X.W1.c's hand-off).

Every figure below is read from the settled bytes and double-run. Commands are quoted as
⟨cmd⟩ → output.

---

## 1 · What landed

| # | commit | scope |
|---|---|---|
| 1 | **`62ccf4a1`** | `ci(release): keep npm pack candidate output machine-readable` — cherry-pick of `e2652f1c`, verbatim |
| 2 | **`8a7792b9`** | `ci(release): hash the sole pack-destination archive` — cherry-pick of `44ddaff7`, verbatim |
| 3 | **`7bc72838`** | `ci(deploy-pages/glass-pin)` — the branch checkout and both sibling builds retired; the registry install asserted |
| 4 | **`2e1fd65f`** | `ci(deploy-pages/glass-pin)` — the subpath census in that assertion corrected to the true bytes |
| 5 | *(this file)* | `docs(X·W1 · evidence)` |

### 1.1 The `release.yml` ride-in (COHESION §0j.F(3), wave-record WO-4) — this unit's FIRST commits

The two `ci(release)` hardening commits ride into `tranche-u` **verbatim, one file each**. They are
not re-authored: the diffs were replayed and the resulting blob compared to the source commits'.

⟨`git rev-parse e2652f1c^:.github/workflows/release.yml HEAD_at_open:...`⟩ →
`76379dda` **= `76379dda`** — `tranche-u`'s `release.yml` was byte-identical to `e2652f1c`'s parent,
so both patches applied with zero fuzz and zero conflict.

| after | blob | source blob | verdict |
|---|---|---|---|
| `62ccf4a1` | `4d5c490e` | `e2652f1c:` `4d5c490e` | **identical** |
| `8a7792b9` | `4f9cd042` | `44ddaff7:` `4f9cd042` | **identical** |

⟨`git diff HEAD:.github/workflows/release.yml 44ddaff7:.github/workflows/release.yml | wc -l`⟩ → **`0`**.

Each commit carries its original subject and body plus `(cherry picked from commit …)` and the
session trailer; each was made with its own pathspec on the commit itself. **Recorded, because it is
visible in the log and would otherwise look like a mistake:** the two are *not adjacent* on
`tranche-u` — a Track C seat committed `121dd4f1` between them. Four tracks share this index; the
pathspec discipline is exactly what makes that harmless.

### 1.2 The `deploy-pages.yml` cure

⟨`git diff --stat 7bc72838^ HEAD -- .github/workflows/deploy-pages.yml`⟩ → `52 insertions(+), 29
deletions(-)`; 174 → 197 lines (the growth is comment and the new assertion, not machinery).

**Steps 11 → 8.** Four removed, one added:

| before (11) | after (8) |
|---|---|
| `actions/checkout@v5` | `actions/checkout@v5` |
| ~~`Checkout glass-ui @ tranche/BG …`~~ | — |
| ~~`Checkout keyframes.js (file: workspace dep)`~~ | — |
| `actions/setup-node@v5` | `actions/setup-node@v5` |
| ~~`Build glass-ui dist …`~~ | — |
| ~~`Build keyframes.js dist …`~~ | — |
| `npm ci` | `npm ci` ← **this IS the registry install the declaration implies** |
| — | **`Assert producer provenance (registry) + the demo's subpaths resolve`** |
| `Build value.js dist (the self-alias target)` | unchanged |
| `Build demo SPA (gh-pages mode → dist/gh-pages)` | unchanged |
| `Deploy to Cloudflare Pages` | unchanged |
| `O-25 — assert prod lineage` | unchanged |

⟨`git diff 7bc72838^ HEAD -- …deploy-pages.yml | grep -E '^[-+]' | grep -icE 'wrangler|O-25|CLOUDFLARE'`⟩
→ **`0`** and the same for `workflow_run|conclusion|head_branch|permissions|concurrency` → **`0`**:
**the trigger gate and the shipping half are untouched.** This unit changed the build's dependency
provenance and nothing else.

**The stale comment is retired** (`W1.md:260-261`). The old text named an un-pin waypoint — *"un-pin
at the 5.0.0 master landing"* — that was skipped outright: glass went 4.2.0 → 7.0.0. The replacement
states what is true at these bytes and why the old text was false, rather than deleting the history.

**Why the keyframes.js checkout went with it, stated rather than slipped in.** It is not named in
`W1.md:258-259`, which names glass. It carried the *identical* dead premise and is measured dead by
the same instrument (`@mkbabb/keyframes.js: ^6.0.0`, registry-pinned in the same lock — §3 below),
and it is what makes the amended job reproducible for G-16 **without writing into a sibling repo**,
which is READ-ONLY to this seat. Recorded as a stated extension of the spec's cure, never a
substitution for it.

**What was deliberately NOT changed: the nested-workspace layout** (`defaults.run.working-directory:
value.js` + `path: value.js`). Its stated rationale — *"the `file:` siblings beside it"* — is now
void, but the layout is orthogonal to the provenance defect, it is the shape every recorded
successful deploy ran under, and unwinding it is a structural change no local run can verify while
X.W1.e's G-19 depends on this workflow's first push-arm success. Its comment is corrected to say
exactly that, and it is **booked as a residual for the X-W11 release close** — not left with a false
justification.

---

## 2 · G-15 — BEFORE → AFTER

> **Condition** (`W1.md:315`): *"`deploy-pages.yml` resolves `@mkbabb/glass-ui` from the registry; no
> `ref: tranche/BG`."*
> **Read per fold R56(b)(c) / §6.2** on its **resolution clause** and its **branch-checkout
> falsifier**, **never on the exports-map cell** — that cell is scoped to glass 5.0.0 and is stale at
> the installed 7.0.0. Re-measured at the pin of the day anyway, in §3.

| probe | BEFORE (wave-open baseline, `X-W1.md:137`) | AFTER (`2e1fd65f`) |
|---|---|---|
| ⟨`grep -c 'ref: tranche/BG' .github/workflows/deploy-pages.yml`⟩ | **1** (`:80`) | **0** |
| ⟨`grep -c 'repository: mkbabb' …`⟩ | 2 (glass-ui, keyframes.js) | **0** |
| ⟨`grep -c 'tranche/BG' …`⟩ | 2 (the key + the stale comment) | 2 — **prose only**, in the comment that records what was retired and why |
| glass-ui resolution in the deploy build | undeclared; the job checked a branch tree out and built it | ⟨assert step⟩ → `@mkbabb/glass-ui 7.0.0 <- https://registry.npmjs.org/@mkbabb/glass-ui/-/glass-ui-7.0.0.tgz` |
| the resolution is not a link | unasserted | ⟨`ls -ld node_modules/@mkbabb/glass-ui`⟩ → `drwxr-xr-x` (a real directory, not a symlink); hidden-lockfile `link` unset |

**G-15: RED → GREEN.** The condition is met at the bytes, and it is met *structurally*: the new step
reds the deploy **before the build** if resolution ever reverts (§5).

### 2.1 The finding that changes what this defect WAS — stated loud

**The branch checkout was already INERT.** `package.json:83` declares `@mkbabb/glass-ui: ^7.0.0` and
`package-lock.json` pins `https://registry.npmjs.org/@mkbabb/glass-ui/-/glass-ui-7.0.0.tgz`, so
`npm ci` has been installing registry bytes **regardless of the checked-out sibling** since W44
adopted glass 7.0.0 whole. Measured here, at the strongest available witness: this worktree sits at
`/Users/mkbabb/Programming/value-js-x-w1-d`, **directly beside `/Users/mkbabb/Programming/glass-ui`,
whose working tree is at version 9.0.0** — and `npm ci` still resolved **7.0.0 from the registry**.

So the defect DR-22 names is **not** "the deploy builds against 5.0.0". It is:

1. a **false provenance claim** — the job asserted, in a step name, a producer tree it did not build
   against (the FM-19 shape this tranche exists to kill); and
2. **wasted runner minutes** — two repository checkouts plus two `npm ci && npm run build` cycles
   whose output nothing consumed.

This is why the *literal* falsifier (*"revert to the branch checkout → the deploy build fails to
resolve `./blob`"*) cannot be staged as a live red on today's lockfile: re-adding the checkout flips
nothing, which is itself the measurement. §5 demonstrates the falsifier's **mechanism** on the real
BG bytes instead of faking a red.

**Cross-read with X.W1.c (its hand-off, `X-W1.md:496-504`):** the same pin was booked in
`DISEASE-REGISTRY.md:327` as *"the highest-probability root cause of NV-7"*. X.W1.c falsified that
claim with a one-knob differential (the inline-vs-external build entry) and withdrew it. This seat's
measurement independently agrees and says why: a workflow step that changes no resolution cannot be
the root of a build-shape defect. **CC-034 is not retired by that** — the provenance defect above is
real and is what G-15 cures.

---

## 3 · The producer at the pin of the day, and the subpath census

⟨fresh `npm ci` in the worktree, then `import.meta.resolve` over every glass-ui specifier the tree
imports⟩:

- installed **`7.0.0`**, `resolved` = the registry tarball, `link` unset
- exports map: **74 entries**; `./blob` **true** · `./blob-config` **true** · `./chip` **true**
- the demo imports **20 distinct** `@mkbabb/glass-ui` specifiers — **20 of 20 resolve**, 0 unresolvable

**B13's census is TRUE-AS-WRITTEN and STALE-AS-USED — corrected here, never edited there (E-3).**
`W1.md:95` reads *"`./blob` ×5 sites incl. `useAtmosphere.ts`; `./chip` ×1"*, against the **5.0.0**
map. At today's bytes the six-row family is still six rows, but it has **split across two subpaths**
and the files have **moved**:

| subpath | rows | sites at the true bytes |
|---|---|---|
| `./blob` | **2** | `demo/picker/visual/HeroBlob.vue:34`, `:35` |
| `./blob-config` | **3** | `demo/scenes/blob/BlobPane.vue:12`, `:13` · `demo/color-picker/composables/boot/useAtmosphere.ts:36` |
| `./chip` | **1** | `demo/workbenches/gradient/GradientVisualizer/easing/EasingSpecimenStrip.vue:14` |

Fold R56's *"6 sites"* holds at the count. Its site list (`BlobPane.vue:12/:13`, `HeroBlob.vue:34/:35`,
`useAtmosphere.ts:36`, `EasingSpecimenStrip.vue:14`) holds row-for-row. What has moved is the
**subpath split**: `useAtmosphere.ts` imports `./blob-config`, not `./blob`. The workflow's assertion
therefore covers **all three** subpaths, so the file's prose and the file's code state the same
measured thing. B13 read 18 subpaths; today 20.

**Re-measure-at-repin clause (R56(e)) restated:** at the 8.0.0/9.0.0 repin this map moves again
(X-EXT-2 renames `./forms` → `./input`; measured today as `forms:true`, `input:false`, i.e. the
rename has **not** landed at 7.0.0). **The repin is X-W0.j / X-W4.g's act, not this unit's** — this
unit installed the *declared* `^7.0.0`; the registry's latest is 9.0.0, which does not satisfy `^7`.

---

## 4 · `./blob` and `./chip` resolve **in the deploy build** (the unit's sub-gate, `W1.md:264`)

Resolution is proved twice — once by the resolver, once in the emitted bytes of the artifact that
would ship.

| # | probe | reading |
|---|---|---|
| 1 | the workflow's own assert step, run from the file's bytes | `./blob` → `…/dist/blob.js` · `./blob-config` → `…/dist/blob-config.js` · `./chip` → `…/dist/chip.js` · exit **0** |
| 2 | ⟨`npm run gh-pages`⟩ under the amended steps | **rc 0** — rolldown resolves every import; an unexported subpath is a build error, not a warning |
| 3 | ⟨`grep -rl '#version 300 es' dist/gh-pages/`⟩ — the `./blob` graph's GLSL | **2 files**: `assets/HeroBlob-ATp0M0T-.js` (the async chunk of the only `./blob` importer) and the entry `assets/index-Bzkubr2p.js` |
| 4 | ⟨`grep -rl 'glass-chip__remove focus-ring' dist/gh-pages/assets/`⟩ — a literal from `dist/chip-DFZQr6rV.js` | **1 file**: `assets/_plugin-vue_export-helper-BY3JwTuk.js` (262,271 B) |
| 5 | ⟨`grep -rlE 'blob-config|BLOB_CONFIG' dist/gh-pages/assets/`⟩ | `HeroBlob-ATp0M0T-.js`, `index-Bzkubr2p.js` |

Probes 3–5 are the load-bearing ones: they say the producer's **bytes**, not merely its module
specifier, are in the artifact.

---

## 5 · G-15's falsifier — three arms and a control, on the REAL branch bytes

Full transcript: `g15-falsifier-2026-09-18.txt`. The step body is **extracted from
`deploy-pages.yml` itself** (`YAML.parse(...).jobs.deploy.steps.find(...).run`), never transcribed.
Each arm is a package root in the session scratchpad: **no sibling tree is written to and no
`node_modules` inside any repo is patched** (glass-ui is READ-ONLY, always).

`tranche/BG`'s head re-measured at this clock — ⟨`gh api 'repos/mkbabb/glass-ui/commits?sha=tranche/BG&per_page=1'`⟩
→ **`f3f3c097`, 2026-07-11T22:29:55Z**; ⟨`gh api 'repos/mkbabb/glass-ui/contents/package.json?ref=tranche/BG'`⟩
→ **version `5.0.0`**, 93 exports entries, `./blob` **false** · `./blob-config` **false** · `./chip`
**false**. (B12 = 5.0.0 confirmed a third time; B13's absence claim confirmed against the live head.)

| arm | one knob | exit | message |
|---|---|---|---|
| **1** | resolution reverts to a **linked** sibling tree | **1** | `@mkbabb/glass-ui is a link to file:../glass-ui — the deploy must build against the registry` |
| **2** | a non-registry resolution that is not a link | **1** | `@mkbabb/glass-ui resolved from file:/some/sibling/tree — not the registry` |
| **3** | **the branch-checkout falsifier** — the real `f3f3c097` (5.0.0) `package.json` | **1** | `ERR_PACKAGE_PATH_NOT_EXPORTED: Package subpath './blob' is not defined by "exports"` |
| **control** | the registry 7.0.0 bytes this deploy installs | **0** | all three subpaths resolve |

Arm 3 is the falsifier `W1.md:315` names, executed rather than argued: **a build whose resolution
goes through the `tranche/BG` tree dies at `./blob`.** Per §2.1, on today's lockfile re-adding the
checkout would not by itself route resolution there — so the arm demonstrates the **mechanism** on
the true bytes rather than manufacturing a red the lockfile would swallow. Arms 1 and 2 are the
falsifiers of the *new* guard: they are how a future seat re-introducing a sibling install gets
caught **before** the build.

---

## 6 · G-16 — the prod-preview probe against the amended workflow's exact steps

> **Condition** (`W1.md:316`): *"CC-032's prod-preview probe runs against a build produced by the
> amended workflow's exact steps and is GREEN."*
> **Falsifier**: *"Passing this while G-13 is red on the same commit means the two builds differ —
> both must run on one commit."*

### 6.1 The steps, taken from the workflow's own bytes

Transcript: `workflow-exact-steps-2026-09-18.txt`. A driver parses `deploy-pages.yml`, takes each
build step's `run:` **verbatim**, prints it, and executes it — so "exact steps" is a fact, not a
transcription. The two shipping steps (wrangler deploy, O-25) are deliberately not run: they need CF
credentials and would cut a Production deployment, which would corrupt X.W1.e's G-20 before/after
probe.

| step | as executed | result |
|---|---|---|
| `actions/checkout@v5` (`ref` = the SHA) | worktree detached at `2e1fd65f` | ok |
| `actions/setup-node@v5` `node-version: 24` | **DIVERGENCE: node v26.0.0 / npm 11.12.1** — this host has no node 24 toolchain (`/opt/homebrew/opt/node@24` is itself v26). Recorded, not papered over | ok |
| `npm ci` | `node_modules` removed first; **added 438 packages**, rc 0 | ok |
| `Assert producer provenance …` | exit **0**, three subpaths resolved, registry tarball named | **ok** |
| `Build value.js dist …` | `✓ built in 2.18s`, exit 0 | ok |
| `Build demo SPA (gh-pages …)` | `✓ built in 3.29s`, exit 0 → `dist/gh-pages`, **130 files** (45 JS · 17 CSS), entry `assets/index-Bzkubr2p.js` | ok |

### 6.2 The probe, and the paired G-13 reading on ONE commit

`node scripts/ci/boot-smoke.mjs --mode=prod-preview` — X.W1.c's gate, unmodified, served at a bare
`127.0.0.1:PORT` origin by the script's own static server.

| run | build provenance | result |
|---|---|---|
| **G-16 run 1** | the workflow's exact steps | **5/8** seed cases |
| **G-16 run 2** (double-run) | the same artifact, untouched | **5/8**, same seeds |
| **G-13 (paired)** | the script's own `--build` at the **same commit** | **5/8**, same seeds |

| seed | class | G-16 (workflow build) | G-13 (script build) |
|---|---|---|---|
| `default` | W1.md's literal cold boot | **PASS 4/4** | **PASS 4/4** |
| `deep-link-grey-808080` | chromatic control | PASS | PASS |
| `deep-link-lch-none` | R17 | PASS | PASS |
| `deep-link-oklch-none` | R17 | PASS | PASS |
| `persisted-oklch-none` | R17 | PASS | PASS |
| `deep-link-black` | R16 (hsv-powerless grey) | **FAIL** `PickerColorError: Missing hsv.h` | **FAIL**, same |
| `deep-link-grey-333333` | R16 | **FAIL**, same | **FAIL**, same |
| `persisted-black` | R16, self-perpetuating | **FAIL**, same | **FAIL**, same |

**And the two builds are the same bytes, not merely the same verdicts.**
⟨`find dist/gh-pages -type f | sort | xargs shasum -a 256`⟩ over both builds, then ⟨`diff`⟩ →
**BYTE-IDENTICAL, 130/130 files** (`artifact-sha256-2026-09-18.txt`). The paired-lock falsifier is
discharged in its strongest form: the two gates cannot be reading different builds.

⟨`diff` of the artifact hashes before and after the probe⟩ → **IDENTICAL**: the probe does not
mutate what it measures.

**G-16: RED → GREEN on its W1.md condition** — the `default` cold boot passes 4/4 against a build
produced by the amended workflow's exact steps, and the reading is identical, seed for seed and byte
for byte, to G-13's at the same commit (`2e1fd65f`, which contains X.W1.c's `cad51f9e`).

**The three seed REDs are the fold's born-RED arm and are NOT masked here.** R16/R17's cures are
routed to **X-W9** and **X-W5 + X-W9** (`X-W1.md:562-569`); `boot-smoke.mjs` exits non-zero on them
and names them. This unit added no `test.skip`, no allowlist, no try/catch, no filtered assertion and
no widened bar. Their presence **is the proof that the matrix discriminates**: the same three seeds,
and only those three, fail on both builds.

---

## 7 · Lint and format cadence (`W1.md:325-334`)

| check | result |
|---|---|
| ⟨`actionlint .github/workflows/deploy-pages.yml .github/workflows/release.yml`⟩ | **exit 0, 0 findings** — actionlint **1.7.12**, installed at this seat (it was ABSENT at open); `shellcheck` present, so the `run:` bodies were linted too |
| ⟨`actionlint`⟩ over the whole workflow directory | exit 0 |
| ⟨`npx prettier --check`⟩ on both files | *All matched files use Prettier code style!* |
| ⟨`git diff --check`⟩ before every commit | rc 0 |
| YAML parse + step enumeration | 8 steps, `defaults.run.working-directory: value.js` intact |

The §Format-And-Lint fallback (*"a pushed scratch-branch run of each amended workflow"*) is **not
needed for `deploy-pages.yml` and would have been harmful**: its only manual arm is
`workflow_dispatch`, and the wrangler step is hard-coded to `--branch=master`, so a scratch dispatch
cuts a **Production** deployment — which would destroy X.W1.e's G-20 before/after asset-hash probe.
actionlint (the cadence's first choice) is green, and every `run:` body this unit touched was
**executed** from the file's own bytes, which is stronger evidence than a lint.

---

## 8 · Residuals and divergences

1. **RESIDUAL — the nested-workspace layout now carries no sibling.** Kept byte-for-byte with a
   corrected comment; booked for the X-W11 release close. §1.2 states why it was not unwound here.
2. **DIVERGENCE — node v26.0.0 locally vs `node-version: 24` on the runner.** No node 24 toolchain
   exists on this host. The steps reproduce; the runner's node major does not. Recorded, not hidden.
3. **DIVERGENCE — B13's subpath split and site paths have moved** (§3). Corrected beside, never in
   the dated baseline.
4. **CONSUMED, not cured — X.W1.c's R1 hand-off** (`X-W1.md:576-583`): `npm run gh-pages` has no
   `pregh-pages` hook, so a build on a stale `dist/` has undefined provenance. **The amended workflow
   is immune by construction** — `npm ci` runs `prepare` (which builds the library dist) and the
   explicit `Build value.js dist` step follows it, both before `npm run gh-pages`; this seat's runs
   began from `rm -rf node_modules`. `package.json` is **X.W1.a's** file, so the hook itself is not
   cured here.
5. **NOT this unit's act — the 8.0.0/9.0.0 repin.** `npm view @mkbabb/glass-ui version` → **9.0.0**;
   the declared range is `^7.0.0`, which 9.0.0 does not satisfy. X-W0.j / X-W4.g own the repin, and
   R56(e) requires the exports map be re-measured at it before any citation of B12/B13.
6. **NO ESCALATION.** No Triumvirate trigger fired: no write under `src/`, `demo/` or `api/`; the
   prod-preview probe is GREEN on its condition after the pin (the trigger is a RED there); no
   diagnostic loop reached a third iteration. Writes stayed inside this unit's bounds —
   `.github/workflows/deploy-pages.yml`, `.github/workflows/release.yml`,
   `docs/tranches/X/evidence/w1/deploy-pin/**`, plus this unit's block in the wave record.
7. **E13 mail** — swept read-only at this seat's clock, four paths, classification from each row's
   **status cell**: (1) `docs/tranches/V/` + `V/coordination/` — newest non-self
   `value-inbox-2026-09-17-o8-o11-amendment-addendum.md`, **ours** (I-26). (2) glass-ui **BK**
   (re-confirmed the newest glass tranche dir by mtime) — newest is
   `valuejs-outbound-2026-09-18-kfw6-bh-relay.md`, **our own outbound**, filed by Track B's KF.W6
   `.b`; the newest *inbound* rows are still **I-32 · I-33 · I-34**, rowed by Track D and routed to
   the X formation mail seat / X-W0.j / X-EXT-1..6. (3) keyframes.js `V/coordination/` — every
   `VALUEJS-INBOUND-*` is ours. (4) atlas `P/coordination/` — newest 2026-08-03, rowed.
   **0 unrowed · 0 new `I-n` minted · 0 UNREAD in this unit's scope**; **not one row names
   `.github/workflows/**`**. ⟨`npm view @mkbabb/glass-ui version`⟩ re-measured here → **9.0.0**
   (§8.5's routing, unchanged).
8. `scripts/dev/dev.sh` untouched and unstaged throughout; every commit carried its own pathspec on
   the commit itself; no sibling seat's path entered any index of mine.
