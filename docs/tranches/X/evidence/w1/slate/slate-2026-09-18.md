SERVED MODEL: claude-opus-5[1m]

# G-5 — the oracle slate, run of record

⟨cmd⟩ `node scripts/ci/oracle-slate.mjs` at `ec654158`, 2026-09-18T05:35:36Z →

```
ORACLE SLATE — X-W1 G-5 (R2 · R47 · R48 · R50)
  projects declared : 8 (smoke, smoke-admin, smoke-mobile, smoke-reactivity, smoke-perf, oracles-safari, smoke-safari, visual)
  projects invoked  : smoke, smoke-admin, smoke-mobile, smoke-reactivity, smoke-perf, smoke-safari, oracles-safari, visual
  tracked e2e specs : 80
  collected by ≥1   : 80
  D · ordinal "o10d" is one oracle over 2 files, each declaring it: e2e/smoke/admin/o10d-admin-title-voice.spec.ts · e2e/smoke/oracles/o10d-display-voice-census.spec.ts
  E · a STATIC census. A locator whose literal EXISTS in product source but is unreachable at runtime (an `aria-label` dropped by a producer component's `inheritAttrs: false`) is not decidable here — that class is caught by G-3's classified run.
  E · 361 literal references checked · 63 dynamic (template/regex/variable) references are UNCHECKABLE by this instrument and are reported, not exempted
  F · 17 eslint `files:` globs read; only path-claiming globs are judged

ROUTED — 6 measured, NOT this wave's to cure
  · [F · DEAD LINT GLOB] eslint.config.js globs "demo/@/components/**/*.ts", whose directory "demo/@/components" does not exist
      owner: X-W8 (eslint.config.js is its modify-carve — fold R48 BOUNDARY LOCK)
  · [F · DEAD LINT GLOB] eslint.config.js globs "demo/@/components/**/*.vue", whose directory "demo/@/components" does not exist
      owner: X-W8 (eslint.config.js is its modify-carve — fold R48 BOUNDARY LOCK)
  · [F · DEAD LINT GLOB] eslint.config.js globs "demo/@/lib/**/*.ts", whose directory "demo/@/lib" does not exist
      owner: X-W8 (eslint.config.js is its modify-carve — fold R48 BOUNDARY LOCK)
  · [F · DEAD LINT GLOB] eslint.config.js globs "demo/@/lib/**/*.vue", whose directory "demo/@/lib" does not exist
      owner: X-W8 (eslint.config.js is its modify-carve — fold R48 BOUNDARY LOCK)
  · [F · DEAD LINT GLOB] eslint.config.js globs "demo/@/composables/**/*.ts", whose directory "demo/@/composables" does not exist
      owner: X-W8 (eslint.config.js is its modify-carve — fold R48 BOUNDARY LOCK)
  · [F · DEAD LINT GLOB] eslint.config.js globs "demo/@/composables/**/*.vue", whose directory "demo/@/composables" does not exist
      owner: X-W8 (eslint.config.js is its modify-carve — fold R48 BOUNDARY LOCK)

SLATE CLEAN — 0 findings (6 routed, named above)
```

⟨cmd⟩ `node scripts/ci/oracle-slate.mjs >/dev/null; echo $?` → `0` (clean)
