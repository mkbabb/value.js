SERVED MODEL: claude-opus-5[1m]

# G-20 + R38 — the PRE-deploy production probe, and the source epoch it belongs to

**Taken BEFORE any act of this unit that could ship** — before the `tranche-u` push, before the
fast-forward, before the merge. G-20 is a before/after gate; a probe taken after a deploy cannot
establish the "before". R38 (`X-W1-FOLD.md:668-678`, cl.3, MEASURE-AT-OPEN) sharpens it: *"record
which source epoch the PRE-deploy artifact belonged to, because a large body of the corpus's A/B
reasoning is conditioned on it."*

## The live fetch

⟨cmd⟩ `curl -sS -D /tmp/prehdr.txt -o /tmp/pre.html https://color.babb.dev/ -w 'HTTP %{http_code} · %{size_download} B · %{time_total}s\n'`
→ `HTTP 200 · 12368 B · 0.311786s`, at **2026-09-18T18:34:18Z** (`date -u`), server `cloudflare`,
`cf-ray: a3d271a94901cb2e-EWR`, `cf-cache-status: DYNAMIC`.

⟨cmd⟩ `shasum -a 256 /tmp/pre.html` →
`abd54ee30799543ce2b5b7279be65a5c19ef468a4d30fc45b355a27f5ecd0092`

⟨cmd⟩ `grep -oE 'assets/[^"]+\.(js|css)' /tmp/pre.html | sort -u` →

```
assets/createLucideIcon-KrxAXTF6.js
assets/glass-fonts-DH5GtBvs.css
assets/glass-ui-CShs8agU.js
assets/index-D9U9KwTn.js
assets/index-DJ8Ije_5.css
assets/prng-19McOvQD-Bldl5jAI.js
assets/rolldown-runtime-QTnfLwEv.js
assets/useDocumentVisibility-B-_HlNK_-Cf-Y8fd2.js
```

**The entry asset is `assets/index-D9U9KwTn.js`** — byte-identical in name to W1.md's B17, measured
2026-08-03. **46 days later production has not moved.** G-20's AFTER reading must show a different
entry hash; an unchanged hash after a green deploy means the deploy did not deploy.

## R38 — which source epoch is that artifact? (the part the corpus needs)

The artifact was cut by the last successful `deploy-pages` run. ⟨cmd⟩
`gh run view 28836880612 --json databaseId,conclusion,event,headBranch,headSha,createdAt` →

```
28836880612 success event=workflow_dispatch branch=master sha=80c5888511441e668990b0279db5e398867d3a20 2026-07-07T02:19:20Z
```

⟨cmd⟩ `git log -1 --format='%h %ad %s' --date=iso-strict 80c58885` →

```
80c58885 2026-07-06T22:19:05-04:00 ci(fix): deploy-pages shipped PREVIEWS, never production — --branch=main vs the project's master production branch
```

Epoch markers read at that commit, against the epoch that WOULD ship:

| marker | live production (`80c58885`) | `tranche-u` HEAD |
|---|---|---|
| `package.json` version | **3.1.0** | **4.0.0** |
| `demo/@` file count (⟨cmd⟩ `git ls-tree -r --name-only … demo/@ \| wc -l`) | **227 — ALIVE** | **0 — dead** |
| `@mkbabb/glass-ui` declared | **`file:../glass-ui`** — a LINKED SIBLING | **`^7.0.0`** — the registry |

**The finding, stated plainly.** Production is a **2026-07-06 build of value.js 3.1.0**, with the
`demo/@` tree alive, **built against a linked sibling glass-ui rather than the registry** — the very
provenance defect CC-034 / G-15 cured in `deploy-pages.yml` at this wave. R38's corollary is
therefore **CONFIRMED and sharpened**: every *"production is correct"* A/B anywhere in this corpus is
conditioned on 3.1.0 + `demo/@` + a linked producer. It is not a March build, as R38 allowed it might
be — it is a July build — but it predates both the `demo/@` death and Glass 7 exactly as R38 warned.

**R38's open disjunction is CLOSED by this fetch.** R38 could not decide *"either the tracking ref is
stale or r2's filename cell is wrong"*:

- **r2's filename cell is CORRECT.** `assets/index-DJ8Ije_5.css` is live at this fetch.
- **The local `origin/gh-pages` ref is STALE, and is not the deploy path at all.** ⟨cmd⟩
  `git log -1 --format='%h %ad' --date=iso-strict origin/gh-pages` → `17c10267 2026-03-24T00:45:58Z`,
  carrying `assets/index-DS-0HlXP.css` + `assets/index-Dq7-wwjC.js`. The site ships to **Cloudflare
  Pages via wrangler** (`deploy-pages.yml:1-9`), so that branch is a vestige no deploy has written
  since March; it was never going to agree with production and its disagreement means nothing.

## G-20 verdict at this unit's close

**RED, honestly.** The AFTER probe cannot differ from the BEFORE probe, because no deploy ran: the
`deploy-pages` push arm is gated on a green master `ci` and that gate did not open (see
`g17-g19-landing-2026-09-18.md`). G-20 is **not** claimed on the strength of the merge landing.
