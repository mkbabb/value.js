SERVED MODEL: claude-opus-5[1m]

# G-17 · G-18 · G-19 — the landing, the green that did not come, and the deploy that did not fire

## G-18 — `tranche-u` merged to `master`: **GREEN**

Executed in an **isolated worktree** on `master`, so the primary tree's sibling-seat dirt (ten
`demo/**` files, `scripts/dev/dev.sh`, three shared ledgers — every one outside this unit's writable
set) was never checked out, staged, or disturbed.

**Step 1 — the fast-forward, first, as §0j.F(2) requires.**
⟨cmd⟩ `git fetch origin master:master` → `6abef800..44ddaff7  master -> master` (140 commits,
**fast-forward, no `+`**). ⟨cmd⟩ `git rev-list --count origin/master..master` → `0`.
**Option D (rebase) was refused**, never attempted: it would re-hash every SHA-pinned authority in
§0h/§0i, the runbook and the ledger.

**Step 2 — `tranche-u` pushed before the merge**, so the landing carries nothing `origin` has not
seen: ⟨cmd⟩ `git push origin tranche-u` → `eb1b21eb..80fe6c75`; ⟨cmd⟩
`git rev-list --count origin/tranche-u..HEAD` → `0`.

**Step 3 — the merge.** ⟨cmd⟩ `git merge --no-commit --no-ff tranche-u` → **90 conflicts**
(10 `UU` · 4 `AA` · 76 `DU`; **zero `UD`/`DD`**). Every §0j.F(2) clause was then verified **at the
blob**, never presumed:

| §0j.F(2) clause | measurement | verdict |
|---|---|---|
| fast-forward local master first | `6abef800..44ddaff7`, no `+` | done first |
| every both-changed path → `tranche-u`'s bytes | 90 conflicts checked out from `tranche-u` | done |
| …**and the three both-changed paths git auto-merged CLEANLY** | `test/math.test.ts` · `…/rescued/deposed-full/src/math.ts` · `…/rescued/deposed-full/src/subpaths/CLAUDE.md` | **caught and resolved to `tranche-u`** |
| the `scripts/ci` probes `tranche-u` deleted stay deleted | `css-emission-probe.mjs` `a80e66ee` and `oracle-slate-teeth.mjs` `caa1f898` are **base == master**, deleted by `tranche-u` | stay deleted |
| X-W1's `boot-smoke.mjs` is the surviving add | base == master `197be1b2`; X.W1.c's `70a5e0af` survives | surviving add |
| except `release.yml`, reconciled by word (1) | base `e60230ca`; master **and** `tranche-u` both `4f9cd042` | nothing left to decide |
| word (2): `7334c793`'s `src/v4` relocation does not bind; `tranche-u` is the tree of record | class-B master-side adds absent from `tranche-u` = **`src/v4/**`, 13 files and nothing else** | dropped |

**The clean-auto-merge catch matters.** A conflict census alone would have missed all three. The
`test/math.test.ts` blend appended master's `lerpArray` block beside `tranche-u`'s, producing a
**duplicated `describe("lerpArray")`** — a merge artifact that would have redded `npm test` for a
cause that is neither the pack step nor a product defect, i.e. precisely the false G-17 signal
`W1.md:317` forbids. §0j.F(2) binds *both-changed* paths, not *conflicted* paths, and the difference
is load-bearing.

**The result, measured not asserted.** ⟨cmd⟩ `git diff --name-status tranche-u` → **0 paths**. The
merged tree **is** `tranche-u`'s tree — the literal content of *"`tranche-u` is the tree of record"*.
Independently confirmed by tree hash:

```
master^{tree}   = a39ed2812770010c5d4836d5b4e10c9bb131c2e2
80fe6c75^{tree} = a39ed2812770010c5d4836d5b4e10c9bb131c2e2   IDENTICAL
```

**Merge commit `04d2d808`**, parents `44ddaff7` (master) ⊕ `80fe6c75` (`tranche-u`).
⟨cmd⟩ `git push origin master` → `44ddaff7..04d2d808`, 2026-09-18T18:41:52Z.

**G-18's falsifier — *"a merge that drops commits: `git rev-list --count master..HEAD` must read
`0` after"*** — ⟨cmd⟩ at the merge point → **`0`**.

> **Read this correctly, later.** The same count now reads **6**, and that is not a regression: three
> sibling tracks have committed to the shared `tranche-u` since the merge. The durable proof is
> ⟨cmd⟩ `git merge-base --is-ancestor 80fe6c75 master` → **YES**. Nothing was dropped.

**795 commits, not 234.** W1.md authored 234 (B16); the open sitting measured 559; the resume
measured 790; this seat measured **795** at `80fe6c75`. Four tracks share the branch.

## G-17 — `ci` green on `master`: **RED**, and not for a cause this unit may cure

Master run **`35381701436`** — `04d2d808`, `event: push`, started 2026-09-18T18:42:04Z.

### The structural half PASSES

Parsed from `master`'s own `ci.yml` (parsed, not grepped):

- `pack producer bytes` step **PRESENT at `:59`**, carrying the fix — the tarball name is derived
  deterministically from `package.json` instead of parsing `npm pack --json`, whose stdout the
  prepare script's vite banner polluted on Node 22. That is the cause of all five recorded master
  failures (B14).
- `verify packed runtime and strict declarations` **PRESENT at `:71`**, invoking
  `scripts/ci/verify-packed-surface.mjs`.
- **LIVE `continue-on-error` keys: 0.** Two occurrences exist at `:26` and `:218`; both are
  **comments**. Nothing is softened.

**So G-17's falsifier — *"a green obtained by removing or softening the `pack producer bytes` step
fails G-17"* — did not fire, because no green was obtained and nothing was softened.**

### The one cause G-17 permits is GREEN when run

The permitted cause was never even exercised: in `producer / Node 22` **and** `/ Node 24`, step 5
`npm run lint` **fails**, and steps 6–10 — `npm run build`, `npm run typecheck`, `npm test`,
**`pack producer bytes`**, `verify packed runtime and strict declarations` — are all **`skipped`**.

Run directly at this seat instead (see `ng13-packed-surface-2026-09-18.md`):
⟨cmd⟩ `node scripts/ci/verify-packed-surface.mjs <tarball>` → exit **0**,
`{"runtime":{"color":23,"value":1,"css":19,"easing":16,"math":9,"transform":9,"quantize":2},"strictTypes":62}`.

**The pack step is sound. The master red is entirely elsewhere.**

### The actual roots, measured at master's bytes

⟨cmd⟩ `npx eslint . --max-warnings=0` on the merged tree → **`55 problems (23 errors, 32 warnings)`**.
Every one of the 23 errors is `Parsing error: 'return' outside of function`, and every erroring file
is under `docs/`:

```
  files with errors: 23
     18  docs/tranches/V
      5  docs/tranches/X
```

The 18 under `docs/tranches/V` are the megatranche workflow fragments; the **5 under
`docs/tranches/X` are the execution chassis** (`x-begin.js`, `x-track-A..D.js`) committed at
`69987a73` — **a contributor that post-dates X.W1.a's own measurement of 30**. `eslint .` globs them
because `eslint.config.js` does not ignore `docs/**`, and **`eslint.config.js` is X-W8's modify-carve
(fold R48 BOUNDARY LOCK)**. Adding the ignore here would be both a bounds violation and the
allowlist-shaped masking the standing LAW names a HIGH defect. `--max-warnings=0` means the 32
warnings would red it even with the errors gone.

The other failing jobs and their owners:

| job | root | owner |
|---|---|---|
| `producer` ×2 | `npm run lint` — 23 `docs/**` parse errors | **X-W8** (`eslint.config.js` carve) |
| `oracle slate` | **3 DEAD ATTRIBUTE findings** — `e2e/smoke/crash-battery.spec.ts` binds `[data-palette-card]`, `[data-extract-swatch]`, `[data-palette-swatch]`, none of which appears in any product or producer byte | see the finding below |
| `lhci` | the four budgets, `lighthouserc.json` unmodified | honest budget reds (G-4) |
| `deploy-age` | G-21's condition — production 73 days stale | this unit's G-21, working |
| `e2e-smoke` · `e2e-safari` · `visual` · `boot-smoke` | product reds routed at G-3's classified baseline | X-W2 / X-W4 / X-W5 / X-W6 / X-W7 / X-W9 |

### A finding against X.W1.a's own G-5 receipt, recorded rather than absorbed

The slate is RED on master while `slate-2026-09-18.md` — a's adopted G-5 receipt — records
**`SLATE CLEAN — 0 findings`** with **80** tracked e2e specs. Reproduced locally on the merged tree:
**`SLATE RED — 3 finding(s)`** with **81** tracked specs.

Provenance, measured: ⟨cmd⟩ `git log --diff-filter=A -- e2e/smoke/crash-battery.spec.ts` →
**`a0df89d9`**, *"the born-RED crash battery…"* — **X.W1.a's own last commit**, landed **after**
`ec654158`, the commit at which a ran its slate. a's seat was killed before it could re-run.

**This is not a merge artifact and not a sibling seat's doing**: master's tree hash is identical to
`tranche-u@80fe6c75`'s, and the concurrent `tranche-u` run `35381213268` reds the slate the same way
on the same bytes. It is **stale evidence inside a retired seat's receipt** — G-5/NG-1 measured one
commit before the tree that shipped. NG-8's born-RED crash battery binds three attributes that do not
yet exist, which is what a born-RED battery is *for*; the slate, correctly, calls them dead. **The
two gates disagree by construction and that collision is X.W1.a's to resolve, not this unit's.**
Returned with the escalation below; **G-5 is not re-claimed green by inheritance.**

## G-19 — ONE `deploy-pages` success on the push arm: **RED**

`deploy-pages.yml:49-53` gates the deploy on a triple: `workflow_run.conclusion == 'success'` **and**
`head_branch == 'master'` **and** `workflow_run.event == 'push'`. Master's `ci` did not conclude
`success`, so the first conjunct is false and the job does not run.

**A drifted anchor, recorded at the true bytes (METHOD).** G-19 is authored as *"ONE `deploy-pages`
run with `conclusion: success`, `event: push`, `head_branch: master`"*. **No `deploy-pages` run can
ever carry `event: push`**: the workflow's only triggers are `workflow_run` and `workflow_dispatch`
(`:27-32`), so `gh`'s `event` field reads `workflow_run` on every gated run — measured across the
last 65. The satisfiable INTENT, which G-19's own falsifier makes unambiguous (*"a
`workflow_dispatch` success does not satisfy G-19 — the `if` gate's push arm is what has never
fired"*), is:

> a `deploy-pages` run with `event: workflow_run`, `head_branch: master`, `conclusion: success`,
> **whose triggering `ci` run had `event: push` on `master`**.

Recorded so a later seat does not read G-19 as unsatisfiable-by-construction and either waive it or
reach for a dispatch. **No `workflow_dispatch` was fired at this seat** — doing so would have cut a
Production deployment (wrangler is hard-coded `--branch=master`), destroyed G-20's before/after
comparison, and satisfied G-19's letter while evading exactly the arm it exists to test.

## G-20 — `color.babb.dev` serves v4: **RED**

No deploy ran, so the served bytes cannot have moved. Full PRE-deploy probe and the R38 source-epoch
settlement are in `g20-r38-prod-epoch-2026-09-18.md`: production is a **2026-07-06 build of value.js
3.1.0**, entry `assets/index-D9U9KwTn.js`, unchanged from B17's 2026-08-03 reading.

## Escalation — ESC-W1E-1

`W1.md` §Triumvirate Dispatch, trigger (i): *"master CI still red after the merge for a cause outside
`.github/workflows/` and the pack step."* **That condition is met exactly**, and the causes are
measured, named and owned above. **G-17, G-19 and G-20 are unreachable by any act inside this unit's
writable set**, and the three ways to reach them are all forbidden here:

1. ignore `docs/**` in `eslint.config.js` — **X-W8's** modify-carve, and allowlist-shaped;
2. soften any job — **G-2/G-17 fail by construction**, and undoing the D48/D55(iv) waivers is the
   reason this wave exists;
3. `workflow_dispatch` the deploy — **G-19's falsifier forbids it** and it would destroy G-20.

**Returned with options, not cured by softening a gate** — the disposition X.W1.a's wave-log entry
already anticipated and handed forward.
