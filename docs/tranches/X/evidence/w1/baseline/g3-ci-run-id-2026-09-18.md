SERVED MODEL: claude-opus-5[1m]

# G-3's second witness — the CI run id, arrived

**Dated addendum-beside (E-3), 2026-09-18.** `g3-full-suite-2026-09-18.md` is committed evidence
and is NOT edited: not one byte of it moves. This file records the reading that discharges the one
clause that file itself named as outstanding, and it is written by the X-W1 REPAIR SEAT (round 1)
against **Check 1 · D-7**.

## The clause

G-3's falsifier (`W1.md:303`): *"A baseline copied from any prior document instead of a run: **the
committed number must carry its run ID and duration**."*

The baseline carried its duration (`1137.5 s`) and said so in its own words about the other half:

> **run id** — *LOCAL — no CI run id exists yet.* Before this unit `.github/workflows/` contained
> the string `playwright` zero times, so there is no CI run to cite. The CI run id is this
> baseline's SECOND witness and arrives with the G-7 falsifier and X.W1.e's master run.

It has arrived.

## The CI run of record

| field | value |
|---|---|
| workflow | `ci` |
| **run id** | **`35381701436`** — <https://github.com/mkbabb/value.js/actions/runs/35381701436> |
| **job** | **`e2e-smoke`**, id **`105719151511`** |
| event / branch / sha | `push` · `master` · `04d2d8089fb21c2bd8568eabd6f8c1b2cf8154e8` |
| started / completed | `2026-09-18T18:42:11Z` → `2026-09-18T19:38:21Z` |
| **conclusion** | **`failure`** (`##[error]Process completed with exit code 1`) |
| **duration** | **`55.0m`** inside Playwright (`160 passed (55.0m)`) |
| command | `npx playwright test --project=smoke --project=smoke-admin --project=smoke-mobile --project=smoke-reactivity --project=smoke-perf` |
| host | `ubuntu-latest` · node 24 · `workers: 1` · `retries: 2` (CI arm of `playwright.config.ts`) |

## The split, read from the job log

```
Running 202 tests using 1 worker
  37 failed
  3 flaky
  2 skipped
  160 passed (55.0m)
```

## What this is, and what it is NOT

It is a **second witness carrying a run id**, not a restatement of the baseline's number, and the
two are deliberately not conflated:

| | the baseline | this reading |
|---|---|---|
| commit | `58d6f731` (before this unit's cures) | `04d2d808` (after the merge) |
| scope | **all six** projects, `npx playwright test` with no `--project` | the **five chromium** projects `e2e-smoke` invokes; `smoke-safari` and `oracles-safari` ride `e2e-safari`, `visual` rides its own job |
| retries | `0` (local) | `2` (the CI arm), which is why `flaky` is non-zero here and zero there |
| collected | 187 in 72 files | 202 |
| run id | none existed | **`35381701436`** |

The collected count rose because the tree grew specs after the baseline was taken —
`crash-battery.spec.ts` alone landed at `a0df89d9`, X.W1.a's own last commit, *after* `58d6f731`.
The baseline is not re-opened and its classification of 40 unexpected cells is not re-litigated
here; per E-3 it stands at its bytes.

## The one thing this reading proves that no local run could

The job **executed 202 browser tests on a pushed commit and carried a non-zero exit code all the
way to the run's conclusion**. `e2e-smoke` is therefore not decorative in the mechanical sense —
it runs, it reports, and its failure is the run's failure, with no `continue-on-error` between them
(G-2). What it does **not** prove is G-7: that a *deliberately introduced* break turns the job red.
That demonstration is its own gate and lives under `evidence/w1/falsifier/`.

## Provenance

- Raised as **Check 1 · D-7** (MINOR) — *"G-3 is published GREEN while its falsifier's run-ID clause
  is unmet"*, with R-CL-10 naming the owner.
- Discharged by reading, not by asserting: every field above comes from `gh run view 35381701436`
  and the job's own log.
