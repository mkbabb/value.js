SERVED MODEL: claude-opus-5[1m]

# G-7 · THE FALSIFIER DEMONSTRATION — PERFORMED

**Date: 2026-09-18.** X-W1 REPAIR SEAT (round 1), against Check 1 defect **D-1**.

`W1.md:69` calls this the ***"mandatory one-time falsifier"***; `:307` says ***"This gate IS the
falsifier"***, failing *"by the scratch run going green — which would prove the jobs are
decorative"*. §Archaeology names why it is a gate at all: V-prime *"delivered two with BOTH
falsifiers waived — D48 `continue-on-error`, D55(iv) branch-push substitution"*.

**Neither waiver is used here.** The demonstration is a real push to a real branch, running the
workflow of record, with the CI run's own conclusion as the verdict.

## The differential

One variable is changed and nothing else. Same workflow, same job, same runner class, same 202
tests, two runs seven minutes of history apart.

| | **control** | **falsifier** |
|---|---|---|
| run | [`35381701436`](https://github.com/mkbabb/value.js/actions/runs/35381701436) | [**`35388604746`**](https://github.com/mkbabb/value.js/actions/runs/35388604746) |
| job | `e2e-smoke` `105719151511` | `e2e-smoke` **`105741411648`** |
| branch / sha | `master` / `04d2d808` | **`x-w1-g7-falsifier`** / **`3d029e44`** |
| the spec's bytes | untouched (⟨`git diff origin/master HEAD -- e2e/smoke/views/browse-loading.spec.ts`⟩ → **prints nothing**; last touched at `52acad46`, long before this wave) | **one assertion inverted** |
| collected | `Running 202 tests using 1 worker` | `Running 202 tests using 1 worker` |
| **failed** | **37** | **38** |
| passed | 160 | 161 |
| flaky / skipped | 3 / 2 | 1 / 2 |
| `browse-loading.spec.ts` | **not in the failed list — it passed** | **failed, three times (initial + `retry1` + `retry2`)** |
| **job conclusion** | `failure` | **`failure`** |
| duration | 55.0m | 54.8m |

**Exactly one more failure, and it is the one that was planted.**

## The break

Scratch commit **`3d029e44`**, base `37f12bbf` (`tranche-u`), one file, one assertion — the full
diff is committed beside this file as `broken-spec-3d029e44.diff`:

```diff
-    await expect(skeletons).toHaveCount(0);
+    // X-W1 · G-7 DELIBERATE BREAK — scratch branch `x-w1-g7-falsifier` ONLY,
+    // never merged. The true assertion is `toHaveCount(0)`: once the wall has
+    // developed into PAGE1_COUNT real cards, zero skeletons remain. Asserting
+    // `1` inverts a proposition the product satisfies, so this spec must fail
+    // in CI. If the `e2e-smoke` job concludes anything but `failure` on this
+    // branch, the job is decorative and G-7 is REFUTED.
+    await expect(skeletons).toHaveCount(1);
```

The inverted proposition is one the product genuinely satisfies: after the browse wall develops
into `PAGE1_COUNT` real cards, zero skeletons remain. Asserting `1` cannot pass unless the product
is broken.

## What CI reported, quoted from the job log

```
  30) [smoke] › e2e/smoke/views/browse-loading.spec.ts:16:1 › browse mid-fetch renders
      developing-plate skeletons, never a spinner

    Error: expect(locator).toHaveCount(expected) failed

    Locator:  getByRole('main', { name: 'Color tool panes' })
                .locator('[data-slot="palette-card-skeleton"]').filter({ visible: true })
    Expected: 1
    Received: 0
    Timeout:  8000ms

    > 48 |     await expect(skeletons).toHaveCount(1);
         |                             ^
        at /home/runner/work/value.js/value.js/e2e/smoke/views/browse-loading.spec.ts:48:29
```

```
  38 failed
  1 flaky
  2 skipped
  161 passed (54.8m)
```

Job conclusion: **`failure`**, completed `2026-09-18T20:51:29Z`.

## Why this satisfies G-7 and not merely G-2

- **No `continue-on-error`** (D48's waiver): the job's failure is the run's failure. ⟨`grep -n continue-on-error .github/workflows/ci.yml`⟩ → 2 hits, **both comment prose**.
- **No branch-push substitution** (D55(iv)'s waiver): the workflow triggers on `push: branches: "**"`,
  so the scratch branch ran **the same `ci.yml` as master**, with no substituted job, no reduced
  matrix and no dispatch.
- **No local run standing in for the CI run.** Every figure above is read from GitHub's own API and
  the job's own log.
- **The control is real.** The same spec, byte-identical, **passed** on the immediately preceding
  master run. A job that reports failures it would have reported anyway proves nothing; this one
  reports one additional failure, exactly where the break was planted, and reproduces it across two
  automatic retries.

**G-7: RED → GREEN.** And with it, `W3.md:15`'s Opens-after conjunct — *"X-W1 (re-gate: CI +
falsifier demonstration must exist before born-RED gates can be trusted to flip)"* — **now measures
TRUE**: the demonstration exists, at a URL, with a control.

## Disposal

The scratch branch is **evidence, not a proposal**. It is never merged; `3d029e44` exists solely to
have been measured. Nothing on it enters `tranche-u` — this repair's own commits are on `tranche-u`
and contain no part of the break (⟨`git log --oneline tranche-u | grep -c 3d029e44`⟩ → **0**).

## Provenance

- Gate: **G-7** (`W1.md` §Hard Gate row 7, CC-031) · unit **X.W1.a**'s falsifier limb.
- Raised as **Check 1 · D-1** (HIGH) — *"never demonstrated, and NO clause of W1.md or the fold
  relieves it … It is also the byte that blocks X-W3."*
