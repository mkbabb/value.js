# T.W8 — ROWS.md (the remediation ledger — the record the OWNER's verdict reads)

**Assembled**: 2026-07-12 by the **non-authoring terminal-assembler agent** (authored NO
critique pass and NO remediation row this wave — the §Recovery non-authoring law). At the
merged wave head `6d95871` (`tranche-t`).
**What this is**: every filed row from all **11 critique passes** + the **11 owner-filed §0.7
WR rows** + the **7 checkpoint BOOK-W8 brackets** — each **landed with its commit, booked BY
NAME to the package/W7/W9, or routed BY NAME to the letter/triumvirate** (`T.W8.md §Hard gate`
row 1: *zero silent drops*). This ledger CERTIFIES NOTHING — it PRESENTS the remediation state
for the owner's eye; the owner's verdict is the gate (§Hard gate row 6).
**Pass journals** (read, not re-critiqued): `passes/<surface>.p1.md` (11) · the checkpoint
record `w45-checkpoint/ROWS.md` · the WR roster `waves/T.W8.md §Scope-rider`.

---

## §0 The merge record (the FIRST assembler act — committed-green only, --no-ff)

Six remediation lanes were cut from the wave head (`6f7cb35` / `823756d`), landed committed-green
in their worktrees, and merged onto `tranche-t` **--no-ff** at assembly. Each lane touches a
**disjoint file set** (no cross-lane file overlap); none touches a forbidden path (`src/`,
`../glass-ui`, `../keyframes.js`, `demo/@/components/ui/`, `docs/precepts/`). All 6 branches are
now **0-unique** (fully merged).

| Lane | Branch | Lane commits (landed) | Merge commit |
|---|---|---|---|
| 14 · picker-console | `worktree-wf_00142843-e53-14` | `e2e8517` `a8dc32a` `5f273ae` `bdd0a9c` | `83d8714` |
| 15 · about-body | `worktree-wf_00142843-e53-15` | `f0ef927` | `41ba7c9` |
| 16 · chrome-frame-atmosphere | `worktree-wf_00142843-e53-16` | `31e7c09` `cca4852` `5cecdf2` `2c1a0f4` | `1e74cf3` |
| 17 · ramp-nav-generate-admin | `worktree-wf_00142843-e53-17` | `ccf4b30` | `18f8263` |
| 18 · extract-skeleton-empty | `worktree-wf_00142843-e53-18` | `1705345` | `f6c595f` |
| 19 · gradient-easing | `worktree-wf_00142843-e53-19` | `31d8ca3` | `6d95871` |

**Post-merge suite verification** (at `6d95871`): `npm run lint` **0** · `npm run typecheck`
**0** · `npm test` **74 files / 2241 tests PASS** · playwright smoke — the first merged-head run
surfaced **2 O-10 title-lock legs RED** (a cross-lane gap: WR-4/T-51's owner-mandated title
step-down `5f273ae` landed `--type-display-2` but left the O-10 oracle asserting the superseded
`--type-display-3` — the "O-10 line-locks re-run" WR-4's own rides column owed). The oracle
REFERENT was re-aimed to the ruled token (`b551755`, e2e-scoped, separate from this docs commit —
the tighten-not-weaken idiom, NOT a gate weakening); **the full smoke slate is GREEN after the
re-aim** (o10 6/6). See `PACKAGE.md §5` for the full ledger.

---

## §1 The master row ledger (every pass's rows folded; landed-commit / booked-to / routed-to)

**Grade key**: LAND = landed in a remediation lane (commit named) · BRACKET = package row, owner
rules (no lane may consume) · PRODUCER = letter/inbox row · WITNESS = re-witnessed standing book,
no new demo row · TRIUMVIRATE = oracle-bounds note routed to the triumvirate record.

### Pass 1 — PICKER (`passes/picker.p1.md`, filed `1142aab`)

| Row | Type | Finding (one line) | Outcome — commit / booked-to / routed-to |
|---|---|---|---|
| **P1-R1** | LAND (A-class) | The active rail seat is dead-by-cascade — producer `.watercolor-swatch` (0-2-0) defeats the flat `.rail-dot` (0-1-0), dot renders 11.6×0px, active glyph inks near-white on cream | **LANDED `e2e8517`** (lane 14) — demo-owned `.rail-dot-seat` (absolute inset:-1px), no producer property overridden, T-28 ABROGATE intact; dot 25.6×26.4 live fill, active glyph white-on-red |
| **P1-R2** | LAND (legibility) | The α rail glyph is a Latin-`a` twin (Fraunces carries no Greek); rail reads `L a b a` | **LANDED `e2e8517`** (lane 14) — LANDED default = α inks in Fira Code's open Greek alpha, upright; the C-4 house-cut-face far pole → package bracket |
| **P1-R3** | PRODUCER | UA blue `outline:auto` double-paints over the house focus ring on the spectrum thumb (glass-ui `Slider.vue:470`) | **ROUTED → the GLASSUI-T-ASKS letter** (P10/P5-adjacent); no demo fork (D7 arms-race fence) |
| **P1-B1** | BRACKET | T-33b seam-air (bottom-anchored tuple ← target → per-value-class line count) | **PACKAGE → owner** — *COLLAPSED per WR-4/§0.7*: the owner redded reservation-as-blank in BOTH placements; both honest §6.1 arms (per-value-class · seam-absorb) + the measured razor-boundary evidence route to the owner (mechanism handed, not landed) |
| **P1-B2** | BRACKET | The C-2 header 4th-alpha cell (3-cell no-alpha ← target → 4th cell re-deriving the O-10b locks) | **PACKAGE → owner** (= checkpoint C-2; BY CONSTRUCTION — a light remediation is forbidden) |
| **P1-B3** | BRACKET | The blob-seat positive domain (corner-flush 0 ← centre-ward → over-inset) | **PACKAGE → owner** (= checkpoint R7/C-3 = pass-2 Row E; ONE ruling) |
| **P1-B4** | BRACKET | T-42 the shrunken scrolled header (compositor-only scale over unshrunken box) | **PACKAGE → owner** — T-61 (`§0.8`) RULES the direction (whole-header contraction); the remaining bracket is the REGISTER of the contraction |

### Pass 2 — BOOT / ATMOSPHERE / BLOB (`passes/boot-atmosphere-blob.p1.md`, `5d5f015`)

| Row | Type | Finding | Outcome |
|---|---|---|---|
| **A** | LAND (A-class) | Config slider TRACK material invisible-class on the well (≈1.09:1 L / 1.26:1 D) — the named O-18 blind spot on the SECOND slider population | **LANDED `bdd0a9c`** (lane 14) — config track re-ink via `--ink-muted` (≥3:1); o18-contrast-census gains a born-RED config GRAPHICS leg, GREEN post-cure (6.07 L / 8.45 D) |
| **B** | LAND (forensic) | The bead re-enters GRAY on the KeepAlive Home-return swap (§0.3 "too gray" family, mid-flight) | **LANDED `5cecdf2`** (lane 16) — `onActivated` re-seed of the ramp before the wake repaint; built-bundle return-swap min chroma 0.143 ≈ baseline, ZERO gray frames. Residual engine-default-frame root stays the P6 producer rider (PR-2 fence) |
| **C** | BRACKET | Q2 boot-settle past the ratified upper (1732–2059ms vs [1.4,1.7]s; crisp-but-late) | **PACKAGE → owner** (= checkpoint R8; payload lever = W7's BY NAME) |
| **D** | BRACKET | R3 neutral-seed whisper floor (silence C≈0.004 ← sage whisper → marigold lie) | **PACKAGE → owner** (= checkpoint R3) |
| **E** | BRACKET | R7+C-3 blob-seat positive domain (crisp bead re-taken) | **PACKAGE → owner** (= P1-B3; ONE ruling) |
| **F** | BRACKET | T-48 mid-flight freeze signature (356ms hole; frozen-mid-pose ← hole-free → over-deferred) | **PACKAGE → owner**; the payload half stays **W7 BY NAME** (WR-10/T-58 MANDATE); W9 re-verifies |
| **G** | TRIUMVIRATE | An o12 boot-time backing-ratio leg would make the R2 regression class visible to the slate | **ROUTED → the triumvirate record** (bounds expansion; W9 inherits the minted leg or the declined-note) |
| — | PRODUCER re-cites | GAP-L2 dark `lBand` · T-32 `MAX_NUCLEI=6` · T-38 aurora-pointer | **STANDING books** (letters out at `f3f3c097`); verify-at-cut is W7's — no demo action |

### Pass 3 — ABOUT (`passes/about.p1.md`, `e73a523`)

| Row | Type | Finding | Outcome |
|---|---|---|---|
| **AB-1** | LAND (A-class render-truth) | KaTeX CSS scope-dead — `@import` inside `<style scoped>`, every formula renders DOUBLED + unstyled + a 6400px brace SVG clipping the card | **LANDED `f0ef927`** (lane 15) — global script-side import in `Katex.vue`; `.katex-mathml` hidden (one layer), `.mord`=KaTeX_Main, brace 6400→22px; sibling `Markdown.vue` `:has(> .katex-display)` re-key so the wide L-a-b row scrolls honestly |
| **AB-2** | LAND (D6) | The About header caption speaks raw `--muted-foreground`, 4.29:1 light on its TRUE veil ground | **LANDED `f0ef927`** (lane 15) — → certified `--ink-muted` (7.01 L / 6.30 D); one class-list edit, all 9 panes inherit (**also discharges admin A-2**) |
| **AB-3** | LAND (D1) | Content chips paint the parallel `--muted` species, off the rung-2 well (opposite dark step) | **LANDED `f0ef927`** (lane 15) — all chips re-seated onto the ONE `bg-well` recipe; alpha sub-species collapse |
| **AB-4** | LAND (token floor) | Light `.hljs-number` 4.46:1 on the code chip | **LANDED `f0ef927`** (lane 15) — all hljs tokens re-censused on the landed (darker) well ground; worst 4.91 L / 4.88 D, both schemes ≥4.5 |
| **AB-5** | BRACKET | Content-heading weight voice (calm-uniform 400 ← target → shipped sectioning 600) | **PACKAGE → owner** (indexed to T-2) |

### Pass 4 — PALETTES / BROWSE (`passes/palettes-browse.p1.md`, `d1e0e43`)

| Row | Type | Finding | Outcome |
|---|---|---|---|
| **P4-R1** | LAND (A-class mechanism) | The Q5 ramp's certified-contrast walk is scheme-blind — 1.83:1 dark at both sanctioned sites | **LANDED `ccf4b30`** (lane 17) — card-SURFACE referent + feasibility-aware cusp walk + per-site floor split (title 3:1 / menu 4.5:1); one resolver, per-site certified outputs; O-14 gains a born-RED feasibility leg. **= P9-R1 = WR-8/T-56** (ONE lane) |
| **P4-R2** | LAND (A-class population) | EmptyState captions never thread the certified rungs (raw `text-muted-foreground` + guard-then-alpha; 3.84:1 light) | **LANDED `1705345`** (lane 18) — eyebrow/hint → `--ink-muted` (4.99–5.07); display line drops `/85` → full `--foreground`; the ONE shared atom, 8 consumers inherit |
| **P4-R3** | LAND (small · a11y) | The populated heading's accessible name concatenates the count Badge: "My Palettes2" | **LANDED `ccf4b30`** (lane 17) — badge `aria-hidden` + sr-only "(N saved)" |
| **P4-R4** | LAND (small · constraint) | The view-dropdown's CURRENT entry inks `font-semibold` (600) | **LANDED `ccf4b30`** (lane 17) — marker retired (ColorSpaceSelector/T-40a precedent); selection speaks reka `aria-selected` |
| **P4-B1** | BRACKET | The T-56 ramp REGISTER (landed analogous fan ← pleasing pastel → over-sweet) | **PACKAGE → owner** (§0.7-named; P4-R1 is its PRECONDITION) |

### Pass 5 — EXTRACT (`passes/extract.p1.md`, `b878417`)

| Row | Type | Finding | Outcome |
|---|---|---|---|
| **E1-R1** | LAND (A-class) | Extract label/caption ink fails the text floor on the plate (2.82–3.08:1 light, all viewports) | **LANDED `1705345`** (lane 18) — labels/captions → `--ink-muted` (4.5–7.98); k label sheds `font-bold` (ONE mono voice); drop-zone prompt 4.06–4.18 flagged as the marginal residual |
| **E1-R2** | LAND (A-class) | The dark-scheme ghost collapses to a featureless slab (1.02:1) — the instrument face's own contract fails | **LANDED `1705345`** (lane 18) — `--skeleton-ink` re-derived as a certified 15%-fg tone-step of `--well-bg`; blocks read in both schemes (1.21–1.26); all 4 skeleton consumers inherit |
| **E1-R3** | LAND (small) | The developed K rail's certified layer is fully occluded by the data gradient; the "by construction" comment is false in that state | **LANDED `1705345`** (lane 18) — a persistent inset hairline ring survives outward beneath the gradient; comment corrected to the honest scope |
| **E1-B1** | BRACKET | The track-voice presence axis (certified-neutral quiet ← target → certified live-accent loud) | **PACKAGE → owner** (indexed to T-44a) |

### Pass 6 — MIX + GENERATE (`passes/mix-generate.p1.md`, `64e6ba9`)

| Row | Type | Finding | Outcome |
|---|---|---|---|
| **P6-R1** | LAND (A-class register) | The dark-scheme cartoon caster paints shadow-as-highlight (+88/255 brighter than the field) — the T-53 forensic answered (NOT the blur class) | **LANDED `31e7c09`** (lane 16) — `--shadow-color` pinned to the stone form in the `.dark` arm; the 8px cartoon offset survives, ONE knob. **= WR-5/T-53**. Pole A of P6-B1 |
| **P6-V1** | VERIFIED-CURED | The T-45 blur-smear root is dead on the built bundle (bisection ≡ no-blur within 0.06/255; carrier aligned, singular, token-locked) | **NO ROW** — verification of the W6.5 landing; the P3 producer rider stays the booked root cure (W7 verify-at-cut) |
| **P6-W1** | WITNESSED OWNER ROW | T-54 — Generate swatches are flat chips while sibling Mix speaks WatercolorDot | **LANDED `ccf4b30`** (lane 17) via **WR-6/T-54** — `.generate-swatch` joins the WatercolorDot register (seeded per color; T-28 outline law, no ring) |
| **P6-W2** | WITNESSED OWNER ROW | T-55 — the plate verb cluster is three button registers in one row | **BRACKET → owner** via **WR-7/T-55** — NOT approximated; the instrument choice (dock-set capsule ↔ dropdown-menu) is the owner's roster bracket |
| **P6-B1** | BRACKET | The cartoon-caster register in dark (scheme-true shadow ← target → inverted cream pop) | **PACKAGE → owner** (= the dark-caster register bracket, WR-5) |

### Pass 7 — GRADIENT (`passes/gradient.p1.md`, `a4d03d3`)

| Row | Type | Finding | Outcome |
|---|---|---|---|
| **P7-R1** | LAND (interaction) | The swept-envelope regime is ONE-WAY — no deselect gesture; the hero read is unreachable without destroying a stop | **LANDED `31d8ca3`** (lane 19) — Escape on a focused handle + re-tap toggle clear the selection; the pin-on-select law untouched; `views/gradient.spec.ts` grows a release leg |
| **P7-R2** | LAND (a11y/touch) | 20×20 handles + a 24×24 remove chip under the producer's 44px floor | **LANDED `31d8ca3`** (lane 19) — coarse-pointer `::before` hit inflation to 44px (fine pointers unchanged, no visual delta) |
| **P7-B1** | BRACKET | The default-seed hatch-dominance of the envelope plate (`AXIS_HEADROOM` 1.15 ← target → 1.0 tight-axis) | **PACKAGE → owner** (indexed to T-46) |

### Pass 8 — EASING (`passes/easing.p1.md`, `c8799fd`)

| Row | Type | Finding | Outcome |
|---|---|---|---|
| **P8-R1** | LAND (D6) | The one-literal readout under-contrasts in light (~2.7:1 unbacked on the atmosphere wash) | **LANDED `31d8ca3`** (lane 19) — reservation-law `bg-well` backing (5.08 L / 6.77 D); dark not regressed |
| **P8-R2** | LAND (WCAG 1.4.11) | The resting specimen portraits sit under 3:1 in BOTH schemes (~2.6:1) | **LANDED `31d8ca3`** (lane 19) — resting stroke 45→65% foreground (4.53 L / 3.72 D), still de-emphasized under the pressed accent |
| **P8-B1** | BRACKET | The desktop authoring-canvas width/alignment (19rem left-aligned ← target → centered / fill) | **PACKAGE → owner** (indexed to T-47 "too small"); three arms presented |

### Pass 9 — DOCK + NAV + MENUS (`passes/dock-nav-menus.p1.md`, `95d806d`)

| Row | Type | Finding | Outcome |
|---|---|---|---|
| **P9-R1** | LAND (A-class mechanism) | The Q5 ramp resolver walks to its near-black clamp against a mid-L page-ambient referent (monochrome light / 1.24:1 dark) — mechanism CONFIRMED | **LANDED `ccf4b30`** (lane 17) — **= P4-R1 = WR-8/T-56** (ONE lane); the second failure face of the same defective resolver |
| **P9-R2** | LAND (measured) | Dock expansion reflows the scene +3.0px (band floor set below the expanded pill height) | **LANDED `31e7c09`** (lane 16) — band floors at `--dock-band-min-h` (expanded-pill reservation ≥71.4px); zero z/margin arms; separator unperturbed. **= WR-9/T-57** |
| **P9-R3** | LAND (A-class gap) | The T-37 derive-seam guarantee is NOT in the tree — wax↔field ΔL .004–.023 light; the read is halo-carried and collapses at pale seeds | **LANDED `cca4852`** (lane 16) — `useAtmosphere` `guaranteeSeamOffset` (DERIVE_SEAM_FLOOR 0.06), a material uniform-lightness offset fired only at collision seeds; ΔL now .164–.186 both seeds; O-15a `border-style:none` stays GREEN |
| **P9-R4** | LAND (small) | The Login pill's Q10 live-accent voice cascade-dead (border color set, width 0) | **LANDED `ccf4b30`** (lane 17) — accent re-lands on channels the outline variant paints (Profile-trigger idiom); Q10 live at Login |
| **P9-R5** | LAND (small · voice) | Generate preset/harmony menus fork the dropdown NAME-lane voice (bare sans) | **LANDED `ccf4b30`** (lane 17) — name lane joins the display voice; the fork is closed |
| **P9-W1** | WITNESS (producer) | T-20 double-trim live at 768/390 | **P4 producer packet** (segmented-tabs double-trim; O-8 rides W7 post-adopt); re-cited, no demo row |
| **P9-W2** | PRODUCER-FORENSIC | T-52 — the first dock control sits with zero inline slack against the producer clip stack (focus ring structurally shaved) | **ROUTED → the GLASSUI-T-ASKS letter** (inline edge-slack / `overflow-clip-margin` door); demo interim = the T-36 `margin-inline` idiom |
| **P9-B1** | BRACKET ANNEX | The ramp register's pole A gains its darkest reproducible witness (monochrome near-black) | **PACKAGE → owner** (annex to P4-B1/T-56; precondition = P9-R1 ≡ P4-R1) |

### Pass 10 — ADMIN (`passes/admin.p1.md`, `6f7cb35`)

| Row | Type | Finding | Outcome |
|---|---|---|---|
| **A-1** | BRACKET | The loading-skeleton register (breath ← target → shimmer) — glass-ui offers both on the same primitive; "shimmer properly" is the owner's word | **PACKAGE → owner** (app-wide; landed default = `variant="breath"`) |
| **A-2** | booked → O-18 | Admin SECONDARY readouts ride the global `text-muted-foreground` (~3.72:1 light proxy) — app-wide D6 population | **DISCHARGED by AB-2 `f0ef927`** (lane 15) — the certified `--ink-muted` cure on `PaneHeader`/the shared atom lands app-wide; the booked O-18 census extension rides the pixel-confirm at the wave's oracle run |
| **A-3** | LAND (small) | The loading toolbar count lies — "0 users" over three loading skeletons | **LANDED `ccf4b30`** (lane 17) — count suppressed while `loading` (AdminUsersPanel + AdminPane) |

### Pass 11 — MICRO-CHROME + THROWN-ERROR (`passes/microchrome-error.p1.md`, `9efe3d1`)

| Row | Type | Finding | Outcome |
|---|---|---|---|
| **P11-R1** | LAND (small) | `::selection` ink undefined → UA-default BLUE over the warm derived atmosphere, both schemes | **LANDED `31e7c09`** (lane 16) — `::selection` + `::-moz-selection` keyed to the accent + foreground TOKENS, per scheme; warm accent tint, not blue |
| **P11-R2** | LAND (cursor) | The color-channel slider thumbs compute `cursor:auto` while the gradient stop-thumb reads `grab` | **LANDED `a8dc32a`** (lane 14) — `.slider-thumb` reads `grab`/`grabbing` (demo-owned reka markup, no `ui/slider` edit) |
| **P11-R3** | LAND (contrast · O-18) | The light-scheme error DETAIL line composites 2.72:1 over the brick field (sub-3:1) | **LANDED `1705345`** (lane 18) — detail line → `--ink-muted` (4.52 L / 5.73 D); the R7/Q6 error SHAPE stands (contrast-only) |
| **P11-W1** | WITNESS | The thrown-error state PASSES its R7/Q6 judgment clean (role=alert + CircleAlert + Fraunces statement + Fira machine-truth + real Retry; trio/eyebrow FALSE) | **NO ROW** — the declined-ledger error species is exact; re-witnessed, not re-filed |
| — | 3 booked notes | view-blind static `<title>` (n1) · no legacy-webkit `::-webkit-scrollbar` fallback (n2) · beyond-dock native-`title` accessible-name residual (n3) | **BOOKED** (owner-visible nicety notes, not rows) |

---

## §2 The §0.7 owner-filed WR rows (WR-1..WR-11) — folded, zero drops

Owner-filed rows are the highest grade (`§Scope-rider`). Several are the build-shaped half of a
pass row (marked ≡).

| WR | Owner row | Outcome — commit / bracket / W7-W9 |
|---|---|---|
| **WR-1** | T-49a emerge → matter-arrival | **LANDED `5cecdf2`** (lane 16) — drop the α leg; goo-scales as opaque matter from frame 1; the bloom half is PRODUCER-ONLY (communiqué §1.1), no counter-filter |
| **WR-2** | T-49c fission register | **LANDED `5cecdf2`** (lane 16) — `heroConfig.surface.fissionAmp` armed (calm ≈[0.16,0.32]) + park runway extended ≥ one fission beat; the 390 perf gate stays green; fission-amp bracket → roster |
| **WR-3** | T-50 veil-signal calibration | **LANDED `a8dc32a`** (lane 14) — `--glass-bg-quiet` α lowered (~0.44 L / 0.56 D); the D6 rail rest ink re-certifies by live probe (O-18 W4 GREEN); veil-clarity bracket → roster; the deeper plate-clarity-window cure booked |
| **WR-4** | T-51 title step-down | **LANDED `5f273ae`** (lane 14) — `.space-trigger` → `--type-display-2` (53.28px); zero mint, Q11a as ruled; the down-two far pole + the title↔readout SEAM (P1-B1 collapsed) → owner |
| **WR-5** | T-53 dark-caster re-cut | **LANDED `31e7c09`** (lane 16) — **= P6-R1**; the P3 producer book untouched |
| **WR-6** | T-54 WatercolorDot swap | **LANDED `ccf4b30`** (lane 17) — **= P6-W1**; T-28 outline law rides |
| **WR-7** | T-55 verb-cluster instrument | **BRACKET → owner** — dock-set capsule ↔ `ui/dropdown-menu`, BY THE OWNER'S WORD; NOT approximated (E-2) |
| **WR-8** | T-56 ramp resolver cure | **LANDED `ccf4b30`** (lane 17) — **= P4-R1 ≡ P9-R1**; Q5 stands RULED; pastel band centers bracket → roster |
| **WR-9** | T-57 `--dock-h` floor correction | **LANDED `31e7c09`** (band floor) **+ `2c1a0f4`** (born-RED invariance probe) (lane 16) — **= P9-R2**; T-31 extends to the expansion axis |
| **WR-10** | T-58 swap-window scheduling re-design | **W7/W9 BY LAW** (payload half) — the measured signature HANDED (t49-research §10); confound isolation FIRST (O-16-R1's 150ms clobber LIVE in dist, PKT-1 unlanded); cure WITH the wake-gray bead (**boot-B landed `5cecdf2`**); pass-2 row F is the partner bracket; W9 walk re-verifies |
| **WR-11** | T-59 one-law rhythm regime | **LANDED `a8dc32a`** (picker console) **+ `bdd0a9c`** (config population, M-34) (lane 14) — ONE container-scaled law; the 44px touch rung preserved by hit-area extension; clamp constants → roster |

---

## §3 The 7 checkpoint BOOK-W8 brackets (from `w45-checkpoint/ROWS.md`) — carried to the package

| Row | Bracket | Package status |
|---|---|---|
| **R3** | T-26 sage-whisper floor (vividness(0)=0 dead-flat ← whisper → over-tinted) | **PACKAGE bracket** (= pass-2 Row D) |
| **R5** | Rail α face (Fraunces-fallback Greek ← distinct-glyph → donor-subset) | **LANDED default `e2e8517`** (Fira Greek upright, = P1-R2); C-4 donor-subset far pole → **PACKAGE bracket** |
| **R7 / C-3** | Blob-seat positive domain (corner-flush 0 ← centre-ward → over-inset) | **PACKAGE bracket** as ONE (= P1-B3 = pass-2 Row E) |
| **R8** | Q2 boot-settle (1.79–2.5s ← [1.4,1.7]s → snappier) | **PACKAGE bracket** (= pass-2 Row C) |
| **C-2** | Readout header 4th-alpha cell (3-cell no-alpha ← target → 4th cell re-deriving the O-10b locks) | **PACKAGE bracket** (= P1-B2; BY CONSTRUCTION) |
| **C-4** | The self-cut tnum Fraunces face — *the flag IS the deliverable* | **PACKAGE bracket** (rides P1-R2 / the rail α face) |
| *(two flagged non-rows)* | rest-floor in-bracket read · dark-scheme field/chrome split (GAP-L2) | **PRESENTED in the package** (rest-floor = in-bracket default, F9.R1 producer knob booked W7; GAP-L2 = standing P1 book, PR-2 fence) |

---

## §4 Tallies + attestations

- **Pass-filed rows**: 40 across 11 passes → **all dispositioned** (LAND with commit · BRACKET
  to package · PRODUCER to letter · WITNESS/VERIFIED with no new row · TRIUMVIRATE routed).
- **Landed defects (this wave)**: 24 LAND rows across 6 lanes (P1-R1/R2 · boot-A/B · AB-1..4 ·
  P4-R1/R2/R3/R4 · E1-R1/R2/R3 · P6-R1 · P7-R1/R2 · P8-R1/R2 · P9-R1..R5 · A-3 · P11-R1/R2/R3).
- **§0.7 WR rows**: 11 → 9 LANDED (WR-1..WR-6, WR-8, WR-9, WR-11) · 1 BRACKET-to-owner (WR-7) ·
  1 W7/W9-by-law (WR-10, its wake-gray compound half landed).
- **Package brackets for the owner**: enumerated in `PACKAGE.md §Bracket table` (the pass-minted
  axes + the §12 taste rows + the wave-gate eye-judge clauses + the §0.7 roster + the 7 checkpoint
  brackets). Every pole is a NAMED reproducible state (tuple / commit / frame path).
- **Producer/letter rows**: P1-R3 · P9-W2 · P9-W1 (T-20) — all ride the standing GLASSUI-T
  communiqué (`f3f3c097`); the producer halves are letters, never demo forks.
- **Triumvirate routes**: pass-2 Row G (o12 backing-ratio leg); the oracle blind-spots each pass
  named (ramp composited-contrast · EmptyState caption census · wax↔field seal delta · slider
  cursor · error-detail contrast) — bounds expansions handed to the triumvirate record, W9-inherited.

**Zero silent drops** · **zero rows contradict a §12 RULED outcome or a §4 retirement** (each
pass attests this in-record; every LAND preserves the ruled FORM and adds/cures, never
re-litigates) · **nothing routes OWNER as a conflict** — the brackets go to the owner *as
brackets*, which is the wave's design.

**EXPECTED-RED oracle legs ride unaltered** (recorded, not weakened): O-16-R1 (150ms clobber,
PKT-1→W7) · O-5 (boot-pacing spike, RP-2→W7) · O-26 (aurora headless→W9). See `PACKAGE.md
§Honest-red ledger`.
