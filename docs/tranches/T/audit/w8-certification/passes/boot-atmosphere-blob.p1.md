# T.W8 · PASS 2 · p1 — boot / atmosphere / blob + ConfigSliderPane population + card-transition motion

**Surface**: the boot overture (B0–B4 · gradient ground · fouc-guard) · the T-26 living field ·
the blob (emerge / seat / hover / park) · **the ConfigSliderPane population (Atmosphere + Blob
config views — the W4-4 population clause's re-judge surface, M-34)** · **the T-48
card-transition motion residual (frame-by-frame, the owner's ordered method)**. This pass OWNS
the package's **boot screencast** and the **T-48 mid-flight captures**.
**Filed**: 2026-07-11, at head `5e4f1f6` (post-T.W6.5-close; W7 TRIGGER-NOT-FIRED, recorded
as-is per PP-5). Iteration **p1** (round 1 of ≤3). Probe-only — this record + the `pi/w8`
probe artefacts are the pass's only writes; **no taste is certified here** (lesson 12; the
terminal authority is the owner).
**Law consumed**: `RATIFICATION-2026-07-09.md §0` (Q2 "full comp." · Q3 "Flush." · Q6 "both." ·
Q14) → `MANDATE-2026-07-06.md §0` + §0.1/§0.3/§0.5/§0.6 (T-1 · T-8 · T-26/T-27 · T-30 · T-32 ·
T-38 · T-39 · T-48 — the owner's verbatim lines for this surface) → `SYNTHESIS.md §2` D1/D3/D4/
D7/D8 + §6.1/§6.2 → `waves/T.W8.md`. Pass inputs: `w45-checkpoint/ROWS.md` (R2/R3/R7+C-3/R8 +
the two flagged non-rows) · `w65-close-artefacts.md` (§2 disposition map; the T-48 residual +
the W8 brackets BY NAME) · `t33-research.md §4/§7`. Anchors re-derived via `w1-move-map.md`
(the boot chain = `demo/color-picker/composables/boot/`; the seat =
`demo/@/components/custom/color-picker/`).

## §0 Probe environment (the O-3 class — driven LIVE, headed real GPU)

- Lane-unique ports `VJS_E2E_PORT=8610` (vite dev) / `VJS_E2E_PERF_PORT=8611` (built
  `dist/gh-pages` via `serve-built.mjs`, tree-fresh at the W6.5 close-cure build); the owner's
  `:9000` untouched (probed live-and-avoided).
- **Headed real GPU**: `ANGLE (Apple, ANGLE Metal Renderer: Apple M5 Max)` — confirmed per-leg.
  Probe instruments: `pi/w8/w8-pass2-probe.mjs` + `w8-pass2-probe-resume.mjs` (the resume is
  the SAME iteration — the first run walled at a 390 wait before the motion leg; per the
  §Recovery rider a wall mid-iteration resumes, never resets the count); log of record
  `pi/w8/w8-pass2-probe-log.txt`. PNGs gitignored-by-class on-disk (the standing convention).
- **Oracle floor re-run at this head** (independent, lane ports): O-1 · O-1b · O-2 · O-4
  (1×+6×) · O-11 (6 gates) · O-12 (3/3) · O-16 (W5 census) · O-26 · atmosphere-cold-load →
  **20 passed**; the born-RED legs reproduced exactly as `test.fail()`-designed: **O-26
  headless** 10s migration 0.000/255 (SwiftShader css-placeholder — the headed annex below is
  the authoritative read) · **O-5 spike** max 192.3ms = 4.6× median vs the 3× line (dropped
  9.4% ≤ 10%; RP-2→W7 cite intact). Perf legs on :8611: O-24 both schemes GREEN (LCP = the
  picker plate, opacity 1, reveal-only law holds) · view-switch first-frame 83.4ms ≤ 100
  GREEN. No gate weakened; the reds ride as reds.

## §1 Coherence verdict (PRE-FILTER only — no taste certified)

**COHERENT with the doctrine — and materially stronger than the W4.5 read of the same
surface**: the R1/R2/R6 checkpoint cures HOLD on the built bundle (returning-dark boots dark;
the bead boots at FULL backing resolution and parks crisp; lockstep byte-identical per O-2);
the headed O-26 read is **all-three-gates GREEN on real GPU** — the field lives; the M-34
ConfigSliderPane population delivers the console grammar at 1440 AND 390 in both views; the
swap choreography reads liquid *when frames flow*. The findings below are: one A-class defect
the oracle census provably cannot see (the SECOND slider population's track material — the
exact T-44a shape), one mid-flight motion defect captured frame-by-frame (the Home-return
freeze — its payload half stays W7's BY LAW), one small visual defect (the gray bead pose
inside that window), and the four standing brackets re-taken with fresh numbers and frames for
the package. Nothing here contradicts a §12 RULED outcome or a §4 retirement; nothing routes
OWNER as a conflict — the brackets go to the owner as brackets, which is the design of the
wave.

## §2 What was judged, item by item

1. **The boot screencast (THE PACKAGE DELIVERABLE — T-1/T-25/T-27/§0.1/§0.3)**: recorded on
   the BUILT bundle, headed real GPU, **cold + returning × light + dark** —
   `pi/w8/boot/{cold,returning}-{light,dark}/f<offset>ms.png` (383/412/392/358 frames,
   compositor-true CDP series, offsets aligned to the page's `timeOrigin` so frames pair with
   the beat marks). Frame-series read: ground is a complete material from first paint (both
   schemes — **cold-dark boots dark from t0**, plum ground + cocoa plate at 319ms, the R1 cure
   visible); the plate lands with title+readout inked and the empty-spectrum window reads as a
   designed skeleton beat; dock/right-plate stagger legible; field wakes (b2) as "the painting
   waking up", never a gray crossfade (O-1b L-series flat/monotone); the bead goo-emerges
   crisp. No five-clock smear, no About pop, no first-frame blob, no freeze-then-pile-up
   inside the boot window itself.
2. **Q2 settle (the R8 re-take — the instrument caveat DISSOLVES)**: on the TRUTH instrument
   (built + real GPU) field-settle (= b2-start + 0.9s derive-in) lands **1732–2059ms** across
   the four arms (cold-light 2012 · returning-light 1732 · cold-dark 1849 · returning-dark
   2059) vs the ratified bracket **[1400, 1700]ms** — past the upper pole on EVERY arm, no
   longer dev-transform noise (dev instrument: 2143/1892ms, same shape). The mover is b2-start
   (832–1159ms vs the D3 gate arithmetic's ~620ms), i.e. the §4-attributed eager-payload wall
   — **the payload half stays W7's** (RP-2/L20/GAP-L5; T-39/Q14 pressure) and is NOT
   re-bracketed as taste; the bracket presentation half goes to the package (row C).
3. **O-26 HEADED (the authoritative perceptibility read)**: warm mid-C seed, real GPU — calm
   2s glance **0.935/255** (< 2 ✓) · 10s migration **8.55/255** (≥ 4 ✓) · 60s min 5s-apart
   pair **2.26/255** (≥ 1 ✓). The field breathes, migrates, never loops — D4 delivered at
   mid-C. The headless born-RED leg stands as the documented env-class (css placeholder on
   SwiftShader), packet cite unaltered.
4. **The R3 neutral-seed whisper (re-taken with numbers)**: neutral `#808080` — field band
   mean OKLCh **C = 0.0036 (light) / 0.0049 (dark)**: chromatically SILENT (the D4 "sage
   whisper, never silence" intent does not land on the chroma axis), while the field is
   otherwise ALIVE (10s migration 13.7/16.6 per 255 — the motion legs would pass). The W4.5
   "dead-flat" read splits: dead-flat in CHROMA, living in L. Bracket input with fresh poles —
   row D. Frames `pi/w8/field/neutral-{light,dark}.png`. (Band-mean C is a cancellation-prone
   metric; at vividness(0)=0 the field is genuinely achromatic everywhere — caveat recorded.)
5. **The blob (T-30/T-8 · R2 verify · the R7+C-3 re-take)**: dpr2, built bundle —
   - **R2 HOLDS at boot**: backing **360×360 for the 180.2px box = ratio 1.998 (full-res)** at
     the early post-emerge read; the park freezes the FULL-RES frame (parked ratio 1.998;
     1s park diff 0.387/0.293 per 255 ≈ static). The W4.5 0.35×-boot blur is dead on the live
     tree. The P6 rider (interior shading + resting halo) stays the residual-softness book.
   - **Hover-mood floor**: 400ms hover-in frame-diff **10.89 (light) / 12.29 (dark)** ≥ the
     6/255 O-12 floor — the bead answers the pointer.
   - **Seat**: anchor gap to card right/top **0.0px / 0.0px** (`--blob-seat: 0px`,
     `--blob-fp: clamp(7rem, 22cqi, 11rem)`) — Q3 "Flush." held verbatim. The dpr2 eye-read on
     the NOW-CRISP bead (`pi/w8/blob/seat-{light,dark}-dpr2.png`): crisp silhouette + specular
     highlight, wholly on-card, tooltip re-seated below, figure-ground delta present (dark
     reads stronger via the halo; light reads pink-on-pink but floored). The bead still reads
     *contained-but-corner-pinned* — the centre-ward knob is the standing ONE bracket (row E).
6. **ConfigSliderPane population (M-34 re-judge — the W4-4 clause)**: `/#/atmosphere` +
   `/#/blob`, both schemes, 1440 + 390 (`pi/w8/config/*.png`):
   - **The grammar DELIVERS**: `.console-well` seat present, **no backdrop-blur** (rung-2 WELL
     honest, D1); section headers (Atmosphere: Field ×3 sliders + 4 enum select rows via the
     default slot; Blob: Geometry/Membrane/Color/Lit Glass/Pointer/Satellites/Tempo ×31
     sliders + quality select); ONE label per row with the paired live readout (no doubled
     row); label ink `rgb(28,25,23)`/`rgb(233,230,226)` and live-value ink at the certified
     `--ink-muted` rung (`oklch(0.4385 …)`/`oklch(0.8954 …)`) — the O-18 config TEXT rows
     green by construction; action bar a flex-none footer (never occludes).
   - **The 390 touch rung DELIVERS**: slider rows **44px** both views (`:scope >
     div:last-child`; the first probe's 16px was a descendant-selector instrument error,
     corrected in-resume). `/#/blob` at 390 lands on the mobile slot's **Picker** tab
     (viewSchema `blob = {left: color-picker, right: blob}`, no `defaultPaneIndex`) with the
     designed segmented cycle (Picker | Blob) revealing the full 31-row console — reachable by
     design, no defect (`config/blob-light-390-cycled.png`).
   - **The defect the census cannot see**: the TRACK material — row A below.
   - The Zones slider renders pinned at 6 (the producer `MAX_NUCLEI` clamp made visible) — the
     standing T-32 ceiling book, re-cited (no new action).
7. **T-48 card-transition motion (mid-flight captures — the owner's frame-by-frame method)**:
   real pane swaps on the built bundle, both schemes, 4 hops each
   (Home→Gradient→Mix→Browse→Home), CDP mid-flight series at `pi/w8/motion/<scheme>-<hop>/` +
   rAF pacing + longtask observers + one `devtools.timeline` trace:
   - **When frames flow, the choreography is coherent**: rAF median 8.3ms (120Hz), slides move
     in unison with the field showing between cards, exits read shorter, O-16 census green
     (this pass's own re-run), trace RunTask>16ms = [] on the traced swap, Layout=38 events =
     one-time mount work, not a per-frame track.
   - **THE CAPTURED DEFECT**: the light **Browse→Home** swap FREEZES mid-flight — a **356ms
     compositor hole** (frames `f00648ms` → `f01004ms`, the two straddling frames near-
     identical mid-pose: outgoing cards held part-slid, incoming spectrum empty), rAF max
     358.4ms + 4×53ms long tasks; the transition then *continues from the held pose*. The
     freeze-then-continue signature IS the owner's "janky, not smooth". Dark Browse→Home ran
     83ms max (variance — one-sample caveat); Home→Gradient carries a 73–96ms first-mount task
     (chunk arrival) under the felt wire. Attribution: Home-face re-activation payload
     (KeepAlive picker + About/KaTeX + spectrum + blob rewake) — **the payload wall class
     stays W7's by the wave law**; the motion-taste residual goes to the package (row F).
   - **A distinct visual defect inside that window**: the bead re-enters **GRAY** (a dark-gray
     bead pose through the frozen window, re-inking to the palette only at settle ~f01147) —
     the §0.3 "too gray" family made visible mid-swap. Row B (forensic).
8. **The two flagged non-rows (W4.5 hand-offs), re-read**:
   - **Rest-floor in-bracket read**: the INK_FLOOR 0.15 ∈ [0.12, 0.20] bead/plate delta reads
     present-but-modest in light (pink-on-pink, floored), strong in dark — the dpr2 crops are
     the package's eye-material; stays an in-bracket default (no row; the F9.R1 producer knob
     swap is booked at W7).
   - **Dark-scheme field/chrome split (GAP-L2)**: VIVID in the dark captures — settled dark =
     dark cocoa cards floating on a BRIGHT salmon/pink field (the L2 `lBand` producer atom
     absent). Standing P1 row, re-cited with frames; **no demo action (PR-2 fence)**.
9. **Environment notes (instrument honesty)**: the `:8611` static instrument has no
   `VITE_API_URL`, so the designed dev-misconfig register fires (1 console.error + the lamp;
   Browse renders "The commons is unreachable") — a LOOPBACK-ONLY designed state, never prod;
   the package assembler must read the lamp in the boot frames as instrument, not product.
   The Browse/Palettes empty-wall frames incidentally show the restored trio+dashes plate and
   non-bold menu options with the Palettes-only ramp (T-41/T-40/Q5/T-10 visible in passing —
   other passes own those surfaces).

## §3 THE ROWS (typed; every row lands-or-books via the wave's remediation machinery — zero silent drops)

| # | Type | Finding | Disposition / route |
|---|---|---|---|
| **A** | **LAND** (defect, A-class — the named oracle blind spot extended to the SECOND population) | Config slider TRACK material is invisible-class on the well: `.slider-track` vs `.console-well` ≈ **1.09:1 light / 1.26:1 dark** (WCAG 1.4.11 wants ≥3:1 for the control's extent), and `.slider-range.glass-liquid-fill` computes transparent + no background-image at element level — no visible filled/unfilled split by eye in either scheme. The W6.5 GRAPHICS-leg cure (T-44a) covered the **Extract** instrument tracks ONLY; O-18's config rows judge TEXT only — the exact S failure shape (named-site-not-population) on the app's second slider population | Remediation lane (single-writer on the files): re-ink the config population's track per the Lane-I contract (`ConfigSliderPane.vue` scoped rung — the same demo-side cure class as `eb7bb2c`-era Extract re-ink), + extend the **O-18 GRAPHICS census** to `.config-console` tracks (`e2e/smoke/oracles/o18-contrast-census.spec.ts` — a census extension, the W6.5 precedent, born-RED against today's track). If the forensic roots the register in the producer's `glass-slider` recipe, the cure re-homes to a **P-family rider** and the demo carries the booked-swap interim — never a fork |
| **B** | **LAND** (defect — forensic-first, producer-escape named) | The bead re-enters **GRAY** on the Home-return swap (mid-flight frames `motion/light-browse-to-home/f00648–f01004ms`: a dark-gray bead pose through the frozen window, palette re-ink only at settle) — a visible "too gray" flash (§0.3 family) on every cached-pane return that wakes the parked engine | Remediation forensic: the wake/repaint order on KeepAlive re-activation (`HeroBlob.vue` wake path / `paletteStops` re-seed vs the engine's default stops). Demo-root → land in-lane; producer-root (engine wake repaints before the consumer's stops arrive) → the **P6 rider** gains the arm BY NAME (PR-2 fence — no demo shader/engine fork) |
| **C** | **BRACKET** (package — the Q2 annex; the R8 re-take, DE-CAVEATED) | Boot settle past the ratified upper on the TRUTH instrument: **1732–2059ms** (4 arms, built + real GPU) vs **[1400, 1700]ms**; cadence itself crisp (no pile-up in-window; O-4 order holds 1×/6×). Poles, both named: **crisp-but-late** (the CURRENT landed state — b2 832–1159ms + 0.9s derive; the measured 1.73–2.06s) ← **the ratified target [1.4, 1.7]s** → **too-fast smear** (the pre-T ~1.0s five-clock boot the owner rejected as T-1/T-27). The payload lever (b2-start, the §4 eager-mount wall) is **W7's BY NAME** (RP-2/L20/GAP-L5; Q14 pressure) — what the owner rules here is the TARGET, not the lever | Package (Q2 annex row) with the before/after screencasts: `pi/w8/boot/*` (this pass) vs the W2 baseline `pi/w2/screencast/`. Indexed to §0.1/§0.3 + T-27 |
| **D** | **BRACKET** (package — R3, re-taken with numbers) | The neutral-seed whisper floor: at seedC=0 the field is chromatically SILENT (band C **0.0036/0.0049**) while alive in L (10s migration 13.7/16.6 per 255) — D4 says "a sage whisper, never silence". Poles, both named: **silence** (CURRENT: `vividness = smoothstep(0.02, 0.10, seedC)` ⇒ vividness(0)=0, C≈0.004 — frames `pi/w8/field/neutral-*.png`) ← **a perceptible sage whisper** (a floor arm on the vividness curve, e.g. field C ≈ 0.02–0.04 at seedC=0, judged by eye) → **marigold** (the D4-named failure: injected chroma reading as a color LIE on a gray pick). In-bracket calibration lever: `boot/atmosphere-calibration.ts` (demo); the bracket remains the P1 variance-atom sizing spec | Package (the T-26/D4 axis) — owner-judged; a remediation lane turns the knob ONLY if the owner rules the whisper arm |
| **E** | **BRACKET** (package — R7 + C-3 merged, ONE ruling; the ordered crisp-bead re-take DONE) | The seat positive-domain: with R2 landed the bead is CRISP and the corner-read is now purely positional — flush 0.0/0.0px, wholly on-card, tooltip below, hover live. Poles, both named: **corner-flush** (`--blob-seat: 0px` — the CURRENT state, the ratified Q3 "Flush.") ← the owner's T-30 words "more towards the centre, deftly integrated" → **over-inset** (bead crowding the title/readout figure — the containment identity still bounds orbit-reach ≤ 0.5). Q3 STANDS untouched; the knob's positive domain is the sanctioned NEW axis (T-30 §0.5) | Package with the dpr2 crops `pi/w8/blob/seat-*-dpr2.png` beside the owner's `t-30-blob-blurry-topright.png` (the §0.5 BEFORE). Indexed to T-30/T-8 |
| **F** | **BRACKET** (package — the T-48 motion residual) + the payload half **stays W7 BY NAME** | The mid-flight freeze signature, captured frame-by-frame: light Browse→Home holds a mid-slide pose for **356ms** then continues (`motion/light-browse-to-home/f00648→f01004`); the curve/duration family itself is house-true (O-16 green; median 8.3ms when flowing). Poles, both named: **frozen-mid-pose** (the CURRENT captured state on Home return) ← **hole-free swap** (no compositor gap > ~2 frame budgets inside the enter window — the felt "smooth") → **over-deferred** (mount pushed so late the incoming pane arrives visibly blank/late). The MOVER is the Home-face re-activation payload — **W7's** (§4 walls; the wave doc's own boundary: the §4-attributed 'janky' walls are never re-bracketed as taste) | Package (the T-48 axis, indexed to §0.6 verbatim) with the mid-flight series both schemes; the payload citation rides the W7 dead-payload/RP-2 rows BY NAME. The W9 walk re-verifies the freeze after W7's levers land |
| **G** | **TRIUMVIRATE-ROUTE** (oracle-bounds note, not a mint) | The R2 oracle note stands open: an **o12 boot-time backing-ratio leg** (assert ratio ≈ dpr at the post-emerge read — this pass measured it by hand: 1.998) would make the R2 regression class visible to the slate; a new leg is a bounds expansion the pass may NOT mint (§Triumvirate dispatch) | Routed to the wave's triumvirate record with the evidence in hand; W9 inherits either the minted leg or the declined-note |
| — | **PRODUCER-PACKET re-cites** (standing rows, no new dispatch) | GAP-L2 dark `lBand` (the field/chrome split, vivid in the dark frames) → the standing **P1** row · T-32 `MAX_NUCLEI=6` ceiling (the pinned Zones knob) → the standing **P1/W7** book · T-38 aurora-pointer "extreme/noteworthy" → **PRODUCER** (relayed `8a0ca24e`; t33-research §7: the demo already consumes every shipped knob — the one demo scalar 0.45 cannot cure dead axes; **a demo shader fork stays forbidden, PR-2**) | The letters are already out; verify-at-cut is W7's. No demo action filed |

**No row contradicts a §12 RULED outcome or a §4 retirement** (row E explicitly leaves Q3
"Flush." standing; row C leaves the Q14/W7 gate routing untouched; rows A/B are breaches of the
landed design's own contracts). **Nothing routes OWNER as a conflict**; the brackets go to the
owner inside the package, which is the wave's design.

## §4 Evidence index

- **The boot screencast (package deliverable)**: `pi/w8/boot/{cold,returning}-{light,dark}/`
  — 1545 compositor frames, offsets ≡ beat-mark clock; beat marks + settle numbers in
  `pi/w8/w8-pass2-probe-log.txt`.
- **T-48 mid-flight captures (package deliverable)**: `pi/w8/motion/<scheme>-<hop>/` (8 hops)
  + pacing/longtask/trace lines in the log; the freeze pair is
  `light-browse-to-home/f00648ms.png` / `f01004ms.png`.
- Blob: `pi/w8/blob/seat-{light,dark}-dpr2.png` + the backing/park/hover numbers (log).
- Config population: `pi/w8/config/{atmosphere,blob}-{light,dark}.png` + `*-light-390.png` +
  `blob-light-390-cycled.png` + the track/ink/rung numbers (log).
- Field: `pi/w8/field/neutral-{light,dark}.png` + the O-26 headed numbers (log).
- Oracle floor: §0 (this pass's runs at 8610/8611, head `5e4f1f6`).
- Probe instruments (committed): `pi/w8/w8-pass2-probe.mjs` · `w8-pass2-probe-resume.mjs` ·
  `pi/w8/w8-pass2-probe-log.txt`.
