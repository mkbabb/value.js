# T.W8 · critique pass — DOCK + NAV + MENUS · p1 (round 1 of ≤3)

**Filed**: 2026-07-11 · Fable + frontend-design · **PROBE-ONLY coherence PRE-FILTER**
(no authoring; no taste certified — a pass verdict is "coherent with the doctrine" or a
filed row, NEVER "beautiful"; lesson 12. Taste is the OWNER's.)
**Branch/head**: `tranche-t` @ `571f8d0` (docs-only commits atop the W6.5 close — the probed
product tree is **`5e4f1f6` byte-exact**, verified `git diff 5e4f1f6..HEAD -- demo/ src/ e2e/
api/` EMPTY; the same tree the owner's §0.7 evening audit judged).
**Spec of record**: `waves/T.W8.md` §Scope-1 · `SYNTHESIS.md §2` D1/D2/D5/D6/D7 + §1.1 R1/R9
· `MANDATE-2026-07-06.md` §0 (T-10 · T-17 · T-20 · T-29) + §0.4 (T-29) + §0.6 (T-36 · T-37 ·
T-40 · T-43) + **§0.7 (T-52 · T-56 · T-57 — this pass files AFTER the addendum and consumes
its named rows)** · `RATIFICATION-2026-07-09.md` §1 (Q5 · Q10 · Q12 · Q18 + cascade 3) ·
`w65-close-artefacts.md` HG1 (the T-36/T-37/T-40b/T-43 VERIFY-GREEN states this pass re-judges
by eye) + §3 books (P4 · P10) · `w45-checkpoint/ROWS.md` B-1/C-5 (the dock-band structural spec
— attested live below) · `audit/t33-research.md` §6.3/§6.4/§6.5 (the handed mechanisms).
**Anchors re-derived via `w1-move-map.md` + the live tree**: `dock/Dock.vue` (seal ~:246+,
band seat) · `dock/ActionBarToggle.vue` (T-29/T-36, the W6-8 cap lift) · `dock/DockViewSelect.vue`
(menu voice, :110-115) · `dock/menus/{ProfileSection,MobileMenuDropdown}.vue` ·
`color-picker/display/ColorSpaceSelector.vue` (option letterform) ·
`@composables/color/palettes-ramp.ts` + `boot/useViewAccents` (the ramp writer) ·
`generate/GenerateControls.vue` + `color-chips/PreviewStrip.vue` (T-17) ·
`panes/PaneSegmentedControl.vue` (T-20, producer `SegmentedTabs`) · `@styles/style.css`
(`.app-layout`/`.dock-band` §487, `--dock-h`).

---

## §1 Method (the O-3 probe class — live drive, real engine, dpr 2)

- **Serve**: the live tree on **VJS_E2E_PORT=8680** (`VITE_API_URL=http://localhost:8680
  npx vite --port 8680`; PERF_PORT=8681 reserved unused; **the owner's :9000 untouched**).
- **Cells**: 1440×900 · 768×1024 · 390×844 × light+dark, deviceScaleFactor 2, `colorScheme`
  emulation; headed Chromium (real compositor — the seal/field reads are GPU-composited).
- **Drives**: dock at rest → Tools hover → action-bar layer swap → Back → idle-collapse (the
  producer timer arms on dock enter/leave — isolated in leg 3b) → re-expand; the view dropdown
  opened at every cell; the letterforms dropdown; the generate preset + harmony dropdowns; the
  @mbabb menu; the ⋮ mobile menu; the T-52 Gradient-trigger rest/hover/focus states; the T-37
  seal probed at TWO seeds (the owner reference `lab(38% 32 24)` + the adversarial pale
  `lab(88% 5 30)` — the "legible against EVERY self-derived field" claim's stress case).
- **Probes**: computed box-model/overflow/weight/background census + clipping-ancestor walks +
  pseudo-element paint reads + **composited pixel-sampled figure/ground** (screenshot-clip →
  in-page canvas decode; OKLab L + WCAG in-script) + the ramp-token settle trace (6 samples
  over 5s + plain-load control).
- **Frames**: `docs/tranches/T/audit/pi/w8/dock/` (28 PNGs, gitignored-by-class on-disk):
  `{1440,768,390}-{light,dark}-dock-rest.png` · `{768,390}-{light,dark}-view-menu.png` ·
  `1440-{light,dark}-{view-menu,tools-hover-zoom,actionbar-layer,dock-collapsed,seal-zoom,
  seal-pale-zoom,ramp-label-zoom,t52-gradient-trigger-zoom,t52-trigger-hover-zoom,
  t52-trigger-focus-zoom,preset-menu,harmony-menu,letterforms-menu,mbabb-menu,login-zoom}.png`
  · `{768,390}-{light,dark}-tabs-zoom.png` · `390-{light,dark}-mobile-menu.png`.
  Instruments + log committed: `pi/w8/dock/w8-dock-probe{,-leg2,-leg3,-leg3b}.mjs` +
  `w8-dock-probe-log.txt`.

## §2 Console attest

**ZERO console errors** across every cell and driven state in legs 1–2 (listeners on every
page: menus, layer swaps, collapse cycles, both seeds). Legs 3/3b ran without listeners
(state-isolation reruns of already-attested drives) — recorded honestly.

## §3 COHERENCE VERDICT

**COHERENT WITH THE DOCTRINE, WITH FILED ROWS.** The landed W6.5/W6-7/W6-8 dock states
re-judged by eye HOLD: the Tools trigger is a true button (T-36 — 8px/12px at the pill scale,
margin off the folded separator, hover capsule + shadows WHOLE at settled rest, T-29 clip
released), the die-rim is dead and the wax fills the seal (Q12/O-15a `border-style: none`
computed), the menu voice is INK with icon+label rows (T-10 — zero per-row hue legend, admin
gold intact), option letterforms compute weight 400 both schemes (T-40a), the T-17 preview
chips ride every generate preset/harmony row at the φ² plate proportion with honest
7-segment truncation, the ⋮/@mbabb menus wear chrome glass unclipped, and the two-band
grid's structural spec (B-1: in-flow nav, zero z arms, designed gap) attests live.
**Except**: the §5 ledger files **one A-class mechanism defect (the Q5 ramp resolver
walks to its near-black clamp against a mid-L page-ambient referent — the owner's ONE
sanctioned rainbow renders monochrome near-black in light and near-invisible in dark, at
BOTH consume sites), one T-57 measured violation (dock expansion reflows the scene 3.0px),
one T-37 mechanism gap (the derive-seam delta the verify record names is not in the tree —
wax↔field ΔL 0.004–0.023 in light at both tested seeds), two small rows (the Login pill's
Q10 accent voice cascade-dead; the generate menus' name-lane voice fork), answers the T-52
routing question with the forensic (producer dock-layer flush-clip geometry), and re-witnesses
the standing P4 (T-20) producer book with live numbers.** Nothing here certifies taste.

## §4 The attested table (owner line → live witness → read)

| Owner line | Live witness (probed, not assumed) | Read |
|---|---|---|
| **T-36** t33-audit-04 "proper margin and padding like a true button element" | Tools trigger 121×40 pill: `padding: 8px 12px` · `margin-inline: 4px` off the folded separator · `gap ≈ 0.5em` glyph→wordmark→arrow · radius 9999px — the Button-primitive scale via the producer's own `--dock-compact-control-padding` hook; frames `1440-{L,D}-tools-hover-zoom.png` | COHERENT (`c237d24` stands; HG1 re-judged by eye GREEN) |
| **T-29** §0.4 "the pseudo dropdown is clipped at the edges" | `.action-bar-toggle-inner` computes `overflow: visible` at settled rest (`is-visible is-live is-settled` stamped); hover capsule + lift shadows render WHOLE (both schemes, by eye); the clip returns only for presence transitions | COHERENT (the W6-8 settle stamp holds at rest) — **the surviving edge-clip class re-homes to T-52, the DockSelectTrigger flush seat (§5 P9-W2)** |
| **T-40** t33-audit-06 "Dropdown options should not be bold" | View menu: all 7 options weight **400** both schemes; letterforms menu: 18 `.specimen-name` weight **400** (the `--type-weight-display` pin live); generate menus: sans 400 | COHERENT on the option voice — **the current-entry `font-semibold` (600) exception is P4-R4 (filed, re-witnessed here: "Home" @600 both schemes); one lane owns `DockViewSelect.vue`** |
| **T-10 / T-43 / Q5** "Only Palettes rainbow, the rest white/black" | DOM census: `.palettes-ramp-text` on EXACTLY the Palettes row; every sibling `background-image: none`, popover ink (light `rgb(28,25,23)` / dark `rgb(233,230,226)`); icon column ink; admin gold = the ledgered exception; the W7-4 legend machinery dead | COHERENT on the ×2-site LAW and the ink menu — **but the ramp's RENDER is broken at the mechanism: §5 P9-R1** (near-black monochrome light / 1.24:1 dark — the rainbow does not exist as rainbow) |
| **T-37** t33-audit-05 "cannot properly see the underlying palette color swatch on the collapsed dock" | Owner seed: wax `rgb(143,68,55)` L .480 vs field L .475 — **ΔL .004, WCAG 1.03:1** (dark: .016/1.06:1); by eye the seal READS via the producer pill's light glass halo + the inked glyph (frames `seal-zoom`); pale seed `lab(88% 5 30)`: light ΔL .023 / **1.08:1**, the halo collapses into the light field — the swatch read goes faint | PARTIALLY COHERENT — legible at the owner's shot-05 seed BY THE HALO, but the §6.4 derive-seam guarantee is NOT in the tree (§5 P9-R3); O-15a `border-style: none` computed ✓ (the rim stays dead) |
| **T-52** t49-audit-04 "left and right edges are clipped slightly in the dock — what do we need to relay to glass-ui?" | THE FORENSIC (both schemes): trigger rect `[484.8, 612.8]` — its left edge sits **EXACTLY at the producer `.dock-layer--full` clip edge** (`[484.8, 955.2]`, `overflow: auto/hidden` + a degenerate 0px-fade `mask-image`; `.dock-layers` `hidden/auto` same box; **zero inline slack**). Rest ring = ::before **inset** 0.75px (unclippable) — the rest-state shave is the shared-edge antialias cut; **focus paints an OUTER `0 0 0 2px` accent ring + 8px glow — structurally shaved at both inline edges by construction**; hover adds no transform on this trigger | ANSWERED — the routing question resolves **PRODUCER** (§5 P9-W2): the relay = inline edge-slack/overflow-clip-margin door on the dock layer (or an outer-paint budget on edge controls); the demo interim idiom exists in-tree (the T-36 `margin-inline` cure) |
| **T-57** t49-audit-09 "when the dock expands, this should NOT shift the UI downwards" | Collapsed band h **65** → expanded h **71.4** (the `--dock-h` floor is set BELOW the expanded pill height) → `.pane-container` top **99.7 → 102.7 = 3.0px downward reflow** on every expand; the action-bar layer swap holds 71→71 (no shift — the layer grammar is clean) | VIOLATION MEASURED (§5 P9-R2) — the T-31 band law stands; the expansion RESERVATION the owner ordered is not yet landed |
| **T-17** "drop downs … better stylized with color previews, deftly and in proportion" | Preset menu 10/10 rows chip'd, harmony 6/6: `PreviewStrip` **41.9×16.4px = 2.618rem × 1em** (the φ² plate), radius 4px, 5 hard segments, `data-stops` stamped (the O-14 referent), description after — no clip at any cell | COHERENT (the chip grammar earns its T-17 seats) — the NAME-lane voice fork is §5 P9-R5 |
| **T-20** "glass-ui tabs … triggers better fill the space and not have gap — proper pilling" | @768: indicator **70.6×24.9** vs active trigger **70.5×32.9** (pill inset 4px top / 8px short); @390: **52×21.8** vs **52×27.8**; triggers themselves tile the track edge-to-edge (adjacent boxes touch/overlap ≤1.9px — no inter-trigger gap) | the PRODUCER double-trim class LIVE as booked — the standing **P4 packet** (O-8 rides W7 post-adopt); re-witnessed with live numbers, NO demo row (§5 P9-W1) |
| **T-56** t49-audit-08/09 "a pleasing pastel rainbow" | This session's pole-A extreme: stops `oklch(0.0228 0.0094 351.6) / (0.0226 0.0086 31.6) / (0.0211 0.0045 71.6)` — stem pixels light `rgb(4,4,3)` hue-spread 19–20° (monochrome), dark label **1.24:1** vs its own menu ground (frames `ramp-label-zoom`, `1440-dark-view-menu`) | the P4-B1 bracket's pole A gains a REPRODUCIBLE DARKEST witness (§5 P9-B1 annex); pastel can never emerge from a floor-only walk against a mid ambient — P9-R1 is the bracket's precondition, with P4-R1 |
| **Menus at large** (D1 chrome; W3-5 ink cure) | @mbabb menu + ⋮ menu: chrome glass `blur(13px) saturate(1.6)` light / `saturate(1.28) brightness(1.1)` dark, radius 12px, unclipped at 1440 AND 390; items stone ink 400 (`rgb(28,25,23)` / `rgb(233,230,226)`) on ~L.94/.38 glass — the A11Y-F2 cured state HOLDS | COHERENT (floating menus wear chrome; one neutral ink family) |
| **B-1 / C-5** (the w45 structural spec) | `.app-layout` grid `auto 1fr`, nav in-flow static/relative, zero z arms, designed `row-gap`; the t31-dock-band oracle rows re-attested live | CONFIRMED (the T-31 band law is real structure) |

## §5 THE ROW LEDGER (typed; anchors; zero adjective-only poles)

### P9-R1 · **LAND** — the Q5 ramp resolver walks to its near-black clamp: the owner's ONE sanctioned rainbow renders monochrome near-black (light) / 1.24:1 invisible (dark) at BOTH consume sites — the mechanism CONFIRMED (joins P4-R1; one lane)
- **Defect (A-class mechanism, not taste)**: live at the owner reference seed (and byte-stable
  across deep-link AND plain load, sampled 6× over 5s — not a frozen transition), the root
  tokens compute `--palettes-ramp-0/1/2 = oklch(0.0228 0.0094 351.6) / (0.0226 0.0086 31.6) /
  (0.0211 0.0045 71.6)` — **L ≈ the walk's own 0.02 clamp, C crushed to ≈0.005–0.009 by the
  gamut map at that depth**. Render truth: light stems `rgb(4,4,3)`, hue spread 19–20° across
  the word — **13.34:1 but MONOCHROME BLACK, no rainbow read at all**; dark label **1.24:1**
  against its own menu glass — visibly darker than every ink sibling (frame
  `1440-dark-view-menu.png`), the exact "dark/muddy" state the owner named at T-56.
- **The mechanism (probed + code-anatomy, for the lane to re-verify)**: `walkToTextFloor`
  (`palettes-ramp.ts`) solves ≥4.5:1 against `bg = OKLCHColor(bgL, 0, 0)` where `bgL` is the
  **page-ambient derived lightness** — over the owner's brick field the ambient sits ≈0.5–0.55,
  the dead band where 4.5:1 is satisfiable only at near-black, so the 12-step walk runs to the
  `clamp(…, 0.02, 0.98)` floor; **scheme-blind** (light 0.0228 / dark 0.0226 — the same
  down-walk in both), and the letterforms actually sit on menu chrome glass (dark L≈0.38) and
  the card plate — the D6 law verbatim: "the referent is a property of the surface the text
  sits on, never a global constant [or the page ambient]". P4-R1's hypothesis is hereby
  CONFIRMED with the second failure face: ambient-dependence means the palettes pass's session
  read L≈0.38 stops while this session reads L≈0.02 — one defective resolver, both states real.
- **Anchors**: `demo/@/composables/color/palettes-ramp.ts` (`walkToTextFloor` referent +
  non-convergent floor-only walk) · `demo/@/composables/boot/useViewAccents` (the `bgL` fed at
  write) · consume sites `DockViewSelect.vue:113` / `PalettesPane.vue:11` (untouched by the
  cure) · the in-tree cure idiom: `ink.ts`/`ink-walk.ts` `resolveSurfaceLightness` +
  `certifyAccentInk` (the W6.5 Lane-I precedent — per-surface, per-scheme referents).
- **Failure scenario**: any session whose derived ambient lands mid-L (the owner's own brick
  reference does) → the app's sole sanctioned rainbow is a black smudge in light and the least
  legible text in the app in dark, at both Q5 sites.
- **Constraints (binding)**: Q5's ruled FORM stands (one resolver · exactly two consume sites ·
  the 3-stop analogous fan · guarded — never re-litigated); per-scheme certified OUTPUTS from
  the one resolver are in-bounds (the P4-R1 clause). O-14's byte-identity referent re-aims in
  the same commit if the token contract changes shape. **Shared-file law: this row and P4-R1
  are ONE row of work in ONE lane** (`palettes-ramp.ts` + `useViewAccents` + `DockViewSelect.vue`
  intersect passes 4 and 9) — re-homed at the batch cut, never two writers.

### P9-R2 · **LAND** — dock expansion reflows the scene 3.0px down (T-57 consumed): the band floor is set below the expanded pill height
- **Defect (measured)**: `.dock-band { min-height: var(--dock-h) }` floors the band at the
  COLLAPSED height (65px measured) while the expanded pill computes **71.4px** → row 1 grows
  6.4px and the centered `.pane-container` shifts **+3.0px** on every idle-collapse → re-expand
  cycle (measured both schemes; the in-file comment "the band never jitters across the
  collapse↔expand morph" is falsified by the arithmetic). The action-bar layer swap is clean
  (71→71) — the defect is the collapse↔expand axis only.
- **Cure shape (lane's call, honoring the owner's reserve-or-overlay order)**: the reservation
  arm — `--dock-h` (or the band's floor) derives from the EXPANDED pill height so expansion
  renders inside a constant band (the seal centers in the same box; T-31's structural
  discipline extends to the expansion axis, never a z/margin patch).
- **Anchors**: `demo/@/styles/style.css` §487 (`.dock-band` floor + `--dock-h` token) ·
  `dock/Dock.vue` (the GlassDock seat; read-only context).
- **Failure scenario**: every collapse/expand cycle nudges the whole scene 3px — the owner saw
  it at t49-audit-09 and ordered the constraint amendment (§0.7 T-57, LAW STANDS + amendment).

### P9-R3 · **LAND** — the T-37 derive-seam guarantee is NOT in the tree: wax↔field ΔL 0.004–0.023 (light) at both tested seeds — the swatch read is carried by the producer halo, which collapses at pale seeds
- **Defect (A-class mechanism gap vs the handed cure arm)**: t33-research §6.4 named the
  idiomatic cure "a scheme-side lightness offset between wax and field — the field is DERIVED,
  so the demo owns a guaranteed delta at the derive seam (e.g. the atmosphere's `lBand` never
  equals the wax L)"; the HG1 verify record carries "derive-seam encoded". **No such mechanism
  exists in the tree** (grep: no lBand/seam guard touching the seal; `3408433` landed rim-death
  + wax-fills — both attested GREEN). Measured figure/ground at the seal: owner seed light
  **ΔL .004 / 1.03:1**, dark .016/1.06:1; pale seed (`lab(88% 5 30)`) light **ΔL .023 /
  1.08:1** — by eye the pale-light seal is a faint blob whose swatch identity rides the glyph,
  not the wax (frame `1440-light-seal-pale-zoom.png`). At the owner's brick seed the read IS
  carried — by the producer collapsed-pill glass halo, which is strong over dark fields and
  vanishes over light ones: the self-camouflage class §6.4 named is structurally alive.
- **Constraints (binding)**: Q12 ABROGATE + the register law stand — the cure is MATERIAL
  (derive-seam lightness offset / elevation), NEVER a ring or rim (O-15a's negative watch is
  the resurrection guard and stays green through any cure). The owner-witnessed shot-05 state
  (brick red) is CURED by eye — this row lands the GUARANTEE, not a re-litigation of the
  landed composition.
- **Anchors**: the atmosphere derive path (`useAtmosphere`/`useColorPipeline` — the field's
  `lBand`/stop-lightness law) · `dock/Dock.vue` seal comment block (the encoding site) ·
  read-only: `WatercolorDot` (producer).
- **Oracle blind spot flagged for the triumvirate record (NOT minted here)**: no slate leg
  measures wax↔field composited delta — O-15a is a negative border watch; the t31-band rows
  are geometry-only.

### P9-R4 · **LAND** (small · cascade-dead class) — the Login pill's Q10 live-accent voice computes to nothing
- **Defect**: `ProfileSection.vue` Login `<Button variant="outline" … class="… border-primary/30">`
  — computed `border: none 0px` (the color is applied, `oklab(0.19… / 0.3)`, but no width
  survives the variant's paint, which renders via inset highlight shadows). The Q10 ruling —
  "Tools/Login CHROME **keeps** the live accent" — is live at Tools (accent icon + wordmark
  probed) and silently DEAD at Login (stone ink, invisible border; frame
  `1440-light-login-zoom.png`). Same family as the cascade-dead `ring-primary/50` W6-7
  verified at MixSourceSelector.
- **Cure shape (lane's call)**: the accent voice re-lands on a channel the variant actually
  paints (ink/icon/inset-rim via the certified rungs), or the dead utility is excised with the
  Q10 voice consciously re-cut — never left as decorative dead code.
- **Anchor**: `demo/@/components/custom/dock/menus/ProfileSection.vue:107` (the Login Button
  class list).

### P9-R5 · **LAND** (small · voice) — the generate preset/harmony menus fork the dropdown NAME-lane voice (bare sans amid a display-voice menu family)
- **Read (typed honestly — coherence, not taste)**: the dropdown family speaks the display
  voice on its name lane — the view menu (`font-display` on the group), the letterforms menu
  (`.specimen-name font-display italic` + pin) — while the generate preset/harmony rows render
  their names in bare body sans (`{{ capitalize(p) }}`, no voice class; frame
  `1440-light-preset-menu.png`: "Vibrant/Pastel/Warm" sans beside the chips). The description
  lane (micro sans) is correct per the T-17 grammar; the NAME lane is the fork — the O-10d
  display-voice family census class at menu granularity.
- **Cure shape (lane's call)**: the name lane joins the display voice (the ColorSpaceSelector
  precedent — weight-pinned per T-40), or the menu-voice law is recorded as deliberately
  two-register (owner-visible either way in the package frames).
- **Anchors**: `demo/@/components/custom/generate/GenerateControls.vue:219-233, 248-261` ·
  read-only: `dock/DockViewSelect.vue:96` (the family precedent).

### P9-W1 · **WITNESS (producer book — no demo row)** — T-20 double-trim live at 768/390
- Indicator pill inset 4px/8px inside the active trigger box (768: 70.6×24.9 in 70.5×32.9;
  390: 52×21.8 in 52×27.8) while triggers tile the track without gap — the "proper pilling"
  owner line remains the **P4 producer packet** (`segmented-tabs.css` double-trim), O-8
  geometry congruence rides W7 post-adopt. Frames `{768,390}-{L,D}-tabs-zoom.png`. Re-cited,
  not re-filed; no demo cascade arms-race (D7's prohibition).

### P9-W2 · **PRODUCER-FORENSIC ANSWERED (T-52) — letter-row candidate (the owner's routing question)**
- **The root (both schemes)**: the first dock control sits with ZERO inline slack against the
  producer clip stack — trigger left edge ≡ `.dock-layer--full` clip edge (484.8px),
  `overflow: auto/hidden` + a degenerate 0px-fade `mask-image` (`.dock-layers` `hidden/auto`,
  same box). Consequences: (a) rest — the shared-edge antialias/rim smoothing is shaved (the
  owner's "clipped slightly"; the rest ring itself is a 0.75px INSET ::before, unclippable);
  (b) focus — the OUTER `0 0 0 2px` accent ring + 8px glow is structurally halved at both
  inline edges; (c) any future hover capsule/lift shadow on edge controls dies the same way
  (the exact class W6-8 cured for Tools INSIDE the demo box).
- **The relay (GLASSUI-T-ASKS rider candidate)**: an inline edge-inset / `overflow-clip-margin`
  door on the dock layer scroller (its mask already has fade stops minted at 0px — the door is
  half-built), or a documented outer-paint budget for first/last dock controls.
- **The demo interim (named, not authored)**: the T-36 idiom — `margin-inline` breathing room
  on the edge controls (`DockViewSelect` trigger / `ProfileSection` end controls).
- Frames: `1440-{L,D}-t52-{gradient-trigger,trigger-hover,trigger-focus}-zoom.png`.

### P9-B1 · **BRACKET ANNEX (to P4-B1 / T-56 — not a new bracket)** — the ramp register's pole A gains its darkest reproducible witness
- Pole A (this session, the mechanism's worst face): stops
  `oklch(0.0228 0.0094 351.6) / (0.0226 0.0086 31.6) / (0.0211 0.0045 71.6)` @ product tree
  `5e4f1f6`, owner seed, both schemes — monochrome near-black; frames
  `1440-{light,dark}-ramp-label-zoom.png`. P4-B1's pole A (L≈0.384 maroon) and this tuple are
  the SAME resolver at two ambients — the bracket's precondition row is P9-R1 ≡ P4-R1 (an
  unguarded register cannot be judged; the owner rules the pastel register inside the honest
  guard).

## §6 Routing + loop state

- **Zero rows contradict a §12 ruling or a §4 retirement; nothing routes OWNER**: Q5's form is
  preserved (P9-R1 fixes the guard, never the fan); Q12/the register law are honored by P9-R3's
  material-only constraint; T-31 stands (P9-R2 lands the owner's OWN §0.7 amendment); Q10 is
  restored TO the ruling by P9-R4; the §4-retired specs stay retired (the W7-4 legend, the
  die-rim, and the seal-rim stay dead — attested, not restored).
- **remediation_1 shared-file map (single-writer keyed on the FILE)**: P9-R1 anchors
  `palettes-ramp.ts` + `useViewAccents` + (consume-verify) `DockViewSelect.vue` — **the same
  anchors as P4-R1/P4-R4: these re-home to ONE lane at the batch cut** (T.W8.md §Scope-2).
  P9-R2 anchors `style.css` (the band section) — also the picker-pass file class; check the
  batch cut. P9-R3 anchors the atmosphere derive path (`useColorPipeline` — SHARED with the
  boot/atmosphere surface: pass-2's lane must not co-write it). P9-R4 anchors
  `ProfileSection.vue` (dock-only). P9-R5 anchors `GenerateControls.vue` (the mix-generate
  pass's file class — one lane). P9-W2 routes to the **letter/inbox** (producer), demo interim
  rides whichever lane owns the dock edge controls. P9-B1 is a PACKAGE row.
- **Loop**: this is pass_1. Rows P9-R1..R5 feed remediation_1; pass_2 re-judges the landed
  state; ≤3 rounds then residual routes by class.
- **EXPECTED-RED context untouched**: O-16-R1 / O-5 / O-26 born-RED legs ride with their packet
  cites; the P4 (tabs) + P10 (weight token) + P3-family books stand as books — re-cited, not
  re-filed; GAP-L2 (the dark ambient light-band) is a standing P1 book that P9-R1's referent
  cure must not paper over (the surface referent makes the guard independent of it — the honest
  cure direction).
- **Oracle blind spots flagged for the triumvirate record (bounds expansion is not a pass's
  call)**: (1) wax↔field composited figure/ground at the seal (P9-R3 — O-15a is a negative
  watch only); (2) the ramp's composited contrast (P4 flagged; re-cited — O-14 byte-identity
  cannot see garbage tokens); (3) trigger-icon-on-pill graphics contrast (the `--accent-view`
  icon over the dark pill glass — the O-18 graphics leg covers only the Extract instrument
  today; recorded, not minted).

## §7 Probe evidence (values of record)

| Probe | 1440 L | 1440 D | 768 L | 768 D | 390 L | 390 D |
|---|---|---|---|---|---|---|
| console errors (legs 1–2, all states) | 0 | 0 | 0 | 0 | 0 | 0 |
| Tools box / padding / margin-inline / radius | 121×40 / 8px 12px / 4px / 9999px | same | — | — | — | — |
| `.action-bar-toggle-inner` overflow at settled rest | visible | visible | — | — | — | — |
| band h: collapsed → expanded / scene Δ | 65 → 71.4 / **+3.0px** | same | — | — | — | — |
| action-bar layer swap band h / scene Δ | 71→71 / 0 | same | — | — | — | — |
| seal `border-style` (O-15a) | none | none | — | — | — | — |
| T-37 wax↔field ΔL / WCAG @ owner seed | **.004 / 1.03:1** | .016 / 1.06:1 | — | — | — | — |
| T-37 @ pale `lab(88% 5 30)` | **.023 / 1.08:1** | .112 / 1.48:1 | — | — | — | — |
| view menu: options / weights / ramp rows | 7 / 400×6 + **600 current** / 1 (Palettes) | same | no-clip ✓ | no-clip ✓ | no-clip ✓ | no-clip ✓ |
| ramp tokens L (0/1/2), stable 5s + plain load | **.0228/.0226/.0211** | same (C ±.0008) | — | — | — | — |
| ramp label: stem rgb / hue spread / vs ground | rgb(4,4,3) / 19–20° / 13.34:1 (mono) | **1.24:1** | — | — | — | — |
| letterforms menu: options / weights / ramps | 18 / {400} / 0 | 18 / {400} / 0 | — | — | — | — |
| T-17 chips: preset · harmony rows chip'd / size | 10/10 · 6/6 / 41.9×16.4px r4 | same | — | — | — | — |
| T-20 indicator vs active btn (w×h) | — | — | 70.6×24.9 vs 70.5×32.9 | same | 52×21.8 vs 52×27.8 | same |
| T-52: trigger.left − layer-clip.left / mask | **0.0px** / 0px-fade mask | same | — | — | — | — |
| T-52 focus outer ring | accent 2px + 8px glow (shaved) | same | — | — | — | — |
| @mbabb / ⋮ menu material · clipped | blur13 sat1.6 r12 · false | blur13 sat1.28 br1.1 · false | — | — | 214.6w · false | same |
| Login: computed border / ink | **none 0px** (color set, width 0) / stone | same | — | — | — | — |

*(— = not probed in that cell; the frame set covers every cell visually. Contrast/ΔL numbers
are screenshot-composited pixel samples at dpr2 — the honest ground, not the alpha-blind
computed approximation. The collapse timer arms on dock enter/leave — the leg-3b isolation;
recorded so no future probe misreads "never collapses" as a defect.)*
