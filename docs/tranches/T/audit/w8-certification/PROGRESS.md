# T.W8 — PROGRESS (the assembler's running record)

**Agent**: the non-authoring terminal-assembler (Fable / frontend-design). Authored NO critique
pass and NO remediation row this wave.
**Wave head at assembly**: `6d95871` (`tranche-t`), 2026-07-12.

## Timeline

1. **Merge the un-merged WRITING-lane worktrees** (the FIRST assembler act, committed-green only,
   --no-ff): the 6 remediation lanes (`worktree-wf_00142843-e53-14..19`) merged onto `tranche-t`.
   Each lane touches a disjoint file set; none touches a forbidden path. Merge commits
   `83d8714` → `41ba7c9` → `1e74cf3` → `18f8263` → `f6c595f` → `6d95871`. All 6 branches now
   0-unique. Record: `ROWS.md §0`.
2. **Post-merge suite verification** at `6d95871`: `npm run lint` **0** · `npm run typecheck`
   **0** · `npm test` **74 files / 2241 tests PASS** · playwright smoke (6 projects, lane-unique
   ports 8490/8491). The smoke run surfaced **2 O-10 title-lock legs RED** — a cross-lane
   integration gap: WR-4/T-51's owner-mandated title step-down (`5f273ae`, `--type-display-3` →
   `--type-display-2`) left the O-10 oracle asserting the superseded token (the "O-10 line-locks
   re-run" WR-4's rides column owed). Completed the owed re-aim (`b551755`, e2e-scoped, separate
   from the docs commit — tighten-not-weaken, the O-14-slim idiom, NOT a gate weakening); the
   full smoke slate is GREEN after. Recorded honestly in `PACKAGE.md §5` — no red hidden.
3. **Read the 11 pass journals** + the checkpoint `ROWS.md` + the §0.7 WR roster + the LAW block
   (`RATIFICATION §0`, `MANDATE §0/§0.1–0.8`, `SYNTHESIS D1–D10`, `waves/T.W8.md`) + `t33-research
   §6.6` (T-42) + `t49-research` + `pi1-delta-ledger.md` (PI-1). Did NOT re-critique.
4. **Inventory the frames**: the boot screencast (4 arms, 1543 frames), the T-48 motion captures
   (8 hops), the per-surface AFTER frames, the owner BEFORE roster.
5. **Assemble the package** into `docs/tranches/T/audit/w8-certification/`: `PACKAGE.md` ·
   `ROWS.md` · `VERDICT-2026-07-12.md` · `PROGRESS.md`; appended the W8 row to `pi1-delta-ledger.md`.
6. **ONE docs-only, path-scoped commit** + push. The tool-artefact grep binds pre-commit.

## Deliverables

| File | What |
|---|---|
| `PACKAGE.md` | the frames index · the bracket table (20 axes + 2 non-rows) · the owner-line index · the boot screencast path · the honest-red ledger · the Q14 material · the hard-gate checklist |
| `ROWS.md` | the remediation ledger: every pass's rows + WR rows + checkpoint brackets → landed commit / booked-to / routed-to; zero drops |
| `VERDICT-2026-07-12.md` | the stub the owner's ruling lands into, verbatim |
| `PROGRESS.md` | this record |

## The honest state

- **Defects landed**: 24 LAND rows across 6 lanes; 9 of 11 §0.7 WR rows landed (WR-7 → owner
  bracket, WR-10 → W7/W9 by law with its wake-gray half landed).
- **Brackets for the owner**: 20 taste axes, every pole a named reproducible state.
- **Honest-red carried**: O-16-R1 (→W7) · O-5 (→W7) · O-26 headless (headed GREEN → W9); the
  §6.2 LCP/TBT budgets honestly RED (Q14 → W7/W9); no gate weakened.
- **The gate is the owner.** The package is delivered; **the wave stays OPEN until the owner
  rules** — no proxy sign-off. Do NOT run the gate or the close (the harness owns those).
