# T.W8 — THE OWNER CERTIFICATION PACKAGE (ONE, tranche-level)

**Assembled**: 2026-07-12 by the **non-authoring terminal-assembler agent** (Fable /
frontend-design; authored NO critique pass and NO remediation row this wave — the §Recovery
non-authoring law binds). At the merged wave head `6d95871` (`tranche-t`).

**This package CERTIFIES NOTHING.** It PRESENTS the landed W8 state for the owner's eye. **The
owner's verdict IS the gate** (`T.W8.md §Hard gate` row 6): the wave closes on the owner's ruling
over the brackets — approve the landed set wholesale, rule per-axis inside a bracket, or file
further rows. **A package delivered but unruled leaves the wave OPEN, honestly** — never a proxy
sign-off. The owner's ruling lands verbatim into `VERDICT-2026-07-12.md`.

**What is bound** (the LAW block, every pass and this assembler): `RATIFICATION-2026-07-09 §0`
→ `MANDATE-2026-07-06 §0/§0.1–0.8` (owner verbatim WINS) → `SYNTHESIS.md §2` D1–D10 →
`waves/T.W8.md`. The remediation ledger is `ROWS.md`; the per-pass journals are `passes/`.

---

## §0 What the owner rules (the one-page map)

1. **The brackets** (§2 below) — every taste axis presented `poleA ← target → poleB`, each pole
   a NAMED reproducible state, each indexed to the owner-verbatim line it answers (§3), each
   carrying its **LANDED default**. Rule per-axis OR **bulk-approve the landed set** ("approve
   all landed defaults").
2. **The Q14 adjudication material** (§6) — the LCP/TBT record + the budget/preset pairing
   decision, WITH the owner at W8/W9. Present, never pre-decided (the owner's own *"performance
   ABOVE ALL"*, E-3).
3. **The honest-red ledger** (§5) — the full oracle slate O-1..O-26, green or honest
   EXPECTED-RED with packet cites (no red weakened, no red hidden).

The defects are already **landed** (24 LAND rows across 6 lanes; `ROWS.md`). The owner is not
asked to re-judge defects — only to rule the taste brackets and the Q14 pairing.

---

## §1 The frames index (annotated AFTER frames · both schemes · 3 viewports · beside the owner's BEFORE shots)

**Convention** (standing): the AFTER PNG series live gitignored-by-class on-disk under
`docs/tranches/T/audit/pi/w8/<surface>/` (the assembler and the owner read them from disk); the
probe instruments + logs are committed. The BEFORE panels are the **owner's own shots** at
`docs/tranches/T/audit/owner-screenshots/`. Each surface's AFTER frames were shot at **dpr 2, both
schemes, 3 viewports (1440 / 768 / 390)** on the frozen product tree `5e4f1f6` (the tree the owner
audited at §0.6/§0.7); the landed remediation is now merged at `6d95871` — the pass-2 re-judge
frames are the W9 walk's obligation.

| Surface | AFTER frames (`pi/w8/<surface>/`) | Owner BEFORE shots (`owner-screenshots/`) |
|---|---|---|
| **picker** | `picker/` (28) — `{1440,768,390}-{light,dark}-{full,console,readout}.png` + `1440-light-{console-active,console-alpha,dropdown,clamp999,seam,rail-closeup,raildot-zoom,alphathumb-ring-zoom}.png` + `1440-{light,dark}-shrunk-header.png` | the §0 picker shots (`t-2000-*`..`t-2010-*`); T-42 → `t61-audit-01-17-45-22.png` |
| **boot / atmosphere / blob** | **the boot screencast** `boot/{cold,returning}-{light,dark}/f<offset>ms.png` (§4) · `blob/seat-{light,dark}-dpr2.png` · `field/neutral-{light,dark}.png` · `config/*.png` (7) · **the T-48 motion captures** `motion/<scheme>-<hop>/` (8 hops) | `t-30-blob-blurry-topright.png` (§0.5) |
| **about** | `about/` (39) — `about-{1440,768,390}-{light,dark}-{top,guide,katex,code,scrolled}.png` + `1440-{light,dark}-{katex-crop,inline-code-crop}.png` + interaction frames | the §0 About card shots (`t-2001-51`, `t-2005-53`, the twice-flagged card) |
| **palettes / browse** | `palettes/` (39) — `{1440,768,390}-{light,dark}-{mypalettes-empty,browse-empty,browse-wall}.png` + `1440-*-{browse-developing,browse-error,card-expanded,card-menu,ramp-dropdown,ramp-title-zoom}.png` | `t33-audit-07/08/12` (T-41) |
| **extract** | `extract/` (21) — `{1440,768,390}-{light,dark}-{full,shadow,controls}.png` + `1440-light-{livek6,developed,reset}.png` | `t33-audit-11` (T-44) |
| **mix / generate** | `mix-generate/` (81) — `built-{mix,generate}-{1440,390}-{light,dark}-{full,corner-*,top-edge}.png` + `dev-*` sweep + `motion/` swap series | `t33-audit-12/13` (T-45/T-41); `t49-audit-05/06/07` (T-53/T-54/T-55) |
| **gradient** | `gradient/` (22) — `dev-{1440,768,390}-{light,dark}-{full,instrument}.png` + `dev-1440-*-{hover-ghost,three-stops,slice-pinned,conic-tile}.png` + `built-1440-*-full.png` | `t33-audit-14` (T-46/T-6/T-21) |
| **easing** | `easing/` (8) — `{1440,768,390}-{light,dark}-bench.png` + `1440-light-{drag,steps}.png` | `t33-audit-15` (T-47); `t-2009-51` (T-22) |
| **dock / nav / menus** | `dock/` (50) — `{1440,768,390}-{light,dark}-dock-rest.png` + the menu-voice census + `1440-*-{seal-zoom,seal-pale-zoom,ramp-label-zoom,t52-*-zoom,login-zoom,dock-collapsed}.png` + `{768,390}-*-tabs-zoom.png` | `t33-audit-04/05/06` (T-36/T-37/T-40); `t49-audit-04/08/09` (T-52/T-56/T-57); `t-31-dock-occluded-by-card.png` (§0.5) |
| **admin** | `admin/` (58) — `{users,names,audit,flagged,tags}-{1440,768,390}-{light,dark}-populated.png` + `*-1440-*-{empty,error}.png` + `users-1440-*-loading.png` + `dock-admin-*` · `mbabb-menu-*` · `dock-loggedout-*` | *(no admin-named owner shot — judged critique-fresh; G-11)* |
| **micro-chrome / error** | `microchrome/` (6) — `1440-{light,dark}-{selection,error-plate}.png` + `1440x420-{light,dark}-scrollbar.png` | *(the declined-ledger error surface, G-8 / VERDICT §5-D3 — no owner shot)* |

**The owner BEFORE roster** (per the §1.0 corrected map): the **23 §0–§0.4 shots** (`t-19xx`/`t-20xx`/`t-2210`/`t-2211`) + the **2 §0.5 shots** (`t-30-blob-blurry-topright.png` · `t-31-dock-occluded-by-card.png`) + the **15 §0.6 shots** (`t33-audit-01..15`) + the **11 §0.7 shots** (`t49-audit-01..11`) + the **T-61 shot** (`t61-audit-01`). All on disk at `owner-screenshots/`.

---

## §2 THE BRACKET TABLE (every taste axis a BRACKET — the owner rules; NEVER self-ruled)

**The T-26 template** (t-prompts-recap F2's law — *brackets, never points*): each axis reads
`poleA ← target → poleB` with **each pole a NAMED reproducible state** (a value tuple / commit /
frame path — an adjective-only pole is a defective row, RF-3/4/5/6). Each axis is **indexed to the
owner-verbatim line it answers** (§3) and carries its **LANDED default** + a **bulk-approve verb**.
The roster is SOURCED: the §12 taste rows + the wave-gate eye-judge clauses + the pass-minted axes
+ the §0.7 roster + the 7 checkpoint BOOK-W8 brackets.

> **Bulk-approve** (the whole set): *"Approve all landed defaults."* Then only the un-landed
> brackets (T-42 register · T-48 residual · T-55 instrument · pastel register · the whisper /
> 4th-cell / blob-seat / boot-target axes) remain for a per-axis word.

### Group A — the §0.6 + checkpoint brackets

| # | Axis (owner line) | Pole A ← **target** → Pole B (named reproducible states) | Landed default |
|---|---|---|---|
| **B-01** | **T-42 shrunken scrolled header** (T-42 "shrunken state totally wrong" + **T-61 §0.8** "the header padding/background should ALSO shrink on scroll — not just the title") | **compositor-only title `scale(0.618)` over an UNSHRUNKEN 136.6px box, veil 1.0** (frames `picker/1440-{light,dark}-shrunk-header.png`) ← **whole-header contraction — T-61 RULES this direction** (padding + background + title condense as ONE strip; honest arm = a designed SHORT stuck strip with the veil range re-keyed, t33-research §6.6) → the **P3 `ScrollCardHeader` producer knobs** (producer-owned arm) | the CURRENT compositor-only scale (Pole A) — the remaining bracket is the **REGISTER** of the contraction, not its existence |
| **B-02** | **T-48 card-transition motion residual** (T-14/T-48/**T-58** "janky, not smooth") | **frozen-mid-pose** — light Browse→Home holds a 356ms compositor hole (frames `motion/light-browse-to-home/f00648ms.png` → `f01004ms.png`) ← **hole-free swap** (no compositor gap > ~2 frame budgets) → **over-deferred** (incoming pane arrives visibly blank/late). The payload MOVER stays **W7's BY LAW** (WR-10/T-58 MANDATE; O-16-R1 the named confound); the §4-attributed janky boot-walls are NEVER re-bracketed as taste; the wake-gray compound half landed (`5cecdf2`) | the current swap; W9 re-verifies after W7's payload levers land |
| **B-03** | **R3 / T-26 sage-whisper floor** (D4 "a sage whisper, never silence") | **silence** — `vividness = smoothstep(0.02,0.10,seedC)` ⇒ vividness(0)=0, band C≈0.004 (frames `field/neutral-{light,dark}.png`) ← **a perceptible sage whisper** (field C≈0.02–0.04 at seedC=0, `boot/atmosphere-calibration.ts`) → **marigold** (injected chroma reading as a color lie on a gray pick) | silence (the CURRENT state) |
| **B-04** | **R5 / C-4 rail α face** (rail α legibility + the self-cut tnum face) | **Fraunces-fallback Greek** — U+03B1 reads as a Latin-`a` twin, rail `L a b a` (the pre-cure state) ← **distinct glyph — Fira Code open Greek α upright** (LANDED `e2e8517`; rail `L a b α`; frame `picker/1440-light-rail-closeup.png`) → **donor-Greek subset INTO the self-cut Fraunces face** (ONE voice; `scripts/fonts/build-fraunces-tnum.py` precedent). **C-4 rider**: the self-cut Fraunces-tnum face itself — a house-cut artifact the owner has never seen named (*the flag IS the deliverable*) | the Fira-Greek distinct glyph (Pole target, landed) |
| **B-05** | **R7 / C-3 / P1-B3 blob-seat positive domain** (T-30/T-8 "more towards the centre, deftly integrated") | **corner-flush** `--blob-seat: 0` — Q3 "Flush.", bead crisp post-R2, distTop/Right 0 at all 6 cells (frames `blob/seat-{light,dark}-dpr2.png`; BEFORE `t-30-blob-blurry-topright.png`) ← **centre-ward** (candidate `--blob-seat: 0.5rem` / `1rem`) → **over-inset** (bead crowding the title/readout; containment bounds orbit-reach ≤0.5). **Q3 "Flush." STANDS untouched** — the positive domain is the sanctioned NEW axis (T-30 §0.5) | corner-flush 0 (the RULED Q3 default) |
| **B-06** | **R8 / Q2 boot-settle** (T-1/T-27 boot; "full comp.") | **crisp-but-late** — measured 1732–2059ms (built + real GPU) vs target (boot screencast `boot/*` vs W2 baseline `pi/w2/screencast/`) ← **the ratified [1.4, 1.7]s** → **too-fast smear** (the pre-T ~1.0s five-clock boot the owner rejected as T-1/T-27). The payload lever (b2-start eager-mount wall) is **W7's BY NAME** (RP-2/L20/GAP-L5; Q14) — the owner rules the TARGET, not the lever | the crisp-but-late cadence; W7's levers move it |
| **B-07** | **C-2 / P1-B2 header 4th-alpha cell** (T-7 "numbers ... x, y, z, or more if needed") | **3-cell tuple** — a live non-1 α speaks ONLY through the console meter C-1 (commit `5b79cb7`; frames `picker/1440-light-{full,console-alpha}.png`) ← **target** → **the 4th α cell joins the tuple** — NAMED COST: silently re-derives the O-10b line-lock tables + the Q11b capacity derivation (ratified locks). **BY CONSTRUCTION — a light remediation is forbidden** | 3-cell (Pole A) |

### Group B — the §0.7 roster brackets (poles from `t49-research.md`)

| # | Axis (owner line) | Pole A ← **target** → Pole B | Landed default |
|---|---|---|---|
| **B-08** | **blob-size** (T-49b "a bit bigger") | current **`22cqi`/`11rem` ⌀≈92px desktop** ← **"a bit bigger"** (ceiling 12–12.5rem + 24cqi, ⌀≈100–105) → **over-grown** (>~12.5rem outgrows the title line, adds header height via the 0.76 seat coupling). Re-derives against WR-4's chosen title line — the two knobs interact | current `11rem` (un-landed — bracket) |
| **B-09** | **fission-amp** (T-49c meatball split) | **0 / never-splits** (the pre-WR-2 state) ← **calm-effective [0.16, 0.32]** (WR-2 LANDED `5cecdf2` `heroConfig.surface.fissionAmp` + park runway ≥ one beat) → **busy colony** ([0.4, 0.8] over-active) | the landed calm value (WR-2) |
| **B-10** | **veil-clarity** (T-50 the veil should read as glass) | landed **opaque** (pre-WR-3 α 0.52/0.678, ~15% surviving variation) ← **visible glass — the field legibly moving under the console** (WR-3 LANDED `a8dc32a`, α ~0.44/0.56) → **too-clear** (the O-18 GRAPHICS leg is the far fence) | the WR-3 visible-glass α |
| **B-11** | **title-step** (T-51 "a bit smaller, down by 1 or 2") | pre-cure **`--type-display-3` 67.78px** ← **down ONE — `--type-display-2` 53.28px** (WR-4 LANDED `5f273ae`) → down TWO — **`--type-display-1` 41.89px** | down-one `--type-display-2` (landed) |
| **B-12** | **dark-caster register** (T-53 top-border artifact) | the **inverted cream pop** `color(srgb 0.914 0.902 0.886 / 0.5) 8px 8px 0` (pre-cure; frame `mix-generate/built-generate-1440-dark-corner-tr.png` + owner `t49-audit-05`) ← **scheme-true shadow** (P6-R1/WR-5 LANDED `31e7c09`, `--shadow-color` pinned to the stone form in `.dark`) → **no caster in dark** (the scheme-asymmetric register cost) | the scheme-true shadow (landed) |
| **B-13** | **verb-cluster instrument** (T-55 "glass-ui dropdown or dock set") | **three registers in one row** — `primary-audacious` pill + two bare ghost glyphs (CURRENT; frame `mix-generate/dev-generate-1440-dark-plate-after-regen.png` + owner `t49-audit-07`) ← **ONE glass-ui instrument** → the owner's OWN disjunction: **dock-set capsule** (DockSection/DockStack/DockIconButton) ↔ **`ui/dropdown-menu`** (all three verbs are immediate plate actions). **NOT approximated by any lane** (WR-7; E-2) | the CURRENT three-register row (un-landed — the owner's instrument word chooses) |
| **B-14** | **pastel band centers** (T-56 "a pleasing pastel rainbow") | the **near-black wreck** — light rgb(4,4,3) monochrome / dark 1.24:1 (the P9-B1 darkest witness; frames `dock/1440-{light,dark}-ramp-label-zoom.png`) ← **the §8 closed-form bands** (dark pastel L∈[0.80,0.88]/C∈[0.06,0.10]; light chalk L∈[0.30,0.40]) — P4-R1/WR-8 LANDED `ccf4b30` the honest guard (dark `oklch(0.8455 0.1386 329.8)`) → **over-sweet** (C>~0.12 candy / L>0.9 dies into the light card). **P4-R1's cure is the PRECONDITION** (an unguarded register cannot be judged) | the landed honest-guard pastel (the register inside it is the owner's call) |
| **B-15** | **rhythm clamp constants** (T-59 — BOTH poles owner-photographed) | **shot-10 desktop 24px rows** ← **the ONE container-scaled law** `clamp(2rem, 7cqi, 2.625rem)`-class (WR-11 LANDED `a8dc32a` + `bdd0a9c`; 44px touch rung preserved by hit-area extension) → **shot-11 mobile 44px visual rows** | the landed one-law clamp |

### Group C — the pass-minted brackets

| # | Axis (owner line) | Pole A ← **target** → Pole B | Landed default |
|---|---|---|---|
| **B-16** | **AB-5 content-heading weight** (T-2 "both should be non-bold" — does it extend into the body?) | **calm-uniform** — body h2/h3 join `--type-weight-display` 400 (hierarchy by size + accent ink) ← **target** → **shipped sectioning** — the 600 register at `5e4f1f6` (frames `about/about-1440-{light,dark}-guide.png`). The owner's non-bold lines named TITLES and OPTIONS, never content headings | the shipped 600 (un-landed — bracket) |
| **B-17** | **E1-B1 extract track-voice** (T-44a "un-readable" sliders) | **certified-neutral quiet** — `var(--ink-muted)` fill, live-pick reserved to thumb/range (C3-stricter; the no-pick fallback path) ← **target** → **certified live-accent loud** — full-width live-pick ink (light rgb(162 29 0) / dark rgb(255 131 106); commit `c2a911f`; frames `extract/1440-{light,dark}-full.png`). Both poles clear 3:1 | the live-accent fill (landed) |
| **B-18** | **P7-B1 gradient hatch-dominance** (T-46 "un-usable" taste half) | `AXIS_HEADROOM = 1.15` → default axis **C ≤ 0.4** (majority-hatch at hue-wide sweeps; `envelopePlatePaint.ts:46-48`; frames `gradient/dev-1440-{light,dark}-instrument.png`) ← **target** → `AXIS_HEADROOM = 1.0` → **C ≤ 0.35** (netting thins ~⅓; solid+belt grows; cusp tick rides the right margin) | `1.15` (landed) |
| **B-19** | **P8-B1 easing authoring-canvas** (T-47 "too small" / "more compactly") | `min(100%, 19rem)` · `margin-inline: 0` → **304px left-aligned, 119px right gutter** (`EasingAuthoringStage.vue:118,121`; frame `easing/1440-light-bench.png`) ← **`margin-inline: auto` → 304px centered** → **drop the 19rem cap → canvas = plate width ~410px** (fill; tracks pane width). Three arms | the 19rem left-aligned (landed) |
| **B-20** | **A-1 loading-skeleton register** (T-41 "shimmer properly as a proper skeleton") | **breath** — `variant="breath"`, 6s opacity swell 0.55→0.95 ("known-imminent, the surface is alive"; `AdminListSkeleton.vue:13-18`) ← **target** → **shimmer** — `variant="shimmer"`, the 1.5s traveling gradient sweep ("watch the bar load"). App-wide (one recipe; the shared PaletteCardSkeleton) | breath (landed) |

### The 2 flagged non-row items (presented for the owner's eye — NOT brackets-for-ruling)

- **Rest-floor in-bracket read** — the bead/plate INK_FLOOR 0.15 ∈ [0.12, 0.20] reads
  present-but-modest in light (pink-on-pink, floored), strong in dark; the dpr2 crops
  (`blob/seat-*-dpr2.png`) are the eye-material. Stays an in-bracket default; the F9.R1 producer
  knob swap is booked at W7.
- **Dark-scheme field/chrome split (GAP-L2)** — settled dark = dark cocoa cards on a BRIGHT
  salmon/pink field (the L2 `lBand` producer atom absent). Standing **P1** producer book, re-cited
  with frames; **no demo action (PR-2 fence)**. The P9-R1 ramp referent cure deliberately makes
  the guard independent of it (the honest direction).

---

## §3 The owner-line index (owner-verbatim → the bracket/row that answers it)

Each owner line (MANDATE §0/§0.1–0.8) mapped to the package row that answers it. Landed lines
carry their commit; bracketed lines carry their §2 axis.

| Owner line (verbatim, abbreviated) | Answered by |
|---|---|
| **T-1 / T-27** boot "full comp." / not a five-clock smear | **B-06** (Q2 boot-settle); the payload lever W7 |
| **T-2** "Lab title ×1.5 golden, non-bold" | LANDED (Q11a ×φ, weight 400; picker + about); extends to **B-16** (content-heading weight) |
| **T-5** "vertical dock-like ring encapsulating the letter column" | LANDED + **P1-R1** (`e2e8517`, the active seat cure) |
| **T-7** "numbers 1.5× golden, contiguous x, y, z ... or more if needed" | LANDED (readout tuple) + **B-07** (4th-alpha cell) |
| **T-8 / T-30** blob "more towards the centre, deftly integrated" | **B-05** (blob-seat) + **B-08** (blob-size); Q3 "Flush." stands |
| **T-22 / T-47** easing "a mess / broken, too small, redesign from kf" | does NOT reproduce (Row-E seat); residual **B-19** (canvas width) + P8-R1/R2 landed |
| **T-26** living field / D4 "sage whisper, never silence" | **B-03** (whisper floor); O-26 headed GREEN |
| **T-28** WatercolorDot outline "too fine or ABROGATE" | LANDED (ABROGATE holds; rides P1-R1 + WR-6) |
| **T-33** "gap between numbers and gradient selector ... 999" | LANDED (`ad301e7`); seam residual **B-01**-adjacent (P1-B1 collapsed → owner) |
| **T-36 / T-29** dock trigger "true button / clipped dropdown" | LANDED (`c237d24`, W6-8); T-52 clip forensic → **P9-W2** letter |
| **T-37** "cannot see the collapsed dock swatch color" | LANDED **P9-R3** (`cca4852`, derive-seam guarantee) |
| **T-40** "dropdown options not bold" | LANDED (weight 400 both schemes; P4-R4/`ccf4b30` retires the current-entry 600) |
| **T-41** shadow palettes / "bring back the dashes" / "shimmer properly" | LANDED (trio+dashes, zero fillers; O-9 5/5); loading register **B-20** |
| **T-42** "shrunken state totally wrong" | **B-01** (T-61 rules the direction) |
| **T-44** "sliders un-readable / redesign the shadow palette" | LANDED **E1-R1/R2/R3** (`1705345`); track-voice **B-17** |
| **T-45 / T-53** "clipping artifacts on card borders" | **P6-V1** blur-smear VERIFIED-CURED; caster **P6-R1**/`31e7c09` + **B-12** register |
| **T-46 / T-6 / T-21** gradient "un-usable / netting / bar too short" | does NOT reproduce (W6-2); **P7-R1/R2** landed; **B-18** hatch-dominance |
| **T-48 / T-58** card transitions "janky" | **B-02** (residual); payload W7; wake-gray landed (boot-B) |
| **T-49a / T-49b / T-49c** blob emerge / size / fission | **WR-1** landed (`5cecdf2`), **B-08** size, **B-09**/**WR-2** fission |
| **T-50** veil clarity | **WR-3** landed (`a8dc32a`); **B-10** |
| **T-51** "'lab' text a bit smaller, down 1 or 2" | **WR-4** landed (`5f273ae`); **B-11** |
| **T-52** "left/right edges clipped in the dock — relay?" | **P9-W2** producer letter (the routing question ANSWERED) |
| **T-54** "not proper watercolor dots" (Generate) | **WR-6** landed (`ccf4b30`, WatercolorDot) |
| **T-55** "redesign the button controls ... one glass-ui instrument" | **B-13** / **WR-7** (owner's instrument word) |
| **T-56** "'Palettes' a pleasing pastel rainbow" | **P4-R1/P9-R1/WR-8** landed (`ccf4b30`); register **B-14** |
| **T-57** "dock expand should NOT shift the UI down" | **P9-R2/WR-9** landed (`31e7c09` + `2c1a0f4` invariance probe) |
| **T-59** rhythm 24px desktop / 44px mobile | **WR-11** landed (`a8dc32a` + `bdd0a9c`); **B-15** |
| **T-61 §0.8** "header padding/background should ALSO shrink" | **B-01** (rules the whole-header-contraction direction) |
| **T-24 / T-3 / T-11 / T-18** neutral consistency / cartoon card | LANDED (D1 ladder; AB-2/AB-3 `f0ef927`; P11-R1 selection ink); GAP-L2 non-row |

---

## §4 The boot screencast (THE PACKAGE DELIVERABLE — §Hard gate row 2)

**Path**: `docs/tranches/T/audit/pi/w8/boot/{cold,returning}-{light,dark}/f<offset>ms.png`

Compositor-true CDP frame series on the **BUILT bundle**, headed real GPU (ANGLE Metal, M5 Max),
**cold + returning × light + dark** — **1543 frames** (cold-light 383 · cold-dark 392 ·
returning-light 411 · returning-dark 357). Offsets sit on the page's `timeOrigin` clock, so frames
pair 1:1 with the overture beat marks in `pi/w8/w8-pass2-probe-log.txt`. The screencast is the
before/after material for **B-06** (the Q2 boot-settle bracket) beside the W2 baseline
`pi/w2/screencast/`.

**The T-48 mid-flight captures** (the frame-by-frame evidence class for **B-02**):
`pi/w8/motion/<scheme>-<hop>/` — 8 real pane swaps (Home→Gradient→Mix→Browse→Home × both schemes);
the freeze pair of record is `light-browse-to-home/f00648ms.png` → `f01004ms.png` (a 356ms held
pose).

**Instrument honesty note** (for the owner reading the boot frames): the `:8611` static perf
instrument has no `VITE_API_URL`, so the *designed* dev-misconfig register fires (1 console.error +
the lamp; Browse renders "The commons is unreachable") — a LOOPBACK-ONLY designed state, never
prod. Read the lamp in the boot frames as instrument, not product.

---

## §5 THE HONEST-RED LEDGER (§Hard gate rows 3+4 — no red weakened, no red hidden)

**The full oracle slate O-1..O-26** — status drawn from each pass's re-run at the frozen tree
`5e4f1f6` + the lane-added born-RED legs + the merged-head (`6d95871`) confirming slate run. The
three standing born-RED legs are carried VERBATIM with their packet cites.

### The standing born-RED legs (EXPECTED-RED — `test.fail()`-designed; cites intact)

| Leg | State | Cite / route |
|---|---|---|
| **O-16-R1** (the 150ms clobber) | EXPECTED-RED — the producer transition-duration clobber is LIVE in the shipped dist (PKT-1 unlanded); the KNOWN CONFOUND to isolate FIRST before any felt-duration retune | **PKT-1 → W7** (communiqué `f3f3c097`); WR-10/T-58 names it |
| **O-5** (the boot-pacing spike) | EXPECTED-RED — max ~192.3ms = 4.6× median vs the 3× line (dropped 9.4% ≤ 10%); the eager-payload mount task gating first paint | **RP-2 → W7** (PI-1; L20/GAP-L5, dead-payload abrogation) |
| **O-26** (aurora headless) | EXPECTED-RED headless — 10s migration 0.000/255 (SwiftShader css-placeholder); the **headed annex is all-3-gates GREEN on real GPU** (calm 0.935 · migration 8.55 · min-pair 2.26 per 255) | headed annex GREEN → **W9** |

### The slate (O-1..O-26) at the wave head

| Oracle | Disposition | Source |
|---|---|---|
| O-1 · O-1b (ground / field-wake) | GREEN | pass-2 §0 (cold-dark boots dark; L-series flat/monotone) |
| O-2 (returning-scheme lockstep) | GREEN | pass-2 §0 (byte-identical) |
| O-3 (headed-GPU probe) | GREEN — re-run W8, both schemes, screencast-judged | passes 1/2/4/5/6/7/9 (headed real GPU, dpr2; §Method each) |
| O-4 (overture order 1×/6×) | GREEN | pass-2 §0 |
| **O-5** (boot-pacing spike) | **EXPECTED-RED** (born-RED leg) | RP-2 → W7 |
| O-7 (rung membership) | GREEN | passes 3/5/6 (well recipe byte-equality) |
| O-8 (segmented-tabs geometry) | rides **W7 post-adopt** (P4 producer packet; T-20 double-trim) | pass-9 P9-W1 |
| O-9 (shadow-palette census, re-aimed) | GREEN 5/5 | passes 4/5/10 (`0ad772f`; the lane-18 re-verify 5/5) |
| O-10 / O-10b / O-10d (line-locks / voice) | GREEN | passes 1/3/9 (weight 400; the ccf4b30 name-lane cure) |
| O-11 (6 gates) | GREEN | pass-2 §0 |
| O-12 (blob hover-mood) | GREEN 3/3 | pass-2 §0 (hover-in 10.89 L / 12.29 D ≥ 6/255) |
| O-13 (slim) | GREEN | (standing) |
| O-14 (preview truth, byte-identity) | GREEN + a NEW **born-RED feasibility leg** (`ccf4b30`) now GREEN post-ramp-cure | pass-4; lane-17 (6/6 incl. both feasibility legs) |
| O-15 / O-15a (dock band / seal border watch) | GREEN + a NEW **born-RED band-invariance leg** (`2c1a0f4`) now GREEN post-`--dock-band-min-h` | pass-9; lane-16 (`border-style:none` computed; band constant 72px) |
| O-16 (motion census, W5) | GREEN (curves house-true; RunTask>16ms=[] on the traced swap) | passes 2/6 |
| O-17 (easing composition) | GREEN 4/4 (letterbox 0 · stamps 0 · dot-rest · one-literal) × 6 cells × 3 regimes | pass-8; lane-19 |
| O-18 (contrast census) | GREEN + **census EXTENSIONS** landed born-RED then GREEN: config GRAPHICS track (`bdd0a9c`, 6.07 L / 8.45 D) · the `--ink-muted` plate-caption leg (`1705345`, 22/22) · About body 2/2 | passes 3/5/10; lanes 14/15/18 |
| O-19 (netting luma) | GREEN — light 62.0/255 ≥ 59 · dark 50.3/255 ≥ 45, all cells + BUILT | pass-7 |
| O-20 (generate plate) | GREEN (regenerate re-stamps; verb-count law) | pass-6; lane-17 re-run |
| O-21 (gradient rail) | GREEN 4/4 (radius/caps/terminals; pill silhouette) | pass-7; lane-19 |
| O-23 (subpath budget) | GREEN (no-blast; eager JS −0.09% at restructure) | W1 record |
| O-24 (LCP reveal-only) | GREEN both schemes (picker plate, opacity 1) | pass-2 §0 |
| **O-16-R1** (150ms clobber) | **EXPECTED-RED** (born-RED leg) | PKT-1 → W7 |
| **O-26** (aurora headless) | **EXPECTED-RED headless; headed annex GREEN** | headed → W9 |

**Merged-head confirming slate run** (`6d95871`, lane-unique ports 8490/8491): `npx playwright
test` — 6 projects (smoke · smoke-admin · smoke-mobile · smoke-reactivity · smoke-perf ·
smoke-safari), workers:1, built-bundle perf on :8491.

**One integration re-aim** (recorded honestly — NOT hidden, NOT a gate weakening): the first
merged-head run read **158 passed · 2 failed · 2 skipped** — the two O-10 title-lock legs asserting
the picker `.space-trigger` ≡ `--type-display-3` (67.78px), while **WR-4/T-51 (owner MANDATE §0.7,
landed `5f273ae`) had stepped it DOWN one golden rung to `--type-display-2`** (53.28px). The WR-4
lane's own rides column named "O-10 line-locks re-run" but left the spec stale (a cross-lane
integration gap — each lane verified its own drive, not the full oracle slate). The oracle's
REFERENT was re-aimed to the ruled token (`display-3 → display-2`, both the desktop-dual-grid and
the 390 mobile legs) — the **tighten-not-weaken idiom** the same wave applied to O-14 in `ccf4b30`:
the lock still asserts host-independence + weight 400 + size ≡ the exact shipped token; only WHICH
token the picker rides changed, **by owner mandate** (`b551755`, e2e-scoped, separate from the
docs commit). **RESULT after the re-aim (full 6-project re-run at the re-aimed head): 160 passed ·
2 skipped · exit 0** — the two O-10 legs now GREEN, and the ONLY EXPECTED-RED in the slate is
**O-5** (the boot-pacing spike, a `test.fail()` designed expected-failure — this run frames=78,
median 49.9ms, max 234.2ms = 4.7× median, dropped 10.3%; the born-RED leg reproducing exactly as
designed, counted as passed by playwright, exit 0 confirms; RP-2→W7). (The lane commits each re-ran their touched
oracles GREEN in-worktree before merge — `ccf4b30`: o14 6/6 · o18/o10d/o20/o9/o15 41/41 · vitest
2241/2241; `1705345`: o9 5/5 · o18 22/22; `31d8ca3`: O-17/O-19/O-21 + gradient 21/21; `bdd0a9c`:
o18 config leg GREEN.)

### The §6.2 budgets re-run

| Budget | Line | State |
|---|---|---|
| view-switch first-frame | ≤ 100ms | **GREEN** — 83.4ms (pass-2 §0, built-bundle :8611) |
| LCP | ≤ 2500ms | **HONESTLY RED** — see Q14 (§6) |
| TBT | ≤ 300ms | **HONESTLY RED** — see Q14 (§6) |

### PI-1 — the fresh Lighthouse delta (W8 row appended)

A W8 row is appended to `audit/pi1-delta-ledger.md` following the append protocol. The W8
remediation is **demo-only + chunk-graph-neutral** (like W1/W4/W6.5 — type/component/CSS work, no
eager-payload movers), so the CI expectation is **FLAT vs the W4/W6.5 CI rows** (LCP 5141 / TBT
5988), budgets stay honestly RED, deltas NOISE (PP-10). The measured render-blocking index CSS at
the built head is **~88.4 KiB gz ≤ 120** (32 KiB headroom). The CI 3-sample spread appends from the
post-push `ci.yml` gh-pages job. **No gate weakened, no re-baseline** — the named gates are W7
(payload half) / W9 (green-or-escalation).

---

## §6 THE Q14 ADJUDICATION MATERIAL (WITH the owner — present, never pre-decided)

Q14 was RULED *"Accept no failures — divine a proper and idiomatic fix. No compromises."* The
LCP/TBT budgets are **must-go-green-by-close HARD GATES** (no re-baseline, no preset-swap, no
deferral). This package presents the record; **the budget/preset pairing decision is WITH the
owner at W8/W9** (weigh the owner's own *"performance ABOVE ALL"*, E-3).

**The LCP/TBT record** (from `pi1-delta-ledger.md`):

- **W0 baseline** (`80c5888`): LCP 5563 / TBT 5618 — honestly RED (LCP ~2.2× over, TBT ~19× over).
- **W2 same-instrument reveal-only cure** (O-24, built bundle): light **2484→2128ms (−14%)** — the
  reveal-only law + the Google-Fonts strike + hydration-first LANDED. LCP identity converged.
- **W2/W3 CI** (`ba0706e`): LCP 5151 / TBT 4223–8740 (the eager-parse-cliff variance band).
- **W4 CI** (`06b793c`): LCP 5141 / TBT 5988 — FLAT vs W2/W3 (chunk-graph-neutral recomposition).
- **W6.5 / round-4**: expected FLAT (demo-only remediation); CI spread appends post-push.
- **W8** (this wave): expected FLAT (demo-only, chunk-graph-neutral).

**The physical blocker, named**: FCP ≈ 4.3s (the ONE eager-payload mount task — RP-2 / the O-5
spike leg, `test.fail()` standing) gates first paint. The reveal-only law's LCP-identity cure
(proven −14% same-instrument) cannot move the CI LCP floor while FCP gates it. **That is the W7
PAYLOAD half** (L20 / GAP-L5 / dead-payload abrogation; the Lane-M-routed `/parsing`-subpath
scroll-timeline tail split) — not a W8 regression.

**The pairing decision the owner rules at W8/W9**: whether the budgets close GREEN on the W7
payload landings, **or** a triumvirate-level owner escalation names the physical blocker (the
eager-parse cliff / the payload chain). No re-baseline / preset-swap / deferral — the Q14 ruling
forbids all three.

---

## §7 Hard-gate checklist (what this package delivers to the gate)

| §Hard gate row | State |
|---|---|
| 1 · every census surface has a filed pass record; every row landed-or-booked (zero silent drops) | **MET** — 11 pass journals; `ROWS.md` folds all 40 pass rows + 11 WR rows + 7 checkpoint brackets |
| 2 · the package (non-authoring): frames per surface · both schemes · 3 viewports · boot screencast · each taste axis a BRACKET indexed to its owner line | **MET** — §1 frames · §4 screencast · §2 bracket table (20 axes + 2 non-rows) · §3 owner-line index |
| 3 · the full oracle slate: green or honest EXPECTED-RED with packet cites | **MET** — §5 (3 born-RED legs verbatim; merged-head slate run) |
| 4 · §6.2 budgets re-run + a fresh Lighthouse delta (PI-1); the Q14 material in the package | **MET** — §5 budgets · PI-1 W8 row · §6 Q14 |
| 5 · PP-8 sweep · lint 0 · typecheck 0 · npm test green · e2e 6-project green | **MET** (post-merge: lint 0 · typecheck 0 · vitest 2241/2241 · e2e 6-project GREEN after the WR-4-owed O-10 referent re-aim `b551755`, recorded honestly in §5) |
| 6 · **THE OWNER'S VERDICT IS THE GATE** | **PENDING THE OWNER** — the ruling lands in `VERDICT-2026-07-12.md`; a package delivered but unruled leaves the wave OPEN, honestly |

**Fences honored**: no gate weakened to green the package · no §4-retired spec cited as best
practice · no row contradicting a §12 ruling or §4 retirement landed (all such route to the owner
as brackets) · producer rows stay letters (P1-R3 · P9-W2 · P9-W1 → the GLASSUI-T communiqué). The
package assembly commit is docs-only, path-scoped.
