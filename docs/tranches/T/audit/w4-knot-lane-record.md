# T.W4 — the picker-recomposition KNOT lane record (the ONE serial writer)

**Lane**: the C1 knot, forced order (W4-1 → W4-2a → W4-2b → W4-5 → W4-4+W4-3
→ W4-7 → cap-cure) — 7 path-scoped commits on `t-w4-picker` (cut from the
round-2-closed `tranche-t` head `2162122`). **W4-6 (the file-disjoint
display-voice sweep) was NOT this lane's** (dispatcher's split — §Agents).
**Law stack honored**: RATIFICATION §0 → MANDATE §0+§0.5 → SYNTHESIS-as-
hardened → `waves/T.W4.md`; anchors re-derived against `w1-move-map.md`
(PP-11 — the knot set resolved to `color-picker/ColorPicker.vue`,
`display/ColorComponentDisplay/*`, `controls/ComponentSliders/*`,
`panes/PaneHeader.vue` [geometry only], `visual/HeroBlob.vue`,
`display/ColorSpaceSelector.vue`, + the index.html font hunk).

## The forced-order commits

| Step | Commit | Landed |
|---|---|---|
| W4-1 | `b16eb07` | Titles ×φ non-bold (picker display-3 · pane display-1 · weight-400 `:root` pin on each surface's OWN class list — the About-700 inheritance bug dead; `inline` = the ONE sanctioned host prop); compositor shrink-ratio endpoints re-targeted CLOSED-FORM (`tan(atan2(heading, display-1))` — ≈1/φ at cap, self-degenerating to 1 on floor-pinned phones); em-relative caret/underline/gap; O-10a/e minted + @900 height gate GREEN + 390 matrix |
| W4-2a | `529bbbc` | The reservation RE-SCOPE as its own commit (t-mobile F-4.4 precondition): per-cell `ch` min-widths DEAD, cells intrinsic, line-level lock only; Q11b lever 1 (integer least-counts, the F6 named set — lab deliberately 1-decimal); gap = 0.75ch ≡ the packing constant |
| W4-2b | `79787ef` | The contiguous tuple at ×φ (display-4-class cqi rung, every bound ×φ) + **THE TNUM MINT**: the F5 premise REFUTED ON THE ARTIFACT (no Fraunces build anywhere ships tabular figures — GF, google/fonts TTF, undercasetype 1.000 all verified); `scripts/fonts/build-fraunces-tnum.py` cuts the .tf set the foundry never did (constant-advance composites 1340/2000upm + zero-delta HVAR rows + a real GSUB tnum on every LangSys); browser-verified equal advance 147.41px at w300+w600, prose stays proportional. Capacity re-derived ON the minted face (tabular ch 0.67em → 11.7, band-invariant — the <sm fork dissolves); lever 2 = the derived `--readout-fit` ≤3% shave. O-10b census (the shipped catalog through the app's own selector, 32rem+390) + O-10c |
| W4-5 | `dfd76b1` | THE SEAT (PI-4 same-commit): `--blob-fp: clamp(7rem, 22cqi, 11rem)` ONE formula; flush `--blob-seat: 0` (Q3); `--z-ornament` named tier (Q3b); title row hosts the bead (reservation + min-height formula-derived — the readout band stays clear, capacity stays full-width); contact-shadow depth; ink floor 0.15 ∈ [0.12,0.20] closed-form (0.28µs/derive — 42,857× inside the 12ms headroom, PI-3; drag canary green); @mouseleave release beat + tooltip re-seat; the ONE timing fixture (`e2e/smoke/fixtures/blob-timing.ts`) replaces the 3-file hand-sync; blob-presence-mobile COMPUTED from the formula (the 180/240 8rem-law pair dead); O-12 minted (identity/occlusion/hover-floor 11.99≥6/255/hover-active budget) |
| W4-4+3 | `f991554` | THE CONSOLE: rung-2 WELL (`.console-well` one-home; Q4/C5), chassis-persistence (rows-only re-key), ink letters (labelColor conceit DEAD — certified muted-on-well rest, contrast-derived active ink), TRUE glyphs (`L a b α`; α everywhere), seal-recipe rail ring (P5 booked), dock-ladder states, WatercolorDot = the ONE active indicator, touch rung ≥44 <lg, tooltip re-seat; W4-3 channel strip (live meter from the SAME formatted cell as the header — zero new state; static ranges retired to their two owners; thumb tooltip dead); readout-frac guard-then-alpha CURE (`--ink-muted`); POPULATION CLAUSE → ConfigSliderPane (well seat + certified live-value ink + touch rung); the 3 O-18 fixme rows FLIPPED LIVE (6/6 both schemes) |
| W4-7 | `3c79556` | Veil geometry re-derived vs the ×φ band: earn-range keyed to OCCLUSION (gap-driven — 0–64px HOLDS by derivation); title range stays the producer grammar's 0–120; MATERIAL untouched; O-11 gates 1+2(+both schemes)+3 re-run GREEN over the settled field |
| PP-8 | `be155e1` | Cap cure: ConsoleRail.vue lift (the P5 seam) + seat.css grammar sheet (the overture.css precedent) — 521/453 → 274+266/378 |

## §Hard-gate evidence (the knot's rows)

1. **O-10a** host-independence (family/style/weight identical; size ≡ each
   host's exact D2 token) + **height gate @900px** GREEN (no console
   scroll at 1440×900 — never escalated) + mobile matrix (About honest
   2-line <sm; floor-pinned no-op) — `o10-type-locks.spec.ts` 6/6.
2. **O-10b** per-space line locks over the SHIPPED catalog (UI-driven
   census, 32rem + 390; rendered ≤ lock, min-height ≡ lock, no overflow) +
   **O-10c** rendered digit-advance equality at w300+w600 on the shipped
   face + honest proportional prose. The re-scope landed FIRST (own commit).
3. **W4-3** live meter per row (COLOR_MODEL_KEY's formatted cell, zero new
   state); static ranges retired; hover tooltip dead; meter ink at the O-18
   rung.
4. **O-18** console population 6/6 both schemes (letters on the well ·
   readout demotions · config-slider rows); ONE active indicator
   (WatercolorDot seat; the neutral pill retired); roving tabindex
   verbatim; touch rung ≥44 <lg; the interim ring = the seal recipe
   verbatim (P5 book in-commit).
5. **O-12 set** green — seat identity (+`--blob-seat` 0 resolve) ·
   occlusion (never dock) · hover-mood floor **11.99/255 ≥ 6/255 within
   400ms** · hover-active budget (renderer-aware; SwiftShader hang-guard
   leg, real-GPU ≤20ms assertion) · mobile bound COMPUTED from the formula
   (canvas 179.2 ≡ formula 179.2 at 390) · the ONE timing fixture — all in
   the SAME commit as the formula (PI-4). Ink floor 0.15, closed-form,
   **measured 0.28µs/derive** (PI-3; drag-frame canary re-run green).
6. (W4-6 — not this lane.)
7. **O-11 gates 1+3** re-run green over the settled post-W2 field (gates
   1+2 both schemes + gate 3; full 1–6 green earlier in-wave); earn-range
   occlusion-keyed.
8. **CSS tripwire**: `index-*.css` **89,490 B gz = 87.4 KiB ≤ 120** (+0.26
   KiB over the W3 close — the type recalibration spend, controlled).
9. **Suites at close**: lint 0 · typecheck 0 (lib+demo) · vitest
   **2192/71** · playwright ALL 6 projects **101 passed / 2 skipped (O-3
   headed class) / 1 environmental flake** = `views/gradient.spec.ts` drag
   (the STANDING contention-class flake named since synthesis; untouched
   files; GREEN solo ×1 this close) · `git status` clean · PP-8 caps clean
   (top: PaletteCard 400 = the W3 close state) · casts src 8/0 ·
   tool-artefact grep over this doc EMPTY.

## Owner-rider dispositions (T-30, MANDATE §0.5)

- **Centre-ward**: delivered by containment — the bead moves from
  half-off-card (overlapping the About seam at the W1 head) to wholly
  inside the plate's title band (edge 0.24·fp ≈ 27px inside both edges),
  contact-shadow seated. `--blob-seat` stays 0 (Q3 verbatim); the knob's
  positive domain is the W4.5 taste axis. **Re-judged at T.W4.5.**
- **The BLUR forensic** (demo-root cures here, producer-root → P6 rider):
  demo-root CLEARED by measurement — backing store = gBCR ×
  resolveBudgetDpr (ratio **1.99** at dpr2, full-res), zero CSS filter on
  the canvas chain, demo quality rung `full` at lg. Producer-root cites for
  the **P6 packet rider**: the silhouette AA is ~1px fwidth-derived
  (`goo-blob/shaders/metaball.frag.ts:315-318` — NOT the cause); the
  softness is the engine's hero-scale INTERIOR shading (featureless at
  ⌀≥90px — the row-A legibility family) + the wrapper's resting
  drop-shadow halo (`0 4px 14px` + `0 1px 3px` reads as focus blur at hero
  scale). Route: append to P6 at the next letter touch (the letter is
  DISPATCHED; the rider rides W7's verify-at-cut walk).

## BOOKS opened/serviced (books-never-gates)

- **P5 letter-rail swap** — the interim = the seal recipe verbatim, now
  isolated in `ConsoleRail.vue` (the swap touches ONE file).
- **P6 `settled` seam** — park stays wall-clock (the ONE fixture carries
  the constants); + the NEW blur-forensic rider rows above.
- **P3 `.glass-well`** — `.console-well` (style.css) is the one-home
  interim for BOTH slider populations.
- **P10 type stations** — `--type-weight-display: 400` pinned at `:root`;
  the trigger's display-3 carried in its own scoped block (exact token).
- **F9.R1 `lightnessFloor`** — the demo ink floor (0.15) is its sizing
  spec; swaps at W7.
- **α glyph face note (W4.5 eye item)**: Fraunces carries no Greek — the
  rail's `α` inks in the metric-tuned fallback face (deliberate, tuned;
  judged at the checkpoint).
- **About title at ≥1440 inks a balanced 2-line** (display-1 cap vs the
  sentence + member; t-mobile F-4.2's 1-line claim held only on the fluid
  768/1024 band) — a W4.5 eye item, not a gate (no O-10 row asserts a
  1-line About at desktop).

## PI-1 (the Q14 tracking instrument)

The lane carries no LHCI binary (the ledger's SAME-INSTRUMENT discipline is
the CI run — the W1/W3 precedent: the row stamps from CI at integration).
The wave's payload delta, measured at this gate: render-blocking CSS
**+267 B gz** (89,223 → 89,490; the type recalibration spend); the eager JS
chunk graph UNTOUCHED (zero new eager imports — ConsoleRail/seat.css are
intra-chunk moves; the blob stays the deferred async chunk); the tnum face
+124 B woff2 (a preloaded asset, size-neutral to LCP/TBT). Expectation:
NOISE vs the `28836873580` baseline; the ledger row stamps from the
integration push's CI run (`audit/pi1-delta-ledger.md`).

## π frames

`docs/tranches/T/audit/pi/w4/w4-picker-{light,dark}-{1440,768,390}.png`
(on-disk, `*.png` self-ignored — the S π convention; the W8 hero-surface
raw material).
