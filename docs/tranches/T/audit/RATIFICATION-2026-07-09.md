# TRANCHE T — THE RATIFICATION (2026-07-09)

**The §12 gate is CLOSED — dispatch OPENS.** The owner ruled on the hardened 20-row slate.

## §0 Verbatim owner text (wins over every encoding below)

> 1.5x bigger, using the steps defined by glass-ui's typography system.
> Only palettes should be rainbow--the letterforms dropdown and the title
> The well.
> Keep.
> Flush.
> full comp.
> First.
> both.
> Accept no failures--divine a proper and idiomatic fix. No compormises.
> Fix all deploy hooks and meta CI items. Our CI should be quick, elegant, clean. A profusion
> of useless CI tests and items should be abrogated, in particular, tautological e2e
> validation tests. You should have VPN access now to mbabb.fridayinstitute.net via SSH, too.

## §1 Per-row encodings

| Row | Ruling | Encoding |
|---|---|---|
| **Q11a** | "1.5x bigger, using the steps defined by glass-ui's typography system" | The sizing AUTHORITY is the producer typography ladder — the growth is the nearest glass-ui ladder move to ×1.5, which is TWO token steps (×φ 1.618; the ladder has no 1.5 and \|ln1.5−lnφ\| < \|ln1.5−ln√φ\|). No demo-minted sizes, ever; if a needed rung is missing, it is a producer ask (the P-packet size-station row stands). |
| **Q11b** | (unaddressed) | DEFAULT STANDS: levers 1+2 (per-space integer least-counts + fit clamp), lever 3 honest 2-line locks for lab-class; lever 1 required at 390. |
| **Q5** | "Only palettes should be rainbow--the letterforms dropdown and the title" | **The owner-verbatim GUARDED LETTERFORM RAMP**, at exactly TWO sites: the "Palettes" entry in the view dropdown AND the Palettes title. Enters the C3 ledger as the sanctioned exception. The data-strip chip arm is DEAD for T-10 (the chip grammar survives only where T-17's previews independently earn it). |
| **Q4** | "The well." | The sliders console = **rung-2 WELL** (the owner-word re-cut ACCEPTED — closed-form certified ink). The row's sibling assignments stand as defaulted (gradient plate → well; eyedropper → chrome; PaletteCard → well). |
| **Q18 + Q10** | "Keep." | Q10: Tools/Login CHROME **keeps** the live accent (menu-scoped ink law). Q18's body stands at DEFAULT: the C3 law + its complete exception ledger RATIFIED; the neutral family = **the house WARM cream/stone-ink form** (raw #fff/#000 only inside color math). |
| **Q3 + Q3b** | "Flush." | The blob seat = **flush** (`--blob-seat: 0`), zero overflow, containment by construction; Q3b stands at DEFAULT (content-stack top, chrome above — immaterial under flush, recorded). |
| **Q2** | "full comp." | The T-26 landing = **the full composition**: chroma-adaptive hueSpread [24°,64°] · energy 0.76 · ONE counterpoint stop (+165°, 0.6×C) · softmaxBeta 4 · breath 26s — judged by eye across green/warm/neutral seeds AFTER W2-1; the Q2-NOW/Q2-FULL split and the boot-fade + settle annex stand. |
| **Q1** | "First." | **Colocation restructure FIRST (W1)** — before every design wave; MOVE-MAP + the mechanical hard gates bind. |
| **Q6** | "both." | **TWO-STAGE hardening (the alternative RULED IN)**: a mid-tranche Fable+frontend-design CHECKPOINT after W4 (a lighter per-surface critique + remediation pass) **AND** the deep close-stage wave (the current W8). THE DAG AMENDS: insert **T.W4.5 — the mid-tranche checkpoint** between W4 and round-5; W8 keeps the terminal owner-certification package. |
| **Q14** | "Accept no failures--divine a proper and idiomatic fix. No compormises." | **THE PERF REDEMPTION MANDATE**: the honestly-red Lighthouse LCP/TBT budgets become **must-go-green-by-close HARD GATES** — no re-baseline, no preset-swap, no deferral. The cure is architectural and idiomatic (the reveal/hydration/payload chain: the W2 overture's LCP reveal-only law, the L20/GAP-L5 payload landings at W7, dead-payload abrogation, font/KaTeX delivery). The per-wave delta ledger becomes the TRACKING instrument for a gate, not a deferral record. W9 closes ONLY with green budgets or a triumvirate-level owner escalation naming the physical blocker. |
| **Q7, Q8, Q9, Q12, Q13, Q15, Q16, Q17** | (the quick confirms — no objection raised) | DEFAULTS STAND as presented: packet dispatch at ratification (NOW) · full TA-4 api excision · the header effect-bracket · seal-ring ABROGATE + P5 the standing law · retain-5/excise-7 proof split · promote the 7 leaked primitives · the two producer-candidates flagged in-letter · `modules/`+`platform/` + colocated `__tests__/` with the named conformance exception. |

## §2 THE NEW CI/DEPLOY MANDATE (rider on T.W0 — ruled scope expansion)

Verbatim: "Fix all deploy hooks and meta CI items. Our CI should be quick, elegant, clean. A
profusion of useless CI tests and items should be abrogated, in particular, tautological e2e
validation tests. You should have VPN access now to mbabb.fridayinstitute.net via SSH, too."

- **W0-CI (the diet)**: the CI becomes QUICK, ELEGANT, CLEAN — audit every workflow job/step
  + every e2e spec for earned existence; ABROGATE tautological e2e validation tests (specs
  that assert what the type system/build already guarantees, self-referential fixtures,
  proxy predicates the oracle slate replaced) and useless meta items; the S oracle floor's
  REAL gates (smoke-safari, hard Lighthouse per Q14, the frame budgets, the born-RED O-26)
  survive — the diet removes tautology, never truth. Wall-clock is a design goal (the
  ~32-minute pipeline should shrink substantially).
- **W0-X1 (deploy hooks)**: fix ALL deploy hooks — the dead `deploy.babb.dev/hooks/value-js`
  webhook chain + the prod api deploy (api.color.babb.dev is I-era). Investigate available
  access (ssh config/keys for the babb.dev spine host; the R FINAL §7 instructions); execute
  what access permits; anything genuinely unreachable gets the honest record with the exact
  missing credential named.
- **W0-X2 (the NCSU alias)**: **VPN/SSH access to `mbabb.fridayinstitute.net` is GRANTED** —
  execute the X2 retirement per the R FINAL §7 instructions (probe first: `ssh
  mbabb.fridayinstitute.net` connectivity, read the live config before touching it; the
  retirement is the documented alias/redirect cleanup). CAUTION LAW: read-only survey → plan
  → execute → verify; never destructive without the survey confirming the documented state.

## §3 Cascades

1. **The DAG amends**: … → W4 → **T.W4.5 (mid-tranche checkpoint — NEW)** → W5/W6 → W7
   (trigger) → W8 (deep hardening + owner certification) → W9. The checkpoint is a LIGHT
   pass: per-surface critique of everything landed through W4 + remediation lanes + a
   delta-report to the owner (no certification package — that stays W8's).
2. **Q14 gates propagate**: W2 (the overture's LCP contribution measured at wave close), W7
   (the payload landings' TBT/JS-eager deltas), W9 (budgets GREEN or the named escalation).
   The W0 CI diet must NOT remove the Lighthouse gate — Q14 hardens it.
3. **Q5's chip-death** simplifies W6's dock/nav lane (the letterform ramp + guard is the
   whole T-10 implementation); O-14's referent re-points to the ramp's guarded stops.
4. **The packet letter dispatches NOW** (Q7) — with the P-row for the size-station updated
   to cite Q11a's ruling (the producer ladder is the sizing authority).
5. **X1/X2 move from maintainer-books to W0 EXECUTABLE items** (§2); the S FINAL §5 book
   rows gain their firing note.

## §4 What does NOT change

The 8-move doctrine (as hardened) · the wave contents outside the named cascades · the
§Recovery rider law · the oracle slate (O-26 born-RED stands; the diet touches tautology,
not oracles) · the colocation program · the E-4 folds · the letters' non-P-row content.
