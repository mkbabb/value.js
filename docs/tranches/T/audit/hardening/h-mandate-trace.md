# T HARDENING · lane h-mandate-trace

**Charge**: adversarial verbatim trace of `MANDATE-2026-07-06.md §0` + addenda §0.1–§0.4 (VERBATIM
LAW) against the T corpus — hunt dropped clauses, diluted verbs, scope-narrowing ("all"→"most"
class), and soft asks left unencoded. Plus: re-read the owner screenshots first-hand and verify the
T-1..T-29 finding rows honor what the pixels actually show.
**Method**: fresh eyes, independent of the prior scoring critic. Every MANDATE §0/§0.1-0.4 sentence
enumerated and mapped to its corpus home. Nine owner shots read directly and re-derived
independently before trusting SYNTHESIS §1.0's re-map.
**Verdict**: the corpus is HIGH-FIDELITY on the trace — every T-1..T-29 finding and every E-1..E-7
edict has a routed home, and the shot-map re-derivation is image-true (see §3, a positive finding).
The defects live in the RATIFICATION/ENCODING layer, not the routing: one owner-word re-cut settled
silently against the corpus's own pass-2 law (MUSTFIX), one dropped intensifier on the one axis the
owner called paramount (SHOULDFIX), one unreconciled verbatim-law count (SHOULDFIX).

Severity tally: **MUSTFIX 1 · SHOULDFIX 2 · NOTE 4**.

---

## MUSTFIX

### H-1 — T-8 "higher z than all" is an owner-word re-cut SETTLED SILENTLY, against the corpus's own pass-2 law

**Corpus location**: SYNTHESIS §2 D8 line 209 (`--z-ornament` = top of the CONTENT stack — chrome
stays above; the two readings of "all" never conflict in paint) = T.md §2 D8 line 193 (byte-identical).
Authoring lane: `t-blob-hero.md:136-142` (the z law).

**MANDATE §0 (line 31, verbatim law)**: *"it should have a higher z than all."*

**The defect**. The owner wrote "higher z than **all**". The corpus reads "all" as "top of the CONTENT
stack, with floating **chrome** (dock, tooltips, dialogs) staying above" — i.e. it narrows "all" to
"all content, not chrome". That is textbook owner-word re-cut, exactly the class the corpus binds
itself to surface. T.md §9 lesson 15 (lines 490-492, verbatim pass-2 law): *"Owner-word re-cuts
surface for ratification, never settle silently … The re-cut is argued in the table the owner rules
on."* T-5 "little glass card"→WELL rides Q4; T-2 "1.5×"→×φ rides Q11; T-10 "rainbow"→chip rides Q5;
the parallel scope question "does chrome fall under 'the rest white/black'?" is Q10. But the z-order
"all" re-cut rides **NO Q** — Q3 (the adjacent blob-seat row, T.md:515) covers flush-vs-corner-kiss,
not z-order. The synthesis states it as settled fact.

**Aggravator**: the authoring lane ASKED for ratification and the synthesis dropped the ask.
`t-blob-hero.md:139-142` reads verbatim: *"Interpretation note **for ratification**: floating CHROME
… remains above by the app's z-grammar … the two readings of 'all' never conflict in paint."* The
synthesis lifted the *conclusion* ("never conflict in paint") but struck the words "for
ratification" — a diluted verb: a lane request-for-owner-decision became a synthesis-level settled
disposition. The materiality claim ("never conflict in paint") is precisely the judgment the owner is
entitled to make; "the two readings never conflict" is true only *under containment* (orbit-reach
0.49 ≤ 0.5), so it silently ties the z-reading to the seat choice without telling the owner.

**Failure scenario**: owner ratifies the slate believing every owner-word re-cut was surfaced (the
corpus tells them so at lesson 15). The blob ships at content-stack z with chrome above. If the owner
meant "over the dock too" (the literal "all"), they never got the row to say so — a silent regolden
of a verbatim clause, the exact disease §4/lesson 13 exists to prevent.

**Proposed amendment**: mint a ratification row (or extend Q3) — *"Q3b · blob z-order: does 'higher z
than all' include floating chrome? DEFAULT: NO — content-stack top; chrome above; immaterial under
the flush seat (blob never enters the chrome band). ALT: true top-of-all (blob paints over dock on
any future overflow)."* And restore "for ratification" to D8's clause so the disposition reads as
provisional, not settled.

---

## SHOULDFIX

### H-2 — "performance ABOVE ALL" — the intensifier is dropped from EVERY E-3 encoding, on the one axis left red-and-deferred

**Corpus location**: MANDATE §2 E-3 line 148 · T.md §2 E-3 line 93 · SYNTHESIS §2 (E-3 doctrine, same
wording). "above all" survives ONLY at MANDATE §0 line 60 (verbatim).

**MANDATE §0 (line 60, verbatim law)**: *"architectural transpositions in the sake of elegance,
simplicity, and performance **above all** are both necessary and desirable."*

**The defect**. Every operational E-3 encoding renders this as *"elegance/simplicity/performance
necessary and desirable"* — dropping "above all". The §0 ceiling technically preserves the phrase
(§0 wins), but no dispatcher reads §0 for E-3 disposition; they read the E-3 rows, which carry the
diluted form. This matters because the corpus's PERFORMANCE disposition is the softest in the slate:
JS eager **347.9 KiB gz vs ≤280** (+24%, RP-2 re-baseline, cleared only at the trigger-gated W7) and
Lighthouse **LCP 5563ms / TBT 5618ms HONESTLY RED**, adjudication DEFERRED to Q14 at W8/W9
(SYNTHESIS §6.2 lines 527-533). So the single quality the owner named paramount is the single one the
corpus leaves red-with-a-later-decision. Dropping "above all" removes the owner's prioritization
signal from exactly the rows where it should bite.

**Failure scenario**: W7/W8 dispatchers weigh the RED budgets against feature richness with no
encoded "performance is paramount" thumb on the scale; the owner's §0 emphasis never reaches the
adjudication because the E-3 rows and Q14 don't carry it.

**Proposed amendment**: restore "above all" to the E-3 encodings (T.md §2 + SYNTHESIS §2) and add one
clause to Q14 noting the owner framed performance as paramount (§0), so the budget/preset
adjudication weighs it explicitly rather than as a neutral trade.

### H-3 — "32 agents" (VERBATIM) vs "36-lane fleet / 37 lane docs" — unreconciled, plus an internal 36-vs-37 self-contradiction

**Corpus location**: MANDATE §0 line 55 + header line 4 ("32 agents" / "32-agent audit fleet");
SYNTHESIS lines 5 + 14 ("36-lane audit fleet" / "36 lanes"); T.md line 6 ("36-lane audit fleet") vs
T.md line 7 + PROGRESS.md line 105 ("37 lane docs"). On-disk count verified: `ls
audit/lanes/*.md | wc -l` = **37**.

**MANDATE §0 (line 55, verbatim law)**: *"DEEPLY audit with 32 agents in parallel our original plan
and waves thereof, alongside all changes made herein."*

**The defect**. The owner asked for 32 parallel agents; the corpus delivered 36 "lanes" / 37 docs and
never reconciles the 32→36/37 delta — nowhere is it said the fleet exceeded the ask (spirit met, but
silently). Worse, the corpus contradicts itself: "36-lane" (three sites) vs "37 lane docs" (two
sites), while 37 files sit on disk. The likely reconciliation (t-plan-audit-1 + t-plan-audit-2 = one
lane, two docs) is never stated. Under the owner's explicit confidence charge — six session-limit
walls, suspect recovery seams — an unexplained count drift against a verbatim-law number is precisely
the kind of seam that erodes trust.

**Proposed amendment**: one reconciling sentence at T.md §1 or the SYNTHESIS header — e.g. *"the
mandate asked for 32 parallel agents; the fleet ran 36 forensic lanes producing 37 docs (the
plan-audit lane split into -1/-2), exceeding the ask."* Pick 36-lane XOR 37-doc as the canonical
phrasing and make the other consistent.

---

## NOTES

### H-N1 (POSITIVE) — the shot-map re-derivation is IMAGE-TRUE across 9 shots

I re-read the owner PNGs first-hand before trusting SYNTHESIS §1.0, and the entire re-map holds
against the pixels:
- **The +1 contiguous shift** (mandate's map off by one, t-2002-09→t-2004-32): `t-2002-09` shows the
  spaced readout "39.2%, 48.5, 42.2" + a hatched netting sliver = **T-7 readout + T-6 sliver** (NOT
  the mandate's T-6). `t-2002-52` shows a maroon blob buried at the "About" bold-serif card =
  **T-8 blob + T-2 title + T-3** (NOT T-7). `t-2004-10` shows the red DevMisconfig banner = **T-9**
  (NOT T-8). `t-2004-32` shows the Home/Palettes/Browse/… colored-dot legend menu = **T-10 nav
  legend** (NOT T-9). All four validated.
- **The "Also" set**: `t-2000-27` shows the "GAMUT LENS — DISPLAY-P3 / SRGB · CUSP L 0.640 C 0.246"
  caption strip = the picker's gamut-lens caption, **not the About card** (mandate said T-3);
  `t-2000-41` shows the L/A/B/A letter column + slider tracks + "(0%)/(-12…)" range captions = the
  **T-5 sliders surface** with the T-4 range captions embedded; `t-2001-51` shows the gamut-lens
  plate + its bottom caption = the **spectrum-plate caption** (T-4's twin), **not the sliders**
  (mandate said T-5). All validated.
- The §0.4 shots: `t-2211-04` shows the pink dock-seal home-glyph with an ill-fitting fine reddish
  concentric ring = **T-28** (the ring crosses/gaps the organic wax — R9's "+1.5px/−2.5px by
  construction" is visible); `t-2210-56` shows the "Tools →" brush toggle clipped at its left edge
  with "Action bar" beneath = **T-29** (the `overflow:hidden` Tools toggle). Both validated.

The forensic spine (lesson 16 — "the shot-map is best-effort; forensics re-derives by READING the
images") did its job; the mandate's own +1 mislabel was caught and corrected correctly. No mis-anchor
found.

### H-N2 — T-1 soft asks "etc" and "in particular" are absorbed but never explicitly widened

MANDATE §0 line 16-17: *"background, blob, hero titles, **etc** … On page load **in particular**."*
The "etc" (items beyond the three named) is absorbed by D3's whole-boot choreography (B0-B4 covers
dock, plates, field, instrument, blob); "in particular" (sync is worst-but-not-exclusive to load) is
picked up by T-25/T-27 (active-state quality). Faithful in effect, but no clause explicitly notes
that "etc"/"in particular" widen T-1 beyond {background, blob, hero-titles} and beyond page-load —
a one-line acknowledgment would close it. Acceptable as-is.

### H-N3 — T-7 "(or more if needed)" is faithfully encoded

MANDATE §0 line 28: *"contigulously: x, y, z **(or more if needed)**."* Absorbed by O-10b's per-space
line-count locks over "all 17+hex" catalog spaces (SYNTHESIS line 508, t-title-typography F6 line
144-160), which explicitly handles 4-component spaces. No drop.

### H-N4 — T-8 "should not clip outside the bounds" — reading-B surfaced via Q3, handled

The owner's "not clip outside the bounds" + "higher z than all" admits a reading-B (blob EXTENDS
beyond bounds but is not CUT OFF, hence higher z). The corpus chose reading-A (containment), which is
well-supported by the same sentence's "placed more into the card, moved down and to the left"
(literally encoded at `t-blob-hero.md:118,123`), and surfaces the overflow alternative via Q3
(corner-kiss / small negative seat). This clause is handled correctly — noted only to record that the
ambiguity was considered, and that it is the z-order half (H-1), not the clip half, that lacks a Q.

---

*Provenance: MANDATE §0 enumerated sentence-by-sentence against SYNTHESIS §1.2/§2/§8, T.md §1/§2/§9/§12,
and the authoring lanes; 9 owner PNGs read first-hand from `audit/owner-screenshots/`. Trace
independent of the 94-scoring critic. Reports only — the amend pass owns the fold.*
