# h-refine-doctrine — REFINEMENT: vagueness in the §2 doctrine + its wave transcriptions that would stall an implementing lane

**Lane class**: hardening / refinement-design (zero product-code, zero corpus edits — findings only;
the amend pass owns folding). **Frontend-design bar governs every proposed amendment.**
**Substrate**: `tranche-t` @ `1987ae3`. **Product under audit**: `audit/SYNTHESIS.md §2` (D1–D10) +
`§3`/`§6.1` gate text, the `T.md` doctrine transcription (§~125–210), and the wave transcriptions
`waves/T.W0/W2/W3/W4/W5/W6.md` — each checked against its own evidence lane for numbers that EXIST
but were not pulled up, numbers that exist NOWHERE, and "design call in-wave" spots that are
pre-specifiable.
**Method**: for every curve, duration, rung, floor, tolerance, and gate predicate the doctrine
invokes — (a) is the value/predicate stated in the governing spec (SYNTHESIS wins per every wave
doc's own precedence clause)? (b) if not, does the cited evidence lane carry it (pull-up fix)?
(c) if nowhere, is it pre-specifiable now at the frontend-design bar (bracket, not point), or is
in-wave/owner deferral honest? Special charge: the W2 luma-leap placeholder bound.

**Bottom line**: the doctrine is unusually well-valued — the W5 retune table, the header rest-floor
bracket, the netting band, the D2 type landings, the seat formula, and the Q2 field composition are
all fully numeric (see §CLEAN). But three spots are literal placeholders or self-contradictions in
GOVERNING text (`>x`, a "verbatim" beat sheet whose B2 row the same paragraph rewrites, an ink-law
floor the corpus itself calls "a named threshold" and never names), and seven gate predicates invoke
numbers no corpus document carries — each one a spot where an implementing lane either stalls or
silently mints an un-ratified point value on a taste axis (the exact brackets-not-points breach
t-prompts-recap F2 forbids). 3 MUSTFIX · 7 SHOULDFIX · 4 NOTE.

---

## F-1 — MUSTFIX — the W2-2 luma-leap bound is a literal `>x` placeholder in the GOVERNING spec; the only resolution text lives in the subordinate wave doc and is itself under-specified

**Evidence.** `SYNTHESIS.md:293` (the spec of record) gates W2-2 with, verbatim: *"O-1; **no >x
luma leap**; F-6 dark first-run honest"* — `x` is an unresolved placeholder in the text every wave
doc subordinates itself to ("on any divergence … the spec wins", `T.W2.md:11-12`). The wave doc
patches it — `T.W2.md:51`: *"no >x luma leap (the numeric bound set at wave-open from the O-1
baseline — PP-10)"*, `:103` "the luma leap bounded (wave-open number, PP-10)" — but (a) the patch
is DIVERGENT from the spec and therefore loses under the precedence clause it restates; (b) even
the patch names no METRIC (whose luma — the O-1 center sample's OKLab L? mean ground-row luma?)
and no WINDOW (inter-beat? consecutive-frame? B0→B2 only?); (c) "set at wave-open from the O-1
baseline" is circular: O-1 is minted **born-RED against today's defective tree** (W0-5,
`T.W0.md:107-110`) — today's baseline CONTAINS the L≈0.35→0.6 leap (t-aurora-boot F-2), so a bound
derived from it has no stated derivation that doesn't bless the defect.
**Corpus locations**: `audit/SYNTHESIS.md:293` · `waves/T.W2.md:51,103` · (charter §3.1 W2 row
omits the leap gate entirely — consistent only by silence).
**Proposed amendment** (replace the gate cell in SYNTHESIS §3 W2-2; mirror at T.W2.md:51/:103):
> O-1 holds over the gradient ground; **no ground-luma leap**: over the B0→B2 window, the O-1
> sample's OKLab L moves monotonically toward the derived field's L with max inter-beat |ΔL| ≤
> **0.10** and max consecutive-frame |ΔL| ≤ **0.04** (the F-2 defect measures ≈0.25 — the born-RED
> reference); bounds re-recorded at wave-open per PP-10, **tighten-only**; F-6 dark first-run honest.

## F-2 — MUSTFIX — D3 adopts the t-load-sync beat sheet "verbatim" while rewriting its B2 row: 450ms/≤1.2s (the verbatim source) vs 0.9s (the doctrine's own parenthetical) — never reconciled anywhere

**Evidence.** `t-load-sync.md:266` (the adopted table, its actual B2 row): *"450ms
`--ease-decelerate` | complete ≤1.2s"*. `t-aurora-boot-active.md:246,255` deliberately re-cuts
exactly this: canvas fade **0.9s** `--ease-decelerate`, *"duration (0.45s → 0.9s: the current fade
is HALF the story too fast"*, settled **≤1.5s** (`:250`). The doctrine (`SYNTHESIS.md:151-154`,
mirrored `T.md:137-138`, `T.W2.md:52`) states *"the five laws + beat sheet of t-load-sync §3 are
adopted **verbatim** (… B2 field derive-in **0.9s** decelerate …)"* — i.e. the verbatim claim and
the quoted number contradict the verbatim source on BOTH the duration (450 vs 900ms) and the
completion bound (≤1.2s is arithmetically incompatible with a 0.9s fade starting at the ≤600ms
post-nav arm target — 600+900=1500ms, t-aurora's own "settled ≤1.5s"). t-contradictions carries no
row for this (C6 is the curve-FAMILY seam, `t-contradictions.md:399`, not this duration clash). An
implementing W2-3 lane that obeys "adopted verbatim" and opens the cited table implements 450ms and
a ≤1.2s gate; one that reads the parenthetical implements 0.9s — and the O-4/O-5 fixtures inherit
whichever bound the lane happened to read.
**Corpus locations**: `audit/SYNTHESIS.md:151-154` · `T.md:137-138` · `waves/T.W2.md:52` ·
`audit/lanes/t-load-sync.md:266` (the un-annotated stale row).
**Proposed amendment** (two coordinated edits):
1. In D3 (SYNTHESIS + T.md + T.W2.md:52), scope the adoption: *"the five laws + beats
   B0/B1/B3/B4 of t-load-sync §3 adopted verbatim; **B2 is superseded by the t-aurora-boot §2.1
   reconciliation: derive-in 0.9s `--ease-decelerate`, arm-start target ≤600ms post-nav, settled
   ≤1.5s normal** (the beat sheet's 450ms/≤1.2s row carried the CURRENT fade, re-cut as half the
   story too fast)"*.
2. A one-line supersession annotation on `t-load-sync.md:266` (record repair, the R2/DELTA.md:25
   precedent — the amend pass's call whether lane files are in-bounds; if not, the D3 note alone
   suffices since the doctrine outranks the lane).
   The B1 stagger values (dock +0 / left +40ms / right +120ms; plate-land 440ms `--spring-snappy`,
   `t-load-sync.md:265`) remain adopted UNCHANGED — the amendment must not orphan them.

## F-3 — MUSTFIX — the D8/W4-5 blob ink-law floor is, in the corpus's own words, "a named threshold" — and no document in the corpus names it

**Evidence.** `t-blob-hero.md:150-153` (the law's authoring text): *"|ΔL(bead body, card plate)| ≥
**a named threshold**, consumer-computed"*. Every transcription carries the floor by reference,
valueless: `SYNTHESIS.md:211` / `T.md:195` *"the ink law = |ΔL(bead, plate)| floor, closed-form
(inside the 12ms drag headroom — PI-3)"* · `T.W4.md:62` (item row) · `T.W4.md:117-122` (hard gate
5: *"the ink floor closed-form inside the 12ms drag headroom (PI-3, measured)"* — the gate binds
the SOLVE'S COST, never the floor's VALUE). PI-3 (`t-perf-implications.md:136-139`) hardens the
closed-form constraint, again valueless. The W4-5 writer cannot author the closed-form solve
without the constant: the only options are stall, or mint an un-ratified point value on a taste
axis (the brackets-not-points law, t-prompts-recap F2, is doctrine at W8 — `SYNTHESIS.md:368-369`).
The F9.R1 `bodyLightness`/`lightnessFloor` knobs consume this exact number at the W7 adopt
(`t-blob-hero.md:153`) — un-specified, the producer swap has no target either.
**Corpus locations**: `audit/SYNTHESIS.md:211` · `T.md:195` · `waves/T.W4.md:62,117-122` ·
`audit/lanes/t-blob-hero.md:150-153`.
**Proposed amendment** (D8 + the W4-5 item/gate rows): specify the floor as a ratified BRACKET
with a working default, sized against the corpus's own nearest precedent (the O-19 netting
luma-delta floors, 45–59/255 ≈ 0.18–0.23 relative luma):
> the ink law: |ΔL(bead body, card plate)| ≥ **floor ∈ [0.12, 0.20] OKLab L, default 0.15**,
> closed-form (inside the 12ms drag headroom — PI-3); the landed value recorded with the W4-5
> commit; owner eye-judge inside the bracket at W8; the same bracket is the sizing spec handed to
> the F9.R1 `lightnessFloor` knob at the W7 swap.

## F-4 — SHOULDFIX — O-1's "(ΔE tolerance)" names neither the metric nor the value nor the achromatic/scheme halves of the predicate

**Evidence.** `SYNTHESIS.md:499`: *"sampled center pixel ∈ the DERIVED seed's hue family (**ΔE
tolerance**)"* — which ΔE (OKLab Euclidean? ΔE2000? Δhue in degrees?) and what number are stated
nowhere; `t-oracle-gaps.md:83-84` (the mint's source) says only *"within a ΔE tolerance — not
'non-black'"*. W0-5 must mint this born-RED (`T.W0.md:107-110`): too loose re-creates the proxy
disease the mint exists to repair (a pink field passes a wide-enough family); too tight is flaky
against a live animated field. The F-6 dark-honesty and dark-slab axes (gray ≠ in-family) also
need the C and L legs, which "hue family" alone cannot carry.
**Corpus locations**: `audit/SYNTHESIS.md:499` · `waves/T.W0.md:44` · `audit/lanes/t-oracle-gaps.md:83-84`.
**Proposed amendment** (the O-1 row): *"sampled center pixel, converted OKLCh: **hue within ±30°
of the derived seed's h · C ≥ 0.03 (never an achromatic slab) · L inside the active scheme's band
(dark: [0.18, 0.72], the L2 lBand; light: complement recorded at mint)**; tolerances re-recorded at
mint per PP-10, tighten-only"*.

## F-5 — SHOULDFIX — O-5 is named "the jitter red line F4 lacked" and no document names the line

**Evidence.** `SYNTHESIS.md:503` / `T.W2.md:118-119` / `t-oracle-gaps.md:102` all describe the
SHAPE (*"inter-frame-delta variance / dropped-frame ratio — the renderer-independent 'jitter' red
line"*) and never a bound. The oracle's entire purpose is to BE a red line (the F4 gap was
exactly that jitter had no number); it must be born-RED at W0-5 against the recorded defect (paint
holes 44→315ms then pile-up, `T.W2.md:52`), which requires the line to exist at mint. As written,
the W0-5 mint lane invents the threshold — the S "proxy predicate" failure shape in a new costume
if it picks a bound today's tree passes.
**Corpus locations**: `audit/SYNTHESIS.md:503` · `waves/T.W0.md:44` · `waves/T.W2.md:118-119`.
**Proposed amendment** (the O-5 row): *"red line, boot window B0→B4 at normal CPU: **no
inter-frame delta > 3× the window's median frame time, AND dropped-frame ratio ≤ 10%**; born-RED
reference = the recorded 44→315ms hole chain; exact numbers recorded at mint per PP-10,
tighten-only"*.

## F-6 — SHOULDFIX — the D4 perceptibility gates' 2s and 60s legs have no measurable predicate (the 10s leg is the only one with a number)

**Evidence.** `SYNTHESIS.md:167-169` / `T.md:151-152` / `T.W2.md:56-57,112-113`: *"2s glance-pair
**sub-JND**; 10s window shows unmistakable migration (**≥4/255 mid-C**); 60s **never repeats**"*.
The 10s leg is measurable (mean abs frame diff, the metric t-aurora-boot:277-279 establishes
against "today's 1.2–1.8 stills"). The 2s leg names no JND metric or value (JND in what space, at
what threshold?); the 60s leg names no repeat-predicate (what operation detects a repeated
composition?). These are HARD-GATE rows for W2-5 (`T.W2.md:111-114`). Complementary to
h-oracle-slate F-1 (which established these gates are un-numbered/outside the slate and un-minted
— a slate-membership finding): even once minted, two of the three legs cannot be implemented as
written — a predicate-content finding.
**Corpus locations**: `audit/SYNTHESIS.md:167-169` · `waves/T.W2.md:56-57,112-113` · `T.md:151-152`.
**Proposed amendment** (wherever the gates are transcribed, one sentence): *"metric = mean abs
8-bit frame diff over the field region (the 10s leg's own unit): 2s leg **< 2/255** (calm); 60s
leg: **no two 5s-apart window pairs within the run at < 1/255** (composition non-recurrence);
thresholds recorded at mint per PP-10"*.

## F-7 — SHOULDFIX — O-12's two hover numbers are invoked as if defined and defined nowhere: the hover-mood frame-diff floor and the hover-active frame-budget ceiling

**Evidence.** `SYNTHESIS.md:510` / `T.W4.md:118-119`: *"hover-mood frame-diff floor · hover-active
frame budget (NEW state, ungated today)"*. (a) The mood floor's value exists nowhere — t-blob-hero
F-5 establishes the born-RED reference (today's mood delta is sub-JND, `t-blob-hero.md:70`) and §3
gives the window (satellites wake *"within ~400ms"*, `:162`) but no floor. (b) The hover-active
budget: PI-3(b)'s mint instruction says *"asserts **a frame p50 ceiling** for that state"*
(`t-perf-implications.md:140-145`) — no number; §6.2's budget roster (`SYNTHESIS.md:529`: drag ≤20
· view ≤100 · idle ≤13) has no hover row to inherit. The W4-5 gate (`T.W4.md:117-122`) requires
both green; the writer picks both numbers unratified.
**Corpus locations**: `audit/SYNTHESIS.md:510,529` · `waves/T.W4.md:118-119` ·
`audit/lanes/t-perf-implications.md:140-145`.
**Proposed amendment** (the O-12 row + §6.2): *"hover-mood floor: mean abs frame diff over the
bead's bounding box ≥ **6/255 within 400ms of hover-in** (rest→curious; the D4 metric family);
hover-active frame budget: **p50 ≤ 20ms** (joins the drag family — same interactive register;
named as a new §6.2 row), sustained-`pointermove` sweep per PI-3(b)"*.

## F-8 — SHOULDFIX — O-17's "canvas aspect ≈ 1" contradicts the easing lane's own designed law (aspect = 1/vb-ratio) and carries no ε — spec-precedence makes the wrong predicate governing

**Evidence.** The designed law (`t-easing-pane.md:85-87`): the canvas sizes by ONE law,
*"`aspect-ratio: 1 / var(--vb-ratio)` (the **overshoot-expanded viewBox ratio**) … no letterbox —
the drawn plot IS the element box"*; today's viewBox is 1×1.2 (`:66`), so the designed aspect for
overshoot presets is ≈0.83, not 1. The oracle as transcribed (`SYNTHESIS.md:96,515`,
`T.W6.md:55,115`): *"canvas aspect **≈ 1** across container widths"* — reds the designed geometry
on every overshoot row, or forces "≈" to a >17% tolerance that gates nothing. Because the spec
wins over the lane (`T.W6.md:12-13`), an obedient W6-3 writer builds aspect=1 exactly — clipping
or re-letterboxing overshoot curves, the precise defect (finding 2's dead bands) the item cures.
("Square-fit", the lane's §267 shorthand, is true only of non-overshoot presets.)
**Corpus locations**: `audit/SYNTHESIS.md:515` (also `:96`) · `waves/T.W6.md:55,115` ·
`audit/lanes/t-easing-pane.md:66,85-87,267`.
**Proposed amendment** (the O-17 row + W6-3 gate 5): *"**zero letterbox**: drawn-plot box ≡ element
content box (**±1px both axes**) across container widths; element aspect ≡ **1/vb-ratio** (=1 for
non-overshoot presets) · ≤1 cartoon stamp in-row · dot rest · one-literal law"*.

## F-9 — SHOULDFIX — Q9/W3-4's rest floor is double-spoken in two different units (fraction-of-the-veil vs producer quiet-rung fill), and the P3 knob's unit is never stated

**Evidence.** Q9's default (`SYNTHESIS.md:580` / `T.md:521`): rest = *"quiet-derived fill inside
the eye bracket [0.45, 0.65 **of the veil**]"* AND *"expressed AS ladder rungs, not an off-ladder
alpha fade"*. The source lane holds these as two ALTERNATIVES with different quantities
(`t-header-shading.md:199-204`): the bracket is a fraction of the CURRENT veil recipe (0.45–0.65 ×
the 60%-`--card` fill ≈ 27–39% added material), while the *"ladder-honest alternative (preferred
if F3's producer consume lands)"* is rest = the `quiet` RUNG's own fill — a different number that
nothing maps back to the bracket. Q9 fuses them without the mapping, and the P3 ask (*"the
scroll-header rest-floor + bottom-feather knobs"*, `GLASSUI-T-ASKS.md:54`) never says which unit
the knob speaks — so the producer can ship a knob the ratified bracket cannot price, and the W3-4
booked swap (`T.W3.md:188-189`) has no acceptance rule. Rider: O-11 gate 3's "ink-contrast floor"
(`t-header-shading.md:217-219`, transcribed in W3-4's gate) names no referent either — it should
cite the D6/O-18 certified rung rather than float.
**Corpus locations**: `audit/SYNTHESIS.md:580` · `T.md:521` · `waves/T.W3.md:51,122-125` ·
`letters/GLASSUI-T-ASKS.md:54` · `audit/lanes/t-header-shading.md:199-204,217-219`.
**Proposed amendment** (the Q9 cell + the W3-4 row + the P3 letter row): *"the ratified frame is
the EFFECT bracket: rest adds **27–39% card material over the wash plate** (= 0.45–0.65 of the
stuck 60%-`--card` veil). The interim demo veil is tuned inside it directly; the producer
rest-floor knob (P3) is ACCEPTED iff the `quiet`-rung rest composites inside the SAME bracket —
the bracket, not the rung name, is what the owner ratified. O-11 gate 3's ink-contrast floor =
the D6/O-18 certified rung for header ink."*

## F-10 — SHOULDFIX — the rung-2 WELL's tone-step is exemplar-only and P3 ships without a sizing bracket — the T-26 precedent (bracket handed to the producer as sizing spec) is not applied to the ladder's new rung

**Evidence.** D1 (`SYNTHESIS.md:126-127`): rung 2 = *"opaque tone-step of the plate … the markdown
`bg-muted` pattern generalized"*. The only quantity anywhere is the lane's exemplar-class
(`t-card-material.md:80`): *"`color-mix(in oklab, var(--card) 92%, var(--foreground) 8%)`-class"*
— the "-class" suffix explicitly declines to pin it. The P3 ask (`GLASSUI-T-ASKS.md:54`:
*"`--glass-bg-well` + `.glass-well` … 6 hand-mints prove demand"*) hands the producer NO sizing
spec — the exact omission the corpus itself corrected for the aurora atoms (*"SIZED BY THE T-26
BRACKET"*, P1) — and the W3-1 interim `--well-bg` (`T.W3.md:48,99`) is likewise unpinned. Six dead
parallel mints (T-CM-4) are the evidence of what unpinned wells breed; a producer well sized
outside the demo's judged range forces a second calibration event at the W7 swap.
**Corpus locations**: `audit/SYNTHESIS.md:126-127` · `waves/T.W3.md:48,54-57` ·
`letters/GLASSUI-T-ASKS.md:54` · `audit/lanes/t-card-material.md:80`.
**Proposed amendment** (D1 rung-2 clause + the P3 letter row + W3-1): *"tone-step ∈ **[6%, 10%]
`--foreground` into `--card`, oklab** (default 8% — the markdown exemplar), scheme-registered; the
bracket is the P3 sizing spec (the T-26 precedent); the interim `--well-bg` lands at the default
and is judged inside the bracket at the O-7 census"*.

---

## NOTES

**N-1 · NOTE — O-18's "the owner's reference color" is defined only in the evidence lane.** The
literal is `lab(38% 32 24)` — *"the brown of the t-20xx [shots]"* — at `t-a11y-contrast.md:15`;
every transcription (`SYNTHESIS.md:516,307` · `T.W3.md:36,52,126-128`) says only "the owner's
reference color". Pull the literal up into the O-18 row + the W3-5 gate so the census predicate is
self-contained (a mint lane should not have to archaeology the referent of its own oracle).

**N-2 · NOTE — D9's known-imminent "6s breath" has a period and no amplitude/curve.**
`t-shadow-palette.md:154` gives *"6s breath"* only. Propose the default in the D9 transcription:
*"opacity 0.55↔0.75, `--ease-standard`, 6s; PRM: still"* — a taste knob, but naming it saves the
W3-2 writer a mint and keeps the ghost's register quieter than the network shimmer by construction.

**N-3 · NOTE — O-8's "(ε px)" congruence epsilon is unnamed.** `SYNTHESIS.md:506` /
`t-oracle-gaps.md:131` (*"within ε px"*). W7-minted (post-P4), so no near-term stall — but the P4
acceptance row ships to the producer without the tolerance its own verification will use. Name it
in the O-8 row + the P4 letter row: *"ε ≤ 0.5px per edge, both engines"*.

**N-4 · NOTE — P6 row A delegates the mood-legibility floor without naming the metric family.**
`t-blob-hero.md:171-172` asks the producer for *"a named observable-delta floor"* (silhouette
area / limb count) while the demo's O-12 verifies in frame-diff units (F-7 above). Add one rider
to the P6 letter row: the floor should be expressed (or convertible) in the frame-diff-at-bead-box
family so producer sizing and consumer verification share units — else the W7 re-verify compares
incommensurables.

---

## §CLEAN — positively verified as fully specified (no action; recorded so the amend pass doesn't over-fix)

- **The W5 retune table** (`t-transitions-liquid §2`, transcribed whole at `T.W5.md:54-77`): every
  row carries current-computed + liquid-target token + channel law; the two F7 KEEP calls stated;
  exit law (R3 `--duration-fast`, strictly shorter) concrete. The doctrine's best-valued table.
- **Header rest floor** (`t-header-shading §3`): probe matrix (0/0.35/0.55/0.8 with verdicts both
  schemes), bracket [0.45, 0.65] ≈ 27–39% material, ≥3 field hues named, stuck state pinned, swell
  ≤64px, feather-at-every-state — exemplary bracket authoring (F-9 is about UNITS, not the values).
- **Netting band** (W6-1): 22%/28%/45%/65%/1.25px + O-19 floors 59/45 per 255 — fully numeric.
- **D2 type landings**: every landing an exact shipped token with px values (41.89→67.78 @1440 ·
  25.89→41.89 · 33.77→54.64 @32rem); weight 400 carried on the trigger's own class list.
- **The seat** (D8 minus the ink floor): `--blob-fp: clamp(9rem, 26cqi, 13rem)`, flush default 0,
  orbit-reach 0.49 ≤ 0.5 identity, reservation re-derivation formula (fp − 0.24·fp), contact-shadow
  register — concrete at every point.
- **B1 plate-land** (adopted beat sheet): dock +0 / left +40ms / right +120ms stagger, 440ms
  `--spring-snappy` (`t-load-sync.md:265`) — F-2's amendment must carry these forward untouched.
- **The channel-strip meter** (`t-misc-elements` F1): Fira Code + `tabular-nums` + mono-caption
  size, one golden step below the header readout, never italic / never `opacity-50`, unit included,
  synchronous with drag — the wave row compresses but the cited lane fully specifies.
- **The Q2/D4 field composition**: hueSpread formula + clamp, energy 0.76 (with derived saturation
  ≈1.12 / valueVariance ≈0.116 / breathDepth ≈0.068), counterpoint +165° @0.6×C deepest-L,
  softmaxBeta 4, breath 26s + drift ×1.6 (driftRadius 0.045→~0.07), vividness smoothstep(0.02,
  0.10), lBand [0.18, 0.72], pointer gates 400ms/120ms/≤2s — the model bracket-as-sizing-spec.
- **Shadow-palette species table**: material + motion register per species (still / 6s breath /
  shimmer / none) with the dashed-vs-solid edge distinction load-bearing and stated (N-2 is
  amplitude-only).
