# T.W4.5 — ROWS.md (the checkpoint row ledger — the record W8's passes re-read)

**Assembled**: 2026-07-11 by the non-authoring delta-report agent, at post-remediation head
`1c0c143`. Every filed row from the three pass records is here — **landed with its commit,
booked BY NAME, or routed BY NAME; zero silent drops** (`T.W4.5.md §Hard gate` row 2).
Remediation lane merges: `e58a213` (boot-atmosphere-blob: `baed206` + `af18e07`) · `1c0c143`
(picker console: `5b79cb7`); the C-6 ledger row landed directly on `tranche-t` (`4e5165b`).
Pass records: `passes/boot-atmosphere-blob.md` (A) · `passes/ladder-neutrals-ink.md` (B) ·
`passes/picker-seams-oracles.md` (C). The owner-facing narrative is `REPORT.md`.

## The ledger

| Row | Surface | Finding (one line) | Disposition | Outcome — landed commit / booked-to / routed-to |
|---|---|---|---|---|
| **R1** | boot-atmosphere-blob | Returning-dark scheme flash — the fouc-guard predicate dropped the `'auto'` arm; every dark-preference return visit booted light and re-themed mid-boot (D3/F-6 breach, invisible to the first-visit oracle slate) | LAND-NOW | **LANDED** `baed206` (merged `e58a213`) — `demo/color-picker/index.html` guard mirrors the producer's darkModeSyncScript semantics; lockstep clause in `boot/ground.ts` header; live-probed pre-module both arms |
| **R2** | boot-atmosphere-blob | The blob boots at 0.35× backing resolution (T-30 blur, demo-root REOPENED) — engine presizes from gBCR mid-`blob-emerge` pose; idle park froze the low-res frame; the W4 "demo-root cleared" verdict measured the awake state | LAND-NOW | **LANDED** `af18e07` (merged `e58a213`) — `HeroBlob.vue` drives the producer's existing re-measure seam (pause()/resume() at emerge `animationend`, name-filtered, park-guarded, PRM-safe); probed post-fix: backing 360×360 = ratio 1.998 full-res, park freezes full-res; stale verdict comments amended in `overture.css` + `seat.css`; the P6 producer rider (interior shading + resting halo) STANDS as residual softness. Oracle note: an o12 boot-time backing-ratio leg is a bounds expansion → triumvirate, W8's call |
| R3 | boot-atmosphere-blob | T-26 neutral-seed "sage whisper" imperceptible — `vividness(0)=0` renders the achromatic field dead-flat (D4 says whisper, never silence) | BOOK-W8 | **BOOKED → T.W8** (bracket flag; frames archived; a whisper floor is an in-bracket calibration for the deep wave; owner-judged). REPORT §6.3 |
| R4 | boot-atmosphere-blob | Dark-scheme field is light-band (standing GAP-L2) + NEW: the picker plate visibly re-materializes rose→cocoa at field arrival in dark | PRODUCER-PACKET | **ROUTED → the standing P1 row (GAP-L2 / dark `lBand`)**; the plate-flip observation appended for the W7 verify-at-cut re-judge; no demo action (PR-2 fence) |
| R5 | boot-atmosphere-blob | The rail's α inks as a near-twin of the a* letter at rail scale (fallback face — Fraunces carries no Greek) | BOOK-W8 | **BOOKED → T.W8** (eye item with cure options: donor-Greek subset into the self-cut face via the `build-fraunces-tnum.py` precedent · distinct styling rung · P10 ask). NOTE: pass C read the same glyph as distinguishable — the two-pass split rides to W8 unreconciled (REPORT §6.5) |
| R6 | boot-atmosphere-blob | `FIRST_VISIT_GROUND` light stop-3 lockstep drift (`#ffd0c8` constant vs `#ffcfc8` live write; sub-JND but breaks the by-construction claim) | LAND-NOW | **LANDED** `baed206` (merged `e58a213`) — all three lockstep sites regenerated (`ground.ts` constant · `index.html` mirror · the `--saved-bg-3` @property initial-value); probed byte-identical |
| R7 | boot-atmosphere-blob | T-30 centre-ward taste axis — containment delivered; bead reads contained-but-corner-pinned; the `--blob-seat` positive domain is the sanctioned bracket | BOOK-W8 | **BOOKED → T.W8** (flagged with dpr2 frames; re-take the read on the now-crisp bead post-R2; Q3 "Flush." untouched). Same bracket as C-3 — ONE W8 ruling covers both. REPORT §6.1 |
| R8 | boot-atmosphere-blob | Boot settle past the Q2 bracket upper on the dev instrument (cold ≈2.5s; returning 1.79–1.92s vs [1.4,1.7]s) — instrument-caveated; cadence crisp | BOOK-W8 | **BOOKED → T.W8** (the Q2 annex bracket read with before/after screencasts) **+ the W7 payload chain** (the O-5 spike / RP-2 cite already routed). REPORT §6.6 |
| R9 | boot-atmosphere-blob | T-32 demo half landed (count 6 + scattered, verified live); 3–5 masses discern by eye, not six — the count-ceiling is producer-side (MAX_NUCLEI=6 clamp) | PRODUCER-PACKET | **ROUTED → the standing T-32 MAX_NUCLEI ceiling book** (opened at W2 close → P1 / W7 re-judge); no new demo action |
| R10 | boot-atmosphere-blob | The dev:web-only red banner dominates the overture's first read in the owner's own environment (overlaps the dock band) | BOOK-WAVE | **BOOKED → T.W6 (W6-6)** — T-9's banner death + lamp re-home, already W6's row; this row is the live-urgency confirm; no W2-surface action |
| **B-1** | ladder-neutrals-ink | T-31 mid-read: occlusion NOT reproducible at this head (17 probed states, 0 hits; dock z 40); the class survives structurally (overlay+padding simulation · 0–1px worst-case clearance · paint priority single-pointed on the producer `z-dock → --z-index-dock → --z-dock` chain) | BOOK-WAVE | **BOOKED → T.W6 (Lane D)** with the sharpened structural spec (pass B §3: two-band grid `auto 1fr`; nav in-flow; fixed overlay + `--dock-total` reservation + load-bearing `z-dock` retire together; designed gap token; portaled menus keep chrome z; ultra-wide pin re-confirmed in-band). No z-patch filed; no re-litigation |
| **C-1** | picker-seams-oracles | The α row's channel-meter never inks — a positioned thumb beside a BLANK meter at the shipped boot default (α=0.827); fails W4-3's own gate text + T-4 by eye | LAND-NOW | **LANDED** `5b79cb7` (merged `1c0c143`) — `ComponentSliders.vue` meterText: alpha formats from `denormalizedCurrentColor` on the SAME injected `COLOR_MODEL_KEY` pipeline at the space's least count (82.7% lab / 83% integer spaces); hex single-cell mode guarded untouched; header tuple untouched (C-2's bracket) |
| C-2 | picker-seams-oracles | The header TUPLE omits a live non-1 alpha — `92.0%, 88.8, 20.0` does not round-trip at α=0.827; T-7 verbatim "or more if needed"; but a 4th cell re-derives the O-10b line-lock tables + the Q11b capacity derivation | BOOK-W8 | **BOOKED → T.W8** (design bracket flagged with the C-1/C-2 frames; a LIGHT remediation would silently re-derive a ratified lock set). Files named for W8: `display/ColorComponentDisplay/*` · `useColorPipeline.ts` · `readoutReservation.ts`. REPORT §6.4 |
| C-3 | picker-seams-oracles | T-30 residual: "more towards the centre" — bead wholly on-card + integrated but corner-flush at the ruled default; the knob's positive domain is owner taste | BOOK-W8 | **BOOKED → T.W8** (bracket FLAGGED, never ruled; annotated frames `pi/w45/picker/*-seat.png` vs the owner's `t-30-blob-blurry-topright.png`; Q3 untouched). Merges with R7 — ONE bracket |
| C-4 | picker-seams-oracles | The tnum-mint face (the self-cut Fraunces set) — clean by eye at both weights, but a house-cut font artifact the owner has never seen named | BOOK-W8 | **BOOKED → T.W8** (the flag IS the deliverable — the certification package presents it; `scripts/fonts/build-fraunces-tnum.py` + the `index.html` preload hunk). REPORT §6.2 |
| C-5 | picker-seams-oracles | MOB-1 stands OPEN (the one W1 §Hard-gate FAIL): `data-layout` witness absent; the aspect-law exception live (cite drifted `:436` → `style.css:508`, updated here); the D8-1 ratified-vs-ratified conflict is a layout-law adjudication | BOOK-WAVE | **BOOKED → T.W6 (Lane D)** — band-layout work where T-31's dock-atop law also lands; the D8-1 conflict adjudicated there; flagged in REPORT §6.8. Files named: `demo/@/styles/style.css` · `demo/color-picker/App.vue` |
| C-6 | picker-seams-oracles | The PI-1 W4 delta row still absent from the ledger — the W4-close MISS-RECORDED obligation, precondition (branch pushed) discharged | LAND-NOW | **LANDED** `4e5165b` (direct on `tranche-t`) — CI run `29137806195` / `06b793c`: LCP 5141 / TBT 5988, medians FLAT vs W2/W3, honestly RED; the W4-close miss is now discharged |

## Tallies

- **17 rows filed → 17 dispositioned**: 5 LAND-NOW (R1 R2 R6 C-1 C-6 — ALL landed+merged) ·
  7 BOOK-W8 (R3 R5 R7 R8 C-2 C-3 C-4) · 3 BOOK-WAVE→T.W6 (R10 B-1 C-5) · 2 PRODUCER-PACKET
  (R4 R9 — standing P1/T-32 rows, no new dispatch needed; the letter is already out).
- **Zero silent drops · zero rows contradict a §12 RULED outcome or a §4 retirement ·
  nothing routed OWNER** (each pass attests this in-record).
- The 7 BOOK-W8 rows + the two flagged non-row items (rest-floor in-bracket read ·
  dark-scheme field/chrome split) are enumerated for the W8 package in `REPORT.md §6`.
- **EXPECTED-RED oracle rows ride unaltered** (not checkpoint rows; recorded for the W8
  re-read): O-5 spike leg (RP-2 → W7) · O-16 R1 (PKT-1, producer) · O-26 headless leg
  (headed annex GREEN). No gate weakened.
