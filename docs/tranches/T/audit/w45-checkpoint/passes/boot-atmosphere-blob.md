# T.W4.5 · PASS A — boot overture + atmosphere + blob emerge (W2's landed surface) + THE T-30/T-32 RE-JUDGE

**Surface**: the W2 boot lane — B0–B4 choreography · gradient ground · T-26 field · emerge beat —
plus the `[OWNER-2026-07-10]` T-30/T-32 re-judge against the landed round-2+W4 state and the two
W4-booked eye items in scope (the rail's α-glyph fallback face · the boot screencast read).
**Filed**: 2026-07-10, at head `06b793c` (post-W4-close). Probe-only — this record is the pass's
only write.
**Law consumed**: `RATIFICATION-2026-07-09.md §0` (Q2 "full comp." · Q3 "Flush." · Q6 "both.") →
`MANDATE-2026-07-06.md §0` + §0.1/§0.3/§0.5 → `SYNTHESIS.md §2` D3/D4/D8 → `waves/T.W4.5.md`.
Anchors re-derived via `audit/w1-move-map.md` C8 (the boot chain =
`demo/color-picker/composables/boot/`). Baseline frames: `audit/w2-close-artefacts.md` +
`audit/pi/w2/`; T-30 dispositions: `audit/w4-close-artefacts.md §5`.

## §0 Probe environment (the O-3 class — driven LIVE)

- Lane-unique ports: `VJS_E2E_PORT=8497` / `VJS_E2E_PERF_PORT=8498` (owner's :9000 untouched);
  vite dev on :8497 + the built `dist/gh-pages` bundle for the perf legs.
- **Headed real GPU**: ANGLE Metal, Apple M5 Max — cold + returning loads, BOTH schemes;
  green/warm/neutral seed matrix ×2 schemes (+10s migration pairs); T-30 seat/hover crops at
  dpr1 AND dpr2; rail zoom at dpr2. Frames archived on-disk (scratchpad; the S π convention —
  probe artefacts, not package inputs).
- **Oracle floor re-run at this head** (independent, not the lane's word): O-1 · O-1b · O-2 ·
  O-4 (1× + 6×) · O-12 (3/3) · blob-390 · O-24 (both schemes, built bundle) · atmosphere-cold-load
  → **15 passed**; the two born-RED legs (O-5 spike 425 ms = 12.7× median; O-26 headless 10s
  migration 0.000/255) reproduced exactly as `test.fail()`-designed, W7 cites intact. O-6 vitest
  6/6. No gate weakened; the reds ride into this record as reds.

## §1 Coherence verdict (PRE-FILTER only — no taste certified)

**COHERENT — the overture's gestalt lands: one material from t0, beats open by gating (order held
at 1× and 6×), the ground is the derived gradient (no slab, no gray crossfade), the field lives
(10s migration unmistakable by eye on real GPU), the bead emerges and seats.** Two findings are
**mechanical breaches of the design's own laws** (rows R1/R2 — the returning-dark scheme flash and
the emerge-pose resolution race), not incoherence; the rest are flagged brackets and standing
books. The frontend-design bar: the cold-light boot reads as a designed choreography — ground →
plates land with the cartoon half-degree → instrument inks in progressively → the bead goo-emerges;
the dark story is undermined by R1 (returning loads) and the known GAP-L2 light-band field.

## §2 What was judged, item by item

1. **B0–B4 choreography, cold, both schemes** — LIVED. Real-GPU marks: light
   `b0@13 → b1@335 → b3@759 → b2@1605 → b4@1622`; dark `b0@9 → b1@264 → b3@662 → b2@1087 →
   b4@1094`. Order invariant under 6× (re-run). The screencast read (frame series 60ms cadence):
   no five-clock smear, no About pop, no first-frame blob; the plate-land family + shadow cast-in
   read as one clock. The mid-boot empty-spectrum window (~300–700ms, plate landed / instrument
   not yet inked) reads as a designed skeleton beat, not a flash.
2. **Returning loads** — beats faster (light `b2@1018`, dark `b2@887`); ground vars persist and
   re-paint pre-module. **BUT the returning-DARK load boots in the LIGHT scheme's material and
   flips mid-boot — row R1** (the pass's highest finding; mechanism + fix below).
3. **Gradient ground honesty** — HONEST on first-visit both schemes: first paint IS the banded
   gradient (dark first paint = the dark constants; F-6 held on true first run); the persisted
   record round-trips `{stops, scheme, deriveVersion}` stops-only; root vars re-read correct after
   settle. One lockstep drift: the live light derive writes stop-3 `#ffcfc8` vs the constant
   `#ffd0c8` (row R6, sub-JND).
4. **T-26 field presence/perceptibility** — PRESENT and ALIVE at mid/high chroma: the green seed
   is unmistakably energetic with visible composition; 10s-apart pairs show clear migration by eye
   (the headed O-26 read stands). The **neutral seed reads dead-flat gray** — the D4 "sage
   whisper" is not perceptible by eye (row R3, bracket flag). Dark scheme: the field is the
   light band (GAP-L2, standing P1 book) — row R4 re-cites with a NEW observation: the picker
   plate visibly re-materializes (rose → cocoa) at field arrival in dark, the ambient-lightness
   leap made visible through the D6 live referent. Root cause is the same producer gap.
5. **The emerge beat** — the bead goo-scales from the seat (mid-emerge crescent captured at
   dark 1729ms), never a first-frame pop; the one-shot release law holds (no squatting animation;
   nav clickable). **BUT the emerge pose poisons the engine's init resolution — row R2.**
6. **T-30 RE-JUDGE (owner order)** — two axes:
   - **Seat/integration**: containment DELIVERED on the live tree — anchor exactly flush
     (`--blob-seat: 0`, Q3 verbatim), wholly inside the plate (anchor right/top ≡ card right/top;
     bead edges ~27px inside), contact shadow present, tooltip re-seated below, hover mood live
     (frame-diff floor re-verified via O-12 3/3). By eye the bead reads *contained and seated but
     corner-pinned*; "more towards the centre" is satisfied only in the containment sense. The
     knob's positive domain (the W4-close-named W4.5 taste axis) is a bracket — FLAGGED, not
     turned (row R7; judge AFTER R2 lands, a crisp bead reads differently).
   - **The BLUR**: **the W4 "demo-root CLEARED" verdict does NOT stand on the live tree** —
     measured at dpr2: the canvas backing store boots at **126×126 for a 180.2px box (ratio
     0.699 ≈ 0.35×dpr)** and the idle park FREEZES that low-res frame; a wake re-measures to
     **360×360 (ratio 1.998, full-res)**. The number is exact: 180.2109375 × 0.35 = 63.07 →
     the engine sized its backing from the gBCR **mid-emerge** (`blob-emerge` `from { scale(0.35) }`)
     and never re-measures until a wake. The owner's "blurry" is real, demo-root, and boot-path
     (the W4 forensic measured the awake state). Row R2 = the demo cure; the P6 producer cites
     (hero-scale interior shading + resting drop-shadow halo) remain the RESIDUAL softness after
     the resolution fix — the packet rider stands, amended.
7. **T-32 RE-JUDGE (owner order)** — the demo-reachable half is LANDED and verified live:
   `zones: { count: 6, arrangement: "scattered" }` (`aurora-atoms.ts:56`), O-6 rows green. By eye
   across seeds: the scattered prior reads as an irregular, composed field (a real improvement on
   the rule-of-thirds wash), but **3–5 soft masses discern, not six distinct glows** — softmax +
   medium blur merge neighbours. "A few more zones" is partially delivered at the shipped ceiling;
   the **MAX_NUCLEI=6 count-ceiling producer book (opened at W2 close) stands** — row R9 routes to
   the packet, no new demo action.
8. **The α-glyph fallback face (W4-booked eye item)** — judged at dpr2 zoom: the rail inks
   `L a b α` in Fraunces italic with the α falling to the metric-tuned fallback — **the α reads as
   a second lowercase 'a' at rail scale**; the a*/α disambiguation intent (D5's A-collision kill)
   does not land by eye (row R5, flagged with cure options).
9. **The boot screencast read (W4-booked eye item)** — read over the captured frame series, both
   schemes, cold + returning: the cadence is crisp (no freeze-then-pile-up; O-5 pile-up leg green
   at 4.1%→6.7% dropped, spike leg honestly red with its W7 cite). Settle lands past the Q2
   bracket upper on the DEV instrument (cold light b2-start 1.6s → field-in ≈2.5s; returning
   1.79–1.92s vs the [1.4, 1.7]s bracket) — dev-transform noise is real (built-bundle O-24 dark
   ran b2-class work at ~1.3s), so this rides as a FLAGGED bracket with numbers, not a red
   (row R8; the W8 Q2 annex + the W7 payload chain own the instruments).
10. **Environment note** — in the owner's own environment (dev:web-only :9000) the red
    DevMisconfig banner dominates the overture's first read and overlaps the dock band; its death
    is the standing **W6-6** book (T-9) — confirmed live urgency, row R10. T-31 (About card
    composites over the dock) also CONFIRMED LIVE at this head — already W6's law row; no new row
    (routing trace only).

## §3 THE ROWS (every row landed-or-booked by the wave's remediation lanes; zero silent drops)

| # | Finding | Severity | Disposition | Files / route |
|---|---|---|---|---|
| **R1** | **Returning-dark scheme flash**: the fouc-guard predicate `stored === 'dark' \|\| (!stored && prefersDark)` mishandles the value the app itself always persists (`vueuse-color-scheme = "auto"`): probed live — post-reload pre-module state `darkClass:false, --saved-bg-0:#b37290, color-scheme:light` under prefers-dark, then the app corrects (`darkClass:true, #673255`) — EVERY dark-preference return visit boots light and re-themes mid-boot (D3 "one material from t0" + F-6 breached; O-2/O-1b run first-visit so the slate cannot see it) | HIGH | **LAND-NOW** | `demo/color-picker/index.html` (the guard predicate mirrors useColorMode: `stored === 'dark' \|\| ((stored === 'auto' \|\| !stored) && prefersDark)`); lockstep note in `boot/ground.ts` header |
| **R2** | **The blob boots at 0.35× resolution (T-30 blur, demo-root REOPENED)**: the engine sizes its backing store from gBCR during the W2-4 `blob-emerge` `scale(0.35)` pose (measured dpr2: backing 126 vs full 360; exact 180.2×0.35=63); the idle park freezes the low-res frame; only a wake re-measures (ratio → 1.998). The W4 demo-clear verdict measured the awake state | HIGH | **LAND-NOW** | `demo/color-picker/composables/boot/overture.css` + `demo/@/components/custom/color-picker/visual/HeroBlob.vue` (force the engine re-measure at emerge `animationend` via an existing seam — the paused/wake path proves one — or re-shape the emerge so the measured element is never mid-transform at init); amend the stale "demo-root cleared" record in `demo/@/components/custom/color-picker/seat.css` comment; the P6 rider keeps the RESIDUAL interior-shading/halo cites. Oracle note: extending o12 with a boot-time backing-ratio leg is a bounds expansion → triumvirate per the wave law |
| R3 | T-26 neutral-seed "sage whisper" imperceptible: `vividnessForSeedChroma(0)=0` renders the achromatic field dead-flat by eye (the D4 intent says whisper, never silence) | MED | **BOOK-W8** | bracket flag (thresholds are defaults-with-brackets, owner-judged); frames archived; a whisper floor is an in-bracket calibration for the deep wave |
| R4 | Dark-scheme field is light-band (standing GAP-L2) + NEW observation: the picker plate visibly re-materializes rose→cocoa at field arrival in dark (the ambient leap through the D6 live referent) | MED | **PRODUCER-PACKET** | the standing P1 row (GAP-L2/lBand); the plate-flip observation appended for the W7 verify-at-cut re-judge — no demo action (a demo fork would violate PR-2) |
| R5 | The rail's α inks as a near-twin of the a* letter at rail scale (fallback face; Fraunces carries no Greek) — the disambiguation intent does not land | LOW | **BOOK-W8** | eye-item flagged with cure options: subset a donor Greek α into the self-cut face (`scripts/fonts/build-fraunces-tnum.py` precedent) · distinct styling rung · P10 ask; a type-authority call, not a LIGHT-round turn |
| R6 | `FIRST_VISIT_GROUND` light stop-3 lockstep drift: constant `#ffd0c8` vs the live first-visit sink write `#ffcfc8` (sub-JND; violates the "constant ≡ first write BY CONSTRUCTION" claim) | LOW | **LAND-NOW** | `demo/color-picker/composables/boot/ground.ts` + `demo/color-picker/index.html` (regenerate both literals; same single-writer files as R1) |
| R7 | T-30 centre-ward taste axis: containment delivered; the bead reads contained-but-corner-pinned; the `--blob-seat` positive domain (the W4-close-named W4.5 taste axis) is a bracket the checkpoint FLAGS, not turns — and the read should be re-taken after R2 (a crisp bead seats differently) | MED | **BOOK-W8** | flagged with dpr2 frames for the owner's eye at the certification package; Q3 "Flush." ruling untouched |
| R8 | Boot settle past the Q2 bracket upper on the dev instrument (cold ≈2.5s; returning 1.79–1.92s vs [1.4, 1.7]s) — instrument-caveated (dev transform; built-bundle legs run faster); the cadence itself is crisp | LOW | **BOOK-W8** | the Q2 annex bracket read with before/after screencasts (W8 package) + the W7 payload chain (O-5 spike cite already routed) |
| R9 | T-32 verdict: demo half landed (count 6 + scattered, verified live); 3–5 masses discern by eye, not six — further zone legibility is the count-ceiling book | LOW | **PRODUCER-PACKET** | the standing T-32 MAX_NUCLEI ceiling book (W2 close → P1/W7 re-judge); no new demo action |
| R10 | The dev:web-only red banner dominates the overture's first read in the owner's own environment (and overlaps the dock band) | MED | **BOOK-WAVE** | **W6-6** (T-9's banner death + lamp re-home — already booked; this row is the live-urgency confirm; no W2-surface action) |

**No row contradicts a §12 RULED outcome or a §4 retirement** (R1/R2 are breaches of the ruled
design's own laws; R7 explicitly leaves Q3 "Flush." standing and flags only the sanctioned
positive-domain bracket; R4/R9 ride their standing producer books). Nothing routes OWNER here.

## §4 Evidence index

- Oracle re-runs: §0 (this pass's own runs at 8497/8498, head `06b793c`).
- Probe measurements (in-body above): beat marks cold/return × light/dark · the reload
  fouc-guard state pair (R1) · the dpr1/dpr2 backing-store triplets early/parked/awake (R2) ·
  seat geometry (anchor ≡ card corner, `--blob-seat: 0px`, `--blob-fp` clamp resolve) ·
  rail glyph computed font census (all four = Fraunces stack italic; the α falls through).
- Frames (scratchpad, gitignored-class): cold/return series ×2 schemes · seed matrix ×2 schemes
  ×(t0, t+10s) · t30 seat/hover crops (dpr1+dpr2) · rail zoom dpr2.
