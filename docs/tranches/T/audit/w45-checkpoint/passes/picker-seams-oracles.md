# T.W4.5 — PASS C: the picker recomposition (W4) · the W1 colocation seams · the W0 oracle-floor state

**Pass**: critique pass C (`picker-seams-oracles`) — LIGHT, ONE round, coherence PRE-FILTER only
(never a taste certification; brackets FLAGGED, never ruled).
**Filed**: 2026-07-11, at head `06b793c` (docs-only above the W4 close head `388c4a9` — the
critiqued code state IS the W4-close state).
**Law honored**: `RATIFICATION-2026-07-09.md §0` (Q11a ×φ ladder-authority · Q3 "Flush." · Q4
"The well." · Q11b defaults) → `MANDATE-2026-07-06.md §0` + addenda **§0.5 (T-30/T-31/T-32
re-judged against the landed round-2 state)** → `SYNTHESIS §2` D1–D10 → `waves/T.W4.5.md`.
Anchors re-derived via `audit/w1-move-map.md` (PP-11); baselines read first:
`w4-close-artefacts.md` + `w4-knot-lane-record.md` + `w1-close-artefacts.md` +
`w0-close-artefacts.md §2` (the born-RED table).
**Method (O-3 probe class, probe-only)**: the live tree served on the lane-unique port **:8520**
(vite, `VITE_API_URL` same-origin; the owner's :9000 untouched; no sibling port reused). Driven
at **1440×900 / 768×1024 / 390×844 × light + dark** (6 cells, dpr 2, SwiftShader args = the
suite's own class). Frames on-disk (the S π convention, `*.png` gitignored):
`docs/tranches/T/audit/pi/w45/picker/{1440,768,390}-{light,dark}-full.png` + `-seat`/`-console`/
`-readout` closeups + the two interaction shots (`-console-active`, `-console-alpha`). Computed-
style + geometry probes and two interaction probes (rail select · alpha keyboard decrement); NO
oracle re-mint, NO gate re-run beyond the live drive (the W4 close independently re-ran the full
mint set at `388c4a9` 12h prior; zero code commits since).

**Console noise**: **zero console errors / zero pageerrors across all 6 cells** (fresh contexts).

---

## §1 Coherence verdict: **COHERENT** — the recomposed picker reads as ONE instrument

At every cell the picker resolves into the D5 gestalt: display-voice title band (bead seated
beside it), the hero tuple, the specimen plate, the rung-2 console WELL (rail + sliders +
meters). One neutral family both schemes; color appears only as data (sliders/spectrum/dot —
C3-legal). The six filed rows below are the pre-filter's catches; none breaks the gestalt.

## §2 What was judged, with evidence

1. **×φ titles (W4-1 / D2 / Q11a)** — CLEAN. Live-probed: picker `.space-trigger` = Fraunces
   italic **400** at **67.776px ≡ `--type-display-3`** (@1440); pane title **41.888px ≡
   `--type-display-1`**, weight 400 (About-700 bug dead — weight carried on the surface's own
   class list); fluid mid-band 55.04px @768, 43.7px @390 = exactly the producer clamp; tokens
   producer-owned, zero demo mints. Host-independence corroborates O-10a (12/12 at W4 close).
2. **The tuple at both bands (W4-2 / Q11b)** — CLEAN as landed (but see rows C-1/C-2 on the
   ALPHA axis). 54.65px display-4-class @1440+768; the 2.618rem floor pins @390;
   `font-variant-numeric: lining-nums tabular-nums` LIVE on the shipped tnum face; cells
   intrinsic + contiguous (inter-cell gap ≈ 0.47em ≈ the 0.75ch packing constant); lab's honest
   2-line lock at 390 (`--readout-lines: 2`), zero overflow, `--readout-fit` 1 (no shave needed).
   The int-strong/frac-muted ink demotion reads instrument-grade by eye (O-18-certified rungs).
3. **The console WELL + rail + strip (W4-4+3 / Q4 / D5)** — CLEAN (one row, C-1). The well is a
   true rung-2 tone-step both schemes — computed `oklab(0.913…)` light / `oklab(0.345…)` dark,
   **`backdrop-filter: none`** (never a second pane of glass), one `.console-well` home. The
   rail inks TRUE glyphs `L a b α` in the seal-recipe portrait ring; interaction-probed: click
   `a` → `aria-selected` flips, **exactly ONE `.rail-dot` (WatercolorDot)** appears, roving
   tabindex goes 0/-1/-1/-1 — the ONE-active-indicator + a11y grammar holds live (at rest no
   channel is selected and no dot shows — quiet-instrument grammar, coherent). L/a/b meters ink
   live values in the mono caption voice.
4. **The seat (W4-5 / D8 / Q3 / T-30)** — CLEAN structurally; T-30 re-judge in §3. Live at all 6
   cells: `--blob-seat: 0` (Q3 "Flush." verbatim), `--blob-fp: clamp(7rem, 22cqi, 11rem)` ONE
   formula (112.6px @512 pane; 112px @390), **wrapper wholly ⊂ card at every cell** (containment
   identity), only the transparent 1.6× canvas overscan (180.2px) crosses the edge by design;
   `z-index` = the named `--z-ornament` (20); `elementFromPoint` dock samples never the blob;
   contact shadow seats the bead on the plate.
5. **The veil (W4-7 / O-11 geometry)** — CLEAN. Live rest veil on `main .pane-header::before` =
   **0.52 ∈ [0.45, 0.65]** both schemes @1440 (the ratified 27–39% EFFECT bracket's landed
   point); the header band reads shaded at rest by eye (T-23 honored); no double-exposure
   observed on the default pane. Corroborates O-11 22/22 at W4 close.
6. **The two W4-booked eye items** —
   - **About title ≥1440**: inks a balanced 2-line — "About the color / spaces, Lab ⌄" — no
     widow, the space member deliberate on line 2. **PASS by eye; the book discharges.**
   - **The α glyph face**: the rail's `α` inks in the metric-tuned fallback (Fraunces carries no
     Greek). By eye at rail size it is distinguishable from the Fraunces italic `a` above it
     (open bowl + tail), both schemes, 390 included — **acceptable as landed**; if the owner
     wants a stronger α, that is the P5 letter-rail primitive's row (already booked). No row.
   - **The tnum mint face**: even digit columns by eye at w300+w600 weights across the tuple —
     the self-cut face reads clean; **flagged to the owner's W8 attention per the lane's own
     note** (row C-4 — the flag is the deliverable, not a defect).
7. **The W1 colocation seams (structural spot-check, greps — not a re-audit)** — CLEAN (one
   standing book, C-5). Re-run at head: `export *` = **0** across `src/ demo/@/ api/src/
   demo/color-picker`; the 10-path old-shim spot set (api index, parsing/{color,stylesheet,
   easing}, units/color/{gamut,boundary}, models, atomdiff, the two keys.ts) **all absent — zero
   forwarding stubs**; barrels named-re-exports-only; the boot chain lives at
   `demo/color-picker/composables/boot/` exactly per the MOVE-MAP (the W2/W4 waves edited it
   in-place — the seams held under two rounds of design work).
8. **The W0 oracle-floor re-read (evidence read; NO new mints)** — the floor is honest and
   current. **Born-RED standing (verified `test.fail()` in-tree)**: **O-5 spike leg**
   (`e2e/smoke/perf/o5-boot-pacing.spec.ts:48` — cadence leg CURED at W2 (median 58.3ms,
   pile-up dead), the eager-payload spike stands EXPECTED-RED, RP-2 cite → the W7 payload row) ·
   **O-16 R1** (`o16-computed-cascade.spec.ts:34` — the PKT-1 producer dist 150ms clobber;
   letter DISPATCHED at W0-1; flips the day the producer lands) · **O-26 headless leg**
   (`o26-aurora-perceptibility.spec.ts:57` — the headed annex re-drove GREEN at W2 close,
   `pi/w2/headed-annex-log.txt`). **Flipped with cites**: O-2 + O-4 (born-GREEN-pending-W2 →
   GREEN at W2-1/W2-3, `w2-close-artefacts §1`) · O-1/O-1b/O-24 (W2) · O-7 armed + green at
   W3-1 (`b608b5f`) · O-9 minted green (W3-2) · O-18 10/10 at W3-5 → the 3 W4 fixme rows flipped
   → **22/22** (W4-4+3) · O-10a–e/O-10d/O-11/O-12 minted green at W4. **O-25** stands as the CI
   prod-lineage guard (live referent `0441aba` via X1); **O-3** headed-annex cadence next fires
   W7/W8. The EXPECTED-REDs ride into the delta-report AS reds with their packet cites,
   unaltered (no gate weakened, no softening).

## §3 The MANDATE §0.5 re-judge against the landed round-2 state

- **T-30 (the bead)** — **substantially CURED; one flagged bracket rides (row C-3)**. Against
  the `c63f9aa` state the owner audited (half-off-card top-right, overlapping the About seam,
  blurry): the bead now sits **wholly inside the plate's title band** (edge 0.24·fp ≈ 27px
  inside both edges, all 6 cells), contact-shadow seated, `--z-ornament` above content, never
  the dock. **The blur percept is demo-cured by eye** — defined silhouette + specular + the
  grounding shadow (frames `*-seat.png`, esp. dark cells where the bead pops); the residual
  featureless interior at hero scale is the PRODUCER-root P6 rider already routed
  (`w4-close-artefacts §5`, rides W7's verify-at-cut). **The residual owner axis**: "more
  towards the centre" — the landed default keeps the wrapper corner-flush (Q3 "Flush." = 0; the
  ruling STANDS untouched); the knob's sanctioned POSITIVE domain (deeper into the card) is
  exactly the taste bracket the knot record named for this checkpoint. A LIGHT pass does not
  rule taste → FLAGGED with annotated frames for the owner at W8 (row C-3).
- **T-31 (dock occlusion)** — **corroborating evidence only; the row is pass B's + W6's**: at
  rest, at all 6 cells, `elementFromPoint` over the dock band lands IN the dock and **zero card
  rect intersects the dock band** (cards begin 151/223/155px down vs the dock's 8–74px band) —
  the occlusion class did not reproduce in this probe. The structural dock-atop BAND law
  remains W6 Lane D's (un-landed surface — not critiqued here, per the fence).
- **T-32 (aurora zones)** — ANSWERED-YES + landed as the W2-5 calibration knob inside the T-26
  bracket; the atmosphere pass (A) owns its re-judgment. No row here.

## §4 Filed rows (every row landed-or-booked BY NAME — zero silent drops)

| ID | Finding | Disposition | Rationale |
|---|---|---|---|
| **C-1** | The α row's channel-meter NEVER inks — `meterText` returns `""` because the formatted-cell map carries no alpha (`colorComponents` filters `key !== "alpha"`, `useColorPipeline.ts:120`), while the boot DEFAULT itself is `lab(92% 88.8 20 / 82.70%)` (`color-picker/index.ts:36`): a live α = 0.827 shows a positioned thumb beside a BLANK meter, next to three inked meters. Fails W4-3's own gate text ("live meter present per row") + T-4 (the dynamic bottom area) by eye — an instrument row with no datum. | **LAND-NOW** | Small + in-bounds: format alpha from the SAME injected model (zero new state, the W4-3 law); the meter ink rung already O-18-certified. Files: `demo/@/components/custom/color-picker/controls/ComponentSliders/ComponentSliders.vue`. Contradicts no ruling. |
| **C-2** | The header TUPLE omits a live non-1 alpha — at the shipped default (α = 0.827) the readout inks `92.0%, 88.8, 20.0`, which does not round-trip (re-parsing yields the OPAQUE color). The owner's T-7 verbatim: "x, y, z **(or more if needed)**" — α < 1 is the needed case. BUT alpha-in-tuple re-derives the O-10b line-lock tables + the Q11b capacity derivation (a 4th cell changes per-space locks at the phone band) — too large for a LIGHT round, and whether α belongs in the hero tuple at all is a design bracket Q11b never ruled. | **BOOK-W8** | Flagged as a bracket (with the C-1/C-2 frames) for the W8 ruling; a remediation here would silently re-derive a ratified lock set. Files (named for W8): `demo/@/components/custom/color-picker/display/ColorComponentDisplay/*`, `demo/@/composables/color/useColorPipeline.ts`, `.../readoutReservation.ts`. |
| **C-3** | T-30 residual: "more towards the centre" — the bead is wholly on-card + integrated (cured axes) but corner-flush at the ruled default (`--blob-seat: 0`); the knob's sanctioned positive domain (deeper into the card) is an owner-taste bracket this LIGHT pass must not rule. | **BOOK-W8** | Bracket FLAGGED, never ruled (the wave's own prohibition); annotated before/after frames ride the delta-report (`pi/w45/picker/*-seat.png` vs the owner's `t-30-blob-blurry-topright.png`). Q3 "Flush." untouched. Files: `demo/@/components/custom/color-picker/seat.css`. |
| **C-4** | The tnum-mint face (the self-cut Fraunces `.tf` set) — clean by eye at both weights, but it is a HOUSE-CUT font artifact the owner has never seen named; the W4 lane's own book demands the mint be flagged for the owner's W8 attention. | **BOOK-W8** | The flag IS the deliverable (no defect found); W8's certification package presents it. Files: `scripts/fonts/build-fraunces-tnum.py`, `demo/color-picker/index.html` (the preload hunk). |
| **C-5** | MOB-1 stands OPEN (the one W1 §Hard-gate FAIL, routed): `data-layout` witness still absent (`grep -rn data-layout demo/` = 0); the aspect-law display exception still live — NOW at `demo/@/styles/style.css:508` (drifted from the W1-close cite `:436` under the round-2/3 edits; the cite updates here). The D8-1 ratified-vs-ratified conflict (`App.vue` "producer-owned; never demo-cured here" vs T's MOB-1 demo-cure) is named in-tree and is a layout-law adjudication, not a mechanical fix. | **BOOK-WAVE (T.W6)** | W1-close routed it "to the T.W4.5 checkpoint / W6 surfaces & shell"; the cure is band-layout work owned by W6 Lane D (where T-31's dock-atop law also lands) — adjudicating D8-1 there, with the conflict flagged in the delta-report. Files: `demo/@/styles/style.css`, `demo/color-picker/App.vue`. |
| **C-6** | The PI-1 W4 delta row is STILL absent from `pi1-delta-ledger.md` (W0/W1/W2/W2-W3-CI rows only) — the W4-close MISS-RECORDED row remains unfired although its named precondition (the branch pushed; origin/tranche-t == local) is now discharged; only the manual `gh workflow run ci.yml --ref tranche-t` + the ledger append remain (completion-brief step 3). | **LAND-NOW** | Mechanical, no gate touched (W4 is not a named Q14 row — only W2/W7/W9 adjudicate); the checkpoint's own delta-report must carry the PI-1 mid-read, so the stamp should fire now. Expectation NOISE (`w4-close-artefacts §8`). Files: `docs/tranches/T/audit/pi1-delta-ledger.md`. |

## §5 Notes (non-rows)

- **Blob emerge latency under software GL**: every `-full.png` catches the bead mid-emerge (a
  faint smear) ~5s post-load, while the closeups seconds later show the fully-material bead —
  a SwiftShader-environment artifact of this probe class (the W2-sanctioned headed-GPU env
  class covers the real-hardware read); O-12/O-4 govern the behavior and are green. Not a row.
- **Dark-scheme ambient**: the dark cells composite dark stone cards over a still-light rosy
  ground (the dark FIRST_VISIT stops + the aurora's lightening arm) — striking by eye; the
  atmosphere is pass A's surface, noted for the assembler only.
- **Boot space**: the app boots in Lab (the default input's parsed space) though
  `DEFAULT_COLOR_SPACE = "oklch"` — the parse path re-selects the input's space; coherent
  behavior (the input IS lab), no row.
- The two probe scripts + JSON live in the session scratchpad (ephemeral by design;
  `w45-passC-probe.mjs` / `w45-passC-interact.mjs` / `w45-passC-report.json`); the frames
  persist on-disk at `docs/tranches/T/audit/pi/w45/picker/` per the S π convention.
- Tool-artefact grep over this doc: run at commit (M-1) — clean.
