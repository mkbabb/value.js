# T.W4.5 — THE DELTA-REPORT (the owner's mid-tranche window, W0 → W4)

**Register**: the S relay-letter register — **INFORMATIONAL**. This is the Q6 "both."
checkpoint's dispatch to the owner: what the tranche's first half landed, what the LIGHT
critique round found and cured, where the honest reds stand, and which brackets are FLAGGED
for the W8 ruling. **No certification is asked here** — no bracket table for ruling, no
verdict requested; the terminal certification package is W8's (`waves/T.W4.5.md`
§No-workaround prohibitions).
**Assembled**: 2026-07-11 by the NON-authoring delta-report agent (wrote no pass, no
remediation) at post-remediation head **`1c0c143`** (branch `tranche-t`; the two remediation
lanes merged `--no-ff` at `e58a213` + `1c0c143`).
**Law**: `RATIFICATION-2026-07-09.md §0` + `MANDATE-2026-07-06.md §0`/addenda (§0.5 =
T-30/T-31/T-32) VERBATIM → `SYNTHESIS.md §2` D1–D10 → `waves/T.W4.5.md`. Anchors re-derived
via `audit/w1-move-map.md` (PP-11).
**Inputs**: the three pass records (`passes/boot-atmosphere-blob.md` ·
`passes/ladder-neutrals-ink.md` · `passes/picker-seams-oracles.md`, filed at the W4-close
head `06b793c`) · the W0–W4 close artefacts (`w0-…w4-close-artefacts.md`) ·
`pi1-delta-ledger.md`. The row ledger W8 re-reads is `ROWS.md` (same directory).

---

## §1 What landed, W0 → W4 (vs the mandate's SYNTHESIS §1.2 findings map)

Five waves closed (W0 `complete` · W1 `complete_with_misses` · W2 `complete` · W3
`complete_with_misses` · W4 `complete_with_misses` — every `_with_misses` an honest recorded
book, zero silent drops; the close artefacts carry the row-by-row gates).

### §1.1 The §1.2 map rows TOUCHED through W4

| §1.2 row | Owner finding (one line) | Wave item | State at this checkpoint |
|---|---|---|---|
| T-1 | cold-load arrival out of sync | W2-1/W2-2 (+P1) | **LANDED** — hydration-before-derivation (O-1/O-2 green); the latent pink flash dead structurally; producer arm-gap half rides P1 |
| T-2 | titles ×φ, non-bold | W4-1 | **LANDED** — picker display-3 / pane display-1, weight 400 on the surface's own class list (O-10a 12/12); producer-token consume only |
| T-3 / T-11 / T-18 | cards too transparent / inconsistent | W3-1 | **LANDED** — the four-rung material ladder; 9 pane cards at `tier="resting"`; the 6 well mints dead (O-7 census green light/dark/768/390) |
| T-4 | bottom text area dynamic | W4-3 | **LANDED** — live channel meters from the ONE injected pipeline; static ranges retired; **+ the checkpoint's C-1 cure: the α row now inks too** (`5b79cb7`) |
| T-5 | sliders: contrast + ring + console card | W4-4 | **LANDED** — the rung-2 console WELL, TRUE glyphs `L a b α`, certified ink (O-18 22/22 incl. the 3 formerly born-RED rows), WatercolorDot = the ONE active indicator; P5 letter-rail swap booked |
| T-7 | readout ×φ, contiguous tuple | W4-2a/b | **LANDED** — intrinsic contiguous cells, per-space line locks (O-10b), the self-cut tnum face (O-10c equal advance both weights); the α-in-tuple question is a flagged bracket (§6.4) |
| T-8 | blob: seat into card, no clip, higher z, all sizes | W4-5 + W2-4 (+P6) | **LANDED** — `--blob-fp` ONE formula, flush seat (Q3 "Flush."), `--z-ornament`, contact shadow, mobile bound computed from the formula (O-12 3/3 + blob-390); **+ the checkpoint's R2 cure: the boot-resolution race** (§3.1); hover/satellite depth rides P6/GAP-L5 |
| T-9 (api half) | the backend question | W1-api (TA-4) + W0-X1 | **LANDED** — prod api restored to HEAD (`0441aba`, O-25 standing guard); TA-4 excision; the banner's death is W6-6 (pending) |
| T-12 | search styled inconsistently | W3-3 | **LANDED** — the `.search-seated` register (well fill · hairline · content voice; the dock 24rem cap dead) |
| T-13 / T-19 | shadow palette missing / in all cases | W3-2 | **LANDED** — the D9 ShadowPalette species at every host class; error ≠ empty (O-9 green) |
| T-15 | wrong font (PaletteCard title) | W4-6 | **LANDED** — the display-voice population sweep (O-10d 6/6) |
| T-23 | header shaded AT REST | W3-4 + W4-7 | **LANDED** — constitutive veil, rest floor 0.52, geometry re-derived over the ×φ band (O-11 22/22); the rest-floor/effect bracket stays owner-judged (§6.7) |
| T-24 | the neutrals audit | W3-1 (C3 LAW) | **LANDED** — color as data only; the warm cream/stone family both schemes; the 9-row exception ledger NORMATIVE in `demo/DESIGN.md` (Q18) |
| T-25 / T-26 / T-27 | aurora + loading quality; the variance bracket | W2-1..5 | **LANDED** — the B0–B4 overture (O-4 order at 1×+6×), the gradient ground (no gray crossfade, luma flat), Q2-NOW calibration inside the bracket (O-6 6/6); T-26's neutral-seed whisper is a flagged bracket (§6.3) |
| T-30/T-31/T-32 | the §0.5 addendum | W4-5 / W6 / W2-5 | **RE-JUDGED at this checkpoint** — §3 below |
| E-1 | THE COLOCATION GRAND EDICT | W1 (all three trees) | **LANDED WHOLE** — 240-row MOVE-MAP; zero shims, zero `export *`; dts additive-only (476→479, 0 removed); PR-7 keyframes 19/19; the seams re-verified INTACT at this checkpoint after two rounds of design work (pass C §2.7) |
| LEG-1..9 · DOC-1..13 (pre-E-1) · Q13 `proof:*` | fleet rows | W0 | **LANDED** — legacy grep-zero; doc-truth pre-E-1 set; retain-5/excise-7 → `test:dist`; CI 32.4 → 16.4 min with zero truth-gates removed |
| MOB-2 | pane-index hash-nav leak | W1-demo | **LANDED** (route-derived index) |
| MOB-1 | 1024-portrait witness | W1-demo | **OPEN** — the one W1 §Hard-gate FAIL, routed; now booked BY NAME to W6 Lane D with the D8-1 ratified-vs-ratified conflict flagged (row C-5, §6.8) |

### §1.2 The §1.2 map rows NOT yet touched (owned by un-landed waves — correct state, not drift)

T-6 (W6-1 netting) · T-10/T-17 (W6-4 menu voice + previews; Q5 ramp = W6's T-10) · T-14 (W5
motion, PKT-1-gated) · T-16 (W6-5) · T-20 (producer P4, W7 re-judge) · T-21 (W6-2) · T-22
(W6-3) · T-28 (W6-7 ABROGATE) · T-29 (W6-8) · T-9's banner death (W6-6 — live urgency
confirmed by pass A row R10). The checkpoint critiqued only landed ground (the W5/W6 fence).

---

## §2 The checkpoint round itself (LIGHT — one critique→remediation round per surface)

Three passes filed at `06b793c`, each driven LIVE on lane-unique ports (the owner's :9000
untouched), judged against D1–D10 + the owner's verbatim lines + the §12 RULED outcomes:

| Pass | Surface | Coherence verdict (pre-filter, never certification) | Rows |
|---|---|---|---|
| A | boot overture + atmosphere + blob emerge (W2) + T-30/T-32 re-judge | **COHERENT** — the overture's gestalt lands; two mechanical breaches of the design's own laws found (R1/R2) | 10 (R1–R10) |
| B | material ladder + neutrals + ink (W3) + T-31 re-judge | **PASS** — zero regressions; 31/31 oracle floor independently re-run | 1 (B-1) |
| C | picker recomposition (W4) + W1 colocation seams + W0 oracle-floor state | **COHERENT** — one instrument gap (C-1); seams intact; the oracle floor honest and current | 6 (C-1..C-6) |

**17 rows filed → 17 dispositioned, zero silent drops**: 5 LAND-NOW (all landed + merged) ·
7 BOOK-W8 (flagged brackets/eye items, §6) · 3 BOOK-WAVE (W6 by name) · 2 PRODUCER-PACKET
(standing P1/T-32 book rows — no new demo action). Full ledger: `ROWS.md`.

**What the remediation round landed** (merged `--no-ff` at `e58a213` / `1c0c143`):

- **R1 — the returning-dark scheme flash** (`baed206`): the index.html fouc-guard predicate
  dropped the `'auto'` arm — the value the app itself always persists — so EVERY
  dark-preference RETURN visit booted the light material and re-themed mid-boot (a D3/F-6
  breach the first-visit oracle slate structurally cannot see). The predicate now mirrors the
  producer's own semantics; live-probed pre-module: auto+prefers-dark → dark class, dark
  ground constants, `color-scheme: dark`.
- **R6 — the stop-3 lockstep drift** (`baed206`): `FIRST_VISIT_GROUND` light stop-3
  regenerated `#ffd0c8` → `#ffcfc8` in all three lockstep sites — the "constant ≡ the sink's
  first write BY CONSTRUCTION" claim holds again (probed byte-identical).
- **R2 — the blob boot-resolution race** (`af18e07`): §3.1 below (the T-30 blur, demo-root).
- **C-1 — the α channel-meter inks** (`5b79cb7`): the α row rendered a positioned thumb
  beside a BLANK meter at the shipped boot default (α = 0.827) — alpha now formats from the
  SAME injected pipeline at the space's own least count (82.7% lab; keyboard-decrement
  synchronous; rgb/hex modes verified). Zero new state; the header tuple untouched (that is
  the C-2 bracket, §6.4).
- **C-6 — the PI-1 W4 row fired** (`4e5165b`): the W4-close MISS-RECORDED obligation
  discharged — §5.

**Post-merge gate state at `1c0c143`** (the PP-8 sweep re-run by this assembler): lint **0** ·
typecheck **0** (lib + demo) · vitest **2192/2192** (71 files) · e2e: see §7 · caps clean
(zero `demo/` non-ui file > 400 LoC; zero `api/src` > 350) · casts src **8**/0 · api **1**/0
· tool-artefact grep over the checkpoint docs **empty** · `git status` clean.

---

## §3 The T-30 / T-31 / T-32 re-judge (`MANDATE §0.5`, [OWNER-2026-07-10] — ordered re-judged
against the landed round-2+W4 state; the findings were audited PRE-round-2 at `c63f9aa`)

### §3.1 T-30 — the blob (blurry · top-right → centre-ward, integrated)

- **Containment/integration: DELIVERED.** Against the `c63f9aa` state you audited
  (half-off-card top-right, overlapping the About seam), the bead now sits wholly inside the
  plate's title band (edges ≈27px inside), contact-shadow seated, `--z-ornament` above
  content and never the dock, at all probed cells (1440/768/390 × both schemes). Q3 "Flush."
  stands untouched (`--blob-seat: 0`).
- **The BLUR: your eye was right, and the W4 verdict was incomplete.** Pass A REOPENED the
  demo-root: the engine sized its canvas backing store from the bounding rect **mid-emerge**
  (the `blob-emerge` `from { scale(0.35) }` pose) — measured at dpr2: **126×126 backing for a
  180.2px box (0.35× resolution)** — and the idle park froze that low-res frame. The W4
  forensic had measured the AWAKE state (ratio 1.99) and honestly missed the boot path.
  **Cured at `af18e07`**: the emerge beat's `animationend` now drives the producer's existing
  re-measure seam — live-probed post-fix: backing **360×360 (ratio 1.998, full-res)** and the
  park freezes the full-res frame. The RESIDUAL softness at hero scale (featureless interior
  shading + the resting drop-shadow halo) is the producer-root **P6 rider**, unchanged —
  rides W7's verify-at-cut walk.
- **"More towards the centre"**: satisfied in the containment sense; the bead reads
  contained-but-corner-pinned at the ruled flush default. The seat knob's sanctioned POSITIVE
  domain (deeper into the card) is a taste bracket this LIGHT pass must not rule — **FLAGGED
  for W8** (§6.1), with the note that the read should be re-taken on the now-crisp bead.

### §3.2 T-31 — the dock-atop layout law

- **How bad is it now: not reproducible.** `elementFromPoint` sampled over the dock band
  across 17 states (7 views × schemes, short viewports to 430px, 390×844): **zero
  card-over-dock hits**; the card top edge is pinned exactly at the dock band's bottom; the
  dock computes z 40 and wins every overlap. Pass C's 6-cell probe corroborates
  independently. Demo-side, nothing in the dock/z/layout mechanics changed since your audit —
  the one adjacent landing (`--z-ornament`) retired the BLOB's half of the band collision
  visible in your shot.
- **The class is ALIVE structurally** — which is why your LAW still bites: the "dock atop"
  is today a fixed overlay + padding arithmetic, not a band the layout owns; worst-case
  clearance is 0–1px at short viewports; and the entire paint priority hangs on ONE producer
  token chain (`z-dock → --z-index-dock → --z-dock`) — a silent chain break (the P9
  phantom-utility class, already burned once) deterministically paints cards OVER the dock,
  which is the only mechanism that reproduces your screenshot. The chain resolves today.
- **Routing**: the structural band-layout cure is W6 Lane D's ruled surface, as you ordered
  ("never by z-index patching"). Pass B hands W6 the sharpened spec: a two-band grid
  (`auto 1fr`), the nav in-flow, the fixed overlay + `--dock-total` reservation + load-bearing
  `z-dock` all retired together — occlusion dies BY CONSTRUCTION (row B-1, booked by name).

### §3.3 T-32 — aurora zones ("is this not configurable?")

- **Answered YES and landed** at W2-5: `zones: { count: 6, arrangement: "scattered" }` —
  verified live at this head; the scattered prior reads as an irregular, composed field, a
  real improvement on the rule-of-thirds wash.
- **The honest ceiling**: 3–5 soft masses discern by eye, not six distinct glows — softmax +
  blur merge neighbours, and 6 IS the producer's MAX_NUCLEI clamp. "A few more zones" beyond
  the shipped ceiling is the **standing producer book** (opened at W2 close, rides P1/W7
  re-judge) — no demo action available (row R9, routed).

---

## §4 Before / after frames per surface (the archived π sets; `*.png` self-ignored on-disk —
the standing S π convention)

| Surface | BEFORE (baseline) | AFTER (checkpoint) | Annotation |
|---|---|---|---|
| Boot overture + atmosphere | `audit/pi/w2/` (the W2 close annex: `headed-annex-log.txt`, `o24-lcp-before.md`, the 223-frame screencast + 6 owner eye-judge frames, real-GPU) | pass A's headed real-GPU series (cold/return ×2 schemes · seed matrix ×2 schemes ×(t0, t+10s) · rail zoom dpr2; session archive, re-runnable via `pi/w2/w2-annex.mjs`) | beats crisp both schemes at 1× and 6×; ground honest from first paint; the R1 returning-dark flash found in the return-visit series and CURED post-frames; the 10s field-migration pairs show the living field at mid/high chroma and the flat neutral seed (§6.3) |
| The blob seat (T-30) | `audit/owner-screenshots/t-30-blob-blurry-topright.png` (your shot at `c63f9aa`) + `audit/pi/w4/w4-picker-*.png` | `audit/pi/w45/picker/{1440,768,390}-{light,dark}-seat.png` (dpr2 crops) + pass A's t30 seat/hover crops (dpr1+dpr2) | half-off-card + soft → wholly in-plate, contact-shadow seated, crisp post-R2; the corner-pinned-vs-centre-ward read is the §6.1 bracket, frames ready for the W8 eye |
| Material ladder / neutrals / ink | `audit/w3-close-artefacts.md` + the W3 lane records' captures | pass B's 16-frame set (7 views × schemes × 1280/390; session archive; probe JSON alongside) | one warm neutral family both schemes; color as data only; veil 0.52 rest / 1.0 stuck with feather intact; no regression found |
| The picker instrument | `audit/pi/w4/w4-picker-{light,dark}-{1440,768,390}.png` (W4 close) | `audit/pi/w45/picker/*-full/-console/-readout.png` + `-console-active`/`-console-alpha` (interaction shots) | ×φ titles exact-token; tuple contiguous; the α meter BLANK in the before → inked 82.7% in the after (`-console-alpha`); rail select → ONE WatercolorDot |
| The dock band (T-31) | `audit/owner-screenshots/t-31-dock-occluded-by-card.png` | pass B's overlap probe frames (`overlap-1100x430.png`, `shorter-Home.png` et al., session archive) | occlusion not reproducible in 17 states; the 0–1px worst-case clearance frames document WHY the structural W6 cure is still owed |

---

## §5 THE PI-1 LEDGER MID-READ (Q14 — the PERF-REDEMPTION tracking instrument;
`audit/pi1-delta-ledger.md`)

Q14 was ruled **"Accept no failures — divine a proper and idiomatic fix. No compormises."**
The budgets (LCP ≤ 2500 · TBT ≤ 300) are must-go-green-by-close HARD gates; the ledger is the
tracking instrument, never a deferral record. The mid-tranche read:

| Wave row | LCP (median) | TBT (median) | Read |
|---|---|---|---|
| **W0 baseline** (`28836873580`) | **5563** | **5618** | honestly RED — LCP ~2.2× over, TBT ~19× over |
| W1 restructure | 5500 | 5858 | NOISE (< the PP-10 30% bar) — chunk-graph-neutral, as priced |
| **W2 (named gate row)** | 4919 local-lab | 125 local-lab | cross-instrument caveat; the SAME-instrument before/after for the reveal-only law = O-24 **light 2484→2128 ms (−14%)**; cures NAMED: reveal-only LCP law · the Google-Fonts strike (0 cross-origin font requests) · hydration-first |
| W2/W3 CI confirm (`ba0706e`) | 5151 | 4223 (spread 4223–8740) | apples-to-apples vs baseline: NOISE — the CI LCP floor cannot move while FCP ≈ 4.3 s (the eager-payload mount) gates first paint |
| W4 recomposition (`06b793c`, row fired at this checkpoint `4e5165b`) | 5141 | 5988 | **FLAT vs W2/W3 medians** (+0.1% / −1.5%); W4 moved no eager payload — expected ~neutral, confirmed; structurally sound run, the ONE failure is the honest-red budget gate itself |

**The honest state at mid-tranche**: both budgets RED, exactly as the instrument was designed
to show — the landed W2 cures fixed the *reveal* half (LCP identity, fonts, hydration order);
the un-landed **PAYLOAD half owns the floor**. **Where the redemption levers sit at W7** (the
next named gate row): the ONE eager-payload mount task that pins FCP ≈ 4.3 s (RP-2 — the O-5
spike leg's standing `test.fail()` cite) · L20/GAP-L5 · dead-payload abrogation · the
font/KaTeX delivery chain. W9 closes ONLY with green budgets or a triumvirate-level owner
escalation naming the physical blocker. No gate weakened, no re-baseline, no preset-swap —
the reds ride this report AS reds.

---

## §6 Open brackets — FLAGGED for W8, NOT ruled (the LIGHT law: this checkpoint files the
flag + the frames; the ruling is the W8 certification package's)

1. **The seat positive-domain knob (T-30 centre-ward)** — rows R7/C-3. Containment delivered;
   the bead reads contained-but-corner-pinned at the ruled flush default. The knob's
   sanctioned positive domain (deeper into the card) is the taste axis the W4 close named for
   this checkpoint — flagged with dpr2 before/after frames; **re-take the read on the
   now-crisp bead** (R2 landed after the frames). Q3 "Flush." untouched.
2. **The tnum-mint face** — row C-4. The readout's tabular figures ride a HOUSE-CUT Fraunces
   (`scripts/fonts/build-fraunces-tnum.py`) — clean by eye at both weights, O-10c-verified —
   but it is a font artifact the owner has never seen named; the flag IS the deliverable.
3. **The T-26 neutral-seed field read** — row R3. `vividness(0) = 0` renders the achromatic
   seed's field dead-flat by eye; the D4 intent says *whisper*, never silence. A whisper
   floor is an in-bracket calibration — frames archived, owner-judged.
4. **α in the hero tuple** — row C-2. At the shipped default (α = 0.827) the tuple inks
   `92.0%, 88.8, 20.0`, which does not round-trip. The owner's T-7 verbatim: "x, y, z **(or
   more if needed)**". A 4th cell re-derives the O-10b line-lock tables + the Q11b capacity
   derivation — too large for a LIGHT round and a genuine design bracket; flagged with the
   C-1/C-2 frames.
5. **The rail's α glyph face** — row R5. Two passes read it differently (pass A at dpr2 rail
   zoom: near-twin of the a*; pass C by eye at all cells: distinguishable). Both reads ride
   to W8 unreconciled — a type-authority call (donor-Greek subset into the self-cut face ·
   distinct styling rung · the P10 ask), not a LIGHT-round turn.
6. **The Q2 boot-settle bracket** — row R8. Dev-instrument reads land past the [1.4, 1.7]s
   upper (cold ≈2.5 s; returning 1.79–1.92 s) while built-bundle legs run faster —
   instrument-caveated numbers for the W8 Q2 annex read; the cadence itself is crisp; the
   payload chain (W7) owns the physical lever.
7. **The header rest-floor / Q9 effect bracket** — observed landed at 0.52 (∈ [0.45, 0.65];
   33.8%L/37.4%D added material ∈ the ratified [27%, 39%] effect bracket) in both schemes at
   every probed pane — an in-bracket default riding to the W8 eye per the D1
   bracket-with-default law. No row filed (nothing out of bounds).
8. **MOB-1 / D8-1 (routed, not a bracket)** — row C-5. The one standing W1 FAIL: the
   `data-layout` witness still absent; the aspect-law exception now at `style.css:508`. The
   D8-1 ratified-vs-ratified conflict (producer-owned `lg:*` cascade vs T's demo-cure) is a
   layout-law adjudication booked BY NAME to W6 Lane D, where T-31's dock-atop band also
   lands.
9. **The dark-scheme field/chrome split** (pass B note, filed nowhere — un-landed surface):
   the dark `lBand` producer atom (GAP-L2) is the already-booked cure riding P1/W7; pass A's
   NEW observation (the picker plate visibly re-materializes rose→cocoa at field arrival in
   dark) is appended to that standing row for the W7 verify-at-cut re-judge (row R4).

---

## §7 The oracle floor + suites at this head (reds ride as reds)

- **EXPECTED-RED, unaltered, packet cites intact**: **O-5 spike leg** (`test.fail()` — the
  eager-payload mount spike; RP-2 → the W7 payload row; the cadence leg is GREEN, pile-up
  dead) · **O-16 R1** (the PKT-1 producer dist 150ms clobber — letter DISPATCHED at W0-1;
  flips the day the producer lands) · **O-26 headless leg** (the headed real-GPU annex read
  is GREEN — 10s migration unmistakable by eye; the headless leg stays honestly red by
  design).
- **Green at this checkpoint** (independently re-run by the passes, not taken on the lanes'
  word): the boot set (O-1 · O-1b · O-2 · O-4 1×+6× · O-12 · O-24 both schemes · blob-390 ·
  O-6 6/6) — 15 passed (pass A §0); the W3 census set (O-7 · O-9 · O-11 · O-18) — 31/31
  (pass B §1); zero console/page errors across pass C's 6 cells.
- **Post-remediation-merge** (this assembler, head `1c0c143`): lint 0 · typecheck 0 · vitest
  2192/2192 · the full e2e suite re-run on lane ports 8611/8612 (result recorded in
  `PROGRESS.md` — the wave-close citation).

---

*End of the mid-tranche window. The next owner touchpoint of record is the W8 certification
package (the deep pass over the full census, `ROWS.md` + these flagged brackets in hand).
Nothing here asks for a ruling.*
