# T.W2 — the wave-close §Verification artefacts (π lane record)

**Wave**: T.W2 — THE BOOT OVERTURE + THE LIVING FIELD (single-writer boot lane).
**Branch**: `worktree-wf_5011a3ae-57c-1` (cut from the W1-close head `c63f9aa`;
tranche-t's owner-findings commit `060fa69` merged in at `030c683`).
**Close date**: 2026-07-10. **Recovery**: the lane walled once mid-close and
resumed per the §Recovery four-step (`audit/recovery/T.W2-boot-brief-2026-07-10.md`
— the annex capture was RE-DRIVEN per the rider; the pre-wall capture discarded).

## §1 Per-item commits

| Commit | Item |
|---|---|
| `5cdccb5` | O-24 mint — LCP identity + reveal-only probe, the BEFORE record (`o24-lcp-before.md`) |
| `ed24358` | W2-1 hydration-before-derivation (the R11 NEW-BUG cite in-body; O-2 + O-1 flip records) |
| `f354f42` | W2-2 the gradient ground (`--saved-bg-0..3` @property template; luma-leap gate minted; F-6 dark-honest) |
| `b496952` | M-15 routed hunk (`derivedLightness` exposed; BG_LIGHTNESS_DARK/LIGHT retired — recorded for W3's log) |
| `e02fa61` | W2-3 THE OVERTURE (B0–B4 gating DAG · LCP reveal-only · self-hosted fonts · appear grammar · dock veil interim) |
| `400fe15` | W2-4 the emerge beat (B4 consume; FSM `emerging` interim; no-pop by construction) |
| `244459e` | W2-5 T-26 calibration Q2-NOW (energy 0.76 · softmaxBeta 4 · breath 26 · vividness=f(seedC); O-6 minted) |
| `c7e0816` | §Recovery brief (the wall audit + work-order) |
| `1f5b4b4` | gate-5b PRM fix — the field pinned at terminal opacity (the producer a11y-overrides forced-fade root cause) |
| `73762b2` | W2-5 rider — T-32 zones: `arrangement: scattered` landed; count-ceiling producer book (MAX_NUCLEI=6) |
| `a25c4d2` | PP-8 cap cure — `boot/overture.css` grammar sheet + `boot/useDockArrival.ts` (App.vue 397 · ColorPicker 400) |
| `eb6d9a6` | the one-shot release law (`backwards` fill) + cold-load settle-band poll — 20 view-nav failures root-cured |
| `d5b3e4d` | dock reveal waits TIME-DRIVEN finite animations only — the WebKit veil-forever cured (smoke-safari catch) |
| `bcb77bb` | settle-stamped measurement — write-through convergence pair + emerge-aware presence poll |

## §2 The gate table (T.W2 §Hard gate — each honest)

| Gate | State | Evidence |
|---|---|---|
| 1 · O-2 real-hydration | **GREEN** (flip: born-RED proven vs pre-fix tree — trace `#757200 → #b27290 → #757200`, the F-3 flash verbatim) | `ed24358` body; `o2-real-hydration-coldload.spec.ts` green in all close suite runs |
| 1 · O-1 color-truth | **GREEN** (settle-stamped sample in the derived seed's family; h=19.1° vs seed 28°, C=0.126, L=0.578) | `ed24358`; holds over the ground (`f354f42`: h=21.3° C=0.147) |
| 2 · luma-leap | **GREEN** (returning boot L series [0.690…0.694] flat/monotone; born-RED ref ≈0.25) | `o1b-ground-luma-leap.spec.ts` |
| 2 · F-6 dark first-run | **GREEN** (first-visit dark boots in the dark-band constant; sink write `{stops:[#673255…], scheme:dark}` probed) | `f354f42` |
| 3 · O-4 order-invariance | **GREEN at 1× + 6× CPU** (close re-run ×4: 1× `b0@25→b1→b3→b2→b4`; 6× `b0@25→b1@768→b3@1838→b2@2160→b4@2561` — ORDER invariant) | close run + `e02fa61` (1×: b0@202→…→b4@5283; 6×: b0@41→…→b4@4293) |
| 3 · O-24 LCP identity + reveal-only | **GREEN** (identity converged: the picker card plate owns LCP BOTH schemes; reveal-only law bound — opacity pinned 1, land = shadow cast-in only; before/after: light 2484→2128ms, dark 1332→1476ms, SwiftShader lab floor) | `o24-lcp-before.md` + `e02fa61` + green in close runs |
| 3 · fonts | **GREEN** (self-hosted Fraunces ×4 woff2, latin-normal preloaded; network log: 4 same-origin `/fonts/*` requests, **google-fonts=0** both schemes) | `headed-annex-log.txt` |
| 4 · no-pop | **GREEN** (the reveal composes from `emerging` inside the 500ms goo-scale from scale 0.35/opacity 0 — no first-frame fully-formed blob; the mobile presence spec now proves the pose live: box 71.68px mid-emerge → 204.8px settled) | `400fe15` + `bcb77bb` |
| 5 · O-6 bracket resolver | **GREEN** (pure-function probes: energy strictly inside (0.7, 0.82) on every co-varied axis; base knobs survive atom resolution; vividness endpoints/monotone; gray ≤0.05; + the T-32 row: ceiling witness `nuclei.length=6`, clamp witness count 9→6, scattered spread > composed spread) | `test/aurora-bracket.test.ts` (vitest 2177) |
| 5 · O-26 perceptibility | **GREEN headed** (calm 2s = 1.54/255 < 2 · 10s migration = 9.20/255 ≥ 4 · 60s min 5s-apart pair = 2.19/255 ≥ 1) — the CI headless leg stays born-RED-by-design (placeholder still) | `headed-annex-log.txt` |
| 5 · drag path byte-identical | **GREEN** — `useColorPipeline.ts` diff vs wave-head is COMMENT-ONLY (0 code hunks); `SpectrumCanvas*`/`ComponentSliders*` diff EMPTY; the per-frame re-seed mechanism (rAF coalesce → seed watch → resolve) untouched (the W2-1 one-shot pre-registration + W2-2 groundPalette are additions BESIDE the watch, not edits of it) | `git diff c63f9aa..HEAD -- <drag files>` |
| 5 · owner eye-judge frames | **ARCHIVED** (green/warm/neutral × light/dark = 6 frames, real-GPU headed, post-T-32; on-disk at `pi/w2/owner-frames/` — `*.png` self-ignored per the S π convention; W8-package inputs, data not judged now) | annex leg 4 |
| 5b · PRM instant-states | **GREEN both schemes** (flip: the re-driven annex — canvas opacity exactly {1} from the first frame · beat-family animations 0 · canvas-targeting animations 0; producer state-fade census 1/0, observation only). Born-RED at the pre-fix annex: dark mid-fade 0.5747 + 1 boot animation — root cause the producer a11y-overrides forced 0.1s state-fade; cured by the terminal-opacity pin (`1f5b4b4`) | `headed-annex-log.txt` |
| 6 · O-3 headed real-GPU annex | **GREEN both schemes** (renderer `ANGLE Metal — Apple M5 Max`; post-arm field OKLCh light L0.687/C0.156/h33.1° · dark L0.631/C0.153/h33.9° vs seed h≈28 — chromatic + hue-family PASS; beat marks ordered: light b0@10→b1@134→b3@505→b2@754→b4@790) | `headed-annex-log.txt` |
| 7 · O-5 pacing | **HONESTLY SPLIT** — pile-up leg GREEN (dropped 5.3% ≤ 10%; the five-clock smear dead); spike leg RED (max 399.9ms = 16× median — the ONE eager-payload mount task, RP-2), `test.fail()` stands with the cite ROUTED TO W7 (L20+GAP-L5, the Q14 chain). A red stays red with its cite. | `e02fa61` + `o5-boot-pacing.spec.ts` |
| 8 · Q14 LCP measured | **MEASURED** — see §4 (the ledger row appended; budgets stay honestly RED; the W7/W9 rows adjudicate) | `pi1-delta-ledger.md` |
| 9 · PP-8 sweep | **PASS** (demo .vue/.ts ≤400 — max = ColorPicker.vue 400 exactly-at-cap, App.vue 397; api ≤350 clean; casts src 8/0 · api 1/0; legacy grep-zero ×5 names) — the sweep CAUGHT the wave's own re-breach (596/445) and the cure landed (`a25c4d2`) | this record |
| 9 · lint · typecheck · vitest | **0 · 0 · 2177/2177 (70 files)** | close cadence |
| 9 · e2e | **SUITE-TRUTH GREEN** — see §3 | `pw-final*.log` (scratch) + this record |

## §3 The close suite-truth story (all 6 projects, 67 tests)

The walled lane had only re-run the targeted oracle specs after W2-3; the
close ran the FULL suite four times:

1. **Run 1–2 (enumeration)**: 20 failed — ~all one class: every spec that
   navigates views through the dock timed out ("element is not stable").
   Root cause LIVE-BISECTED: the W2-3 dock land ran `overture-plate-land …
   both` — a permanently-FILLING transform animation left current on the
   `<nav>` renders every dock control forever unstable to Playwright
   actionability. → **the ONE-SHOT RELEASE LAW** (every land/emerge beat
   fills `backwards`; each `to` ≡ the natural resting state; a finished beat
   retires).
2. **Run 3**: 63 passed / 2 failed — the write-through settle-capture pair
   (1–2/255 pre-settle sample) + the mobile presence gate sampling the
   emerge pose mid-flight (71.68px = 204.8 × 0.35 exactly) → both cured
   settle-stamped (`bcb77bb`). Plus the smoke-safari catch (run-2 residual):
   the dock reveal fallback awaited two ScrollTimeline gl-fade animations
   whose `finished` never resolves — WebKit-mobile veiled FOREVER, b2 never
   opened, the blob never mounted → cured (`d5b3e4d`, time-driven-finite
   filter).
3. **Run 4 (final tree)**: **64 passed · 2 skipped · 1 failed** — the one
   failure = O-4 at 1× dying "browser closed" (the documented headless
   renderer-death class, N.W2); **re-run ×4 (both legs ×2): 4/4 GREEN** with
   ordered marks. Every spec green on the final tree in ≥1 clean run;
   wall-clock 4.6m (the enumeration runs read 12.8m — the timeouts were the
   clock).

The 2 standing skips + the `o5-boot-pacing` `test.fail()` born-RED (gate 7)
are by-design states, unchanged.

## §4 Q14 — the PI-1 measurement at W2 close (gate 8)

**Instrument**: local-lab LHCI 0.15.1 (`lhci collect --static-dist-dir
./dist/gh-pages -n 3`), Apple M5 Max, close-tree build. The CI baseline
(`28836873580`: LCP 5563 (7340/5653/5563) · TBT 5618 (5618/6029/6593)) was
collected on the GitHub runner — a slower CPU class, so cross-instrument
absolute deltas carry a caveat; the same-instrument CI confirmation rides
this branch's CI run at merge (the worktree branch is unpushed at close —
recorded honestly, not deferred silently).

- **LCP**: median **4919ms** (5812 / 4907 / 4919) vs CI baseline 5563 —
  directionally down ~12%, below the PP-10 30% noise bar on its own; the
  SAME-INSTRUMENT before/after for the reveal-only cure is O-24's built-
  bundle record: **light 2484 → 2128ms (−14%)**, dark 1332 → 1476ms (lab-
  floor variance) — the LCP element identity CONVERGED (the picker plate,
  both schemes) and its paint chain carries no opacity gate.
- **TBT**: median **125ms** (2152 / 105 / 125) — NOT comparable to the CI
  baseline's 5618 (CPU-class instrument difference; the cold run-1 2152
  shows the eager-parse cliff the warm runs skip).
- **Attribution (named)**: the reveal-only law (O-24 breach cured — the LCP
  owner's former `from { opacity: 0 }` keyframe excised) · the font strike
  (zero Google-Fonts requests — the cross-origin render chain gone) ·
  hydration-first (no default-material repaint). **What remains is the
  PAYLOAD half**: FCP ≈ 4353ms ≈ LCP floor — the ONE eager-payload mount
  task (RP-2/O-5 spike leg) gates first paint; that cure is W7's (L20 +
  GAP-L5, dead-payload abrogation) per the ledger design. Budgets stay
  honestly RED (LCP 4919 > 2500) — measured, named, routed; no re-baseline,
  no gate edit.

## §5 T-32 rider record (MANDATE §0.5, owner 2026-07-10)

"The aurora should have a few more zones" — landed as `zones: { count: 6,
arrangement: "scattered" }` (`73762b2`): count 6 **is** the producer ceiling
(`MAX_NUCLEI = 6`, a shader `#define`; count 8/9 clamp to 6 — probed), so
the count raise beyond 6 is a **producer book** (P1 aurora surface, W7
re-judge); the demo-reachable half is zone LEGIBILITY — the scattered prior
spreads the same six nuclei across the full frame (x 0.13–0.84, y 0.22–0.81
vs composed's middle-thirds lattice), judged in-bracket across
green/warm/neutral A/B frames. Energy/chroma registers untouched — the T-26
bracket holds by construction; O-6 gained the T-32 row.

## §6 Artefact inventory (this directory)

| Artefact | State |
|---|---|
| `w2-annex.mjs` | committed (the durable, re-runnable O-3/5b/O-26/frames/screencast harness) |
| `headed-annex-log.txt` | committed (the re-driven close annex — O-3 · 5b · O-26 legs) |
| `o24-lcp-before.md` | committed (the pre-beat BEFORE record) |
| `owner-frames/*.png` (6) | on-disk, self-ignored (`*.png` — the S π convention); green/warm/neutral × light/dark, real-GPU, post-T-32 |
| `screencast/*.png` (223 frames, 97MB) | on-disk, self-ignored; the returning warm/light boot at 2-frame cadence — the W8 package's raw material |
| `w2-close-artefacts.md` | this record |

## §7 Books opened/serviced at close

- **GAP-ARM demo half discharged** (W2-1); producer half rides P1 → W7.
- **The emerge pose interim** → P6 row-F booked swap.
- **Pointer retune** DEFERRED-BOOKED on P1 F-10 (dead axes
  `flow.glsl.ts:82-93` · `brush.glsl.ts:274` carried by name).
- **T-26 bracket record** = W2-5 values + the archived owner frames (P1
  variance-atom sizing spec; GAP-L2 rides it).
- **T-32 count-ceiling lift** = NEW producer book (MAX_NUCLEI raise; the
  shader arrays are sized to it), W7 re-judge with the P1 surface.
- **O-5 spike leg RED** routed to W7 (RP-2 → L20/GAP-L5, the Q14 chain).
- **The M-14 dock-veil interim** stands (dies on the P7 arrive-expanded
  landing); the reveal fallback now waits only time-driven finite
  animations (`d5b3e4d`).
- **M-15 routed hunk recorded for W3's log** (`b496952`): `derivedLightness`
  exposed; W3-5 threads the non-boot consumers.
