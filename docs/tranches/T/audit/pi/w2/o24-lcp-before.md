# T.W2 · O-24 — the LCP identity BEFORE record (pre-beat, built bundle)

**Minted + run BEFORE any W2 beat landed** (T.W2.md §Hard gate 3; PI-2). Substrate:
`dist/gh-pages` built at the wave head (`c63f9aa`), smoke-perf project (:8091,
SwiftShader), 1280×720, warmed symmetric contexts per scheme.

## The identity (both schemes — CONVERGED)

The LCP element is **the picker card plate** — the `.pane-shell > :first-child`
`<div class="rounded-card text-card-foreground …">` (the picker Card's root; the
element that carries the R.W3 `plate-land` animation). Candidate sequence per
scheme: the dock pill (~3.4k px²) is the interim candidate; the card's text
aggregation (~40k px²) supersedes it and is final.

| scheme | LCP t | size | element |
|---|---|---|---|
| light | 2484 ms | 40010 | `div.rounded-card` (picker plate) |
| dark | 1332 ms | 40000 | `div.rounded-card` (picker plate) |

## The reveal-only breach (the F-4 finding, live)

The LCP owner's entrance is TODAY an opacity-keyed animation —
`ColorPicker.vue` `@keyframes plate-land { from { opacity: 0; … } }` applied to
`.pane-shell > :first-child`, i.e. exactly the LCP element. This is the
h-refine-overture F-4 class: the plate's paint (and so the page's LCP) waits on
the animation's opacity ramp. The W2-3 cure binds this element to the
REVEAL-ONLY law: opacity pinned 1 from B0; its land is transform + shadow ONLY.

## Instrument note (recorded for the re-run)

The first probe ran the two schemes on asymmetric contexts (cold fixture page
vs warmed second context) and read a FALSE identity divergence (the light leg's
card entry never fired inside the cold window; final candidate = the dock).
Symmetric warmed contexts converge the identity. The spec now warms once and
measures both legs on identically-created fresh contexts — a swallowed-wait
class instrument hazard (PP-15) recorded so the re-run is never compared
against the asymmetric numbers.

## The measurement of record (pre-beat)

- O-24 identity leg: **GREEN** (converged, both schemes).
- O-24 reveal-only leg: opacity=1 AT OBSERVATION (the animation has completed by
  the 3s read) — but the paint CHAIN is opacity-keyed (the breach above); the
  armed post-W2-3 spec adds the first-paint-time ≤ B1-start + 1 frame leg
  against the overture marks.
- Baseline LCP (SwiftShader lab floor, not the Lighthouse run of record):
  light 2484 ms · dark 1332 ms. The Q14 ledger row compares the wave-close
  re-run against these + the `28836873580` LHCI baseline.
