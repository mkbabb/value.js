# t49-research — the T-49..T-59 RESEARCH lane (triumvirate-2, owner order §0.7)

**Lane class**: research / probe-only (zero product-code changes; this doc is the only write).
**Dispatched**: 2026-07-11, per the §0.7 process order ("mark me with a proper research, harden,
and tranche-wave write set"). **Substrate**: `tranche-t` @ `de3ec50` (the §0.7 MARK commit),
**finalized at `71ae027` (the §0.7 sub-note / T-60 MARK)** — the T-60 goo-blob cross-link
("useMetaballRenderer.ts:338 arms the SAME bloom — a named candidate root for T-49 axis 1") is
CONSUMED here with a fresh computed-style trace (§2.1), and it OVERTURNS this lane's own earlier
pixel-capture negative. **The product tree ≡ `5e4f1f6`** (verified: `git diff 5e4f1f6..HEAD --
':!docs'` is EMPTY — every commit since the W6.5 close is docs-only, so the tree probed here IS
the tree the owner judged).
**Instruments**: the standing built bundle `dist/gh-pages` (fresh at the W6.5 close-cure build)
served static on a lane port (:8710, `serve-built.mjs`; the owner's :9000 untouched); headless
Chromium (playwright) dpr2, light+dark `colorScheme` cells, 1440×900 + 390×844; CDP screencast +
rAF/longtask observers for the motion leg; the library's own math (`dist/value.js` —
`wcagContrastRatio`/`gamutMapOKLab`) for every contrast number. **Headless caveat, stated once**:
compositor holes measure SMALLER headless than on the owner's GPU (the pass-2 headed reference is
cited beside each such number); nothing else here is instrument-sensitive. **Method**: (1) MANDATE
§0.7 + the 11 shots read as images + `t33-research.md` + `w65-close-artefacts.md` + the three
in-flight W8 pass records; (2) code anatomy on BOTH trees (demo + `../glass-ui` HEAD, READ-ONLY);
(3) seven live probe legs (emerge trajectory · dock-clip anatomy · card-edge bisection · dock
expansion hunt · swap frame-pacing · veil/ramp/rhythm computed-style census · closed-form pastel
arithmetic). Probe scripts + frames are scratchpad artifacts; every number is reproducible from
the commands and files named inline.

**The one cross-cutting verification this lane adds**: the local `../glass-ui` tree (the `file:`
dep the demo consumes TODAY) is **5.0.0 in-tree** (`package.json`; npm registry still 4.2.0,
tag absent — W7 stays TRIGGER-NOT-FIRED), and its built `dist/goo-blob.js` **ships the
mercury-colony fission register** — several GAP-L5 dispositions below flip from "producer-gated"
to "shipped-but-unarmed". Each row is split **demo-now / producer-relay / taste-bracket**.

---

## §1 Verdict table (T-49..T-59 against the live tree)

| Row | Verdict (verified) | One-line root |
|---|---|---|
| T-49a | **PRODUCER-ROOT (the T-60 bloom, measured ON the blob canvas) + demo α-compound** | The reveal-bloom's `brightness(0.4) saturate(0.7)` floor stamps AT the emerge-fade start and holds ≤0.50/≤0.75 through the WHOLE 500ms fade — the bead completes its α arrival at brightness ≈0.73 (§2.1); the demo's α-fade × translucent body over a same-family plate compounds; the wake-gray is pass-2 row B's (distinct) |
| T-49b | **TASTE-BRACKET (demo knob)** | `--blob-fp: clamp(7rem, 22cqi, 11rem)` rides its 11rem ceiling at desktop — the ceiling/coefficient pair is the knob (§2.2) |
| T-49c | **DEMO-NOW (flipped from producer-gated) + P6 residue** | The fission register SHIPPED (`surface.fissionAmp`); default 0, the hero never arms it; the park runway (2.7s) < one fission beat (5.2s) (§2.3) |
| T-50 | **DEMO-NOW calibration — the primitive is ALREADY seated** | `<Card surface="veil">` is live; the α stack (0.52 over 0.678) starves the blur of backdrop — ~15% variation survives → reads opaque (§3) |
| T-51 | **DESIGN MANDATE (bracket collapsed) + token swap** | The 61.2px reserved line now sits ABOVE the numbers = under the title; the owner has redded BOTH poles of P1-B1. Step-down = `--type-display-2` (1) / `--type-display-1` (2) (§4) |
| T-52 | **PRODUCER-RELAY (P7·L13 rider)** | The dock layer's 0px-fade scroll mask antialiases the edge + the trigger sits flush at layer x=0 — glass-ui `dock/overflow.css:86-99` (§5) |
| T-53 | **NOT a T-45 recurrence — a distinct DEMO root** | Bisected live: backdrop-filter kill changes nothing; box-shadow kill removes it. The dark-scheme cartoon caster paints 50%-α CREAM (`--shadow-color: var(--foreground)`) — the corner crescent is the artifact (§6) |
| T-54 | **DEMO-NOW (consume the shipped primitive)** | `.generate-swatch` plain rounded divs (`GenerateControls.vue:193`); WatercolorDot (color+seed+variant) is the ruled species, 9 consumers already (§7) |
| T-55 | **DEMO-NOW re-seat; both owner-named primitives exist** | Three button registers in one row; glass-ui ships BOTH the dock-set family and `ui/dropdown-menu` (§7) |
| T-56 | **DEMO-NOW resolver cure + closed-form bands** | Landed stops compute **near-BLACK** (L≈0.02, C≤0.01 — 2.03:1 on the dark card, 3.66:1 vs its own referent): wrong referent + infeasible-floor walk + direction heuristic (§8) |
| T-57 | **DEMO-NOW one-token cure** | `--dock-h` floor = 55px vs the rendered pill 70.9px — the reservation is UNDERSIZED ~16px, so seal↔pill morphs grow row 1 and shift the scene (§9) |
| T-58 | **MEASURED: one mid-swap compositor hole + sub-60 tail; scheduling, not easing** | 135ms hole headless / 356ms headed on face re-activation; O-16 curves green; the 150ms producer clobber confound is LIVE in dist (§10) |
| T-59 | **DEMO-NOW one-law regime** | Desktop rows ≈24px vs mobile 44px — a hard `<lg` band switch; the union is one container-scaled clamp + hit-extended touch (§11) |

Nothing below contradicts a §12 RULED outcome; T-51/T-56 encode owner recalibrations of landed
gate-green work per the `T.md §4` grammar (the tranche-update lane formalizes). The in-flight W8
passes are EXTENDED, not contradicted: pass-2 owns T-49/T-58's surfaces (its rows B/F are cited
as partners), picker.p1's P1-B1 bracket is COLLAPSED by T-51 (both poles now owner-photographed).

---

## §2 T-49 — the blob, three axes

### §2.1 The gray fade-in (axis 1) — PRODUCER-ROOTED: the T-60 reveal-bloom plays ON the blob canvas, floor-aligned with the emerge fade (measured; overturns this lane's earlier negative)

**The T-60 cross-link, consumed and CONFIRMED** (the sub-note's named candidate, probed fresh on
the built bundle — computed-style ground truth, both schemes, `t49-blob-bloom-probe.mjs`
scratchpad): goo-blob's substrate arms the SAME **BG.W-VIZ-REVEAL-BLOOM** the T-60 aurora
forensic pinned — `revealBloom: true` HARDWIRED at glass-ui `useMetaballRenderer.ts:338` (the
leaf option exists, `createCanvasLifecycle.ts:141`, but `BlobConfig` ships NO consumer door —
the same no-door state as the aurora's `runtime.ts:261`). Measured per-rAF on the blob canvas
(reveal attr + computed `filter` + emerge-host opacity): the canvas mounts inside the emerge
host held at α=0 (`blob-emerge` backwards fill, t≈1714 dark / 1907 light), then
`data-substrate-reveal` stamps at **t≈2119/2335 — the SAME tick the α-fade starts** — the bloom
floor and the arrival are WORST-CASE aligned (the aurora at least burned most of its floor
invisibly; the blob burns NONE of it): the filter holds **brightness 0.38–0.50 · saturate
0.69–0.75 through the ENTIRE 500ms emerge**, the bead completes its α arrival still under
**brightness ≈0.73**, crosses 1.0 only ≈600ms AFTER the fade ends, and punch-overshoots
(≈1.09–1.10) before settling at bloom+1100ms. The owner's "fades in from an ugly gray" is
literally this: a `saturate<1 · brightness≪1` veil over the derived red family for the whole
arrival — the T-60 palette-honest-entrance-floor violation at its SECOND consumer surface.

**What IS exonerated** (the task's two hypotheses): (a) the engine's default ramp never paints —
`GooBlob.vue` seeds `resolvedStops` from the config prop at setup and the demo's derive is
`immediate` (`HeroBlob.vue:162`), so the default stops (`types.ts` `["#b5947f","#d4b27d",
"#dad6b1"]`, warm cream — not gray anyway) are unreachable; (b) no source-color-pre-derive
transit — hue stays in-family under the bloom (saturate scales C, never rotates H). This lane's
own earlier pixel capture ("red family from the first frame") under-sampled the window — its
clock started at capture, not at the attr stamp; the computed-style trace supersedes it.

**The demo's compounding half** (real, secondary): the W2-4 emerge pose fades `opacity: 0 → 1`
over 500ms while scaling from 0.35 (`boot/overture.css:152-165`); the bead's own body is
translucent (BASE_OPACITY 0.75 class) over a plate derived from the SAME seed — mid-fade the
bead↔plate delta collapses (measured washed state: bead rgb(192,125,126) vs plate
rgb(199,132,134)); the INK_FLOOR (0.15, `HeroBlob.vue:126`) guards the SETTLED delta only.
α-wash × bloom-veil is the full percept. Dev-server caveat: on :9000 a stalled main thread
HOLDS the from-pose longer (the `backwards` fill), worsening the felt fade.

- **Producer-relay (PRIMARY)**: the T-60 **P1 AURORA-BOOT rider EXTENDS to the goo-blob leaf —
  one register, not two** (T-60's own routing; glass-ui's same-evening UF-E10 book): the
  reveal-bloom consumer door (opt-out / arrival-sync) must reach `BlobConfig` as well, + the
  palette-honest entrance floor (never a `saturate<1` veil over a chromatic field). The demo
  consumes the door when it ships (E-3: no counter-filter, no demo filter-fighting — and a demo
  emerge-delay chase is DEAD ON ARRIVAL, since the bloom stamps at first-visible and would
  simply re-align with any delayed fade).
- **Demo-now (E-3-clean, stands on its own merits)**: re-cut the emerge pose to matter-arrival —
  goo-scale WITHOUT the α leg (or α floored ≥ ~0.6 from frame 1); the no-pop law holds via scale
  alone. This cures the demo's compounding wash and it is the demo's ONLY clean lever — the
  bloom half is producer-only. One keyframe block (`overture.css`), the R2 pose/arm race
  contract (`animationend` name filter) unchanged. **The wake-gray bead stays DISTINCT** (pass-2
  row B: dark-gray pose through the frozen swap window, palette re-ink at settle) — its forensic
  rides row B's named route (P6 arm if producer-rooted).
- **Taste-bracket**: emerge duration/curve (500ms decelerate today) — minor poles beside the two
  roots above.

### §2.2 The size knob (axis 2) — the W4-5 cqi formula, a taste bracket

`seat.css:43` — `--blob-fp: clamp(7rem, 22cqi, 11rem)`, with the COUPLED seat arithmetic at
`:89-90` (`padding-right: 0.76·fp + 0.5rem`; `min-height: 0.76·fp − 0.75rem`). At the desktop
pane the fp rides the **11rem = 176px ceiling** (measured live: canvas box 180×180, bead ⌀≈92px).
The formula's own derivation (seat.css:14-20) sizes the bead to the display-3 title line so it
adds ZERO header height — the constraint any growth must respect.

- **Taste-bracket (pure demo knob, no producer)**: poles — current (`22cqi`/`11rem`, ⌀≈92px
  desktop, ⌀≈58 @390) ← "a bit bigger" (candidates: ceiling 12–12.5rem + 24cqi + floor 7.5rem →
  ⌀≈100–105 desktop, ⌀≈62 @390) → over-grown (past ~12.5rem the bead outgrows the title line and
  starts ADDING header height via the 0.76 coupling — the formula's zero-added-height property
  breaks; that is the far pole's cost, nameable in px). If T-51's title step-down lands, the
  title line SHRINKS — the two knobs interact: re-derive the ceiling against the NEW title band
  in the same pass (a smaller title line argues for the bead carrying MORE of the band, not
  less — the eye-judge is the owner's).

### §2.3 Satellites never meatball off (axis 3) — the register shipped; the demo never arms it

**GAP-L5 re-verified against what P6/P1 ships TODAY** (the task's order): the consumed glass-ui
tree (file: dep, 5.0.0 in-tree; `dist/goo-blob.js` carries it — grep `fissionAmp` hits the dist)
ships **BD.W-GOOBLOB-MERCURY-COLONY**, which IS the "mood-gated satellite emergence" P6 asked
for: `useBlobSatellites.ts:142-144` (`effFissionAmp = (config.surface.fissionAmp ?? 0) ×
mood-arousal`), `:167-185` (the split branch — bud OUT through a thinning neck → SNAP),
`:317-354` (the `fissioning` excursion: `fissionSnap` reach envelope, pinch past the smin skin,
recoil kick into the body-pulse spring); `constants.ts:182-219` (REACH 0.18→0.4 · BEAT 5200ms ·
PROB_AT_FULL 0.75); `types.ts:463` — **`BLOB_CONFIG_DEFAULTS.surface.fissionAmp = 0`, "the calm
default, never splits"**.

**The root today**: the hero register overlays `geometry`/`color`/`quality` ONLY
(`HeroBlob.vue:165-188`) — `surface` passes through the app default → **fissionAmp 0 → the
entire fission branch is dead at the hero**. The "small spikes" the owner photographs are the
only satellite show reachable at amp 0: bonded orbit/merge necks (orbit 0.4 > bodyRadius 0.26
bulging the skin) that never detach.

**Two interactions any arming must design WITH** (not around): (1) **the park runway** — the
demo parks the hero at 2.7s idle (`BLOB_IDLE_MS` 2000 + `SLEEPY_POSE_MS` 700, HeroBlob.vue:210-211)
while one fission beat is **5.2s**: an armed-but-idle hero would freeze mid-split. The FSM's
`fissioning` phase IS quiescence-visible (`isQuiescent` returns false for any non-orbiting
phase, `useBlobSatellites.ts:400-405`), so the producer's own wake scheduler keeps a split
rendering — but the DEMO's wall-clock park does not consult it. The P6 `settled`/
park-only-from-settled seam (verified NOT yet shipped — no `settled` export, no HERO preset
export at HEAD) is the missing coordination; the interim is demo-side: park countdown ≥ one
fission beat, or gate the park on the engine's quiescence read. (2) **arousal coupling** — the
resting hero is sleepy by design (mood-arousal scales amp ×0.4 at sleepy), so the armed value
must be sized for the CALM state, not the excited one.

- **Demo-now**: arm `surface: { ...appBlobConfig.surface, fissionAmp: <bracket> }` in
  `heroConfig` + the park-runway extension. Zero producer wait.
- **Producer-relay**: P6 rows STAND for the remainder — the exported HERO preset (W-1),
  the `settled` seam, the hero-scale legibility floor rows A–E — re-verify at the 5.0.0 adopt;
  the letter needs no re-dispatch, only the verify-at-cut.
- **Taste-bracket**: fissionAmp ∈ [0.4, 0.8] (calm-effective ≈ [0.16, 0.32] → a split every few
  merge windows vs a busy colony), judged by eye at the hero scale.

---

## §3 T-50 — the glass-veil card: the primitive is seated; the material signal is starved

**Which surface** (shot 02): the sliders console — `.sliders-console`, and it is **ALREADY**
`<Card surface="veil" tier="quiet">` (`ComponentSliders.vue:25-31`, Lane P `ad301e7`), riding the
exact producer primitive the owner names (`veil-surface`, glass-ui `cards.css:445-456` —
`--veil-bg`/`--veil-blur`/`--veil-border/-shadow/-feather` knobs on the `--glass-*` ladder).

**Why it does not READ veil (measured)**: the veil's backdrop is the aurora seen THROUGH the pane
card's own tint. Computed live: console `--veil-bg` → `oklab(0.9156 … / 0.52)` +
`backdrop-filter: blur(8px) saturate(1.4)`; the pane card beneath: `oklab(0.8845 … / 0.678)`,
no host blur (the T-45 cure moved it to the pseudo); beneath that, the atmosphere canvas
(`absolute inset-0` in `.app-layout`) over the opaque body. The backdrop variation surviving to
the veil ≈ (1−0.678)·(1−0.52) ≈ **15%** — a blur(8px) over a 15%-contrast, already-fogged field
is visually indistinguishable from a flat plate. A/B verified: veil forced off vs on at the
console — near-identical frames (`t50-console-asis/noveil.png`, scratchpad). **Two stacked
milks**: the veil is live but starving.

- **Demo-now (calibration, not construction)**: give the veil a real backdrop — lower the
  console's tint α (re-point `--veil-bg` at a clearer mix) AND/OR open a clarity window in the
  pane card behind the console region. Every move drags the D6 ink referent (the veil-rung
  certification in `ink.ts` — Lane P already threaded it; O-18's W4 rows re-run at any α change).
- **Producer-relay**: none — the knobs exist; `--veil-feather` is available if the re-cut wants
  the bleed register.
- **Taste-bracket**: the α pair (console tint, card ground) — poles: current (0.52/0.678, reads
  opaque tone-step) ← visible glass (the field legibly moves under the console) → too-clear
  (slider tracks/letters lose their certified ground; the O-18 GRAPHICS leg is the fence).

---

## §4 T-51 — the lab-label gap + the golden step-down

**The gap, measured**: at 1440, title box bottom y=274 → painted tuple ink top y=336 —
**62px**, of which 61.2px is the W4-2 line-lock's reserved second line (lab's honest 2-line
worst case) rendered as air ABOVE the bottom-anchored numbers; at 390 the honest 2-line case
consumes it (gap 4px). **The root is the tuple seam's history**: T-33 photographed the reserved
line BELOW the numbers (readout↔rail); Lane P's cure bottom-anchored the tuple — the SAME 61px
moved ABOVE, under the title; T-51 photographs it there. **The owner has now redded BOTH
placements of the reservation-as-blank — picker.p1's P1-B1 bracket is COLLAPSED**: the air is
rejected wherever it sits, which graduates the row from bracket to design mandate. The two
honest arms stand as named in t33-research §6.1: (a) **per-VALUE-CLASS reservation** — the
line-count rides the LIVE digit class from the same static ch table (cost: card-rect movement at
class boundaries — the lock's raison d'être, paid rarely instead of always); (b) **seam-absorb**
— the console/plate rises into the reservation so the air reads as designed inter-block rhythm,
never a band pinned under the title. The wave designs INSIDE these two arms.

**The step-down (closed answer)**: the landed title is `--type-display-3` (W4-1/Q11a ×φ —
67.78px @1440, weight 400, probed). The producer ladder is half-φ-stepped (glass-ui
`typography/scale.css:120-127`): **one step down = `--type-display-2`** (`clamp(2.058rem,
1.5rem + 2.2vw, 3.33rem)` → **53.28px @1440**); **two steps = `--type-display-1`**
(`clamp(1.618rem, 1.2rem + 1.6vw, 2.618rem)` → **41.89px @1440**). Both are existing tokens —
zero mint; the swap is one class-list line on `.space-trigger`. This RECALIBRATES the T-2 ruling
(the owner's own 2026-07-06 "1.5× bigger") — encode per the `T.md §4` grammar as an owner
recalibration with the T-26 both-sides template: original (~display-1) ← target → landed
(display-3).

- **Demo-now**: the token swap + the seam mandate above. **Producer-relay**: none.
- **Taste-bracket**: display-2 vs display-1 (the owner's "by 1 or 2" IS the bracket); note the
  §2.2 interaction — the blob ceiling re-derives against the chosen title line in the same pass.
- The mobile-tightness kernel of shot 03 is carried in **T-59's regime** (§11).

---

## §5 T-52 — the dock inline-edge clip: producer mask geometry, relay row named

**Measured anatomy** (the clip reproduced headless — `t52-trigger-zoom.png`): the view-select
trigger's box `x=495.3` is EXACTLY flush with `.dock-layer--full`'s left edge (`x=495.3`); the
layer computes `overflow: auto/hidden`, `padding: 0px`, and
`mask-image: linear-gradient(to right, transparent 0, black 0px, black 100%, transparent 100%)`
— the fade customs sit at **0px at rest**. A 0px-fade mask is NOT a no-op: the zero-width
transparent stop antialiases the outermost pixel column at BOTH inline edges, shaving the pill
ring's outer arc — the owner's "clipped slightly" at left and right.

**Ownership**: the recipe is the PRODUCER's — glass-ui `src/styles/dock/overflow.css:86-99`
(the mask + `gl-fade-start-in/out` keyframes driving `--fade-start/--fade-end`) with the layer
strategies in `dock/layers.css`. The DEMO's own clipper class was already released at rest by
W6-8 (`c237d24`, the settle-stamped 0fr-reveal release — T-29/T-36) and is verified NOT the
residual clipper here (the trigger's clip chain contains only the producer layer + `.app-layout`).

**The relay (the exact row)**: the carrier is **GLASSUI-T-ASKS §P7, the "L13 dock residuals"
member** — the packet gains a NEW named arm: (1) **mask honesty at rest** — when neither fade is
active the layer's `mask-image` must compute `none` (the mask exists only while scrollable), and
(2) **an inline safe-inset** on `.dock-layer` ≥ the ring weight (edge padding ~2–4px +
`scroll-padding`) so flush items' rings render whole even mid-scroll.

- **Producer-relay**: the P7·L13 rider above. **Demo-now**: none legitimate — a demo override on
  `.dock-layer` is the D7 arms-race class the fences forbid. **Taste**: none (A-class geometry).

---

## §6 T-53 — the T-45 "recurrence" forensics: a DIFFERENT root, bisected live

**The verdict the task asked for**: the landed demo mitigation is **NOT insufficient — it is
innocent**. Live bisection at the Generate card (dark scheme, the shot's own): forcing
`backdrop-filter: none` on host AND pseudos → **the sliver persists**; forcing
`box-shadow: none` → **the sliver is GONE** (`t53-bisect-*.png`). The T-45 oversampled-pseudo
carrier (`7d3900a`) holds at ≤1/255 as certified; the P3 producer book remains booked for ITS
class but is NOT this artifact's discharge — tracking it there would wait forever.

**The actual mechanism**: the pane card's caster computes
`color(srgb 0.914 0.902 0.886 / 0.5) 8px 8px 0 0` — the **dark-scheme cartoon caster paints
50%-α CREAM**, because `--shadow-color: var(--foreground)` (`style.css:355`) resolves to the
light ink under `.dark`, and the dark arm deliberately re-mixes at 50% ("Lighter cartoon shadows
in dark mode", `style.css:597-600`; the root recipe `:363`). Geometry: the +8/+8 hard offset
slab's 16px arc emerges from behind the card's own arc at the top-right corner — a light
crescent at the top border + a cream band down the right edge, reading on the dark magenta
ground as a paint defect (shot 05 reproduced pixel-identical headless). In light scheme the
same caster reads as the deliberate pop-art stack; the DARK register is the whole delta.

- **Demo-now**: the dark caster recipe is the root — a design re-cut, not a paint fix: either
  the caster reads as shadow-side ink in both schemes, or the inverted (light) caster earns its
  read deliberately (see bracket). The token is demo-owned (`style.css:599`); one declaration.
- **Producer-relay**: NONE for this artifact. The standing P3 rider (backdrop-blur edge
  sampling) is untouched — different mechanism, already cured demo-side.
- **Taste-bracket**: what dark mode's caster IS — poles: current (cream @0.5, reads artifact) ←
  a caster that reads deliberate (dark-on-dark caster; or cream at much lower α with a larger
  offset so it reads paper-stack, not seam) → no caster in dark (the cartoon language goes
  scheme-asymmetric — a register cost worth naming).

---

## §7 T-54 + T-55 — the Generate plate: swatch species + the verb cluster

**T-54, which swatches**: the plate's specimen row — `GenerateControls.vue:193`:
`.generate-swatch w-9 h-9 sm:w-10 sm:h-10 rounded-md` — **plain rounded-rect `<button>`s** (each
already the direct per-swatch copy verb) with a rectangular `focus-visible:ring-2` (shot 06's
flat chips above `seed: e581b2e2`). The ruled species is the producer **WatercolorDot**
(`@mkbabb/glass-ui/watercolor-dot` — `color` + `seed` + `variant solid|ghost`; per-instance
seeded silhouette + wet-edge filter; "the ONE shape source"; already 9 demo consumers). **What
the swap needs vs what these render**: the dot is presentational — the existing button stays as
the wrapper seat and the dot becomes its face; sizing stays the w-9/w-10 rung; the seed should
be stable per color (`color+i`) so a re-roll reads as new specimens. **T-28's outline law rides**:
the current rectangular focus ring dies with the chip — the focus register must fit the organic
edge (material/elevation treatment or the wrapper seat), never a geometric ring on the dot.
**Split**: demo-now entirely; producer none; taste = seed/size only.

**T-55, the verb cluster** (`GenerateControls.vue:145-181`): name input (dashed underline) +
count badge + `Regenerate` pill Button + a BARE floppy `Save` icon button + a circled `Copy` —
three button registers in one chrome row (shot 07). **Both owner-named primitives exist at the
producer root**: (a) the **dock-set family** — `DockSection`/`DockStack`/`DockIconButton`
(glass-ui `dock/index.ts:4/28/39`), a capsule of uniform icon-verbs with the dock's own state
ladder; (b) the producer's **`ui/dropdown-menu`** (+ the P7·L11 glass-token polish in flight).
**Fit ground (research, not certified)**: all three verbs are immediate plate actions — a
dropdown hides the plate's primary verb behind a click; the dock-set capsule keeps the trio
visible in ONE register (Regenerate carrying the label slot, Save/Copy as icon members,
separator grammar free). The dropdown wins only if the row grows secondary verbs (export/share).
**Split**: demo-now = the re-seat on the chosen primitive; producer-relay = none new; taste =
which primitive + label-vs-icon weighting — the owner's "dropdown or dock set" IS the bracket.

---

## §8 T-56 — the PASTEL calibration: three stacked roots + closed-form bands

**The landed state, measured** (computed tokens at the owner color, BOTH schemes): all three
`--palettes-ramp-*` stops ≈ `oklch(0.021–0.024 ≤0.01 {352.6, 32.6, 72.6})` — **near-black,
chroma-dead, indistinguishable**. Library-computed ratios: vs the ACTUAL dark card ground
(measured composited paint `oklch(0.383 0.090 19.5)`) — **2.03:1, failing even the 3:1 graphics
floor**; vs the resolver's OWN referent (`--ink-ambient-l` = 0.5103) — **3.66:1: it never
reached its 4.5 floor at all**. "Dark/muddy on the dark card" is exactly this.

**Three stacked roots** (`palettes-ramp.ts` + `useViewAccents.ts:81-95`):
1. **Wrong referent** — `bgL = derivedLightness`, the FIELD ambient (0.5103), not the CARD the
   letterforms sit on (the T-35 cream-collapse's surface-referent lesson, un-applied here; the
   file's own D6 note concedes the field band doesn't even flip with scheme — GAP-L2).
2. **Infeasible-floor walk** — 4.5:1 against a mid-L ground is unreachable in EITHER direction
   (measured ceiling: pure black = 3.66:1). `walkToTextFloor` has no feasibility check: it walks
   12 × 0.04 steps to the L=0.02 clamp and ships the wreck; C dies by gamut en route (three
   near-identical blacks — the "rainbow" is gone before contrast even fails).
3. **Direction heuristic** — `away = bgL < 0.5 ? +1 : −1` at 0.5103 walks DOWN, onto the dark
   card, in dark scheme.

**The closed-form bands** (library-computed against the MEASURED live grounds; reproducible via
`wcagContrastRatio` + `gamutMapOKLab` on `dist/value.js`):
- **Dark scheme** (card `oklch(0.383 0.090 19.5)`): the true pastel band **L ∈ [0.80, 0.88],
  C ∈ [0.06, 0.10]** at the ±40° fan measures **5.3–7.2:1** — pastel passes text-AA with
  margin. Recommended center: **L 0.84, C 0.09** (6.1–6.3:1 across the three hues).
- **Light scheme** (card `oklch(0.682 0.083 17.5)` — the mid-pink derived plate at this pick):
  pastel is **arithmetically infeasible** — the pastel band measures 1.5–2.1:1 and even pure
  WHITE reaches only **2.97:1**. Feasible registers: the deepened **chalk band L ∈ [0.30,
  0.40], C ∈ [0.09, 0.12]** (4.78:1 at L 0.30 · 3.98 at 0.35 · 3.25 at 0.40). The **WCAG
  large-text carve-out (3:1 at ≥24px)** legitimately applies to the display-scale pane title —
  it does NOT apply to the ~16px dock menu entry, so the two Q5 sites diverge honestly:
  light title ∈ [0.35, 0.40] under 3:1; light menu entry ≤ ~0.33 to hold 4.5.
- The ground is PICK-DEPENDENT (the plate derives from the seed) — the bands above must be
  solved per-frame against the LIVE card rung (the `ink.ts` surface machinery Lane I built is
  the referent source), with the band constants as the per-scheme targets.

- **Demo-now**: the resolver cure — card-surface referent + feasibility-aware walk (detect the
  unreachable floor, choose the FEASIBLE direction, hold C) + the per-scheme/per-site band
  targets. All in `palettes-ramp.ts`/`useViewAccents.ts`; library leaves suffice.
- **Producer-relay**: none. **Taste-bracket**: the band centers — poles: landed near-black ←
  the pastel/chalk targets above → over-sweet (C > ~0.12 reads candy; L > 0.9 dies into the
  light card). Q5 itself (rainbow at exactly these two sites) stands RULED, untouched.

---

## §9 T-57 — the dock-expand downshift: an undersized reservation, one-token cure

**Mechanism, measured**: `.app-layout` is `grid-template-rows: auto 1fr` (`style.css:473`); the
band's floor is `min-height: var(--dock-h)` where `--dock-h = calc(var(--size-icon-btn) +
var(--dock-padding-y) + var(--dock-border-width))` (`style.css:397`) = 40 + 12 + 3 = **55px** —
but the EXPANDED pill renders **70.9px** (probed; the calc counts padding-y ONCE and predates
the pill's current metrics). The collapsed seal is height-pinned to the token; the expanded pill
exceeds the floor by **~16px** → the `auto` row grows on every seal↔pill morph and the whole
scene band shifts down. (At 1440 rest the dock sits expanded, and neither the Tools reveal nor
the dropdown changes band height — probed flat at 70.9/86.9 across rest/scroll/hover — which is
why the shift only bites on the collapse↔expand cycle the owner drove.)

**The KISS cure (reservation, per the addendum's reserve-or-overlay)**: make the floor TRUE —
`--dock-h` equals the expanded pill's real height (correct the arithmetic: 2× padding-y against
the pill's actual content rung — or consume an authoritative producer token). Then the band
NEVER changes height across seal/pill/action-bar states by construction; zero z-index arms; the
T-31 law extends to the expansion axis exactly as the addendum orders. The overlay arm is
unnecessary once the floor is honest.

- **Demo-now**: the `--dock-h` calc correction (one declaration, `style.css:397`) + a probe
  assert (band height invariant across the morph — an O-15/t31-dock-band census extension).
- **Producer-relay**: only if the pill's height rung is not exposed — a P7·L13 candidate rider
  (`--dock-pill-h` authoritative export) so the demo floor can never drift again.
- **Taste**: none (A-class layout).

---

## §10 T-58 — the escalation forensics: measured frame-by-frame, twice

**Fresh captures on the live product tree** (≡ `5e4f1f6`, built bundle, CDP screencast + rAF +
longtask observers): **home→browse** — ONE compositor hole of **135ms at t=579ms** into the
swap (28 frames; rAF median 8ms — flowing before and after the hole); **browse→home** — a 58ms
gap at entry then **rAF median degraded to 25ms** (≈40Hz sustained through the return; max
66ms; zero longtasks >50ms headless — many medium tasks, not one wall). **Headed same-tree
reference** (pass-2, M5 Max): the same hop class froze **356ms mid-pose** with 4×53ms tasks.
The signature is stable across instruments and matches the owner's percept exactly:
**freeze-then-continue on pane-face (re)activation + a sub-60 cadence tail** — the swap holds a
mid-slide pose, then finishes.

**What specifically janks, by the task's axes**: paint holes — **YES, the dominant defect**
(measured above). Enter/exit clock pairing — **NO**: O-16's census is green both schemes
(per-row duration/curve ≡ the liquid target), and when frames flow the choreography reads
coherent (pass-2's own read). Compositor promotion — **not the mover**: the holes co-occur with
re-activation main-thread work (KeepAlive picker re-mount + About/KaTeX + spectrum + blob
rewake), i.e. the frame budget is stolen, not the layers un-promoted; a layer-tree dump remains
the one un-run probe if remediation wants belt-and-braces.

**Two named confounds bind any cure** (isolate FIRST, the owner has ordered this analysis
twice): (1) **O-16-R1 is LIVE in the shipped CSS** — `--default-transition-duration:.15s`
verified present in `dist/gh-pages/assets/index-*.css` (the producer clobber; PKT-1 unlanded) —
any felt-duration retune before PKT-1 lands is tuning against a corrupted clock; (2) **the
payload half is W7's BY LAW** (RP-2/L20/the KaTeX ride — the §4-attributed walls are never
re-bracketed as taste). What remains genuinely MOTION-side — and is this row's mandate — is
**window scheduling**: the re-activation payload executes INSIDE the enter window. The cure
class: evict it — pre-warm the incoming face on intent (hover/press), or defer content re-mount
until after transition settle so the enter clock never shares its budget with mount walls. The
wake-gray bead (§2.1's partner, pass-2 row B) rides the same window — the swap wreck is a
compound percept; cure both or the owner will red it a third time.

- **Demo-now**: the scheduling re-design + re-measure after W7's payload levers land (the W9
  walk re-verifies). **Producer-relay**: PKT-1 (the clobber) + the P6 wake-order arm.
- **Taste-bracket**: per the addendum this row is now a MANDATE, not taste — the only bracket
  left is the residual poles (frozen-mid-pose ← hole-free swap → over-deferred/blank-arrival).

---

## §11 T-59 — the perfected-union rhythm: one law, not two states

**The two shots are the poles, measured**: desktop 1440 (shot 10) — console box **138px**, pad
12×14, row air 6px, slider rows ≈ **24px** (TIGHT — the owner's "a bit too tight on desktop");
mobile 390 (shot 11) — console box **218px**, rows **44px** (the touch rung; AIRY — "too spaced
out"). The regime today is a hard `<lg` band switch — a **1.83× discontinuity** between two
hand-tuned states; T-51's "mobile slider space a bit tighter" kernel is the same defect's other
face.

**The KISS union (the regime, with the constants as the bracket)**: ONE container-scaled law,
no band arm — row block-size `clamp(2rem, 7cqi, 2.625rem)`-class (32px desktop-feel floor,
42px ceiling) + row-gap `clamp(0.375rem, 1.5cqi, 0.625rem)` — **with the 44px TOUCH TARGET
preserved by hit-area extension** (the touch-gate inflation idiom already in
`ComponentSliders.vue`), so the VISUAL rhythm converges (desktop rises 24→~32px; mobile
tightens 44→~40px visual) while the a11y rung holds unchanged. One law serves both breakpoints
by construction — the T-26 template with reproducible poles (the owner's own two shots).

- **Demo-now**: the one-law re-cut in `ComponentSliders`/`ConsoleRail` (+ the same law offered
  to the ConfigSliderPane population — the M-34 surface — so the app has ONE rhythm source).
  Gates that ride: O-10b line-locks, the 44px touch oracle, O-18's W4 rows (unmoved ground).
- **Producer-relay**: none now; the P5 letter-rail primitive inherits the law when it ships.
- **Taste-bracket**: the clamp constants only — poles: shot-10 (24px rows) ← target → shot-11
  (44px visual rows).

---

## §12 Summary for the harden + tranche-wave lanes

- **Flips this research establishes**: T-49a "no gray ramp" → **producer reveal-bloom on the
  blob canvas, floor-aligned with the emerge fade** (the T-60 cross-link confirmed by
  computed-style trace — this lane's own earlier pixel negative overturned, §2.1); T-49c
  producer-gated → **shipped-but-unarmed** (fission register live in the consumed dist; demo
  never arms it); T-53 "T-45 recurrence" → **distinct demo root** (dark caster color, bisected);
  T-51 P1-B1 bracket → **collapsed by the owner's own second shot** (both reservation placements
  redded); T-56's "muddy" → **a resolver defect with a closed-form cure**, not a taste miss (the
  landed stops fail every floor including their own).
- **Producer packet deltas** (riders on EXISTING rows, no new packet): the T-60 **P1
  AURORA-BOOT rider extends to the goo-blob leaf** — the reveal-bloom consumer door must reach
  `BlobConfig` too, one register with T-60/UF-E10 (§2.1); P7·L13 gains the dock-layer
  mask-honesty + inline safe-inset arm (§5) and optionally `--dock-pill-h` (§9); P6 otherwise
  unchanged (verify fission/HERO/settled at the 5.0.0 adopt; wake-order arm rides pass-2 row B);
  PKT-1 re-cited as the standing T-58 confound (§10).
- **Demo-now inventory** (all measured, all in named files): emerge α re-cut (§2.1) · fissionAmp
  arm + park runway (§2.3) · veil α calibration (§3) · title token swap + seam mandate (§4) ·
  dark-caster re-cut (§6) · WatercolorDot swap + verb-cluster re-seat (§7) · palettes-ramp
  resolver cure with per-scheme bands (§8) · `--dock-h` floor correction (§9) · swap-window
  scheduling (§10) · the one-law rhythm regime (§11).
- **Taste-brackets carried to the owner** (poles named, numbers attached): blob size (§2.2) ·
  fission amp (§2.3) · veil clarity (§3) · display-2 vs display-1 (§4) · dark-caster register
  (§6) · dropdown vs dock-set (§7) · pastel band centers (§8) · rhythm clamp constants (§11) ·
  the T-58 residual poles (§10, now mandate-bound).
- **Oracle notes for the harden lane** (flags, not mints): band-height invariance across the
  dock morph (§9); a ramp-stop feasibility leg on O-14 (the near-black wreck was invisible — the
  oracle compares sites to tokens, never tokens to their floors); the veil's material-visibility
  has no oracle class at all (§3's A/B is the seed of one); the boot gates never trace computed
  `filter` on entrance canvases — the §2.1 reveal-attr/filter trace is the seed of that oracle
  (it would have caught BOTH T-60 surfaces).
