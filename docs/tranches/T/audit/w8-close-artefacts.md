# T.W8 — CLOSE ARTEFACTS (E-7 THE HARDENING/CRITIQUE WAVE — the terminal owner-certification package)

**Verdict**: **CLOSED-`complete_with_misses`** (2026-07-12).
**Gate**: `CLOSE_WITH_MISSES` — the 6-row `T.W8.md §Hard gate` returned **5 PASS + 1
MISS-RECORDED, zero FAIL**.
**Close head**: `286619e` (the package assembly commit, docs-only) + `b551755` (the WR-4-owed
O-10 referent re-aim, e2e-scoped) atop the merged wave head `6d95871` + this docs commit.
**Gate of record**: the composite `T.W8.md §Hard gate` (6 rows), re-run against the merged
wave head `6d95871` (`tranche-t`) + the assembled package (`audit/w8-certification/`).
**Spec of record**: `RATIFICATION-2026-07-09 §0` → `MANDATE-2026-07-06 §0/§0.1–§0.8` (owner
verbatim WINS) → `SYNTHESIS.md §2` D1–D10 + §3 (the T.W8 block) + §6.1–§6.2 → `waves/T.W8.md`.

---

## §0 The verdict + its honest reconciliation (OPEN-vs-CLOSED, the owner-verdict-pending state)

This wave produces TWO distinct things, and the honest close must hold both:

1. **The wave's producible obligations** — the per-surface critique passes, the remediation
   lanes, the ONE non-authoring certification package, the full oracle slate + budgets + fresh
   Lighthouse delta, the PP-8 sweep, and the suite gates. These are **DISCHARGED**: 11 passes
   filed, 24 defects landed + 9/11 §0.7 WR rows, the package assembled, the slate run, the
   budgets recorded. The gate ruled these **`CLOSE_WITH_MISSES`** — the one recorded miss is
   **HG5** (the PP-8 caps breach, §4 below).
2. **The owner's verdict over the taste brackets** — `T.W8.md §Hard gate` row 6: *"THE OWNER'S
   VERDICT IS THE GATE."* This is the terminal act; **it is PENDING.** `VERDICT-2026-07-12.md`
   is the empty stub the owner's verbatim ruling lands into. Per row 6, *"a package delivered
   but unruled leaves the wave OPEN, honestly (never a proxy sign-off)."*

**The reconciliation** (no proxy sign-off, no gate weakened): the wave closes
`complete_with_misses` for its **gate-agent-producible work** — the package is DELIVERED and the
HG5 miss RECORDED — while the **owner's taste ruling is neither produced nor proxied**. It is
carried forward as the delivered-but-unruled honest state that **T.W9 consumes BY PATH**
(the close wave inherits the verdict-stub, the package, the booked rows, and the Q14 material).
The `_with_misses` verdict string carries BOTH the HG5 caps breach AND this pending-owner state.
**The package PRESENTS; it CERTIFIES NOTHING** — the brackets go to the owner *as brackets*,
which is the wave's design (lesson 12: the S taste-by-proxy disease must not recur).

---

## §1 The 6 §Hard-gate rows — VERBATIM (as the `CLOSE_WITH_MISSES` gate delivered)

### HG1 — every census surface has a filed pass · every row landed-or-booked · zero silent drops → **PASS**

> Every census surface has a filed pass record, every row landed-or-booked, zero silent drops.
> 11 pass journals present at `docs/tranches/T/audit/w8-certification/passes/` (picker,
> boot-atmosphere-blob, about, palettes-browse, extract, mix-generate, gradient, easing,
> dock-nav-menus, admin, microchrome-error). The 12+ census surfaces fold into 11 passes per
> the documented rationale (ConfigSliderPane[M-34]+screencast+T-48 motion → boot pass; T-42
> header → picker; mix+generate → one; micro-chrome+thrown-error → one). ROWS.md dispositions
> all 40 pass rows + 11 WR rows + 7 checkpoint brackets by grade (LAND with named commit /
> BRACKET to package / PRODUCER to letter / WITNESS / TRIUMVIRATE). admin.p1.md + the admin/
> frame dir (58 frames) + the e2e admin-walk cover all 5 named views.

### HG2 — the certification package (NON-authoring): frames · both schemes · 3 viewports · boot screencast · brackets indexed to owner lines → **PASS**

> Package assembled by the non-authoring terminal-assembler; assembly commit `286619e` is
> docs-only, path-scoped. Frames on-disk (gitignored-by-class per standing convention) match or
> exceed PACKAGE.md §1: picker 28, about 39, palettes 39, extract 24, mix-generate 82, gradient
> 22, easing 12, dock 55, admin 58, microchrome 10, blob/field/config for boot. Both schemes +
> 3 viewports (1440/768/390) per the naming scheme. Boot screencast present = 1543 frames
> (cold-light 383 / cold-dark 392 / returning-light 411 / returning-dark 357), exactly matching
> §4. T-48 motion = 8 hops (both schemes × 4). Bracket table §2 = 20 axes B-01..B-20 + 2 flagged
> non-rows, each pole a NAMED reproducible state (tuple/commit/frame), indexed to its
> owner-verbatim line via §3.

### HG3 — the full oracle slate: green or honest EXPECTED-RED with packet cites (no red weakened, no red hidden) → **PASS**

> Ran the full 6-project e2e (smoke/smoke-admin/smoke-mobile/smoke-reactivity/smoke-perf/
> smoke-safari) on lane-unique ports VJS_E2E_PORT=8790 / VJS_E2E_PERF_PORT=8791 (owner's :9000
> untouched): 160 passed, 2 skipped, exit 0 — matching the package §5 claim exactly. Exactly 3
> `test.fail()`-designed born-RED legs reproduce as documented: O-16-R1 (o16-computed-cascade,
> 150ms dist clobber, PKT-1→W7), O-26 (aurora perceptibility headless, SwiftShader, headed annex
> GREEN→W9), O-5 (o5-boot-pacing spike, RP-2→W7). All three carry their W7/W9 packet cites in
> §5. No red hidden, no red weakened. Minor prose imprecision noted (see notes).

### HG4 — the §6.2 budgets re-run + a fresh Lighthouse delta recorded (PI-1); the Q14 material in the package → **PASS**

> §6.2 budgets re-run and recorded in PACKAGE.md §5: view-switch first-frame GREEN (83.4ms);
> LCP/TBT honestly RED → Q14. The fresh PI-1 Lighthouse delta W8 row is appended to
> `docs/tranches/T/audit/pi1-delta-ledger.md` (line 49), expected-FLAT vs W4/W6.5 (demo-only,
> chunk-graph-neutral; render-blocking index CSS ~88.4 KiB gz ≤ 120), budgets stay honestly RED,
> no re-baseline. Q14 adjudication material present in PACKAGE.md §6 (LCP/TBT record +
> budget/preset pairing WITH the owner at W8/W9, FCP-blocker named).

### HG5 — PP-8 repo-wide sweep · lint 0 · typecheck 0 · npm test green · e2e all-project green → **MISS-RECORDED**

> lint EXIT 0; typecheck EXIT 0 (vue-tsc lib+demo); vitest 74 files / 2241 tests PASS exit 0;
> e2e 160 passed / 2 skipped / exit 0 across all 6 projects — all four suite legs GREEN. BUT
> the PP-8 sweep's caps-recomputed leg (T.md:549: 'caps recomputed … in EVERY wave gate') is
> BREACHED: two demo files now exceed the CLAUDE.md ≤400-LoC god-module cap, both introduced by
> W8 remediation — `demo/color-picker/composables/boot/useAtmosphere.ts` 344→411 (P9-R3/T-37
> derive-seam, `c116250`) and `demo/@/components/custom/markdown/Markdown.vue` 396→408 (AB-1
> KaTeX, `433ae22`). At the pre-W8 baseline `5e4f1f6` there were ZERO breaches; both are also
> over-cap at the assembly head `6d95871` (411/408), so the package §7 'row 5 MET' claim
> under-reports this. Both are comment-heavy (206/280 code lines) so it is a raw-LoC breach not
> a complexity explosion, but it literally breaches a stated invariant + PP-8's caps leg. PP-8's
> other legs are clean: `src` as-any = 0 (the one grep hit is a comment), as-unknown-as = 8
> (documented class), no tracked *.log residue. Recorded honestly, not weakened — routes as a
> booked re-encapsulation row (touching demo/ to split is a remediation act, not a gate act).

### HG6 — THE OWNER'S VERDICT IS THE GATE → **PASS** (the gate mechanism; the owner's ruling itself is PENDING)

> `VERDICT-2026-07-12.md` exists as the stub awaiting the owner's verbatim ruling (THE RULING
> block empty). The wave is correctly OPEN pending the owner — the honest terminal state per
> §Hard gate row 6 ('a package delivered but unruled leaves the wave OPEN, honestly; never a
> proxy sign-off'). No proxy sign-off present; the package PRESENTS (CERTIFIES NOTHING), brackets
> go to the owner AS brackets. The gate mechanism is correctly in place; the owner's ruling
> itself is not something the gate agent can produce.

**Final gate tally**: HG1 PASS · HG2 PASS · HG3 PASS · HG4 PASS · **HG5 MISS-RECORDED** ·
HG6 PASS → **`CLOSE_WITH_MISSES`**. `_with_misses` = HG5 (the PP-8 caps breach, booked §4) +
the owner-verdict-PENDING delivered-but-unruled state (§0) + the standing born-RED producer legs
carried by-design (§5).

**Gate note (HG3 "minor prose imprecision")**: the gate flagged a non-blocking prose slip in the
package's honest-red narrative (§5 wording); it does not touch a value, a commit, or a
disposition — recorded here for the record, not routed as a row.

---

## §2 The package — path + what it delivers (the frames · brackets · owner-line index · screencast)

**Package home**: `docs/tranches/T/audit/w8-certification/`

| File | What it holds |
|---|---|
| `PACKAGE.md` | the frames index (§1) · **the bracket table (§2: 20 axes B-01..B-20 + 2 non-rows)** · the owner-line index (§3) · the boot screencast (§4) · the honest-red ledger (§5) · the Q14 material (§6) · the hard-gate checklist (§7) |
| `ROWS.md` | the remediation ledger — every pass's 40 rows + 11 WR rows + 7 checkpoint brackets → landed commit / booked-to / routed-to; **zero silent drops** |
| `VERDICT-2026-07-12.md` | **the stub the OWNER's ruling lands into, verbatim** (empty at close — the wave's honest OPEN-for-the-owner terminal state) |
| `PROGRESS.md` | the non-authoring assembler's running record (merge log + timeline + deliverables) |
| `passes/<surface>.p1.md` (11) | the per-surface critique-pass journals (coherence verdict + filed rows) — the §Recovery-canonical homes W9 reads BY PATH |

**Frames on-disk** (gitignored-by-class per the standing convention; probe instruments + logs
committed): `docs/tranches/T/audit/pi/w8/<surface>/` — dpr 2, both schemes, 3 viewports
(1440/768/390) on the frozen product tree `5e4f1f6`. The **owner BEFORE roster** is the owner's
own shots at `docs/tranches/T/audit/owner-screenshots/` (23 §0–§0.4 + 2 §0.5 + 15 §0.6 + 11 §0.7
+ the T-61 shot). **The boot screencast** (§Hard gate row 2 deliverable):
`pi/w8/boot/{cold,returning}-{light,dark}/f<offset>ms.png` — **1543 frames**, compositor-true CDP
on the BUILT bundle, headed real GPU. **The T-48 motion captures** (the frame-by-frame evidence
class for B-02): `pi/w8/motion/<scheme>-<hop>/` — 8 real pane swaps.

---

## §3 THE BRACKET ROSTER handed to the OWNER (20 axes + 2 non-rows — the owner rules; the package NEVER self-rules)

Every axis reads `poleA ← target → poleB` with **each pole a NAMED reproducible state** (tuple /
commit / frame path — RF-3/4/5/6), indexed to its owner-verbatim line, carrying its LANDED
default. **Bulk-approve verb** (the whole set): *"Approve all landed defaults."* — then only the
un-landed axes need a per-axis word: **B-01** T-42 register · **B-02** T-48 residual · **B-07**
4th-alpha cell · **B-13** T-55 instrument · **B-16** content-heading weight (+ the un-landed poles
B-03 whisper / B-05 blob-seat / B-06 boot-target / B-08 blob-size). Full poles in `PACKAGE.md §2`.

### Group A — the §0.6 + checkpoint brackets

| # | Axis (owner line) | LANDED default | Status |
|---|---|---|---|
| **B-01** | T-42 shrunken scrolled header (+ T-61 §0.8 "padding/background should ALSO shrink") | the CURRENT compositor-only title `scale(0.618)` (Pole A) | **un-landed** — T-61 RULES the direction (whole-header contraction); the remaining bracket is the REGISTER, not the existence |
| **B-02** | T-48/T-58 card-transition motion residual ("janky") | the current swap; W9 re-verifies after W7's payload | **un-landed** — the payload MOVER stays W7's BY LAW (WR-10/T-58 MANDATE; O-16-R1 the confound); wake-gray compound half landed `5cecdf2` |
| **B-03** | R3/T-26 sage-whisper floor (D4 "a sage whisper, never silence") | silence (the CURRENT state) | **un-landed** — the owner rules whether the field carries a perceptible whisper at seedC=0 |
| **B-04** | R5/C-4 rail α face + the self-cut tnum face | Fira-Greek distinct glyph (landed `e2e8517`) | LANDED default; the C-4 donor-subset far pole → bracket (*the flag IS the deliverable*) |
| **B-05** | R7/C-3/P1-B3 blob-seat positive domain (T-30 "towards the centre") | corner-flush 0 (the RULED Q3 "Flush." default) | **un-landed** — the positive domain is the sanctioned NEW axis; Q3 "Flush." STANDS untouched |
| **B-06** | R8/Q2 boot-settle ("full comp.") | the crisp-but-late cadence; W7's levers move it | **un-landed** — the owner rules the TARGET, not the lever (payload = W7's RP-2/L20/GAP-L5) |
| **B-07** | C-2/P1-B2 header 4th-alpha cell (T-7 "or more if needed") | 3-cell tuple (Pole A) | **un-landed** — NAMED COST: the 4th cell re-derives the O-10b locks + Q11b capacity; a light remediation is FORBIDDEN |

### Group B — the §0.7 roster brackets (poles from `t49-research.md`)

| # | Axis (owner line) | LANDED default | Status |
|---|---|---|---|
| **B-08** | blob-size (T-49b "a bit bigger") | current `11rem` (un-landed) | **un-landed** — re-derives against WR-4's chosen title line (the two knobs interact) |
| **B-09** | fission-amp (T-49c meatball split) | the landed calm value (WR-2 `5cecdf2`, `[0.16,0.32]`) | LANDED default |
| **B-10** | veil-clarity (T-50 "should read as glass") | the WR-3 visible-glass α (`a8dc32a`, ~0.44/0.56) | LANDED default |
| **B-11** | title-step (T-51 "down by 1 or 2") | down-one `--type-display-2` (WR-4 `5f273ae`) | LANDED default; down-two far pole → owner |
| **B-12** | dark-caster register (T-53 top-border artifact) | the scheme-true shadow (WR-5/P6-R1 `31e7c09`) | LANDED default |
| **B-13** | verb-cluster instrument (T-55 "glass-ui dropdown or dock set") | the CURRENT three-register row (un-landed) | **un-landed** — the owner's instrument word chooses; NOT approximated by any lane (WR-7; E-2) |
| **B-14** | pastel band centers (T-56 "a pleasing pastel rainbow") | the landed honest-guard pastel (P4-R1/WR-8 `ccf4b30`) | LANDED guard is the PRECONDITION; the register inside it is the owner's call |
| **B-15** | rhythm clamp constants (T-59 — BOTH poles owner-photographed) | the landed one-law clamp (WR-11 `a8dc32a`+`bdd0a9c`) | LANDED default |

### Group C — the pass-minted brackets

| # | Axis (owner line) | LANDED default | Status |
|---|---|---|---|
| **B-16** | AB-5 content-heading weight (T-2 "both should be non-bold" — extends into the body?) | the shipped 600 (un-landed) | **un-landed** — the owner's non-bold lines named TITLES + OPTIONS, never content headings |
| **B-17** | E1-B1 extract track-voice (T-44a "un-readable") | the live-accent fill (landed `c2a911f`) | LANDED default; both poles clear 3:1 |
| **B-18** | P7-B1 gradient hatch-dominance (T-46 "un-usable" taste half) | `AXIS_HEADROOM = 1.15` (landed) | LANDED default |
| **B-19** | P8-B1 easing authoring-canvas (T-47 "too small") | the 19rem left-aligned (landed) | LANDED default; three arms |
| **B-20** | A-1 loading-skeleton register (T-41 "shimmer properly") | `variant="breath"` (landed) | LANDED default; app-wide |

### The 2 flagged non-rows (presented for the owner's eye — NOT brackets-for-ruling)

- **Rest-floor in-bracket read** — the bead/plate INK_FLOOR 0.15 ∈ [0.12, 0.20]; stays an
  in-bracket default; the F9.R1 producer knob swap is booked at W7.
- **Dark-scheme field/chrome split (GAP-L2)** — the L2 `lBand` producer atom absent; standing
  **P1** producer book, **no demo action (PR-2 fence)**.

---

## §4 The HG5 miss — the booked W9-handed re-encapsulation row (honest, not weakened)

**The breach** (recorded, not cured in this docs-only close): two demo files, both grown by W8
remediation, sit over the CLAUDE.md ≤400-LoC god-module cap — a named PP-8 caps leg and a
standing precept.

| File | LoC (baseline `5e4f1f6` → live) | Growth commit | Row that grew it |
|---|---|---|---|
| `demo/color-picker/composables/boot/useAtmosphere.ts` | 344 → **411** | `c116250` | P9-R3 / T-37 derive-seam guarantee |
| `demo/@/components/custom/markdown/Markdown.vue` | 396 → **408** | `433ae22` | AB-1 KaTeX scope-dead cure |

**Verified**: ZERO breaches at the pre-W8 baseline `5e4f1f6`; both over-cap at the assembly head
`6d95871` (411/408) and at close — so `PACKAGE.md §7`'s "row 5 MET" claim UNDER-REPORTS this
(recorded here, not laundered). Both are comment-heavy raw-LoC breaches (206/280 code lines), a
size-not-complexity class. PP-8's other legs are clean: `src/` as-any = 0 (the one grep hit is a
comment) · as-unknown-as = 8 (the documented irreducible class) · no tracked `*.log` residue.

**Why booked, not cured at close**: the CLOSE scribe pass is docs-only and path-scoped. Splitting
either module (the idiomatic cure — a cohesion-honest lift, as W6.5 did for `ink.ts`/
`useColorPipeline.ts`) is a **demo/ remediation act, not a gate act**; performing it in the close
commit would breach the close's own bounds. It routes as a **W9-handed re-encapsulation row**
(the honest §RF-8 disposition: a defect → a booked row, named). **No gate weakened** — the miss
is recorded as a miss, and the wave closes `_with_misses` because of it.

**The W9 row (named)**: *split `useAtmosphere.ts` (411) and `Markdown.vue` (408) back under the
≤400 cap by behaviour-preserving cohesion lifts (the W2 `a25c4d2` / W6.5 `69500b7` precedent) —
suites re-run green, no gate weakened.* Handed to `T.W9`.

---

## §5 The honest-red ledger + budgets + Q14 (no red weakened, no red hidden)

**The three standing born-RED legs** (EXPECTED-RED, `test.fail()`-designed; cites intact —
`PACKAGE.md §5`):

| Leg | State | Cite / route |
|---|---|---|
| **O-16-R1** (the 150ms transition-duration clobber) | EXPECTED-RED — LIVE in the shipped dist (PKT-1 unlanded); the KNOWN CONFOUND to isolate FIRST | **PKT-1 → W7** (communiqué `f3f3c097`; WR-10/T-58 names it) |
| **O-5** (the boot-pacing spike) | EXPECTED-RED — max ~234ms ≈ 4.7× median; the eager-payload mount task gating first paint | **RP-2 → W7** (PI-1; L20/GAP-L5 dead-payload abrogation) |
| **O-26** (aurora headless) | EXPECTED-RED headless (SwiftShader 0.000/255); the **headed annex is all-3-gates GREEN on real GPU** | headed annex GREEN → **W9** |

**The §6.2 budgets** (re-run): view-switch first-frame **GREEN** (83.4ms ≤ 100) · LCP **HONESTLY
RED** (≤ 2500) · TBT **HONESTLY RED** (≤ 300) → both → **Q14**.

**PI-1 — the fresh W8 delta**: appended to `audit/pi1-delta-ledger.md`. The W8 remediation is
demo-only + chunk-graph-neutral (like W1/W4/W6.5), so the CI expectation is **FLAT vs the
W4/W6.5 CI rows** (LCP 5141 / TBT 5988); budgets stay honestly RED, deltas NOISE (PP-10).
Render-blocking index CSS ~88.4 KiB gz ≤ 120 (32 KiB headroom). **No re-baseline.**

**Q14 adjudication material** (`PACKAGE.md §6` — WITH the owner, never pre-decided): the LCP/TBT
record + the named physical blocker (FCP ≈ 4.3s, the ONE eager-payload mount task = RP-2 / the
O-5 spike leg — **W7's PAYLOAD half**) + the pairing decision the owner rules at W8/W9 (budgets
GREEN on the W7 landings **or** a triumvirate-level owner escalation names the physical blocker;
no re-baseline / preset-swap / deferral — the Q14 ruling forbids all three).

**One integration re-aim, recorded honestly** (NOT hidden, NOT a gate weakening): the first
merged-head e2e read 158 passed / 2 failed — the two O-10 title-lock legs asserted the picker
`.space-trigger` ≡ `--type-display-3`, while WR-4/T-51 (owner MANDATE §0.7, landed `5f273ae`)
had stepped it DOWN one golden rung to `--type-display-2`. The oracle REFERENT was re-aimed to
the ruled token (`b551755`, e2e-scoped, separate from this docs commit) — the tighten-not-weaken
idiom (the same wave's O-14 precedent): the lock still asserts host-independence + weight 400 +
size ≡ the exact shipped token; only WHICH token the picker rides changed, **by owner mandate**.
**Result after the re-aim: 160 passed / 2 skipped / exit 0.**

---

## §6 Books carried into W9 (books, never gates — zero silent drops)

- **HG5 re-encapsulation row** → `T.W9` (§4; `useAtmosphere.ts`/`Markdown.vue` split).
- **The standing born-RED producer legs** → O-16-R1 (PKT-1→W7) · O-5 (RP-2→W7) · O-26 (aurora
  headless→W9). Carried unaltered into the package.
- **WR-10 / T-58 swap-window scheduling** → the payload half is **W7/W9's BY LAW** (confound
  isolation FIRST — O-16-R1's 150ms clobber LIVE in dist; the wake-gray compound half landed
  `5cecdf2`); W9 walk re-verifies.
- **The §0.7 producer halves** → BOOKED at the consolidated communiqué (glass-ui BI inbox @
  `f3f3c097`): the T-60/T-49a reveal-bloom door (with UF-E10) · the GAP-L5 `settled` seam · the
  T-52 dock-layer mask-honesty (P7·L13) · the optional `--dock-pill-h` rung · PKT-1 re-cited.
  T-53 is NOT booked producer-side (exonerated by bisection). Books, never gates.
- **The Q14 budget/preset pairing** → prepared here, ruled by the owner at W8/W9, encoded at W9.
- **The triumvirate routes** → pass-2 Row G (the o12 backing-ratio oracle leg) + the per-pass
  oracle blind-spots — bounds expansions handed to the triumvirate record, W9-inherited.
- **The OWNER's per-axis bracket rulings** (the 20 axes) → land in `VERDICT-2026-07-12.md`; the
  W9 walk consumes them BY PATH (per-axis rulings → the walk; further rows → routed by class).

---

## §7 Suites + evidence (at close)

- lint **0** (`eslint --max-warnings=0`) · typecheck **0** (`vue-tsc` lib+demo) · vitest
  **2241/2241** (74 files) · e2e full 6-project **160 passed / 2 skipped / exit 0** (lane-unique
  ports `VJS_E2E_PORT=8790` / `VJS_E2E_PERF_PORT=8791`; the owner's :9000 untouched).
- **PP-8 caps sweep: 2 demo files > 400 LoC** (`useAtmosphere.ts` 411 · `Markdown.vue` 408) —
  the HG5 MISS-RECORDED, booked to W9 (§4). Other PP-8 legs clean (`src` as-any 0 ·
  as-unknown-as 8 documented · no tracked *.log).
- **W7 trigger re-probe (2026-07-12)**: `git -C ../glass-ui tag --list 'v5*'` = EMPTY ·
  `npm view @mkbabb/glass-ui version` = **4.2.0** → **TRIGGER-NOT-FIRED** (the joint BG/BH 5.0.0
  cut is USER-GATED + untagged; PP-5: books never gates).
- **Tool-artefact grep** (`grep -rnE '</?(content|invoke|parameter|antml)'`) over the wave's
  touched docs: **CLEAN** (bound pre-commit — the §Recovery seam class M-1).
- **Foreign-tree fence held**: no edit to `src/`, `../glass-ui`, `../keyframes.js`,
  `demo/@/components/ui/`, `docs/precepts/`; producer rows stayed letters (P1-R3 · P9-W2 ·
  P9-W1 → the GLASSUI-T communiqué `f3f3c097`). The package + close commits are docs-only,
  path-scoped.
- **Merge record** (the FIRST assembler act, committed-green, --no-ff): lanes 14–19
  (`83d8714`→`41ba7c9`→`1e74cf3`→`18f8263`→`f6c595f`→`6d95871`) — `ROWS.md §0`.

---

## §8 Hand-off

**T.W9 flips DISPATCHABLE** — W8 CLOSED-`complete_with_misses` discharges the DAG dep. The close
wave inherits, BY PATH: the owner's verdict (encoded at `VERDICT-2026-07-12.md` **when the owner
rules** — the wave's honest OPEN-for-the-owner terminal state until then), the package as the
tranche's taste record, the booked rows (HG5 re-encapsulation · the born-RED producer legs ·
WR-10/T-58 payload · the §0.7 communiqué books), and the Q14 material. **The Q14 PERF-REDEMPTION
close gate binds W9: LCP/TBT budgets GREEN or the named owner escalation** (no re-baseline /
preset-swap / deferral).

**T.W7** stays **TRIGGER-NOT-FIRED** (re-probed 2026-07-12: no glass-ui v5 tag; npm 4.2.0) — it
FLOATS into whatever round is current when the tag lands, NOT on the critical path. Its payload
owns the standing born-RED producer legs, the WR-10/T-58 scheduling payload half, and the
`/parsing` scroll()/view() grammar dead-payload split (the Q14 W7 half).

The per-surface E-7 pass grammar minted here is the standing shape for successor tranches (the
S-era non-authoring taste reviews, formalized).
