# T.W4 — THE PICKER RECOMPOSITION (the C1 knot — ONE wave, single-writer, forced order)

**Name**: W4 — The picker recomposition (T-2 · T-7 · T-4 · T-5 · T-8 · T-23-geometry + T-15:
the header + console + seat re-authored as one composition)
**Opens after**: T.W2 + T.W3 (round 3). W4 REQUIRES W3 (the console/ink/seat composite is
judged against the SETTLED ladder) and consumes W2's settled boot (T-23's veil is judged over
the TRUE field) — the §3 DAG law.
**Spec of record**: `audit/SYNTHESIS.md §3` T.W4 rows (items W4-1..W4-7 + the FORCED order) ·
§2 D2 (THE ×φ TYPE RECALIBRATION) + D5 (THE PLATE + CONSOLE GESTALT) + D8 (THE SEAT) · §1.1
R3/R4 (what retires vs what SURVIVES) · Q3/Q11a/Q11b/Q4-console outcomes from `T.md §12` **as
ratified (the ratification record wins over the defaults quoted below** [AMENDED-AT-HARDENING —
h-refine-console F-3: the W6-header hedge carried here]**)** · the C1/C5
reconciliations (t-contradictions) · §6.1 rows O-10/O-11/O-12/O-18 · PI-3/PI-4 · the §6.2 CSS
tripwire [AMENDED-AT-PASS-2].
**On any divergence between this wave doc and its spec-of-record sections, the spec wins** (the
T.md charter clause, restated here so the rule is self-evident in-file); above both,
`MANDATE-2026-07-06.md §0` + addenda are VERBATIM LAW.
**Agents**: **1 serial writer over the C1 knot in the FORCED order** (§Forced order — the C1
reconciliation: these items contend for the SAME band geometry; parallel writers would
re-litigate each other's reservations). **W4-6 is the one file-disjoint sweep** (PaletteCard +
sibling title surfaces — outside the knot's file set) and may run as a second lane or fold into
the serial walk at the dispatcher's call. Fable + frontend-design (design lanes, E-6).
**Hard gate**: composite (§Hard gate) — O-10a-d type locks · O-12 blob-seat set (PI-4
same-commit law) · O-11 gates 1+3 re-run over the settled band · meter presence · ONE active
indicator · height gate @900px · **the CSS tripwire: re-measure CSS gz at this gate and RED
the wave at >120 KiB**.
**Status**: RATIFIED 2026-07-09 — PENDING, gated on T.W2 + T.W3 close (round 3; **Q3 RULED
"Flush."** — `--blob-seat: 0`; **Q11a RULED**: the producer-ladder sizing AUTHORITY, ×φ = two
token steps; Q3b/Q11b defaults stand). **NOTE (cascade 1): T.W4.5 — the mid-tranche checkpoint
— now sits between this wave and round 4** (`waves/T.W4.5.md`).

---

## §Goal criterion

The header + console + seat re-authored as one composition. (SYNTHESIS §3 T.W4 Goal, verbatim.)

## §Completion criterion

Every W4 item gate green (per-item gates transcribed; T.md §3.1 summary): O-10a
host-independence + height gate @900px + mobile matrix · O-10b per-space line-count locks +
O-10c tnum digit-advance · meter presence + the O-18 rung · O-18 console + ONE active indicator
+ roving-tabindex · the O-12 set with PI-4's same-commit law · O-10d family census · O-11 gates
1+3 re-run over the settled post-W2 field · CSS gz ≤120 KiB at the wave gate.

---

## §Forced order (SYNTHESIS §3 T.W4, VERBATIM — binding; t-contradictions C1)

**(1) W4-1 titles ×φ → (2) W4-2 readout tuple (reservation re-scope FIRST at the phone band —
t-mobile F-4.4) → (3) W4-5 SEAT (reservation re-derives; T-7's freed band is the seat's) →
(4) W4-4 console + W4-7 veil re-derive against the settled band.** W4-3 (the channel strip)
rides the console leg (same file family as W4-4, after the band settles); W4-6 is
order-independent (file-disjoint population sweep).

## §Scope (SYNTHESIS §3 T.W4 item table, transcribed verbatim — gates intact; anchors + evidence appended)

Anchors below are pre-W1 (as-audited) coordinates; the wave re-derives them against the
committed MOVE-MAP at wave-open (PP-11).

| # | Item | Gate | Anchors · evidence |
|---|---|---|---|
| W4-1 | Titles ×φ non-bold (D2); About inline member inherits `1em`; **re-target the compositor title-shrink RATIO** (rest scale 1 at display-1 → stuck scale = heading/display-1; NO font-size keyframe — W3-4 owns the compositor conversion, W4 re-targets its ENDPOINTS; W3's gate-4 stays green by construction) [AMENDED-AT-HARDENING — M-18: "shrink keyframes re-lock" was the layout-keyframe vocabulary W3-4 retires; the "W3=material / W4=geometry" framing corrected accordingly. **W3-4's F3 option is PINNED: the compositor-transform arm** — the consume-vs-transpose fork no longer floats]; em-relative caret/underline | O-10a; height gate @900px (`--content-max-h`); mobile matrix | `ColorSpaceSelector.vue:35,37` (no weight utility → About inherits 700 — R4's latent BUG, S-21 broken on the weight axis); `PaneHeader.vue:12,122-129`; the D2 landings: picker title display-1→display-3 (41.89→67.78px @1440) · pane title heading→display-1 (25.89→41.89, phones floor-pinned = deliberate no-op); weight 400 carried ON the trigger's own class list · t-title-typography F1-F3/F8; t-mobile F-4 (About = honest 2-line <sm) |
| W4-2 | The contiguous tuple + tnum self-host + per-space locks (levers 1+2, lever 3 for lab-class — **Q11**); the W4-2 gate text re-authored ("every space inks its own locked line count") | O-10b + O-10c | `ColorComponentDisplay.vue:12,21` (full-span dies per R4); `readoutReservation.ts:55-89` (20ch → 12.36ch at ×φ; the re-scope is a PRECONDITION at 390 — t-mobile F-4.4); tnum is a NO-OP on Google-served Fraunces (F5) — self-host with a VERIFIED `tnum`; readout display-2→display-4-class rung (33.77→54.64 @32rem); D2: intrinsic cells, tuple-level anti-reflow re-earned · t-title-typography F4-F6 |
| W4-3 | The channel strip (T-4): name·signal·meter; live meter from `COLOR_MODEL_KEY` (zero new state); static ranges retire to their two owners; hover tooltip dies | meter-presence + O-18 rung | `ComponentSliders.vue:49-51` (static ranges); `:88-94` (hover-jailed live value) · t-misc-elements F1; plan-audit-2 F5 |
| W4-4 | The console — the material per the Q4 RATIFIED row (default rung-2 WELL, C5; **if the quiet-glass arm is ruled, the O-18 referent re-prices to live composited lightness and this gate text updates with it** [h-refine-console F-3]): well sub-card + ink letters speaking **each channel's TRUE glyph per-space (`L a b α` is the lab exemplar, not a stamp** [N-1]**)** + dock-trigger state ladder + the interim seal-recipe ring (producer swap booked, P5) + WatercolorDot active seat + touch rung (≥44px hits <lg; tooltip re-seat) + **the chassis-persistence law: space-change re-keys the ROWS only — the console card and ring are persistent chassis, never re-mounted scenery** [N-2] + **the readout-frac guard-then-alpha CURE (handed from W3-5's contract — h-dag D-4)**. **THE POPULATION CLAUSE** [AMENDED-AT-HARDENING — M-34]: the console grammar (well seat, ink labels, live meter where a live value exists, touch rung) **extends to `ConfigSliderPane` (the public Atmosphere + Blob views — the app's SECOND slider population)**; O-18 gains config-slider rows | O-18 (incl. config-slider rows); ONE active indicator; a11y roving-tabindex verbatim | `ComponentSliders.vue:171-182,337,343-350`; measured 1.01:1 dark (T-5's contrast defect — the W3-5 contract prices the cure); C5 settles the material: rung-2 WELL, opaque tone-step, NO blur (the Q4 owner-word re-cut); letters speak INK (the channel-color conceit retires — degenerate by construction); the letter column = a vertical micro-dock ring in `--accent-view` (seal-rim recipe turned portrait) · t-sliders-hierarchy F-1..F-6; t-a11y-contrast F-1; t-mobile F-5 |
| W4-5 | THE SEAT (D8): flush anchor + one cqi formula + `--z-ornament` + contact-shadow depth + closed-form ink floor + the leave-bind/park/tooltip beats (park stays wall-clock until P6's `settled` seam; booked) | **O-12 set** (seat identity · occlusion · hover budget · mobile gate RE-DERIVED from the formula · timing fixture centralized) — PI-4's same-commit law | `ColorPicker.vue:344-375` (the R3 corner-break law DIES: center-on-radius-origin + ≥25%-overflow arms + the <lg 8rem hand arm); `HeroBlob.vue:26-28` (no `@mouseleave`), `:156-171` (wall-clock park); orbit-reach 0.49 ≤ 0.5 containment identity (no clip/no dock collision/no seam skewer BY CONSTRUCTION; all three mobile bands collapse to one composition — t-mobile F-3); `--blob-seat` default 0 (**Q3** flush); the ink law = \|ΔL(bead, plate)\| floor, closed-form (inside the 12ms drag headroom — PI-3); the 7-beat hover/morph score (t-blob-hero §3) — demo beats now, engine floors via P6 · t-blob-hero §2/§3/§4; perf PI-3/PI-4 |
| W4-6 | Display-voice population sweep (T-15): PaletteCard + sibling `text-subheading`-as-title sites; rename-input/line-clamp re-verified under the serif | O-10d family census | `PaletteCard.vue:49` `text-subheading` → Jakarta 600 20.352px vs every sibling title = Fraunces; population, not the one card; non-italic for user-data names (D2) · t-title-typography F7 |
| W4-7 | Veil earn-range + shrink re-derived from the taller band (keyed to OCCLUSION, not the shrink); the T-23 rest-floor bracket RE-JUDGED over the settled post-W2 field (W3-4 calibrated parallel to the boot fix — AMENDED-AT-PASS-2) | O-11 gates 1+3 re-run | `PaneHeader.vue` (GEOMETRY only — W3-4 settled the material; this wave re-derives the earn-range against the ×φ-taller band) · t-header-shading §3/§4 |

## §Triumvirate dispatch

Mandatory on:

- **bounds expansion**: any `../glass-ui`/`../keyframes.js` write (the letter-rail primitive,
  the solid-ring register, the engine pose/`settled` seam, and the type stations are
  P5/P6/P10 — interim demo forms are sanctioned ONLY as booked swaps); any write to the tier
  tokens or `PaneHeader.vue` MATERIAL (W3's settled surface — this wave holds geometry only);
  any `src/` write; any new font-size value off the token scale (a D2 breach is a halt, not a
  judgment call);
- **non-local gate failures**: O-10b failing at 390 BEFORE the reservation re-scope lands (the
  forced order was violated — t-mobile F-4.4 makes the re-scope a precondition, not a fix-up);
  the O-12 mobile gate disagreeing with the seat formula's computed bound (the formula, not the
  gate, is then in question — PI-4's re-derivation is the point); the closed-form ink solve
  exceeding the 12ms drag headroom (PI-3 — route to redress, never ship an iterative solve on
  the drag path); O-11 gate 1 landing outside [0.45, 0.65] over the settled field (ratification
  material — the bracket is the owner's frame);
- **loop halt**: the third iteration of any reservation/seat geometry loop halts and routes
  (the band is ONE contended resource — that is why the order is forced);
- **the @900px height-gate failing under the ×φ titles routes to RATIFICATION — never shrink the
  ×φ title to pass the gate** (the owner's size ruling outranks the height gate; the gate failure
  is bracket material) [AMENDED-AT-HARDENING — h-wave-w4-w5 S4].

## §File bounds · disjointness · worktrees

| Item | Files (the knot set, SYNTHESIS verbatim + per-item) | Access |
|---|---|---|
| knot (W4-1..W4-5, W4-7) | `ColorPicker.vue` · `ColorComponentDisplay/` · `ComponentSliders/` · `readoutReservation.ts` · `PaneHeader.vue` (geometry only) · `HeroBlob/` · `ColorSpaceSelector.vue` (trigger class list) · the compositor shrink-ratio wiring + font self-host (W4-2's verified-tnum face — **W2-3's self-host carries the explicit tnum-RETENTION precondition: the face W2 preloads is the tnum-verified face, O-10c depends on it; W4-2's `index.html` permission is scoped to the font wiring hunk ONLY, coordinated through W2's file record** [AMENDED-AT-HARDENING — h-wave-w4-w5 S3]) | modify |
| W4-6 (disjoint sweep) | `PaletteCard.vue` + sibling `text-subheading`-as-title sites · rename-input/line-clamp re-verify | modify |

Single-writer (§3.2): ONE serial writer over the knot set, forced order; W4-6 alone may run
beside it (zero file overlap with the knot). Do NOT touch: `../glass-ui`, `../keyframes.js`,
`src/`, the tier tokens (consume only — W3 settled them), the boot chain (W2's, settled),
`docs/precepts/`. Worktrees: the knot writer on the wave head; W4-6's lane (if split) in a
sibling worktree.

## §Hard gate (verbatim-faithful to SYNTHESIS §3 T.W4 + §6.1)

1. **W4-1**: O-10a host-independence census — the trigger's computed family/style/size/WEIGHT
   identical across hosts (host-divergence itself is the red condition; the About-700
   inheritance bug dead by construction); the height gate vs `--content-max-h` @900px; the
   mobile matrix (About = honest 2-line <sm; phones floor-pinned = deliberate no-op).
2. **W4-2**: O-10b per-space line-count locks (all 17+hex @32rem + 390 — "every space inks its
   own locked line count", the re-authored gate text; Q11 levers 1+2, lever 3 for lab-class;
   lever 1 REQUIRED at 390) + O-10c tnum equal digit-advance on the SHIPPED face (the
   self-host regression class); the reservation re-scope landed FIRST (phone band).
3. **W4-3**: live meter present per row (from `COLOR_MODEL_KEY`, zero new state); static
   ranges retired to their two owners; the hover tooltip dead; the meter's ink at its O-18
   rung.
4. **W4-4**: O-18 green over the console population (letters, meters, captions on the WELL
   ground — the W3-5 contract, fed the console's tier lightness); **ONE active indicator**
   (the WatercolorDot ring stays the ONE live-color voice); a11y roving-tabindex verbatim;
   touch rung ≥44px hits <lg; the interim ring is the seal recipe turned portrait (P5 swap
   booked).
5. **W4-5**: the **O-12 set** green — seat containment identity (orbit-reach ≤ 0.5 +
   `--blob-seat` resolve) · occlusion (`elementFromPoint` never dock) · hover-mood frame-diff
   floor · hover-active frame budget (the NEW state, ungated today) · mobile width bound
   COMPUTED from the seat formula · ONE timing fixture replacing the three duplicating specs —
   **PI-4's same-commit law: the re-derived mobile gate lands in the SAME commit as the seat
   formula**; the ink floor **∈ [0.12, 0.20] OKLab L, default 0.15** [AMENDED-AT-HARDENING — M-9:
   the value finally named; owner eye-judge inside the bracket at W8; the landed value recorded
   with the commit], closed-form inside the 12ms drag headroom (PI-3, measured); the hover-mood
   floor ≥6/255 within 400ms + hover-active p50 ≤20ms [h-refine-doctrine F-7].
6. **W4-6**: O-10d display-voice family census green over EVERY title surface (population, not
   the one card); rename-input/line-clamp re-verified under the serif.
7. **W4-7**: O-11 gates 1+3 re-run green over the settled post-W2 field (rest floor re-judged in
   the Q9 EFFECT bracket — **27–39% added card material**; gate 3's ink-contrast floor referent =
   the D6/O-18 certified rung [AMENDED-AT-HARDENING — h-refine-doctrine F-9]; no double-exposure
   against the taller band); the earn-range keyed to OCCLUSION, not the shrink.
8. **The CSS tripwire [AMENDED-AT-PASS-2]**: CSS gz re-measured at this gate; **>120 KiB REDS
   the wave** (the type recalibration is a named spender).
9. PP-8 repo-wide sweep · PI-1 Lighthouse delta recorded · `npm run lint` 0 · `npm run
   typecheck` 0 · `npm test` green · e2e green (incl. the O-10/O-12 mints + the centralized
   timing fixture) · clean `git status`.

## §No-workaround prohibitions (binding)

- **The forced order is binding** — no parallel writers inside the knot; no "provisional"
  reservations to unblock a later item early (C1: the band is one resource).
- **Every type landing is an exact shipped token** (D2) — no new font-size values; a literal
  ×1.5 mints a non-token size and is the REJECTED Q11 alternative.
- **No declared-but-dead `tabular-nums`** — the tnum face is self-hosted and VERIFIED by
  rendered digit-advance (O-10c), never by declaration (the F5 class).
- **No ch-reservation return** (R4 retired the full-span + per-cell `ch` reservation) — the
  tuple is intrinsic cells; anti-reflow is re-earned at tuple/line level.
- **ONE cqi seat formula** — no per-band hand arms (R3 retired the 8rem arm and the
  corner-break law; Q7 full presence + the slot-owned token SURVIVE).
- **The interim letter ring rides the SEAL recipe** — never a new bespoke ring class (P5 is
  the standing law; T-28's register law binds any ring to the dot's own silhouette).
- **No iterative ink solve on the drag path** — closed-form inside the 12ms headroom or route
  to redress (PI-3).
- **`PaneHeader.vue` material untouched** — geometry only; the material is W3-4's settled
  work.

## §Format + lint cadence

`npm run lint` + `npm run typecheck` + `npm test` after each forced-order step and before
close; `npx playwright test` (incl. O-10/O-11/O-12) after W4-2, after W4-5, and at close; the
CSS gz measure + PI-1 LHCI run at the wave gate; the 12ms ink-solve measurement with the W4-5
commit. The tool-artefact grep `grep -rnE '</?(content|invoke|parameter|antml)'` over the wave's touched docs MUST be empty before any docs commit (the §Recovery seam class — M-1).

## §Verification artefacts (π lane)

Saved at close (cited in `PROGRESS.md`): the O-10a-d census tables (host-independence ·
per-space line locks @32rem+390 · digit-advance record · family census); the O-12 evidence set
(containment resolve, occlusion probe, frame-diff/budget numbers, the formula-derived mobile
bound, the ONE timing fixture path); the O-11 gates 1+3 re-run record over the settled field;
the meter-presence capture; the CSS gz number at gate; π paired frames (both schemes × 3
viewports — the picker is the hero surface of the W8 owner package); per-item commit hashes.

## §Commit plan

Forced-order, per-step commits: W4-1 (titles ×φ, body cites the About-700 kill); W4-2
(reservation re-scope FIRST as its own commit, then tuple + tnum self-host + locks); W4-5 (the
seat — formula + re-derived mobile gate + timing fixture in the SAME commit, PI-4; body cites
Q3/R3); W4-4 + W4-3 (console + channel strip, row-scoped; interim-ring commit body carries the
P5 book); W4-7 (veil geometry re-derive); W4-6 (display-voice sweep, own commit); a status
commit at close.

## §Recovery (STANDING — the `T.md §8` completion-brief rider binds every dispatch AND resume of this wave; PP-14/PP-15 operationalized) [AMENDED-AT-HARDENING — M-29/h-exec-recovery]

The four-step protocol (audit-partial → patch-brief at `audit/recovery/T.W<n>-<lane>-brief-<date>.md` → resume-from-work-order → seam-audit-at-close) is standing law in `T.md §8`; E-6 batches-of-three is the prevention half, this rider the cure. This wave's type-specific deltas:

**Partial signature (W4-specific)**: a wall mid-forced-order leaves a **PROVISIONAL RESERVATION in
the ONE contended band** (C1) — a resumed agent that INHERITS a half-derived reservation
re-litigates exactly what the forced order exists to prevent; a wall inside the PI-4 same-commit
step leaves the seat formula committed without its re-derived mobile gate = a structurally broken
commit.
**Resume specifics**: **re-derive the band from the LAST COMPLETED forced-order STEP's COMMITTED
state, never from an uncommitted half-step** — if the wall fell mid-step, that step is re-driven
WHOLE against the prior step's committed band (the §No-workaround "no provisional reservations"
law binds the resume literally). The 6 forced-order commit points are the FORMAL PP-14 resumption
units — expect ≥2-3 recoveries on this wave [h-exec-budget F2]. Seam-audit: verify the PI-4
same-commit invariant held; a formula/gate split across two commits is a partial-fire to re-drive.

## §Dependencies

- **Depends on**: T.W3 (the settled ladder — console WELL tokens, the ink-on-tier contract,
  the settled header material) + T.W2 (the settled boot field — W4-7's re-judge ground) + the
  Q3/Q11 rulings (+ Q4's console row).
- **Blocks**: T.W5 + T.W6 (round 4) and the W8 package (the picker is its hero surface).

## §BOOKS opened/serviced (books-never-gates)

- **W4-4 interim seal-recipe letter ring → the P5 letter-rail primitive** — booked swap; fires
  when P5 lands (S-3 upgraded to MANDATED by T-5; do not wait on the book).
- **W4-5 wall-clock park → park-only-from-`settled` (P6 seam)** — booked swap.
- **The satellite emergence + HERO preset halves (GAP-L5 rows A-E)** — producer-gated; land at
  W7 (the ≥96px bead gate); W4-5 ships the demo beats only.
- **The P10 type stations** (SelectTrigger size station · weight tokenization) — the demo pins
  400 at `:root` now; the token consume fires when P10 answers (verify-first — the rung may
  exist).
- **T-28's register law** — encoded here as the ring prohibition (any future ring rides the
  dot's own silhouette or does not exist); the abrogation itself is W6-7's.

## §Evidence packets consumed

`audit/lanes/t-title-typography.md` (F1-F8, §1) · `audit/lanes/t-sliders-hierarchy.md`
(F-1..F-6) · `audit/lanes/t-misc-elements.md` (F1) · `audit/lanes/t-blob-hero.md` (§2/§3/§4) ·
`audit/lanes/t-mobile-audit.md` (F-3/F-4/F-5) · `audit/lanes/t-a11y-contrast.md` (F-1) ·
`audit/lanes/t-header-shading.md` (§3/§4) · `audit/lanes/t-contradictions.md` (C1/C5) ·
`audit/lanes/t-perf-implications.md` (PI-3/PI-4) · `audit/lanes/t-plan-audit-2.md` (F5).

## §Hand-off

Round 4 (W5 ∥ W6) opens on this wave: W5 retunes motion over a picker whose geometry is
settled; W6-7's ring abrogation inherits the register law encoded here; W6's surfaces inherit
the display voice. The recomposed picker headlines the W8 owner certification package (π frames
from this wave are its raw material). W7 fires the P5/P6 booked swaps against this wave's
interim forms and re-measures RP-2 with the blob's L20 split.
