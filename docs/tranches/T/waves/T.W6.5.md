# T.W6.5 — THE OWNER-AUDIT REMEDIATION WAVE (T-33..T-48; the §0.6 proper addendum)

**Name**: W6.5 — Owner-audit remediation (the 2026-07-11 owner audit's execution wave — the
"proper addendum" the owner ordered; **[OWNER-2026-07-11]**, minted by the triumvirate's
tranche-update lane per `MANDATE-2026-07-06.md §0.6`'s process order).
**Opens after**: round 4 (T.W5 ∥ T.W6) closes — round **4.5**, before T.W8 (the DAG amends
`… → { W5 · W6 } → T.W6.5 → W8 → W9`). Opening on the MERGED round-4 tree is load-bearing: six
of the sixteen §0.6 rows bind the in-flight round-4 lanes as merge constraints and are
**VERIFIED here, never duplicated**.
**Spec of record**: `MANDATE-2026-07-06.md §0.6` (verbatim law — the 18 kernels / 16 rows
T-33..T-48 + the OVERLAP column) · **`audit/t33-research.md`** (the triumvirate RESEARCH lane —
root causes, genesis referents, probe evidence; §-cites below are into it) · `T.md §4` **R12**
(the ShadowPalette overrule row this wave executes) · `audit/SYNTHESIS.md §2` (D1–D10).
**On any divergence between this wave doc and its spec-of-record sections, the spec wins**;
above all `MANDATE-2026-07-06.md §0` + addenda are VERBATIM LAW.
**Agents**: the four single-writer lanes below + one verify agent, dispatched in batches of
three (E-6); Fable + frontend-design on every design surface; E-3 re-bound by owner order
("No adhoc temp fixes. NO quick solutions, NO workarounds").
**Routing decision (recorded)**: MINTED as its own wave, not folded into W8 — the research
verified T-33/34/35/40/41+44/45 + the mechanical set have **no in-flight owner**; W8's
remediation lanes land rows filed by ITS critique passes, its scale is already the DAG's
fattest by an order of magnitude (M-32), and parking owner-ordered cures behind the whole
critique cycle inverts the checkpoint lesson (Q6 "both." — catch and CURE early). W8 then
re-judges these surfaces critique-fresh.
**Hard gate**: composite (§Hard gate) — the VERIFY-AT-ROUND-4-GATE walk complete · per-lane
gates green · O-9 re-aimed + O-18 two new legs live · PI-1 delta recorded · suites green.
**Status**: MINTED 2026-07-11 — PENDING, gated on round-4 close (T.W5 + T.W6 both closed).

---

## §Goal criterion

Every T-33..T-48 row is discharged at its root per the §0.6 OVERLAP reconciliation: the
round-4-bound rows VERIFIED at the merged tree, the overruled/new rows CURED by design-grade
lanes (never point patches), the producer row RELAYED (T-38 — dispatched with this addendum),
the Q14 row's levers NAMED to their standing gates — so W8 opens on ground the owner has
already re-judged once.

## §Completion criterion

The verify walk filed (6 rows, each VERIFIED-or-reopened); Lanes S/P/I/M landed with their
per-finding gates green; the T-47 conditional row closed VERIFIED or executed; T-42 + the T-48
residual handed to W8 as named brackets; T-39's levers named at W7/W9 (no gate re-routed);
zero silent drops across the sixteen rows.

---

## §Scope — the T-33..T-48 disposition map, then the lanes

**VERIFY-AT-ROUND-4-GATE (the verify agent walks these against the round-4 close artefacts +
the live merged tree; a miss re-opens as a W6.5 row in the owning files — round 4 is closed,
so single-writer holds):**

| Row | What round 4 owes it | Verify referent |
|---|---|---|
| T-36 | Lane D: Tools trigger true-button box-model (research §6.3 — inline padding ∝ cap radius; the 0fr-reveal crop released with W6-8) | computed padding/margin probe vs the glass-ui Button scale |
| T-37 | Lane D: collapsed-seal swatch visibility (Q12 rim dies; the cure is MATERIAL — §6.4's derive-seam lightness-delta arm is the handed design ground: the atmosphere `lBand` never equals the wax L) | figure/ground delta probe on a self-derived field |
| T-40b | Lane D: NON-BOLD consumed on every dropdown it re-authored | computed weight 400 on option letterforms |
| T-43 | Lane D: the Q5 ramp ×2 sites (owner CONFIRMS; landed `5833474`) | O-14 re-pointed referent green |
| T-46 | Lane G: the gradient instrument re-author (unusable verdict + glass-ui rounding bind its merge gate; rail terminal caps rounded per `rounded-card`) | O-21 + the rounding delta vs shot t33-audit-14 |
| T-48 | W5: the frame-by-frame re-analysis binds its gates (mid-flight captures = the F2 evidence class); the payload half of "janky" is W7's (§4) | O-16 census + the frame captures; residual motion taste → the W8 bracket |

**Lane S — THE SHADOWPALETTE FIRST-PRINCIPLES REDESIGN (T-41 + T-44's redesign half; executes
`T.md §4` R12):**

1. The as-filler deployment RETIRES at the three empty-host classes (Browse wall · Commons
   wall · Mix) — the TRUE empty state only, `no palettes found` never preceded by ghosts.
2. The watercolor three-dot + dashes placeholder RESURRECTED (research §2.1 — the R.W4 ghost
   trio never left the tree; it was shed by the N-3 `dots=false` clause): the hosts stop
   passing `:dots="false"`; the N-3 clause is RE-AIMED, not deleted — at true-empty the DOT
   register is the one that speaks.
3. The species REDESIGNED from first principles with the **genesis extract-era register
   assayed directly** (§2.2 — `ec1b200`'s PaletteCardSkeleton: card-true material, solid
   hairline edge, muted blocks, a LIVING staggered pulse at `i × 0.12s`): the standing
   INSTRUMENT face (Extract's k-threaded undeveloped plate) gets the living register, PRM
   degrades it static for free; R7's semantics stack SURVIVES untouched (aria split ·
   error ≠ empty · never announce work that isn't happening — announcement, not motion).

**Lane P — the console + readout re-seat (T-34 + T-33):**

4. T-34: the sliders console re-seats from the Q4 rung-2 WELL onto the glass-ui
   `<Card surface="veil">` / `veil-surface` (§6.2) + the air recalibration (row-gap/padding).
   The owner's own word re-opens the RATIFIED Q4 on the material axis — encoded (MANDATE §0.6
   + this row), never silent. **The D6 ink referent moves with the material**
   (`resolveSurfaceLightness` → the veil's rung) and the O-18 W4 rows re-run on the new ground.
5. T-33a — **the dynamic-max value-domain law**: the live value clamps into the space's ranges
   at the demo model write seam (`useColorPipeline`; ranges from `getColorSpaceBound`) — the
   static reservation table's worst case becomes TRUE by construction. Library-adjacent: the
   src parser keeps parsing out-of-range CSS lab() legitimately; NO src write.
6. T-33b — the readout↔rail seam: the reserved-line blank band dies (§6.1 measured: 61px of
   the 122px lock inks blank at a one-line tuple). Both research arms are the design ground —
   the seam absorbs the reservation as DESIGNED AIR, or the reservation rides the live
   line-count class inside the lock's card-never-moves law; the lane designs with Fable;
   an unresolved taste residual rides to W8 as a bracket, the FUNCTIONAL gap cure lands here.

**Lane I — the ink delineation (T-35 + T-44's A-class half + T-40a):**

7. T-35 root: `certifyAccentInk` cured (§5.1 — certify against the SURFACE rung, not the one
   page-ambient referent; walk the gamut CUSP (max-C per L) instead of constant-C-then-clamp;
   the cream collapse `oklch(0.97 0.014 32.6)` dies — "the current contrast friendly color"
   stays the CURRENT color).
8. "Delineate all" cured as a CENSUS EXTENSION, the roster gap closed BY NAME (§5.2): O-18
   gains the **IDENTITY leg** (accent-voice rows assert hue ≈ live accent AND C ≥ floor) +
   the **GRAPHICS leg** (WCAG 1.4.11 ≥3:1 — slider tracks, meter rails, checker tiles); the
   delinquent set swept onto the contract: the 8-file `text-muted-foreground/NN` F-4
   survivors + the `opacity-60` specimen-row survivor.
9. T-44a: the Extract kC/K slider tracks re-inked against the well ground (`--slider-track-bg:
   var(--muted)` dark-on-dark dies; §6.7) — the graphics leg is its born-RED gate.
10. T-40a: the option-letterform bold dies via the W4-1 idiom — `font-weight:
    var(--type-weight-display)` pinned on the surface's own class (the root is glass-ui's
    `text-title` @utility 700 hardcode, §6.5; P10 weight-tokenization stays the producer cure).

**Lane M — the mechanical set (T-45 + the three demo-now perf movers, §4.3):**

11. T-45: the card border-clip cured — the bisection-proven backdrop-filter edge-sampling
    bleed (§6.8) gets the demo-interim **oversampled-pseudo** cure on the pane-card recipe
    (filter onto `::before { inset: calc(-2 * blur) }` under the host's radius clip); the
    producer glass-recipe root rides a BOOKED P3-family letter rider (never a demo fork of
    the ladder).
12. The 10 residual root-barrel `from "@mkbabb/value.js"` imports → subpaths (drops the
    32.3 KiB gz scroll-timeline chunk + barrel graph from eager; mechanical, zero design
    risk).
13. B2/B4 de-coincidence: the blob engine mount schedules off the field-arrival frame (idle
    slice / next beat — the W2 beat DAG owns the ordering; the ~505ms one-frame wall splits).
    W2's closed boot files re-open under THIS wave's writ, single-writer.

**Row E — the easing seat (T-47, CONDITIONAL — verify-first):** if Lane E's round-4 landing
already speaks the owner-escalated spec (§3.4: container-typed seat so `38cqi` resolves
against the pane · the kf division — closed rows = selection gallery, open row = the ONE
vendor `EasingPicker`, `:playback="false"` · SegmentedTabs + regex autoplay DELETED · ≤1
cartoon stamp), the row closes VERIFIED; else it executes §3.4 in Lane E's files. The
keyframes.js assay (§3.1–§3.2) is the reference table of record — **seat, don't re-author**;
compactness comes from deletion.

**Named routings (no work here, no silent drops):** **T-38** → the P1-family producer rider
DISPATCHED with this addendum (the letter's `§P1 rider` records the inbox SHA); verify-at-cut
W7; the W2-5 pointer retune stays DEFERRED on F-10. **T-39** → Q14 PRESSURE: the gates STAY
W7 (RP-2 eager-mount · L20 · GAP-L5 · the About/KaTeX boot ride, sharpened by §4.2) and W9
(green-or-escalation); Lane M's movers are demo-now contributions recorded in PI-1, never a
re-route. **T-42** → the W8 shrunken-state bracket (§6.6's mechanism handed by name).

## §Triumvirate dispatch

Mandatory on: any `src/` write (the T-33a clamp is DEMO-side by design) · any
`../glass-ui`/`../keyframes.js` write (T-45's root, P10, F-10 are letter rows) · a lane
finding a round-4 merge constraint UNMET (re-open, and the round-4 close record gains an
honest annotation — never a quiet fix) · any weakening of R7's surviving semantics stack
(E-3's over-delete hazard, named in R12) · **loop halt**: the third iteration of any register
tuning halts and routes (taste → W8/the owner).

## §File bounds · disjointness · worktrees

| Lane | Files | Access |
|---|---|---|
| verify | round-4 close artefacts + the live merged tree (probe-only) | probe |
| S | `common/EmptyState.vue` · `card/ShadowPalette.vue` (+ colocated kin) · the three host files (Browse/Commons/Mix) | modify |
| P | the picker console/readout tree (`ComponentSliders/` · `ColorComponentDisplay/` · `readoutReservation.ts` · `useColorPipeline` clamp seam) | modify |
| I | `composables/color/ink.ts` · the O-18 census spec · `ExtractControls.vue` (tracks) · the option-letterform class site + the 8-file F-4 sweep set | modify |
| M | the 10 root-barrel import sites · the boot beat files (B2/B4 seam — W2 re-opened by writ) · the pane-card recipe (T-45 pseudo) | modify |
| E (conditional) | Lane E's W6-3 files ONLY, iff the verify fails | modify |

Do NOT touch: `src/`, `../glass-ui`, `../keyframes.js`, `docs/precepts/`, the §4 retired
specs. Lane worktrees cut from the wave head; anchors re-derived via the W1 MOVE-MAP (PP-11).
Overlap note: Lane I's letterform pin site and Lane P's console files are disjoint by
inspection at dispatch — if an anchor re-derivation collides, the hunk re-homes to ONE lane
(the W8 M-20 device).

## §Hard gate

1. The verify walk filed: 6 rows each VERIFIED (evidence cited) or REOPENED-and-landed; zero
   unwalked.
2. Lane S: O-9 census RE-AIMED and green all-cases — the trio + dashes present at every
   true-empty host; zero ShadowPalette-as-filler (locator sweep over the three host classes);
   the instrument face pulses live + PRM-static; R7 semantics assertions unchanged and green.
3. Lane P: the clamp oracle green (999 → the space max, per-space, vitest); the blank-band
   measure green (reserved-vs-painted delta = designed air, not dead band); O-18 W4 rows
   re-run green on the veil ground; O-10b line-lock + the card-never-moves law still green.
4. Lane I: O-18 + IDENTITY leg + GRAPHICS leg green, both schemes (the cream-collapse repro
   from §5.1 now certifies a chromatic ink); the F-4 grep zero on the named 8-file set;
   option letterforms computed weight 400.
5. Lane M: the eager graph proof (scroll-timeline chunk ABSENT from eager; demo root-barrel
   grep zero); the b2/b4 longtask split shown on a re-run of the §4 profile (archived
   before/after); T-45 rim-gone frames both schemes (the §6.8 bisection probe re-run green);
   the P3-family rider BOOKED by name.
6. Row E: VERIFIED record, or O-17 + the §3.4 conformance green (canvas ≥ ~200px at desktop
   pane width; no wrapped pill blob; the picker owns mode).
7. PP-8 repo-wide sweep + PI-1 Lighthouse delta recorded (the Lane-M movers are the first
   expected non-NOISE demo delta — recorded either way, budgets adjudicate at W7/W9);
   CSS tripwire ≤120 KiB gz.
8. `npm run lint` 0 · `npm run typecheck` 0 · `npm test` green · e2e all-project green ·
   clean `git status`.

## §No-workaround prohibitions (binding)

- **E-3 by owner order**: no adhoc temp fixes — the ShadowPalette redesign is a species
  redesign, never a CSS mute of the current one; the ink cure is the CONTRACT, never per-site
  color pins.
- **R7 survives**: the aria split · error ≠ empty · never-announce-work-that-isn't-happening
  are NOT motion clauses — a shimmering `aria-hidden` plate does not lie to AT; over-deleting
  them is the named R12 hazard.
- **No src writes** for the clamp (parse must keep accepting out-of-range CSS); no demo forks
  for T-45's root / P10 / F-10 (producer letters).
- **No re-litigation** of §12 RULED outcomes beyond what the OWNER's own §0.6 words re-open
  (Q4's material axis; the D9 species) — both encoded, never silently widened.
- **Verified means verified** — a round-4 constraint taken on the lane's word is a process
  breach; the verify agent probes the live tree.

## §Format + lint cadence

`npm run lint` + `npm run typecheck` + `npm test` after each lane batch; `npx playwright test`
(incl. the re-aimed O-9 + extended O-18) per lane close and at wave close. The tool-artefact
grep `grep -rnE '</?(content|invoke|parameter|antml)'` over the wave's touched docs MUST be
empty before any docs commit (M-1).

## §Verification artefacts

Saved at close (cited in `PROGRESS.md`): the verify-walk record (6 rows → evidence); per-lane
before/after frames (both schemes; the S-lane host trio, the P-lane seam, the I-lane
cream-collapse repro, the M-lane rim probe + profile); the O-9/O-18 census outputs; the eager
chunk-graph diff; the PI-1 delta row; per-lane commit hashes.

## §Commit plan

Per-lane row-scoped commits (S: retire-fillers / trio-restore / species-redesign; P: veil
re-seat / clamp law / seam; I: contract cure / census legs / sweeps; M: subpath codemod /
beat split / pseudo cure); the verify-walk record commit (docs); a wave status commit.

## §Recovery (STANDING — the `T.md §8` completion-brief rider binds every dispatch AND resume of this wave; PP-14/PP-15 operationalized)

The four-step protocol (audit-partial → patch-brief at
`audit/recovery/T.W<n>-<lane>-brief-<date>.md` → resume-from-work-order → seam-audit-at-close)
is standing law in `T.md §8`. This wave's type-specific deltas: **partial signatures** — a
retire-fillers commit without the trio restore (the empty hosts go BARE — worse than either
state); a census leg minted but not wired into O-18; the verify walk half-filed. **Resume
specifics** — the verify-walk record is the journal (a resumed wave reads it, never re-walks
green rows); Lane S resumes species-first (the hosts stay whole until the redesign lands).

## §Dependencies

- **Depends on**: round-4 close (T.W5 + T.W6 both closed — the verify set needs the MERGED
  tree); `audit/t33-research.md` (the design grounds); the W1 MOVE-MAP (PP-11).
- **Blocks**: T.W8 (the round barrier moves — W8 opens only after this wave closes; its
  passes re-judge these surfaces with `MANDATE §0.6` + this wave's ledger in hand).

## §BOOKS opened/serviced (books-never-gates)

- **T-45 producer root** → the P3-family glass-recipe rider (oversampled-pseudo at the
  ladder; the demo-interim is the booked-swap interim) — letter row, walked at W7.
- **T-38 rider** → dispatched with this addendum (P1 family; the letter records the SHA);
  verify-at-cut W7.
- **T-40 root** → P10 weight tokenization (the pin retires when it lands).
- **T-42** → the W8 shrunken-state bracket; **T-48 residual** → the W8 motion bracket
  (both named in `T.W8.md`).
- The T-33b taste residual (seam air), if any, rides to W8 as a bracket with both §6.1 arms
  as its poles.

## §Evidence packets consumed

`MANDATE-2026-07-06.md §0.6` · `audit/t33-research.md` (all §-cites above) ·
`audit/w45-checkpoint/ROWS.md` · the round-4 close artefacts (the verify referents) ·
`audit/SYNTHESIS.md §2` · `T.md §4` R12 · `letters/GLASSUI-T-ASKS.md` (P1/P3/P10 + the §P1
rider).

## §Hand-off

W8 opens on ground the owner's own audit has shaped: its passes re-judge these surfaces
critique-fresh with this wave's ledger + the two named brackets (T-42 · T-48 residual) in the
package roster; W7 walks the T-38/T-45/P10 rider rows at the cut; W9's zero-drop walk counts
all sixteen T-33..T-48 rows against this wave's record.
