# TRANCHE T — THE OWNER MANDATE (2026-07-06, post-S-close audit)

**Status**: DEVELOPMENT ONLY — "This is NOT an implementation phase. Tranche development
only." The T corpus (charter + waves + letters) is authored from this mandate + the 32-agent
audit fleet, then STOPS at the owner ratification gate.
**Substrate**: master @ `cc4f4fa` (= the S close, tag `tranche-s-close`); branch `tranche-t`.
**Evidence**: the owner audited the live S build on :9000 and delivered ~24 findings with 21
screenshots, preserved at `audit/owner-screenshots/t-<HHMM-SS>.png` (gitignored PNGs, on-disk;
lanes read them from disk; timestamps map below).

---

## §0 Verbatim owner text (wins over every encoding below)

> The synchronization between the various items--background, blob, hero titles, etc--does not
> seem to be right. On page load in particular. [t-1959-21]
> The "Lab" title should be 1.5x bigger using our golden scale, as should the "about the color
> spaces" title--both should be non-bold.
> The "about the color spaces" card is a bit too transparent and should be more cartoon like
> the other picker card. [t-2000-27]
> This bottom text area should be made dynamic. [t-2000-41]
> --we should have properly contrasted color variants such that our text and so forth are
> legible. And this area should have a vertical ring--dock like--that encapuslates the letter
> area in the sliders. The sliders should be encapsulated--with the letters--in a little glass
> card to delineate hierarchy. [t-2001-51]
> The gradient netting should be more visible. [t-2002-09]
> These numbers should be 1.5x bigger in the golden scale; and they should not be spaced out
> as such. They should follow like true values, contigulously: x, y, z (or more if needed)
> [t-2002-52]
> The blob needs a great deal of on-hover and satellite morphing work. And it should not clip
> outside the bounds--it should have a higher z than all. And it should be placed more into
> the card, moved down and to the left. And work on all screen sizes. [t-2004-10]
> This banner should be removed. And why does the backend not work hereof? [t-2004-32]
> Only "palletes" should be rainbow. The rest should be white/black.
> Too glassy is this card [t-2004-55]--too transparent.
> Why is this search area no styled the same as the other areas? [t-2005-08]
> What happened to the shadow palette herein? And this is too transparent too. [t-2005-53]
> All of the card transition animations need to be tweaked, made more inline with our liquid
> glass easing curves.
> This font is not right. [t-2006-46]
> What's with this strange bottom left corner element? [t-2006-56]
> These drop downs should be better stylized with color previews, deftly and in proporition.
> [t-2007-18, t-2007-15]
> background colors for the shading/glass cards are inconsistentn. [t-2008-09]
> This should display a shadow palette instead in all cases. [t-2008-34]
> The glass-ui tabs should have their indidviual triggers better fill the space and not have
> gap--proper pilling. [t-2008-51]
> buugged out--and the gradient bottom bar is not the proper length, too short. [t-2009-29]
> This area in easing is still a mess. [t-2009-51]
> The scrolling header area should be properly shaded when NOT scrolled. [t-2010-19]
> Do a full audit of all glass card areas to use consistent gray/black/white colors.
> Full glass-ui audit, with a series of request packets, based on the most recent glass-ui and
> forthcoming BG/BH changes. All component level items should be done at the root, within
> glass-ui, or herein.
> DEEPLY audit with 32 agents in parallel our original plan and waves thereof, alongside all
> changes made herein.
> Devise a path forward: audit the hitherto made changes and the remaining plan; recapitulate
> our original prompts, plans, and precepts: NO quick solutions, NO workarounds: idiomatic,
> gestalt approaches. This is a development product, architectural transpositions in the sake
> of elegance, simplicity, and performance above all are both necessary and desirable.
> NO legacy code.
> Delineate any chronically deferred items and fold them into this tranche.
> Delineate any deferred items and fold them into this tranche.
> Recap ALL of our prompts and requests hitherto and ensure they've been addressed.
> This is NOT an implementation phase. Tranche development only.
> Use your core model for orchestration, design (all design must be routed using Fable and the
> frontend design plugin), synthesis, but defer to Opus or Sonnet for workflow fanout. Use
> batches of three agents in parallel to avoid rate limit walls.
> Further, our frontend structure--and this is a grand edict for ALL file directories--needs
> to be wildly re-structured: components should be COLOCATED with their sub-components,
> composables, skeletons, constants, etc (and this should be done recursively for nested
> components).
> Composables that are truly module-level or global-level—and other dirs of that nature—can be
> found within a composables/ dir therein, but otherwise they're to be COLOCATED--same for
> styles, etc.
> Long running dirs must and always be broken into common modules and encapsulated thereof.
> Similar treatment and enforcement should be applied to all backend files, too—though
> abstracted and made befitting for those languages and implementations.

### §0.1 Addendum (2026-07-06, later — verbatim)

> Further, the loading color animation and aurora on load and whilst active leave much to be
> desired, too, in booting and page load. Continue to audit and develop.

### §0.2 Addendum (2026-07-06, later — verbatim)

> Further, the frontend design plugin with a fable agent must be used for hardening and
> criqutining of our frontend. Done at a later stage.

### §0.3 Addendum (2026-07-06, later still — verbatim)

> Ecoute-moi: the c-h variation is now a bit too muted, and the aurora not quite noticable
> enough. The loading, on page load and on color transition, animations need a great deal of
> work. Are too gray/slow/jittery.

### §0.4 Addendum (2026-07-06, 22:11 — verbatim)

> This current color outline is either too fine, or should be abrogated--does not fit
> correctly and obscures the watercolor dot. [t-2211-04]
> the psuedo dropdown is clipped at the edges and needs refinement. [t-2210-56]
> Ecoute-moi and fold into your analysis.

### §0.5 Addendum (2026-07-10, ~14:41 ET — verbatim; the LIVE :9000 audit at the W1-close head)

> The blob is blurry, and placed far too much in the top right corner--it should be more
> towards the centre, more deftly and naturally integrated into the card. [t-30-blob-blurry-topright]
> The dock is occluded by the cards--our core layout should have the dock atop, with the
> card/scene area on the bottom below that. [t-31-dock-occluded-by-card]
> The aurora should have a few more zones--is this not configurable with glass-ui?
> Ecoute-moi and mark me. […] Mark.

**Audited state (binding context)**: the owner live-audited :9000 at the **W1-close head
`c63f9aa`** — **PRE-round-2** (the round-2 W2/W3 lanes were mid-flight in worktrees, un-landed).
These findings judge the W1 tree, NOT round 2's work; **T.W4.5 RE-JUDGES all three against the
landed round-2 state**. Screenshots: `audit/owner-screenshots/t-30-blob-blurry-topright.png`
(the blob top-right, soft/blurry, overlapping the About panel edge + nav) ·
`t-31-dock-occluded-by-card.png` (the About card compositing OVER the dock — Login/@mbabb
occluded behind card glass); gitignored on-disk, the copies persist locally.

**Findings (the T-N grammar; routed same-day — see the wave riders + `PROGRESS.md`):**

| # | Finding | Shots |
|---|---|---|
| T-30 | The blob is **BLURRY** + mis-seated **top-right** → centre-ward, **deftly/naturally INTEGRATED into the card** (refines **Q3 FLUSH** — the ruling STANDS; the SEAT POSITION + integration is the NEW axis, not the seat-inset re-litigated). The BLUR is a **forensic**: canvas DPR/resolution vs engine raster vs CSS filter — producer-root routes **P6**, demo-root cures in-wave (**W4-5**) | t-30-blob-blurry-topright |
| T-31 | **THE DOCK-ATOP LAYOUT LAW**: the dock sits ATOP as its OWN BAND, the card/scene area BELOW it; **no card composites over the dock** — the occlusion class dies STRUCTURALLY (band layout), never by z-index patching (**W6**, Fable design lane) | t-31-dock-occluded-by-card |
| T-32 | Aurora zone count: **ANSWERED — YES, configurable**. The producer **AuroraZones atom IS demo-configurable** (glass-ui `src/components/custom/aurora/composables/atoms.ts:64-71` — zones `{count: 1..MAX_NUCLEI clamped, arrangement?: scattered\|composed\|centred}`; verified live 2026-07-10). "A few more zones" = a **W2-5 calibration knob** INSIDE the T-26 bracket judgment | — |

### §0.6 Addendum (2026-07-11, ~00:32–00:42 ET — verbatim; the 2026-07-11 owner audit: the LIVE :9000 FULL-stack audit at the W4.5-close head)

> [t33-audit-01] Too large of a gap between the numbers and the gradient selector. Ensure that
> we gracefully handle pathologically long or short numbers, like 999 and have a maximal value
> that's possible (dynamically within that color space's ranges hereof)
> [t33-audit-02] This is a bit too tight and should be a bit more spaced out--use a glass-ui
> veil card, too.
> [t33-audit-03] Many of the text items that SHOULD be the current contrast friendly color are
> not. Like this one--delineate all.
> [t33-audit-04] This does note have the proper margin and padding like a true button element.
> [t33-audit-05] You cannot properly see the underlying palette color swatch on the collpased
> dock.
> The mouse interactivtiy of the background aurora is not extreme enough or noteworthy enough
> and should be relayed to glass-ui.
> [at `http://localhost:9000/#/?space=lab&color=lab(40.39%25+52.94+47.26+/+82.7%25)`] The
> overall perfroamce of the applicatoin is awful, particularly on load, which is entirely
> unacceptabel.
> [t33-audit-06] Dropdown options should not be bold.
> [t33-audit-07] These shadow palettes--which are poorly designed and do not shimmer properly
> as a proper skeleton--should not be displayed herein.
> [t33-audit-08] Why does this display several shadow palettes and then the 'no palettes found'
> item--just display the no palettes--what happened to our three palette item of water color
> swatches that were used in place holder areas--bring that iconset with the dashes back.
> [t33-audit-09] The shrunken state for the scrolled cards is totally wrong and is far too big.
> The text is too small.
> [t33-audit-10] 'Palettes' should be rainbow.
> [t33-audit-11] These sliders are un-readable. And the shadow palette needs to be entirely
> re-designed. The style we had many many versions ago, when the extract feature was first
> instrocued, was closer to what we want.
> [t33-audit-12] There are superflous shadow palettes everywhere.
> [t33-audit-13] Strange clipping artifacts on the borders of these cards.
> [t33-audit-14] Gradient is totally un-usable. Gradient slider area should be more rounded
> like our glass-ui elements.
> [t33-audit-15] Again, this entire easing selector area is broken, too small, and should
> closer resemble the keyframes.js easing slector curve selector. Redesign this from first
> principles with that assayed directly. And more compactly than it is now.
> The card transitions animations are totally borked, janky, and not smooth at all and need an
> entire re-analysis. View these frame by frame. Refine this dramatically. Adhere to our
> glass-ui easing curves, liquid glass animation smoothing principles.
> Ecoute-moi: mark me and deploy a triumvariate of research, harden, and tranche update for a
> proper addendum. No adhoc temp fixes.
> NO quick solutions, NO workarounds: idiomatic, gestalt approaches. This is a development
> product, architectural transpositions in the sake of elegance, simplicity, and performance
> above all are both necessary and desirable.

**Audited state (binding context)**: the owner live-audited **:9000 as the FULL dev stack**
(`npm run dev`, backend up) at the **W4.5-close head `25924d1`** — **PRE-round-4-merge** (the
round-4 **T.W5 ∥ T.W6 lanes were IN FLIGHT in worktrees, unmerged**; several findings name
surfaces those lanes are actively re-authoring — the OVERLAP column below is the honest
reconciliation, and each in-flight lane **consumes its rows as binding constraints at merge**,
never re-litigates them). Screenshots: **15**, archived on-disk at
`audit/owner-screenshots/t33-audit-01..15-<HH-MM-SS>.png` (gitignored PNGs per the standing
convention; lanes read them from disk; rows cite the `t33-audit-NN` stem; every forensics lane
re-derives its shot↔finding pairing by READING the images). **The 18 verbatim kernels encode as
SIXTEEN rows (T-33..T-48)**: the t33-audit-08 and t33-audit-12 kernels fold into the **T-41**
ShadowPalette-rejection row — one rejection, three sightings (Browse wall · Commons wall · Mix).

**Findings (the §1 grammar + the honest OVERLAP column — IN-FLIGHT-ROUND-4 ·
LANDED-DESIGN-OVERRULED · NEW · PRODUCER · Q14-PRESSURE):**

| # | Finding | Shots | OVERLAP |
|---|---|---|---|
| T-33 | "Too large of a gap between the numbers and the gradient selector" + "gracefully handle pathologically long or short numbers, like 999" + "a maximal value that's possible (dynamically within that color space's ranges hereof)" → the readout↔gradient-rail SEAM + the readout's VALUE GRAMMAR (the W4-2-landed console header) | t33-audit-01 | **LANDED-DESIGN-OVERRULED** (the W4-2/R4 readout seam re-opened on the gap axis) **+ NEW** (a per-space dynamic max/clamp + pathological-width contract — a functional ask, not taste) |
| T-34 | "a bit too tight and should be a bit more spaced out--use a glass-ui veil card, too" → the sliders console (the ratified Q4 rung-2 WELL, W4-landed) | t33-audit-02 | **LANDED-DESIGN-OVERRULED** — re-opens the RATIFIED **Q4 "The well."** on the material axis by the owner's own word (veil CARD) + an air/spacing recalibration; encoded below per the §4 grammar, never silently |
| T-35 | "Many of the text items that SHOULD be the current contrast friendly color are not. Like this one--delineate all" → the accent-ink population (the shot = the "Lab" title); the W3-5 D6 ink-on-tier contract + O-18 census UNDER-COVER the population | t33-audit-03 | **NEW** — a census-extension ORDER ("delineate all"); A-class against the verification method (named-site-not-population), a W8-shaped population sweep |
| T-36 | "does not have the proper margin and padding like a true button element" → the dock **Tools** trigger box-model | t33-audit-04 | **IN-FLIGHT-ROUND-4** — W6-8/Lane D is re-authoring the Tools toggle NOW (clip release); the true-button box-model ask RIDES that lane as a binding merge constraint |
| T-37 | "You cannot properly see the underlying palette color swatch on the collpased dock" → the collapsed-dock seal/swatch visibility | t33-audit-05 | **IN-FLIGHT-ROUND-4** — W6-7/Lane D owns the seal register (Q12 outline ABROGATE in flight); the swatch-visibility ask rides the same files |
| T-38 | "The mouse interactivtiy of the background aurora is not extreme enough or noteworthy enough and should be relayed to glass-ui" → the aurora pointer response (producer atom) | — | **PRODUCER** — an owner-ORDERED relay: the W2 F-10 pointer-retune book escalates to an owner ask; a P1-family packet rider (glass-ui inbox), verify-at-cut at W7 |
| T-39 | "The overall perfroamce of the applicatoin is awful, particularly on load, which is entirely unacceptabel" (at the lab(…) deep-link) | (url) | **Q14-PRESSURE** — the PERF REDEMPTION mandate's owner-felt face; PI-1 mid-read honestly RED (W4: LCP 5141 / TBT 5988); the levers stay W7 (RP-2 eager-payload / L20 / GAP-L5), W9 green-or-escalation — the owner's word RAISES THE STAKES, it does not re-route the gates |
| T-40 | "Dropdown options should not be bold" → the letterforms (color-space) dropdown entries' display voice (the W4 o10d-landed Fraunces walk renders the option letterforms bold) | t33-audit-06 | **LANDED-DESIGN-OVERRULED** (weight recalibration on the landed dropdown display voice) — and the in-flight W6-4 nav-voice lane consumes NON-BOLD as a constraint for every dropdown it re-authors |
| T-41 | **THE SHADOWPALETTE REJECTION** (three sightings, one kernel): "should not be displayed herein" (Browse wall) + "just display the no palettes… bring that iconset with the dashes back" (Commons wall — RESTORE the three-palette watercolor-swatch + dashes placeholder iconset) + "superflous shadow palettes everywhere" (Mix) → the W3-2/D9 species AS-FILLER at the empty hosts | t33-audit-07, t33-audit-08, t33-audit-12 | **LANDED-DESIGN-OVERRULED** — the owner OVERRULE of the landed W3-2/D9 design (gate-green, O-9); encoded below per the §4 grammar |
| T-42 | "The shrunken state for the scrolled cards is totally wrong and is far too big. The text is too small" → the scrolled pane-header shrink register (the W4-2b swell / O-11-gated state) | t33-audit-09 | **LANDED-DESIGN-OVERRULED** — recalibration of the landed shrunken state on BOTH axes (footprint far too big · text too small) |
| T-43 | "'Palettes' should be rainbow" → the Palettes title (My Palettes) | t33-audit-10 | **IN-FLIGHT-ROUND-4** — the owner **CONFIRMS the ruled Q5** (Lane D/W6-4 is landing the guarded letterform ramp ×2 sites NOW); an owner-confirms row, **NOT a new ask** |
| T-44 | "These sliders are un-readable" (the Extract k/kC config sliders — dark track on dark well) + "the shadow palette needs to be entirely re-designed. The style we had many many versions ago, when the extract feature was first instrocued, was closer to what we want" → the species REDESIGN half of the ShadowPalette rejection, anchored on the GENESIS extract-era register | t33-audit-11 | **LANDED-DESIGN-OVERRULED** (the species redesign — genesis register the anchor) **+ NEW** A-class (the slider TRACK contrast: O-18 certified the text rows ≥4.5:1, it never measured the TRACK material — an oracle blind spot, named) |
| T-45 | "Strange clipping artifacts on the borders of these cards" → card-border paint (the shot = the Generate card; the owner's plural = a population class) | t33-audit-13 | **NEW** — A-class paint/geometry defect; a forensic (radius/overflow/shadow seam); W6-5/Lane N touches GenerateControls in flight but the clip CLASS is population-wide |
| T-46 | "Gradient is totally un-usable. Gradient slider area should be more rounded like our glass-ui elements" → the gradient instrument (rail + envelope plate) | t33-audit-14 | **IN-FLIGHT-ROUND-4** — W6-2/Lane G is re-authoring the WHOLE gradient instrument NOW (T-21); the unusable verdict + the glass-ui rounding ask bind that lane's merge gate |
| T-47 | "this entire easing selector area is broken, too small, and should closer resemble the keyframes.js easing slector curve selector. Redesign this from first principles with that assayed directly. And more compactly than it is now" → the easing editor | t33-audit-15 | **IN-FLIGHT-ROUND-4 with SPEC ESCALATION** — W6-3/Lane E's specimen-bench spec is WIDENED by owner order: first-principles redesign with the **keyframes.js easing-selector curve picker assayed DIRECTLY** + more compact; the lane consumes this as its new spec, not a rider |
| T-48 | "The card transitions animations are totally borked, janky, and not smooth at all and need an entire re-analysis. View these frame by frame. Refine this dramatically. Adhere to our glass-ui easing curves, liquid glass animation smoothing principles" → ALL card transition motion | — | **IN-FLIGHT-ROUND-4** — T.W5 MOTION LIQUID **IS this wave** (T-14); the frame-by-frame re-analysis + glass-ui curves + liquid-glass smoothing bind the in-flight lane's gates |

**THE SHADOWPALETTE OVERRULE (T-41/T-44 vs W3-2/D9 — encoded by name per the `T.md §4` ledger
grammar, never silently; the formal R-row lands with the owner-ordered tranche-update
addendum):** class **B owner overrule of gate-green work + D scope-widening** — the landed
W3-2 ShadowPalette species (`9b7fbd2`, O-9 census green) is REJECTED as displayed: no
shadow-palette FILLERS at the empty hosts (display the TRUE empty state only), no
shimmer-less pseudo-skeleton, and the species goes back for an ENTIRE redesign anchored on the
genesis extract-era register, with the three-palette watercolor-swatch + dashes placeholder
iconset RESTORED. **What RETIRES**: the as-filler deployment at the empty-host classes; the
current non-shimmering skeleton presentation. **What SURVIVES (E-3 must not over-delete)**:
R7's UPHOLD-on-semantics stack — S-Q6 true-empty-ONLY scope · error ≠ empty · "never announce
work that isn't happening" (the aria split) · the loading-lie cure on the MOTION axis; O-9's
census discipline (its referent re-points to the redesigned species/empty states — the oracle
is re-aimed, not deleted).

**Process order (verbatim, binding)**: "mark me and deploy a triumvariate of research, harden,
and tranche update for a proper addendum. No adhoc temp fixes." — the audit is MARKED (this
§0.6), and the TRIUMVIRATE — **research · harden · tranche-update** — deploys for the proper
addendum; "NO quick solutions, NO workarounds" re-binds **E-3** over the whole T-33..T-48 set.

## §1 Finding enumeration (T-1..T-24; the screenshot map is BEST-EFFORT — every forensics
lane re-derives its shot↔finding mapping by READING the images)

| # | Finding | Shots |
|---|---|---|
| T-1 | Load synchronization: background/blob/hero-titles arrive out of sync on page load (note the standing GAP-ARM producer defect + the W6-1 entrance interact here) | t-1959-21 |
| T-2 | "Lab" title + "About the color spaces" title → **1.5× on the golden scale, non-bold** (amends W4-1's landed size/weight) | — |
| T-3 | The About card too transparent — more cartoon, like the picker card | t-2000-27 |
| T-4 | The bottom text area made DYNAMIC | t-2000-41 (verify) |
| T-5 | Sliders area: properly contrasted color variants for legibility; a vertical dock-like RING encapsulating the letter column; sliders + letters in a little glass card (hierarchy delineation) | t-2001-51 |
| T-6 | The gradient netting MORE visible (amends the W5-8 webbing register's intensity) | t-2002-09 |
| T-7 | The readout numbers → 1.5× golden scale; NOT spread apart — contiguous like true values: x, y, z (amends W4-2's full-header-span readout layout) | t-2002-52 |
| T-8 | The blob: serious on-hover + satellite morphing work; NO clipping outside bounds — higher z than all; placed INTO the card (down + left); ALL screen sizes (amends W6-4's placement law; the satellite half interacts with GAP-L5) | t-2004-10 |
| T-9 | The banner REMOVED + answer/redesign the backend-not-working dev UX (the W0-1 honest-dev states: :9000 bare dev:web-only has no backend BY DESIGN — the UX must communicate this without a banner) | t-2004-32 |
| T-10 | Menu voice: ONLY "Palettes" rainbow; the rest white/black (PARTIAL REVERSAL of W7-4's landed color-wheel-legend menu — encode explicitly as an owner overrule) | — |
| T-11 | Another card too glassy/transparent | t-2004-55 |
| T-12 | The search area styled inconsistently vs its sibling areas | t-2005-08 |
| T-13 | The shadow palette MISSING (what happened?) + that surface too transparent | t-2005-53 |
| T-14 | ALL card transition animations onto the liquid-glass easing curves | — |
| T-15 | A wrong font (identify surface from the shot) | t-2006-46 |
| T-16 | The strange bottom-left corner element (identify + verdict) | t-2006-56 |
| T-17 | Dropdowns stylized with color previews, deftly and in proportion | t-2007-15, t-2007-18 |
| T-18 | Glass-card background colors inconsistent (shading/glass) | t-2008-09 |
| T-19 | A surface that should display a shadow palette IN ALL CASES | t-2008-34 |
| T-20 | Glass-ui Tabs: triggers fill the space, no gap — proper pilling (producer-root candidate) | t-2008-51 |
| T-21 | A bugged-out surface + the gradient bottom bar too short | t-2009-29 |
| T-22 | The easing area still a mess (amends W5-9) | t-2009-51 |
| T-23 | The scrolling header area properly shaded when NOT scrolled (amends the W5-2 scroll-gated band — the owner wants shading AT REST) | t-2010-19 |
| T-24 | THE GRAY/BLACK/WHITE CONSISTENCY AUDIT: all glass card areas onto consistent neutrals | (cross-cutting) |
| T-26 | **The variance bracket closes** (the THIRD calibration): triad/colorEnergy-0.82 was "too strong" (2026-07-05); the landed analogous/0.7 pull-back is now "a bit too muted, and the aurora not quite noticable enough" — the target band is BRACKETED FROM BOTH SIDES. T designs INSIDE the bracket (more presence + C/H variance than analogous/0.7, less than triad/0.82 — e.g. a wider analogous fan or an energy step, judged by eye) and the bracket becomes the SIZING SPEC for the producer variance atoms (L2/A3) | (live) |
| T-27 | The loading animations — page load AND color transition — are **too gray / too slow / too jittery**: gray = the transition path desaturates mid-flight (interpolation crossing gray — the derive/transition must ride a chroma-preserving path) or the muted register bleeds; slow = duration/curve; jittery = frame pacing (the rAF coalesce/paint chain). A quality triple on both the boot animation and the per-pick field/background transition | (live) |
| T-28 | The current-color OUTLINE on the WatercolorDot: too fine or ABROGATE — it does not fit correctly and obscures the dot (the active-ring/outline recipe re-judged: fit the dot's organic edge or die) | t-2211-04 |
| T-29 | The pseudo-dropdown CLIPPED at the edges — needs refinement (identify the surface; likely an overflow/portal clip on a popover/select content) | t-2210-56 |
| T-25 | The loading color animation + the aurora — on load AND whilst active — leave much to be desired (booting + page load): the boot color animation itself, the aurora's arrival, and its ongoing ACTIVE behavior (drift/breath/response) are all under the bar; extends T-1 beyond arrival-ordering into the quality of the boot animation and the field's living behavior (GAP-ARM interacts on load; the W6-3 analogous field + W6-7 pointer response are the active-state baseline being judged) | (live) |

## §2 Structural edicts (binding on the T corpus)

- **E-1 THE COLOCATION GRAND EDICT** (all file directories, recursive): components colocated
  with their sub-components/composables/skeletons/constants/styles; truly module/global-level
  composables (and kin) live in a `composables/` dir at their level; long-running dirs broken
  into common modules and encapsulated; the backend gets the same treatment, abstracted
  befittingly for its language/idioms.
- **E-2** Full glass-ui audit → REQUEST PACKETS against the most recent glass-ui + the
  forthcoming BG/BH; all component-level items at the ROOT (glass-ui or herein).
- **E-3** NO quick solutions, NO workarounds — idiomatic, gestalt approaches; architectural
  transpositions for elegance/simplicity/performance are necessary and desirable. NO legacy
  code.
- **E-4** ALL deferred items — chronic and recent — delineated and FOLDED INTO THIS TRANCHE.
- **E-5** ALL owner prompts/requests hitherto recapped and verified addressed.
- **E-6** Process: Fable + the frontend-design plugin for all design; opus/sonnet fanout;
  batches of three; development only, ratification-gated.
- **E-7 THE HARDENING/CRITIQUE STAGE** (§0.2): a dedicated LATE-stage wave in the T plan —
  Fable agents under the frontend-design plugin harden and critique the whole frontend after
  the implementation waves land (the S-era non-authoring taste reviews, formalized and
  widened into a standing late-stage gate: per-surface critique passes + remediation lanes +
  a final tranche-level taste certification; sequenced near close, before the T equivalent of
  the close ceremony).

## §3 Known interactions the T corpus must reconcile explicitly

- T-10 vs W7-4 (the landed one-voice/color-wheel-legend menu) — an owner overrule, not a bug.
- T-2/T-7 vs W4-1/W4-2 (title size/weight; readout span/spacing) — owner recalibrations.
- T-23 vs the W5-2 forensics rider (the band was deliberately gated OFF at rest) — reversal.
- T-6 vs the W5-8 webbing "subtle" register — intensity recalibration.
- T-8 vs W6-4's corner-break/overflow law (the owner now wants containment: INTO the card,
  higher z, no outside-bounds clip) — a placement-law revision grounded in the genesis brief.
- T-1 vs GAP-ARM (producer) + W6-1's entrance — the sync story spans both.
- T-9 vs the W0-1 honest-dev design (the banner was the misconfigured-state affordance).
