# U.W-CLOSE — THE OWNER-ATTEST ANNEX PACKET (the whole-tranche owner-eye slate)

*Assembled by the U.W-CLOSE annex+publish+relay lane (2026-07-13). This packet GATHERS every
owner-attest obligation across tranche U into ONE slate for the owner's terminal eye. It does **NOT**
rule, cut, or attest — every VERDICT field is left **EMPTY for the owner**. CLOSE presents; the owner
judges.*

**Precedence**: owner verbatim → `audit/registry.md` (§8/§13.5/§16/§21/§26/§28/§28.3) →
`DISPOSITION-LEDGER.md` → `U.md` → `waves/U.W-CLOSE.md`. The registry WINS on divergence.

---

## §0 · THE LAW THAT GOVERNS THIS PACKET (G-CLOSE-4 — owner-attested / real-GPU ONLY)

Every row below is an **owner-attest** obligation: its operability/geometry/serialize legs are
REPORTED green where GPU-independent (the headless-trustworthy half that DID land), but the
perceptual/real-GPU/non-macOS half is **the owner's terminal call** — attested against a REAL-GPU or
OWNER frame, **NEVER a headless assertion**.

**Why owner-only, not a headless green** (registry §21): this formation logged **TWO demonstrated
headless false-reds on EXACTLY these surfaces** — the **U-F4 `overture.css` red herring** (a
SwiftShader runner "saw" a collapsed dock that a real GPU renders expanded) and the **U-F13 PRM
confound** (a false edge-ring shave attributed to a PRM freeze that was never applied). A headless
runner CANNOT honestly judge the "reads-correct" half of these families. That is the whole reason
these rows are owner-reserved and NOT flipped by any gate.

**The completion contract (G-CLOSE-4)**: an un-attested-in-window row closes **`complete_with_misses`
with its frame obligation NAMED** — never a headless false-green, never a silent drop. Each row's
last line names the exact frame the owner's attestation needs.

**Slate at a glance** — 11 owner-attest rows + 1 owner-reserved taste stub:

| Family surface | Rows | Attest kind | Gated on |
|---|---|---|---|
| VISUAL ANNEX-7 (§1) | u-f5 · u-f11 · u-f8 · u-f10 · u-f12 · u-f7 · zd3-microchrome | real-GPU / owner-eye | in-window (frames captured) |
| A11Y Register-2 (§2) | OA-1 · OA-2 | owner-eye aesthetic/gestalt | in-window (frames captured, `-before` paired) |
| ADOPT U-F54 annex (§3) | OA-B1 · OA-B2 | **PRODUCER-gated real-GPU / owner** | **glass-ui 5.0.0 cut — UNFIRED** |
| owner-reserved (§6) | HG6 taste | pure taste verdict | owner-held (no attest leg) |

The two ADOPT rows are **cut-gated**: they ride forward with the cut-execution residual because the
glass-ui v5 cut is UNFIRED (verified: `git -C ../glass-ui tag --list 'v5*'` EMPTY; glass-ui HEAD
`8b0f9acc` on `tranche/BI`).

---

## §1 · VISUAL ANNEX-7 — the owner-eye still-reds (source: `w-visual-close-artefacts.md §1`)

Frames live under `docs/tranches/U/audit/w-visual/frames/` (merged gh-pages @ :8599, dpr2, both
schemes). No row was fake-judged from a headless assertion; each names its GPU-dependent dependency.

### Row u-f5 — WebGL-blob perceptibility / visible-bead containment (T-30/T-49 · OA-4)

| Field | Value |
|---|---|
| **Family / id** | U-F5 `blob-card-seat` |
| **Headless leg REPORTED (GPU-independent)** | canvas SEAT box unchanged from T.W8, inside the designed transparent **1.6× overscan budget** (crosses card-content +55/+49, card-edge +34, viewport +18@390). Geometry is measured, landed. |
| **Owner-attest DEPENDENCY** | **WebGL-blob perceptibility** — does the visible bead read as CONTAINED (not bleeding past the seat)? The bead is a WebGL canvas render a headless SwiftShader runner cannot honestly judge. Producer GAP-L5 / revealBloom → ADOPT/BH relay. |
| **π-frame pointers** | `w-visual/frames/pane-picker-{light,dark}-1440.png` · `picker-{light,dark}-{390,1440}.png` · `rail-{light,dark}-{390,1440}.png` (the seat + rail-dot containment, both viewports × both schemes) |
| **VERDICT** | *(EMPTY — owner)* |
| **complete_with_misses obligation** | if un-attested in-window: CLOSED `complete_with_misses` — the **WebGL-blob visible-bead containment read on the `picker-*` / `rail-*` frames (real-GPU/owner eye)** is the named frame obligation. |

### Row u-f11 — WebGL atmosphere-field derive-seam gestalt (T-37)

| Field | Value |
|---|---|
| **Family / id** | U-F11 `collapsed-dock-swatch-seam` |
| **Headless leg REPORTED (GPU-independent)** | `useAtmosphere.ts` `DERIVE_SEAM_FLOOR=0.06` + `guaranteeSeamOffset()` → **ΔL ≥ 0.06 OKLab (~3× JND) provable at ANY seed** (pure JS pre-upload); composite dE2000 **8.19–13.03 ≫ 2.3**. The floor is landed, seed-independent. |
| **Owner-attest DEPENDENCY** | **WebGL atmosphere-field gestalt** — does the field "read as a deliberate derive-seam"? The field is a WebGL canvas (`gl.readPixels` cleared under headless) → GPU-dependent gestalt only the owner's eye judges. |
| **π-frame pointers** | `w-visual/frames/pane-atmosphere-{light,dark}-1440.png` |
| **VERDICT** | *(EMPTY — owner)* |
| **complete_with_misses obligation** | if un-attested: CLOSED `complete_with_misses` — the **"reads as a deliberate derive-seam" gestalt on `pane-atmosphere-*` (real-GPU/owner eye)** is the named frame obligation. |

### Row u-f8 — SVG-filter organic-edge material + one-verb-instrument register (T-54/T-55 · OA-2)

| Field | Value |
|---|---|
| **Family / id** | U-F8 `generate-plate-species-chrome` |
| **Headless leg REPORTED (GPU-independent)** | each `.generate-swatch` is a `watercolor-swatch` (`feTurbulence`/`feDisplacementMap` + organic `border-radius`, rect→WatercolorDot ✅); verb register **1 primary + 2 quiet icon-buttons** (WR-7) — countable, landed. |
| **Owner-attest DEPENDENCY** | **SVG-filter organic-edge MATERIAL** (does the feTurbulence read organic, not muddy?) + the **one-verb-instrument register** coherence (OA-2, owner-attested by construction). Producer register → RELAY. |
| **π-frame pointers** | `w-visual/frames/pane-generate-{light,dark}-{390,1440}.png` |
| **VERDICT** | *(EMPTY — owner)* |
| **complete_with_misses obligation** | if un-attested: CLOSED `complete_with_misses` — the **SVG-filter organic-edge material + verb-register read on `pane-generate-*` (owner eye)** is the named frame obligation. |

### Row u-f10 — GPU backdrop-filter veil translucency (T-34/T-50 · OA-3)

| Field | Value |
|---|---|
| **Family / id** | U-F10 `console-veil-material` |
| **Headless leg REPORTED (GPU-independent)** | veil α **light 0.443 (lowered) / dark 0.56 (held, `brightness(1.16)`-compensated)**; clarity window `blur(8px) saturate brightness` ACTIVE both schemes; backdrop near-uniform warm (C≈0.014–0.02). Calibration landed. |
| **Owner-attest DEPENDENCY** | **GPU-composited backdrop-filter veil translucency** — the actual composited translucency read (explicit O-14 warning: `backdrop-filter` is GPU-composited, headless is uninterpretable). Veil primitive → RELAY. |
| **π-frame pointers** | `w-visual/frames/console-veil-{light,dark}.png` |
| **VERDICT** | *(EMPTY — owner)* |
| **complete_with_misses obligation** | if un-attested: CLOSED `complete_with_misses` — the **backdrop-filter veil translucency read on `console-veil-*` (real-GPU/owner eye)** is the named frame obligation. |

### Row u-f12 — dark-scheme derived-tint muddiness · **owner-ruling bracket, Pole A/B UN-PICKED** (OA-4)

| Field | Value |
|---|---|
| **Family / id** | U-F12 `dark-scheme-derived-tint-muddiness` |
| **Headless leg REPORTED (GPU-independent)** | dark `--card` = UNIFORM neutral warm-brown (C **0.0216**, hue spread **0°**); accent lane `--accent-view` seed-tracked (hue 30/190/270/310, spread 160°, C 0.055–0.08 ≥ floor). Channels measured. |
| **Owner-attest DEPENDENCY** | **the owner-ruling bracket — Pole A/B UN-PICKED** + the clean/muddy read (OA-4, BR-7 pole-conditional). NO pole picked by the census or U.W-VISUAL. See §4 for the explicit bracket. |
| **π-frame pointers** | `w-visual/frames/pane-{atmosphere,generate,gradient,mix,picker}-dark-1440.png` (the dark-scheme derived-tint surfaces) |
| **VERDICT** | *(EMPTY — owner: **PICK Pole A or Pole B** + clean/muddy read)* |
| **complete_with_misses obligation** | if un-picked: CLOSED `complete_with_misses` — the **Pole A/B pick + clean/muddy dark-tint read on `pane-*-dark-1440.png`** is the named obligation (see §4 for the C3-ledger amendment Pole A needs). |

### Row u-f7 — real-GPU scene-transition swap-window frame (T-14/T-48/T-58 · OA-1)

| Field | Value |
|---|---|
| **Family / id** | U-F7 `scene-transition-motion` |
| **Headless leg REPORTED (GPU-independent)** | both confounds ABSENT/ISOLATED: PKT-1 dist-clock sane (`.15s`, swap uses `vj-enter @ .3s`); O-16-R1 150ms clobber has zero literals + lives in the stubbed `useLayerTransition`. Headless rAF deltas confound-flagged (uninterpretable — the U-F54 3rd demonstration); wake-gray cure structurally intact (`HeroBlob.vue onActivated` re-seed, boot-B `5cecdf2`). |
| **Owner-attest DEPENDENCY** | **real-GPU swap-window frame** (OA-1) — the actual scene-transition compositor read + the **wake-gray must-not-regress**. The rAF deltas are headless-uninterpretable by design. **Carries the T-58 owner-escalated MANDATE** (see §5). |
| **π-frame pointers** | `w-visual/frames/uf7-wake-reveal-start-{light,dark}.png` (swap-window start) · `uf7-wake-settled-{light,dark}.png` (the wake-gray must-not-regress settled read) |
| **VERDICT** | *(EMPTY — owner)* |
| **complete_with_misses obligation** | if un-attested: CLOSED `complete_with_misses` — the **real-GPU swap-window + wake-gray-no-regress read on `uf7-wake-*.png` (T-58 mandate)** is the named frame obligation. |

### Row zd3-microchrome — non-macOS scrollbar chrome + native-title tooltips (ZD-3 close slot)

| Field | Value |
|---|---|
| **Family / id** | zd3-microchrome (never-filed W8 pass, ZD-3 close slot) |
| **Headless leg REPORTED (GPU-independent)** | two sub-items cured-GREEN: `::selection` bg = `--accent-live @32%` + `--foreground` ink (no default blue); cursor grammar per-role. **Rider halves landed** (BR gate): dynamic `<title>` TRUE (3 live titles observed) + `.scrollbar-thin` consumers > 0. |
| **Owner-attest DEPENDENCY** | **non-macOS scrollbar-chrome frame** (`.scrollbar-thin` visible-track render on a non-macOS platform — macOS overlays scrollbars, so the track is invisible on the author's box) + native-`title` tooltips (producer-relay) + the static-title owner-eye defensibility. |
| **π-frame pointers** | `w-visual/frames/microchrome-selection-{light,dark}.png` (selection + cursor grammar); the non-macOS scrollbar-track render is owed on a non-macOS frame (not capturable on the macOS author box) |
| **VERDICT** | *(EMPTY — owner)* |
| **complete_with_misses obligation** | if un-attested: CLOSED `complete_with_misses` — the **non-macOS `.scrollbar-thin` visible-track frame + native-title tooltip read** is the named frame obligation (`microchrome-selection-*` covers the selection/cursor half only). |

---

## §2 · A11Y REGISTER-2 — the owner-attested aesthetic rows (source: `w-a11y-close-artefacts.md §1`)

A11y is deterministic — the 11 Register-1 gates are machine-terminal born-RED→GREEN (no U-F54 annex
applies; contrast/geometry/attribute/media-emulation are GPU-independent). The owner-attest register
is deliberately SMALL: only the AESTHETIC coherence of the high-contrast fallbacks + the authed
gestalt. Frames under `docs/tranches/U/audit/w-a11y/pi/`.

### Row OA-1 — forced-colors / prefers-contrast / reduced-transparency fallback COHERENCE (U-F57)

| Field | Value |
|---|---|
| **Family / id** | U-F57 `a11y-modality-gaps` (Register-2 OA-1) |
| **Headless leg REPORTED (GPU-independent)** | operability is born-RED GREEN (BR-5 forced-colors, BR-6 prefers-contrast, BR-7 reduced-transparency — designed two-tier `forced-color-adjust`, tint 4%→0%, elevated `--muted-foreground`). Operability is proven. |
| **Owner-attest DEPENDENCY** | the **AESTHETIC coherence** of the high-contrast register — does the fallback READ coherent (not just function)? That is taste, owner-terminal. |
| **π-frame pointers** | `w-a11y/pi/modality-forced-colors.png` + `-before.png` · `modality-contrast-more.png` + `-before.png` · `modality-reduced-transparency.png` + `-before.png` · `rtl-layout.png` + `-before.png` · `print-artifact.png` + `-before.png` (all `-before` paired) |
| **VERDICT** | *(EMPTY — owner)* |
| **complete_with_misses obligation** | if un-attested: CLOSED `complete_with_misses` — the **high-contrast fallback aesthetic-coherence read on `modality-*` / `rtl-layout` / `print-artifact` (owner eye)** is the named obligation. |

### Row OA-2 — authed + populated surface gestalt (U-F56)

| Field | Value |
|---|---|
| **Family / id** | U-F56 `authenticated-populated-surface-uneyeballed` (Register-2 OA-2) |
| **Headless leg REPORTED (GPU-independent)** | the a11y battery is born-RED GREEN (BR-9 — accessible-name · target ≥24px · keyboard-operability over 5 populated states, 7/7 smoke-admin; 2 defects found→cured: admin row-expander role + thrown-error boundary). Operability is proven. |
| **Owner-attest DEPENDENCY** | the **authed+populated surface reads as a coherent GESTALT** (login/admin/populated-browse critique + the ErrorBoundary surface) — owner-terminal (lesson 12). |
| **π-frame pointers** | `w-a11y/pi/authed-{login,admin,browse}-{light,dark}.png` + `error-boundary-{light,dark}.png` |
| **VERDICT** | *(EMPTY — owner)* |
| **complete_with_misses obligation** | if un-attested: CLOSED `complete_with_misses` — the **authed+populated gestalt read on `authed-*` + `error-boundary-*` (owner eye)** is the named obligation. |

---

## §3 · ADOPT U-F54 ANNEX — the producer-gated owner-attest rows (source: `w-adopt/producer-bi/uf54-annex-rows.md`) — **CUT-GATED**

Both rows are **PRODUCER-ONLY** (value.js/demo has NO cure — E-3). Both are producer-gated on the
glass-ui **5.0.0 cut, which is UNFIRED**. Per each row's Discharge clause: **if the cut has not fired,
the row rides forward with the cut-execution residual** (PP-16, `complete_with_misses`). NEITHER is
headless-assertable — the registry §21 false-red history is on EXACTLY these surfaces.

### Row OA-B1 — U-F4 PRM-dock "reads-correct-when-expanded" (owner-attested) — **cut-gated**

| Field | Value |
|---|---|
| **Family / id** | U-F4 `reduced-motion-dock-collapse` (producer half; **demo EXONERATED** — the `overture.css` root is a CONFIRMED RED HERRING) |
| **Headless leg REPORTED (GPU-independent)** | the width+reachability probe (G-ADOPT-2) is **headless-trustworthy** post R-1: under PRM, dock width > 44px AND ≥ 19/20 controls reachable. RED NOW (width stays 44px = 1/20 usable at glass-ui HEAD `8b0f9acc` + pinned `2e559f7a`). |
| **Owner-attest DEPENDENCY** | the **"reads-correct-when-expanded"** half (chrome/spacing/glyph legibility of the expanded surface) — **REAL-GPU or OWNER frame, NEVER headless** (the `overture.css` false-red is logged here). |
| **π-frame pointers** | *(prose before→after; NO captured frame — producer-gated, cut UNFIRED)*: PRM dock = 44px empty pill, 1/20 controls reachable → PRM dock EXPANDED full-width, 20/20 reachable + reads-correct. Frame owed on the owner's PRM-emulated desktop frame, BOTH schemes, at the cut. |
| **Producer gate** | flips only when glass-ui ships `prm-dock-arm-static-expanded-fallback` at 5.0.0 (relay-spec §Constraint #1). |
| **VERDICT** | *(EMPTY — owner)* |
| **complete_with_misses obligation** | cut UNFIRED → CLOSED `complete_with_misses` — the row **rides forward with the cut-execution residual**; the **owner PRM-frame reads-correct attestation (both schemes, desktop)** is the named obligation, discharged at the fired cut. |

### Row OA-B2 — U-F13-producer edge-ring un-shaved (real-GPU / owner ONLY) — **cut-gated**

| Field | Value |
|---|---|
| **Family / id** | U-F13-producer `dock-edge-clip` (producer clip/fade-mask half; demo Tools box-model half is CENSUS-GREEN → U.W-VISUAL) |
| **Headless leg REPORTED (GPU-independent)** | **NONE that is trustworthy** — U-F13-producer has NO headless-trustworthy half (the PRM confound already demonstrated a false-red on THIS exact surface). The mechanism is LIVE: `overflow.css` `mask-image: linear-gradient(...)` 0px-fade transparent-stop (`black var(--fade-start, 0px)`) antialiases the outermost pixel column at both inline edges. RED NOW. |
| **Owner-attest DEPENDENCY** | edge-item ring pixel-coverage ≡ interior-item ring pixel-coverage, at rest AND mid-scroll, BOTH edges, BOTH schemes — **REAL-GPU or OWNER-attested frame ONLY** (U-F54; NO headless-trustworthy half exists). |
| **π-frame pointers** | *(prose before→after; NO captured frame — real-GPU/owner ONLY, cut UNFIRED)*: edge-item focus ring shaved by the clip/fade-mask (clipped arc) → edge ring un-shaved ≡ interior ring (full arc). Frame owed on a real-GPU/owner frame at the cut. |
| **Producer gate** | flips only when glass-ui ships `dock-edge-clip-fade-mask-focus-ring-honesty` at 5.0.0 (relay-spec §Constraint #2: mask honesty at rest = `none` + inline safe-inset ≥ ring weight). |
| **VERDICT** | *(EMPTY — owner)* |
| **complete_with_misses obligation** | cut UNFIRED → CLOSED `complete_with_misses` — the row **rides forward with the cut-execution residual**; the **real-GPU/owner edge-vs-interior ring pixel-coverage read** is the named obligation, discharged at the fired cut. |

---

## §4 · THE u-f12 POLE A/B BRACKET — explicit (owner picks; do NOT proxy)

The U-F12 dark-scheme derived-tint muddiness row (§1) is an **owner-ruling bracket** with TWO poles,
UN-PICKED by the census or any wave:

| Pole | State as-built | What it needs |
|---|---|---|
| **Pole B** (C3-compliant) | **MET as-built** — dark `--card` is a uniform neutral warm-brown at C 0.0216 within the C3 chroma ceiling; the derivation stays under the muddiness floor. | nothing (already compliant) — the owner may simply RATIFY Pole B. |
| **Pole A** (richer derived tint) | at its **RED pole** — a richer per-view derived tint would exceed the current C3 ledger ceiling. | a **C3-ledger amendment** (raise the chroma ceiling for the dark-card derivation) before Pole A is buildable. The owner picks the pole; if Pole A, the C3 ledger is amended. |

**Coupled residual — the A11Y U-F26 dock-icon 2.26:1, booked onto U-F12**: the dock view-select icon
(`color:var(--accent-view)`, a WCAG 1.4.11 graphic) reads **2.26:1** on its LIVE mid-tint dock chrome
(`rgb(108,82,73)`, the U-F12 tint). Feasibility (measured) shows NO chromatic accent clears 3:1 while
serving BOTH the mid dock band AND the dark resting plate with ONE token → its ROOT is U-F12 giving
the dark dock a PROPER dark tier. Logged diagnostic in `o18-contrast-census` (not a hard gate). **The
U-F26↔U-F12 coupling is registry-named**: the dark-scheme reds share the derivation surface and cure
coherently. **This residual is discharged BY the owner's u-f12 pole pick** — a proper dark dock tier
(Pole A with amended ledger, or a dark-tier token under Pole B) is what lets the dock icon clear 3:1.

**VERDICT** *(EMPTY — owner: pick Pole A (with C3-ledger amendment) or ratify Pole B; the U-F26
dock-icon residual rides the pick)*.

---

## §5 · U-F7 T-58 — the real-GPU swap-window frame MANDATE (explicit)

The U-F7 row (§1) and the A11Y OA-1 motion sibling share ONE surface: the scene-transition
swap-window. **The T-58 obligation is carried EXPLICITLY here** because it is the owner-escalated
MANDATE (not a mere annex row): T.W8 escalated the real-GPU swap-window frame to an owner mandate
because the headless rAF deltas are structurally uninterpretable (the U-F54 3rd demonstration). The
mandate has TWO legs the owner attests on a real-GPU frame:

1. **the swap-window read** — the scene transition composites cleanly (no black flash / no torn
   frame) — `uf7-wake-reveal-start-{light,dark}.png`;
2. **the wake-gray MUST-NOT-REGRESS** — the landed boot-B fix (`5cecdf2`, `HeroBlob.vue onActivated`
   re-seed) holds: the blob wakes colored, never gray — `uf7-wake-settled-{light,dark}.png`.

**VERDICT** *(EMPTY — owner: the T-58 mandate is discharged only on the owner's real-GPU frame; the
confounds are ISOLATED but the compositor read is the owner's)*.

---

## §6 · HG6 STUB POINTER — the T.W8 taste verdict (OWNER-RESERVED — DECISION-PENDING-OWNER)

The **T.W8 HG6 taste verdict** is OWNER-RESERVED and is **NOT ruled here**. This is a STUB pointer
only:

- **The verify-at-execution half** (the HG6 demo-caps re-encapsulation) rides **U.W-DEMO's BOOK** —
  that is a mechanical verify, sequenced.
- **The TASTE verdict** — whether the HG6 re-encapsulation READS right — is the **owner's terminal
  call**, part of the coupled owner event (ratify U + close T as ONE). It is not an attest row with a
  headless leg; it is pure taste.

**Status: DECISION-PENDING-OWNER.** CLOSE does **not** rule HG6, does **not** fill a verdict. The
stub is recorded so the owner event carries it (never a silent drop).

---

## §7 · THE COMPLETE_WITH_MISSES CLOSE STATEMENT (G-CLOSE-4)

Every row in this slate closes under ONE law: **an un-attested owner-attest row closes
`complete_with_misses` with its frame obligation NAMED — never a headless false-green.** Restated per
family:

- **VISUAL ANNEX-7** (u-f5 · u-f11 · u-f8 · u-f10 · u-f12 · u-f7 · zd3-microchrome): the headless legs
  (α, chroma, ΔL, seat geometry, seam floor) are REPORTED green where GPU-independent; the
  perceptual/GPU gestalt (OA-1..OA-7) + the u-f12 Pole A/B pick are the owner's. Frames captured
  in-window under `w-visual/frames/`.
- **A11Y Register-2** (OA-1 · OA-2): operability is machine-terminal green; only the fallback
  aesthetic-coherence + the authed gestalt are the owner's. Frames captured `-before`-paired under
  `w-a11y/pi/`.
- **ADOPT U-F54 annex** (OA-B1 · OA-B2): **CUT-GATED** — the glass-ui v5 cut is UNFIRED, so both rows
  **ride forward with the cut-execution residual**; the owner-attest frames are owed at the fired cut.
  An un-taken cut is NOT a miss (PP-16); the un-attested owner frame is the named obligation.
- **HG6** (§6): OWNER-RESERVED taste verdict, DECISION-PENDING-OWNER — not an attest row, carried on
  the owner event.

**No row is fake-judged from a headless assertion. The registry §21 two demonstrated false-reds
(U-F4 `overture.css` + U-F13 PRM confound) are the reason these are owner-only. Zero silent drops.**

**Precedence chain** (restated): owner verbatim → `audit/registry.md §8/§13.5/§16/§21/§26/§28/§28.3`
→ `DISPOSITION-LEDGER.md` → `U.md` → `waves/U.W-CLOSE.md` → this packet. CLOSE presents; the owner
attests.
