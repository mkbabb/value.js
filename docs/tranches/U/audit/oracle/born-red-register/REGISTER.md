# The born-RED cure-OWNERSHIP register (U.W-ORACLE · U-F42 + U-F15-fold)

**Gate**: G-ORACLE-3 (born-RED). **Family**: U-F42 (`vacuous-ci-tripwire`, **escalate**) folding
U-F15 (`o26-softwaregl-nonflip`, **fold**). **Owner lane**: U.W-ORACLE / TEST-INTEGRITY.

**What this is**: the standing record that maps every armed `test.fail()` shipping at head to the
wave that removes it (its cure) + the condition under which its `test.fail()` flips, AND the decision
that re-hosts the software-GL-unflippable aurora pair to the U-F54 owner-attested annex. It is the
value.js-side gate itself (M1 ruling: *the record IS the gate; a producer ack is a bonus, never
waited on*).

**Precedence** (verbatim from the wave doc): owner verbatim (§13.5 · probe-parsimony · the born-RED
law) → `../../registry.md` (§13 U-F42 · §3 U-F15) + `../../../DISPOSITION-LEDGER.md` (§A U-F15/F42 ·
§B born-RED-cure-ownership chronic) → `../../../U.md` (§The real-GPU annex, U-F54) → the wave doc
`../../../waves/U.W-ORACLE.md` (§U-F42+U-F15 · §Hard gate G-ORACLE-3 · §BOOKS).

**E-3 binds** (the close-class lie the charter forbids): a `test.fail()` is an HONEST born-RED whose
cure wave has not landed. It is **never deleted to green CI**, **never softened to a warn**, and
**never authored as a flip-on-headless gate for a GPU-only visual** (the §21 caveat). This register
OWNS them; it does not weaken or delete them, and it edits NONE of the o16/o26/o5/o3 spec files (they
are mapped here BY NAME only).

---

## §1 The gate + its RED→GREEN

> **G-ORACLE-3** (born-RED): the cure-ownership register is complete (each `test.fail()` mapped by
> name to its cure wave + flip condition) **AND** the software-GL-unflippable pair is
> annex-hosted-or-acknowledged (U-F54, NOT a flip-on-headless gate).

| State | Orphaned `test.fail()` (mapped by name) | Software-GL-unflippable pair (O-26 + O-3) |
|---|---|---|
| **RED** (before this register) | 3 unmapped | 2 undecided |
| **GREEN** (this register) | **3 mapped** (§2) | **annex-hosted** — Pole B adopted (§3) |

This escalate is a **DECISION**, satisfied INDEPENDENTLY of whether the producer cures fire in-window
(PP-16: gates-armed / cure-wave-gated → `complete_with_misses`). The three flips are **booked, not
held** (§4).

---

## §2 The register — each `test.fail()` mapped by name → cure wave + flip condition

Three armed `test.fail()` ship at head, each honest born-RED (verified this pass — §5).

### O-16 · the dist `:root` 150ms transition-default clobber

- **Tripwire**: `e2e/smoke/oracles/o16-computed-cascade.spec.ts:34` — `test.fail()`. The assertion
  (`:71–74`): the live-computed `--default-transition-duration` on `document.documentElement` is
  `.not.toBe("150ms")` (`CLOBBER`, `:27`). The census also proves the house tokens ARE the liquid
  curves — only the cascade DEFAULT is dead.
- **Cure wave**: **U.W-ADOPT** / the landed formation communiqué `17e0f522` **§2b** (the carried-forward
  T producer reds — PKT-1/P2, the "PKT-1 producer-root" cascade cure). This is a **glass-ui PRODUCER**
  root; E-3 forbids a consumer workaround.
- **Flip condition**: the day the producer clears the dist `:root` cascade default so the ~46 bare-
  `transition` sites resolve to a house liquid curve instead of the dead `150ms`. When it clears, the
  computed read is no longer `150ms`, the assertion passes, and the `test.fail()` annotation is deleted
  (its removal is forced — the tripwire reddens the suite the moment the cure lands). O-16 then arms
  fully as the house-token congruence census (both schemes).
- **RE-VERIFIED still shipped** (this pass, §5): `--default-transition-duration: 150ms` is present at
  the head (line 1) of glass-ui dist `styles/components.css`, resolved version **5.0.0** (the pinned
  substrate worktree `glass-ui-pinned` @ `2e559f7a`). The born-RED is honest at this substrate.
- **RELAY** (E-2): a glass-ui producer surface — **already dispatched** in the communiqué §2b; cited
  by name, NO second book (§4).

### O-26 · aurora perceptibility (the field must migrate)

- **Tripwire**: `e2e/smoke/oracles/o26-aurora-perceptibility.spec.ts:57` — `test.fail()`. The bands:
  calm 2s glance `< 2/255` (`:83–86`), migration over the 10s window `≥ 4/255` (max pairwise diff,
  `:94–105`). Today's field is a still (sub-perceptual breath ±3.25%, two dead pointer axes,
  marigold-for-gray) → the migration band FAILS.
- **Cure wave**: **U.W-ADOPT** / the communiqué `17e0f522` **§2b** — the **producer aurora composition
  cure**. The value.js-side cure driver was T's **W2-5** T-26 in-bracket calibration (chroma-adaptive
  hueSpread `[24°,64°]`, energy `0.76`, one counterpoint stop, softmaxBeta `4`, breath `26s`); the
  live migration is a glass-ui aurora-shader producer deliverable that rides the adopt.
- **Flip condition**: when the producer aurora migrates unmistakably — the 10s max-pairwise frame diff
  crosses `≥ 4/255` on a runner that renders the real shader. Then delete the `test.fail()` and O-26
  becomes the hard perceptibility gate.
- **Software-GL caveat (U-F15)**: on the CI runner (ANGLE-SwiftShader) the atmosphere takes the
  CSS-gradient **placeholder** path — definitionally a still (migration ≈ 0 **forever** headless). So
  **O-26 can NEVER flip GREEN on the CI runner**; its AUTHORITATIVE perceptibility measurement is the
  O-3 headed real-GPU annex. O-26 is therefore re-hosted per §3 — its headless `test.fail()` stays as
  the honest headless born-RED, but its GREEN certification is owner-attested, NOT CI.
- **RELAY** (E-2): a glass-ui producer surface — **already dispatched** in the communiqué §2b; cited
  by name, NO second book (§4).

### O-5 · boot pacing (no jitter spike over the boot window)

- **Tripwire**: `e2e/smoke/perf/o5-boot-pacing.spec.ts:48` — `test.fail()`. The gates: max inter-frame
  delta `≤ 3× median` (`:71–74`) + dropped-frame ratio `≤ 10%` (`:75–78`), both ratio-metrics
  (software-invariant) over the B0→B4 boot window, on the `smoke-perf` built bundle.
- **Cure wave**: **U.W-PERF** (U-F3, the escalate) / **U.W-ADOPT-gated**. The pile-up leg is already
  GREEN post-overture (median 25.0ms, dropped 5.3%); the **SPIKE** leg reds on the ONE eager-payload
  mount task — the **RP-2 payload cut** (the ~347.9 KiB gz eager index; the L20 `/blob/config` +
  GAP-L5 → the Q14 chain). An overture cannot spread a synchronous framework mount; the payload cut is
  the lever.
- **Flip condition**: re-measure at the U.W-ADOPT payload cut (T's "W7 adopt"); when the eager index is
  cut, the max spike drops below `3× median` and the `test.fail()` is removed. A red stays red with its
  cite until then.
- **RELAY**: the O-5/RP-2 payload cut is U.W-PERF/U.W-ADOPT's producer lever (relayed there), NOT
  re-booked here.

**DELTA** (machine-checkable): orphaned `test.fail()` RED **3 unmapped → GREEN 3 mapped by name**.

---

## §3 The software-GL-unflippable pair — the annex DECISION (U-F15 fold)

**The mis-hosting** (U-F15): on the software-GL CI runner the aurora has **NO executable gate** —
**O-26** can never flip (SwiftShader forces the static CSS placeholder → migration ≈ 0 forever) AND
**O-3**'s chroma assertion fully **SKIPS** (`e2e/smoke/oracles/o3-headed-gpu-probe.spec.ts:44`,
`test.skip(isSoftwareGL(renderer), …)` — "would certify the CSS placeholder, not the live aurora";
the chroma assert `C ≥ 0.03` is `:59–62`). An oracle whose cure-path is unmeasurable on its runner is
**mis-hosted**. Pretending a headless assertion verifies a GPU frame is the close-class lie the charter
forbids (§21 caveat; U.md §The real-GPU annex).

**The decision — RE-HOST O-26 + O-3 to the U-F54 headed-GPU / owner-attested annex.** They are NOT
authored as flip-on-headless gates. Two poles:

- **Pole A — a real-GPU headed CI lane**: if a headed lane is stood up, O-26/O-3 run there and their
  born-RED becomes a real gate on real hardware (O-3 already SKIPS software-GL by design and RUNS its
  canvas-pixel asserts only when a real GPU is present, both schemes; O-3's own cadence is W0 mint ·
  re-run W2/W7/W8). **Status: not stood up in this wave** (the headless env forces SwiftShader; the
  registry logged two demonstrated headless false-reds — U-F4 `overture.css` + U-F13 PRM).

- **Pole B — acknowledge (the owner-attested annex)** — **ADOPTED**. Absent a headed lane, O-26 + O-3
  are the U-F54 owner-attested annex: their aurora perceptibility / chromaticity is **"verified by
  owner attestation, not by CI"** (owner-attested, or a headed-CI lane if one is later stood up — Pole
  A remains the future upgrade). The specs stay exactly as authored — O-26's headless `test.fail()` is
  the honest headless born-RED (it would only ever flip on a runner with a real GPU), O-3 stays
  SKIP-on-software-GL. **Neither is deleted, neither is softened, neither is turned into a pretend-
  headless flip.** The ATTESTATION itself is owner-held at close (§4).

**DELTA** (machine-checkable): software-GL-unflippable oracles RED **2 undecided → GREEN 2
annex-hosted** (Pole B; Pole A available if a headed lane is stood up).

---

## §4 §BOOKS — what rides to a later wave (by name, no silent drop)

- **O-16 + O-26 producer flips → U.W-ADOPT / the landed communiqué `17e0f522` §2b.** The producer roots
  (the dist `:root` cascade clobber; the aurora composition) that flip O-16/O-26's `test.fail()` unblock
  at the adopt / the BI-acceptance constraints. **ALREADY DISPATCHED** — cited by name, NO new addendum
  authored by this wave (relay discipline M1: the record here IS the gate; an ack is a bonus).
- **O-5 / RP-2 boot-pacing flip → U.W-PERF / U.W-ADOPT.** The spike leg flips on the adopt-gated RP-2
  payload cut (U.W-PERF's U-F3 escalate). This wave owns the tripwire; the flip rides the payload cut.
  Not re-booked.
- **O-26 / O-3 headed-GPU annex ATTESTATION → U.W-CLOSE / U-F54.** The §3 re-host decision is recorded
  HERE; the ATTESTATION is **owner-held at close** (U-F54; U-F61 attested-flags the born-RED-cure-
  ownership single-sourced claim). This wave records the RE-HOST decision; **CLOSE attests the frame.**
- **The moved dts-surface check → the standing CI-wired `test:dist` slate.** (U-F43, this lane) — the
  two `extractFunctions` published-`.d.ts` checks moved from the vitest `beforeAll` build into
  `scripts/gates/proof-dts-surface.mjs`, spliced into `test:dist` (which builds first, so the check
  reads fresh dist for free). The unit suite is now build-free (G-ORACLE-4). See the sibling
  `../../../waves/U.W-ORACLE.md §BOOKS` + `package.json`.

---

## §5 Verification appendix (this pass — the honest re-witness)

- **O-16** `test.fail()` at `o16-computed-cascade.spec.ts:34`; assertion `:71–74` (`.not.toBe("150ms")`).
  Producer root RE-VERIFIED present: glass-ui dist `styles/components.css` line 1 carries
  `--default-transition-duration: 150ms` (resolved glass-ui **5.0.0**, pinned worktree @ `2e559f7a`).
- **O-26** `test.fail()` at `o26-aurora-perceptibility.spec.ts:57`; calm `:83–86`, migration `:94–105`;
  renderer-note `:36–40` (software-GL → CSS placeholder still; authoritative read = O-3 annex).
- **O-5** `test.fail()` at `o5-boot-pacing.spec.ts:48`; spike gate `:71–74`, drop gate `:75–78`.
- **O-3** `test.skip(isSoftwareGL(renderer), …)` at `o3-headed-gpu-probe.spec.ts:44`; chroma assert
  `C ≥ 0.03` at `:59–62`; header `:6–24` self-declares it the headed real-GPU / owner-attested annex.

**Spec files are UNTOUCHED** by this register (mapped by name only; ownership discipline). The
`test.fail()` legs remain armed and honest.
