# U.W-VISUAL — Wave-Open W8-Inheritance Census — VERDICT TABLE (the census seal)

The census law (U.W-VISUAL spec of record): U does **not** re-litigate T.W8's
landed cures. It re-judges the owner-uncertified still-reds inherited from
T.W8's terminal state **LIVE**, against the served T.W8-terminal build. Each row
carries one of three verdicts:

- **CENSUS-GREEN** — retire; the cure holds by eye/predicate against the live build.
- **CENSUS-RED** — the still-red reproduces; a re-cure is owed **this wave** (routing named).
- **ANNEX-OWNER-ATTEST** — a real-GPU / owner-attested / non-macOS frame is required
  (the **U-F54 law**: NEVER fake-judge a GPU frame from a headless assertion — flag
  it OWNER-ATTEST honestly and name the dependency).

A fourth disposition, **RECORDED-ELSEWHERE**, covers the Lane-F static-pointer set:
VISUAL-adjacent rows owned by another wave / the producer — recorded (not judged
live) so no successor silently re-opens them.

**Serve provenance (all lanes):** worktree `.claude/worktrees/u-visual-census`
HEAD `e97a9d1` (`docs(T · FINAL)` — T.W8-terminal; master-merged `6e14e90`), served
as the built `dist/gh-pages` bundle on census ports **8591–8596** (owner's :9000
untouched; sibling repos read-only). A shared **UNCOMMITTED census scaffold** (the
`goo-blob→blob` / `GooBlob→Blob` re-label shim + an inert `ActionBarLayer` layer
stub + a `vite.config.ts` alias) unblocks the render of the T.W8-terminal demo
against the freshly-cut sibling **glass-ui 5.0.0** producer renames. **The scaffold
re-labels the producer rename ONLY — it touches no picker/dock/ramp/material geometry
any row judges, and the real 5.0.0 adoption is U.W-ADOPT scope** (hand-forward, §Routing).

**BR-14/Q10 law honoured:** the Login-accent cascade-dead class (u-f13-menuink) is
judged by a **computed `getComputedStyle` predicate**, never an eye-read — the dead
accent renders as a coherent neutral pill and blinds an eye-census.

---

## THE VERDICT TABLE (one row per census row)

| # | Row (id) | Lane | Verdict | Evidence cite (predicate class · location) | Re-cure routing (RED only) |
|---|---|---|---|---|---|
| 1 | **u-f5** blob-card-seat (T-30·T-49) | A | **ANNEX-OWNER-ATTEST** | Measured canvas SEAT box `.goo-blob-canvas` UNCHANGED from T.W8 & still crosses card-content +55.3/+48.9, card-edge +33.8/+33.6, viewport +17.6@390, readout +55.5@390 — all inside the **designed transparent 1.6× overscan budget**. Visible-bead containment is a **WebGL-blob perceptibility** read → **U-F54 (GPU)**. `census-probe-log.txt`; frames `picker-{light,dark}-{1440,390}.png`. | — |
| 2 | **u-f9-atrest** header-spacing (T-2·T-51·T-59) | A | **CENSUS-RED** | Computed layout (pure text, GPU-free, confident): BR-4 title-token step **HELD green** (fs 53.28@1440 / 32.928@390 = `--type-display-2`, never the un-stepped 67.776). BR-5 seam **reproduces**: gap 4.0px@1440 vs **24.6px@390** → rhythm-ratio 0.075em↔0.75em (~6× divergence) + a 390 dead/blank band on the readout left. `census-probe-log.txt` (`DELTA readout.top−title.bottom=24.6`). | **WR-11 one-law rhythm** (this wave's VISUAL remediation slate): collapse the 4px↔24.6px divergence to one rhythm law; absorb the 24.6px@390 dead-band → 0. Demo-side (header rhythm tokens). |
| 3 | **u-f9-rail** channel-rail active-seat (T-5/P1-R1) | A | **CENSUS-GREEN** | BR-12 computed geometry: `.rail-dot-seat` (WatercolorDot) center ≡ active `.channel-rail-item[aria-selected]` center at **(0,0) offset on every rung (L/a/b/α) × both viewports × both schemes**; WatercolorDot is producer SVG per-instance filter (renders faithfully headless — NOT GPU). `census-probe-log.txt` (`rail-active-seat … offset:{dx:0,dy:0}` ×16); frame `rail-{light,dark}-{1440,390}.png`. | — |
| 4 | **u-f9-scrolled** whole-header scrolled contraction (T-61/T-42 B-01) | A | **CENSUS-RED** | Computed layout (pure, confident): BR-9/BR-11 contraction **ENTIRELY ABSENT** — header band block-size Δ0 (182.9→182.9@390 / 223.4→223.4@1440), padding-top 12px→12px, title fs Δ0 under scroll; @390 the header is not even sticky (scrolls off). Static grep: no `useScroll`/`sticky`/`scale` on the demo `CardHeader`. `census-probe-log.txt` (`SCROLL DELTA header.h … contract if <`); frames `scrolled2-*`. | Whole-header contraction re-cure owed. The cleanest door — the **P3 `ScrollCardHeader` producer contraction primitive — is PRODUCER-GATED → coordinate with U.W-ADOPT**; the demo band today carries neither a sticky condensed strip nor any padding/scale contraction to land unilaterally. |
| 5 | **u-f11** collapsed-dock swatch seam (T-37) | B | **ANNEX-OWNER-ATTEST** | Cure code-CONFIRMED LANDED: `useAtmosphere.ts:53-110` `DERIVE_SEAM_FLOOR=0.06` + `guaranteeSeamOffset()` → **GPU-independent ΔL ≥ 0.06 OKLab (≈3× JND) provable at any seed** (pure JS pre-upload). Composite corroboration dE2000 **8.19–13.03 ≫ 2.3** both schemes. But the atmosphere FIELD is a **WebGL canvas** (`gl.readPixels` cleared) → the "reads as a deliberate derive-seam" gestalt is **U-F54 (GPU)**. `pi/lane-b-{light,dark}-seal.png`. | — |
| 6 | **u-f13-demo** dock-edge-clip · box-model half (T-36) | B | **CENSUS-GREEN** | Computed box-model: `ActionBarToggle.vue:154-158` `margin-inline:4px/4px` (computed, both schemes); the Tools true-button is **INTERIOR** (flanked by DockViewSelect left + Login/@mbabb right) so its focus-ring envelope sits within the active dock-layer content box, renders whole/unclipped. Frame `pi/lane-b-{light,dark}-tools-focus.png`. (Producer clip/fade/mask honesty half = T-52 → ADOPT.) | — |
| 7 | **u-f13-band** dock-expand scene reflow (T-57/WR-9) | B | **CENSUS-GREEN** | Computed geometry, live drive (hover→leave collapse timer): band block-size **72.00px INVARIANT** + `.pane-container` top **103.0px INVARIANT** across expanded↔collapsed↔re-expanded, both schemes; scene-top shift Δ**0.00px**, band Δ**0.00px**. Cure `style.css:403-419` `--dock-band-min-h = 40+32 = 72px ≥ 71.4px expanded pill`. `pi/lane-b-probe.mjs`. | — |
| 8 | **u-f13-menuink** Login/Tools Q10 accent (P9-R4) | B | **CENSUS-GREEN** (computed-only) | **BR-14/Q10 computed `getComputedStyle` predicate** (NO eye arm, per law): accent re-homed onto the ink `color` channel (`ProfileSection.vue:116` / `ActionBarToggle.vue:95`). Login+Tools both: `oklch(0.4158 0.15 30)` light / `oklch(0.8528 0.0804 30)` dark → chroma **C 0.15/0.0804 ≥ ~0.02 floor**, opacity **1**, the dead `border-primary/30`→width:0 defect retired (border still 0px but the accent no longer rides it). `pi/lane-b-probe2.mjs`. | — |
| 9 | **u-f6-ramp** Q5-ramp-register (T-10·T-43·T-56) | C | **CENSUS-GREEN** | BR-3 computed CSS-text (`background-clip:text`, NOT GPU): per-stop token L + WCAG contrast vs the LIVE composited card rung — min L **0.427 (light) / 0.818–0.898 (dark)**, `nearBlackSink=false` all 4 legs (the 0.02 clamp is the walk BOUND, never a landing); min contrast **menu ≥5.19 (≥4.5), title ≥3.67 (≥3)**. The A-class near-black-sink resolver defect retired. Frames `ramp-title-{light,dark}.png`, `ramp-menu-{light,dark}.png`. Aesthetic "reads beautiful" → OA-4. | — |
| 10 | **u-f8** generate-plate-species-chrome (T-54·T-55) | C | **ANNEX-OWNER-ATTEST** | Countable legs GREEN + reported: each `.generate-swatch` is a `watercolor-swatch` with `feTurbulence`/`feDisplacementMap` + organic blob `border-radius` (rect→WatercolorDot ✅); verb register 1 primary (Regenerate) + 2 quiet icon-buttons (WR-7). Gate **OA-2 owner-attested by construction**: SVG-filter organic-edge MATERIAL perception + "one verb instrument" register a headless read cannot finalize → **U-F54 (SVG-filter perceptibility)**. Frames `pane-generate-{light,dark}-{1440,390}.png`. | — |
| 11 | **u-f19-t53** dark-caster material (WR-5) | D | **CENSUS-GREEN** | BR-13 computed `getComputedStyle` on `.dark` root (GPU-independent): `.dark --shadow-color` = `color(srgb 0.11 0.098 0.09)` designed dark stone (L 0.216), **≠ `--foreground`** cream (L 0.905) — the RED cream-slab eliminated; caster effective α **0.50 rest / 0.55 hover ≤ floor**; shadow composites DARKER than ground (honest depth). `pi/lane-d-probe.mjs`; frame `pane-picker-dark-1440.png`. Perceptual residual → OA-5. | — |
| 12 | **u-f10** console-veil-material (T-34·T-50) | D | **ANNEX-OWNER-ATTEST** | Measurable calibration reported: veil α **light 0.443 (lowered)** / dark 0.56 (held, `brightness(1.16)`-compensated); clarity window `blur(8px) saturate brightness` ACTIVE both schemes; backdrop a near-uniform warm card (C≈0.014–0.02 — the row's "no chromatic backdrop to sample" corroborated). Gate **OA-3**: veil translucency is a **GPU-composited backdrop-filter perceptual read (explicit O-14 warning + U-F54)**. `pi/lane-d-probe.mjs`; frames `console-veil-{dark,light}.png`. Veil primitive = glass-ui → RELAY. | — |
| 13 | **u-f12** dark-scheme-derived-tint-muddiness (NEW) | D | **ANNEX-OWNER-ATTEST** · owner-ruling bracket | Computed `getComputedStyle` across 4 panes (GPU-free): dark `--card` = UNIFORM neutral warm-brown (C **0.0216**, hue spread **0°** — the row's core claim confirmed); accent lane `--accent-view` seed-tracked (hue **30/190/270/310** = base+view-shift, spread 160°; C 0.055–0.08 ≥ floor). **Pole B (C3-compliant) met as-built; Pole A at its RED pole (needs C3 ledger amendment)**. Gate BR-7 pole-conditional + **OA-4** (clean-vs-muddy read = owner's). Frames `pane-{picker,mix,gradient,atmosphere}-dark-1440.png`. **NO pole picked.** | — |
| 14 | **u-f7** scene-transition-motion (T-14·T-48·T-58) | E | **ANNEX-OWNER-ATTEST** | **ANNEX by law (U-F54).** Both named confounds ABSENT/ISOLATED: PKT-1 dist-clock sane (`.15s`, swap uses `vj-enter` @ `--duration-normal .3s`); O-16-R1 150ms clobber has zero literals + lives in the stubbed `useLayerTransition` sub-layer (isolated by construction). Headless leg confound-flagged (rAF deltas uninterpretable — U-F54 demonstrated a 3rd time; longtask 71/91ms the honest P1-mount signal); wake-gray cure structurally intact (`HeroBlob.vue:289` `onActivated` re-seed). Gate **OA-1: real-GPU swap-window frame** owed (T-58 mandate). Frames `uf7-wake-*`. | — |
| 15 | **zd3-easing** easing never-filed W8 pass (T-22·T-47) | E | **CENSUS-GREEN** | O-17 seat laws measured (3 cells): zero letterbox (`getScreenCTM` deltas 0/0/0/0), one column, `box-shadow:none` well, one bezier literal, travel-dot off; aliveness live (`drag writes `cubic-bezier(0.181,0.23,1,1)` through + persists close/reopen`). Residual was a never-FILED pass DOC, not a red surface. `pi/lane-e-probe-log.txt`; frames `zd3-easing-{1440-light,1440-dark,390-light}-well.png`. | — |
| 16 | **zd3-microchrome** micro-chrome roster (never-filed W8 pass) | F | **ANNEX-OWNER-ATTEST** | Two decisive sub-items cured-GREEN & dispositive: (a) `::selection` computed bg = `--accent-live @32%` + `--foreground` ink both schemes (no default blue); (b) cursor grammar per-role computed (pointer/text/grab/crosshair/default). The roster cannot close all-GREEN: (c) non-macOS scrollbar `.scrollbar-thin` consumers=**0** (wiring gap recorded) needs a **U-F54 non-macOS frame**; (d) static `<title>="Color Picker"` dynamic=false is a RED-on-predicate owner-eye disposition; (e) native tooltips = producer-relay. `microchrome-probe-log.txt`; frames `microchrome-selection-{light,dark}.png`. | (sub-item (d) static-title: owner-eye — trivial dynamic cure OR retire-as-defensible; not a wave re-cure route.) |

---

## STATIC POINTERS — RECORDED-ELSEWHERE (Lane F; zero silent drops, NOT judged live)

| Row | Owning wave / relay | Rationale |
|---|---|---|
| static-t39 (T-39 Q14 perf face; LCP 5141 / TBT 5988) | U.W-PERF (LCP·U-F61) / U.W-CLOSE (TBT) | Perf-budget row, not a VISUAL surface; eager-WebGL-blob boot blocker. |
| static-t20 (T-20 P4 glass-ui Tabs pilling) | U.W-ADOPT (BH relay 17e0f522) | PRODUCER-DECLINED → owner-escalated; no value.js-side cure. |
| static-producer-relay (GAP-L5/revealBloom, PKT-1/O-16-R1, T-52 mask honesty, P3 ScrollCardHeader, WatercolorDot/verb instrument, veil primitive) | U.W-ADOPT / BH relay 17e0f522 | Demo halves judged in Lanes A–D; producer halves ride the glass-ui 5.0.0 adopt. |
| static-uf6-oracle (O-14 proxy-oracle law) | U.W-ORACLE (U-F62) | VISUAL owns the ramp BAND only; the guard-constant-feasibility generalization is ORACLE material. |
| static-ab1 (About-KaTeX scope-dead boot seam) | U.W-CANON | Not a VISUAL material row; pairs with T-53 (material half judged in Lane D). |
| static-zd3-admin (admin never-filed pass + thrown-error a11y half) | U.W-A11Y (U-F56 · U-F58) | Admin surfaces + thrown-error a11y driven live by the A11Y wave. |
| static-t5-letterrail (T-5 second residual: P5/S-3 letter-rail) | U.W-ADOPT (BH relay) | PRODUCER-GATED; distinct from the u-f9-rail active-seat arm (judged GREEN, row 3). |

---

## HONEST COVERAGE STATEMENT

- **Census rows judged live: 16** (7 CENSUS-GREEN · 2 CENSUS-RED · 7 ANNEX-OWNER-ATTEST).
- **Rows annexed to the owner (real-GPU / owner-attest / non-macOS frame required — U-F54): 7** — u-f5, u-f11, u-f8, u-f10, u-f12, u-f7, zd3-microchrome. Each names its dependency (WebGL blob perceptibility · WebGL atmosphere-field gestalt · SVG-filter organic-edge material · GPU backdrop-filter veil · owner-ruling pole + clean/muddy read · real-GPU swap-window frame · non-macOS scrollbar chrome). **No annex row was fake-judged from a headless assertion.**
- **Rows pointed to owning waves (RECORDED-ELSEWHERE, not judged live): 7** static pointers — each homed to a named wave / BH relay (no silent drop).

- **CENSUS-GREEN (retire — cure holds, 7):** u-f9-rail · u-f13-demo · u-f13-band · u-f13-menuink · u-f6-ramp · u-f19-t53 · zd3-easing. Every GREEN row rests on a computed predicate (getComputedStyle / getScreenCTM / measured layout / CSS-text), not an eye-read alone; the two computed-only law rows (u-f13-menuink BR-14, u-f19-t53 BR-13) carry `getComputedStyle` chroma/opacity/shadow-color tables.
- **CENSUS-RED (re-cure owed this wave, 2):**
  - **u-f9-atrest → WR-11 one-law rhythm** (VISUAL remediation slate, demo-side header rhythm tokens): collapse the 0.075em↔0.75em gap divergence; absorb the 24.6px@390 dead-band.
  - **u-f9-scrolled → whole-header scrolled contraction** — re-cure owed, but the cleanest door (P3 `ScrollCardHeader` producer contraction primitive) is **PRODUCER-GATED → coordinate with U.W-ADOPT**; no demo-unilateral landing available (no sticky condensed strip today).

### Routing / hand-forward (non-RED)

- **U.W-ADOPT owns the real glass-ui 5.0.0 adoption** (goo-blob→blob / GooBlob→Blob / `useLayerTransition`→`dockCrossfadeContext`) — the census scaffold re-labels the rename ONLY; the producer halves of u-f5 (GAP-L5/revealBloom), u-f7 (PKT-1/O-16-R1), u-f8 (verb-instrument capsule), u-f10 (veil primitive → BH relay), u-f13-demo (T-52 mask honesty), u-f9-scrolled (P3 ScrollCardHeader) all ride the adopt.
- **U-F76 mount-box change (Lane A):** the WR-4 title step-down moved the picker/blob mount box (title y 166→151.5@1440; blob y 119→104.7) — a reseat that re-opens CLS → **PERF re-measures the picker/readout CLS budget** against the post-WR-4 footprint.
- **u-f12 is an OWNER-RULING bracket** (Pole A ground-tint requires a C3-ledger re-ratification; Pole B is the C3-compliant as-built default) — **no pole picked by the census**; the pole choice + the clean/muddy read are the owner's.

**No census row contradicts a §12 ruling or a §4 retirement. The wave-open W8-inheritance census carries zero silent drops.**
