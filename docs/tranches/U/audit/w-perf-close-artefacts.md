# U.W-PERF — THE SHARED-SURFACE PERF WAVE — CLOSE ARTEFACTS (the wave roll-up)

**Wave**: U.W-PERF — the Q14 LCP/CLS + the U-F76 mount-box ordering law. **designHeavy**: no
(STRUCTURAL — precise gates, born-RED, mechanical rigor; the metric gates ride the LHCI ratified
instrument, NOT the U-F54 real-GPU/owner annex — that annex is U.W-VISUAL's).
**Verdict**: **`complete_with_misses`** (PP-16 — gates-armed-goal-producer-gated: **three of the four
born-RED gates flipped to their true terminal state** — G-PERF-1 CLS RED→GREEN, G-PERF-2 flake
RED→GREEN, G-PERF-4 settle-guard GREEN; **G-PERF-3 (LCP) is the DECIDED escalate** — armed-RED,
producer-gated, handed to the owner as a structural fact, never a gate edit; the U-F3 escalate is the
archetypal gates-armed / goal-producer-gated close).
**Branch**: `tranche-u`. **Lane commits**: `ca8dbca`+`a16e1f4` (perf-lcp-escalate · U-F3 · DOCS-ONLY) ·
`b3f4f76` (perf-dist-gate · U-F14 · `scripts/gates/proof-perf-target.mjs`) · `a0d777d`
(perf-cls · U-F16/U-F76 · `SpectrumPlateCaption.vue` + the PI-1 CLS column). **Close stamp**: this commit.
**Spec of record**: `waves/U.W-PERF.md` (governs) · `audit/registry.md §1/§3/§10/§20/§26` · the lane
records `audit/w-perf/u-f3-escalate.md` · `audit/w-perf/dist-gate/perf-ratio-reanchor.md` ·
`audit/w-perf/pi/cls-delta-record.md` · π-frames under `audit/w-perf/pi/` · the tracking instrument
`docs/tranches/T/audit/pi1-delta-ledger.md`.
**Precedence**: the owner's verbatim Q14 ruling (`RATIFICATION-2026-07-09 §1` — *"Accept no failures —
divine a proper and idiomatic fix. No compromises"*; no re-baseline, no deferral) → `audit/registry.md`
→ `U.md` → `waves/U.W-PERF.md` → this roll-up. Downstream never overrides upstream.

**The wave's thesis, discharged**: three shared-surface perf reds outlived T's visual campaign — a
deterministic mobile CLS nobody watched (a HARD `error` gate the PI-1 ledger never tracked), a flagship
dist perf gate that flaked RED at idle on a false ratio premise, and the ~2× Q14 LCP the reveal cure
cannot reach. Each is brought to its DECIDED terminal state over the picker/readout mount that
U.W-VISUAL + U.W-A11Y SETTLED — **PERF is the TAIL of the U-F76 single-writer chain (VISUAL → A11Y →
PERF), reserve-only**: it locked the settled geometry and MEASURED over it, it did not move the box.
The two value.js-side reds are CURED (born-RED flipped GREEN); the LCP is the DECIDED escalate (the
producer payload half, gated on the unfired glass-ui 5.0.0 adopt) — no gate weakened.

---

## §1 · THE GATE ROW TABLE — four born-RED gates (three flipped to terminal, one DECIDED escalate)

Every metric row is a reading at the **LHCI ratified instrument** (compressed `staticDistDir` per
`lighthouserc.json:5`, mobile w412 dpr1.75, 4× CPU, simulated slow-4G) — instrument parity (registry
§10, load-bearing). NEVER an uncompressed static serve (the ~2.2× inflation artifact — the false
11085 ms → the true 5141 CI). G-PERF-2 is the DIST perf gate (`proof:perf-target`, a Node bench) — a
DIFFERENT instrument, never conflated with the CWV budgets.

| Gate | Family | Lane | RED (born, measured) | Terminal state (measured) | Gate spec |
|---|---|---|---|---|---|
| **G-PERF-1** mobile CLS ≤ 0.1 over the SETTLED mount box **AND** PI-1 tracks CLS | U-F16 | perf-cls `a0d777d` | mobile CLS **0.2146** deterministic ×3 (2.1× the 0.1 budget); `pi1-delta-ledger.md:24-27` budget table omitted CLS entirely | **GREEN** — CLS **0.0010** (−99.5%) over the settled box; PI-1 CLS budget row + per-wave CLS column landed (the untracked-gate ambush closed) | `audit/w-perf/pi/cls-delta-record.md` · LHCI ×3 |
| **G-PERF-2** `proof:perf-target` GREEN K/K at idle **AND** each ratio clause clears its RE-ANCHORED floor by a MEASURED margin | U-F14 | perf-dist-gate `b3f4f76` | `v/json` swung 0.0105–0.0140 (33% spread), floor margin spent to **5%** not 25%; ~50% idle-flake; `test:dist` exited 1 once at head | **GREEN** — premise re-anchored on the co-scaling parse-that `jsonParser` normaliser + peak-of-15; **32/32** clean under variable load (5–28 uptime), 0 false reds; re-probed live at close **PASS 2/2** (v/pt 0.0601 ≥ 0.05, s/pt 0.1256 ≥ 0.1) | `audit/w-perf/dist-gate/perf-ratio-reanchor.md` · `scripts/gates/proof-perf-target.mjs` |
| **G-PERF-3** mobile LCP ≤ 2500 — **ARMED-RED escalate** (flips ONLY on the adopt-gated producer cut OR an owner-ruled re-scope; NEVER a gate edit) | U-F3 | perf-lcp-escalate `ca8dbca`·`a16e1f4` | mobile LCP **~4919 local / 5141 CI** (~2× the 2500 budget); FCP ≈ 4.3 s ≈ LCP floor, paint-gated on the eager payload | **DECIDED ESCALATE** — stays HONESTLY RED; every in-scope value.js lever spent (§3); residual = the producer payload half (RP-2 331.0 KiB JS-eager), gated on the **unfired** glass-ui 5.0.0 adopt (`git -C ../glass-ui tag --list 'v5*'` EMPTY 2026-07-13). **No gate edited** — `lighthouserc.json:13` untouched | `audit/w-perf/u-f3-escalate.md` (the RECORD half) |
| **G-PERF-4** the SETTLE-GUARD — the `.pane-shell > :first-child` mount box IDENTICAL before/after **AND** CLS/LCP measured over the settled box | U-F76 | perf-cls `a0d777d` | registry §20 R-3: 6 findings edit ONE mount across 3 waves; a reseat re-opens CLS → the box was UN-settled pre-chain | **GREEN** — settled plate rect **IDENTICAL** before/after (`y=199 h=613` both); mount guards **o10/o11/o21 16/16**; CLS/LCP measured over THIS settled box. PERF reserved the geometry VISUAL/A11Y settled — never moved it | `audit/w-perf/pi/cls-delta-record.md §G-PERF-4` |

**The count: 3 GREEN (G-PERF-1, G-PERF-2, G-PERF-4) + 1 DECIDED ESCALATE (G-PERF-3), zero FAIL.** The
escalate is a **DECISION, not a flip** (spec §Hard gate): the disease-row law is satisfied when U-F3 is
DECIDED with the structural fact filed + the payload lever BI-acceptance-relayed by name + booked to
W9 — independent of whether the cut fires in-window.

---

## §2 · π/DELTA obligations discharged (every claim with a visual manifestation carries a frame + DELTA)

| Family | π-frame (before → after) | DELTA (measured, at the LHCI instrument) |
|---|---|---|
| **U-F16** | `cls-{before,after}-final-lhci.jpg` + `cls-{before,after}-film-{mid,end}.jpg` — the plate mounts then SHIFTS → mounts at its settled box, no shift; the transient 2-line caption wrap captured NUMERICALLY (the per-frame rAF timeline, `cls-delta-record.md`; a screenshot cannot reliably hit the sub-100ms window — recorded honestly) | mobile CLS **0.2146 → 0.0010 (−99.5%)**, ≤ 0.1; PI-1 CLS-tracked (absent → budget row + per-wave column present) |
| **U-F3** | mobile LHCI LCP paint timeline — first paint gated at ~4.3 s FCP≈LCP; the after-frame is **cure-dependent** (producer-gated), so the escalate's deliverable is the structural-fact document + the residual-recorded PI-1 row, NOT a fabricated GREEN frame (obligation-law honored) | mobile LCP **~4919 local / 5141 CI** (RED, ~2×) → the escalate records the residual; instrument parity noted (NOT the uncompressed 11085) |
| **U-F76** | the SETTLED mount-box geometry `.pane-shell > :first-child`, pre-settle (6-finding flux) → post-chain settled (`y=199 h=613`) | mount-box bounding-rect residual-reflow Δ = **0** (settled); CLS measured over the FINAL geometry, mount guards 16/16 |
| U-F14 | — (pure instrument gate, no visual manifestation) | idle-flake **~50% → 0/32**; ratio-clause measured margin **0–7% → arch-invariant 9–30%**; `test:dist` exit **1-once-at-head → 0 (3/3)** |

---

## §3 · The U-F3 escalate — the structural fact, in one screen (the RECORD half of G-PERF-3)

Every in-scope value.js LCP lever is SPENT (each measured + attributed in PI-1):

| # | Landed cure | Ledger row | Measured effect |
|---|---|---|---|
| 1 | Reveal-only law (LCP element opacity pinned to its settled box) | W2 `:43` | O-24 light **2484 → 2128 ms (−14%)**, LCP identity converged on the picker plate |
| 2 | Google-Fonts strike (self-hosted) | W2 `:43` | **0** cross-origin font requests on the critical path |
| 3 | Hydration-first | W2 `:43` | paint no longer waits on the full component tree |
| 4 | KaTeX lazy (global-import cure) | W8 `:49` | `vendor-katex-*.js` not modulepreloaded |
| 5 | Root-barrel ×10 → subpaths | W6.5 `:47` | grep ZERO root-barrel imports; chunk-graph-neutral |
| 6 | CLS `--dock-band-min-h` + U-F16 plate-caption reservation | W8/U.W-PERF | CLS within budget (**0.0010**) — not the binding red (LCP is) |

**The residual is a single physical fact**: the eager WebGL blob/aurora engine is statically imported
into the **184.9 KiB boot entry** and parses/compiles/mounts before first paint (the ~505 ms
blob-engine-mount task; FCP ≈ 4.3 s ≈ LCP floor). **RP-2 = eager JS 331.0 KiB gz > 280** (the T
close-of-record, `T/FINAL.md §3`; the 347.9 KiB S figure was superseded — directionally lower, NOT
cleared; RP-2 now rides its THIRD tranche). The demo cannot make the engine lazy without deleting the
hero or a fence-forbidden shader fork — the lever is the **producer** `/blob/config` eager-config /
lazy-engine split, gated on the glass-ui 5.0.0 adopt (**no v5 tag**, USER-GATED, UNFIRED).

**The escalate bracket (owner rules; this lane decided nothing):** Pole A cut-in-window flips G-PERF-3
GREEN at the same instrument (PERF measures + records it in PI-1); Pole B owner-ruled re-scope — the
LCP stays HONESTLY RED, the blocker NAMED not swallowed, no gate weakened. Either way the disease-row
law is satisfied by the DECISION. **Booked BY NAME to W9/U.W-CLOSE** (the Q14 LCP adjudication + the
cut-execution residual) — never re-booked to a successor tranche.

---

## §4 · Per-family Dispositions walk (each family → its decided terminal state; zero silent drops)

- **U-F3** `q14-perf-redemption-uncloseable` → **escalate — DECIDED.** The DISEASE/RP-2 arm (its 3rd
  tranche): the LCP gate is producer-coupled and uncloseable by value.js alone (registry §10 direction
  STANDS, magnitude WEAKENED 11085→5141 by the R-1 instrument-parity reconciliation). Value.js landed
  every in-scope idiomatic cure (§3); the residual is the producer payload cut. Presented to the owner
  as a structural fact with both bracket poles; NEVER a gate re-baseline (Q14). `audit/w-perf/u-f3-escalate.md`.
- **U-F14** `perf-ratio-nonportable-flake` → **build — GREEN.** The flagship dist gate's ratio PREMISE
  was false (native `JSON.parse` does not co-scale with the interpreted parser → 25% headroom spent to
  0–7%, ~50% idle-flake). **Pole A picked**: replace the `JSON.parse` denominator with parse-that's own
  `jsonParser` (the identical combinator machinery → numerator + denominator co-scale across arch AND
  node version; the ORACLE `0.0200→0.0160` node-24 threshold-nudge is SUPERSEDED by the premise fix).
  Stability arm: gate on the **peak-of-15** (contention-immune, teeth-preserving). New floors 0.05/0.10
  on the parse-that scale, cleared by an arch-invariant 9–30% margin; 32/32 clean; `test:dist` exit 0.
  A gate-INSTRUMENT fix, orthogonal to the LHCI CWV budgets. `audit/w-perf/dist-gate/perf-ratio-reanchor.md`.
- **U-F16** `untracked-CLS-gate` → **build — GREEN, two arms.** *arm 1 (cure the mount reflow):* the
  `figcaption.plate-caption` wrapped to 2 lines during the boot font transient (self-hosted Fira Code
  arrives after first paint), collapsing 32→14px and re-centring the whole ~613px plate → the
  deterministic CLS. E-3 architectural cure — the single-line MOUNT RESERVATION (`flex-wrap: nowrap;
  overflow: hidden` + a shrinkable lens span in `SpectrumPlateCaption.vue`), locking the settled box
  from frame one (**Pole A at the true source; not a min-height nudge** — Pole B `content-visibility`
  rejected: the plate is above-the-fold primary content, never content-skipped, so `contain-intrinsic-size`
  is ignored). *arm 2 (track CLS in PI-1):* a CLS budget row + per-wave CLS column added to
  `pi1-delta-ledger.md`; the baseline captured HONESTLY (0.2146), not asserted. Shares the U-F76 mount
  (landed LAST). `audit/w-perf/pi/cls-delta-record.md`.
- **U-F76** `shared-surface-coordination` → **build (THE ORDERING LAW) — CLOSED.** 6 findings edit ONE
  picker/readout mount across 3 waves; a reseat re-opens CLS. The single-writer chain VISUAL → A11Y →
  PERF held: VISUAL settled the mount box (U-F5 blob + U-F9 header), A11Y held it CLEAN (U-F27 chose
  Pole-A hit-inflation precisely to hold the box, guards 16/16), **PERF is the TAIL — reserve-only**:
  the U-F16 arm-1 reservation LOCKED the settled geometry (`y=199 h=613` IDENTICAL before/after) and
  CLS/LCP were measured over THIS box. The settle-guard (G-PERF-4) is GREEN — zero residual reflow, no
  writer downstream of PERF exists to re-open it.

Zero silent drops: F3 (escalate, decided + booked), F14/F16 (build, born-RED value.js cure), F76
(build, the ordering law CLOSED) each reach a decided home.

---

## §5 · §BOOKS walk (what rides forward — BY NAME, no silent drop)

- **U-F3 Q14 LCP adjudication → W9 / U.W-CLOSE.** The Q14 close is GREEN-or-escalation (`U.md §Mission`;
  `pi1-delta-ledger.md:19-20`, `:51`). PERF hands the structural fact (`u-f3-escalate.md`) + the
  residual-recorded PI-1 W9 row (LANDED this close, §6); the close ledger flags the LCP as
  attested/escalated. If the producer cut does not fire in-window, the cut-execution residual is booked
  BY NAME to U.W-CLOSE, closing `complete_with_misses` (PP-16) — **DECIDED here, never re-booked to a
  successor tranche** (the disease-row law).
- **The RP-2 / L20 / GAP-L5 producer payload cut → U.W-ADOPT (BI-acceptance) + the landed communiqué
  `17e0f522`.** The producer lever U-F3 needs unblocks at the 5.0.0 adopt; ALREADY DISPATCHED in the
  formation communiqué (`valuejs-inbox-2026-07-12-u-formation.md` §3 the 5.0.0 co-land / §2b GAP-L5) —
  cited BY NAME, **no new addendum authored** (M1/E-2 — the value.js-side record IS the gate). The
  amended gate law: *"L20 + GAP-L5 land TOGETHER or the re-baseline carries a third tranche."*
- **The settled picker/readout mount box → U.W-CLOSE (the settle-guard record).** U-F76's settle-guard
  (G-PERF-4) is the CLOSED single-writer chain; the close ledger records that PERF reserved the
  geometry the VISUAL/A11Y reseats settled (`y=199 h=613`, guards 16/16, zero residual reflow).
- **U-F14 re-anchored floors → `test:dist` / CI (the wired gate).** The re-anchored `proof:perf-target`
  rides the CI-wired `test:dist` slate (the Q13 retain-5, `package.json:83`); its determinism (32/32,
  peak-of-15 on the co-scaling reference) is the standing regression signal.
- **The CLS row → the PI-1 ledger (standing tracking instrument).** U-F16 arm-2 added CLS to the budget
  table (`:24-27` + the per-wave column); every future richness wave re-runs LHCI and records its CLS
  delta (the same append protocol LCP/TBT run — drift is signal too).
- **RECEIVED fold — A11Y's `long-session / memory` watch → the standing `smoke-safari` sustained-30s
  probe** (`U.W-A11Y §modality-triage`; `U.W-PERF §BOOKS`). The historical iOS 294-frame
  `ValueUnit`-nesting class (MEMORY.md — the accumulation site fixed Mar 2026) is perf-adjacent, not a
  U-Fxx family (nothing to build). It is ACKNOWLEDGED here as its perf home and rests on the existing
  `smoke-safari` sustained-30s context-loss probe (the real standing instrument). The A11Y
  `fold → U.W-PERF` booking is thereby closed — received, named, not dropped.

---

## §6 · The single-writer bridge — the U-F3 W9 row LANDED in PI-1 (the CLOSE scribe's serialized write)

The escalate lane (`u-f3-escalate.md §8`) does NOT write `pi1-delta-ledger.md` (the single-writer
rule — the CLOSE scribe is the serialized ledger writer). This close **APPENDED** the U-F3 escalate
row to the ledger in the extended 7-column CLS schema (Lane 1's `a0d777d` added the CLS column first,
so the row carries the CLS cell **≤ 0.1** per the §8 handoff note):

> **U.W-PERF / U.W-CLOSE (U-F3 escalate — the tranche-U inheritance close)** — LCP **5141** (6850/5156/5141,
> ~2× over, RED) · TBT **5988** (the 2-core CI-only red re-homed to U-F61 → G-CLOSE-3; local TBT 188
> GREEN) · CLS **≤ 0.1** (within budget — not the binding red) · LCP FLAT vs T-W4/W8/W9 (NOISE) ·
> **G-PERF-3 ESCALATE (the ruled valid close)** — residual RP-2 331.0 KiB producer-gated, unfired
> adopt, DECIDED here, PP-16 → `complete_with_misses`.

The LCP/TBT cells are the U-F3 escalate's authoritative content (registry §10 / R-1 reconciled, **no
fresh LHCI run** — probe-parsimony, and the blob-RAF hang is the escalated blocker's own manifestation).

---

## §7 · Cross-repo RELAY (E-2) — discharged by citation, no second book

U-F3's escalate is coupled to a glass-ui-level lever (the RP-2 / L20 / GAP-L5 producer payload cut)
and the U-F76 mount coordination depends on the producer blob-`settled` seam (GAP-L5). Both are
**ALREADY DISPATCHED** in the landed formation communiqué (glass-ui HEAD `17e0f522`;
`valuejs-inbox-2026-07-12-u-formation.md` §3 co-land / §2b GAP-L5 / T-60). This wave **cites them BY
NAME and authors NO new addendum** (M1 ruling — the value.js-side record IS the gate; an ack is a
bonus, never waited on). The library-correctness convention co-migration (spectrum-walk /
backward-color) is U.W-LIB/U.W-ADOPT's RELAY, NOT this wave's (PERF touches no color-convention
surface). **SHA-drift note**: the communiqué landed at `17e0f522`; the live glass-ui HEAD has since
advanced (`9feed5e1` co-land preview) — this wave cites by PATH + row names precisely because the
foreign tree moves; no SHA line-integer is threaded into it.

---

## §8 · Suites at close

- **`proof:perf-target` re-probed live at close: PASS 2/2** (v/pt 0.0601 ≥ 0.05 · s/pt 0.1256 ≥ 0.1) —
  G-PERF-2 deterministic GREEN on this machine at close, corroborating the lane's 32/32.
- **CLS cure (`a0d777d`)**: LHCI ×3 mobile CLS 0.0010 (deterministic); mount guards o10/o11/o21 16/16
  (U-F76 handoff stays clean — the settled box is reserved, never reseated).
- **The U-F14 change is source (`scripts/gates/proof-perf-target.mjs`)**; the U-F16 change is source
  (`SpectrumPlateCaption.vue`) — both landed under their own lane commits with their gate proofs; the
  other 9 `test:dist` clauses are untouched (only `proof:perf-target` re-anchored).
- **Instrument parity disciplined throughout**: every CWV number is a reading at the LHCI ratified
  instrument (compressed static-dir, mobile+simulate) — NEVER an uncompressed serve. G-PERF-2 is the
  Node dist bench — a different instrument, never conflated with the CWV budgets.

---

## §9 · Verdict + the PP-16 close honesty

**`complete_with_misses`.** Three born-RED gates flipped to their true terminal state (G-PERF-1 CLS
RED→GREEN, G-PERF-2 flake RED→GREEN, G-PERF-4 settle-guard GREEN); **G-PERF-3 (LCP) is the DECIDED
escalate** — armed-RED, producer-gated, handed to the owner as a structural fact. PP-16 binds
(gates-armed / goal-producer-gated): the U-F3 escalate is the archetypal such row — value.js has spent
every lever it owns, the residual is physically the producer's, and the decision (present the fact +
both poles, book to W9) is the honest terminal state. No gate weakened, no re-baseline, no preset-swap,
no deferral (Q14 verbatim). **U-F76 closed the single-writer chain (VISUAL → A11Y → PERF)** — PERF was
the reserve-only TAIL and did not move the mount box.

**Booked to U.W-CLOSE**: the Q14 LCP adjudication (attested/escalated flag) + the cut-execution
residual (if the adopt does not fire in-window) — both DECIDED here, riding to the close ledger by
name; the ADOPT-gated producer cut (Pole A) floats on the still-unfired glass-ui 5.0.0 tag.

**Remaining DAG**: U.W-ADOPT (trigger-gated on the v5 tag) + U.W-CLOSE (the zero-drop ledger walk). The
Q14 W9 close inherits U-F3/F14/F16 (registry §26); G-PERF-3 rides to CLOSE as the armed-RED escalate.
