# U-F54 OWNER-ATTEST annex rows — U.W-ADOPT Lane B (ride to U.W-CLOSE)

**Filed by**: U.W-ADOPT Lane B (the producer-visual bi-acceptance lane). **Rides to**: **U.W-CLOSE**
(owner-attested). **Binds**: registry §21 (U-F54 — real-GPU never run → §2 still-red TARGETS carry a
demonstrated headless false-red risk; the registry logged TWO on EXACTLY these families: the U-F4
`overture.css` red herring + the U-F13 PRM confound). **Law**: ANY U-F4 or U-F13-producer VISUAL claim
is attested against a REAL-GPU or OWNER frame, or FILED as an owner-attest annex row — NEVER a headless
assertion, never fake-judged.

**Why annex rows and not a headless green:** the two visual claims below are the "reads-correct" halves
that a headless SwiftShader runner cannot honestly judge (the false-red history is on EXACTLY these
surfaces). The headless-TRUSTWORTHY half of U-F4 (the width+reachability probe, post-R-1) is armed
separately in `../pi/g-adopt-2-3-red-evidence.md`; U-F13-producer has NO headless-trustworthy half (the
edge-ring pixel-coverage read is real-GPU/owner ONLY). Both rows below are the OWNER-ATTEST residue.

---

## ANNEX ROW OA-B1 — U-F4 PRM-dock "reads-correct-when-expanded" (owner-attested)

| Field | Value |
|---|---|
| **Family** | U-F4 `reduced-motion-dock-collapse` (producer half; demo EXONERATED) |
| **Claim class** | VISUAL "reads-correct" — the owner-attested half of the U-F4 π/DELTA obligation |
| **What is attested** | Under `prefers-reduced-motion: reduce`, the expanded/always-expanded dock renders full-width with every nav+action control reachable AND the expanded surface READS correct (chrome/spacing/glyph legibility) — desktop viewport, BOTH schemes |
| **π-frame (before → after)** | PRM dock = 44px empty pill, 1/20 controls reachable → PRM dock EXPANDED, controls reachable + reads-correct |
| **DELTA** | dock-width Δ (44px → expanded-width px) + reachable-control Δ (1/20 → 20/20); the width+reachability Δ is headless-trustworthy (G-ADOPT-2), the reads-correct Δ is OWNER-ATTESTED |
| **Attestation** | **REAL-GPU or OWNER frame — NEVER headless** (U-F54). The registry logged a headless false-red here (the `overture.css` red herring). |
| **Gate coupling** | G-ADOPT-2 (born-RED). The width probe flips headless; the reads-correct half flips on the owner frame. |
| **Producer gate** | PRODUCER-ONLY — flips only when glass-ui ships `prm-dock-arm-static-expanded-fallback` at 5.0.0 (relay-spec §Constraint #1). value.js has NO cure (E-3). |
| **RED at filing** | RED (defect LIVE at glass-ui HEAD `8b0f9acc` + pinned preview `2e559f7a`; the `[data-morphing]:not([data-pane-swap])` derivation of `--dock-expand-t` from the frozen `--dock-morph-t`). |
| **Discharge** | The owner attestation is discharged at U.W-CLOSE (owner frame under emulated PRM, both schemes, desktop). If the cut has not fired, the row rides forward with the cut-execution residual (PP-16, complete_with_misses). |

---

## ANNEX ROW OA-B2 — U-F13-producer edge-ring un-shaved (real-GPU / owner-attested)

| Field | Value |
|---|---|
| **Family** | U-F13-producer `dock-edge-clip` (producer clip/fade-mask half ONLY; demo Tools box-model half → U.W-VISUAL) |
| **Claim class** | VISUAL edge-ring pixel-coverage — real-GPU/owner ONLY (no headless-trustworthy half; the PRM confound already demonstrated a false-red on this exact surface) |
| **What is attested** | The producer dock-layer clip/fade-mask does NOT shave EDGE-item focus rings: edge-item ring pixel-coverage ≡ interior-item ring pixel-coverage, at rest AND mid-scroll, BOTH edges, BOTH schemes |
| **π-frame (before → after)** | edge-item focus ring shaved by the clip/fade-mask (clipped arc) → edge ring un-shaved ≡ interior ring (full arc) |
| **DELTA** | edge-ring pixel-coverage Δ (clipped arc → full arc, ≡ interior-item coverage) |
| **Attestation** | **REAL-GPU or OWNER-attested frame — NEVER headless** (U-F54; the PRM confound false-red is the demonstrated hazard on this surface). |
| **Gate coupling** | G-ADOPT-3 (born-RED against a real-GPU/owner frame; ARMED as this annex row — NOT headless-asserted). |
| **Producer gate** | PRODUCER-ONLY — flips only when glass-ui ships `dock-edge-clip-fade-mask-focus-ring-honesty` at 5.0.0 (relay-spec §Constraint #2: mask honesty at rest = `none` + inline safe-inset ≥ ring weight). value.js has NO cure (E-3). |
| **RED at filing** | RED (defect LIVE at glass-ui HEAD `8b0f9acc` + pinned preview `2e559f7a`; the `mask-image: linear-gradient(...)` 0px-fade transparent-stop antialiasing the outermost pixel column at both inline edges — the `black var(--fade-start, 0px)` stop). |
| **Discharge** | The owner/real-GPU attestation is discharged at U.W-CLOSE (edge-vs-interior ring pixel-coverage read on a real-GPU or owner frame). If the cut has not fired, rides forward with the cut-execution residual. |

---

## §Cross-references

- **The relay SPEC** (the two NAMED BI-acceptance constraints, verbatim): `./relay-spec.md`.
- **The G-ADOPT-2/3 born-RED evidence + π/DELTA obligation**: `../pi/g-adopt-2-3-red-evidence.md`.
- **The landed producer record** (cite by name): communiqué §2a (U-F4) + §2b T-52 (U-F13-producer) at
  `../glass-ui/docs/tranches/BH/coordination/valuejs-inbox-2026-07-12-u-formation.md`; BI
  `-u-w-visual.md §B` (T-52 re-cite + the demo-half census-green).
- **U.W-CLOSE obligation**: these two owner-attest rows are the U-F54 annex the U-F4 + U-F13-producer
  visual gates ride; the owner attestation is discharged at close (registry §26 · U.W-ADOPT §BOOKS).
