# U.W-ADOPT — THE DISEASE WAVE (glass-ui 5.0.0 adopt + lib-cut co-land) — CLOSE ARTEFACTS

**Wave**: U.W-ADOPT — the glass-ui 5.0.0 adopt (THE DISEASE ROW, ridden S.W8 → T.W7 → a THIRD tranche
whole) + the U.W-LIB library-cut co-land. **designHeavy**: no (STRUCTURAL — precise gates, born-RED,
mechanical rigor; the two visual gates ride the U-F54 real-GPU/owner-attested annex, NOT a
frontend-design loop).
**Verdict**: **`complete_with_misses`** (PP-16 — **gates-armed-goal-trigger-gated**: the glass-ui v5
cut is USER-GATED and **UNFIRED**; the four born-RED gates are ARMED against the LIVE `tranche/BG`-pinned
tree and flip only at the owner-gated cut; **the disease row is DECIDED in this named wave** independent
of whether the cut fires — the constraints FILED, the co-land ordering AUTHORED, the version cut
OWNER-HELD, the cut-execution residual booked BY NAME to U.W-CLOSE).
**Families**: U-F2 · U-F4 · U-F13-producer · U-F28 · U-F68 · U-F77 — **six, all reaching a decided
disposition; zero silent drops.**
**Branch**: `tranche-u`. **Lane commits**: `f0f2965` (Lane A adopt-cut · U-F2/U-F68 · G-ADOPT-1) ·
`b9a9290` (Lane B producer-bi · U-F4/U-F13-producer · G-ADOPT-2/3 + the OA-B1/OA-B2 annex) · `e82d27a`
(Lane C co-land · U-F77/U-F28 · G-ADOPT-4). **Already-landed wave infra** (evidence, not redone): the
substrate PIN `2e559f7a` (`audit/w-adopt/substrate-pin.md`), the co-land PREVIEW `296b8b2`, the L17
goo-blob→blob consume-swap `110b56f` (`audit/w-adopt/l17-early-consume-swap.md`), the LIB publish packet
`audit/w-lib/publish-packet.md`, the BI relays `c66b5354`+`9feed5e1` (local-only on their `tranche/BI`).
**Close stamp**: this commit.
**Spec of record**: `waves/U.W-ADOPT.md` (governs) · `audit/registry.md §1/§2/§7/§10 R-1/§17/§20 R-4/§22/
§25/§28` (wins on divergence) · the lane records `audit/w-adopt/adopt-cut/G-ADOPT-1-arming.md` ·
`audit/w-adopt/coland/coland-ordering.md` · `audit/w-adopt/producer-bi/relay-spec.md` ·
`audit/w-adopt/producer-bi/uf54-annex-rows.md` · `audit/w-adopt/pi/g-adopt-2-3-red-evidence.md`.
**Precedence**: owner verbatim (§13.5) → `audit/registry.md` → `U.md` → `waves/U.W-ADOPT.md` → this
roll-up. Downstream never overrides upstream.

**Trigger state at close (re-probed live)**: `git -C ../glass-ui tag --list 'v5*'` → **EMPTY** (UNFIRED,
re-checked at close); glass-ui HEAD **`8b0f9acc`** branch `tranche/BI` (disk labels `5.0.0`); the CI pin
`ref: tranche/BG` is now MORE stale than at authoring (points at a branch older than disk HEAD). **The
v5 probe stays STANDING** — if the tag appears during U.W-CLOSE, the unpin + the armed-gate flips execute
there (the orchestrator owns the unpin; the version cut is OWNER-HELD; a lane never unpins/publishes).

**The wave's thesis, discharged**: a ~10-tranche primitive-asks lineage — the glass-ui 5.0.0 adopt that
rode three whole tranches un-landed — is brought to its DECIDED terminal state. The adopt is executed as
an **adopt-EVENT** (an explicit booked cut per the §3.4 pin policy), not a silent floor-bump: the pin
retirement ordering is authored across all 7 CI sites, the §1 producer design reds (U-F4 PRM-dock,
U-F13-producer edge-clip) are re-filed as **EXPLICIT NAMED glass-ui BI-acceptance constraints** (NOT
trusted to topical waves, NOT cured by a demo override — E-3 forbids a consumer CSS override that would
breach glass-ui-first), and the library-correctness version cut is sequenced against BOTH `@mkbabb/value.js
'^3.1.0'` floors (glass-ui peer + keyframes runtime) so the fix and the cut land coherently (U-F77).
**No source edit, no foreign-tree edit, no unpin, no publish, no floor widen lands from this wave** —
the trigger is unfired and the version decision is owner-held.

---

## §1 · THE GATE ROW TABLE — four born-RED gates, ALL ARMED (zero flipped — the cut is unfired; zero FAIL; zero fabricated flip)

Every RED is established against the LIVE `tranche/BG`-pinned tree (source-mechanism verified in BOTH
the pinned preview `2e559f7a` AND the real glass-ui HEAD `8b0f9acc` — the `file:` dep the local demo
resolves) + the registry's CONFIRMED state. **E-3 binds: no gate is flipped over the pinned sandbox
preview** — the armed gates flip only at the REAL owner-gated cut. **The two visual gates (G-ADOPT-2,
G-ADOPT-3) ride the U-F54 real-GPU/owner-attested annex — NEVER a headless assertion** (the registry
logged TWO demonstrated headless false-reds on EXACTLY these families: the U-F4 `overture.css` red
herring + the U-F13 PRM confound, §21).

| Gate | Family | Lane | RED reason (born, LIVE evidence) | Flip condition (owner-gated cut) | Record |
|---|---|---|---|---|---|
| **G-ADOPT-1** the adopt-cut verification | U-F2 + U-F68 | Lane A `f0f2965` | **ARMED-RED (3 of 4 arms RED).** (a) **7** `ref: tranche/BG` pins live (`ci.yml` ×6 + `deploy-pages.yml` ×1) — grep must return 0; (b) package-lock records a STALE `~4.0.0` @`:193` vs disk `5.0.0` (U-F68); (c) demo `goo-blob` import surface **already GREEN** (`110b56f`, `grep @mkbabb/glass-ui/goo-blob demo/` → 0); (d) boot-smoke armed-not-run vs the sandbox (the §3.4 Tabs-class named-export drift catch) | retire all 7 pins → tagged 5.0.0; `npm install` refreshes the lock to 5.0.0; the import surface holds (CSS-selector re-probe watch); boot-smoke passes vs the built 5.0.0 dist | `audit/w-adopt/adopt-cut/G-ADOPT-1-arming.md` |
| **G-ADOPT-2** U-F4 PRM-dock BI-acceptance | U-F4 | Lane B `b9a9290` | **ARMED born-RED.** Under `prefers-reduced-motion: reduce` the dock stays a 44px empty pill on every desktop view — 19/20 nav+action controls clipped, NO hover/click/keyboard recovery (WCAG 2.4.3/2.4.7/1.4.13). Mechanism (cite by BLOCK): `morph.css` `[data-morphing]:not([data-pane-swap])` derives `--dock-expand-t: var(--dock-morph-t)` for `.expanded`/`.always-expanded` while `--dock-morph-t` freezes at 0 → the static endpoint `--dock-expand-t: 1` is overridden. PRODUCER-ONLY; demo EXONERATED (`overture.css` a RED HERRING) | glass-ui ships `prm-dock-arm-static-expanded-fallback` at 5.0.0 (relay-spec Constraint #1). Width+reachability probe headless-trustworthy (post-R-1); reads-correct half OWNER-ATTESTED (OA-B1). value.js has NO cure (E-3) | `audit/w-adopt/pi/g-adopt-2-3-red-evidence.md` |
| **G-ADOPT-3** U-F13-producer edge-clip (real-GPU/owner) | U-F13-producer | Lane B `b9a9290` | **ARMED born-RED — NO headless-trustworthy half.** The producer dock-layer clip/fade-mask shaves EDGE-item focus rings (interior rings full). Mechanism (cite by BLOCK): `overflow.css` `mask-image: linear-gradient(...)` — the 0px-fade transparent stop (`black var(--fade-start, 0px)`) antialiases the outermost pixel column at both inline edges; `overflow-clip-margin` is a PARTIAL mitigation that does NOT close it | glass-ui ships `dock-edge-clip-fade-mask-focus-ring-honesty` at 5.0.0 (mask honesty at rest = `none` + inline safe-inset ≥ ring weight; relay-spec Constraint #2). Attested on a **real-GPU/owner frame ONLY** (OA-B2 — the PRM confound demonstrated a false-red on this exact surface). value.js has NO cure (E-3) | `audit/w-adopt/producer-bi/uf54-annex-rows.md` |
| **G-ADOPT-4** U-F77 co-land ordering | U-F77 | Lane C `e82d27a` | **ARMED born-RED, TWO arms.** **4a** build-time re-enumeration: the source-tree census returns the FOUR raw-channel surfaces (glass-ui spectrum-walk + parseCSSColor; keyframes backward-color + parseCSSValue×3) and the invariant is un-re-run against the THEN-CURRENT constellation. **4b** BOTH `^3.1.0` floors: the version decision is un-taken (owner-held, §13.5); both floors read `^3.1.0`, value.js is 3.1.0 | **4a** flips GREEN when re-run at the cut confirms every enumerated surface preserved-or-co-migrated (Locus-P → surfaces 2+4 PROVABLY PRESERVED, byte-identical, zero co-migrants — §3) AND the U-F34 rename set is complete on live glass-ui src. **4b** flips GREEN when the cut is taken with both floors held (a 3.x minor auto-adopts) OR both siblings co-widened + relayed (a 4.0.0 MAJOR) | `audit/w-adopt/coland/coland-ordering.md` |

**U-F28** carries **no gate** — a WATCH row (the one keyframes ask is fixed-unreleased at their HEAD;
value.js has ZERO direct keyframes imports, a standing GREEN structural fact, not a born-RED). Retires
the moment keyframes cuts its next tag; booked to U.W-CLOSE's book re-probe.

**The count: 4 born-RED gates ARMED (0 flipped — the cut is UNFIRED · 0 FAIL · 0 fabricated red · 0
pretend-headless flip). The disease row is DECIDED in-wave.** The completion LAW (spec §Completion
criterion) is satisfied by the DECISION (build here, with named BI-acceptance constraints + the co-land
ordering), independent of whether the owner-gated cut fires in-window — the archetypal PP-16
gates-armed-goal-trigger-gated close.

---

## §2 · The LIVE-TREE drift the lanes CONFIRMED (the doc/registry counts were stale — corrected here)

The arming lanes re-verified every count against the live tree; the wave-doc / registry integers had
drifted (a moving foreign tree + intervening waves). Corrected, source-of-truth = the re-probe command,
not the integer:

| Fact | Doc / registry said | LIVE tree reads (this close) | Cause |
|---|---|---|---|
| `tranche/BG` pin count | **6** (gate §Hard-gate; registry §1 cites 3) | **7** — `ci.yml` ×6 + `deploy-pages.yml` ×1 | the PERF wave grew `ci.yml`: a 6th glass-ui checkout in the `boot-smoke:` job (`ci.yml:668` header) |
| lock workspace-dep node | `~4.0.0` @`:194` (gate); `4.2.0` (registry §17) | `~4.0.0` @`:193` | line-shift; the `~4.0.0` record is the current false record vs disk `5.0.0` |
| goo-blob→blob demo import | "WILL break 3 demo sites at the cut" (registry §1) | **ALREADY SWAPPED** (`110b56f`) — grep returns 0 | the L17 early consume-swap landed; residual `.goo-blob-*` are glass-ui-INTERNAL CSS selectors, not dead imports |
| U-F34 conversion renames (glass-ui) | **three** (publish-packet §3 vs `c66b5354`) | **FIVE** (pinned preview `2e559f7a`): + `oklab2linearSrgb` + `oklab2rgb255` in `composables/color/index.ts` | the packet §3 rename table is an UNDERCOUNT; the real cut re-greps live (registry §28.2 — the count is a BUILD concern) |
| glass-ui HEAD | `tranche/BG` (gate authoring) | `tranche/BI` @ `8b0f9acc` | glass-ui advanced BG→BI; the CI pin is now more stale than disk HEAD |
| `v5*` tag | none (USER-GATED) | **none** (re-probed at close) | UNFIRED |

These drifts are **corrected in the lane records** and do not change any disposition — they SHARPEN the
flip conditions the orchestrator executes at the cut (7 pins not 6; 5 renames not 3; the CSS-selector
re-probe watch).

---

## §3 · Per-family §Dispositions walk (each family → its decided terminal state; zero silent drops)

- **U-F2** `adopt-trigger-disease` → **build (THE DISEASE LAW — its own wave) — DECIDED, gate ARMED.**
  Ridden ≥3 tranches un-decided → a disease row → a wave of its own. The adopt is an explicit booked
  EVENT at the owner-gated cut: retire the pin at all 7 CI sites → tagged 5.0.0, adopt the goo-blob→blob
  rename (already done at the import surface, E-3 forbids a re-export alias), refresh the lock. The §1
  producer design reds re-file as EXPLICIT NAMED BI-acceptance constraints (U-F4 + U-F13-producer, below).
  G-ADOPT-1 ARMED-RED. `audit/w-adopt/adopt-cut/G-ADOPT-1-arming.md`.
- **U-F4** `reduced-motion-dock-collapse` → **fold → U-F2's BI-acceptance list (PRODUCER-ONLY; demo
  EXONERATED) — DECIDED, gate ARMED.** R-1 corrected the mechanism to PRODUCER-ONLY (`morph.css`
  `[data-morphing]` PRM latch; `overture.css` a RED HERRING). The phantom demo static-fallback row is
  RETIRED; there is NO value.js cure — the build is the producer BI-acceptance constraint
  `prm-dock-arm-static-expanded-fallback` (relay-spec Constraint #1). E-3 forbids a demo override.
  G-ADOPT-2 ARMED-RED + OA-B1 owner-attest annex. `audit/w-adopt/producer-bi/relay-spec.md`.
- **U-F13-producer** `dock-edge-clip` (producer half only) → **build via RELAY + BI-acceptance —
  DECIDED, gate ARMED.** The clip/fade-mask is producer-side; named as constraint
  `dock-edge-clip-fade-mask-focus-ring-honesty` (one of the §2b nine — communiqué §2b T-52 — sharpened
  to the edge-ring-shave a11y class). Gated on a real-GPU/owner frame (NO headless-trustworthy half).
  The demo Tools true-button box-model half is CENSUS-GREEN (interior ring) → U.W-VISUAL's row (already
  dispatched, do NOT re-book). G-ADOPT-3 ARMED-RED + OA-B2 owner-attest annex.
- **U-F28** `kf-prm-expand-fixed-unreleased` → **retire on their next tag (WATCH row, no work, no gate)
  — DECIDED.** Fixed-unreleased at keyframes HEAD (`managed-stepper.ts` PRM arm now emits, disk `5.2.0`,
  no tag past it); value.js has ZERO direct keyframes imports (the keyframes devDep is the demo build's
  provision of glass-ui's kf peerDependency — CLAUDE.md §Dependencies) → nothing to build. A standing
  GREEN structural fact, not a born-RED. Retires the moment keyframes cuts its next tag; **booked to
  U.W-CLOSE's book re-probe.** `audit/w-adopt/coland/coland-ordering.md §5`.
- **U-F68** `glass-ui-lock-adopt-drift` → **fold → the adopt cut (lock refresh) — DECIDED, sub-assertion
  of G-ADOPT-1(b).** The lock's `~4.0.0` @`:193` is a false record vs disk `5.0.0`; the adopt gap is
  ALREADY LIVE locally. glass-ui 5.0.0 peers ARE satisfiable now (value 3.1.0, kf 5.2.0). No independent
  work — the lock refreshes at the cut (`npm install` after the pin retires).
- **U-F77** `library-cut-adopt-ordering` → **build (THE CO-LAND ORDERING) — DECIDED, gate ARMED.** The
  lib-correctness version cut is semver-MAJOR (publish-packet §2) against BOTH `^3.1.0` floors (glass-ui
  peer `:1091`/`:1129` + keyframes RUNTIME `dependencies:268`). The co-land sequences the version
  decision (owner-held, §13.5), the build-time re-enumeration gate, and the two sibling co-migrations.
  **KEY FINDING: LIB's landed invariant is Locus-P (denorm-on-output + normalize-on-construct), leaving
  the raw `mixColors`/`sampleColorRamp`/`color2` UNCHANGED → surfaces 2 + 4 are PROVABLY PRESERVED
  byte-identical, ZERO co-migrants** (LIB-G6 `CONVENTION_INVARIANT_LANDED = true`). The §28 4-surface
  worry RESOLVES: neither sibling direct-channel reader co-migrates. G-ADOPT-4 ARMED-RED (both arms).
  `audit/w-adopt/coland/coland-ordering.md`.

Zero silent drops: U-F2, U-F4, U-F13-producer, U-F28, U-F68, U-F77 each reach a decided home.

---

## §4 · The ARMED-GATE SLATE (each gate · its RED reason · its flip condition — the orchestrator's cut runbook)

The four born-RED gates are the cut-execution runbook. Each stays RED until the owner-gated cut; NONE is
faked over the sandbox (E-3). Restated compactly for the successor:

| Gate | RED reason (today) | Flip condition (at the cut) | Trustworthiness |
|---|---|---|---|
| **G-ADOPT-1** (U-F2 + U-F68) | 7 `tranche/BG` pins live · lock `~4.0.0` @`:193` · boot-smoke un-run vs 5.0.0 dist (import surface already GREEN) | retire all 7 pins → tagged 5.0.0 · `npm install` refreshes lock · boot-smoke green vs the built 5.0.0 dist (§3.4 named-export drift catch) | CI / `test:dist` (deterministic) |
| **G-ADOPT-2** (U-F4) | PRM dock = 44px empty pill, 1/20 controls reachable (`morph.css` frozen-clock derivation) | glass-ui ships `prm-dock-arm-static-expanded-fallback` at 5.0.0 | width+reachability HEADLESS-TRUSTWORTHY (post-R-1); reads-correct OWNER-ATTESTED (OA-B1) |
| **G-ADOPT-3** (U-F13-producer) | edge-item focus rings shaved by the `overflow.css` mask 0px-fade transparent stop | glass-ui ships `dock-edge-clip-fade-mask-focus-ring-honesty` at 5.0.0 (mask `none` at rest + inline safe-inset ≥ ring weight) | **real-GPU/owner ONLY** — NO headless-trustworthy half (OA-B2) |
| **G-ADOPT-4a** (U-F77) | the re-enumeration returns 4 raw-channel surfaces; the invariant un-re-run at the live cut | re-run confirms every surface preserved-or-co-migrated (Locus-P preserves 2+4) + the U-F34 rename set complete on live src | build-time census (deterministic) |
| **G-ADOPT-4b** (U-F77) | the version decision is un-taken (owner-held); both floors read `^3.1.0` | cut taken with both floors held (3.x minor auto-adopts) OR both siblings co-widened + relayed (4.0.0 MAJOR) | package.json grep (deterministic) |

**The version cut is OWNER-HELD (§13.5).** The LIB publish packet recommends `4.0.0` (MAJOR — the class
is forced; the number is the owner's; `audit/w-lib/publish-packet.md`). This wave does not choose the
number, does not widen any floor, does not publish.

---

## §5 · The co-land ORDERING pointer (authored; executed by the orchestrator at the owner-gated cut)

The full co-land sequence is authored in **`audit/w-adopt/coland/coland-ordering.md §6`** (and the CI
retirement ordering in `audit/w-adopt/adopt-cut/G-ADOPT-1-arming.md §2`). In brief, the cut executes in
ONE window:

```
1. value.js cuts <N>.0.0            ← OWNER-HELD (§13.5); number owner's, MAJOR class forced
   └─ (a 3.x minor auto-adopts both floors → binds (b)/(c) reduce to the U-F34/U-F29 renames only)
2. IN THE SAME WINDOW:
   ├─ glass-ui: widen peer+dev floor → ^<N>  +  apply the U-F34 renames (FIVE — §1.1, re-grep live)
   │            └─ U-F30: no source change (spectrum-walk PRESERVED); U-F29: none (zero parseCSSValue)
   └─ keyframes: widen runtime `dependencies` floor → ^<N>  +  parseCSSSubValue→parseCSSValues
                └─ U-F30: no source change (backward-color PRESERVED); 3 parseCSSValue sites guarded
3. Retire the 7 CI pins → tagged 5.0.0 · refresh the lock · boot-smoke green vs 5.0.0 dist.
4. G-ADOPT-4a re-run against the THEN-CURRENT constellation → GREEN; G-ADOPT-4b → GREEN.
5. RELAY-ADOPT addendum extends the landed communiqué (foreign-tree fence; written only at the fired cut).
```

glass-ui's own 5.0.0 tag (which U.W-ADOPT floats on) sequences INDEPENDENTLY; NEITHER floor is stranded.

---

## §6 · §BOOKS walk — what rides forward, BY NAME (no silent drop)

- **The cut-execution residual → U.W-CLOSE.** The disease row is DECIDED here regardless; the cut
  execution (the actual unpin + rename + lock refresh + boot-smoke, if the trigger is gated past the
  window) is booked BY NAME to U.W-CLOSE's book re-probe. **Never re-booked to a successor tranche** (the
  disease-row law). The v5 probe stays standing at U.W-CLOSE; on fire the unpin + the armed-gate flips
  execute there.
- **The four armed-gate flips → U.W-CLOSE (re-probe at the cut).** G-ADOPT-1/2/3/4 are ARMED-RED; each
  flip condition (§4) is re-probed live at close and executes only at the real owner-gated cut. The
  build-time re-enumeration (G-ADOPT-4a) re-greps the THEN-CURRENT constellation — the exact consumer
  count is a BUILD concern, never a formation gate.
- **The U-F54 owner-attest annex rows OA-B1 + OA-B2 → U.W-CLOSE (owner-attested).** OA-B1 (U-F4 PRM-dock
  "reads-correct-when-expanded") + OA-B2 (U-F13-producer edge-ring un-shaved) are the visual "reads-
  correct" halves a headless runner cannot honestly judge (the false-red history is on EXACTLY these
  surfaces). Discharged at close on a real-GPU or owner frame; if the cut has not fired, the rows ride
  forward with the cut-execution residual. `audit/w-adopt/producer-bi/uf54-annex-rows.md`.
- **U-F3 (Q14/RP-2 LCP producer cut) / G-PERF-3 inheritance → U.W-CLOSE.** The producer payload lever
  U-F3 needs unblocks ONLY at this adopt cut (ADOPT → U.W-PERF, registry §26). U.W-PERF already CLOSED
  with G-PERF-3 as the DECIDED escalate (armed-RED, producer-gated on THIS unfired adopt); its close
  hands the structural fact + the residual-recorded PI-1 W9 row to U.W-CLOSE. If this adopt fires
  in-window, the RP-2 331.0 KiB JS-eager cut (Pole A) flips G-PERF-3 GREEN; if not, the LCP stays
  honestly RED and the owner rules the re-scope (Pole B). **The adopt state IS the input to the U-F3
  adjudication** — recorded here so the coupling is not invisible.
- **U-F28 keyframes next-tag retire → U.W-CLOSE book re-probe.** The WATCH row re-probes live at close;
  retires the moment keyframes cuts its next tag.
- **The exhaustive-census producer residue (DISPOSITION-LEDGER §C.1 — three T→U orphans homed here).**
  - **CC-1 `.glass-wash` demo consume-half → CONSUME-AT-CUT** (gated on THIS adopt). The producer half
    rides packet P8 in the §B BH-relay set (already in the letter lane, `17e0f522`); the demo consume-
    half (paint the two demo sites' rung once glass-ui ships the `.glass-wash` fill) WAITS on the
    primitive — E-3 forbids a demo override of the producer fill. Named so the consume is not invisible;
    no demo edit today.
  - **L8 / the L2..L16 open GLASSUI asks → U-F2 (the §1 design reds).** L8's 5th-booking ESCALATION is a
    disease-adjacent signal (kin of the ≥3-tranche disease-row law) — recorded, not swept. The per-item
    book-reverify is G-ADOPT-1's adopt-cut ledger-vs-rebuilt-dist EXTENDED to each open ask, re-probed at
    U.W-CLOSE's book re-probe.
  - **W8-2 rest-floor F9.R1 producer knob → the §B BH relay (producer knob at the cut).** Rides the adopt
    cut like the GAP-* reds; the in-bracket rest-floor DEFAULT already landed in T's package, only the
    producer-knob residual carries. PR-2 fence: no demo cure for a producer knob.

---

## §7 · Cross-repo RELAY (E-2) — the honest declaration

This wave touches glass-ui component surfaces (the dock morph geometry, the dock-layer clip/fade-mask)
AND the glass-ui-level raw-channel convention contract — it carries a RELAY row. **In this UNFIRED arm,
the relay SPEC is authored in-tree (the value.js audit doc is the UNFIRED-arm home); NO foreign-tree edit
lands from this campaign** (the foreign-tree fence — the addendum is written into the glass-ui BI
coordination tree ONLY at the fired cut, path-scoped, plain-push-only, single-file).

- **The producer material is ALREADY DISPATCHED — cited BY NAME, no second book (M1/E-2):**
  - The FORMATION communiqué landed at glass-ui HEAD `17e0f522` (`valuejs-inbox-2026-07-12-u-formation.md`,
    §28.3): §1 the mixColors-convention coupling, §2a U-F4 PRM-dock, §2b the 9 carried-forward T producer
    reds (incl. T-52 = the U-F13-producer half), §3 the 5.0.0 + parseCSSValue-reshape co-land.
  - The LIB-invariant addendum landed at their BI inbox `valuejs-inbox-2026-07-13-u-w-lib-invariant.md`
    (commit `6c3e1609`, local at glass-ui HEAD).
  - The BI letters (`../glass-ui/docs/tranches/BI/coordination/`) — `-u-w-visual.md §B` re-cites T-52 as
    "ALREADY DISPATCHED — do NOT re-book" and confirms the demo Tools box-model half is CENSUS-GREEN.
- **What THIS wave authors (the co-land ADDENDUM SPEC, written to the foreign tree only at the cut):**
  (1) the U-F4 PRM-dock arm-static-fallback as a NAMED 5.0.0 BI-acceptance constraint (SHARPENING §2a);
  (2) the U-F13-producer edge-clip/fade-mask focus-ring constraint (one of the §2b nine, sharpened to the
  edge-ring-shave a11y class — RE-CITE, no second book); (3) the U-F77 co-land — the chosen Locus-P
  invariant + whether glass-ui's spectrum-walk HOLDS the raw convention (it DOES — surfaces 2+4 preserved,
  §3); (4) the version decision against glass-ui's `^3.1.0` peer floor (owner-held, presented WITH the
  landed fix).
- **The keyframes co-migration** rides U-F77's pin-widen (its `^3.1.0` RUNTIME `dependencies` floor + the
  backward-color raw-oklab convention — PRESERVED, Locus-P) — relayed through keyframes' active dev
  process at the cut. Cross-repo convention changes are RELAYED, not silently shipped.
- **SHA-drift note**: the communiqué landed at `17e0f522`; the live glass-ui HEAD has since advanced
  through `dd9af7cf` → `ff0f62f2` → `8b0f9acc`. The relay cites by PATH + row names precisely because the
  foreign tree moves; no SHA line-integer is threaded into the constraint text (the `morph.css` /
  `overflow.css` mechanisms are cited by BLOCK, never `:70-73`).

---

## §8 · The BI relay commits + the substrate infra (evidence; not this close's work)

The already-landed wave infra (do NOT redo — cited for the record):

- The substrate PIN `2e559f7a` (both symlinks → `.claude/worktrees/glass-ui-pinned`;
  `audit/w-adopt/substrate-pin.md`; UNPIN at the v5 tag).
- The co-land PREVIEW `296b8b2` (the U-F34 renames patched UNCOMMITTED into the pinned sandbox — FIVE
  conversions, the ref-drift undercount flagged for the cut; the keyframes `parseCSSSubValue→parseCSSValues`
  node_modules copy; pinned dist rebuilt, gh-pages + typecheck + LIB slate 20/20 GREEN). **EPHEMERAL —
  discarded at unpin; the real migration rides U.W-ADOPT.**
- The L17 goo-blob→blob consume-swap `110b56f` (`audit/w-adopt/l17-early-consume-swap.md`).
- Their BI relay commits `c66b5354` (dist breakage) + `9feed5e1` (co-land preview) — **LOCAL-ONLY on
  their `tranche/BI` (no upstream)**; the foreign tree stays dirty; value.js added only its own files.

---

## §9 · Verdict + the PP-16 close honesty

**`complete_with_misses`.** All four born-RED gates are ARMED against the LIVE `tranche/BG`-pinned tree;
**NONE is flipped, because the glass-ui v5 cut is USER-GATED and UNFIRED** (`git -C ../glass-ui tag
--list 'v5*'` EMPTY, re-probed at close). PP-16 binds (gates-armed-goal-trigger-gated) — the archetypal
such close, alongside U.W-PERF's U-F3 escalate. **The disease row is DECIDED in this named wave**: the
adopt-EVENT ordering is authored (7 pins → 5.0.0, lock refresh, boot-smoke), the §1 producer design reds
are re-filed as EXPLICIT NAMED BI-acceptance constraints (`prm-dock-arm-static-expanded-fallback` +
`dock-edge-clip-fade-mask-focus-ring-honesty`), and the U-F77 co-land ordering is sequenced against BOTH
`^3.1.0` floors with Locus-P PROVABLY preserving both raw-channel sibling surfaces (zero co-migrants).

**The doubled honesty discipline held**: no gate weakened, no fabricated red, no pretend-headless flip
(the two visual gates ride the U-F54 owner-attested annex; E-3 forbids a flip over the pinned sandbox
preview), no demo override of a producer defect (glass-ui-first), no unpin, no publish, no floor widen
(the version cut is OWNER-HELD). Six families reach a decided disposition; zero silent drops.

**Booked to U.W-CLOSE (BY NAME)**: the cut-execution residual · the four armed-gate flips · the U-F54
owner-attest annex rows OA-B1/OA-B2 · U-F28's keyframes next-tag retire · the U-F3/G-PERF-3 LCP
adjudication (the adopt state is its input) · the census-residue rows CC-1/L8/F9.R1. **The v5 probe stays
STANDING — if the tag appears during U.W-CLOSE, the unpin + the armed-gate flips execute there** (the
orchestrator owns the unpin; the version cut is owner-held).

**Remaining DAG**: **U.W-CLOSE only** (the terminal sink UNBLOCKS — 9 of 10 waves decided; CLOSE fires
next and LAST). The zero-drop ledger walk · the publish decision presented WITH the landed fix · the
real-GPU visual annex owner-attested · every book re-probed live.
