# U.W-VISUAL — THE OWNER-EYE STILL-REDS — CLOSE ARTEFACTS (the wave roll-up)

**Wave**: U.W-VISUAL — the owner-eye still-reds (T.W8 terminal-state inherited). **designHeavy** (E-6).
**Verdict**: **`complete_with_misses`** (PP-16 — gates-pass-goal-unmet: every headless-verifiable
re-cure flipped GREEN under an independent gate; the 7 owner-attest ANNEX rows remain honestly OPEN
by law U-F54, and the gh-pages substrate break is routed to a sibling wave — neither is a VISUAL
defect).
**Branch**: `tranche-u`. **Gate stamp**: `c0dfcb8`. **Integration head**: `be807ce` (lane-1 header
`cfa8c31` + lane-2 riders `be807ce`; pushed `8a95a26..be807ce`).
**Spec of record**: `waves/U.W-VISUAL.md` · the wave-open census `audit/w-visual/census.md` (the
verdict table) · the independent gate `audit/w-visual/pi/gate/gate-verdict-log.txt`.
**Precedence**: the owner's verbatim `MANDATE §0.8` (T-61 — the WHOLE header block contracts on
scroll, title + padding + background band as ONE condensed strip) wins over every encoding below.

---

## §1 · THE CENSUS SUMMARY — 16 rows judged live at wave-open, terminal after this wave

The wave-open W8-inheritance census (`audit/w-visual/census.md`) re-judged the owner-uncertified
still-reds T.W8 leaves red, LIVE against the served T.W8-terminal build (worktree
`u-visual-census` HEAD `e97a9d1`, census ports 8591–8596). It filed **16 rows: 7 CENSUS-GREEN · 2
CENSUS-RED · 7 ANNEX-OWNER-ATTEST**, plus 7 RECORDED-ELSEWHERE static pointers (zero silent drops).
This wave re-cured the 2 CENSUS-RED and landed the two rider halves; the 7 GREEN retire; the 7 ANNEX
carry to the owner.

### 7 CENSUS-GREEN — RETIRED (the cure holds by a computed predicate, not an eye-read alone)

| Row | Family | The retiring predicate (computed) |
|---|---|---|
| **u-f9-rail** (T-5/P1-R1) | U-F9-rail | `.rail-dot-seat` (WatercolorDot) center ≡ active `.channel-rail-item[aria-selected]` center at **(0,0) offset on every rung (L/a/b/α) × 2 viewports × 2 schemes** (16 cells). OA-7 coherence residual → owner. |
| **u-f13-demo** (T-36) | U-F13-demo | `ActionBarToggle.vue` `margin-inline:4px`; Tools true-button is INTERIOR (flanked) so its focus-ring envelope sits within the dock-layer content box, renders whole. Producer clip/fade/mask (T-52) → ADOPT. |
| **u-f13-band** (T-57/WR-9) | U-F13-band | band block-size **72.00px INVARIANT** + `.pane-container` top **103.0px INVARIANT** across expanded↔collapsed↔re-expanded; scene-top shift **Δ0.00px** (BR-10). Cure `style.css` `--dock-band-min-h = 40+32 = 72px ≥ 71.4px expanded pill`. |
| **u-f13-menuink** (P9-R4/Q10) | U-F13-menuink | BR-14 computed `getComputedStyle`: accent re-homed onto the ink `color` channel — Login+Tools `oklch(0.4158 0.15 30)` light / `oklch(0.8528 0.0804 30)` dark → chroma **C 0.15/0.0804 ≥ ~0.02 floor**, opacity **1**; the dead `border-primary/30`→width:0 defect retired. |
| **u-f6-ramp** (T-10/T-43/T-56) | U-F6-ramp | BR-3 computed CSS-text (`background-clip:text`): min L **0.427 light / 0.818–0.898 dark**, `nearBlackSink=false` all 4 legs (the 0.02 clamp is the walk BOUND, never a landing); min contrast **menu ≥5.19, title ≥3.67**. A-class near-black-sink resolver defect retired. Aesthetic → OA-4. |
| **u-f19-t53** (WR-5) | U-F19 | BR-13 computed `getComputedStyle` on `.dark`: `--shadow-color = color(srgb 0.11 0.098 0.09)` designed dark stone (L 0.216) ≠ `--foreground` cream (L 0.905); caster α **0.50 rest / 0.55 hover ≤ floor**; shadow composites DARKER than ground. OA-5 residual → owner. AB-1 KaTeX → U.W-CANON (RETIRE-census). |
| **zd3-easing** (T-22/T-47) | (never-filed W8 pass) | O-17 seat laws measured (3 cells): zero letterbox (`getScreenCTM` 0/0/0/0), one column, `box-shadow:none` well, one bezier literal; aliveness live (drag writes `cubic-bezier(0.181,0.23,1,1)` through + persists close/reopen). The residual was a never-FILED pass DOC, not a red surface — retires-with-the-O-17-cite. |

### 2 CENSUS-RED — RE-CURED THIS WAVE (born-RED headless-verifiable, flipped GREEN under the independent gate, with DELTAs)

| Row | Family | Cure (landed) | DELTA (measured, RED→GREEN) | Gate |
|---|---|---|---|---|
| **u-f9-atrest** (T-2/T-51/T-59) | U-F9 | ONE cqi rhythm law — the hard `<lg` band switch killed; `useHeaderCondense.ts` + `header.css` + `ColorPicker.vue` (lane-1). BR-4 guard STAYS. | inter-row rhythm gap: census **4.0px@1440 vs 24.6px@390 (~6× divergence) → 7.168px@1440 vs 5.012px@390 (~1.4×, ONE law)**. The 24.6px@390 dead-band → title-row→readout flex gap **5.0px = the rowGap token** (designed air, readout-seam T-33b oracle confirms — NOT a dead band). *(The residual title-TEXT→readout 25.6px@390 is the U-F5 blob-seat reservation — `.title-row` min-height 73.12px, the bead visibly occupies it — annexed owner-attest u-f5, NOT the U-F9 rhythm.)* | **BR-5 PASS** |
| **u-f9-scrolled** (T-61/T-42 · §0.8) | U-F9 | WHOLE-header contraction — the demo-unilateral sticky condensed strip the census said did not exist. `useHeaderCondense.ts` scroll-driver + `condensed`-class re-key title + padding + background veil band together. | @390x520 (both schemes, card overflow 149px, condensed TRUE): header block-size **183.95→87.29px Δ-96.66px** (element screenshot 368→176px dpr2 — a REAL layout box shrink, NOT a compositor transform); padding-top **12→6px** (computed box-model); title fs **32.928→25.888px** (real token step display-2→display-1). Census RED was "contraction ENTIRELY ABSENT, Δ0, not even sticky." @1440 dormant-by-design (desktop `lg:overflow-visible`, no scroll surface — owner mandate is "ON scroll"). | **BR-9 PASS** (real box shrink) · **BR-11 PASS** (condensed title 25.888px = `--type-display-1` clamp min = the named legibility floor; NOT a compositor scale of the un-stepped rung) |

The two RED re-cures are the **U-F9 family's two owner-verbatim arms** — the §2 at-rest rhythm/seam
and the §0.8 whole-header scrolled contraction. The census had routed u-f9-scrolled to "PRODUCER-GATED
→ coordinate with U.W-ADOPT (no demo-unilateral landing available, no sticky condensed strip today)";
the header lane **found the demo-unilateral door** (a real sticky short strip whose layout box
actually shrinks — Pole A of the wave-doc contraction bracket) and landed it, so the wave did not have
to wait on the producer. The P3 `ScrollCardHeader` producer primitive remains the **booked long-term
home** (§4 RELAY), absorbing the demo strip at glass-ui 5.0.0+.

### 7 ANNEX-OWNER-ATTEST — OPEN FOR THE OWNER (real-GPU / owner-attested / non-macOS frame required — the U-F54 law)

No annex row was fake-judged from a headless assertion. Each names its dependency.

| Row | Family | Headless leg REPORTED (GPU-independent) | The owner-attest dependency (U-F54) |
|---|---|---|---|
| **u-f5** (T-30/T-49) | U-F5 | canvas SEAT box unchanged from T.W8, inside the designed transparent **1.6× overscan budget** (crosses card-content +55/+49, card-edge +34, viewport +18@390). | **WebGL-blob perceptibility** — visible-bead containment (OA-4). Producer GAP-L5/revealBloom → ADOPT. |
| **u-f11** (T-37) | U-F11 | `useAtmosphere.ts` `DERIVE_SEAM_FLOOR=0.06` + `guaranteeSeamOffset()` → **ΔL ≥ 0.06 OKLab (~3× JND) provable at any seed** (pure JS pre-upload); composite dE2000 **8.19–13.03 ≫ 2.3**. | **WebGL atmosphere-field gestalt** — the "reads as a deliberate derive-seam" read; the field is a WebGL canvas (`gl.readPixels` cleared). |
| **u-f8** (T-54/T-55) | U-F8 | each `.generate-swatch` is a `watercolor-swatch` (`feTurbulence`/`feDisplacementMap` + organic `border-radius`, rect→WatercolorDot ✅); verb register 1 primary + 2 quiet icon-buttons (WR-7). | **SVG-filter organic-edge material + one-verb-instrument register** (OA-2, owner-attested by construction). Producer register → RELAY. |
| **u-f10** (T-34/T-50) | U-F10 | veil α **light 0.443 (lowered) / dark 0.56 (held, `brightness(1.16)`-compensated)**; clarity window `blur(8px) saturate brightness` ACTIVE both schemes; backdrop near-uniform warm (C≈0.014–0.02). | **GPU-composited backdrop-filter veil translucency** (OA-3, explicit O-14 warning). Veil primitive → RELAY. |
| **u-f12** (NEW) | U-F12 | dark `--card` = UNIFORM neutral warm-brown (C **0.0216**, hue spread **0°**); accent lane `--accent-view` seed-tracked (hue 30/190/270/310, spread 160°, C 0.055–0.08 ≥ floor). **Pole B (C3-compliant) met as-built; Pole A at its RED pole (needs C3 ledger amendment).** | **owner-ruling bracket — Pole A/B UN-PICKED** + the clean/muddy read (OA-4, BR-7 pole-conditional). NO pole picked by the census or this wave. |
| **u-f7** (T-14/T-48/T-58) | U-F7 | both confounds ABSENT/ISOLATED: PKT-1 dist-clock sane (`.15s`, swap uses `vj-enter @ .3s`); O-16-R1 150ms clobber has zero literals + lives in the stubbed `useLayerTransition`. Headless rAF deltas confound-flagged (uninterpretable — U-F54 3rd demonstration); wake-gray cure structurally intact (`HeroBlob.vue onActivated` re-seed). | **real-GPU swap-window frame** (OA-1, the T-58 owner-escalated mandate) + the wake-gray must-not-regress from the landed boot-B `5cecdf2`. Producer confounds → relayed. |
| **zd3-microchrome** | (never-filed W8 pass) | two decisive sub-items cured-GREEN: `::selection` bg = `--accent-live @32%` + `--foreground` ink (no default blue); cursor grammar per-role. **PLUS the rider halves landed this wave** (§3): dynamic `<title>` TRUE + `.scrollbar-thin` consumers > 0. | **non-macOS scrollbar chrome frame** (`.scrollbar-thin` visible-track render) + native-`title` tooltips (producer-relay) + the static-title owner-eye defensibility. |

### 7 RECORDED-ELSEWHERE — VISUAL-adjacent, owned by a named wave / the producer (not judged live; zero silent drops)

`static-t39` (T-39 Q14 perf face) → U.W-PERF (U-F61) / U.W-CLOSE · `static-t20` (T-20 Tabs pilling) →
U.W-ADOPT (BH relay `17e0f522`, PRODUCER-DECLINED) · `static-producer-relay` (GAP-L5/revealBloom ·
PKT-1/O-16-R1 · T-52 mask · P3 ScrollCardHeader · WatercolorDot/verb · veil primitive) → U.W-ADOPT /
BH relay · `static-uf6-oracle` (O-14 proxy-oracle law) → U.W-ORACLE (U-F62, CLOSED) · `static-ab1`
(About-KaTeX boot seam) → U.W-CANON (U-F19 AB-1 RETIRE-census) · `static-zd3-admin` (admin never-filed
pass + thrown-error a11y half) → U.W-A11Y (U-F56 · U-F58) · `static-t5-letterrail` (T-5 P5/S-3
letter-rail) → U.W-ADOPT (BH relay, PRODUCER-GATED — distinct from the u-f9-rail active-seat arm).

---

## §2 · THE GATE ROW TABLE — the independent gate (merged `be807ce` built gh-pages @ :8599, dpr2, both schemes)

The gate was assembled by a NON-authoring integrator agent (E-6 lesson-12: the owner is the terminal
taste authority; a gate VERDICT is "coherent with the doctrine + owner's word," never "beautiful").
Serve provenance: `dist/gh-pages` from the MERGED tree (lane-1 header + lane-2 riders).

| Gate | Register | Family | Verdict | Evidence |
|---|---|---|---|---|
| **BR-4** | born-GREEN must-STAY guard | U-F9 | **PASS** | title fs @1440 **53.28px** / @390 **32.928px** = `--type-display-2`, `position:sticky`, both schemes; never the 67.776 un-stepped. Unchanged from census baseline. |
| **BR-5** | born-RED → GREEN | U-F9 | **PASS** | one cqi rhythm law: **6× divergence → 1.4×**; the 390 dead band absorbed to the 5.0px rowGap token; readout-seam T-33b oracle confirms designed air. |
| **BR-9** | born-RED → GREEN | U-F9 (scrolled) | **PASS** @390 (dormant-by-design @1440) | whole-header **REAL box shrink Δ-96.66px** + padding **12→6px** + title token step **32.928→25.888** condense as ONE strip (frame `c-{light,dark}-390x520-scrolled.png`). |
| **BR-11** | born-RED → GREEN | U-F9 (scrolled) | **PASS** | condensed title **25.888px = 1.618rem = `--type-display-1` clamp min** = the wave-doc named legibility floor; real token step, not a compositor scale (the T-42 "text too small" below-bound). |
| **rider: dynamic `<title>`** | born-RED → GREEN | zd3-microchrome | **PASS** | `document.title` dynamic = TRUE; 3 live titles observed (`oklch(…) — Color Picker`, `tomato · Palettes — Color Picker`, `rebeccapurple · Gradient — Color Picker`); static was "Color Picker". |
| **rider: `.scrollbar-thin`** | born-RED → GREEN | zd3-microchrome | **PASS** | `.scrollbar-thin` consumers **> 0** (≥1 live computed `scrollbar-width:thin`: atmosphere `ConfigSliderPane`; gradient `GradientCodeEditor` gates on interaction; 5 consumers wired). |
| **ANNEX ×7** | owner-attest (U-F54) | u-f5·u-f11·u-f8·u-f10·u-f12·u-f7·zd3-microchrome | **honestly OPEN** | not the gate's to flip; perceptual/GPU/non-macOS halves carry to the owner. |

**Suites at the gate**: typecheck **0** (vue-tsc lib + demo) · lint **0** (eslint `--max-warnings=0`) ·
`npm test` **84 files / 2312 tests ALL PASS** · e2e (smoke) **15/15 header-adjacent oracles PASS**
(o10-type-locks 6 · o11-header-gates 6, incl. gate 6 now sanctioning `.picker-header` sticky + gate 4
compositor-only pane keyframes still flat · readout-seam T-33b 3, "reserved-line band is designed air,
never a dead band").

**Integration**: lane-1 `worktree-wf_237d5120-d5a-1` (U-F9 header: `ColorPicker.vue` +
`useHeaderCondense.ts` + `header.css` + o11 gate + π frames) and lane-2 `worktree-wf_237d5120-d5a-2`
(zd3-microchrome riders: `useDocumentTitle.ts` + `router/index.ts` + `.scrollbar-thin` ×5 consumers)
both clean auto-merged to `tranche-u` (ColorPicker regions disjoint), pushed `8a95a26..be807ce`;
lane-1 unique = 0 · lane-2 unique = 0 (both fully absorbed); both lane worktrees removed + branches
deleted.

---

## §3 · THE §Dispositions WALK — each family → its terminal state

Walking `waves/U.W-VISUAL.md §Dispositions` row-by-row; every family reaches a named terminal state,
zero silent drops.

| Family | Wave-doc disposition | TERMINAL state (after census + this wave) |
|---|---|---|
| **U-F5** blob-card-seat | build | **ANNEX-OWNER-ATTEST** (row u-f5). Seat box unchanged, inside the 1.6× overscan budget; visible-bead containment = WebGL perceptibility (OA-4/U-F54). Producer GAP-L5/revealBloom → ADOPT. |
| **U-F6-ramp** q5-ramp-register | build (oracle-half → U.W-ORACLE) | **CENSUS-GREEN — RETIRED** (row u-f6-ramp). BR-3 per-stop L + WCAG contrast, nearBlackSink=false; A-class resolver defect retired. Oracle-half U-F62 → U.W-ORACLE **CLOSED**. Aesthetic → OA-4. |
| **U-F7** scene-transition-motion | build (real-GPU annex) | **ANNEX-OWNER-ATTEST** (row u-f7). Confounds ABSENT/ISOLATED; OA-1 real-GPU swap-window frame owed (T-58 mandate) + wake-gray must-not-regress. Producer confounds → relayed/booked. |
| **U-F8** generate-plate-species-chrome | build (RELAY) | **ANNEX-OWNER-ATTEST** (row u-f8). Countable legs GREEN (WatercolorDot swatch + 1 verb + 2 quiet); OA-2 SVG-filter perceptibility. Producer register → **RELAY (§4)**. |
| **U-F9** picker-header-spacing-regime + scrolled arm | build | **BOTH CENSUS-RED RE-CURED GREEN** (rows u-f9-atrest → BR-5 · u-f9-scrolled → BR-9/BR-11). BR-4 guard STAYS. The demo-unilateral whole-header strip landed; P3 ScrollCardHeader (Pole B) → **RELAY (§4)** as the long-term home. OA-6 "one strip" gestalt → owner. |
| **U-F9-rail** picker-active-rail-seat | build-if-CENSUS-RED | **CENSUS-GREEN — RETIRED** (row u-f9-rail). BR-12 seat ≡ active rung (0,0) offset × 16 cells. OA-7 coherence → owner. T-5 letter-rail 2nd residual → U.W-ADOPT. |
| **U-F10** console-veil-material | build (RELAY) | **ANNEX-OWNER-ATTEST** (row u-f10). α calibration reported (light 0.443 / dark 0.56); clarity window active; OA-3 GPU backdrop-filter. Veil primitive → **RELAY (§4)**. |
| **U-F11** collapsed-dock-swatch-seam | build (AUTOMATIC — uncured by W8) | **ANNEX-OWNER-ATTEST** (row u-f11). DERIVE_SEAM_FLOOR=0.06 GPU-independent, dE2000 8.19–13.03; the "reads as a seam" gestalt is a WebGL-field read → OA/U-F54. |
| **U-F12** dark-scheme-derived-tint-muddiness | build (owner-judgment-at-formation) | **ANNEX-OWNER-ATTEST · owner-ruling bracket, Pole A/B UN-PICKED** (row u-f12). Pole B (C3-compliant) met as-built; Pole A at its RED pole (needs C3-ledger amendment). NO pole picked; the pole + clean/muddy read are the owner's (OA-4). |
| **U-F13-demo** dock-edge-clip | build (demo half; producer → relay) | **CENSUS-GREEN — RETIRED** (row u-f13-demo). Tools true-button INTERIOR, ring within content box; `margin-inline:4px`. Producer clip/fade/mask honesty (T-52) → **RELAY (§4)** / ADOPT. |
| **U-F13-band** dock-expand-scene-reflow | build-if-CENSUS-RED | **CENSUS-GREEN — RETIRED** (row u-f13-band). BR-10 band 72.00px INVARIANT, scene shift Δ0.00px; `--dock-band-min-h = 72px ≥ 71.4px expanded pill`. π/DELTA +3.0px → 0px. |
| **U-F13-menuink** dock-nav Q10 accent-ink | build-if-CENSUS-RED (LANDED `ccf4b30`) | **CENSUS-GREEN — RETIRED (computed-only)** (row u-f13-menuink). BR-14 accent re-homed to the ink `color` channel, C 0.15/0.0804 ≥ floor, opacity 1. NO OA arm (the O-14 green-over-broken trap → computed teeth mandatory). |
| **U-F19 residue** T-53 dark caster | build-if-CENSUS-RED | **CENSUS-GREEN — RETIRED** (row u-f19-t53). BR-13 `.dark --shadow-color` = designed dark stone ≠ foreground cream; α ≤ floor. OA-5 → owner. AB-1 KaTeX → U.W-CANON (RETIRE-census). |

**Never-filed W8 passes (the ZD-3 close slot)**: **zd3-easing** → CENSUS-GREEN (retires-with-the-O-17-cite);
**zd3-microchrome** → ANNEX (perceptual roster) + the two headless rider halves LANDED this wave;
**admin** → U.W-A11Y (U-F56) driven-live (not this census). Every T.W8 surface had a slot.

---

## §4 · THE RELAY (E-2) — the producer-material addendum, dispatched to the ACTIVE glass-ui BI inbox

Per E-2 (THE RELAY EDICT — every glass-ui-level change lands in the active inbox, a fond), a
path-scoped single-file addendum landed at the ACTIVE glass-ui inbox
`../glass-ui/docs/tranches/BI/coordination/valuejs-inbox-2026-07-13-u-w-visual.md`, supplementing
(never superseding) the u-formation communiqué (`17e0f522` / `BH`) and the U.W-LIB invariant addendum.
M1 dispatch rule: the value.js-side record IS the gate; an ack is a bonus, never waited on.

- **(a) The whole-header contraction REFERENCE landed demo-side** (T-61/§0.8): the demo-unilateral
  sticky condensed strip (`useHeaderCondense.ts` + `header.css`) is the reference contraction the owner
  mandated. The **P3 `ScrollCardHeader` producer primitive remains the booked long-term home** — the
  demo strip is Pole A; at glass-ui 5.0.0+ the producer door (Pole B) absorbs it. **The absorption
  seam noted**: `useHeaderCondense.ts` (the scroll-driver + `condensed`-class + token step) is the demo
  counterpart the P3 primitive replaces at adopt — the knobs are the padding + background-band + title
  co-contraction, keyed off the same sticky scroll signal.
- **(b) The ANNEX producer-material rows riding their cut** (re-cited from `census.md`, no second book
  for the already-dispatched): **veil primitive** (U-F10, `surface="veil"` material — glass+blur;
  calibration levers α/clarity-window named) · **WatercolorDot register + verb-cluster instrument**
  (U-F8; dock-set-capsule vs `ui/dropdown-menu` bracket — owner's word) · **T-52 dock-layer mask
  honesty** (U-F13-demo producer half — ALREADY DISPATCHED communiqué §2b, cited by name) ·
  **GAP-L5/revealBloom** (U-F5 goo-blob `settled` seam + reveal-bloom door — ALREADY DISPATCHED §2b) ·
  **PKT-1/O-16-R1** (U-F7 swap-window confounds — ALREADY DISPATCHED §2b, booked U.W-ADOPT).

---

## §5 · MISSES (PP-16 — named, none a VISUAL defect)

1. **The 7 ANNEX-OWNER-ATTEST rows remain honestly OPEN** (u-f5 · u-f11 · u-f8 · u-f10 · u-f12 · u-f7 ·
   zd3-microchrome). By law U-F54 these are real-GPU / owner-attested / non-macOS-frame judgments the
   gate MUST NOT fake-flip from a headless assertion. Each names its dependency; the headless legs are
   REPORTED GREEN where GPU-independent (α, chroma, ΔL, seat geometry, seam floor). The gestalt sign-off
   (OA-1..OA-7) and the u-f12 Pole A/B pick are the owner's.
2. **The gh-pages substrate break** (routed to U.W-ADOPT / U.W-LIB, NOT a VISUAL defect): `npm run
   gh-pages` FAILS at raw committed `tranche-u` HEAD because the pinned glass-ui 5.0.0 (`2e559f7a`) and
   the live sibling import pre-rename value.js names U.W-LIB's `{from}2{to}` sweep DROPPED (bare
   `srgbToOKLab`/`oklabToLinearSRGB`/`oklabToRgb255`/`rawOklabToOklch`/`rawOklchToOklab`; parsing
   `parseCSSSubValue`). This is a value.js↔glass-ui-5.0.0/keyframes producer/consumer export-contract
   break — substrate territory, orthogonal to the VISUAL cures (**the VISUAL merge touches NO
   color/parse export**). Certification used an UNCOMMITTED compat re-export shim (the census-scaffold
   pattern) to unblock the local serve only; reverted, never committed.
3. **OA-6 legibility floor (the T-42 second axis)**: BR-11 machine-confirms the contracted title is
   `--type-display-1` (the named floor), but "reads legible, not too small" is owner-terminal — carried
   as an OA-6 owner-attest alongside the "reads as ONE strip" gestalt.

---

## §6 · HAND-FORWARD — U-F76 mount SETTLED → U.W-A11Y then U.W-PERF

U.W-VISUAL is the **HEAD single-writer of the shared picker/readout mount** (U-F76: VISUAL → A11Y →
PERF). The two mount-changing edits this wave landed — the WR-4 title step-down footprint (title y
166→151.5@1440; blob y 119→104.7) and the whole-header condense strip — **settle the mount box HERE**.
The mount is now handed forward:

- **→ U.W-A11Y** (target-size / `aria-valuetext` over the SAME settled mount): **U.W-VISUAL CLOSE
  UNBLOCKS U.W-A11Y** — the shared picker-mount single-writer has gone first; A11Y may now reseat
  nothing structural, only harden the settled geometry.
- **→ U.W-PERF** (after A11Y): re-measure the picker/readout **CLS budget** against the post-WR-4 +
  post-condense footprint (a reseat re-opens CLS, so PERF reserves over the SETTLED box).
- **The real-GPU visual annex attest → U.W-CLOSE** (U-F54 / U-F61): the 7 ANNEX rows' owner-attested
  frames are flagged in the close ledger as attested-not-headless-verified.

---

**Precedence chain** (restated): the owner's verbatim `MANDATE §0.8` (T-61 whole-header contraction) →
`audit/registry.md §2/§16/§21/§26` → `U.md` → `waves/U.W-VISUAL.md` → this roll-up. No row here
contradicts a §12 ruling or a §4 retirement. Zero silent drops.
