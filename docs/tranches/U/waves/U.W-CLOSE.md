# U.W-CLOSE — CLOSE: zero-drop ledger walk · book re-probe · publish presentation · real-GPU annex attest

**Wave**: U.W-CLOSE · **designHeavy**: no (structural — the terminal close ceremony: precise
completeness gates, born-RED where the close-work is un-done, mechanical rigor; the ONE visual
obligation rides the U-F54 real-GPU/owner-attested annex, not a frontend-design loop) ·
**Owned family**: U-F61 (`single-sourced-claims` — the attested-not-verified flags, **fold**).
**Discharges here** (owned elsewhere, attested/presented at close): U-F54 (the real-GPU visual
annex — owner-attested) · the library-correctness PUBLISH decision (LIB-authored, ADOPT-sequenced,
owner-held §13.5) · the whole-tranche zero-drop DISPOSITION-LEDGER walk (E-4's completion proof).

**Opens after**: **ALL nine waves** (U.W-LIB · U.W-ADOPT · U.W-VISUAL · U.W-A11Y · U.W-PERF ·
U.W-CANON · U.W-SEC · U.W-ORACLE · U.W-DEMO). CLOSE is the terminal DAG sink — it cannot walk the
ledger, re-probe the books, or present the publish decision until every wave has reached its
decided state (build landed / booked / escalated / retired). Two upstream waves are
**trigger-/terminal-gated** and hand CLOSE a residual by contract: U.W-ADOPT floats on the
owner-gated glass-ui 5.0.0 cut (USER-GATED, UNFIRED at authoring), and U.W-VISUAL is
W8-terminal-dependent (scoped once T.W8 CLOSES). CLOSE absorbs both residuals BY NAME — never
re-booking an UN-DECIDED family (the disease-row law), only recording legitimately trigger-gated
executions as `complete_with_misses` (PP-16).

**The spec of record is `../audit/registry.md`** (§16 U-F61 the single-sourced claims · §16 U-F54
the real-GPU-oracle-never-run acknowledgement · §13.5 the owner ruling / owner-held publish · §8 the
zero-drop WINS · §21 the demonstrated headless false-red risk · §28 + §28.3 the RELAY discharge) +
`../DISPOSITION-LEDGER.md` (the row-by-row walk this wave executes). Where this doc and the registry
could diverge, **the registry wins**; above BOTH, **the owner's verbatim wins** (§13.5). Precedence:
owner verbatim → registry → `U.md` → this wave doc.

---

## §Goal criterion

The tranche CLOSES with a **`FINAL.md` that walks the `DISPOSITION-LEDGER.md` row-by-row with zero
silent drops** — every one of the 77 U-Fxx families, every embedded chronic/disease class, every
prompt-recap row (owner asks T-1..T-61 + edicts E-1..E-7), and the retired-WINS set reaches its
DECIDED terminal state (build LANDED / build BOOKED-as-`complete_with_misses` with a named external
gate / escalate DELIVERED-as-structural-fact / fold LANDED-in-its-sibling / retire RECORDED) with a
one-line evidence cite. Every **booked item** from the nine waves' `§BOOKS` sections is **re-probed
LIVE** at close (not asserted from the wave doc) and its outcome recorded. The **U-F61
attested-not-verified claims** (X2 NCSU-301, the CI TBT red, the born-RED cure-ownership register,
the deploy-webhook repair) are each **explicitly FLAGGED** in the close ledger with their true
verification status — never laundered into a false-green. The **library-correctness PUBLISH decision**
(the version cut, semver-loaded, §13.5) is **PRESENTED to the owner WITH the landed fix**, never
taken unilaterally. The **real-GPU visual annex** (U-F54: the §2 owner-eye still-reds + U-F7
scene-transition) is **owner-attested** against a real-GPU or owner frame — NEVER a headless
assertion.

## §Completion criterion

- **`FINAL.md` exists and its zero-drop walk is GATE-VERIFIED** (G-CLOSE-1): a completeness
  assertion confirms every ledger row maps to a `FINAL.md` disposition-with-evidence; a row without
  one is RED. The ledger's `Family audit` invariant (F1..F77 each exactly once; F6/F55 split-homed;
  F18/F19 → the W8-inheritance census; F20/F59/F60 retired; F54 the annex) is machine-confirmed.
- **Every booked item is re-probed LIVE and its outcome recorded** (G-CLOSE-2) — the master book
  register (§BOOKS below) is walked; each entry carries a discharge outcome (LANDED / STILL-BOOKED
  with the named external gate / RETIRED-on-re-probe).
- **The U-F61 four single-sourced claims are each FLAGGED attested-not-verified** (G-CLOSE-3) with
  status; X2 and U-F41 cross-reference; no attested claim is presented as verified.
- **The real-GPU annex is owner-attested** (G-CLOSE-4) — the §2 still-reds (U-F5..F13) + U-F7 carry
  owner-attested before/after frames, NOT headless assertions. Where the owner has not attested
  in-window, the still-red is recorded `complete_with_misses` with the frame obligation named — never
  asserted green from a headless run (the registry logged TWO demonstrated headless false-reds
  against exactly these surfaces — the U-F4 `overture.css` red herring + the U-F13 PRM confound, §21).
- **The publish decision is presented WITH the landed fix** (G-CLOSE-5) — the LIB-authored,
  ADOPT-sequenced publish packet (the reshape's semver classification + the both-`^3.1.0`-floor
  co-land plan + the confirmed-preserved-or-co-migrated FOUR consumer surfaces) is handed to the owner
  for the version-cut decision; §13.5 forbids the unilateral cut.
- **PP-16 binds**: gates-pass-goal-unmet closes `complete_with_misses`. The named residuals (the
  trigger-gated glass-ui cut, the VPN-gated NCSU re-verify, the deploy-dependent header/impersonation
  confirmations, the owner-un-attested annex frames, an un-taken publish decision, the U-F3 Q14
  GREEN-or-escalation) do NOT block close — they are recorded, each with its external gate named. No
  UN-DECIDED family rides to a successor tranche (re-booking is forbidden).

---

## §Scope — the work this wave builds (each with its APPROACH; E-3 binds — no workaround, no legacy)

CLOSE is not a defect-fix wave: it is the terminal close ceremony that PROVES the tranche's
zero-silent-drop contract, discharges the deferred books, flags the honest-caveat claims, presents
the semver-loaded publish decision, and attests the real-GPU visual annex. Its ONE owned family is
U-F61 (fold); its build-work is the close instrumentation (the ledger-walk gate + `FINAL.md`), which
is E-3-idiomatic (a regenerable completeness assertion over the ledger, not a hardcoded checklist —
the LoC-precept pattern the codebase already uses for `as unknown as` counts).

### The zero-drop DISPOSITION-LEDGER walk · **build — E-4's completion proof**

**Evidence** (`../DISPOSITION-LEDGER.md` header + `Family audit` + `Ledger completeness statement`;
U.md §Completion criterion; registry §The registry law "re-booking is forbidden"): the ledger's own
audit invariant is *rows = 77 families + the CHRONIC set + the PROMPT-RECAP set + the RETIRED-WINS
set*. E-4 is discharged by the LEDGER; CLOSE proves the discharge held through execution.

**APPROACH**: author `FINAL.md` as the row-by-row walk. For EACH ledger row, record: the family/row
ID, its owning wave, its disposition (build/fold/retire/escalate), and its TERMINAL evidence
(commit/gate-artefact for a landed build; the sibling remediation cite for a fold; the recorded
rationale for a retire; the structural-fact document for an escalate; the named external gate for a
booked `complete_with_misses`). The walk is GATE-BACKED (G-CLOSE-1) so a dropped row is RED, not a
prose omission. The ledger walk absorbs the two terminal-dependent censuses BY NAME: (a) the
**W8-terminal inheritance** (U-F18/U-F19 — the still-reds T.W8's remediation did NOT land, routed
T-56→U-F6, error-detail→U-F26, T-53/AB-1→U.W-VISUAL/U.W-CANON, the 8 producer reds→U.W-ADOPT relay);
(b) the **trigger-gated adopt residual** (U-F2 DECIDED, the cut execution booked if the glass-ui
5.0.0 trigger did not fire in-window).

### U-F61 — `single-sourced-claims` · **fold (the close ledger FLAGS each attested-not-verified)**

**Evidence** (registry §16, B): four claims are single-sourced / network-unverified across all
rounds and MUST be flagged, never presented as verified —
1. **X2 NCSU-301** (VPN-gated, unverified both rounds; §8 zero-drop WINS records X2's on-host claim
   "verified via close artefacts only"; **ties U-F41** — the `mbabb.fi.ncsu.edu/colors/` alias, §12).
2. **The CI TBT red** (a pure ledger claim — local runs UNDERSTATE it; the 2-core CI runner is the
   only place TBT reddens; §10 R-2 "TBT 187ms local, only reddens on the 2-core CI runner"; **ties
   U-F3**).
3. **The born-RED cure-ownership register** (the 3 armed `test.fail()`: O-16 clobber · O-26 aurora ·
   O-5 boot — cure waves un-landed at audit, O-26 can never flip on software-GL; §13 U-F42; **ties
   U.W-ORACLE**).
4. **The deploy-webhook repair** (attested from close artefacts, network-unverified).

**APPROACH** (fold — no independent gate beyond the flag): `FINAL.md` carries an **§attested-not-verified**
section that names each of the four with its TRUE status at close (verified-live / still-attested /
retired-on-re-probe), cross-referencing U-F41 (X2), U-F3 (TBT), U.W-ORACLE (cure-ownership). This is
the anti-close-class-lie discipline the charter demands: a green CI proves almost nothing (U.md
thesis) — the close records what was measured versus what was attested, honestly. G-CLOSE-3 gates it.

### U-F54 — the real-GPU visual annex · **attest here (owner-attested; home = charter §real-GPU annex)**

**Evidence** (registry §16 U-F54, A; §21 the demonstrated headless false-red risk; charter §The
real-GPU annex): the headed-GPU oracle slate (O-1..O-26 live legs — the tranche-T core deliverable)
was NEVER executed across all seven audit rounds (headless forces SwiftShader → the static CSS
placeholder → migration ≈ 0 forever; O-3's chroma assertion SKIPS on software-GL). U-F54's
disposition is **acknowledge** (its home is the charter annex, not this wave); CLOSE **discharges the
attestation**.

**APPROACH**: the annex is **owner-attested** (or a headed-CI lane if one is stood up). At close, the
§2 owner-eye still-reds (U-F5..F13, from U.W-VISUAL's post-W8-terminal row-set) + U-F7
(scene-transition, thrice-red, MANDATE-escalated T-58) carry **owner-attested before/after π-frames**
(§π/DELTA). CLOSE does NOT chase the annex further in the headless env and does NOT assert a §2
still-red green from a headless run (G-CLOSE-4 born-RED against a real-GPU/owner frame). Un-attested
in-window ⇒ recorded `complete_with_misses` with the frame obligation named.

### The library-correctness PUBLISH presentation · **present WITH the landed fix (owner-held §13.5)**

**Evidence** (registry §13.5 the owner ruling; §26 W-lib-correctness "owner-held publish"; U.W-LIB
§BOOKS "The PUBLISH / version-cut decision → U.W-ADOPT (U-F77)"; U.W-ADOPT §Goal "the
library-correctness version cut is owner-held and sequenced against BOTH `^3.1.0` peer floors"; U.md
§Completion "the library-correctness publish decision is owner-held and taken WITH the landed fix"):
U-F29's amelioration reshape is semver-loaded against glass-ui's + keyframes' `@mkbabb/value.js
'^3.1.0'` peer floors; the version cut is OWNER-HELD, never unilateral.

**APPROACH**: LIB authors the fix + the publish packet (semver classification + both-floor co-land
plan); ADOPT sequences the version decision against both peer floors + the FOUR-surface
convention co-migration (§28). CLOSE is the close-ceremony locus where the **assembled packet is
PRESENTED to the owner WITH the landed fix** for the final version-cut decision (G-CLOSE-5). This
discharges the U.md completion criterion "the publish decision is owner-held and taken WITH the
landed fix" — the owner sees the reshape, the four preserved-or-co-migrated consumer surfaces, and
the both-floor plan together, then rules. §13.5 binds: the cut is not taken without this presentation.

---

## §Hard gate (born-RED where the close-work is un-done — registry-cited; the ONE visual gate against a real-GPU/owner frame)

Five born-RED gates. Each is **ARMED now** (the close-work is un-done at authoring — `FINAL.md`
absent, the books un-discharged, the claims un-flagged, the annex un-attested, the publish
un-presented) and flips only at close-execution. G-CLOSE-4 gates against a **real-GPU/owner-attested
frame, NEVER a headless assertion** (the U-F54 annex; the registry logged TWO demonstrated headless
false-reds against exactly these surfaces, §21).

### G-CLOSE-1 — the zero-drop ledger-walk gate (E-4 completion proof) · **born-RED**

A completeness assertion (a `test:dist`-class script, regenerable — the LoC-precept pattern, NOT a
hardcoded checklist) that: (a) parses `DISPOSITION-LEDGER.md §A/§B/§C/§D` to the row set; (b) parses
`FINAL.md` to the disposition-with-evidence set; (c) asserts EVERY ledger row has a `FINAL.md`
disposition + a terminal-evidence cite; (d) re-confirms the ledger's own `Family audit` invariant
(each integer 1..77 present exactly once; F6/F55 split-homed; F18/F19 → W8-census; F20/F59/F60
retired; F54 the annex). **RED today** (`FINAL.md` absent — no wave has closed; the walk is
un-executed). **Flips GREEN** only when `FINAL.md` walks all rows with zero drops. This is the
born-RED that guards the tranche's core zero-silent-drop contract.

### G-CLOSE-2 — the book re-probe gate · **born-RED (each booked item live-re-probed)**

An assertion that every entry in the master book register (§BOOKS) carries a **live re-probe
outcome** recorded in `FINAL.md`, not an inherited wave-doc claim. **RED today** (the books are
un-discharged; the waves that book them have not closed). The re-probe is LIVE per the
probe-parsimony edict (a purpose-built script per book, compact summary — never a raw dump). The
enumerated books (each flips its own arm GREEN on discharge, or stays STILL-BOOKED with its named
external gate):

| # | Book (source wave) | Live re-probe at close | Green condition |
|---|---|---|---|
| B1 | **U-F28 keyframes next-tag retire** (ADOPT) | `grep`/read `../keyframes.js` for a tag past `5.2.0` | tag cut → RETIRE; else STILL-BOOKED (WATCH, zero value.js kf imports) |
| B2 | **glass-ui 5.0.0 cut-execution residual** (ADOPT, U-F2) | did the owner-gated cut fire? (`grep -c 'ref: tranche/BG'` → 0; lock ≡ 5.0.0; boot-smoke green) | cut fired → LANDED; else `complete_with_misses` (trigger-gated, DECIDED, NOT re-booked un-decided) |
| B3 | **U-F3 Q14 LCP adjudication** (PERF / W9 close gate) | LHCI mobile LCP vs budget on the settled geometry | GREEN (producer cut landed in-window) OR the escalate structural-fact document handed (§13.5-class, never a gate re-baseline — Q14 verbatim) |
| B4 | **U-F76 settled picker/readout mount settle-guard** (PERF) | the standing CLS/LCP reservation over the SETTLED (reseated) box is a live guard | the settle-guard fires on a mount-box regression (not a one-shot measurement) |
| B5 | **U-F22 barrel-parity + U-F64 size-graph gates** (CANON) | the two STANDING tooling gates are live guards | each RED-on-regression, GREEN on the current tree (a live guard, not a landed one-shot) |
| B6 | **U-F42 born-RED cure-ownership register** (ORACLE) | each of the 3 armed `test.fail()` (O-16/O-26/O-5) flipped or is annex-booked | O-16/O-5 flip on cure; O-26/O-3 ride the headed-GPU annex (can never flip on software-GL) → G-CLOSE-4 |
| B7 | **the CI-teeth soft-posture decision** (ORACLE, U-F1/F55-CI) | did the owner rule the soft posture stands, or is the slate blocking? | the decision is RECORDED (blocking-promotion OR an explicit owner soft-posture ruling) |
| B8 | **U-F54 / O-26 · O-3 headed-GPU annex attestation** (ORACLE, VISUAL, ADOPT) | owner-attested frames present | → G-CLOSE-4 (owner-attested, NOT headless) |
| B9 | **G-DEMO-1 · G-DEMO-3 standing import-boundary ESLint gates** (DEMO) | the two ESLint boundary gates are wired + still guard a future re-inversion | each RED-on-re-inversion, GREEN on the current tree |
| B10 | **the two adjacent shared-tree → app-root up-imports** (DEMO, design-loop judgment) | relocate-or-ratify per instance surfaced (not silently dropped) | each recorded (relocated OR ratified-with-rationale) |
| B11 | **U-F41 NCSU-origin on-wire re-verify** (SEC → U-F61) | VPN-gated live probe (301→api.color.babb.dev or dead) | → G-CLOSE-3 attested-not-verified (VPN-gated; ties X2) |
| B12 | **U-F39 live security-header confirmation** (SEC → U-F61) | live `curl -I color.babb.dev` post-deploy for CSP/HSTS/X-Frame-Options | deploy-landed → LANDED; else deploy-dependent, flagged (never asserted green pre-deploy) |
| B13 | **U-F36 impersonation + U-F38 token-at-rest live confirmations** (SEC) | drive the deployed auth middleware post-deploy | deploy-landed → LANDED; else deploy-dependent, flagged |
| B14 | **the library-correctness PUBLISH decision** (LIB→ADOPT, U-F77) | the assembled packet + landed fix | → G-CLOSE-5 (owner-held §13.5) |

### G-CLOSE-3 — the U-F61 attested-not-verified flags · **born-RED (each claim explicitly flagged)**

An assertion that `FINAL.md` carries an **§attested-not-verified** section naming EACH of the four
single-sourced claims (X2 NCSU-301 · CI TBT red · born-RED cure-ownership · deploy-webhook) with its
TRUE status and cross-reference (X2↔U-F41, TBT↔U-F3, cure-ownership↔U.W-ORACLE). **RED today** (the
section is absent). A claim presented as verified without a live probe is a close-class lie — the
gate forbids it. Flips GREEN when all four are flagged honestly.

### G-CLOSE-4 — the real-GPU visual annex attest (U-F54) · **born-RED against a real-GPU/owner-attested frame (NEVER headless)**

The §2 owner-eye still-reds (U-F5..F13, from U.W-VISUAL's post-W8-terminal row-set) + U-F7
(scene-transition, T-58 MANDATE-escalated) carry **owner-attested before/after π-frames**. **RED
today** (the annex was NEVER run in seven rounds; the frames are un-attested). **Flips GREEN** only
on an owner attestation (or a headed-CI lane frame) — NOT a headless assertion (the registry logged
TWO headless false-reds against exactly these surfaces: the U-F4 `overture.css` red herring + the
U-F13 PRM confound, §21). Un-attested in-window ⇒ `complete_with_misses` with the frame obligation
named, NEVER a false-green.

### G-CLOSE-5 — the publish-presentation gate · **born-RED (present WITH the landed fix, owner-held §13.5)**

An assertion that the library-correctness PUBLISH packet is **PRESENTED to the owner WITH the landed
fix** — the reshape's semver classification + the both-`^3.1.0`-floor (glass-ui + keyframes) co-land
plan + a confirmation that the U.W-LIB build-time re-enumeration born-RED drove **ALL FOUR**
convention-sensitive consumer surfaces provably preserved-or-co-migrated (registry §28: glass-ui
`parseCSSColor` + glass-ui direct `mixColors`/`sampleColorRamp` [spectrum-walk] + keyframes
`parseCSSValue`×3 + keyframes direct `sampleColorRamp`/`color2` [backward-color] — §28 AMENDED R6's
"three" to FOUR; **the registry wins: all four**). **RED today** (the fix is un-landed at authoring,
the packet un-assembled, un-presented). Flips GREEN when the packet is presented and the owner rules
the version cut. §13.5 forbids the unilateral cut — the fix landing WITHOUT the owner's publish is
NOT a miss (PP-16): the wave records the fix landed + the cut sequenced, and the owner holds it.

---

## §π / DELTA obligations (every VISUAL claim carries a before/after π-frame + a DELTA measurement)

CLOSE has ONE visual obligation — the U-F54 real-GPU annex attest (G-CLOSE-4). Every §2 still-red +
U-F7 frame is **owner-attested**, never headless. The other four gates are structural (no π-frame).

| Family / claim | π-frame (before → after) | DELTA measurement | Attestation |
|---|---|---|---|
| **U-F54 annex — U-F5..F13 §2 still-reds** | each still-red's owner-eye before → the re-cured after (from U.W-VISUAL's landed cures) | per-still-red owner verdict Δ (RED → GREEN-by-eye) + the U.W-VISUAL DELTA cites (ΔL, px reseat, ΔE) carried forward | **owner-attested / real-GPU frame** (U-F54 annex — NEVER headless; §21 false-red risk) |
| **U-F7 scene-transition** (T-58 MANDATE) | the thrice-red swap-window before → the confound-isolated after | swap-window Δ (ms, real-GPU frame-by-frame with the O-16-R1 clobber + PKT-1 dist-clock confounds isolated) | **owner-attested / real-GPU frame** (NEVER headless — the T-58 mandate) |
| U-F61 (structural, no π-frame) | — | 4 single-sourced claims → 4 explicitly flagged (attested-not-verified section present) | `FINAL.md` completeness gate (G-CLOSE-3) |
| the ledger walk (structural, no π-frame) | — | ledger rows → `FINAL.md` dispositions (zero-drop delta = 0 un-mapped rows) | G-CLOSE-1 completeness assertion |
| the publish presentation (structural, no π-frame) | — | packet-assembled + FOUR surfaces preserved-or-co-migrated + both floors satisfied → presented | G-CLOSE-5 (owner-held §13.5) |

No VISUAL claim in this wave is gated by a headless assertion. The single visual obligation (the
U-F54 annex) rides the owner-attested annex, exactly as S and T did.

---

## §Cross-repo RELAY (the E-2 owner edict — every glass-ui/contract change relays to the BH inbox at root)

CLOSE makes **no new** glass-ui/component change — but it **VERIFIES the RELAY discharge** the
tranche's cross-repo waves owe (E-2: "all component-level and glass-ui-level changes communicated
directly, at the root, a fond"). It carries a RELAY-VERIFICATION row, not a new communiqué.

- **RELAY-CLOSE (verification, path-scoped to the glass-ui BH inbox at glass-ui HEAD, foreign-tree
  fence otherwise)** — confirm at close that the three relay obligations landed at the siblings'
  HEADs, grep-clean, local-only per the fence:
  1. **The U-formation BH communiqué** (already LANDED at glass-ui HEAD `17e0f522`,
     `valuejs-inbox-2026-07-12-u-formation.md`, §28.3: the mixColors-convention coupling · U-F4
     PRM-dock · the 8 carried-forward T producer reds · the 5.0.0 + parseCSSValue-reshape co-land) —
     confirm it is still present at close.
  2. **The U.W-ADOPT co-land ADDENDUM** (authored at the glass-ui cut, extending the communiqué with
     the U-F4 arm-static-fallback BI-acceptance constraint + the U-F13-producer edge-ring constraint +
     the U-F77 co-land + the version decision against glass-ui's `^3.1.0` floor) — confirm landed at
     the cut, or STILL-BOOKED if the cut is trigger-gated past the window.
  3. **The keyframes co-migration** (U-F77's `^3.1.0` pin-widen + the backward-color raw-oklab
     convention gate) — confirm relayed through keyframes' active dev process at the cut, or
     STILL-BOOKED (ties B1/B2).
- No convention change ships un-relayed (§28.3). CLOSE's job is to prove the relay half of the U-F30
  structural resolution (§28) is REAL at close — not to open a new one.

**Planning-only note**: this wave doc AUTHORS the RELAY-verification SPEC (what MUST be confirmed at
close); the actual confirmation is executed at close, path-scoped, grep-clean, local-only per the
foreign-tree fence. No source or foreign-tree edit lands from THIS campaign.

---

## §Dispositions (each family/close-obligation → the exact build/fold/retire + rationale)

| Family / obligation | Disposition | Rationale |
|---|---|---|
| **U-F61** `single-sourced-claims` | **fold** (the close ledger FLAGS each) | The four attested-not-verified claims (X2 NCSU-301 · CI TBT red · born-RED cure-ownership · deploy-webhook) get an explicit `FINAL.md §attested-not-verified` flag with true status + cross-reference (X2↔U-F41, TBT↔U-F3, cure-ownership↔U.W-ORACLE). No independent gate beyond the honest flag (G-CLOSE-3). |
| **U-F54** real-GPU annex | **attest here** (home = charter §annex; disp **acknowledge**) | The headed-GPU slate never ran in 7 rounds (SwiftShader confound); CLOSE discharges the attestation — the §2 still-reds + U-F7 owner-attested against a real-GPU/owner frame (G-CLOSE-4), NEVER headless. Un-attested in-window ⇒ `complete_with_misses`. |
| **the zero-drop ledger walk** | **build** (E-4's completion proof) | `FINAL.md` walks `DISPOSITION-LEDGER.md` row-by-row; a regenerable completeness assertion (G-CLOSE-1) gates it so a dropped row is RED, not a prose omission. Absorbs the W8-terminal inheritance (U-F18/F19) + the trigger-gated adopt residual (U-F2) by name. |
| **the PUBLISH presentation** | **present WITH the landed fix** (owner-held §13.5) | LIB authors the fix + packet; ADOPT sequences the version decision against both floors + the FOUR-surface co-migration; CLOSE presents the assembled packet to the owner for the cut (G-CLOSE-5). §13.5 forbids the unilateral cut — un-taken publish is not a miss (PP-16). |
| **the RELAY discharge** | **verify** (E-2) | CLOSE confirms the three relay obligations landed at the siblings' HEADs (the formation communiqué `17e0f522` + the ADOPT addendum + the keyframes co-migration), grep-clean per the foreign-tree fence. No new communiqué. |

Zero silent drops: U-F61 folds to the flag; U-F54 attests; the ledger walk, publish presentation,
and relay discharge each reach a decided close-obligation.

---

## §Dependencies

- **ALL nine waves (the terminal DAG sink)** — CLOSE opens after U.W-LIB · U.W-ADOPT · U.W-VISUAL ·
  U.W-A11Y · U.W-PERF · U.W-CANON · U.W-SEC · U.W-ORACLE · U.W-DEMO have each reached a decided state
  (build landed / booked / escalated / retired). CLOSE cannot walk the ledger or re-probe the books
  before the waves that populate them have closed.
- **U.W-LIB → the publish packet** — the fix + the semver classification + the both-floor co-land
  plan are LIB-authored; CLOSE cannot present an un-landed fix (G-CLOSE-5 is RED until LIB lands).
- **U.W-ADOPT → the version-cut sequencing + the trigger-gated residual** — ADOPT sequences the cut
  against both `^3.1.0` floors; if the glass-ui 5.0.0 trigger did not fire in-window, ADOPT hands
  CLOSE the cut-execution residual BY NAME (B2, `complete_with_misses`).
- **U.W-VISUAL → the §2 still-red frames (W8-terminal-dependent)** — VISUAL's row-set is scoped once
  T.W8 CLOSES; the owner-attested annex frames (G-CLOSE-4) ride VISUAL's landed cures.
- **U.W-PERF → the U-F3 Q14 GREEN-or-escalation** — PERF hands CLOSE the W9 close gate (green
  producer cut OR the escalate structural fact, never a gate re-baseline — Q14 verbatim; B3).
- **U.W-ORACLE / U.W-SEC / U.W-CANON / U.W-DEMO → the standing gates + attested claims** — the
  re-probe books (B5–B13) ride these waves' standing deliverables + deploy-dependent confirmations.

## §BOOKS — what closes `complete_with_misses` (named, never re-booked UN-DECIDED)

CLOSE is the terminal wave: nothing rides to a **later U wave**. What follows are the legitimately
external-gated residuals that close `complete_with_misses` (PP-16) — each DECIDED, each with its
external gate named, none re-booked as an un-decided family to a successor tranche (the disease-row
law: a family DECIDED here does not ride un-decided).

- **The glass-ui 5.0.0 cut execution** (U-F2 DECIDED build; the cut is owner-/USER-gated) — if the
  trigger did not fire in-window, the cut execution is recorded `complete_with_misses` with the
  external trigger named (the owner-gated `v5*` tag). The disease row is DECIDED (build, with named
  BI-acceptance constraints + the co-land ordering); only the execution is trigger-gated.
- **The VPN-gated NCSU on-wire re-verify** (U-F41 escalate → U-F61 attested-not-verified; ties X2) —
  recorded attested-not-verified with the NCSU-VPN gate named; the deploy-ceremony action item stands.
- **The deploy-dependent live confirmations** (U-F39 headers · U-F36 impersonation · U-F38
  token-at-rest) — the repo artefacts (born-RED tests + the `_headers`/fix) LANDED; the live-wire
  confirmation is deploy-gated, flagged (never asserted green pre-deploy).
- **The owner-un-attested annex frames** (U-F54 / U-F7 + any §2 still-red the owner did not attest
  in-window) — recorded `complete_with_misses` with the real-GPU/owner-frame obligation named, NEVER
  a headless false-green.
- **The library-correctness version cut** (U-F77 / §13.5) — the fix LANDED (LIB) + the packet
  PRESENTED (CLOSE); the version cut is owner-held. An un-taken cut is not a miss — it is sequenced,
  owner-held, and the fix is certified against the published surface either way.
- **The U-F3 Q14 close** (GREEN-or-escalation) — green if the producer cut landed in-window; else the
  escalate structural-fact document handed to the owner (cut-in-window OR a ruled re-scope, never a
  gate re-baseline — Q14 verbatim). The disease/RP-2 arm is DECIDED by the decision.
- **U-F28 keyframes next-tag retire** (WATCH row) — retires the moment keyframes cuts its next tag;
  re-probed live at close (B1), STILL-BOOKED with zero value.js kf imports if untagged.

**PP-16 statement**: gates-pass-goal-unmet closes `complete_with_misses`. CLOSE's own five born-RED
gates (G-CLOSE-1..5) are the close-work; the residuals above are external-gated, DECIDED, and named.
No UN-DECIDED family, chronic, or prompt-recap row rides to a successor tranche — E-4's discharge
holds through close.

---

**Precedence chain** (restated): the owner's verbatim (§13.5 + any live ruling) → `audit/registry.md`
(§16 U-F61/U-F54 · §8 the zero-drop WINS · §21 the false-red risk · §28/§28.3 the relay) →
`DISPOSITION-LEDGER.md` (the row set this wave walks) → `U.md` (charter) → this wave doc. Downstream
never overrides upstream. CLOSE proves the tranche's zero-silent-drop contract, flags the honest
caveats, presents the semver-loaded publish decision, and attests the real-GPU annex — against a
real-GPU/owner frame, never a headless assertion.
