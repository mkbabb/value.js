# U.W-ADOPT — THE DISEASE WAVE (glass-ui 5.0.0 adopt + the lib-cut co-land)

**Wave**: U.W-ADOPT · **designHeavy**: no (structural — precise gates, born-RED, mechanical rigor;
the two visual gates ride the U-F54 real-GPU/owner-attested annex, not a frontend-design loop) ·
**Families**: U-F2 · U-F4 · U-F13-producer · U-F28 · U-F68 · U-F77.

**Opens after**: **U.W-LIB** (the U-F77 co-land — U.W-LIB authors the U-F29/F30/F31/F32/F33 fix +
owns the invariant + the build-time re-enumeration born-RED; U.W-ADOPT sequences the *version-cut
decision* against both floors (glass-ui peer + keyframes runtime) so the fix and the cut land coherently — LIB → ADOPT, registry
R-4/§22/§25/§28) **AND the external glass-ui 5.0.0 trigger** (USER-GATED, UNFIRED at authoring:
`../glass-ui` disk labels `5.0.0` but sits on branch `tranche/BG` with NO `v5*` tag; value.js CI
still checks it out `ref: tranche/BG` at six sites). **This wave FLOATS on the owner-gated cut** —
its born-RED gates are ARMED now (the defects are LIVE in the pinned tree) and flip only when the
owner takes the cut. **Downstream**: ADOPT → U.W-PERF (U-F3's producer LCP lever unblocks only at
this adopt; registry §26).

**The spec of record is `../audit/registry.md`** (§1 U-F2/U-F4 · §2 U-F13 · §7 U-F28 · §10 R-1
U-F4-sharpened · §17 U-F68 · §20 R-4 · §22 · §25 · §28 the structural resolution + the §28.3 relay
discharged). Where this doc and the registry could diverge, **the registry wins**; above both, **the
owner's verbatim wins** (§13.5). Precedence: owner verbatim → registry → `U.md` → this wave doc.

---

## §Goal criterion

The glass-ui 5.0.0 adopt — the DISEASE ROW (ridden S.W8 → T.W7 → a THIRD tranche whole, a
~10-tranche primitive-asks lineage) — is DECIDED and, at the owner-gated cut, LANDED: value.js
resolves glass-ui at the tagged 5.0.0 master (the `tranche/BG` pin retired at all six CI sites +
`deploy-pages.yml`), the `goo-blob → blob` clean-break rename is adopted with zero dead import, the
package-lock's stale glass-ui record is refreshed to the resolved 5.0.0, and boot-smoke is green
against the 5.0.0 dist. The §1 producer design reds (U-F4 PRM-dock, U-F13-producer edge-clip) are
re-filed as **EXPLICIT NAMED glass-ui BI-acceptance constraints** — not trusted to topical waves,
not cured by a demo override (E-3: a consumer CSS override would breach glass-ui-first). The
library-correctness version cut (U.W-LIB's U-F29/F30 amelioration, §13.5) is **owner-held** and
sequenced against BOTH `@mkbabb/value.js '^3.1.0'` floors (glass-ui peer + keyframes runtime) AND the
spectrum-walk/backward-color raw-channel convention co-migration, so the fix and the cut land
coherently (U-F77).

## §Completion criterion

- **The disease row is DECIDED in this named wave** — the disease-row LAW is satisfied by the
  DECISION (build here, with named BI-acceptance constraints + the co-land ordering), independent of
  whether the owner-gated cut fires in-window. If the cut fires: the born-RED adopt gates flip GREEN.
  If it does not: the constraints are FILED, the co-land ordering AUTHORED, the gates ARMED, and the
  wave closes `complete_with_misses` (PP-16 — gates-armed-goal-trigger-gated), with the residual
  cut-execution booked BY NAME to U.W-CLOSE's book re-probe.
- Every family (U-F2, U-F4, U-F13-producer, U-F28, U-F68, U-F77) reaches its decided disposition
  (§Dispositions); zero silent drops.
- Each VISUAL claim (U-F4, U-F13-producer) carries a before/after π-frame obligation + a DELTA
  measurement, attested against a **real-GPU or owner frame, NEVER a headless assertion** (the U-F54
  annex; the registry logged TWO demonstrated headless false-reds against exactly these — the U-F4
  `overture.css` red herring and the U-F13 PRM confound, §21).
- The cross-repo RELAY row is discharged: the U→glass-ui BH communiqué (already landed at glass-ui
  HEAD `17e0f522`, §28.3) is EXTENDED at the cut with the co-land addendum; the keyframes
  co-migration rides U-F77's pin-widen. No convention change ships un-relayed (E-2).

---

## §Scope — the families this wave builds (each with its cure APPROACH; E-3 binds — no workaround, no legacy)

### U-F2 — `adopt-trigger-disease` · **build (THE DISEASE LAW: its own wave)**

**Evidence** (registry §1, CONFIRMED against glass-ui HEAD `e4e7f660` re-verified file:line; disk
re-confirmed this authoring): the 5.0.0 adopt cohort has ridden three tranches un-landed; the
sibling tree reads `5.0.0` (`../glass-ui/package.json:3`) but on branch `tranche/BG`, **no `v5*`
tag** (USER-GATED); value.js CI pins `ref: tranche/BG` at `ci.yml:80,277,373,472,557` +
`deploy-pages.yml:80`; the `goo-blob → blob` clean-break rename is still `goo-blob` on disk
(`../glass-ui/package.json:202,538` exports `./goo-blob`) and WILL break the demo import surface at
the master 5.0.0 landing; glass-ui's BI formation absorbed only the mechanical asks — **every §1
design red is LIVE at their HEAD and none is rowed as a BI acceptance constraint** (each topical
wave declares "No cross-repo ask" and diagnoses a different root).

**Cure APPROACH** (E-3 — idiomatic, gestalt, no workaround): the adopt is executed as an
**adopt-EVENT** (an explicit booked cut, per the §3.4 pin policy), not a silent floor-bump. At the
owner-gated cut: (1) retire the `tranche/BG` pin at all six CI sites + `deploy-pages.yml` → the
tagged 5.0.0 master; (2) adopt the `goo-blob → blob` rename at the demo import surface (migrate the
consumer to the new name at the root — E-3 forbids a compat shim / re-export alias); (3) the §1
producer design reds re-file as EXPLICIT NAMED BI-acceptance constraints in the BH communiqué
addendum (U-F4, U-F13-producer, + the 9 carried-forward T producer reds already in §2b). No demo
cure is invented for a producer defect.

### U-F4 — `reduced-motion-dock-collapse` · **fold → U-F2's BI-acceptance list (PRODUCER-ONLY; demo EXONERATED)**

**Evidence** (registry §1 + §10 R-1 SHARPENED + §23 R-1 SOUND): under `prefers-reduced-motion:
reduce` the dock stays a **44px pill on every view — 19/20 nav+action controls visually clipped
away, with NO hover/click/keyboard recovery** (probes: width stays 44px through all interactions).
The mechanism was HALF WRONG in §1 and CORRECTED by R-1: **PRODUCER-ONLY** — `[data-morphing]`
latches ON under PRM → glass-ui `styles/dock/morph.css` derives `--dock-expand-t` from the frozen
`--dock-morph-t=0` while the "expanded"/"always-expanded" class is set (`morph.css` `[data-morphing]`
armed by `useLayerTransition`, the `--dock-expand-t` directional derivation ~:26–53); **DESKTOP
-viewport-specific** (`always-expanded=false`; mobile degrades to 244px, not empty). `overture.css`
(`:171–181`) is a **confirmed RED HERRING** (never applied under PRM). **Demo EXONERATED.** A
WCAG 2.4.3 / 2.4.7 / 1.4.13-class app-level nav failure.

**Cure APPROACH**: **there is no value.js/demo cure to make** (R-1 retired the phantom demo
static-fallback row). The build is the BI-acceptance constraint: glass-ui must, at 5.0.0, make the
PRM-frozen dock present an arm-static EXPANDED fallback (controls reachable without motion). E-3
explicitly forbids a demo CSS override here — it would breach glass-ui-first. RELAYED to BH.

### U-F13-producer — `dock-edge-clip` (producer half) · **build via RELAY + BI-acceptance**

**Evidence** (registry §2, rows T-36 · T-52; §21 the milder read after the PRM confound retired):
the producer clip/fade-mask shaves EDGE items' focus rings only (interior rings full) — milder than
the earlier total-shave read, which was the U-F4 PRM confound. **This wave owns the PRODUCER half
only** (the clip/fade-mask); the demo Tools true-button box-model half is U.W-VISUAL's row (ledger:
"U.W-VISUAL (demo Tools box-model) + U.W-ADOPT/relay (producer clip/fade)").

**Cure APPROACH**: BI-acceptance constraint — the producer dock-layer clip/fade-mask must not shave
edge-item focus rings (the ring is the a11y affordance — see U-F25's focus-affordance class). Named
in the BH communiqué (§2b already carries the 9 T producer reds; this is one of them). No demo cure
in this wave. VISUAL still-red → gate against a real-GPU/owner-attested frame (U-F54 annex).

### U-F28 — `kf-prm-expand-fixed-unreleased` · **retire on their next tag (WATCH row, no work)**

**Evidence** (registry §7): the one keyframes ask is FIXED at their HEAD (`managed-stepper.ts` PRM
arm now emits) but unreleased (disk `5.2.0`, no tag past it). **value.js has ZERO direct keyframes
imports** (standing structural fact — the keyframes devDep is the demo build's provision of glass-ui's
kf peerDependency, not a value.js import; CLAUDE.md §Dependencies). **No work, no gate.** A cross-repo
WATCH row: retire the moment keyframes cuts its next tag; booked to U.W-CLOSE's book re-probe.

### U-F68 — `glass-ui-lock-adopt-drift` · **fold → the adopt cut (lock refresh)**

**Evidence** (registry §17): the package-lock records a STALE glass-ui version (registry read
`4.2.0`; the workspace-dep node reads `~4.0.0` at `package-lock.json:194`, the resolved node reads
`resolved: "../glass-ui"` at `:1432–1433`) — the adopt gap is ALREADY LIVE locally; the lock's
recorded version is a false record vs disk `5.0.0`. glass-ui 5.0.0 peers ARE satisfied (value 3.1.0,
kf 5.2.0). **Cure APPROACH**: no independent work — the lock refreshes at the adopt cut (a
sub-assertion of the U-F2 cut gate, G-ADOPT-1).

### U-F77 — `library-cut-adopt-ordering` · **build (THE CO-LAND ORDERING)**

**Evidence** (registry R-4 §20 + §22 amendment + §25 amendment + §28 the structural resolution):
U-F29's honest amelioration (§13.5) is **semver-loaded**. BOTH siblings pin `@mkbabb/value.js
'^3.1.0'` — glass-ui at `package.json` → `peerDependencies["@mkbabb/value.js"]` +
`devDependencies["@mkbabb/value.js"]` (a **peer floor**; cite block+key, never a line integer into a
moving foreign tree), keyframes at `../keyframes.js/package.json` → **`dependencies["@mkbabb/value.js"]`**
(a **RUNTIME dependency floor** — NOT `devDependencies`; the STRONGER coupling: a 4.0.0 cut strands
keyframes' RUNTIME dependency AND every transitive keyframes consumer, not merely keyframes' own build)
— so a **4.0.0 cut strands TWO `^3.1.0` floors** (glass-ui peer + keyframes runtime), not one; a 3.x
minor auto-adopts. U-F30's fix changes
the raw-channel OUTPUT convention that **FOUR convention-sensitive surfaces read RAW, bypassing
`toString`** (registry §28, enumerated across 2 siblings × 2 pipelines):

| # | Surface | Site (verified this authoring) | Reads |
|---|---|---|---|
| 1 | glass-ui `parseCSSColor` | `../glass-ui/.../color-picker/composables/color/index.ts:24,119` | parse→`colorUnit2` (auto-adopts the ^3.1.0 minor) |
| 2 | glass-ui direct `mixColors`/`sampleColorRamp` | `spectrum-walk.ts:22` (import), `:58` (`mixColors`), `:90` (`sampleColorRamp`), raw `c.l`/`c.c`/`c.h*360` `:37–39` | RAW `OKLCHColor` channels, bypassing `toString` |
| 3 | keyframes `parseCSSValue` | `resolve-function.ts:12,61,159` + `resolve-if.ts:14,131` (try/catch-guarded) | the reshaped parse output |
| 4 | keyframes direct `sampleColorRamp`/`color2` | `backward-color.ts:25` (import), `:205/:221` (`sampleColorRamp`), `rawOklab` `:58–67` (`color2`→`.l/.a/.b`) | RAW oklab channels, bypassing `toString` |

**Cure APPROACH** (§28 — the STRUCTURAL resolution, supersedes enumerate-every-consumer): U.W-ADOPT
does NOT re-enumerate a living codebase's consumers (a moving target). It sequences the **co-land
ordering** in THREE binds at the cut: (a) the version decision (owner-held, §13.5) against BOTH
`^3.1.0` floors (glass-ui peer + keyframes runtime); (b) glass-ui's spectrum-walk color-convention co-migration; (c) keyframes'
backward-color color-convention co-migration. The **build-time re-enumeration born-RED** (authored in
U.W-LIB, §28.2; re-run and confirmed at THIS cut) greps the THEN-CURRENT constellation for
raw-channel readers and gates the chosen U.W-LIB invariant against ALL of them — so the exact
consumer count is a BUILD concern, never a formation gate. The design loop's preferred invariant
(fix only the `toString`/serialize layer, preserving the raw `mixColors`/`sampleColorRamp`/`color2`
channel convention — §28.1) leaves surfaces 2 + 4 UNAFFECTED regardless of count; if the chosen
invariant instead changes the raw convention, surfaces 2 + 4 co-migrate (fold into U-F77's co-land
exactly as the pin-widen carries the version). RELAYED to BOTH sibling dev processes (E-2).

---

## §Hard gate (born-RED where the defect is LIVE — registry-cited; visual gates against a real-GPU/owner frame)

Four born-RED gates. Each is ARMED now (the defect is LIVE in the `tranche/BG`-pinned tree) and
flips only at the owner-gated cure. G-ADOPT-2 and G-ADOPT-3 are producer-gated (the cure is
glass-ui-side; value.js has no source edit to make) and gate against a **real-GPU/owner-attested
frame, NEVER a headless assertion** (U-F54 annex).

### G-ADOPT-1 — the adopt-cut verification (U-F2 + U-F68 lock refresh) · **born-RED**

`test:dist` / a CI assertion that: (a) NO `tranche/BG` pin remains (`grep -c 'ref: tranche/BG'
.github/workflows/*.yml` → **0**; RED today = 6 — `ci.yml:80,277,373,472,557` + `deploy-pages.yml:80`);
(b) the package-lock's recorded glass-ui version ≡ the resolved disk `5.0.0` (RED today: lock reads
`~4.0.0` / stale vs disk `5.0.0` — U-F68); (c) the demo build has zero dead `goo-blob` import (RED
today: rename un-adopted); (d) boot-smoke is green against the 5.0.0 dist (the Tabs-class
named-export drift catch, per §3.4 pin policy). **Flips GREEN only at the owner-gated cut.**

### G-ADOPT-2 — U-F4 PRM-dock BI-acceptance · **born-RED (behavioral probe + owner-attested π-frame)**

Under `prefers-reduced-motion: reduce`, drive the demo dock across views; assert **dock width > 44px
AND ≥ 19/20 nav+action controls reachable** (probe: R-1 measured width stays 44px = the LIVE defect,
1/20 usable). Born-RED NOW (dock is a 44px empty pill). The R-1 verify RETIRED the headless confound
(`overture.css` red herring → producer-only), so the PRM width+reachability probe is
headless-trustworthy — BUT the visual "reads-correct-when-expanded" confirmation is **owner-attested**
(π-frame, §π/DELTA). The cure is PRODUCER-ONLY → the gate flips only when glass-ui ships the accepted
BI-acceptance constraint at 5.0.0; value.js has NO cure to make (E-3 forbids a demo override).

### G-ADOPT-3 — U-F13-producer edge-clip · **born-RED against a real-GPU/owner-attested frame + RELAY**

On a **real-GPU or owner-attested frame** (NEVER headless — U-F54 annex; the PRM confound already
demonstrated a false-red here), assert EDGE-item dock focus rings render **un-shaved (≡ interior-item
ring pixel-coverage)**. Born-RED NOW (registry §2: producer clip/fade-mask shaves edge rings).
Producer-gated (BI-acceptance, RELAYED to BH); the demo Tools box-model half is NOT this gate
(U.W-VISUAL).

### G-ADOPT-4 — U-F77 co-land ordering · **born-RED (build-time re-enumeration + BOTH `^3.1.0` floors: glass-ui peer + keyframes runtime)**

Two arms, both born-RED at authoring:

- **4a — build-time re-enumeration** (§28.2, authored in U.W-LIB, re-run at THIS cut): a script greps
  the THEN-CURRENT constellation for raw-channel readers of the changed U-F30 functions
  (`mixColors`/`sampleColorRamp`/`color2` + `parseCSSValue`/`parseCSSColor`) and gates the chosen
  U.W-LIB invariant against ALL enumerated surfaces. RED today: the enumeration returns the FOUR
  surfaces above (glass-ui spectrum-walk + parseCSSColor; keyframes backward-color + parseCSSValue×3)
  and the invariant is un-chosen/un-landed. Flips GREEN when the invariant lands AND every enumerated
  surface is provably preserved-or-co-migrated. **This is the born-RED that drives the consumer
  surfaces** the task names — registry §25 said THREE (glass-ui parseCSSColor + glass-ui direct-mix +
  keyframes parseCSSValue); §28 (round 7) AMENDED to FOUR (added keyframes' backward-color color
  half). The registry wins: the gate drives all FOUR; the "three" framing is the pre-§28 count.
- **4b — BOTH `^3.1.0` floors satisfied at the version cut** (glass-ui peer + keyframes runtime): assert the owner-held version decision keeps
  glass-ui's `^3.1.0` (`package.json` → `peerDependencies["@mkbabb/value.js"]` + `devDependencies["@mkbabb/value.js"]`, a **peer floor**) AND keyframes' `^3.1.0` (`../keyframes.js/package.json` → **`dependencies["@mkbabb/value.js"]`**, a **RUNTIME dependency floor** — grep the `dependencies` block, NOT `devDependencies`) satisfied
  — a 3.x minor auto-adopts; a 4.0.0 cut RED until both siblings co-widen. Born-RED where the version
  decision is un-taken (§13.5, owner-held). Flips GREEN when the cut is taken with both floors held (or
  both co-widened + relayed).

**U-F28** carries no gate (WATCH row, zero value.js kf imports — a standing GREEN structural fact,
not a born-RED).

---

## §π / DELTA obligations (every VISUAL claim carries a before/after π-frame + a DELTA measurement)

| Family | π-frame (before → after) | DELTA measurement | Attestation |
|---|---|---|---|
| **U-F4** | PRM dock = 44px empty pill, 1/20 controls reachable → PRM dock expanded, controls reachable | dock-width Δ (44px → expanded-width px) + reachable-control Δ (1/20 → 20/20) | **owner-attested** (visual "reads-correct" half) + headless-trustworthy width probe (post-R-1) |
| **U-F13-producer** | edge-item focus ring shaved by clip/fade-mask → edge ring un-shaved ≡ interior ring | edge-ring pixel-coverage Δ (clipped arc → full arc, ≡ interior-item coverage) | **real-GPU / owner-attested frame** (U-F54 annex — NEVER headless) |
| U-F2 (structural, no π-frame) | — | pin-count Δ (`tranche/BG` ×6 → 0); lock glass-ui-version Δ (stale → 5.0.0); boot-smoke pass Δ (dead-import → green) | CI / `test:dist` |
| U-F77 (structural, no π-frame) | — | enumeration surfaces preserved-or-co-migrated Δ (4 un-guarded → 0 un-guarded); BOTH peer-floor satisfaction (glass-ui ^3.1.0 + keyframes ^3.1.0 green at cut) | build-time re-enumeration gate |

No VISUAL claim in this wave is gated by a headless assertion. The two visual gates (U-F4,
U-F13-producer) ride the U-F54 owner-attested annex.

---

## §Cross-repo RELAY (the E-2 owner edict — every glass-ui/component change relays to the BH inbox at root)

This wave touches glass-ui component surfaces (the dock morph geometry, the dock-layer clip/fade-mask)
AND the glass-ui-level raw-channel convention contract (mixColors/sampleColorRamp/color2) — it carries
a RELAY row.

- **RELAY-ADOPT (path-scoped to `../glass-ui/docs/tranches/BH/coordination/valuejs-inbox-<date>-adopt.md`
  at glass-ui HEAD, foreign-tree fence otherwise)** — the FORMATION communiqué already LANDED at
  glass-ui HEAD `17e0f522` (`valuejs-inbox-2026-07-12-u-formation.md`, §28.3: §1 the mixColors-convention
  coupling, §2a U-F4 PRM-dock, §2b the 9 carried-forward T producer reds, §3 the 5.0.0 +
  parseCSSValue-reshape co-land). **This wave EXTENDS it at the cut with the co-land ADDENDUM**:
  (1) the U-F4 PRM-dock arm-static-fallback as a NAMED 5.0.0 BI-acceptance constraint;
  (2) the U-F13-producer edge-clip/fade-mask focus-ring constraint (one of the §2b nine, sharpened to
  the edge-ring-shave a11y class);
  (3) the U-F77 co-land — the chosen U.W-LIB invariant + whether glass-ui's spectrum-walk HOLDS the
  current raw convention (invariant-preserving fix) or CO-MIGRATES at their cut;
  (4) the version decision against glass-ui's `^3.1.0` peer floor (owner-held, presented WITH the
  landed fix).
- **The keyframes co-migration** rides U-F77's pin-widen (its structural `^3.1.0` peer + the
  backward-color raw-oklab convention gate) — relayed through keyframes' active dev process at the
  cut. Cross-repo convention changes are RELAYED, not silently shipped (§28.3).

**Planning-only note**: this wave doc AUTHORS the RELAY-row SPEC (what MUST be relayed at execution);
the cut-time addendum is written at execution, path-scoped to the glass-ui BH tree, grep-clean,
local-only per the foreign-tree fence. No source or foreign-tree edit lands from THIS campaign.

---

## §Dispositions (each family → the exact build/fold/retire + rationale)

| Family | Disposition | Rationale |
|---|---|---|
| **U-F2** | **build** (THE DISEASE LAW — its own wave) | Ridden ≥3 tranches un-decided → a disease row → a wave of its own; the §1 design reds re-file as EXPLICIT NAMED BI-acceptance constraints (not trusted to topical waves). The adopt is an explicit booked EVENT at the owner-gated cut. |
| **U-F4** | **fold → U-F2's BI-acceptance list** | R-1: PRODUCER-ONLY (`morph.css` `[data-morphing]` PRM latch); `overture.css` a RED HERRING; demo EXONERATED. The phantom demo static-fallback row is RETIRED; there is NO value.js cure — the build is the producer BI-acceptance constraint. E-3 forbids a demo override. |
| **U-F13-producer** | **build via RELAY + BI-acceptance** (producer half only) | The clip/fade-mask is producer-side; named in the BH communiqué (one of the §2b nine, sharpened to the edge-ring-shave a11y class). Gated on a real-GPU/owner frame. The demo Tools box-model half → U.W-VISUAL. |
| **U-F28** | **retire on their next tag** (WATCH row, no work) | Fixed-unreleased at keyframes HEAD; value.js has ZERO direct kf imports → nothing to build. Retires the moment keyframes cuts its next tag; booked to U.W-CLOSE re-probe. |
| **U-F68** | **fold → the adopt cut** (lock refresh) | The stale glass-ui lock record is a false record vs disk 5.0.0; it refreshes at the cut (a sub-assertion of G-ADOPT-1). No independent work. |
| **U-F77** | **build** (THE CO-LAND ORDERING) | The lib-correctness version cut is semver-loaded against BOTH `^3.1.0` floors (glass-ui peer + keyframes runtime `dependencies`) + the 4-surface raw-channel convention; the co-land ordering sequences the version decision (owner-held, §13.5), the build-time re-enumeration gate, and the two sibling co-migrations. LIB authors the invariant + build-time gate; ADOPT owns the co-land. |

Zero silent drops: F2, F4, F13-producer, F28, F68, F77 each reach a decided home.

---

## §Dependencies

- **U.W-LIB** (LIB → ADOPT) — U.W-LIB authors the U-F29/F30/F31/F32/F33 fix + owns the U-F30 invariant
  choice + the build-time re-enumeration born-RED (§28.2); U.W-ADOPT sequences the *version-cut
  decision* (owner-held) against both floors (glass-ui peer + keyframes runtime) so the fix and the cut land coherently (U-F77). The
  co-land cannot sequence a version until LIB has landed a fix to version.
- **External glass-ui 5.0.0 trigger** (USER-GATED, UNFIRED) — glass-ui disk `5.0.0` on branch
  `tranche/BG`, no `v5*` tag; the wave FLOATS on the owner-gated cut. The born-RED gates are ARMED but
  flip only when the cut fires.
- **Downstream: ADOPT → U.W-PERF** — U-F3's producer LCP lever (the RP-2 331.0 KiB JS-eager payload
  cut — T close-of-record; 347.9 was the S re-baseline) is gated on THIS adopt; U.W-PERF's U-F3
  escalate inherits the adopt state (registry §26).

---

## §BOOKS (what rides to a later wave — by name)

- **U-F3 (Q14/RP-2 LCP producer cut) → U.W-PERF (escalate).** The producer payload lever U-F3 needs
  unblocks ONLY at this adopt cut; if the cut does not fire in-window, U.W-PERF presents the owner the
  structural fact (green needs the producer cut in-window, or a ruled re-scope). Booked BY NAME.
- **U-F13-demo (the Tools true-button box-model half) → U.W-VISUAL.** This wave owns only the producer
  clip/fade-mask half; the demo box-model half is U.W-VISUAL's row (ledger).
- **The visual still-reds U-F5..F12 + U-F13-demo → U.W-VISUAL (W8-terminal-dependent).** Only the
  U-F13 PRODUCER half lives here; the rest ride U.W-VISUAL.
- **The real-GPU annex attest (U-F54) → U.W-CLOSE (owner-attested).** The U-F4 + U-F13-producer visual
  gates ride the annex; the owner attestation is discharged at close.
- **U-F28 keyframes next-tag retire → U.W-CLOSE book re-probe.** The WATCH row is re-probed live at
  close; retires the moment keyframes cuts its next tag.
- **The cut-execution residual (if the trigger does not fire in-window) → U.W-CLOSE.** The disease row
  is DECIDED here regardless; the cut execution (if trigger-gated past the window) is booked BY NAME to
  U.W-CLOSE, closing `complete_with_misses` (PP-16), never re-booked to a successor tranche.
