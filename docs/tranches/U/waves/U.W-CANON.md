# U.W-CANON — CANON · HYGIENE · BUILD-TOOLING · LEDGER-TRUTH

**Wave**: U.W-CANON · **designHeavy**: no (STRUCTURAL — precise gates, born-RED where the defect is
LIVE, mechanical rigor; ZERO visual claims → no frontend-design loop, no π-frame, no real-GPU
annex) · **Families**: U-F17 · U-F21 · U-F22 · U-F23 · U-F24 · U-F49 · U-F50 · U-F51 · U-F52 ·
U-F53 · U-F63 · U-F64 · U-F65 · U-F66 · U-F69 · U-F70 · U-F71 · U-F75 (18 families) + the **inherited
U-F19 AB-1 residue-half** (About-KaTeX boot-scope, build-if-CENSUS-RED — routed here from U.W-VISUAL's
W8-inheritance census; not one of the 18 CANON-native families).

**Opens after**: **root** (U.md §The wave DAG — `dependsOn: root`). No upstream wave gates this
wave; it opens at formation. The ONE external coupling is a READ-not-dependency: U-F17's honest §4
amendment cross-references the T-tree `T.md §4` (a T-tranche closed-ledger doc) — U does not EDIT
T's ledger (path-scope fence: U commits touch `docs/tranches/U/**` ONLY); the amendment lands as a
U-side ledger row and BOOKS a one-line T.md §4 pointer to T's own close. **Runs concurrently with
T.W8 remediation on the shared `tranche-t` branch → every commit pull-rebases first.**

**The spec of record is `../audit/registry.md`** (§4 U-F17 · §5 U-F21/F22/F23/F24 · §17
U-F63/F64/F65/F66/F69/F70/F71 · §19 U-F75 · §13 U-F49/F50/F51/F52/F53). Where this doc and the
registry could diverge, **the registry wins**; above both, **the owner's verbatim wins**.
Precedence: owner verbatim → registry → `U.md` → this wave doc.

---

## §Goal criterion

The tree tells the truth about itself. Every canon document (root + api + **demo** `CLAUDE.md`,
`README.md`, **`demo/DESIGN.md`** facilities, the parse-that pin prose, the precept infra label — the
full T/FINAL §5:305 doc-rewrite remainder) is **regenerably** in sync with the landed tree
(the CLAUDE.md LoC-precept pattern applied — documented counts derive from the tree, not from a
hand-typed number that drifts each wave); the library's OWN public surface is coherent (root and
subpath barrels agree, guarded by a standing parity gate); the one ratified-then-reversed decision
(the Q4 WELL → glass-ui veil) is **honestly amended** so no successor reading `T.md §4` believes a
reversed decision is live; the boot-time GroundRecord read is single-sourced from its typed origin
(a version bump can no longer silently strand the boot script); the build/pack/lint/typecheck/size
tooling **measures what it claims to measure** (the npm tarball ships the library not the demo, the
size gate sees the chunk-graph total not a 14.9 KB facade, the typecheck resolves fresh types not a
stale dist); and the repo hygiene traps are closed (the `auth/` silent-drop, the 58 MB tracked
heapsnapshot, the orphan branches/worktrees/scratch, the unused root zod). Every family reaches its
DECIDED disposition (build / fold / retire); zero silent drops.

## §Completion criterion

- Every family (the 18 above) reaches its decided disposition (§Dispositions); zero silent drops.
- Every **build** family that guards a LIVE defect carries a **born-RED** gate (armed now, flips
  GREEN only at the cure) — G-CANON-1..10 (§Hard gate), each registry-cited to its live evidence.
- The barrel-parity gate (G-CANON-3) and the re-anchored size gate (G-CANON-8) are **wired into
  CI** (the `test:dist` / size-budget lane) as STANDING regression guards — the crown deliverable
  of the wave is not a one-shot fix but a gate that keeps the surface honest.
- The retire families are executed AND recorded so no successor re-opens them (the §D
  zero-drop-retire discipline): the heapsnapshot `git rm`'d, the orphan branches/worktrees pruned
  WITH the prune-law's proof, the scratch swept, the zod removed, the precept label corrected.
- The fold families (U-F65 lint, U-F66 typecheck, U-F71 dev-deps) reach a NAMED fold target with a
  recorded owner-call where the disposition is owner-decidable (lint rule-set restore vs
  types+review acceptance; pretypecheck gate vs documented build-state dependence).
- **This wave has NO visual claim** — no π-frame, no real-GPU annex (the U-F54 annex is
  U.W-VISUAL's, not here). Every claim is textual / structural / mechanically-measurable, so each
  family carries a **DELTA count** in lieu of a π-frame (§π/DELTA).
- The **inherited U-F19 AB-1 residue-half** (About-KaTeX boot-scope) reaches its decided
  build-if-CENSUS-RED disposition (§Scope · §Dispositions): CANON owns the boot-scope build seam IF
  the U.W-VISUAL census reds it, else it retires into the census record — the "T-53/AB-1 →
  VISUAL/CANON" ledger routing is now carried on both sides, zero silent drop.
- PP-16 binds: gates-pass-goal-unmet closes `complete_with_misses`.

---

## §Scope — the families this wave builds (each with its cure APPROACH; E-3 binds — no workaround, no legacy)

The 18 families cluster into four coherent surfaces. E-3 (no quick solution / no legacy shim /
idiomatic gestalt; architectural transposition for elegance/simplicity is DESIRABLE) binds every
cure: a canon doc is not patched line-by-line where a regenerable check is the gestalt fix; a barrel
is not hand-synced where a parity gate is the standing cure; the GroundRecord fork is not
double-maintained where single-sourcing is the idiomatic transposition.

### Cluster 1 — LEDGER-TRUTH (the honest amendment + the forked contract)

#### U-F17 — `q4-well-veil-silent-reversal` · **build (the honest §4 amendment — the wave's named headline)**

**Evidence** (registry §4, CONFIRMED): the owner twice overruled the ratified Q4 WELL (opaque,
NO-blur) toward the glass-ui veil; the demo LANDED the reversal (`ComponentSliders.vue:26
surface="veil"`, whose glass-ui producer definition IS glass+blur; the Lane-P re-seat `ad301e7`) —
yet `T.md §4` still reads the WELL "STANDS": verified live at `docs/tranches/T/T.md:391` ("Lane-P
veil re-seat (`ad301e7`) — **Q4 'The well.' stands**") and `:682` ("**RULED — 'The well.'**"). A
ratified decision was materially reversed with NO re-ratification row; a successor reading §4 would
believe the WELL is live. This is the lesson-15 class (a ledger reversal recorded nowhere).

**Cure APPROACH** (E-3 — honest, not a cover-up patch): author an **honest §4 amendment row** in
U's ledger (the U.W-CANON canon-sync artefact + the FINAL.md ledger walk) that (1) NAMES the
reversal locus (`ComponentSliders.vue:26 surface="veil"` = glass+blur, via `ad301e7`), (2)
records that the ratified "opaque WELL, NO-blur" was superseded toward the glass-ui veil material,
(3) states the ledger-integrity lesson (a ratified decision reversed must carry a re-ratification
row, never a silent land). Because U is path-scoped to `docs/tranches/U/**`, the amendment lands
U-side and **BOOKS a one-line `T.md §4` pointer** ("superseded — see U-F17 amendment") to T's own
close (T.W8/T-close owns the T.md edit; U never rewrites a foreign closed ledger). The material
still-red (does the veil READ as a veil?) is **U-F10's row in U.W-VISUAL** — NOT this wave; U-F17
is purely the value.js-side ledger TRUTH, no glass-ui surface change (see §Cross-repo RELAY).

#### U-F23 — `ground-record-forked-read` · **build (single-source the record contract)**

**Evidence** (registry §5 + §10 WEAKENED-but-confirmed): the GroundRecord WRITE is TS
(`ground.ts` `buildGroundRecord` / `GROUND_RECORD_VERSION`), but the only READ is inline vanilla JS
in `demo/color-picker/index.html:157-169` hand-duplicating the constants: `var FIRST_VISIT = {…}`
(`:160`), `var VERSION = 1; // boot/ground.ts GROUND_RECORD_VERSION` (`:164`), the version-guard
`if (rec && rec.deriveVersion === VERSION …)` (`:169`). The typed `parseGroundRecord` (with the
version-invalidation logic) is DEAD; `GROUND_RECORD_VERSION` is NOT zero-ref (live via the write) —
so a `ground.ts` version bump silently STRANDS the boot script (it keeps comparing against the
hard-typed `VERSION = 1`).

**Cure APPROACH** (E-3 — architectural transposition, not a duplicated-constant sync): single-source
the record contract so the boot read derives from the TS origin — a **build-time inline** (a vite
transform / define that injects `GROUND_RECORD_VERSION` + the `FIRST_VISIT` seed into the HTML at
build) OR **codegen** (emit the boot snippet from `ground.ts`). The dead `parseGroundRecord` either
becomes the boot read (if the boot can import the built module early enough) or is retired with its
logic moved into the single source. No hand-duplicated constant survives.

### Cluster 2 — CANON-SYNC (the doc-lies, regenerably cured)

#### U-F21 + U-F69 — `canon-structure-drift` + `parse-that-doc-lie` · **build (ONE regenerable canon-sync row; F69 joins F21)**

**Evidence** (registry §5 U-F21 + §17 U-F69, CONFIRMED): (a) root `CLAUDE.md` Structure block
predates the T.W1 reorg — dead parsing/gamut paths, stale module counts; (b) **api/`CLAUDE.md`'s
entire Structure section is INVALID** — all 9 documented paths gone (the `modules/`+`platform/`
transposition; verified live: `api/src/platform/http/inject-services.ts` exists, none of the old
paths do); (c) `README.md` says "15 color spaces" at `:11` AND `:66` while the tree ships 17
(`src/units/color/` — the ICtCp/Jzazbz additions of S.W1 are un-counted); (d) `CLAUDE.md:154` says
parse-that "shipped 2.0.1 `a7eabcc`" while spec/lock/node_modules are ALL `1.0.0` and the caret
`^1.0.0` FORBIDS 2.x — the prose is FALSE (U-F69; the dep state is coherent, only the prose lies);
(e) **the T/FINAL §5:305 doc-rewrite remainder names TWO more tracked canon surfaces** the root+api
framing dropped — **`demo/CLAUDE.md`** (a tracked ~17 KB demo-canon doc, predating the T.W1 reorg like
its root/api siblings) and **`demo/DESIGN.md`** (the T.W3 C3-LAW + 9-row exception ledger, `T/FINAL:52`;
the "DESIGN.md facilities" T hands to U's doc lane). Both are in-scope for this canon-sync row.

**Cure APPROACH** (E-3 — the **regenerable-count precept**, CLAUDE.md's own LoC-precept pattern —
NOT a hand-retype that drifts next wave): the counts and structure blocks are re-derived FROM the
tree, and a canon-sync check makes the derivation the source of truth. Root + api + `demo/CLAUDE.md`
Structure/facilities blocks regenerated against the actual tree; `README.md` "15 spaces" → 17 (or a
regenerable "`ls src/units/color/spaces` count"); `demo/DESIGN.md`'s facility references (the C3-LAW +
exception-ledger doc) reconciled to the landed tree; `CLAUDE.md:154` parse-that prose corrected to the
PINNED reality (`^1.0.0`, 1.0.0 resolved — the "shipped 2.0.1" claim struck). One row, one gate
(G-CANON-2) asserting documented-count ≡ tree-derived-count and pin-prose ≡ pinned-version across ALL
of T's §5:305 remainder (root + api + demo CLAUDE.md + DESIGN.md facilities). U-F69
joins U-F21 (both canon-sync; the registry rules F69 "joins U-F21").

#### U-F22 — `barrel-parity-drift` · **build (cure the 10 + MINT the parity gate — the crown tooling deliverable)**

**Evidence** (registry §5 + §10 CONFIRMED-exact, all 10 verified in src AND dist `.d.ts`): root vs
subpath barrels disagree **bidirectionally** on 10 symbols — `ICtCpColor`/`JzazbzColor` classes are
in root (`src/index.ts:92,93`) but ABSENT from the `/color` subpath barrel (`src/subpaths/color.ts`
grep-count 0); the timeline `List`/`Named` variants are root-only (`parseAnimationTimelineList`
`src/index.ts:377`, `extractNamedTimelines` `:383`, `NamedTimelineRegistry` `:392`) and in NO
subpath; the raytrace + slice-boundary fns are in a subpath but ABSENT from root. A copy-omission
signature across 3 tranche edits; ships live in dist `.d.ts`; **no parity gate exists**.

**Cure APPROACH** (E-3 — the standing gate IS the gestalt fix, not a one-time hand-sync that
re-drifts): (1) cure the 10 — **ADDITIVE only** (add the missing symbols to the barrel that lacks
them; the fix removes/renames NO consumed symbol → no sibling is affected, see §Cross-repo RELAY);
(2) **MINT the parity gate** — a `test:dist` assertion that every subpath-barrel symbol ∈ (root
barrel ∪ a documented-exclusion allowlist) AND every root color/easing/math/etc. symbol appears in
its owning subpath, with the allowlist carrying an inline rationale per intentional asymmetry. The
gate is the deliverable: it keeps the surface honest across future tranche edits. Wired into CI
(the retained-5 `test:dist` lane).

#### U-F75 — `precept-stale-label` · **retire (label-only; the convention IS followed)**

**Evidence** (registry §19): `docs/precepts/infra/domains.md:25` names value.js's app "palette"
(`palette.babb.dev` / `api.palette.babb.dev`); the landed reality is "color" (color.babb.dev). The
`<app>.babb.dev` convention IS followed — only the illustrative label is stale.

**Cure APPROACH**: correct the illustrative label to the landed "color" reality (a one-line precept
edit — the precept submodule; if foreign-tree, relayed via the precept dev process per the same
fence discipline). Retire — recorded so no successor re-opens; no gate (a one-shot label fix, no
ongoing defect to guard once corrected).

### Cluster 3 — BUILD-TOOLING (the gates that measure what they claim)

#### U-F63 — `npm-pack-ships-demo` · **build (pack the library, not the demo)**

**Evidence** (registry §17, measured ~4.3 MB unpackedSize): `package.json:67` `files:["dist"]` +
NO `.npmignore` (verified absent) + a no-op `prepare` (`:77` `test -f dist/value.js || npm run
build`) → `npm pack` ships the WHOLE `dist/` including `dist/gh-pages/` (the entire demo app) inside
the tarball. `files:["dist"]` is the exact mechanism: the allow-list names the whole dir, and the
demo build lives INSIDE it.

**Cure APPROACH** (E-3 — narrow the contract, not a post-pack rm hack): narrow the packed surface
to the library artefacts only — either `files:["dist/value.js","dist/value.cjs","dist/*.d.ts",…]`
(the flat W12 dist layout) excluding `dist/gh-pages/`, OR a `.npmignore` excluding `dist/gh-pages/`,
OR a `prepack` that cleans `dist/gh-pages/` before packing. The born-RED (G-CANON-6) asserts the
`npm pack --dry-run` manifest contains ZERO `dist/gh-pages/**` entry.

#### U-F64 — `size-gate-blind` · **retire/re-anchor (gate the chunk-graph total)**

**Evidence** (registry §17): CI gates `dist/value.js ≤ 145 KB` (`ci.yml:170`, `148480` bytes) but
the file is a **14.9 KB post-split facade** — the core bloat now lives in hashed shared chunks the
gate CANNOT see (the Rolldown codeSplitting layout). The gate is GREEN over a surface it does not
measure.

**Cure APPROACH** (E-3 — measure the real thing): re-anchor the size budget to the **chunk-graph
total** — the facade PLUS every reachable chunk (the tree-shakeable bundle-trace floor the
`subpath-budget` gate already knows how to walk). A real budget over the real total; the born-RED
(G-CANON-8) asserts the gate reads the graph total, not the facade byte-count. Re-anchor (the ≤145KB
facade assertion retires into the graph-total assertion).

#### U-F65 — `lint-vacuity` · **fold (owner call — real rule set OR types+review, recorded)**

**Evidence** (registry §17): `npm run lint` (`package.json:80` `eslint . --max-warnings=0`) is GREEN
over ~680 files but ~every rule is OFF — `eslint.config.js` enforces only the src/ glass-ui import
ban (breadth genuine, depth ~nil). CLAUDE.md's own §Conventions already states the disciplines
"stand by the type system + eslint + review."

**Cure APPROACH**: an **owner call**, recorded — either restore a real rule set (the idiomatic
"turn the linter back on" gestalt) OR formally ACCEPT the types+review posture (making the
near-empty lint an explicit, documented decision, not an accident). Folds into the build-tooling
coherence row; the decision is RECORDED (not silently left). If restore is chosen, the restored
rule-set is the deliverable; if accept, a one-line CLAUDE.md rationale ("lint = import-ban only;
depth by types+review") makes the vacuity honest.

#### U-F66 — `typecheck-stale-dist` · **fold (pretypecheck build OR documented dependence)**

**Evidence** (registry §17): `npm run typecheck` (`package.json:79`) resolves demo→value.js through
`dist/*.d.ts` with NO prebuild (no `pretypecheck` script verified) → it silently PASSES against
STALE published types; a src change to a value.js type is invisible to the demo typecheck until a
manual rebuild.

**Cure APPROACH** (E-3): add a `pretypecheck` build (fresh dist before the demo typecheck resolves)
— the idiomatic "typecheck the current tree" fix — OR document the build-state dependence explicitly
(a CLAUDE.md note: "typecheck resolves demo→value.js through dist; run `build` first"). Owner-call
recorded; the born-RED (G-CANON-10) is armed IF the pretypecheck path is chosen (assert typecheck
resolves fresh dist), else the documented-dependence note is the discharge. Folds into the
build-tooling coherence row.

#### U-F71 — `dev-toolchain-advisories` · **fold (batch dev-dep bump)**

**Evidence** (registry §17): the remaining `npm audit` hits (esbuild / js-yaml / vitest / vite) are
dev/build-only — NONE ship in dist (the `--omit=dev` production audit is the discriminant). A batch
dev-dep bump clears them.

**Cure APPROACH**: a single batched dev-dependency bump (idiomatic minor/patch bumps within range);
verified by `npm audit --omit=dev` clean (the production surface) + the dev audit reduced. Folds
into the build-tooling coherence row; a verification assertion (not a standing born-RED — dev-dep
advisories churn continuously, a fixed gate would false-red on the next disclosure).

### Cluster 4 — REPO-HYGIENE (the silent-drop traps + the tracked bloat)

#### U-F49 — `gitignore-auth-unanchored` · **build (anchor `/auth/` — a silent-drop guard)**

**Evidence** (registry §13, CONFIRMED): `.gitignore:8 auth/` is UNANCHORED → it matches the tracked
SOURCE dir `demo/@/composables/auth/` (4 live composables). They survive only by tracked-exemption;
any NEW or re-added file under that dir is silently ignored — a silent-drop trap (the same class the
whole tranche is built to close).

**Cure APPROACH** (E-3 — anchor to the intended target): change `.gitignore:8` `auth/` → `/auth/`
(anchor to the repo-root build-artefact `auth/` dir only, not any nested source `auth/`). The
born-RED (G-CANON-5) asserts every `demo/@/composables/auth/*.ts` source file is git-tracked (no
silent drop) AND the ignore pattern is anchored.

#### U-F24 — `dead-orphans` + `over-export-hygiene` · **retire (orphans) + RULE (over-export)**

**Evidence** (registry §5 + §10 CONFIRMED): 3-4 true orphans — the api `migrate-soft-delete.ts` (vs
the delete-after-run convention), `__resetServicesForTest` (verified live at
`api/src/platform/http/inject-services.ts:112`), the prng radii trio (`mulberry32` in `prng.ts` is
LIVE — the trio only); PLUS ~150 over-exported-but-live internals (candidate only — idiomatic type
aliases + testability seams).

**Cure APPROACH**: **retire the 3-4 orphans** (delete the dead files/exports — E-3 forbids leaving
dead code as "compat"); **RULE the over-export class leave-as-is with a NAMED rationale** (the ~150
are idiomatic type aliases + testability seams, not accidental leaks — the registry's own candidate
verdict; a one-line CLAUDE.md/FINAL.md rationale makes the leave-as-is an explicit decision, not an
un-audited gap). No gate on the over-export (RULED, not built); the orphan-delete is a verify-at-
execution done-state.

#### U-F50 — `tracked-binary-bloat` · **retire (git rm the heapsnapshot + a shot policy)**

**Evidence** (registry §13, CONFIRMED): 133 MB (84%) of the tracked tree is audit binaries; a **58
MB heapsnapshot** (N-tranche) is STILL tracked — verified live at
`docs/tranches/N/audit/lanes2/shots/L-PERF1-heap.heapsnapshot`; 156 tracked PNGs, 154 of which match
the repo's OWN `*.png` ignore (tracked-exemption bloat).

**Cure APPROACH**: `git rm` the heapsnapshot from the tip (history-rewrite is OUT of scope — a
tip-removal shrinks the working tree + future clones' checkout; the pack-history reduction is booked
as a separate owner-decidable if it matters); institute a **shot policy** — a born-RED-able guard
(G-CANON-7) asserting no tracked file exceeds a size threshold outside a named allowlist, so a 58 MB
binary can never re-enter tracked silently. The 154 tracked-exempt PNGs are ruled (audit evidence,
intentionally tracked) OR swept per the same policy at owner call.

#### U-F51 — `stale-local-branches` · **retire (delete the orphans)**

**Evidence** (registry §13, CONFIRMED): orphan local branches == master + `t-w5-motion-liquid`
(0-unique, merged) — live inventory shows the candidates (`modernize`, `post-refactor`,
`pre-refactor`, `t-w5-motion-liquid` and peers). **Verify-before-delete** at execution: only
branches that are 0-unique-vs-master (fully merged, no unique commit) are deleted; any branch with a
unique commit is PROTECTED (the count drifts — the registry's "3 orphan" is a round-time snapshot;
the DELETE-LAW is 0-unique-merged, applied live).

**Cure APPROACH**: `git branch -d` (safe-delete, refuses un-merged) each 0-unique branch after a
live `git branch --merged master` + `git cherry` proof. Retire — recorded; no gate (one-shot;
worktree-attached branches are excluded).

#### U-F52 — `scratch-accumulation` · **retire (sweep before U opens)**

**Evidence** (registry §13, CONFIRMED): `docs/tranches/T/audit/pi/` holds **1.8 GB** untracked
scratch (verified live: `du -sh` = 1.8G; w8/ the bulk); the mass is `*.png`-ignored (0.5 MB real
accidental-add exposure — the 7 untracked entries `git status --porcelain` surfaces). Note the
un-tracked scratch dirs already visible at branch-open: `docs/tranches/T/audit/pi/t60/` +
`docs/tranches/T/audit/pi/w8/easing/`.

**Cure APPROACH**: sweep the untracked scratch (a working-tree `rm` of the 1.8 GB pi/ scratch — it
is untracked, so no history touch); the 0.5 MB accidental-add exposure is triaged (any real doc
kept, the scratch removed). Retire — recorded; no gate (working-tree sweep, the `*.png` ignore
already guards re-accumulation of the bulk).

#### U-F53 — `worktree-prune-proof` · **retire (actionable — prune the 0-unique WITH the law's proof)**

**Evidence** (registry §13, actionable): the worktree set is live (verified: 12 worktrees at
branch-time, more than the registry's round-time 9 — the count DRIFTS as T.W8 runs); the registry's
"5 of 9 0-unique + clean, 4 protected" is a snapshot. The **PRUNE-LAW** is the invariant, not the
count: prune ONLY worktrees that are (0-unique-vs-their-base) AND (clean working tree) AND (not
OS-locked) AND (no live in-flight commit) — WITH the proof delivered (a `git worktree list` +
per-worktree `git status` + `git cherry` before each `git worktree remove`).

**Cure APPROACH**: at a SAFE moment (not while T.W8 lands into a worktree), `git worktree remove`
each worktree passing the four-clause prune-law, delivering the proof inline. Protected worktrees
(live W8 commits `e53-14/15`, uncommitted `e53-16/ec3-2`, OS-locked) are left. Retire (actionable) —
recorded; no standing gate (the prune is a moment-in-time action, the proof-law is the discipline).

#### U-F70 — `root-zod-orphan` · **retire (remove the unused root devDep)**

**Evidence** (registry §17, CONFIRMED): root devDep `zod ^3.23.8` (`package.json:152`) has 0 imports
in src/demo/test/e2e/scripts; all zod is api-side (its own `4.4.3`). A dead dependency.

**Cure APPROACH**: remove `zod` from the root `package.json` devDependencies + lock refresh. The
born-RED (G-CANON-9) asserts zod is absent from root deps (RED today: present + 0-import). Retire —
the api's own zod is untouched (a separate tree).

### Inherited W8-census residue — U-F19 (AB-1 half) · `about-katex-boot-scope` · build-if-CENSUS-RED

**Routed HERE, not a CANON-native family** (registry §4 U-F19; `U.W-VISUAL` census map L110 + §Scope
L308 + §BOOKS L456 all route it here): U-F19 is the fold family that absorbs the terminal-W8
live-defect residue BY NAME — its T-53 dark-caster MATERIAL half is U.W-VISUAL's row, and its **AB-1
About-KaTeX scope-dead half routes to U.W-CANON** (the boot-scope build seam), explicitly NOT
U.W-VISUAL's material row. AB-1 is an A-class ordinary bug (registry §4: "AB-1 About-KaTeX
scope-dead"), a build/boot-scope defect in the About page's KaTeX, not a taste bracket. It is
surfaced here so no successor mistakes it for a silent drop between the two waves that each disclaim
the other's half.

**Cure APPROACH** (build-if-CENSUS-RED — the honest inheritance, not a native CANON build): the
U.W-VISUAL W8-inheritance census re-judges at wave-open whether the landed-but-owner-uncertified W8
remediation (the AB-1..4 LAND rows, ROWS.md §4) left AB-1 RED. **If CENSUS-RED**, U.W-CANON owns the
About-KaTeX boot-scope build seam — a real fix at the boot/build-scope origin (E-3: correct the
scope-dead KaTeX path, never a suppression). **If CENSUS-GREEN** (the W8 LAND rows carried it), it
RETIRES into the census record. No standing gate (a one-shot build-if-RED; the census verdict is the
trigger). This closes the ledger's "T-53/AB-1 → VISUAL/CANON" routing (§C) — CANON now carries the
AB-1 half it is routed.

---

## §Hard gate (born-RED where the defect is LIVE — registry-cited; ZERO visual gates, no annex)

**Ten born-RED gates**, each ARMED now against a LIVE defect and flipping GREEN only at the cure.
This is a STRUCTURAL wave: every gate is a mechanical assertion (a `test:dist` check, a CI
assertion, a canon-sync script, a git-state proof) — **none is a visual/headless assertion**, so
none rides the U-F54 real-GPU annex (this wave has no visual surface). The two STANDING deliverables
(G-CANON-3 barrel-parity, G-CANON-8 size-graph) are wired into CI as regression guards; the rest
are one-cure flips.

### G-CANON-1 — U-F17 · the honest §4 Q4-WELL amendment · **born-RED (ledger-integrity)**

Assert U's ledger (the U.W-CANON canon-sync artefact + FINAL.md walk) carries the honest §4
amendment row that (a) NAMES the reversal locus (`ComponentSliders.vue:26 surface="veil"` = glass+
blur, via `ad301e7`), (b) records the ratified "opaque WELL / NO-blur" was superseded, (c) states
the lesson-15 re-ratification law. **RED today**: no such amendment exists; `T.md:391`/`:682` still
read "Q4 'The well.' STANDS". Flips GREEN when the amendment lands U-side + the `T.md §4` pointer is
BOOKED to T's close.

### G-CANON-2 — U-F21 + U-F69 · canon-sync regenerable-count gate · **born-RED**

A regenerable canon-check (the LoC-precept `grep|wc` pattern): assert documented counts ≡
tree-derived counts AND doc structure ≡ tree structure AND pin-prose ≡ pinned-version, across the
FULL T/FINAL §5:305 doc-rewrite remainder. **RED today** (four confirmed live lies + the two
T-named surfaces reconciled at execution): (a) root `CLAUDE.md` Structure ≠ tree (dead T.W1
paths); (b) `api/CLAUDE.md` Structure INVALID (all 9 paths gone; the `platform/` transposition);
(c) `README.md:11,66` "15 color spaces" ≠ 17 shipped; (d) `CLAUDE.md:154` parse-that "shipped
2.0.1" ≠ the pinned `^1.0.0`/1.0.0 (U-F69); (e) **`demo/CLAUDE.md`** structure + **`demo/DESIGN.md`**
facilities reconciled to the landed tree (the two T-named remainder surfaces the root+api framing
dropped — drift confirmed/cleared at execution; canon-sync targets either way). Flips GREEN when
every doc regenerates from the tree and the check passes.

### G-CANON-3 — U-F22 · barrel-parity gate · **born-RED (the crown STANDING tooling gate, CI-wired)**

A `test:dist` assertion: every subpath-barrel symbol ∈ (root barrel ∪ a rationale-annotated
exclusion allowlist), AND every root symbol of an owned domain appears in its subpath. **RED today**:
the 10-symbol bidirectional drift — `ICtCpColor`/`JzazbzColor` in root (`index.ts:92,93`) but absent
from `/color` (`subpaths/color.ts` count 0); `parseAnimationTimelineList`/`extractNamedTimelines`/
`NamedTimelineRegistry` root-only (`index.ts:377,383,392`); raytrace+slice-boundary in a subpath but
absent from root. Flips GREEN when the 10 are cured (ADDITIVE) + the gate passes. **Wired into CI**
as a standing regression guard (the deliverable is the gate, not the one-time sync).

### G-CANON-4 — U-F23 · GroundRecord single-source · **born-RED**

Assert the boot-time GroundRecord constants derive from the TS origin (build-time inline / codegen),
NOT hand-duplicated: the inline `VERSION`/`FIRST_VISIT` ≡ `ground.ts` `GROUND_RECORD_VERSION`/seed at
build. **RED today**: `demo/color-picker/index.html:164` `var VERSION = 1` is a hand-typed constant
independent of `ground.ts`; a version bump strands the boot script. Flips GREEN when the read is
single-sourced (a bump propagates automatically) and the born-RED (bump `ground.ts` → the boot read
tracks) passes.

### G-CANON-5 — U-F49 · gitignore `/auth/` anchor + silent-drop guard · **born-RED**

Assert `.gitignore` anchors the `auth/` ignore to `/auth/` (build-artefact root only) AND every
`demo/@/composables/auth/*.ts` source file is git-tracked. **RED today**: `.gitignore:8 auth/`
unanchored matches the tracked SOURCE dir; a new file there is silently ignored. Flips GREEN when
anchored to `/auth/` + the tracked-source assertion passes.

### G-CANON-6 — U-F63 · npm-pack ships no demo · **born-RED (pack-manifest gate)**

Assert `npm pack --dry-run` manifest contains ZERO `dist/gh-pages/**` entry (the library ships,
the demo does not). **RED today**: `files:["dist"]` + no `.npmignore` + no-op `prepare` → the whole
`dist/gh-pages/` (~4.3 MB) is in the tarball. Flips GREEN when `files` is narrowed / `.npmignore`
excludes gh-pages / `prepack` cleans it + the manifest assertion passes.

### G-CANON-7 — U-F50 · no tracked large binary + shot policy · **born-RED**

Assert no git-tracked file exceeds a size threshold outside a named allowlist (the shot policy).
**RED today**: `docs/tranches/N/audit/lanes2/shots/L-PERF1-heap.heapsnapshot` (58 MB) is tracked.
Flips GREEN when the heapsnapshot is `git rm`'d from the tip + the shot-policy guard passes (a 58 MB
binary can never silently re-enter tracked). History-rewrite is out-of-scope (tip-removal only).

### G-CANON-8 — U-F64 · size-gate re-anchored to the chunk-graph total · **born-RED (CI-wired)**

Assert the CI size budget reads the **chunk-graph total** (the facade `dist/value.js` PLUS every
reachable hashed chunk — the bundle-trace the `subpath-budget` gate walks), against a real budget.
**RED today**: `ci.yml:170` gates only `dist/value.js ≤ 145KB` — a 14.9 KB facade; the core bloat
in hashed chunks is invisible. Flips GREEN when the gate re-anchors to the graph total. **Wired into
CI** (replaces the facade byte-count assertion).

### G-CANON-9 — U-F70 · root zod orphan removed · **born-RED**

Assert `zod` is absent from the root `package.json` dependencies/devDependencies. **RED today**:
`package.json:152` `"zod": "^3.23.8"` present + 0 imports. Flips GREEN when removed + the lock
refreshes (the api's own zod untouched).

### G-CANON-10 — U-F66 · typecheck resolves fresh dist · **born-RED (conditional — owner-call gate/doc)**

IF the pretypecheck path is chosen (the registry's owner-decidable fold): assert `npm run typecheck`
resolves demo→value.js against a FRESH-built dist (a `pretypecheck` build ran), so a src type change
is visible to the demo typecheck. **RED today**: no `pretypecheck`; typecheck resolves stale
`dist/*.d.ts`. Flips GREEN when the pretypecheck build lands. IF the owner chooses the
documented-dependence path instead, this gate is discharged by the recorded CLAUDE.md note (the born-
RED is retired into the doc-decision — a fold, honestly recorded either way).

**No born-RED for**: U-F65 (lint — owner call, no gate until the rule-set/accept decision is taken)
· U-F71 (dev-dep advisories — a verification assertion `npm audit --omit=dev` clean, not a standing
gate; dev advisories churn) · U-F24 over-export (RULED leave-as-is, not built) · U-F51/F52/F53/F75
(retires — one-shot verify-at-execution done-states, no ongoing defect to guard once done). U-F24
orphan-delete is a verify-at-execution done-state.

---

## §π / DELTA obligations (no π-frame — ZERO visual claims; every family carries a mechanical DELTA)

**This wave has NO visual claim** — nothing renders, nothing is eyeballed, so there is no
before/after π-frame obligation and no real-GPU/owner-attested annex (that discipline is
U.W-VISUAL's, for the §2 still-reds; U-F17's material still-red is U-F10 THERE, not here). The one
inherited render-truth residue routed here — AB-1 (U-F19 About-KaTeX, §the Inherited W8-census
residue) — is a BUILD/boot-scope defect whose cure signature is DOM/SVG-STRUCTURAL, not perceptual;
its DELTA below is the mechanical render-truth (node count, SVG geometry — GPU-independent), and the
perceptual re-judge stays U.W-VISUAL's census. This closes the round-7 finding that AB-1 carried no
π-frame + no DELTA in EITHER wave. In lieu
of a π-frame, every family carries a **mechanical DELTA** — a before→after count/state, measurable
without a GPU:

| Family | DELTA measurement (before → after) | Evidence anchor |
|---|---|---|
| U-F17 | §4-amendment-rows: 0 → 1 (T.md "STANDS" superseded); reversal-locus named | `T.md:391,682` · `ComponentSliders.vue:26` |
| U-F19 (AB-1 half — build-if-CENSUS-RED) | About-KaTeX scope-dead build seam: doubled render layers 2 → 1 (`.katex-mathml` hidden, one layer paints); brace SVG height 6400px → 22px (card-clipping → contained). A DOM/SVG-geometry DELTA — GPU-INDEPENDENT (KaTeX is HTML/CSS; render-truth is structural, not a perceptual claim). RE-MEASURED only if the U.W-VISUAL census opens AB-1 RED (else RETIRES with the AB-1..4 LAND cite) | `Katex.vue` (global script import, `f0ef927`) · `audit/w8-certification/ROWS.md` AB-1 |
| U-F21+F69 | canon-sync-lies: 4 → 0 (root+api Structure, README 15→17, parse-that prose) + `demo/CLAUDE.md` + `demo/DESIGN.md` facilities reconciled (the T §5:305 remainder) | `README.md:11,66` · `CLAUDE.md:154` · `demo/CLAUDE.md` · `demo/DESIGN.md` |
| U-F22 | barrel-drift symbols: 10 → 0; parity-gate: absent → present (CI) | `index.ts:92,93,377,383,392` · `subpaths/color.ts` |
| U-F23 | hand-duplicated boot constants: 2 (`VERSION`,`FIRST_VISIT`) → 0 (single-sourced) | `index.html:160,164,169` |
| U-F49 | silent-drop trap: 1 unanchored `auth/` → 0 (`/auth/` anchored); tracked-source guard green | `.gitignore:8` |
| U-F50 | tracked-binary MB: 58 (heapsnapshot) → 0; shot-policy: absent → present | `…/L-PERF1-heap.heapsnapshot` |
| U-F51 | orphan branches (0-unique-merged): N → 0 (live-verified DELETE-LAW) | `git branch --merged` |
| U-F52 | untracked scratch: 1.8 GB → swept (0.5 MB exposure triaged) | `docs/tranches/T/audit/pi/` |
| U-F53 | prunable worktrees: (4-clause-passing set) → 0 (WITH proof); protected left | `git worktree list` |
| U-F63 | tarball demo payload: ~4.3 MB (`dist/gh-pages/`) → 0 | `package.json:67,77` |
| U-F64 | size-gate coverage: 14.9 KB facade → chunk-graph total (real budget) | `ci.yml:170` |
| U-F65 | lint depth: ~0 rules → (owner-call: real rule-set OR recorded accept) | `eslint.config.js` · `package.json:80` |
| U-F66 | typecheck freshness: stale-dist → fresh (pretypecheck) OR documented | `package.json:79` |
| U-F70 | root unused deps: 1 (zod) → 0 | `package.json:152` |
| U-F71 | dev-audit hits: N → reduced; production audit (`--omit=dev`) clean | `npm audit` |
| U-F75 | stale precept labels: 1 (palette→color) → 0 | `domains.md:25` |
| U-F24 | dead orphans: 3-4 → 0; over-export: RULED (named rationale) | `inject-services.ts:112` |

No claim in this wave is gated by a headless-visual assertion; there is nothing visual to gate.

---

## §Cross-repo RELAY (the E-2 owner edict — CHECKED, found NOT-triggered; the honest declaration)

The E-2 relay edict fires when a U wave "touches a glass-ui component or the glass-ui-level contract
(shared exports, token conventions, the mixColors/sampleColorRamp/color2 raw-channel convention, the
producer surfaces)." **U.W-CANON is checked against each clause and found entirely value.js-repo-
internal — NO RELAY row is triggered.** The check, honestly recorded (not a blanket "no cross-repo
ask"):

- **U-F17** records a glass-ui-veil REVERSAL (the WELL → `surface="veil"`) — but it changes NO
  glass-ui surface; it is a value.js-side LEDGER amendment (the material still-red "does the veil
  read as a veil?" is **U-F10's row in U.W-VISUAL**, which DOES carry a producer RELAY). U-F17 is
  truth-recording, not a component change → no relay.
- **U-F22** changes value.js's OWN barrel exports (the "shared exports" clause) — but the fix is
  **ADDITIVE ONLY** (adds missing symbols to the barrel that lacks them; removes/renames NO consumed
  symbol). glass-ui consumes `parseCSSColor`/`mixColors`/`sampleColorRamp`; keyframes consumes
  `parseCSSValue`/`color2`/`sampleColorRamp` — NONE of the 10 drifted symbols is on either consumed
  surface, and additive additions break no consumer. The "shared exports" clause is CHECKED and
  found not-triggered because the change is non-breaking-additive. (Were the parity fix ever
  SUBTRACTIVE, it would relay — it is not.)
- **U-F75** edits a PRECEPT (`docs/precepts/infra/domains.md`) — if the precepts live in a foreign
  submodule tree, the one-line label edit is path-scoped/relayed via the PRECEPT dev process under
  the same foreign-tree fence discipline (a precept-tree edit, not a glass-ui-component change) —
  noted for execution, not a glass-ui BH relay.

**No convention change, no producer surface, no consumed-export break ships from this wave** →
E-2 discharged by the CHECK (the honest declaration), not by a communiqué row. The U-formation BH
communiqué (`17e0f522`) already carries the cross-repo coupling that OTHER waves own; U.W-CANON adds
nothing to it.

---

## §Dispositions (each family → the exact build/fold/retire + rationale)

| Family | Disposition | Rationale |
|---|---|---|
| **U-F17** | **build** (the honest §4 amendment) | A ratified decision was reversed with no re-ratification row; U authors the honest §4 amendment U-side + BOOKS the T.md pointer (path-scope fence). The material still-red is U-F10 (U.W-VISUAL), not here. |
| **U-F21** | **build** (canon-sync) | Root+api+demo CLAUDE.md Structure predate the T.W1 reorg / are invalid; README 15≠17; `demo/DESIGN.md` facilities (the T/FINAL §5:305 remainder); regenerable-count precept applied, ONE gate (G-CANON-2). |
| **U-F22** | **build** (cure 10 + MINT the parity gate) | Bidirectional 10-symbol drift ships in dist `.d.ts`, no parity gate exists; the standing CI gate is the gestalt cure. ADDITIVE → no RELAY. |
| **U-F23** | **build** (single-source) | The only GroundRecord READ is hand-duplicated vanilla JS; a version bump silently strands boot; build-time inline / codegen single-sources it. |
| **U-F24** | **retire** orphans + **RULE** over-export | 3-4 true orphans deleted (E-3: no dead-code-as-compat); the ~150 over-exports RULED leave-as-is with a named rationale (idiomatic aliases + testability seams), not silently un-audited. |
| **U-F49** | **build** (`/auth/` anchor) | Unanchored `auth/` matches the tracked SOURCE dir — a silent-drop trap; anchor + a tracked-source guard (G-CANON-5). |
| **U-F50** | **retire** (git rm + shot policy) | 58 MB tracked heapsnapshot; tip-removal + a standing shot-policy guard (G-CANON-7) so it can't re-enter; history-rewrite out-of-scope. |
| **U-F51** | **retire** (delete orphans) | 0-unique-merged local branches; live DELETE-LAW (`git branch -d` after a merged-proof); one-shot, no gate. |
| **U-F52** | **retire** (sweep) | 1.8 GB untracked scratch; working-tree sweep, no history touch; the `*.png` ignore guards re-accumulation. |
| **U-F53** | **retire** (prune WITH proof) | The 4-clause prune-law applied at a safe moment WITH the proof delivered; protected worktrees left; the count drifts, the law is the invariant. |
| **U-F63** | **build** (pack the library) | `files:["dist"]` ships the whole `dist/gh-pages/` demo; narrow the packed surface + a pack-manifest gate (G-CANON-6). |
| **U-F64** | **retire/re-anchor** (chunk-graph) | The ≤145KB gate is a 14.9 KB facade; re-anchor to the chunk-graph total (G-CANON-8, CI-wired). |
| **U-F65** | **fold** (owner call, recorded) | Lint is import-ban-only over 680 files; owner call — restore a rule set OR record the types+review acceptance; recorded either way. |
| **U-F66** | **fold** (pretypecheck OR documented) | Typecheck resolves stale dist; add a `pretypecheck` build (G-CANON-10 conditional) OR document the build-state dependence; owner-call recorded. |
| **U-F69** | **build** (joins U-F21) | `CLAUDE.md:154` parse-that "shipped 2.0.1" is FALSE (pinned `^1.0.0`/1.0.0); canon-sync, folds into G-CANON-2. |
| **U-F70** | **retire** (remove zod) | Root devDep `zod` 0-import orphan; remove + lock refresh (G-CANON-9); api zod untouched. |
| **U-F71** | **fold** (batch dev-dep bump) | Dev/build-only advisories, none ship in dist; batch bump verified by `npm audit --omit=dev` clean; no standing gate (churn). |
| **U-F75** | **retire** (label fix) | `domains.md:25` names the app "palette"; the convention IS followed, only the label stale; one-line correction. |
| **U-F19 AB-1 residue** (inherited) | **build-if-CENSUS-RED** | The About-KaTeX boot-scope half of U-F19, routed here by the ledger §C + U.W-VISUAL's census (NOT U.W-VISUAL's material row); CANON owns the boot-scope build seam IF the census reds it, else retire-to-census. Closes the two-wave routing. |

Zero silent drops: all 18 CANON-native families reach a decided home (7 build · 3 fold · 8 retire),
plus the inherited U-F19 AB-1 residue-half decided build-if-CENSUS-RED.

---

## §Dependencies

- **Root** (U.md §The wave DAG — `dependsOn: root`). No upstream wave gates U.W-CANON; it opens at
  formation and runs in parallel with U.W-SEC / U.W-ORACLE / U.W-DEMO (all root-dependent).
- **T.W8 concurrent-landing coupling** (NOT a wave dependency — a branch-hygiene law): T.W8
  remediation lands commits on the shared `tranche-t` branch; every U.W-CANON commit **pull-rebases
  first** and is path-scoped to `docs/tranches/U/**` (planning) / to the CANON-owned source+config
  files (execution). The U-F52/F53 sweeps + the U-F51 branch deletes MUST NOT run while T.W8 is
  landing into a worktree/branch (the "safe moment" clause — U-F53's prune-law fourth clause).
- **U-F17 reads `T.md §4`** (a T-tranche doc) — a READ, not a dependency; the amendment lands U-side
  and BOOKS the T.md pointer to T's close (U never edits a foreign closed ledger).
- **No downstream wave depends on U.W-CANON** — it is a leaf feeding U.W-CLOSE's ledger walk only.

---

## §BOOKS (what rides to a later wave — by name)

- **The `T.md §4` pointer-line → T.W8 / T-close (U-F17).** U authors the honest §4 amendment U-side;
  the one-line `T.md §4` pointer ("superseded — see U-F17 amendment") is BOOKED to T's own close
  (T owns its ledger edit; U's path-scope forbids the foreign edit). Named, not dropped.
- **The pack-history reduction (U-F50) → owner-decidable.** This wave tip-removes the heapsnapshot +
  institutes the shot policy; the git-HISTORY rewrite (to reclaim the packed 58 MB from past commits)
  is OUT of scope here — booked as an owner-decidable follow-up if the clone-size matters (a
  history-rewrite is a coordinated force-push event, not a canon-hygiene row).
- **The lint rule-set decision (U-F65) → owner call at execution.** The fold is owner-decidable
  (restore vs accept); the DECISION rides to the execution moment (recorded there), not a successor
  tranche.
- **The over-export leave-as-is rationale (U-F24) → the FINAL.md ledger walk.** The RULED
  over-export class carries its named rationale into FINAL.md so no successor re-audits the ~150 as
  an open gap.
- **The barrel-parity + size-graph gates (U-F22/F64) → U.W-CLOSE re-probe.** The two STANDING
  CI-wired gates are re-probed live at close (the gate is GREEN and STILL guards) — booked to
  U.W-CLOSE's book re-probe, confirming the deliverable is a live guard, not a one-shot fix.
- **U-F71 dev-dep advisories → a rolling verification (not a standing gate).** The batch bump is
  verified at execution; future advisories are a rolling `npm audit` concern, not re-booked as a U
  family (dev-dep churn is not a tranche defect).
- **The chronic cross-repo spec-status 7-set → a rolling spec-status WATCH (no work).** CH-10 ·
  CH-13 · FN-7 · kf `resolveEasing` · R8-23 · R-5 · R-10 — handed by `T/FINAL.md §5` to "U.W-CANON /
  spec-status" (§2.3 records the drifts). All KEEP-BOOKED: **no fired trigger crosses value.js's
  consume-edge**; 3 spec drifts recorded — `random()` Safari 26.2-stable but still Safari-only ·
  scroll-timeline Firefox-behind, NOT Baseline · kf touched its easing surface but the touch is a
  kf-internal subpath refactor, not a value.js-consume decision. A rolling cross-repo spec-status
  concern (re-surfaces only if a trigger crosses the consume-edge) — NOT a U family, NOT a build
  row, NOT a producer RELAY (a watch, not an ask; §Cross-repo RELAY stays not-triggered). Recorded
  so no successor mistakes the 7-set for dropped.
