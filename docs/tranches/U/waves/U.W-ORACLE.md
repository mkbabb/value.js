# U.W-ORACLE — ORACLE / GATE / TEST INTEGRITY (CI-teeth · the feasibility-leg law · born-RED ownership · color test-hardening)

**Wave**: U.W-ORACLE · **designHeavy**: no (STRUCTURAL — precise gates, born-RED, mechanical rigor;
this wave hardens the INSTRUMENTS the other waves are measured by, so its own gates are
source-read/config-parse assertions, NOT the U-F54 real-GPU/owner annex — the ONE genuinely-visual
oracle it touches, the aurora, is DELIBERATELY re-hosted to that annex, never chased headless) ·
**Families**: U-F1 (build) · U-F6-oracle-half (build, via the U-F62 split) · U-F15 (fold) · U-F42
(escalate) · U-F43 (fold) · U-F44 (fold) · U-F55-CI-teeth-half (build) · U-F62 (fold, the split
executor) · U-F72 (build) · U-F73 (build).

**Opens after**: **root** (an independent STRUCTURAL wave — the CI-teeth promotion, the feasibility-
leg law, the born-RED cure-ownership register, the slow-build move, the test-decoupling, and the
color test-hardening are all root-independent and land without waiting on any richness wave). Three
FLIPS are booked to their owning waves, not held here: the O-16 cascade clobber + O-26 aurora
producer cures ride **U.W-ADOPT** / the landed communiqué (`17e0f522`); the O-26/O-3 headed-GPU annex
attestation rides **U.W-CLOSE** / the U-F54 owner-attested annex; the feasibility-leg law's VISUAL
π-frame (the near-black ramp the missing leg was blind to) rides **U.W-VISUAL** / U-F6-ramp
(cross-referenced, not duplicated). **Downstream**: ORACLE → **U.W-CLOSE** (the born-RED cure-
ownership register + the annex attestation + the CI-teeth soft-posture decision are close-ledger
inputs; U-F61 attested-flags the CI-TBT-red + born-RED-cure-ownership single-sourced claims).

**The spec of record is `../audit/registry.md`** (§1 U-F1 · §2/§16 U-F6-oracle + U-F62 the split · §3
U-F15 · §13 U-F42 the 3 armed `test.fail()` · §16 U-F55 the CI-teeth/a11y split · §19 U-F72/U-F73 ·
§20 the a11y-half MEASURED→PASSES-1.0-RETIRED · §21 CI-teeth sharpened "`page-load.spec.ts` is the
SOLE hard e2e — smoke + smoke-safari BOTH continue-on-error" + the born-RED-never-for-a-headless-
visual caveat · §26 the `W-oracle-integrity` wave-shape) · `U.md §The wave DAG` / §real-GPU annex ·
`DISPOSITION-LEDGER §A` (the ten U-Fxx rows) + `§B` (the born-RED-cure-ownership chronic). Where this
doc and the registry could diverge, **the registry wins**; above both, **the owner's verbatim wins**
(§13.5; the probe-parsimony edict; the born-RED law). Precedence: owner verbatim → registry → `U.md`
→ this wave doc.

**Mode**: PLANNING-ONLY at authorship — this doc is the SPEC. The named cures LAND in-wave when U
executes; nothing here edits source. Every U commit is path-scoped to `docs/tranches/U/**` and
pull-rebased first (T.W8/T.W9 land on the shared `tranche-t` branch concurrently).

**Status**: PENDING (root-independent — armable now). The four born-RED gates are ARMED against LIVE
reds the registry cites: G-ORACLE-1 (the soft/unrun slate), G-ORACLE-2 (the proxy-predicate oracles
without a feasibility leg), G-ORACLE-3 (the 3 un-owned `test.fail()` + the software-GL-unflippable
pair), G-ORACLE-4 (the build in the unit-suite `beforeAll`). The two color-hardening gates
(G-ORACLE-6/7) are ARMED-**GREEN** by honest necessity — the numeric core is SOUND (U-F60) and the
impl already passes the omitted Sharma pairs (§19), so a born-RED there would be a fabricated red,
the mirror-image of the O-14 proxy-green sin this wave exists to outlaw.

---

## §Goal criterion

The gate-and-infrastructure class the T campaign's green CI was blind to — *"a green CI is zero
evidence any visual oracle or built-bundle perf gate passed"* (`U.md §Mission`) — is brought to a
DECIDED terminal state so the tranche's own instruments can be trusted, with zero silent drop:

- the **oracle slate gains TEETH** — the visual-oracle + built-bundle-perf slate (the tranche-T core
  deliverable) is either promoted to a BLOCKING CI gate OR the soft posture is an EXPLICIT, ratified,
  cited owner decision — never the un-owned `continue-on-error` drift it is today (`ci.yml:334`/`:430`
  both soft; smoke-perf/reactivity/mobile/admin invoked by NO workflow — verified 0 matches across
  all three workflow files);
- **the feasibility-leg LAW is minted and generalized** — the O-14 proxy-predicate class (paint ≡
  token, but *never* token ≡ floor — the "S-disease recurring in T's own instrument" that shipped
  gate-GREEN over the near-black ramp wreck) is closed as a CLASS: every guard-constant oracle carries
  a FEASIBILITY LEG (its constant certified against the real-world referent, not merely against its
  own serialization). O-14's own leg already landed via WR-8/T.W8 (`o14-preview-truth.spec.ts:44-58`,
  `:264`) — the LAW's proof-of-concept; U OWNS the generalization + the mechanism that keeps a proxy
  predicate from ever again passing green over a wreck;
- **the born-RED tripwires are OWNED, not orphaned** — the 3 armed `test.fail()` at head (O-16
  `o16-computed-cascade.spec.ts:34`, O-26 `o26-aurora-perceptibility.spec.ts:57`, O-5
  `o5-boot-pacing.spec.ts:48`) are each mapped BY NAME to their cure wave, AND the software-GL-
  unflippable pair (O-26 + O-3's `test.skip`, `o3-headed-gpu-probe.spec.ts:44`) is re-hosted to the
  U-F54 headed-GPU/owner-attested annex OR explicitly acknowledged — an oracle whose cure-path is
  unmeasurable on its runner is mis-hosted (U-F15), and pretending a headless assertion verifies a
  GPU frame is the close-class lie the charter forbids;
- **the unit suite stops paying a build tax** — the full `npm run build` in the vitest `beforeAll`
  (`dts-published-surface.test.ts:23`, ~70–80% of unit-suite wall time for 2 string checks) moves to
  the post-build `test:dist` slate where the build already runs (`package.json:83`); the unit suite
  becomes build-free;
- **the color oracles prove what they assert** — the XYZ/Lab/OKLab suite (which validates almost
  entirely by ROUND-TRIP, blind to a shared-error class) gains an EXTERNAL-vector anchor for
  rgb2xyz/xyz2lab/xyz2oklab, and the CIEDE2000 suite completes from 14 of Sharma's 34 pairs to carry
  the omitted large-ΔE + achromatic-transition pairs — coverage against the classes a round-trip
  cannot see (the core is SOUND — this proves it, honestly, born-GREEN).

## §Completion criterion

- Every family (U-F1, U-F6-oracle, U-F15, U-F42, U-F43, U-F44, U-F55-CI, U-F62, U-F72, U-F73) reaches
  its decided disposition (§Dispositions); zero silent drops. The build rows land their cure
  (born-RED flips GREEN, or born-GREEN coverage lands); U-F42 is the DECIDED escalate (own-all-3 +
  annex-or-acknowledge, booked); U-F15/F43/F44/F62 fold into their named sibling rows.
- The four born-RED gates (§Hard gate) each flip only on the TRUE cure — G-ORACLE-1 on a blocking
  slate or a ratified soft-posture cite, G-ORACLE-2 on every guard-constant oracle carrying a
  feasibility leg, G-ORACLE-3 on the complete cure-ownership register + the annex decision, G-ORACLE-4
  on a build-free unit suite. The two born-GREEN coverage gates (G-ORACLE-6/7) land as ADDED coverage
  that passes on write — and the wave doc states, on the record, that they are born-GREEN because no
  defect exists to flip (the born-RED law's exception applied honestly).
- **The honesty discipline is doubled** (the wave's own theme): a gate is authored born-RED **iff** it
  guards a LIVE defect and is headless-verifiable; it is NOT authored born-RED over sound code (the
  U-F72/F73 fabricated-red sin) and it is NOT authored as a flip-on-headless gate for a GPU-only
  visual (the O-26 mis-hosting sin — U-F15). Both mirror the O-14 lesson: an oracle must certify the
  REFERENT, never a proxy.
- Each metric/coverage claim carries a machine-checkable DELTA (`§π/DELTA obligations`); the ONE
  visual-adjacent claim (the aurora, O-26/O-3) carries its π-frame at the U-F54 real-GPU/owner-attested
  frame ONLY, and the feasibility-leg law's VISUAL π-frame (the near-black ramp) is discharged in
  U.W-VISUAL (U-F6-ramp) — cross-referenced, not duplicated (the oracle-half is a predicate, not a
  pixel).
- The cross-repo RELAY row is discharged (O-16/O-26 producer cures are BI-acceptance constraints
  already dispatched in the formation communiqué `17e0f522` — cited BY NAME, no second book; E-2).
- The close obligations ride to U.W-CLOSE BY NAME (`§BOOKS`). PP-16 binds: gates-pass-goal-unmet
  closes `complete_with_misses` (the U-F42 escalate is the archetypal gates-armed / cure-wave-gated
  row — the O-16/O-26/O-5 flips are producer/adopt-gated).

---

## §Scope — the families this wave builds (each with its cure APPROACH; E-3 binds — no workaround, no legacy, no gate deletion)

Each cure axis with design latitude is a **BRACKET with both poles named** (the house convention);
the design loop picks inside the bracket against the owner's word and the source. E-3 binds
throughout: the CI-teeth cure is REAL teeth or a ratified soft-posture cite (never a silent
continue-on-error drift), the born-RED cure is OWNERSHIP + an honest annex (never deleting a
`test.fail()` to green CI), the color-hardening cure is coverage against EXTERNAL references (never
tuning a tolerance to pass), and the slow-build cure MOVES the build (never deletes the check).

### U-F1 + U-F55-CI-teeth-half — `ci-oracle-slate-nonblocking` / `ci-oracle-slate-no-teeth` · **build (give the slate teeth — or ratify the soft posture explicitly)**

**Evidence** (registry §1 U-F1 · §16 U-F55 "merges U-F1+U-F15+U-F42" · §20 the a11y-half RETIRED · §21
"page-load.spec.ts is the SOLE hard e2e gate — smoke + smoke-safari are BOTH continue-on-error"):
`page-load.spec.ts` is the SOLE hard e2e CI step (`ci.yml:326-327`, `npx playwright test --project=smoke
e2e/smoke/page-load.spec.ts`, the comment at `:324` "No continue-on-error → a page-load regression
reddens CI"). The full `smoke` project — which HOSTS the entire visual-oracle slate (o1..o26 under
`e2e/smoke/oracles/`) — runs `continue-on-error: true` (`ci.yml:333-336`, `:334`); `smoke-safari` is
soft too (`ci.yml:429-432`, `:430`); and `smoke-perf` (the built-bundle frame-budget project that
hosts O-5, `playwright.config.ts:225`), `smoke-reactivity` (`:194`), `smoke-mobile` (`:176`),
`smoke-admin` (`:164`) are invoked by NO workflow (VERIFIED: 0 matches for those four project names
across `ci.yml` + `deploy-pages.yml` + `release.yml`). Net: a green CI is ZERO evidence any visual
oracle or built-bundle perf gate passed — which defeats the tranche-T core deliverable (the oracle
slate). This is a deliberate "soft-launch" posture (`ci.yml:14`, the S.W0 flip) — but the posture was
ratified for a DIFFERENT reason (a soft launch of a known-red suite), and no owner has ruled that the
tranche's own oracle slate should stay toothless once its reds are cured. **Sharp contrast** (the
proof the posture is a drift, not a design): the CWV Lighthouse gate IS blocking (`ci.yml:507`,
"continue-on-error removed → the lighthouserc.json error-level gates") — the perf instrument has
teeth the VISUAL oracle instrument does not.

**Cure APPROACH** (E-3 — REAL teeth or an explicit ratified soft-posture cite; never leave the
continue-on-error un-owned). **The CI-teeth BRACKET**:
- **Pole A — promote the cured slate to BLOCKING**: the oracle specs whose reds are CURED (or whose
  born-RED is OWNED with a named flip, per G-ORACLE-3) move to a HARD CI step (drop `continue-on-error`
  on the cured subset), and `smoke-perf` (O-5's home) is WIRED into a CI job (it exists as a project but
  no workflow runs it). The still-red / GPU-only specs stay soft BY NAME with a cite (they are the
  U-F42 escalate / U-F54 annex), so "soft" means "a named-red-or-GPU-only annex", never "unwatched".
- **Pole B — an explicit ratified soft-posture**: the owner rules the whole oracle slate stays soft
  through U (a real launch decision), recorded as a cited ledger row with its rationale + a review
  date — so the soft posture is an OWNED decision, not a `continue-on-error` nobody re-decided.
Either pole makes the posture EXPLICIT + OWNED. The born-RED (G-ORACLE-1) is a CI-config assertion
(parse the workflow YAML) that RED today because the oracle/perf slate carries un-owned
`continue-on-error` + unrun projects; it flips when the slate is blocking OR the soft posture carries
a ratified cite. **U-F55's a11y-half is already RETIRED** (§20: lighthouse accessibility MEASURED →
PASSES 1.0, not a W9 ambush) — only the CI-teeth half lands here.

### U-F6-oracle-half — `proxy-predicate-oracle` / the FEASIBILITY-LEG LAW · **build (generalize the law + mint the mechanism); via the U-F62 split**

**Evidence** (registry §2 U-F6 "its oracle O-14 is a proxy predicate — paint ≡ token, never token ≡
floor — that was gate-green over the wreck, the S-disease recurring in T's own instrument; U owns the
oracle-class law: every guard-constant gets a feasibility leg" · §16 U-F62 the SPLIT: U-F6 = the Q5
ramp resolver (→ U.W-VISUAL, WR-8's cure) + the O-14 proxy oracle (→ here)). O-14's own confession is
in its header (`o14-preview-truth.spec.ts:22-24`: "the near-black wreck was invisible: the oracle
compared sites to tokens, never tokens to their floors"). The O-14-SPECIFIC feasibility leg ALREADY
LANDED via WR-8/T.W8 (`o14-preview-truth.spec.ts:44-58` the `T.W8 FEASIBILITY LEG` helper measuring
the ramp's effective stops against the composited surface + the O-18 census ambient; `:264` the
"O-14 · the ramp feasibility leg" describe) — that is the LAW's proof-of-concept, NOT the law. The
open work is the GENERALIZATION: every OTHER guard-constant oracle in the slate (an oracle that
asserts an internal serialization ≡ a paint, e.g. the O-19 netting-luma floors, the O-18 contrast
census, the O-21 gradient-rail span, the O-14 T-17 chip half) must carry a feasibility leg (its
constant certified against the real-world referent — a floor, a surface, a perceptual threshold —
not merely against its own output), so a proxy predicate can never again ship gate-green over a
wreck.

**Cure APPROACH** (E-3 — a CLASS law + a mechanism, not a per-oracle patch): U owns the LAW ("every
guard-constant gets a feasibility leg") + the AUDIT that enforces it. **The feasibility-leg
mechanism BRACKET**:
- **Pole A — a slate-wide meta-audit gate**: a test (or a lint over the oracle sources) that
  ENUMERATES the guard-constant oracles (those asserting output ≡ paint / constant ≡ serialization)
  and FAILS while any lacks a declared feasibility leg (a second assertion binding the constant to its
  real referent). The audit's registry is the machine-checkable form of the law; born-RED today (the
  non-O-14 guard-constant oracles are unaudited).
- **Pole B — a per-oracle feasibility-leg convention + a doc-law**: each guard-constant oracle carries
  a named `feasibility leg` block (the O-14 pattern generalized) enforced by a naming/structure
  convention + a canon-doc entry (the oracle-authoring precept), reviewed at authorship — teeth via
  the convention + review rather than a meta-test.
The design loop picks against the owner's word; either pole makes the LAW enforceable, not
aspirational. **This is the S-disease's structural cure** — the S/T recurring instrument-blindness is
closed as a class, not re-fixed per wave. Cross-reference: the VISUAL π-frame that proves the leg
CATCHES the wreck (the near-black ramp) is U.W-VISUAL/U-F6-ramp's obligation — not re-captured here
(the oracle-half is a predicate).

### U-F42 + U-F15 (fold) — `vacuous-ci-tripwire` + `o26-softwaregl-nonflip` · **escalate (own all 3 born-RED + a headed-GPU annex or acknowledge)**

**Evidence** (registry §13 U-F42 + §3 U-F15 fold + `DISPOSITION-LEDGER §B` the born-RED-cure-ownership
chronic): 3 armed `test.fail()` ship at head — O-16 (`o16-computed-cascade.spec.ts:34`, the dist
`:root` 150ms transition-default clobber, "PKT-1 producer-root", independently re-verified STILL
shipped at glass-ui dist `components.css:1`), O-26 (`o26-aurora-perceptibility.spec.ts:57`, the
aurora-perceptibility red, W2-5 cure un-landed), O-5 (`o5-boot-pacing.spec.ts:48`, the boot-pacing
spike, RP-2 → the adopt-gated W7 payload cut) — each a HONEST born-RED whose cure wave has NOT landed.
The mis-hosting (U-F15): on the software-GL CI runner O-26 can NEVER flip (SwiftShader forces the
static CSS placeholder → migration ≈ 0 forever) AND O-3's chroma assertion fully SKIPS
(`o3-headed-gpu-probe.spec.ts:44`, `test.skip(isSoftwareGL(...), "would certify the CSS placeholder,
not the live aurora")`) — so the aurora has NO executable gate on CI, and its born-RED is a tripwire
that can never be tripped GREEN on its own runner. An oracle whose cure-path is unmeasurable on its
runner is mis-hosted (U-F15).

**Cure APPROACH** (E-3 — OWNERSHIP + an honest annex; NEVER delete a `test.fail()` to green CI, which
would be the close-class lie): U OWNS all 3 born-RED as its escalate. **The ownership + annex
BRACKET**:
- **the OWNERSHIP register** (build): each of the 3 `test.fail()` is mapped BY NAME to its cure wave +
  its flip condition — O-16 → the glass-ui producer cascade cure (U.W-ADOPT / communiqué `17e0f522`,
  the dist `:root` clobber); O-5 → the RP-2 payload cut (U.W-ADOPT-gated, U.W-PERF's U-F3 escalate);
  O-26 → the aurora composition cure (producer, U.W-ADOPT / communiqué) — so no tripwire is orphaned
  and each carries the wave that removes its `test.fail()`.
- **the software-GL-unflippable pair — annex OR acknowledge** (the U-F15 fold): O-26 + O-3 are RE-HOSTED
  to the U-F54 headed-GPU/owner-attested annex (the charter §real-GPU annex) — they are NOT authored
  as flip-on-headless gates (they CAN'T flip headless; a headless assertion there is the exact §21
  caveat the born-RED law forbids). **Pole A — a headed-CI lane**: if a real-GPU headed lane is stood
  up, O-26/O-3 run there and their born-RED becomes a real gate on real hardware. **Pole B —
  acknowledge**: absent a headed lane, O-26/O-3 are the owner-attested annex (U-F54), explicitly
  recorded as "verified by owner attestation, not by CI" — not a pretend-headless gate.
The escalate is a DECISION (own + annex/acknowledge), satisfied independently of whether the producer
cures fire in-window. The born-RED gate (G-ORACLE-3) is the ownership register + the annex decision:
RED today (register absent, annex undecided), GREEN when both are complete. **U-F15 folds here** (it
is the O-26 mis-hosting, cured by the annex re-host); the O-16/O-26 producer FLIPS book to U.W-ADOPT,
the annex attestation to U.W-CLOSE.

### U-F43 — `slow-build-in-beforeAll` · **fold (move the build to `test:dist`)**

**Evidence** (registry §13 U-F42-adjacent, `DISPOSITION-LEDGER §A` U-F43): `dts-published-surface.test.ts:23`
runs a full `npm run build` in a vitest `beforeAll` (120s timeout) for TWO string checks
(`:30-38`: root `dist/index.d.ts` + `dist/subpaths/parsing.d.ts` each `.toContain("extractFunctions")`)
— ~70–80% of the unit-suite wall time spent building dist inside the UNIT suite. The check is a
dist-surface assertion; its home is the post-build slate.

**Cure APPROACH** (E-3 — MOVE the check, never delete it; the dts-surface guard is real and stays):
migrate the 2 string checks to the CI-wired `test:dist` slate (`package.json:83` — where
`npm run build` ALREADY runs as the slate's first step, so the check reads the fresh dist for FREE, no
second build). The vitest unit suite becomes build-FREE (no `execFileSync('npm',['run','build'])` in
any `beforeAll`). This FOLDS into the test-hygiene remediation (the same idiom as the Q13 retained-5
`test:dist` disposition). Born-RED (G-ORACLE-4): assert the unit suite invokes no build; RED today,
GREEN when moved. **DELTA-bearing**: the unit-suite wall-time drop is the measurable win.

### U-F44 — `impl-detail-coupled-tests` · **fold (decouple to the contract; snapshots are defensible)**

**Evidence** (registry §13, `DISPOSITION-LEDGER §A` U-F44): `test/units/value-unit.test.ts` couples to
the INTERNAL representation — `superType` array identity (`:75`, `:90-95` "does not share references
for superType array", `:133`, `:156`, `:168`) + `.value` nesting (`:73`, `:103`, `:115`, `:253`,
`:259`) — so a no-op representation refactor (e.g. changing how `superType` is stored) breaks the test
even though the PUBLIC contract (the parse/clone output) is unchanged.

**Cure APPROACH** (E-3 — decouple to the CONTRACT, not delete coverage): re-anchor the assertions to
the observable contract — the parse/serialize OUTPUT (the round-trip string, the public `toString`),
which IS the contract; **snapshots are defensible** here (the registry's own note: "parse output IS
the contract"). Where an internal invariant genuinely must be asserted (clone independence), assert
the OBSERVABLE consequence (mutating the clone does not mutate the original — a behavioral assertion),
not the array identity. **NOT born-RED** (this is a test-QUALITY refactor — a brittle-but-passing test
is not a LIVE behavioral defect; there is nothing red to flip). Folds into the test-hygiene
remediation alongside U-F43.

### U-F72 — `test-ground-truth-circularity` · **build (external-vector anchors; born-GREEN — the core is SOUND)**

**Evidence** (registry §19 U-F72 + §19 U-F60 verdict "the numeric color core is SOUND"): the XYZ/Lab/OKLab
suite validates almost entirely by ROUND-TRIP (forward ∘ inverse = identity — `color-roundtrip.test.ts`
`circularDistance` + `toBeCloseTo`; `color-conversions.test.ts:382` xyz2lab round-trip, `:447` xyz2oklab
round-trip) — which is BLIND to a shared-error class (an error present in both the forward and inverse
transform cancels in the round-trip and passes). The only external anchors are weak: `color-conversions.test.ts:364`
`rgb2xyz known values` asserts merely white Y ≈ 1 + black = 0 (2 coarse points), and xyz2lab/xyz2oklab
have ZERO external-vector anchor (VERIFIED: the only xyz2lab/xyz2oklab test uses are the round-trip
describes). The CORE is sound (U-F60: sRGB→XYZ matches the CSS Color 4 matrix to ≤ 1.11e-16, sRGB→OKLab
to ≤ 1e-6) — but the TEST cannot PROVE it.

**Cure APPROACH** (E-3 — an EXTERNAL reference anchor, never a self-referential round-trip): add an
external-vector anchor test for rgb2xyz / xyz2lab / xyz2oklab against a PUBLISHED reference (the CSS
Color 4 sample values / culori goldens / the Ottosson OKLab reference triples) — a non-round-trip
oracle that catches the shared-error class. **BORN-GREEN by honest necessity**: because the core is
SOUND (U-F60), the anchor PASSES on write — there is NO defect to flip, and authoring it born-RED would
be a FABRICATED red (the mirror-image of the O-14 proxy-green-over-a-wreck sin this wave exists to
outlaw). Its value is COVERAGE against the class the round-trip is blind to, recorded honestly as a
born-GREEN add. (This is the same feasibility-leg lesson at the unit layer: a test must certify the
REFERENT — here an external vector — not a proxy — here a round-trip.)

### U-F73 — `test-ground-truth-incomplete` · **build (complete the Sharma table; born-GREEN — the impl passes the omitted 20)**

**Evidence** (registry §19 U-F73): the CIEDE2000 suite carries 14 of Sharma, Wu & Dalal's 34 canonical
pairs (`color-difference.test.ts:8-22` — VERIFIED exactly 14 rows in the `SHARMA` table; asserted to
1e-3 at `:26-28`). The omitted 20 include the large-ΔE pairs (#17–20) + the achromatic-transition
pairs (#13, 16, 31, 32) — the classes that exercise the rotation term at extremes + the near-neutral
hue-wraparound; the IMPL PASSES them (§19: "the coverage, not the code, is short").

**Cure APPROACH** (E-3 — the canonical table completed, never a subset chosen to pass): extend the
`SHARMA` table from 14 → the omitted-20 subset (at minimum the large-ΔE #17–20 + achromatic-transition
#13, 16, 31, 32 the registry names; ideally the full 34 the canonical certification suite carries) at
the same 1e-3 tolerance. **BORN-GREEN by honest necessity**: the impl already passes them (§19), so
the added pairs pass on write — coverage completion, NOT a defect flip; authoring a born-RED here
would be a fabricated red. Recorded honestly as a born-GREEN coverage add.

---

## §Hard gate (born-RED where the defect is LIVE — registry-cited; NOT the U-F54 real-GPU annex except where explicitly re-hosted there)

Six gates. **Four are born-RED** (G-ORACLE-1..4 — each ARMED against a LIVE red the registry cites,
each headless-verifiable by source/config read). **Two are born-GREEN** (G-ORACLE-6/7 — coverage adds
over PROVEN-sound code; a born-RED there would be a fabricated red, the O-14 sin inverted). The ONE
visual oracle in scope (the aurora, O-26/O-3) is DELIBERATELY re-hosted to the U-F54 real-GPU/owner-
attested annex (G-ORACLE-3's annex arm) — NEVER authored as a flip-on-headless gate (the §21 caveat).

| # | Gate | Family | Born | LIVE evidence (the RED it guards) | Flips GREEN when |
|---|---|---|---|---|---|
| **G-ORACLE-1** | CI-config assertion (parse `.github/workflows/*.yml`): the oracle + built-bundle-perf slate carries NO un-owned `continue-on-error`, AND every playwright project is invoked by a workflow — OR the soft posture carries a ratified owner cite | U-F1 · U-F55-CI | **RED** | `ci.yml:334`/`:430` both `continue-on-error: true`; smoke-perf/reactivity/mobile/admin in 0 workflows (verified); only `page-load.spec.ts` HARD (`ci.yml:327`) | the cured slate is BLOCKING (Pole A) OR the owner ratifies a cited soft posture (Pole B) |
| **G-ORACLE-2** | the FEASIBILITY-LEG LAW: every guard-constant oracle (output ≡ paint / constant ≡ serialization) carries a feasibility leg (constant ≡ real referent) — enforced by the meta-audit (Pole A) or the convention+doc-law (Pole B) | U-F6-oracle (via U-F62) | **RED** | `o14-preview-truth.spec.ts:22-24` the proxy-predicate confession; O-14's own leg landed (WR-8), the CLASS unaudited (other guard-constant oracles lack legs) | every guard-constant oracle carries a declared feasibility leg |
| **G-ORACLE-3** | the born-RED cure-OWNERSHIP register complete (each `test.fail()` mapped by name to its cure wave + flip condition) AND the software-GL-unflippable pair annex-hosted-or-acknowledged (U-F54, NOT a flip-on-headless gate) | U-F42 · U-F15 (fold) | **RED** | 3 orphaned `test.fail()`: `o16-…:34`, `o26-…:57`, `o5-…:48`; O-26 can't flip on SwiftShader + O-3 `test.skip` on software-GL (`o3-…:44`) | the register is complete + the annex decision (headed lane OR acknowledge) is recorded |
| **G-ORACLE-4** | the vitest unit suite invokes NO `npm run build` (no `execFileSync('npm',['run','build'])` in any `beforeAll`); the dts-surface check runs under `test:dist` | U-F43 | **RED** | `dts-published-surface.test.ts:23` full build in `beforeAll` for 2 string checks (`:30-38`), ~70–80% of unit-suite wall time | the 2 checks move to `test:dist` (build already runs there); unit suite build-free |
| **G-ORACLE-6** | an EXTERNAL-vector anchor test for rgb2xyz / xyz2lab / xyz2oklab against a published reference (non-round-trip) | U-F72 | **GREEN** (honest) | round-trip-only validation (`color-roundtrip.test.ts`; `color-conversions.test.ts:382`/`:447`), no external anchor for xyz2lab/xyz2oklab — but the CORE is SOUND (U-F60), so it passes on write | it lands (coverage add; NO defect to flip — born-RED would be fabricated) |
| **G-ORACLE-7** | the `SHARMA` table completed with the omitted large-ΔE (#17–20) + achromatic-transition (#13,16,31,32) pairs at 1e-3 | U-F73 | **GREEN** (honest) | `color-difference.test.ts:8-22` carries 14 of 34; the impl PASSES the omitted 20 (§19) | it lands (coverage completion; impl already passes — born-RED would be fabricated) |

**U-F44 has no gate row** — it is a test-QUALITY decouple (brittle-but-passing → contract-anchored),
not a LIVE-defect flip; it lands as the test-hygiene fold beside U-F43, verified by the decoupled test
still asserting the contract (the round-trip/`toString` output) GREEN.

**The born-RED honesty law** (the wave's spine, restated): a gate is born-RED **iff** it guards a LIVE
defect AND is headless-verifiable. G-ORACLE-1..4 qualify (config/source-read reds). G-ORACLE-6/7 do
NOT — the code is sound, so they are born-GREEN coverage; fabricating a red there is the O-14 proxy-sin
inverted (a gate lying about a defect that isn't there). O-26/O-3 do NOT qualify as headless gates —
they are GPU-only, re-hosted to the U-F54 annex (G-ORACLE-3), never a pretend-headless flip (the §21
caveat). This double discipline — no fabricated red, no pretend-headless flip — IS the oracle-integrity
this wave delivers.

---

## §π/DELTA obligations (every VISUAL claim carries a before/after π-frame + a DELTA; the structural gates are DELTA-only)

This is a STRUCTURAL wave — its gates are config/source-read assertions, so MOST carry a DELTA only
(no π-frame; there is no pixel to capture — the same posture U.W-PERF took for its pure-instrument
U-F14). The ONE visual-adjacent claim (the aurora, O-26/O-3) carries its π-frame at the U-F54 real-GPU
/ owner-attested frame ONLY — NEVER a headless capture (that is the whole point of U-F15/U-F42). The
feasibility-leg law's visual manifestation (the near-black ramp the missing leg was blind to) is
U.W-VISUAL/U-F6-ramp's π-frame — cross-referenced, not duplicated here.

| Family / gate | π-frame (before → after) | DELTA (machine-checkable) |
|---|---|---|
| **G-ORACLE-1** CI-teeth | — (no visual — CI config) | un-owned `continue-on-error` oracle/perf steps + unrun projects (RED: `smoke`+`smoke-safari` soft, 4 projects unrun → GREEN: 0 un-owned soft / 0 unrun, OR a ratified soft-posture cite present) |
| **G-ORACLE-2** feasibility-leg law | the VISUAL proof (the near-black ramp the leg catches) → **U.W-VISUAL / U-F6-ramp π-frame** (cross-ref, not duplicated — the oracle-half is a predicate) | guard-constant oracles WITHOUT a declared feasibility leg (RED: N → GREEN: 0) |
| **G-ORACLE-3** born-RED ownership + annex | the aurora migration (O-26) / the live aurora vs CSS placeholder (O-3) → **the U-F54 real-GPU / owner-attested frame ONLY** (NEVER a headless assertion — §21) | orphaned `test.fail()` (RED: 3 unmapped → GREEN: 3 mapped by name); software-GL-unflippable oracles undecided (RED: 2 → GREEN: annex-hosted-or-acknowledged) |
| **G-ORACLE-4** slow-build move | — (no visual — test-runner topology) | unit-suite wall time (RED: full build in `beforeAll`, ~70–80% of wall time → GREEN: build-free; the ~70–80% reduction is the DELTA); build calls in the unit suite (RED: 1 → GREEN: 0) |
| **G-ORACLE-6** external anchors | — (no visual — numeric coverage) | external-vector anchors for rgb2xyz/xyz2lab/xyz2oklab (before: 0 real / weak white+black → after: 3 published-reference anchors); born-GREEN |
| **G-ORACLE-7** Sharma completion | — (no visual — numeric coverage) | SHARMA pairs (before: 14/34 → after: the omitted large-ΔE + achromatic subset present, ≥ the #17–20/#13,16,31,32 the registry names); born-GREEN |

**Obligation law**: a family with a VISUAL manifestation may not claim GREEN without its after π-frame
— but the ONLY visual here (O-26/O-3) is GPU-only, so its π-frame is the U-F54 owner-attested/real-GPU
frame, and the feasibility-leg visual is discharged in U.W-VISUAL. The structural gates carry the DELTA
alone (the born-RED flip / the coverage add is the DELTA's machine-checkable half). No headless π-frame
is fabricated for the aurora (the §21 caveat is a HARD line, not a preference).

---

## §Cross-repo RELAY (the E-2 owner edict — the O-16 / O-26 producer cures; already dispatched)

Two of the born-RED tripwires this wave OWNS flip only on a GLASS-UI PRODUCER cure, so U.W-ORACLE
carries a RELAY row per E-2 (*"All component level and glass-ui level changes must be communicated to
them directly, at the root, a fond"*). **These are ALREADY DISPATCHED in the formation communiqué —
cited BY NAME, no second book** (the relay half of §28.3 is REAL at glass-ui HEAD `17e0f522`):

| Producer surface | The ORACLE-wave dependency | Status |
|---|---|---|
| **the dist `:root` 150ms transition-default clobber** (O-16, glass-ui dist `components.css:1`) | O-16's `test.fail()` (`o16-computed-cascade.spec.ts:34`) flips ONLY when the producer cascade default clears — the PKT-1 producer-root cure | **ALREADY DISPATCHED** — communiqué §2b (the carried-forward T producer reds); rides U.W-ADOPT's BI-acceptance; cite by name |
| **the aurora composition** (O-26 perceptibility, glass-ui aurora shader) | O-26's `test.fail()` (`o26-…:57`) flips ONLY when the producer aurora migrates unmistakably — AND is measurable only on a real GPU (the U-F54 annex, NOT headless) | **ALREADY DISPATCHED** — communiqué §2b; the flip is producer + annex-gated; cite by name |

**Relay discipline** (M1 ruling): the value.js-side record (this wave's ownership register + the
G-ORACLE-3 annex decision) IS the gate; an ack is a bonus, never waited on. **NO NEW addendum is
authored by THIS wave** — the O-16/O-26 producer cures are already in the landed communiqué
(`17e0f522` §2b); U.W-ORACLE cites them by name and books the in-window FLIP to U.W-ADOPT. The
O-5/RP-2 payload cut is U.W-PERF/U.W-ADOPT's producer lever (already relayed there), not re-booked
here. **Planning-only note**: this wave doc AUTHORS the RELAY-row SPEC (what the tripwires depend on);
no source or foreign-tree edit lands from this campaign.

---

## §Dispositions (each family → the exact build/fold/retire/escalate + rationale)

| Family | Disposition | Rationale |
|---|---|---|
| **U-F1** ci-oracle-slate-nonblocking | **build** | `page-load.spec.ts` is the SOLE hard e2e; the visual-oracle slate runs soft (`ci.yml:334`) + 4 projects unrun. Give the cured slate teeth (Pole A) OR ratify + cite the soft posture (Pole B) — never an un-owned `continue-on-error`. Born-RED: the CI-config assertion (G-ORACLE-1). Merges with U-F55-CI. |
| **U-F55-CI-teeth-half** ci-oracle-slate-no-teeth | **build** (a11y-half RETIRED) | The a11y-half is RETIRED (§20: lighthouse a11y MEASURED → PASSES 1.0, not a W9 ambush). The CI-teeth half is the same mechanism as U-F1 (§16 "merges U-F1+U-F15+U-F42") → G-ORACLE-1. |
| **U-F6-oracle-half** proxy-predicate-oracle | **build** (via U-F62 split) | O-14 was a proxy predicate (paint ≡ token, never token ≡ floor) that shipped gate-green over the wreck; its own leg landed via WR-8. U owns the CLASS LAW ("every guard-constant gets a feasibility leg") + the mechanism (meta-audit OR convention+doc-law) — the S-disease's structural cure. Born-RED: G-ORACLE-2. |
| **U-F62** families-that-are-two-mechanisms | **fold** (the split executor) | Registry-hygiene: U-F6 = the Q5 ramp resolver (→ U.W-VISUAL, WR-8's cure) + the O-14 proxy oracle (→ here). The split is EXECUTED at those two rows; U-F62 is the bookkeeping that records it — no independent build. |
| **U-F15** o26-softwaregl-nonflip | **fold** (into U-F42's escalate) | An oracle whose cure-path is unmeasurable on its runner is MIS-HOSTED: O-26 can never flip on SwiftShader + O-3 SKIPS on software-GL. Cured by the G-ORACLE-3 annex re-host (O-26/O-3 → the U-F54 headed-GPU/owner-attested annex, not a flip-on-headless gate). |
| **U-F42** vacuous-ci-tripwire | **escalate** | The 3 armed `test.fail()` (O-16/O-26/O-5) whose cure waves have not landed; O-26 can never flip on software-GL. Own all 3 born-RED (map each to its cure wave by name) + wire a headed-GPU annex or acknowledge (the U-F15 fold). A DECISION, satisfied independently of whether the producer cures fire in-window. Booked. |
| **U-F43** slow-build-in-beforeAll | **fold** (test-hygiene) | `dts-published-surface.test.ts:23` runs a full build in `beforeAll` for 2 string checks (~70–80% of unit-suite wall time). MOVE the 2 checks to `test:dist` (build already runs there); unit suite build-free. Born-RED: G-ORACLE-4. Never delete the dts-surface guard. |
| **U-F44** impl-detail-coupled-tests | **fold** (test-hygiene) | `value-unit.test.ts` couples to internal `superType` identity + `.value` nesting (breaks on a no-op rep refactor). Decouple to the CONTRACT (parse/serialize output; snapshots defensible) + assert observable clone-independence. NOT born-RED (test-quality, no behavioral defect). Folds beside U-F43. |
| **U-F72** test-ground-truth-circularity | **build** (born-GREEN coverage) | The XYZ/Lab/OKLab suite validates by round-trip (blind to a shared-error class); add an external-vector anchor for rgb2xyz/xyz2lab/xyz2oklab. BORN-GREEN — the core is SOUND (U-F60), so it passes on write; a born-RED would be a fabricated red (the O-14 sin inverted). G-ORACLE-6. |
| **U-F73** test-ground-truth-incomplete | **build** (born-GREEN coverage) | The CIEDE2000 suite carries 14 of Sharma's 34 pairs; add the omitted large-ΔE (#17–20) + achromatic-transition (#13,16,31,32) pairs. BORN-GREEN — the impl passes them (§19); a born-RED would be fabricated. G-ORACLE-7. |

Zero silent drops: F1/F55-CI (build, G-ORACLE-1), F6-oracle (build, G-ORACLE-2, via F62's split),
F62 (fold, the split executor), F15 (fold into F42's annex), F42 (escalate, own-3 + annex/acknowledge),
F43/F44 (fold, test-hygiene), F72/F73 (build, born-GREEN coverage) each reach a decided home. No
family folds away without a named target.

---

## §Dependencies

- **opens-after: root** — an independent STRUCTURAL wave. The CI-teeth promotion, the feasibility-leg
  law + mechanism, the born-RED cure-ownership register, the slow-build move, the test-decoupling, and
  the color test-hardening are all root-independent (source/config edits + test adds), landable
  without waiting on any richness wave.
- **Three FLIPS are booked, not held** (the escalate discipline): the O-16 cascade clobber + the O-26
  aurora producer cures are GLASS-UI PRODUCER work → ride **U.W-ADOPT** / the landed communiqué
  (`17e0f522` §2b); the O-5/RP-2 boot-pacing flip rides **U.W-PERF/U.W-ADOPT** (the adopt-gated payload
  cut, U-F3). U.W-ORACLE OWNS the tripwires + the register; it does NOT itself cure the producer
  defects (E-3: no consumer workaround for a producer root).
- **The U-F54 real-GPU/owner-attested annex** (charter §real-GPU annex) — the G-ORACLE-3 annex arm
  re-hosts O-26/O-3 there. Its ATTESTATION is owner-held and rides **U.W-CLOSE** (the annex is
  owner-attested at close, per U-F54 + U-F61 attested-flags). U.W-ORACLE authors the re-host decision;
  it does not attest the frame (planning-only + headless-can't).
- **The feasibility-leg law's VISUAL π-frame** rides **U.W-VISUAL / U-F6-ramp** (the near-black ramp
  the leg catches) — cross-referenced from G-ORACLE-2, not captured here (the oracle-half is a
  predicate).
- **The CI-wired `test:dist` slate** (`package.json:83`, the Q13 retained-5) — U-F43's moved dts-surface
  check + any born-GREEN color anchors that belong to dist ride the standing slate; the unit suite
  (`package.json:81`) sheds the build.
- **Downstream: ORACLE → U.W-CLOSE** — the born-RED cure-ownership register, the annex attestation
  decision, and the CI-teeth soft-posture ruling are close-ledger inputs (U-F61 attested-flags the CI
  TBT-red + the born-RED-cure-ownership single-sourced claims).

---

## §BOOKS (what rides to a later wave — by name, no silent drop)

- **The O-16 cascade clobber + O-26 aurora producer cures → U.W-ADOPT / the landed communiqué
  (`17e0f522` §2b).** The producer roots that flip O-16/O-26's `test.fail()` unblock at the adopt;
  already dispatched — cited by name, not re-booked. U.W-ORACLE owns the tripwires; ADOPT lands the
  producer cure that removes the `test.fail()`.
- **The O-5 / RP-2 boot-pacing flip → U.W-PERF / U.W-ADOPT.** The boot-pacing spike (`o5-…:48`) flips
  on the adopt-gated RP-2 payload cut (U.W-PERF's U-F3 escalate); U.W-ORACLE owns the tripwire, the
  flip rides the payload cut. Not re-booked.
- **The O-26 / O-3 headed-GPU annex attestation → U.W-CLOSE / U-F54.** The G-ORACLE-3 annex arm
  re-hosts O-26/O-3 to the owner-attested/real-GPU annex; the ATTESTATION is owner-held at close
  (U-F54, U-F61 attested-flags). U.W-ORACLE records the re-host decision; CLOSE attests the frame.
- **The feasibility-leg law's VISUAL π-frame → U.W-VISUAL / U-F6-ramp.** The near-black ramp the leg
  catches is VISUAL's obligation (real-GPU/owner-attested); G-ORACLE-2 cross-references it, no
  duplicate capture.
- **The CI-teeth soft-posture decision → U.W-CLOSE.** If the owner rules the soft posture stands
  (Pole B), it is a ratified, cited ledger row; U-F61 attested-flags the CI-TBT-red single-sourced
  claim the same instrument understates. The close ledger records the DECISION (blocking OR ratified-
  soft), never an un-owned drift.
- **The moved dts-surface check → CI-wired `test:dist`.** U-F43's 2 string checks join the standing
  post-build slate; the unit suite's build-free state is the standing signal (G-ORACLE-4).
- **boot-G — the o12 boot-time backing-ratio slate-bounds decision (the T triumvirate leg) → this
  wave's oracle-slate BOUNDS-EXPANSION row.** `ROWS.md:72` (pass-2 Row G) routed the o12 boot-time
  backing-ratio leg to the triumvirate record ("would make the R2 regression class visible to the
  slate"; bounds-expansion; W9 inherits the minted-leg-or-the-declined-note). The census
  (`DISPOSITION-LEDGER §C.1`) found it un-homed in U. DECISION: **MINT** the o12 backing-ratio leg
  (arm the R2 boot-regression class as a slate-visible born-RED/GREEN gate) OR record the
  **declined-note** with rationale — parallels U-F1 (ci-oracle-slate) + the U-F6-oracle-half
  feasibility-leg law (an oracle must certify the FLOOR, not a proxy). A triumvirate-minted leg is an
  oracle-hardening act decided in THIS wave; the minted-or-declined outcome rides U.W-CLOSE's ledger
  walk. Not re-booked, not silently dropped.

---

**Precedence chain** (restated): the owner's verbatim (§13.5 + the probe-parsimony edict + the
born-RED law) → `audit/registry.md §1/§2/§3/§13/§16/§19/§20/§21/§26` → `U.md` (charter §real-GPU annex
+ §Mission gate-and-infrastructure class) → this wave doc. Downstream never overrides upstream.
ORACLE's four born-RED gates are config/source-read reds (headless-verifiable); its two color-hardening
gates are born-GREEN over PROVEN-sound code (no fabricated red); its ONE visual oracle (the aurora) is
the U-F54 owner-attested annex, NEVER a pretend-headless flip. Both disciplines — no fabricated red, no
pretend-headless flip — ARE the oracle-integrity this wave delivers.
